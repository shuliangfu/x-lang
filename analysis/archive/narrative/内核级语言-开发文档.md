# Shux 内核级语言 — 开发文档（按序实施清单）

> 配套 [`内核级语言.md`](内核级语言.md)（设计/缺口/测试分析）与本开发文档的关系:那份是「为什么、做什么、怎么测」,本文件是「按什么顺序做、做到什么算完」。
> 标记:**✅ 已完成** / ☐ 待办。顺序即实施顺序;后一项尽量以前一项出口为前提。
> 测试层:T1=纯 host 侧(最快)、T2=QEMU 本地(需装 QEMU,非外部硬件)、T3=真机(仅最终验证)。
> 更新时间:2026-07-03(全部完成 — 120 项 ✅ / 30 QEMU 内核 gate / 83 总 gate / 含 asm goto + bitfield + AtomicU32 + intrinsics + multiboot2 + x86_64 长模式 + UEFI + IST + bss_clear)。

---

## 0. 已就绪底座(✅ 全部完成 — 内核工具链地基)

| 状态 | 项 | 说明 |
|------|----|----|
| ✅ | 编译器自举 + 原生后端(出 `.o`/ELF/Mach-O/COFF) | D+E+F v1 闭合;充当内核工具链底座 |
| ✅ | `-freestanding` / 零 libc / syscall 走 `extern` 链 `.s` | 裸机可跑(但缺内联汇编,见 K1) |
| ✅ | 整数→指针强转 `0xB8000 as *u8` | MMIO 强转基本可用 |
| ✅ | `packed` struct / `align(N)` 字段前缀 / `repr(C)` | GDT/IDT/硬件描述符可直接映射 |
| ✅ | 条件编译 `#[cfg]` + `-target` triple 交叉 | `x86_64-unknown-none` 可走 |
| ✅ | 多架构编码(x86_64 / arm64 / riscv64) | 内核可多架构起步 |
| ✅ | 控制流 rel32(PC 相对,`R_X86_64_PC32`) | `jmp/call` 跨段天然可重定位 |
| ✅ | `core.mem` 运行时 volatile_load/store | 临时底层(K2 升级为语言级前的占位) |
| ✅ | `core` 12 模块(无 OS 依赖) | 内核可仅依赖 core |
| ✅ | 诊断体系(编号/JSON/did-you-mean) | 内核 panic/错误信息底座 |

> ⚠️ 已就绪但有硬伤:`core.mem.volatile_*` 是普通函数调用,会被 DCE 消除 MMIO 探测读 → 必须 K2 升级为语言级。

---

## M1 硬件直通(第一波 — 让 shux 能在 QEMU 点字)

| 状态 | ID | 项 | 范围 | 测试层 | 出口标志 |
|------|----|----|----|--------|--------|
| ✅ | K1 | **内联汇编 `asm!`** | lexer/parser/typeck/codegen;模板 + in/out/clobber 约束 + options;**必须在 `unsafe{}`** | T1(parse/typeck)+ T2(执行) | `asm!("out %1,%0"... )` 写串口、`mov %cr3` 读出、`hlt` 不死机,QEMU 显字 |
|   |   | └ K1a 模板版(C 前端) | `asm!("template")` parse→typeck(强制 unsafe)→C codegen `__asm__("...")`;新增 `AST_EXPR_ASM`/`asm_tmpl` union | T1 | ✅ `asm!("nop")` 经 shux-c -E 出 `__asm__("nop")`,host cc 编译运行 rc=0;unsafe 外报 `asm! requires unsafe block`;多指令模板转义正确;无回归 |
|   |   | └ K1b 操作数 in/out/clobber(C 前端) | `asm!("t" : "=r"(out) : "r"(in) : "memory","cc")`;`ASTAsmOperand` outputs/inputs + clobbers;typeck 递归 typeck 操作数 + output 须左值;codegen 发 GCC extended asm;**修了 DCE 不递归 unsafe region 的旧 bug**(collect_var_names_from_block 加 regions 递归) | T1 | ✅ `asm!("mov %1,%0" : "=r"(b) : "r"(a))` 经 shux-c -E 出 `__asm__("mov %1, %0" : "=r"(b) : "=r"(a))`,host cc 编译运行 b=a=42(rc=42);clobber `"memory","cc"` 通过;非左值 output 报 `asm! output operand must be an lvalue`;无回归 |
|   |   | └ K1c options(pure/nostack/preserves_flags/noreturn/volatile)(C 前端) | 第四段 `:` 后裸标识符 options 列表;`asm_tmpl.options` 数组;**默认 `__asm__ __volatile__`**(内核 MMIO 安全,不可消除);`options(pure)` 改 `__asm__`(允许消除/重排);其余 option 解析并存储、留 asm 后端用;修了 clobber 段 while 不停在 COLON 导致第四段被误读为 clobber 的 bug | T1 | ✅ 默认 `__asm__ __volatile__("mov %1,%0" : "=r"(x) : "r"(7))` 运行 x=7;`options(pure)` 出 `__asm__(...)` 运行 x=7;多选项 `nostack, preserves_flags` 解析存储、仍按默认 volatile 运行 x=9;clobber-only 仍 `__asm__ __volatile__("nop" :  :  : "memory","cc")`;无回归 |
| ✅ | L8 | **`asm!` 操作数补全** | `sym` 符号操作数、`label` 跳转操作数、clobber memory vs reg、`nomem/readonly/pure` 语义 | T1+T2 | `sym(idt)` 引用 IDT 地址、asm 内 `label:` 跳转可用 |
|   |   | └ L8a `sym` 符号操作数(C 前端 + QEMU) | `asm!("mov %1, %0" : "=r"(val) : "sym"(idt_base))` — `sym` 约束让编译器直接引用符号(C 编译器解析,不用模板 `$` 转义);现有 parser/codegen 已透传约束字符串,无需改动;zig cc 交叉编译支持 `sym`,host macOS clang 不支持(仅内核用) | T1+T2(QEMU) | ✅ asm_sym.x gate: `S:589824`(sym 加载 idt_base 值=0x90000);zig cc 交叉编译通过;无回归;**`label`(asm goto)待后续**(需新增 `asm goto!` 语法+parser 改动) |
| ✅ | K2 | **语言级 `volatile`** | typeck 识别 `*volatile T`;codegen 标记不 DCE/不 GVN/不 autovec、emit 真实 load/store | T1(查产物未被消除)+ T2(VGA/串口) | `*volatile` 改 VGA 字符 QEMU 显字;编译产物确认 volatile 读未被删 |
|   |   | └ K2a `*volatile T` 类型 + 读不被 DCE(C 前端) | `ASTType.is_volatile`;parse `*volatile T`;C codegen 发 `volatile T *`(声明/转型/deref);`let_init_has_side_effects` 把 volatile 解引用读判为副作用(不被块内 DCE 消除) | T1 | ✅ `let mmio:*volatile u32 = 0xB8000 as *volatile u32` 出 `volatile uint32_t * mmio = (volatile uint32_t*)753664`;未用的 `let v=*mmio` 出 `(void)((*(mmio)));`(MMIO 探测读保留、未被 DCE);C 编译通过;无回归 |
|   |   | └ K2b 解引用赋值 `*ptr = val` / `*ptr += val`(C 前端) | `expr_is_lvalue` 加 `AST_EXPR_DEREF`(原仅 VAR/INDEX/FIELD_ACCESS,致 `*p=1` 被 parse 当 final_expr 而 `=` 残留→报 `expected '}'`);补后 `*p=val` 与 `*p+=val` 走 ASSIGN/复合赋值;配合 K2a 实现 volatile 写 | T1 | ✅ `*p=42`(`p=&x`)出 `((*(p)) = (42))` 运行 x=42;`*p+=10` 运行 x=15;`*mmio=0x0720` 出 `((*(mmio)) = (1824))`(volatile 写);C 编译通过;无回归 |

> **K2 进度(2026-07-02)**:K2a(`*volatile T` 类型 + 读不被 DCE,C 前端)已落地并 T1 验证——`ASTType.is_volatile`;`*volatile T` parse;C codegen 发 `volatile T*`(声明/转型/deref);`let_init_has_side_effects` 把 volatile 解引用读判为副作用,**MMIO 探测读不再被块内 DCE 消除**(K2 硬伤修复)。未用的 `let v=*mmio` 出 `(void)((*(mmio)));`、保留读;C 编译通过。
>
> **K2 仍待办**:K2c asm 后端 / `.x` 前端移植(须 bstrict 重编,macOS 无法限内存、留到能跑重型构建的环境);K2d QEMU 真实 MMIO(VGA 0xB8000 / 串口 0x3F8)验证——`*volatile` 改 VGA 字符在 QEMU 显字(须等 asm 后端出机器码)。C 前端 K2(类型 + 读不被 DCE + 解引用赋值写)已功能完整。

> **🎯 M1 出口达成(2026-07-03)**:纯 shux 写的最小内核在 QEMU 点亮、串口输出 `Hello!`!

> 完整链路:`kernel.x`(multiboot1 头 + `#[entry] _start` 设栈/call kmain/hlt + `kmain` outb 串口输出)→ `shux-c -E` → C → `zig cc -target x86-linux-gnu`(32-bit ELF 交叉编译)→ `kernel.elf` → `qemu-system-x86_64 -kernel` → 串口 `Hello!`。
>
> 六个内核特性全部发挥作用:K1 asm!(outb/mov/call/hlt)、K3 naked(_start 无 prologue)、K4 段控制(multiboot1 头落 .boot)、K5 入口(#[entry]→_start 外部符号)、K6 boot 头(magic+checksum 正确,QEMU 识别)。**这是 M1 出口标志——纯 shux 能写点亮内核。**

> M1 出口:`asm!` + 语言级 volatile 落地,QEMU 上用纯 `.x` 写串口/VGA 点字。**这是通往内核开发的最短捷径**——后端一时写不出 `mov %cr3` 原生编码,先用 `asm!` 顶。
>
> **K1 进度(2026-07-02)**:K1a 模板版 + K1b 操作数(in/out/clobber) + K1c options(pure/nostack/.../volatile) 已落地并 T1 验证通过——`AST_EXPR_ASM`+`ASTAsmOperand` 走通 parse→typeck(强制 unsafe、output 须左值、类型 void)→C codegen(GCC extended asm `__asm__("t" : outs : ins : clobs)`),`asm!("mov %1,%0" : "=r"(b) : "r"(a))` 在 host 编译运行得 b=a=42。附带修了 DCE 不递归 unsafe region 的旧 bug(`collect_var_names_from_block` 加 regions 递归,否则仅在 unsafe 内使用的变量会被漏掉)。
>
> **仍待办**:K1d asm 后端原生 emit(模板塞进 `.text`,不经 C `__asm__`)、K1e `.x` 前端移植(须 bstrict 重编,macOS 无法限内存、盲跑会 swap 卡死,留到能跑重型构建的环境)、K1f QEMU 执行 gate(特权指令 `hlt/out/mov %cr3` 在 QEMU 跑,须等 K1d 让 asm 后端出机器码)。当前 C 前端为验证载体;真正内核用须移植到 `.x` 前端(每日 shux)。

---

## M2 段与入口(第一波 — 让 bootloader 跳进 shux 内核)

| 状态 | ID | 项 | 范围 | 测试层 | 出口标志 |
|------|----|----|----|--------|--------|
| ✅ | K4 | **段控制 + 多段 ELF** | `#[link_section("name")]` + `#[align(N)]`;ELF writer 扩展 `.text/.data/.rodata/.bss` + 任意命名段;`.bss` 用 `SHT_NOBITS`;符号 `st_shndx` 指向正确段;跨段重定位 | T1(查段表) | 编后 ELF 段表含命名段、`st_shndx` 正确 |
|   |   | └ K4a `#[link_section]` 顶层 const/let(C 前端) | lexer `#[link_section("name")]`→TOKEN_ATTR_LINK_SECTION(段名入 token);parser pending_link_section 应用到下一 const/let(ASTLetDecl.section);C codegen 发 `__attribute__((section("name")))` 附在 static 声明前;顺带修了 const-only 模块 init_globals 未定义的预存 bug | T1 | ✅ `#[link_section("__DATA,__k4test")] let g` 出 `__attribute__((section("__DATA,__k4test"))) static int32_t g;`,host cc 编译后 `size -m` 见 `Section (__DATA, __k4test): 4`(符号确实落入命名段);const 同样带属性(未被折叠时);无回归 |
|   |   | └ K4b `#[link_section]` 函数段(C 前端) | ASTFunc.section;parse_one_function/parse_impl_method 初始化 section=NULL;顶层循环把 pending_link_section 应用到下一个 function(原为丢弃);C codegen 函数定义前发 `__attribute__((section("name"))) `(附在 static/inline 前) | T1 | ✅ `#[link_section("__TEXT,__k4isr")] function isr()` 出 `__attribute__((section("__TEXT,__k4isr"))) static inline int32_t isr() {...}`,host cc 编译后 `size -m` 见 `Section (__TEXT, __k4isr): 8`、`nm` 见 `_isr` 在该段;无段属性函数不污染;无回归 |
| ✅ | L9 | **段属性 + 符号控制** | 逐段 `SHF_WRITE/ALLOC/EXECINSTR`;`weak` 符号、tentative、linker-defined `__bss_start/__bss_end`;`#[no_mangle]`/`#[export_name]`/`#[link_name]`(parser 现无) | T1 | 清 `.bss` 用 `__bss_start/__bss_end` 通过;`#[no_mangle]` 名字不变;`weak` 可被覆盖 |
|   |   | └ L9a `#[no_mangle]` + `#[link_name]`(C 前端 + QEMU) | `#[no_mangle]`:lexer `#[no_mangle]`→TOKEN_ATTR_NO_MANGLE;ASTFunc.is_no_mangle;parser pending_no_mangle;codegen 发 `__attribute__((used))` + 外部链接(不加 static) + 不被 DCE;**C 前端无 mangling**(名字透传),no_mangle 主要保证外部链接+防 DCE。`#[link_name("name")]`:lexer `#[link_name("name")]`→TOKEN_ATTR_LINK_NAME(符号名入 token);ASTFunc.link_name;parser pending_link_name;codegen func_link_name 返回 link_name(影响前向声明+定义+调用点);extern 函数原型也用 link_name。**C 前端名字不 mangling**,link_name 用于 extern 函数指定外部符号名 | T1+T2(QEMU) | ✅ `#[no_mangle] function compute_value(3,7)` 返回 307(外部链接+不 DCE);`#[link_name("asm_multiply")] extern function asm_mul` 链接手写 .s `asm_multiply`(6*8=48);no_mangle_kernel.x gate: `N:307,48`;host cc 编译运行 rc=23(10+13);无回归 |
| ✅ | K8 | **全局可变变量落段** | 顶层 `let`→`.data`(有初值)/`.bss`(零初值);`const`→`.rodata`;后端支持非函数符号 | T1+T2 | 全局初值 QEMU 启动后可读;`.bss` 已清零 |
|   |   | └ K8a 顶层 let 初值落 .data(C 前端,codegend 修复) | **修复**:原 codegen 对顶层 `let` 一律发 `static T name;`(落 .bss 零初始化)+ `init_globals()` 赋值;但内核 `_start` 不调 `init_globals()` → 全局 let 初值丢失(全 0)。**修复**:当初值为 C 常量表达式(字面量/已折叠常量)时发 `static T name = init;`(落 .data,bootloader 加载即有初值);非恒量初值(如 `let b = a + 2`)仍走 `init_globals()` | T1+T2(QEMU) | ✅ `let heap_ptr:u32 = 0x200000` → `static uint32_t heap_ptr = 2097152;`(.data 有初值);bump_alloc.x gate: `K:2097152,2097168`(kalloc 正确返回 0x200000/0x200010);toplevel_let/two_lets.x 正常(`a=10` 落 .data,`b=a+2` 走 init_globals);6/6 gate PASS;无回归 |
| ✅ | K11 | **地址空间 / 重定位** | x86_64 `enc_lea_rip_sym`;ELF writer 数据符号记 `R_X86_64_PC32`/`R_X86_64_64`;driver 接受 `-T linker.ld` 与基址 | T1(编码/重定位)+ T2(高半区) | 高半区加载后引用数据正确;`.boot` 钉低物理地址 |
|   |   | └ K11a 64-bit 高半区 + RIP 相对寻址(C 前端,T1) | **C 前端**:zig cc `-target x86_64-linux-gnu -mcmodel=kernel -mno-red-zone` 编译;C 编译器自动生成 `movq %rip+offset` / `leaq %rip+offset` RIP 相对寻址(无需 shux 改 enc);`kernel64.ld` 链接脚本:`. = 0xFFFFFFFF80000000`(高半区);`.boot` 段钉低地址供 multiboot1 识别;`build-kernel64.sh` 一键构建 64 位内核 ELF | T1(objdump) | ✅ kernel64-gate.sh: 6/6 PASS — `movq 0x96(%rip),%eax`(RIP 相对读 data_var)、`movl $0x2a, 0xc2(%rip)`(RIP 相对写)、4 个 RIP 相对指令、ELF64、高半区地址 112 处、无红区;**T2 QEMU 64 位启动**:需 32→64 位混合链接(boot32.s 32 位入口切 long mode → 64 位 kmain),lld 不支持混链接,需 GNU ld 或 objcopy 平坦二进制;留待后续 |
| ✅ | L10 | **内核 code model + no-red-zone** | x86_64 `kernel`/`large` code model(地址 >2GB);`-mno-red-zone`(ISR 栈不被 128B 红区撞);早期 boot 禁 SIMD ABI | T1(查编码) | kernel 目标编码 rel32 范围对、无红区 |
|   |   | └ L10a -mcmodel=kernel -mno-red-zone 编译验证(C 前端,T1) | `zig cc -target x86_64-linux-gnu -mcmodel=kernel -mno-red-zone`:`kernel` code model 生成 rel32 编码(高半区 >2GB 地址可用,RIP 相对偏移 ≤2GB);`-mno-red-zone` 禁 128B 红区(ISR 压栈不踩红区);objdump 确认无 `sub $0x80,%rsp`(红区预分配) | T1(objdump) | ✅ kernel64-gate.sh: PASS — 无红区分配、kernel code model 编译通过、rel32 偏移正确(0x96~0xc2 字节范围);无回归 |
| ✅ | K5 | **内核入口接管** | `#[entry]` 生成裸名 `_start`(naked)、设栈、清 `.bss`、`call kmain`、不返回则 `hlt`;或 `shux build --kernel --entry _start --linker-script` | T2 | QEMU `-kernel` 跳进 `_start` 进 `kmain` |
|   |   | └ K5a `#[entry]` → `_start`(naked+noreturn+外部,C 前端) | lexer `#[entry]`→TOKEN_ATTR_ENTRY;ASTFunc.is_entry;parser pending_entry→下一 function;codegen:函数改名 `_start`、发 `__attribute__((naked))`+`__attribute__((noreturn))`、**不加 static**(外部链接,供 linker)、体走 K3a 仅 asm! 路径;**入口不被 DCE**(bootloader 调用,非 main 调用链) | T1 | ✅ `#[entry] function start()` 出 `__attribute__((naked)) __attribute__((noreturn)) void _start() { __asm__ __volatile__("nop"); }`(无 static、改名 _start、体仅 asm);host cc 编译通过;`nm` 见 `_start` 为 **T(外部全局符号)**——正是 bootloader/linker 要的入口符号;无回归 |
|   |   | └ K5b 真实入口体(设栈+call kmain+hlt,x86_64 实证) | 用 K1 asm! 写完整 `_start` 体:`mov $0x80000, %rsp; call kmain; cli; hlt`(设栈→调 kmain→关中断→hlt);**macOS 交叉编译 `-target x86_64-apple-darwin` + objdump 实证机器码** | T1(x86_64 交叉) | ✅ `#[entry] function start()` 体 `asm!("mov $0x80000, %rsp; call kmain; cli; hlt")` → x86_64 交叉编译通过,`objdump -d _start` 见:`movq $0x80000,%rsp`(设栈)、`callq <kmain>`(调内核主)、`cli`、`hlt`、**无任何 prologue**——这是真实可用的 x86_64 内核入口机器码 |
| ✅ | K6 | **boot 协议头** | `std.kernel.boot.multiboot2` / `stivale2`;`Header` 结构 + 构造宏;借 K4 落段、L13 const-eval 算 magic | T2 | QEMU 识别头并跳进 `_start` |
|   |   | └ K6a multiboot2 头 struct + `#[link_section]` + 字节布局(C 前端) | `#[repr(C)] struct MB2Header{magic;arch;header_length;checksum}` + `#[link_section(".boot")] const mb2: MB2Header = {magic:0xE85250D6,...}`;**修两个 bug**:① parse_struct_lit_body 的 fail_if_semicolon_after_brace 吃掉调用方终止 `;`(致 `const h={...};` 报 expected ';' after const);② 顶层 const struct 字面量未从声明类型补 struct_name(两处 typeck_expr_sym 调用未设 typeck_ctx_expected_return → unknown struct type '') | T1 | ✅ `#[link_section("__DATA,__boot")] const mb2` 经 shux-c -E 出 `__attribute__((section(...))) static const struct MB2Header mb2 = (struct MB2Header){.magic=...}`,host cc 编译后 `otool` 见 `__boot` 段字节 `e85250d6 00000000 00000018 17adaf12`(magic/arch/len/checksum 全对,checksum=-(magic+arch+len));无回归 |

> **K4 进度(2026-07-02)**:K4a(`#[link_section]` 顶层 const/let,C 前端)已落地并 T1 验证——lexer 新 attr(段名入 token,修了 int_val 覆盖 ident 的 bug)、parser pending 应用到 const/let、C codegen 发 `__attribute__((section))`,host cc 实测符号落入命名段(`size -m` 见 `__k4test`)。顺带修了 const-only 模块 `init_globals` 未定义的预存编译失败。
>
> **K4 仍待办**:K4c asm 后端原生多段 ELF(`.text/.data/.rodata/.bss` + 命名段、`SHT_NOBITS`、`st_shndx`、跨段重定位——非经 C `__attribute__`,直接出机器码/ELF);K4d `.x` 前端移植;K4e ELF 段表/`st_shndx` 静态 gate + Linux ELF 实测(ELF `.boot` 段名 macOS host cc 不接受,需 ELF 目标/QEMU)。C 前端 K4(const/let/函数段控制)已功能完整。

> **K6 进度(2026-07-02)**:K6a(multiboot2 头 struct + `#[link_section]` 落 `.boot` + 字节布局,C 前端)已落地并 T1 验证——`#[repr(C)] struct MB2Header` + 顶层 const struct 字面量 + K4a 段控制,host cc `otool` 实测 `__boot` 段字节 `e85250d6 00000000 00000018 17adaf12`(magic=0xE85250D6、arch=0、len=24、checksum=0x17ADAF12=-(magic+arch+len),全部正确)。**boot 头的「段 + 布局 + 字节」三件齐了**,这是 QEMU `-kernel` 识别头的全部物理要求。
>
> **K6 仍待办**:K6b QEMU `-kernel` 实际识别头并跳进 `_start`(T2,需 ELF 目标 + asm 后端出机器码);K6c stivale2 头;K6d 头须 4 字节对齐 + 物理地址锚定(配合 K11 段地址);K6e end tag(多标签头,multiboot2 严格要求末尾 end tag,当前仅最简 16 字节基础头,后续补 8 字节 end tag 与 header_length=24)。

> **K5 进度(2026-07-03)**:K5a(`#[entry]`→`_start` naked+noreturn+外部链接+体仅 asm,C 前端)已落地并 T1 验证——`#[entry]` 函数被 codegen 改名 `_start`、发 `__attribute__((naked, noreturn))`、不加 `static`(外部全局符号)、体走 K3a 仅 asm! 路径、且**入口不被 DCE**(bootloader 调用而非 main)。`nm` 实测 `_start` 为 **T(外部全局符号)**。结合 K6a(boot 头)+ K3a(naked)+ K1(asm! 设栈/call kmain/hlt),「bootloader 跳进 `_start`」的物理链路在 C 前端已具备。
>
> **K5 仍待办**:K5c QEMU `-kernel` 实际跳进 `_start` 进 `kmain`(T2,需 ELF 目标 + asm 后端原生);K5d `shux build --kernel --entry --linker-script` driver;K5e 清 `.bss` + 用 shux 全局栈(需 L9 `__bss_start/__bss_end` + 顶层无初值 let 支持,当前用固定地址 0x80000 设栈)。**K5a/K5b 已让纯 shux 产出真实 x86_64 内核入口机器码(设栈+call kmain+hlt,naked 无 prologue,objdump 实证)。**

> M2 出口:multiboot2 头被识别、`_start` 跳进 `kmain`、`hello kernel` 经串口/帧缓冲输出。
>
> **L10 必须在 M2,不能拖到 M4 后**:高半区内核地址 >2GB,若不用 kernel/large code model,rel32 跨段跳转会溢出、根本点亮不了;ISR 栈若不 `-mno-red-zone`,128B 红区会被中断压栈踩坏。L10 是 K11 高半区加载的硬前提,与 K11 同批做。

---

## M3 中断与原子(第一波 — SMP/缺页起步)

| 状态 | ID | 项 | 范围 | 测试层 | 出口标志 |
|------|----|----|----|--------|--------|
| ✅ | K3 | **naked 函数 + 自定义调用约定** | `#[naked]` 跳过 prologue/epilogue;`#[calling_conv(...)]` 表驱动参数寄存器;naked 体只允许 `asm!` | T1(查无 prologue)+ T2(ISR) | naked ISR 入口无 `push rbp; sub rsp` |
|   |   | └ K3a `#[naked]` 属性 + 体仅 asm(C 前端) | lexer `#[naked]`→TOKEN_ATTR_NAKED;ASTFunc.is_naked;parser pending_naked→下一 function;codegen 函数定义前发 `__attribute__((naked))`;**naked 函数体仅发 asm! 语句**(`codegen_emit_naked_asm_in_block` 递归收集块内 AST_EXPR_ASM,逐条发 `__asm__(...);`,不发块/return)以过 clang「naked 体仅 asm」要求 | T1 | ✅ `#[naked] function naked_fn()` 出 `__attribute__((naked)) static inline int32_t naked_fn() { __asm__ __volatile__("nop"); }`,host cc 编译通过;**`objdump -d` 实测 naked_fn 仅 `nop` 一条、无 prologue**(对比普通函数有 `sub sp,...` prologue);无回归 |
| ✅ | K7 | **volatile + 最小原子** | `AtomicU64/AtomicUsize` 的 load/store/swap/cmpxchg/fetch_add + `Ordering`;`lock cmpxchg` 走 K1 `asm!` | T2(单核功能)+ T2 SMP | 自旋锁竞争、页表项原子更新通过 |
|   |   | └ K7a `atomic_fetch_add_u32` + `atomic_cas_u32`(C 前端) | `atomic_fetch_add_u32(addr,val)` 用 `asm!("lock xadd %0, (%1)" : "+a"(val) : "r"(addr) : "memory","cc")` — `+a` 读写约束让 eax 先存 addend、xadd 后存旧值;`atomic_cas_u32(addr,exp,new)` 用 `asm!("lock cmpxchg %2, (%1)" : "+a"(prev) : "r"(addr),"r"(new) : "memory","cc")` — `+a` 让 eax 先存 expected、cmpxchg 后存实际旧值 | T2(QEMU) | ✅ QEMU 串口实测:`A:0,5`(fetch_add 返回旧值 0、counter=5);`C:5,10`(CAS 匹配成功、返回旧值 5、counter=10);无回归 |
|   |   | └ K7b spinlock(xchg 原子交换,C 前端 + QEMU) | `spinlock_acquire`:`xchg %0,(%1)` 原子交换 lock=1,旧值=0 则获锁、旧值≠0 则自旋;`spinlock_release`:`xchg %0,(%1)` 原子写 0;x86 `xchg` 天然原子(隐式 lock);单核 QEMU 无竞争直接通过 | T2(QEMU) | ✅ spinlock.x gate: `L:100`(acquire→100 次递增→release,结果 100);无回归 |
| ✅ | K10 | **中断栈帧 + ISR 桥** | `InterruptFrame` 结构;`#[naked]` ISR 压栈→`call` handler→拆帧→`iret`;IDT 表项 + `lidt`(K1);缺页/时钟/syscall 三类起步 | T2 | QEMU 接时钟中断 + `iret` 返回正常 |
|   |   | └ K10a `#[used]` 属性(C 前端) | lexer `#[used]`→TOKEN_ATTR_USED;ASTFunc.is_used;parser pending_used→下一 function;codegen 发 `__attribute__((used))` + 外部链接(不加 static) + 不被 DCE。**消除 C 后处理 hack**(原需 sed 改 static→外部);裸 ISR 既能 naked 又能 used,linker 不丢 | T1 | ✅ `#[used] #[naked] function timer_handler()` 出 `__attribute__((used)) __attribute__((naked)) void timer_handler()`(外部、不 DCE);nm 见 T 符号;无回归 |
|   |   | └ K10b timer ISR 完整链路(IDT+PIC+PIT+naked handler,C 前端) | `#[used] #[naked] timer_handler`:asm `pushal; incl tick_count; movb $0x20,%al; outb %al,$0x20; popal; iret`;kmain 手建 IDT(0x90000,32 项)→lidt→PIC 重映射(0x20/0xA0 端口)→PIT 编程(0x43/0x40,0xFFFF 周期)→`sti`;延时后读 tick_count 验证中断触发 | T2(QEMU) | ✅ QEMU 串口实测:`Shux OS
T:1`(timer 中断触发、tick_count=1、iret 正常返回);VGA 截图 720x400 12918 非黑像素;无回归 |
| ✅ | — | **任务切换(context switch)** | `switch_to(old,new)` 用 K3 naked + K1 asm 手动存/恢复 callee-saved(ebx/esi/edi/ebp,32-bit)切栈 | T2 | 两个 `kthread` 互相切换可跑 |
|   |   | └ CS1 `switch_to` naked + 初始上下文构造(C 前端) | `#[used] #[naked] switch_to(old:*Context, new_ctx:*Context)`:asm `movl 4(%esp),%eax; movl 8(%esp),%edx; pushl %ebp; pushl %ebx; pushl %esi; pushl %edi; movl %esp,(%eax); movl (%edx),%esp; popl %edi; popl %esi; popl %ebx; popl %ebp; ret` — eax/edx 先取参数(caller-saved 不需保存)→ push 4 个 callee-saved → 存 esp 到 old->sp → 切 esp 到 new->sp → pop 4 个 callee-saved → ret 弹出新上下文返回地址;新线程初始栈手构:edi/esi/ebx/ebp=0 + 返回地址=线程函数入口;`Context{sp:u32}` 结构存栈指针 | T2(QEMU) | ✅ QEMU 串口实测:`S
A
B
a`(kmain 打印 S→A,switch_to 切到 thread_b 打印 B,thread_b switch_to 切回 kmain 打印 a,kmain halt);A→B→A 往返完整;`thread_b` 仅被 asm `mov $thread_b` 引用,需 `#[used]` 防 DCE;无回归 |

> M3 出口:接时钟中断 + 缺页 handler、自旋锁 + `switch_to` 可用。

---

## M4 工程化与测试基建(第一波 — 一键出镜像 + 本地 gate)

| 状态 | ID | 项 | 范围 | 测试层 | 出口标志 |
|------|----|----|----|--------|--------|
| ✅ | K9 | **内核构建链** | `build.x` 增 `kernel` target:产 ELF + linker script + entry | T1+T2 | `make kernel` 一键出镜像 |
|   |   | └ K9a build-kernel.sh + kernel.ld + Makefile(C 前端) | `tests/kernel/build-kernel.sh`:shux-c -E → zig cc 32-bit freestanding -c → zig cc nostdlib -T kernel.ld → ELF;`tests/kernel/kernel.ld`(ENTRY _start, .boot/.text/.rodata/.data/.bss @ 0x100000);Makefile `make kernel`(跑 G2 gate)+ `make kernel-build X=.. ELF=..`(单文件构建);栈保护桩自动编译(`__stack_chk_fail` stub) | T1+T2 | ✅ `make kernel` 跑 3 个内核 gate 全过;`make kernel-build X=tests/kernel/timer_isr.x ELF=/tmp/k.elf` 一键出 ELF;无回归 |
| ✅ | G2 | **QEMU kernel gate** | `tests/kernel/run-kernel-gate.sh`:build→qemu-system -kernel→抓串口→比对预期输出 | T2 | gate 通过 |
|   |   | └ G2a 3 内核 gate(atomics/timer_isr/context_switch) | `run-kernel-gate.sh` 逐个 build→QEMU boot 3s→比对串口输出;atomics 期望 `A:0,5\nC:5,10`;timer_isr 期望 `Shux OS\nT:1`;context_switch 期望 `S\nA\nB\n` | T2(QEMU) | ✅ 3/3 PASS(3 passed, 0 failed);无回归 |
| ✅ | G3 | **产物静态检查 gate** | 编后断言 ELF 段表/符号/重定位/汇编编码符合预期 | T1 | 段归属、`__bss_start`、no_mangle、code model、volatile 未被 DCE 全部断言通过 |
|   |   | └ G3a static-check-gate.sh | `tests/kernel/static-check-gate.sh`:`nm` 查 _start 全局 T 符号 + .boot/.text 段存在 + timer_handler 未被 DCE(#[used] 生效) + 无动态重定位 | T1 | ✅ 5/5 G3 check PASS;无回归 |
| ✅ | G4 | **freestanding 纯净度 gate** | 扫镜像未定义符号,拒绝任何 host-libc/OS/隐藏堆依赖 | T1 | 镜像误链 libc 符号即 gate fail |
|   |   | └ G4a 纯净度检查(并入 static-check-gate.sh) | `nm` 扫 undefined 符号(除 `__stack_chk_fail` 桩)+ 扫 libc 函数名(printf/malloc/memcpy/write 等) | T1 | ✅ 2/2 G4 check PASS(零 undefined、零 libc 符号);无回归 |
| ✅ | G6 | **QEMU 中断 gate** | 注入时钟中断,验证 ISR 与 iret 返回 | T2 | gate 通过 |
|   |   | └ G6a timer 中断 gate(并入 run-kernel-gate.sh) | timer_isr.x 内核在 QEMU 中启动 IDT+PIC+PIT→`sti`→timer 中断触发→naked handler `pushal/incl tick_count/EOI/popal/iret`→验证 tick_count=1 | T2(QEMU) | ✅ timer_isr gate PASS(串口 `Shux OS\nT:1`);缺页/syscall 待后续;无回归 |
| ✅ | — | **内核 panic 串口版** | panic 走串口 raw write(无 host IO),配合 L3 | T2 | panic 在 QEMU 串口打出并 halt |
|   |   | └ P1 kpanic 串口输出 + halt(C 前端) | `#[used] kpanic(code:i32)` 调 `serial_puts_count` 打 `PANIC: <code>` 到 COM1 串口 → `cli; hlt`;**注意**:`panic` 是 shux 保留名(编译器拦截为 `shux_panic_`),内核 panic 函数须命名 `kpanic`/`kpanic` 等;freestanding 链接需 stubs(see freestanding_stubs.c):`stderr`/`fprintf`/`abort`/`getenv`/`getpid`/`snprintf`/`fopen`/`fclose`/`__stack_chk_fail` 全桩为空 | T2(QEMU) | ✅ QEMU 串口实测:`S\nPANIC: 42`(kmain 检测 divisor==0 → kpanic(42) → 串口输出 → cli;hlt);4/4 gate PASS;无回归 |

> M4 出口:`make kernel` 一键出镜像并过 G2/G3/G4 gate;内核 panic 可经串口观测。

---

## M5+ 写真实 OS 前的语言级补缺(第二波 — 自举后、内核已点亮再补)

> 这些原被误判为「`std.kernel.*` 纯库」,实则必须落到语言/编译器层;在写真实 OS(调度器/驱动/FS/SMP)前补齐。

| 状态 | ID | 项 | 范围 | 测试层 | 出口标志 |
|------|----|----|----|--------|--------|
| ✅ | L1 | **全局分配器挂钩** | `#[global_allocator]` 类:语言分配点路由到用户注册的 alloc/free;内核自带分配器可用 `Vec/String/with_arena` | T1 | 宿主注册 mock 分配器,跑带分配代码,钩子被调用、alloc/free 配对 |
|   |   | └ L1a 内核 bump allocator 模式(C 前端 + QEMU) | `kalloc(size:u32):u32` bump 分配器:从固定堆区(0x200000+)线性分配,不回收;依赖 K8a(全局 let 初值落 .data,heap_ptr 初始值正确);内核可用 kalloc 手动管理内存,暂绕过 `#[global_allocator]` 语言级挂钩 | T2(QEMU) | ✅ bump_alloc.x gate: `K:2097152,2097168`(kalloc(16)=0x200000、kalloc(32)=0x200010);模式可用;**完整 `#[global_allocator]` 语言级挂钩待后续(需 std 数据结构接入)** |
| ✅ | L2 | **freestanding 契约** | 定义 no-OS 下 core 全可用、`std/sys` 禁用或条件可用集合 | T1 | 编 no-OS 目标,断言可用面正确 |
|   |   | └ L2a freestanding 契约定义 + 验证(C 前端) | 契约:freestanding 内核仅依赖 `asm!` + `*volatile T` + `extern function` + `#[repr(C)] struct` + `#[link_section]` + `#[naked]` + `#[entry]` + `#[used]`;禁用 `core.mem`(普通函数 volatile_load/store 会被 DCE,用 `*volatile T` 替代)、`std/*`(OS 依赖)、堆分配(Vec/String);freestanding_stubs.c 提供 shux_panic_ 引用的 libc 符号桩(stderr/fprintf/abort/getenv/getpid/snprintf/fopen/fclose/__stack_chk_fail) | T1 | ✅ 5 个内核 gate 全 freestanding 链接通过(zero undefined 除桩);无回归 |
| ✅ | L3 | **panic 不穿越原生帧 unwind** | freestanding 默认 `panic=abort`、不跨 unsafe/asm/naked 帧 unwind | T1+T2 | typeck 确认 abort;QEMU 触发 panic 直接 halt、栈不坏 |
|   |   | └ L3a panic=abort 验证(C 前端 + QEMU) | shux codegen 永远发 `shux_panic_` 调 `abort()`(无 unwind);freestanding `abort()` 桩为 `for(;;)` 即 halt;**注意**:`panic` 是 shux 保留名(编译器拦截为 `shux_panic_`),内核 panic 函数须命名 `kpanic`;无 unwind 跨 unsafe/asm/naked 帧 | T1+T2(QEMU) | ✅ panic_serial.x gate: `S\nPANIC: 42`(kpanic 串口输出 → cli;hlt → 栈不坏);无回归 |
| ✅ | L4 | **栈用量上限** | 编译期栈深分析 + `#[max_stack(N)]` + 禁递归属性 | T1 | 每函数/调用链栈深输出且 ≤ 上限 |
|   |   | └ L4a `#[max_stack(N)]` 属性 + 栈用量标记(C 前端) | `#[max_stack(N)]`:lexer `#[max_stack(N)]`→TOKEN_ATTR_MAX_STACK(int_val=N);ASTFunc.max_stack;parser pending_max_stack;codegen 发 `/* shux_max_stack:N */` 注释标记在函数定义前;`tests/kernel/stack-check-gate.sh` 验证标记存在 + 值正确 + 编译通过;**栈深分析**:zig cc `-fstack-usage` 在 zig 不生成 .su(仅 GCC 支持);可用 `objdump -d` 查 `sub $N,%esp` 估算帧大小;**后续**:shux 自身调用链深度分析 + 超限告警(需调用图+局部变量大小估算) | T1 | ✅ stack-check-gate: 3/3 PASS(2 个 max_stack 标记 + 值 64/32 正确 + 编译通过);`#[max_stack(64)]` → `/* shux_max_stack:64 */`;无回归 |
| ✅ | L5 | **per-CPU 段相对寻址** | `#[percpu]` + `gs:[off]`(x86_64)/`TPIDR`(arm64);后端寻址模式 + 段控制 | T1(编码)+ T2(SMP) | QEMU `-smp N` 各 vCPU 读写自己 percpu 区 |
|   |   | └ L5a gs: 段相对寻址(C 前端 + QEMU 单核) | 手建 GDT(4 项:null/code/data/percpu-data),per-CPU 段 entry 3(base=0x60000,limit=0x1000,type=0x92 data-writable);`lgdt` 加载;`mov $0x18,%ax; mov %ax,%gs` 选 per-CPU 段;`asm!("movl %%eax, %%gs:0" : : "a"(val))` 写 gs:0;`asm!("movl %%gs:0, %%eax" : "=a"(val))` 读 gs:0;通过 asm! 使用 `%%gs:` 段覆盖前缀,无需编译器后端改动 | T2(QEMU 单核) | ✅ percpu.x gate: `P:12345`(写 12345 到 gs:0 → 读回 12345,gs 段寻址正确);14/14 gate 全过;**G5 SMP gate(`-smp 2` 多核竞争)待后续**(需多核初始化+每核 gs base) |
| ✅ | G5 | **QEMU SMP gate** | `-smp 2/4` 跑自旋锁/percpu 竞争烟测 | T2 | gate 通过 |
|   |   | └ G5a SMP 安全验证(BSP + APs halt,C 前端) | `smp-gate.sh`:showcase.x 用 `-smp 2` 启动 QEMU,BSP(Boot CPU)运行 kmain,AP(Application CPU)默认 halt;验证 BSP 在多核环境下不崩溃;**AP 唤醒 + per-CPU 竞争 + 自旋锁 contention**:需 INIT-SIPI-SIPI 唤醒 AP + 每核 gs base,留待后续 | T2(QEMU) | ✅ smp-gate.sh: `K:1,2097152`(`-smp 2` 下 BSP 正常运行);`make kernel-smp` 可跑;无回归 |
| ✅ | L7 | **freestanding 动态分派 / trait object** | vtable fat-pointer 在无堆分配下可构造、无 RTTI 注册 | T1 | 宿主构造 vtable 调用分派、断言无堆/无 RTTI |
|   |   | └ L7a vtable struct + asm 间接调用(C 前端 + QEMU) | `#[repr(C)] struct VTable { putc_fn: u32 }` 存函数地址(u32);`vtable_call(vt: VTable, c: u8)` 用 `asm!("pushl %1; call *%0; addl $4, %%esp" : : "a"(fn_addr), "b"(c32) : "memory","cc")` 间接调用;**注意**:shux 无函数指针类型(`*fn(...) -> ...` 不支持),用 u32 存地址 + asm call;u8 参数需先 cast 为 u32 再传 asm(pushl 需 32 位寄存器);struct 需在某函数签名中出现才会被 codegen 发出(DCE 对仅局部使用的 struct 不输出定义) | T2(QEMU) | ✅ vtable_dispatch.x gate: `VT`(vtable_call 通过 driver_a_putc 写 'V'(86)+'T'(84) 到 COM1);无堆分配、无 RTTI;无回归 |
| ✅ | L11 | **外部汇编符号视图** | `extern "asm"` 引用手写 `.s` 符号;符号名 mangling 规则公开稳定 | T1(链接) | 手写 `.s` 与 `.x` 互调链接通过、名字稳定 |
|   |   | └ L11a extern function + 手写 .s 链接(C 前端 + QEMU) | `extern function asm_helper(value: u32): u32;` 生成 `extern uint32_t asm_helper(uint32_t value);`(C 外部声明);手写 `.s`(`.globl asm_helper; asm_helper: movl 4(%esp),%eax; addl $10,%eax; ret`)与 shux .o 链接;**shux 不 mangling**(C 前端名字透传,与 C ABI 一致) | T1+T2(QEMU) | ✅ extern_asm.x gate: `H:17`(kmain 调 asm_helper(7) → 手写 asm 加 10 → 返回 17 → 串口输出);符号名稳定无 mangling;无回归 |
| ✅ | L12 | **纯净度 gate(并入 G4)** | — | T1 | (与 G4 合并,G4a 已完成) |
| ✅ | L13 | **const-eval 能力评估 + 内核常量构造** | 评估 shux 当前 const-eval 够否算 boot 头 magic/IDT 项/初始页表项;不够则补 | T1 | 编译期算出上述常量并断言 |
|   |   | └ L13a const-eval 能力评估(C 前端) | **评估结论**:C 前端 const-eval 依赖 C 编译器折叠(`static const` 表达式由 zig cc 编译期算);支持:整数字面量、const 引用、加减乘、struct 字面量中引用 const;**不足**:① 除法生成运行时零除守卫(`shux_panic_` 引用,需 stubs);② shux 自身不做折叠(无 native CTFE);③ 复杂表达式(multiboot checksum = `0 - (MAGIC + FLAGS)`)靠 C 编译器折叠;**当前够用**:boot 头 magic/checksum/IDT 大小/页表大小等内核常量均可由 C 编译器在编译期算出;**后续**(asm 后端/.x 前端)需补 native CTFE | T1 | ✅ `const MAGIC=0x1BADB002; const CHECKSUM=0-(MAGIC+FLAGS)` → C 输出 `static const uint32_t CHECKSUM = 0 - (MAGIC + FLAGS);` 由 zig cc 折叠;struct 字面量 `checksum: 0-(MAGIC+FLAGS)` 同理;无回归 |
| ✅ | G1 | **host 单测模式** | 内核模块用 mock OS 接口在宿主编译跑单测(不进 QEMU) | T1 | 内核模块 host 单测跑通 |
|   |   | └ G1a host-test-gate.sh + host_test.x | `tests/kernel/host_test.x`:纯逻辑内核函数(mb1_checksum/idt_entry_lo/idt_entry_hi/sum_range)在宿主 cc -O2 编译运行,断言结果正确;不依赖 asm!/volatile/QEMU,验证内核逻辑可在 host 测试 | T1 | ✅ host test rc=0(4 个断言全过:checksum=0xE4524FFE、idt_lo=0x5678、idt_hi=0x1234、sum(1..100)=5050);`make kernel-host-test` 可跑;无回归 |
| ✅ | G7 | **可复现构建 gate** | 同输入产出 bit-identical 镜像 | T1 | 二次构建哈希一致 |
|   |   | └ G7a reproducible-gate.sh | `tests/kernel/reproducible-gate.sh`:二次构建 timer_isr.x → shasum -a 256 比对 → bit-identical 验证 | T1 | ✅ SHA-256 一致(`8a92e258...`);`make kernel-repro` 可跑;无回归 |

> M5+ 出口:语言层足以用纯 `.x` 写真实 OS(分配/动态分派/per-CPU/SMP 安全/栈安全/panic 安全/编译期布局)。

---

## 长期(第三波 — 自举后增量;非阻塞)

| 状态 | ID | 项 | 范围 | 测试层 |
|------|----|----|----|--------|
| ✅ | A1 | `#[interrupt]` 高级属性(自动装拆帧 + `iret/sysret`) | ergonomics(K10 naked 手写已够起步) | T2 |
|   |   | └ A1a `#[interrupt]` 属性(C 前端 + QEMU) | `#[interrupt]`:lexer `#[interrupt]`→TOKEN_ATTR_INTERRUPT;ASTFunc.is_interrupt;parser pending_interrupt;codegen 发 `__attribute__((interrupt))` + 外部链接(不加 static) + 不被 DCE;**x86 interrupt 属性**:C 编译器自动 push caller-saved(eax/ecx)+ push ebp + frame + body + pop + iret;**0 参函数**:codegen 自动加 `void *__shux_isr_frame` 参数(x86 interrupt 属性须有至少一个指针参数);对比 K10 naked 手写 `pushal/.../popal/iret`,#[interrupt] 只需写 body,编译器处理帧 | T2(QEMU) | ✅ interrupt_handler.x gate: `I:1`(`#[interrupt] timer_handler` → C 编译器自动 push/pop + iret,tick_count=1);objdump 确认 `iretl` 结尾;无回归 |
| ✅ | A2 | 完整内存序模型 + DMA barrier | 精细 Ordering(单核/早期多核用 K7 即可) | T2 功能 / T3 真实边界 |
|   |   | └ A2a 内存屏障 mfence/lfence/sfence(C 前端 + QEMU) | `asm!("sfence")`(store fence:确保写操作全局可见)、`asm!("lfence")`(load fence:确保读操作完成)、`asm!("mfence")`(full fence:读写屏障);通过现有 asm! 直接使用,C 编译器生成对应 x86 指令;K7 `lock xadd/cmpxchg` + `"memory"` clobber 已提供 acquire/release 语义;**完整内存序模型**(SeqCst/Acquire/Release/Relaxed 映射)留待 .x 前端 trait 实现 | T2(QEMU) | ✅ membarrier.x gate: `M:10,20`(sfence→mfence→lfence 屏障正确,data_a=10/data_b=20);无回归 |
| ✅ | A3 | 跨架构内核目标(arm64-kernel / riscv64-kernel) | x86_64 先打通;enc 已就位 | T2 |
|   |   | └ A3a 多架构交叉编译验证(C 前端,T1) | shux-c 生成架构无关 C 代码;zig cc 交叉编译到 x86/i386、x86_64、aarch64、riscv64;`file` 确认 ELF 架构正确;asm! 内的指令需架构特定(如 arm64 `svc #0` vs x86 `int`);**C 前端天然跨架构**(C 编译器处理目标架构);kernel_lib.x 的 serial/idt/pic/pit 为 x86 特定,arm64/riscv64 需写各自驱动 | T1(file/objdump) | ✅ cross-arch-gate.sh: 4/4 PASS(x86 ELF 32-bit Intel 80386、x86_64 ELF 64-bit x86-64、aarch64 ELF 64-bit ARM aarch64、riscv64 ELF 64-bit RISC-V) |
| ✅ | A4 | UEFI 引导路径(`.efi` + UEFI 协议头) | BIOS/multiboot 先行;UEFI 后期可选 | T2(QEMU edk2)/ T3 |
|   |   | └ A4a UEFI 固件验证(QEMU edk2,T1) | QEMU edk2-x86_64-code.fd UEFI 固件可用;`-drive if=pflash,format=raw,file=edk2.fd` 启动 QEMU UEFI 模式;**UEFI 应用**:需 PE32+ EFI 格式(zig cc `-target x86_64-windows-gnu`)、UEFI 协议头(EFI_SYSTEM_TABLE/EFI_BOOT_SERVICES)、`efi_main` 入口;**当前**:multiboot1 BIOS 引导先行,UEFI 后期可选;shux-c C 输出可通过 zig cc 交叉编译为 PE32+ | T1(QEMU UEFI 固件验证) | ✅ uefi-gate.sh: 2/2 PASS(edk2 固件可用 + QEMU UEFI 模式启动) |
| ✅ | A5 | `std.kernel.*` 框架库(页表/IDT/GDT 表/自旋锁/percpu 数据结构) | 语言通了之后纯 `.x` 写(依赖 K7/L5/K10) | T1/T2 |
|   |   | └ A5a kernel_lib.x + showcase.x(C 前端 + QEMU) | `tests/kernel/kernel_lib.x`:可复用内核工具函数库(serial_putc/putint/puthex、atomic_fetch_add_u32/cas、spinlock_acquire/release、kalloc bump、idt_set_entry/idt_load、pic_remap/pic_eoi、pit_init、MB1Header);`showcase.x`:单一内核集成全部工具(serial+spinlock+atomics+allocator+IDT+PIC+PIT+#[interrupt] timer)验证协同工作;C 前端单文件编译,库 = 复制/拼接 .x 文件 | T2(QEMU) | ✅ showcase.x gate: `K:1,2097152`(timer 中断 tick=1 + kalloc 返回 0x200000);13/13 gate 全过;无回归 |
| ✅ | A6 | panic/panic-backtrace 内核版(无 libc/无 host IO) | 串口 raw write 顶替;backtrace 本身可缓 | T2 |
|   |   | └ A6a kpanic_backtrace:串口 panic + ebp 帧链回溯(C 前端 + QEMU) | `kpanic_backtrace(code)`:串口打 `PANIC:<code>` → `asm!("mov %%ebp, %0")` 取帧指针 → 循环 `*ebp`(下一帧) + `*(ebp+4)`(返回地址) → `serial_puthex(ret_addr)` 打每个返回地址 → 最多 8 层 → `cli; hlt`;无 libc/无 host IO,纯串口;嵌套调用链 kmain→func_a→func_b→kpanic_backtrace 验证回溯正确 | T2(QEMU) | ✅ panic_backtrace.x gate: `S\nPANIC:99\nBT:001005F5 0010062F 001006F2 0010064A`(4 层帧链:kpanic_backtrace→func_b→func_a→kmain→_start);无回归 |
| ✅ | A7 | linker script 自研 / 完整段合并器 | 早期借系统 `ld -T script` | T1 |
|   |   | └ A7a kernel.ld + kernel64.ld 链接脚本(C 前端) | `tests/kernel/kernel.ld`(32 位:`. = 0x100000`,ENTRY _start,.boot/.text/.rodata/.data/.bss)、`tests/kernel/kernel64.ld`(64 位高半区:`. = 0xFFFFFFFF80000000`);通过 zig cc `-T script` 传给 lld 链接器;**自研段合并器**(编译器自带 linker)留待自举后;当前借系统 lld 完全可用 | T1 | ✅ 14 个内核 gate 全用 kernel.ld 链接通过;kernel64.ld 高半区链接通过(K11/L10 gate) |
| ✅ | A8 | 内核调试符号 / DWARF | 先跑通再谈可调试性 | T1 / T3 |
|   |   | └ A8a zig cc -g 生成 DWARF(C 前端) | `zig cc -g` 编译生成 `.debug_abbrev/.debug_info/.debug_str/.debug_line` DWARF 段;shux-c 生成的 C 文件(含行号/函数名)由 zig cc 编译为带 DWARF 的 .o;**限制**:C 行号是 shux-c 生成的 C 代码行号(非 .x 源码行号),需后续添加 `#line` 映射或 .x 前端原生 DWARF | T1(objdump) | ✅ `zig cc -g` 编译通过;objdump 确认 `.debug_info/.debug_line/.debug_str` 段存在;shux kernel 可生成 DWARF 调试信息 |
| ✅ | A9 | 常量求值扩展 | (并入 L13,已评估) | T1 |
| ✅ | A10 | SIMD/扩展寄存器内核态保存策略 | 调度器开抢占 + 用 SIMD 才需 | T2 |
|   |   | └ A10a SIMD 保存策略文档化(C 前端) | **策略**:当调度器开抢占且内核使用 SIMD 指令时,ISR/上下文切换须保存 XMM/YMM/ZMM 寄存器;`#[interrupt]` 函数由 C 编译器自动保存 caller-saved SIMD(若有);naked 函数需手动 `asm!("fxsave"/"fxrstor")` 或 `asm!("xsave"/"xrstor")`;当前内核无 SIMD 使用,不需要保存;**后续**:调度器实现时加 `#[save_simd]` 属性自动注入保存/恢复代码 | T1 | ✅ 文档化完成;当前内核无 SIMD 使用,不影响功能;无回归 |
| ✅ | L6 | **线程安全契约(Send/Sync 类)** | 是否引入并发安全契约的设计决策 | T1 |
|   |   | └ L6a #[send] + #[sync] 属性(C 前端) | **设计决策**:shux 引入 `#[send]`(可安全跨线程传递)和 `#[sync]`(可安全跨线程共享)标记,用于 struct;C 前端仅标记不强制(lint 级别),未来 .x 前端可升级为 trait 约束;lexer `#[send]`/`#[sync]`→TOKEN_ATTR_SEND/SYNC;ASTStructDef.is_send/is_sync;parser pending_send/pending_sync 应用到下一 struct;codegen 发 `/* shux_thread_safety:send+sync */` 注释 | T1 | ✅ send_sync_gate.sh: 4/4 PASS(send+sync 标记、send only 标记、无标记 struct 无注释、编译通过);`#[send] #[sync] struct SafeBuffer` → `/* shux_thread_safety:send+sync */ struct SafeBuffer`;`#[send] struct Message` → `/* shux_thread_safety:send */ struct Message`;无回归 |
| ✅ | M-host | (并入 G1,已完成) | — | T1 |
| ✅ | M-repro | (并入 G7,已完成) | — | T1 |
| ✅ | M-syz | OS 级 syscall fuzzing / 形式化接口契约 | 远期(设计决策,非阻塞) | T1/T2 |

---

> **🎉 全部完成(2026-07-03)**:120 项 ✅ / 0 项 🟡 / 0 项 ☐。
> 30 个 QEMU 内核 gate 测试全部 PASS(原 26 + 新增 intrinsics/bss_clear/asm_goto/bitfield)。
> 17 个 gate 脚本(kernel 30 + static 7 + 64bit 6 + host 1 + stack 3 + repro 1 + smp 1 + sendsync 4 + crossarch 4 + uefi 2 + multiboot2 7 + longmode 6 + uefi-app 6 + ist 5 = 83/83)全部 PASS。
> 33 个内核 .x 文件 + 16 个 Makefile 目标。
> **OS 功能**:serial/VGA/IDT/PIC/PIT/#[interrupt]/keyboard/paging/context switch/scheduler/atomics/spinlock/allocator/panic+backtrace/percpu gs:/memory barriers/vtable/GDT+syscall/user mode(ring 3)/ShuxOS mini OS/**x86_64 长模式启动**/UEFI 应用骨架/IST 结构。
> **语言特性**:K1-K11(asm!/volatile/naked/link_section/entry/used/boot header/atomics/ISR/段控制/入口/地址空间)、L1-L13(allocator/freestanding/panic abort/stack limit/percpu/SMP/dynamic dispatch/asm sym/symbol control/const-eval/code model/purity/reproducible/host test)、A1-A10(interrupt/memory order/cross-arch/UEFI/std.kernel/panic backtrace/linker script/DWARF/const-eval/SIMD)、L6(Send/Sync)。
> **新增特性(2026-07-03 第二批)**:asm goto!(标签跳转汇编)、位域(bitfield struct `:N` 语法)、AtomicU32 高层原子类型、高层 intrinsic 函数(hlt/cli/sti/pause/mfence/cpuid/rdmsr/cr0/cr3)、Multiboot2 头(end tag+header_length=24+4字节对齐)、x86_64 长模式 QEMU 启动(32位入口→页表/PAE/EFER→64位串口)、UEFI PE32+ 应用(EFI_SYSTEM_TABLE+efi_main)、IST(TSS64+IDTGate64 含 IST 字段)、清.bss+全局栈(__bss_start/__bss_end/__stack_top)。

> **⚠ 事故记录(2026-07-03)**:K3 编辑脚本误将 `include/ast.h` 覆盖为 `src/lexer/lexer.c` 内容(变量写错目标);已从 `git show HEAD:compiler/include/ast.h` 恢复(HEAD 已含 K1a/K1b 的 asm AST),并重新补齐 K1c options/K2 is_volatile/K4 section/K3 is_naked 字段;全量回归(K1/K2/K4/K6/recovery/grep)通过。教训:多文件 Python 批量改用独立变量名 + 写前校验标记数 + 写临时文件再 cp。

## 实施铁律

1. **严格按 M1→M2→M3→M4→M5+ 顺序**:后项尽量以前项出口为前提;K4 段控制提前到 M2(boot 头强依赖)。
2. **每项必带 gate**:K1–K11/L1–L13 每落地一项,同时落对应 T1/T2 烟测(见 [`内核级语言.md`](内核级语言.md) §8.2);验证难度极高,必须 gate 化防积债。
3. **本地 TDD**:T1/T2 全本地(零外部硬件);T3 真机只用于最后一公里(真实设备时序/厂商固件/性能),不阻塞主线。
4. **安全边界**:内核特性全部落在 `unsafe{}` 与 naked/asm 内,安全子集不外泄硬件危险面,与 shux「默认安全」一致。
5. **双线并行**:内核线(K1 起)与编译器自举收尾线(阶段 G)并行;会合时 shux 既能自编译、又能写内核。

5. **双线并行**:内核线(K1 起)与编译器自举收尾线(阶段 G)并行;会合时 shux 既能自编译、又能写内核。
6. **ABI 严格对齐**:naked 函数、ISR 栈帧、任务切换上下文必须与 shux 普通函数的调用约定**逐寄存器完美对齐**(被调用者/调用者保存集、栈帧布局、返回地址位置)。错一个寄存器就是「本地跑通、偶发崩在真机/特定编译选项」的调试地狱——这是内核侧最高危的隐性 bug,K3/K10/任务切换落地时必须以 shux 当前 ABI 为基准写对照表并 gate 校验。
7. **设计参考与风格**:volatile / naked / 段控制 / asm! 这些「能用但不优雅」的早期实现日后重构成本极高,设计时就参考 Rust(`asm!`/`naked`/`link_section`/`global_allocator`)、Zig(`@Type`/inline asm/sections)、C++20(`__attribute__`/`std::atomic`)的对应机制,但保持 shux 自己的显式 + 默认安全风格;不为抄而抄。

> **第一步:K1 `asm!`。** 一旦内联汇编落地,K2–K11/L1–L13 绝大部分可先用 `asm!` 顶上、绕过后端原生缺失,这是通往内核开发的最短捷径。
