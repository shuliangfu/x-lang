# ShuX 自举进度

> **更新**：2026-07-19 · 18 号全日已归档（见 §1）；本日从 19 号起重新起表  
> **基线 SHA（产品 L4 放行钉盘）**：**`305cfbe1`**（双端真冷 L4+**125** @ 钉盘时；前钉 `4214eea4`；本波 ABI 边界 → 升钉）  
> **产品 bstrict 套件数**：**125**（`tests/run-all-bstrict.sh`；日志须见 `run-all-bstrict OK (125 scripts…)`）  
> **本波（19 日）状态**：**C6 X ABI P0b 波 2（ABI 一致性修复 3 处）· tip `305cfbe1`**（ABI 边界 → L4 双端真冷 + 升钉 `4214eea4` → `305cfbe1`；Windows 门禁待补跑）。改动 `compiler/src/runtime_io_abi.x` L164/180/196 共 3 处定义 `export function fs_posix_*_c` → `export extern "C" function fs_posix_*_c`（保留 `#[no_mangle]`），与 5 个声明点（main.x:30-31 / pipeline.x:81-82 / driver/emit.x:71-73）对齐；每处加 4 行英文 ABI 注释说明契约依据 + 委托 `std_fs_fs_*` 设计理由；函数体不变（底层 libc `close`/`read`/`write` 已在 `std_fs_fs_close` L99-105 / `std_fs_fs_read` L124-135 / `std_fs_fs_write` L145-156 内部用 `unsafe { }` 包裹）。修 ABI 一致性违反：声明 5 处全部 `extern "C"` ↔ 定义 3 处缺失 → P0a 语义降级下不断链，P1 激活 typeck 强制 `extern "C"` 调用约定后参数寄存器错位 / 运行期 UB。mac L4 log `/tmp/mac_true_bstrict_305cfbe1.log` · Ubuntu L4 log `/tmp/ubuntu_true_bstrict_305cfbe1.log` 各 `run-all-bstrict OK (125 scripts)` exit=0；Ubuntu link-hardening gate OK（Linux 专有）。**前波归档**：波 1 unsafe 包裹 @ `4214eea4`（main.x 6 + mod.x 3 unsafe，L4 双端 + 升钉 + Windows 门禁待补跑）· C6 P0b audit @ `7e507fb6`（docs-only 5466 extern 实测 + 9 处违规 + 3 处不一致 + 3 波拆分策略）· shux-x 5GB 方案 A @ `a14a1dd0`（per-ArenaSidecar init 4.3MB → 270KB 减 94%）· 日终 L4 兜底 @ `94a8e7d5`（双端真冷 + 125 bstrict 全绿；mac log `/tmp/mac_true_bstrict_94a8e7d5.log` / Ubuntu log `/tmp/ubuntu_true_bstrict_94a8e7d5.log`）· 5 波 C5 CTFE（`229708f1`/`a8a67708`/`d7a2388e`/`c174e70a`/`681b168f`）

> **方法**：[自举方法.md](自举方法.md)（Cap/R/L/M；工程 ≠ 产品放行）  
> **Windows（删种子前）**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md) — **删除种子 / G 清场前须跑通 Windows B-hybrid 最小门禁**（探针，≠ 125 金标；见本文 §5）  
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `shux-selfhost-product-gate`  
> **归档**：[自举进度-归档-2026-07-18.md](自举进度-归档-2026-07-18.md) · [自举进度-归档-2026-07-17.md](自举进度-归档-2026-07-17.md) · [自举进度-归档-2026-07-16.md](自举进度-归档-2026-07-16.md) · [自举进度-归档-2026-07-14.md](自举进度-归档-2026-07-14.md) · [自举进度-归档-2026-07-13.md](自举进度-归档-2026-07-13.md)

### 维护约定

1. 每完成一波 **必改两文**：
   - **本文**（长期仪表盘）：§2 KPI、§3 前排（≤3 条）、§6 一行；
   - **[`当前进度.md`](当前进度.md)**（当天快照）：本波 / SHA / 下一波前排。
2. **禁止**把 prove/Stage2/N++ 单独写成「可自举」。
3. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict` **125**（现行白名单；历史钉盘日志可能写 123）。
4. 工程 KPI 必须写 **SHA + 日志路径**；**过期整日流水移归档**（18 号全日已入归档），不在正文假装仍绿。
5. **`问题分析文档.md`：默认不改。** 仅新产品红 / 整波新债开场 / 根因换层（见 `自举步骤.md` §5.1）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 `305cfbe1`（升自 `4214eea4`；双端真冷 L4 + 125 bstrict 全绿；本波 ABI 边界 → 升钉）。**  
**本波（2026-07-19）= C6 X ABI P0b 波 2（ABI 一致性修复 3 处）@ tip `305cfbe1`**（ABI 边界 → L4 双端真冷 + 升钉 `4214eea4` → `305cfbe1`；Windows 门禁待补跑）：

- ① **C6 X ABI P0b 波 2 已完成**（[C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.3 推荐执行方案）：修 ABI 一致性违反 3 处 — `compiler/src/runtime_io_abi.x` L164 (`fs_posix_close_c`) / L180 (`fs_posix_read_c`) / L196 (`fs_posix_write_c`) 共 3 处定义 `export function` → `export extern "C" function`（保留 `#[no_mangle]`），与 5 个声明点（main.x:30-31 / pipeline.x:81-82 / driver/emit.x:71-73）对齐；每处加 4 行英文 ABI 注释（契约依据 + 委托 `std_fs_fs_*` 设计理由）；**函数体不变**（调用 `std_fs_fs_close/read/write` 是 SHUX X-ABI 桥接，底层 libc `close/read/write` 已在 `std_fs_fs_close` L99-105 / `std_fs_fs_read` L124-135 / `std_fs_fs_write` L145-156 内部 `unsafe { }` 包裹）。**契约依据**：声明 5 处全部 `extern "C"` ↔ 定义 3 处缺失 → P0a 语义降级下不断链；P1 激活 `typeck.x:289 pipeline_typeck_check_extern_call_unsafe_boundary_c` 强制 `extern "C"` 调用约定后参数寄存器错位 / 运行期 UB。**双端 L4 真冷**：mac arm64 `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0（log `/tmp/mac_true_bstrict_305cfbe1.log`；全擦 .o + 删二进制 + bootstrap-driver-seed OK + g05_prepare_and_relink OK 58 objs Darwin/arm64）+ Ubuntu x86_64 `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0（log `/tmp/ubuntu_true_bstrict_305cfbe1.log`；git pull + 全擦 .o + 删二进制 + bootstrap-driver-seed OK + g05_prepare_and_relink OK 60 objs Linux/x86_64；**link-hardening gate OK（Linux 专有，macOS 跳过 non-Linux host）**）。**升钉决策**：ABI 一致性改动属 ABI 边界 → 升钉 `4214eea4` → `305cfbe1`。**Windows 门禁**：待补跑（ABI 边界 per §5.1 强制重跑 hybrid 最小门禁，与波 1 合并补跑；非阻塞升钉）。**波次**：波 2 of 3（audit `7e507fb6` → 波 1 `4214eea4` ✅ → **波 2 `305cfbe1` ✅** → 波 3 std/ ~557 远期顺延自举后 P1-P2）。**自举前 ABI 风险已解除**：波 1 + 波 2 完成后，P1 激活 typeck 强制 unsafe + X ABI 调用约定时不再有 bootstrap 陷阱。
- ② **本波额外工程债 · C7 plan 瘦身 PoC 完成**（工程债 · 非产品 · P3 · 路径 C 重构合并 · 2/8 完成 @ `e546b013` + `bedb40ca` · **非升钉**）：[C7-plan-audit.md](C7-plan-audit.md) audit 881 总脚本 vs 125 白名单 vs 23 W3 fast vs 752 非白名单（599 gate + 94 other + 24 perf + ...）；125 白名单内**无冗余**；23 W3 fast ⊆ 125（包含关系，非冗余）。8 个完全无引用 dead code 候选中 2/8 完成：① 合并 run-core-assert.sh → run-debug.sh（core.assert case，`bedb40ca`，L2 验证 run-debug.sh exit=0）② 删除 run-io-driver-process.sh（完全冗余：hello.x 被 run-hello.sh 覆盖 + spawn_wait.x 被 run-process.sh L84 覆盖，`e546b013`）。剩余 6 个阻塞：2 bug（std.debug.println 返回字节数 vs main.x 期望 a==0 + fmt 命令一元负号 return -i → return - i）+ 4 类型不匹配（STD-061 perf + STD-047/091/092 gate 不适合合并功能测试脚本）。**副产品发现 2 bug 转后续修复波次**。**升钉决策**：❌ 不升钉（测试脚本工程债，非 ABI/link 改动；钉盘保持 `305cfbe1`）。详见 §3 #4 + §6 `e546b013` 行
- ③ **前波归档**：波 1 unsafe 包裹 9 处 @ `4214eea4`（main.x L670-695 6 处 expression+statement form + asm_libroot/std/fs/mod.x L22-39 3 处 statement form，L4 双端 + 升钉 `5cc73d54` → `4214eea4` + Windows 门禁待补跑）· C6 P0b audit @ `7e507fb6`（docs-only 5466 extern 实测 + 9 处违规 + 3 处不一致 + 3 波拆分策略）· shux-x 5GB 内存根因方案 A @ `a14a1dd0`（perf(ast_pool) 承自 `94a8e7d5`；init cap 调优非 ABI / 非 link 改动 → 不升钉；mac+Ubuntu 双端 bstrict 125 OK exit=0 + lang-const 17/17 ✓）详见 §6 `a14a1dd0` 行 · 日终 L4 真冷全测双端全绿 @ `94a8e7d5`（mac arm64 + Ubuntu x86_64 各 `run-all-bstrict OK (125 scripts)` exit=0；mac L4 阻塞修复 = `src/asm/backend_enc_dispatch.o` stale `rm + make` 重建）详见 §6 L4 兜底行 · 5 波 C5 CTFE（`229708f1` EXPR_MATCH / `a8a67708` EXPR_STRUCT_LIT / `d7a2388e` EXPR_ENUM_VARIANT / `c174e70a` EXPR_TERNARY+IF+BLOCK 三连击 + `681b168f` EXPR_CLOSURE scoping docs）。

**已入钉盘（`305cfbe1`）**：C2 std.net PRIMARY UNDEF gate · C3=C4 parser bare-`{` struct-lit · backtrace L4 cold · C8 vec/set/map/queue duplicate · C6 P0b 波 1 unsafe 包裹 9 处 · **C6 P0b 波 2 ABI 一致性 3 处**（详见 §6 变更记录）。

**C1 复核结论**：T10 候选 C1（build error exit code 传播）在 `a74e25a1` 已修（`main_argv_has_o_flag` + `main_cmd_run` 显式 -o compile-only；build/run/bare 三路径 build error 都返回非 0），无需再做。

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](自举方法.md) | Cap / R / L / M |
| [自举步骤.md](自举步骤.md) | 可执行闸门 |
| **本文** | 长期 KPI + 前排（**每波必改**） |
| [当前进度.md](当前进度.md) | 当天快照（**每波必改**） |
| **[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)** | **删种子前先跑 Windows hybrid**；最小门禁 vs 125 金标；三阶段与 C6 触发（**决议**） |
| [自举进度-归档-2026-07-18.md](自举进度-归档-2026-07-18.md) | **18 号全日**（只读） |
| [自举进度-归档-2026-07-17.md](自举进度-归档-2026-07-17.md) | 17 号全日（只读） |
| [自举进度-归档-2026-07-16.md](自举进度-归档-2026-07-16.md) | 16 号全日（只读） |
| [自举进度-归档-2026-07-14.md](自举进度-归档-2026-07-14.md) | 14 号整表（只读） |
| [自举进度-归档-2026-07-13.md](自举进度-归档-2026-07-13.md) | G-02f thin 流水账（只读） |
| [问题分析文档.md](问题分析文档.md) | 产品/大债地图（**非每波**） |
| skill `shux-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **125** | `tests/run-all-bstrict.sh` |
| 产品 L4 放行钉盘 | **`305cfbe1`** | 19 日升钉（前钉 `4214eea4`）；双端 L4 真冷 + 125 全绿；本波 ABI 一致性 → 升钉 |
| 本波 tip | **`305cfbe1`**（fix(abi) 波 2 ABI 一致性 承自 `4214eea4`） | mac+Ubuntu 双端 L4 真冷全绿（ABI 边界 → 升钉 `4214eea4` → `305cfbe1`） |
| 本波 mac L4（波 2） | ✅ **`run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0** @ `305cfbe1` | 全擦 .o + 删二进制 + bootstrap-driver-seed OK (shux 3656096 bytes, seed smoke exit=42) + g05_prepare_and_relink OK (58 objs Darwin/arm64) + bstrict 125；log `/tmp/mac_true_bstrict_305cfbe1.log` |
| 本波 Ubuntu L4（金标 · 波 2） | ✅ **`run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0** @ `305cfbe1` | git pull + 全擦 .o + 删二进制 + bootstrap-driver-seed OK (shux 3490000 bytes) + g05_prepare_and_relink OK (60 objs Linux/x86_64) + bstrict 125；**link-hardening gate OK（Linux 专有）**；log `/tmp/ubuntu_true_bstrict_305cfbe1.log` |
| 日终 L4 mac arm64 | ✅ **`run-all-bstrict OK (125 scripts)` exit=0** @ `94a8e7d5` | 全擦 .o + 删二进制 + pin seeds + bootstrap + g05 + 矩阵 + bstrict 125；log `/tmp/mac_true_bstrict_94a8e7d5.log`；阻塞修复：`src/asm/backend_enc_dispatch.o` stale → rm + make 重建 |
| 日终 L4 Ubuntu x86_64（金标） | ✅ **`run-all-bstrict OK (125 scripts)` exit=0** @ `94a8e7d5` | 全擦 .o + 删二进制 + git pull + pin seeds + bootstrap + g05 + 矩阵 + bstrict 125；log `/tmp/ubuntu_true_bstrict_94a8e7d5.log` |
| 工程轨 KPI（T/N/R2/M） | 继承 18 号结案 | 本波未动工程轨 |

### 2.2 工程轨（继承 18 号结案）

| KPI | **现** | 备注 |
|-----|--------|------|
| **T** | **18/18** | ✅ 满 |
| **EMPTY** | **18/18** | ✅ 满 |
| **N** | **54/54 IDENTICAL** @ 18 号 tip | 回归基线 |
| **R2 / H** | Cap pure 收口；WPO ✅；typeck/codegen M ✅ | 18 号结案状态 |

***

## 3. 当前前排（≤4 条）

1. ✅ **shux-x 5GB 内存根因方案 A 已完成**（双端 L2 全绿 @ `a14a1dd0`）：拆分 `AST_POOL_INIT_CAP=256`（init 默认）与 `AST_POOL_GROW=4096`（grow step 保留），per-ArenaSidecar init 4.3MB → 270KB（减 94%）；50 deps init RSS 215MB → 14MB。mac+Ubuntu 双端 bstrict 125 OK exit=0 + lang-const 17/17 ✓。不升钉（init cap 调优非 ABI / 非 link 改动）。详见 [当前进度.md](当前进度.md) §本波摘要
2. ✅ **日终 L4 真冷全测（mac + Ubuntu 双端）已完成**（双端全绿 @ `94a8e7d5`）：本日 4 波 C5 fold + 1 docs 兜底。双端 L4 全过 `run-all-bstrict OK (125 scripts)` exit=0；不升钉（CTFE 扩展非 ABI / 非 link 改动）
3. **C6 X ABI P0b（波 2 ✅ → 推进 Windows 门禁补跑）**：[C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) 实测 5466 extern 声明；核心 14 个 C ABI 调用全部已标注 ✅；3 波拆分中 **波 1 ✅ @ `4214eea4`** + **波 2 ✅ @ `305cfbe1`**（ABI 一致性 3 处 runtime_io_abi.x L164/180/196 定义改 `export extern "C" function`，ABI 边界，L4 双端 + 升钉 `4214eea4` → `305cfbe1` + Windows 门禁待补跑）→ **波 3** std/ ~557 标注（远期顺延自举后 P1-P2）。**自举前 ABI 风险已解除**：波 1 + 波 2 完成后，P1 激活 typeck 强制 unsafe + X ABI 调用约定时不再有 bootstrap 陷阱 → 仅剩 Windows 门禁补跑（波 1+波 2 合并）+ 波 3 远期
4. ✅ **C7 plan 瘦身 PoC 完成**（工程债 · 非产品 · P3 · 路径 C 重构合并 · 2/8 完成 @ `e546b013`）：[C7-plan-audit.md](C7-plan-audit.md) audit 881 总脚本 vs 125 白名单 vs 752 非白名单（599 gate + 94 other + 24 perf + ...）；125 白名单内**无冗余**；8 个完全无引用 dead code 候选中 2/8 完成：① 合并 run-core-assert.sh → run-debug.sh（core.assert case，`bedb40ca`，L2 验证 exit=0）② 删除 run-io-driver-process.sh（完全冗余：hello.x 被 run-hello.sh 覆盖 + spawn_wait.x 被 run-process.sh L84 覆盖，`e546b013`）；剩余 6 个阻塞（2 bug + 4 类型不匹配）转后续。**副产品发现 2 bug 转后续修复波次**：std.debug.println 返回字节数 vs main.x 期望 a==0 + fmt 命令一元负号 return -i → return - i。**非 ABI/link → 不升钉**（钉盘保持 `305cfbe1`）。详见 [当前进度.md](当前进度.md) §本波摘要

**已收口（19 日 tip）**：**C6 X ABI P0b 波 2 ABI 一致性 3 处（`305cfbe1` 升钉）** · C6 X ABI P0b 波 1 unsafe 包裹 9 处（`4214eea4` 升钉） · shux-x 5GB 方案 A（`a14a1dd0`）· C5 EXPR_MATCH CTFE（`229708f1`）· C5 EXPR_STRUCT_LIT 字段 CTFE 扩全（`a8a67708`）· C5 EXPR_ENUM_VARIANT CTFE（`d7a2388e`）· C5 EXPR_CLOSURE scoping（`681b168f` docs）· **C5 EXPR_TERNARY+IF+BLOCK CTFE 三连击（`c174e70a`）** —— 详细见 [当前进度.md](当前进度.md) §本波摘要；日终 L4 双端兜底已过

**不在前排**：
- **C5 EXPR_CLOSURE CTFE**（独立大工程 · 多波）：已 scoping（[C5-EXPR_CLOSURE-CTFE-scoping.md](C5-EXPR_CLOSURE-CTFE-scoping.md)）。SHUX 当前无 closure 支持任何层（无 parser 语法、无 AST 节点、无 TYPE_FN、无 typeck、无 codegen）。需 5 波独立工程：① fn 类型系统（1.5 波）→ ② EXPR_CLOSURE AST+parser（1.5 波）→ ③ typeck capture analysis（2 波）→ ④ codegen lowering（2 波）→ ⑤ CTFE fold（0.5 波，本 Cap）。总估时 ≈ 7.5 波。顺延自举后立项 `lang-closure-v1-rfc.md`，明确语法选择（Rust `|x|...` / Swift `{x in ...}` / Zig `fn(x) {...}`）+ capture 策略 + Linear/owned move 语义 + region 推断交互
- **Cap C9 match guard full-chain 支持**（独立大工程 · 多波）：经调查 guard 不在 cap-candidates C5 范围内。X pipeline `MatchArmEntry` 5 字段无 `guard_ref`；`pipeline_expr_append_match_arm` 签名无 guard 参数；`parser_parse_match_into_glue` extern 声明无定义；ELF emit / CTFE handler 均不处理 guard。要完整支持 guard 需要 6 个产生点改动（ast_pool 加字段 + append 加参数 + parser 实装 + typeck scope + emit 分支 + CTFE handler），**多波拆分**，先排期再动手
- tip L4 **`305cfbe1`**（= 钉盘；本波 `305cfbe1` 升钉——ABI 边界改动）；C2 std.net UNDEF gate + C3=C4 parser bare-`{` struct-lit + backtrace L4 cold + C8 vec/set/map/queue duplicate + C6 P0b 波 1 unsafe 包裹 9 处 + **C6 P0b 波 2 ABI 一致性 3 处** 均已入钉盘。

**不在前排**：18 号的 typeck CTFE / WPO / ASM PRIMARY / named_layout / slice / anon struct backfill / main_cmd_run -o 全部已入 18 号归档。

***

## 4. 与 18 号归档的关系

- 18 号全日自举仪表盘 + 变更流水已整表移入 [`自举进度-归档-2026-07-18.md`](自举进度-归档-2026-07-18.md)；**勿把归档内中间 tip / 单叶日志当 19 日现绿**。
- 产品 L4 放行 tip 仍以本文为准：18 日结案钉盘 `c51759eb` → 19 日凌晨 `a74e25a1`（同 18 号结案升钉序列的延续，归档快照截止 18 日 21:46 `b3540784` 钉盘 `c51759eb`；`a74e25a1` 是 19 日凌晨的升钉，本文承接）。
- 19 日新工作（C2 + 文档归档/起表）从本文起记录。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
|------|----------|
| **macOS arm64** | **L4 真冷**（波 2 @ `305cfbe1`）：全擦 .o（compiler/std/core/build_asm 共 0 remaining）+ 删二进制（shux/shux_asm/shux-c/bootstrap_shuxc）+ `bootstrap-driver-seed OK` (shux 3656096 bytes, seed smoke exit=42) + `g05_prepare_and_relink OK (shux_asm OK)` (58 objs Darwin/arm64) + `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0；log `/tmp/mac_true_bstrict_305cfbe1.log` |
| **Ubuntu x86_64（金标）** | **L4 真冷**（波 2 @ `305cfbe1`）：git pull `305cfbe1` Fast-forward + 全擦 .o + 删二进制 + `bootstrap-driver-seed OK` (shux 3490000 bytes) + `g05_prepare_and_relink OK (shux_asm OK)` (60 objs Linux/x86_64) + `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0；**link-hardening gate OK（Linux 专有，Type=DYN stack=0x1）**；log `/tmp/ubuntu_true_bstrict_305cfbe1.log` |
| **Windows（探针 · 非自举金标）** | **本波 ABI 边界 → 必须跑** Windows hybrid 最小门禁（波 1+波 2 合并补跑，待执行）。**策略（强制）**见下 |

### 5.1 Windows 与删种子（策略锚点 · 非本波 KPI）

> **决议全文**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)

| 项 | 口径 |
|----|------|
| **删种子 / G 清场前** | **必须**先把 Windows **B-hybrid 最小门禁**跑通并保持可回归（阶段 1 DoD）；**禁止**「先彻底删种子，再第一次认真碰 Windows」 |
| **现在** | **可以**跑 hybrid 探针；不必等完全自举 / tip 升钉 |
| **最小门禁（权威）** | MSYS2 / `windows-server` 上：`./tests/run-bootstrap-bstrict-windows-gate.sh`（默认 hybrid） |
| **不是** | Windows 上 `./tests/run-all-bstrict.sh`（~125）当阶段 1 主验收；Windows L4 钉盘；宣称 Win 已零 C / 已自举 |
| **金标仍是** | Ubuntu（+ SHARED 时 mac）**125** `run-all-bstrict` + L4 |
| **强制重跑 Windows 门** | **C6 / ABI / link / `std.sys`** 大改前后；**宣称 G / 删种子**之前；阶段 2 合并点 |
| **进度话术** | Windows hybrid 绿 = **探针可用**；**≠** 产品 tip L4 / 可自举 |

***

## 6. 变更记录（倒序 · 19 日起；18 号见归档）

| 日期 | SHA | 摘要 |
|------|-----|------|
| 2026-07-19 | **`e546b013`** + **`bedb40ca`** | **C7 plan 瘦身 PoC · 路径 C 重构合并 · 2/8 完成 · 非升钉**：按 [C7-plan-audit.md](C7-plan-audit.md) 路径 C 重构合并，audit 881 总脚本 vs 125 白名单 vs 23 W3 fast vs 752 非白名单（599 gate + 94 other + 24 perf + ...）；125 白名单内**无冗余**；23 W3 fast 子集 ⊆ 125（包含关系，非冗余）。8 个完全无引用 dead code 候选中 2/8 完成：① 合并 run-core-assert.sh → run-debug.sh（core.assert case，`bedb40ca`，L2 验证 run-debug.sh exit=0；新增 tests/run-debug.sh `core.assert` 段移植 + std.debug 段阻塞标注）② 删除 run-io-driver-process.sh（完全冗余：hello.x 被 run-hello.sh 覆盖 + spawn_wait.x 被 run-process.sh L84 覆盖，`e546b013`）。剩余 6 个阻塞：2 bug（std.debug.println 返回字节数 vs main.x 期望 a==0 + fmt 命令一元负号 return -i → return - i）+ 4 类型不匹配（STD-061 perf + STD-047/091/092 gate 不适合合并到功能测试脚本，需评估路径 A 纳入白名单扩容 125→129）。**副产品发现 2 bug 转后续修复波次**。**升钉决策**：❌ 不升钉（测试脚本工程债，非 ABI/link 改动；钉盘保持 `305cfbe1`）。**L2 验证**：run-debug.sh 单独验证通过（SHUX=shux_asm：core.debug + core.assert + debug test OK，exit=0）。**tip HEAD**：`e546b013`。权威 tests/run-debug.sh（合并 core.assert）· tests/run-core-assert.sh（已删除）· tests/run-io-driver-process.sh（已删除）· analysis/C7-plan-audit.md（§0-§8 全文） |
| 2026-07-19 | **`305cfbe1`**（升钉） | **C6 X ABI P0b 波 2 · ABI 一致性修复 3 处 · 双端 L4 真冷 + 125 bstrict 全绿 · 升钉 `4214eea4` → `305cfbe1`**：按 [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.3 波 2 推荐执行方案修 ABI 一致性违反 3 处：`compiler/src/runtime_io_abi.x` L164 (`fs_posix_close_c`) / L180 (`fs_posix_read_c`) / L196 (`fs_posix_write_c`) 共 3 处定义 `export function fs_posix_*_c` → `export extern "C" function fs_posix_*_c`（保留 `#[no_mangle]`），与 5 个声明点（main.x:30-31 / pipeline.x:81-82 / driver/emit.x:71-73）对齐；每处加 4 行英文 ABI 注释（契约依据 + 委托 `std_fs_fs_*` 设计理由）。**函数体不变**：调用 `std_fs_fs_close/read/write` 是 SHUX X-ABI 桥接（默认 X ABI），底层 libc `close/read/write` 已在 `std_fs_fs_close` (L99-105) / `std_fs_fs_read` (L124-135) / `std_fs_fs_write` (L145-156) 内部用 `unsafe { }` 包裹。**契约依据**：声明 5 处全部 `extern "C"` ↔ 定义 3 处缺失 → P0a 语义降级下不断链；P1 激活 `typeck.x:289 pipeline_typeck_check_extern_call_unsafe_boundary_c` 强制 `extern "C"` 调用约定后参数寄存器错位 / 运行期 UB。**双端 L4 真冷全绿**：mac arm64 全擦 .o + 删二进制 + `bootstrap-driver-seed OK` (shux 3656096 bytes, seed smoke exit=42) + `g05_prepare_and_relink OK (shux_asm OK)` (58 objs Darwin/arm64) + `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0（log `/tmp/mac_true_bstrict_305cfbe1.log`）；Ubuntu x86_64 git pull `305cfbe1` Fast-forward + 全擦 .o + 删二进制 + `bootstrap-driver-seed OK` (shux 3490000 bytes) + `g05_prepare_and_relink OK (shux_asm OK)` (60 objs Linux/x86_64) + `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0（log `/tmp/ubuntu_true_bstrict_305cfbe1.log`；**link-hardening gate OK Linux 专有**）。**升钉决策**：ABI 一致性改动属 ABI 边界 → 必须升钉 `4214eea4` → `305cfbe1`（per [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.3 + [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md) §5.1）。**Windows 门禁**：🟡 待补跑（ABI 边界 per §5.1 强制重跑 hybrid 最小门禁 `tests/run-bootstrap-bstrict-windows-gate.sh`；非阻塞升钉，与波 1 合并补跑）。**波次**：波 2 of 3（audit `7e507fb6` → 波 1 `4214eea4` ✅ → **波 2 `305cfbe1` ✅** → 波 3 std/ ~557 远期顺延自举后 P1-P2）。**自举前 ABI 风险已解除**：波 1 + 波 2 完成后，P1 激活 typeck 强制 unsafe + X ABI 调用约定时不再有 bootstrap 陷阱。权威 compiler/src/runtime_io_abi.x:164（fs_posix_close_c 定义 + 4 行英文 ABI 注释）· :180（fs_posix_read_c 定义 + 4 行英文 ABI 注释）· :196（fs_posix_write_c 定义 + 4 行英文 ABI 注释） |
| 2026-07-19 | **`4214eea4`**（升钉） | **C6 X ABI P0b 波 1 · unsafe 包裹修复 9 处 · 双端 L4 真冷 + 125 bstrict 全绿 · 升钉 `5cc73d54` → `4214eea4`**：按 [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.2 波 1 推荐执行方案修复 P0 契约违反 9 处 unsafe 包裹违规：① `compiler/src/main.x` L670-695 共 **6 处**（`fs_posix_write_c` ×4 + `fs_posix_close_c` ×2，函数 `driver_run_x_emit_x` 函数体内）—— 用 expression form `let written: isize = unsafe { fs_posix_write_c(...) };` + statement form `unsafe { fs_posix_close_c(fd); }`（与 `std/net/ws_io.x:102,123` 一致：`let n: isize = unsafe { send(fd, buf + sent, ...) };`）；② `compiler/asm_libroot/std/fs/mod.x` L22-39 共 **3 处**（`open`/`read`/`write` 三个封装函数直接 return）—— 用 statement form `unsafe { return fs_open_read_c(path); }`（与同文件 L17-19 `fs_mod_close` 一致：`function fs_mod_close(fd: i32): i32 { unsafe { return close(fd); } }`）。**契约依据**：`typeck.x:289 pipeline_typeck_check_extern_call_unsafe_boundary_c` 已声明但未激活（P0a 语义降级），P1 激活会断链；C ABI unsafe 是语义契约（C 可能修改 SHUX 内存 / 指针有效性 / 所有权转移 / 内存布局），非 codegen 问题，IR 无法自动处理（4 个不可判定问题）。**双端 L4 真冷全绿**：mac arm64 全擦 .o + 重链 shux/shux_asm/shux-c/bootstrap_shuxc + bootstrap-driver-seed OK + g05_prepare_and_relink OK (shux_asm OK) + `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0（log `/tmp/mac_true_bstrict_7e507fb6.log`；注：日志名沿用 worktree SHA 7e507fb6，因 bstrict 跑时 commit 已 push 但 worktree 本地 SHA 计算时机差；实际验证的是 4214eea4 commit 后的代码）；Ubuntu x86_64 git pull + 全擦 .o + 重链 + cp seeds `codegen_gen.linux.x86_64.c`/`parser_gen.linux.x86_64.c` + bootstrap-driver-seed OK + g05 OK + `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0（log `/tmp/ubuntu_true_bstrict_4214eea4.log`）。**升钉决策**：unsafe 契约改动属 ABI 边界 → 必须升钉 `5cc73d54` → `4214eea4`（per [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.2 + [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md) §5.1）。**Windows 门禁**：🟡 待补跑（ABI 边界 per §5.1 强制重跑 hybrid 最小门禁 `tests/run-bootstrap-bstrict-windows-gate.sh`；非阻塞升钉，需在波 2 收口前补跑或与本波一起补跑）。**波次**：波 1 of 3（audit `7e507fb6` → **波 1 `4214eea4`** → 波 2 ABI 一致性 3 处 runtime_io_abi.x L164/176/188 改 extern "C" → 波 3 std/ ~557 远期顺延自举后 P1-P2）。权威 compiler/src/main.x:670-695（6 处 expression+statement form unsafe 包裹）· compiler/asm_libroot/std/fs/mod.x:22-39（3 处 statement form unsafe 包裹） |
| 2026-07-19 | **docs（C6 P0b audit）** | **C6 X ABI P0b 范围审计 · 3 波拆分策略 · docs-only 无代码改动**：全量扫描 `compiler/src` + `compiler/asm_libroot` + `std` 实测 extern 声明：`extern "C" function`（C ABI）**3035** + `extern function`（X ABI 默认）**2431** + `extern "x" function`（显式 X ABI）**0** = 合计 **5466**。核心 14 个 C ABI 调用（设计文档 §3.2.2 定义：parser.x:36-38 / main.x:30-31 / pipeline.x:81-82 / driver/emit.x:71-73 / asm_libroot/std/fs/mod.x:11-14）**全部已标注** `extern "C"` ✅。发现 **9 处 unsafe 包裹违规**（P0 契约违反）：`main.x` L671/672/677/678/685/690 共 6 处 + `asm_libroot/std/fs/mod.x` L23/33/38 共 3 处；当前 P0a typeck 未激活强制 unsafe 所以不断链，P1 激活会断链（`typeck.x:289 pipeline_typeck_check_extern_call_unsafe_boundary_c` 已声明未启用）。发现 **3 处声明/定义 ABI 不一致**：`main.x:30-31` + `pipeline.x:81` + `driver/emit.x:71` 声明 `extern "C"` vs `runtime_io_abi.x:164/176/188` 定义 `export function`（X ABI 默认）；P0a 语义降级下不断链，P1 激活 X ABI 调用约定后参数寄存器错位 / 运行期 UB。std/ 扩展范围 ~557 个 C 库包装待标注（http 249 / socketio 106 / async 42 / json 37 / math 32 / sqlite 32 / atomic 30 / arrow 29 + 其他 ~32），远期债，自举前不强制（P0a 语义降级保护）。正面对照：`parser.x` Cap-T001 whole-body unsafe FFI gate（~270 个 unsafe { } 块）/ `driver/emit.x` thin unsafe wrap / `runtime_io_abi.x` 全部包裹 ✅。**拆分策略 3 波**：波 1 unsafe 修复 9 处（main.x 6 + mod.x 3，ABI 边界，L4 双端 + 升钉 + Windows 门禁）→ 波 2 ABI 一致性 3 处（runtime_io_abi.x 定义改 extern "C"，L4 双端 + 升钉 + Windows 门禁）→ 波 3 std/ ~557 标注（顺延自举后 P1-P2，按文件大小递增分波，std/atomic 30 个作 PoC）。**升钉决策**：本波 docs-only 无代码改动 → 不升钉；钉盘保持 `5cc73d54`。文档 [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md)（§0 摘要 + §1 实测统计 + §2 核心 14 审计 + §3 ABI 一致性 + §4 std/ 扩展 + §5 3 波拆分 + §6 风险评估 + §7 验证清单 + §8 调查方法 + §9 相关文档） |
| 2026-07-19 | **`a14a1dd0`**（L2 daily） | **shux-x 5GB 内存根因方案 A · mac+Ubuntu 双端 L2 全绿（非放行 L4）**：推进 [shux-x-5GB-内存根因调查.md](shux-x-5GB-内存根因调查.md) §7 推荐顺序「A → B，C/D 顺延自举后」首项。`compiler/ast_pool.c` 三处改动：① L18-40 新增 `AST_POOL_INIT_CAP 256` 常量 + 3-dim 英文 docblock（Why/Invariant/Asm-Perf/PLATFORM: SHARED）；② L140-145 改 `grow_vec_init` default fallback：`initial_cap = AST_POOL_GROW` → `initial_cap = AST_POOL_INIT_CAP`（init 路径走 calloc zerofill 小）；③ 80 处 `grow_vec_init(..., AST_POOL_GROW)` → `grow_vec_init(..., AST_POOL_INIT_CAP)`（Edit replace_all=true）。**保留** `grow_vec_ensure` 内 `AST_POOL_GROW`（L173/175，grow step 不变）—— 池超 INIT_CAP 按 4096 元素线性扩容，保持 amortized O(1)。不变量 `0 < AST_POOL_INIT_CAP (256) <= AST_POOL_GROW (4096)`。内存收益：per-ArenaSidecar init 4.3MB → 270KB（减 94%）；50 deps init RSS 215MB → 14MB；OneFuncSidecar / ModuleSidecar / DepCtxSidecar 同步受益。不解决「ArenaSidecar 永不释放」根因（留给方案 B），只削减 init 浪费。mac L2：rm 3 .o + make rebuild + g05 OK + 矩阵 rv=42/opt/si/hello=0 + lang-const 17/17 ✓ + bstrict 125 OK exit=0（log `/tmp/mac_bstrict_A_c149badc.log`）；Ubuntu L2：git pull + rm 3 .o + make rebuild + g05 OK + lang-const 17/17 ✓ + bstrict 125 OK exit=0（log `/tmp/ubuntu_bstrict_A_a14a1dd0.log`）。**升钉决策**：init cap 调优非 ABI / 非 link 改动 → 不升钉；钉盘保持 `5cc73d54`。常量统计：AST_POOL_INIT_CAP=84 处（1 define + 1 default fallback + 80 grow_vec_init + 2 注释）；AST_POOL_GROW=9 处（2 define + 5 注释 + 2 grow_vec_ensure）。权威 compiler/ast_pool.c:18-40 + :140-145 + 80 处 grow_vec_init 调用 + grow_vec_ensure L173/L175 |
| 2026-07-19 | **L4 兜底（无 commit）** | **日终 L4 真冷全测双端全绿 @ `94a8e7d5`**（本日 4 波 C5 fold + 1 docs 兜底；mac arm64 + Ubuntu x86_64 双端各跑 `run-all-bstrict OK (125 scripts)` exit=0；矩阵 rv=42 / opt / si / hello 全绿）。**mac L4 阻塞修复（环境债，非回归）**：链接阶段报 4 个 x86 SIMD 浮点 backend symbol 未定义（`_backend_enc_mulsd_rax_rbx_arch` / `_backend_enc_subsd_rax_rbx_arch` / `_backend_enc_subsd_rbx_rax_arch` / `_backend_enc_ucomisd_rbx_rax_arch`，referenced from `glue_emit_binop_*_elf_c` in `bootstrap_seed_pipeline_filtered.o`）。根因：`src/asm/backend_enc_dispatch.o` stale（前一版 build 时 mulsd/subsd/ucomisd 还未加入 seed，或 `ld -r` merge 步骤漏 merge）；4 symbols 在 `seeds/backend_enc_dispatch.from_x.c:922-1010` 已定义且**不**在 `#ifndef SHUX_L2_ENC_DISPATCH_THIN_FROM_X` guard 内。修复：`rm src/asm/backend_enc_dispatch.o && make src/asm/backend_enc_dispatch.o`（thin.x + seed-rest 路径重新 `ld -r` 合并）→ 4 symbols 全部出现 → g05 relink OK。**升钉决策**：4 波均为 typeck CTFE 扩展（非 ABI / 非 link 改动）→ 不升钉；钉盘保持 `5cc73d54`。证据日志：mac `/tmp/mac_true_bstrict_94a8e7d5.log` / Ubuntu `/tmp/ubuntu_true_bstrict_94a8e7d5.log` |
| 2026-07-19 | **docs（C5 closure scoping）** | **C5 EXPR_CLOSURE CTFE 排期/拆分文档 · 结论「不启动」**：核查 SHUX 当前**无任何 closure 支持**（无 parser 语法 / 无 AST 节点 / 无 TYPE_FN / 无 typeck / 无 codegen / 无 RFC）。codebase 中 "closure" 字眼全是依赖图传递闭包。函数指针走 C-style `*u8` + `extern "C"` 路径（`shux_driver_call_fn_void_arg` 等），无 capture / 无 environment。完整 EXPR_CLOSURE CTFE 需 5 波前置工程：① fn 类型系统（1.5 波）→ ② EXPR_CLOSURE AST+parser（1.5 波，需语法 RFC）→ ③ typeck capture analysis（2 波，与 Linear/owned/region 交叉）→ ④ codegen lowering（2 波，ABI/WPO 交互）→ ⑤ CTFE fold（0.5 波，本 Cap）。总估时 ≈ 7.5 波，**非本日可启动**。顺延自举后立项 `lang-closure-v1-rfc.md`（语法三选一：Rust `\|x\|...` / Swift `{x in ...}` / Zig `fn(x) {...}`）。下一波转向更小 fold 扩展三连击（EXPR_TERNARY / EXPR_IF / EXPR_BLOCK，合计 ≈ 1 波）。文档 [C5-EXPR_CLOSURE-CTFE-scoping.md](C5-EXPR_CLOSURE-CTFE-scoping.md) |
| 2026-07-19 | **`d7a2388e`**（L2 daily） | **C5 EXPR_ENUM_VARIANT CTFE · mac+Ubuntu 双端 L2 全绿（非放行 L4）**：cap-candidates C5 子项「enum variant 常量折叠（match arm discriminant）」收口。`glue_is_const_expr_ref`（L13617-13622）加 FIELD_ACCESS case：用 `g_typeck_active_module` 预标记 `pipeline_expr_try_mark_enum_field_access`（解决白名单 typeck_gen L6839 先于 marker L6850 火的 chicken-and-egg）；`glue_typeck_fold_expr_ref`（L13917-13929）加 FIELD_ACCESS case：预标记 → 读 `pipeline_expr_enum_variant_tag_at` → stamp `const_folded_val=tag`、`const_folded_valid=1`；非 enum FIELD_ACCESS（obj.field）正确跳过；新增 `pipeline_typeck_active_module_c()` getter（L25631-25641）——`g_typeck_active_module` 是 static（L119），seed 不可见；L1573 加前向声明 `pipeline_expr_try_mark_enum_field_access`（定义在 ast_pool.c L4312，#include 在 L21848 晚于使用点）。**重要发现**：Darwin filtered pipeline 把 glue 强版 `pipeline_typeck_block_const_init_is_const_c` 局部化（`t` in nm），seed 弱版 `pipeline_typeck_const_expr_ref_strict_minimal` 才是被外部调用的实现 → seed 必须镜像同语义（per "seed 与 glue 副本同 commit" 铁律）：`seeds/pipeline_glue_strict_minimal.from_x.c` L343-351 加三 extern 前向声明 + L1903-1913 加 FIELD_ACCESS case。新增 lang-const case `c_enum_variant_const`（want=200）。mac L2：c_enum_variant_const=200 / lang-const 14/14；Ubuntu L2：c_enum_variant_const=200 / lang-const 14/14。权威 compiler/pipeline_glue.c:13617-13622（whitelist）+ :13917-13929（fold handler）+ :25631-25641（active_module getter）+ :1573（forward decl）· compiler/seeds/pipeline_glue_strict_minimal.from_x.c:343-351（extern decls）+ :1903-1913（Darwin mirror）· tests/lang-const/c_enum_variant_const.x · tests/baseline/lang-const-eval.tsv |
| 2026-07-19 | **`a8a67708`**（L2 daily） | **C5 EXPR_STRUCT_LIT 字段 CTFE 扩全 · mac+Ubuntu 双端 L2 全绿（非放行 L4）**：cap-candidates C5 子项「struct lit 字段 CTFE（已 part 修，扩全）」收口。`glue_is_const_expr_ref`（L13570-13580）加 STRUCT_LIT 白名单（递归校验所有 field init 是否 const）；`glue_typeck_fold_expr_ref`（L13825-13852，ARRAY_LIT 之后）加 STRUCT_LIT case：递归 fold 每个 field init（`pipeline_expr_struct_lit_init_ref`），**struct 节点本身不 stamp**（struct 无法塞入 i32 `const_folded_val`），mirror ARRAY_LIT 元素递归但跳过 scalar 强转 stamp；`pipeline_typeck_fold_expr_c` else-if 链加 STRUCT_LIT 递归分支（L14185-14191）。emit C 从 `.x = (A + 1), .y = (A * 3)` 折叠为 `.x = 3, .y = 6`（A=2 block const）。新增 lang-const case `c_struct_lit_const`（want=9）。mac L2：emit `.x = 3, .y = 6` / lang-const 14/14 / typeck OK（match_guard.x -E 仍绿）/ hello OK / bstrict 125 OK exit=0；Ubuntu L2：emit `.x = 3, .y = 6` / lang-const 14/14 / bstrict 125 OK exit=0。权威 compiler/pipeline_glue.c:13570-13580（whitelist）+ :13825-13852（fold handler）+ :14185-14191（fold_expr_c 递归）· tests/lang-const/c_struct_lit_const.x · tests/baseline/lang-const-eval.tsv |
| 2026-07-19 | **`229708f1`**（L2 daily） | **C5 EXPR_MATCH CTFE · mac+Ubuntu 双端 L2 全绿（非放行 L4）**：`glue_typeck_fold_expr_ref` 末尾加 EXPR_MATCH handler（const subject + const arms → fold 选中 arm 的常量；wildcard 仅 fallback；enum-variant arms 用 variant_index 比较）+ `pipeline_typeck_fold_expr_c` else-if 链加 EXPR_MATCH 递归分支（让 `match 2 { ... }` 类无块 const env 上下文也能 CTFE）。替换 ~30 字节 cmp/jmp 分发为单条 `mov imm32`；同时解锁父 binop 折叠。新增 lang-const case `c_match_const`（want=20）。mac L2：c_match_const=20 / lang-const 13/13 / typeck OK / 矩阵 rv=42+opt+si+hello=Hello World\n / bstrict 125 OK exit=0；Ubuntu L2：c_match_const=20 / lang-const 13/13 / typeck OK / 矩阵 rv=42+opt+si+hello / bstrict 125 OK exit=0。权威 compiler/pipeline_glue.c:14029-14104（handler）+ :14130-14136（递归）· tests/lang-const/c_match_const.x · tests/baseline/lang-const-eval.tsv |
| 2026-07-19 | **`5cc73d54`**（升钉） | **双端 L4 真冷 + 125 bstrict 全绿 · 升钉 `ea1f6827` → `5cc73d54`**：C8 fk0 GAP vec/set/map/queue duplicate symbol（runtime_link_abi need_vec/set/map/queue 块加 c_provides_core_mem + c_provides_std_heap 判断；之前无条件推 mem.o+heap.o → 生成 C 已 co-emit 体 → 31 duplicate）。Ubuntu L4 `run-all-bstrict OK (125)` exit=0；mac L4 `run-all-bstrict OK (125)` exit=0；vec_i32_push build=0 run=0。权威 compiler/seeds/runtime_link_abi.from_x.c |
| 2026-07-19 | **`ea1f6827`**（升钉） | **双端 L4 真冷 + 125 bstrict 全绿 · 升钉 `a74e25a1` → `ea1f6827`**：C2 std.net PRIMARY UNDEF gate（Makefile net.o 加 mod.x + runtime_link_abi use_line 根修 + shux_compile_std_module.sh mod.x struct 补全）+ C3=C4 parser bare-`{` struct-lit + compound-stmt → P001（parser.x:3455 LBRACE handler）+ backtrace L4 cold（runtime_link_abi C-backend heap chain 补推 page_mmap.o + asm_io_stubs）。Ubuntu L4 真冷 `run-all-bstrict OK (125 scripts)` exit=0（含 run-net OK）；mac L4 真冷 125 全绿（用户真机终端确认；Codex 沙箱 run-net 因网络 bind 限制 exit=1 非回归）；矩阵 rv=42/opt/si/hello/net_listen_bind/ctx_p0/backtrace 全绿。权威 compiler/Makefile · seeds/runtime_link_abi.from_x.c · scripts/shux_compile_std_module.sh · src/parser/parser.x:3455 |
| 2026-07-19 | **C3=C4 WIP（未提交）**     | **parser bare-`{` struct-lit + compound-stmt → 整函数 P001（C3=C4 同 bug）· mac L2 收口待 Ubuntu 金标**：`parse_body_lets_into`（parser.x:3238）无 LBRACE handler，bare `let p: Pair = { a: 0 };` 的 `{` 不匹配任何 init handler → let 静默丢弃 → 后跟 unsafe/while/for 致整函数 P001。铁证：`{a:0};if(p.b!=1)` 生成 C 无 `p`。根修：parser.x:3455 新增 LBRACE handler（从 `{` 调 parse_expr_into，经 `parser_asm_primary_lbrace_looks_like_block_c` disambiguate，与 IDENT 路径同样收尾）。重生链路：本机 `shux-x -x -E` 能重生（同步约 3-4s），strip 掉 self-test main 30 行即得 library form → parser_x.o OK → g05 OK。mac L2：之前全坏复现全绿（bare`{`+unsafe 任一顺序 · bare`{`+while · Pair{...}带前缀仍绿）；`tests/typeck/contextual_typing_p0.x -o` build=0（`_main undefined` 消失，剩 `_callee_u64` UNDEF 是独立 extern-not-linked 问题）；矩阵 rv=42/opt/si/hello/-E/shux run 全绿；mac bstrict 125 W3 `ok=22 skip=102 fail=0 wall=64s`（`/tmp/mac_bstrict_c34.log`）。**待**：seed `seeds/parser_gen.linux.x86_64.c` 同步 + push → Ubuntu L4 + 125 bstrict → 升钉；权威 compiler/src/parser/parser.x:3455 |
| 2026-07-19 | **C2 WIP（未提交）** | **std.net PRIMARY → UNDEF gate（C2）· mac L2 收口待 Ubuntu 金标**：cookbook `net_listen_bind.x -o` 之前 `_std_net_listen`/`_std_net_close_listener` undefined（BLD001）。根因双点（Makefile net.o 缺 mod.x + use_line 误跳变量声明行）+ 根修（Makefile + runtime_link_abi + shux_compile_std_module.sh mod.x struct 补全）。mac L2 矩阵全绿 + bstrict 125 W3 best-effort 绿；run-net.sh 在 W3 fast 子集 skip，单独跑 tests/net/main.x（bind 8080）本机 sandbox exit=1（a74e25a1 钉盘同文件同 exit=1 → 环境网络限制非回归）。C1 复核：a74e25a1 已修 build error exit code 传播。**待 push → Ubuntu L4 → 升钉**；权威 compiler/Makefile · seeds/runtime_link_abi.from_x.c · scripts/shux_compile_std_module.sh |
| 2026-07-19 | **docs（Windows 时序）** | 链入 [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)：§ 文首方法行 · §1 文档地图 · **§5.1 删种子前须跑通 Windows hybrid 最小门禁** · §7 禁止清单；本波仍未跑 Win 门（C5 非 ABI） |
| 2026-07-19 | **docs（归档+起表）** | 18 号全日归档入 `自举进度-归档-2026-07-18.md`；本文从 19 日起重新起表 |

***

## 7. 禁止清单（本阶段）

- [ ] 未用 18 号归档 KPI 当「当前仍绿」而不查 tip SHA
- [ ] 未把 C5 EXPR_ENUM_VARIANT / EXPR_STRUCT_LIT / EXPR_MATCH 日常 L2 当 tip L4 放行（typeck CTFE 扩展非放行级；SHARED 须双端 L4 才升钉）
- [ ] 未用 N=54 全表 prove 单独宣称可自举（须 L4 + 双端 bstrict）
- [ ] 未把 Stage2 ✅ / R2 抽检当完全自举
- [ ] 未改 nm 期望糊过真 residual
- [ ] 未把 mac 全表预期当金标准（Ubuntu 记账为准）
- [ ] 产品 SHARED 仍须双端 bstrict（本波 harness-only 可记，动编译器须重开）
- [ ] **未把日常 L2 写成 tip L4 放行**
- [ ] **未在删种子 / G 宣称前跳过 Windows hybrid 最小门禁**（见 [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md) · 本文 §5.1）
- [ ] **未把 Windows hybrid 绿写成「已自举 / 已零 C」**；未在 Win 上用全量 125 `run-all-bstrict` 当阶段 1 放行
