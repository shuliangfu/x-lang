# Xlang / X language 自举进度

> **本文 = 2026-09-15 日切归档**（**70** 行 §6）。现行 KPI／前排见 [`自举进度.md`](自举进度.md)。

> **更新**：2026-09-16 · **日切归档** · 钉盘 **`ecdb5cc1e`**（未升钉）· §6 收 15 日 **70** 行 · 9 月 16 日起主文档重写 · **金标 SSH＝`ubuntu-server`（Ubuntu 26.04）** · Ubuntu 验＝**git pull（禁 scp）**\
> **产品 L4 放行钉盘**：**`ecdb5cc1e`**（2026-09-13 晚 · 双端 **tip-egg** L4 真冷全绿：Ubuntu 10m23s＠md5 982a47b4＋Darwin 21m35s；bstrict 0 fail）· 前 **`5cac88d00`**／`fa1061224`\
> **日切时本波状态**（一行）：15 日主战场＝**Route C dest-buffer／T-shrink 连收**：P18d P010–P014／dup · P1f copy_token_bytes · P14c enum register · P6b struct name-match · P4uc unary wrap · P4bc binop wrap · P6c packed／soa · P3c type-inst mangle · P3d consume_qualified／angle close · P5e dest enum tag · P12i–P12u trait 链（self／rewrite／type_params／implements／bound／impl-seen／F3／method_on_param／skip_hoist／trait_check）· P5f parse_if_expr · P4tb ternary wrap · P4as as_suffix wrap · P4tc assign wrap · P3e TYPE_DYN wrap · P5g match wrap · P7c simd wrap · P6d library wrap · P4be suffix_loop XT001 pin · P4bf suffix_loop BODIES · P4bg IDENT 头 · **P4ud parse_unary dest-buffer**。另：P4b-egg 金标复现（侧支 egg 21.4G vs pin 蛋 7.4MB）收回主支。钉盘仍 **`ecdb5cc1e`**。\
> **下波候选**（日切时）：①主迁刀真减 host-cc（glue_tail 包装／type_ref parse／parse_block_return_end_tail／register_pending／dest-SLICE／anonymous-struct alloc／body_let_bracket；unary parse／IDENT 头／suffix_loop BODIES／library wrap／simd wrap／match wrap／TYPE_DYN wrap 已迁）②P4b 侧支 dest-buffer ＋ suffix_loop `&pending_n`（pin 蛋＋RSS 闸，不切金标）③MSVC va 立卡。vx／summit HARD BAN。日常闸门＝Darwin L2 矩阵＋Ubuntu 同序 L2。

***

## 6. 变更记录（**9 月 15 日**）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-15 | **docs(selfhost)** **SHA-sync P4ud tip 到 `6978e433d` — Darwin＋Ubuntu L2 绿（矩阵 5/5）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P4ud：parse_unary dest-buffer 迁 pthin_expr_unary.x（有则补全 P4uc）。P9a peek/step 走 unary 前缀；primary 走 unary.inc 零算法 ptr shim。C trampoline 留 AUDIT、写 next_lex。Darwin 矩阵 5/5＠5887896；Ubuntu 5/5＠6857400。hello／option `-E` 双端秒过。tip `6978e433d`。** | **代码** · **不升钉**。G.7：权威＝unary.x `parser_asm_parse_unary_x_into_c`；wrap＝P4uc；primary shim 零算法。禁 FORCE pabi mega。禁新 P 道。禁 dest-buffer parse_primary。禁并 wrap。实验室：Win11＋Ubuntu26 并行。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P4bg tip 到 `d1e872bfd` — Darwin＋Ubuntu L2 绿（矩阵 5/5）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P4bg：IDENT 头 `.x` 接线。根修 ident_x 填序 zeros THEN set_var_name（arena zeros 清 var_name_len；C 本地 wipe 不碰 name 槽）。C trampoline 写 next_lex。hybrid 跳过 C IDENT 孪生。Darwin 矩阵 5/5＠5886888；Ubuntu 5/5＠6828296。hello／option `-E` 双端秒过。tip `d1e872bfd`。** | **代码** · **不升钉**。G.7：权威＝primary.x `parser_asm_primary_ident_x_into_c`；trampoline 零算法只发布 cursor。禁 FORCE pabi mega。禁改 pabi zeros 清 var_name_len。禁新 P 道。实验室：Win11＋Ubuntu26 并行。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P4bf tip 到 `0a63ae30b` — Darwin＋Ubuntu L2 绿（矩阵 5/5）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P4bf：suffix_loop C trampoline 写 next_lex（C 孪生 stop 契约）＋g05 开 BODIES。P4be RSS＝IDENT 调用方陈旧 cursor 重解析 import method；不是 DOT＋METHOD 算法。Darwin 矩阵 5/5＠5886728；Ubuntu 5/5＠6803464。hello／option `-E` 双端秒过。IDENT 头仍 C。tip `0a63ae30b`。** | **代码** · **不升钉**。G.7：权威仍＝primary.x suffix_loop；trampoline 零算法只发布 cursor。禁把 IDENT 头 `.x` 当 extra。禁 FORCE pabi mega。实验室：Win11＋Ubuntu26 并行。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P4be tip 到 `dead44aa8` — Darwin＋Ubuntu L2 绿（矩阵 5/5）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P4be：suffix_loop `-E` XT001 根修（补 TOKEN／writer pin）＋INT 臂双 step／零 ref 修；IDENT 头不接线；g05 停放 BODIES（import-method RSS）。Darwin 矩阵 5/5＠5868520；Ubuntu 5/5＠6757456。tip `dead44aa8`。** | **代码** · **不升钉**。G.7：权威仍＝primary.x suffix_loop；产品仍 C 孪生。禁把 IDENT 头 `.x` 当 extra。禁 FORCE pabi mega。实验室：Win11＋Ubuntu26 并行。 |
| 2026-09-15 | **feat(selfhost)** **P6d Route C：library-shape TYPE_BOOL／TYPE_NAMED／VAR／FIELD／ENUM／EQ wrap dest-buffer 迁 pthin_fn_block.x（有则补全 P6c）。C trampoline 转发 scan name 缓冲。kind／var_name／field 走既有 pipeline_expr_*／pipeline_type_init_*。binop 走既有 P4bc set_binop_operands_c（G.7 各一 writer；禁 FORCE pabi mega；禁并 P4bc wrap／P5g match wrap／P3e TYPE_DYN wrap）。parse_one_function_library 仍 C。register_pending 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋typekind_field_compare run=0＠5851320；Ubuntu 5/5＋typekind_field_compare run=0＠6728464。tip `162982baf`。** | **代码** · **不升钉**。G.7：权威＝fn_block.x `parser_asm_library_bool_eq_shape_wrap_into_c`；C trampoline 零算法；set_binop_operands_c＝既有 P4bc（禁抄进 P6 seed）。禁新 P 道。禁抄 wrap 进 parse_type_ref／P15 scan。禁 dest-buffer parse。禁包 AUDIT。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P7c tip 到 `a00212def` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋at_builtin run=0）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P7c Route C：simd callee VAR＋CALL wrap dest-buffer 迁 pthin_simd.x（有则补全 P7b）。C trampoline 持 name[256]。fill 走既有 P7b callee_name_fill。kind／var_name 走既有 pipeline_expr_*。call_callee 走既有 pabi set_call_c。call_resolved=-1 走既有 init_call_resolve_at_ref（G.7 各一 writer；禁 FORCE pabi mega；禁并 P4b suffix CALL wrap）。parse_at_simd_builtin 仍 C。register_pending 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋at_builtin run=0＠5850584；Ubuntu 5/5＋at_builtin run=0＠6666240。tip `a00212def`。** | **代码** · **不升钉**。G.7：权威＝simd.x `parser_asm_simd_call_wrap_into_c`；C trampoline 零算法；set_call_c／init_call_resolve＝既有 pabi（禁抄进 P7 seed）。禁新 P 道。禁抄 wrap 进 parse。禁并 P4b CALL wrap。禁 dest-buffer parse。禁包 AUDIT。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P5g tip 到 `a206222a1` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋match OK run=2＋struct-field=0）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P5g Route C：match wrap dest-buffer 迁 pthin_ctrl.x（有则补全 P5f）。C trampoline 持 name[256]。kind／var_name／int_val／field_access 走既有 pipeline_expr_*。binop 走 P4bc set_binop（G.7 一 writer；禁 FORCE pabi mega；禁并 P4bc wrap）。match_matched_ref 走 P5 seed set_match_matched。parse_match 仍 C。register_pending 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋match OK run=2＋struct-field=0＠5850200；Ubuntu 5/5＋match OK run=2＋struct-field=0＠6641248。tip `a206222a1`。** | **代码** · **不升钉**。G.7：权威＝ctrl.x `parser_asm_match_var_wrap_into_c`／`parser_asm_match_field_wrap_into_c`／`parser_asm_match_lit_wrap_into_c`／`parser_asm_match_binop_wrap_into_c`／`parser_asm_match_expr_wrap_into_c`；C trampoline 零算法；set_match_matched＝P5 seed consumer-wave writer（禁抄进 pabi／typeck）。禁新 P 道。禁抄 wrap 进 parse。禁并 P4bc wrap／P5e dest-tag。禁 dest-buffer parse。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P3e tip 到 `e220c55a9` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋lang-trait OK＋lang-generic OK＋dyn_type／dyn_coerce／dyn_call run=7）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P3e Route C：TYPE_DYN wrap dest-buffer 迁 pthin_type_ref.x（有则补全 P3d）。C trampoline 持 name[256]。kind／named_name 走既有 pipeline_type_*。TYPE_DYN 写入走 P3 seed init_dyn（G.7 一 writer；禁 FORCE pabi mega；禁复用 init_compound）。parse_type_ref 仍 C。register_pending 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋lang-trait OK＋lang-generic OK＋dyn_type／dyn_coerce／dyn_call run=7＠5832344；Ubuntu 5/5＋lang-trait OK＋lang-generic OK＋dyn_type／dyn_coerce／dyn_call run=7＠6582720。tip `e220c55a9`。** | **代码** · **不升钉**。G.7：权威＝type_ref.x `parser_asm_wrap_registered_trait_as_dyn_into_c`／`parser_asm_alloc_dyn_type_ref_into_c`；C trampoline 零算法；init_dyn＝P3 seed consumer-wave writer（禁抄进 skip_tl／typeck）。禁新 P 道。禁抄 wrap 进 parse。禁并 P3c mangle。禁 dest-buffer parse。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P4tc tip 到 `a9b0b4a10` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋ternary 2/2＋compound-assign OK＋assign=42）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P4tc Route C：assign wrap dest-buffer 迁 pthin_expr_ternary.x（有则补全 P4tb）。C trampoline 持 parse_expr_result。binop left/right 走 P4bc set_binop（G.7 一 writer；禁 FORCE pabi mega；禁扩 P4bc wrap 加 line/col）。parse_assign 仍 C。register_pending 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋ternary 2/2＋compound-assign OK＋assign=42＠5831688；Ubuntu 5/5＋ternary 2/2＋compound-assign OK＋assign=42＠6528672。tip `a9b0b4a10`。** | **代码** · **不升钉**。G.7：权威＝ternary.x `parser_asm_assign_wrap_into_c`；C trampoline 零算法；set_binop＝P4bc seed consumer-wave writer（禁抄进 P4t seed）。禁新 P 道。禁抄 wrap 进 parse_assign。禁并 unary／binop／ternary wrap。禁 dest-buffer parse。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P4as tip 到 `0f9d8e6d1` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋as i32=42＋result OK）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P4as Route C：as_suffix 两条 wrap dest-buffer 迁 pthin_expr_as_suffix.x（P4as 道首体；TRY_PROPAGATE＋EXPR_AS 一波）。C trampoline 持 parse_expr_result。unary_operand 走 P4u set_unary；as_* 走 P4as seed set_as（G.7 各一 writer；禁 FORCE pabi mega）。parse_as_suffix 仍 C。assign wrap 仍 C。register_pending 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋as i32=42＋result OK＠5831256；Ubuntu 5/5＋as i32=42＋result OK＠6470584。tip `0f9d8e6d1`。** | **代码** · **不升钉**。G.7：权威＝as_suffix.x `parser_asm_try_propagate_wrap_into_c`／`parser_asm_as_wrap_into_c`；C trampoline 零算法；set_unary＝P4u seed；set_as＝P4as seed consumer-wave writer。禁新 P 道。禁抄 wrap 进 parse。禁并 unary／binop／ternary wrap。禁 dest-buffer parse。禁把 assign wrap 当 extra。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P4tb tip 到 `d9d9cf4e1` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋ternary 2/2＋compound-assign OK）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P4tb Route C：ternary wrap dest-buffer 迁 pthin_expr_ternary.x（P4t 道首体）。C trampoline 持 parse_expr_result。EXPR_TERNARY 走 pipeline_expr_set_*＋P5 seed set_if（G.7 一 writer；禁 FORCE pabi mega）。parse_ternary／assign wrap 仍 C。register_pending 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋ternary 2/2＋compound-assign OK＠5813800；Ubuntu 5/5＋ternary 2/2＋compound-assign OK＠6441336。tip `d9d9cf4e1`。** | **代码** · **不升钉**。G.7：权威＝ternary.x `parser_asm_ternary_wrap_into_c`；C trampoline 零算法；set_if＝P5 seed consumer-wave writer（禁抄进 P4t seed）。禁新 P 道。禁抄 wrap 进 parse_ternary。禁并 unary／binop wrap。禁并 skip_if_expr_finish。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P5f tip 到 `f129d574b` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋if-expr 4/4）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P5f Route C：parse_if_expr dest-buffer 迁 pthin_ctrl.x（有则补全 P5e）。C trampoline 持 parse_expr_result。wrap_block_ref＋EXPR_IF 走 pipeline_expr_set_*＋P5 seed consumer-wave writer。else-if 递归本 dest-buffer。register_pending 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋if-expr 4/4＠5813496；Ubuntu 5/5＋if-expr 4/4＠6412240。tip `f129d574b`。** | **代码** · **不升钉**。G.7：权威＝ctrl.x `parser_asm_parse_if_expr_x_into_c`＋`parser_asm_wrap_block_ref_as_expr_into_c`＋helper `skip_if_expr_finish`；C trampoline 零算法；set_block_ref／set_if＝P5 seed consumer-wave writer（禁 FORCE pabi mega）。禁新 P 道。禁抄 wrap 进 parse_unary／primary。禁并 P4uc unary wrap。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P12u tip 到 `625c2a97f` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＋default_probe run=42）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P12u Route C：trait_check_impls_complete dest-buffer 迁 pthin_skip_tl.x（有则补全 P12t）。C trampoline 传 g_xlang_skip_impl_*＋fat trait-reg 字节镜像＋stash src。dest-SLICE param/ret 形状核与 varargs diag 走 C helper。hoist 走 P12t trampoline。bound_scan/check 走历史 _c。register_pending 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＋default_probe run=42＠5812920；Ubuntu 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＋default_probe run=42＠6383096。tip `625c2a97f`。** | **代码** · **不升钉**。G.7：权威＝skip_tl.x `xlang_trait_check_impls_complete_into_c`＋helper `skip_trait_check_try_impl`／`skip_trait_check_try_method`／`skip_trait_check_find_method`；C trampoline 零算法；shape/diag＝语言限制的 dest-SLICE＋varargs。表 layout＝g_xlang_skip_impl_*＋g_xlang_skip_trait_reg[] 字节镜像。禁新 P 道。禁抄进 typeck／codegen。禁并 skip_hoist／method_on_param／F3 lookup。禁 FORCE pabi mega。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P12t tip 到 `5d0dc90d3` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＋default_probe run=42）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P12t Route C：skip_hoist_default_methods dest-buffer 迁 pthin_skip_tl.x（有则补全 P12s）。C trampoline 传 g_xlang_skip_impl_*＋fat trait-reg 字节镜像＋stash src。parse+commit 走 C inject_one（onefunc 按值）。self_matches_for 走 P12i dest-buffer。trait_check_impls_complete 仍 C。register_pending 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＋default_probe run=42＠5811816；Ubuntu 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＋default_probe run=42＠6353568。tip `5d0dc90d3`。** | **代码** · **不升钉**。G.7：权威＝skip_tl.x `xlang_skip_hoist_default_methods_into_c`＋helper `skip_hoist_try_impl`／`skip_hoist_try_method`／`skip_hoist_method_exists`；C trampoline 零算法；inject_one＝语言限制的 parse+commit。表 layout＝g_xlang_skip_impl_*＋g_xlang_skip_trait_reg[] 字节镜像。禁新 P 道。禁抄进 typeck／codegen。禁并 method_on_param／F3 lookup。禁把 trait_check 当 extra。禁 FORCE pabi mega。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P12s tip 到 `04c45ff54` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P12s Route C：method_on_param dest-buffer 迁 pthin_skip_tl.x（有则补全 P12r）。C trampoline 传 g_fn_bound_*＋fat trait-reg 字节镜像。type_param_index 走 P12k 历史 `_c`。method 走查在本函数（arity continue ≠ method_slot first-match）。register_pending 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＠5811112；Ubuntu 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＠6320224。tip `04c45ff54`。** | **代码** · **不升钉**。G.7：权威＝skip_tl.x `xlang_generic_bound_method_on_param_into_c`＋helper `skip_method_on_param_try_trait`／`skip_method_on_param_fill_ret`；C trampoline 零算法。表 layout＝g_fn_bound_*＋g_xlang_skip_trait_reg[] 字节镜像。禁新 P 道。禁抄进 typeck／codegen。禁并 bound_check_type_args／F3 lookup。禁复用 skip_copy_row64／ret_name_dest。禁 FORCE pabi mega。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P12r tip 到 `b9951f9ed` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P12r Route C：F3 dest-extras elem_array_dim dest-buffer 迁 pthin_skip_tl.x（有则补全 P12q）。C trampoline 传 fat trait-reg 字节镜像。wrap soup（ndims＝-2／0／dim_ix≥ndims）在单一 helper。method_on_param 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＠5794040；Ubuntu 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＠6295152。tip `b9951f9ed`。** | **代码** · **不升钉**。G.7：权威＝skip_tl.x `xlang_skip_trait_method_ret_elem_array_dim_into_c`／`param_elem_array_dim_into_c`＋helper `skip_trait_elem_array_dim_at`；C trampoline 零算法。表 layout＝g_xlang_skip_trait_reg[] 字节镜像＋P12g 偏移。禁新 P 道。禁抄进 typeck／codegen。禁包 method_on_param。禁并 simple ret／param array_dim。禁 FORCE pabi mega。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P12q tip 到 `489fea0e3` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P12q Route C：F3 simple getters dest-buffer 迁 pthin_skip_tl.x（有则补全 P12p）。C trampoline 传 fat trait-reg 字节镜像＋offsetof（slot i32／param i32／simple dims／name copy）。dest-extras elem_array_dim 仍 C。method_on_param 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＠5792920；Ubuntu 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＠6265960。tip `489fea0e3`。** | **代码** · **不升钉**。G.7：权威＝skip_tl.x `xlang_skip_trait_method_slot_i32_into_c`／`param_i32_into_c`／`ret_array_dim_into_c`／`param_array_dim_into_c`／`ret_name_dest_into_c`／`param_name_dest_into_c`；C trampoline 零算法。表 layout＝g_xlang_skip_trait_reg[] 字节镜像＋C offsetof。禁新 P 道。禁抄进 typeck／codegen。禁包 method_on_param。禁把 dest-extras dim 当 extra。禁 FORCE pabi mega。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P12p tip 到 `1fe849c69` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P12p Route C：F3 lookup dest-buffer 迁 pthin_skip_tl.x（有则补全 P12o）。C trampoline 传 fat trait-reg 字节镜像（stride＝sizeof(ent)／cap 16）。is_registered 仍薄包装。method_on_param／F3 标量 getter 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＠5791640；Ubuntu 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＠6236464。tip `1fe849c69`。** | **代码** · **不升钉**。G.7：权威＝skip_tl.x `xlang_skip_trait_find_reg_into_c`／`xlang_skip_trait_method_count_into_c`／`xlang_skip_trait_method_slot_into_c`／`xlang_skip_trait_method_name_dest_into_c`；C trampoline 零算法。表 layout＝g_xlang_skip_trait_reg[] 字节镜像＋P12g 偏移。禁新 P 道。禁抄进 typeck／codegen。禁包 method_on_param。禁把剩余 F3 标量 getter 当 extra。禁 FORCE pabi mega。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P12o tip 到 `0c54b7a16` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P12o Route C：bound_check dest-buffer 迁 pthin_skip_tl.x（有则补全 P12n）。C trampoline 传 g_call_*。method_on_param／F3 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＠5790952；Ubuntu 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＠6211280。tip `0c54b7a16`。** | **代码** · **不升钉**。G.7：权威＝skip_tl.x `xlang_generic_bound_check_into_c`；C trampoline 零算法。表 layout＝g_call_*。禁新 P 道。禁并 type_args。禁抄进 typeck。禁包 method_on_param。禁 FORCE pabi mega。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P12n tip 到 `749f517b4` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P12n Route C：impl-seen accessors dest-buffer 迁 pthin_skip_tl.x（有则补全 P12m）。C trampoline 传 g_xlang_skip_impl_*。method_on_param／F3／bound_check_c 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＠5790664；Ubuntu 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＠6182232。tip `749f517b4`。** | **代码** · **不升钉**。G.7：权威＝skip_tl.x `xlang_skip_impl_seen_count_into_c`／`xlang_skip_impl_trait_name_dest_into_c`／`xlang_skip_impl_for_type_dest_into_c`；C trampoline 零算法。表 layout＝g_xlang_skip_impl_*。禁新 P 道。禁并 concrete_implements。禁抄进 typeck／codegen。禁包 method_on_param。禁 FORCE pabi mega。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P12m tip 到 `f74274df8` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P12m Route C：bound_check_type_args dest-buffer 迁 pthin_skip_tl.x（有则补全 P12l）。C trampoline 传 g_fn_bound_*＋g_xlang_skip_impl_*。diag 走 C varargs helper。bound_check_c／method_on_param／accessors 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＠5790008；Ubuntu 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＠6148920。tip `f74274df8`。** | **代码** · **不升钉**。G.7：权威＝skip_tl.x `xlang_generic_bound_check_type_args_into_c`；C trampoline 零算法。表 layout＝g_fn_bound_*＋g_xlang_skip_impl_*。禁新 P 道。禁抄进 typeck。禁并 fat trait-reg。禁包 method_on_param。禁 FORCE pabi mega。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P12l tip 到 `1ba350d99` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋trait 2/0/0＋lang-trait OK）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P12l Route C：concrete_implements_trait dest-buffer 迁 pthin_skip_tl.x（有则补全 P12i）。C trampoline 传 g_xlang_skip_impl_* 并持 gnm[64]。复用 P12i self_matches_for。accessors／method_on_param／bound_check 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋trait 2/0/0＋lang-trait OK＠5838888；Ubuntu 5/5＋trait 2/0/0＋lang-trait OK＠6177328。tip `1ba350d99`。** | **代码** · **不升钉**。G.7：权威＝skip_tl.x `xlang_skip_impl_concrete_implements_trait_into_c`；C trampoline 零算法。表 layout＝g_xlang_skip_impl_*。禁新 P 道。禁抄进 typeck。禁并 P6b。禁并 trait-reg accessors。禁 FORCE pabi mega。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P12k tip 到 `67d170327` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P12k Route C：register_type_params＋type_param_index dest-buffer 迁 pthin_skip_tl.x（有则补全 P12d）。C trampoline 传 g_fn_gp_*。register_pending／method_on_param／bound_check 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＠5838552；Ubuntu 5/5＋trait 2/0/0＋lang-trait OK＋lang-generic OK＠6148264。tip `67d170327`。** | **代码** · **不升钉**。G.7：权威＝skip_tl.x `xlang_generic_func_register_type_params_into_c`／`xlang_generic_func_type_param_index_into_c`；C trampoline 零算法。表 layout＝P12d g_fn_gp_*。禁新 P 道。禁并 P1c pending。禁 FORCE pabi mega。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P12j tip 到 `537e91717` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋trait 2/0/0＋lang-trait OK）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P12j Route C：named_eq_self＋rewrite_self dest-buffer 迁 pthin_skip_tl.x（有则补全 P12i）。named_eq_self 指针 ABI；rewrite_self C trampoline 持 gnm[64]＋for_copy[64]。seed twin 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋trait 2/0/0＋lang-trait OK＠5821592；Ubuntu 5/5＋trait 2/0/0＋lang-trait OK＠6119136。tip `537e91717`。** | **代码** · **不升钉**。G.7：权威＝skip_tl.x `xlang_skip_trait_named_eq_self_c`／`xlang_skip_rewrite_self_type_ref_into_c`；C trampoline 零算法。sidecar＝pipeline_type_*（含既有 find_or_alloc）。禁新 P 道。禁抄进 typeck。禁并 P6b。禁并 concrete_implements_trait。禁 FORCE pabi mega。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P12i tip 到 `79ac37521` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋trait 2/0/0＋lang-trait OK）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P12i Route C：skip_name_is_self＋self_matches_for dest-buffer 迁 pthin_skip_tl.x（有则补全 P12h）。C trampoline 持 gnm[64]。concrete_implements_trait 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋trait 2/0/0＋lang-trait OK＠5821096；Ubuntu 5/5＋trait 2/0/0＋lang-trait OK＠6085936。tip `79ac37521`。** | **代码** · **不升钉**。G.7：权威＝skip_tl.x `xlang_skip_impl_self_matches_for_into_c`／`xlang_skip_name_is_self_c`；C trampoline 零算法。sidecar＝pipeline_type_*。禁新 P 道。禁抄进 typeck。禁并 P6b。禁抄 skip_name_is_self 进 P4b。禁 FORCE pabi mega。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P5e tip 到 `99de3821f` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋enum 3/0/0＋match 2/0/0）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P5e Route C：match_dest_enum_tag dest-buffer 迁 pthin_ctrl.x（有则补全 P5d）。C trampoline 持 ename[256]。parse_match 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋enum 3/0/0＋match 2/0/0＠5820376；Ubuntu 5/5＋enum 3/0/0＋match 2/0/0＠6056752。tip `99de3821f`。** | **代码** · **不升钉**。G.7：权威＝ctrl.x `parser_asm_match_dest_enum_tag_into_c`；C trampoline 零算法。sidecar＝pipeline_module_enum_*。禁新 P 道。禁并 P14c register。禁抄进 parse_match／P12e。禁 FORCE pabi mega。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P3d tip 到 `00421c35f` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋option/result generic＋struct 12/12）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P3d Route C：consume_qualified IDENT 路径＋type_angle_close dest-buffer 迁 pthin_type_ref.x（有则补全 P3c）。C trampoline 持 r／转发 next_lex。parse_type_ref 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋option/result generic＋struct 12/12＠5820056；Ubuntu 5/5＋option/result generic＋struct 12/12＠6027696。tip `00421c35f`。** | **代码** · **不升钉**。G.7：权威＝type_ref.x `parser_asm_consume_qualified_type_ident_name_into_c`／`type_angle_close_into_c`；C trampoline 零算法。walk 走既有 P9a peek／step。名拷贝走 P1b at_end。禁新 P 道。禁抄 copy 循环。禁碰 P4b pending_n。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **日切归档 14 号 43 行＋15 号主文档重写** | **文档** · **不升钉**。归档＝P12g 纯 asm 解禁／P12h／if_stmt 三波／primary 战役／金标切 Ubuntu 26.04／P19e align／P4b 激活回退侧支。主文档＝KPI／前排／§6 仅 15 日（P18d／P1f／P14c／P6b／P4uc／P4bc／P6c／P3c）。钉盘仍 `ecdb5cc1e`。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P3c tip 到 `18d52eec7` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋option/result generic＋struct 12/12）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P3c Route C：type-inst mangle dest-buffer 迁 pthin_type_ref.x（有则补全 P3b）。7-arg trampoline 持 suf[64]。parse／suffix_loop 仍 C。P4b thin 仍 XT001（suffix_loop；C 回退）。双端 L2：Darwin 5/5＋option/result generic＋struct 12/12＠5819592；Ubuntu 5/5＋option/result generic＋struct 12/12＠5998496。tip `18d52eec7`。** | **代码** · **不升钉**。G.7：mangle 权威＝type_ref.x `parser_asm_type_ref_mangle_suffix_c`／`append_into_c`；C trampoline 零算法。禁新 P 道。禁抄进 parse／codegen／typeck。禁碰 P4b pending_n。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P6c tip 到 `5da62be8d` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋struct 12/12）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P6c Route C：packed／soa 修饰符谓词迁 pthin_fn_block.x（有则补全 P6b）。parse／field walk 仍 C。C trampoline 持 lexer_result*。双端 L2：Darwin 5/5＋struct 12/12＠5818952；Ubuntu 5/5＋struct 12/12＠5976576。tip `5da62be8d`。** | **代码** · **不升钉**。G.7：修饰符权威＝fn_block.x `parser_asm_tok_is_modifier_packed_c`／`soa_c`；C trampoline 零算法。禁新 P 道。禁抄进 parse。禁并 P19d field-name kind。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P4bc tip 到 `22f3cdce1` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋binary-expr 13/13）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P4bc Route C：binop wrap dest-buffer 迁 pthin_expr_binop.x（有则补全 P4bb）。parse／peek 仍 C。setter 落 P4b seed（禁 FORCE pabi mega）。双端 L2：Darwin 5/5＋binary-expr 13/13＠5802008；Ubuntu 5/5＋binary-expr 13/13＠5947464。tip `22f3cdce1`。** | **代码** · **不升钉**。G.7：wrap 权威＝binop.x `parser_asm_binop_wrap_into_c`；C trampoline 零算法。禁新 P 道。禁抄进 parse_*。禁并 unary wrap。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P4uc tip 到 `a21c205f2` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋binary-expr 13/13）。属性语法维持 `#[xxx]`。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P4uc Route C：unary wrap dest-buffer 迁 pthin_expr_unary.x（有则补全 P4ub）。parse 仍 C。setter 落 P4u seed（禁 FORCE pabi mega）。双端 L2：Darwin 5/5＋binary-expr 13/13＠5801736；Ubuntu 5/5＋binary-expr 13/13＠5914264。tip `a21c205f2`。** | **代码** · **不升钉**。G.7：wrap 权威＝unary.x `parser_asm_unary_wrap_operand_into_c`；C trampoline 零算法。禁新 P 道。禁抄进 parse_unary。禁并 binop_wrap。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **feat(selfhost)** **P6b Route C：struct_layout name-match 三件迁 pthin_fn_block.x（有则补全 P6）。parse／library 仍 C 调此符号。sidecar 权威＝pipeline_module_struct_layout_*。禁抄进 parse／library。禁新 P 道。双端 L2：Darwin 5/5＋struct 12/0/0＠5783544；Ubuntu 5/5＋struct 12/0/0＠5885152。tip `24d973914`。** | **代码** · **不升钉**。G.7：layout 名匹配权威＝fn_block.x 同名导出。禁新 P 道。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **docs(selfhost)** **SHA-sync P14c tip 到 `227158f87` — Darwin＋Ubuntu L2 绿（矩阵 5/5＋enum 3/3）。** | **文档** · **不升钉**。 |
| 2026-09-15 | **feat(selfhost)** **P14c Route C：module_try_register_enum_name 迁 pthin_skip_if.x（有则补全 P14）。P12e 仍 extern。sidecar 权威＝pipeline_module_enum_*。禁抄进 skip_one_enum_register。双端 L2：Darwin 5/5＋enum 3/3＠5783240；Ubuntu 5/5＋enum 3/3＠5856160。tip `227158f87`。** | **代码** · **不升钉**。G.7：enum 注册权威＝skip_if.x 同名导出。禁新 P 道。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **feat(selfhost)** **P1f Route C：copy_token_bytes 128 字节零填迁 pthin_lex_skip.x（有则补全 P1 copy 族）。历史 slice 名 trampoline 留 imports.inc。禁并进 param32／name64。双端 L2：Darwin 5/5＋import 5/5＠5783000；Ubuntu 5/5＋import 5/5＠5827160。tip `197f2710e`。Win11 SHA 已对齐。** | **代码** · **不升钉**。G.7：copy 权威＝lex_skip；slice 包装零算法。禁新 P 道。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **feat(selfhost)** **P18d Route C：P010–P014 reports＋onefunc_param_name_dup 迁 pthin_body_tl.x（有则补全 P18）。文案表 parser_body_tl_p0xx_msg_c 仍 C（bootstrap STRING_LIT 64B 截断）；sticky 经 set_c。双端 L2：Darwin 5/5＋P011 4/4＠5684616；Ubuntu 5/5＋P011 4/4＋heads 46/106/28＠5802200。tip `40d934dae`。Win11 SHA 已对齐。** | **代码** · **不升钉**。G.7：控制流权威＝.x 同名导出；长文案权威＝C 表（语言上限，非第二套算法）。禁新 P 道。实验室：Win11＋Ubuntu26 并行（见测试环境.md）。 |
| 2026-09-15 | **investigate+recover(P4b-egg)** **金标复现：侧支 egg `bootstrap_xlangc -backend c -E pthin_type_ref.x` RSS 21.4G（活抓）；pin 蛋同命令 7.4MB 绿。金标切回 `self-hosting`＠`aea8a760e`＋pin 蛋 L2 重链：矩阵 5/5＋primary-heads 46/106/28＠xlang_asm 5773104。Darwin 18:58 旧二进制编 option 打到 31G 已杀、本波未重链。** | **侦查＋收回** · **不升钉**。三案仍分立：①并发双建 OOM（流程）；②x86_64 suffix_loop 局部取址 `&pending_n`（语言禁 local i32 取址；IDENT 头 `#if 0` 裸 VAR）；③**侧支自产 egg** `-backend c -E` 失控（靶＝type_ref.x 非 simd 四件）。铁律：金标只许 pin 蛋冷启；禁侧支 egg；g05 必须 RSS 闸（Darwin 合法峰 ~4.5G，闸 8G）。 |
| 2026-09-15 | **investigate(P4b-egg)** **Darwin tip `823f945a5`：冷 boot 绿；`-E src/asm/pthin_simd.x` ~2s exit=0、codegen ~697KB，无 RSS 失控（采样峰值远低于 4G 闸）——文档「四件 PABI 二分」在 tip 上至少 Darwin 已不成立，更像已被 `06216f604`（gcc 前向声明）治好的坏 egg** | **侦查（Darwin）** · **不升钉**。已被金标复现修正：Darwin 测的是 simd／无 `-backend c`；Ubuntu 活抓是 `-backend c -E type_ref.x` 21.4G。 |
| 2026-09-15 | **docs+coord(selfhost)** **三线认领开工：迁移＝P4b-egg；闸门＝日常 L2＋bstrict；文档＝本文收口；协调听进度自动拆下一刀** | **文档** · **不升钉**。总判写入：尚未完全自举（E／F 未达）；钉盘对齐 **`ecdb5cc1e`**；前排改写为 P4b-egg 第一刀＋主迁刀分轨＋立卡。升钉默认不做。 |
