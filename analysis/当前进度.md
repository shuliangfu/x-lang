# X 语言 / X language 当前进度（2026-07-29 · wave645 Cap arm64 freestanding rem · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`fd7782ec8`**（wave645 arm64 freestanding rem `%`/`%=`） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave645（已闭 · Cap residual arm64 freestanding rem `%` / `%=` · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | freestanding `x %= 5` / `x % 5` host+pure=2、mac fs=17（未 rem）；disasm 仅 zero-check 无 sdiv/msub |
| tip 产品 | **`fd7782ec8`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave645 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| freestanding arm64 `%` / `%=` 假跑（留 dividend） | 产品 seed `arch_arm64_enc_enc_mov_edx_to_eax` 空桩；`backend_enc_rem_mod_arch(ta==1)` 只调该 helper | ✅ sdiv w2,w0,w1 + msub w0,w2,w1,w0（≡ arm64_enc.x） |

**数据流**：产生 = rem_mod 分派（ta=1 只走 mov_edx 历史名）· 存储 = arm64 product seed · 消费 = freestanding MOD / MOD_ASSIGN。根因在 seed 半残（.x 已有 sdiv+msub；Ubuntu x86 走 cltd+idiv+mov_edx 本绿）。G.7：补全同一 `arch_arm64_enc_enc_mov_edx_to_eax`，不新开 rem 第二套。

**权威**：`compiler/seeds/backend_arm64_enc_c.from_x.c` · `arch_arm64_enc_enc_mov_edx_to_eax`（孪 `arch/arm64_enc.x`）

### wave645 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `x %= 5` (17→2) | ✅ host/pure/fs=2 | ✅ 2 |
| `x % 5` binop | ✅ 2 | ✅ 2 |
| `x %= d` var | ✅ 2 | ✅ 2 |
| `x % 7` | ✅ 6 | ✅ 6 |
| `/=` `+=` `*=` 回归 | ✅ | ✅ |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |
| disasm fs `%=` | `sdiv`+`msub` | `cltd`+`idiv`+`mov %edx,%eax` |

### 仍 soft

- untyped `self`（wave493 非标准）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶，或整波 L4 + 升钉  

## wave644（已闭 · Cap residual freestanding *i64/*u64/*Named ptr±int esz · L2）

tip 产品 **`85c07dbf3`**。freestanding *i64/*Named ptr scale。

## wave643（已闭 · Cap residual compound `*T +=/-=` int typeck · L2）

tip 产品 **`131c1d53b`**。compound `p+=`/`p-=` 整数 offset typeck。

## wave642（已闭 · Cap residual freestanding ptr±int / ptr-ptr C-scale · L2）

tip 产品 **`0f3be5125`**。freestanding C-scale `p±n` / `q-p`（i32/u8/f64 主路径）。
