# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（本波未改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`11a57351`**（双端 129） |
| tip | **`33881f79`**（R2 `async_cps` Cap residual pure wave3 walk_impl） |
| 产品 bstrict | **129** · 钉盘双端全绿 |
| 本波性质 | Cap residual pure：CPS walk `_impl`（无 FILE*） |
| 工程轨 prove | ✅ mac IDENTICAL · Ubuntu IDENTICAL（L2） |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `async_cps_codegen` Cap residual pure wave3（walk_impl）**（`33881f79`）  
   - `.x`：`expr_references_run_async_impl` / `block_has_run_async_ref_impl`  
   - helpers：`async_cps_ptr_at` / `is_binop_kind` / `is_walk_unary_kind`  
   - 宿主布局：ASTExpr kind/value；CALL resolved@72；ASTBlock expr_stmts/final/labeled/loops/for  
   - 语义对齐 seed：RUN/SPAWN 先匹配 `resolved_callee_func==target`；CALL 只扫 args；递归 block  
   - seed：`#ifndef SHUX_ASYNC_CPS_CODEGEN_FROM_X` 省略 walk `_impl` + pure wrappers；FILE* emit 始终 seed  
   - surface re-pin（`-E`）+ prove MODULES 注释  

2. 验证  
   - mac：`SHUX=./compiler/shux bash tests/prove_module_selfhost.sh async_cps_codegen` → IDENTICAL · `/tmp/mac_prove_async_cps_walk_impl.log`  
   - Ubuntu：pull tip 后 prove IDENTICAL（-E 偶发 SIGSEGV 重试）· `/tmp/ubuntu_prove_async_cps_walk_impl.log`  
   - **日常 L2 非 tip L4**；钉盘仍 `11a57351`  

3. 前序：liveness Cap residual pure @ `b06cf472` · CPS wave2 @ `ec2336da` · wave1 `expr_is_*` · 钉盘 `11a57351`

## 下一波前排

1. **Cap residual 余（FILE* emit）**：CPS begin/end/after_await/sched/hoist · liveness emit_*  
2. **或** 产品 PREFER 链 / `pipeline_glue` unbundle `async_asm_pool`  
3. **可选** tip 升钉：Ubuntu L4 + 双端 129  
4. **可选** Windows hybrid（需切双系统时再喊人）  

```text
钉盘：11a57351
tip：33881f79（CPS Cap residual pure wave3 walk_impl；双端 L2 prove IDENTICAL）
Windows：本波不需要切换
下一：FILE* emit Cap residual / 可选 tip L4
```
