# X 语言 / X language 当前进度（2026-07-29 · wave708 match struct field lit · 钉盘 `4fa4f07e7`）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 05:59 · 28 全日（wave534–617）已归楼；本文 29 日 tip 快照

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| 钉盘 | **`4fa4f07e7`**（wave706 升钉 · wave700–705） |
| tip 产品 | **`9126f207b`**（wave708 match struct field literal pattern implicit guard） |
| bstrict | 钉盘 **129 双端绿**；wave708 **日常 L2**（未 L4 / 未升钉） |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave708（已闭 · match struct field literal pattern implicit guard · L2）

| 项 | 状态 |
|----|------|
| 触发 | wave707 soft：`Point { x: 0, y: 0 }` 多臂 classify 仍 first-wildcard（字段字面量未真比较） |
| 根因 | wave703 结构体模式存为 wildcard；wave707 仅 field bind `Point { x, y }`，字段 `: lit` 值被跳过 |
| 权威 | G.7 parser 解析 `field: lit` 构造 `FIELD_ACCESS + EQ + LOGAND` 隐式 guard 链 → `MatchArmEntry.guard_ref`；codegen stmt/expr 双路径发 `&& (guard)`；seed codegen_gen 同 commit |
| 探针 | mac match_struct_lit (expr/ternary) + match_struct_lit_stmt (stmt/if-else) 双绿；-E 验证 `(p.x==0) && (p.y==0)` guard 生成 |
| 回归 | match_guard / match_struct_bind / match_struct_destructure_min 绿；hello 矩阵绿；match_struct_classify/destructure 仍 pre-existing fail（钉盘也 fail，含 `y: _` 通配符 + `if x > y` 显式 guard，属 wave709+ 范围） |
| tip | **`9126f207b`** |
| soft | `Point { x: 0, y: _ }` 字段通配符值；`Point { x, y } if x > y` field bind + 显式 guard 组合；freestanding match field bind；untyped self；LANG-006 |
| 构建级别 | **日常 L2** · 钉盘不升 |

## tip L4 升钉（2026-07-29 · wave706 文书）

| 项 | 内容 |
|----|------|
| 前钉 | **`01479a857`**（wave576） |
| 新钉 / 产品 tip | **`4fa4f07e7`**（wave700–705 产品收口） |
| 证据 | Ubuntu **L4** remaining_o=0 + 矩阵 + **OK (129)** · mac 全量 **OK (129)** |
| 日志 | Ubuntu `/tmp/ubuntu_true_cold_ae71aa621.log` · `/tmp/ubuntu_true_bstrict_4fa4f07e7.log` · mac `/tmp/mac_bstrict_4fa4f07e7.log` |
| 构建级别 | **tip L4 放行**（Ubuntu 金标全擦；mac 产品链全量 bstrict） |

### L4 后已闭债（wave700–705 · 含于钉盘）

| 波 | tip 锚 | 债 | 权威落点 |
|----|--------|-----|----------|
| **700** | `8e931baf5` | match arm `pat if cond =>` | MatchArmEntry.guard_ref · parser match slice · codegen |
| **701** | `51b368bc6` | `for (i : lo..hi)` | parser_gen seed range-for desugar |
| **702** | `598f2aec9` | type alias peel · import const 扫描 | field_access peel + top-level const |
| **703** | `ccd1fe3b3` | import const 产品链 / repr(compatible) / match struct 模式 | strict_minimal 调 resolve · call_arg_repr_ok · match LBRACE 模式 |
| **704** | `3359e89d4` | `*T` 函数 `if (n<=0)` 假 T001 | if/while/for 条件 ambient=0 |
| **705** | `4fa4f07e7` | `i32x4 = [..] + [..]` aggregate 假红 | array_lit ambient VECTOR stamp |

附：binary-expr 对齐 wave677 bool 规则 · `std.queue` SyncQueue `length` · fs i64 `!= -1 as i64`。

### 下一波

1. **match struct 字段通配符值**（`Point { x: 0, y: _ }`；wave708 直接延伸，闭合 match_struct_classify 第 2 arm）  
2. **match struct field bind + 显式 guard**（`Point { x, y } if x > y`；wave707 + wave700 组合，闭合第 3 arm）  
3. **Cap residual 下一硬叶**（skip untyped `self`；LANG-006 保留）  
4. 日常 L2；整波收口后再谈 L4 / 升钉

## wave699（已闭 · INDEX index ambient i32 · 修 L4 `&arr[lit]` 假 T001）

| 项 | 状态 |
|----|------|
| tip | **`f53c479a2`** |
| 触发 | tip 双端 L4 → bstrict `ptr_arith_i32` 双端红 |
| 根因 | `typeck_check_expr_index` 对 index 传 `return_type_ref`；`&buf[0]` 外层 `*T` 使 lit 0 成指针 → wave664 T001 |
| 构建级别 | 修后 **L4 复测**：冷建+矩阵+ptr ✅；bstrict 仍红在 `match_guard` |

## wave698（已闭 · Cap residual host-C [][][][][][][][]T + [][][][][][][]Named fat · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave697 soft：更深八层+ / NAMED 七层；host-C `take([][][][][][][][]i32)` / `take([][][][][][][]Cell)` 缺完整 fat 布局（BLD001 incomplete） |
| tip 产品 | **`1b1a1f347`** |
