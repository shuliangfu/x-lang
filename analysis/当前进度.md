# X 语言 / X language 当前进度（2026-07-29 · tip L4 真冷 + wave699 INDEX index ambient · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 05:59 · 28 全日（wave534–617）已归楼；本文仅保留 29 日 tip 快照

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`f53c479a2`**（wave699 INDEX index ambient i32；修 L4 暴露的 `&arr[lit]` 假 T001） |
| 钉盘 | **`01479a857`**（wave576 L4 129 全绿；**本波 tip 未升钉** — 全量 bstrict 仍红） |
| bstrict | **129**（tip L4：冷建+矩阵绿；**run-all 仍红**） |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## tip 双端 L4 真冷（2026-07-29 · 用户要求 · 钉盘不升）

| 项 | macOS | Ubuntu 金标 |
|----|-------|-------------|
| SHA | **`f53c479a2`** | **`f53c479a2`** |
| 构建级别 | **L4** remaining_o=0 · bootstrap+g05 本波 mtime | 同左 |
| 产品矩阵 | rv42 / option / si / hello / **ptr_arith_i32** ✅ | 同左 ✅ |
| 全量 bstrict | ❌ `run-typeck.sh` → **`match_guard.x`** | 同左 ❌ |
| 日志 | `/tmp/mac_true_cold_f53c479a2.log` · `/tmp/mac_true_bstrict_f53c479a2.log` | `/tmp/ubuntu_true_cold_f53c479a2.log` · `/tmp/ubuntu_true_bstrict_f53c479a2.log` |

### L4 暴露债（诚实）

1. **wave699 已闭**：`&buf[0]` / `let p:*T = &arr[lit]` — wave664 把外层 `*T` ambient 误传给 INDEX 下标 → lit 0 被 contextual 成指针 → 假 T001。**权威**：`typeck_check_expr_index` 下标 ambient = `ensure_i32`（`.x` + seed + empty_surface + `pipeline_glue`）。
2. **下一硬叶（bstrict 现红）**：`tests/typeck/match_guard.x`（及同批正例：`match_struct_destructure` / `range_for` / `struct_repr_compatible` / `type_alias` / `import_const_qualified_ok`）。首轮 L4 停在 `ptr_arith_i32` 掩了后继。
3. **bstrict ensure std soft 红**（矩阵不依赖）：`std/log` / `std/ffi` / `std/queue` / `std/heap/libc` 等 typeck 不相容/未解析（冷 ensure 才刷；**不**当矩阵假绿）。
4. **钉盘**：**不升**；升钉条件仍 = 双端 L4 + **129 bstrict 全绿**。

### 下一波

1. 修 `match_guard` 等 typeck 正例链（bstrict `run-typeck` 下一叶）  
2. 或 Cap residual 其它硬叶（skip untyped self；LANG-006 保留）  
3. tip 全量 bstrict 绿后再谈 **L4 升钉**

## wave699（已闭 · INDEX index ambient i32 · 修 L4 `&arr[lit]` 假 T001）

| 项 | 状态 |
|----|------|
| tip | **`f53c479a2`** |
| 触发 | tip 双端 L4 → bstrict `ptr_arith_i32` 双端红 |
| 根因 | `typeck_check_expr_index` 对 index 传 `return_type_ref`；`&buf[0]` 外层 `*T` 使 lit 0 成指针 → wave664 T001 |
| 构建级别 | 修后 **L4 复测**：冷建+矩阵+ptr ✅；bstrict 仍红在 `match_guard` |

## wave698（已闭 · Cap residual host-C [][][][][][][][]T + [][][][][][][]Named fat · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave697 soft：更深八层+ / NAMED 七层；host-C `take([][][][][][][][]i32)` / `take([][][][][][][]Cell)` 缺完整 fat 布局（BLD001 incomplete） |
| tip 产品 | **`1b1a1f347`** |
| 构建级别 | **L2**（mac g05 已有链 + Ubuntu `make codegen_x.o` + g05；**非** L4；钉盘不升） |

### wave698 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 缺七层 nested scalar layout | XLANG_SLICE_LAYOUTS 仅 wave697 六层 nested → 缺 `xlang_slice`×8 + elem | ✅ 补八层标量布局（rt_preamble + emit_header；行长约 215–239B → 仍 `u8[256]`） |
| 缺 `[][][][][][][]Named` companion | companion 仅六层 `xlang_slice`×6 + TAG | ✅ 同权威再发 `xlang_slice`×7 + TAG |

**数据流**：产生=`take(a:[][][][][][][][]i32)` / `take(a:[][][][][][][]Cell)` host-C type_to_c_repr + companion。存储=XLANG_SLICE_LAYOUTS + companion 表。消费=host-cc 完整 struct。G.7：补全同一 layout/companion 权威；seed 同 commit。

**权威**：`seeds/rt_preamble.from_x.c` · `src/codegen/codegen.x` · `seeds/codegen_gen.linux.x86_64.c`（同 commit）

**命名说明**：host-C fat tag = 每层 `[]` 拼一次 `xlang_slice_` + 元素 C 名（非业务变量名）；C 无泛型，层数必须写进标签才能区分 `[]T` / `[][]T` / …。

### wave698 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| host-C nested8 sig → build+run 0 | ✅ | ✅ |
| host-C nested_named7 sig → build+run 0 | ✅ | ✅ |
| 回归 nested7 / named6 | ✅ | ✅ |
| `-E` 完整 layout：`xlang_slice`×8 + `int32_t { … }` · companion 七层 `…_Cell { … }` | ✅ | ✅（Ubuntu 真 rebuild 后 grep 命中） |
| 产品矩阵 rv42 / option / hello / si（**repo root cwd**） | ✅ | ✅ |

日志：`/tmp/mac_*wave698*` · `/tmp/ubuntu_g05_wave698b.log` · `/tmp/ubuntu_w698_rebuild.log` · `/tmp/ubuntu_w698_matrix2.log`

> **注意**：Ubuntu 首轮 `rm codegen_x.o` 后 g05 报 MISSING 仍用旧 `xlang_asm` → 已 `make codegen_x.o` + g05 真链后再验；矩阵勿对 `rv` 用 `set -e`（exit 42 会假中断）。

### 仍 soft

- untyped `self`（wave493 · **跳过**；P011 更早失败）  
- LANG-006 标量 let/const bool→int（**产品保留**）  
- 更深九层+ / 其它复合嵌套边缘（未扩；再深先撞 `u8[256]` 行长）  
- 深 nest 字面量 assign/call 的 typeck fixed-array vs slice（未本波；sig-only 探针）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self；LANG-006 保留），或整波 L4 + 升钉  

## wave697（已闭 · Cap residual host-C [][][][][][][]T + [][][][][][]Named fat · L2 工程轨）

见 wave697 节历史；tip **`de8f069fb`** · 钉盘仍 **`01479a857`**。更深 nested / NAMED 已由 **wave698** 闭。

## wave696（已闭 · Cap residual host-C [][][][][][]T + [][][][][]Named fat · L2 工程轨）

见 wave696 节历史；更深 nested / NAMED 已由 **wave697–698** 闭。
