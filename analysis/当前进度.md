# X 语言 / X language 当前进度（2026-07-28 · wave617 Cap f32 ARRAY_LIT typeck · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`1a11a6a72`**（wave617 Cap f32/f64 ARRAY_LIT FLOAT_LIT stamp · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave617（已闭 · Cap residual f32 ARRAY_LIT typeck stamp · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Ubuntu pure-asm `let a: f32[2] = [10.0, 32.0]` host+fs run=0（mac 常 CTFE 折叠假绿） |
| tip 产品 | **`1a11a6a72`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave617 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| ARRAY_LIT 元素仅整数 lit coerce | `typeck_coerce_array_lit_elem_types_to_decl` | ✅ 复用 `typeck_coerce_init_float_lit_to_decl` |
| bare FLOAT_LIT 默认 f64 位装 f32 槽 | emit 读 resolved → movabs f64 低 32 常 0 | ✅ stamp f32 后 imm32 `0x41200000` / `0x42000000` |

**数据流（五问）**：产生 = typeck ARRAY_LIT→decl coerce 漏 FLOAT_LIT；存储 = resolved_type 仍 f64；消费 = `glue_emit_float_lit_to_rax` 发 f64 位 + store_sz=4 截低半。host-C braces / `as f32` 掩盖。G.7 **补全** 既有 float lit coerce 权威 — 不新开第二套。

**权威**：`typeck.x` + `seeds/typeck_gen.linux.x86_64.c` 同 commit。改后 `typeck_x.o` + g05。

### wave617 验证

| 探针 | mac L2 host/fs | Ubuntu L2 host/fs |
|------|----------------|-------------------|
| `let a: f32[2]=[10,32]` sum cast | ✅ 42 | ✅ 42（was 0） |
| `a[1] as i32` / `[-10,52]` | ✅ 32 / 42 | ✅ 32 / 42 |
| f32 cmp / f32 add / f64 arr / i32 arr | ✅ | ✅ |
| disasm f32 imm | — | ✅ `mov $0x41200000`（非 f64 movabs） |
| rv / option / si / hello | ✅ | ✅ |

### 仍 soft

- freestanding **`f32[]` slice** lit（`let a: f32[] = [10,32]`）仍可能 run=0（fat slice 路径，非本波固定 T[N]）  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual soft（f32 slice lit / 其它叶）或整波 L4 + 升钉  

## wave616（已闭 · Cap residual arm64 freestanding float · L2）

tip 产品 **`dbd0d2d16`** / docs **`1aa53a14f`**。arm64 cast/ALU/imm32；f32 cmp 双端绿。

## wave615（已闭 · Cap residual freestanding multi-dim INDEX subrow · L2）

tip 产品 **`aeac7201f`** / docs **`414cef4c7`**。

## wave614（已闭 · Cap residual arm64 freestanding INDEX assign · L2）

tip 产品 **`eb92a18b1`** / docs **`8fa734639`**。

## wave613（已闭 · Cap residual freestanding multi-dim nested ARRAY_LIT · L2）

tip 产品 **`82a1c1e1f`** / docs **`c3f870e1e`**。
