# X 语言 / X language 当前进度（2026-07-29 · wave725 Track MG 11.0.3 §5b #1/#2/#8 闭 · 钉盘仍 `53fd80927`）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **终局债地图**：[C迁移追踪.md](C迁移追踪.md) · **Makefile 迁移表**：[Makefile迁移表.md](Makefile迁移表.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 · tip 快照；产品 tip L4 钉盘不变

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| 钉盘 | **`53fd80927`**（wave710 升钉 · match 模式链整波收口） |
| tip 产品 | **文档 tip 领先钉盘**（wave714–725 Track MG 文档/shell；**不升钉**） |
| bstrict | 钉盘 **129 双端 L4 真冷绿**；wave710 升钉 |
| prove 白名单 | **111/111 IDENTICAL**（不变） |
| **C迁移前排** | **11.0.1 ✅** · **11.0.2 🟡**（class-G ✅）· **11.0.3 🟡**（§5b 白名单全 🟢；OBJS 变量仍 make）· 8.3 glue ⬜ |

## wave709（已闭 · match struct field wildcard value · L2 · match 模式链完整闭环）

| 项 | 状态 |
|----|------|
| 触发 | wave708 soft：`Point { x: 0, y: _ }` 字段通配符值；parser 遇 `_` 静默丢弃函数 |
| 根因 | wave708 parser 在 `field:` 后只接受 TOKEN_INT，遇 TOKEN_UNDERSCORE 直接 return |
| 权威 | G.7 parser 在 `field:` 后加 TOKEN_UNDERSCORE 分支：跳过该字段（不生成 guard，通配符匹配任意值），仅非通配符字段构造 guard 链 |
| 探针 | mac match_struct_lit_wild 绿；-E `(p.x==0)&&(p.y==0) ? 1 : (p.x==0 ? 2 : 0)` |
| 闭环 | **match struct 模式链完整闭环**：match_struct_classify.x + match_struct_destructure.x（钉盘 pre-existing fail）现在完全通过；4 arm 全绿（field lit guard + field wildcard + field bind + 显式 guard 组合） |
| 回归 | match_guard / match_struct_bind / match_struct_lit (expr) / match_struct_lit_stmt (stmt) / match_struct_destructure_min / hello 全绿 |
| tip | **`84644f4fa`** |
| soft | freestanding match field bind；untyped self；LANG-006 |
| 构建级别 | **日常 L2** · 钉盘不升 |

### match struct 模式链收口（wave707–709 + wave700）

| arm 模式 | 波次 | -E 生成 |
|----------|------|---------|
| `Point { x, y } => x + y` | wave707 field bind | `(p.x)+(p.y)` |
| `Point { x: 0, y: 0 } => 1` | wave708 field lit guard | `(p.x==0)&&(p.y==0) ? 1 : ...` |
| `Point { x: 0, y: _ } => 2` | wave709 field wildcard | `p.x==0 ? 2 : ...` |
| `Point { x, y } if x > y => 2` | wave700+707 组合 | `(p.x>p.y) ? 2 : ...` |

## tip L4 升钉（2026-07-29 · wave710 文书 · 双端真冷）

| 项 | 内容 |
|----|------|
| 前钉 | **`4fa4f07e7`**（wave706） |
| 新钉 / 产品 tip | **`53fd80927`**（match 模式链整波收口） |
| 证据 | **双端 L4 真冷** remaining_o=0 + bootstrap-driver-seed + g05 + 矩阵（rv=42 / option / si / hello 双端绿）+ **OK (129)** |
| 日志 | mac `/tmp/mac_cold_53fd80927.log` · `/tmp/mac_bstrict_53fd80927.log`；Ubuntu `/tmp/ubuntu_cold_53fd80927.log` · `/tmp/ubuntu_bstrict_53fd80927.log` |
| 构建级别 | **tip L4 双端真冷放行**（mac arm64 + Ubuntu x86_64 全擦 .o + 删二进制 + pin seed + bootstrap + g05 + 矩阵 + bstrict 129） |
| 含于钉盘 | wave707 match field bind · wave708 field lit guard · wave709 field wildcard（match struct 模式链完整闭环） |

### L4 后已闭债（wave700–705 · 含于前钉 `4fa4f07e7`）

| 波 | tip 锚 | 债 | 权威落点 |
|----|--------|-----|----------|
| **700** | `8e931baf5` | match arm `pat if cond =>` | MatchArmEntry.guard_ref · parser match slice · codegen |
| **701** | `51b368bc6` | `for (i : lo..hi)` | parser_gen seed range-for desugar |
| **702** | `598f2aec9` | type alias peel · import const 扫描 | field_access peel + top-level const |
| **703** | `ccd1fe3b3` | import const 产品链 / repr(compatible) / match struct 模式 | strict_minimal 调 resolve · call_arg_repr_ok · match LBRACE 模式 |
| **704** | `3359e89d4` | `*T` 函数 `if (n<=0)` 假 T001 | if/while/for 条件 ambient=0 |
| **705** | `4fa4f07e7` | `i32x4 = [..] + [..]` aggregate 假红 | array_lit ambient VECTOR stamp |

附：binary-expr 对齐 wave677 bool 规则 · `std.queue` SyncQueue `length` · fs i64 `!= -1 as i64`。

### wave712（已闭 · i32→u8 窄存储 leave-off 验证 · L2 注释更新）

| 项 | 状态 |
|----|------|
| 触发 | Cap residual 聚类：typeck.x L4106 注释标记 "i32→u8 leave-off residual for var init/assign/return" |
| 根因 | 注释过时：typeck_integer_widen_ok L4142 已 `dest_kind==ord_u8 return true`；var 路径实际已绿 |
| 探针 | mac 双端验证：var init `let b1:u8 = i` / assign `b2 = i` / call return `get_u8(i): u8` 全绿；fs(asm) build=0 run=0；host-C build=0 run=0（300 & 0xFF = 44，3*44-132=0） |
| 修复 | 纯注释更新：typeck.x L4106-4108 标记 "verified green wave712"；无代码改动；无 seed twin 同步（注释不进 seed） |
| 回归 | rv=42 / option / si / hello 全绿（mac L2 relink） |
| 构建级别 | **日常 L2** · 钉盘不升 |

## wave725（本波 · Track MG · 11.0.3 §5b #1/#2/#8 闭）

| 项 | 状态 |
|----|------|
| 触发 | 11.0.3 残：#1 check-abi Makefile 内联；#2 编排 `mk pipeline_x.o FORCE`；#8 半 shell 无命名薄叶 |
| 权威 | G.7 **`check_pipeline_gen_expr_i64_abi.sh`** · **`DRIVER_SEED_PIPELINE_X_OBJS` + export-pipeline-x** · **`DRIVER_SEED_ASM_HOST_DISPATCH_OBJS` + bootstrap-driver-seed-asm-host** |
| 改动 | 编排直调 check-abi shell；pipeline-x 走 rebuild_leaves；asm-host 薄叶，`build-seed-asm-host` 历史别名 |
| 验证 | 0-make 闸门绿；mac check-abi + export-pipeline-x OK |
| 构建级别 | **日常 L2 / 脚本** · **不升钉** · 无产品 codegen |
| 禁止本波 | shell 双清单 · tip L4 · 全量 bstrict |
| 下一 | OBJS 变量/xbuild 吞并 · PATH 探针 · tests/lib |

## wave724（已闭 · Track MG · 11.0.3 bridge/panic/user-asm/glue 导出 + shell）

| 项 | 状态 |
|----|------|
| 触发 | 11.0.3 续：§5b #5/#12 编排 `mk` 裸 `.o`；#6/#7 仅 prereq 薄目标无 export 体 |
| 权威 | G.7 扩 **`bootstrap_driver_seed_rebuild_leaves.sh`** + `DRIVER_SEED_BRIDGE/PANIC/USER_ASM_SEED/ASM_GLUE_OBJS` + export |
| 改动 | 薄叶子 bridge/panic/user-asm/glue 转调 shell；编排改 `mk bootstrap-driver-seed-{bridge,panic,…}` |
| 验证 | 0-make 闸门绿；mac 真跑 bridge/panic/user-asm(10)/glue OK |
| 构建级别 | **日常 L2 / 脚本** · **不升钉** · 无产品 codegen |
| 禁止本波 | shell 硬编码 .o 清单 · tip L4 · 全量 bstrict |

## wave723（已闭 · Track MG · 11.0.3 host-stubs 导出 + shell 体）

| 项 | 状态 |
|----|------|
| 触发 | 11.0.3 续：§5b #9 host-stubs 仍 Makefile 内联 perl+`$(CC)`；partial 非 prereq 时二次刷新可能假 skip |
| 权威 | G.7 **`DRIVER_SEED_HOST_STUBS_SCAN_BASE`** + export-host-stubs + **`bootstrap_driver_seed_host_stubs.sh`** |
| 改动 | 薄叶子 PHONY 恒刷；file 规则 `$(USER_ASM_SEED_HOST_STUBS)` 同调 shell；SCAN/CFLAGS/OUT 仅 Makefile 导出 |
| 验证 | 0-make 闸门绿；mac 真跑 scan **13** objs → **397** weak stubs → `asm_full_link_stubs.o` OK |
| 构建级别 | **日常 L2 / 脚本** · **不升钉** · 无产品 codegen |
| 禁止本波 | shell 硬编码 SCAN .o 清单 · tip L4 · 全量 bstrict |

## wave722（已闭 · Track MG · 11.0.3 sat/lsp rebuild 导出 + shell 体）

| 项 | 状态 |
|----|------|
| 触发 | 11.0.3 续：§5b #3/#4 sat/lsp 仍 Makefile 内联 `.o` 清单 + 递归 make |
| 权威 | G.7 **`DRIVER_SEED_SAT_REBUILD_OBJS` / `DRIVER_SEED_LSP_X_OBJS`** + export leaves + **`bootstrap_driver_seed_rebuild_leaves.sh`** |
| 改动 | 薄叶子转调 shell；make 命令行 `XLANG_G05_PREFER_X_O=0`（与旧 recipe 同优先级） |
| 验证 | 0-make 闸门绿；dry-run sat **10** targets / lsp **5** targets OK |
| 构建级别 | **日常 L2 / 脚本** · **不升钉** · 无产品 codegen |
| 禁止本波 | shell 硬编码 .o 清单 · tip L4 · 全量 bstrict |

## wave721（已闭 · Track MG · 11.0.3 phase1/final 链接导出 + shell 体）

| 项 | 状态 |
|----|------|
| 触发 | 11.0.3 续：phase1/final 仍 Makefile 内联 `$(CC)`；须迁 shell 且 **禁** 复制 OBJS |
| 权威 | G.7 **Makefile 导出** `bootstrap-driver-seed-export-{phase1,final}-link`（OBJS/CFLAGS 单权威）+ **`bootstrap_driver_seed_link.sh`** 链接体 |
| 改动 | 薄叶子转调 shell；`gen_g06_phase1_backend_stub` 读 export（去 `make -n` 刮命令）；MAKEFLAGS= 防 dry-run 污染 |
| 验证 | 0-make 闸门绿；mac phase1 真链 **59 objs** → `xlang-seed-phase1` OK |
| 构建级别 | **日常 L2 / 脚本** · **不升钉** · 无产品 codegen |
| 禁止本波 | shell 硬编码 .o 清单 · tip L4 · 全量 bstrict |

## wave720（已闭 · Track MG · 11.0.3 test*/bootstrap-verify shell · xlang-build 0-make）

| 项 | 状态 |
|----|------|
| 触发 | 11.0.3 续：`xlang-build.sh` 仍 4 处 `make -C`（test*/bootstrap-verify） |
| 权威 | G.7 **`run_compiler_tests.sh`** · **`bootstrap_verify_bstrict.sh`**；Makefile 薄转调；xlang-build 直调 |
| 改动 | test/test_c/test_x/bootstrap-verify 无 make；sites **4→0**；0-make 闸门硬检 + sites=0 |
| 验证 | `./tests/run-product-path-zero-make-gate.sh` 绿；`make -n test_c` / `check-7.2-bstrict` 转调 shell |
| 构建级别 | **日常 L2 / 脚本** · **不升钉** · 无产品 codegen |
| 禁止本波 | 复制 phase1 OBJS · tip L4 · 跑全量 test_c/x / xlang test |

## wave719（已闭 · Track MG · 11.0.3 token/lexer/bstrict 迁 shell）

| 项 | 状态 |
|----|------|
| 触发 | 11.0.3 续：`xlang-build.sh` 仍 7 处 `make -C`（test*/bootstrap-*） |
| 权威 | G.7 **`bootstrap_token_lexer_smoke.sh`** · **`bootstrap_driver_bstrict.sh`**；Makefile 薄转调；xlang-build 直调 |
| 改动 | token/lexer/bstrict 无 make；sites **7→4**（仅 test*/bootstrap-verify）；bstrict 内白名单 `make refresh-xlang-asm-gate`（OBJS 单权威）；0-make 闸门硬检 + sites≤4 |
| 验证 | `./tests/run-product-path-zero-make-gate.sh` 绿；`make -n bootstrap-token` / `bootstrap-driver-bstrict` 转调 shell |
| 构建级别 | **日常 L2 / 脚本** · **不升钉** · 无产品 codegen |
| 禁止本波 | 复制 phase1 OBJS · tip L4 · xlang test |

## wave718（已闭 · Track MG · 11.0.3 build-tool/clean 迁 shell）

| 项 | 状态 |
|----|------|
| 触发 | 11.0.3 续：`xlang-build.sh` 11 处 `make -C`（含 build-tool×3 + clean） |
| 权威 | G.7 **`compiler/scripts/build_tool.sh`** · **`clean_compiler.sh`**；Makefile 薄转调；xlang-build 直调 |
| 改动 | ensure/first-time/build-tool/clean 无 make；sites **11→7**（仅 test*/bootstrap-*）；0-make 闸门硬检 + sites≤7 |
| 验证 | `sh scripts/build_tool.sh` OK；`./xlang-build.sh build-tool` OK；`./tests/run-product-path-zero-make-gate.sh` 绿 |
| 构建级别 | **日常 L2 / 脚本** · **不升钉** · 无产品 codegen |
| 禁止本波 | 复制 phase1 OBJS · tip L4 · xlang test |

## wave717（已闭 · Track MG · 11.0.3 编排迁 shell）

| 项 | 状态 |
|----|------|
| 触发 | 11.0.3：`bootstrap-driver-seed` 主体仍 Makefile 内联 ~60 行 shell/make |
| 权威 | G.7 **`compiler/scripts/bootstrap_driver_seed.sh`**（步序/partial/smoke/别名）；Makefile 仅 prereq + §5b 薄叶子（OBJS/CFLAGS 单权威） |
| 改动 | 薄叶子：sat-rebuild / lsp-x-objs / user-asm-seed-objs / asm-glue-standalone / filtered-objs / host-stubs / phase1-link / final-link；主目标转调 shell；0-make 闸门硬检 |
| 验证 | `make -n` phase1/final 链行与旧一致；`./tests/run-product-path-zero-make-gate.sh` 绿 |
| 构建级别 | **日常 L2 / 脚本** · **不升钉** · 无产品 codegen |
| 禁止本波 | 复制 OBJ 列表进 shell · tip L4 · xlang test |

## wave716（已闭 · Track MG · 11.0.3 起点 · 类 G 全 shell）

| 项 | 状态 |
|----|------|
| 触发 | 11.0.3 冷启动减 make；类 G 另 3 filtered 仍 Makefile 内联 nm/ld |
| 权威 | G.7 **`filter_o_export_against_deps.sh`** + 命名包装 pipeline / against_partial；Makefile + g05_ensure 同调 |
| 改动 | 4 filtered 配方纯 shell；g05 Darwin USER_ASM trio 无 make；迁移表 §5b 冷启动 `$(MAKE)` 白名单；0-make 闸门硬检 |
| 验证 | mac dry-run T 与旧 filtered 一致（17/34/102/1388）；`./tests/run-product-path-zero-make-gate.sh` 绿 |
| 构建级别 | **日常 L2 / 脚本** · **不升钉** · 无产品 codegen |
| 禁止本波 | 整份 bootstrap-driver-seed 重写 · tip L4 · xlang test |

## wave715（已闭 · Track MG · 11.0.2 residual · pipeline filtered 去 make）

| 项 | 状态 |
|----|------|
| 触发 | wave714 后 0-make 闸门 WARN：`g05_ensure` `make -s` 重建 `bootstrap_seed_pipeline_filtered.o` |
| 权威 | G.7 **`filter_bootstrap_seed_pipeline_o.sh`**（wave716 起转调共用核心） |
| 验证 | mac T=1388；0-make 闸门绿 |
| 构建级别 | **日常 L2 / 脚本** · **不升钉** |

## wave714（已闭 · Track MG · 阶段 11.0.1 + 11.0.2 静态闸门）

| 项 | 状态 |
|----|------|
| 触发 | 终局 = 去掉 Makefile + 零 host-cc 三义；选题改挂 C迁移 DAG |
| **11.0.1** | ✅ 盘点 `compiler/Makefile` → [Makefile迁移表.md](Makefile迁移表.md)（类 A–O · ~288 目标）+ C迁移附录 E |
| **11.0.2 静态** | ✅ `tests/run-product-path-zero-make-gate.sh` allowlist；class-G 见 wave715/716 |
| 构建级别 | **文档 + 静态闸门** · **不升钉** |

### 下一波

1. **11.0.3 续**：§5b #1/#2 check-abi / pipeline_x FORCE · #8 build-seed-asm-host；OBJS 清单仍 make 直至 xbuild 吞并  
2. **辅**：0-make 运行时 PATH 探针；tests/lib make -C（11.2.3）；8.3 glue 地图  
3. **Cap 硬叶** untyped `self` — 仅挡产品时  
4. ~~bridge/panic/user-asm/glue~~（**wave724**）· ~~host-stubs~~（**wave723**）· ~~sat/lsp~~（**wave722**）· ~~phase1/final~~（**wave721**）· ~~test*/verify 0-make~~（**wave720**）· ~~token/lexer/bstrict~~（**wave719**）· ~~build-tool/clean~~（**wave718**）· ~~bootstrap 编排~~（**wave717**）· ~~类 G~~（**wave716**）· ~~11.0.1~~ · ~~match 升钉 wave710~~

## wave699（已闭 · INDEX index ambient i32 · 修 L4 `&arr[lit]` 假 T001）

| 项 | 状态 |
|----|------|
| tip | **`f53c479a2`** |
| 触发 | tip 双端 L4 → bstrict `ptr_arith_i32` 双端红 |
| 根因 | `typeck_check_expr_index` 对 index 传 `return_type_ref`；`&buf[0]` 外层 `*T` 使 lit 0 成指针 → wave664 T001 |
| 构建级别 | 修后 **L4 复测**：冷建+矩阵+ptr ✅；bstrict 仍红在 `match_guard` |

## wave698（已闭 · Cap residual host-C [][][][][][][][]T + [][][][][][][]Named fat · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave697 soft：更深八层+ / NAMED 七层；host-C `take([][][][][][][][]i32)` / `take([][][][][][][]Cell)` 缺完整 fat 布局（BLD001 incomplete） |
| tip 产品 | **`1b1a1f347`** |
