# X 语言 / X language 当前进度（2026-07-29 · wave624 Cap host-C []NamedStruct fat slice · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`97b3e5d82`**（wave624 Cap host-C `[]Named` fat slice · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave624（已闭 · Cap residual host-C `[]NamedStruct` fat slice · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `[]Pt` host-C BLD001 incomplete `xlang_slice_ast_Pt` / dual tag vs `struct Pt`；scalar `T[]` wave623 已绿 |
| tip 产品 | **`97b3e5d82`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave624 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 无 named fat 布局 | `codegen_emit_module_struct_definitions` 只吐 `struct TAG` | ✅ companion `xlang_slice_TAG` |
| empty prefix → `ast_` 与 entry bare 漂移 | `type_to_c_repr` / `emit_type` TYPE_SLICE | ✅ entry bare + ctx 前缀对齐 |
| call-arg 空前缀 type_to_c_repr | call-arg materialize | ✅ 改 `emit_type(formal SLICE)` |

**数据流（五问）**：产生 = TYPE_SLICE of TYPE_NAMED → `struct xlang_slice_<tag>` 无定义；存储 = 仅标量 preamble；消费 = host-cc incomplete。G.7：**补全** companion 布局 + 单 tag 权威（与 struct emit 同前缀规则）。

**权威**：`codegen.x` + seed `codegen_gen.linux.x86_64.c` + `ast_pool.c` type_to_c_repr。改后 **须** `codegen_x.o` + `pipeline_glue_standalone.o` + g05。

### wave624 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| host-C `[]Pt` take/local/bare-E+cc =42 | ✅ | ✅ |
| fixed `Pt[N]` / `take(Pt[N])` / `[]i64` 回归 | ✅ | ✅ |
| pure-asm `[]Pt` | soft run≠42 | soft run≠42 |
| rv / option / si / hello | ✅ | ✅ |

### 仍 soft

- **pure-asm / freestanding** `[]Named` INDEX esz（host-C 本波已绿；fs 错值另叶）  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一叶（候选 pure-asm `[]Named` esz），或整波 L4 + 升钉  

## wave623（已闭 · Cap residual host-C bare `-E` slice layouts · L2）

tip 产品 **`0081c8812`** / docs **`901d50233`**。

## wave622（已闭 · Cap residual pure-asm slice lit call-arg · L2）

tip 产品 **`f917861d7`** / docs **`827134001`**。
