# Shux 当前进度（2026-07-19）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **归档**：[自举进度-归档-2026-07-18.md](自举进度-归档-2026-07-18.md) · [17 号](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`305cfbe1`**（fix(abi) 波 2 ABI 一致性 3 处；ABI 边界 → 升钉） |
| 产品 L4 放行钉盘 | **`305cfbe1`**（双端真冷 L4+125；前钉 `4214eea4`；本波 ABI 边界 → 升钉） |
| 双端 L4 兜底（本波） | ✅ 双端全绿（`305cfbe1` SHA：mac log `/tmp/mac_true_bstrict_305cfbe1.log` · Ubuntu log `/tmp/ubuntu_true_bstrict_305cfbe1.log`）；详见 [自举进度.md](自举进度.md) §6 |
| 产品 bstrict 套件 | **125** |
| 本波性质 | **C6 X ABI P0b 波 2**（ABI 一致性修复 3 处 · ABI 边界 → L4 双端 + 升钉 + Windows 门禁） |
| 本波输出 | `compiler/src/runtime_io_abi.x` L164/180/196 共 3 处定义 `export function` → `export extern "C" function`（fs_posix_close_c / fs_posix_read_c / fs_posix_write_c），与 main.x / pipeline.x / driver/emit.x 共 5 个声明点对齐；每处加 4 行英文 ABI 注释；函数体不变（底层 libc 调用已在 std_fs_fs_* 内部包裹 unsafe） |

## 本波摘要（2026-07-19 · C6 X ABI P0b 波 2 · ABI 一致性修复 3 处 · 升钉）

| 项 | 结果 |
|----|------|
| **入口** | 推进 [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.3 波 2 推荐执行方案 + [自举进度.md](自举进度.md) §3 前排 #3 波 2 |
| **改动** | `compiler/src/runtime_io_abi.x` L164/180/196 共 **3 处** 定义：`export function fs_posix_*_c` → `export extern "C" function fs_posix_*_c`（保留 `#[no_mangle]` 属性），每处加 4 行英文 ABI 注释说明契约依据 + 委托 `std_fs_fs_*` 的设计理由 |
| **声明/定义对齐** | 5 个声明点（main.x:30-31 / pipeline.x:81-82 / driver/emit.x:71-73）已标 `extern "C"` ✅ ↔ 3 个定义点（runtime_io_abi.x L164/180/196）此前缺失 `extern "C"` 🔴 → **本波修复后 5+3=8 点全部对齐** ✅ |
| **函数体不变** | 函数体 `return std_fs_fs_close/read/write(...)` 调用的是 SHUX X-ABI 桥接（默认 X ABI），不是直接 libc；底层 libc `close/read/write` 调用已在 `std_fs_fs_close` (L99-105) / `std_fs_fs_read` (L124-135) / `std_fs_fs_write` (L145-156) 内部用 `unsafe { }` 包裹 → 本波只需改 ABI 标注，函数体零改动 |
| **契约依据** | P0a 语义降级下不一致不断链；P1 激活 typeck 强制 `extern "C"` 调用约定后，声明/定义不匹配会导致参数寄存器错位 / 运行期 UB；`typeck.x:289 pipeline_typeck_check_extern_call_unsafe_boundary_c` 已声明未激活（P0a），P1 激活会断链 |
| **波次** | 波 2 of 3（audit `7e507fb6` → 波 1 `4214eea4` ✅ → **波 2 `305cfbe1` ✅** → 波 3 std/ ~557 远期顺延自举后 P1-P2） |
| **mac L4 真冷** | ✅ 全绿：全擦 .o（compiler/std/core 共 0 remaining）+ 删二进制（shux/shux_asm/shux-c/bootstrap_shuxc）+ `bootstrap-driver-seed OK` (shux 3656096 bytes, seed smoke exit=42) + `g05_prepare_and_relink OK (shux_asm OK)` (58 objs Darwin/arm64) + `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0；log `/tmp/mac_true_bstrict_305cfbe1.log` |
| **Ubuntu L4 真冷** | ✅ 全绿：git pull `305cfbe1` Fast-forward + 全擦 .o + 删二进制 + `bootstrap-driver-seed OK` (shux 3490000 bytes) + `g05_prepare_and_relink OK (shux_asm OK)` (60 objs Linux/x86_64) + `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0；log `/tmp/ubuntu_true_bstrict_305cfbe1.log`；**link-hardening gate OK（Linux 专有，macOS 跳过 non-Linux host）** |
| **升钉决策** | ✅ **升钉** `4214eea4` → `305cfbe1`（ABI 一致性改动属 ABI 边界，per [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.3 + [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md) §5.1） |
| **Windows 门禁** | 🟡 待跑（ABI 边界改动 per §5.1 强制重跑 Windows hybrid 最小门禁 `tests/run-bootstrap-bstrict-windows-gate.sh`；非阻塞升钉，但需在波 3 启动前或与波 1+波 2 合并补跑） |
| **本波 commit** | `305cfbe1` fix(abi): align fs_posix_*_c definitions with extern "C" declarations（1 文件 +15/-3 行） |

## 下一波前排（按优先级排序）

### 1. Windows 门禁补跑（波 1 + 波 2 遗留 · ABI 边界）

按 [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md) §5.1 强制要求：
- ssh `windows-server` → `set TEMP=C:\shux_tmp` + `set CC=gcc` → `./tests/run-bootstrap-bstrict-windows-gate.sh`
- 验证波 1 unsafe 包裹 + 波 2 ABI 一致性在 Windows MSYS2/MinGW 下无回归
- 波 1 (`4214eea4`) + 波 2 (`305cfbe1`) 同属 ABI 边界，可合并一次跑

### 2. C7 plan 瘦身（工程债 · 非产品）

plan-only bstrict 模板裁剪（按 `tests/run-all-bstrict.sh` plan 子集）

## 不在前排

- **C6 X ABI P0b 波 3：std/ 扩展标注**（远期 · 多波 · 顺延自举后 P1-P2）：~557 个 C 库包装分文件标注 `extern "C"`（http 249 / socketio 106 / async 42 / json 37 / math 32 / sqlite 32 / atomic 30 / arrow 29）；按文件大小递增推进，`std/atomic/mod.x`（30 个）作 PoC 验证 typeck 强制 unsafe 后无回归再扩展；详见 [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §4 / §5.4
- **shux-x 5GB 方案 B**（远期 · 非 KPI）：为 ArenaSidecar/ModuleSidecar 加 release API；前提 typeck 完成后 dep_arenas 不再被访问；方案 C/D 详见 [shux-x-5GB-内存根因调查.md](shux-x-5GB-内存根因调查.md) §6
- **Cap C9 match guard full-chain 支持**（独立大工程 · 多波）：6 个产生点改动，先排期再动手
- **C5 EXPR_CLOSURE CTFE**（独立大工程 · 多波）：已 scoping（[C5-EXPR_CLOSURE-CTFE-scoping.md](C5-EXPR_CLOSURE-CTFE-scoping.md)），顺延自举后立项 `lang-closure-v1-rfc.md`
- **历史进度**：波 1 unsafe 包裹 9 处（`4214eea4` 升钉）/ C6 P0b audit（`7e507fb6` docs-only）/ shux-x 5GB 方案 A（`a14a1dd0`）/ 日终 L4 兜底（`94a8e7d5`）/ C5 三连击（`c174e70a`）/ 4 波 C5 CTFE / shux-x 5GB 调查 / Windows 时序决议 / C2/C3=C4/backtrace/C8 钉盘波，全部归档在 [自举进度.md](自举进度.md) §6 变更记录

```text
本波 tip：305cfbe1 (fix(abi): align fs_posix_*_c definitions with extern "C" declarations)
本波性质：C6 X ABI P0b 波 2（ABI 一致性修复 3 处 · ABI 边界 → L4 双端 + 升钉 + Windows 门禁）
本波输出：compiler/src/runtime_io_abi.x L164/180/196 3 处定义 export function → export extern "C" function（fs_posix_close_c / fs_posix_read_c / fs_posix_write_c）+ 每处 4 行英文 ABI 注释
构建级别：L4 真冷双端（mac arm64 + Ubuntu x86_64）；SHUX_BSTRICT_SKIP_BUILD=1 run-all-bstrict 125 全绿 exit=0
产品 tip L4 钉盘：305cfbe1（升钉自 4214eea4；本波 ABI 边界改动 → 必须升钉）
Windows 门禁：待跑（ABI 边界改动 per §5.1 强制重跑 hybrid 最小门禁；非阻塞升钉，与波 1 合并补跑）

实测统计（2026-07-19 全量扫描 compiler/src + compiler/asm_libroot + std）：
  extern "C" function (C ABI):       3035 个（含合法 dispatch / runtime / libc 封装；Top 20 全部合法）
  extern function   (X ABI 默认):    2431 个（含合法 SHUX 桥接；std/ 下 ~557 个为 C 库包装待标注）
  extern "x" function (显式 X ABI):    0 个（语法槽位已就位，暂未使用）
  合计 extern 声明:                  5466 个

核心 14 个 C ABI 调用（设计文档 §3.2.2 定义）：
  parser.x:36-38 (3) ✅ + main.x:30-31 (2) ✅ + pipeline.x:81-82 (2) ✅ + driver/emit.x:71-73 (3) ✅ + asm_libroot/std/fs/mod.x:11-14 (4) ✅
  → 100% 已标注 extern "C"（波 1 + 波 2 后声明 + 定义全部对齐）

声明/定义 ABI 一致性状态（波 2 后）：
  fs_posix_close_c: 声明 main.x:31 / pipeline.x:82 / driver/emit.x:73 = extern "C" ✅ ↔ 定义 runtime_io_abi.x:164 = extern "C" ✅（波 2 修复）
  fs_posix_read_c:  声明 pipeline.x:81 / driver/emit.x:71 = extern "C" ✅ ↔ 定义 runtime_io_abi.x:180 = extern "C" ✅（波 2 修复）
  fs_posix_write_c: 声明 main.x:30 / driver/emit.x:72 = extern "C" ✅ ↔ 定义 runtime_io_abi.x:196 = extern "C" ✅（波 2 修复）
  → 5 声明点 + 3 定义点 = 8 点全部 extern "C" 对齐 ✅

调用点 unsafe 包裹状态（波 1 后）：
  main.x L670-695 6 处 + asm_libroot/std/fs/mod.x L22-39 3 处 = 9 处全部已包裹 unsafe ✅
  runtime_io_abi.x 内部 std_fs_fs_close/read/write 已包裹 unsafe ✅（波 2 无需改动）

拆分策略 3 波：
  波 1（✅ 已完成 @ 4214eea4）: unsafe 包裹修复 9 处（main.x 6 + mod.x 3）→ L4 双端 + 升钉 + Windows 门禁（待补跑）
  波 2（✅ 已完成 @ 305cfbe1）: ABI 一致性修复 3 处（runtime_io_abi.x 定义改 extern "C"）→ L4 双端 + 升钉 + Windows 门禁（待补跑）
  波 3（顺延自举后 P1-P2）: std/ 库 ~557 个 C 库包装标注 extern "C"，按文件大小递增分波（std/atomic 30 个作 PoC）

风险：自举前必须完成波 1（✅）+ 波 2（✅），否则 P1 激活 typeck 强制 unsafe + X ABI 调用约定时 bootstrap 陷阱 → 已解除

权威文档：analysis/C6-P0b-scope-audit.md（§0 摘要 + §1 实测统计 + §2 核心 14 审计 + §3 ABI 一致性 + §4 std/ 扩展 + §5 3 波拆分 + §6 风险评估 + §7 验证清单 + §8 调查方法 + §9 相关文档）
历史进度归档：analysis/自举进度.md §6 变更记录（波 2 305cfbe1 升钉 / 波 1 4214eea4 升钉 / C6 P0b audit 7e507fb6 / shux-x 5GB 方案 A a14a1dd0 / 日终 L4 兜底 94a8e7d5 / C5 三连击 c174e70a / 4 波 C5 CTFE / shux-x 5GB 调查 / Windows 时序决议 / C2/C3=C4/backtrace/C8 钉盘波）
```
