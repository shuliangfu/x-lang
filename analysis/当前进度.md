# X 语言 / X language 当前进度（2026-07-28 · wave604 Cap arm64 MEMORY CALL · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`f1c9909aa`**（wave604 Cap arm64 freestanding MEMORY CALL call-arg · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave604（已闭 · Cap residual arm64 freestanding MEMORY call-arg from CALL · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | mac fs `sum(mk())` CG002（wave603 soft：store_memory 仅 VAR） |
| tip 产品 | **`f1c9909aa`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave604 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| MEMORY call-arg from CALL | `store_memory_by_value_to_sp` `ko!=3` 早退 | ✅ CALL/METHOD sret→frame temp（x8）+ multi-word → SP |

**数据流（五问）**：产生 = CALL/METHOD >16B 返回；存储 = 帧 temp（AAPCS64 sret x8）→ 出栈 MEMORY；消费 = param_home（wave599/603）。权威：G.7 补全 `store_memory` ≡ x86 `push_sysv_memory` CALL 叶（wave601），不新开第二套。

**权威**：`compiler/pipeline_glue.c`（`pipeline_asm_store_memory_by_value_to_sp_elf_c`）。改后 rm `pipeline_glue_standalone.o` + g05。

### wave604 验证

| 探针 | mac L2 host/fs | Ubuntu L2 pure/fs |
|------|----------------|-------------------|
| mem_call `sum(mk())` | ✅ 42/42（was CG002） | ✅ host/pure/fs 42 |
| mem_call_arg `sum(mk(1))` | ✅ 21/21 | —（x86 同源） |
| mem_dual_call 双 MEMORY CALL | ✅ 42/42 | ✅ host/pure/fs 42 |
| mem_var 回归 | ✅ 42 | ✅ 42 |
| var12/var16/call_dual/mem_param/var_take2 | ✅ 42/42/42/42/41 | ✅ 回归 |
| disasm | ✅ lea→x8 bl mk · 3×str [sp] bl sum | — |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/hello |

### 仍 soft

- arm64 MEMORY call-arg from STRUCT_LIT / FIELD（`sum(Big{…})` / `sum(o.t)`）— 与 x86 push 同「先 let」  
- UFCS method 同模块 typeck residual（本叶未开）  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual soft（MEMORY STRUCT_LIT/FIELD / 其它叶），或整波双端 L4 + 升钉  

## wave603（已闭 · Cap residual arm64 freestanding VAR dual-GP + MEMORY call-arg · L2）

tip 产品 **`00d2059d0`** / docs **`06c3d4e1a`**。soft「MEMORY from CALL」本波由 **wave604** 收口。

## wave602（已闭 · Cap residual freestanding UFCS method self SysV · L2）

tip 产品 **`ddb5f61ff`** / docs **`fb307d12a`**。
