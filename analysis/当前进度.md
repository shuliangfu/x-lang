# X 语言 / X language 当前进度（2026-07-29 · wave731 Track MG 11.4 residual → xbuild · 钉盘仍 `53fd80927`）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威 · **唯一波次变更记录**）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **终局债地图**：[C迁移追踪.md](C迁移追踪.md)（状态/地图 only）· **Makefile 迁移表**：[Makefile迁移表.md](Makefile迁移表.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 · tip 快照；产品 tip L4 钉盘不变

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` · 产品入口 **`./xbuild`**（别名 `./build.sh`） |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| 钉盘 | **`53fd80927`**（wave710 升钉 · match 模式链整波收口） |
| tip 产品 | **文档 tip 领先钉盘**（wave714–731 Track MG 文档/shell；**不升钉**） |
| bstrict | 钉盘 **129 双端 L4 真冷绿**；wave710 升钉 |
| prove 白名单 | **111/111 IDENTICAL**（不变） |
| **C迁移前排** | **11.4.1/6 ✅** · **11.4.5 🟡** · **11.2.5/11.4.3 ✅** · **11.0.4 🟡** · **11.2.3 🟡** · 11.1 ⬜ |

## wave731（本波 · Track MG · 11.4 residual 入口 → xbuild）

| 项 | 状态 |
|----|------|
| 触发 | 根 `build.sh` 仍 host-cc 双权威；delete-one Docker/host 入口；Dockerfile 未标 xbuild |
| 权威 | G.7：外层一律 `./xbuild`；host-cc residual 仅 `build_tool.sh`（至 12） |
| 产物 | `build.sh` 薄转发；`delete-one-c-file` → xbuild bstrict；Dockerfile entry env/label；g05/bootstrap 提示 |
| 闸门 | 0-make 闸门硬检 build.sh 无 cc + delete-one + Dockerfile 入口 + 既有 CI/hub |
| 构建级别 | **日常 L2 / shell** · **不升钉** `53fd80927` |
| 下一 | 11.1 吞 g05 · tests/`make -C` residual · 11.5 bench 对照 C |

## wave730（已闭 · Track MG · 11.2.5 CI + 11.4.3 docker-ci → xbuild）

| 项 | 状态 |
|----|------|
| 触发 | 外层 CI/docker 仍 raw `make -C`；与产品 `./xbuild` 双入口 |
| 权威 | G.7：`run_compiler_make` 单 hub；产品 `all`=g05；CI=`compiler-all` |
| 产物 | xlang-build 增 `compiler-all` / `bootstrap-driver-seed` / `compiler-make`；workflows + docker-ci 全改 `./xbuild`；11.4.4 提示 |
| 闸门 | 0-make 闸门硬检 CI/docker 0× 外层 make -C + hub 目标 + 产品 all 仍 g05 |
| 构建级别 | **日常 L2 / shell** · **不升钉** `53fd80927` |
| 下一 | 11.1 吞 g05 · 11.4 residual（build.sh / Dockerfile） |

## wave729（已闭 · Track MG · 11.0.4 根 help→xbuild + 8.3 地图）

| 项 | 状态 |
|----|------|
| 触发 | 11.0.4 收口根入口；辅 8.3 消费方地图；文档纪律：C迁移/Makefile 表不写 changelog |
| 权威 | G.7：`./xbuild` 仅转调 `xlang-build.sh`；根 Makefile help-only（`%` 兼容转发） |
| 产物 | `./xbuild`；根 Makefile 瘦身；C迁移 §8.3 体积+消费方表；去掉 C迁移/Makefile 表变更记录段 |
| 闸门 | 0-make 闸门硬检 xbuild 薄转调 + help-only + 无 C迁移 `## 变更记录` + 8.3 消费方 + 既有 catalog/hub/PATH |
| 构建级别 | **日常 L2 / shell** · **不升钉** `53fd80927` |
| 下一 | 11.2.5 CI workflow · 11.4 `docker-ci-local.sh` 去 make |

### 下一波

1. **11.1**：xbuild / `build.x` 吞 g05 依赖图（compiler/Makefile 本体）  
2. **tests residual**：更多 `tests/run-*.sh` raw `make -C` → hub/`./xbuild`  
3. **11.5 / 12**：bench 对照 C 策略；Docker 卸 gcc/make  
4. ~~11.4 residual build.sh/Dockerfile/delete-one~~（**wave731**）· ~~11.2.5/11.4.3 CI·docker~~（**wave730**）· ~~根 help→xbuild + 8.3~~（**wave729**）· … · ~~match 升钉 wave710~~
