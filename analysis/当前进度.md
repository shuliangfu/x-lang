# X 语言 / X language 当前进度（2026-07-29 · wave692 pure-asm nested [][]T INDEX · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 05:59 · 28 全日（wave534–617）已归楼；本文仅保留 29 日 tip 快照

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`c48923df3`**（wave692 pure-asm nested `[][]T` INDEX fat esz=16） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave692（已闭 · Cap residual pure-asm nested `[][]T` INDEX · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave691 soft：Ubuntu pure-asm/fs nested INDEX SIGSEGV（host-C 绿；mac arm64 CTFE 常假绿） |
| tip 产品 | **`c48923df3`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave692 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| fat 元素 esz 剥成标量 | force_esz / array_lit_elem_byte_sz / index_elem_byte_sz 对 TYPE_SLICE 元素 peel→4 → outer stride 4；COMMON 只 `mov eax` 写 .data 半字 | ✅ esz/force=**16** |
| durable/stack 不打包 dual-GP fat | esz>8 bulk 走 struct let-init 不适 TYPE_SLICE；scalar store clamp 4 | ✅ dual-GP → C fat 16B pack |
| INDEX 结果 TYPE_SLICE 无 dual-GP rvalue | eff_addr 后 leave/半 load；嵌套 `rows[i][j]` 重发 outer | ✅ load data@rax length@rdx；once-path INDEX base |

**数据流**：产生=`let rows:[][]i32=[inner]` ARRAY_LIT + `rows[i][j]` INDEX。存储=COMMON/栈 fat 槽。消费=pure-asm INDEX stride+dual-GP。G.7：补全同一 force_esz/esz/emit_index/once 权威。

**权威**：`compiler/pipeline_glue.c` only（无 seed 分叉本波）

### wave692 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| pure-asm take_outer / local / var-index → 30/20/30 | ✅ | ✅ |
| host-C nested → 30 | ✅ | ✅ |
| freestanding nested → 30 | — | ✅ |
| take_two 回归 30 · 矩阵 rv42/option/hello/si | ✅ | ✅ |
| disasm Ubuntu：`mov qword` data@0 + length@8 into `Lxlang_al_*` | — | ✅ |

日志：`/tmp/mac_g05_wave692b.log` · `/tmp/ubuntu_g05_wave692.log` · `/tmp/ubuntu_w692_probes.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**；P011 更早失败）  
- LANG-006 标量 let/const bool→int（**产品保留**）  
- 更深 `[][][]T` / nested of NAMED fat 布局（未扩）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self；LANG-006 保留；候选 deeper nested / NAMED fat），或整波 L4 + 升钉  

## wave691（已闭 · Cap residual nested `[][]T` host-C fat · L2 工程轨）

见 wave691 节历史；tip **`fb4ba91ac`** · 钉盘仍 **`01479a857`**。pure-asm nested INDEX 已由 **wave692** 闭。

## wave690（已闭 · Cap residual bare-T free `[]T` ret mono · L2 工程轨）

见 wave690 节历史；tip **`8aec77df0`** · 钉盘仍 **`01479a857`**。

## wave689（已闭 · Cap residual free `[]T` ret mono · L2 工程轨）

见 wave689 节历史；tip **`3ef22fd0b`** · 钉盘仍 **`01479a857`**。

## wave688（已闭 · Cap residual `*T` call ret mono · L2 工程轨）

见 wave688 节历史；tip **`930b247df`** · 钉盘仍 **`01479a857`**。

## wave687（已闭 · Cap residual `T[N]`/`[]T` formal mono mangle · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave686 typeck 已接受 `first_a(T[2])` / `first_s([]T)`，codegen mono **ec=-6** / XP003；`[]T` 另 BLD001 形参按值 vs 指针 ABI |
| tip 产品 | **`c7fc0e79d`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave687 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| mono mangle 无 ARRAY/SLICE 后缀 | `codegen_type_ref_to_suffix` 仅 PTR/NAMED/builtin/VECTOR；combo key `i32[2]`/`[]i32` → suffix 0 → `emit_mono_mangled_name` -1 | ✅ `_a<N>` / `_slc` |
