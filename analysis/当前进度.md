# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **pipeline_abi wave46 pure residual** @ `77dcc558`（产品 `ad7359a6` + hybrid extern fix） |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **R2 pipeline_abi Cap residual pure wave46**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + pure-asm 矩阵 rv42 / option / hello / nest / stdlib-import；pure 符号 **W** @ `77dcc558` |
| mac | ✅ L2 PREFER hybrid + c-backend 矩阵（Darwin pure-asm CG002 tip 既有）@ `77dcc558` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave46** @ `ad7359a6` + fix `77dcc558`  
   - **迁 pure**：`shux_ptr_slot_{get,set}` · `shux_size_slot_{get,set}` · `shux_i32_store` · `shux_module_num_imports` · `shux_module_import_path_cstr` · `shux_collect_to_load_has` · `pipeline_diag_preprocess_directive_code`（固定 PP002 文案，无 va_list）  
   - **seed**：冷孪生 `#ifndef SHUX_RUNTIME_PIPELINE_ABI_FROM_X`；FROM_X rest 补 pure 符号 **extern 原型**（防 implicit → hybrid fallback full seed）  
   - **权威**：`compiler/src/runtime_pipeline_abi.x` + `seeds/runtime_pipeline_abi.from_x.c` 同向；G.7 ptr/size slot 单路径  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave46b.log` · `/tmp/ubuntu_g05_wave46c.log`

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：pipeline_abi 重 C residual（collect/read_file/resolve/thread/FILE*）/ 其它 rest pure 深迁  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave46 未升钉）
产品 tip：77dcc558 pipeline_abi residual pure wave46 L2
Windows：本波不需要切换
下一：pipeline 重 C residual / 其它 rest / Darwin CG002（需时再说）
```
