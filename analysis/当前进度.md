# X 语言 / X language 当前进度（2026-07-28 · wave583 Cap struct layout/field · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`9283a6cfb`**（wave583 Cap struct layout/field · L2 mac 绿 · Ubuntu 待本波） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave583（已闭 · Cap residual struct 类型名/字段名/type-param getter 64→127 · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave582 后 residual：`struct Sss…` / 长字段名 ≥64 → parse 无 main / XP003 |
| tip | **`9283a6cfb`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave583 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| struct 类型名拒 >63 | `parser_asm_struct_layout_slice` `sname_buf[64]` + `ident_len>63` | ✅ 128/127 |
| 字段名拒 >63 | `struct_field_name_from_tok` `<=63`；`fname_buf[64]` | ✅ 127 · buf 128 |
| STRUCT_LIT 类型名/字段名 | `finish_struct_lit` `tlen/flen>63`、`tnm/fnbuf[64]` | ✅ 127/128 |
| type-param 登记缓冲 | `tp_names[8][64]` + gate 63 | ✅ [8][128]/127 |
| getter 假 0 | `field_name_len` / `type_param_name_len` `<=63` | ✅ 127 · into 128 |
| typeck merge/SoA/coerce | `nl/flen/backfill <=63` | ✅ 127 · typeck.x+seed pin |
| SoA 侧门 | `pipeline_typeck_soa` name/field 63 + [64] | ✅ 127/128 |

**数据流（五问）**：产生 = parser_asm struct_layout + finish_struct_lit + field_name_from_tok；存储 = StructLayout/Field/LayoutTypeParam name[128]；消费 = typeck merge/coerce/field_access、SoA layout lookup。

**操作纪律**：改 parser_asm slices → `parser_asm_thin_glue.o` + g05；改 `ast_pool` / typeck_soa → **PIPELINE_X_FORCE_COMPILE=1** `pipeline_x.o` + g05；改 typeck.x → seed pin + `typeck_x.o`。

### wave583 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| struct 类型名 63/64/75/127 | ✅ 42 | 待 pull 后 |
| struct 字段名 63/64/75/127 | ✅ 42 | 待 pull 后 |
| type_param 63/64/75（本波前已绿） | ✅ 42 | — |
| rv / option / si / hello | ✅ | 待 |

### 仍 soft

- residual：library wrap / import binding 等其它 `>63` 侧门  
- 整波双端 **L4** + 升钉  

### 下一波

1. library wrap / import 等 residual `>63` 或整波 L4  
2. 整波双端 L4 + 升钉  

## wave582（已闭 · Cap type_alias/enum/match）

见上波；tip `c7dbb960d` / docs `2083eaddf`。
