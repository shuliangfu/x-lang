# X 语言 / X language 当前进度（2026-07-29 · wave625 Cap pure-asm []Named force_esz + FIELD dual-slot · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`b278583c4`**（wave625 Cap pure-asm `[]Named` · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave625（已闭 · Cap residual pure-asm `[]Named` force_esz + FIELD dual-slot · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | pure-asm/fs `[]Pt` run≠42（mac/Ubuntu ~12）；host-C wave624 已绿 |
| tip 产品 | **`b278583c4`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave625 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| durable force_esz 仅标量 | let-init / call-arg / return 4× 复制 | ✅ `glue_array_lit_force_esz_from_elem_type_c`（TYPE_NAMED→layout size） |
| COMMON 只 str w0 | force_esz=0→infer 4 | ✅ Pt=8 → str x0 全 8B |
| `a[0].x+a[1].y` dual-slot | clobber 分类器不 peel FIELD | ✅ peel FIELD→INDEX；reuse preserve |

**数据流（五问）**：产生 = ARRAY_LIT→`[]Named` force_esz 缺 NAMED + binop FIELD-of-INDEX 未标 clobber；存储 = durable COMMON 4B store；消费 = INDEX esz=8 / bounds 盖掉 rhs@x1（10+length=12）。G.7：**补全** force_esz 单权威 + **补全** 既有 clobber 分类器（禁第二套 preserve）。

**权威**：`pipeline_glue.c` only（glue_standalone 入链）。改后 **须** `pipeline_glue_standalone.o` + g05。

### wave625 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| pure-asm/fs `[]Pt` take/sum/let =42 | ✅ | ✅ |
| host-C `[]Pt` =42 | ✅ | ✅ |
| `[]i64` call-arg / dual ARRAY_LIT binop | ✅ | ✅ |
| fixed `Pt[2]` pure-asm | ✅ 42 | soft p0x=0（sum=32；host-C 42） |
| rv / hello | ✅ | ✅ |

### 仍 soft

- Ubuntu pure-asm fixed `Pt[N]` 首元（本波未改 vector_let；slice 叶已闭）  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一叶，或整波 L4 + 升钉  

## wave624（已闭 · Cap residual host-C `[]NamedStruct` fat slice · L2）

tip 产品 **`97b3e5d82`** / docs **`ad577c123`**。host-C `[]Pt` companion fat 绿；pure-asm 本波 **wave625** 闭。

## wave623（已闭 · Cap residual host-C bare `-E` slice layouts · L2）

tip 产品 **`0081c8812`** / docs **`901d50233`**。

## wave622（已闭 · Cap residual pure-asm slice lit call-arg · L2）

tip 产品 **`f917861d7`** / docs **`827134001`**。
