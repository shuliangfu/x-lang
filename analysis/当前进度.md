# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[自举进度-归档-2026-07-17.md](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **本波 WPO call-site CTFE 收口后 tip**（钉盘仍 **`c51759eb`**） |
| 产品 L4 放行 | ✅ **钉盘 `c51759eb`**（不升钉） |
| tip 双端 bstrict | ✅ **123/123 @ `c51759eb`**（钉盘） |
| **typeck CTFE fold** | ✅ 主路径（lit/binop/unary）+ **CALL site** |
| **LANG-006 residual** | ✅ 已闭（BITNOT + 逻辑 const） |
| **WPO call-site fold** | ✅ **本波迁 pre-emit**（scalar binop CALL） |

## 本波摘要（2026-07-18 · WPO call-site fold → typeck CTFE）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步自举」；前排可选 WPO call-site fold |
| **地图** | 权威应在 typeck CTFE 写 `const_folded_*`；emit 只消费 mov imm。历史在 `try_inline_wpo_const_scalar_binop_call_elf` 于 emit 内联 fold |
| **根修** | G.7 补全 `glue_typeck_fold_expr_ref`：`EXPR_CALL` 当 callee 为 `return p0 binop p1` 且两常量实参 → stamp CALL；`SHUX_WPO_MONO`/`NO_FOLD` 跳过 stamp 保 call site；emit fast 对 CALL 同 env 门控。须 **`pipeline_x.o` FORCE + `bootstrap_seed_pipeline_filtered.o` + g05**（standalone  alone 不进 g05 链） |
| **验收** | mac L2：`-E const_spec_fold` → `main { return 0; }`；`SHUX_WPO_MONO=1` → 保留 `scale(1024,64)-65536`；lang-const **12/12**；矩阵 rv=42 · hello |
| **残差** | vector lane-of-binop 仍 emit try_inline；（旁）Darwin pure-asm `.o` CG002 / callgraph dump 非本波 |

### residual 下一波

1. （旁）vector `lane0(vec_binop const…)` 同步迁 pre-emit（可选）  
2. 下一产品债另开地图  

```text
构建级别：L2（pipeline_x FORCE + filtered + g05；非 tip L4）
产品 tip L4 钉盘：c51759eb
权威：pipeline_glue.c glue_typeck_fold_expr_ref EXPR_CALL
```

---

## 前波摘要（2026-07-18 · LANG-006 residual：`~` + `&&`/`||`）

| 波 | 结果 |
|----|------|
| **根修** | unary `TOKEN_TILDE`→`EXPR_BITNOT`；bool→int init coerce |
| **验收** | lang-const **12/12**；矩阵绿 |
| **残差** | WPO call-site → **本波已迁 pre-emit** |

## 前波摘要（2026-07-18 · typeck CTFE 常量折叠权威恢复）

| 波 | 结果 |
|----|------|
| **根修** | typeck glue 恢复 fold 生产者 + typeck.x 挂钩 + C codegen 消费 folded |
