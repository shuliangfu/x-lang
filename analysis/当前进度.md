# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（本波未改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`11a57351`**（双端 129） |
| tip | **`faf5b269`**（PREFER full.x：`async_liveness` + `async_cps_codegen`；-E 5× 重试） |
| 产品 bstrict | **129** · 钉盘双端全绿 |
| 本波性质 | 产品 PREFER：liveness/cps 独立 TU full.x+rest（仿 pool；本非 glue 内嵌） |
| 工程轨 prove | ✅ mac IDENTICAL · Ubuntu IDENTICAL（L2 · liveness + cps） |
| 产品矩阵 | ✅ mac/Ubuntu rv=42 · option · stdlib-import · hello（L2/L3 g05） |
| Windows | **本波不需要** |

## 本波摘要

1. **产品 PREFER `async_liveness` / `async_cps_codegen`**（`05e2506c` + retry `faf5b269`）  
   - TU 本已独立于 `pipeline_glue`（非 unbundle）；冷路径仍整 seed C  
   - PREFER：full `.x` + rest（`-DSHUX_ASYNC_LIVENESS_FROM_X` / `-DSHUX_ASYNC_CPS_CODEGEN_FROM_X`，slice_marker）`ld -r`  
   - `Makefile` + `g05_ensure` 双路径；Makefile 对 -E 产物塞 stdlib/string/stdio prologue（对齐 `g05_try_x_to_o`）  
   - Ubuntu 偶发 `-E` SIGSEGV → PREFER/`g05_try_x_to_o` 最多 5 次重试再 cold fallback  

2. 验证  
   - mac：PREFER phase4 · g05 OK · 矩阵绿 · prove 2× IDENTICAL · `/tmp/mac_aliv_acps_g05.log` · `/tmp/mac_prove_aliv_acps_prefer.log`  
   - Ubuntu：双 PREFER + slice_marker · g05 OK · 矩阵绿 · prove 2× IDENTICAL · `/tmp/ubuntu_aliv_acps_g05b.log` · `/tmp/ubuntu_prove_aliv_acps_prefer.log`  
   - **日常 L2/L3 非 tip L4**；钉盘仍 `11a57351`  

3. 前序：pool unbundle @ `708d63e0` · 钉盘 `11a57351`

## 下一波前排

1. **可选** tip 升钉：Ubuntu L4 + 双端 129（async 产品 PREFER 族收口后）  
2. **可选** 其它 R2 rest / Cap residual  
3. **可选** Windows hybrid（需切双系统时再喊人）  

```text
钉盘：11a57351
tip：faf5b269（async_liveness + async_cps_codegen 产品 PREFER full.x；双端 L2/L3 矩阵+prove）
Windows：本波不需要切换
下一：可选 tip L4 升钉 / 其它 R2
```
