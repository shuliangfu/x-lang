# X 语言 / X language 当前进度（2026-07-29 · wave628 Cap pure-asm var-index STRUCT_LIT assign · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`74c40b979`**（wave628 Cap pure-asm var-index STRUCT_LIT assign · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave628（已闭 · Cap residual pure-asm var-index STRUCT_LIT assign · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Ubuntu pure-asm `a[i]=S24{…}` sum=64 / s24_one=200 / host-C 66·6；Pt 变下标 mac 假绿、S24 红 |
| tip 产品 | **`74c40b979`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave628 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| var-index STRUCT_LIT | wave627 仅 INT_LIT；generic 留指针 + finish_store ≤8B | ✅ TYPE_ARRAY+非 lit → eff_addr + DEST_IN_RBX 字段直写 |
| esz=24 scale | assign-side `rbx_plus_index_scratch` esz∉{1,4} 落 ×8 | ✅ 走 `glue_emit_index_eff_addr_scaled`（imul 通用 esz） |
| DEST_IN_RBX re-home | sret_direct 每字段从 sret_home 重载（未激活→垃圾） | ✅ dest 指针 spill 帧槽 `rehome_off` |

**数据流（五问）**：产生 = `a[i]=StructLit` 非 lit 回落 generic → rax=temp 指针；存储 = finish_store 只写 8B 指针进元素（s24 字段 a=栈地址位）；消费 = 读 a/b/c 垃圾。G.7：**扩** wave627 同一 in-place 权威至 runtime index（禁第二套 bulk store）。

**权威**：`pipeline_glue.c` only（`pipeline_asm_emit_assign_elf_c` INDEX 叶 + `pipeline_asm_emit_struct_lit_fields_elf_c` `GLUE_STRUCT_LIT_DEST_IN_RBX`）。改后 **须** `pipeline_glue_standalone.o` + g05。

### wave628 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `a[i]=Pt; a[j]=Pt` sum / p0x / p1y | ✅ 42 / 10 / 32 | ✅ 42 / 10 / — |
| `a[i]=S24` sum / one | ✅ 66 / 6 | ✅ 66 / 6 |
| lit-index S24 / Pt 回归 | ✅ 66 / 42 | ✅ 66 / 42 |
| fixed let / field asg / `[]Pt` | ✅ 42 | ✅ 42 |
| freestanding 同上 | ✅ | ✅ |
| host-C | ✅ | ✅ |
| rv / option / si / hello | ✅ | ✅ |
| Ubuntu disasm | — | `imul $0x18` + 字段写 [rbx+0/8/10] |

### 仍 soft

- 其它 Cap residual 叶（slice INDEX STRUCT_LIT 大对象仍走 generic；assign-side scale 对非 STRUCT_LIT 的 esz∉{1,4,8} 仍 soft）；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一叶，或整波 L4 + 升钉  

## wave627（已闭 · Cap residual pure-asm INDEX STRUCT_LIT assign + multi-dim flat · L2）

tip 产品 **`ab3873604`** / docs **`b39d57bbc`**。INT_LIT INDEX STRUCT_LIT + multi-dim flat 绿；var-index 本波 **wave628** 闭。

## wave626（已闭 · Cap residual pure-asm fixed `Pt[N]` STRUCT_LIT in-place · L2）

tip 产品 **`becc92c72`** / docs **`dbf9a2fe7`**。

## wave625（已闭 · Cap residual pure-asm `[]Named` force_esz + FIELD dual-slot · L2）

tip 产品 **`b278583c4`** / docs **`994568244`**。

## wave624（已闭 · Cap residual host-C `[]NamedStruct` fat slice · L2）

tip 产品 **`97b3e5d82`** / docs **`ad577c123`**。
