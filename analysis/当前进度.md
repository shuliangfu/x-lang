# X 语言 / X language 当前进度（2026-07-29 · wave630 Cap pure-asm INDEX assign bulk esz>8 · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`66a0f3ffa`**（wave630 Cap pure-asm INDEX assign bulk esz>8 · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave630（已闭 · Cap residual pure-asm INDEX assign bulk esz>8 · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Ubuntu pure-asm `a[i]=t` S24/S12 非 STRUCT_LIT：s24_asg=14、s12=33、copy=184、call=139；host-C 66/120；arm64 常假绿 |
| tip 产品 | **`66a0f3ffa`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave630 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| assign-side try_* ×8 | `backend_enc_rbx_plus_index_scratch_scaled` 仅 {1,4,8}；esz=12/24 落 ×8 | ✅ try_* 仅 esz∈{1,4,8}；否则 scaled imul |
| finish_store 宽≤8 + rhs VAR 只 load 首 qword | generic path 写 8B 指针/字段到错位 dest | ✅ esz>8：src lea/CALL materialize + dest scaled + chunked bulk copy |

**数据流（五问）**：产生 = `a[i]=t`/`a[j]=a[i]`/`a[i]=mk()` esz>8 回落 try_×8 + store≤8；存储 = 元素只得 8B；消费 = 字段垃圾（s24_asg=1+10+3=14）。G.7：**扩** wave628/629 INDEX 权威 — bulk 非 STRUCT_LIT 叶；STRUCT_LIT 仍走 in-place fields。

**权威**：`pipeline_glue.c` only（`pipeline_asm_emit_assign_elf_c` INDEX 叶 + `glue_emit_bulk_mem_copy_spills_elf_c`）。改后 **须** `pipeline_glue_standalone.o` + g05。

### wave630 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| fixed S24[2] `a[i]=t` sum | ✅ 66 | ✅ 66 |
| fixed S24[2] `a[j]=a[i]` | ✅ 120 | ✅ 120 |
| fixed S12[2] `a[i]=t` | ✅ 66 | ✅ 66 |
| fixed S24 `a[i]=mk()` CALL | ✅ 66 | ✅ 66 |
| TYPE_SLICE dual bulk VAR（零 init） | ✅ 66 | ✅ 66 |
| STRUCT_LIT dual / lit 回归 | ✅ 66 | ✅ 66 |
| freestanding 同上 | ✅ | ✅（`XLANG=./compiler/xlang_asm`） |
| host-C | ✅ | ✅ |
| i32 var-index | ✅ 33 | ✅ 33 |
| rv / option / si / hello | ✅ | ✅ |

### 仍 soft

- `S24[]` **非零 ARRAY_LIT 初值** 后仅 bulk 写一元（Ubuntu s24_sl 初值+`s[i]=t` 曾 12）— 偏 durable ARRAY_LIT 残；零 init + 双 bulk 已绿  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一叶（ARRAY_LIT durable 大 struct / 其它），或整波 L4 + 升钉  

## wave629（已闭 · Cap residual pure-asm TYPE_SLICE INDEX STRUCT_LIT assign · L2）

tip 产品 **`2a0496153`** / docs **`e55a906d9`**。TYPE_SLICE STRUCT_LIT 绿。

## wave628（已闭 · Cap residual pure-asm var-index STRUCT_LIT assign · L2）

tip 产品 **`74c40b979`** / docs **`c3ebac45a`**。

## wave627（已闭 · Cap residual pure-asm INDEX STRUCT_LIT assign + multi-dim flat · L2）

tip 产品 **`ab3873604`** / docs **`b39d57bbc`**。

## wave626（已闭 · Cap residual pure-asm fixed `Pt[N]` STRUCT_LIT in-place · L2）

tip 产品 **`becc92c72`** / docs **`dbf9a2fe7`**。
