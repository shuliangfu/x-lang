# X 语言 / X language 当前进度（2026-07-29 · wave650 Cap if/while 条件整式含分组 `(` · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 05:59 · 28 全日（wave534–617）已归楼；本文仅保留 29 日 tip 快照

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`d5aec6a64`**（wave650 if/while 条件不剥前导 `(`） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave650（已闭 · Cap residual if/while cond 整式分组 · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `if (x >> 2) != 4` / `if (x << 2) == 16` / `while (x >> 2) != 4` → XP003 `out_len=0` 静默丢 main；`if ((x>>2)!=4)` / bare `if x>>2!=4` / let 中转已绿 |
| tip 产品 | **`d5aec6a64`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave650 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| wave361 见 `(` 剥括号再 parse_cond | if_stmt / if_expr / while 条件入口 | ✅ 不剥 `(`；条件从首 token 整式解析（primary 分组） |

**数据流**：产生 = wave361 可选括号实现剥 `(` → 只解析 `x >> 2` → 外层 `!= 4` 失败 → 丢 main。存储 = AST 无 main。消费 = pipeline XP003。G.7：单一 cond 入口补全；不新开第二套 if 解析。

**权威**：`parser_asm_if_stmt_slice.inc` / `parser_asm_if_expr_slice.inc` · `parser.x` while 双径 · `seeds/parser_gen.linux.x86_64.c` 同 commit

### wave650 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `if (x>>2)!=4` / `<<` / while / ashr | ✅ host/fs=42 | ✅ 42 |
| field / if-expr / bare / paren | ✅ 42 | ✅ 42 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

日志：`/tmp/mac_g05_wave650*.log` · `/tmp/ubuntu_g05_wave650.log` · `/tmp/ubuntu_w650_probes2.log`

### 仍 soft

- untyped `self`（wave493 非标准）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶，或整波 L4 + 升钉  

## wave649（已闭 · Cap residual freestanding FIELD STRUCT_LIT/CALL → []T · L2）

tip 产品 **`84f9b813c`**。freestanding FIELD STRUCT_LIT/CALL as TYPE_SLICE。

## wave648（已闭 · Cap residual freestanding signed `>>` / `>>=` · L2）

tip 产品 **`af5e1c609`**。freestanding signed `>>`/`>>=` SAR。
