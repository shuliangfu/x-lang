# X 语言 / X language 当前进度（2026-07-24）

> **长期仪表盘**：[自举进度.md](自举进度.md)（24 日起重新起表）  
> **23 号全日归档**：[自举进度-归档-2026-07-23.md](自举进度-归档-2026-07-23.md)  
> **重命名分析**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)  
> **改名前锚点**：`anchor-pre-xlang-rename` @ `d07bcf155`（父钉曾 `a77fc5d90`）  
> **项目目录**：现行 `…/xlang/x-lang`（mac/Ubuntu/Windows 同构）  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only**（第三品牌 `shu_*` / `SHU_*` / `shux` 已清） |
| 主域 | **xlang.dev** |
| 工程 | 钉盘仍 **`75c638465`** · **tip L4 未升钉**（bstrict 红于 run-string） |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **已恢复** · tip **`63ebeacf7`**（wave363 T[N]=0 + L4 测试对齐） |

## 验收

### tip L4 钉盘（仍有效 · 未升）

| 项 | 状态 |
|----|------|
| SHA | **`75c638465`**（wave288） |
| 构建级别 | **L4 真冷** + 双端 **OK (129)** |
| 日志 | `/tmp/{mac,ubuntu}_true_{cold,bstrict}_75c638465.log` |

### 本波 tip L4 尝试（2026-07-24 · **未全绿 · 不升钉**）

| 项 | 状态 |
|----|------|
| tip SHA | **`63ebeacf7`**（前：wave362 `4f6f891a3` + typeck 对齐 `18f7ba42d` + wave363 glue） |
| 构建级别 | **L4 真冷** remaining_o=0 起编（双端 @ `308b55349` 冷编；后续 tip 产品 g05 热跟） |
| 产品矩阵 | ✅ 双端 rv=42 · option · hello |
| 全量 bstrict | ❌ **红 · run-string**（双端）；**禁止升钉** |
| mac | Abort trap:6 on `tests/lib/xlang-backend-wrap.sh` → xlang-c `-backend c` 编 `tests/string/main.x`（exit 134） |
| Ubuntu 金标 | `xlang_asm` 对 `string.new()`+`string.len(s)` 静默 **exit 127**（`-c`/`-E`/`build` 均空输出）；`string_empty` / 仅 `new()` 绿 |
| 本波已修 | ① typeck 负例→正例 u64↔usize（wave312 产品语义）`18f7ba42d` ② freestanding `T[N]=0` 零填 `63ebeacf7`（Ubuntu array CG002） |
| 日志 | `/tmp/mac_true_cold_308b55349.log` · `/tmp/ubuntu_true_cold_308b55349.log` · `/tmp/*_true_bstrict_63ebeacf7.log`（红） |
| 产品钉盘 | **仍 `75c638465`** |

### 下一优先

```text
下一：wave363 续 — tip L4 红点 run-string（金标）
  最小复现（Ubuntu）：
    const string = import("std.string");
    function main(): i32 {
      let s: String = string.new();
      return string.len(s);  // exit 127 silent
    }
  对照绿：string.string_empty()；仅 string.new() 无 len
  leave-off 其它 soft residual 仍挂
  钉盘 75c638465 直至 tip 双端 L4+129 全绿再升
```
