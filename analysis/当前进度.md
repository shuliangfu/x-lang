# X 语言 / X language 当前进度（2026-07-29 · wave633 Cap pure-asm fixed TYPE_ARRAY let bulk esz>8 · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`9a16fb325`**（wave633 Cap pure-asm fixed S24[2] let bulk · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave633（已闭 · Cap residual pure-asm fixed TYPE_ARRAY let from CALL/VAR bulk esz>8 · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Ubuntu pure-asm `let s: S24[2] = mk()`：fx=11≠110；host-C 110；临时 `mk()[i].field` 与 slice return 绿 |
| tip 产品 | **`9a16fb325`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave633 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| fixed `S24[2]` CALL let 半字拷 | `glue_struct_lit_store_fixed_array_field` CALL/INDEX：esz∉{1,4,8} → `load_i32` + store esz | ✅ esz>8 整块 `n*esz` bulk（`glue_emit_bulk_mem_copy_spills`） |
| VAR/FIELD 同形 `let b = a` | 同函数 VAR 分支 else `load_i32` | ✅ 孪 bulk |

**数据流（五问）**：产生 = wave632 已使 `return [S24…]` durable COMMON + E*@rax；消费 = fixed TYPE_ARRAY let 仍按标量装载每元首 i32 → 仅 a0/a1（sum=11）。G.7：**扩** wave351/399 fixed-array let 权威 + wave630 bulk，**不**新开路径。

**权威**：`pipeline_glue.c` only。改后 **须** `pipeline_glue_standalone.o` + g05。

### wave633 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `let s: S24[2] = mk()` sum | ✅ 110 | ✅ 110 |
| 各字段 a0..d1 | ✅ | ✅ 1/2/3/4/10/20/30/40 |
| `let b = a` VAR 拷 | ✅ 110 | ✅ 110 |
| `s = mk()` assign（零初值后） | ✅ 110 | ✅ 110 |
| 临时 `mk()[i].*` | ✅ 110 | ✅ 110 |
| `S24[]` return let（wave632） | ✅ 110 | ✅ 110 |
| `i32[2]` / `Pt[2]` return let | ✅ 42 | ✅ 42 |
| host-C fx | ✅ 110 | ✅ 110 |
| rv / option / si / hello | ✅ | ✅ |

Ubuntu disasm：main 对 48B 载荷 6× qword bulk（off 0..0x28），不再 `mov (%rax),%eax` 半字。

### 仍 soft

- 未初始化 `let s: S24[2]; s = mk()`（缺 init 时可能丢 main / BLD001）  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一叶，或整波 L4 + 升钉  

## wave632（已闭 · Cap residual pure-asm return large NAMED ARRAY_LIT durable + reent · L2）

tip 产品 **`2a9d40d0c`** / docs **`407d17be9`**。return S24[] simple/deep/dual 绿；soft fixed `S24[2]` let 已由 **wave633** 闭。

## wave631（已闭 · Cap residual pure-asm TYPE_SLICE ARRAY_LIT large NAMED · L2）

tip 产品 **`8bbbf951b`** / docs **`42a9f0bbf`**。S24[] lit RO/sl 绿。

## wave630（已闭 · Cap residual pure-asm INDEX assign bulk esz>8 · L2）

tip 产品 **`66a0f3ffa`** / docs **`0a78ca598`**。bulk esz>8 绿。
