# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave157** unix_gcc_tail_libs_impl pure @ `4e3173a10` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **R2 Cap residual pure · invoke_ld L6**（`shux_asm_ld_append_unix_gcc_tail_libs_impl`） |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：其它 rest R2 可迁叶 — `shux_asm_ld_append_unix_gcc_tail_libs_impl` hybrid 下仍 always mega C（纯 flag 表已 L6，append 体仍 mega；Linux -ldl / else -lc 分支）  
2. **根修**：迁入 `labi_invoke_ld_list` L6 pure orch（`ShuAsmLdStdLinkFlags` LP64 i32 布局 + pure `labi_ld_flag_*` + peer compress orch + peer needs_compress + peer `shux_host_is_linux` + Cap residual `link_abi_host_is_apple`）  
3. **权威**：`.x` + cold twin（`#include`）+ surface re-pin + mega 删 always 体；prove **IDENTICAL**  
4. **注意**：gate `shux_asm_ld_append_unix_gcc_tail_libs` 仍 labi_gates pure thin；mach/unix tail 双叶均已 pure  
5. **验收**：双端 L2 g05 + 矩阵 + string/vec/slice/compress 邻域 + prove IDENTICAL；钉盘仍 **`3c88f7e51`**  

## 下一波前排

1. 其它 rest R2 / mega residual（可迁：net_tls / path / freestanding residual …；常驻 Cap 不硬迁：contains_substr / undef_sym / path_io stat / wait / strerror / mega spawn-IO / ld_debug_argv / invoke_cc_argv_push_existing realpath 池 …）  
2. 可选：未闭合块注释硬诊断（EOF depth>0）  
3. 可选 Windows hybrid（**需要时再通知重启**）  

```text
钉盘：3c88f7e51（未升）
tip：4e3173a10 wave157 unix_gcc_tail_libs_impl pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：net_tls / path / freestanding residual / 其它 rest R2
```
