# X 语言 / X language 当前进度（2026-07-29 · wave623 Cap host-C bare -E slice layouts · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`0081c8812`**（wave623 Cap host-C bare `-E` slice layouts · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave623（已闭 · Cap residual host-C bare `-E` minimal preamble · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | bare `-E` 对 `[]i64`/`[]f32`/… 生成 `struct xlang_slice_*` 但无布局 → host-cc incomplete type；`-backend c -o` / 全 rt_preamble 绿 |
| tip 产品 | **`0081c8812`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave623 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| bare prologue 仅 3 includes | `codegen_x_ast_emit_header` | ✅ 扩 wave618–619 全套 `xlang_slice_*` + `XLANG_SLICE_LAYOUTS` |
| full `-o` 再吐 header 重定义 | rt_preamble 无 guard | ✅ 同宏 guard（G.7 单布局权威） |

**数据流（五问）**：产生 = bare `-E` 走 `codegen_x_ast` 最小 header；存储 = 无 fat layout；消费 = host-cc 见 incomplete type。G.7：**补全** emit_header ≡ rt_preamble 标量 slice 集，**禁止**第二套布局表。

**权威**：`compiler/src/codegen/codegen.x` + seed `codegen_gen.linux.x86_64.c` + `seeds/rt_preamble.from_x.c`。改后 **须** `codegen_x.o` + `rt_preamble.o` + g05。

### wave623 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| bare `-E` + host-cc i32/i64/u8/f32/f64/u32/isize/bool =42 | ✅（i64 main 用 entry 包装；布局完整） | ✅（i64 main 直链） |
| `-backend c -o` i64 回归 | ✅ | ✅ |
| pure-asm i64 take 回归 | ✅ | ✅ |
| rv / option / si / hello | ✅ | ✅ |

### 仍 soft

- 其它 Cap residual 叶；整波双端 **L4** + 升钉  
- Darwin host-cc 对 `int64_t main` 硬要求 `int`（非本波；`-backend c -o` 产品路径绿）

### 下一波

1. Cap residual 下一叶，或整波 L4 + 升钉  

## wave622（已闭 · Cap residual pure-asm slice lit call-arg · L2）

tip 产品 **`f917861d7`** / docs **`827134001`**。

## wave621（已闭 · Cap residual pure-asm f32 ordered cmp · L2）

tip 产品 **`2d81d0dff`** / docs **`93733710b`**。
