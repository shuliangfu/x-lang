# X 语言 / X language 当前进度（2026-07-27 · wave523）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`0bff9947a`**（双端 L2，wave523；L4 待整波收口） |
| 钉盘 | **`2bc0885d3`**（待 Ubuntu 钉盘升级） |
| bstrict | **129** |

## wave523（已闭 · runtime_net_udp_batch 根因修复 + Makefile R2 thin+rest 补全 双端 L2）

| 项 | 状态 |
|----|------|
| 触发 | wave521 发现的 6 个"半残"模块之一；审计发现 pre-existing `__GLIBC__` guard bug |
| 模块 | `runtime_net_udp_batch`（226 行；Linux recvmmsg/sendmmsg UDP 批量 syscall 胶层） |
| 根因 bug | `.from_x.c` 的 `#if defined(__linux__) && defined(__GLIBC__)` 在 `#include` 之前检查，但 `__GLIBC__` 由 `<features.h>` 定义，在 `#include` 前不可见 → 整个 TU 恒为空 → recvmmsg/sendmmsg 符号从未被编译 → cold path 始终产出空 .o；被 `runtime_asm_io_stubs.c` 的 weak fallback 掩盖，从未被发现 |
| 根修 | ① `#define _GNU_SOURCE` 移到 `#if` 之前（必须在系统头文件前定义才能暴露 recvmmsg/sendmmsg 原型）② `#if defined(__GLIBC__)` → `#if defined(__linux__)`（编译器预定义，不需 include；glibc/musl 都支持）③ 补全 Makefile thin+rest `ld -r` 规则（Linux 专属：`UNAME_S=Linux` 判断，macOS 保持冷路径空 .o） |
| 构建模式 | **thin+rest（Linux only）**——thin (.x) 提供 2 个 `#[no_mangle]` wrappers（set_addr_port/poll_readable）；rest (.from_x.c) 提供 _impl（sockaddr/poll）+ 4 个 recvmmsg/sendmmsg weak API；macOS cold = 空 .o（`#if __linux__` 不满足） |
| nm 一致性 macOS | cold vs R2：均为空 .o（Linux 专属，macOS 不编译）✅ 一致 |
| nm 一致性 Ubuntu | cold vs R2：8 核心 T 符号对齐（xlang_udp_batch_set_addr_port/set_addr_port_impl/poll_readable/poll_readable_impl + 4 recvmmsg/sendmmsg weak API），仅 R2 多 `_doc_anchor` |
| 产品矩阵 macOS | g-ffi-5-std-wrap ✅ · crypto 全链路 ✅ |
| 产品矩阵 Ubuntu | g-ffi-5-std-wrap ✅ · crypto 全链路 ✅ |
| 验证级别 | **L2 日常真测**（按自举方法 §0.2 节奏；非每微 commit 全量 L4） |
| PLATFORM | LINUX — R2 路径 gated on `UNAME_S=Linux`；macOS 保持冷路径（空 .o，`#if __linux__` 不满足） |
| 关键发现 | **pre-existing `__GLIBC__` bug**：`#if defined(__GLIBC__)` 在 `#include` 前检查导致整个 TU 恒为空。`__GLIBC__` 由 `<features.h>` 定义，`#include` 前不可见。被 `runtime_asm_io_stubs.c` weak fallback 掩盖，从未被发现。修复后 recvmmsg/sendmmsg 符号终于被编译。根源治理优先：发现 bug 必须从根源修复，不能只补 Makefile 规则而忽略底层 bug |
