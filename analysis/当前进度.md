# X 语言 / X language 当前进度（2026-07-27 · wave524）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`656e04617`**（双端 L2，wave524；L4 待整波收口） |
| 钉盘 | **`2bc0885d3`**（待 Ubuntu 钉盘升级） |
| bstrict | **129** |

## wave524（已闭 · runtime_arrow_simd_glue 根因修复 + Makefile R2 thin+rest 补全 双端 L2）

| 项 | 状态 |
|----|------|
| 触发 | wave521 发现的 6 个"半残"模块之一；审计发现 pre-existing 编译错误 bug |
| 模块 | `runtime_arrow_simd_glue`（289 行；db.arrow SSE2/NEON SIMD 胶层：f32 求和/点积 + i32/f32 null bitmap 有效元素求和 + 4 个 column glue） |
| 根因 bug | `arrow_column_i32_sum_valid_c` 和 `arrow_column_f32_sum_c` 声明局部变量为 `length` 但全程使用 `len` → 8 个 "use of undeclared identifier 'len'" 编译错误 → TU 不可编译 → arrow column glue 符号从未被生成 → Makefile 冷路径从未成功构建此模块 |
| 根修 | ① 变量声明 `length` → `len`（拼写错误修复，无语义改变）② 补全 Makefile thin+rest `ld -r` 规则 |
| 构建模式 | **thin+rest**——thin (.x) 提供 4 个 `#[no_mangle]` SIMD kernel wrappers（arrow_f32_sum_kernel/dot_kernel/i32_sum_valid_kernel/f32_sum_valid_kernel）；rest (.from_x.c) 提供 _impl（SSE2/NEON SIMD 循环）+ 4 个 arrow_column_*_c glue + arrow_hsum_ps helper（始终编译，被 _impl 调用） |
| nm 一致性 macOS | cold vs R2：12 核心 T 符号对齐（4 kernels + 4 _impl + 4 column glue；arrow_hsum_ps 在 macOS 为 static 不导出），仅 R2 多 `_doc_anchor` |
| nm 一致性 Ubuntu | cold vs R2：13 核心 T 符号对齐（4 kernels + 4 _impl + 4 column glue + arrow_hsum_ps），仅 R2 多 `_doc_anchor` |
| 产品矩阵 macOS | g-ffi-5-std-wrap ✅ · crypto 全链路 ✅ |
| 产品矩阵 Ubuntu | g-ffi-5-std-wrap ✅ · crypto 全链路 ✅ |
| 验证级别 | **L2 日常真测**（按自举方法 §0.2 节奏；非每微 commit 全量 L4） |
| PLATFORM | SHARED — SSE2/NEON 在编译期检测（`__SSE2__`/`__ARM_NEON`/`__aarch64__`）；无 OS 专属代码 |
| 关键发现 | **pre-existing 拼写错误**：`length` 声明 vs `len` 使用导致 TU 不可编译。与 wave523 的 `__GLIBC__` bug 类似，都是 pre-existing bug 被 Makefile 冷路径失败掩盖。根源治理模式：发现编译错误必须修复变量名，不能只补 Makefile 规则。连续两波（wave523/524）发现 pre-existing bug 说明"半残"模块审计价值高——不仅补全 Makefile 规则，还暴露并修复了隐藏的编译错误 |
