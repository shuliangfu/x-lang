# Shux 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-22.md](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d6648bee4`**（升自 `3c88f7e51`；双端 tip L4 + 129） |
| tip 产品代码 | **wave223** pure modules raw getenv → link_abi_getenv @ **`524176a84`** |
| 产品 bstrict | 钉盘 **129**；本波 **日常 L2**（非 tip L4） |
| 本波性质 | **R2 Cap residual pure** · G.7 getenv call-site converge |
| Ubuntu | ✅ L2：g05 + 矩阵 + string `-c` + path/icc/fs/diag prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 + string `-c` + path/icc/fs/diag prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **wave223**：G.7 收敛 pure 模块 raw `getenv` → 已建权威 **`link_abi_getenv`**（wave222 pure thin → `_impl` host getenv）。迁出：`labi_path_pure`（`SHUX_LIB` / `SHUX_DEBUG_LD`）、`labi_invoke_cc_list`（skip_native 三 env + `SHUX_RUN_QUIET`）、`labi_freestanding_list`（`SHUX_FREESTANDING`）。cold + surface twins + mega path_pure cold fallback ≡ .x。
2. **闭合 soft residual**「hybrid pure 模块仍 raw getenv」（path_pure / invoke_cc quiet / freestanding）。mega 体 `SHUX_MINIMAL_CC_LINK` 门闩与 debug-only mega getenv 仍 Cap residual。
3. **验收**：双端 L2 矩阵 rv42/option/si/hello + string `-c`；path_pure / invoke_cc_list / freestanding / diag prove **IDENTICAL**。钉盘仍 **`d6648bee4`**。

## 下一波前排

1. 常驻 Cap residual 可迁叶：resolve 池 `_impl` 体 / system·make pure thin / mega 体 residual raw getenv（`SHUX_MINIMAL_CC_LINK`、debug `SHUX_DEBUG_*` …）；link_abi_getenv @ wave222 pure · pure 模块 env call sites @ wave223 · path_executable @ wave221 pure
2. 可选：未闭合块注释硬诊断（EOF depth>0）
3. 可选 Windows hybrid（**需要时再通知重启**）

```text
钉盘：d6648bee4
tip：524176a84 wave223 pure modules raw getenv → link_abi_getenv
构建级别=L2 日常（非 tip L4）
Windows：不需要切换
下一：常驻 Cap residual（resolve 池 _impl / system·make / mega raw getenv …）
```
