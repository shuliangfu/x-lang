# X 语言 / X language 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **重命名分析**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)  
> **改名前锚点**：`anchor-pre-xlang-rename` @ `d07bcf155`（父钉曾 `a77fc5d90`）  
> **项目目录**：现行 `…/xlang/x-lang`（mac/Ubuntu/Windows 同构）  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only**（第三品牌 `shu_*` / `SHU_*` / `shux` 已清） |
| 主域 | **xlang.dev** |
| 工程 | **全库重命名 R0–R3 已闭** · 双端 tip L4 **129 全绿** · 钉盘 **`75c638465`** |
| process_xlang 兼容别名 | **已删除** |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **已恢复** · **wave289** Cap residual pure 已闭（typeck 非法 unary `~float`/`~ptr`/`-ptr` 硬失败） |

## 验收

### tip L4 钉盘（仍有效）

| 项 | 状态 |
|----|------|
| SHA | **`75c638465`**（wave288：invoke_cc push `runtime_link_abi_user_env.o`） |
| 构建级别 | **L4 真冷**（全擦 compiler/std/core `.o` + 重链产品二进制） |
| remaining_o 起编 | ✅ **0**（双端） |
| mac 全量 bstrict | ✅ **OK (129)** · `/tmp/mac_true_cold_75c638465.log` · `/tmp/mac_true_bstrict_75c638465.log` |
| Ubuntu 全量 bstrict | ✅ **OK (129)** · `/tmp/ubuntu_true_cold_75c638465.log` · `/tmp/ubuntu_true_bstrict_75c638465.log` |
| 前钉 | `3698e557f`（R3 重命名 tip L4；wave238–287 仅 L2） |

### 本波 tip（wave289 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`2b286e989`** — R2 Cap residual pure wave289 |
| 构建级别 | **L2**（pin typeck seed + 相关 `.o` + g05 + 探针 + 产品矩阵；**非 tip L4**） |
| 根因 | `typeck_check_expr_unary` 对 NEG/BITNOT 只复制操作数类型，无 host-cc 合法性 → soft residual **BLD001**（`~double` / `~uint8_t*` / `-uint8_t*`） |
| 修法 | G.7 有则补全：`typeck.x` 硬失败 + 复用 `driver_diagnostic_typeck_invalid_{float,ptr}_binop`；seed `typeck_gen` / empty_surface + thin/full/surface diagnostic 文案同 commit |
| 允许 | `~int` · `-int` · `-float` · `!any`（LOGNOT→bool） |
| 拒绝 | `~f32/f64` · `~ptr` · `-ptr` |
| mac 矩阵 | ✅ rv=42 · option/si/hello；探针 T001 硬 / bitnot_int=252 / float_neg=255 · `/tmp/mac_g05_wave289.log` · `/tmp/mac_w289_matrix.log` |
| Ubuntu 矩阵 | ✅ rv=42 · option/si/hello；T001 硬；float_add/neg freestanding run leave-off · `/tmp/ubuntu_g05_wave289.log` · `/tmp/ubuntu_w289_matrix.log` |
| 产品钉盘 | **仍 `75c638465`**（日常 L2 不升钉） |

```text
下一：wave290 Cap residual
  leave-off：
   · AST name/var_name 升布局（仍 cap 63；L011/L012 诚实失败）
   · `*u8 + *u8` 运行时字符串拼接运算符
   · Ubuntu freestanding 运行时 f64（-o 链；emit 已正；host gcc float OK）
   · Ubuntu asm 后端 unary BITNOT 发射（-E 有 `~(3)`；-o 主路径 eax 未写；mac CTFE fold OK）
   · host residual：getenv/system/realpath _impl + resolve pool；freestanding stubs
  钉盘 75c638465；日常 L2，整波/日终再 L4
```
