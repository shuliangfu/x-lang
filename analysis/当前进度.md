# X 语言 / X language 当前进度（2026-07-27 · wave535 lsp_glue 守卫增量）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md）  
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`f1250f415`**（双端 L4 真冷全测 129 全绿，wave534；wave535 增量守卫未升钉盘） |
| 钉盘 | **`f1250f415`**（升自 `d5de2f13b`；双端 L4 129 全绿） |
| bstrict | **129** |

## wave535（进行中 · runtime_lsp_glue R2 守卫增量 — 守卫已落，thin 未切）

| 项 | 状态 |
|----|------|
| 触发 | wave533 记录的 `runtime_lsp_glue` 3 重障碍（44 函数未守卫 + 31 `_impl` 重命名缺失 + g05 双 thin 源），本波系统性消除障碍 1+2 |
| 范围 | seeds/runtime_lsp_glue.from_x.c 增量守卫（不切 thin，不碰 g05/Makefile） |
| 守卫提交 | `1ab0afcbb` — `feat(lsp-glue): add XLANG_L2_LSP_GLUE_FULL_FROM_X guards for R2 migration` |
| 守卫内容 | 21 SKIP（`#ifndef`/`#endif`，纯 .x 函数体 R2 路径省略）+ 17 RENAME（`#ifndef`/`#else`，_impl 签名切换匹配 .x `*u8` 契约）+ 26 函数前向声明块（`#ifdef`，内部调用解析到 extern→.x wrapper→_impl） |
| 新宏 | `XLANG_L2_LSP_GLUE_FULL_FROM_X`（Makefile/g05 未定义 = 守卫休眠，冷路径字节级不变） |
| 冷路径验证 | 编译通过 · 2 个预存告警（line 1858 format / line 2882 comment，与改动无关）· 无回归 |
| R2 路径验证 | `-DXLANG_L2_LSP_GLUE_FULL_FROM_X=1` 编译通过 · **0 错误** · 21 个预期 `-Wpointer-sign`（`uint8_t*`→`char*`，源于 `*u8`↔`uint8_t*` 精确类型匹配规则） |
| 回归证明 | `make xlang` 失败为预存问题（stash 后干净树 `f1250f415` 同样失败，需 bootstrap 而非直接 make）· 与 lsp_glue 无关 |
| 障碍 3 状态 | g05 双 thin 源未解（`lsp_fmt_pure_thin.x` 17 个 vs `runtime_lsp_glue.x` 60 个 `#[no_mangle]` 部分重叠）· 留待 thin 切换波次 |
| 构建级别 | L2 编译验证（守卫休眠，未动产品面，不触发 L4） |

## wave534（已闭 · 双端 L4 129 全绿 · 钉盘 `f1250f415`）

| 项 | 状态 |
|----|------|
| 范围 | wave528-532 共 4 个 R2 migration 收口（http_glue thin+rest + slice_glue/path_fast/std_runtime_fast DIRECT） |
| macOS L4 | remaining_o=0 ✅ · bstrict OK (129) exit 0 ✅ · 9m47s |
| Ubuntu L4 | remaining_o=0 ✅ · bstrict OK (129) exit 0 ✅ · 3m25s |
| 钉盘 | `f1250f415`（升自 `d5de2f13b`） |

## 下一步候选

1. **wave536：runtime_lsp_glue thin 切换** — 解障碍 3（g05 统一 thin 源，合并 `lsp_fmt_pure_thin.x` 与 `runtime_lsp_glue.x` 重叠函数），Makefile 加 R2 规则，`XLANG_L2_LSP_GLUE_FULL_FROM_X=1` 激活，跑 L2 验证 thin+rest 链接
2. **wave537：lsp_glue L4 收口** — thin 切换绿后整波 L4 真冷全测，升钉盘
3. **继续审计其他 half-done 模块**
