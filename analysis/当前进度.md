# X 语言 / X language 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **重命名分析**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)  
> **改名前锚点**：`anchor-pre-xlang-rename` @ `d07bcf155`（父钉曾 `a77fc5d90`）  
> **项目目录**：现行 `…/xlang/x-lang`（mac/Ubuntu/Windows 同构）  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only**（第三品牌 `shu_*` / `SHU_*` / `shux` 已清） |
| 主域 | **xlang.dev** |
| 工程 | **全库重命名 R0–R3 已闭** · 双端 L4 **129 全绿** · 钉盘 **`3698e557f`** |
| process_xlang 兼容别名 | **已删除** |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **已恢复** · **wave251** Cap residual pure 已闭（user-face `link_abi_getenv` in runtime_panic） |

## 验收

### tip L4 钉盘（不变）

| 项 | 状态 |
|----|------|
| SHA | **`3698e557f`**（`rename(R3): shu_* → xlang_*`） |
| 构建级别 | **L4 真冷**（全擦 compiler/std/core `.o` + 重链产品二进制） |
| mac / Ubuntu 全量 bstrict | ✅ **OK (129)** |

### 本波 tip（wave251 · 日常 L2，**≠** tip L4 升钉）

| 项 | 状态 |
|----|------|
| SHA | **`e6567d260`** — R2 Cap residual pure wave251 |
| 构建级别 | **L2**（`XLANG_G05_PREFER_X_O=0` g05 + 产品矩阵 + MINIMAL；panic.o `nm` face） |
| mac 矩阵 | ✅ rv=42 · option · si · hello · MINIMAL rv=42 · `/tmp/mac_g05_wave251.log` · `/tmp/mac_w251_*.log` |
| Ubuntu 矩阵 | ✅ rv=42 · option · si · hello · MINIMAL rv=42 · `/tmp/ubuntu_wave251_g05.log` · `/tmp/ubuntu_w251_*.log` |
| 产品钉盘 | 仍 **`3698e557f`**（本波未 L4 升钉） |

```text
下一：wave252 Cap residual
  （用户面 face 策略已开 @ wave251：runtime_panic C seed 用户域 cold twin
     link_abi_getenv + _impl；CRASH_EVIDENCE residual → face
   · 余 STD_AND_PANIC 叶 backtrace/env_os/process_os/log_os/http_glue
     仍 raw getenv；可共链 panic twin 或抽 runtime_link_abi_user_env.o
   · Linux x86_64 产品 panic 仍 .s（无 getenv）；C seed 面在 mac/非 x86_64 C 路径
   · runtime.from_x.c fprintf CRASH_EVIDENCE 用户 C 模板保留 raw getenv
   · host residual 产品 g05 仅 link_abi_getenv_impl / link_abi_system_impl
   · freestanding bootstrap_nostdlib_stubs 自实现 getenv/system 保留
   · 可选下叶：余 STD_AND_PANIC 叶迁 face；resolve 池 _impl 体）
  钉盘仍 3698e557f；日常 L2，整波收口再 L4
```
