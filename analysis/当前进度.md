# X 语言 / X language 当前进度（2026-07-28 · wave612 Cap dual ARRAY_LIT INDEX binop · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`05dd5df6f`**（wave612 Cap dual ARRAY_LIT INDEX binop rbx · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave612（已闭 · Cap residual freestanding dual ARRAY_LIT INDEX binop rbx · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `return [10,32][0]+[10,32][1]` freestanding 错值（mac=114 / Ubuntu=218；host-C 与 `let a=…; let b=…; a+b` 已绿） |
| tip 产品 | **`05dd5df6f`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave612 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| dual INDEX binop rbx 互踩 | `glue_binop_operand_index_addr_clobbers_rbx_elf_c` 把 TYPE_ARRAY+lit 当「不碰 rbx」 | ✅ 物化基（ARRAY_LIT/STRUCT_LIT/CALL/METHOD/嵌套 INDEX）恒 clobber |

**数据流（五问）**：产生 = 交换律 binop 右装 rbx → 左 INDEX 再 emit；存储 = 分类器布尔；消费 = `glue_binop_preserve_rbx_for_index_elf_c`。旧路径：分类器仅对 TYPE_SLICE / 非 lit 标 1；`pipeline_asm_emit_array_lit` 始终 `mov rax→rbx` 停 payload 基址 → 第二叶盖掉第一叶标量结果。权威：G.7 **补全同一分类器**（preserve/restore 路径已有，不新开第二套 spill）。

**权威**：`compiler/pipeline_glue.c` · `glue_binop_operand_index_addr_clobbers_rbx_elf_c`。改后 rm `pipeline_x.o` + `build_asm/pipeline_glue_standalone.o` + g05。

### wave612 验证

| 探针 | mac L2 host/fs | Ubuntu L2 host/fs |
|------|----------------|-------------------|
| lit_sum `[…][0]+[…][1]` | ✅ 42/42（was fs 114） | ✅ 42/42（was fs 218） |
| lit_mul / lit_sub | ✅ 42/42 | ✅ 42/42 |
| lit_sum_let 回归 | ✅ 42/42 | ✅ 42/42 |
| lit_idx / lit_idx0 | ✅ 32/10 | ✅ 32/10 |
| var_sum / take_field 回归 | ✅ 42/32 | ✅ 42/32 |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/0/0 |

disasm：Ubuntu `push %rbx` 于左 INDEX 前 / `pop %rbx` 后 `add`；mac arm64 `mov x2,x1` 暂存。

### 仍 soft

- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual soft 其它叶，或整波 L4 + 升钉  

## wave611（已闭 · Cap residual ARRAY_LIT INDEX parse+typeck · L2）

tip 产品 **`1c4a631e1`** / docs **`b619af4d6`**。soft「Ubuntu fs dual ARRAY_LIT INDEX binop rbx」本波由 **wave612** 收口。

## wave610（已闭 · Cap residual freestanding FIELD TYPE_ARRAY as T[N] call-arg · L2）

tip 产品 **`d742a7ec7`** / docs **`9d3138f7a`**。
