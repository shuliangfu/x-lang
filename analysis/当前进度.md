# Shux 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-22.md](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave184** empty_cstr + std_io/compress path + effective_link pure @ `1ef8092d1` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **R2 Cap residual pure · empty path stubs + effective_link** → L0 path_pure |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + path_pure prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + path_pure prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：常驻 Cap residual soft — `shux_empty_cstr` / `shux_std_io_o_path` / `shux_std_compress_o_path` / `shux_asm_ld_effective_link_argv0` 体仍 always mega C  
2. **根修 / pure**：真迁 `labi_path_pure.x` L0 pure（count 53→57；empty BSS + peer empty_cstr；effective_link = Cap residual resolve + pure `/shux` join）  
3. **Cap residual**：`shu_resolve_compiler_dir` 仍在 effective_link 内；empty 薄叶不再 always mega  
4. **产品调用方**：invoke_cc / asm ld 缺 argv0 合成；retired std.io / std.compress 空 path API  
5. **验收**：双端 L2 g05 + 矩阵 + string/vec/slice/compress 邻域 + path_pure prove IDENTICAL；钉盘仍 **`3c88f7e51`**  

## 下一波前排

1. 其它 rest R2 / mega residual（可迁：常驻 Cap — undef_sym / path_io stat / wait / strerror / mega spawn-IO / ld_debug_argv / ensure_std_net_o_auto_tls system·make / getenv / shu_resolve_compiler_dir / rel_o_path / path_readable …）  
2. 可选：未闭合块注释硬诊断（EOF depth>0）  
3. 可选 Windows hybrid（**需要时再通知重启**）  

```text
钉盘：3c88f7e51（未升）
tip：1ef8092d1 wave184 empty_cstr + std_io/compress + effective_link pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：其它 rest R2 / mega residual（常驻 Cap）
```
