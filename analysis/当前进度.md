# X 语言 / X language 当前进度（2026-07-28 · wave592 Cap freestanding_io ensure on nostdlib face · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`2b2b2921b`**（wave592 Cap freestanding_io ensure · L2 双端绿） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave592（已闭 · Cap residual freestanding_io companion on nostdlib face · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave591 soft：Ubuntu `-freestanding` true-sret BLD001 missing freestanding_io.o（`xlang_sys_write`） |
| tip | **`2b2b2921b`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave592 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| Ubuntu `-freestanding` true-sret / let / Big32 | prepare 仅 `needs_io` 才 ensure freestanding_io | ✅ needs_io **或** nostdlib face |
| ld `need_io` force（stubs→mmap）无 ensure | skip_missing 前无 `xlang_ensure_freestanding_io_o` | ✅ 对齐 stubs ensure 模式 |
| STRUCT_LIT / short 假绿对照 | 无 memcpy UNDEF → 不触 need_io | ✅ 回归仍 42 |

**数据流（五问）**：产生 = freestanding sret/large 路径 `call memcpy`；存储 = user.o UNDEF `memcpy`；消费 = freestanding ld `need_nostdlib_face` → force `need_io`（stubs 要 `xlang_sys_mmap`）。prepare 只扫 `xlang_sys_*` → 冷树无 `compiler/freestanding_io.o` → BLD001 文案写 write（误导）。权威：G.7 ensure 与 ld 同语义 — 凡 ld 会 need_io 则 ensure。

**权威**：`labi_ensure_list.x`（+ seed/surface 同 commit）· `runtime_link_abi.from_x.c` freestanding ld。改后 `src/runtime_link_abi.o` + g05。

### wave592 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| true sret `-freestanding`（**删** freestanding_io.o 后） | ✅ 42 | ✅ 42（ensure 自愈生成 .o） |
| let / Big32 / two `-freestanding` | ✅ 42 | ✅ 42 |
| STRUCT_LIT / short `-freestanding` 回归 | ✅ 42 | ✅ 42 |
| pure-asm 默认 true-sret | ✅ 42 | ✅ 42 |
| rv / option / si / hello | ✅ 42/0/0/`Hello World` | ✅ 42/0/0/`Hello World` |

### 仍 soft

- 其它 Cap residual 侧门  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual：其它侧门，或整波 L4  
2. 整波双端 L4 + 升钉  

## wave591（已闭 · Cap residual arm64 freestanding >16B sret）

tip `4a1c8def5` / docs `1358552b5`。soft `-freestanding` companion 已由 **wave592** 闭。
