# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`86281cc6`**（双端 L4 真冷 + 129；未升） |
| tip | **R2 driver_abi Cap residual pure wave23**（calloc + host_defaults；日常 L2） |
| 产品 bstrict | **129** · tip L4 钉盘仍 `@86281cc6` |
| 本波性质 | **工程轨 R2 pure 微步**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42/option/si/hello + pure-asm rv42 @ `1c8ff602` |
| mac | ✅ L2 同 |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `runtime_driver_abi` Cap residual pure wave23（本波）**  
   - 真迁：calloc 族 + `driver_asm_pctx_apply_host_defaults`  
   - calloc：`driver_codegen_outbuf_calloc` / `driver_pipeline_dep_ctx_calloc` /  
     `driver_ptr_table_calloc` / `driver_size_table_calloc` /  
     `driver_diag_snapshot_alloc` / `driver_asm_elf_ctx_calloc`  
     → libc `calloc`/`malloc`/`memset` + 固定 LP64 size / `pipeline_sizeof_*`  
   - host_defaults：wave19 字段 setter + `strstr` 针 + 永久 OS residual  
     `shux_driver_host_{default_target_arch,prefer_macho,prefer_coff}`（host `#ifdef`）  
   - seed：cold twins 包 `#ifndef SHUX_L2_RDABI_THIN_FROM_X`；OS residual 始终链接  
   - 仍 seed：table free/get/set、outbuf len/data、tmp 槽、巨型 `rt_preamble` 表  
   - 注释：多行块注释 `*/` 单独成行  
   - 代码 SHA：**`1c8ff602`**（docs 本 commit 跟进）  
   - 矩阵：rv_build=0 / rv_run=42 / opt 0 / si 0 / hello 0 / pure-asm rv42  
   - 钉盘仍 **`86281cc6`**（skill §3.3：微步不自动 tip L4）

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文；**不**改 `当前问题分析.md` / `问题分析文档.md`

## 下一波前排

1. **可选** driver_abi 余 Cap residual（**tmp 访问器** / table free·get·set）  
2. **可选** g05 sed / harness `-E` 评估  
3. **可选** Windows hybrid；Darwin arm64 纯 asm Vec4f CG002  

```text
钉盘：86281cc6（tip L4 双端 129）
tip：R2 driver_abi wave23 calloc+host_defaults pure（L2 @ 1c8ff602）
Windows：本波不需要切换
下一：tmp / table free·get·set / g05 sed / Windows / Darwin CG002
```
