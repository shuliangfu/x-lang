# X 语言 / X language 当前进度（2026-07-26）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **25 号全日归档**：[自举进度-归档-2026-07-25.md](自举进度-归档-2026-07-25.md)（含 26 日初 wave446）  
> **项目目录**：现行 `…/xlang/x-lang`  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| 工程 | **双端 tip L4 129 全绿** · 钉盘 **`70ca1089b`**（升自 `605e7efa4`） |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **续 Cap residual** · **wave447 已闭** · 下一 **wave448** |

## 验收

| 项 | 状态 |
|----|------|
| 钉盘 L4 双端 129 | ✅ **`70ca1089b`** · `/tmp/{mac,ubuntu}_true_{cold,bstrict}_70ca1089b.log` |
| tip 日常 L2 | ✅ wave447 · codegen mono non-identity · 前 tip wave446 `8a8aa1841` |
| 产品 tip L4（含 wave445 ABI） | ⏳ 待阶段性收口（日常 L2 已过，**不**冒充已升钉） |

## Trae 25 日接续 · 审计结论（日切前已闭合）

| 维度 | 结论 |
|------|------|
| 产品代码 wave432–445 | ✅ **真实有效**（trait 签名簇 / bound / mono C1–C6 等，非空 docs） |
| 钉盘 `70ca1089b` 双端 L4 129 | ✅ 日志与文档一致 |
| 文档问题 | ⚠️ 曾误诊 lexer typeck=`&&`；写「排除整棵 trait」与已合入能力矛盾；tip/钉盘口吻混乱 → **wave446 已校正** |
| 结论 | **可以进入下一步**；**不是**「Trae 全程零问题」，而是 **代码可接、文档已修、阻塞根修已闭** |

## 26 日承接（wave446 · 日常 L2 · 已闭 · 已入 25 归档）

| 项 | 状态 |
|----|------|
| SHA | **`8a8aa1841`**（parser）+ **`54293a3aa`**（lexer.x）+ docs **`e1e49fe88`** |
| 债 | Trae 阻塞：lexer.x / 全库 *if condition must be bool*（误诊为 typeck `&&` 链） |
| 根因 | `skip_generic_angle_list` 跨语句贪吞：`pos < n` … 后 `pos > (` 假 generic CALL |
| 根修 | 角列表语句边界中止 + lexer 结构体 lit `;` / docblock 去字面 `/*` |
| 构建级别 | **L2**（parser_asm_thin_glue + bootstrap-driver-seed） |
| 钉盘 | 仍 **`70ca1089b`** |
| 探针 | mac：`pos < n`+`pos > (0 as usize)` 绿 · nest skip_ws 绿 · `xlang -c lexer.x` typeck 绿 |
| soft | dyn；XP003；Darwin residual；wave445 mono ABI tip L4 待收口 |

## 26 日 wave447（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | non-identity generic mono 不 emit → BLD001；arg0 mono 类型用 call 返回类型；i32→i32 self-map `emit_type` 递归 SEGV |
| 根修 | 放宽 identity 门控 + 签名用 ret/param+mono_active；mono 类型优先实参；`conc != type_ref` 防自递归 |
| 权威 | `codegen.x` + seed `codegen_gen.linux.x86_64.c` 同 commit |
| 探针 | getv 字段 / absdiff if / clone / identity / multi_mono / i32-param =42 |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

### 为何文档里有 `fn`/`Self`/`var`？

| 写法 | 说明 |
|------|------|
| **不是**产品矩阵债 | 标准面只认 `function` / `let` / `self: Type` |
| 来源 | wave445 探针曾用 Rust 风格 `fn`/`-> Self`/无类型 `self` 测 mono → 失败被记 soft |
| 正确探针 | `function` + 显式 `self: A` → 已绿；**勿**再用错误语法制造伪 gap |
| `var` | 非关键字；写 `var x` 整函数解析失败，用 `let` |

## 26 日下波（wave448 · 待开）

| 候选 | 说明 |
|------|------|
| A. XP003 | 泛型调用无 turbofish 类型推断 |
| B. R2 真迁 | rest seed 小模块；避开函数指针/`&var` |
| soft 勿抢 | dyn Trait；`fn`/`Self`/`var` 非语言债；Darwin residual；wave445 ABI tip L4 |

**明细流水（wave396–446）** → 只读归档 [`自举进度-归档-2026-07-25.md`](自举进度-归档-2026-07-25.md)。
