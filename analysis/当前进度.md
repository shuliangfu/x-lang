# X 语言 / X language 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **重命名分析**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)  
> **改名前锚点**：`anchor-pre-xlang-rename` @ `d07bcf155`（父钉曾 `a77fc5d90`）  
> **项目目录**：现行 `…/xlang/x-lang`（mac/Ubuntu/Windows 同构）  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only**（第三品牌 `shu_*` / `SHU_*` / `shux` 已清） |
| 主域 | **xlang.dev** |
| 工程 | **全库重命名 R0–R3 已闭** · 双端 tip L4 **129 全绿** · 钉盘 **`75c638465`** |
| process_xlang 兼容别名 | **已删除** |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **已恢复** · **wave294** Cap residual pure 已闭（freestanding f32 MUL `mulss`） |

## 验收

### tip L4 钉盘（仍有效）

| 项 | 状态 |
|----|------|
| SHA | **`75c638465`**（wave288：invoke_cc push `runtime_link_abi_user_env.o`） |
| 构建级别 | **L4 真冷**（全擦 compiler/std/core `.o` + 重链产品二进制） |
| remaining_o 起编 | ✅ **0**（双端） |
| mac 全量 bstrict | ✅ **OK (129)** · `/tmp/mac_true_cold_75c638465.log` · `/tmp/mac_true_bstrict_75c638465.log` |
| Ubuntu 全量 bstrict | ✅ **OK (129)** · `/tmp/ubuntu_true_cold_75c638465.log` · `/tmp/ubuntu_true_bstrict_75c638465.log` |
| 前钉 | `3698e557f`（R3 重命名 tip L4；wave238–287 仅 L2） |

### 本波 tip（wave294 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`ddd24de58`** — R2 Cap residual pure wave294（freestanding f32 MUL `mulss`） |
| 构建级别 | **L2**（rebuild `backend_enc_dispatch.o` + `pipeline_glue_standalone.o` + g05 + 探针 + 产品矩阵；**非 tip L4**） |
| 根因 | `glue_emit_binop_mul` 仅双方 scalar f64 走 `mulsd`；纯 **f32×f32** 落 `imul` IEEE 位型 → Ubuntu freestanding run=0（mac host-gcc 可藏；`addss` 已有对称缺口） |
| 修法 | G.7：新 `backend_enc_mulss_rax_rbx_arch`（邻接 `addss`/`mulsd`；seed + thin/full `.x` + surface 同 commit；thin u32 `0xc1590ff3=3243839475`）；`glue_emit_binop_mul` 接线 f32；整数 lit 快路径排除 f32 |
| 探针 | Ubuntu freestanding：`f32*f32`=42 · disasm `mulss`；`f64*f64`/`f32+f32`/cast 回归绿；`f32*f64` / `u64 as f64` 仍 leave-off |
| mac 矩阵 | ✅ rv=42 · option/si/hello · host float 绿 · `/tmp/mac_g05_wave294.log` · `/tmp/mac_w294_matrix.log` |
| Ubuntu 矩阵 | ✅ rv=42 · option/si/hello · freestanding f32 mul 绿 · `/tmp/ubuntu_g05_wave294.log` · `/tmp/ubuntu_w294_probes2.log` |
| 产品钉盘 | **仍 `75c638465`**（日常 L2 不升钉） |

```text
下一：wave295 Cap residual
  leave-off：
   · AST name/var_name 升布局（仍 cap 63；L011/L012 诚实失败）
   · `*u8 + *u8` 运行时字符串拼接运算符
   · host residual：getenv/system/realpath _impl + resolve pool；freestanding stubs
   ·（可选）u64→f64 cast（缺 TYPE_U64 kind=4 + REX.W cvtsi2sd）；f32*f64 mixed mul 提升路径
  钉盘 75c638465；日常 L2，整波/日终再 L4
```
