# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`11a57351`**（双端 129） |
| tip | **`b12bf000`**（R2 async_liveness pure + prove -E retry） |
| 产品 bstrict | **129** · 钉盘双端全绿 |
| 本波性质 | R2 `async_liveness` pure surface + harness retry |
| 工程轨 prove | ✅ mac IDENTICAL · Ubuntu 5/5 IDENTICAL |
| Windows | **本波不需要**（无 hybrid/Windows 探针） |

## 本波摘要

1. **R2 `async_liveness` pure surface**（`f2ca6580`）  
   - `.x`：pure helpers（await walk / live frame / mangle / `frame_build_tag`）  
   - seed：`frame_build_tag` 纳入 `#ifndef SHUX_ASYNC_LIVENESS_FROM_X`；`#else` `slice_marker`  
   - Cap residual 仍 seed C：lookup / type_to_c / layout / analyze / emit FILE*  
   - `analyze_block_linear`：`malloc(4096)` 替代 `u8[4096]`（Ubuntu -E 栈类债）  
   - surface + `prove_module_selfhost` 条目  

2. **prove harness**（`b12bf000`）  
   - Ubuntu `shux -E` 偶发 SIGSEGV（小模块亦见）→ 最多 5 次重试 + 0.3s sleep  
   - IDENTICAL nm 仍硬失败；**非**产品 soft-skip  

3. 前序：R2 `async_asm_pool`（`2a88053b`）· P1 preprocess/libc · 钉盘 `11a57351`

## 下一波前排

1. **R2 下一 rest** 或 **async_liveness Cap residual**（layout/type/emit）  
2. **可选** tip 升钉：Ubuntu L4 + 双端 129（含 SHARED + 本波 R2）  
3. **可选** Windows hybrid（需切双系统时再喊人）  
4. **可选** async_asm_pool 产品 unbundle；Ubuntu `-E` SIGSEGV 根因（编译器）

```text
钉盘：11a57351
tip：b12bf000（R2 async_liveness pure；双端 L2 prove 5/5）
Windows：本波不需要切换
下一：R2 下一 rest / Cap residual 或 tip L4 升钉
```
