# Shux 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-22.md](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave180** scheduler_o_for_task_link pure @ `e3b091eb7` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **R2 Cap residual pure · scheduler_o_for_task_link 叶** → L0 path_pure |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + path_pure prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + path_pure prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：常驻 Cap residual — `scheduler_o_for_task_link` 体仍 always mega C（strstr + memmove + access F_OK + realpath）  
2. **根修 / pure**：真迁 `labi_path_pure.x` L0 pure orch（explicit 短路 + pure cstr copy + pure needle scan + expand rewrite + Cap residual `link_abi_path_readable` + `link_abi_realpath_cap`；count 22→23）  
3. **Cap residual**：`link_abi_path_readable` / `link_abi_realpath_cap` 常驻 mega（access / realpath）  
4. **产品调用方**：`shux_invoke_cc_impl` / asm ld task 链 scheduler  
5. **验收**：双端 L2 g05 + 矩阵 + string/vec/slice/compress 邻域 + path_pure prove IDENTICAL；钉盘仍 **`3c88f7e51`**  

## 下一波前排

1. 其它 rest R2 / mega residual（可迁：常驻 Cap — undef_sym / path_io stat / wait / strerror / mega spawn-IO / ld_debug_argv / ensure_std_net_o_auto_tls system·make / getenv / shu_resolve_compiler_dir / bootstrap_nostdlib …）  
2. 可选：未闭合块注释硬诊断（EOF depth>0）  
3. 可选 Windows hybrid（**需要时再通知重启**）  

```text
钉盘：3c88f7e51（未升）
tip：e3b091eb7 wave180 scheduler_o_for_task_link pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：其它 rest R2 / mega residual（常驻 Cap）
```
