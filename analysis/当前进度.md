# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave132** runtime PRIMARY OS pure @ `f932033fc` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **其它 rest R2 · Cap residual pure 可迁叶**（`labi_user_needs_runtime_{time_os,random_fill,env_os}`） |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + env/time/random + prove labi_ondemand_list **IDENTICAL** |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + env/time/random + prove labi_ondemand_list **IDENTICAL** |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：wave131 后 PRIMARY OS bulk gates 可迁 pure-logic（exact UNDEF 表；null/empty → 1 legacy hard-link）  
2. **根修**：pure 迁入 `labi_ondemand_list.x`（time_os ×10 + random_fill ×12 + env_os ×19 + pure orch）  
3. **G.7**：权威表取 seed 完整表面；单一 L8b 表+orch；Cap residual `undef_sym`  
4. **容量裁剪**：同模块再并 `process_argv`+`std_task` 会触 codegen 截断（丢 `rel_*`、hybrid 回退 cold）；二者**仍 mega static**  
5. **验收**：双端 L2 g05 + §4 矩阵 + env/time/random + prove **IDENTICAL**；钉盘仍 **`3c88f7e51`**

## 下一波前排

1. 其它 rest R2 可迁叶（`labi_user_needs_runtime_process_argv` / `labi_user_needs_std_task` 需先抬本模块 codegen 容量或拆 L8b 文件；fk0 表 / 其它 string needs_*；常驻 Cap 不硬迁）  
2. **可选** Windows hybrid（**需要时再通知重启**）  
3. 其它工程 residual（labi_ondemand_list 模块容量 / g05 sed 等，按债裁剪）  

```text
钉盘：3c88f7e51（未升）
tip：f932033fc wave132 runtime PRIMARY OS pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：process_argv / std_task（容量）/ fk0 / 其它 string needs_* pure
```
