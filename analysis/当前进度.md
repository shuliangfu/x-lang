# X 语言 / X language 当前进度（2026-07-29 · wave695 host-C [][][][][]T + [][][][]Named fat · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 05:59 · 28 全日（wave534–617）已归楼；本文仅保留 29 日 tip 快照

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`0e4fec690`**（wave695 host-C `[][][][][]T` + `[][][][]Named` fat layouts） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave695（已闭 · Cap residual host-C [][][][][]T + [][][][]Named fat · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave694 soft：更深五层+ / NAMED 四层；host-C `take([][][][][]i32)` / `take([][][][]Cell)` BLD001 incomplete/conflicting types |
| tip 产品 | **`0e4fec690`** |
| 构建级别 | **L2**（mac+Ubuntu bootstrap-driver-seed + g05；**非** L4；钉盘不升） |

### wave695 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 缺四层 nested scalar layout | XLANG_SLICE_LAYOUTS 仅 wave694 三层 → 缺 `xlang_slice`×5 + elem | ✅ 补五层标量布局（rt_preamble + emit_header；行长≤167 → u8[192]） |
| 缺 `[][][][]Named` companion | companion 仅三层 `xlang_slice`×3 + TAG | ✅ 同权威再发 `xlang_slice`×4 + TAG |

**数据流**：产生=`take(a:[][][][][]i32)` / `take(a:[][][][]Cell)` host-C type_to_c_repr + companion。存储=XLANG_SLICE_LAYOUTS + companion 表。消费=host-cc 完整 struct。G.7：补全同一 layout/companion 权威；seed 同 commit。

**权威**：`seeds/rt_preamble.from_x.c` · `src/codegen/codegen.x` · `seeds/codegen_gen.linux.x86_64.c`（同 commit）

### wave695 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| host-C nested5 sig take → 30 | ✅ | ✅ |
| pure-asm nested5 → 30 | ✅ | ✅ |
| Ubuntu freestanding nested5 → 30 | — | ✅ |
| host-C nested_named4 take → 35 | ✅ | ✅ |
| pure-asm/fs named4 → 35 | ✅ | ✅ |
| wave694 nested4=30 · named3=35 · wave693=10 · wave692=60 · 矩阵 rv42/option/hello/si | ✅ | ✅ |
| 标签：`xlang_slice`×5 + `int32_t` · companion 四层 `…_xlang_slice_<pfx>Cell` | ✅ | ✅ |

日志：`/tmp/mac_g05_wave695.log` · `/tmp/ubuntu_g05_wave695.log` · `/tmp/ubuntu_w695_probes2.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**；P011 更早失败）  
- LANG-006 标量 let/const bool→int（**产品保留**）  
- 更深六层+ / 其它复合嵌套边缘（未扩）  
- 深 nest 字面量 assign/call 的 typeck fixed-array vs slice（未本波）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self；LANG-006 保留），或整波 L4 + 升钉  

## wave694（已闭 · Cap residual host-C [][][][]T + [][][]Named fat · L2 工程轨）

见 wave694 节历史；tip **`f96dd3a35`** · 钉盘仍 **`01479a857`**。更深 nested / NAMED 已由 **wave695** 闭。
