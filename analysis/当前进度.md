# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **pipeline_abi wave78 pure residual** @ `c6e36f0e` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **R2 pipeline_abi Cap residual pure wave78**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + pure-asm 矩阵 rv42 / option / hello / stdlib-import；wave78 pure **W**（slot_clear / fputs / fp_is_stdout / fclose_file）@ `c6e36f0e` |
| mac | ✅ L2 PREFER hybrid + c-backend 矩阵（Darwin pure-asm CG002 tip 既有）@ `c6e36f0e` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave78** @ `c6e36f0e`  
   - **迁 pure soft residual**：`shu_lsp_ptr_slot_clear`（G.7 `shux_ptr_slot_set` null）+ `shux_fputs_stdout`（G.7 g05 `stdout_ptr` + `fputs_opaque`）+ `driver_asm_fp_is_stdout` / `driver_asm_fclose_file`（G.7 g05 stdout compare + `fclose_opaque`）  
   - **seed**：冷孪生在 `#ifndef FROM_X`；FROM_X rest **extern** 供 hybrid 调用 pure  
   - **Cap residual 仍**：fn-ptr / product_emit / typeck_module C 前端 / `pipeline_sizeof_*` / preprocess 引擎 / OS realpath  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave78.log` · `/tmp/ubuntu_g05_wave78.log`

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：pipeline_abi 硬 Cap residual 叶（fn-ptr / product_emit / typeck_module C 前端；sizeof / preprocess / OS realpath…）/ 整波 tip L4  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave78 未升钉）
产品 tip：c6e36f0e pipeline_abi residual pure wave78 L2
Windows：本波不需要切换
下一：pipeline_abi 硬 Cap residual / 整波 tip L4（需时再说）
```
