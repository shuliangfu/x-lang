# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave131** compress family pure @ `ddc854c04` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **其它 rest R2 · Cap residual pure 可迁叶**（mega compress family pure orch） |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + compress + prove labi_ondemand_list **IDENTICAL** |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + compress + prove labi_ondemand_list **IDENTICAL** |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：wave130 后 compress family 可迁 pure-logic（marker + UNDEF/prefix 表；Cap residual `exports_marker` / `has_undef_sym`）  
2. **根修**：pure 迁入 `labi_ondemand_list.x`（zlib/zstd/brotli tables + `link_abi_obj_needs_*` + `needs_compress_libs` pure orch）  
3. **G.7**：权威表取 seed 完整表面（zlib exact ×4 + zstd prefix ×2 + brotli exact ×2 + 三 marker）；单一 L8b 表+orch  
4. **权威**：full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove IDENTICAL；mega 体→ forward decls  
5. **验收**：双端 L2 g05 + §4 矩阵 + `tests/compress` + prove **IDENTICAL**；钉盘仍 **`3c88f7e51`**

## 下一波前排

1. 其它 rest R2 可迁叶（mega pure-logic：`labi_user_needs_runtime_*` / `labi_user_needs_std_task` / fk0 表 / 其它 string needs_*；常驻 Cap 不硬迁）  
2. **可选** Windows hybrid（**需要时再通知重启**）  
3. 其它工程 residual（g05 sed 防御层等，按债裁剪）  

```text
钉盘：3c88f7e51（未升）
tip：ddc854c04 wave131 compress family pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：runtime_* / std_task / fk0 / 其它 string needs_* pure
```
