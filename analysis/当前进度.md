# X 语言 / X language 当前进度（2026-07-24）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **重命名分析**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)  
> **改名前锚点**：`anchor-pre-xlang-rename` @ `d07bcf155`（父钉曾 `a77fc5d90`）  
> **项目目录**：现行 `…/xlang/x-lang`（mac/Ubuntu/Windows 同构）  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only**（第三品牌 `shu_*` / `SHU_*` / `shux` 已清） |
| 主域 | **xlang.dev** |
| 工程 | **全库重命名 R0–R3 已闭** · 双端 tip L4 **129 全绿** · 钉盘 **`75c638465`** |
| process_xlang 兼容别名 | **已删除** |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **已恢复** · **wave302** Cap residual pure 已闭（i32-family int→f32/f64 kinds u8+NAMED） |

## 验收

### tip L4 钉盘（仍有效）

| 项 | 状态 |
|----|------|
| SHA | **`75c638465`**（wave288：invoke_cc push `runtime_link_abi_user_env.o`） |
| 构建级别 | **L4 真冷**（全擦 compiler/std/core `.o` + 重链产品二进制） |
| remaining_o 起编 | ✅ **0**（双端） |
| mac 全量 bstrict | ✅ **OK (129)** · `/tmp/mac_true_cold_75c638465.log` · `/tmp/mac_true_bstrict_75c638465.log` |
| Ubuntu 全量 bstrict | ✅ **OK (129)** · `/tmp/ubuntu_true_cold_75c638465.log` · `/tmp/ubuntu_true_bstrict_75c638465.log` |
| 前钉 | `3698e557f`（R3 重命名 tip L4；wave238–287 仅 L2） |

### 本波 tip（wave302 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`26c8538aa`** — R2 Cap residual pure wave302（i32-family int→f32/f64 kinds u8+NAMED） |
| 构建级别 | **L2**（rebuild `pipeline_glue_standalone.o`/`pipeline_x.o` + g05 + 探针 + 产品矩阵；**非 tip L4**） |
| 根因 | `pipeline_asm_emit_as_elf` f32 路径 kinds `0/3/13`：`13` 是 VECTOR 非 NAMED；缺 `u8=2`（f64 已有）；`NAMED=8`（i8/i16/u16）从不命中 → 再发射整数位型 + 外层 `as i32` 对 denormal 做 `cvttss2si` → freestanding `(u8 as f32)` / `(i16 as f32)*f32` **run=0**（i32→f32 / u8→f64 绿） |
| 修法 | G.7：f32/f64 的 i32 族 kinds 统一为 `0/2/3/8`；复用既有 `cvtsi2ss`/`cvtsi2sd` eax encoder（无新 encoder） |
| 探针 | Ubuntu：`u8/i16/u16 as f32`=6 · `i16/u16 as f64`=6 · `u8 as f32 * f32`=42 · disasm `cvtsi2ss %eax,%xmm0` · i32/u64→f32 / pure f32 mul 回归 |
| mac 矩阵 | ✅ rv=42 · option/si/hello · `/tmp/mac_g05_wave302.log` |
| Ubuntu 矩阵 | ✅ rv=42 · option/si/hello · `/tmp/ubuntu_g05_wave302.log` · `/tmp/ubuntu_w302_probes.log` |
| 产品钉盘 | **仍 `75c638465`**（日常 L2 不升钉） |

```text
下一：wave303 Cap residual
  leave-off / soft residual：
   · f32/f64 → i64/u64/u32/usize/isize（缺 cvttss2si/cvttsd2si 宽目标；Ubuntu fs run=0）
   · AST name/var_name 升布局（仍 cap 63；L011/L012 诚实失败）
   · `*u8 + *u8` 运行时字符串拼接运算符
   · host residual：getenv/system/realpath _impl + resolve pool；freestanding stubs
   · u64→f32/f64 当值 >2^63-1 的无符号转换序列（ISA 为 signed i64 convert）
  钉盘 75c638465；日常 L2，整波/日终再 L4
```
