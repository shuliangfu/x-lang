# X 语言 / X language 当前进度（2026-07-28 · wave589 Cap freestanding CALL.field · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`f82b1390a`**（wave589 Cap freestanding CALL.field · L2 双端绿） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave589（已闭 · Cap residual freestanding FIELD_ACCESS on CALL return · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Cap residual soft leave：`mk().f…` CG002（误归为「长字段」） |
| tip | **`f82b1390a`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave589 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| freestanding `mk().x` CG002 | asm FIELD_ACCESS | ✅ CALL/METHOD 基址 rvalue 物化 + field load |
| soft 误诊「仅长字段」 | 进度文 | ✅ 短名 `mk().x` 同路径失败 |

**数据流（五问）**：产生 = freestanding `EXPR_FIELD_ACCESS` 基为 `EXPR_CALL`/`METHOD_CALL`；存储 = 帧 temp（≤16B dual-GP/`store_retval`）；消费 = layout field offset load。旧路径走 `lvalue_eff_addr`（仅 VAR/INDEX/DEREF）→ CALL 基 -1 → CG002。host-C 临时量 `.` 掩盖。

**权威**：G.7 `pipeline_glue.c` `glue_field_access_call_base_rvalue_elf_c`（复用 `glue_store_retval_pair` ≡ `let s = mk()`）。改 glue → `pipeline_x.o` + g05。

### wave589 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `mk().x` short | ✅ 42 | ✅ 42 |
| `mk().f` 4/63/64/75/127 | ✅ 42 | ✅ 42 |
| `let s = …; s.f` 同宽 | ✅ 42 | ✅ 42 |
| two-field `mk().a+mk().b` | ✅ 42 | ✅ 42 |
| `-freestanding` fld75 | （mac hybrid） | ✅ 42 |
| rv / option / si / hello | ✅ 42/0/0/`Hello World` | ✅ 42/102/0/`Hello World` |

### 仍 soft

- freestanding `mk().f` **>16B** sret rvalue 字段（可用 `let s = mk()`）  
- 其它 Cap residual 侧门 / 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual：其它 `>63` 侧门 / large sret rvalue field，或整波 L4  
2. 整波双端 L4 + 升钉  

## wave588（已闭 · Cap host-C designator 64→128）

tip `2b18debe0` / docs `9e7d83cf0`。
