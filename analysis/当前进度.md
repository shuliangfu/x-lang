# X 语言 / X language 当前进度（2026-07-29 · wave627 Cap pure-asm INDEX STRUCT_LIT assign + multi-dim flat · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`ab3873604`**（wave627 Cap pure-asm INDEX STRUCT_LIT assign · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave627（已闭 · Cap residual pure-asm INDEX STRUCT_LIT assign + multi-dim flat · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Ubuntu pure-asm `a[i]=Pt{…}` sum=32 / `S24[i]=` run=3 / multi-dim `Pt[2][1]` sum=32（mac 假绿；host-C 42） |
| tip 产品 | **`ab3873604`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave627 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| INDEX assign STRUCT_LIT | emit rhs slot=-1 @ next_offset 别名 a[0] 再 bulk≤8 | ✅ TYPE_ARRAY+lit → 字段直写 elem_home |
| multi-dim nested ARRAY_LIT of STRUCT_LIT | flat path 同 rvalue 别名 + store clamp | ✅ flat 叶 STRUCT_LIT/>8B 走 glue_emit_struct_type_let_init |
| TYPE_SLICE 误用 in-place | fat dual-GP home 当 payload | ✅ 仅 base_tk==TYPE_ARRAY 门控 |

**数据流（五问）**：产生 = `a[i]=StructLit` 经 rvalue STRUCT_LIT(slot=-1) 写 `next_offset`（≡ a[0]）；存储 = 第二笔字段写覆盖 a[0]；消费 = finish_store 把 8B 拷到 a[i] → sum=a[0].x+a[1].y=0+32。G.7：**扩** wave626 同一 in-place 权威至 INDEX assign + array_lit_flat（禁第二套 store）。

**权威**：`pipeline_glue.c` only（`pipeline_asm_emit_assign_elf_c` INDEX 叶 + `pipeline_asm_emit_array_lit_flat_elf_c`）。改后 **须** `pipeline_glue_standalone.o` + g05。

### wave627 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `a[0]=Pt; a[1]=Pt` sum / p0x | ✅ 42 / 10 | ✅ 42 / 10 |
| multi-dim `Pt[2][1]` sum | ✅ 42 | ✅ 42 |
| `S24[i]=` sum | ✅ 42 | ✅ 42 |
| fixed let / field asg / `[]Pt` 回归 | ✅ 42 | ✅ 42 |
| freestanding 同上 | ✅ | ✅（须 crt0_user.o） |
| host-C | ✅ | ✅ |
| rv / option / si / hello | ✅ | ✅ |
| Ubuntu disasm | — | a[1] 字段直写 @−0x18 |

### 仍 soft

- 其它 Cap residual 叶（slice INDEX STRUCT_LIT 大对象仍走 generic；var-index STRUCT_LIT assign）；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一叶，或整波 L4 + 升钉  

## wave626（已闭 · Cap residual pure-asm fixed `Pt[N]` STRUCT_LIT in-place · L2）

tip 产品 **`becc92c72`** / docs **`dbf9a2fe7`**。fixed let-init 绿；INDEX assign 本波 **wave627** 闭。

## wave625（已闭 · Cap residual pure-asm `[]Named` force_esz + FIELD dual-slot · L2）

tip 产品 **`b278583c4`** / docs **`994568244`**。

## wave624（已闭 · Cap residual host-C `[]NamedStruct` fat slice · L2）

tip 产品 **`97b3e5d82`** / docs **`ad577c123`**。

## wave623（已闭 · Cap residual host-C bare `-E` slice layouts · L2）

tip 产品 **`0081c8812`** / docs **`901d50233`**。

## wave622（已闭 · Cap residual pure-asm slice lit call-arg · L2）

tip 产品 **`f917861d7`** / docs **`827134001`**。
