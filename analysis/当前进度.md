# X 语言 / X language 当前进度（2026-07-27）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **25 号全日归档**：[自举进度-归档-2026-07-25.md](自举进度-归档-2026-07-25.md)（含 26 日初 wave446）  
> **项目目录**：现行 `…/xlang/x-lang`  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| 工程 | **双端 tip L4 129 全绿** · tip L4 **`d812aeb41`**（双端，wave499-500）· 产品 tip **`d812aeb41`**（wave499-500）· 钉盘 **`d812aeb41`**（升自 `c3a81bfd6`；双端验证通过） |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **续 Cap residual** · **wave500 已闭（Ubuntu L4 双端验收 wave491-498）** · 下一 **wave501** |

## 27 日 wave500（已闭 · Ubuntu L4 双端验收 wave491-498 整体功能波）

| 项 | 状态 |
|----|------|
| 触发 | wave499 mac L4 已绿 `d812aeb41`，按 AGENTS.md「SHARED 改动双端验证」+ 钉盘升级条件（Ubuntu 金标 + mac 双端 L4 全绿）触发 Ubuntu L4 验收；wave491-498 改 typeck + codegen + parser_asm 产品面 SHARED 改动，需 Ubuntu 金标验证无回归 |
| 流程 | ① git push `self-hosting` 到 origin → ② SSH ubuntu-server `git pull` → ③ 全擦 `compiler|std|core` 下全部 `.o` + 产品二进制（`xlang`/`xlang_asm`/`xlang-c`/`bootstrap_xlangc`）→ `remaining_o=0` ④ `make bootstrap-driver-seed` 冷启动 → ⑤ `FULL=0 scripts/g05_prepare_and_relink.sh` relink → 二进制 09:03 本波 mtime ⑥ 探针验证 ⑦ 全量 `XLANG_BSTRICT_SKIP_BUILD=1 run-all-bstrict.sh` |
| 探针 | `/tmp/w498_multi_combo_probe.x`：`Box<i32>.get()=42` + `Box<bool>.get()=true` → exit 0 ✓（Ubuntu 冷编译验证 wave498 功能） |
| bstrict | `run-all-bstrict OK (129 scripts)` exit 0（耗时 3 分 24 秒）✓ |
| 判据自查 | remaining_o=0 ✓ · 二进制本波 mtime ✓ · 探针全绿 ✓ · bstrict exit 0 + 129 scripts ✓ · 与 mac L4 结果一致 ✓ |
| 非回归证据 | wave491-498 改 typeck + codegen + parser_asm 产品面双端无回归；`simd-s4 FAIL: at_builtin_smoke.x` 是 std-simd-shuffle-select-gate 内部 non-fatal warning（gate 整体 OK，同 mac L4 + 钉盘模式） |
| tip L4 | 升 **`d812aeb41`**（双端）· 前基线 `c3a81bfd6`（wave495） |
| 钉盘 | 升 **`d812aeb41`**（升自 `c3a81bfd6`；双端 L4 全绿） |
| 关键教训 | ① **SHARED 改动必须双端验证**：mac `ld -dead_strip` 可能藏 UNDEF，链接完整性真值在 Ubuntu 全链；mac L4 绿只能升 tip L4，不能升钉 ② **bstrict FAIL 不等于回归**：需区分 gate 内 non-fatal warning（`simd-s4 FAIL` 但 `std-simd-shuffle-select gate OK`）与真测试失败；双端 simd-s4 模式一致 = 预存非本波问题 ③ **整波 L4 节奏**：wave491-498 八波日常 L2 后做一次 mac L4 + 一次 Ubuntu L4，符合「整体功能收口 = 主闸门」；升钉 = 双端验证，不升钉 = 单端 tip |

## 27 日 wave499（已闭 · mac L4 真冷验收 wave491-498 整体功能波）

| 项 | 状态 |
|----|------|
| 触发 | wave498 完成多 combo 泛型 impl 方法 codegen 单态化，wave491-498 构成「泛型 inherent impl 方法完整链路」整体功能波（Self 返回字面 + default 方法 → typeck 推断 → codegen unique combo → codegen multi-combo → 调用端 mangle），按自举方法.md §0.2「整波结束做一次扎实 L4」触发；wave498 改 codegen（codegen.x + seed）产品面 SHARED 改动，需 L4 验证无回归 |
| 流程 | ① 全擦 `compiler|std|core` 下 193 个 `.o` + 产品二进制（`xlang`/`xlang_asm`/`xlang-c`/`bootstrap_xlangc`）→ `remaining_o=0` 确认 ② pin 产品 seed（`codegen_gen.linux.x86_64.c` / `parser_gen.linux.x86_64.c` / `typeck_gen.linux.x86_64.c`）③ `make bootstrap-driver-seed` 冷启动 → ④ `FULL=0 scripts/g05_prepare_and_relink.sh` relink → 二进制 08:37 本波 mtime ⑤ 探针验证 ⑥ 全量 `XLANG_BSTRICT_SKIP_BUILD=1 run-all-bstrict.sh` |
| 探针 | `/tmp/w498_multi_combo_probe.x`：`Box<i32>.get()=42` + `Box<bool>.get()=true` → exit 0 ✓（冷编译验证 wave498 功能）· `/tmp/w499_full_chain.x`：`Wrap<i32>.get()=100` + `Wrap<bool>.get()=false` → exit 0 ✓（冷编译验证完整链路） |
| bstrict | `run-all-bstrict OK (129 scripts)` exit 0（耗时 11 分 33 秒）✓ |
| 判据自查 | remaining_o=0 ✓ · 二进制本波 mtime ✓ · 探针全绿 ✓ · bstrict exit 0 + 129 scripts ✓ · 含本 SHA `d812aeb41` ✓ |
| 非回归证据 | wave491-498 改 typeck + codegen + parser_asm 产品面 mac L4 无回归；`simd-s4 FAIL: at_builtin_smoke.x` 是 std-simd-shuffle-select-gate 内部 non-fatal warning（gate 整体 OK，同 wave496 mac L4 + 钉盘 `c3a81bfd6` 模式） |
| tip L4 | 升 **`d812aeb41`**（mac）· 前基线 `c3a81bfd6`（wave495） |
| 钉盘 | 仍 **`c3a81bfd6`**（SHARED 改动需 Ubuntu 金标双端验证后可升钉） |
| 关键教训 | ① **wave491-498 是完整的泛型 inherent impl 方法链路**：从 Self 返回类型（wave491）→ default 方法（wave492/493）→ typeck 推断（wave494）→ codegen unique combo 单态化（wave495）→ codegen multi-combo 单态化 + 调用端 mangle（wave498），覆盖产品路径上所有 gap ② **整波 L4 节奏**：8 波功能 + 2 波 L4（mac + Ubuntu），符合「日常微步 L2 + 整波收口 L4」 ③ **mac L4 不能直接升钉**：SHARED 改动必须 Ubuntu 金标双端验证 ④ **过时注释及时清理**：wave498 解决了 multi-combo soft gap 后，codegen.x + seed 中的「still soft」注释需同 commit 更新，避免后续误导 |

## 27 日 wave498（已闭 · 双端 L2 · 多 combo 泛型 inherent impl 方法 codegen 单态化）

| 项 | 状态 |
|----|------|
| 债 | 多 combo 泛型 inherent impl 方法 codegen 单态化：`impl Box<T> { function get(self: Box<T>): T }` 同时实例化 `Box<i32>` + `Box<bool>` → 只发射一个泛型定义（ret=`struct T` incomplete BLD001）+ 符号冲突（wave490 mangle 已修，但定义侧未做单态化，wave495 仅 unique combo） |
| 根因 | ① hoisted impl methods（`impl Box<T> { function get(...) }`）`num_generic_params == 0`（`<T>` 在 impl 不在 fn），bypass `codegen_try_emit_generic_identity_mono` ② wave495 仅处理 unique combo（nc==1），多 combo 仍发裸定义 ③ 调用端 mangle 只处理函数级泛型，不处理 generic inherent impl 方法 → 调用点裸名 vs 定义端 mangled 名冲突 |
| 根修 | ① 新增 `codegen_try_emit_generic_impl_method_mono`：遍历函数参数，找第一个带 free type-arg 的命名结构体 param → 收集所有 mono combo → 对每个 combo 激活 `mono_active` 上下文 → 发射 mangled 函数定义（签名+体） ② 新增 `codegen_try_emit_generic_impl_method_extern_mono`：extern 声明版本，同上逻辑 ③ `emit_func` 函数发射循环在函数级泛型检查后调用 impl 级 mono ④ `emit_func_extern_declaration` 同理调用 extern 版本 ⑤ 新增 `codegen_try_emit_impl_method_mono_call_name`：调用端 mangle 辅助，从 receiver 类型提取具体类型参数 → 生成 mangled 调用名 ⑥ 修改所有调用端路径（UFCS / dep module resolved / C6 re-resolution / `codegen_emit_call_func_name` re-search / PTR overload fallback）调用此辅助 ⑦ codegen.x + seed `codegen_gen.linux.x86_64.c` 同 commit 同语义同步 |
| 权威 | G.7 单源：`codegen_try_emit_generic_impl_method_mono` + `codegen_try_emit_generic_impl_method_extern_mono` + `codegen_try_emit_impl_method_mono_call_name`（codegen.x + seed 同步） |
| 探针 | `/tmp/w498_multi_combo_probe.x`：`Box<i32>.get()=42` + `Box<bool>.get()=true` → exit 0 ✓（双端 L2） |
| 矩阵 | mac L2 bstrict 129 OK ✓ · Ubuntu L2 bstrict 129 OK ✓（耗时 2分39秒） |
| 钉盘 | 仍 **`c3a81bfd6`**（日常 L2 非 tip L4） |
| soft | dyn Trait（无 KW/vtable）；P010 untyped-let（语言契约）；`fn`/`var`/untyped-`self` 非产品债 |
| seed 一致性 | codegen.x + seeds/codegen_gen.linux.x86_64.c 同 commit 同语义 |
| 关键教训 | ① **泛型有两层：函数级 vs impl 级**：`num_generic_params > 0` 只覆盖函数级泛型，impl 级泛型（`impl Wrap<T>`）需要单独路径 ② **调用端与定义端必须对称**：定义端 mangled 了，调用端所有路径（UFCS / dep / C6 / re-search）都要 mangled，否则 undeclared function ③ **static 函数前向声明陷阱**：seed C 中 static 函数定义在调用点之后会报 implicit declaration，需改为 extern 或移到前面 ④ **Ubuntu 同步需检查 codegen_gen.c 实际内容**：`xlang-build.sh build` 可能不触发 codegen_gen.c 重新同步（需 `rm -f codegen_gen.c && make codegen_gen.c` 强制） |

## 27 日 wave497（已闭 · Ubuntu L4 双端验收 wave491-495 整体功能波）

| 项 | 状态 |
|----|------|
| 触发 | wave496 mac L4 已绿 `c3a81bfd6`，按 AGENTS.md「SHARED 改动双端验证」+ 钉盘升级条件（Ubuntu 金标 + mac 双端 L4 全绿）触发 Ubuntu L4 验收；wave491-495 改 typeck + codegen + parser_asm 产品面 SHARED 改动，需 Ubuntu 金标验证无回归 |
| 流程 | ① git push `self-hosting` 到 origin → ② SSH ubuntu-server `git pull` → ③ 全擦 `compiler|std|core` 下全部 `.o` + 产品二进制（`xlang`/`xlang_asm`/`xlang-c`/`bootstrap_xlangc`）→ `remaining_o=0` ④ `make -C compiler build-tool` ⑤ `make -C compiler bootstrap-driver-seed` 冷启动 → ⑥ `./xlang-build.sh build` (g05 relink) → ⑦ 产品矩阵 ⑧ 全量 `XLANG_BSTRICT_SKIP_BUILD=1 run-all-bstrict.sh` |
| 矩阵 | `rv_build=0 rv_run=42` / `opt_build=0 opt_run=102` / `si_build=0 si_run=0` / `hello_build=0 hello_run=0`（hello.out 含 `0a` 真换行）全绿 ✓ |
| bstrict | `run-all-bstrict OK (129 scripts)` exit 0（耗时 3 分 26 秒）✓ |
| 判据自查 | remaining_o=0 ✓ · 二进制本波构建 ✓ · 矩阵全绿 ✓ · bstrict exit 0 + 129 scripts ✓ · 与 mac L4 结果一致（simd-s4 non-fatal 相同模式）✓ |
| 非回归证据 | wave491-495 改 typeck（pipeline_glue + strict_minimal）+ codegen（codegen.x + seed）+ parser_asm 产品面双端无回归；`simd-s4 FAIL: at_builtin_smoke.x` 是 std-simd-shuffle-select-gate 内部 non-fatal warning（gate 整体 OK，同 mac L4 + 钉盘 `70ca1089b` 模式） |
| tip L4 | 升 **`c3a81bfd6`**（双端）· 前基线 `b99e25de5`（wave490） |
| 钉盘 | 升 **`c3a81bfd6`**（双端验证通过；前钉 `70ca1089b`） |
| 关键教训 | ① **SHARED 改动必须双端验证**：mac `ld -dead_strip` 可能藏 UNDEF，链接完整性真值在 Ubuntu 全链；mac L4 绿只能升 tip L4，不能升钉盘 ② **bstrict FAIL 不等于回归**：需区分 gate 内 non-fatal warning（`simd-s4 FAIL` 但 `std-simd-shuffle-select gate OK`）与真测试失败；双端 simd-s4 模式一致 = 预存非本波问题 ③ **整波 L4 节奏**：wave491-495 五波日常 L2 后做一次 mac L4 + 一次 Ubuntu L4，符合「整体功能收口 = 主闸门」；升钉 = 双端验证，不升钉 = 单端 tip |

## 27 日 wave496（已闭 · mac L4 真冷验收 wave491-495 整体功能波）

| 项 | 状态 |
|----|------|
| 触发 | wave491-495 构成「泛型 inherent impl 方法完整链路」整体功能波（Self 返回字面 + default 方法 → 伪 gap 清理 → typeck 推断 → codegen 定义/extern 单态化），按自举方法.md §0.2「整波结束做一次扎实 L4」触发；wave491-495 改 typeck + codegen + parser_asm 产品面需 L4 验证无回归 |
| 流程 | ① 全擦 `compiler|std|core` 下 207 个 `.o` + 产品二进制（`xlang`/`xlang_asm`/`xlang-c`/`bootstrap_xlangc`）→ `remaining_o=0` 确认 ② pin 产品 seed（`codegen_gen.linux.x86_64.c` / `parser_gen.linux.x86_64.c` / `typeck_gen.linux.x86_64.c`）③ `make bootstrap-driver-seed` 冷启动 → `FULL=0 g05_prepare_and_relink.sh` relink → 二进制 06:54 本波 mtime ④ 产品矩阵 ⑤ 全量 `XLANG_BSTRICT_SKIP_BUILD=1 run-all-bstrict.sh` |
| 矩阵 | `rv_build=0 rv_run=42` / `opt_build=0` / `si_build=0` / `hello_build=0 hello_run=0` 全绿 ✓ |
| bstrict | `run-all-bstrict OK (129 scripts)` exit 0（耗时 11 分 11 秒）✓ |
| 判据自查 | remaining_o=0 ✓ · 二进制本波 mtime ✓ · 矩阵全绿 ✓ · bstrict exit 0 + 129 scripts ✓ · 日志含本 SHA `c3a81bfd6` ✓ |
| 非回归证据 | wave491-495 改 typeck（pipeline_glue + strict_minimal）+ codegen（codegen.x + seed）+ parser_asm 产品面无回归；`simd-s4 FAIL: at_builtin_smoke.x` 是 std-simd-shuffle-select-gate 内部 non-fatal warning（gate 整体 OK，同钉盘 `70ca1089b` 的 simd-perf FAIL 模式） |
| tip L4 | 升 **`c3a81bfd6`**（mac）· 前基线 `b99e25de5`（wave490） |
| 钉盘 | 仍 **`70ca1089b`**（Ubuntu 双端未验；SHARED 改动需 Ubuntu 金标双端验证后可升钉） |
| 关键教训 | ① **整波 L4 节奏**：wave491-495 五波日常 L2 后做一次扎实 L4，符合「整体功能收口 = 主闸门」而非每 commit 全量 L4 ② **bstrict FAIL 不等于回归**：需区分 gate 内 non-fatal warning（`simd-s4 FAIL` 但 `std-simd-shuffle-select gate OK`）与真测试失败；钉盘 `70ca1089b` 也有同类 simd gate non-fatal ③ **mac L4 ≠ 双端放行**：mac `ld -dead_strip` 可能藏 UNDEF，链接完整性真值在 Ubuntu 全链；mac L4 绿只能升 tip L4，不能升钉盘 |

## 27 日 wave495（已闭 · 泛型 inherent impl 方法定义/extern codegen 单态化）

| 项 | 状态 |
|----|------|
| 债 | wave494 typeck 已修 ret=i32，但 codegen 仍以原始 `T` 发射函数定义 + extern 前向声明 → `struct T` incomplete BLD001 + conflicting types（`extern struct T get(...)` vs `int32_t get(...)`） |
| 根因 | ① hoisted impl methods（`impl Wrap<T> { function get(self: Wrap<T>): T }`）`num_generic_params == 0`（`<T>` 在 impl 不在 fn），bypass `codegen_try_emit_generic_identity_mono`，由 `emit_func` + `emit_func_extern_declaration` 发射 ② self 形参 `Wrap<T>` 被 wave489 unique-combo 后缀机制救援，但自由返回类型 `T` 无救援机制 ③ `mono_active` 未设，`emit_type` 的 name-based fallback 不替换 `T` → extern 声明与定义冲突 |
| 根修 | ① 新增 `codegen_build_func_param_mono_map` 辅助（G.7 单一权威）：遍历函数参数，对 TYPE_NAMED + 有 free type-arg 的 param，用 `codegen_collect_generic_struct_mono_combos` 收集 combo，仅 unique combo（nc==1）映射 formal type-arg → concrete combo；dedup by formal_arg type_ref；guards：null/越界返回 0、已 concrete 跳过、nc!=1 跳过、bare name 跳过 ② `emit_func` 在 ret 类型发射前用此 map 设 `mono_active`（save/restore 上下文），两条成功返回路径（std-io early + final）都 restore ③ `emit_func_extern_declaration` 同样设 `mono_active`，成功返回前 restore ④ codegen.x + seed `codegen_gen.linux.x86_64.c` 同 commit 同语义同步（包括 hoist 变量到函数作用域以覆盖两条 restore 路径） |
| 权威 | G.7 单源：`codegen_build_func_param_mono_map`（codegen.x + seed 同步）+ `emit_func` / `emit_func_extern_declaration` 双调用点 |
| 探针 | `/tmp/w493_generic_explicit.x` 返回 42 ✓ · `-E` 验证 `extern int32_t w493_generic_explicit_get(...)` + `int32_t w493_generic_explicit_get(...) {...}` 均 `int32_t`（不再是 `struct T`）✓ |
| 矩阵 | bstrict 129 OK · rv42 / option / hello / stdlib-import · simd-s4 `at_builtin_smoke.x` 预存在 import 'std.simd' 缺失（非本波引入） |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | dyn Trait（无 KW/vtable）；P010 untyped-let（语言契约）；**多 combo** 泛型 impl 方法 codegen 单态化（本波仅 unique combo，多 combo 仍 soft per wave490）；`fn`/`var`/untyped-`self` 非产品债 |
| seed 一致性 | codegen.x + seeds/codegen_gen.linux.x86_64.c 同 commit 同语义（包括变量 hoist 到函数作用域） |
| 关键教训 | ① **extern 前向声明与定义是两条独立 codegen 路径**：wave494 只改 typeck，wave495 必须同时改 `emit_func` + `emit_func_extern_declaration`，否则定义 `int32_t` 与声明 `struct T` 冲突 ② **变量作用域覆盖所有 restore 路径**：seed C 中变量声明在嵌套块内但 restore 在块外会导致 undeclared 错误，必须 hoist 到函数作用域 ③ **复用 wave489/490 combo 收集机制**：`codegen_collect_generic_struct_mono_combos` + `codegen_generic_struct_fill_concrete_args` 已是 G.7 单一权威，新辅助只组装映射不重写收集逻辑 |

## 27 日 wave494（已闭 · 泛型 inherent impl 方法 typeck 推断）

| 项 | 状态 |
|----|------|
| 债 | 泛型 inherent impl 方法返回 T 的 typeck 推断（`w.get()` where w: Wrap<i32>, `impl Wrap<T> { function get(self: Wrap<T>): T }` → typeck `expected i32, found T`） |
| 根因 | ① `w.get()` 被解析为 EXPR_METHOD_CALL (kind 49)，走 `pipeline_typeck_check_expr_method_call_c`（strong 版本在 strict_minimal seed 中），**非** EXPR_CALL+ord_field ② 非泛型 UFCS 路径用 `pipeline_typeck_type_refs_equal_c`（指针相等）匹配 self 形参，`Wrap<i32>` ≠ `Wrap<T>` 失败 ③ **但** weak integer match (score 100) 按 TYPE_NAMED kind 匹配成功，stamp 原始返回类型 `T` 并 `return 0`，在 generic fixup 之前退出 ④ 初次 wave494 代码放在 `glue_generic_call_fixup_resolved_type_c`（EXPR_CALL 路径），EXPR_METHOD_CALL 永远不执行 |
| 根修 | ① 导出 3 个 pattern-unify 辅助（`glue_typeck_type_tree_has_free_param_c` / `glue_typeck_pattern_unify_bind_c` / `glue_typeck_subst_type_ref_c`，去 static → 可跨 TU 调用）② 新建 `pipeline_typeck_method_call_generic_ufcs_c` 共享函数（G.7 单一权威）：扫描同名 free fn，检查 self 形参有 free type-param → pattern-unify self 形参与 receiver 具体类型 → 建 free-name→concrete map → subst 返回类型 → stamp resolved_type ③ **关键**：放在非泛型 UFCS **之前**调用（否则 weak integer match 先 stamp 错误 ret 并 return 0）④ weak (pipeline_glue.c) + strong (pipeline_glue_strict_minimal.from_x.c) + .x (pipeline_glue_strict_minimal.x) 三处同 commit 同语义同步 |
| 权威 | G.7 单源：`pipeline_typeck_method_call_generic_ufcs_c`（pipeline_glue.c）+ 3 个导出的 pattern-unify 辅助 |
| 探针 | typeck 正确推断 ret=i32 ✓（`return w.get()` 不再报 `found T`）· codegen 仍以原始 T 发射函数定义（`struct T` incomplete）= codegen 单态化 gap（非产品阻塞）· 已有探针（self: Point / trait chain / Wrap<T>+i32 ret）全绿 |
| 矩阵 | bstrict 129 OK · rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | dyn Trait（无 KW/vtable）；P010 untyped-let（语言契约）；泛型 inherent impl 方法定义 codegen 单态化（typeck 已修，codegen 仍以原始 T 发射）；`fn`/`var`/untyped-`self` 非产品债 |
| seed 一致性 | pipeline_glue.c + pipeline_glue_strict_minimal.from_x.c + pipeline_glue_strict_minimal.x 同 commit 同语义 |
| 关键教训 | ① **确认表达式 kind 再选路径**：`w.get()` 是 EXPR_METHOD_CALL (49) 而非 EXPR_CALL+ord_field；调试 fprintf 在错误路径不打印是路径错误的信号 ② **weak integer match 先于 generic fixup 的陷阱**：score 100 的 kind-match 会先 stamp 错误 ret 并 return，必须在非泛型 UFCS **之前**调用 generic 路径 ③ **G.7 导出辅助而非复制**：strict_minimal seed 通过 extern 调用 pipeline_glue.c 的导出函数，避免重复实现 pattern-unify 逻辑 |

## 27 日 wave493（已闭 · 伪 gap 清理 + 文档审计）

| 项 | 状态 |
|----|------|
| 债 | inherent impl self 类型解析（`impl Point { function get_x(self): i32 {...} }` untyped self → `self.x` resolves to `?`） |
| 根修 | **伪 gap 清理波（无根修）**：① 经上帝视角分析发现 `parser_asm_seed_parse_into_buf_slice.inc` 被 `PARSER_ASM_THIN_GLUE_NO_SEED_PARSE` 宏排除出 xlang 二进制构建；xlang 实际使用 `parser_asm_skip_tl_slice.inc` 的 `parser_asm_skip_one_impl_into_slice_c` 记录 impl 头并定位到 `{` 之后，方法由外层 parse loop 作普通 free function 提交 ② untyped `self`（无类型标注）**非标准 X 语法**；标准面只认 `self: Type`（显式类型标注）③ 探针验证：`self: Point`（非泛型 inherent impl）/ `self: Config`（trait 链式）/ `self: Wrap<T>` + i32 返回（泛型 inherent）**全绿** ④ 唯一真实 gap：泛型 inherent impl 返回 `T`（`impl Wrap<T> { function get(self: Wrap<T>): T }` → typeck `found T`）—— 但产品代码无此用法，非自举阻塞 ⑤ 已回滚 seed 修改 + 删除 7 个伪探针 |
| 权威 | G.7 单源：`parser_asm_skip_tl_slice.inc`（impl 记录 + for-type 改写）+ `parser_asm_one_function_buf_slice.inc`（default 方法 hoist）|
| 探针 | `self: Point` inherent=42 · `self: Config` trait chain `set_a().set_b().build()`=42 · `self: Wrap<T>` + i32 ret=42 · untyped `self` 仍红（非标准语法，预期）· `self: Wrap<T>` + T ret 仍红（真实 gap，非产品阻塞）|
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4）|
| soft | dyn Trait（无 KW/vtable）；P010 untyped-let（语言契约）；泛型 inherent impl 返回 T（typeck 推断 gap，非产品阻塞）；`fn`/`var`/untyped-`self` 非产品债 |
| seed 一致性 | 无变更（伪 gap 清理波）|
| 关键教训 | ① **动手前先确认构建路径**：seed slice 是否被宏排除（`PARSER_ASM_THIN_GLUE_NO_SEED_PARSE`）② **勿用非标准语法造伪 gap**：`self`（无类型）非标准，正确写法 `self: Type` ③ **上帝视角纪律**：先查产生/存储/消费链再动手，避免在错误文件打补丁 |

## 26 日 wave492（已闭 · L2 探针）

| 项 | 状态 |
|----|------|
| 债 | 多 empty impl Self + default 方法体返回类型验证：① 多 impl + default i32 返回类型（`Counter::value` → `return 42`）② 多 impl + default `Self` 返回 + struct-lit（`get(self): Self` → `return Dog {…}`）③ string 字面返回类型 |
| 根修 | **探针验证波（无根修）**：① 多 impl + default i32 返回 **已绿**（wave490/491 结构类型匹配 + 方法名查找间接解决）② 多 impl + `Self` 返回 + struct-lit **已绿** ③ string 字面返回 typeck `expected string, found *u8` → **独立语言特性**（需语言级 `String` 类型 + 字面→`String` 转换），非 multi-impl default 债 |
| 权威 | 探针 `tests/trait/multi_empty_impl_self.x`（多 impl + default i32 + `Self` 返回） |
| 探针 | multi_empty_impl_self: trait 套件 OK · Dog/Cat 各 impl default `value()=42` 绿 · `get(self): Self` + struct-lit 绿 |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | dyn Trait（无 KW/vtable）；P010 untyped-let（语言契约）；string 返回类型需语言级支持（独立特性）；`fn`/`var` 非产品债 |
| seed 一致性 | 无变更（探针波） |

## 26 日 wave491（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | ① `Self` 返回字面 method_call 链式调用 segfault 假红（实际 exit=11 = 10+1 正确结果）② trait default 方法 `Self` 返回 + method_call 假红 missing method ③ `struct` 返回值嵌套 method_call 栈损坏 |
| 根修 | wave490 multi-combo generic impl mangle 修复间接解决：结构类型匹配 + 方法名查找使 `Self` 返回类型正确解析；清理 parser_asm seed 中 wave491 调试遗留 DEBUG fprintf（35 处），消除 stdout 污染 |
| 权威 | `codegen.x` + seed `codegen_gen.linux.x86_64.c`（wave490 延续）+ `parser_asm_skip_tl_slice.inc` + `parser_asm_one_function_buf_slice.inc`（DEBUG 清理） |
| 探针 | self_return_call=11 · self_default_call=11 · self_default_simple=42 · default_minimal=42 · method_struct_return=11 · chained_call=11 · simple_return=42 · method_i32=42 · 全量 trait/std/generic 绿 |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | dyn Trait（无 KW/vtable）；P010 untyped-let（语言契约）；`fn`/`var` 非产品债 |
| seed 一致性 | wave490 `codegen_combo_slots_equal` 替代 `pipeline_typeck_type_refs_equal_c` · 与 .x `codegen_type_refs_same_for_mono` 对齐 |

## 26 日 wave490（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | multi-combo generic impl 方法 mangle：`impl Trait for Box<A>` + `impl Trait for Box<B>` → 方法符号冲突 BLD001（wave489 唯一 combo 已绿） |
| 根修 | ① `codegen_maybe_emit_generic_struct_mono_suffix_for_type`：遍历所有 combo，匹配当前 type_ref 的具体类型参数，emit 对应后缀 ② `codegen_find_impl_method_for_type`：对 TYPE_NAMED 类型增加 `codegen_type_refs_same_for_mono` 结构检查 ③ seed 同步用 `codegen_combo_slots_equal` 做结构类型比对（与 .x `codegen_type_refs_same_for_mono` 等价），替代 `pipeline_typeck_type_refs_equal_c` 的指针级相等 |
| 权威 | `codegen.x` + seed `codegen_gen.linux.x86_64.c` 同 commit |
| 探针 | generic/trait/std 全绿 · bstrict 129 OK |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | dyn Trait；P010 untyped-let；`Self` 返回字面 |
| seed 一致性 | `codegen_combo_slots_equal` 替代 `pipeline_typeck_type_refs_equal_c` · 与 .x `codegen_type_refs_same_for_mono` 对齐 |

## 26 日 wave489（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | `impl Trait for Box&lt;T&gt;` 方法形参仍发裸 `struct Box` → host-C incomplete / 与 `Box__i32` 调用点冲突 BLD001（concrete `impl for Box&lt;i32&gt;` 已绿） |
| 根修 | `codegen_maybe_emit_generic_struct_mono_suffix_for_type`：free T 且无 mono_active 时，**唯一** host-concrete mono combo → 用该后缀（复用 collect）；多 combo 仍 soft |
| 权威 | `codegen.x` + seed `codegen_gen.linux.x86_64.c` 同 commit |
| 探针 | box_gen `impl for Box&lt;T&gt;` get=42 · self.v=42 · concrete Box&lt;i32&gt; 回归 · multi A+i32 仍红 soft · unwrap/pair/trait/矩阵 |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | dyn Trait；P010 untyped-let；`Self` 返回字面；multi-combo generic impl 方法 mangle |

## 26 日 wave488（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | mono 标签 `Wrap__A` / `Pair__S_i32` 在非泛型字段类型 `A`/`S` 之前发射 → host-C incomplete type BLD001（`wrap(A{…}).v.n` / typed `let w: Wrap&lt;A&gt;` 同红） |
| 根修 | `codegen_emit_module_struct_definitions` **两阶段**：phase0 非泛型 layout；phase1 全局收集 mono combo → 按 type-arg nest depth 排序（跨 layout：`Pair__S_i32` 先于 `Wrap__Pair…`）再 emit |
| 权威 | `codegen.x` + seed `codegen_gen.linux.x86_64.c` 同 commit |
| 探针 | unwrap_field/typed/wrap_arg=42 · pair_a=42 · wrap_pair_mod=42 · chain_ambient=42 · nest_ww/bare_unwrap 回归 · 矩阵 |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | dyn Trait；P010 untyped-let（语言契约）；generic `impl Box&lt;T&gt;` 方法（**wave489 唯一 combo 闭**）；`Self` 返回 |

## 26 日 wave487（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | formal `Wrap&lt;T&gt;` / `Pair&lt;A,B&gt;` pattern-unify：`unwrap&lt;T&gt;(w: Wrap&lt;T&gt;): T` bare CALL → typeck `found T` |
| 根修 | `glue_generic_call_fixup`：值形参 identity + **递归 pattern-unify**（模块 formal 的 type-arg 树 vs 实参 type-arg）建 free 名→concrete map；stamp free ret 或 module ret mono |
| 权威 | `compiler/pipeline_glue.c`（standalone 覆盖；G.7 单源） |
| 探针 | unwrap=42 · nested unwrap_inner=42 · first/second=42 · id/nest/make_pair 回归 · 矩阵 |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | dyn Trait；P010 untyped-let（语言契约）；turbofish+`Wrap&lt;T&gt;` formal 宿主 emit（bare 已绿）；ambient 无 typed let |

## 26 日 wave486（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | 链式 `nest(…).inner.inner.v` typeck：`expected i32, found ?`（typed local 绿） |
| 根修 | `glue_generic_call_fixup`：模块返回类型树（`Wrap&lt;Wrap&lt;T&gt;&gt;`）按值形参 free 名→concrete 递归 subst 后 stamp CALL；field mono 可走 Wrap→Wrap→A→i32 |
| 权威 | `compiler/pipeline_glue.c`（standalone 覆盖；G.7 单源） |
| 探针 | chain=42 · one_inner=42 · typed=42 · nest_pair chain=42 · make_pair chain=41 · trait · 矩阵 |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | dyn Trait；P010 untyped-let；ambient STRUCT_LIT typeck；formal `Wrap&lt;T&gt;` pattern-unify |

## 26 日 wave485（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | 泛型函数返回嵌套 STRUCT_LIT：`nest&lt;T&gt;(x:T):Wrap&lt;Wrap&lt;T&gt;&gt;` → mono 体浅 `T→A` 发 `Wrap__A` + incomplete `Wrap__Wrap_T` BLD001 |
| 根修 | ① `host_concrete` 泛型 layout 要求 type-arg 递归完整 ② STRUCT_LIT 字段 mono 带 mono ctx；叶 free T / 无 stamp INT lit；layout 类型参 mono 回落 |
| 权威 | `codegen.x` + seed `codegen_gen.linux.x86_64.c` 同 commit |
| 探针 | nest=42 · nest_pair=43 · local/triple/wm/pair 回归 · trait · 矩阵 |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | dyn Trait；P010 untyped-let；链式 `nest(…).inner` typeck（**wave486 闭**）；ambient STRUCT_LIT typeck |

## 26 日 wave484（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | 嵌套 generic struct mono：`Wrap&lt;Wrap&lt;A&gt;&gt;` / 三重嵌套 → suffix 仅名层碰撞 + 定义序 incomplete + STRUCT_LIT 继承外层 ambient 类型 BLD001 |
| 根修 | ① `type_ref_to_suffix` 递归 type-pos 后缀（`Wrap_Wrap_A`）② combo 结构相等 + 浅→深排序发射 ③ STRUCT_LIT mono 按字段结构推导（不信 ambient resolved） |
| 权威 | `codegen.x` + seed `codegen_gen.linux.x86_64.c` 同 commit |
| 探针 | ww=42 · triple=42 · Wrap&lt;Pair&gt;=42 · ww_ab=42 · pair/wrap multi · wave483 无空格 nest |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | dyn Trait；P010；nest 返回体 mono（**wave485 闭**） |

## 26 日 wave483（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | 嵌套 type-pos `Name<Name<T>>` / turbofish `foo<Bar<A>>(` → 相邻 `>>` 被 lexer max-munch 成 TOKEN_RSHIFT → 角列表闭合失败 XP003 |
| 根修 | type_ref 角列表与 primary turbofish：见 RSHIFT 时关一层并回退 1 字节留外层 `>`（C++/Rust 惯例） |
| 权威 | `parser_asm_type_ref_slice.inc` + `parser_asm_primary_slice.inc`（产品 thin glue） |
| 探针 | nest Wrap&lt;Wrap&lt;A&gt;&gt;=42 · tf nest=42 · shift a&gt;&gt;b=63 · pair ret=42 · multi Wrap · cmp · trait |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | dyn Trait；P010 untyped-let；嵌套 mono（**wave484 闭**） |

## 26 日 wave482（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | bare multi mono：`return make_pair(A,B)` 有 ambient 返回位 / 链式 `make_pair(…).a` → incomplete `Pair`/`ast_T` 或签名 `Pair` vs 体 `Pair__A_B` BLD001 |
| 根修 | fill 经 mono map 解析自由 T/U；collect 从 generic function mono 收割 combo；maybe_emit layout 参名 mono 回落 |
| 权威 | `codegen.x` + seed `codegen_gen.linux.x86_64.c` 同 commit |
| 探针 | chain=41 · ret_bare=42 · pair_multi=84 · wrap_multi=42 · typed/ambient 回归 · dual POST · run-trait |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | dyn Trait；P010 untyped-let（语言契约）；零参仅 phantom 无 ambient 仍 requires_type_args |

## 26 日 wave481（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | 同模块 `Wrap<A>+Wrap<B>` / `Pair<A,B>+Pair<i32,i32>` 共用单 C 标签 → BLD001 |
| 根修 | 按 concrete type-arg combo 发射 mangled 定义与使用点（`Name__suf`）；mono 体内 STRUCT_LIT 经 mono map 补后缀 |
| 权威 | `codegen.x` + seed `codegen_gen.linux.x86_64.c` 同 commit |
| 探针 | wrap_one/multi=42 · pair_multi=84 · pair/wrap ret=42 · i32 typed=42 · dual POST · Method.POST · run-trait |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | dyn Trait；bare 无 ambient（**wave482 闭**）；P010 untyped-let（语言契约） |

## 验收

| 项 | 状态 |
|----|------|
| 钉盘 L4 双端 129 | ✅ **`70ca1089b`** · `/tmp/{mac,ubuntu}_true_{cold,bstrict}_70ca1089b.log` |
| **tip L4 双端 129（wave472 收口）** | ✅ **`b99e25de5`** · 产品 **`f99fafda8`** · mac `/tmp/mac_true_bstrict_b99e25de5.log` · Ubuntu `/tmp/ubuntu_true_bstrict_b99e25de5.log` · cold 基线 `/tmp/{mac,ubuntu}_true_cold_000d9a5ef.log` |
| 产品 tip L4 | ✅ 矩阵 rv42/option/hello + **129**；**未**自动升钉（钉盘仍 `70ca1089b`） |

## Trae 25 日接续 · 审计结论（日切前已闭合）

| 维度 | 结论 |
|------|------|
| 产品代码 wave432–445 | ✅ **真实有效**（trait 签名簇 / bound / mono C1–C6 等，非空 docs） |
| 钉盘 `70ca1089b` 双端 L4 129 | ✅ 日志与文档一致 |
| 文档问题 | ⚠️ 曾误诊 lexer typeck=`&&`；写「排除整棵 trait」与已合入能力矛盾；tip/钉盘口吻混乱 → **wave446 已校正** |
| 结论 | **可以进入下一步**；**不是**「Trae 全程零问题」，而是 **代码可接、文档已修、阻塞根修已闭** |

## 26 日承接（wave446 · 日常 L2 · 已闭 · 已入 25 归档）

| 项 | 状态 |
|----|------|
| SHA | **`8a8aa1841`**（parser）+ **`54293a3aa`**（lexer.x）+ docs **`e1e49fe88`** |
| 债 | Trae 阻塞：lexer.x / 全库 *if condition must be bool*（误诊为 typeck `&&` 链） |
| 根因 | `skip_generic_angle_list` 跨语句贪吞：`pos < n` … 后 `pos > (` 假 generic CALL |
| 根修 | 角列表语句边界中止 + lexer 结构体 lit `;` / docblock 去字面 `/*` |
| 构建级别 | **L2**（parser_asm_thin_glue + bootstrap-driver-seed） |
| 钉盘 | 仍 **`70ca1089b`** |
| 探针 | mac：`pos < n`+`pos > (0 as usize)` 绿 · nest skip_ws 绿 · `xlang -c lexer.x` typeck 绿 |
| soft | dyn；XP003；Darwin residual；wave445 mono ABI tip L4 待收口 |

## 26 日 wave447（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | non-identity generic mono 不 emit → BLD001；arg0 mono 类型用 call 返回类型；i32→i32 self-map `emit_type` 递归 SEGV |
| 根修 | 放宽 identity 门控 + 签名用 ret/param+mono_active；mono 类型优先实参；`conc != type_ref` 防自递归 |
| 权威 | `codegen.x` + seed `codegen_gen.linux.x86_64.c` 同 commit |
| 探针 | getv 字段 / absdiff if / clone / identity / multi_mono / i32-param =42 |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

### 为何文档里有 `fn`/`Self`/`var`？

| 写法 | 说明 |
|------|------|
| **不是**产品矩阵债 | 标准面只认 `function` / `let` / `self: Type` |
| 来源 | wave445 探针曾用 Rust 风格 `fn`/`-> Self`/无类型 `self` 测 mono → 失败被记 soft |
| 正确探针 | `function` + 显式 `self: A` → 已绿；**勿**再用错误语法制造伪 gap |
| `var` | 非关键字；写 `var x` 整函数解析失败，用 `let` |

## 26 日 wave448（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | 泛型调用无 turbofish（`id(a)`）→ typeck `requires_type_args` → XT001/XP003；codegen mono 已能从实参取类型 |
| 根修 | `pipeline_typeck_try_infer_generic_call_from_args_c`：省略 type args 时从 value 实参推断；同名 TYPE_NAMED 形参统一；失败仍 `requires_type_args` |
| 权威 | `compiler/pipeline_glue.c`（standalone TU 入链）；重建 `pipeline_glue_standalone.o` + g05 |
| 探针 | `id(a)` / `getv(a)` / `absdiff(10,52)` / `copy_c(a)` / `copy_both(a,99)` / 同名统一 mismatch 红 / turbofish 回归 =42 |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | 零参泛型仅靠返回位推断；dyn；Darwin；wave445 ABI tip L4 |

## 26 日 wave449（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | wave448 后 `copy(b)` 无 turbofish 且 B 未 impl bound → **假绿** run=99；parse-time 源扫只捕 `foo<Type>(…)` |
| 根修 | 抽 `xlang_generic_bound_check_type_args_c`（G.7 单权威）；typeck 推断成功后 `pipeline_typeck_check_inferred_generic_bounds_c` 按形参 TYPE_NAMED 首现序取具体类型名再检 bound |
| 权威 | `parser_asm_skip_tl_slice.inc` + `pipeline_glue.c`；重建 thin glue + standalone + g05 |
| 探针 | no-tf viol T001 红 · no-tf ok=42 · multi no-tf Default 违红 · multi ok=42 · turbofish viol 仍红 · id(a)=42 |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | 零参泛型；推断路径 diag 行号可弱；dyn；formals 非声明序稀有形；Darwin；wave445 ABI tip L4 |

## 26 日 wave450（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | 零参泛型 turbofish `unit_t<Marker>()` typeck 绿但 **BLD001** undeclared；mono 硬门控 `num_params<=0` 不 emit，调用点 C3 回落裸名 |
| 根修 | `codegen_try_emit_generic_identity_mono` 放行 `num_params==0`：有匹配 CALL 时按 **裸 link_name** 单次 emit body（与调用点 bare 契约一致；parser 仅存 type_arg 个数） |
| 权威 | `codegen.x` + seed `codegen_gen.linux.x86_64.c` 同 commit |
| 探针 | za_tf run=7 · multi A/B=22 · bare `unit_t()` 仍 requires_type_args 红 · zero-arg bound ok=5 / viol T001 · id/getv=42 · one-arg 回归=42 |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | bare 零参仅靠返回位推断；零参 ret/body 需 T 替换且无 type_arg type_ref；dyn；Darwin；wave445 ABI tip L4 |

## 26 日 wave451（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | 泛型 CALL 返回位仅 identity（ret 名==param0）→ `second<T,U>(a,b):U` 等报 *found U* / *found T* 假红 |
| 根修 | `glue_generic_call_fixup_resolved_type_c` 扫**全部**形参：ret TYPE_NAMED 名==formal[i] 名 → 用 arg[i] resolved 戳记 CALL |
| 权威 | `compiler/pipeline_glue.c`（G.7 单源 fixup）；重建 `pipeline_glue_standalone.o` + g05 |
| 探针 | second=42 · first_of=17 · tf second=33 · id=42 · getv=42 · za_tf=14 · bound ok=42 / viol T001 · multi-file-generic OK |
| soft 仍红 | `mk_default<T>():T` 零参 / `as_t<T>(i32):T` ret 未出现在形参（需 type_arg type_ref 或 expected-type） |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

## 26 日 wave452（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | ret `T` 不在值形参（`as_t<T>(i32):T` / `mk_default<T>():T`）→ *found T* 假红；parser 仅存 type_arg 个数 |
| 根修 | ① ast_pool 侧车存 CALL turbofish type_arg type_ref；② primary 解析并 append；③ glue fixup 按 type_arg 戳记 CALL 返回位；④ codegen mono 对 ret T→concrete（含零参 bare） |
| 权威 | G.7：ast_pool + primary_slice + pipeline_glue fixup + codegen.x/seed 同 commit |
| 探针 | as_t=42 · mk_default=7 · second=42 · first_of=17 · tf second=33 · id/getv=42 · za_tf=14 · bound ok=42 / viol **T001 红**（预期） · multi-file-generic · rv42 · hello · stdlib-import |
| soft 曾红 | bare 零参仅靠返回位推断（无 turbofish）→ **wave453 收**；dyn Trait；多 type_arg 稀有形；Darwin；wave445 ABI tip L4 |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

## 26 日 wave453（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | bare 零参 / ret 仅类型参无 turbofish（`let a: A = mk_default()` / `as_t(42)`）→ `requires_type_args` 或 *found T*；wave448 只从值实参推断 |
| 根修 | ① 值实参路径失败时：`n_gp==1` 且 ret 为类型参 + ambient expected → 放行；② fixup `n_ta==0` 时用 expected 戳记 CALL；③ bound 检查 ret 仅类型参时从 expected 填 slot0；④ phantom-only `unit_t<T>():i32` 仍 fail-closed |
| 权威 | G.7 单源 `pipeline_glue.c`（standalone 入链）；重建 `pipeline_glue_standalone.o` + bootstrap-driver-seed |
| 探针 | bare mk=7 · bare as_t=42 · bare bound ok=5 / viol **T001 红** · as_t bound viol **T001 红** · unit_t bare 仍 requires_type_args · tf mk/as 回归 · second/first/id/za · multi-file-generic · rv42 · hello |
| soft 曾红 | 链式 `mk_default().v` → **wave454 收**；dyn Trait；多 type_arg；Darwin；wave445 ABI tip L4 |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

## 26 日 wave454（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | 链式 `return mk_default().v`：field_access 把外层 `i32` 传给 CALL → wave453 误把 T 钉成 i32 → `found ?` / 或 requires_type_args；`return mk_default()` 假绿 typeck 后 BLD001 |
| 根修 | ① field_access **禁止**把 field-result ambient 传给 base；② CALL base 从字段名在 module layouts 中 **唯一**反推 owner TYPE_NAMED 作 expected；③ expected-ret 推断 / fixup 仅接受 **module TYPE_NAMED**（拒 i32） |
| 权威 | G.7：heavy `pipeline_typeck_field_access.c` + **Darwin 产品 live 路径** `pipeline_glue_strict_minimal`（filtered 把 heavy 降为 local，weak 为 typeck 实链）· seed C 与 `.x` 同语义 |
| 探针 | chain=7 · chain bound ok=5 / viol **T001 红** · bare mk=7 · as_t=42 · ret bare **requires_type_args 红** · ambig 多 struct 同名 field **红** · id/second/za_tf · multi=42 · rv42/option/hello/si |
| soft 曾红 | multi type_arg ret-only turbofish → **wave455 收**（bare multi / mk2u ret-U mono 仍 soft）；dyn Trait；Darwin；wave445 ABI tip L4 |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

## 26 日 wave455（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | multi type_arg ret-only turbofish（`as2<T,U>(i32):T` / `mk2<T,U>():T`）→ *found T*；`gnames_n != n_gp` 门控 + 无声明序参名 |
| 根修 | ① angle-list count 捕 type-param 名 → parse 时 `register_pending` 入 g_fn_gp_*；② fixup 按声明序 index 取 type_arg；③ mono `call_ret_concrete` 优先 resolved_type_ref；④ trait_reg_reset 清 registry；scan 无源不 wipe |
| 权威 | G.7：skip_tl registry + lex_skip pending + parser 登记 + pipeline_glue fixup + codegen mono |
| 探针 | as2/mk2 tf=7 · wrap_u bare+tf=42 · as_t/id2/chain · multi_ret_tf=7 · rv42/option/hello/si |
| soft 曾红 | 零参 ret-U mono → **wave456 收**；bare multi / dyn Trait 仍 soft |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

## 26 日 wave456（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | 零参 multi type_arg ret 非 slot0（`mk2u<T,U>():U` + turbofish）typeck 绿 → host C 签名 `A`、体 `B` → BLD001 |
| 根修 | ① zero-param mono 扫 CALL：**优先 `call_resolved_func_index==fi`**（typeck 真 site）；name-only `rfi=-1` 幽灵不得先吃 type_arg；② `concrete_at`：resolved 优先；sole type_arg 才可 slot0；**multi 无 stamp 返 0** |
| 权威 | G.7：codegen.x + `seeds/codegen_gen.linux.x86_64.c` 同 commit（mono 扫描 + concrete_at） |
| 探针 | mk2u=9 · as2u=9 · mk2/as2=7 · mk_default/chain=7 · id2/wrap=42 · rv42/option/hello/si |
| soft 曾红 | bare multi → **wave457 收**；dyn Trait 仍 soft |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

## 26 日 wave457（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | bare multi ret-only 无 turbofish（`mk2<T,U>():T` / `mk2u<T,U>():U` + `let x: A = mk2()`）→ requires_type_args |
| 根修 | expected-return 推断放宽 `n_gp≥1`（原 wave453 仅 `n_gp==1`）；fixup/mono 已能 stamp resolved + map ret→concrete |
| 权威 | G.7：`pipeline_glue.c`（`pipeline_typeck_try_infer_generic_call_from_args_c`）→ rebuild `pipeline_glue_standalone.o` + g05 |
| 探针 | bare mk2=7 · bare mk2u=9 · as2/id2/mk_default · tf 回归 · bound ok=5/viol T001 · unit_t/prim 仍 requires · rv=42/option=102/hello/si |
| soft 曾红 | 多 T 裸 link_name → **wave458 收**；dyn Trait 仍 soft |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

## 26 日 wave458（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | 多不同 T 共享 mono 符号（`mk<A>+mk<B>` / `as_t<A>+as_t<B>` 值形参同 i32）+ `T {…}` STRUCT_LIT 未 mono 替换 → incomplete T / BLD001 |
| 根修 | ① mono combo 键 `ret_extra`（ret 类型参不在值形参时追加 call ret concrete）② 零参 ret-T 走 multi-combo mangle ③ STRUCT_LIT 名 mono 替换 ④ combo 去重按 TYPE_NAMED 名等价 |
| 权威 | G.7：`codegen.x` + seed `codegen_gen.linux.x86_64.c` 同 commit |
| 探针 | mk T{}=7 · multi mk tf/bare=14 · as_t T{} multi=16 · id/mkd/unit/mk2u · rv42/option/hello/si |
| soft 仍红 | dyn Trait；`as U` 结构体 cast 宿主 C；Darwin；wave445 ABI tip L4 |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

## 26 日 wave459（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | struct `as` 宿主 C 非法转型（`x as A` / mono `as_t<A>(7)` → `((struct A)(x))` → BLD001） |
| 根修 | EXPR_AS：target 经 alias+mono 后若 module user struct → C99 compound literal `((TYPE){ (op) })`；标量/指针仍 cast |
| 权威 | G.7：`codegen.x` + seed `codegen_gen.linux.x86_64.c` 同 commit；复用 `codegen_mono_subst_type` + `codegen_type_is_module_user_struct` |
| 探针 | simple as=42 · mono as_t multi=16 · STRUCT_LIT 回归=16 · scalar=42 · mk multi=14 · rv42/option/hello/si |
| soft 仍红 | dyn Trait；lit `7 as MultiField` 整式折叠边角；Darwin；wave445 ABI tip L4 |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

## 26 日 wave460（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | lit `7 as MultiField` / `7 as A` CTFE 整式折叠 → host C `struct T m = 7` BLD001（wave459 soft leave-off） |
| 根修 | CTFE 产生点 `glue_typeck_fold_expr_ref` EXPR_AS：目标 TYPE_NAMED/array/slice/vector/linear **不** stamp `const_folded_valid`；操作数仍 fold；标量 AS 仍 stamp。emit 走 wave459 compound literal |
| 权威 | G.7：`pipeline_glue.c` CTFE producer（`pipeline_glue_standalone.o` 入链）；镜像 STRUCT_LIT never-stamp |
| 探针 | lit multi=7 · lit single=7 · var multi=7 · scalar=7 · var single=42 · as_t mono=7 · STRUCT_LIT=16 · lit call-arg=7 · 矩阵 rv42/option/hello/si |
| soft 仍红 | dyn Trait（impl self / vtable 大）；Darwin；wave445 ABI tip L4 |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

## 26 日 wave461（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | struct 操作数 `as Struct`：wave459 一律 compound first-field → `a as A` / `a as B` host C BLD001 |
| 根修 | EXPR_AS：op 经 resolved+alias+mono 亦为 module user struct → identity `(op)`；compound 仅非 struct 操作数（标量→struct 保留） |
| 权威 | G.7：`codegen.x` + seed `codegen_gen.linux.x86_64.c` 同 commit |
| 探针 | same-type as=42 · scalar multi=7 · as_field=17 · lit/var/as_t/mk 回归 · 矩阵 rv42/option/hello/si |
| soft 仍红 | A→B / struct→scalar → **wave462 闭**；dyn Trait（无 KW/vtable）；Darwin；wave445 ABI tip L4 |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

## 26 日 wave462（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | A→B / struct→scalar `as`：wave461 identity 仅同型合法；`struct B b=(a)` 与 `((i32)(a))` host BLD001 |
| 根修 | EXPR_AS：同型 struct 仍 identity；**异型** struct op（A→B 或 struct→scalar）发 GNU stmt-expr type-pun `({ OP_TY __xlang_as_o=(op); *(TGT*)(void*)&__xlang_as_o; })`；标量→struct compound / 标量 cast 不变 |
| 权威 | G.7：`codegen.x` + seed `codegen_gen.linux.x86_64.c` 同 commit；复用 mono + module_user_struct + `pipeline_typeck_type_refs_equal_c` |
| 探针 | A→B=42 · multi A→B=7 · struct→i32=42 · same-type=42 · scalar multi=7 · as_field=17 · as_t mono=7 · 矩阵 rv42/option/hello/si |
| soft 仍红 | dyn Trait（无 KW/vtable）；Darwin；wave445 ABI tip L4 |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

## 26 日 wave463（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | CORE-001 泛型 `size_of<T>`/`align_of<T>`：import 路径 BLD001（裸 `core_types_size_of()`）+ 桩体恒 0 |
| 根修 | CALL 站 intrinsic：`((int32_t)(sizeof(T)))` / `_Alignof`；TYPE_ARRAY 用 wave357 `E[N]` 非 `E*` |
| 权威 | G.7：`codegen_try_emit_size_align_of_call` · codegen.x + seed 同 commit |
| 探针 | free/import size_of=4 · generic_layout=0 · Pair/ptr/u8[4] · 矩阵 rv42/option/hello/si · A→B 回归 |
| soft 曾红 | trait 方法签名无 `;` 假红 → **wave464 收**；dyn Trait；Darwin；wave445 ABI tip L4 |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

## 26 日 wave464（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | trait 方法签名缺 `;` → 注册误吞 trait `}` → 整 trait 软跳 → impl 方法未 hoist → typeck *no impl for type with method*（`function get(self): i32` 无分号假红；加 `;` 才绿） |
| 根修 | seed `register_trait` + skip_tl 孪生：depth==0 的 `}` 不再当方法体结束；`function` 作 ASI 切下一方法；签名无 `;` 合法 |
| 权威 | G.7：`parser_asm_seed_parse_into_buf_slice.inc` + `parser_asm_skip_tl_slice.inc` 同 commit |
| 探针 | ASI no-semi get=42 · multi-method ASI=42 · with-semi=42 · mono clone bound=42 · trait main=42 · missing method 仍红 · 矩阵 rv42/option/hello/si |
| soft 仍红 | dyn Trait（无 KW/vtable）；Darwin residual；wave445 ABI tip L4 |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

## 26 日 wave465（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | 泛型 struct 类型参字段 `struct Wrap<T>{v:T}`：`return w.v` → *found ?*；`let x:i32=w.v` / `w.v+0` 绿（ambient/binop） |
| 根修 | field_access 在 layout 后：若字段类型为无 module 具体类型的 TYPE_NAMED（类型参）且存在 ambient expected → stamp ambient；**不**把 ambient 灌进 base（保 wave454） |
| 权威 | G.7：heavy `pipeline_typeck_field_access.c` + seed `pipeline_glue_strict_minimal.from_x.c` + `.x` 孪生同 commit |
| 探针 | get_v return=42 · if-return=42 · chain mk_default().v=7 · Point 嵌套=42 · enum 字段 Color 仍绿 · plain A.v=42 · 矩阵 rv42/option/hello/si · trait main=42 |
| soft 曾红 | `Wrap<i32>` 类型位 → **wave466 收**；dyn Trait；默认方法体；Darwin residual；wave445 ABI tip L4 |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |

## 26 日 wave466（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | 类型位 `Wrap<i32>` XP003（type_ref 不吃角括号）；`struct Wrap<T>` 布局失败假靠 STRUCT_LIT invent；host-C `struct ast_T` BLD001 |
| 根修 | ① type_ref IDENT 后解析 `<T,…>`（slot0→`elem_type_ref`）② struct/skip 跳过类型参角列表使 layout 登记 `v:T` ③ field_access 单参 mono（base 类型参→具体）④ host-C 字段发射前 mono 替换类型参字段 |
| 权威 | G.7：`parser_asm_type_ref_slice` + struct_layout/skip_tl + field_access heavy/strict 孪生 + `codegen.x`/seed `codegen_gen` 同 commit |
| 探针 | type-pos get_v=42 · bare=42 · take(w.v)=42 · chain=7 · Point=42 · 矩阵 rv/option/hello · trait main=42 |
| soft 仍红 | dyn Trait；多参 `Name<T,U>` 字段映射（仅 slot0 mono）；默认方法体；Darwin residual；wave445 ABI tip L4 |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| SHA | 产品 **`ad3fb69ca`** |


## 26 日 wave467（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | 多参类型位 `Name<T,U>` / 泛型 struct 字段 mono 假绿（`Pair<A,B>` 两字段皆 mono 成 A → host BLD001） |
| 根因 | wave466 仅存 slot0（elem_type_ref）；mono 一律用首 type arg |
| 根修 | type type-arg sidecar + layout 类型参名登记；parse 角列表全捕；codegen/field_access/strict 按参名→槽 mono |
| 权威 | `ast_pool.c` + type_ref/struct_layout slice + codegen.x/seed + field_access + strict_minimal 同 commit |
| 探针 | Pair&lt;A,B&gt; host=42 · Wrap 回归=42 · trip=42 · scalar Pair=42 |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| tip | 产品 **`df917e968`** |


## 26 日 wave468（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | trait 默认方法体：空/部分 `impl` 假红 missing method；body 被 skip 丢弃 |
| 根因 | skip_one_trait 只登记签名；completeness 要求 impl free fn；onefunc buf 拒 TOKEN_SELF |
| 根修 | `method_has_default`+fn pos；check 前 onefunc re-parse + 最小 commit；override 跳过 inject；TOKEN_SELF 形参名 |
| 权威 | skip_tl + seed twin + onefunc_buf 同 commit |
| 探针 | empty impl=42 · override=42 · partial=42 · sig-only 仍红 · run-trait · Pair=42 |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| tip | 产品 **`7a89607fa`** |

## 26 日 wave469（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | trait 签名 `Self`（TYPE_NAMED）与 impl 具体类型假红 parameter/return type mismatch |
| 根因 | completeness 字面比对 `"Self"` vs `A`，无 `for` 类型改写 |
| 根修 | `xlang_skip_trait_named_eq_self_c` + seed twin；全部 NAMED 名比对走 helper |
| 权威 | skip_tl + seed twin 同 commit |
| 探针 | Self param+ret=42 · ret-only=42 · param-only=42 · wrong B 仍红 · explicit A=42 · default 回归=42 · Pair=42 |
| soft | Self+default hoist（字面 Self 与 for 检）；*Self；多 mono 共享单 layout；dyn Trait |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| tip | 产品 **`b045c5175`** |

## 26 日 wave470（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | trait 默认方法 `self: Self` + 空 `impl` 假红 *no impl* / missing method |
| 根因 | hoist re-parse 保留字面 Self；UFCS 要 param0 type_ref==receiver；inject 非 per-impl |
| 根修 | Self↔for 别名（named_eq + self_matches）；per-impl hoist；commit 改写 Self→for named |
| 权威 | skip_tl + seed twin 同 commit |
| 探针 | Self+default empty=42 · override=42 · concrete default=42 · explicit=42 · wrong 仍红 · run-trait |
| soft | *Self；Self ret/struct-lit default body；多 empty impl Self；多 mono layout；dyn |
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| tip | 产品 **`4198ebbd4`** |

## 26 日 wave471（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | ① 多 empty-impl Self default → parameter count mismatch ② \*Self completeness 假红 ③ Self-ret default `return self` missing method |
| 根因 | ① buf onefunc 栈槽复用 sidecar，`append_param` 累加 ② rewrite/match 仅 NAMED Self ③ onefunc 体仅 INT |
| 根修 | `ast_pool_onefunc_reset` 于 buf parse 入口；\*Self→\*for + \*For 接收者匹配；`return self`/IDENT |
| 权威 | onefunc_buf + skip_tl + seed twin 同 commit |
| 探针 | multi empty=42 · \*Self override/default=42 · Self-ret id=42 · wrong \*B 红 · run-trait |
| soft | Self `{…}` struct-lit default body；多 mono layout；dyn Trait |
| 矩阵 | rv42 / option / hello / stdlib-import（**mac+Ubuntu L2**） |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| tip | 产品 **`06fd3d453`** |

## 26 日 wave472（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | trait 默认方法 `return Type {…}` / `return Self {…}` → 空 impl 假红 *missing method* |
| 根因 | onefunc return 仅 INT / bare self\|ident；`IDENT {` 当失败 → hoist 不 inject |
| 根修 | onefunc：LBRACE → G.7 `finish_struct_lit_from_type_ident` → `return_expr_ref`；skip_tl commit：STRUCT_LIT 名 Self→for + stamp resolved_type |
| 权威 | `parser_asm_one_function_buf_slice.inc` + `parser_asm_skip_tl_slice.inc`（thin glue） |
| 探针 | concrete A lit=7 · Self lit=7 · return self=42 · multi empty=42 · run-trait · 矩阵 |
| soft | let+return 复杂体；dyn Trait；`method(Method.POST)` dual 直调；multi mono 稀有形 |
| 矩阵 | rv42 / option / hello / stdlib-import（**mac+Ubuntu L2**） |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| tip | 产品 **`c4c4ed1ed`** |

## 26 日 wave473（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | trait 默认 `let s: T = …; return s` → typeck *cannot resolve return subexpression* / 或污染 main body |
| 根因 | ① onefunc buf `body_skip_let_const` 丢 let 仍收 `return s`；② commit 预置 `num_lets` 再 fill → 全局 lets 池 insert/shift 污染邻函数 |
| 根修 | G.7 `parser_parse_body_lets_into` 收集 body let/const；fill 前 `num_lets/num_consts=0`；fill 后 Self→for 改写 let 类型与 STRUCT_LIT init |
| 权威 | `parser_asm_one_function_buf_slice.inc` + `parser_asm_skip_tl_slice.inc`（thin glue） |
| 探针 | A/Self lit 回归=7 · let Self/A/Self=A=7 · multi-let INT+STRUCT_LIT=7 · return self/multi=42 · run-trait · 矩阵 |
| soft | `let x = self.v` 字段访问 init；mid-body if/loop；dyn Trait；dual Method.POST；multi mono 稀有形 |
| 矩阵 | rv42 / option / hello / stdlib-import（**mac+Ubuntu L2**） |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| tip | 产品 **`161632b90`** |

## 26 日 wave474（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | trait 默认 / free 体 `let x = self.v`、`return self.v` → missing method 或 return type `?` |
| 根因 | ① `parse_body_lets_into` init 仅 TOKEN_IDENT，TOKEN_SELF 未入 `parse_expr` → init_handled=0；② onefunc return 仅 bare self\|ident / STRUCT_LIT，DOT 后缀失败 → hoist 不 inject |
| 根修 | G.7 body_lets：IDENT\|SELF 同走 `parse_expr_into`；onefunc：非 `;`/`}` 的 self\|ident 后缀走 `parse_expr` → `return_expr_ref` |
| 权威 | `parser.x` + seed `parser_gen.linux.x86_64.c` + `parser_asm_one_function_buf_slice.inc` |
| 探针 | free/trait let self.v=7 · return self.v=7 · combo multi-let+get=7 · let STRUCT 回归=7 · return self=42 · run-trait · 矩阵 |
| soft | mid-body if/loop/expr_stmt；dyn Trait；dual Method.POST；multi mono 稀有形 |
| 矩阵 | rv42 / option / hello / stdlib-import（**mac+Ubuntu L2**） |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| tip | 产品 **`6a64de294`** |

## 26 日 wave475（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | trait 默认体 mid-body `if` / `while` / assign expr_stmt → 假红 missing method（free 路径同体绿） |
| 根因 | default hoist 的 thin onefunc buf 在 body_lets 后硬要求 `return`；commit 只 fill lets+final，不填 if/while/expr_stmt |
| 根修 | G.7 mid-body 循环：`parse_if_stmt_into` / cond+`parse_block`+`append_while` / `parse_expr` → push_stmt_order；commit fill ifs/whiles/expr_stmts + src_stmt_order（seed_commit 双生） |
| 权威 | `parser_asm_one_function_buf_slice.inc` + `parser_asm_skip_tl_slice.inc`（thin glue） |
| 探针 | if=7 · while=10 · assign=7 · letif=7 · combo if+asg=7 · ret7/self.v/let 回归 · run-trait · 矩阵 |
| soft | dyn Trait；dual Method.POST；multi mono 稀有形；for/match mid-body in default |
| 矩阵 | rv42 / option / hello / stdlib-import（**mac+Ubuntu L2**） |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| tip | 产品 **`dc5c74615`** |
| 日志 | Ubuntu `/tmp/ubuntu_w475_probes.log` |

## 26 日 wave476（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | trait 默认体 mid-body C-style `for` / stmt-position `match` → 假红 missing method（free 路径同体绿） |
| 根因 | thin onefunc buf 无 FOR/MATCH 分支；commit 不 fill fors（match 走 body_expr_stmt 已有） |
| 根修 | G.7：`for (init;cond;step){body}` → append_for + stmt_order kind4；`match` → parse_match_into → body_expr_stmt；commit fill_fors |
| 权威 | `parser_asm_one_function_buf_slice.inc` + `parser_asm_skip_tl_slice.inc`（thin glue） |
| 探针 | for_let=3 · for_expr_init=2 · for_empty_cond=3 · match_stmt=10 · match_retarm=10 · for+while combo=4 · if/while/assign/self.v 回归 · run-trait · 矩阵 |
| soft | dyn Trait；dual Method.POST；multi mono 稀有形；mid-body let（prefix lets only）；return-match compound |
| 矩阵 | rv42 / option / hello（**mac+Ubuntu L2**） |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| tip | 产品 **`808769ac5`** |
| 日志 | Ubuntu `/tmp/ubuntu_w476_probes.log` |

## 26 日 wave477（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | trait 默认体 mid-body `let`/`const`（控制流之后）→ 假红 missing method（free 路径同体绿） |
| 根因 | thin onefunc 仅 prefix `parse_body_lets_into`；后置 `let j` 落入 expr_stmt → parse_expr 失败 → 默认方法丢弃 |
| 根修 | G.7 mid-body `TOKEN_LET\|CONST` 再入 `parse_body_lets_into` + stmt_order kind 0/1（free onefunc mid_let 孪） |
| 权威 | `parser_asm_one_function_buf_slice.inc`（thin glue；commit fill_lets 已覆盖全量 lets） |
| 探针 | while+midlet=3 · for+midlet=3 · if+midlet=7 · asg+midlet=7 · multi midlet=4 · Self midlet=7 · match+midlet=11 · free=3 · for 回归=3 · run-trait · 矩阵 |
| soft | dyn Trait；dual Method.POST；multi mono 稀有形；return-match compound；expr-style match 后 mid-let 角 |
| 矩阵 | rv42 / option / hello（**mac+Ubuntu L2**） |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| tip | 产品 **`7da3bff0d`** |
| 日志 | Ubuntu `/tmp/ubuntu_w477_probes.log` |

## 26 日 wave478（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | trait 默认 `return match …` 假红 missing method（free 同体绿） |
| 根因 | thin onefunc `return` 后仅 if-expr / self\|ident\|INT；`TOKEN_MATCH` 失败 → 默认方法丢弃 |
| 根修 | G.7：`parse_match_into` → `return_expr_ref`；match 后若有二元后缀则自 MATCH 起点 `parse_expr_into` 重解析（`match + n`） |
| 权威 | `parser_asm_one_function_buf_slice.inc`（thin glue；commit 已 prefer return_expr_ref） |
| 探针 | ret_match=42 · block_arm=42 · self.v subject=42 · match+binop=42 · multi/if 嵌套=42 · midlet/while/match_stmt 回归 · free · run-trait · 矩阵 |
| soft | dyn Trait；dual Method.POST；multi mono 稀有形（`Pair<T,U>` 返回仍 incomplete type） |
| 矩阵 | rv42 / option / hello / si（**mac+Ubuntu L2**） |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| tip | 产品 **`408b66117`** |
| 日志 | Ubuntu `/tmp/ubuntu_w478_probes.log` |

## 26 日 wave479（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | dual `method(Method.POST)` 误选 `method(u8)`（http execute 需中间 `let mm: Method`） |
| 根因 | 产品 **strict_minimal** field_access 无用户 enum 路径；wave465 ambient 对 **null** 字段 stamp 调用点 ambient（main **i32**）→ overload 按整数抬宽选 u8 |
| 根修 | G.7：`pipeline_typeck_field_layout_named_c` return 2（TYPE_NAMED 枚举）；wave472 纪律：null **不** stamp ambient；seed C + `.x` twin |
| 权威 | `seeds/pipeline_glue_strict_minimal.from_x.c` + `src/asm/pipeline_glue_strict_minimal.x`（链上 live） |
| 探针 | dual_post=1 · dual_order=1 · dual_u8=2 · dual_let=1 · as Method=1 · wrap.v=42 · http request_owned · run-trait · 矩阵 |
| soft | dyn Trait；multi mono 稀有形（`return Pair<T,U>` incomplete `ast_T`）；`return Method.POST` 作 Method 返回位 XP003 角 |
| 矩阵 | rv42 / option / hello / si（**mac+Ubuntu L2**） |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| tip | 产品 **`25a31ee88`** |
| 日志 | Ubuntu `/tmp/ubuntu_w479_probes.log` |

## 26 日 wave480（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| 债 | generic `return Pair<T,U>` / `return Wrap<T>` host-C incomplete `struct ast_T`/`ast_U`（BLD001）；typed let 绿、无类型标注 bare 仍可红 |
| 根因 | `codegen_resolve_generic_struct_field_type` 取首个 layout 同名 TYPE_NAMED（常为泛型函数返回 `Pair<T,U>`），type-arg 为自由类型参 → 发射 incomplete |
| 根修 | G.7：`codegen_type_ref_is_host_concrete`；mono / STRUCT_LIT 仅接受 concrete（builtin 或 module layout 名）；跳过自由 T/U 继续扫 `Pair<A,B>` |
| 权威 | `codegen.x` + seed `codegen_gen.linux.x86_64.c` 同 commit |
| 探针 | pair_ret typed=42 · wrap_ret=42 · pair_i32 typed=42 · tf+typed=42 · local Pair/Wrap 回归=42 · dual POST=1 · Method.POST ret=1 · run-trait · 矩阵 |
| soft | dyn Trait；无 ambient 的 bare `let p = make_pair<…>` / multi-instantiation 同 C 标签 `Wrap<A>+Wrap<B>`；Darwin residual |
| 矩阵 | rv42 / option / hello / si（**mac+Ubuntu L2**） |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| tip | 产品 **`99dc4095c`** |
| 日志 | Ubuntu `/tmp/ubuntu_w480_probes.log` |

## 26 日下波（wave492 · 待开）

| 候选 | 说明 |
|------|------|
| A. Cap residual | dyn Trait 仍 **soft 大特性**（无关键字/无 TYPE_DYN/无 vtable；勿硬开） |
| B. Cap residual | 多 empty impl Self + default body struct-lit（wave472/491 已收但多 impl 场景仍 soft） |
| C. R2 真迁 | rest seed 小模块；避开函数指针/`&var` |
| D. 验收 | tip L4 日终/整波收口；升钉待你确认 |
| soft 勿抢 | `fn`/`var` 非语言债；Darwin residual；P010 untyped-let |

**明细流水（wave396–446）** → 只读归档 [`自举进度-归档-2026-07-25.md`](自举进度-归档-2026-07-25.md)。
