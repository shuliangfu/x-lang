# X 语言 / X language 当前进度（2026-07-27 · wave519）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`562488e58`**（双端 L2，wave519；L4 待整波收口） |
| 钉盘 | **`2bc0885d3`**（待 Ubuntu 钉盘升级） |
| bstrict | **129** |

## wave519（已闭 · runtime_compress_zlib_glue R2 migration thin+rest 双端 L2）

| 项 | 状态 |
|----|------|
| 触发 | wave518 完成后，继续剩余纯 C seed 小模块 |
| 模块 | `runtime_compress_zlib_glue`（41 行，2 公开函数：deflateInit2 / inflateInit2） |
| 债 | `.x` 仅 doc anchor；zlib.h 宏包装必须 `#include <zlib.h>`，`.x` 无法表达 |
| 根修 | ① `.x`：`#[no_mangle] deflateInit2` / `inflateInit2` wrappers（无 `_c` 后缀，匹配 libz.x extern 声明）+ 英文 docblock ② `.from_x.c`：`deflateInit2_impl_c` / `inflateInit2_impl_c` 桥（始终编译，`#include <zlib.h>` + `#undef` 宏 + 调用真实 `deflateInit2_`/`inflateInit2_`） ③ `Makefile` 加 R2 thin+rest `ld -r` 路径 |
| 构建模式 | **thin+rest**——必须 `#include <zlib.h>` 获取 `ZLIB_VERSION` / `sizeof(z_stream)` / `deflateInit2_` 符号声明，`.x` 无法 `#include` C 头文件 |
| nm 一致性 macOS | cold vs R2：4 核心 T 符号对齐（仅 doc_anchor 多余） |
| nm 一致性 Ubuntu | cold vs R2：4 核心 T 符号对齐（仅 doc_anchor 多余） |
| 产品矩阵 macOS | hello ✅ · g-ffi-5-std-wrap ✅ |
| 产品矩阵 Ubuntu | hello ✅ · g-ffi-5-std-wrap ✅ |
| 验证级别 | **L2 日常真测**（按自举方法 §0.2 节奏；非每微 commit 全量 L4） |
| PLATFORM | SHARED — zlib 跨平台可移植；无 OS 专属代码 |
| 关键发现 | ① `#[no_mangle]` 关键：libz.x 用 `extern function deflateInit2(...)`（无 `_c` 后缀），thin wrapper 必须用 `#[no_mangle]` 才能生成匹配的符号名（无 `_c` 后缀） ② `_impl_c` 桥 + `#include <zlib.h>` 必须始终编译 ③ zlib.h 宏包装模式：`#undef deflateInit2` 后调用 `deflateInit2_` 完成宏展开等价行为 |
