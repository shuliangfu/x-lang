# Shux 当前进度（2026-07-19）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[自举进度-归档-2026-07-17.md](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`60811830` 波**（钉盘仍 **`c51759eb`**） |
| 产品 L4 放行钉盘 | ✅ **`c51759eb`**（钉盘时 123；**不**因本波升钉） |
| 产品 bstrict 套件 | **125**（+void-main + comment-prefix；日志 `OK (125 scripts…)`） |
| tip mac 真冷 L4+125 | ✅ **125/125**（anon struct backfill + main_cmd_run -o 修后；ok=22 skip=102 fail=0 wall=68s） |
| tip Ubuntu 125 bstrict | ⏳ 待 T8 验证（mac 已绿，Ubuntu 待 push 后金标） |
| **typeck CTFE fold** | ✅ 主路径 + **scalar 2-arg** + **1-arg pure** + **nested** + **vector lane** |
| **ASM bulk residual** | ✅ **PRIMARY** + **TASK_SPECIAL** + **prepare-ensure OS** + **process_argv** + **time formal** |
| **WPO call-site fold** | ✅ **flat + nested pre-emit** |
| **anon struct lit backfill** | ✅ **typeck → codegen** contextual return-type 推断（setter 避免 Expr 值传 SIGBUS） |
| **main_cmd_run -o 语义** | ✅ **显式 -o → compile-only**（不再泄漏 binary exit code 到 shux） |

## 本波摘要（2026-07-19 · anon struct backfill + -o compile-only）

| 波 | 结果 |
|----|------|
| **入口** | tip `60811830` 引入 `shux run / bare shux file.x` in-memory compile-and-run；附带回归：`shux file.x -o out` 误走 exec 路径，binary exit code 污染 shux exit（run-if-expr/run-enum bstrict 失败） |
| **anon struct backfill** | typeck `typeck_check_expr_struct_lit`：匿名 `{ a: 1, b: 2 }` 从 contextual `return_type_ref` 回填 `struct_lit_struct_name`；经 `typeck_resolve_type_alias_ref_local` 解析 alias；codegen emit `(struct <module>_Pair){...}` 而非 `(struct <module>_){...}`（避免 cc incomplete type） |
| **ABI 根修** | 原 backfill 用 `pipeline_arena_expr_get_copy/set_copy`（~400B Expr 值传 X-ABI sret 不匹配 → SIGBUS on arm64）；新增 `pipeline_expr_struct_lit_type_name_set(arena, ref, name, len)` setter，避开值传；typeck.x + pipeline_glue.c + typeck_gen.c seed 三同步 |
| **main.x 根修** | `main_cmd_run` 在 `driver_argv_ensure_run_o` 前先 `main_argv_has_o_flag` 扫描 argv；显式 -o → 仅 `main_run_compiler_x_path_impl` 编译，不调 `driver_exec_compiled`，不 setenv `SHUX_RUN_QUIET` |
| **mac 验收** | 真冷 L4（rm 全 .o + rm 二进制 + `make bootstrap-driver-seed` + `g05_prepare_and_relink.sh`）→ **125 bstrict ok=22 skip=102 fail=0 wall=68s**；run-if-expr OK · run-enum OK · if-expr/simple binary exit=10（非 138 SIGBUS）· anon struct `{a:10,b:20}` → exit 30 ✓ |
| **回归** | shux run hello.x ✓ · bare shux hello.x ✓ · shux hello.x -o out → exit 0 ✓ |
| **残差** | 待 T7 commit + push；T8 Ubuntu L4 金标；T9 升钉 + 文档；T10 Cap 候选 |

### residual 下一波

1. **T7**：commit + push（英文 message：`fix(typeck,driver): anon struct lit backfill + main_cmd_run -o compile-only`）  
2. **T8**：Ubuntu L4 金标（`ssh ubuntu-server` + `git pull` + 真冷 L4 + 125 bstrict）  
3. **T9**：升钉 + 自举进度 / 当前进度 / 验证文档 + docs commit + push  
4. **T10**：下一波 Cap 候选规划（parser struct-lit + unsafe-block bug P1；其它 fk0 GAP）

```text
构建级别：mac L4 真冷绿（ok=22 skip=102 fail=0）；Ubuntu 待 T8 验证
产品 tip L4 钉盘：c51759eb（钉盘时 123；现行套件 125）
权威：ast.x Expr.struct_lit_struct_name · pipeline_glue.c pipeline_expr_struct_lit_type_name_set · main.x main_argv_has_o_flag · seeds/typeck_gen + driver_gen.linux.x86_64.c
```

---

## 前波摘要（2026-07-18 · 真冷 125 + i64 ABI）

| 波 | 结果 |
|----|------|
| **入口** | 双端 tip 真冷 L4 + 全量 bstrict；产品套件已扩到 **125** |
| **mac** | 真冷 L4 + **125/125** 绿（`d8aa86e2`；含 i64-ctfe / void-main / comment-prefix） |
| **Ubuntu** | tip L4 冷链挂 **i64-ctfe**（`pipeline_gen`/`glue` `Expr.int_val=int32_t` 截断 INT64_MAX→折 0）；恢复 seed int64 + relink → 门禁绿；**125 bstrict OK**（`SKIP_BUILD`） |
| **根修** | Makefile：`check-pipeline-gen-expr-i64-abi`（bootstrap / types.inc 前强制）；driver_gen L4 seed pin（防冷启动 -E 挂死） |
| **文档** | 自举进度 / 验证 / README：现行套件 **125**；钉盘 `c51759eb` 历史 **123** 保留 |
| **残差** | Makefile 改动待 commit；tip **未**升 L4 钉盘；其它 fk0 GAP / plan 瘦身 |

```text
构建级别：mac L4 真冷绿；Ubuntu 修 i64 后 bstrict 125（SKIP_BUILD；≠ tip L4 升钉）
产品 tip L4 钉盘：c51759eb（钉盘时 123；现行套件 125）
权威：ast.x Expr.int_val=i64 · seeds/pipeline_gen.linux.x86_64.c · check-pipeline-gen-expr-i64-abi
```

---

## 前波摘要（2026-07-18 · process_argv 按需 ensure）

| 波 | 结果 |
|----|------|
| **根修** | prepare process_argv 门控；use_line weak-skip；post-module process_shux U 补链 |
| **验收** | mac L2 pure/option 不重建 process_argv |
| **残差** | → **本波 formal time.o** |

## 前波摘要（2026-07-18 · prepare-ensure OS 按需）

| 波 | 结果 |
|----|------|
| **根修** | prepare 对 time/random/env 门控 ensure；删 invoke_cc warm time.o early ensure |
| **验收** | mac/Ubuntu L2 pure 不重建三 OS .o |
