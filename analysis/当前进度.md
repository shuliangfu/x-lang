# X 语言 / X language 当前进度（2026-07-28 · wave579 Cap DepCtx 布局 · 产品矩阵双端绿 · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`5cc9417d1`**（wave579 Cap DepCtx [64]→[128] 布局 · L2 双端矩阵绿 · 未 L4） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave579（进行中 · Cap DepCtx 布局根因 · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave578 Ubuntu hello SEGV（std.fmt C emit / emit_skipped） |
| tip | **`5cc9417d1`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave579 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| hello SEGV | `runtime_pipeline_abi.h` 仍 `prefix/entry/region` **[64]**，ast.x/seed **[128]**；`driver_run_compiler_parsed` / cold seed `sizeof(*pctx)` 分 **0x800588**；typeck stack-escape `memset(region_label,0,128)` 写穿 DepCtx 进 **dep_modules grow_vec** → dep[11]=`0x7fff00000000` | ✅ 头文件对齐 [128]；重建 `runtime_driver_no_c.o`（含 parsed seed） |
| dep_path_rows 溢出 | Cap 后 `set_import_path` 可写 127 字节进 64 行 | ✅ ast_pool 行宽 64→128 |

**数据流（五问）**：产生点 = Cap 后 ABI 头未跟 [128]；存储 = freestanding bump 上 DepCtx 欠分配；消费 = `pipeline_typeck_scan_module_struct_stack_escape_c` + 后续 `codegen_emit_skipped_dep_type_definitions`。

**操作纪律**：改 `runtime_pipeline_abi.h` 后必须重建 **`runtime_driver_no_c.o`**（不只 `runtime_driver_abi.o`）；g05 不因仅头文件 mtime 自动刷 parsed seed。

### wave579 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| rv | ✅ 42 | ✅ 42 |
| option | ✅ 102 | ✅ 102 |
| stdlib-import | ✅ 0 | ✅ 0 |
| hello | ✅ Hello World | ✅ Hello World（`0a`） |
| long75 | （未本波强验） | ⛔ 默认 asm residual；`-backend c` 前波 42 |

### 下一波

- long75 默认 **asm ELF**（C 已证 Cap 长名）  
- residual `>63` / memcpy64；strict_minimal -E  
- 整波双端 **L4** + 升钉  

## wave578（半闭 · Cap Ubuntu 布局+dep 回绑 · tip a1a1c2fe7 文档）

| 项 | 状态 |
|----|------|
| Ubuntu pipeline_x seed 编不过 | ✅ `codegen_out_buf_len` |
| dep co-emit module_at NULL | ✅ emit 入口 driver 槽回绑 |
| hello SEGV | ✅ 转 wave579 布局根因 |

## wave577（已闭 mac L2 · Cap AST name 63→127）

见上波；钉盘仍 `01479a857`。
