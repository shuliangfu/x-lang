# X 语言 / X language 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **重命名权威**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)（主域 **`xlang.dev`**）  
> **git 锚点（改名前）**：tag **`anchor-pre-xlang-rename`** @ `d07bcf155`  
> **项目目录**：仍为 `…/shu/shux`（本波**不改**工作区路径）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **工程** | **Shux → Xlang 全库重命名进行中**（CLI / 源码 / ABI / 文档） |
| 产品二进制 | **`xlang` / `xlang_asm` / `xlang-c` / `bootstrap_xlangc`** |
| 宿主变量 | **`XLANG=`**（原 `SHUX=`） |
| 配置 | **`xlang.toml`** / **`xlang-build.sh`** |
| 主域 | **`xlang.dev`**
| 语言品牌 | 中文 **X 语言** · 英文 **X language**（CLI 仍 `xlang`） |
| 项目目录 | **保持** `/Users/…/shu/shux`（及 Ubuntu 同构） |
| mac 矩阵（本波） | ✅ rv=42 · option · si · hello |
| Ubuntu | 待 pull 后 L2/L4 |
| 自举 Cap residual | **仍冻结**至重命名收口 + 双端 L4 |

## 本波摘要

1. 全库文本/路径 `shux`→`xlang`（约 2700+ 文件内容；139 路径 `git mv`）。
2. 二进制与构建：`TARGET=xlang`，g05 → `xlang_asm`，脚本 `xlang_compile_*` / `g05_*_xlang`。
3. ABI：`shux_*`→`xlang_*`；codegen 字节数组 `s,h,u,x`→`x,l,a,n,g`；emit 长度与 `struct xlang_slice_`（19）已修。
4. 冷启动兼容：`process_shux_*` 薄别名 → `process_xlang_*`（seed 二进制仍可能 emit 旧名，直至重生 bootstrap）。
5. **mac**：bootstrap-driver-seed + g05 绿；产品矩阵 **rv/option/si/hello 全绿**。
6. **未改**：工作区目录名 `shux`；`github.com/shuliangfu/shux` remote（待仓库改名）；外部 skill 路径。

## 下一波

1. Ubuntu `git pull` + L2/L4 金标  
2. 扫残留（`.base` 备份、tree-sitter 包名、probe 日志、mind 图）  
3. 重生 `bootstrap_xlangc` 后删除 `process_shux_*` 别名  
4. 双端 L4 + 升钉 → 恢复 Cap residual  

```text
锚点：anchor-pre-xlang-rename (d07bcf155)
产品：xlang_asm · XLANG=
mac 矩阵：绿
目录：…/shu/shux 不变
下一：Ubuntu 验 + 收口残留 + L4
```
