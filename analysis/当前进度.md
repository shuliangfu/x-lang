# X 语言 / X language 当前进度（2026-07-28 · wave581 Cap let long-name · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`dec1ef1fe`**（wave581 Cap let long-name · L2 双端绿 · 未 L4） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave581（已闭 · Cap let/const 长名 64→128 · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave580 后 let 名 ≥75 → `_main` UNDEF / XP003；let 63/64 绿 |
| tip | **`dec1ef1fe`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave581 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| OneFunc append 拒 >64 | `pipeline_onefunc_append_{let,const}` `name_len>64` + 侧车行宽 64 | ✅ 门控 127 · 行宽 128 · copy64 载荷 128 |
| block/top-level 截断 | `pipeline_block_append_{let,const}` memcpy 64；top_level_let set/hoist >64 | ✅ 127 content；top-level slice 63→127 |
| host-C 夹 NUL | codegen emit_nm `ci2<64` 但 `emit_nml=lname_len` | ✅ 拷贝环 128；codegen.x + pin 同 commit |
| 调用方 64 缓冲 | `asm_ctx_local_name_copy64` + bootstrap_glue `nb[64]` | ✅ 载荷 128 · `nb[128]` |

**数据流（五问）**：产生 = parse_body_lets → onefunc_append_let；存储 = OneFunc 侧车 + LetDecl.name[128]；消费 = fill_block / asm local / host emit_nm。append 失败 → 整函数 parse 失败 → 无 `_main`。

**操作纪律**：改 `ast_pool.c` 后重建 **pipeline_x.o + standalone + parser_asm_thin_glue**；改 codegen pin 后重建 **codegen_x.o**。

### wave581 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| let 名 63/64/75/127 默认 asm | ✅ 42 | ✅ 42 |
| let75 `-backend c` | ✅ 42 | ✅ 42 |
| top-level let75 | ✅ 42 | ✅ 42 |
| fn75 回归 | ✅ 42 | ✅ 42 |
| rv / option / si / hello | ✅ | ✅ Hello World + `0a` |

### 仍 soft

- residual：其它 `name_len>64` 侧门（type_alias_set、enum variant byte_at、match/library slice 等）  
- 整波双端 **L4** + 升钉  

### 下一波

1. residual `>64` 侧门扫尾（type_alias / enum / match 等按产品阻塞裁剪）  
2. 整波双端 L4 + 升钉  

## wave580（已闭 · Cap long-name pure-asm）

见上波；tip `9da8b5f7e` / docs `59d5137a7`。
