# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[自举进度-归档-2026-07-17.md](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **本波 nested unary CALL CTFE 收口后 tip**（钉盘仍 **`c51759eb`**） |
| 产品 L4 放行 | ✅ **钉盘 `c51759eb`**（不升钉） |
| tip 双端 bstrict | ✅ **123/123 @ `c51759eb`**（钉盘） |
| **typeck CTFE fold** | ✅ 主路径 + **scalar 2-arg** + **1-arg pure** + **nested** + **vector lane** |
| **LANG-006 residual** | ✅ 已闭（BITNOT + 逻辑 const） |
| **WPO call-site fold** | ✅ **flat + nested pre-emit** |

## 本波摘要（2026-07-18 · nested pure CALL → typeck CTFE）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排「下一产品债另开地图」 |
| **地图** | 平面 `scale(1024,64)` 已 `return 0`；嵌套 `f(g(3),4)` / 一元 `g(41)` 仍发 live CALL——CALL CTFE 只盖 `nargs==2` 的 `p0 op p1`，一元 `return p0 op lit` 不 stamp |
| **根修** | G.7：`glue_try_eval_pure_param0_scalar_func_c`（id / p0 op lit / lit op p0 / unary p0）+ `glue_typeck_fold_expr_ref` nargs==1 先 stamp 一元再 lane 路径；实参先递归 fold → 嵌套 `f(g(c),…)` 自然通；MONO/NO_FOLD 仍跳过 stamp |
| **验收** | mac L2：`pipeline_x` + **`bootstrap_seed_pipeline_filtered`** + g05；`-E nested_const_spec_fold` / unary / deep nest → **`return 0`**；scale/vec 不回归；MONO 保留 call；lang-const **12/12**；rv=42 · opt=102 · hello=0 · nested run=0 |
| **操作债** | 动 `pipeline_glue.c` 须重建 **`build_asm/bootstrap_seed_pipeline_filtered.o`**（链上权威，非仅 `pipeline_x.o`） |
| **残差** | 下一产品债另开地图；（旁）ASM bulk std on_demand；Darwin pure-asm 旁支 |

### residual 下一波

1. 下一产品债：另开地图再动（禁无地图 tip L4）  

```text
构建级别：L2（pipeline_x + bootstrap_seed_pipeline_filtered + g05；非 tip L4）
产品 tip L4 钉盘：c51759eb
权威：glue_try_eval_pure_param0_scalar_func_c + glue_typeck_fold_expr_ref CALL nargs=1
```

---

## 前波摘要（2026-07-18 · vector lane WPO → typeck CTFE）

| 波 | 结果 |
|----|------|
| **根修** | INDEX NAMED SIMD + laneK(vec_binop) stamp |
| **验收** | `-E vec_const_spec_fold` → `return 0`；MONO 保留 lane0 call |
| **残差** | nested unary → **本波已迁 pre-emit** |

## 前波摘要（2026-07-18 · WPO scalar call-site fold → typeck CTFE）

| 波 | 结果 |
|----|------|
| **根修** | `glue_typeck_fold_expr_ref` scalar `f(c0,c1)`；MONO/NO_FOLD 门控 |
| **验收** | `-E const_spec_fold` → `return 0`；lang-const 12/12 |
| **残差** | vector lane → 前波闭；nested → **本波闭** |

## 前波摘要（2026-07-18 · LANG-006 residual：`~` + `&&`/`||`）

| 波 | 结果 |
|----|------|
| **根修** | unary `TOKEN_TILDE`→`EXPR_BITNOT`；bool→int init coerce |
| **验收** | lang-const **12/12**；矩阵绿 |
| **残差** | WPO call-site → 已闭 |

## 前波摘要（2026-07-18 · typeck CTFE 常量折叠权威恢复）

| 波 | 结果 |
|----|------|
| **根修** | typeck glue 恢复 fold 生产者 + typeck.x 挂钩 + C codegen 消费 folded |
