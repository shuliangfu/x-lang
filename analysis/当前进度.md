# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（本波未改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`11a57351`**（双端 129） |
| tip | **`708d63e0`**（PREFER unbundle `async_asm_pool` from `pipeline_glue`） |
| 产品 bstrict | **129** · 钉盘双端全绿 |
| 本波性质 | 产品链 unbundle：pool 独立 TU + PREFER full.x；glue 不再 #include seed |
| 工程轨 prove | ✅ mac IDENTICAL · Ubuntu IDENTICAL（L2 · async_asm_pool） |
| 产品矩阵 | ✅ mac/Ubuntu rv=42 · option · stdlib-import · hello（L2/L3 g05） |
| Windows | **本波不需要** |

## 本波摘要

1. **PREFER unbundle `async_asm_pool`**（`708d63e0`）  
   - 删 `pipeline_glue.c` 对 `seeds/async_asm_pool.from_x.c` 的 `#include`  
   - 独立 `src/async/async_asm_pool.o` 入 **全部** `DRIVER_SEED_SUPPORT_EXTRA` + `g05_relink_env`  
   - PREFER：`src/asm/async_asm_pool.x` + rest `-DSHUX_ASYNC_ASM_POOL_FROM_X`（marker）`ld -r`  
   - 冷：整 seed C；`g05_ensure` 重建 stale `pipeline_x` / standalone / Darwin filtered（防双定义）  
   - `nm`：`pipeline_x` / filtered **无** pool T；pool 符号仅 `async_asm_pool.o`  

2. 验证  
   - mac：g05 OK · 矩阵绿 · prove IDENTICAL · `/tmp/mac_aap_g05d.log` · `/tmp/mac_prove_aap_unbundle2.log`  
   - Ubuntu：g05 OK（cold abi/diag 对齐后）· 矩阵绿 · prove IDENTICAL · `/tmp/ubuntu_aap_g05f.log` · `/tmp/ubuntu_prove_aap_unbundle.log`  
   - **日常 L2/L3 非 tip L4**；钉盘仍 `11a57351`  

3. 前序：G.7 fputs @ `e38f6624` · 钉盘 `11a57351`

## 下一波前排

1. **可选** tip 升钉：Ubuntu L4 + 双端 129（本波 unbundle 后产品面已动）  
2. **可选** 其它 R2 rest / Cap residual（liveness/cps PREFER unbundle 仿本波）  
3. **可选** Windows hybrid（需切双系统时再喊人）  

```text
钉盘：11a57351
tip：708d63e0（async_asm_pool PREFER unbundle from pipeline_glue；双端 L2/L3 矩阵+prove）
Windows：本波不需要切换
下一：可选 tip L4 升钉 / 其它 R2
```
