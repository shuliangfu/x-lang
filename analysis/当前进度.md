# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **lexer 嵌套块注释** @ `f16da857` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **语言词法 SHARED 改进**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 g05 + pure-asm 矩阵 + nested 探针 rv42 / `/**/`→99 @ `f16da857` |
| mac | ✅ L2 g05 + c-backend 矩阵 + nested 探针 @ `f16da857`（Darwin pure-asm CG002 tip 既有） |
| Windows | **本波不需要切换** |

## 本波摘要

1. **lexer 嵌套 `/* */` 块注释** @ `f16da857`  
   - **动机**：AI/手写 docblock 常在注释内写 `/**/` 或嵌套 `/* … */`，非嵌套词法会在首个 `*/` 提前结束 → 后续源码成碎 token / 静默丢 AST（wave45 历史踩点同类）  
   - **根修**：`skip_whitespace_and_comments` 深度计数（`/*` +1，`*/` −1，depth→0 才结束）  
   - **权威**：`compiler/src/lexer/lexer.x`；同 commit 对齐 `seeds/lexer_gen.linux.x86_64.c` + `lexer_empty_surface.from_x.c`  
   - **探针**：`tests/lexer/nested_block_comment.x`（深嵌套 return 42）；`/**/` 散文 return 99  
   - **语义边界**：无内层 `/*` 时，正文裸 `*/`（如 `char**/void*`）仍结束 depth=1（G.9 仍建议避免）  
   - **文档**：`docs/01-关键字.md` · `docs/08-语法规范.md`；skill G.9 注记同步  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_nested_comment.log` · `/tmp/ubuntu_g05_nested_comment.log`

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：pipeline_abi Cap residual 重 C 面 / 其它 rest pure 深迁  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；nested-comment 未升钉）
产品 tip：f16da857 lexer nested block comments L2
Windows：本波不需要切换
下一：pipeline residual 深迁 / 其它 rest / Darwin CG002（需时再说）
```
