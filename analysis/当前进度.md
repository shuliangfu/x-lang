# X 语言 / X language 当前进度（2026-07-29 · wave632 Cap pure-asm return large NAMED ARRAY_LIT durable + reent deep-copy · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`2a9d40d0c`**（wave632 Cap pure-asm return large NAMED durable + reent · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave632（已闭 · Cap residual pure-asm return large NAMED ARRAY_LIT durable + reent deep-copy · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Ubuntu pure-asm `return [S24{…}]`：simple=193/deep=113≠110；host-C 110；mac 常假绿 |
| tip 产品 | **`2a9d40d0c`**（product chain `fde6a0366` + `ff3b021d6` + polarity） |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave632 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| durable 拒 force_esz>8 → 栈 return 悬垂 | `glue_asm_emit_array_lit_durable` force_esz∉{1,2,4,8} → -1 | ✅ force_esz>8 COMMON bulk（struct let-init + bulk_mem_copy） |
| `let s = mk()` deep-copy esz=24→4 | `glue_slice_let_reent_deep_copy` clamp non-scalar → 4 | ✅ 保留 esz>8 + chunked bulk；frame pre-sum 对齐 |
| x86 high-end dest 过浅 + 大 nbytes | dest_off=shallow next；+ai·esz 越 rbp → SIGSEGV | ✅ byte0 @ dest_base+nbytes（deep end）；arm64 low-end 不变 |

**数据流（五问）**：产生 = `return [S24{…}]` force_esz=24 → durable 拒 → 栈 payload 出 dual-GP → 调用方悬垂；即便 durable COMMON 绿，`let s=mk()` reent deep-copy 再以 esz=4 拷坏。G.7：**扩** wave341 durable + wave418 reent deep-copy + wave598/630 bulk。

**权威**：`pipeline_glue.c` only。改后 **须** `pipeline_glue_standalone.o` + g05。

### wave632 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `return [S24…]` simple sum | ✅ 110 | ✅ 110 |
| return + noise 冲栈 | ✅ 110 | ✅ 110 |
| dual `let a/b = mk()` | ✅ 53 | ✅ 53 |
| let-local S24[] lit | ✅ 110 | ✅ 110 |
| `return [i32…]` / `[]Pt` | ✅ 42 | ✅ 42 |
| freestanding absolute XLANG | ✅ | ✅ 110 |
| host-C | ✅ | ✅ 110 |
| fixed `return S24[2]` | soft | soft fx=11（TYPE_ARRAY by-value） |
| rv / option / si / hello | ✅ | ✅ |

Ubuntu：`Lxlang_al_0` COMMON + deep-copy frame `sub $0x6280`；simple 绿。

### 仍 soft

- fixed `return S24[2]` / 大 NAMED TYPE_ARRAY by-value（fx=11）  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一叶（fixed large NAMED return 或其它），或整波 L4 + 升钉  

## wave631（已闭 · Cap residual pure-asm TYPE_SLICE ARRAY_LIT large NAMED · L2）

tip 产品 **`8bbbf951b`** / docs **`42a9f0bbf`**。S24[] lit RO/sl 绿；soft return 已由 **wave632** 闭。

## wave630（已闭 · Cap residual pure-asm INDEX assign bulk esz>8 · L2）

tip 产品 **`66a0f3ffa`** / docs **`0a78ca598`**。bulk esz>8 绿。
