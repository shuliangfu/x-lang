# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`11a57351`**（双端 129） |
| tip | **`d194aff5`**（R2 `async_cps_codegen` pure surface） |
| 产品 bstrict | **129** · 钉盘双端全绿 |
| 本波性质 | R2 `async_cps_codegen` pure surface |
| 工程轨 prove | ✅ mac IDENTICAL · Ubuntu IDENTICAL（L3 后 -E） |
| Windows | **本波不需要**（无 hybrid/Windows 探针） |

## 本波摘要

1. **R2 `async_cps_codegen` pure surface**（`d194aff5`）  
   - `.x`：pure name gates（`async_cps_callee_is_io` / future_wait* / `async_cps_is_sched_wrapper_name`）  
   - thin wrappers：`emit_hoisted_lets` / `expr_references_run_async(e,target)` / `block_has_run_async_ref(b,target)` — **修 arity** 与 C 公共面一致  
   - seed：pure C 体 `#ifndef SHUX_ASYNC_CPS_CODEGEN_FROM_X`；walk `_impl` 始终 seed；FILE* emit Cap residual；FROM_X `slice_marker`  
   - surface + `prove_module_selfhost` 条目  
   - 英文 G.9 + `#[no_mangle]` surface  

2. 验证  
   - mac：`SHUX=./compiler/shux bash tests/prove_module_selfhost.sh async_cps_codegen` → IDENTICAL  
   - Ubuntu：L3 bootstrap+g05 后 prove IDENTICAL（`/tmp/ubuntu_prove_async_cps_d194aff5.log`）  
   - **日常 L2 非 tip L4**；钉盘仍 `11a57351`

3. 前序：R2 `async_liveness` pure（`b12bf000`/`f2ca6580`）· `async_asm_pool` · P1 · 钉盘 `11a57351`

## 下一波前排

1. **R2 下一 rest** 或 **Cap residual**（liveness layout/emit · CPS FILE* emit / walk `_impl` 真迁）  
2. **可选** tip 升钉：Ubuntu L4 + 双端 129（含 SHARED + 本波 R2）  
3. **可选** Windows hybrid（需切双系统时再喊人）  
4. **可选** async_asm_pool 产品 unbundle；Ubuntu `-E` SIGSEGV 根因（编译器）

```text
钉盘：11a57351
tip：d194aff5（R2 async_cps_codegen pure；双端 L2 prove IDENTICAL）
Windows：本波不需要切换
下一：R2 下一 rest / Cap residual 或 tip L4 升钉
```
