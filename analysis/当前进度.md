# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave146** ld argv_has_obj pure @ `6dcea17c8` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **R2 Cap residual pure · path_pure L0**（`link_abi_asm_ld_argv_has_obj`） |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：其它 rest R2 可迁叶 — `link_abi_asm_ld_argv_has_obj` hybrid 下仍 always mega C（realpath 去重 .o 路径）  
2. **根修**：迁入 `labi_path_pure` L0 pure orch（纯字节 cstr 等值 + argv 扫）；Cap residual `link_abi_realpath_cap`  
3. **权威**：`.x` + cold twin + surface re-pin count 10→11 + mega `#ifndef PATH_PURE_FROM_X`；prove **IDENTICAL**  
4. **语义**：on-demand 推 .o 时按路径/realpath 去重，避免重复入链  
5. **验收**：双端 L2 g05 + 矩阵 + string/vec/slice/compress 邻域 + prove IDENTICAL；钉盘仍 **`3c88f7e51`**  

## 下一波前排

1. 其它 rest R2 / mega residual（常驻 Cap 不硬迁：contains_substr / undef_sym / path_io stat / wait / strerror / mega spawn-IO / ld_debug_argv 遍历 char** …；同类可评估 `link_abi_asm_ld_argv_push_stable` / `link_abi_asm_ld_push_obj`）  
2. 可选：未闭合块注释硬诊断（EOF depth>0）  
3. 可选 Windows hybrid（**需要时再通知重启**）  

```text
钉盘：3c88f7e51（未升）
tip：6dcea17c8 wave146 ld argv_has_obj pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：其它 rest R2 / mega residual
```
