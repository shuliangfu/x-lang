# Shux 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-22.md](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave168** ensure_freestanding_io_o pure @ `e5d51bb63` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **R2 Cap residual pure · freestanding ensure residual**（`shux_ensure_freestanding_io_o` → L7 pure） |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：freestanding ensure·path 第二叶 — `shux_ensure_freestanding_io_o` 体仍 always mega C（tables pure 后仍 snprintf join + access + cc）  
2. **根修 / pure**：真迁 `labi_freestanding_list.x` L7 pure orch（peer freestanding_enabled + pure io out_base/src_rel/stem(1) + pure byte join）  
3. **Cap residual**：`shu_resolve_compiler_dir` + `link_abi_path_readable`（access R_OK）+ `shux_cc_compile_sync` + `asm_link_obj_skip_missing`（stat）+ peer freestanding_io path/diag  
4. **未动**：其它 rest R2（常驻 Cap：contains_substr / undef_sym / getenv / resolve / path_io / mega spawn-IO …）  
5. **验收**：双端 L2 g05 + 矩阵 + string/vec/slice/compress 邻域 + freestanding_list prove IDENTICAL；钉盘仍 **`3c88f7e51`**  

## 下一波前排

1. 其它 rest R2 / mega residual（常驻 Cap 不硬迁：contains_substr / undef_sym / path_io stat / wait / strerror / mega spawn-IO / ld_debug_argv / invoke_cc_argv_push_existing realpath 池 / ensure_std_net_o_auto_tls system·make / getenv / shu_resolve_compiler_dir / 其它 `*_o_path` 静态缓冲 …）  
2. 可选：未闭合块注释硬诊断（EOF depth>0）  
3. 可选 Windows hybrid（**需要时再通知重启**）  

```text
钉盘：3c88f7e51（未升）
tip：e5d51bb63 wave168 ensure_freestanding_io_o pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：其它 rest R2 / mega residual
```
