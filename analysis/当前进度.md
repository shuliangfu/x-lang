# Shux 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-22.md](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d6648bee4`**（升自 `3c88f7e51`；双端 tip L4 + 129） |
| tip 产品代码 | **wave225** mega residual raw getenv → link_abi_getenv @ **`ea82a9879`** |
| 产品 bstrict | 钉盘 **129**；本波 **日常 L2**（非 tip L4） |
| 本波性质 | **R2 Cap residual pure** · mega G.7 getenv 收敛（MINIMAL / DEBUG_*） |
| Ubuntu | ✅ L2：g05 + 矩阵 + MINIMAL + string `-c` + diag/icc prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 + MINIMAL + string `-c` + diag/icc prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **wave225**：mega 体 residual raw getenv → **`link_abi_getenv`**（wave222 pure thin → `_impl` host getenv）。`runtime_link_abi.from_x.c`：`SHUX_MINIMAL_CC_LINK` 闸门、`SHUX_DEBUG_PIPE`（link_only）、`SHUX_DEBUG_LD`（minimal/gcc ld argv dump）。G.7 单权威 env 面；host residual 仅 **`link_abi_getenv_impl`**。
2. **闭合 soft residual**「mega 体仍 raw getenv（MINIMAL / DEBUG_*）」（wave208/223 遗留）。resolve 池 `_impl` / system `_impl` / 其它 seed（pipeline/runtime）raw getenv 仍 Cap residual。
3. **验收**：双端 L2 矩阵 rv42/option/si/hello + MINIMAL rv + string `-c`；labi_diag_pure + labi_invoke_cc_list prove **IDENTICAL**。钉盘仍 **`d6648bee4`**。

## 下一波前排

1. 常驻 Cap residual 可迁叶：resolve 池 `_impl` 体 / 可选其它 raw system 调用方（rt_entry/rt_run_exec）G.7 / 其它 seed 模块 residual raw getenv（pipeline/runtime 非 link_abi）；mega link_abi getenv @ wave225 · link_abi_system @ wave224 pure · pure 模块 env @ wave223 · link_abi_getenv @ wave222 pure
2. 可选：未闭合块注释硬诊断（EOF depth>0）
3. 可选 Windows hybrid（**需要时再通知重启**）

```text
钉盘：d6648bee4
tip：ea82a9879 wave225 mega residual raw getenv → link_abi_getenv
构建级别=L2 日常（非 tip L4）
Windows：不需要切换
下一：常驻 Cap residual（resolve 池 _impl / 其它 raw system …）
```
