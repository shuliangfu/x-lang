# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave161** thin `*_o_path` G.7 join @ `c8d709359` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **R2 Cap residual pure · path residual G.7**（薄 `*_o_path` → `compiler_o_path_copy`） |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：path residual 薄叶 — 27× `shux_runtime_*_o_path` 仍 per-leaf resolve+snprintf（wave160 已有 pure join 权威）  
2. **根修 / G.7**：全部路由 `shux_runtime_compiler_o_path_copy`（wave160 pure orch / cold twin）；删重复 resolve+snprintf  
3. **权威**：join 单权威 = pure L0；静态 `PATH_MAX` 返回缓冲仍 mega Cap residual  
4. **未动**：panic / async_scheduler / crt0_user / freestanding_io / repo_root（cwd·realpath 阶梯）  
5. **验收**：双端 L2 g05 + 矩阵 + string/vec/slice(20)/compress 邻域 + prove IDENTICAL；钉盘仍 **`3c88f7e51`**  

## 下一波前排

1. 其它 rest R2 / mega residual（可迁：freestanding ensure·path residual / repo_root / complex path ladders …；常驻 Cap 不硬迁：contains_substr / undef_sym / path_io stat / wait / strerror / mega spawn-IO / ld_debug_argv / invoke_cc_argv_push_existing realpath 池 / ensure_std_net_o_auto_tls system·make / getenv / shu_resolve_compiler_dir / `*_o_path` 静态缓冲 …）  
2. 可选：未闭合块注释硬诊断（EOF depth>0）  
3. 可选 Windows hybrid（**需要时再通知重启**）  

```text
钉盘：3c88f7e51（未升）
tip：c8d709359 wave161 thin *_o_path G.7 join
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：freestanding ensure·path residual / repo_root / 其它 rest R2
```
