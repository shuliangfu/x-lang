# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`1f73ed275`**（双端 tip L4 真冷 + 129；未升 — 本波日常 L2） |
| tip 产品代码 | **pipeline_abi wave90 soft_suppress pure** @ `ce96dc47` |
| 产品 bstrict | **129** · 钉盘 tip L4 全绿；wave90 **日常 L2** 矩阵绿（未全量 bstrict） |
| 本波性质 | **wave90 pipeline_typeck_diag_soft_suppress_{set,get} Cap residual 真迁 pure BSS** |
| Ubuntu | ✅ L2 g05 hybrid pure **W**（soft_suppress）+ 矩阵 rv42/option/si/hello + nest #if/-D + force_c @ `ce96dc47` · `/tmp/ubuntu_g05_wave90.log` · `/tmp/ubuntu_g05_wave90_matrix.log` |
| mac | ✅ L2 g05 hybrid pure soft_suppress **T** + 矩阵 rv42/option/si/hello + nest #if/-D + force_c @ `ce96dc47` · `/tmp/mac_g05_wave90.log` · `/tmp/mac_g05_wave90_matrix.log` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave90** @ 产品 **`ce96dc47`**  
   - `pipeline_typeck_diag_soft_suppress_set` / `_get`：G.7 真迁 pure BSS  
   - `g_pipe_typeck_diag_soft_suppress` i32（0/1）；set 非零→1；get 返回 0/1  
   - wave89 pure dep-prerun orch same-TU set(1)/set(0)；diagnostic thin get 共享 pure flag  
   - `pipeline_glue.c` 同名 **SHUX_WEAK** 冷 twin（独立 cold BSS）  
   - 闭合 Cap residual「soft-suppress」叶（under pure dep-prerun orch）  
   - Cap residual 仍：`load_and_sync_direct_import_deps_c`；cfg_eval 复杂；g05 &fn cast；set_dep_ctx；layout validate/patch  
   - **构建级别 L2**（日常真测；非 tip L4）  
   - 注：bootstrap 后若 seed `.o` 新于 `.x` 会跳过 hybrid，须 touch `.x` 或 rm `.o` 再 g05 确保 pure 入链  
2. 继承 tip L4 钉盘 **`1f73ed275`**（wave76–81 收口；wave82–90 未升钉）

## 下一波前排

1. rest R2：pipeline_abi **硬 Cap residual** 叶（`load_and_sync` 大 glue；cfg_eval 复杂；g05 &fn cast；set_dep_ctx / layout helpers）  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 硬 Cap / soft 再有整波收口时再 tip L4（非每微步）

```text
钉盘：1f73ed275（tip L4 双端 129；本波未升）
产品 tip：ce96dc47 pipeline_abi residual pure wave90 soft_suppress
文档：21 号归档 · 22 号 wave76–90 + tip L4 升钉 @1f73ed275
Windows：本波不需要切换
下一：pipeline_abi 硬 Cap residual（load_and_sync / cfg_eval / g05 &fn / set_dep_ctx）或整波 tip L4
```
