# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **pipeline_abi wave51 pure residual** @ `bdf150cf` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **R2 pipeline_abi Cap residual pure wave51**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + pure-asm 矩阵 rv42 / option / hello / nest / stdlib-import / overload；load_one + fail_cleanup pure **W**；Cap residual resolve_read_preprocess **T** @ `bdf150cf` |
| mac | ✅ L2 PREFER hybrid + c-backend 矩阵（Darwin pure-asm CG002 tip 既有）@ `bdf150cf` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave51** @ `bdf150cf`  
   - **迁 pure**：`shux_load_one_direct_import_at` + `shux_load_direct_fail_cleanup` orch  
   - **Cap residual**：`shux_load_one_direct_resolve_read_preprocess`（PATH_MAX + FILE view + preprocess → owned prep）  
   - **G.7**：`paths_tmp_resolve_parse_enqueue` 复用同一 Cap residual（消双份 resolve/read/preprocess）  
   - **仍 seed 重 C**：tmp_parse / strdup / FILE Cap residual；thread emit  
   - **seed**：冷孪生 `#ifndef FROM_X`；FROM_X rest 补 pure load_one / fail_cleanup **extern**  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave51.log` · `/tmp/ubuntu_g05_wave51.log` · matrix `/tmp/mac_g05_wave51_matrix.log` · `/tmp/ubuntu_g05_wave51_matrix.log`

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：tmp_parse Cap residual 再拆 / thread emit residual / 其它 rest pure  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave51 未升钉）
产品 tip：bdf150cf pipeline_abi residual pure wave51 L2
Windows：本波不需要切换
下一：tmp_parse / thread residual pure / Darwin CG002（需时再说）
```
