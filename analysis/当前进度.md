# X 语言 / X language 当前进度（2026-07-28 · wave614 Cap arm64 INDEX assign · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`eb92a18b1`**（wave614 Cap arm64 INDEX assign store width + EA@x1 · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave614（已闭 · Cap residual arm64 freestanding INDEX assign · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `m[0][1]=32; m[0][0]=10` freestanding 和=10（host-C=42）；`a[i]=32; a[0]=10` fs=10 |
| tip 产品 | **`eb92a18b1`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave614 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| INDEX assign 恒 `str x0`（8B） | seed `arch_arm64_enc_enc_store_rax_to_rbx_indirect` 忽略 `elem_sz` | ✅ 按 1/4/8 发 strb/str w/str x（≡ wave391 offset 孪） |
| 变下标 EA 落 x0 被 pop 盖 | `enc_rbx_plus_x2_scale*` 为 `add x0,x1,x2`；finish_store pop→x0 后 `str [x1]` 写 base | ✅ `add x1,x1,x2,lsl #n`（≡ x86 lea rbx / riscv a1） |

**数据流（五问）**：产生 = INDEX assign finish_store 编码；存储 = 帧邻槽；消费 = 后续 INDEX load。glue 已传 esz，权威半残在 arm64 product seed。G.7 **补全** `store_rax_to_rbx_indirect` + `rbx_plus_x2_scale*` 与 x86/offset 极性一致；`arch/arm64_enc.x` 同 commit。

**权威**：`compiler/seeds/backend_arm64_enc_c.from_x.c` + `compiler/src/asm/arch/arm64_enc.x`。改后 rm `src/asm/backend_arm64_enc_c.o` + g05。

### wave614 验证

| 探针 | mac L2 host/fs | Ubuntu L2 host/fs |
|------|----------------|-------------------|
| asg_ord `m[0][1]=32; m[0][0]=10` | ✅ 42（was fs 10） | ✅ 42 |
| asg_1dv `a[i]=32; a[0]=10` | ✅ 42（was fs 10） | ✅ 42 |
| asg_var / asg_vv 多维变下标 | ✅ 42 | ✅ 42 |
| asg_u8 邻元 1B store | ✅ 66 | ✅ 66 |
| n_dual wave613 回归 | ✅ 42 | ✅ 42 |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/0/0 |

disasm（mac）：assign 用 `str w0,[x1]`；变下标 `add x1,x1,x2,lsl #2`。

### 仍 soft

- 其它 Cap residual 叶；整波双端 **L4** + 升钉  
- 绝对 `XLANG` freestanding crt0 双前缀（ensure 路径，非本波）  
- host-C multi-dim field call-arg `take(W{}.xs)` 偶发 SIGBUS（非本波 arm64 pure-asm）

### 下一波

1. Cap residual soft 其它叶，或整波 L4 + 升钉  

## wave613（已闭 · Cap residual freestanding multi-dim nested ARRAY_LIT · L2）

tip 产品 **`82a1c1e1f`** / docs **`c3f870e1e`**。

## wave612（已闭 · Cap residual freestanding dual ARRAY_LIT INDEX binop rbx · L2）

tip 产品 **`05dd5df6f`** / docs **`dc4ac53e4`**。
