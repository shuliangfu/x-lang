# X 语言 / X language 当前进度（2026-07-27 · wave533 障碍记录 + L4 收口待启动）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`9ccc811ef`**（双端 L2，wave532；L4 待整波收口） |
| 钉盘 | **`d5de2f13b`**（升自 `2bc0885d3`；双端 L4 129 全绿） |
| bstrict | **129** |

## wave533（未闭 · runtime_lsp_glue 迁移障碍记录 — 需后续波次处理）

### 模块概况
- `runtime_lsp_glue`（产物 `src/lsp/lsp_diag.o`；1761 行 .x + 3527 行 .from_x.c；60 `#[no_mangle]` + 37 `extern "C"` + 79 `_impl` 引用）

### 障碍 1：44 个函数未守卫
- .from_x.c 中 44 个函数在 .x 中已有 `#[no_mangle]` 实现，但未用 `#ifndef XLANG_L2_LSP_FMT_THIN_FROM_X` 守卫排除
- 导致 R2 thin+rest 合并时 44 个重复符号（macOS `ld -r` 不允许重复定义）

### 障碍 2：31 个 `_impl` 重命名缺失
- .x thin wrapper 调用 `_impl` 后缀函数（如 `lsp_free_import_cache_impl`）
- 但 .from_x.c 中函数名是原名（如 `lsp_free_import_cache`），**未重命名为 `_impl`**
- 导致 thin U 符号 `_impl` 在 rest 中找不到 T 定义

### 障碍 3：g05 双 thin 源
- g05_ensure_relink_prereqs.sh 用 `src/lsp/lsp_fmt_pure_thin.x`（17 个 `#[no_mangle]`）作 thin 源
- 完整 .x 在 `src/asm/runtime_lsp_glue.x`（60 个 `#[no_mangle]`）
- 两个 .x 文件部分函数重叠，存在双权威风险

### 正确迁移路径（未来 wave）
1. 在 .from_x.c 中把 31 个函数重命名为 `_impl` 后缀（与 .x thin wrapper 的 `extern "C"` 声明对齐）
2. 给 44 个函数加 `#ifndef XLANG_L2_LSP_FMT_THIN_FROM_X` 守卫排除（R2 模式下由 .x 提供）
3. 修改 g05 脚本统一用 `runtime_lsp_glue.x` 作 thin 源（消除双权威）
4. 修改 Makefile 加 R2 thin+rest 规则
5. 双端 L2 验证

### 撤销记录
- 所有修改已撤销（.from_x.c + Makefile + g05 脚本），工作区干净
- 无回归风险

## wave528-532 累积（4 个 R2 migration · 待 L4 收口）

| 波次 | 模块 | 模式 | 双端 L2 |
|------|------|------|---------|
| wave529 | runtime_http_glue | thin+rest | ✅ |
| wave530 | runtime_slice_glue | DIRECT | ✅ |
| wave531 | runtime_path_fast | DIRECT | ✅ |
| wave532 | runtime_std_runtime_fast | DIRECT | ✅ |

### 下一步：L4 真冷全测双端 129 bstrict
- 按自举方法 §0.2「整波结束做一次扎实 L4」+ 日终兜底（已累积 4 波 R2 migration，动过 runtime 产品面）
- 流程：全擦 `compiler|std|core` 全部 `.o` + 产品二进制 → `remaining_o=0` → `bootstrap-driver-seed` → g05 relink → 产品矩阵 → `XLANG_BSTRICT_SKIP_BUILD=1 run-all-bstrict.sh`
- 双端（macOS + Ubuntu）129 全绿 → 升钉自 `d5de2f13b`
