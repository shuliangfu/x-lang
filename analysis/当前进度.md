# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **pipeline_abi wave52 pure residual** @ `f4b3b34e` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **R2 pipeline_abi Cap residual pure wave52**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + pure-asm 矩阵 rv42 / option / hello / import / stdlib-import / overload；tmp_parse pure **W**；Cap residual paths_tmp / strdup / resolve_read_preprocess **T** @ `f4b3b34e` |
| mac | ✅ L2 PREFER hybrid + c-backend 矩阵（Darwin pure-asm CG002 tip 既有）@ `f4b3b34e` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave52** @ `f4b3b34e`  
   - **迁 pure**：`shux_collect_tmp_parse_and_enqueue` orch（malloc/memset ensure + parse_into_bytes + G.7 pure enqueue）  
   - **G.7**：paths_tmp Cap residual / deps_process_one 复用 pure tmp_parse  
   - **仍 seed 重 C**：strdup / resolve_read_preprocess / paths_tmp shell / thread emit  
   - **seed**：冷孪生 `#ifndef FROM_X`；FROM_X rest 补 pure tmp_parse **extern**  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave52.log` · `/tmp/ubuntu_g05_wave52.log` · matrix `/tmp/mac_g05_wave52_matrix.log` · `/tmp/ubuntu_g05_wave52_matrix.log`

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：paths_tmp Cap residual 再拆 / thread emit residual / strdup 仍 seed  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave52 未升钉）
产品 tip：f4b3b34e pipeline_abi residual pure wave52 L2
Windows：本波不需要切换
下一：paths_tmp / thread residual pure / Darwin CG002（需时再说）
```
