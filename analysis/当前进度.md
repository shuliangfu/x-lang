# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[自举进度-归档-2026-07-17.md](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **本波 vector lane CTFE 收口后 tip**（钉盘仍 **`c51759eb`**） |
| 产品 L4 放行 | ✅ **钉盘 `c51759eb`**（不升钉） |
| tip 双端 bstrict | ✅ **123/123 @ `c51759eb`**（钉盘） |
| **typeck CTFE fold** | ✅ 主路径 + **scalar CALL** + **vector lane-of-binop CALL** |
| **LANG-006 residual** | ✅ 已闭（BITNOT + 逻辑 const） |
| **WPO call-site fold** | ✅ **scalar + vector lane 均 pre-emit** |

## 本波摘要（2026-07-18 · vector lane WPO → typeck CTFE）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 vector lane-of-binop 同步 pre-emit |
| **地图** | ① typeck INDEX 拒 `TYPE_NAMED` `i32x4` 下标 → T001 挡 `lane0`；② emit `try_inline_wpo_const_vector_lane_of_binop_call_elf` 仍是 fold 权威，应迁 typeck CTFE |
| **根修** | G.7：`typeck_check_expr_index` 认 NAMED SIMD 拼写 + `typeck_vector_elem_type_ref`；`glue_typeck_fold_expr_ref` 补 `laneK(vec_binop([const…],[const…]))` stamp `const_folded_*`；MONO/NO_FOLD 跳过；seed typeck_gen 同 commit |
| **验收** | mac L2：`-E vec_const_spec_fold` → `main { return 0; }`；`SHUX_WPO_MONO=1` 保留 `lane0(vec_add4…)`；run=0；lang-const **12/12**；rv=42 · hello；scalar fold 不回归 |
| **残差** | 下一产品债另开地图；（旁）Darwin pure-asm CG002 / callgraph |

### residual 下一波

1. 下一产品债：另开地图再动（禁无地图 tip L4）  

```text
构建级别：L2（typeck_x + pipeline_x FORCE + filtered + g05；非 tip L4）
产品 tip L4 钉盘：c51759eb
权威：typeck_check_expr_index + glue_typeck_fold_expr_ref lane path
```

---

## 前波摘要（2026-07-18 · WPO scalar call-site fold → typeck CTFE）

| 波 | 结果 |
|----|------|
| **根修** | `glue_typeck_fold_expr_ref` scalar `f(c0,c1)`；MONO/NO_FOLD 门控 |
| **验收** | `-E const_spec_fold` → `return 0`；lang-const 12/12 |
| **残差** | vector lane → **本波已迁 pre-emit** |

## 前波摘要（2026-07-18 · LANG-006 residual：`~` + `&&`/`||`）

| 波 | 结果 |
|----|------|
| **根修** | unary `TOKEN_TILDE`→`EXPR_BITNOT`；bool→int init coerce |
| **验收** | lang-const **12/12**；矩阵绿 |
| **残差** | WPO call-site → **本波已迁 pre-emit** |

## 前波摘要（2026-07-18 · typeck CTFE 常量折叠权威恢复）

| 波 | 结果 |
|----|------|
| **根修** | typeck glue 恢复 fold 生产者 + typeck.x 挂钩 + C codegen 消费 folded |
