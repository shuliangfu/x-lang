# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`1f73ed275`**（双端 tip L4 真冷 + 129；未升 — 本波日常 L2） |
| tip 产品代码 | **pipeline_abi wave89 typeck_dep_prerun pure** @ `293bc6f9a` |
| 产品 bstrict | **129** · 钉盘 tip L4 全绿；wave89 **日常 L2** 矩阵绿（未全量 bstrict） |
| 本波性质 | **wave89 pipeline_typeck_dep_prerun_module_c Cap residual 真迁 pure** |
| Ubuntu | ✅ L2 g05 hybrid pure **W**（dep_prerun）+ 矩阵 rv42/option/si/hello + nest #if/-D + force_c @ `293bc6f9a` · `/tmp/ubuntu_g05_wave89.log` |
| mac | ✅ L2 g05 hybrid pure dep_prerun + 矩阵 rv42/option/si/hello + nest #if/-D + force_c rv42 @ `293bc6f9a` · `/tmp/mac_g05_wave89.log` · `/tmp/mac_g05_wave89_matrix.log` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave89** @ 产品 **`293bc6f9a`**  
   - `pipeline_typeck_dep_prerun_module_c`：G.7 真迁 pure orch  
   - null → -5；`pipeline_typeck_set_dep_ctx` + soft_suppress(1)  
   - `typeck_x_ast_library`（G.7 typeck.x 权威，同 wave87 library 路由）  
   - soft_suppress(0)；成功 → 0；失败 → Cap residual layout validate（-7）+ patch parent links → 0  
   - `pipeline_glue.c` 同名 **SHUX_WEAK** 冷 twin（含 SHUX_DEBUG_PIPE）；strict_stubs weak -1 注释对齐  
   - 闭合 Cap residual「typeck dep-prerun」叶（wave60 typeck_only 同 TU pure）  
   - Cap residual 仍：`load_and_sync_direct_import_deps_c`；cfg_eval 复杂 #if；g05 &fn cast；set_dep_ctx / soft_suppress / layout helpers  
   - **构建级别 L2**（日常真测；非 tip L4）  
2. 继承 tip L4 钉盘 **`1f73ed275`**（wave76–81 收口；wave82–89 未升钉）

## 下一波前排

1. rest R2：pipeline_abi **硬 Cap residual** 叶（`load_and_sync` 大 glue；cfg_eval 复杂；g05 &fn cast）  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 硬 Cap / soft 再有整波收口时再 tip L4（非每微步）

```text
钉盘：1f73ed275（tip L4 双端 129；本波未升）
产品 tip：293bc6f9a pipeline_abi residual pure wave89 typeck_dep_prerun
文档：21 号归档 · 22 号 wave76–89 + tip L4 升钉 @1f73ed275
Windows：本波不需要切换
下一：pipeline_abi 硬 Cap residual（load_and_sync / cfg_eval / g05 &fn）或整波 tip L4
```
