# X 语言 / X language 当前进度（2026-07-28 · wave580 Cap long-name pure-asm · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`9da8b5f7e`**（wave580 Cap long-name pure-asm · L2 双端绿 · 未 L4） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave580（已闭 · Cap long-name 默认 asm · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave579 后 long75 默认 asm residual（`-backend c` 已 42） |
| tip | **`9da8b5f7e`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave580 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| CALL clen>63 / out_cap=64 | `backend_call_dispatch` seed：`clen>63` 拒；`glue_asm_build_call_export_sym_c(..., 64)` 且 `clen<out_cap` → 最长 63 | ✅ clen≤127 · out_cap=128 |
| export 标签截断 | mega_body `glue_asm_build_func_export_sym_c(..., 64)` + `build_dep_export` `pos<out_cap-1` | ✅ out_cap=128 · 满 cap 拷贝 |
| ELF 槽仍 64 拷贝 | `pipeline_elf_ctx_add_{label,sym}` / patch / reloc | ✅ 128 字节行；link 名可至 128（`_`+127） |
| Darwin `_` 双权威 | 硬编码 offsetof **598052**（Cap 前）；setter 写错位，API 读真字段 | ✅ `pipeline_elf_ctx_macho_leading_underscore` + 结构体字段写 |

**数据流（五问）**：产生点 = Cap 后 name 槽 [128] 未贯通 pure-asm 出口与 Darwin 下划线权威；存储 = ElfCodegenCtx 表 + export/call 符号缓冲；消费 = enc_label / enc_call / macho_write。

**操作纪律**：改 `ast_pool.c` ELF 名 cap 后须重建 **`pipeline_x.o` + `pipeline_glue_standalone.o`**（Darwin `-multiply_defined suppress` 时 **先定义的 pipeline_x 胜出**，只刷 standalone 不够）。

### wave580 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| fn 名 63..127 | ✅ 42 | ✅ 42 |
| long75 默认 asm | ✅ 42 | ✅ 42 |
| rv / option / si / hello | ✅ | ✅ Hello World + `0a` |

### 仍 soft

- 超长 **let 局部名** 75/127 在 mac 仍偶发 BLD001（`_main` UNDEF）；let 63/64 绿 — 非本波主债  
- residual：ast_pool 其它 `>64` 侧门（top-level let register 等）  
- 整波双端 **L4** + 升钉  

### 下一波

1. let 长名 residual（若产品阻塞）或 residual `>64` 侧门扫尾  
2. 整波双端 L4 + 升钉  

## wave579（已闭 · Cap DepCtx 布局 · hello SEGV）

见上波；tip `5cc9417d1` / docs `231940da3`。
