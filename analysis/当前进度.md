# X 语言 / X language 当前进度（2026-07-24）

> **长期仪表盘**：[自举进度.md](自举进度.md)（24 日起重新起表）  
> **23 号全日归档**：[自举进度-归档-2026-07-23.md](自举进度-归档-2026-07-23.md)  
> **重命名分析**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)  
> **项目目录**：现行 `…/xlang/x-lang`  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| 工程 | **双端 tip L4 129 全绿** · 钉盘 **`605e7efa4`** · tip 日常 L2 wave394 |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **已恢复** · Cap residual 日常 L2（钉盘仍 L4 `605e7efa4`） |

## 验收

### tip L4 钉盘（**已升 · wave383 收口**）

| 项 | 状态 |
|----|------|
| SHA | **`605e7efa4`**（wave383：redef 全签名 + return `;` + labeled final pin + crypto 空桩外置） |
| 构建级别 | **L4 真冷**（全擦 compiler/std/core `.o` + 重链产品二进制） |
| remaining_o 起编 | ✅ **0**（双端） |
| 产品矩阵 | ✅ rv=42 · option · hello · overload · goto · crypto · parser |
| mac 全量 bstrict | ✅ **OK (129)** · `/tmp/mac_true_cold_605e7efa4.log` · `/tmp/mac_true_bstrict_605e7efa4.log` |
| Ubuntu 全量 bstrict | ✅ **OK (129)** · `/tmp/ubuntu_true_cold_605e7efa4.log` · `/tmp/ubuntu_true_bstrict_605e7efa4.log` |
| 前钉 | `ace59b6cd`（wave363 NAMED 64B；其后 wave364–382 L2 堆债 → 本波 tip L4 升钉） |

### 本波根修（升钉内容 · wave383）

| SHA | 摘要 |
|-----|------|
| `42be7ded3` | redef first-wins 比 param type_refs（保真 overload；types gate `pick_i64`） |
| `1ca3d7c04` | onefunc `return` 无 `;` 不得 Cap-T001 吞后续（semicolon_missing） |
| `4e6d46b39` | `L: return e;` 紧邻 `}` 时 pin final_expr（Linux x86_64 asm goto→42） |
| **`605e7efa4`** | **core.x 删空 `crypto_sha256_c` 桩**（G.7 权威 = runtime_crypto_inc_glue；双端 crypto 绿） |

### wave383（已闭 · tip L4 升钉）

| 项 | 状态 |
|----|------|
| SHA | **`605e7efa4`** |
| 债 | tip L4 收口：wave377 redef 误伤 overload；wave379 Cap-T001 吞缺分号；asm labeled return；crypto 双强/空桩 |
| 构建级别 | **L4 真冷双端 129** |
| 钉盘 | **已升** 自 `ace59b6cd` |

### wave390（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`446cbf66b`** |
| 债 | **`impl Trait for Struct` 方法 UFCS 红**：`skip_one_impl` 整块跳过 IDENT for-type 体（struct 丢方法）；类型关键字 for-type 中途失败仅 i32/u64 方法「漏」成顶层 free（假绿） |
| 根修 | G.7 `skip_one_impl`：只消 header（for-type 接受 type keyword / `*T`）；**不** `skip_balanced_braces`，body 方法由外层 `parse_into` 当 free 登记 → wave358 UFCS；seed 路径另有显式 hoist fallback |
| 构建级别 | **L2**（parser_asm_thin_glue.o + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | 双端 Box.get=42 host+asm · multi sum+prod=55 · `*self` bump=42 · i32 impl body=121 · run-trait OK · 矩阵 rv42/option/hello（`/tmp/mac_g05_wave390*.log` · `/tmp/ubuntu_g05_wave390.log` · `/tmp/ubuntu_w390_probes.log`） |
| soft | untyped-let（docs 禁推断）；trait 更深（bounds/dyn/缺方法诊断）；static/COMMON reentrancy last-wins |

```text
下一：wave395 Cap residual（日常 L2）
  leave-off：untyped-let（docs 禁）· trait 深债 · host-C static __xlang_sp last-wins
  钉盘 605e7efa4；约每 10–15 波或日终 tip L4
  纪律：改 seed parser_gen/codegen_gen 必须同 commit 同步链上 pin
```

### wave394（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`e2ff0b1fa`** |
| 债 | **arm64 freestanding 多 INDEX 求和 / slice* call-arg**：dual-GP length@home-8 在 `[x29+off]` 极性下盖住定长数组元（s[2]=4、sum=34）且 fat*+8 读空（len_of=0）。Ubuntu x86 `[rbp-off]` 绿 |
| 根修 | G.7 `glue_slice_dual_gp_length_off_c` / `bump_past_home`：arm64 home+8 · x86 home-8 → 内存恒 C fat；TYPE_SLICE dual-GP 全路径；slot fill 预留 home+8 |
| 构建级别 | **L2**（`pipeline_glue_standalone.o` + `pipeline_x.o` + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | 双端 asm+host length=4 · len_of=4 · s[2]=30 · sum4=100 · bind=100 · 矩阵 rv42/option/hello（`/tmp/mac_g05_wave394b.log` · `/tmp/ubuntu_g05_wave394.log` · `/tmp/ubuntu_w394_probes.log`） |
| soft | untyped-let；trait 深债；`__xlang_sp` last-wins |

### wave393（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`f2a9f29ad`** |
| 债 | **freestanding 本地 `let s: T[] = a` 后 `s.length`=0**（host-C `{.length=N}` 绿）。根因：dual-GP home 存 data@off / length@off-8，FIELD_ACCESS 恒 lea+C layout (+8) → 读 junk |
| 根修 | G.7 `pipeline_asm_emit_var_field_access_elf_c`：by-value dual-GP 直接 load length@off-8 / data@off；slice* 形参仍 fat*+8（与 INDEX bounds 同权威） |
| 构建级别 | **L2**（`pipeline_glue_standalone.o` + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | 双端 asm+host length=4 · +38=42 · lit length=3 · s[0]+s[1]=30 · slice main=20 · 矩阵 rv42/option/hello（`/tmp/mac_g05_wave393.log` · `/tmp/ubuntu_g05_wave393.log`） |
| soft | untyped-let；trait 深债；`__xlang_sp` last-wins；slice* call-arg / 多 INDEX（wave394 已闭） |

### wave392（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`75a605a2a`** |
| 债 | **arm64 多结构体实参 reentrancy**：① 产品 `store_x_reg_to_rbp` 签名只收 offset、恒 STR x0，dispatch 传 (reg,offset) → 形参 home 两次 `str x0,[x29]` ② AAPCS64 实参直落 xN 无 spill，嵌套 CALL 毁掉已就位寄存器；load 须高→低（x0 兼 temp 与 arg0） |
| 根修 | G.7：`store_x_reg(reg,offset)` 正 imm STR；`emit_call_args` ta==1 spill-then-load + high→low reload；spill helper 接受 arm64 |
| 构建级别 | **L2**（backend_arm64_enc_c + backend_call_dispatch + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | mac re2/re3/re1=42 · sumf=42 · 矩阵 rv42/option/hello（`/tmp/mac_g05_wave392c.log`）；Ubuntu re2/re3=42 · 矩阵绿（`/tmp/ubuntu_g05_wave392.log`） |
| soft | untyped-let（docs 禁推断）；trait 深债；host-C static `__xlang_sp` last-wins |

### wave391（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`40b073d81`**（docs chore `8f0cb679a`） |
| 债 | **macOS arm64 multi-field STRUCT_LIT 字段覆盖**：`arch_arm64_enc_enc_store_rax_to_rbx_offset` 只收 `offset`、恒 STR X 且 imm=`offset/8` → i32 foff=4 成 imm12=0 → 后字段写回 `[x1+0]`（`Pt{x:20,y:22}` → `p.x+p.y=22`；host-C 绿） |
| 根修 | G.7 产品 seed 补全 `store_size`：1=STRB / 2=STRH / 4=STR W / 8=STR X（scale 与 x86 路径一致）；twin `arm64_enc.x` STRH+负 offset clamp；dispatch 已传 size |
| 构建级别 | **L2**（`src/asm/backend_arm64_enc_c.o` + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | mac suml/three/sumf=42 · 矩阵 rv42/option/hello（`/tmp/mac_g05_wave391c.log`）；Ubuntu suml/three=42 · 矩阵绿（`/tmp/ubuntu_g05_wave391.log`） |
| soft | untyped-let（docs 禁推断）；trait 深债；reentrancy last-wins |

### wave389（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`69ec159a2`** |
| 债 | **Linux freestanding `runtime_panic_x86_64.s` 整数 has_msg=1 不打印**：wave386 仅闭 cstr（has_msg=2）；`cmpl $2` 失败直接 exit(134) |
| 根修 | G.7 权威 `.s`：`has_msg==1` 写 `panic: ` + 有符号十进制 itoa + NL（`write(2)`）；0 bare / 2 cstr 不变；`run-panic` 验收 `42`；docs/03 |
| 构建级别 | **L2**（runtime_panic.o + g05；Linux 金标重编 `.s`） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | mac host-C/asm `panic: 42`/`boom` · run-panic OK · 矩阵 rv42/option/hello（`/tmp/mac_g05_wave389.log`）；Ubuntu L2 见日志 |
| soft | untyped-let；trait（wave390 已闭 struct UFCS）；reentrancy last-wins |

### wave388（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`0626ee5b5`** |
| 债 | **macOS arm64 asm var–var 比较**（`!=`/`>`/`<`/`>=`/`<=`）：Ubuntu x86 已绿；mac 字面量路径常折叠假绿，var-var 一律 `cset eq` |
| 根修 | G.7 产品权威 `seeds/backend_arm64_enc_c.from_x.c`：`enc_cmp_setcc_movzbl` 经 `pipeline_asm_arm64_cset_cond_enc_from_cc` 真设 cond；`enc_cmp_rbx_rax` → `cmp x1,x0`（left−right）；`enc_cmp_rax_rbx` 对调；`arm64_enc.x` setcc-only 对齐 |
| 构建级别 | **L2**（backend_arm64_enc_c + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | mac var-var ==/!=/</>/<=/>= 矩阵全 OK（含负 i32）；disasm `cmp x1,x0` + `cset ne`；rv42/option/hello/goto=42（`/tmp/mac_g05_wave388.log`）；Ubuntu x86 本叶不改路径（先前已绿） |
| soft | untyped-let；trait；reentrancy；Linux .s int panic（wave389 已闭） |

### wave387（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`0933ffc2a`** |
| 债 | **默认 asm / freestanding `goto`/`L:`**：wave379 仅闭 host-C kind=7；`pipeline_asm_emit_block_body_sync_elf` 跳过 labeled → 产品 `-backend asm`（默认）落到首个 return（goto_ec 0/1） |
| 根修 | G.7：`item_kind==7` 发射 labeled 池 — `goto`→`backend_enc_jmp_arch`；`L:`→`backend_enc_label_arch`；`L: return e`→emit 操作数 + jmp tail_join；kind=7 计入 cfg/hoist 屏障与 has_return 扫描 |
| 构建级别 | **L2**（pipeline_x + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | 双端 asm bare/mid/if-eq/label-loop=42 · Ubuntu fs=42 · host-C=42 · 矩阵 rv42/option/hello（`/tmp/mac_g05_wave387.log` · `/tmp/ubuntu_g05_wave387.log` · `/tmp/ubuntu_w387_probes.log`） |
| soft | untyped-let；trait；reentrancy；Linux .s int panic；asm `i32 !=`（wave388 已闭 mac arm64） |

### wave386（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`4b56b1b35`**（+ `9b7bd2a87` .s 前缀） |
| 债 | **panic cstr 完整打印**：wave384 后 `has_msg=1` + `(int)(intptr_t)` 截断；runtime 仅 int evidence，不打印字符串 |
| 根修 | G.7 ABI `xlang_panic_(int has_msg, intptr_t)`：0 bare / 1 int / 2 cstr；host codegen + seed；freestanding `pipeline_asm_emit_panic_elf_c` 同分类；runtime C/arm64/.s 打印 `panic: <msg>` 后 abort/exit |
| 构建级别 | **L2**（codegen_x + pipeline_x + runtime_panic + rt_preamble + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | 双端 asm+host-C `panic: hello-panic-msg` · int/bare abort · run-panic boom · 矩阵 rv42/option/hello |
| soft | untyped-let（docs 禁推断）；trait 深债；static/COMMON reentrancy last-wins 细节；Linux .s 整数 has_msg=1 不打印（仅 cstr）；asm kind=7 深边（wave387 已闭） |

### wave385（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`52906bdf4`** |
| 债 | **`let mut name: T`** 解析硬红：`mut` 为 plain IDENT 被当成绑定名 → XP003 out_len=0 |
| 根修 | G.7：`parse_body_lets_into` 在 `let` 后可选跳过 span=`"mut"` 的 IDENT；顶层 `parser_asm_top_level_let_slice` 同形；**不**接受 `const mut`。docs/06：let 默认可再赋值，`mut` 仅拼写标记 |
| 构建级别 | **L2**（parser_x + parser_asm_thin_glue + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | 双端 mut body/for-init/top=42 · plain let 回归 · const mut 仍红；矩阵 rv42/option/hello |
| soft | untyped-let；trait；panic cstr（wave386 已闭）；reentrancy last-wins |

### wave384（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`9060ec005`** |
| 债 | **`panic(string)` / `panic(*u8)` host-C BLD001**：`xlang_panic_(int,int)` 第二参被直接发射指针 → `-Wint-conversion` |
| 根修 | G.7 权威 host EXPR_PANIC emit：非空 msg 统一包 **`(int)(intptr_t)(…)`**；seed `codegen_gen` + pin 同 commit；docs/03；`tests/panic/with_str.x` + run-panic |
| 构建级别 | **L2**（codegen_x + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | mac panic str/int/bare abort=134 · *u8 panic · 矩阵 rv42/option/hello · run-panic OK；Ubuntu L2 见日志 |
| soft | untyped-let（docs 禁推断）；static/COMMON reentrancy；`mut`（wave385 已闭拼写）；trait 深债；panic cstr **完整打印**（现 int evidence）；asm kind=7 完整发射 |

### wave382（已闭 · 日常 L2 · 随 383 进 tip L4）

| 项 | 状态 |
|----|------|
| SHA | **`53f283f24`** |
| 债 | **长度全写**：字段 **`length` / `.length`**；API **`mod.length(...)`**；禁成员 `.len` |
| 根修 | G.7 单一命名：std/core/compiler `.x` + seed + `fs_abi.h` + docs/02 |
| 构建级别 | 当时 **L2**；现含于 tip L4 **`605e7efa4`** |
| 钉盘 | 已随 383 升至 **`605e7efa4`** |

### wave381（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`85cfbf7e8`**（revert `f76c68af2` + 主 revert；docs） |
| 债 | **撤销 wave380 切片/数组 `.len` 别名**：内置成员只允许 **`.length`** |
| 根修 | 回退 typeck/glue/codegen `len`→`length` 别名；docs/02 禁简写 |
| 构建级别 | **L2** |
| 钉盘 | 仍 **`ace59b6cd`** |
| 备注 | wave382 起库字段也统一 `length`（非仅 slice 内置） |

### wave380（已撤 · 曾日常 L2）

| 项 | 状态 |
|----|------|
| SHA | 曾 **`a3b1b7416`** / **`96c136e22`**；**wave381 整波 revert** |
| 债 | 曾把 `.len` 当 `.length` 别名；**产品决策改为禁止简写** |
| 备注 | 历史提交保留；代码 tip 已回退别名路径 |

### wave379（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`3081f5dc6`** |
| 债 | **真 bare `goto T;` / `L:`**：docs/03；仅 `L: return` 假绿；OneFunc 丢 labeled、无 stmt_order、host 不 emit |
| 根修 | G.7：OneFunc labeled 池 + fill；stmt_order **kind=7**；`goto`/`L:`/`L: return`；onefunc mid-body return 仅当后随 label（保 Cap-T001）；host-C emit `goto`/`L:`/`return`。parser.x+seed+pin · codegen.x+seed · ast_pool |
| 构建级别 | **L2**（pipeline + parser_x + codegen_x + g05） |
| 钉盘 | 仍 **`ace59b6cd`** |
| 探针 | mac goto_bare/if/mid/label_return=42 · void_fn/plain/cfor · 矩阵 rv/option/hello；Ubuntu L2 见日志 |
| soft | freestanding asm goto（本机 default 已绿）；untyped-let（docs 禁）；static/COMMON reentrancy；`mut`；trait |

### wave378（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`62ab0a6dd`** |
| 债 | **onefunc bare `return;`（void return）**：嵌套 `parse_block` 已接受空操作数；onefunc 最终 return 路径强制要表达式 → `return;` XP003 / void 函数丢失 |
| 根修 | G.7：TOKEN_RETURN 后 SEMICOLON/RBRACE 早分支，分配 bare `EXPR_RETURN`（null unary）；typeck 拒非 void；codegen 已有 `return;` / void main `return 0`。parser.x + seed + pin 同 commit |
| 构建级别 | **L2**（parser_x + g05） |
| 钉盘 | 仍 **`ace59b6cd`** |
| 探针 | 双端 void_fn/void_ret_mid=42 · empty_ret_i32 typeck 红 · reent=1 · block_let/cfor/alias 回归；矩阵 rv/option/hello |
| soft | 真 bare goto/label（wave379 已闭 host）；untyped-let；reentrancy；`mut`；trait |

### wave377（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`634d6789a`** |
| 债 | **同模块同名同 arity 重定义**：parse/typeck 绿，call resolve **first-wins**；host-C **双 strong body** → BLD001 redefinition |
| 根修 | G.7 权威 `codegen_should_skip_later_same_name_body`：有更早的非 extern 同名+同 arity body 则跳过后续 body（对齐 call resolve first-wins）。codegen.x + seed codegen_gen 同 commit |
| 构建级别 | **L2**（codegen_x + g05） |
| 钉盘 | 仍 **`ace59b6cd`** |
| 探针 | 双端 reent run=1 · reent2=8 · ovl 不同 arity=42 · alias/block_let/cfor 回归；矩阵 rv/option/hello |
| soft | 真 bare goto/label（多层）；untyped-let（docs 禁）；static/COMMON reentrancy last-wins；`mut`；trait；`(): Alias` 已绿 |

### wave376（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`56324d4c2`** |
| 债 | **`type Alias = T`**：`return` 别名变量 → expected i32 found Coord；host-C 发 `struct ast_Coord` 不完整类型 BLD001（`tests/typeck/type_alias.x`） |
| 根修 | 双权威：glue 权威 `type_refs_equal_c` 会 peel 别名，但产品链 **localize** 后 **weak** `strict_minimal` 实现胜出且**无 peel**。G.7：weak equal 补 `resolve_type_alias`；`emit_type` 发射前 peel 底层类型。权威：strict_minimal seed+.x + codegen.x + codegen_gen seed 同 commit |
| 构建级别 | **L2**（strict_minimal + codegen_x + g05） |
| 钉盘 | 仍 **`ace59b6cd`** |
| 探针 | 双端 alias_ret/param=42 · type_alias.x=0 · block_let=42 · match_reg=42 · cfor_let=45；矩阵 rv/option/hello |
| soft | 返回类型位 `(): Alias`（ec=-4，wave376 后已绿）；真 goto/label；untyped-let；reentrancy；`mut`；trait |

### wave375（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`ce79cc32f`** |
| 债 | **`let` 初值裸 `{ … }`**：`let x: i32 = { 42 }` / 匿名 `let p: T = { f: e }` 产品链 XP003 out_len=0 |
| 根修 | 与 wave373 同模式双权威：`parser.x` 已有 LBRACE let-init（→`parse_expr` 消歧 block vs struct lit），**seed** `parser_gen.linux.x86_64.c` 的 `parse_body_lets_into` 从 IDENT 直接跳 STRING，缺 TOKEN_LBRACE=84。G.7：seed 补 IDENT 同形 LBRACE 分支 + 链上 pin 同 commit |
| 构建级别 | **L2**（parser_x + g05） |
| 钉盘 | 仍 **`ace59b6cd`** |
| 探针 | 双端 block_let=42 · block_let2=42 · struct_anon=42 · match_reg=42 · cfor_let=45；矩阵 rv/option/hello |
| soft | untyped-let（docs 禁推断）；reentrancy；`mut`；trait；**真** label/goto（tests/goto 仅 `label: return` 假绿） |

### wave374（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`102ca5f6d`** |
| 债 | **match 块臂 `=> { return N; }` final / `return match`**：host C 发 `return (?:(({ return N; })):…)` → void→int32 BLD001 |
| 根修 | G.7 扩 `codegen_match_has_return_arm`：块内 explicit return（非值 final_expr）；`emit_block_final_expr` + `emit_return_stmt_with_context` 对 return-control match 走 `codegen_emit_match_as_stmt`；stmt 臂体 `EXPR_BLOCK`→`emit_block`。权威：codegen.x + seed codegen_gen 同 commit |
| 构建级别 | **L2**（codegen_x + g05） |
| 钉盘 | 仍 **`ace59b6cd`** |
| 探针 | 双端 final/mid/return-match 块 return=42 · 值块=42 · plain return 回归；矩阵 rv/option/hello；tests/match |
| soft | untyped-let；reentrancy；`mut`；trait；label/goto |

### wave373（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`3a1149a3c`** |
| 债 | **docs/03 C 风格 for-init `let`**：`for (let i: i32 = 0; …)` 产品 `xlang_asm` 仍 XP003 out_len=0 |
| 根修 | wave347 仅写了 `parser.x` + **seed** `parser_gen.linux.x86_64.c`，**未同步** 链上 `parser_gen.c`（`parser_x.o` 源）→ 双权威漂移。G.7：产品 pin 自 seed 恢复 for-init（`parse_body_lets` hoist + stmt kind=1；seed 已含 wave371 match） |
| 构建级别 | **L2**（parser_x + g05） |
| 钉盘 | 仍 **`ace59b6cd`** |
| 探针 | 双端 host+fs cfor_let=45 · body=21 · nest=12 · asg=45 · match_reg=42；矩阵 rv/option/hello |
| soft | untyped-let（docs 禁推断）；reentrancy last-wins；`mut` 关键字；trait 深债；label/goto |

### wave372（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`8ecd413b4`** |
| 债 | **match 臂 `=> return` 中置后仍有可达语句**：host 三元 unwrap 成 `(void)((v==1?(42):0)); return 7` → 非真 early-return（exit 7≠42）；fs 同 mid-body fall-through |
| 根修 | ① host body_expr_stmt：任一臂 `EXPR_RETURN` → `codegen_emit_match_as_stmt` if/else 真 `return`（三元仍 value 位）② freestanding match：RETURN 臂后 skip jmp-done（已 tail_join）。权威：codegen.x + seed codegen_gen · pipeline_glue |
| 构建级别 | **L2**（codegen_x + pipeline_glue_standalone + g05） |
| 钉盘 | 仍 **`ace59b6cd`** |
| 探针 | 双端 host+fs follow/only/mixed/wild/expr=42；tests/match=2；矩阵 rv/option/hello |
| soft | untyped-let；reentrancy；`mut`；trait/impl |

### wave371（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`78708a97f`** |
| 债 | **match 语句形式**：中置 `match {…}` 后仍有 return/let → XP003；臂 `=> return N` 解析失败 / host C 发非法三元 `return (…?(return N):…)` |
| 根修 | ① `parse_one_function_impl` / `parse_block_into`：MATCH 不 break 循环；`parse_match_into` → body_expr_stmt 或 final（next `}`）② primary `TOKEN_RETURN` → `EXPR_RETURN` ③ host match 三元臂 unwrap RETURN 操作数。权威：parser.x+parser_gen seed · primary_slice · codegen.x+codegen_gen seed |
| 构建级别 | **L2**（parser_x + thin_glue + codegen_x + g05） |
| 钉盘 | 仍 **`ace59b6cd`** |
| 探针 | 双端 match_stmt_semi/then_ret/expr/tail/assign_only/field_eq=42；tests/match main=2；矩阵 rv/option/hello |
| soft | match 臂 return 后可达语句非真 early-return（wave372 已闭） |

### wave370（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`039461054`** |
| 债 | bare **if/while 条件末位 FIELD_ACCESS** 后 `{` 被吞为 STRUCT_LIT → XP003 |
| 根修 | FIELD_ACCESS+LBRACE 复用 block disambiguation；权威 primary_slice |
| 构建级别 | **L2** · 钉盘仍 **`ace59b6cd`** |

### wave369（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`928e3b389`**（ensure）· 连带 **`7e1b6a8b3`**（ZST store_sz / slot metrics） |
| 债 | freestanding **中部/末位** empty-of-empty Nest（`Box { a, n: Nest{e:Empty{}}, b }`）退出码乱 |
| 根修 | ① `ensure_struct_layout_from_struct_lit`：parser `nf >= lit nf` **禁止 append** ② slot metrics 真 ZST 不 invent ③ STRUCT_LIT empty ZST `store_sz=0` |
| 构建级别 | **L2** · 钉盘仍 **`ace59b6cd`** |

### wave368（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`2eb763a4d`** |
| 债 | 全空字段嵌套 ZST（`Nest { e: Empty }` 作他结构字段）metrics **T001** |
| 根修 | G.7 扩展 `typeck_type_is_empty_struct`：`nf==0` **或** 全字段递归 empty（+depth>64）；glue twin；seed typeck_gen/empty_surface 同 commit |
| 构建级别 | **L2** · 钉盘仍 **`ace59b6cd`** |

### wave367（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`e2f8e07e8`** |
| 债 | STRUCT_LIT **末字段** `A { x: 42, e: E {} }` 解析 P001 / out_len=0 |
| 根修 | G.7 field-value depth + wave364 empty `Type {}` 在 field 下遇外层 `}` 仍 keep struct lit |
| 构建级别 | **L2** · 钉盘仍 **`ace59b6cd`** |

### wave366（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`1c1c1cd59`** |
| 债 | freestanding **`Empty {}` CG002** + nested empty field **T001**（`fsize<=0`） |
| 根修 | G.7 `typeck_type_is_empty_struct` + metrics 接受 ZST fsize==0；glue twin；STRUCT_LIT `nf==0` |
| 构建级别 | **L2** · 钉盘仍 **`ace59b6cd`** |

### wave365（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`8ec8a48f0`** |
| 债 | host-C **`struct Empty {}`** incomplete type BLD001 |
| 根修 | G.7 仅 `nl<=0` 跳过；`nf==0` 发 `struct T {\n};\n` |
| 构建级别 | **L2** · 钉盘仍 **`ace59b6cd`** |

### wave364（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`d0dc16218`** |
| 债 | bare **`if b {}` / `while b {}`** 空体 XP003 |
| 根修 | G.7 `parser_asm_primary_empty_ident_braces_prefer_block_c` |
| 构建级别 | **L2** · 钉盘仍 **`ace59b6cd`** |
