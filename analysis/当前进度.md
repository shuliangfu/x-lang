# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **pipeline_abi wave76 pure residual** @ `f0a89715` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **R2 pipeline_abi Cap residual pure wave76**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + pure-asm 矩阵 rv42 / option / hello / stdlib-import；`shux_cstr_offset` pure **W** @ `f0a89715` |
| mac | ✅ L2 PREFER hybrid + c-backend 矩阵（Darwin pure-asm CG002 tip 既有）@ `f0a89715` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave76** @ `f0a89715`  
   - **迁 pure**：`shux_cstr_offset`（G.7 `&s[off]` → C `&((s)[off])` ≡ `s+off`；闭合 Cap residual 指针算术叶）  
   - **消费方**：pure `pipe_dir_tail` / `pipe_strip_prefix_seg`；driver_abi / thin `-D` parse 继续 **import** 本符号（无第二权威）  
   - **seed**：冷孪生 `#ifndef FROM_X`  
   - **Cap residual 仍**：fn-ptr / product_emit / typeck_module C 前端（+ `typeck_dep_*` / `typeck_ndep` 跨 TU 全局 BSS；`pipeline_sizeof_*`；preprocess 引擎；OS path realpath / FILE*）  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave76.log` · `/tmp/ubuntu_g05_wave76.log`

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：pipeline_abi 其它 Cap residual 叶（fn-ptr / product_emit / typeck_module C 前端；typeck_dep 跨 TU 全局需另波收敛 writers→accessor 再迁 pure BSS；sizeof / preprocess / OS residual…）/ 整波 tip L4  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave76 未升钉）
产品 tip：f0a89715 pipeline_abi residual pure wave76 L2
Windows：本波不需要切换
下一：pipeline_abi 其它 residual / 整波 tip L4（需时再说）
```
