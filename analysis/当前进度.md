# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（本波未改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`46ba2297`**（升自 `11a57351`；双端 L4 + 129） |
| tip | **`46ba2297`**（= 钉盘；opaque DIR* / libc skip↔preamble 对齐） |
| 产品 bstrict | **129** · 双端 L4 真冷全绿 |
| 本波性质 | tip L4 升钉：产品 C 链 libc 冲突与 opaque `DIR*` 根修 |
| Ubuntu L4 | ✅ bootstrap/g05/rv42/fs + bstrict 129 · `/tmp/ubuntu_true_bstrict_46ba2297.log` |
| mac L4 | ✅ bootstrap/g05/rv42/fs + bstrict 129 · `/tmp/mac_true_bstrict_46ba2297.log` |
| Windows | **本波不需要** |

## 本波摘要

1. **产品轨 libc / preamble 对齐（升钉主因）**  
   - `113fd367`：libc skip 与 `stdlib`/`string` preamble 成对；Darwin multi-file heap 用 `libtool -static`  
   - `836d0d0d` / `769356dd`：`strto*` / `memchr`/`strchr*` 族跳过再声明（string/stdlib 头后冲突）  
   - `25f043ee` → `52dabaf6` → **`46ba2297`**：opaque `DIR*` 为 `*u8` — **不** include `dirent.h`；**不** skip `opendir`/`closedir`（emit SHUX 型 extern）；去掉 `fcntl.h`（`open` 签名冲突）  

2. **双端 L4 真冷 + 129**  
   - Ubuntu：全擦 + bootstrap + g05 + rv=42 + fs + bstrict **OK (129)** · `/tmp/ubuntu_gate_46ba2297.status`  
   - mac：同上 · `/tmp/mac_gate_46ba2297.status`  
   - → **钉盘 `11a57351` → `46ba2297`**

3. 前序：async 产品 PREFER 族 @ `faf5b269`（工程轨 L2；本波已并入 tip L4）

## 下一波前排

1. **可选** 其它 R2 rest / Cap residual  
2. **可选** g05 sed 防御 / Ubuntu `-E` SIGSEGV 根因 / simd-s4 WARN  
3. **可选** Windows hybrid（需切双系统时再喊人）

```text
钉盘：46ba2297
tip：46ba2297（opaque DIR* + libc skip/preamble；双端 L4 129）
Windows：本波不需要切换
下一：其它 R2 / 可选 Windows
```
