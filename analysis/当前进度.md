# X 语言 / X language 当前进度（2026-07-28 · wave610 Cap FIELD TYPE_ARRAY as T[N] call-arg · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`d742a7ec7`**（wave610 Cap freestanding FIELD TYPE_ARRAY as T[N] call-arg · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave610（已闭 · Cap residual freestanding FIELD TYPE_ARRAY as T[N] call-arg · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `take(Wrap{…}.xs)` / `take(w.xs)` / `take(mk().xs)` → freestanding SIGSEGV（host-C 32 绿） |
| tip 产品 | **`d742a7ec7`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave610 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| fs FIELD fixed TYPE_ARRAY as T[N] formal (E*) | `pipeline_asm_emit_expr_elf_for_call_args` 仅 wave396 盖 slice；T[N] 回落 emit_expr field rvalue load 首元 | ✅ 复用 INDEX base helper 传 &field |

**数据流（五问）**：产生 = CALL 实参 `FIELD_ACCESS`（字段 TYPE_ARRAY）；形参 = 固定 `T[N]` 降 E*（wave417 8B GP）；消费 = callee `a[i]` 经 E* 基址。旧路径：非 slice 的 FIELD 未拦截 → field rvalue leave_addr=0 → **load 首元**当指针 → take SEGV。`take([10,32])` 已 lea 绿。权威：G.7 补全同一 call-arg 入口 — formal TYPE_ARRAY + FIELD 定长数组 → `glue_try_index_var_or_field_base_to_rax`（wave609 leave_addr 权威），不新开第二套 FIELD 数组路径。

**权威**：`compiler/pipeline_glue.c` · `pipeline_asm_emit_expr_elf_for_call_args` · `glue_try_index_var_or_field_base_to_rax_elf_c`。改后 rm `pipeline_x.o` + g05。

### wave610 验证

| 探针 | mac L2 host/fs | Ubuntu L2 host/fs |
|------|----------------|-------------------|
| lit_arr_field_only `take(Wrap{…}.xs)` | ✅ 32/32（was fs 139） | ✅ 32/32 |
| take_arr_let `take(w.xs)` | ✅ 32/32 | ✅ 32/32 |
| take_arr_call `take(mk().xs)` | ✅ 32/32 | ✅ 32/32 |
| take_nest `take(Outer{…}.w.xs)` | ✅ 32/32 | ✅ 32/32 |
| take_arr_lit 回归 | ✅ 32/32 | ✅ 32/32 |
| lit_arr_field / let_arr_field 回归 | ✅ 32/32 | ✅ 32/32 |
| lit_field / lit_method 回归 | ✅ 40/42 | ✅ 40/42 |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/0/0 |

### 仍 soft

- ARRAY_LIT 基 INDEX（`[10,32][1]` XP003）  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual soft：ARRAY_LIT INDEX，或其它 Cap 叶，或整波 L4 + 升钉  

## wave609（已闭 · Cap residual freestanding INDEX on STRUCT_LIT/CALL array field · L2）

tip 产品 **`e641caa4c`** / docs **`d4452217c`**。soft「`take(Wrap{}.xs)` ARRAY call-arg」本波由 **wave610** 收口。

## wave608（已闭 · Cap residual freestanding STRUCT_LIT.field rvalue · L2）

tip 产品 **`d6ac2e362`** / docs **`bfbf026b6`**。soft「fs INDEX on STRUCT_LIT array field」由 **wave609** 收口。

## wave607（已闭 · Cap residual STRUCT_LIT postfix method/field · L2）

tip 产品 **`cfa113ffc`** / docs **`042d52b1b`**。

## wave606（已闭 · Cap residual arm64 freestanding UFCS method MEMORY · L2）

tip 产品 **`d4e63a37d`** / docs **`17186ce40`**。
