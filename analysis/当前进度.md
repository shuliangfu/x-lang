# X 语言 / X language 当前进度（2026-07-28 · wave616 Cap arm64 freestanding float · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`dbd0d2d16`**（wave616 Cap arm64 freestanding float cast/ALU/imm32 · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave616（已闭 · Cap residual arm64 freestanding float · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | mac fs `return (f32) as i32` / `(x+y) as i32` CG002 或 run=0（Ubuntu x86 已绿） |
| tip 产品 | **`dbd0d2d16`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave616 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| float cast encoders `ta!=0` → -1 | `backend_enc_cvtt*` / `cvtsi*` / `cvtss*` thin+seed | ✅ arm64 fmov+fcvtzs/scvtf/fcvt 序列 |
| f32/f64 binop only `ta==0` | `pipeline_glue.c` float gates | ✅ `(ta==0\|\|ta==1)` + arm64 fadd/fmul/… |
| f32 store 负向 STUR vs load 正向 LDR | `arm64_enc_store_w0_to_rbp_c` | ✅ 正向 STR W `[x29,#off]` |
| f32 imm MOVK hw=0 | `xlang_arm64_mov_imm32_to_w0_c` / `arch_arm64_enc_enc_mov_imm32_to_w0` | ✅ MOVK hw=1 `lsl #16` |

**数据流（五问）**：产生 = EXPR_AS / float binop / f32 lit / 32-bit frame store；存储 = GP 中 IEEE 位型 + 帧槽；消费 = fcvtzs / fadd / 后续 INDEX。host-C 绿掩盖 pure-asm。G.7 **补全** 既有 encoder/store/imm 权威 — 不新开第二套 float 路径。

**权威**：`backend_enc_dispatch_thin.x` + `backend_enc_dispatch.from_x.c`（cast/ALU）· `asm_backend_compat_stubs` + `backend_arm64_enc_c.from_x.c`（imm32）· `pipeline_glue.c`（ta 门控）。改后 rm 相关 `.o` + g05。

### wave616 验证

| 探针 | mac L2 host/fs | Ubuntu L2 host/fs |
|------|----------------|-------------------|
| f32/f64 `as i32` | ✅ 42 | ✅ 42（既有） |
| `(f32+f32) as i32` / f64 add cast | ✅ 42 | ✅ 42 |
| i32↔f32 / f32↔f64 cast let | ✅ 42 | ✅ 42 |
| wave615 row_let 回归 | ✅ | ✅ |
| rv / option / si / hello | ✅ | ✅ |

### 仍 soft

- freestanding f32 **ARRAY_LIT** 仍按 f64 位装元（`f32[2]=[10,32]` INDEX cast run=0）  
- freestanding f32 **cmp** 走整数 cmpq 读 64 位槽（高位噪声）  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual soft（f32 array lit / f32 cmp）或其它叶，或整波 L4 + 升钉  

## wave615（已闭 · Cap residual freestanding multi-dim INDEX subrow · L2）

tip 产品 **`aeac7201f`** / docs **`414cef4c7`**。

## wave614（已闭 · Cap residual arm64 freestanding INDEX assign · L2）

tip 产品 **`eb92a18b1`** / docs **`8fa734639`**。

## wave613（已闭 · Cap residual freestanding multi-dim nested ARRAY_LIT · L2）

tip 产品 **`82a1c1e1f`** / docs **`c3f870e1e`**。
