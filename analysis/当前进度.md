# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`86281cc6`**（双端 L4 真冷 + 129；未升） |
| tip | **R2 driver_abi Cap residual pure wave20**（preamble line_at/count 桥；日常 L2） |
| 产品 bstrict | **129** · tip L4 钉盘仍 `@86281cc6` |
| 本波性质 | **工程轨 R2 pure 微步**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42/option/si/hello + pure-asm rv42 @ `abf7ea8e` |
| mac | ✅ L2 同 |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `runtime_driver_abi` Cap residual pure wave20（本波）**  
   - 真迁：`driver_preamble_io_net_line_at` / `line_count` /  
     `driver_preamble_fs_path_line_at` / `line_count`  
   - 手段：固定 N（io_net=219 / fs_path=21，与 `rt_preamble` sizeof 对齐）+  
     G.7 `shux_ptr_slot_get` on always-seed `driver_preamble_*_lines_raw`  
   - 巨型字串表文本权威仍：`seeds/rt_preamble.from_x.c`（Cap-giant-string data）  
   - 仍 seed：`driver_preamble_fputs`；`fmt_argv`；calloc/host_defaults；tmp 访问器  
   - seed：cold C line_at/count twin；`#ifndef SHUX_L2_RDABI_THIN_FROM_X` 剔 pure-dup  
   - 附带：cold seed 中 `pipeline_dep_ctx_heap_destroy` extern 提前声明（host-cc）  
   - 代码 SHA：**`abf7ea8e`**（docs 本 commit 跟进）  
   - 矩阵：rv_build=0 / rv_run=42 / opt 0 / si 0 / hello 0 / pure-asm rv42  
   - 钉盘仍 **`86281cc6`**（skill §3.3：微步不自动 tip L4）

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文；**不**改 `当前问题分析.md` / `问题分析文档.md`

## 下一波前排

1. **可选** driver_abi 余 Cap residual（**fmt_argv** 待 pointer-array pure / calloc / host_defaults）  
2. **可选** g05 sed / harness `-E` 评估  
3. **可选** Windows hybrid；Darwin arm64 纯 asm Vec4f CG002  

```text
钉盘：86281cc6（tip L4 双端 129）
tip：R2 driver_abi wave20 preamble line bridge pure（L2 @ abf7ea8e）
Windows：本波不需要切换
下一：fmt_argv / g05 sed / Windows / Darwin CG002
```
