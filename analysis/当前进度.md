# X 语言 / X language 当前进度（2026-07-28 · wave590 Cap freestanding CALL.field >16B · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`73815a8d7`**（wave590 Cap freestanding CALL.field >16B · L2 双端绿） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave590（已闭 · Cap residual freestanding CALL.field >16B sret/inline · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave589 soft leave：`mk().f` ret_sz>16 UNHANDLED → CG002 |
| tip | **`73815a8d7`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave590 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| freestanding `mk().f` >16B CG002 | asm FIELD_ACCESS CALL base | ✅ 物化路径对齐 `let s=mk()` |
| ≤16B 回归 | dual-GP store_retval | ✅ short/sm 仍 42 |

**数据流（五问）**：产生 = freestanding `EXPR_FIELD_ACCESS` 基为 CALL/METHOD 且返回 >16B MEMORY；存储 = 帧 temp（STRUCT_LIT return inline 或 SysV sret home）；消费 = layout field offset load。wave589 仅 ≤16B；`ret_sz>16` 早退 UNHANDLED → CG002。host-C 临时量 `.` 掩盖。

**权威**：G.7 `pipeline_glue.c` `glue_field_access_call_base_rvalue_elf_c` — 与 `glue_emit_struct_type_let_init` 同序：① `try_inline_struct_lit_return_call_to_slot` ② x86 SysV sret（hidden rdi=home + `call_sret_reg_shift`）③ field load。改 glue → `pipeline_x.o` + g05。

### wave590 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `mk().f` Big 24B（STRUCT_LIT ret） | ✅ 42 | ✅ 42 |
| `let s=mk(); s.f` | ✅ 42 | ✅ 42 |
| two-call `mk().a+mk().f` | ✅ 52 | ✅ 52 |
| Big32 32B | ✅ 42 | ✅ 42 |
| true sret `return t; mk().f` | ✅ 42（host/hybrid） | ✅ 42（pure-asm 默认） |
| ≤16B short/sm 回归 | ✅ 42 | ✅ 42 |
| `-freestanding` STRUCT_LIT `mk().f` | ✅ 42 | ✅ 42 |
| rv / option / si / hello | ✅ 42/0/0/`Hello World` | ✅ 42/0/0/`Hello World` |

### 仍 soft

- arm64 freestanding **non-inline** large CALL（`return local` 本体即 CG002，非本波 FIELD 独有）  
- Ubuntu `-freestanding` 旗标下 true-sret 链 `freestanding_io.o` / `xlang_sys_write`（let 同形也红；产品默认 pure-asm 绿）  
- 其它 Cap residual 侧门 / 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual：其它侧门 / arm64 large sret / freestanding 链 companion，或整波 L4  
2. 整波双端 L4 + 升钉  

## wave589（已闭 · Cap freestanding CALL.field ≤16B）

tip `f82b1390a` / docs `ea4bba20e`。soft >16B 已由 **wave590** 闭。
