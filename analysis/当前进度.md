# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave136** generated_c needs pure @ `ce180645f` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **其它 rest R2 · Cap residual pure 可迁叶**（`link_abi_generated_c_needs_{fs,random,time,runtime}`） |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + fs/time/random + prove labi_freestanding_list **IDENTICAL** |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + fs/time/random + prove labi_freestanding_list **IDENTICAL** |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：wave135 fk0 pure 后，C 路径 string needs residual — `generated_c_needs_{fs,random,time,runtime}`（PRIMARY OS/fs 邻域；user_o needs 族已闭）  
2. **根修**：pure 迁入 `labi_freestanding_list.x`（`labi_fs_gen_{fs,random,time,runtime}_needle_*` 表 + pure orch；null/empty c_path → 0；Cap residual `contains_substr`）  
3. **G.7**：权威表取 mega 完整表面；单一 L7 表+orch；mega 仅 forward decl  
4. **仍 mega / 常驻 Cap**：其它 `generated_c_needs_*`（win32/db/compress/core_slice/async…）· `provides_*` · nm/push/ensure · `undef_sym` / `contains_substr`  
5. **验收**：双端 L2 g05 + §4 矩阵 + fs/time/random 邻域 + prove **IDENTICAL**；钉盘仍 **`3c88f7e51`**

## 下一波前排

1. 其它 rest R2 可迁叶（其它 `generated_c_needs_*` / mega residual；常驻 Cap 不硬迁）  
2. **可选** Windows hybrid（**需要时再通知重启**）  
3. 其它工程 residual（按债裁剪）  

```text
钉盘：3c88f7e51（未升）
tip：ce180645f wave136 generated_c needs pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：其它 generated_c_needs_* / Cap residual pure
```
