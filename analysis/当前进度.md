# Shux 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-22.md](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave182** ensure_bootstrap_nostdlib_stubs_o pure @ `af1af5768` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **R2 Cap residual pure · ensure_bootstrap_nostdlib_stubs_o 叶** → L4 ensure_list |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + ensure_list prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + ensure_list prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：常驻 Cap residual — `shux_ensure_bootstrap_nostdlib_stubs_o` 体仍 always mega C（`system("cc -c -O2 -fno-builtin …")` + snprintf）  
2. **根修 / pure**：真迁 `labi_ensure_list.x` L4 pure orch（peer wave181 path + pure byte joins out/src/inc；Cap residual resolve / path_readable / **`shux_cc_compile_sync_one_extra(-fno-builtin)`** / skip_missing + peer diags；无 system Cap 路径）  
3. **Cap residual**：resolve / access / spawn·cc one_extra / stat；**path** peer pure @ wave181  
4. **产品调用方**：asm freestanding ld bag（`shux_asm_invoke_ld_platform` nostdlib stubs ensure）  
5. **验收**：双端 L2 g05 + 矩阵 + string/vec/slice/compress 邻域 + ensure_list prove IDENTICAL；钉盘仍 **`3c88f7e51`**  

## 下一波前排

1. 其它 rest R2 / mega residual（可迁：常驻 Cap — undef_sym / path_io stat / wait / strerror / mega spawn-IO / ld_debug_argv / ensure_std_net_o_auto_tls system·make / getenv / shu_resolve_compiler_dir / thin runtime_*_o_path BSS …）  
2. 可选：未闭合块注释硬诊断（EOF depth>0）  
3. 可选 Windows hybrid（**需要时再通知重启**）  

```text
钉盘：3c88f7e51（未升）
tip：af1af5768 wave182 ensure_bootstrap_nostdlib_stubs_o pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：其它 rest R2 / mega residual（常驻 Cap）
```
