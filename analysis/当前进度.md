# X 语言 / X language 当前进度（2026-07-28 · wave605 Cap MEMORY STRUCT_LIT/FIELD · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`46aa389a1`**（wave605 Cap freestanding MEMORY STRUCT_LIT/FIELD call-arg · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave605（已闭 · Cap residual freestanding MEMORY call-arg from STRUCT_LIT/FIELD · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | mac fs `sum(Big{…})` / `sum(o.t)` CG002（wave604 soft：store/push 仅 VAR+CALL） |
| tip 产品 | **`46aa389a1`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave605 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| MEMORY call-arg from STRUCT_LIT | push/store `else → -1` | ✅ emit_struct_let_init → frame temp → multi-word |
| MEMORY call-arg from FIELD | 同上；初版 `load_qword_from_rbx` arm64 恒 -1 | ✅ 逐字 lvalue_eff_addr + load_64_from_rax（SHARED） |

**数据流（五问）**：产生 = STRUCT_LIT/FIELD >16B 实参；存储 = 帧 temp（x86 high-end / arm64 low-end）→ 出栈 MEMORY；消费 = param_home。权威：G.7 补全 `push_sysv_memory` + `store_memory_to_sp` 源分类，不新开第二套。FIELD 禁用 x86-only `load_qword_from_rbx`（`ta!=0` 早退）。

**权威**：`compiler/pipeline_glue.c`（`pipeline_asm_push_sysv_memory_by_value_elf_c` · `pipeline_asm_store_memory_by_value_to_sp_elf_c`）。改后 rm `pipeline_glue_standalone.o` + g05。

### wave605 验证

| 探针 | mac L2 host/fs | Ubuntu L2 host/pure/fs |
|------|----------------|------------------------|
| mem_lit `sum(Big{…})` | ✅ 42/42（was CG002） | ✅ 42/42/42 |
| mem_field `sum(o.t)` | ✅ 42/42（was CG002） | ✅ 42/42/42 |
| mem_call / mem_var 回归 | ✅ 42 | ✅ 42 |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/hello |

### 仍 soft

- UFCS method 同模块 typeck residual（本叶未开）  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual soft（UFCS method typeck / 其它叶），或整波双端 L4 + 升钉  

## wave604（已闭 · Cap residual arm64 freestanding MEMORY call-arg from CALL · L2）

tip 产品 **`f1c9909aa`** / docs **`59139dae0`**。soft「MEMORY STRUCT_LIT/FIELD」本波由 **wave605** 收口。

## wave603（已闭 · Cap residual arm64 freestanding VAR dual-GP + MEMORY call-arg · L2）

tip 产品 **`00d2059d0`** / docs **`06c3d4e1a`**。soft「MEMORY from CALL」由 **wave604** 收口。

## wave602（已闭 · Cap residual freestanding UFCS method self SysV · L2）

tip 产品 **`ddb5f61ff`** / docs **`fb307d12a`**。
