# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`86281cc6`**（双端 L4 真冷 + 129；未升） |
| tip | **R2 driver_abi Cap residual pure wave25**（tmp path BSS slots + open_out via accessor；日常 L2） |
| 产品 bstrict | **129** · tip L4 钉盘仍 `@86281cc6` |
| 本波性质 | **工程轨 R2 pure 微步**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42/option/si/hello + pure-asm rv42 @ `35c93231` |
| mac | ✅ L2 同 |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `runtime_driver_abi` Cap residual pure wave25（本波）**  
   - 真迁：tmp path BSS 槽（与 wave15 缓冲槽同形态）  
   - `driver_asm_tmp_path_slot`：64B BSS（mkstemp 路径）  
   - `driver_parsed_tmp_c_slot`：64B BSS（reset 清 [0]）  
   - `driver_parsed_tmp_c_buf`：256B BSS（open_out 写 `.c` 路径；Windows long TEMP）  
   - **根修双权威**：seed `driver_parsed_open_out_file` **始终**经 `driver_parsed_tmp_c_buf()` 写缓冲（禁止直写 static）  
   - seed：cold twins 包 `#ifndef SHUX_L2_RDABI_THIN_FROM_X`；FROM_X 无 pure-dup  
   - 仍 seed：巨型 `rt_preamble` 表、open_out/fclose/write_out/invoke_cc 体、FILE* 等  
   - 注释：**JSDoc**（`@param` type+role / `@return`；`*/` 单独成行；供未来 **`shux doc`**）  
   - 代码 SHA：**`35c93231`**（docs 本 commit 跟进）  
   - 矩阵：rv_build=0 / rv_run=42 / opt 102 / si 0 / hello 0 / pure-asm rv42  
   - 日志：`/tmp/mac_g05_wave25.log` · `/tmp/ubuntu_g05_wave25.log`  
   - 钉盘仍 **`86281cc6`**（skill §3.3：微步不自动 tip L4）

2. **注释纪律（已固化）**  
   - 方法块注释 = JSDoc；参数类型与作用写全；日后 **`shux doc`** 生成  
   - skill G.9 + MEMORY + `references/x-english-comments.md`

3. **文档纪律**  
   - 只改 `自举进度.md` + 本文；**不**改 `当前问题分析.md` / `问题分析文档.md`

## 下一波前排

1. **可选** driver_abi 余 Cap residual（**巨型 `rt_preamble` 表** / open_out·write_out·invoke_cc 体）  
2. **可选** g05 sed / harness `-E` 评估  
3. **可选** Windows hybrid；Darwin arm64 纯 asm Vec4f CG002  

```text
钉盘：86281cc6（tip L4 双端 129）
tip：R2 driver_abi wave25 tmp slots pure（L2 @ 35c93231）
Windows：本波不需要切换
下一：rt_preamble 巨型表 / open_out 体 / g05 sed / Windows / Darwin CG002
```
