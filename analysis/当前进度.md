# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`11a57351`**（双端 129） |
| tip | **`2a88053b`**（R2 async_asm_pool） |
| 产品 bstrict | **129** · 钉盘双端全绿 |
| 本波性质 | R2 `async_asm_pool` full true-migrate (H=0) |
| 工程轨 prove | ✅ mac + Ubuntu IDENTICAL（Ubuntu 5/5 稳） |
| Windows | **本波不需要**（无 hybrid/Windows 探针） |

## 本波摘要

1. **R2 `async_asm_pool` full H=0**（`f57e23df` + 栈修 `2a88053b`）  
   - `.x` 吃满 helpers + `async_asm_pool_build_layout`  
   - seed：`#ifndef SHUX_ASYNC_ASM_POOL_FROM_X` 全 C；`#else` 业务 H=0；`slice_marker`  
   - surface + `prove_module_selfhost` 条目  
   - **产品**仍 `pipeline_glue.c` #include 冷 seed（unbundle 另波）  
   - Ubuntu：大栈 `u8[4096]` → prove -E 偶发 SIGSEGV；改为只缓存 let 下标后 5/5 绿  

2. 前序：P1 preprocess EOF（`157f1dc1`）· libc skip（`fda84704`）· 钉盘 `11a57351`

## 下一波前排

1. **R2 下一 rest**（选题标准同前）  
2. **可选** tip 升钉：Ubuntu L4 + 双端 129（含 SHARED preprocess + 本 R2 后）  
3. **可选** Windows hybrid（需切双系统时再喊人）  
4. **可选** async_asm_pool 产品 unbundle（glue 去 include + PREFER `.o`）

```text
钉盘：11a57351
tip：2a88053b（R2 async_asm_pool H=0；双端 L2 prove）
Windows：本波不需要切换
下一：R2 下一 rest 或 tip L4 升钉
```
