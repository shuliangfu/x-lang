# X 语言 / X language 当前进度（2026-07-29 · wave629 Cap pure-asm TYPE_SLICE INDEX STRUCT_LIT assign · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`2a0496153`**（wave629 Cap pure-asm TYPE_SLICE INDEX STRUCT_LIT assign · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave629（已闭 · Cap residual pure-asm TYPE_SLICE INDEX STRUCT_LIT assign · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Ubuntu pure-asm `S24[]` `s[i]=S24{…}` 垃圾/panic:0；host-C 66；fixed TYPE_ARRAY wave628 已绿 |
| tip 产品 | **`2a0496153`**（含 `25e612de7` 扩 TYPE_SLICE + stack dest spill） |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave629 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| TYPE_SLICE INDEX STRUCT_LIT | wave628 仅 TYPE_ARRAY；slice fat home 非 payload | ✅ 任意 index → eff_addr（.data+imul esz）+ DEST_IN_RBX |
| DEST_IN_RBX 帧 spill 叠局部 | `ly->next_offset` 与 `j` 同槽；二次 bounds 把指针当 index → panic:0 | ✅ push dest on CPU stack；rehome pop/push；exit pop |

**数据流（五问）**：产生 = `s[i]=StructLit` TYPE_SLICE 回落 generic → rax=temp 指针 + finish_store ≤8；存储 = 元素仅 8B 指针；消费 = 字段垃圾。二次修：帧 spill 写穿 `j` → bounds panic。G.7：**扩** wave628 同一 in-place 权威至 TYPE_SLICE；dest 保存改 CPU 栈（禁 next_offset 叠局部）。

**权威**：`pipeline_glue.c` only（`pipeline_asm_emit_assign_elf_c` INDEX 叶 TYPE_SLICE 支 + `pipeline_asm_emit_struct_lit_fields_elf_c` DEST_IN_RBX CPU-stack rehome）。改后 **须** `pipeline_glue_standalone.o` + g05。

### wave629 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `S24[]` var-index 双写 sum | ✅ 66 | ✅ 66 |
| `S24[]` one / lit-index | ✅ 6 / 66 | ✅ 6 / 66 |
| formal take(S24[]) 双写 | ✅ 66 | ✅ 66 |
| fixed S24[2] / Pt[] / Pt[2] 回归 | ✅ 66 / 42 / 42 | ✅ 66 / 42 / 42 |
| freestanding 同上 | ✅ | ✅ |
| host-C | ✅ | ✅ |
| rv / option / si / hello | ✅ | ✅ |

### 仍 soft

- assign-side scale 对非 STRUCT_LIT 的 esz∉{1,4,8} 仍 soft；其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一叶，或整波 L4 + 升钉  

## wave628（已闭 · Cap residual pure-asm var-index STRUCT_LIT assign · L2）

tip 产品 **`74c40b979`** / docs **`c3ebac45a`**。TYPE_ARRAY var-index 绿；TYPE_SLICE 本波 **wave629** 闭。

## wave627（已闭 · Cap residual pure-asm INDEX STRUCT_LIT assign + multi-dim flat · L2）

tip 产品 **`ab3873604`** / docs **`b39d57bbc`**。

## wave626（已闭 · Cap residual pure-asm fixed `Pt[N]` STRUCT_LIT in-place · L2）

tip 产品 **`becc92c72`** / docs **`dbf9a2fe7`**。

## wave625（已闭 · Cap residual pure-asm `[]Named` force_esz + FIELD dual-slot · L2）

tip 产品 **`b278583c4`** / docs **`994568244`**。

## wave624（已闭 · Cap residual host-C `[]NamedStruct` fat slice · L2）

tip 产品 **`97b3e5d82`** / docs **`ad577c123`**。
