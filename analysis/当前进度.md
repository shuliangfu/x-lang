# Shux 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-22.md](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d6648bee4`**（升自 `3c88f7e51`；双端 tip L4 + 129） |
| tip 产品代码 | **wave237** parser_asm_parse_expr_link residual raw getenv → link_abi_getenv @ **`b4b116f2`** |
| 产品 bstrict | 钉盘 **129**；本波 **日常 L2**（非 tip L4） |
| 本波性质 | **R2 Cap residual pure** · G.7 调用方 getenv 收敛（parser-asm parse_expr debug env gate） |
| Ubuntu | ✅ L2：g05 + 矩阵 + MINIMAL + string `-c` |
| mac | ✅ L2：g05 + 矩阵 + MINIMAL + string `-c` |
| Windows | **不需要切换** |

## 本波摘要

1. **wave237**：产品 **parser_asm_parse_expr_link** residual raw `getenv()` → **`link_abi_getenv`**（wave222 pure thin → `_impl` host getenv）。`SHUX_PARSER_ASM_DEBUG`（`parser_asm_parse_expr_debug_enabled`）。pure `.x` 全量 orch；cold seed `#ifndef FROM_X` 同语义 twin；hybrid rest 不再 rename 为 `*_impl`。语义保留：null/empty/leading `'0'` → off。G.7 单权威 env 面；host residual 仅 **`link_abi_getenv_impl`**。`nm`：`U link_abi_getenv`（无 `U getenv`）。
2. **闭合 soft residual**「parser_asm_parse_expr_link 仍 raw getenv」。resolve 池 `_impl` / system `_impl` 体 / 其它 seed residual raw getenv（pipeline_glue · runtime mega · runtime_link_abi.x · rt_run_* …）仍 Cap residual。
3. **验收**：双端 L2 矩阵 rv42/option/si/hello + MINIMAL rv + `std/string/string.x` `-c`。钉盘仍 **`d6648bee4`**。

## 下一波前排

1. 常驻 Cap residual 可迁叶：resolve 池 `_impl` 体 / 其它 seed residual raw getenv（pipeline_glue · runtime mega · runtime_link_abi.x · rt_run_* …）/ system `_impl` 体；parser_asm getenv @ wave237 · user_asm getenv @ wave236 · pipeline_abi getenv @ wave235 · …
2. 可选：未闭合块注释硬诊断（EOF depth>0）
3. 可选 Windows hybrid（**需要时再通知重启**）

```text
钉盘：d6648bee4
tip：b4b116f2 wave237 parser_asm_parse_expr_link residual raw getenv → link_abi_getenv
构建级别=L2 日常（非 tip L4）
Windows：不需要切换
下一：常驻 Cap residual（resolve 池 _impl / 其它 seed raw getenv …）
```
