# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[自举进度-归档-2026-07-17.md](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **本波 formal time.o fk0+ensure**（钉盘仍 **`c51759eb`**） |
| 产品 L4 放行 | ✅ **钉盘 `c51759eb`**（不升钉） |
| tip 双端 bstrict | ✅ **123/123 @ `c51759eb`**（钉盘） |
| **typeck CTFE fold** | ✅ 主路径 + **scalar 2-arg** + **1-arg pure** + **nested** + **vector lane** |
| **ASM bulk residual** | ✅ **PRIMARY** + **TASK_SPECIAL** + **prepare-ensure OS** + **process_argv** + **time formal** |
| **WPO call-site fold** | ✅ **flat + nested pre-emit** |

## 本波摘要（2026-07-18 · formal time.o 按需 ensure）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前波 residual：fk0 / plan / heap；探到 **tests/time wipe 红** |
| **地图** | ① fk0 无 `std/time/time.o` 探针 → plan 永不推 time.o；② C `need_time` 只 `push_existing` 无 formal ensure（L4 wipe 静默 skip）；③ on_demand time 同样 skip_missing；PRIMARY 只 ensure `time_os` |
| **根修** | G.7 补全权威：`labi_std_fk0_user_needs_rel` 加 `std_time_*`；C `need_time` ensure formal + re-resolve；on_demand time 推入前 ensure formal（对齐 plan STD ensure 块） |
| **验收** | mac L2：wipe 后 pure **不重建** time.o/time_os；time C/asm run=0；format_timezone C/asm；矩阵 rv/opt/si/hello/env |
| **残差** | 其它 fk0 GAP 模块（datetime/uuid/…）；plan bulk 瘦身；hello preamble→process_argv 设计残；禁无地图 tip L4 |

### residual 下一波

1. 下一产品债：另开地图（其它 fk0 GAP / plan 瘦身）；禁无地图 tip L4  

```text
构建级别：L2（runtime_link_abi.o + g05；非 tip L4）
产品 tip L4 钉盘：c51759eb
权威：labi_std_fk0_user_needs_rel(time) + need_time formal ensure + on_demand time formal ensure
```

---

## 前波摘要（2026-07-18 · process_argv 按需 ensure）

| 波 | 结果 |
|----|------|
| **根修** | prepare process_argv 门控；use_line weak-skip；post-module process_shux U 补链 |
| **验收** | mac L2 pure/option 不重建 process_argv |
| **残差** | → **本波 formal time.o** |

## 前波摘要（2026-07-18 · prepare-ensure OS 按需）

| 波 | 结果 |
|----|------|
| **根修** | prepare 对 time/random/env 门控 ensure；删 invoke_cc warm time.o early ensure |
| **验收** | mac/Ubuntu L2 pure 不重建三 OS .o |
