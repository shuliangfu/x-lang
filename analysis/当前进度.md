# X 语言 / X language 当前进度（2026-07-29 · wave644 Cap freestanding *i64/*Named ptr scale · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`85c07dbf3`**（wave644 freestanding *i64/*Named ptr esz） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave644（已闭 · Cap residual freestanding *i64/*u64/*Named ptr±int esz · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | freestanding `*i64 p+1` / `p+=` / `*S p+=` host+pure=32、fs=0；disasm `mul #4`（应 ×8） |
| tip 产品 | **`85c07dbf3`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave644 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| freestanding `*i64`/`*u64`/`*usize`/`*isize` 指针算术步长 | `glue_index_elem_byte_sz_from_type_ref_c` PTR peel 仅 F64→8 | ✅ 4..7 与 bare 面一致 → 8 |
| freestanding `*NamedStruct` 指针算术步长 | 同函数 PTR peel 无 NAMED layout | ✅ `glue_type_size_simple`（≡ ARRAY-of-NAMED） |

**数据流**：产生 = typeck 放行 ptr±int（wave285）+ scale emit（wave642）· 存储 = PTR peel esz · 消费 = freestanding add/sub/`p±=`。根因在 esz 权威半残，非新开 scale 路径。G.7：补全同一 `glue_index_elem_byte_sz_from_type_ref_c`。

**权威**：`compiler/pipeline_glue.c` · `glue_index_elem_byte_sz_from_type_ref_c`（PTR peel）

### wave644 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `*(p+1)` *i64 | ✅ 32 host/pure/fs | ✅ 32 |
| `p += 1` *i64 | ✅ 32 | ✅ 32 |
| `p += 1` *u64 | ✅ 32 | ✅ 32 |
| `p += 1` *S {a,b} | ✅ 32 | ✅ 32 |
| `q - p` *i64 | ✅ 2 | ✅ 2 |
| *i32 / *u8 / *f64 回归 | ✅ | ✅ *i32 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |
| disasm fs *i64 `p+=` | `mul #0x8` | `imul $0x8` |

### 仍 soft

- untyped `self`（wave493 非标准）  
- freestanding `%=` 假跑（host/pure 绿、fs 未写回）— 另叶  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（如 `%=` freestanding），或整波 L4 + 升钉  

## wave643（已闭 · Cap residual compound `*T +=/-=` int typeck · L2）

tip 产品 **`131c1d53b`**。compound `p+=`/`p-=` 整数 offset typeck。

## wave642（已闭 · Cap residual freestanding ptr±int / ptr-ptr C-scale · L2）

tip 产品 **`0f3be5125`**。freestanding C-scale `p±n` / `q-p`（i32/u8/f64 主路径）。
