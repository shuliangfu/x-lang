# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；大债地图 → [当前问题分析.md](当前问题分析.md)  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **归档**：[自举进度-归档-2026-07-19.md](自举进度-归档-2026-07-19.md) · [18 号](自举进度-归档-2026-07-18.md) · [17 号](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`d45ee9a4`** + harness P0（net-context / user .o / run-json cooldown / lang-unsafe SHUX_BIN） |
| 产品 L4 放行钉盘 | **`deaf773b`**（125；**待** tip 双端 L4 后升钉） |
| 产品 bstrict 套件 | **129**（mac 产品链已绿；Ubuntu L4 待） |
| 本波性质 | **产品轨 P0 收口**：mac 产品链 `run-all-bstrict OK (129)`；best-effort 假红甄别结案；**禁止在 Ubuntu 金标前开 R2** |
| 本波输出 | ① `d45ee9a4` net-context 弱符号 + std.context link_only + user .o 注入 + C-style cast；② harness：`run-json` heavy cooldown 15s；lang-unsafe 不再硬绑 `./compiler/shux`；③ 地图 [当前问题分析.md](当前问题分析.md) |

## 本波摘要（2026-07-20 · R2 真迁 target_cpu_pure full true-migrate · 2 commit · L2 验证未跑 L4）

| 项 | 结果 |
|----|------|
| **入口** | parser 修复端到端 -E 验证通过后（前置条件满足），用户明确要求"按你的建议进行下一步自举"，选定 `target_cpu_pure` 作为 R2 真迁目标（rest 能力够 · hybrid 仍有业务 C · 矩阵绿） |
| **R2 模板** | [自举步骤.md](自举步骤.md) §2 R2 full true-migrate：把 seed .from_x.c 里的业务 C 体迁到 .x 文件，让 rest 在 FROM_X 下业务符号 H=0（仅 marker），但保留冷启动全 C 体 |
| **commit 1 `99e1af69`** | **R2 target_cpu_pure full true-migrate (H=0)**：① 新增 `seeds/target_cpu_pure_surface.from_x.c`（355 行 · shux -E 产物抛光版 · 用于 prove IDENTICAL 验证）；② 重写 `seeds/target_cpu_pure.from_x.c`（581 行 · R2 full 结构）：`#ifndef SHUX_L2_TARGET_CPU_FLAGS_FROM_X` 包裹 13 个业务函数 C 体（driver_set_pending / driver_get_pending / tcp_tolower / tcp_eq_at / tcp_set_u32 / tcp_parse_named / shu_target_cpu_resolve / tcp_eq5 / tcp_eq6 / shu_simd_is_vector_type_spelling / shu_simd_vector_lanes_esz_from_spelling / append_feat_name / flags_has_token）+ BSS `g_driver_pending_target_cpu_features`；`#else` 仅 extern 声明（FROM_X 业务 H=0）；`#endif` 后 `slice_marker` + Cap residual（`shu_target_cpu_print` FILE/fprintf + OS detect sysctl/proc/#if 不迁）；③ `tests/prove_module_selfhost.sh` MODULES 添加 `target_cpu_pure` 条目 |
| **commit 2 `e55adc65`** | **跨平台 codegen 前缀修复**：macOS `./shux` 对非 `#[no_mangle]` 的 export function 不加前缀（`tcp_eq_at`），Ubuntu `./shux` 加 `driver_` 前缀（`driver_tcp_eq_at`）→ Ubuntu prove IDENTICAL 失败。**根因修复**：给 `.x` 的 `tcp_eq_at` / `tcp_set_u32` / `tcp_parse_named` 加 `#[no_mangle]` 属性（其他 9 个 export function 已有该属性），让两平台 codegen 都不加前缀。**根因层级**：codegen ABI 属性层，非 symptom patch — 对齐跨平台 codegen ABI 契约 |
| **冷启动符号集** | 19 个（13 业务 + 5 language-limit + slice_marker） |
| **FROM_X 符号集** | 6 个（slice_marker + 5 language-limit Cap residual） |
| **Cap residual** | `shu_target_cpu_print`（FILE/fprintf 语言限制）+ OS detect 函数群（sysctl/proc/#if platform）保留在 seed 中，不迁到 .x |
| **macOS prove 验证** | **✅ IDENTICAL**：`SHUX=compiler/shux bash tests/prove_module_selfhost.sh target_cpu_pure` → `.x→.o` 与 surface→.o 已定义符号完全相同（13 业务函数 + BSS）|
| **Ubuntu prove 验证（金标）** | **✅ IDENTICAL**：`ssh ubuntu-server` git pull + `cd compiler && SHUX=./shux bash ../tests/prove_module_selfhost.sh target_cpu_pure` → IDENTICAL 通过 |
| **构建级别** | L2（prove IDENTICAL 双端通过 · C seed 编译验证）；未跑 L4（待 C6 波 3 整波 L4 一并跑） |
| **PLATFORM** | **SHARED** — .x 改动 + seed 改动跨平台一致；macOS + Ubuntu 双端 prove IDENTICAL 通过 |

## 上一波摘要（2026-07-20 · parser 根源修复 + 端到端验证 + seed re-pin · 3 commit · L2 + 端到端 -E 验证）

| 项 | 结果 |
|----|------|
| **入口** | R2 真迁评估发现 `export extern "C" function fs_posix_close_c(fd: i32): i32 { body }` 带 body 时整个函数不被 codegen 发射 |
| **根因（上帝视角）** | parser top-level TOKEN_EXTERN 分发 → `parse_one_extern_skip_into_slice_c` 期望 `;` 遇到 `{` fail → `parse_one_extern_and_add_into_slice_c` 调用 `parser_skip_one_extern_into_glue` 跳过整个声明 → 函数完全没注册到 module → codegen 无函数可发射 |
| **修复方案** | C seed 检测 `{` 设 `has_body=1` + `is_extern=0`（注册函数但不跳过 body）；.x 层在 TOKEN_EXTERN 分发后调用 `parse_block_into` 解析 body 并设 `body_ref` |
| **AST 双字段设计** | `is_extern`（标记纯声明无 body）vs `abi_kind`（0=X ABI, 1=C ABI）— 正交语义不冲突 |
| **commit 1 `f5435136`** | parser.x 6 处修改 + parser_asm_skip_tl_slice.inc 4 处 + 3 处 C 镜像 struct 字段同步（pthin_skip_tl / parser_asm_thin_c / pthin_glue） |
| **commit 2 `e63ef396`** | Makefile L1927 `RELINK_SHUX_GLUE_SUFFIX` 加 `src/runtime_driver_strict_glue_stubs.o`（修 `make shux-x` 在 macOS arm64 上的 12 个 undefined symbols：`_parse`/`_lexer_new`/`_typeck_module`/`_pipeline_block_labeled_set_names` 等）。修复对齐 `DRIVER_SEED_GLUE_SUFFIX`，ELF/Darwin weak 覆盖 + PE/MinGW `--allow-multiple-definition` 双平台安全 |
| **commit 3 `93126709`** | parser_gen.c re-pin：用 `shux-x -x -E -E-extern src/parser/parser.x` 重新生成 8555 行 → 注入 C-04 markers + extern 声明 → strip `int32_t main(void)`（产品 seed 不含 main，mirrors `fcb44879`）→ 最终 8530 行。has_body count=2 / C-04 count=2 / main count=0 |
| **重编验证** | `rm -f parser_x.o && make parser_x.o` exit=0（164288 bytes，含 has_body）+ `make shux-x` exit=0（3676104 bytes，含修复后 parser） |
| **端到端 -E 验证** | **✅ 通过**：`./compiler/shux-x -x -E -E-extern -o /tmp/runtime_io_abi_gen.c compiler/src/runtime_io_abi.x` → 生成 C 包含 `runtime_io_abi_fs_posix_close_c` / `runtime_io_abi_fs_posix_read_c` / `runtime_io_abi_fs_posix_write_c` 3 个函数 body（委托 std_fs_fs_close / std_fs_fs_read / std_fs_fs_write）。**对比修复前**：body 完全不被发射。**验证方式**：仅重建 parser_x.o + 重链 shux-x（不重建 pipeline_x.o），即 parser 修复通过 parser_x.o 生效，不触发用户禁止的 shux-x -E 全量 dep codegen / pipeline_x.o relinking |
| **构建级别** | L2（C seed 编译 + struct 一致性 + 代码审查）+ 端到端 -E 验证通过；未跑 L4（待 C6 波 3 整波 L4 一并跑） |
| **PLATFORM** | **SHARED** — 影响 macOS + Ubuntu dep codegen 路径；macOS 端到端 -E 验证通过；Ubuntu L4 验证待 C6 波 3 整波 L4 时一并跑 |

## 上一波摘要（2026-07-20 · shux-x -E 爆内存方案 B · 1 commit · L2 验证未跑 L4）

| 项 | 结果 |
|----|------|
| **入口** | [shux-x-5GB-内存根因调查.md](shux-x-5GB-内存根因调查.md) §4 真凶：`g_arena_sc[512]` ArenaSidecar 池（18 GrowVec × 4.3 MB init cap）+ `g_module_sc[512]` ModuleSidecar 池（11 GrowVec）永不释放；`ast_pool_arena_reset` 只 reset len=0 不动 data/cap；dep 循环每个新 arena 指针触发 `arena_sidecar_get(create=1)` → 4.3 MB × N 累积 → GB 级 |
| **方案 B 实施（ast_pool.c）** | 新增 `arena_sidecar_free(ArenaSidecar *sc)` static：调用 18 个 `grow_vec_free` + `memset(sc, 0, sizeof(*sc))`；新增 `module_sidecar_free(ModuleSidecar *sc)` static：调用 11 个 `grow_vec_free` + memset；新增 `ast_pool_arena_release` / `ast_pool_module_release` 公开 API |
| **方案 B 实施（rt_run_x_emit.from_x.c）** | 所有 `free(arena)` / `free(dep_arenas[i/di/j])` 路径（~20 处含错误 goto）前调用 `ast_pool_arena_release(...) + ast_pool_module_release(...)` |
| **-E 内存验证矩阵** | ① 无 import exit=0；② 1 import exit=0 peak RSS **12.6 MB**；③ 3 imports exit=0 peak RSS **32.2 MB**；④ std.fmt transitive deps exit=0 peak RSS **90.7 MB**（从原 ~5GB 显著降低） |

## 上一波摘要（2026-07-20 · C6 波 3 子波 1+2+3+4+5+6+7+8 · 8 commit · L2 验证未跑 L4）

| 项 | 结果 |
|----|------|
| **入口** | [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.4 波 3 子波划分：8 子波按文件大小递增，PoC 验证 typeck 强制 unsafe 后无回归再扩展 |
| **子波 1-8** | `std/atomic` `std/db/arrow` `std/db/sqlite` `std/math` `std/json` `std/async` `std/socketio` `std/http` 共 557 个 `extern function xxx_c` → `extern "C" function`；英文 ABI 块注释 |
| **整波进度** | **557/557 完成（100%）· C6 波 3 整波 8/8 全部收口** |

## 下一波前排（按优先级排序）

### 1. Ubuntu L4 真冷 + tip 升钉（当前）

- mac 产品链 129 已绿（`/tmp/mac_bstrict_d45ee9a4_p0.log`）
- push 后 Ubuntu：全擦 `.o` + bootstrap-driver-seed + g05 + 矩阵 + `SHUX_BSTRICT_SKIP_BUILD=1` 全量 bstrict
- 双端绿 → 升钉 tip；更新仪表盘

### 2. P1 根源债（不挡升钉时可并行）

- codegen `extern … : *u8` → 错误成 `extern int`（getenv）；修 `codegen.x` + seed 同 commit
- parser 末尾无 `\n` → `num_funcs=0`
- 见 [当前问题分析.md](当前问题分析.md) §3.3–§3.4

### 3. R2 真迁下一个 rest（**仅 tip 产品轨双端绿后**）

- `target_cpu_pure` 已完成；下一 rest 按 R2 模板选题
- **禁止**在 bstrict 红 / 未 Ubuntu 金标时开 R2 庆祝

## 不在前排

- **C5 EXPR_CLOSURE CTFE**（独立大工程 · 多波）：已 scoping，顺延自举后立项 `lang-closure-v1-rfc.md`
- **Cap C9 match guard full-chain 支持**（独立大工程 · 多波）：6 个产生点改动，先排期再动手
- **shux-x 5GB 方案 C**（远期 · 非 KPI）：streaming dep codegen（架构改动，自举后再做）
- **历史进度**：shux-x 5GB 方案 B（`9579124c`）/ C6 波 3 整波收口（`8c2bad96`）/ C7 plan 瘦身 8/8 收口（`1d41e903` 125→129 待 L4 验证）/ Windows 门禁收口（`deaf773b` 升钉）/ C6 波 2 ABI 一致性（`305cfbe1` 升钉）/ C6 波 1 unsafe 包裹（`4214eea4` 升钉）/ Windows shux-c exit=1 根因修复（`d25b82cc`）/ C6 P0b audit（`7e507fb6`）/ shux-x 5GB 方案 A（`a14a1dd0`）/ C7 plan 瘦身 PoC（`e546b013`+`bedb40ca`）/ CLI 优化（Phase 1 + help 美化 8 commit）/ std.print 架构优化 / C5 三连击（`c174e70a`）/ 4 波 C5 CTFE / Windows 时序决议 / C2/C3=C4/backtrace/C8 钉盘波，全部归档在 [自举进度.md](自举进度.md) §6 变更记录

```text
本波 tip：e55adc65 (fix(selfhost): add #[no_mangle] to target_cpu_pure.x tcp_eq_at/set_u32/parse_named)
本波性质：R2 真迁 target_cpu_pure full true-migrate（H=0）· 双端 prove IDENTICAL 通过（2 commit · L2 验证未跑 L4）
本波输出：
  99e1af69: R2 target_cpu_pure full true-migrate (H=0)
            - 新增 seeds/target_cpu_pure_surface.from_x.c（355 行 · shux -E 产物抛光版）
            - 重写 seeds/target_cpu_pure.from_x.c（581 行 · R2 full 结构）
              #ifndef SHUX_L2_TARGET_CPU_FLAGS_FROM_X 全 C 体（13 业务函数 + BSS）
              #else 仅 extern 声明（FROM_X 业务 H=0）
              #endif slice_marker + Cap residual（shu_target_cpu_print + OS detect）
            - tests/prove_module_selfhost.sh MODULES 添加 target_cpu_pure 条目
            - 冷启动 19 符号 / FROM_X 6 符号（slice_marker + 5 language-limit）
  e55adc65: .x 3 个函数加 #[no_mangle]（tcp_eq_at/tcp_set_u32/tcp_parse_named）
            修 macOS vs Ubuntu shux codegen 前缀差异（macOS 不加 driver_ · Ubuntu 加 driver_）
            其他 9 个 export function 已有 #[no_mangle] 属性
构建级别：L2（prove IDENTICAL 双端通过）；未跑 L4（待 C6 波 3 整波 L4）
产品 tip L4 钉盘：deaf773b（20 日升钉；前钉 305cfbe1；双端 L4 真冷 + 125 全绿 + Windows hybrid 门禁首次全绿）

R2 真迁结构（自举步骤.md §2 R2 full true-migrate 模板）：
  seed .from_x.c:
    #ifndef SHUX_L2_<MODULE>_FROM_X
      <full C bodies for all .x business funcs>
    #else
      <extern declarations only>
    #endif
    <slice_marker + Cap residual (language-limit) always compiled>

  surface .from_x.c:
    shux -E 产物抛光版（去 #include prologue + 加头部注释）
    用于 prove IDENTICAL 验证（.x→.o vs surface→.o 符号集相同）

跨平台 codegen 前缀差异（commit e55adc65 修复）：
  macOS ./shux 对 export function 不加前缀（tcp_eq_at）
  Ubuntu ./shux 对 export function 加 driver_ 前缀（driver_tcp_eq_at）
  #[no_mangle] 强制两平台 codegen 都不加前缀
  根因修复在 codegen ABI 属性层（非 symptom patch）

Cap residual（language-limit · 保留在 seed 不迁到 .x）：
  shu_target_cpu_print: FILE/fprintf（stdio 语言限制）
  OS detect 函数群: sysctl/proc/#if platform（平台分支语言限制）

双端 prove IDENTICAL 验证：
  macOS: SHUX=compiler/shux bash tests/prove_module_selfhost.sh target_cpu_pure
    → IDENTICAL（.x→.o vs surface→.o 符号集相同）
  Ubuntu: ssh ubuntu-server → git pull → cd compiler →
    SHUX=./shux bash ../tests/prove_module_selfhost.sh target_cpu_pure
    → IDENTICAL

PLATFORM: SHARED — .x 改动 + seed 改动跨平台一致
Ubuntu L4 验证待 C6 波 3 整波 L4 时一并跑

权威文档：analysis/自举进度.md §6 变更记录
历史进度归档：analysis/自举进度.md §6 变更记录
```
