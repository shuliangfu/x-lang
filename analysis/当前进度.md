# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`1f73ed275`**（双端 tip L4 真冷 + 129；未升 — 本波日常 L2） |
| tip 产品代码 | **wave100 g05 &fn → fn-as-\*u8** @ `035356e1` |
| 产品 bstrict | **129** · 钉盘 tip L4 全绿；wave100 **日常 L2** 矩阵绿（未全量 bstrict） |
| 本波性质 | **wave100 语言 residual：typeck bare fn→\*u8 + pure Cap-fn-ptr 脱 g05 cast** |
| Ubuntu | ✅ L2 g05 + pure fn_ptr **T**（`return ((uint8_t *)(pipeline_run_x_thread_fn))`；无 U g05 harness）+ 矩阵 rv42/option/si/hello + linux nest→11 + force_c @ `035356e1` · `/tmp/ubuntu_g05_wave100.log` · `/tmp/ubuntu_g05_wave100_matrix.log` |
| mac | ✅ L2 g05 + pure fn_ptr adrp 体 + 矩阵 rv42/option/si/hello + nest→11 + force_c @ `035356e1` · `/tmp/mac_g05_wave100.log` · `/tmp/mac_g05_wave100_matrix.log` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave100** @ 产品 **`035356e1`**  
   - 根因：`.x` 无法形成函数指针常量 → pure Cap-fn-ptr 仍 export-extern g05 `shux_driver_*_thread_fn_ptr`（&fn cast residual）  
   - 根修：typeck 同模块 bare 函数名 → Cap-fn-ptr `*u8`（CALL 路径仍按名解析）；pure thin 体 `(pipeline_run_x_thread_fn as *u8)` / 同理 asm elf_o；seed typeck pin 同语义  
   - 闭合硬 Cap residual「g05 &fn cast 产品 pure 面」叶  
   - soft residual：非 `#[no_mangle]` fn-as-value 仍发源名（codegen mangle）；ImportEntry 存储仍 Cap（ast_pool）  
   - **构建级别 L2**（日常真测；非 tip L4）  
2. 继承 tip L4 钉盘 **`1f73ed275`**（wave76–81 收口；wave82–100 未升钉）

## 下一波前排

1. rest R2：ImportEntry 存储 Cap residual（ast_pool）或 codegen fn-as-value mangle soft residual  
2. **可选** 整波 tip L4（wave82–100 收口升钉）  
3. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）

```text
钉盘：1f73ed275（tip L4 双端 129；本波未升）
产品 tip：035356e1 wave100 g05 &fn cast → language fn-as-*u8
文档：21 号归档 · 22 号 wave76–100 + tip L4 升钉 @1f73ed275
Windows：本波不需要切换
下一：ImportEntry Cap residual / fn-as-value mangle soft / 整波 tip L4
```
