# X 语言 / X language 当前进度（2026-07-29 · wave622 Cap pure-asm slice lit call-arg · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`f917861d7`**（wave622 Cap pure-asm slice lit call-arg · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave622（已闭 · Cap residual pure-asm slice lit call-arg · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | pure-asm `take([10,32])` 对 `[]i64`/`[]u8`/`[]f64`：`s[1]=0` / sum=10；i32/f32 绿；`let a: T[] = […]; take(a)` 绿；host-C `-backend c -o` 绿 |
| tip 产品 | **`f917861d7`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave622 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| call-arg 载荷 esz≠formal | `pipeline_asm_emit_expr_elf_for_call_args` wave332 | ✅ formal `force_esz` + durable（≡ let-init） |
| 仅帧尺寸用 formal esz | 旧路径 `emit_array_lit` 按 stamp | ✅ 优先 `glue_asm_emit_array_lit_durable_ptr_rax_elf_c` |

**数据流（五问）**：产生 = 未 stamp INT_LIT 默认 i32；存储 = wave332 只按 formal 预留栈、载荷仍 esz=4；消费 = INDEX 用 formal 宽 → 步长错。G.7：**补全** call-arg 到 let-init 同一 durable+force_esz 权威，**禁止**第二套 packer。

**权威**：`compiler/pipeline_glue.c`（call-arg ARRAY_LIT）。改后 **须** `PIPELINE_X_FORCE_COMPILE=1` `pipeline_x.o` + g05。

### wave622 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| pure-asm/fs `take([10,32])` i32/i64/u8/f32/f64 idx1=32 · sum=42 | ✅ | ✅ |
| pure-asm let→take 回归 | ✅ | ✅ |
| host-C `-backend c -o` i64/u8/f64 | ✅ | ✅ |
| rv / option / si / hello | ✅ | ✅ |

### 仍 soft

- host-C bare `-E` 缺 minimal preamble 的 `xlang_slice_*` 布局（`-backend c -o` / 全 preamble 绿）  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一叶（建议：host-C bare `-E` minimal preamble），或整波 L4 + 升钉  

## wave621（已闭 · Cap residual pure-asm f32 ordered cmp · L2）

tip 产品 **`2d81d0dff`** / docs **`93733710b`**。

## wave620（已闭 · Cap residual pure-asm float→int AS binop peel · L2）

tip 产品 **`6b7d3ba67`** / docs **`9d40d3bb1`**。
