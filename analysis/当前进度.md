# X 语言 / X language 当前进度（2026-07-29 · wave653 Cap freestanding loop-body final_expr + for continue→step · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 05:59 · 28 全日（wave534–617）已归楼；本文仅保留 29 日 tip 快照

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`07ee3bae0`**（wave653 loop final_expr + for continue→step） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave653（已闭 · Cap residual freestanding while/for continue · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `while_cont` ASI 无 `;`：`s=s+i` 为 while 体 final_expr → freestanding **fs=1**（host=12）；for+step+continue 被 final_expr 早退掩盖，修后暴露 continue 跳过 step 死循环 |
| tip 产品 | **`07ee3bae0`**（前 `402a4881b` final_expr · 本 tip 含 for step） |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave653 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 嵌套块 final_expr 恒 jmp 函数 tail_join | `glue_emit_block_final_expr_elf` | ✅ 仅 **function body** final_expr 允许 tail_join（if-expr arm 仍 depth 门控） |
| for continue 跳 cond head 跳过 step | `backend_emit_for_loop_elf_sync` | ✅ continue_label = **step** 标签（C for 语义） |

**数据流**：产生 = ASI 省略 `;` 使 trailing assign 成 while 体 `final_expr`；旧路径 emit 后 jmp `tail_join` → 只跑一轮（fs=1）。存储 = CFG。消费 = 运行时 exit code。G.7：单一 final_expr 权威 — 函数体才汇合 epilogue；while/for/if-stmt 嵌套块 fall-through 给父 CFG。连带：for continue 必须进 step，否则 `i=i+1` 永不执行。

**权威**：`compiler/pipeline_glue.c` · `glue_emit_block_final_expr_elf` · `backend_emit_for_loop_elf_sync`

### wave653 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| while_cont ASI（expect 12） | ✅ 12 | ✅ 12（was fs=1） |
| for+step+continue（expect 7） | ✅ 7 | ✅ 7（was hang/0） |
| break_cont / wb_full | ✅ 25 / 10 | ✅ 25 / 10 |
| binop while/for continue·break 回归 | ✅ | ✅ |
| pad wave652 回归 | ✅ 42 | ✅ 42 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |
| Ubuntu for disasm | — | continue → step（`i++`）→ jmp head |

日志：`/tmp/mac_g05_wave653*.log` · `/tmp/ubuntu_g05_wave653.log`

### 仍 soft

- untyped `self`（wave493 非标准 · **跳过**）  
- bare `break` 无 `;` 且后续还有 stmt 时 XP003（parse/ASI；与本波 freestanding CFG 分叶）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（bare break ASI / 其它），或整波 L4 + 升钉  

## wave652（已闭 · Cap residual x86 high-end STRUCT_LIT 固定数组字段 · L2）

tip 产品 **`be48b6acf`**。`glue_struct_field_frame_mag_c` x86 high-end base−foff。

## wave651（已闭 · Cap residual freestanding `m[i].xs` call-arg · L2）

tip 产品 **`a9256927e`**。FIELD over INDEX as T[N]/[]T call-arg。

## wave650（已闭 · Cap residual if/while cond 整式分组 · L2）

tip 产品 **`d5aec6a64`**。if/while 条件不剥前导 `(`。
