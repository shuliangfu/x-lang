# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave162** repo_root pure @ `e1bbddb96` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **R2 Cap residual pure · path residual**（`shux_repo_root_from_argv0` → L0 pure） |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：path residual — `shux_repo_root_from_argv0` 体仍 always mega C（compiler-dir parent + process.o 暖树回退）  
2. **根修 / pure**：真迁 `labi_path_pure.x` L0 pure orch（BSS 512 + pure `path_last_sep` 剥父目录 / 三 sep）；count 17→18  
3. **Cap residual**：`shu_resolve_compiler_dir` + `shux_rel_o_path_from_argv0`（平台 resolve / realpath·getcwd）  
4. **未动**：panic / async_scheduler / crt0_user / freestanding_io path ladders；freestanding ensure·path（cc/access Cap）  
5. **验收**：双端 L2 g05 + 矩阵 + string/vec/slice(20)/compress 邻域 + prove IDENTICAL；钉盘仍 **`3c88f7e51`**  

## 下一波前排

1. 其它 rest R2 / mega residual（可迁：freestanding ensure·path residual / complex path ladders panic·crt0·io·async_scheduler …；常驻 Cap 不硬迁：contains_substr / undef_sym / path_io stat / wait / strerror / mega spawn-IO / ld_debug_argv / invoke_cc_argv_push_existing realpath 池 / ensure_std_net_o_auto_tls system·make / getenv / shu_resolve_compiler_dir / `*_o_path` 静态缓冲 …）  
2. 可选：未闭合块注释硬诊断（EOF depth>0）  
3. 可选 Windows hybrid（**需要时再通知重启**）  

```text
钉盘：3c88f7e51（未升）
tip：e1bbddb96 wave162 repo_root pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：freestanding ensure·path residual / complex path ladders / 其它 rest R2
```
