# X 语言 / X language 当前进度（2026-07-27 · wave516）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`d9619af42`**（双端 L2，wave516；L4 待整波收口） |
| 钉盘 | **`2bc0885d3`**（待 Ubuntu 钉盘升级） |
| bstrict | **129** |

## wave516（已闭 · runtime_net_io_batch_fast R2 migration thin+rest 双端 L2）

| 项 | 状态 |
|----|------|
| 触发 | wave515 完成后，继续剩余纯 C seed 模块 R2 migration |
| 模块 | `runtime_net_io_batch_fast`（96 行，8 函数：3 weak io_* + 3 stream + 2 UDP） |
| 债 | `.x` 仅 doc anchor；8 函数全在 C；嵌入 net.o 复合构建规则 |
| 根修 | ① `.x`：8× `#[no_mangle]` thin wrappers + 2 `extern "C" function` _impl bridges + 英文 docblock ② `.from_x.c`：`_impl_c` 桥始终编译；`#ifndef XLANG_RUNTIME_NET_IO_BATCH_FAST_FROM_X` 守卫 cold-only `_c` wrappers + weak io_* ③ `Makefile` net.o 规则加 R2 路径（thin+rest `ld -r`） |
| 构建模式 | **thin+rest**（非 DIRECT）—— UDP 批量函数需平台专属 syscall（recvmmsg/sendmmsg），`.x` 无法表达 `#if defined(__linux__)` |
| nm 一致性 macOS | cold vs R2：10 核心 T 符号对齐（仅 doc_anchor 多余） |
| nm 一致性 Ubuntu | cold vs R2：10 核心 T+W 符号对齐（仅 doc_anchor 多余） |
| 产品矩阵 macOS | hello ✅ · g-ffi-5-std-wrap ✅ |
| 产品矩阵 Ubuntu | hello ✅ · g-ffi-5-std-wrap ✅ · freestanding-hello baseline mismatch（与 wave516 无关） |
| bstrict macOS | 部分 83 OK / 0 FAIL（被中断；L4+bstrict 延至整波收口） |
| 验证级别 | **L2 日常真测**（按自举方法 §0.2 节奏；非每微 commit 全量 L4） |
| PLATFORM | SHARED net · **双端 L2 验证通过** |
| 关键发现 | ① `extern "C" function` 在 Xlang codegen 会加 `_c` 后缀，rest C 端 `_impl` 须改名 `_impl_c` ② `_impl` 桥必须始终编译（不能被 `#ifndef FROM_X` 守卫排除），否则 rest.o 为空 ③ weak 符号在 macOS `nm` 显 `T`、Linux 显 `W`，对比须用 `[TW]` |
