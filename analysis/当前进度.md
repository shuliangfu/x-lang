# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave154** invoke_cc append_compress pure @ `323917cd9` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **R2 Cap residual pure · invoke_ld L6**（`invoke_cc_append_compress_ld`） |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：其它 rest R2 可迁叶 — `invoke_cc_append_compress_ld` hybrid 下仍 always mega C（needs_* + brew + -l* + push_existing glue）  
2. **根修**：迁入 `labi_invoke_ld_list` L6 pure orch（peer pure needs_* + pure brew/flag + Cap residual ensure/path + Cap residual `invoke_cc_argv_push_existing`）  
3. **权威**：`.x` + cold twin（`#include`）+ surface re-pin + mega 仅 forward；prove **IDENTICAL**  
4. **注意**：与 wave153 `asm_ld_append_compress_libs` 同 needs/brew/flag；glue 路径 asm 直 append、cc 走 push_existing（realpath/skip/dedup 仍 Cap residual）  
5. **验收**：双端 L2 g05 + 矩阵 + string/vec/slice/compress 邻域 + prove IDENTICAL；钉盘仍 **`3c88f7e51`**  

## 下一波前排

1. 其它 rest R2 / mega residual（可迁：其它 invoke_ld / path / freestanding residual …；常驻 Cap 不硬迁：contains_substr / undef_sym / path_io stat / wait / strerror / mega spawn-IO / ld_debug_argv / invoke_cc_argv_push_existing realpath 池 …）  
2. 可选：未闭合块注释硬诊断（EOF depth>0）  
3. 可选 Windows hybrid（**需要时再通知重启**）  

```text
钉盘：3c88f7e51（未升）
tip：323917cd9 wave154 invoke_cc append_compress pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：其它 rest R2 / mega residual
```
