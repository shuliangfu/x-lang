# X 语言 / X language 当前进度（2026-07-29 · wave682 STRUCT_LIT type-param mono · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 05:59 · 28 全日（wave534–617）已归楼；本文仅保留 29 日 tip 快照

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`2e151198a`**（wave682 STRUCT_LIT type-param field mono） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave682（已闭 · Cap residual STRUCT_LIT type-param field mono · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `let w: Wrap<i32> = Wrap { val: 42 }` / `Pair { a,b }` / nest `Box<Inner>` typeck 红 *expected T, found i32*（layout 字段仍自由 type-param；codegen mono 已有） |
| tip 产品 | **`2e151198a`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave682 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| STRUCT_LIT 字段 coerce 比 layout `T` | `typeck_coerce_struct_lit_field_inits_to_layout` 无 mono | ✅ 期望基 `Wrap<i32>` → `pipeline_typeck_mono_field_type_from_base_c` |
| field-access mono 双份 | heavy apply_mono 内联 | ✅ 抽出同一权威，apply_mono 转调 |
| bool→i32 字段假绿 | 代入后仍 hard-fail | ✅ `Wrap { val: true }` → expected i32 found bool |

**数据流**：产生=generic layout `v: T` + 上下文 `Name<Concrete>`。存储=type-arg sidecar + layout type-param 名。消费=STRUCT_LIT field coerce / field-access stamp。G.7：补全 field_access mono 权威并接线 coerce；seed typeck_gen/empty_surface 同 commit。

**权威**：`pipeline_typeck_mono_field_type_from_base_c`（`pipeline_typeck_field_access.c`）· `typeck_coerce_struct_lit_field_inits_to_layout(..., base_ty)`

### wave682 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| Wrap/Pair/nest/ret STRUCT_LIT | ✅ 42 | ✅ 42 |
| Wrap val:true mismatch | T001 expected i32 found bool | 同 |
| concrete S / geta&lt;S&gt; / redef | ✅ 42/1 | ✅ |
| wave674 miss / LANG-006 | T001 / run=1 | 同 |
| 矩阵 rv/option/hello/si | ✅ | ✅ |

日志：`/tmp/mac_g05_wave682c.log` · `/tmp/ubuntu_g05_wave682.log` · `/tmp/ubuntu_w682_probes.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**；P011 更早失败）  
- LANG-006 标量 let/const bool→int（**产品保留**）  
- freestanding multi-method bare `get` UNDEF（既有 soft）  
- bare type-param 接收者上的未知字段（无 mono 上下文仍 soft）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self；候选 fs multi-method mangle），或整波 L4 + 升钉  

## wave681（已闭 · Cap residual redef first-wins structural type equality · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | 同模块同名同 arity 有参重定义 / 同 impl 同签名 method typeck 绿 → host BLD001 redefinition（wave377 零参 first-wins 已绿） |
| tip 产品 | **`d9bf89f7c`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave681 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 有参 free redef BLD001 | `codegen_should_skip_later_same_name_body` 比 type_ref **身份** | ✅ 结构相等 `pipeline_typeck_type_refs_equal_c` |
| 同 impl 同签名 method BLD001 | 同上（每 parse 点新 type_ref 槽） | ✅ 同上 + return 结构相等 |
| 真 overload（pick i32/i64） | 类型不同应双 emit | ✅ 结构不等 → 仍 emit |
| 零参 first-wins | 无 type 环 | ✅ 回归 run=1 |

**数据流**：产生=同名+同 arity+结构同签名的第二 body。存储=module.funcs。消费=host-C 同 mangle 双 strong body → BLD001。G.7：补全 wave377 同一 helper；seed `codegen_gen.linux.x86_64.c` 同 commit。

**权威**：`codegen.x` / seed `codegen_should_skip_later_same_name_body(arena, module, fi)` + `pipeline_typeck_type_refs_equal_c`

### wave681 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| method redef first-wins | ✅ 41（host） | ✅ 41（`-backend c`） |
| free param / zero redef first-wins | ✅ 1 / 1 | ✅ 1 / 1（fs+host） |
| overload i32+i64 | ✅ 9 | ✅ 9 |
| ok method / two_impl / method arity / only_dup | ✅ 42 | ✅ host 42；only_dup fs=42 |
| 矩阵 rv/option/hello/si | ✅ | ✅ |

日志：`/tmp/mac_g05_wave681b.log` · `/tmp/ubuntu_g05_wave681.log` · `/tmp/ubuntu_w681_probes.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**；P011 更早失败）  
- LANG-006 标量 let/const bool→int（**产品保留**）  
- type-param NAMED 无 layout 的字段（generic mono soft）  
- freestanding multi-method bare `get` UNDEF（模块内 ≥2 个同名 method 时 call 未 mangle；既有 soft，非本波 host-C 债）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self；候选 type-param field），或整波 L4 + 升钉  

## wave680（已闭 · Cap residual same-block let/const redecl hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `let x; let x` / `const x` 两次 / let+const 同名 / 函数体 `let` 与形参同名 typeck 绿 → host BLD001 redefinition |
| tip 产品 | **`ea3b1c22c`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave680 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 同块重复 let/const | typeck one_let/one_const 不查重 | ✅ `pipeline_block_local_name_redecl_c` → T001 |
| 函数体 let 与形参同名 | C 同 scope 无 shadow | ✅ body_ref 对齐时扫 params |
| 嵌套块 shadow | 合法 | ✅ 仅同 block / body-param |
| stack smash | copy64 写 128 字节 | ✅ 缓冲 128B |

**数据流**：产生=同块二次 let/const 或 body 与 formal 同名。存储=Block lets/consts + func params。消费=host-C redefinition。G.7：`pipeline_block_local_name_redecl_c` + one_let/one_const 门；diag `duplicate_local`；seed typeck_gen/empty_surface + diagnostic twin 同 commit。

**权威**：`ast_pool.c`（redecl helper）· typeck.x / typeck_gen / empty_surface · `driver_diagnostic_typeck_duplicate_local`（thin+full+seed）· `patch_typeck_gen_lang007.py` weak call/deref/method

### wave680 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| dup_let / dup_const / let_const / param_let / only_dup | T001 duplicate_local | 同 |
| shadow nested / ok_let / const_ok / LANG-006 | ✅ 42/1 | 同 |
| wave678 const assign | T001 | 同 |
| 矩阵 rv/option/hello/si | ✅ | 同 |

日志：`/tmp/mac_g05_wave680c.log` · `/tmp/ubuntu_g05_wave680c.log` · `/tmp/ubuntu_w680_probes.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**；P011 更早失败）  
- LANG-006 标量 let/const bool→int（**产品保留**）  
- type-param NAMED 无 layout 的字段（generic mono soft）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self；候选 type-param field），或整波 L4 + 升钉  

## wave679（已闭 · Cap residual dup param/field hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `f(x:i32,x:i32)` / `struct S{a:i32,a:i32}` parse+typeck 绿 → host BLD001 |
| tip 产品 | **`d36f5d447`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave679 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 同名形参假绿 | append_param 不查重 | ✅ 追加前 `parser_onefunc_param_name_dup_c` → P012 |
| 同名字段假绿 | set_field 不查重 | ✅ 登记前扫 0..nf-1 → P012 |
| soft-skip 假绿 | layout/func fail 后 continue | ✅ sticky `g_parser_sig_type_hard` → ok=-2 |

**数据流**：产生=定义侧同名 param/field。存储=OneFunc sidecar / struct layout 表。消费=host-C redefinition / duplicate member。G.7：`parser_report_duplicate_name_p012_c` + 同 sticky 于 P011；parser.x/seed/buf/struct_layout 同门。

**权威**：`parser_asm_body_tl_slice.inc`（P012 + param_dup）· `parser_asm_struct_layout_slice.inc`（field）· parser.x / parser_gen / seed_parse_into_buf sticky

### wave679 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| dup_param / only_def / dup_field / only_field | P012 + ok=-2 | 同 |
| ok_param / ok_field | ✅ 42 | 同 |
| wave678 const / LANG-006 | T001 / ✅ | 同 |
| 矩阵 rv/option/hello/si | ✅ | ✅ |

日志：`/tmp/mac_g05_wave679b.log` · `/tmp/ubuntu_g05_wave679.log` · `/tmp/ubuntu_w679_probes.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**；P011 更早失败）  
- LANG-006 标量 let/const bool→int（**产品保留**）  
- type-param NAMED 无 layout 的字段（generic mono soft）  
- dup_func first-wins（wave377 产品语义）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self），或整波 L4 + 升钉  

## wave678（已闭 · Cap residual const assign hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `const x: i32 = 1; x = 2` / `x += 1` / 顶层 `const N` 再赋值 typeck 绿 |
| tip 产品 | **`0a5d3a672`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave678 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| const 再赋值假绿 | assign 未区分 const/let | ✅ VAR LHS 块链+顶层 const → T001 |
| 复合 `const +=` | 同门 | ✅ |
| 诊断 | 无专用文案 | ✅ `assign_to_const` |

**数据流**：产生=`const` 绑定后 `=`/`+=` 等。存储=block ConstDecl / top_level is_const。消费=host 可假改写。G.7：`pipeline_block_name_binding_kind` + `pipeline_module_top_level_name_is_const`；产品 mega `pipeline_typeck_check_expr_assign_c` + typeck.x/seed 孪。let 仍可赋值；`mut` 仅拼写（docs/06）。

**权威**：`pipeline_typeck_check_expr_assign_c`（pipeline_glue）· typeck.x / seed typeck_gen/empty_surface · `driver_diagnostic_typeck_assign_to_const`（thin+full+seed）· ast_pool helpers

### wave678 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| body/nested/top const `=` / `+=` | T001 assign_to_const | 同 |
| `let x=1; x=2` / `const x=42; return x` | ✅ | ✅ |
| LANG-006 `let x:i32=true` / wave677 bool arith | ✅ / T001 | 同 |
| 矩阵 rv/option/hello/si | ✅ | ✅ |

日志：`/tmp/mac_g05_wave678g.log` · `/tmp/ubuntu_g05_wave678.log` · `/tmp/ubuntu_w678_probes.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**；P011 更早失败）  
- LANG-006 标量 let/const bool→int（**产品保留**）  
- type-param NAMED 无 layout 的字段（generic mono soft）  
- dup_func first-wins（wave377 产品语义）/ dup_param·dup_field host BLD001  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self），或整波 L4 + 升钉  

## wave677（已闭 · Cap residual bool arith hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `true+false` / `true*2` / `x<<true` / `x&true` / `true^false` / `-true` typeck 绿 → 假跑 |
| tip 产品 | **`a51f44eb6`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave677 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| binop bool→i32 假绿 | `typeck_check_expr_binop_arith` 末尾 `allow_i32_fallback` 把 TYPE_BOOL 提升为 i32 | ✅ 任一侧 bool → T001 |
| unary `-true` / `~true` 假绿/晚失败 | unary 未拒 bool | ✅ -/~ 同拒 |
| 诊断 | 无专用文案 | ✅ `invalid_bool_binop` |

**数据流**：产生=bool 参与 + - * / % << >> & | ^ 或 unary -/~。存储=历史 bool→i32 提升。消费=host/fs 按 int 假跑。G.7：补全 `typeck_check_expr_binop_arith` + unary；删 bool→i32 提升；**LANG-006 let/const 保留**。

**权威**：`typeck_check_expr_binop_arith` / `typeck_check_expr_unary`（typeck.x + seed typeck_gen/empty_surface）· `driver_diagnostic_typeck_invalid_bool_binop`（thin+full+seed）

### wave677 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `true+false` / `*2` / `<<true` / `&` / `^` / `-true` / `~true` / `b+1` | T001 invalid bool | 同 |
| `true&&false` / `!false` / `true==false` / `(b as i32)+1` / `x<<2` | ✅ | 同 |
| LANG-006 `let x:i32=true` | ✅ | ✅ |
| `return true` / void+int | T001 / T001 | 同 |
| 矩阵 rv/option/hello/si | ✅ | ✅ |

日志：`/tmp/mac_g05_wave677.log` · `/tmp/ubuntu_g05_wave677.log` · `/tmp/ubuntu_w677_probes.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**；P011 更早失败）  
- LANG-006 标量 let/const bool→int（**产品保留**）  
- type-param NAMED 无 layout 的字段（generic mono soft）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self），或整波 L4 + 升钉  

## wave676（已闭 · Cap residual untyped formal / missing ret hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `function bad(x): i32` / `function bad(x)` 静默丢弃或假绿；仅 def 时 main 仍绿 |
| tip 产品 | **`9897f7759`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave676 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 无类型形参 silent drop | `set_onefunc_fail` 无诊断；buf 路径允许 `name)` 无 `: Type` | ✅ P011 + sticky hard abort |
| 缺 return `: Type` silent drop | 同上 | ✅ P011 kind=1 |
| parse_into_buf soft-skip | `!res.ok` continue → only_def 假绿 | ✅ sticky → ok=-2 |

**数据流**：产生=param 缺 `:` / return 缺 `:`。存储=OneFunc fail + 无 AST 函数。消费=soft-skip 后 main 仍绿或 call T001。G.7：`parser_report_untyped_formal_p011_c`（body_tl）+ call sites parser.x/seed/buf；sticky `parser_sig_type_hard_*`；parse_into_buf abort。

**权威**：`parser_report_untyped_formal_p011_c` · `parser.x` + `seeds/parser_gen.linux.x86_64.c` + `one_function_buf_slice` + seed/parser_gen `parse_into_buf` sticky

### wave676 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `bad(x): i32` / only_def / missing ret | P011 + P001 ok=-2 | 同 |
| typed `good(x: i32): i32` | ✅ | 同 |
| wave675 unresolved / wave674 field / P010 / LANG-006 | T001 / T001 / P010 / ✅ | 同 |
| 矩阵 rv/option/hello/si | ✅ | ✅ |

日志：`/tmp/mac_g05_wave676c.log` · `/tmp/ubuntu_g05_wave676.log` · `/tmp/ubuntu_w676_probes.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**；现 P011 更早失败，非产品语义）  
- LANG-006 标量 let/const bool→int（产品保留）  
- type-param NAMED 无 layout 的字段（generic mono soft）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self），或整波 L4 + 升钉  

## wave675（已闭 · Cap residual unresolved CALL hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `use_x(1)` 未定义 / `function bad(x)` 静默丢弃后 `bad(1)` typeck 绿 → host BLD001 undeclared |
| tip 产品 | **`e7e783a3b`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave675 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 未解析 bare VAR CALL soft | `typeck_check_call_arity` 仅 `name_hits>0&&arity_hits==0` 硬拒 | ✅ `name_hits==0` → T001 |
| 无类型形参函数静默丢弃 + call | parse onefunc fail 丢函数；call 仍 emit 名 | ✅ 同 gate（call 侧 T001） |
| 诊断 | 无 unresolved 文案 | ✅ `driver_diagnostic_typeck_call_unresolved` |

**数据流**：产生=CALL resolve 后 `fi<0` 且本地 `name_hits==0`。存储=无 resolved_func。消费=codegen 按名 emit → host undeclared。G.7：补全 wave660 同一 gate；read_ptr_slice 与 non-VAR soft；param_raw≤0 仍 soft（调用已 resolve 时）。

**权威**：`typeck_check_call_arity`（typeck.x + seed typeck_gen/empty_surface）· diag thin/full/seed 同 commit

### wave675 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `use_x(1)` 未定义 | T001 unresolved | 同 |
| 无类型形参 `bad(x)` + `bad(1)` | T001 unresolved | 同 |
| 有类型 `use_x(x:i32)` 调用 | ✅ run=1 | 同 |
| wave660 arity / wave661 arg / wave674 field | T001 | 同 |
| LANG-006 `let x:i32=true` | ✅ | 同 |
| 矩阵 rv/option/hello/si | ✅ | ✅ |

日志：`/tmp/mac_g05_wave675.log` · `/tmp/ubuntu_g05_wave675.log` · `/tmp/ubuntu_w675_probes.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**）  
- untyped formals 定义侧（param 无类型 → parse 静默丢函数；call 已 T001）  
- LANG-006 标量 let/const bool→int（产品保留）  
- type-param NAMED 无 layout 的字段（generic mono soft）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self），或整波 L4 + 升钉  

## wave674（已闭 · Cap residual unknown field hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `s.nope=1` / `a.nope` / `s.nope` slice / `E.Nope` typeck 绿 → host BLD001 |
| tip 产品 | **`70a23295d`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave674 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 已知基类型字段 miss soft 0 | `pipeline_typeck_check_expr_field_access_c` 恒 return 0 | ✅ `pipeline_typeck_field_unknown_hard_fail_c` |
| 产品 weak twin 非 NAMED 早 return | strict_minimal 见非 NAMED 即 0 | ✅ slice/array/scalar 先 hard_fail |
| assign 对 null LHS field stamp RHS | 根修在 field resolve（非 assign 补丁） | ✅ 字段 resolve 硬拒后不再 stamp |

**数据流**：产生=FIELD_ACCESS layout miss。存储=resolved_type null + assign 把 RHS 印到 LHS。消费=host-C `no member named`。G.7：单 gate `pipeline_typeck_field_unknown_hard_fail_c`；heavy + weak twin 同调；type-param 无 layout 仍 soft。

**权威**：`pipeline_typeck_field_access.c` · seed `pipeline_glue_strict_minimal.from_x.c`（产品 weak twin）

### wave674 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `s.nope=` / compound / read / nest / `*S.nope` | T001 unknown field | 同 |
| `a.nope` / `slice.nope` / `x.nope` i32 | T001 | 同 |
| `E.Nope` | T001 enum has no variant | 同 |
| `s.n` / `s.length` / `a.length` / enum ok / method | ✅ | ✅ |
| wave673 call bool / LANG-006 let | T001 / ✅ | 同 |
| 矩阵 rv/option/hello/si | ✅ | ✅ |

日志：`/tmp/mac_g05_wave674*.log` · `/tmp/ubuntu_g05_wave674.log` · `/tmp/ubuntu_w674_probes.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**）  
- untyped formals（param_raw≤0 soft-skip）  
- LANG-006 标量 let/const bool→int（产品保留）  
- type-param NAMED 无 layout 的字段（generic mono soft）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self），或整波 L4 + 升钉  

## wave673（已闭 · Cap residual call unknown arg_ty hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `f(s.nope)` / `f(g(1))` / `f(x.y)` typeck 绿 → host BLD001 |
| tip 产品 | **`5bb4f8112`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave673 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| sc&lt;0 且 arg_ty≤0 soft-skip | `typeck_check_call_arg_types` wave661 门控 | ✅ 形参已知时 sc&lt;0 一律 T001 |
| lit/null 无 stamp 假红风险 | score 本就支持 lit/string/null→*T | ✅ 仍走 score 成功路径，不依赖 arg_ty |

**数据流**：产生=CALL 实参未 resolve 类型但 score 已 -1。存储=wave661「未知 arg_ty soft-skip」。消费=host-C BLD001。G.7：补全同一 `typeck_check_call_arg_types`；param_raw≤0 仍 soft。

**权威**：`typeck.x` · seed `typeck_gen` / `typeck_empty_surface`（glue 已转调此 gate）

### wave673 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `f(s.nope)` / `f(g(1))` / `f(x.y)` | T001 | T001 |
| bool/f32/null→i32 call | T001 | T001 |
| lit/widen/`f(null)` *T / `f(0)` *T / field / arr call | ✅ | ✅ |
| wave672 arr bool call / LANG-006 let / ret true | T001 / ✅ / T001 | 同 |
| 矩阵 rv/option/hello/si | ✅ | ✅ |

日志：`/tmp/mac_g05_wave673.log` · `/tmp/ubuntu_g05_wave673.log` · `/tmp/ubuntu_w673_probes.log`

### 仍 soft（wave673 闭时；unknown field 已由 wave674 闭）

- untyped `self`（wave493 · **跳过**）  
- untyped formals（param_raw≤0 soft-skip）  
- LANG-006 标量 let/const bool→int（产品保留）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self），或整波 L4 + 升钉  

## wave672（已闭 · Cap residual array/struct lit elem mismatch · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `let a:i32[2]=[true,false]` / `S{v:true}` / `f([true,false])` 假绿 run=1 |
| tip 产品 | **`8f3908d75`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave672 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| ARRAY_LIT 外层 stamp 不顾元素 | `typeck_coerce_array_lit_elem_types_to_decl` 恒 stamp outer | ✅ 已知 elem 不匹配硬失败；不 stamp outer |
| STRUCT_LIT 字段 bool→int | 全量 `coerce_init_expr` 含 LANG-006 + 无字段比对 | ✅ 字段路径无 bool→int；equal/widen 门控 |
| call `f([true])` 同 kind 弱分 | score `ak==pk` 对 TYPE_ARRAY 不比 elem | ✅ ARRAY/SLICE 须 elem（+size）匹配 |
| -1 被 let 当成功 | `coerce_init_expr` `!=0→1` | ✅ 传播 -1；let/const 检 |

**数据流**：产生=ARRAY_LIT/STRUCT_LIT 元素/字段类型已知但 ≠ 声明元素/字段。存储=outer stamp / score 弱分。消费=let/assign/call。G.7：coerce 权威 + score 权威；LANG-006 仅标量 let/const。

**权威**：`typeck.x` coerce_array_lit / struct_field / overload_arg_param_score · seed typeck_gen/empty_surface · strict_minimal score twin

### wave672 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `i32[2]=[true,false]` / `[1.0 as f32]` | T001 | T001 |
| `i32[2]=[1,2]` / `f32[2]` lit | ✅ | ✅ |
| `S{v:true}` / `v:f32 as i32` | T001 | T001 |
| `S{v:42}` | ✅ 42 | ✅ 42 |
| `f([true,false])` i32[2] | T001 | T001 |
| `f([10,32])` | ✅ 42 | ✅ 42 |
| `let x:i32=true`（LANG-006） | ✅ | ✅ |
| `return true` as i32（wave671） | T001 | T001 |
| 矩阵 rv/option/hello/si | ✅ | ✅ |

日志：`/tmp/mac_g05_wave672*.log` · `/tmp/ubuntu_g05_wave672.log` · `/tmp/ubuntu_w672_probes2.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**）  
- 未知 arg_ty（**wave673 已闭**）  
- LANG-006 标量 let/const bool→int（产品保留）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self），或整波 L4 + 升钉  

## wave671（已闭 · Cap residual return bool→i32 hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `return true` / `return !x` 声明 `: i32` 假绿；`return (1==1)` / bool VAR 已 mismatch |
| tip 产品 | **`e6db3cddf`**（链 `4d0e37918` typeck/seed + `e6db3cddf` glue twin） |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave671 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| BOOL_LIT/LOGNOT→i32 return 特例 | `typeck_return_operand_matches` `kop==2\|\|24` | ✅ 删除；bool→非 bool return 硬失败 |
| Ubuntu 产品路径仍假绿 | glue `pipeline_typeck_return_operand_matches_c` 双权威 | ✅ 同策略硬失败（G.7 twin） |
| 边界用例未启 | `tests/return-expr/return_type_mismatch.x` 断言注释 | ✅ `run-return-expr.sh` 启用 |

**数据流**：产生=return 操作数 TYPE_BOOL + expect i32。存储=历史 BOOL_LIT/LOGNOT 白名单。消费=typeck.x 与 glue return 双路径。G.7：双 twin 同语义；`as i32` 与 LANG-006 let/const bool→int 保留。

**权威**：`typeck.x` / seed `typeck_gen` / empty_surface · `pipeline_glue.c` `pipeline_typeck_return_operand_matches_c`

### wave671 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `return true` / `return false` as i32 | T001 mismatch | T001 mismatch |
| `return !false` / `return !true` as i32 | T001 | T001 |
| `return true as i32` | ✅ 1 | ✅ 1 |
| `return 42` / 矩阵 rv/option/hello/si | ✅ | ✅ |
| `let x:i32=true`（LANG-006 let） | ✅ | ✅ |
| `run-return-expr.sh`（含 mismatch） | ✅ | ✅ |

日志：`/tmp/mac_g05_wave671*.log` · `/tmp/ubuntu_g05_wave671*.log` · `/tmp/ubuntu_w671_probes2.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**）  
- 未知 arg_ty（wave661 soft-skip）  
- LANG-006 let/const bool→int coerce（产品 `const V: i32 = …` 邻域；非 return）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self），或整波 L4 + 升钉  

## wave670（已闭 · Cap residual null→non-ptr hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `null` 降为 lit 0 后 let/assign/return/call/cmp 对非指针假绿（与 `=0` 同） |
| tip 产品 | **`e1783964f`**（链 ee0ceba68 → 0efd23f25 → 85f426f78 → e1783964f） |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave670 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| null ≡ bare INT 0 无标记 | wave668 故意 EXPR_LIT 0 | ✅ var_name="null" 标签 + `pipeline_expr_tag_null_keyword_c` 写 live ast_Expr |
| let/assign/return 非指针假绿 | coerce + 默认 i32 stamp | ✅ null 仅 TYPE_PTR coerce；非 ptr 硬失败 |
| call `f(null)` i32 假绿 | score 先 exact 1000 | ✅ call_arg 在 score 前硬拒 null→non-ptr |
| cmp `null==i32` Ubuntu 假绿 | 门控嵌在 both-typed 内 | ✅ 操作数 check_expr 后立刻门控 |
| primary memcpy 丢标签（gcc） | parser_asm↔ast_Expr 布局 | ✅ tag 在 set 后走 glue 真指针 |

**数据流**：产生=parser TOKEN_NULL → tag。存储=EXPR_LIT + var_name 标记。消费=typeck let/assign/return/call/cmp。G.7：`pipeline_expr_is_null_keyword_c` 单检测；裸 `0` 不变。

**权威**：`parser_asm_primary_slice` + `parser_alloc_null_lit` · `pipeline_glue` tag/is_null · `typeck.x`/`typeck_gen` gates

### wave670 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `let x:i32=null` / f32/struct | T001 | T001 |
| `x=null` / `return null` as i32 | T001 | T001 |
| `f(null)` f:i32 | T001 | T001 |
| `null==i32` / `i32==null` | T001 | T001 |
| `let p:*T=null` / `p==null` / `f(null)` *T | ✅ 1 | ✅ 1 |
| `let x:i32=0` / `null==null` | ✅ | ✅ |
| 矩阵 rv/option/hello | ✅ | ✅ |

日志：`/tmp/mac_g05_wave670*.log` · `/tmp/ubuntu_g05_wave670*.log` · `/tmp/ubuntu_w670_*.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**）  
- 未知 arg_ty（wave661 soft-skip）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self），或整波 L4 + 升钉  

## wave669（已闭 · Cap residual freestanding lit-left ptr cmp · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Ubuntu freestanding `null==p` / `0==p` 在 p 非空时假绿 run=8；`p==null` 与 host-C 绿 |
| tip 产品 | **`fc9c3bd53`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave669 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| VAR slot cache 假命中 | `glue_try_binop_cmp_rbx_rax` 右→rax 后 emit 左 imm 覆盖 rax 未 invalidate | ✅ invalidate_rax + invalidate_rbx 后重载右 |
| 无 lit-left 快路径 | `pipeline_asm_emit_cmp_elf` 仅 right-lit | ✅ 对称 left imm→rbx / right→rax |
| is_cmp_64bit 只看左 | lit-left 时宽可能丢 | ✅ 兼取右侧 ptr/i64 kind |

**数据流**：产生 = freestanding cmp 双槽装载顺序 + cache。存储 = `glue_binop_var_slot_cache.valid_rax`。消费 = `cmp` setcc 假 equal。G.7：补全 `emit_cmp` 权威；不新开第二套 cmp loader。

**权威**：`compiler/pipeline_glue.c`（`pipeline_asm_emit_cmp_elf` + `glue_try_binop_cmp_rbx_rax_elf_c`）

### wave669 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `null==p` / `0==p`（p=&x 非空） | ✅ 1 | ✅ 1（原 run=8） |
| `p==null` | ✅ 1 | ✅ 1 |
| `p==0` / `0==p`（p=null） | ✅ 1 | ✅ 1 |
| `null==null` / `5==x` | ✅ 1 | ✅ 1 |
| host-C lit-left · 矩阵 rv/option/hello | ✅ | ✅ |

日志：`/tmp/mac_g05_wave669.log` · `/tmp/ubuntu_g05_wave669.log` · `/tmp/ubuntu_w669_probes.log`

### 仍 soft

- untyped `self`（wave493 非标准 · **跳过**）  
- 未知 arg_ty（wave661 soft-skip）  
- `null` 赋值到非指针类型仍可能按 lit 0 绿（与 `=0` 同；严格拒留 soft）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self；其它 soft residual），或整波 L4 + 升钉  

## wave668（已闭 · Cap residual null keyword · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | 关键字 `null` 被当未解析 IDENT → XT001 假红（`let p:*T=null` / `p==null` / `f(null)` / `return null`） |
| tip 产品 | **`41f26d92b`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave668 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 无 TOKEN_NULL | token 枚举末尾 + lexer keyword | ✅ ordinal **132**（不挤 IDENT=59/TRUE=75） |
| primary 不识 null | `parser_asm_primary_slice` + let-init | ✅ → EXPR_LIT 0 |
| 实参 `0`/`null`→*T 假红 | `typeck_overload_arg_param_score` | ✅ lit 0 + TYPE_PTR score 100 |
| let/cmp/ret 0→*T | 既有 coerce | ✅ 复用，无第二套 null 语义 |

**数据流**：产生 = lexer 把 `null` 当 IDENT。存储 = 无字面量。消费 = typeck 无法 resolve。G.7：null ≡ EXPR_LIT 0；score/coerce 单路径；seed lexer/parser/typeck + strict_minimal 同 commit。

**权威**：`token.h`/`token.x` · `lexer.x` · `parser_asm_primary_slice.inc` · `typeck.x` score · seeds 孪

### wave668 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `let p:*i32=null` / `p==null` | ✅ 1 | ✅ 1 |
| `null==p` / `0==p`（host-C） | ✅ 1 | ✅ 1（`-backend c`） |
| `null==p` / `0==p`（Ubuntu freestanding） | mac fs ✅ | ⚠ 当时假绿（**wave669 已闭**） |
| `f(null)` / `f(0)` *T 实参 | ✅ 1 | ✅ 1 |
| `return null` as *T | ✅ 1 | ✅ 1 |
| true/false · void arith 回归 · 矩阵 | ✅ | ✅ |

日志：`/tmp/mac_g05_wave668.log` · `/tmp/ubuntu_g05_wave668.log` · `/tmp/ubuntu_w668_probes.log`

## wave667（已闭 · Cap residual void arithmetic / unary hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `f():void` 参与 `+ - * / % &` 或 unary `-`/`~` → typeck OK 后 host-C BLD001（`void` operands） |
| tip 产品 | **`5a7e34524`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave667 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| void 操作数 arith fallthrough stamp | `typeck_check_expr_binop_arith` | ✅ 任一侧 TYPE_VOID 硬失败 |
| unary `-`/`~` 对 void stamp | `typeck_check_expr_unary` | ✅ TYPE_VOID 硬失败 |
| LOGNOT on void | wave665 non-bool | ✅ 已由 bool 门覆盖 |

**数据流**：产生 = void 调用结果类型未在 arith/unary 门控。存储 = typeck 过。消费 = host-C BLD001。G.7：`driver_diagnostic_typeck_invalid_void_binop`；seed typeck_gen/empty_surface + diagnostic twin 同 commit。

**权威**：`compiler/src/typeck/typeck.x` · seed `typeck_gen` + empty_surface · diagnostic twin 同 commit

### wave667 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `f()+1` / `* / - / &` / `-f()` / `~f()` | ✅ T001 hard | ✅ T001 hard |
| bare `f()` / int arith / unary int / land bool / `p+i` | ✅ 42/66 | ✅ 42/66 |
| cmp_mixed 回归 | ✅ T001 | ✅ T001 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

日志：`/tmp/mac_g05_wave667.log` · `/tmp/ubuntu_g05_wave667.log` · `/tmp/ubuntu_w667_probes.log`

### 仍 soft（wave667 闭时；null keyword 已由 wave668 闭）

- untyped `self`（wave493 非标准 · **跳过**）  
- 未知 arg_ty（wave661 soft-skip）  
- 整波双端 **L4** + 升钉  

## wave666（已闭 · Cap residual mixed-type comparison hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `i32 == f32` / `i32 == i64` / `i32 == bool` / `*i32 == *u8` → typeck OK 后 freestanding/host C 提升假绿 |
| tip 产品 | **`dd0dcda30`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave666 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 比较 binop 任意非聚合操作数 stamp bool | `typeck_check_expr_binop_cmp` | ✅ 操作数须 `type_refs_equal`（peer lit 后） |
| INT_LIT / `0`→ptr 同型仍绿 | peer `typeck_coerce_init_lit_to_decl` | ✅ `a == 1` / `p == 0` 绿 |
| 无 widen / 无 int↔bool | 严格 Cap 策略 | ✅ 硬失败 |

**数据流**：产生 = EQ/NE/rel 不校验左右类型，恒 stamp bool。存储 = 混型比较仍过 typeck。消费 = host-C 提升/警告假绿。G.7：复用 lit coerce + `type_refs_equal`；diag `comparison_type_mismatch`；seed/diagnostic twin 同 commit。

**权威**：`compiler/src/typeck/typeck.x` · seed `typeck_gen` + empty_surface · diagnostic twin 同 commit

### wave666 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `i32==f32` / `i32==i64` / `!=` / `<` / `i32==bool` / `*i32==*u8` | ✅ T001 hard | ✅ T001 hard |
| `i32==i32` / lit / `f32==f32` / f32 lit / bool / `*T==*T` / `p==0` | ✅ 42 | ✅ 42 |
| freestanding `i32==f32` | ✅ T001 hard | ✅ T001 hard |
| land_bool / land_i32 / if_i32 回归 | ✅ 绿/T001 | ✅ 绿/T001 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

日志：`/tmp/mac_g05_wave666.log` · `/tmp/ubuntu_g05_wave666.log` · `/tmp/ubuntu_w666_probes.log`

### 仍 soft（wave666 闭时；void arith 已由 wave667 闭）

- untyped `self`（wave493 非标准 · **跳过**）  
- 未知 arg_ty（wave661 soft-skip）  
- `null` 关键字 vs `*T` 比较（既有假红）  
- 整波双端 **L4** + 升钉  

## wave665（已闭 · Cap residual non-bool logical operands hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `i32 && i32` / `f32 && f32` / `!i32` / `bool && i32` → typeck OK 后 freestanding/host C 真值假绿；docs/04 要求逻辑运算操作数为 bool |
| tip 产品 | **`9530ed26c`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave665 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| LOGAND/LOGOR 任意操作数 stamp bool | `typeck_check_expr_binop_cmp` | ✅ 操作数须 `type_ref_is_bool` |
| LOGNOT 任意非聚合 stamp bool | `typeck_check_expr_unary` | ✅ 操作数须 bool（wave662 仅聚合） |
| if/while 已 bool，嵌套 `a && b` 绕过 | 逻辑 binop 路径 | ✅ 与 docs/04 对齐 |

**数据流**：产生 = `&&`/`||`/`!` 不校验操作数 bool，恒 stamp TYPE_BOOL。存储 = 错误类型逻辑式仍过 typeck。消费 = host-C / freestanding 用 C 真值假绿。G.7：复用 `type_ref_is_bool`；diag `logical_operand_not_bool`；seed/diagnostic twin 同 commit。

**权威**：`compiler/src/typeck/typeck.x` · seed `typeck_gen` + empty_surface · diagnostic twin 同 commit

### wave665 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `i32 &&` / `f32 &&` / `\|\| i32` / `!i32` / mixed | ✅ T001 hard | ✅ T001 hard |
| `bool &&` / `!bool` / cmp `&&` / `!!` | ✅ 42 | ✅ 42 |
| freestanding `i32 &&` | ✅ T001 hard | ✅ T001 hard |
| if i32 回归 | ✅ T001 hard | ✅ T001 hard |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

日志：`/tmp/mac_g05_wave665.log` · `/tmp/ubuntu_g05_wave665.log` · `/tmp/ubuntu_w665_probes.log`

### 仍 soft（wave665 闭时；mixed cmp 已由 wave666 闭）

- untyped `self`（wave493 非标准 · **跳过**）  
- 未知 arg_ty（wave661 soft-skip）  
- void 参与 arith（BLD001）  
- 整波双端 **L4** + 升钉  

## wave664（已闭 · Cap residual non-integer INDEX index hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `a[p]` / `a[f32]` / `a[struct]` / `a[bool]` / `slice[p]` / `p[f32]` → typeck OK 后 host-C BLD001 或 freestanding 静默假绿 |
| tip 产品 | **`873efa7f6`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave664 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| INDEX 只查 base 不查 index 类型 | `typeck_check_expr_index` | ✅ `typeck_type_is_valid_subscript_index` |
| 允许 i32/u* / NAMED 非 struct（i8/enum） | helper | ✅ 整数族 + 非聚合 NAMED 绿 |
| 拒 bool/ptr/float/struct/array | helper + diag | ✅ T001 `subscript index must be an integer type` |

**数据流**：产生 = INDEX 检查 base 后未校验 index 类型。存储 = 错误类型下标仍过 typeck。消费 = host-C BLD001 / freestanding 用指针位当下标假绿。G.7：`typeck_type_is_valid_subscript_index`（复用 aggregate helper 拒 struct NAMED）；diag `subscript_index`；seed/glue/diagnostic twin 同 commit。

**权威**：`compiler/src/typeck/typeck.x` · seed `typeck_gen` + empty_surface · `pipeline_glue.c` · diagnostic twin 同 commit

### wave664 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| ptr/f32/struct/bool/slice/ptr base 下标 | ✅ T001 hard | ✅ T001 hard |
| lit/i32/u8/i8/enum/usize 下标 | ✅ 20–32 | ✅ 20–32 |
| freestanding `a[p]` | ✅ T001 hard | ✅ T001 hard |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

日志：`/tmp/mac_g05_wave664c.log` · `/tmp/ubuntu_g05_wave664.log` · `/tmp/ubuntu_w664_probes.log`

### 仍 soft

- untyped `self`（wave493 非标准 · **跳过**）  
- 未知 arg_ty（wave661 soft-skip）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self；其它 residual），或整波 L4 + 升钉  

## wave663（已闭 · Cap residual void function value return hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `function f(): void { return 42; }` / bare `42` / `return x` → typeck OK 后 host-C `(void)(e)` 假绿 |
| tip 产品 | **`ea75d5d57`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave663 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| void 体 `return e` 降为非 RETURN final/expr_stmt | parser 落点 + `skip_tail_ty_cmp` | ✅ `typeck_void_reject_value_expr` |
| 仅 statement-like 放行 | final + stmt_order + legacy expr_stmt | ✅ CALL/ASSIGN/bare RETURN 仍绿 |
| 诊断 `expected ?` | `typeck_diag_fmt_type_at` VOID | ✅ 打印 `void` |

**数据流**：产生 = void 函数值 return/final 未硬拒。存储 = 非 RETURN expr_stmt/final 绕过 `typeck_check_expr_return` void 门。消费 = host-C `(void)(e);` 假绿。G.7：单一 helper `typeck_void_reject_value_expr`（final + expr_stmt）；复用 `return_mismatch` diag。

**权威**：`compiler/src/typeck/typeck.x` · seed `typeck_gen` + empty_surface 同 commit

### wave663 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `return 42` / bare `42` / `return x` in void | ✅ T001 hard | ✅ T001 hard |
| bare `return;` / call / assign in void | ✅ 42 | ✅ 42 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

日志：`/tmp/mac_g05_wave663c.log` · `/tmp/ubuntu_g05_wave663.log` · `/tmp/ubuntu_w663_probes.log`

### 仍 soft

- untyped `self`（wave493 非标准 · **跳过**）  
- 未知 arg_ty（wave661 soft-skip）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self；其它 residual），或整波 L4 + 升钉  

## wave662（已闭 · Cap residual unary -/~/! on aggregates hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `-struct` / `~struct` / `!slice` / `-array` → typeck OK 后 host-cc BLD001 |
| tip 产品 | **`85b19b241`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave662 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| LOGNOT 恒 stamp bool（含聚合） | `typeck_check_expr_unary` | ✅ 聚合先硬拒 |
| NEG/BITNOT stamp 聚合类型 | 同左（wave289 仅 ptr/float） | ✅ 复用 aggregate helper |

**数据流**：产生 = unary 路径未对 ARRAY/SLICE/struct 做 host 有效性检查。存储 = LOGNOT→bool 假合法、NEG→struct。消费 = host-C `invalid argument type to unary` BLD001。G.7：复用 `typeck_type_is_aggregate_cmp_operand`；拓宽 `invalid_aggregate_cmp` 文案；seed + diagnostic twin 同 commit。

**权威**：`compiler/src/typeck/typeck.x` · seed `typeck_gen` + empty_surface · diagnostic twin 同 commit

### wave662 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `-s` / `~s` / `!s` / `!slice` / `-arr` | ✅ T001 hard | ✅ T001 hard |
| `-i32` / `~` / `!0` / `-f32` / `~ptr` 回归 | ✅ 42 / T001 ptr | ✅ 42 / T001 ptr |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

日志：`/tmp/mac_g05_wave662b.log` · `/tmp/ubuntu_g05_wave662.log` · `/tmp/ubuntu_w662_probes.log`

### 仍 soft

- untyped `self`（wave493 非标准 · **跳过**）  
- 未知 arg_ty（wave661 soft-skip）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self；其它 residual），或整波 L4 + 升钉  

## wave661（已闭 · Cap residual free-function call arg types hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `take_i32(p:*u8)` / struct / f32 / bool overload → typeck OK 后 host BLD001 或 C 静默转换假绿 |
| tip 产品 | **`dd6f3dc2e`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave661 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| resolve+arity 后未比对 arg vs param | `typeck_check_expr_call` / glue 产品路径 | ✅ `typeck_check_call_arg_types` |
| 假绿 f32/bool→i32 | host-C 静默转换 | ✅ score&lt;0 且 arg_ty&gt;0 → T001 |

**数据流**：产生 = CALL resolve 绑定 fi 后仅 wave660 查 arity，从不对实参做 `typeck_overload_arg_param_score`。存储 = 错误类型实参仍过 typeck。消费 = host-C BLD001（*u8/struct）或隐式转换假绿（f32/bool→i32）。G.7：复用 overload score（exact/lit/widen/array→slice）；未知 arg/param soft-skip；diag `call_arg_type_mismatch`；glue 同 gate。

**权威**：`compiler/src/typeck/typeck.x` · seed `typeck_gen` + empty_surface · `pipeline_glue.c` · diagnostic twin 同 commit

### wave661 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| *u8/struct/f32/bool→i32 / i64→i32 | ✅ T001 hard | ✅ T001 hard |
| lit / i32→i64 widen / two-arg / arity 回归 | ✅ 42 / T001 arity | ✅ 42 / T001 arity |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

日志：`/tmp/mac_g05_wave661b.log` · `/tmp/ubuntu_g05_wave661.log` · `/tmp/ubuntu_w661_probes.log`

### 仍 soft

- untyped `self`（wave493 非标准 · **跳过**）  
- 未知 arg_ty（score&lt;0 且 arg_ty≤0）仍 soft-skip  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self；其它 residual），或整波 L4 + 升钉  

## wave660（已闭 · Cap residual free-function call arity hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `add(1)` / `add(1,2,3)` / overload 无匹配 arity → typeck OK 后 host-cc BLD001 |
| tip 产品 | **`30697e2eb`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave660 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| overload `first_idx` 忽略 arity | `find_func_return_type_in_module_by_name_overload` / `_module_overload` | ✅ 纯 arity miss 不再 bind first_idx |
| typeck 未硬拒 | resolve 后缺门控 | ✅ `typeck_check_call_arity` + glue 产品路径 |

**数据流**：产生 = overload 在 `nparams!=num_args` 时仍 fallback `first_idx`。存储 = `resolved_func_index` 指错 arity 的同名函数。消费 = host-C 发错误实参个数 → BLD001。G.7：停 pure-arity first_idx；`typeck_check_call_arity`（resolved fi 或 bare 名扫描）；diag `call_arity_mismatch`；产品 seed 的 `typeck_check_expr_call` 委托 `pipeline_typeck_check_expr_call_c` 亦调同一 gate。

**权威**：`compiler/src/typeck/typeck.x` · seed `typeck_gen` + empty_surface · `pipeline_glue.c` · diagnostic twin 同 commit

### wave660 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| too few / too many / overload 无匹配 arity | ✅ T001 hard | ✅ T001 hard |
| correct arity / overload pick | ✅ 42 | ✅ 42 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

日志：`/tmp/mac_g05_wave660.log` · `/tmp/ubuntu_g05_wave660.log` · `/tmp/ubuntu_w660_probes.log`

## wave659（已闭 · Cap residual illegal `as` hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `struct`/`array` `as i32` 假绿可跑；`f32 as *T` 落 host-cc BLD001 |
| tip 产品 | **`e889a459e`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave659 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| `as` 无条件 stamp 目标类型 | `typeck_check_expr_as` | ✅ 硬拒 aggregate / float↔ptr；合法 numeric/ptr/enum 仍绿 |

**数据流**：产生 = `typeck_check_expr_as` 只 check 操作数后 stamp tgt。存储 = resolved 目标类型。消费 = host-C 发非法 cast → BLD001 或静默假绿。G.7：`typeck_as_cast_allowed` + `typeck_as_cast_type_class_ok`；复用 `typeck_type_is_aggregate_cmp_operand`（wave657）；diag `invalid_as_cast`。

**权威**：`compiler/src/typeck/typeck.x` · seed `typeck_gen.linux.x86_64.c` + empty_surface · diagnostic twin（full/thin/surface）同 commit

### wave659 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| struct/array `as i32` / i32 `as struct` / f32→ptr | ✅ T001 hard | ✅ T001 hard |
| i32↔i64 / f32↔i32 / ptr↔ptr / 0 as *u8 / enum as i32 | ✅ 42/0 | ✅ 42/0 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

日志：`/tmp/mac_g05_wave659.log` · `/tmp/ubuntu_g05_wave659.log` · `/tmp/ubuntu_w659_probes.log`

### 仍 soft

- untyped `self`（wave493 非标准 · **跳过**）  
- call arity 落 BLD001（typeck 未硬拒）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self；候选：call arity），或整波 L4 + 升钉  

## wave658（已闭 · Cap residual aggregate + - * / % bitops hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `struct`/`array`/`slice` `a + b`（及 `-* / % &`）→ host-cc BLD001；wave657 已硬拒 cmp |
| tip 产品 | **`260162665`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave658 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| binop_arith `type_refs_equal` 落聚合结果 | `typeck_check_expr_binop_arith` | ✅ 硬拒 ARRAY/SLICE/LINEAR/struct；VECTOR 同形仍绿 |

**数据流**：产生 = arith 无聚合门控、`type_refs_equal` 直接 stamp。存储 = resolved aggregate。消费 = host-C 发 `a+b` → BLD001。G.7：复用 `typeck_type_is_aggregate_cmp_operand`（wave657）+ 拓宽 diag；enum/scalar/ptr±int/`a.x+b.x`/f32+ 仍绿。

**权威**：`compiler/src/typeck/typeck.x` · seed `typeck_gen.linux.x86_64.c` + empty_surface · diagnostic twin · `ast_pool` infer leave-unresolved 同 commit

### wave658 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| struct/array/slice `+`/`-`/`*`/`&` | ✅ T001 hard | ✅ T001 hard |
| cmp 回归 / i32+ / ptr± / field+ / f32+ / enum== | ✅ 42 | ✅ 42 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

日志：`/tmp/mac_g05_wave658.log` · `/tmp/ubuntu_g05_wave658.log` · `/tmp/ubuntu_w658_probes.log`

### 仍 soft

- untyped `self`（wave493 非标准 · **跳过**）  
- 非法 `as` 重解释假绿；call arity 落 BLD001  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self；候选：非法 `as` / call arity），或整波 L4 + 升钉  

## wave657（已闭 · Cap residual aggregate ==/!=/relational hard-fail · L2）

tip 产品 **`315018523`**。aggregate cmp hard-fail。

## wave656（已闭 · Cap residual onefunc bare return ASI + Cap-T001 · L2）

tip 产品 **`d364ce605`**。onefunc return ASI + Cap-T001。

## wave655（已闭 · Cap residual mid-block return + more stmts · L2）

tip 产品 **`027c42a49`**。parse_block return ASI。
