# X 语言 / X language 当前进度（2026-07-29 · wave691 nested [][]T host-C fat · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 05:59 · 28 全日（wave534–617）已归楼；本文仅保留 29 日 tip 快照

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`fb4ba91ac`**（wave691 nested `[][]T` host-C fat tag + layout） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave691（已闭 · Cap residual nested `[][]T` host-C fat · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `[][]i32` → incomplete `struct xlang_slice_xlang_slice_struct`（BLD001）；非 free-T mono |
| tip 产品 | **`fb4ba91ac`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave691 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| nested fat 标签自毁 | `pipeline_codegen_type_to_c_repr_inner` 用 **static** `inner`/`eb`；`[][]T` 递归 TYPE_SLICE 时 outer 以 `eb` 为 scratch、inner 再写同缓冲 → 标签腐为 `xlang_slice_xlang_slice_struct` | ✅ stack-local scratch |
| nested fat 无布局 | 标签修对后 `struct xlang_slice_xlang_slice_int32_t` 仍无 body | ✅ XLANG_SLICE_LAYOUTS 一层 nested 全标量 |

**数据流**：产生=`type_to_c_repr(TYPE_SLICE(TYPE_SLICE(i32)))`。存储=static 中间缓冲。消费=host-C formal/local by-value + ARRAY_LIT fat。G.7：修同一 type_to_c_repr + 同 guard 布局表（非第二套命名）。

**权威**：`ast_pool.c`（type_to_c_repr）+ `rt_preamble.from_x.c` + `codegen.x` / seed `codegen_gen` emit_header

### wave691 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| host-C `-backend c` nested take_outer / local → 10/20 · C `struct xlang_slice_xlang_slice_int32_t` | ✅ | ✅ |
| wave690 `take_two` / wave689 `id_s` → 30/30 | ✅ | ✅ |
| 矩阵 rv42 / option / hello / si | ✅ | ✅ |
| pure-asm nested INDEX（default `-o` Ubuntu） | mac 绿 | ⚠️ SIGSEGV soft |

日志：`/tmp/mac_g05_wave691.log` · `/tmp/ubuntu_g05_wave691.log` · `/tmp/ubuntu_w691_probes.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**；P011 更早失败）  
- LANG-006 标量 let/const bool→int（**产品保留**）  
- pure-asm nested `[][]T` INDEX（Ubuntu freestanding SIGSEGV；host-C 已绿）  
- 更深 `[][][]T` / nested of NAMED fat 布局（未扩）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self；LANG-006 保留；候选 pure-asm nested INDEX），或整波 L4 + 升钉  

## wave690（已闭 · Cap residual bare-T free `[]T` ret mono · L2 工程轨）

见 wave690 节历史；tip **`8aec77df0`** · 钉盘仍 **`01479a857`**。

## wave689（已闭 · Cap residual free `[]T` ret mono · L2 工程轨）

见 wave689 节历史；tip **`3ef22fd0b`** · 钉盘仍 **`01479a857`**。

## wave688（已闭 · Cap residual `*T` call ret mono · L2 工程轨）

见 wave688 节历史；tip **`930b247df`** · 钉盘仍 **`01479a857`**。

## wave687（已闭 · Cap residual `T[N]`/`[]T` formal mono mangle · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave686 typeck 已接受 `first_a(T[2])` / `first_s([]T)`，codegen mono **ec=-6** / XP003；`[]T` 另 BLD001 形参按值 vs 指针 ABI |
| tip 产品 | **`c7fc0e79d`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave687 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| mono mangle 无 ARRAY/SLICE 后缀 | `codegen_type_ref_to_suffix` 仅 PTR/NAMED/builtin/VECTOR；combo key `i32[2]`/`[]i32` → suffix 0 → `emit_mono_mangled_name` -1 | ✅ `_a<N>` / `_slc` |
