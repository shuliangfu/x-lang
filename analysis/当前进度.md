# X 语言 / X language 当前进度（2026-07-28 · wave603 Cap arm64 dual-GP VAR + MEMORY · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`00d2059d0`**（wave603 Cap arm64 freestanding dual-GP VAR + MEMORY · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave603（已闭 · Cap residual arm64 freestanding VAR dual-GP + MEMORY call-arg · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | mac fs `take(p)` 12B/16B Pair 错值（var12=78/var16=70）；`sum(s)` 24B MEMORY=3；Ubuntu x86 已绿 |
| tip 产品 | **`00d2059d0`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave603 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| dual-GP VAR 只 load 低半 | `glue_load_var_as_value_to_rax_rdx` `ta!=0` 早退 | ✅ arm64 low@off + high@off+8 → x0+x1 |
| MEMORY 强制 1 GP | arm64 `u<1→u=1` | ✅ units=0 + stack multi-word |
| MEMORY 传 lea 指针 | emit lea vs param_home by-value | ✅ `store_memory_…_to_sp` |
| stack_pos=[x29+16] identity | wave414 low-end 与 classic 帧冲突 | ✅ stack_pos=frame_size |

**数据流（五问）**：产生 = call-arg 物化；存储 = GP 对 / 出栈 MEMORY；消费 = param_home（wave599）。权威：G.7 补全 `glue_load_var` + arm64 CALL 分类 ≡ x86 wave601，不新开第二套。

**权威**：`compiler/pipeline_glue.c`（load_var dual + store MEMORY to SP + frame_size 写 param_home）· `compiler/seeds/backend_call_dispatch.from_x.c`（arm64 classify/reserve/cleanup）。改后 rm `pipeline_glue_standalone.o` + `backend_call_dispatch.o` + g05。

### wave603 验证

| 探针 | mac L2 host/fs | Ubuntu L2 pure/fs |
|------|----------------|-------------------|
| var12 / var16 dual-GP VAR | ✅ 42/42 | ✅ 回归（x86 本绿） |
| call_dual (mk 返回) | ✅ 42 | ✅ |
| mem_param 24B MEMORY VAR | ✅ 42/42（was 3） | ✅ |
| var_take2 multi dual VAR | ✅ 41/41 | ✅ |
| free_dual 回归 | ✅ 42 | ✅ |
| disasm dual load | ✅ high@+8→x1 low→x0 | — |
| rv / option / si / hello | ✅ 42/0/0/0 | 待 pull 后批跑 |

### 仍 soft

- arm64 MEMORY call-arg from CALL/METHOD（`sum(mk())`）— 本波仅 VAR  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual soft（arm64 MEMORY CALL 物化 / 其它叶），或整波双端 L4 + 升钉  

## wave602（已闭 · Cap residual freestanding UFCS method self SysV · L2）

tip 产品 **`ddb5f61ff`** / docs **`fb307d12a`**。soft「其它 Cap residual」本波由 **wave603** 收 arm64 VAR/MEMORY 叶。

## wave601（已闭 · Cap residual freestanding >16B MEMORY by-value CALL · L2）

tip 产品 **`58431a7fb`** / docs **`5fb4fe1bf`**。
