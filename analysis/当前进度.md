# X 语言 / X language 当前进度（2026-07-29 · wave621 Cap pure-asm f32 ordered cmp · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`2d81d0dff`**（wave621 Cap pure-asm f32 ordered cmp · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave621（已闭 · Cap residual pure-asm f32 ordered cmp · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | pure-asm `if x == y` / `<=` 等值 / `let b: bool = x < y` f32 → run=0；`f64 ==` 与正数 `f32 <` 偶然绿；host-C 绿 |
| tip 产品 | **`2d81d0dff`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave621 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| f32 cmp 落整数 cmp | `glue_emit_cmp_finish_rbx_rax_elf_c` | ✅ 补 f32 门控 → `ucomiss` / arm64 `fcmp s` + fp setcc |
| 缺 f32 编码器 | `backend_enc_ucomiss_rbx_rax_arch` | ✅ G.7 新建为 ucomisd **兄弟**（movd+ucomiss / fmov s+fcmp s） |
| int-imm 捷径误吃 f32 | VAR/left lit_i32 shortcut | ✅ skip scalar f32（同 f64） |

**数据流（五问）**：产生 = freestanding dual-slot 装 f32 位；存储 = 栈上 32 位 + 高半垃圾；消费 = 须 IEEE 有序比较（CF/ZF / NZCV），禁止 64 位整数 cmp。G.7：**补全**既有 f64 finish 路径，**禁止**第二套 cmp finish。

**权威**：`compiler/pipeline_glue.c`（finish）+ `compiler/seeds/backend_enc_dispatch.from_x.c`（ucomiss）。改后 **须** 重建 `backend_enc_dispatch.o` + `PIPELINE_X_FORCE_COMPILE=1` `pipeline_x.o` + g05。

### wave621 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| pure-asm/fs f32 `==` / `!=` / `<>` / `<=` 等值 / `>=` 等值 | ✅ 42 | ✅ 42（disasm `ucomiss`） |
| pure-asm `let b: bool = x < y` / `x == 10.0` | ✅ 42 | ✅ 42 |
| pure-asm 负 f32 `(-32) < (-10)` | ✅ 42 | ✅ 42 |
| f64 `==` / wave620 f32 cast+add 回归 | ✅ 42 | ✅ 42 |
| rv / option / si / hello | ✅ | ✅ |

### 仍 soft

- pure-asm `take([10,32])` 对 i64/u8/f32 字面量 call-arg（i32 绿；VAR call 绿）  
- host-C bare `-E` 缺 minimal preamble 的 `xlang_slice_*` 布局（`--emit-c -o` 全 preamble 绿）  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一叶（建议：slice lit call-arg 或 host-C minimal preamble），或整波 L4 + 升钉  

## wave620（已闭 · Cap residual pure-asm float→int AS binop peel · L2）

tip 产品 **`6b7d3ba67`** / docs **`9d40d3bb1`**。

## wave619（已闭 · Cap residual host-C 全标量 TYPE_SLICE · L2）

tip 产品 **`6c1262ee9`** / docs **`95689c7ea`**。
