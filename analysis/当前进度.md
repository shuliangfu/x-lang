# X 语言 / X language 当前进度（2026-07-27 · wave530）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`387fd9966`**（双端 L2，wave530；L4 待整波收口） |
| 钉盘 | **`d5de2f13b`**（升自 `2bc0885d3`；双端 L4 129 全绿） |
| bstrict | **129** |

## wave530（已闭 · runtime_slice_glue Makefile R2 DIRECT 补全 双端 L2）

| 项 | 状态 |
|----|------|
| 触发 | wave529 闭后，按"含 `src/asm/**/*.x` glob 模式"继续审计剩余"半残"模块；定位 `runtime_slice_glue` — .x 已完整实现 6 函数但 Makefile 仅冷路规则 |
| 模块 | `runtime_slice_glue`（核心 `core/slice/slice.o`；6 函数：i32/u8/u64 × from_ptr/subslice） |
| 债 | `.x`（src/asm/runtime_slice_glue.x）已含 3 struct + 6 个 `#[no_mangle]` 完整实现 + doc_anchor；`.from_x.c` 已有 `XLANG_RUNTIME_SLICE_GLUE_FROM_X` 守卫；**Makefile 规则仅冷路 cc -c** |
| 根修 | Makefile 加 R2 DIRECT 分支：`XLANG_G05_PREFER_X_O=1` 时用 `xlang-c -lib-name "" -o` 直接编译 .x（无需 thin+rest `ld -r`，因无 OS bridge）；否则走冷路 cc -c .from_x.c |
| 构建模式 | **DIRECT**——纯计算模块（指针加法 + min 钳制），无 OS 依赖、无 `_impl` bridge、无 C-only 函数 |
| nm 一致性 macOS | cold 6 T vs R2 DIRECT 7 T 符号（R2 多 `_runtime_slice_glue_x_doc_anchor`，与其他 R2 模块一致） |
| nm 一致性 Ubuntu | cold 6 T vs R2 DIRECT 7 T 符号（同上；macOS/Ubuntu 完全一致） |
| 产品矩阵 macOS | run-hello.sh ✅ exit 0 · g-ffi-5-std-wrap ✅ exit 0 · g-ffi-5-release-ci ✅ exit 0 |
| 产品矩阵 Ubuntu | run-hello.sh ✅ exit 0 · g-ffi-5-std-wrap ✅ exit 0 · g-ffi-5-release-ci ✅ exit 0 |
| 验证级别 | **L2 日常真测**（按自举方法 §0.2 节奏；非每微 commit 全量 L4） |
| PLATFORM | SHARED slice — 纯计算，无 OS 依赖；macOS + Ubuntu 双端验证 |
| 关键发现 | ① `runtime_slice_glue` 是 wave515 `runtime_string_fast` 同类型的 DIRECT 模式候选（纯计算无 OS 依赖） ② `.x` 内 struct 复合字面量 `XlangSliceI32 { data, length }` + 指针算术 `data + start` 均能被 xlang-c 正确 codegen 为 C ③ 双端 nm 一致（6/7 多 doc_anchor），与其他 R2 模块行为相同 ④ wave529 的"`src/asm/**/*.x` glob 审计"成功定位到该模块（前波次扫描遗漏） |

## 下一步候选

1. **继续审计剩余 half-done 模块**：含 `#[no_mangle]` 但 Makefile 缺 R2 规则的（已确认 net_io_batch_fast 已嵌入 std/net/net.o 规则；剩余需查 `runtime_asm_build` 等次要模块）
2. **整波收口 L4**：当前累积 wave528-530 三波 R2 migration（runtime_http_glue + runtime_slice_glue + 钉盘升级），可按 §0.2「整波结束做一次扎实 L4」+ 日终兜底触发 L4 真冷全测
3. **钉盘升级候选**：若 L4 双端 129 全绿，可升钉自 `d5de2f13b`
