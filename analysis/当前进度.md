# X 语言 / X language 当前进度（2026-07-29 · wave626 Cap pure-asm fixed Pt[N] STRUCT_LIT in-place · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`becc92c72`**（wave626 Cap pure-asm fixed `Pt[N]` · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave626（已闭 · Cap residual pure-asm fixed `Pt[N]` STRUCT_LIT in-place · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Ubuntu pure-asm `let a: Pt[2]=[Pt{10,0},Pt{0,32}]` p0x=0 / sum=32（mac 假绿；host-C 42） |
| tip 产品 | **`becc92c72`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave626 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| ≤8B STRUCT_LIT 数组元 rvalue | vector_let / emit_array_lit 仅 esz>8 走 in-place | ✅ STRUCT_LIT(ko=45) 任意 esz → glue_emit_struct_type_let_init |
| high-end next_offset 别名 | rvalue STRUCT_LIT slot=-1 用 next_offset 不抬升 | ✅ 字段直写 elem_home（x86 base−ai·esz） |

**数据流（五问）**：产生 = fixed `TYPE_ARRAY` of STRUCT_LIT 经 rvalue 物化到 `ly->next_offset`；存储 = high-end 分配后 next_offset ≡ 数组 byte0；消费 = 第二元字段写覆盖 a[0] 后再 bulk store 到 +ai·esz → p0x=0/p0y=32/sum=32。G.7：**补全** wave598 同一权威（禁第二套 store 路径）。

**权威**：`pipeline_glue.c` only（`vector_let_init` + `emit_array_lit`）。改后 **须** `pipeline_glue_standalone.o` + g05。

### wave626 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| fixed `Pt[2]` p0x/p0y/p1x/p1y/sum | ✅ 10/0/0/32/42 | ✅ 10/0/0/32/42 |
| pure-asm/fs sum + host-C | ✅ 42 | ✅ 42 |
| `[]Pt` sum / take 回归 | ✅ 42 | ✅ 42 |
| `i32[2]` 标量回归 | — | ✅ 42 |
| rv / hello | ✅ | ✅ |
| Ubuntu disasm | — | elem0@−0x20 · elem1@−0x18 |

### 仍 soft

- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一叶，或整波 L4 + 升钉  

## wave625（已闭 · Cap residual pure-asm `[]Named` force_esz + FIELD dual-slot · L2）

tip 产品 **`b278583c4`** / docs **`994568244`**。pure-asm/fs `[]Pt`=42；fixed `Pt[N]` 本波 **wave626** 闭。

## wave624（已闭 · Cap residual host-C `[]NamedStruct` fat slice · L2）

tip 产品 **`97b3e5d82`** / docs **`ad577c123`**。

## wave623（已闭 · Cap residual host-C bare `-E` slice layouts · L2）

tip 产品 **`0081c8812`** / docs **`901d50233`**。

## wave622（已闭 · Cap residual pure-asm slice lit call-arg · L2）

tip 产品 **`f917861d7`** / docs **`827134001`**。
