# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（本波未改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`11a57351`**（双端 129） |
| tip | **`33505fbe`**（R2 `async_cps` Cap residual pure wave4 FILE* emit） |
| 产品 bstrict | **129** · 钉盘双端全绿 |
| 本波性质 | Cap residual pure：CPS FILE* emit（opaque fputs 桥） |
| 工程轨 prove | ✅ mac IDENTICAL · Ubuntu IDENTICAL（L2） |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `async_cps_codegen` Cap residual pure wave4（FILE* emit）**（`33505fbe`）  
   - `.x`：`async_cps_codegen_end` / `emit_phase_reset` / `after_await` / `after_await_io` / `after_await_impl` / `emit_sched_wrapper`  
   - helpers：`async_cps_store_i32` / `async_cps_fputs_i32_dec`  
   - 桥（always seed）：`async_cps_fputs(*u8,*u8)` — opaque FILE*，对齐 `driver_preamble_fputs`  
   - 宿主布局：AsyncCpsCodegenCtx size24（func@0 layout@8 phase_next@16 switch_open@20）；AsyncFrameLive names@0 n@4096  
   - seed：`#ifndef SHUX_ASYNC_CPS_CODEGEN_FROM_X` 省略 wave4 emit；`async_cps_fputs` 始终在 seed；begin/param_statics/hoist_impl 仍 seed  
   - surface re-pin（`-E` 去 preamble）+ prove MODULES 注释  

2. 验证  
   - mac：`SHUX=./compiler/shux bash tests/prove_module_selfhost.sh async_cps_codegen` → IDENTICAL · `/tmp/mac_prove_async_cps_wave4.log`  
   - Ubuntu：pull tip 后 prove IDENTICAL · `/tmp/ubuntu_prove_async_cps_wave4.log`  
   - **日常 L2 非 tip L4**；钉盘仍 `11a57351`  

3. 前序：wave3 walk_impl @ `33881f79` · liveness Cap residual pure @ `b06cf472` · 钉盘 `11a57351`

## 下一波前排

1. **Cap residual 余**：CPS begin / emit_param_statics / emit_hoisted_lets_impl（需 type_to_c_buf + run-seed 注入）  
2. **或** liveness FILE* emit_*  
3. **或** 产品 PREFER 链 / `pipeline_glue` unbundle `async_asm_pool`  
4. **可选** tip 升钉：Ubuntu L4 + 双端 129  
5. **可选** Windows hybrid（需切双系统时再喊人）  

```text
钉盘：11a57351
tip：33505fbe（CPS Cap residual pure wave4 FILE* emit；双端 L2 prove IDENTICAL）
Windows：本波不需要切换
下一：CPS begin/hoist emit residual 或 liveness emit_* / 可选 tip L4
```
