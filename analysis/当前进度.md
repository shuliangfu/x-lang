# X 语言 / X language 当前进度（2026-07-28 · wave595 Cap nested STRUCT_LIT in-place + arm64 dual-GP return · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`9bedc1917`**（wave595 Cap nested STRUCT_LIT · L2 双端绿） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave595（已闭 · Cap residual nested STRUCT_LIT in-place + arm64 9–16B dual-GP return · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Outer+Mid+Inner multi-field：Ubuntu pure-asm nest3 SIGSEGV；mac freestanding 错值（nest3=33 / nest3_let=1） |
| tip 产品 | **`b7a768f4b`** + **`9bedc1917`**（x86 high-end nest_slot） |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave595 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 嵌套 STRUCT_LIT 共享 parent temp | `emit_expr_elf_rec` + `slot_off=-1` → 同 `next_offset`；子字段覆盖父字段；9–16B dual store 恒写 16B 越界 | ✅ 嵌套 lit 就地物化到字段 home |
| x86 high-end nest_slot | `base+foff` 把 Mid 放到 Outer **下方**；子 +foff 上写砸 `a` | ✅ `nest_slot = base-foff`（x86）/ `base+foff`（arm64） |
| arm64 9–16B STRUCT_LIT return | dual-GP 仅 `ta==0` → `mov rbx→rax` 返回栈指针 | ✅ frame load `x0=[home], x1=[home+8]` |
| 返回前 rbx 错位 | 嵌套就地后 rbx 停在子 home | ✅ re-home `lea base_off → rbx` 再 dual-GP/≤8 load |

**数据流（五问）**：产生 = nested STRUCT_LIT field init；存储 = 父帧字段 home（arch-aware）；消费 = dual-GP return / CALL.field load。权威：G.7 补全同一 `pipeline_asm_emit_struct_lit_fields_elf_c`（就地 + 返回路径）。

**权威**：`compiler/pipeline_glue.c` · `pipeline_asm_emit_struct_lit_fields_elf_c`（`pipeline_glue_standalone.o`）。改后 rm standalone.o + g05。

### wave595 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| nest3 `mk().m.i.f+g` freestanding/pure-asm | ✅ 41 | ✅ 41 |
| nest3_let `o.m.i.f` | ✅ 42 | ✅ 42 |
| n3f/n3g/n3a/n2f 单字段 | ✅ 20/21/1/20 | ✅ 20/21/1/20 |
| nest2 / sz12 回归 | ✅ 42 | ✅ 42 |
| disasm Mid@Outer+4 · re-home · dual-GP | ✅ home+4 | ✅ `-0xc` / re-home `-0x10` |
| rv / option / si / hello | ✅ 42/102/0/0 | ✅ 42/102/0/0 |

### 仍 soft

- 其它 Cap residual 侧门（更深 / 指针中间字段等按探针暴露）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 其它侧门，或整波 L4  
2. 整波双端 L4 + 升钉  

## wave594（已闭 · Cap residual pure-asm 9–16B dual-GP return-local）

tip `13ced06b5` / docs `53b30ed68`。soft「深嵌套 STRUCT_LIT nest3」本波由 **wave595** 收。
