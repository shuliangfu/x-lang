# X 语言 / X language 当前进度（2026-07-27 · wave517）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`dd86c4427`**（双端 L2，wave517；L4 待整波收口） |
| 钉盘 | **`2bc0885d3`**（待 Ubuntu 钉盘升级） |
| bstrict | **129** |

## wave517（已闭 · runtime_net_workers R2 migration thin+rest 双端 L2）

| 项 | 状态 |
|----|------|
| 触发 | wave516 完成后，继续 net.o 链路最后一个未迁模块 |
| 模块 | `runtime_net_workers`（59 行，2 公开函数：accept entry ptr + weak affinity stub） |
| 债 | `.x` 仅 doc anchor；线程入口 `void *(*)(void *)` 无法用 `.x` 表达，全在 C |
| 根修 | ① `.x`：`xlang_net_worker_accept_entry_ptr_c` thin wrapper + `thread_set_affinity_self_c` weak stub + 英文 docblock ② `.from_x.c`：`xlang_net_worker_accept_loop` 线程体始终编译 + `xlang_net_worker_accept_entry_ptr_impl_c` 桥 ③ `Makefile` 加 R2 thin+rest `ld -r` 路径 |
| 构建模式 | **thin+rest**——线程入口 `void *(*)(void *)` C ABI 签名 `.x` 无法表达，accept 循环体必须留 C |
| nm 一致性 macOS | cold vs R2：3 核心 T 符号对齐（仅 doc_anchor 多余） |
| nm 一致性 Ubuntu | cold vs R2：3 核心 T+W 符号对齐（仅 doc_anchor 多余） |
| 产品矩阵 macOS | hello ✅ · g-ffi-5-std-wrap ✅ |
| 产品矩阵 Ubuntu | hello ✅ · g-ffi-5-std-wrap ✅ |
| 验证级别 | **L2 日常真测**（按自举方法 §0.2 节奏；非每微 commit 全量 L4） |
| PLATFORM | SHARED net · **双端 L2 验证通过** |
| 关键发现 | ① 线程入口 `void *(*)(void *)` 必须留 C，`.x` 只能导出 ptr-returning wrapper ② `_impl_c` 桥 + 线程体必须始终编译（不能被 `#ifndef FROM_X` 排除） ③ `thread_set_affinity_self_c` 在 R2 模式由 thin 提供，但 C 线程体仍需调用它 → 需前向声明 ④ Makefile recipe 用固定文件名（非 mktemp）避免 `$$` 转义陷阱 |
| net.o 链路 | **net.o 复合规则内 5 子模块 + 外部 net_workers 模块全部完成 R2 migration** |
