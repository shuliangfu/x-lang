# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`ed2dc1a1e`**（双端 tip L4 真冷 + 129；升自 `1f73ed275`） |
| tip 产品代码 | **wave103 CG002 macho_write 强符号**（钉盘未升） |
| 产品 bstrict | **129** · tip L4 双端全绿（钉盘） |
| 本波性质 | **CG002 Darwin pure-asm 收口叶：MH_OBJECT 强写出（日常 L2）** |
| Ubuntu | 钉盘 129 继承；本波 SHARED glue/ast_pool 须 pull 验矩阵 |
| mac | ✅ g05 L2 + §4 矩阵 rv42/option/si/hello；**pure-asm rv build/run=42** |
| Windows | **本波不需要切换** |

## 本波摘要

1. **wave102 后 residual 地图**  
   - mega_body enc 已绿（code_len=32）；`macho_write=-1` ← experimental bridge weak 桩  
   - 产生点：产品链无真 `macho.x` / MH_OBJECT writer  
2. **wave103 叶**：`ast_pool.c` 内 `pipeline_macho_write_o_to_buf_c` + 强符号 `platform_macho_write_macho_o_to_buf`  
   - G.7 与 `pipeline_elf_write_o_standard_to_buf_c` 同层（`PipelineElfCtxAccess` + glue `code_data` 偏移）  
   - 端口 `macho.x::write_macho_o_to_buf`；覆盖 Darwin weak -1  
   - 重建 `pipeline_glue_standalone.o` + g05  
3. mac 验证：`macho_write=303` · Mach-O arm64 `.o` · pure-asm **rv run=42**；§4 矩阵绿  
4. ImportEntry 仍结构债；**构建级别 L2**；钉盘不升  

## 下一波前排

1. ImportEntry 结构债（ast_pool GrowVec；非固定 BSS 微步 pure）  
2. 可选 pure-asm 扩矩阵（option/hello）/ 整波 tip L4  
3. **可选** Windows hybrid  

```text
钉盘：ed2dc1a1e（tip L4 双端 129）
产品 tip：wave103 macho_write 强符号（日常 L2；Darwin pure-asm rv42）
文档：22 号 wave103 CG002 macho MH_OBJECT
Windows：本波不需要切换
下一：ImportEntry / 可选 tip L4 / Windows
```
