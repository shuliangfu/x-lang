# X 语言 / X language 当前进度（2026-07-27）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **26 日全日归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)（wave446-491）  
> **25 号全日归档**：[自举进度-归档-2026-07-25.md](自举进度-归档-2026-07-25.md)（含 26 日初 wave446）  
> **项目目录**：现行 `…/xlang/x-lang`  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| 工程 | **双端 tip L4 129 全绿** · tip L4 **`2bc0885d3`**（双端，wave509-510）· 产品 tip **`2bc0885d3`**（wave509-510）· 钉盘 **`2bc0885d3`**（升自 `d812aeb41`；双端验证通过） |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **续 Cap residual** · **wave510 已闭（Ubuntu L4 金标验证 · 双端钉盘升级 `2bc0885d3`）** · 下一 wave511 |

## 27 日 wave510（已闭 · Ubuntu L4 金标验证双端钉盘升级）

| 项 | 状态 |
|----|------|
| 触发 | wave501-509 共 9 个 SHARED wave（8 R2 OS glue migration + 1 mac L4 冷验证），按 AGENTS.md「SHARED 改动双端验证」原则，必须 Ubuntu 金标验证通过才能升级钉盘 |
| 流程 | ① `git push origin self-hosting`（`2bc0885d3`）→ ② SSH ubuntu-server `git pull` → ③ 全擦 `compiler\|std\|core` 下全部 `.o` + 产品二进制 → `remaining_o=0` ④ `make -C compiler build-tool` ⑤ `make -C compiler bootstrap-driver-seed` 冷启动 → ⑥ `FULL=0 scripts/g05_prepare_and_relink.sh` relink → ⑦ `XLANG_BSTRICT_SKIP_BUILD=1 run-all-bstrict.sh` |
| 冷擦 remaining_o | 0 ✓ |
| bstrict | `run-all-bstrict OK (129 scripts)` exit 0（耗时 3 分 22 秒）✓ |
| 钉盘 | 升 **`2bc0885d3`**（升自 `d812aeb41`；双端验证通过：mac wave509 + Ubuntu wave510） |
| 判据自查 | remaining_o=0 ✓ · 二进制本波 mtime ✓ · bstrict exit 0 + 129 scripts ✓ · 与 mac L4 结果一致 ✓ · 包含本 SHA `2bc0885d3` ✓ |
| 关键发现 | ① Ubuntu 冷启动 8 个 R2 OS glue 模块（time/dynlib/env/sync/log/backtrace/process/atomic）无回归 ② Linux x86_64 上 `__atomic_*`/`pthread_*`/`dlopen`/`fork`/`backtrace` 桥接全部正常 ③ 钉盘从 `d812aeb41` 升级到 `2bc0885d3`（自 wave500 后累积 10 个 SHARED wave）|

## 27 日 wave509（已闭 · tip L4 冷验证 wave501-508 整体 R2 波）

| 项 | 状态 |
|----|------|
| 触发 | wave501-508 连续 8 个 SHARED R2 OS glue migration wave 完成，按「整波结束做一次扎实 L4」原则，触发 tip L4 冷验证 |
| 流程 | ① 全擦 `compiler\|std\|core` 下全部 `.o`（193→0）+ 产品二进制（`xlang`/`xlang_asm`/`xlang-c`/`bootstrap_xlangc`） ② `make -C compiler build-tool` 重编 build_tool ③ `make -C compiler bootstrap-driver-seed` 冷启动 → ④ `FULL=0 scripts/g05_prepare_and_relink.sh` relink ⑤ `XLANG_BSTRICT_SKIP_BUILD=1 run-all-bstrict.sh` 全量测试 |
| 冷擦 remaining_o | 0 ✓ |
| bstrict | `run-all-bstrict OK (129 scripts)` exit 0（耗时 10 分 17 秒）✓ |
| 钉盘 | 仍 **`d812aeb41`**（mac L4 真冷，Ubuntu 金标未验；SHARED 改动双端验证需 Ubuntu L4 才能升级钉盘） |
| 判据自查 | remaining_o=0 ✓ · 二进制本波 mtime ✓ · bstrict exit 0 + 129 scripts ✓ · wave501-508 全部 R2 OS glue migration 无回归 ✓ |
| 关键发现 | ① R2 thin+rest 模式在冷路径（纯 seed C）和 R2 路径（xlang-c -E + seed rest）均通过 ② 8 个 OS glue 模块（time/dynlib/env/sync/log/backtrace/process/atomic）迁移无相互干扰 ③ 编译器内建 `__atomic_*` / `pthread_*` / `dlopen` / `fork` / `backtrace` 等跨平台桥接在冷启动路径工作正常 |

## 27 日 wave508（已闭 · runtime_atomic_glue R2 migration 双路验证）

| 项 | 状态 |
|----|------|
| 债 | `runtime_atomic_glue` 产品 seed 仍为纯 C：33× public API（i16/u16/i32/u32/i64/u64 load/store/CAS/fetch_add/fetch_sub + seq_cst/acquire/release fence）全在 C seed，`.x` 仅有 doc anchor |
| 根因 | G-02f-19 runtime OS glue 需 R2 full 模式：`.x` 提供全部 public API + `#[no_mangle]`，C seed 仅保留 OS bridge `_impl` |
| 根修 | ① 更新 `src/asm/runtime_atomic_glue.x`：33× `#[no_mangle]` public API + 33× `extern "C"` bridge 声明 ② 更新 `seeds/runtime_atomic_glue.from_x.c`：33 函数重命名为 `_impl`（编译器内建 `__atomic_*` + C11 `<stdatomic.h>` + 内联 fallback）+ `#ifndef XLANG_RUNTIME_ATOMIC_GLUE_FROM_X` 守卫 + 前向声明补全 ③ 更新 `Makefile`：`runtime_atomic_glue.o` 支持 PREFER_X_O=1 thin+rest `ld -r` |
| 权威 | G.7 单源：`.x` 提供 33× public API + `#[no_mangle]`；`.from_x.c` 仅 33× `_impl` 编译器内建桥接 + 33× 守卫 `_c` 包装；PLATFORM: SHARED（编译器内建 `__atomic_*` 跨平台一致） |
| 冷路验证 | `nm` 显示 60 T 符号：30× `_impl` + 30× `_c` wrapper ✓ |
| R2 路验证 | `XLANG_G05_PREFER_X_O=1` → `nm` 显示 91 T 符号（含 auto-imported std 符号）合并成功 ✓ |
| bstrict | 冷路 `run-all-bstrict OK (129 scripts)` exit 0（耗时 11 分 0 秒）✓；R2 路 `run-all-bstrict OK (129 scripts)` exit 0（耗时 10 分 54 秒）✓ |
| 钉盘 | 仍 **`d812aeb41`**（日常 L2，未触发 tip L4 升级条件） |
| 关键教训 | ① **原子操作类型映射**：`int16_t`/`int32_t`/`int64_t` 直接映射到 xlang 的 `i16`/`i32`/`i64`；`uint16_t`/`uint32_t`/`uint64_t` 映射到 `u16`/`u32`/`u64`，无需额外类型转换 ② **编译器内建安全性**：`__atomic_*` 内建由编译器直接生成原子指令，不依赖内联汇编，R2 thin+rest 模式完全兼容 ③ **前向声明必要性**：33 个 `_c` 函数的前向声明供 smoke test 引用 thin 符号使用 |

## 27 日 wave507（已闭 · runtime_process_os_glue R2 migration 双路验证）

| 项 | 状态 |
|----|------|
| 债 | `runtime_process_os_glue` 产品 seed 仍为纯 C：22× public API（getenv/setenv/unsetenv/getpid/getppid/getcwd/chdir/self_exe_path/spawn/exec/waitpid/pipe 等）全在 C seed，`.x` 仅有 doc anchor |
| 根因 | G-02f-18 runtime OS glue 需 R2 full 模式：`.x` 提供全部 public API + `#[no_mangle]`，C seed 仅保留 OS bridge `_impl` |
| 根修 | ① 更新 `src/asm/runtime_process_os_glue.x`：22× `#[no_mangle]` public API + 21× `extern "C"` bridge 声明 ② 更新 `seeds/runtime_process_os_glue.from_x.c`：22 函数重命名为 `_impl` bridge（POSIX: fork/execve/waitpid/pipe/getcwd/readlink；Windows: CreateProcess/GetExitCodeProcess/CreatePipe）+ `#ifndef XLANG_RUNTIME_PROCESS_OS_GLUE_FROM_X` 守卫 + Windows 桩补全（`nop_sigchld_impl` / `dup_stdio_posix_impl`） ③ 更新 `Makefile`：`runtime_process_os_glue.o` 支持 PREFER_X_O=1 thin+rest `ld -r` ④ 类型适配：`int` → `int32_t`（`sig` / `slot` 参数与返回值） |
| 权威 | G.7 单源：`.x` 提供 22× public API + `#[no_mangle]`；`.from_x.c` 仅 21× `_impl` OS bridge + Windows 桩；PLATFORM: SHARED（POSIX vs Windows 分支在 `_impl` 内） |
| 冷路验证 | `nm` 显示 42 T 符号：21× `_impl` + 21× `_c` wrapper ✓ |
| R2 路验证 | `XLANG_G05_PREFER_X_O=1` → `nm` 显示 73 T 符号：21× thin（`.x` public API）+ 21× rest（`_impl` OS bridge）+ 31× auto-imported 合并成功 ✓ |
| bstrict | 冷路 `run-all-bstrict OK (129 scripts)` exit 0（耗时 11 分 15 秒）✓；R2 路 `run-all-bstrict OK (129 scripts)` exit 0（耗时 9 分 42 秒）✓ |
| 钉盘 | 仍 **`d812aeb41`**（日常 L2，未触发 tip L4 升级条件） |
| 关键教训 | ① **平台桩补全**：`.x` 中 `process_nop_sigchld` / `process_dup_stdio_posix` 在所有平台声明，但 `.c` 中仅 POSIX 实现；需在 Windows 分支添加空桩，否则 R2 链接会报 UNDEF ② **类型一致性**：`int` vs `int32_t` 参数/返回值必须与 `.x` 声明一致，否则前向声明冲突 ③ **thin 引用 rest 静态变量**：`getcwd` / `self_exe_path` 缓存（`process_cwd_cache` / `process_exe_cache`）在 rest 内，thin 的 `_c` 函数通过 `_impl` 桥接访问 |

## 27 日 wave506（已闭 · runtime_backtrace_platform R2 migration 双路验证）

| 项 | 状态 |
|----|------|
| 债 | `runtime_backtrace_platform` 产品 seed 仍为纯 C：14× public API（backtrace capture/symbolicate/crash evidence）全在 C seed，`.x` 仅有 doc anchor |
| 根因 | G-02f-19 runtime OS glue 需 R2 full 模式：`.x` 提供全部 public API + `#[no_mangle]`，C seed 仅保留 OS bridge `_impl` |
| 根修 | ① 更新 `src/asm/runtime_backtrace_platform.x`：14× `#[no_mangle]` public API（13× backtrace 函数 + 1× `name_has_gold_anchor`）+ 13× `extern "C"` bridge 声明 ② 更新 `seeds/runtime_backtrace_platform.from_x.c`：13 函数重命名为 `_impl` bridge（POSIX: execinfo/dladdr；Windows: CaptureStackBackTrace/DbgHelp）+ `#ifndef XLANG_RUNTIME_BACKTRACE_PLATFORM_FROM_X` 守卫 + 前向声明补全（供 smoke test 引用 thin 符号） ③ 更新 `Makefile`：`runtime_backtrace_platform.o` 支持 PREFER_X_O=1 thin+rest `ld -r` + 修复 mktemp TMPDIR 变量 ④ 类型适配：`*char` → `*u8`（xlang-c 将 `*char` 映射为 struct 类型，与 C `char*` 不兼容） |
| 权威 | G.7 单源：`.x` 提供 14× public API + `#[no_mangle]`；`.from_x.c` 仅 13× `_impl` OS bridge + 3× smoke test；PLATFORM: SHARED（POSIX vs Windows 分支在 `_impl` 内） |
| 冷路验证 | `nm` 显示 32 T 符号：14× public API + 13× `_impl` + 3× smoke + 2× 其他 ✓ |
| R2 路验证 | `XLANG_G05_PREFER_X_O=1` → `nm` 显示 61 T 符号（含 auto-imported std 符号）合并成功 ✓ |
| bstrict | `run-all-bstrict OK (129 scripts)` exit 0（耗时 11 分 9 秒）✓ |
| 钉盘 | 仍 **`d812aeb41`**（日常 L2，未触发 tip L4 升级条件） |
| 关键教训 | ① **xlang-c 类型映射陷阱**：`*char` 在 xlang-c 中映射为 `struct xxx_char*`（不透明类型），与 C 的 `char*` 不兼容；必须使用 `*u8` 映射到 `uint8_t*` ② **Makefile mktemp 变量**：`"${TMPDIR:-/tmp}"` 在 Makefile 中不展开，必须用 `"$${TMPDIR:-/tmp}"`（双 `$`）③ **前向声明类型一致**：rest C 中的前向声明必须与 thin `.x` 生成的类型完全一致，包括 `const` 修饰符 |

## 27 日 wave505（已闭 · runtime_log_os R2 migration 双路验证）

| 项 | 状态 |
|----|------|
| 债 | `runtime_log_os` 产品 seed 仍为混合模式：15× `_impl` 中 7× 已在 `.x`/C 同步，8× state/configuration 函数仅在 C；`_c` 包装 + convenience bridge 全在 C |
| 根因 | G-02f-19 runtime OS glue 需 R2 full 模式：`.x` 提供全部 public API + `#[no_mangle]`，C seed 仅保留 OS bridge `_impl` |
| 根修 | ① 更新 `src/asm/runtime_log_os.x`：17× `#[no_mangle]` public API（7× core + 10× `_c`）+ 15× `extern "C"` bridge 声明 ② 更新 `seeds/runtime_log_os.from_x.c`：8 个 `_c` 函数新增 `_impl` 实现（state access: `get_min_level` / `set_min_level` / `set_sink_mask`; file ops: `set_file_sink` / `close_file_sink`; config: `set_rotate` / `set_async_enabled` / `async_flush`）+ `#ifndef XLANG_RUNTIME_LOG_OS_FROM_X` 守卫全部 `_c` 包装 + 3× convenience bridge + 前向声明补全供 smoke test 引用 thin 符号 ③ 更新 `Makefile`：`runtime_log_os.o` 支持 PREFER_X_O=1 thin+rest `ld -r` |
| 权威 | G.7 单源：`.x` 提供 17× public API + `#[no_mangle]`；`.from_x.c` 仅 15× `_impl` OS bridge + 2× smoke test；PLATFORM: SHARED（Windows `_write`/`_open` vs POSIX `write`/`open` 分支在 `_impl` 内） |
| 冷路验证 | `nm` 显示 36 T 符号：7× non-`_c` wrapper + 10× `_c` wrapper + 15× `_impl` + 3× convenience + 2× smoke ✓ |
| R2 路验证 | `XLANG_G05_PREFER_X_O=1` → `nm` 显示 34 T 符号：17× thin（`.x` public API）+ 17× rest（15× `_impl` + 2× smoke）合并成功 ✓ |
| bstrict | `run-all-bstrict OK (129 scripts)` exit 0（耗时 9 分 53 秒）✓ |
| 钉盘 | 仍 **`d812aeb41`**（日常 L2，未触发 tip L4 升级条件） |
| 关键教训 | ① **smoke test 前向声明补全**：R2 模式下 `_c` 函数由 thin 提供，rest 中 smoke test 需 `extern` 前向声明引用 ② **convenience bridge 也需守卫**：`log_apply_env_once_c` / `log_get_min_level_c` / `log_emit_bytes_c` 三个桥接函数在 R2 模式下与 thin 重复定义，必须守卫 ③ **异步状态跨 thin/rest**：`_impl` 函数中的 `s_async_enabled` / `s_async_queue` 等静态变量在 rest 内，thin 的 `log_emit_bytes` 通过 `_impl` 桥接访问 |

## 27 日 wave504（已闭 · runtime_sync_os R2 migration 双路验证）

| 项 | 状态 |
|----|------|
| 债 | `runtime_sync_os` 产品 seed 仍为纯 C：16× public API（mutex/rwlock/condvar）全在 C seed，`.x` 仅有 doc anchor |
| 根因 | G-02f-19 runtime OS glue 需 R2 full 模式：`.x` 提供全部 public API + `#[no_mangle]`，C seed 仅保留 OS bridge `_impl` |
| 根修 | ① 更新 `src/asm/runtime_sync_os.x`：16× `#[no_mangle]` public API（5× mutex + 6× rwlock + 5× condvar）+ 16× `extern "C"` bridge 声明 ② 更新 `seeds/runtime_sync_os.from_x.c`：16 函数重命名为 `_impl` bridge（Windows: CRITICAL_SECTION/SRWLOCK/CONDITION_VARIABLE；POSIX: pthread_mutex_t/pthread_rwlock_t/pthread_cond_t）+ `#ifndef XLANG_RUNTIME_SYNC_OS_FROM_X` 守卫 + 2× smoke test 保留 C ③ 更新 `Makefile`：`runtime_sync_os.o` 支持 PREFER_X_O=1 thin+rest `ld -r` |
| 权威 | G.7 单源：`.x` 提供 16× public API + `#[no_mangle]`；`.from_x.c` 仅 16× `_impl` OS bridge + 2× smoke test；PLATFORM: SHARED（Windows vs POSIX 分支在 `_impl` 内） |
| 冷路验证 | `nm` 显示 34 T 符号：16× `_c` wrapper + 16× `_impl` + 2× smoke ✓ |
| R2 路验证 | `XLANG_G05_PREFER_X_O=1` → `nm` 显示合并成功：16× `_c`（thin）+ 16× `_impl`（rest）+ 2× smoke + 1× doc_anchor + auto-imported std = sync 符号正确 ✓ |
| bstrict | `run-all-bstrict OK (129 scripts)` exit 0（耗时 9 分 50 秒）✓ |
| 钉盘 | 仍 **`d812aeb41`**（日常 L2，未触发 tip L4 升级条件） |
| 关键教训 | ① **平台分支在 `_impl` 内**：Windows/POSIX 分支在 `#if defined(_WIN32)` 内，thin `.x` 保持平台无关 ② **smoke test 保留 C**：竞争测试调用 public API，rest 模式通过 `extern` 声明引用 thin 函数 ③ **诊断钩子跨模块**：`sync_lock_diag_*` 由 `sync.x` 提供，C seed 通过 `extern` 引用 |

## 27 日 wave503（已闭 · runtime_env_os R2 migration 双路验证）

| 项 | 状态 |
|----|------|
| 债 | `runtime_env_os` 产品 seed 仍为混合模式：`env_build_key` 已迁 `.x`，其余 9 个 public API（getenv/setenv/unsetenv/temp_dir/iter）全在 C seed |
| 根因 | G-02f-19 runtime OS glue 需 R2 full 模式：`.x` 提供全部 public API + `#[no_mangle]`，C seed 仅保留 OS bridge `_impl` |
| 根修 | ① 更新 `src/asm/runtime_env_os.x`：新增 9× `#[no_mangle]` public API（`env_getenv_c` / `env_getenv_ptr_c` / `env_getenv_z_c` / `env_getenv_exists_c` / `env_setenv_c` / `env_unsetenv_c` / `env_temp_dir_c` / `env_iter_count_c` / `env_iter_at_c`）+ 9× `extern "C"` bridge 声明 ② 更新 `seeds/runtime_env_os.from_x.c`：9 个函数重命名为 `_impl` bridge + `#ifndef XLANG_RUNTIME_ENV_OS_FROM_X` 守卫包裹 thin wrapper（冷路保留完整实现） ③ 更新 `Makefile`：`runtime_env_os.o` 支持 PREFER_X_O=1 thin+rest `ld -r` ④ 更新 `build_xlang_asm.sh`：同步 R2 hybrid 构建规则 |
| 权威 | G.7 单源：`.x` 提供 10× public API（含纯计算 `env_build_key`）+ `#[no_mangle]`；`.from_x.c` 仅 9× `_impl` OS bridge；PLATFORM: SHARED（Windows `GetEnvironmentVariableA` vs POSIX `getenv` 分支在 `_impl` 内） |
| 冷路验证 | `nm` 显示 19 T 符号：10× public API + 9× `_impl` ✓ |
| R2 路验证 | `XLANG_G05_PREFER_X_O=1` → `nm` 显示合并成功：10× public API（含 `env_build_key`）+ 9× `_impl` + 1× doc_anchor = 20 env 符号 ✓ |
| bstrict | `run-all-bstrict OK (129 scripts)` exit 0（耗时 10 分 54 秒）✓ |
| 钉盘 | 仍 **`d812aeb41`**（日常 L2，未触发 tip L4 升级条件） |
| 关键教训 | ① **部分迁移模块可就地扩**：`runtime_env_os` 已有 `env_build_key` 部分迁移，直接扩至剩余 9 函数比从零新建更高效 ② **`rest` 跨 `thin` 依赖**：`_impl` 函数调用 `env_build_key`（由 thin 提供），rest 模式下 `extern` 声明确保链接正确 ③ **`ld -r` 自动处理重复**：thin.o 中 auto-imported std 符号（如 `io_register_buffers`）不影响链接 |

## 27 日 wave502（已闭 · runtime_dynlib_os R2 migration 双路验证）

| 项 | 状态 |
|----|------|
| 债 | `runtime_dynlib_os` 产品 seed 仍为混合模式（Windows 函数 `dynlib_win_*` 部分 R2，主 API `dynlib_os_*_c` 全在 C） |
| 根因 | G-02f-20：dynlib OS glue 需 R2 full 模式，public API 全在 `.x`，C 仅保留 OS bridge `_impl` |
| 根修 | ① 更新 `src/asm/runtime_dynlib_os.x`：完整 `#[no_mangle]` public API（`dynlib_os_copy_last_error_c` / `dynlib_os_open_c` / `dynlib_os_sym_c` / `dynlib_os_close_c` / `dynlib_os_win_path_smoke_c`）+ `extern "C"` bridge 声明 ② 更新 `seeds/runtime_dynlib_os.from_x.c`：全部 public API 改为 `_impl` bridge（守卫激活时仅保留 `_impl`），冷路保留 full wrapper ③ 更新 `Makefile`：`runtime_dynlib_os.o` 支持 PREFER_X_O=1 thin+rest `ld -r` ④ `build_xlang_asm.sh` 无需更新（该模块仅由 Makefile 构建） |
| 权威 | G.7 单源：`.x` 提供 public API + `#[no_mangle]`；`.from_x.c` 仅 `_impl` OS bridge；PLATFORM: SHARED（Windows/POSIX 分支在 `_impl` 内） |
| 冷路验证 | `nm` 显示 10 T 符号：5× `_impl` + 5× `_c` ✓（macOS 上 Windows-only 函数 `dynlib_win_*` 被平台条件编译排除，正确行为） |
| R2 路验证 | `XLANG_G05_PREFER_X_O=1` → `nm` 显示合并成功：`.x` 提供 7× public API（含 `dynlib_win_normalize_path` 纯计算 + `dynlib_win_load_library_w_utf8`）+ rest 提供 5× `_impl` ✓ |
| bstrict | `run-all-bstrict OK (129 scripts)` exit 0（耗时 9 分 39 秒）✓ |
| 钉盘 | 仍 **`d812aeb41`**（日常 L2，未触发 tip L4 升级条件） |
| 关键教训 | ① **纯计算函数无需 `_impl`**：`dynlib_win_normalize_path` 是纯字符串变换，直接在 `.x` 实现即可 ② **平台条件编译在 `_impl` 内**：Windows-only `_impl` 函数（如 `dynlib_win_load_library_w_utf8_impl`）用 `#ifdef _WIN32` 守护，macOS 上不编译，不影响 R2 合并 ③ **`.x` auto-import 符号无害**：thin.o 中出现的 `io_register_buffers` 等符号来自 Xlang 标准库自动导入，不影响链接 |

## 27 日 wave501（已闭 · runtime_time_os R2 migration 双路验证）

| 项 | 状态 |
|----|------|
| 债 | `runtime_time_os` 产品 seed 仍为纯 C 实现（`runtime_time_os.from_x.c` 包含完整 public API + OS bridge），R2 迁移将 public API 迁到 `.x`，C 仅保留 OS bridge `_impl` |
| 根因 | G-02f-19：所有 runtime OS glue 模块需 R2 full 模式（`.x` 提供 public API + `#[no_mangle]`；`.from_x.c` 仅保留 OS bridge `_impl`；Makefile 拆分为 `thin.o` + `rest.o` + `ld -r` 链接） |
| 根修 | ① 新建 `src/asm/runtime_time_os.x`：声明 `extern "C"` bridge 函数 + `#[no_mangle]` public API 包装函数（`time_now_monotonic_ns_c` / `time_now_wall_ns_c` / `time_sleep_ns_c` / `time_format_wall_rfc3339_c` / `time_wall_local_offset_min_c`） ② 更新 `seeds/runtime_time_os.from_x.c` 为 R2 full 模式：`#ifndef XLANG_RUNTIME_TIME_OS_FROM_X` 守卫包裹原 public API，守卫激活时仅保留 `_impl` bridge（`time_monotonic_ns_impl` / `time_wall_ns_impl` / `time_sleep_ns_impl` / `time_format_rfc3339_impl` / `time_local_offset_min_impl`） ③ 更新 `Makefile`：`runtime_time_os.o` 构建规则支持 PREFER_X_O=1 时 `.x → thin.o` + `rest.o` + `ld -r`，冷路径仍为纯 C ④ 更新 `build_xlang_asm.sh`：同步 R2 hybrid 构建规则 |
| 权威 | G.7 单源：`runtime_time_os.x` 与 `runtime_time_os.from_x.c` 职责分离；`_impl` 前缀 = OS bridge 权威；`#[no_mangle]` 确保 public API 符号名与 C 兼容 |
| 冷路验证 | `nm runtime_time_os.o` 显示 10 个 T 符号：5× `_impl` + 5× `_c` ✓（public API + bridge 齐全） |
| R2 路验证 | `XLANG_G05_PREFER_X_O=1 make runtime_time_os.o` → `nm` 显示 `ld -r` 合并成功，含 5× `_impl` + 5× `_c` 公共符号 ✓ |
| bstrict | `run-all-bstrict OK (129 scripts)` exit 0（耗时 11 分 04 秒）✓ |
| 矩阵 | macOS L2 无回归（bstrict 全绿） |
| 钉盘 | 仍 **`d812aeb41`**（日常 L2，未触发 tip L4 升级条件） |
| 关键教训 | ① **R2 migration 双路必须同时验证**：冷路（无 `FROM_X` 守卫）需确保 public API 仍在 C 中完整；R2 路（有守卫）需确保 `.x` + rest 合并后符号完整 ② **`ld -r` 重复符号无害**：thin.o 含 `.x` 依赖链的多余 `T` 符号（来自 auto-imported mod），`ld -r $(LD_R_MULTIDEF_FLAGS)` 在 macOS 用 `-multiply_defined suppress` 抑制重复 ③ **`build_xlang_asm.sh` 与 `Makefile` 必须同步**：产品构建路径可能走 asm.sh，两套规则需一致 |

## 27 日 wave500（已闭 · Ubuntu L4 双端验收 wave491-498 整体功能波）

| 项 | 状态 |
|----|------|
| 触发 | wave499 mac L4 已绿 `d812aeb41`，按 AGENTS.md「SHARED 改动双端验证」+ 钉盘升级条件（Ubuntu 金标 + mac 双端 L4 全绿）触发 Ubuntu L4 验收；wave491-498 改 typeck + codegen + parser_asm 产品面 SHARED 改动，需 Ubuntu 金标验证无回归 |
| 流程 | ① git push `self-hosting` 到 origin → ② SSH ubuntu-server `git pull` → ③ 全擦 `compiler|std|core` 下全部 `.o` + 产品二进制（`xlang`/`xlang_asm`/`xlang-c`/`bootstrap_xlangc`）→ `remaining_o=0` ④ `make bootstrap-driver-seed` 冷启动 → ⑤ `FULL=0 scripts/g05_prepare_and_relink.sh` relink → 二进制 09:03 本波 mtime ⑥ 探针验证 ⑦ 全量 `XLANG_BSTRICT_SKIP_BUILD=1 run-all-bstrict.sh` |
| 探针 | `/tmp/w498_multi_combo_probe.x`：`Box<i32>.get()=42` + `Box<bool>.get()=true` → exit 0 ✓（Ubuntu 冷编译验证 wave498 功能） |
| bstrict | `run-all-bstrict OK (129 scripts)` exit 0（耗时 3 分 24 秒）✓ |
| 判据自查 | remaining_o=0 ✓ · 二进制本波 mtime ✓ · 探针全绿 ✓ · bstrict exit 0 + 129 scripts ✓ · 与 mac L4 结果一致 ✓ |
| 非回归证据 | wave491-498 改 typeck + codegen + parser_asm 产品面双端无回归；`simd-s4 FAIL: at_builtin_smoke.x` 是 std-simd-shuffle-select-gate 内部 non-fatal warning（gate 整体 OK，同 mac L4 + 钉盘模式） |
| tip L4 | 升 **`d812aeb41`**（双端）· 前基线 `c3a81bfd6`（wave495） |
| 钉盘 | 升 **`d812aeb41`**（升自 `c3a81bfd6`；双端 L4 全绿） |
| 关键教训 | ① **SHARED 改动必须双端验证**：mac `ld -dead_strip` 可能藏 UNDEF，链接完整性真值在 Ubuntu 全链；mac L4 绿只能升 tip L4，不能升钉 ② **bstrict FAIL 不等于回归**：需区分 gate 内 non-fatal warning（`simd-s4 FAIL` 但 `std-simd-shuffle-select gate OK`）与真测试失败；双端 simd-s4 模式一致 = 预存非本波问题 ③ **整波 L4 节奏**：wave491-498 八波日常 L2 后做一次 mac L4 + 一次 Ubuntu L4，符合「整体功能收口 = 主闸门」；升钉 = 双端验证，不升钉 = 单端 tip |

## 27 日 wave499（已闭 · mac L4 真冷验收 wave491-498 整体功能波）

| 项 | 状态 |
|----|------|
| 触发 | wave498 完成多 combo 泛型 impl 方法 codegen 单态化，wave491-498 构成「泛型 inherent impl 方法完整链路」整体功能波（Self 返回字面 + default 方法 → typeck 推断 → codegen unique combo → codegen multi-combo → 调用端 mangle），按自举方法.md §0.2「整波结束做一次扎实 L4」触发；wave498 改 codegen（codegen.x + seed）产品面 SHARED 改动，需 L4 验证无回归 |
| 流程 | ① 全擦 `compiler|std|core` 下 193 个 `.o` + 产品二进制（`xlang`/`xlang_asm`/`xlang-c`/`bootstrap_xlangc`）→ `remaining_o=0` 确认 ② pin 产品 seed（`codegen_gen.linux.x86_64.c` / `parser_gen.linux.x86_64.c` / `typeck_gen.linux.x86_64.c`）③ `make bootstrap-driver-seed` 冷启动 → ④ `FULL=0 scripts/g05_prepare_and_relink.sh` relink → 二进制 08:37 本波 mtime ⑤ 探针验证 ⑥ 全量 `XLANG_BSTRICT_SKIP_BUILD=1 run-all-bstrict.sh` |
| 探针 | `/tmp/w498_multi_combo_probe.x`：`Box<i32>.get()=42` + `Box<bool>.get()=true` → exit 0 ✓（冷编译验证 wave498 功能）· `/tmp/w499_full_chain.x`：`Wrap<i32>.get()=100` + `Wrap<bool>.get()=false` → exit 0 ✓（冷编译验证完整链路） |
| bstrict | `run-all-bstrict OK (129 scripts)` exit 0（耗时 11 分 33 秒）✓ |
| 判据自查 | remaining_o=0 ✓ · 二进制本波 mtime ✓ · 探针全绿 ✓ · bstrict exit 0 + 129 scripts ✓ · 含本 SHA `d812aeb41` ✓ |
| 非回归证据 | wave491-498 改 typeck + codegen + parser_asm 产品面 mac L4 无回归；`simd-s4 FAIL: at_builtin_smoke.x` 是 std-simd-shuffle-select-gate 内部 non-fatal warning（gate 整体 OK，同 wave496 mac L4 + 钉盘 `c3a81bfd6` 模式） |
| tip L4 | 升 **`d812aeb41`**（mac）· 前基线 `c3a81bfd6`（wave495） |
| 钉盘 | 仍 **`c3a81bfd6`**（SHARED 改动需 Ubuntu 金标双端验证后可升钉） |
| 关键教训 | ① **wave491-498 是完整的泛型 inherent impl 方法链路**：从 Self 返回类型（wave491）→ default 方法（wave492/493）→ typeck 推断（wave494）→ codegen unique combo 单态化（wave495）→ codegen multi-combo 单态化 + 调用端 mangle（wave498），覆盖产品路径上所有 gap ② **整波 L4 节奏**：8 波功能 + 2 波 L4（mac + Ubuntu），符合「日常微步 L2 + 整波收口 L4」 ③ **mac L4 不能直接升钉**：SHARED 改动必须 Ubuntu 金标双端验证 ④ **过时注释及时清理**：wave498 解决了 multi-combo soft gap 后，codegen.x + seed 中的「still soft」注释需同 commit 更新 |

## 27 日 wave498（已闭 · 双端 L2 · 多 combo 泛型 inherent impl 方法 codegen 单态化）

| 项 | 状态 |
|----|------|
| 债 | 多 combo 泛型 inherent impl 方法 codegen 单态化：`impl Box<T> { function get(self: Box<T>): T }` 同时实例化 `Box<i32>` + `Box<bool>` → 只发射一个泛型定义（ret=`struct T` incomplete BLD001）+ 符号冲突（wave490 mangle 已修，但定义侧未做单态化，wave495 仅 unique combo） |
| 根因 | ① hoisted impl methods（`impl Box<T> { function get(...) }`）`num_generic_params == 0`（`<T>` 在 impl 不在 fn），bypass `codegen_try_emit_generic_identity_mono` ② wave495 仅处理 unique combo（nc==1），多 combo 仍发裸定义 ③ 调用端 mangle 只处理函数级泛型，不处理 generic inherent impl 方法 → 调用点裸名 vs 定义端 mangled 名冲突 |
| 根修 | ① 新增 `codegen_try_emit_generic_impl_method_mono`：遍历函数参数，找第一个带 free type-arg 的命名结构体 param → 收集所有 mono combo → 对每个 combo 激活 `mono_active` 上下文 → 发射 mangled 函数定义（签名+体） ② 新增 `codegen_try_emit_generic_impl_method_extern_mono`：extern 声明版本，同上逻辑 ③ `emit_func` 函数发射循环在函数级泛型检查后调用 impl 级 mono ④ `emit_func_extern_declaration` 同理调用 extern 版本 ⑤ 新增 `codegen_try_emit_impl_method_mono_call_name`：调用端 mangle 辅助，从 receiver 类型提取具体类型参数 → 生成 mangled 调用名 ⑥ 修改所有调用端路径（UFCS / dep module resolved / C6 re-resolution / `codegen_emit_call_func_name` re-search / PTR overload fallback）调用此辅助 ⑦ codegen.x + seed `codegen_gen.linux.x86_64.c` 同 commit 同语义同步 |
| 权威 | G.7 单源：`codegen_try_emit_generic_impl_method_mono` + `codegen_try_emit_generic_impl_method_extern_mono` + `codegen_try_emit_impl_method_mono_call_name`（codegen.x + seed 同步） |
| 探针 | `/tmp/w498_multi_combo_probe.x`：`Box<i32>.get()=42` + `Box<bool>.get()=true` → exit 0 ✓（双端 L2） |
| 矩阵 | mac L2 bstrict 129 OK ✓ · Ubuntu L2 bstrict 129 OK ✓（耗时 2分39秒） |
| 钉盘 | 仍 **`c3a81bfd6`**（日常 L2 非 tip L4） |
| soft | dyn Trait（无 KW/vtable）；P010 untyped-let（语言契约）；`fn`/`var`/untyped-`self` 非产品债 |
| seed 一致性 | codegen.x + seeds/codegen_gen.linux.x86_64.c 同 commit 同语义 |
| 关键教训 | ① **泛型有两层：函数级 vs impl 级**：`num_generic_params > 0` 只覆盖函数级泛型，impl 级泛型（`impl Wrap<T>`）需要单独路径 ② **调用端与定义端必须对称**：定义端 mangled 了，调用端所有路径（UFCS / dep / C6 / re-search）都要 mangled，否则 undeclared function ③ **static 函数前向声明陷阱**：seed C 中 static 函数定义在调用点之后会报 implicit declaration，需改为 extern 或移到前面 ④ **Ubuntu 同步需检查 codegen_gen.c 实际内容**：`xlang-build.sh build` 可能不触发 codegen_gen.c 重新同步（需 `rm -f codegen_gen.c && make codegen_gen.c` 强制） |

## 27 日 wave497（已闭 · Ubuntu L4 双端验收 wave491-495 整体功能波）

| 项 | 状态 |
|----|------|
| 触发 | wave496 mac L4 已绿 `c3a81bfd6`，按 AGENTS.md「SHARED 改动双端验证」+ 钉盘升级条件（Ubuntu 金标 + mac 双端 L4 全绿）触发 Ubuntu L4 验收；wave491-495 改 typeck + codegen + parser_asm 产品面 SHARED 改动，需 Ubuntu 金标验证无回归 |
| 流程 | ① git push `self-hosting` 到 origin → ② SSH ubuntu-server `git pull` → ③ 全擦 `compiler|std|core` 下全部 `.o` + 产品二进制（`xlang`/`xlang_asm`/`xlang-c`/`bootstrap_xlangc`）→ `remaining_o=0` ④ `make -C compiler build-tool` ⑤ `make -C compiler bootstrap-driver-seed` 冷启动 → ⑥ `./xlang-build.sh build` (g05 relink) → ⑦ 产品矩阵 ⑧ 全量 `XLANG_BSTRICT_SKIP_BUILD=1 run-all-bstrict.sh` |
| 矩阵 | `rv_build=0 rv_run=42` / `opt_build=0 opt_run=102` / `si_build=0 si_run=0` / `hello_build=0 hello_run=0`（hello.out 含 `0a` 真换行）全绿 ✓ |
| bstrict | `run-all-bstrict OK (129 scripts)` exit 0（耗时 3 分 26 秒）✓ |
| 判据自查 | remaining_o=0 ✓ · 二进制本波构建 ✓ · 矩阵全绿 ✓ · bstrict exit 0 + 129 scripts ✓ · 与 mac L4 结果一致（simd-s4 non-fatal 相同模式）✓ |
| 非回归证据 | wave491-495 改 typeck（pipeline_glue + strict_minimal）+ codegen（codegen.x + seed）+ parser_asm 产品面双端无回归；`simd-s4 FAIL: at_builtin_smoke.x` 是 std-simd-shuffle-select-gate 内部 non-fatal warning（gate 整体 OK，同 mac L4 + 钉盘 `70ca1089b` 模式） |
| tip L4 | 升 **`c3a81bfd6`**（双端）· 前基线 `b99e25de5`（wave490） |
| 钉盘 | 升 **`c3a81bfd6`**（双端验证通过；前钉 `70ca1089b`） |
| 关键教训 | ① **SHARED 改动必须双端验证**：mac `ld -dead_strip` 可能藏 UNDEF，链接完整性真值在 Ubuntu 全链；mac L4 绿只能升 tip L4，不能升钉盘 ② **bstrict FAIL 不等于回归**：需区分 gate 内 non-fatal warning（`simd-s4 FAIL` 但 `std-simd-shuffle-select gate OK`）与真测试失败；双端 simd-s4 模式一致 = 预存非本波问题 ③ **整波 L4 节奏**：wave491-495 五波日常 L2 后做一次 mac L4 + 一次 Ubuntu L4，符合「整体功能收口 = 主闸门」；升钉 = 双端验证，不升钉 = 单端 tip |

## 27 日 wave496（已闭 · mac L4 真冷验收 wave491-495 整体功能波）

| 项 | 状态 |
|----|------|
| 触发 | wave491-495 构成「泛型 inherent impl 方法完整链路」整体功能波（Self 返回字面 + default 方法 → 伪 gap 清理 → typeck 推断 → codegen 定义/extern 单态化），按自举方法.md §0.2「整波结束做一次扎实 L4」触发；wave491-495 改 typeck + codegen + parser_asm 产品面需 L4 验证无回归 |
| 流程 | ① 全擦 `compiler|std|core` 下 207 个 `.o` + 产品二进制（`xlang`/`xlang_asm`/`xlang-c`/`bootstrap_xlangc`）→ `remaining_o=0` 确认 ② pin 产品 seed（`codegen_gen.linux.x86_64.c` / `parser_gen.linux.x86_64.c` / `typeck_gen.linux.x86_64.c`）③ `make bootstrap-driver-seed` 冷启动 → `FULL=0 g05_prepare_and_relink.sh` relink → 二进制 06:54 本波 mtime ④ 产品矩阵 ⑤ 全量 `XLANG_BSTRICT_SKIP_BUILD=1 run-all-bstrict.sh` |
| 矩阵 | `rv_build=0 rv_run=42` / `opt_build=0` / `si_build=0` / `hello_build=0 hello_run=0` 全绿 ✓ |
| bstrict | `run-all-bstrict OK (129 scripts)` exit 0（耗时 11 分 11 秒）✓ |
| 判据自查 | remaining_o=0 ✓ · 二进制本波 mtime ✓ · 矩阵全绿 ✓ · bstrict exit 0 + 129 scripts ✓ · 日志含本 SHA `c3a81bfd6` ✓ |
| 非回归证据 | wave491-495 改 typeck（pipeline_glue + strict_minimal）+ codegen（codegen.x + seed）+ parser_asm 产品面无回归；`simd-s4 FAIL: at_builtin_smoke.x` 是 std-simd-shuffle-select-gate 内部 non-fatal warning（gate 整体 OK，同钉盘 `70ca1089b` 的 simd-perf FAIL 模式） |
| tip L4 | 升 **`c3a81bfd6`**（mac）· 前基线 `b99e25de5`（wave490） |
| 钉盘 | 仍 **`70ca1089b`**（Ubuntu 双端未验；SHARED 改动需 Ubuntu 金标双端验证后可升钉） |
| 关键教训 | ① **整波 L4 节奏**：wave491-495 五波日常 L2 后做一次扎实 L4，符合「整体功能收口 = 主闸门」而非每 commit 全量 L4 ② **bstrict FAIL 不等于回归**：需区分 gate 内 non-fatal warning（`simd-s4 FAIL` 但 `std-simd-shuffle-select gate OK`）与真测试失败；钉盘 `70ca1089b` 也有同类 simd gate non-fatal ③ **mac L4 ≠ 双端放行**：mac `ld -dead_strip` 可能藏 UNDEF，链接完整性真值在 Ubuntu 全链；mac L4 绿只能升 tip L4，不能升钉盘 |

## 27 日 wave495（已闭 · 泛型 inherent impl 方法定义/extern codegen 单态化）

| 项 | 状态 |
|----|------|
| 债 | wave494 typeck 已修 ret=i32，但 codegen 仍以原始 `T` 发射函数定义 + extern 前向声明 → `struct T` incomplete BLD001 + conflicting types（`extern struct T get(...)` vs `int32_t get(...)`） |
| 根因 | ① hoisted impl methods（`impl Wrap<T> { function get(self: Wrap<T>): T }`）`num_generic_params == 0`（`<T>` 在 impl 不在 fn），bypass `codegen_try_emit_generic_identity_mono`，由 `emit_func` + `emit_func_extern_declaration` 发射 ② self 形参 `Wrap<T>` 被 wave489 unique-combo 后缀机制救援，但自由返回类型 `T` 无救援机制 ③ `mono_active` 未设，`emit_type` 的 name-based fallback 不替换 `T` → extern 声明与定义冲突 |
| 根修 | ① 新增 `codegen_build_func_param_mono_map` 辅助（G.7 单一权威）：遍历函数参数，对 TYPE_NAMED + 有 free type-arg 的 param，用 `codegen_collect_generic_struct_mono_combos` 收集 combo，仅 unique combo（nc==1）映射 formal type-arg → concrete combo；dedup by formal_arg type_ref；guards：null/越界返回 0、已 concrete 跳过、nc!=1 跳过、bare name 跳过 ② `emit_func` 在 ret 类型发射前用此 map 设 `mono_active`（save/restore 上下文），两条成功返回路径（std-io early + final）都 restore ③ `emit_func_extern_declaration` 同样设 `mono_active`，成功返回前 restore ④ codegen.x + seed `codegen_gen.linux.x86_64.c` 同 commit 同语义同步（包括 hoist 变量到函数作用域以覆盖两条 restore 路径） |
| 权威 | G.7 单源：`codegen_build_func_param_mono_map`（codegen.x + seed 同步）+ `emit_func` / `emit_func_extern_declaration` 双调用点 |
| 探针 | `/tmp/w493_generic_explicit.x` 返回 42 ✓ · `-E` 验证 `extern int32_t w493_generic_explicit_get(...)` + `int32_t w493_generic_explicit_get(...) {...}` 均 `int32_t`（不再是 `struct T`）✓ |
| 矩阵 | bstrict 129 OK · rv42 / option / hello / stdlib-import · simd-s4 `at_builtin_smoke.x` 预存在 import 'std.simd' 缺失（非本波引入） |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | dyn Trait（无 KW/vtable）；P010 untyped-let（语言契约）；**多 combo** 泛型 impl 方法 codegen 单态化（本波仅 unique combo，多 combo 仍 soft per wave490）；`fn`/`var`/untyped-`self` 非产品债 |
| seed 一致性 | codegen.x + seeds/codegen_gen.linux.x86_64.c 同 commit 同语义（包括变量 hoist 到函数作用域） |
| 关键教训 | ① **extern 前向声明与定义是两条独立 codegen 路径**：wave494 只改 typeck，wave495 必须同时改 `emit_func` + `emit_func_extern_declaration`，否则定义 `int32_t` 与声明 `struct T` 冲突 ② **变量作用域覆盖所有 restore 路径**：seed C 中变量声明在嵌套块内但 restore 在块外会导致 undeclared 错误，必须 hoist 到函数作用域 ③ **复用 wave489/490 combo 收集机制**：`codegen_collect_generic_struct_mono_combos` + `codegen_generic_struct_fill_concrete_args` 已是 G.7 单一权威，新辅助只组装映射不重写收集逻辑 |

## 27 日 wave494（已闭 · 泛型 inherent impl 方法 typeck 推断）

| 项 | 状态 |
|----|------|
| 债 | 泛型 inherent impl 方法返回 T 的 typeck 推断（`w.get()` where w: Wrap<i32>, `impl Wrap<T> { function get(self: Wrap<T>): T }` → typeck `expected i32, found T`） |
| 根因 | ① `w.get()` 被解析为 EXPR_METHOD_CALL (kind 49)，走 `pipeline_typeck_check_expr_method_call_c`（strong 版本在 strict_minimal seed 中），**非** EXPR_CALL+ord_field ② 非泛型 UFCS 路径用 `pipeline_typeck_type_refs_equal_c`（指针相等）匹配 self 形参，`Wrap<i32>` ≠ `Wrap<T>` 失败 ③ **但** weak integer match (score 100) 按 TYPE_NAMED kind 匹配成功，stamp 原始返回类型 `T` 并 `return 0`，在 generic fixup 之前退出 ④ 初次 wave494 代码放在 `glue_generic_call_fixup_resolved_type_c`（EXPR_CALL 路径），EXPR_METHOD_CALL 永远不执行 |
| 根修 | ① 导出 3 个 pattern-unify 辅助（`glue_typeck_type_tree_has_free_param_c` / `glue_typeck_pattern_unify_bind_c` / `glue_typeck_subst_type_ref_c`，去 static → 可跨 TU 调用）② 新建 `pipeline_typeck_method_call_generic_ufcs_c` 共享函数（G.7 单一权威）：扫描同名 free fn，检查 self 形参有 free type-param → pattern-unify self 形参与 receiver 具体类型 → 建 free-name→concrete map → subst 返回类型 → stamp resolved_type ③ **关键**：放在非泛型 UFCS **之前**调用（否则 weak integer match 先 stamp 错误 ret 并 return 0）④ weak (pipeline_glue.c) + strong (pipeline_glue_strict_minimal.from_x.c) + .x (pipeline_glue_strict_minimal.x) 三处同 commit 同语义同步 |
| 权威 | G.7 单源：`pipeline_typeck_method_call_generic_ufcs_c`（pipeline_glue.c）+ 3 个导出的 pattern-unify 辅助 |
| 探针 | typeck 正确推断 ret=i32 ✓（`return w.get()` 不再报 `found T`）· codegen 仍以原始 T 发射函数定义（`struct T` incomplete）= codegen 单态化 gap（非产品阻塞）· 已有探针（self: Point / trait chain / Wrap<T>+i32 ret）全绿 |
| 矩阵 | bstrict 129 OK · rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4） |
| soft | dyn Trait（无 KW/vtable）；P010 untyped-let（语言契约）；泛型 inherent impl 方法定义 codegen 单态化（typeck 已修，codegen 仍以原始 T 发射）；`fn`/`var`/untyped-`self` 非产品债 |
| seed 一致性 | pipeline_glue.c + pipeline_glue_strict_minimal.from_x.c + pipeline_glue_strict_minimal.x 同 commit 同语义 |
| 关键教训 | ① **确认表达式 kind 再选路径**：`w.get()` 是 EXPR_METHOD_CALL (49) 而非 EXPR_CALL+ord_field；调试 fprintf 在错误路径不打印是路径错误的信号 ② **weak integer match 先于 generic fixup 的陷阱**：score 100 的 kind-match 会先 stamp 错误 ret 并 return，必须在非泛型 UFCS **之前**调用 generic 路径 ③ **G.7 导出辅助而非复制**：strict_minimal seed 通过 extern 调用 pipeline_glue.c 的导出函数，避免重复实现 pattern-unify 逻辑 |

## 27 日 wave493（已闭 · 伪 gap 清理 + 文档审计）

| 项 | 状态 |
|----|------|
| 债 | inherent impl self 类型解析（`impl Point { function get_x(self): i32 {...} }` untyped self → `self.x` resolves to `?`） |
| 根修 | **伪 gap 清理波（无根修）**：① 经上帝视角分析发现 `parser_asm_seed_parse_into_buf_slice.inc` 被 `PARSER_ASM_THIN_GLUE_NO_SEED_PARSE` 宏排除出 xlang 二进制构建；xlang 实际使用 `parser_asm_skip_tl_slice.inc` 的 `parser_asm_skip_one_impl_into_slice_c` 记录 impl 头并定位到 `{` 之后，方法由外层 parse loop 作普通 free function 提交 ② untyped `self`（无类型标注）**非标准 X 语法**；标准面只认 `self: Type`（显式类型标注）③ 探针验证：`self: Point`（非泛型 inherent impl）/ `self: Config`（trait 链式 `set_a().set_b().build()`）/ `self: Wrap<T>` + i32 返回（泛型 inherent）**全绿** ④ 唯一真实 gap：泛型 inherent impl 返回 `T`（`impl Wrap<T> { function get(self: Wrap<T>): T }` → typeck `found T`）—— 但产品代码无此用法，非自举阻塞 ⑤ 已回滚 seed 修改 + 删除 7 个伪探针 |
| 权威 | G.7 单源：`parser_asm_skip_tl_slice.inc`（impl 记录 + for-type 改写）+ `parser_asm_one_function_buf_slice.inc`（default 方法 hoist）|
| 探针 | `self: Point` inherent=42 · `self: Config` trait chain `set_a().set_b().build()`=42 · `self: Wrap<T>` + i32 ret=42 · untyped `self` 仍红（非标准语法，预期）· `self: Wrap<T>` + T ret 仍红（真实 gap，非产品阻塞）|
| 矩阵 | rv42 / option / hello / stdlib-import |
| 钉盘 | 仍 **`70ca1089b`**（日常 L2 非 tip L4）|
| soft | dyn Trait（无 KW/vtable）；P010 untyped-let（语言契约）；泛型 inherent impl 返回 T（typeck 推断 gap，非产品阻塞）；`fn`/`var`/untyped-`self` 非产品债 |
| seed 一致性 | 无变更（伪 gap 清理波）|
| 关键教训 | ① **动手前先确认构建路径**：seed slice 是否被宏排除（`PARSER_ASM_THIN_GLUE_NO_SEED_PARSE`）② **勿用非标准语法造伪 gap**：`self`（无类型）非标准，正确写法 `self: Type` ③ **上帝视角纪律**：先查产生/存储/消费链再动手，避免在错误文件打补丁 |

## 验收

| 项 | 状态 |
|----|------|
| 钉盘 L4 双端 129 | ✅ **`d812aeb41`**（wave500 升钉；双端全绿） |
| tip L4 双端 129 | ✅ **`d812aeb41`**（wave500 双端验收；mac 3分24秒 / Ubuntu 11分33秒） |
| 产品 tip L4 矩阵 | ✅ rv42 / option102 / hello0 / si0 |

## 归档导航

- **[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)**：26 日全日 wave446-491（泛型推断链 / trait 默认方法体 / struct mono 链 / multi-combo mangle）
- **[自举进度-归档-2026-07-25.md](自举进度-归档-2026-07-25.md)**：25 号全日 Cap residual wave396-445 + 26 日初 wave446
- **当前文档**：27 日 wave492-500（泛型 inherent impl 方法完整链路 + 双端 L4 验收）

### 为何文档里有 `fn`/`Self`/`var`？

| 写法 | 说明 |
|------|------|
| **不是**产品矩阵债 | 标准面只认 `function` / `let` / `self: Type` |
| 来源 | wave445 探针曾用 Rust 风格 `fn`/`-> Self`/无类型 `self` 测 mono → 失败被记 soft |
| 正确探针 | `function` + 显式 `self: A` → 已绿；**勿**再用错误语法制造伪 gap |
| `var` | 非关键字；写 `var x` 整函数解析失败，用 `let` |
