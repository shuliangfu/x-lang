# Shux 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-22.md](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave188** shux_ensure_formal_std_make_o pure @ `b2885a850` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **R2 Cap residual pure · formal_std_make** → L6 invoke_ld_list |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + invoke_ld prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + invoke_ld prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：常驻 Cap residual soft — `shux_ensure_formal_std_make_o` 体仍 always mega C（getenv + access + realpath + system make）  
2. **根修 / pure**：真迁 `labi_invoke_ld_list.x` L6 pure（pure path/SHUX/make-cmd join；G.7 复用 `labi_net_tls_*`；Cap residual **getenv** + **access** + **realpath_cap** + **system** + **asm_link_obj_skip_missing**）  
3. **Cap residual**：shell make / getenv / access / realpath / skip_missing 仍 Cap；append_std / invoke_cc_impl / undef_sym 等仍 residual  
4. **验收**：双端 L2 g05（L6 ← pure .x）+ 矩阵 + 邻域 + invoke_ld_list prove IDENTICAL；钉盘仍 **`3c88f7e51`**  

## 下一波前排

1. 其它 rest R2 / mega residual（可迁：常驻 Cap — undef_sym / path_io / wait / strerror / mega spawn-IO / ld_debug_argv / getenv / resolve / path_readable / invoke_cc_impl·append_std_objs 大叶 …；formal_std_make @ wave188 pure · ensure_std_net @ wave187 · prepare @ wave186）  
2. 可选：未闭合块注释硬诊断（EOF depth>0）  
3. 可选 Windows hybrid（**需要时再通知重启**）  

```text
钉盘：3c88f7e51（未升）
tip：b2885a850 wave188 shux_ensure_formal_std_make_o pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：其它 rest R2 / mega residual（常驻 Cap）
```
