# X 语言 / X language 当前进度（2026-07-28 · wave600 Cap arm64 multi dual-GP call-arg · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`b4a9519bd`**（wave600 Cap multi dual-GP call-arg · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave600（已闭 · Cap residual arm64 multi dual-GP call-arg spill/load · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | mac freestanding `take2(mkA(),mkB())` 63≠41；`f(n,s12,m)` 143≠42；wave599 单 dual-GP 形参已绿 |
| tip 产品 | **`b4a9519bd`**（`e33fc704d` + x86 arg0 补丁） |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave600 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| AAPCS64 call-arg `units=1` 硬编码 + 只 spill x0 | `pipeline_asm_emit_call_args_elf_c_impl` ta==1 | ✅ gp_units from size + dual spill |
| arm64 dual-GP high 半未 spill/load | `glue_sysv_spill/load` 仅 ta==0 | ✅ x1@home+8（low-end） |
| dual load gp==0 先 low 再 high 经 x0 盖掉 low | `glue_sysv_load_spill_to_arg_regs` | ✅ high-first；x86 再 mov rax→rdi |

**数据流（五问）**：产生 = CALL 实参 dual-GP 打包；存储 = 帧 spill；消费 = 形参 home（wave599）。权威：G.7 补全同一 spill/load 族（不新开第二套）。

**权威**：`compiler/seeds/backend_call_dispatch.from_x.c`（`backend_call_dispatch.o`）。改后 rm `src/asm/backend_call_dispatch.o` + g05。

### wave600 验证

| 探针 | mac L2 fs | Ubuntu L2 pure-asm / fs |
|------|-----------|-------------------------|
| take2b `p.a+p.b+q.x+q.y` | ✅ 41 | ✅ 41 / 41 |
| take2_onlyp / onlyq / fields | ✅ 30/11/43 | — |
| mix `f(1,s12,8)` | ✅ 42 | ✅ 42 / 42 |
| take3 三 dual-GP | ✅ 42 | ✅ 42 / 42 |
| ret_param / param_nest | ✅ 41/43 | — |
| wave599 take_field/param_sum/param_field | ✅ 41/42/20 | ✅ 41/42 |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/0/0 |

### 仍 soft

- freestanding >16B MEMORY 形参（p24 fs=138）  
- dual-GP **VAR** 实参 arm64 load 高半（`glue_load_var_as_value` ta!=0 早退）  
- method_call arm64 非 spill 直 place dual-GP  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual soft 叶（MEMORY 形参 / VAR dual-GP call-arg / method dual-GP），或整波双端 L4 + 升钉  
2. 整波双端 L4 + 升钉  

## wave599（已闭 · Cap residual arm64 dual-GP param home）

tip `bf8e22476` / docs `795d71f70`。soft「multi dual-GP 实参」本波由 **wave600** 收。
