# X 语言 / X language 当前进度（2026-07-29 · wave620 Cap pure-asm float→int AS binop peel · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`6b7d3ba67`**（wave620 Cap pure-asm float→int AS · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave620（已闭 · Cap residual pure-asm float→int AS binop peel · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | pure-asm `(x:f32 as i32)+(y as i32)` / `f32[2]` INDEX cast sum / `f32[]` → run=0；host-C=42；`let z=x+y; z as i32` 绿 |
| tip 产品 | **`6b7d3ba67`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave620 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| binop 操作数 peel integer-target AS | `glue_try_binop_load_operand_elf_c` | ✅ float **源** → 全路径 `pipeline_asm_emit_as_elf_impl`（cvttss2si / fcvtzs） |
| dual-slot load-to-rbx 假「不碰 rax」 | `glue_binop_operand_load_to_rbx_clobbers_rax` | ✅ full-AS 恒 clobber rax |
| 分类器重复 | — | ✅ `glue_binop_as_needs_full_emit_elf_c` 单权威（float-target wave301 + float-source wave620） |

**数据流（五问）**：产生 = freestanding dual-slot binop 装操作数；存储 = IEEE f32 栈槽；消费 = 须 cvtt 再整数加。G.7：**补全**既有 peel 门控（wave301 仅 float-target），**禁止**第二套 cast 路径。

**权威**：`compiler/pipeline_glue.c` 单源。改后 **须** `PIPELINE_X_FORCE_COMPILE=1` 重建 `pipeline_x.o` + g05（双定义 `pipeline_x.o` 内嵌 glue）。

### wave620 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| pure-asm + host `(x as i32)+(y as i32)` f32 | ✅ 42 | ✅ 42（disasm `cvttss2si` / `fcvtzs`） |
| pure-asm f32[2] INDEX / asg / f32[] slice | ✅ 42 | ✅ 42 |
| pure-asm f32 add + top-level cast | ✅ 42 | ✅ 42 |
| peel 回归 `(u8 as i32)+(u8 as i32)` | ✅ 42 | ✅ 42 |
| i64[] local / `take(a)` call（简化探针） | — | ✅ 42 |
| rv / option / si / hello | ✅ | ✅ |

### 仍 soft

- 其它 Cap residual 叶；整波双端 **L4** + 升钉  
- （历史 soft `sum(i64[])` lit call 本波未硬复现；VAR call 已绿）

### 下一波

1. Cap residual 下一叶，或整波 L4 + 升钉  

## wave619（已闭 · Cap residual host-C 全标量 TYPE_SLICE · L2）

tip 产品 **`6c1262ee9`** / docs **`95689c7ea`**。

## wave618（已闭 · Cap residual f32[]/f64[] host-C TYPE_SLICE C 名 · L2）

tip 产品 **`7a943902f`** / docs **`551d34837`**。
