# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`86281cc6`**（双端 L4 真冷 + 129；未升） |
| tip | **R2 rt_preamble Cap residual pure wave29**（N=224 + WEAK_IO 178..181；日常 L2） |
| 产品 bstrict | **129** · tip L4 钉盘仍 `@86281cc6` |
| 本波性质 | **工程轨 R2 pure/根修微步**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42/option/si/hello + pure-asm rv42 @ `b9bac87b` |
| mac | ✅ L2 产品 C 路径矩阵绿；本机构建 pure-asm CG002 预存（金标 Ubuntu pure-asm 绿） |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `rt_preamble` Cap residual pure wave29（本波）**  
   - 根修双权威：pure `io_net` N **219→224**（表实长；hybrid `line_at` 越界曾截尾）  
   - 根修 **WEAK_IO_BATCH** skip：**178..181**（stdio 守卫 + weak shux_io/process/ctx）  
     - 旧 cold `i==174` 仅命中注释；旧 .x `124..134` 是 extern 段  
   - 对齐：`seeds/rt_preamble.from_x.c` cold · `rt_preamble.x` · surface · thin pure N  
   - write_* 本已 R2 pure；产品链 permanent slice 仍 **cold write**（Darwin 不 cp hybrid 到 RT_SEED_SLICE）  
   - Cap residual 数据：巨型字串表 **仍 always-seed**  
   - 注释：rt_preamble.x **JSDoc** 重写  
   - 代码 SHA：**`b9bac87b`**（docs 本 commit 跟进）  
   - Ubuntu 矩阵：rv42 / opt102 / si0 / hello0 / pure-asm rv42  
   - 日志：`/tmp/mac_g05_wave29e.log` · `/tmp/ubuntu_g05_wave29.log`  
   - 钉盘仍 **`86281cc6`**

2. **注释纪律（已固化）**  
   - 方法块注释 = JSDoc；参数类型与作用写全；日后 **`shux doc`** 生成  

3. **文档纪律**  
   - 只改 `自举进度.md` + 本文；**不**改 `当前问题分析.md` / `问题分析文档.md`

## 下一波前排

1. **可选** g05 sed / harness `-E` 评估  
2. **可选** Windows hybrid；Darwin arm64 纯 asm / Vec4f CG002  
3. Cap residual 数据（巨型表）长期 always-seed（.x 禁巨型字串表）  

```text
钉盘：86281cc6（tip L4 双端 129）
tip：R2 rt_preamble wave29 N=224 + WEAK_IO 178..181（L2 @ b9bac87b）
Windows：本波不需要切换
下一：g05 sed / Windows / Darwin CG002
```
