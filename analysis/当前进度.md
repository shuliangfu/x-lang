# X 语言 / X language 当前进度（2026-07-28 · wave615 Cap multi-dim INDEX subrow · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`aeac7201f`**（wave615 Cap freestanding multi-dim INDEX subrow fixed TYPE_ARRAY let · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave615（已闭 · Cap residual freestanding multi-dim INDEX subrow · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `let r: i32[2] = m[0]` freestanding CG002（host-C memcpy=42） |
| tip 产品 | **`aeac7201f`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave615 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 定长数组 let 拒 INDEX 初值 | `glue_struct_lit_store_fixed_array_field` 仅 VAR/FIELD/CALL | ✅ INDEX 并入 E* 逐元拷贝（≡ CALL `fill(n)`） |

**数据流（五问）**：产生 = multi-dim INDEX 子行（TYPE_ARRAY 留址，wave357）；存储 = let 槽 / STRUCT_LIT 字段 / 整数组 assign；消费 = 后续 INDEX load。host-C 权威 = `memcpy(dst, (m)[0], sizeof)`。G.7 **补全** 同一 `glue_struct_lit_store_fixed_array_field` — `init_ko=47` 与 CALL 共用 spill+esz load/store。

**权威**：`compiler/pipeline_glue.c`（`pipeline_glue_standalone.o` 覆盖）。改后 rm `build_asm/pipeline_glue_standalone.o` + g05。

### wave615 验证

| 探针 | mac L2 host/fs | Ubuntu L2 host/fs |
|------|----------------|-------------------|
| row_let `let r=m[0]` | ✅ 42（was fs CG002） | ✅ 42 |
| row1 `m[1]` | ✅ 3 | ✅ 3 |
| row_field `w.m[0]` | ✅ 42 | ✅ 42 |
| row_asg `r = m[0]` | ✅ 42 | ✅ 42 |
| row_var `m[i]` | ✅ 42 | ✅ 42 |
| row_deep `t[0][0]` | ✅ 42 | ✅ 42 |
| arr_copy / asg_ord 回归 | ✅ 42 | ✅ 42 |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/0/0 |

### 仍 soft

- 其它 Cap residual 叶（含 freestanding f32/f64 标量/数组 emit CG002）  
- 整波双端 **L4** + 升钉  
- 绝对 `XLANG` freestanding crt0 双前缀（ensure 路径，非本波）

### 下一波

1. Cap residual soft 其它叶（如 fs float），或整波 L4 + 升钉  

## wave614（已闭 · Cap residual arm64 freestanding INDEX assign · L2）

tip 产品 **`eb92a18b1`** / docs **`8fa734639`**。

## wave613（已闭 · Cap residual freestanding multi-dim nested ARRAY_LIT · L2）

tip 产品 **`82a1c1e1f`** / docs **`c3f870e1e`**。
