# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **pipeline_abi wave48 pure residual** @ `fbc5b80f` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **R2 pipeline_abi Cap residual pure wave48**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + pure-asm 矩阵 rv42 / option / hello / nest / stdlib-import；process_one pure **W** + tmp_parse/strdup residual **T** @ `fbc5b80f` |
| mac | ✅ L2 PREFER hybrid + c-backend 矩阵（Darwin pure-asm CG002 tip 既有）@ `fbc5b80f` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave48** @ `fbc5b80f`  
   - **迁 pure**：`shux_collect_deps_process_one`（orch）  
   - **G.7 复用**：`shux_load_one_direct_import_at`（resolve + read_file_view + preprocess → dep 槽）  
   - **Cap residual always-seed**：`shux_collect_tmp_parse_and_enqueue`（tmp arena/module malloc+memset + parse_into_bytes + enqueue）  
   - **仍 seed 重 C**：`paths_process_one` / `transitive_impl` / FILE view load 路径  
   - **seed**：冷孪生 `#ifndef FROM_X`；FROM_X rest 补 pure process_one **extern**  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave48.log` · `/tmp/ubuntu_g05_wave48.log` · matrix `/tmp/mac_g05_wave48_matrix.log` · `/tmp/ubuntu_g05_wave48_matrix2.log`

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：pipeline collect paths_process_one / transitive_impl / resolve / thread 重 C residual / 其它 rest pure  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave48 未升钉）
产品 tip：fbc5b80f pipeline_abi residual pure wave48 L2
Windows：本波不需要切换
下一：collect paths_process_one / transitive residual / Darwin CG002（需时再说）
```
