# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **pipeline_abi wave75 pure residual** @ `5421694c` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **R2 pipeline_abi Cap residual pure wave75**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + pure-asm 矩阵 rv42 / option / hello / stdlib-import；`entry_lib_*` pure **W** @ `5421694c` |
| mac | ✅ L2 PREFER hybrid + c-backend 矩阵（Darwin pure-asm CG002 tip 既有）@ `5421694c` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave75** @ `5421694c`  
   - **迁 pure**：`shux_cstr_typeck_lit` / `shux_entry_lib_keyword_lit` / `shux_entry_lib_name_from_path_impl` + thin `shux_entry_lib_name_from_path`  
     （模块 BSS keyword lits + `stem_buf[128]`；G.7 单权威；**关键词先于 std/ stem**——对齐 seed C，闭合历史 pure `std/` 优先双路径漂移）  
   - **顺带**：删 pure 死 `export extern`（`pipeline_asm_debug_enabled_impl` / `pipeline_set_dep_slots_impl` / `shux_import_dep_dir_from_path_impl`）  
   - **Cap residual 仍**：fn-ptr / product_emit / typeck_module C 前端（+ `typeck_dep_*` / `typeck_ndep` 跨 TU 全局 BSS；`shux_cstr_offset` 指针算术 residual）  
   - **seed**：冷孪生 `#ifndef FROM_X`（impl + lit + gate）  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave75.log` · `/tmp/ubuntu_g05_wave75.log`

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：pipeline_abi 其它 Cap residual 叶（fn-ptr / product_emit / typeck_module C 前端；typeck_dep 跨 TU 全局需另波收敛；cstr_offset…）/ 整波 tip L4  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave75 未升钉）
产品 tip：5421694c pipeline_abi residual pure wave75 L2
Windows：本波不需要切换
下一：pipeline_abi 其它 residual / 整波 tip L4（需时再说）
```
