# X 语言 / X language 当前进度（2026-07-29 · wave655 Cap parse_block return ASI · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 05:59 · 28 全日（wave534–617）已归楼；本文仅保留 29 日 tip 快照

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`027c42a49`**（wave655 parse_block return ASI） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave655（已闭 · Cap residual mid-block return + more stmts · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | if/while 块内 `return 3;`/`return 3` 后仍有 stmt → **XP003**（return 后仅 `}` 本绿；wave654 expr-stmt ASI 不覆盖 return 专用臂） |
| tip 产品 | **`027c42a49`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave655 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| return 后无 ASI；`;` 后 return_end_tail 双前进 | `parse_block_into` TOKEN_RETURN 臂 | ✅ 复用 `advance_past_stmt_semicolon_into`；下一 stmt 头 `stmt_tok_ready` 不再二次 advance |

**数据流**：产生 = parse_block return 臂在操作数后只认 `;`/`}`，且 `return 3;` 消费 `;` 后 `r` 已在下一语句上却仍走 `return_end_tail` 再 `lexer_next` 跳过。存储 = 无。消费 = pipeline XP003。G.7：与 expr-stmt 同一 ASI 权威；`RBRACE`→final_expr；否则 append expr_stmt + 保留 `r`。

**权威**：`compiler/src/parser/parser.x` `parse_block_into` · seed `parser_gen.linux.x86_64.c` / `parser_gen.c` 同 commit

### wave655 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| if 内 `return 3;` + more（expect 3） | ✅ 3 | ✅ 3（was XP003） |
| if 内 bare `return 3` + more（expect 3） | ✅ 3 | ✅ 3（was XP003） |
| return + let / nested if（expect 5） | ✅ 5 | ✅ 5 |
| bare return 块末 / return; 回归 | ✅ 3 | ✅ 3 |
| Cap-T001 `return 3; return x;`（expect 3） | ✅ 3 | ✅ 3 |
| wave654 break bare + more | ✅ 2 | ✅ 2 |
| continue bare + more | — | ✅ 9 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

日志：`/tmp/mac_g05_wave655.log` · `/tmp/ubuntu_g05_wave655.log` · `/tmp/ubuntu_w655_probes.log`

### 仍 soft

- untyped `self`（wave493 非标准 · **跳过**）  
- onefunc 顶层 bare `return 1` 后 `return 2` 无 `;` 仍 Cap-T001 硬失败（嵌套块已绿）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self），或整波 L4 + 升钉  

## wave654（已闭 · Cap residual bare break/continue ASI + IDENT · L2）

tip 产品 **`e6e56f13d`**。ASI 后继补 IDENT/control heads。

## wave653（已闭 · Cap residual freestanding while/for continue · L2）

tip 产品 **`07ee3bae0`**。function-body final_expr + for continue→step。

## wave652（已闭 · Cap residual x86 high-end STRUCT_LIT 固定数组字段 · L2）

tip 产品 **`be48b6acf`**。`glue_struct_field_frame_mag_c` x86 high-end base−foff。

## wave651（已闭 · Cap residual freestanding `m[i].xs` call-arg · L2）

tip 产品 **`a9256927e`**。FIELD over INDEX as T[N]/[]T call-arg。
