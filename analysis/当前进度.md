# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（本波未改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`11a57351`**（双端 129） |
| tip | **`ec2336da`**（R2 `async_cps_codegen` Cap residual pure wave2） |
| 产品 bstrict | **129** · 钉盘双端全绿 |
| 本波性质 | Cap residual pure wave2：module/sched resolve + void_entry 真迁 |
| 工程轨 prove | ✅ mac IDENTICAL · Ubuntu IDENTICAL（L2） |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `async_cps_codegen` Cap residual pure wave2**（`ec2336da`）  
   - `.x`：`async_cps_func_uses_void_entry` / `module_references_run_async` / `resolve_sched_target` / `module_has_sched_extern`  
   - helpers：`async_cps_cstr_eq` / `cstr_skip` / `module_func_at`  
   - 宿主布局：ASTFunc name@8 body@56 is_extern@64 is_async@68；ASTModule funcs@152 num_funcs@160  
   - `module_has_sched_extern`：prefix gate + cstr_eq（等价 seed snprintf+strcmp，无 FILE*）  
   - extern residual：`async_liveness_func_needs_cps_frame` / `func_has_await`  
   - seed：`#ifndef SHUX_ASYNC_CPS_CODEGEN_FROM_X` 省略 pure residual C；FROM_X 仅 `slice_marker` + FILE*/walk `_impl`  
   - surface re-pin（`-E`）+ prove MODULES 注释  

2. 验证  
   - mac：`SHUX=./compiler/shux bash tests/prove_module_selfhost.sh async_cps_codegen` → IDENTICAL · `/tmp/mac_prove_async_cps_wave2.log`  
   - Ubuntu：pull tip 后 prove IDENTICAL（-E 偶发 SIGSEGV 重试后绿）· `/tmp/ubuntu_prove_async_cps_wave2.log`  
   - **日常 L2 非 tip L4**；钉盘仍 `11a57351`  

3. 前序：wave1 `expr_is_*` @ `8d1f2452` · pure surface `d194aff5` · liveness pure · pool · 钉盘 `11a57351`

## 下一波前排

1. **CPS Cap residual 余**：walk `_impl`（typed AST）· FILE* emit（begin/end/after_await/sched）  
2. **或** liveness Cap residual：layout/type（无 FILE*）  
3. **可选** tip 升钉：Ubuntu L4 + 双端 129  
4. **可选** Windows hybrid（需切双系统时再喊人）  

```text
钉盘：11a57351
tip：ec2336da（CPS Cap residual pure wave2 module/sched；双端 L2 prove IDENTICAL）
Windows：本波不需要切换
下一：walk _impl / FILE* emit / liveness layout pure / 可选 tip L4
```
