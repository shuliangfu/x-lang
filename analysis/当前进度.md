# X 语言 / X language 当前进度（2026-07-28 · wave596 Cap pointer intermediate FIELD_ACCESS + arm64 sret x8 save · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`5c982f705`**（wave596 Cap pointer intermediate + arm64 sret · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave596（已闭 · Cap residual pointer intermediate FIELD_ACCESS + arm64 sret x8 save · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | freestanding `w.p.f` / `o.m.p.f` 读指针低 32 位错值；`mk(&i)` >16B sret 冲掉 x0 形参 |
| tip 产品 | **`5c982f705`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave596 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 链式 `w.p.f` 中间 `*T` 未解引用 | `pipeline_asm_emit_lvalue_eff_addr_*` 只 lea/add 偏移；INDEX 已有 `glue_index_deref_ptr_field_slot_rax`，FIELD 链缺 | ✅ FA 中间字段 *T/slice auto-deref（ELF+text） |
| CALL 链 `mk().m.p.f` 纯 offset 和 | `glue_field_access_call_base_rvalue_elf_c` 累加偏移跳过 *T load | ✅ 保留 chain_fa[]，root→outer 逐步 add + 中间 *T load |
| arm64 sret 序言冲 x0 | wave591 `mov x8→x0` + store 销毁第一 GP 形参（AAPCS64 x8 独立、无 GP shift） | ✅ `store_x_reg_to_rbp(reg=8)` 直存 x8 |

**数据流（五问）**：产生 = FIELD 链中间 *T / sret 序言；存储 = 指针值 / sret_home；消费 = 外层字段 load / 形参 home。权威：G.7 补全 lvalue 链 + call_base 链 + param_home sret 保存（不新开第二套）。

**权威**：`compiler/pipeline_glue.c`（`pipeline_glue_standalone.o`）。改后 rm `build_asm/pipeline_glue_standalone.o` + g05。

### wave596 验证

| 探针 | mac L2 | Ubuntu L2 pure-asm |
|------|--------|---------------------|
| ptr_simple `w.p.f` | ✅ fs/host 42 | ✅ 42 |
| ptr_field_only `w.p.f+w.a` | ✅ 42 | ✅ 42 |
| ptr_mid `o.m.p.f+a+c` | ✅ 45 | ✅ 45 |
| ptr_nest_call `mk(&i).m.p.f+a` | ✅ fs 41 | soft 139（>16B sret 深链，见 soft） |
| nest3 / nest4 回归 | ✅ 41 / 41 | ✅ nest3=41 · nest4 soft 139（>16B） |
| mk prologue disasm | ✅ `str x8,[fp,#sret]; str x0,[fp,#arg]` | — |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/0/0 |

### 仍 soft

- Ubuntu pure-asm **>16B sret** 深嵌套 CALL.field（nest4 / ptr_call 链）SIGSEGV（mac freestanding 已绿）  
- freestanding `take_m(o).f` dual-GP 形参字段抽取；`ARRAY_LIT` of CALL structs  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual：x86 sret 深嵌套 / take_field / arr_simple，或整波 L4  
2. 整波双端 L4 + 升钉  

## wave595（已闭 · Cap residual nested STRUCT_LIT in-place + arm64 dual-GP return）

tip `9bedc1917` / docs `1aa9ac54f`。soft「指针中间字段」本波由 **wave596** 收。
