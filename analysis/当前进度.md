# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`86281cc6`**（双端 L4 真冷 + 129；未升） |
| tip | **wave32 driver_abi Cap residual pure**（parsed accessors + try_c_after_pp；日常 L2） |
| 产品 bstrict | **129** · tip L4 钉盘仍 `@86281cc6` |
| 本波性质 | **工程轨 Cap residual pure**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42/opt/si/hello + pure-asm rv42 @ `cd367c6c`（pure 符号 W） |
| mac | ✅ L2 产品 C 路径矩阵绿（rv42 via `-backend c`/默认 C 路径；opt/si/hello）；pure-asm CG002 预存 |
| Windows | **本波不需要** |

## 本波摘要

1. **driver_abi Cap residual pure wave32（本波）**  
   - 真迁：`driver_parsed_input_path` / `out_path` / `lib_roots` / `n_lib_roots` / `want_asm` / `target` / `opt_level` / `use_lto` + `try_c_after_pp`  
   - 权威：`compiler/src/runtime_driver_abi_thin.x`（PREFER hybrid）  
   - LP64 `DriverCompileParsedAbi`：fixed offsetof + LE i32 / G.7 `shux_ptr_slot_get`；lib_roots = 嵌入数组基址 `p+16`；opt 默认 BSS lit `"2"`  
   - try_c：产品 `SHUX_NO_C_FRONTEND` 固定 return -2  
   - seed：`#ifndef SHUX_L2_RDABI_THIN_FROM_X` cold twins，无 pure-dup  
   - 代码 SHA：**`cd367c6c`**（docs 本 commit 跟进）  
   - 日志：`/tmp/mac_g05_wave32.log` · `/tmp/ubuntu_g05_wave32.log`  
   - 钉盘仍 **`86281cc6`**

2. **注释纪律（已固化）**  
   - 方法块注释 = JSDoc；参数类型与作用写全；日后 **`shux doc`** 生成  

3. **文档纪律**  
   - 只改 `自举进度.md` + 本文；**不**改 `当前问题分析.md` / `问题分析文档.md`

## 下一波前排

1. **可选** Windows hybrid；Darwin arm64 纯 asm / Vec4f CG002  
2. Cap residual 巨型表 always-seed；always-seed OS residual（host clock / pthread / fopen 表等）  
3. 其它 rest R2 / 整波 tip L4 升钉（波收口时）  

```text
钉盘：86281cc6（tip L4 双端 129）
tip：wave32 parsed accessors + try_c_after_pp pure（L2 @ cd367c6c）
Windows：本波不需要切换
下一：巨型表 / OS residual / Darwin CG002 / tip L4 收口
```
