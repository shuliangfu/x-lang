# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（本波未改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`11a57351`**（双端 129） |
| tip | **`e38f6624`**（G.7 fputs Cap residual 收敛 → `driver_preamble_fputs`） |
| 产品 bstrict | **129** · 钉盘双端全绿 |
| 本波性质 | Cap residual G.7：删 module-local async_*_fputs 双克隆 |
| 工程轨 prove | ✅ mac IDENTICAL · Ubuntu IDENTICAL（L2 · liveness+cps） |
| Windows | **本波不需要** |

## 本波摘要

1. **G.7 fputs Cap residual 收敛**（`e38f6624`）  
   - 权威：`driver_preamble_fputs`（`runtime_driver_abi` seed；opaque FILE*）  
   - `.x`：`async_liveness` / `async_cps_codegen` emit pure 改调 `driver_preamble_fputs`  
   - 删除 seed 内 `async_liveness_fputs` / `async_cps_fputs` 体；冷路径仍用原生 `fputs`/`fprintf`  
   - 保留模块本地 `*_fputs_i32_dec`（pure digit helper，非桥）  
   - surface re-pin + prove MODULES 注释；`runtime_driver_abi` 标注 G.7 权威  

2. 验证  
   - mac：`SHUX=./compiler/shux bash tests/prove_module_selfhost.sh async_liveness async_cps_codegen` → 2× IDENTICAL · `/tmp/mac_prove_fputs_g7.log`  
   - Ubuntu：同上 IDENTICAL · `/tmp/ubuntu_prove_fputs_g7.log`（无需 L3）  
   - cold seed `cc -c` OK；**日常 L2 非 tip L4**；钉盘仍 `11a57351`  

3. 前序：liveness FILE* emit @ `0efbc570` · CPS wave5 @ `e364aaf2` · 钉盘 `11a57351`

## 下一波前排

1. **或** 产品 PREFER 链 / `pipeline_glue` unbundle `async_asm_pool`（H=0 已 prove；glue 仍 #include 冷 seed）  
2. **可选** tip 升钉：Ubuntu L4 + 双端 129  
3. **可选** 其它 R2 rest / Cap residual  
4. **可选** Windows hybrid（需切双系统时再喊人）  

```text
钉盘：11a57351
tip：e38f6624（G.7 fputs → driver_preamble_fputs；双端 L2 prove IDENTICAL）
Windows：本波不需要切换
下一：PREFER unbundle async_asm_pool / 可选 tip L4
```
