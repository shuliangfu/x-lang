# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[自举进度-归档-2026-07-17.md](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **本波 ASM PRIMARY gate + random fk0**（钉盘仍 **`c51759eb`**） |
| 产品 L4 放行 | ✅ **钉盘 `c51759eb`**（不升钉） |
| tip 双端 bstrict | ✅ **123/123 @ `c51759eb`**（钉盘） |
| **typeck CTFE fold** | ✅ 主路径 + **scalar 2-arg** + **1-arg pure** + **nested** + **vector lane** |
| **ASM bulk PRIMARY residual** | ✅ **time/env/random OS 按 UNDEF 门控** + **random fk0** |
| **WPO call-site fold** | ✅ **flat + nested pre-emit** |

## 本波摘要（2026-07-18 · ASM bulk PRIMARY → on_demand gate）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；旁支「ASM bulk std on_demand」 |
| **地图** | std 多数已 fk0/flag 门控；残差 **PRIMARY_TIME_OS / RANDOM_FILL / ENV_OS** 仍无条件入链；门控后暴露 **random.o 无 fk0 探针** → asm BLD001 |
| **根修** | G.7：`labi_user_needs_runtime_{time_os,random_fill,env_os}` 门控 PRIMARY；env/random/time formal companion OS；**`labi_std_fk0_user_needs_rel` 补 std/random**；权威 `seeds/runtime_link_abi.from_x.c` |
| **验收** | mac L2：`runtime_link_abi.o`+g05；rv=42 · opt · hello · env/random/time C；Ubuntu L2：asm rv/hello/env/time/math · **random 修复后**；日志 `/tmp/ubuntu_primary_gate_matrix.log` |
| **残差** | plan 表仍列 bulk 步（多数 skip）；prepare ensure 仍预编 OS .o；（旁）Darwin pure-asm 旁支 |

### residual 下一波

1. 下一产品债：另开地图（如 TASK_SPECIAL gate / prepare ensure 收敛）；禁无地图 tip L4  

```text
构建级别：L2（runtime_link_abi.o + g05；非 tip L4）
产品 tip L4 钉盘：c51759eb
权威：shux_asm_ld_append_std_objs_for_user + labi_std_fk0_user_needs_rel
```

---

## 前波摘要（2026-07-18 · nested pure CALL → typeck CTFE）

| 波 | 结果 |
|----|------|
| **根修** | 一元 pure CALL + nested CTFE |
| **验收** | nested `-E return 0`；filtered pipeline |
| **残差** | → **本波 bulk PRIMARY 门控** |

---

## 前波摘要（2026-07-18 · vector lane WPO → typeck CTFE）

| 波 | 结果 |
|----|------|
| **根修** | INDEX NAMED SIMD + laneK(vec_binop) stamp |
| **验收** | `-E vec_const_spec_fold` → `return 0`；MONO 保留 lane0 call |
| **残差** | nested unary → **本波已迁 pre-emit** |

## 前波摘要（2026-07-18 · WPO scalar call-site fold → typeck CTFE）

| 波 | 结果 |
|----|------|
| **根修** | `glue_typeck_fold_expr_ref` scalar `f(c0,c1)`；MONO/NO_FOLD 门控 |
| **验收** | `-E const_spec_fold` → `return 0`；lang-const 12/12 |
| **残差** | vector lane → 前波闭；nested → **本波闭** |

## 前波摘要（2026-07-18 · LANG-006 residual：`~` + `&&`/`||`）

| 波 | 结果 |
|----|------|
| **根修** | unary `TOKEN_TILDE`→`EXPR_BITNOT`；bool→int init coerce |
| **验收** | lang-const **12/12**；矩阵绿 |
| **残差** | WPO call-site → 已闭 |

## 前波摘要（2026-07-18 · typeck CTFE 常量折叠权威恢复）

| 波 | 结果 |
|----|------|
| **根修** | typeck glue 恢复 fold 生产者 + typeck.x 挂钩 + C codegen 消费 folded |
