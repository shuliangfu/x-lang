# X 语言 / X language 当前进度（2026-07-24）

> **长期仪表盘**：[自举进度.md](自举进度.md)（24 日起重新起表）  
> **23 号全日归档**：[自举进度-归档-2026-07-23.md](自举进度-归档-2026-07-23.md)  
> **重命名分析**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)  
> **项目目录**：现行 `…/xlang/x-lang`  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| 工程 | **双端 tip L4 129 全绿** · 钉盘 **`ace59b6cd`** |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **已恢复** · tip L4 升钉后可继续 Cap residual |

## 验收

### tip L4 钉盘（**已升**）

| 项 | 状态 |
|----|------|
| SHA | **`ace59b6cd`**（wave363：NAMED score 64B + T[N]=0 + typeck u64 对齐） |
| 构建级别 | **L4 真冷**（全擦 compiler/std/core `.o` + 重链产品二进制） |
| remaining_o 起编 | ✅ **0**（双端） |
| 产品矩阵 | ✅ rv=42 · option · hello |
| mac 全量 bstrict | ✅ **OK (129)** · `/tmp/mac_true_cold_ace59b6cd.log` · `/tmp/mac_true_bstrict_ace59b6cd.log` |
| Ubuntu 全量 bstrict | ✅ **OK (129)** · `/tmp/ubuntu_true_cold_ace59b6cd.log` · `/tmp/ubuntu_true_bstrict_ace59b6cd.log` |
| 前钉 | `75c638465`（wave288；其后 70+ 波 L2 堆债 → 本波 L4 收口） |

### 本波根修（升钉内容）

| SHA | 摘要 |
|-----|------|
| `18f7ba42d` | typeck：u64↔usize 负例→正例（对齐 wave312） |
| `63ebeacf7` | freestanding `T[N]=0` 零填（array CG002） |
| **`ace59b6cd`** | **strict_minimal NAMED score `pnm`/`anm` 8→64**：`named_name_into` 固定 memcpy 64B；wave313 8B 缓冲 → `string.len(String)` 栈粉碎 / `__stack_chk_fail` / exit 127 |

### wave369（本步 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`928e3b389`**（ensure）· 连带 **`7e1b6a8b3`**（ZST store_sz / slot metrics） |
| 债 | freestanding **中部/末位** empty-of-empty Nest（`Box { a, n: Nest{e:Empty{}}, b }`）退出码乱 |
| 根修 | ① `ensure_struct_layout_from_struct_lit`：parser `nf >= lit nf` **禁止 append**（防 Box 扩成 nf=4 n/b/b/a）；set_field 前重读 field 名（scratch）② slot metrics `mrc==0 && sz==0` 真 ZST 不 invent ③ STRUCT_LIT empty ZST `store_sz=0` skip 写指针。typeck.x+seed+glue/ast_pool 同向 |
| 构建级别 | **L2**（typeck_x + pipeline_glue_standalone + **pipeline_x 强制** + filtered + g05；Linux 须 pipeline_x 带新 glue） |
| 钉盘 | 仍 **`ace59b6cd`** |
| 探针 | Ubuntu fs nest_mid/last/first/nofold/eoe=42 · mid_e/sub=38 · host=42；mac fold/sub=38；矩阵 rv/option/hello |
| soft | untyped-let；reentrancy；`mut`；trait/impl |

```text
下一：wave370 Cap residual（日常 L2）
  leave-off：untyped-let / reentrancy / mut / trait…
  钉盘 ace59b6cd；约每 10–15 波或日终 tip L4
```

### wave368（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`2eb763a4d`** |
| 债 | 全空字段嵌套 ZST（`Nest { e: Empty }` 作他结构字段）metrics **T001** |
| 根修 | G.7 扩展 `typeck_type_is_empty_struct`：`nf==0` **或** 全字段递归 empty（+depth>64）；glue twin；seed typeck_gen/empty_surface 同 commit |
| 构建级别 | **L2** · 钉盘仍 **`ace59b6cd`** |

### wave367（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`e2f8e07e8`** |
| 债 | STRUCT_LIT **末字段** `A { x: 42, e: E {} }` 解析 P001 / out_len=0 |
| 根修 | G.7 field-value depth + wave364 empty `Type {}` 在 field 下遇外层 `}` 仍 keep struct lit |
| 构建级别 | **L2** · 钉盘仍 **`ace59b6cd`** |

### wave366（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`1c1c1cd59`** |
| 债 | freestanding **`Empty {}` CG002** + nested empty field **T001**（`fsize<=0`） |
| 根修 | G.7 `typeck_type_is_empty_struct` + metrics 接受 ZST fsize==0；glue twin；STRUCT_LIT `nf==0` |
| 构建级别 | **L2** · 钉盘仍 **`ace59b6cd`** |

### wave365（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`8ec8a48f0`** |
| 债 | host-C **`struct Empty {}`** incomplete type BLD001 |
| 根修 | G.7 仅 `nl<=0` 跳过；`nf==0` 发 `struct T {\n};\n` |
| 构建级别 | **L2** · 钉盘仍 **`ace59b6cd`** |

### wave364（已闭 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`d0dc16218`** |
| 债 | bare **`if b {}` / `while b {}`** 空体 XP003 |
| 根修 | G.7 `parser_asm_primary_empty_ident_braces_prefer_block_c` |
| 构建级别 | **L2** · 钉盘仍 **`ace59b6cd`** |
