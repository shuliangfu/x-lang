# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`86281cc6`**（双端 L4 真冷 + 129；未升） |
| tip | **wave30 g05 sed 根修**（mkstemp/rename → codegen skip；日常 L2） |
| 产品 bstrict | **129** · tip L4 钉盘仍 `@86281cc6` |
| 本波性质 | **工程轨 G.7 双权威根修**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 codegen_x + g05 + 矩阵 rv42/opt/si/hello + pure-asm rv42 @ `cae60d4c` |
| mac | ✅ L2 产品 C 路径矩阵绿；`-E` thin 0× extern mkstemp/rename；pure-asm CG002 预存 |
| Windows | **本波不需要** |

## 本波摘要

1. **g05 sed dual-auth root fix wave30（本波）**  
   - 地图：sed 有、codegen skip 无 = **`mkstemp` / `rename`**（thin.x `export extern`）  
   - 根修：`codegen_is_libc_conflicting_extern_name` 补两项（`.x` + seed 同 commit）  
   - g05 sed **保留**防御：cold/old `-E`、opendir opaque（产品故意不 skip）、`shux_fmt_*` / `shux_driver_*` harness  
   - 验证：`-E` thin **0** `extern *mkstemp/rename`，调用仍在  
   - 代码 SHA：**`cae60d4c`**（docs 本 commit 跟进）  
   - 日志：`/tmp/mac_g05_wave30b.log` · `/tmp/ubuntu_g05_wave30.log`  
   - 钉盘仍 **`86281cc6`**

2. **注释纪律（已固化）**  
   - 方法块注释 = JSDoc；参数类型与作用写全；日后 **`shux doc`** 生成  

3. **文档纪律**  
   - 只改 `自举进度.md` + 本文；**不**改 `当前问题分析.md` / `问题分析文档.md`

## 下一波前排

1. **可选** Windows hybrid；Darwin arm64 纯 asm / Vec4f CG002  
2. Cap residual 数据（巨型表）长期 always-seed  
3. 其它 rest R2 / 整波 tip L4 升钉（波收口时）  

```text
钉盘：86281cc6（tip L4 双端 129）
tip：wave30 mkstemp/rename codegen skip（L2 @ cae60d4c）
Windows：本波不需要切换
下一：Windows / Darwin CG002 / tip L4 收口
```
