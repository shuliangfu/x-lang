# X 语言 / X language 当前进度（2026-07-29 · wave637 Cap array-of-ptr · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`e82ecdbd2`**（wave637 产品） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave637（已闭 · Cap residual freestanding array-of-pointers `*T[N]` · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | freestanding `let a: *i32[2] = [&x,&y]; *a[0]+*a[1]` SEGV/错值；host 绿 |
| tip 产品 | **`e82ecdbd2`**（`7dbc5278a` INDEX esz + `e82ecdbd2` slot） |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave637 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| INDEX 元素宽 | `pipeline_asm_index_elem_byte_sz_c` 对 INDEX 结果 TYPE_PTR 调 peel helper → *i32 当 base 剥成 4 | ✅ 结果 TYPE_PTR **return 8**；ARRAY/SLICE of PTR esz=8；**T base stride 8 |
| 帧槽宽 | `glue_fixed_array_total_bytes` / `asm_local_slot_bytes` PTR 元默认 4 → `*i32[2]` 槽 8B | ✅ PTR 元 **8**；Ubuntu high-end a[1] 不再盖 y |

**数据流**：产生 = INDEX esz + fixed-array slot size → 存储 = 指针数组帧 → 消费 = load ptr + DEREF。G.7：补全既有 index_elem / fixed_array_total / local_slot，不新开第二套 INDEX。

**权威**：`pipeline_glue.c` · `pipeline_asm_index_elem_byte_sz_c` · `glue_index_elem_byte_sz_from_type_ref_c` · `glue_fixed_array_total_bytes_c`；`ast_pool.c` · `asm_local_slot_bytes_mod`。

### wave637 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `*i32[2]` lit INDEX sum pure/fs/host | ✅ 42 | ✅ 42 |
| `a[i]=&x` assign fs | ✅ 42 | ✅ 42 |
| wave636 `*[2]i32` p2a fs | ✅ 42 | ✅ 42 |
| plain `i32[2]` INDEX | ✅ 42 | ✅ 42 |
| `*i32` base `p[i]` stride 4 | ✅ 42 | ✅ 42 |
| Ubuntu disasm | — | `lea -0x30`；`add $8`；`mov (%rax)` 64 + `mov (%rax)` 32 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

### 仍 soft

- untyped `self` method resolve  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶，或整波 L4 + 升钉  

## wave636（已闭 · Cap residual pure-asm + host-C PTR→fixed TYPE_ARRAY · L2）

tip 产品 **`7bce90b01`**。`*[N]T` DEREF leave-addr + host `E (*)[N]`。

## wave635（已闭 · Cap residual pure-asm fixed TYPE_ARRAY call-arg E* not MEMORY · L2）

tip 产品 **`b5615797e`**。freestanding `take(S24[2])` 双端 77。

## wave634（已闭 · Cap residual soft 假红：uninit TYPE_ARRAY assign · docs-only）

产品 tip 仍曾为 `9a16fb325`；合法 `let s: S24[2]; s=mk();` 双端 110。

## wave633（已闭 · Cap residual pure-asm fixed TYPE_ARRAY let bulk esz>8）

tip 产品 **`9a16fb325`**。fixed S24[2] let bulk 绿。
