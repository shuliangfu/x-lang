# X 语言 / X language 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **重命名分析**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)  
> **改名前锚点**：`anchor-pre-xlang-rename` @ `d07bcf155`（父钉曾 `a77fc5d90`）  
> **项目目录**：现行 `…/xlang/x-lang`（mac/Ubuntu/Windows 同构）  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only**（第三品牌 `shu_*` / `SHU_*` / `shux` 已清） |
| 主域 | **xlang.dev** |
| 工程 | **全库重命名 R0–R3 已闭** · 双端 L4 **129 全绿** · 钉盘 **`3698e557f`** |
| process_xlang 兼容别名 | **已删除** |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **已恢复** · **wave262** Cap residual pure 已闭（path_pure/invoke_ld private helper 短名 + surface re-pin） |

## 验收

### tip L4 钉盘（不变）

| 项 | 状态 |
|----|------|
| SHA | **`3698e557f`**（`rename(R3): shu_* → xlang_*`） |
| 构建级别 | **L4 真冷**（全擦 compiler/std/core `.o` + 重链产品二进制） |
| mac / Ubuntu 全量 bstrict | ✅ **OK (129)** |

### 本波 tip（wave262 · 日常 L2，**≠** tip L4 升钉）

| 项 | 状态 |
|----|------|
| SHA | **`4c7450d13`** — R2 Cap residual pure wave262 |
| 构建级别 | **L2**（`XLANG_G05_PREFER_X_O=1` g05 hybrid + 产品矩阵 + prove 邻域） |
| mac 矩阵 | ✅ rv=42 · option · si · hello · `/tmp/mac_g05_wave262c.log` · `/tmp/mac_w262_*.log` |
| mac prove | ✅ path_pure / invoke_ld / path_io / ensure / diag / invoke_cc **IDENTICAL** · `/tmp/mac_w262_prove2.log` |
| Ubuntu 矩阵 | ✅ rv=42 · option · si · hello · `/tmp/ubuntu_wave262_g05b.log` · `/tmp/ubuntu_w262_*.log` |
| Ubuntu prove | ✅ path_pure / invoke_ld / path_io / ensure / diag / invoke_cc **IDENTICAL** · `/tmp/ubuntu_w262_prove.log` |
| 产品钉盘 | 仍 **`3698e557f`**（本波未 L4 升钉） |

```text
下一：wave263 Cap residual
  （path_pure / invoke_ld private helper 短名 + surface re-pin 闭 @ wave262
   · host residual leave-off：link_abi_getenv_impl / link_abi_system_impl
     + link_abi_realpath_cap_impl 体 + resolve pool 多槽结构
   · freestanding bootstrap_nostdlib_stubs 自实现 getenv/system/realpath 保留
   · soft residual：labi_ondemand_list .x -E 丢 14 个 export（含
     labi_std_fk0_user_needs_rel / labi_user_needs_std_task /
     xlang_asm_ld_append_on_demand_user_objs 等）→ PREFER full L8b 链缺符号；
     本波 g05 用 L8b seed 回退（hide .x）通过双端 L2；根修在 codegen/typeck
     完整发射 ondemand 大模块，或 surface re-pin + 容量债）
  钉盘仍 3698e557f；日常 L2，整波收口再 L4
```
