# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **pipeline_abi wave53 pure residual** @ `6a127355` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **R2 pipeline_abi Cap residual pure wave53**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + pure-asm 矩阵 rv42 / option / hello / import / stdlib-import / overload；paths_tmp / tmp_parse pure **W**；Cap residual strdup / resolve_read_preprocess **T** @ `6a127355` |
| mac | ✅ L2 PREFER hybrid + c-backend 矩阵（Darwin pure-asm CG002 tip 既有）@ `6a127355` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave53** @ `6a127355`  
   - **迁 pure**：`shux_collect_paths_tmp_resolve_parse_enqueue` orch（early ensure tmp + Cap residual resolve_read_preprocess + G.7 pure tmp_parse + free prep）  
   - **G.7**：paths_process_one 复用 pure paths_tmp；resolve 仍单一 Cap residual  
   - **仍 seed 重 C**：strdup / resolve_read_preprocess / thread emit  
   - **seed**：冷孪生 `#ifndef FROM_X`；FROM_X rest 补 pure paths_tmp **extern**  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave53.log` · `/tmp/ubuntu_g05_wave53b.log` · matrix 本机 / Ubuntu 会话输出

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：strdup Cap residual / resolve_read_preprocess 仍 seed / thread emit residual  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave53 未升钉）
产品 tip：6a127355 pipeline_abi residual pure wave53 L2
Windows：本波不需要切换
下一：strdup / resolve_read residual / thread residual pure / Darwin CG002（需时再说）
```
