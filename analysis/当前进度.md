# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **pipeline_abi wave47 pure residual** @ `1dd91711` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **R2 pipeline_abi Cap residual pure wave47**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + pure-asm 矩阵 rv42 / option / hello / nest / stdlib-import；pure **W** + strdup residual **T** @ `1dd91711` |
| mac | ✅ L2 PREFER hybrid + c-backend 矩阵（Darwin pure-asm CG002 tip 既有）@ `1dd91711` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave47** @ `1dd91711`  
   - **迁 pure**：`shux_collect_seed_to_load` · `shux_collect_enqueue_module_imports`（G.7 复用 wave46 import cstr / to_load_has / find_loaded / ptr slots）  
   - **Cap residual always-seed**：`shux_collect_strdup`（禁直接 export-extern libc `strdup`，-E 后与 `string.h` 冲突）  
   - **仍 seed 重 C**：`process_one` / `transitive_impl`（read_file + preprocess + FILE view）  
   - **seed**：冷孪生 `#ifndef FROM_X`；FROM_X rest 补 pure collect 符号 **extern**  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave47b.log` · `/tmp/ubuntu_g05_wave47.log` · matrix `/tmp/mac_g05_wave47_matrix.log` · `/tmp/ubuntu_g05_wave47_matrix2.log`

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：pipeline collect process_one / read_file / resolve / thread 重 C residual / 其它 rest pure  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave47 未升钉）
产品 tip：1dd91711 pipeline_abi residual pure wave47 L2
Windows：本波不需要切换
下一：collect process_one / read_file residual / Darwin CG002（需时再说）
```
