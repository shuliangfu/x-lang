# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`1f73ed275`**（双端 tip L4 真冷 + 129；未升 — 本波日常 L2） |
| tip 产品代码 | **pipeline_abi wave87 typeck→x_ast 路由** @ `a395783e` |
| 产品 bstrict | **129** · 钉盘 tip L4 全绿；wave87 **日常 L2** 矩阵绿（未全量 bstrict） |
| 本波性质 | **wave87 typeck_module C 前端 Cap residual 路由收敛** |
| Ubuntu | ✅ L2 g05 hybrid pure **W**（for_ctx）+ 矩阵 rv42/option/si/hello + nest #if + force_c rv42 @ `a395783e` · `/tmp/ubuntu_g05_wave87.log` |
| mac | ✅ L2 g05 hybrid pure for_ctx→typeck_x_ast + 矩阵 rv42/option/si/hello + nest #if + force_c rv42 @ `a395783e` · `/tmp/mac_g05_wave87.log` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave87** @ 产品 **`a395783e`**  
   - `pipeline_typeck_module_for_ctx_impl`：G.7 路由 → `typeck_x_ast` / `typeck_x_ast_library`  
   - 按 `pipeline_module_main_func_index` 选 entry/library（≡ `pipeline_impl_typecheck`）  
   - 不再调用已删 C `typeck_module`（曾仅 glue weak -1）  
   - `typeck_module_entry_only` / `with_sidecar` fail-closed（X 需 arena+ctx，走 for_ctx）  
   - seed 冷孪生 `#ifndef FROM_X` 同语义  
   - 闭合 Cap residual「typeck_module C 前端」叶  
   - Cap residual 仍：preprocess_x_buf（preprocess.x 已 pure 引擎，pipeline orch export-extern）；g05 &fn cast  
   - **构建级别 L2**（日常真测；非 tip L4）  
2. 继承 tip L4 钉盘 **`1f73ed275`**（wave76–81 收口；wave82–87 未升钉）

## 下一波前排

1. rest R2：pipeline_abi **硬 Cap residual** 叶（preprocess_x_buf 跨 TU / g05 &fn cast）  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 硬 Cap / soft 再有整波收口时再 tip L4（非每微步）

```text
钉盘：1f73ed275（tip L4 双端 129；本波未升）
产品 tip：a395783e pipeline_abi residual pure wave87 typeck→x_ast
文档：21 号归档 · 22 号 wave76–87 + tip L4 升钉 @1f73ed275
Windows：本波不需要切换
下一：pipeline_abi 硬 Cap residual（preprocess_x_buf / g05 &fn）
```
