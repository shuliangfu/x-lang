# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`07684b813`**（双端 tip L4 129；未升 — wave105 日常 L2） |
| tip 产品代码 | **`4918983cf`** wave105 pure-asm hello METHOD_CALL leaf |
| 产品 bstrict | **129** · 钉盘继承；本波未 tip L4 |
| 本波性质 | **pure-asm 扩矩阵 · hello 叶**（日常 L2 非 tip L4） |
| Ubuntu | ✅ L2 g05 + pure-asm **hello build/run**（U `std_fmt_println`）；rv42/si；option pure-asm SEGV residual |
| mac | ✅ L2 g05 + c-backend 矩阵；pure-asm rv42；hello pure-asm **`.o` CG002**（arm64 ta≠0 字符串 embed residual；`-o` exe 仍 C 回退） |
| Windows | **本波不需要切换** |

## 本波摘要

1. **切入**：ImportEntry 结构债暂缓 → pure-asm 扩矩阵 option/hello  
2. **根因**：产品解析 `fmt.println("…")` 为 **METHOD_CALL (49)**，非 CALL+FIELD_ACCESS；METHOD_CALL import-binding 缺 fmt 字符串 lit 特化 → 误 mangle `println_i32_reti32` → Ubuntu pure-asm BLD001  
3. **wave105**：`glue_asm_try_emit_fmt_string_lit_import_call_elf_c` 接受 CALL/METHOD_CALL 实参表；method_call 路径先特化再 mangle；seed + `.x` 同 commit  
4. **金标**：Ubuntu pure-asm hello 绿；option pure-asm SEGV / Darwin arm64 string embed 仍 residual  

## 下一波前排

1. pure-asm option SEGV / Darwin arm64 string-lit embed（ta≠0）  
2. ImportEntry 结构债（ast_pool GrowVec；重设计）  
3. **可选** Windows hybrid  

```text
钉盘：07684b813（未升）
产品 tip：4918983cf wave105 pure-asm hello METHOD_CALL
日常 L2 · 非 tip L4
Windows：本波不需要切换
下一：pure-asm option SEGV / arm64 string embed / ImportEntry
```
