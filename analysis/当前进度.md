# Shux 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-22.md](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d6648bee4`**（升自 `3c88f7e51`；双端 tip L4 + 129） |
| tip 产品代码 | **wave214** L4 cold bank unify + comment gate @ **`d6648bee4`**（= 钉盘） |
| 产品 bstrict | 钉盘 **129**；本波 **tip L4** 双端全绿 |
| 本波性质 | **tip L4 批闸门** · cold bank `void*` 签名统一 · comment gate 修 |
| Ubuntu | ✅ L4：全擦 + g05 + 矩阵 + bstrict 129 |
| mac | ✅ L4：全擦 + g05 + 矩阵 + bstrict 129 |
| Windows | **不需要切换** |

## 本波摘要

1. **L4 节奏**：前钉 `3c88f7e51` 于 07-22 ~14:30；此后 ~213 commit（wave111–213）全是日常 L2。用户问「要不要跑 L4」→ **要**（skill §3.3 一批 R2 收口 / 日终兜底）。
2. **wave214 冷路径根修**：L4 全擦后 mega 冷编译报 `try_under_lib_roots` / `push_stable` **bank 指针类型冲突**（pure/`void*` vs mega/`ShuAsmLdPathBank*`；hybrid L2 不进同一 TU）。对齐为 opaque `void*` + `bank_push` cast（header + mega + ondemand）。
3. **bstrict 闸门**：`// Skip ld flags` 后 `continue;` 被 `verify_comment_prefixes` 误判为破续行 → CODE_START 补 `continue|break|…` + 改块注释。
4. **验收**：双端 remaining_o=0 @ `56c49d17e`；矩阵绿；gate 后 **129** @ `d6648bee4`。钉盘升 **`d6648bee4`**。

## 下一波前排

1. 常驻 Cap residual 可迁叶：wait / strerror / getenv / resolve …（_impl / resolve 池 / realpath / spawn·cc / system·make 仍 hard Cap）
2. 可选：未闭合块注释硬诊断（EOF depth>0）
3. 可选 Windows hybrid（**需要时再通知重启**）

```text
钉盘：d6648bee4（升自 3c88f7e51）
tip：d6648bee4 wave214 tip L4 批闸门
构建级别=L4 真冷（全擦 .o + 双端 129）
Windows：不需要切换
下一：常驻 Cap residual（wait / strerror / getenv / resolve …）
```
