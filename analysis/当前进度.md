# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) · **验证**：[自举验证.md](自举验证.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)  
> **纪律**：每波更新 **本文 + 自举进度**  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)（20 号全日）· [19](自举进度-归档-2026-07-19.md) · …

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`11a57351`** |
| 产品 L4 放行钉盘 | **`deaf773b`**（125；tip **未**升钉） |
| 产品 bstrict 套件 | **129** |
| 本波性质 | **产品轨 P0 收口**（mac 129 绿 → Ubuntu L4 金标 → 再谈 R2） |
| 构建级别（mac 129） | 产品链 `SHUX_BSTRICT_SKIP_BUILD=1` + 本波 `shux_asm`（非 BEST_EFFORT） |
| Ubuntu L4 | ⏳ 全擦 + bootstrap + g05 + 矩阵 ✅；全量 bstrict **跑中** |

## 本波摘要（2026-07-21 · 产品轨 P0）

### 已完成

1. **`d45ee9a4` 产品根修**  
   - net-context：io stubs `SHUX_WEAK`；`std.context` 入 `pipeline_codegen_std_dep_link_only`  
   - C 后端 user `.o` 不再被 `shux_invoke_cc` 丢弃（setter/clearer 单一权威）  
   - 去掉 parser 不支持的 C-style `(void)cast`（防挂死）

2. **P0 甄别 + harness**（`11a57351`）  
   - best-effort 8 gate 产品链甄别：**7 绿 + 1 harness 假红**（无 typeck 产品真红）  
   - `run-json` → Darwin heavy 列表；默认 heavy cooldown **15s**  
   - `lang-unsafe` 不再强制 `SHUX=./compiler/shux`（用 `SHUX_BIN`）  
   - mac：`run-all-bstrict OK (129 scripts, compiler/shux is shux_asm)`  
     日志：`/tmp/mac_bstrict_d45ee9a4_p0.log`

3. **文档**  
   - 债地图：[当前问题分析.md](当前问题分析.md)（替换 `问题分析文档.md`）  
   - 20 号全日 → [自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)  
   - 本文 + 自举进度 21 号起表

### 进行中

| 步骤 | 状态 |
|------|------|
| push `11a57351` | ✅ 已 push |
| Ubuntu pull + 全擦 `.o` | ✅ `remaining_o=0` |
| `bootstrap-driver-seed` | ✅ smoke OK · shux ~3.4MB |
| `g05_prepare_and_relink` | ✅ 60 objs Linux/x86_64 · shux_asm 同步 |
| 产品矩阵 rv/option/hello/si | ✅ rv=42 · hello 输出 OK |
| 全量 `run-all-bstrict` | ⏳ `/tmp/ubuntu_true_bstrict_11a57351.log` |

### 仍挂（P1，不挡本波升钉叙事）

- codegen：`extern function … : *u8` → 误 `extern int`（getenv 等）  
- parser：源末无 `\n` → 偶发 `num_funcs=0`  
- 细节见 [当前问题分析.md](当前问题分析.md) §3.3+

## 下一波前排

1. **收 Ubuntu L4 bstrict** → 双端绿 → **升 tip 钉盘**（写清 129）  
2. **P1 根源**（codegen extern 返回类型 / parser EOF）— 一条债一个 commit  
3. **R2 下一 rest** — **仅** tip 产品轨双端绿之后

## 不在前排

- C5 EXPR_CLOSURE CTFE、C9 match guard（独立大工程）  
- Windows L4 真冷（非双端硬门；hybrid 已在钉盘 `deaf773b` 绿过）  
- 20 号已归档流水（R2 target_cpu_pure / C6 波 3 / C7 / parser has_body 等）— 只读归档

```text
tip：11a57351
钉盘：deaf773b（125）— tip 待 Ubuntu 全绿后升钉
mac：run-all-bstrict OK (129) · /tmp/mac_bstrict_d45ee9a4_p0.log
Ubuntu：L4 冷启+矩阵绿 · bstrict 进行中 · /tmp/ubuntu_true_bstrict_11a57351.log
地图：analysis/当前问题分析.md
归档：analysis/自举进度-归档-2026-07-20.md
PLATFORM：SHARED 产品面须双端；Darwin OOM 为环境 harness
```
