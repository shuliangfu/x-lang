# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **wave43 driver_abi Cap residual pure** @ `77a23d78`（parser 根修 `c5994b0c`） |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **residual pure 微波 + parser 根修**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42 @ `77a23d78`（pure 符号 **W**） |
| mac | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42 @ `77a23d78` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **parser 根修 `c5994b0c`（wave43 探针归因）**  
   - **非**多行 `export extern` 问题（A/B/H 复现：多行 export extern 后简单 export **正常入 AST**）  
   - **真因**：形参/let 名 `self` → lexer `TOKEN_SELF`（ident_len=0）；param 只认 `TOKEN_IDENT` → `set_onefunc_fail` → **整函数从多函数 AST 静默丢失**  
   - 根修：param / let / primary EXPR_VAR **接受 TOKEN_SELF**，绑定名固定拼写 len=4 从 `token_start` 拷贝  
   - 权威：`parser.x` + `seeds/parser_gen.linux.x86_64.c` + `parser_asm_primary_slice.inc` 同 commit  

2. **R2 driver_abi Cap residual pure wave43** @ `77a23d78`  
   - `driver_dispatch_sibling_try_spawn` pure orch：null/argc + G.7 `driver_argv0_basename_is` + path pure BSS 512  
   - Cap residual always-seed：`shux_driver_sibling_argv0_get` + `shux_driver_sibling_access_spawn`  
   - seed cold twin `#ifndef FROM_X`；无 pure-dup  
   - **仍 always-seed**：`print_usage_write`（巨型 usage lit + color 表）  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave43f.log` · `/tmp/ubuntu_g05_wave43b.log`；Ubuntu pure **W**

3. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. Cap residual 下一刀：**print_usage_write** 仍 always-seed（巨型 usage lit；可拆 color orch + residual write）  
2. **可选** Windows hybrid；Darwin arm64 纯 asm / Vec4f CG002  
3. 其它 rest R2（非每微步 L4；下一整波收口再 tip L4）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave43 未升钉）
产品 tip：77a23d78 wave43 L2（parser c5994b0c）
Windows：本波不需要切换
下一：print_usage_write 或其它 rest R2
```
