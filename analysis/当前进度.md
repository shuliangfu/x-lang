# X 语言 / X language 当前进度（2026-07-25）

> **长期仪表盘**：[自举进度.md](自举进度.md)（25 日起重新起表）  
> **24 号全日归档**：[自举进度-归档-2026-07-24.md](自举进度-归档-2026-07-24.md)  
> **重命名分析**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)  
> **项目目录**：现行 `…/xlang/x-lang`  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| 工程 | **双端 tip L4 129 全绿** · 钉盘 **`70ca1089b`**（升自 `605e7efa4`） |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **续 Cap residual** · wave445 已闭；钉盘 L4 **`70ca1089b`**；下一 wave446 |

## 验收

## 25 日承接（wave445 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`d759421df`** |
| 债 | **generic 函数完整单态化假绿**（identity mono body 硬编 `return param0` → `foo<T: Clone>(x: T): T { return x.clone(); }` 编译通过 run=42 但 clone() 从未调用） |
| 根因 | wave444 的 `codegen_try_emit_generic_identity_mono`（C1+C2+C3）解决了符号冲突但 body 仍硬编 `return param0`（identity shape），非 identity body（含方法调用 `x.clone()`）的泛型函数体从未被遍历 emit → 方法调用被静默跳过 → run=42 假绿 |
| 根修 | codegen.x 单源（同步 seed `codegen_gen.linux.x86_64.c` + runtime_pipeline_abi.h + ast.x + emit.x）— C4+C5+C6：① **C4 body AST 遍历** — `codegen_try_emit_generic_identity_mono` 替换硬编 `return param0` 为 `emit_block`/`emit_expr` 遍历 body AST（设置 `ctx.mono_active=1` + `mono_generic_type_refs`/`mono_concrete_type_refs` 映射 T→具体）；② **C5 类型替换** — `emit_type` 添加类型替换 hook（`mono_active=1` 时，type_ref 匹配 `mono_generic_type_refs[i]` 则替换为 `mono_concrete_type_refs[i]`；TYPE_NAMED 走名称匹配 fallback——`pipeline_type_named_name_into` 比较类型名，解决 `let y: T` 的 T type_ref 与 param T type_ref 不同节点问题）；③ **C6 方法调用 per-mono 重新解析** — `emit_expr` EXPR_METHOD_CALL 分支添加 mono 分支，receiver 类型推断三段式（`pipeline_expr_resolved_type_ref` 返 0 → param-name fallback `x.dup()` → local-let fallback `y.dup()`）+ `codegen_mono_subst_type`（含 name-match fallback）替换 T→具体 + `codegen_find_impl_method_for_type`（含 name-match fallback）查 impl 方法 + receiver 作 self 首参 emit + module 前缀 emit（防与 libc `dup(int)` 冲突）。**PipelineDepCtx SHARED ABI 扩展**（append-only）：`mono_active`/`mono_num_types`/`mono_generic_type_refs[8]`/`mono_concrete_type_refs[8]` |
| 构建级别 | **L2**（codegen_x.o + g05） |
| 钉盘 | 仍 **`70ca1089b`** |
| 探针 | **mac** w445_method_call（`x.dup()` param receiver run=42）· w445_let_binding（`let y: T; y.dup()` local-let receiver run=42；C5 类型替换 `let y: T`→`let y: A` 生效无 `ast_T`）· w445_no_method（identity mono `foo<T>{ return x }` run=42）· w445_clone_v2（`function`+显式 `self: A` 语法 `x.clone()` run=42）· w445_clone_transform（`fn`+`-> Self`+无类型 `self` 语法 typeck gap → BLD001，typeck 债非 codegen）· wave444 regression（multi_mono/three_mono/single/multi_arg=42）· wave443 regression（multi_arg/three_arg/single_ret/bound_ok/compare/no_bound=42）· wave439 bound regression（bound_violation 红 T001 / bound_ok/minimal/no_bound=42）· 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（588s） |
| Ubuntu | ✅ 日常 L2：探针（18 绿+4 红全 OK：w445 method_call/let_binding/no_method/clone_v2=42 + clone_transform 红；w444 multi_mono/three_mono/single/multi_arg=42；w443 multi_arg/three_arg/single_ret/bound_ok/compare/no_bound=42 + bound_violation/multi_bound_viol 红；w439 bound_ok/minimal/no_bound/compare=42 + bound_violation 红）+ 矩阵核心绿（hello/option/import/rv42/types/fmt）+ bstrict 129 OK（170s skip build）；矩阵 exit=1 为 pre-existing `asm_libroot/std/fs/mod.x` L7 unused-private-function lint 债（wave445 未碰该文件，最后改于 `d60903807` rename）；SHARED ABI 改动 L4 待阶段性收口 |
| soft | dyn Trait；generic 调用无 turbofish `<T>` 类型推断（XP003）；`fn`/`-> Self`/无类型 `self` 语法 typeck gap（typeck 未从 impl 上下文推断 self 类型）；Ubuntu 矩阵 pre-existing std/fs L7 lint 债 |

## 25 日承接（wave444 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`cc721e73d`** |
| 债 | **generic mono 单实例符号冲突**（`copy<A>(a)` + `copy<i32>(n)` 同泛型函数多类型调用 → 单实例化共享裸 link_name → C link 冲突/类型不匹配 BLD001） |
| 根因 | identity mono 早期实现仅生成首个 call site 的 mono 实例并用裸 link_name（`codegen_emit_func_link_name`），多 type-arg combo 无法共存 → wave443 多参数 emit 后多类型调用暴露符号冲突 |
| 根修 | codegen.x 单源（同步 seed `codegen_gen.linux.x86_64.c`）— C1+C2+C3：① C1 `codegen_emit_mono_mangled_name` 生成 `<link_name>__<suffix>` mangled 符号（双下划线分隔防与 overload `_` mangling 冲突，suffix 复用 `codegen_type_ref_to_suffix` 权威）；② C2 `codegen_collect_mono_combos_for_generic_func` 扫 arena 收集所有 (func, type-args) 组合并去重（≤16 combos × ≤8 params）；③ C3 `codegen_emit_call_func_name` call site 按 type args 构造 mangled 符号（与 emit 侧 combo 对齐，失败 fallback 裸 link_name）；④ 重构 `codegen_try_emit_generic_identity_mono` 循环 per-combo emit mangled 实例。另修：pn_len 重构回归（`func_name_len_at` → `param_name_len_at`）+ seed C 三新函数 null-check 括号不匹配（改 split-if 早返回） |
| 构建级别 | **L2**（codegen_x.o + g05） |
| 钉盘 | 仍 **`70ca1089b`** |
| 探针 | **mac** w444_multi_mono_ok（`copy<A>` + `copy<i32>` 双实例 run=42）· w444_three_mono_ok（3 combo run=42）· w444_single_regression（单参 identity=42）· w444_multi_arg_regression（多参 identity=42）· wave443 regression（multi_arg/three_arg/single_ret/bound_ok/compare/no_bound=42）· bound_violation/multi_bound_viol 红 T001 · 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（618s） |
| Ubuntu | 待跑（日终批跑） |
| soft | dyn Trait；generic 调用无 turbofish `<T>` 类型推断（XP003，`ast_Func` 不存泛型名需 L4 ABI 扩展）；generic 函数完整单态化（body 仍硬编返回 param0，trait 方法体 `x.clone()` 不 emit → BLD001，C6 待调研）；Ubuntu 失败路径次级 139 |

## 25 日承接（wave443 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`f0c7449e3`** |
| 债 | **多参数 identity mono 不 emit**（`copy_both<T,U>` 因 `codegen_try_emit_generic_identity_mono` 门控 `num_params != 1` 返回 0 → 调用方 `continue` 跳过 → 永不 emit → 调用点 BLD001 undeclared） |
| 根因 | identity mono 早期实现仅支持单参 identity shape（`fn(x: T): T { return x; }`），多类型参数泛型函数（`copy_both<T,U>`）被硬门控跳过 → wave442 修复 multi-arg bound 后 BLD001 主因暴露 |
| 根修 | codegen.x 单源（同步 seed `codegen_gen.linux.x86_64.c`）：① `codegen_try_emit_generic_identity_mono` 放宽门控至 N 参数（`num_params <= 0 \|\| num_params > 8` 才返回 0，支持 2..8 个参数）；② `codegen_find_mono_type_for_generic_func` 补 `arg_idx` 参数按位置取 call site arg 类型 — `arg_idx == 0` 时保留 `e.resolved_type_ref` 快捷路径（identity 形状下 ret==param0）；`arg_idx > 0` 时**不**使用 resolved_type_ref 快捷路径（返回类型仅对 param0 有效），改走 `pipeline_expr_call_arg_ref(arena, ei, arg_idx)` + `pipeline_expr_resolved_type_ref(arena, a0)` 取对应位置 arg 类型；③ 循环 emit param 1..N-1 的 mono 类型与名称（param0 已由调用点 emit） |
| 构建级别 | **L2**（parser_asm_thin_glue.o + g05） |
| 钉盘 | 仍 **`70ca1089b`** |
| 探针 | **mac** w443_multi_arg_ok（`copy_both<A,i32>` run=42）· w443_three_arg_ok（三参数泛型 run=42）· w443_bound_violation_regression（wave439 bound 检查不回归 红 T001）· w443_single_ret_regression（单参 identity 不回归=42）· w443_multi_bound_viol（wave442 multi-arg bound 不回归 红 T001）· w443_bound_ok/compare/no_bound（wave439/440 不回归=42）· 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（626s） |
| Ubuntu | 待跑（日终批跑） |
| soft | dyn Trait；generic 调用无 turbofish `<T>` 类型推断（XP003，`ast_Func` 不存泛型名需 L4 ABI 扩展）；generic 函数完整单态化（body 仍硬编返回 param0，trait 方法体 `x.clone()` 不 emit）；Ubuntu 失败路径次级 139 |

## 25 日承接（wave442 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`f4c4b7327`** |
| 债 | **multi-arg generic bound 假绿**（`copy_both<T:Clone,U:Default>` 调用 `copy_both<A,B>` 时 B 违反 `U:Default` 未报 T001 落 BLD001） |
| 根因 | wave439 实现的 `xlang_generic_bound_check_c`（skip_tl_slice.inc）scan 阶段只捕首个 type arg 入单槽 `g_call_typearg[ci]`，check 阶段对每条 bound 都用首 type arg 比对 → 多 type arg 泛型调用只校验 T0，T1+ 违反 bound 不报 → 假绿落 BLD001 undeclared（多类型参数泛型函数 codegen 限制不 emit 的次级症状掩盖了主因） |
| 根修 | G.7 单源 thin-glue TU：① scan 阶段扩展捕全 type args — `g_call_typeargs[ci][pos][64]`（最多 4 args，宏 `XLANG_GENERIC_CALL_MAX_ARGS`）+ `g_call_typearg_lens[ci][pos]` + `g_call_nargs[ci]`（计数），angle list 内遇 `,` 切下一 pos 继续捕 IDENT；② scan 阶段同时记每条 bound 的位置 `g_fn_bound_pos[bi]`（bound 声明时第 N 个形参对应 type arg pos N）；③ check 阶段按 `pos` 取对应 type arg 比对 trait（`pos = g_fn_bound_pos[bi]`，越界 skip），而非全用首 arg |
| 构建级别 | **L2**（parser_asm_thin_glue.o + g05） |
| 钉盘 | 仍 **`70ca1089b`** |
| 探针 | **mac** w441_multi_bound_viol（`copy_both<A,B>` B 未 impl Default）红 T001 :10:23 · w442_three_arg_viol（三 type param 第二个违反 bound）红 T001 · wave438-441 regression（单参泛型 bound_violation 红/bound_ok=42、compare `a<b` 不误捕=42、no_bound=42）无回归 · 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（600s） |
| Ubuntu | 待跑（日终批跑） |
| soft | dyn Trait；generic 调用无 turbofish `<T>` 类型推断（XP003，`ast_Func` 不存泛型名需 L4 ABI 扩展）；generic 函数完整单态化（多类型参数 `foo<T,U>` 与 trait 方法体 `x.clone()` 不 emit → BLD001）；Ubuntu 失败路径次级 139 |

## 25 日承接（wave441 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`fed3fc2b0`** |
| 债 | **两个 struct impl 同一 trait 假红**（method clone self type mismatch） |
| 根因 | `xlang_trait_check_impls_complete_c`（skip_tl_slice.inc）+ `parser_asm_seed_check_impl_missing_methods_c`（seed_parse_into_buf_slice.inc）方法查找 `found_fi` 只按方法名全 module 扫，找到第一个同名方法就 break，未限定到当前 impl 块 → 两个 struct（A/B）impl 同一 trait（Clone）时，检查 B 的 impl 找到 A 的 clone 方法（self: A），wave428 self 检查 param0（A）≠ for（B）→ 假红 "self type mismatch" |
| 根修 | G.7 双轨单源：① 提取 `xlang_{skip,seed}_impl_self_matches_for_c` helper（self 类型匹配 for 类型，复用 wave428 检查逻辑消除重复）— `for_k<0` 或 `got0<0`（untyped self）时返回 1（保旧行为）；PTR 分支 walk elem kind/name；NAMED 分支 memcmp name；② 查找循环优先选 self 类型匹配 for 的方法（两 impl 同 trait 时按 self 区分），无匹配时 fallback 到首个同名方法（静态方法 w/o self 的 param0≠for-type → helper 返 0 → fallback 保旧行为，不回归 signature-only 检查）；③ wave428 self 检查改调 helper 消除重复逻辑 |
| 构建级别 | **L2**（parser_asm_thin_glue.o + g05） |
| 钉盘 | 仍 **`70ca1089b`** |
| 探针 | **mac** two_impl（A/B impl Clone）run=42 · multi_impl_call（3 struct + 方法调用）run=60 · p2_bound_ok（copy<A>(a) A impls Clone）=42 · p3_bound_viol（copy<B>(b) B 未 impl Clone）红 T001 :9:23 · wave432-438 regression mm 红（parameter/return type mismatch）/ok=42 · r_ok2（两 struct self 方法调用）run=142 · 矩阵 rv=42/hello · bstrict 129 OK（673s） |
| Ubuntu | 待跑（日终批跑） |
| soft | dyn Trait；generic 调用无 turbofish `<T>` 类型推断（XP003，根源障碍 `ast_Func` 不存泛型名需 L4 ABI 扩展）；Ubuntu 失败路径次级 139 |

## 25 日承接（wave440 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`016d42ca1`** |
| 债 | **generic bound diag 行号偏 -1**（wave439 引入的 soft 债：报错指向上一行而非实际调用点） |
| 根因 | `xlang_generic_bound_scan_c` 用 `memset(&lex, 0, sizeof(lex))` 初始化 lexer 致 `lex.line=0`/`lex.col=0`；但 lexer 约定（`lexer_init` in lexer.x:1392）为 `line=1`/`col=1` — `advance_one` 仅 newline 时 `line++` 且不 reset（line 累积），newline 时 `col=1` reset（col 仅 line 1 受影响）→ 所有报错行号差一（`copy<B>(b)` 在 line 23，报错指向 line 22） |
| 根修 | `memset` 后补 `lex.line = 1; lex.col = 1;`（与 `lexer_init` 一致）；详细注释说明为何 line/col 不能用 0 |
| 构建级别 | **L2**（parser_asm_thin_glue.o + g05） |
| 钉盘 | 仍 **`70ca1089b`** |
| 探针 | **mac** bound_violation T001 现 `:23:23`（caret 指 `(` in `copy<B>(b)`；原 `:22:23` 指上一行 `b.w = 99;`）· bound_minimal/bound_ok=42 · no_bound/compare=42（无误捕）· wave438 regression ret_arr_elem_mm 红/cast_only 绿=42 · wave439 bound check 全探针过 · 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（614s） |
| Ubuntu | 待跑（日终批跑） |
| soft | dyn Trait；generic 调用无 turbofish `<T>` 类型推断（XP003）；Ubuntu 失败路径次级 139 |

## 25 日承接（wave439 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`d0a3830cc`** |
| 债 | **generic trait bound 假绿**（`copy<B>(b)` 其中 B 未 impl bound trait 仍过编 run=42） |
| 根因 | 编译器未验证泛型调用点 `foo<Type>(...)` 的类型实参是否实现了函数声明的边界 trait（`function foo<T: Trait>`）→ B 未 impl Clone 仍过编 run=42 假绿 |
| 根修 | G.7 单源 thin-glue TU（skip_tl_slice.inc 内 scan+check）：① `xlang_generic_bound_stash_source_c` 顶层 skip 函数（struct/enum/trait/impl）暂存全文件源码切片指针（trait check 仅收 *Module 无法 token 化源码）；② `xlang_generic_bound_scan_c` token 流扫两类模式 — 边界声明 `function IDENT < IDENT : IDENT >`（存 fn_name→trait_name 对；进 LT 分支先 `lex=r.next_lex` 前进过 `<` 防 angle_depth 膨胀）与调用点 `IDENT < IDENT > (`（存 callee→typearg+line+col；仅 `>` 后跟 `(` 才记，避免 `a < b` 比较误捕；嵌套泛型 `Vec<i32>`（angle_depth>1）soft-skip）；③ `xlang_generic_bound_check_c` 对每个有 bound 的调用点查 impl registry `g_xlang_skip_impl_*` 验证 `impl Trait for Type` 存在 → `generic bound not satisfied: %s does not impl %.*s`（T001）；④ `xlang_trait_check_impls_complete_c` 末尾调 scan+check 保证产品路径必跑 |
| 构建级别 | **L2**（parser_asm_thin_glue.o + g05） |
| 钉盘 | 仍 **`70ca1089b`** |
| 探针 | **mac** bound_violation（B 未 impl Clone）红（T001 generic bound not satisfied）· bound_minimal/bound_ok（A impls Clone）=42（parse OK + run=42）· no_bound（非泛型 fn）=42 · compare `a < b` 不误捕（=42）· 嵌套泛型 soft-skip（=42）· wave438 regression ret_arr_elem_mm 红/cast_only 绿=42 · 矩阵 rv=42/option/hello(run=0, Hello World\n)/stdlib-import · bstrict 129 OK（609s） |
| Ubuntu | 待跑（日终批跑） |
| soft | dyn Trait；bound diag 位置偏（报错 line/col 差一）；generic 调用无 turbofish `<T>` 类型推断（XP003）；Ubuntu 失败路径次级 139 |

## 25 日承接（wave438 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`1807ddceb`** |
| 债 | **trait 方法返回值 `*[]T`/`*[N]T`/`*[N][M]T` 类型不匹配假绿 + `as *[N]T` cast parse fail 假红**（双轨；参数侧 wave434-437 对称移植到返回值侧） |
| 根因 | ① 返回值侧状态机未扩展（wave434-437 仅扩展参数侧 `param_elem_*`，返回值侧 `ret_elem_*` 仍 soft-skip）→ trait ret `*[3]i32` vs impl `*[3]u8` 元素类型错/维度错仍过编 run=42；② `parser_asm_as_suffix_slice.inc` STAR 分支仅支持 `as *T`（简单指针，调 `parser_asm_alloc_pointee_type_ref_from_tok_c`），见 `as *[N]T` 的 `[` 返 0 → 整函数解析失败 → impl 方法被丢弃 → trait checker 报「missing method f」（对任意含 `as *[N]T` 转换的 impl 假红，含同形 c1） |
| 根修 | G.7 双轨单源：① skip_tl/seed 返回值侧对称扩展 `ret_elem_slice_need_rb`/`ret_elem_prefix_arr_need_rb`/`ret_elem_elem_pending`/`ret_elem_arr_need_rb`/`ret_elem_arr_need_size`/`ret_elem_suffix_pending`/`ret_elem_prefix_arr_more`（与参数侧 wave434-437 `param_elem_*` 同构）；`ret_elem_elem_pending` 见 `[` 转 `ret_elem_prefix_arr_more=1` 收集多维 dims；提交 dims 后 reset `ret_elem_dim_n=0` 防 postfix lift 误覆盖；check `gek==ek && eek==SLICE/ARRAY` 分支 walk pipeline PTR→elem(SLICE/ARRAY)→dims→leaf 递归比对 elem_elem（base T）→ `return type mismatch`；② `parser_asm_as_suffix_slice.inc` STAR 分支委托 `parser_asm_parse_type_ref_for_arena_into_slice_c`（复用 type_ref_slice.inc 完整类型解析，支持 `as *[N]T`/`as *[]T`/`as *[N][M]T`）；`pthin_expr_as_suffix.from_x.c` 补 extern 声明；移除手工 `tptr`/`elem_ref` 局部 |
| 构建级别 | **L2**（parser_asm_thin_glue.o + g05） |
| 钉盘 | 仍 **`70ca1089b`** |
| 探针 | **mac** ret_arr_elem_mm（`*[3]i32` vs `*[3]u8` elem 错）/ret_arr_sz_mm（`*[3]i32` vs `*[5]i32` size 错）红（T001 return type mismatch）· ret_slice_elem_mm（`*[]i32` vs `*[]u8` elem 错）红 · ret_arr2_elem_mm（`*[2][3]i32` vs `*[2][3]u8` elem 错）/ret_arr2_sz1_mm/ret_arr2_sz2_mm 红 · ret_arr_ok2/ret_slice_ok/ret_arr2_ok=42（parse OK + run=42）· cast_only（`as *[3]i32` 解析成功，方法不再 missing）绿 · simple `as *i32`/`*i32` ret 无回归 · wave437/436/435 regression mm 红/c1 绿=42 · 矩阵 rv=42/option/hello/import · bstrict 129 OK（601s） |
| Ubuntu | 待跑（日终批跑） |
| soft | dyn Trait；generic trait bounds；Ubuntu 失败路径次级 139 |

## 25 日承接（wave437 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`6e36d441f`** |
| 债 | **trait 方法形参 `*[N][M]T` 多维 PTR-to-ARRAY 假绿**（wave436 soft-skip -1，registry check 未实现） |
| 根因 | wave436 为 `*[N][M]T`（PTR to multi-dim ARRAY）走 soft-skip：`param_elem_elem_pending` 见 `[` 即设 `elem_kinds=-1`（保 wave434 行为）→ check 跳过 → 同形 impl 假绿、元素类型错/任一维度错仍过编 run=42；wave436 已为 `*[N]T` 单维实现真 check，但多维仍 soft-skip |
| 根修 | G.7 双轨单源：① skip_tl/seed `param_elem_elem_pending` 见 `[` 不再 soft-skip -1，转 `param_elem_prefix_arr_more=1` + `param_elem_arr_need_size=1` 收集下一维 INT N，`param_elem_arr_need_rb` 见 `]` 后回 `param_elem_elem_pending` 捕 base T；base T 捕获时提交累积 dims 到 `method_param_elem_array_ndims/dims`；**关键 bugfix**：提交 dims 后必须 reset `param_elem_dim_n=0`，否则 wave434 `*T[N]` postfix lift 在 `,`/`)` 时因 `param_elem_dim_n>0` 误触发，把正确的 PTR-of-ARRAY 覆盖为 ARRAY-of-PTR（导致 wave436 c1 单维 `*[3]i32` 假红 — wave437 初版引入的回归，根因是 dims 提交点从 `param_elem_prefix_arr_need_rb` 延迟到 `param_elem_elem_pending` 但未同步搬移 reset）；② check 复用 wave436 `gek==ek && eek==ARRAY` 分支 walk pipeline PTR→elem(ARRAY)→dims[N][M]→leaf 比对 elem_elem（base T）→ `parameter type mismatch`（多维 dims 循环 wave436 已支持，无需改动 check） |
| 构建级别 | **L2**（parser_asm_thin_glue.o + g05） |
| 钉盘 | 仍 **`70ca1089b`** |
| 探针 | **mac** m1_ptr_arr2_elem_mm（`*[2][3]i32` vs `*[2][3]u8` elem 错）/m2_ptr_arr2_sz1_mm（`*[2][3]i32` vs `*[5][3]i32` 第一维 size 错）/m3_ptr_arr2_sz2_mm（`*[2][3]i32` vs `*[2][5]i32` 第二维 size 错）红（T001 parameter type mismatch）· c1_ptr_arr2_ok=42（parse OK + run=42）· wave436 regression mm 红/c1 绿=42 · wave435 regression mm 红/c1 绿=42/edge 绿 · wave434 regression mm 红/c1 绿=42 · wave433 regression mm 红/correct 绿=42 · 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（600s） |
| Ubuntu | 待跑（日终批跑） |
| soft | dyn Trait；generic trait bounds；Ubuntu 失败路径次级 139 |

## 25 日承接（wave436 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`5f3486baf`** |
| 债 | **trait 方法形参 `*[N]T` PTR-to-ARRAY 假绿**（wave435 soft-skip -1，registry check 未实现） |
| 根因 | wave435 为 `*[N]T`（PTR to ARRAY）走 soft-skip：registry `param_elem_slice_need_rb` 见 INT N 即设 `elem_kinds=-1`（保 wave434 行为）→ check 跳过 → 同形 impl 假绿、元素类型错/数组维度错仍过编 run=42；wave435 已为 pipeline `*[` 递归解析铺路（`parser_asm_type_ref_slice.inc` STAR 后递归），registry 侧却未跟上 |
| 根修 | G.7 双轨单源：① skip_tl/seed `param_elem_slice_need_rb` 见 INT N 时改设 `elem_kinds=ARRAY`（不再 -1）+ 捕获 N 入 `param_elem_dims`；新增 `param_elem_prefix_arr_need_rb` 状态等 `]` 闭合 → 存 `method_param_elem_array_ndims/dims` → 转 `param_elem_elem_pending`（复用 wave435 slice 路径）捕获 base T；`param_elem_elem_pending` 新增 `[` 分支软跳过多维 `*[N][M]T`；② check `gek==ek && eek==ARRAY` 分支 walk pipeline PTR→elem(ARRAY)→dims[N]→leaf 比对 elem_elem（base T）→ `parameter type mismatch`（与 wave433 ARRAY walk 对称，但作用于 elem 层） |
| 构建级别 | **L2**（parser_asm_thin_glue.o + g05） |
| 钉盘 | 仍 **`70ca1089b`** |
| 探针 | **mac** m1_ptr_arr_elem_mm（`*[3]i32` vs `*[3]u8` elem 错）/m2_ptr_arr_sz_mm（`*[3]i32` vs `*[5]i32` size 错）/m3_ptr_arr_named_mm（`*[2]Point` vs `*[2]Other` named 错）红（T001 parameter type mismatch）· c1_ptr_arr_ok=42（parse OK + run=42）· wave435 regression mm 红/c1 绿=42 · wave434 regression mm 红/c1 绿=42 · wave433 regression mm 红/correct 绿=42 · wave435 edge `*[N]T` trait decl 绿（从 soft-skip 升为真 check 仍不假红）· 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（667s） |
| Ubuntu | 待跑（日终批跑） |
| soft | dyn Trait；generic trait bounds；`*[N][M]T` 多维 PTR-to-ARRAY（registry soft-skip -1）；Ubuntu 失败路径次级 139 |

## 25 日承接（wave435 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`de6dad1a5`** |
| 债 | **trait 方法形参 `*[]T` pointer-to-slice 假绿 + pipeline `*[` parse fail 假红**（双轨） |
| 根因 | ① trait registry（skip_tl/seed）wave434 对 `*[]T` soft-skip（elem=-1，check 跳过）→ 同形 impl 假绿、元素类型错仍过编；② pipeline parser（`parser_asm_type_ref_slice.inc` `parse_type_ref_impl_c`）STAR 分支只支持标量 pointee（`alloc_pointee_type_ref_from_tok_c`），见 `*[` 返 0 → 整函数解析失败 → impl 方法未 hoist → trait checker 报「missing method f」（对任意带 `*[]T` 形参的 impl 假红，含同形 c1） |
| 根修 | G.7 双轨单源：① pipeline `parser_asm_type_ref_slice.inc` STAR 后若下一 token 是 `[`，递归调 `parse_type_ref_impl_c` 解析内部 slice/array，再包 PTR；标量路径不变；C 风格后缀 `*T[N]`=ARRAY of PTR 保留；② skip_tl/seed 新增 `param_elem_slice_need_rb` 状态区分 `*[]T`(SLICE, 捕获 base T)/`*[N]T`(INT N, soft-skip -1 保 wave434 行为) + `param_elem_elem_pending` 状态捕获 slice base T（NAMED name / builtin）；check `gek==ek && eek==SLICE` 分支 walk pipeline SLICE leaf → elem → elem_elem（slice base T）比对 → `parameter type mismatch` |
| 构建级别 | **L2**（parser_asm_thin_glue.o + g05） |
| 钉盘 | 仍 **`70ca1089b`** |
| 探针 | **mac** m1_ptr_slice_elem_mm/m2_ptr_slice_named_mm/m3_ptr_slice_sz_mm 红（T001 parameter type mismatch）· c1_ptr_slice_ok=42（parse OK + run=42）· diag 红 · pipe_ok 绿（pipeline 支持 `*[]T`）· edge `*[N]T` trait decl 绿（soft-skip 不假红）· wave434 regression mm 红/correct 绿（m3 改善：从副作用 missing method f → 正确 T001）· wave433 regression mm 红/correct 绿 · 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（586s） |
| Ubuntu | 待跑（日终批跑） |
| soft | dyn Trait；generic trait bounds；`*[N]T` PTR-to-ARRAY（registry check 未实现，pipeline 已绿）；Ubuntu 失败路径次级 139 |

## 25 日承接（wave434 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`5f57abb98`** |
| 债 | **trait 方法形参嵌套 `*T[N]` postfix wrap 假绿**（指针元素为数组） |
| 根因 | wave433 仅捕获参数 ARRAY 后缀的顶层维度，未处理指针元素为数组 → `*T[N]` 解析为 PTR（见 `*` 即收 elem），但类型语法 `[]` 优先级高于 `*`，应解析为 ARRAY of PTR；impl `i32[3]`（ARRAY of PTR）vs trait `*i32[2]`（registry PTR）kind 不等 → 同形假红；impl scalar `*i32` 与 trait PTR 顶层等 → 假绿 |
| 根修 | G.7：skip_tl/seed 双生 `method_param_elem_elem_kinds` + `method_param_elem_array_ndims` + `method_param_elem_array_dims`；解析状态机扩 `param_elem_suffix_pending` + `param_elem_arr_need_size` + `param_elem_arr_need_rb`；finalize 将 `*T[N]` 提升为 ARRAY of PTR（kind=ARRAY, elem=PTR, elem_elem=T, dims 复用顶层字段）；check `gek==ek && ek==PTR` 分支 walk pipeline ARRAY leaf（PTR）→ elem → elem_elem 比对 → `parameter type mismatch` |
| 构建级别 | **L2**（parser_asm_thin_glue.o + g05） |
| 钉盘 | 仍 **`70ca1089b`** |
| 探针 | **mac** m1_ptr_arr_sz_mm/m2_ptr_arr_scalar_mm 红（T001 parameter type mismatch）· m3_ptr_slice_elem_mm 红（副作用 missing method f，`*[]T` 仍 soft 非真比对）· c1_ptr_arr_ok=42 · wave433 regression mm 仍红/correct 仍绿 · 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（604s 附赠非 L2 必需） |
| Ubuntu | 待跑（日终批跑） |
| soft | dyn Trait；generic bounds；`*[]T`（指针指向切片，elem 未捕获）；Ubuntu 失败路径次级 139 |

## 25 日承接（wave433 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`5fca9ad38`** |
| 债 | **trait 方法形参后缀 `T[N]`/`T[N][M]` 假红/假绿** |
| 根因 | wave432 仅捕获 NAMED/PTR/SLICE 形参 elem kind，未处理 ARRAY 后缀 → trait `x: i32[2]` 注册为 TYPE_I32（解析器见 IDENT 即停）；impl `i32[2]`/`i32[3]` 在 pipeline 为 TYPE_ARRAY → 比对 kind 不等 → 同形假红；impl scalar `i32` 与 trait I32 顶层等 → 假绿 |
| 根修 | G.7：skip_tl/seed 双生 `method_param_array_ndims` + `method_param_array_dims`（与 wave431 ret 对称）；解析状态机扩 `param_suffix_pending` + `param_arr_need_size` + `param_arr_need_rb`；`T[]` → TYPE_SLICE 旧路径保留；finalize 在 `,`/`)`/非 `[` token 时若 dims>0 则改 kind 为 ARRAY + 写 ndims/dims；check `got_pk==ek && ek==ARRAY` 分支递归 walk TYPE_ARRAY dims[0]=outer… + leaf kind/name → `parameter type mismatch` |
| 构建级别 | **L2**（parser_asm_thin_glue.o + g05） |
| 钉盘 | 仍 **`70ca1089b`** |
| 探针 | **mac** m1_suffix_mm/m3_multidim_mm/m5_suffix_scalar_mm/m6_suffix_elem_mm 红（T001）· m2_suffix_ok/m4_multidim_ok=0 · p0_baseline/p1_scalar/p2_trait_only=0 · p3_impl_only 红（trait scalar vs impl ARRAY）· wave432 regression mm 仍红/correct 仍绿 · 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（附赠非 L2 必需） |
| Ubuntu | 待跑（日终批跑） |
| soft | dyn Trait；generic bounds；nested `*[]T`/`*T[N]` postfix wrap；Ubuntu 失败路径次级 139 |

## 25 日承接（wave432 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`67f89dd45`** |
| 债 | **trait 方法形参非 builtin 类型假绿**（NAMED / `*T` / `[]T` 元素类型不匹配） |
| 根因 | wave426 参数检查仅比顶层 TypeKind ord → Point vs Other、`*i32` vs `*u8`、`[]i32` vs `[]u8` 落 kind=-1 跳过 → impl 形参类型错仍过编 run=42 |
| 根修 | G.7：skip_tl/seed 双生 `method_param_names` + `method_param_name_lens` + `method_param_elem_kinds`；解析状态机扩 `param_elem_pending`（STAR）/ `param_slice_need_rb`（LBRACKET）；check 在顶层 kind 之外补 NAMED name memcmp + PTR/SLICE elem-kind + elem-name memcmp → `parameter type mismatch` |
| 构建级别 | **L2**（parser_asm_thin_glue.o + g05） |
| 钉盘 | 仍 **`70ca1089b`** |
| 探针 | **mac** mm_named/mm_ptr/mm_slice 红（T001）· correct noparam/builtin/named/ptr/slice=42 · main=42 · run-trait · 矩阵 rv/option/hello · bstrict 129 OK |
| Ubuntu | hybrid pthin 自定义探针 segfault（ld -r 大 BSS 非逻辑 bug）· monothin mm 诊断正确 · bstrict 无回归 · PTR/SLICE elem kind 检测为 pre-existing（`pipeline_module_func_param_type_ref_at` 对 builtin 返 0，非本波引入） |
| soft | param suffix `T[N]` / multi-dim `T[N][M]`（与 wave430/431 ret 对称，延后）；dyn Trait；generic bounds；nested `*[]T`/`*T[N]` postfix；Ubuntu 失败路径次级 139 |

## 25 日 tip L4 升钉（`70ca1089b` · 含 wave431）

| 项 | 状态 |
|----|------|
| SHA | **`70ca1089b`**（docs tip；产品 **`15a09c5c5`** wave431） |
| 构建级别 | **L4 真冷**（全擦 compiler/std/core `.o` + 重链 xlang/xlang_asm/xlang-c） |
| remaining_o | **0** 起编（双端） |
| 产品矩阵 | ✅ 双端 rv_run=42 · opt/si/hello_build=0 · hello_run=0 · `Hello World\n` |
| mac 全量 bstrict | ✅ **OK (129)** · `/tmp/mac_true_cold_70ca1089b.log` · `/tmp/mac_true_bstrict_70ca1089b.log` |
| Ubuntu 全量 bstrict | ✅ **OK (129)** · `/tmp/ubuntu_true_cold_70ca1089b.log` · `/tmp/ubuntu_true_bstrict_70ca1089b.log` |
| 前钉 | `605e7efa4`（wave383） |
| soft 仍挂 | dyn Trait；generic trait bounds；nested `*[]T` / `*T[N]` postfix；Ubuntu 失败路径次级 diag 139 |

```text
wave443 已闭 · 下一 wave444 Cap residual（日常 L2）
  候选：① dyn Trait（vtable + fat pointer 设计，多波主特性）  ② generic 调用无 turbofish 类型推断（XP003，根源障碍 ast_Func 不存泛型名需 L4 ABI 扩展）  ③ 转 R2 真迁（产品矩阵已绿）  ④ generic 函数完整单态化（identity-only mono 债，多类型参数 foo<T,U> 已 emit（wave443）但 body 仍硬编返回 param0；trait 方法体 x.clone() 不 emit → BLD001）
  leave-off：dyn Trait；generic 调用无 turbofish 类型推断（XP003）；generic 函数完整单态化（body 解析 + trait 方法体 + 符号 mangling）；Ubuntu 失败路径次级 diag 139
  钉盘 70ca1089b
```

## 25 日承接（wave431 · 日常 L2 · 已闭 · 已进钉盘）

| 项 | 状态 |
|----|------|
| SHA | **`15a09c5c5`** |
| 债 | **trait multi-dim `T[N][M]` ret 假红/假绿**（registry 只存一维） |
| 根因 | wave430 后缀只吃一层 `[N]` → trait `: i32[2][3]` 成 ARRAY size=2 elem=i32；impl 同形 elem=ARRAY → ok 假红；impl `: i32[2]` 与错误 registry 假绿 |
| 根修 | G.7：skip_tl/seed 收集 suffix dims[0]=outer… + `method_ret_array_ndims`/`dims`；check 递归 TYPE_ARRAY size 再 leaf kind/name → `method %s return type mismatch` |
| 构建级别 | **L2** 合入；**L4 升钉**见上节 **`70ca1089b`** |
| 钉盘 | **`70ca1089b`**（升自 `605e7efa4`） |
| 探针 | **双端** md_ok=42 · md_mm/md_sz/md_inner_sz 红 · md1/arr/suffix_slice/named_arr 绿 · self_mm 红 · main=42 · run-trait · 矩阵；Ubuntu mm 次级 139（wave425 起已有） |
| soft | dyn Trait；generic trait bounds；nested `*[]T` / `*T[N]` postfix wrap；Ubuntu 失败路径次级 diag 139 |

## 25 日承接（wave430 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`338688aae`** |
| 债 | **trait 后缀 `T[N]`/`T[]` ret 假红/假绿**（registry 只吃 base kind） |
| 根因 | wave427 登记 ret 时 `: i32[2]` 见 builtin 即停 → kind=I32；impl `i32[2]` 为 TYPE_ARRAY → ok 假红；impl 标量 `i32` 与错误 registry 假绿 |
| 根修 | G.7：skip_tl/seed 后缀 `T[N]`/`T[]` 捕获 + `method_ret_array_sizes`；check TYPE_ARRAY size+elem → `method %s return type mismatch` |
| 构建级别 | **L2**（parser_asm_thin_glue.o + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | **双端** arr_ok/slice_ok/suffix_slice_ok=41 · named_arr_ok=42 · arr_scalar/size/elem_mm + suffix_slice_mm 红 · self_mm 红 · main=42 · run-trait · 矩阵 rv/option/hello；Ubuntu mm 次级 139（wave425 起已有） |
| soft | dyn Trait；generic trait bounds；nested `*[]T` / multi-dim `T[N][M]` ret（→ wave431 闭）；Ubuntu 失败路径次级 diag 139 |

## 25 日承接（wave429 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`6f696781c`** |
| 债 | **trait typed self vs for-type 假绿**（wave428 仅 ek0=-1 门控） |
| 根因 | wave428 只在 trait 写 untyped `self` 时比 for-type；trait 已注 `self: i32` 且 impl `for u64` 仍写 `self: i32` 时 param 循环只对 trait 签名绿 → 绑到 i32 接收者 run=42 |
| 根修 | G.7：skip_tl/seed 去掉 `ek0<0` 门控；凡 for-type 与 impl param0 均已知即硬比 → `method %s self type mismatch` |
| 构建级别 | **L2**（parser_x.o + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | **双端** typed_for_ok/untyped_ok/named_for_ok=42 · typed_for_mm/untyped_mm/named_for_mm 红（self type mismatch；Ubuntu mm 次级 139 同 wave425 起）· main=42 · run-trait · 矩阵 rv/option/hello |
| soft | dyn Trait；generic trait bounds；nested `*[]T`/`T[N]` ret（→ wave430 闭）；Ubuntu 失败路径次级 diag 139 |

## 25 日承接（wave428 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`512a9757c`** |
| 债 | **trait untyped self vs for-type 假绿**（param0 ek=-1 跳过） |
| 根因 | wave426 仅在 trait 写 `: Ty` 时存 param kind；`function m(self)` untyped → ek=-1 跳过 → `impl … for i32` 写 `self: u64` 过编 run≠42 |
| 根修 | G.7：skip_tl/seed 登记 for-type（kind / named / `*T`）；trait param0 untyped 时硬比 impl param0 → `method %s self type mismatch` |
| 构建级别 | **L2**（thin glue + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | **双端** self_ok/named_ok/named_ret=42 · self_mm/named_mm 红（诊断 self type mismatch；Ubuntu self_mm 次级 139 同 wave425 起）· main=42 · miss · run-trait · 矩阵 rv/option/hello |
| soft | dyn Trait；generic trait bounds；nested `*[]T`/`T[N]` ret；typed self 与 for-type 不一致（→ wave429 闭）；Ubuntu 失败路径次级 diag 139 |

## 25 日承接（wave427 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`23130f11a`** |
| 债 | **trait named/ptr/slice ret 假绿**（complex ret kind=-1 跳过） |
| 根因 | wave425 仅存 builtin ret TypeKind；IDENT/`*T`/`[]T` 落 kind=-1 → Point vs Other、[]i32 vs []u8 过编 run=42 |
| 根修 | G.7：skip_tl/seed `method_ret_names` + `method_ret_elem_kinds`；登记 TYPE_NAMED/PTR/SLICE；check `pipeline_type_named_name_into` + `pipeline_type_elem_ref_at` |
| 构建级别 | **L2**（thin glue + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | **双端** named/slice/ptr-ok=42 · named/slice mm 红（Ubuntu slice_mm 次级 139 同 wave425 起，诊断已出）· main=42 · arity/miss/unk · fs named · run-trait · 矩阵 rv/option/hello |
| soft | dyn Trait；generic trait bounds；untyped self vs for-type（→ wave428 闭）；nested `*[]T`/`T[N]` ret；Ubuntu 失败路径次级 diag 139 |

## 25 日承接（wave426 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`b725de103`** |
| 债 | **trait 方法形参 arity / typed param 假绿**（签名 params 未入库） |
| 根因 | wave421–425 仅登记方法名+ret；impl 多/少参或 typed 形参 TypeKind 错仍过编（arity 假绿 run=42） |
| 根修 | G.7：skip_tl/seed `method_param_counts` + `method_param_kinds`（builtin）；check `pipeline_module_func_num_params_at` / `param_type_ref_at` → `parameter count/type mismatch` |
| 构建级别 | **L2**（thin glue + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | **双端** ok=42 · arity/pty 诊断红（Ubuntu 次级 P001 后偶发 139，wave425 起已有，非本波引入）· rmm/miss 同 · unk=1 · run-trait · 矩阵 rv/option/hello |
| soft | dyn Trait；generic trait bounds；named/complex ret（→ wave427 闭）；untyped self vs for-type；Ubuntu 失败路径次级 diag 139 |

## 25 日承接（wave425 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`49aacb209`** |
| 债 | **trait return_mismatch 假绿**（签名 ret 未入库） |
| 根因 | wave421 仅登记方法名；impl `: bool` vs trait `: i32` 只认名不过 ret → 过编 |
| 根修 | G.7：skip_tl/seed `method_ret_kinds`（builtin TypeKind）；reset stash arena；check 比 `pipeline_module_func_return_type_at` → `method %s return type mismatch`；pin parse_into/_buf `reset(arena)` |
| 构建级别 | **L2**（thin glue + parser_x pin + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | **双端** ok double=42 · mm/i64/void 红 · missing/unknown 红 · run-trait · 矩阵 rv/option/hello |
| soft | trait bounds/dyn；named/complex ret（kind=-1） |

## 25 日承接（wave424 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`2fcbae844`** |
| 债 | **trait unknown_trait 假绿**（LANG-004 residual） |
| 根因 | ① `xlang_trait_check` 对 `ti<0` soft-skip ② freestanding `-o` 只调 `parser_parse_into_buf`，不经 with_init 后检查，typeck 亦可跳过 |
| 根修 | G.7：skip_tl/seed 对 unknown 硬失败 `impl for unknown trait`；`parse_into_finish_ok` 在 parse_into/_buf 成功出口统一跑 completeness |
| 构建级别 | **L2**（thin glue + parser_x pin + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | **mac** pure unknown fs+host 红 · trait+unknown 红 · main=42 · missing/no-impl 红 · inherent=42 · 矩阵 rv/option/hello · run-trait OK |
| soft | trait bounds/dyn；return_mismatch（→ wave425 闭） |

## 25 日承接（wave423 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`0ff65027b`** |
| 债 | **const 类型推断**（docs/06 允许 `const name = init`） |
| 根因 | ① body/top 解析强制 `:` → P010 ② import 收集器把 `const name =` 吞掉后丢绑定 ③ typeck 不从 init 戳记 type_ref |
| 根修 | G.7：parser 允许 const+`=`（type_ref=0）；`pipeline_block_set_const_type_ref` + body typeck stamp；`typeck_infer_top_level_const_types`；import 仅 `= import` 才消费 |
| 构建级别 | **L2**（thin glue + parser_x + typeck_x + standalone glue + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | **mac** body/top/typed/as/chain=42 · fs body=42 · let 仍 P010 · trait main=42 · 矩阵 rv/option/hello |
| soft | trait bounds/dyn |

## 25 日承接（wave422 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`a1b9ca3a2`** |
| 债 | **untyped-let 软 P001 / 缺 `_main`**（docs/06 禁推断） |
| 根因 | `parse_body_lets_into` / top-level let 遇缺 `:` 仅 `return false` → 整函数丢弃 → 软 P001「no functions」或 BLD001 |
| 根修 | G.7：`parser_report_untyped_binding_p010_c` + body/top 调用；**P010** 明确文案；pin `parser_gen` + `parser.x` + thin body_tl 同 commit |
| 构建级别 | **L2**（parser_asm_thin_glue + parser_x + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | **mac** body/top P010 · typed let=42 · empty S nest/ret/call=42 · trait main=42 · missing/no-impl · 矩阵 rv/option/hello |
| soft | const 类型推断（→ wave423 闭）；trait bounds/dyn |

## 25 日承接（wave421 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`98f9a49e5`** |
| 债 | **trait 缺方法假绿 + no-impl 诊断缺失**（LANG-004 residual） |
| 根因 | 产品 path 只 hoist impl 方法为 free fn，从不登记 trait 签名 → `impl_missing_method` 过编；method_call 失败仅 XT001 |
| 根修 | G.7：`skip_tl` trait 方法表 + impl-seen + `xlang_trait_check_impls_complete_c`（parse 后）；method_call `no impl for type`；探针 `method_no_impl` 非空 struct |
| 构建级别 | **L2**（parser_asm_thin_glue + pipeline_x + strict_minimal + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | **mac** missing method triple · no impl for type double · trait main=42 · run-lang-trait · 矩阵 rv/option/hello |
| soft | untyped-let（→ wave422 闭 P010）；trait bounds/dyn；empty-struct `S{}`（本波复核绿） |

## 25 日承接（wave420 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`123555700`** |
| 债 | **arm64 帧 [x29,#off] 字节 clamp→4095 导致 dual-GP data/length 别名** |
| 根因 | LDR/STR 编码把 **字节** off 硬帽 4095 再 /8；s.length@0x1000→0xff8 盖 s.data（escape n≥1017 SIGSEGV；local n=2048 mid 错） |
| 根修 | G.7：`backend_arm64_enc_c` scaled imm12=off/8（≤32760）+ multi-chunk ADD LEA/add_imm；大 off 用 x16 scratch；`arm64_enc.x` lea 孪 |
| 构建级别 | **L2**（backend_arm64_enc_c.o + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | **mac** single/dual n=1017..1024=42 · local 1024/2048=42 · disasm data@0xff8 length@0x1000 · 矩阵 rv/option/hello |
| soft | untyped-let（docs 禁）；trait 深债 |

## 25 日承接（wave419 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`c8b15e988`** |
| 债 | **host fixed→slice escape 硬帽 256→1024**（durable `__xlang_esc[N]`） |
| 根因 | host `emit_return` 仅 `arr_sz<=256` 走 escape；n>256 裸 `return s` 栈悬垂；双 call 靠 call-arg deep-copy 碰运气 |
| 根修 | G.7：codegen.x + seed 同 gate **1024**（≡ `GLUE_ARRAY_LIT_MAX_ELEMS` / deep-copy max_n） |
| 构建级别 | **L2**（codegen_x + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | **-backend c** use/dual n=300..1024=42 · esc 发射 · dual lit/esc4 · walk=12 · 矩阵 rv/option/hello · fs dual≤800=42 |
| soft | untyped-let；trait；fs dual n=1024（→ wave420 闭） |

## 25 日承接（wave418 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`27fff1d7b`** |
| 债 | **TYPE_SLICE reent deep-copy：let 帧缓冲真递归 + max_n 512→1024 + arm64 fixed→slice escape** |
| 根因 | ① COMMON 单缓冲同 site 递归 last-wins（walk=8≠36）② max_n=512 截断 ③ arm64 escape `ta!=0`/`jle` 拒编，栈 view 悬垂 |
| 根修 | G.7：let use_frame=1 + frame_size 预留；call-arg 仍 COMMON；host `__xlang_sd/ld[1024]`+length=sn；escape arm64 jge+ADRP |
| 构建级别 | **L2**（glue standalone + codegen_x + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | walk=36 · lit dual=19 · len=8 · dual-only≤600 · single≤600 · 矩阵 rv/option/hello |
| soft | untyped-let；trait；host dual fixed-escape n≥512（→ wave419）；asm n=1024 帧压 |

## 25 日承接（wave417 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`53be4f60e`**（`f8bdf67a5` + x86 residual） |
| 债 | **fs `T[N]` 形参 by-value / `return T[N]` / arm64 动态 INDEX 步长** |
| 根因 | ① `agg_byte_size` 把 T[N] 当整 blob → home/INDEX 错；② arm64 scale4/8 imm6=6/7；③ return 栈悬垂 + sret；④ mid(a) dual-GP 误解 |
| 根修 | G.7：formal home=8 · use_lea/load · durable ARRAY_LIT→TYPE_ARRAY · return size=8 · scale imm6=2/3 |
| 构建级别 | **L2**（glue + `backend_arm64_enc_c` + g05） |
| 钉盘 | 仍 **`605e7efa4`** |
| 探针 | **双端** pass n=2..16 · fwd=100 · ret_only/ret_pass=36 · plit=10 · localw · i64=100 · reent=15 · 矩阵 rv/option/hello |
| soft | untyped-let（docs 禁）；trait 深债；deep-copy max_n=512（>512 仍 soft） |

```text
（wave418 已闭 · 见上文）
```

## 25 日承接（wave416 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`4613c30c9`** |
| 债 | **fs i64/u64 ARRAY_LIT 步长 4 写 / 8 读**（host-C 已绿；sum/idx 错） |
| 根因 | `pipeline_asm_array_lit_elem_byte_sz_c` 仅 F64→8；U64/I64/USIZE/ISIZE 落默认 4；INDEX 用 glue_index esz=8 |
| 根修 | G.7：ARRAY_LIT face 补 8B 整数/指针 kind（与 index 标量面一致；PTR=指针宽 8 非 pointee） |
| soft | ret `T[N]` by-value → wave417 闭 |

## 25 日承接（wave415 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`2550b26e6`** |
| 债 | fs ARRAY_LIT nbytes>2048 / i32 n>512 CG002 |
| 根修 | G.7 `GLUE_ARRAY_LIT_MAX_ELEMS=1024` · `MAX_PAYLOAD=4096` |
| soft | i64 fs ARRAY_LIT → wave416 闭 |

## 25 日承接（wave414 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`bcee3af87`** |
| 债 | arm64 freestanding 大栈数组 SIGBUS |
| 根修 | G.7 整帧 prologue `sub sp; stp [sp]; mov x29,sp` |
| soft | nbytes>2048 → wave415 闭 |

## 25 日承接（wave413 · 日常 L2 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`d41a7ce2e`** |
| 债 | fs ARRAY_LIT 元数硬帽 256→512 |
| 根修 | G.7 `GLUE_ARRAY_LIT_MAX_ELEMS` |
| soft | arm64 大栈 SIGBUS → wave414 闭；nbytes 仍 soft |

## 25 日承接（wave412 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`07a561ab4`** |
| 债 | fs TYPE_SLICE deep-copy max_n=64 vs host 512 |
| 根修 | G.7：runtime loop max_n=512 · COMMON 零帧 |

## 25 日承接（wave411 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`7e3482b8e`** |
| 债 | fs TYPE_SLICE deep-copy 帧膨胀 |
| 根修 | COMMON BSS 零帧；max_n=64 soft（wave412 升 512） |

## 25 日承接（wave410 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`c4cd674ca`** |
| 债 | fs 同 call 双 CALL 返回 TYPE_SLICE last-wins |
| 根修 | call-arg 复用 deep-copy（当时帧内；wave411 改 COMMON） |

## 25 日承接（wave409 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`de0af37b9`** |
| 债 | 真递归 TYPE_SLICE let from CALL static last-wins |
| 根修 | host `__xlang_ldN` + fs deep-copy（jge；wave411 改 COMMON） |

## 25 日承接（wave408 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`c5501b9e3`** |
| 债 | mac arm64 freestanding TYPE_SLICE return→local dual-GP |
| 根修 | durable COMMON+ADRP(ta=1)；return push/pop + len_arg；store_rdx→str x1 |

## 25 日承接（wave407 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`096c47278`** |
| 债 | ARRAY_LIT 内嵌 INDEX 未 typeck → nested INDEX slice flag |
| 根修 | G.7：`typeck_check_expr_array_lit` + mega kind=46 |

### tip L4 钉盘（**已升 · 25 日 · `70ca1089b`**）

| 项 | 状态 |
|----|------|
| SHA | **`70ca1089b`**（含 wave421–431 Cap residual；产品 **`15a09c5c5`**） |
| 构建级别 | **L4 真冷**（全擦 compiler/std/core `.o` + 重链产品二进制） |
| remaining_o 起编 | ✅ **0**（双端） |
| 产品矩阵 | ✅ rv=42 · option · hello |
| mac 全量 bstrict | ✅ **OK (129)** · `/tmp/mac_true_cold_70ca1089b.log` · `/tmp/mac_true_bstrict_70ca1089b.log` |
| Ubuntu 全量 bstrict | ✅ **OK (129)** · `/tmp/ubuntu_true_cold_70ca1089b.log` · `/tmp/ubuntu_true_bstrict_70ca1089b.log` |
| 前钉 | `605e7efa4`（wave383；其后 wave384–431 L2 → 本波 tip L4 升钉） |

### 本波根修（升钉内容 · wave383）

| SHA | 摘要 |
|-----|------|
| `42be7ded3` | redef first-wins 比 param type_refs（保真 overload；types gate `pick_i64`） |
| `1ca3d7c04` | onefunc `return` 无 `;` 不得 Cap-T001 吞后续（semicolon_missing） |
| `4e6d46b39` | `L: return e;` 紧邻 `}` 时 pin final_expr（Linux x86_64 asm goto→42） |
| **`605e7efa4`** | **core.x 删空 `crypto_sha256_c` 桩**（G.7 权威 = runtime_crypto_inc_glue；双端 crypto 绿） |

## 25 日承接（wave406 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`e5b5fae51`**（seed 对齐 **`45f288fc8`**） |
| 债 | host-C 同 call 双 `take()` 返回 TYPE_SLICE last-wins |
| 根修 | `__xlang_spN` + deep-copy `__xlang_sdN[512]` |

## 25 日承接（wave405 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`f300deeb7`** |
| 债 | arm64 freestanding 跨函数模块 `let g`（set_g/get_g） |
| 根修 | modlet ta==1 + ADRP/COMMON typed reloc |

## 25 日承接（wave404 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`47e566b31`** |
| 债 | fs CALL 返回 T[N] 作 TYPE_SLICE 实参 fat+deep-copy |
| 根修 | call-arg dual-GP + 帧内 deep-copy；x86 payload_off=home+N×esz |

## 25 日承接（wave403 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`0e40fb0d0`** |
| 债 | arm64 freestanding binop 左操作数跨 CALL 被冲（x9 / SP） |
| 根修 | arm64 帧槽 nest spill；x86 push/pop |

## 25 日承接（wave402 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`c909650b8`** |
| 债 | arm64 freestanding 多定长数组局部槽重叠（low-end home） |
| 根修 | `asm_local_slot_reg_offset` MACOS\|ARM64 low-end |

## 25 日承接（wave401 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`de9a5c6b8`** |
| 债 | freestanding 转发 TYPE_SLICE 形参作 call-arg（load fat* not lea） |
| 根修 | `glue_enc_local_slot_ptr_or_addr_elf_c` |

## 25 日承接（wave400 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`24ca1d0fd`** |
| 债 | host-C `ARRAY_LIT` 作 TYPE_SLICE 形参（`__xlang_sp` 物化） |
| 根修 | `emit_call_arg_slice_abi` need_addr 扩 `EXPR_ARRAY_LIT=46` |

## 25 日承接（wave399 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`abd606098`** |
| 债 | freestanding VAR/FIELD → `T[N]` 整数组拷贝（arm64） |
| 根修 | lea(src)+i×esz + esz 宽 load |

## 25 日承接（wave398 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`94df961e3`** |
| 债 | freestanding `let t: T[N] = call()` arm64 |
| 根修 | CALL/METHOD E* spill + esz 宽 ptr-copy |

## 25 日承接（wave397 · 已闭）

| 项 | 状态 |
|----|------|
| SHA | **`784e592e8`** |
| 债 | 同 call 双 CALL/METHOD 返回 `T[N]` 作 slice* 实参 last-wins |
| 根修 | host unique `__xlang_caN` 深拷 |
## 25 日承接（wave395 · 跨日已闭）

| 项 | 状态 |
|----|------|
| SHA | **`e7cac169e`**（产品 `aee7a226d` + formal 门控；docs `8df932721`） |
| 债 | 定长 `TYPE_ARRAY` **VAR** 直接作 slice* 实参 |
| 根修 | host fat compound 仅 formal TYPE_SLICE；fs dual-GP |

## 24 日主线摘要（详表见归档）

| 段 | 内容 |
|----|------|
| **L4 升钉** | wave383 @ **`605e7efa4`** 双端 129（前钉 `ace59b6cd`） |
| **wave295–356** | float cast/arith · slice/array lit/assign/INDEX · multi-dim · UFCS 起步 · bare if/while … |
| **wave357–382** | nested INDEX · empty/ZST · match 语句 · type alias · redef · void return · bare goto host · `.length` 全写 |
| **wave384–390** | panic cstr · `let mut` · asm goto · arm64 cmp · Linux .s panic int · **impl Trait UFCS** |
| **wave391–395** | arm64 STRUCT_LIT store_size · multi-struct reentrancy · dual-GP `s.length` · length polarity · **TYPE_ARRAY VAR→slice*** |

更早全日：[`自举进度-归档-2026-07-23.md`](自举进度-归档-2026-07-23.md) 及更前归档。

## 文档

| 文档 | 用途 |
|------|------|
| [自举进度.md](自举进度.md) | 25 日起 KPI / 前排 / §6 |
| [自举进度-归档-2026-07-24.md](自举进度-归档-2026-07-24.md) | **24 号全日**（只读） |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图 |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |
