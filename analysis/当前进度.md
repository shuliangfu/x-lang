# Shux 当前进度（2026-07-20）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **归档**：[自举进度-归档-2026-07-19.md](自举进度-归档-2026-07-19.md) · [18 号](自举进度-归档-2026-07-18.md) · [17 号](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`2ecffeca`**（C6 波 3 子波 1 PoC：std/atomic 30 个 `extern "C"` 标注） |
| 产品 L4 放行钉盘 | **`deaf773b`**（20 日升钉；前钉 `305cfbe1`；mac+Ubuntu 双端 L4 真冷 + 125 全绿 + Windows hybrid 门禁首次全绿 → 升钉） |
| 产品 bstrict 套件 | **125**（钉盘时 · 当前白名单 129 待 shux_asm 构建 L4 验证） |
| 本波性质 | **C6 X ABI P0b 波 3 std/ 扩展标注**（8 子波分批推进 · 当前子波 1/8 PoC 完成） |
| 本波输出 | std/atomic/mod.x 30 个 `extern function atomic_*_c` → `extern "C" function`（C11 stdatomic 包装）；文件顶部加英文 ABI 块注释；`shux-c check` 验证通过（L2 · 未跑 L4 避免爆内存） |

## 本波摘要（2026-07-20 · C6 波 3 子波 1 PoC · 1 commit `2ecffeca` · L2 验证未跑 L4）

| 项 | 结果 |
|----|------|
| **入口** | [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.4 波 3 子波划分：8 子波按文件大小递增，PoC 验证 typeck 强制 unsafe 后无回归再扩展 |
| **子波 1** | `std/atomic/mod.x`（30 个 `extern function atomic_*_c` 声明）：批量标注为 `extern "C" function atomic_*_c`；文件顶部"See implementation."占位符替换为详细英文 ABI 块注释（ABI 契约 + 平台范围 + 内存模型 + unsafe 调用约定） |
| **标注模式** | 声明侧：`extern function xxx_c` → `extern "C" function xxx_c`（与 `compiler/src/parser/parser.x:36-38` / `main.x:30-31` 等已标 `extern "C"` 声明同模式） |
| **不改动 export function** | `load/store/compare_exchange/fetch_add/fetch_sub/fence_*/atomic_module_anchor` 等 SHUX 模块桥接函数保持 `export function`（X ABI 默认，按 X-ABI-设计分析.md §3.2.1 ABI 判定标准是合法 SHUX-to-SHUX 桥接） |
| **unsafe 包裹状态** | 所有 `atomic_*_c` 调用点已在 `unsafe { return ...; }` 或 `unsafe { ...; }` thin-wrap 内（line 58/67/76/85/94/103/114/123/132/141/150/159/170/181/192/203/214/225/236/246/256/266/276/286/297/307/317/343/351/359）— P0 契约合规，无需改动 |
| **本机验证** | `./compiler/shux-c check -L . std/atomic/mod.x` exit=0（parse + typeck only · 不编译不链接 · 不触发 shux-x -E · 不爆内存） |
| **构建级别** | L2（本机 check 验证）；未跑 L4 避免爆内存（shux_asm 构建触发 shux-x -E 爆内存 ~5GB） |
| **未跑 L4 理由** | std/atomic/atomic.c 在 F-07 v1/v2 禁用列表（tests/lib/build-std-c-o.sh:34），必须用 shux_asm 编译；shux_asm 构建触发 shux-x -E 爆内存 ~5GB；用户明确要求"不能爆 CPU 和内存"；标注是语法改动，P0a 语义降级下不影响 codegen，无回归风险 |

## 下一波前排（按优先级排序）

### 1. C6 波 3 子波 2-8（继续 std/ extern "C" 标注 · 不爆内存）

按 [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.4 子波划分（按文件大小递增）：

| 子波 | 文件 | extern function 数 | 状态 |
|------|------|-------------------|------|
| 1 | `std/atomic/mod.x` | 30 | ✅ 完成 @ `2ecffeca`（PoC） |
| 2 | `std/db/arrow/mod.x` | 29 | ⏳ 下一波 |
| 3 | `std/db/sqlite/mod.x` | 32 | ⏳ |
| 4 | `std/math/mod.x` | 32 | ⏳ |
| 5 | `std/json/mod.x` | 37 | ⏳ |
| 6 | `std/async/mod.x` | 42 | ⏳ |
| 7 | `std/socketio/mod.x` | 106 | ⏳ |
| 8 | `std/http/mod.x` | 249 | ⏳ 最大，最后做 |
| **合计** | 8 文件 | **557** | **30/557 完成（5.4%）** |

**每子波流程**（不爆内存）：
1. Read 文件确认 `extern function xxx_c` 数量
2. Edit replace_all 批量标注 `extern "C" function xxx_c`
3. 加文件顶部英文 ABI 块注释（如未有）
4. `./compiler/shux-c check -L . <file>` 验证（L2 · 不编译不链接）
5. commit（feat(abi): annotate std/<mod> C ABI wrappers）
6. 更新 当前进度.md + 自举进度.md §6

### 2. C7 L4 验证（待 shux_asm 构建后验证 129 白名单 · 当前避免爆内存未跑）

- **C7 plan 瘦身 8/8 完成**（3 合并 + 1 删除 + 4 纳入白名单 125→129）：[C7-plan-audit.md](C7-plan-audit.md) §5
- **待验证项**（需 shux_asm，避免爆内存未构建）：4 个新加白名单脚本 + fmt 一元负号 LEGACY 重链接后端到端验证
- **触发条件**：下次需要 L4 真冷验证时（如新升钉、Cap 收口），一并跑 C7 L4 验证 + C6 波 3 整波 L4 验证

### 3. Windows L4 真冷测（可选 · 非必需）

- 非必需（双端门禁已满足），但若用户希望 Windows 也达到 L4 冷测级别可补跑

## 不在前排

- **C5 EXPR_CLOSURE CTFE**（独立大工程 · 多波）：已 scoping，顺延自举后立项 `lang-closure-v1-rfc.md`
- **Cap C9 match guard full-chain 支持**（独立大工程 · 多波）：6 个产生点改动，先排期再动手
- **shux-x 5GB 方案 B**（远期 · 非 KPI）：ArenaSidecar release API
- **历史进度**：C7 plan 瘦身 8/8 收口（`1d41e903` 125→129 待 L4 验证）/ Windows 门禁收口（`deaf773b` 升钉）/ C6 波 2 ABI 一致性（`305cfbe1` 升钉）/ C6 波 1 unsafe 包裹（`4214eea4` 升钉）/ Windows shux-c exit=1 根因修复（`d25b82cc`）/ C6 P0b audit（`7e507fb6`）/ shux-x 5GB 方案 A（`a14a1dd0`）/ C7 plan 瘦身 PoC（`e546b013`+`bedb40ca`）/ CLI 优化（Phase 1 + help 美化 8 commit）/ std.print 架构优化 / C5 三连击（`c174e70a`）/ 4 波 C5 CTFE / Windows 时序决议 / C2/C3=C4/backtrace/C8 钉盘波，全部归档在 [自举进度.md](自举进度.md) §6 变更记录

```text
本波 tip：2ecffeca (C6 波 3 子波 1 PoC：std/atomic 30 个 extern "C" 标注)
本波性质：C6 X ABI P0b 波 3 std/ 扩展标注（8 子波分批推进 · 当前 1/8 PoC 完成）
本波输出：std/atomic/mod.x 30 个标注 + 英文 ABI 块注释；shux-c check 验证通过
构建级别：L2（本机 check 验证）；未跑 L4 避免爆内存（shux_asm 构建触发 shux-x -E 爆内存 ~5GB）
产品 tip L4 钉盘：deaf773b（20 日升钉；前钉 305cfbe1；双端 L4 真冷 + 125 全绿 + Windows hybrid 门禁首次全绿）

C6 波 3 子波进度（8 子波 · 557 个标注 · 按文件大小递增）：
  子波 1: std/atomic/mod.x    30 个 ✅ @ 2ecffeca（PoC）
  子波 2: std/db/arrow/mod.x  29 个 ⏳ 下一波
  子波 3: std/db/sqlite/mod.x 32 个 ⏳
  子波 4: std/math/mod.x      32 个 ⏳
  子波 5: std/json/mod.x      37 个 ⏳
  子波 6: std/async/mod.x     42 个 ⏳
  子波 7: std/socketio/mod.x 106 个 ⏳
  子波 8: std/http/mod.x     249 个 ⏳ 最大，最后做
  合计: 30/557 完成（5.4%）

标注模式（声明侧）：
  extern function xxx_c(...) -> extern "C" function xxx_c(...)
  （与 compiler/src/parser/parser.x:36-38 / main.x:30-31 等已标 extern "C" 声明同模式）

不改动 export function：
  load/store/compare_exchange/fetch_add/fetch_sub/fence_*/atomic_module_anchor
  保持 export function（X ABI 默认 · 合法 SHUX-to-SHUX 桥接）

unsafe 包裹状态：所有 atomic_*_c 调用点已在 unsafe { return ...; } 或 unsafe { ...; } thin-wrap 内
  （line 58/67/76/85/94/103/114/123/132/141/150/159/170/181/192/203/214/225/236/246/256/266/276/286/297/307/317/343/351/359）
  — P0 契约合规，无需改动

本机验证：./compiler/shux-c check -L . std/atomic/mod.x exit=0（parse + typeck only · 不爆内存）
未跑 L4 理由：std/atomic/atomic.c 在 F-07 v1/v2 禁用列表，必须用 shux_asm 编译；shux_asm 构建触发 shux-x -E 爆内存 ~5GB

权威文档：analysis/C6-P0b-scope-audit.md（C6 P0b 范围审计 · §5.4 波 3 子波划分）
权威文档：analysis/X-ABI-设计分析.md §3.2.1 ABI 判定标准 + §3.2.2 P0b 范围
历史进度归档：analysis/自举进度.md §6 变更记录
```
