# Shux 当前进度（2026-07-19）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **归档**：[自举进度-归档-2026-07-18.md](自举进度-归档-2026-07-18.md) · [17 号](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`a14a1dd0`**（perf(ast_pool) 方案 A 承自前波；本波 docs-only 调查，无代码改动 → tip 不变） |
| 产品 L4 放行钉盘 | **`5cc73d54`**（双端真冷 L4+125；前钉 `ea1f6827`；本波 docs-only → 不升钉） |
| 双端 L4 兜底（本日） | ✅ 双端全绿（`94a8e7d5` SHA）；详见 [自举进度.md](自举进度.md) §6 |
| 产品 bstrict 套件 | **125** |
| 本波性质 | **docs-only 调查**（C6 X ABI P0b 范围审计，无代码改动 → 无 L2 / 无升钉） |
| 本波输出 | [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md)（5466 extern 声明实测 + 9 处 unsafe 违规 + 3 处 ABI 不一致 + 3 波拆分策略） |

## 本波摘要（2026-07-19 · C6 X ABI P0b 范围审计 · docs-only · 无代码改动）

| 项 | 结果 |
|----|------|
| **入口** | 推进 [自举进度.md](自举进度.md) §3 前排 #3「C6 X ABI P0b」 + [X-ABI-设计分析.md](X-ABI-设计分析.md) §3.2.2 P0b 范围。先做范围审计，再分波执行 |
| **实测总量** | `extern "C" function`（C ABI）**3035 个** + `extern function`（X ABI 默认）**2431 个** + `extern "x" function`（显式 X ABI）**0 个** = 合计 **5466** extern 声明 |
| **核心 14 个 C ABI 调用** | ✅ **全部已标注** `extern "C"`（parser.x:36-38 / main.x:30-31 / pipeline.x:81-82 / driver/emit.x:71-73 / asm_libroot/std/fs/mod.x:11-14） |
| **调用点 unsafe 包裹违规** | 🔴 **9 处**：`main.x` L671/672/677/678/685/690 共 6 处 + `asm_libroot/std/fs/mod.x` L23/33/38 共 3 处（P0 契约违反；P0a typeck 强制 unsafe 未激活所以当前不断链，P1 激活会断链） |
| **声明/定义 ABI 不一致** | 🔴 **3 处**：`main.x:30-31` + `pipeline.x:81` + `driver/emit.x:71` 声明 `extern "C"` vs `runtime_io_abi.x:164/176/188` 定义 `export function`（X ABI 默认）。当前 P0a 语义降级下不断链，P1 激活 X ABI 调用约定后参数寄存器错位 / 运行期 UB |
| **std/ 库扩展范围** | 🟡 **~557 个** C 库包装待标注（http 249 / socketio 106 / async 42 / json 37 / math 32 / sqlite 32 / atomic 30 / arrow 29 + 其他 ~32）；远期债，自举前不强制（P0a 语义降级保护） |
| **正面对照** | `parser.x` ~270 个 `unsafe { }` 块（Cap-T001 whole-body unsafe FFI gate）✅ / `driver/emit.x` thin unsafe wrap 模式 ✅ / `runtime_io_abi.x` 全部包裹 ✅ |
| **typeck enforcement 状态** | `pipeline_typeck_check_extern_call_unsafe_boundary_c` 已在 `typeck.x:289` 声明但**未激活**（P0a 语义降级）→ 9 处违规当前可编译通过 |
| **拆分策略** | **3 波收口**：波 1 unsafe 修复（9 处）→ 波 2 ABI 一致性（3 处）→ 波 3 std/ 扩展标注（~557 处，顺延自举后 P1-P2） |
| **风险** | 自举前必须完成波 1 + 波 2（共 12 处改动），否则 P1 激活 typeck 强制 unsafe / X ABI 调用约定时 bootstrap 陷阱 |
| **本波 commit** | docs-only：[C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) 新建 + 本文更新 + [自举进度.md](自举进度.md) §3/§6 更新；无代码改动 → 无 L2 / 无升钉 / 无 Windows 门禁触发 |

## 下一波前排（按优先级排序）

### 1. C6 X ABI P0b 波 1：unsafe 包裹修复（9 处 · ABI 边界 · 触发 Windows 门禁）

按 [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.2 推荐执行：
- `compiler/src/main.x` L671/672/677/678/685/690 共 6 处 → 包裹 `unsafe { }`（thin unsafe wrap 模式，与 `driver/emit.x:178` 一致）
- `compiler/asm_libroot/std/fs/mod.x` L22-39 共 3 处（`open`/`read`/`write` 封装函数体）→ 包裹 `unsafe { return ...; }`（与同文件 L18 `fs_mod_close` 一致）
- 验证级别：**L4 双端真冷**（mac arm64 + Ubuntu x86_64）；触发 Windows 门禁强制重跑（ABI 边界改动 per [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md) §5.1）
- **必须升钉**（unsafe 契约改动属 ABI 边界）

### 2. C6 X ABI P0b 波 2：ABI 一致性修复（3 处 · ABI 边界 · 触发 Windows 门禁）

按 [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.3 推荐执行：
- `compiler/src/runtime_io_abi.x` L164/176/188 三个函数定义 → 改为 `export extern "C" function`（与 `main.x:30-31` / `pipeline.x:81` / `driver/emit.x:71` 声明一致）
- 函数体内 libc `write`/`close`/`read` 调用包裹 `unsafe { }`（如尚未包裹）
- 验证级别：**L4 双端真冷**；触发 Windows 门禁；**必须升钉**

### 3. C7 plan 瘦身（工程债 · 非产品）

plan-only bstrict 模板裁剪（按 `tests/run-all-bstrict.sh` plan 子集）

## 不在前排

- **C6 X ABI P0b 波 3：std/ 扩展标注**（远期 · 多波 · 顺延自举后 P1-P2）：~557 个 C 库包装分文件标注 `extern "C"`（http 249 / socketio 106 / async 42 / json 37 / math 32 / sqlite 32 / atomic 30 / arrow 29）；按文件大小递增推进，`std/atomic/mod.x`（30 个）作 PoC 验证 typeck 强制 unsafe 后无回归再扩展；详见 [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §4 / §5.4
- **shux-x 5GB 方案 B**（远期 · 非 KPI）：为 ArenaSidecar/ModuleSidecar 加 release API；前提 typeck 完成后 dep_arenas 不再被访问；方案 C/D 详见 [shux-x-5GB-内存根因调查.md](shux-x-5GB-内存根因调查.md) §6
- **Cap C9 match guard full-chain 支持**（独立大工程 · 多波）：6 个产生点改动，先排期再动手
- **C5 EXPR_CLOSURE CTFE**（独立大工程 · 多波）：已 scoping（[C5-EXPR_CLOSURE-CTFE-scoping.md](C5-EXPR_CLOSURE-CTFE-scoping.md)），顺延自举后立项 `lang-closure-v1-rfc.md`
- **历史进度**：shux-x 5GB 方案 A（`a14a1dd0`）/ 日终 L4 兜底（`94a8e7d5`）/ C5 三连击（`c174e70a`）/ 4 波 C5 CTFE / shux-x 5GB 调查 / Windows 时序决议 / C2/C3=C4/backtrace/C8 钉盘波，全部归档在 [自举进度.md](自举进度.md) §6 变更记录

```text
本波 tip：a14a1dd0 (perf(ast_pool): split AST_POOL_INIT_CAP=256 from AST_POOL_GROW=4096) — 承自前波，本波 docs-only 无代码改动
本波性质：docs-only 调查（C6 X ABI P0b 范围审计）
本波输出：analysis/C6-P0b-scope-audit.md（新建）+ analysis/当前进度.md（本文更新）+ analysis/自举进度.md（§3 前排 + §6 变更记录）
构建级别：无（docs-only，无代码改动 → 无 L2 / 无 L4 / 无升钉 / 无 Windows 门禁触发）
产品 tip L4 钉盘：5cc73d54（未升；本波 docs-only 调查，不需要升钉）

实测统计（2026-07-19 全量扫描 compiler/src + compiler/asm_libroot + std）：
  extern "C" function (C ABI):       3035 个（含合法 dispatch / runtime / libc 封装；Top 20 全部合法）
  extern function   (X ABI 默认):    2431 个（含合法 SHUX 桥接；std/ 下 ~557 个为 C 库包装待标注）
  extern "x" function (显式 X ABI):    0 个（语法槽位已就位，暂未使用）
  合计 extern 声明:                  5466 个

核心 14 个 C ABI 调用（设计文档 §3.2.2 定义）：
  parser.x:36-38 (3) ✅ + main.x:30-31 (2) ✅ + pipeline.x:81-82 (2) ✅ + driver/emit.x:71-73 (3) ✅ + asm_libroot/std/fs/mod.x:11-14 (4) ✅
  → 100% 已标注 extern "C"

调用点 unsafe 包裹违规 9 处：
  main.x L671/672/677/678/685/690 (6 处，driver_run_x_emit_c_set_path_and_run 函数体内未包裹 unsafe)
  asm_libroot/std/fs/mod.x L23/33/38 (3 处，open/read/write 三个封装函数直接 return 未包裹 unsafe)
  → P0 契约违反；当前 P0a typeck 未强制 unsafe 所以不断链；P1 激活会断链

声明/定义 ABI 不一致 3 处：
  main.x:30 (声明 extern "C") vs runtime_io_abi.x:188 (定义 export function，X ABI 默认) → fs_posix_write_c
  main.x:31 (声明 extern "C") vs runtime_io_abi.x:164 (定义 export function，X ABI 默认) → fs_posix_close_c
  pipeline.x:81 + driver/emit.x:71 (声明 extern "C") vs runtime_io_abi.x:176 (定义 export function) → fs_posix_read_c
  → P0a 语义降级下不断链；P1 激活 X ABI 调用约定后参数寄存器错位 / 运行期 UB

正面对照（合规模式）：
  parser.x: Cap-T001 whole-body unsafe FFI gate（~270 个 unsafe { } 块）
  driver/emit.x: thin unsafe wrap（每个 extern "C" 调用一个 unsafe { return ...; } 块）
  runtime_io_abi.x: 全部 extern "C" 调用都在 unsafe { } 块内

拆分策略 3 波：
  波 1（推荐立即执行）: unsafe 包裹修复 9 处（main.x 6 + mod.x 3）→ L4 双端 + 升钉 + Windows 门禁
  波 2（波 1 后立即）: ABI 一致性修复 3 处（runtime_io_abi.x 定义改 extern "C"）→ L4 双端 + 升钉 + Windows 门禁
  波 3（顺延自举后 P1-P2）: std/ 库 ~557 个 C 库包装标注 extern "C"，按文件大小递增分波（std/atomic 30 个作 PoC）

风险：自举前必须完成波 1 + 波 2（共 12 处），否则 P1 激活 typeck 强制 unsafe + X ABI 调用约定时 bootstrap 陷阱

权威文档：analysis/C6-P0b-scope-audit.md（§0 摘要 + §1 实测统计 + §2 核心 14 审计 + §3 ABI 一致性 + §4 std/ 扩展 + §5 3 波拆分 + §6 风险评估 + §7 验证清单 + §8 调查方法 + §9 相关文档）
历史进度归档：analysis/自举进度.md §6 变更记录（shux-x 5GB 方案 A a14a1dd0 / 日终 L4 兜底 94a8e7d5 / C5 三连击 c174e70a / 4 波 C5 CTFE / shux-x 5GB 调查 / Windows 时序决议 / C2/C3=C4/backtrace/C8 钉盘波）
```
