# X 语言 / X language 当前进度（2026-07-29 · wave638 Cap host-C `(*p).field` · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`b1a8c6cfc`**（wave638 产品） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave638（已闭 · Cap residual host-C FIELD base primary · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | host-C `(*p).v` / `(*self).v` 发 `(*(p).v)` → BLD001；freestanding 已绿 |
| tip 产品 | **`b1a8c6cfc`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave638 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| FIELD 括号 | host-C 发 `(base.field)`；base=DEREF `*(p)` → `(*(p).v)`；C `.` 优先于一元 `*` 解析成 `*((p).v)` | ✅ 发 **`((base).field)`** / `((base)->field)` |

**数据流**：产生 = FIELD host emit 括号 → 存储 = 生成 C → 消费 = host cc。G.7：补全既有 FIELD 发射，不新开第二套。INDEX 已单独包 base，不动。

**权威**：`compiler/src/codegen/codegen.x` · `compiler/seeds/codegen_gen.linux.x86_64.c`（同 commit）。

### wave638 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `(*p).v` take host/fs | ✅ 42 | ✅ 42 |
| `(*p).v` assign bump host/fs | ✅ 42 | ✅ 42 |
| `impl` `*self` `(*self).v` host/fs | ✅ 42 | ✅ 42 |
| `self.v` with `self: *T` 回归 | ✅ 42 | ✅ 42 |
| plain `s.x` / `p.x` arrow 回归 | ✅ 42 | ✅ 42 |
| wave637 `*i32[2]` 回归 | ✅ 42 | ✅ 42 |
| `-E` 形 | `((*(p)).v)` | 同 |
| rv / option / hello | ✅ 42/102/0 | ✅ 42/102/0 |

### 仍 soft

- untyped `self`（`function f(self)` 无 `: Type`）— wave493：**非标准** X 语法；须 `self: Point`  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶，或整波 L4 + 升钉  

## wave637（已闭 · Cap residual freestanding array-of-pointers `*T[N]` · L2）

tip 产品 **`e82ecdbd2`**。INDEX esz=8 + fixed-array PTR slot=8。

## wave636（已闭 · Cap residual pure-asm + host-C PTR→fixed TYPE_ARRAY · L2）

tip 产品 **`7bce90b01`**。`*[N]T` DEREF leave-addr + host `E (*)[N]`。

## wave635（已闭 · Cap residual pure-asm fixed TYPE_ARRAY call-arg E* not MEMORY · L2）

tip 产品 **`b5615797e`**。freestanding `take(S24[2])` 双端 77。
