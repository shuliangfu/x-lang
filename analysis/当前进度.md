# X 语言 / X language 当前进度（2026-07-27 · wave529）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`18f7c0488`**（双端 L2，wave529；L4 待整波收口） |
| 钉盘 | **`d5de2f13b`**（升自 `2bc0885d3`；双端 L4 129 全绿） |
| bstrict | **129** |

## wave529（已闭 · runtime_http_glue Makefile R2 thin+rest 补全 双端 L2）

| 项 | 状态 |
|----|------|
| 触发 | wave528 钉盘升级后，继续审计剩余"半残"模块；发现 runtime_http_glue .x 已有 25 函数但 Makefile 未跟上 |
| 模块 | `runtime_http_glue`（1154 行；HTTP 客户端/服务端胶层：parse/transport/TLS/send/recv/format + 65 公开 API） |
| 债 | `.x` 已有 25 个 `#[no_mangle]` wrappers（转发到 `_impl`）+ `extern "C"` _impl 声明 + doc_anchor；`.from_x.c` 已有多处 `XLANG_RUNTIME_HTTP_GLUE_FROM_X` guard + _impl 实现；**Makefile 规则未跟上** |
| 根修 | 补全 Makefile 规则为 thin+rest `ld -r` 模式（无需改 .x/.from_x.c 代码）；保留 `-Iseeds/http` include 路径（http2/*.inc.c 头文件） |
| 构建模式 | **thin+rest**——thin (.x) 提供 25 个 `#[no_mangle]` 纯转发 wrappers；rest (.from_x.c) 提供 _impl 实现（HTTP parse/transport/TLS/send/recv/format）+ C-only 函数（http_parse_status_line_c, http_get_c, http_head_c 等） |
| nm 一致性 macOS | cold vs R2：317 cold vs 318 R2 T 符号（R2 多 `_doc_anchor`） |
| nm 一致性 Ubuntu | cold vs R2：305 cold vs 306 R2 T 符号（R2 多 `_doc_anchor`） |
| 产品矩阵 macOS | g-ffi-5-std-wrap ✅ · crypto 全链路 ✅ |
| 产品矩阵 Ubuntu | g-ffi-5-std-wrap ✅ · crypto 全链路 ✅ |
| 验证级别 | **L2 日常真测**（按自举方法 §0.2 节奏；非每微 commit 全量 L4） |
| PLATFORM | SHARED — HTTP 客户端/服务端胶层，POSIX socket syscalls；include 路径 `-Iseeds/http` 保留 |
| 关键发现 | wave528 钉盘升级后，重新审计发现 wave521-526 扫描遗漏的"半残"模块（.x 在 `src/asm/http/` 子目录，wave521 审计脚本只扫 `src/asm/*.x` 漏了子目录）。后续审计需包含 `src/asm/**/*.x` glob |
