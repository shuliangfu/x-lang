# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`86281cc6`**（双端 L4 真冷 + 129；未升） |
| tip | **R2 driver_abi Cap residual pure wave22**（fputs 桥；日常 L2） |
| 产品 bstrict | **129** · tip L4 钉盘仍 `@86281cc6` |
| 本波性质 | **工程轨 R2 pure 微步**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42/option/si/hello + pure-asm rv42 @ `a11f7134` |
| mac | ✅ L2 同；另验 `shux fmt` 路径 |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `runtime_driver_abi` Cap residual pure wave22（本波）**  
   - 真迁：`driver_preamble_fputs`  
   - 手段：.x 禁 FILE*；直接 `fputs(*u8,*u8)` 会 -Werror=incompatible-pointer-types  
     → pure null 守卫在 thin.x；FILE* cast 走 g05 prologue  
     `static inline shux_driver_fputs_opaque`（对齐 `shux_fmt_opendir`）  
   - G.7：单一权威符号仍 `driver_preamble_fputs`（rt_preamble / async emit 调用面不变）  
   - seed：cold C twin；`#ifndef SHUX_L2_RDABI_THIN_FROM_X` 剔 pure-dup  
   - 仍 seed：巨型表文本 `rt_preamble`；`*_lines_raw`；calloc/host_defaults；tmp 访问器  
   - 代码 SHA：**`a11f7134`**（docs 本 commit 跟进）  
   - 矩阵：rv_build=0 / rv_run=42 / opt 0 / si 0 / hello 0 / pure-asm rv42  
   - 钉盘仍 **`86281cc6`**（skill §3.3：微步不自动 tip L4）

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文；**不**改 `当前问题分析.md` / `问题分析文档.md`

## 下一波前排

1. **可选** driver_abi 余 Cap residual（**calloc** / **host_defaults** / tmp）  
2. **可选** g05 sed / harness `-E` 评估  
3. **可选** Windows hybrid；Darwin arm64 纯 asm Vec4f CG002  

```text
钉盘：86281cc6（tip L4 双端 129）
tip：R2 driver_abi wave22 fputs pure（L2 @ a11f7134）
Windows：本波不需要切换
下一：calloc·host_defaults / tmp / g05 sed / Windows / Darwin CG002
```
