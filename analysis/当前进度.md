# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`86281cc6`**（双端 L4 真冷 + 129；未升） |
| tip | **R2 driver_abi Cap residual pure wave28**（invoke_cc；日常 L2） |
| 产品 bstrict | **129** · tip L4 钉盘仍 `@86281cc6` |
| 本波性质 | **工程轨 R2 pure 微步**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42/option/si/hello + pure-asm rv42 @ `7e7589ee` |
| mac | ✅ L2 同 |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `runtime_driver_abi` Cap residual pure wave28（本波）**  
   - 真迁：`driver_parsed_invoke_cc` → thin.x  
   - pure：std .o path pack + set/clear user .o + `shux_invoke_cc` + fail/KEEP_C cleanup  
   - `c_paths[1]` via G.7 `shux_ptr_slot_set`（.x 禁 `**u8` 表 lit）  
   - BLD001 / KEEP_C note：`driver_diag_append_*` + `diag_report(_with_code)`（无 va_list reportf）  
   - seed：cold twin `#ifndef SHUX_L2_RDABI_THIN_FROM_X`；FROM_X 无 pure-dup  
   - 仍 seed：巨型 `rt_preamble` 表（`write_io_net` / `write_fs_path`）  
   - 注释：**JSDoc**（`@param` type+role / `@return`；`*/` 单独成行）  
   - 代码 SHA：**`7e7589ee`**（docs 本 commit 跟进）  
   - 矩阵：rv_build=0 / rv_run=42 / opt 102 / si 0 / hello 0 / pure-asm rv42  
   - 日志：`/tmp/mac_g05_wave28b.log` · `/tmp/ubuntu_g05_wave28b.log`  
   - Ubuntu pure 符号：`driver_parsed_invoke_cc` = **W**（G05_X_O_WEAK；OK）  
   - 钉盘仍 **`86281cc6`**（skill §3.3：微步不自动 tip L4）

2. **注释纪律（已固化）**  
   - 方法块注释 = JSDoc；参数类型与作用写全；日后 **`shux doc`** 生成  
   - skill G.9 + MEMORY + `references/x-english-comments.md`

3. **文档纪律**  
   - 只改 `自举进度.md` + 本文；**不**改 `当前问题分析.md` / `问题分析文档.md`

## 下一波前排

1. **可选** driver_abi 余 Cap residual（巨型 **`rt_preamble`** 表：`write_io_net` / `write_fs_path`）  
2. **可选** g05 sed / harness `-E` 评估  
3. **可选** Windows hybrid；Darwin arm64 纯 asm Vec4f CG002  

```text
钉盘：86281cc6（tip L4 双端 129）
tip：R2 driver_abi wave28 invoke_cc pure（L2 @ 7e7589ee）
Windows：本波不需要切换
下一：rt_preamble 巨型表 / g05 sed / Windows / Darwin CG002
```
