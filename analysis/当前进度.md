# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（本波未改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`11a57351`**（双端 129） |
| tip | **`e364aaf2`**（R2 `async_cps` Cap residual pure wave5 begin/hoist） |
| 产品 bstrict | **129** · 钉盘双端全绿 |
| 本波性质 | Cap residual pure：CPS begin / param_statics / hoist_impl |
| 工程轨 prove | ✅ mac IDENTICAL · Ubuntu IDENTICAL（L2） |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `async_cps_codegen` Cap residual pure wave5（begin/hoist）**（`e364aaf2`）  
   - `.x`：`async_cps_codegen_begin` / `emit_param_statics` / `emit_hoisted_lets_impl`  
   - helpers：`async_cps_store_ptr` / `emit_static_decl` / `type_is_run_seed_scalar` / `emit_run_seed_take`  
   - G.7：`async_liveness_type_to_c_buf` 权威（无第二套 type 表）  
   - 桥（always seed）：`async_cps_fputs`；宿主 LE：ASTFunc params@32/num@40/body@56；ASTParam size24；ASTLetDecl size48；layout num_awaits@4100  
   - seed：`#ifndef SHUX_ASYNC_CPS_CODEGEN_FROM_X` 省略 wave5；surface re-pin + prove MODULES 注释  

2. 验证  
   - mac：`SHUX=./compiler/shux bash tests/prove_module_selfhost.sh async_cps_codegen` → IDENTICAL · `/tmp/mac_prove_async_cps_wave5.log`  
   - Ubuntu：pull tip 后 prove IDENTICAL · `/tmp/ubuntu_prove_async_cps_wave5.log`  
   - **日常 L2 非 tip L4**；钉盘仍 `11a57351`  

3. 前序：wave4 FILE* emit @ `33505fbe` · wave3 walk_impl @ `33881f79` · liveness Cap residual pure @ `b06cf472` · 钉盘 `11a57351`

## 下一波前排

1. **Cap residual 余**：liveness FILE* emit_*（typedef/local/comment）  
2. **或** 产品 PREFER 链 / `pipeline_glue` unbundle `async_asm_pool`  
3. **可选** tip 升钉：Ubuntu L4 + 双端 129  
4. **可选** Windows hybrid（需切双系统时再喊人）  

```text
钉盘：11a57351
tip：e364aaf2（CPS Cap residual pure wave5 begin/hoist；双端 L2 prove IDENTICAL）
Windows：本波不需要切换
下一：liveness emit_* residual / 可选 tip L4
```
