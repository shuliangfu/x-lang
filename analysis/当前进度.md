# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`46ba2297`**（升自 `11a57351`；双端 L4 + 129） |
| tip | **本波 bare CLI path**（日常 L2，**非** tip L4 升钉；前 tip `961207bf`） |
| 产品 bstrict | **129** · 钉盘双端 L4 真冷全绿（本波未重跑 tip L4） |
| 本波性质 | 产品 CLI：裸 `shux file.x [-o]` 恢复为 run 语义；simd-s4 非 x86 用 C backend 硬绿 |
| Ubuntu | 待 pull 后 L2：bare path + 矩阵（SHARED） |
| mac | ✅ L2 g05 + bare rv=42 + simd-s4 gate OK + shuxffle parent OK + 矩阵 |
| Windows | **本波不需要** |

## 本波摘要

1. **bare CLI path（产品根修）**  
   - 地图：`main.x` `entry`/`main_entry` 对非 subcommand 的 argv[1] 一律 usage → 大量 gate `shux file.x -o` 假红（打印 help）  
   - 根修：路径启发式（`.x` 或含 `/` `\`）→ `main_cmd_run`（有 `-o` 编译-only；无 `-o` compile+exec）  
   - 权威：`compiler/src/main.x` + pin `seeds/driver_gen.linux.x86_64.c` 同 commit（FORCE_REGEN 不可用）  
   - **日常 L2**（非 tip L4）

2. **simd-s4 harness**  
   - 非 x86_64：compile smoke 用 `build -backend c`（Darwin arm64 纯 asm `-o *.o` 对 Vec4f mul 可 CG002；HW 检查本就只在 x86）  
   - mac：`run-simd-s4-gate` OK；parent shuxffle OK（s4=1）

3. 前序钉盘 **`46ba2297`**；§3.6 tip **`961207bf`** 仍未 tip L4 升钉

## 下一波前排

1. **可选** Ubuntu L2 复核本波 bare path + 整波 tip L4 升钉（含 `961207bf`+本波）  
2. **可选** 其它 R2 rest / Cap residual；g05 sed 防御；Ubuntu `-E` SIGSEGV 根因  
3. **可选** Windows hybrid；Darwin arm64 纯 asm Vec4f `.o` CG002（与 s4 HW 无关）

```text
钉盘：46ba2297
tip：bare CLI path + simd-s4 harness（L2）
Windows：本波不需要切换
下一：Ubuntu L2 复核 / 可选 tip L4 升钉
```
