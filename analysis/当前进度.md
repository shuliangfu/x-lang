# Shux 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-22.md](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave187** ensure_std_net_o_auto_tls pure @ `353be503c` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **R2 Cap residual pure · ensure_std_net_o_auto_tls** → L6 invoke_ld_list |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + invoke_ld prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + invoke_ld prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：常驻 Cap residual soft — `ensure_std_net_o_auto_tls` 体仍 always mega C（getenv + marker 探针 + system make）  
2. **根修 / pure**：真迁 `labi_invoke_ld_list.x` L6 pure（pure path/make-cmd join helpers + mode orch；Cap residual **getenv** + **system** + **realpath_cap** + **exports_marker**；peer wave158 marker 名）  
3. **Cap residual**：shell make / getenv / realpath / exports_marker 仍 Cap；append_std / invoke_cc_impl / formal_make / undef_sym 等仍 residual  
4. **验收**：双端 L2 g05（L6 ← pure .x）+ 矩阵 + 邻域 + invoke_ld_list prove IDENTICAL；钉盘仍 **`3c88f7e51`**  

## 下一波前排

1. 其它 rest R2 / mega residual（可迁：常驻 Cap — undef_sym / path_io stat / wait / strerror / mega spawn-IO / ld_debug_argv / getenv / resolve / path_readable / invoke_cc_impl·append_std_objs 大叶 / formal_std_make …；ensure_std_net @ wave187 pure · prepare @ wave186）  
2. 可选：未闭合块注释硬诊断（EOF depth>0）  
3. 可选 Windows hybrid（**需要时再通知重启**）  

```text
钉盘：3c88f7e51（未升）
tip：353be503c wave187 ensure_std_net_o_auto_tls pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：其它 rest R2 / mega residual（常驻 Cap）
```
