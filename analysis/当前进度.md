# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`cf07b0d9`**（双端 L4 真冷 + 129；未升） |
| tip | **R2 runtime_io_abi Cap residual pure**（日常 L2） |
| 产品 bstrict | **129** · 钉盘 tip L4 仍绿；本波未重跑 tip L4 |
| 本波性质 | **工程轨 R2 pure**（skill §3.3 日常真测 L2） |
| Ubuntu | ✅ L2 g05 PREFER + 矩阵 + pure-asm rv42 @ `72bb1963` · `/tmp/ubuntu_g05_rio_pure_72bb1963.log` |
| mac | ✅ L2 g05 PREFER + 矩阵 + pure-asm rv42 @ `e18e801d` |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `runtime_io_abi` Cap residual pure**  
   - 真迁：`shux_fs_open_write_flags_impl`（cfg O_*）/ `shux_write_path_bytes_impl` /  
     `runtime_release_file_view_impl` / `runtime_read_file_view_impl`（lseek+malloc）  
   - seed：`#ifndef SHUX_RUNTIME_IO_ABI_FROM_X`；PREFER rest **H=0**（仅 slice_marker）  
   - `fs_posix_*`：`#[no_mangle] export function`（去掉 `extern "C"` 定义，避免模块前缀）  
   - Ubuntu：nostdlib 补 `lseek`（`bootstrap_nostdlib_stubs` · SYS_lseek=8）  
   - 构建级别：**L2**（非 tip L4；钉盘仍 `cf07b0d9`）

## 下一波前排

1. **可选** 其它 rest R2（driver_abi / fmt_check / diagnostic Cap residual；dispatch 产品 H=0）  
2. **可选** 整波收口 tip L4 升钉（多波 pure 后）  
3. **可选** g05 sed；Darwin CG002；Windows hybrid  

```text
钉盘：cf07b0d9（tip L4 双端 129，未升）
tip：R2 runtime_io_abi Cap residual pure（L2）
Windows：本波不需要切换
下一：其它 rest / tip L4 升钉 / g05 sed / Windows / Darwin CG002
```
