# X 语言 / X language 当前进度（2026-07-27 · wave521）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`7db9c47c6`**（双端 L2，wave521；L4 待整波收口） |
| 钉盘 | **`2bc0885d3`**（待 Ubuntu 钉盘升级） |
| bstrict | **129** |

## wave521（已闭 · runtime_process_argv Makefile R2 thin+rest 补全 双端 L2）

| 项 | 状态 |
|----|------|
| 触发 | 审计发现 6 个"半残"模块：.x wrapper + .from_x.c guard 已就绪但 Makefile 规则仍是冷路径单行编译 |
| 模块 | `runtime_process_argv`（168 行；codegen 入口 argc/argv 全局 + CRT 绑定 + constructor + getters + weak fallback） |
| 债 | `.x` 已有 `#[no_mangle] xlang_process_argv_bind_from_crt` wrapper（调 _impl）；`.from_x.c` 已有 `#ifndef XLANG_RUNTIME_PROCESS_ARGV_FROM_X` guard 包裹 wrapper + `xlang_process_argv_bind_from_crt_impl` 始终编译；**Makefile 规则未跟上**——仍是冷路径单行编译，R2 路径未启用 |
| 根修 | 补全 Makefile 规则为 thin+rest `ld -r` 模式（`XLANG_G05_PREFER_X_O=1` 时 thin .x + rest .from_x.c 合并） |
| 构建模式 | **thin+rest**——thin (.x) 提供 `#[no_mangle] xlang_process_argv_bind_from_crt` 公开符号；rest (.from_x.c) 提供 `_impl`（CRT 绑定逻辑含 `__APPLE__`/`__linux__` 分支）+ `__attribute__((constructor))` + getters + weak fallback（均为 C-only 构造，始终编译） |
| nm 一致性 macOS | cold vs R2：6 核心 T 符号对齐（process_arg_c/process_args_count_c/process_xlang_argc_get/process_xlang_argv_get/xlang_process_argv_bind_from_crt/xlang_process_argv_bind_from_crt_impl），仅 R2 多 `_doc_anchor` |
| nm 一致性 Ubuntu | cold vs R2：核心 T 符号对齐（process_xlang_argc_get/process_xlang_argv_get/xlang_process_argv_bind_from_crt/xlang_process_argv_bind_from_crt_impl；weak 符号 process_arg_c/process_args_count_c 在 Ubuntu 显示为 W 而非 T） |
| 产品矩阵 macOS | g-ffi-5-std-wrap ✅ · crypto 全链路 ✅（含 ed25519） |
| 产品矩阵 Ubuntu | g-ffi-5-std-wrap ✅ · crypto 全链路 ✅ |
| 已知 pre-existing | process 测试失败（`_link_abi_getenv` 未定义，属于 runtime_link_abi_user_env 模块问题，clean tip 也失败，与 argv 无关） |
| 验证级别 | **L2 日常真测**（按自举方法 §0.2 节奏；非每微 commit 全量 L4） |
| PLATFORM | SHARED — constructor/getters 可移植；CRT 绑定含 `__APPLE__`/`__linux__` 分支已在 _impl 中 |
| 关键发现 | **"半残"模块模式**：审计发现 6 个模块（runtime_process_argv / runtime_tls_mbedtls_bio / runtime_net_udp_batch / runtime_arrow_simd_glue / runtime_crypto_inc_glue / runtime_panic）的 .x wrapper + .from_x.c guard 已就绪但 Makefile 规则未跟上。这类只需补全 Makefile thin+rest `ld -r` 规则即可启用 R2 路径，无需改 .x/.from_x.c 代码。后续 wave 可批量补全剩余 5 个 |
