# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **pipeline_abi wave81 pure residual** @ `6e0796c6` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **R2 pipeline_abi Cap residual pure wave81**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + pure-asm 矩阵 rv42 / option / stdlib-import；wave81 pure **W**（`shux_preprocess` / `quiet` / `with_path`）@ `6e0796c6`；hello pure-asm BLD001 `std_fmt_*` ensure tip 既有 |
| mac | ✅ L2 PREFER hybrid + c-backend 矩阵（Darwin pure-asm CG002 tip 既有）@ `6e0796c6` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave81** @ `6e0796c6`（本日产品 tip）  
   - **迁 pure soft residual 叶**：`shux_preprocess` / `shux_preprocess_quiet` / `shux_preprocess_with_path` thin → G.7 pure `shux_preprocess_raw_to_malloc_impl`（产品 X-pipeline 路径；`source_len==0` → `pipe_cstr_len` ≡ seed `strlen`）  
   - seed 冷孪生 `#ifndef FROM_X`（保留 LEGACY `preprocess_c_fallback`）  
   - **22 日已闭**：wave76 cstr_offset · wave77 typeck_dep BSS · wave78 soft FILE · wave79 realpath · wave80 product_emit · **wave81 preprocess 公共面**  
   - **Cap residual 仍**：fn-ptr / typeck_module C 前端 / `pipeline_sizeof_*` / **preprocess 引擎**（`preprocess_define_*` / `preprocess_x_buf`）  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave81.log` · `/tmp/ubuntu_g05_wave81.log` · `/tmp/mac_wave81_matrix.log` · `/tmp/ubuntu_wave81_matrix.log`

## 下一波前排

1. rest R2：pipeline_abi 硬 Cap residual 叶（fn-ptr / typeck_module C 前端；sizeof / preprocess 引擎…）/ 整波 tip L4  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）；hello pure-asm `std_fmt` ensure  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave81 未升钉）
产品 tip：6e0796c6 pipeline_abi residual pure wave81 L2
文档：21 号归档 · 22 号起表 + wave76–81
Windows：本波不需要切换
下一：pipeline_abi 硬 Cap residual（fn-ptr / typeck_module / sizeof / preprocess 引擎）/ 整波 tip L4（需时再说）
```
