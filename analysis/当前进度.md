# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`86281cc6`**（双端 L4 真冷 + 129；未升） |
| tip | **R2 driver_abi Cap residual pure wave21**（fmt_argv 桥；日常 L2） |
| 产品 bstrict | **129** · tip L4 钉盘仍 `@86281cc6` |
| 本波性质 | **工程轨 R2 pure 微步**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42/option/si/hello + pure-asm rv42 @ `914a7658` |
| mac | ✅ L2 同；另验 `shux fmt` 路径 |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `runtime_driver_abi` Cap residual pure wave21（本波）**  
   - 真迁：`driver_entry_fmt_argv_slot`  
   - 手段：绕 *u8[2] / char*[2] lit init（XT001 史）——  
     BSS 字节 lit `"shux\0"` / `"fmt\0"` + 2× LP64 raw ptr 槽 +  
     首次调用 G.7 `shux_ptr_slot_set`；返回表基址（ABI = `char**` / opaque `*u8`）  
   - 模式同源：`fmt_check_cmd_thin` check_argv / wave16 work_p  
   - seed：cold C `static char*[2]` twin；`#ifndef SHUX_L2_RDABI_THIN_FROM_X` 剔 pure-dup  
   - 仍 seed：巨型表文本 `rt_preamble`；`*_lines_raw`；fputs；calloc/host_defaults；tmp 访问器  
   - 代码 SHA：**`914a7658`**（docs 本 commit 跟进）  
   - 矩阵：rv_build=0 / rv_run=42 / opt 0 / si 0 / hello 0 / pure-asm rv42  
   - 钉盘仍 **`86281cc6`**（skill §3.3：微步不自动 tip L4）

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文；**不**改 `当前问题分析.md` / `问题分析文档.md`

## 下一波前排

1. **可选** driver_abi 余 Cap residual（**fputs** / **calloc** / **host_defaults**）  
2. **可选** g05 sed / harness `-E` 评估  
3. **可选** Windows hybrid；Darwin arm64 纯 asm Vec4f CG002  

```text
钉盘：86281cc6（tip L4 双端 129）
tip：R2 driver_abi wave21 fmt_argv pure（L2 @ 914a7658）
Windows：本波不需要切换
下一：fputs / calloc·host_defaults / g05 sed / Windows / Darwin CG002
```
