# X 语言 / X language 当前进度（2026-07-29 · wave643 Cap compound `*T +=/-=` int · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`131c1d53b`**（wave643 产品 glue；typeck twin `5752e153c`） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave643（已闭 · Cap residual compound `*T +=/-=` int typeck · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `p += 1` / `p -= 1` typeck `expected *i32, found i32`；`p = p + 1` 已绿（wave285/642） |
| tip 产品 | **`131c1d53b`**（mega glue）+ **`5752e153c`**（typeck.x + seed） |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave643 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| compound `p += n` / `p -= n` | assign 把 RHS ambient 当 `*T` 并要求 `type_refs_equal` | ✅ RHS 为整数 offset；不 stamp 为 `*T`；emit 已 scale（wave642） |

**数据流**：产生 = compound ADD/SUB_ASSIGN · LHS `*T` · RHS 整数 → 存储 = 指针槽 + 整数 offset 类型 → 消费 = freestanding scale emit（wave642）。G.7：产品 mega 权威是 `pipeline_typeck_check_expr_assign_c`（**非**仅 typeck.x）；typeck.x + seed 同语义孪生。

**权威**：
1. **产品 mega**：`compiler/pipeline_glue.c` · `pipeline_typeck_check_expr_assign_c`
2. **孪生**：`compiler/src/typeck/typeck.x` + `compiler/seeds/typeck_gen.linux.x86_64.c`

### wave643 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `p += 1` *i32 | ✅ 42 | ✅ 42 host/pure/fs |
| `p -= 1` *i32 | ✅ 42 | ✅ 42 |
| `p += n` var | ✅ 42 | ✅ 42 |
| `p += 2` | ✅ 42 | ✅ 42 |
| `p = p + 1` 回归 | ✅ 42 | ✅ 42 |
| `p + 1` / `q-p` 回归 | ✅ | ✅ 42 / 2 |
| `p += q`（ptr+ptr） | ✅ 硬红 | ✅ 硬红 |
| int `i += 2` / compound-assign | ✅ | — |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |
| disasm fs `p+=1` | — | `imul $0x4` + add |

### 仍 soft

- untyped `self`（wave493 非标准）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶，或整波 L4 + 升钉  

## wave642（已闭 · Cap residual freestanding ptr±int / ptr-ptr C-scale · L2）

tip 产品 **`0f3be5125`**。freestanding C-scale `p±n` / `q-p`。

## wave641（已闭 · Cap residual freestanding `&(*p)` · L2）

tip 产品 **`1bc77f0bc`**。ADDR_OF of EXPR_DEREF 复用 lvalue_eff_addr。

## wave640（已闭 · Cap residual freestanding nested `**pp` · L2）

tip 产品 **`92c07bfaa`**。`&p` lea + DEREF TYPE_PTR load 8。

## wave639（已闭 · Cap residual freestanding DEREF FIELD INDEX · L2）

tip 产品 **`2b274afdc`**。freestanding `(*p).xs[i]` INDEX/call-arg。

## wave638（已闭 · Cap residual host-C FIELD base primary · L2）

tip 产品 **`b1a8c6cfc`**。host-C `((base).field)` 修 `(*p).v` BLD001。
