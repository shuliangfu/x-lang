# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave116** try_under_lib_roots pure @ `d381f08ba` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **其它 rest R2 · Cap residual pure 可迁叶**（mega link_abi pure-logic path join） |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + pure-asm option102/hello + prove labi_path_pure **IDENTICAL** |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + pure-asm option102/hello + prove labi_path_pure **IDENTICAL** |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：wave115 后 path join 可迁 pure-logic `shux_asm_ld_try_under_lib_roots`  
2. **根修**：pure 迁入 `labi_path_pure.x`（low-tag 守卫 + 纯字节 `root/rel` join + 扫 24；count 9→10）；Cap residual：`skip_missing` + `bank_push`  
3. **权威**：mega / cold seed twin `#ifndef SHUX_LABI_PATH_PURE_FROM_X`；surface re-pin prove IDENTICAL  
4. **坑**：多行 `export function` 签名 → parse 丢函数（`num_funcs` 缺 1、链接 UNDEF）；须单行签名  
5. **验收**：双端 L2 g05 + §4 矩阵 + pure-asm + prove **IDENTICAL**；钉盘仍 **`3c88f7e51`**

## 下一波前排

1. 其它 rest R2 可迁叶（mega pure-logic：string needs_* 等；常驻 Cap 不硬迁）  
2. **可选** Windows hybrid（**需要时再通知重启**）  
3. 其它工程 residual（g05 sed 防御层等，按债裁剪）  

```text
钉盘：3c88f7e51（未升）
tip：d381f08ba wave116 try_under_lib_roots pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：其它 rest R2 可迁叶 / 可选 Windows
```
