# X 语言 / X language 当前进度（2026-07-24）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **重命名分析**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)  
> **改名前锚点**：`anchor-pre-xlang-rename` @ `d07bcf155`（父钉曾 `a77fc5d90`）  
> **项目目录**：现行 `…/xlang/x-lang`（mac/Ubuntu/Windows 同构）  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only**（第三品牌 `shu_*` / `SHU_*` / `shux` 已清） |
| 主域 | **xlang.dev** |
| 工程 | **全库重命名 R0–R3 已闭** · 双端 tip L4 **129 全绿** · 钉盘 **`75c638465`** |
| process_xlang 兼容别名 | **已删除** |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **已恢复** · **wave299** Cap residual pure 已闭（u64/i64→f32 REX.W `cvtsi2ss`） |

## 验收

### tip L4 钉盘（仍有效）

| 项 | 状态 |
|----|------|
| SHA | **`75c638465`**（wave288：invoke_cc push `runtime_link_abi_user_env.o`） |
| 构建级别 | **L4 真冷**（全擦 compiler/std/core `.o` + 重链产品二进制） |
| remaining_o 起编 | ✅ **0**（双端） |
| mac 全量 bstrict | ✅ **OK (129)** · `/tmp/mac_true_cold_75c638465.log` · `/tmp/mac_true_bstrict_75c638465.log` |
| Ubuntu 全量 bstrict | ✅ **OK (129)** · `/tmp/ubuntu_true_cold_75c638465.log` · `/tmp/ubuntu_true_bstrict_75c638465.log` |
| 前钉 | `3698e557f`（R3 重命名 tip L4；wave238–287 仅 L2） |

### 本波 tip（wave299 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`e791b7e20`** — R2 Cap residual pure wave299（u64/i64→f32 REX.W `cvtsi2ss`） |
| 构建级别 | **L2**（rebuild enc + `pipeline_x.o`/glue + g05 + 探针 + 产品矩阵；**非 tip L4**） |
| 根因 | EXPR_AS TYPE_F32 仅 i32/u32 走 `cvtsi2ss`；u64/i64/usize/isize 再发射整数位型或 32-bit form → Ubuntu freestanding **run=0**（mac host-gcc 可藏；对标 wave295 u64→f64） |
| 修法 | G.7：新 `backend_enc_cvtsi2ss_eax_from_i64_arch`（seed + thin/full `.x` + surface 同 commit；thin first4 u32 `705644787`=`F3 48 0F 2A`）+ glue kinds 4–7 |
| 探针 | Ubuntu freestanding：`(u64 as f32) as i32`=6 · `(i64 as f32) as i32`=6 · u64→f32×`let c:f32=7.0`=42 · disasm `cvtsi2ss %rax,%xmm0` · u64→f64/f32 mul 回归 |
| mac 矩阵 | ✅ rv=42 · option/si/hello · host cast=42 · `/tmp/mac_g05_wave299.log` · `/tmp/mac_w299_matrix.log` |
| Ubuntu 矩阵 | ✅ rv=42 · option/si/hello · freestanding 绿 · `/tmp/ubuntu_g05_wave299.log` · `/tmp/ubuntu_w299_probes.log` |
| 产品钉盘 | **仍 `75c638465`**（日常 L2 不升钉） |

```text
下一：wave300 Cap residual
  leave-off：
   · AST name/var_name 升布局（仍 cap 63；L011/L012 诚实失败）
   · `*u8 + *u8` 运行时字符串拼接运算符
   · host residual：getenv/system/realpath _impl + resolve pool；freestanding stubs
   · u64→f32/f64 当值 >2^63-1 的无符号转换序列（ISA 为 signed i64 convert）
   · soft：`b * 7.0 as f32` freestanding binop 内嵌 float-lit `as f32` 发射（mulss 吃 f64 低 32；`let c:f32=7.0` 路径绿）
  钉盘 75c638465；日常 L2，整波/日终再 L4
```
