# X 语言 / X language 当前进度（2026-07-29 · wave657 Cap aggregate cmp · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 05:59 · 28 全日（wave534–617）已归楼；本文仅保留 29 日 tip 快照

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`315018523`**（wave657 aggregate cmp hard-fail） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave657（已闭 · Cap residual aggregate ==/!=/relational hard-fail · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `struct`/`slice` `a == b` → host-cc BLD001；`T[N]` `a == b` 静默指针身份假绿（exit 0）；enum/ptr/scalar 本绿 |
| tip 产品 | **`315018523`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave657 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| binop_cmp 不检操作数 kind，恒 stamp bool | `typeck_check_expr_binop_cmp` | ✅ 硬拒 ARRAY/SLICE/LINEAR/VECTOR + struct TYPE_NAMED |

**数据流**：产生 = typeck cmp 无聚合门控。存储 = resolved bool。消费 = host-C 发 `a==b`（struct/slice 非法 C；array 衰变指针身份）。G.7：扩 `typeck_check_expr_binop_cmp` + `typeck_type_is_aggregate_cmp_operand`；diag `invalid_aggregate_cmp`；enum/alias/ptr/scalar/`a.x == b.x` 仍绿。

**权威**：`compiler/src/typeck/typeck.x` · seed `typeck_gen.linux.x86_64.c` + empty_surface · `runtime_driver_diagnostic{,_thin}.x` + seeds 同 commit

### wave657 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| struct/slice/array `==`/`!=`/`<` | ✅ T001 hard | ✅ T001 hard |
| enum / alias / ptr / i32 / f32 / field `==` | ✅ 42 | ✅ 42 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

日志：`/tmp/mac_g05_wave657.log` · `/tmp/ubuntu_g05_wave657.log` · `/tmp/ubuntu_w657_probes2.log`

### 仍 soft

- untyped `self`（wave493 非标准 · **跳过**）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self），或整波 L4 + 升钉  

## wave656（已闭 · Cap residual onefunc bare return ASI + Cap-T001 · L2）

tip 产品 **`d364ce605`**。onefunc return ASI + Cap-T001。

## wave655（已闭 · Cap residual mid-block return + more stmts · L2）

tip 产品 **`027c42a49`**。parse_block return ASI。

## wave654（已闭 · Cap residual bare break/continue ASI + IDENT · L2）

tip 产品 **`e6e56f13d`**。ASI 后继补 IDENT/control heads。
