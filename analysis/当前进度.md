# X 语言 / X language 当前进度（2026-07-28 · wave601 Cap SysV MEMORY by-value call-arg · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`58431a7fb`**（wave601 Cap SysV MEMORY by-value call-arg · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave601（已闭 · Cap residual freestanding >16B MEMORY by-value CALL · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Ubuntu pure-asm `sum(mk())` / `sum(s)` 24B+ SEGV 139 / 错值；callee `param_home` 读 `[rbp+0x10..]`，caller 却把指针塞进 `rdi` |
| tip 产品 | **`58431a7fb`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave601 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| CALL 路径 `sz>16` 当 1 GP（lea/指针） | `glue_sysv_arg_gp_units_from_size` + slot/n_stack | ✅ MEMORY → 0 GP + 多字栈 |
| 栈 push 只 `push rax` 一次 | f32_xmm + legacy CALL | ✅ `push_sysv_memory` 多 qword |
| push 仅 VAR | `pipeline_asm_push_sysv_memory_by_value` | ✅ + CALL/METHOD sret 物化再 push |
| cleanup 只按 `nargs-6` | `glue_asm_emit_call_with_cleanup` | ✅ `n_stack` 字数 + pad |

**数据流（五问）**：产生 = CALL 实参 MEMORY 打包；存储 = outgoing 栈；消费 = 形参 home（wave599 MEMORY copy）。权威：G.7 补全 CALL 路径对齐 method import 已有 MEMORY push（不新开第二套）。

**权威**：`compiler/seeds/backend_call_dispatch.from_x.c` + `compiler/pipeline_glue.c`（`push_sysv_memory`）。改后 rm `backend_call_dispatch.o` + `pipeline_glue_standalone.o` + g05。

### wave601 验证

| 探针 | mac L2 host/fs | Ubuntu L2 pure/fs |
|------|----------------|-------------------|
| p16 dual-GP 回归 | ✅ 42/42 | ✅ 42/42 |
| p24 `sum(mk())` | ✅ 42/42 | ✅ 42/42（was 139） |
| p24_var `sum(s)` | ✅ 42/42 | ✅ 42/42 |
| p24_mix `f(n,mk(),m)` | ✅ 42/42 | ✅ 42/42 |
| p32 32B MEMORY | ✅ 42/42 | ✅ 42/42 |
| var_dual / take2b 回归 | ✅ 42/41 | ✅ 42/41 |
| method_impl / method_mem | ✅ 42/42 | ❌ soft 220/222（UFCS self 路径） |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/0/0 |

### 仍 soft

- Ubuntu pure-asm **method/UFCS** dual-GP / MEMORY `self`（method_impl/method_mem 错值；mac 绿）  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual soft 叶（method dual-GP / method MEMORY self），或整波双端 L4 + 升钉  
2. 整波双端 L4 + 升钉  

## wave600（已闭 · Cap residual arm64 multi dual-GP call-arg）

tip `b4a9519bd` / docs `101c977af`。soft「>16B MEMORY 形参」本波由 **wave601** 收（free CALL）。
