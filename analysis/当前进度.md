# Shux 当前进度（2026-07-20）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **归档**：[自举进度-归档-2026-07-19.md](自举进度-归档-2026-07-19.md) · [18 号](自举进度-归档-2026-07-18.md) · [17 号](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`1d41e903`**（C7 plan 瘦身 8/8 收口：3 合并 + 1 删除 + 4 纳入白名单 125→129） |
| 产品 L4 放行钉盘 | **`deaf773b`**（20 日升钉；前钉 `305cfbe1`；mac+Ubuntu 双端 L4 真冷 + 125 全绿 + Windows hybrid 门禁首次全绿 → 升钉） |
| 产品 bstrict 套件 | **125**（钉盘时 · 当前白名单 129 待 shux_asm 构建 L4 验证） |
| 本波性质 | **C7 plan 瘦身 8/8 收口**（3 合并 + 1 删除 + 4 纳入白名单 125→129 · 避免爆内存未跑 L4） |
| 本波输出 | C7 plan 瘦身 8/8 全部完成；2 个脚本合并（run-fmt-unary-gate→run-fmt-cmd / run-std-debug→run-debug）；2 个脚本删除；4 个边界 case 脚本纳入白名单；2 个副产品 bug 已在前波修复（std.debug.println 返回值 / fmt 一元负号） |

## 本波摘要（2026-07-20 · C7 plan 瘦身 8/8 收口 · 1 commit `1d41e903` · 未跑 L4 避免爆内存）

| 项 | 结果 |
|----|------|
| **入口** | [C7-plan-audit.md](C7-plan-audit.md) §0 摘要：752 非白名单中 8 个完全无引用 dead code 候选；用户选择路径 C（重构合并）+ 路径 A（纳入白名单）混合策略 |
| **路径 C 合并**（2 个） | ① `run-fmt-unary-gate.sh`（83 行）合并入 `run-fmt-cmd.sh`：unary 测试段加旧种子检测（检测到 `return - i` 时 SKIP，等 LEGACY 重链接后启用）；② `run-std-debug.sh`（12 行）合并入 `run-debug.sh`：std.debug stderr 测试段 |
| **删除**（2 个） | `run-fmt-unary-gate.sh` / `run-std-debug.sh`（已合并到对应白名单脚本） |
| **路径 A 白名单扩容**（4 个 · 125 → 129） | ① `run-perf-simd-shuxffle-select.sh`（SIMD shuffle/select perf，需 shux_asm）② `run-std-simd-shuxffle-select-gate.sh`（std SIMD gate，无 shux_asm 时 SKIP exit 0）③ `run-std-io-context-gate.sh`（std.io context gate，需 shux_asm 或更新 shux-c）④ `run-std-net-context-gate.sh`（std.net context gate，需 shux_asm 或更新 shux-c） |
| **副产品修复**（前波已 commit） | ① `std.debug.println` 返回值语义：从字节数改为 0/-1（std.fmt 作为唯一格式化输出权威，std.debug 复用 std.fmt 输出到 stderr）；② `fmt` 命令一元负号解析：`runtime_lsp_glue.from_x.c` 的 `lsp_format_emit_segment` 添加 `is_return` 上下文识别，`return -i` 不再被拆开为 `return - i` |
| **修复状态** | std.debug.println 已端到端验证；fmt 一元负号源码已修复，端到端验证待 LEGACY 重链接（`bootstrap_shuxc` 是 7月16日旧种子不含此修复，`run-fmt-cmd.sh` 检测旧种子时条件性 SKIP unary 测试） |
| **本机验证** | `SHUX_SKIP_SUBSCRIPT_MAKE=1 SHUX=./compiler/shux-c ./tests/run-debug.sh` exit=0（std.debug test OK）；`SHUX_SKIP_SUBSCRIPT_MAKE=1 SHUX=./compiler/shux-c RUN_ALL_USE_C=1 ./tests/run-fmt-cmd.sh` exit=0（fmt cmd test OK · unary skipped: legacy seed） |
| **构建级别** | L2（本机单脚本验证，未跑 L4 避免爆内存） |
| **未跑 L4 理由** | L4 真冷需要构建 `shux_asm`（触发 `shux-x -E` 爆内存 ~5GB）；用户明确要求"不能爆 CPU 和内存"；4 个新加白名单脚本需要 shux_asm 才能验证；待下次需要 L4 时一并验证 |

## 下一波前排（按优先级排序）

### 1. C7 L4 验证（待 shux_asm 构建后验证 129 白名单 · 当前避免爆内存未跑）

- **C7 plan 瘦身 8/8 完成**（3 合并 + 1 删除 + 4 纳入白名单 125→129）：[C7-plan-audit.md](C7-plan-audit.md) §5 实际执行结果
- **待验证项**（需 shux_asm，避免爆内存未构建）：
  - 4 个新加白名单脚本（run-perf-simd-shuxffle-select.sh / run-std-simd-shuxffle-select-gate.sh / run-std-io-context-gate.sh / run-std-net-context-gate.sh）在 shux_asm 存在时能否通过
  - fmt 一元负号修复在 LEGACY 重链接后的端到端验证（`run-fmt-cmd.sh` unary 测试不再 SKIP）
- **验证命令**：构建 shux_asm 后 `./tests/run-all-bstrict.sh`（应见 `run-all-bstrict OK (129 scripts)`）
- **触发条件**：下次需要 L4 真冷验证时（如新升钉、Cap 收口），一并跑 C7 L4 验证

### 2. 下一个 Cap 候选（短波 Cap 已全部完成 · 剩余均为远期大工程）

cap-candidates-2026-07-19.md 中 C1-C8 短波 Cap 已全部收口：
- C1 ✅ build error exit code 传播（`a74e25a1` 已修）
- C2 ✅ std.net PRIMARY → UNDEF gate
- C3 ✅ parser struct-lit P1
- C4 ✅ unsafe-block bug P1
- C5 ✅ typeck CTFE fold 扩展（多波）
- C6 ✅ 波 1 + 波 2（波 3 远期顺延自举后）
- C7 ✅ plan 瘦身 8/8（本波 `1d41e903` 收口）
- C8 ✅ fk0 GAP std.vec/set/map

**剩余候选均为远期大工程**（需用户确认是否立项）：
- C5 EXPR_CLOSURE CTFE（独立大工程 · ~7.5 波 · 顺延自举后立项 `lang-closure-v1-rfc.md`）
- C6 X ABI P0b 波 3（std/ ~557 个 C 库包装分文件标注 `extern "C"` · 多波 · 顺延自举后 P1-P2）
- Cap C9 match guard full-chain 支持（6 个产生点改动 · 多波 · 先排期再动手）
- shux-x 5GB 方案 B（ArenaSidecar release API · 远期 · 非 KPI）

### 3. Windows L4 真冷测（可选 · 非必需）

- 非必需（双端门禁已满足），但若用户希望 Windows 也达到 L4 冷测级别可补跑
- 命令：`ssh windows-server` → 全擦 .o + 删二进制 + `make bootstrap-driver-seed SHUX_LEGACY_C_FRONTEND=1` + `SHUX_WIN_BSTRICT=1 ./tests/run-bootstrap-bstrict-windows-gate.sh`

## 不在前排

- **C6 X ABI P0b 波 3：std/ 扩展标注**（远期 · 多波 · 顺延自举后 P1-P2）：~557 个 C 库包装分文件标注 `extern "C"`
- **shux-x 5GB 方案 B**（远期 · 非 KPI）：ArenaSidecar release API
- **Cap C9 match guard full-chain 支持**（独立大工程 · 多波）：6 个产生点改动，先排期再动手
- **C5 EXPR_CLOSURE CTFE**（独立大工程 · 多波）：已 scoping，顺延自举后立项 `lang-closure-v1-rfc.md`
- **历史进度**：Windows 门禁收口（`deaf773b` 升钉）/ C6 波 2 ABI 一致性（`305cfbe1` 升钉）/ C6 波 1 unsafe 包裹（`4214eea4` 升钉）/ Windows shux-c exit=1 根因修复（`d25b82cc`，mac+Ubuntu L4 全绿）/ C6 P0b audit（`7e507fb6` docs-only）/ shux-x 5GB 方案 A（`a14a1dd0`）/ C7 plan 瘦身 PoC（`e546b013`+`bedb40ca`）/ C7 plan 瘦身 8/8 收口（`1d41e903` 125→129 待 L4 验证）/ CLI 优化（Phase 1 + help 美化 8 commit）/ std.print 架构优化 / C5 三连击（`c174e70a`）/ 4 波 C5 CTFE / Windows 时序决议 / C2/C3=C4/backtrace/C8 钉盘波，全部归档在 [自举进度.md](自举进度.md) §6 变更记录

```text
本波 tip：1d41e903 (C7 plan 瘦身 8/8 收口：3 合并 + 1 删除 + 4 纳入白名单 125→129)
本波性质：C7 plan 瘦身 8/8 收口（避免爆内存未跑 L4）
本波输出：2 脚本合并 + 2 脚本删除 + 4 脚本纳入白名单（125→129）；2 个副产品 bug 已前波修复
构建级别：L2（本机单脚本验证）；未跑 L4 避免爆内存（shux_asm 构建触发 shux-x -E 爆内存 ~5GB）
产品 tip L4 钉盘：deaf773b（20 日升钉；前钉 305cfbe1；双端 L4 真冷 + 125 全绿 + Windows hybrid 门禁首次全绿）

C7 plan 瘦身 8/8 收口（路径 C + 路径 A 混合）：
  路径 C（合并）：
    run-fmt-unary-gate.sh → run-fmt-cmd.sh（含旧种子检测 SKIP）
    run-std-debug.sh → run-debug.sh（std.debug test OK）
  删除（已合并）：
    run-fmt-unary-gate.sh
    run-std-debug.sh
  路径 A（白名单扩容 125 → 129）：
    run-perf-simd-shuxffle-select.sh
    run-std-simd-shuxffle-select-gate.sh
    run-std-io-context-gate.sh
    run-std-net-context-gate.sh

副产品 bug 修复（前波已 commit）：
  std.debug.println 返回值 0/-1（std.fmt 唯一格式化权威 + std.debug 复用输出 stderr）
  fmt 一元负号解析（runtime_lsp_glue.from_x.c is_return 上下文识别）
  端到端验证：std.debug.println 已过；fmt 一元负号待 LEGACY 重链接

待 L4 验证（避免爆内存未跑）：
  4 个新加白名单脚本在 shux_asm 下能否通过
  fmt 一元负号修复 LEGACY 重链接后端到端验证

短波 Cap 全部收口（C1-C8）：
  C1 ✅ a74e25a1（build error exit code）
  C2 ✅ std.net PRIMARY → UNDEF gate
  C3 ✅ parser struct-lit P1
  C4 ✅ unsafe-block bug P1
  C5 ✅ typeck CTFE fold 扩展（多波）
  C6 ✅ 波 1 + 波 2（波 3 远期）
  C7 ✅ 1d41e903（本波收口 · 8/8）
  C8 ✅ fk0 GAP std.vec/set/map

剩余均为远期大工程（需用户确认立项）：
  C5 EXPR_CLOSURE CTFE（~7.5 波 · 顺延自举后）
  C6 X ABI P0b 波 3（std/ ~557 个标注 · 多波 · 顺延自举后）
  Cap C9 match guard full-chain（6 产生点 · 多波 · 先排期）
  shux-x 5GB 方案 B（ArenaSidecar release API · 非 KPI）

权威文档：analysis/C7-plan-audit.md（C7 plan 瘦身 audit · 8/8 实际执行结果）
权威文档：analysis/问题分析文档.md（codex 重写，§9 测试结果 + 验证方法 + 6-commit 修复 + 最终真根因详解）
权威文档：analysis/Windows平台限制与测试指南.md（13 硬限制 + 12 已修复 + 5 未解决 + 测试方法 + 跨平台对比表）
权威文档：analysis/SHUX 命令行.md（CLI 子命令化改造 13 节分析，自举后工程优化）
历史进度归档：analysis/自举进度.md §6 变更记录
```
