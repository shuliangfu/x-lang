# X 语言 / X language 当前进度（2026-07-29 · wave647 Cap freestanding []f32 call-arg · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`6e501513d`**（wave647 freestanding `[]f32` ARRAY_LIT call-arg f32 pack） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave647（已闭 · Cap residual freestanding `[]f32` call-arg · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Ubuntu freestanding `take([1.0, 2.0])` as `[]f32` host=42、fs=0；`let a` 后 take 绿；length=2 对；mac CTFE 常 `mov #42` 假绿 |
| tip 产品 | **`6e501513d`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave647 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| freestanding `[]f32` bare ARRAY_LIT call-arg 载荷 0 | durable/force_esz fill 发 FLOAT_LIT 为 f64 位，store esz=4 只写低 32（f64 1.0 低半=0） | ✅ force_ty f32 / force_esz==4 包 f32 位 |

**数据流**：产生 = `glue_asm_emit_array_lit_durable_ptr_rax_elf_c` / `pipeline_asm_emit_array_lit_force_esz_elf_c` 填 COMMON/栈 · 存储 = Lxlang_al / 帧 · 消费 = INDEX f32 load + addss。host-C 复合字面量绿；wave622 force_esz 只修整数宽，FLOAT 仍走 rec f64。G.7：`glue_array_lit_emit_scalar_elem_to_rax_elf_c` 复用 `glue_emit_float_lit`（wave300）+ peel SLICE/ARRAY。

**权威**：`compiler/pipeline_glue.c` · `glue_array_lit_emit_scalar_elem_to_rax_elf_c` + durable/force_esz 填环

### wave647 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `take([1.0,2.0])` []f32 | ✅ host/fs=42 | ✅ 42（was fs=0） |
| let→take / s[0]/s[1] | ✅ 42 | ✅ 42 |
| `sum([1.0,2.0]): f32` | ✅ 42 | ✅ 42 |
| []f64 / []i32 / []i64 call | ✅ 42 | ✅ 42 |
| wave646 `~` / wave645 `%=` | ✅ 42/2 | ✅ 42/2 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |
| disasm fs f32 lit | （CTFE 可 fold） | `mov $0x3f800000` + `0x40000000` |

### 仍 soft

- untyped `self`（wave493 非标准）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶，或整波 L4 + 升钉  

## wave646（已闭 · Cap residual freestanding unary `~` / `-` · L2）

tip 产品 **`c0067fb97`**。freestanding unary `~`/`-` GP canonicalize。

## wave645（已闭 · Cap residual arm64 freestanding rem `%` / `%=` · L2）

tip 产品 **`fd7782ec8`**。arm64 freestanding rem sdiv+msub。
