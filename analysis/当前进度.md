# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`1f73ed275`**（双端 tip L4 真冷 + 129；未升 — 本波日常 L2） |
| tip 产品代码 | **pipeline_abi wave86 preprocess #if stack pure** @ `128092729` |
| 产品 bstrict | **129** · 钉盘 tip L4 全绿；wave86 **日常 L2** 矩阵绿（未全量 bstrict） |
| 本波性质 | **wave86 preprocess if_stack pure residual**（硬 Cap residual 再收一叶） |
| Ubuntu | ✅ L2 g05 hybrid pure **W**（if_stack_*）+ 矩阵 rv42/option/si/hello + nest #if @ `128092729` · `/tmp/ubuntu_g05_wave86.log` |
| mac | ✅ L2 g05 hybrid pure **weak**（if_stack_* 入链 pure BSS）+ 矩阵 rv42/option/si/hello + nest #if @ `128092729` · `/tmp/mac_g05_wave86.log` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave86** @ 产品 **`128092729`**  
   - `preprocess_if_stack_reset` / `len` / `push` / `pop` / `at` / `set_at` 真迁 pure 固定 `i32[32]` BSS  
   - G.7 单权威：`g_pipe_pp_if_stack` + `g_pipe_pp_if_n`（历史 GrowVec 前固定栈容量 32）  
   - `ast_pool.c` 同名 GrowVec 体改 **SHUX_WEAK** 冷 fallback（pipeline_x / standalone 嵌入）  
   - 闭合 Cap residual「preprocess #if stack」叶  
   - Cap residual 仍：typeck_module C 前端 / preprocess_x_buf（g05 仍持 &fn cast）  
   - **构建级别 L2**（日常真测；非 tip L4）  
2. 继承 tip L4 钉盘 **`1f73ed275`**（wave76–81 收口；wave82–86 未升钉）

## 下一波前排

1. rest R2：pipeline_abi **硬 Cap residual** 叶（typeck_module C 前端；preprocess_x_buf…）  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 硬 Cap / soft 再有整波收口时再 tip L4（非每微步）

```text
钉盘：1f73ed275（tip L4 双端 129；本波未升）
产品 tip：128092729 pipeline_abi residual pure wave86 if_stack
文档：21 号归档 · 22 号 wave76–86 + tip L4 升钉 @1f73ed275
Windows：本波不需要切换
下一：pipeline_abi 硬 Cap residual（typeck_module / preprocess_x_buf）
```
