# X 语言 / X language 当前进度（2026-07-27 · wave526）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`63a7778d7`**（双端 L2，wave526；L4 待整波收口） |
| 钉盘 | **`2bc0885d3`**（待 Ubuntu 钉盘升级） |
| bstrict | **129** |

## wave526（已闭 · runtime_panic Makefile R2 thin+rest 补全（3 平台分支）双端 L2）

| 项 | 状态 |
|----|------|
| 触发 | wave521 发现的 6 个"半残"模块之最后一个；3 平台分支需要分别补 R2 规则 |
| 模块 | `runtime_panic`（140 行 plain + 111 行 arm64；panic + crash evidence + weak fallback；user-domain host residual twin of product link_abi_getenv face） |
| 债 | `.x` 已有 1 个 `#[no_mangle]` wrapper（`xlang_crash_evidence_minimal` 纯转发到 `_impl`）；`.from_x.c` 已有 guard + _impl 实现；**Makefile 规则未跟上** |
| 根修 | 补全 Makefile 规则为 3 平台分支的 thin+rest `ld -r` 模式：① Linux x86_64 .s twin（不变，不涉及 R2）② Linux x86_64 C 变体（无 .s）→ thin (.x) + rest (.from_x.c)，guard `XLANG_RUNTIME_PANIC_FROM_X` ③ Linux arm64 → thin (runtime_panic_arm64.x) + rest (arm64 .from_x.c)，guard `XLANG_RUNTIME_PANIC_ARM64_FROM_X` ④ macOS/其他 → thin (.x) + rest (.from_x.c)，guard `XLANG_RUNTIME_PANIC_FROM_X` |
| 构建模式 | **thin+rest（3 C 变体分支）**——thin (.x) 提供 1 个 `#[no_mangle]` 纯转发 wrapper（`xlang_crash_evidence_minimal`）；rest (.from_x.c) 提供 _impl（crash evidence 收集 + getenv）+ `xlang_panic_` + weak fallback（始终编译） |
| nm 一致性 macOS | cold vs R2：7 核心 T 符号对齐（link_abi_getenv/impl + xlang_crash_evidence_minimal/impl/collect_c + xlang_panic_ + io_register_buffers_buf_c），仅 R2 多 `_doc_anchor` |
| nm 一致性 Ubuntu | Ubuntu x86_64 用 .s 汇编 twin（`xlang_panic_` 符号），R2 thin+rest 不适用（.s 优先） |
| 产品矩阵 macOS | g-ffi-5-std-wrap ✅ · crypto 全链路 ✅ |
| 产品矩阵 Ubuntu | g-ffi-5-std-wrap ✅ · crypto 全链路 ✅ |
| 验证级别 | **L2 日常真测**（按自举方法 §0.2 节奏；非每微 commit 全量 L4） |
| PLATFORM | SHARED — user-domain host residual twin of product face；3 平台分支（Linux x86_64 .s/C，Linux arm64，macOS） |
| 关键发现 | **arm64 变体的独立 guard**：arm64 .from_x.c 用 `XLANG_RUNTIME_PANIC_ARM64_FROM_X`（非 plain 的 `XLANG_RUNTIME_PANIC_FROM_X`）。用错 guard 会导致 `xlang_crash_evidence_minimal` 重复符号（thin + rest 都发射）。3 平台分支需要分别处理，每个分支用对应的 guard 名 |

## wave521-526 "半残"模块批量补全总结

wave521 审计发现 6 个"半残"模块（`.x` wrapper + `.from_x.c` guard 已就绪但 Makefile 规则未跟上），wave521-526 逐一补全：

| wave | 模块 | 发现的 pre-existing bug |
|------|------|-------------------------|
| wave521 | runtime_process_argv | 无（纯 Makefile 补全） |
| wave522 | runtime_tls_mbedtls_bio | 无（mbedtls include fallback 模式） |
| wave523 | runtime_net_udp_batch | **`__GLIBC__` guard bug**（#if 在 #include 前，TU 恒为空） |
| wave524 | runtime_arrow_simd_glue | **undeclared `len` bug**（变量名 length vs len，8 编译错误） |
| wave525 | runtime_crypto_inc_glue | **xlang-c backend ABI bug**（非标准 calling convention，C → .x segfault） |
| wave526 | runtime_panic | 无（3 平台分支 + arm64 独立 guard） |

**审计价值**：6 个模块中 3 个有 pre-existing bug（wave523/524/525），说明"半残"模块审计不仅补全 Makefile 规则，还暴露并修复了隐藏的编译错误和 backend ABI bug。这些 bug 之前被 Makefile 冷路径失败或 weak fallback 掩盖，从未被发现。
