# X 语言 / X language 当前进度（2026-07-29 · wave646 Cap freestanding unary ~/- · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`c0067fb97`**（wave646 freestanding unary `~`/`-` GP canonicalize） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave646（已闭 · Cap residual freestanding unary `~` / `-` · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | freestanding `~x == -1` / `-x == -5` host+pure=42、fs=0；`return ~x` 仍 255（低 8 位掩盖） |
| tip 产品 | **`c0067fb97`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave646 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| freestanding i32 `~x == -1` / `-x == -5` 假跑 | 32 位 not/neg 高半零扩展；cmp 走 64 位；负 lit 全宽符号扩展 | ✅ i32 后 sxtw/cdqe |
| freestanding i64 `~x == -1` | BITNOT 无 wave306 式 64 位 not | ✅ REX.W not / mvn x0 |

**数据流**：产生 = `pipeline_asm_emit_bitnot_elf_impl` / `emit_neg` · 存储 = GP rax/x0 · 消费 = freestanding 64 位 cmp + 全宽负 lit。host-C 用 C `~`/`-`；pure 常 CTFE 折叠假绿。G.7：补全同一 emit 路径；u32 保持 zext（`~0u == 4294967295`）。

**权威**：`compiler/pipeline_glue.c` · `pipeline_asm_emit_bitnot_elf_impl` + `glue_enc_sxt_i32_result_to_rax_elf_c` + neg i32 分支

### wave646 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `~x == -1` (i32) | ✅ host/pure/fs=42 | ✅ 42 |
| `-x == -5` (i32) | ✅ 42 | ✅ 42 |
| `~x == -1` (i64) | ✅ 42 | ✅ 42 |
| `~u32 == 4294967295` | ✅ 42 | ✅ 42 |
| `return ~0` | ✅ 255 | ✅ 255 |
| `%=` 回归 | ✅ 2 | ✅ 2 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |
| disasm fs `~` | `mvn`+`sxtw` | `not %eax`+`cltq` |

### 仍 soft

- untyped `self`（wave493 非标准）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶，或整波 L4 + 升钉  

## wave645（已闭 · Cap residual arm64 freestanding rem `%` / `%=` · L2）

tip 产品 **`fd7782ec8`**。arm64 freestanding rem sdiv+msub。

## wave644（已闭 · Cap residual freestanding *i64/*u64/*Named ptr±int esz · L2）

tip 产品 **`85c07dbf3`**。freestanding *i64/*Named ptr scale。
