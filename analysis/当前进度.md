# X 语言 / X language 当前进度（2026-07-28 · wave587 Cap field typeck mnl/gnl · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`bbb7579f6`**（wave587 Cap field typeck mnl/gnl · L2 双端绿） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave587（已闭 · Cap residual field typeck mnl/gnl 63→127 · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Cap residual 再扫；soft leave mnl/gnl≤63 + .x prebind 漂移 |
| tip | **`bbb7579f6`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave587 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| mono concrete 扫描 mnl≤63 | seed + `.x` field_access | ✅ ≤127（mnm[128]） |
| ambient concrete 扫描 gnl≤63 | seed + `.x` + heavy field_access | ✅ ≤127（gnm[128]） |
| prebind / base_nlen / lnl ≤63 | `.x` 相对 seed 漂移 | ✅ ≤127（与 seed wave582 对齐） |
| reverse_infer fl / lval out_len / rlen ≤63 | seed 落后于 `.x` | ✅ ≤127 |

**数据流（五问）**：产生 = TYPE_NAMED 字段类型 / 枚举 prebind / 方法 import 绑定名；存储 = arena name + layout；消费 = mono stamp / ambient stamp / reverse-infer。门控 ≤63 时长 concrete 名被当 type-param → 错误 mono/ambient 覆盖。

**权威**：G.7 `seeds/pipeline_glue_strict_minimal.from_x.c` + `pipeline_glue_strict_minimal.x` + `pipeline_typeck_field_access.c`（heavy ambient）。改 seed → 重建 `build_asm/pipeline_glue_strict_minimal.o` + g05。

### wave587 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| 长 concrete 字段类型 ty 63/64/75/127 → h.x.n | ✅ 42 | ✅ 42 |
| 长 type-param mono 63..127 → w.v | ✅ 42 | ✅ 42 |
| 长 enum 绑定 envt 63..127 | ✅ 42 | ✅ 42 |
| host-C reverse-infer fld 4/63/64 | ✅ 42 | ✅ 42 |
| short nested Outer.x.n | ✅ 42 | （同形） |
| rv / option / si / hello | ✅ 42/0/0/`Hello World` | ✅ 同左 |

### 仍 soft

- host-C 字段 designator 发射仍截断 ~64（fldh 75/127 host-C 红；codegen Cap residual）  
- freestanding 长字段名 mk().f… CG002（asm 路径；非本波）  
- 其它 Cap residual 侧门 / 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual：codegen host-C designator / 其它 `>63` 侧门，或整波 L4  
2. 整波双端 L4 + 升钉  

## wave586（已闭 · Cap label/goto）

tip `520593143` / docs `e8cc1957a`。
