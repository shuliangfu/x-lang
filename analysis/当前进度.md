# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[自举进度-归档-2026-07-17.md](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **本波 process_argv 按需**（钉盘仍 **`c51759eb`**） |
| 产品 L4 放行 | ✅ **钉盘 `c51759eb`**（不升钉） |
| tip 双端 bstrict | ✅ **123/123 @ `c51759eb`**（钉盘） |
| **typeck CTFE fold** | ✅ 主路径 + **scalar 2-arg** + **1-arg pure** + **nested** + **vector lane** |
| **ASM bulk residual** | ✅ **PRIMARY** + **TASK_SPECIAL** + **prepare-ensure OS** + **process_argv** |
| **WPO call-site fold** | ✅ **flat + nested pre-emit** |

## 本波摘要（2026-07-18 · process_argv 按需 ensure）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前波 residual：process_argv prepare 仍恒 ensure |
| **地图** | ① `prepare_for_exe_link` 对 process_argv 无条件 ensure；② C 后端 `need_process_argv_glue` 裸 substr `process_shux_*`（rt_preamble 恒注入 weak 转发）→ 纯 rv 每 `-o` 仍 fork-cc process_argv |
| **根修** | G.7：`labi_user_needs_runtime_process_argv` 门控 prepare ensure；`use_line` 跳过 `__attribute__((weak))` 行；glue 改 use_line（args/process_shux/std_process_args/env_args_iter）；C 后扫链上 .o 的 process_shux U 补 ensure+push（对齐 asm 补链） |
| **验收** | mac L2：wipe process_argv 后 pure rv/option **不重建**；hello（warm heap 入链）/process/env/time/string/si 绿；矩阵 |
| **残差** | hello 在 warm heap.o 入链时仍可能补 process_argv（heap preamble U；非本波假绿）；plan bulk 瘦身；fk0 未覆盖；（旁）group_smoke T001 |

### residual 下一波

1. 下一产品债：另开地图（fk0 / plan 瘦身 / heap 按需入链）；禁无地图 tip L4  

```text
构建级别：L2（runtime_link_abi.o + g05；非 tip L4）
产品 tip L4 钉盘：c51759eb
权威：use_line weak-skip + need_process_argv_glue + labi_user_needs_runtime_process_argv + C post-module scan
```

---

## 前波摘要（2026-07-18 · prepare-ensure OS 按需）

| 波 | 结果 |
|----|------|
| **根修** | prepare 对 time/random/env 门控 ensure；删 invoke_cc warm time.o early ensure |
| **验收** | mac/Ubuntu L2 pure 不重建三 OS .o |
| **残差** | → **本波 process_argv** |

## 前波摘要（2026-07-18 · ASM bulk TASK_SPECIAL → UNDEF gate）

| 波 | 结果 |
|----|------|
| **根修** | `labi_user_needs_std_task` 门控 TASK_SPECIAL |
| **验收** | mac/Ubuntu L2；pure 无 task/scheduler |
| **残差** | → prepare-ensure（已闭） |
