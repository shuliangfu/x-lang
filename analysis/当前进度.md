# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（本波未改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`11a57351`**（双端 129） |
| tip | **`0efbc570`**（R2 `async_liveness` Cap residual pure FILE* emit_*） |
| 产品 bstrict | **129** · 钉盘双端全绿 |
| 本波性质 | Cap residual pure：liveness emit_typedef/local/codegen_comment |
| 工程轨 prove | ✅ mac IDENTICAL · Ubuntu IDENTICAL（L2；Ubuntu 先 L3 后 -E） |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `async_liveness` Cap residual pure FILE* emit_***（`0efbc570`）  
   - `.x`：`async_liveness_emit_frame_typedef` / `emit_frame_local` / `emit_codegen_comment` + `fputs_i32_dec`  
   - 桥（always seed）：`async_liveness_fputs`；长字面量拆片（-E 串 cap ~63）  
   - G.7：`async_liveness_type_to_c_buf` + `lookup_var_type` 权威（无第二套 type 表）  
   - 宿主 LE：layout num_awaits@4100 / frame_bytes@4104 / frame_tag@4108 / io_rd@4188 / io_wr@4192；ASTFunc name@8  
   - seed：`#ifndef SHUX_ASYNC_LIVENESS_FROM_X` 省略 emit；fputs always；surface re-pin + prove MODULES  

2. 验证  
   - mac：`SHUX=./compiler/shux bash tests/prove_module_selfhost.sh async_liveness` → IDENTICAL · `/tmp/mac_prove_async_liveness_emit.log`  
   - Ubuntu：L3 g05 后 prove IDENTICAL · `/tmp/ubuntu_prove_async_liveness_emit.log`（旧 shux -E 5× SIGSEGV 假红）  
   - **日常 L2 非 tip L4**；钉盘仍 `11a57351`  

3. 前序：CPS wave5 begin/hoist @ `e364aaf2` · wave4 FILE* emit @ `33505fbe` · liveness Cap pure type/layout @ `b06cf472` · 钉盘 `11a57351`

## 下一波前排

1. **Cap residual 余**：`async_liveness_fputs` / `async_cps_fputs` 桥（always seed；opaque FILE*）  
2. **或** 产品 PREFER 链 / `pipeline_glue` unbundle `async_asm_pool`  
3. **可选** tip 升钉：Ubuntu L4 + 双端 129  
4. **可选** Windows hybrid（需切双系统时再喊人）  

```text
钉盘：11a57351
tip：0efbc570（liveness Cap residual pure FILE* emit_*；双端 L2 prove IDENTICAL）
Windows：本波不需要切换
下一：fputs 桥 residual / PREFER unbundle / 可选 tip L4
```
