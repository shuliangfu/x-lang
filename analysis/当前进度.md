# X 语言 / X language 当前进度（2026-07-29 · wave641 Cap freestanding `&(*p)` · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`1bc77f0bc`**（wave641 产品） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave641（已闭 · Cap residual freestanding `&(*p)` · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | freestanding `let q = &(*p)` → **CG002**；host-C 绿；pure-asm 常 CTFE 假绿 |
| tip 产品 | **`1bc77f0bc`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave641 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| `&(*p)` / `&(*(*pp))` | `pipeline_asm_emit_addr_of_elf_c` 仅 VAR(3)/INDEX(47)/FIELD(44) → DEREF(52) **UNHANDLED** | ✅ DEREF 复用 wave324 `lvalue_eff_addr`（操作数指针位 = 被指物地址） |

**数据流**：产生 = ADDR_OF(DEREF) ELF → 存储 = 与 `*p=` lhs 同路径指针位 → 消费 = `let q=&(*p)` / call-arg。G.7：补全既有 ADDR_OF，不新开 cancel 第二套。

**权威**：`compiler/pipeline_glue.c`（`pipeline_asm_emit_addr_of_elf_c` + `pipeline_asm_emit_lvalue_eff_addr_elf_c` ko=52）。

### wave641 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `&(*p)` struct field / scalar | ✅ 42 | ✅ 42 |
| `&(*(*pp))` nested | ✅ 42 | ✅ 42 |
| `&(*p) == p` identity | ✅ 42 | ✅ 42 |
| `load(&(*p))` call-arg | ✅ 42 | ✅ 42 |
| wave640 `(*(*pp)).v` | ✅ 42 | ✅ 42 |
| wave639 `(*p).xs[1]` | ✅ 32 | ✅ 32 |
| `*i32[2]` arr-of-ptr | ✅ 42 | ✅ 42 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

### 仍 soft

- untyped `self`（wave493 非标准）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶，或整波 L4 + 升钉  

## wave640（已闭 · Cap residual freestanding nested `**pp` · L2）

tip 产品 **`92c07bfaa`**。`&p` lea + DEREF TYPE_PTR load 8。

## wave639（已闭 · Cap residual freestanding DEREF FIELD INDEX · L2）

tip 产品 **`2b274afdc`**。freestanding `(*p).xs[i]` INDEX/call-arg。

## wave638（已闭 · Cap residual host-C FIELD base primary · L2）

tip 产品 **`b1a8c6cfc`**。host-C `((base).field)` 修 `(*p).v` BLD001。
