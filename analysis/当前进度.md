# Shux 当前进度（2026-07-19）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **归档**：[自举进度-归档-2026-07-18.md](自举进度-归档-2026-07-18.md) · [17 号](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`d7a2388e`**（已 push origin；C5 EXPR_ENUM_VARIANT CTFE 日常 L2；前 tip `a8a67708` C5 EXPR_STRUCT_LIT 字段 CTFE 扩全） |
| 产品 L4 放行钉盘 | **`5cc73d54`**（双端真冷 L4+125；前钉 `ea1f6827`；本波未升钉） |
| 产品 bstrict 套件 | **125** |
| 当天已入钉盘波 | C2 std.net UNDEF gate · C3=C4 parser bare-`{` struct-lit · backtrace L4 cold · C8 vec/set/map/queue duplicate → `5cc73d54`（详见 [自举进度.md](自举进度.md) §6） |
| 当天 tip 波（非钉盘） | C5 EXPR_MATCH CTFE（`229708f1`）· C5 EXPR_STRUCT_LIT 字段 CTFE 扩全（`a8a67708`）· C5 EXPR_ENUM_VARIANT CTFE（`d7a2388e`） |

## 本波摘要（2026-07-19 · C5 EXPR_ENUM_VARIANT CTFE · 日常 L2 · 双端 L2 全绿）

| 波 | 结果 |
|----|------|
| **入口** | T10 Cap 候选 C5 子项「enum variant 常量折叠（match arm discriminant）」（[.trae/documents/cap-candidates-2026-07-19.md](../.trae/documents/cap-candidates-2026-07-19.md):151）：probe `const X: Color = Color.Green; let Y = match X { Color.Red => 100; Color.Green => 200; ... }` 应让 X 折叠为 Green 的 variant tag (1)，match 命中 Green arm 返回 200，否则走 runtime enum-load |
| **根因** | 双重缺失：① `glue_typeck_fold_expr_ref` 无 EXPR_FIELD_ACCESS case → `Color.Green` 不折叠；② `glue_is_const_expr_ref` 白名单也无 FIELD_ACCESS case → `const X: Color = Color.Green;` 在白名单阶段被拒（typeck_gen L6839 白名单先于 L6850 marker `pipeline_typeck_try_mark_enum_field_access` 火的点 → chicken-and-egg）。同时发现 Darwin filtered pipeline 把 glue 的强版 `pipeline_typeck_block_const_init_is_const_c` 局部化（`t` in nm），seed 弱版才是被外部调用的版本，故 seed 必须镜像同语义 |
| **根修（产生点权威 + seed 镜像同 commit）** | `compiler/pipeline_glue.c` 四处补充：① `glue_is_const_expr_ref`（L13617-13622）加 FIELD_ACCESS case：用 `g_typeck_active_module` 预标记 `pipeline_expr_try_mark_enum_field_access`，再判 `field_access_is_enum_variant`；② `glue_typeck_fold_expr_ref`（L13917-13929）加 FIELD_ACCESS case：预标记 → 读 `pipeline_expr_enum_variant_tag_at` → stamp `const_folded_val=tag`、`const_folded_valid=1`；③ 新增 `pipeline_typeck_active_module_c()` getter（L25631-25641）——`g_typeck_active_module` 是 `static`，seed 不可见；④ L1573 加 `pipeline_expr_try_mark_enum_field_access` 前向声明（定义在 ast_pool.c L4312，#include 在 L21848 晚于使用点）。`compiler/seeds/pipeline_glue_strict_minimal.from_x.c` 镜像：L343-351 加三 extern 前向声明 + L1903-1913 加 FIELD_ACCESS case（Darwin filtered pipeline 下生效）。3-dim 英文 docblock（Why/Invariant/Asm-Perf）。PLATFORM: SHARED |
| **新测试** | `tests/lang-const/c_enum_variant_const.x`（`const X: Color = Color.Green; let Y = match X { Color.Red => 100; Color.Green => 200; Color.Blue => 300; _ => 400; }; return Y;`，want=200）+ `tests/baseline/lang-const-eval.tsv` 注册 `case_enum_variant_const` 行 |
| **mac L2 验收** | `rm pipeline_x.o + build_asm/pipeline_glue_strict_minimal.o + build_asm/bootstrap_seed_pipeline_filtered.o` → `make pipeline_x.o PIPELINE_X_FORCE_COMPILE=1` + `make build_asm/bootstrap_seed_pipeline_filtered.o` + `g05_prepare_and_relink.sh` → c_enum_variant_const exit=200 ✓；lang-const 14/14 ✓ |
| **Ubuntu L2 金标** | `git pull d7a2388e` → `rm pipeline_x.o build_asm/pipeline_glue_strict_minimal.o && make pipeline_x.o PIPELINE_X_FORCE_COMPILE=1 && g05_prepare_and_relink.sh` → lang-const 14/14 ✓（c_enum_variant_const=200） |
| **链接图** | 本波为 typeck CTFE 扩展（非 ABI / 非 link 改动）。Darwin filtered pipeline 下 glue 强版 `pipeline_typeck_block_const_init_is_const_c` 被局部化（`t` in nm），seed 弱版 `pipeline_typeck_const_expr_ref_strict_minimal` 才是被外部调用的实现 → seed 必须镜像 FIELD_ACCESS case；Linux 直接链 `pipeline_x.o`（glue 强版生效，seed 镜像为 dormant weak，`--allow-multiple-definition` 兜底）。`g_typeck_active_module` 是 pipeline_glue.c 内 `static`（L119），seed 通过 `pipeline_typeck_active_module_c()` getter 访问 |
| **结案话术** | mac+Ubuntu 双端 L2 全绿；**非放行 L4**；C5 EXPR_ENUM_VARIANT CTFE 已入 tip `d7a2388e`；钉盘未升（typeck CTFE 扩展非 ABI / 非 link 改动） |

## 下一波前排（按优先级排序）

### 1. C5 EXPR_CLOSURE CTFE（大债 · 前置 closure 支持）

cap-candidates-2026-07-19.md C5 子项「closure / lambda CTFE（前置 closure 支持）」。前置依赖 closure capture 语义完整（独立大工程），非单纯 fold 扩展。先排期、估时、拆 sub-item，再决定是否本日启动。

### 2. C6 X ABI P0b（大债 · 跨 47 个 .x 文件）

381 个 C ABI 声明 `extern "C"` 标注（按 `analysis/X-ABI-roadmap.md` P0b 阶段；自举后激活 X ABI typeck 契约）

### 3. C7 plan 瘦身（工程债 · 非产品）

plan-only bstrict 模板裁剪（按 `tests/run-all-bstrict.sh` plan 子集）

## 不在前排

- **Cap C9 match guard full-chain 支持**（独立大工程 · 多波）：经调查 guard 不在 cap-candidates C5 范围内。X pipeline `MatchArmEntry`（[compiler/ast_pool.c](../compiler/ast_pool.c):36-43）5 字段无 `guard_ref`；`pipeline_expr_append_match_arm` 签名无 guard 参数；`parser_parse_match_into_glue` extern 声明无定义；ELF emit / CTFE handler 均不处理 guard。当前 parser 遇到含 guard 的函数会静默丢弃（`num_funcs=1` 而非 3，`tests/typeck/match_guard.x` 的 classify/pick 函数被 parser drop）。要完整支持 guard 需要：① `MatchArmEntry` 加 `guard_ref` 字段；② `append_match_arm` 加 guard 参数 + accessor；③ parser `parse_match_into` 完整实装（不再走 extern glue）；④ typeck guard scope 求值；⑤ ELF emit guard 分支；⑥ CTFE handler guard fold。**6 个产生点，多波拆分**，先排期再动手
- **extern-function-not-linked 副问题**（`callee_u64` UNDEF）：经核为 expected behavior（extern 声明未实装），非 Cap 候选
- **C1 build error exit code 传播**：在 `a74e25a1` 已修（`main_argv_has_o_flag` + `main_cmd_run`），无需再做
- **C2 / C3=C4 / backtrace / C8**：均已入钉盘 `5cc73d54`，历史详情见 [自举进度.md](自举进度.md) §6 变更记录

```text
构建级别：mac + Ubuntu 双端 L2 真测（rm 相关 .o + make pipeline_x.o PIPELINE_X_FORCE_COMPILE=1 + g05 relink；非 tip L4）
产品 tip L4 钉盘：5cc73d54（未升；本波 C5 EXPR_ENUM_VARIANT fold 是 typeck CTFE 扩展，非 ABI / 非 link 改动，不需要升钉）
本波 tip：d7a2388e（C5 EXPR_ENUM_VARIANT CTFE，双端 L2 全绿）
权威：compiler/pipeline_glue.c:13617-13622 (is_const whitelist) + :13917-13929 (fold handler) + :25631-25641 (active_module getter) + :1573 (forward decl)
        compiler/seeds/pipeline_glue_strict_minimal.from_x.c:343-351 (extern decls) + :1903-1913 (Darwin mirror)
mac lang-const log: 14/14 OK exit=200 (c_enum_variant_const)
Ubuntu lang-const log: 14/14 OK exit=200 (c_enum_variant_const)
```
