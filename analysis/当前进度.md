# X 语言 / X language 当前进度（2026-07-28 · wave586 Cap label/goto · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`520593143`**（wave586 Cap label/goto · L2 双端绿） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave586（已闭 · Cap residual label/goto 32→128 · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Cap residual 再扫；label/goto silent clamp 31 → 前缀碰撞 / host-C 截断 |
| tip | **`520593143`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave586 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| LabeledStmt label/goto_target[32] | `ast.x` + seed 镜像 | ✅ [128] · content ≤127 |
| OneFuncLabeledEntry [32] | `ast_pool` | ✅ [128] · append/fill ≤127 |
| set_names clamp >31 | `runtime_driver_strict_glue_stubs` | ✅ ≤127 |
| *copy32 只填 32 | label/goto_target_copy32 | ✅ ABI 名保留 · payload 128 |
| host-C / asm 发射 scratch [32] · 门 ≤32 | codegen.x + seed + pipeline_glue | ✅ [128] · 门 ≤127 |

**数据流（五问）**：产生 = parser `label:` / `goto T` IDENT；存储 = LabeledStmt / OneFunc labeled 池；消费 = host-C emit / asm enc_jmp·enc_label。

**操作纪律（关键）**：

- 改 `ast_pool` / LabeledStmt 布局 → **`cp seeds/pipeline_gen.linux.x86_64.c pipeline_gen.c`**（gitignored 本地 pin）再 **PIPELINE_X_FORCE_COMPILE=1** `pipeline_x.o` + g05  
- 未 pin 时 Ubuntu 用旧 `pipeline_gen.c` 的 [32] sizeof 与 set_names [128] 视图错位 → 长 label 截断 / ELF unresolved patch  
- 改 stubs set_names → 重建 `runtime_driver_strict_glue_stubs.o`  
- 改 codegen seed → pin `codegen_gen.c` + `codegen_x.o`

### wave586 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| goto 名 4/31/32/63/64/75/127 | ✅ 42 | ✅ 42 |
| labeled-return 31..127 | ✅ 42 | ✅ 42 |
| 碰撞（同前 31 不同末字节）→ 目标 return 7 | ✅ 7 | ✅ 7 |
| -E 全名（无 silent 31 截断） | ✅ | ✅（pin 后） |
| rv / option / si / hello | ✅ 42/102/0/`Hello World`+`0a` | ✅ 同左 |
| tests/goto 短名 | ✅ 42 | ✅ 42 |

### 仍 soft

- Cap residual 其它 `>63` 侧门（codegen 部分路径、strict_minimal mnl/gnl≤63、region 等）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 再扫（codegen/strict_minimal mnl·gnl residual 等）或整波 L4  
2. 整波双端 L4 + 升钉  

## wave585（已闭 · Cap param name）

tip `ff25582ec` / docs `d5a3d7658`。
