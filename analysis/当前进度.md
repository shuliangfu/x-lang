# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **pipeline_abi wave54 pure residual** @ `910e0390` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **R2 pipeline_abi Cap residual pure wave54**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + pure-asm 矩阵 rv42 / option / hello / import / stdlib-import / overload；strdup / paths_tmp / tmp_parse pure **W**；Cap residual resolve_read_preprocess **T** @ `910e0390` |
| mac | ✅ L2 PREFER hybrid + c-backend 矩阵（Darwin pure-asm CG002 tip 既有）@ `910e0390` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave54** @ `910e0390`  
   - **迁 pure**：`shux_collect_strdup` 薄壳（malloc + length scan + byte copy + trailing NUL）  
   - **G.7**：seed_to_load / enqueue / load_one / paths_process_one 复用 pure strdup  
   - **仍 seed 重 C**：resolve_read_preprocess / thread emit  
   - **seed**：冷孪生 `#ifndef FROM_X`（libc strdup wrap）；FROM_X rest 补 pure strdup **extern**  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave54b.log` · `/tmp/ubuntu_g05_wave54.log` · matrix 本机 / Ubuntu 会话输出

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：resolve_read_preprocess Cap residual / thread emit residual  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave54 未升钉）
产品 tip：910e0390 pipeline_abi residual pure wave54 L2
Windows：本波不需要切换
下一：resolve_read residual / thread residual pure / Darwin CG002（需时再说）
```
