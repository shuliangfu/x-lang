# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`ff064051`**（双端 L4 真冷 + 129；本波未升钉） |
| tip | **`bcd63b9c`** nostdlib realloc 根修（产品 tip） |
| 产品 bstrict | **129** · 前 tip L4 双端全绿；本波日常 L2 |
| 本波性质 | **Ubuntu `-E` SIGSEGV 根修**（nostdlib realloc OOB） |
| Ubuntu | ✅ L2：大 TU `-E` 50/40/40 零段错误 + rv=42 |
| mac | ✅ L2 g05 + rv=42（Darwin 多走 libc malloc，非本债主路径） |
| Windows | **本波不需要** |

## 本波摘要

1. **Ubuntu 偶发 `-E` SIGSEGV 根因（`bcd63b9c`）**  
   - 现象：大模块（async_cps / liveness / simd_enc）`-E` 约 50% `SIGSEGV`；小文件 0%；`setarch -R` 0%  
   - 栈：`memcpy ← realloc ← grow_vec_ensure ← pipeline_arena_expr_alloc`（GrowVec 256→4352 扩容）  
   - 根因：`bootstrap_nostdlib_stubs` bump `realloc` 按**新** `size` 拷贝，旧块长度未知 → 扩容读穿未映射页（ASLR 间歇）  
   - 修法：`BootstrapHeapHdr` 记录 `user_size`；`realloc` 只 `memcpy min(old,new)`  
   - 验收：Ubuntu 重建 stubs + g05；cps 50/50、liv 40/40、simd 40/40 **segs=0**；rv=42  
   - 构建级别：**L2**（非 tip L4 升钉）

2. **前波已闭**  
   - tip L4 升钉 `ff064051`；async Cap residual pure 族已耗尽（仅 opaque `driver_preamble_fputs` 常 seed）

## 下一波前排

1. **可选** R2 rest（新模块）/ Cap residual pure 叶（async pure 已闭）  
2. **可选** g05 sed 防御收敛；harness `-E` 5 次重试可评估降级  
3. **可选** Windows hybrid；Darwin arm64 纯 asm Vec4f `.o` CG002

```text
钉盘：ff064051（tip L4 双端 129）
tip：bcd63b9c nostdlib realloc size-header
Windows：本波不需要切换
下一：R2 rest / g05 sed / Windows / Darwin CG002
```
