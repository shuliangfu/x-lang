# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`1f73ed275`**（双端 tip L4 真冷 + 129；未升 — 本波日常 L2） |
| tip 产品代码 | **pipeline_abi wave91 set_dep_ctx pure** @ `455a9457` |
| 产品 bstrict | **129** · 钉盘 tip L4 全绿；wave91 **日常 L2** 矩阵绿（未全量 bstrict） |
| 本波性质 | **wave91 pipeline_typeck_set_dep_ctx / get_dep_ctx Cap residual 真迁 pure BSS** |
| Ubuntu | ✅ L2 g05 hybrid pure **W**（set/get_dep_ctx）+ 矩阵 rv42/option/si/hello + nest #if/-D + force_c @ `455a9457` · `/tmp/ubuntu_g05_wave91.log` · `/tmp/ubuntu_g05_wave91_matrix.log` |
| mac | ✅ L2 g05 hybrid pure set/get_dep_ctx + 矩阵 rv42/option/si/hello + nest #if/-D + force_c @ `455a9457` · `/tmp/mac_g05_wave91.log` · `/tmp/mac_g05_wave91_matrix.log` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave91** @ 产品 **`455a9457`**  
   - `pipeline_typeck_set_dep_ctx` / `pipeline_typeck_get_dep_ctx`：G.7 真迁 pure LP64 BSS  
   - `g_pipe_typeck_dep_ctx` u8[8] cell via `shux_ptr_slot_*`；无跨 TU 裸全局  
   - 删 `ast_pool` static `g_typeck_dep_ctx` 双权威；enum variant fallback 只读 `get_dep_ctx`  
   - `pipeline_glue.c` 同名 **SHUX_WEAK** 冷 twin（独立 cold BSS）  
   - 闭合 Cap residual「set_dep_ctx」叶（under pure dep-prerun orch）  
   - Cap residual 仍：`load_and_sync_direct_import_deps_c`；cfg_eval 复杂；g05 &fn cast；layout validate/patch  
   - **构建级别 L2**（日常真测；非 tip L4）  
   - 注：改 `ast_pool.c`/`pipeline_glue.c` 须重建 `build_asm/pipeline_glue_standalone.o`；touch `.x` 再 g05 确保 pure 入链  
2. 继承 tip L4 钉盘 **`1f73ed275`**（wave76–81 收口；wave82–91 未升钉）

## 下一波前排

1. rest R2：pipeline_abi **硬 Cap residual** 叶（`load_and_sync` 大 glue；cfg_eval 复杂；g05 &fn cast；layout validate+patch helpers under pure orch）  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 硬 Cap / soft 再有整波收口时再 tip L4（非每微步）

```text
钉盘：1f73ed275（tip L4 双端 129；本波未升）
产品 tip：455a9457 pipeline_abi residual pure wave91 set_dep_ctx
文档：21 号归档 · 22 号 wave76–91 + tip L4 升钉 @1f73ed275
Windows：本波不需要切换
下一：pipeline_abi 硬 Cap residual（load_and_sync / cfg_eval / g05 &fn / layout）或整波 tip L4
```
