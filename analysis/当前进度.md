# X 语言 / X language 当前进度（2026-07-28 · wave584 Cap library/import · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`dbfa284da`**（wave584 Cap library wrap/import binding · L2 双端绿） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave584（已闭 · Cap residual library wrap / import binding 64→127 · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave583 后 residual：library scan name/param_type/field >63；import binding >63 未登记 |
| tip | **`dbfa284da`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave584 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| library 扫描 fn 名拒 >63 | `parser_asm_library_wrap_slice` `name_len>63` | ✅ 127 · name[128] |
| library param 类型名 / field | `param_type_len`/`field_len` >63 | ✅ 127 |
| import binding 拒 >63 | collect `ident_len<=63` + `bind_buf[64]` | ✅ 127 · bind_buf[128] |
| set_binding_name 仍 >64 | `ast_pool` `len>64` / `byte_at off>=64` | ✅ 127 / 128 |
| select 行宽 64 vs content 127 | `import_select_name_rows` 64 + memset/memcpy | ✅ 行 128 |
| path 侧门 63 | consume_path / path_buf；dep copy64 仅 64B | ✅ path 127 · copy64 填 128 |
| typeck binding hint | `bl<=63` | ✅ 127 · seed pin |

**数据流（五问）**：产生 = parser_asm library_wrap + imports collect；存储 = LibraryParseResult / ImportEntry.binding_name[128] / dep_path_rows[128]；消费 = typeck binding resolve/hint、codegen dep path。

**操作纪律**：改 parser_asm slices → `parser_asm_thin_glue.o` + g05；改 `ast_pool` → **PIPELINE_X_FORCE_COMPILE=1** `pipeline_x.o` + g05；改 typeck.x → seed pin + `typeck_x.o`。

### wave584 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| import binding 63/64/75/127 | ✅ 42 | ✅ 42 |
| library form fn 名 63/64/75/127 | ✅ 42 | ✅ 42 |
| rv / option / si / hello | ✅ 42/102/0/`Hello World`+`0a` | ✅ 同左 |

### 仍 soft

- Cap residual 其它 `>63` 侧门（codegen/asm 内联路径、path_cnt 邻域等）若仍有则下一波扫  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 全库再扫 `>63` 硬门（或整波 L4）  
2. 整波双端 L4 + 升钉  

## wave583（已闭 · Cap struct layout/field）

见上波；tip `9283a6cfb` / docs `e5d3a8b78`。
