# X 语言 / X language 当前进度（2026-07-29 · wave693 host-C [][][]T + [][]Named fat · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 05:59 · 28 全日（wave534–617）已归楼；本文仅保留 29 日 tip 快照

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`8fc6b4da6`**（wave693 host-C `[][][]T` + `[][]Named` fat layouts） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave693（已闭 · Cap residual host-C [][][]T + [][]Named fat · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave692 soft：更深 nested / NAMED fat；host-C `[][][]i32` 与 `[][]Cell` BLD001 incomplete；pure-asm Ubuntu 已绿 |
| tip 产品 | **`8fc6b4da6`** |
| 构建级别 | **L2**（mac+Ubuntu bootstrap-driver-seed + g05；**非** L4；钉盘不升） |

### wave693 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 缺两层 nested scalar layout | XLANG_SLICE_LAYOUTS 仅 wave691 一层 `xlang_slice_xlang_slice_*` → `[][][]i32` 标签 incomplete | ✅ 补 `xlang_slice_xlang_slice_xlang_slice_*`（rt_preamble + emit_header） |
| 缺 `[][]Named` companion | companion 仅一层 `xlang_slice_<TAG>` | ✅ 同权威再发 `xlang_slice_xlang_slice_<TAG>` |
| nested TYPE_SLICE 双标签 | emit_type 回落 type_to_c_repr 时 struct_prefix 空，而一层 NAMED 用 file-stem → local `xlang_slice_xlang_slice_Cell` vs formal/companion 带前缀 | ✅ fallback 用 `codegen_emit_prefix_len_from_ctx` |

**数据流**：产生=`let cube:[][][]i32` / `let m:[][]Cell` host-C type_to_c_repr + companion。存储=XLANG_SLICE_LAYOUTS + companion 表。消费=host-cc 完整 struct。G.7：补全同一 layout/emit_type 权威；seed 同 commit。

**权威**：`seeds/rt_preamble.from_x.c` · `src/codegen/codegen.x` · `seeds/codegen_gen.linux.x86_64.c`（同 commit）

### wave693 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| host-C nested3 take/local → 10 | ✅ | ✅ |
| host-C nested_named take/local → 35 | ✅ | ✅ |
| pure-asm/default nested3/named → 10/35 | ✅ | ✅ |
| Ubuntu freestanding nested3/named → 10/35 | — | ✅ |
| wave692 host-C [][]i32 回归 60 · 矩阵 rv42/option/hello/si | ✅ | ✅ |
| 标签：`xlang_slice_xlang_slice_xlang_slice_int32_t` · companion 双层 `xlang_slice_xlang_slice_<pfx>Cell` 与 local 一致 | ✅ | ✅ |

日志：`/tmp/mac_g05_wave693.log` · `/tmp/ubuntu_g05_wave693.log` · `/tmp/ubuntu_w693_probes2.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**；P011 更早失败）  
- LANG-006 标量 let/const bool→int（**产品保留**）  
- 更深四层+ / 其它 NAMED 嵌套边缘（未扩）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self；LANG-006 保留），或整波 L4 + 升钉  

## wave692（已闭 · Cap residual pure-asm nested [][]T INDEX · L2 工程轨）

见 wave692 节历史；tip **`c48923df3`** · 钉盘仍 **`01479a857`**。host-C 更深 nested / NAMED 已由 **wave693** 闭。

## wave691（已闭 · Cap residual nested [][]T host-C fat · L2 工程轨）

见 wave691 节历史；tip **`fb4ba91ac`** · 钉盘仍 **`01479a857`**。
