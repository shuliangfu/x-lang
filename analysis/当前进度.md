# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **wave44 driver_abi Cap residual pure** @ `cfcd00ce` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **residual pure 微波**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 pure-asm rv42 @ `cfcd00ce`（pure 符号 **W**） |
| mac | ✅ L2 PREFER hybrid + g05 + help/color/c-backend check @ `cfcd00ce`（Darwin pure-asm CG002 **tip 既有**，非本波回归） |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 driver_abi Cap residual pure wave44** @ `cfcd00ce`  
   - `driver_print_usage_write` pure orch：color 策略  
     `NO_COLOR` 任意存在 → 无色；`CLICOLOR_FORCE` / `SHUX_FORCE_COLOR` truthy → 强制色；否则 `isatty(1)`  
   - G.7 复用 `driver_env_nonnull` / `driver_env_flag_truthy`（与 cold getenv 语义对齐）  
   - Cap residual always-seed：`shux_driver_usage_write_stdout`（巨型 plain/color 多行 lit + fwrite + fflush）  
   - **根因拆分**：`.x -E` 不能承载长 `\\n` 字串表 → 表权威唯一在 residual（禁止改成单行 usage 绕过）  
   - seed cold twin `#ifndef FROM_X`；FROM_X 无 pure-dup  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave44d.log` · `/tmp/ubuntu_g05_wave44.log`；Ubuntu pure **W**

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. Cap residual / rest R2：driver_abi 本轮 always-seed 大 lit / OS 面已收口；下一刀看 rest 切片或其它 Cap  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 其它 rest R2（非每微步 L4；下一整波收口再 tip L4）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave44 未升钉）
产品 tip：cfcd00ce wave44 L2
Windows：本波不需要切换
下一：rest R2 或 Darwin CG002 / Windows hybrid（需时再说）
```
