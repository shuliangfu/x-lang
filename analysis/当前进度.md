# X 语言 / X language 当前进度（2026-07-28 · wave582 Cap type_alias/enum/match · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`c7dbb960d`**（wave582 Cap type_alias/enum/match · L2 双端绿 · 未 L4） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave582（已闭 · Cap residual type_alias/enum/match 名 64→127 · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave581 后 type_alias/enum 名 ≥64 → typeck fail；let 长名已绿 |
| tip | **`c7dbb960d`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave582 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| type_alias 拒 >63 | `parser_asm_type_alias_slice` `name_buf[64]` + `name_len>63`；typeck `alias_name_len<=63` | ✅ 127 · typeck.x+seed pin |
| TYPE_NAMED 截断 63 | `consume_qualified_type_ident_name` clamp 63 | ✅ 127 |
| enum 登记拒 >63 | `try_register_enum` / skip_tl `nb[64]` / variants `vb[64]` | ✅ 127 · 侧车 API 已 [128] |
| Darwin enum.Variant 失败 | **strict_minimal** `prebind_len<=63` 跳过长 enum 名 base | ✅ prebind/field_len 127 |
| match 模式截断 | match_subject enum/variant buf 64 + clamp 63 | ✅ 128/127 |
| ast_pool 门控残留 | type_alias_set/enum_set/byte_at 仍 64 | ✅ 127/128 |

**数据流（五问）**：产生 = parser_asm type_alias/enum/type_ref + typeck prebind；存储 = TypeAliasEntry/ModuleEnumEntry name[128] + Type.name[128]；消费 = typeck resolve/alias、field_layout_named、match arms。Darwin 产品链 field_access 权威 = **strict_minimal weak twin**（须与 heavy 同门控）。

**操作纪律**：改 `ast_pool.c` / `pipeline_typeck_field_access.c` 后 **PIPELINE_X_FORCE_COMPILE=1** 重建 `pipeline_x.o`（Darwin 链 `bootstrap_seed_pipeline_filtered` 源自它；仅 rebuild standalone 不够）；改 strict_minimal 重建 `pipeline_glue_strict_minimal.o`；改 parser_asm 重建 thin glue。

### wave582 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| type_alias 名 63/64/75/127 | ✅ 42 | ✅ 42 |
| enum 类型名 63/64/75/127 + `.Variant` | ✅ 42 | ✅ 42 |
| enum 变体名 75 | ✅ 42 | ✅ 42 |
| match 长 enum 名 | ✅ 42 | ✅ 42 |
| rv / option / si / hello | ✅ | ✅ Hello World + `0a` |

### 仍 soft

- residual：struct layout type-param / library wrap / import 等其它 `>63` 侧门  
- 整波双端 **L4** + 升钉  

### 下一波

1. 其它 residual `>63` 侧门（按产品阻塞裁剪）或整波 L4  
2. 整波双端 L4 + 升钉  

## wave581（已闭 · Cap let/const 长名）

见上波；tip `dec1ef1fe` / docs `83f715035`。
