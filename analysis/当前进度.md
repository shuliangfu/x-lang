# X 语言 / X language 当前进度（2026-07-28 · wave606 Cap arm64 UFCS method MEMORY · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`d4e63a37d`**（wave606 Cap arm64 UFCS method MEMORY by-value · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave606（已闭 · Cap residual arm64 freestanding UFCS method MEMORY · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | mac fs `s.sum()` MEMORY self=234≠42；`o.t.sum`/`mk().sum`/`s.add(Big{…})` 同错 |
| tip 产品 | **`d4e63a37d`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave606 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| arm64 UFCS method MEMORY self | wave602 仅 `ta==0` 标 is_mem；arm64 `u=0→1` lea→x0 | ✅ MEMORY 双端 stack-only |
| arm64 method MEMORY 实参 | 同上；free CALL wave603 已绿、method 叶缺 | ✅ reserve + `store_memory_to_sp` + cleanup |

**数据流（五问）**：产生 = METHOD_CALL places=[recv,args] 中 >16B；存储 = arm64 [sp+#off] multi-word / x86 push；消费 = param_home 栈拷。权威：G.7 补全 `emit_method_call_elf_c_impl` ≡ free CALL wave603，不新开第二套。

**权威**：`compiler/seeds/backend_call_dispatch.from_x.c`（`pipeline_asm_emit_method_call_elf_c_impl`）。改后 rm `backend_call_dispatch.o` + g05。

### wave606 验证

| 探针 | mac L2 host/fs | Ubuntu L2 host/pure/fs |
|------|----------------|------------------------|
| ufcs_method_mem `s.sum()` 24B | ✅ 42/42（was 234） | ✅ 42/42/42 |
| mem_method_field `o.t.sum()` | ✅ 42/42 | ✅ 42/42/42 |
| mem_method_call `mk().sum()` | ✅ 42/42（was 138） | ✅ 42/42/42 |
| method_arg_mem `s.add(Big{…})` | ✅ 42/42（was 2） | ✅ 42/42/42 |
| dual / free_mem 回归 | ✅ 42 | ✅ 42 |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/0/0 |

### 仍 soft

- 其它 Cap residual 叶（如 STRUCT_LIT 作 method base 的链接边角）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual soft 其它叶，或整波双端 L4 + 升钉  

## wave605（已闭 · Cap residual freestanding MEMORY call-arg from STRUCT_LIT/FIELD · L2）

tip 产品 **`46aa389a1`** / docs **`4223a337d`**。soft「UFCS method」本波由 **wave606** 收口（arm64 MEMORY 物化，非 typeck）。

## wave604（已闭 · Cap residual arm64 freestanding MEMORY call-arg from CALL · L2）

tip 产品 **`f1c9909aa`** / docs **`59139dae0`**。

## wave603（已闭 · Cap residual arm64 freestanding VAR dual-GP + MEMORY call-arg · L2）

tip 产品 **`00d2059d0`** / docs **`06c3d4e1a`**。

## wave602（已闭 · Cap residual freestanding UFCS method self SysV · L2）

tip 产品 **`ddb5f61ff`** / docs **`fb307d12a`**。arm64 MEMORY residual 由 **wave606** 补全。
