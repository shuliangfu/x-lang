# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`ff064051`**（升自 `46ba2297`；双端 L4 真冷 + 129） |
| tip | **`ff064051`** bare CLI path（产品 tip；验证 HEAD 含 docs `da9cfcc2`） |
| 产品 bstrict | **129** · 本波 tip L4 双端全绿 |
| 本波性质 | **tip L4 升钉**：全擦 `.o` + bootstrap + g05 + 矩阵 + `run-all-bstrict` 129 |
| Ubuntu | ✅ L4 真冷 + rv=42 + option/hello + **OK (129)** · `/tmp/ubuntu_true_bstrict_da9cfcc2.log` |
| mac | ✅ L4 真冷 + rv=42 + option/hello + **OK (129)** · `/tmp/mac_true_bstrict_da9cfcc2.log` |
| Windows | **本波不需要** |

## 本波摘要

1. **tip L4 真冷全测（主闸门）**  
   - 覆盖钉盘 `46ba2297` 之后产品 tip：§3.6 asm user `.o`（`961207bf`）+ bare CLI path / simd-s4（`ff064051`）及中间 PREFER/R2/opaque 谱系  
   - 构建级别：**L4**（`compiler|std|core` 全擦 `.o` + 删产品二进制 → pin seed → `bootstrap-driver-seed` → g05 → 矩阵 → bstrict）  
   - mac：`remaining_o=0` 起编；矩阵 rv=42 / option / hello；`run-all-bstrict OK (129 scripts, compiler/shux is shux_asm)`  
   - Ubuntu 金标：同；日志 `@ da9cfcc2`（docs-only 在 `ff064051` 之上，产品树同 tip）  
   - **升钉**：`46ba2297` → **`ff064051`**

2. **前波已闭（本波未改产品代码）**  
   - bare CLI path + simd-s4 harness（日常 L2 已双端绿）  
   - §3.6 asm CLI user `.o` plumb

## 下一波前排

1. **可选** R2 rest / Cap residual（async FILE* emit 等）  
2. **可选** g05 sed 防御；Ubuntu `-E` SIGSEGV 根因  
3. **可选** Windows hybrid；Darwin arm64 纯 asm Vec4f `.o` CG002

```text
钉盘：ff064051（tip L4 双端 129）
tip：ff064051 bare CLI path + simd-s4（L4 已升钉）
Windows：本波不需要切换
下一：R2 residual / -E SIGSEGV / Windows
```
