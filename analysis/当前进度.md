# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[自举进度-归档-2026-07-17.md](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **本波 LANG-006 residual 收口后 tip**（钉盘仍 **`c51759eb`**） |
| 产品 L4 放行 | ✅ **钉盘 `c51759eb`**（不升钉） |
| tip 双端 bstrict | ✅ **123/123 @ `c51759eb`**（钉盘） |
| **typeck CTFE fold** | ✅ 主路径（`const_folded_*`） |
| **LANG-006 residual** | ✅ **本波闭**（BITNOT + 逻辑 const） |

## 本波摘要（2026-07-18 · LANG-006 residual：`~` + `&&`/`||`）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续接着完成刚才的自举」；前排 residual BITNOT / 逻辑 |
| **地图** | ① unary：lexer 有 `TOKEN_TILDE`，权威 parse 在 `parser_asm_unary_slice.inc` 未 wrap→`EXPR_BITNOT` → XP003；② 逻辑：CTFE 已 fold，但 LOGAND/OR 类型为 bool，`const V: i32 = …` 缺 bool→int init coerce → XT001 |
| **根修** | G.7 补全权威：unary 切片 `TOKEN_TILDE`→`EXPR_BITNOT`；`typeck_coerce_init_bool_to_int_decl` 挂入 `typeck_coerce_init_expr_to_decl`（typeck.x + seed typeck_gen 同 commit）；**不**扩 emit fold |
| **验收** | mac L2：`typeck_x.o` + `parser_asm_thin_glue.o` + g05；lang-const **12/12**（unary=4 · logical=1）；let-const OK；矩阵 rv=42 · opt · si · hello |
| **残差** | 无 LANG-006 residual；（旁）WPO call-site fold 可选；**不** tip L4 |

### residual 下一波

1. （旁）WPO call-site fold 从 emit 迁到 pre-emit（可选）  
2. 下一产品债另开地图  

```text
构建级别：L2（parser_asm_thin_glue + typeck_x + g05；非 tip L4）
产品 tip L4 钉盘：c51759eb
权威：parser_asm_unary_slice.inc · typeck_coerce_init_bool_to_int_decl
文档根：analysis/（中文过程文已迁出仓库根）
```

---

## 前波摘要（2026-07-18 · typeck CTFE 常量折叠权威恢复）

| 波 | 结果 |
|----|------|
| **根修** | typeck glue 恢复 fold 生产者 + typeck.x 挂钩 + C codegen 消费 folded |
| **验收** | mac L2 g05；lang-const 算术/链/array_len 绿；矩阵绿 |
| **当时 residual** | BITNOT / 逻辑 → **本波已闭** |

## 前波摘要（2026-07-18 · tip L4 升钉 f16f7d48 → c51759eb）

| 波 | 结果 |
|----|------|
| **验收** | 双端真冷 L4 + bstrict **123/123** |
| **钉盘** | **`c51759eb`** |
