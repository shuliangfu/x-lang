# X 语言 / X language 当前进度（2026-07-27 · wave520）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`53001ce24`**（双端 L2，wave520；L4 待整波收口） |
| 钉盘 | **`2bc0885d3`**（待 Ubuntu 钉盘升级） |
| bstrict | **129** |

## wave520（已闭 · runtime_ed25519_ref10_glue R2 migration thin+rest 双端 L2）

| 项 | 状态 |
|----|------|
| 触发 | wave519 完成后，继续剩余纯 C seed 加密模块 |
| 模块 | `runtime_ed25519_ref10_glue`（链入 8 个 .inc：fixedint.h/sha512/fe/ge/sc/keypair/sign/verify） |
| 债 | `.x` 仅 doc anchor；ref10 实现在 .inc 文件（纯 C + 宏 + 内部 helper fe_*/ge_*/sc_*），无法整体迁 .x |
| 根修 | ① `.from_x.c`：5 宏重命名 `ed25519_*` → `ed25519_ref10_*_impl_c`（.inc 发射 _impl_c 实现符号）；sha512 宏保留 `ed25519_ref10_sha512*` 名（.c 消费者直接链接）② `.from_x.c`：`#ifndef XLANG_RUNTIME_ED25519_REF10_GLUE_FROM_X` guarded 3 个公开 wrapper（create_keypair/sign/verify 调 _impl_c）③ `.from_x.c`：`ed25519_create_keypair` 兼容别名始终编译（不 guard），直接调 _impl_c ④ `.x`：3 个 `#[no_mangle]` wrappers（ed25519_ref10_create_keypair/sign/verify，无 _c 后缀匹配 ed25519.x extern）+ `extern "C"` _impl_c 声明 ⑤ `Makefile` 加 R2 thin+rest `ld -r` 路径 |
| 构建模式 | **thin+rest**——.inc 文件是纯 C + 宏 + 内部 helper（fe/ge/sc/sha512），.x 无法表达；thin 只暴露 3 个 .x 消费的公开入口 |
| 关键设计 | **sha512 不 wrap**：`ed25519_ref10_sha512` 被 `runtime_crypto_inc_glue.from_x.c`（C 消费者）extern 引用，.inc 直接发射此符号即可链接，无需 .x 间接层。仅 3 个 .x 消费符号（create_keypair/sign/verify）迁到 thin wrappers |
| 审计结论 | .inc 间无交叉调用这 5 个公开函数（sign/verify/keypair.inc 仅定义、不互相调用），宏重命名 `_impl_c` 安全；`ed25519_sign_open`/`ed25519_keygen` 宏是 no-op（.inc 未定义，保留以备扩展） |
| nm 一致性 macOS | cold vs R2：11 核心 T 符号对齐（create_keypair/ref10_create_keypair/ref10_create_keypair_impl_c/sha512/sha512_init/sha512_update/sha512_final/sign/sign_impl_c/verify/verify_impl_c），仅 R2 多 `_doc_anchor` |
| nm 一致性 Ubuntu | cold vs R2：11 核心 T 符号对齐（同上），仅 R2 多 `_doc_anchor` |
| 产品矩阵 macOS | g-ffi-5-std-wrap ✅ · crypto 全链路 ✅（含 ed25519 sign/verify/sha512）· ed25519 RFC 8032 roundtrip ✅ |
| 产品矩阵 Ubuntu | g-ffi-5-std-wrap ✅ · crypto 全链路 ✅ · ed25519 RFC 8032 roundtrip ✅ |
| 已知 pre-existing | hello 产品矩阵失败（wave382 `.length` 字段重构引起 `std_io_Buffer` field designator 错误，与 ed25519 无关，clean tip 也失败） |
| 验证级别 | **L2 日常真测**（按自举方法 §0.2 节奏；非每微 commit 全量 L4） |
| PLATFORM | SHARED — ref10 是可移植 C；无 OS 专属代码 |
| 关键发现 | ① 审计驱动迁移：先确认外部消费者（.x vs .c）再决定哪些符号 wrap 到 thin——sha512 是 .c 消费者无需 wrap，避免不必要间接层 ② 宏重命名 `_impl_c` 后缀模式适用于 .inc 文件模块：.inc 发射 _impl_c，thin .x 提供 #[no_mangle] 公开符号 ③ 兼容别名始终编译（不 guard）+ 直接调 _impl_c：冷/R2 路径均可用，避免 R2 路径别名丢失 ④ `#[no_mangle]` 关键：ed25519.x 用 `extern function ed25519_ref10_create_keypair`（无 `_c` 后缀），thin wrapper 必须用 `#[no_mangle]` 才能生成匹配的符号名 |
