# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **pipeline_abi wave77 pure residual** @ `ba2c8362` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **R2 pipeline_abi Cap residual pure wave77**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + pure-asm 矩阵 rv42 / option / hello / stdlib-import；`typeck_ndep_slot` pure **T** / get/ptrs_base **W** @ `ba2c8362` |
| mac | ✅ L2 PREFER hybrid + c-backend 矩阵（Darwin pure-asm CG002 tip 既有）@ `ba2c8362` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave77** @ `ba2c8362`  
   - **迁 pure BSS + accessors**：`typeck_ndep` / `typeck_dep_module_ptrs` / `typeck_dep_arena_ptrs` → `g_pipe_typeck_*`；`typeck_ndep_slot` / `store_impl` / `typeck_dep_*_get` / `set_impl` / `ptrs_base`（G.7 `shux_ptr_slot_*`）  
   - **writers→accessor**：product seed cold（`rt_run_*` / `runtime.from_x` / driver_abi cold）裸写全局改为 `typeck_ndep_store` / `typeck_dep_*_set`，hybrid seed-fallback 与 pure 共用单存储  
   - **seed**：冷孪生 C 全局 + accessors 在 `#ifndef FROM_X`  
   - **Cap residual 仍**：fn-ptr / product_emit / typeck_module C 前端 / `pipeline_sizeof_*` / preprocess 引擎 / OS realpath·FILE*  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave77.log` · `/tmp/ubuntu_g05_wave77.log`

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：pipeline_abi 其它硬 Cap residual 叶（fn-ptr / product_emit / typeck_module C 前端；sizeof / preprocess / OS residual…）/ 整波 tip L4  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave77 未升钉）
产品 tip：ba2c8362 pipeline_abi residual pure wave77 L2
Windows：本波不需要切换
下一：pipeline_abi 硬 Cap residual / 整波 tip L4（需时再说）
```
