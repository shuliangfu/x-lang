# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **lexer path-safe nested block comments** @ `7d64368f1`（wave138 根因） |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **词法 · 嵌套块注释路径安全 nest-open**（`/*.o` / `src/*.x` 不误加深） |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option/hello + nest 探针 path42/dot99/dense7/nested42 |
| mac | ✅ L2：g05 + 矩阵 rv42/option/hello + nest 探针（path glob / dense / nested_block_comment） |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：wave138 迁 `generated_c_needs_{core_slice,db_kv,db_arrow}` 时，块注释正文写 `std/db/{kv,arrow}/*.o` → 嵌套语义把 `/*` 当内层开启 → 一层 `*/` 关不完外层 → **静默吞掉后续 export**（`num_funcs` 截断，假绿 typeck OK）  
2. **澄清**：嵌套块注释**已实现且工作**；不是「嵌套坏了」，而是 **路径通配与 nest-open 撞车** + 未闭合时无硬诊断  
3. **根修**：`skip_whitespace_and_comments` 路径安全 nest-open：  
   - prev 为路径/标识连续（alnum/`_`/`.`/`}`/`)`/`]`…）→ **不** nest-open  
   - `/*` 后紧跟 `.`（`/*.ext`）→ **不** nest-open  
   - 仍保留真嵌套：`/**/`、`/* inner */`、dense `/*/*inner*/*/`  
4. **权威**：`lexer.x` + seed `lexer_gen.linux.x86_64.c` + `lexer_empty_surface` 同 commit；docs 01/08  
5. **验收**：双端 L2 探针 path_glob=42 / dotglob=99 / dense=7 / nested_block_comment=42 + 矩阵；钉盘仍 **`3c88f7e51`**  
6. **WIP**：wave138 freestanding pure 产品迁（已可 `-E` num_funcs=54）留工作区，下一波收口

## 下一波前排

1. 收口 wave138：`core_slice`/`db_kv`/`db_arrow` pure orch + surface re-pin + 双端 L2  
2. 可选：未闭合块注释硬诊断（EOF depth>0）  
3. 其它 rest R2 / provides_*  

```text
钉盘：3c88f7e51（未升）
tip：7d64368f1 lexer path-safe nested comments
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：wave138 freestanding pure 收口
```
