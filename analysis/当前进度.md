# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **wave34 driver_abi Cap residual pure** @ `440de2b0` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **residual pure 微波**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42 + pure-asm rv42 @ `440de2b0` |
| mac | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42 @ `440de2b0` |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 driver_abi Cap residual pure wave34**  
   - `driver_asm_try_c_frontend_early`（产品 NO_C 固定 **-2**）  
   - `driver_asm_try_c_typeck_precheck`（产品 NO_C 固定 **-1**）  
   - `driver_asm_use_compiler_impl_c`（产品固定 **0**）  
   - `driver_asm_bind_lib_roots`（one_root BSS + G.7）  
   - `driver_asm_argv0`（`driver_argv_at`）  
   - `driver_asm_collect_defines` / `defines_as_u8` / `ndefines_get`（64×LP64 BSS + pure `argv_collect_defines`）  
   - seed cold twins `#ifndef SHUX_L2_RDABI_THIN_FROM_X`；无 pure-dup / 双 defines BSS  
   - **永久 OS residual 仍 seed**：asm `fopen_wb` / `mkstemp_fdopen` / `fwrite` / `fflush` / `write_metric_o`  
   - 构建级别：**日常 L2**（非 tip L4）；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave34e.log` · `/tmp/ubuntu_g05_wave34.log`；Ubuntu pure 符号 **W**

2. **注释纪律**  
   - 新 pure 方法 JSDoc（`@param` type+role；`*/` 单独成行）

3. **文档纪律**  
   - 只改 `自举进度.md` + 本文；**不**改 `当前问题分析.md` / `问题分析文档.md`

## 下一波前排

1. Cap residual 下一刀 pure-eligible：`driver_dispatch_*` / `driver_typeck_*_set` / `driver_diag_push_file` 等；巨型表 / fopen OS residual 仍 always-seed  
2. **可选** Windows hybrid；Darwin arm64 纯 asm / Vec4f CG002  
3. 其它 rest R2（非每微步 L4；下一整波收口再 tip L4）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave34 未升钉）
产品 tip：440de2b0 wave34 L2
Windows：本波不需要切换
下一：dispatch / typeck_set / diag_push pure-eligible 或 OS residual
```
