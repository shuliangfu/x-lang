# Shux 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-22.md](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d6648bee4`**（升自 `3c88f7e51`；双端 tip L4 + 129） |
| tip 产品代码 | **wave224** link_abi_system pure thin @ **`68f26865b`** |
| 产品 bstrict | 钉盘 **129**；本波 **日常 L2**（非 tip L4） |
| 本波性质 | **R2 Cap residual pure** · system·make pure thin + ensure G.7 |
| Ubuntu | ✅ L2：g05 + 矩阵 + string `-c` + diag/ld prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 + string `-c` + diag/ld prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **wave224**：system·make pure thin — 公开 **`link_abi_system`**（`labi_diag_pure.x` L1：null/empty cmd 门闩；Cap residual **`link_abi_system_impl`** = host **system**）。G.7 单权威 public；`ensure_std_net_o_auto_tls` / `shux_ensure_formal_std_make_o` 迁离 raw `system()`。mega 常驻 `_impl` + hybrid pure；cold/surface twins ≡ .x。
2. **闭合 soft residual**「hybrid 下 formal_std / ensure_std_net 仍 raw system(make)」。system `_impl` 仍 Cap residual；rt_entry / rt_run_exec 等非 pure ensure 路径仍可 raw system。
3. **验收**：双端 L2 矩阵 rv42/option/si/hello + string `-c`；labi_diag_pure + labi_invoke_ld_list prove **IDENTICAL**。钉盘仍 **`d6648bee4`**。

## 下一波前排

1. 常驻 Cap residual 可迁叶：resolve 池 `_impl` 体 / mega 体 residual raw getenv（`SHUX_MINIMAL_CC_LINK`、debug `SHUX_DEBUG_*` …）/ 其它 raw system 调用方（rt_entry/rt_run_exec 可选 G.7）；link_abi_system @ wave224 pure · pure 模块 env @ wave223 · link_abi_getenv @ wave222 pure
2. 可选：未闭合块注释硬诊断（EOF depth>0）
3. 可选 Windows hybrid（**需要时再通知重启**）

```text
钉盘：d6648bee4
tip：68f26865b wave224 link_abi_system pure thin
构建级别=L2 日常（非 tip L4）
Windows：不需要切换
下一：常驻 Cap residual（resolve 池 _impl / mega raw getenv …）
```
