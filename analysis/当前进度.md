# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **pipeline_abi wave74 pure residual** @ `8d2e12eb` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **R2 pipeline_abi Cap residual pure wave74**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + pure-asm 矩阵 rv42 / option / hello / stdlib-import；`driver_dep_seeded_slot` pure **T** + path/set/buf **W** @ `8d2e12eb` |
| mac | ✅ L2 PREFER hybrid + c-backend 矩阵（Darwin pure-asm CG002 tip 既有）@ `8d2e12eb` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave74** @ `8d2e12eb`  
   - **迁 pure**：`driver_dep_seeded_slot` / `driver_dep_arena_ptr_set_impl` / `module_ptr_set_impl` / `path_registry_set` / `path_registry_at` / `arena_buf` / `module_buf`  
     （模块 BSS 32 槽：arena/module/path_registry u8[256] + seeded i32[32]；G.7 `shux_ptr_slot_*`；lazy `*_buf` + `pipeline_sizeof_*`；path set 接受 null 以支持 pure clear_slots；无跨 TU 裸写——消费者仅 accessor）  
   - **Cap residual 仍**：fn-ptr / product_emit / typeck_module C 前端（+ `typeck_dep_*` / `typeck_ndep` 跨 TU 全局 BSS）  
   - **seed**：冷孪生 `#ifndef FROM_X`（driver_dep 静态表 + slot/set/at/buf）  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave74.log` · `/tmp/ubuntu_g05_wave74.log`

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：pipeline_abi 其它 Cap residual 叶（fn-ptr / product_emit / typeck_module C 前端；typeck_dep 跨 TU 全局需另波收敛…）/ 整波 tip L4  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave74 未升钉）
产品 tip：8d2e12eb pipeline_abi residual pure wave74 L2
Windows：本波不需要切换
下一：pipeline_abi 其它 residual / 整波 tip L4（需时再说）
```
