# X 语言 / X language 当前进度（2026-07-28 · wave602 Cap SysV UFCS method self · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`ddb5f61ff`**（wave602 Cap SysV UFCS method self dual-GP/MEMORY · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave602（已闭 · Cap residual freestanding UFCS method self SysV · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Ubuntu pure-asm `s.sum()` Pair(16B) run=59；S24(24B) run=57；callee 读 rdi+rsi / `[rbp+0x10..]`，caller 只 `mov rdi,rax` 或 lea→rdi |
| tip 产品 | **`ddb5f61ff`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave602 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| UFCS self 恒 1 GP | `pipeline_asm_emit_method_call_elf_c_impl` 非 import 叶 | ✅ places [recv,args] dual-GP/MEMORY |
| 丢 rdx 高半 | 仅 `mov rax→rdi` | ✅ spill/load reg_units=2 → rdi+rsi |
| >16B self lea 指针 | 与 wave599 MEMORY home 不匹配 | ✅ `push_sysv_memory` + cleanup |
| method 实参 formal 下标 | 旧路径 arg_index=方法实参序 | ✅ formal_ix=place（self=0） |

**数据流（五问）**：产生 = UFCS METHOD_CALL receiver 打包；存储 = GP 对 / 出栈 MEMORY；消费 = 形参 home（wave599）。权威：G.7 补全同模块 UFCS 叶 ≡ import METHOD + free CALL（wave601），不新开第二套。

**权威**：`compiler/seeds/backend_call_dispatch.from_x.c`（`emit_method_call_elf_c_impl` UFCS 叶）。改后 rm `backend_call_dispatch.o` + g05。thin 转调 `_impl`。

### wave602 验证

| 探针 | mac L2 host/fs | Ubuntu L2 pure/fs |
|------|----------------|-------------------|
| method_impl Pair dual-GP | ✅ 42/42 | ✅ 42/42（was 59） |
| method_mem S24 MEMORY | ✅ 42/42 | ✅ 42/42（was 57） |
| method_args / call_recv | ✅ 42/42 | ✅ 42/42 |
| method_dual2 self+other | ✅ 41/41 | ✅ 41/41 |
| free_dual / p24 / var_dual 回归 | ✅ 42/42/41 | ✅ 42/42/41 |
| disasm dual-GP | — | ✅ spill + rsi/rdi |
| disasm MEMORY | — | ✅ 4×push + `add $0x20` |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/0/0 |

### 仍 soft

- 其它 Cap residual 叶；整波双端 **L4** + 升钉  
- `function Pair.sum` 无 `impl` 语法 typeck（非本波 ABI）

### 下一波

1. Cap residual 其它 soft 叶，或整波双端 L4 + 升钉  
2. 整波双端 L4 + 升钉  

## wave601（已闭 · Cap residual freestanding >16B MEMORY by-value CALL · L2）

tip 产品 **`58431a7fb`** / docs **`5fb4fe1bf`**。soft「method dual/MEMORY self」本波由 **wave602** 收。

## wave600（已闭 · Cap residual arm64 multi dual-GP call-arg）

tip `b4a9519bd` / docs `101c977af`。
