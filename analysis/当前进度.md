# X 语言 / X language 当前进度（2026-07-27 · wave518）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`618d09691`**（双端 L2，wave518；L4 待整波收口） |
| 钉盘 | **`2bc0885d3`**（待 Ubuntu 钉盘升级） |
| bstrict | **129** |

## wave518（已闭 · runtime_test_fn_invoke R2 migration thin+rest 双端 L2）

| 项 | 状态 |
|----|------|
| 触发 | wave517 完成后，继续剩余纯 C seed 小模块 |
| 模块 | `runtime_test_fn_invoke`（20 行，1 公开函数：test_call_i32_void_c） |
| 债 | `.x` 仅 doc anchor；`void*` 转函数指针调用无法用 `.x` 表达，全在 C |
| 根修 | ① `.x`：`test_call_i32_void_c` thin wrapper（接收 u64，校验非零，转发 _impl_c）+ 英文 docblock ② `.from_x.c`：`test_call_i32_void_impl_c` 桥（uintptr_t → `int32_t (*)(void)` 强转调用） ③ `Makefile` 加 R2 thin+rest `ld -r` 路径 |
| 构建模式 | **thin+rest**——`((int32_t (*)(void))fn)()` 这种 void*→函数指针强转调用 `.x` 无法表达 |
| nm 一致性 macOS | cold vs R2：2 核心 T 符号对齐（仅 doc_anchor 多余） |
| nm 一致性 Ubuntu | cold vs R2：2 核心 T 符号对齐（仅 doc_anchor 多余） |
| 产品矩阵 macOS | hello ✅ · g-ffi-5-std-wrap ✅ |
| 产品矩阵 Ubuntu | hello ✅ · g-ffi-5-std-wrap ✅ |
| 验证级别 | **L2 日常真测**（按自举方法 §0.2 节奏；非每微 commit 全量 L4） |
| PLATFORM | SHARED — 无 OS 依赖；纯计算 wrapper |
| 关键发现 | ① `void*` → 函数指针强转调用必须留 C，`.x` 只能导出接收 u64/usize 的 wrapper ② `_impl_c` 桥必须始终编译 ③ thin+rest 模式已形成稳定模板：`#ifndef FROM_X` 守卫 cold-only `_c` wrappers，`_impl_c` 桥始终编译 |
