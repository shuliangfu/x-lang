# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **wave35 driver_abi Cap residual pure** @ `d213a091` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **residual pure 微波**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42 + pure-asm rv42 @ `d213a091` |
| mac | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42 @ `d213a091` |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 driver_abi Cap residual pure wave35**  
   - `driver_typeck_ndep_set`（G.7 `typeck_ndep_store`）  
   - `driver_typeck_dep_ptrs_set`（G.7 `typeck_dep_module_set` + `typeck_dep_arena_set`；j∈[0,32)）  
   - `driver_diag_push_file` / `driver_diag_restore`（null 守卫 + `diag_push_file` / `diag_restore`）  
   - `driver_dispatch_lib_root_at`（G.7 `shux_ptr_slot_get`；max 16）  
   - `driver_dispatch_opt_default`（共用 wave32 BSS lit `"2"`）  
   - seed cold twins `#ifndef SHUX_L2_RDABI_THIN_FROM_X`；无 pure-dup  
   - **仍 always-seed**：`dispatch_lib_roots_from_key`（16×512 静态槽）/ `run_compiler_parsed`（struct pack）/ `sibling_try_spawn`（access/fork/exec OS residual）  
   - 构建级别：**日常 L2**（非 tip L4）；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave35.log` · `/tmp/ubuntu_g05_wave35.log`；Ubuntu pure 符号 **W**

2. **注释纪律**  
   - 新 pure 方法 JSDoc（`@param` type+role；`*/` 单独成行）

3. **文档纪律**  
   - 只改 `自举进度.md` + 本文；**不**改 `当前问题分析.md` / `问题分析文档.md`

## 下一波前排

1. Cap residual 下一刀 pure-eligible：`dispatch_lib_roots_from_key` / `run_compiler_parsed`（静态槽+struct pack，可拆）或其它 rest；`sibling_try_spawn` / fopen 族仍 OS residual always-seed  
2. **可选** Windows hybrid；Darwin arm64 纯 asm / Vec4f CG002  
3. 其它 rest R2（非每微步 L4；下一整波收口再 tip L4）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave35 未升钉）
产品 tip：d213a091 wave35 L2
Windows：本波不需要切换
下一：dispatch lib_roots_from_key / run_compiler_parsed 或 OS residual
```
