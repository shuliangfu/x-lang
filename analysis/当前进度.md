# X 语言 / X language 当前进度（2026-07-24）

> **长期仪表盘**：[自举进度.md](自举进度.md)（24 日起重新起表）  
> **23 号全日归档**：[自举进度-归档-2026-07-23.md](自举进度-归档-2026-07-23.md)  
> **重命名分析**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)  
> **改名前锚点**：`anchor-pre-xlang-rename` @ `d07bcf155`（父钉曾 `a77fc5d90`）  
> **项目目录**：现行 `…/xlang/x-lang`（mac/Ubuntu/Windows 同构）  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only**（第三品牌 `shu_*` / `SHU_*` / `shux` 已清） |
| 主域 | **xlang.dev** |
| 工程 | **全库重命名 R0–R3 已闭** · 双端 tip L4 **129 全绿** · 钉盘 **`75c638465`** |
| process_xlang 兼容别名 | **已删除** |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **已恢复** · **wave359** Cap residual pure 已闭（freestanding i32.double → x*2） |

## 验收

### tip L4 钉盘（仍有效）

| 项 | 状态 |
|----|------|
| SHA | **`75c638465`**（wave288：invoke_cc push `runtime_link_abi_user_env.o`） |
| 构建级别 | **L4 真冷**（全擦 compiler/std/core `.o` + 重链产品二进制） |
| remaining_o 起编 | ✅ **0**（双端） |
| mac 全量 bstrict | ✅ **OK (129)** · `/tmp/mac_true_cold_75c638465.log` · `/tmp/mac_true_bstrict_75c638465.log` |
| Ubuntu 全量 bstrict | ✅ **OK (129)** · `/tmp/ubuntu_true_cold_75c638465.log` · `/tmp/ubuntu_true_bstrict_75c638465.log` |
| 前钉 | `3698e557f`（R3 重命名 tip L4；wave238–287 仅 L2） |

### 本波 tip（wave359 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`6a3f2d024`** — R2 Cap residual pure wave359（freestanding i32.double） |
| 产品 commits | `6a3f2d024` seed `_impl` + `backend_call_dispatch.x` |
| 前产品 | wave358 UFCS free method `807859715` |
| 构建级别 | **L2**（`backend_call_dispatch.o` + g05；**非 tip L4**） |
| 根因 | host METHOD_CALL 已 `(x * 2)`；freestanding `pipeline_asm_emit_method_call_elf_c_impl` 对非 import 调 bare `double` → Ubuntu UNDEF（mac 常 host-C / dead_strip 藏） |
| 修法 | G.7：`call_resolved_func_index < 0` 且 `name==double` / nargs=0 → emit base + mov rax→rbx + add rax,rbx（2x）；UFCS free `double` 仍走 call；seed + full.x 同 commit |
| 探针 | mac/Ubuntu **fs** `x.double()`=42 · UFCS free double(x+1)=42 · wave358 get=7 · multi-dim=5；host-C `(x * 2)`；Ubuntu disasm `mov $0x15`+`add %rbx,%rax` · 无 `double` 符号 |
| soft residual | untyped-let；reentrancy；`mut`；trait/impl methods |
| mac 矩阵 | ✅ rv=42 · option · hello |
| Ubuntu 矩阵 | ✅ rv=42 · option · hello（`/tmp/ubuntu_g05_wave359.log` · `/tmp/ubuntu_w359_probes.log`） |
| 产品钉盘 | **仍 `75c638465`**（日常 L2 不升钉） |

### 文档（2026-07-24 · 已合入 tip 链）

| 项 | 状态 |
|----|------|
| `docs/02-类型定义.md` | ✅ 补全：`i8`/`i16`/`u16`、宽度表、文本 L0–L3 现状、**§规划中的类型（自举后）**；wave355 struct 字段 `,`/`;`；**wave356 多维 `T[N][M]`** |
| `analysis/基础类型缺口与自举时机分析.md` | ✅ 决策：语言级 string **自举后** |

```text
下一：wave360 Cap residual
  leave-off：
   · AST name/var_name 升布局（仍 cap 63；L011/L012 诚实失败）
   · `*u8 + *u8` 运行时字符串拼接运算符（语言糖 · 自举后；现用 concat API）
   · 语言级 string/str（docs/02 已登记 · 自举后 · 禁现在实现）
   · host residual：getenv/system/realpath _impl + resolve pool；freestanding stubs
   · legacy if_then/return_val；riscv64 64-bit shift
   · 窄化 f64→f32 仍须 `as`（有意）
   · soft：untyped-let（docs 禁推断）；reentrancy last-wins static/COMMON
   · soft：`mut` 非产品关键字（docs 未列；let 可再赋）
   · soft：trait/impl methods（非本叶；i32.double bootstrap 已闭）
  钉盘 75c638465；日常 L2，整波/日终再 L4
```
