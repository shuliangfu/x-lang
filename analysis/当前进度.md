# X 语言 / X language 当前进度（2026-07-29 · wave640 Cap freestanding `**pp` · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`92c07bfaa`**（wave640 产品） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave640（已闭 · Cap residual freestanding nested `**pp` · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | freestanding `**pp` / `(*(*pp)).v` / `let q=*pp` → SEGV；host-C 绿；pure-asm CTFE 假绿 |
| tip 产品 | **`92c07bfaa`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave640 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| `&p` 当 `p:*T` | `pipeline_asm_emit_addr_of_elf_c` 复用 `glue_enc_local_slot_ptr_or_addr` → *T 槽 **load**（INDEX/field 基正确；ADDR_OF 错）→ `let pp=&p` 存 p 值 | ✅ VAR 槽 **始终 lea** |
| DEREF 结果 TYPE_PTR | `pipeline_asm_emit_deref_elf_c` 用 `glue_index_elem_byte_sz` 剥 *T→sizeof(T) → 半指针 load | ✅ TYPE_PTR → load 64（wave637 孪） |

**数据流**：产生 = ADDR_OF + DEREF ELF → 存储 = lea 槽 / load 指针位 → 消费 = 嵌套 `**` / FIELD。G.7：补全既有两函数，不改 `needs_ptr_load`（INDEX/field 仍 load）。

**权威**：`compiler/pipeline_glue.c`（`pipeline_asm_emit_addr_of_elf_c` · `pipeline_asm_emit_deref_elf_c`）。

### wave640 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `(*(*pp)).v` host/pure/fs | ✅ 42 | ✅ 42 |
| `**pp` host/pure/fs | ✅ 42 | ✅ 42 |
| `let q=*pp; *q` host/pure/fs | ✅ 7 | ✅ 7 |
| `**pp = 42` assign | ✅ 42 | ✅ 42 |
| `&p` 判别 `*pp==p` | ✅ 1 | ✅ 1 |
| wave639 `(*p).xs[1]` | ✅ 32 | ✅ 32 |
| wave638 `(*p).v` | ✅ 42 | ✅ 42 |
| wave637 `*i32[2]` | ✅ 42 | ✅ 42 |
| disasm | `lea` &p + `ldr x` mid | `lea -0x20; mov (%rax)` |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

### 仍 soft

- untyped `self`（wave493 非标准）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶，或整波 L4 + 升钉  

## wave639（已闭 · Cap residual freestanding DEREF FIELD INDEX · L2）

tip 产品 **`2b274afdc`**。freestanding `(*p).xs[i]` INDEX/call-arg。

## wave638（已闭 · Cap residual host-C FIELD base primary · L2）

tip 产品 **`b1a8c6cfc`**。host-C `((base).field)` 修 `(*p).v` BLD001。

## wave637（已闭 · Cap residual freestanding array-of-pointers `*T[N]` · L2）

tip 产品 **`e82ecdbd2`**。INDEX esz=8 + fixed-array PTR slot=8。
