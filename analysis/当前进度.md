# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（本波未改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`11a57351`**（双端 129） |
| tip | **`8d1f2452`**（R2 `async_cps_codegen` Cap residual pure wave1） |
| 产品 bstrict | **129** · 钉盘双端全绿 |
| 本波性质 | Cap residual pure wave1：`expr_is_*` await 分类器真迁 |
| 工程轨 prove | ✅ mac IDENTICAL · Ubuntu IDENTICAL（L2） |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `async_cps_codegen` Cap residual pure wave1**（`8d1f2452`）  
   - `.x`：`async_cps_expr_is_io_await` / `await_read` / `await_read_fd` / `await_write_fd` / `await_write` / `await_future_wait`  
   - helpers：`async_cps_load_i32` / `load_ptr` / `name_is_read|write|read_fd|write_fd`  
   - 宿主 AST 偏移（与 liveness 一致）：AWAIT=54 CALL=48 METHOD=49 FIELD=44 VAR=3；resolved@72 method name@32 impl@56  
   - seed：`#ifndef SHUX_ASYNC_CPS_CODEGEN_FROM_X` 省略 pure classifier C 体；FROM_X 仅 `slice_marker` + FILE*/walk/module residual  
   - surface re-pin（`-E` 产物）+ prove MODULES 注释  

2. 验证  
   - mac：`SHUX=./compiler/shux bash tests/prove_module_selfhost.sh async_cps_codegen` → IDENTICAL · `/tmp/mac_prove_async_cps_wave1.log`  
   - Ubuntu：pull tip 后 prove IDENTICAL · `/tmp/ubuntu_prove_async_cps_wave1.log`  
   - **日常 L2 非 tip L4**；钉盘仍 `11a57351`  

3. 前序：pure surface `d194aff5` · liveness pure · async_asm_pool · P1 · 钉盘 `11a57351`

## 下一波前排

1. **CPS Cap residual 续**：module/sched resolve · walk `_impl` · FILE* emit（按 pure 可迁性裁剪）  
2. **或** liveness Cap residual：layout/type（无 FILE*）  
3. **可选** tip 升钉：Ubuntu L4 + 双端 129  
4. **可选** Windows hybrid（需切双系统时再喊人）  

```text
钉盘：11a57351
tip：8d1f2452（CPS Cap residual pure wave1 expr_is_*；双端 L2 prove IDENTICAL）
Windows：本波不需要切换
下一：CPS Cap residual 续 / liveness layout pure / 可选 tip L4
```
