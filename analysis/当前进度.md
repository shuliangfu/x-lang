# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[自举进度-归档-2026-07-17.md](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **本波 prepare-ensure OS 按需**（钉盘仍 **`c51759eb`**） |
| 产品 L4 放行 | ✅ **钉盘 `c51759eb`**（不升钉） |
| tip 双端 bstrict | ✅ **123/123 @ `c51759eb`**（钉盘） |
| **typeck CTFE fold** | ✅ 主路径 + **scalar 2-arg** + **1-arg pure** + **nested** + **vector lane** |
| **ASM bulk residual** | ✅ **PRIMARY time/env/random** + **random fk0** + **TASK_SPECIAL** + **prepare-ensure** |
| **WPO call-site fold** | ✅ **flat + nested pre-emit** |

## 本波摘要（2026-07-18 · prepare-ensure OS 按需）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前波 residual：prepare-ensure 预编 OS .o |
| **地图** | `shux_asm_ld_prepare_for_exe_link` 对 random/time/env **无条件 ensure**；`invoke_cc` 在 warm `time.o` 存在时 early 强制 ensure time_os → 纯 rv/hello 仍预编 |
| **根修** | G.7：prepare 用 `labi_user_needs_runtime_{random_fill,time_os,env_os}` 门控 ensure；PRIMARY 推送前 ensure（冷树）；**删** invoke_cc「time.o 存在即 ensure」；need_time/formal/on_demand 仍权威 ensure |
| **验收** | mac L2：`src/runtime_link_abi.o`+g05；wipe 三 OS .o 后 pure rv/hello **asm+C 不重建**；env/time/random asm 再建且 run=0；opt/si/math |
| **残差** | plan 表 bulk 步（多数 skip）；process_argv prepare 仍恒 ensure；（旁）group_smoke T001 / Darwin pure-asm 全链 |

### residual 下一波

1. 下一产品债：另开地图（fk0 未覆盖模块 / process_argv 收敛 / plan 瘦身）；禁无地图 tip L4  

```text
构建级别：L2（runtime_link_abi.o + g05；非 tip L4）
产品 tip L4 钉盘：c51759eb
权威：shux_asm_ld_prepare_for_exe_link + labi_user_needs_runtime_* + need_time ensure
```

---

## 前波摘要（2026-07-18 · ASM bulk TASK_SPECIAL → UNDEF gate）

| 波 | 结果 |
|----|------|
| **根修** | `labi_user_needs_std_task` 门控 TASK_SPECIAL |
| **验收** | mac/Ubuntu L2；pure 无 task/scheduler |
| **残差** | → **本波 prepare-ensure** |

## 前波摘要（2026-07-18 · ASM bulk PRIMARY → on_demand gate）

| 波 | 结果 |
|----|------|
| **根修** | PRIMARY time/env/random OS + random fk0 |
| **验收** | mac/Ubuntu L2 |
| **残差** | → TASK_SPECIAL 门控（已闭） |

## 前波摘要（2026-07-18 · nested pure CALL → typeck CTFE）

| 波 | 结果 |
|----|------|
| **根修** | 一元 pure CALL + nested CTFE |
| **验收** | nested `-E return 0`；filtered pipeline |
| **残差** | → bulk PRIMARY 门控（已闭） |
