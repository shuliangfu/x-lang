# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`07684b813`**（双端 tip L4 129；未升 — wave106 日常 L2） |
| tip 产品代码 | **`83c09e1bc`** wave106 pure-asm pass1 deferred let leaf |
| 产品 bstrict | **129** · 钉盘继承；本波未 tip L4 |
| 本波性质 | **pure-asm option SEGV 根修**（日常 L2 非 tip L4） |
| Ubuntu | ✅ L2 g05 + pure-asm **SEGV 消**（ord3=11 / noptr2=101 / hello）；full option 错返 -4（frame residual）；c-backend option102 |
| mac | ✅ L2 g05 + pure-asm ord3=11 / option102 + c 矩阵 |
| Windows | **本波不需要切换** |

## 本波摘要

1. **切入**：pure-asm option SEGV（wave105 residual）  
2. **根因**：`pipeline_asm_emit_block_body_sync_elf` 两遍 emit — pass0 提前「纯」let，但依赖 CALL 初值的 binop（`extra = a + buf[0]`）也被 hoist → 入口 `movzbl (%rax)` 读未初始化指针  
3. **wave106**：`glue_block_compute_pass1_deferred_lets` — seed INDEX/CALL/METHOD_CALL + 传递闭包；pass0/pass1 用 mask  
4. **金标**：SEGV 消；full `tests/option/main.x` pure-asm 仍 residual（`compute_frame_size` +512 不够 expr temp）  

## 下一波前排

1. pure-asm `compute_frame_size` 补 expr/call temp 预留（full option → 102）· Darwin arm64 string-lit embed  
2. ImportEntry 结构债（ast_pool GrowVec）  
3. **可选** Windows hybrid  

```text
钉盘：07684b813（未升）
产品 tip：83c09e1bc wave106 pure-asm pass1 deferred let
日常 L2 · 非 tip L4
Windows：本波不需要切换
下一：frame_size temp headroom / arm64 string embed / ImportEntry
```
