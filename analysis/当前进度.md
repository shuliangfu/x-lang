# Shux 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-22.md](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave171** ensure_runtime_test_fn_invoke_o pure @ `c37e8291e` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **R2 Cap residual pure · special ensure residual**（`shux_ensure_runtime_test_fn_invoke_o` → L4 pure） |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：RFC 特殊 ensure residual — `shux_ensure_runtime_test_fn_invoke_o` 体仍 always mega C（wave170 heap_user 后；wrap.c+#include seed）  
2. **根修 / pure**：真迁 `labi_ensure_list.x` L4 pure orch（peer test_fn_invoke_o_path + pure byte join out/src/inc；Cap residual resolve/access/cc/stat；**direct seed compile 无 wrap.c**）  
3. **Cap residual**：`shu_resolve_compiler_dir` + `link_abi_path_readable` + `shux_cc_compile_sync` + `asm_link_obj_skip_missing`；peer diags  
4. **未动**：其它 special ensure（mbedtls）/ catalog `ensure_from_catalog` / 常驻 Cap rest  
5. **验收**：双端 L2 g05 + 矩阵 + string/vec/slice/compress 邻域 + ensure_list prove IDENTICAL；钉盘仍 **`3c88f7e51`**  

## 下一波前排

1. 其它 rest R2 / mega residual（可迁：special ensure mbedtls / ensure_from_catalog；常驻 Cap：contains_substr / undef_sym / path_io stat / wait / strerror / mega spawn-IO / realpath 池 / ensure_std_net_o_auto_tls / getenv / shu_resolve_compiler_dir …）  
2. 可选：未闭合块注释硬诊断（EOF depth>0）  
3. 可选 Windows hybrid（**需要时再通知重启**）  

```text
钉盘：3c88f7e51（未升）
tip：c37e8291e wave171 ensure_runtime_test_fn_invoke_o pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：其它 rest R2 / mega residual
```
