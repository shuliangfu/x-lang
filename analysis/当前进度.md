# X 语言 / X language 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **重命名分析**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)  
> **改名前锚点**：`anchor-pre-xlang-rename` @ `d07bcf155`（父钉曾 `a77fc5d90`）  
> **项目目录**：现行 `…/xlang/x-lang`（mac/Ubuntu/Windows 同构）  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only**（第三品牌 `shu_*` / `SHU_*` / `shux` 已清） |
| 主域 | **xlang.dev** |
| 工程 | **全库重命名 R0–R3 已闭** · 双端 L4 **129 全绿** · 钉盘 **`3698e557f`** |
| process_xlang 兼容别名 | **已删除** |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **已恢复** · **wave280** Cap residual pure 已闭（bare `return "…"` / string re-lex） |

## 验收

### tip L4 钉盘（不变）

| 项 | 状态 |
|----|------|
| SHA | **`3698e557f`**（`rename(R3): shu_* → xlang_*`） |
| 构建级别 | **L4 真冷**（全擦 compiler/std/core `.o` + 重链产品二进制） |
| mac / Ubuntu 全量 bstrict | ✅ **OK (129)** |

### 本波 tip（wave280 · 日常 L2，**≠** tip L4 升钉）

| 项 | 状态 |
|----|------|
| SHA | **`eef86e646`** — R2 Cap residual pure wave280 |
| 构建级别 | **L2**（seed helpers + g05 + 产品矩阵） |
| mac 矩阵 | ✅ rv=42 · `return "hi"` rv=104 · bare `"x";`=42 · paren/call/let 同 · i32←\*u8 type mismatch · L002 · L009 · digit_sep · empty-frac · binary · `/tmp/mac_g05_wave280.log` · `/tmp/mac_w280_matrix.log` |
| Ubuntu 矩阵 | ✅ rv=42 · `return "hi"`=104 · bare=42 · L002 · type mismatch · `/tmp/ubuntu_g05_wave280.log` · `/tmp/ubuntu_w280_matrix.log` |
| 产品钉盘 | 仍 **`3698e557f`**（本波未 L4 升钉） |

```text
下一：wave281 Cap residual
  （wave280 闭 bare return/expr string —
     根因：lex_at_token_from_result 对 TOKEN_STRING 用 content token_start
     重入 parse_expr → 假 L002 / 跨串误绿；现回退到开引号）
  leave-off：
   · `*u8 + *u8` 字符串拼接运算符（现指针加 → BLD001；用 std.string append）
   · invalid escape L010 / `\xHH` 解码
   · host residual：getenv/system/realpath _impl + freestanding stubs
  钉盘仍 3698e557f；日常 L2，整波收口再 L4
```
