# Shux 当前进度（2026-07-19）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **归档**：[自举进度-归档-2026-07-18.md](自举进度-归档-2026-07-18.md) · [17 号](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`e546b013`**（refactor(tests) C7 PoC 删除冗余 + 记录进度；非 ABI/link → 不升钉） |
| 产品 L4 放行钉盘 | **`305cfbe1`**（C6 波 2 升钉；C7 PoC 非 ABI/link 不升钉） |
| 产品 bstrict 套件 | **125**（不变；C7 删除的是非白名单脚本） |
| 本波性质 | **C7 plan 瘦身 PoC**（工程债 · 非产品 · P3 · 路径 C 重构合并） |
| 本波输出 | audit 881 总脚本 vs 125 白名单 + 23 W3 fast + 752 非白名单；合并 run-core-assert.sh → run-debug.sh（core.assert case）；删除 run-io-driver-process.sh（完全冗余）；发现 2 个 bug（std.debug.println + fmt 一元负号）转后续 |

## 本波摘要（2026-07-19 · C7 plan 瘦身 PoC · 路径 C 重构合并 · 非升钉）

| 项 | 结果 |
|----|------|
| **入口** | 推进 [C7-plan-audit.md](C7-plan-audit.md) + [cap-candidates-2026-07-19.md](../.trae/documents/cap-candidates-2026-07-19.md) §C7（用户选路径 C 重构合并） |
| **audit** | [C7-plan-audit.md](C7-plan-audit.md)：881 总脚本 vs 125 白名单 vs 23 W3 fast vs 752 非白名单；752 中 599 gate + 94 other + 24 perf + ...；8 个完全无引用 dead code 候选；125 白名单内**无冗余** |
| **路径 C 执行** | 2/8 完成：① run-core-assert.sh → run-debug.sh（合并 core.assert case，commit `bedb40ca`）② run-io-driver-process.sh 直接删除（hello.x 被 run-hello.sh 覆盖 + spawn_wait.x 被 run-process.sh L84 覆盖，commit `e546b013`） |
| **阻塞（6/8）** | 2 个 bug 阻塞：std.debug.println 返回字节数（11）vs main.x 期望 a==0 + fmt 命令一元负号 bug（return -i → return - i）；4 个类型不匹配：STD-061 perf + STD-047/091/092 gate 不适合合并到功能测试脚本 |
| **副产品发现 bug** | ① std.debug.println 返回值语义 bug（`println(ptr,len)` 返回 `io.write_stderr` 字节数而非 0/1）② fmt 命令一元负号 bug（`return -i` → `return - i`，但 `let a = -1` 正确）— 两个 bug 超出 C7 范围，转后续修复波次 |
| **L2 验证** | ✅ run-debug.sh 单独验证通过（SHUX=shux_asm：core.debug + core.assert + debug test OK，exit=0） |
| **升钉决策** | ❌ **不升钉**（C7 是测试脚本工程债，非 ABI/link 改动；钉盘保持 `305cfbe1`） |
| **本波 commit** | `bedb40ca` refactor(tests): merge run-core-assert.sh into run-debug.sh（3 files +177/-16）+ `e546b013` refactor(tests): delete redundant run-io-driver-process.sh + record C7 progress（2 files +28/-26） |

## 下一波前排（按优先级排序）

### 1. C7 剩余 6 个候选（转后续波次）

- **修 2 个 bug**（阻塞合并）：std.debug.println 返回值语义 + fmt 命令一元负号
- **评估 4 个 gate/perf 路径 A**（纳入白名单扩容 125→129）：STD-061 perf simd + STD-047 simd gate + STD-091 io-context gate + STD-092 net-context gate
- 详见 [C7-plan-audit.md](C7-plan-audit.md) §6 建议下一步

### 2. Windows 门禁补跑（C6 波 1 + 波 2 遗留 · ABI 边界）

按 [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md) §5.1 强制要求：
- ssh `windows-server` → `set TEMP=C:\shux_tmp` + `set CC=gcc` → `./tests/run-bootstrap-bstrict-windows-gate.sh`
- 验证波 1 unsafe 包裹 + 波 2 ABI 一致性在 Windows MSYS2/MinGW 下无回归

## 不在前排

- **C6 X ABI P0b 波 3：std/ 扩展标注**（远期 · 多波 · 顺延自举后 P1-P2）：~557 个 C 库包装分文件标注 `extern "C"`
- **shux-x 5GB 方案 B**（远期 · 非 KPI）：ArenaSidecar release API
- **Cap C9 match guard full-chain 支持**（独立大工程 · 多波）：6 个产生点改动，先排期再动手
- **C5 EXPR_CLOSURE CTFE**（独立大工程 · 多波）：已 scoping，顺延自举后立项 `lang-closure-v1-rfc.md`
- **历史进度**：C6 波 2 ABI 一致性（`305cfbe1` 升钉）/ C6 波 1 unsafe 包裹（`4214eea4` 升钉）/ C6 P0b audit（`7e507fb6` docs-only）/ shux-x 5GB 方案 A（`a14a1dd0`）/ 日终 L4 兜底（`94a8e7d5`）/ C5 三连击（`c174e70a`）/ 4 波 C5 CTFE / shux-x 5GB 调查 / Windows 时序决议 / C2/C3=C4/backtrace/C8 钉盘波，全部归档在 [自举进度.md](自举进度.md) §6 变更记录

```text
本波 tip：e546b013 (refactor(tests): delete redundant run-io-driver-process.sh + record C7 progress)
本波性质：C7 plan 瘦身 PoC（工程债 · 非产品 · P3 · 路径 C 重构合并 · 非升钉）
本波输出：audit 881 vs 125 vs 752 + 合并 run-core-assert.sh → run-debug.sh + 删除 run-io-driver-process.sh + 发现 2 bug（std.debug + fmt unary）
构建级别：L2 探针验证（run-debug.sh exit=0）；非 ABI/link 改动 → 不需要 L4 真冷 + 不升钉
产品 tip L4 钉盘：305cfbe1（保持；C7 非_ABI/link 改动不升钉）
Windows 门禁：未触发（C7 非 ABI 边界改动）

C7 路径 C 执行结果（2/8 完成）：
  ✅ run-core-assert.sh → run-debug.sh（合并 core.assert case，commit bedb40ca）
  ✅ run-io-driver-process.sh 直接删除（完全冗余，commit e546b013）
  🔴 run-std-debug.sh 阻塞（std.debug.println 返回字节数 bug）
  🔴 run-fmt-unary-gate.sh 阻塞（fmt 命令一元负号 bug）
  🟡 run-perf-simd-shuxffle-select.sh 类型不匹配（STD-061 perf）
  🟡 run-std-simd-shuxffle-select-gate.sh 类型不匹配（STD-047 gate）
  🟡 run-std-io-context-gate.sh 类型不匹配（STD-091 gate）
  🟡 run-std-net-context-gate.sh 类型不匹配（STD-092 gate）

副产品发现 bug（超出 C7 范围，转后续）：
  1. std.debug.println 返回值语义 bug：println(ptr,len) 返回 io.write_stderr 字节数（11）而非 0/1
  2. fmt 命令一元负号 bug：return -i → return - i（一元负号被加空格）；let a = -1 正确

权威文档：analysis/C7-plan-audit.md（§0 摘要 + §1 问题地图 + §2 752 分类 + §3 8 候选 + §4 30+ 对成对 + §5 3 路径 + §5.1 实际执行 + §6 建议 + §7 验证清单 + §8 相关）
历史进度归档：analysis/自举进度.md §6 变更记录（C7 PoC bedb40ca+e546b013 / C6 波 2 305cfbe1 升钉 / 波 1 4214eea4 升钉 / C6 P0b audit 7e507fb6 / shux-x 5GB 方案 A a14a1dd0 / 日终 L4 兜底 94a8e7d5 / C5 三连击 c174e70a / 4 波 C5 CTFE / shux-x 5GB 调查 / Windows 时序决议 / C2/C3=C4/backtrace/C8 钉盘波）
```
