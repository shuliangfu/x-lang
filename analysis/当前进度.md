# X 语言 / X language 当前进度（2026-07-29 · wave648 Cap freestanding signed >> / >>= · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`af5e1c609`**（wave648 freestanding signed `>>` / `>>=` SAR） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave648（已闭 · Cap residual freestanding signed `>>` / `>>=` · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Ubuntu freestanding `-16 >> 2` / `x >>= 2` host=42、fs=0；正数 `16>>2` 与 `u32` 逻辑右移本绿；mac CTFE 常 `mov #42` 假绿 |
| tip 产品 | **`af5e1c609`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave648 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| freestanding 有符号 `>>` / `>>=` 假跑 | `pipeline_asm_emit_binop_shift_elf_c` + assign case 38 恒发逻辑 SHR | ✅ 有符号 SAR / 无符号 SHR；64-bit 走 sar_cl_rax |

**数据流**：产生 = freestanding SHR emit · 存储 = eax/rax 结果 · 消费 = cmp 与 -4。host-C 走算术右移；编码器 `backend_enc_sar_cl_{eax,rax}_arch` 已有（wave306 arm64 asr）但 glue 未接。G.7：复用 `glue_binop_operand_is_unsigned_elf_c` + `is_64bit`，不新开第二套 shift。

**权威**：`compiler/pipeline_glue.c` · `pipeline_asm_emit_binop_shift_elf_c` + `glue_emit_assign_rhs_to_rax_elf_c` case 38

### wave648 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `-16 >> 2 == -4` | ✅ host/fs=42 | ✅ 42（was fs=0） |
| `x >>= 2` / i64 / let / var count | ✅ 42 | ✅ 42 |
| `16 >> 2` / `u32` 逻辑右移 | ✅ 42 | ✅ 42 |
| wave647 []f32 · wave646 `~` · wave645 `%=` | ✅ 42/2 | ✅ 42/2 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |
| disasm fs ashr | （CTFE 可 fold） | `sar %cl,%eax`（was `shr`） |

### 仍 soft

- untyped `self`（wave493 非标准）  
- `take(W{xs:f32[2]}.xs)` as `[]f32` freestanding CG002（本波 soft 再扫见）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶，或整波 L4 + 升钉  

## wave647（已闭 · Cap residual freestanding `[]f32` call-arg · L2）

tip 产品 **`6e501513d`**。freestanding `[]f32` ARRAY_LIT call-arg f32 pack。

## wave646（已闭 · Cap residual freestanding unary `~` / `-` · L2）

tip 产品 **`c0067fb97`**。freestanding unary `~`/`-` GP canonicalize。

## wave645（已闭 · Cap residual arm64 freestanding rem `%` / `%=` · L2）

tip 产品 **`fd7782ec8`**。arm64 freestanding rem sdiv+msub。
