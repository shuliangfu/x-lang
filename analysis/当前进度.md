# Shux 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-22.md](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d6648bee4`**（升自 `3c88f7e51`；双端 tip L4 + 129） |
| tip 产品代码 | **wave219** spawn_sync pure thin @ **`37c00ea1`** |
| 产品 bstrict | 钉盘 **129**；本波 **日常 L2**（非 tip L4） |
| 本波性质 | **R2 Cap residual pure** · diag Cap residual pure thin orch |
| Ubuntu | ✅ L2：g05 + 矩阵 + string `-c` + labi_diag_pure prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 + string `-c` + labi_diag_pure prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **wave219**：`shux_spawn_sync` pure thin（`labi_diag_pure.x` L1）：null/empty prog + null argv 门闩迁 pure；Cap residual **`shux_spawn_sync_impl`** = POSIX **fork+execvp+waitpid** / Windows **_spawnvp**（mega always）。闭合 soft residual「hybrid 下 spawn_sync 体仍 always mega（fork/exec/wait）」。
2. **踩坑**：export 函数形参写 **`**u8` 时 product codegen 会丢掉函数体**（仅留 extern _impl）→ hybrid UNDEF。argv 改 **`*u8` 不透明指针**（char** 同宽 ABI）。
3. **G.7**：public spawn_sync 单权威（hybrid L1）；invoke_cc / ld / strip 调用方经 public thin → `_impl`。cold + surface twins ≡ .x。
4. **验收**：双端 L2 矩阵 rv42/option/si/hello + string `-c`；labi_diag_pure prove **IDENTICAL**。钉盘仍 **`d6648bee4`**。

## 下一波前排

1. 常驻 Cap residual 可迁叶：getenv 包装 / resolve 池 _impl / system·make / access _impl / strip_out_x …；spawn_sync @ wave219 pure · realpath_cap @ wave218 pure · strerror+wait @ wave217 pure · waitpid @ wave216 pure
2. 可选：未闭合块注释硬诊断（EOF depth>0）
3. 可选 Windows hybrid（**需要时再通知重启**）

```text
钉盘：d6648bee4
tip：37c00ea1 wave219 spawn_sync pure thin
构建级别=L2 日常（非 tip L4）
Windows：不需要切换
下一：常驻 Cap residual（getenv / resolve 池 _impl / system·make …）
```
