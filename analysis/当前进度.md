# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`1f73ed275`**（双端 tip L4 真冷 + 129；未升 — 本波日常 L2） |
| tip 产品代码 | **pipeline_abi wave96 parse_into_buf pure orch** @ `ff79c20a` |
| 产品 bstrict | **129** · 钉盘 tip L4 全绿；wave96 **日常 L2** 矩阵绿（未全量 bstrict） |
| 本波性质 | **wave96 pipeline_parse_into_buf Cap residual 真迁 pure orch** |
| Ubuntu | ✅ L2 g05 hybrid pure **W**（parse_into_buf size≡pure 0xec）+ 矩阵 rv42/option/si/hello + nest #if/-D + force_c @ `ff79c20a` · `/tmp/ubuntu_g05_wave96.log` · `/tmp/ubuntu_g05_wave96_matrix.log` |
| mac | ✅ L2 g05 hybrid pure parse_into_buf orch + 矩阵 rv42/option/si/hello + nest #if/-D + force_c @ `ff79c20a` · `/tmp/mac_g05_wave96.log` · `/tmp/mac_g05_wave96_matrix.log` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave96** @ 产品 **`ff79c20a`**  
   - `pipeline_parse_into_buf`：G.7 真迁 pure orch（`parser_parse_into_init` + `driver_parse_into_buf_rc` + same-TU pure debug_trace/fixup on ok==0）  
   - 契约：`buf_len<=0` → -1；非 0 ok（含 -2）→ -1；异于 wave64 `parse_into_bytes`（允许 len0、无 post fixup）  
   - wave94 pure `load_import_from_disk_c` 同 TU 调 pure parse（闭合 load_import 下 parse 硬 Cap residual）  
   - glue 同名 **SHUX_WEAK** 冷 twin（须重建 `pipeline_x.o` + `pipeline_glue_standalone.o`）  
   - Cap residual 仍：typeck merge+wpo 调用面；cfg_eval 复杂 #if；g05 &fn cast  
   - **构建级别 L2**（日常真测；非 tip L4）  
2. 继承 tip L4 钉盘 **`1f73ed275`**（wave76–81 收口；wave82–96 未升钉）

## 下一波前排

1. rest R2：pipeline_abi **硬 Cap residual** 子叶（typeck merge 调用面；cfg_eval 复杂；g05 &fn cast）  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 硬 Cap / soft 再有整波收口时再 tip L4（非每微步）

```text
钉盘：1f73ed275（tip L4 双端 129；本波未升）
产品 tip：ff79c20a pipeline_abi residual pure wave96 parse_into_buf orch
文档：21 号归档 · 22 号 wave76–96 + tip L4 升钉 @1f73ed275
Windows：本波不需要切换
下一：pipeline_abi 硬 Cap 子叶（typeck merge / cfg_eval / &fn）或整波 tip L4
```
