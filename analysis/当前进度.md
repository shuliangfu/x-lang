# X 语言 / X language 当前进度（2026-07-28 · wave597 Cap x86 sret nest polarity + ARRAY_LIT NAMED esz · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`ca0e97771`**（wave597 Cap x86 sret nest + ARRAY_LIT NAMED · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave597（已闭 · Cap residual x86 sret nest high-end polarity + ARRAY_LIT NAMED esz · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | Ubuntu pure-asm nest4/ptr_nest_call SIGSEGV；arr_simple run=10≠22 |
| tip 产品 | **`ca0e97771`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave597 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| sret 嵌套 STRUCT_LIT temp 与 sret_home 重叠 + copy 极性错 | wave595 sret 路径 `nest_off=next` + `load(nest+cop)`；x86 high-end lea(-nest)+foff 向 rbp 增长 | ✅ nest_off 抬升 nest_alloc；load `nest-cop`（arm64 仍 +cop） |
| `S[2]=[mk(),mk()]` 元素宽默认 4 | `pipeline_asm_array_lit_elem_byte_sz_c` TYPE_NAMED=8 落默认 4 | ✅ `glue_type_size_simple` 作 NAMED 布局权威 |

**数据流（五问）**：产生 = sret nest 帧分配/copy · ARRAY_LIT esz；存储 = 帧 temp / 数组槽；消费 = sret dest memcpy 字段 · INDEX 读。权威：G.7 补全既有 nest temp + elem_byte_sz（不新开第二套）。

**权威**：`compiler/pipeline_glue.c`（`pipeline_glue_standalone.o`）。改后 rm `build_asm/pipeline_glue_standalone.o` + g05。

### wave597 验证

| 探针 | mac L2 | Ubuntu L2 pure-asm |
|------|--------|---------------------|
| nest4 `mk().m.m.i.f+g` | ✅ 41 | ✅ 41（原 139） |
| ptr_nest_call `mk(&i).m.p.f+a` | ✅ 41 | ✅ 41（原 139） |
| arr_simple `xs[0].a+xs[1].b` | ✅ 22 | ✅ 22（原 10） |
| take_field / nest3 / ptr_mid | ✅ 41/41/45 | ✅ 41/41/45 |
| sret_field | ✅ 60 | ✅ 60 |
| nest4 mk disasm | — | nest@-0x28 · sret@-0x10 · copy `-0x28`/`-0x20` |
| arr_simple main | 常折叠 22 | `mov %rax,0/8(%rbx)` esz=8 |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/0/0 |

### 仍 soft

- freestanding ARRAY_LIT of **>8B** CALL struct（dual-GP / sret 逐元素）  
- 其它 Cap residual 叶（dyn Trait / XP003 等历史 soft）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 其它 soft 叶，或整波双端 L4 + 升钉  
2. 整波双端 L4 + 升钉  

## wave596（已闭 · Cap residual pointer intermediate FIELD_ACCESS + arm64 sret x8 save）

tip `5c982f705` / docs `feab1009f`。soft「Ubuntu nest4/arr_simple」本波由 **wave597** 收。
