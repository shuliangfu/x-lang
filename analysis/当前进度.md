# X 语言 / X language 当前进度（2026-07-29 · wave642 Cap freestanding ptr±int C-scale · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`0f3be5125`**（wave642 产品） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave642（已闭 · Cap residual freestanding ptr±int / ptr-ptr C-scale · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | freestanding `p+1` / `p-1` / `1+p` / `q-p` 错值；host-C 绿；pure-asm 常 CTFE 假绿；`*u8` esz=1 偶然绿 |
| tip 产品 | **`0f3be5125`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave642 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| `p+n` / `n+p` / `p-n` / `q-p` | freestanding ADD/SUB 对 GP 原值加减，无 `sizeof(*p)` 缩放 | ✅ `glue_try_emit_ptr_arith_scaled_elf_c`：offset×esz；ptr-ptr 后 idiv esz；复用 INDEX PTR peel |

**数据流**：产生 = typeck 合法 ptr±int / ptr-ptr（wave285）→ 存储 = resolved TYPE_PTR / isize → 消费 = freestanding ADD/SUB 须 C 语义缩放。G.7：补全同一 ADD/SUB 权威，不新开第二套算术。

**权威**：`compiler/pipeline_glue.c`（`glue_try_emit_ptr_arith_scaled_elf_c` + ADD/SUB 入口 + assign `p±=` scale）。

### wave642 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `p+1` *i32 | ✅ 42 | ✅ 42 |
| `p-1` *i32 | ✅ 42 | ✅ 42 |
| `1+p` | ✅ 42 | ✅ 42 |
| `q-p` element count | ✅ 2 | ✅ 2 |
| `p+n` var offset | ✅ 42 | ✅ 42 |
| `p+1` *u8 | ✅ 42 | ✅ 42 |
| `p+1` *S (8B) | ✅ 42 | ✅ 42 |
| `p = p + 1` | ✅ 42 | ✅ 42 |
| wave641 `&(*p)` 回归 | ✅ 42 | ✅ 42 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |
| disasm | `mov #4` + add | `mov $0x4` + add |

### 仍 soft

- untyped `self`（wave493 非标准）  
- compound `p += 1` typeck（expected *i32 found i32；`p = p + 1` 已绿）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶，或整波 L4 + 升钉  

## wave641（已闭 · Cap residual freestanding `&(*p)` · L2）

tip 产品 **`1bc77f0bc`**。ADDR_OF of EXPR_DEREF 复用 lvalue_eff_addr。

## wave640（已闭 · Cap residual freestanding nested `**pp` · L2）

tip 产品 **`92c07bfaa`**。`&p` lea + DEREF TYPE_PTR load 8。

## wave639（已闭 · Cap residual freestanding DEREF FIELD INDEX · L2）

tip 产品 **`2b274afdc`**。freestanding `(*p).xs[i]` INDEX/call-arg。

## wave638（已闭 · Cap residual host-C FIELD base primary · L2）

tip 产品 **`b1a8c6cfc`**。host-C `((base).field)` 修 `(*p).v` BLD001。
