# X 语言 / X language 当前进度（2026-07-28 · wave611 Cap ARRAY_LIT INDEX · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`1c4a631e1`**（wave611 Cap ARRAY_LIT INDEX parse+typeck · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave611（已闭 · Cap residual ARRAY_LIT INDEX parse+typeck · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `return [10, 32][1]` → XP003 out_len=0（host/fs 同；`let a: i32[2]=[10,32]; a[1]` 已绿） |
| tip 产品 | **`1c4a631e1`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave611 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| ARRAY_LIT 后无 postfix | `parser_asm_primary_slice.inc` LBRACKET 成功后 early return | ✅ 复用 `parser_asm_primary_ident_suffix_loop_c` |
| untyped ARRAY_LIT 作 INDEX 基 | `typeck_check_expr_array_lit` 不 stamp → base_ty=0 → T001 | ✅ 齐次元素推断 `TYPE_ARRAY[N]` |

**数据流（五问）**：产生 = primary `[e0,…]` + 后缀 `[i]`；存储 = EXPR_ARRAY_LIT + EXPR_INDEX；消费 = typeck INDEX + fs/host emit。旧路径：(1) primary 不进 suffix → 整函数 parse skip；(2) array_lit 故意不 stamp（仅 let/return coerce）→ INDEX 拒 base。权威：G.7 补全同一 primary suffix 权威 + 同一 array_lit typeck 权威；g05 监视 `primary_slice.inc` mtime。

**权威**：`compiler/seeds/parser_asm/parser_asm_primary_slice.inc` · `compiler/src/typeck/typeck.x` + `seeds/typeck_gen.linux.x86_64.c` · `g05_ensure_relink_prereqs.sh`。改后 rm `parser_asm_thin_glue.o` + `typeck_x.o` + g05。

### wave611 验证

| 探针 | mac L2 host/fs | Ubuntu L2 host/fs |
|------|----------------|-------------------|
| lit_idx `[10,32][1]` | ✅ 32/32（was XP003） | ✅ 32/32 |
| lit_idx0 `[10,32][0]` | ✅ 10/10 | ✅ 10/10 |
| lit_sum `[…][0]+[…][1]` | ✅ 42/42 | ✅ 42 / **soft fs**（rbx 互踩） |
| lit_idx_dyn / paren_idx | ✅ 32/32 | ✅ 32/32 |
| lit_call_arg / let_idx | ✅ 32/32 | ✅ 32/32 |
| take_field / lit_arr_field 回归 | ✅ 32/32 | ✅ 32/32 |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/0/0 |

### 仍 soft

- Ubuntu freestanding **同式双** ARRAY_LIT INDEX 作 binop 操作数（`[10,32][0]+[10,32][1]`）：left/right 结果与 INDEX 基址争用 **rbx**（disasm 可见第二叶 `mov %rax,%rbx` 盖掉第一叶结果）；`let a=…; let b=…; a+b` 已绿  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual soft：Ubuntu fs dual ARRAY_LIT INDEX binop spill，或其它 Cap 叶，或整波 L4 + 升钉  

## wave610（已闭 · Cap residual freestanding FIELD TYPE_ARRAY as T[N] call-arg · L2）

tip 产品 **`d742a7ec7`** / docs **`9d3138f7a`**。soft「ARRAY_LIT 基 INDEX XP003」本波由 **wave611** 收口。

## wave609（已闭 · Cap residual freestanding INDEX on STRUCT_LIT/CALL array field · L2）

tip 产品 **`e641caa4c`** / docs **`d4452217c`**。
