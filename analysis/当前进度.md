# X 语言 / X language 当前进度（2026-07-27 · wave532）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`bf27b82ad`**（双端 L2，wave532；L4 待整波收口） |
| 钉盘 | **`d5de2f13b`**（升自 `2bc0885d3`；双端 L4 129 全绿） |
| bstrict | **129** |

## wave532（已闭 · runtime_std_runtime_fast Makefile R2 DIRECT 补全 双端 L2）

| 项 | 状态 |
|----|------|
| 触发 | wave531 闭后继续迁移 wave530 审计发现的 3 个真 half-done 模块之一；`runtime_std_runtime_fast` 是最小的一个（6 函数 thin wrapper） |
| 模块 | `runtime_std_runtime_fast`（产物 `../std/runtime/runtime.o`；6 函数：`std_runtime_crash_evidence_collect` / `runtime_crash_evidence_collect_c` / `std_runtime_runtime_panic` / `runtime_panic` / `std_runtime_runtime_abort` / `runtime_abort`） |
| 债 | `.x`（src/asm/runtime_std_runtime_fast.x）已含 6 个 `#[no_mangle]` 完整实现 + 2 个 `extern "C"` 声明（xlang_panic_ / xlang_crash_evidence_collect_c）；`.from_x.c` 已有 `XLANG_RUNTIME_STD_RUNTIME_FAST_FROM_X` 守卫；**Makefile 规则仅冷路 cc -c + ld -r**（且 .x 未列入依赖） |
| 根修 | Makefile 加 R2 DIRECT 分支：`XLANG_G05_PREFER_X_O=1` 时用 `xlang-c -lib-name "" -o` 直接编译 .x → runtime_core.o，再 `ld -r` 链接成 `../std/runtime/runtime.o`；冷路 fallback 保留 |
| 构建模式 | **DIRECT**——6 函数全为 thin wrapper，转发到 `extern "C" xlang_panic_` / `xlang_crash_evidence_collect_c`（由 runtime_panic 模块在链接期解决）；无 OS 依赖、无 `_impl` bridge、无 C-only 函数 |
| nm 一致性 macOS | cold 6 T == R2 DIRECT 6 T 符号（**完全一致**，无 doc_anchor 差异，因 .x 无 doc_anchor 函数） |
| nm 一致性 Ubuntu | cold 6 T == R2 DIRECT 6 T 符号（同上；macOS/Ubuntu 完全一致） |
| 产品矩阵 macOS | run-hello.sh ✅ exit 0 · g-ffi-5-std-wrap ✅ exit 0 · g-ffi-5-release-ci ✅ exit 0 |
| 产品矩阵 Ubuntu | run-hello.sh ✅ exit 0 · g-ffi-5-std-wrap ✅ exit 0 · g-ffi-5-release-ci ✅ exit 0 |
| 验证级别 | **L2 日常真测**（按自举方法 §0.2 节奏；非每微 commit 全量 L4） |
| PLATFORM | SHARED runtime — pure thin wrappers to panic；无 OS 依赖；macOS + Ubuntu 双端验证 |
| 关键发现 | ① `runtime_std_runtime_fast` 与 wave531 path_fast 同类的 DIRECT 模式（无 OS 依赖） ② **首次出现 cold 与 R2 DIRECT 符号完全一致（无 doc_anchor 差异）**——前几个 R2 模块都多 1 个 doc_anchor，本模块 .x 没有该函数故无差异 ③ xlang-c backend 对同 TU 内互调（`runtime_crash_evidence_collect_c` → `std_runtime_crash_evidence_collect`）会生成一个额外 U 符号，但链接期由本 TU strong 定义解决，无回归 ④ `extern "C"` 声明在 xlang-c `-E` 后能正确 emit 为 C 风格声明，与其他 thin wrapper 模式一致 |

## 下一步候选

1. **`runtime_lsp_glue`**（60 `#[no_mangle]`，量较大，需评估迁移风险）— wave530 审计发现的最后一个真 half-done 模块
2. **整波收口 L4**：当前累积 wave528-532 共 4 个 R2 migration（runtime_http_glue + runtime_slice_glue + runtime_path_fast + runtime_std_runtime_fast），可按 §0.2「整波结束做一次扎实 L4」+ 日终兜底触发 L4 真冷全测双端 129 bstrict
3. **钉盘升级候选**：若 L4 双端 129 全绿，可升钉自 `d5de2f13b`
