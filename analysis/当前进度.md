# X 语言 / X language 当前进度（2026-07-28 · wave609 Cap INDEX on STRUCT_LIT/CALL array field · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`e641caa4c`**（wave609 Cap freestanding INDEX on STRUCT_LIT/CALL array field · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave609（已闭 · Cap residual freestanding INDEX on STRUCT_LIT/CALL array field · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `Wrap{xs:[10,32]}.xs[1]` / `mk().xs[1]` → freestanding SIGSEGV（host-C 绿） |
| tip 产品 | **`e641caa4c`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave609 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| fs INDEX base = FIELD on STRUCT_LIT/CALL | `glue_try_index_var_or_field_base_*` 仅 VAR；回落 emit_expr 走 call_base load | ✅ leave_addr=1 物化后留字段槽地址 |

**数据流（五问）**：产生 = INDEX base=`FIELD_ACCESS`（根 STRUCT_LIT/CALL/METHOD，字段 TYPE_ARRAY）；存储 = 帧 temp（wave608 物化）；消费 = INDEX lit 路径 base→rax 后 add imm*esz。旧路径：base 非 VAR → `emit_expr` 对 FIELD 走 leave_addr=0 → **load 首元**当指针 → add+load → SEGV。权威：G.7 补全同一 `glue_field_access_call_base_rvalue_elf_c`（`leave_addr`）+ INDEX base helper 转调，不新开第二套 INDEX 物化。

**权威**：`compiler/pipeline_glue.c` · `glue_field_access_call_base_rvalue_elf_c`（leave_addr）· `glue_try_index_var_or_field_base_to_{rax,rbx}_elf_c`。改后 rm `pipeline_x.o` + g05。

### wave609 验证

| 探针 | mac L2 host/fs | Ubuntu L2 host/pure/fs |
|------|----------------|------------------------|
| lit_arr_field `Wrap{…}.xs[1]` | ✅ 32/32（was fs 139） | ✅ 32/32/32 |
| lit_arr_field0 `.xs[0]` | ✅ 10/10 | ✅ 10/10/10 |
| call_arr_field `mk().xs[1]` | ✅ 32/32 | ✅ 32/32/32 |
| nest_lit_arr `Outer{…}.w.xs[1]` | ✅ 32/32 | ✅ 32/32/32 |
| let_arr_field 回归 | ✅ 32/32 | ✅ 32/32/32 |
| lit_field / lit_method 回归 | ✅ 40/42 | ✅ 40/42 |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/0/0 |

### 仍 soft

- `take(Wrap{…}.xs)`：TYPE_ARRAY 字段作 call-arg（非 INDEX；mac fs 仍 139）  
- ARRAY_LIT 基 INDEX（`[10,32][1]` XP003）  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual soft：`FIELD`→TYPE_ARRAY call-arg，或 ARRAY_LIT INDEX，或整波 L4 + 升钉  

## wave608（已闭 · Cap residual freestanding STRUCT_LIT.field rvalue · L2）

tip 产品 **`d6ac2e362`** / docs **`bfbf026b6`**。soft「fs INDEX on STRUCT_LIT array field」本波由 **wave609** 收口。

## wave607（已闭 · Cap residual STRUCT_LIT postfix method/field · L2）

tip 产品 **`cfa113ffc`** / docs **`042d52b1b`**。

## wave606（已闭 · Cap residual arm64 freestanding UFCS method MEMORY · L2）

tip 产品 **`d4e63a37d`** / docs **`17186ce40`**。
