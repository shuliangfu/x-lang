# X 语言 / X language 当前进度（2026-07-24）

> **长期仪表盘**：[自举进度.md](自举进度.md)（24 日起重新起表）  
> **23 号全日归档**：[自举进度-归档-2026-07-23.md](自举进度-归档-2026-07-23.md)  
> **重命名分析**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)  
> **项目目录**：现行 `…/xlang/x-lang`  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| 工程 | **双端 tip L4 129 全绿** · 钉盘 **`ace59b6cd`** |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **已恢复** · tip L4 升钉后可继续 Cap residual |

## 验收

### tip L4 钉盘（**已升**）

| 项 | 状态 |
|----|------|
| SHA | **`ace59b6cd`**（wave363：NAMED score 64B + T[N]=0 + typeck u64 对齐） |
| 构建级别 | **L4 真冷**（全擦 compiler/std/core `.o` + 重链产品二进制） |
| remaining_o 起编 | ✅ **0**（双端） |
| 产品矩阵 | ✅ rv=42 · option · hello |
| mac 全量 bstrict | ✅ **OK (129)** · `/tmp/mac_true_cold_ace59b6cd.log` · `/tmp/mac_true_bstrict_ace59b6cd.log` |
| Ubuntu 全量 bstrict | ✅ **OK (129)** · `/tmp/ubuntu_true_cold_ace59b6cd.log` · `/tmp/ubuntu_true_bstrict_ace59b6cd.log` |
| 前钉 | `75c638465`（wave288；其后 70+ 波 L2 堆债 → 本波 L4 收口） |

### 本波根修（升钉内容）

| SHA | 摘要 |
|-----|------|
| `18f7ba42d` | typeck：u64↔usize 负例→正例（对齐 wave312） |
| `63ebeacf7` | freestanding `T[N]=0` 零填（array CG002） |
| **`ace59b6cd`** | **strict_minimal NAMED score `pnm`/`anm` 8→64**：`named_name_into` 固定 memcpy 64B；wave313 8B 缓冲 → `string.len(String)` 栈粉碎 / `__stack_chk_fail` / exit 127 |

### wave364（本步 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`d0dc16218`** |
| 债 | bare **`if b {}` / `while b {}`** 空体 XP003 |
| 根修 | G.7 `parser_asm_primary_empty_ident_braces_prefer_block_c`：空 `IDENT{}` 按 `}` 后 token 消歧（值位置 `;),].as` 仍 `S {}`；else 留 `{` 给 then/while） |
| 构建级别 | **L2**（重建 `parser_asm_thin_glue.o` + g05；**非 tip L4**） |
| 钉盘 | 仍 **`ace59b6cd`** |
| 探针 | 双端 empty_if/while/else/bare/if_asg=42；`S {}` -c typeck OK；矩阵 rv/option/hello |
| soft | untyped-let；reentrancy；`mut`；trait/impl；AST 升布局；host empty-struct BLD001 |

```text
下一：wave365 Cap residual（日常 L2）
  leave-off：untyped-let / reentrancy / mut / trait…
  钉盘 ace59b6cd；约每 10–15 波或日终 tip L4
```
