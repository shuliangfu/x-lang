# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave160** compiler_o_path_copy pure @ `4bc90d1f5` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **R2 Cap residual pure · path residual L0**（`shux_runtime_compiler_o_path_copy`） |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：path residual 可迁叶 — `shux_runtime_compiler_o_path_copy` hybrid 下仍 always mega C（resolve + snprintf join）  
2. **根修**：迁入 `labi_path_pure` L0 pure orch（Cap residual `shu_resolve_compiler_dir` + pure 字节 join `compiler-dir/leaf`，无 snprintf）  
3. **权威**：`.x` + cold twin + surface re-pin（count 16→17）+ mega `#ifndef PATH_PURE_FROM_X`；prove **IDENTICAL**  
4. **规则**：null/empty leaf/out 或 out_sz=0 → -1；resolve 失败 → -1；`dn+1+ln >= out_sz` → -1 并清 out；else 写 `dir/leaf` + NUL → 0（≡ 历史 mega snprintf）  
5. **验收**：双端 L2 g05 + 矩阵 + string/vec/slice/compress 邻域 + prove IDENTICAL；钉盘仍 **`3c88f7e51`**  

## 下一波前排

1. 其它 rest R2 / mega residual（可迁：其它 path residual `*_o_path` 静态缓冲叶 / freestanding ensure·path residual / repo_root …；常驻 Cap 不硬迁：contains_substr / undef_sym / path_io stat / wait / strerror / mega spawn-IO / ld_debug_argv / invoke_cc_argv_push_existing realpath 池 / ensure_std_net_o_auto_tls system·make / getenv / shu_resolve_compiler_dir …）  
2. 可选：未闭合块注释硬诊断（EOF depth>0）  
3. 可选 Windows hybrid（**需要时再通知重启**）  

```text
钉盘：3c88f7e51（未升）
tip：4bc90d1f5 wave160 compiler_o_path_copy pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：其它 path residual / freestanding ensure·path residual / 其它 rest R2
```
