# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`1f73ed275`**（双端 tip L4 真冷 + 129；未升 — 本波日常 L2） |
| tip 产品代码 | **pipeline_abi wave95 resolve/read/pp pure orch** @ `b498928e` |
| 产品 bstrict | **129** · 钉盘 tip L4 全绿；wave95 **日常 L2** 矩阵绿（未全量 bstrict） |
| 本波性质 | **wave95 resolve_path_x + read_file_x + preprocess_loaded Cap residual 真迁 pure orch** |
| Ubuntu | ✅ L2 g05 hybrid pure **W**（resolve/read/pp size≡pure）+ 矩阵 rv42/option/si/hello + nest #if/-D + force_c @ `b498928e` · `/tmp/ubuntu_g05_wave95.log` · `/tmp/ubuntu_g05_wave95_matrix.log` |
| mac | ✅ L2 g05 hybrid pure resolve/read/pp orch + 矩阵 rv42/option/si/hello + nest #if/-D + force_c @ `b498928e` · `/tmp/mac_g05_wave95.log` · `/tmp/mac_g05_wave95_matrix.log` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave95** @ 产品 **`b498928e`**  
   - `pipeline_resolve_path_x`：G.7 真迁 pure orch（Cap residual try_one_lib_root / try_entry_dir / loop_should_continue）  
   - `pipeline_read_file_x`：G.7 真迁 pure thin（`shux_read_file_into_path` + set_loaded_len）  
   - `pipeline_preprocess_loaded_into_ctx`：G.7 真迁 pure orch（`preprocess_x_buf` + pure preprocess_len store）  
   - Cap residual 仍：`pipeline_parse_into_buf`；typeck merge+wpo / `parser_copy_module_import_path64`  
   - 闭合 pure load_import 下 **resolve / read / preprocess 硬 Cap residual** 叶  
   - glue/ast_pool 同名 **SHUX_WEAK** 冷 twin（须重建 `pipeline_x.o` + `pipeline_glue_standalone.o`）  
   - Cap residual 仍：parse_into_buf；typeck merge；cfg_eval 复杂 #if；g05 &fn cast  
   - **构建级别 L2**（日常真测；非 tip L4）  
2. 继承 tip L4 钉盘 **`1f73ed275`**（wave76–81 收口；wave82–95 未升钉）

## 下一波前排

1. rest R2：pipeline_abi **硬 Cap residual** 子叶（parse_into_buf；typeck merge+wpo 已 typeck 权威；cfg_eval 复杂；g05 &fn cast）  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 硬 Cap / soft 再有整波收口时再 tip L4（非每微步）

```text
钉盘：1f73ed275（tip L4 双端 129；本波未升）
产品 tip：b498928e pipeline_abi residual pure wave95 resolve/read/pp orch
文档：21 号归档 · 22 号 wave76–95 + tip L4 升钉 @1f73ed275
Windows：本波不需要切换
下一：pipeline_abi 硬 Cap 子叶（parse_into_buf / cfg_eval / &fn）或整波 tip L4
```
