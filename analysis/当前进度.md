# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（本波未改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`11a57351`**（双端 129） |
| tip | **`b06cf472`**（R2 `async_liveness` Cap residual pure type/layout） |
| 产品 bstrict | **129** · 钉盘双端全绿 |
| 本波性质 | Cap residual pure：liveness type/layout/has_await/needs_cps（无 FILE*） |
| 工程轨 prove | ✅ mac IDENTICAL · Ubuntu IDENTICAL（L2） |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `async_liveness` Cap residual pure（type/layout）**（`b06cf472`）  
   - `.x`：`lookup_var_type` / `type_to_c_buf` / `type_size_bytes_module` / `layout_func*` /  
     `func_has_await` / `func_needs_cps_frame` / `expr_has_await` / `analyze_func` / `module_struct_in_frame`  
   - helpers：`async_live_store_i32/ptr` / `cstr_copy_cap` / `sort_frame_names`（insertion，免 qsort）  
   - 宿主布局：ASTType/Param/Let/Func/Block/Module/StructDef/Impl + AsyncFrameLayout（4196）  
   - 栈纪律：`analyze_block_linear` malloc(4096)；layout 临时帧 malloc(4196)（防 -E SIGSEGV）  
   - seed：`#ifndef SHUX_ASYNC_LIVENESS_FROM_X` 省略 pure residual；FILE* `emit_*` 始终 seed  
   - surface re-pin（`-E`）+ prove MODULES 注释  

2. 验证  
   - mac：`SHUX=./compiler/shux bash tests/prove_module_selfhost.sh async_liveness` → IDENTICAL · `/tmp/mac_prove_async_liveness_cap_pure.log`  
   - Ubuntu：pull tip 后 prove IDENTICAL · `/tmp/ubuntu_prove_async_liveness_cap_pure.log`  
   - **日常 L2 非 tip L4**；钉盘仍 `11a57351`  

3. 前序：CPS Cap residual pure wave2 @ `ec2336da` · wave1 `expr_is_*` · liveness pure surface · pool · 钉盘 `11a57351`

## 下一波前排

1. **CPS Cap residual 余**：walk `_impl`（typed AST）· FILE* emit（begin/end/after_await/sched）  
2. **或** liveness FILE* emit pure（fprintf 族；需 FILE* 桥）  
3. **可选** tip 升钉：Ubuntu L4 + 双端 129  
4. **可选** Windows hybrid（需切双系统时再喊人）  

```text
钉盘：11a57351
tip：b06cf472（liveness Cap residual pure type/layout；双端 L2 prove IDENTICAL）
Windows：本波不需要切换
下一：CPS walk_impl / FILE* emit / 可选 tip L4
```
