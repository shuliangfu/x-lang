# Shux 当前进度（2026-07-19）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **归档**：[自举进度-归档-2026-07-18.md](自举进度-归档-2026-07-18.md) · [17 号](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`4214eea4`**（fix(abi) 波 1 unsafe 包裹 9 处；ABI 边界 → 升钉） |
| 产品 L4 放行钉盘 | **`4214eea4`**（双端真冷 L4+125；前钉 `5cc73d54`；本波 ABI 边界 → 升钉） |
| 双端 L4 兜底（本日） | ✅ 双端全绿（`4214eea4` SHA：mac log `/tmp/mac_true_bstrict_7e507fb6.log` · Ubuntu log `/tmp/ubuntu_true_bstrict_4214eea4.log`）；详见 [自举进度.md](自举进度.md) §6 |
| 产品 bstrict 套件 | **125** |
| 本波性质 | **C6 X ABI P0b 波 1**（unsafe 包裹修复 9 处 · ABI 边界 → L4 双端 + 升钉 + Windows 门禁） |
| 本波输出 | `compiler/src/main.x` L670-695 6 处 + `compiler/asm_libroot/std/fs/mod.x` L22-39 3 处 unsafe 包裹（expression form + statement form，与 `ws_io.x` / `fs_mod_close` 风格一致） |

## 本波摘要（2026-07-19 · C6 X ABI P0b 波 1 · unsafe 包裹修复 9 处 · 升钉）

| 项 | 结果 |
|----|------|
| **入口** | 推进 [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.2 波 1 推荐执行方案 + [自举进度.md](自举进度.md) §3 前排 #3 波 1 |
| **改动** | `compiler/src/main.x` L670-695 共 **6 处**（`fs_posix_write_c` ×4 + `fs_posix_close_c` ×2）+ `compiler/asm_libroot/std/fs/mod.x` L22-39 共 **3 处**（`open`/`read`/`write` 函数体）= **9 处** unsafe 包裹 |
| **风格** | main.x 用 expression form `let x = unsafe { expr };` + statement form `unsafe { stmt; }`（与 `std/net/ws_io.x:102,123` 一致）；mod.x 用 statement form `unsafe { return ...; }`（与同文件 L17-19 `fs_mod_close` 一致） |
| **契约依据** | P0 契约违反修复：`typeck.x:289 pipeline_typeck_check_extern_call_unsafe_boundary_c` 已声明但未激活（P0a 语义降级）；P1 激活会断链。C ABI unsafe 是语义契约（C 可能修改 SHUX 内存 / 指针有效性 / 所有权转移），非 codegen 问题，IR 无法自动处理（4 个不可判定问题） |
| **波次** | 波 1 of 3（audit `7e507fb6` → **波 1 `4214eea4`** → 波 2 ABI 一致性 3 处 → 波 3 std/ ~557 远期） |
| **mac L4 真冷** | ✅ 全绿：`bootstrap-driver-seed OK` + `g05_prepare_and_relink OK (shux_asm OK)` + `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0；log `/tmp/mac_true_bstrict_7e507fb6.log`（注：日志名沿用 worktree SHA 7e507fb6，因 bstrict 跑时 commit 已 push 但 worktree 本地 SHA 计算时机差；实际验证的是 4214eea4 commit 后的代码） |
| **Ubuntu L4 真冷** | ✅ 全绿：`bootstrap-driver-seed OK` + `g05_prepare_and_relink OK` + `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0；log `/tmp/ubuntu_true_bstrict_4214eea4.log` |
| **升钉决策** | ✅ **升钉** `5cc73d54` → `4214eea4`（unsafe 契约改动属 ABI 边界，per [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.2 + [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md) §5.1） |
| **Windows 门禁** | 🟡 待跑（ABI 边界改动 per §5.1 强制重跑 Windows hybrid 最小门禁 `tests/run-bootstrap-bstrict-windows-gate.sh`；非阻塞升钉，但需在波 2 收口前补跑或与本波一起补跑） |
| **本波 commit** | `4214eea4` fix(abi): wrap 9 unsafe C-ABI calls in main.x and std/fs/mod.x（2 文件 +9/-9 行） |

## 下一波前排（按优先级排序）

### 1. C6 X ABI P0b 波 2：ABI 一致性修复（3 处 · ABI 边界 · 触发 Windows 门禁）

按 [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.3 推荐执行：
- `compiler/src/runtime_io_abi.x` L164/176/188 三个函数定义 → 改为 `export extern "C" function`（与 `main.x:30-31` / `pipeline.x:81` / `driver/emit.x:71` 声明一致）
- 函数体内 libc `write`/`close`/`read` 调用包裹 `unsafe { }`（如尚未包裹；正面对照 `runtime_io_abi.x:98-105` 已用 `unsafe { let r = close(fd); return r; }` 模式）
- 验证级别：**L4 双端真冷**；触发 Windows 门禁；**必须升钉**

### 2. Windows 门禁补跑（波 1 遗留 · ABI 边界）

按 [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md) §5.1 强制要求：
- ssh `windows-server` → `set TEMP=C:\shux_tmp` + `set CC=gcc` → `./tests/run-bootstrap-bstrict-windows-gate.sh`
- 验证波 1 unsafe 包裹在 Windows MSYS2/MinGW 下无回归
- 可与波 2 一起跑（同属 ABI 边界）

### 3. C7 plan 瘦身（工程债 · 非产品）

plan-only bstrict 模板裁剪（按 `tests/run-all-bstrict.sh` plan 子集）

## 不在前排

- **C6 X ABI P0b 波 3：std/ 扩展标注**（远期 · 多波 · 顺延自举后 P1-P2）：~557 个 C 库包装分文件标注 `extern "C"`（http 249 / socketio 106 / async 42 / json 37 / math 32 / sqlite 32 / atomic 30 / arrow 29）；按文件大小递增推进，`std/atomic/mod.x`（30 个）作 PoC 验证 typeck 强制 unsafe 后无回归再扩展；详见 [C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §4 / §5.4
- **shux-x 5GB 方案 B**（远期 · 非 KPI）：为 ArenaSidecar/ModuleSidecar 加 release API；前提 typeck 完成后 dep_arenas 不再被访问；方案 C/D 详见 [shux-x-5GB-内存根因调查.md](shux-x-5GB-内存根因调查.md) §6
- **Cap C9 match guard full-chain 支持**（独立大工程 · 多波）：6 个产生点改动，先排期再动手
- **C5 EXPR_CLOSURE CTFE**（独立大工程 · 多波）：已 scoping（[C5-EXPR_CLOSURE-CTFE-scoping.md](C5-EXPR_CLOSURE-CTFE-scoping.md)），顺延自举后立项 `lang-closure-v1-rfc.md`
- **历史进度**：C6 P0b audit（`7e507fb6` docs-only）/ shux-x 5GB 方案 A（`a14a1dd0`）/ 日终 L4 兜底（`94a8e7d5`）/ C5 三连击（`c174e70a`）/ 4 波 C5 CTFE / shux-x 5GB 调查 / Windows 时序决议 / C2/C3=C4/backtrace/C8 钉盘波，全部归档在 [自举进度.md](自举进度.md) §6 变更记录

```text
本波 tip：4214eea4 (fix(abi): wrap 9 unsafe C-ABI calls in main.x and std/fs/mod.x)
本波性质：C6 X ABI P0b 波 1（unsafe 包裹修复 9 处 · ABI 边界 → L4 双端 + 升钉 + Windows 门禁）
本波输出：compiler/src/main.x L670-695 6 处 + compiler/asm_libroot/std/fs/mod.x L22-39 3 处 unsafe 包裹
构建级别：L4 真冷双端（mac arm64 + Ubuntu x86_64）；SHUX_BSTRICT_SKIP_BUILD=1 run-all-bstrict 125 全绿 exit=0
产品 tip L4 钉盘：4214eea4（升钉自 5cc73d54；本波 ABI 边界改动 → 必须升钉）
Windows 门禁：待跑（ABI 边界改动 per §5.1 强制重跑 hybrid 最小门禁；非阻塞升钉，需在波 2 收口前补跑）

实测统计（2026-07-19 全量扫描 compiler/src + compiler/asm_libroot + std）：
  extern "C" function (C ABI):       3035 个（含合法 dispatch / runtime / libc 封装；Top 20 全部合法）
  extern function   (X ABI 默认):    2431 个（含合法 SHUX 桥接；std/ 下 ~557 个为 C 库包装待标注）
  extern "x" function (显式 X ABI):    0 个（语法槽位已就位，暂未使用）
  合计 extern 声明:                  5466 个

核心 14 个 C ABI 调用（设计文档 §3.2.2 定义）：
  parser.x:36-38 (3) ✅ + main.x:30-31 (2) ✅ + pipeline.x:81-82 (2) ✅ + driver/emit.x:71-73 (3) ✅ + asm_libroot/std/fs/mod.x:11-14 (4) ✅
  → 100% 已标注 extern "C"

调用点 unsafe 包裹违规 9 处：
  main.x L671/672/677/678/685/690 (6 处，driver_run_x_emit_c_set_path_and_run 函数体内未包裹 unsafe)
  asm_libroot/std/fs/mod.x L23/33/38 (3 处，open/read/write 三个封装函数直接 return 未包裹 unsafe)
  → P0 契约违反；当前 P0a typeck 未强制 unsafe 所以不断链；P1 激活会断链

声明/定义 ABI 不一致 3 处：
  main.x:30 (声明 extern "C") vs runtime_io_abi.x:188 (定义 export function，X ABI 默认) → fs_posix_write_c
  main.x:31 (声明 extern "C") vs runtime_io_abi.x:164 (定义 export function，X ABI 默认) → fs_posix_close_c
  pipeline.x:81 + driver/emit.x:71 (声明 extern "C") vs runtime_io_abi.x:176 (定义 export function) → fs_posix_read_c
  → P0a 语义降级下不断链；P1 激活 X ABI 调用约定后参数寄存器错位 / 运行期 UB

正面对照（合规模式）：
  parser.x: Cap-T001 whole-body unsafe FFI gate（~270 个 unsafe { } 块）
  driver/emit.x: thin unsafe wrap（每个 extern "C" 调用一个 unsafe { return ...; } 块）
  runtime_io_abi.x: 全部 extern "C" 调用都在 unsafe { } 块内

拆分策略 3 波：
  波 1（✅ 已完成 @ 4214eea4）: unsafe 包裹修复 9 处（main.x 6 + mod.x 3）→ L4 双端 + 升钉 + Windows 门禁（待补跑）
  波 2（下一波）: ABI 一致性修复 3 处（runtime_io_abi.x 定义改 extern "C"）→ L4 双端 + 升钉 + Windows 门禁
  波 3（顺延自举后 P1-P2）: std/ 库 ~557 个 C 库包装标注 extern "C"，按文件大小递增分波（std/atomic 30 个作 PoC）

风险：自举前必须完成波 1（✅）+ 波 2（待执行），否则 P1 激活 typeck 强制 unsafe + X ABI 调用约定时 bootstrap 陷阱

权威文档：analysis/C6-P0b-scope-audit.md（§0 摘要 + §1 实测统计 + §2 核心 14 审计 + §3 ABI 一致性 + §4 std/ 扩展 + §5 3 波拆分 + §6 风险评估 + §7 验证清单 + §8 调查方法 + §9 相关文档）
历史进度归档：analysis/自举进度.md §6 变更记录（C6 P0b audit 7e507fb6 / shux-x 5GB 方案 A a14a1dd0 / 日终 L4 兜底 94a8e7d5 / C5 三连击 c174e70a / 4 波 C5 CTFE / shux-x 5GB 调查 / Windows 时序决议 / C2/C3=C4/backtrace/C8 钉盘波）
```
