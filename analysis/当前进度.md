# X 语言 / X language 当前进度（2026-07-29 · wave619 Cap host-C scalar TYPE_SLICE complete · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`6c1262ee9`**（wave619 Cap host-C scalar TYPE_SLICE complete · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave619（已闭 · Cap residual host-C 全标量 TYPE_SLICE 标签补全 · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | host-C `bool[]`/`i64[]`/`u32[]`/`isize[]`/`i16[]`/`u16[]`/`i8[]` → BLD001 incomplete `struct xlang_slice_*`（wave618 仅补 f32/f64） |
| tip 产品 | **`6c1262ee9`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave619 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| preamble 缺多数标量 slice 布局 | `rt_preamble.from_x.c` | ✅ 全套：int/int8/16/32/64、uint8/16/32/64、size/ssize、float/double |
| NAMED i8/i16/u16 → `struct ast_*` 进 slice 名 | `type_to_c_repr_inner` | ✅ 映射 `int8_t`/`int16_t`/`uint16_t`（≡ scalar host 发射） |
| call-arg fat 手写 map 与 local 漂移 | codegen.x + seed | ✅ `type_to_c_repr(formal SLICE)` 单权威，删平行 map |

**数据流（五问）**：产生 = `type_to_c_repr` 拼 `xlang_slice_`+elemC；存储 = preamble 须有完整 struct；消费 = host-cc。G.7：**有则补全** preamble + NAMED map；call-arg **收敛**到 type_to_c_repr，禁止第三套 elem→suffix。

**权威**：`ast_pool.c`（type_to_c_repr）+ `rt_preamble` + `codegen.x`/seed 同 commit。改后 **须** `PIPELINE_X_FORCE_COMPILE=1` 重建 `pipeline_x.o` + `rt_preamble.o` + `codegen_x.o` + g05。

### wave619 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| host-C bool/i8/i16/u16/i32/u32/i64/u64/usize/isize/f32/f64/u8 `T[]` | ✅ 42（tag 对齐） | ✅ 42 |
| host-C call-arg bool/i16/u32/i64/f32 | ✅ 42 | ✅ 42 |
| pure-asm i32[] / i64[] let / i16 call | — | ✅ 42 |
| pure-asm f32[] / f32[2] cast sum | soft 0 | soft 0 |
| pure-asm `sum(i64[])` array-lit call | — | soft 10 |
| rv / option / si / hello | ✅ | ✅ |

### 仍 soft

- pure-asm / freestanding **f32** INDEX/cast 载荷（host-C 绿）  
- pure-asm **i64[] call-arg** fat（local i64[] 绿；host-C call 绿）  
- 其它 Cap residual 叶；整波双端 **L4** + 升钉  

### 下一波

1. Cap residual soft：pure-asm f32 或 i64[] call-arg，或整波 L4 + 升钉  

## wave618（已闭 · Cap residual f32[]/f64[] host-C TYPE_SLICE C 名 · L2）

tip 产品 **`7a943902f`** / docs **`551d34837`**。

## wave617（已闭 · Cap residual f32 ARRAY_LIT typeck stamp · L2）

tip 产品 **`1a11a6a72`** / docs **`6f5fe2205`**。
