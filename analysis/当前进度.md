# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **wave42 driver_abi Cap residual pure** @ `8e5c2286` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **residual pure 微波**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42 @ `8e5c2286`（pure 符号 **W**） |
| mac | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42 @ `8e5c2286` |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 driver_abi Cap residual pure wave42**  
   - `driver_exec_compiled_body` pure orch：null/argc 守卫 + G.7  
     `driver_exec_path_is_non_exe`（`rt_run_exec.x` 权威，不另开第二套）  
   - Cap residual always-seed：  
     `shux_driver_exec_scan_out_path_opaque`（`*u8`→`char**` cast + scan）  
     + `shux_driver_exec_spawn_wait`（WINDOWS spawnvp / POSIX fork+execv+wait）  
   - seed cold twin `#ifndef SHUX_L2_RDABI_THIN_FROM_X`；无 pure-dup  
   - **仍 always-seed OS residual**：`sibling_try_spawn` / `print_usage_write`（巨型 usage 字串）  
   - 构建级别：**日常 L2**（非 tip L4）；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave42.log` · `/tmp/ubuntu_g05_wave42.log`；Ubuntu pure 符号 **W**

2. **注释纪律**  
   - 新 pure 方法 JSDoc（`@param` type+role；`*/` 单独成行）

3. **文档纪律**  
   - 只改 `自举进度.md` + 本文；**不**改 `当前问题分析.md` / `问题分析文档.md`

## 下一波前排

1. Cap residual 下一刀：**sibling_try_spawn / print_usage_write** 仍 always-seed（path+fork / 巨型 usage 字串）；可拆 color orch 或 sibling path pure；若无可 pure 面则切其它 rest R2  
2. **可选** Windows hybrid；Darwin arm64 纯 asm / Vec4f CG002  
3. 其它 rest R2（非每微步 L4；下一整波收口再 tip L4）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave42 未升钉）
产品 tip：8e5c2286 wave42 L2
Windows：本波不需要切换
下一：sibling/usage always-seed 或其它 rest R2
```
