# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **wave39 driver_abi Cap residual pure** @ `7ab9a80b` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **residual pure 微波**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42 @ `7ab9a80b`（pure 符号 **W**） |
| mac | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42 @ `7ab9a80b` |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 driver_abi Cap residual pure wave39**  
   - `driver_stdio_stdout`（G.7 g05 `shux_driver_stdout_ptr`）  
   - `driver_asm_fwrite`（null/len 守卫 pure；stdout 门控 + g05 `shux_driver_fwrite_opaque`）  
   - `driver_x_emit_fwrite_stdout`（null/len 守卫 pure；Cap OS residual `shux_driver_fwrite_stdout_n` 藏 fwrite+fflush 与字节数返回 ABI）  
   - seed cold twins `#ifndef SHUX_L2_RDABI_THIN_FROM_X`；无 pure-dup  
   - **仍 always-seed OS residual**：`sibling_try_spawn` / fopen 族 / fflush / write_metric / usage / `exec_compiled_body` / stderr 等  
   - 构建级别：**日常 L2**（非 tip L4）；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave39.log` · `/tmp/ubuntu_g05_wave39.log`；Ubuntu pure 符号 **W**

2. **注释纪律**  
   - 新 pure 方法 JSDoc（`@param` type+role；`*/` 单独成行）

3. **文档纪律**  
   - 只改 `自举进度.md` + 本文；**不**改 `当前问题分析.md` / `问题分析文档.md`

## 下一波前排

1. Cap residual 下一刀：driver_abi rest 若还有 pure-eligible 再拆；**fopen/mkstemp/fflush/write_metric/sibling/usage/exec 仍 OS residual always-seed**  
2. **可选** Windows hybrid；Darwin arm64 纯 asm / Vec4f CG002  
3. 其它 rest R2（非每微步 L4；下一整波收口再 tip L4）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave39 未升钉）
产品 tip：7ab9a80b wave39 L2
Windows：本波不需要切换
下一：OS residual always-seed 或其它 rest R2
```
