# X 语言 / X language 当前进度（2026-07-29 · wave651 Cap freestanding FIELD over INDEX call-arg · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 05:59 · 28 全日（wave534–617）已归楼；本文仅保留 29 日 tip 快照

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`a9256927e`**（wave651 FIELD over INDEX as T[N]/[]T call-arg） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave651（已闭 · Cap residual freestanding `m[i].xs` call-arg · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `take(m[0].xs)` / `take(m[i].xs)` freestanding SEGV（host-C 42；`take(w.xs)` VAR 绿；wave610 STRUCT_LIT/CALL 绿） |
| tip 产品 | **`a9256927e`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave651 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| FIELD over INDEX 未进 leave-addr helper | `glue_try_index_var_or_field_base_to_{rax,rbx}` | ✅ ko=47 基：INDEX eff_addr + field_off |

**数据流**：产生 = helper 仅 VAR/DEREF/call_base → `m[i].xs` 回落 field rvalue load 首元。存储 = 错误 E*（i32 位当指针）。消费 = callee INDEX → SEGV。G.7：同一 helper 补全；不新开第二套 FIELD 数组路径。

**权威**：`compiler/pipeline_glue.c` · `glue_try_index_var_or_field_base_to_rax_elf_c` + rbx 孪 · wave610/649 call-arg 复用

### wave651 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `take(m[0].xs)` T[N] | ✅ host/fs/asm=42 | ✅ 42 |
| `take(m[i].xs)` / `m[1]` / slice []i32 | ✅ 42 | ✅ 42 |
| wave610 `w` / lit / `mk()` 回归 | ✅ 42 | ✅ 42 |
| pad 多字段 `W{pad,xs}` | ✅ 42（arm64） | soft host=42 fs/asm=0 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

日志：`/tmp/mac_g05_wave651.log` · `/tmp/ubuntu_g05_wave651.log` · `/tmp/ubuntu_w651_probes.log`

### 仍 soft

- untyped `self`（wave493 非标准）  
- pad 多字段 STRUCT_LIT 在 Ubuntu high-end 数组元布局（host-C 绿）  
- `break` / `continue` freestanding 假跑（扫叶顺带发现）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（pad 布局 / break·continue / 其它），或整波 L4 + 升钉  

## wave650（已闭 · Cap residual if/while cond 整式分组 · L2）

tip 产品 **`d5aec6a64`**。if/while 条件不剥前导 `(`。

## wave649（已闭 · Cap residual freestanding FIELD STRUCT_LIT/CALL → []T · L2）

tip 产品 **`84f9b813c`**。freestanding FIELD STRUCT_LIT/CALL as TYPE_SLICE。
