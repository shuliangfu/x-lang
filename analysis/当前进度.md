# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **pipeline_abi wave79 pure residual** @ `ccc0f99e` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **R2 pipeline_abi Cap residual pure wave79**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + pure-asm 矩阵 rv42 / option / hello / stdlib-import；wave79 pure **W**（`shux_path_try_realpath_inplace`）@ `ccc0f99e` |
| mac | ✅ L2 PREFER hybrid + c-backend 矩阵（Darwin pure-asm CG002 tip 既有）@ `ccc0f99e` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave79** @ `ccc0f99e`  
   - **迁 pure OS residual**：`shux_path_try_realpath_inplace`（G.7 g05 `shux_driver_realpath_opaque` + 栈 `resolved[1024]` + `pipe_cstr_copy`；失败保留 path；非 POSIX harness null ≡ seed no-op）  
   - **seed**：冷孪生在 `#ifndef FROM_X`；FROM_X rest **extern** 供 always-seed resolve_*_impl 调 pure  
   - **Cap residual 仍**：fn-ptr / product_emit / typeck_module C 前端 / `pipeline_sizeof_*` / preprocess 引擎  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave79.log` · `/tmp/ubuntu_g05_wave79.log`

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：pipeline_abi 硬 Cap residual 叶（fn-ptr / product_emit / typeck_module C 前端；sizeof / preprocess…）/ 整波 tip L4  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave79 未升钉）
产品 tip：ccc0f99e pipeline_abi residual pure wave79 L2
Windows：本波不需要切换
下一：pipeline_abi 硬 Cap residual / 整波 tip L4（需时再说）
```
