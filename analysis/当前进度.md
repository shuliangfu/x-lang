# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（§3.6 本波收口说明见摘要）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`46ba2297`**（升自 `11a57351`；双端 L4 + 129） |
| tip | **`961207bf`**（asm CLI user `.o`；日常 L2，**非** tip L4 升钉） |
| 产品 bstrict | **129** · 钉盘双端 L4 真冷全绿（本波未重跑 tip L4） |
| 本波性质 | §3.6 根修：纯 asm `invoke_ld` 接入 CLI user `.o`（G.7 与 invoke_cc 同表） |
| Ubuntu | ✅ L2 g05 + pure-asm marker glue.o→42 + ioctx/rv/hello |
| mac | ✅ L2 g05（`SHUX_G05_PREFER_X_O=0`）+ 矩阵 rv/hello/ioctx + C-path marker |
| Windows | **本波不需要** |

## 本波摘要

1. **§3.6 asm user `.o`（产品根修 · `961207bf`）**  
   - 地图：C 链已有 `g_shux_user_extra_o_files`；纯 asm `shux_invoke_ld_for_exe` 只收 temp user.o → CLI `glue.o` 被丢  
   - G.7：**不**新建第二表；asm ld 各分支 `shux_asm_ld_append_user_extra_o_files`；`driver_run_asm_backend` set/clear  
   - Ubuntu 金标：`-backend asm need.x -o out glue.o` → run **42**；无 glue → UNDEF `shux_user_extra_o_marker`  
   - 构建级别：**日常 L2**（非 tip L4 升钉）

2. 前序钉盘 **`46ba2297`** 仍为产品 L4 放行（opaque DIR* / libc skip↔preamble）

## 下一波前排

1. **可选** 其它 R2 rest / Cap residual  
2. **可选** g05 sed 防御 / Ubuntu `-E` SIGSEGV 根因 / simd-s4 WARN  
3. **可选** Windows hybrid；整波收口时 tip L4 升钉（含 `961207bf`）

```text
钉盘：46ba2297
tip：961207bf（asm CLI user .o · L2）
Windows：本波不需要切换
下一：其它 R2 / 可选 tip L4 升钉
```
