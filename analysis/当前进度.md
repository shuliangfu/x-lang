# Shux 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-22.md](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave163** panic_o_path pure @ `413dc5182` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **R2 Cap residual pure · path residual**（`shux_runtime_panic_o_path` → L0 pure） |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：complex path ladder 首叶 — `shux_runtime_panic_o_path` 体仍 always mega C（cwd realpath 阶梯 + argv0 父目录 join）  
2. **根修 / pure**：真迁 `labi_path_pure.x` L0 pure orch（BSS 512/4096 + pure join / last-sep index）；count 18→19  
3. **Cap residual**：`shux_runtime_o_realpath_if_exists` + `getcwd` + `asm_link_obj_skip_missing`  
4. **未动**：crt0_user / freestanding_io / async_scheduler path ladders；freestanding ensure·path（cc/access Cap）  
5. **验收**：双端 L2 g05 + 矩阵 + string/vec/slice/compress 邻域 + prove IDENTICAL；钉盘仍 **`3c88f7e51`**  
6. **日切**：0 点后归档 22 号 `自举进度` 全文 → `自举进度-归档-2026-07-22.md`；23 号重新起表  

## 下一波前排

1. 其它 rest R2 / mega residual（可迁：complex path ladders crt0·freestanding_io·async_scheduler / freestanding ensure·path residual …；常驻 Cap 不硬迁：contains_substr / undef_sym / path_io stat / wait / strerror / mega spawn-IO / ld_debug_argv / invoke_cc_argv_push_existing realpath 池 / ensure_std_net_o_auto_tls system·make / getenv / shu_resolve_compiler_dir / `*_o_path` 静态缓冲 …）  
2. 可选：未闭合块注释硬诊断（EOF depth>0）  
3. 可选 Windows hybrid（**需要时再通知重启**）  

```text
钉盘：3c88f7e51（未升）
tip：413dc5182 wave163 panic_o_path pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：complex path ladders crt0/freestanding_io/async_scheduler / freestanding ensure·path / 其它 rest R2
```
