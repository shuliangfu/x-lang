# X 语言 / X language 当前进度（2026-07-28 · wave594 Cap pure-asm 9–16B dual-GP return-local · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`13ced06b5`**（wave594 Cap dual-GP return-local · L2 双端绿） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave594（已闭 · Cap residual pure-asm 9–16B dual-GP return-local · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | freestanding/pure-asm `let t=S{…}; return t` 再 `mk().a` / `let s=mk()`：Ubuntu SIGSEGV；mac freestanding CG002 |
| tip | **`13ced06b5`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave594 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 中宽 return-local CALL 物化 | `pipeline_asm_call_struct16_ret_needs_rax_deref_c` 对 **一切** size>8 named 返回 1（C pointer-in-rax） | ✅ pure-asm body → dual-GP by value（0）；仅 skip_heavy C 体仍 1 |
| 调用方假解引用 | store_retval / CALL.field：`mov rax→rbx; load *rbx` 把 rax=0x2a 当指针 → SIGSEGV | ✅ 不再 deref；`mov rax/rdx → [rbp+home]` |
| STRUCT_LIT return 对照 | try_inline 跳过 deref 路径故 wave593 绿 | 回归仍 42 |

**数据流（五问）**：产生 = pure-asm `return local` 中宽 dual-GP（rax+rdx）；存储 = 调用方帧 temp；消费 = field load / let。分类器误把 freestanding `mk():S` 当 C 指针返回 → 消费端假 deref。权威：G.7 补全同一 `needs_rax_deref`（skip_heavy 为唯一 deref 门控）。

**权威**：`compiler/pipeline_glue.c` · `pipeline_asm_call_struct16_ret_needs_rax_deref_c`（`pipeline_glue_standalone.o`）。改后 rm standalone.o + g05。

### wave594 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| sz12/sz16 return-local `mk().a` freestanding/pure-asm | ✅ 42 | ✅ 42 |
| sz12 `let s=mk(); return 42` / sum a+b+c | ✅ 42 | ✅ 42 |
| sz12 STRUCT_LIT inline / nest2 回归 | ✅ 42 | ✅ 42 |
| sz24 true-sret return-local | ✅ 42 | （邻域） |
| disasm store_retval 无 `*(rax)` | — | ✅ `mov rax/rdx,[rbp]` |
| rv / option / si / hello | ✅ 42/102/0/0 | ✅ 42/102/0/0 |

### 仍 soft

- 更深嵌套大布局 STRUCT_LIT（Outer+Mid+Inner multi-field：Ubuntu nest3 pure-asm SIGSEGV；mac freestanding 错值）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual：深嵌套 STRUCT_LIT 布局/物化，或整波 L4  
2. 整波双端 L4 + 升钉  

## wave593（已闭 · Cap residual nested CALL.field + mid-size STRUCT_LIT inline）

tip `789b485b0` / docs `6c6b2e23f`。soft「return-local mid-size」本波由 **wave594** 收。
