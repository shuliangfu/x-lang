# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`86281cc6`**（双端 L4 真冷 + 129；未升） |
| tip | **R2 driver_abi Cap residual pure wave18**（parsed work BSS+cleanup；日常 L2） |
| 产品 bstrict | **129** · tip L4 钉盘仍 `@86281cc6` |
| 本波性质 | **工程轨 R2 pure 微步**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42/option/si/hello + pure-asm rv42 @ `184b09d5` |
| mac | ✅ L2 同 |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `runtime_driver_abi` Cap residual pure wave18（本波）**  
   - 真迁：`driver_parsed_work_reset` / `p_get`/`p_set` / `i_get`/`i_set` / `z_get`/`z_set` / `cleanup`  
   - BSS：`g_parsed_work_p_raw` `u8[192]`（24× LP64 指针槽，G.7 `shux_ptr_slot_*`）+ `i32[14]` + `usize[4]`  
   - reset：清 work 槽 + seed `driver_parsed_tmp_c_slot()[0]` + `driver_parsed_tmp_c_buf()[0]`  
   - cleanup：ndeps=`i[1]`；free 依赖表/行 + out_buf + `pipeline_dep_ctx_heap_destroy(pctx)`；`!emit_stdout` 时 `driver_parsed_fclose(cf)` + `unlink(tmp_c 或 seed buf)`；free arena/module/src/kind/code/msg/tmp_c，再 reset  
   - seed：always-seed tmp 访问器；cold 保留 C 静态表 + memset；`#ifndef SHUX_L2_RDABI_THIN_FROM_X` 剔 pure-dup  
   - 代码 SHA：**`184b09d5`**（docs 本 commit 跟进）  
   - 矩阵：rv_build=0 / rv_run=42 / opt 102 / si 0 / hello 0 / pure-asm rv42  
   - 钉盘仍 **`86281cc6`**（skill §3.3：微步不自动 tip L4）

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文；**不**改 `当前问题分析.md` / `问题分析文档.md`

## 下一波前排

1. **可选** driver_abi 余 Cap residual（**dep_ctx 字段** / preamble line 桥 / fmt_argv 待 pointer-array pure）  
2. **可选** g05 sed / harness `-E` 评估  
3. **可选** Windows hybrid；Darwin arm64 纯 asm Vec4f CG002  

```text
钉盘：86281cc6（tip L4 双端 129）
tip：R2 driver_abi wave18 parsed work BSS+cleanup pure（L2 @ 184b09d5）
Windows：本波不需要切换
下一：dep_ctx / preamble / fmt_argv / g05 sed / Windows / Darwin CG002
```
