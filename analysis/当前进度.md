# X 语言 / X language 当前进度（2026-07-28 · wave613 Cap multi-dim nested ARRAY_LIT · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`82a1c1e1f`**（wave613 Cap multi-dim nested ARRAY_LIT flatten · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave613（已闭 · Cap residual freestanding multi-dim nested ARRAY_LIT · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `[[10,32],[1,2]][0][0]` freestanding 错值（mac fs=32≠10；host-C 与 `let m:i32[2][2]=[[…]]` 已绿） |
| tip 产品 | **`82a1c1e1f`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave613 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| rvalue 嵌套 ARRAY_LIT 成指针数组 | `pipeline_asm_emit_array_lit_elf_c` 对嵌套 elem `emit_rec`→rax 指针再 store | ✅ 复用 wave357 `emit_array_lit_flat` 连续 row-major |
| 三层+ leaf_esz 用行宽 8 | flat 只 peel 一层 → `str x0` 步长 8，INDEX i32 步长 4 | ✅ `pipeline_asm_array_lit_leaf_elem_byte_sz_c` 剥到标量；vector_let 同修 |

**数据流（五问）**：产生 = rvalue / let multi-dim ARRAY_LIT 物化；存储 = 帧 temp 布局；消费 = multi-dim INDEX leave_addr+load。let 路径 wave357 已 flat；rvalue 仍指针槽 → INDEX 读指针低半/错槽。权威：G.7 **补全同一 flat 写手** + 单一 leaf peel helper。

**权威**：`compiler/pipeline_glue.c` · `pipeline_asm_emit_array_lit_elf_c` + `pipeline_asm_array_lit_leaf_elem_byte_sz_c` + `pipeline_asm_emit_vector_let_init_elf_c`。改后 rm `pipeline_x.o` + `build_asm/pipeline_glue_standalone.o` + g05。

### wave613 验证

| 探针 | mac L2 host/fs | Ubuntu L2 host/fs |
|------|----------------|-------------------|
| n_outer0/1 `[[…]][0][i]` | ✅ 10/32 | ✅ 10/32 |
| nested dual `[0][0]+[0][1]` | ✅ 42/42（was fs 65） | ✅ 42/42 |
| n_row / n_deep / n_let2 | ✅ 11/42/42 | ✅ 11/42/42 |
| t_mid1 `[[[10,32]]][0][0][1]` | ✅ 32（was fs 0） | ✅ 32 |
| n_var / lit_sum / struct_idx 回归 | ✅ 42 | ✅ 42 |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/0/0 |

disasm：Ubuntu/mac 连续 `str w` @+0/+4/+8/+c；无 row 指针 `str x`。

注：Ubuntu 绝对路径 `XLANG=/…/compiler/xlang_asm` 时 freestanding ensure 会误 stat `compiler/compiler/crt0_user.o`（既有路径债）；相对 `./compiler/xlang_asm` 正常。

### 仍 soft

- 其它 Cap residual 叶；整波双端 **L4** + 升钉  
- 绝对 `XLANG` freestanding crt0 双前缀（ensure 路径，非本波）

### 下一波

1. Cap residual soft 其它叶，或整波 L4 + 升钉  

## wave612（已闭 · Cap residual freestanding dual ARRAY_LIT INDEX binop rbx · L2）

tip 产品 **`05dd5df6f`** / docs **`dc4ac53e4`**。

## wave611（已闭 · Cap residual ARRAY_LIT INDEX parse+typeck · L2）

tip 产品 **`1c4a631e1`** / docs **`b619af4d6`**。
