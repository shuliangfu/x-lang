# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **wave45 pipeline_abi pure AST 根修 + PREFER hybrid** @ `b023e122` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **rest R2 pure 产品化微波**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 pure-asm rv42 @ `b023e122`（pipeline pure 符号 **W**） |
| mac | ✅ L2 PREFER hybrid + g05 + help/c-backend rv42 @ `b023e122`（Darwin pure-asm CG002 **tip 既有**） |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave45** @ `b023e122`  
   - **根因**：`runtime_pipeline_abi.x` 中 `shux_ptr_slot_get` docblock 写了 `char**/void*`，`*/` 提前结束注释 → 后续全部 `export function` **静默未入 AST**；`-E` 只剩 extern（~5.8KiB），pure 从未产品化  
   - **根修**：改写 docblock（G.9 禁止块注释内嵌结束符）；`-E` 恢复 ~111 pure 体 / g05 115 T  
   - **产品 PREFER hybrid**：`g05_try_x_to_o` full `.x` + seed rest `-DSHUX_RUNTIME_PIPELINE_ABI_FROM_X`  
   - Cap residual always-seed BSS writer：`typeck_ndep_store_impl` / `typeck_dep_*_set_impl` / `driver_dep_*_ptr_set_impl`  
   - 去掉 `pipeline_debug_trace_named_func_bodies` 的 export-extern 双权威（hybrid 下 mangle UNDEF）  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave45b.log` · `/tmp/ubuntu_g05_wave45.log`；Ubuntu pure **W**

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：pipeline_abi Cap residual 重 C 面（collect/thread/FILE*）继续 pure 拆；或其它 rest 模块  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave45 未升钉）
产品 tip：b023e122 wave45 L2
Windows：本波不需要切换
下一：pipeline residual 深迁 / 其它 rest / Darwin CG002（需时再说）
```
