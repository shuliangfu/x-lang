# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **pipeline_abi wave56 pure residual** @ `9059161f` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **R2 pipeline_abi Cap residual pure wave56**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + pure-asm 矩阵 rv42 / option / hello / import / stdlib-import / overload；pipeline large-stack pure **W** + Cap-fn-ptr **T** @ `9059161f` |
| mac | ✅ L2 PREFER hybrid + c-backend 矩阵（Darwin pure-asm CG002 tip 既有）@ `9059161f` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave56** @ `9059161f`  
   - **迁 pure**：`pipeline_run_x_thread_fn_impl` + `shux_pipeline_run_x_pipeline_large_stack_impl`  
     （栈 `PipelineRunSuArgs` u8[56]；Cap-fn-ptr `pipeline_run_x_thread_fn_ptr`；G.7 `driver_run_thread_on_large_stack`；sentinel -99 回退当前线程）  
   - **SHUX_DEBUG_PIPE** notes 仅 cold twin（pure 跳过，同 tmp_parse）  
   - **仍 seed**：asm `shux_asm_codegen_elf_o_*_impl`（同 TU pure stub `asm_asm_codegen_elf_o` 恒 -1，不可 pure 调）  
   - **seed**：冷孪生 `#ifndef FROM_X`；always-seed Cap-fn-ptr；FROM_X rest pure **extern**  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave56.log` · `/tmp/ubuntu_g05_wave56.log` · matrix 本机 / Ubuntu 会话输出

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：asm elf_o large-stack `_impl` residual（需 Cap residual 真 emit 面，禁同 TU stub）  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave56 未升钉）
产品 tip：9059161f pipeline_abi residual pure wave56 L2
Windows：本波不需要切换
下一：asm large-stack residual pure / Darwin CG002（需时再说）
```
