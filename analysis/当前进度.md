# X 语言 / X language 当前进度（2026-07-28 · wave607 Cap STRUCT_LIT postfix · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`cfa113ffc`**（wave607 Cap STRUCT_LIT postfix `.method`/`.field` · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave607（已闭 · Cap residual STRUCT_LIT postfix method/field · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | 裸 `Big { … }.sum()` → parse `num_funcs=1 main_idx=-1` → ld 无 `_main`（paren `(Big{…}).sum()` 绿） |
| tip 产品 | **`cfa113ffc`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave607 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 裸 STRUCT_LIT 后 `.method()` / `.field` | `finish_struct_lit` 成功后 early return，未进 suffix loop | ✅ 命名 / 限定 / 匿名 lit 后均续 `ident_suffix_loop` |

**数据流（五问）**：产生 = primary IDENT+`{…}` 写完 STRUCT_LIT 即 return；存储 = 无 METHOD_CALL AST → body parse fail 静默丢 main；消费 = ld `_main`。权威：G.7 复用 `parser_asm_primary_ident_suffix_loop_c`（≡ `(expr).method()`），不新开第二套 postfix。

**权威**：`compiler/seeds/parser_asm/parser_asm_primary_slice.inc`（产品链 `parser_asm_thin_glue.o`）。改后 rm `parser_asm_thin_glue.o` + g05。

### wave607 验证

| 探针 | mac L2 host/fs | Ubuntu L2 host/pure/fs |
|------|----------------|------------------------|
| mem_method_lit `Big{…}.sum()` 24B | ✅ 42/42（was 无 main） | ✅ 42/42/42 |
| dual_method_lit `Pair{…}.sum()` | ✅ 42/42 | ✅ 42/42/42 |
| lit_method_let / paren 回归 | ✅ 42 | ✅ — |
| ufcs / field / call / arg_mem / free | ✅ 42 | ✅ 42 |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/0/0 |

### 仍 soft

- freestanding `Big{…}.a` FIELD rvalue（host 绿；fs CG002 — emit 叶，非本波 parse）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual soft 其它叶（如 fs STRUCT_LIT.field rvalue），或整波双端 L4 + 升钉  

## wave606（已闭 · Cap residual arm64 freestanding UFCS method MEMORY · L2）

tip 产品 **`d4e63a37d`** / docs **`17186ce40`**。soft「STRUCT_LIT method base」本波由 **wave607** 收口（parse postfix，非 MEMORY emit）。

## wave605（已闭 · Cap residual freestanding MEMORY call-arg from STRUCT_LIT/FIELD · L2）

tip 产品 **`46aa389a1`** / docs **`4223a337d`**。

## wave604～wave602

见历史流水；arm64 UFCS MEMORY 由 wave606 闭。
