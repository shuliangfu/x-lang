# Shux 当前进度（2026-07-20）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`../AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **归档**：[自举进度-归档-2026-07-19.md](自举进度-归档-2026-07-19.md) · [18 号](自举进度-归档-2026-07-18.md) · [17 号](自举进度-归档-2026-07-17.md) · [16 号](自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`deaf773b`**（docs(windows) 记录测试结果 + 验证方法 + 6-commit 修复） |
| 产品 L4 放行钉盘 | **`305cfbe1`**（C6 波 2 ABI 一致性升钉；本波 Windows 平台脚本修复非 ABI/link → 不升钉） |
| 产品 bstrict 套件 | **125**（不变） |
| 本波性质 | **C6 波 1+波 2 Windows 门禁补跑收口**（codex 7 commit · 平台脚本修复 · 非升钉） |
| 本波输出 | Windows MSYS2/MinGW hybrid 门禁从阻塞→全绿（4 项：bootstrap-driver-bstrict + return-value 42 + win32-write-gate + win32-read-file-gate + C-03）；macOS + Ubuntu 双端 L4 真冷全绿 @ `deaf773b`；Windows 门禁首次全绿满足 §5.1 阶段 1 DoD |

## 本波摘要（2026-07-20 · C6 波 1+波 2 Windows 门禁补跑收口 · codex 7 commit · 非升钉）

| 项 | 结果 |
|----|------|
| **入口** | C6 波 1+波 2（ABI 边界改动）按 [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md) §5.1 强制要求合并补跑 Windows hybrid 最小门禁 |
| **codex 7 commit** | `1c3b54b4` docs(windows) 平台限制指南 · `3f495941` fix g05_relink_env MINGW host · `47ea02c0` fix gen_asm_full_link_stubs.pl 原子替换 · `41f1fe7a` fix g05_ensure 重新生成 stale stubs · `ba9e03b4` fix Windows gate 捕获真实 exit code（防假绿）· `e56ab351` fix g05 link order 对齐 Makefile + smoke exe 签名 · `9f68cda6` fix build_shux_asm 不覆盖 runtime_driver.o（最终真根因）· `deaf773b` docs 记录测试结果 + 验证方法 |
| **最终真根因** | `build_shux_asm.sh` 的 `ensure_runtime_cc_stubs` 用残缺 flags 覆盖 make/g05 已构建的 `runtime_driver.o`，导致 g05 链接污染。修复：`ensure_runtime_cc_stubs` 不覆盖已存在的 `runtime_driver.o` |
| **macOS L4 真冷** | ✅ 全绿 @ `deaf773b`（用户验证）：全擦 .o + 删二进制 + bootstrap-driver-seed + g05 relink + bstrict 125 |
| **Ubuntu L4 真冷（金标）** | ✅ 全绿 @ `deaf773b`（本会话验证）：git pull + 全擦 .o + 删二进制 + bootstrap-driver-seed (shux 3490000 bytes) + g05_prepare_and_relink (60 objs Linux/x86_64) + return-value 42 + bstrict 125（SHUX_BSTRICT_SKIP_BUILD=1）；耗时 ~3.5 分钟 |
| **Windows hybrid 门禁** | ✅ warm 绿 @ `deaf773b`（4 项全过）：`SHUX_WIN_BSTRICT=1 ./tests/run-bootstrap-bstrict-windows-gate.sh` GATE_EXIT=0 |
| **三平台验证矩阵** | macOS arm64 L4 ✅ · Ubuntu x86_64 L4 ✅（金标）· Windows MSYS2 warm ✅ |
| **升钉决策** | ❌ **不升钉**（codex 7 commit 是 Windows 平台脚本修复，非 ABI/link 改动；钉盘保持 `305cfbe1`） |
| **Windows L4 真冷测** | 非必需（AGENTS.md §5 双端门禁 = macOS + Ubuntu；§5.1 Windows hybrid 绿 = 探针可用 ≠ 产品 tip L4） |

## 下一波前排（按优先级排序）

### 1. CLI help 优化（§11-§13，自举后工程优化）

用户原话"等codex把Windows的测试全部跑通了再来写"——现已全通，可启动。详见 [SHUX 命令行.md](SHUX%20命令行.md)：
- §11 `--help` 信息架构优化（flag 按子命令分组：编译/运行通用 · 仅编译 · 顶层 action）
- §12 flag 命名规范（`--long, -short` 全写在前）+ 新增 `--version, -v`
- §13 `--help` 彩色输出美化（语义着色 + TTY 检测 + NO_COLOR + Windows VT）
- §6 Phase 1 脚本迁移（~30-50 处隐式调用 → `shux build`）+ Phase 2 CLI 代码改造（删除 L838 隐式 fallback）+ Phase 3 `shux run` mmap 优化
- 时机决策（§10）：推荐自举后再做（CLI 改造是工程优化，非自举阻塞项；seed 镜像同步成本高）

### 2. C7 剩余 6 个候选（转后续波次）

- **修 2 个 bug**（阻塞合并）：std.debug.println 返回值语义 + fmt 命令一元负号
- **评估 4 个 gate/perf 路径 A**（纳入白名单扩容 125→129）：STD-061 perf simd + STD-047 simd gate + STD-091 io-context gate + STD-092 net-context gate
- 详见 [C7-plan-audit.md](C7-plan-audit.md) §6 建议下一步

### 3. Windows L4 真冷测（可选 · 非必需）

- 非必需（双端门禁已满足），但若用户希望 Windows 也达到 L4 冷测级别可补跑
- 命令：`ssh windows-server` → 全擦 .o + 删二进制 + `make bootstrap-driver-seed SHUX_LEGACY_C_FRONTEND=1` + `SHUX_WIN_BSTRICT=1 ./tests/run-bootstrap-bstrict-windows-gate.sh`

## 不在前排

- **C6 X ABI P0b 波 3：std/ 扩展标注**（远期 · 多波 · 顺延自举后 P1-P2）：~557 个 C 库包装分文件标注 `extern "C"`
- **shux-x 5GB 方案 B**（远期 · 非 KPI）：ArenaSidecar release API
- **Cap C9 match guard full-chain 支持**（独立大工程 · 多波）：6 个产生点改动，先排期再动手
- **C5 EXPR_CLOSURE CTFE**（独立大工程 · 多波）：已 scoping，顺延自举后立项 `lang-closure-v1-rfc.md`
- **历史进度**：C6 波 2 ABI 一致性（`305cfbe1` 升钉）/ C6 波 1 unsafe 包裹（`4214eea4` 升钉）/ Windows shux-c exit=1 根因修复（`d25b82cc`，mac+Ubuntu L4 全绿）/ C6 P0b audit（`7e507fb6` docs-only）/ shux-x 5GB 方案 A（`a14a1dd0`）/ C7 plan 瘦身 PoC（`e546b013`+`bedb40ca`，2/8 完成）/ C5 三连击（`c174e70a`）/ 4 波 C5 CTFE / Windows 时序决议 / C2/C3=C4/backtrace/C8 钉盘波，全部归档在 [自举进度.md](自举进度.md) §6 变更记录

```text
本波 tip：deaf773b (docs(windows): record test results, verification method, and 6-commit fixes)
本波性质：C6 波 1+波 2 Windows 门禁补跑收口（codex 7 commit · 平台脚本修复 · 非升钉）
本波输出：Windows hybrid 门禁从阻塞→全绿（4 项）+ macOS/Ubuntu 双端 L4 真冷全绿 @ deaf773b
构建级别：L4 真冷全测（mac arm64 + Ubuntu x86_64 双端）；Windows warm 绿（非 L4，非必需）
产品 tip L4 钉盘：305cfbe1（保持；codex 7 commit 非 ABI/link 改动不升钉）

三平台验证矩阵：
  ✅ macOS arm64 L4 真冷全绿 @ deaf773b（用户验证；全擦 .o + 删二进制 + bootstrap-driver-seed + g05 + bstrict 125）
  ✅ Ubuntu x86_64 L4 真冷全绿 @ deaf773b（金标；本会话验证；git pull + 全擦 .o + 删二进制 + bootstrap-driver-seed shux 3490000 bytes + g05 60 objs Linux/x86_64 + return-value 42 + bstrict 125）
  ✅ Windows MSYS2 hybrid warm 绿 @ deaf773b（4 项：bootstrap-driver-bstrict + return-value 42 + win32-write-gate + win32-read-file-gate + C-03）

codex 7 commit（Windows MSYS2/MinGW 兼容修复）：
  1c3b54b4 docs(windows): add Windows platform limits and testing guide
  3f495941 fix(windows): g05_relink_env.sh supports MINGW host + LEGACY/no_c mode
  47ea02c0 fix(windows): gen_asm_full_link_stubs.pl atomic temp-file replace
  41f1fe7a fix(windows): g05_ensure regenerates stale asm_full_link_stubs.o
  ba9e03b4 fix(tests): catch false-green make exit code in Windows B-strict gate
  e56ab351 fix(windows): align g05 link order with Makefile + sign smoke exes for SAC
  9f68cda6 fix(windows): build_shux_asm must not clobber make/g05 runtime_driver.o（最终真根因）
  deaf773b docs(windows): record test results, verification method, and 6-commit fixes

最终真根因：build_shux_asm.sh ensure_runtime_cc_stubs 用残缺 flags 覆盖 make/g05 已构建的 runtime_driver.o → g05 链接污染
修复：ensure_runtime_cc_stubs 不覆盖已存在的 runtime_driver.o

Windows L4 真冷测：非必需
  理由 1：AGENTS.md §5 双端门禁 = macOS + Ubuntu（不含 Windows）
  理由 2：自举进度.md §5.1 Windows hybrid 绿 = 探针可用；≠ 产品 tip L4 / 可自举
  理由 3：当前 Windows hybrid warm 绿已满足 §5.1 阶段 1 DoD

权威文档：analysis/问题分析文档.md（codex 重写，§9 测试结果 + 验证方法 + 6-commit 修复 + 最终真根因详解 + 剩余未处理项）
权威文档：analysis/Windows平台限制与测试指南.md（13 硬限制 + 12 已修复 + 5 未解决 + 测试方法 + 跨平台对比表）
权威文档：analysis/SHUX 命令行.md（CLI 子命令化改造 13 节分析，自举后工程优化）
历史进度归档：analysis/自举进度.md §6 变更记录（Windows 门禁收口 deaf773b / d25b82cc mac+Ubuntu L4 / C6 波 2 305cfbe1 升钉 / 波 1 4214eea4 升钉 / C6 P0b audit 7e507fb6 / shux-x 5GB 方案 A a14a1dd0 / C7 PoC e546b013+bedb40ca / 日终 L4 兜底 94a8e7d5 / C5 三连击 c174e70a / 4 波 C5 CTFE / Windows 时序决议 / C2/C3=C4/backtrace/C8 钉盘