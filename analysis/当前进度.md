# X 语言 / X language 当前进度（2026-07-29 · wave634 Cap soft 假红闭：uninit TYPE_ARRAY assign 探针缺 `;` · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`9a16fb325`**（产品仍 wave633；本波 **docs-only**） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave634（已闭 · Cap residual soft 假红：uninit `let s: S24[2]; s = mk()` · docs-only · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave633 soft：未初始化 `let s: S24[2]; s = mk()` 疑丢 main / BLD001 |
| tip 产品 | **不变** `9a16fb325`（无产品代码改动） |
| 构建级别 | **L2 验证**（**非** L4；钉盘不升） |

### wave634 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| soft「uninit assign 丢 main」 | 探针 **缺 let 后 `;`**（`docs/06`：`let name: Type;` 须分号）→ P001 `expected ';' after let` → pipeline `num_funcs=0` → ld 无 `_main` | ✅ **假 soft**：合法语法双端 fx=110 |
| 产品路径 uninit + assign | `let s: S24[2]; s = mk();` + wave334/354/633 fixed-array assign 权威 | ✅ 已绿（无需改 glue/codegen） |

**数据流（五问）**：产生 = 探针语法非法（bare let 后无 `;` 再 `IDENT =`）→ parser 硬失败；存储 = 无模块函数；消费 = ld BLD001。**非** codegen bulk/半字。G.7：**不**为假 soft 开第二路径；文档闭叶。

**权威**：产品路径既有（wave334 assign + wave633 bulk CALL let）；本波 **0 行产品代码**。

### wave634 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `let s: S24[2]; s = mk();` uninit asg | ✅ 110 | ✅ 110 |
| host-C 同形 | ✅ 110 | ✅ 110 |
| 标量 `let x: i32; x = 42;` | ✅ 42 | ✅ 42 |
| `let s = mk()` / zero+asg / varc / direct / sl | ✅ 110 | ✅ 110 |
| `i32[2]` / `Pt[2]` return let | ✅ 42 | ✅ 42 |
| 缺 `;` 探针 | ✅ 硬 P001 | ✅ 硬 P001 |
| rv / option / hello | ✅ 42/102/0 | ✅ 42/102/0 |

### 仍 soft

- 其它 Cap residual 叶（无本波新开）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶，或整波 L4 + 升钉  

## wave633（已闭 · Cap residual pure-asm fixed TYPE_ARRAY let from CALL/VAR bulk esz>8 · L2 工程轨）

tip 产品 **`9a16fb325`** / docs **`0f834183e`**。fixed S24[2] let bulk 绿。soft uninit asg 已由 **wave634** 证为探针缺 `;` 假红。

## wave632（已闭 · Cap residual pure-asm return large NAMED ARRAY_LIT durable + reent · L2）

tip 产品 **`2a9d40d0c`** / docs **`407d17be9`**。return S24[] simple/deep/dual 绿；soft fixed `S24[2]` let 已由 **wave633** 闭。

## wave631（已闭 · Cap residual pure-asm TYPE_SLICE ARRAY_LIT large NAMED · L2）

tip 产品 **`8bbbf951b`** / docs **`42a9f0bbf`**。S24[] lit RO/sl 绿。

## wave630（已闭 · Cap residual pure-asm INDEX assign bulk esz>8 · L2）

tip 产品 **`66a0f3ffa`** / docs **`0a78ca598`**。bulk esz>8 绿。
