# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`1f73ed275`**（双端 tip L4 真冷 + 129；未升 — 本波日常 L2） |
| tip 产品代码 | **wave98 product cfg_eval.x for complex #if** @ `ac475548`（链 `40a908f5`+`aed9290b`） |
| 产品 bstrict | **129** · 钉盘 tip L4 全绿；wave98 **日常 L2** 矩阵绿（未全量 bstrict） |
| 本波性质 | **wave98 产品链 cfg_eval.x 真体（闭合 complex #if bootstrap stub 双权威）** |
| Ubuntu | ✅ L2 g05 + cfg_eval **linux pin gen + alias**（`lexer_cfg_eval_expr_c` T）+ 矩阵 rv42/option/si/hello + `-DFOO` + **complex `target_os=="linux"`→11** + force_c @ `ac475548` · `/tmp/ubuntu_g05_wave98.log` · `/tmp/ubuntu_g05_wave98_matrix.log` |
| mac | ✅ L2 g05 + cfg_eval **-E-extern + alias** + 矩阵 rv42/option/si/hello + `-DFOO` + **complex `target_os=="macos"`→11** + force_c @ `ac475548` · `/tmp/mac_g05_wave98.log` · `/tmp/mac_g05_wave98_matrix.log` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave98** @ 产品 **`ac475548`**（产品根修 `40a908f5`）  
   - 根因：`cfg_eval.x` 多 `#[cfg]` 同名 host lit 在 `-E-extern` 双重定义 → Makefile 静默回退 **bootstrap stub**；pure `preprocess_eval_condition_c` 仍调 `cfg_eval_expr_c` 但体是 C stub（双权威）  
   - 根修：host OS/arch lit → `cfg_eval_link_alias` / `cfg_eval_host_lit`（C `#if`）；eval 权威仍 `cfg_eval.x`；`PIPELINE_GEN_CFLAGS` + Clang `-Wno-parentheses-equality`  
   - Ubuntu 金标：live `-E-extern` 不可用时 pin **`seeds/cfg_eval_gen.linux.x86_64.c`**（禁 plain `-E` 悬空 BSS SEGV）  
   - 闭合硬 Cap residual「产品 complex #if 体仍是 bootstrap stub」叶  
   - Cap residual 仍：g05 &fn cast（语言）；`parser_copy_module_import_path64`；cfg 跨 TU 调用永久 G.7  
   - **构建级别 L2**（日常真测；非 tip L4）  
2. 继承 tip L4 钉盘 **`1f73ed275`**（wave76–81 收口；wave82–98 未升钉）

## 下一波前排

1. rest R2：pipeline_abi **硬 Cap residual** 子叶（g05 &fn cast 语言 residual；`parser_copy_module_import_path64`）  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 硬 Cap / soft 再有整波收口时再 tip L4（非每微步）

```text
钉盘：1f73ed275（tip L4 双端 129；本波未升）
产品 tip：ac475548 wave98 product cfg_eval.x complex #if（非 bootstrap stub）
文档：21 号归档 · 22 号 wave76–98 + tip L4 升钉 @1f73ed275
Windows：本波不需要切换
下一：g05 &fn / parser_copy path64 或整波 tip L4
```
