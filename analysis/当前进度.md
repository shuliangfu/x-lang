# X 语言 / X language 当前进度（2026-07-28 · wave539 已闭 · runtime_time_os R2 prove 文书补全 · L2 真车双路 129 全绿）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`2e4530ff9`**（wave538-539 L2 双路 129 全绿） |
| 钉盘 | **`213bd306b`**（升自 `f1250f415`；wave537 L4 双端 129 全绿；wave538-539 L2 收口未升钉） |
| bstrict | **129** |

## wave539（已闭 · runtime_time_os R2 prove 文书补全 · L2 真车双路 129 全绿）

| 项 | 状态 |
|----|------|
| 触发 | wave538 审计发现 runtime_time_os 的 wave501 R2 thin 代码已就位（.x 5 `#[no_mangle]` + Makefile R2 规则 L1254-1270 + .c rest 守卫宏），但缺 `surface.from_x.c` + `prove_module_selfhost.sh` 注册条目，prove 无法锁 |
| 范围 | 补全 R2 prove 文书，让 runtime_time_os 进入 prove nm IDENTICAL 白名单 |
| 构建级别 | **日常 L2**（双路构建 + nm IDENTICAL + bstrict 129） |

### wave539 已完成子任务

| 模块 | #[no_mangle] | 模式 | prove IDENTICAL | L2 bstrict | 状态 |
|------|------|------|------|------|------|
| runtime_time_os | 5 | thin+rest（5 public API 薄壳 + 5 _impl OS 桥 Cap residual） | ✅ 双端（mac + Ubuntu） | ✅ 129 OK (2m46s) | 已闭 |

### wave539 关键产出

1. **新建 `compiler/seeds/runtime_time_os_surface.from_x.c`**：镜像 .x 的 5 个 public API（monotonic/wall/sleep/rfc3339/local_offset），5 个 _impl OS 桥声明 extern（Cap residual 留 .c rest），用于 prove nm IDENTICAL 比对。
2. **`tests/prove_module_selfhost.sh` 注册条目**：`"runtime_time_os|src/asm/runtime_time_os.x|seeds/runtime_time_os_surface.from_x.c||"`，与 runtime_io_abi / async_liveness 同族格式。
3. **Cap residual 决策**：5 个 _impl OS 桥（clock_gettime/nanosleep/gmtime_r/QPC/Sleep 等）保留在 .c rest，与 runtime_io_abi「平台 _impl 留 seed」纪律一致。.x 表达 struct tm / LARGE_INTEGER 联合体能力未见验证，强行推入 .x 收益低风险高。

### wave539 验证状态

| 路径 | 构建 | prove nm | bstrict |
|------|------|---------|---------|
| macOS 本地 prove | ✅ | ✅ IDENTICAL | - |
| Ubuntu prove 金标 | ✅ `2e4530ff9` | ✅ IDENTICAL | - |
| Ubuntu L2 双路（冷+R2） | ✅ 双路 0 errors | - | ✅ 129 OK (2m46s) |

### wave539 关键发现

1. **wave501 R2 thin 代码已就位但文书缺失**：runtime_time_os 的 .x（5 `#[no_mangle]` + 5 extern 桥声明）+ Makefile R2 规则（thin+rest ld -r）+ .c rest 守卫宏 `XLANG_RUNTIME_TIME_OS_FROM_X` 在 wave501（2026-07-27）已完成，但缺 surface 文件 + prove 注册，导致 prove 白名单无法锁。这是 half-done 审计的延伸——代码完成但文书未跟上。
2. **L2 真车双路验证流程**：路 A 冷启动（seed C）→ bstrict 基线；路 B R2（`XLANG_G05_PREFER_X_O=1`）→ g05 重链 → bstrict 对照。两路 bstrict 129 全绿即 L2 通过。日常迭代用 L2，阶段收口才跑 L4 真冷全测。

## 前波 wave538（已闭 · half-done 模块审计 + net 模块 R2 迁移 · L2 双路 129 全绿）

| 项 | 状态 |
|----|------|
| 触发 | wave537 L4 收口后，继续审计并迁移有 `.x` wrapper + guards 但缺 Makefile R2 规则的 half-done 模块 |
| 范围 | 全量审计 .x #[no_mangle] / .from_x.c guards / Makefile R2 规则，找出 half-done 模块并补全 |
| 构建级别 | **日常 L2**（nm 符号一致性 + bstrict 129） |

### wave538 已完成子任务

| 模块 | #[no_mangle] | 模式 | L2 bstrict | 状态 |
|------|------|------|------|------|
| runtime_sync_lock_diag_tls | 5 | thin+rest（2 _impl + 3 pure DIRECT） | ✅ 129 OK (9m24s) | 已闭 |
| runtime_net_dns_fast | 3 | thin+rest | ✅ 129 OK (9m22s) | 已闭 |
| runtime_net_ipv6_fast | 5 | thin+rest | ✅ 129 OK (9m22s) | 已闭 |
| runtime_net_sock_fast | 2 | thin+rest | ✅ 129 OK (9m22s) | 已闭 |
| runtime_net_addr_fast | 1 | DIRECT（纯计算） | ✅ 129 OK (9m22s) | 已闭 |

### wave538 关键发现

1. **xlang-c `_impl_c` 命名约定**：xlang-c 对 `extern "C"` 函数调用自动加 `_c` 后缀，`.from_x.c` 中的 `_impl` 定义必须改为 `_impl_c` 才能匹配 thin wrapper 的调用符号。参照 `runtime_net_io_batch_fast` 的 `_impl_c` 模式。
2. **Windows-only 函数 POSIX stub**：`runtime_net_sock_fast` 的 `net_ensure_wsa_impl_c` / `net_wsa_ctor_impl_c` 在 Windows-only `#if` 块内，macOS/Linux 下不编译 → R2 thin wrapper 调用时未定义引用。根修：在 `#else` 分支添加 POSIX no-op stub。
3. **half-done 审计方法**：交叉比对 3 份数据——`.x` 文件的 `#[no_mangle]` / `.from_x.c` 的 guard 宏 / Makefile 的 R2 条件（`XLANG_G05_PREFER_X_O`）。

## 前波 wave537（已闭 · runtime_lsp_glue 一波 R2 收口 L4 真冷全测 · 双端 129 全绿 · 钉盘升 `213bd306b`）

| 项 | 状态 |
|----|------|
| 钉盘升级 | **`213bd306b`**（升自 `f1250f415`；双端 L4 129 全绿；wave535-536 runtime_lsp_glue R2 迁移） |
| macOS L4 | remaining_o=0 ✅ · bstrict OK (129) exit 0 ✅ · 11m28s |
| Ubuntu L4 | remaining_o=0 ✅ · bstrict OK (129) exit 0 ✅ · 3m25s |

## 下一步候选

1. **wave540+ 继续 R2 迁移**：按 [当前问题分析.md](当前问题分析.md) §2.1 候选清单，继续迁移有 .x wrapper 但缺 R2 规则的模块。优先纯计算 / 低平台依赖模块。
2. **N++ 扩白名单**：wave539 新增 runtime_time_os prove IDENTICAL，下次 N prove 时扩入白名单（当前 54/54，加 1 → 55/55）。
3. **runtime_lsp_glue 深度迁移**：14 个签名不兼容函数的 _impl 实现补全（当前 seed 为唯一权威，未来可迁移到 .x）。
4. **整波 L4 收口**：wave538-539 累积 R2 migration 后，做一次扎实 L4 真冷全测（按自举方法 §0.2），通过则升钉盘。
