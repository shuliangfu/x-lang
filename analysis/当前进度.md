# X 语言 / X language 当前进度（2026-07-29 · wave654 Cap ASI mid-block bare break/continue + IDENT · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 05:59 · 28 全日（wave534–617）已归楼；本文仅保留 29 日 tip 快照

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`e6e56f13d`**（wave654 ASI IDENT/control heads） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave654（已闭 · Cap residual bare break/continue ASI + IDENT · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | mid-block `break`/`continue` 无 `;` 后接 IDENT 语句 → **XP003** out_len=0；同 break 后接 `}`/`if` 已绿（ASI 仅认 let/if/while/for/return/`}`） |
| tip 产品 | **`e6e56f13d`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave654 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| ASI 后继 token 白名单缺 IDENT/控制流头 | `parser_asm_advance_past_stmt_semicolon_into_slice_c` | ✅ 补 IDENT · BREAK/CONTINUE · MATCH/LOOP · PANIC/DEFER/GOTO · STAR · SELF |

**数据流**：产生 = 表达式语句后无 `;` 时 `advance_past_stmt_semicolon` 见 IDENT（`s = 99`）返 0 → parse_block 失败 num_funcs=0 → XP003。存储 = 无。消费 = pipeline。G.7：同一 ASI 权威扩后继；expr 已 parse 完再 ASI，IDENT 不会粘连前序 binop。

**权威**：`compiler/seeds/parser_asm/parser_asm_helpers_slice.inc` · `parser_asm_advance_past_stmt_semicolon_into_slice_c`

### wave654 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| bare break + more stmt（expect 3） | ✅ 3 | ✅ 3（was XP003） |
| bare continue + more（expect 13） | ✅ 13 | ✅ 13 |
| two assign no `;`（expect 3） | ✅ 3 | ✅ 3（was XP003） |
| bare break 块末 / break; | ✅ 3 | ✅ 3 |
| while_cont / for cont 回归 | ✅ 12 / 8 | ✅ 12 / 8 |
| break_cont 回归 | — | ✅ 19 |
| rv / option / hello | ✅ 42/102/0 | ✅ 42/102/0 |

日志：`/tmp/mac_g05_wave654.log` · `/tmp/ubuntu_g05_wave654.log` · `/tmp/ubuntu_w654_probes.log`

### 仍 soft

- untyped `self`（wave493 非标准 · **跳过**）  
- bare `return expr` 无 `;` 后仍有 stmt（return 专用路径；与本波 expr-stmt ASI 分叶）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（bare return ASI / 其它；skip untyped self），或整波 L4 + 升钉  

## wave653（已闭 · Cap residual freestanding while/for continue · L2）

tip 产品 **`07ee3bae0`**。function-body final_expr + for continue→step。

## wave652（已闭 · Cap residual x86 high-end STRUCT_LIT 固定数组字段 · L2）

tip 产品 **`be48b6acf`**。`glue_struct_field_frame_mag_c` x86 high-end base−foff。

## wave651（已闭 · Cap residual freestanding `m[i].xs` call-arg · L2）

tip 产品 **`a9256927e`**。FIELD over INDEX as T[N]/[]T call-arg。
