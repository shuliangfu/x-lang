# X 语言 / X language 当前进度（2026-07-27 · wave522）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`93e7a3273`**（双端 L2，wave522；L4 待整波收口） |
| 钉盘 | **`2bc0885d3`**（待 Ubuntu 钉盘升级） |
| bstrict | **129** |

## wave522（已闭 · runtime_tls_mbedtls_bio Makefile R2 thin+rest 补全 双端 L2）

| 项 | 状态 |
|----|------|
| 触发 | wave521 发现的 6 个"半残"模块之一（.x wrapper + .from_x.c guard 已就绪但 Makefile 规则未跟上） |
| 模块 | `runtime_tls_mbedtls_bio`（82 行；mbedTLS BIO send/recv 胶层 + ssl_bind_fd_c） |
| 债 | `.x` 已有 2 个 `#[no_mangle]` wrappers（bio_send/bio_recv 调 _impl）；`.from_x.c` 已有 guard + _impl 实现；**Makefile 规则未跟上** |
| 根修 | 补全 Makefile 规则为 thin+rest `ld -r` 模式；两个 CC fallback 路径（homebrew mbedtls / 基本 include）都加 `-DXLANG_RUNTIME_TLS_MBEDTLS_BIO_FROM_X=1` guard；用 `{ }` 分组确保 `\|\|` fallback 与 `&& ld -r` 正确组合 |
| 构建模式 | **thin+rest**——thin (.x) 提供 2 个 BIO wrappers；rest (.from_x.c) 提供 _impl（send/recv syscall + errno→mbedTLS 映射）+ `xlang_mbedtls_ssl_bind_fd_c`（始终编译，取 thin wrappers 函数地址 via 前向声明） |
| nm 一致性 macOS | cold vs R2：5 核心 T 符号对齐（xlang_mbedtls_bio_send/send_impl/recv/recv_impl/ssl_bind_fd_c），仅 R2 多 `_doc_anchor` |
| nm 一致性 Ubuntu | cold vs R2：5 核心 T 符号对齐（同上） |
| 产品矩阵 macOS | g-ffi-5-std-wrap ✅ · crypto 全链路 ✅ |
| 产品矩阵 Ubuntu | g-ffi-5-std-wrap ✅ · crypto 全链路 ✅ |
| 验证级别 | **L2 日常真测**（按自举方法 §0.2 节奏；非每微 commit 全量 L4） |
| PLATFORM | SHARED — Unix BIO guarded by `!WIN32`；mbedtls include 路径有 homebrew fallback（macOS）vs 系统路径（Ubuntu） |
| 关键发现 | **mbedtls include fallback 模式**：rest 编译需要 `mbedtls/ssl.h`，macOS 用 `/opt/homebrew/opt/mbedtls/include`，Ubuntu 用系统路径。用 `{ CC1 \|\| CC2 } && ld -r` 分组确保 fallback 与 ld -r 正确组合（避免 `\|\|` 优先级陷阱） |
