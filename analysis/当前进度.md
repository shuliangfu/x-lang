# Shux 当前进度（2026-07-22）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`3c88f7e51`**（含 wave110 ImportEntry pure 整波 tip L4） |
| tip 产品代码 | **wave158** invoke_cc_append_net_tls_ld pure @ `c6af17207` |
| 产品 bstrict | 钉盘 **129**；本波日常 L2（未 tip L4） |
| 本波性质 | **R2 Cap residual pure · invoke_ld L6**（`invoke_cc_append_net_tls_ld`） |
| Ubuntu | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| mac | ✅ L2：g05 + 矩阵 rv42/option102/si/hello + string/vec/slice/compress + prove IDENTICAL |
| Windows | **不需要切换** |

## 本波摘要

1. **切入**：其它 rest R2 可迁叶 — `invoke_cc_append_net_tls_ld` hybrid 下仍 always mega C（OpenSSL/mbedTLS marker 探测 + brew -L + -l*）  
2. **根修**：迁入 `labi_invoke_ld_list` L6 pure orch（pure flag/marker/rel 表 + peer `labi_append_{openssl,mbedtls}_ld_flags` + Cap residual exports_marker / realpath_cap / rel_o_path / push_existing / host_is_apple）  
3. **权威**：`.x` + cold twin（`#include`）+ surface re-pin + mega 删 always 体；prove **IDENTICAL**  
4. **注意**：`ensure_std_net_o_auto_tls`（system/make）仍 Cap residual 不硬迁  
5. **验收**：双端 L2 g05 + 矩阵 + string/vec/slice/compress 邻域 + prove IDENTICAL；钉盘仍 **`3c88f7e51`**  

## 下一波前排

1. 其它 rest R2 / mega residual（可迁：path / freestanding residual …；常驻 Cap 不硬迁：contains_substr / undef_sym / path_io stat / wait / strerror / mega spawn-IO / ld_debug_argv / invoke_cc_argv_push_existing realpath 池 / ensure_std_net_o_auto_tls system·make …）  
2. 可选：未闭合块注释硬诊断（EOF depth>0）  
3. 可选 Windows hybrid（**需要时再通知重启**）  

```text
钉盘：3c88f7e51（未升）
tip：c6af17207 wave158 invoke_cc_append_net_tls_ld pure
构建级别=L2 日常真测（非 tip L4）
Windows：不需要切换
下一：path / freestanding residual / 其它 rest R2
```
