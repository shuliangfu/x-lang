# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`d4e2dca6`**（双端 tip L4 真冷 + 129；升自 `86281cc6`） |
| tip 产品代码 | **pipeline_abi wave50 pure residual** @ `d2aeab88` |
| 产品 bstrict | **129** · tip L4 钉盘仍 **`d4e2dca6`**（本波日常 L2，未升钉） |
| 本波性质 | **R2 pipeline_abi Cap residual pure wave50**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + pure-asm 矩阵 rv42 / option / hello / nest / stdlib-import；transitive_impl pure **W** + Cap residual paths_tmp/tmp_parse/strdup **T** @ `d2aeab88` |
| mac | ✅ L2 PREFER hybrid + c-backend 矩阵（Darwin pure-asm CG002 tip 既有）@ `d2aeab88` |
| Windows | **本波不需要切换** |

## 本波摘要

1. **R2 pipeline_abi Cap residual pure wave50** @ `d2aeab88`  
   - **迁 pure**：`shux_collect_deps_transitive_impl` + `shux_collect_dep_paths_transitive_impl`（orch）  
   - **G.7 复用**：`shux_collect_seed_to_load` + pure `deps_process_one` / `paths_process_one`  
   - **栈布局**：`to_load[32]` → 256B；tmp_arena/tmp_module → 16B void-star cells（同 check_one_file argv 模式）  
   - **仍 seed 重 C**：FILE view / resolve multi / preprocess / tmp_parse Cap residual；thread emit  
   - **seed**：冷孪生 `#ifndef FROM_X`；FROM_X rest 补 pure transitive_impl **extern**  
   - 构建级别：**日常 L2**；钉盘仍 **`d4e2dca6`**  
   - 日志：`/tmp/mac_g05_wave50.log` · `/tmp/ubuntu_g05_wave50.log` · matrix `/tmp/mac_g05_wave50_matrix.log` · `/tmp/ubuntu_g05_wave50_matrix.log`

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文

## 下一波前排

1. rest R2：pipeline resolve / load_one_direct / thread 重 C residual / 其它 rest pure  
2. **可选** Windows hybrid；Darwin arm64 pure-asm CG002（tip 既有）  
3. 整波收口再 tip L4（非每微步）  

```text
钉盘：d4e2dca6（tip L4 双端 129；wave50 未升钉）
产品 tip：d2aeab88 pipeline_abi residual pure wave50 L2
Windows：本波不需要切换
下一：resolve/load residual pure / Darwin CG002（需时再说）
```
