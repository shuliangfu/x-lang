# X 语言 / X language 当前进度（2026-07-24）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **重命名分析**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)  
> **改名前锚点**：`anchor-pre-xlang-rename` @ `d07bcf155`（父钉曾 `a77fc5d90`）  
> **项目目录**：现行 `…/xlang/x-lang`（mac/Ubuntu/Windows 同构）  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only**（第三品牌 `shu_*` / `SHU_*` / `shux` 已清） |
| 主域 | **xlang.dev** |
| 工程 | **全库重命名 R0–R3 已闭** · 双端 tip L4 **129 全绿** · 钉盘 **`75c638465`** |
| process_xlang 兼容别名 | **已删除** |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **已恢复** · **wave296** Cap residual pure 已闭（f32*f64 mixed mul 提升 + mulsd） |

## 验收

### tip L4 钉盘（仍有效）

| 项 | 状态 |
|----|------|
| SHA | **`75c638465`**（wave288：invoke_cc push `runtime_link_abi_user_env.o`） |
| 构建级别 | **L4 真冷**（全擦 compiler/std/core `.o` + 重链产品二进制） |
| remaining_o 起编 | ✅ **0**（双端） |
| mac 全量 bstrict | ✅ **OK (129)** · `/tmp/mac_true_cold_75c638465.log` · `/tmp/mac_true_bstrict_75c638465.log` |
| Ubuntu 全量 bstrict | ✅ **OK (129)** · `/tmp/ubuntu_true_cold_75c638465.log` · `/tmp/ubuntu_true_bstrict_75c638465.log` |
| 前钉 | `3698e557f`（R3 重命名 tip L4；wave238–287 仅 L2） |

### 本波 tip（wave296 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`731b6c6e8`** — R2 Cap residual pure wave296（f32*f64 mixed mul promote + mulsd） |
| 构建级别 | **L2**（rebuild `typeck_x.o` + `pipeline_glue_standalone.o` + g05 + 探针 + 产品矩阵；**非 tip L4**） |
| 根因 | typeck 混合 f32*f64 先命中 f32 分支 → 结果 f32；glue MUL 仅双方同标量类走 mulss/mulsd，混合落 `imul` IEEE 位型 → Ubuntu freestanding **run=0**（mac host-gcc 可藏） |
| 修法 | G.7：usual arithmetic conversion — **f64 before f32**（`typeck.x` + `typeck_gen` + empty_surface + `ast_pool` 同 commit）；glue `glue_try_emit_mixed_f32_f64_mul_elf_c` 固定放置 left=rbx/right=rax，复用 wave293 `cvtss2sd` 提升 f32 侧后 `mulsd`（无新 encoder） |
| 探针 | Ubuntu freestanding：`f32*f64`=42 · `f64*f32`=42 · `let c:f64=a*b`=42 · disasm `cvtss2sd`+`mulsd`+`cvttsd2si`；f32 mul / f64 mul / u64 as f64 回归绿 |
| mac 矩阵 | ✅ rv=42 · option/si/hello · host mixed/assign/f32/f64 绿 · `/tmp/mac_g05_wave296.log` · `/tmp/mac_w296_matrix.log` |
| Ubuntu 矩阵 | ✅ rv=42 · option/si/hello · freestanding mixed mul 全绿 · `/tmp/ubuntu_g05_wave296.log` · `/tmp/ubuntu_w296_probes.log` |
| 产品钉盘 | **仍 `75c638465`**（日常 L2 不升钉） |

```text
下一：wave297 Cap residual
  leave-off：
   · AST name/var_name 升布局（仍 cap 63；L011/L012 诚实失败）
   · `*u8 + *u8` 运行时字符串拼接运算符
   · host residual：getenv/system/realpath _impl + resolve pool；freestanding stubs
   · f32±/÷f64 mixed 提升（本波仅 MUL；ADD/SUB/DIV 可邻接同模式）
   · u64→f64 当值 >2^63-1 的无符号转换序列（ISA 为 signed i64 convert）
  钉盘 75c638465；日常 L2，整波/日终再 L4
```
