# X 语言 / X language 当前进度（2026-07-29 · wave652 Cap x86 high-end STRUCT_LIT 固定数组字段 frame mag · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 05:59 · 28 全日（wave534–617）已归楼；本文仅保留 29 日 tip 快照

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`be48b6acf`**（wave652 STRUCT_LIT fixed-array field frame mag） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave652（已闭 · Cap residual x86 high-end STRUCT_LIT 固定数组字段 · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `W{ pad: i32, xs: i32[2] }` Ubuntu freestanding：`return w.pad`→32（=xs[1]）、`take(w.xs)`/`m[0].xs`=0（host-C 42；arm64 本绿；`xs` 在首字段时绿） |
| tip 产品 | **`be48b6acf`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave652 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| 固定数组字段 dest frame mag 恒 `base+foff` | `glue_struct_lit_store_fixed_array_field_elf_c` | ✅ `glue_struct_field_frame_mag_c`：x86 high-end `base-foff` · arm64 `base+foff`（≡ wave595 nest_slot） |

**数据流**：产生 = STRUCT_LIT 固定 TYPE_ARRAY 字段写 `base_off+foff` 当 frame mag。存储 = 数组落在 Outer **之前**（lea -0x24 vs byte0 -0x20）。消费 = xs[1]@+4 覆盖 pad；读 pad 得 32；take(w.xs) 错址。G.7：单一 `glue_struct_field_frame_mag_c`；ARRAY_LIT/zero/CALL/VAR dest + FIELD src + nest_slot 复用；sret 真地址 `+foff` 不变。

**权威**：`compiler/pipeline_glue.c` · `glue_struct_field_frame_mag_c` + `glue_struct_lit_store_fixed_array_field_elf_c`

### wave652 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `take(m[0].xs)` / `take(w.xs)` / lit pad | ✅ 42 | ✅ 42（was fs=0） |
| `return w.pad` / dump pad+xs0+xs1 | ✅ 7 / 42 | ✅ 7 / 42（was 32 / 1） |
| i64 pad / a,b,xs / nopad / fsrc FIELD copy | ✅ 42 | ✅ 42 |
| wave651 mdim 回归 | ✅ 42 | ✅ 42 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |
| Ubuntu disasm | — | `lea -0x1c`（xs@byte0+4；was `-0x24`） |

日志：`/tmp/mac_g05_wave652.log` · `/tmp/ubuntu_g05_wave652.log` · `/tmp/ubuntu_w652_probes.log`

### 仍 soft

- untyped `self`（wave493 非标准）  
- `break` / `continue` freestanding 假跑（扫叶顺带发现）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（break·continue / 其它），或整波 L4 + 升钉  

## wave651（已闭 · Cap residual freestanding `m[i].xs` call-arg · L2）

tip 产品 **`a9256927e`**。FIELD over INDEX as T[N]/[]T call-arg。

## wave650（已闭 · Cap residual if/while cond 整式分组 · L2）

tip 产品 **`d5aec6a64`**。if/while 条件不剥前导 `(`。
