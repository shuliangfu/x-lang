# X 语言 / X language 当前进度（2026-07-29 · wave689 []T ret mono · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)  
> **日切**：2026-07-29 05:59 · 28 全日（wave534–617）已归楼；本文仅保留 29 日 tip 快照

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`3ef22fd0b`**（wave689 free `[]T` ret mono structural + intermediate peel） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave689（已闭 · Cap residual free `[]T` ret mono · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | `id_s([]T): []T` → mono ret `struct xlang_slice_<mod>_T` incomplete（by-value BLD001）；`*[]T` ret 假绿 incomplete `struct *` |
| tip 产品 | **`3ef22fd0b`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave689 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| ret `[]T` 发 free-T slice 标签 | `emit_type` TYPE_SLICE NAMED-elem 路径**不**递归 `emit_type(elem)`（与 TYPE_PTR 不同）；C5 身份只命中 formal 的 `[]T` 节点，ret/body 另一节点不命中 | ✅ TYPE_SLICE 入口 structural free-leaf 名匹配 mono gen SLICE → emit concrete |
| `*[]T` 中间层无 map | wave688 peel 只 append free TYPE_NAMED leaf；`*[]T` 内层 `[]T` 不进 map → ret `*[]T` peel 到 free `[]T` 仍 incomplete | ✅ peel 同时 map intermediate free compounds（`[]T`→`[]i32`） |

**数据流**：产生=identity mono 签名 ret `emit_type([]T)`。存储=mono map formal/combo + peel。消费=host-C ret/body 类型标签。G.7：补全同一 emit_type + 同一 peel（非第二套 subst）。

**权威**：`codegen.x` + seed `codegen_gen.linux.x86_64.c`（emit_type TYPE_SLICE structural + peel intermediate）

### wave689 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `id_s([]T):[]T` assign / index → 30/10 · C `struct xlang_slice_int32_t` | ✅ | ✅ |
| `id_ptr_slc(*[]T)` → 10 · C `struct xlang_slice_int32_t *` | ✅ | ✅ |
| wave685–688 `id_ptr` / `first_s` / `first_a` | ✅ 42/10/10 | ✅ |
| 矩阵 rv42 / option / hello / si | ✅ | ✅ |

日志：`/tmp/mac_g05_wave689.log` · `/tmp/ubuntu_g05_wave689.log` · `/tmp/ubuntu_w689_probes.log`

### 仍 soft

- untyped `self`（wave493 · **跳过**；P011 更早失败）  
- LANG-006 标量 let/const bool→int（**产品保留**）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（skip untyped self；LANG-006 保留），或整波 L4 + 升钉  

## wave688（已闭 · Cap residual `*T` call ret mono · L2 工程轨）

见 wave688 节历史；tip **`930b247df`** · 钉盘仍 **`01479a857`**。

## wave687（已闭 · Cap residual `T[N]`/`[]T` formal mono mangle · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | wave686 typeck 已接受 `first_a(T[2])` / `first_s([]T)`，codegen mono **ec=-6** / XP003；`[]T` 另 BLD001 形参按值 vs 指针 ABI |
| tip 产品 | **`c7fc0e79d`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave687 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| mono mangle 无 ARRAY/SLICE 后缀 | `codegen_type_ref_to_suffix` 仅 PTR/NAMED/builtin/VECTOR；combo key `i32[2]`/`[]i32` → suffix 0 → `emit_mono_mangled_name` -1 | ✅ `_a<N>` / `_slc` |
| `[]T` mono 形参按值 | identity mono 只 `emit_type`，未仿 `emit_func` 的 TYPE_SLICE→`struct *` | ✅ mono 形参后追加 ` *` |

**权威**：`codegen.x` · seed `codegen_gen.linux.x86_64.c`

### wave687 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `first_a(T[2])` → run=10 · mangle `*__i32_a2_i32` | ✅ | ✅ |
| `first_s([]T)` → run=10 · `struct * s` | ✅ | ✅ |
| wave685/686 `id` / `load(*T)` | ✅ 42 | ✅ 42 |
| arr size mismatch `T[2]` vs `i32[3]` | T001 红 | T001 红 |
| 矩阵 rv42 / option / hello / si | ✅ | ✅ |

日志：`/tmp/mac_g05_wave687b.log` · `/tmp/ubuntu_g05_wave687.log`

## wave686（已闭 · Cap residual compound free-T CALL formals · L2 工程轨）

见 wave686 节历史；tip **`045870cbf`** · 钉盘仍 **`01479a857`**。
