# X 语言 / X language 当前进度（2026-07-29 · wave635 Cap pure-asm TYPE_ARRAY call-arg E* · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`b5615797e`**（wave635 产品） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave635（已闭 · Cap residual pure-asm fixed TYPE_ARRAY call-arg E* not MEMORY · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | freestanding `take(s: S24[2])` / `take(mk())` / `take(lit)` SEGV；S12[2] 同；S8[2] 与 named S24 MEMORY 绿 |
| tip 产品 | **`b5615797e`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave635 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| T[N] call-arg 按 payload MEMORY | `pipeline_asm_call_arg_value_byte_size_c` 对 formal TYPE_ARRAY 走 `glue_type_size_simple` 全载荷（48/24）→ `gp_units=0` MEMORY 多字压栈；callee `glue_func_param_agg_byte_size` 已 wave417 定为 E* 8B + INDEX 当指针 | ✅ 补全 call-arg size → **8**（≡ TYPE_SLICE / param_agg） |

**数据流（五问）**：产生 = call-arg size 分类 → 存储 = SysV/AAPCS pack → 消费 = param_home E* + INDEX。同模式：TYPE_SLICE 已 8；param_agg 已 8；**缺 call-arg 孪**。G.7：只补 `pipeline_asm_call_arg_value_byte_size_c`，不新开第二套传参路径。

**权威**：`compiler/pipeline_glue.c` · `pipeline_asm_call_arg_value_byte_size_c`（standalone glue 入链）。

### wave635 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `take(mk())` S24[2] pure/fs/host | ✅ 77 | ✅ 77 |
| `take(s)` local VAR pure/fs/host | ✅ 77 | ✅ 77 |
| `take([…])` ARRAY_LIT pure/fs/host | ✅ 77 | ✅ 77 |
| `take(mk())` S12[2] pure/fs/host | ✅ 44 | ✅ 44 |
| S8[2] dual-GP / named S24 MEMORY | ✅ 42 | ✅ 42 |
| Ubuntu disasm | — | `lea …,%rax; mov %rax,%rdi; call take` |
| mac disasm | `add x0,x29,#0x10; bl take` | — |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

### 仍 soft

- 其它 Cap residual 叶（impl method on `s[0]` typeck；`*S24[2]` 解析序）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶，或整波 L4 + 升钉  

## wave634（已闭 · Cap residual soft 假红：uninit TYPE_ARRAY assign · docs-only）

产品 tip 仍曾为 `9a16fb325`；合法 `let s: S24[2]; s=mk();` 双端 110。

## wave633（已闭 · Cap residual pure-asm fixed TYPE_ARRAY let bulk esz>8）

tip 产品 **`9a16fb325`**。fixed S24[2] let bulk 绿。
