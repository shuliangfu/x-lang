# X 语言 / X language 当前进度（2026-07-28 · wave618 Cap f32[] host-C slice type · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`7a943902f`**（wave618 Cap f32[]/f64[] host-C slice type · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave618（已闭 · Cap residual f32[]/f64[] host-C TYPE_SLICE C 名 · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | mac host-C / `-E`：`let a: f32[] = [10.0, 32.0]` → `struct xlang_slice_int32_t` + `static float[]`；clang -O2 UB（int 读 float 位再整数加）→ run 假 0；Ubuntu pure-asm 本绿掩盖 |
| tip 产品 | **`7a943902f`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave618 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| `type_kind_cstr` F32/F64/VOID 序数漂移 | `ast_pool.c` `pipeline_codegen_type_kind_cstr` | ✅ case 14/15/16（旧 11/12/13 实为 SLICE/LINEAR/VECTOR） |
| TYPE_SLICE of f32 → int32_t fallback | `type_to_c_repr` → `xlang_slice_int32_t` | ✅ `struct xlang_slice_float` / `_double` |
| preamble 缺 float/double slice 布局 | `rt_preamble.from_x.c` | ✅ 增 `xlang_slice_float` / `xlang_slice_double` |
| call-arg fat 映射缺 F32/F64 | codegen.x + seed | ✅ G.7 补全同 map |

**数据流（五问）**：产生 = `type_kind_cstr` 枚举漂移后 F32=14 落 default → int32_t；存储 = TYPE_SLICE C 标签；消费 = host-cc 按 `int32_t*` 读 `float` 载荷。G.7 **单表权威** 修 ordinal，不新开第二套 slice 命名。

**权威**：`ast_pool.c` + `rt_preamble` + `codegen.x`/seed 同 commit。改后 **须** `PIPELINE_X_FORCE_COMPILE=1` 重建 `pipeline_x.o`（内嵌 ast_pool）+ standalone + preamble + g05；仅 rebuild standalone 时 mac 链序可能仍用旧 `pipeline_x.o` 的 cstr。

### wave618 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| host-C `f32[]` sum cast | ✅ 42（`-E` → `xlang_slice_float`） | ✅ 42 |
| host-C `f64[]` | ✅ 42 | ✅ 42 |
| pure-asm / freestanding f32[] f64[] | ✅ 42 | ✅ 42 |
| f32[2] / i32[] 回归 | ✅ 42 | ✅ 42 |
| rv / option / si / hello | ✅ | ✅（cwd=仓根） |

### 仍 soft

- 其它 Cap residual 叶；整波双端 **L4** + 升钉  
- 动 `ast_pool` 时勿只热 relink standalone 冒充产品链（双定义符号）

### 下一波

1. Cap residual soft 其它叶或整波 L4 + 升钉  

## wave617（已闭 · Cap residual f32 ARRAY_LIT typeck stamp · L2）

tip 产品 **`1a11a6a72`** / docs **`6f5fe2205`**。

## wave616（已闭 · Cap residual arm64 freestanding float · L2）

tip 产品 **`dbd0d2d16`** / docs **`1aa53a14f`**。
