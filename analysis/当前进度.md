# X 语言 / X language 当前进度（2026-07-29 · wave639 Cap freestanding `(*p).xs` · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`2b274afdc`**（wave639 产品） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave639（已闭 · Cap residual freestanding DEREF FIELD INDEX · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | freestanding `(*p).xs[i]` / `take((*p).xs)` / assign → SEGV；host-C 与 `p.xs` 已绿 |
| tip 产品 | **`2b274afdc`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave639 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| DEREF-base FIELD INDEX | `glue_try_index_var_or_field_base_*` 仅 VAR / CALL·STRUCT_LIT；`(*p).xs` 回落 field rvalue `ldr w` 首元当指针 | ✅ emit DEREF 操作数 + field_off（rax/rbx） |

**数据流**：产生 = INDEX base helper（含 wave610 call-arg 复用）→ 存储 = 机器码 lea/add → 消费 = load/store。G.7：补全既有 helper，不新开第二套 FIELD 数组路径。

**权威**：`compiler/pipeline_glue.c`（`glue_try_index_var_or_field_base_to_{rax,rbx}_elf_c`）。

### wave639 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `(*p).xs[1]` host/pure/fs | ✅ 32 | ✅ 32 |
| `take((*p).xs)` host/pure/fs | ✅ 32 | ✅ 32 |
| assign `(*p).xs[i]` host/pure/fs | ✅ 42 | ✅ 42 |
| `p.xs[1]` / `take(p.xs)` 回归 | ✅ 32 | ✅ 32 |
| wave638 `(*p).v` | ✅ 42 | ✅ 42 |
| wave637 `*i32[2]` | ✅ 42 | ✅ 42 |
| disasm | `add #4` / `add $0x4` | 同 |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

### 仍 soft

- 嵌套 `(*(*pp)).v` — DEREF 结果 TYPE_PTR 经 `glue_index_elem_byte_sz` 剥到 pointee 宽 4（半指针 load）  
- untyped `self`（wave493 非标准）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶（如 DEREF-of-PTR load 宽 8），或整波 L4 + 升钉  

## wave638（已闭 · Cap residual host-C FIELD base primary · L2）

tip 产品 **`b1a8c6cfc`**。host-C `((base).field)` 修 `(*p).v` BLD001。

## wave637（已闭 · Cap residual freestanding array-of-pointers `*T[N]` · L2）

tip 产品 **`e82ecdbd2`**。INDEX esz=8 + fixed-array PTR slot=8。
