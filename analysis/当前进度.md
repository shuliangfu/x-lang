# X 语言 / X language 当前进度（2026-07-27 · wave531）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`6a2cae741`**（双端 L2，wave531；L4 待整波收口） |
| 钉盘 | **`d5de2f13b`**（升自 `2bc0885d3`；双端 L4 129 全绿） |
| bstrict | **129** |

## wave531（已闭 · runtime_path_fast Makefile R2 DIRECT 补全 双端 L2）

| 项 | 状态 |
|----|------|
| 触发 | wave530 闭后，按"含 `#[no_mangle]` 但 Makefile 缺 R2 规则"三路交叉对比（.x / .from_x.c 守卫 / Makefile R2 规则）继续审计；定位 `runtime_path_fast` — .x 已完整实现 16 函数但 Makefile 仅冷路规则 |
| 模块 | `runtime_path_fast`（产物 `../std/path/path.o`；16 函数：path_sep/is_sep/last_sep/last_dot + std_path_join/dirname/basename/is_absolute/extension/stem/extension_and_stem/clean/resolve + empty_len/sep） |
| 债 | `.x`（src/asm/runtime_path_fast.x）已含 16 个 `#[no_mangle]` 完整实现 + doc_anchor；`.from_x.c` 已有 `XLANG_RUNTIME_PATH_FAST_FROM_X` 守卫；**Makefile 规则仅冷路 cc -c + ld -r**（且 .x 未列入依赖） |
| 根修 | Makefile 加 R2 DIRECT 分支：`XLANG_G05_PREFER_X_O=1` 时用 `xlang-c -lib-name "" -o` 直接编译 .x → path_core.o，再 `ld -r` 链接成 `../std/path/path.o`（保留原 `ld -r` 模式兼容 std/path 路径聚合）；冷路 fallback 保留 |
| 构建模式 | **DIRECT**——纯计算模块（指针扫描 + min 钳制），无 OS 依赖、无 `_impl` bridge、无 C-only 函数 |
| nm 一致性 macOS | cold 16 T vs R2 DIRECT 17 T 符号（R2 多 `_runtime_path_fast_x_doc_anchor`，与其他 R2 模块一致） |
| nm 一致性 Ubuntu | cold 16 T vs R2 DIRECT 17 T 符号（同上；macOS/Ubuntu 完全一致） |
| 产品矩阵 macOS | run-hello.sh ✅ exit 0 · g-ffi-5-std-wrap ✅ exit 0 · g-ffi-5-release-ci ✅ exit 0 |
| 产品矩阵 Ubuntu | run-hello.sh ✅ exit 0 · g-ffi-5-std-wrap ✅ exit 0 · g-ffi-5-release-ci ✅ exit 0 |
| 验证级别 | **L2 日常真测**（按自举方法 §0.2 节奏；非每微 commit 全量 L4） |
| PLATFORM | SHARED path — 纯计算，无 OS 依赖；macOS + Ubuntu 双端验证 |
| 关键发现 | ① 三路交叉对比审计有效：含 `#[no_mangle]` 的 .x 95 个 / .from_x.c 守卫 33 个 / Makefile R2 规则 32 个 → 真未覆盖候选仅 3 个（path_fast、std_runtime_fast、lsp_glue）+ 若干已通过其他模块规则编译的（net_io_batch_fast 嵌入 std/net/net.o 等） ② `runtime_path_fast` 是 wave515 `runtime_string_fast`、wave530 `runtime_slice_glue` 同类的 DIRECT 模式候选（纯计算无 OS 依赖） ③ DIRECT 模式下 `ld -r` 链接产物与冷路 `cc -c .from_x.c + ld -r` 在符号上一致（除 doc_anchor 外） ④ .from_x.c 中 `path_sep_c` Win 分支返回 `'\'` (92)，R2 模式下 .x 总返回 `'/'` (47)，posix 验收路径无回归 |

## 下一步候选

1. **`runtime_std_runtime_fast`**（6 `#[no_mangle]`，调用 `extern "C" xlang_panic_` / `xlang_crash_evidence_collect_c` — thin wrapper 模式，需查 _impl 落点）
2. **`runtime_lsp_glue`**（60 `#[no_mangle]`，量较大，需评估迁移风险）
3. **整波收口 L4**：当前累积 wave528-531 共 3 个 R2 migration（runtime_http_glue + runtime_slice_glue + runtime_path_fast），可按 §0.2「整波结束做一次扎实 L4」+ 日终兜底触发 L4 真冷全测双端 129 bstrict
4. **钉盘升级候选**：若 L4 双端 129 全绿，可升钉自 `d5de2f13b`
