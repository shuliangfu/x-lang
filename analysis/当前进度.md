# X 语言 / X language 当前进度（2026-07-28 · wave593 Cap nested CALL.field + mid-size STRUCT_LIT inline · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`789b485b0`**（wave593 Cap nested CALL.field · L2 双端绿） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave593（已闭 · Cap residual nested CALL.field + mid-size STRUCT_LIT inline · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | freestanding `mk().i.f` CG002；9–16B `mk().a` CG002（let 内联绿） |
| tip | **`789b485b0`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave593 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| `mk().i.f` 嵌套 FIELD | `glue_field_access_call_base` 仅 base=CALL；base=FIELD 回落 lvalue → CG002 | ✅ 沿 FIELD 链走到 CALL/METHOD + 累加 AoS offset |
| 9–16B STRUCT_LIT `mk().a` | ≤16 只走 emit CALL+store_retval；let 先 try_inline 故绿 | ✅ 任意 size 先 try_inline（≡ let_init） |
| arm64 dual-GP 第二 half | `store_retval_pair` `ta!=0` 早退只写 rax | ✅ arm64 home+8 写 rdx/x1 |

**数据流（五问）**：产生 = freestanding FIELD_ACCESS 链 / 中宽 STRUCT_LIT 返回 CALL；存储 = 帧 temp home + 字段偏移；消费 = load outermost field。wave589/590 只开单层 CALL 基；嵌套与 mid-size 洞仍红。权威：G.7 补全 `glue_field_access_call_base_rvalue_elf_c` + dual-GP arm64 半字。

**权威**：`compiler/pipeline_glue.c`（`pipeline_glue_standalone.o`）。改后 rm standalone.o + g05。

### wave593 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| nest2 `mk().i.f` `-freestanding` / pure-asm | ✅ 42 | ✅ 42 |
| sz12/sz16 STRUCT_LIT `mk().a` | ✅ 42 | ✅ 42 |
| sz24 true-sret / callarg 三字段 | ✅ 42 | ✅ 42 |
| nest2step/mid 回归 | ✅ 42 | ✅ 42 |
| rv / option / si / hello | ✅ 42/102/0/0 | ✅ 42/102/0/0 |

### 仍 soft

- 非 inline 中宽 return-local CALL.field（`let t=S{…}; return t` 再 `mk().a`）  
- 更深嵌套 / 大布局 STRUCT_LIT（如 Outer+Mid+Inner multi-field let 亦红 → 非本波 FIELD 链）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual：return-local mid-size dual-GP / 深嵌套 STRUCT_LIT，或整波 L4  
2. 整波双端 L4 + 升钉  

## wave592（已闭 · Cap residual freestanding_io ensure on nostdlib face）

tip `2b2b2921b` / docs `46eb10388`。soft 侧门本波部分由 **wave593** 收。
