# X 语言 / X language 当前进度（2026-07-23）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **重命名分析**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)  
> **改名前锚点**：`anchor-pre-xlang-rename` @ `d07bcf155`（父钉曾 `a77fc5d90`）  
> **项目目录**：现行 `…/xlang/x-lang`（mac/Ubuntu/Windows 同构）  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only**（第三品牌 `shu_*` / `SHU_*` / `shux` 已清） |
| 主域 | **xlang.dev** |
| 工程 | **全库重命名 R0–R3 已闭** · 双端 L4 **129 全绿** · 钉盘 **`3698e557f`** |
| process_xlang 兼容别名 | **已删除** |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **已恢复** · **wave284** Cap residual pure 已闭（标识符容量 L012） |

## 验收

### tip L4 钉盘（不变）

| 项 | 状态 |
|----|------|
| SHA | **`3698e557f`**（`rename(R3): shu_* → xlang_*`） |
| 构建级别 | **L4 真冷**（全擦 compiler/std/core `.o` + 重链产品二进制） |
| mac / Ubuntu 全量 bstrict | ✅ **OK (129)** |

### 本波 tip（wave284 · 日常 L2，**≠** tip L4 升钉）

| 项 | 状态 |
|----|------|
| SHA | **`3abd20317`** — R2 Cap residual pure wave284 |
| 构建级别 | **L2**（seed lexer/parser + g05 + 产品矩阵） |
| mac 矩阵 | ✅ rv=42 · option/si/hello · id63=42 · id64 L012 · L011/L010 回归 · `/tmp/mac_g05_wave284.log` · `/tmp/mac_w284_matrix.log` |
| Ubuntu 矩阵 | ✅ 同 · `/tmp/ubuntu_g05_wave284.log` · `/tmp/ubuntu_w284_matrix.log` |
| 产品钉盘 | 仍 **`3698e557f`**（本波未 L4 升钉） |

```text
下一：wave285 Cap residual
  （wave284 闭 标识符 AST 名容量 soft residual —
     根因：ident span >63 静默 clamp / XP003 无硬 L0xx）
  leave-off：
   · AST name/var_name 升布局（仍 cap 63；L011/L012 诚实失败）
   · `*u8 + *u8` 运行时字符串拼接运算符（现指针加 → BLD001；用 std.string）
   · host residual：getenv/system/realpath _impl + resolve pool；freestanding stubs
  钉盘仍 3698e557f；日常 L2，整波收口再 L4
```

