# X 语言 / X language 当前进度（2026-07-29 · wave649 Cap freestanding FIELD STRUCT_LIT/CALL → []T · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`84f9b813c`**（wave649 freestanding FIELD STRUCT_LIT/CALL → TYPE_SLICE） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave649（已闭 · Cap residual freestanding FIELD STRUCT_LIT/CALL as `[]T` · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | freestanding `take(W{xs:T[2]}.xs)` / `take(mk().xs)` 作 `[]T` → CG002；host-C 绿；`take(w.xs)` VAR 绿；`take(W{}.xs)` 作 `T[N]` wave610 绿；i32/f32 同径 |
| tip 产品 | **`84f9b813c`** |
| 构建级别 | **L2**（mac+Ubuntu g05；**非** L4；钉盘不升） |

### wave649 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| STRUCT_LIT/CALL 基 FIELD→`[]T` fat 实参 | wave396 用 `pipeline_asm_emit_lvalue_eff_addr`（仅真左值）→ -1 → CG002 | ✅ 复用 `glue_try_index_var_or_field_base_to_rax`（≡ wave610）+ 既有 dual-GP fat pack |

**数据流**：产生 = call-arg FIELD 定长数组作 TYPE_SLICE · 存储 = fat home {data,len} · 消费 = callee INDEX。host-C 临时量 &field 绿；VAR-base lvalue 绿；STRUCT_LIT/CALL 非 lvalue。G.7：不新开第二套 FIELD→slice，只把 data 半地址权威对齐 wave609/610。

**权威**：`compiler/pipeline_glue.c` · `pipeline_asm_emit_expr_elf_for_call_args` wave396/649 块

### wave649 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| `take(W{…}.xs)` []i32 / []f32 | ✅ host/fs=42 | ✅ 42（was CG002） |
| `take(mk().xs)` []i32 | ✅ 42 | ✅ 42 |
| `take(w.xs)` VAR 回归 | ✅ 42 | ✅ 42 |
| nest `Outer.inn.xs` []i32 | ✅ 42 | ✅ 42 |
| wave610 T[N] / wave622 ARRAY_LIT / wave647 []f32 lit | ✅ 42 | ✅ 42 |
| ashr（let 中转） / `return x>>2` SAR | ✅ | ✅ 42 / disasm `sar` |
| rv / option / hello | ✅ 42/0/0 | ✅ 42/0/0 |

### 仍 soft

- untyped `self`（wave493 非标准）  
- `if (x >> 2) != k` 直接 binop 入 if 可丢 main（let 中转已绿；非本波债）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 下一硬叶，或整波 L4 + 升钉  

## wave648（已闭 · Cap residual freestanding signed `>>` / `>>=` · L2）

tip 产品 **`af5e1c609`**。freestanding signed `>>`/`>>=` SAR。

## wave647（已闭 · Cap residual freestanding `[]f32` call-arg · L2）

tip 产品 **`6e501513d`**。freestanding `[]f32` ARRAY_LIT call-arg f32 pack。

## wave646（已闭 · Cap residual freestanding unary `~` / `-` · L2）

tip 产品 **`c0067fb97`**。freestanding unary `~`/`-` GP canonicalize。

## wave645（已闭 · Cap residual arm64 freestanding rem `%` / `%=` · L2）

tip 产品 **`fd7782ec8`**。arm64 freestanding rem sdiv+msub。
