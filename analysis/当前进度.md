# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`86281cc6`**（双端 L4 真冷 + 129；未升） |
| tip | **R2 driver_abi Cap residual pure wave24**（table free/get/set + outbuf free/len/data；日常 L2） |
| 产品 bstrict | **129** · tip L4 钉盘仍 `@86281cc6` |
| 本波性质 | **工程轨 R2 pure 微步**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42/option/si/hello + pure-asm rv42 @ `cff39ad6` |
| mac | ✅ L2 同 |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `runtime_driver_abi` Cap residual pure wave24（本波）**  
   - 真迁：配对 wave23 calloc 的 free/get/set  
   - outbuf：`driver_codegen_outbuf_{free,len,data}`（len = LE i32 @ CAP；data = 基址）  
   - ptr 表：`driver_ptr_table_{free,get,set}`（G.7 `shux_ptr_slot_*`）  
   - size 表：`driver_size_table_{free,get,set}`（LP64 LE usize @ i*8）  
   - `driver_diag_snapshot_free`  
   - seed：cold twins 包 `#ifndef SHUX_L2_RDABI_THIN_FROM_X`  
   - 仍 seed：**tmp 槽**（`asm_tmp_path` / `parsed_tmp_c_*` — open_out 直写 static，暂不可迁）、巨型 `rt_preamble` 表  
   - 注释：**JSDoc 风格**（`@param` type+role / `@return`；`*/` 单独成行）  
   - 代码 SHA：**`cff39ad6`**（docs 本 commit 跟进）  
   - 矩阵：rv_build=0 / rv_run=42 / opt 102 / si 0 / hello 0 / pure-asm rv42  
   - 钉盘仍 **`86281cc6`**（skill §3.3：微步不自动 tip L4）

2. **注释纪律（固化）**  
   - 所有方法块注释参考 JSDoc；参数类型与作用写全；供未来 **`shux doc`** 生成  
   - skill G.9 + MEMORY 已更新  

3. **文档纪律**  
   - 只改 `自举进度.md` + 本文；**不**改 `当前问题分析.md` / `问题分析文档.md`

## 下一波前排

1. **可选** driver_abi 余 Cap residual（**tmp 访问器** 须先让 open_out 走 accessor；巨型 preamble 表）  
2. **可选** g05 sed / harness `-E` 评估  
3. **可选** Windows hybrid；Darwin arm64 纯 asm Vec4f CG002  

```text
钉盘：86281cc6（tip L4 双端 129）
tip：R2 driver_abi wave24 table free/get/set pure（L2 @ cff39ad6）
Windows：本波不需要切换
下一：tmp（open_out 同路径）/ rt_preamble 巨型表 / g05 sed / Windows / Darwin CG002
```
