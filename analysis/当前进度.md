# X 语言 / X language 当前进度（2026-07-28 · wave598 Cap ARRAY_LIT >8B dual-GP/sret + INDEX NAMED stride · 钉盘仍 01479a857）

> **长期仪表盘**：[自举进度.md](自举进度.md)（KPI 权威）  
> **问题地图**：[当前问题分析.md](当前问题分析.md)  
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only** |
| 主域 | **xlang.dev** |
| tip | **`eabf6abc1`**（wave598 Cap ARRAY_LIT >8B + INDEX NAMED · L2） |
| 钉盘 | **`01479a857`**（wave576 L4；本波**未升钉**） |
| bstrict | **129** |
| prove 白名单 | **111/111 IDENTICAL**（不变） |

## wave598（已闭 · Cap residual ARRAY_LIT >8B struct elems + INDEX NAMED stride · L2 工程轨）

| 项 | 状态 |
|----|------|
| 触发 | freestanding `S12[2]=[mk(),mk()]` run≠42；STRUCT_LIT 控制同红；host-C 假绿 |
| tip 产品 | **`eabf6abc1`** |
| 构建级别 | **L2**（**非** L4；钉盘不升） |

### wave598 根因收口

| 债 | 产生点 | 状态 |
|----|--------|------|
| >8B 数组元素只 store rax 且 store_sz clamp→4 | `pipeline_asm_emit_vector_let_init_elf_c` / `emit_array_lit` | ✅ per-elem `glue_emit_struct_type_let_init`（dual-GP/sret/STRUCT_LIT）arch-aware home |
| INDEX `xs[i]` 对 NAMED 元素步长默认 8 | `glue_index_elem_byte_sz_from_type_ref_c` bare TYPE_NAMED 落 8 | ✅ `glue_type_size_simple`（≡ wave597 array_lit esz） |

**数据流（五问）**：产生 = ARRAY_LIT 逐元素物化 + INDEX 步长；存储 = 帧数组槽；消费 = INDEX.field load。权威：G.7 复用 struct let_init + 补全 index NAMED（不新开第二套）。

**权威**：`compiler/pipeline_glue.c`（`pipeline_glue_standalone.o`）。改后 rm `build_asm/pipeline_glue_standalone.o` + g05。

### wave598 验证

| 探针 | mac L2 fs | Ubuntu L2 pure-asm |
|------|-----------|---------------------|
| arr_sz12 CALL 12B dual-GP | ✅ 42（原 20） | ✅ 42（原 20） |
| arr_sz16 CALL 16B dual-GP | ✅ 42（原 20） | ✅ 42 |
| arr_sz24 CALL 24B sret | ✅ 42（原 138） | ✅ 42 |
| arr_lit12 STRUCT_LIT | ✅ 32（原 11） | ✅ 32 |
| arr_simple 8B 回归 | ✅ 22 | ✅ 22 |
| nest4 / nest3 / ptr_mid / ptr_nest_call | ✅ 41/41/45/41 | ✅ nest4=41 |
| rv / option / si / hello | ✅ 42/0/0/0 | ✅ 42/0/0/0 |

### 仍 soft

- mac freestanding `take_m(mk()).f` 等 12B 形参 dual-GP 叶（tip 已 soft；Ubuntu pure-asm 仍 41）  
- 其它 Cap residual 叶（dyn Trait / XP003 等历史 soft）  
- 整波双端 **L4** + 升钉  

### 下一波

1. Cap residual 其它 soft 叶，或整波双端 L4 + 升钉  
2. 整波双端 L4 + 升钉  

## wave597（已闭 · Cap residual x86 sret nest high-end polarity + ARRAY_LIT NAMED esz）

tip `ca0e97771` / docs `d01d2c522`。soft「>8B CALL 数组元素」本波由 **wave598** 收。
