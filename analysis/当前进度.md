# X 语言 / X language 当前进度（2026-07-24）

> **长期仪表盘**：[自举进度.md](自举进度.md)（24 日起重新起表）  
> **23 号全日归档**：[自举进度-归档-2026-07-23.md](自举进度-归档-2026-07-23.md)  
> **重命名分析**：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)  
> **改名前锚点**：`anchor-pre-xlang-rename` @ `d07bcf155`（父钉曾 `a77fc5d90`）  
> **项目目录**：现行 `…/xlang/x-lang`（mac/Ubuntu/Windows 同构）  
> **GitHub**：`https://github.com/shuliangfu/x-lang`

## 总体状态

| 指标 | 状态 |
|------|------|
| 语言品牌 | 中文 **X 语言** · 英文 **X language** |
| 工具链 | `xlang` / `xlang_asm` / `xlang-c` · `XLANG=` |
| ABI 前缀 | **`xlang_*` only**（第三品牌 `shu_*` / `SHU_*` / `shux` 已清） |
| 主域 | **xlang.dev** |
| 工程 | **全库重命名 R0–R3 已闭** · 双端 tip L4 **129 全绿** · 钉盘 **`75c638465`** |
| process_xlang 兼容别名 | **已删除** |
| skill | `~/.grok/skills/xlang-selfhost-product-gate` |
| 自举 | **已恢复** · **wave329** Cap residual pure 已闭（freestanding slice array-lit） |

## 验收

### tip L4 钉盘（仍有效）

| 项 | 状态 |
|----|------|
| SHA | **`75c638465`**（wave288：invoke_cc push `runtime_link_abi_user_env.o`） |
| 构建级别 | **L4 真冷**（全擦 compiler/std/core `.o` + 重链产品二进制） |
| remaining_o 起编 | ✅ **0**（双端） |
| mac 全量 bstrict | ✅ **OK (129)** · `/tmp/mac_true_cold_75c638465.log` · `/tmp/mac_true_bstrict_75c638465.log` |
| Ubuntu 全量 bstrict | ✅ **OK (129)** · `/tmp/ubuntu_true_cold_75c638465.log` · `/tmp/ubuntu_true_bstrict_75c638465.log` |
| 前钉 | `3698e557f`（R3 重命名 tip L4；wave238–287 仅 L2） |

### 本波 tip（wave329 · 日常 L2）

| 项 | 状态 |
|----|------|
| SHA | **`aa38bd724`** — R2 Cap residual pure wave329（freestanding `i32[] = [1,2,3]`） |
| 前产品 | wave328 slice array-lit host C `725497942`；wave329a glue length `ec9f6bed3` |
| 构建级别 | **L2**（`pipeline_glue_standalone.o` + g05 + 探针 + 产品矩阵；**非 tip L4**） |
| 根因 | ① freestanding `TYPE_SLICE+ARRAY_LIT` 只 store data 指针、不写 dual-GP length → `a.length=0` / index panic；② slice INDEX lit 的 bounds guard 仍用 rbx，但 dual-slot classifier 把 lit 当下「不 clobber」→ `a[i]+a[j]` 右值被冲（sum 24） |
| 修法 | G.7：`glue_emit_slice_from_array_let_init_elf_c` 扩 ARRAY_LIT（temp + `{data,length}` + bump）；`glue_binop_operand_index_addr_clobbers_rbx_elf_c` 对 slice base 恒 true |
| 探针 | Ubuntu fs：len=3 · idx sum=42 · fixed=6 · var-slice=3 · var-idx=42 · u64[]=4 · empty=0；`-backend c` 同；mac host/fs 同 |
| soft residual | untyped-let 无注解（docs 禁推断）；empty slice 初值仍靠栈清零（未主动写 length=0） |
| mac 矩阵 | ✅ rv=42 · option/hello（`/tmp/mac_w329b_matrix.log` · g05 `/tmp/mac_g05_wave329b.log`） |
| Ubuntu 矩阵 | ✅ rv=42 · option/hello（`/tmp/ubuntu_w329b_matrix.log` · g05 `/tmp/ubuntu_g05_wave329b.log`） |
| 产品钉盘 | **仍 `75c638465`**（日常 L2 不升钉） |

### 文档（2026-07-24 · 已合入 tip 链）

| 项 | 状态 |
|----|------|
| `docs/02-类型定义.md` | ✅ 补全：`i8`/`i16`/`u16`、宽度表、文本 L0–L3 现状、**§规划中的类型（自举后）** |
| `analysis/基础类型缺口与自举时机分析.md` | ✅ 决策：语言级 string **自举后** |

```text
下一：wave330 Cap residual
  leave-off：
   · AST name/var_name 升布局（仍 cap 63；L011/L012 诚实失败）
   · `*u8 + *u8` 运行时字符串拼接运算符（语言糖 · 自举后；现用 concat API）
   · 语言级 string/str（docs/02 已登记 · 自举后 · 禁现在实现）
   · host residual：getenv/system/realpath _impl + resolve pool；freestanding stubs
   · legacy if_then/return_val；riscv64 64-bit shift
   · 窄化 f64→f32 仍须 `as`（有意）
   · soft residual：untyped-let 无注解（语言设计禁推断；可改硬诊断）；
     empty slice `[]` 主动写 {data,length=0}（现靠 prologue 清零）
  钉盘 75c638465；日常 L2，整波/日终再 L4
```
