# Shux 当前进度（2026-07-21）

> **长期仪表盘**：[自举进度.md](自举进度.md)  
> **债地图**：[当前问题分析.md](当前问题分析.md)（非每微步必改）  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 钉盘 | **`86281cc6`**（双端 L4 真冷 + 129；未升） |
| tip | **R2 driver_abi Cap residual pure wave19**（dep_ctx i32 字段 get/set；日常 L2） |
| 产品 bstrict | **129** · tip L4 钉盘仍 `@86281cc6` |
| 本波性质 | **工程轨 R2 pure 微步**（日常 L2；非 tip L4） |
| Ubuntu | ✅ L2 PREFER hybrid + g05 + 矩阵 rv42/option/si/hello + pure-asm rv42 @ `176adc00` |
| mac | ✅ L2 同 |
| Windows | **本波不需要** |

## 本波摘要

1. **R2 `runtime_driver_abi` Cap residual pure wave19（本波）**  
   - 真迁：`driver_pipeline_dep_ctx_set_use_asm` / `set_target_arch` / `set_target_cpu_features` /  
     `set_use_macho_o` / `set_use_coff_o` / `set_entry_already_parsed` /  
     `set_asm_entry_module_only` / `get_asm_entry_module_only` / `get_use_macho_o` /  
     `get_use_coff_o` / `set_skip_codegen_dep_0`  
   - 手段：LP64 固定 `offsetof(ast_PipelineDepCtx, …)` + wave14 `driver_abi_{load,store}_i32_le`  
     （布局权威 = `runtime_pipeline_abi.h` / `ast.x` PipelineDepCtx）  
   - 仍 seed：`driver_pipeline_dep_ctx_calloc`（sizeof）；`driver_asm_pctx_apply_host_defaults`（host `#ifdef`）  
   - seed：cold C 字段 twin；`#ifndef SHUX_L2_RDABI_THIN_FROM_X` 剔 pure-dup  
   - 代码 SHA：**`176adc00`**（docs 本 commit 跟进）  
   - 矩阵：rv_build=0 / rv_run=42 / opt 0 / si 0 / hello 0 / pure-asm rv42  
   - 钉盘仍 **`86281cc6`**（skill §3.3：微步不自动 tip L4）

2. **文档纪律**  
   - 只改 `自举进度.md` + 本文；**不**改 `当前问题分析.md` / `问题分析文档.md`

## 下一波前排

1. **可选** driver_abi 余 Cap residual（**preamble line 桥** / **fmt_argv** 待 pointer-array pure）  
2. **可选** g05 sed / harness `-E` 评估  
3. **可选** Windows hybrid；Darwin arm64 纯 asm Vec4f CG002  

```text
钉盘：86281cc6（tip L4 双端 129）
tip：R2 driver_abi wave19 dep_ctx field pure（L2 @ 176adc00）
Windows：本波不需要切换
下一：preamble / fmt_argv / g05 sed / Windows / Darwin CG002
```
