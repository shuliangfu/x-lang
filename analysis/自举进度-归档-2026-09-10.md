# Xlang / X language 自举进度

> **本文 = 2026-09-10 日切归档**（**73** 行 §6）。现行 KPI／前排见 [`自举进度.md`](自举进度.md)。

> **更新**：2026-09-10 · **日切归档** · 钉盘 **`5cac88d00`**（当日两度升钉：晨 `fa1061224`→晚二度 `5cac88d00`）· §6 收 10 日 **73** 行 · 9 月 11 日起主文档重写 · **金标 SSH＝`ubuntu-server`** · Ubuntu 验＝**git pull（禁 scp）**\
> **产品 L4 放行钉盘**：**`5cac88d00`**（2026-09-10 晚二度升钉 · 双端 L4 真冷全绿＋bstrict 129/129；当日面＝v3 `-lib-name`／g05 保鲜基建／driver_gen 产品再生重钉／CI 接线）· 前 **`fa1061224`**（晨 · §3.3 compress 车辆前缀注入）· 再前 `e194cd294`／`d583f61f5`／`b5be5ed97` \
> **日切时本波状态**（一行）：本日主战场＝**7.2.1 B-minus 量产**（生成器 v1→v5.12 连波；日切时 tip 总 **361/1,956**／v5.12 exact-hyper；过 0 点后 v5.13 计入 11 号）。另收：parse 悬空值守卫／§3.3 compress 前缀→两度升钉／7.4.4 pin↔gen 漂移闸＋g05 保鲜／driver_gen 产品再生重钉／8.3.6 六片／7.4.4 全族／Route C 试点＋B-minus 航道立起。
> **下波候选**（日切时）：①7.2.1 B-minus 续（exact-ultra_hyper／可控 max／import·struct·try_skip 根墙）②MSVC va／seed WIN leftover（须切 Windows）③7.2.1 parser `.inc` 真逻辑手工迁。日常闸门＝**SHARED 双端 L2 产品矩阵**。

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1\
> **验证纪律**：**自举期暂停 check 闸门**（2026-08-05）；日常闸门＝SHARED 双端 L2 产品矩阵

***

## 6. 变更记录（倒序 · **9 月 10 日**）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus v5.12：可控分批 mega exact-hyper（＋13 总 361）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。软刀＝exact hyper_mega 短组合子（expr/primary/match/fn/control_flow/body_skip＋library buf；无新手迁根）；拒 ultra_hyper／max／apex／88+ versal 与 blocked import/struct/try_skip hyper；拒 simd from_at。eq 墙钟＝`EQ_MAX_FILE_OFF`（本波 128）。generator v5.12；suite 门控＋call-site 同步；harness twins/table 刷新。验＝Darwin：361 T＋等价 **273394 checks OK**（off=128）＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 同 tip 同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus v5.11：可控分批 mega ultra+super（＋22 总 348）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。软刀＝ultra_mega／super_mega 短组合子（无新手迁根；callee 已迁）；拒 hyper／ultra_hyper／max／apex／88+ versal（eq 墙钟）与 simd from_at。generator v5.11；suite 门控＋call-site 同步；harness twins/table 刷新。验＝Darwin：348 T＋等价 **1424867 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 同 tip 同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus v5.10：diag_lex_after_imports 无 lex 根（＋5 总 326）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。拒因墙＝suite 外 source-only／`(data,len)` 无 lex 签名。修＝手迁根＋buf（ABI 加宽 `(lex,source)`／`(lex,data,len)`；snap+reset-to-init+restore）；软刀固定点 3（preamble_deep／full／entry_full）；harness 纳 `lexer_init` 副本；拒 simd from_at／mega 墙钟。验＝Darwin：326 T＋等价 **943776 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 随 push 后同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus v5.9：import_select_list inout 根（＋9 总 321）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。拒因墙＝suite 外 `*inout_lex` 选择列表。修＝手迁 flag3 inout 根；软刀固定点 8（select_deep／stmt_full／collect_imports／skip_imports_deep）；拒 mega 墙钟。验＝Darwin：321 T＋等价 **929856 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 随 push 后同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus v5.8：loop_stmt_body 根（＋30 总 312）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。拒因墙＝`loop_stmt_body` 三参 flag 在 suite 外／harness 恒 0 stub 使 score+= 假绿。修＝手迁 flag3 根（restore trio＋parens inplace＋brace／assign）；撤 stub；软刀固定点 29 短名。**诚实入库 30**（1 根＋29；拒 88 versal mega 全量——eq 墙钟）。验＝Darwin：312 T＋等价 **892944 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 随 push 后同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus v5.7：if_stmt_body／layout（＋34 总 282）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。拒因墙＝void `struct_layout_name`／`if_stmt_body` 混用原游标／`lex_at_if` 非主名／if 块 `score+=(&r.next_lex)`／`return &r.next_lex` 先 restore 后 call／AUDIT_CALL(init)。修＝G.7 补全生成器：fold layout_name／mixed if_advance fallthrough＋else 回 entry／rename lex_at_if／score next_lex 快照／return 先 call 后 restore／AUDIT_CALL 临时 lex；harness 纳 if_advance＋layout_name。**诚实入库 34**（struct_record_layout／if_stmt_body／if_expr／skip_one_if 及 layout／if 深链＋buf）。验＝Darwin：282 T＋等价 **1156471 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 随 push 后同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus v5.6：after_imports／deep-scan（＋27 总 248）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。拒因墙＝buftail 剥 `&r` 而 handler 仍要 `&`／单行 `if(kind) next_into`／`skip_one_struct`＋`skip_imports` 无 inplace／rebase 后 inout 回 pos0 撤 skip／`match_subject_ident(r)`＋`spawn_kw(kind)`。修＝G.7 补全生成器＋桥 inplace；harness 轻量 skip stub（产品链仍走 suite 权威）。**诚实入库 27**（after_imports／collect／parse_into＋library_scan／match 深链；if_stmt_body／if_advance 仍拒）。验＝Darwin：248 T＋等价 **1022273 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 随 push 后同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus v5.5：function_advance 展开（＋9 总 221）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。拒因墙＝function_advance 的 `lex_cur`＋skip_parens＋peek-only `ret_audit` 块，顶层 translate 无 `skip_parens_into_slice`。修＝G.7 补全既有生成器：丢 `lex_cur` 声明／elide ret_audit／顶层 skip→step+inplace；harness 纳 `function_advance`。**诚实入库 9**（function_body＋buf／onefunc／parse_one 链／balanced_delim mega；if_advance／混用原游标仍拒）。验＝Darwin：221 T＋等价 **915704 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 随 push 后同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus v5.4：peek_ident_ptr 源字节根修（＋12 总 212）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。拒因墙＝`return bind_name_validate` 经 `peek_ident_ptr` 得 null（`tok.ident` 常空）而 C 用 `source->data+token_start`→c=1/x=0。修＝G.7 补全桥 `peek_ident_ptr`（优先 `tok.ident`，否则源相对字节）；harness 纳 `impl_advance`。**诚实入库 12**（impl_type_for_trait＋impl_items_body＋trait_impl_* deep／mega／buf）。验＝Darwin：212 T＋等价 **880181 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 随 push 后同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v5.3：advance_to 薄展开（`a1068d914`，＋7 总 200）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。拒因墙＝`*_advance_to_*_lex` 二级 cursor 出参（opaque *u8 无第二游标）。修＝G.7 补全既有生成器：薄 `advance_to`＋`return probe(&body_lex)` 展开到主游标 inplace（`*out=r.next_lex`→`from_result`；成功直返 probe）；harness 同步纳入 struct/enum/trait/match advance 助手。**诚实入库 7**（struct/enum/trait/match body＋buf；impl/function/if 与 trait_impl_type 族红未入）。验＝Darwin：200 T＋等价 **832817 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 随 push 后同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v5.2：厚 buf score 墙（`c36a52112`，＋15 总 193）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。拒因墙四闸＝buftail 只改 `&sl)` 漏中参 `&sl,`／buf score 拒识 `&lex, data, len`／无尾旗／slice `&out_local` 未映 null。修＝G.7 补全既有生成器。**诚实入库 15**（厚 buf／extern／enum／type_ref／async／trait_impl_header；trait_impl_type 族 3 例 harness 红未入）。验＝Darwin：193 T＋等价 **805188 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 随 push 后同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v5.1：score+=／负字节链／拒 advance_to（`c9748e07c`，＋34 总 178）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。拒因墙三闸＝inout 路径 `score+=` 被写成 `score=`（后写覆盖→深链 c=1/x=0）／负字节链 `!=`/`\|\|` hoist 成 `bhit==1`（linear_type 极性反）／`advance_to_*_lex` 二级 cursor 漏译成未声明 `&body_lex`（XT001）。修＝G.7 补全既有生成器三闸。**诚实入库 34**（deep／mega／buf 委托；trait_impl 族 3 例 harness 红未入）。验＝Darwin：178 T＋等价 **745983 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 随 push 后同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v5.0：elide void `&r.next_lex` 双步进根修（`a13114c2f`，＋3 总 144）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。拒因墙＝`(void)CALLEE(&r.next_lex)` 被误译为 step+call，与随后 skip／`lexer_next_into(&r,r.next_lex)` 叠成双步进。修＝G.7 补全既有生成器：丢弃式 `&r.next_lex` 调用 elide（C 拷贝＋restore 净零）。**诚实入库 3**（trait_methods_probe／array_type_bracket／slice_type_bracket）；out 参 **9／9 收官**。验＝Darwin：144 T＋等价 **611785 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 随 push 后同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v4.9：块嵌套 while／else-if／guard-return／look-ahead／braces／probe-copy（`02aab775e`，＋4 总 141）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。拒因墙＝块内嵌套 kind-while／single-if 后 else-if／for(;;) return 被 break 阴影／rnx look-ahead／braces skip／`fn_lex=lex` 探针拷。修＝G.7 补全既有生成器＋braces inplace 桥（parens／brackets 同形）；`&r,r.next_lex` 步进与 look-ahead 分家（防 kind→kind2 污染）。**诚实入库 4**（impl_items／block_stmt／extern_param／struct_lit）；trait_methods 因 `&r.next_lex` void-audit＋skip 双步进墙诚实未入。out 累计 8／9。验＝Darwin：141 T＋等价 **595997 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 随 push 后同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v4.8：bind_audit 折叠根（`65b8baa41`，＋2 总 137）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。拒因墙＝void `*_bind_audit_c`／`function_name_audit_c`／纯 kind 审计进 if-block。修＝G.7 折到既有 `peek_ident_ptr＋bind_name_validate`；kind void 可消；field_name／continues_kind 条件 bool＋PURE_HELPERS；void `&lex` 调 `strip_inout`；harness HELPERS 补齐同名权威。**诚实入库 2**（enum_variants_probe／struct_fields_probe）。余 out 5。验＝Darwin：137 T＋等价 **564421 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 随 push 后同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v4.7：标量 out 参→`*i32`＋`out[0]=`（`736960fe0`，＋2 总 135）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。拒因墙＝`int32_t *out_*` 签名不进 parse_suite（9 根）。修＝G.7 补全既有生成器：out 签名解析；空指针安全 `out[0]=`；depth while／else-if depth±／int 三元返回；harness null＋packed-slot 双极性。**诚实入库 2**（match_arms_probe／call_args_shape_probe）。验＝Darwin：135 T＋等价 **548633 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 随 push 后同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v4.6：栈 kinds[]→标量 peek 链（`896f59fae`，＋5 总 133）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。拒因墙＝`int32_t kinds[N]`／`peek_kind_chain_c(..., kinds, N)` out-array（6 hard；级联解锁 diag/collect buf）。修＝G.7 补全既有生成器：栈数组→`kinds_i` 标量；中途 chain_pos restore 复刻 C 按值；classify_toplevel PURE_HELPERS；harness 抽 suite 权威拷贝。**诚实入库 5**；17 deep/bracket harness 红未入。验＝Darwin：133 T 零重复＋等价 **532845 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 随 push 后同序。PLATFORM: SHARED。 |
| 2026-09-10 | **docs(selfhost)** **C迁移追踪极简瘦身** | **文档** · **不升钉**。全文归档 [`archive/C迁移追踪-流水账归档-20260910.md`](archive/C迁移追踪-流水账归档-20260910.md)（952 行）；主文档重写为状态勾选 only（✅ 一句话／🟡⬜ 短待办；删验收流水／SHA 日记）。约 **952→277 行**。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v4.5：call-then-restore＋if-return 字节链（`9f60568d3`，＋8 总 128）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。根因＝`if (COND) return` 不走 hoist_byte_chain／`return CALLEE(&cursor)` 被 v4.4 纯委托误吞守卫。修＝G.7 补全既有生成器：if-return 接字节链提升；多语句 return CALLEE 改为 call-then-restore；条件 `&lex` 去 C 取址；buf shim `&?lex`＋prologue 不计入委托句数；harness 孪生前向声明；不动点批跑。**诚实入库 8**（library_fn_shape／type_ref_deep／diag_fn_param_sig／library_return_type 及对应 buf／library_scan_buf）；19 个 mega/full_deep／bracket harness 语义红未入。验＝Darwin：128 T 零重复＋等价 **513110 checks OK**＋矩阵 5/5＋闸 15/5/0＋compress OK。Ubuntu 随 push 后同序。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v4.4：有守卫委托＋inout 恢复（`db4556085`，＋1 总 120）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。**纯委托守卫**（诚实修正：Delegation 只在单 return 体触发——diag_fn_param_sig 前置 LPAREN peek 被误判纯委托丢守卫，harness 1877 红抓出；有逻辑委托显式拒）；**inout callee 恢复**（组合器调 inout 后 emit 恢复 trio——C 按值语义要求每次从原位起，harness 1408 红根因）；buf 委托 return 形态；块内 brackets `&after` 放宽；**字节链双极性**（负式 `bounds-fail||bytes!=` 与正式 `data&&bounds&&bytes==`）；emit_buf_x 双模板。总 **120/1,956**。验＝双端：120 T＋双态 0 错＋零重复＋矩阵 5/5＋闸 15/5/0＋compress OK＋等价 harness **481534 checks OK**。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v4.3：buf→buf 委托链（`001347eeb`，＋5 总 119）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。buf→buf 调用映射（`CALLEE(lex,data,len)` 直传——score=/+=/(void)/return± 四形态；translate_call buf 分支）；三行 buf 签名解析（首行含 data 尾两形态）——**tier52 长名族入库**（ultimate/supreme/crown/pinnacle/zenith/peak/summit/apex/max/ultra/hyper/mega 前缀 14 层委托链，链底=已迁 trait_header）；score handler group(5) 越界守卫。总 **119/1,956**。验＝双端：119 T＋双态 0 错＋零重复＋矩阵 5/5＋闸 15/5/0＋compress OK＋等价 harness **477587 checks OK**。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v4.2：依赖墙批量解锁（`63c7dacfd`，＋23 总 114）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。extern_fn「backtrack」误判修正（from_result 写游标参数＝合法推进，C 副本语义——状态机补 cursor-advance peek 路径）；条件内子审计调用映射；通用 kind while；块内 helper 赋值别名（adv0 快照）；**自增脱糖**（`guard++>N` → 先自增后比旧值——dispatcher/block 三处 if 处理器全接；块版 if-return 首漏接为本轮主战场）。**不动点 8 轮（+2→+5→+4→+4→+4→+1→+3→+0）＝依赖墙批量解锁实证**：extern_fn/modifiers 根拔后 expr 组合器层连锁入库。总 **114/1,956**。验＝双端：114 T＋双态 0 错＋零重复＋矩阵 5/5＋闸 15/5/0＋compress OK＋等价 harness **457852 checks OK**（116 用例）。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v4.1：字节比对链＋guard-break（`72aeb2863`，＋2 总 91）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。字节链 hoist（「allow」守卫链→预计算 temp：data2/ts2/sln2/bhit 提升＋守卫 if 内 unsafe 包裹比较——.x 条件内指针下标须 unsafe；块路径经 BLOCK_HOIST_USED 回传）；guard-break 头变体；**生成器卫生大修**（删 6 个重复函数定义——历波补丁落死副本的根因；修 BYTE_CHAIN_RE `\1` 反向引用被解析为 `\x01`）；块内 cur_results 播种。struct_modifiers＋skip_one_struct_buf 解锁（字节链最深复合体）。总 **91/1,956**。验＝双端：91 T＋双态 0 错＋P9a 全片 hybrid＋矩阵 5/5＋闸 15/5/0＋compress OK＋等价 harness **367071 checks OK**。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v4：递归块翻译器（`3e5b2ba2f`，＋1 总 89）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。translate_block 升全递归：同层 if/else-if 链收集（collect_braced 深度判别＋融合 `} else {` 行不吸收）＋臂体递归；单行 if-break/continue；桥 +parens in-place 适配器（18 原语）＋块内 helper `&lex` 直写双族。sync 三修（helper 行首锚定——曾命中调用文本吞声明区／parens 源回退／审计门宏剥离）。**发现**：.x 重复 extern 声明致 -E 发射 mangled 面（去重归零）。primary_suffix_chain_probe（4 臂 else-if 链）首过＝递归翻译器实证。总 **89/1,956**。验＝双端：89 T＋双态 0 错＋矩阵 5/5＋闸 15/5/0＋compress OK＋等价 harness **359177 checks OK**。PLATFORM: SHARED。 |
| 2026-09-10 | **fix(selfhost)** **7.2.1 B-minus 回退卡重开：stale-idlen 根因修复（`295020176`，＋2 总 88）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。impl 族 pos0 分歧定位：**块内 peek 只刷 kind 不刷 idlen**——FOR 块 ident 检查读到 FOR 关键字旧 idlen(0) 假败；修＝块内两种步进形态均补 idlen 刷新。impl_header/skip_one_impl_buf 回归（上波诚实回退→本波根因修复重开）。附带硬化：`LOCAL=rX.next_lex` 直赋别名／continue 透传／复合 bail（`guard-1>B` 精确复刻短路序）。构造尾函数均为复合体（各需 2-3 新构造）本波＋0 全量——**v4 卡＝块翻译器升全递归语句树＋字节比对链＋guard-break 循环**。验＝双端：88 T＋双态 0 错＋矩阵 5/5＋闸 15/5/0＋compress OK＋等价 harness **355230 checks OK**。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v3.1：多 lexer 别名链＋厚 buf 体（`e6412718e`，＋6 总 86）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。别名追踪（lexer 局部 from_result＝步进／从别名读＝peek／helper 赋值＝step+adv0 快照＋in-place；`LOCAL=lex` 播种；位置比较双形态映射）；厚 buf 体翻译（wrap 前导＋标准语句＋专用模板；prologue 宽容剥离含 if/return 配对）；单行 if-return／`while(guard++<N)`／取反 helper；**ternary 返回 restore 进分支**（修复比较前归零 pos 的序错——type_ref_peek 808 红根因）。**诚实处置**：impl_header/skip_one_impl_buf 迁移回退（pos0 态 rc 分歧未定位）——安全名单重走全绿后收口。验＝双端：86 T＋双态 TU 0 错＋矩阵 5/5＋闸 15/5/0＋compress OK＋等价 harness **347336 checks OK**。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v3：buf 族解锁——wrap 环形服务（`6d8e5d1fd`，＋22 总 80）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。桥 +`parser_asm_lex_wrap_buf_c`：raw (data,len)→opaque 切片，**深度 16 环形暂存**（单线程管线、审计委托链 ≤16 层、嵌套 wrap 各得其所）——一个原语接通 buf 族与 .x。生成器 v3：buf 签名三形态解析＋纯 shim→三行 wrap+delegate 体；thick buf 诚实拒（v3.1 卡）；buf 孪生/调用点/跨行声明同步。harness buf 用例（shim 双侧提 data/len＋空守卫——null-guard 传 NULL 被 segfault 抓出即修）。**形态普查入档**：1,035 签名拒因＝1,023 buf（504 薄 shim＋279 厚体）＋9 out 参＋3 其他。本波 +22，总 **80/1,956**。验＝双端：80 T＋双态/imports/try_skip TU 0 错＋P9a 全片 hybrid 零重复＋矩阵 5/5＋闸 15/5/0＋compress OK＋等价 harness **327601 checks OK**（82 用例含 buf）。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v2.1：循环族＋kinds 内联（`c1b437560`，＋13 总 58）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。循环模板三件：kind 成员 while（统一计数模式）／for(;;) guard 循环／块内 step＋嵌套 if VAR++；**kinds 数组委托→内联展开**（static const kinds[N]→OR 条件统一循环——首版漏循环后 return n 被 harness 141 红诚实抓出即修）；逻辑行合并（跨行条件）＋单行 bail 形态＋CALL(r.next_lex) 步进调用三形态＋任意游标参数名；expr_binop **fn-ptr 回调面指针化**（audit_fn 参数＋穿透调用取址）。本波 +13（mul/addsub/unary/kinds 全家＋chain 组合器层），总 **58/1,956**。验＝双端：58 T＋双态 TU 0 错＋矩阵 5/5＋闸 15/5/0＋compress OK＋等价 harness **244714 checks OK**（62 用例）。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v2：组合器模板＋链式解锁＋不动点批跑（`b07f1656d`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。桥 +peek_ident_ptr（16 函数）。生成器 v2：switch→if/else 链（fallthrough 分组）／score 算术（score=CALL/±N）／子审计调用（含 flag 3 参）／bind_validate 经 ident_ptr／helper 丢弃调用／条件内 helper→桥名映射＋裸调用 `!=0` 包裹／**迭代批跑到不动点**（+9→+5→+0 链式解锁——依赖层自动逐层入库）；生成函数自动确保前置声明＋孪生 source 成员 cast。**.x 语言面三卡**：嵌套调用实参不支持（生成器拍平）／-E 对双闭括号畸形静默丢函数／if 禁隐式 int-bool。**词法哨兵契约发现**：pin 越界即 panic→产品切片 length 含 NUL；harness 同步 +1（栈爆追至 `'1 as u8 as i32'` 尾数字边界——EQ_TRACE 逐例打印＋单点二分定位）。harness 全自动化（sync 脚本全量再生 45 孪生/47 用例）。本波 +14（组合器层首破），总 45/1,956。验＝双端：45 T＋P9a 全片 hybrid＋零重复＋矩阵 5/5＋闸 15/5/0＋compress OK＋等价 harness **185509 checks OK**。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v1.1：13 函数批量入库——委托组合器解锁（`ede04ec5c`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。v1 四枚硬化：①纯标量 helper ternary＋extern 自动发射（is_compound_assign）；②**委托组合器**（return CALLEE(&lex) 且被调者已迁 → 薄 .x 委托体——自底向上解锁机制落地）；③多行签名 parse/孪生 splice；④通用调用点同步（任意非 & 非 struct 实参含 r.next_lex）＋任意参数名声明正则＋ locator 遇声明继续扫。本波 13 函数＝11 线性叶子＋2 委托（let_in_block/if_expr_branch）；1 诚实拒（array_lit_head 双条件 ternary，入手工积压）。验＝双端：双后端 31 T＋P9a 全片 hybrid＋零重复＋矩阵 5/5＋闸 15/5/0＋compress OK＋等价 harness **242668 checks OK**（31 用例；twins.h 13 孪生＋is_compound 权威拷贝＋语料扩 13 型）。**B-minus 已迁 29/1,956**；生成器覆盖面＝线性叶子＋纯委托，下一步 v2＝switch-score/if-chain 组合器模板（~1,700 个的主体）。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 生成器 v1：线性叶子自动化首航（`597856ed0`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。`scripts/gen_stretch_audit_x.py`：C 线性叶子→.x 全链生成器——单游标 in-place 翻译模型（peek 读/step 进/快照恢复复现按值）；**严格解析越界即拒**（循环/数组参/buf/out 参/helper/回溯全拒并给因，永不硬翻）；四步产出（.x 追加/孪生门控逐行锚定/声明调用点全树同步/**常量完备性预检**——wave-3 TOKEN_FUNCTION 教训产品化）；幂等安全。试点 2 函数（struct_header/panic_kw）全链绿：双端双后端 16 T＋P9a 全片 hybrid＋1T+1U 零重复＋矩阵 5/5＋闸 15/5/0＋compress OK＋等价 harness **139554 checks OK**（18 用例）。**61 候选批跑诚实暴露 v1.1 硬化清单（立卡）**：漏网调用点变量名/多行 extern 声明/assign_op typeck 残留——硬化后约 10 可译叶子一波入库。**形态普查结论**：suite 1,965 审计＝33 叶子＋~1,800 层叠组合器（1,065 score 组合＋629 调用组合）——**必须自底向上**（.x 不能调用未迁按值 C）。**B-minus 已迁 16/1,956**。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 第三刀：函数签名审计族 4 函数迁 `.x`——组合模式立起（`a464af7bb`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。4 helper un-static（suite 内多 C 调用者保留 C 权威）＋桥 4 in-place 适配器（桥面 15 函数）。`.x` 增 fn_param_list/skip_return_type（inout ABI）＋fn_sig/function_header（按值快照恢复）——**组合审计经 opaque 指针贯通零本地 struct**；**inout 失败不写回语义**＝入口快照＋失败恢复＋成功保留推进（harness 首跑 7014 红诚实抓出——等价闸的价值实证）。harness 16 用例表（inout 终态比对分支）＋函数签名语料＋twins.h 嵌 4 helper 权威拷贝。验＝双端：双后端绿（14 T）＋P9a 入链全片 hybrid＋四函数 1T+1U 零重复＋矩阵 5/5＋drift-gate 15/5/0＋run-compress OK＋等价 harness **123760 checks OK**。途中记：typeck 对未声明标识符只报 check_block failed（TOKEN_FUNCTION 漏声明二度踩中——**生成器必须做常量完备性预检**）。**B-minus 已迁 14/1,956；三种契约（按值/inout/组合）模板齐**。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 第二刀：tier26/27 审计家族 9 函数迁 `.x`（`00fe55e7c`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。B-minus 量产首波：桥补 `peek_ident_len`/`peek_token_start`（纯窥视不推进，同 token 多查询天然一致无隐藏状态，桥面 11 函数）；`pthin_stretch_audit.x` 增 loop_header/break_continue（flag 参）/else_stmt/else_if_chain/let_const_decl/enum_header/match_kw（guard 512）/return_stmt（256）/import_stmt（64＋'as' 字节比对保留 fidelity 死支）；guard 循环精确复刻 C `guard++>N` 迭代语义。9 个 C 孪生机械指针化＋门控（逐字保留原逻辑）；~73 调用点跨 8 切片文件＋5 seed 前向声明同步；双态 TU 语法 0 错。harness 表驱动化（12 用例×34 合成串全 token 偏移＋3 真实文件×2500 偏移＋逐函数 null 守卫；参考孪生自门控体机械生成 twins.h）。验＝双端：双后端编译绿（pure-asm 10 T）＋P9a 入链全片 hybrid＋10 函数单 T 零重复＋矩阵 5/5＋drift-gate 15/5/0＋run-compress OK＋等价 harness **92040 checks OK**。途中记：变换器首版跨行正则吞声明区（即回退重做，逐行锚定版零损）；`.x` extern 漏声明两枚（peek_kind/source_data）被 typeck 诚实拦下。**B-minus 已迁 10/1,956**。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 产品化：if_header_audit `.x` 权威入产品链（`b9326977c`）** | **代码＋文档** · **不升钉** · **构建级别 L2**。试点刀完整落产品：suite C 孪生改 pointer ABI＋`XLANG_PTHIN_STRETCH_AUDIT_FROM_X` 门控（hybrid 编出＝.x 同名同符号，冷/默认 lane C 孪生唯一定义）；11 调用点（suite 9＋skip_if/body_tl）＋4 声明同步取址。g05 新 **P9a 航道**＝`g05_try_x_to_o`（pure-asm 优先，-E 回落）＋bridge TU＋define 双落点（P9 seed cc 与 rest defs）＋mtime 触发，任一步败回落 seed C 孪生（单权威不变量）。**根修两枚**：①`g05_try_x_to_o` -E 回落 prologue 缺 `<xlang_weak.h>`（G05_X_O_WEAK perl 标记首见 lane 即撞——PEL 恒走 pure-asm 从未暴露）；②pure-asm typeck 要求 extern 调用包 unsafe（-E 宽松＝前端分歧，`pthin_stretch_audit.x` 语义不变重排）。验＝双端 P9a 入链（Darwin pure-asm T／Ubuntu -E W 双路径皆单定义无重定义，bridge T 齐，`lexer_next_into` U 由 lexer_x.o 终链解析）＋双端矩阵 5/5＋drift-gate 15/5/0＋run-compress OK＋等价 harness 双端 8411 checks OK。**B-minus 航道正式立起**：1,956 audit 探针族后续按本刀模板逐波迁。PLATFORM: SHARED。 |
| 2026-09-10 | **fix(selfhost)** **g05 hybrid 航道饥饿根修（P10/P12/P20 三片，`25179e45c`）** | **代码＋文档** · **不升钉** · **构建级别 L2（基建修复）**。静默降级考古：三个 hybrid seed TU cc 红导致 P1–P20 切片合并长期假死、ensure 恒回落 full seed（产品绿掩盖）。产生点＝Cap 9.5.3（`xlang_io_write`）与 wave473（onefunc arena 路径）改 slice 时只同步了主 TU 声明面，漏 hybrid TU：P10 补 `<xlang_io_cap.h>`；P12 补 ast_Block/ast_Expr/onefunc_result 镜像＋TypeKind 枚举＋`ast_ast_arena_*` 外置＋fill/skip_generic_angle 声明（出处行号注）＋`<xlang_io_cap.h>`；P20 补 `<xlang_io_cap.h>`。验＝三 TU -fsyntax-only 0 错＋g05 全片 hybrid 恢复「omit empty rest」＋双端矩阵 5/5。镜像纪律：布局变同 commit 同步。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 B-minus 试点：lexer-step 桥＋if_header_audit 全量等价（`8a9825dd6`）** | **代码＋测试＋文档** · RFC §5c 首刀。`seeds/parser_asm_lex_step_bridge.from_x.c`（9 函数：step/peek＋pos·line·col 读写三对＋source 读；**restore trio**＝按值语义承重证据——`(void)audit(lex,·)` 后主循环续 lex，仅 set_pos 不够 line/col 漂移）；`src/asm/pthin_stretch_audit.x` 首页（`#[no_mangle]` 同名 pointer ABI，快照/恢复复现按值契约；独立文件避免迫使 pthin_stretch.x 13 函数连带门控）；等价 harness（by-value C 参考孪生 vs .x，返回值＋调用方 lexer 不动双不变量；20 合成串×每 token 偏移＋3 真实 .x×4000 偏移＋null 守卫）。**枚举权威核验**：token.h≡lexer_gen 133 项零分歧（TOKEN_IF=4/TOKEN_LPAREN=82）。验＝双端 **8405 checks OK**。PLATFORM: SHARED。 |
| 2026-09-10 | **feat/docs(selfhost)** **Route C 试点：pthin_stretch.x 首个真体（`b75b8e692`＋`1cb77e5e5`）** | **代码＋文档** · RFC C 路线首刀。`parser_asm_emit_heavy_stretch_slice.inc`（490 行）＝ 35 切片中唯一 0/0/0（无按值传参/无字段访问/无 C 内建）纯字节整数域——`src/asm/pthin_stretch.x` 从 6 行 stub 长出 528 行真体（11 公开函数＋2 辅助＋4 表含 35×10 kw 拼写表），全英文 docblock。验＝产品 `-E` 编译干净（13 T 导出）＋`.x` 面与 lite 切片函数清单 comm 空（全覆盖）＋Ubuntu 同步实跑 11 函数发射＋闸 15/5/0＋矩阵 5/5。**RFC 决策记录**：C 试点 Agent 先行执行 ✅；A/B（struct-by-value 密集域）**待用户拍板**（suite 切片 2,755 处按值传参=重度 blocked）。 |
| 2026-09-10 | **docs(selfhost)** **7.2.1 主 debt ABI 路线 RFC 立案（`886b22457`）** | **文档** · [7.2.1-parser-inc-port-ABI-RFC.md](7.2.1-parser-inc-port-ABI-RFC.md)：问题＝`.inc` 按值传参 `.x` 无面；三路线量化对比（A opaque＝零语言改动但访问器膨胀／C 行数未必减；B struct＝零 ABI 变更但语言面大改＋全编译器爆炸半径；C 试点＝先迁一个无按值切片域验证刀法）；**建议 C→B 序**；决策记录留空待用户拍板。RFC 即闸门——未决前禁开首域。 |
| 2026-09-10 | **test/docs(selfhost)** **日终 L4 兜底双端全绿 → 三度升钉 `5cac88d0`→`30f509d8e`** | **测试＋文档** · **升钉**。双端全擦冷启＠`30f509d8e`：Ubuntu 15m3s（bstrict **129/129**；`ubuntu-server:/tmp/xlang_l4_Linux_final.log`）＋Darwin 22m18s（bstrict **129/129** 17m0s；`/tmp/xlang_l4_Darwin_final.log`）。当日全收口：7.4.4 全族＋8.3.6 六片（−157k 行）＋7.2.1 十一刀＋素材清零＋主 debt 建图到 ABI 层。 |
| 2026-09-10 | **docs(selfhost)** **7.2.1 主 debt 两轮深查＋ABI 障碍立卡（`8df7301d8`＋`3752e3ad9`）** | **文档** · 零代码。①二次深查修正：pthin 24 个 `.x` 全是 **6–22 行 logic-source stub**（合计 237 行、0 个 function 体）——「`.x` 权威」实为壳层标记，全部函数体仍在 `.inc` C；hybrid 机制只是把 C 体挪独立 TU ld -r，**host-cc 行数未减**。②真实剩余＝**~74k 行 `.inc` C parser 逻辑移植为 `.x` 函数体**。③ABI 障碍首查：`.inc` 函数以 `struct parser_asm_token`／`lexer_result` **按值传参**（helpers 单切片 14 处、4+ 切片文件涉及）——`.x` 无此 C 结构体面，按值须改 opaque 指针＝全链 ABI 变更（thin .x 委托与冷 C 双面同步）。**结论立卡**：主 debt 是「ABI 决策＋74k 行逐域移植」多波史诗；建议立 RFC 先决 ABI 路线（opaque 指针化 vs .x struct 支持）再开首域。双端闸 15/5/0 绿。 |
| 2026-09-10 | **docs(selfhost)** **7.2.1 主 debt 精确建图（`2d4bf3e50`）** | **文档** · 零代码。parser_asm_thin_c 实际结构＝主文件 4,555 行（3,893 手工 C＋33 切片 include＋174 P8 尾）＋`seeds/parser_asm/` 35 切片 73,854 行；**pthin 24/24 已全覆盖**（`.x` 权威＋冷孪生＋hybrid P1–P20）。**符号级水位**：glue `.o` 2,337 T 中 133 hybrid 覆盖／**2,204 仍纯主文件 C**（切片 `#include` 同 TU 故 hybrid 只挑战部分面）。**首片方案立卡**：增量切片 hybrid 化（每波 1–2 个 `.inc` 建守卫剥离）→ 主文件逐片缩壳至 0——不走「迁 21,935 行」整体路线。 |
| 2026-09-10 | **docs(selfhost)** **7.2.1 素材终局：三域件深查全改判 D，迁移面清零（`092d4b731`）** | **文档** · 零代码。深查三域件真实性质：①codegen_cap_residual（422 行）＝assemble_codegen_gen_from_x.py **机械追加伴随件**（头注自述）；②typeck_cap_residual（1472 行）＝同（append-only companion for typeck.x re-pin）；③backend_arm64_enc_c（1360 行）＝`src/asm/arch/arm64_enc.x`（1689 行）**已有权威的冷编产物**（头注自述「port of」＝类 A 缩壳面）。**7.2.1 素材清单终局**＝11 迁＋1 回滚卡＋10 D 类＋**0 残**。parser_asm_thin_c 主 debt（21,935 行）是独立战场（pthin 24 文件渐进），与本清单正交。 |
| 2026-09-10 | **docs(selfhost)** **7.2.1 素材终判（`9f2f54a51`）** | **文档** · 零代码。三维度（ifdef／Cap-inline／raw-syscall）终判剩余：①build_tool_libc_bridge（122 行）D 类——头注自述「.x extern system 与 Darwin stdlib 头冲突」即立桥原因；②asm_experimental_symbol_bridge（389 行）D 类——尾段 `#if defined(__APPLE__)` weak Mach-O 桩（独立 .o link 语义，同 TU 静态绑定会遮真 writer）；③lsp_diag_sizes_weak（126 行）D 类（3 ifdef）。**真素材 21→18**：余三件全 0/0/0 可迁但均为域文件级多波工程——codegen_cap_residual(422)／backend_arm64_enc_c(1360)／typeck_cap_residual(1472)。**7.2.1 单刀薄壳区收官**：11 迁＋1 回滚卡＋7 D 类＋3 域件待多波。 |
| 2026-09-10 | **revert(selfhost)** **7.2.1 十二刀回滚＋双 D 类改判（`a35a3072a`）** | **回滚＋文档** · **不升钉**。①runtime_process_import_alias（117 行）`.x` 化后 Ubuntu run-process 红——`xlang_proc_exit` 为 Cap 头 **static inline**（raw syscall）无导出定义者；`.x` extern 调用 UNDEF（Darwin 假绿／Ubuntu 诚实拒）→ seed 恢复＋接线撤销＋重开条件立卡。②pipeline_phase_parse_only（67 行，struct-by-value 消费）＋typeck_mangle_link（80 行，机械再生伴随件）改判 D 类——真素材 23→**21**。**预检新维度**：Cap-header static-inline 依赖纳入前判清单。验＝双端 run-process 绿＋闸 15/5/0＋矩阵 5/5。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 十一刀：pipeline_asm_typecheck_alias 迁 `.x`（`7f9f755dd`）** | **代码＋文档** · **不升钉**。61 行 strict asm typecheck 阶段编排（skip 门／library-main face 选／force_c 变体／诊断；C 体因＝第二遍 force_c/x_ast 分支不完整致 null-module smoke 败）：权威＝`src/pipeline_asm_typecheck_alias.x`；ensure `--auto`＋lane 验 `pipeline_impl_typecheck`。seed 物理删。验＝双端 T＋relink 0＋闸 15/5/0＋矩阵 5/5＋m2=42。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 十刀：pipeline_run_x_link_alias 迁 `.x`（`5691da723`）＋impl_alias 改判 D** | **代码＋文档** · **不升钉**。53 行实验链四别名（`run_x_pipeline_*` 裸名→pipeline_x.o 前缀导出）：权威＝`src/pipeline_run_x_link_alias.x`；src/asm ensure `--auto`＋lane 逐叶验。seed 物理删。**诚实前判**：pipeline_run_impl_alias（53 行）因 `#if !defined(PARSE_ALIASES)` 编译期开关＋**struct-by-value 返回**双 C 固有改判 D 类——真素材 24→23。验＝双端四 T＋relink 0＋闸 15/5/0＋矩阵 5/5＋m2=42。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 九刀：pipeline_asm_run_all_alias 迁 `.x`（`20b11ad97`）** | **代码＋文档** · **不升钉**。51 行 strict 链编排（`pipeline_impl_run_all`：parse/load→typeck 失败即返→skip→codegen 链；C 体存在因＝build_asm 第二遍 run_all post-typecheck cbz 挂死）＋`run_x_pipeline_impl` 裸名：权威＝`src/pipeline_asm_run_all_alias.x`；partial-merge ensure `--auto`＋lane 逐叶验。seed 物理删。验＝双端双 T＋relink 0＋闸 15/5/0＋矩阵 5/5＋m2=42。**批量预检**：剩余 alias 族 ifdef 密度已扫（impl_alias/x_link/mangle/process_import＝0 可迁；short_face/cfg_eval_link＝2 待判）。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 八刀：driver_compile_asm_link_alias 迁 `.x`（`8d6802cb0`）＋双 D 类改判** | **代码＋文档** · **不升钉**。49 行 strict 链 driver 四别名（runtime/C-gen `driver_*` 链接名→compile.x EMIT_HEAVY 裸名）：权威＝`src/driver_compile_asm_link_alias.x`；ensure_driver_compile_link `--auto`＋lane 逐叶验。seed 物理删。**诚实前判**：trampoline(41)/cfg_eval_host_lit(45) 因 `#ifdef`/`#if defined(__APPLE__)` 编译期开关 intrinsic 改判 D 类（强迁＝权威翻倍零收益）——真素材 26→24。验＝双端四 T＋relink 0＋闸 15/5/0＋矩阵 5/5＋m2=42。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 七刀：pipeline_wpo_strict_link_alias 迁 `.x`（`f0e2d5983`）** | **代码＋文档** · **不升钉**。40 行 strict WPO 双别名（`pipeline_run_x_pipeline_impl` 统一名＋`run_x_pipeline_typecheck_entry_emit` thin bl）：权威＝`src/pipeline_wpo_strict_link_alias.x`；**双 ensure 站**接线（第二站点日志风格差异首版锚未中即修）；与五刀同符号航道隔离注文档化（不同 strict lane 永不共链）。seed 物理删。验＝双端双 T＋relink 0＋闸 15/5/0＋矩阵 5/5＋m2=42。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 六刀：typeck_lsp_io_stub 迁 `.x`（`09b4ef3ee`）** | **代码＋文档** · **不升钉**。37 行 typeck_* IO 四桩（read_message→-1／alloc→null／free no-op／is_null）：权威＝`src/typeck_lsp_io_stub.x`（ptrdiff_t→`.x` isize＝ssize_t，LP64 ABI 同构）；lsp-diag ensure `--auto` 优先＋lane 逐叶验自身签名。seed 物理删。验＝双端四 T 导出＋relink 0＋闸 15/5/0＋矩阵 5/5＋m2=42。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 五刀：pipeline_wpo_typecheck_emit_bridge 迁 `.x`（`f24d26c90`）** | **代码＋文档** · **不升钉**。33 行 WPO 双桥（`run_x_pipeline_typecheck_entry_emit` thin bl 目标＋`resolve_path_try_one_lib_root` 裸名→pipeline.x 导出）：权威＝`src/pipeline_wpo_typecheck_emit_bridge.x`（禁供 `pipeline_run_x_pipeline_impl` 不变量文档化）；ensure `--auto` 优先＋lane 逐叶验自身导出。seed 物理删。验＝双端 `--auto` 出双 T 导出＋relink 0＋闸 15/5/0＋矩阵 5/5＋m2=42。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 四刀：runtime_process_args_thin 迁 `.x`（`4a12cbafb`）** | **代码＋文档** · **不升钉**。29 行 std/process argv 包装（历史冷权威因＝库 `-x -E` 截断——今已不截）：权威＝`src/runtime_process_args_thin.x`（裸符号不变量文档化）。process_merge 接线＝map 有产品时指 `.x`＋merge 体 `-x -E` 再生稳定 gen（两导出验后受）＋seed 表项＝无产品冷兜底；seed 物理删。验＝双端 process_merge 重建（24/23 面；首试 LC_SEGMENT 报错＝pure_ld_partial_merge 既有 ld -r→libtool 回退，与本刀无关）＋run-process/env/set 绿＋闸 15/5/0＋矩阵 5/5＋m2=42。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 三刀：pipeline_glue_link 迁 `.x`（`e4c3dd410`）** | **代码＋文档** · **不升钉**。29 行 Target-B C 桥：权威＝`src/pipeline_glue_link.x`（`pipeline_run_x_pipeline`→`_impl` 转发）。cc_inc_tu lane 升级**逐叶有效性**（entry 叶验 main＋char\*\*；桥叶验自身导出、ABI 不动——首版统一 main 判据致桥叶不命中，即修）；build_xlang_asm ensure 改 `--auto`（.x＋seed 双 mtime 新鲜度）。**考古注**：该 ensure 树内无调用点（Target-B 实验链遗留）＝潜在第 4 D 类。验＝双端 relink 0／闸 15/5/0／矩阵 5/5／m2=42。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 次刀：crt0_mingw 迁 `.x` 权威（`67d2971a7`）** | **代码＋文档** · **不升钉**。高价值活叶（21 行 Win64 PE 入口 seed，`.inc` 晋升遗产；`.o` 在 G05_OBJS）：权威＝`src/crt0_mingw.x`（转调 `xlang_forward_main_to_main_entry`）；cc_inc_tu prefer lane 扩至该叶（逐叶 `.x` 源＋`<leaf>_gen.c` 稳定 gen＋char\*\* fixup 同款）；ensure r2-crt0 新 `cc_inc_tu_x` kind→`--auto`（WIN32_O_CFLAGS 透传保留；MINGW/MSYS/XLANG_IS_WIN_HOST host 门不变）；seed 物理删。验＝双端 `XLANG_IS_WIN_HOST=1` 强测 dispatch（`T main`）＋闸 15/5/0＋矩阵 5/5；Windows 端到端待 VM（立卡）。序推进：pipeline_glue_link(29)→runtime_process_args_thin(29)→…。PLATFORM: WINDOWS\|MINGW 入口；lane host-portable。 |
| 2026-09-10 | **feat(selfhost)** **7.2.1 首刀：build_tool_main 迁 `.x` 权威（`8a7efbf7e`）** | **代码＋文档** · **不升钉**。26 真素材最小件（16 行 C seed，.inc 晋升遗产）：权威＝`src/build_tool_main.x`（最薄 main 转调 build_runner entry）；`cc_inc_tu` 新 `--auto` 优先航道＝产品 `-x -E`→稳定工作树 gen（driver_gen 生命周期）＋**char\*\* main 签名 fixup**（C main 强制 char\*\*，.x 无 char 型——u8 发射 uint8_t\*\*；实测首版被 clang 双错拒后加 sed）；无产品二进制时 seed 冷兜底；`build_tool.sh` 调用面切 `--auto`（首验漏改调用面 missing-file 红即修）。seed 物理删。验＝双端：`--auto` 出 `T _main`／`./xbuild build-tool` rc=0（删后）／`build_tool ./xlang_asm` 跑 g05 relink OK／闸 15/5/0／矩阵 5/5。次刀序：crt0_mingw(21)→pipeline_glue_link(29)→…。PLATFORM: SHARED。 |
| 2026-09-10 | **chore(selfhost)** **8.3.6 第五片：25 契约注清理＋26 件删（`f6af9bca9`）** | **注释＋删除** · **不升钉**。第四片定性的 25 处 prove 契约注（描述已退役手工工作流的 `nm IDENTICAL` 孪生）逐处清理：20 seed 头注＋3 `.x`（fmt_check_cmd_thin／runtime_driver_diagnostic_thin／labi_ondemand_heavy）＋`xlang_posix_env.h` 文件列表子串；runtime.x 两处两行 PLATFORM 注改写保留「product g05 does not link this mega」有用尾句（首版脚本残留 pair 已手工修复）。删 26 件（25 surface＋diag_surface 假阳性）。**8.3.6 累计删除 114 件（−157,003 行）**。验＝编辑面 cc 语法零错＋双端闸 15/5/0＋矩阵 5/5＋m2=42＋run-compress OK。误提交构建包装件（会话前未跟踪碎屑）`5974a75fa` 移除。PLATFORM: SHARED。 |
| 2026-09-10 | **chore(selfhost)** **8.3.6 第三片：88 死件物理删（`88cc6d028`，−123,566 行）** | **代码（纯删）＋文档** · **不升钉**（无产品面行为变化）。核证链：构建系统零引用（片二）→ 全仓 git grep（排自身；覆盖 seed 互引/scripts py+pl/CI 面；确认无 `seeds/*.from_x.c` 动态 glob——唯一真 glob＝pin 族不受影响）→ 88 个 `*_surface`/prove 孪生 DEAD 物理删；27 个弱引用者（兄弟 seed 头注/再生脚本）判活保留。验＝删后双端 drift-gate 15/5/0＋矩阵 5/5＋m2=42＋run-compress OK。8.3.6 进度：311→普查→四类骨架→消费面→**−88 件**；残＝80 有引用三分＋27 弱引用复核。PLATFORM: SHARED（文件删除 only）。 |
| 2026-09-10 | **docs(selfhost)** **define 微谜终解（无代码改动）** | **文档** · 树净。仪器化（脚本内置位，修上轮 /tmp 路径错位）三中间件实测：raw/stripped/fixed 全 0 define、写入后追加——注入者＝`ensure_driver_gen` 尾部 `fix_driver_gen_duplicate_main.pl`（历史 Makefile 平衡步骤，「driver_gen thin TU 别名」段：`main.x -E-extern 调用 main_*，build_asm/main.o 为单前缀符号`；对 pin 与再生统一跑）。**再生链至此端到端全解**；注意裸名消费面在 build_asm 航道（产品 `.o` 树外，「零引用」结论范围随之修正）。Windows VM 仍离线。 |
| 2026-09-10 | **feat(selfhost)** **7.4.4 staged-diff 禁令闸（`976f475cb`）＝家族收口** | **代码＋文档** · **不升钉**。`pin_gen_drift_gate` 新 `--staged/--cached/--head` 模式：范围内 seed/pin/gen 变更而无 `.x` 伴随 → WARN 列名（advisory——合法 seed-only commit 存在：再生成追平/退役/重钉波；警示非阻断）；`compiler_all_ci` 尾挂 `--head`。验＝内容级双态（seed-only→WARN 列名／seed+.x→OK）＋mtime-only 正确忽略＋Ubuntu 同步跑绿（--head OK＋闸 15/5/0）。**7.4.4 全家族收口**：v1 闸（`34a87530d`）／v2 pin-mtime（`28315bed2`）／v3 前缀＋再生重钉＋双端激活（`41fc308c1`/`64f493ffc`/`ee2f5f46c`）／CI＋staged-diff（`5cac88d00`/`976f475cb`）。PLATFORM: SHARED。 |
| 2026-09-10 | **fix(selfhost)** **剥器 v3＝Ubuntu 再生航道激活＋双端收敛（`ee2f5f46c`）** | **代码＋文档** · **不升钉**。三轮诊断终者定罪：`gen_strip_dep_bodies` 返回类型枚举漏 `int64_t`→`std_sys_linux_raw_syscallN` dep 体漏剥→其 `xlang_panic_` 调用爆 cc 自检（「发射分歧」「切片冻结」皆误诊，已纠）。修＝v3 块累积剥离器（col-0 头累积至 `{`，任意返回类型/多行签名/混合 extern+def 块；途中修两实现坑：skip 元组空串毒化、重写切掉写盘块）。验＝**Ubuntu 产品再生航道首次走通**（regenerated via／0 panic／cc 0 错／1840004B）＋**双端 gen T 集收敛**（各 25 comm 空）＋双端 drift-gate 15/5/0＋矩阵 5/5＋m2/m6=42。**微谜已解**：`#define` 来自 `ensure_driver_gen` 尾部 `fix_driver_gen_duplicate_main.pl` 后归一化（历史 Makefile 平衡「thin TU 别名」段；三中间件实测全 0 define、perl 追加）——再生链端到端全解；裸名消费面在 build_asm 航道（产品 `.o` 树外）。PLATFORM: SHARED。 |
| 2026-09-10 | **test/docs(selfhost)** **双端 tip L4 真冷全绿 → 升钉 `fa1061224`→`5cac88d00`（当日二度）** | **测试＋文档** · **升钉**。双端全擦冷启＠`5cac88d00`：Ubuntu 15m1s（bstrict **129/129** 7m46s；`ubuntu-server:/tmp/xlang_l4_Linux_5cac88d00.log`）＋Darwin 21m45s（bstrict **129/129** 16m22s；`/tmp/xlang_l4_Darwin_5cac88d00.log`）。当日面全收口：v3 opt-in `-lib-name`（`41fc308c1`）／g05 新鲜度洞根修（`31e09653b`）／driver_gen 产品再生＋重钉（`64f493ffc`）／CI advisory drift-gate 接线（`5cac88d00`）。 |
| 2026-09-10 | **feat(selfhost)** **driver_gen 产品再生＋重钉（`64f493ffc`）** | **代码＋文档** · **不升钉** · **构建级别 L2**。v3 的兑现波：ensure_driver_gen 新**产品再生航道**（`./xlang_asm -x -E -lib-name main`→`gen_strip_dep_bodies.py` 剥 35 个 dep 体（裸名＝真提供者权威，复刻 -E-extern 语义）→`post_E_fixup.py` 注入→`driver_get_argv_i` 外置声明（其 -E extern 仅原型作用域）→**cc -fsyntax-only 自检门**；历史 seed/xlang-x/xlang-c 链保留回落）。重钉安全核验＝**10 个 pin-only T 符号全树零外部引用**（死导出）；唯一再生冲突 `std_sys_read_file_into` 恰为剥离面。途中两小坑：有效性标记空格（新发射 `!=0` 无空格）、外置声明须插 include 后（未知类型名）。验＝双端：gen 1.83MB cc 零错（无 -Wno-implicit 逃生）／relink 0（+ad-hoc codesign）／矩阵 5/5／wave943 11/11／run-compress OK／drift-gate 15/5/0（重钉同 commit）。**注**：发射平台分歧（Darwin 无 panic 引用＝航道主用；Ubuntu 含之→回落 pin＝已提交内容双端 gcc 干净）——Ubuntu 航道激活立小卡（补 panic 前向声明）。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.4.4 v3 opt-in `-lib-name` 前缀落地（`41fc308c1`）** | **代码＋文档** · **不升钉** · **构建级别 L2**。`-x -E` 航道 opt-in 前缀：裸默认不变；`-lib-name <v>`→发射导出全带 `name_` 前缀（双端验：`main_probe_lane`／`main_driver_argv_parse_x_path` 前缀化；`""` 与无 flag 裸保持）。关键根因两枚：①**`-E` 子循环抓路径**——首攻零效果真因（子循环把 `-lib-name` 当路径 return，外层分支永不可达）；②**macOS rc=137＝码签名失效**（`.o` 手术+relink 后 Killed:9，codesign ad-hoc 修；回溯解释昨夜「OOM」）。载体：rt_emit_state always-seed（槽＋pctx setter，**不进 pabi**——inject-only 不吃新 rest／FORCE 禁令）＋main.x 三处 parse（.x＋pin）＋rt_run_x_emit 三孪生。途中 FORCE pabi 毒化产品一次（矩阵 0/5），按禁令冷重建恢复。验＝双端：微输入+main.x 前缀化／cc 编译绿／矩阵 5/5／drift-gate 15/5/0（gen 已重生成）／wave943 14/14／run-compress OK。残＝driver_gen 全量再生决策＋CI 接线。PLATFORM: SHARED。 |
| 2026-09-10 | **docs(selfhost)** **v3 二轮重试前侦：分派链钉死＋lldb 变体陷阱（无代码改动）** | **文档** · 零改动（树干净：drift-gate 15/5/0）。静态钉死 `-x -E` 分派：main_entry（首参旗标）→ `driver_argv_parse_x` → `-E` 子循环 **return 1** → 直调 `driver_run_x_emit_c()`（ew_set_* 全局）——不走 x_path_impl；v3 的写入点＝该函数**实际执行体**。lldb 实测：同名体 ld -r 合并出导出/局部变体，按名断点绑导出体不中执行体（微输入两断点零命中但有输出）——下轮须 `image lookup -rn` 列变体逐断。微输入探针法（单函数 .x 秒判）已入卡。 |
| 2026-09-10 | **fix(selfhost)** **g05 新鲜度洞根修（`31e09653b`）** | **代码＋文档** · **不升钉** · **构建级别：脚本基建**。v3 两波混乱的干扰源根修（复现→定罪→修→双端复演）：洞①＝G05_OBJS 链接五件独立 rt 切片（`_RT_SEED_SLICE_OBJS`）但 g05 只查存在（新鲜度权威 `ensure_rt_seed_slice_objs` 仅 asm-strict 航道调用）——修＝g05 ensure 挂既有 `rt-slice` 族权威（seed -nt .o → cc）；洞②＝pabi 航道复验**无洞**（推翻）；洞③＝driver_x.o 的 Track L 新鲜度只比 `main.x`——pin 驱动的 `driver_gen.c` 刷新后 `.o` 仍「up-to-date」（昨夜 parse guard 首层缺失的真机制）——修＝leaf 判定纳入 gen 孪生（`./<stem>.c`），-E prefer 失败自然回落刷新后的冷种＝pin 刷新正路。验＝双端 touch 复演（seed/gen→g05 自动重建，无手工 rm/FORCE）＋矩阵 5/5＋drift-gate 15/5/0＋m2=42。此基建直接服务 v3 重试（消除航道陈旧干扰）。PLATFORM: SHARED。 |
| 2026-09-10 | **test/docs(selfhost)** **7.4.4 v3 收尾：冷重建画布＋命名考古（`a58899348`）** | **测试＋文档** · **不升钉**。按知识卡姿势执行：①`./xbuild l4 --rebuild-only` 全量冷消航道混（4m52s 绿）——留确定态画布供 v3 单变量重试；②三验绿（矩阵 5/5／drift-gate 15/5/0／m2=m6=42）；③命名考古：基线裸 `-E` 的 8 个 `main_` 面系 main.x **字面函数名**（无隐藏前缀机制——上波「泄漏」系误判）；pin T 面为旧发射器**历史混合命名**（eq_asm 裸 vs main_driver_emit_* 带）→「`-E`＋注入」再生须逐名复刻旧命名（否决该捷径）；④原型实证：`-E`＋注入＋宽松 cc 可编译通过（rc=0 零 error），唯命名面不对。正路仍＝emit 航道 opt-in `-lib-name`（v3 知识卡）。 |
| 2026-09-10 | **docs(selfhost)** **7.4.4 v3 `-lib-name` 尝试波（回退收口）** | **文档** · **代码已回退** · 树复稳（矩阵 5/5＋drift-gate 15/5/0＋m6=42）。尝试＝opt-in `-lib-name` 前缀全链（BSS 槽／parse 双循环／三航道 setter＋Cap 层 entry 偏移 8389876/8390004／五 C 孪生／双 pin）——符号链路全通但输出零效果＋航道级联陈旧干扰（pabi/rt_emit_state/driver_x ensure 新鲜度洞）＋一次 T001 瞬态，按「一波一闭」纪律整体回退。**知识收获**：裸 `-x -E` 对 main.x 发射完整（唯一差异＝entry 前缀）；活跃航道＝`dispatch_x_emit_to_c`→`driver_run_x_emit_c` thin slice（非 run_x_emit_x）；警惕 rt_run_compiler_parsed writer 双写。重试姿势与完整地图入 C迁移追踪 7.4.4。 |
| 2026-09-10 | **fix(selfhost)** **7.4.4 v2 pin mtime 产生点根修（`28315bed2`）** | **代码＋文档** · **不升钉** · **脚本层（无产品面改动）**。v1 闸检出的陷阱在产生点杀死：driver_gen／preprocess／archaeology／lsp 四族 ensure 的 pinned 判定原先只看存在（+FORCE），git 改 pin 永不失效工作树 gen；现纳入 pin mtime——driver_gen 走 any_dep_newer 依赖集（-E 尝试→fallback seed），其余五处 pinned 需 seed 非新、pin 更新→恢复 seed 拷贝。migrate 族（parser/typeck/codegen/lexer）本就正确（seed -nt gen→刷新）未动。验＝双端复演昨夜陷阱（touch pin→无 FORCE ensure→`fallback seed`／`restored (pin newer)`／lsp 刷新）＋drift-gate 仍 15/5/0 绿；gen 字节相同无产品重建。v3 立卡＝.x↔pin 语义闸（**被 §3.1 `-E-extern` 发射缺口阻塞**：产品 rc=1/0 行，实测）＋CI 接线＋staged-diff 禁令。PLATFORM: SHARED。 |
| 2026-09-10 | **feat(selfhost)** **7.4.4 v1 pin↔gen 漂移闸（`34a87530d`）** | **代码＋文档** · **不升钉** · **构建级别：脚本闸（无产品面改动）**。选题＝Windows 卡阻塞（VM 离线无法拉起）后按 DAG 取 7.4.4。实现＝`compiler/scripts/pin_gen_drift_gate.sh`（G.7 单体）＋`./xbuild drift-gate` 入口：每对 pin seed↔工作树 gen 做 cc -c＋nm T 符号集对照；文本漂移（发射器版本格式化）不算、T 集漂移＝语义信号；缺 gen（按需构建 4 对）与考古退役（lsp_io_gen，wave1036）SKIP。验＝双端 15 PASS／5 SKIP 绿＋负样本（driver_gen.c 追加假符号→FAIL 报 only-in-gen）＋还原绿。直捕 2026-09-10 driver_gen 陈旧坑类（pin 改后工作树 gen 陈旧、mtime up-to-date 判定不感知）。v2 立卡＝.x↔pin 语义漂移（须产品 -E）／CI 接线／staged-diff 只改 seed 禁令。另：本波顺带实测 7.2.1 真实规模（seed 4,555 行＋切片族；pthin_*.x 仅 237 行→多波史诗，更新认知非 21,935 单文件）。PLATFORM: SHARED。 |
| 2026-09-10 | **test/docs(selfhost)** **双端 tip L4 真冷全绿 → 升钉 `e194cd294`→`fa1061224`** | **测试＋文档** · **升钉**。双端全擦冷启＠`fa1061224`：Ubuntu bootstrap＋g05＋矩阵 5/5＋bstrict **129/129**（7m46s）＝15m18s（`ubuntu-server:/tmp/xlang_l4_Linux_fa1061224.log`）；Darwin 同序全绿（bstrict 16m27s）＝21m50s（`/tmp/xlang_l4_Darwin_fa1061224.log`）。§3.3 收口后首钉。 |
| 2026-09-10 | **fix(selfhost)** **std/compress impl 前缀注入（`fa1061224`）** | **代码＋文档** · **升钉见上行** · **构建级别 L4**。根因（旧产品 A/B 定罪）：worktree 检出 `e194cd294` 造旧产品——旧 mod.x `-E` 的生成 C **仍含限定 import dep 的前缀函数体**（`std_compress_gzip_libz_compress_gzip_compress_c` 有 def）；`7e2fef61b`（skip std/ in need_coemit）停掉 std/ dep co-emit 后体消失，而车辆 impl 发射两 lane（`-lib-name ""` 与无 flag）**均裸名**（新旧产品 A/B 同裸——发射层非回归）→ mod.o U 前缀 vs T 裸错配。修＝G.7 补全车辆自有契约（「impl .x 无 `-lib-name` → 路径前缀」）：`xlang_compile_std_module.sh` 对 `std/compress/` 下 impl 源（**非 mod.x**——首版误注 mod 致双前缀，已守卫）的 `^export function` 名单在生成 C 上 perl 词界前缀化；名单动态取自源文件＝无第二套符号表；FFI extern "C" 名不在名单不动。验＝双端四 .o 重建（zlib T=4/U=0；gzip/zstd/brotli mod＋lib 面齐）＋`run-compress.sh` 双端 **compress test OK** ＋双端 L4 全绿升钉。运维记录：Darwin nm 前导下划线坑（`T _std_…` 须带 `_` grep）。PLATFORM: SHARED。 |
| 2026-09-10 | **fix(selfhost)** **link_alias 4 参 mangled 面＋L4 兜底双跑（`7f62aa9da`）** | **代码＋测试＋文档** · **不升钉（bstrict 新卡红）** · **构建级别 L4**。日终 L4 兜底首跑双端红在 seed-phase1 pure-ld：UNDEF `glue_try_std_heap_redirect_sym_local_u8_ptr_i32_u8_ptr_i32_reti32`（引用者＝prefer thin+rest 航道 `-E` 全量 dispatch.x 的 mangled X-ABI 调用）。根因＝`x_frontend_link_alias.from_x.c`（自述职责「backend_call_dispatch X-ABI mangled faces → C 产品符号」）对该函数仍是旧 3 参 weak 桩化石；昨日产品链绿纯靠生成式 `asm_full_link_stubs` 扫描偶然含面，L4 清场再生（扫描集无 dispatch）丢面→phase1／g05／CC 逃生口三连诚实红。修＝G.7 补全既有桥面（同胞 `glue_asm_build_func_export_sym_c_u8_ptr…` 同型）：4 参 mangled 转发替化石桩。验＝g05 恢复链绿（alias.o T-mangled＋U-plain）＋wave943 10/10＋矩阵 5/5；重跑双端 L4 **过 phase1／bootstrap（Ubuntu 329s／Darwin 199s）／g05／矩阵 5/5**（Ubuntu 14m33s／Darwin 21m11s 到 bstrict）。**新卡＝run-compress 冷链红（§3.3）**：双端 bstrict 唯一红，std/compress 子模块 .o lib 面裸名 vs mod.x 前缀调用错配（取证链完整：T=0/U=8、KEEP_C 裸名、探针分岔），下刀方向＝vehicle 层禁 bare（spec `mod|1`→compress 族自适应）。PLATFORM: SHARED。下一刀＝§3.3 根修→双端 L4 全绿升钉。 |
| 2026-09-10 | **fix(selfhost)** **parse 层悬空值吞噬守卫（`45f8296a5`）** | **代码＋测试＋文档** · **不升钉** · **构建级别 L2**。五问：产生＝parse 各层对取值 flag（`-o`／`-O`／`-L`／`-backend`／`-target`／`-target-cpu`）无条件 `i+2` 吞下一项——run 路径 `driver_argv_ensure_run_o` 注入的 `-o <temp>` 尾对被当值吃掉→产物未链 temp→execv ENOENT 127；存储＝parse state（out_path／lib_roots 等）；消费＝run／bare／build 全编译路径。修＝G.7 单权威新谓词 `driver_compile_argv_next_is_value_c`（rt_compile.x `#[no_mangle]`：下一项存在·非空·非 `-` 才吞；转发侧守卫的 parse 层孪生）；七文件同 commit 同语义——main.x `parse_x_path` 5支＋`parse_x` 3支＋`-E` 子循环、compile.x step 6支、rt_compile.x `step_c` 6支＋`apply_*_next_c` 6件、冷体 `rt_compile.from_x.c`、surface `-E` 风格、双 pin（`driver_gen`／`driver_compile_gen`）。**运维坑（立记录）**：`driver_gen.c` up-to-date 判定只看 `MAIN_X_DEPS` mtime、不感知 pin 变更——改 pin 后须 `XLANG_FORCE_REGEN_GEN=1 ./xbuild driver-gen` 再 `ensure_host_cc_seed_o try-heat driver_x.o`（Darwin／Ubuntu 双端同坑；首验 m6 绿曾靠第二层 parse 假象，nm U/T 三 .o 验证后才真全层就位）。验＝双端 wave943 **20/20**（m1–m5 回归＋五悬空形状 `hi -L`／`-backend`／`-target`／`-O`／`-target-cpu` 全 127→42＋悬空无位置参＝7＋bare）＋矩阵 5/5；g05 重建 driver_x.o／driver_compile_x.o／runtime_driver_no_c.o。PLATFORM: SHARED。 |
