# ShuX 自举进度 · 归档 2026-07-22（只读）

> **归档说明（2026-07-23）**：22 号全日仪表盘 + 变更流水整表移入本文；**勿把归档 tip / 单叶日志当 23 日现绿**。
> **快照 tip（22 日收口）**：`413dc5182`（path residual Cap residual pure **wave163** panic_o_path · 日常 L2）。
> **产品 L4 钉盘（22 日）**：`3c88f7e51`（双端 tip L4 真冷 + **129**；升自 `df1dde5b6`；含 wave110 ImportEntry pure）。
> **22 日主线摘要**：Cap residual pure path residual wave114–163（lib_root / ld argv / push_obj / thin `*_o_path` G.7 / compiler_o_path_copy / repo_root / panic_o_path）+ freestanding/ondemand/invoke_ld/cc 多叶 pure + tip L4 升钉至 **`3c88f7e51`**。
> **接续**：23 日起见 [`自举进度.md`](自举进度.md)。
> **更早**：[21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md) · [19](自举进度-归档-2026-07-19.md) · [18](自举进度-归档-2026-07-18.md) · [17](自举进度-归档-2026-07-17.md) · [16](自举进度-归档-2026-07-16.md) · [14](自举进度-归档-2026-07-14.md) · [13](自举进度-归档-2026-07-13.md)。
> 请勿再向本归档追加「当前前排」。

---

# ShuX 自举进度

> **更新**：2026-07-22 · 21 号全日已归档；22 号起重新起表\
> **基线 SHA（产品 L4 放行钉盘）**：**`3c88f7e51`**（升自 `df1dde5b6`；双端 tip L4 真冷 + 产品轨 **129** 全绿；含 wave110 ImportEntry pure 整波收口）\
> **产品 bstrict 套件数**：**129**\
> **本波 tip**：**wave163** panic_o_path pure @ `413dc5182`（日常 L2；钉盘仍 **`3c88f7e51`**）\
> **本波（22 日）状态**：path residual — `shux_runtime_panic_o_path` 真迁 L0 pure orch（count 18→19）；22 日 23:59 产品合入。地图：[当前问题分析.md](当前问题分析.md)（非每微步必改）。

> **方法**：[自举方法.md](自举方法.md)（Cap/R/L/M；工程 ≠ 产品放行）\
> **Windows（删种子前）**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `shux-selfhost-product-gate`\
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md) · [19](自举进度-归档-2026-07-19.md) · [18](自举进度-归档-2026-07-18.md) · [17](自举进度-归档-2026-07-17.md) · [16](自举进度-归档-2026-07-16.md) · [14](自举进度-归档-2026-07-14.md) · [13](自举进度-归档-2026-07-13.md)

### 维护约定

1. 每完成一波 **必改两文**：本文（KPI/前排/§6）+ [`当前进度.md`](当前进度.md)。
2. **禁止**把 prove/Stage2/N++ 单独写成「可自举」。
3. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**；历史钉盘 125）。
4. 工程 KPI 写 **SHA + 日志路径**；过期整日流水移归档。
5. 大债地图默认写 [`当前问题分析.md`](当前问题分析.md)（承接原 `问题分析文档.md`）；非每微步。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 =** **`3c88f7e51`（双端 tip L4 真冷 + 129；wave110 ImportEntry pure 整波收口）。**\
**产品代码 tip =** **`413dc5182`** wave163 panic_o_path pure（日常 L2；钉盘未升）。

| 轨                | 状态                                                                                           |
| ---------------- | -------------------------------------------------------------------------------------------- |
| 工程轨              | R2 + **wave163 panic_o_path pure** @ `413dc5182`（前波 wave162 repo_root @ `e1bbddb96`；钉盘仍 wave110 `3c88f7e51`） |
| 产品轨 tip / 钉盘     | tip **`413dc5182`** · 钉盘 **`3c88f7e51`**                                                                |
| mac 产品链 bstrict  | ✅ **OK (129)** 钉盘 tip L4 · `/tmp/mac_true_bstrict_3c88f7e51.log` |
| Ubuntu L4（金标）    | ✅ **OK (129)** 钉盘 tip L4 · `/tmp/ubuntu_true_bstrict_3c88f7e51.log` |
| 前钉 L4            | ✅ 129 @ `3c88f7e51` · `df1dde5b6` · `07684b813` · `ed2dc1a1e` 等                               |

***

## 1. 文档地图

| 文档                                             | 看什么                   |
| ---------------------------------------------- | --------------------- |
| [自举方法.md](自举方法.md)                             | Cap / R / L / M       |
| [自举步骤.md](自举步骤.md)                             | 可执行闸门                 |
| [自举验证.md](自举验证.md)                             | 怎么测                   |
| **本文**                                         | 长期 KPI + 前排（**每波必改**） |
| [当前进度.md](当前进度.md)                             | 当天快照（**每波必改**）        |
| **[当前问题分析.md](当前问题分析.md)**                     | tip 级产品债地图（21 日起权威）   |
| [自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) | **21 号全日**（只读）        |
| skill `shux-selfhost-product-gate`             | L4 / 双端 / 假绿禁止        |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项                          | 结果                                                                                  | 日志 / 备注                                      |
| -------------------------- | ----------------------------------------------------------------------------------- | -------------------------------------------- |
| 产品 bstrict 套件数             | **129**                                                                             | 现行白名单                                        |
| 产品 L4 放行钉盘                 | **`3c88f7e51`**                                                                      | 22 日 tip L4 升钉（前钉 `df1dde5b6`）；双端 L4 129；含 wave110 ImportEntry pure      |
| 本波 tip                     | wave163 panic_o_path pure @ `413dc5182`（钉盘仍 `3c88f7e51`）                             | **构建级别=L2** 日常真测                     |
| mac 产品链 bstrict（tip L4） | ✅ **OK (129)**                                                                      | L4 · `/tmp/mac_true_bstrict_3c88f7e51.log` · cold `/tmp/mac_true_cold_3c88f7e51.log`    |
| Ubuntu L4（金标 · tip L4）  | ✅ **OK (129)**                                                                      | L4 · `/tmp/ubuntu_true_bstrict_3c88f7e51.log` · cold `/tmp/ubuntu_true_cold_3c88f7e51.log` |
| 产品矩阵（双端 tip L4）           | ✅ rv42 / option102 / si / hello + pure-asm option102/hello                                                                | mac + Ubuntu §4                                      |
| 前钉 mac/Ubuntu L4           | ✅ 129 @ `df1dde5b6` · `07684b813` · `ed2dc1a1e` · `1f73ed275` 等                                                                | 见 [21 号归档](自举进度-归档-2026-07-21.md) / 更早归档                             |

### 2.2 工程轨（继承）

| KPI           | 现                                        | 备注                   |
| ------------- | ---------------------------------------- | -------------------- |
| **T / EMPTY** | 18/18                                    | 18 号结案               |
| **N**         | 54/54 IDENTICAL                          | 回归基线                 |
| **R2 / 根修本波** | **wave163** panic_o_path pure → L0 path_pure | tip **`413dc5182`** · 钉盘 **`3c88f7e51`** |

***

## 3. 当前前排（≤3 条）

1. 其它 rest R2 / mega residual（可迁：complex path ladders crt0·freestanding_io·async_scheduler / freestanding ensure·path residual …；常驻 Cap 不硬迁：contains_substr / undef_sym / path_io stat / wait / strerror / mega spawn-IO / ld_debug_argv 遍历 char** / invoke_cc_argv_push_existing realpath 池 / ensure_std_net_o_auto_tls system·make / getenv / shu_resolve_compiler_dir / `*_o_path` 静态缓冲 …）
2. **可选** 未闭合块注释硬诊断（EOF depth>0）
3. **可选** Windows hybrid（**需要时再通知重启**）

***

## 4. 与 21 号归档的关系

- 21 号全日（async PREFER、bxec/rio、driver\_abi wave14–44、pipeline\_abi wave45–75、nested comments、钉盘升至 **`d4e2dca6`** 等）已整表移入 [`自举进度-归档-2026-07-21.md`](自举进度-归档-2026-07-21.md)。
- **勿**用归档 tip（如 `5421694c` wave75）或旧钉 `df1dde5b6` / `07684b813` / `ed2dc1a1e` / `1f73ed275` / `d4e2dca6` 等冒充现行 tip L4（产品 **`3c88f7e51`**）。
- 22 日新工作只记本文 §0/§2/§3/§6。

***

## 5. 双端 / 平台

| 平台                     | 本波状态                                                              |
| ---------------------- | ----------------------------------------------------------------- |
| **macOS arm64**        | ✅ tip L4 全擦 + 矩阵 + pure-asm option102/hello + **129** @ `3c88f7e51` |
| **Ubuntu x86\_64（金标）** | ✅ tip L4 全擦 + 矩阵 + pure-asm option102/hello + **129** @ `3c88f7e51` |
| **Windows MSYS2**      | 继承 hybrid；本波未重跑（**不需要切换**）                                             |

***

## 6. 变更记录（倒序 · 22 日起；21 号见归档）

| 日期         | SHA                                                     | 摘要                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------- | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-22 | **`413dc5182`** / docs | **R2 Cap residual pure wave163**：path residual 可迁叶 — panic_o_path pure：`shux_runtime_panic_o_path` 真迁 pure orch（`labi_path_pure.x`：Cap residual `shux_runtime_o_realpath_if_exists` + `getcwd` + `asm_link_obj_skip_missing`；pure cwd/argv0 字节 join + last-sep index；BSS 512/4096；count 18→19）。**单叶**迁入 L0（wave162 repo_root 后；complex path ladder 首叶）。mega `#ifndef PATH_PURE_FROM_X` 冷体；hybrid `SHUX_LABI_PATH_PURE_FROM_X` → L0 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 panic path ladder 体仍 always mega C」。crt0 / freestanding_io / async_scheduler path ladders 与 freestanding ensure·path 仍 Cap residual。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave163.log` · `/tmp/mac_w163_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave163.log` · `/tmp/ubuntu_w163_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED orch · **日常 L2 非 tip L4** |
| 2026-07-22 | **`e1bbddb96`** / docs | **R2 Cap residual pure wave162**：path residual 可迁叶 — repo_root pure：`shux_repo_root_from_argv0` 真迁 pure orch（`labi_path_pure.x`：Cap residual `shu_resolve_compiler_dir` + pure 剥父目录 → repo root；暖树 fallback Cap residual `shux_rel_o_path_from_argv0` + pure 三 sep 剥 `std/process/process.o`；BSS 512 返回缓冲；count 17→18）。**单叶**迁入 L0（wave160 compiler_o_path_copy / wave161 thin join 后）。mega `#ifndef PATH_PURE_FROM_X` 冷体；hybrid `SHUX_LABI_PATH_PURE_FROM_X` → L0 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 repo_root 体仍 always mega C」。complex path ladders（panic / crt0 / freestanding_io / async_scheduler）与 freestanding ensure·path 仍 Cap residual。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave162.log` · `/tmp/mac_w162_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave162.log` · `/tmp/ubuntu_w162_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED orch · **日常 L2 非 tip L4** |
| 2026-07-22 | **`c8d709359`** / docs | **R2 Cap residual pure wave161**：path residual G.7 — 薄 `shux_runtime_*_o_path` join 收敛：27 叶（process_os_glue … ed25519_ref10_glue）删 per-leaf resolve+snprintf，统一路由 wave160 pure `shux_runtime_compiler_o_path_copy`（hybrid L0 pure / cold twin）。**G.7 单 join 权威**；静态 `PATH_MAX` 返回缓冲仍 mega Cap residual。未迁 complex ladders：panic / async_scheduler / crt0_user / freestanding_io / repo_root。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave161.log` · `/tmp/mac_w161_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave161.log` · `/tmp/ubuntu_w161_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`4bc90d1f5`** / docs | **R2 Cap residual pure wave160**：path residual 可迁叶 — compiler_o_path_copy pure：`shux_runtime_compiler_o_path_copy` 真迁 pure orch（`labi_path_pure.x`：Cap residual `shu_resolve_compiler_dir` + pure 字节 join `compiler-dir/leaf` 入调用方缓冲，无 snprintf；count 16→17）。**单叶**迁入 L0（wave151 append_user_extra 后；wave159 freestanding_enabled 后切 path residual）。mega `#ifndef PATH_PURE_FROM_X` 冷体；hybrid `SHUX_LABI_PATH_PURE_FROM_X` → L0 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 compiler-dir/leaf join 体仍 always mega C」。薄 `*_o_path` 静态缓冲叶 / freestanding ensure·path / repo_root 仍 Cap residual。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave160.log` · `/tmp/mac_w160_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave160.log` · `/tmp/ubuntu_w160_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED orch · **日常 L2 非 tip L4** |
| 2026-07-22 | **`70c81d787`** / docs | **R2 Cap residual pure wave159**：freestanding residual 可迁叶 — freestanding_enabled pure：`shux_link_freestanding_enabled` 真迁 pure orch（`labi_freestanding_list.x`：peer `shux_host_is_linux` + pure `labi_fs_env_freestanding` + Cap residual `getenv`；规则：非 Linux→0 / driver≠0→1 / env null·empty·`'0'`→0 / else→1）。**单叶**迁入 L7（wave144 needs_io/panic 后；wave158 net_tls 后切 freestanding residual）。mega 删 always 体；冷体经 freestanding `#include`；hybrid `SHUX_LABI_FREESTANDING_LIST_FROM_X` → L7 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 freestanding env 门闩体仍 always mega C」。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave159.log` · `/tmp/mac_w159_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave159.log` · `/tmp/ubuntu_w159_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED orch / LINUX freestanding consumers · **日常 L2 非 tip L4** |
| 2026-07-22 | **`c6af17207`** / docs | **R2 Cap residual pure wave158**：其它 rest R2 可迁叶 — net_tls pure：`invoke_cc_append_net_tls_ld` 真迁 pure orch（`labi_invoke_ld_list.x`：pure flag/marker/rel 表 + peer `labi_append_{openssl,mbedtls}_ld_flags` + Cap residual `link_abi_obj_exports_marker` / `link_abi_realpath_cap` / `shux_rel_o_path_from_argv0` / `invoke_cc_argv_push_existing` / `link_abi_host_is_apple` brew -L）。**单叶**迁入 L6（wave157 unix gcc tail 后）。mega 删 always 体；冷体经 invoke_ld `#include`；hybrid `SHUX_LABI_INVOKE_LD_LIST_FROM_X` → L6 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 net_tls -L/-l 体仍 always mega C」。Sibling `ensure_std_net_o_auto_tls`（system/make）仍 Cap residual 不硬迁。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave158.log` · `/tmp/mac_w158_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave158.log` · `/tmp/ubuntu_w158_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED orch / MACOS brew -L · **日常 L2 非 tip L4** |
| 2026-07-22 | **`4e3173a10`** / docs | **R2 Cap residual pure wave157**：其它 rest R2 可迁叶 — unix_gcc_tail_libs_impl pure：`shux_asm_ld_append_unix_gcc_tail_libs_impl` 真迁 pure orch（`labi_invoke_ld_list.x`：`ShuAsmLdStdLinkFlags` LP64 i32 布局读 have_io/net/math/compress/dynlib/sqlite/heap/fs + pure `labi_ld_flag_*` + peer compress orch + peer needs_compress + peer `shux_host_is_linux` + Cap residual `link_abi_host_is_apple`；pthread 选择 have_io→`-pthread` / else need_pt→`-lpthread`；`-ldl` 仅 Linux+dynlib；`-lc` 在 io/net/need_pt 必加，else 在 linux\|apple 且 heap/fs/math/compress/sqlite/dynlib 条件加）。**单叶**迁入 L6（wave156 mach tail 后）。mega 删 always 体（含 `#if __linux__` / linux\|\|apple 分支）；冷体经 invoke_ld `#include`；hybrid `SHUX_LABI_INVOKE_LD_LIST_FROM_X` → L6 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 unix gcc tail -l* 体仍 always mega C」。gate `shux_asm_ld_append_unix_gcc_tail_libs` 仍 labi_gates pure thin。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave157.log` · `/tmp/mac_w157_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave157.log` · `/tmp/ubuntu_w157_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED orch / LINUX primary · **日常 L2 非 tip L4** |
| 2026-07-22 | **`a35c1bc86`** / docs | **R2 Cap residual pure wave156**：其它 rest R2 可迁叶 — mach tail_libs_impl pure：`shux_asm_ld_append_mach_tail_libs_impl` 真迁 pure orch（`labi_invoke_ld_list.x`：`ShuAsmLdStdLinkFlags` LP64 i32 布局读 have_thread/sync/channel/math/compress/sqlite + pure `labi_ld_flag_*` + peer `asm_ld_append_compress_libs` + peer `link_abi_user_o_needs_compress_libs`）。**单叶**迁入 L6（wave155 harden 后）。mega 删 always 体；冷体经 invoke_ld `#include`；hybrid `SHUX_LABI_INVOKE_LD_LIST_FROM_X` → L6 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 mach tail -l* 体仍 always mega C」。gate `shux_asm_ld_append_mach_tail_libs` 仍 labi_gates pure thin。Sibling `unix_gcc_tail_libs_impl` 仍 always mega。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave156.log` · `/tmp/mac_w156_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave156.log` · `/tmp/ubuntu_w156_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED orch / MACOS consumers · **日常 L2 非 tip L4** |
| 2026-07-22 | **`4c8685123`** / docs | **R2 Cap residual pure wave155**：其它 rest R2 可迁叶 — linux harden_impl pure：`shux_append_linux_link_harden_impl` 真迁 pure orch（`labi_invoke_cc_list.x`：pure `labi_linux_harden_flag_{count,at}` 扫表 + 容量守卫追加 -pie/-fpie/-Wl,-z,*）。**单叶**迁入 L5（wave154 append_compress 后）。mega 删 always 体（含非 Linux no-op stub）；冷体经 invoke_cc `#include`；hybrid `SHUX_LABI_INVOKE_CC_LIST_FROM_X` → L5 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 harden append 体仍 always mega C」。gate `shux_append_linux_link_harden` 仍 labi_gates pure thin。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave155.log` · `/tmp/mac_w155_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave155.log` · `/tmp/ubuntu_w155_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED orch / LINUX consumers · **日常 L2 非 tip L4** |
| 2026-07-22 | **`323917cd9`** / docs | **R2 Cap residual pure wave154**：其它 rest R2 可迁叶 — invoke_cc append_compress pure：`invoke_cc_append_compress_ld` 真迁 pure orch（`labi_invoke_ld_list.x`：peer pure `link_abi_obj_needs_{zlib,zstd,brotli}` + pure brew/flag + Cap residual ensure/path + Cap residual `invoke_cc_argv_push_existing` realpath/skip/dedup 推 glue）。**单叶**迁入 L6（wave153 asm append_compress 后）。与 asm 叶同 needs/brew/flag 结构；glue 路径 asm 直 append、cc 走 push_existing。mega 冷体经 invoke_ld `#include`；hybrid `SHUX_LABI_INVOKE_LD_LIST_FROM_X` → L6 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 invoke_cc compress -l* 体仍 always mega C」。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave154.log` · `/tmp/mac_w154_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave154.log` · `/tmp/ubuntu_w154_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`e3be3d9af`** / docs | **R2 Cap residual pure wave153**：其它 rest R2 可迁叶 — asm append_compress pure：`asm_ld_append_compress_libs` 真迁 pure orch（`labi_invoke_ld_list.x`：peer pure `link_abi_obj_needs_{zlib,zstd,brotli}` + pure brew/flag + Cap residual `shux_ensure_runtime_compress_zlib_glue_o` / `shux_runtime_compress_zlib_glue_o_path` 直 append）。**单叶**迁入 L6（wave152 append_brew 后）。Sibling `invoke_cc_append_compress_ld` 仍 always mega（`invoke_cc_argv_push_existing` glue 路径）。mega 冷体经 invoke_ld `#include`；hybrid `SHUX_LABI_INVOKE_LD_LIST_FROM_X` → L6 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 asm compress -l* 体仍 always mega C」。**踩坑**：C block comment 内禁写 `*/`（`needs_*/…` 会提前结束注释）。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave153.log` · `/tmp/mac_w153_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave153.log` · `/tmp/ubuntu_w153_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`aaaa7c3a2`** / docs | **R2 Cap residual pure wave152**：其它 rest R2 可迁叶 — ld append_brew pure：`ld_append_brew_lib_paths` 真迁 pure orch（`labi_invoke_ld_list.x`：Cap residual `link_abi_host_is_apple`（#if __APPLE__ 全 arch，≠ aarch64-only host_lit）+ pure brew table count/at 扫表追加）。**单叶**迁入 L6（wave151 append_user_extra 后）。Cap residual：host #if 仍 mega。mega 冷体经 invoke_ld `#include`；hybrid `SHUX_LABI_INVOKE_LD_LIST_FROM_X` → L6 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 brew -L 体仍 always mega C #if APPLE」。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave152.log` · `/tmp/mac_w152_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave152.log` · `/tmp/ubuntu_w152_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED orch / MACOS consumers · **日常 L2 非 tip L4** |
| 2026-07-22 | **`bab2bcd0b`** / docs | **R2 Cap residual pure wave151**：其它 rest R2 可迁叶 — ld append_user_extra pure：`shux_asm_ld_append_user_extra_o_files` 真迁 pure orch（`labi_path_pure.x`：Cap residual `link_abi_user_extra_o_count` / `_at` + `link_abi_path_readable`（access R_OK）；纯 argv 扫表追加；count 15→16）。**单叶**迁入 L0（wave150 push_minimal 后）。Cap residual：CLI 表 globals 与 host access 仍 mega。**踩坑**：`argv == 0 as **u8` 静默 codegen 丢函数 → 用 `argv as *u8` 守卫（同 wave147）。mega 冷体 `#ifndef PATH_PURE_FROM_X`；hybrid `SHUX_LABI_PATH_PURE_FROM_X` → L0 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 append_user_extra 体仍 always mega C static」。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave151.log` · `/tmp/mac_w151_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave151.log` · `/tmp/ubuntu_w151_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`7fece731e`** / docs | **R2 Cap residual pure wave150**：其它 rest R2 可迁叶 — ld push_minimal pure：`link_abi_asm_ld_push_minimal_runtime_objs` 真迁 pure orch（`labi_path_pure.x`：Cap residual `shux_runtime_{asm_io_stubs,process_argv,panic}_o_path` primary + 复用 wave148 pure peer push_obj ×3；count 14→15）。**单叶**迁入 L0（wave149 push_glue 后）。Cap residual：既有 `*_o_path`（compiler-dir/cwd 解析；空串 → primary 不用）。mega 冷体 `#ifndef PATH_PURE_FROM_X`；hybrid `SHUX_LABI_PATH_PURE_FROM_X` → L0 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 push_minimal 体仍 always mega C」。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave150.log` · `/tmp/mac_w150_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave150.log` · `/tmp/ubuntu_w150_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`20215989f`** / docs | **R2 Cap residual pure wave149**：其它 rest R2 可迁叶 — ld push_glue pure：`link_abi_asm_ld_push_glue_after_std` 真迁 pure orch（`labi_path_pure.x`：have_std 门控 + Cap residual `link_abi_call_ensure_argv0` 持 C fnptr 调用 + 复用 wave148 pure peer push_obj；count 13→14）。**单叶**迁入 L0（wave148 push_obj 后）。Cap residual：`link_abi_call_ensure_argv0`（ensure 失败则静默 return）。mega 冷体 `#ifndef PATH_PURE_FROM_X`；hybrid `SHUX_LABI_PATH_PURE_FROM_X` → L0 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 push_glue 体仍 always mega C」。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave149.log` · `/tmp/mac_w149_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave149.log` · `/tmp/ubuntu_w149_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`051d0b34e`** / docs | **R2 Cap residual pure wave148**：其它 rest R2 可迁叶 — ld push_obj pure：`link_abi_asm_ld_push_obj` 真迁 pure orch（`labi_path_pure.x`：容量守卫 + pure 调试目标 cstr 匹配 + resolve 阶梯 primary→argv0/rel→lib_roots + hard bank_push + 复用 wave147 push_stable(bank=null) 去重追加 + flag_out；count 12→13）。**单叶**迁入 L0（wave147 push_stable 后）。Cap residual：`asm_link_obj_skip_missing` / `shux_rel_o_path_from_argv0` / `shux_asm_ld_bank_push` / `getenv`+`link_diag_ld_debug_push`。mega 冷体 `#ifndef PATH_PURE_FROM_X`；hybrid `SHUX_LABI_PATH_PURE_FROM_X` → L0 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 push_obj 体仍 always mega C」。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave148.log` · `/tmp/mac_w148_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave148.log` · `/tmp/ubuntu_w148_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`f5899a398`** / docs | **R2 Cap residual pure wave147**：其它 rest R2 可迁叶 — ld argv 稳定入链 pure：`link_abi_asm_ld_argv_push_stable` 真迁 pure orch（`labi_path_pure.x`：容量守卫 + Cap residual `shux_asm_ld_bank_push` + pure has_obj 去重 + argv 追加；count 11→12）。**单叶**迁入 L0（wave146 has_obj 后；同类 push_obj 下波）。mega 冷体 `#ifndef PATH_PURE_FROM_X`；hybrid `SHUX_LABI_PATH_PURE_FROM_X` → L0 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 argv_push_stable 体仍 always mega C」。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave147.log` · `/tmp/mac_w147_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave147.log` · `/tmp/ubuntu_w147_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`6dcea17c8`** / docs | **R2 Cap residual pure wave146**：其它 rest R2 可迁叶 — ld argv 路径去重 pure：`link_abi_asm_ld_argv_has_obj` 真迁 pure orch（`labi_path_pure.x`：null 守卫 + 纯字节 cstr 等值 + argv 扫描；Cap residual `link_abi_realpath_cap` 持 POSIX realpath，Windows 恒 null ≡ mega #if）。**单叶**迁入 L0（wave145 聚合 needs 后；同类 argv 聚合叶）。mega 冷体 `#ifndef PATH_PURE_FROM_X`；hybrid `SHUX_LABI_PATH_PURE_FROM_X` → L0 pure；surface re-pin count 10→11；prove **IDENTICAL**。闭合 soft residual「hybrid 下 argv_has_obj 体仍 always mega C」。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave146.log` · `/tmp/mac_w146_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave146.log` · `/tmp/ubuntu_w146_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`be0bf4382`** / docs | **R2 Cap residual pure wave145**：其它 rest R2 可迁叶 — heap 聚合 needs pure：`link_abi_link_needs_{heap_user_c,std_heap_import}` 真迁 pure orch（`labi_ondemand_list.x`：user_o + argv `**u8` 扫 .o；reuses pure needs_heap_user_syms / needs_std_heap_api + path_pure `ld_argv_entry_is_obj`；null argv 经 `*u8` 转型守卫）。**单叶**聚合迁入 L8b（wave144 freestanding 后；叶 needs 早已 pure，本波闭合 argv 循环仍 always mega）。mega 仅 forward decl。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 link_needs_* 聚合体仍 always mega C」。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave145.log` · `/tmp/mac_w145_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave145.log` · `/tmp/ubuntu_w145_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`83f67a222`** / docs | **R2 Cap residual pure wave144**：其它 rest R2 可迁叶 — freestanding user.o needs pure：`shux_freestanding_user_o_needs_{io,panic}` 真迁 pure orch（`labi_freestanding_list.x`：既有 io_sym ×13 + panic_sym 表 + pure 扫表；null/empty user_o → 0；Cap residual `undef_sym`）。**单叶**迁入 L7（wave143 async 后；表早已 pure，本波闭合 orch 仍 always mega）。mega 仅 forward decl。full-seed 体经 freestanding `#include`；hybrid `SHUX_LABI_FREESTANDING_LIST_FROM_X` → L7 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_io/panic 体仍 always mega C 扫表」。邻域：string/vec/slice/compress。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave144.log` · `/tmp/mac_w144_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave144.log` · `/tmp/ubuntu_w144_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`cb6727d13`** / docs | **R2 Cap residual pure wave143**：其它 rest R2 可迁叶 — C-path async scheduler needs pure：`shux_generated_c_needs_async_scheduler` 真迁 pure orch（`labi_freestanding_list.x`：async_scheduler ×9 string needles + pure 扫表；null/empty c_path → 0；Cap residual `contains_substr`）。**单叶**迁入 L7（wave142 stub0 后）。mega 仅 forward decl。full-seed 体经 freestanding `#include`；hybrid `SHUX_LABI_FREESTANDING_LIST_FROM_X` → L7 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_async_scheduler 体仍 always mega C 硬编码串」。与 wave130 user.o UNDEF 表分权威（C-path substr ≠ nm UNDEF）。邻域：string/vec/slice/compress + nest42。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave143.log` · `/tmp/mac_w143_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave143.log` · `/tmp/ubuntu_w143_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`4d6f495f`** / docs | **R2 Cap residual pure wave142**：其它 rest R2 可迁叶 — C-path core.builtin / core.mem needs pure stub0：`link_abi_generated_c_needs_{core_builtin,core_mem}` 真迁 pure orch（`labi_freestanding_list.x`：G-01 always 0；无 needle 表；不硬链 builtin.o / mem.o）。**单叶**迁入 L7（wave141 win32 后）。mega 仅 forward decl。full-seed 体经 freestanding `#include`；hybrid `SHUX_LABI_FREESTANDING_LIST_FROM_X` → L7 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_core_builtin/mem 体仍 always mega C stub0」。邻域：string/vec/slice/compress + nest42。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave142.log` · `/tmp/mac_w142_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave142.log` · `/tmp/ubuntu_w142_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`266fd49d`** / docs | **R2 Cap residual pure wave141**：其它 rest R2 可迁叶 — C-path Windows needs pure：`link_abi_generated_c_needs_{win32,win32_wsa}` 真迁 pure orch（`labi_freestanding_list.x`：win32 ×9 + win32_wsa ×3 string needles + pure 扫表；null/empty c_path → 0；Cap residual `contains_substr`）。**单叶**迁入 L7（wave139 provides / wave140 user_o provides 后）。mega 仅 forward decl。full-seed 体经 freestanding `#include`；hybrid `SHUX_LABI_FREESTANDING_LIST_FROM_X` → L7 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_win32{,_wsa} 体仍 always mega C 硬编码串」。邻域：string/vec/slice/compress + nest42。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave141.log` · `/tmp/mac_w141_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave141.log` · `/tmp/ubuntu_w141_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED orch / WINDOWS consumers · **日常 L2 非 tip L4** |
| 2026-07-22 | **`0a85bdfd`** / docs | **R2 Cap residual pure wave140**：其它 rest R2 可迁叶 — user.o co-emit provides pure：`link_abi_user_o_provides_{core_mem,std_heap}` 真迁 pure orch（`labi_ondemand_list.x`：core_mem ×2 + std_heap ×2 defined-sym 表 + pure 扫表；null/empty user_o → 0；Cap residual `has_defined_sym`）。**单叶**迁入 L8b（wave139 generated_c provides 后）。mega 仅 forward decl。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 user_o provides_* 体仍 always mega C 硬编码串」。邻域：string/vec/slice/compress + nest42。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave140.log` · `/tmp/mac_w140_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave140.log` · `/tmp/ubuntu_w140_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`88d85746`** / docs | **R2 Cap residual pure wave139**：其它 rest R2 可迁叶 — C-path co-emit provides pure：`link_abi_generated_c_provides_{core_mem,std_heap}` 真迁 pure orch（`labi_freestanding_list.x`：core_mem ×3 + std_heap ×3 definition-line needles + pure 扫表；null/empty c_path → 0；Cap residual `contains_substr`）。**单叶**迁入 L7（wave138 core_slice/db 后）。mega 仅 forward decl。full-seed 体经 freestanding `#include`；hybrid `SHUX_LABI_FREESTANDING_LIST_FROM_X` → L7 pure；surface re-pin prove **IDENTICAL**（export 58）。闭合 soft residual「hybrid 下 provides_* 体仍 always mega C 硬编码串」。邻域：string/vec/slice/compress build/run=0 + nest42。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave139.log` · `/tmp/mac_w139_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave139.log` · `/tmp/ubuntu_w139_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`1811f0ae0`** / docs | **R2 Cap residual pure wave138**：其它 rest R2 可迁叶 — C-path core/db needs pure：`link_abi_generated_c_needs_{core_slice,db_kv,db_arrow}` 真迁 pure orch（`labi_freestanding_list.x`：core_slice ×6 + db_kv ×7 + db_arrow ×3 needles + pure 扫表；null/empty c_path → 0；Cap residual `contains_substr`）。**单叶**迁入 L7（wave137 compress 后）。mega 仅 forward decl。full-seed 体经 freestanding `#include`；hybrid `SHUX_LABI_FREESTANDING_LIST_FROM_X` → L7 pure；surface re-pin prove **IDENTICAL**。依赖 path-safe nest @ `7d64368f1`（`/*.o` 不吞 export）。邻域：compress/slice build/run=0 + nest42。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave138.log` · `/tmp/mac_w138_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave138.log` · `/tmp/ubuntu_w138_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`7d64368f1`** / docs | **词法 · 嵌套块注释路径安全 nest-open（wave138 根因）**：嵌套语义本已工作；正文 `}/*.o` / `src/*.x` / 行首 `/*.ext` 被当成内层 `/*` → 单层 `*/` 关不完外层 → 静默吞后续 export。根修 `skip_whitespace_and_comments`：prev 路径/标识连续或 `/*` 后紧跟 `.` → 不 nest-open；真嵌套 `/**/` / `/* inner */` / dense 仍绿。权威 `lexer.x` + seed `lexer_gen.linux.x86_64.c` + empty_surface；docs 01/08；探针 `tests/lexer/nested_block_comment.x` + path/dot/dense。mac L2：g05 + 矩阵 + 探针（`/tmp/mac_g05_lexer_pathsafe2.log`）。Ubuntu L2：g05 + 矩阵 + 探针（`/tmp/ubuntu_g05_lexer_pathsafe.log` · `/tmp/ubuntu_w_lexer_pathsafe_probes.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`982f21f2c`** / docs | **R2 Cap residual pure wave137**：其它 rest R2 可迁叶 — C-path compress lib needs pure：`link_abi_generated_c_needs_{zlib,zstd,brotli}` 真迁 pure orch（`labi_freestanding_list.x`：zlib ×7 + zstd ×5 + brotli ×2 needles + pure 扫表；null/empty c_path → 0；Cap residual `contains_substr`）。**单叶**迁入 L7（wave136 OS/fs 后；本波 compress 表）。mega 仅 forward decl。full-seed 体经 freestanding `#include`；hybrid `SHUX_LABI_FREESTANDING_LIST_FROM_X` → L7 pure；surface re-pin prove **IDENTICAL**。邻域：compress/zstd/brotli/gzip roundtrip build/run=0。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave137.log` · `/tmp/mac_w137_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave137.log` · `/tmp/ubuntu_w137_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`ce180645f`** / docs | **R2 Cap residual pure wave136**：其它 rest R2 可迁叶 — C-path string needs pure：`link_abi_generated_c_needs_{fs,random,time,runtime}` 真迁 pure orch（`labi_freestanding_list.x`：fs ×5 + random ×3 + time ×10 + runtime ×3 needles + pure 扫表；null/empty c_path → 0；Cap residual `contains_substr`）。**单叶**迁入 L7（user_o needs 族 wave118–135 已闭；本波切 generated_c 表）。mega 仅 forward decl。full-seed 体经 freestanding `#include`；hybrid `SHUX_LABI_FREESTANDING_LIST_FROM_X` → L7 pure；surface re-pin prove **IDENTICAL**。邻域：fs/time/random build/run=0。mac L2：g05 + 矩阵 rv42/option102/si/hello + 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave136.log` · `/tmp/mac_w136_prove2.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave136.log` · `/tmp/ubuntu_w136_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`865e96113`** / docs | **R2 Cap residual pure wave135**：其它 rest R2 可迁叶 — fk==0 plan gate pure：`labi_std_fk0_user_needs_rel` 真迁 pure orch（`labi_ondemand_list.x`：16 rel path needles × 106 exact UNDEF 表 + pure 扫表；null/empty user_o → 1 legacy hard-link；unknown rel → 0；Cap residual `strstr` + `undef_sym`）。**单叶**整函数迁入（wave134 后容量 OK）。mega 仅 forward decl（was static）。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。邻域：string/vec/fs/sort + process build/run=0。mac L2：g05 + 矩阵 rv42/option102/si/hello + fk0 邻域 + prove IDENTICAL（`/tmp/mac_g05_wave135.log` · `/tmp/mac_w135_prove.log`）。Ubuntu L2：g05 + 矩阵 + 邻域 + prove IDENTICAL（`/tmp/ubuntu_g05_wave135.log` · `/tmp/ubuntu_w135_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`893b65e9a`** / docs | **R2 Cap residual pure wave134**：其它 rest R2 可迁叶 — TASK_SPECIAL bulk gate pure：`labi_user_needs_std_task` 真迁 pure orch（`labi_ondemand_list.x`：exact UNDEF ×29 + pure 扫表；null/empty user_o → 1 legacy hard-link；Cap residual `undef_sym`；不 probe `shux_async_task_submit*`）。**单叶**迁入（wave133 后 process_argv 已 pure，本叶再加 29 needles 容量 OK）。mega 仅 forward decl。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。邻域：`tests/process/main.x` build=0（`std-task/group_smoke.x` 仍 typeck T001 既有债，非本波）。mac L2：g05 + 矩阵 rv42/option102/si/hello + process + prove IDENTICAL（`/tmp/mac_g05_wave134.log` · `/tmp/mac_w134_prove.log`）。Ubuntu L2：g05 + 矩阵 + process + prove IDENTICAL（`/tmp/ubuntu_g05_wave134.log` · `/tmp/ubuntu_w134_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`96359fe63`** / docs | **R2 Cap residual pure wave133**：其它 rest R2 可迁叶 — PRIMARY process_argv bulk gate pure：`labi_user_needs_runtime_process_argv` 真迁 pure orch（`labi_ondemand_list.x`：exact UNDEF ×9 + pure 扫表；null/empty user_o → 1 legacy hard-link；Cap residual `undef_sym`）。**单叶**迁入（wave132 同迁 process_argv+std_task 会 codegen 截断 rel_*）。`std_task` **仍 mega static**。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。邻域：`tests/process/main.x` build=0。mac L2：g05 + 矩阵 rv42/option102/si/hello + process + prove IDENTICAL（`/tmp/mac_g05_wave133.log` · `/tmp/mac_w133_prove.log`）。Ubuntu L2：g05 + 矩阵 + process + prove IDENTICAL（`/tmp/ubuntu_g05_wave133.log` · `/tmp/ubuntu_w133_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`f932033fc`** / docs | **R2 Cap residual pure wave132**：其它 rest R2 可迁叶 — PRIMARY OS bulk gates pure：`labi_user_needs_runtime_{time_os,random_fill,env_os}` 真迁 pure orch（`labi_ondemand_list.x`：time_os exact ×10 + random_fill ×12 + env_os ×19 + pure 扫表；null/empty user_o → 1 legacy hard-link；Cap residual `undef_sym`）。`process_argv` / `std_task` **仍 mega static**（同模块再并会 codegen 截断 rel_*、hybrid 回退 cold）。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。邻域：env/time/random build/run=0。mac L2：g05 + 矩阵 rv42/option102/si/hello + env/time/random + prove IDENTICAL（`/tmp/mac_g05_wave132.log` · `/tmp/mac_w132_prove.log`）。Ubuntu L2：g05 + 矩阵 + env/time/random + prove IDENTICAL（`/tmp/ubuntu_g05_wave132.log` · `/tmp/ubuntu_w132_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`ddc854c04`** / docs | **R2 Cap residual pure wave131**：其它 rest R2 可迁叶 — compress family pure：`link_abi_obj_needs_{zlib,zstd,brotli}` + `link_abi_user_o_needs_compress_libs` 真迁 pure orch（`labi_ondemand_list.x`：zlib exact UNDEF ×4 + zstd prefix needles ×2 + brotli exact ×2 + 三 marker；pure 扫表 + Cap residual `exports_marker` / `has_undef_sym`）。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 compress needs_* 体仍 always mega C 硬编码串」。邻域：`tests/compress/main.x` build/run=0。mac L2：g05 + 矩阵 rv42/option102/si/hello + compress + prove IDENTICAL（`/tmp/mac_g05_wave131.log` · `/tmp/mac_w131_prove.log`）。Ubuntu L2：g05 + 矩阵 + compress + prove IDENTICAL（`/tmp/ubuntu_g05_wave131.log` · `/tmp/ubuntu_w131_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`620cd62d5`** / docs | **R2 Cap residual pure wave130**：其它 rest R2 可迁叶 — string needs pure：`link_abi_user_o_needs_async_scheduler` 真迁 pure orch（`labi_ondemand_list.x`：async scheduler exact UNDEF 表 ×35 + pure 扫表；product complete：coop/cps/frame/run/task/worker/io + async read/write complete surface；无前缀 probes；Cap residual `undef_sym`）。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_async_scheduler 体仍 always mega C 硬编码串」。mac L2：g05 + 矩阵 rv42/option102/si/hello + prove IDENTICAL（`/tmp/mac_g05_wave130.log` · `/tmp/mac_w130_prove.log`）。Ubuntu L2：g05 + 矩阵 + prove IDENTICAL（`/tmp/ubuntu_g05_wave130.log` · `/tmp/ubuntu_w130_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`e7a4d1e7b`** / docs | **R2 Cap residual pure wave129**：其它 rest R2 可迁叶 — string needs pure：`link_abi_user_o_needs_heap_user_syms` 真迁 pure orch（`labi_ondemand_list.x`：runtime_heap_user exact UNDEF 表 ×7 + pure 扫表；product complete：alloc/free/realloc/arena64_alloc + with_arena `heap_arena_init_c` / `heap_arena64_{init,deinit}_c`；G.7 闭合 residual mega.x 仅 4/7 漂移；无前缀 probes；Cap residual `undef_sym`）。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_heap_user_syms 体仍 always mega C 硬编码串」。mac L2：g05 + 矩阵 rv42/option102/si/hello + prove IDENTICAL（`/tmp/mac_g05_wave129.log` · `/tmp/mac_w129_prove.log`）。Ubuntu L2：g05 + 矩阵 + prove IDENTICAL（`/tmp/ubuntu_g05_wave129.log` · `/tmp/ubuntu_w129_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`d0072ca79`** / docs | **R2 Cap residual pure wave128**：其它 rest R2 可迁叶 — string needs pure：`link_abi_user_o_needs_std_heap_api` 真迁 pure orch（`labi_ondemand_list.x`：heap formal API exact UNDEF 表 ×25 + pure 扫表；Allocator/libc/arena 族；无前缀 probes；Cap residual `undef_sym`）。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_std_heap_api 体仍 always mega C 硬编码串」。mac L2：g05 + 矩阵 rv42/option102/si/hello + prove IDENTICAL（`/tmp/mac_g05_wave128.log` · `/tmp/mac_w128_prove.log`）。Ubuntu L2：g05 + 矩阵 + prove IDENTICAL（`/tmp/ubuntu_g05_wave128.log` · `/tmp/ubuntu_w128_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`b34fb153`** / docs | **R2 Cap residual pure wave127**：其它 rest R2 可迁叶 — string needs pure：`link_abi_user_o_needs_std_sys` 真迁 pure orch（`labi_ondemand_list.x`：sys facade exact UNDEF 表 ×8 + pure 扫表；无前缀 probes；Cap residual `undef_sym`）。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_std_sys 体仍 always mega C 硬编码串」。mac L2：g05 + 矩阵 rv42/option102/si/hello + prove IDENTICAL（`/tmp/mac_g05_wave127.log` · `/tmp/mac_w127_prove.log`）。Ubuntu L2：g05 + 矩阵 + prove IDENTICAL（`/tmp/ubuntu_g05_wave127.log` · `/tmp/ubuntu_w127_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`ab28c55a`** / docs | **R2 Cap residual pure wave125**：其它 rest R2 可迁叶 — string needs pure：`link_abi_user_o_needs_std_heap_page_mmap` 真迁 pure orch（`labi_ondemand_list.x`：page_mmap exact UNDEF 表 ×5 + pure 扫表；无前缀 probes；Cap residual `undef_sym`）。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_std_heap_page_mmap 体仍 always mega C 硬编码串」。mac L2：g05 + 矩阵 rv42/option102/si/hello + prove IDENTICAL（`/tmp/mac_g05_wave125.log` · `/tmp/mac_w125_prove.log`）。Ubuntu L2：g05 + 矩阵 + prove IDENTICAL（`/tmp/ubuntu_g05_wave125.log` · `/tmp/ubuntu_w125_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`9bee87a7`** / docs | **R2 Cap residual pure wave126**：其它 rest R2 可迁叶 — string needs pure：`link_abi_user_o_needs_std_sys_linux` 真迁 pure orch（`labi_ondemand_list.x`：sys_linux exact UNDEF 表 ×7 + pure 扫表；无前缀 probes；Cap residual `undef_sym`）。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_std_sys_linux 体仍 always mega C 硬编码串」。mac L2：g05 + 矩阵 rv42/option102/si/hello + prove IDENTICAL（`/tmp/mac_g05_wave126.log` · `/tmp/mac_w126_prove.log`）。Ubuntu L2：g05 + 矩阵 + prove IDENTICAL（`/tmp/ubuntu_g05_wave126.log` · `/tmp/ubuntu_w126_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`1bea63d4`** / docs | **R2 Cap residual pure wave124**：其它 rest R2 可迁叶 — string needs pure：`link_abi_user_o_needs_core_slice` 真迁 pure orch（`labi_ondemand_list.x`：core.slice exact UNDEF 表 ×6 + pure 扫表；无前缀 probes；Cap residual `undef_sym`）。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_core_slice 体仍 always mega C 硬编码串」。mac L2：g05 + 矩阵 rv42/option102/si/hello + prove IDENTICAL（`/tmp/mac_g05_wave124.log` · `/tmp/mac_w124_prove.log`）。Ubuntu L2：g05 + 矩阵 + prove IDENTICAL（`/tmp/ubuntu_g05_wave124.log` · `/tmp/ubuntu_w124_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`02a278db`** / docs | **R2 Cap residual pure wave123**：其它 rest R2 可迁叶 — string needs pure：`link_abi_user_o_needs_core_mem` 真迁 pure orch（`labi_ondemand_list.x`：core.mem exact UNDEF 表 ×7 + pure 扫表；无前缀 probes；Cap residual `undef_sym`）。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_core_mem 体仍 always mega C 硬编码串」。mac L2：g05 + 矩阵 rv42/option102/si/hello + prove IDENTICAL（`/tmp/mac_g05_wave123.log` · `/tmp/mac_w123_prove.log`）。Ubuntu L2：g05 + 矩阵 + prove IDENTICAL（`/tmp/ubuntu_g05_wave123.log` · `/tmp/ubuntu_w123_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`65d49067`** / docs | **R2 Cap residual pure wave122**：其它 rest R2 可迁叶 — string needs pure：`link_abi_user_o_needs_std_test` 真迁 pure orch（`labi_ondemand_list.x`：test UNDEF/prefix 表 ×7 + pure 扫表；前缀 `test_runner_` 等保留 Cap residual `strstr`；Cap residual `undef_sym`）。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_std_test 体仍 always mega C 硬编码串」。邻域：`tests/stdtest/main.x` + `tests/std-test/runner_smoke.x` build/run=0。常驻 Cap 仍：`ld_debug_argv` char** / host_lit #if / path_io stat / wait / strerror / mega spawn-IO。mac L2：g05 + 矩阵 rv42/option102/si/hello + queue + stdtest/runner + prove IDENTICAL（`/tmp/mac_g05_wave122.log` · `/tmp/mac_w122_prove.log`）。Ubuntu L2：g05 + 矩阵 + queue + stdtest/runner + prove IDENTICAL（`/tmp/ubuntu_g05_wave122.log` · `/tmp/ubuntu_w122_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`eed03582`** / docs | **R2 Cap residual pure wave121**：其它 rest R2 可迁叶 — string needs pure：`link_abi_user_o_needs_std_queue` 真迁 pure orch（`labi_ondemand_list.x`：product queue_api UNDEF 表 ×12 + pure 扫表；与 contention `labi_od_queue_sym_*` 分表；Cap residual `undef_sym`）。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_std_queue 体仍 always mega C 硬编码串」。邻域：`tests/queue/main.x` build/run=0。常驻 Cap 仍：`ld_debug_argv` char** / host_lit #if / path_io stat / wait / strerror / mega spawn-IO。mac L2：g05 + 矩阵 rv42/option102/si/hello + queue + prove IDENTICAL（`/tmp/mac_g05_wave121.log` · `/tmp/mac_w121_prove.log`）。Ubuntu L2：g05 + 矩阵 + queue + prove IDENTICAL（`/tmp/ubuntu_g05_wave121.log` · `/tmp/ubuntu_w121_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`7f271d10`** / docs | **R2 Cap residual pure wave120**：其它 rest R2 可迁叶 — string needs pure：`link_abi_user_o_needs_std_map` 真迁 pure orch（`labi_ondemand_list.x`：map UNDEF 表 ×9 empty_size+Map_i32+str + pure 扫表；Cap residual `undef_sym`）。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_std_map 体仍 always mega C 硬编码串」。邻域：`tests/map/main.x` build/run=0。常驻 Cap 仍：`ld_debug_argv` char** / host_lit #if / path_io stat / wait / strerror / mega spawn-IO。mac L2：g05 + 矩阵 rv42/option102/si/hello + map + prove IDENTICAL（`/tmp/mac_g05_wave120.log` · `/tmp/mac_w120_prove.log`）。Ubuntu L2：g05 + 矩阵 + map + prove IDENTICAL（`/tmp/ubuntu_g05_wave120.log` · `/tmp/ubuntu_w120_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`caa5e18d7`** / docs | **R2 Cap residual pure wave119**：其它 rest R2 可迁叶 — string needs pure：`link_abi_user_o_needs_std_set` 真迁 pure orch（`labi_ondemand_list.x`：set UNDEF 表 ×20 formal+legacy + pure 扫表；Cap residual `undef_sym`）。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_std_set 体仍 always mega C 硬编码串」。邻域：`tests/set/main.x` build/run=0。常驻 Cap 仍：`ld_debug_argv` char** / host_lit #if / path_io stat / wait / strerror / mega spawn-IO。mac L2：g05 + 矩阵 rv42/option102/si/hello + pure-asm option102/hello + set + prove IDENTICAL（`/tmp/mac_g05_wave119.log` · `/tmp/mac_w119_prove.log`）。Ubuntu L2：g05 + 矩阵 + set + prove IDENTICAL（`/tmp/ubuntu_g05_wave119.log` · `/tmp/ubuntu_w119_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`ffe2a563a`** / docs | **R2 Cap residual pure wave118**：其它 rest R2 可迁叶 — string needs pure：`link_abi_user_o_needs_std_net` 真迁 pure orch（`labi_ondemand_list.x`：net UNDEF 表 ×17 + pure 扫表；Cap residual `undef_sym`）。full-seed 体经 ondemand `#include`；hybrid `SHUX_LABI_ONDEMAND_LIST_FROM_X` → L8b pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_std_net 体仍 always mega C 硬编码串」。常驻 Cap 仍：`ld_debug_argv` char** / host_lit #if / path_io stat / wait / strerror / mega spawn-IO。mac L2：g05 + 矩阵 rv42/option102/si/hello + pure-asm option102/hello + prove IDENTICAL（`/tmp/mac_g05_wave118.log` · `/tmp/mac_w118_prove.log`）。Ubuntu L2：g05 + 矩阵 + pure-asm option102/hello + prove IDENTICAL（`/tmp/ubuntu_g05_wave118.log` · `/tmp/ubuntu_w118_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`f8d54c23d`** / docs | **R2 Cap residual pure wave117**：其它 rest R2 可迁叶 — string needs pure：`link_abi_generated_c_needs_libc_heap` / `link_abi_user_o_needs_libc_heap` / `link_abi_user_o_needs_freestanding_nostdlib_face` 真迁 pure orch（`labi_freestanding_list.x`：heap C needles ×9 + user.o UNDEF ×6 + memcpy-face ×3 + pure 扫表；Cap residual `contains_substr` + `undef_sym`）。full-seed 体经 freestanding `#include`；hybrid `SHUX_LABI_FREESTANDING_LIST_FROM_X` → L7 pure；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 needs_libc_heap / nostdlib_face 体仍 always mega C 硬编码串」。常驻 Cap 仍：`ld_debug_argv` char** / host_lit #if / path_io stat / wait / strerror / mega spawn-IO。mac L2：g05 + 矩阵 rv42/option102/si/hello + pure-asm option102/hello + prove IDENTICAL（`/tmp/mac_g05_wave117.log` · `/tmp/mac_w117_prove.log`）。Ubuntu L2：g05 + 矩阵 + pure-asm option102/hello + prove IDENTICAL（`/tmp/ubuntu_g05_wave117.log` · `/tmp/ubuntu_w117_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`d381f08ba`** / docs | **R2 Cap residual pure wave116**：其它 rest R2 可迁叶 — `shux_asm_ld_try_under_lib_roots` 真迁 pure orch（`labi_path_pure.x`：low-tag 守卫 + 纯字节 `root/rel` join（无 snprintf）+ 扫 24 根；count 9→10；Cap residual `asm_link_obj_skip_missing` + `bank_push`）。mega + cold seed twin `#ifndef SHUX_LABI_PATH_PURE_FROM_X`；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 try_under path-join 体仍 always mega C」。注意：多行 `export function` 签名会导致 parse 丢函数（须单行）。常驻 Cap 仍：`ld_debug_argv` char** / host_lit #if / path_io stat / wait / strerror / mega spawn-IO。mac L2：g05 + 矩阵 rv42/option102/si/hello + pure-asm option102/hello + prove IDENTICAL（`/tmp/mac_g05_wave116.log` · `/tmp/mac_w116_prove.log`）。Ubuntu L2：g05 + 矩阵 + pure-asm option102/hello + prove IDENTICAL（`/tmp/ubuntu_g05_wave116.log` · `/tmp/ubuntu_w116_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`f92096261`** / docs | **R2 Cap residual pure wave115**：其它 rest R2 可迁叶 — `shux_asm_ld_lib_root_default` 真迁 pure orch（`labi_path_pure.x`：默认 `"."` + pure `lib_root_ptr_usable` on `getenv("SHUX_LIB")` + 字节拷贝 ≡ mega；count 8→9；Cap residual `getenv` only）。mega + cold seed twin `#ifndef SHUX_LABI_PATH_PURE_FROM_X`；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 lib_root_default 体仍 always mega C」。常驻 Cap 仍：`ld_debug_argv` char** / host_lit #if / path_io stat / wait / strerror / mega spawn-IO。mac L2：g05 + 矩阵 rv42/option102/si/hello + pure-asm option102/hello + prove IDENTICAL（`/tmp/mac_g05_wave115.log` · `/tmp/mac_w115_prove.log`）。Ubuntu L2：g05 + 矩阵 + pure-asm option102/hello + prove IDENTICAL（`/tmp/ubuntu_g05_wave115.log` · `/tmp/ubuntu_w115_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`77c5636e`** / docs | **R2 Cap residual pure wave114**：其它 rest R2 可迁叶 — `shux_asm_ld_lib_root_ptr_usable` 真迁 pure（`labi_path_pure.x`：null + low-tag `<4096` + empty 守卫 ≡ mega；count 7→8）。mega + cold seed twin `#ifndef SHUX_LABI_PATH_PURE_FROM_X`；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 lib_root_ptr_usable 体仍 always mega C」（G-02f-115 批折叠未产品 pure）。常驻 Cap 仍：`ld_debug_argv` char** / host_lit #if / path_io stat / wait / strerror / mega spawn-IO。mac L2：g05 + 矩阵 rv42/option102/si/hello + pure-asm option102/hello + prove IDENTICAL（`/tmp/mac_g05_wave114.log` · `/tmp/mac_w114_prove.log`）。Ubuntu L2：g05 + 矩阵 + pure-asm option102/hello + prove IDENTICAL（`/tmp/ubuntu_g05_wave114.log` · `/tmp/ubuntu_w114_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`632a7d8cf`** / docs | **R2 Cap residual pure wave113**：其它 rest R2 可迁叶 — `link_diag_errno` / `link_diag_errno_path` 真迁 pure orch（`labi_diag_pure.x`：kind/op 默认 + `link_diag_code_for_kind` + `labi_diag_append` + `diag_report_with_code`；Cap residual 缩为 `link_diag_strerror_current` 持 errno+strerror 常驻 mega）。mega + cold seed twin `#ifndef SHUX_LABI_DIAG_PURE_FROM_X`；surface re-pin prove **IDENTICAL**；`shux_link_perror` 调 pure errno/_path。闭合 soft residual「hybrid 下 errno/_path 体仍全在 mega reportf」。常驻 Cap 仍：`ld_debug_argv` char** / host_lit #if / path_io stat / wait / strerror Cap。mac L2：g05 + 矩阵 rv42/option102/si/hello + pure-asm option102/hello + prove IDENTICAL（`/tmp/mac_g05_wave113.log` · `/tmp/mac_w113_prove.log`）。Ubuntu L2：g05 + 矩阵 + pure-asm option102/hello + prove IDENTICAL（`/tmp/ubuntu_g05_wave113.log` · `/tmp/ubuntu_w113_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`83deafda3`** / docs | **R2 Cap residual pure wave112**：其它 rest R2 可迁叶 — `link_diag_tool_status` / `link_diag_runtime_obj_build_status` 真迁 pure orch（`labi_diag_pure.x`：`labi_diag_append` + pure `labi_diag_append_i32` + `diag_report_with_code`；Cap residual `link_diag_wait_is_signaled` / `link_diag_wait_code` 持 WIF* 常驻 mega）。mega + cold seed twin `#ifndef SHUX_LABI_DIAG_PURE_FROM_X`；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 tool/obj status 体仍全在 mega reportf」。常驻 Cap 仍：`ld_debug_argv` char** / host_lit #if / path_io stat / wait / errno。mac L2：g05 + 矩阵 rv42/option102/si/hello + pure-asm option102/hello + prove IDENTICAL（`/tmp/mac_g05_wave112.log` · `/tmp/mac_w112_prove.log`）。Ubuntu L2：g05 + 矩阵 + pure-asm option102/hello + prove IDENTICAL（`/tmp/ubuntu_g05_wave112.log` · `/tmp/ubuntu_w112_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`036434e74`** / docs | **R2 Cap residual pure wave111**：其它 rest R2 可迁叶 — `shux_link_perror` 真迁 pure orch（`labi_diag_pure.x`：可选 `shux: ` 前缀 + 末段 `op (path)` 拆分 + 栈 op/path；Cap residual `link_diag_errno` / `_path` 持 errno+strerror+reportf 常驻 mega）。mega + cold seed twin `#ifndef SHUX_LABI_DIAG_PURE_FROM_X`；surface re-pin prove **IDENTICAL**。闭合 soft residual「hybrid 下 perror 体仍全在 mega C」。常驻 Cap 仍：`ld_debug_argv` char** / host_lit #if / path_io stat。mac L2：g05 + 矩阵 rv42/option102/si/hello + pure-asm option102 + prove IDENTICAL（`/tmp/mac_g05_wave111.log` · `/tmp/mac_w111_prove3.log`）。Ubuntu L2：g05 + 矩阵 + pure-asm option102/hello + prove IDENTICAL（`/tmp/ubuntu_g05_wave111.log` · `/tmp/ubuntu_w111_prove.log`）。钉盘仍 **`3c88f7e51`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`3c88f7e51` tip L4 升钉** | **真冷全测 L4**：wave110 ImportEntry pure 结构债整波收口。双端 `remaining_o=0` → bootstrap-driver-seed → g05 → §4 矩阵 rv42/option102/si/hello + pure-asm option102/hello → `SHUX_BSTRICT_SKIP_BUILD=1` **129** 全绿。mac `/tmp/mac_true_cold_3c88f7e51.log` · `/tmp/mac_true_bstrict_3c88f7e51.log`；Ubuntu 金标 `/tmp/ubuntu_true_cold_3c88f7e51.log` · `/tmp/ubuntu_true_bstrict_3c88f7e51.log`。钉盘 **`df1dde5b6` → `3c88f7e51`**。PLATFORM: SHARED · **构建级别=L4** |
| 2026-07-22 | **`814f0f89`** / docs | **R2 Cap residual pure wave110 ImportEntry 结构债**：pure 多模块 malloc 表（128 slot）+ 全套 `pipeline_module_import_*`（alloc/path/kind/binding/select）+ `pipeline_module_import_storage_release`；C `ImportEntry` 340B LE 布局；header `num_imports` off=8 soft-reset；`ast_pool` 同名 **SHUX_WEAK** 冷 twin + `ast_pool_module_release` 钩 pure free。闭合硬 Cap residual「ImportEntry 存储仍在 ast_pool GrowVec」。mac L2：hybrid pure **T** + 矩阵 rv42/option102/si/hello + pure-asm option102（`/tmp/mac_g05_wave110c.log`）。Ubuntu L2：hybrid pure（final alloc size 0x10e≡pure）+ 矩阵 + pure-asm option/hello（`/tmp/ubuntu_g05_wave110.log`）。钉盘仍 **`df1dde5b6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`df1dde5b6` tip L4 升钉** | **真冷全测 L4**：wave102–109 CG002/pure-asm expand 整波收口（arm64 enc + macho_write + Darwin formal net.o / METHOD_CALL / deferred let / frame spill / arm64 string / GP spill-preserve mov）。ImportEntry 仍结构债暂缓。双端 `remaining_o=0` → bootstrap-driver-seed → g05 → §4 矩阵 + pure-asm option=102/hello → `SHUX_BSTRICT_SKIP_BUILD=1` **129** 全绿。mac `/tmp/mac_true_cold_df1dde5b6.log` · `/tmp/mac_true_bstrict_df1dde5b6.log`；Ubuntu 金标 `/tmp/ubuntu_true_cold_df1dde5b6.log` · `/tmp/ubuntu_true_bstrict_df1dde5b6.log`。钉盘 **`07684b813` → `df1dde5b6`**。PLATFORM: SHARED · **构建级别=L4** |
| 2026-07-22 | **`e1cff0c6`** / docs | **R2 CG002 wave109 arm64 GP spill/preserve mov**：根因 Darwin pure-asm option `let extra = m1+(m2 as i32)+…+(bp[0] as i32)` 左结合 ADD 后装 INDEX/AS 右操作数 → `glue_binop_preserve_rax_for_rbx_load` → `arch_arm64_enc_enc_mov_rax_to_x9` 仍为 seed_link_compat **weak stub return -1**（非 macho patch）。G.7：`backend_arm64_enc_c.from_x.c` 补全 ORR-as-MOV **x2/x9/x10–x15** 强符号（INDEX preserve + 7.3 spill）。mac L2：pure-asm **option -c/-o=102** · hello/rv pure-asm -c · c 矩阵。Ubuntu：x86 路径未改。钉盘仍 **`07684b813`**。PLATFORM: MACOS arm64 pure-asm · **日常 L2 非 tip L4** |
| 2026-07-22 | **`c79c01d32`** / docs | **R2 CG002 wave108 arm64 string-lit embed**：根因 `glue_asm_emit_jmp_skip_string_then_lea` / fmt string-lit import call 门控 `ta!=0` → Darwin pure-asm hello CG002。G.7：seed `_impl` + `.x` 补全 aarch64 **B + 4 对齐尾挂串 + ADR x0**；fmt 路径 len 用 `backend_enc_mov_imm32_to_rbx_arch`（w1）避免冲 x0。mac L2：g05 + 矩阵 rv42/option102/hello · pure-asm **hello -c** / rv42（option pure-asm 仍 CG002 residual）。Ubuntu：x86 路径未改，pull 后 g05 回归。钉盘仍 **`07684b813`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`0f2e810b1`** / docs | **R2 CG002 wave107 pure-asm compute_frame_size call-spill headroom**：根因 SysV `glue_sysv_spill_rax_rdx_to_frame_c` 每寄存器实参永久 `next_offset+=32`；`pipeline_asm_compute_frame_size_c` 仅历史 +512 → full option 帧 `0x360` ≪ max off `0xa80` → 栈写穿错返 -4。G.7：`glue_asm_sum_block_call_spill_bytes` AST 累加 CALL/METHOD_CALL spill；`scratch=max(512,call_spill)` 补全权威。Ubuntu L2：pure-asm **option=102** · frame `0x1280`≥max off `0x1058` · hello pure-asm · c rv42/option/si。mac L2：矩阵 + pure-asm option102。钉盘仍 **`07684b813`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`83c09e1bc`** / docs | **R2 CG002 wave106 pure-asm pass1 deferred let leaf**：根因两遍 emit pass0 把「纯」let 提前，但 `let extra = a + buf[0]` 等依赖 CALL 初值的 binop 也被 hoist → 入口读未初始化 `bp`/`a` → Ubuntu pure-asm option **SEGV**。G.7：`glue_block_compute_pass1_deferred_lets` seed INDEX/CALL/METHOD_CALL + **传递闭包**（init 用 deferred 槽则 defer）；pass0 skip deferred、pass1 原位 emit。Ubuntu L2：ord3=11 · noptr2=101 · pure-asm **hello** · c-backend option102；full option 仍 residual（frame `0x360` vs max off `0xa80` → 错返 -4）。mac L2：pure-asm ord3=11 · option run102 · 矩阵。钉盘仍 **`07684b813`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`4918983cf`** / docs | **R2 CG002 wave105 pure-asm hello METHOD_CALL leaf**：根因产品解析 `fmt.println("…")` 为 **METHOD_CALL (49)** 非 CALL+FIELD_ACCESS；METHOD_CALL import-binding 无 fmt 字符串 lit 特化 → 误 mangle `std_fmt_println_i32_reti32` → Ubuntu pure-asm BLD001 UNDEF。G.7：`glue_asm_try_emit_fmt_string_lit_import_call_elf_c` 接受 CALL/METHOD_CALL 实参表；method_call 路径先特化再 mangle；seed + `.x` 同 commit。Ubuntu L2：pure-asm **hello build/run** · U `std_fmt_println` · rv42/si · c-backend 矩阵（`/tmp/ubuntu_wave105_g05.log` · `/tmp/ubuntu_w105_hello.log`）；option pure-asm **SEGV residual**。mac L2：c-backend 矩阵 + pure-asm rv42；hello pure-asm **`.o` CG002**（arm64 ta≠0 string embed residual）。钉盘仍 **`07684b813`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`07684b813` tip L4 升钉** | **真冷全测 L4**：wave102–104 CG002 整波收口（arm64 enc + macho_write + Darwin formal net.o C path / `net_connect_many_c` no_mangle）。ImportEntry 仍结构债暂缓。双端 `remaining_o=0` → bootstrap-driver-seed → g05 → §4 矩阵 → `SHUX_BSTRICT_SKIP_BUILD=1` **129** 全绿。mac `/tmp/mac_true_cold_b5967f1b5.log` · `/tmp/mac_true_bstrict_07684b813.log`；Ubuntu 金标 `/tmp/ubuntu_true_cold_07684b81.log` · `/tmp/ubuntu_true_bstrict_07684b81.log`。钉盘 **`ed2dc1a1e` → `07684b813`**。PLATFORM: SHARED · **构建级别=L4** |
| 2026-07-22 | **`07684b813`** | **R2 CG002 wave104**：根因 tip L4 首跑 mac STD-092 net-context BLD001 — wave102 后 Darwin `-backend asm` 对 `std/net/{alpn,tcp,…}` 成功（pre-wave102 `code_len=0` 回退 C）；`ld -r` 合并 C mod.o 后 tls_stub/tcp_pool/io UNDEF 在产品 `-dead_strip` 下 live。G.7：Makefile Darwin net 子模块强制 `./shux-c`（Linux 仍 asm）；`net_connect_many_c` 双端 `#[no_mangle]` ≡ `net_accept_many_c` / mod.x extern。mac 重建 net.o + net/io-context 门禁绿 → 全量 bstrict 129。PLATFORM: MACOS formal net.o / SHARED no_mangle |
| 2026-07-22 | **wave103** / docs | **R2 CG002 Darwin pure-asm · macho_write 强符号叶**：根因 wave102 后 mega_body 绿仍 `macho_write=-1`（`asm_experimental_symbol_bridge` / full_link stubs **SHUX_WEAK** `platform_macho_write_macho_o_to_buf` return -1；真 `macho.x` 未入产品链）。G.7 在 `ast_pool.c` 新增 `pipeline_macho_write_o_to_buf_c`（端口 `macho.x::write_macho_o_to_buf`，与 `pipeline_elf_write_o_standard_to_buf_c` 同 `PipelineElfCtxAccess` + glue code_data 偏移）+ 强符号 `platform_macho_write_macho_o_to_buf`；`pipeline_glue.c` 前置声明。重建 `pipeline_glue_standalone.o` + g05。mac L2：pure-asm **macho_write=303** · Mach-O arm64 · **rv build/run=42**；§4 矩阵 rv42/option/si/hello（`/tmp/mac_wave103_g05.log` · `/tmp/mac_wave103_pureasm.log` · `/tmp/mac_wave103_matrix.log`）。钉盘仍 **`ed2dc1a1e`**。PLATFORM: MACOS MH_OBJECT + SHARED glue 面 · **日常 L2 非 tip L4** |
| 2026-07-22 | **wave102** / docs | **R2 CG002 Darwin pure-asm · arm64 enc 强符号叶**：根因产品 g05 链仅 `seed_link_compat` **SHUX_WEAK** `arch_arm64_enc_*`（体 `return -1`）→ `mega_body_c enc_label fail` / `code_len=0`；Ubuntu x86 有 `backend_x86_64_enc_c` 强符号故 pure-asm 绿。G.7 新增 `seeds/backend_arm64_enc_c.from_x.c`（端口 `arm64_enc.x` via `pipeline_elf_ctx_*`）+ Makefile/`g05_relink_env` 入链 `src/asm/backend_arm64_enc_c.o`（Darwin filtered + Linux USER_ASM；与 enc_dispatch 已有 call/sp 符号去重）。mac L2：g05 + §4 矩阵 rv42/option/si/hello；pure-asm 探针 **mega_body rc=0 · code_len=32 · num_labels=2**，仍 **CG002** 于 `macho_write=-1`（`asm_experimental_symbol_bridge` weak 桩；真 `macho.x` 未入产品链）。钉盘仍 **`ed2dc1a1e`**。PLATFORM: MACOS arm64 enc + SHARED 矩阵 · **日常 L2 非 tip L4** |
| 2026-07-22 | **`ed2dc1a1e` tip L4 升钉** | **真冷全测 L4**：wave82–101 Cap residual pure 整波收口（含 wave101 fn-as-value mangle soft）。决策：ImportEntry 为 ast_pool GrowVec **结构债**（非固定 BSS 微步 pure）→ 可迁 soft residual 已闭则整波主闸门 L4。双端 `remaining_o=0` → bootstrap-driver-seed → g05 → §4 矩阵（rv42 / option / si / hello）→ `SHUX_BSTRICT_SKIP_BUILD=1` **129** 全绿。mac `/tmp/mac_true_cold_ed2dc1a1e.log` · `/tmp/mac_true_bstrict_ed2dc1a1e.log`；Ubuntu 金标 `/tmp/ubuntu_true_cold_ed2dc1a1.log` · `/tmp/ubuntu_true_bstrict_ed2dc1a1.log`。钉盘 **`1f73ed275` → `ed2dc1a1e`**（产品 `fada2499` 含于钉盘）。PLATFORM: SHARED · **构建级别=L4** |
| 2026-07-22 | **`fada2499`** / docs | **R2 Cap residual pure wave101**：codegen soft residual — EXPR_VAR 同模块自由函数值（含 `(f as *u8)`）走 G.7 `codegen_try_emit_fn_as_value`（模块前缀 + `codegen_emit_func_link_name` ≡ def/call/extern）；`codegen_name_is_local_binding` 挡 param/let/const 误 mangle。闭合 soft residual「非 no_mangle fn-as-value 发裸源名」。Cap residual 仍 ImportEntry 存储（ast_pool）。seed pin + codegen.x 同 commit。mac L2：探针 build/run=0 + 矩阵 rv42/option/si/hello + force_c + shadow→9（`/tmp/mac_g05_wave101.log`）。Ubuntu L2：`-E` 同名 `fn_as_probe_helper_inner` + 矩阵 + force_c（`/tmp/ubuntu_g05_wave101b.log`；pure-asm 探针 CG002 tip 既有）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`035356e1`** / docs | **R2 pipeline_abi Cap residual pure wave100**：语言 residual — typeck 同模块 bare 函数名 → Cap-fn-ptr `*u8`（CALL 仍按名解析，不受影响）；pure `pipeline_run_x_thread_fn_ptr` / `shux_asm_codegen_elf_o_thread_fn_ptr` 体为 `(fn as *u8)`，无 g05 `shux_driver_*_thread_fn_ptr` U；seed typeck pin + pipeline cold twin 同 commit。闭合硬 Cap residual「g05 &fn cast 产品 pure 面」。soft：非 `#[no_mangle]` fn-as-value 仍发源名（codegen mangle residual）；ImportEntry 存储仍 Cap（ast_pool）。mac L2：pure fn_ptr adrp 体 + 矩阵 rv42/option/si/hello + nest→11 + force_c（`/tmp/mac_g05_wave100.log` · `/tmp/mac_g05_wave100_matrix.log`）。Ubuntu L2：pure **T** `return ((uint8_t *)(pipeline_run_x_thread_fn))` + 矩阵 + linux nest→11 + force_c（`/tmp/ubuntu_g05_wave100.log` · `/tmp/ubuntu_g05_wave100_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`bc3d2859`** / docs | **R2 pipeline_abi Cap residual pure wave99**：`parser_copy_module_import_path64` 真迁 pure thin（G.7 `pipeline_module_import_path_copy` + NUL 扫描 len ≡ 历史 parser_gen；parser_gen path64 demote **weak** cold twin；strict_glue 空 stub → 同语义 cold twin）。闭合硬 Cap residual「path64 under pure load_and_sync / load_import」叶。ImportEntry 存储仍 Cap residual（ast_pool）。Cap residual 仍 g05 &fn cast（语言）。mac L2：pure path64 bl path_copy + 矩阵 + nest→11 + force_c（`/tmp/mac_g05_wave99.log` · `/tmp/mac_g05_wave99_matrix.log`）。Ubuntu L2：pure path64 **W** call path_copy + 矩阵 rv42/option/si/hello + linux nest→11 + force_c（`/tmp/ubuntu_g05_wave99.log` · `/tmp/ubuntu_g05_wave99_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`ac475548`** / `40a908f5` / docs | **R2 pipeline_abi Cap residual pure wave98**：产品 complex #if 根修 — 闭 bootstrap stub 双权威。根因：`cfg_eval.x` 多 `#[cfg]` 同名 host lit 在 `-E-extern` 双重定义 → Makefile 静默 stub pin；pure 调 `cfg_eval_expr_c` 体是 C stub。根修：host lit → link_alias/host_lit C `#if`；eval 权威 cfg_eval.x；`PIPELINE_GEN_CFLAGS`+Clang `-Wno-parentheses-equality`；Ubuntu pin `seeds/cfg_eval_gen.linux.x86_64.c`（禁 plain `-E` 悬空 BSS SEGV）。闭合硬 Cap residual「产品 complex #if 体仍是 bootstrap stub」叶。Cap residual 仍 g05 &fn cast / `parser_copy_module_import_path64`。mac L2：`-E-extern+alias` + 矩阵 + complex macos→11（`/tmp/mac_g05_wave98.log`）。Ubuntu L2：linux pin gen + alias（`lexer_cfg_eval_expr_c` T）+ 矩阵 + complex linux→11（`/tmp/ubuntu_g05_wave98.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`626d430e`** / docs | **R2 pipeline_abi Cap residual pure wave97**：pure `pipeline_load_and_sync_direct_import_deps_c` step5 真迁 G.7 typeck.x 调用面（`typeck_merge_dep_struct_layouts_into_entry` / `typeck_wpo_unify_soa_layouts`；删 pure 上 `typeck_typeck_*` double-prefix hop；link-alias 仍服务其它 TU；cold twin 可保留 hop）。闭合硬 Cap residual「typeck merge+wpo 调用面 under pure load_and_sync」叶。Cap residual 仍 cfg_eval 复杂 / g05 &fn cast / `parser_copy_module_import_path64`。mac L2：hybrid pure merge U→typeck + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave97.log` · `/tmp/mac_g05_wave97_matrix.log`）。Ubuntu L2：hybrid pure **W**（load_and_sync size≡pure 0x734；U→typeck_merge/wpo）+ 矩阵 rv42/option/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave97.log` · `/tmp/ubuntu_g05_wave97_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`ff79c20a`** / docs | **R2 pipeline_abi Cap residual pure wave96**：`pipeline_parse_into_buf` 真迁 pure orch（G.7 pure `parser_parse_into_init` + `driver_parse_into_buf_rc` Cap-struct ok unpack + same-TU pure debug_trace/fixup on ok==0；`buf_len<=0` → -1；非 0 ok → -1；异于 wave64 `parse_into_bytes`）；glue 同名 **SHUX_WEAK** 冷 twin（改后须重建 `pipeline_x.o` + `pipeline_glue_standalone.o`）。闭合硬 Cap residual「parse under pure load_import」叶。Cap residual 仍 typeck merge 调用面 / cfg_eval 复杂 / g05 &fn cast。mac L2：hybrid pure parse_into_buf + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave96.log` · `/tmp/mac_g05_wave96_matrix.log`）。Ubuntu L2：hybrid pure **W**（parse_into_buf size≡pure 0xec）+ 矩阵 rv42/option/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave96.log` · `/tmp/ubuntu_g05_wave96_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`b498928e`** / docs | **R2 pipeline_abi Cap residual pure wave95**：`pipeline_resolve_path_x` / `pipeline_read_file_x` / `pipeline_preprocess_loaded_into_ctx` 真迁 pure orch（G.7 Cap residual try_one_lib_root / try_entry_dir / loop_should_continue；pure `shux_read_file_into_path` + set_loaded_len；pure `preprocess_x_buf` + preprocess_len LE store）；glue/ast_pool 同名 **SHUX_WEAK** 冷 twin（改后须重建 `pipeline_x.o` + `pipeline_glue_standalone.o`）。闭合硬 Cap residual「resolve/read/pp under pure load_import」叶。Cap residual 仍 parse_into_buf / typeck merge 调用面 / cfg_eval 复杂 / g05 &fn cast。mac L2：hybrid pure resolve/read/pp size≡pure + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave95.log` · `/tmp/mac_g05_wave95_matrix.log`）。Ubuntu L2：hybrid pure **W**（resolve 217 / read 191 / pp 222 size≡pure）+ 矩阵 rv42/option/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave95.log` · `/tmp/ubuntu_g05_wave95_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`30d64a37`** / docs | **R2 pipeline_abi Cap residual pure wave94**：`pipeline_load_import_from_disk_c` / `pipeline_sync_dep_slots_from_driver_c` 真迁 pure orch（G.7 same-TU pure `pipeline_bind_import_dep_buffers` + `pipeline_sync_one_dep_slot`；Cap residual `pipeline_resolve_path_x` / `pipeline_read_file_x` / `pipeline_preprocess_loaded_into_ctx` / `pipeline_parse_into_buf`）；glue/ast_pool 同名 **SHUX_WEAK** 冷 twin（改后须重建 `pipeline_x.o` + `pipeline_glue_standalone.o`）。闭合硬 Cap residual「disk load + dep-sync under pure load_and_sync」叶。Cap residual 仍 typeck merge+wpo / resolve-read-pp-parse 子叶 / cfg_eval 复杂 / g05 &fn cast。mac L2：hybrid pure load_import/sync/bind/sync_one size≡pure + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave94.log` · `/tmp/mac_g05_wave94_matrix.log`）。Ubuntu L2：hybrid pure **W**（load_import/sync/bind/sync_one size≡pure）+ 矩阵 rv42/option/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave94.log` · `/tmp/ubuntu_g05_wave94_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`d327590e`** / docs | **R2 pipeline_abi Cap residual pure wave93**：`pipeline_load_and_sync_direct_import_deps_c` 真迁 pure orch（G.7 same-TU pure `pipeline_try_bind_seeded_import` + `pipeline_dep_ctx_realign_ndep_for_entry_c`；Cap residual `load_import_from_disk_c` / `sync_dep_slots_from_driver_c` / typeck merge+wpo / `parser_copy_module_import_path64`）；`ast_pool` 同名 **SHUX_WEAK** 冷 twin（改后须重建 `pipeline_x.o` + `pipeline_glue_standalone.o`）。闭合硬 Cap residual「load_and_sync under pure dep-prerun typeck_only」叶。Cap residual 仍 disk load/sync/merge 子叶 / cfg_eval 复杂 / g05 &fn cast。mac L2：hybrid pure load_and_sync size≡pure + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave93b.log` · `/tmp/mac_g05_wave93_matrix.log`）。Ubuntu L2：hybrid pure **W**（load_and_sync/try_bind/realign size≡pure）+ 矩阵 rv42/option/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave93.log` · `/tmp/ubuntu_g05_wave93_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`f1e2be96`** / docs | **R2 pipeline_abi Cap residual pure wave92**：`pipeline_typeck_validate_struct_layouts_zero_padding_c` / `pipeline_typeck_patch_all_body_parent_links_c` 真迁 pure thin → G.7 typeck.x（`typeck_validate_struct_layouts_zero_padding` / `typeck_patch_all_body_parent_links`）；`pipeline_glue` 同名 **SHUX_WEAK** 冷 twin（glue metrics / inline parent walk）。闭合 Cap residual「layout validate+patch helpers under pure dep-prerun」叶。Cap residual 仍 load_and_sync / cfg_eval 复杂 / g05 &fn cast。mac L2：hybrid pure validate/patch + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave92.log` · `/tmp/mac_g05_wave92_matrix.log`）。Ubuntu L2：hybrid pure **W**（validate/patch_c U→typeck）+ 矩阵 rv42/option/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave92.log` · `/tmp/ubuntu_g05_wave92_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`455a9457`** / docs | **R2 pipeline_abi Cap residual pure wave91**：`pipeline_typeck_set_dep_ctx` / `get_dep_ctx` 真迁 pure LP64 BSS（G.7 `g_pipe_typeck_dep_ctx` u8[8] + `shux_ptr_slot_*`；删 ast_pool static `g_typeck_dep_ctx` 双权威；enum variant fallback 只读 get）；`pipeline_glue` 同名 **SHUX_WEAK** 冷 twin。闭合 Cap residual「set_dep_ctx」叶。Cap residual 仍 load_and_sync / cfg_eval 复杂 / g05 &fn cast / layout helpers。mac L2：hybrid pure set/get_dep_ctx + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave91.log` · `/tmp/mac_g05_wave91_matrix.log`）。Ubuntu L2：hybrid pure **W**（set/get_dep_ctx）+ 矩阵 rv42/option/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave91.log` · `/tmp/ubuntu_g05_wave91_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`ce96dc47`** / docs | **R2 pipeline_abi Cap residual pure wave90**：`pipeline_typeck_diag_soft_suppress_set` / `_get` 真迁 pure BSS（G.7 `g_pipe_typeck_diag_soft_suppress`；wave89 pure dep-prerun same-TU set；diagnostic get 共享 pure flag）；`pipeline_glue` 同名 **SHUX_WEAK** 冷 twin。闭合 Cap residual「soft-suppress」叶。Cap residual 仍 load_and_sync / cfg_eval 复杂 / g05 &fn cast / set_dep_ctx / layout helpers。mac L2：hybrid pure soft_suppress **T** + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave90.log` · `/tmp/mac_g05_wave90_matrix.log`）。Ubuntu L2：hybrid pure **W**（soft_suppress）+ 矩阵 rv42/option/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave90.log` · `/tmp/ubuntu_g05_wave90_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`293bc6f9a`** / docs | **R2 pipeline_abi Cap residual pure wave89**：`pipeline_typeck_dep_prerun_module_c` 真迁 pure orch（G.7 set_dep_ctx + soft_suppress + `typeck_x_ast_library` + Cap residual `validate_struct_layouts_zero_padding_c` / `patch_all_body_parent_links_c`；SHUX_DEBUG_PIPE 注 cold-only）；`pipeline_glue` 同名 **SHUX_WEAK** 冷 twin；strict_stubs weak -1 注释对齐。闭合 Cap residual「typeck dep-prerun」叶（wave60 typeck_only 调同 TU pure）。Cap residual 仍 load_and_sync / cfg_eval 复杂 / g05 &fn cast。mac L2：hybrid pure dep_prerun + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave89.log` · `/tmp/mac_g05_wave89_matrix.log`）。Ubuntu L2：hybrid pure **W**（dep_prerun）+ 矩阵 rv42/option102/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave89.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`10905a275`** / docs | **R2 pipeline_abi Cap residual pure wave88**：`preprocess_eval_condition_c` 真迁 pure orch（trim 首尾空白 + 简单标识 → 同 TU pure `preprocess_define_has`；含空格/`=`/`!`/`(`/`)` 复杂条件 → Cap residual `cfg_eval_expr_c` under unsafe）；glue `runtime_driver_strict_glue_stubs` 同名 **SHUX_WEAK** 冷 fallback。闭合 Cap residual「preprocess #if condition-eval」叶。Cap residual 仍 cfg_eval 复杂分支 / g05 &fn cast（`preprocess_x_buf` 已 pure preprocess.x cross-TU）。mac L2：hybrid pure eval + 矩阵 + nest #if/-D（`/tmp/mac_g05_wave88.log`）。Ubuntu L2：hybrid pure **W**（eval）+ 矩阵 rv42 + nest #if/-D（`/tmp/ubuntu_g05_wave88.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`a395783e`** / docs | **R2 pipeline_abi Cap residual pure wave87**：`pipeline_typeck_module_for_ctx` / `_impl` 真迁路由 → G.7 单权威 `typeck_x_ast` / `typeck_x_ast_library`（`pipeline_module_main_func_index` 选 entry/library ≡ `pipeline_impl_typecheck`）；`typeck_module_entry_only` / `with_sidecar` fail-closed（X 需 arena+ctx）。闭合 Cap residual「typeck_module C 前端」叶（已删 C 前端曾仅 weak -1）。Cap residual 仍 preprocess_x_buf（preprocess.x 引擎已 pure，pipeline orch export-extern）/ g05 &fn cast。mac L2：hybrid pure for_ctx + 矩阵 + force_c rv42（`/tmp/mac_g05_wave87.log`）。Ubuntu L2：hybrid pure **W**（for_ctx）+ 矩阵 rv42 + force_c（`/tmp/ubuntu_g05_wave87.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`128092729`** / docs | **R2 pipeline_abi Cap residual pure wave86**：`preprocess_if_stack_reset` / `len` / `push` / `pop` / `at` / `set_at` 真迁 pure 固定 `i32[32]` BSS（G.7 单权威 `g_pipe_pp_if_stack` + `g_pipe_pp_if_n`；`ast_pool` GrowVec 同名 **SHUX_WEAK** 冷 fallback）。闭合 Cap residual「preprocess #if stack」叶。Cap residual 仍 typeck_module C 前端 / preprocess_x_buf。mac L2：PREFER hybrid pure if_stack + 矩阵 + nest #if（`/tmp/mac_g05_wave86.log`）。Ubuntu L2：hybrid pure **W**（if_stack_*）+ 矩阵 rv42 + nest #if（`/tmp/ubuntu_g05_wave86.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`3b9f9f435`** / docs | **R2 pipeline_abi Cap residual pure wave85**：`preprocess_define_reset` / `preprocess_define_add` / `preprocess_define_has` 真迁 pure BSS（G.7 单权威 `g_pipe_pp_defines` 128×64 + `g_pipe_pp_ndefines`；glue `runtime_driver_strict_glue_stubs` 同名 **SHUX_WEAK** 冷 fallback）。闭合 Cap residual「preprocess -D define table」叶。Cap residual 仍 typeck_module C 前端 / preprocess_x_buf / if_stack。mac L2：PREFER hybrid pure **T** + 矩阵（`/tmp/mac_g05_wave85.log`）。Ubuntu L2：hybrid pure **W**（define_*）+ 矩阵 rv42（`/tmp/ubuntu_g05_wave85.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`458a1efa`** / **`e7d7c569`** / docs | **R2 pipeline_abi Cap residual pure wave84**：`pipeline_run_x_thread_fn_ptr` / `shux_asm_codegen_elf_o_thread_fn_ptr` 真迁 pure thin（G.7 g05 `shux_driver_pipeline_run_x_thread_fn_ptr` / `shux_driver_asm_elf_o_thread_fn_ptr` 持 &fn cast residual；`.x` 无法形成函数指针常量；seed 冷孪生 `#ifndef FROM_X`）。闭合 Cap residual「always-seed Cap-fn-ptr 产品面」叶。`e7d7c569`：g05 prologue 声明对齐 pure `*u8` 签名（Ubuntu gcc void\* 冲突 → hybrid 假回退）。mac L2：PREFER hybrid + 矩阵（`/tmp/mac_g05_wave84.log`）。Ubuntu L2：hybrid pure **W**（fn_ptr）+ 矩阵 rv42（`/tmp/ubuntu_g05_wave84.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`c4698724`** / docs | **R2 pipeline_abi Cap residual pure wave83**：`pipeline_sizeof_arena` / `pipeline_sizeof_module` 真迁 pure（固定 SHARED LP64 常量 **16 / 68** ≡ 双端 C `sizeof(struct ast_ASTArena/Module)`；glue `__attribute__((weak))` 冷 fallback）。闭合 Cap residual「pure export-extern glue sizeof」叶。Cap residual 仍 fn-ptr / typeck_module C 前端（+ preprocess 引擎）。mac L2：PREFER hybrid + 矩阵（`/tmp/mac_g05_wave83.log`）。Ubuntu L2：hybrid pure **W**（sizeof_arena/module）+ 矩阵 rv42（`/tmp/ubuntu_g05_wave83.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED LP64 · **日常 L2 非 tip L4** |
| 2026-07-22 | **`1e4fb85a5`** / docs | **R2 pipeline_abi Cap residual pure wave82**：`pipeline_debug_trace_named_func_bodies_impl` 真迁 pure orch（G.7 getenv + `pipeline_module_*` / `ast_ast_block_*` Cap residual + pure `pipeline_debug_body_func_match` + same-TU `pipe_diag_msg_append_*` + `diag_report`；无 va_list reportf；seed 冷孪生 `#ifndef FROM_X` 保留 reportf）。闭合 soft residual「pure 仍调 always-seed body-trace `_impl`」叶。Cap residual 仍 fn-ptr / typeck_module C 前端（+ sizeof / preprocess 引擎）。mac L2：PREFER hybrid + 矩阵（`/tmp/mac_g05_wave82.log`）。Ubuntu L2：hybrid pure **W**（body-trace + pipe_diag_msg_*）+ 矩阵 rv42（`/tmp/ubuntu_g05_wave82.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`1f73ed275` tip L4 升钉** | **真冷全测 L4**：wave76–81 soft residual 整波收口。双端 `remaining_o=0` → bootstrap-driver-seed → g05 → §4 矩阵（rv42 / option / si / hello）→ `SHUX_BSTRICT_SKIP_BUILD=1` **129** 全绿。mac `/tmp/mac_true_cold_1f73ed275.log` · `/tmp/mac_true_bstrict_1f73ed275.log`；Ubuntu 金标 `/tmp/ubuntu_true_cold_1f73ed27.log` · `/tmp/ubuntu_true_bstrict_1f73ed27.log`。钉盘 **`d4e2dca6` → `1f73ed275`**（产品 `6e0796c6` 含于钉盘）。PLATFORM: SHARED · **构建级别=L4** |
| 2026-07-22 | **`6e0796c6`** / docs | **R2 pipeline_abi Cap residual pure wave81**：`shux_preprocess` / `shux_preprocess_quiet` / `shux_preprocess_with_path` 真迁 pure thin（G.7 pure `shux_preprocess_raw_to_malloc_impl`；产品始终 X-pipeline 路径；seed 冷孪生保留 LEGACY `preprocess_c_fallback` under `#ifndef FROM_X`）。闭合 soft residual「preprocess 公共面 always-seed」叶（引擎 `preprocess_*` 仍 Cap residual）。Cap residual 仍 fn-ptr / typeck_module C 前端（+ sizeof / preprocess 引擎）。mac/Ubuntu 日常 L2 后 **tip L4 升钉见上行**。PLATFORM: SHARED |
| 2026-07-22 | **`8b45a69a`** / docs | **R2 pipeline_abi Cap residual pure wave80**：`shux_asm_codegen_elf_o_product_emit` 真迁 pure thin（G.7 export-extern `asm_asm_codegen_elf_o` only — **删**同 TU weak `-1` stub，call 保持 external reloc → final strong `user_asm_seed_bridge`）；seed 冷孪生 `#ifndef FROM_X`；FROM_X rest pure **extern**。闭合硬 Cap residual「product_emit」叶。Cap residual 仍 fn-ptr / typeck_module C 前端（+ sizeof / preprocess）。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave80.log`）。Ubuntu L2：hybrid pure **W**（product_emit）+ **U** `asm_asm_codegen_elf_o` + pure-asm rv42/option/si（`/tmp/ubuntu_g05_wave80.log`）。PLATFORM: SHARED · **日常 L2 后并入 tip L4** |
| 2026-07-22 | **docs 归档**（本提交） | **文档**：21 号全日仪表盘 + §6 流水整表迁入 [`自举进度-归档-2026-07-21.md`](自举进度-归档-2026-07-21.md)；本文 22 日起重新起表（保留 wave76–80 当日流水）。当时钉盘 **`d4e2dca6`** · tip **`8b45a69a`**（已由 tip L4 升钉覆盖）。 |
| 2026-07-22 | **`ccc0f99e`** / docs | **R2 pipeline_abi Cap residual pure wave79**：`shux_path_try_realpath_inplace` 真迁 pure orch（G.7 g05 `shux_driver_realpath_opaque` char\* cast residual + 栈 `resolved[1024]` + `pipe_cstr_copy` ≡ seed snprintf；失败保留 path；非 POSIX harness null ≡ seed `#else` no-op）；seed 冷孪生 `#ifndef FROM_X`；FROM_X rest pure **extern**。闭合历史 Cap residual「OS realpath」叶。PLATFORM: SHARED · **日常 L2 后并入 tip L4** |
| 2026-07-22 | **`c6e36f0e`** / docs | **R2 pipeline_abi Cap residual pure wave78**：`shu_lsp_ptr_slot_clear` 真迁 pure thin（G.7 `shux_ptr_slot_set` null）+ `shux_fputs_stdout`（G.7 g05 `shux_driver_stdout_ptr` + `shux_driver_fputs_opaque`）+ `driver_asm_fp_is_stdout` / `driver_asm_fclose_file`（G.7 g05 stdout 身份比较 + `shux_driver_fclose_opaque`）；seed 冷孪生 `#ifndef FROM_X`；FROM_X rest pure **extern**。PLATFORM: SHARED · **日常 L2 后并入 tip L4** |
| 2026-07-22 | **`ba2c8362`** / docs | **R2 pipeline_abi Cap residual pure wave77**：`typeck_ndep` + `typeck_dep_{module,arena}_ptrs` 真迁 pure BSS（`g_pipe_typeck_*`）+ pure accessors；product seed cold writers→accessor。PLATFORM: SHARED LP64 · **日常 L2 后并入 tip L4** |
| 2026-07-22 | **`f0a89715`** / docs | **R2 pipeline_abi Cap residual pure wave76**：`shux_cstr_offset` 真迁 pure orch。PLATFORM: SHARED · **日常 L2 后并入 tip L4** |

***

## 7. 禁止清单（本阶段）
