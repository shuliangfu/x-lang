# 自举进度归档 · 2026-08-08

> **归档范围**：8 月 8 日全日 §6 波次流水（B′ pipeline_x mega 首刀 bootstrap_glue→ast_pool 各域→elf/wpo→parse_orch/forwarders→typeck 域→pipeline_x residual 收尾→8.3.6 strict_minimal 切片→8.3 结构地板→gen dual leave→typeck.x 全量 `-E` T001→runtime driver/fmt thin dual leave 前序 **wave262–314**）。
> **当日主线**：① 日切 7 号归档后 8 日起表 present 36；② asm_wpo／sidecar_pool／arena Cap pure leave wave274-276；③ pipeline_x mega 首刀 bootstrap_glue wave282 + ast_pool 12 域（elf_codegen_forwarders／typeck_check_expr／parse_orch 等）整域 Cap seed ALWAYS leave wave283-291；④ 8.3.5-8.3.7 host wrappers／stubs／bare alias leave wave293-298（present 36→14）；⑤ 8.3.6 strict_minimal 四波切片 leave wave300-304；⑥ 8.3 pipeline.x residual pure leave 五波 wave305-309 + 8.3 结构地板闭（present 14→0）；⑦ pipeline_gen／typeck／codegen／parser mega＋cfg_eval/labi surface monofile dual WEAK leave wave310-313；⑧ typeck.x 全量 `-E` T001 clear wave314。
> **当日钉盘**：仍 **`36363b90f`**（8 月 7 日升钉，本波未升钉）。
> **日终 residual tip**：wave315 runtime driver／fmt thin monofile dual WEAK leave（主文档 §6 该行；git `~95f450f90` 前一带）。
> **回到主文档**：[自举进度.md](自举进度.md)（9 日起表 · residual tip 续 wave315+ → wave324 M4 3/5）。
> **勿**把本归档 tip／单叶日志冒充现行 tip L4。现行钉盘仍以主文档为准（**`36363b90f`**）。

---

## 8 月 8 日 §6（倒序）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-08-08 | **feat(typeck)** typeck.x 全量 `-E` T001 clear (wave314) | **代码** · **不升钉** · **双端 L2 绿**。G.7：`typeck_check_expr_try_propagate` C 遗留名 `typeck_expr_type_ref`／`typeck_type_refs_equal` → 权威 `expr_type_ref`／`type_refs_equal`；method_call `typeck_import_binding_name_equal` bool 与 `!= 0` 不相容；wave242–260 若干 Cap 面补 LANG-007 整身 `unsafe`（stack-escape／read_ptr／fold thin／linear／apply_call_resolve 等）。**证据**：`xlang_asm -E compiler/src/typeck/typeck.x` → typeck OK · gen C **~725kB**。**产品仍 pin** `typeck_gen`（未 re-pin／M4）。mac：`./xbuild l2-matrix` OK · inventory present **0**。下刀：**runtime mega host-cc seed** 或 typeck M4 re-pin（BC 终局 ⬜）。PLATFORM: SHARED freestanding typeck -E path. |
| 2026-08-08 | **feat(gen)** cfg_eval／labi surface monofile dual WEAK leave (wave313) | **代码** · **不升钉** · **双端 L2 绿**。G.7：`seeds/cfg_eval_gen.linux.x86_64.c` 29× + `labi_diag_pure_surface.from_x.c` 30× + `labi_freestanding_list_surface.from_x.c` 30× freestanding monofile dual `__attribute__((weak))` polyfill → **extern-only**（live STRONG＝runtime／product .o）。host-cc T：cfg **53→24** · labi_diag **55→25** · labi_fs **108→78**。**freestanding dual polyfill 域闭**（wave311–313）。mac／Ubuntu（**`ubuntu-remote-server`**）@ **`374ff0efd`**：`./xbuild l2-matrix` OK · pure-ld 58／60 · inventory present **0**。下刀：**runtime mega host-cc seed** 或 typeck 全量 `-E`（BC 终局 ⬜）。PLATFORM: SHARED freestanding dual leave. |
| 2026-08-08 | **feat(gen)** codegen／parser mega monofile dual WEAK leave (wave312) | **代码** · **不升钉** · **双端 L2 绿**。G.7：`seeds/codegen_gen.linux.x86_64.c` 29× + `parser_gen.linux.x86_64.c` 29× freestanding monofile dual `__attribute__((weak))` polyfill → **extern-only**（live STRONG＝runtime／product .o；parser 保留 1× `parser_copy_module_import_path64` weak）。host-cc T：codegen **194→165** · parser **296→267**（Ubuntu pr **266**）。mac／Ubuntu（**`ubuntu-remote-server`**）@ **`2f841c661`／`706d2bcd9`**：`./xbuild l2-matrix` OK · pure-ld 58／60 · inventory present **0**。下刀：**runtime host-cc seed** 或 typeck 全量 `-E`（BC 终局 ⬜）。PLATFORM: SHARED freestanding mega gen dual leave. |
| 2026-08-08 | **feat(gen)** typeck_gen monofile dual WEAK leave + typeck.x L003 (wave311) | **代码** · **不升钉** · **双端 L2 绿**。G.7：`seeds/typeck_gen.linux.x86_64.c` 30× monofile dual `__attribute__((weak))` polyfill → **extern-only**（live STRONG＝`runtime_pipeline_abi`；typeck_x T **409→379** · dual_pabi **30→0**）。根修 `typeck.x` 块注释 `U*/F*` 提前闭注释 → L003（波次内 `-E` 过 lexer；全量 `-E` 仍 T001）。mac／Ubuntu（**`ubuntu-remote-server`**）@ **`48491e221`**：`./xbuild l2-matrix` OK · pure-ld 58／60 · inventory present **0**。下刀：**codegen／parser mega dual** 或 **runtime host-cc seed**（BC 终局 ⬜）。PLATFORM: SHARED freestanding typeck_gen dual leave. |
| 2026-08-08 | **feat(gen)** pipeline_gen seed re-pin residual shell (wave310) | **代码** · **不升钉** · **双端 L2 绿**。G.7：`seeds/pipeline_gen.linux.x86_64.c` 对齐 tip residual shell（wave305–309 leave 后）；stale pin 仍含 ~40 dual WEAK orch 体 → re-pin 后 sole T＝weak `io_register_buffers_buf_c`（STRONG live＝pipeline_abi）；host-cc seed T **~40→1**。产品 pure-ld 本不编 pipeline_gen。mac／Ubuntu（**`ubuntu-remote-server`**）@ **`67471641b`**：`./xbuild l2-matrix` OK · pure-ld 58／60 · inventory present **0**。下刀：**typeck／codegen／parser mega gen** 或 **runtime host-cc seed**（BC 终局 ⬜）。PLATFORM: SHARED freestanding pipeline_gen seed re-pin. |
| 2026-08-08 | **feat(8.3)** glue shell／typedefs／9×fwd／standalone floor leave (wave309) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：结构地板 0 函数体 → 删 glue 壳＋9×fwd＋ast_pool／typedefs＋standalone seed；g05 空 `_PIPELINE_LINK_O`；emit include no-op；inventory present **13→0**。mac／Ubuntu（**`ubuntu-remote-server`**）@ **`3eb1171ac`**：`./xbuild l2-matrix` OK · pure-ld 无 pipeline mega。下刀：**gen／runtime host-cc seeds**（BC 终局 ⬜）。PLATFORM: SHARED freestanding 8.3 structure-floor leave. |
| 2026-08-08 | **feat(8.3)** Cap-struct residual seed ALWAYS leave (wave308) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：`pipeline_parse_into_with_init_buf` Cap 面 leave host pipeline_x → **runtime_pipeline_abi seed ALWAYS**（thin→impl_c）；pure 仍 scalars／impl_rc（禁 by-value Cap）；`pipeline.x` **export-extern**；gen／pin strip Cap body；pipeline_x T **2→1**（残 gen stub `io_register`）。present **13** 地板。mac：prefer+pure-ld + `./xbuild l2-matrix` OK。Ubuntu（**`ubuntu-remote-server`**）@ **`f0ba8941c`**：同矩阵 · Cap T on abi。下刀：**glue 壳／typedefs／9×fwd／standalone**。PLATFORM: SHARED freestanding Cap-struct residual leave. |
| 2026-08-08 | **feat(8.3)** pipeline.x run_x／thin residual pure leave (wave307) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：wave306 后 pure 仍 U 调 `should_skip`／`run_x_pipeline_impl`；Cap `_c` orch 已 pure（wave105–112）。产品面 **25** residual thin／orch → **runtime_pipeline_abi pure**（thin→Cap + `pipeline_run_x_pipeline_impl` orch）；`pipeline.x` **export-extern**；soft residual Cap-struct `parse_into_with_init_buf`；seed cold twins under `#ifndef FROM_X`；pipeline_x T **27→2**（gen 双端手同步）。present **13** 地板。mac：pure-ld + `./xbuild l2-matrix` OK。Ubuntu（**`ubuntu-remote-server`**）@ **`40154ddc1`**：同矩阵。下刀：**Cap-struct residual** 或 **glue 壳／typedefs／9×fwd／standalone**。PLATFORM: SHARED freestanding pipeline.x residual pure leave. |
| 2026-08-08 | **feat(8.3)** pipeline.x resolve residual pure leave (wave306) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：wave305 后 pure `resolve_path_x` 仍 U 调 try_*；Cap `_c` 已 pure（wave105）。产品面 **try_one／try_entry／try_flat／import_has_dot／path_append_*** → **runtime_pipeline_abi pure**（thin 组合 Cap）；`pipeline.x` **export-extern**；seed cold twins under `#ifndef FROM_X`；pipeline_x T **34→27**（gen 双端手同步）。present **13** 地板。mac：pure-ld + `./xbuild l2-matrix` OK。Ubuntu（**`ubuntu-remote-server`**）@ **`a7c40c9c3`**：同矩阵 T27。下刀：**run_x orch／thin→_c** 或 **glue 壳／typedefs／9×fwd／standalone**。PLATFORM: SHARED freestanding pipeline.x resolve residual pure leave. |
| 2026-08-08 | **feat(8.3)** pipeline.x dual-export thin leave (wave305) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：`pipeline.x` 五 dual XLANG_LIB_WEAK twin → **export-extern**（`parse_into_buf`／`resolve_path_probe_dot`／`resolve_path_x`／`read_file_x`／`lsp_diag_parse_typeck_buf`）；live＝**runtime_pipeline_abi pure／seed**；try_* 仍 pipeline_x（pure U）；host-cc pipeline_x T **39→34**（gen 本地手同步 · `*_gen.c` gitignore）。present **13** 地板。mac：pure-ld + `./xbuild l2-matrix` OK。Ubuntu（**`ubuntu-remote-server`**）@ **`7429c7ab4`**：同矩阵。下刀：**pipeline.x residual thin**（try_*／run_x）或 **glue 壳／typedefs／9×fwd／standalone**。PLATFORM: SHARED freestanding pipeline.x dual leave. |
| 2026-08-08 | **feat(8.3.6)** strict_minimal seed shell retire (wave304) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7 8.3.6：wave303 后 seed 已 0 T（nm 空）；删 `seeds/pipeline_glue_strict_minimal.from_x.c`；产品 g05 `_GLUE_SUFFIX`／HOT／ensure 摘链（stubs only）；catalog RELINK／R1_MISC／LEGACY prereqs／driver_seed ensure 不再 force companion；filter／asm ensure soft；inventory **absent** · present **14→13**。mac：pure-ld + `XLANG=…/xlang ./xbuild l2-matrix` OK。Ubuntu（**`ubuntu-remote-server`**）@ **`5f44363fd`**：同矩阵。下刀：**pipeline.x thin residual** 或 **glue 壳／typedefs／9×fwd**。PLATFORM: SHARED freestanding 8.3.6 shell retire. |
| 2026-08-08 | **feat(8.3.6)** find_func_return overload → typeck_x leave (wave303) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7 8.3.6 切片：`find_func_return_type_*_call` overload 链 leave 至 **typeck_x**（`typeck_overload_arg_param_score` METHOD_CALL 实参 + by_name_overload last-segment expect + Cap face）；strict_minimal seed 删 body（~900→~571 shell · 0 T export）；`.x` export-extern；tip seed pin `typeck_gen.linux.x86_64.c` 同步产品 twin（`.x` -E 反引号债）。present **14** 地板。mac：pure-ld + `XLANG=…/xlang ./xbuild l2-matrix` OK。Ubuntu（**`ubuntu-remote-server`**）@ **`5c3a66e59`**：同矩阵。下刀：**pipeline.x thin residual** 或 **strict_minimal shell retire**。PLATFORM: SHARED freestanding 8.3.6 leave. |
| 2026-08-08 | **feat(8.3.6)** parse_commit_pre／post → diagnostic thin leave (wave302) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7 8.3.6 切片：`parser_diagnostic_parse_commit_pre／post` 权威迁至 **runtime_driver_diagnostic thin**（gather→`driver_diagnostic_parse_commit_shape`；product PREFER_X_O；链序 diagnostic 在 strict_minimal 前）；cold twins thin／surface／rest `#ifndef FROM_X`；strict_minimal 删 dual body（~957→~900）；残 export 仅 `find_func_return_type_*_call` overload 链。present **14** 地板。mac：pure-ld + `./xbuild l2-matrix` OK。Ubuntu（**`ubuntu-remote-server`**）@ **`6afc19ae6`**：同矩阵。下刀：**8.3.6 残 overload** 或 **pipeline.x thin residual**。PLATFORM: SHARED freestanding 8.3.6 leave. |
| 2026-08-08 | **feat(8.3.6)** dual-export WEAK via pipeline_abi leave (wave301) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7 8.3.6 切片：`pipeline_glue_strict_minimal.from_x.c` 删已在 **runtime_pipeline_abi.o／driver_x.o／pipeline_x.o STRONG** 的 dual-export `XLANG_WEAK`（check_expr dispatch／after_parse_ok／layout glue／main_entry／pad·hot_reorder 等）+ 仅 dual 用 helpers；残 export：`parse_commit_pre／post` + `find_func_return_type_*_call` overload 链（typeck_x U）。seed ~2546→~957；present **14** 地板。mac：pure-ld g05 + `./xbuild l2-matrix` OK。Ubuntu（**`ubuntu-remote-server`**）@ **`0d03dc599`**：同矩阵。下刀：**8.3.6 残 overload／parse_commit** 或 **pipeline.x thin residual**。PLATFORM: SHARED freestanding 8.3.6 slice. |
| 2026-08-08 | **feat(8.3.6)** dual-export WEAK typeck twins leave (wave300) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7 8.3.6 切片：`pipeline_glue_strict_minimal.from_x.c` 删 **28**× 已在 typeck_x.o **STRONG** 的 `XLANG_WEAK` Cap 孪生（method_call／region／linear／block_impl／type_refs_equal／soa glue 等）+ 死 linear BSS／helper + 孤儿 ifndef；剩余 strict dispatch `U` typeck_x 面。seed ~3405→~2546；defined 74→45；present **14** 地板（seed twin 仍链）。mac：pure-ld g05 + `./xbuild l2-matrix` OK。Ubuntu（**`ubuntu-remote-server`**）@ **`fff0725ee`**：同矩阵。下刀：**8.3.6 残 strict_only WEAK／helpers** 或 **pipeline.x thin residual**。PLATFORM: SHARED freestanding 8.3.6 slice. |
| 2026-08-08 | **feat(L2 matrix)** harness root fix (wave299) | **代码** · **不升钉** · **双端 L2 绿**。根因：`return 42`／option `102` 是成功退出码；Agent／文档在 `set -e` 下手写 `/tmp/rv` → 矩阵在 rv 后截断（假事故「被 rv=42 打断」）。G.7：`compiler/scripts/product_l2_matrix.sh` + `./xbuild l2-matrix` 捕获 run rc、跑全 rv／opt／si／hello／f32 再汇总；skill §4 + `自举验证.md` §5 改权威入口。present **14** 地板不变。mac：`./xbuild l2-matrix` OK。Ubuntu（**`ubuntu-remote-server`**）@ **`6db18690a`**：同 harness OK。下刀：**8.3.6 strict_minimal** 或 **pipeline.x thin residual**。PLATFORM: SHARED L2 process. |
| 2026-08-08 | **feat(8.3.2)** ptr_at dead leave (wave298) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：删 `ast_pool_ptr_at.c`（present−1）；static `block_at`／`module_layout_at`／`module_import_at` 在 domain pure／seed ALWAYS leave 后**零 residual 调用方**（block Cap＝`w277_block_at`）；撤 `#include` + `PIPELINE_X_DEPS` + g05 STALE；inventory present **15→14**（ROWS 128）。**结构地板诚实**：glue 壳／typedefs／9×fwd／strict_minimal+standalone seed twin 非可 leave 功能叶。mac：heat pipeline_x + pure-ld；rv42／opt102／hello0／si0／f32。Ubuntu（**`ubuntu-remote-server`**）@ **`99283c4f6`**：同矩阵。下刀：**8.3.6 strict_minimal** 或 **pipeline.x thin residual**。PLATFORM: SHARED freestanding dead residual leave. |
| 2026-08-08 | **feat(8.3.7)** scripts stubs seed leave (wave297) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：删 `scripts/asm_text_stub.c`／`asm_xlang_lsp_diag_stub.c`（present−2）；权威 `seeds/asm_text_stub.from_x.c` + `asm_xlang_lsp_diag_stub.from_x.c`；build_xlang_asm／strict_glue／experimental seed-only .o；G05 本不链 host 叶。inventory present **17→15**（ROWS 128）。**8.3.7 host leave ✅**；8.3.5 残 scripts 闭。残：glue 壳／typedefs／9×fwd／strict_minimal seed twin。mac：pure-ld g05 + seed stubs cc；rv42／opt102／hello0／si0／f32。Ubuntu（**`ubuntu-remote-server`**）@ **`8473f35e2`**：同矩阵。下刀：**glue 壳／typedefs／9×fwd**。PLATFORM: SHARED 8.3.7 leave. |
| 2026-08-08 | **feat(8.3.5)** typeck bare alias seed leave (wave296) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：删 host `typeck_asm_bare_link_alias.c`（present−1）；权威 `seeds/typeck_asm_bare_link_alias.from_x.c`；gen／build_xlang_asm／strict_glue seed-only .o；G05 本不链该叶。inventory present **18→17**（ROWS 128）。残：scripts stubs + glue 壳／typedefs／9×fwd。mac：pure-ld g05 + seed bare cc；rv42／opt102／hello0／si0／f32。Ubuntu（**`ubuntu-remote-server`**）@ **`9198c5f3a`**：同矩阵。下刀：**glue 壳／typedefs／9×fwd／scripts stubs**。PLATFORM: SHARED 8.3.5 partial leave. |
| 2026-08-08 | **feat(8.3.5)** `_x_stubs2` dead dual leave (wave295) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：删 `_x_stubs2.c`（G05_OBJS 与 stage2 均不链；符号均有产品实叶／bridge）；B4 GEN_C_TO_O **5→4**（mk／ensure／leaf residual）；inventory present **19→18**（ROWS 128）。残：`typeck_asm_bare_link_alias`／scripts stubs + glue 壳／typedefs／9×fwd。mac：pure-ld g05 + seed stubs cc；rv42／opt102／hello0／si0／f32。Ubuntu（**`ubuntu-remote-server`**）@ **`9acd22fc9`**：同矩阵。下刀：**glue 壳／typedefs／9×fwd／typeck bare／scripts stubs**。PLATFORM: SHARED 8.3.5 partial leave. |
| 2026-08-08 | **feat(8.3.5)** cold host stubs leave (wave294) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：删 `_stubs.c`／`xlang_x_stubs.c`（产品 g05 未链；死双份）；权威收敛 `seeds/x_stubs.from_x.c`（吸收 `driver_exec_cmd`→`link_abi_system`；preprocess→typeck 桥）；`build_and_test_x` 改 seed-only；inventory present **21→19**（ROWS 128）。残：`typeck_asm_bare_link_alias`／`_x_stubs2`／scripts stubs + glue 壳／typedefs／9×fwd。mac：pure-ld g05 + seed stubs cc；rv42／opt102／hello0／si0／f32。Ubuntu（**`ubuntu-remote-server`**）@ **`b236900e7`**：同矩阵。下刀：**glue 壳／typedefs／9×fwd／typeck bare／_x_stubs2／scripts stubs**。PLATFORM: SHARED 8.3.5 partial leave. |
| 2026-08-08 | **feat(8.3.5)** seed host wrappers leave (wave293) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：整域删 5× 5 行 host 占位（`ast_asm_bare_link_alias`／`backend_asm_bare_link_alias`／`backend_asm_strict_fallback_alias`／`x_frontend_link_alias`／`typeck_c_module_stubs`；均 `#include` seed only）；产品／ensure 已直编 `seeds/*from_x.c` → `.o`；inventory present **26→21**（ROWS 128）。残：`typeck_asm_bare_link_alias` 真体 + `_stubs`／`_x_stubs2`／`xlang_x_stubs` + scripts stubs。mac：seed-cc 5×.o + pure-ld g05；rv42／opt102／hello0／si0／f32。Ubuntu（**`ubuntu-remote-server`**）@ **`a5264542c`**：同矩阵。下刀：**glue 壳／typedefs／9×fwd／typeck bare alias／_stubs**。PLATFORM: SHARED 8.3.5 partial leave. |
| 2026-08-08 | **feat(8.3.4)** bootstrap_orchestration host wrapper leave (wave292) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：删 `pipeline_bootstrap_orchestration.c` 5 行 host 占位（`#include` seed only）；产品／ensure 已直编 `seeds/pipeline_bootstrap_orchestration.from_x.c` → `.o`；clangd gen_compile_commands 改 seed 路径；inventory present **27→26**（ROWS 128）。mac：try-heat orch.o + pure-ld g05；rv42／opt102／hello0／si0／f32。Ubuntu（**`ubuntu-remote-server`**）@ **`ebf7a2f1b`**：同矩阵。下刀：**glue 壳／typedefs／9×fwd／alias·stub**。PLATFORM: SHARED 8.3.4 residual leave. |
| 2026-08-08 | **feat(8.3.2)** elf_codegen_forwarders Cap seed ALWAYS leave (wave291) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：`pipeline_elf_codegen_forwarders.c` 整域 leave — **pipeline_x mega LOC 刀**；seed ALWAYS 拥有 18× platform.elf／codegen／pipeline rename shims + `pipeline_sizeof_elf_ctx`（LP64 固定 size 18939952；无 host sizeof）；pure ELF wave273 + codegen_x.o 拥有未前缀 callee（dual-export ban · pipeline_abi T · pipeline_x 无 T）；host 叶已删；present **27**（LOC−mega；ROWS 127→128 absent）；glue `#include` 撤。mac：prefer + heat pipeline_x + pure-ld；rv42／opt102／hello0／si0／f32。Ubuntu（**`ubuntu-remote-server`**）@ **`53ef67096`**：同矩阵。下刀：**glue 壳／typedefs／mega residual**。PLATFORM: SHARED freestanding Cap leave (seed residual class). |
| 2026-08-08 | **feat(8.3.2)** mega_body Cap seed ALWAYS leave (wave290) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：`pipeline_asm_codegen_mega_body.c` 整域 leave — **pipeline_x mega LOC 刀**；seed ALWAYS 拥有 `pipeline_asm_ctx_reset_for_func_c` + `pipeline_backend_asm_codegen_ast_to_elf_mega_body_c`（WPO／PGO emit 序主循环；LE AsmFuncCtx 全 overlay + Elf e_machine／reloc 偏移 + DepCtx.target_arch）；dual-export ban（pipeline_abi T · pipeline_x 无 T）；host 叶已删；present **27**（LOC−mega；ROWS 126→127 absent）；glue `#include` 撤。mac：prefer + heat pipeline_x + pure-ld；rv42／opt0／hello0／si0／f32。Ubuntu（**`ubuntu-remote-server`**）@ **`e5471ebee`**：同矩阵。下刀：**glue 壳／typedefs／elf forwarders／mega residual**。PLATFORM: SHARED freestanding Cap leave (seed residual class). |
| 2026-08-08 | **feat(8.3.2)** codegen_outbuf Cap seed ALWAYS leave (wave289) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：`pipeline_codegen_outbuf.c` 整域 leave — **pipeline_x mega LOC 刀**；seed ALWAYS 拥有 append_bytes／cstr helpers + `pipeline_codegen_emit_float_lit_c` + `pipeline_codegen_emit_expr_try_propagate_c`；**slice_init 不进 ALWAYS**（产品 OMIT_X_DUP · 权威＝codegen_x.o）；strict_minimal float_lit 副本同 commit 删（dual-export ban · pipeline_abi T · pipeline_x 无 T）；host 叶已删；present **27**（LOC−mega；ROWS 125→126 absent）；glue `#include` 撤。mac：prefer + heat pipeline_x + pure-ld；rv42／opt0／hello0／si0／f32。Ubuntu（**`ubuntu-remote-server`**）@ **`1079815fb`**：同矩阵。下刀：**glue 壳／typedefs／mega residual**。PLATFORM: SHARED freestanding Cap leave (seed residual class). |
| 2026-08-08 | **feat(8.3.2)** label_format Cap seed ALWAYS leave (wave288) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：`pipeline_asm_label_format.c` 整域 leave — **pipeline_x mega LOC 刀**；seed ALWAYS 拥有 format_u32／i32 helpers + `pipeline_asm_emit_next_label_c`（`.Lf<scope>_<n>`；label_counter@12 LE）+ `pipeline_asm_format_label_id_c`（`.L_<id>`）；deps pure／seed `pipeline_asm_ctx_layout` + `pipeline_elf_label_mod_scope_active`；dual-export ban（pipeline_abi T · pipeline_x U）；host 叶已删；present **27**（LOC−mega；ROWS 124→125 absent）；glue `#include` 撤。mac：prefer/cold + heat pipeline_x + pure-ld；rv42／opt102／hello0／si0。Ubuntu（**`ubuntu-remote-server`**）@ **`a9b869677`**：同矩阵。下刀：**codegen_outbuf**／glue 壳／typedefs。PLATFORM: SHARED freestanding Cap leave (seed residual class). |
| 2026-08-08 | **feat(8.3.2)** parser_result Cap seed ALWAYS leave (wave287) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：`pipeline_parser_result.c` 整域 leave — **pipeline_x mega LOC 刀**；seed ALWAYS 拥有 slice／lex／result-copy Cap faces（parser_slice_from_buf／lexer_parser_slice_from_buf／pipeline_source_slice／parser_lexer_pos_before／parser_lex_from_*／pipeline_parser_*_set_*／library／try_skip copy）；product LE layouts Lexer／LexerResult／OneFunc／Extern／Library／TrySkip；dual-export ban；host 叶已删；present **27**（LOC−mega；ROWS 123→124 absent）；glue `#include` 撤。mac：prefer/cold + heat pipeline_x + pure-ld；rv42／opt102／hello0／si0。Ubuntu（**`ubuntu-remote-server`**）@ **`a64480278`**：同矩阵。下刀：**glue 壳／typedefs**。PLATFORM: SHARED freestanding Cap leave (seed residual class). |
| 2026-08-08 | **feat(8.3.2)** typeck_check_expr Cap seed ALWAYS leave (wave286) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：`pipeline_typeck_check_expr.c` 整域 leave — **pipeline_x mega LOC 刀**；seed ALWAYS 拥有 dispatch（impl_mega_c／impl_c／c）+ match subject BSS + thin panic／match／return／unary／addr／deref／index／var／try／call + repr／extern_unsafe；XLANG_WEAK check_expr_impl{,_mega}；typeck.x 仍拥有 live typeck_check_expr_* 子 helper（dual-export ban）；host 叶已删；present **27**（LOC−mega；ROWS 122→123 absent）；glue `#include` 撤。mac：prefer/cold + heat pipeline_x + pure-ld；rv42／opt102／hello0／si0。Ubuntu（**`ubuntu-remote-server`**）@ **`8f853606d`**：同矩阵。下刀：**glue 壳／typedefs／parser_result**。PLATFORM: SHARED freestanding Cap leave (seed residual class). |
| 2026-08-08 | **feat(8.3.2)** typeck_orch Cap seed ALWAYS leave (wave285) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：`pipeline_typeck_orch.c` 整域 leave — **pipeline_x mega LOC 刀**；seed ALWAYS 拥有 thin `pipeline_typeck_x_ast{,_impl,_library,_check_one_func}_c` + layout glue（size／align／zero_padding）；pure 仍拥有 soft_suppress／dep_ctx／dep_prerun 强符号；cold WEAK 仅 `#ifndef FROM_X`；host 叶已删；present **27**（LOC−mega；ROWS 121→122 absent）；glue `#include` 撤。mac：prefer/cold + heat pipeline_x + pure-ld；rv42／opt102／hello0／si0。Ubuntu（**`ubuntu-remote-server`**）@ **`4e7a98f8f`**：同矩阵。下刀：**typeck_check_expr thin／glue 壳／typedefs／parser_result**。PLATFORM: SHARED freestanding Cap leave (seed residual class). |
| 2026-08-08 | **feat(8.3.2)** parse_orch Cap seed ALWAYS leave (wave284) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：`pipeline_parse_orch.c` 整域 leave — **pipeline_x mega LOC 刀**；seed ALWAYS 拥有 parse/load/typeck orch Cap residual（impl_c／Cap-struct-return／diagnostics／sizeof_dep_ctx／expr helpers；LE 字段访问 + pure accessors；DepCtx gate 固定 LP64 size）；pure duals 仅 `#ifndef FROM_X` cold（read_file_x cold 已有）；host 叶已删；present **27**（LOC−mega；ROWS 120→121 absent）；glue `#include` 撤。mac：prefer/cold + heat pipeline_x + pure-ld；rv42／opt102／hello0／si0。Ubuntu（**`ubuntu-remote-server`**）@ **`2658f1ccb`**：同矩阵。下刀：**typeck thin／glue 壳／typedefs／parser_result**。PLATFORM: SHARED freestanding Cap leave (seed residual class). |
| 2026-08-08 | **feat(8.3.2)** ast_forwarders Cap seed ALWAYS leave (wave283) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：`pipeline_ast_forwarders.c` 整域 leave — **pipeline_x mega LOC 刀**；seed ALWAYS 拥有 ~186 `ast_pipeline_*` thin rename shims + `pipeline_copy_lib_root_to_buf256`（struct* 匹配 seed 早原型；FROM_X 下 pure void* callees 再声明；blacklist always-visible struct* ndep／import_path_copy64）；host 叶已删；present **27**（原无 inventory present 行；ROWS 119→120 absent）；glue `#include` 撤。mac：prefer thin+rest + heat pipeline_x + pure-ld；rv42／opt102／hello0／si0。Ubuntu（**`ubuntu-remote-server`**）：同矩阵。下刀：**parse_orch／typeck thin／glue 壳**。PLATFORM: SHARED freestanding Cap leave (seed residual class). |
| 2026-08-08 | **feat(8.3.4)** bootstrap_glue Cap seed ALWAYS leave (wave282) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：`ast_pool_bootstrap_glue.c` 整域 leave — **pipeline_x mega 首刀**；seed ALWAYS 拥有 typeck_i32_ptr_*／layout_metrics_*／asm scope BSS／asm_local_slot_reg_offset（arm64 低端／x86 高端 polarity）／dep_skip 簇／redirect_std_c_wrapper；host 叶已删；present **28→27**。mac：prefer thin+rest + heat pipeline_x + refilter + pure-ld；rv42／opt102／hello0／si0。Ubuntu（**`ubuntu-remote-server`**）@ **`5854b4d2c`**：prefer + heat + pure-ld + 同矩阵。下刀：**pipeline_x mega** 残域（parse_orch／forwarders／typeck thin／glue 壳）。PLATFORM: SHARED freestanding bootstrap Cap leave (seed residual class). |
| 2026-08-08 | **feat(8.3.2)** onefunc Cap seed ALWAYS leave (wave281) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：`ast_pool_onefunc.c` 整域 leave — seed ALWAYS 拥有 `pipeline_onefunc_*`／`pipeline_block_fill_*_from_onefunc`（消费 pure `onefunc_sidecar_get`+`grow_vec_*`+wave277 block append；LE OneFuncSidecar 944 · Region 140 · Labeled 272 · Block 92）；typedef Cap face decls；host 叶已删；present **29→28**。mac：prefer thin+rest + heat pipeline_x + refilter + pure-ld；rv42／opt102／hello0／si0。Ubuntu（**`ubuntu-remote-server`**）@ **`90fb128de`**：prefer + heat + pure-ld + 同矩阵。下刀：**pipeline_x mega**。PLATFORM: SHARED freestanding onefunc leave (seed residual class). |
| 2026-08-08 | **feat(8.3.2)** module_func Cap seed ALWAYS leave (wave280) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：`ast_pool_module_func.c` 整域 leave — seed ALWAYS 拥有 `pipeline_module_func_*`／`pipeline_arena_func_param_write`／`copy_slot_from_module`／`pipeline_asm_module_func_*`／`arch_arm64_pipeline_asm_module_func_*`（消费 pure `module｜arena_sidecar_get`+`grow_vec_*`+`pipeline_arena_func_ptr`；LE Func 196 · FuncParam 136 · ModuleSidecar 432 · ArenaSidecar 816）；typedef Cap face decls；host 叶已删；present **30→29**。mac：prefer thin+rest + heat pipeline_x + refilter + pure-ld；rv42／opt102／hello0／si0。Ubuntu（**`ubuntu-remote-server`**）@ **`06b0ff801`**：prefer + heat + pure-ld + 同矩阵。下刀：**pipeline_x mega** 或 onefunc。PLATFORM: SHARED freestanding module_func leave (seed residual class). |
| 2026-08-08 | **feat(8.3.2)** lifecycle Cap seed ALWAYS leave (wave279) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：`ast_pool_lifecycle.c` 整域 leave — seed ALWAYS 拥有 `ast_pool_block_on_alloc`／`module｜arena_reset｜release`／`drop_bodies_for_check`／`onefunc_reset｜release`（消费 pure `*_sidecar_get｜free`+`grow_vec_*`+pure storage_reset｜release；LE Arena 816 · Module 432 · OneFunc 944 · Block 92 · Func body_ref@148）；module_func residual 收纳 static helpers；typedef Cap face decls；host 叶已删；present **31→30**。mac：prefer thin+rest + pure-ld；rv42／opt102／hello0／si0。Ubuntu（**`ubuntu-remote-server`**）@ **`34b7d95e7`**：prefer + pure-ld + 同矩阵。下刀：**pipeline_x mega** 或 onefunc／module_func。PLATFORM: SHARED freestanding lifecycle leave (seed residual class). |
| 2026-08-08 | **feat(8.3.2)** expr_sidecar Cap seed ALWAYS leave (wave278) | **代码** · **不升钉** · **双端 L2 绿**（mac + Ubuntu）。B′ G.7：`ast_pool_expr_sidecar.c` 整域 leave — seed ALWAYS 拥有 call／method／match／struct_lit／array_lit GrowVec mutators + type-pos type_arg + Expr field Cap accessors（消费 pure `arena_sidecar_get`+`grow_vec_*`+`pipeline_arena_expr_ptr`；LE Expr 712 · ArenaSidecar 816 · MatchArm 24 · StructLitField 136）；typedef Cap face decls；FIELD_ACCESS kind=44；host 叶已删；present **32→31**。mac：prefer thin+rest + pure-ld；rv42／opt102／hello0／si0／struct42。下刀：**pipeline_x mega** 或 lifecycle／onefunc／module_func。PLATFORM: SHARED freestanding expr_sidecar leave (seed residual class). |
| 2026-08-08 | **feat(8.3.2)** block Cap seed ALWAYS leave (wave277) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：`ast_pool_block.c` 整域 leave — seed ALWAYS 拥有 `pipeline_block_append_*`／cold getters／parent patch／name resolve／stmt_order fixup／`ast_ast_block_*` 前缀转发（消费 pure `arena_sidecar_get`+`grow_vec_*`+`pipeline_arena_block_ptr`；LE Block 92 · Const/Let 140 · Region 140 · Labeled 272 · StmtOrder 8）；pure 空 `pipeline_module_fixup_with_arena_stmt_orders` 降为 export-extern；typedef Cap face decls；host 叶已删；present **33→32**。mac：prefer thin+rest + pure-ld；rv42／opt102／hello0／si0／loop45。Ubuntu（**`ubuntu-remote-server`**）@ **`5a927c3c3`**：prefer + pure-ld + block Cap T 面 + 同矩阵。下刀：**expr_sidecar** 或 pipeline_x mega。PLATFORM: SHARED freestanding block domain leave (seed residual class). |
| 2026-08-08 | **feat(8.3.2)** arena Cap pure-owned leave (wave276) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：`ast_pool_arena.c` 整域 leave — pure `#[no_mangle]` 拥有 `pipeline_arena_{type,expr,block,func}_{ptr,alloc}`／caps／num_types／write_var｜binop／parser_library_init／fill_u8／init aliases／implicit_tail／glue_arena_expr_at_ref（消费 pure `arena_sidecar_get`+`grow_vec_*`；LE Type 276／Expr 712／Block 92／Func 196）；**seed always-C** 拥有 by-value get/set_copy + name aliases + float IEEE（双端 sret ABI）；cold twins under `#ifndef FROM_X`；typedef Cap face decls；dual-export ban；host 叶已删；present **34→33**。mac：prefer thin+rest + pure-ld；rv42／opt102／hello0／si0。Ubuntu（**`ubuntu-remote-server`**）@ **`1dfda4b5b`**：prefer + pure-ld + arena pure W 面 + 同矩阵。下刀：**block／expr_sidecar** 或 pipeline_x mega。PLATFORM: SHARED freestanding arena leave. |
| 2026-08-08 | **feat(8.3.2)** sidecar_pool Cap pure-owned leave (wave275) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：`ast_pool_sidecar_pool.c` 整域 pure `#[no_mangle]`（`arena｜module｜onefunc_sidecar_get｜free` + BSS `g_pipe_*_sc_blob`；LE Arena 816／Module 432／OneFunc 944；cap 512／512／1024）；seed cold twins under `#ifndef FROM_X`；lifecycle free-by-key；typedef Cap face decls；dual-export ban；host 叶已删；present **35→34**。mac：prefer thin+rest + pure-ld；rv42／opt102／hello0／si0。Ubuntu（**`ubuntu-remote-server`**）@ **`545073438`**：prefer + pure-ld + 同矩阵。**pipeline_x mega 地基**：后续 arena／block／expr_sidecar 可消费 pure get。下刀：arena／block／expr_sidecar 或 pipeline_x mega。PLATFORM: SHARED freestanding sidecar leave. |
| 2026-08-08 | **feat(8.3.2)** asm WPO Cap pure-owned leave (wave274) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：`pipeline_asm_wpo.c` 整域 pure `#[no_mangle]`（reach clear／compute／should_emit／pgo emit_order／is_hot + BSS graph／PGO tables）；seed cold twins under `#ifndef FROM_X`；dual-export ban（pipeline_abi T · pipeline_x U）；host 叶已删；present **36→35**。mac：prefer thin+rest + pure-ld；rv42／hello0／opt102。Ubuntu（**`ubuntu-remote-server`**）@ **`8e277d8bc`**：prefer + pure-ld + 同矩阵。mega `-E` ~200s／~3GB 正常（勿当 hang 早杀）。下刀：**pipeline_x mega**。PLATFORM: SHARED freestanding WPO leave. |
| 2026-08-08 | **docs／L2** wave273 Ubuntu 金标 L2 收口 + 文档对齐 | **文档 only** · **不升钉**。Ubuntu（**`ubuntu-remote-server`**）@ tip **`87291d0b2`**：`git pull --ff-only` → g05 pure-ld → `runtime_pipeline_abi.o` 含 `pipeline_elf_*` pure W 面 → 矩阵 rv42／opt0／hello0／si0／import0／fs0；log `/tmp/ubuntu_w273_l2_87291d0b2.log`。与 mac L2 合称 **wave273 双端 L2 绿**。抬头／KPI／前排／§5／时序／C迁移对齐 present **36** · 下刀 **asm_wpo**／pipeline_x mega。SSH 权威写死 `ubuntu-remote-server`。 |
| 2026-08-08 | **docs** · 日切归档 8 月 7 日 | **文档 only** · **不升钉**。7 号全日 §6（B′ pure leave wave200–261 + 升钉 `36363b90f` · **64** 行）整表迁入 [`自举进度-归档-2026-08-07.md`](自举进度-归档-2026-08-07.md)；本文从 8 日起重新起表。钉盘仍 **`36363b90f`** · tip residual **wave273** @ present **36** · 下刀 asm_wpo／pipeline_x mega。 |
| 2026-08-08 | **docs／L2** wave272 tip 双端收口 + 文档同步 | **不升钉**。mac tip 复跑 L2：`link-product` pure-ld + rv／opt／hello／import／fs + inventory present **36**。Ubuntu L2 已在 wave272 行收口。文档：抬头／KPI／前排／§5 对齐 residual tip wave272；C迁移 dep_ctx／grow_vec absent；自举时序 pin+tip；inventory NEXT 指向 elf／wpo。已知债：`typeck.x` 注释 `` ` `` → L003 挡全量 `-E`。下刀：asm_wpo／pipeline_x mega。 |
| 2026-08-08 | **feat(8.3.2)** ELF ctx/write_o Cap pure-owned leave (wave273) | **代码** · **不升钉** · **双端 L2 绿**（mac 先绿；Ubuntu 同日于 `ubuntu-remote-server` 补绿 · 见上 docs／L2 行）。B′ G.7：`pipeline_elf_ctx.c`＋`pipeline_elf_write_o.c` 整域 pure `#[no_mangle]`（ctx accessors＋PGO/Mach-O/ELF writers＋BSS sidecars）；seed cold twins under `#ifndef FROM_X`；dual-export ban（pipeline_abi T · pipeline_x U）；host 叶已删；present **38→36**。mac：prefer thin+rest pipeline_abi（cached -E）＋gen-x pipeline_x＋pure-ld；hello0／return42／option／import。Ubuntu：g05 pure-ld＋ELF pure W 面＋rv／opt／hello／import／fs。下刀：asm_wpo／pipeline_x mega。PLATFORM: SHARED freestanding ELF leave. |
| 2026-08-08 | **feat(8.3.2)** DepCtx Cap residual pure-owned leave (wave272) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：`ast_pool_dep_ctx.c` host-cc leave — `pipeline_dep_ctx_*`／`pipeline_ctx_lib_root_*`／`pipeline_dep_ctx_sidecar_release` → runtime_pipeline_abi pure（#[no_mangle]；DepCtxSidecar BSS 表；PipelineDepCtx LE offsets）；删顶层 export-extern（T001）；free／driver_check_only_get 包 unsafe；ast_pool 同 TU 仅留 extern 原型；seed cold twins under #ifndef FROM_X；删 residual + DepCtx 表出 sidecar_pool + DEPS／STALE／inventory；present **39→38**；dual-export ban（pipeline_abi T · pipeline_x U）。mac：prefer thin+rest + gen-x + pure-ld；rv／opt／hello0／import／fs OK。Ubuntu（`ubuntu-remote-server`）：prefer／gen-x 后 pure-ld 初败于 **stale typeck_x.o 缺 Cap T 面**（`typeck.x` 全量 `-E` 仍被注释反引号 L003 挡；本波 scp mac `typeck_gen` + FORCE 编 `typeck_x` 后 pure-ld OK）；rv／opt／hello0／import／fs；inventory present **36**。另：`typeck_match_subject_field_type` 内 `let match`→`name_eq`（`match` 保留字 · tip `35ec2e240`）。下刀：elf_ctx／elf_write／wpo；pipeline_x mega。PLATFORM: SHARED freestanding DepCtx Cap leave. |
| 2026-08-08 | **feat(8.3.2)** GrowVec Cap residual pure-owned leave (wave271) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu 随 wave272 双端 L2 同路径补绿 · 非单独 SSH 债）。B′ G.7：`pipeline_grow_vec.c` host-cc leave — grow_vec_init／free／ensure／at／push／copy_append → runtime_pipeline_abi pure（#[no_mangle]；GrowVec LE sizeof 32 data*@0 cap@8 len@12 elem_sz@16 mmap@24；POSIX mmap large path）；typedef+extern 留 ast_pool_typedefs；seed cold twins under #ifndef FROM_X；删 residual + DEPS／STALE／inventory；present **40→39**；dual-export ban（pipeline_abi T · pipeline_x U）。mac：prefer thin+rest pipeline_abi + gen-x pipeline_x + pure-ld；rv42／opt102／hello0／si0／fs0；nm T Cap on pipeline_abi。下刀：elf_ctx／elf_write／wpo／dep_ctx；pipeline_x mega。PLATFORM: SHARED freestanding GrowVec Cap leave. |
| 2026-08-08 | **feat(8.3.2)** type pool Cap residual pure-owned leave (wave270) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu L2 SSH 待补）。B′ G.7：`ast_pool_type.c` host-cc leave — `pipeline_type_*` pool faces（named／region／kind／elem／array／find_or_alloc slice｜ptr｜named｜compound／init／ensure）→ runtime_pipeline_abi pure（#[no_mangle]；Type LE 276B；Cap residual type_ptr／alloc 仍 arena）；param 名禁 `region` 关键字（否则 -E 静默丢函数体）；seed cold twins under #ifndef FROM_X；删 residual + STALE／inventory／leaf residual；present **41→40**；dual-export ban（pipeline_abi T）。mac：prefer thin+rest pipeline_abi + gen-x pipeline_x + pure-ld；rv42／opt102／hello0／si0／fs42／nested62；nm T Cap on pipeline_abi。下刀：elf_ctx／elf_write／wpo；pipeline_x mega。PLATFORM: SHARED freestanding type pool Cap leave. |
| 2026-08-08 | **feat(8.3.2)** block_tree Cap residual pure-owned leave (wave269) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu L2 SSH 待补）。B′ G.7：`pipeline_asm_block_tree.c` host-cc leave — `asm_sum_block_local_slot_bytes`／`asm_count_block_stack_slots`／`asm_sum_block_array_temp_bytes`／`asm_sum_block_wa_temp_bytes`／`asm_ctx_fill_locals_block_tree` + fixed i32[256] BSS DFS → runtime_pipeline_abi pure（#[no_mangle]）；GrowVec host DFS retired；seed cold twins under #ifndef FROM_X；删 residual + DEPS／STALE／inventory；present **42→41**；dual-export ban（pipeline_abi T）。mac：prefer thin+rest pipeline_abi + gen-x pipeline_x + pure-ld；rv42／opt102／hello0／string0／si0／five231／four10／nested62／wa0／wn10；nm T Cap on pipeline_abi。下刀：elf_ctx／elf_write／wpo；pipeline_x mega。PLATFORM: SHARED freestanding block_tree Cap leave. |
| 2026-08-08 | **feat(8.3.2)** slot_bytes Cap residual pure-owned leave (wave268) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu L2 SSH 待补）。B′ G.7：`pipeline_asm_slot_bytes.c` host-cc leave — `asm_local_slot_bytes`／`asm_fixed_array_total_bytes_mod`／`asm_ctx_ensure_block_locals` + named／mod helpers → runtime_pipeline_abi pure（#[no_mangle]）；Cap residual slot_reg_offset／simd spelling／SOA+layout typeck glues；block_tree 仍 host-cc 消费 pure faces（extern）；seed cold twins under #ifndef FROM_X；删 residual + DEPS／STALE／inventory；present **43→42**；dual-export ban（pipeline_abi T · pipeline_x U）。mac：prefer thin+rest pipeline_abi + gen-x pipeline_x + pure-ld；rv42／opt102／hello0／string0／si0／five231／four10／nested62／fs42；nm T Cap on pipeline_abi。下刀：elf_ctx／elf_write／block_tree／wpo；pipeline_x mega。PLATFORM: SHARED freestanding slot_bytes Cap leave. |
| 2026-08-08 | **feat(8.3.2)** asm_locals Cap residual pure-owned leave (wave267) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu L2 SSH 待补）。B′ G.7：`pipeline_asm_locals.c` host-cc leave — AsmLocalSlot (136B LE) + AsmBlockSlot parallel i32 maps keyed by AsmFuncCtx* (64 slots) + `asm_ctx_local_*`／`block_slot_*`／`pipeline_asm_local_offset_c` → runtime_pipeline_abi pure（#[no_mangle]）；Cap residual find_offset_scoped 仍 bootstrap_glue；slot_bytes pure (wave268)／block_tree 仍消费 pure faces；seed cold twins under #ifndef FROM_X；删 residual + DEPS／STALE／inventory；present **44→43**；dual-export ban（pipeline_abi T · pipeline_x U）。mac：prefer thin+rest pipeline_abi + gen-x pipeline_x + pure-ld；rv42／opt102／hello0／string0／si0／nested22／many36／block_five231／block_nested62／block_four10／shadow42；nm T Cap on pipeline_abi。下刀：elf_ctx／elf_write／asm slot·block_tree·wpo；pipeline_x mega。PLATFORM: SHARED freestanding asm_locals Cap leave. |
| 2026-08-08 | **feat(8.3.2)** struct_layout Cap residual pure-owned leave (wave266) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu L2 SSH 待补）。B′ G.7：`ast_pool_struct_layout.c` host-cc leave — StructLayout multi-module malloc map + fields/tp + `pipeline_module_struct_layout_*`／next_field_offset／type_ref_byte_size／storage_reset｜release → runtime_pipeline_abi pure（#[no_mangle]；168B layout LE；header num_struct_layouts@16 soft-sync）；sizing pure glue_type_* + emit module cell；lifecycle reset／release 钩 pure；seed cold twins under #ifndef FROM_X；删 residual + DEPS／STALE／inventory；present **45→44**；dual-export ban。mac：prefer thin+rest pipeline_abi + gen-x pipeline_x + pure-ld；rv42／opt102／hello0／string0／si0／while42／struct42／pack42／soa42／nest42／generic42／export_struct42；nm T Cap on pipeline_abi · pipeline_x U pure faces。下刀：elf／asm locals·slot·block_tree·wpo；pipeline_x mega。PLATFORM: SHARED freestanding struct_layout Cap leave. |
| 2026-08-08 | **feat(8.3.2)** top_level Cap residual pure-owned leave (wave265) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu L2 SSH 待补）。B′ G.7：`ast_pool_top_level.c` host-cc leave — TopLevelLetEntry multi-module malloc map + `pipeline_module_top_level_let_*`／name_is_const／hoist／hoist_target／sum_stack／storage_reset｜release → runtime_pipeline_abi pure（#[no_mangle]；148B entry LE；header num_top_level_lets@12 soft-sync）；hoist Cap faces：`pipeline_block_append_let` + `pipeline_block_stmt_order_prepend_lets`（prepend 去 static）；lifecycle reset／release 钩 pure；seed cold twins under #ifndef FROM_X；删 residual + DEPS／STALE／inventory；present **46→45**；dual-export ban。mac：prefer thin+rest pipeline_abi + gen-x pipeline_x + pure-ld；rv42／opt102／hello0／string0／si0／while42／tl42／tl2=12／const_only7／const_expr10／enum0；nm T Cap on pipeline_abi · pipeline_x U pure faces。下刀：elf／struct_layout；pipeline_x mega。PLATFORM: SHARED freestanding top_level Cap leave. |
| 2026-08-08 | **feat(8.3.2)** module_import Cap residual pure-owned leave (wave263) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu L2 SSH 待补）。B′ G.7：`ast_pool_module_import.c` host-cc leave — wave110 pure 已权威 ImportEntry multi-module map + `pipeline_module_import_*` full set／storage_release（#[no_mangle]；340B entry LE）；删 Cap XLANG_WEAK GrowVec residual；seed cold twins under #ifndef FROM_X；same-TU pure face decls（ast_pool + forwarders）；DEPS／STALE／inventory；present **48→47**；dual-export ban。mac：prefer thin+rest pipeline_abi + try-gen-x pipeline_x + pure-ld；rv42／opt102／hello0／string0／mf42／while3／if7／si0／imp_call0／const_select_alias0／many_sel9／ta0；nm T Cap on pipeline_abi · pipeline_x U pure faces。下刀：elf／ast_pool enum·top_level·struct_layout；pipeline_x mega。PLATFORM: SHARED freestanding module_import Cap leave. |
| 2026-08-08 | **feat(8.3.2)** module_enum Cap residual pure-owned leave (wave264) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu L2 SSH 待补）。B′ G.7：`ast_pool_module_enum.c` host-cc leave — ModuleEnumEntry multi-module malloc map + `pipeline_module_enum_*`／try_mark_enum／storage_reset｜release → runtime_pipeline_abi pure（#[no_mangle]；33932B entry LE；header num_module_enums@64 soft-sync）；lifecycle reset／release 钩 pure；seed cold twins under #ifndef FROM_X；删 residual + DEPS／STALE／inventory；present **47→46**；dual-export ban。mac：prefer thin+rest pipeline_abi + gen-x pipeline_x + pure-ld；rv42／opt／hello／string／import／while／if／enum 探针；nm T Cap on pipeline_abi · pipeline_x U pure faces。下刀：elf／top_level／struct_layout；pipeline_x mega。PLATFORM: SHARED freestanding module_enum Cap leave. |
| 2026-08-08 | **feat(8.3.2)** type_alias Cap residual pure-owned leave (wave262) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu L2 SSH 待补）。B′ G.7：`ast_pool_type_alias.c` host-cc leave — TypeAliasEntry multi-module malloc map + `pipeline_module_type_alias_*`／`num_type_aliases_at`／storage_reset｜release → runtime_pipeline_abi pure（#[no_mangle]；136B entry LE）；lifecycle reset／release 钩 pure；seed cold twins under #ifndef FROM_X；删 residual + DEPS／STALE／inventory；present **49→48**；dual-export ban。mac：prefer thin+rest pipeline_abi + heat pipeline_x + pure-ld；rv42／opt102／hello0／string0／mf42／while3／if7／ta42／type_alias fixture0／const_select_alias0；nm T Cap on pipeline_abi · pipeline_x U pure faces。下刀：elf／ast_pool；pipeline_x mega。PLATFORM: SHARED freestanding type_alias Cap leave. |
