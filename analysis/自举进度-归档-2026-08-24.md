# 自举进度归档 · 2026-08-24

> **归档日期**：2026-08-25（24 日条目从 [自举进度.md](自举进度.md) §6 迁出）
> **钉盘**：仍 **`e8176cbe5`**（**不升钉**；升钉在 23 日归档）
> **条目范围**：日切重写起点 · Stage2／WPO／零 make／假权威诚实 DOC 族 · b04 invoke · std-config c · NL-04 · generic_layout · vscode 0.2 · import async ·（25 日初 std-config .x on-demand 留主文档）· **64** 行
> **当日 tip 跨度**：日切 docs → LANG-002 `04ce31129`（末刀 24 日；25 日初 `f36a20909` 见主文档）

## §6 变更记录（24 日 · 倒序）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-08-24 | **fix(selfhost)** **LANG-002 import async scheduler_reset 硬绿** · tip **`04ce31129`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`import("std.async").scheduler_reset` → mangled `std_async_scheduler_reset`，unique 表仅 placeholder／drain_idle＋formal_surface 缺 T → BLD001；存储＝`labi_od_async_sym_*`／`formal_surface.c`／闸 TSV；消费＝LANG-002／cookbook。**修**：G.7 补 formal T＋unique 表 count 2→3（seed＋`.x` 同 commit）；闸 observe→run。证：双端 `-o`＋run OK＋gate OK＋L2 **5／5**（Darwin `/tmp/import_async_probe2.log`／`/tmp/lang_import_hard.log`／`/tmp/darwin_l2_import_async.log`；Ubuntu `/tmp/ubuntu_import_async_probe.log`／`/tmp/ubuntu_lang_import_hard.log`／`/tmp/ubuntu_l2_import_async.log`）。预存延后：L6 unused／soft T001／`.x` std_config on-demand／slot dep-max。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED unique async c_face。 |
| 2026-08-24 | **fix(selfhost)** **TOOL-009 vscode 0.2.0＋ENG-005 VERSION 硬绿** · tip **`d39b3e092`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝无根 `VERSION`＋`package.json`＝0.1.0 → 闸观测 SKIP；存储＝`VERSION`／`editors/vscode/package.json`／闸；消费＝TOOL-009／ENG-005／portable。**修**：G.7 权威根 `VERSION=0.2.0` 与 package／lock／README 同步；闸硬失败（撤 SKIP；无 node 仅 SKIP pack）；archive honesty 闭口。证：双端 VERSION sync OK＋L2 **5／5**（Darwin grammar_ok=8／vsix_ok=1／skip=0 `/tmp/vscode020_hard.log`／`/tmp/eng005_hard.log`／`/tmp/darwin_l2_vscode020.log`；Ubuntu grammar_ok=8／vsix SKIP=no-node `/tmp/ubuntu_vscode020_gate.log`／`/tmp/ubuntu_eng005_gate.log`／`/tmp/ubuntu_l2_vscode020.log`）。预存延后：import async／L6 unused／soft T001／`.x` std_config on-demand／slot dep-max。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED archaeology／IDE。 |
| 2026-08-24 | **fix(selfhost)** **CORE-001 generic_layout -o 硬绿（asm size_of／align_of fold）** · tip **`242ad78ed`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝默认 `-backend asm` 零参泛型跳过 mono → 裸 `core_types_size_of`／`align_of` → BLD001 UNDEF（`-E` host-C 已折叠）；存储＝`pipeline_asm_emit_call_elf_c`／seed；消费＝CORE-001 闸／`-o`。**修**：G.7 补 `try_fold_size_align_of_call_elf`（布局 glue size／align → imm w0；import mangle 前）；闸 `-o` 硬失败；seed＋`.x` 同 commit。证：双端 gate generic=1／skip=0＋L2 **5／5**（Darwin `/tmp/darwin_l2_gl.log`／`/tmp/gl_gate2.log`；Ubuntu `/tmp/ubuntu_gl_gate.log`／`/tmp/ubuntu_l2_gl.log`）。预存延后：vscode 0.2／soft T001／`.x` std_config on-demand／slot dep-max 产品入链。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED asm call fold。 |
| 2026-08-24 | **fix(selfhost)** **NL-04 freestanding fs 硬绿（烟测局部序＋slot dep-max）** · tip **`f1ae1971a`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝import STRUCT_LIT `PageMmapHeap` metrics 瘦槽 16 vs dep 字段 offset 0/8/16 写 24B 砸坏更早局部（path）→ open 失败 exit=5；存储＝`pipe_local_slot_bytes_mod`／wave268／烟测；消费＝NL-04 闸。**修**：G.7 slot 取 max(emit,dep)＋raise 到 stored-offset；烟测 `h` 先于 `path[]`（邻域 L 证）；hybrid 仍 thin-first（seed-first Ubuntu 掉 diag）。证：Ubuntu `XLANG_NOLIBC_FS_FAIL=1` gate OK＋**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_nl04.log`；Ubuntu `/tmp/ubuntu_nl04_gate.log`／`/tmp/ubuntu_l2_nl04.log`）。预存延后：generic_layout -o／vscode 0.2／soft T001／`.x` std_config on-demand／slot dep-max 产品入链。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED slot／LINUX fs smoke。 |
| 2026-08-24 | **fix(selfhost)** **std-config c smoke 硬绿（fs_open_read_c＋env prefix）** · tip **`14bf36a30`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝① 缺短名 `fs_open_read_c`（std/fs mangle）＋闸未链 `runtime_io_abi`／`link_abi_getenv`；② 烟测 `env_key` 无 NUL＋`prefix_len=9`→键 `_debug`；③ Ubuntu `process.o`＋glue 双链 multiple definition。存储＝`runtime_io_abi`／`config.x`／闸。消费＝STD-086／119。**修**：G.7 补 `fs_open_read_c`（seed 同 commit）＋闸链活面；烟测 NUL／len=10；去 process 双链；`.x` on-demand 观测 SKIP。证：双端 c_smoke=1／yaml c=1＋L2 **5／5**（Darwin `/tmp/darwin_std_config_min.log`／`/tmp/darwin_l2_std_config2.log`；Ubuntu `/tmp/ubuntu_std_config_gate.log`／`/tmp/ubuntu_l2_std_config.log`）。预存延后：NL-04／generic_layout -o／vscode 0.2／soft T001／`.x` std_config on-demand。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED io ABI／LINUX gold link。 |
| 2026-08-24 | **fix(selfhost)** **b04 Linux invoke 硬绿（write_stdout）** · tip **`dd9250f6a`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝invoke 烟测调退役 `sys.os_write_stdout`＝XT001 硬红；存储＝`tests/sys/linux_syscall_invoke_smoke.x`；消费＝b04／B-14 闸。**修**：G.7 对齐活面 `sys.write_stdout`；闸拒调用点复活＋Linux invoke 硬闸（撤观测）；软 T001 table_available dep typeck 残余不挡 `-o`。证：Ubuntu invoke OK＋b04 OK＋**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_b04_invoke.log`；Ubuntu `/tmp/ubuntu_b04_invoke_hard.log`／`/tmp/ubuntu_l2_b04_invoke.log`）。预存延后：std-config c smoke／NL-04 exit=5／generic_layout -o／vscode 0.2／soft T001。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: LINUX invoke／SHARED gate。 |
| 2026-08-24 | **fix(selfhost)** **eng／exc／doc-07／bootstrap／phase-d DOC 假权威诚实** · tip **`5cde55f98`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝ENG-001–007／EXC-002–004/006／DOC-07*／自举前必须清单／D-01–D-05 硬要已删顶层 DOC＝portable 假红；存储＝门／TSV／archive；消费＝portable。**修**：G.7 有则补全 → DOC→`analysis/archive/{eng,exc,doc,narrative,phase}/`；拒顶层复活；inventory→`labi_invoke_ld_list.x`／`build_xlang_asm.sh`；cirrus／VERSION／exc runnable 观测 SKIP。证：eng+doc-07 **11／11**＋exc **4／4**＋phase-d／checklist DOC＋**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_eng_exc_doc_honesty.log`；Ubuntu `/tmp/ubuntu_l2_eng_exc_doc_honesty.log` spot **18／18**）。预存延后：std-config c smoke／b04／NL-04／generic_layout -o／vscode 0.2。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **剩余 core-* DOC／codegen.c 假权威诚实** · tip **`efe9c0094`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝CORE-004/005/007–012/014/015／STD-131 硬要已删顶层 DOC／`codegen.c`＝portable 假红；存储＝门／TSV／archive；消费＝portable。**修**：G.7 有则补全 → DOC→`analysis/archive/core/`（strview→`archive/std/`）；活锚 `codegen.x`＋`core/{builtin,mem}/mod.x` 纯 .x；映射 helper 改验 `.x` 符号；拒顶层 DOC／`codegen.c` 复活；观测 SKIP＝check 烟测（暂停）、`__builtin_*` emit、fmt `to_buf` typeck。证：gate **11／11**＋**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_core_rest_honesty.log`；Ubuntu `/tmp/ubuntu_l2_core_rest_honesty.log`）。预存延后：eng-*/exc-*/doc-07／bootstrap 清单／phase-d/f／std-config c smoke／b04／NL-04。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **lang-option／result／generic／const-eval＋core／stbl／obs 假权威诚实** · tip **`6f9007575`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝LANG-003/006/009/010／CORE-002/003/006/016／STBL-001/002/003／OBS-004 硬要已删顶层 DOC／`typeck.c`／`codegen.c`／`parser.c`／`typeck_generic_struct.c`＝portable 假红；存储＝门／TSV／archive；消费＝portable。**修**：G.7 有则补全 → DOC→`analysis/archive/{lang,core,stbl,obs,std,eng}/`；mono→`codegen_emit_mono_mangled_name`／`codegen_collect_generic_struct_mono_combos`；CTFE→`typeck_expr_is_const_with_module_consts`／`typeck_fold_expr`；Result demangle→`typeck_check_expr_try_propagate`；拒复活；观测 SKIP＝check 烟测（暂停）、const-eval runnable 残、STBL-001 Tier-S 符号漂移、std.io `stdin_ptr_slice`／`print` 退役。证：gate **11／11**＋**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_lang_core_stbl_honesty.log`；Ubuntu `/tmp/ubuntu_l2_lang_core_stbl_honesty.log`）。预存延后：其余顶层 DOC 门／std-config c smoke／b04／NL-04／generic_layout -o。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **tool-scaffold／lint／fmt／vscode＋lang-trait／abi／import 假权威诚实** · tip **`75dde7a93`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝TOOL-001/002/006/009／LANG-002/004/005 硬要已删顶层 DOC／`typeck.c`／`parser.c`＝portable 假红；存储＝门／TSV／archive；消费＝portable。**修**：G.7 有则补全 → DOC→`analysis/archive/{tool,lang}/`；活锚 `fmt.x`／`lsp_diag.h`／`typeck.x`／`parser.x`；`find_trait_def`→`xlang_trait_check_impls_complete_c`；padding→`typeck_validate_struct_layouts_zero_padding`；拒复活；观测 SKIP＝vscode VERSION／0.2.0＋vsix、L6 unused-hint（check 暂停）、import async UNDEF。证：gate **7／7**＋**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_tool_lang_honesty.log`；Ubuntu `/tmp/ubuntu_l2_tool_lang_honesty.log`）。预存延后：lang-option／result／generic／core-iterator／stbl／obs／std-config c smoke／b04／NL-04。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **tool-lsp／lifetime／core-types／exc 假权威诚实** · tip **`bdb9526de`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝TOOL-003/004／LANG-008／CORE-001/013／EXC-005 硬要已删 `lsp_diag.c`／`typeck.c`／`codegen.c`＋顶层 DOC＝portable 假红；存储＝门／TSV／archive；消费＝portable。**修**：G.7 有则补全 → DOC→`analysis/archive/{tool,lang,core,exc}/`；LSP→`lsp_diag.h`＋`runtime_lsp_glue`；typeck／codegen→`.x`；拒 c 复活；删顶层双权威 lifetime DOC；check 烟测观测 SKIP。证：gate **6／6**＋**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_tool_lsp_lifetime_core_honesty.log`；Ubuntu `/tmp/ubuntu_l2_tool_lsp_lifetime_core_honesty.log`）。预存延后：其它顶层 DOC 门／std-config c smoke／b04／NL-04／generic_layout -o。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **tool-pkgmgr／no-libc／incr-compile 假权威诚实** · tip **`b7236d327`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝TOOL-007/008／STBL-004／OBS-001／COMP-007／NL-01/05/06/07 硬要已删 monofile＋顶层 DOC＋死 `get_std_fs_o_path`／`lsp_diag.c`／`bench/loop_i32.x`＝portable 假红／软绿；存储＝门／TSV／archive／pkg lock；消费＝portable。**修**：G.7 有则补全 → DOC→`analysis/archive/{tool,phase,comp,obs,stbl}/`；resolve→`runtime_pipeline_abi.h`；std prelink→`labi_path_pure`（`xlang_rel_o_path_from_argv0`）；LSP→`lsp_diag.h`；fixture→`examples/hello.x`；pkg lock digests 刷新；OBS check 烟测观测 SKIP；拒 monofile／`lsp_diag.c` 复活。证：gate **7／7**＋**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_pkgmgr_nolibc_incr_honesty.log`；Ubuntu `/tmp/ubuntu_l2_pkgmgr_nolibc_incr_honesty.log`）。预存延后：其它 monofile／`lsp_diag.c` 门／std-config c smoke／b04 invoke typeck／NL-04 fs exit=5。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **comp-wpo／tool-debug／lang-feature／b20／win 假权威诚实** · tip **`8194d8e8f`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝TOOL-005／COMP-004／LANG-001／B-20／COMP-011 硬要已删 monofile＋顶层 DOC＝portable 假红／软绿；存储＝门／TSV／archive；消费＝portable。**修**：G.7 有则补全 → DOC→`analysis/archive/{tool,lang,comp}/`；strip/NDEBUG→`labi_invoke_cc_list`；WPO dump/reach→`codegen.h`；defines→`runtime_driver_abi`；needs_*→`labi_freestanding`；windows/lld-link→`rt_run_asm_backend`＋`runtime_link_abi`；拒复活；edition 包装 `export XLANG`（Ubuntu 无裸 `xlang`）；产品钩子（Darwin dead_strip／WPO_DUMP／COFF CG002）观测 SKIP。证：gate **7／7**＋**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_comp_wpo_tool_debug_honesty.log`；Ubuntu `/tmp/ubuntu_l2_comp_wpo_tool_debug_honesty.log`）。主刀 `3d130e25e`＋XLANG `8194d8e8f`。预存延后：tool-pkgmgr／no-libc DOC／incr-compile／std-config c smoke／b04 invoke typeck。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **e03／e04／e05／f06＋e-soft 簇假权威诚实** · tip **`a01e4a432`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝E-03/04/05／F-06／e-soft／C-03/04/05/06/09 硬要已删 monofile＋顶层 DOC＋已删 Makefile＝假红／软绿；存储＝门／TSV／archive／mk；消费＝portable／e-soft 聚合。**修**：G.7 有则补全 → DOC→`analysis/archive/phase/`；e04 塌成 `runtime_*_abi`＋`mk/driver_seed_*`；e03 PREPROCESS_LINK_O；e05 rt_* NO_C；f06 link_abi；e-soft＋C 委托同向；g02f／link-harden 拒复活；c04 `-E-extern` NO_C 跳过。证：gate **15／15**＋**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_e_soft_honesty.log`；Ubuntu `/tmp/ubuntu_l2_e_soft_honesty.log`；Ubuntu link-harden Type=DYN）。预存延后：comp-wpo／tool-debug／lang-feature／std-config c smoke／b04 invoke typeck。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **c08／b30 假权威诚实** · tip **`37f727054`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝C-08／B-30（+B-04/05/31）硬要已删 `runtime.from_x.c`＋顶层 phase DOC＋已删 Makefile＝portable 假红；存储＝门／TSV／archive DOC；消费＝portable／CI。**修**：G.7 有则补全 → DOC→`analysis/archive/phase/`；RUNTIME→`rt_*`／`runtime_io_abi`／`runtime_link_abi`；DRIVER_SUBCMD→`mk/driver_subcmd_objs.mk`；build→`./xbuild`；`main_entry` ABI；b04 Linux invoke 观测延后（typeck 另债）。证：gate OK＋**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_c08_b30_honesty.log`；Ubuntu `/tmp/ubuntu_l2_c08_b30_honesty.log`）。预存延后：其它 monofile 门／e04／f06／comp-wpo／tool-debug／std-config c smoke。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **boot-014 假权威诚实** · tip **`0cdfa36d1`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝BOOT-014 硬要已删 `runtime.from_x.c`＋已删 `Makefile`＋死 `get_*_o_path`＝portable 假红；存储＝门／TSV／archive DOC；消费＝portable／契约。**修**：G.7 有则补全 → RUNTIME＝labi 五切片并集；MAKEFILE→`mk/std_and_panic_objs.mk`；死 getter=`-`；活 `xlang_runtime_*` 保留；on-demand-only glues→`std_on_demand`；core_mem 针→`core_mem_mem_copy`；json always 烟测硬闸；async／core_mem on_demand 观测延后（mangle／ensure 另债）。证：gate OK＋**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_boot014_honesty.log`；Ubuntu `/tmp/ubuntu_l2_boot014_honesty.log`）。预存延后：其它 monofile 门／std-config c smoke。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **boot-016 假权威诚实** · tip **`28dfed180`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝BOOT-016 硬要已删 `runtime.from_x.c`＋死 `get_*_o_path`＋旧裸 C 锚点＝portable 假红；存储＝门／TSV／archive DOC；消费＝portable／Top-N nm。**修**：G.7 有则补全 → RUNTIME＝`labi_std_list`＋`labi_ondemand_list` 并集；getter=`-`（E-04）；锚点→活导出 `std_*`／`path_sep_c`；DOC §2／§3 同向。证：gate **12／12**＋**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_boot016_honesty.log`；Ubuntu `/tmp/ubuntu_l2_boot016_honesty.log`）。预存延后：cookbook／std-config c smoke／其它仍指 monofile 的门。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **comp-link-sym fixture 假权威诚实** · tip **`f40fb165f`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝COMP-008 cases／manifest／DOC 硬指死 `duplicate_parser_x.log`＋`undefined_wpo` 仍写退休 `run_sx_pipeline_impl`＋`*.log` gitignore 藏全套 fixture＝portable 假红；存储＝TSV／archive DOC／fixtures；消费＝门／归因烟测。**修**：G.7 有则补全 → `duplicate_parser_sx.log`；wpo 对齐活面 `run_x_pipeline_impl`；`.gitignore` 例外＋七 fixture 入库。证：gate OK＋**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_comp_link_sym_honesty.log`；Ubuntu `/tmp/ubuntu_l2_comp_link_sym_honesty.log`）。预存延后：boot-016 seed／cookbook typeck／std-config c smoke。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **phase-f／comp／std／phase3 死归档 DOC＋NEXT／Makefile 假权威诚实** · tip **`cbf6fc2fb`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝phase-f／std／comp 归档兄弟／phase3 硬要已归档顶层 DOC＋死 `NEXT.md`／死 `Makefile`＝假红（phase-f 默软绿假权威）；存储＝门／TSV／archive DOC；消费＝portable／考古卫生。**修**：G.7 有则补全 → DOC→`analysis/archive/{phase,std,comp,other-tickets}/`；phase3 任务／§→archive DOC（拒 `NEXT.md`；live＝`自举进度.md`）；phase-f makefile→`xbuild`；isel micro→`r01_`／`a01_`／`r10_`；std-bytes `len`→`length`。证：phase3 全波＋isel／std-cli／bytes／f-cli 绿＋**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_phase_f_comp_std_honesty.log`；Ubuntu `/tmp/ubuntu_l2_phase_f_comp_std_honesty.log`）。预存延后：boot-016 seed／cookbook typeck／comp-link-sym fixture／std-config c smoke。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：预存债择优。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **BOOT-015～028／DOC public-roadmap 死归档 DOC＋NEXT 假权威诚实** · tip **`c6ff9835b`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝BOOT-015～028／DOC-005＋q*／cookbook／perf／d06 硬要已归档顶层 DOC＋死 `NEXT.md`／死 `Makefile`／把历史季度锚钉在已前滚 live 路线图＝假红；存储＝门／TSV／archive DOC；消费＝portable／考古卫生。**修**：G.7 有则补全 → DOC→`analysis/archive/{boot,doc,phase,comp,perf,zc}/`；路线图＝`自举进度.md`（拒复活）；Phase3 §→archive phase3；q1／q2／q4 接受 live 前滚＋季度 DOC 权威；Makefile xref→`xbuild`。证：邻域门绿＋**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_boot_doc_honesty2.log`；Ubuntu `/tmp/ubuntu_l2_boot_doc_honesty2.log`）。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：phase-f／comp／std 死归档 DOC。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **DOC-002／BOOT 死归档 DOC＋NEXT.md 假权威诚实** · tip **`26ae51b1f`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝DOC-002／BOOT-003/004/005/011 硬要已归档顶层 boot／eng DOC＋死 `NEXT.md`（portable 假红）；BOOT-011 拉 BOOT-012／COMP-001／dogfood 同债；dogfood 仍指已改名 `bench/loop_i32.x` 等。存储＝门禁／归档 DOC／dogfood 映射；消费＝portable／BOOT-011／考古卫生。**修**：G.7 有则补全 → DOC→`analysis/archive/{boot,eng,comp,perf}/`；路线图＝`自举进度.md`（拒 `NEXT.md` 复活）；micro→`r01_`／`m03_`／`r10_`／`a01_`；Darwin FAIL_REG=0 SKIP bench（Linux 金标）。证：**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_boot_doc_honesty.log`；Ubuntu `/tmp/ubuntu_l2_boot_doc_honesty.log`）＋DOC-002／BOOT-011 OK。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：邻域非 mega（其它死归档 DOC／`NEXT.md` 门）。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **feb／boot／hotpath 死 mega 假权威诚实** · tip **`e8c64f5f3`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝feb-contract／boot-force-stub 硬要已删 `pipeline_glue.c`／`ast_pool.c`＋顶层 DOC 已归档／typeck-hotpath 硬要已删 `pipeline_typeck_soa.c`＋DOC／e-extern 仍要 `#include pipeline_glue.c`（emit 已 no-op＝假 FAIL／软绿）；存储＝门禁／tsv／归档 DOC；消费＝bootstrap-ci／考古卫生。**修**：G.7 有则补全 → DOC→`analysis/archive/...`；B6／PARSER_STUB_EQ／hot_reorder／soa→`runtime_pipeline_abi`／`typeck.x`；拒复活；e-extern 禁再注入 include；boot check_only 自举期 SKIP。证：**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_feb_boot_honesty.log`；Ubuntu `/tmp/ubuntu_l2_feb_boot_honesty.log`）＋feb／boot／hotpath gate OK。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：邻域非 mega。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **测试／IDE 死 mega 假权威诚实** · tip **`972c64de9`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝b06 硬要已删 `ast_pool.c`（sit-red）／regalloc·quality 硬要已删 `pipeline_glue.c`＋归档 DOC 仍指旧路径／spill 硬要已归档 `自举前必须清单`／parser-safe-helper 对已删 mega 静默 SKIP＝假绿／l1 Docker `touch ast_pool.c` 造化石／IDE preamble＋s2·s3 文案仍写活权威；存储＝门禁脚本／tsv／preamble；消费＝bootstrap-ci／portable／考古卫生。**修**：G.7 有则补全 → b06 改盯 `runtime_pipeline_abi`＋`ast_pool_*`＋拒复活；regalloc／quality／spill DOC→`analysis/archive/...`＋符号→abi.x；safe-helper 跑 thin-delegate 审计；l1 touch `pipeline.x`／`typeck.x`；IDE／s2·s3 诚实。证：**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_test_gate_mega_honesty.log`；Ubuntu `/tmp/ubuntu_l2_test_gate_mega_honesty.log`）＋b06／safe-helper／spill OK。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：邻域非 mega（feb-contract／boot-force-stub 等）。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **backend.x L1200+ 乱码注释英文化** · tip **`13e4515b0`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`backend.x` L1200+（及 L1190 漏网）mojibake／断行中文 docblock；存储＝注释；消费＝G.9 卫生／可读性。**修**：还原语义后英文化（import／AsmFuncCtx／arch emit／fold／while·for／labels／SKIP stub）；整文件乱码标记 **0**。证：**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_backend_mojibake_l1200p_en.log`；Ubuntu `/tmp/ubuntu_l2_backend_mojibake_l1200p_en.log`）。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：邻域非 mega（其它死引用／考古文案）。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **backend.x L1–1200 乱码注释英文化** · tip **`725e0ee3f`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`backend.x` L1–1200 mojibake／乱码中文 docblock；存储＝注释；消费＝G.9 卫生／可读性。**修**：latin1 还原后英文化（诊断／enc／AsmFuncCtx／import）；删 L25 重复乱码。证：**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_backend_mojibake_en.log`；Ubuntu `/tmp/ubuntu_l2_backend_mojibake_en.log`）。残留 L1200+ ≈96。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：backend L1200+ 乱码／其它死引用。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **typeck_cap／gen＋backend.x mega 注释诚实** · tip **`df9f1f889`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`typeck_cap_residual`／`typeck_gen` 仍写「ast_pool.c L…／pipeline_glue.c L…」当现权威；`backend.x` 乱码 docblock 点名已删 mega；存储＝注释；消费＝考古卫生／双权威误导。**修**：G.7 有则补全 → 改盯 `runtime_pipeline_abi`（historical L* left wave309）；backend 英文化。证：**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_typeck_backend_comment_auth.log`；Ubuntu `/tmp/ubuntu_l2_typeck_backend_comment_auth.log`）。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：邻域非 mega（backend 其余乱码等）。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **`.x`／seed 死 glue／ast_pool 注释假权威诚实** · tip **`ad85d5596`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝活面 `.x`／seed／abi.h／AGENTS／build 仍写「G.7 authority／见／强定义在」已删 `ast_pool.c`／`pipeline_glue.c`；存储＝注释／协作准则；消费＝考古卫生／双权威误导。**修**：G.7 有则补全 → 改盯 `runtime_pipeline_abi`／`pipeline.x`／`backend_seed_mega_fallback` seed；历史 L 脚注／已诚实 left-wave309 文案保留。证：**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_x_seed_glue_comment_auth.log`；Ubuntu `/tmp/ubuntu_l2_x_seed_glue_comment_auth.log`）。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：邻域非 mega（typeck_cap／gen 历史 L 等）。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **perl gen 死 glue／ast_pool 假权威诚实** · tip **`0d2f3ff6b`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`fix_pipeline_extern_gen_c` 仍末尾再注入 `#include "pipeline_glue.c"`；`hoist_pipeline_prototypes`／`fix_slim_arena inject_*` 对已删 glue 静默 `-f`／空转；文案仍写 `ast_pool.c`。存储＝gen 补丁／Stage2／glue_types 路径；消费＝考古卫生／假 include。**修**：G.7 有则补全 → 停再注入（仅剥离残留）；hoist／slim 拒收化石 glue；锚点改盯 `runtime_pipeline_abi`／`pipeline.x`。证：**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_perl_glue_fake_auth.log`；Ubuntu `/tmp/ubuntu_l2_perl_glue_fake_auth.log`）＋本地 strip／refuse 探针。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：邻域非 mega（`.x`／seed 死注释等）。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **thin-delegate 审计假权威诚实** · tip **`2a56877eb`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`audit_parser_thin_delegate`／`rebuild_parser_emit_heavy_ast_pool` 仍挂已删 `ast_pool.c`（＋已缺 `parser_asm_thin_c.c`）→ FileNotFoundError／无诚实文案；活表已在 `runtime_pipeline_abi.x`。存储＝审计／重建脚本；消费＝考古卫生。**修**：G.7 有则补全 → 审计改盯 `asm_parser_func_is_thin_delegate`／`asm_parser_m8_tail_thin_delegate_c_name`（长度＋parser.x＋集合一致；116／116／0）；rebuild 诚实退役 exit 1（禁写 `.base` 化石）。证：**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_thin_delegate_fake_auth.log`；Ubuntu `/tmp/ubuntu_l2_thin_delegate_fake_auth.log`）＋双端 audit OK／rebuild RETIRED。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：邻域非 mega（perl gen 死文案等）。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **audit／compile_commands 假权威诚实** · tip **`f28c77c83`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`audit_static_limits` 仍 `rg compiler/ast_pool.c`（已删＝静默 `MODULE=256`＋sentinel 恒 0-hit 仍 OK＝假绿）；`gen_compile_commands` 静态库仍列已删 `pipeline_glue.c`／`ast_pool.c`；bear 盲 `make`（MF 已删）。存储＝审计输出／IDE compile_commands；消费＝上限门／clangd。**修**：G.7 有则补全 → MODULE＝`pipe_en_max_variants`（`runtime_pipeline_abi.x`）；sentinel 诚实退役；撤 glued 死路径；bear+make 仅 MF 恢复；`ast.h` 注释对齐。证：**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_audit_fake_auth.log`；Ubuntu `/tmp/ubuntu_l2_audit_fake_auth.log`）＋ Ubuntu audit OK（grep／无 rg；`f28c77c83`）。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：邻域非 mega（其他死引用）。PLATFORM: SHARED archaeology。 |
| 2026-08-24 | **fix(selfhost)** **ensure／g05／strict_glue 假权威诚实** · tip **`211ba618c`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`ensure_host_cc` glue_standalone freshness 仍列已删 `ast_pool.c`／`pipeline_glue.c`；`force_thin`／strict_glue abi 仍 `[ Makefile -nt ]`（wave941 已删＝永不触发）；g05 skip 门控已删路径→诚实消息永不印；strict info 仍写「←ast_pool」。存储＝ensure／g05／strict 门；消费＝热路径／考古。**修**：G.7 有则补全 → freshness＝types.inc only；`force_thin` 命名空操作；abi＝seed only；g05 永久 skip 文案；撤假 info。证：**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_ensure_fake_auth.log`；Ubuntu `/tmp/ubuntu_l2_ensure_fake_auth.log`）。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：selftest／audit Makefile 假权威等邻域。PLATFORM: SHARED ensure。 |
| 2026-08-24 | **fix(selfhost)** **SYMS／glue_standalone 死 `-nt` 诚实** · tip **`a3656f256`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝SYMS 导出／glue_standalone ensure（build／strict_glue／experimental）仍以已删 `ast_pool.c`／`pipeline_glue.c` 做 `-nt`（永不触发＝假权威）；存储＝export txt／glue `.o` 门；消费＝strict partial／experimental LD。**修**：G.7 有则补全 → 撤死 `-nt`；权威＝生产者 `.o`／seed／glue_types／extract 脚本；`scripts/` 内该模式清零。证：**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_syms_glue_dead_nt.log`；Ubuntu `/tmp/ubuntu_l2_syms_glue_dead_nt.log`）。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：邻域非 mega。PLATFORM: SHARED ensure。 |
| 2026-08-24 | **fix(selfhost)** **pipeline_x ensure 死 `-nt` 诚实** · tip **`9f086dabf`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`ensure_pipeline_x_o_fresh`（build／strict_glue 双孪生）＋ experimental `ensure_pipeline_x_fresh_for_ast_pool` 仍以已删 `ast_pool.c`／`pipeline_glue.c` 做 `-nt`（永不触发＝假权威）；存储＝`pipeline_x.o` 重建门；消费＝strict_glue／build／experimental bootstrap。**修**：G.7 有则补全 → 撤死 `-nt`；权威＝`PIPELINE_X_DEPS`／`src/pipeline/pipeline.x`（try-gen-x）；**禁**误盯 `runtime_pipeline_abi.x`（错生产者；stub `.o` 会 forever need=1）。abi／WPO 仍归前波 `ensure_experimental_ast_pool_for_wpo`。证：**双端** L2 **5／5**（Darwin `/tmp/darwin_l2_pipeline_x_fresh_nt.log`；Ubuntu `/tmp/ubuntu_l2_pipeline_x_fresh_nt.log`）。残：SYMS／glue_standalone 等死 `-nt`。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：邻域非 mega。PLATFORM: SHARED ensure。 |
| 2026-08-24 | **fix(selfhost)** **WPO experimental abi 新鲜度** · tip **`d410bad5a`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`ensure_experimental_ast_pool_for_wpo` 仍以已删 `ast_pool.c`／`pipeline_glue.c` 做 `-nt`（永不触发）→ experimental 落后 `ASM_WPO_MAX_FUNCS`／emit_order clamp（前波 tip 优先后仍为第三候选）；存储＝experimental／ARTIFACTS_ONLY；消费＝WPO dogfood 回退编译器。**修**：G.7 有则补全 → 新鲜度＝`runtime_pipeline_abi.x`＋seed／`pipeline_x.o`；ARTIFACTS_ONLY strict_glue 仅 `pipeline_x.o` 更新时重链（禁 abi-mtime 每刀全量 strict）。证：**双端** chain **5／5** · L2 **5／5**（Darwin `/tmp/darwin_wpo_abi_fresh_chain.log`／`/tmp/darwin_l2_wpo_abi_fresh.log`；Ubuntu `/tmp/ubuntu_wpo_abi_fresh_chain.log`／`/tmp/ubuntu_l2_wpo_abi_fresh.log`；T **1995**／strchr **0**）。残：`relink_xlang_asm_strict_glue` 等仍有死 `ast_pool.c` `-nt`。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：邻域非 mega。PLATFORM: SHARED WPO ensure。 |
| 2026-08-24 | **fix(selfhost)** **ASM_WPO_MAX_FUNCS 容量诚实** · tip **`e3a8f1b9f`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`ASM_WPO_MAX_FUNCS=1024` 静默截断（prepare／register 满帽停写）；坐实＝`pipeline_wpo.o` T **恰 1024**（abi 活体 ~1995）。存储＝`g_aw_*`／seed `ASM_WPO_MAX_*`；消费＝WPO dogfood／chain。**修**：G.7 有则补全 → FUNCS **2048**／EDGES **8192**；`.x`＋seed 同 commit；候选序 tip `xlang_asm` 优先（陈旧 experimental 仍 1024 会回灌 ~814KiB）。证：**双端** Darwin `__text` **36864→71820**／Ubuntu **24576→47880**（T **1024→1995**／strchr **0**）· chain **5／5** · L2 **5／5**（Darwin `/tmp/darwin_wpo_chain_cap.log`；Ubuntu `/tmp/ubuntu_wpo_cap_verify.log`）。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：邻域非 mega。PLATFORM: SHARED WPO leave。 |
| 2026-08-24 | **fix(selfhost)** **abi WPO 真压缩** · tip **`099812edf`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`pipeline_asm_wpo_pgo_emit_order_prepare` 在 `n≥ASM_WPO_MAX_FUNCS(1024)` 仍 `n++`、数组停写；`emit_n=n` 后 `order_at` 读越界邻接 BSS → 再 emit ~971× `T _strchr`（Darwin `__text`~814KiB／Ubuntu~1.5MiB 假体积）。存储＝`g_aw_pgo_emit_order[1024]`／seed twin；消费＝`pipeline_wpo.o`／WPO chain／strict helpers。**修**：G.7 有则补全 → 仅写入时 `n++`；`order_at` 容量界；ELF 环 skip extern（backend.x＋seed mega）；soft cap **98304**。证：**双端** Darwin **813664→36864**／Ubuntu **1584358→24576**（T_strchr **0**）· chain **5／5** · L2 **5／5**（Darwin `/tmp/darwin_wpo_chain_compress.log`；Ubuntu `/tmp/ubuntu_wpo_compress_verify.log`）。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：邻域非 mega。PLATFORM: SHARED WPO leave。 |
| 2026-08-24 | **fix(selfhost)** **Darwin stubs 双定义收敛** · tip **`571b5c184`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝relink `filter_strict_asm_objs` 漏 Darwin filt／`seed_link_compat`／CI text stub 排除（build 已有）；终链又硬链 full stubs.o → 与 filt／runtime_asm_build 双权威（28 multiply_defined）。存储＝`build_asm/*.o` glob＋`ST_STRICT_GLUE_STUBS_O`；消费＝WPO strict_link Darwin。**修**：G.7 有则补全 → skip 表对齐 build；Darwin 只链 `bstrict_strict_glue_stubs_darwin.o`；CI ≤64B text stub skip。证：**双端 FAIL=1 全绿**（Darwin `/tmp/darwin_stubs_dual_lift3.log`；Ubuntu `/tmp/ubuntu_stubs_dual_lift.log`）· L2 **5／5**。残：abi WPO 未压缩。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：abi WPO 真压缩。PLATFORM: DARWIN filt／SHARED filter twin。 |
| 2026-08-24 | **fix(selfhost)** **strict_link 0-symbol／假 N/A 真抬** · tip **`a9eb8fefe`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`pipeline.x` 纯 extern→空 `pipeline.o`；relink `asm_strict_pipeline_selfhosted` 仍查 stub；rebuild 仍编 `pipeline.x`；门禁硬 Darwin N/A（假「parser partial」）；0-symbol 硬错。存储＝`relink_xlang_asm_strict_glue`／ensure／gate；消费＝WPO strict_link dogfood。**修**：G.7 有则补全 → selfhosted＝abi；空 stub／WPO 重叠 soft-skip；rebuild 源＝abi＋heavy=0；helpers 在 abi 在链时跳过；async／arm64 enc 入链；gate 撤假 N/A＋FAIL=0 捕获 relink。证：**Ubuntu FAIL=1 全绿**（`/tmp/ubuntu_strict_link_lift.log`）· Darwin soft（`/tmp/darwin_strict_link_soft2.log`）· 双端 L2 **5／5**。残：Darwin 终链 stubs 双定义；abi WPO 未压缩。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：stubs 双定义／abi 压缩。PLATFORM: SHARED。 |
| 2026-08-24 | **fix(selfhost)** **双端 WPO pipeline_wpo 5／5（abi 源）** · tip **`9ae94ae04`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝wave335 后 `pipeline.x` 纯 extern（0 体）→ asm exit-0 空 `.o`；编排体在 `runtime_pipeline_abi.x`；WPO 仍编空头＝假债。存储＝`rebuild_pipeline_wpo_o`／ensure／chain／baseline；消费＝WPO dogfood。**修**：G.7 有则补全 → 源＝abi；SHARED `EMIT_HEAVY=0` 先试；soft cap（Darwin~814KiB／Ubuntu~1.5MiB）；typeck Linux max **8192**；抬五模块硬门。证：双端 chain **5／5**（Darwin `/tmp/darwin_wpo_chain5b.log`；Ubuntu `/tmp/ubuntu_wpo_chain5c.log`）· L2 **5／5**。残：strict_link 0-symbol；abi 未真压缩。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：strict_link／abi 压缩。PLATFORM: SHARED。 |
| 2026-08-24 | **fix(selfhost)** **Darwin WPO dogfood partial 4／5** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝门禁硬 N/A＋`entry` 符号过时（产品 ABI＝`main_entry`）＋Mach-O nm `_` 前缀＋entry smoke 依赖空 `pipeline.x`→候选编译器空；存储＝ensure／chain／rebuild；消费＝WPO dogfood。**修**：G.7 有则补全 → `wpo_nm_has_main_cli_entry`／`(_)?sym`；抬 ensure／chain／full；Darwin pipeline 软残；typeck `EMIT_HEAVY=0` 先试；backend reach min＝4。证：main **52252**／driver **496**／typeck **9792**／backend **144** · chain／full OK（`/tmp/darwin_wpo_chain2.log`／`/tmp/darwin_wpo_full.log`）· L2 **5／5**。残：pipeline 空发射＋strict_link。禁假绿五模块全绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：pipeline.x 空发射。PLATFORM: SHARED 门禁／MACOS partial。 |
| 2026-08-24 | **fix(selfhost)** **`driver_compile_link.o` 真 EMIT_HEAVY** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝bridge／selfhosted 强制 `SKIP_DRIVER_EMIT_HEAVY=1`＋`ensure_driver_compile_link_obj` 裸 `ld -r` 撞 F7 双 `LC_SEGMENT`（Apple「more than one LC_SEGMENT」）→ Stage2 假 SKIP／假 OOM 叙事；存储＝`build_asm/driver_compile_{emit_heavy,link}.o`；消费＝strict `STRICT_LINK_BUILD_ASM_DRIVER`／Stage2 门禁。**修**：G.7 有则补全 → merge 权威 `pure_ld_partial_merge`；bridge **不再**强制 SKIP（仍 SKIP WPO／main）；Darwin filter／strict_glue 展 ar→eh+alias MH。证：`OK via ./xlang_asm (__text=14180B, link.o ready)` · Stage2 双遍 EXIT=0（`/tmp/stage2_darwin_emit_heavy.log`）· L2 **5／5**（`/tmp/mac_l2_emit_heavy.log`）。残：Darwin WPO chain／strict_link 仍 N/A。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：Darwin WPO dogfood。PLATFORM: SHARED merge／MACOS libtool＋MH expand。 |
| 2026-08-24 | **fix(selfhost)** **Darwin Stage2 pure-asm hello／struct_mk** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝Stage2 链 prefer／libtool **ar** `user_asm` 在 weak stubs／bridge 之后 → 档案成员未抽出 → `_asm_asm_codegen_elf_o` 落 36／68B weak −1（CG002 `code_len=0`）；缺 `backend_arm64_enc_c.o`。存储＝`build_xlang_asm` BSTRICT／`verify-selfhost-stage2-bstrict`；消费＝Step3／4／4b。**修**：G.7 有则补全 → Darwin seed host-cc **MH_OBJECT**＋`filter_o_export`；EARLY 先于 stubs；arm64 enc 入链；stubs omit `asm_asm_codegen_*`；`host.o` 剔出 ASM_TRY；harness 开 pure-asm hello／smk；EMIT_HEAVY WARN 诚实 SKIP。证：双代 codegen **1812**／enc **612** · rv **42／42** · hello **Hello** · smk **10** · hash match · EXIT=0（`/tmp/stage2_darwin_pureasm_gate.log`）；L2 **5／5**。残：WPO Darwin N/A；EMIT_HEAVY 仍 SKIP。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：WPO／真 EMIT_HEAVY。PLATFORM: DARWIN BSTRICT／SHARED Stage2。 |
| 2026-08-24 | **fix(selfhost)** **Darwin Stage2 postlink ALLOW＋HASH_STRICT 默认=1** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`xlang_asm_postlink_smoke` Darwin 选 `-backend c` 却未 export `XLANG_ALLOW_HOST_CC=1` → BLD001 `host-cc-requires-allow`（WARN「strict `-o` broken」假叙事）；HASH 默认 Darwin track-only（STRICT=0）与 tip 已 match 分叉。存储＝postlink／`verify-selfhost-stage2-bstrict` Step4c；消费＝Stage2 build 尾／A-09。**修**：G.7 有则补全 → postlink 孪生 `run_xlang_asm_smoke`／Step3 export ALLOW；Darwin／Linux 统一 `_s2_hash_strict` 默认 **1**（`STRICT=0` 逃生）。证：SKIP_BOOTSTRAP Stage2 双遍 **`postlink_smoke: OK`** · SHA256 **`42ffac6e…`**／5094888 match · 行为 **42／42** · EXIT=0（`/tmp/stage2_darwin_postlink_hash.log`）；L2 **5／5**（`/tmp/mac_l2_postlink_allow.log`／`/tmp/mac_l2_post_stage2_postlink.log`）。残：hello／struct_mk／WPO Darwin N/A；`driver_compile_link.o` EMIT_HEAVY WARN。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：Darwin Mach-O hello／EMIT_HEAVY。PLATFORM: SHARED postlink／HASH_STRICT。 |
| 2026-08-24 | **docs(selfhost)** **Darwin Stage2 全量 E2E 真定点 dogfood** | **文档** · **不升钉** · **日常 L2**。五问：产生＝前波已收 UNDEF／`__text=0B`／同配方 gen1／atoi；消费＝`verify-selfhost-stage2-bstrict` Darwin 全量。证：SKIP_BOOTSTRAP tip soft g05 → Step1＋2 **`asm_only_strict`** · gen1／gen2 **5094888B** · SHA256 **`42ffac6e7d135909ffe9c9fe297327647d71e87db8b7a5432eba0ab566f202a6`** match · 行为 **42／42**（`-backend c`＋ALLOW_HOST_CC）· Step5 refresh OK · EXIT=0（`/tmp/stage2_darwin_e2e_full.log`）；产品 L2 **5／5**（`/tmp/mac_l2_pre_darwin_stage2.log`／`/tmp/mac_l2_post_darwin_stage2.log`）。残：round2 `postlink_smoke` WARN（strict `-o`；非闸门）；hello／struct_mk／WPO 仍 Darwin N/A。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：postlink_smoke／HASH_STRICT 默认抬升。PLATFORM: DARWIN Stage2／SHARED 文档。 |
| 2026-08-24 | **fix(stage2)** **atoi_stub 稳定 TU 名 → Ubuntu Stage2 SHA256 真定点** · tip **`1ac895e51`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`ensure_atoi_stub_obj` 用 `/tmp/atoi_stub_$$.c`；gcc 把 basename 嵌进最终 ELF `.strtab`；拓扑收敛后 gen1／gen2 仅差 PID 五字节（646045≠745671）。存储＝`bootstrap_nostdlib_shared.sh`；消费＝Stage2 hash-gate。**修**：G.7 有则补全 → `./atoi_stub_tu.c`。证：Ubuntu hash **OK** SHA256=`0ae06666dade10b24b878e0d4d800426a208f3e5abf309045a0205e5ef9b83d6` bytes=5760344＋`cmp` 全同（`/tmp/stage2_atoi_stable_ubuntu.log`）；行为 **42／42**＋struct_mk **10**；双端 soft g05＋L2 **5／5**（mac `/tmp/mac_l2_matrix_atoi.log`；Ubuntu `/tmp/xlang_l2_matrix_stage2_atoi_ubuntu.log`）。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：Darwin Stage2 全量 E2E。PLATFORM: LINUX atoi_stub／SHARED Stage2。 |
| 2026-08-24 | **fix(selfhost)** **Stage2 gen1 同配方 asm_only_strict（拓扑收敛）** · tip **`3532df206`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝Step1 冻结 g05 产品链（~4.8M／LSP 面）当 gen1，Step2 round2 真 `asm_only_strict`（~5.5M／libc 面）→ 诚实拓扑红；存储＝`verify-selfhost-stage2-bstrict`；消费＝hash-gate。**修**：G.7 有则补全 → 共享 `stage2_build_asm_only_strict`；Step1 同配方物化 gen1（非 g05 freeze）。证：双轮 **5.5M／10190** 同符号集／同 section 尺寸；残留仅 `atoi_stub_<PID>.c` 五字节（见下行）。日志 `/tmp/stage2_topology_converge_ubuntu.log`。禁假绿／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED Stage2。 |
| 2026-08-24 | **fix(selfhost)** **Ubuntu Stage2 hash 假 fixed-point 诚实刀** · tip **`d01dfef31`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`xlang_asm_sync_stage1_from_strict` 在 `XLANG_ASM_BOOTSTRAP_ROUND2=1` 仍把 gen2 盖回 `xlang_asm_stage1` → Step4c 比对 gen2≡gen2（假 SHA256 绿）；消费＝`verify-selfhost-stage2-bstrict`／A-09。**修**：G.7 有则补全 → ROUND2 **skip sync**；Step1 冻结 `xlang_asm_gen1_for_hash`；hash-gate 报告 topology-fork 字节差。证：Ubuntu Step2 **`asm_only_strict`**＋skip sync；行为 **42／42**＋struct_mk **10**；hash 诚实红 gen1 **4937360** ≠ gen2 **5739776**（`/tmp/stage2_hash_honesty_ubuntu.log`）；先前假绿 match `/tmp/stage2_hash_tip_ubuntu.log`。双端 soft g05＋L2 **5／5**（`/tmp/g05_stage2_hash_honesty_*.log`／`/tmp/xlang_l2_matrix_stage2_hash_honesty_*.log`）。禁假绿金标／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：g05↔round2 拓扑收敛。PLATFORM: SHARED Stage2／hash-gate。 |
| 2026-08-24 | **fix(selfhost)** **Stage2 pipeline `__text=0B`（runtime_pipeline_abi G.7）** · tip **`881283463`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`107d09af2` 自举判定改查 `runtime_pipeline_abi.o`，但 STRICT_TRY 仍要求 stub `pipeline.o` `__text>200`；promote 拷空 `pipeline_x.o`；下游仍从 stub／ar 抽 `run_x_pipeline_impl`／`preprocess_if_stack_*`（UNDEF／Missing -arch）；FILTERED 4B CI text stub 触发 Apple ld 多弱符号。存储＝`rebuild_pipeline_o_second_pass`／STRICT_TRY／`ensure_pipeline_runtime_bootstrap_partial`／`ensure_preprocess_if_stack_provider`／`filter_strict_asm_objs`。消费＝Stage2 Step2 B-strict。**修**：G.7 有则补全 → abi 自举即过门；skip 已在 LD argv 的 partial；strict 跳过纯 CI text stub。证：Darwin Step2 **`asm_only_strict`**（`/tmp/stage2_pipeline_text0_probe_darwin2.log`）；双端 soft g05＋L2 **5／5**（Darwin restore `/tmp/g05_pipeline_text0_restore_darwin.log`／`/tmp/xlang_l2_matrix_pipeline_text0_darwin.log`；Ubuntu `/tmp/g05_pipeline_text0_ubuntu.log`／`/tmp/xlang_l2_matrix_pipeline_text0_ubuntu.log`）。禁假绿全量 Stage2 hash／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：Ubuntu hash STRICT 拓扑。PLATFORM: SHARED build_xlang_asm。 |
| 2026-08-24 | **fix(selfhost)** **Darwin Stage2 stubs filt（ar→MH_OBJECT＋filter_o_export）** · tip **`dc555af5b`** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝prefer／libtool 把 `runtime_driver_strict_glue_stubs.o` 留成 **ar**；`ld_partial_export` 无 `-arch` 且不能滤 ar → filt 失败；历史回退**整段丢掉** stubs → Stage2 round2 UNDEF `codegen_set_*`／`pipeline_block_labeled_set_names`。存储＝`ensure_bstrict_darwin_strict_glue_stubs_filt_obj`；消费＝experimental／strict Darwin 链。**修**：G.7 有则补全 → ar 则强制 `cc -c` MH_OBJECT；滤走权威 `filter_o_export --omit-sym asm_driver_*`；filt 失败保留 unfiltered stubs（禁再 drop）。证：Step2 探针 experimental **链接过**（三项 UNDEF **0**；`/tmp/stage2_step2_undef_probe_darwin.log`）；残留 **`pipeline.o` 第二遍 `__text=0B`**（另债）。双端 soft g05＋L2 **5／5** @ `dc555af5b`（`/tmp/g05_stage2_undef_*.log`／`/tmp/xlang_l2_matrix_stage2_undef_*.log`；Darwin restore `/tmp/g05_stage2_undef_restore_darwin.log`）。禁 mega／升钉／假绿 Stage2 E2E。钉盘仍 **`e8176cbe5`**。下波：pipeline `__text=0B`／Ubuntu hash 拓扑。PLATFORM: DARWIN filt／SHARED build_xlang_asm。 |
| 2026-08-24 | **docs(selfhost)** **Stage2 bstrict 全量 E2E 诚实 dogfood（双端）** | **文档** · **不升钉** · **日常 L2**。五问：产生＝产品活 Stage2＝`verify-selfhost-stage2-bstrict`（X 路径 NO_C 已诚实 soft-skip）；存储＝round2 `build_xlang_asm`＋hash gate；消费＝CI／工程轨 Stage2。证：Darwin Step2 experimental link **UNDEF** `codegen_set_dep_slots_for_x_pipeline`／`codegen_set_preamble_has_core_option_result`／`pipeline_block_labeled_set_names` → `xlang_asm missing after second pass`（`/tmp/stage2_bstrict_darwin.log`）；Ubuntu 行为 **42／42**＋struct_mk **10**＋hash STRICT **MISMATCH**（stage1 4801584 ≠ stage2 5350512；`/tmp/stage2_bstrict_ubuntu.log`）。软刀 wave309 standalone skip @ `a6e8e05c0`（不顶 UNDEF／hash）。双端 soft g05＋L2 **5／5** @ `a6e8e05c0`（`/tmp/g05_stage2_bstrict_*.log`／`/tmp/xlang_l2_matrix_stage2_bstrict_*.log`；Ubuntu `/tmp/g05_stage2_bstrict_ubuntu2.log`）。禁假绿 SHA256／禁 mega／升钉。钉盘仍 **`e8176cbe5`**。下波：Darwin round2 UNDEF／Ubuntu hash 拓扑。PLATFORM: SHARED Stage2。 |
| 2026-08-24 | **fix(selfhost)** **skip retired pipeline_glue_standalone seed（Stage2 build）** · tip **`a6e8e05c0`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝wave309 退役 `seeds/pipeline_glue_standalone.from_x.c` 后 `ensure_asm_pipeline_glue_standalone_obj` 仍 `-E`／`cc_inc_tu` → Stage2 round2 missing-seed 噪；存储＝`build_xlang_asm.sh`；消费＝Stage2／strict 考古。**修**：G.7 有则补全 → seed 缺 early-return（孪 g05_ensure／experimental_bootstrap／strict_glue）；禁复活 mega glue／禁顶 Darwin UNDEF。证：`bash -n` **OK** · Darwin soft g05＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED build_xlang_asm。 |
| 2026-08-24 | **docs(日切)** **23 号归档 + 24 号主文档重写** | **文档** · 钉仍 **`e8176cbe5`**（**不升钉**）。①`analysis/自举进度-归档-2026-08-23.md`：§6 收 23 日 **44** 行（升钉 `e8176cbe5` ＋ check-gate al06 ＋ labi／stubs unique UNDEF 族 ＋ Darwin CG002／process_merge／minos／g05 ＋ sat L4／fmt pure-asm／modlet `.data` ＋ ALLOW ensure 空 ＋ g05 stubs scan ＋ Ubuntu tip L4）。②本文日切重写：KPI／前排保留 · §6 **仅 24 日**（asm_seed_full 恢复阶梯 → capture／seed／experimental／stage2／VIA_MAKE／错误文案／Stage2 链接卫生 → tests 残／深层裸 make → 零 make post_ship → Stage2 X E2E 诚实门禁）。下一刀：Stage2 **bstrict** 全量 E2E dogfood（产品活 Stage2；mega 硬禁须点名）。PLATFORM: SHARED 文档。 |
| 2026-08-24 | **fix(selfhost)** **Stage2 X E2E 诚实门禁（产品 NO_C／ALLOW_HOST_CC）** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝G-02a 删 C frontend 后产品 `driver_x_emit_try_extern_via_cparser` 固定 BLD001，`cparser_impl` 体已无；存储＝`verify-selfhost-stage2` 仍假定 `-x -E -E-extern`；消费＝CI／工程轨 Stage2 X → 必红或空 gen。另 Step5 Darwin `-backend c` 缺 `ALLOW_HOST_CC`（bstrict 已有）。**修**：G.7 有则补全 → probe soft-skip（`REQUIRE_X_EMIT=1` 硬失败）；Step5 export ALLOW＝bstrict 孪生；活 Stage2＝bstrict。禁假跑通／禁 mega／升钉。证：Darwin／Ubuntu soft-skip **0**＋REQUIRE **1**＋soft g05＋L2 **5／5** @ `9a2ff18d2`（`/tmp/stage2_x_e2e_*.log`／`/tmp/g05_stage2_x_e2e_*.log`／`/tmp/xlang_l2_matrix_stage2_x_e2e_*.log`）。钉盘仍 **`e8176cbe5`**。下波：mega 硬禁须点名；可续 Stage2 bstrict 全量 dogfood。PLATFORM: SHARED Stage2。 |
| 2026-08-24 | **fix(selfhost)** **零 make 门禁 post_ship（MF phys-del 后再扫）** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`run-product-path-zero-make-gate` 仍假定 `compiler/Makefile`／root Makefile 在→grep thin-call 必红；存储＝门禁＋leaf／ensure 地板；消费＝CI／日常 0-make 宣称。**修**：G.7 有则补全 wave948 twin → `_MF_PRESENT` 门控 thin-call；shell／catalog／mk 权威；leaf dump 落盘避 pipefail／SIGPIPE；地板对齐 mk（std_x **17**／formal_mod **59**／driver_leaf **9**／R1 misc **8**／seed-map **4**／glue suffix **1**／SOURCE_DEPS **16**）；ENDGAME／STILL_MAKE post_ship＝1／0。禁第二套门禁／禁 mega／升钉。证：Darwin／Ubuntu soft g05＋L2 **5／5**＋gate OK @ `5042669b0`（`/tmp/g05_zero_make_gate_*.log`／`/tmp/xlang_l2_matrix_zero_make_gate_*.log`／`/tmp/xlang_zero_make_gate_*.log`）。钉盘仍 **`e8176cbe5`**。下波：mega 硬禁须点名；可续 Stage2 X 全量 E2E dogfood。PLATFORM: SHARED 门禁／脚本。 |
| 2026-08-24 | **fix(selfhost)** **tests 深层裸 make → shell／xbuild（a09／g06／compress／perf）** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝MF phys-del 后 `run-linux-a09` 仍裸 `make` 编 net／runtime；`run-g06-bootstrap-docker` 仍 `make clean`／`pipeline_gen`／seed；`run-compress`／`std-compress*` 仍 `make compress-o-*`；perf BT05／wpo-docker 仍裸 make。存储＝tests／hub；消费＝Docker 冷启动／compress 门禁／perf 矩阵 → 无 MF 必红。**修**：G.7 有则补全 → hub `compress-o-*` no-op＋`sqlite-o-stub`；a09→`xlang_compiler_make`；g06→`clean_compiler`＋`ensure_lsp_pipeline_gen`＋try-heat＋`bootstrap_driver_seed`；perf→`./xbuild compiler-make`；wpo→`compiler_all_ci`／try-heat；manifest／gate target→hub；die／echo→hub；禁第二套／禁 mega／升钉。证：`bash -n`＋hub compress-o **OK** · Darwin soft g05＋L2 **5／5**（`/tmp/g05_tests_deep_make_darwin.log`／`/tmp/xlang_l2_matrix_tests_deep_make_darwin.log`）· Ubuntu soft g05＋L2 **5／5** @ `54ace50f0`（`/tmp/g05_tests_deep_make_ubuntu.log`／`/tmp/xlang_l2_matrix_tests_deep_make_ubuntu.log`）。钉盘仍 **`e8176cbe5`**。下波：mega 硬禁须点名；可续 Stage2 全量 E2E／零 make 再扫。PLATFORM: SHARED tests。 |
| 2026-08-24 | **fix(selfhost)** **tests 残裸 make → shell／xbuild** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝MF phys-del 后 `run-l1-relink-fast` 仍裸 `make`／`make -n` 刮链接行；`run-b20` 仍 `make -C`；大量 f-* 门禁已走 hub 却 die「make … failed」。存储＝tests 脚本；消费＝L1 快路径／b20／bstrict 门禁 → 无 Makefile 必红或误导。**修**：G.7 有则补全 → l1 INNER＝`ensure_host_cc try-heat`＋`g05`／`bootstrap_driver_seed`；b20＝`xlang_compiler_make`；die／echo→`xlang_compiler_make`／`./xbuild`；禁第二套 make 图／禁 mega／升钉。证：`bash -n` **OK** · Darwin soft g05＋L2 **5／5**（`/tmp/g05_tests_make_darwin.log`／`/tmp/xlang_l2_matrix_tests_make_darwin.log`）· Ubuntu soft g05＋L2 **5／5** @ `bae2b6e82`（`/tmp/g05_tests_make_ubuntu.log`／`/tmp/xlang_l2_matrix_tests_make_ubuntu.log`）。钉盘仍 **`e8176cbe5`**。下波：mega 硬禁须点名；可续 Stage2 全量 E2E／深层 tests make。PLATFORM: SHARED tests。 |
| 2026-08-24 | **fix(selfhost)** **Stage2 X dogfood 链接卫生（g05 平台面＋filter omit-sym）** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`verify-selfhost-stage2` 硬编码 Darwin `crt0_arm64`／`-multiply_defined`／裸 `ld -r -exported_symbols_list`（MF 删后与 g05 平台表分叉；Linux 必红）。存储＝Stage2 链接行／filter；消费＝Stage2 X dogfood E2E。**修**：G.7 有则补全 → `g05_relink_env` 导出 MAIN_LINK／DUP／USER_ASM；Stage2 eval；`filter_o_export --omit-sym`；禁第二套平台表／禁 mega／升钉。证：`bash -n` **OK** · filter omit-sym 烟雾 **OK** · Darwin soft g05＋L2 **5／5**（`/tmp/g05_stage2_e2e_darwin.log`／`/tmp/xlang_l2_matrix_stage2_e2e_darwin.log`）· Ubuntu soft g05＋L2 **5／5** @ `33b33fc7f`（`/tmp/g05_stage2_e2e_ubuntu.log`／`/tmp/xlang_l2_matrix_stage2_e2e_ubuntu.log`）。钉盘仍 **`e8176cbe5`**。下波：mega 硬禁须点名；可续 Stage2 全量 E2E。PLATFORM: SHARED Stage2／g05 faces。 |
| 2026-08-24 | **fix(selfhost)** **错误文案 make→shell／xbuild（＋烟雾引号／SKIP 0-make）** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝MF phys-del 后脚本仍 hint「须先 make …」；`bootstrap_{token_lexer,parser}_smoke` 未闭合引号语法死；`bootstrap_driver_seed` SKIP 缺 TARGET 仍嵌套 `make`。存储＝scripts 文案／SKIP 分支；消费＝人／Agent／考古入口 → 误导或无 MF 必红。**修**：G.7 有则补全 → hint→`./xbuild`／`scripts/*.sh`；闭引号；SKIP 缺 TARGET 落 shell 冷体（VIA_MAKE＋MF 仅逃生）；禁第二套／禁 mega／升钉。证：`bash -n` **OK** · SKIP 有 TARGET 软退 **OK** · Darwin soft g05＋L2 **5／5**（`/tmp/g05_errtext_darwin.log`／`/tmp/xlang_l2_matrix_errtext_darwin.log`）· Ubuntu soft g05＋L2 **5／5** @ `ba750a280`（`/tmp/g05_errtext_ubuntu.log`／`/tmp/xlang_l2_matrix_errtext_ubuntu.log`）。钉盘仍 **`e8176cbe5`**。下波：mega 硬禁须点名。PLATFORM: SHARED scripts。 |
| 2026-08-24 | **fix(selfhost)** **build_xlang_asm VIA_MAKE 残留（解 MF 门控 shell ensure）** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝Makefile phys-del 后 `ensure_asm_experimental_lsp_objs` 无 MF 早退 gen_driver、`ensure_asm_gen_driver_x_objs` 把 try-heat 锁在 MF 分支（无 MF 走 raw `cc -c`）、`ensure_gen_driver_typeck_companion_objs` 无 MF 静默跳过；存储＝`build_xlang_asm.sh`；消费＝experimental lsp／gen_driver／typeck companions → 无 Makefile 错路径或漏 ensure。**修**：G.7 有则补全 → shell try-heat／migrate 为默认；VIA_MAKE＋MF 仅逃生；撤 MF 门控 else raw cc；禁第二套／禁 mega／升钉。证：无 MF 探针 typeck／gen-x-driver **OK** · Darwin soft g05＋L2 **5／5**（`/tmp/g05_via_make_darwin.log`／`/tmp/xlang_l2_matrix_via_make_darwin.log`）· Ubuntu soft g05＋L2 **5／5** @ `49b867a63`（`/tmp/g05_via_make_ubuntu.log`／`/tmp/xlang_l2_matrix_via_make_ubuntu.log`）。钉盘仍 **`e8176cbe5`**。下波：mega 硬禁须点名。PLATFORM: SHARED build_xlang_asm。 |
| 2026-08-24 | **fix(selfhost)** **verify-selfhost-stage2 0-make（撤死 make）** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝Makefile phys-del 后 `verify-selfhost-stage2.sh` 仍裸 `make`（bootstrap-driver-seed／build-seed-asm-host／main_driver／fmt_check_cmd_driver）；另截断 `cc … -o` bitrot。存储＝Stage2 X dogfood 脚本；消费＝CI／工程轨 stage2 → 无 Makefile 必红。**修**：G.7 有则补全 → `bootstrap_driver_seed.sh`／`build_seed_asm_host.sh`／`ensure try-heat`＋`try-other-l2-prefer`；撤截断 cc；`build_xlang_asm` 文案→`bootstrap_driver_*.sh`；VIA_MAKE 仅 MF 在时；禁第二套／禁 mega／升钉。证：Darwin soft g05＋L2 **5／5**（`/tmp/g05_stage2_make_darwin.log`／`/tmp/xlang_l2_matrix_stage2_make_darwin.log`）· Ubuntu soft g05＋L2 **5／5** @ `7eaead5d6`（`/tmp/g05_stage2_make_ubuntu.log`／`/tmp/xlang_l2_matrix_stage2_make_ubuntu.log`）。钉盘仍 **`e8176cbe5`**。下波：mega 硬禁须点名。PLATFORM: SHARED stage2／文案。 |
| 2026-08-24 | **fix(selfhost)** **experimental／strict 考古 0-make（撤死 MF-gated make）** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝Makefile phys-del 后 `relink_xlang_asm_experimental_bootstrap`（pipeline／companions／parser／relink-xlang）与 `strict_glue ensure_typeck_x` 仅 MF 在时 `make`、无 shell 回退；`build_xlang_asm ensure_parser_x_o` 无 MF 早退。存储＝考古／strict 脚本；消费＝experimental 重链／strict typeck／parser override → 无 Makefile 静默不重建。**修**：G.7 有则补全 → `try-heat`（PIPELINE_X_FORCE）／`migrate_x_objs`／`driver_leaf`／`g05 --no-sync`；修无 MF 早退；错误文案→`bootstrap_driver_*.sh`；VIA_MAKE 仅 MF 在时；禁第二套 cc／禁 mega／升钉。证：try-heat／migrate **OK** · Darwin soft g05＋L2 **5／5**（`/tmp/g05_arch_exp_darwin.log`／`/tmp/xlang_l2_matrix_arch_exp_darwin.log`）· Ubuntu soft g05＋L2 **5／5** @ `a662586f5`（`/tmp/g05_arch_exp_ubuntu.log`／`/tmp/xlang_l2_matrix_arch_exp_ubuntu.log`）。钉盘仍 **`e8176cbe5`**。下波：mega 硬禁须点名；可续收 `verify-selfhost-stage2` 等。PLATFORM: SHARED 考古脚本。 |
| 2026-08-24 | **fix(selfhost)** **seed／g05／strict_glue 考古 0-make（撤 ungated make）** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝Makefile phys-del 后 `capture_bootstrap_seeds`／`g05_build_xlang_asm FULL`／`gen_bootstrap_gens`／`relink_xlang_asm_strict_glue ensure_pipeline_x_o_fresh` 仍裸 `make`；存储＝考古脚本；消费＝G-06 捕 seed／FULL bstrict／LEGACY gen／strict pipeline_x → 无 Makefile 必红。**修**：G.7 有则补全 → `clean_compiler`＋`bootstrap_driver_seed`／`bootstrap_driver_bstrict`／`legacy_xlang_c_link`／`try-heat pipeline_x`（PIPELINE_X_FORCE）；VIA_MAKE 仅 MF 在时；禁第二套 cc／禁 mega／升钉。证：try-heat pipeline_x **OK** · Darwin soft g05＋L2 **5／5**（`/tmp/g05_arch0make2_darwin.log`／`/tmp/xlang_l2_matrix_arch0make2_darwin.log`）· Ubuntu soft g05＋L2 **5／5** @ `a75e9f76b`（`/tmp/g05_arch0make2_ubuntu.log`／`/tmp/xlang_l2_matrix_arch0make2_ubuntu.log`）。钉盘仍 **`e8176cbe5`**。下波：mega 硬禁须点名；可续收其它考古／错误文案 `make`。PLATFORM: SHARED 考古脚本。 |
| 2026-08-24 | **fix(selfhost)** **capture／link_abi_only 考古 0-make（撤死 make）** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝Makefile phys-del 后 `capture_asm_partial_seed{,_legacy}.sh`／`relink_xlang_asm_link_abi_only.sh` 仍 `make` 编 dispatch／xlang-c／labi／io_stubs；存储＝脚本；消费＝G-06 再捕 partial／考古 relink → 无 Makefile 必红。**修**：G.7 有则补全 → `ensure try-r3-cold`（四 dispatch）／`legacy_xlang_c_link`／`try-labi-prefer`＋`try-r1`；撤独立 `pipeline_abi_f32_xmm.o`（已并 call_dispatch）；禁第二套 cc／禁 mega／升钉。证：强制 `try-r3-cold` 重编 try_inline **OK** · capture reuse seed **OK** · Darwin soft g05＋L2 **5／5**（`/tmp/g05_capture0make_darwin.log`／`/tmp/xlang_l2_matrix_capture0make_darwin.log`）· Ubuntu soft g05＋L2 **5／5** @ `ede1ccde2`（`/tmp/g05_capture0make_ubuntu.log`／`/tmp/xlang_l2_matrix_capture0make_ubuntu.log`）。钉盘仍 **`e8176cbe5`**。下波：mega 硬禁须点名；可续收其它考古 `make`。PLATFORM: SHARED ensure／考古脚本。 |
| 2026-08-24 | **fix(selfhost)** **asm_seed_full -E 失败恢复阶梯（撤 60 err 软噪）** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`asm_seed_full.x` `-E` typeck 红（`expr_kind_ordinal` T001）后旧阶梯**优先**沿用陈旧 `asm_full_gen.c`；存储＝`build_asm/seed_host/asm_full_gen.c`（双声明 `backend_enc_*`：`ElfCodegenCtx*` vs `uint8_t*`）；消费＝`cc -c` → L4 cold **cc FAILED (60 errors)** 软噪，再 source fallback 仍绿。**修**：G.7 单恢复权威＝已有强 partial → SEED_PARTIAL pin → source fallback → **末** OOM 沿用 gen；禁第二套静默吞错／禁 mega／升钉／禁开 backend typeck 大债。证：Ubuntu `-E` 失败路径 **沿用 partial**／**0**×60（`/tmp/build_seed_asm_host_ladder2.log`）· soft g05＋L2 **5／5**（`/tmp/g05_ladder_ubuntu.log`／`/tmp/xlang_l2_matrix_ladder_ubuntu.log`）· Darwin soft g05＋L2 **5／5** @ `ea1ad5dac`（`/tmp/g05_ladder_darwin.log`／`/tmp/xlang_l2_matrix_ladder_darwin.log`）。钉盘仍 **`e8176cbe5`**。下波：mega 硬禁须点名。PLATFORM: SHARED build_seed_asm_host。 |
