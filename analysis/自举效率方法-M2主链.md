# 自举效率方法：M2 主链（M1 工具 · M3 收口）

> **日期**：2026-09-16  
> **决策**：主攻 **M2**（纯 asm / CG002 / mega FORCE）。**M1** 只做 M2 需要的语言/codegen 刀。**M3** 放最后。  
> **枝**：`dev` · 钉盘仍 **`ecdb5cc1e`**（升钉默认不做）  
> **交叉**：终局债 [`C迁移追踪.md`](C迁移追踪.md) · 仪表盘 [`自举进度.md`](自举进度.md) · RFC [`7.2.1-parser-inc-port-ABI-RFC.md`](7.2.1-parser-inc-port-ABI-RFC.md)

日常波次流水仍只写 `自举进度.md` §6。本文是**方法决策**，不是 tip 日记。

---

## 0. 一句话

dest-buffer 工厂（9 月 10–16 日一天 20–44 波）已到**局部最优**。再切 header 不推进阶段 13。  
终局函数是 **host `cc` → 0**，不是「C 算法搬进 `.x`」。对准该函数的主刀是 **M2**。

---

## 1. 效率函数（必须先改对）

| 旧工厂最大化 | 终局要最大化 |
|--------------|--------------|
| 每波一个 compositor dest-buffer | 编译器 TU 不再被 host `cc` 编 |
| 独立 `FROM_X` 门 + C trampoline 持 `name[256]` | asm backend 直接出 `.o` |
| 双端 L2 不红 | 冷启动不 `execve(cc)`（阶段 12–13） |

分清两个「无 C」，禁止混谈：

1. **手写 C = 0**：权威在 `.x`，host 仍可 `cc` `-E` 产物。dest-buffer 服务这个。  
2. **host-cc = 0**（阶段 12–13 宣布自举）：永不 `execve(cc)`。dest-buffer 几乎不碰这个。

typeck 墙（mega `-E`）已关。再为 (1) 加班是爬错的山。

---

## 2. 为何现行 dest-buffer 不再高效

9 月工厂当时正确：无 struct 按值、无 `local u8[N]`、RFC Route B 被认为太大，于是 B-minus + trampoline。

边际收益已掉到接近 0：

- P6h 切完 header，body 仍 C；产品 hello 不走 buf 路径  
- 每波仍 `cc` trampoline；pthin 常因 CG002 走 `-E`+`cc`  
- 剩下没切的全是工厂绕开的墙（栈数组、嵌套 `while`、`STRING_LIT≤64`、P3f、glue_tail、mega assemble）

**继续绕墙切小片 = 用最高熟练度打最低杠杆。**

---

## 3. 三方法

### M1 — 语言墙打包 + parser 整块迁

解开 trampoline 的根：函数内 `u8[N]`/`i32[N]`、嵌套 `while`+`break` 不再 parse-drop、`STRING_LIT` 放宽或诊断走表。  
RFC Route B 的缩小版。单独做完：parser 可删 C 孪生，**产品仍 `cc` mega / pin**。

### M2 — 纯 asm / CG002 / mega FORCE（主刀）

山在：

- `runtime_pipeline_abi.x` 98k（`-E` typeck 已过；assemble / CG002 / 产品 FORCE 未开）  
- `runtime_pipeline_abi.from_x.c` 68k 仍可能 host-cc  
- pthin 多条 lane 因 CG002 / weak polish 走 `-E`+`cc`  
- 前端 `.x` 已在（typeck 25k / codegen 27k），产品仍 pin-first  

CG002 常是缺一类 emit（局部数组、label、按值返回、嵌套控制流），与 M1 的墙同一座山。  
**不打穿 M2，M1 迁再干净也还是 hybrid `cc`，M3 也无法零 cc。**

### M3 — 二进制 stage0 收口

stage0 = 现有双端 pin 蛋（已存在），不是手写最小 asm seed。  
源全 `.x` → stage0 `-backend asm` → stage1 → 再编自己 → v2==v3 → 删 C seed / 删 `labi_invoke_cc`。  
**依赖 M2。** 现在开 M3 = 验的仍是 C 链。

---

## 4. 决策（已拍板）

**主攻 M2。M1 是 M2 的工具，不是并列主战场。M3 是 M2 打穿后的收口。**

单独比效率：**M2 > M1 > M3**（M3 不能先开，故排第三）。

落地序：

```text
M2：CG002 分类 + 小 pthin 纯 asm 站住（不再回落 -E+cc）
        │
        ├─ 缺栈数组 / 嵌套循环 / 某 emit → 插一刀薄 M1，立刻回到 M2
        │
        ▼
M2：mega FORCE，替换 from_x.c（产品 .o 是 asm）
        │
        ▼
M3：pin 蛋当 stage0，产品禁 labi_invoke_cc，v2==v3
        │
        ▼
parser 残体按文件整迁（那时的 M1 收尾）→ 删 C 孪生
```

禁止：

- 再开「一 helper 一波」dest-buffer 当主刀  
- 完整 M1（glue_tail / `parse_type_ref` / `append_byte` 整块迁）抢在 CG002 分类之前  
- 无视分类产品 FORCE 整 mega  
- 先手写最小 asm seed（pin 蛋已能当 stage0）  
- Windows / MSVC / NT 进金标终局  
- 注释-only 波；eq / 覆盖戏

以前工厂时代的禁令（P3f dest-buffer `parse_type_ref`、`append_byte`、glue_tail、mega FORCE）**不是终局护栏**。在语言墙 / emit 类未开时硬开仍会红；分类证明需要时再开，且须根修产生点。

---

## 5. 本波起怎么干（M2 第一步）

1. **分类 CG002**（最小复现，按 emit 类记账，禁止对着 2554 函数盲 FORCE）  
2. **一类一刀**：先清挡住大文件 typeck 的类，再碰 stretch CG002，最后 mega FORCE  
3. mega 仍 **禁产品默认 FORCE**，直到该类在小文件上复现绿  

验证：日常 **L2**（Darwin + Ubuntu）；不跑 `xlang check`；L4 仅整波 / 谈自举。  
SHARED 改动双端。Windows：`ssh windows-server` 对齐 SHA，不跑本地 xlang。

### 5.1 现场分类（2026-09-16 · Darwin `xlang_asm` `-backend asm -c`）

产品 hello 纯 asm 已绿。小/中 pthin **多数已能 emit**（glue / unary / binop / primary / ctrl / helpers / lex_skip / …）。

| 类 | 文件 | 现象 | 根因层 | 下一刀 |
|----|------|------|--------|--------|
| **A T001 extern 须 `unsafe`** | `pthin_fn_block.x` ✅；`pthin_type_ref.x` ✅；`pthin_skip_tl.x` ✅ 双端 PREFER_ASM 入链。Darwin 债是 Apple 栈 i32 自然对齐（非 bound_scan 装参）。 | Darwin ARM64 入链 | codegen param-home | 类 A 收；禁盲 FORCE mega |
| **B CG002** | `pthin_stretch.x` | 表 `u8[360]`／COUNT=36。standalone `-c` 绿。**Ubuntu P9b 入链 L2 5/5**。g05 P9b try 已开。 | 源表尺寸（已修）＋P9b | class B 收 |
| **C P001 空文件** | `pthin_foundation.x` | parse 0 functions | 桩，非产品活面 | 跳过 |
| **D mega 产品 skip** | `runtime_pipeline_abi.x` | `pure_asm_x_to_o` 基名禁 | 产品完整性 / Cap residual | 小 thin 类 A 绿后再点名 FORCE |
| **E mega thin 类 A** | …；**w408** fixed_array_copy MACOS PREFER／LINUX BAN；**w407** binop_block_peel MACOS PREFER／LINUX BAN；**w406** w157_sum HARD BAN tip reinject（BRANCH26）；**w405** binop_stack_spill_try_reload PREFER 双端；**w404/404b** binop_var_slot_cache HARD BAN tip reinject（BRANCH26／SEGV）；**w403** assign MACOS PREFER／LINUX BAN；**w402** param_ptr_slot MACOS PREFER／LINUX BAN；**w401** field_load_sz MACOS PREFER／LINUX BAN；**w400** slot_bytes MACOS PREFER／LINUX BAN；**w399** fnptr_array_esz PREFER 双端；**w398** unused_hints PREFER 双端；**w397** type_to_c_repr MACOS PREFER／LINUX BAN；**w396** wpo_dump PREFER 双端；**w395** Ubuntu heal w383 green pabi＠2675520（opt=102；禁 tip type_pool reinject）；**w394/394b** mega 三叶 split＋Darwin PREFER／LINUX BAN（EM:0）；**w393d** mega LOOP split（emit_one；Ubuntu tip -c CG002）；**w392** Type LE root map；**w390/391** module_import＋bootstrap_glue HARD BAN reinject 双端；**w389** mega_body HARD BAN reinject 双端；**w388** parse_orch HARD BAN reinject 双端；**w387** emit_index HARD BAN reinject 双端；**w386** module_enum HARD BAN reinject 双端；**w385** module_func HARD BAN reinject 双端；**w384** value_abi HARD BAN reinject 双端；**w383/383b** type_pool PREFER 双端＋HARD BAN tip force-reinject；**w382** elf_ctx HARD BAN reinject 双端；**w381** parser_result HARD BAN reinject 双端；**w380** Cap A HARD BAN reinject 双端（typeck_active＋emit_ctx_*；Darwin BRANCH26／Ubuntu SEGV）；**w379** onefunc HARD BAN PREFER＋check_expr HARD BAN reinject（Ubuntu XT001／Darwin BRANCH26）；**w378** value_abi BAN PREFER（sret · `-E` 双端）＋emit_index Ubuntu PREFER BAN 复认（option=240；Darwin PREFER 仍）；**w377** module_import T001＋BAN PREFER（BRANCH26）Darwin `-E`／Ubuntu hard-skip；**w376** bootstrap_glue T001＋BAN PREFER；**w375** for_call_args PREFER＋BAN parser_result；**w374b** parse_orch Darwin PREFER／Ubuntu hard-skip；**w374** T001；**w373** type_pool option T001 根因（仍 w372b）；**w372b** type_pool Darwin PREFER／Ubuntu `-E`；**w371b** mega_body Darwin PREFER／Ubuntu hard-skip（fn#116）；**w370b BAN** macho_write PREFER（BRANCH26）；**w369b BAN** asm_wpo PREFER（BRANCH26）；**w368b elf_ctx** Darwin PREFER／Ubuntu `-E`；**w367b BAN** dep_ctx PREFER；**w366 sidecar_pool** PREFER 双端；**w365 expr_sidecar** PREFER 双端；**w364 block_domain** PREFER 双端；**w363b module_func** Darwin PREFER／Ubuntu `-E`；**w361/362 BAN** asm_locals／struct_layout PREFER；**w360b module_enum** Darwin PREFER／Ubuntu `-E`；**w359b BAN** top_level_let；**w358 type_alias** PREFER 双端；残 BAN 叶／mega Ubuntu／Type LE asm。 | 混成 `-E`+纯 asm | 下一＝余 soft -E（slot_bytes／field_load／param_ptr／assign）／mega Ubuntu tip BAN／type_to_c_repr split；禁 tip type_pool force-reinject \| 禁盲翻；禁 Darwin mega `-E`（内存）；保 `/tmp/w383_pabi_u_prefer_green.o` \|

产品 pthin 活面 22 件 standalone `-c` 全绿（2026-09-16 分类）。跳过：`stretch_audit`（P9a C twin 已解耦；84k audit 禁工厂回潮）、`foundation`／`seed_parse`／`diag_pipeline`（P001 桩）。g05 仍 `G05_X_O_WEAK=1`。

---

## 6. 日历（粗）

| 段 | 估计 |
|----|------|
| M2 分类 + 小 pthin 纯 asm 站住 | 数日～2 周 |
| 薄 M1（仅分类点名的墙）穿插 | 按类，每次数日 |
| mega FORCE 产品替换 | 2 周～3 月（最大方差） |
| M3 v2==v3 + 零 cc | M2 之后 3–6 周 |
| parser 整块迁收尾 | M2 能吃 pthin 之后 1–2 周 |

基数仍是数月不是数周。斜率第一次对准终局函数。
