# 当前进度与问题分析

> **更新时间**：2026-08-23（core_str_index g21 已闭；前序 sys_write_stdout need_sys ensure `7ce46ae02`／`fbb3ce099`）
> **分支**：`self-hosting`
> **已推 tip（origin）**：**`37bbdb94f`** — 代码（formal_mod `core/str/mod.o` ＋ labi g==21）
> **前一 tip**：**`6e3600563`** — docs 点 SHA；代码权威当时 **`fbb3ce099`**
> **产品 L4 放行钉盘（冻结 · 不升钉）**：**`f7424ae47`**
> **权威入口**：日常 L2 `./xbuild l2-matrix`；真冷 `./xbuild l4`（**不跑** `xlang check`）
> **本文定位**：给下一会话的**问题地图 + WIP 状态 + 续作步骤**；禁止就近补丁叙事。

---

## 1. 总体结论（一句话）

**pin 假绿债已还清（`70b06690f`），Ubuntu ELF `-o` SIGSEGV 已从 x86_64 编码器根修（`541a1d551`），cookbook `http_chunked_decode` BLD001 已从 host-C STRUCT_LIT 根修（`8cd7e5943`），cookbook `cli_subcommand` 软 P001 已从 parse 绑定名闸门根修（P014），cookbook `csv_write_row` T001 已从 std.csv 门面根修（`fd4d27933`），cookbook `csv.parse_row` 第 7 参 SIGSEGV 已从 import METHOD_CALL GP 上限根修（`600288bd7`），cookbook `cli_subcommand` `-o` UNDEF `std_cli_*` 已从 formal_mod + fk0 根修（`320984e60`），`xlang fmt` 静默 exit=1 已从 library-TU host-C 声明处初值根修（`396ea11ff`），vec-probe +pop/+extend BLD001 已从 labi 精确 UNDEF 针表根修（`a69c01dd9`），vec.new／push／pop 运行时 SIGSEGV 已从 AAPCS64 default_alloc dest-shadow 根修（`5ee5e764d`），append_roundtrip 138／139 已从 import-binding ARRAY→PTR 打分根修（`5c0bb0c6f`），sole `from_slice_u64`／`f64` BLD001 已从 labi 精确针根修（`33c974158`），METHOD `new` 名-first 已从 try_inline lookup stamp 根修（`b9839f7c4`），`vec.push(&u64)` first-wins 已从 std 补 `push(*Vec_u64)`／`push(*Vec_f64)` + labi 针根修（`4e69286b3`），sole `new+length`／`new+deinit` Vec_u64／f64 BLD001 已从 labi 精确针根修（`f9aadfb19`），`tests/vec/vec3f_soa_smoke` BLD001 已从 labi 精确针根修（`2ae56e216`），cookbook `io_read_chunk` BLD001 已从 host-C STRUCT_LIT dest peel／stamp 根修（`588e48a3d`），cookbook `fmt_template_i32` BLD001 已从 labi g14 针＋`fmt.o` c_face T 根修（`af419eabb`），cookbook `tar_append_entry` run 1 已从 `tar.x` STD-038 短路径体根修（`c28540b51`：`tar_append_entry_c` 曾是永远 −1 的桩且缺 `is_dir`；历史权威在已删 `tar.c`；cookbook 512 字节装不下头+数据块 → 1024），`tests/tar/long_path_dir` run 3 已从同文件 STD-152 prefix／Pax 根修（`9b5bf04e0`：101–256 末级 `/` 拆 prefix；257–512 Pax `'x'`＋`path=`；`next_entry` 跳 Pax 拼完整路径；`read_entry_data` 跳领先 Pax 块），cookbook `compress_stream_br_zs` BLD001 已从 labi g==15 针＋c_face T 根修（`e3b1cae8b`：g15 只覆盖 one-shot gzip／brotli／zstd；stream unique 名 `compress_init`／`process`／`end`／`state_bytes_for`／`format_*`／`mode_*` 不拉 `compress.o`；c_face 无 T。init／process／end 仍 −1，与 gzip cookbook codec-not-linked 成功 0 同契约），cookbook `io_fallback_read`／`io_mmap_read` BLD001 已从 always-linked `runtime_asm_io_stubs` 根修（`eaf75be24`：产品 `-o` 已链 stubs 且有 `std_io_read`／`write`／`ptr_len`；缺 `from_fd`／`read_fd`／`write_fd`／`std_io_read_ptr`；`io_read_ptr` 曾 `handle!=0 → NULL`；`io_read_ptr_len` 曾永远 0。同一 `g_io_read_ptr_len` 格，禁 c_face 第二套缓冲），cookbook `slice_u64_subslice` BLD001 已从 labi g==9 针根修（`62d057b79`：g9 10 针只有 len／get／subslice i32|u8 与 len／get u64；matcher 精确不覆盖 unique 名 `subslice_u64`／`split_at_u64`／`chunks_len_u64`。`needs_core_slice` 已探这些名但只 skip-missing 推 glue `slice.o`；formal `mod.o` 从未 ensure），cookbook `sys_write_stdout` BLD001 已从 need_sys ensure＋formal_mod clash 根修（`7ce46ae02`／`fbb3ce099`：针已开火且 catalog 有 `sys.o`，push_obj skip-missing 从不编；Darwin `#define write` 改写 macos FFI libc `write()`；Ubuntu objcopy 路径 X `msync(ptr,size)` 与 libc 3 参冲突），cookbook `core_str_index` BLD001 已从 formal_mod ＋ labi g==21 根修（`37bbdb94f`：无 `core/str/mod.o` catalog、无 simple-group；`bytes_view` 常内联，unique 名 `index_of`／`index_of_byte`／`starts_with` 从不开火；cookbook 曾用 mid-string `"ll"` 测 starts_with，永远 return 3）。双端 L2 5/5；cli／csv／http cookbook **0**；`fmt --check` 绿；`io_read_chunk` build **0** run **104**（成功分）；`fmt_template_i32`／`template_smoke` **0**；`tar_append_entry`／`ustar_roundtrip`／`tests/tar/main.x`／`long_path_dir` **0**；`compress_stream_br_zs`／`compress_gzip_roundtrip`／`tests/compress/main.x` **0**；`io_fallback_read`／`io_mmap_read`／`tests/io/fallback_matrix` **0**；`slice_u64_subslice`／`tests/slice/subslice_split_chunks`／`length` **0**；`sys_write_stdout` **0**；`core_str_index`／`tests/str/bytes_view`／`find_split` **0**。钉盘仍 `f7424ae47`，不升钉。**

下一刀候选：check-gate bstrict 闸门仍暂停（须点名「修 check／跑 check」）。dest extras／asm 库 TU `.data` 仍冻。其余见 §5。

### 1.1 双端钉盘 vs tip

| 端 / 对象 | SHA | 状态 | 备注 |
|-----------|-----|------|------|
| **产品 L4 钉盘** | **`f7424ae47`** | ✅ 双端 L4 + bstrict 129 | 谈自举 / 升钉才再 L4 |
| **origin tip** | **`37bbdb94f`** | ✅ 已推 | 代码（formal_mod `core/str/mod.o` ＋ labi g==21） |
| **Ubuntu 工作树** | **`37bbdb94f`** | ✅ L2 5/5；cli／csv／http **0**；fmt_template **0**；io_read_chunk **104**（成功分）；tar／compress **0**；io_fallback／io_mmap **0**；slice_u64 **0**；sys_write_stdout **0**；core_str_index **0** | heat `compiler/src/runtime_link_abi.o` ＋ `./xbuild link-product-asm`（g21 在 L8b 全 seed） |
| **macOS** | **`37bbdb94f`** | ✅ L2 5/5；cli／csv／http **0**；fmt OK；fmt_template **0**；io_read_chunk **104**；tar／compress **0**；io_fallback／io_mmap **0**；slice_u64 **0**；sys_write_stdout **0**；core_str_index **0** | 同上 heat；mac `bl _core_str_bytes_view_index_of`／Ubuntu `call core_str_bytes_view_index_of` |

日常 L2 矩阵尾标：

```text
pass=5 fail=0 SHA=<sha> XLANG=.../compiler/xlang_asm
product_l2_matrix OK rv42／opt102／hello0／si0／f32
```

---

## 2. 已闭（2026-08-22 · 勿重开）

### 2.1 pin 假绿根因 + 修复（commit `70b06690f`）

`565e105b3` 修了 `typeck_named_is_module_concrete` 的 ambient 污染（`heap.Allocator` vs 布局表裸名 `Allocator`），但 **pin 从未含过修复**。产品链 typeck 来自 pin（L4 step4 / Track L cold seed）→ 工程轨绿、产品链旧 bug = 假绿。

外科 pin twin：extern + `typeck_named_last_segment_off` / `typeck_named_spelling_eq`；4 处比较点改为 last-dot strip。macOS 产品链先绿。

### 2.2 Ubuntu ELF SIGSEGV 根因 + 修复（commit `541a1d551`）

**现象**：`build -o` SIGSEGV 139；栈 `try_inline_struct_lit_return_call_to_slot_elf` → `glue_call_lookup_callee_mod_fi_arena` → `glue_module_func_index_by_name` → `pipeline_module_num_funcs`；module=`0xf7ef9950`。

**gdb 证据**（option.none_i32）：

```text
LOOKUP  ctx.module_ref@16 = 0x7ffff7ef9950   ← 合法指针
BAD_MOD                    0xf7ef9950        ← 高 32 位被清掉
name = "none_i32"
```

**产生点**：x86_64 ELF enc `arch_x86_64_enc_enc_or_rbx_rax` 发 `orl %ebx,%eax`（`09 D8`）。`g02f_load_ptr_at` 按 usize `| (byte << n)` 拼 64 位指针；**SHL 已是 64 位**（`shl %cl,%rax`），**OR 是 32 位**，每次 OR 把 EAX 零扩展、高 32 清零。ARM64 ELF seed 早已是 `orr x0,x0,x1`（64 位）→ macOS 不崩。

**修**：`orq`/`andq`/`xorq`（REX.W `48 09 D8` / `48 21 D8` / `48 31 D8`）。权威：

- `compiler/src/asm/backend_x86_64_enc_c.x`（产品 prefer）
- `compiler/src/asm/arch/x86_64_enc.x`
- `compiler/seeds/backend_x86_64_enc_c.from_x.c` + surface

同 commit 同语义。**禁止**在 `pipeline_module_num_funcs` 加判空糊绿。

**验证节奏（Ubuntu 金标 · 2-pass，非 L4）**：

1. 重编 `backend_x86_64_enc_c.o` + `link-product-asm`（编码器字面量 `48 09 d8` 入链）
2. 删 `backend_try_inline_dispatch.o` + 再 relink（**新 xlang** 纯 asm 重编 `g02f_load_ptr_at` → `or %rax,mem` 64 位）
3. L2 5/5；fao build+run=0；htr build+run=0

L4 单 pass 仍会用 pin 蛋编 try_inline（旧 `orl`）。谈自举 / 升钉时 L4 后须再 heat 一次 try_inline，或接受「L4 后再 `rm try_inline.o && link-product-asm`」。

### 2.3 本会话验证

| 闸门 | Ubuntu | macOS |
|------|--------|-------|
| `./xbuild l2-matrix` | **5/5** @ `541a1d551` | **5/5** |
| `tests/boundary/field_alloc_overload.x` | ✅ build+run=0 | ✅ build+run=0 |
| `examples/cookbook/heap_trace_reset.x` | ✅ build+run=0 | （未本波重跑） |
| `g02f_load_ptr_at` objdump | `48 09 45 f8` orq-to-mem | n/a（ARM64） |

---

## 3. 当前非主刀 / 下一刀候选

### 3.1 cookbook `http_chunked_decode` — **已闭（本波）**

**现象**：formal_mod cc `std/http/mod.x` → incomplete `struct core_result_` → http.o 未入链 UNDEF `std_http_*` → cookbook BLD001。Clang 文案是 incomplete type；GCC 可能写成 no member `value`。

**产生**：host-C `emit_expr` STRUCT_LIT。`core.result` 的 `ok`／`err` 写匿名 `{ value: x, … }`。入口模块 typeck 会 backfill `struct_lit_struct_name`；作为 **dep 共发射**（http `-o` C）时名字仍空，只发当前模块前缀 → `(struct core_result_){ .value = … }`。函数签名走 `emit_type` Result_* 改写，已是完整 `struct core_result_Result_i32`。

**存储**：`codegen.x` STRUCT_LIT 发射 + `seeds/codegen_gen.linux.x86_64.c` twin。

**消费**：`xlang_compile_std_module.sh` host-cc；随后 ld。

**修**：G.7 补既有 STRUCT_LIT——空名从 `resolved_type_ref`／当前函数返回 TYPE_NAMED 恢复，再走 skip_abi_dup。禁 shell `#define`／改 std。双端 heat codegen 后：L2 5/5；`http_chunked_decode`／`http_parse_status` 0。

### 3.2 cookbook `cli_subcommand` — **parse P001 已闭（本波）**；`-o` UNDEF 另叶

**现象**：`-E` `parse error[P001]: no functions`；`-o` 缺 `_main`。

**产生**：`parse_body_lets_into` 绑定名只收 IDENT(59)／SELF(51)／UNDERSCORE(52)。cookbook `let run: u8[3] = [114,117,110]` 的 `run` 是 TOKEN_RUN（一元 `run <expr>`，lexer `ident_len=0`）→ `return false` 整函数软丢掉。`run[0]` 与 `run <expr>` 同 token 流，**不能**把 RUN 收成 IDENT。

**存储**：产品 pin `parser_gen.linux.x86_64.c` + twin `parser.x`；诊断权威 `parser_asm_body_tl_slice.inc`（P010–P013 族）。

**消费**：`parse_into_buf` → num_funcs=0 → P001／ld 缺 `_main`。

**修**：G.7 补既有 IDENT|SELF 闸门——非法关键字硬 **P014**（sticky，与 P011/P013 同）；**不**把 `run` 当变量。cookbook 改为 `let cmd`（字节仍是 `"run"`）。同模式：param 表／top-level let／buf onefunc。禁改 std／禁第二套名字解析。

**证（双端 L2）**：`let run`／`function f(run: i32)` → P014；`let cmd`／`let self` 绿；cli `-E` num_funcs=1；双端 L2 5/5；http cookbook 仍 0。

**`-o` UNDEF 已闭（`320984e60`）**：formal_mod 无 `std/cli` 时 std_x 只编裸 `cli.x`（T `cli_*`）；`import("std.cli")` 要 `std_cli_*`。且 `cli.o` 是 OP_STD flag_kind=0 却不在 fk0 rel×sym → 有前缀也不拉。**修**：G.7 补 formal_mod `mod|1|mod.x|cli.x`（csv/http 同型）并迁出 std_x；fk0 k==19 公共面；k==12 +`std_env_args_iter_count`（env.o 已有 T，sole UNDEF 不开闸）。证：双端 L2 5/5；cli／csv／http cookbook **0**。

### 3.3 cookbook `csv_write_row` — **T001 已闭（`fd4d27933`）**；parse_row 7 参 **已闭（`600288bd7`）**

**现象**：`csv.write_row` → T001 `no impl for type with method write_row`。

**产生**：`import("std.csv")` 方法表只看 `mod.x` export。`write_row`／`parse_row` 实现在 `csv.x`（`csv_write_row_c`／`csv_parse_row_c` + leaf 薄包装），门面没有。typeck 当 method_call → LANG-004。

**存储**：`std/csv/mod.x` 门面；`--bare-impl` `csv.x` 裸 `_c`；labi simple g==4／fk0 k==5 精确 UNDEF 探针。

**消费**：typeck T001；补门面后 Darwin ld-only 若探针无 `std_csv_write_row` 则不拉 `csv.o`。

**修**：G.7 补既有门面（tar／base64 `extern *_c` + `_rc`／unsafe 薄包装）+ 补全 labi csv 针表（count 3→5／fk0 2→4）。禁复制算法、禁改 typeck 去合并 csv.x export（会与 `next_field` 双份爆炸）。

**证（T001）**：双端 L2 5/5。Ubuntu cookbook／`write_row`／`tests/csv/main.x`／http **0**。mac `write_row` 单路径 0 · `tests/csv/main.x` 0。

**7 参 SIGSEGV 已闭（`600288bd7`）**：Darwin 产品 L2 是 `cc -c seeds/backend_call_dispatch.from_x.c`。import METHOD 分类把 GP 上限写死成 6（SysV）；AAPCS64 应 8（`x0`–`x7`）。`_main` `str [sp]`、callee 读 `x6`。同模块 7 参 CALL／UFCS／prefer `.x` 已走 `glue_asm_call_reg_max`。**修**：G.7 补 seed 分类用 `glue_asm_call_reg_max`（x86 仍 6）。证：双端 L2 5/5；cookbook／csv main／http **0**；mac `mov x6`。

### 3.4 `xlang fmt` 静默失败 — **已闭（`396ea11ff`）**

**现象**：`xlang fmt path.x` exit=1、stderr 仅换行；`--check` 同。文档旧标 `driver_fmt_one_file`——lldb 证明 **one_file 从未进入**。

**产生**：prefer 默认 `pure_asm_x_to_o` 编 `fmt_check_cmd_thin.x`。模块 `u8[N] = [bytes…]` 进 SHN_COMMON／Mach-O `__common`（**永远 BSS 0**）；非零 ARRAY_LIT 只在 hoist-target 入口 `pipeline_asm_modlet_seed_nonzero_inits_elf_c` 播种。本文件无 `main`，第一非 extern 体是 `driver_check_quiet_ok_get`，`xlang fmt` 不进 → ignore 针／FMT001 文案全 0。`strstr(path, "")` 命中一切 → `file_list_n()==0` → 空诊断 + exit 1。

**存储**：COMMON 符号 + hoist seed（`runtime_pipeline_abi.x`）；host-C 权威已是 `codegen.x` `want_decl_init`（库 TU 无 `init_globals`／无 main）。

**消费**：`path_should_ignore`／`driver_run_fmt` 空列表 FMT001；`diag_report_with_code` 空 kind／msg → 只剩换行。

**修**：G.7 补既有 prefer 跳过（与 `runtime_pipeline_abi.x` mega 同型）——`pure_asm_x_to_o` 对 `fmt_check_cmd_thin.x` return 1，prefer 回落 `-E`+$CC，声明处 `static uint8_t g[] = {47,…}`。**禁止** `path_should_ignore` 空针特例。asm 库 TU `.data` 非零 ARRAY_LIT 是后叶（dest extras 仍冻）。Opt-in：`XLANG_FMT_ALLOW_PURE_ASM=1`。

**证（双端 L2）**：双端 L2 5/5；cli／csv／http **0**；脏文件 `fmt OK` 写回；`--check` 脏路径 FMT001「needs formatting」exit 1；已规范 hello `--check` 0。Ubuntu heat `fmt_check_cmd_driver.o` 后 needles 在 `.o`。

### 3.5 vec-probe +pop/+extend — **已闭（`a69c01dd9`）**

**现象**：`import("std.vec")` 只 `pop`／`extend`（或 `Vec_u64`／`Vec_f64` extend）→ Darwin ld BLD001 UNDEF `std_vec_pop_*`／`std_vec_extend_Vec_u64_*`。`tests/vec/append_roundtrip.x` 同。

**产生**：`labi_od_vec_sym_*`（12）与 fk0 k==10（10）精确 UNDEF 针只有 push/get/length/from_slice_u8 等；matcher 精确，push 不覆盖 pop，`from_slice_u8` 不覆盖 u64/f64 extend。另：树上 stale `std/vec/vec.o` 常是 prefer 纯 asm 裸名（`_pop_Vec_i32_ptr`），即使用户 UNDEF 命中针，拉进去也对不上 `std_vec_*`。ensure skip-up-to-date 不重编。

**存储**：L8b `labi_ondemand_list.x` + seed twin；L8c／fk0 `labi_ondemand_heavy.x` + seed／heavy surface twin。Darwin 本波 labi 走 **L8b 全 seed**（L8c unused）。

**消费**：`link_abi_user_o_needs_std_vec`／`labi_std_fk0_user_needs_rel` 不开火 → 不 ensure／不 push `std/vec/vec.o` → ld UNDEF。

**修**：G.7 补既有针表——count 12→18／fk0 10→16（pop i32/u8 + extend i32/u8/u64/f64）。禁第二套表／禁改 std／禁搅 check。heat：`rm compiler/src/runtime_link_abi.o`；`rm std/vec/vec.o` 让 ensure 走 host-C 出 `std_vec_*`（Darwin 无 objcopy，纯 asm 裸名）。

**证（双端 L2）**：双端 L2 **5/5**；cli／csv／http **0**；`fmt --check` hello 0；extend_u64／pop_from_slice build+run **0**；append_roundtrip **build 0**（曾 UNDEF）。pop_only／boundary 运行时 SIGSEGV 已由 `5ee5e764d` 闭；append_roundtrip **run 138** 仍是后叶。

### 3.6 vec.new／push／pop 运行时 SIGSEGV — **已闭（`5ee5e764d`）**

**现象**：针表把 `vec.o` 拉进来之后，`vec.new()` 即 SIGSEGV 139（不必 push／pop）。`tests/vec/boundary.x` 同。lldb：`str x0, [x1, #0x18]` 地址 `0x18`。

**产生**：`try_inline_const_struct_lit_return_call_to_slot_elf` 内联 `new()` 的 STRUCT_LIT；`al: heap.default_alloc()` 走 `glue_emit_default_alloc_to_rbx_offset`。`field_store_sz` 对跨模块 Allocator 常报 8，CALL 后 `store_rax_to_rbx_offset(sz=8)` 用 dest-in-x1。AAPCS64 16B 返回 `x0`+`x1`（kind + arena=0）打掉 dest → `[0+0x18]`。

**存储**：`.x` `glue_emit_default_alloc_to_rbx_offset` + seed `_impl` + surface twin。Darwin 本波 try_inline 走 **seed `from_x.c`**（prefer .x 未胜）。ARM64 enc 早已：`mov x19,x0` 阴影 + `store_size>=16` 双 GP。

**消费**：用户 `_main` 内联 stores；`from_slice` 非 const STRUCT_LIT、真调用，绿。x86 rbx callee-saved，本路径本不崩。

**修**：G.7 补既有 helper——`ta==1` 强制 `store_size=16`（`str x0,[x19,#off]`／`str x1,[x19,#off+8]`）。x86 仍 rbx + leftover fsz=8 清高 8。禁改 enc_store 全局；禁改 std；禁搅 check／append 138。

**证（双端 L2）**：双端 L2 **5/5**；cli／csv／http **0**；`fmt --check` hello 0；new_nouse／push_only／pop_only／`boundary` **0**；mac 反汇编 `str [x19]`。append_roundtrip 138 已由 `5c0bb0c6f` 闭。

### 3.7 append_roundtrip 138／139 — **已闭（`5c0bb0c6f`）**

**现象**：`tests/vec/append_roundtrip.x` 链上后 mac abort **138**／Ubuntu SIGSEGV **139**。`_main` `bl _std_vec_from_slice_i32_ptr_i32`；lldb `from_slice_i32 +168` `str q0,[x8]` 写 `_main`（32B sret，调用方当 16B Vec_u64 用 x0/x1）。host-C `-E` 已是 `from_slice_u64_ptr_i32(src, 4)`。

**产生**：import-binding mangle `glue_asm_score_import_binding_func_ix` 只比 suffix。`u64[4]` → `u64_a4`，`*u64` → `u64_ptr`，两边 score==1；`sc_best>=11` 不覆盖 typeck／first-wins。`from_slice` 同名同 arity，first = Vec_i32（带 Allocator，AAPCS64 MEMORY sret）。typeck 权威 `typeck_overload_arg_param_score` 早已 ARRAY→PTR（ak==10 && pk==9）打 1000；asm 打分未对齐。ADDR_OF `&src[0]` 本就绿（suffix `u64_ptr`）。

**存储**：seed `glue_asm_score_import_binding_func_ix_ex_c`（Darwin 产品 L2 体）+ `.x` `glue_asm_score_import_binding_func_ix_c`。

**消费**：用户 `_main` 调错 from_slice；callee 写 x8 sret。

**修**：G.7 补既有打分——ARRAY T vs *T 且 elem suffix 相同 → +10（≡ typeck）。FLOAT_LIT splat 仍 score==1，闸门不变。禁改 std／禁扩针表／禁搅 METHOD `new` 名-first／禁搅 check。

**证（双端 L2）**：双端 L2 **5/5**；cli／csv／http **0**；`fmt --check` 0；append 成功分 **10**（`length(round_f)+u_len`＝4+6，非错误码 10）；f64 extend+roundtrip **0**；i32 from_slice **0**；boundary **0**；splat／dot **0**；mac `bl from_slice_u64`／Ubuntu `call from_slice_u64`。sole `from_slice_u64`／`f64`（无 extend）已由 `33c974158` 闭。

### 3.8 sole `from_slice_u64`／`f64` BLD001 — **已闭（`33c974158`）**

**现象**：`import("std.vec")` 只 `from_slice` Vec_u64／Vec_f64（无 extend）→ Darwin／Ubuntu ld BLD001 UNDEF `std_vec_from_slice_u64_ptr_i32`／`_f64_ptr_i32`（及同宽 get／length／deinit）。`from_slice_i32` 已绿（length／get／deinit i32 针已开火）。append_roundtrip 因 extend 针仍拉 vec.o。

**产生**：`labi_od_vec_sym_*`（18）与 fk0 k==10（16）精确 UNDEF 针只有 `from_slice_u8`；matcher 精确，`from_slice_u8` 不覆盖 u64／f64。

**存储**：L8b `labi_ondemand_list.x` + seed twin；fk0 `labi_ondemand_heavy.x` + seed／heavy surface twin。双端本波 labi 走 **L8b 全 seed**（L8c unused）。

**消费**：`link_abi_user_o_needs_std_vec`／`labi_std_fk0_user_needs_rel` 不开火 → 不 ensure／不 push `std/vec/vec.o` → ld UNDEF。

**修**：G.7 补既有针表——count 18→20／fk0 16→18（`from_slice_u64_ptr_i32`／`from_slice_f64_ptr_i32`）。禁第二套表／禁改 std／禁搅 check／METHOD `new`。heat：`rm compiler/src/runtime_link_abi.o`；`rm std/vec/vec.o` 让 ensure 走 host-C 出 `std_vec_*`。

**证（双端 L2）**：双端 L2 **5/5**；cli／csv／http **0**；`fmt --check` hello 0；sole fs_u64／fs_f64 build+run **0**；append 成功分 **10**；boundary **0**。

### 3.9 METHOD `new` 名-first try_inline — **已闭（`b9839f7c4`）**

**现象**：`let v: Vec_u64 = vec.new()` 内联 `Vec_i32.new` 体（`bl _std_heap_default_alloc` + 32B `al` 存）。Vec_u16／f64 同（16B 槽写 32B）。`Vec_i32.new` 本身绿。

**产生**：`glue_call_lookup_callee_mod_fi_arena` 对 METHOD_CALL=49 只走 `glue_module_func_index_by_name` first-wins。`std/vec/mod.x` 第一个 `new` 是 Vec_i32（带 `default_alloc`）。历史跳过 CALL resolve 槽是防 stale `dep_ix` → Ubuntu `increment().field` SIGSEGV。typeck dest-type pick（`let v: Vec_u64 = vec.new()`）已 `apply_call_resolve`；CALL 路径早已读 `resolved_func_index`。

**存储**：Darwin L2 产品体 seed `backend_try_inline_dispatch.from_x.c` `_impl`；Ubuntu L2 prefer thin+rest `.x`。W278 `call_resolved_*` 是独立字段（非 METHOD union）。

**消费**：`try_inline_const_struct_lit_return_call_to_slot_elf` 零额外参工厂。

**修**：G.7 补既有 lookup——METHOD 优先 stamp：`dep_ix==-2` 不内联（dyn）；`0<=dep_ix<ndep` 且 `module_at` 非空且 `func_ix<nfuncs` 才用 dep；`dep_ix<0` 当本模块 UFCS；stamp 缺／OOB 才名扫描。禁第二套 lookup／禁改 std／禁搅 check／push mangle／labi 针。

**证（双端 L2）**：双端 L2 **5/5**；cli／csv／http **0**；`fmt --check` 0；nouse_u64／f64／u16／i32 **0**；mac u64 `_main` 三 `str #0/#8/#0x10` 无 `bl`；mac i32 仍 `bl default_alloc`；Ubuntu u64 仅 `ret`；Ubuntu i32 `call std_heap_default_alloc`；smf／boundary **0**；append 成功分 **10**；fs_u64／fs_f64 **0**。

**本叶不搅（已暴露、均已闭）**：`vec.push(&u64)` first-wins 已由 `4e69286b3` 闭（std 补候选，非改 emit）。sole `new+length`／`new+deinit` u64／f64 已由 `f9aadfb19` 闭。

### 3.10 import METHOD `push` first-wins — **已闭（`4e69286b3`）**

**现象**：`vec.push(&u64_vec, 1 as u64)` `bl _std_vec_push_Vec_i32_ptr_i32`；host-C 同。`vec.push(&u16)`／`u8`／`i32` 已绿。run=0 是假绿（i32 4 字节步长写进 u64 槽）。

**产生**：`std/vec/mod.x` 有 `push(*Vec_i32)`／`u8`／`u16`，**没有** `push(*Vec_u64)`／`f64`。import-binding 打分（`glue_asm_score_import_binding_func_ix`，Darwin 全 seed）对已有 overload 已能打 u16（`sc_best>=11` 覆盖 stamp）。缺候选时 score==1 不够闸门，回落 typeck／first-wins 第一个 2 参 `push`＝Vec_i32。不是 try_inline（有参 CALL 不内联）。

**存储**：std `mod.x` 方法表；labi `labi_od_vec_sym_*`（20）与 fk0 k==10（18）精确针只有 push_i32／u8／u16。

**消费**：用户 `_main` 调错 push；sole `push_u64` 即便有符号，针表不拉 vec.o → BLD001。

**修**：G.7 补既有 std API（与 u16 同形，grow 走 `reserve_one`）+ 补既有针表 20→22／fk0 18→20。禁改 emit mangle／禁搅 check／length／deinit 针。cookbook `vec_u64_push.x`／`vec_f64_push.x`：两值 round-trip 卡住 i32 步长假绿。

**证（双端 L2）**：双端 L2 **5／5**；cli／csv／http **0**；`fmt --check` 0；u64／f64 cookbook build+run **0**；mac `bl push_Vec_u64`／Ubuntu `call push_Vec_u64`；u16／i32 **0**；append 成功分 **10**；boundary／smf **0**。sole `new+deinit` u64 已由 `f9aadfb19` 闭。

### 3.11 labi `length`／`deinit`／`get` u64／f64 — **已闭（`f9aadfb19`）**

**现象**：`import("std.vec")` 只 `new()+length()` 或 `new()+deinit()` Vec_u64／Vec_f64（无 push／from_slice／extend）→ Darwin／Ubuntu ld BLD001 UNDEF `std_vec_length_Vec_u64`／`std_vec_deinit_Vec_u64_ptr`（f64 同）。u16／i32 针已有；push／from_slice 针已拉 vec.o 的路径绿。

**产生**：`labi_od_vec_sym_*`（22）与 fk0 k==10（20）精确 UNDEF 针只有 length／deinit／get 的 u16／i32（及 u8 length）。matcher 精确，`length_Vec_i32` 不覆盖 `length_Vec_u64`。

**存储**：L8b `labi_ondemand_list.x` + seed twin；fk0 `labi_ondemand_heavy.x` + seed／heavy surface twin。双端本波 labi 走 **L8b 全 seed**（L8c unused）。std 已有 `length`／`get`／`deinit` Vec_u64／f64（不改 std）。

**消费**：`link_abi_user_o_needs_std_vec`／`labi_std_fk0_user_needs_rel` 不开火 → 不 ensure／不 push `std/vec/vec.o` → ld UNDEF。

**修**：G.7 补既有针表——count 22→28／fk0 20→26（length／deinit／get × u64／f64）。禁第二套表／禁改 std／禁搅 check／dest extras。cookbook 拆成 **unique UNDEF**：length 不调 deinit、deinit 不调 length。

**证（双端 L2）**：双端 L2 **5／5**；cli／csv／http **0**；`fmt --check` 0；u64／f64 length／deinit cookbook build+run **0**；user.o `U length_Vec_u64`／`U deinit_Vec_u64_ptr`（及 f64）；mac `bl length_Vec_u64`／`bl deinit_Vec_u64_ptr`；Ubuntu `call length_Vec_u64`／`call deinit_Vec_u64_ptr`；push／u16／i32 **0**；append 成功分 **10**；boundary／smf **0**。

**本叶不搅（已暴露、本波已闭）**：`tests/vec/vec3f_soa_smoke` BLD001 已由 `2ae56e216` 闭。

### 3.12 labi Vec3f SOA／AOS — **已闭（`2ae56e216`）**

**现象**：`import("std.vec")` `vec3f_soa_new`＋`push`／`deinit`／`reserve_one`／`sum_x`（AOS 同）→ Darwin／Ubuntu ld BLD001 UNDEF `std_vec_vec3f_soa_push`／`vec3f_aos_deinit`／`reserve_one`／`sum_x`。Vec_* push／length／deinit 针已有；`new()` 是 const STRUCT_LIT 内联，无 UNDEF。

**产生**：`labi_od_vec_sym_*`（28）与 fk0 k==10（26）精确 UNDEF 针只有 Vec_i32／u8／u16／u64／f64 面。matcher 精确，`std_vec_push_Vec_i32_ptr_i32` 不覆盖 unique 名 `std_vec_vec3f_soa_push`。

**存储**：L8b `labi_ondemand_list.x` + seed twin；fk0 `labi_ondemand_heavy.x` + seed／heavy surface twin。双端本波 labi 走 **L8b 全 seed**（L8c unused）。std 已有 vec3f SOA／AOS API（不改 std）。

**消费**：`link_abi_user_o_needs_std_vec`／`labi_std_fk0_user_needs_rel` 不开火 → 不 ensure／不 push `std/vec/vec.o` → ld UNDEF。

**修**：G.7 补既有针表——count 28→44／fk0 26→42（soa／aos push／deinit／reserve_one／sum_x／len／get／set／with_capacity）。禁第二套表／禁改 std／禁搅 check／dest extras／`io_read_chunk`／`fmt_template`。cookbook 拆成 **unique UNDEF**：push 不调 deinit、deinit 不调 push。

**证（双端 L2）**：双端 L2 **5／5**；cli／csv／http **0**；`fmt --check` 0；soa／aos push／deinit cookbook **0**；user.o unique `U vec3f_soa_push`／`U vec3f_soa_deinit`（及 aos）；mac `bl vec3f_soa_push`／`bl vec3f_soa_deinit`；Ubuntu `call vec3f_soa_push`／`call vec3f_soa_deinit`；smoke 成功分 **3**（`2+1`，非错误码）；sum 成功分 **6**（1+2+3）；read_y／alias **0**；append 成功分 **10**；boundary **0**。

### 3.13 cookbook `io_read_chunk` — **已闭（`588e48a3d`）**

**现象**：`import("std.fs")` create／write／close／open／read → Darwin／Ubuntu ld BLD001 UNDEF `std_fs_*`。labi fk0 k==15 针表本已有这五个名字。

**产生**：`xlang_compile_std_fs_formal.sh` 用 KEEP_C 车 vehicle `import("std.fs")` 再 host-cc。dep 共发射 posix／io 时匿名 `{ fields }` 空名：① `let iov: Iovec[4] = [{ base, length }, …]` 数组元素发不完整 `struct std_fs_posix_`；② `let buf: Buffer = { ptr, length, handle }` 发不完整 `struct std_io_`。http Result 空名恢复只看 resolved／函数返回（readv4 返回 i64）。host-cc 失败 → fs.o 未产出 → ld UNDEF。

**存储**：`codegen.x` emit_expr STRUCT_LIT + `emit_braced_array_lit_init` + `emit_local_fixed_array_let_finish`；twin `seeds/codegen_gen.linux.x86_64.c`。消费＝fs_formal cc 再 ld。

**修**：G.7 补既有 STRUCT_LIT dest（http `8cd7e5943`）——剥 TYPE_ARRAY／PTR／SLICE 到 TYPE_NAMED；let dest 戳 ARRAY_LIT；空名 STRUCT_LIT 用 name setter 打 dest（禁 Expr sret SIGBUS）。skip_abi_dup 再把 Iovec → `std_io_sync_`。禁第二套 tagger／禁改 std／禁加针／禁搅 check／fmt_template。

**证（双端 L2）**：双端 L2 **5／5**；cli／csv／http **0**；`fmt --check` 0；vec3f unique **0**；io_read_chunk build **0** run **104**（payload 10+…+80＝360＆255，成功分，禁 set -e）；fs.o T `std_fs_create`／`open`／`read`／`write`／`close`。

### 3.14 cookbook `fmt_template_i32` — **已闭（`af419eabb`）**

**现象**：`import("std.fmt")` `format_template` → Darwin／Ubuntu ld BLD001 UNDEF `std_fmt_format_template`。unique 名、无类型后缀。

**产生**：labi simple g==14 精确针 8 项只有 `format_i32`／`to_buf_*`／`hex_to_buf_*`／`append_to_buf_*`／`format_u8_ptr_i32_i32_i32`。matcher 精确，`format_i32` 不覆盖 unique 名 `format_template`。另：产品 `fmt.o` 是 `c_face|0|formal_surface.c`（mod.x 共发射 io／context／error 会留 companion U），c_face 也无 T。mod.x 本已 export（C 路径权威）。

**存储**：L8b `labi_ondemand_list.x` + seed twin；`std/fmt/formal_surface.c`。双端本波 labi 走 **L8b 全 seed**（L8c unused）。fmt 不走 fk0。

**消费**：g14 不开火 → 不 ensure／不 push `std/fmt/fmt.o` → ld UNDEF。只加针不加 T 仍 UNDEF。

**修**：G.7 补既有针表 count 8→9（`std_fmt_format_template`）＋补既有 c_face T（体 ≡ mod.x 首个 `{}` → i32 十进制，`face_i64`）。禁改 catalog 去 host-cc mod.x／禁第二套表／禁改 emit mangle／禁搅 check／tar。

**证（双端 L2）**：双端 L2 **5／5**；cli／csv／http **0**；`fmt --check` 0；fmt_template／`tests/fmt/template_smoke` **0**；user.o unique `U std_fmt_format_template`；fmt.o T 同名；mac `bl _std_fmt_format_template`／Ubuntu `call std_fmt_format_template`；io_read_chunk 成功分 **104**；vec3f unique **0**。

### 3.15 cookbook `tar_append_entry` — **已闭（`c28540b51`）**

**现象**：`import("std.tar")` `append_entry` → Darwin／Ubuntu **build 0 · run 1**（非 UNDEF）。labi 针与 `tar.o` T 本已有 `std_tar_append_entry`。

**产生**：`std/tar/tar.x` 的 `tar_append_entry_c` 是永远 `return -1` 的桩，且签名少 `is_dir`（mod.x 8 参）。历史全量在已删 `std/tar/tar.c`（`2963c3aaf`）；write_header／read_header 已恢复，append／next／read_data 未迁。另：cookbook `u8[512]` 装不下 512 头 + 512 数据块，即使体正确也会 cap 失败。

**存储**：`std/tar/tar.x`（`--bare-impl`）＋ `std/tar/mod.x` 薄包装。消费＝cookbook／`ustar_roundtrip` 运行时。

**修**：G.7 补既有 `tar_append_entry_c`（对齐 `is_dir`）＋同文件 `next_entry`／`read_entry_data`（STD-038 短路径：name≤100、typeflag `'0'`／`'5'`、512 对齐）。cookbook 缓冲 512→1024。禁第二套 glue／禁重建 `tar_glue.c`／禁搅 Pax／禁搅 check。

**证（双端 L2）**：双端 L2 **5／5**；cli／csv／http **0**；`fmt --check` 0；fmt_template **0**；io_read_chunk 成功分 **104**；cookbook／`ustar_roundtrip`／`tests/tar/main.x` **0**；tar.o T `std_tar_append_entry`／`tar_append_entry_c`；mac `bl _std_tar_append_entry`／Ubuntu `call std_tar_append_entry`。`long_path_dir` 已由 `9b5bf04e0` 闭。

### 3.16 `tests/tar/long_path_dir` — **已闭（`9b5bf04e0`）**

**现象**：`import("std.tar")` 第三条 `append_entry`（270 字节全 `p`）→ Darwin／Ubuntu **build 0 · run 3**。前两条短路径（75／46）在 STD-038 叶已绿。

**产生**：`tar_append_entry_c` 仍拒 `name_len>100`。STD-152：101–256 走 UStar prefix（offset 345，末级 `/` 为界）；257–512 走 Pax typeflag `'x'`＋`path=` 记录再跟截断 name 头。`next_entry` 不跳 Pax、不拼 prefix；`read_entry_data` 不跳领先 Pax 块（测试在 `next_entry` 前进前快照 `entry_off`）。历史全量在已删 `std/tar/tar.c`。

**存储**：`std/tar/tar.x`（`--bare-impl`）＋ `std/tar/mod.x` 薄包装。消费＝`long_path_dir`／`tar_extended_smoke_c` 运行时。

**修**：G.7 补既有 `tar_append_entry_c`／`next_entry`／`read_entry_data`（对齐历史 `tar.c`：`tar_split_path`／`tar_write_header_split`／`tar_write_pax_header`／`tar_parse_pax_path`／`tar_read_full_name`）。`tar_extended_smoke_c` 恢复（无 ARRAY_LIT，库 TU）。禁第二套 glue／禁重建 `tar_glue.c`／禁搅 check／dest extras。

**证（双端 L2）**：双端 L2 **5／5**；cli／csv／http **0**；`fmt --check` 0；fmt_template **0**；io_read_chunk 成功分 **104**；`long_path_dir`／cookbook／`ustar_roundtrip`／main **0**；101 字节 prefix 探针 **0**；tar.o T `std_tar_append_entry`／`tar_append_entry_c`／`tar_extended_smoke_c`；mac `bl _std_tar_append_entry`／Ubuntu `call std_tar_append_entry`。

### 3.17 cookbook `compress_stream_br_zs` — **已闭（`e3b1cae8b`）**

**现象**：`import("std.compress")` stream／format／mode → Darwin／Ubuntu ld BLD001 UNDEF `std_compress_compress_init`／`process`／`end`／`state_bytes_for`／`format_brotli`／`format_zstd`／`mode_compress`／`mode_decompress`。`compress_gzip_roundtrip` 已绿（g15 one-shot 针）。

**产生**：labi simple g==15 精确针 6 项只有 `gzip_compress`／`gzip_decompress`／brotli／zstd one-shot。matcher 精确，`gzip_compress` 不覆盖 unique 名 `compress_init`。产品 `compress.o` 是 `c_face|0|formal_surface.c`（mod.x 共发射 deflate／inflate 与 zlib 类型冲突），c_face 也无 stream T。

**存储**：L8b `labi_ondemand_list.x` + seed twin；`std/compress/formal_surface.c`。双端本波 labi 走 **L8b 全 seed**（L8c unused）。compress 不走 fk0。

**消费**：g15 不开火 → 不 ensure／不 push `std/compress/compress.o` → ld UNDEF。只加针不加 T 仍 UNDEF。

**修**：G.7 补既有针表 count 6→14 ＋补既有 c_face T。format／mode ≡ mod.x 常量；`state_bytes_for` 回 128／32／32（gzip／brotli／zstd 真实 cap，过 cookbook 1..512 闸）；init／process／end 仍 −1（codec-not-linked；cookbook `init!=0 → return 0`，与 gzip `n_gz<=0 → return 0` 同契约）。禁改 catalog 去 host-cc mod.x／禁第二套表／禁改 emit mangle／禁搅 check／async。

**证（双端 L2）**：双端 L2 **5／5**；cli／csv／http **0**；`fmt --check` 0；fmt_template **0**；io_read_chunk 成功分 **104**；compress_stream／gzip／`tests/compress/main.x` **0**；compress.o T `std_compress_compress_init`／`format_brotli`／`mode_compress`；mac `bl _std_compress_compress_init`／Ubuntu `call std_compress_compress_init`。heat：`rm std/compress/compress.o` ＋ `./xbuild link-product-asm`（针在 `xlang_asm`）。

### 3.18 cookbook `io_fallback_read`／`io_mmap_read` — **已闭（`eaf75be24`）**

**现象**：`import("std.io")` unique 名 → Darwin／Ubuntu ld BLD001 UNDEF `std_io_read_fd`／`write_fd`（fallback）与 `std_io_from_fd`／`read_ptr`（mmap）。`ptr_len` 已在 always-linked stubs 有 T。

**产生**：产品 `-o` 永远链 `compiler/runtime_asm_io_stubs.o`，已有 `std_io_read`／`write`／`ptr_len`／`xlang_io_read_ptr`。缺 import mangle `std_io_from_fd`／`read_fd`／`write_fd`／`std_io_read_ptr`。`io_read_ptr` 曾 `handle!=0 → NULL`（stdin-only）；`io_read_ptr_len` 曾永远 `return 0`（`std_io_ptr_len` 转发它 → mmap 会 run 6）。g==19 针只有 ctx 三面；io.o c_face 也只有 ctx。

**存储**：`compiler/seeds/runtime_asm_io_stubs.from_x.c`（产品 user-link TU；thin `.x` 无此体）。消费＝user ld。

**修**：G.7 补既有 stubs——任意 fd `io_read_ptr`；`io_read_ptr_len` 读 `g_io_read_ptr_len`；补 T `std_io_from_fd`／`read_fd`／`write_fd`／`std_io_read_ptr`。同一格，禁 c_face 第二套缓冲／禁加 labi 针（already-linked）／禁改 catalog／禁搅 check／async／`ptr_gen`。

**证（双端 L2）**：双端 L2 **5／5**；cli／csv／http **0**；`fmt --check` 0；fmt_template／tar／compress **0**；io_read_chunk 成功分 **104**；fallback／mmap／`tests/io/fallback_matrix` **0**（fallback 须 `</dev/null`）；stubs T 四处 unique 名；mac `bl _std_io_read_ptr`／Ubuntu `call std_io_read_ptr`。heat：`rm compiler/runtime_asm_io_stubs.o`（下一 `-o` ensure seed）。`tests/io/read_ptr_mmap_smoke` 仍 UNDEF `ptr_gen`／`ptr_valid`／`ptr_backend`（另叶）。

### 3.19 cookbook `slice_u64_subslice` — **已闭（`62d057b79`）**

**现象**：`import("core.slice")` unique 名 → Darwin／Ubuntu ld BLD001 UNDEF `core_slice_subslice_u64`／`split_at_u64`／`chunks_len_u64`。`tests/slice/length`／`subslice_split_chunks` 已绿（g9 已有 i32／u8）。

**产生**：labi simple g==9 精确针 10 项只有 `len_i32`／`get_i32`／`subslice_i32`／u8 同款／`len_u64`／`get_u64`。matcher 精确，`subslice_i32`／`len_u64` 不覆盖 unique 名 `subslice_u64`／`split_at_u64`／`chunks_len_u64`。`needs_core_slice` 表已含这三名，但只 skip-missing 推 glue `core/slice/slice.o`（`*_c`）；formal `mod.o`（g9 rel、X-facing T）从未 ensure。

**存储**：L8b `labi_ondemand_list.x` + seed twin。双端本波 labi 走 **L8b 全 seed**（L8c unused）。catalog 已有 `core/slice/mod.o`＝`mod|0|mod.x`。

**消费**：g9 不开火 → 不 ensure／不 push `core/slice/mod.o` → ld UNDEF。glue `slice.o` 仍入链（needs_core_slice）。

**修**：G.7 补既有针表 count 10→13。禁改 catalog／禁把 skip_missing 改成第二套 ensure／禁搅 sys／async／check／`ptr_gen`。

**证（双端 L2）**：双端 L2 **5／5**；cli／csv／http **0**；`fmt --check` 0；fmt_template／tar／compress **0**；io_read_chunk 成功分 **104**；io_fallback／io_mmap **0**；slice_u64／`tests/slice/subslice_split_chunks`／`length` **0**；mod.o T 三处 unique 名；mac `bl _core_slice_subslice_u64`／Ubuntu `call core_slice_subslice_u64`。heat：`rm compiler/src/runtime_link_abi.o` ＋ `./xbuild link-product-asm`。

### 3.20 cookbook `sys_write_stdout` — **已闭（`7ce46ae02`／`fbb3ce099`）**

**现象**：`import("std.sys")` `write_stdout` → Darwin／Ubuntu ld BLD001 UNDEF `std_sys_write_stdout`。针表 `labi_od_sys_sym_*` 8 项已含此名；catalog 已有 `std/sys/sys.o`＝`mod|0|mod.x`；磁盘无 `sys.o`。

**产生**：`needs_std_sys` 开火（同块把 `core/mem/mem.o` 推进 argv），`link_abi_asm_ld_push_obj` skip-missing 不 ensure（≡ heap 注释）。L4 wipe 后 `sys.o` 永不落地。Darwin 即便 ensure：file-wide `#define write` 把 co-emit macos FFI `r = write(fd,buf,count)` 改成 `xlang_formal_bare_write`，再 `static` X `write` 撞声明。Ubuntu 有 objcopy 不走 Mac wrapper：X `msync(ptr,size)` 与 libc／FFI 3 参 `msync` 冲突。

**存储**：L8b `labi_ondemand_list.from_x.c` need_sys 消费（产品 L2 全 seed；L8c unused）＋ `labi_ondemand_heavy.x` twin；formal_mod `xlang_compile_std_module.sh`。

**消费**：skip-missing 不 push → ld UNDEF。ensure 失败则同样 UNDEF。

**修**：G.7 补既有 need_sys——`xlang_ensure_formal_std_make_o` 后 push（禁第二套 simple-group 针）。Mac wrapper 对 libc 冲突名按 **同 arity** 改定义／包装 callee；SHARED 再对定义／同 arity 前向声明改 `xlang_formal_bare_*`（禁 file-wide `#define msync`，否则 linux FFI 3 参被改写）；post-o objcopy 加 mmap／munmap／msync／ftruncate／lseek。禁改 catalog／禁搅 check／async／linux.o companion。

**证（双端 L2）**：双端 L2 **5／5**；cli／csv／http **0**；`fmt --check` 0；fmt_template／tar／compress **0**；io_read_chunk 成功分 **104**；io_fallback／io_mmap／slice_u64 **0**；sys_write_stdout **0**；sys.o T `std_sys_write_stdout`；mac `bl _std_sys_write_stdout`／Ubuntu `call std_sys_write_stdout`。heat：`rm compiler/src/runtime_link_abi.o` ＋ `./xbuild link-product-asm`（ensure 在 L8b seed）。formal_mod 脚本改动即时生效。

### 3.21 cookbook `core_str_index` — **已闭（`37bbdb94f`）**

**现象**：`import("core.str")` unique 名 → Darwin／Ubuntu ld BLD001 UNDEF `core_str_bytes_view_index_of`／`index_of_byte`／`starts_with`。`bytes_view` 本身常被内联（STRUCT_LIT 返回），user.o 无此 UNDEF。

**产生**：无 simple-group、无 formal_mod catalog `core/str/mod.o`。g0–g20 精确针不覆盖 `core_str_*`。consume 循环已对每个 simple-group ensure＋push，缺表项＝永不编。

**存储**：L8b `labi_ondemand_list.x` + seed twin；catalog `xlang_compile_std_module.sh`。双端本波 labi 走 **L8b 全 seed**（L8c unused）。

**消费**：无组开火 → 不 ensure／不 push → ld UNDEF。

**修**：G.7 无才新增 g==21（count 21→22；12 针＝`mod.x` 全 export 面）＋ catalog `core/str/mod.o`＝`mod|0|mod.x`。cookbook starts_with 改测前缀 `"he"`（mid-string `"ll"` 永远失败，非编译器债）。禁第二套表／禁搅 check／async／datetime／`ptr_gen`。

**证（双端 L2）**：双端 L2 **5／5**；cli／csv／http **0**；`fmt --check` 0；fmt_template／tar／compress **0**；io_read_chunk 成功分 **104**；io_fallback／io_mmap／slice_u64／sys_write_stdout **0**；core_str_index／`tests/str/bytes_view`／`find_split` **0**；mod.o T 三处 unique 名；mac `bl _core_str_bytes_view_index_of`／Ubuntu `call core_str_bytes_view_index_of`。heat：`rm compiler/src/runtime_link_abi.o` ＋ `./xbuild link-product-asm`。

---

## 4. Trae 续作步骤（推荐顺序）

### 4.0 前置

```bash
cd /Users/shuliangfu/worker/xlang/x-lang
git checkout self-hosting && git status -sb
# Ubuntu：ssh ubuntu-remote-server → /home/shuliangfu/worker/xlang/x-lang
```

**同步**：mac → Ubuntu 只 git push/pull。**git commit 仅 macOS**。

### 4.1 Ubuntu 收本波（heat labi seed）

```bash
git fetch origin && git merge --ff-only origin/self-hosting
rm -f compiler/src/runtime_link_abi.o
./xbuild link-product-asm
./xbuild l2-matrix
```

针／need_sys ensure 在 L8b 全 seed（入 `runtime_link_abi.o`／`xlang_asm`）。io_read_chunk 成功分 **104**（禁 set -e 当失败）。io_fallback 须 `</dev/null`。slice_u64／fallback／mmap／compress／tar／sys_write_stdout 期望 **0**。

### 4.2 下一刀（若继续债）

check-gate 闸门暂停，须点名「修 check／跑 check」。`core_str_index` 本波已闭（`37bbdb94f`）。勿开 dest extras／asm 库 TU `.data`。扫描还暴露 async／datetime／db／net unique UNDEF（zc `ptr_view`／mmap_smoke `ptr_gen` 另叶），勿搅本叶。heat 本叶：`rm -f compiler/src/runtime_link_abi.o && ./xbuild link-product-asm`。

### 4.3 若谈自举 / 升钉

`./xbuild l4` 后 **必须再** `rm compiler/src/asm/backend_try_inline_dispatch.o && ./xbuild link-product-asm`，否则 pin 蛋会把 `g02f_load_ptr_at` 再编成旧 `orl`。然后矩阵 + 全量 bstrict。钉盘仍不自动升。

---

## 5. 既有债登记（勿搅本叶）

| 债 | 现象 | 定性 |
|----|------|------|
| `http_chunked_decode.x` | 曾 BLD001：`struct core_result_` incomplete（§3.1） | **已闭** · STRUCT_LIT 空名恢复 |
| `cli_subcommand.x` | 曾 P001 `no functions`；曾 `-o` UNDEF `std_cli_*` | **parse 已闭（P014 + `let cmd`）** · **`-o` 已闭 `320984e60`** |
| `csv_write_row.x` | 曾 T001 no impl `write_row`；曾全路径 SIGSEGV `parse_row` 第 7 参 | **T001 已闭 `fd4d27933`** · **7 参 ABI 已闭 `600288bd7`** |
| **fmt 静默失败** | 曾 `xlang fmt` exit=1 零输出 | **已闭 `396ea11ff`** · library-TU host-C 声明处初值（非 `driver_fmt_one_file`） |
| **vec probe 表 +pop/+extend** | 曾 sole UNDEF 不拉 vec.o | **已闭 `a69c01dd9`** · labi 精确针 12→18／fk0 10→16 |
| **vec.new／push／pop 运行时 139** | 曾 `str [x1,#0x18]` 地址 0x18 | **已闭 `5ee5e764d`** · ta==1 store_size=16 走 x19 |
| **append_roundtrip 138／139** | 曾 `bl from_slice_i32` 32B sret | **已闭 `5c0bb0c6f`** · ARRAY→PTR 打分 +10 |
| **sole `from_slice_u64`／`f64` UNDEF** | 无 extend 时针表只有 `from_slice_u8` | **已闭 `33c974158`** · labi 精确针 18→20／fk0 16→18 |
| **METHOD `new` 名-first** | 曾内联 Vec_i32.new 进 Vec_u64 槽 | **已闭 `b9839f7c4`** · METHOD 优先 stamp；bounded dep |
| import METHOD `push` first-wins | 曾 `vec.push(&u64)` → `push_Vec_i32` | **已闭 `4e69286b3`** · std 补 `push(*Vec_u64)`／f64 + labi 针 20→22／fk0 18→20；emit 打分本已能打 u16 |
| labi `length`／`deinit`／`get` u64／f64 | 曾 sole `new+deinit`／`new+length` BLD001 | **已闭 `f9aadfb19`** · labi 精确针 22→28／fk0 20→26 |
| labi Vec3f SOA／AOS | 曾 `vec3f_soa_smoke` BLD001 unique 名 | **已闭 `2ae56e216`** · labi 精确针 28→44／fk0 26→42 |
| cookbook `io_read_chunk` | 曾 BLD001 UNDEF `std_fs_*`（针已有；fs_formal incomplete Iovec／Buffer） | **已闭 `588e48a3d`** · STRUCT_LIT dest peel／stamp；run **104** 成功分 |
| cookbook `fmt_template_i32` | 曾 BLD001 UNDEF `std_fmt_format_template` | **已闭 `af419eabb`** · labi g14 8→9 ＋ c_face T |
| cookbook `tar_append_entry` | 曾 build 0 · run 1 | **已闭 `c28540b51`** · `tar.x` STD-038 短路径；cookbook 1024 |
| `tests/tar/long_path_dir` | 曾 build 0 · run 3 | **已闭 `9b5bf04e0`** · STD-152 prefix／Pax |
| cookbook `compress_stream_br_zs` | 曾 BLD001 UNDEF `std_compress_compress_*`／format／mode | **已闭 `e3b1cae8b`** · labi g15 6→14 ＋ c_face T |
| cookbook `io_fallback_read`／`io_mmap_read` | 曾 BLD001 UNDEF `std_io_read_fd`／`from_fd`／`read_ptr` | **已闭 `eaf75be24`** · always-linked stubs unique T；`io_read_ptr` 任意 fd；`io_read_ptr_len` 读 cell |
| cookbook `slice_u64_subslice` | 曾 BLD001 UNDEF `core_slice_subslice_u64`／`split_at_u64`／`chunks_len_u64` | **已闭 `62d057b79`** · labi g9 10→13；formal `mod.o` ensure |
| cookbook `sys_write_stdout` | 曾 BLD001 UNDEF `std_sys_write_stdout`（针已有；`sys.o` 从未 ensure） | **已闭 `7ce46ae02`／`fbb3ce099`** · need_sys ensure ＋ Mac／Ubuntu clash 同 arity 改名 |
| cookbook `core_str_index` | 曾 BLD001 UNDEF `core_str_bytes_view_index_of*`／`starts_with`（无 catalog／无组；`bytes_view` 内联） | **已闭 `37bbdb94f`** · formal_mod ＋ labi g==21 12 针；cookbook starts_with 改 `"he"` |
| macOS bstrict 10 fail | check/typeck gate | 既有；闸门暂停；须点名 |

---

## 6. 纪律摘要

| 规则 | 要求 |
|------|------|
| 钉盘 | **`f7424ae47` 不升钉** |
| check | **暂停**；L2/L4 默认不跑 `xlang check` |
| 矩阵 | 只 `./xbuild l2-matrix` |
| 双权威 | seed / .x 同 commit；本波 codegen prefer assemble from `.x` + pin twin `codegen_gen.linux.x86_64.c` |
| 平台 | import METHOD GP 上限 = SHARED classify；AAPCS64 热路径在 Darwin |
| 根源治理 | 禁下游判空；禁 shell `#define Result_*`；OR 宽度在编码器 |
| L4 + try_inline | pin 蛋编 g02f 仍可能旧 `orl` → L4 后 heat try_inline |

Skill：`~/.grok/skills/xlang-selfhost-product-gate/SKILL.md`
进度 KPI：`analysis/自举进度.md`

---

## 7. 关键路径索引

| 路径 | 角色 |
|------|------|
| `std/csv/mod.x` | std.csv 用户面：`parse_row`／`write_row` 薄包装 `csv_*_c` |
| `compiler/seeds/labi_ondemand_list.from_x.c` | L8b 产品 on_demand 壳：g==21 core.str 针（本波）＋ need_sys ensure `sys.o`（前波） |
| `compiler/scripts/xlang_compile_std_module.sh` | formal_mod：`core/str/mod.o`＝`mod.x`（本波）＋ Mac／Ubuntu clash 同 arity 改名 |
| `compiler/src/runtime/labi_ondemand_list.x` | simple g==21 core.str 针 12（本波）＋ g==9 core.slice ＋ g==15 compress ＋ g==14 fmt ＋ g==4 csv ＋ `labi_od_vec_sym_*`（+ seed twin） |
| `compiler/seeds/runtime_asm_io_stubs.from_x.c` | 产品 user `-o` always-linked std.io 桩（本波 +from_fd／read_fd／write_fd／std_io_read_ptr；`io_read_ptr` 任意 fd；`io_read_ptr_len` 读 cell） |
| `std/compress/formal_surface.c` | 产品 `compress.o` c_face（前波 +stream／format／mode T） |
| `std/tar/tar.x` | STD-038 短路径 ＋ STD-152 prefix／Pax 权威：`tar_append_entry_c`／`next_entry`／`read_entry_data`（本波） |
| `std/fmt/formal_surface.c` | 产品 `fmt.o` c_face（前波 +`std_fmt_format_template` ≡ mod.x） |
| `compiler/src/runtime/labi_ondemand_heavy.x` | fk0 k==5 csv／k==10 vec UNDEF 针（本波 +vec3f SOA／AOS；+ seed／surface twin） |
| `compiler/src/parser/parser.x` | 绑定名 IDENT\|SELF 闸门 + P014 调用（.x twin） |
| `compiler/seeds/parser_gen.linux.x86_64.c` | **产品 parser pin twin**（XLANG_PARSER_FROM_X=0） |
| `compiler/seeds/parser_asm/parser_asm_body_tl_slice.inc` | **P014 诊断权威**（P010–P013 族补全） |
| `compiler/src/codegen/codegen.x` | host-C STRUCT_LIT dest peel／stamp（本波 io_read_chunk；前波 http Result） |
| `compiler/seeds/codegen_gen.linux.x86_64.c` | codegen seed twin（L4 蛋） |
| `compiler/scripts/xlang_compile_std_module.sh` | **formal_mod 权威**：`std/cli/cli.o` = `mod.x` + `--bare-impl` `cli.x` |
| `compiler/src/runtime/labi_ondemand_heavy.x` | fk0 k==19 `std_cli_*`／k==12 +`std_env_args_iter_count`（+ seed／surface twin） |
| `std/vec/mod.x` | Vec_u64／f64 `push`（本波；grow＝`reserve_one`） |
| `compiler/seeds/backend_call_dispatch.from_x.c` | **Darwin 产品 L2 权威**：import METHOD GP 上限 + ARRAY→PTR 打分 `_ex` |
| `compiler/src/asm/backend_call_dispatch.x` | prefer twin：`glue_asm_score_import_binding_func_ix_c` 同款 decay |
| `compiler/src/asm/backend_x86_64_enc_c.x` | 产品 x86_64 enc AND/OR/XOR（REX.W） |
| `compiler/src/asm/backend_try_inline_dispatch.x` | `g02f_load_ptr_at`；`glue_emit_default_alloc_to_rbx_offset` ta==1 store 16；METHOD lookup stamp（Ubuntu prefer） |
| `compiler/seeds/backend_try_inline_dispatch.from_x.c` | Darwin L2 try_inline 产品体；`glue_call_lookup` METHOD stamp `_impl` |
| `compiler/seeds/typeck_gen.linux.x86_64.c` | 产品 pin typeck（spelling_eq @ `70b06690f`） |
| `./xbuild l2-matrix` / `./xbuild l4` | 矩阵 / 真冷 |

---

## 8. 环境信息

| 项 | 值 |
|----|-----|
| 开发机 | macOS Darwin arm64 |
| **Ubuntu 金标** | **`ssh ubuntu-remote-server`** → `/home/shuliangfu/worker/xlang/x-lang` |
| 已推 tip | **`37bbdb94f`**（formal_mod `core/str/mod.o` ＋ labi g==21） |
| 钉盘 | **`f7424ae47`**（不变） |
| check 闸门 | **暂停** |
| bstrict 脚本数（钉盘绿） | 129 |

---

## 9. 变更记录

| 日期 | 内容 |
|------|------|
| **2026-08-23** | formal_mod ＋ labi g==21 闭 `core_str_index` BLD001（无 catalog／无组；`bytes_view` 内联；unique `index_of`／`index_of_byte`／`starts_with`；cookbook starts_with 改 `"he"`）；双端 L2 5/5 · cookbook／bytes_view／find_split **0** · mac `bl index_of`／Ubuntu `call index_of` @ `37bbdb94f` |
| **2026-08-23** | need_sys ensure ＋ formal_mod clash 闭 `sys_write_stdout` BLD001（针已开火；skip-missing 不编 `sys.o`；Darwin `#define write` 改 FFI；Ubuntu X `msync` 2 参 vs libc 3 参）；双端 L2 5/5 · cookbook **0** · mac `bl write_stdout`／Ubuntu `call write_stdout` @ `7ce46ae02`／`fbb3ce099` |
| **2026-08-23** | labi g9 10→13 闭 `slice_u64_subslice` BLD001（unique `subslice_u64`／`split_at_u64`／`chunks_len_u64`；g9 ensure formal `mod.o`）；双端 L2 5/5 · cookbook／slice tests **0** · mac `bl subslice_u64`／Ubuntu `call subslice_u64` @ `62d057b79` |
| **2026-08-23** | always-linked stubs 闭 `io_fallback_read`／`io_mmap_read` BLD001（补 `from_fd`／`read_fd`／`write_fd`／`std_io_read_ptr`；`io_read_ptr` 任意 fd；`io_read_ptr_len` 读 cell）；双端 L2 5/5 · fallback／mmap／fallback_matrix **0** · mac `bl read_ptr`／Ubuntu `call read_ptr` @ `eaf75be24` |
| **2026-08-23** | labi g15 6→14 ＋ c_face T 闭 `compress_stream_br_zs` BLD001（stream／format／mode unique 名；init／process／end 仍 −1 codec-not-linked）；双端 L2 5/5 · stream／gzip／compress main **0** · mac `bl compress_init`／Ubuntu `call compress_init` @ `e3b1cae8b` |
| **2026-08-23** | `tar.x` STD-152 prefix／Pax 闭 `long_path_dir` run 3（末级 `/` 拆 prefix；Pax `'x'`＋`path=`；next 跳 Pax；read_data 跳领先 Pax）；双端 L2 5/5 · long_path_dir／cookbook／ustar_roundtrip／main **0** · prefix 101 探针 **0** · mac `bl append_entry`／Ubuntu `call append_entry` @ `9b5bf04e0` |
| **2026-08-23** | `tar.x` STD-038 短路径闭 `tar_append_entry` run 1（补 `is_dir`＋头／垫块／next／read_data；cookbook 512→1024）；双端 L2 5/5 · cookbook／ustar_roundtrip／main **0** · mac `bl append_entry`／Ubuntu `call append_entry` · long_path_dir **3** @ `c28540b51` |
| **2026-08-23** | labi g14 8→9 ＋ c_face T 闭 `fmt_template_i32` BLD001 `std_fmt_format_template`；双端 L2 5/5 · cookbook／template_smoke **0** · mac `bl format_template`／Ubuntu `call format_template` @ `af419eabb` |
| **2026-08-23** | labi 精确针闭 `vec3f_soa_smoke` BLD001（`labi_od_vec_sym_*` 28→44／fk0 k==10 26→42）；双端 L2 5/5 · unique UNDEF cookbook **0** · mac `bl vec3f_soa_push`／Ubuntu `call vec3f_soa_push` · smoke 成功分 3 · sum 成功分 6 @ `2ae56e216` |
| **2026-08-23** | labi 精确针闭 sole `new+length`／`new+deinit` Vec_u64／f64 BLD001（`labi_od_vec_sym_*` 22→28／fk0 k==10 20→26）；双端 L2 5/5 · unique UNDEF cookbook **0** · mac `bl length_Vec_u64`／Ubuntu `call length_Vec_u64` @ `f9aadfb19` |
| **2026-08-23** | std 补 `push(*Vec_u64)`／`f64` + labi 针 20→22／fk0 18→20 闭 `vec.push(&u64)` first-wins；双端 L2 5/5 · cookbook u64／f64 **0** · mac `bl push_Vec_u64`／Ubuntu `call push_Vec_u64` @ `4e69286b3` |
| **2026-08-23** | METHOD `new` 名-first try_inline 闭（`glue_call_lookup` 优先 stamp；bounded dep）；双端 L2 5/5 · nouse_u64 无 `default_alloc` · nouse_i32 仍 allocator @ `b9839f7c4` |
| **2026-08-23** | labi 精确针闭 sole `from_slice_u64`／`f64` BLD001（`labi_od_vec_sym_*` 18→20／fk0 k==10 16→18）；双端 L2 5/5 · fs_u64／fs_f64 **0** · append 成功分 10 @ `33c974158` |
| **2026-08-23** | import-binding ARRAY→PTR 打分闭 append_roundtrip 138（`glue_asm_score_import_binding_func_ix` ≡ typeck ak==10 pk==9）；双端 L2 5/5 · append 成功分 10 · splat 0 @ `5c0bb0c6f` |
| **2026-08-23** | AAPCS64 dest-shadow 闭 vec.new／push／pop 139（`glue_emit_default_alloc_to_rbx_offset` ta==1 store 16 走 x19）；macOS L2 5/5 · boundary／pop／push **0** @ `5ee5e764d` |
| **2026-08-22 夜** | formal_mod + fk0 闭 cli_subcommand `-o` UNDEF `std_cli_*`（catalog `mod.x`+bare `cli.x`；fk0 k==19／k==12 +`args_iter_count`）；双端 L2 5/5 · cli／csv／http **0** @ `320984e60` |
| **2026-08-22 夜** | import METHOD AAPCS64 第 7 参闭 csv.parse_row SIGSEGV（seed `glue_asm_call_reg_max` 非硬编码 6）；双端 L2 5/5 · cookbook **0** @ `600288bd7` |
| **2026-08-22 夜** | std.csv 门面闭 csv_write_row T001（mod.x `parse_row`／`write_row` + labi 针）；双端 L2 5/5 @ `fd4d27933`；Ubuntu cookbook **0**；mac `write_row` 0、全路径 SIGSEGV 另叶（已由 `600288bd7` 闭） |
| **2026-08-22 夜** | parse P014 闭 cli_subcommand 软 P001（关键字不能当绑定名；cookbook `let cmd`）；双端 L2 5/5 @ `19351aa19`；`-o` UNDEF `std_cli_*` 另叶 |
| **2026-08-22** | STRUCT_LIT 匿名 Result tag 根修闭 http BLD001（macOS L2 5/5 · cookbook 0）；前序 pin 假绿 `70b06690f` + orq `541a1d551` |
| 2026-08-22 早 | 旧版：Ubuntu SIGSEGV 地图（已闭，见 §2.2） |
| 2026-08-19 | 旧版：F7 impl 闭合 + Ubuntu dyn CG002（已过期） |
