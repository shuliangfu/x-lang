# 当前进度与问题分析

> **更新时间**：2026-08-22（Ubuntu ELF SIGSEGV 根因已合入 `541a1d551`：x86_64 `orl`→`orq`）
> **分支**：`self-hosting`
> **已推 tip（origin）**：**`541a1d551`** — fix(selfhost): emit REX.W orq/andq/xorq on x86_64 so usize bitwise keeps high 32
> **前一 tip**：**`70b06690f`** — typeck pin twin spelling_eq（产品链对齐 typeck.x）
> **产品 L4 放行钉盘（冻结 · 不升钉）**：**`f7424ae47`**
> **权威入口**：日常 L2 `./xbuild l2-matrix`；真冷 `./xbuild l4`（**不跑** `xlang check`）
> **本文定位**：给下一会话的**问题地图 + WIP 状态 + 续作步骤**；禁止就近补丁叙事。

---

## 1. 总体结论（一句话）

**pin 假绿债已还清（`70b06690f`），Ubuntu ELF `-o` SIGSEGV 已从 x86_64 编码器根修（`541a1d551`：`orl` 截断 usize OR → `orq`）。双端 L2 5/5；`field_alloc_overload` 双端 build+run=0；`heap_trace_reset` Ubuntu build+run=0。钉盘仍 `f7424ae47`，不升钉。**

下一刀候选（非本波引入）：cookbook `http_chunked_decode` 现为 **BLD001**（`std/http/mod.x` 生成 C：`struct core_result_` 无 `value` 成员 → http.o 未入链 UNDEF），**不再 139**。其余既有债见 §5。

### 1.1 双端钉盘 vs tip

| 端 / 对象 | SHA | 状态 | 备注 |
|-----------|-----|------|------|
| **产品 L4 钉盘** | **`f7424ae47`** | ✅ 双端 L4 + bstrict 129 | 谈自举 / 升钉才再 L4 |
| **origin tip** | **`541a1d551`** | ✅ 已推 | spelling_eq pin + x86_64 REX.W bitwise |
| **Ubuntu 工作树** | `541a1d551` | ✅ L2 5/5；2-pass 重编 enc + try_inline | 见 §2.3 |
| **macOS** | `541a1d551` | ✅ L2 5/5；fao build+run=0 | ARM64 不走 x86_64 enc 热路径 |

日常 L2 矩阵尾标：

```text
pass=5 fail=0 SHA=<sha> XLANG=.../compiler/xlang_asm
product_l2_matrix OK rv42／opt102／hello0／si0／f32
```

---

## 2. 已闭（2026-08-22 · 勿重开）

### 2.1 pin 假绿根因 + 修复（commit `70b06690f`）

`565e105b3` 修了 `typeck_named_is_module_concrete` 的 ambient 污染（`heap.Allocator` vs 布局表裸名 `Allocator`），但 **pin 从未含过修复**。产品链 typeck 来自 pin（L4 step4 / Track L cold seed）→ 工程轨绿、产品链旧 bug = 假绿。

外科 pin twin：extern + `typeck_named_last_segment_off` / `typeck_named_spelling_eq`；4 处比较点改为 last-dot strip。macOS 产品链先绿。

### 2.2 Ubuntu ELF SIGSEGV 根因 + 修复（commit `541a1d551`）

**现象**：`build -o` SIGSEGV 139；栈 `try_inline_struct_lit_return_call_to_slot_elf` → `glue_call_lookup_callee_mod_fi_arena` → `glue_module_func_index_by_name` → `pipeline_module_num_funcs`；module=`0xf7ef9950`。

**gdb 证据**（option.none_i32）：

```text
LOOKUP  ctx.module_ref@16 = 0x7ffff7ef9950   ← 合法指针
BAD_MOD                    0xf7ef9950        ← 高 32 位被清掉
name = "none_i32"
```

**产生点**：x86_64 ELF enc `arch_x86_64_enc_enc_or_rbx_rax` 发 `orl %ebx,%eax`（`09 D8`）。`g02f_load_ptr_at` 按 usize `| (byte << n)` 拼 64 位指针；**SHL 已是 64 位**（`shl %cl,%rax`），**OR 是 32 位**，每次 OR 把 EAX 零扩展、高 32 清零。ARM64 ELF seed 早已是 `orr x0,x0,x1`（64 位）→ macOS 不崩。

**修**：`orq`/`andq`/`xorq`（REX.W `48 09 D8` / `48 21 D8` / `48 31 D8`）。权威：

- `compiler/src/asm/backend_x86_64_enc_c.x`（产品 prefer）
- `compiler/src/asm/arch/x86_64_enc.x`
- `compiler/seeds/backend_x86_64_enc_c.from_x.c` + surface

同 commit 同语义。**禁止**在 `pipeline_module_num_funcs` 加判空糊绿。

**验证节奏（Ubuntu 金标 · 2-pass，非 L4）**：

1. 重编 `backend_x86_64_enc_c.o` + `link-product-asm`（编码器字面量 `48 09 d8` 入链）
2. 删 `backend_try_inline_dispatch.o` + 再 relink（**新 xlang** 纯 asm 重编 `g02f_load_ptr_at` → `or %rax,mem` 64 位）
3. L2 5/5；fao build+run=0；htr build+run=0

L4 单 pass 仍会用 pin 蛋编 try_inline（旧 `orl`）。谈自举 / 升钉时 L4 后须再 heat 一次 try_inline，或接受「L4 后再 `rm try_inline.o && link-product-asm`」。

### 2.3 本会话验证

| 闸门 | Ubuntu | macOS |
|------|--------|-------|
| `./xbuild l2-matrix` | **5/5** @ `541a1d551` | **5/5** |
| `tests/boundary/field_alloc_overload.x` | ✅ build+run=0 | ✅ build+run=0 |
| `examples/cookbook/heap_trace_reset.x` | ✅ build+run=0 | （未本波重跑） |
| `g02f_load_ptr_at` objdump | `48 09 45 f8` orq-to-mem | n/a（ARM64） |

---

## 3. 当前非主刀 / 下一刀候选

### 3.1 cookbook `http_chunked_decode`（独立债 · 非 139）

Ubuntu @ `541a1d551` 2-pass 后：

```text
xlang_compile_std_module.sh: cc -c failed for ../std/http/mod.x
struct core_result_ has no member named 'value'
ld: undefined std_http_has_chunked_encoding / has_keep_alive / ...
build error[BLD001]
```

**不是** SIGSEGV。std/http 生成 C 的 Result 字段名与 `core.result` 布局不一致（或 -E 把 Result 压成空 struct）。五问未开刀；勿与 bitwise 修搅一波。

wave957/958 L4 时该 cookbook 曾绿，可能吃了当时的 std/http.o 缓存。真冷后才会再暴露。

### 3.2 五问草稿（http，待下一会话）

| 项 | 内容 |
|----|------|
| **产生** | codegen / typeck 把 `Result` 字段 `value` 发射成空 `struct core_result_`？或 std/http .x 用了 `.value` 而布局名不同 |
| **存储** | `std/http/mod.x`；`core/result`；生成 `/tmp/.../mod_0.c` |
| **消费** | host cc -c std module；随后 ld UNDEF |
| **爆炸半径** | http cookbook；可能其它 Result 用户 |
| **最小回归** | `http_chunked_decode` / `http_parse_status` + L2 |

---

## 4. Trae 续作步骤（推荐顺序）

### 4.0 前置

```bash
cd /Users/shuliangfu/worker/xlang/x-lang
git checkout self-hosting && git status -sb   # tip 541a1d551；analysis 文档可脏
# Ubuntu：ssh ubuntu-server → /home/shuliangfu/worker/xlang/x-lang
```

**同步**：mac → Ubuntu 只 git push/pull。**git commit 仅 macOS**。

### 4.1 下一刀（若继续 cookbook）

1. Ubuntu 复现 `std/http/mod.x` 生成 C 的 `core_result_` 定义 vs `.value` 使用点
2. 归层：codegen 字段名 vs typeck Result 布局 vs std/http 源
3. 单点根修；seed/.x 同 commit；禁止改测试期望
4. 探针：`http_chunked_decode` + `http_parse_status` + L2 5/5；mac 同批

### 4.2 若谈自举 / 升钉

`./xbuild l4` 后 **必须再** `rm compiler/src/asm/backend_try_inline_dispatch.o && ./xbuild link-product-asm`，否则 pin 蛋会把 `g02f_load_ptr_at` 再编成旧 `orl`。然后矩阵 + 全量 bstrict。钉盘仍不自动升。

---

## 5. 既有债登记（勿搅本叶）

| 债 | 现象 | 定性 |
|----|------|------|
| `http_chunked_decode.x` | BLD001：`core_result_` 无 `value`（§3.1） | std/http 生成 C · **下一刀候选** |
| `cli_subcommand.x` | `parse error[P001]: no functions` | parse 层独立债 |
| `csv_write_row.x` | T001 no impl `write_row` | std/csv 缺方法 |
| **fmt 静默失败** | `xlang fmt` exit=1 零输出 | `driver_fmt_one_file`；卡 bstrict `run-check.sh`；check 闸门暂停 |
| macOS bstrict 10 fail | check/typeck gate | 既有；A/B 确认 |
| vec probe 表 +pop/+extend | 上会话目标 | 未动 |

---

## 6. 纪律摘要

| 规则 | 要求 |
|------|------|
| 钉盘 | **`f7424ae47` 不升钉** |
| check | **暂停**；L2/L4 默认不跑 `xlang check` |
| 矩阵 | 只 `./xbuild l2-matrix` |
| 双权威 | seed / .x 同 commit；本波 enc 四文件已对齐 |
| 平台 | x86_64 bitwise = LINUX 金标热路径；ARM64 已 64 位 |
| 根源治理 | 禁下游判空；OR 宽度在编码器 |
| L4 + try_inline | pin 蛋编 g02f 仍可能旧 `orl` → L4 后 heat try_inline |

Skill：`~/.grok/skills/xlang-selfhost-product-gate/SKILL.md`
进度 KPI：`analysis/自举进度.md`

---

## 7. 关键路径索引

| 路径 | 角色 |
|------|------|
| `compiler/src/asm/backend_x86_64_enc_c.x` | **产品 x86_64 enc AND/OR/XOR（REX.W）** |
| `compiler/src/asm/arch/x86_64_enc.x` | text-emit twin |
| `compiler/seeds/backend_x86_64_enc_c.from_x.c` | cold seed twin |
| `compiler/src/asm/backend_try_inline_dispatch.x` | `g02f_load_ptr_at`（usize OR 消费端） |
| `compiler/seeds/typeck_gen.linux.x86_64.c` | 产品 pin typeck（spelling_eq @ `70b06690f`） |
| `compiler/src/typeck/typeck.x` | typeck .x 权威 |
| `./xbuild l2-matrix` / `./xbuild l4` | 矩阵 / 真冷 |

---

## 8. 环境信息

| 项 | 值 |
|----|-----|
| 开发机 | macOS Darwin arm64 |
| **Ubuntu 金标** | **`ssh ubuntu-server`** → `/home/shuliangfu/worker/xlang/x-lang` |
| 已推 tip | **`541a1d551`** |
| 钉盘 | **`f7424ae47`**（不变） |
| check 闸门 | **暂停** |
| bstrict 脚本数（钉盘绿） | 129 |

---

## 9. 变更记录

| 日期 | 内容 |
|------|------|
| **2026-08-22** | pin 假绿 `70b06690f` + Ubuntu ELF SIGSEGV 根修 `541a1d551`（x86_64 `orl`→`orq`）；双端 L2 5/5；fao 双端 0；htr Ubuntu 0；http cookbook 转 BLD001（非 139） |
| 2026-08-22 早 | 旧版：Ubuntu SIGSEGV 地图（已闭，见 §2.2） |
| 2026-08-19 | 旧版：F7 impl 闭合 + Ubuntu dyn CG002（已过期） |
