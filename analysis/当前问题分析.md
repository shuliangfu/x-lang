# XLANG 自举当前问题全图（wave583 Cap struct layout/field · L2）

> **文档目的**：wave583 Cap residual struct 类型名/字段名长名根因与产品矩阵状态；供下一波接手  
> **配套**：[自举进度.md](自举进度.md) · [当前进度.md](当前进度.md)  
> **最后更新**：2026-07-28（tip `9283a6cfb` · 钉盘 `01479a857` · 未 L4）

---

## 0. 全局状态速览

| 维度 | 状态 |
|------|------|
| 产品钉盘 | **`01479a857`**（wave576 L4；本波未升钉） |
| tip | **`9283a6cfb`** |
| 当前波次 | **wave583 Cap struct layout/field**（L2 收口中） |
| 构建级别 | **L2 工程轨**（**未** L4 真冷） |
| mac L2 | ✅ sn/fn 63..127=42 · 矩阵 |
| Ubuntu L2 | 待本波 pull 后验 |

---

## 1. 本波已修根因（wave583）

### 1.1 Storage 已 128，parser 侧门仍 63

- **现象**：`struct` 类型名 ≥64 → parse 失败 / 无 main；字段名 ≥64 → XP003 out_len=0  
- **产生点**：`parser_asm_struct_layout_slice` sname/fname；`struct_field_name_from_tok`；`finish_struct_lit` type/field；ast_pool field/type_param **getter** ≤63；typeck merge/SoA/coerce  
- **机制**：wave577 已把 StructLayout / Field / TypeParam name 扩到 128，set 已接受 127，但 parse 缓冲与 getter 仍 63 → 布局未登记 / 字段 len=0  
- **修法**：全路径 content 127、缓冲 128；typeck.x + seed pin 同 commit；SoA 同门控  
- **PLATFORM: SHARED**

### 1.2 type_param 探针

- 单参 `struct Box<T…>` 在本波前 63/64/75 已绿（append 已 127）；本波补齐 parse 缓冲与 getter，避免长 type-param 读回 0  

---

## 2. 验证

| 探针 | mac L2 | Ubuntu L2 |
|------|--------|-----------|
| struct 类型名 63..127 | ✅ 42 | 待 |
| struct 字段名 63..127 | ✅ 42 | 待 |
| 矩阵 rv/option/si/hello | ✅ | 待 |

**未做**：双端 L4 · 全量 bstrict 129 · 升钉  

---

## 3. 下一波（wave584）

1. residual library wrap / import binding 等 `>63` 侧门  
2. 整波双端 L4 + 升钉  

---

## 4. 执行纪律提醒

- 改 `ast_pool.c` / typeck_soa：Darwin **PIPELINE_X_FORCE_COMPILE=1** pipeline_x + filtered  
- 改 parser_asm：重建 **parser_asm_thin_glue.o** + g05  
- 改 typeck.x：seed pin `typeck_gen.linux.x86_64.c` 同 commit + typeck_x.o  
- 钉盘未双端 L4 不升  
