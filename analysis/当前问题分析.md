# XLANG 自举当前问题全图（wave708 match struct field lit guard 闭后 · Cap residual）

> **文档目的**：产品钉盘已升至 tip `4fa4f07e7`；记录升钉后前排与 residual 边界  
> **配套**：[自举进度.md](自举进度.md) · [当前进度.md](当前进度.md)  
> **最后更新**：2026-07-29（钉盘 = tip **`4fa4f07e7`** · wave700–705 含于钉盘；wave707/708 tip L2）

---

## 0. 全局状态速览

| 维度 | 状态 |
|------|------|
| 产品钉盘 | **`4fa4f07e7`**（升自 `01479a857`；Ubuntu L4 + 双端 129） |
| 产品 tip | **`9126f207b`**（wave708） |
| 当前波次 | **wave708 已闭** → match struct 字段通配符 / field bind + 显式 guard / Cap residual |
| 构建级别 | 钉盘 **tip L4 放行**；wave708 **L2** |
| bstrict | 钉盘 ✅ **OK (129)**；wave708 未全量 |

---

## 1. wave707–708 已修（host-C match struct field bind + 字段 lit guard）

| 项 | 内容 |
|----|------|
| wave707 | `Point { x, y } => x + y` field bind → codegen VAR 发 `(subject).field`；tip `d79bea121` |
| wave708 | `Point { x: 0, y: 0 } => 1` 多臂 classify → parser 构造隐式 guard `subject.x==0 && subject.y==0` → `MatchArmEntry.guard_ref`；codegen stmt/expr 双路径；tip `9126f207b` |
| -E 验证 | expr path: `(p.x==0) && (p.y==0) ? 1 : ...`；stmt path: `if ((p.x==0) && (p.y==0)) {...}` |
| 回归 | match_guard / match_struct_bind / match_struct_destructure_min 绿；hello 矩阵绿 |
| pre-existing fail | match_struct_classify / match_struct_destructure（钉盘也 fail；含 `y: _` 通配符 + `if x > y` 显式 guard，属 wave709+ 范围） |

---

## 2. 前排 residual

| # | 叶 | 状态 | 备注 |
|---|-----|------|------|
| 1 | match struct 字段通配符值 `Point { x: 0, y: _ }` | **下一硬叶候选** | wave708 直接延伸；parser 遇 `_` 静默丢弃函数 |
| 2 | match struct field bind + 显式 guard `Point { x, y } if x > y` | open | wave707 + wave700 组合；闭合 match_struct_classify 第 3 arm |
| 3 | Cap residual 其它硬叶 | open | skip untyped `self`；LANG-006 保留 |
| 4 | freestanding match field bind | soft | host-C 已绿 |

---

## 3. 执行纪律提醒

- **钉盘 = 产品放行面**；日常 Cap 微步 **L2**，勿把 L2 写成 tip L4  
- 整波 Cap 收口或 SHARED ABI/glue 大改 → 再 **L4 真冷 + 129** 后谈下一次升钉  
- untyped `self` / LANG-006：**不**当本波糊绿目标  
- PLATFORM: SHARED 改动须双端；Ubuntu 金标  
