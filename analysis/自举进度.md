# ShuX 自举进度

> **更新**：2026-07-20 · 19 号全日已归档（见 §6 归档行）；本日从 20 号起重新起表  
> **基线 SHA（产品 L4 放行钉盘）**：**`deaf773b`**（双端真冷 L4+**125** @ 钉盘时；前钉 `305cfbe1`；本波 mac+Ubuntu 双端 L4 真冷全绿 + Windows hybrid 门禁首次全绿 → 升钉）  
> **产品 bstrict 套件数**：**125**（`tests/run-all-bstrict.sh`；日志须见 `run-all-bstrict OK (125 scripts…)`）  
> **本波（20 日）状态**：**CODEGEN BUG parser 根源修复 + 端到端 -E 验证通过 + seed re-pin · 3 commit · tip `93126709`**（`export extern "C" function ... { body }` 带 body 时整个函数不被 codegen 发射 · 根因：parser TOKEN_EXTERN 分发遇到 `{` 时跳过整个声明导致函数未注册 · 修复：C seed 检测 `{` 设 `has_body=1` + `is_extern=0` + .x 层调用 `parse_block_into` 解析 body · 3 commit · L2 + 端到端 -E 验证通过 · 未跑 L4 待 C6 波 3 整波 L4）。**3 commit**：① `f5435136` parser.x 6 处修改 + parser_asm_skip_tl_slice.inc 4 处 + 3 处 C 镜像 struct 字段同步（has_body 字段）；② `e63ef396` Makefile L1927 `RELINK_SHUX_GLUE_SUFFIX` 加 `src/runtime_driver_strict_glue_stubs.o`（修 `make shux-x` 在 macOS arm64 上的 12 个 undefined symbols：`_parse`/`_lexer_new`/`_typeck_module`/`_pipeline_block_labeled_set_names` 等，对齐 `DRIVER_SEED_GLUE_SUFFIX`）；③ `93126709` parser_gen.c re-pin（用 `shux-x -x -E -E-extern src/parser/parser.x` 重新生成 8555 行 → 注入 C-04 markers + extern 声明 → strip `int32_t main(void)`（mirrors `fcb44879`）→ 最终 8530 行 · has_body=2 · C-04=2 · main=0）。**重编验证**：`rm -f parser_x.o && make parser_x.o` exit=0（164288 bytes 含 has_body）+ `make shux-x` exit=0（3676104 bytes 含修复后 parser）。**端到端 -E 验证 ✅ 通过**：`./compiler/shux-x -x -E -E-extern -o /tmp/runtime_io_abi_gen.c compiler/src/runtime_io_abi.x` 生成 C 包含 `runtime_io_abi_fs_posix_close_c` / `fs_posix_read_c` / `fs_posix_write_c` 3 个函数 body（委托 std_fs_fs_close / fs_read / fs_write）；对比修复前 body 完全不被发射；**验证方式**：仅重建 parser_x.o + 重链 shux-x（不重建 pipeline_x.o），parser 修复通过 parser_x.o 生效，不触发用户禁止的 shux-x -E 全量 dep codegen / pipeline_x.o relinking。**前波**：`9579124c` shux-x -E 爆内存方案 B（ArenaSidecar/ModuleSidecar release API · -E 内存从 ~5GB 降至 90.7 MB）· C6 波 3 整波 8/8 收口 `8c2bad96`（std/ 557 个 `extern "C"` 标注）· C7 plan 瘦身 8/8 收口 `1d41e903`（125→129 待 L4 验证）· Windows 门禁收口 `deaf773b` 升钉（codex 7 commit 修复 Windows MSYS2/MinGW hybrid 门禁；最终真根因：`build_shux_asm.sh` `ensure_runtime_cc_stubs` 覆盖 `runtime_driver.o` → g05 链接污染）· `305cfbe1` C6 波 2 ABI 一致性升钉 · `d25b82cc` Windows shux-c exit=1 根因修复（mac+Ubuntu L4 全绿）· 波 1 unsafe 包裹 `4214eea4` 升钉 · C6 P0b audit `7e507fb6` · shux-x 5GB 方案 A `a14a1dd0` · C7 PoC `e546b013`+`bedb40ca` · 日终 L4 兜底 `94a8e7d5` · 5 波 C5 CTFE

> **方法**：[自举方法.md](自举方法.md)（Cap/R/L/M；工程 ≠ 产品放行）  
> **Windows（删种子前）**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md) — **删除种子 / G 清场前须跑通 Windows B-hybrid 最小门禁**（探针，≠ 125 金标；见本文 §5）  
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `shux-selfhost-product-gate`  
> **归档**：[自举进度-归档-2026-07-19.md](自举进度-归档-2026-07-19.md) · [自举进度-归档-2026-07-18.md](自举进度-归档-2026-07-18.md) · [自举进度-归档-2026-07-17.md](自举进度-归档-2026-07-17.md) · [自举进度-归档-2026-07-16.md](自举进度-归档-2026-07-16.md) · [自举进度-归档-2026-07-14.md](自举进度-归档-2026-07-14.md) · [自举进度-归档-2026-07-13.md](自举进度-归档-2026-07-13.md)

### 维护约定

1. 每完成一波 **必改两文**：
   - **本文**（长期仪表盘）：§2 KPI、§3 前排（≤3 条）、§6 一行；
   - **[`当前进度.md`](当前进度.md)**（当天快照）：本波 / SHA / 下一波前排。
2. **禁止**把 prove/Stage2/N++ 单独写成「可自举」。
3. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict` **125**（现行白名单；历史钉盘日志可能写 123）。
4. 工程 KPI 必须写 **SHA + 日志路径**；**过期整日流水移归档**（18 号全日已入归档），不在正文假装仍绿。
5. **`问题分析文档.md`：默认不改。** 仅新产品红 / 整波新债开场 / 根因换层（见 `自举步骤.md` §5.1）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 `deaf773b`（升自 `305cfbe1`；mac+Ubuntu 双端 L4 真冷 + 125 bstrict 全绿 + Windows hybrid 门禁首次全绿 → 升钉）。**  
**本波（2026-07-20）= CODEGEN BUG parser 根源修复 + 端到端 -E 验证通过 + seed re-pin @ tip `93126709`**（`export extern "C" function ... { body }` 带 body 时整个函数不被 codegen 发射 · 根因：parser TOKEN_EXTERN 分发遇到 `{` 时跳过整个声明导致函数未注册 · 修复：C seed 检测 `{` 设 `has_body=1` + `is_extern=0` + .x 层调用 `parse_block_into` 解析 body · 3 commit `f5435136` + `e63ef396` + `93126709` · L2 + 端到端 -E 验证通过 · 未跑 L4 待 C6 波 3 整波 L4）；**前波 shux-x -E 爆内存方案 B @ tip `9579124c`**（ArenaSidecar/ModuleSidecar release API · -E 内存从 ~5GB 降至 90.7 MB · L2 验证未跑 L4）；**前波 C6 波 3 子波 1+2+3+4+5+6+7+8 @ tip `2ecffeca`+...+`8c2bad96`**（std/ 557 个 `extern "C"` 标注 · 整波 8/8 收口 · 100% · L2 验证未跑 L4）；**前波 C7 plan 瘦身 8/8 收口 @ tip `1d41e903`**（路径 C+A 混合 125→129 · L2 验证未跑 L4）；**前波 C6 波 1+波 2 Windows 门禁补跑收口 · codex 7 commit @ tip `deaf773b`**（双端 L4 真冷全绿 + Windows hybrid 门禁首次全绿 → 升钉 `305cfbe1` → `deaf773b`）：

- ① **本波 parser 根源修复 + 端到端 -E 验证通过 + seed re-pin**：R2 真迁评估发现 `export extern "C" function fs_posix_close_c(fd: i32): i32 { body }` 带 body 时整个函数不被 codegen 发射。**根因（上帝视角）**：parser top-level TOKEN_EXTERN 分发 → `parse_one_extern_skip_into_slice_c` 期望 `;` 遇到 `{` fail → `parse_one_extern_and_add_into_slice_c` 调用 `parser_skip_one_extern_into_glue` 跳过整个声明 → 函数完全没注册到 module → codegen 无函数可发射。**修复方案（双层级联）**：① C seed 层（`parser_asm_skip_tl_slice.inc`）：检测 `{` 设 `has_body=1` + `is_extern=0`（注册函数但不跳过 body）；② .x 层（`parser.x` TOKEN_EXTERN 分发 source/buf 双路径）：`parse_one_extern_and_add_into` 注册后检查 `is_extern==0` → `parse_block_into` 解析 body + `pipeline_module_func_set_body_ref`。**AST 双字段设计**：`is_extern`（纯声明标记）vs `abi_kind`（ABI 标记）正交不冲突。**3 commit**：① `f5435136` parser.x 6 处 + parser_asm_skip_tl_slice.inc 4 处 + 3 处 C 镜像 struct 字段同步；② `e63ef396` Makefile L1927 `RELINK_SHUX_GLUE_SUFFIX` 加 `src/runtime_driver_strict_glue_stubs.o`（修 `make shux-x` macOS arm64 12 个 undefined symbols，对齐 `DRIVER_SEED_GLUE_SUFFIX`）；③ `93126709` parser_gen.c re-pin（8530 行 · has_body=2 · C-04=2 · main=0 strip）。**重编验证**：parser_x.o exit=0（164288 bytes 含 has_body）+ shux-x exit=0（3676104 bytes 含修复后 parser）。**端到端 -E 验证 ✅ 通过**：`shux-x -x -E -E-extern -o /tmp/runtime_io_abi_gen.c compiler/src/runtime_io_abi.x` 生成 C 包含 3 个函数 body（fs_posix_close_c/read_c/write_c 委托 std_fs_fs_*）；对比修复前 body 完全不被发射；仅重建 parser_x.o + 重链 shux-x（不重建 pipeline_x.o），parser 修复通过 parser_x.o 生效，不触发用户禁止的 shux-x -E 全量 dep codegen / pipeline_x.o relinking。**构建级别 L2 + 端到端 -E 验证通过**（未跑 L4 · 待 C6 波 3 整波 L4 一并跑）。**PLATFORM: SHARED**。
- ② **前波 shux-x -E 爆内存方案 B**：[shux-x-5GB-内存根因调查.md](shux-x-5GB-内存根因调查.md) §4 真凶定位 `g_arena_sc[512]` ArenaSidecar 池（18 GrowVec × 4.3 MB init cap）+ `g_module_sc[512]` ModuleSidecar 池（11 GrowVec）永不释放 → GB 级。**方案 B**：ast_pool.c 新增 `arena_sidecar_free` / `module_sidecar_free` static + `ast_pool_arena_release` / `ast_pool_module_release` 公开 API；rt_run_x_emit.from_x.c 所有 free 路径（~20 处含错误 goto）前调用 release。**-E 内存验证**：1 import 12.6 MB / 3 imports 32.2 MB / std.fmt transitive 90.7 MB（从原 ~5GB 显著降低）。**构建级别 L2**（待 C6 波 3 整波 L4 一并跑）。**PLATFORM: SHARED**。
- ③ **前波 C6 波 3 整波 8/8 收口**：[C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.4 子波划分（按文件大小递增）。std/atomic 30 + std/db/arrow 29 + std/db/sqlite 32 + std/math 32 + std/json 37 + std/async 42 + std/socketio 106 + std/http 249 = **557 个 `extern "C"` 标注 · 100%**。子波 8 修复 C 风格注释 `*/` 序列陷阱。**构建级别 L2**（未跑 L4 避免爆内存）。
- ④ **Windows 门禁补跑收口已完成**（[问题分析文档.md](问题分析文档.md) §9 codex 测试结果）：C6 波 1+波 2（ABI 边界改动）按 §5.1 强制要求合并补跑 Windows hybrid 最小门禁。codex 7 commit 修复 Windows MSYS2/MinGW hybrid 门禁从阻塞→全绿：① `1c3b54b4` docs 平台限制指南 ② `3f495941` fix g05_relink_env MINGW host ③ `47ea02c0` fix gen_asm_full_link_stubs.pl 原子替换 ④ `41f1fe7a` fix g05_ensure 重新生成 stale stubs ⑤ `ba9e03b4` fix Windows gate 捕获真实 exit code（防假绿）⑥ `e56ab351` fix g05 link order 对齐 Makefile + smoke exe 签名 ⑦ `9f68cda6` fix build_shux_asm 不覆盖 runtime_driver.o（**最终真根因**）+ `deaf773b` docs 记录测试结果。**最终真根因**：`build_shux_asm.sh` `ensure_runtime_cc_stubs` 用残缺 flags 覆盖 make/g05 已构建的 `runtime_driver.o` → g05 链接污染；修复为不覆盖已存在的 `runtime_driver.o`。**三平台验证矩阵**：mac arm64 L4 真冷全绿 @ `deaf773b`（用户验证）+ Ubuntu x86_64 L4 真冷全绿 @ `deaf773b`（金标 · 本会话验证 · shux 3490000 bytes + g05 60 objs Linux/x86_64 + return-value 42 + bstrict 125）+ Windows MSYS2 hybrid warm 绿 @ `deaf773b`（4 项：bootstrap-driver-bstrict + return-value 42 + win32-write-gate + win32-read-file-gate + C-03）。**升钉决策**：✅ 升钉 `305cfbe1` → `deaf773b`（满足产品放行钉盘条件：L4 真冷（至少 Ubuntu）+ 双端 125 全绿；mac+Ubuntu 双端 L4 真冷全绿 + Windows hybrid 门禁首次全绿）。**Windows L4 真冷测非必需**（AGENTS.md §5 双端门禁 = macOS + Ubuntu；§5.1 Windows hybrid 绿 = 探针可用 ≠ 产品 tip L4）。
- ⑤ **前波（19 日）**：C6 X ABI P0b 波 2（ABI 一致性修复 3 处）@ `305cfbe1`（ABI 边界 → L4 双端真冷 + 升钉 `4214eea4` → `305cfbe1`；Windows 门禁待补跑 → **本波已收口并升钉**）。改动 `compiler/src/runtime_io_abi.x` L164/180/196 共 3 处定义 `export function fs_posix_*_c` → `export extern "C" function fs_posix_*_c`（保留 `#[no_mangle]`），与 5 个声明点（main.x:30-31 / pipeline.x:81-82 / driver/emit.x:71-73）对齐；每处加 4 行英文 ABI 注释说明契约依据 + 委托 `std_fs_fs_*` 设计理由；函数体不变。mac L4 log `/tmp/mac_true_bstrict_305cfbe1.log` · Ubuntu L4 log `/tmp/ubuntu_true_bstrict_305cfbe1.log` 各 `run-all-bstrict OK (125 scripts)` exit=0；Ubuntu link-hardening gate OK（Linux 专有）。
- ⑥ **前波归档**：`d25b82cc` Windows shux-c exit=1 根因修复（mac+Ubuntu L4 全绿）· 波 1 unsafe 包裹 9 处 @ `4214eea4` · C6 P0b audit @ `7e507fb6` · shux-x 5GB 方案 A @ `a14a1dd0` · C7 plan 瘦身 PoC @ `e546b013`+`bedb40ca`（2/8 完成，2 bug 转后续）· 日终 L4 兜底 @ `94a8e7d5` · 5 波 C5 CTFE（`229708f1`/`a8a67708`/`d7a2388e`/`c174e70a`/`681b168f`）。

**已入钉盘（`deaf773b`）**：C2 std.net PRIMARY UNDEF gate · C3=C4 parser bare-`{` struct-lit · backtrace L4 cold · C8 vec/set/map/queue duplicate · C6 P0b 波 1 unsafe 包裹 9 处 · C6 P0b 波 2 ABI 一致性 3 处 · **Windows 门禁收口 codex 7 commit**（详见 §6 变更记录）。

**C1 复核结论**：T10 候选 C1（build error exit code 传播）在 `a74e25a1` 已修（`main_argv_has_o_flag` + `main_cmd_run` 显式 -o compile-only；build/run/bare 三路径 build error 都返回非 0），无需再做。

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](自举方法.md) | Cap / R / L / M |
| [自举步骤.md](自举步骤.md) | 可执行闸门 |
| **本文** | 长期 KPI + 前排（**每波必改**） |
| [当前进度.md](当前进度.md) | 当天快照（**每波必改**） |
| **[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)** | **删种子前先跑 Windows hybrid**；最小门禁 vs 125 金标；三阶段与 C6 触发（**决议**） |
| [自举进度-归档-2026-07-18.md](自举进度-归档-2026-07-18.md) | **18 号全日**（只读） |
| [自举进度-归档-2026-07-17.md](自举进度-归档-2026-07-17.md) | 17 号全日（只读） |
| [自举进度-归档-2026-07-16.md](自举进度-归档-2026-07-16.md) | 16 号全日（只读） |
| [自举进度-归档-2026-07-14.md](自举进度-归档-2026-07-14.md) | 14 号整表（只读） |
| [自举进度-归档-2026-07-13.md](自举进度-归档-2026-07-13.md) | G-02f thin 流水账（只读） |
| [问题分析文档.md](问题分析文档.md) | 产品/大债地图（**非每波**） |
| skill `shux-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **125** | `tests/run-all-bstrict.sh` |
| 产品 L4 放行钉盘 | **`deaf773b`** | 20 日升钉（前钉 `305cfbe1`）；mac+Ubuntu 双端 L4 真冷 + 125 全绿 + Windows hybrid 门禁首次全绿 → 升钉 |
| 本波 tip | **`93126709`**（fix(parser): re-pin parser_gen.c seed with body-bearing extern C fix · parser 修复 seed pin） | 3 commit（`f5435136` parser fix + `e63ef396` Makefile L1927 + `93126709` parser_gen.c re-pin）· L2 + 端到端 -E 验证通过 · 未跑 L4 |
| 本波 mac L4（20 日） | ✅ **`run-all-bstrict OK (125 scripts)` exit=0** @ `deaf773b` | 用户验证；全擦 .o + 删二进制 + bootstrap-driver-seed + g05 relink + bstrict 125 |
| 本波 Ubuntu L4（金标 · 20 日） | ✅ **`run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0** @ `deaf773b` | 本会话验证；git pull + 全擦 .o + 删二进制 + bootstrap-driver-seed (shux 3490000 bytes) + g05_prepare_and_relink (60 objs Linux/x86_64) + return-value 42 + bstrict 125（SHUX_BSTRICT_SKIP_BUILD=1）；耗时 ~3.5 分钟 |
| 本波 Windows hybrid（20 日） | ✅ **`GATE_EXIT=0`** @ `deaf773b`（warm 绿） | `SHUX_WIN_BSTRICT=1 ./tests/run-bootstrap-bstrict-windows-gate.sh`；4 项全过：bootstrap-driver-bstrict + return-value 42 + win32-write-gate + win32-read-file-gate + C-03 |
| 19 日 L4 mac arm64（波 2 钉盘） | ✅ **`run-all-bstrict OK (125 scripts)` exit=0** @ `305cfbe1` | log `/tmp/mac_true_bstrict_305cfbe1.log` |
| 19 日 L4 Ubuntu x86_64（金标 · 波 2 钉盘） | ✅ **`run-all-bstrict OK (125 scripts)` exit=0** @ `305cfbe1` | log `/tmp/ubuntu_true_bstrict_305cfbe1.log`；link-hardening gate OK（Linux 专有） |
| 工程轨 KPI（T/N/R2/M） | 继承 18 号结案 | 本波未动工程轨 |

### 2.2 工程轨（继承 18 号结案）

| KPI | **现** | 备注 |
|-----|--------|------|
| **T** | **18/18** | ✅ 满 |
| **EMPTY** | **18/18** | ✅ 满 |
| **N** | **54/54 IDENTICAL** @ 18 号 tip | 回归基线 |
| **R2 / H** | Cap pure 收口；WPO ✅；typeck/codegen M ✅ | 18 号结案状态 |

***

## 3. 当前前排（≤4 条）

1. **R2 真迁一个 rest 模块（前置条件已满足 · 启动）**：用户明确要求"按你的建议进行下一步自举"。**前置条件已满足**：本波 parser 修复端到端 -E 验证通过 ✅（`export extern "C" function ... { body }` 能正确发射 fs_posix_close_c/read_c/write_c body）。选定一个 rest 模块进行 R2 真迁（按 [自举步骤.md](自举步骤.md) §2 R2 模板）。模块选择标准：rest 能力够、hybrid 仍有业务 C、矩阵绿。验证：prove IDENTICAL + Ubuntu 金标 + §0.D 产品矩阵。**同步到 Ubuntu 后启动评估**。详见 [当前进度.md](当前进度.md) §下一波前排 #1
2. **C6 波 3 整波 L4 验证（含 parser 修复 L4 + C7 白名单）**：① C6 波 3 整波 8/8 收口（557 个 `extern "C"` 标注 · `2ecffeca`+...+`8c2bad96`）；② shux-x -E 内存修复（`9579124c` · 90.7 MB vs 原 ~5GB）后 shux_asm 构建可考虑跑；③ 本波 parser 修复 L4 真冷验证（端到端 -E 已通过，待 L4 真冷）；④ C7 4 个新加白名单脚本 + fmt 一元负号 LEGACY 重链接后端到端验证。**触发条件**：用户允许跑 pipeline_x.o relinking + shux_asm 构建时。详见 [当前进度.md](当前进度.md) §下一波前排 #2
3. **C7 plan 瘦身 8/8 完成（待 L4 验证）**：[C7-plan-audit.md](C7-plan-audit.md) audit 881 总脚本 vs 125 白名单；路径 C + 路径 A 混合 8/8 完成 @ `e546b013`+`bedb40ca`+本波（3 合并 + 1 删除 + 4 纳入白名单 125→129）。**修复 2 个副产品 bug**：① std.debug.println 返回值语义（已修复，返回 0/-1）② fmt 一元负号（源码已修复 `runtime_lsp_glue.from_x.c` is_return 识别，端到端验证待 LEGACY 重链接，`run-fmt-cmd.sh` 检测旧种子时条件性跳过）。**待验证**：4 个新加白名单脚本（run-perf-simd-shuxffle-select.sh 等）需 shux_asm 构建后验证（避免爆内存未构建）。详见 [当前进度.md](当前进度.md) §下一波前排 #1

**已收口（20 日 tip）**：**CODEGEN BUG parser 根源修复 + 端到端 -E 验证通过 + seed re-pin（`f5435136` + `e63ef396` + `93126709` · 待 L4 验证）** · **shux-x -E 爆内存方案 B（`9579124c` · 待 L4 验证）** · **C6 波 3 整波 8/8 收口（`8c2bad96` · 557 个标注 · 待 L4 验证）** · **Windows 门禁补跑收口（`deaf773b` 升钉）** · C6 X ABI P0b 波 2 ABI 一致性 3 处（`305cfbe1` 升钉） · C6 X ABI P0b 波 1 unsafe 包裹 9 处（`4214eea4` 升钉） · Windows shux-c exit=1 根因修复（`d25b82cc` mac+Ubuntu L4 全绿） · **CLI Phase 1 脚本迁移（5 commit · 208 处）** · **CLI help 美化（3 commit）** · **std.print 架构优化** · shux-x 5GB 方案 A（`a14a1dd0`）· **C7 plan 瘦身 8/8（3 合并 + 1 删除 + 4 纳入白名单 125→129 · 待 L4 验证）** · C5 EXPR_TERNARY+IF+BLOCK CTFE 三连击（`c174e70a`）· 5 波 C5 CTFE —— 详细见 [当前进度.md](当前进度.md) §本波摘要

**不在前排**：
- **C5 EXPR_CLOSURE CTFE**（独立大工程 · 多波）：已 scoping（[C5-EXPR_CLOSURE-CTFE-scoping.md](C5-EXPR_CLOSURE-CTFE-scoping.md)）。SHUX 当前无 closure 支持任何层（无 parser 语法、无 AST 节点、无 TYPE_FN、无 typeck、无 codegen）。需 5 波独立工程：① fn 类型系统（1.5 波）→ ② EXPR_CLOSURE AST+parser（1.5 波）→ ③ typeck capture analysis（2 波）→ ④ codegen lowering（2 波）→ ⑤ CTFE fold（0.5 波，本 Cap）。总估时 ≈ 7.5 波。顺延自举后立项 `lang-closure-v1-rfc.md`，明确语法选择（Rust `|x|...` / Swift `{x in ...}` / Zig `fn(x) {...}`）+ capture 策略 + Linear/owned move 语义 + region 推断交互
- **Cap C9 match guard full-chain 支持**（独立大工程 · 多波）：经调查 guard 不在 cap-candidates C5 范围内。X pipeline `MatchArmEntry` 5 字段无 `guard_ref`；`pipeline_expr_append_match_arm` 签名无 guard 参数；`parser_parse_match_into_glue` extern 声明无定义；ELF emit / CTFE handler 均不处理 guard。要完整支持 guard 需要 6 个产生点改动（ast_pool 加字段 + append 加参数 + parser 实装 + typeck scope + emit 分支 + CTFE handler），**多波拆分**，先排期再动手
- tip L4 **`deaf773b`**（= 钉盘；20 日 `deaf773b` 升钉——双端 L4 真冷全绿 + Windows hybrid 门禁首次全绿）；C2 std.net UNDEF gate + C3=C4 parser bare-`{` struct-lit + backtrace L4 cold + C8 vec/set/map/queue duplicate + C6 P0b 波 1 unsafe 包裹 9 处 + C6 P0b 波 2 ABI 一致性 3 处 + **Windows 门禁收口 codex 7 commit** 均已入钉盘。

**不在前排**：18 号的 typeck CTFE / WPO / ASM PRIMARY / named_layout / slice / anon struct backfill / main_cmd_run -o 全部已入 18 号归档。

***

## 4. 与 18 号归档的关系

- 18 号全日自举仪表盘 + 变更流水已整表移入 [`自举进度-归档-2026-07-18.md`](自举进度-归档-2026-07-18.md)；**勿把归档内中间 tip / 单叶日志当 19 日现绿**。
- 产品 L4 放行 tip 仍以本文为准：18 日结案钉盘 `c51759eb` → 19 日凌晨 `a74e25a1`（同 18 号结案升钉序列的延续，归档快照截止 18 日 21:46 `b3540784` 钉盘 `c51759eb`；`a74e25a1` 是 19 日凌晨的升钉，本文承接）。
- 19 日新工作（C2 + 文档归档/起表）从本文起记录。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
|------|----------|
| **macOS arm64** | **L4 真冷全绿**（20 日 @ `deaf773b`）：用户验证；全擦 .o + 删二进制 + bootstrap-driver-seed + g05 relink + `run-all-bstrict OK (125 scripts)` exit=0 |
| **Ubuntu x86_64（金标）** | **L4 真冷全绿**（20 日 @ `deaf773b`）：本会话验证；git pull + 全擦 .o + 删二进制 + `bootstrap-driver-seed OK` (shux 3490000 bytes) + `g05_prepare_and_relink OK` (60 objs Linux/x86_64) + return-value 42 + `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0（SHUX_BSTRICT_SKIP_BUILD=1）；耗时 ~3.5 分钟 |
| **Windows MSYS2（探针 · 非自举金标）** | **hybrid warm 绿**（20 日 @ `deaf773b`）：`SHUX_WIN_BSTRICT=1 ./tests/run-bootstrap-bstrict-windows-gate.sh` GATE_EXIT=0；4 项全过：bootstrap-driver-bstrict + return-value 42 + win32-write-gate + win32-read-file-gate + C-03。codex 7 commit 修复 Windows MSYS2/MinGW 兼容（最终真根因：`build_shux_asm.sh` 覆盖 `runtime_driver.o`）。**Windows L4 真冷测非必需**（双端门禁 = macOS + Ubuntu 已满足；§5.1 Windows hybrid 绿 = 探针可用 ≠ 产品 tip L4） |

### 5.1 Windows 与删种子（策略锚点 · 非本波 KPI）

> **决议全文**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)

| 项 | 口径 |
|----|------|
| **删种子 / G 清场前** | **必须**先把 Windows **B-hybrid 最小门禁**跑通并保持可回归（阶段 1 DoD）；**禁止**「先彻底删种子，再第一次认真碰 Windows」 |
| **现在** | **可以**跑 hybrid 探针；不必等完全自举 / tip 升钉 |
| **最小门禁（权威）** | MSYS2 / `windows-server` 上：`./tests/run-bootstrap-bstrict-windows-gate.sh`（默认 hybrid） |
| **不是** | Windows 上 `./tests/run-all-bstrict.sh`（~125）当阶段 1 主验收；Windows L4 钉盘；宣称 Win 已零 C / 已自举 |
| **金标仍是** | Ubuntu（+ SHARED 时 mac）**125** `run-all-bstrict` + L4 |
| **强制重跑 Windows 门** | **C6 / ABI / link / `std.sys`** 大改前后；**宣称 G / 删种子**之前；阶段 2 合并点 |
| **进度话术** | Windows hybrid 绿 = **探针可用**；**≠** 产品 tip L4 / 可自举 |

***

## 6. 变更记录（倒序 · 20 日起；19 号见归档）

| 日期 | SHA | 摘要 |
|------|-----|------|
| 2026-07-20 | **`93126709`**（parser_gen.c re-pin · L2 + 端到端 -E 验证通过） | **parser_gen.c seed re-pin（反映 `f5435136` parser 根源修复）· 1 commit · L2 + 端到端 -E 验证通过**：re-pin parser_gen.c 以反映 commit `f5435136` 的 parser 根源修复（`export extern "C" function ... { body }` 带 body 时 codegen 正确发射）。**修复前**：codegen 将带 body 的 extern C 函数错误发射为 header-only extern 声明，丢弃函数定义。这破坏了 `runtime_io_abi.x` 中 `fs_posix_*_c` 委托 `std_fs_fs_*` 的 helpers。**修复后**：codegen 检查 `has_body`；当存在时，发射完整函数定义（header extern 声明 + body）。**重新生成流程**：① `shux-x -x -E -E-extern src/parser/parser.x` → 8555 行；② 注入 C-04 markers + extern 声明（在第一个 `#include <stdint.h>` 后，避免 `int32_t` 未定义）；③ strip `int32_t main(void)`（产品 seed 不导出 main，mirrors `fcb44879` "strip main"）；④ 最终 8530 行。**has_body count=2** / **C-04 marker count=2** / **main count=0**（stripped）。**端到端 -E 验证 ✅ 通过**：`./compiler/shux-x -x -E -E-extern -o /tmp/runtime_io_abi_gen.c compiler/src/runtime_io_abi.x` 生成 C 包含 `runtime_io_abi_fs_posix_close_c` / `fs_posix_read_c` / `fs_posix_write_c` 3 个函数 body（委托 `std_fs_fs_close` / `std_fs_fs_read` / `std_fs_fs_write`）。**验证方式**：仅重建 parser_x.o + 重链 shux-x（不重建 pipeline_x.o），parser 修复通过 parser_x.o 生效，不触发用户禁止的 shux-x -E 全量 dep codegen / pipeline_x.o relinking。**重编验证**：`rm -f parser_x.o && make parser_x.o` exit=0（164288 bytes 含 has_body，比旧版 162448 大）+ `make shux-x` exit=0（3676104 bytes）。**构建级别 L2 + 端到端 -E 验证通过**（未跑 L4 · 待 C6 波 3 整波 L4 一并跑）。**PLATFORM: SHARED** — 影响 macOS + Ubuntu dep codegen 路径。权威 compiler/parser_gen.c（8530 行 · has_body=2 · C-04=2 · main=0 strip） |
| 2026-07-20 | **`e63ef396`**（Makefile L1927 fix · 修 `make shux-x` 链接失败） | **Makefile `RELINK_SHUX_GLUE_SUFFIX` 加 `src/runtime_driver_strict_glue_stubs.o` · 1 commit · L2 验证通过**：`RELINK_SHUX_GLUE_SUFFIX`（`make shux-x` 链接规则使用）缺少 `src/runtime_driver_strict_glue_stubs.o`（只有 `build_asm/pipeline_glue_strict_minimal.o`），导致 `make shux-x` 在 macOS arm64 上失败，12 个 undefined symbols：`_parse` / `_lexer_new` / `_typeck_module`（referenced from `lsp_diag.o`）/ `_pipeline_block_labeled_set_names`（referenced from `parser_x.o`）/ `_codegen_set_preamble_has_core_option_result` / `_codegen_wpo_mono_sym_format` / `_preprocess_define_add` / `_preprocess_define_reset` / `_preprocess_eval_condition_c` / `_shu_lsp_resolve_and_load_imports` / `_typeck_one_function`。**根因**：`RELINK_SHUX_GLUE_SUFFIX` 未与 `DRIVER_SEED_GLUE_SUFFIX` 对齐（后者已含 stubs .o）。stubs 文件提供：① `SHUX_WEAK` defs（parse/lexer_new/typeck_module）— 在 ELF/Darwin 上被 pipeline_x.o/parser_x.o/typeck_x.o 的真实实现覆盖；② 强 stub-only def（pipeline_block_labeled_set_names）— 无真实实现，无重复符号风险。**修复**：L1927 加入 `src/runtime_driver_strict_glue_stubs.o`，mirror `DRIVER_SEED_GLUE_SUFFIX`。**双平台安全**：macOS/Linux（ELF weak 覆盖）+ Windows（PE 用 `WIN_LDFLAGS` 的 `--allow-multiple-definition`）。**新增注释**（L1935-1942）记录 bug 来源 + 修复理由 + PLATFORM: SHARED。**本机验证**：macOS arm64 `make shux-x` exit=0（修复前 12 个 undefined symbols）。**构建级别 L2**（macOS 重链验证通过 · 未跑 L4）。**PLATFORM: SHARED**。权威 compiler/Makefile:1927（RELINK_SHUX_GLUE_SUFFIX）+ L1936（DRIVER_SEED_GLUE_SUFFIX 对齐参考）+ L2054-2055（stubs .o 编译规则）+ L2651-2654（shux-x 链接规则）+ include/shux_weak.h（SHUX_WEAK 宏定义）· seeds/runtime_driver_strict_glue_stubs.from_x.c:71（parse SHUX_WEAK stub）· L916（pipeline_block_labeled_set_names 强 stub-only def） |
| 2026-07-20 | **`f5435136`**（parser 根源修复 · L2 验证待 C6 波 3 整波 L4） | **CODEGEN BUG parser 根源修复（`export extern "C" function ... { body }` 不被 codegen 发射）· 1 commit · L2 验证**：R2 真迁评估发现 `export extern "C" function fs_posix_close_c(fd: i32): i32 { body }` 带 body 时整个函数不被 codegen 发射。**根因（上帝视角）**：parser top-level TOKEN_EXTERN 分发 → `parse_one_extern_skip_into_slice_c` 期望 `;` 遇到 `{` fail → `parse_one_extern_and_add_into_slice_c` 调用 `parser_skip_one_extern_into_glue` 跳过整个声明 → 函数完全没注册到 module → codegen 无函数可发射。**修复方案（双层级联）**：① C seed 层（`parser_asm_skip_tl_slice.inc`）：检测到 `{` 时设 `has_body=1` + `next_lex` 指向 `{` 前 + 写入 name/return_ty_ref/num_params 后 return（不跳过 body）；`parse_one_extern_and_add_into_slice_c` 修改 `f.is_extern = (res.has_body == 1) ? 0 : 1`；② .x 层（`parser.x` TOKEN_EXTERN 分发 source/buf 双路径）：调用 `parse_one_extern_and_add_into` 注册函数后，检查 `pipeline_module_func_is_extern_at(module, fi_ext) == 0`，若是则 `lexer_next_into` 获取 TOKEN_LBRACE + `parse_block_into` 解析 body + `pipeline_module_func_set_body_ref` 设置 body_ref。**AST 双字段设计**：`is_extern`（标记纯声明无 body）vs `abi_kind`（0=X ABI, 1=C ABI）— 正交语义不冲突。**修改文件**：① `compiler/src/parser/parser.x` 6 处（L133 extern 声明 + L6583 ExternParseResult has_body 字段 + L6648 extern_parse_set_fail 清零 + L7666 source 路径 body 解析 + L9772 buf 路径 body 解析 + 英文注释）；② `compiler/seeds/parser_asm/parser_asm_skip_tl_slice.inc` 4 处（L953 fail 清零 + L996 初始化 + L1128 LBRACE 检测块 + L1207 is_extern 条件设置）；③ 3 处 C 镜像 struct 字段同步（`pthin_skip_tl.from_x.c` L53 / `parser_asm_thin_c.from_x.c` L115 / `pthin_glue.from_x.c` L87 新增 `int32_t has_body;`）。**本机验证**：① `parser_asm_thin_c.from_x.c` 在 Ubuntu 上 `cc -c` 编译 exit=0（仅预存 format warning）；② 确认 `parser_asm_thin_c.from_x.c` L4473 `#include "parser_asm_skip_tl_slice.inc"` → 重编 `parser_asm_thin_glue.o` 即包含 C seed 修改；③ struct 字段布局一致性确认（3 镜像 + .x 定义 + .inc 全部 has_body 字段）。**端到端 -E 验证 ✅ 通过**（在后续 commit `93126709` parser_gen.c re-pin 后完成）：`./compiler/shux-x -x -E -E-extern -o /tmp/runtime_io_abi_gen.c compiler/src/runtime_io_abi.x` 生成 C 包含 3 个函数 body（fs_posix_close_c/read_c/write_c 委托 std_fs_fs_*）。**验证方式**：仅重建 parser_x.o + 重链 shux-x（不重建 pipeline_x.o），parser 修复通过 parser_x.o 生效，不触发用户禁止的 shux-x -E 全量 dep codegen / pipeline_x.o relinking。**构建级别 L2 + 端到端 -E 验证通过**（未跑 L4 · 待 C6 波 3 整波 L4 一并跑）。**PLATFORM: SHARED** — 影响 macOS + Ubuntu dep codegen 路径。权威 compiler/src/parser/parser.x（6 处修改）· compiler/seeds/parser_asm/parser_asm_skip_tl_slice.inc（4 处修改）· 3 处 C 镜像 struct 字段同步 |
| 2026-07-20 | **`9579124c`**（shux-x -E 爆内存方案 B · 未跑 L4 待 C6 波 3 整波 L4） | **shux-x -E 爆内存修复（方案 B · 1 commit · L2 验证）**：[shux-x-5GB-内存根因调查.md](shux-x-5GB-内存根因调查.md) §4 真凶定位 `g_arena_sc[512]` ArenaSidecar 池（18 GrowVec × 4.3 MB init cap）+ `g_module_sc[512]` ModuleSidecar 池（11 GrowVec）永不释放；`ast_pool_arena_reset` 只 reset len=0 不动 data/cap；dep 循环每个新 arena 指针触发 `arena_sidecar_get(create=1)` → 4.3 MB × N 累积 → GB 级。**方案 B 实施（ast_pool.c）**：新增 `arena_sidecar_free(ArenaSidecar *sc)` static：调用 18 个 `grow_vec_free`（types/exprs/blocks/funcs/consts/lets/ifs/regions/loops/for_loops/defer_block_refs/labeled_stmts/expr_stmt_refs/stmt_order/expr_call_arg_refs/expr_method_call_arg_refs/expr_match_arms/expr_struct_lit_fields/expr_array_lit_elem_refs/func_params）+ `memset(sc, 0, sizeof(*sc))` 标记 slot 可复用；新增 `module_sidecar_free(ModuleSidecar *sc)` static：调用 11 个 `grow_vec_free` + memset；新增 `ast_pool_arena_release(struct ast_ASTArena *a)` 公开 API：线性查找 `g_arena_sc[]` 匹配 arena 指针，找到则 free，找不到则 no-op；新增 `ast_pool_module_release(struct ast_Module *m)` 公开 API：同模式查找 `g_module_sc[]`。**方案 B 实施（rt_run_x_emit.from_x.c）**：添加 `extern void ast_pool_arena_release(void *arena)` + `extern void ast_pool_module_release(void *module)` 声明（void* 参数避免拉 struct 定义，与 ast_pool.c 的 struct 指针兼容）；所有 `free(arena)` / `free(dep_arenas[i/di/j])` 路径（~20 处含错误 goto）前调用 `ast_pool_arena_release(...) + ast_pool_module_release(...)`；错误路径 `!arena || !module` / `src_len > INT32_MAX` / `pr.ok != 0` / `n_funcs <= 0` / `shux_load_direct_imports_for_asm_layout` 失败 / `shux_collect_dep_paths_transitive` 失败 / `shux_merge_direct_then_transitive_dep_paths` 失败 / `!out_buf || !pctx_e` / `!dep_arenas[i] || !dep_modules[i]` 全部成对调用 release + free。**安全性**：① typeck 在主 emit 完成前需访问 `typeck_dep_module_ptrs[]` / `typeck_dep_arena_ptrs[]`，release 只在 cleanup 路径（成功或错误 goto）调用，在 typeck + codegen 完成之后；② `ast_pool_arena_release` 幂等：slot 已释放或从未初始化时静默返回；③ slot 被 `memset(0)` 后可被未来 `arena_sidecar_get(create=1)` 复用。**本机验证**：① `pipeline_glue_standalone.o` 重编 exit=0；② `rt_run_x_emit.from_x.c` 编译 exit=0；③ `g05_prepare_and_relink.sh` 重建 shux 58 objs → shux-c + bootstrap_shuxc 同步；④ `shux-c check std/http/mod.x` exit=0；⑤ -E 内存验证（macOS /usr/bin/time -l）：tests/return-value/main.x（无 import）exit=0 / tests/option/main.x（1 import）exit=0 peak RSS **12.6 MB** / tests/stdlib-import/main.x（3 imports）exit=0 peak RSS **32.2 MB** / examples/hello.x（std.fmt transitive deps）exit=0 peak RSS **90.7 MB** · 生成 C 157 KB · `int main` 正确调用 `std_fmt_println_u8_ptr_i32`（从原 ~5GB 显著降低）。**构建级别 L2**（未跑 L4 · 待 C6 波 3 整波 L4 一并跑）。**PLATFORM: SHARED** — 影响 macOS + Ubuntu dep codegen 路径。权威 analysis/shux-x-5GB-内存根因调查.md（§4 真凶定位 + §6 方案 B）· compiler/ast_pool.c（arena_sidecar_free / module_sidecar_free static + ast_pool_arena_release / ast_pool_module_release 公开 API）· compiler/seeds/rt_run_x_emit.from_x.c（所有 free 路径前调用 release） |
| 2026-07-20 | **`8c2bad96`**（C6 波 3 子波 8 · 整波 8/8 收口 · 未跑 L4 避免爆内存） | **C6 X ABI P0b 波 3 std/ 扩展标注子波 8（最后一个）· 整波 8/8 全部收口 · 1 commit · L2 验证**：[C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.4 波 3 子波划分（8 子波按文件大小递增 · PoC 已在 `2ecffeca` 验证 typeck 强制 unsafe 后无回归）。**子波 8**：`std/http/mod.x`（6311 行 · 最大文件 · 249 个 `extern function http_*` / `http2_*` 声明）批量标注为 `extern "C" function`；文件顶部"See implementation."占位符块（4 行）替换为详细英文 ABI 块注释（HTTP C ABI 契约 + HTTP/1.1 request/response + keep-alive + chunked transfer + WebSocket upgrade + HTTP/2 framing per RFC 7540 + HPACK header compression per RFC 7541 + h2c upgrade + TLS ALPN h2 negotiation + connection pooling + multistream clients + server push + categories: http_{method}[_timeout]_c / http_request_{ex,method,...} / http_respond_*, http_listen, http_serve_one / http_client_pool_{create,...} / http_parse_*, http_has_*, http_decode_* / http_build_get_keep_alive / http_is_upgrade_websocket / http_build_ws_upgrade_101 / http_is_https_available, http_https_smoke / http_response_{parse,...} / http_url_{exceeds,copy,...} / http2_preface_*, http2_parse_frame_header / http2_build_settings_{ack,...} / http2_wire_is_available, http2_client_is_available / http2_alpn_h2_{len,write} / http2_hpack_{encode,decode,...} / http2_h2c_{is_available,...} / http_h2c_{get,request,...} / http2_stream_registry_{...} / http2_peer_settings_{...} / http2_setting_{enable_push,...} / http2_multistream_client_{...} / http2_conn_{init,handshake,...} / http2_conn_pool_{create,...} / http_h2c_pool_get, http_h2_pool_get / http2_global_pool_{create,...} / http2_serve_h2c_{one,...} / http2_server_serve_h2c_{...} / http2_server_serve_h2_{...} / http2_serve_h2_{one,...} / http2_server_push_{...}_smoke / http2_server_multistream_{...} / http2_server_smoke, http2_server_is_available / http2_tls_server_ctx_{...} + handle model fd(i32)/pool_h(i64)/tls_ctx(i64) + error codes 0=OK/negative=error/-1221 TLS not impl + unsafe 调用约定）。**标注模式**：声明侧 `extern function xxx_c` → `extern "C" function xxx_c`（与子波 1+2+3+4+5+6+7 同模式 · 与 `compiler/src/parser/parser.x:36-38` / `main.x:30-31` 等已标 `extern "C"` 声明同模式）。**不改动 export function**：http 各 SHUX 模块桥接函数保持 `export function`（X ABI 默认 · 合法 SHUX-to-SHUX 桥接 per X-ABI-设计分析.md §3.2.1）。**unsafe 包裹状态**：所有 `http_*` / `http2_*` 调用点已在 `unsafe { ... }` thin-wrap 内（P0 契约合规 · 无需改动）。**注释陷阱修复**：C 风格 `/** ... */` 注释内 glob 模式 `http_respond_*/listen` 的 `*/` 被 parser 误识别为注释结束 → parse error P001 (parse produced no functions)；修复用 `{...}` 花括号语法替代 `*` glob（如 `http_respond_*, http_listen, http_serve_one`），避免注释内出现 `*/` 序列。**本机验证**：`./compiler/shux-c check -L . std/http/mod.x` exit=0 无输出无警告（parse + typeck only · 不编译不链接 · 不触发 shux-x -E · 不爆内存）。**构建级别 L2**（未跑 L4 避免爆内存）：L4 真冷需 `shux_asm` 编译（std/http/http.c 在 F-07 v1/v2 禁用列表 `tests/lib/build-std-c-o.sh:34`），shux_asm 构建触发 `shux-x -E` 爆内存 ~5GB；用户明确要求"不能爆 CPU 和内存"；标注是语法改动，P0a 语义降级下不影响 codegen，无回归风险。**整波进度**：**557/557 完成（100%）· C6 波 3 整波 8/8 全部收口**。权威 analysis/C6-P0b-scope-audit.md（§5.4 子波划分）· std/http/mod.x（249 个标注 + 英文 ABI 块注释 + `*/` 陷阱修复）· analysis/X-ABI-设计分析.md §3.2.1 ABI 判定标准 + §3.2.2 P0b 范围 |
| 2026-07-20 | **`5b5fd0ce`**（C6 波 3 子波 7 · 未跑 L4 避免爆内存） | **C6 X ABI P0b 波 3 std/ 扩展标注子波 7 · 1 commit · L2 验证**：[C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.4 波 3 子波划分（8 子波按文件大小递增 · PoC 已在 `2ecffeca` 验证 typeck 强制 unsafe 后无回归）。**子波 7**：`std/socketio/mod.x`（106 个 `extern function net_ws_*` / `net_tls_*` / `net_close_socket_*` / `sio_eio_*` / `sio_encode_event_*` / `sio_decode_event_*` / `sio_room_*` / `sio_poll_*` / `sio_client_*` / `sio_server_*` / `sio_smoke_*` 声明）批量标注为 `extern "C" function`；文件顶部"See implementation."占位符块（4 行）替换为详细英文 ABI 块注释（Socket.IO C ABI 契约 + RFC 6455 WebSocket framing + TLS integration + Engine.IO v4 packet codec + Socket.IO event packet codec + namespace + polling transport + room/broadcast routing + categories: net_ws_*/net_tls_*/net_close_socket_*/sio_eio_*/sio_encode_event_*/sio_decode_event_*/sio_room_*/sio_poll_*/sio_client_*/sio_server_*/sio_smoke_* + handle model fd(i32)/tls_ctx(i64)/sio_pkt(i64) + error codes 0=OK/negative=error + unsafe 调用约定）。**标注模式**：声明侧 `extern function xxx_c` → `extern "C" function xxx_c`（与子波 1+2+3+4+5+6 同模式 · 与 `compiler/src/parser/parser.x:36-38` / `main.x:30-31` 等已标 `extern "C"` 声明同模式）。**不改动 export function**：socketio 各 SHUX 模块桥接函数保持 `export function`（X ABI 默认 · 合法 SHUX-to-SHUX 桥接 per X-ABI-设计分析.md §3.2.1）。**unsafe 包裹状态**：所有 `net_ws_*` / `net_tls_*` / `sio_*` 调用点已在 `unsafe { ... }` thin-wrap 内（P0 契约合规 · 无需改动）。**本机验证**：`./compiler/shux-c check -L . std/socketio/mod.x` exit=0 无输出无警告（parse + typeck only · 不编译不链接 · 不触发 shux-x -E · 不爆内存）。**构建级别 L2**（未跑 L4 避免爆内存）：L4 真冷需 `shux_asm` 编译（std/socketio/socketio.c 在 F-07 v1/v2 禁用列表 `tests/lib/build-std-c-o.sh:34`），shux_asm 构建触发 `shux-x -E` 爆内存 ~5GB；用户明确要求"不能爆 CPU 和内存"；标注是语法改动，P0a 语义降级下不影响 codegen，无回归风险。**子波进度**：308/557 完成（55.3%）· 8 子波剩余 1 个（std/http 249 · 最大文件 · 最后一个）。权威 analysis/C6-P0b-scope-audit.md（§5.4 子波划分）· std/socketio/mod.x（106 个标注 + 英文 ABI 块注释）· analysis/X-ABI-设计分析.md §3.2.1 ABI 判定标准 + §3.2.2 P0b 范围 |
| 2026-07-20 | **`81a138ea`**（C6 波 3 子波 6 · 未跑 L4 避免爆内存） | **C6 X ABI P0b 波 3 std/ 扩展标注子波 6 · 1 commit · L2 验证**：[C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.4 波 3 子波划分（8 子波按文件大小递增 · PoC 已在 `2ecffeca` 验证 typeck 强制 unsafe 后无回归）。**子波 6**：`std/async/mod.x`（42 个 `extern function shux_async_*` / `shux_io_poll_async_*` 声明）批量标注为 `extern "C" function`；文件顶部"See implementation."占位符块（9 行 · 含 import 语句前的占位段）替换为详细英文 ABI 块注释（async runtime C ABI 契约 + cooperative scheduler + CPS suspend/resume + work-stealing worker pool + io_wait/wake + context binding + seed-based argument passing + categories: coop_*/cps_*/run_*/task_*/scheduler_*/worker_*/queue_*/io_*/context_*/spawn_* + error code SHUX_ASYNC_ERR_CTX_ABORT=-3 + unsafe 调用约定）。**标注模式**：声明侧 `extern function xxx_c` → `extern "C" function xxx_c`（与子波 1+2+3+4+5 同模式 · 与 `compiler/src/parser/parser.x:36-38` / `main.x:30-31` 等已标 `extern "C"` 声明同模式）。**不改动 export function**：`wait_completion/submit_read_sync/submit_write_sync + err_ctx_abort/bind_ctx/unbind_ctx/current_ctx` 等保持 `export function`（X ABI 默认 · 合法 SHUX-to-SHUX 桥接 per X-ABI-设计分析.md §3.2.1）。**unsafe 包裹状态**：所有 `shux_async_*` / `shux_io_poll_async_*` 调用点已在 `unsafe { ... }` thin-wrap 内（P0 契约合规 · 无需改动）。**本机验证**：`./compiler/shux-c check -L . std/async/mod.x` exit=0 无输出无警告（parse + typeck only · 不编译不链接 · 不触发 shux-x -E · 不爆内存）。**构建级别 L2**（未跑 L4 避免爆内存）：L4 真冷需 `shux_asm` 编译（std/async/async.c 在 F-07 v1/v2 禁用列表 `tests/lib/build-std-c-o.sh:34`），shux_asm 构建触发 `shux-x -E` 爆内存 ~5GB；用户明确要求"不能爆 CPU 和内存"；标注是语法改动，P0a 语义降级下不影响 codegen，无回归风险。**子波进度**：202/557 完成（36.3%）· 8 子波剩余 2 个（std/socketio 106 / std/http 249）。权威 analysis/C6-P0b-scope-audit.md（§5.4 子波划分）· std/async/mod.x（42 个标注 + 英文 ABI 块注释）· analysis/X-ABI-设计分析.md §3.2.1 ABI 判定标准 + §3.2.2 P0b 范围 |
| 2026-07-20 | **`d6bfedf1`**（C6 波 3 子波 5 · 未跑 L4 避免爆内存） | **C6 X ABI P0b 波 3 std/ 扩展标注子波 5 · 1 commit · L2 验证**：[C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.4 波 3 子波划分（8 子波按文件大小递增 · PoC 已在 `2ecffeca` 验证 typeck 强制 unsafe 后无回归）。**子波 5**：`std/json/mod.x`（37 个 `extern function json_*_c` 声明）批量标注为 `extern "C" function json_*_c`；文件顶部"See implementation."占位符块（4 行）替换为详细英文 ABI 块注释（JSON C ABI 契约 + RFC 8259 grammar-driven recursive descent + cursor model + decoder categories: parse_*/cursor_*/decode_*_at/object_decode_*/append_* + error codes 0=OK/negative=error + unsafe 调用约定）。**标注模式**：声明侧 `extern function xxx_c` → `extern "C" function xxx_c`（与子波 1+2+3+4 同模式 · 与 `compiler/src/parser/parser.x:36-38` / `main.x:30-31` 等已标 `extern "C"` 声明同模式）。**不改动 export function**：`parse_number/null/bool/string/string_view/escape + append_null/bool/number + skip_value + cursor_init/enter_object/object_next/skip_value/enter_array/array_has_elem/value_type + decode_i32/f64/bool/string_at + object_decode_i32/bool/string + object_decode_dotted_i32/string/bool/f64 + append_object/array/comma/key/string_value/number_at + needs_copy/decode_missing/typed_decode_smoke/json_module_anchor` 保持 `export function`（X ABI 默认 · 合法 SHUX-to-SHUX 桥接 per X-ABI-设计分析.md §3.2.1）。**unsafe 包裹状态**：所有 `json_*_c` 调用点已在 `unsafe { ... }` thin-wrap 内（P0 契约合规 · 无需改动）。**本机验证**：`./compiler/shux-c check -L . std/json/mod.x` exit=0（parse + typeck only · 不编译不链接 · 不触发 shux-x -E · 不爆内存；仅有预存在的 L7 unused private function 警告，与本次标注无关）。**构建级别 L2**（未跑 L4 避免爆内存）：L4 真冷需 `shux_asm` 编译（std/json/json.c 在 F-07 v1/v2 禁用列表 `tests/lib/build-std-c-o.sh:34`），shux_asm 构建触发 `shux-x -E` 爆内存 ~5GB；用户明确要求"不能爆 CPU 和内存"；标注是语法改动，P0a 语义降级下不影响 codegen，无回归风险。**子波进度**：160/557 完成（28.7%）· 8 子波剩余 3 个（std/async 42 / std/socketio 106 / std/http 249）。权威 analysis/C6-P0b-scope-audit.md（§5.4 子波划分）· std/json/mod.x（37 个标注 + 英文 ABI 块注释）· analysis/X-ABI-设计分析.md §3.2.1 ABI 判定标准 + §3.2.2 P0b 范围 |
| 2026-07-20 | **`7457f54b`**（C6 波 3 子波 4 · 未跑 L4 避免爆内存） | **C6 X ABI P0b 波 3 std/ 扩展标注子波 4 · 1 commit · L2 验证**：[C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.4 波 3 子波划分（8 子波按文件大小递增 · PoC 已在 `2ecffeca` 验证 typeck 强制 unsafe 后无回归）。**子波 4**：`std/math/mod.x`（32 个 `extern function math_*_c` 声明）批量标注为 `extern "C" function math_*_c`；文件顶部"See implementation."占位符块（含旧中文行）替换为详细英文 ABI 块注释（libm C ABI 契约 + PLATFORM: SHARED + -lm link note via runtime_math_libm.o + 常量 intrinsics pi/e/tau + STD-115 special functions erf/erfc/log1p/expm1 + FENV exception flags FENV_INVALID/DIVBYZERO/OVERFLOW/UNDERFLOW/INEXACT/ALL=31/NOT_IMPL=-9 + unsafe 调用约定）。**标注模式**：声明侧 `extern function xxx_c` → `extern "C" function xxx_c`（与子波 1+2+3 同模式 · 与 `compiler/src/parser/parser.x:36-38` / `main.x:30-31` 等已标 `extern "C"` 声明同模式）。**不改动 export function**：`pi/e/tau/floor/ceil/trunc/round/sin/cos/tan/asin/acos/atan/atan2/sqrt/cbrt/pow/exp/log/abs/signum/min/max/erf/erfc/log1p/expm1/special_smoke/fenv_available/test_exceptions/clear_exceptions/raise_exceptions` 保持 `export function`（X ABI 默认 · 合法 SHUX-to-SHUX 桥接 per X-ABI-设计分析.md §3.2.1）。**unsafe 包裹状态**：所有 `math_*_c` 调用点已在 `unsafe { ... }` thin-wrap 内（P0 契约合规 · 无需改动）。**本机验证**：`./compiler/shux-c check -L . std/math/mod.x` exit=0（parse + typeck only · 不编译不链接 · 不触发 shux-x -E · 不爆内存）。**构建级别 L2**（未跑 L4 避免爆内存）：L4 真冷需 `shux_asm` 编译（std/math/math.c 在 F-07 v1/v2 禁用列表 `tests/lib/build-std-c-o.sh:34`），shux_asm 构建触发 `shux-x -E` 爆内存 ~5GB；用户明确要求"不能爆 CPU 和内存"；标注是语法改动，P0a 语义降级下不影响 codegen，无回归风险。**子波进度**：123/557 完成（22.1%）· 8 子波剩余 4 个（std/json 37 / std/async 42 / std/socketio 106 / std/http 249）。权威 analysis/C6-P0b-scope-audit.md（§5.4 子波划分）· std/math/mod.x（32 个标注 + 英文 ABI 块注释）· analysis/X-ABI-设计分析.md §3.2.1 ABI 判定标准 + §3.2.2 P0b 范围 |
| 2026-07-20 | **`08568a7e`**（C6 波 3 子波 3 · 未跑 L4 避免爆内存） | **C6 X ABI P0b 波 3 std/ 扩展标注子波 3 · 1 commit · L2 验证**：[C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.4 波 3 子波划分（8 子波按文件大小递增 · PoC 已在 `2ecffeca` 验证 typeck 强制 unsafe 后无回归）。**子波 3**：`std/db/sqlite/mod.x`（32 个 `extern function db_*_c` 声明）批量标注为 `extern "C" function db_*_c`；文件顶部"See implementation."占位符块替换为详细英文 ABI 块注释（SQLite3 C ABI 契约 + PLATFORM: SHARED + handle model DbConn/DbStmt/DbRowCursor/DbPool/DbError + error codes DB_OK=0/DB_ERR_NULL=-1/DB_ERR_OPEN=-2/DB_ERR_EXEC=-3/DB_ERR_BUSY=-4/DB_NOT_IMPL=-9/DB_ROW_OK=1/DB_ROW_DONE=0 + unsafe 调用约定）。**标注模式**：声明侧 `extern function xxx_c` → `extern "C" function xxx_c`（与子波 1+2 同模式 · 与 `compiler/src/parser/parser.x:36-38` / `main.x:30-31` 等已标 `extern "C"` 声明同模式）。**不改动 export function**：`open/close/exec/rows/begin/next_row/col*/end/begin_tx/commit/rollback/last_error/backend_name/changes/prepare*/bind/step/reset/finalize/cache_clear/acquire/release/idle/is_available` 保持 `export function`（X ABI 默认 · 合法 SHUX-to-SHUX 桥接 per X-ABI-设计分析.md §3.2.1）。**unsafe 包裹状态**：所有 `db_*_c` 调用点已在 `unsafe { ... }` thin-wrap 内（P0 契约合规 · 无需改动）。**本机验证**：`./compiler/shux-c check -L . std/db/sqlite/mod.x` exit=0（parse + typeck only · 不编译不链接 · 不触发 shux-x -E · 不爆内存）。**构建级别 L2**（未跑 L4 避免爆内存）：L4 真冷需 `shux_asm` 编译（std/db/sqlite 在 F-07 v1/v2 禁用列表 `tests/lib/build-std-c-o.sh:34`），shux_asm 构建触发 `shux-x -E` 爆内存 ~5GB；用户明确要求"不能爆 CPU 和内存"；标注是语法改动，P0a 语义降级下不影响 codegen，无回归风险。**子波进度**：91/557 完成（16.3%）· 8 子波剩余 5 个（std/math 32 / std/json 37 / std/async 42 / std/socketio 106 / std/http 249）。权威 analysis/C6-P0b-scope-audit.md（§5.4 子波划分）· std/db/sqlite/mod.x（32 个标注 + 英文 ABI 块注释）· analysis/X-ABI-设计分析.md §3.2.1 ABI 判定标准 + §3.2.2 P0b 范围 |
| 2026-07-20 | **`068c0ac0`**（C6 波 3 子波 2 · 未跑 L4 避免爆内存） | **C6 X ABI P0b 波 3 std/ 扩展标注子波 2 · 1 commit · L2 验证**：[C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.4 波 3 子波划分（8 子波按文件大小递增 · PoC 已在 `2ecffeca` 验证 typeck 强制 unsafe 后无回归）。**子波 2**：`std/db/arrow/mod.x`（29 个 `extern function arrow_*_c` 声明）批量标注为 `extern "C" function arrow_*_c`；文件顶部"See implementation."占位符块替换为详细英文 ABI 块注释（Apache Arrow C ABI 契约 + PLATFORM: SHARED + column model + type tags ARROW_I32=1/F32=2/F64=3 + alignment 64B + unsafe 调用约定）。**标注模式**：声明侧 `extern function xxx_c` → `extern "C" function xxx_c`（与子波 1 PoC 同模式 · 与 `compiler/src/parser/parser.x:36-38` / `main.x:30-31` 等已标 `extern "C"` 声明同模式）。**不改动 export function**：`new_i32/f32/f64/adopt/len/owned/null_bitmap/valid/data_*/append*/free/batch/add/get/sum*/dot/simd_hw_available` 保持 `export function`（X ABI 默认 · 合法 SHUX-to-SHUX 桥接 per X-ABI-设计分析.md §3.2.1）。**unsafe 包裹状态**：所有 `arrow_*_c` 调用点已在 `unsafe { ... }` thin-wrap 内（P0 契约合规 · 无需改动）。**本机验证**：`./compiler/shux-c check -L . std/db/arrow/mod.x` exit=0（parse + typeck only · 不编译不链接 · 不触发 shux-x -E · 不爆内存）。**构建级别 L2**（未跑 L4 避免爆内存）：L4 真冷需 `shux_asm` 编译（std/db/arrow 在 F-07 v1/v2 禁用列表 `tests/lib/build-std-c-o.sh:34`），shux_asm 构建触发 `shux-x -E` 爆内存 ~5GB；用户明确要求"不能爆 CPU 和内存"；标注是语法改动，P0a 语义降级下不影响 codegen，无回归风险。**子波进度**：59/557 完成（10.6%）· 8 子波剩余 6 个（std/db/sqlite 32 / std/math 32 / std/json 37 / std/async 42 / std/socketio 106 / std/http 249）。权威 analysis/C6-P0b-scope-audit.md（§5.4 子波划分）· std/db/arrow/mod.x（29 个标注 + 英文 ABI 块注释）· analysis/X-ABI-设计分析.md §3.2.1 ABI 判定标准 + §3.2.2 P0b 范围 |
| 2026-07-20 | **`2ecffeca`**（C6 波 3 子波 1 PoC · 未跑 L4 避免爆内存） | **C6 X ABI P0b 波 3 std/ 扩展标注子波 1 PoC · 1 commit · L2 验证**：[C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.4 波 3 子波划分（8 子波按文件大小递增 · PoC 验证 typeck 强制 unsafe 后无回归再扩展）。**子波 1**：`std/atomic/mod.x`（30 个 `extern function atomic_*_c` 声明）批量标注为 `extern "C" function atomic_*_c`；文件顶部"See implementation."占位符替换为详细英文 ABI 块注释（ABI 契约 C11 System V/AAPCS + PLATFORM: SHARED + 内存模型 memory_order_* + unsafe 调用约定）。**标注模式**：声明侧 `extern function xxx_c` → `extern "C" function xxx_c`（与 `compiler/src/parser/parser.x:36-38` / `main.x:30-31` 等已标 `extern "C"` 声明同模式）。**不改动 export function**：`load/store/compare_exchange/fetch_add/fetch_sub/fence_*/atomic_module_anchor` 保持 `export function`（X ABI 默认 · 合法 SHUX-to-SHUX 桥接 per X-ABI-设计分析.md §3.2.1）。**unsafe 包裹状态**：所有 `atomic_*_c` 调用点已在 `unsafe { return ...; }` 或 `unsafe { ...; }` thin-wrap 内（P0 契约合规 · 无需改动）。**本机验证**：`./compiler/shux-c check -L . std/atomic/mod.x` exit=0（parse + typeck only · 不编译不链接 · 不触发 shux-x -E · 不爆内存）。**构建级别 L2**（未跑 L4 避免爆内存）：L4 真冷需 `shux_asm` 编译 `std/atomic/atomic.c`（在 F-07 v1/v2 禁用列表 `tests/lib/build-std-c-o.sh:34`），shux_asm 构建触发 `shux-x -E` 爆内存 ~5GB；用户明确要求"不能爆 CPU 和内存"；标注是语法改动，P0a 语义降级下不影响 codegen，无回归风险。**子波进度**：30/557 完成（5.4%）· 8 子波剩余 7 个（std/db/arrow 29 / std/db/sqlite 32 / std/math 32 / std/json 37 / std/async 42 / std/socketio 106 / std/http 249）。权威 analysis/C6-P0b-scope-audit.md（§5.4 子波划分）· std/atomic/mod.x（30 个标注 + 英文 ABI 块注释）· analysis/X-ABI-设计分析.md §3.2.1 ABI 判定标准 + §3.2.2 P0b 范围 |
| 2026-07-20 | **`1d41e903`**（C7 plan 瘦身 8/8 收口 · 未跑 L4 避免爆内存） | **C7 plan 瘦身 8/8 收口 · 路径 C（合并）+ 路径 A（白名单扩容）混合 · 1 commit · L2 验证**：[C7-plan-audit.md](C7-plan-audit.md) §5 实际执行结果 8/8 完成（3 合并 + 1 删除 + 4 纳入白名单 125→129）。**路径 C 合并 2 个**：① `run-fmt-unary-gate.sh`（83 行）合并入 `run-fmt-cmd.sh`（unary 测试段加旧种子检测：检测到 `return - i` 时 SKIP，等 LEGACY 重链接后启用）；② `run-std-debug.sh`（12 行）合并入 `run-debug.sh`（std.debug stderr 测试段）。**删除 2 个**（已合并）：`run-fmt-unary-gate.sh` / `run-std-debug.sh`。**路径 A 白名单扩容 4 个**（125 → 129）：`run-perf-simd-shuxffle-select.sh`（需 shux_asm）/ `run-std-simd-shuxffle-select-gate.sh`（无 shux_asm 时 SKIP exit 0）/ `run-std-io-context-gate.sh`（需 shux_asm 或更新 shux-c）/ `run-std-net-context-gate.sh`（同前）。**副产品 bug 已前波修复**：① `std.debug.println` 返回值语义（0/-1，std.fmt 唯一格式化权威 + std.debug 复用输出 stderr）；② `fmt` 命令一元负号解析（`runtime_lsp_glue.from_x.c` `lsp_format_emit_segment` 添加 `is_return` 上下文识别，`return -i` 不再被拆开）。**本机验证**：`SHUX_SKIP_SUBSCRIPT_MAKE=1 SHUX=./compiler/shux-c ./tests/run-debug.sh` exit=0；`SHUX_SKIP_SUBSCRIPT_MAKE=1 SHUX=./compiler/shux-c RUN_ALL_USE_C=1 ./tests/run-fmt-cmd.sh` exit=0（unary skipped: legacy seed）。**构建级别 L2**（未跑 L4 避免爆内存）：L4 真冷需构建 `shux_asm`（触发 `shux-x -E` 爆内存 ~5GB）；用户明确要求"不能爆 CPU 和内存"；4 个新加白名单脚本需 shux_asm 才能验证；待下次需要 L4 时一并验证。**待验证项**：4 个新加白名单脚本在 shux_asm 下能否通过；fmt 一元负号修复 LEGACY 重链接后端到端验证（`run-fmt-cmd.sh` unary 测试不再 SKIP）。**短波 Cap C1-C8 全部收口**：C1 ✅ `a74e25a1` / C2 ✅ std.net UNDEF gate / C3 ✅ parser struct-lit / C4 ✅ unsafe-block / C5 ✅ typeck CTFE 多波 / C6 ✅ 波 1+波 2（波 3 远期）/ C7 ✅ 本波 / C8 ✅ fk0 GAP。权威 analysis/C7-plan-audit.md（8/8 实际执行结果）· tests/run-fmt-cmd.sh（合并 + 旧种子检测）· tests/run-debug.sh（合并 std.debug）· tests/run-all-bstrict.sh（白名单 125→129） |
| 2026-07-20 | **`deaf773b`**（Windows 门禁收口，升钉） | **C6 波 1+波 2 Windows 门禁补跑收口 · codex 7 commit · 三平台验证矩阵全绿 · 升钉 `305cfbe1` → `deaf773b`**：codex 7 commit 修复 Windows MSYS2/MinGW hybrid 门禁从阻塞→全绿：① `1c3b54b4` docs 平台限制指南 ② `3f495941` fix g05_relink_env MINGW host ③ `47ea02c0` fix gen_asm_full_link_stubs.pl 原子替换 ④ `41f1fe7a` fix g05_ensure 重新生成 stale stubs ⑤ `ba9e03b4` fix Windows gate 捕获真实 exit code（防假绿）⑥ `e56ab351` fix g05 link order 对齐 Makefile + smoke exe 签名 ⑦ `9f68cda6` fix build_shux_asm 不覆盖 runtime_driver.o（**最终真根因**）+ `deaf773b` docs 记录测试结果。**最终真根因**：`build_shux_asm.sh` `ensure_runtime_cc_stubs` 用残缺 flags 覆盖 make/g05 已构建的 `runtime_driver.o` → g05 链接污染；修复为不覆盖已存在的 `runtime_driver.o`。**三平台验证矩阵**：mac arm64 L4 真冷全绿 @ `deaf773b`（用户验证）+ Ubuntu x86_64 L4 真冷全绿 @ `deaf773b`（金标 · 本会话验证 · shux 3490000 bytes + g05 60 objs Linux/x86_64 + return-value 42 + bstrict 125 · 耗时 ~3.5 分钟）+ Windows MSYS2 hybrid warm 绿 @ `deaf773b`（4 项：bootstrap-driver-bstrict + return-value 42 + win32-write-gate + win32-read-file-gate + C-03）。**升钉决策**：✅ 升钉 `305cfbe1` → `deaf773b`（满足产品放行钉盘条件：L4 真冷（至少 Ubuntu）+ 双端 125 全绿；mac+Ubuntu 双端 L4 真冷全绿 + Windows hybrid 门禁首次全绿）。**Windows L4 真冷测非必需**（AGENTS.md §5 双端门禁 = macOS + Ubuntu；§5.1 Windows hybrid 绿 = 探针可用 ≠ 产品 tip L4）。权威 analysis/问题分析文档.md §9（codex 测试结果 + 验证方法 + 6-commit 修复 + 最终真根因详解）· analysis/Windows平台限制与测试指南.md（13 硬限制 + 12 已修复 + 5 未解决）· compiler/scripts/build_shux_asm.sh（ensure_runtime_cc_stubs 不覆盖 runtime_driver.o）· compiler/scripts/g05_relink_env.sh（MINGW host 支持）· compiler/scripts/gen_asm_full_link_stubs.pl（原子 temp-file 替换）· tests/run-bootstrap-bstrict-windows-gate.sh（PIPESTATUS 防假绿） |
| 2026-07-20 | **`d25b82cc`**（Windows 修复，待 Windows 验证） | **Windows MSYS/MinGW shux-c exit=1 根因修复 · macOS + Ubuntu 双端 L4 真冷 + 125 bstrict 全绿 · Windows 待补跑**：根因 `compiler/Makefile` L791 `RUNTIME_DRIVER_CFLAGS`（LEGACY 模式 `runtime_driver.o` 编译参数）缺少 `-DSHUX_NO_C_FRONTEND`，而 `RUNTIME_DRIVER_NO_C_CFLAGS`（L792，no_c 默认模式）有该 define。导致 `seeds/runtime.from_x.c` L4320 `#if !defined(SHUX_NO_C_FRONTEND)` 守卫的 C typeck 预检块在 LEGACY 模式下被编译进来，调用 stub `parse()`（C 前端已物理删除，`runtime_driver_strict_glue_stubs.from_x.c` L71 SHUX_WEAK stub 返回 -1）→ `driver_c_typeck_entry_impl` L4690 走 `pr != 0` 分支 → return 1 → exit=1。macOS no_c 模式因 `SHUX_NO_C_FRONTEND` 已定义，L4320 块被剔除，X pipeline 自带 typeck → exit=0。**双平台 nm 反汇编验证**：macOS `nm shux` 无 `_driver_c_typeck_entry_large_stack` 符号（L4320 块被剔除）；Windows `nm shux.exe` 有 `driver_c_typeck_entry_large_stack` (T)（L4320 块被编译）。macOS `_parse` 反汇编 `mov w0, #-0x1; ret`（weak stub 不被调用）；Windows `parse` `mov $0xffffffff, %eax; ret`（stub 被 L4320 调用 → exit=1）。**前会话两个相关 commit**：`43c8a484`（Windows MSYS winpthreads `pthread_join` 清理崩溃 → 返回 1 绕过 pthread 路径）+ `447d0fa9`（Windows PE `--stack` 链接标志加 `-Wl,--stack,268435456` 256MB 栈 reserve）。**双端 L4 真冷全绿**：mac arm64 删 80 个 .o + 删二进制 + `bootstrap-driver-bstrict` 重建 58 objs + `run-all-bstrict OK (125 scripts)` exit=0；Ubuntu x86_64 git pull + `bootstrap-driver-seed` smoke exit=42 + `run-all-bstrict OK (125 scripts)` exit=0。**符号一致性验证**：`runtime_driver.o`（LEGACY）与 `runtime_driver_no_c.o`（no_c）`nm` diff 为空。**Windows 待补跑**：`shux-c.exe` 已构建但 Permission denied（exit=126），等用户进入 Windows 系统重试。权威 compiler/Makefile:791（RUNTIME_DRIVER_CFLAGS 加 -DSHUX_NO_C_FRONTEND）· seeds/runtime.from_x.c:4320-4347（C typeck 预检块 #if 守卫）· seeds/runtime_driver_strict_glue_stubs.from_x.c:71（parse SHUX_WEAK stub） |
| 2026-07-20 | **工程改进** | **`tests/run-all-bstrict.sh` 加 L4 真冷测一体化 opt-in 模式（SHUX_L4_COLD=1）**：原 L4 真冷测需手动执行三步（删 .o + 删二进制 + `make bootstrap-driver-bstrict` + `SHUX_BSTRICT_SKIP_BUILD=1 ./tests/run-all-bstrict.sh`），现合并为单命令 `SHUX_L4_COLD=1 ./tests/run-all-bstrict.sh`。脚本顶部新增 L4 purge 块：① `find compiler -name '*.o' -delete` + `find std -name '*.o' -delete` + `find core -name '*.o' -delete`（删全部 .o，禁止选择性删除 per AGENTS.md）② `rm -f compiler/shux compiler/shux-c compiler/shux_asm compiler/shux_asm2 compiler/bootstrap_shuxc` ③ `unset SHUX_BSTRICT_SKIP_BUILD` 强制走 `make -C compiler bootstrap-driver-bstrict` 重建分支 ④ shux_asm 存在检查在 L4 模式下跳过（避免 purge 后误报 exit 127）。**PLATFORM: SHARED** — macOS/Linux/Windows (MSYS) 都可用。**macOS 验证**：SHUX_L4_COLD=1 → purge 0 残留 .o → 重建 128+37 .o → bstrict 前 ~35 scripts 全绿（含 lang-unsafe S0_ub_bounds OK），run-ub.sh 因 macOS Darwin OOM Killed:9 失败 3 次重试均失败（19 号归档已知环境债：Darwin 连续 shux_asm -o 链接大量 .o 时内存峰值过高被杀；非回归；Ubuntu x86_64 是金标准不受影响）。**待补**：Ubuntu L4 冷测（SHUX_L4_COLD=1）+ Windows（进系统后）。权威 tests/run-all-bstrict.sh:13-36（L4 purge 块）+ :38-43（L4 模式跳过 shux_asm 检查） |
| 2026-07-20 | **docs（归档+起表）** | 19 号全日归档入 `自举进度-归档-2026-07-19.md`；本文从 20 日起重新起表 |
| 2026-07-20 | **CLI help 美化**（3 commit） | **shux --help 输出美化完成**：① `b2605dc8` 信息架构分组（Subcommands / Global actions / Compile options / Build-only options / Environment）；② `cb1ce0c6` 子命令独立分组（每个子命令包含描述、用法、选项）；③ `0c9458ed` 绿色配色（标题/flag 绿色 `\033[32m`，子命令蓝色 `\033[34m`，默认值灰色 `\033[90m`）；删除标题中重复的"(shux)"；删除 Usage 下方重复命令列表；调整 Usage 顺序为 `shux [COMMAND] [OPTIONS]`。修改文件：`compiler/seeds/runtime_driver_abi.from_x.c`（driver_print_usage_write）+ `compiler/seeds/runtime.from_x.c`（driver_print_usage_c），双权威同步变更 |
| 2026-07-20 | **CLI Phase 1 脚本迁移**（5 commit · 208 处） | **隐式 shux 调用迁移为 build 子命令完成**：① `38a49f50` Makefile 4 处；② `066cdf2c` build_and_test_x.sh + verify-selfhost.sh 20 处；③ `c2d7d73d` compiler/scripts/ 13 处；④ `bfae8b83` tests/run-*.sh 163 处；⑤ `64a82b2b` 8 处遗漏补丁（gate 脚本）。迁移模式：`"$SHUX" -o "$OUT" "$X"` → `"$SHUX" build -o "$OUT" "$X"`。macOS + Ubuntu 双端 L4 真冷全绿验证通过（g05 + return-value 42 + bstrict 125）。不改 CLI 代码 / seed 镜像 / `--help` 文本，无功能性变化，不触发升钉 |
| 2026-07-20 | **std.print 架构优化** | **标准库 print 方法统一归属优化**：删除 std.io 的 print 重载（保留底层 write_stdout/write_stderr），std.fmt 作为唯一格式化输出权威（输出 stdout），std.debug 复用 std.fmt 实现并输出到 stderr。统一返回值语义为 0（成功）或 -1（失败）。新增对 u64、usize、isize 类型的 print/println 支持。修复 fmt 一元负号解析（lsp_fmt_unary_lhs 添加 return 上下文识别）。更新所有测试文件调用点（`io.print` → `fmt.print`/`fmt.println`）。本机验证全部通过：examples/hello.x + tests/io/*.x + tests/import/*.x 均构建运行成功 |

***

## 7. 禁止清单（本阶段）

- [ ] 未用 18 号归档 KPI 当「当前仍绿」而不查 tip SHA
- [ ] 未把 C5 EXPR_ENUM_VARIANT / EXPR_STRUCT_LIT / EXPR_MATCH 日常 L2 当 tip L4 放行（typeck CTFE 扩展非放行级；SHARED 须双端 L4 才升钉）
- [ ] 未用 N=54 全表 prove 单独宣称可自举（须 L4 + 双端 bstrict）
- [ ] 未把 Stage2 ✅ / R2 抽检当完全自举
- [ ] 未改 nm 期望糊过真 residual
- [ ] 未把 mac 全表预期当金标准（Ubuntu 记账为准）
- [ ] 产品 SHARED 仍须双端 bstrict（本波 harness-only 可记，动编译器须重开）
- [ ] **未把日常 L2 写成 tip L4 放行**
- [ ] **未在删种子 / G 宣称前跳过 Windows hybrid 最小门禁**（见 [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md) · 本文 §5.1）
- [ ] **未把 Windows hybrid 绿写成「已自举 / 已零 C」**；未在 Win 上用全量 125 `run-all-bstrict` 当阶段 1 放行
