# Xlang / X language 自举进度

> **更新**：2026-07-29 · **wave652 Cap x86 high-end STRUCT_LIT 固定数组字段 frame mag**（tip **`be48b6acf`**）· 钉盘 **`01479a857`** · 下一 Cap residual / L4
> **基线 SHA（产品 L4 放行钉盘）**：**`01479a857`**（升自 `213bd306b`；双端 L4 129 全绿）\
> **产品 bstrict 套件数**：**129**\
> **产品代码 tip**：**wave652 Cap**（STRUCT_LIT fixed-array field frame mag；**未** L4）\
> **git 锚点（重命名前）**：tag **`anchor-pre-xlang-rename`** · **git 锚点（Cap 轨前）**：tag **`anchor-pre-wave576`** / **`anchor-pre-wave577`**\
> **现行工程**：品牌/CLI/env/ABI 已 **xlang**（主域 **`xlang.dev`** · 仓库 **`github.com/shuliangfu/x-lang`**）。\
> **本波（29 日）状态**：**wave652 Cap 闭**（pad+`i32[2]` 字段 Ubuntu fs 绿；钉盘仍 `01479a857`）。日切：28 全日已归楼。\
> **方法**：[自举方法.md](自举方法.md)（Cap/R/L/M；工程 ≠ 产品放行）\
> **Windows（删种子前）**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md) · [25](自举进度-归档-2026-07-25.md) · [24](自举进度-归档-2026-07-24.md) · [23](自举进度-归档-2026-07-23.md) · [22](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md) · [19](自举进度-归档-2026-07-19.md) · [18](自举进度-归档-2026-07-18.md) · [17](自举进度-归档-2026-07-17.md) · [16](自举进度-归档-2026-07-16.md) · [14](自举进度-归档-2026-07-14.md) · [13](自举进度-归档-2026-07-13.md)

### 维护约定

1. 每完成一波 **必改两文**：本文（KPI/前排/§6）+ [`当前进度.md`](当前进度.md)。
2. **禁止**把 prove/Stage2/N++ 单独写成「可自举」。
3. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**；历史钉盘 125）。
4. 工程 KPI 写 **SHA + 日志路径**；过期整日流水移归档。
5. 大债地图默认写 [`当前问题分析.md`](当前问题分析.md)（承接原 `问题分析文档.md`）；非每微步。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 =** **`01479a857`**（升自 `213bd306b`；**双端 L4 129 全绿**；28 日 wave576）。\
**产品代码 tip =** **`be48b6acf`**（STRUCT_LIT fixed-array field frame mag；双端 L2；**未** L4 / **未**升钉）。\
**git 锚点 =** tag **`anchor-pre-xlang-rename`** · **`anchor-pre-wave576`** · **`anchor-pre-wave577`**。\
**29 日进度 =** Cap residual **wave618–652**（host-C slice / pure-asm float·ARRAY·INDEX·ptr / if-cond / INDEX.field / pad 多字段 layout）；**日切已归楼 28 全日**。

| 指标 | 状态 |
|------|------|
| 工程轨 | **wave576 已闭**（L4 双端 129 · 钉盘 `01479a857`）；prove **111/111 IDENTICAL** |
| 产品轨 tip / 钉盘 | tip = **`be48b6acf`**（wave652 · **未** L4）· 钉盘 **`01479a857`** |
| mac L4 真冷（钉盘） | ✅ **`01479a857`** remaining_o=0 · **OK (129)** · 10分17秒 · `/tmp/mac_true_bstrict_01479a857.log` |
| Ubuntu L4（金标 · 钉盘） | ✅ **`01479a857`** remaining_o=0 · **OK (129)** · 3分22秒 · `/tmp/ubuntu_true_bstrict_01479a857.log` |
| 29 日构建级别 | **日常 L2**（wave618–652；**非** tip L4；**未**升钉） |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](自举方法.md) | Cap / R / L / M |
| [自举步骤.md](自举步骤.md) | 可执行闸门 |
| [自举验证.md](自举验证.md) | 怎么测 |
| **本文** | 长期 KPI + 前排（**每波必改**） |
| [当前进度.md](当前进度.md) | 当天快照（**每波必改**） |
| **[当前问题分析.md](当前问题分析.md)** | tip 级产品债地图 |
| [自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) | **28 日全日 wave534–617**（只读） |
| [自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) | **27 日全日 wave491–533**（只读） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单 |
| 产品 L4 放行钉盘 | **`01479a857`** | 28 日 wave576 L4 升钉（前钉 `213bd306b`）；双端 L4 129 |
| 本波 tip（29 日） | **wave652** Cap STRUCT_LIT fixed-array field frame mag | tip **`be48b6acf`** · **日常 L2 非 tip L4** · 钉盘仍 **`01479a857`** |
| mac 产品链 bstrict（钉盘 L4） | ✅ **OK (129)** | L4 · `/tmp/mac_true_bstrict_01479a857.log` · 10分17秒（wave576） |
| Ubuntu L4（金标 · 钉盘） | ✅ **OK (129)** | L4 · `/tmp/ubuntu_true_bstrict_01479a857.log` · 3分22秒（wave576） |
| 产品矩阵（29 日 tip L2） | ✅ rv42 / option / hello（wave652 邻域） | mac + Ubuntu L2 · **非** tip L4 |
| 前钉 mac/Ubuntu L4 | ✅ 129 @ `01479a857` · `213bd306b` · `f1250f415` · … | 钉盘日志 `/tmp/*_01479a857.log` |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 18 号结案 |
| **N** | 111/111 IDENTICAL | 28 日 wave539–575 扩至 111；本波不变 |
| **R2 / Cap 本波** | **wave652 已闭**（x86 high-end STRUCT_LIT 固定数组字段 frame mag）；wave618–651 Cap residual 链 | tip `be48b6acf` · 钉盘 **`01479a857`** · **日常 L2** |

***

## 3. 当前前排（≤3 条）

1. **钉盘 `01479a857`**（wave576 双端 L4 129）仍为产品放行基线；**未**因 wave618–652 升钉。
2. **Cap residual 下一硬叶**（break/continue fs 假跑；skip untyped `self` / wave493 非标准）；或用户要求时整波 **双端 L4 + 升钉**。
3. **29 日已闭 wave618–652**：host-C slice · pure-asm float/ARRAY/INDEX/ptr · FIELD as slice · if-cond · pad 多字段 high-end layout。

***

## 4. 与归档的关系

- **28 日归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) 包含 wave534–617（lsp R2 L4 升钉链 · prove 111 · wave576 钉盘 `01479a857` · Cap freestanding 至 f32 ARRAY_LIT）。
- **27 日归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) 包含 wave491–533。
- **29 日新内容**：本文记录 wave618–650（日切后起表；wave618 产品 commit 在 29 日 00:03）。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行钉盘 `01479a857`**；tip 产品 **`be48b6acf`**（未 L4）。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
|------|----------|
| **macOS arm64** | ✅ 钉盘 L4 @ **`01479a857`**；29 日 tip **L2** g05 + 探针 @ **`be48b6acf`** |
| **Ubuntu x86_64（金标）** | ✅ 钉盘 L4 @ **`01479a857`**；29 日 tip **L2** g05 + 探针 @ **`be48b6acf`** |
| **Windows MSYS2** | 继承 hybrid；本波未重跑 |

***

## 6. 变更记录（倒序 · 29 日）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-07-29 | **wave652** | **R2 Cap residual pure wave652**：**x86 high-end STRUCT_LIT 固定 TYPE_ARRAY 字段 frame mag**。根因：`glue_struct_lit_store_fixed_array_field` 对 dest 恒 `base_off+foff`；x86 high-end lea -base(%rbp)=Outer byte0，字段@byte0+foff 应为 mag **base-foff**（≡ wave595 nest_slot）。`base+foff` 把数组写在 Outer 之前 → xs[1]@+esz 覆盖 pad（Ubuntu fs `w.pad`→32、`take(w.xs)`=0；host-C 绿；arm64 low-end +foff 本绿；`xs` 在首字段 foff=0 绿）。权威：G.7 `glue_struct_field_frame_mag_c` — arm64 base+foff / x86 base-foff；ARRAY_LIT/zero/CALL/VAR dest + FIELD src + nest_slot 复用；sret 真地址 +foff 不变。探针：mac/Ubuntu pad/pad_var/lit/read/dump/i64/ab/nopad/fsrc/mdim host+fs+asm=42/7 · 矩阵 · Ubuntu disasm `lea -0x1c`（was `-0x24`）。soft：untyped self；break/continue fs 假跑；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave652.log` · `/tmp/ubuntu_g05_wave652.log` · `/tmp/ubuntu_w652_probes.log`）。tip **`be48b6acf`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave651** | **R2 Cap residual pure wave651**：**freestanding FIELD over INDEX as T[N]/[]T call-arg**。根因：`glue_try_index_var_or_field_base_to_rax` 仅 VAR / DEREF / STRUCT_LIT·CALL（wave609/639/610）→ `m[i].xs` 回落 field rvalue load 首元当 E* → freestanding SEGV（host-C &field 绿；`take(w.xs)`/`take(Wrap{}.xs)` 已绿；disasm `ldr w` vs `lea`）。权威：G.7 补全同一 helper — FIELD 基 ko=47 走 `glue_emit_index_eff_addr_scaled` + field_off（rax+rbx 孪）；wave610/649 call-arg 入口复用。探针：mac/Ubuntu mdim0/1/4/6/8/w610 host+fs+asm=42 · pad 多字段 Ubuntu soft · 矩阵 rv/option/hello · disasm lea→rdi/x0。soft：untyped self；pad 多字段 STRUCT_LIT 布局（Ubuntu high-end）；`break`/`continue` fs 假跑；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave651.log` · `/tmp/ubuntu_g05_wave651.log` · `/tmp/ubuntu_w651_probes.log`）。tip **`a9256927e`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX gold · MACOS\|ARM64 co-path · **日常 L2 非 tip L4** |
| 2026-07-29 | docs | **日切归档**：28 号全日仪表盘 + §6 流水整表迁入 [`自举进度-归档-2026-07-28.md`](自举进度-归档-2026-07-28.md)；本文 29 日起重新起表（保留 wave618–650）。钉盘仍 **`01479a857`** · tip **wave650** @ **`d5aec6a64`** · 下波 **wave651**。 |
| 2026-07-29 | **wave650** | **R2 Cap residual pure wave650**：**if/while 条件整式含分组括号**。根因：wave361 见 `(` 剥括号再 parse_cond → `if (x >> 2) != 4` 只解析 `x >> 2`，外层 `!= 4` 使 LBRACE 失败 → 静默丢 main → XP003 out_len=0（`if ((x>>2)!=4)` / bare `if x>>2!=4` / let 中转已绿；`<<`/`while` 同形）。权威：G.7 补全同一 cond 入口 — 不剥前导 `(`，条件从首 token 整式解析（primary 分组）；if_stmt/if_expr/while block+onefunc；seed parser_gen 同 commit。探针：mac host+fs if_shift/eq/shl/while/ashr/paren/bare/let/gt/lt/field/call/nested/if-expr/elseif=42 · 矩阵 rv/option/hello。soft：untyped self；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave650*.log` · `/tmp/ubuntu_g05_wave650.log` · `/tmp/ubuntu_w650_probes2.log`）。tip **`d5aec6a64`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave649** | **R2 Cap residual pure wave649**：**freestanding FIELD STRUCT_LIT/CALL fixed array as TYPE_SLICE call-arg**。根因：wave396 `take(field)` fat pack 用 `pipeline_asm_emit_lvalue_eff_addr`（仅 VAR/INDEX/DEREF 真左值）→ `W{…}.xs` / `mk().xs` -1 → freestanding CG002（host-C 绿；`take(w.xs)` VAR 绿；wave610 `T[N]` formal 绿）。权威：G.7 补全同一 call-arg — data 半复用 `glue_try_index_var_or_field_base_to_rax`（wave609 leave_addr / wave610 E*）；dual-GP length 不变。探针：mac/Ubuntu lit/call/var/f32/nest/arr/ARRAY_LIT host+fs=42 · 矩阵 · pure-asm 同。soft：untyped self；`if (x>>k)` 直接 binop 入 if 可丢 main（let 中转绿）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave649*.log` · `/tmp/ubuntu_g05_wave649*.log` · `/tmp/ubuntu_w649_probes.log`）。tip **`84f9b813c`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX gold · MACOS\|ARM64 co-path · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave648** | **R2 Cap residual pure wave648**：**freestanding signed `>>` / `>>=` arithmetic right shift**。根因：`pipeline_asm_emit_binop_shift_elf_c`（op=1）与 assign case 38 恒发逻辑 `shr`/`lsr`，有符号 `-16>>2` 得 `0x3ffffffc`（host-C SAR 绿；Ubuntu fs `== -4` 为 0；正数/`u32` 逻辑右移本绿；mac CTFE 常 fold 假绿）。权威：G.7 补全同一 shift — `glue_binop_operand_is_unsigned` → SHR 否则 SAR；`is_64bit` → `backend_enc_sar_cl_{eax,rax}_arch`（编码器 wave306 已有 arm64 asr）。探针：mac/Ubuntu ashr_neg/asg/i64/let/var/lshr/u32 host+fs=42 · f32/~/ %= 回归 · 矩阵 · Ubuntu disasm `sar %cl,%eax`（was `shr`）。soft：untyped self；`take(W.xs)` f32[2]→[]f32 fs CG002；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave648.log` · `/tmp/ubuntu_g05_wave648.log` · `/tmp/ubuntu_w648_probes.log`）。tip **`af5e1c609`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX x86 sar · MACOS\|ARM64 asr · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave647** | **R2 Cap residual pure wave647**：**freestanding `[]f32` bare ARRAY_LIT call-arg pack**。根因：durable/force_esz fill 对 FLOAT_LIT 走默认 f64 IEEE 位，再 `store esz=4`（formal []f32）只写低 32 → f64 1.0 低半=0 → Ubuntu fs `take([1.0,2.0])`=0（host-C 绿；let→take 绿；length=2 对；mac CTFE 常 fold 假绿）。权威：G.7 `glue_array_lit_emit_scalar_elem_to_rax_elf_c` — peel SLICE/ARRAY elem force_ty 入 `glue_emit_float_lit`（wave300）；force_esz==4 无 peel 仍包 f32 位。探针：mac/Ubuntu f32 call/let/i0/idx1/retv host+fs=42 · f64/i32/i64/bnot/%= 回归 · 矩阵 · Ubuntu disasm `mov $0x3f800000`/`0x40000000`。soft：untyped self；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave647.log` · `/tmp/ubuntu_g05_wave647.log` · `/tmp/ubuntu_w647_probes.log`）。tip **`6e501513d`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX gold · MACOS\|ARM64 co-path · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave646** | **R2 Cap residual pure wave646**：**freestanding unary `~` / `-` GP canonicalize**。根因：32 位 `not`/`neg` 高半零扩展，freestanding cmp 走 64 位 GP，负 i32 lit 全宽符号扩展 → `~x == -1` / `-x == -5` fs=0（host-C 绿；pure 常 CTFE 假绿；`return ~x` 低 8 位 255 掩盖）。BITNOT 另缺 wave306 式 i64 全宽 not → `~i64(0)==-1` 同假跑。权威：G.7 补全 `pipeline_asm_emit_bitnot_elf_impl` + neg i32 分支 — ① i64/u64/usize/isize/ptr 全宽 not（REX.W / mvn x0）② i32 后 sxtw/cdqe（`glue_enc_sxt_i32_result_to_rax_elf_c`）③ u32 保持 zext。探针：mac/Ubuntu bnot_eq/neg_eq/i64not/u32eq host+pure+fs=42 · 回归 %= · 矩阵 · disasm mac `mvn`+`sxtw` · Ubuntu `not %eax`+`cltq`。soft：untyped self；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave646.log` · `/tmp/ubuntu_g05_wave646.log` · `/tmp/ubuntu_w646_probes.log`）。tip **`c0067fb97`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX x86 cdqe · MACOS\|ARM64 sxtw · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave645** | **R2 Cap residual pure wave645**：**arm64 freestanding `%` / `%=` rem**。根因：产品 seed `arch_arm64_enc_enc_mov_edx_to_eax` 为空桩（return 0），而 `backend_enc_rem_mod_arch(ta==1)` 只调该 helper（历史名 edx:eax rem；arm64 无 cltd/idiv 前缀）→ mac fs `17%5`/`x%=5` 留 17（host-C 绿；Ubuntu x86 走 cltd+idiv+mov_edx 本绿；`arm64_enc.x` 已有 sdiv+msub 但产品链用 C seed）。权威：G.7 补全同一 seed — `sdiv w2,w0,w1; msub w0,w2,w1,w0`（≡ arm64_enc.x 编码 0x1ac10c02/0x1b018040）。探针：mac/Ubuntu mod_asg/mod_bin/mod_var/mod0 host+pure+fs=2/2/2/6 · /= += *= 回归 · 矩阵 · disasm mac `sdiv`+`msub` · Ubuntu `cltd`+`idiv`+`mov %edx,%eax`。soft：untyped self；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave645.log` · `/tmp/ubuntu_g05_wave645.log`）。tip **`fd7782ec8`**；钉盘仍 **`01479a857`**。PLATFORM: MACOS\|ARM64 product seed · SHARED rem 契约 · LINUX\|x86 不变 · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave644** | **R2 Cap residual pure wave644**：**freestanding `*i64`/`*u64`/`*Named` 指针算术 esz**。根因：`glue_index_elem_byte_sz_from_type_ref_c` PTR peel 仅 F64(ord15)→8，U64/I64/USIZE/ISIZE(4..7) 落默认 4，*Named 无 layout → `p+1`/`p+=` 半元素步进（host-C 绿；pure CTFE 常假绿；disasm `mul #4`）。权威：G.7 补全同一 PTR peel — 8B 整数与 bare 面一致；*Named `glue_type_size_simple`（≡ ARRAY-of-NAMED）。探针：mac/Ubuntu i64 bin/comp/u64/struct/*i32/*u8/*f64/diff host+pure+fs=32/32/32/32/32/32/32/2 · 矩阵 · disasm `mul #8`/`imul $8`。soft：untyped self；`%=` fs 假跑；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave644.log` · `/tmp/ubuntu_g05_wave644.log`）。tip **`85c07dbf3`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave643** | **R2 Cap residual pure wave643**：**compound `*T +=/-=` integer offset typeck（`p += 1` / `p -= n`）**。根因：assign 把 RHS ambient 设为 lt（`*T`）并 `type_refs_equal` → expected `*i32` found `i32`；`p = p + 1` 已绿（wave285 binop + wave642 scale）。产品 mega 权威是 `pipeline_typeck_check_expr_assign_c`（非仅 typeck.x）——仅改 typeck.x 时 Ubuntu 仍红。权威：G.7 补全同一 assign — ADD/SUB_ASSIGN + TYPE_PTR → RHS 整数 offset、不 stamp `*T`、拒非 int RHS；typeck.x + seed + glue 同语义。探针：mac/Ubuntu p+=/p-=/var/p+=2 host+pure+fs=42 · p=p+1/ptr_arith/diff 回归 · p+=ptr 硬红 · int compound-assign · 矩阵 · Ubuntu disasm `imul $4`+add。soft：untyped self；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave643b.log` · `/tmp/ubuntu_g05_wave643b.log` · `/tmp/ubuntu_w643_probes2.log`）。tip **`131c1d53b`**（typeck twin `5752e153c`）；钉盘仍 **`01479a857`**。PLATFORM: SHARED typeck · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave642** | **R2 Cap residual pure wave642**：**freestanding C-like pointer arithmetic scale（`p±n` / `n+p` / `q-p`）**。根因：typeck wave285 已放行 ptr±int / ptr-ptr，但 freestanding ADD/SUB 对 GP 原值加减无 `sizeof(*p)` 缩放 → `*i32` `p+1` 进 1 字节读 0（pure CTFE 假绿；host-C 绿；`*u8` esz=1 偶然绿；`q-p` 返字节差 8≠2）。权威：G.7 补全同一 ADD/SUB — `glue_try_emit_ptr_arith_scaled_elf_c`（ptr@rax · offset×esz@rbx · add/sub；ptr-ptr sub 后 idiv esz）；pointee 宽复用 `glue_index_elem_byte_sz_from_type_ref_c` PTR peel；assign `p±=` scale rbx。探针：mac/Ubuntu ptr_arith/sub/int_ptr/diff/add_var/u8/struct/pae host+pure+fs=42/42/42/2/42/42/42/42 · aod 回归 · 矩阵 · disasm `mov $4`+add。soft：`p+=1` typeck found i32（**wave643 已闭**）；untyped self；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave642.log` · `/tmp/ubuntu_g05_wave642.log` · `/tmp/ubuntu_w642_probes.log`）。tip **`0f3be5125`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave641** | **R2 Cap residual pure wave641**：**freestanding ADDR_OF of EXPR_DEREF（`&(*p)` / `&(*(*pp))`）**。根因：`pipeline_asm_emit_addr_of_elf_c` 仅 VAR(3)/INDEX(47)/FIELD(44) → DEREF(52) 回落 UNHANDLED → freestanding CG002（host-C `&(*(p))` 抵消为 `p`；pure-asm CTFE 常假绿）。语义：被指物地址 = DEREF 操作数指针位 ≡ wave324 lvalue_eff_addr（`*p=` lhs）。权威：G.7 补全同一 ADDR_OF — ko=52 转调 `pipeline_asm_emit_lvalue_eff_addr_elf_c`，不新开 cancel 第二套。探针：mac/Ubuntu aod/aod2/nest/eq/pass host+pure+fs=42 · w640/w639/arrptr 回归 · 矩阵。soft：untyped self；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave641.log` · `/tmp/ubuntu_g05_wave641.log` · `/tmp/ubuntu_w641_probes.log`）。tip **`1bc77f0bc`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave640** | **R2 Cap residual pure wave640**：**freestanding nested `**pp` / `(*(*pp)).v` / mid `let q=*pp`**。根因双层：(1) `pipeline_asm_emit_addr_of_elf_c` 对 VAR 复用 `glue_enc_local_slot_ptr_or_addr` → *T 槽 load（INDEX/field 基正确）使 `&p`≡`p`，`let pp:**T=&p` 存 pointee；(2) `pipeline_asm_emit_deref_elf_c` 对结果 TYPE_PTR 用 `glue_index_elem_byte_sz` 剥 *T→sizeof(T) → 半指针 load（wave637 INDEX 孪）。pure-asm CTFE 折叠 42 假绿；host-C 真 `&`。权威：G.7 ① ADDR_OF VAR **始终 lea** ② DEREF TYPE_PTR **load 64**；不改 needs_ptr_load。探针：mac/Ubuntu d2_field/load/mid/asg/addr_p_chk host+pure+fs=42/42/7/42/1 · w639/w638/arrptr 回归 · 矩阵 · disasm lea &p + mov64 mid。soft：untyped self；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave640.log` · `/tmp/ubuntu_g05_wave640.log` · `/tmp/ubuntu_w640_probes.log`）。tip **`92c07bfaa`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave639** | **R2 Cap residual pure wave639**：**freestanding FIELD over EXPR_DEREF as INDEX/call-arg base（`(*p).xs[i]` / `take((*p).xs)` / assign）**。根因：`glue_try_index_var_or_field_base_*` 仅 VAR-base + wave609 CALL/STRUCT_LIT；DEREF 基回落 field rvalue → `ldr w` 首元当指针 → SEGV（host-C 绿；`p.xs` 自动 `->` 绿；标量 `(*p).v` 绿）。权威：G.7 补全同一 helper — emit DEREF 操作数（指针位，wave324 lvalue 孪）+ field_off（rax/rbx 孪）；wave610 call-arg 复用。探针：mac/Ubuntu dref_idx/take/asg host+pure+fs=32/32/42 · p.xs/w638/arrptr 回归 · 矩阵 · disasm `add $4` 非 `ldr w` 误径。soft：嵌套 `(*(*pp)).v`（**wave640 已闭**）；untyped self；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave639.log` · `/tmp/ubuntu_g05_wave639.log`）。tip **`2b274afdc`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave638** | **R2 Cap residual pure wave638**：**host-C FIELD base primary 括号（`(*p).field`）**。根因：FIELD 发 `(base.field)`，base=DEREF 为 `*(p)` → `(*(p).v)`；C 中 `.` 优先于一元 `*` → `*((p).v)` BLD001（freestanding 绿；`self.v` with `self:*T` 绿）。权威：G.7 codegen.x + seed pin 发 **`((base).field)` / `((base)->field)`**；INDEX 已单独包 base 不动。探针：mac/Ubuntu take/asg/impl*self/plain/arrow/arrptr host+fs=42 · `-E` `((*(p)).v)` · 矩阵。soft：untyped self（wave493 非标准）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave638.log` · `/tmp/ubuntu_g05_wave638.log`）。tip **`b1a8c6cfc`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED host-C · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave637** | **R2 Cap residual pure wave637**：**freestanding array-of-pointers `*T[N]` INDEX esz + frame slot**。根因双层：(1) INDEX 结果 TYPE_PTR 经 `glue_index_elem_byte_sz_from_type_ref` 当 pointer-base 剥 *i32→4 → `ldr w` 半指针 SEGV（pure CTFE/host 假绿）；(2) `glue_fixed_array_total_bytes` / `asm_local_slot_bytes` PTR 元默认 4 → `*i32[2]` 槽 8B，Ubuntu x86 high-end a[1] 盖 y（mac low-end 空隙假绿）。权威：G.7 ① INDEX result PTR=8 ② ARRAY/SLICE of PTR=8 ③ **T base stride 8 ④ fixed-array total + local_slot PTR=8。探针：mac/Ubuntu pure+fs+host lit/asg=42 · p2a/i32/ptridx 回归 · disasm add$8+mov64 · 矩阵。soft：untyped self；L4。mac/Ubuntu L2。tip **`e82ecdbd2`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave636** | **R2 Cap residual pure wave636**：**PTR-to-fixed-array DEREF leave-addr + host-C `E (*)[N]`**。根因双层：(1) freestanding `pipeline_asm_emit_deref_elf_c` 对 DEREF 结果 TYPE_ARRAY（`*[N]T` → `T[N]`）仍按 esz 间接 load 首元，INDEX `(*p)[i]` 把标量当指针 → SEGV；(2) host-C `emit_type(PTR)` 对 TYPE_ARRAY 再 decay `E *` → `E * *`，而 `&a` 为 `E (*)[N]` → clang 警告 + 运行错。语法：`*T[N]`=array-of-pointers（C 式故意）；pointer-to-array 写 **`*[N]T`**。权威：G.7 ① deref TYPE_ARRAY leave-addr ② `emit_c_ptr_to_fixed_array_decl`（codegen.x+seed）named/abstract + let/param 名嵌声明符。探针：mac/Ubuntu pure+fs+host ptr2d/asg/s24take=42/42/14 · wave635 take 77 · arr-of-ptr pure/host 42 · 矩阵。soft：fs array-of-ptr（**wave637 已闭**）；untyped self method；L4。mac/Ubuntu L2。tip **`7bce90b01`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding+host-C · LINUX gold · MACOS\|ARM64 co-path · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave635** | **R2 Cap residual pure wave635**：**pure-asm fixed TYPE_ARRAY call-arg E* not MEMORY payload**。根因：wave417 `glue_func_param_agg_byte_size` 已把 T[N] 形参 home 定为 E*（8B），但 `pipeline_asm_call_arg_value_byte_size_c` 仍 `glue_type_size_simple` 全载荷（S24[2]=48 / S12[2]=24）→ SysV/AAPCS MEMORY 多字压栈，callee INDEX 当指针读 x0/rdi → freestanding SEGV（mac 139/138；Ubuntu 同）；pure-asm 无 `-freestanding` 常 CTFE 折叠假绿；host-C T[N] decay 绿；S8[2]≤16 dual-GP 与 named S24 MEMORY（wave601）本绿。权威：G.7 补全 call-arg size ≡ TYPE_SLICE / param_agg → formal/arg TYPE_ARRAY **return 8**；emit 既有 lea local / load param E* / CALL return E*（wave417/610）。探针：mac/Ubuntu pure+fs+host take_call/var/lit/s12/s8/s24 · disasm lea→rdi/x0 · 矩阵 rv/option/hello。soft：impl method on INDEX；`*T[N]` 解析序；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave635.log` · `/tmp/ubuntu_g05_wave635.log`）。tip **`b5615797e`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX gold · MACOS\|ARM64 co-path · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave634** | **R2 Cap residual pure wave634**：**soft 假红闭 — uninit fixed TYPE_ARRAY assign**。现象：wave633 soft 写 `let s: S24[2]; s=mk()` 丢 main/BLD001。根因：现场探针**缺 let 后分号**（`let s: S24[2]` 下一行 `s = mk()`）→ P001 `expected ';' after let` → pipeline `num_funcs=0`（`docs/06`/`08`：bare let 须 `let name: Type;`）。合法语法 + 既有 wave334/354/633 assign/bulk：**双端 pure-asm+host-C fx=110**；标量 `let x: i32; x=42;`=42；邻域 fx/asg2/varc/sl/i32/pt + 矩阵绿。**0 行产品代码**。soft：其它 Cap 叶；L4。mac/Ubuntu L2 验证。产品 tip 仍 **`9a16fb325`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED 语法契约 · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave633** | **R2 Cap residual pure wave633**：**pure-asm fixed TYPE_ARRAY let from CALL/VAR bulk esz>8**。根因：wave632 已闭 `return [S24…]` durable E*；`let s: S24[2]=mk()` 走 `glue_struct_lit_store_fixed_array_field` CALL 路径，esz∉{1,4,8} 落 `load_i32` → 每元仅首 i32（Ubuntu fx=11=a0+a1；host-C memcpy 110；临时 INDEX 绿）。权威：G.7 扩 wave351/399 fixed-array let + wave630 bulk — esz>8 整块 `n*esz` `glue_emit_bulk_mem_copy_spills`（CALL/INDEX + VAR/FIELD 孪）；标量 1/4/8 仍 per-elem。探针：mac/Ubuntu fx/fields/varc/asg/direct/sl/i32/pt · host-C · 矩阵；Ubuntu disasm 6× qword bulk。soft uninit asg 已由 **wave634** 证假红。mac/Ubuntu L2（`/tmp/mac_g05_wave633.log` · `/tmp/ubuntu_g05_wave633.log`）。tip **`9a16fb325`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX gold · MACOS\|ARM64 co-path · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave632** | **R2 Cap residual pure wave632**：**pure-asm return large NAMED ARRAY_LIT durable + reent deep-copy**。根因三层：(1) wave631 durable 拒 force_esz>8 → `return [S24{…}]` 栈 dual-GP 悬垂（Ubuntu simple=193/deep=113≠110；host-C 110；mac 假绿）(2) 即便 COMMON 绿，`let s=mk()` reent deep-copy 把 esz=24 夹成 4 → 半字拷 (3) x86 high-end dest_off 过浅 + 大 nbytes 越 rbp → SIGSEGV 139。权威：G.7 扩 wave341/418/598/630 — ① durable force_esz>8 COMMON bulk（struct let-init + bulk_mem_copy）② deep-copy 保留 esz>8 + chunked bulk + frame pre-sum 64KiB ③ high-end byte0@dest_base+nbytes。探针：mac/Ubuntu simple/deep/dual/let/i32/pt · fs_abs · host-C · 矩阵；soft fixed `return S24[2]` fx=11。mac/Ubuntu L2。tip **`2a9d40d0c`**（`fde6a0366`+`ff3b021d6`+polarity）；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave631** | **R2 Cap residual pure wave631**：**pure-asm TYPE_SLICE ARRAY_LIT large NAMED esz（S24[]）**。根因：wave630 soft — `let s: S24[] = [S24{…},…]` force_esz=24 进 durable 后非 {1,2,4,8} 再 infer（`array_lit_elem_byte_sz` 默认 4 / `elem_type_ref` 仅 peel TYPE_ARRAY 对 SLICE stamp 返 0）→ COMMON 以 esz=4 存栈指针半字；INDEX 步长 24 → Ubuntu pure-asm RO=120·fields=200·sl bulk=66≠76（host-C 11/10/76；fixed S24[N] 绿）。权威：G.7 扩 wave625 durable + wave598/626 栈 — ① force_esz>0 非标量宽 → durable -1 ② `array_lit_elem_type_ref` peel TYPE_SLICE(11) ③ `pipeline_asm_emit_array_lit_force_esz_elf_c` 栈回落带 force_esz（let-init/call-arg/return）。探针：mac/Ubuntu s24_ro/fields/sl/z/fx/pt/asg/copy · fs · host-C · 矩阵；Ubuntu disasm 栈 in-place 无 Lxlang_al 4B pack。soft：其它 Cap 叶；return 大 NAMED durable 悬垂；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave631.log` · `/tmp/ubuntu_g05_wave631.log`）。tip **`8bbbf951b`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX gold · MACOS\|ARM64 co-path · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave630** | **R2 Cap residual pure wave630**：**pure-asm INDEX assign bulk esz>8 + try_* scale gate**。根因：wave629 soft — 非 STRUCT_LIT `a[i]=t`/`a[j]=a[i]`/`a[i]=mk()` esz∉{1,4,8}：① assign-side try_* → `rbx_plus_index_scratch_scaled` 仅×1/4/8（esz=12/24 落 ×8）② finish_store `store_rax_to_rbx_indirect` 宽≤8 且 rhs VAR 只 load 首 qword（Ubuntu s24_asg=14=1+10+3；s12=33；copy=184；call=139；host-C 66/120；arm64 常假绿）。权威：G.7 扩 wave628/629 INDEX — ① esz>8：src lvalue lea 或 CALL→`glue_emit_struct_type_let_init` temp，dest `glue_emit_index_eff_addr_scaled`（imul esz / slice .data），`glue_emit_bulk_mem_copy_spills` 8/4/1 chunk（禁 libc）② try_* 仅 esz∈{1,4,8} 否则 fallthrough imul。探针：mac/Ubuntu s24_asg/copy/s12/call/sl2(dual bulk)/lit 回归 · fs（`XLANG=./compiler/xlang_asm`）· host-C · 矩阵；Ubuntu disasm `imul $0x18` + 三 qword store。soft：非零 ARRAY_LIT（**wave631 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave630.log` · `/tmp/ubuntu_g05_wave630.log` · `/tmp/ubuntu_w630_probes2.log`）。tip **`66a0f3ffa`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave629** | **R2 Cap residual pure wave629**：**pure-asm TYPE_SLICE INDEX STRUCT_LIT assign in-place**。根因：wave628 仅 TYPE_ARRAY；`S24[] s[i]=S24{…}` 回落 generic → temp 指针 + finish_store ≤8（host-C 绿；fixed 绿）。扩 TYPE_SLICE→eff_addr(.data+imul esz)+DEST_IN_RBX 后 lit/one 绿，但 dual var-index Ubuntu panic:0 — DEST_IN_RBX 帧 `next_offset` spill 叠局部 `j`，二次 bounds 把指针当 index。权威：G.7 ① TYPE_SLICE 任意 index 走 scaled+DEST_IN_RBX（禁 fat home 当地址）② dest push CPU 栈 rehome（禁 next_offset 叠局部）。探针：mac/Ubuntu s24/s24one/s24lit/s24fx/s24take/pt/fx · fs/host-C · 矩阵。soft：assign-side non-STRUCT_LIT esz（**wave630 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave629c.log` · `/tmp/ubuntu_g05_wave629c.log` · `/tmp/ubuntu_w629_probes.log`）。tip **`2a0496153`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave628** | **R2 Cap residual pure wave628**：**pure-asm var-index STRUCT_LIT assign in-place**。根因：wave627 仅 INT_LIT；`a[i]=S24{…}` 回落 generic → rax=temp 指针 + finish_store 宽≤8 写指针进元素（Ubuntu s24_var=64 / s24_one=200；host-C 66·6；Pt 变下标 8B 常假绿）。assign-side `rbx_plus_index_scratch` 对 esz∉{1,4} 落 ×8 → a[j] esz=24 错位。权威：G.7 扩 wave627 — ① non-lit TYPE_ARRAY 基 `glue_emit_index_eff_addr_scaled`（通用 imul esz）→ rbx ② `GLUE_STRUCT_LIT_DEST_IN_RBX` 字段直写 ③ dest 指针 spill 帧槽 `rehome_off`（禁 sret_home 未激活重载）。探针：mac/Ubuntu fx_var/p0x/p1y/s24_var/s24_one/s24_lit/fx_lit/fx_sum/fld/sl · fs/host-C · 矩阵；Ubuntu disasm `imul $0x18` + [rbx+0/8/10]。soft：slice INDEX STRUCT_LIT（**wave629 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave628.log` · `/tmp/ubuntu_g05_wave628.log` · `/tmp/ubuntu_w628_probes.log`）。tip **`74c40b979`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave627** | **R2 Cap residual pure wave627**：**pure-asm INDEX STRUCT_LIT assign + multi-dim flat in-place**。根因：`a[i]=Pt{…}` 经 rvalue STRUCT_LIT(slot=-1) 物化到 `ly->next_offset`（fixed TYPE_ARRAY 后 ≡ a[0]）字段写覆盖 a[0]，再 finish_store bulk≤8 到 a[i] → Ubuntu pure-asm sum=32 / p0x=0（单 a[0]= 与 VAR/CALL rhs 绿；host-C 42）；`S24[i]=` store 宽≤8 → run=3；multi-dim `Pt[2][1]=[[…]]` flat 同 rvalue 类。权威：G.7 扩 wave626 — ① `emit_assign` INDEX 叶对 TYPE_ARRAY+INT_LIT 基址 `pipeline_asm_emit_struct_lit_fields` 于 arch-aware elem_home（禁 SLICE fat home）② `array_lit_flat` STRUCT_LIT/>8B 叶 `glue_emit_struct_type_let_init`。探针：mac/Ubuntu fx_asg/p0x/arr_of_arr/s24/fx_sum/fld/sl 回归 · fs/host-C · 矩阵；Ubuntu disasm a[1]@−0x18 直写。soft：var-index（**wave628 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave627b.log` · `/tmp/ubuntu_g05_wave627.log`）。tip **`ab3873604`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave626** | **R2 Cap residual pure wave626**：**pure-asm fixed `Pt[N]` STRUCT_LIT in-place（esz≤8）**。根因：rvalue STRUCT_LIT（slot_off=-1）物化到 `ly->next_offset` 且不抬升 high-end top；fixed TYPE_ARRAY 分配后 next_offset ≡ 数组 byte0 → 第二元字段写覆盖 a[0] 后再 bulk store +ai·esz（Ubuntu p0x=0/p0y=32/sum=32；host-C 42；arm64 low-end next 过分配假绿）。权威：G.7 扩 wave598 — `vector_let_init` + `emit_array_lit` 对 STRUCT_LIT(ko=45) **任意 esz** 走 `glue_emit_struct_type_let_init` 于 arch-aware elem_home（x86 base−ai·esz / arm64 base+ai·esz）。探针：mac/Ubuntu fixed p0x/p0y/p1x/p1y/sum=10/0/0/32/42 · fs/host-C · `[]Pt` take/sum · i32[2] · 矩阵；Ubuntu disasm elem0@−0x20 · elem1@−0x18。soft：其它 Cap 叶；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave626.log` · `/tmp/ubuntu_g05_wave626.log`）。tip **`becc92c72`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave625** | **R2 Cap residual pure wave625**：**pure-asm `[]Named` force_esz + FIELD dual-slot**。根因双层：(1) ARRAY_LIT→TYPE_SLICE force_esz 仅标量 TypeKind → durable default esz=4 只 str w0 of by-value STRUCT_LIT（y 丢）而 INDEX 用 glue_type_size_simple 步长 8；(2) `a[0].x+a[1].y` 的 clobber 分类器不 peel FIELD→INDEX → rhs@x1 被左 bounds/length 盖掉（sum=10+2=12；p1y 单读=32；let 拆分=42）。权威：G.7 ① `glue_array_lit_force_esz_from_elem_type_c`（scalar+TYPE_NAMED layout）接 let-init/call-arg/return 四口 ② `glue_binop_operand_index_addr_clobbers_rbx_elf_c` peel FIELD（复用既有 preserve）。探针：mac/Ubuntu pure+fs take/sum/let=42 · host-C · `[]i64`/dual lit binop · 矩阵；Ubuntu pure-asm fixed `Pt[2]` soft p0x=0。soft：其它 Cap 叶；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave625b.log` · `/tmp/ubuntu_g05_wave625.log`）。tip **`b278583c4`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave624** | **R2 Cap residual pure wave624**：**host-C `[]NamedStruct` incomplete type**。根因：TYPE_SLICE of user NAMED 发 `struct xlang_slice_<tag>` 无布局；empty prefix 强制 `ast_` 与 entry bare / 模块前缀 struct 双标签（formal `xlang_slice_mod_Pt` vs local `xlang_slice_ast_Pt`）。权威：G.7 ① struct 定义后 companion `xlang_slice_TAG { struct TAG *data; size_t length; }` ② `emit_type` TYPE_SLICE NAMED 用与 TYPE_NAMED 同前缀规则（entry bare）③ `type_to_c_repr` empty→bare ④ call-arg `emit_type(formal SLICE)`；codegen.x+seed+ast_pool 同 commit。探针：mac/Ubuntu host-C `[]Pt` take/local/bare-E+cc=42 · fixed/arr_take/`[]i64` 回归 · 矩阵；pure-asm `[]Pt` soft。soft：pure-asm/fs named INDEX esz；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave624.log` · `/tmp/ubuntu_g05_wave624.log`）。tip **`97b3e5d82`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED host-C named slice · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave623** | **R2 Cap residual pure wave623**：**host-C bare `-E` incomplete type**。根因：`codegen_x_ast_emit_header` 仅吐 stdint/stddef/sys/types，body 发 `struct xlang_slice_*` 无布局 → host-cc incomplete type（`-backend c -o` 经全量 rt_preamble 绿；wave619 已补全 preamble 标量集）。权威：G.7 扩 emit_header ≡ wave618–619 全套 fat layout + `XLANG_SLICE_LAYOUTS`；rt_preamble 同 guard 防 full `-o` 再吐 header 重定义；seed codegen_gen + codegen.x 同 commit。探针：mac/Ubuntu bare `-E`+host-cc i32/i64/u8/f32/f64/u32/isize/bool=42 · `-backend c -o`/pure-asm 回归 · 矩阵。soft：其它 Cap 叶；L4；Darwin `int64_t main` 硬要求 int。mac/Ubuntu L2（`/tmp/mac_g05_wave623.log` · `/tmp/ubuntu_g05_wave623.log`）。tip **`0081c8812`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED host-C minimal preamble · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave622** | **R2 Cap residual pure wave622**：**pure-asm slice lit call-arg 假绿**。根因：wave332 `pipeline_asm_emit_expr_elf_for_call_args` 对 ARRAY_LIT→TYPE_SLICE 仅用 formal esz 预留帧，载荷仍走 `emit_array_lit`→`array_lit_elem_byte_sz`（未 stamp INT_LIT→i32 esz=4），INDEX 按 formal 宽读 → `take([10,32])` 对 []i64/[]u8/[]f64 为 s[1]=0/sum=10（i32/f32 绿；let→take 经 force_esz durable 绿；host-C `-backend c -o` 绿）。权威：G.7 补全 call-arg ≡ let-init — formal force_esz + `glue_asm_emit_array_lit_durable_ptr_rax_elf_c` 优先，栈 fallback 保留。探针：mac/Ubuntu pure+fs i32/i64/u8/f32/f64 idx1=32·sum=42 · let 回归 · host-C · 矩阵。soft：host-C bare `-E`（**wave623 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave622.log` · `/tmp/ubuntu_g05_wave622.log`）。tip **`f917861d7`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding call-arg · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave621** | **R2 Cap residual pure wave621**：**pure-asm f32 ordered cmp 假绿**。根因：`glue_emit_cmp_finish_rbx_rax_elf_c` 仅门控 scalar f64→ucomisd/fcmp d；f32 落整数 cmp 64 位栈加载（高半垃圾→等值假红；负序 SF 错）。权威：G.7 扩 finish + `backend_enc_ucomiss_rbx_rax_arch`（x86 movd+ucomiss；arm64 fmov s+fcmp s）+ fp setcc；int-imm 捷径 skip f32。探针：mac/Ubuntu pure-asm/fs f32 ==/!=/<>/<=eq/>=eq/bool/eq_lit/neg_lt=42 · f64/wave620 回归 · 矩阵；Ubuntu disasm `ucomiss`；mac disasm `fcmp s`。soft：i64/u8/f32 slice lit call-arg（**wave622 已闭**）；host-C bare `-E` minimal preamble 缺 slice 布局；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave621.log` · `/tmp/ubuntu_g05_wave621.log`）。tip **`2d81d0dff`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding f32 cmp · LINUX x86 ucomiss · MACOS\|ARM64 fcmp s · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave620** | **R2 Cap residual pure wave620**：**pure-asm dual-slot float→int AS peel 假绿**。根因：`glue_try_binop_load_operand` 对 integer-target EXPR_AS peel（wave301 为 `u8[i] as i32`）；float-target 已 full emit_as，**float-source** `(f32 as i32)` 仍 peel → 整数加 IEEE 位（mac/Ubuntu pure-asm run=0，`$?` 8 位截断；host-C 绿；top-level `z as i32` 走 emit_as 绿）。权威：G.7 `glue_binop_as_needs_full_emit_elf_c` + full `pipeline_asm_emit_as_elf_impl`（cvttss2si/fcvtzs）；load-to-rbx full-AS 恒 clobber rax。探针：mac/Ubuntu host+pure f32_lit/idx/slice/asg/add=42 · u8 peel 回归=42 · 矩阵；Ubuntu disasm `cvttss2si`。soft：其它 Cap 叶；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave620.log` · `/tmp/ubuntu_g05_wave620b.log`）。tip **`6b7d3ba67`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding dual-slot · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave619** | **R2 Cap residual pure wave619**：**host-C 全标量 TYPE_SLICE 标签补全**。根因：wave618 仅补 f32/f64；`bool[]`/`i64[]`/`u32[]`/`isize[]` 及 NAMED `i8`/`i16`/`u16`（旧 `xlang_slice_ast_*`）preamble 缺布局 → host-cc BLD001 incomplete type；call-arg 手写 map 与 local 漂移（缺 case→int32_t）。权威：G.7 ① type_to_c_repr NAMED i8/i16/u16→stdint ② preamble 全套 xlang_slice_* ③ call-arg `type_to_c_repr(formal SLICE)` 收敛（codegen.x+seed）。探针：mac/Ubuntu host-C 全标量 T[]+call-arg=42 · tag 对齐 · 矩阵；pure-asm f32 soft 0 · pure-asm i64[] call soft 10（local i64[] 绿）。soft：pure-asm f32；i64[] call-arg fs；L4。mac/Ubuntu L2。tip **`6c1262ee9`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED host-C type_to_c_repr+preamble · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave618** | **R2 Cap residual pure wave618**：**f32[]/f64[] host-C TYPE_SLICE C 名**。根因：`pipeline_codegen_type_kind_cstr` 仍用 LINEAR/VECTOR 前的 11/12/13 映射 float/double/void；TYPE_F32=14 落 default→int32_t → `type_to_c_repr` 发 `struct xlang_slice_int32_t` 而载荷 `static float[]`；mac host-C/clang -O2 UB（int 读 float 位再加）run 假 0；Ubuntu pure-asm 本绿。权威：G.7 单表修 ordinal 14/15/16 + preamble `xlang_slice_float`/`double` + call-arg map F32/F64。探针：mac/Ubuntu host-C+asm+fs f32[]/f64[]/f32[2]/i32[]=42 · `-E` `xlang_slice_float` · 矩阵。soft：其它 Cap 叶；L4。mac/Ubuntu L2。tip **`7a943902f`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED host-C type_to_c_repr · **日常 L2 非 tip L4** |

***

## 7. 禁止清单（本阶段）

- [ ] 未用旧 stage1 / 未用归档 tip 冒充当前 tip L4
- [ ] 未把 prove / Stage2 / N++ 单独写成「可自举」
- [ ] 未 BEST_EFFORT / soft-skip 糊绿
- [ ] 未 seed/.x/glue 分叉合入
- [ ] 未把 mac `-dead_strip` 假绿当 Ubuntu 全链已过
- [ ] 日常 L2 未写成 tip L4；谈升钉须双端 L4 + 129
