# Xlang / X language 自举进度

> 更新：2026-09-26 · w1055 Darwin labi 针表只留一份。L5 与 L8b 冷 seed 不再内嵌针表。现对象侧路能链上，L2 0/5，没有装入。钉盘 `ecdb5cc1e` 不升。没有跑 L4。
> 状态：尚未完全自举。option=102。
> 下波：不跑 g05_prepare。不跑 L4，不升钉。禁 bak 175735。不要重编 `backend_x86_64_enc_c.o`，也不要重编 `backend_enc_dispatch.o`。不要用 gcc `-E` 编 emit_one。不要装入 w1055 侧路 `765f6f8ed4821daf`。

闸门：日常三端 L2。暂停 `xlang check`。L4 仅整波另派。

### 维护约定

1. 每波只改仪表盘、前排、流水一行。勾选改 [`C迁移追踪.md`](C迁移追踪.md)，一行一个 ✅。
2. 不写 Class 长文，不写「本日要点」。
3. 日切：新的一天先归档本文为 `自举进度-归档-YYYY-MM-DD.md`，再重写当天本文。
4. 流水是第 4 节。表固定三列：`日期 | 波 | 一行`。描述里不用半角 `|`。

## 0. 一句话

尚未完全自举。钉盘 `ecdb5cc1e`。w1055：Darwin `runtime_link_abi.o` 里 53 个针表强符号来自 L5、L8b 冷 seed 内嵌同一份 `labi_od_needle_tables.c`，合并时又链了 needle 成员。多切片路径在 needle 对象编成后给这两份 seed 加上 `XLANG_LABI_NEEDLE_TABLES_EXTERNAL`。重编后是单个 Mach-O，强符号 546 个，重复 0。现对象侧路 4101976 `765f6f8ed4821daf` 能链上。L2 0/5：`use_asm_backend` 走进 `main_run_compiler_c`，`driver_run_compiler_full` 返回 1，没有诊断。没有装入。没有跑 L4。option=102。

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举效率方法-M2主链.md](自举效率方法-M2主链.md) | M2 主链 |
| [C迁移追踪.md](C迁移追踪.md) | 已完成勾选 |
| 本文 | 当天流水 |
| [自举进度-归档-2026-09-25.md](自举进度-归档-2026-09-25.md) | 9 月 25 日 |
| [自举进度-归档-2026-09-24.md](自举进度-归档-2026-09-24.md) | 9 月 24 日 |
| [自举进度-归档-2026-09-23.md](自举进度-归档-2026-09-23.md) | 9 月 23 日 |

## 2. 仪表盘

| 项 | 结果 |
|----|------|
| 钉盘 | `ecdb5cc1e`（不升） |
| Darwin | 4091096，`181f5b58c5a40862`，L2 5/5。针表重复已从产生点去掉。侧路未装入 |
| Windows | 4739967，`1120a659cc81979e`，L2 5/5。尾跳 `e9`。pabi 1675145 |
| Ubuntu | 5594304，`1afb107d005e68da`，L2 5/5。`add` 为 `e9` jmp。pabi 2738144 |
| 构建级别 | L2，不跑 `xlang check` |

## 3. 当前前排（≤3）

1. 不跑 g05_prepare。不升钉。不跑 L4。
2. 禁 bak 175735。
3. Darwin 现对象侧路能链上，L2 0/5。`driver_run_compiler_full` 返回 1。不要装入 `765f6f8ed4821daf`。不要用 gcc `-E` 编 emit_one。

## 4. 波次流水（倒序 · 9 月 26 日）

| 日期 | 波 | 一行 |
|------|----|------|
| 2026-09-26 | w1055 | Darwin labi 针表只留一份。L5 与 L8b 冷 seed 在多切片合并时内嵌 `labi_od_needle_tables.c`，同时又链了 needle 成员，53 个 `*_at` 强符号重复，Darwin ld 拒绝。needle 对象编成后，这两份 seed 改带 `XLANG_LABI_NEEDLE_TABLES_EXTERNAL`。`runtime_link_abi.o` 从归档变成单个 Mach-O，414560 字节，强符号 546，重复 0。现对象侧路 4101976 `765f6f8ed4821daf` 链上。L2 0/5。`use_asm_backend` 调用 `main_run_compiler_c`，`driver_run_compiler_full` 返回 1，没有诊断。产品仍是 4091096 `181f5b58c5a40862`。没有装入。没有跑 L4。 |
| 2026-09-26 | w1054 | Darwin 尾跳没有调用方。pabi 归档成员里 `w499_mega_try_tail_jmp` 是强符号，mega 到它的重定位是 0，直接分支也是 0。调用方在 emit_one 薄层。现行 Darwin 编译器编这份薄层时在 `asm_module_top_level_const_lit_i32` 偏移 108 读坏指针，退出 139，没有诊断。尾跳薄层本身能编过。现对象按 g05 重链失败：`runtime_link_abi.o` 里两个成员重复 53 个 labi 符号。产品仍是 4091096 `181f5b58c5a40862`。没有装入。没有跑 L4。 |
| 2026-09-26 | w1053 | Windows 链接让 dispatch 赢过条件跳、字节存储、加法和减法。enc_c 副本把这四个定义收成未定义，重定位仍指向原名，PE 先到先得绑到 dispatch。objcopy -N 去不掉条件跳，对象里还有 4 处重定位。没有重编两份编码器，原对象仍是 50699。脚本重链与已测侧路是同一份 4739967 `1120a659cc81979e`。L2 5/5。`return forty_two()` 是 `e9`，运行 42。e_machine 立即数仍是 0x2990018。已装入。egg 换成完整 mega 1675145 `6bce6dbd2a26ce8c`。Darwin 与 Ubuntu 未重链。没有跑 L4。 |
| 2026-09-26 | w1052 | Windows option 非法指令不是尾跳。现行 `x86_enc_jcc_rel32` 在 6 字节条件跳写入之前就算好补丁位置，把前一条 `movzbl`／`test` 盖成 `0f 37`，跳转位移留 0。源码注释已写明这会让 option 非法指令、stdlib-import 段错误。dispatch 那份先写入再取长度。字节存储改到 dispatch 后 `u8[4]` 是 `88 83`。加法改到 dispatch 后是 `01 d8`。这四处调用一起改完，w1050 完整尾跳侧路 L2 5/5，`return forty_two()` 是 `e9`，运行 42。手改侧路 4740039 `a5f9fe04c4b1e3d3` 未装入。产品仍 4710943 `3361754edc8e2184`。没有跑 L4。 |
| 2026-09-26 | w1051 | Windows option／si 段错误不是尾跳。`try_tail` 恒返回 0 时，编出的程序与完整尾跳同一份。现行 `backend_x86_64_enc_c.o` 在链接里盖过 `backend_enc_dispatch.o`。字节存储把 `u8[6]` 模板写在 `rbp-0x60`，位移写在 `rbp-0x68`，用户码是 `00 00 disp32`，运行当成 `add %al,(%rax)` 段错误。把这一处调用改到 dispatch 的副本后，`let buf: u8[4]` 是 `88 83`，运行 1。option 仍因 i32 加法 `48 01 d8`（`enc_add_rax_rbx`）非法指令退出 132，stdlib-import 仍段错误。产品未重链，4710943 `3361754edc8e2184`。没有跑 L4。 |
| 2026-09-26 | w1050 | Windows mega 重编后调用 `w499_mega_try_tail_jmp`。PE 里同名符号有两份，新 mega 的重定位必须绑活 mega 正在调用的那份，否则 `return 42` 丢掉 `mov $42`。绑对之后 forwarder 是 `e9`，运行 42；`return 42` 是 42；普通 `return a+b` 仍是 `push %rbp`。option 与 stdlib-import 运行 SEGV。侧路 `b0a3fee8c0af308d`（4740039）未装。产品恢复 4710943 `3361754edc8e2184`。Ubuntu／Darwin 既有二进制 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1049 | 收尾 `return` 在 `final_expr`（stmt_order 不记）。`var_name_into` 清 256 字节，32 字节槽把帧打穿。参数对比改走 `glue_expr_is_func_param_at_c`，jmp 名缓冲 256。Ubuntu `add` 是 `e9`，运行 42；`unsafe` 同；`return a+b` 仍是胖体。L2 5/5。`1afb107d005e68da`，5594304。pabi 2738144。`xlang-c` 仍是 5573672。Windows 4710943 L2 5/5，egg 无此调用。Darwin 未重链。没有跑 L4。 |
| 2026-09-26 | w1048 | tip 尾呼 jmp（中途）：活路＝seed mega（loop tip BAN）。落地 mega overlay（egg `0x10a0018`）调 try_tail＋`enc_jmp_sym`；检测未命中仍 fat。Ubuntu L2 5/5 `f487d9f51fdd8bc7`。没有跑 L4。 |
| 2026-09-26 | w1047 | Win 全 tip thin：地图＝call_spill=0 forwarder 出栈参与 param home 重叠（7＋参）。落地 compute_frame／overlay 保留 shadow＋8*(n-4)；ensure 默认全 tip。三端 L2 5/5。`e83d53eb98e21b84`／`181f5b58c5a40862`／`3361754edc8e2184`。没有跑 L4。 |
| 2026-09-26 | w1046 | Win tip thin 装入：地图＝tip fat forwarder（非叶体）→ option CG002／si；叶体＋host `jmp _impl` 绿。落地 cfg `call_reg_max`、prologue 恒 push rbx、ensure tip-leaf＋host-fwd（默认 ON）。三端 L2 5/5。`35cbb73f8cfd74f8`／`181f5b58c5a40862`／`30b9e95bd2ed3668`。没有跑 L4。 |
| 2026-09-26 | w1045 | Win64 根修：enc k≥4＋reg_max=16＋leftover param_home `glue_asm_call_reg_max`＋SysV slot/n_stack 分类器同源；fwd8 `[rsp+0x20…0x38]` 无 push。三端 L2 5/5；Win host tramp；tip thin 装入仍 option CG002／si SEGV。`389153082d170108`／`181f5b58c5a40862`／`fb6e96853858588f`。没有跑 L4。 |
| 2026-09-26 | w1044 | tip thin 装入：地图＝`$0x30` 帧却写 `-0x40..-0x80`（f32 默认路径无 elide）。落地 f32／legacy VAR-home elide＋禁 ARRAY/SLICE；cold_twin 同步。Ubuntu／Darwin tip thin L2 5/5；Win host tramp。`cbcca23c0619b674`／`0250214638644ea4`／`822b6ae31c979d3a`。没有跑 L4。 |
| 2026-09-26 | w1043 | tip `$0x48`＝push rbx＋compute 56→64 pad。落地：frame≤48 去 rbx（pad≡0）＋param-only 封顶 48；epilogue 跟 flag。tip emit_call `sub $0x30`＝host。Win `5888886250a8e0b6`；三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1042 | tip 帧：`$0xf8`＝pad64+trailer64＋全 VAR 仍按 spill 计价。落地 spill&lt;256 去 pad/trailer；call_spill 不计 EXPR_VAR（对齐 call_dispatch home elide）。tip emit_call `$0xf8`→`$0x48`（host `$0x30`）。hybrid 仍红；产品 host-cc。Win `c7be9e9c0eb83ec2`；三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1041 | tip 帧：egg call_spill 仍 `*(n+1)*32`（w157 thin HARD BAN）。落地 host-cc overlay `*8`＋g05 先于 Linux `spill.o`；权威／thin／cold 同步；call_dispatch 单 GP EXPR_VAR 复用 home。tip 叶 `$0x28`→`$0x18`，emit_call `$0x188`→`$0xf8`。hybrid 仍红；产品 host-cc。Win `2cb72462ea201569`；三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1040 | tip 帧根修：单 GP `glue_sysv_spill` 步进 32→8；叶去 `+64`；小 spill pad 256→64；w157 `(n+1)*16`。tip 叶 `$0x68`→`$0x28`，emit_call `$0x248`→`$0x188`。cold twin `_impl`→heap `_impl`＋cold-full heap wrapper。hybrid 仍红；产品 host-cc。Win `b3c39b8fea6e377b`；三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1039 | tip call_dispatch 地图：w1033 后 tip thin 叶 `$0x68`、emit_call trampoline `$0x248`（host `$0x30`）。hybrid tip-FWD＋host-BODY：`f(7)` 绿、`f()` 编译器 SEGV；全 tip thin 仍 option CG002。产品仍 host-cc；ensure／tramp 注释固化。Win `b203e3476155148b`；三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1038 | PE `XLANG_WEAK` 空→seed_link_compat enc_label stub／asm stubs emit_call 强 T first-wins。落地 weaken 副本；权威单 T＋W。Win `b203e3476155148b`；三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1037 | egg 双 T 根收：`--demote-all-dual` 一次 Cap-band demote 559 符号（替 w1031–w1036 逐名表）。pabi_weak 0 双 T；Win `4647b851077938a5`；三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1036 | egg `glue_type_is_fixed_array`／`struct_lit_store_fixed_array`：`--prefer-cap-band` 保 Cap 带、demote 低 VA stub＋tip inject。产品单 T；Win `78325ebd15c86e11`；三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1035 | egg bake_string／bake_ptr 双 T：`keep_earliest` 扩 demote（Cap earliest）。产品单 T；Win `0ba7cfbb4b559a6d`；三端 L2 5/5。tip call_dispatch thin 试装 option CG002→仍 host-cc。没有跑 L4。 |
| 2026-09-26 | w1034 | egg typed／absolute64 多 T：`win_coff_keep_earliest` 扩 demote；有 wave743 sidecar 时 weaken egg typed。产品 typed／absolute64 单 T；Win `851eb6de3d76a456`；三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1033 | 有 call 仍强制 2048→`sub $0x858`。落地：spill&lt;1024 用 measured+256；≥1024 仍 2048 地板。单 call main `sub $0x178`；叶子仍 `$0x58`。Win `3b76b621f36f8da2`；三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1032 | tip `0x888`＝`compute_frame_size` 无条件 scratch≥2048。落地：`.x`／`windows_e` 仅 `call_spill>0` 时抬地板；host-cc overlay＋Win weaken／win_patch W→T。叶子 `sub $0x58`；有 call 的 main 仍 `sub $0x858`。Win `ecf699939640bec1`；Ubuntu／Darwin 重链；三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1031 | append_reloc 双 T 根修：Win egg 内 leftover Cap＋historic tip elf_ctx inject 两套 EXTERNAL；同 TU 已 call earliest。落地 `win_coff_keep_earliest_sym.py`（later→STATIC）入 `g05_relink_env` pabi_weak；`win_patch` dual-T 表清空。产品单 T（`51c0351c5a75aef6`）；`f()`／`id(42)`／`id(&x)`／`take("Hi")`＝Hi；三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1030 | enc_label 双 T 根修：tip 编 `backend_x86_64_enc_c.x` 仍 export（seed w927 已删）→ PE first-wins fat＋win_patch earliest→later。落地：`.x` 去掉 export（权威仅 `backend_enc_dispatch_thin.x`）；`win_patch` 表删 enc_label；Win `objcopy -N` 清旧 `.o`＋g05 relink。产品 4795534（`0052fc2f46f2b0e6`）单 T；`f()`／`id(42)`／`id(&x)`／`take("Hi")`＝Hi；三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1029 | `take(*u8)`／`fmt.println(s)`：无 `println(*u8)` 时 typeck first_idx 误绑 `u8[]`→`println_u8_slc`（*u8 当 fat slice）→ Linux 空 stdout／Win SEGV。落地 `std/fmt/mod.x`＋`runtime_asm_io_stubs` `println_u8_ptr`／`print_u8_ptr`（同 commit）。三端 `take("Hi")`／`id("Hi")`＝Hi；L2 5/5。产品未重链。没有跑 L4。 |
| 2026-09-26 | w1028 | 地图：tip thin `0x888`＝`compute_frame_size` 2048 scratch 地板（非 call 面根因）；host-cc thin 已正确 `call _impl`。默认关 call-surface fat→`_impl` jmp（`XLANG_WIN_FORCE_CALL_PATCH=1` 逃生）；enc_label 双 T 仍开。产品 4795436（`f0a189a54396489c`）；`f()`／`id(42)`／`id(&x)`；三端 L2 5/5。`take("Hi")` 仍 exit 127（park）。没有跑 L4。 |
| 2026-09-26 | w1027 | Win call_dispatch 薄层 first-wins：地图＝prefer 先 full.x→tip fat；tip pure-asm thin.x 仍肥帧；Win `-E` SEGV。落地 `seeds/backend_call_dispatch_win_thin_trampolines.c`＋ensure `ensure_win_call_dispatch_host_thin`（host-cc trampoline＋THIN_FROM_X `_impl`）；禁 tip 编 thin／full、禁 bak。win_patch 调用面改安全网（`XLANG_WIN_SKIP_CALL_PATCH` 探针闸）。产品 tip 4795436（`c2fe425cf3c0ae41`）；`f()`／`id(42)`／`id(&x)`；三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1026 | Win tip 调用面根修：tip fat `emit_call*`／`jmp_skip`／`enc_call`／`build_call_export_sym`→`_impl`；`enc_label`／`append_reloc` earliest→later Cap twin。根因＝PE first-wins tip fat 砸 Win64 多参＋第 2 函数 Value=0＋reloc 名截成 `t`。`win_patch` 收口；禁 bak 175735。产品 tip 4797967（`cff06ea0cfe437c0`）；`f()`／`id(42)`／`take("Hi")`／`id(&x)`／`copy_mod=10`；L2 5/5。Darwin／Ubuntu 产品未重链，L2 仍绿。没有跑 L4。 |
| 2026-09-26 | w1025 | Win tip 调用面地图：`f()`／`id(*u8)`／`id("Hi")`／`id(&x)` CG002；`id(42)`／`let s="Hi"` 绿。for_call_args tip≡pre；fat trampoline 补丁无效；bak 175735 假绿。产品 pre 4683263；tip 侧 `xlang_tip_w1025.exe` 4797967。三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1024b | Win L2 假红：产品读无扩展名 `xlang_asm`（旧 tip 链）而 `xlang_asm.exe` 绿。g05 强制同步 `xlang_asm`＋`.exe`；`win_patch` let_init 多 T 无 W 亦 fold。tip 栈侧证 `copy_mod=10`；03:38 egg／call_dispatch 砸 `println("…")`→产品暂 pre 4683263。三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1024 | 模块 VAR→本地固定数组：store 无栈槽返回 -2，let_init 非 0→CG002／Ubuntu SEGV。tip `fixed_array_let_init_module_var`（store 后 COMMON lea＋bulk copy）；Cap residual from_x 同补。`let a=g` 嵌套／flat 和＝10；嵌套 ARRAY_LIT 回归。L2 5/5。Darwin 4074360（`52d3f3b506fcba98`）。没有跑 L4。 |
| 2026-09-26 | w1023 | Darwin 本地嵌套 CG002：Cap residual mangled `vector_let_init` 对嵌套 ARRAY_LIT 直接 -1；store 调 stubdead。tip `vector_let_init_nested`（nested→`array_lit_flat`；短名＋mangled＋stubdead）；Cap residual stubs 同补。三端本地 i8／i32 和＝10。`let a=g` 仍 CG002（park）。三端 L2 5/5。Darwin 4074424（`e2889526a84da524`）。Ubuntu 5317896（`cf8df9028fe65fa0`）。Windows 4686810（`6fed878a23173878`）。没有跑 L4。 |
| 2026-09-26 | w1022 | `.data` 真打包核验齐；嵌套：本地存 dense、INDEX 行宽 Cap residual 8→和＝3。tip `fixed_array_total_bytes_true_pack`（named i8→1）。模块 `.data` `01020304`；Ubuntu／Win 本地和＝10；Darwin 本地 CG002（park）。三端 L2 5/5。Darwin 4074296（`9da1595979eec6a9`）。Ubuntu 5242568（`8ae54aa848c8aef2`）。Windows 4685706（`b469a325b26fb447`）。没有跑 L4。 |
| 2026-09-26 | w1021 | 邻域回归：Darwin／Win 13/13；Ubuntu 模块 `S { v: i8 }[2]` CG002——`bake_struct.o` 曾 park 为 `.off`、orphan 闸掉 bake_elems。g05 Linux：恢复 `.off`＋自建 host-gcc bake_elems。结构体和＝12；L2 5/5。Ubuntu 5242496（`510621e7ecbc7198`）。没有跑 L4。 |
| 2026-09-26 | w1020 | Win tip 非确定 CG002 根因：`bake_elems_thin.x` 编出的 tip 体 UB（host-gcc 孪生经同 weaken+jmp 16/16 稳；禁 jmp 时 tip 死码亦稳）。落地 `seeds/win_bake_elems_override.c`；force／elem_byte_sz 拆两 `.o`；g05 默认自建 tip 栈（`XLANG_WIN_BAKE_TIP=0` 关）。模块／本地 i8 和＝3；L2 5/5。Win 4684861（`ad0eb7a14c9ae7a8`）。没有跑 L4。 |
| 2026-09-26 | w1019 | Win tip 栈深入：INDEX seed 加 `XLANG_WIN_TRUE_PACK`（tip 编时去 Cap residual esz→4）；force 与 `elem_byte_sz` 同 `.o` 会砸 i32。全 tip 上线后同源非确定 CG002（Cap residual 稳 5/5）。产品默认仍 Cap residual；tip 仅 `XLANG_WIN_BAKE_TIP=1`。三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1018 | Win bake tip 摸底：`win_patch` extraT／W→T 改 earliest-VA（禁 tip→后链 jmp）；g05 全 tip 栈 opt-in `XLANG_WIN_BAKE_TIP=1`（需 bake+struct+index+emit+assign+force）；tip ON 时跳过 `src/win_index` 双权威。根因未收：`force_esz`／`elem_byte_sz` tip 与模块纯 lit 预约 span 打架（i32 `[1,2]` CG002；DIV／本地 lit 常绿）。默认仍 Cap residual。三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1017 | `[8 / 0, 1]`／`%`／INT_MIN/-1：folder 曾 loud-fail→CG002；改为折 0 入 `.data`（`00000000 00000001`，和＝1）。`[8/2,1]` 仍 5。运行时 `let x=8/0` 仍 panic。elem_const＋modlet twin；Linux modlet T001→elem_const tip first-wins；g05 闸：bake_elems 无 twin／modlet 不入链。三端 L2 5/5。Darwin 4074344（`df188def53115e1f`）。Ubuntu 5234192（`8afd32213b636fe1`）。Windows 4679616（`30d6be0d412cb27e`）。没有跑 L4。 |
| 2026-09-26 | w1016 | u16 真打包：named u16→2（force_esz／elem_byte_sz／INDEX／bake）；load zext16（ldrh／movzwl）。模块 `.data` `0100 0200`；`ffff 0100`→42；本地 `strh`+`ldrh`+#2；i8／i16 回归和＝3。Win Cap residual 和＝3。三端 L2 5/5。Darwin 4074344（`fdc523df0229eb39`）。Ubuntu 5283648（`b430f5643f53bfe0`）。Windows 4679104（`7f9dfcd4d30f90a2`）。没有跑 L4。 |
| 2026-09-26 | w1015 | i16 真打包：INDEX／force_esz／elem_byte_sz／bake tip named i16→2；emit sext16（movswl／LDRSH）。模块 `.data` `0100 0200`；本地 `strh`／`+2`＋`ldrsh`／movswl；负元和＝1；i8 回归和＝3。Win Cap residual 和＝3。三端 L2 5/5。Darwin 4074344（`b79b951be904d7de`）。Ubuntu 5283648（`9a687d8065fbc5d8`）。Windows 4679104（`7f9dfcd4d30f90a2`）。没有跑 L4。 |
| 2026-09-26 | w1014 | 本地 i8 ARRAY_LIT：根因 force_esz=0→`array_lit_elem_byte_sz` named i8 仍走 glue_type_size_simple→4。tip＋thin＋seed 同语义 i8→1；Darwin weaken 两符号。本地／模块和＝3。三端 L2 5/5。Darwin 4074344（`507ffacad53785ea`）。Ubuntu 5279552（`d9e3cf9569d30a1d`）。Windows Cap residual 4679104（`7f9dfcd4d30f90a2`）。没有跑 L4。 |
| 2026-09-26 | w1013 | Win PE：bake tip weaken+jmp 可达 `.data` `0102`；无 tip 时 extraT 曾互跳打坏 egg（已闸：仅有 W 才 jmp）。assign／emit／force_esz tip park。默认 Cap residual＋`_WIN32` INDEX i8→4；`XLANG_WIN_BAKE_TIP=1` 可选。三端 L2 5/5。没有跑 L4。 |
| 2026-09-26 | w1012 | 模块真 i8：bake esz=1（`.data` `0102`）、INDEX esz=1、sext8、assign tip。Darwin tip 绿；Ubuntu modlet+bake_elems+host-gcc tip（modlet 全编 T001→薄 bake）；Windows PE bake 同 TU → Cap residual park（`_WIN32` INDEX i8→4）。本地 lit park。三端 L2 5/5。Darwin 4074392（`9436ab4c251f2d0b`）。Ubuntu 5279552（`c00b7a8579e624a5`）。Windows 4679104（`7f9dfcd4d30f90a2`）。没有跑 L4。 |
| 2026-09-26 | w1011 | 前排核实：三端 `[(1 as i32), 2]` 和＝3、`.data` `01000000 02000000`；`[8 / 2, 1]` 和＝5、`.data` `04000000 01000000`；`[true, false]`／`[1 as u8, 2]` 已真打包 `0100`／`0102`。Win bool 曾误读 objdump 字面为四字节格，实为 `01 00`＋对齐。未改产品 tip。真 i8 打包仍 park（缺 sext8；named_builtin_cap／bake_elems 明示）。L2 探针三端绿。没有跑 L4。 |
| 2026-09-26 | w1010 | `5c8132b0a` Windows PE：host-gcc `body_sync_let_order`＋`emit_let_init`（BSS vn）first-wins；weaken **pabi_weak 副本**（不改 egg）；post-link `jmp` leftover W→T（含 emit_let_init）。根因链：mega 同 TU REL32 绑 leftover；tip emit_let_init 栈 vn 砸 C twin；tip `store_eax` 在 host-gcc float-lit 后返 -1（code_len=17）→ twin 内联 `mov [rbp],eax`。三端 L2 5/5；Win hoist2／hoist3=7。Win 4685695（`37fe6887f5f76c55`）。Darwin／Ubuntu 未重链产品，L2 仍绿。没有跑 L4。 |
| | 更早 | 见 [9 月 25 日归档](自举进度-归档-2026-09-25.md)。 |
