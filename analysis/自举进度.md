# Xlang / X language 自举进度

> **更新**：2026-08-23 · **Darwin 冷矩阵 CG002 ✅**（prefer libtool `.o` archive ≤2 员 `-force_load`）· **sat L4 身份（Ubuntu 金标）✅** @ tip `d1cc0d5dd` · fmt 冷启动 auto host-C／g05 stage2 ✅ · Darwin filter `-arch`＋ar ✅ · ALLOW ensure 再瘦库存空 ✅ · Darwin `__common` align 软警已闭 · fmt pure-asm format 残已闭 · **钉盘 `e8176cbe5`** · check-gate @ `889e5c33d` · 22 日已归档\
> **产品 L4 放行钉盘**：**`e8176cbe5`**（2026-08-23 升钉 · 前 `f7424ae47` 2026-08-15 · 再前 `e364f4a37` 2026-08-11）\
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅**（12 labi_*.x 切片 prefer；默认 FROM_X=1）· parser **7.2.2 关 pin ✅**（tip `-E` + assemble；产品默认 FROM_X=1）· codegen 7.4.2 wave323 ✅ · typeck 7.4.1 wave322 ✅ · runtime monofile wave321 ✅ · present **0** · 钉盘 **`e8176cbe5`** · Stage 8 tip **wave332 Track L 30/30 FULLY CLOSED**（**PRODUCT HALF=0** · **PRODUCT PINNED=0** · **PRODUCT RETIRED 23/23 = 100%**）· **7.4.3 PREFER_X_O 双端 8/8 wave338 ✅** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁**\
> **tip L4 安全网（历史）**：**`ffc0b646f`**（已由 tip L4 升钉吸收；前网 `4bacaf18c`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：**Darwin 冷矩阵 CG002 已闭**（`asm_asm_codegen_elf_o` 强符号回链；矩阵 **5／5**）。sat L4 Ubuntu 金标绿仍有效。钉盘仍 **`e8176cbe5`**（**不升钉**）。\
> **下波候选**：pipeline_abi mega **仍硬禁**／Darwin tip **真冷 L4** 再核（本波日常 L2）。ALLOW ensure 再瘦 **已空**。**nest 冻 64**。LINEAR／VECTOR suffix **非 sit-red**。

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1；过程上 prefer+heat 一次／域，双端矩阵同波一次\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；**SHARED 双端 L2**；日常矩阵 **只** `./xbuild l2-matrix`（wave299：捕获 rv=42／opt=102 成功码，禁 set -e 截断）；谈自举／升钉才 L4 + 矩阵 + 全量 bstrict；bstrict **per-script timeout=300s** + catalog cache（防无界卡死）；**墙钟**：`product_l4` 分步 wall + `run-all-bstrict` 每脚本 wall／top-N（`XLANG_BSTRICT_TIMING_FILE`）；并行 **`XLANG_BSTRICT_JOBS` 默认 1**（Ubuntu 可 opt-in 2–4；Darwin 慎用 OOM）

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[22](自举进度-归档-2026-08-22.md) · [21](自举进度-归档-2026-08-21.md) · [20](自举进度-归档-2026-08-20.md) · [19](自举进度-归档-2026-08-19.md) · [18](自举进度-归档-2026-08-18.md) · [17](自举进度-归档-2026-08-17.md) · [16](自举进度-归档-2026-08-16.md) · [15](自举进度-归档-2026-08-15.md) · [14](自举进度-归档-2026-08-14.md) · [13](自举进度-归档-2026-08-13.md) · [12](自举进度-归档-2026-08-12.md) · [11](自举进度-归档-2026-08-11.md) · [09](自举进度-归档-2026-08-09.md) · [08](自举进度-归档-2026-08-08.md) · [07](自举进度-归档-2026-08-07.md) · [06](自举进度-归档-2026-08-06.md) · [05](自举进度-归档-2026-08-05.md) · [04](自举进度-归档-2026-08-04.md) · [03](自举进度-归档-2026-08-03.md) · [02](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · …\

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `e8176cbe5`**（2026-08-23 升钉 · 前 `f7424ae47` 2026-08-15 · 再前 `e364f4a37` 2026-08-11）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**双端 Stage2 = ✅ SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709` · `xlang_asm_stage1`／`xlang_asm2` bit-identical · 黄金自举金标准 · strict-link 根修后双端首过）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（Makefile 已删除；**变更记录只在本文 §6**）。

| 指标 | 状态 |
|------|------|
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`e8176cbe5`**（L4 升钉冻结 · 双端 L4 真冷绿 · check-gate al06 入口 typeck）；residual tip **wave332** present **0**；Stage 8 tip **wave332**（**30/30 FULLY CLOSED**；**PRODUCT RETIRED 23/23=100%**；PRODUCT HALF=0；PRODUCT PINNED=0） |
| Track MG | **11.3.1 MG ✅**（wave941 物理删 Makefile · wave942 catalog CFLAGS · 0 make in bootstrap · 产品入口 `./xbuild`） |
| Track BC | **开 🟢（30/30 FULLY CLOSED wave332）**（B′ 至 **wave326** link_abi **7.3.1 关 pin** ✅ · M4 **5/5 全 ✅**（runtime/typeck/codegen/parser/link_abi 五域冷链全闭）· present **0**；**BC Stage8 Batch3 收**＝**30/30 FULLY CLOSED**（PRODUCT 23 RETIRED 100% catalog+ladder；NON-PRODUCT 7 正确分类 never-product-chain = TEST×2 / STAGE×2 / EXTRACT_ONLY×1 / DELETED_ORPHAN×2；G.7 无 HALF 无 unresolved PINNED）|
| Track L (Stage 8) | **收（Batch 3 30/30 FULLY CLOSED wave332）**（批次 0 清理 ✅ · 批次 2 **wave1036–1041** 退役 ✅ · 批次 3 **wave327–332**（lsp_diag+lsp+pipeline+driver+preprocess+lexer+parser/typeck/codegen/ast_gen2 10 项 + audit 分类误判 3 项 RETIRED 修正 + ast_gen.c 孤儿物理删 + ast_gen2 cold-seed pin + 分母口径统一 is_product_denominator）✅ · **30/30 FULLY CLOSED**；PRODUCT RETIRED 23/23=100%；HALF=0；PINNED 产品=0 · 详见 [阶段8高效退役分析.md](阶段8高效退役分析.md)） |
| mac / Ubuntu L4（钉盘） | ✅ **双端 L4 真冷 remaining_o=0 + 矩阵 + bstrict 129** @ **`e8176cbe5`**（mac wall=85m0s · Ubuntu rebuild 7m27s＋heat＋bstrict 4m10s · G.8 SHARED 双端同钉闭环）|
| pin 蛋 | ✅ `seeds/bootstrap_xlangc.darwin.arm64` + `linux.x86_64` 自本波 L4 产品 `xlang_asm` 刷新 |
| 本波构建级别 | 钉盘 **L4 已结**；日常续 **L2**（check 闸门暂停） |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录**（**8 月 23 日**） |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图（本波不改） |
| [自举进度-归档-2026-08-22.md](自举进度-归档-2026-08-22.md) | **8月22日** typeck pin spelling_eq ＋ x86_64 `orq` ＋ http STRUCT_LIT ＋ P014 ＋ csv T001 ＋ AAPCS64 第 7 参 ＋ cli `-o` ＋ fmt 静默失败（**8** 行） |
| [自举进度-归档-2026-08-21.md](自举进度-归档-2026-08-21.md) | **8月21日** leftover extra SLICE peels RET twin ＋ dest-PARAM／RET extras ＋ link-abi wave956–958 ＋ 双端 L4（**13** 行） |
| [自举进度-归档-2026-08-20.md](自举进度-归档-2026-08-20.md) | **8月20日** dest extras dest-RET／PTR-outer ＋ ld-only／prefer hybrid／inject ＋ host-C／w189／dyn 前缀（**47** 行） |
| [自举进度-归档-2026-08-19.md](自举进度-归档-2026-08-19.md) | **8月19日** F7 dest leftover 收口（dest-SLICE／dest-ARRAY／INDEX／host-C ret）＋ `[2]*[2]i32` skip-trait T001 |
| [自举进度-归档-2026-08-18.md](自举进度-归档-2026-08-18.md) | **8月18日** TYPE_DYN F1–F6 + F7 asm 开刀 + hello `af 02 _main`（**10** 行） |
| [自举进度-归档-2026-08-17.md](自举进度-归档-2026-08-17.md) | **8月17日** nest 30–64／dest leftover／4.2.x pin-seed |
| [自举进度-归档-2026-08-16.md](自举进度-归档-2026-08-16.md) | **8月16日** dest-in-rbx 族／匿名 match／裸 enum／SIMD dest／nest 21–29（**124** 行） |
| [自举进度-归档-2026-08-15.md](自举进度-归档-2026-08-15.md) | **8月15日** 过夜 Darwin 刀 + 升钉 `f7424ae47` + dest／SIMD／METHOD／nest 至 DEREF dest（**66** 行） |
| [自举进度-归档-2026-08-14.md](自举进度-归档-2026-08-14.md) | **8月14日** dest-SLICE／import 模块 const／INDEX-on-METHOD／f32／Vec4f／宽 lit／STRUCT_LIT／http+queue companions（**79** 行） |
| [自举进度-归档-2026-08-13.md](自举进度-归档-2026-08-13.md) | **8月13日** METHOD(49) 物化／try_inline／>16B memcpy + SIMD leftover + soft residual 梯（**66** 行） |
| [自举进度-归档-2026-08-12.md](自举进度-归档-2026-08-12.md) | **8月12日** Stage12.0.5 prefer 默认 pure-asm · PC FORBID／ALLOW · mega ban／RCA · opaque WEAK · catalog cache · L4 墙钟 · **47** 行 |
| [自举进度-归档-2026-08-11.md](自举进度-归档-2026-08-11.md) | **8月11日** Stage12.0.5 pure-asm residual 闭口 + **升钉 `e364f4a37`**（pure_asm 13/13 · L2-asm／tcpu／ldpc · labi residual · full12 双端地图 · **41** 行 + 10 日余 **4**） |
| [自举进度-归档-2026-08-09.md](自举进度-归档-2026-08-09.md) | **8月9日** runtime thin dual leave → M4 五域关 pin → Stage 8 Batch 3 30/30 → PREFER_X_O 双端 8/8（**28** 行 · 钉盘 `36363b90f`） |
| [自举进度-归档-2026-08-08.md](自举进度-归档-2026-08-08.md) | **8月8日** B′ pipeline_x mega 首刀 bootstrap_glue→ast_pool 各域→elf/wpo→8.3 结构地板→gen dual leave→typeck.x 全量 `-E` T001（**wave262–314** · **56** 行） |
| [自举进度-归档-2026-08-07.md](自举进度-归档-2026-08-07.md) | **8月7日** B′ pure leave wave200–261 + 升钉 `36363b90f` + glue_statics（**64** 行） |
| [自举进度-归档-2026-08-06.md](自举进度-归档-2026-08-06.md) | **8月6日** B′ pure leave field_access→wave199 CALL-target resolve（**50** 行） |
| [自举进度-归档-2026-08-05.md](自举进度-归档-2026-08-05.md) | **8月5日** L4 冷恢复 + 8.3.3 R2 + pure-asm match + B′ leave 至 binop（**74** 行） |
| [自举进度-归档-2026-08-04.md](自举进度-归档-2026-08-04.md) | **8月4日 wave1241–1291**（BC ast_pool 16 波域分解 + glue 壳 thin + check 收口 + 升钉 `db809e00f`） |
| [自举进度-归档-2026-08-03.md](自举进度-归档-2026-08-03.md) | **8月3日 wave1227–1240**（BC glue 叶收口 + check 工具链启动 · 15 行） |
| [自举进度-归档-2026-08-02.md](自举进度-归档-2026-08-02.md) | **8月2日 wave1130–1220**（BC glue 续迁 + typeck 跨模块字段根修 · 43 行） |
| [自举进度-归档-2026-08-01.md](自举进度-归档-2026-08-01.md) | **8月1日 wave964–1111-1129**（BC 8.3.1 G.7 fold 50+ 叶 + Stage 8 退役 13/30） |
| [自举进度-归档-2026-07-31.md](自举进度-归档-2026-07-31.md) | **31 日全日 wave895–963**（MG 真删 + post-delete + 开 BC） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单；per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`e8176cbe5`** | 2026-08-23 升钉；双端 L4 真冷 + 129（mac 85m0s · Ubuntu rebuild 7m27s＋heat＋bstrict 4m10s）；含 check-gate al06 入口 typeck；前 `f7424ae47` |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自本波 L4 `xlang_asm` 刷新（darwin 4.7MiB · linux 4.6MiB） |
| residual tip（BC） | wave339 | M4 **5/5** 全闭 · Stage 8 Batch 3 **30/30 FULLY CLOSED** · 7.4.3 PREFER_X_O **双端 8/8** · pure-ld 58/60 objs · **双端 L4** @ **`e8176cbe5`** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁** · sat `try-r1` 已走 `ensure_labi_prefer_one`（产品仍 mega） |
| mac 钉盘 L4 | ✅ L4 + bstrict **129** · 85m0s | remaining_o=0 · `./xbuild l4` · `/tmp/xlang_l4_Darwin_e8176cbe5.log` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 remaining_o=0＋heat try_inline＋矩阵 5／5＋bstrict **129** · rebuild 7m27s＋bstrict 4m10s | `ubuntu-remote-server` · `/tmp/xlang_l4_Linux_e8176cbe5.log` · `/tmp/xlang_bstrict_Linux_e8176cbe5.log` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | G.7 单权威 `product_l2_matrix.sh`（wave299）；rv42／opt102／hello0／si0／f32 一次跑全 |
| 前钉 | `f7424ae47` · `e364f4a37` · `d79a368b2` · … | 过夜 Darwin 刀 → Stage12.0.5 residual → formal_mod → … |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 18 号结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ **双端 SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709`） | `stage2-hash-gate OK (SHA256 match — 黄金自举金标准)` · `xlang_asm_stage1`／`xlang_asm2` 行为一致（42 + hello）· strict-link 根修后双端首过 · 日志 `/tmp/xlang_stage2_ubuntu_9e60e1290.log` + `/tmp/xlang_stage2_macos_fix2.log` |
| **R2 / Cap** | wave710 升钉后按需 | 明细见 29 归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1**（已执行） |
| `TREE_ARMED` | **1** |
| `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1**（wave941 物理删除） |
| `PHYS_DEL_WINDOWS_GATE_STATUS` | `reproven_green` @ `71dd60a12`（wave922 · wave940 一键冷启动） |
| preflight blockers | **全清零**（wave937–939） |
| 真删条件 | ✅ 已完成 @ wave941/942 · 钉盘 **`77b334842`**（后升至 **`db809e00f`**） |
| post-delete residual | **✅ 已收**（wave944–964） |

***

## 3. 当前前排（≤3 条）

1. **已升钉 `e8176cbe5`** — 双端 L4 remaining_o=0＋矩阵 5／5＋bstrict **129**（al06 T001）。Ubuntu 冷矩阵 option／si BUILD **139** 仍须 heat try_inline **try-r3-cold**（旧 pin 蛋 `orl`；新蛋已刷，下一次冷测应不再需要）。23 日已闭 unique UNDEF／labi／stubs／al06 假绿勿重开。
2. **邻域** — F7 wrapper 形参已走 rdi＝data（勿回头改）。**禁**空 struct 糊绿／mega prefer／默认 assemble parser.x。dyn SSE extras **禁**放 ta==1。**禁**用 `mov_rax_to_rbx` 当 wrapper 暂存（兼写 x1）。**禁** `pipeline_module_num_funcs` 判空糊绿。**禁** shell `#define Result_*` 双权威。**禁**把 `run` 收成 IDENT 糊绿（与一元 `run <expr>` 撞 token）。**禁**改 enc_store sz>=16 全局顶 leftover fsz=8。**禁**伪造 mmap backend 1／2。**禁** `extern uint8_t *malloc`（撞 libc `void *malloc`）。**禁**改 al06 期望／禁把 check 整棵 std／core dep typeck。**禁** `-w` 糊 Darwin minos 警告／**禁**把 `macho.x` minos 抬到 26.0。
3. **PC 底盘** — prefer 族 pure-asm 默认 ✅ · **sat L4 身份 Ubuntu ✅**（`d1cc0d5dd`；PREFER=0 不盖 prefer）· **fmt 冷 egg auto host-C／g05 stage2 ✅** · **Darwin filter `-arch`＋libtool ar ✅** · **Darwin 冷矩阵 CG002 ✅**（prefer archive `-force_load`）· Darwin 产品 `-o` ld-only ✅ · Linux hosted 产品 `-o` ld-only ✅ · Darwin labi prefer hybrid ✅ · Ubuntu pipeline_abi thin inject first-wins ✅ · **ALLOW ensure 再瘦库存空 ✅** · pipeline_abi mega **仍硬禁**。nest 64 **冻帽**。未混 L2 sit-red 产品残叶空；续作点名 mega／Darwin tip L4。

***

## 4. 与归档的关系

- **8月22日归档**：[自举进度-归档-2026-08-22.md](自举进度-归档-2026-08-22.md) — typeck pin spelling_eq ＋ x86_64 `orl`→`orq` ＋ http STRUCT_LIT Result tag ＋ parser P014 ＋ csv 门面 T001 ＋ AAPCS64 第 7 参 ＋ formal_mod std.cli `-o` ＋ fmt 静默失败 · **8** 行 · 钉盘仍 **`f7424ae47`**。
- **8月21日归档**：[自举进度-归档-2026-08-21.md](自举进度-归档-2026-08-21.md) — leftover extra SLICE peels RET twin ＋ dest-PARAM／RET extras ndims＝0／−2 ＋ link-abi wave956–958 ＋ 双端 L4 · **13** 行 · 钉盘仍 **`f7424ae47`**。
- **8月20日归档**：[自举进度-归档-2026-08-20.md](自举进度-归档-2026-08-20.md) — dest extras dest-RET／PTR-outer 族 ＋ Darwin／Linux `-o` ld-only ＋ prefer hybrid／inject ＋ host-C／w189／dyn 前缀 · **47** 行 · 钉盘仍 **`f7424ae47`**。
- **8月19日归档**：[自举进度-归档-2026-08-19.md](自举进度-归档-2026-08-19.md) — F7 dest leftover 收口＋`[2]*[2]i32` skip-trait T001 + dest-stamp · 钉盘仍 **`f7424ae47`**。
- **8月18日归档**：[自举进度-归档-2026-08-18.md](自举进度-归档-2026-08-18.md) — TYPE_DYN／vtable **F1–F6** + F7 asm 开刀 + hello `af 02 _main` store 拷贝 · **10** 行 · 钉盘仍 **`f7424ae47`**。
- **8月17日及前**：见各日归档链接（§1 文档地图）。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `e8176cbe5`**（2026-08-23 升钉 · 前 `f7424ae47` · 再前 `e364f4a37`）；residual tip **wave339**（present **0** · 日常 L2 可续）。
- 日切后主文档只保留 KPI + 前排 + **8 月 23 日** §6（22 日及前见归档）；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
|------|----------|
| **macOS arm64** | **钉盘 L4+bstrict 129 ✅** @ **`e8176cbe5`** · tip 矩阵 CG002 **已闭**（soft g05＋`l2-matrix` **5／5**；真冷 L4 另核）· 产品只走 `./xbuild` |
| **Ubuntu x86_64（金标）** | **tip sat L4 ✅** @ **`d1cc0d5dd`** remaining_o=0＋矩阵 5／5＋bstrict **129**＋fmt／wrap **0** · wall **11m48s** · 钉盘仍 **`e8176cbe5`** · **SSH＝`ubuntu-remote-server`** |
| **Windows MSYS2** | wave940 一键冷启动绿 · STATUS=`reproven_green` · Makefile 已物理删除 · 本波未升 Windows pin（仓内无 windows pin 蛋） |

***

## 6. 变更记录（倒序 · **8 月 23 日**；22 日见 [归档](自举进度-归档-2026-08-22.md) · 21 日见 [归档](自举进度-归档-2026-08-21.md) · 20 日见 [归档](自举进度-归档-2026-08-20.md)）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-08-23 | **fix(darwin)** **冷矩阵 CG002 code_len=0（prefer libtool archive `-force_load`）** | **代码+文档** · **不升钉** · **日常 L2**。五问：产生＝`pure_ld_partial_merge` Darwin `ld -r` 失败后 `libtool -static` 把 thin＋rest 收成 **名 `.o` 的 archive**；存储＝g05／cold `USER_ASM` 袋（bridge／compat／x86_enc filtered 等 ≤2 员）；消费＝Apple ld：真 MH_OBJECT 里 `asm_experimental_symbol_bridge` 的 **弱** `asm_asm_codegen_elf_o` 已定义 → archive 强体永不拉入 → tip 产品只剩弱 stub → mega_body 未进 → **CG002 `code_len=0`**（pin 蛋仍强符号绿；passthru／filter 对照非根因）。**修**：G.7 有则补全 `pure_ld_try_link` → `pure_ld_darwin_force_load_prefer_archives`（≤2 员 prefer archive `-force_load`；labi／pabi／driver_no_c 多员 first-wins **不** force）。禁删 experimental 弱桩／禁 force 多员 archive／禁 mega／升钉。证：soft g05 见 force×9 · `asm_asm_codegen_elf_o` **external** · rv **42** · 矩阵 **5／5**（`/tmp/cg002_probe/mac_matrix_force_load.log`）。Linux 恒等 no-op。钉盘仍 **`e8176cbe5`**。下波：mega 硬禁／Darwin tip 真冷 L4。PLATFORM: MACOS pure-ld／SHARED 脚本。 |
| 2026-08-23 | **fix+docs(selfhost)** **sat L4 身份核验（Ubuntu 金标）** | **代码+文档** · **不升钉** · **构建级别=L4**。点名 sat L4：Ubuntu `./xbuild l4` @ tip **`d1cc0d5dd`** remaining_o=0＋矩阵 **5／5**＋bstrict **129**＋fmt／wrap **0**（wall **11m48s**；`/tmp/xlang_l4_Linux_d1cc0d5dd.log`）。PREFER=0 sat 仍 **prefer multi-slice labi**／pipeline thin+rest（未盖 mega）。活身份 labi **480496**／pabi **1746928**。根修：①`pure_asm_x_to_o` 无 `lea_rax` 则 fmt auto host-C＋g05 stage2 FORCE fmt（pin 蛋冷编假绿）②Darwin `filter_o` `-arch`＋libtool ar 成员过滤（bootstrap pure-ld 解阻）。Darwin 冷矩阵仍 **CG002 code_len=0**（passthru 对照仍红；另叶）。禁升钉／禁 mega。钉盘仍 **`e8176cbe5`**。下波：Darwin CG002／mega 硬禁。PLATFORM: SHARED fmt／MACOS filter／LINUX gold L4。 |
| 2026-08-23 | **docs(selfhost)** **ALLOW ensure 再瘦库存空（无 path-nonempty always-push sit-red）** | **文档** · **不升钉** · 无产品代码。五问：候选「ALLOW ensure 再瘦」曾指 heap path-nonempty always-push；Stage 12.2.3 已把 heap 改 `invoke_cc_append_heap_f06_ondemand`（nm＋needles）；ensure-push front／mid／heavy 均 `need_flags` 门控。本波再扫 `labi_invoke_cc_list.x`：无剩余「path 非空即推」sit-red；LINEAR／VECTOR host-C suffix 非 sit-red（`i32x4`＝TYPE_NAMED 双端 7）。g05 **0** soft warn · hello **0**。禁搅 pipeline_abi mega／sat L4（须点名）／升钉。钉盘仍 **`e8176cbe5`**。下波：mega 硬禁／sat L4 须点名。PLATFORM: SHARED 文档。 |
| 2026-08-23 | **fix(darwin)** **Mach-O COMMON `n_desc` GET_COMM_ALIGN（fmt 4MiB `__common` align 软警）** | **代码** · **Darwin L2 闭**（Ubuntu 同波 SHARED seed 孪；ELF 路径不变）。五问：产生＝`pipeline_macho_write_o_to_buf_c` COMMON 只写 `N_UNDF\|N_EXT`＋`n_value=size`，`n_desc` 对齐位＝0；Apple ld 对大 COMMON 按 size 推 `__DATA,__common` 段对齐到 **0x8000**（段上限 0x4000）→ 每次 g05／产品链警告。存储＝`g_pipe_elf_sym_common_align` 已有（ELF `st_value` 用；Mach-O 漏写）。消费＝pure-asm fmt `g_fmt_file_list_paths` 4MiB COMMON 入链。**修**：G.7 有则补全 Mach-O COMMON 写 `n_desc` GET_COMM_ALIGN＝log2（floor 8／cap 2^14）；seed cold 孪同 commit；禁 `-w`／禁改 COMMON→`.data` 烤 4MiB／禁搅 pipeline_abi mega／升钉。证：g05 自链 **0** common align · thin `n_desc=0x400`（log2＝4）· fmt dirty `--check`＝**1**／write＝**0**／clean＝**0** · lib_tu **42**／dyn **7** · 矩阵 **5／5**。heat：FORCE pipeline_abi＋fmt（须用新 `xlang_asm` 再编 fmt）＋g05。钉盘仍 **`e8176cbe5`**。PLATFORM: MACOS macho COMMON／SHARED seed。 |
| 2026-08-23 | **fix(asm)** **fmt pure-asm format 残（`modlet_load_to_rax` lea→rax）** | **代码** · **Darwin L2 闭**（Ubuntu 同波 SHARED）。五问：产生＝`pipeline_asm_modlet_load_to_rax` lea 进 rbx／x1 再 ldr；cmp 左侧停在 rbx／x1 → `i >= g_fmt_file_list_n[0]` 变成 `(&n) >= n` 恒真 → `fmt_file_list_at` 总 NULL → `driver_fmt_one_file` 从未调用（针已烤仍 `--check`／write no-op）。存储＝modlet COMMON／`.data` 标号。消费＝pure-asm 编 `fmt_check_cmd_thin`。**修**：G.7 有则补全 load_to_rax 走 lea_rax＋`ldr x0,[x0]`／`movq (%rax),%rax`（array decay 只 lea）；store 仍 lea_rbx；seed cold 孪同 commit；撤 `XLANG_FMT_ALLOW_PURE_ASM` skip（产品默认 pure-asm；`XLANG_FMT_DENY_PURE_ASM=1` 回落）。禁 path_should_ignore 空针／禁搅 dest extras／pipeline_abi mega／升钉。证：产品 prefer pure hybrid · dirty `--check`＝**1**／write＝**0**／clean＝**0** · at() 反汇编 `adrp x0`＋`cmp x1,x0` · one_file＋format 命中 · 邻域 lib_tu **42**／dyn **7** · 矩阵 **5／5**。soft：pure-asm fmt 入链时 Darwin `__common` align 软警（4MiB paths COMMON；**本波已闭**）。heat：FORCE pipeline_abi＋fmt_check_cmd_driver＋g05。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED modlet load／MACOS g05。 |
| 2026-08-23 | **fix(darwin)** **g05 `-multiply_defined` obsolete ＋ `__DATA,__common` 对齐** | **代码** · **Darwin L2 闭**（Ubuntu 同波 SHARED seed）。五问：产生＝①`pure_ld_multidef_flags`／`g05_relink_env` 仍传废弃 `-multiply_defined suppress`；②`rt_arena_buf` tentative COMMON `driver_arena_static[128MiB]` 使终链 `__DATA,__common` 段对齐要 0x8000（段上限 0x4000）。存储＝G05_CFLAGS／pure-ld argv＋seed BSS。消费＝每次 g05 pure-ld 两警告。**修**：G.7 有则补全 Darwin multidef→空（同 experimental；Linux 仍 `--allow-multiple-definition`）＋`driver_arena_static`／`driver_module_static`／`g_lsp_state_buf` `={0}` 强制 defined zerofill。禁 `-w`／禁抬 macho／禁搅 fmt／dest extras／pipeline_abi mega／升钉。证：g05 自链 **0** multiply_defined／**0** common align · 矩阵 **5／5** · hello **0**。heat：`rm src/runtime/rt_arena_buf.o`＋`lsp_diag_pipeline_ctx.o`＋g05。钉盘仍 **`e8176cbe5`**。PLATFORM: MACOS g05／SHARED seed zerofill。 |
| 2026-08-23 | **fix(asm)** **库 TU `.data`（非空 ARRAY_LIT 烤进 `.data`／`__DATA,__data`）** | **代码** · **双端 L2 闭**。五问：产生＝模块非空 ARRAY_LIT 进 SHN_COMMON／`__common`（BSS 0）＋仅 hoist 入口 `seed_nonzero`；库 TU 无 main／不进第一体 → 针全 0。存储＝modlet cell bit29＝`.data` 已烤；F7 `g_pipe_elf_data_buf`。消费＝LEA 同名标号。**修**：G.7 有则补全 `pipeline_asm_modlet_prepare_and_emit_elf_c`（ne>0 且 ≤64KiB → bake；空 `[]` 仍 COMMON）＋Mach-O 段名 `__const`→`__data`（可变计数器可写；禁 RO SIGBUS）＋seed 孪同 commit；`seed_nonzero` 跳过 bit29。fmt skip 仍留（pure-asm format 残另刀；`XLANG_FMT_ALLOW_PURE_ASM=1`）。禁搅 g05 multiply_defined／dest extras／pipeline_abi mega。证：双端 `lib_tu_modlet_data.x` asm／host-C **42** · Darwin `.o` `__DATA,__data`／Ubuntu `.data`＝`ABC\\0`＋`10,32` · 邻域 const_module_nonmain／mut_slice **42** · dyn_call_ok **7** · fmt host-C dirty **1**／write **0**／clean **0** · 双端矩阵 **5／5**。heat：FORCE `runtime_pipeline_abi.o`。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED modlet／MACOS `__DATA,__data`。 |
| 2026-08-23 | **fix(typeck／asm)** **dest extras dest-PTR stamp（嵌套 extra lit CG002→7）** | **代码** · **双端 L2 闭**。五问：产生＝TYPE_DYN extras 缺 dyn_pk＝9 PTR-to-ARRAY 重建＋ADDR_OF 不认 ARRAY_LIT；存储＝param_elem_*／elem_array unused slots；消费＝coerce＋`pipeline_asm_emit_addr_of_elf_c`。**修**：G.7 有则补全 PARAM 孪 dest-RET PTR-to-ARRAY extra empty `[]`（剥 ADDR_OF 后 coerce 操作数）＋ADDR_OF kind 46→`pipeline_asm_emit_array_lit_elf_c`（`.x`／seed／thin first-wins 同 commit）。禁 −3／第二套 dest-PTR stamp／搅 dest-SLICE wrap of ARRAY_LIT／asm `.data`／g05 multiply_defined。证：双端 `dyn_add_ptr_arr_slice_lit.x` compile＝0 run＝**7** · Darwin `-E` fat `slice_int32_t (*)[2]` · 邻域 ptr_arr_slice／ptr／arr_slice **7** · 双端矩阵 **5／5**。heat：`rm typeck_x.o`＋`runtime_pipeline_abi.o`＋thin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED typeck／asm。 |
| 2026-08-23 | **fix(darwin)** **host-cc minos 11.0 对齐 macho.x（ld 26 vs 11）** | **代码** · **Darwin L2 闭**（Ubuntu 同波）。五问：产生＝host-cc 默认 SDK **26.0**；存储＝`macho.x` LC_BUILD_VERSION **11.0**；消费＝Apple ld 每用户 `-o`。**修**：G.7 有则补全 `xlang_cc_compile_sync_ex` ＋ catalog CFLAGS ＋ ensure／cc_inc_tu ＋ formal_mod CFLAGS／c_face cc ＋ fs_formal／string_o。禁 `-w`／禁抬 macho minos 到 26.0／禁搅 g05 `-multiply_defined`／`__DATA,__common`／禁搅 dest extras。证：hello `-o` 三行警告消 · 伴生／c_face／mod **minos 11.0** · 矩阵 **5／5** · 0 minos 警告。heat：`rm compiler/src/runtime_link_abi.o` ＋ 三件伴生。钉盘仍 **`e8176cbe5`**。PLATFORM: MACOS host-cc／SHARED 脚本 Darwin 袋。 |
| 2026-08-23 | **docs+pin** **🔔 升钉 `f7424ae47` → `e8176cbe5`** | **真冷** · **钉盘 `e8176cbe5`**。双端 `./xbuild l4` remaining_o=0 · 矩阵 **5／5** · bstrict **129**（al06 T001）。Ubuntu 冷矩阵 option／si BUILD **139** → heat try_inline **try-r3-cold** 后 5／5。mac wall **85m0s**（bootstrap 214s／g05 93s／矩阵 67s／bstrict 4726s）· Ubuntu rebuild **7m27s**＋bstrict **4m10s**。pin 蛋自本波 `xlang_asm` 刷新（darwin 4.7MiB／linux 4.6MiB）。日志 `/tmp/xlang_l4_Darwin_e8176cbe5.log` · `/tmp/xlang_l4_Linux_e8176cbe5.log` · `/tmp/xlang_bstrict_Linux_e8176cbe5.log`。禁搅 dest extras／禁改 al06。下一刀：Darwin ld minos 26 vs 11。PLATFORM: SHARED 升钉 · **构建级别=L4**。 |
| 2026-08-23 | **fix(check)** **check-gate 入口 typeck（al06 假绿）** · tip **`889e5c33d`** | **代码** · **双端 L2 闭**。五问：产生＝`rt_cp_step_pipeline` 遇 check＋std／core 闭包 `print_ok`＋return 2；存储＝`.x`＋seed／surface；消费＝al06 grep。**修**：G.7 有则补全入口仍 pipeline typeck；dep 仍 parse-only；diag_emitted 失败。禁改 al06 期望／禁搅 dest extras。证：双端 L2 **5／5** · al06 **OK** · Darwin `run-check` **OK**。heat：`rm compiler/src/runtime_driver_no_c.o`。钉盘仍 **`f7424ae47`**。下一刀：双端 L4＋升钉。PLATFORM: SHARED check orch。 |
| 2026-08-23 | **test(selfhost)** **tip 双端 L4 未升钉** · tip **`67a343feb`** | **真冷** · **钉盘仍 `f7424ae47`**。`./xbuild l4` remaining_o=0 · seed pin · bootstrap-driver-seed · g05 --sync · 矩阵 **5／5** · **不跑 check 闸门**。bstrict **FAIL** `run-al06-gate.sh`：`xlang check` 在 `XLANG_NO_C_FRONTEND`＋std／core 闭包 dep 上 quiet OK（`driver_print_check_ok` return 0）；产品 `-o` 仍 **T001 allocator region escape**。Ubuntu 冷矩阵 option／si BUILD **139** → heat try_inline **try-r3-cold** 后 5／5。mac wall **77m14s**（bootstrap 217s／g05 92s／矩阵 57s／bstrict 4268s）· Ubuntu rebuild **7m28s**＋bstrict **4m7s**。日志 `/tmp/xlang_l4_Darwin_67a343feb.log` · `/tmp/xlang_l4_Linux_67a343feb.log`。禁改 al06 期望／禁搅 dest extras。下一刀：check-gate。PLATFORM: SHARED 升钉未过 · **构建级别=L4**。 |
| 2026-08-23 | **fix(link-abi)** **std.async formal_mod c_face＋unique 针（async_mod_import／drain_idle BLD001）** · tip **`07055c8cb`／`100ca10ac`** | **代码** · **双端 L2 闭**。五问：产生＝无 `async.o`（std_x 仅 scheduler.o／future.o auto-soft）；scheduler C ABI 针 ×35 从不 unique import METHOD；整份 `mod.x` host-cc 会 U ~50 C ABI。存储＝L8b seed＋list.x／heavy twin ＋ formal_mod c_face。消费＝user ld UNDEF。**修**：G.7 无才新增 catalog c_face（placeholder＝0；drain_idle 转调 `xlang_async_run_drain_until_idle`）＋ unique 针 2 ＋ ensure glue；Ubuntu 全链再暴露 glue 硬 extern `task_echo_fn_c`，有则补全既有 XLANG_WEAK 可选族（禁拉 task.o＋context＋atomic）。禁把 unique 名塞进 scheduler C ABI 表／禁 host-cc 整份 mod.x／禁搅 check／dest extras。证：双端 L2 **5／5** · async BUILD／RUN **0** · mac `bl placeholder`／`drain_idle` · Ubuntu `call placeholder`／`drain_idle` · sqlite／datetime／net／cli／csv **0** · db_kv_arrow Darwin RUN **134**／Ubuntu **0**。heat：`rm compiler/src/runtime_link_abi.o` ＋ `rm compiler/runtime_scheduler_glue.o`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi／formal_mod／glue。 |
| 2026-08-23 | **fix(link-abi)** **std.db.kv／arrow formal_mod＋unique 针（db_kv_arrow BLD001）** · tip **`ea0f37a03`** | **代码** · **双端 L2 闭**。五问：产生＝std_x auto-soft 裸 `kv.x`／`arrow.x` → T `db_kv_*`／`arrow_*`，import 要 `std_db_kv_*`／`std_db_arrow_*`；针只有 C ABI，matcher 精确从不开火；skip-missing 从不 ensure。存储＝L8b seed＋list.x／heavy twin ＋ formal_mod。消费＝user ld UNDEF。**修**：G.7 有则补全 formal_mod `mod 1 mod.x kv.x`／`arrow.x`（≡ sqlite）＋迁出 std_x 19→17 ＋ nested leaf `db_kv`／`db_arrow` ＋ unique 针 2→14／2→29 ＋ glue ensure ＋ simd c_face C ABI 别名 ＋混 arity `open`／`sync` 跳过 file-wide `#define`。禁第二套 simple-group／禁搅 check／async／dest extras。证：双端 L2 **5／5** · db_kv_arrow BUILD **0** · mac `bl mmap_available`／Ubuntu `call mmap_available` · Darwin RUN **134**（真 LSM mmap）· Ubuntu RUN **0** · sqlite／datetime／net／cli／csv **0** · io_read_chunk 成功分 **104** · zc_arena_concat 成功分 **8**。heat：`rm compiler/src/runtime_link_abi.o`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi／formal_mod。 |
| 2026-08-23 | **fix(link-abi)** **std.net need_net ensure（net_listen_bind／resolve BLD001）** · tip **`4aac5f13f`／`b1e2d6d53`** | **代码** · **双端 L2 闭**。五问：产生＝针已含 `std_net_listen`／`resolve_ex`，push_obj skip-missing 从不 ensure `net.o`。产出权威＝既有 `net_merge` try-heat。Darwin `-dead_strip` 藏 workers U `thread_create_c`；Ubuntu 全链暴露。存储＝L8b seed＋list.x／heavy twin。消费＝user ld UNDEF。**修**：G.7 有则补全既有 need_net ensure `std/net/net.o`（≡ need_sys）＋ unique 针 `close_listener` 27→28 ＋ companion ensure `thread.o`。禁第二套 simple-group／禁把 net_merge 改 formal_mod／禁搅 check／async／db.kv。证：双端 L2 **5／5** · net_listen／resolve **0** · mac `bl listen`／Ubuntu `call listen` · sqlite／datetime／cli／csv **0** · io_read_chunk 成功分 **104** · zc_arena_concat 成功分 **8**。heat：`rm compiler/src/runtime_link_abi.o`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi。 |
| 2026-08-23 | **fix(link-abi)** **std.db.sqlite fk10＋catalog（sqlite_available BLD001）** · tip **`0aa4acd3e`／`7e8868a86`／`ca38f9c73`** | **代码** · **双端 L2 闭**。五问：产生＝std_x auto-soft 裸 `sqlite.x` → T `db_*`，import 要 `std_db_sqlite_*`；OP_STD flag_kind＝10 已列但 fk10 精确针无 `is_available`。Darwin snprintf fortify 撞 generated extern。Ubuntu objcopy 叶名错前缀；ensure 无 sqlite3.h 硬失败。存储＝L8b seed＋heavy fk10 ＋ formal_mod ＋ glue OP 18。消费＝不拉 formal sqlite.o／glue stub。**修**：G.7 有则补全 formal_mod `mod 1 mod.x sqlite.x` ＋迁出 std_x ＋ fk10 3→32 ＋ nested leaf `db_sqlite` ＋剥 snprintf extern ＋ SQLITE ensure stub 重试 ＋ `-lsqlite3` 仅 glue U `sqlite3_open`。禁第二套 simple-group／禁搅 check／async／net。证：双端 L2 **5／5** · sqlite_available **0** · mac `bl is_available`／Ubuntu `call is_available` · datetime／cli／csv **0** · io_read_chunk 成功分 **104** · zc_arena_concat 成功分 **8**。heat：`rm compiler/src/runtime_link_abi.o`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi／formal_mod。 |
| 2026-08-23 | **fix(link-abi)** **std.datetime fk0 k20＋catalog（datetime_iana BLD001）** · tip **`0866f4540`** | **代码** · **双端 L2 闭**。五问：产生＝std_x auto-soft 裸 `datetime.x` → T `datetime_*`，import 要 `std_datetime_*`；OP_STD flag_kind＝0 已列 datetime.o 但 fk0 无 k==20，门从不开。存储＝L8b list seed fk0 ＋ heavy twin ＋ formal_mod ＋ invoke_ld companion。消费＝不拉 formal datetime.o。**修**：G.7 有则补全 formal_mod `mod 1 mod.x datetime.x`（≡ csv／cli／http）＋迁出 std_x ＋ fk0 20→21（27 针＝全 export 面）＋ companion time／time_os。禁第二套 simple-group／禁搅 check／async／db／net。证：双端 L2 **5／5** · datetime_iana／roundtrip／timezone／iana_dst **0** · mac `bl timezone_iana`／Ubuntu `call timezone_iana` · cli／csv／http **0** · io_read_chunk 成功分 **104** · zc_arena_concat 成功分 **8**。heat：`rm compiler/src/runtime_link_abi.o`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi／formal_mod。 |
| 2026-08-23 | **fix(link-abi)** **std.bytes g23 针＋catalog（arena_external BLD001）** · tip **`17fec31d7`** | **代码** · **双端 L2 闭**。五问：产生＝无 simple-group、无 catalog `std/bytes/bytes.o`；unique 名 `from_external`／`is_owned`／`recommend_bytes_alloc_arena`／`extend`／`length`／`deinit` 从不开火。roundtrip 另 U heap.alloc／copy／realloc／free（user.o 无 heap_api 针）。存储＝L8b list.x＋seed twin ＋ formal_mod。消费＝不拉 `bytes.o`。**修**：G.7 无才新增 g==23（29 针＝全 export 面）＋ catalog `mod 0 mod.x` ＋ companion heap／string。禁第二套表／禁搅 check／async／datetime／db／net。证：双端 L2 **5／5** · arena_external／roundtrip **0** · mac `bl from_external`／Ubuntu `call from_external` · cli／csv／http **0** · io_read_chunk 成功分 **104** · zc_arena_concat 成功分 **8**。heat：`rm compiler/src/runtime_link_abi.o`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi／formal_mod。 |
| 2026-08-23 | **fix(link-abi)** **g0 string unique 针＋malloc clash（zc_arena_concat Ubuntu）** · tip **`a3f38858a`** | **代码** · **双端 L2 闭**。五问：产生＝Ubuntu 内联 `string.view` 使 g0 不开火；string_formal 注入 `extern uint8_t *malloc` 撞 stdlib `void *malloc`，string.o 永不落地。存储＝L8b g0 ＋ string_formal 脚本。消费＝ld UNDEF concat_arena／length_StrView／view_get。**修**：G.7 补 g0 10→13 ＋去掉冲突 malloc 声明、对 libc malloc 转型。禁第二套表／禁改 cookbook／禁搅 check／async／datetime／db／net／`std_bytes_*`。证：双端 L2 **5／5** · cookbook 成功分 **8** · mac `bl concat_arena`／Ubuntu `call concat_arena` · string_case_fold／heap_trace **0**。heat：`rm compiler/src/runtime_link_abi.o`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi／string_formal。 |
| 2026-08-23 | **fix(link-abi)** **heap_api unique arena64 针（zc_arena_concat BLD001）** · tip **`2ed48b8e6`** | **代码** · **双端 L2 闭**（Darwin；Ubuntu string 另叶 `a3f38858a`）。五问：产生＝heap_api 27 针只有 `arena64_alloc`；matcher 精确不覆盖 unique `empty`／`init`／`deinit`。heap.o 已有 T。string.o 注入 T libc arena64_*_c，不能当副作用拉 heap。存储＝L8b list.x＋seed twin。消费＝不拉 heap.o。**修**：G.7 补既有针 27→30。禁第二套表／禁搅 check／async／datetime／db／net。证：Darwin L2 **5／5** · cookbook 成功分 **8** · `bl arena64_empty`。heat：`rm compiler/src/runtime_link_abi.o`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi。 |
| 2026-08-23 | **fix(io)** **stubs unique register_provided T（zc_provided_buffers BLD001）** · tip **`06f69f1ba`** | **代码** · **双端 L2 闭**。五问：产生＝always-linked stubs 已有 `std_io_read_ptr`／`ptr_view`，缺 unique 名 `register_provided`／`unregister_provided`。存储＝`runtime_asm_io_stubs.from_x.c`。消费＝user ld UNDEF。**修**：G.7 补既有 stubs（对齐 `stubs.x`：register 永远 0，unregister 空操作）。禁伪造 io_uring buffer ring／禁 c_face 第二套缓冲／禁加 labi 针／禁搅 check／async／datetime／db／net／`arena64`。证：双端 L2 **5／5** · cookbook **0** · mac `bl register_provided`／Ubuntu `call register_provided` · cli／csv／http **0** · io_read_chunk 成功分 **104** · mmap_smoke 成功分 **9** · iter_slice_sum 成功分 **10**。heat：`rm compiler/runtime_asm_io_stubs.o`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED stubs。 |
| 2026-08-23 | **fix(io)** **stubs unique ptr_view T（zc_read_ptr_slice BLD001）** · tip **`bbed9a476`** | **代码** · **双端 L2 闭**。五问：产生＝always-linked stubs 已有 `std_io_read_ptr`／`ptr_len`／`ptr_gen`，缺 unique 名 `ptr_view`／`ptr_view_valid`。存储＝`runtime_asm_io_stubs.from_x.c` 24B ReadPtrView pack。消费＝user ld UNDEF。**修**：G.7 补既有 stubs（pack last read_ptr／len／gen；valid＝非空且 gen 格相等；`stdin_ptr_view` 薄包装）。禁 c_face 第二套缓冲／禁加 labi 针／禁搅 check／async／datetime／db／net／`provided`／`arena64`。证：双端 L2 **5／5** · cookbook **0** · mac `bl ptr_view`／Ubuntu `call ptr_view` · cli／csv／http **0** · io_read_chunk 成功分 **104** · mmap_smoke 成功分 **9** · iter_slice_sum 成功分 **10**。heat：`rm compiler/runtime_asm_io_stubs.o`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED stubs。 |
| 2026-08-23 | **fix(io)** **stubs unique ptr_gen T（mmap_smoke BLD001）** · tip **`f4f3ca03e`** | **代码** · **双端 L2 闭**。五问：产生＝always-linked stubs 已有 `std_io_read_ptr`／`ptr_len`，缺 unique 名 `ptr_gen`／`ptr_valid`／`ptr_backend`；`io_read_ptr` 不递增 gen。存储＝`runtime_asm_io_stubs.from_x.c` cells。消费＝user ld UNDEF。**修**：G.7 补既有 stubs（gen 格＋每次 `io_read_ptr` 递增；backend 保持 0）。禁伪造 mmap 1／2／禁 c_face 第二套缓冲／禁加 labi 针／禁搅 check／async／datetime／`ptr_view`。证：双端 L2 **5／5** · mmap_smoke build **0** run **9** · mac `bl ptr_gen`／Ubuntu `call ptr_gen` · cli／csv／http **0** · io_read_chunk 成功分 **104** · iter_slice_sum 成功分 **10**。heat：`rm compiler/runtime_asm_io_stubs.o`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED stubs。 |
| 2026-08-23 | **fix(labi)** **core.iterator g22 针＋catalog（iter_slice_sum BLD001）** · tip **`0eb85fa6a`** | **代码** · **双端 L2 闭**。五问：产生＝无 simple-group、无 catalog `core/iterator/mod.o`；unique 名 `iter_i32`／`next_i32` 从不开火。存储＝L8b list.x＋seed twin ＋ formal_mod。消费＝不拉 `mod.o`。**修**：G.7 无才新增 g==22（10 针＝全 export 面）＋ catalog `mod|0|mod.x`。禁第二套表／禁搅 check／async／datetime／db／net／`ptr_gen`。证：双端 L2 **5／5** · cookbook 成功分 **10** · tests/iterator **0** · mac `bl iter_i32`／Ubuntu `call iter_i32` · cli／csv／http **0** · io_read_chunk 成功分 **104**。heat：`rm compiler/src/runtime_link_abi.o` ＋ `./xbuild link-product-asm`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi／formal_mod。 |
| 2026-08-23 | **fix(labi)** **core.str g21 针＋catalog（core_str_index BLD001）** · tip **`37bbdb94f`** | **代码** · **双端 L2 闭**。五问：产生＝无 simple-group、无 catalog `core/str/mod.o`；`bytes_view` 常内联，unique 名 `index_of`／`index_of_byte`／`starts_with` 从不开火。存储＝L8b list.x＋seed twin ＋ formal_mod。消费＝不拉 `mod.o`。**修**：G.7 无才新增 g==21（12 针＝全 export 面）＋ catalog `mod|0|mod.x`；cookbook starts_with 改前缀 `"he"`。禁第二套表／禁搅 check／async／datetime／`ptr_gen`。证：双端 L2 **5／5** · cookbook／bytes_view／find_split **0** · mac `bl index_of`／Ubuntu `call index_of` · cli／csv／http **0** · io_read_chunk 成功分 **104**。heat：`rm compiler/src/runtime_link_abi.o` ＋ `./xbuild link-product-asm`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi／formal_mod。 |
| 2026-08-23 | **fix(labi)** **need_sys ensure＋formal_mod clash（sys_write_stdout BLD001）** · tip **`7ce46ae02`／`fbb3ce099`** | **代码** · **双端 L2 闭**。五问：产生＝针已含 `std_sys_write_stdout`、catalog 有 `sys.o`，push_obj skip-missing 从不 ensure；Darwin `#define write` 改 macos FFI libc `write()`；Ubuntu X `msync(ptr,size)` 撞 libc 3 参。存储＝L8b list.from_x.c ＋ heavy.x twin ＋ formal_mod 脚本。消费＝无 `sys.o` → ld UNDEF。**修**：G.7 补既有 need_sys ensure（禁第二套 simple-group）＋ 同 arity 定义改 `xlang_formal_bare_*`。禁改 catalog／禁搅 check／async。证：双端 L2 **5／5** · cookbook **0** · mac `bl write_stdout`／Ubuntu `call write_stdout` · cli／csv／http **0** · io_read_chunk 成功分 **104**。heat：`rm compiler/src/runtime_link_abi.o` ＋ `./xbuild link-product-asm`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi／formal_mod。 |
| 2026-08-23 | **fix(labi)** **core.slice unique u64 针（slice_u64_subslice BLD001）** · tip **`62d057b79`** | **代码** · **双端 L2 闭**。五问：产生＝g==9 10 针只有 len／get／subslice i32|u8 与 len／get u64；matcher 精确不覆盖 unique 名 `subslice_u64`／`split_at_u64`／`chunks_len_u64`；`needs_core_slice` 已探这些名但只 skip-missing 推 glue `slice.o`，formal `mod.o` 从未 ensure。存储＝L8b list.x + seed twin。消费＝不拉 `mod.o`。**修**：G.7 补既有针 10→13。禁改 catalog／禁第二套 ensure／禁搅 sys／check。证：双端 L2 **5／5** · cookbook／slice tests **0** · mac `bl subslice_u64`／Ubuntu `call subslice_u64` · cli／csv／http **0** · io_read_chunk 成功分 **104**。heat：`rm compiler/src/runtime_link_abi.o` ＋ `./xbuild link-product-asm`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi。 |
| 2026-08-23 | **fix(io)** **stubs unique std_io_* T（io_fallback／io_mmap BLD001）** · tip **`eaf75be24`** | **代码** · **双端 L2 闭**。五问：产生＝always-linked stubs 已有 `std_io_read`／`write`／`ptr_len`，缺 unique 名 `from_fd`／`read_fd`／`write_fd`／`std_io_read_ptr`；`io_read_ptr` 曾 `handle!=0 → NULL`；`io_read_ptr_len` 曾永远 0。存储＝`runtime_asm_io_stubs.from_x.c`。消费＝user ld UNDEF。**修**：G.7 补既有 stubs（任意 fd＋len 读 `g_io_read_ptr_len`＋四处 T）。禁 c_face 第二套缓冲／禁加 labi 针／禁改 catalog／禁搅 check／async／`ptr_gen`。证：双端 L2 **5／5** · fallback／mmap／fallback_matrix **0** · mac `bl read_ptr`／Ubuntu `call read_ptr` · cli／csv／http **0** · io_read_chunk 成功分 **104**。heat：`rm compiler/runtime_asm_io_stubs.o`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED stubs。 |
| 2026-08-23 | **fix(labi)** **std.compress stream 针＋c_face（compress_stream BLD001）** · tip **`e3b1cae8b`** | **代码** · **双端 L2 闭**。五问：产生＝g==15 6 针只有 one-shot gzip／brotli／zstd；matcher 精确不覆盖 unique 名 `compress_init`／`process`／`end`／`state_bytes_for`／`format_*`／`mode_*`；产品 `compress.o` 是 c_face 且无 T。存储＝L8b list.x + seed twin／`formal_surface.c`。消费＝不拉 compress.o。**修**：G.7 补既有针 6→14 ＋ c_face T（format／mode 常量；state_bytes_for 128／32／32；init／process／end −1，与 gzip cookbook codec-not-linked 成功 0 同契约）。禁改 catalog 去 host-cc mod.x／禁第二套表／禁搅 check／async。证：双端 L2 **5／5** · stream／gzip／compress main **0** · mac `bl compress_init`／Ubuntu `call compress_init` · cli／csv／http **0** · io_read_chunk 成功分 **104**。heat：`rm compress.o` ＋ `./xbuild link-product-asm`。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi／c_face。 |
| 2026-08-23 | **fix(std)** **tar prefix／Pax STD-152（long_path_dir run 3）** · tip **`9b5bf04e0`** | **代码** · **双端 L2 闭**。五问：产生＝`tar_append_entry_c` 拒 `name_len>100`；next 不跳 Pax／不拼 prefix；read_data 不跳领先 Pax。存储＝`std/tar/tar.x` bare-impl。消费＝270 字节第三条 append return 3。**修**：G.7 补既有 append／next／read（prefix 末级 `/`；Pax `'x'`＋`path=`；smoke 无 ARRAY_LIT）。禁重建 glue／禁搅 check。证：双端 L2 **5／5** · long_path_dir／ustar_roundtrip／main／tar_append_entry **0** · prefix 101 探针 **0** · mac `bl append_entry`／Ubuntu `call append_entry` · cli／csv／http **0** · io_read_chunk 成功分 **104**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED std.tar。 |
| 2026-08-23 | **fix(std)** **tar_append_entry_c STD-038 短路径（cookbook run 1）** · tip **`c28540b51`** | **代码** · **双端 L2 闭**。五问：产生＝`tar_append_entry_c` 永远 −1 且缺 `is_dir`；历史体在已删 `tar.c`；cookbook `u8[512]` 装不下头+数据块。存储＝`std/tar/tar.x` bare-impl。消费＝运行时 return 1。**修**：G.7 补既有短路径（头／512 垫／dir `'5'`／next／read_data）＋ cookbook 1024。禁重建 glue／禁搅 Pax／禁搅 check。证：双端 L2 **5／5** · tar_append_entry／ustar_roundtrip／main **0** · mac `bl append_entry`／Ubuntu `call append_entry` · long_path_dir **3** · cli／csv／http **0** · io_read_chunk 成功分 **104**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED std.tar。 |
| 2026-08-23 | **fix(labi)** **std_fmt_format_template 针＋c_face（fmt_template BLD001）** · tip **`af419eabb`** | **代码** · **双端 L2 闭**。五问：产生＝g==14 8 针无 unique 名 `std_fmt_format_template`，产品 `fmt.o` 是 c_face 且无 T；mod.x 本已 export。存储＝L8b list.x + seed twin／`formal_surface.c`。消费＝g14 不开 → 不拉 fmt.o。**修**：G.7 补既有针 8→9 ＋ c_face T（首个 `{}` → i32，`face_i64`）。禁改 catalog 去 host-cc mod.x／禁第二套表／禁搅 check／tar。证：双端 L2 **5／5** · fmt_template／template_smoke **0** · mac `bl format_template`／Ubuntu `call format_template` · io_read_chunk 成功分 **104** · cli／csv／http **0**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi／c_face。 |
| 2026-08-23 | **fix(codegen)** **STRUCT_LIT dest peel／stamp（io_read_chunk BLD001）** · tip **`588e48a3d`** | **代码** · **双端 L2 闭**。五问：产生＝labi fk0 k==15 针已有 `std_fs_*`；fs_formal KEEP_C 空名 `{fields}` 在 dep 共发射下发不完整 `struct std_fs_posix_`（Iovec[4] 数组元素）／`struct std_io_`（Buffer let）；http 恢复只看函数返回（readv4＝i64）。存储＝`codegen.x` + seed twin。消费＝host-cc 失败 → fs.o 未产出 → ld UNDEF。**修**：G.7 补既有 STRUCT_LIT dest——剥 ARRAY／PTR／SLICE；let dest 戳 ARRAY_LIT；空名 setter 打 dest。禁第二套 tagger／禁改 std／禁加针／禁搅 check／fmt_template。证：双端 L2 **5／5** · io_read_chunk build **0** run **104**（payload sum＆255 成功分）· cli／csv／http **0** · fs.o T `std_fs_create`／open／read／write／close。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED host-C。 |
| 2026-08-23 | **fix(labi)** **vec3f SOA／AOS 精确 UNDEF 针（smoke BLD001）** · tip **`2ae56e216`** | **代码** · **双端 L2 闭**。五问：产生＝`labi_od_vec_sym_*` 28 项与 fk0 k==10 26 项精确针只有 Vec_* 面；matcher 精确，`push_Vec_i32` 不覆盖 unique 名 `vec3f_soa_push`／`aos_deinit`／`reserve_one`／`sum_x`。存储＝L8b list.x + seed twin／fk0 heavy.x + surface。消费＝needs_std_vec／fk0 不开 → 不拉 vec.o。**修**：G.7 补既有表 28→44／fk0 26→42。禁第二套表／禁改 std／禁搅 check／io_read_chunk。证：双端 L2 **5／5** · unique UNDEF cookbook **0** · mac `bl vec3f_soa_push`／Ubuntu `call vec3f_soa_push` · smoke 成功分 **3**（2+1）· sum 成功分 **6**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi。 |
| 2026-08-23 | **fix(labi)** **vec length／deinit／get u64／f64 精确 UNDEF 针（sole BLD001）** · tip **`f9aadfb19`** | **代码** · **双端 L2 闭**。五问：产生＝`labi_od_vec_sym_*` 22 项与 fk0 k==10 20 项精确针只有 length／deinit／get 的 u16／i32；matcher 精确，i32 不覆盖 u64／f64。存储＝L8b list.x + seed twin／fk0 heavy.x + surface。消费＝needs_std_vec／fk0 不开 → 不拉 vec.o。**修**：G.7 补既有表 22→28／fk0 20→26。禁第二套表／禁改 std／禁搅 check。证：双端 L2 **5／5** · unique UNDEF cookbook **0** · mac `bl length_Vec_u64`／Ubuntu `call length_Vec_u64` · append 成功分 **10**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi。 |
| 2026-08-23 | **fix(std)** **Vec_u64／f64 `push` + labi 精确针（import METHOD first-wins）** · tip **`4e69286b3`** | **代码** · **双端 L2 闭**。五问：产生＝std 无 `push(*Vec_u64)`／f64，打分缺候选 score==1 回落 first-wins `push_Vec_i32`；u16／u8 本已绿（host-C／asm）。存储＝`std/vec/mod.x` + L8b list 20 项／fk0 k==10 18 项。消费＝`bl push_i32` 假绿 run=0。**修**：G.7 补 u16 同形 push（`reserve_one`）+ 针 20→22／fk0 18→20。禁改 emit mangle／禁搅 check／length 针。证：双端 L2 **5／5** · cookbook u64／f64 **0** · mac `bl push_Vec_u64`／Ubuntu `call push_Vec_u64` · u16／i32／cli／csv／http **0** · append 成功分 **10**。sole `new+deinit` u64 已由上行 `f9aadfb19` 闭。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED std／labi。 |
| 2026-08-23 | **fix(asm)** **METHOD try_inline 优先 typeck stamp（Vec_u64.new 名-first）** · tip **`b9839f7c4`** | **代码** · **双端 L2 闭**。五问：产生＝`glue_call_lookup` METHOD 名-first 内联 Vec_i32.new（`default_alloc` 32B）进 Vec_u64／u16／f64 16B 槽；typeck dest-type 已 stamp。存储＝Darwin 全 seed `_impl`／Ubuntu prefer `.x`。消费＝const STRUCT_LIT 工厂。**修**：G.7 补 METHOD 优先 `resolved_func_index`；bounded dep（防 stale `increment().field`）；dyn −2 不内联；名扫描 fallback。禁改 std／禁搅 check／push mangle／labi 针。证：双端 L2 **5／5** · nouse_u64 无 allocator · nouse_i32 仍 `default_alloc` · smf／boundary **0** · append 成功分 **10**。暴露：`push(&u64)` 仍 i32 mangle；sole `new+length` u64 BLD001。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED lookup。 |
| 2026-08-23 | **fix(labi)** **vec from_slice_u64／f64 精确 UNDEF 针（sole BLD001）** · tip **`33c974158`** | **代码** · **双端 L2 闭**。五问：产生＝`labi_od_vec_sym_*` 18 项与 fk0 k==10 16 项精确针只有 `from_slice_u8`；matcher 精确，u8 不覆盖 u64／f64。存储＝L8b list.x + seed twin／fk0 heavy.x + surface。消费＝needs_std_vec／fk0 不开 → 不拉 vec.o。**修**：G.7 补既有表 18→20／fk0 16→18。禁第二套表／禁改 std／禁搅 check／METHOD new。证：双端 L2 **5／5** · cli／csv／http **0** · fs_u64／fs_f64 **0** · append 成功分 **10**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi。 |
| 2026-08-23 | **fix(asm)** **import-binding ARRAY→PTR 打分（append_roundtrip 138）** · tip **`5c0bb0c6f`** | **代码** · **双端 L2 闭**。五问：产生＝`glue_asm_score_import_binding_func_ix` 只比 suffix，`u64[4]` vs `*u64` score==1，first-wins `from_slice_i32`（32B sret）；host-C `-E` 已是 u64。存储＝seed `_ex`（Darwin 全 seed／Ubuntu thin+rest）+ `.x` twin。消费＝`bl from_slice_i32` abort 138。**修**：G.7 补既有打分 ARRAY T→*T +10（≡ typeck ak==10 pk==9）；FLOAT_LIT splat 仍 1。禁改 std／针表／check／METHOD new。证：双端 L2 **5／5** · cli／csv／http **0** · append 成功分 **10**（4+6）· splat 0 · mac `bl from_slice_u64`／Ubuntu `call from_slice_u64`。sole from_slice_u64 针后叶。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED score。 |
| 2026-08-23 | **fix(asm)** **AAPCS64 default_alloc dest-shadow x19（vec.new／push／pop 139）** · tip **`5ee5e764d`** | **代码** · **双端 L2 闭**。五问：产生＝内联 `new()` STRUCT_LIT 的 `glue_emit_default_alloc_to_rbx_offset` CALL 后 `store(sz=8)` 用 dest-in-x1；AAPCS64 16B 返回 x0+x1 打掉 dest（lldb 地址 0x18）。存储＝try_inline `.x` + seed `_impl` + surface。消费＝用户 `_main` 内联 stores。**修**：G.7 补 helper `ta==1` 强制 store_size=16 走 x19 双 GP。禁改 enc_store 全局／禁改 std／禁搅 check。证：双端 L2 **5／5** · cli／csv／http **0** · boundary／pop_only／push_only **0** · mac `str [x19]`。append mac 138／Ubuntu 139 后叶。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED helper · MACOS ARM64 热路径。 |
| 2026-08-23 | **fix(labi)** **vec pop／extend 精确 UNDEF 针（append_roundtrip BLD001）** · tip **`a69c01dd9`** | **代码** · **双端 L2 闭**。五问：产生＝`labi_od_vec_sym_*` 12 项与 fk0 k==10 10 项精确针缺 pop／Vec_u64／f64 extend；matcher 精确。另 stale 纯 asm `vec.o` 裸名（Darwin 无 objcopy）。存储＝L8b list.x + seed twin／L8c fk0 heavy.x + surface。消费＝needs_std_vec／fk0 不开 → 不拉 vec.o。**修**：G.7 补既有表 12→18／fk0 10→16。禁第二套表／禁改 std／禁搅 check。证：双端 L2 **5／5** · cli／csv／http **0** · extend_u64／pop_from_slice **0** · append **build 0**。pop_only／boundary 运行时 139 已由上行 `5ee5e764d` 闭。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED labi。 |
