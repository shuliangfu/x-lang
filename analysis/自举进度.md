# Xlang / X language 自举进度

> **更新**：2026-09-24 · Ubuntu `xlang`／`xlang_asm` 换成 **4755456**（07:10，sha256 前缀 `3a931c826cae6870`）L2 **5/5**（option=102）。15:46 的 **12537856** 留在 `~/w887_tip_bak/`。Windows `xlang.exe`／`xlang_asm.exe` 换成 **4556732**（07:51，sha256 前缀 `53dce48f476a2be3`）L2 **5/5**。22:42 的 **4531299** 留在 `C:/xlang_tmp/w888_bak/`。编码对象仍是 9 月 22 日那份，因为用这版 Windows 编译器重编会让 stdlib-import 在 `.Lf0_2` 上报 CG002。钉盘 **`ecdb5cc1e`** 不升。三端 L4 未跑。上一波 **w885** 把 `backend_enc_append_u32_le_c_impl` 收进 `backend_enc_dispatch_thin.x`。它把一个 32 位字的四个小端字节交给 `pipeline_elf_ctx_append_bytes`，符号仍是强符号。这是 i32 与 u32 两参，98 条指令，`sub sp, #0x880`。首字与 w884 那条 i32 两参相同，长度不同（那些是 54 条），也与 w882 那条 i32 四参相同，长度不同（那些是 44 条）。当前编译器重汇编后，既有薄层仍是链上对象的精确前缀（13324 条指令，到 `0xd030`）。去掉被删的 35 条 clang 指令（`sub sp, #0x30`，与 w884 删掉的 23 条同开场、长度不同）后，其余尾的指令字与删前相同。`__const` 仍是 168。对象文件是 **91728** 字节，`__text` **61208**（原 60956）。Darwin 四个二进制 **3929608**（06:13，sha256 前缀 `bd226a448e12e70b`）L2 **5/5**。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`；既有二进制 L2 **5/5**。钉盘 **`ecdb5cc1e`**（不升）。上一波 **w884** 把 `backend_enc_append_u8_c_impl` 收进 `backend_enc_dispatch_thin.x`。它把一个字节的低 8 位交给 `pipeline_elf_ctx_append_bytes`，符号仍是强符号。这是 i32 两参，54 条指令，`sub sp, #0x880`。首字与 w882 那条 i32 四参相同，长度不同（那些是 44 条）。当前编译器重汇编后，既有薄层仍是链上对象的精确前缀（13270 条指令，到 `0xcf58`）。去掉被删的 23 条 clang 指令（`sub sp, #0x30`）后，其余尾的指令字与删前相同。`__const` 仍是 168。对象文件是 **91520** 字节，`__text` **60956**（原 60832）。Darwin 四个二进制 **3929608**（06:00，sha256 前缀 `7873746d35e065f3`）L2 **5/5**。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`；既有二进制 L2 **5/5**。钉盘 **`ecdb5cc1e`**（不升）。上一波 **w883** 把 `arch_x86_64_enc_enc_cdqe_rax_impl` 收进 `backend_enc_dispatch_thin.x`。它自己发出 x86_64 cdqe 的 `0x48 0x98`，符号仍是强符号。这是 i32 单参，57 条指令，`sub sp, #0x870`。首字与 w881 那条 i32 两参相同，长度不同（那些是 28 条）。当前编译器重汇编后，既有薄层仍是链上对象的精确前缀（13213 条指令，到 `0xce74`）。去掉被删的 21 条 clang 指令（`sub sp, #0x20`）后，其余尾的指令字与删前相同。`__const` 末尾的 `48 98` 随 C 体离开。对象文件是 **91432** 字节，`__text` **60832**（原 60688）。Darwin 四个二进制 **3929608**（05:45，sha256 前缀 `d2fc13253f39d594`）L2 **5/5**。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`；既有二进制 L2 **5/5**。钉盘 **`ecdb5cc1e`**（不升）。上一波 **w882** 把 `arch_riscv64_enc_enc_ldr_xreg_xreg_imm` 收进 `backend_enc_dispatch_thin.x`。它仍转调 `backend_enc_riscv64_ldr_xreg_xreg_imm_c`，被调函数留在 C 尾，符号仍是强符号。这是 i32 四参，44 条指令，`sub sp, #0x880`，与 w880 的 `arch_x86_64_enc_enc_load_rax_rbx_disp32` 和 w878 的 `arch_arm64_enc_enc_ldr_xreg_xreg_imm` 同帧，符号和被调函数不同；不是 w881 那条 i32 两参的 28 条 `sub sp, #0x870`，也不是 `x_frontend` 那组 i32 两参的 28 条 `sub sp, #0x880`。当前编译器重汇编后，既有薄层仍是链上对象的精确前缀（13169 条指令，到 `0xcdc4`）。去掉被删的 15 条 clang 指令后，其余尾的指令字与删前相同。对象文件是 **91408** 字节，`__text` **60688**（原 60572）。Darwin 四个二进制 **3929672**（05:26，sha256 前缀 `dbabd1b00d02b654`）L2 **5/5**。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`；既有二进制 L2 **5/5**。钉盘 **`ecdb5cc1e`**（不升）。上一波 **w881** 把 `arch_riscv64_enc_enc_jalr_reg` 收进 `backend_enc_dispatch_thin.x`。它仍转调 `backend_enc_riscv64_jalr_reg_c`，被调函数留在 C 尾，符号仍是强符号。这是 i32 两参，28 条指令，`sub sp, #0x870`，与 w879 的 `arch_x86_64_enc_enc_call_reg` 和 w877 的 `arch_arm64_enc_enc_blr` 同帧，符号和被调函数不同；不是 w880 那条 i32 四参的 44 条 `sub sp, #0x880`，也不是 `x_frontend` 那组 i32 两参的 28 条 `sub sp, #0x880`。当前编译器重汇编后，既有薄层仍是链上对象的精确前缀（13141 条指令，到 `0xcd54`）。去掉被删的 11 条 clang 指令后，其余尾的指令字与删前相同。对象文件是 **91328** 字节，`__text` **60572**（原 60504）。Darwin 四个二进制 **3929672**（05:15，sha256 前缀 `1372cda497b48dbc`）L2 **5/5**。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`；既有二进制 L2 **5/5**。钉盘 **`ecdb5cc1e`**（不升）。上一波 **w880** 把 `arch_x86_64_enc_enc_load_rax_rbx_disp32` 收进 `backend_enc_dispatch_thin.x`。它仍转调 `backend_enc_x86_64_load_rax_rbx_disp32_c`，被调函数留在 C 尾，符号仍是强符号。这是 i32 四参，44 条指令，`sub sp, #0x880`，与 w878 的 `arch_arm64_enc_enc_ldr_xreg_xreg_imm` 同帧，符号和被调函数不同；不是 w879 那条 i32 两参的 28 条 `sub sp, #0x870`，也不是 `x_frontend` 那组 i32 两参的 28 条 `sub sp, #0x880`。当前编译器重汇编后，既有薄层仍是链上对象的精确前缀（13097 条指令，到 `0xcca4`）。去掉被删的 15 条 clang 指令后，其余尾的指令字与删前相同。对象文件是 **91304** 字节，`__text` **60504**（原 60388）。Darwin 四个二进制 **3929672**（05:03，sha256 前缀 `e14d803bae7da3ea`）L2 **5/5**。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`；既有二进制 L2 **5/5**。钉盘 **`ecdb5cc1e`**（不升）。上一波 **w879** 把 `arch_x86_64_enc_enc_call_reg` 收进 `backend_enc_dispatch_thin.x`。它仍转调 `backend_enc_x86_64_call_reg_c`，被调函数留在 C 尾，符号仍是强符号。这是 i32 两参，28 条指令，`sub sp, #0x870`，与 w877 的 `arch_arm64_enc_enc_blr` 同帧，符号和被调函数不同；不是 w878 那条 i32 四参的 44 条 `sub sp, #0x880`，也不是 `x_frontend` 那组 i32 两参的 28 条 `sub sp, #0x880`。当前编译器重汇编后，既有薄层仍是链上对象的精确前缀（13069 条指令，到 `0xcc34`）。去掉被删的 11 条 clang 指令后，其余尾的指令字与删前相同。对象文件是 **91224** 字节，`__text` **60388**（原 60320）。Darwin 四个二进制 **3929672**（04:52，sha256 前缀 `48fc031c09febdf2`）L2 **5/5**。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`；既有二进制 L2 **5/5**。钉盘 **`ecdb5cc1e`**（不升）。上一波 **w878** 把 `arch_arm64_enc_enc_ldr_xreg_xreg_imm` 收进 `backend_enc_dispatch_thin.x`。它仍转调 `backend_enc_arm64_ldr_xreg_xreg_imm_c`，被调函数留在 C 尾，符号仍是强符号。这是 i32 四参，44 条指令，`sub sp, #0x880`，不是 w877 那条 i32 两参的 28 条 `sub sp, #0x870`，也不是 `x_frontend` 那组 i32 两参的 28 条 `sub sp, #0x880`。当前编译器重汇编后，既有薄层仍是链上对象的精确前缀（13025 条指令，到 `0xcb84`）。去掉被删的 15 条 clang 指令后，其余尾的指令字与删前相同。对象文件是 **91200** 字节，`__text` **60320**（原 60204）。Darwin 四个二进制 **3929672**（04:39，sha256 前缀 `3e758c9665ec5de3`）L2 **5/5**。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`；既有二进制 L2 **5/5**。钉盘 **`ecdb5cc1e`**（不升）。上一波 **w877** 把 `arch_arm64_enc_enc_blr` 收进 `backend_enc_dispatch_thin.x`。它仍转调 `backend_enc_arm64_blr_c`，被调函数留在 C 尾，符号仍是强符号。这是 i32 两参，28 条指令，`sub sp, #0x870`，不是 `x_frontend` 那组 i32 两参的 `sub sp, #0x880`。当前编译器重汇编后，既有薄层仍是链上对象的精确前缀（12997 条指令，到 `0xcb14`）。去掉被删的 11 条 clang 指令后，其余尾的指令字与删前相同。对象文件是 **91120** 字节，`__text` **60204**（原 60136）。Darwin 四个二进制 **3929672**（04:25，sha256 前缀 `4c2d288e02614681`）L2 **5/5**。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`；既有二进制 L2 **5/5**。钉盘 **`ecdb5cc1e`**（不升）。上一波 **w876** 把 `pipeline_module_import_path_byte_at_u8_ptr_i32_i32_retu8` 收进 `x_frontend_link_alias.x`。它仍转调 `pipeline_module_import_path_byte_at`，无后缀实现留在 pipeline 对象，符号仍是弱符号。这是 `u8` 三参，36 条指令，`sub sp, #0x880`，不是零参 `*u8` 的 14 条指令，也不是 `*u8` 两参、i32 单参、void 三参或 i32 两参。当前编译器重汇编后，既有 `.x` 面仍是 03:42 对象的精确前缀（920 条指令，到 `0xe60`）。去掉被删的 13 条 clang 指令后，lexer 尾的指令字与删前相同。对象文件是 **8360** 字节，`__text` **3980**（原 3888）。Darwin 四个二进制 **3929672**（03:55，sha256 前缀 `4ed2d9d9ab64fd89`）L2 **5/5**。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `x_frontend_link_alias.o`；既有二进制 L2 **5/5**。钉盘 **`ecdb5cc1e`**（不升）。
> **状态**：**尚未完全自举**。`backend_enc_append_u32_le_c_impl` 已不在 C，符号仍强。`backend_enc_append_u8_c_impl` 已不在 C，符号仍强。`arch_x86_64_enc_enc_cdqe_rax_impl` 已不在 C，符号仍强。`arch_riscv64_enc_enc_ldr_xreg_xreg_imm` 已不在 C，符号仍强。`arch_riscv64_enc_enc_jalr_reg` 已不在 C，符号仍强。`arch_x86_64_enc_enc_load_rax_rbx_disp32` 已不在 C，符号仍强。`arch_x86_64_enc_enc_call_reg` 已不在 C，符号仍强。`arch_arm64_enc_enc_ldr_xreg_xreg_imm` 已不在 C，符号仍强。`arch_arm64_enc_enc_blr` 已不在 C，符号仍强。`pipeline_module_import_path_byte_at` 的带签名转发已不在 C，符号仍弱。`pipeline_asm_emit_dep_pipe_c` 的带签名转发已不在 C，符号仍弱。`pipeline_dep_ctx_module_at` 的带签名转发已不在 C，符号仍弱。`pipeline_dep_ctx_ndep` 的带签名转发已不在 C，符号仍弱。`pipeline_expr_field_access_name_into` 的带签名转发已不在 C，符号仍弱。`pipeline_expr_binop_right_ref_at` 的带签名转发已不在 C，符号仍弱。`pipeline_expr_binop_left_ref_at` 的带签名转发已不在 C，符号仍弱。`pipeline_expr_field_access_base_ref` 的带签名转发已不在 C，符号仍弱。`pipeline_expr_field_access_name_len` 的带签名转发已不在 C，符号仍弱。`glue_codegen_import_path_to_c_prefix_into` 的带签名转发已不在 C。`glue_try_std_heap_redirect_sym_local` 的带签名转发已不在 C。`glue_asm_build_import_binding_call_sym` 的带签名转发已不在 C。`glue_asm_build_func_export_sym_c` 的带签名转发已不在 C。`pipeline_type_kind_ord_at` 的带签名转发已不在 C。lexer 结构返回尾仍 host-cc。编码 f64／Cap 尾仍 host-cc。preamble 的字符串表仍 host-cc。恢复诊断仍 host-cc。诊断 asm BSS 家族仍 host-cc。`lsp_diag_pipeline_ctx` 的 C 尾仍 host-cc。`cfg_eval` 后段阶梯仍 host-cc。arena 的 BSS 仍 host-cc。emit 的 BSS／lib-name／入口前缀仍 host-cc。`rt_stack.x` 纯 asm CG002，本波不动。不重编 `runtime_driver_no_c.o`。pabi 活 rest 仍 host-cc。
> **下波**：再找一份业务已经在 `.x`、产品对象是独立 `.o`、且当前编译器重汇编的 `__text` 与链上对象一致的 from_x，再物理删除 C 体。对不上就不要重链。不要把 `backend_enc_append_u32_le_c_impl` 加回 C，不要把它改成弱符号，不要在 13324 条前缀对不上的编译器上重编 `backend_enc_dispatch.o`。不要把这 98 条 `sub sp, #0x880` 当成 w884 的 54 条，也不要当成 w882 的 44 条。不要把删掉的 35 条 `sub sp, #0x30` 当成 w884 删掉的 23 条。不要把 `backend_enc_append_u8_c_impl` 加回 C，不要把它改成弱符号，不要在 13270 条前缀对不上的编译器上重编 `backend_enc_dispatch.o`。不要把这 54 条 `sub sp, #0x880` 当成 w882 的 44 条 `sub sp, #0x880`。不要把 `arch_x86_64_enc_enc_cdqe_rax_impl` 加回 C，不要把它改成弱符号，不要在 13213 条前缀对不上的编译器上重编 `backend_enc_dispatch.o`。不要把这 57 条 `sub sp, #0x870` 当成 w881 的 28 条 `sub sp, #0x870`。不要把 `arch_riscv64_enc_enc_ldr_xreg_xreg_imm` 加回 C。原下波继续：再找一份业务已经在 `.x`、产品对象是独立 `.o`、且当前编译器重汇编的 `__text` 与链上对象一致的 from_x，再物理删除 C 体。对不上就不要重链。不要把 `arch_riscv64_enc_enc_ldr_xreg_xreg_imm` 加回 C，不要把它改成弱符号，不要在 13169 条前缀对不上的编译器上重编 `backend_enc_dispatch.o`。不要把这 44 条 `sub sp, #0x880` 当成 w881 的 28 条 `sub sp, #0x870`，也不要当成 `x_frontend` 那组 i32 两参的 28 条 `sub sp, #0x880`。它与 w880 的 `arch_x86_64_enc_enc_load_rax_rbx_disp32` 和 w878 的 `arch_arm64_enc_enc_ldr_xreg_xreg_imm` 同帧，符号和被调函数不同。不要把 `arch_riscv64_enc_enc_jalr_reg` 加回 C，不要把它改成弱符号，不要在 13141 条前缀对不上的编译器上重编 `backend_enc_dispatch.o`。不要把这 28 条 `sub sp, #0x870` 当成 w880 的 44 条 `sub sp, #0x880`，也不要当成 `x_frontend` 那组 i32 两参的 28 条 `sub sp, #0x880`。它与 w879 的 `arch_x86_64_enc_enc_call_reg` 和 w877 的 `arch_arm64_enc_enc_blr` 同帧，符号和被调函数不同。不要把 `arch_x86_64_enc_enc_load_rax_rbx_disp32` 加回 C，不要把它改成弱符号，不要在 13097 条前缀对不上的编译器上重编 `backend_enc_dispatch.o`。不要把这 44 条 `sub sp, #0x880` 当成 w879 的 28 条 `sub sp, #0x870`，也不要当成 `x_frontend` 那组 i32 两参的 28 条 `sub sp, #0x880`。它与 w878 的 `arch_arm64_enc_enc_ldr_xreg_xreg_imm` 同帧，符号和被调函数不同。不要把 `arch_x86_64_enc_enc_call_reg` 加回 C，不要把它改成弱符号，不要在 13069 条前缀对不上的编译器上重编 `backend_enc_dispatch.o`。不要把这 28 条 `sub sp, #0x870` 当成 w878 的 44 条 `sub sp, #0x880`，也不要当成 `x_frontend` 那组 i32 两参的 28 条 `sub sp, #0x880`。它与 w877 的 `arch_arm64_enc_enc_blr` 同帧，符号和被调函数不同。不要把 `arch_arm64_enc_enc_ldr_xreg_xreg_imm` 加回 C，不要把它改成弱符号，不要在 13025 条前缀对不上的编译器上重编 `backend_enc_dispatch.o`。不要把这 44 条 `sub sp, #0x880` 当成 w877 的 28 条 `sub sp, #0x870`，也不要当成 `x_frontend` 那组 i32 两参的 28 条 `sub sp, #0x880`。不要把 `arch_arm64_enc_enc_blr` 加回 C，不要把它改成弱符号。不要拿掉 `addsd` 门，不要改走 full `.x`。不要把 `pipeline_module_import_path_byte_at_u8_ptr_i32_i32_retu8` 加回 C，不要把它改成强符号，不要在 920 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`。不要把这条 `u8` 三参转发当成零参 `*u8`、`*u8` 两参、i32 单参、void 三参或 i32 两参。不要把 `pipeline_asm_emit_dep_pipe_c_retu8_ptr` 加回 C，不要把它改成强符号，不要在 906 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`。不要把 `pipeline_dep_ctx_module_at_u8_ptr_i32_retu8_ptr` 加回 C，不要把它改成强符号，不要在 880 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`。不要把剩余的零参指针返回或 uint8 返回别名当成这一刀的 `*u8` 两参转发。不要把 `pipeline_dep_ctx_ndep_u8_ptr_reti32` 加回 C，不要把它改成强符号，不要在 860 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`。不要把剩余的指针返回或 uint8 返回别名当成这一刀的 i32 单参转发。不要把 `pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 加回 C，不要把它改成强符号，不要在 833 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`。不要把剩余的指针返回或 uint8 返回别名当成 void 三参转发。不要把 `pipeline_expr_binop_right_ref_at_u8_ptr_i32_reti32` 加回 C，不要把它改成强符号，不要在 805 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`。void 三参 `pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 已经收进 `.x`。不要把剩余的单参 i32、指针返回或 uint8 返回别名当成同形转发。不要把 `pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 加回 C，不要把它改成强符号，不要在 777 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`。不要把 `pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 加回 C，不要把它改成强符号，不要在 749 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`。不要把 `pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 加回 C，不要把它改成强符号，不要在 721 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`。不要把 `glue_codegen_import_path_to_c_prefix_into_u8_ptr_u8_ptr_i32` 加回 C，不要把它改成 weak，不要在 694 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`。不要移动结构返回。不要把其余 `XLANG_WEAK` 别名改成强符号。不要把 `glue_try_std_heap_redirect_sym_local_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C，不要把它改成 weak，不要在 652 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`。不要把 `glue_asm_build_import_binding_call_sym_u8_ptr_i32_u8_ptr_i32_u8_ptr_reti32` 加回 C，不要把它改成 weak，不要在 604 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`。不要把 `glue_asm_build_func_export_sym_c_u8_ptr_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C，不要把它改成 weak，不要在 556 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`。不要把 `pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 加回 C。不要把编码 marker 加回产品路径的 C，不要改走 full `.x`，不要拿掉 `addsd` 的 nm 门，不要在纯 asm 前缀对不上的编译器上重编 `backend_enc_dispatch.o`。不要把 preamble 的 marker 加回 C，不要在纯 asm 前缀对不上的编译器上重编 `rt_preamble.o`。不要把 parse 的 marker 加回 C，不要在纯 asm 前缀对不上的编译器上重编 `rt_parse_diag.o`。不要把 emit 的 marker 加回 C，不要在纯 asm 前缀对不上的编译器上重编 `rt_emit_state.o`。不要把 arena 的 marker 加回 C，不要在纯 asm 前缀对不上的编译器上重编 `rt_arena_buf.o`。不要把 arch-emit 的 C 种子加回去。不要把这五份 thin 种子加回去。不要把编码薄层的冷路径 C 体加回去。不要把诊断薄层的冷路径 C 体加回去。不要重开 `lsp_diag_pipeline_ctx` 的九个别名，不要重开 `async_asm_pool`。不要重链对不上的 `backend_try_inline_dispatch`、`backend_call_dispatch`、`simd_enc`、`simd_loop`。lexer 尾还没有 `.x` 体。不要对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编。`rt_stack` 的 CG002 不要用 gcc `-E` 修。不要跑 `cfg_eval` 阶梯去重编 `cfg_eval.o`。活 pabi rest 的 thin 面仍是 HARD BAN。禁 mega FORCE 当产品默认；禁 glue_tail；禁剥 thin_glue；禁升钉。Ubuntu 产品对已是 **4755456**（07:10，sha256 前缀 `3a931c826cae6870`）。裸 g05 不会带上那 11 个注入函数，会退回丢掉立即数的发射器；在把 15:46 tip 的注入页按符号重定位接回去之前，不要用裸 g05 覆盖这版。不要升钉。不要跑三端 L4 全擦，pabi mega `-E` 会打满客人内存，而且自托管重编那 11 个函数会回到坏帧。

闸门：**日常放行＝三端 L2 硬闸已启用**（见自举验证／测试环境）；暂停 `xlang check`；L4 仅整波／谈自举／经理另派。

### 维护约定

1. 每波必改本文（KPI／前排≤3／波次流水一行）。终局债改 [`C迁移追踪.md`](C迁移追踪.md)。
2. **禁止**新增／恢复「本日要点」章节；刀次只记波次流水（及既有其它节）。
3. 波次流水倒序 · 一行一波。过期整日流水日切归档后主文档重写。
4. **日切**：新的一天先归档前一天本文为 `自举进度-归档-YYYY-MM-DD.md`，再重写当天本文。

***

## Class ED · Windows 新 xlang 编进 9 月 24 日的对象

- **刀**：macOS 重编之后，Windows 也要用同一份源码编出新的 `xlang.exe`。无后缀的 `compiler/xlang` 和 `compiler/xlang_asm`（各 **6218774**，9 月 22 日／23 日）盖住了产品 `.exe`，ensure 会拿那两份旧编译器去编。把它们挪到 `C:/xlang_tmp/w888_bak/` 之后，用 22:42 的 `xlang.exe` 重编了这些对象：`x_frontend_link_alias.o`、`runtime_driver_diagnostic.o`、`backend_arch_emit_dispatch.o`、`rt_preamble.o`、`rt_parse_diag.o`、`rt_emit_state.o`、`rt_arena_buf.o`，以及 `runtime_pipeline_abi_data_len.o`、`runtime_pipeline_abi_reloc_typed.o`。侧路 g05（CC 残留，81 个对象）链到临时文件，没有覆盖 `xlang-c.exe`。
- **证**：装上之后 Windows `xlang.exe`／`xlang_asm.exe` **4556732**（07:51，sha256 `53dce48f476a2be3f85fe0826a8823c6eab69c34e6231f2143613a1a1d5fc5e4`）L2 **5/5**（rv=42，option=102，stdlib-import，hello，f32）。22:42 的 **4531299** 在 `C:/xlang_tmp/w888_bak/`。`xlang-c.exe` 与 `bootstrap_xlangc.exe` 仍是 **5008116**（9 月 22 日 13:42）。Darwin 四个二进制仍是 **3929608**。Ubuntu 产品对仍是 **4755456**。构建级别 L2。不跑 `xlang check`。
- **残**：不是 host-cc=0。`backend_enc_dispatch.o` 仍是 **62251**（2026-09-22 13:41）。用这版 Windows 编译器把 `backend_enc_dispatch_thin.x` 编进对象（**88806**）之后，stdlib-import 报 CG002：`.Lf0_2` 的 offset 是 -1。那份对象留在 `C:/xlang_tmp/w888/enc_new.o`，没有装进产品。三端 L4 没跑，钉不升。
- **禁**：把 `enc_new.o` 装回产品；把无后缀的旧 `xlang` 放回 `compiler/` 盖住 `.exe`；用裸 g05 覆盖 Ubuntu 4755456；升钉。
- **钉盘**：ecdb5cc1e 不升。

## Class EC · Ubuntu 新 xlang 接上 15:46 注入页

- **刀**：侧路纯 ld 把缺的 `g_pipeline_elf_*` BSS 和堆重定向表接上之后，`option` 仍不是 102。试用编译器发出的 `main` 没有 `mov $imm`，调用之后也不从栈槽重新装载。15:46 tip 里这 11 个函数是跳进 `0xA000000` 注入页的好代码：`pipeline_asm_emit_expr_elf_rec`、`pipeline_asm_emit_expr_elf_for_call_args`、`pipeline_asm_emit_as_elf_impl`、`pipeline_asm_emit_array_lit_flat_elf_c`、`pipeline_asm_array_lit_elem_byte_sz_c`、`glue_array_lit_force_esz_from_elem_type_c`、`glue_struct_lit_store_fixed_array_field_elf_c`、`glue_call_arg_var_use_lea_not_load_elf_c`、`pipeline_asm_local_offset_c`、`pipe_local_slot_bytes_mod`、`glue_fixed_array_temp_bytes`。把该页前 72KiB 接到新链的同一虚拟地址，784 处页外调用按两边的符号重定位，入口改成 `jmp`。没有 mega `-E`，没有覆盖 Darwin，没有重编 `backend_enc_dispatch.o`。
- **证**：装上之后 Ubuntu `xlang`／`xlang_asm` **4755456**（07:10，sha256 `3a931c826cae687084a61041d2d993323ce465bd904df630a0a772b6992a94a7`）L2 **5/5**（rv=42，option=102，hello，stdlib-import，f32）。`just_some` 的 `main` 重新出现 `mov $0x7, %eax` 和调用后的栈槽重装。15:46 的 **12537856** 在 `/home/shuliangfu/w887_tip_bak/`。Windows 产品对仍是 **4531299**（22:42，sha256 前缀 `1b33a18f5822d7b8`），对象没变，重链字节相同。Darwin 四个二进制仍是 **3929608**（06:13，sha256 前缀 `bd226a448e12e70b`）。构建级别 L2。不跑 `xlang check`。
- **残**：不是 host-cc=0。这 11 个好函数体仍在旧 tip 的注入页里，不在当前 `.o` 里。裸 g05 会丢掉它们。三端 L4 没跑，钉不升。
- **禁**：用裸 g05 覆盖这版 Ubuntu `xlang`；全擦后 mega `-E` pabi；在前缀对不上的编译器上重编 `backend_enc_dispatch.o`；升钉。
- **钉盘**：ecdb5cc1e 不升。

## Class EB · w885 · backend_enc_append_u32_le_c_impl 收入 .x

- **刀**：`backend_enc_append_u32_le_c_impl` 从 `seeds/backend_enc_dispatch.from_x.c` 移进 `src/asm/backend_enc_dispatch_thin.x`。它把一个 32 位字的四个小端字节交给 `pipeline_elf_ctx_append_bytes`，符号仍是强符号。这是 i32 与 u32 两参，98 条指令，`sub sp, #0x880`。首字与 w884 那条 i32 两参相同，长度不同（那些是 54 条），也与 w882 那条 i32 四参相同，长度不同（那些是 44 条）。已经在薄层里的 `backend_enc_append_u32_le_c` 仍转调这个实现。C 尾的 `backend_enc_arm64_call_c_impl` 仍直接调用这个符号。`ensure_enc_dispatch_pure` 纯 asm 薄层，再以默认 unwind `cc` 种子（`-DXLANG_L2_ENC_DISPATCH_THIN_FROM_X`），部分链接合并。没有 gcc `-E`，没有 full `.x`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有薄层的 `__text` 是链上对象的精确前缀（13324 条指令，到 `0xd030`）。新实现在 `0xd030`，98 条指令，`sub sp, #0x880`，`bl` 在 `0xd188`，重定位指向 `pipeline_elf_ctx_append_bytes`。符号是强符号。去掉原先 `0xd128` 处 35 条 clang 指令（`sub sp, #0x30`，与 w884 删掉的 23 条同开场、长度不同）后，其余尾的指令字与删前相同。`__const` 仍是 168。`backend_enc_dispatch_slice_marker` 仍在 `0xcaec`。`arch_x86_64_enc_enc_cdqe_rax` 仍是强符号，停在 `0xca9c`。`arch_x86_64_enc_enc_cdqe_rax_impl` 仍是强符号，停在 `0xce74`。`backend_enc_append_u8_c_impl` 仍是强符号，停在 `0xcf58`。`arch_arm64_enc_enc_blr` 仍是强符号，停在 `0xcb14`。`arch_arm64_enc_enc_ldr_xreg_xreg_imm` 仍是强符号，停在 `0xcb84`。`arch_x86_64_enc_enc_call_reg` 仍是强符号，停在 `0xcc34`。`arch_x86_64_enc_enc_load_rax_rbx_disp32` 仍是强符号，停在 `0xcca4`。`arch_riscv64_enc_enc_jalr_reg` 仍是强符号，停在 `0xcd54`。`arch_riscv64_enc_enc_ldr_xreg_xreg_imm` 仍是强符号，停在 `0xcdc4`。`backend_enc_x86_jcc_rel32_c_impl` 从 `0xd030` 移到 `0xd1b8`。`backend_enc_arm64_call_c_impl` 从 `0xd1b4` 移到 `0xd2b0`。`backend_enc_addsd_rax_rbx_arch` 从 `0xd714` 移到 `0xd810`，nm 门还在。`backend_enc_arm64_blr_c` 从 `0xe3fc` 移到 `0xe4f8`。`backend_enc_blr_arch` 从 `0xea34` 移到 `0xeb30`。`backend_enc_ldr_xreg_xreg_imm_arch` 从 `0xeab8` 移到 `0xebb4`。对象文件是 **91728** 字节（原 91520），`__text` **61208**（原 60956），`__compact_unwind` **896**（原 928），`__const` **168** 不变。Darwin 四个二进制 **3929608**（06:13，sha256 前缀 `bd226a448e12e70b`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`（Ubuntu 该对象 73248，2026-09-23 13:17；Windows 该对象 62251，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136。不跑 mega `-E`。
- **残**：不是 host-cc=0。f64／Cap 尾、`backend_enc_arm64_call_c_impl`、`backend_enc_x86_jcc_rel32_c_impl`、被调函数和分发器仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，13324 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这个实现的 C 体加回去；把它改成弱符号；在 13324 条前缀对不上的编译器上重编这个对象；把这 98 条 `sub sp, #0x880` 当成 w884 的 54 条或 w882 的 44 条；把删掉的 35 条 `sub sp, #0x30` 当成 w884 删掉的 23 条；把 `backend_enc_append_u8_c_impl` 或 `arch_x86_64_enc_enc_cdqe_rax_impl` 加回 C；把已收入 `.x` 的六条 `arch_*_enc_enc_*` 转发加回 C；拿掉 `addsd` 门；改走 full `.x`；传 `-DXLANG_BACKEND_ENC_DISPATCH_FROM_X`；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class EA · w884 · backend_enc_append_u8_c_impl 收入 .x

- **刀**：`backend_enc_append_u8_c_impl` 从 `seeds/backend_enc_dispatch.from_x.c` 移进 `src/asm/backend_enc_dispatch_thin.x`。它把一个字节的低 8 位交给 `pipeline_elf_ctx_append_bytes`，符号仍是强符号。这是 i32 两参，54 条指令，`sub sp, #0x880`。首字与 w882 那条 i32 四参相同，长度不同（那些是 44 条）。已经在薄层里的 `backend_enc_append_u8_c` 仍转调这个实现。`ensure_enc_dispatch_pure` 纯 asm 薄层，再以默认 unwind `cc` 种子（`-DXLANG_L2_ENC_DISPATCH_THIN_FROM_X`），部分链接合并。没有 gcc `-E`，没有 full `.x`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有薄层的 `__text` 是链上对象的精确前缀（13270 条指令，到 `0xcf58`）。新实现在 `0xcf58`，54 条指令，`sub sp, #0x880`，`bl` 在 `0xd000`，重定位指向 `pipeline_elf_ctx_append_bytes`。符号是强符号。去掉原先 `0xd0dc` 处 23 条 clang 指令（`sub sp, #0x30`）后，其余尾的指令字与删前相同。`__const` 仍是 168。`backend_enc_dispatch_slice_marker` 仍在 `0xcaec`。`arch_x86_64_enc_enc_cdqe_rax` 仍是强符号，停在 `0xca9c`。`arch_x86_64_enc_enc_cdqe_rax_impl` 仍是强符号，停在 `0xce74`。`arch_arm64_enc_enc_blr` 仍是强符号，停在 `0xcb14`。`arch_arm64_enc_enc_ldr_xreg_xreg_imm` 仍是强符号，停在 `0xcb84`。`arch_x86_64_enc_enc_call_reg` 仍是强符号，停在 `0xcc34`。`arch_x86_64_enc_enc_load_rax_rbx_disp32` 仍是强符号，停在 `0xcca4`。`arch_riscv64_enc_enc_jalr_reg` 仍是强符号，停在 `0xcd54`。`arch_riscv64_enc_enc_ldr_xreg_xreg_imm` 仍是强符号，停在 `0xcdc4`。`backend_enc_append_u32_le_c_impl` 从 `0xd050` 移到 `0xd128`。`backend_enc_addsd_rax_rbx_arch` 从 `0xd698` 移到 `0xd714`，nm 门还在。`backend_enc_arm64_blr_c` 从 `0xe380` 移到 `0xe3fc`。`backend_enc_blr_arch` 从 `0xe9b8` 移到 `0xea34`。`backend_enc_ldr_xreg_xreg_imm_arch` 从 `0xea3c` 移到 `0xeab8`。对象文件是 **91520** 字节（原 91432），`__text` **60956**（原 60832），`__compact_unwind` **928**（原 960），`__const` **168** 不变。Darwin 四个二进制 **3929608**（06:00，sha256 前缀 `7873746d35e065f3`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`（Ubuntu 该对象 73248，2026-09-23 13:17；Windows 该对象 62251，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136。不跑 mega `-E`。
- **残**：不是 host-cc=0。f64／Cap 尾、`backend_enc_append_u32_le_c_impl`、`backend_enc_arm64_call_c_impl`、被调函数和分发器仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，13270 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这个实现的 C 体加回去；把它改成弱符号；在 13270 条前缀对不上的编译器上重编这个对象；把这 54 条 `sub sp, #0x880` 当成 w882 的 44 条；把 `arch_x86_64_enc_enc_cdqe_rax_impl` 加回 C；把已收入 `.x` 的六条 `arch_*_enc_enc_*` 转发加回 C；拿掉 `addsd` 门；改走 full `.x`；传 `-DXLANG_BACKEND_ENC_DISPATCH_FROM_X`；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class DZ · w883 · arch_x86_64_enc_enc_cdqe_rax_impl 收入 .x

- **刀**：`arch_x86_64_enc_enc_cdqe_rax_impl` 从 `seeds/backend_enc_dispatch.from_x.c` 移进 `src/asm/backend_enc_dispatch_thin.x`。它自己把 `0x48 0x98` 交给 `pipeline_elf_ctx_append_bytes`，符号仍是强符号。这是 i32 单参，57 条指令，`sub sp, #0x870`。首字与 w881 那条 i32 两参相同，长度不同（那些是 28 条）。已经在薄层里的 `arch_x86_64_enc_enc_cdqe_rax` 仍转调这个实现。`ensure_enc_dispatch_pure` 纯 asm 薄层，再以默认 unwind `cc` 种子（`-DXLANG_L2_ENC_DISPATCH_THIN_FROM_X`），部分链接合并。没有 gcc `-E`，没有 full `.x`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有薄层的 `__text` 是链上对象的精确前缀（13213 条指令，到 `0xce74`）。新实现在 `0xce74`，57 条指令，`sub sp, #0x870`，`bl` 在 `0xcf28`，重定位指向 `pipeline_elf_ctx_append_bytes`。符号是强符号。去掉原先 `0xe29c` 处 21 条 clang 指令（`sub sp, #0x20`）后，其余尾的指令字与删前相同。`__const` 末尾两字节 `48 98` 随 C 体离开。`backend_enc_dispatch_slice_marker` 仍在 `0xcaec`。`arch_x86_64_enc_enc_cdqe_rax` 仍是强符号，停在 `0xca9c`。`arch_arm64_enc_enc_blr` 仍是强符号，停在 `0xcb14`。`arch_arm64_enc_enc_ldr_xreg_xreg_imm` 仍是强符号，停在 `0xcb84`。`arch_x86_64_enc_enc_call_reg` 仍是强符号，停在 `0xcc34`。`arch_x86_64_enc_enc_load_rax_rbx_disp32` 仍是强符号，停在 `0xcca4`。`arch_riscv64_enc_enc_jalr_reg` 仍是强符号，停在 `0xcd54`。`arch_riscv64_enc_enc_ldr_xreg_xreg_imm` 仍是强符号，停在 `0xcdc4`。`backend_enc_addsd_rax_rbx_arch` 从 `0xd5b4` 移到 `0xd698`，nm 门还在。`backend_enc_arm64_blr_c` 从 `0xe2f0` 移到 `0xe380`。`backend_enc_blr_arch` 从 `0xe928` 移到 `0xe9b8`。`backend_enc_ldr_xreg_xreg_imm_arch` 从 `0xe9ac` 移到 `0xea3c`。对象文件是 **91432** 字节（原 91408），`__text` **60832**（原 60688），`__compact_unwind` **960**（原 992），`__const` **168**（原 170）。Darwin 四个二进制 **3929608**（05:45，sha256 前缀 `d2fc13253f39d594`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`（Ubuntu 该对象 73248，2026-09-23 13:17；Windows 该对象 62251，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136。不跑 mega `-E`。
- **残**：不是 host-cc=0。f64／Cap 尾、被调函数和分发器仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，13213 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这个实现的 C 体加回去；把它改成弱符号；在 13213 条前缀对不上的编译器上重编这个对象；把这 57 条 `sub sp, #0x870` 当成 w881 的 28 条；把已收入 `.x` 的六条 `arch_*_enc_enc_*` 转发加回 C；拿掉 `addsd` 门；改走 full `.x`；传 `-DXLANG_BACKEND_ENC_DISPATCH_FROM_X`；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class DY · w882 · arch_riscv64_enc_enc_ldr_xreg_xreg_imm 收入 .x

- **刀**：`arch_riscv64_enc_enc_ldr_xreg_xreg_imm` 从 `seeds/backend_enc_dispatch.from_x.c` 移进 `src/asm/backend_enc_dispatch_thin.x`。它仍转调 `backend_enc_riscv64_ldr_xreg_xreg_imm_c`，被调函数留在 C 尾，符号仍是强符号。这是 i32 四参，44 条指令，`sub sp, #0x880`。它与 w880 的 `arch_x86_64_enc_enc_load_rax_rbx_disp32` 和 w878 的 `arch_arm64_enc_enc_ldr_xreg_xreg_imm` 同帧，符号和被调函数不同。它不是 w881 那条 i32 两参的 28 条指令（那些是 `sub sp, #0x870`），也不是 `x_frontend` 那组 i32 两参的 28 条指令（那些也是 `sub sp, #0x880`，长度不同）。`ensure_enc_dispatch_pure` 纯 asm 薄层，再以默认 unwind `cc` 种子（`-DXLANG_L2_ENC_DISPATCH_THIN_FROM_X`），部分链接合并。没有 gcc `-E`，没有 full `.x`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有薄层的 `__text` 是链上对象的精确前缀（13169 条指令，到 `0xcdc4`）。新转发在 `0xcdc4`，44 条指令，`sub sp, #0x880`，`bl` 在 `0xce44`，重定位指向 `backend_enc_riscv64_ldr_xreg_xreg_imm_c`。符号是强符号。去掉原先 `0xe878` 处 15 条 clang 指令（`sub sp, #0x30`）后，其余尾的指令字与删前相同。`backend_enc_dispatch_slice_marker` 仍在 `0xcaec`。`arch_arm64_enc_enc_blr` 仍是强符号，停在 `0xcb14`。`arch_arm64_enc_enc_ldr_xreg_xreg_imm` 仍是强符号，停在 `0xcb84`。`arch_x86_64_enc_enc_call_reg` 仍是强符号，停在 `0xcc34`。`arch_x86_64_enc_enc_load_rax_rbx_disp32` 仍是强符号，停在 `0xcca4`。`arch_riscv64_enc_enc_jalr_reg` 仍是强符号，停在 `0xcd54`。`backend_enc_addsd_rax_rbx_arch` 从 `0xd504` 移到 `0xd5b4`，nm 门还在。`backend_enc_riscv64_ldr_xreg_xreg_imm_c` 从 `0xe790` 移到 `0xe840`。对象文件是 **91408** 字节（原 91328），`__text` **60688**（原 60572），`__compact_unwind` **992**（原 1024），`__const` **170** 不变。Darwin 四个二进制 **3929672**（05:26，sha256 前缀 `dbabd1b00d02b654`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`（Ubuntu 该对象 73248，2026-09-23 13:17；Windows 该对象 62251，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136。不跑 mega `-E`。
- **残**：不是 host-cc=0。f64／Cap 尾仍要 `cc`，`.c` 文件还在。`arch_*_enc_enc_*` 单行转发已经不在 C。分发器和被调函数仍在 C 尾。这份对象是 Darwin 当前编译器编出来的，13169 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成弱符号；在 13169 条前缀对不上的编译器上重编这个对象；把这 44 条 `sub sp, #0x880` 当成 w881 的 28 条 `sub sp, #0x870` 或 `x_frontend` 的 i32 两参；把 `arch_riscv64_enc_enc_jalr_reg`、`arch_x86_64_enc_enc_load_rax_rbx_disp32`、`arch_x86_64_enc_enc_call_reg`、`arch_arm64_enc_enc_ldr_xreg_xreg_imm` 或 `arch_arm64_enc_enc_blr` 加回 C；拿掉 `addsd` 门；改走 full `.x`；传 `-DXLANG_BACKEND_ENC_DISPATCH_FROM_X`；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class DX · w881 · arch_riscv64_enc_enc_jalr_reg 收入 .x

- **刀**：`arch_riscv64_enc_enc_jalr_reg` 从 `seeds/backend_enc_dispatch.from_x.c` 移进 `src/asm/backend_enc_dispatch_thin.x`。它仍转调 `backend_enc_riscv64_jalr_reg_c`，被调函数留在 C 尾，符号仍是强符号。这是 i32 两参，28 条指令，`sub sp, #0x870`。它与 w879 的 `arch_x86_64_enc_enc_call_reg` 和 w877 的 `arch_arm64_enc_enc_blr` 同帧，符号和被调函数不同。它不是 w880 那条 i32 四参的 44 条指令（那些是 `sub sp, #0x880`），也不是 `x_frontend` 那组 i32 两参的 28 条指令（那些也是 `sub sp, #0x880`）。`ensure_enc_dispatch_pure` 纯 asm 薄层，再以默认 unwind `cc` 种子（`-DXLANG_L2_ENC_DISPATCH_THIN_FROM_X`），部分链接合并。没有 gcc `-E`，没有 full `.x`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有薄层的 `__text` 是链上对象的精确前缀（13141 条指令，到 `0xcd54`）。新转发在 `0xcd54`，28 条指令，`sub sp, #0x870`，`bl` 在 `0xcd94`，重定位指向 `backend_enc_riscv64_jalr_reg_c`。符号是强符号。去掉原先 `0xe808` 处 11 条 clang 指令（`sub sp, #0x20`）后，其余尾的指令字与删前相同。`backend_enc_dispatch_slice_marker` 仍在 `0xcaec`。`arch_arm64_enc_enc_blr` 仍是强符号，停在 `0xcb14`。`arch_arm64_enc_enc_ldr_xreg_xreg_imm` 仍是强符号，停在 `0xcb84`。`arch_x86_64_enc_enc_call_reg` 仍是强符号，停在 `0xcc34`。`arch_x86_64_enc_enc_load_rax_rbx_disp32` 仍是强符号，停在 `0xcca4`。`backend_enc_addsd_rax_rbx_arch` 从 `0xd494` 移到 `0xd504`，nm 门还在。`backend_enc_riscv64_jalr_reg_c` 从 `0xe6a0` 移到 `0xe710`。对象文件是 **91328** 字节（原 91304），`__text` **60572**（原 60504），`__compact_unwind` **1024**（原 1056），`__const` **170** 不变。Darwin 四个二进制 **3929672**（05:15，sha256 前缀 `1372cda497b48dbc`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`（Ubuntu 该对象 73248，2026-09-23 13:17；Windows 该对象 62251，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136。不跑 mega `-E`。
- **残**：不是 host-cc=0。f64／Cap 尾和其余 `arch_*_enc_enc_*` 单行转发仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，13141 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成弱符号；在 13141 条前缀对不上的编译器上重编这个对象；把这 28 条 `sub sp, #0x870` 当成 w880 的 44 条 `sub sp, #0x880` 或 `x_frontend` 的 i32 两参；把 `arch_x86_64_enc_enc_load_rax_rbx_disp32`、`arch_x86_64_enc_enc_call_reg`、`arch_arm64_enc_enc_ldr_xreg_xreg_imm` 或 `arch_arm64_enc_enc_blr` 加回 C；拿掉 `addsd` 门；改走 full `.x`；传 `-DXLANG_BACKEND_ENC_DISPATCH_FROM_X`；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class DW · w880 · arch_x86_64_enc_enc_load_rax_rbx_disp32 收入 .x

- **刀**：`arch_x86_64_enc_enc_load_rax_rbx_disp32` 从 `seeds/backend_enc_dispatch.from_x.c` 移进 `src/asm/backend_enc_dispatch_thin.x`。它仍转调 `backend_enc_x86_64_load_rax_rbx_disp32_c`，被调函数留在 C 尾，符号仍是强符号。这是 i32 四参，44 条指令，`sub sp, #0x880`。它与 w878 的 `arch_arm64_enc_enc_ldr_xreg_xreg_imm` 同帧，符号和被调函数不同。它不是 w879 那条 i32 两参的 28 条指令（那些是 `sub sp, #0x870`），也不是 `x_frontend` 那组 i32 两参的 28 条指令（那些也是 `sub sp, #0x880`，长度不同）。`ensure_enc_dispatch_pure` 纯 asm 薄层，再以默认 unwind `cc` 种子（`-DXLANG_L2_ENC_DISPATCH_THIN_FROM_X`），部分链接合并。没有 gcc `-E`，没有 full `.x`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有薄层的 `__text` 是链上对象的精确前缀（13097 条指令，到 `0xcca4`）。新转发在 `0xcca4`，44 条指令，`sub sp, #0x880`，`bl` 在 `0xcd24`，重定位指向 `backend_enc_x86_64_load_rax_rbx_disp32_c`。符号是强符号。去掉原先 `0xe758` 处 15 条 clang 指令（`sub sp, #0x30`）后，其余尾的指令字与删前相同。`backend_enc_dispatch_slice_marker` 仍在 `0xcaec`。`arch_arm64_enc_enc_blr` 仍是强符号，停在 `0xcb14`。`arch_arm64_enc_enc_ldr_xreg_xreg_imm` 仍是强符号，停在 `0xcb84`。`arch_x86_64_enc_enc_call_reg` 仍是强符号，停在 `0xcc34`。`backend_enc_addsd_rax_rbx_arch` 从 `0xd3e4` 移到 `0xd494`，nm 门还在。`backend_enc_x86_64_load_rax_rbx_disp32_c` 从 `0xe38c` 移到 `0xe43c`。对象文件是 **91304** 字节（原 91224），`__text` **60504**（原 60388），`__compact_unwind` **1056**（原 1088），`__const` **170** 不变。Darwin 四个二进制 **3929672**（05:03，sha256 前缀 `e14d803bae7da3ea`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`（Ubuntu 该对象 73248，2026-09-23 13:17；Windows 该对象 62251，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136。不跑 mega `-E`。
- **残**：不是 host-cc=0。f64／Cap 尾和其余 `arch_*_enc_enc_*` 单行转发仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，13097 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成弱符号；在 13097 条前缀对不上的编译器上重编这个对象；把这 44 条 `sub sp, #0x880` 当成 w879 的 28 条 `sub sp, #0x870` 或 `x_frontend` 的 i32 两参；把 `arch_x86_64_enc_enc_call_reg`、`arch_arm64_enc_enc_ldr_xreg_xreg_imm` 或 `arch_arm64_enc_enc_blr` 加回 C；拿掉 `addsd` 门；改走 full `.x`；传 `-DXLANG_BACKEND_ENC_DISPATCH_FROM_X`；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class DV · w879 · arch_x86_64_enc_enc_call_reg 收入 .x

- **刀**：`arch_x86_64_enc_enc_call_reg` 从 `seeds/backend_enc_dispatch.from_x.c` 移进 `src/asm/backend_enc_dispatch_thin.x`。它仍转调 `backend_enc_x86_64_call_reg_c`，被调函数留在 C 尾，符号仍是强符号。这是 i32 两参，28 条指令，`sub sp, #0x870`。它与 w877 的 `arch_arm64_enc_enc_blr` 同帧，符号和被调函数不同。它不是 w878 那条 i32 四参的 44 条指令（那些是 `sub sp, #0x880`），也不是 `x_frontend` 那组 i32 两参的 28 条指令（那些也是 `sub sp, #0x880`）。`ensure_enc_dispatch_pure` 纯 asm 薄层，再以默认 unwind `cc` 种子（`-DXLANG_L2_ENC_DISPATCH_THIN_FROM_X`），部分链接合并。没有 gcc `-E`，没有 full `.x`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有薄层的 `__text` 是链上对象的精确前缀（13069 条指令，到 `0xcc34`）。新转发在 `0xcc34`，28 条指令，`sub sp, #0x870`，`bl` 在 `0xcc74`，重定位指向 `backend_enc_x86_64_call_reg_c`。符号是强符号。去掉原先 `0xe6e8` 处 11 条 clang 指令后，其余尾的指令字与删前相同。`backend_enc_dispatch_slice_marker` 仍在 `0xcaec`。`arch_arm64_enc_enc_blr` 仍是强符号，停在 `0xcb14`。`arch_arm64_enc_enc_ldr_xreg_xreg_imm` 仍是强符号，停在 `0xcb84`。`backend_enc_addsd_rax_rbx_arch` 从 `0xd374` 移到 `0xd3e4`，nm 门还在。`backend_enc_x86_64_call_reg_c` 从 `0xe244` 移到 `0xe2b4`。对象文件是 **91224** 字节（原 91200），`__text` **60388**（原 60320），`__compact_unwind` **1088**（原 1120），`__const` **170** 不变。Darwin 四个二进制 **3929672**（04:52，sha256 前缀 `48fc031c09febdf2`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`（Ubuntu 该对象 73248，2026-09-23 13:17；Windows 该对象 62251，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136。不跑 mega `-E`。
- **残**：不是 host-cc=0。f64／Cap 尾和其余 `arch_*_enc_enc_*` 单行转发仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，13069 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成弱符号；在 13069 条前缀对不上的编译器上重编这个对象；把这 28 条 `sub sp, #0x870` 当成 w878 的 44 条 `sub sp, #0x880` 或 `x_frontend` 的 i32 两参；把 `arch_arm64_enc_enc_ldr_xreg_xreg_imm` 或 `arch_arm64_enc_enc_blr` 加回 C；拿掉 `addsd` 门；改走 full `.x`；传 `-DXLANG_BACKEND_ENC_DISPATCH_FROM_X`；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class DU · w878 · arch_arm64_enc_enc_ldr_xreg_xreg_imm 收入 .x

- **刀**：`arch_arm64_enc_enc_ldr_xreg_xreg_imm` 从 `seeds/backend_enc_dispatch.from_x.c` 移进 `src/asm/backend_enc_dispatch_thin.x`。它仍转调 `backend_enc_arm64_ldr_xreg_xreg_imm_c`，被调函数留在 C 尾，符号仍是强符号。这是 i32 四参，44 条指令，`sub sp, #0x880`。它不是 w877 那条 i32 两参的 28 条指令（那些是 `sub sp, #0x870`），也不是 `x_frontend` 那组 i32 两参的 28 条指令（那些也是 `sub sp, #0x880`，长度不同）。`ensure_enc_dispatch_pure` 纯 asm 薄层，再以默认 unwind `cc` 种子（`-DXLANG_L2_ENC_DISPATCH_THIN_FROM_X`），部分链接合并。没有 gcc `-E`，没有 full `.x`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有薄层的 `__text` 是链上对象的精确前缀（13025 条指令，到 `0xcb84`）。新转发在 `0xcb84`，44 条指令，`sub sp, #0x880`，`bl` 在 `0xcc04`，重定位指向 `backend_enc_arm64_ldr_xreg_xreg_imm_c`。符号是强符号。去掉原先 `0xe638` 处 15 条 clang 指令后，其余尾的指令字与删前相同。`backend_enc_dispatch_slice_marker` 仍在 `0xcaec`。`arch_arm64_enc_enc_blr` 仍是强符号，停在 `0xcb14`。`backend_enc_addsd_rax_rbx_arch` 从 `0xd2c4` 移到 `0xd374`，nm 门还在。对象文件是 **91200** 字节（原 91120），`__text` **60320**（原 60204），`__compact_unwind` **1120**（原 1152），`__const` **170** 不变。Darwin 四个二进制 **3929672**（04:39，sha256 前缀 `3e758c9665ec5de3`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`（Ubuntu 该对象 73248，2026-09-23 13:17；Windows 该对象 62251，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136。不跑 mega `-E`。
- **残**：不是 host-cc=0。f64／Cap 尾和其余 `arch_*_enc_enc_*` 单行转发仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，13025 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成弱符号；在 13025 条前缀对不上的编译器上重编这个对象；把这 44 条 `sub sp, #0x880` 当成 w877 的 28 条 `sub sp, #0x870` 或 `x_frontend` 的 i32 两参；把 `arch_arm64_enc_enc_blr` 加回 C；拿掉 `addsd` 门；改走 full `.x`；传 `-DXLANG_BACKEND_ENC_DISPATCH_FROM_X`；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class DT · w877 · arch_arm64_enc_enc_blr 收入 .x

- **刀**：`arch_arm64_enc_enc_blr` 从 `seeds/backend_enc_dispatch.from_x.c` 移进 `src/asm/backend_enc_dispatch_thin.x`。它仍转调 `backend_enc_arm64_blr_c`，被调函数留在 C 尾，符号仍是强符号。这是 i32 两参，28 条指令，`sub sp, #0x870`。它不是 `x_frontend` 那组 i32 两参的 28 条指令（那些是 `sub sp, #0x880`）。`ensure_enc_dispatch_pure` 纯 asm 薄层，再以默认 unwind `cc` 种子（`-DXLANG_L2_ENC_DISPATCH_THIN_FROM_X`），部分链接合并。没有 gcc `-E`，没有 full `.x`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有薄层的 `__text` 是链上对象的精确前缀（12997 条指令，到 `0xcb14`）。新转发在 `0xcb14`，28 条指令，`sub sp, #0x870`，`bl` 在 `0xcb54`，重定位指向 `backend_enc_arm64_blr_c`。符号是强符号。去掉原先 `0xe5c8` 处 11 条 clang 指令后，其余尾的指令字与删前相同。`backend_enc_dispatch_slice_marker` 仍在 `0xcaec`。`backend_enc_addsd_rax_rbx_arch` 从 `0xd254` 移到 `0xd2c4`，nm 门还在。对象文件是 **91120** 字节（原 91096），`__text` **60204**（原 60136），`__compact_unwind` **1152**（原 1184）。Darwin 四个二进制 **3929672**（04:25，sha256 前缀 `4c2d288e02614681`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`（Ubuntu 该对象 73248，2026-09-23 13:17；Windows 该对象 62251，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136。不跑 mega `-E`。
- **残**：不是 host-cc=0。f64／Cap 尾和其余 `arch_*_enc_enc_*` 单行转发仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，12997 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成弱符号；在 12997 条前缀对不上的编译器上重编这个对象；把这 28 条 `sub sp, #0x870` 当成 `x_frontend` 的 i32 两参；拿掉 `addsd` 门；改走 full `.x`；传 `-DXLANG_BACKEND_ENC_DISPATCH_FROM_X`；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class DS · w876 · import_path_byte 带签名转发收入 .x

- **刀**：`pipeline_module_import_path_byte_at_u8_ptr_i32_i32_retu8` 从 `seeds/x_frontend_link_alias.from_x.c` 移进 `x_frontend_link_alias.x`。它仍转调 `pipeline_module_import_path_byte_at`，无后缀实现留在 pipeline 对象，符号仍是弱符号，写进 `G05_X_O_WEAK_FUNCS`。这是 `u8` 三参，36 条指令，`sub sp, #0x880`。它不是零参 `*u8` 的 14 条指令，也不是 `*u8` 两参的 26 条指令、i32 单参的 20 条指令、void 三参的 27 条指令、i32 两参的 28 条指令。种子只留下 lexer 结构返回尾。`ensure_x_frontend_link_alias_prefer` 纯 asm 这一 `.x`，再以默认 unwind `cc` 种子，部分链接合并。失败会删掉产品对象；本波只在 `/tmp` 核对通过后才安装。没有 gcc `-E`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有 `.x` 面的 `__text` 是 03:42 对象的精确前缀（920 条指令，到 `0xe60`）。新转发在 `0xe60`，36 条指令，`sub sp, #0x880`，`bl` 在 `0xec0`，重定位指向 `pipeline_module_import_path_byte_at`。符号是 `weak external`。去掉原先 `0xefc` 处 13 条 clang 指令（`sub sp, #0x20`）后，lexer 尾的指令字与删前相同。原有五个 weak 别名地址不变。`pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xb44`。`pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xbb4`。`pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xc24`。`pipeline_expr_binop_right_ref_at_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xc94`。`pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 仍是弱符号，停在 `0xd04`。`pipeline_dep_ctx_ndep_u8_ptr_reti32` 仍是弱符号，停在 `0xd70`。`pipeline_dep_ctx_module_at_u8_ptr_i32_retu8_ptr` 仍是弱符号，停在 `0xdc0`。`pipeline_asm_emit_dep_pipe_c_retu8_ptr` 仍是弱符号，停在 `0xe28`。`lexer_lexer_init` 从 `0xe60` 移到 `0xef0`。`lexer_lexer_next_into` 从 `0xe8c` 移到 `0xf1c`。`lexer_lexer_next_buf` 从 `0xec8` 移到 `0xf58`。import_path 仍是强符号，停在 `0xad8`。对象文件是 **8360** 字节（原 8304），`__text` **3980**（原 3888），`__compact_unwind` **96**（原 128）。Darwin 四个二进制 **3929672**（03:55，sha256 前缀 `4ed2d9d9ab64fd89`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `x_frontend_link_alias.o`（Ubuntu 该对象 9264，2026-09-20 06:51；Windows 该对象 8760，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。lexer 结构返回尾仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，920 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成强符号；在 920 条前缀对不上的编译器上重编这个对象；把这条 `u8` 三参当成零参 `*u8`、`*u8` 两参、i32 单参、void 三参或 i32 两参；移动结构返回；把 `pipeline_asm_emit_dep_pipe_c_retu8_ptr` 加回 C；把 `pipeline_dep_ctx_module_at_u8_ptr_i32_retu8_ptr` 加回 C；把 `pipeline_dep_ctx_ndep_u8_ptr_reti32` 加回 C；把 `pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 加回 C；把 `pipeline_expr_binop_right_ref_at_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 加回 C；把 `glue_codegen_import_path_to_c_prefix_into_u8_ptr_u8_ptr_i32` 加回 C；把 `glue_try_std_heap_redirect_sym_local_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `glue_asm_build_import_binding_call_sym_u8_ptr_i32_u8_ptr_i32_u8_ptr_reti32` 加回 C；把 `glue_asm_build_func_export_sym_c_u8_ptr_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 加回 C；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把编码 marker 加回产品路径的 C；改走 full `.x`；拿掉 `addsd` 门；把 preamble／parse／emit／arena marker 加回 C；重编那四个对象和 `backend_enc_dispatch.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## Class DR · w875 · emit_dep_pipe 带签名转发收入 .x

- **刀**：`pipeline_asm_emit_dep_pipe_c_retu8_ptr` 从 `seeds/x_frontend_link_alias.from_x.c` 移进 `x_frontend_link_alias.x`。它仍转调 `pipeline_asm_emit_dep_pipe_c`，无后缀实现留在 pipeline 对象，符号仍是弱符号，写进 `G05_X_O_WEAK_FUNCS`。这是零参 `*u8`，14 条指令，`sub sp, #0x870`。首字与 i32 单参的 20 条指令相同，长度不同；也不同于 `*u8` 两参的 26 条指令、void 三参的 27 条指令、i32 两参的 28 条指令。种子留下 lexer 结构返回尾和其余 uint8 三参 `XLANG_WEAK` 别名。`ensure_x_frontend_link_alias_prefer` 纯 asm 这一 `.x`，再以默认 unwind `cc` 种子，部分链接合并。失败会删掉产品对象；本波只在 `/tmp` 核对通过后才安装。没有 gcc `-E`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有 `.x` 面的 `__text` 是 03:24 对象的精确前缀（906 条指令，到 `0xe28`）。新转发在 `0xe28`，14 条指令，`sub sp, #0x870`，`bl` 在 `0xe38`，重定位指向 `pipeline_asm_emit_dep_pipe_c`。符号是 `weak external`。去掉原先 `0xec4` 处 5 条 clang 指令后，lexer 尾和 uint8 别名的指令字与删前相同。原有五个 weak 别名地址不变。`pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xb44`。`pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xbb4`。`pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xc24`。`pipeline_expr_binop_right_ref_at_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xc94`。`pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 仍是弱符号，停在 `0xd04`。`pipeline_dep_ctx_ndep_u8_ptr_reti32` 仍是弱符号，停在 `0xd70`。`pipeline_dep_ctx_module_at_u8_ptr_i32_retu8_ptr` 仍是弱符号，停在 `0xdc0`。`lexer_lexer_init` 从 `0xe28` 移到 `0xe60`。`lexer_lexer_next_into` 从 `0xe54` 移到 `0xe8c`。`lexer_lexer_next_buf` 从 `0xe90` 移到 `0xec8`。洞后的别名后移 `0x24`：`pipeline_module_import_path_byte_at_u8_ptr_i32_i32_retu8` 从 `0xed8` 移到 `0xefc`，仍是 weak。import_path 仍是强符号，停在 `0xad8`。对象文件是 **8304** 字节（原 8312），`__text` **3888**（原 3852），`__compact_unwind` **128**（原 160）。Darwin 四个二进制 **3929672**（03:42，sha256 前缀 `2831d49204e7a198`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `x_frontend_link_alias.o`（Ubuntu 该对象 9264，2026-09-20 06:51；Windows 该对象 8760，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。lexer 结构返回尾和其余 `XLANG_WEAK` 别名（uint8 三参返回）仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，906 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成强符号；在 906 条前缀对不上的编译器上重编这个对象；把剩余的 uint8 三参返回别名当成这一刀的零参 `*u8`；移动结构返回；把其余 `XLANG_WEAK` 别名改成强符号；把 `pipeline_dep_ctx_module_at_u8_ptr_i32_retu8_ptr` 加回 C；把 `pipeline_dep_ctx_ndep_u8_ptr_reti32` 加回 C；把 `pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 加回 C；把 `pipeline_expr_binop_right_ref_at_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 加回 C；把 `glue_codegen_import_path_to_c_prefix_into_u8_ptr_u8_ptr_i32` 加回 C；把 `glue_try_std_heap_redirect_sym_local_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `glue_asm_build_import_binding_call_sym_u8_ptr_i32_u8_ptr_i32_u8_ptr_reti32` 加回 C；把 `glue_asm_build_func_export_sym_c_u8_ptr_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 加回 C；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把编码 marker 加回产品路径的 C；改走 full `.x`；拿掉 `addsd` 门；把 preamble／parse／emit／arena marker 加回 C；重编那四个对象和 `backend_enc_dispatch.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## Class DQ · w874 · dep_ctx_module_at 带签名转发收入 .x

- **刀**：`pipeline_dep_ctx_module_at_u8_ptr_i32_retu8_ptr` 从 `seeds/x_frontend_link_alias.from_x.c` 移进 `x_frontend_link_alias.x`。它仍转调 `pipeline_dep_ctx_module_at`，无后缀实现留在 pipeline 对象，符号仍是弱符号，写进 `G05_X_O_WEAK_FUNCS`。这是 `*u8` 两参，26 条指令，`sub sp, #0x880`，与 i32 单参的 20 条指令、void 三参的 27 条指令、i32 两参的 28 条指令不同。种子留下 lexer 结构返回尾和其余 `XLANG_WEAK` 别名（零参指针返回、uint8 返回）。`ensure_x_frontend_link_alias_prefer` 纯 asm 这一 `.x`，再以默认 unwind `cc` 种子，部分链接合并。失败会删掉产品对象；本波只在 `/tmp` 核对通过后才安装。没有 gcc `-E`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有 `.x` 面的 `__text` 是 03:11 对象的精确前缀（880 条指令，到 `0xdc0`）。新转发在 `0xdc0`，26 条指令，`sub sp, #0x880`，`bl` 在 `0xe00`，重定位指向 `pipeline_dep_ctx_module_at`。符号是 `weak external`。去掉原先 `0xe5c` 处 11 条 clang 指令（`sub sp, #0x20`）后，lexer 尾和其余 `XLANG_WEAK` 别名的指令字与删前相同。原有五个 weak 别名地址不变。`pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xb44`。`pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xbb4`。`pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xc24`。`pipeline_expr_binop_right_ref_at_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xc94`。`pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 仍是弱符号，停在 `0xd04`。`pipeline_dep_ctx_ndep_u8_ptr_reti32` 仍是弱符号，停在 `0xd70`。`lexer_lexer_init` 从 `0xdc0` 移到 `0xe28`。`lexer_lexer_next_into` 从 `0xdec` 移到 `0xe54`。`lexer_lexer_next_buf` 从 `0xe28` 移到 `0xe90`。洞后的别名后移 `0x3c`：`pipeline_asm_emit_dep_pipe_c_retu8_ptr` 从 `0xe88` 移到 `0xec4`，`pipeline_module_import_path_byte_at_u8_ptr_i32_i32_retu8` 从 `0xe9c` 移到 `0xed8`，这些别名仍是 weak。import_path 仍是强符号，停在 `0xad8`。对象文件是 **8312** 字节，`__text` **3852**（原 3792），`__compact_unwind` **160**（原 192）。Darwin 四个二进制 **3929672**（03:24，sha256 前缀 `51183947b30ce322`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `x_frontend_link_alias.o`（Ubuntu 该对象 9264，2026-09-20 06:51；Windows 该对象 8760，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。lexer 结构返回尾和其余 `XLANG_WEAK` 别名（零参指针返回、uint8 返回）仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，880 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成强符号；在 880 条前缀对不上的编译器上重编这个对象；把剩余的零参指针返回或 uint8 返回别名当成这一刀的 `*u8` 两参；移动结构返回；把其余 `XLANG_WEAK` 别名改成强符号；把 `pipeline_dep_ctx_ndep_u8_ptr_reti32` 加回 C；把 `pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 加回 C；把 `pipeline_expr_binop_right_ref_at_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 加回 C；把 `glue_codegen_import_path_to_c_prefix_into_u8_ptr_u8_ptr_i32` 加回 C；把 `glue_try_std_heap_redirect_sym_local_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `glue_asm_build_import_binding_call_sym_u8_ptr_i32_u8_ptr_i32_u8_ptr_reti32` 加回 C；把 `glue_asm_build_func_export_sym_c_u8_ptr_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 加回 C；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把编码 marker 加回产品路径的 C；改走 full `.x`；拿掉 `addsd` 门；把 preamble／parse／emit／arena marker 加回 C；重编那四个对象和 `backend_enc_dispatch.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## Class DP · w873 · dep_ctx_ndep 带签名转发收入 .x

- **刀**：`pipeline_dep_ctx_ndep_u8_ptr_reti32` 从 `seeds/x_frontend_link_alias.from_x.c` 移进 `x_frontend_link_alias.x`。它仍转调 `pipeline_dep_ctx_ndep`，无后缀实现留在 pipeline 对象，符号仍是弱符号，写进 `G05_X_O_WEAK_FUNCS`。这是 i32 单参，20 条指令，`sub sp, #0x870`，与 void 三参的 27 条指令和 i32 两参的 28 条指令不同。种子留下 lexer 结构返回尾和其余 `XLANG_WEAK` 别名（指针返回、uint8 返回）。`ensure_x_frontend_link_alias_prefer` 纯 asm 这一 `.x`，再以默认 unwind `cc` 种子，部分链接合并。失败会删掉产品对象；本波只在 `/tmp` 核对通过后才安装。没有 gcc `-E`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有 `.x` 面的 `__text` 是 02:56 对象的精确前缀（860 条指令，到 `0xd70`）。新转发在 `0xd70`，20 条指令，`sub sp, #0x870`，`bl` 在 `0xd90`，重定位指向 `pipeline_dep_ctx_ndep`。符号是 `weak external`。去掉原先 `0xe0c` 处 9 条 clang 指令（`sub sp, #0x20`）后，lexer 尾和其余 `XLANG_WEAK` 别名的指令字与删前相同。原有五个 weak 别名地址不变。`pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xb44`。`pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xbb4`。`pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xc24`。`pipeline_expr_binop_right_ref_at_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xc94`。`pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 仍是弱符号，停在 `0xd04`。`lexer_lexer_init` 从 `0xd70` 移到 `0xdc0`。`lexer_lexer_next_into` 从 `0xd9c` 移到 `0xdec`。`lexer_lexer_next_buf` 从 `0xdd8` 移到 `0xe28`。洞后的别名后移 `0x2c`：`pipeline_dep_ctx_module_at_u8_ptr_i32_retu8_ptr` 从 `0xe30` 移到 `0xe5c`，`pipeline_asm_emit_dep_pipe_c_retu8_ptr` 从 `0xe5c` 移到 `0xe88`，`pipeline_module_import_path_byte_at_u8_ptr_i32_i32_retu8` 从 `0xe70` 移到 `0xe9c`，这些别名仍是 weak。import_path 仍是强符号，停在 `0xad8`。对象文件仍是 **8288** 字节，`__text` **3792**（原 3748），`__compact_unwind` **192**（原 224）。Darwin 四个二进制 **3929672**（03:11，sha256 前缀 `ee6427c645ae2119`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `x_frontend_link_alias.o`（Ubuntu 该对象 9264，2026-09-20 06:51；Windows 该对象 8760，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。lexer 结构返回尾和其余 `XLANG_WEAK` 别名（指针返回、uint8 返回）仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，860 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成强符号；在 860 条前缀对不上的编译器上重编这个对象；把剩余的指针返回或 uint8 返回别名当成这一刀的 i32 单参；移动结构返回；把其余 `XLANG_WEAK` 别名改成强符号；把 `pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 加回 C；把 `pipeline_expr_binop_right_ref_at_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 加回 C；把 `glue_codegen_import_path_to_c_prefix_into_u8_ptr_u8_ptr_i32` 加回 C；把 `glue_try_std_heap_redirect_sym_local_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `glue_asm_build_import_binding_call_sym_u8_ptr_i32_u8_ptr_i32_u8_ptr_reti32` 加回 C；把 `glue_asm_build_func_export_sym_c_u8_ptr_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 加回 C；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把编码 marker 加回产品路径的 C；改走 full `.x`；拿掉 `addsd` 门；把 preamble／parse／emit／arena marker 加回 C；重编那四个对象和 `backend_enc_dispatch.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## Class DO · w872 · field_name_into 带签名转发收入 .x

- **刀**：`pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 从 `seeds/x_frontend_link_alias.from_x.c` 移进 `x_frontend_link_alias.x`。它仍转调 `pipeline_expr_field_access_name_into`，无后缀实现返回 void 并留在 pipeline 对象，符号仍是弱符号，写进 `G05_X_O_WEAK_FUNCS`。这是 void 三参，27 条指令，与 i32 两参的 28 条指令不同。种子留下 lexer 结构返回尾和其余 `XLANG_WEAK` 别名。`ensure_x_frontend_link_alias_prefer` 纯 asm 这一 `.x`，再以默认 unwind `cc` 种子，部分链接合并。失败会删掉产品对象；本波只在 `/tmp` 核对通过后才安装。没有 gcc `-E`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有 `.x` 面的 `__text` 是 02:39 对象的精确前缀（833 条指令，到 `0xd04`）。新转发在 `0xd04`，27 条指令，`sub sp, #0x880` 与同一对象里已有的 void 转发相同，`bl` 在 `0xd5c`，重定位指向 `pipeline_expr_field_access_name_into`。符号是 `weak external`。去掉原先 `0xda0` 处 13 条 clang 指令（`sub sp, #0x30`）后，lexer 尾和其余 `XLANG_WEAK` 别名的指令字与删前相同。原有五个 weak 别名地址不变。`pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xb44`。`pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xbb4`。`pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xc24`。`pipeline_expr_binop_right_ref_at_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xc94`。`lexer_lexer_init` 从 `0xd04` 移到 `0xd70`。`lexer_lexer_next_into` 从 `0xd30` 移到 `0xd9c`。`lexer_lexer_next_buf` 从 `0xd6c` 移到 `0xdd8`。洞后的别名后移 `0x38`：`pipeline_dep_ctx_ndep_u8_ptr_reti32` 从 `0xdd4` 移到 `0xe0c`，`pipeline_module_import_path_byte_at_u8_ptr_i32_i32_retu8` 从 `0xe38` 移到 `0xe70`，这些别名仍是 weak。import_path 仍是强符号，停在 `0xad8`。对象 **8288**（原 8272），`__text` **3748**（原 3692），`__compact_unwind` **224**（原 256）。Darwin 四个二进制 **3929672**（02:56，sha256 前缀 `8e1a9991b3d41bec`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `x_frontend_link_alias.o`（Ubuntu 该对象 9264，2026-09-20 06:51；Windows 该对象 8760，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。lexer 结构返回尾和其余 `XLANG_WEAK` 别名（单参 i32、指针返回、uint8 返回）仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，833 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成强符号；在 833 条前缀对不上的编译器上重编这个对象；把剩余的单参 i32、指针返回或 uint8 返回别名当成这一刀的 void 三参；移动结构返回；把其余 `XLANG_WEAK` 别名改成强符号；把 `pipeline_expr_binop_right_ref_at_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 加回 C；把 `glue_codegen_import_path_to_c_prefix_into_u8_ptr_u8_ptr_i32` 加回 C；把 `glue_try_std_heap_redirect_sym_local_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `glue_asm_build_import_binding_call_sym_u8_ptr_i32_u8_ptr_i32_u8_ptr_reti32` 加回 C；把 `glue_asm_build_func_export_sym_c_u8_ptr_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 加回 C；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把编码 marker 加回产品路径的 C；改走 full `.x`；拿掉 `addsd` 门；把 preamble／parse／emit／arena marker 加回 C；重编那四个对象和 `backend_enc_dispatch.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## Class DN · w871 · binop_right 带签名转发收入 .x

- **刀**：`pipeline_expr_binop_right_ref_at_u8_ptr_i32_reti32` 从 `seeds/x_frontend_link_alias.from_x.c` 移进 `x_frontend_link_alias.x`。它仍转调 `pipeline_expr_binop_right_ref_at`，无后缀实现留在 pipeline 对象，符号仍是弱符号，写进 `G05_X_O_WEAK_FUNCS`。种子留下 lexer 结构返回尾和其余 `XLANG_WEAK` 别名。`ensure_x_frontend_link_alias_prefer` 纯 asm 这一 `.x`，再以默认 unwind `cc` 种子，部分链接合并。失败会删掉产品对象；本波只在 `/tmp` 核对通过后才安装。没有 gcc `-E`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有 `.x` 面的 `__text` 是 02:28 对象的精确前缀（805 条指令，到 `0xc94`）。新转发在 `0xc94`，28 条指令，`sub sp, #0x880` 与同一对象里已有的两参 i32 转发相同，`bl` 在 `0xcd4`，重定位指向 `pipeline_expr_binop_right_ref_at`。符号是 `weak external`。去掉原先 `0xd64` 处 11 条 clang 指令（`sub sp, #0x20`）后，lexer 尾和其余 `XLANG_WEAK` 别名的指令字与删前相同。原有五个 weak 别名地址不变。`pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xb44`。`pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xbb4`。`pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xc24`。`lexer_lexer_init` 从 `0xc94` 移到 `0xd04`。洞前的 `pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 从 `0xd30` 移到 `0xda0`。洞后的别名后移 `0x44`：`pipeline_dep_ctx_ndep_u8_ptr_reti32` 从 `0xd90` 移到 `0xdd4`，`pipeline_module_import_path_byte_at_u8_ptr_i32_i32_retu8` 从 `0xdf4` 移到 `0xe38`，这些别名仍是 weak。import_path 仍是强符号，停在 `0xad8`。对象 **8272**（原 8240），`__text` **3692**（原 3624），`__compact_unwind` **256**（原 288）。Darwin 四个二进制 **3929672**（02:39，sha256 前缀 `1d460cd90f5f3df5`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `x_frontend_link_alias.o`（Ubuntu 该对象 9264，2026-09-20 06:51；Windows 该对象 8760，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。lexer 结构返回尾和其余 `XLANG_WEAK` 别名仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，805 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成强符号；在 805 条前缀对不上的编译器上重编这个对象；把剩余的 void 三参、单参 i32、指针返回或 uint8 返回别名当成同形转发；移动结构返回；把其余 `XLANG_WEAK` 别名改成强符号；把 `pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 加回 C；把 `glue_codegen_import_path_to_c_prefix_into_u8_ptr_u8_ptr_i32` 加回 C；把 `glue_try_std_heap_redirect_sym_local_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `glue_asm_build_import_binding_call_sym_u8_ptr_i32_u8_ptr_i32_u8_ptr_reti32` 加回 C；把 `glue_asm_build_func_export_sym_c_u8_ptr_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 加回 C；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把编码 marker 加回产品路径的 C；改走 full `.x`；拿掉 `addsd` 门；把 preamble／parse／emit／arena marker 加回 C；重编那四个对象和 `backend_enc_dispatch.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## Class DM · w870 · binop_left 带签名转发收入 .x

- **刀**：`pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 从 `seeds/x_frontend_link_alias.from_x.c` 移进 `x_frontend_link_alias.x`。它仍转调 `pipeline_expr_binop_left_ref_at`，无后缀实现留在 pipeline 对象，符号仍是弱符号，写进 `G05_X_O_WEAK_FUNCS`。种子留下 lexer 结构返回尾和其余 `XLANG_WEAK` 别名。`ensure_x_frontend_link_alias_prefer` 纯 asm 这一 `.x`，再以默认 unwind `cc` 种子，部分链接合并。失败会删掉产品对象；本波只在 `/tmp` 核对通过后才安装。没有 gcc `-E`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有 `.x` 面的 `__text` 是 02:16 对象的精确前缀（777 条指令，到 `0xc24`）。新转发在 `0xc24`，28 条指令，`sub sp, #0x880` 与同一对象里已有的两参 i32 转发相同，`bl` 在 `0xc64`，重定位指向 `pipeline_expr_binop_left_ref_at`。符号是 `weak external`。去掉原先 `0xcf4` 处 11 条 clang 指令（`sub sp, #0x20`）后，lexer 尾和其余 `XLANG_WEAK` 别名的指令字与删前相同。原有五个 weak 别名地址不变。`pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xb44`。`pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xbb4`。`lexer_lexer_init` 从 `0xc24` 移到 `0xc94`。洞前的 `pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 从 `0xcc0` 移到 `0xd30`。洞后的别名后移 `0x44`：`pipeline_expr_binop_right_ref_at_u8_ptr_i32_reti32` 从 `0xd20` 移到 `0xd64`，`pipeline_module_import_path_byte_at_u8_ptr_i32_i32_retu8` 从 `0xdb0` 移到 `0xdf4`，这些别名仍是 weak。import_path 仍是强符号，停在 `0xad8`。对象 **8240**（原 8216），`__text` **3624**（原 3556），`__compact_unwind` **288**（原 320）。Darwin 四个二进制 **3929672**（02:28，sha256 前缀 `02cf6d45de9adf09`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `x_frontend_link_alias.o`（Ubuntu 该对象 9264，2026-09-20 06:51；Windows 该对象 8760，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。lexer 结构返回尾和其余 `XLANG_WEAK` 别名仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，777 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成强符号；在 777 条前缀对不上的编译器上重编这个对象；移动结构返回；把其余 `XLANG_WEAK` 别名改成强符号；把 `pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 加回 C；把 `pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 加回 C；把 `glue_codegen_import_path_to_c_prefix_into_u8_ptr_u8_ptr_i32` 加回 C；把 `glue_try_std_heap_redirect_sym_local_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `glue_asm_build_import_binding_call_sym_u8_ptr_i32_u8_ptr_i32_u8_ptr_reti32` 加回 C；把 `glue_asm_build_func_export_sym_c_u8_ptr_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 加回 C；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把编码 marker 加回产品路径的 C；改走 full `.x`；拿掉 `addsd` 门；把 preamble／parse／emit／arena marker 加回 C；重编那四个对象和 `backend_enc_dispatch.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## Class DL · w869 · field_base_ref 带签名转发收入 .x

- **刀**：`pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 从 `seeds/x_frontend_link_alias.from_x.c` 移进 `x_frontend_link_alias.x`。它仍转调 `pipeline_expr_field_access_base_ref`，无后缀实现留在 pipeline 对象，符号仍是弱符号，写进 `G05_X_O_WEAK_FUNCS`。种子留下 lexer 结构返回尾和其余 `XLANG_WEAK` 别名。`ensure_x_frontend_link_alias_prefer` 纯 asm 这一 `.x`，再以默认 unwind `cc` 种子，部分链接合并。失败会删掉产品对象；本波只在 `/tmp` 核对通过后才安装。没有 gcc `-E`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有 `.x` 面的 `__text` 是 02:02 对象的精确前缀（749 条指令，到 `0xbb4`）。新转发在 `0xbb4`，28 条指令，`sub sp, #0x880` 与同一对象里已有的两参 i32 转发相同，`bl` 在 `0xbf4`，重定位指向 `pipeline_expr_field_access_base_ref`。符号是 `weak external`。去掉原先 `0xc84` 处 11 条 clang 指令（`sub sp, #0x20`）后，lexer 尾和其余 `XLANG_WEAK` 别名的指令字与删前相同。原有五个 weak 别名地址不变。`pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 仍是弱符号，停在 `0xb44`。`lexer_lexer_init` 从 `0xbb4` 移到 `0xc24`。洞前的 `pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 从 `0xc50` 移到 `0xcc0`。洞后的别名后移 `0x44`：`pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 从 `0xcb0` 移到 `0xcf4`，`pipeline_module_import_path_byte_at_u8_ptr_i32_i32_retu8` 从 `0xd6c` 移到 `0xdb0`，这些别名仍是 weak。import_path 仍是强符号，停在 `0xad8`。对象 **8216**（原 8184），`__text` **3556**（原 3488），`__compact_unwind` **320**（原 352）。Darwin 四个二进制 **3929672**（02:16，sha256 前缀 `8a911fc6906fdccd`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `x_frontend_link_alias.o`（Ubuntu 该对象 9264，2026-09-20 06:51；Windows 该对象 8760，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。lexer 结构返回尾和其余 `XLANG_WEAK` 别名仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，749 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成强符号；在 749 条前缀对不上的编译器上重编这个对象；移动结构返回；把其余 `XLANG_WEAK` 别名改成强符号；把 `pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 加回 C；把 `glue_codegen_import_path_to_c_prefix_into_u8_ptr_u8_ptr_i32` 加回 C；把 `glue_try_std_heap_redirect_sym_local_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `glue_asm_build_import_binding_call_sym_u8_ptr_i32_u8_ptr_i32_u8_ptr_reti32` 加回 C；把 `glue_asm_build_func_export_sym_c_u8_ptr_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 加回 C；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把编码 marker 加回产品路径的 C；改走 full `.x`；拿掉 `addsd` 门；把 preamble／parse／emit／arena marker 加回 C；重编那四个对象和 `backend_enc_dispatch.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## Class DK · w868 · field_name_len 带签名转发收入 .x

- **刀**：`pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 从 `seeds/x_frontend_link_alias.from_x.c` 移进 `x_frontend_link_alias.x`。它仍转调 `pipeline_expr_field_access_name_len`，无后缀实现留在 pipeline 对象，符号仍是弱符号，写进 `G05_X_O_WEAK_FUNCS`。种子留下 lexer 结构返回尾和其余 `XLANG_WEAK` 别名。`ensure_x_frontend_link_alias_prefer` 纯 asm 这一 `.x`，再以默认 unwind `cc` 种子，部分链接合并。失败会删掉产品对象；本波只在 `/tmp` 核对通过后才安装。没有 gcc `-E`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有 `.x` 面的 `__text` 是 01:45 对象的精确前缀（721 条指令，到 `0xb44`）。新转发在 `0xb44`，28 条指令，`sub sp, #0x880` 与同一对象里已有的两参 i32 转发相同，`bl` 在 `0xb84`，重定位指向 `pipeline_expr_field_access_name_len`。符号是 `weak external`。去掉原先 `0xbe0` 处 11 条 clang 指令（`sub sp, #0x20`）后，lexer 尾和其余 `XLANG_WEAK` 别名的指令字与删前相同。原有五个 weak 别名地址不变。`lexer_lexer_init` 从 `0xb44` 移到 `0xbb4`。洞后的别名后移 `0x44`：`pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 从 `0xc0c` 移到 `0xc50`，`pipeline_module_import_path_byte_at_u8_ptr_i32_i32_retu8` 从 `0xd28` 移到 `0xd6c`，这些别名仍是 weak。import_path 仍是强符号，停在 `0xad8`。对象 **8184**（原 8160），`__text` **3488**（原 3420），`__compact_unwind` **352**（原 384）。Darwin 四个二进制 **3929672**（02:02，sha256 前缀 `75e247bc72708346`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `x_frontend_link_alias.o`（Ubuntu 该对象 9264，2026-09-20 06:51；Windows 该对象 8760，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。lexer 结构返回尾和其余 `XLANG_WEAK` 别名仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，721 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成强符号；在 721 条前缀对不上的编译器上重编这个对象；移动结构返回；把其余 `XLANG_WEAK` 别名改成强符号；把 `glue_codegen_import_path_to_c_prefix_into_u8_ptr_u8_ptr_i32` 加回 C；把 `glue_try_std_heap_redirect_sym_local_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `glue_asm_build_import_binding_call_sym_u8_ptr_i32_u8_ptr_i32_u8_ptr_reti32` 加回 C；把 `glue_asm_build_func_export_sym_c_u8_ptr_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 加回 C；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把编码 marker 加回产品路径的 C；改走 full `.x`；拿掉 `addsd` 门；把 preamble／parse／emit／arena marker 加回 C；重编那四个对象和 `backend_enc_dispatch.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## Class DJ · w867 · import_path 带签名转发收入 .x

- **刀**：`glue_codegen_import_path_to_c_prefix_into_u8_ptr_u8_ptr_i32` 从 `seeds/x_frontend_link_alias.from_x.c` 移进 `x_frontend_link_alias.x`。它仍转调 `glue_codegen_import_path_to_c_prefix_into`，无后缀实现返回 void，符号仍是强符号，不进五个 weak 名单。无后缀实现仍在 `backend_call_dispatch`。种子留下 lexer 结构返回尾和 `XLANG_WEAK` 别名簇。`ensure_x_frontend_link_alias_prefer` 纯 asm 这一 `.x`，再以默认 unwind `cc` 种子，部分链接合并。失败会删掉产品对象；本波只在 `/tmp` 核对通过后才安装。没有 gcc `-E`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有 `.x` 面的 `__text` 是 01:33 对象的精确前缀（694 条指令，到 `0xad8`）。新转发在 `0xad8`，27 条指令，`sub sp, #0x880` 与同一对象里已有的 void 转发相同，`bl` 在 `0xb30`，重定位指向 `glue_codegen_import_path_to_c_prefix_into`。去掉原先 `0xcf0` 处 13 条 clang 指令（`sub sp, #0x30`）后，lexer 尾和 `XLANG_WEAK` 簇的指令字与删前相同。五个 weak 别名地址不变。`XLANG_WEAK` 簇仍是 weak，整体后移 `0x6c`。`lexer_lexer_init` 从 `0xad8` 移到 `0xb44`。heap_redirect 仍是强符号，停在 `0xa30`。对象 **8160**（原 8144），`__text` **3420**（原 3364），`__compact_unwind` **384**（原 416）。Darwin 四个二进制 **3929672**（01:45，sha256 前缀 `0729a97ede1570e2`）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `x_frontend_link_alias.o`（Ubuntu 该对象 9264，2026-09-20 06:51；Windows 该对象 8760，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。lexer 结构返回尾和 `XLANG_WEAK` 别名簇仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，694 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成 weak；在 694 条前缀对不上的编译器上重编这个对象；移动结构返回；把 `XLANG_WEAK` 别名改成强符号；把 `glue_try_std_heap_redirect_sym_local_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `glue_asm_build_import_binding_call_sym_u8_ptr_i32_u8_ptr_i32_u8_ptr_reti32` 加回 C；把 `glue_asm_build_func_export_sym_c_u8_ptr_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 加回 C；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把编码 marker 加回产品路径的 C；改走 full `.x`；拿掉 `addsd` 门；把 preamble／parse／emit／arena marker 加回 C；重编那四个对象和 `backend_enc_dispatch.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## Class DI · w866 · heap_redirect 带签名转发收入 .x

- **刀**：`glue_try_std_heap_redirect_sym_local_u8_ptr_i32_u8_ptr_i32_reti32` 从 `seeds/x_frontend_link_alias.from_x.c` 移进 `x_frontend_link_alias.x`。它仍转调 `glue_try_std_heap_redirect_sym_local`，符号仍是强符号，不进五个 weak 名单。无后缀实现仍在 `backend_call_dispatch`。种子留下 lexer 结构返回尾、import-path 强转发和 `XLANG_WEAK` 别名簇。`ensure_x_frontend_link_alias_prefer` 纯 asm 这一 `.x`，再以默认 unwind `cc` 种子，部分链接合并。失败会删掉产品对象；本波只在 `/tmp` 核对通过后才安装。没有 gcc `-E`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，既有 `.x` 面的 `__text` 是 01:20 对象的精确前缀（652 条指令，到 `0xa30`）。新转发在 `0xa30`，42 条指令，`sub sp, #0x890` 与同一对象里已有的 `codegen_codegen_x_ast` 相同，`bl` 在 `0xaa8`，重定位指向 `glue_try_std_heap_redirect_sym_local`。去掉原先 `0xc7c` 处 15 条 clang 指令（`sub sp, #0x30`）后，lexer 尾和其余带修饰别名的指令字与删前相同。五个 weak 别名地址不变。`XLANG_WEAK` 簇仍是 weak。import-path 仍是强符号，从 `0xc48` 移到 `0xcf0`。对象 **8144**（原 8072），`__text` **3364**（原 3256），`__compact_unwind` **416**（原 448）。Darwin 四个二进制 **3929672**（01:33）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）与 Ubuntu 既有 tip **12537856**（15:46）本波不重链，也不重编 `x_frontend_link_alias.o`（Ubuntu 该对象 9264，2026-09-20 06:51；Windows 该对象 8760，2026-09-22 13:41）；既有二进制 L2 **5/5**。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。lexer 结构返回尾、import-path 强转发和 `XLANG_WEAK` 别名簇仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，652 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成 weak；在 652 条前缀对不上的编译器上重编这个对象；把 `glue_asm_build_import_binding_call_sym_u8_ptr_i32_u8_ptr_i32_u8_ptr_reti32` 加回 C；把 `glue_asm_build_func_export_sym_c_u8_ptr_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 加回 C；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把编码 marker 加回产品路径的 C；改走 full `.x`；拿掉 `addsd` 门；把 preamble／parse／emit／arena marker 加回 C；重编那四个对象和 `backend_enc_dispatch.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## Class DH · w865 · import_binding 带签名转发收入 .x

- **刀**：`glue_asm_build_import_binding_call_sym_u8_ptr_i32_u8_ptr_i32_u8_ptr_reti32` 从 `seeds/x_frontend_link_alias.from_x.c` 移进 `x_frontend_link_alias.x`。它仍转调 `glue_asm_build_import_binding_call_sym`，符号仍是强符号，不进五个 weak 名单。无后缀实现仍在 `backend_call_dispatch`。种子留下 lexer 结构返回尾和其余带修饰别名。`ensure_x_frontend_link_alias_prefer` 纯 asm 这一 `.x`，再以默认 unwind `cc` 种子，部分链接合并。失败会删掉产品对象；本波只在 `/tmp` 核对通过后才安装。没有 gcc `-E`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，18 个别名加上 type_kind 与 func_export 转发的 `__text` 是 01:08 对象的精确前缀（604 条指令，到 `0x970`）。新转发在 `0x970`，48 条指令，`sub sp, #0x890` 与同一对象里已有的 `codegen_codegen_x_ast` 相同，`bl` 的重定位指向 `glue_asm_build_import_binding_call_sym`。去掉原先 `0xb88` 处 17 条 clang 指令后，lexer 尾和其余带修饰别名的指令字与删前相同。五个 weak 别名地址不变。对象 **8072**（原 7992），`__text` **3256**（原 3132），`__compact_unwind` **448**（原 480）。Darwin 四个二进制 **3929656**（01:21）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）L2 **5/5**。Ubuntu 既有 tip **12537856**（15:46）L2 **5/5**。两端本波不重链，也不重编 `x_frontend_link_alias.o`（Ubuntu 该对象 9264，2026-09-20 06:51；Windows 该对象 8760，2026-09-22 13:41）。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。lexer 结构返回尾和其余带修饰别名仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，604 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成 weak；在 604 条前缀对不上的编译器上重编这个对象；把 `glue_asm_build_func_export_sym_c_u8_ptr_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；把 `pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 加回 C；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把编码 marker 加回产品路径的 C；改走 full `.x`；拿掉 `addsd` 门；把 preamble／parse／emit／arena marker 加回 C；重编那四个对象和 `backend_enc_dispatch.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## Class DG · w864 · func_export 带签名转发收入 .x

- **刀**：`glue_asm_build_func_export_sym_c_u8_ptr_u8_ptr_i32_u8_ptr_i32_reti32` 从 `seeds/x_frontend_link_alias.from_x.c` 移进 `x_frontend_link_alias.x`。它仍转调 `glue_asm_build_func_export_sym_c`，符号仍是强符号，不进五个 weak 名单。种子留下 lexer 结构返回尾和其余带修饰别名。`ensure_x_frontend_link_alias_prefer` 纯 asm 这一 `.x`，再以默认 unwind `cc` 种子，部分链接合并。失败会删掉产品对象；本波只在 `/tmp` 核对通过后才安装。没有 gcc `-E`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，18 个别名加上 w863 转发的 `__text` 是 00:54 对象的精确前缀（556 条指令，到 `0x8b0`）。新转发在 `0x8b0`，48 条指令，`sub sp, #0x890` 与同一对象里已有的 `codegen_codegen_x_ast` 相同，`bl` 的重定位指向 `glue_asm_build_func_export_sym_c`。去掉原先 `0xac8` 处 17 条 clang 指令后，lexer 尾和其余带修饰别名的指令字与删前相同。五个 weak 别名地址不变。对象 **7992**（原 7904），`__text` **3132**（原 3008），`__compact_unwind` **480**（原 512）。Darwin 四个二进制 **3929656**（01:08）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）L2 **5/5**。Ubuntu 既有 tip **12537856**（15:46）L2 **5/5**。两端本波不重链，也不重编 `x_frontend_link_alias.o`（Ubuntu 该对象 9264，2026-09-20 06:51；Windows 该对象 8760，2026-09-22 13:41）。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。lexer 结构返回尾和其余带修饰别名仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，556 条前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成 weak；在 556 条前缀对不上的编译器上重编这个对象；把 `pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 加回 C；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把编码 marker 加回产品路径的 C；改走 full `.x`；拿掉 `addsd` 门；把 preamble／parse／emit／arena marker 加回 C；重编那四个对象和 `backend_enc_dispatch.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## Class DF · w863 · type_kind_ord 带签名转发收入 .x

- **刀**：`pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 从 `seeds/x_frontend_link_alias.from_x.c` 移进 `x_frontend_link_alias.x`。它仍转调 `pipeline_type_kind_ord_at`，符号仍是强符号，不进五个 weak 名单。种子留下 lexer 结构返回尾和其余带修饰别名。`ensure_x_frontend_link_alias_prefer` 纯 asm 这一 `.x`，再以默认 unwind `cc` 种子，部分链接合并。失败会删掉未完成的临时对象；本波只在 `/tmp` 核对通过后才安装。没有 gcc `-E`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，原来 18 个别名的 `__text` 是 20:10 对象的精确前缀（528 条指令，到 `0x840`）。新转发在 `0x840`，`bl` 的重定位指向 `pipeline_type_kind_ord_at`。去掉原先 `0xb14` 处 11 条 clang 指令后，lexer 尾和其余带修饰别名的指令字与删前相同。五个 weak 别名地址不变。对象 **7904**（原 7880），`__text` **3008**（原 2940），`__compact_unwind` **512**（原 544）。Darwin 四个二进制 **3929656**（00:55）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）L2 **5/5**。Ubuntu 既有 tip **12537856**（15:46）L2 **5/5**。两端本波不重链，也不重编 `x_frontend_link_alias.o`（Ubuntu 该对象 9264，2026-09-20 06:51；Windows 该对象 8760，2026-09-22 13:41）。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。lexer 结构返回尾和其余带修饰别名仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，18 个别名前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把这条转发的 C 体加回去；把它改成 weak；在 18 个别名前缀对不上的编译器上重编这个对象；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把编码 marker 加回产品路径的 C；改走 full `.x`；拿掉 `addsd` 门；把 preamble／parse／emit／arena marker 加回 C；重编那四个对象和 `backend_enc_dispatch.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## Class DE · w862 · backend_enc_dispatch 切片 marker 收入 .x

- **刀**：`backend_enc_dispatch_slice_marker`（产品路径返回 1，产品里没有调用方）从 `seeds/backend_enc_dispatch.from_x.c` 的产品分支移进 `src/asm/backend_enc_dispatch_thin.x`。种子留下 f64／Cap 尾，含 `backend_enc_addsd_rax_rbx_arch`。未使用的 `XLANG_BACKEND_ENC_DISPATCH_FROM_X` 空尾仍返回 0，产品不定义这个宏。`ensure_enc_dispatch_pure` 纯 asm 薄层 `.x`，再以默认 unwind `cc` 种子，`ld -r` 合并到临时对象；nm 要求 `backend_enc_addsd_rax_rbx_arch`、`backend_enc_append_u32_le_c` 和 marker。失败留下原对象。没有 gcc `-E`，没有 full `.x`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，薄层公共函数的 `__text` 是 22:22 对象的精确前缀（12987 条指令）。marker 从文本末尾 8 字节 `mov w0, #1; ret` 变成这个编译器的返回 1 帧，插在薄层前缀之后、f64／Cap 尾之前，返回值仍是 1。去掉末尾那 2 条指令后，f64／Cap 尾的指令字与删前相同。`__TEXT,__const` 仍是 170，与删前逐字节相同。对象 **91096**（原 91104），`__text` **60136**（原 60104），`__compact_unwind` **1184**（原 1216）。Darwin 四个二进制 **3929656**（00:37）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）L2 **5/5**。Ubuntu 既有 tip **12537856**（15:46）L2 **5/5**。两端本波不重链，也不重编 `backend_enc_dispatch.o`（Ubuntu 该对象 73248，2026-09-23 13:17；Windows 该对象 62251，2026-09-22 13:41）。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。f64／Cap 尾仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把产品路径的 marker C 体加回去；改走 full `.x`；拿掉 `addsd` 的 nm 门；去掉默认 unwind；在纯 asm 前缀对不上的编译器上重编这个对象；把编码薄层冷路径 C 体加回去；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把 preamble／parse／emit／arena marker 加回 C；重编 `rt_preamble.o`／`rt_parse_diag.o`／`rt_emit_state.o`／`rt_arena_buf.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## Class DD · w861 · rt_preamble 切片 marker 收入 .x

- **刀**：`labi_rt_preamble_slice_marker`（返回 1，产品里没有调用方）从 `seeds/rt_preamble.from_x.c` 移进 `src/runtime/rt_preamble.x`。种子留下巨型字符串表和两条不发符号的声明。`ensure_rt_preamble_prefer` 纯 asm 这一 `.x`，再 `cc` 种子，`ld -r` 合并到临时对象；nm 要求两个写入函数、marker 和四枚表符号。失败留下原对象。没有 gcc `-E`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，两个写入函数的 `__text` 是 19:02 对象的精确前缀（271 条指令）。marker 从 8 字节 `mov w0, #1; ret` 变成这个编译器的返回 1 帧，返回值仍是 1。`__cstring` 仍是 41091，`__TEXT,__const` 仍是 8，`__DATA,__const` 仍是 1912，三者与删前逐字节相同。对象 **52320**（原 52408），`__text` **1124**（原 1092）。原先 32 字节的 `__compact_unwind` 只属于那条 clang marker，种子里不再有函数，这一节随之消失。Darwin 四个二进制 **3929656**（00:19）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）L2 **5/5**。Ubuntu 既有 tip **12537856**（15:46）L2 **5/5**。两端本波不重链，也不重编 `rt_preamble.o`（Ubuntu 该对象 53104，2026-09-22 21:46；Windows 该对象 48513，2026-09-22 09:35）。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。字符串表仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把 marker 的 C 体加回去；在纯 asm 前缀对不上的编译器上重编这个对象；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把 parse／emit／arena marker 加回 C；重编 `rt_parse_diag.o`／`rt_emit_state.o`／`rt_arena_buf.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；把编码或诊断冷路径 C 体加回去；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## Class DC · w860 · rt_parse_diag 切片 marker 收入 .x

- **刀**：`labi_rt_parse_diag_slice_marker`（返回 1，产品里没有调用方）从 `seeds/rt_parse_diag.from_x.c` 移进 `src/runtime/rt_parse_diag.x`。种子留下 `runtime_report_parse_recovery_diagnostics` 和它的静态辅助。`ensure_rt_parse_diag_prefer` 纯 asm 这一 `.x`，再 `cc` 种子，`ld -r` 合并到临时对象；nm 要求精确诊断、marker 和恢复诊断。失败留下原对象。没有 gcc `-E`，没有整份回退。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：当前编译器重汇编后，精确诊断的 `__text` 是 19:56 对象的精确前缀（1438 条指令）。marker 从夹在 C 正文中间的 8 字节 `mov w0, #1; ret` 变成这个编译器的返回 1 帧，返回值仍是 1。去掉这 2 条指令后，恢复诊断和全部静态辅助的指令字与删前相同。对象 **16784**（原 16792），`__text` **12492**（原 12460），`__compact_unwind` **384**（原 416），`__cstring` 仍是 443。Darwin 四个二进制 **3929656**（00:02）L2 **5/5**（option=102）。Windows 既有产品对 **4531299**（22:42）L2 **5/5**。Ubuntu 既有 tip **12537856**（15:46）L2 **5/5**。两端本波不重链，也不重编 `rt_parse_diag.o`（Ubuntu 该对象 11400，2026-09-22 21:46；Windows 该对象 9406，2026-09-22 09:35）。`runtime_driver_no_c.o` 仍是 125136（19:23）。不跑 mega `-E`。
- **残**：不是 host-cc=0。恢复诊断仍要 `cc`，`.c` 文件还在。这份对象是 Darwin 当前编译器编出来的，前缀对不上的编译器不能重编它。Windows 产品对仍是 **4531299**（22:42）。Ubuntu tip 仍是 **12537856**（15:46）。
- **禁**：把 marker 的 C 体加回去；在纯 asm 前缀对不上的编译器上重编这个对象；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；把 emit／arena marker 加回 C；重编 `rt_emit_state.o`／`rt_arena_buf.o`；把 arch-emit 种子加回去；重链对不上的 try_inline／call／simd；把编码或诊断冷路径 C 体加回去；重链 `async_asm_pool`；跑阶梯重编 `cfg_eval.o`；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 或文件级 `let` 赋值的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

***

## 0. 一句话

**尚未完全自举** · 钉盘 **`ecdb5cc1e`** · **三端 L2 硬闸已启用** · **Class EB** `backend_enc_append_u32_le_c_impl` 已收入 `.x`，这是 i32 与 u32 两参，98 条指令，`sub sp, #0x880`，符号仍强；f64／Cap 尾仍 host-cc。**Class EA** `backend_enc_append_u8_c_impl` 已收入 `.x`，这是 i32 两参，54 条指令，`sub sp, #0x880`，符号仍强；f64／Cap 尾仍 host-cc。**Class DZ** `arch_x86_64_enc_enc_cdqe_rax_impl` 已收入 `.x`，这是 i32 单参，57 条指令，`sub sp, #0x870`，符号仍强；f64／Cap 尾仍 host-cc。**Class DY** `arch_riscv64_enc_enc_ldr_xreg_xreg_imm` 已收入 `.x`，这是 i32 四参，`sub sp, #0x880`，符号仍强；f64／Cap 尾仍 host-cc。**Class DX** `arch_riscv64_enc_enc_jalr_reg` 已收入 `.x`，这是 i32 两参，`sub sp, #0x870`，符号仍强；f64／Cap 尾仍 host-cc。**Class DW** `arch_x86_64_enc_enc_load_rax_rbx_disp32` 已收入 `.x`，这是 i32 四参，`sub sp, #0x880`，符号仍强；f64／Cap 尾仍 host-cc。**Class DV** `arch_x86_64_enc_enc_call_reg` 已收入 `.x`，这是 i32 两参，`sub sp, #0x870`，符号仍强；f64／Cap 尾仍 host-cc。**Class DU** `arch_arm64_enc_enc_ldr_xreg_xreg_imm` 已收入 `.x`，这是 i32 四参，`sub sp, #0x880`，符号仍强。**Class DT** `arch_arm64_enc_enc_blr` 已收入 `.x`，这是 i32 两参，`sub sp, #0x870`，符号仍强。**Class DS** `pipeline_module_import_path_byte_at` 的带签名转发已收入 `.x`，这是 `u8` 三参，符号仍弱；lexer 尾仍 host-cc。**Class DR** `pipeline_asm_emit_dep_pipe_c` 的带签名转发已收入 `.x`，这是零参 `*u8`，符号仍弱。**Class DQ** `pipeline_dep_ctx_module_at` 的带签名转发已收入 `.x`，这是 `*u8` 两参，符号仍弱；lexer 尾与其余 `XLANG_WEAK` 别名仍 host-cc。**Class DP** `pipeline_dep_ctx_ndep` 的带签名转发已收入 `.x`，这是 i32 单参，符号仍弱；lexer 尾与其余 `XLANG_WEAK` 别名仍 host-cc。**Class DO** `pipeline_expr_field_access_name_into` 的带签名转发已收入 `.x`，无后缀实现返回 void，符号仍弱。**Class DN** `pipeline_expr_binop_right_ref_at` 的带签名转发已收入 `.x`，符号仍弱。**Class DM** `pipeline_expr_binop_left_ref_at` 的带签名转发已收入 `.x`，符号仍弱。**Class DL** `pipeline_expr_field_access_base_ref` 的带签名转发已收入 `.x`，符号仍弱。**Class DK** `pipeline_expr_field_access_name_len` 的带签名转发已收入 `.x`，符号仍弱。**Class DJ** `glue_codegen_import_path_to_c_prefix_into` 的带签名转发已收入 `.x`，无后缀实现返回 void，符号仍强。**Class DI** `glue_try_std_heap_redirect_sym_local` 的带签名转发已收入 `.x`，仍是强符号。**Class DH** `glue_asm_build_import_binding_call_sym` 的带签名转发已收入 `.x`。**Class DG** `glue_asm_build_func_export_sym_c` 的带签名转发已收入 `.x`。**Class DF** `pipeline_type_kind_ord_at` 的带签名转发已收入 `.x`。**Class DE** 编码切片 marker 已收入 `.x`，仍返回 1；f64／Cap 尾仍 host-cc。preamble／parse／emit／arena 的 marker 已在 `.x`。arch-emit 切片不再 host-cc。诊断 asm BSS 仍 host-cc。`lsp_diag_pipeline_ctx` 的 C 尾仍 host-cc。`cfg_eval` 后段阶梯仍 host-cc。pabi 活 rest 仍 host-cc。

| 指标 | 状态 |
|------|------|
| 钉盘 L4 | ✅ 双端＠**`ecdb5cc1e`**（不升） |
| tip L2 | ✅ Darwin 本波重链 **5/5**（3929608，06:13，sha256 前缀 `bd226a448e12e70b`）。Windows 既有 **4531299**（22:42）与 Ubuntu 既有 **12537856**（15:46）本波不重链，也不重编 `backend_enc_dispatch.o`；既有二进制 L2 **5/5** |
| BC inventory | present **0**（真债＝map 外 from_x／`.inc`／大户 leftover） |
| 本波级别 | **M2 Class EB** `backend_enc_append_u32_le_c_impl` 收入 `.x`，这是 i32 与 u32 两参，98 条指令，`sub sp, #0x880`，符号仍强；f64／Cap 尾仍 host-cc。构建级别 L2，不跑 `xlang check` |
| 下一波 | 再找一份业务已在 `.x`、产品对象是独立 `.o`、当前编译器 `__text` 与链上对象一致的 from_x 再删 C（对不上不重链；不把 `backend_enc_append_u32_le_c_impl` 加回 C；不把它改成弱符号；不在 13324 条前缀对不上的编译器上重编 `backend_enc_dispatch.o`；不把这 98 条 `sub sp, #0x880` 当成 w884 的 54 条或 w882 的 44 条；不把 `backend_enc_append_u8_c_impl` 加回 C；不把它改成弱符号；不在 13270 条前缀对不上的编译器上重编 `backend_enc_dispatch.o`；不把这 54 条 `sub sp, #0x880` 当成 w882 的 44 条；不把 `arch_x86_64_enc_enc_cdqe_rax_impl` 加回 C；不把它改成弱符号；不在 13213 条前缀对不上的编译器上重编 `backend_enc_dispatch.o`；不把这 57 条 `sub sp, #0x870` 当成 w881 的 28 条；再找一份业务已在 `.x`、产品对象是独立 `.o`、当前编译器 `__text` 与链上对象一致的 from_x 再删 C（对不上不重链；不把 `arch_riscv64_enc_enc_ldr_xreg_xreg_imm` 加回 C；不把它改成弱符号；不在 13169 条前缀对不上的编译器上重编 `backend_enc_dispatch.o`；不把这 44 条 `sub sp, #0x880` 当成 w881 的 28 条 `sub sp, #0x870` 或 `x_frontend` 那组 i32 两参的 28 条 `sub sp, #0x880`；它与 w880 的 `arch_x86_64_enc_enc_load_rax_rbx_disp32` 和 w878 的 `arch_arm64_enc_enc_ldr_xreg_xreg_imm` 同帧，符号和被调函数不同；不把 `arch_riscv64_enc_enc_jalr_reg` 加回 C；不把它改成弱符号；不在 13141 条前缀对不上的编译器上重编 `backend_enc_dispatch.o`；不把这 28 条 `sub sp, #0x870` 当成 w880 的 44 条 `sub sp, #0x880` 或 `x_frontend` 那组 i32 两参的 28 条 `sub sp, #0x880`；它与 w879 的 `arch_x86_64_enc_enc_call_reg` 和 w877 的 `arch_arm64_enc_enc_blr` 同帧，符号和被调函数不同；不把 `arch_x86_64_enc_enc_load_rax_rbx_disp32` 加回 C；不把它改成弱符号；不在 13097 条前缀对不上的编译器上重编 `backend_enc_dispatch.o`；不把这 44 条 `sub sp, #0x880` 当成 w879 的 28 条 `sub sp, #0x870` 或 `x_frontend` 那组 i32 两参的 28 条 `sub sp, #0x880`；它与 w878 的 `arch_arm64_enc_enc_ldr_xreg_xreg_imm` 同帧，符号和被调函数不同；不把 `arch_x86_64_enc_enc_call_reg` 加回 C；不把它改成弱符号；不在 13069 条前缀对不上的编译器上重编 `backend_enc_dispatch.o`；不把这 28 条 `sub sp, #0x870` 当成 w878 的 44 条 `sub sp, #0x880` 或 `x_frontend` 那组 i32 两参的 28 条 `sub sp, #0x880`；不把 `arch_arm64_enc_enc_ldr_xreg_xreg_imm` 加回 C；不把它改成弱符号；不在 13025 条前缀对不上的编译器上重编 `backend_enc_dispatch.o`；不把这 44 条 `sub sp, #0x880` 当成 w877 的 28 条 `sub sp, #0x870` 或 `x_frontend` 那组 i32 两参的 28 条 `sub sp, #0x880`；不把 `arch_arm64_enc_enc_blr` 加回 C；不把它改成弱符号；不拿掉 `addsd` 门；不改走 full `.x`；不把 `pipeline_module_import_path_byte_at_u8_ptr_i32_i32_retu8` 加回 C；不把它改成强符号；不在 920 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`；不把这条 `u8` 三参转发当成零参 `*u8`、`*u8` 两参、i32 单参、void 三参或 i32 两参；不把 `pipeline_asm_emit_dep_pipe_c_retu8_ptr` 加回 C；不把它改成强符号；不在 906 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`；不把 `pipeline_dep_ctx_module_at_u8_ptr_i32_retu8_ptr` 加回 C；不把它改成强符号；不在 880 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`；不把剩余的零参指针返回或 uint8 返回别名当成 `*u8` 两参转发；不把 `pipeline_dep_ctx_ndep_u8_ptr_reti32` 加回 C；不把它改成强符号；不在 860 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`；不把剩余的指针返回或 uint8 返回别名当成 i32 单参转发；不把 `pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 加回 C；不把它改成强符号；不在 833 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`；不把剩余的指针返回或 uint8 返回别名当成 void 三参转发；不把 `pipeline_expr_binop_right_ref_at_u8_ptr_i32_reti32` 加回 C；不把它改成强符号；不在 805 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`；不把 `pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 加回 C；不把它改成强符号；不在 777 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`；不把 `pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 加回 C；不把它改成强符号；不在 749 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`；不把 `pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 加回 C；不把它改成强符号；不在 721 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`；不把 `glue_codegen_import_path_to_c_prefix_into_u8_ptr_u8_ptr_i32` 加回 C；不把它改成 weak；不在 694 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`；不移动结构返回；不把其余 `XLANG_WEAK` 别名改成强符号；不把 `glue_try_std_heap_redirect_sym_local_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；不把它改成 weak；不在 652 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`；不把 `glue_asm_build_import_binding_call_sym_u8_ptr_i32_u8_ptr_i32_u8_ptr_reti32` 加回 C；不把它改成 weak；不在 604 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`；不把 `glue_asm_build_func_export_sym_c_u8_ptr_u8_ptr_i32_u8_ptr_i32_reti32` 加回 C；不把它改成 weak；不在 556 条前缀对不上的编译器上重编 `x_frontend_link_alias.o`；不把 `pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 加回 C；不把编码 marker 加回产品路径的 C；不改走 full `.x`；不拿掉 `addsd` 门；不在前缀对不上的编译器上重编 `backend_enc_dispatch.o`；不把 preamble／parse／emit／arena marker 加回 C；不在前缀对不上的编译器上重编那四个对象；不把 arch-emit 种子加回去；不把五份 thin 种子加回去；不重开编码薄层冷路径；不重开诊断冷路径；不重开九个别名；不重开 `async_asm_pool`；不重链对不上的 try_inline／call／simd；不跑阶梯重编 `cfg_eval.o`）；不要优先 `.x` 整包重编 `runtime_driver_no_c.o`；`rt_stack` CG002 不拿 gcc `-E` 修；pabi 活 rest 的 thin 面仍是 HARD BAN；Ubuntu 在能打开 `std.fmt` 之前不重链；L4／升钉仍经理另派 |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举效率方法-M2主链.md](自举效率方法-M2主链.md) | M2 主链 |
| [C迁移追踪.md](C迁移追踪.md) | 终局债（短状态表） |
| **本文** | KPI · 前排 · 波次流水（**9 月 24 日**） |
| [自举进度-归档-2026-09-23.md](自举进度-归档-2026-09-23.md) | **9 月 23 日**全日弧（止于 w859／Class DB） |
| [自举进度-归档-2026-09-22.md](自举进度-归档-2026-09-22.md) | **9 月 22 日**全日弧（w749–w778／Class F–AC） |

***

## 2. 仪表盘

| 项 | 结果 |
|----|------|
| 产品 bstrict | **131** 套（双端 tip 全绿＠`eab2c15b1`） |
| 钉盘 | **`ecdb5cc1e`**（不升） |
| MG／Stage8 | MG ✅ · Stage8 **30/30** |
| SSH | `ubuntu-server`／`windows-server` |
| tip | **三端 L2 硬闸已启用** · Darwin 本波重链 **5/5**（3929608，06:13，sha256 前缀 `bd226a448e12e70b`） · Ubuntu 15:46 tip／Win `xlang_asm.exe` 22:42 本波未重链，也不重编 `backend_enc_dispatch.o`；既有二进制 L2 **5/5** |
| host-cc 真债 | BC present **0**；余＝map 外 seed／from_x／产品 `.inc`／大户 leftover 真减 |

***

## 3. 当前前排（≤3）

1. **主迁刀** — **M2 大户真减 host-cc**：w885 把 `backend_enc_append_u32_le_c_impl` 收进 `.x`，它把一个 32 位字的四个小端字节交给 `pipeline_elf_ctx_append_bytes`，这是 i32 与 u32 两参，98 条指令，`sub sp, #0x880`，符号仍强。f64／Cap 尾、`backend_enc_arm64_call_c_impl`、`backend_enc_x86_jcc_rel32_c_impl`、被调函数和分发器仍 host-cc。w884 把 `backend_enc_append_u8_c_impl` 收进 `.x`，它把一个字节的低 8 位交给 `pipeline_elf_ctx_append_bytes`，这是 i32 两参，54 条指令，`sub sp, #0x880`，符号仍强。f64／Cap 尾、arm64 call、被调函数和分发器仍 host-cc。w883 把 `arch_x86_64_enc_enc_cdqe_rax_impl` 收进 `.x`，它自己发出 `0x48 0x98`，这是 i32 单参，57 条指令，`sub sp, #0x870`，符号仍强。f64／Cap 尾、被调函数和分发器仍 host-cc。w882 把 `arch_riscv64_enc_enc_ldr_xreg_xreg_imm` 收进 `.x`，仍转调 `backend_enc_riscv64_ldr_xreg_xreg_imm_c`，这是 i32 四参，`sub sp, #0x880`，符号仍强。`arch_*_enc_enc_*` 单行转发已经不在 C。f64／Cap 尾仍 host-cc。w881 把 `arch_riscv64_enc_enc_jalr_reg` 收进 `.x`，仍转调 `backend_enc_riscv64_jalr_reg_c`，这是 i32 两参，`sub sp, #0x870`，符号仍强。f64／Cap 尾仍 host-cc。w880 把 `arch_x86_64_enc_enc_load_rax_rbx_disp32` 收进 `.x`，仍转调 `backend_enc_x86_64_load_rax_rbx_disp32_c`，这是 i32 四参，`sub sp, #0x880`，符号仍强。f64／Cap 尾仍 host-cc。w879 把 `arch_x86_64_enc_enc_call_reg` 收进 `.x`，仍转调 `backend_enc_x86_64_call_reg_c`，这是 i32 两参，`sub sp, #0x870`，符号仍强。f64／Cap 尾仍 host-cc。w878 把 `arch_arm64_enc_enc_ldr_xreg_xreg_imm` 收进 `.x`，仍转调 `backend_enc_arm64_ldr_xreg_xreg_imm_c`，这是 i32 四参，`sub sp, #0x880`，符号仍强。w877 把 `arch_arm64_enc_enc_blr` 收进 `.x`，仍转调 `backend_enc_arm64_blr_c`，这是 i32 两参，`sub sp, #0x870`，符号仍强。lexer 结构返回尾仍 host-cc。字符串表仍 host-cc。恢复诊断仍 host-cc。BSS／lib-name／入口前缀仍 host-cc。128MiB／2MiB BSS 仍 host-cc。诊断 asm BSS 仍 host-cc。`lsp_diag_pipeline_ctx` 的 C 尾仍 host-cc。`cfg_eval` 后段阶梯仍 host-cc。不重编 `runtime_driver_no_c.o`。不重开 `async_asm_pool`。不跑阶梯重编 `cfg_eval.o`。`rt_stack.x` CG002，不用 gcc `-E` 修。pabi 活 rest 仍 host-cc。Ubuntu／Windows 本波产品不重链。
2. **次排** — Ubuntu tip leftover；L6／L8 tip asm 码生成（指针下标）治本后可再 prefer。禁 mega FORCE。
3. **立卡不动** — arr_return Soft-Cap；MSVC va；eq 深链；spawn 真体；nest 64；L4／升钉仅经理另派。

***

## 4. 方法论要点

- 权威残量口径：BC inventory＝结构孤儿（present **0**）；真残＝map 外 seed／from_x／`.inc`／Cap always-mega／Darwin leftover 大户。
- 禁：CloudAgent／升钉／默 bstrict／glue_tail 主刀／mega FORCE／Soft-Cap tip `-E`／HARD BAN 分类主刀／FORCE 冲 Darwin leftover hybrid。
- Win：`XLANG_LEGACY_C_FRONTEND=1` 默认勿破。
- Ubuntu：tip 全量重链 SEGV 时用 **warm** 金标；硬闸仍启用。

***

## 5. 波次流水（倒序 · 9 月 24 日起）

| 日期 | 波 | 一行 |
|------|-----|------|
| 2026-09-24 | **wave885** | 把 `backend_enc_append_u32_le_c_impl` 收进 `backend_enc_dispatch_thin.x`，它把一个 32 位字的四个小端字节交给 `pipeline_elf_ctx_append_bytes`，符号仍强；这是 i32 与 u32 两参，98 条指令，`sub sp, #0x880`；既有薄层是链上对象的精确前缀（13324 条指令，到 `0xd030`）；去掉 35 条 clang 指令后其余尾指令字不变；对象文件 91728，`__text` 61208；Darwin 重链 L2 **5/5**（3929608，06:13，sha256 前缀 `bd226a448e12e70b`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 73248／2026-09-23 13:17，Windows 对象 62251／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave884** | 把 `backend_enc_append_u8_c_impl` 收进 `backend_enc_dispatch_thin.x`，它把一个字节的低 8 位交给 `pipeline_elf_ctx_append_bytes`，符号仍强；这是 i32 两参，54 条指令，`sub sp, #0x880`；既有薄层是链上对象的精确前缀（13270 条指令，到 `0xcf58`）；去掉 23 条 clang 指令后其余尾指令字不变；对象文件 91520，`__text` 60956；Darwin 重链 L2 **5/5**（3929608，06:00，sha256 前缀 `7873746d35e065f3`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 73248／2026-09-23 13:17，Windows 对象 62251／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave883** | 把 `arch_x86_64_enc_enc_cdqe_rax_impl` 收进 `backend_enc_dispatch_thin.x`，它自己把 `0x48 0x98` 交给 `pipeline_elf_ctx_append_bytes`，符号仍强；这是 i32 单参，57 条指令，`sub sp, #0x870`；既有薄层是链上对象的精确前缀（13213 条指令，到 `0xce74`）；去掉 21 条 clang 指令后其余尾指令字不变；对象文件 91432，`__text` 60832；Darwin 重链 L2 **5/5**（3929608，05:45，sha256 前缀 `d2fc13253f39d594`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 73248／2026-09-23 13:17，Windows 对象 62251／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave882** | 把 `arch_riscv64_enc_enc_ldr_xreg_xreg_imm` 收进 `backend_enc_dispatch_thin.x`，仍转调 `backend_enc_riscv64_ldr_xreg_xreg_imm_c`，符号仍强；这是 i32 四参，44 条指令，`sub sp, #0x880`；既有薄层是链上对象的精确前缀（13169 条指令，到 `0xcdc4`）；去掉 15 条 clang 指令后其余尾指令字不变；对象文件 91408，`__text` 60688；Darwin 重链 L2 **5/5**（3929672，05:26，sha256 前缀 `dbabd1b00d02b654`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 73248／2026-09-23 13:17，Windows 对象 62251／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave881** | 把 `arch_riscv64_enc_enc_jalr_reg` 收进 `backend_enc_dispatch_thin.x`，仍转调 `backend_enc_riscv64_jalr_reg_c`，符号仍强；这是 i32 两参，28 条指令，`sub sp, #0x870`；既有薄层是链上对象的精确前缀（13141 条指令，到 `0xcd54`）；去掉 11 条 clang 指令后其余尾指令字不变；对象文件 91328，`__text` 60572；Darwin 重链 L2 **5/5**（3929672，05:15，sha256 前缀 `1372cda497b48dbc`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 73248／2026-09-23 13:17，Windows 对象 62251／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave880** | 把 `arch_x86_64_enc_enc_load_rax_rbx_disp32` 收进 `backend_enc_dispatch_thin.x`，仍转调 `backend_enc_x86_64_load_rax_rbx_disp32_c`，符号仍强；这是 i32 四参，44 条指令，`sub sp, #0x880`；既有薄层是链上对象的精确前缀（13097 条指令，到 `0xcca4`）；去掉 15 条 clang 指令后其余尾指令字不变；对象文件 91304，`__text` 60504；Darwin 重链 L2 **5/5**（3929672，05:03，sha256 前缀 `e14d803bae7da3ea`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 73248／2026-09-23 13:17，Windows 对象 62251／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave879** | 把 `arch_x86_64_enc_enc_call_reg` 收进 `backend_enc_dispatch_thin.x`，仍转调 `backend_enc_x86_64_call_reg_c`，符号仍强；这是 i32 两参，28 条指令，`sub sp, #0x870`；既有薄层是链上对象的精确前缀（13069 条指令，到 `0xcc34`）；去掉 11 条 clang 指令后其余尾指令字不变；对象文件 91224，`__text` 60388；Darwin 重链 L2 **5/5**（3929672，04:52，sha256 前缀 `48fc031c09febdf2`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 73248／2026-09-23 13:17，Windows 对象 62251／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave878** | 把 `arch_arm64_enc_enc_ldr_xreg_xreg_imm` 收进 `backend_enc_dispatch_thin.x`，仍转调 `backend_enc_arm64_ldr_xreg_xreg_imm_c`，符号仍强；这是 i32 四参，44 条指令，`sub sp, #0x880`；既有薄层是链上对象的精确前缀（13025 条指令，到 `0xcb84`）；去掉 15 条 clang 指令后其余尾指令字不变；对象文件 91200，`__text` 60320；Darwin 重链 L2 **5/5**（3929672，04:39，sha256 前缀 `3e758c9665ec5de3`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 73248／2026-09-23 13:17，Windows 对象 62251／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave877** | 把 `arch_arm64_enc_enc_blr` 收进 `backend_enc_dispatch_thin.x`，仍转调 `backend_enc_arm64_blr_c`，符号仍强；这是 i32 两参，28 条指令，`sub sp, #0x870`；既有薄层是链上对象的精确前缀（12997 条指令，到 `0xcb14`）；去掉 11 条 clang 指令后其余尾指令字不变；对象文件 91120，`__text` 60204；Darwin 重链 L2 **5/5**（3929672，04:25，sha256 前缀 `4c2d288e02614681`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 73248／2026-09-23 13:17，Windows 对象 62251／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave876** | 把 `pipeline_module_import_path_byte_at_u8_ptr_i32_i32_retu8` 收进 `x_frontend_link_alias.x`，仍转调 `pipeline_module_import_path_byte_at`，符号仍弱；这是 `u8` 三参，36 条指令，`sub sp, #0x880`；既有 `.x` 面是 03:42 对象的精确前缀（920 条指令，到 `0xe60`）；去掉 13 条 clang 指令后 lexer 尾指令字不变；对象文件 8360，`__text` 3980；Darwin 重链 L2 **5/5**（3929672，03:55，sha256 前缀 `4ed2d9d9ab64fd89`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 9264／2026-09-20 06:51，Windows 对象 8760／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave875** | 把 `pipeline_asm_emit_dep_pipe_c_retu8_ptr` 收进 `x_frontend_link_alias.x`，仍转调 `pipeline_asm_emit_dep_pipe_c`，符号仍弱；这是零参 `*u8`，14 条指令，`sub sp, #0x870`；既有 `.x` 面是 03:24 对象的精确前缀（906 条指令，到 `0xe28`）；去掉 5 条 clang 指令后 lexer 尾与 uint8 别名指令字不变；对象文件 8304，`__text` 3888；Darwin 重链 L2 **5/5**（3929672，03:42，sha256 前缀 `2831d49204e7a198`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 9264／2026-09-20 06:51，Windows 对象 8760／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave874** | 把 `pipeline_dep_ctx_module_at_u8_ptr_i32_retu8_ptr` 收进 `x_frontend_link_alias.x`，仍转调 `pipeline_dep_ctx_module_at`，符号仍弱；这是 `*u8` 两参，26 条指令，`sub sp, #0x880`；既有 `.x` 面是 03:11 对象的精确前缀（880 条指令，到 `0xdc0`）；去掉 11 条 clang 指令后 lexer 尾与其余别名指令字不变；对象文件 8312，`__text` 3852；Darwin 重链 L2 **5/5**（3929672，03:24，sha256 前缀 `51183947b30ce322`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 9264／2026-09-20 06:51，Windows 对象 8760／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave873** | 把 `pipeline_dep_ctx_ndep_u8_ptr_reti32` 收进 `x_frontend_link_alias.x`，仍转调 `pipeline_dep_ctx_ndep`，符号仍弱；这是 i32 单参，20 条指令，`sub sp, #0x870`；既有 `.x` 面是 02:56 对象的精确前缀（860 条指令，到 `0xd70`）；去掉 9 条 clang 指令后 lexer 尾与其余别名指令字不变；对象文件仍 8288，`__text` 3792；Darwin 重链 L2 **5/5**（3929672，03:11，sha256 前缀 `ee6427c645ae2119`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 9264／2026-09-20 06:51，Windows 对象 8760／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave872** | 把 `pipeline_expr_field_access_name_into_u8_ptr_i32_u8_ptr` 收进 `x_frontend_link_alias.x`，仍转调 `pipeline_expr_field_access_name_into`，无后缀实现返回 void，符号仍弱；既有 `.x` 面是 02:39 对象的精确前缀（833 条指令，到 `0xd04`）；新转发 27 条指令，`sub sp, #0x880`；去掉 13 条 clang 指令后 lexer 尾与其余别名指令字不变；对象 8288；Darwin 重链 L2 **5/5**（3929672，02:56，sha256 前缀 `8e1a9991b3d41bec`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 9264／2026-09-20 06:51，Windows 对象 8760／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave871** | 把 `pipeline_expr_binop_right_ref_at_u8_ptr_i32_reti32` 收进 `x_frontend_link_alias.x`，仍转调 `pipeline_expr_binop_right_ref_at`，符号仍弱；既有 `.x` 面是 02:28 对象的精确前缀（805 条指令，到 `0xc94`）；新转发 28 条指令，`sub sp, #0x880`；去掉 11 条 clang 指令后 lexer 尾与其余别名指令字不变；对象 8272；Darwin 重链 L2 **5/5**（3929672，02:39，sha256 前缀 `1d460cd90f5f3df5`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 9264／2026-09-20 06:51，Windows 对象 8760／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave870** | 把 `pipeline_expr_binop_left_ref_at_u8_ptr_i32_reti32` 收进 `x_frontend_link_alias.x`，仍转调 `pipeline_expr_binop_left_ref_at`，符号仍弱；既有 `.x` 面是 02:16 对象的精确前缀（777 条指令，到 `0xc24`）；新转发 28 条指令，`sub sp, #0x880`；去掉 11 条 clang 指令后 lexer 尾与其余别名指令字不变；对象 8240；Darwin 重链 L2 **5/5**（3929672，02:28，sha256 前缀 `02cf6d45de9adf09`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 9264／2026-09-20 06:51，Windows 对象 8760／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave869** | 把 `pipeline_expr_field_access_base_ref_u8_ptr_i32_reti32` 收进 `x_frontend_link_alias.x`，仍转调 `pipeline_expr_field_access_base_ref`，符号仍弱；既有 `.x` 面是 02:02 对象的精确前缀（749 条指令，到 `0xbb4`）；新转发 28 条指令，`sub sp, #0x880`；去掉 11 条 clang 指令后 lexer 尾与其余别名指令字不变；对象 8216；Darwin 重链 L2 **5/5**（3929672，02:16，sha256 前缀 `8a911fc6906fdccd`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 9264／2026-09-20 06:51，Windows 对象 8760／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave868** | 把 `pipeline_expr_field_access_name_len_u8_ptr_i32_reti32` 收进 `x_frontend_link_alias.x`，仍转调 `pipeline_expr_field_access_name_len`，符号仍弱；既有 `.x` 面是 01:45 对象的精确前缀（721 条指令，到 `0xb44`）；新转发 28 条指令，`sub sp, #0x880`；去掉 11 条 clang 指令后 lexer 尾与其余别名指令字不变；对象 8184；Darwin 重链 L2 **5/5**（3929672，02:02，sha256 前缀 `75e247bc72708346`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 9264／2026-09-20 06:51，Windows 对象 8760／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave867** | 把 `glue_codegen_import_path_to_c_prefix_into_u8_ptr_u8_ptr_i32` 收进 `x_frontend_link_alias.x`，仍转调 `glue_codegen_import_path_to_c_prefix_into`，无后缀实现返回 void，符号仍强；既有 `.x` 面是 01:33 对象的精确前缀（694 条指令，到 `0xad8`）；新转发 27 条指令，`sub sp, #0x880`；去掉 13 条 clang 指令后 lexer 尾与 `XLANG_WEAK` 簇指令字不变；对象 8160；Darwin 重链 L2 **5/5**（3929672，01:45，sha256 前缀 `0729a97ede1570e2`）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 9264／2026-09-20 06:51，Windows 对象 8760／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave866** | 把 `glue_try_std_heap_redirect_sym_local_u8_ptr_i32_u8_ptr_i32_reti32` 收进 `x_frontend_link_alias.x`，仍转调 `glue_try_std_heap_redirect_sym_local`，符号仍强；既有 `.x` 面是 01:20 对象的精确前缀（652 条指令，到 `0xa30`）；新转发 42 条指令，`sub sp, #0x890`；去掉 15 条 clang 指令后 lexer 尾与其余别名指令字不变；对象 8144；Darwin 重链 L2 **5/5**（3929672，01:33）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 9264／2026-09-20 06:51，Windows 对象 8760／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave865** | 把 `glue_asm_build_import_binding_call_sym_u8_ptr_i32_u8_ptr_i32_u8_ptr_reti32` 收进 `x_frontend_link_alias.x`，仍转调 `glue_asm_build_import_binding_call_sym`，符号仍强；18 个别名加 type_kind 与 func_export 转发是 01:08 对象的精确前缀（604 条指令，到 `0x970`）；去掉 17 条 clang 指令后 lexer 尾与其余别名指令字不变；对象 8072；Darwin 重链 L2 **5/5**（3929656，01:21）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 9264／2026-09-20 06:51，Windows 对象 8760／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | **wave864** | 把 `glue_asm_build_func_export_sym_c_u8_ptr_u8_ptr_i32_u8_ptr_i32_reti32` 收进 `x_frontend_link_alias.x`，仍转调 `glue_asm_build_func_export_sym_c`，符号仍强；18 个别名加 type_kind 转发是 00:54 对象的精确前缀（556 条指令，到 `0x8b0`）；去掉 17 条 clang 指令后 lexer 尾与其余别名指令字不变；对象 7992；Darwin 重链 L2 **5/5**（3929656，01:08）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 9264／2026-09-20 06:51，Windows 对象 8760／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | wave863 | 把 `pipeline_type_kind_ord_at_u8_ptr_i32_reti32` 收进 `x_frontend_link_alias.x`，仍转调 `pipeline_type_kind_ord_at`，符号仍强；18 个别名 `__text` 是 20:10 对象的精确前缀（528 条指令）；去掉 11 条 clang 指令后 lexer 尾与其余别名指令字不变；对象 7904；Darwin 重链 L2 **5/5**（3929656，00:55）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象（Ubuntu 对象 9264／2026-09-20 06:51，Windows 对象 8760／2026-09-22 13:41），既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | wave862 | 把 `backend_enc_dispatch_slice_marker` 收进 `backend_enc_dispatch_thin.x`，仍返回 1；种子留 f64／Cap 尾；薄层 `__text` 是 22:22 对象的精确前缀（12987 条指令）；去掉末尾 2 条 `mov`／`ret` 后尾部指令字不变；`__const` 170 字节不变；对象 91096；Darwin 重链 L2 **5/5**（3929656，00:37）；Ubuntu 15:46／Win 22:42 本波不重链、不重编该对象，既有二进制 L2 **5/5**；钉盘不升 |
| 2026-09-24 | wave861 | 把 `labi_rt_preamble_slice_marker` 收进 `rt_preamble.x`，仍返回 1；种子留字符串表；两个写入函数的 `__text` 是 19:02 对象的精确前缀（271 条指令）；字符串表字节不变；对象 52320；Darwin 重链 L2 **5/5**（3929656，00:19）；Ubuntu 15:46／Win 22:42 既有二进制 L2 **5/5**，不重链、不重编该对象；钉盘不升 |
| 2026-09-24 | wave860 | 把 `labi_rt_parse_diag_slice_marker` 收进 `rt_parse_diag.x`，仍返回 1；种子留恢复诊断；精确诊断的 `__text` 是 19:56 对象的精确前缀（1438 条指令）；去掉中间 2 条 `mov`／`ret` 后恢复诊断与静态辅助指令字不变；对象 16784；Darwin 重链 L2 **5/5**（3929656，00:02）；Ubuntu 15:46／Win 22:42 既有二进制 L2 **5/5**，不重链、不重编该对象；钉盘不升 |
| 2026-09-23 | wave859–w824 等 | 见 [自举进度-归档-2026-09-23.md](自举进度-归档-2026-09-23.md) |
