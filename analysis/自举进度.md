# Xlang / X language 自举进度

> **更新**：2026-09-05 · tip **🔔 升钉 `e8176cbe5` → `b5be5ed97`（双端 L4 129）** · 日切：4 日 **40** 行已入 [归档](自举进度-归档-2026-09-04.md) · **金标 SSH＝`ubuntu-server`** · Ubuntu 验＝**git pull（禁 scp）** · Ubuntu L2 **钉盘节奏**\
> **产品 L4 放行钉盘**：**`b5be5ed97`**（2026-09-02 升钉 · 前 `e8176cbe5` 2026-08-23 · 再前 `f7424ae47` 2026-08-15）\
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅**（12 labi_*.x 切片 prefer；默认 FROM_X=1）· parser **7.2.2 关 pin ✅**（tip `-E` + assemble；产品默认 FROM_X=1）· codegen 7.4.2 wave323 ✅ · typeck 7.4.1 wave322 ✅ · runtime monofile wave321 ✅ · present **0** · 钉盘 **`b5be5ed97`** · Stage 8 tip **wave332 Track L 30/30 FULLY CLOSED**（**PRODUCT HALF=0** · **PRODUCT PINNED=0** · **PRODUCT RETIRED 23/23 = 100%**）· **7.4.3 PREFER_X_O 双端 8/8 wave338 ✅** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁**\
> **tip L4 安全网（历史）**：**`ffc0b646f`**（已由 tip L4 升钉吸收；前网 `4bacaf18c`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：Windows 纯 asm：邻域 **6／6** ＋同文件 call／两 let／赋值／while／for／break／continue／`loop`／match／数组 INDEX／STRUCT_LIT＋FIELD／裸 STRUCT_LIT.field／CALL.field `mk().x`／nested `mk().p.f`／pointer intermediate／METHOD.field `P.mk().x`／`P.mk().get()`／`P.get(p)`／guarded match／match subject CALL／INDEX／FIELD／const-int guard `gfalse`／9–16B dual-GP CALL.field／sret ＞16B CALL.field／`take(p: *P) { p.x }`／`x as i32`／ADDR_OF／DEREF `unsafe { return *p }`／let-init DEREF／Cap-fn-ptr `foo as *u8`／TYPE_FN `function(): i32` 间接 call／参／字段／ADDR_OF INDEX／FIELD `&a[0]`／`&s.x`／TYPE_FN return `return foo`／TYPE_FN CALL of CALL `getf()()`／nested ADDR_OF `&s.a[0]`／`&a[0].x`／`&*p`／ADDR_OF CALL／STRUCT_LIT 基 `&mk().x`／`&(P { x: 3 }).x`／`&P.mk().x`／ARRAY_LIT TYPE_FN `[foo, bar]`／`fs[0]()`／STRUCT_LIT 字段 `H { fs: [foo] }`／`h.fs[0]()`／作参 `apply([foo])`／16B 作参 `apply([foo, bar])`／TYPE_SLICE 作参 `take([foo])`／VAR TYPE_SLICE packing lea fat\*／TYPE_SLICE TYPE_FN let-init COMMON BSS／INDEX TYPE_SLICE 形参 bounds／nso_at 非 RETURN expr-stmt／INDEX TYPE_SLICE 形参／局部 store／ADDR_OF INDEX TYPE_SLICE `&xs[0]`／ADDR_OF INDEX TYPE_SLICE 非字面量 `&xs[i]`／INDEX DEREF `*[]i32` `(*p)[1]`／TYPE_SLICE DEREF 作参 `take(*p)`／CALL 返回 TYPE_SLICE 作参 `take(mk())`／FIELD TYPE_SLICE 作参 `take(s.xs)`／CALL 返回 TYPE_SLICE let-init `let s = mk()`／CALL 返回 TYPE_SLICE let-init 局部 `.length`／ARRAY_LIT TYPE_SLICE 局部 `.length`／TYPE_ARRAY 8B DEREF 作参 `take(*p)` of `*[2]i32`／FIELD TYPE_ARRAY 8B 作参 `take(s.a)`／CALL 返回 TYPE_ARRAY 8B 作参 `take(mk())`／INDEX of `[2][2]i32` 作 TYPE_ARRAY 形参 `take(rows[1])`／FIELD TYPE_ARRAY 16B 作参 `take(s.a)` of `[4]i32`／TYPE_ARRAY DEREF let-init `unsafe { let y: [2]i32 = *p }`／TYPE_ARRAY 整数组赋值 `y = *p`／TYPE_ARRAY dest-in-rbx `*p = a`／TYPE_ARRAY 整数组 return `return a`／TYPE_NAMED dest-in-rbx STRUCT_LIT `*p = P { x: 3, y: 4 }`／TYPE_SLICE dest-in-rbx ARRAY_LIT `*p = [3, 4]`／TYPE_SLICE dest-in-rbx FIELD `*p = s.xs`／TYPE_SLICE dest-in-rbx INDEX `*p = rows[1]`／TYPE_SLICE VAR dest FIELD `b = s.xs`／TYPE_SLICE let-init INDEX `let s = rows[1]`／ADDR_OF INDEX of `[2][]i32` `&rows[1]`／TYPE_SLICE DEREF let-init `unsafe { let s: []i32 = *p }`／TYPE_SLICE VAR let-init `let s: []i32 = xs`／TYPE_SLICE dest-in-rbx VAR `*p = xs`／TYPE_NAMED dest-in-rbx STRUCT_LIT `(*p).y`／**TYPE_NAMED dest-in-rbx MATCH 8B**／**TYPE_NAMED dest-in-rbx MATCH 16B**／**TYPE_NAMED dest-in-rbx MATCH 24B**／**TYPE_NAMED dest-in-rbx nested STRUCT_LIT**／**TYPE_NAMED dest-in-rbx MATCH arm VAR**／**TYPE_NAMED dest-in-rbx MATCH arm CALL**／**TYPE_NAMED dest-in-rbx MATCH arm CALL 24B**／**TYPE_NAMED dest-in-rbx MATCH arm CALL 32B**／**TYPE_NAMED dest-in-rbx CALL**／**TYPE_NAMED dest-in-rbx METHOD**／**TYPE_NAMED dest-in-rbx MATCH arm METHOD**／**TYPE_ARRAY dest-in-rbx MATCH**／**CALL.field 32B** `mk().h`／**VAR dest MATCH arm CALL** `d = match { 1 => mk(); }`／**TYPE_ARRAY dest-in-rbx MATCH arm CALL** `*p = match { 1 => mk(); }`／**TYPE_SLICE dest-in-rbx MATCH** `*p = match { 1 => [3, 4]; }`／**TYPE_SLICE VAR dest MATCH** `d = match { 1 => [3, 4]; }`（`slice_asg_match` RUN=4／`_len` RUN=2）。leftover unique **0**。钉盘仍 `b5be5ed97`。
> **下波候选**：leftover-PE `arr_star_match_field` 拦截已写＠`b7d6f175a`，**未勾**（WIN leftover unique emit_match；POSIX FROM_X ABSENT）。日常闸门＝**双端 L2 产品矩阵**（mac+Ubuntu 5/5＠`b7d6f175a`）。leftover unique dest-parked FIELD／INDEX 已覆盖 dest_tk 11／0／10。leftover-PE dest-in-rbx MATCH 要 Windows leftover-PE hybrid L2。下一未认领：VAR dest TYPE_ARRAY MATCH（rec ASSIGN TYPE_ARRAY 只拦 DEREF dest）。WAVE277 ALWAYS 已 T `rebuild_sparse_ifs`（**禁** leftover rest 第二份 dual-def）。钉盘不升。

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1；过程上 prefer+heat 一次／域，双端矩阵同波一次\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；**日常闸门＝SHARED 双端 L2 产品矩阵**（mac + Ubuntu `./xbuild l2-matrix`；wave299：捕获 rv=42／opt=102 成功码，禁 set -e 截断）。leftover-PE unique（`!FROM_X` 或 `WIN_LEFTOVER`）不进 leftover rest FROM_X 产品；leftover-PE 探针红 **不**当双端 L2 产品红。谈自举／升钉才 L4 + 矩阵 + 全量 bstrict；bstrict **per-script timeout** Ubuntu 300s／Darwin L4 1200s + catalog cache；**墙钟**：`product_l4` 分步 wall + `run-all-bstrict`（`XLANG_BSTRICT_TIMING_FILE`）；并行 **`XLANG_BSTRICT_JOBS` 默认 1**（Ubuntu 金标 1；Darwin L4 默认 1／GATE_CASE_JOBS=1；JOBS=8／4／2 本机 OOM）

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[04](自举进度-归档-2026-09-04.md) · [03](自举进度-归档-2026-09-03.md) · [02](自举进度-归档-2026-09-02.md) · [01](自举进度-归档-2026-09-01.md) · [31](自举进度-归档-2026-08-31.md) · [30](自举进度-归档-2026-08-30.md) · [29](自举进度-归档-2026-08-29.md) · [28](自举进度-归档-2026-08-28.md) · [27](自举进度-归档-2026-08-27.md) · [26](自举进度-归档-2026-08-26.md) · [25](自举进度-归档-2026-08-25.md) · [24](自举进度-归档-2026-08-24.md) · [23](自举进度-归档-2026-08-23.md) · [22](自举进度-归档-2026-08-22.md) · [21](自举进度-归档-2026-08-21.md) · [20](自举进度-归档-2026-08-20.md) · [19](自举进度-归档-2026-08-19.md) · [18](自举进度-归档-2026-08-18.md) · [17](自举进度-归档-2026-08-17.md) · [16](自举进度-归档-2026-08-16.md) · [15](自举进度-归档-2026-08-15.md) · [14](自举进度-归档-2026-08-14.md) · [13](自举进度-归档-2026-08-13.md) · [12](自举进度-归档-2026-08-12.md) · [11](自举进度-归档-2026-08-11.md) · [09](自举进度-归档-2026-08-09.md) · [08](自举进度-归档-2026-08-08.md) · [07](自举进度-归档-2026-08-07.md) · [06](自举进度-归档-2026-08-06.md) · [05](自举进度-归档-2026-08-05.md) · [04](自举进度-归档-2026-08-04.md) · [03](自举进度-归档-2026-08-03.md) · [02](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · …\

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `b5be5ed97`**（2026-09-02 升钉 · 前 `e8176cbe5` 2026-08-23 · 再前 `f7424ae47` 2026-08-15）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**双端 Stage2 = ✅ SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709` · `xlang_asm_stage1`／`xlang_asm2` bit-identical）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（**变更记录只在本文 §6**）。\
**本波主刀 = 双端 L2 产品矩阵＠`b7d6f175a`**（mac 5/5 ＋ Ubuntu 金标 5/5）。leftover-PE `arr_star_match_field` 拦截已写、**未勾**（WIN leftover unique；要 Windows leftover-PE hybrid）。钉盘仍 `b5be5ed97`。

| 指标 | 状态 |
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`b5be5ed97`**；residual tip present **0**；Stage 8 **30/30 FULLY CLOSED**；PRODUCT RETIRED 23/23 |
| Track MG | **11.3.1 MG ✅** |
| Track BC | **🟢 Stage8 30/30** · M4 **5/5** · present **0**；`pipeline_x` mega 仍 host-cc |
| Track L | **收 30/30 FULLY CLOSED** |
| mac / Ubuntu L4（钉盘） | ✅ 双端 L4 + bstrict **129** @ **`b5be5ed97`** |
| pin 蛋 | ✅ darwin.arm64 + linux.x86_64（本波 `xlang_asm`） |
| 本波构建级别 | 钉盘 **L4 已结＠b5be5ed97**；Ubuntu **129 OK** 17m47s；Darwin JOBS=1 **129 OK** 137m25s。check 闸门暂停 |

***

## 1. 文档地图

| 文档 | 看什么 |
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录**（**9 月 5 日**；4 日见 [归档](自举进度-归档-2026-09-04.md)） |
| [自举进度-归档-2026-09-04.md](自举进度-归档-2026-09-04.md) | **9月4日** 纯 asm 邻域→ARRAY_LIT TYPE_FN STRUCT_LIT 字段（**40** 行） |
| [自举进度-归档-2026-09-03.md](自举进度-归档-2026-09-03.md) | **9月3日** leftover unique 113→0／P2 Darwin `-o`／windows-gate 全绿（**90** 行） |
| [自举进度-归档-2026-09-02.md](自举进度-归档-2026-09-02.md) | **9月2日** 升钉 `b5be5ed97`／Darwin L4 JOBS／Windows leftover-PE hybrid unique 113→50（**52** 行） |
| [自举进度-归档-2026-09-01.md](自举进度-归档-2026-09-01.md) | **9月1日** typeck pin 7.4.6–8／L4 多轮／Cap va／stage10（**131** 行） |
| [自举进度-归档-2026-08-31.md](自举进度-归档-2026-08-31.md) | **8月31日** stage10 开刀／seed dest-SLICE／leftover／假权威诚实等（**113** 行） |
| [自举进度-归档-2026-08-30.md](自举进度-归档-2026-08-30.md) | **8月30日** leftover XLANG fallthrough F-06／F-07 等（**111** 行） |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图（本波不改） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单；per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`b5be5ed97`** | 2026-09-02 升钉；双端 L4 真冷 + 129 |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自钉盘 L4 `xlang_asm` 刷新 |
| residual tip（BC） | present **0** | Stage 8 30/30 · M4 5/5 · prefer pure-asm 默认 · pipeline_abi mega **硬禁** |
| mac 钉盘 L4 | ✅ L4 + bstrict **129** · 137m25s JOBS=1 | `@b5be5ed97` `/tmp/xlang_l4_Darwin_b5be5ed97_jobs1.log` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 + bstrict **129** · 17m47s | `@b5be5ed97` `ubuntu-server` `/tmp/xlang_l4_Linux_b5be5ed97.log` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | rv42／opt102／hello0／si0／f32 |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ 双端 tip SHA256 STRICT match | 钉盘历史见归档 |
| **R2 / Cap** | 按需 | 明细见 29／31／1 日归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1** |
| `TREE_ARMED` / `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1** / **1** |
| post-delete residual | **✅** wave944–964 |

***

## 3. 当前前排（≤3 条）

1. **Windows 纯 asm 邻域** — `return`／`3+4`／嵌套／`ifelse`／`let+return`／`void` **6／6 RUN 绿**；同文件 call／两 let／赋值／while／for／break／continue／`loop`／match／数组 INDEX／STRUCT_LIT＋FIELD／裸 STRUCT_LIT.field／CALL.field `mk().x`／nested `mk().p.f`／pointer intermediate／METHOD.field `P.mk().x`／`P.mk().get()`／`P.get(p)`／guarded match／match subject CALL／INDEX／FIELD／const-int guard／9–16B dual-GP CALL.field／sret ＞16B CALL.field／`take(p: *P) { p.x }`／`x as i32`／`unsafe { return *p }`／`let y: i32 = *p`／Cap-fn-ptr `foo as *u8`／TYPE_FN `function(): i32` 间接 call／参／字段／ADDR_OF INDEX／FIELD `&a[0]`／`&s.x`／TYPE_FN return `return foo`／TYPE_FN CALL of CALL `getf()()`／nested ADDR_OF `&s.a[0]`／`&a[0].x`／`&*p`／ADDR_OF CALL／STRUCT_LIT 基 `&mk().x`／`&(P { x: 3 }).x`／`&P.mk().x`／ARRAY_LIT TYPE_FN `[foo, bar]`／`fs[0]()`／STRUCT_LIT 字段 `H { fs: [foo] }`／作参 `apply([foo])`／16B `apply([foo, bar])`／TYPE_SLICE `take([foo])`／VAR packing／TYPE_FN let-init COMMON BSS／INDEX TYPE_SLICE 形参 bounds／nso_at 非 RETURN expr-stmt／INDEX TYPE_SLICE 形参／局部 store／ADDR_OF INDEX TYPE_SLICE `&xs[0]`／ADDR_OF INDEX TYPE_SLICE 非字面量 `&xs[i]`／INDEX DEREF `*[]i32` `(*p)[1]`／TYPE_SLICE DEREF 作参 `take(*p)`／CALL 返回 TYPE_SLICE 作参 `take(mk())`／FIELD TYPE_SLICE 作参 `take(s.xs)`／CALL 返回 TYPE_SLICE let-init `let s = mk()`／CALL 返回 TYPE_SLICE let-init 局部 `.length`／ARRAY_LIT TYPE_SLICE 局部 `.length`／TYPE_ARRAY 8B DEREF 作参 `take(*p)` of `*[2]i32`／FIELD TYPE_ARRAY 8B 作参 `take(s.a)`／CALL 返回 TYPE_ARRAY 8B 作参 `take(mk())`／INDEX of `[2][2]i32` 作 TYPE_ARRAY 形参 `take(rows[1])`／FIELD TYPE_ARRAY 16B 作参 `take(s.a)` of `[4]i32`／TYPE_ARRAY DEREF let-init `unsafe { let y: [2]i32 = *p }`／TYPE_ARRAY 整数组赋值 `y = *p`／TYPE_ARRAY dest-in-rbx `*p = a`／TYPE_ARRAY 整数组 return `return a`／TYPE_NAMED dest-in-rbx STRUCT_LIT `*p = P { x: 3, y: 4 }`／TYPE_SLICE dest-in-rbx ARRAY_LIT `*p = [3, 4]`／TYPE_SLICE dest-in-rbx FIELD `*p = s.xs`／TYPE_SLICE dest-in-rbx INDEX `*p = rows[1]`／TYPE_SLICE VAR dest FIELD `b = s.xs`／TYPE_SLICE let-init INDEX `let s = rows[1]`／ADDR_OF INDEX of `[2][]i32` `&rows[1]`／TYPE_SLICE DEREF let-init `unsafe { let s: []i32 = *p }`／TYPE_SLICE VAR let-init `let s: []i32 = xs`／TYPE_SLICE dest-in-rbx VAR `*p = xs`／TYPE_NAMED dest-in-rbx STRUCT_LIT `(*p).y`／**TYPE_NAMED dest-in-rbx MATCH 8B**／**TYPE_NAMED dest-in-rbx MATCH 16B**／**TYPE_NAMED dest-in-rbx MATCH 24B**／**TYPE_NAMED dest-in-rbx nested STRUCT_LIT**／**TYPE_NAMED dest-in-rbx MATCH arm VAR**／**TYPE_NAMED dest-in-rbx MATCH arm CALL**／**TYPE_NAMED dest-in-rbx MATCH arm CALL 24B**／**TYPE_NAMED dest-in-rbx MATCH arm CALL 32B**／**TYPE_NAMED dest-in-rbx CALL**／**TYPE_NAMED dest-in-rbx METHOD**／**TYPE_NAMED dest-in-rbx MATCH arm METHOD**／**TYPE_ARRAY dest-in-rbx MATCH**／**CALL.field 32B** `mk().h`／**VAR dest MATCH arm CALL** `d = match { 1 => mk(); }`／**TYPE_ARRAY dest-in-rbx MATCH arm CALL** `*p = match { 1 => mk(); }`／**TYPE_SLICE dest-in-rbx MATCH** `*p = match { 1 => [3, 4]; }`／**TYPE_SLICE VAR dest MATCH** `d = match { 1 => [3, 4]; }`。
2. **双端 L2＠`b7d6f175a`** — 产品矩阵 mac 5/5 ＋ Ubuntu 金标 5/5。leftover unique MATCH arm TYPE_ARRAY FIELD／INDEX dest-parked dest_tk==10（POSIX FROM_X ABSENT）。
3. **下一刀** — leftover-PE `arr_star_match_field` 未勾（要 Windows leftover-PE hybrid L2）。下一未认领：VAR dest TYPE_ARRAY MATCH。钉盘不升。

***

## 4. 与归档的关系

- **9月4日归档**：[自举进度-归档-2026-09-04.md](自举进度-归档-2026-09-04.md) — **40** 行 · leftover unique **0** · 钉盘 **`b5be5ed97`**。
- **9月3日及前**：见各日归档链接（§1）。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `b5be5ed97`**。
- 日切后主文档只保留 KPI + 前排 + **9 月 5 日** §6；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
| **macOS arm64** | L4＠**`b5be5ed97` JOBS=1** **`product_l4_true_cold OK` 129** wall **137m25s**（bstrict 130m27s；hello／fs／channel／STD-092 **OK**）／tip **L2＠`b7d6f175a`** 矩阵 **5／5** `/tmp/xlang_l2_Darwin_b7d6f175a.log` · 钉盘 **`b5be5ed97`** |
| **Ubuntu x86_64（金标）** | 钉盘 L4＠**`b5be5ed97`** **`product_l4_true_cold OK` 129** wall **17m47s** · tip **L2＠`b7d6f175a`** 矩阵 **5/5** `/tmp/xlang_l2_Linux_b7d6f175a.log`（g05 静态无 INTERP；xlang_asm 5070640＠21:35）· SSH＝**`ubuntu-server`** |
| **Windows MSYS2** | leftover unique **0** · min-gate **全绿** · 纯 asm邻域 **6／6**＋call／两 let／赋值／while／for／break／continue／`loop`／match／数组 INDEX／STRUCT_LIT＋FIELD／裸 STRUCT_LIT.field／CALL.field `mk().x`／nested `mk().p.f`／pointer intermediate／METHOD.field `P.mk().x`／9–16B dual-GP CALL.field／sret ＞16B／`take(p: *P)`／`x as i32`／`unsafe { return *p }`／`let y: i32 = *p`／Cap-fn-ptr `foo as *u8`／TYPE_FN `function(): i32` 间接 call／参／字段／ADDR_OF INDEX／FIELD `&a[0]`／`&s.x`／TYPE_FN return `return foo`／TYPE_FN CALL of CALL `getf()()`／nested ADDR_OF `&s.a[0]`／`&a[0].x`／`&*p`／ADDR_OF CALL／STRUCT_LIT 基 `&mk().x`／`&(P { x: 3 }).x`／`&P.mk().x`／ARRAY_LIT TYPE_FN `[foo, bar]`／`fs[0]()`／STRUCT_LIT 字段 `H { fs: [foo] }`／作参 `apply([foo])`／16B `apply([foo, bar])`／TYPE_SLICE `take([foo])`／VAR packing／TYPE_FN let-init COMMON BSS／INDEX TYPE_SLICE 形参 bounds／nso_at 非 RETURN expr-stmt／INDEX TYPE_SLICE 形参／局部 store／ADDR_OF INDEX TYPE_SLICE `&xs[0]`／ADDR_OF INDEX TYPE_SLICE 非字面量 `&xs[i]`／INDEX DEREF `*[]i32` `(*p)[1]`／TYPE_SLICE DEREF 作参 `take(*p)`／CALL 返回 TYPE_SLICE 作参 `take(mk())`／FIELD TYPE_SLICE 作参 `take(s.xs)`／CALL 返回 TYPE_SLICE let-init `let s = mk()`／CALL 返回 TYPE_SLICE let-init 局部 `.length`／ARRAY_LIT TYPE_SLICE 局部 `.length`／TYPE_ARRAY 8B DEREF 作参 `take(*p)` of `*[2]i32`／FIELD TYPE_ARRAY 8B 作参 `take(s.a)`／CALL 返回 TYPE_ARRAY 8B 作参 `take(mk())`／INDEX of `[2][2]i32` 作 TYPE_ARRAY 形参 `take(rows[1])`／FIELD TYPE_ARRAY 16B 作参 `take(s.a)` of `[4]i32`／TYPE_ARRAY DEREF let-init `unsafe { let y: [2]i32 = *p }`／TYPE_ARRAY 整数组赋值 `y = *p`／TYPE_ARRAY dest-in-rbx `*p = a`／TYPE_ARRAY 整数组 return `return a`／TYPE_NAMED dest-in-rbx STRUCT_LIT `*p = P { x: 3, y: 4 }`／TYPE_SLICE dest-in-rbx ARRAY_LIT `*p = [3, 4]`／TYPE_SLICE dest-in-rbx FIELD `*p = s.xs`／TYPE_SLICE dest-in-rbx INDEX `*p = rows[1]`／TYPE_SLICE VAR dest FIELD `b = s.xs`／TYPE_SLICE let-init INDEX `let s = rows[1]`／ADDR_OF INDEX of `[2][]i32` `&rows[1]`／TYPE_SLICE DEREF let-init `unsafe { let s: []i32 = *p }`／TYPE_SLICE VAR let-init `let s: []i32 = xs`／TYPE_SLICE dest-in-rbx VAR `*p = xs`／TYPE_NAMED dest-in-rbx STRUCT_LIT `(*p).y`／**TYPE_NAMED dest-in-rbx MATCH 8B**／**TYPE_NAMED dest-in-rbx MATCH 16B**／**TYPE_NAMED dest-in-rbx MATCH 24B**／**TYPE_NAMED dest-in-rbx nested STRUCT_LIT**／**TYPE_NAMED dest-in-rbx MATCH arm VAR**／**TYPE_NAMED dest-in-rbx MATCH arm CALL**／**TYPE_NAMED dest-in-rbx MATCH arm CALL 24B**／**TYPE_NAMED dest-in-rbx MATCH arm CALL 32B**／**TYPE_NAMED dest-in-rbx CALL**／**TYPE_NAMED dest-in-rbx METHOD**／**TYPE_NAMED dest-in-rbx MATCH arm METHOD**／**TYPE_ARRAY dest-in-rbx MATCH**／**CALL.field 32B** `mk().h`／**VAR dest MATCH arm CALL** `d = match { 1 => mk(); }`／**TYPE_ARRAY dest-in-rbx MATCH arm CALL** `*p = match { 1 => mk(); }`／**TYPE_SLICE dest-in-rbx MATCH** `*p = match { 1 => [3, 4]; }`／**TYPE_SLICE VAR dest MATCH** `d = match { 1 => [3, 4]; }`／guarded match／match subject CALL／INDEX／FIELD／const-int guard · 10.1.3 NT **暂缓** |

***

## 6. 变更记录（倒序 · **9 月 5 日**；4 日见 [归档](自举进度-归档-2026-09-04.md)）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-05 | **fix(windows)** **TYPE_ARRAY dest MATCH arm FIELD** | **代码** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。五问：产生＝leftover unique emit_match dest-parked FIELD／INDEX 拦 dest_tk!=10；TYPE_ARRAY dest MATCH（leftover rest unique store MATCH parks dest dest_tk=10 nbytes=n_arr*esz）臂 FIELD `s.a` 落到 thin if_arm SAT rec，rec ko==44 仅 enum-namespace。存储＝WIN leftover unique emit_match first-wins。消费＝want-exe MATCH arm `s.a`／INDEX of `[N][M]T`。修：G.7 补全 leftover_emit_match_arm_result rko==44／47 走 leftover_emit_slice_lvalue（dest_tk!=11 已 qword-copy dest_nbytes）。禁 leftover rest remaining-wave。禁 leftover rest T SAT emit_assign／emit_field。证＠`b7d6f175a`：mac L2 矩阵 5/5 `/tmp/xlang_l2_Darwin_b7d6f175a.log`；Ubuntu 金标 L2 矩阵 5/5 `/tmp/xlang_l2_Linux_b7d6f175a.log`。**未勾** leftover 清单（要 Windows leftover-PE hybrid L2）。禁改 mega／测试期望。禁升钉／L4。PLATFORM: SHARED 双端 L2 产品／WINDOWS leftover-PE unique。下一刀：`arr_star_match_field` 要 Windows leftover-PE hybrid L2。 |
| 2026-09-05 | **fix(windows)** **TYPE_NAMED dest MATCH arm FIELD** | **代码** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。五问：产生＝leftover unique emit_match dest-parked 只拦 dest_tk==11 FIELD／INDEX；TYPE_NAMED dest MATCH（rec ASSIGN parks dest，dest_tk 仍 0）臂 FIELD `s.p` 落到 thin if_arm SAT rec，rec ko==44 仅 enum-namespace。存储＝WIN leftover unique emit_match first-wins。消费＝want-exe MATCH arm `s.p`／INDEX of `[N]P`。修：G.7 补全 leftover_emit_slice_lvalue qword-copy nbytes（dest_tk==11 仍 16B fat）。TYPE_ARRAY dest_tk==10 未认领。禁 leftover rest remaining-wave。禁 leftover rest T SAT emit_assign／emit_field。证＠`729fab580`：mac L2 矩阵 5/5 `/tmp/xlang_l2_Darwin_729fab580.log`；Ubuntu 金标 L2 矩阵 5/5 `/tmp/xlang_l2_Linux_729fab580.log`。**未勾** leftover 清单（要 Windows leftover-PE hybrid L2）。禁改 mega／测试期望。禁升钉／L4。PLATFORM: SHARED 双端 L2 产品／WINDOWS leftover-PE unique。下一刀：`named_star_match_field` 要 Windows leftover-PE hybrid L2。 |
| 2026-09-05 | **docs(selfhost)** **双端 L2＠34917a11e** | **文档** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。代码拦截＠`34917a11e`（leftover unique emit_match arm FIELD／INDEX dest_tk==11）。五问：产生＝leftover unique emit_match 在 `!FROM_X` 或 `WIN_LEFTOVER`；POSIX leftover rest FROM_X ABSENT（`.x` thin 拥产品）。存储＝WIN leftover-PE hybrid rest-first。消费＝want-exe MATCH arm `s.xs`／`rows[1]`。证：mac L2 矩阵 5/5 `/tmp/xlang_l2_Darwin_34917a11e.log`；Ubuntu 金标 L2 矩阵 5/5 `/tmp/xlang_l2_Linux_34917a11e.log`。leftover-PE dest-in-rbx MATCH 探针 leftover rest FROM_X CG002 `.Lf0_2`（SAT／thin emit_match；**不**当双端 L2 产品红）。**未勾** leftover 清单。禁 leftover rest remaining-wave via `!FROM_X` 或 WIN。禁改 mega／测试期望。禁升钉／L4。PLATFORM: SHARED 双端 L2 产品／WINDOWS leftover-PE unique。下一刀：`slice_star_match_field` 要 Windows leftover-PE hybrid L2。 |
| 2026-09-05 | **fix(linux)** **nostdlib TLS image** | **代码** · **不升钉** · Ubuntu 金标 L2。五问：产生＝`bootstrap_init_static_tls` 只设 48B canary TCB；gcc IE `__thread`（parser binop peek PT_TLS memsz=0x70）负 FS 写入 BSS 把 `environ` 清零。存储＝BSS `environ`＠stubs。消费＝ensure `runtime_link_abi_user_env.o` `execve("/bin/cc", …, NULL)` → gcc 15 `posix_spawnp(cc1)` ENOENT，矩阵 5/5 FAIL_BUILD。修：G.7 补全该函数 PT_TLS 映像在 TCB 前、FS=TCB、canary＠FS+0x28。禁下游给 execve 塞 PATH 糊绿。禁改 mega／测试期望。证＠`23550463a`：`L2_GOLD_OK` 矩阵 pass=5 fail=0 rv=42／opt=102／hello=0／si=0／f32=0；smoke OK；INTERP `/lib64/ld-linux-x86-64.so.2`；xlang／xlang_asm／xlang-c 5070688＠20:50 静态。PLATFORM: LINUX x86_64 nostdlib。下一刀：leftover-PE `slice_star_match_field` 未认领（要 Windows）。 |
| 2026-09-05 | **fix(linux)** **pure-ld PT_INTERP** | **代码** · **不升钉** · Ubuntu 金标。五问：产生＝`pure_ld_platform_prefix` LINUX 空，GNU ld 2.46 默认 PT_INTERP `/lib/ld64.so.1`（ENOENT）。存储＝cold seed `compiler/xlang` INTERP。消费＝bootstrap smoke execve rc=127。修：G.7 补全 LINUX prefix `--dynamic-linker` 取 labi 候选首个可读（`/lib64/ld-linux-x86-64.so.2`）。禁把 va_cap `-I compiler/include` 搅进本 commit。证＠`cc6256b5a` 后 L2＠`23550463a`：seed 动态 INTERP glibc；g05 `-static` 无 INTERP。PLATFORM: LINUX。 |
| 2026-09-05 | **fix(windows)** **TYPE_SLICE VAR dest MATCH** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest unique rec ASSIGN TYPE_SLICE MATCH 只拦 DEREF dest（asg_lko==52）；VAR dest `d = match { 1 => [3, 4]; }` 落到 SAT emit_assign（SAT global T），MATCH dest 未 park，dest `[0]` length 仍 1（`slice_asg_match` INDEX [1] panic 127）。存储＝leftover rest unique rec first-wins。消费＝want-exe `let mut d: []i32 = [0]; d = match 1 { 1 => [3, 4]; _ => [0, 0]; }; return d[1];`（臂 `;`；`[]i32`）。修：G.7 补全 leftover rest unique rec ASSIGN TYPE_SLICE MATCH 扩 VAR dest（asg_lko==3）park dest CPU 栈＋leftover rest unique emit_match dest-parked dest_tk=11。禁 leftover rest T SAT emit_assign／emit_array_lit。禁 leftover rest U SAT local t copy_large。禁 dual-def WAVE277。证＠`586d8ad12`：leftover rest 462495B＋merged pabi 1443119B＋redirect patched **674**＋SKIP_HOT（xlang_asm 4697108＠19:59；rec＠0xf5be first-wins vs SAT t 0x64729；emit_match＠0x8d97 first-wins vs SAT later 0x5f701；SAT emit_assign＠0x5c00f／SAT emit_array_lit＠0x5e043 非 leftover rest T）；`slice_asg_match`／`0`／`_len`／`_wild`／`_wild0`／`_empty`／`4` RUN=4／3／2／9／8／0／4；顺带 `slice_asg_match_var`／`call`／`slice_match_let` RUN=4；slice_star_match／slice_star_lit／var／arr_star_match／arr_star_match_call／match_star／slice_asg_field／match_asg_call／arr0／rv42 不回归。`slice_star_match_field`／`slice_asg_match_field`／INDEX 臂 RUN=127 另邻域。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：`slice_star_match_field` 未认领（TYPE_SLICE dest-in-rbx MATCH arm FIELD RUN=127），勿搅。 |
| 2026-09-05 | **fix(windows)** **TYPE_SLICE dest-in-rbx MATCH** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest unique rec ASSIGN TYPE_SLICE dest-in-rbx 只拦 ARRAY_LIT／VAR／CALL／FIELD／INDEX，MATCH 落到 SAT emit_assign（SAT global T）DEREF dest SAT local t struct_let_init -2 后 emit MATCH 冲 rbx 再 4B store（`slice_star_match` SEGV 139／`_len` RUN=1 dest `[0]` length 仍 1）；臂 ARRAY_LIT leftover_emit_array_lit 把 i32 载荷写进 fat pair。存储＝leftover rest unique rec first-wins。消费＝want-exe `unsafe { *p = match 1 { 1 => [3, 4]; _ => [0, 0]; }; return (*p)[1]; }`（臂 `;`；`[]i32`）。修：G.7 补全 leftover rest unique rec ASSIGN TYPE_SLICE MATCH park dest CPU 栈＋leftover rest unique emit_match dest-parked dest_tk=11；leftover_emit_array_lit dest_tk==11 SAT emit_array_lit store fat data@0 length@8。禁 leftover rest T SAT emit_assign／emit_array_lit。禁 leftover rest U SAT local t copy_large。禁 dual-def WAVE277。证＠`906f66134`：leftover rest 462479B＋merged pabi 1443103B＋redirect patched **674**＋SKIP_HOT（xlang_asm 4697108＠19:50；rec＠0xf5be first-wins；emit_match＠0x8d97 first-wins；SAT emit_assign＠0x5bfff／SAT emit_array_lit＠0x5e033 非 leftover rest T）；`slice_star_match`／`0`／`_len`／`_wild`／`_wild0`／`_empty`／`4` RUN=4／3／2／9／8／0／4；顺带 `slice_star_match_var`／`call`／`slice_match_let` RUN=4；slice_star_lit／var／var_len／call／field／arr_star_match／arr_star_match_call／match_star／arr0／rv42 不回归。`slice_asg_match` VAR dest RUN=127 另邻域。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：`slice_asg_match` 未认领（VAR dest TYPE_SLICE MATCH RUN=127），勿搅。 |
| 2026-09-05 | **fix(windows)** **TYPE_ARRAY dest-in-rbx MATCH arm CALL** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover_emit_call 把 TYPE_ARRAY CALL 当 NAMED dual-GP／sret，把 E* 指针当载荷写入 dest（`arr_star_match_call` RUN=246）；MEGA ASSIGN rhs 未 stamp 臂 CALL resolved_type，kind==10 检测打不中 dest-in-rbx（let-init `arr_match_let_call` 已 stamp 绿）。存储＝leftover rest unique rec leftover_emit_call first-wins。消费＝want-exe `unsafe { *p = match 1 { 1 => mk(); _ => [0, 0]; }; return (*p)[1]; }`（臂 `;`；`[2]i32`）。修：G.7 补全 leftover_emit_call TYPE_ARRAY SAT emit_call 留 E*＋leftover rest unique copy_from_e_star 写入 parked rbx；store MATCH dest_tk=10 即使臂未 stamp 也走 E* 拷贝。禁 leftover rest T SAT emit_call／emit_assign／emit_array_lit。禁 leftover rest U SAT local t copy_large。禁 dual-def WAVE277。证＠`d04a46574`（E* 路径＠`97b86ee04`）：leftover rest 460399B＋merged pabi 1441023B＋redirect patched **674**＋SKIP_HOT（xlang_asm 4695060＠19:39；rec＠0xf4a8 first-wins；emit_match＠0x8d97 first-wins；store＠0x14d9c first-wins）；`arr_star_match_call`／`0`／`_wild`／`_wild0`／`16`／`16a`／`24`／`arr_match_let_call` RUN=4／3／4／3／4／1／6／4；顺带 `arr_star_match_method`／`var`／`arr_asg_match_call` RUN=4；arr_star_match／arr_star_call／arr_call_let／match_asg_call32／match_arm_call32／star_call32／arr_match_let／arr0／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：`slice_star_match` 未认领（TYPE_SLICE dest-in-rbx MATCH SEGV 139／`_len` RUN=1），勿搅。 |
| 2026-09-05 | **fix(windows)** **VAR dest MATCH arm CALL** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest unique rec ASSIGN TYPE_NAMED MATCH 只拦 DEREF dest（asg_lko==52）；VAR dest `d = match { 1 => mk(); }` 落到 SAT emit_assign（SAT global T），MATCH dest 未 park → 16B dual-GP 不入库（`match_asg_call16` RUN=0）；24／32B callee sret dest 穿栈（`match_asg_call24`／`32` SEGV 139）。存储＝leftover rest unique rec first-wins。消费＝want-exe `let mut d: S = S { … }; d = match 1 { 1 => mk(); _ => S { … }; }; return d.h;`（臂 `;`）。修：G.7 补全 leftover rest unique rec ASSIGN TYPE_NAMED MATCH 扩 VAR dest（asg_lko==3）park dest CPU 栈＋leftover rest unique emit_match dest-parked（臂 CALL leftover_emit_call SysV sret rdi／dual-GP）。禁 leftover rest T SAT emit_assign／emit_call／emit_struct_lit。禁 leftover rest U SAT local t copy_large。禁 dual-def WAVE277。证＠`f0bc07f51`：leftover rest 459454B＋merged pabi 1440078B＋redirect patched **674**＋SKIP_HOT（xlang_asm 4694505＠19:22；rec＠0xf1c7 first-wins；emit_match＠0x8d97 first-wins）；`match_asg_call32`／`32a`／`32e`／`32_wild`／`32_wild_call`／`16`／`16a`／`24`／`24a`／`8`／`28` RUN=8／1／5／9／8／4／1／6／1／4／7；顺带 `match_asg_method32`／`var32`／`lit32` RUN=8；match_asg／match_arm_call32／match_star32／star_call32／let_call32／call32_field／arr_star_match／arr0／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：`arr_star_match_call` 未认领（TYPE_ARRAY dest-in-rbx MATCH arm CALL RUN=246），勿搅。 |
| 2026-09-05 | **docs(selfhost)** **CALL.field 32B** | **文档** · **不升钉** · Windows leftover-PE L2 探针-only（无 leftover rest 重编；代码已在 `f8fa30de5` leftover rest rec callee sret STRUCT_LIT DEST_IN_RBX＋leftover rest unique field_access_fast >16B SysV sret rdi＠`a63f9bb7f`）。五问：产生＝callee SAT emit_struct_lit implicit dest 固定 lea `[rbp-0x18]`（24B）；32B 字段写 +0x1c 穿栈（MATCH arm CALL 32B 已修；FIELD of CALL 32B 同 callee 未单独探针）。存储＝leftover rest unique rec／field_access_fast first-wins。消费＝want-exe `return mk().h`。证＠`4a695017c` 二进制／docs tip 本波：xlang_asm 4694505＠19:05；`call32_field`／`a`／`e`／`g`／`_dy` RUN=8／1／5／7／8；`call28_field` RUN=7；call24_field／method_field32／let_call32／star_call32／match_arm_call32／addrof_call32_field／arr_star_match／arr_star_match_var／arr0／rv42 不回归。禁 leftover rest T SAT emit_assign／emit_call／emit_struct_lit。禁 dual-def WAVE277。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：`match_asg_call32` 未认领（VAR dest MATCH arm CALL 32B SEGV 139／16B RUN=0／24B SEGV 139），勿搅。 |
| 2026-09-05 | **fix(windows)** **TYPE_ARRAY dest-in-rbx MATCH** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest unique store MATCH iko==43 未处理返 -2，rec ASSIGN TYPE_ARRAY dest-in-rbx 再 SAT emit_assign（scout `arr_star_match` RUN=0／`arr_star_match0` 垃圾；dump rec MATCH 无 dest-parked，ARRAY_LIT SAT implicit dest；VAR let-init `arr_match_let` CG002）。存储＝leftover rest unique store／emit_match first-wins。消费＝want-exe `unsafe { *p = match 1 { 1 => [3, 4]; _ => [0, 0]; }; return (*p)[1]; }`（臂 `;`）。修：G.7 补全 leftover rest unique store MATCH park dest CPU 栈＋leftover rest unique emit_match dest-parked；leftover_emit_match_arm_result rko==46 ARRAY_LIT 逐 elem store parked rbx。禁 leftover rest T SAT emit_assign／emit_array_lit／try_index。禁 leftover rest U SAT local t copy_large。禁 dual-def WAVE277。证＠`4a695017c`：leftover rest 459438B＋merged pabi 1440062B＋redirect patched **674**＋SKIP_HOT（xlang_asm 4694505＠19:05；rec＠0xf1c7 first-wins；leftover_emit_array_lit＠0xe939；store＠0x14aa9 first-wins）；`arr_star_match`／`0`／`_dy`／`_wild`／`_wild0`／`16`／`16a`／`arr_match_let`／`let0` RUN=4／3／4／9／8／4／1／4／3；arr_star_asg／arr_star_lit／match_star／match_star16／named_star_lit／match_asg／nest_star_lit／match_arm_method32／star_method32／star_call32／arr0／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：`call32_field` 未认领，勿搅。 |
| 2026-09-05 | **fix(windows)** **TYPE_NAMED dest-in-rbx MATCH arm METHOD** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝MATCH 臂 `S.mk()` MEGA ASSIGN rhs 未 stamp resolved_func_index；SAT emit_method has_recv=1 rec 类型名 VAR（scout `match_arm_method32` CG002 未定义 done 标签；code_len 365→373 +8 sret rdi 后 SAT emit_method 返 -1；dump rec VAR after subject INT；ASSIGN `*p = S.mk()` 已 stamp 绿）。存储＝leftover rest unique resolve／rec first-wins。消费＝want-exe `unsafe { *p = match 1 { 1 => S.mk(); _ => S { a: 0, … }; }; return (*p).h; }`（inherent `impl S`；臂 `;`）。修：G.7 补全 leftover unique resolve associated Type.method()（layout 名＋nparams==nargs；prefer 返回 TYPE_NAMED 拼 layout）＋leftover_emit_call 未解析则 stamp 再 SAT emit_method。禁 leftover rest T SAT emit_assign／emit_call／emit_method。禁 leftover rest U SAT local t copy_large。禁 dual-def WAVE277。证＠`876cee300`：leftover rest 458438B＋merged pabi 1439062B＋redirect patched **674**＋SKIP_HOT（xlang_asm 4693423＠18:47；rec＠0xefef first-wins；leftover_emit_call＠0xec13；resolve＠0x1b19a first-wins）；`match_arm_method32`／`32a`／`32e`／`32_wild`／`32_wild_method`／`24`／`16`／`8` RUN=8／1／5／9／8／6／4／4；star_method32／star_call32／match_arm_call32／let_method32／method_field32／named_star_lit／match_star16／nest_star_lit／match_asg／arr0／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：邻域 `arr_star_match` 仍红、`call32_field` 未认领，勿搅。 |
| 2026-09-05 | **fix(windows)** **TYPE_NAMED dest-in-rbx METHOD** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest rec ASSIGN 只拦 MATCH／STRUCT_LIT／CALL；METHOD=49 落到 SAT emit_assign 不传 rdi 作 SysV sret（scout `star_method32`／`24` SEGV 139；`star_method16` RUN=0；MATCH 臂 METHOD CG002 未定义 done 标签）。存储＝leftover rest unique rec first-wins。消费＝want-exe `unsafe { *p = S.mk(); return (*p).h; }`（inherent `impl S`）。修：G.7 补全 leftover_emit_call ko==49 SAT emit_method（nbytes>16 SysV sret rdi=parked dest；≤16 dual-GP）＋leftover_emit_match_arm_result rko==49＋rec ASSIGN asg_rko==49。禁 leftover rest T SAT emit_assign／emit_call／emit_method。禁 leftover rest U SAT local t copy_large。禁 dual-def WAVE277。证＠`63de7a1fa`：leftover rest 457400B＋merged pabi 1438024B＋redirect patched **674**＋SKIP_HOT（xlang_asm 4692399＠18:24；rec＠0xef74 first-wins；leftover_emit_call＠0xec13）；`star_method32`／`32a`／`32e`／`24`／`24a`／`28`／`16`／`8` RUN=8／1／5／6／1／7／4／4；star_call32／match_arm_call32／let_method32／method_field32／named_star_lit／match_star16／nest_star_lit／match_asg／arr0／rv42 不回归。MATCH 臂 METHOD 仍 CG002 另邻域。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_NAMED dest-in-rbx MATCH arm METHOD=49。 |
| 2026-09-05 | **fix(windows)** **TYPE_NAMED dest-in-rbx CALL** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest rec ASSIGN 只拦 MATCH／STRUCT_LIT；CALL 落到 SAT emit_assign 不传 rdi 作 SysV sret（scout `star_call32` dump：call mk 无 rdi，再 store rax 进 *p 前 8B；callee sret 字段写垃圾 rdi → SEGV 139）。存储＝leftover rest unique rec first-wins。消费＝want-exe `unsafe { *p = mk(); return (*p).h; }`。修：G.7 补全 leftover rest unique rec ASSIGN TYPE_NAMED dest-in-rbx CALL：park dest CPU 栈＋leftover_emit_call（nbytes>16 SysV sret rdi=parked dest；helper nbytes max park＋named_layout＋call_return_byte_size）。禁 leftover rest T SAT emit_assign／emit_call。禁 leftover rest U SAT local t copy_large。禁 dual-def WAVE277。证＠`5498d1cb4`：leftover rest 457268B＋merged pabi 1437892B＋redirect patched **674**＋SKIP_HOT（xlang_asm 4692399＠18:09；rec＠0xeeff first-wins；leftover_emit_call＠0xec0d）；`star_call32`／`32a`／`32e`／`call24`／`24a`／`call28`／`call16`／`call8` RUN=8／1／5／6／1／7／4／4；match_arm_call32／call24／call16／call8／match_star32／let_call32／match_arm_var16／match_star16／named_star_lit／match_asg／nest_star_lit／nest_star_match／arr0／rv42 不回归。METHOD=49 未拦另邻域。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_NAMED dest-in-rbx METHOD=49。 |
| 2026-09-05 | **fix(windows)** **TYPE_NAMED dest-in-rbx MATCH arm CALL 32B** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest rec ko==45 callee sret 调 SAT emit_struct_lit implicit dest 固定 lea `[rbp-0x18]`（24B）；28B／32B 字段写 +0x18／+0x1c 穿 saved rbp／返回地址（scout `match_arm_call32` SEGV 139；`let_call32` 绿因 try_inline 字面量）。存储＝leftover rest unique rec first-wins。消费＝want-exe `unsafe { *p = match 1 { 1 => mk(); _ => S { a: 0, b: 0, c: 0, d: 0, e: 0, f: 0, g: 0, h: 0 }; }; return (*p).h; }`。修：G.7 补全 leftover rest rec sret STRUCT_LIT：load sret dest＋leftover_emit_struct_lit_into_parked_rbx。禁 leftover rest T SAT emit_struct_lit／emit_call。禁 leftover rest U SAT local t copy_large。禁 dual-def WAVE277。证＠`f8fa30de5`：leftover rest 456516B＋merged pabi 1437140B＋redirect patched **674**＋SKIP_HOT（xlang_asm 4691887＠17:55；rec＠0xee53 first-wins；leftover_emit_struct_lit＠0xe6b8）；`match_arm_call32`／`32a`／`32e`／`32_wild`／`32_wild_call`／`call28` RUN=8／1／5／9／8／7；call24／call16／call8／match_star32／let_call32／match_arm_var16／match_star16／named_star_lit／match_asg／nest_star_lit／nest_star_match／arr0／rv42 不回归。`star_call32`／`star_call24` SEGV 139 另邻域。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_NAMED dest-in-rbx CALL（无 MATCH）。 |
| 2026-09-05 | **fix(windows)** **TYPE_NAMED dest-in-rbx MATCH arm CALL 24B** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest unique leftover_emit_call 只拦 CALL nbytes≤16；>16 掉 thin if_arm SAT sret implicit dest 叠 p（scout `match_arm_call24` SEGV 139）。存储＝leftover rest unique emit_match first-wins。消费＝want-exe `unsafe { *p = match 1 { 1 => mk(); _ => R { a: 0, b: 0, c: 0, d: 0, e: 0, f: 0 }; }; return (*p).f; }`。修：G.7 补全 leftover_emit_call dest-parked CALL >16B：SysV sret rdi=parked dest＋SAT set_call_sret_reg_shift（SAT global T）。禁 leftover rest T SAT emit_call／emit_assign。禁 leftover rest U SAT local t copy_large。禁 dual-def WAVE277。证＠`d54890fdd`：leftover rest 456700B＋merged pabi 1437324B＋redirect patched **674**＋SKIP_HOT（xlang_asm 4691887＠17:35；rec＠0xee53 first-wins；leftover_emit_call＠0xec0d）；`match_arm_call24`／`24a`／`24e`／`24_wild`／`24_wild_call` RUN=6／1／5／7／6；call16／call8／match_arm_var16／match_star16／star24／nest_star_lit／nest_star_match／named_star_lit／match_asg／arr0／rv42 不回归。`match_arm_call32` SEGV 139 另邻域。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：MATCH dest-in-rbx arm CALL 32B。 |
| 2026-09-05 | **fix(windows)** **TYPE_NAMED dest-in-rbx MATCH arm CALL** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest unique leftover_emit_match_arm_result 只拦 ko==45／ko==3；CALL=48 掉 thin if_arm SAT rec，parked dest 仍 0（`match_arm_call16` RUN=0）。存储＝leftover rest unique emit_match first-wins。消费＝want-exe `unsafe { *p = match 1 { 1 => mk(); _ => Q { a: 0, b: 0, c: 0, d: 0 }; }; return (*p).d; }`。修：G.7 补全 leftover_emit_match_arm_result dest-parked CALL：SAT emit_call＋dual-GP store rax@0 rdx@8 parked rbx（8B 只 store rax）。禁 leftover rest T SAT emit_call／emit_assign。禁 leftover rest U SAT local t copy_large。禁 dual-def WAVE277。证＠`6ddcd9178`：leftover rest 456364B＋merged pabi 1436988B＋redirect patched **674**＋SKIP_HOT（xlang_asm 4691375＠17:21；rec＠0xed55 first-wins；leftover_emit_call＠0xec18）；`match_arm_call16`／`16a`／`16c`／`16_wild`／`16_wild_call`／`call8` RUN=4／1／3／7／4／4；match_arm_var16／match_star16／star24／nest_star_lit／nest_star_match／named_star_lit／match_asg／arr0／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：MATCH dest-in-rbx arm CALL 24B（helper 只拦 nbytes≤16）。 |
| 2026-09-05 | **fix(windows)** **TYPE_NAMED dest-in-rbx MATCH arm VAR** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest unique leftover_emit_match_arm_result 只拦 ko==45；VAR=3 掉 thin if_arm SAT rec，parked dest 仍 0（`match_arm_var16` RUN=0）；lvalue／ty_ref -1 则 emit_match 提前失败 CG002 标签。存储＝leftover rest unique emit_match first-wins。消费＝want-exe `unsafe { let q: Q = Q { a: 1, b: 2, c: 3, d: 4 }; *p = match 1 { 1 => q; _ => Q { a: 0, b: 0, c: 0, d: 0 }; }; return (*p).d; }`。修：G.7 补全 leftover_emit_match_arm_result dest-parked VAR：park dest nbytes＋glue_var_expr_stack_off lea＋qword-copy parked rbx。禁 leftover rest T SAT emit_assign／U SAT local t copy_large。禁 dual-def WAVE277。证＠`4fdbaf0c0`：leftover rest 455829B＋merged pabi 1436453B＋redirect patched **674**＋SKIP_HOT（xlang_asm 4691322＠17:09；rec＠0xebe6 first-wins）；`match_arm_var16`／`16a`／`16c`／`16_wild`／`16_wild_var`／`var8`／`var24` RUN=4／1／3／7／4／4／6；match_star16／star24／nest_star_lit／nest_star_match／named_star_lit／match_asg／arr0／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：MATCH dest-in-rbx arm CALL（`match_arm_call16` RUN=0）。 |
| 2026-09-05 | **fix(windows)** **TYPE_NAMED dest-in-rbx nested STRUCT_LIT** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest unique helper 对嵌套 STRUCT_LIT 字段 rec emit；dest-parked rec ko==45 把 Inner 写 dest+0（`nest_star_match` RUN=0）；SAT emit_struct_lit implicit dest 叠 `p`（`nest_star_lit` SEGV 139）。存储＝leftover rest unique rec ASSIGN／emit_match first-wins。消费＝want-exe `unsafe { *p = Outer { a: 1, inner: Inner { x: 3, y: 4 } }; return (*p).inner.y; }`。修：G.7 补全 leftover_emit_struct_lit_into_parked_rbx 嵌套 STRUCT_LIT recurse dest+base_off+foff。禁 leftover rest T SAT emit_struct_lit／emit_assign。禁 dual-def WAVE277。证＠`ef2d5608f`：leftover rest 454610B＋merged pabi 1435234B＋redirect patched **674**＋SKIP_HOT（xlang_asm 4690199＠16:54；rec＠0xe893 first-wins）；`nest_star_lit`／`lit_a`／`lit_x`／`nest_star_match`／`match_a`／`match_wild` RUN=4／1／3／4／1／7；named_star_lit／star24／match_star16／star24／arr0／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：MATCH dest-in-rbx arm VAR（`match_arm_var16` RUN=0）。 |
| 2026-09-05 | **docs(selfhost)** **TYPE_NAMED dest-in-rbx MATCH 24B** | **文档** · **不升钉** · Windows leftover-PE L2 探针-only（无 leftover rest 重编；代码已在 `db4780c2f` leftover rest unique emit_match DEST_IN_RBX helper）。五问：产生＝MATCH 16B helper 已按 nf≤64 逐字段 store fsz≤8；6×i32／8×i32 标量无需新路径。存储＝leftover rest unique emit_match first-wins。消费＝want-exe `unsafe { *p = match 1 { 1 => R { a: 1, b: 2, c: 3, d: 4, e: 5, f: 6 }; _ => … }; return (*p).f; }`（臂 `;`）。证＠`db4780c2f` 二进制／docs tip 本波：xlang_asm 4690199＠16:42；`match_star24`／`24a`／`24e`／`24_dy`／`24_wild` RUN=6／1／5／6／7；顺带 `match_star32`／`32a` RUN=8／1；match_star／star16／named_star24／named_star_lit16／match_asg／arr0／rv42 不回归。禁 leftover rest T SAT emit_struct_lit／emit_assign。禁 dual-def WAVE277。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_NAMED dest-in-rbx nested STRUCT_LIT（`nest_star_lit` SEGV 139）。 |
| 2026-09-05 | **fix(windows)** **TYPE_NAMED dest-in-rbx MATCH 16B** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest unique emit_match 调 thin if_arm（`#ifndef FROM_X` ABSENT）intra SAT rec → SAT emit_struct_lit implicit dest lea 临时槽，parked dest 仍 0（`match_star16` dump rbp-0x60；先 SEGV 139 后 RUN=0）。存储＝leftover rest unique emit_match first-wins。消费＝want-exe `unsafe { *p = match 1 { 1 => Q { a: 1, b: 2, c: 3, d: 4 }; _ => … }; return (*p).d; }`（臂 `;`）。修：G.7 leftover rest unique emit_match dest-parked STRUCT_LIT 臂走同一 DEST_IN_RBX 字段写 helper（`named_star_lit` 孪）。禁 leftover rest T SAT emit_struct_lit／emit_assign。禁 dual-def WAVE277。证＠`db4780c2f`（helper＠`7df3a0138`／always-flag＠`aaded152d`）：merged pabi 1435074B＋redirect patched **674**＋SKIP_HOT（xlang_asm 4690199＠16:42）；`match_star16`／`16a`／`16_dy`／`16_wild` RUN=4／1／4／7；match_star／0／_dy／named_star_lit／lit16／star24／match_asg／arr0／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：MATCH dest-in-rbx 24B。 |
| 2026-09-05 | **fix(windows)** **TYPE_NAMED dest-in-rbx MATCH 8B** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT emit_assign DEREF dest emit MATCH 冲 rbx 再 4B store（`match_star_dy` SEGV）；arm STRUCT_LIT SAT dest 叠 `p`（`(*p).y` SEGV）。存储＝leftover rest unique rec first-wins ASSIGN。消费＝want-exe `unsafe { *p = match 1 { 1 => P { x: 3, y: 4 }; _ => … }; return (*p).y; }`（臂 `;` 不是 `,`）。修：G.7 park dest＋bump next_offset 过 p＋leftover rest unique emit_match＋store rax。禁 leftover rest T SAT emit_assign。禁 dual-def WAVE277。证＠`3ec4b0cab`（park＠`1f9d2dd5a`／bump＠`bb95b91da`）：merged pabi 1435041B＋redirect patched **674**＋SKIP_HOT（xlang_asm 4690038＠16:20）；`match_star`／`match_star0`／`match_star_dy` RUN=4／3／4；match_i32／gfalse／match_let／match_asg／named_star_lit／slice_star_var_len／arr0／rv42 不回归。`match_star16` 仍 SEGV 139（另邻域）。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：MATCH dest-in-rbx 16B。 |
| 2026-09-05 | **fix(windows)** **TYPE_NAMED dest-in-rbx STRUCT_LIT `(*p).y`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest unique rec ASSIGN park dest 后 SAT emit_struct_lit SAT global T implicit dest 叠 `p`（`return d.y` 绿／`(*p).y` SEGV 139）。存储＝leftover rest unique rec first-wins ASSIGN。消费＝want-exe `unsafe { *p = P { x: 3, y: 4 }; return (*p).y; }`。修：G.7 补全 leftover rest unique rec ASSIGN POSIX DEST_IN_RBX 逐字段 emit＋store [rbx+foff]（禁 SAT implicit dest）。禁 leftover rest T SAT emit_struct_lit。禁 leftover rest U SAT local t fields_elf。禁 leftover rest remaining-wave `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`9578af192`：merged pabi 1433919B＋redirect patched **674**＋SKIP_HOT（xlang_asm 4689014＠16:03）；`named_star_lit`／`named_star_lit0`／`named_star_lit_dy`／`named_star_lit16`／`named_star_lit16b`／`named_star24` RUN=4／3／4／4／2／6；named_star_asg／named_let_lit／named_asg_lit／named_star_deref／slice_star_var_len／arr_star_asg／arr0／callid／ifelse／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：MATCH dest-in-rbx STRUCT_LIT（`match_star` BLD001）。 |
| 2026-09-05 | **fix(windows)** **TYPE_SLICE dest-in-rbx VAR `*p = xs`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest unique rec ASSIGN 对 dest-in-rbx TYPE_SLICE VAR／CALL／DEREF fallthrough SAT emit_assign memcpy 只 data 半边（dest `[0,0]` 假绿；dest `[0]` length 仍 1，`slice_star_var` panic 127）。存储＝leftover rest unique rec first-wins ASSIGN。消费＝want-exe `unsafe { *p = xs; return (*p).length; }` dest `[0]`。修：G.7 补全 leftover rest unique rec ASSIGN dest-in-rbx TYPE_SLICE VAR lvalue qword-copy 16B＋CALL／METHOD／DEREF dual-GP store rax@0 rdx@8。禁 leftover rest T SAT emit_assign。禁拦截 VAR dest（SAT store rax+rdx 已绿）。禁 leftover rest remaining-wave `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`2a027afc5`：merged pabi 1434007B＋redirect patched **674**（rec＠0xe5be first-wins＋SAT t＠0x62999）＋SKIP_HOT（xlang_asm 4689014＠15:35）；`slice_star_var`／`0`／`_len`／`slice_star_call`／`_len`／`slice_star_deref`／`_len` RUN=4／3／2／4／2／4／2 dest `[0]`；slice_star_field／idx／lit／slice_var_let_len／arr_star_asg／arr0／callid／ifelse／rv42 不回归。`named_star_lit` `(*p).y` RUN=139 在＠`839768596` 已红（`return d.y` 绿；非本刀回归）。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_NAMED dest-in-rbx STRUCT_LIT 后 `(*p).y`。 |
| 2026-09-05 | **fix(windows)** **TYPE_SLICE VAR let-init `let s = xs`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest unique slice_from_array VAR 臂只 wrap TYPE_ARRAY（`arr_sz<=0` 返 0），SAT emit_block_inits store_rax 不存 rdx（`slice_var_let` data RUN=4／`slice_var_let_len` RUN=127）。存储＝leftover rest unique T of SAT local t slice_from_array＋redirect SAT emit_block_inits intra（已 SCAN）。消费＝want-exe `let s: []i32 = xs; return s.length`。修：G.7 补全 leftover rest unique VAR TYPE_SLICE：local 16B fat win_glue polarity（x86 home-8）；formal fat* C-order qword-copy。禁 leftover rest T SAT emit_block_inits／struct_let_init。禁 leftover rest unique T VAR-SLICE-only。禁 leftover rest remaining-wave `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`839768596`：merged pabi 1432803B＋redirect patched **674**（slice_from_array＠0x11c6b first-wins＋SAT t＠0x764ee）＋SKIP_HOT（xlang_asm 4687990＠15:11）；`slice_var_let`／`slice_var_let0`／`slice_var_let_len`／`slice_var_param`／`slice_var_param_len` RUN=4／3／2／4／2；slice_arr_wrap／slice_let_lit／slice_let_idx／slice_let_field／addrof_deref_let／slice_call_let／arr0／i32store／fnslice_let／i32slen_var／callid／ifelse／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_SLICE dest-in-rbx VAR `*p = xs`（`slice_star_var_len` RUN=1）。 |
| 2026-09-05 | **fix(windows)** **TYPE_SLICE DEREF let-init `let s = *p`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT emit_block_inits SAT global T intra leftover rest unique slice_from_array 对 DEREF of `*[]T` 返 0，再 store_rax 不存 rdx（`addrof_deref_let` SEGV 139）。leftover rest unique emit_deref TYPE_SLICE 已 dual-GP。存储＝leftover rest unique T of SAT local t slice_from_array＋redirect SAT emit_block_inits intra（已 SCAN）。消费＝want-exe `unsafe { let s: []i32 = *p; return s[1]; }`。修：G.7 补全 leftover rest unique FULL ARRAY_LIT／VAR wrap／FIELD／INDEX＋DEREF TYPE_SLICE emit＋store_retval_pair。禁 leftover rest T SAT emit_block_inits／struct_let_init。禁 leftover rest unique T DEREF-only。禁 leftover rest remaining-wave `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`847ccdddd`：merged pabi 1431947B＋redirect patched **674**（slice_from_array＠0x11c6b first-wins＋SAT t＠0x7620e）＋SKIP_HOT（xlang_asm 4687478＠14:52）；`addrof_deref_let`／`addrof_deref_let0`／`addrof_deref_let_len`／`addrof_deref_lit`／`addrof_deref_lit_len` RUN=4／3／2／4／2；addrof_row／slice_let_idx／slice_let_lit／slice_let_field／slice_call_let／slice_ptr／slice_asg_deref／arr_deref_let／arr0／i32store／fnslice_let／callid／ifelse／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_SLICE VAR let-init `let s = xs` length（`slice_var_let_len` RUN=127）。 |
| 2026-09-05 | **fix(windows)** **ADDR_OF INDEX of `[2][]i32` `&rows[1]`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT egg rvalue_slice_once 无 DEREF=52，INDEX of `*[]i32` 落到 SAT length_to_rbx non-VAR `add 8; load 64` 把 `*(data+8)` 当 length（32 位 cmp panic 127；`addrof_row` RUN=127）。ADDR_OF INDEX lea fat-row 已对（`addrof_row0`／`addrof_row_len` 已绿）。存储＝leftover rest unique T of SAT local t rvalue_slice_once FULL CALL／METHOD／INDEX／DEREF＋redirect SAT eff_addr_scaled intra。消费＝want-exe `let p: *[]i32 = &rows[1]; unsafe { return (*p)[1]; }`。修：G.7 补全 leftover rest unique T 含 DEREF=52 dual-GP once-eval。禁 leftover rest T SAT emit_index／length_to_rbx／try_index。禁 leftover rest remaining-wave `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`782165b50`：merged pabi 1431665B＋redirect patched **674**（rvalue_slice_once＠0x1283f first-wins＋SAT t＠0x61183）＋SKIP_HOT（xlang_asm 4686966＠14:39）；`addrof_row`／`addrof_row0`／`addrof_row_len`／`addrof_rows0`／`addrof_row_nest`／`addrof_via_let` RUN=4／3／2／2／4／4；slice_let_idx／slice_ptr／slice_star_idx／arr0／i32store／fnslice_let／callid／ifelse／rv42 不回归。`addrof_deref_let` 仍 SEGV 139（另邻域）。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_SLICE DEREF let-init `let s = *p`。 |
| 2026-09-05 | **fix(windows)** **TYPE_SLICE let-init INDEX `let s = rows[1]`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT emit_block_inits SAT global T intra SAT local t slice_from_array 对 INDEX of `[2][]i32` 返 0，再 store_rax 不存 rdx（`slice_let_idx` SEGV 139／`slice_let_idx_len` RUN=0）。存储＝leftover rest unique T of SAT local t slice_from_array＋redirect SAT intra。消费＝want-exe `let s: []i32 = rows[1]; return s[1]`。修：G.7 补全 leftover rest unique FULL ARRAY_LIT／VAR wrap＋INDEX TYPE_SLICE emit dual-GP＋store_retval_pair＋FIELD TYPE_SLICE lvalue qword-copy。禁 leftover rest T SAT emit_block_inits／try_index。禁 leftover rest remaining-wave bump `!FROM_X` 或 WIN。禁 leftover rest U SAT local t copy_large。禁 dual-def WAVE277。证＠`5ecf2773e`：merged pabi 1428734B＋redirect patched **664**（slice_from_array＠0x11c6b first-wins／SAT emit_block_inits＠0x75f82／rec＠0xe5be）＋SKIP_HOT（xlang_asm 4684279＠14:16）；`slice_let_idx`／`slice_let_idx0`／`slice_let_idx_len`／`slice_let_idx_row0` RUN=4／3／2／2；`slice_let_field`／`slice_let_field_len` RUN=4／2；slice_let_lit／slice_call_let／slice_asg_idx／slice_asg_field／slice_star_idx／slice_star_field／slice_idx_rval／i32store／i32slen_let／fnslice_let／arr0／callid／ifelse／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：ADDR_OF INDEX `&rows[1]`（`addrof_row` RUN=127）。 |
| 2026-09-05 | **fix(windows)** **TYPE_SLICE VAR dest FIELD `b = s.xs`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest unique rec ASSIGN 只拦 DEREF dest；VAR dest 落到 SAT emit_assign SAT global T，intra emit FIELD 无 dual-GP 再 store rax+rdx（垃圾 rdx）（`slice_asg_field` SEGV 139）。存储＝leftover rest unique rec first-wins ASSIGN。消费＝want-exe `b = s.xs; return b[1]`。修：G.7 补全 leftover rest unique rec ASSIGN TYPE_SLICE FIELD／INDEX 臂扩 VAR dest（asg_lko==3）park dest＋lvalue qword-copy 16B。禁 leftover rest T SAT emit_assign／try_index。禁 leftover rest remaining-wave `!FROM_X` 或 WIN。禁 leftover rest U SAT local t copy_large。禁 dual-def WAVE277。证＠`2b329b46e`：merged pabi 1425688B＋redirect patched **659**（rec＠0xe5be first-wins／SAT emit_assign＠0x586cf／SAT emit_array_lit＠0x5a703）＋SKIP_HOT（xlang_asm 4681701＠13:56）；`slice_asg_field`／`slice_asg_field0`／`slice_asg_field_len`／`slice_asg_nest` RUN=4／3／2／4；slice_star_field／slice_star_idx／slice_asg_idx／slice_asg_lit／slice_star_lit／slice_star_asg／named_star_lit／arr_star_lit／arr0／callid／ifelse／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_SLICE let-init INDEX `let s = rows[1]`（`slice_let_idx` SEGV 139）。 |
| 2026-09-05 | **fix(windows)** **TYPE_SLICE dest-in-rbx INDEX `*p = rows[1]`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT emit_array_lit SAT global T 把 `[[1, 2], [3, 4]]` of `[2][]i32` 压成 i32 步长 4（dump mov 1,2,3,4）；leftover rest unique INDEX／lvalue 要 16B fat 行（`slice_star_idx` SEGV 139）。存储＝leftover rest unique store_fixed_array_field first-wins（SAT emit_block_inits intra redirect）。消费＝want-exe `unsafe { *p = rows[1]; return b[1]; }`。修：G.7 补全 leftover rest unique store ARRAY_LIT of TYPE_SLICE 逐 elem 发 fat（rax+rdx）写 dest+ai*16；rec ASSIGN TYPE_SLICE INDEX 臂 park dest＋lvalue qword-copy 16B。禁 leftover rest T SAT emit_array_lit／emit_assign／try_index。禁 leftover rest remaining-wave `!FROM_X` 或 WIN。禁 leftover rest U SAT local t copy_large。禁 dual-def WAVE277。证＠`55c93ba2f`（INDEX 拦截＠`938f5f362`）：merged pabi 1425534B＋redirect patched **659**（rec＠0xe5be first-wins／store_fixed＠0x12000 first-wins／SAT emit_assign＠0x5863f／SAT emit_array_lit＠0x5a673）＋SKIP_HOT（xlang_asm 4681701＠13:43）；`slice_star_idx`／`slice_star_idx0`／`slice_star_idx_len`／`slice_star_idx_row0`／`slice_star_idx_var` RUN=4／3／2／2／4；`slice_idx_rval`／`slice_idx_len`／`slice_asg_idx` RUN=4／2／4；slice_star_field／slice_star_lit／slice_star_asg／named_star_lit／arr_star_lit／arr0／callid／ifelse／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_SLICE VAR dest FIELD `b = s.xs`（`slice_asg_field` SEGV 139）。 |
| 2026-09-05 | **fix(windows)** **TYPE_SLICE dest-in-rbx FIELD `*p = s.xs`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT emit_assign SAT global T DEREF dest 走 SAT local t struct_let_init -2（FIELD 非 VAR／CALL／DEREF）后 SAT intra emit FIELD 冲 rbx 再 4B store（`slice_star_field` SEGV 139）。存储＝leftover rest unique rec first-wins ASSIGN。消费＝want-exe `unsafe { *p = s.xs; return b[1]; }`。修：G.7 补全 leftover rest unique rec ASSIGN TYPE_SLICE FIELD 臂 park dest CPU 栈＋leftover rest unique lvalue FIELD（rax=&fat）qword-copy 16B data@0+length@8。禁 leftover rest T SAT emit_assign。禁 leftover rest remaining-wave emit_assign／bump `!FROM_X` 或 WIN。禁 leftover rest U SAT local t copy_large／field_type_ref。禁 glue_emit_slice_from_array_let_init(-3)。禁 dual-def WAVE277。证＠`10cb14b3a`：merged pabi 1424780B＋redirect patched **659**（rec＠0xe5be first-wins／SAT emit_assign＠0x583cf／SAT emit_array_lit＠0x5a403）＋SKIP_HOT（xlang_asm 4681189＠13:22）；`slice_star_field`／`slice_star_field0`／`slice_star_field_len`／`slice_star_nest` RUN=4／3／2／4；slice_star_lit／slice_star_asg／named_star_lit／arr_star_lit／arr_star_asg／slice_field_arg／arr0／callid／ifelse／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_SLICE dest-in-rbx INDEX `*p = rows[1]`（`slice_star_idx` SEGV 139）。 |
| 2026-09-05 | **fix(windows)** **TYPE_SLICE dest-in-rbx ARRAY_LIT `*p = [3, 4]`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT emit_assign SAT global T DEREF dest 走 SAT local t struct_let_init -2 后 emit ARRAY_LIT 冲 rbx 再 4B store（`slice_star_lit` SEGV 139）。存储＝leftover rest unique rec first-wins ASSIGN。消费＝want-exe `unsafe { *p = [3, 4]; return b[1]; }`。修：G.7 补全 leftover rest unique rec ASSIGN TYPE_SLICE ARRAY_LIT 臂 park dest CPU 栈（POSIX DEST_IN_RBX push rbx）＋SAT emit_array_lit store data＋n_arr。禁 leftover rest T SAT emit_assign。禁 leftover rest remaining-wave emit_assign／bump `!FROM_X` 或 WIN。禁 glue_emit_slice_from_array_let_init(-3)。禁 dual-def WAVE277。证＠`631e7c8aa`：merged pabi 1424200B＋redirect patched **659**（rec＠0xe5be first-wins／SAT emit_assign＠0x581ef／SAT emit_array_lit＠0x5a223）＋SKIP_HOT（xlang_asm 4680677＠13:08）；`slice_star_lit`／`slice_star_lit0`／`slice_star_lit_len`／`slice_star_empty`／`slice_star_lit4` RUN=4／3／2／0／4；named_star_lit／arr_star_lit／arr_star_asg／slice_star_asg／arr0／callid／ifelse／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_SLICE dest-in-rbx FIELD `*p = s.xs`（`slice_star_field` SEGV 139）。 |
| 2026-09-05 | **fix(windows)** **TYPE_NAMED dest-in-rbx STRUCT_LIT `*p = P { x: 3, y: 4 }`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT emit_assign SAT global T DEREF dest 走 SAT local t struct_let_init → SAT local t fields_elf(-3) lea rbp-3；frame spill 又与 SAT implicit dest 叠槽（`named_star_lit` dump SAT 把 x=3 写到 dest 指针＠rbp-0x38 → SEGV 139）。存储＝leftover rest unique rec first-wins ASSIGN。消费＝want-exe `unsafe { *p = P { x: 3, y: 4 }; return b.y; }`。修：G.7 补全 leftover rest unique rec ASSIGN TYPE_NAMED STRUCT_LIT 臂 park dest CPU 栈（POSIX DEST_IN_RBX push rbx）＋SAT emit_struct_lit qword-copy。禁 leftover rest T SAT emit_assign。禁 leftover rest unique T SAT local t struct_let_init／fields_elf。禁 remaining-wave `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`46f12fcd0`（park 修；拦截＠`8b43968f2`）：merged pabi 1423714B＋redirect patched **659**（rec＠0xe5be first-wins／SAT emit_assign＠0x5804f）＋SKIP_HOT（xlang_asm 4680165＠12:54）；`named_star_lit`／`named_star_lit0`／`named_star_lit16`／`named_star_lit16b` RUN=4／3／4／2；named_star_asg／arr_star_asg／arr_star_lit／arr_ret_whole／arr0／callid／ifelse／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_SLICE dest-in-rbx ARRAY_LIT `*p = [3, 4]`（`slice_star_lit` SEGV 139）。 |
| 2026-09-05 | **fix(windows)** **TYPE_ARRAY 整数组 return `return a`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT remaining-wave Path B0 `#ifndef FROM_X` ABSENT；leftover rest unique emit_return first-wins 只 emit_expr，VAR load_var payload 当 E*（`arr_ret_whole` SEGV 139）。存储＝leftover rest unique emit_return first-wins。消费＝want-exe `let y: [2]i32 = mk(); return y[1]`。修：G.7 补全 leftover rest unique emit_return Path B0 durable COMMON（lvalue src E*＋COMMON 拷贝；禁 leftover rest remaining-wave emit_return `!FROM_X` 或 WIN）。禁 dual-def WAVE277。证＠`0e21c1b0b`（helper＠`9fdb8daf7`）：merged pabi 1422896B＋redirect patched **659**（emit_return＠0x20bc3 first-wins／helper＠0x12042／rec＠0xe5be）＋SKIP_HOT（xlang_asm 4679653＠12:36）；`arr_ret_whole`／`arr_ret_whole0`／`arr_ret_whole16`／`arr_ret_formal`／`arr_ret_field`／`arr_ret_idx`／`arr_ret_deref`／`arr_ret_lit` RUN=4／3／2／4／4／4／4／4；arr_star_asg／arr_asg_deref／arr_deref_let／arr_ptr_arg／arr_var_let／arr0／callid／ifelse／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_NAMED dest-in-rbx STRUCT_LIT `*p = P { x: 3, y: 4 }`（`named_star_lit` SEGV 139）。 |
| 2026-09-05 | **fix(windows)** **TYPE_ARRAY dest-in-rbx `*p = a`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT emit_assign SAT global T DEREF dest 走 SAT local t struct_let_init，TYPE_ARRAY 8B -2 后 4B store（`arr_star_asg` RUN=0）。存储＝leftover rest unique rec first-wins＋leftover rest unique store dest-in-rbx。消费＝want-exe `unsafe { *p = a; return b[1]; }`。修：G.7 补全 leftover rest unique rec ASSIGN 臂 lea dest＋let_init(-3)；store 跳过 mag=-3、park rbx。禁 leftover rest T SAT emit_assign。禁 remaining-wave `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`921fee867`：merged pabi 1421352B＋redirect patched **659**（store＠0x119d0 first-wins／let_init＠0x11fa5／rec＠0xe5be）＋SKIP_HOT（xlang_asm 4677495＠12:14）；`arr_star_asg`／`arr_star_asg0`／`arr_star_lit`／`arr_star_deref`／`arr_star_asg16` RUN=4／3／4／4／2；named_star_asg／arr_asg_deref／arr_deref_let／arr_ptr_arg／arr_var_let／arr0／callid／ifelse／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_ARRAY 整数组 return `return a`（`arr_ret_whole` SEGV 139）。 |
| 2026-09-05 | **docs(selfhost)** **TYPE_ARRAY 整数组赋值 `y = *p` 证绿** | **文档** · **不升钉** · Windows leftover-PE L2 探针-only（无 leftover rest 重编；代码已在 `be3cbf90e` SAT let_init SCAN＋store REDIRECT）。五问：产生＝SAT emit_assign 未入 SCAN，但 SAT let_init 已 SCAN，intra store 已 redirect leftover rest unique T（DEREF E* 拷贝）。存储＝SAT emit_assign SAT global T（禁 leftover rest T）。消费＝want-exe `unsafe { y = *p; return y[1]; }`。证＠`be3cbf90e` 二进制／docs tip 本波：xlang_asm 4676471＠11:51；`arr_asg_deref`／`arr_asg_deref0`／`arr_asg_deref16`／`arr_asg_deref_idx` RUN=4／3／2／9；`arr_struct_deref`／`arr_asg_lit`／`arr_asg_var`／`arr_asg_call`／`arr_asg_field`／`arr_asg_idx`／`arr_asg_call16` RUN=4／4／4／4／4／4／2；arr_deref_let／arr_ptr_arg／arr_var_let／arr0／callid／ifelse／rv42 不回归。禁 leftover rest T SAT emit_assign。禁 remaining-wave `!FROM_X` 或 WIN。禁 dual-def WAVE277。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_ARRAY dest-in-rbx `*p = a`（`arr_star_asg` RUN=0）。 |
| 2026-09-05 | **fix(windows)** **TYPE_ARRAY DEREF let-init E\* 拷贝** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT local t store_fixed_array_field CALL／INDEX E* 拷贝；DEREF=52 返 -2 → SAT emit_block_inits CG002。存储＝leftover rest unique FROM_X&&WIN T first-wins＋redirect SAT intra。消费＝want-exe `unsafe { let y: [2]i32 = *p; return y[1]; }`。修：G.7 补全权威 E* 分支含 DEREF；leftover rest unique FULL body（禁 DEREF-only 藏 SAT ARRAY_LIT／VAR／CALL）；redirect 表收 callee。禁 leftover rest T SAT emit_block_inits。禁 remaining-wave `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`be3cbf90e`（unique＠`9a89f1c3a`／UNDEF-fix＠`7bb45684b`）：merged pabi 1420294B＋redirect patched **659**（store_fixed_array_field＠0x11726 first-wins／let_init＠0x11c00 first-wins）＋SKIP_HOT（xlang_asm 4676471＠11:51）；`arr_deref_let`／`arr_deref_let0`／`arr_deref_let16`／`arr_deref_let_idx` RUN=4／3／2／9；arr_var_let／arr_call_let／arr_idx_let／arr_field_let／arr_ptr_arg／arr0／fnarr0／arr_field_arg16／arr_ptr_arg16／slice_ptr_let／letinit_i32／callid／ifelse／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_ARRAY 整数组赋值 `y = *p`。 |
| 2026-09-05 | **docs(selfhost)** **FIELD TYPE_ARRAY 16B 作参 leave E\* 证绿** | **文档** · **不升钉** · Windows leftover-PE L2 探针-only（无 leftover rest 重编；代码已在 `b6bdaf3b5` rec leave-E*＋for_call_args lvalue ＞8B leave E*）。五问：产生＝旧 SEGV 139 是 8B 刀闭合前 rec 4B-load 把 `a[0]=1` 当 dest；＞8B leftover-PE 形参槽是 dest E*。存储＝leftover rest WIN leftover for_call_args／var_field_access first-wins。消费＝want-exe `take(s.a)` of `[4]i32`。证＠`9739736be` 二进制／docs tip `9424c5e76`：pabi 1417176B（for_call_args＠0x14256 first-wins／GET＠0x1b9f9）＋xlang_asm 4674205＠09:59；`arr_field_arg16`／`arr_field_arg16_0`／`arr_field_arg16_3`／`arr_field_idx16` RUN=2／1／4／2；顺带 `arr_call_arg16`／`arr_idx_arg16`／`i32arg16_let`／`arr_ptr_arg16` RUN=2；arr_field_arg／arr_idx_arg／arr0／fnarr0／fnarr16／slice_field_arg／arr_ptr_arg／callid／ifelse／rv42 不回归。禁 leftover rest T SAT try_index。禁 remaining-wave `!FROM_X` 或 WIN。禁 dual-def WAVE277。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_ARRAY DEREF let-init `unsafe { let y: [2]i32 = *p }`。 |
| 2026-09-05 | **fix(windows)** **INDEX of `[2][2]i32` 作 TYPE_ARRAY 形参 rec load_64** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT emit_index dest-stamp rtk==10 留行 E*（SysV always-E*）；leftover rest for_call_args INDEX 无 tk==10 packing，dest 当 8B INTEGER 后 SAT INDEX lea-home（`arr_idx_arg` RUN=229）。存储＝leftover rest WIN leftover for_call_args first-wins。消费＝want-exe `take(rows[1])` of `[2][2]i32`。修：G.7 补全 leftover rest for_call_args INDEX tk==10 rec 后 ≤8B load_64／＞8B leave E*（CALL／DEREF／ARRAY_LIT 孪生）。只在 WAVE278 INDEX resolved 是 TYPE_ARRAY 时 packing（leave-E* 门）。禁 leftover rest T SAT try_index。禁 remaining-wave scaled `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`9739736be`：merged pabi 1417176B＋redirect patched **654**（for_call_args＠0x14256 first-wins）＋SKIP_HOT（xlang_asm 4674205＠09:59）；`arr_idx_arg`／`arr_idx_arg0`／`arr_idx_row0`／`arr_idx_id` RUN=4／3／2／4；arr_call_arg／arr_field_arg／arr_ptr_arg／fnarr_arg／arr0／fnarr0／i32one_var／slice_ptr_arg／slice_field_arg／slice_call_arg／nest_deref_arr／arr_ptr_arg16／nest_idx／callid／ifelse／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：FIELD TYPE_ARRAY 16B 作参 `take(s.a)` of `[4]i32`。 |
| 2026-09-05 | **fix(windows)** **CALL 返回 TYPE_ARRAY 8B 作参 durable E\*＋load_64** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover-PE get_return 剥 RETURN 成 ARRAY_LIT；SAT emit_array_lit 留栈 dest（remaining-wave emit_return TYPE_ARRAY durable E*＠19252 `#ifndef FROM_X` ABSENT）；for_call_args CALL 无 tk==10 packing，dest 当 8B INTEGER 后 SAT INDEX lea-home（`arr_call_arg` RUN=226）。存储＝leftover rest WIN leftover mega_body first-wins＋for_call_args first-wins。消费＝want-exe `take(mk())` of `[2]i32`。修：G.7 补全 leftover rest mega_body ARRAY_LIT TYPE_ARRAY return durable COMMON E*（TYPE_SLICE 孪生、无 length）；for_call_args CALL／METHOD tk==10 rec 后 ≤8B load_64／＞8B leave E*（ARRAY_LIT／DEREF 孪生）。dest pty 走 leftover rest unique GET。禁 leftover rest T SAT try_index。禁 remaining-wave emit_return `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`c11bc6a1d`：merged pabi 1416964B＋redirect patched **654**（for_call_args＠0x14256 first-wins／mega_body＠0x39055 first-wins／durable＠0x15972 first-wins）＋SKIP_HOT（xlang_asm 4673693＠09:37）；`arr_call_arg`／`arr_call_arg0`／`arr_call_id` RUN=4／3／4；slice_call_arg／arr_field_arg／arr_ptr_arg／fnarr_arg／arr0／fnarr0／i32one_var／slice_ptr_arg／slice_field_arg／nest_deref_arr／arr_ptr_arg16／callid／ifelse／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：INDEX of `[2][2]i32` 作 TYPE_ARRAY 形参 `take(rows[1])`。 |
| 2026-09-05 | **fix(windows)** **FIELD TYPE_ARRAY 8B 作参 rec load_64** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest unique var_field_access rec 4B-load（SAT load_byte_sz TYPE_ARRAY catch-all 4）；INTEGER home 后 SAT INDEX lea-home of 3（`arr_field_arg` RUN=0）。存储＝leftover rest WIN leftover var_field_access first-wins。消费＝want-exe `take(s.a)` of `[2]i32` 字段。修：G.7 补全 rec：layout TYPE_ARRAY ≤8B load_64；＞8B leave E*；helper 用 emit_module_ref（ctx module 可非空无 layout）。禁 leftover rest T SAT load_byte_sz／try_index。禁 remaining-wave 221 `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`b6bdaf3b5`：merged pabi 1416512B＋redirect patched **654**（var_field_access＠0x1e58f first-wins／helper＠0x13ea0）＋SKIP_HOT（xlang_asm 4673181＠09:16）；`arr_field_arg`／`arr_field_arg0` RUN=4／3；arr_field_idx／arr_ptr_arg／arr_ptr_arg0／arr_ptr_arg16／slice_field_arg／arr0／fnarr0／fnarr_arg／i32one_var／slice_ptr_arg／nest_deref_arr／rv42 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：CALL 返回 TYPE_ARRAY 作参 `take(mk())`。 |
| 2026-09-05 | **docs(selfhost)** **日切：4 日归档／5 号主文档重写** | **文档** · **不升钉**。[自举进度-归档-2026-09-04.md](自举进度-归档-2026-09-04.md) 收 4 日 **40** 行（纯 asm 邻域→ARRAY_LIT TYPE_FN STRUCT_LIT 字段；leftover unique **0**）；主文档只留 KPI＋前排＋5 日 §6。活面＠`92177b527`：TYPE_ARRAY 8B DEREF 作参 `arr_ptr_arg` RUN=4；FIELD TYPE_ARRAY 8B 作参 `arr_field_arg` 仍 RUN=0。WAVE277 ALWAYS 已 T `rebuild_sparse_ifs`（禁 leftover rest 第二份 dual-def）。禁改测试期望。PLATFORM: SHARED 文档／WINDOWS leftover-PE 探针。下一刀：FIELD TYPE_ARRAY 8B 作参 `take(s.a)`。 |
| 2026-09-05 | **fix(windows)** **TYPE_ARRAY 8B DEREF 作参 load_64** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest emit_deref TYPE_ARRAY leave-ptr；8B leftover-PE 形参槽是 payload（VAR load_var／ARRAY_LIT load_64），rec E* 当 8B INTEGER 后 SAT INDEX lea-home-as-payload（`arr_ptr_arg` RUN=110＝E* 高 32）。存储＝leftover rest WIN leftover for_call_args。消费＝want-exe `take(*p)` of `*[2]i32`。修：G.7 补全 leftover rest for_call_args DEREF tk==10：rec 后 ≤8B load_64；＞8B 仍 E*。nbytes 走 leftover rest `glue_fixed_array_total_bytes`（已 T＠15009）。禁 leftover rest T SAT try_index（arr0 intercept）。禁 remaining-wave `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`db81db6f2`：merged pabi 1413386B＋redirect patched **654**（for_call_args＠0x13ea0 first-wins）＋SKIP_HOT（xlang_asm 4670405＠06:38）；`arr_ptr_arg`／`arr_ptr_arg0`／`arr_ptr_arg16` RUN=4／3／4；arr_ptr／arr_ptr_var／arr_ptr_lit／arr0／fnarr0／fnarr_arg／fnarr16／i32one_var／i32s1／i32slen_let／slice_ptr_arg／slice_call_let／slice_call_let_len／slice_call_arg／slice_field_arg／fnslice／fnslice_let／i32store／rv42／callid／ifelse 不回归。`take(s.a)` FIELD TYPE_ARRAY 仍 RUN=0。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：FIELD TYPE_ARRAY 8B 作参 `take(s.a)`。 |
| 2026-09-05 | **fix(windows)** **ARRAY_LIT TYPE_SLICE 局部 `.length` win_glue C fat** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝命名 let 正 product offset（`[rbp-off]`）；SAT slice_from_array SAT t x86 home-8 写 length（C fat 内存 data+8）；leftover rest unique store／FIELD 曾 always `+8`（内存 data-8）。`i32slen_let` 先 RED=56，always `+8` 后局部 FIELD／INDEX 自洽但 `lea fat*` 作参 SEGV。存储＝leftover rest unique length_off＋store_retval_pair＋var_field_access。消费＝want-exe `let xs: []i32 = [3, 4]; return xs.length`。修：G.7 补全 leftover rest unique length_off／store／FIELD 共用 win_glue x86 home-8；redirect SAT intra。禁 leftover rest T SAT try_index／length_to_rbx／remaining-wave `!FROM_X` 或 WIN／dual-def WAVE277。证＠`35f5b39f8`（unique T＠`554267d0c`）：leftover rest 432178B＋redirect patched **654**（length_off＠0x111dd first-wins）＋SKIP_HOT（pabi 1413204；xlang_asm 4670405＠06:14）；`i32slen_let`／`i32slen_let0`／`i32local` RUN=2／3／4；i32one_var／slice_ptr_arg／slice_call_let_len／slice_call_let／i32slen／i32s1／fnslice／fnslice_let／arr0／fnarr0／i32store／slice_call_arg／slice_field_arg／rv42／callid／ifelse 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_ARRAY 8B DEREF 作参 `take(*p)` of `*[2]i32`。 |
| 2026-09-05 | **fix(windows)** **CALL TYPE_SLICE let-init 局部 `.length` C fat** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest unique store_retval_pair 把 rdx 存 home+8（C fat／SAT INDEX）；leftover rest var_field_access `.length` 仍 win_glue x86 home-8（`slice_call_let_len` RUN=0）。存储＝leftover rest WIN leftover `pipeline_asm_emit_var_field_access_elf_c`。消费＝want-exe `let s: []i32 = mk(); return s.length`。修：G.7 补全局部 dual-GP `.length` 读 `var_off+8`。win_glue home-8 留给 for_call_args 正 next_offset 临时槽。形参仍 fat*+8。禁 leftover rest T SAT try_index／remaining-wave `!FROM_X` 或 WIN／dual-def WAVE277。证＠`a14d2bf15`：leftover rest 431894B＋redirect patched **646**（var_field_access＠0x1dbcb first-wins）＋SKIP_HOT（pabi 1412952；xlang_asm 4669821＠05:42）；`slice_call_let_len` RUN=2；slice_call_let／slice_call_let0／slice_call_arg／slice_field_arg／slice_ptr_arg／slice_ptr_len／i32slen／i32s1／i32one_var／i32local／fnslice／fnslice_let／arr0／fnarr0／i32store／rv42／callid／ifelse 不回归。`i32slen_let` ARRAY_LIT 局部 `.length` 仍 RUN=56。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：ARRAY_LIT TYPE_SLICE 局部 `.length`。 |
| 2026-09-05 | **fix(windows)** **CALL TYPE_SLICE let-init dual-GP** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT emit_block_inits intra SAT glue_emit_struct_type_let_init intra SAT store_retval_pair（local t）存 rax+rdx 后 reent 深拷贝用 32 位 addl 截断 PE COMMON（`slice_call_let` SEGV 139）。存储＝leftover rest WIN leftover unique store_retval_pair＋redirect SAT intra。消费＝want-exe `let s: []i32 = mk(); return s[1]`。修：G.7 补全 leftover rest unique 存 rax+rdx、跳过 reent（payload 已是 durable COMMON）；redirect SAT store_retval_pair。禁 leftover rest U SAT local t copy_large。禁 leftover rest remaining-wave stub `!FROM_X` 或 WIN。禁 leftover rest T SAT try_index。禁 dual-def WAVE277。证＠`48727f045`（unique＠`32e15fd07`／UNDEF-fix＠`53ccf20b2`）：leftover rest 432046B＋redirect patched **646**（store_retval_pair＠0x111dd first-wins）＋SKIP_HOT（pabi 1413104；xlang_asm 4669875＠05:22）；`slice_call_let`／`slice_call_let0`／`slice_call_let_id`／`fnslice_call_let` RUN=4／3／4／7；slice_call_arg／slice_field_arg／slice_ptr_arg／fnslice／i32s1／i32one_var／arr0／fnarr0／i32store／slice_ptr／nest_deref_arg／rv42／callid／ifelse 不回归。`slice_call_let_len` 仍 RUN=0。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：CALL 返回 TYPE_SLICE let-init 的局部 `.length`。 |
| 2026-09-05 | **fix(windows)** **FIELD TYPE_SLICE wrap fat\*** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝STRUCT_LIT TYPE_SLICE 字段 field_store_sz 捕到 8 只存 ARRAY_LIT dest 指针；FIELD load_byte_sz 4（`mov (%rax),%eax`）。存储＝leftover rest field_store_sz／rec ARRAY_LIT／var_field_access／for_call_args。消费＝want-exe `take(s.xs)`。修：G.7 补全 store_sz=16、rec ARRAY_LIT dual-GP（rax=data／rdx=n_arr）、FIELD deref_struct16、for_call_args FIELD wrap lea fat*。禁 leftover rest T SAT try_index／remaining-wave 221／WAVE277 dual-def。证＠`96364cea6`：leftover rest 431578B＋redirect patched **642**（for_call_args＠0x13d43 first-wins）＋SKIP_HOT（pabi 1412672；xlang_asm 4669857＠04:46）；`slice_field_arg`／`slice_field_arg0`／`slice_field_len`／`slice_field_idx`／`slice_field_bare` RUN=4／3／2／4／4；slice_call_arg／slice_ptr_arg／fnslice／i32s1／i32one_var／arr0／fnarr0／i32store／slice_ptr／rv42／callid／ifelse／nest_deref_arg 不回归。`let s=mk()` 仍 SEGV 139。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：CALL 返回 TYPE_SLICE let-init `let s = mk()`。 |
| 2026-09-05 | **fix(windows)** **CALL 返回 TYPE_SLICE wrap fat\*** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover-PE get_return 剥 RETURN 成 ARRAY_LIT，SAT emit_array_lit 留 dest 指针（remaining-wave emit_return dual-GP `#ifndef FROM_X` ABSENT）；caller rec 把 16B rax+rdx 当 2 GP，take 要 1 GP fat*。存储＝leftover rest mega_body get_return＋leftover rest for_call_args。消费＝want-exe `take(mk())`。修：G.7 补全 leftover rest mega_body ARRAY_LIT TYPE_SLICE return durable COMMON＋rdx=n_arr；for_call_args CALL／METHOD wrap store rax+rdx／lea fat*（POSIX ＠79744 同 wrap，不抄 reent deep-copy）。禁 leftover rest remaining-wave emit_return／221／SAT try_index。禁 dual-def WAVE277。证＠`8b98c0572`：leftover rest 430750B＋redirect patched **642**（for_call_args＠0x13c24 first-wins）＋SKIP_HOT（pabi 1411844；xlang_asm 4668833＠04:35）；`slice_call_arg`／`slice_call_arg0`／`slice_call_len`／`slice_id_arg`／`fnslice_call` RUN=4／3／2／4／7；slice_ptr_arg／slice_ptr／twoarg_deref／i32one_var／i32s1／fnslice_let／fnarr0／arr0／i32store／fnslice／rv42／callid／ifelse／nest_deref_arg 不回归。`take(s.xs)`／`let s=mk()` 仍 SEGV 139。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：FIELD TYPE_SLICE 作参 `take(s.xs)`。 |
| 2026-09-05 | **fix(windows)** **TYPE_SLICE DEREF 作参 wrap fat\*** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest for_call_args TYPE_SLICE packing 只认 VAR `enc_local`（local lea／formal load）；DEREF 走 rec 留 dual-GP（rax=data／rdx=length）当 1 GP fat*，callee extra-deref → SEGV 139。存储＝leftover rest WIN leftover for_call_args（`!FROM_X` 或 WIN；POSIX `.x` ＠79135）。消费＝want-exe `take(*p)`。修：G.7 补全 leftover rest for_call_args DEREF tk==11：rec 后 store rax+rdx 到 16B home／lea fat*（POSIX CALL TYPE_SLICE ＠79744 同 wrap）。kind 走 WAVE278 `resolved_type_ref`。禁 leftover rest T SAT try_index（arr0 intercept）。禁 remaining-wave 221／`!FROM_X` 或 WIN。禁抄 `.x` packer。禁 dual-def WAVE277。证＠`5322f3b2b`：leftover rest 429754B＋redirect patched **642**（for_call_args＠0x13c24 first-wins）＋SKIP_HOT（pabi 1410848；xlang_asm 4667809＠04:21）；`slice_ptr_arg`／`slice_ptr_arg0`／`slice_ptr_arg_len` RUN=4／3／2；`fnslice_ptr_arg`／`nest_deref_arg`／`twoarg_deref` RUN=7／4／4；slice_ptr／slice_ptr0／slice_ptr_var／slice_ptr_formal／addrof_sidx_var／addrof_sidx／i32store／i32one_var／i32s1／fnslice_let／fnarr0／arr0／arrstore／i32slen_var／fnslice／rv42／callid／ifelse／dbl_deref／ptr_store／ptr_add／slice_ptr_store／slice_ptr_len／addrof_slice_ptr／slice_ptr_let／slice_ptr_addasg／arr_ptr 不回归。`take(mk())` 仍 SEGV 139。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：CALL 返回 TYPE_SLICE 作参 `take(mk())`。 |
| 2026-09-05 | **fix(windows)** **INDEX DEREF `*[]i32` dual-GP fat** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest emit_deref TYPE_SLICE 留指针（ARRAY twin，`*i32` 刀）；SAT rvalue_slice_once wave773 把 DEREF of *[]T 当 dual-GP（rax=data／rdx=length），leave-ptr 把 fat home 当 data → `(*p)[1]` 读 *(fat+4)（`slice_ptr` RUN=246）。存储＝leftover rest WIN leftover emit_deref（rec 簇 `#ifdef FROM_X`；remaining-wave140 `#ifndef FROM_X` SAT T）。消费＝want-exe `(*p)[1]`。修：G.7 补全 leftover rest emit_deref TYPE_SLICE 走 SAT T `pipeline_asm_deref_struct16_rax_ptr_elf_c`（leftover rest remaining-wave stub `#ifndef FROM_X` ABSENT；同 leftover rest `glue_load_var_as_value_to_rax_rdx` U）。禁 leftover rest T SAT try_index（arr0 intercept）。禁 remaining-wave stub `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`3acdc367c`：leftover rest gcc＋redirect patched **642**（emit_deref＠0xfb26）＋SKIP_HOT（xlang_asm 5028033＠04:04）；`slice_ptr`／`slice_ptr0`／`slice_ptr_var`／`slice_ptr_formal` RUN=4／3／4／4；`(*p)[0]=9`／`(*p).length`／`&(*p)[1]`／`let s=*p`／`(*p)[0]+=6`／`(*a)[1]` TYPE_ARRAY 绿；addrof_sidx_var／addrof_sidx／i32store／i32one_var／i32s1／fnslice_let／fnarr0／arr0／arrstore／i32slen_var／fnslice／rv42／callid／ifelse／dbl_deref／ptr_store／ptr_add 不回归。`take(*p)` 仍 SEGV 139。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_SLICE DEREF 作参 `take(*p)`。 |
| 2026-09-05 | **fix(windows)** **ADDR_OF INDEX TYPE_SLICE 非字面量 scaled lea** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest lvalue INDEX 非字面量 `mul_imm_to_rbx`＋`backend_enc_add_rax_rbx_arch`（32 位 ADD EAX,EBX 零扩展 rax，丢掉 PE `0x14000xxxx` 高位 → SEGV 139；字面量 `&xs[1]` 走 REX.W `add_imm_to_rax` 仍绿）。存储＝leftover rest WIN leftover lvalue（rec 簇 `#ifdef FROM_X`；remaining-wave stub `#ifndef FROM_X` ABSENT）。消费＝want-exe `&xs[i]` 再 `unsafe { return *p }`。修：G.7 补全 leftover rest lvalue INDEX 非字面量：emit idx（i32 sxt）后 64-bit `lea [rax+rbx*esz]`。禁 leftover rest T SAT try_index（arr0 intercept）。禁 remaining-wave scaled `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`817b6447e`：leftover rest 429368B＋redirect patched **642**（lvalue＠0xef14）＋SKIP_HOT（pabi 1410462；xlang_asm 4667809＠03:47）；`addrof_sidx_var`／`addrof_sidx_var0`／`addrof_sidx_var_formal` RUN=4／3／4；`addrof_idx_var` RUN=4；objdump `lea (%rax,%rbx,4)`；addrof_sidx／addrof_sidx1／addrof_sidx_formal／i32store／i32store_local／i32one_var／i32s1／i32local／i32slen_var／fnslice／fnslice_let／fnslice_var／fnarr_arg／fnarr0／arr0／arrstore／rv42／callid／ifelse 不回归。`slice_ptr` `(*p)[1]` 仍 RUN=246。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：INDEX DEREF `*[]i32` `(*p)[1]`。 |
| 2026-09-05 | **fix(windows)** **ADDR_OF INDEX TYPE_SLICE fat.data** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest lvalue INDEX 对 TYPE_SLICE VAR 只 lea fat／load fat*，未剥 data（POSIX `.x` try_index 先 load fat.data；`&xs[0]` 把 fat 当地址 → RUN=127）。存储＝leftover rest WIN leftover lvalue（rec 簇 `#ifdef FROM_X`；remaining-wave stub `#ifndef FROM_X` ABSENT）。消费＝want-exe `&xs[0]` 再 `unsafe { return *p }`。修：G.7 补全 leftover rest lvalue INDEX：base rtk==11 则 load_64_from_rax。禁 leftover rest T SAT try_index（arr0 intercept）。禁 remaining-wave `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`80e3c6e04`：leftover rest 429226B＋redirect patched **642**（lvalue＠0xef14）＋SKIP_HOT（pabi 1410430；xlang_asm 4667809＠03:28）；`addrof_sidx`／`addrof_sidx1`／`addrof_sidx_formal` RUN=3／4／3；addrof_idx／i32store／i32store_local／i32one_var／i32s1／i32local／i32slen_var／fnslice／fnslice_let／fnslice_var／fnarr_arg／fnarr0／arr0／arrstore／rv42／callid／ifelse 不回归。`addrof_sidx_var` `&xs[i]` 仍 SEGV 139。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：ADDR_OF INDEX TYPE_SLICE 非字面量下标 `&xs[i]`。 |
| 2026-09-05 | **fix(windows)** **INDEX TYPE_SLICE store COMMON BSS** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT durable jmp-over 把 const i32 嵌进 RX `.text`（`let xs: []i32 = [3, 4]; xs[0] = 9` 写 RX → SEGV 139；`take([3, 4], 9)` 栈 dest 已绿）。存储＝SAT local t durable（leftover rest remaining-wave `#ifndef FROM_X` ABSENT）。消费＝want-exe INDEX TYPE_SLICE 形参／局部 store。修：G.7 leftover rest WIN leftover unique 跳过 jmp-over，走 SHN_COMMON BSS；redirect SAT slice_from_array intra SAT durable。禁 leftover rest T SAT emit_assign／try_index／global T。禁 remaining-wave `!FROM_X` 或 WIN。禁 leftover rest U SAT local t `seq_take`／`scalar_elem`（const-only＋本地 seq；非 const 退栈）。禁 dual-def WAVE277。证＠`f9f6f08ba`（unique＠`68d712661`）：leftover rest 429126B＋redirect patched **642**（durable＠0x14c89）＋SKIP_HOT（pabi 1410330；xlang_asm 4667809＠03:11）；`i32store`／`i32store_onearg`／`i32store_local`／`i32store1`／`i32asgslice` RUN=9；`nm` `B Lxlang_al_0`＠14000d1e0；i32one_var／i32s1／i32local／i32slen_var／fnslice／fnslice_let／fnslice_var／fnarr_arg／fnarr0／arr0／arrstore／arrstore_arg／param_asg／rv42／callid／ifelse 不回归。`addrof_sidx` `&xs[0]` 仍 127。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：ADDR_OF INDEX TYPE_SLICE `&xs[0]`。 |
| 2026-09-05 | **fix(windows)** **nso_at 非 RETURN expr-stmt** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest nso_at 只认 if／let／loop／for／region，无 let 的 ASSIGN／CALL mega 走 get_return 丢掉（`take(x){x=9; return x}` RUN=3）。存储＝W277_Block.num_expr_stmts＠0x48。消费＝mega nso＞0 → emit_block。修：G.7 补全 nso_at：非 RETURN expr-stmt 才抬 nso；纯 RETURN 仍 get_return（leftover-PE parser 不一定 fold trailing return；emit_block 与 final_expr 对 RETURN 互跳）。禁 leftover rest T SAT emit_assign／try_index。禁 dual-def WAVE277。禁 remaining-wave `!FROM_X` 或 WIN。证＠`824c7c23d`（盲 nexpr＠`5e5b82ac2` 曾 rv42＝1）：leftover rest 427620B＋redirect patched **633**＋SKIP_HOT（pabi 1408870；xlang_asm 4666215＠02:43）；`param_asg` RUN=9／`arrstore_arg` RUN=9；`i32store`／`i32store_local` 仍 SEGV 139；rv42／i32one_var／i32s1／i32local／i32slen_var／fnslice／fnslice_let／fnslice_var／fnarr_arg／fnarr0／arr0／arrstore／callid／ifelse 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：INDEX TYPE_SLICE 形参／局部 store。 |
| 2026-09-05 | **fix(windows)** **INDEX TYPE_SLICE 形参 bounds fat\*+8** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT emit_index intra SAT length_to_rbx intra SAT needs_ptr_load（local t 返 0）把 8B fat\* home 当 16B dual-GP，length 读 saved rbx（param_home+8）→ panic 127（`i32one_var`／`xs[1]`；`xs[0]` 可假绿）。存储＝redirect EMIT_SCAN。消费＝want-exe `take(xs){return xs[0]}`。修：G.7 补全表扫 SAT length → leftover rest WIN leftover needs_ptr_load tk==11。禁 leftover rest T SAT length／try_index／remaining-wave `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`2fbf82dff`：redirect patched **7**（含 needs_ptr_load＠0x149ab）＋SKIP_HOT（xlang_asm 4666215＠02:20）；`i32one_var`／`i32s1`／`i32s1_var` RUN=3／4／4；objdump `load fat*; add 8; load length`；i32local／i32slen_var／fnslice／fnslice_let／fnarr_arg／fnarr16／fnarr0／arr0／rv42／callid／ifelse 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：INDEX TYPE_SLICE 形参 store。 |
| 2026-09-05 | **fix(windows)** **COFF COMMON BSS（TYPE_FN let-init）** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝COFF writer 把 SHN_COMMON 写成 SectionNumber=1 Type=FUNCTION，`Lxlang_al_*` 落 RX `.text`（store／call SEGV 139）；ELF／Mach-O 已进 BSS。存储＝`ElfSymEntry.sym_shndx=0xfff2`＋offset=size。消费＝want-exe `let fs: []function(): i32 = [foo]; fs[0]()`。修：G.7 补全 seed `seed_platform_coff_write_coff_o_to_buf`＋`coff.x` 孪生：COMMON → IMAGE_SYM_UNDEFINED＋Value=size＋EXTERNAL。禁 leftover rest remaining-wave `!FROM_X` 或 WIN。禁 leftover rest T SAT try_index／WAVE277。证＠`966d3c4c7`：seed_bridge 12082B＠01:59＋SKIP_HOT（xlang_asm 4666215＠02:03）；`nm` `B Lxlang_al_0`；`fnslice_let`／`fnslice_var` RUN=7；i32slen_var／fnslice／fnarr_arg／fnarr16／fnarr0／arr0／rv42／callid／ifelse 不回归。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／SHARED COFF cross-emit。下一刀：INDEX TYPE_SLICE 形参 bounds。 |
| 2026-09-05 | **fix(windows)** **leftover rest VAR TYPE_SLICE packing lea fat\*** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest for_call_args VAR 走 `load_var` dual-GP（data 当 1 GP）；形参要 fat\*。存储＝leftover rest WIN leftover for_call_args。消费＝want-exe `let fs; take(fs)`。修：G.7 补全 tk==11 走 leftover rest `glue_enc_local`（local lea／formal load）。kind 走 var_decl／WAVE278。禁 remaining-wave 221 pty。禁 leftover rest T SAT try_index。禁抄 `.x` TYPE_ARRAY-as-SLICE wrap。证＠`f736b04a4`：leftover rest 427508B＋redirect patched **626**＋SKIP_HOT（pabi 1408758；xlang_asm 4666548＠01:36）；`i32slen_var` RUN=2（fat\* 绿）；`fnslice_var`／`fnslice_let` 仍 SEGV 139（TYPE_FN let-init COMMON 落 `.text`）；`i32one_var` INDEX 127（INDEX 形参 bounds 读 saved rbx）。fnslice／fnarr_arg／fnarr16／fnarr0／arr0／rv42／callid／ifelse 不回归。刀未闭。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_SLICE TYPE_FN let-init COMMON。 |
| 2026-09-05 | **fix(windows)** **leftover rest TYPE_SLICE 作参 `take([foo])`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest for_call_args ARRAY_LIT tk≠10 仍 dest 指针；TYPE_SLICE 形参要 fat*（1 GP）；SAT dest 与 fat home 叠槽则 INDEX0 读 dest 指针位（127）。存储＝leftover rest WIN leftover for_call_args。消费＝want-exe `take([foo])`。修：G.7 补全 dest-SLICE stamp 后 wrap `{data,n_arr}`；先留 home＋16B 空隙再 rec。kind 走 WAVE278 `resolved_type_ref`。禁抄 `.x` durable／SIMD packer。禁 leftover rest T SAT try_index。禁 remaining-wave 221 `call_param_ty_get`。证＠`3b28e38ab`（wrap＠`e9590c023`／home＠`13361d13c`）：leftover rest 427444B＋redirect patched **626**＋SKIP_HOT（pabi 1408238→1408694；xlang_asm 4666548＠01:24）；`fnslice`／`fnslice2`／`i32slice` RUN=7；`i32one`／`i32s0`／`i32s1`／`i32slen` 绿；fnarr_arg／fnarr16／i32arg16／fnarr0／arr0／rv42／callid／ifelse 不回归。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：VAR TYPE_SLICE 作参。 |
| 2026-09-05 | **fix(windows)** **leftover rest TYPE_ARRAY 16B 作参 `apply([foo, bar])`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SysV ARRAY→8 E*，SAT try_index intra SAT enc_local lea 8B 槽当 payload（8B load_64 补偿）；16B dest 指针进 rdi → INDEX `call *dest` SEGV 139／`[4]i32` 垃圾和。VAR 16B dual-GP 同红。存储＝leftover rest WIN leftover needs_ptr_load／for_call_args。消费＝want-exe `apply([foo, bar])`。修：G.7 补全 T[N] 形参 nbytes＞8 load E*；8B 仍 payload lea；＞8B VAR lea dest；redirect SAT try_index → leftover rest enc_local。禁 leftover rest T SAT try_index（arr0 intercept）。TYPE_FN elem 宽 8 补 leftover rest `glue_fixed_array_total_bytes`。证＠`c04ad21c2`：leftover rest 426988B＋redirect patched **626**＋SKIP_HOT（pabi 1407792→1408238；xlang_asm 4666036＠00:52）；`fnarr16`／`fnarr16_let` RUN=7／`i32arg16` RUN=10；fnarr_arg／i32arg8／fnarr0／arr0／rv42／callid／ifelse 不回归。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：ARRAY_LIT TYPE_SLICE 作参。 |
| 2026-09-05 | **fix(windows)** **leftover rest ARRAY_LIT TYPE_FN 作参 `apply([foo])`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest rec ko==46 → SAT emit_array_lit dest 指针进 rdi，callee INDEX 对 E* 槽 lea → SEGV 139；VAR `apply(fs)` 已 load_var 8B。SAT `glue_try_index` 与局部 ARRAY 同面（禁拦截）。SAT remaining-wave 221 `call_param_ty_get` SAT standalone ABSENT（禁 leftover rest UNDEF／禁 remaining-wave 原件 WIN leftover 第二份）。存储＝leftover rest WIN leftover for_call_args。消费＝want-exe `apply([foo])`。修：G.7 补全 ARRAY_LIT TYPE_ARRAY ≤8B load_64 dest payload；kind 走 WAVE278 `resolved_type_ref`。证＠`eb50d1198`（load＠`5bfc3698d`）：leftover rest 426542B＋redirect patched **613**＋SKIP_HOT（pabi 1407548→1407792；xlang_asm 4665524＠00:25）；`fnarr_arg` RUN=7；fnarr_arg_let／fnarr0／fnarr_st／arr0／rv42／callid／ifelse 不回归。16B `apply([foo, bar])` 仍 dest 指针（另邻域）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：ARRAY_LIT TYPE_ARRAY 16B 作参。 |
