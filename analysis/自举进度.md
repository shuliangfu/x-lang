# Xlang / X language 自举进度

> **更新**：2026-07-31 · tip **wave929**（drop 3 B-class make calls in build_xlang_asm FORCE leaves · macOS L2 绿 · parity 验证）\
> **产品 L4 放行钉盘**：**`53fd80927`**（wave710 双端 L4 真冷 + **129**）· **wave923 双端 L4 候选**：**`81285129e`**（待升钉）\
> **产品 residual tip**：**`83e0e8c6d`**（wave929 代码改动）· **文档 tip**: **`39baf14b4`**（wave922 docs）\
> **tip L4 安全网**：**`f8be401e9`**（wave840 · **不升钉**）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：**11.3.1 residual 🟡** · TREE_ARMED ✅ · Makefile 仍在 · never-rm · 双端 L4 绿 · Windows reproof done · 待 explicit auth\
> **下波候选**：**explicit auth** 真删（phys-del-gate --delete-body）→ 升钉盘 `81285129e`\
> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[30](自举进度-归档-2026-07-30.md) · [29](自举进度-归档-2026-07-29.md) · [28](自举进度-归档-2026-07-28.md) · …

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `53fd80927`**（wave710 · 双端 L4 + 129）。\
**MG 本波 = wave929**（drop 3 B-class make calls in build_xlang_asm FORCE leaves · macOS L2 绿 · parity 验证 · **非**物理删 · **不升钉** · 代码改动波）。\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（删 Makefile 主闸门；**变更记录只在本文 §6**）。

| 指标 | 状态 |
|------|------|
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`53fd80927`**（129 ✅）；residual tip **`83e0e8c6d`**（wave929 代码 + macOS L2 绿 + parity）；docs tip **`39baf14b4`**（wave922 reproof）；tip L4 安全网 **`f8be401e9`** |
| Track MG | **11.3.1 路径 🟡**（wave746–924 · LEAF + §6）· residual：**thin edges / mk lists → ✅ Windows reproof + ✅ 双端 L4 → 终局真删**（待 explicit auth） |
| mac / Ubuntu L4（钉盘） | ✅ `/tmp/*_53fd80927.log` |
| mac / Ubuntu L4（tip 安全网） | ✅ `/tmp/*_f8be401e9.log`（wave840） |
| 本波构建级别 | **L4**（wave923 双端真冷：mac 699s + Ubuntu 477s · 129 bstrict 全绿）；tip L4 安全网 wave840 已绿 |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / 删 Makefile DAG**（状态 only） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射 |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 11.3.1 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录** |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图 |
| [自举进度-归档-2026-07-30.md](自举进度-归档-2026-07-30.md) | **30 日全日 wave758–894**（只读 · 含二次归楼 856–894） |
| [自举进度-归档-2026-07-29.md](自举进度-归档-2026-07-29.md) | **29 日** wave618–757 + tip L4 升钉 `53fd80927` |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单 |
| 产品 L4 放行钉盘 | **`53fd80927`** | 29 日 wave710 升钉；双端 L4 真冷 + 129 |
| tip L4 安全网 | **`f8be401e9`** | wave840 · **钉盘不升** |
| residual tip（MG） | **`81285129e`** | wave923 代码（async fix）+ 双端 L4 绿 |
| mac 钉盘 L4 | ✅ 129 | `/tmp/mac_cold_53fd80927.log` · `/tmp/mac_bstrict_53fd80927.log` |
| Ubuntu 钉盘 L4（金标） | ✅ 129 | `/tmp/ubuntu_cold_53fd80927.log` · `/tmp/ubuntu_bstrict_53fd80927.log` |
| 产品矩阵（钉盘） | ✅ rv42 / option / si / hello | 双端 |
| 前钉 | `4fa4f07e7` · `01479a857` · … | 见 29 归档 |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 18 号结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **R2 / Cap** | wave710 升钉后按需 | 明细见 29 归档 |

### 2.3 删 Makefile 闸门（诚实）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1**（tree arm） |
| `TREE_ARMED` | **1** |
| `BODY_SHIPPED` / `DELETE_ALLOWED` | **0** / **0** |
| `PHYS_DEL_WINDOWS_GATE_STATUS` | `reproven_green` @ `39baf14b4`（wave922 reproof · tip 漂移自 `bb8f07263` 后复证绿） |
| preflight blockers | `makefile_thin_call_edges\|b7b_lists_in_mk\|std_core_product_make_graph` |
| 真删条件 | ✅ Windows 复证（wave922）· ✅ 双端 L4（wave923 @ `81285129e`）· ⬜ **explicit user auth**（裸「继续下一步」≠ 真删） |

***

## 3. 当前前排（≤3 条）

1. **11.3.1 residual（非删）**：thin edges + mk lists hybrid（wave923 async rule parse-order fix · **双端 L4 绿**；wave921 class-G filter multi 4 · **filter family closed**；wave920 cp-alias multi 3 · **cp-alias family closed**；wave919 migrate_x multi 3 · **migrate_x family closed**；wave918 cc_inc_tu Linux guard multi 1 · **cc_inc_tu family closed**；wave917 cc_inc_tu SHARED multi 5 · --auto+seed-map；wave916 B5 CFG_EVAL multi 1；wave915 R2 PANIC multi 1；wave914 R2 TYPECK_F64 multi 1；wave913 R2 CRT0 multi 6…wave894 formal_mod）· ✅ Windows reproof done（wave922 @ `39baf14b4`）· ✅ 双端 L4 done（wave923 @ `81285129e` · mac 699s + Ubuntu 477s · 129 bstrict 全绿）· → **explicit auth** 终局真删（`phys-del-gate --delete-body`）。Makefile 仍在；`--delete` never-rm。细节：[LEAF](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) · [30 归档](自举进度-归档-2026-07-30.md)。
2. **真删闸门**（待 explicit auth）：三条件前二齐（✅ Windows reproof + ✅ 双端 L4）；用户明确授权后跑 `phys-del-gate --delete-body` 物理删 Makefile leaf residual → 验证双端 bstrict 全绿 → 升钉盘 `81285129e`。
3. **11.1.5 / Cap 硬叶** — 仅挡产品红时插队；默认不打断 residual 收口。

***

## 4. 与归档的关系

- **30 日归档**：[自举进度-归档-2026-07-30.md](自举进度-归档-2026-07-30.md) — wave758–894 全日流水（R4 residual → phys-del arm → FORCE/list→mk → tip L4 安全网 → B7C shell-primary → B7B lists/shell-load → LINK export-leaf / env inject hygiene / formal_mod list→mk）。
- **29 日归档**：[自举进度-归档-2026-07-29.md](自举进度-归档-2026-07-29.md) — wave618–710 升钉 + wave711–757 Track MG 起盘。
- **勿**用归档 tip 或旧钉冒充现行钉盘。**现行产品钉盘 = `53fd80927`**。
- 归楼后主文档只保留 KPI + 前排 + 短 §6；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
|------|----------|
| **macOS arm64** | 钉盘 L4 ✅ · tip L4 安全网 ✅ · MG residual 须本波 `--check` |
| **Ubuntu x86_64（金标）** | 钉盘 L4 ✅ · tip L4 安全网 ✅ · **波绿 = pull 后同 SHA** |
| **Windows MSYS2** | wave804 proof 绿 · wave922 reproof @ `39baf14b4` 绿（B-hybrid + win32 write/read gates）· STATUS=`reproven_green` · 真删待 explicit auth |

***

## 6. 变更记录（倒序 · 仅 31 日；30 日见归档）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-07-31 | **wave929** · 11.3.1 · drop 3 B-class make calls in build_xlang_asm FORCE leaves | **代码改动波** · **非**物理删 · **不升钉**。清理 `build_xlang_asm.sh` B类 multi-target FORCE leaves 3 处 make 调用：① `ensure_experimental_ast_pool_for_wpo` — `make pipeline_x.o PIPELINE_X_FORCE_COMPILE=1` → `PIPELINE_X_FORCE_COMPILE=1 ensure_host_cc_seed_o.sh try-heat pipeline_x.o`（GEN_C_TO_O_SEED_OBJS body；ensure_gen_x_o.sh 支持 PIPELINE_X_FORCE_COMPILE env）；② `ensure_parser_x_o_for_strict_link` — `make -s parser_x.o` → `migrate_x_objs.sh parser_x.o`（MIGRATE_X_OBJS body 权威）；③ `ensure_gen_driver_typeck_companion_objs` — `make typeck_x.o x_frontend_link_alias.o` → `migrate_x_objs.sh typeck_x.o` + `ensure_host_cc_seed_o.sh try-heat x_frontend_link_alias.o`（MIGRATE_X_OBJS + R1_ALIAS_STUBS_OBJS bodies）。逃生口 `XLANG_ASM_LINK_VIA_MAKE=1`。**Parity 验证**：migrate_x_objs parser_x.o 与 make **字节级一致**（185984B）；migrate_x_objs typeck_x.o 与 make **字节级一致**（166496B）；try-heat x_frontend_link_alias.o 与 make **字节级一致**（5824B）。**macOS L2 验证**：bstrict smoke 23s 完成，4 个目标 .o 全部经 wave929 路径生成，xlang 重建成功（4203896B），bstrict OK。**非**物理删 — build_xlang_asm.sh 仍有 6 处 make 调用（C类长串 companion list line 3762 + C类复合 targets；后续波次）。Next: 清理 C 类 make 调用 / 清零 preflight blockers → explicit auth → 真删 → 升钉盘。PLATFORM: SHARED. Tip **`83e0e8c6d`** |
| 2026-07-31 | **wave927** · 11.3.1 · drop residual make calls in gen_g06 + bstrict ensure-seed | **代码改动波** · **非**物理删 · **不升钉**。清理 wave924 遗留 + bstrict ensure-seed 入口 make 调用：`gen_g06_phase1_backend_stub.sh` 两处——`make src/x_seed_bridge.o` 改 shell `cc -c seeds/x_seed_bridge.from_x.c`（g05 G-02f-11 cold fallback 模式）+ SEED_LINK_OBJS probe loop 内 `make $obj` 改 shell `cc -c ${obj%.o}.c`；`bootstrap_driver_bstrict.sh` 一处——`make bootstrap-driver-seed`（Makefile thin-call 同脚本）改直调 `bash scripts/bootstrap_driver_seed.sh`。逃生口保留：`XLANG_SEED_LINK_VIA_MAKE=1` / `XLANG_BSTRICT_ENSURE_VIA_MAKE=1`。**macOS L2 验证**：gen_g06 destructive test（rm x_seed_bridge.o + partial + seed）→ wave927 路径触发，rebuild x_seed_bridge.o 与备份**字节级一致**（5752B / 48 syms）；bstrict ensure-seed test（rm xlang）→ 完整 bstrict 流程走 wave927 路径（1:19 total），xlang 重建成功（4186824B），bstrict OK。Makefile thin-call targets 保留为 parity / escape（**非**物理删）。Next: 清理 `build_xlang_asm.sh` 12 处 make 调用 / 清零 preflight blockers → explicit auth → 真删 → 升钉盘。PLATFORM: SHARED. Tip **`e1b891c50`** |
| 2026-07-31 | **wave926** · 11.3.1 · shell-primary link-objs/cflags-export for archaeology bags | **代码改动波** · **非**物理删 · **不升钉**。`driver_seed_obj_catalog.sh` 新增 `--link-objs-export <bag>` 和 `--link-cflags-export <bag>` 模式：支持 8 个 OBJ bags（xnc / legacy-xlang-c / relink-product / xlang-x / bxf / bs / objs-core / bxc）+ 4 个 CFLAGS bags（relink-product / xnc / btc-typeck / bxf）。每个 bag 映射到 Makefile 变量 / CFLAGS 公式（与 export-*-link-objs/cflags targets 完全一致）。`legacy_xlang_c_link.sh` 和 `xlang_no_c_frontend.sh` 改为 shell catalog（0 make；`XLANG_LEGACY_LINK_VIA_MAKE=1` / `XLANG_XNC_LINK_VIA_MAKE=1` 逃生口）。**Parity 验证**：shell==make OK（6 OBJ bags + 4 CFLAGS bags 全一致）。**双端 L2 绿**：mac + Ubuntu bootstrap-driver-seed OK + bootstrap-driver-bstrict OK + xnc --check + legacy --check OK。Tip **`b976c82b3`** |
| 2026-07-31 | **wave925** · 11.3.1 · shell-primary CFLAGS export via catalog --cflags-export | **代码改动波** · **非**物理删 · **不升钉**。将 `PIPELINE_GEN_CFLAGS_BASE` / `CLANG` / `PIPELINE_GEN_CFLAGS` 从 Makefile inline 迁至 `mk/driver_seed_mode_objs.mk`（G.7 单权威；转 `ifeq` 替代 `$(if)`）。`CC_IS_CLANG` 留在 Makefile（pre-include `$(shell)`）+ `catalog_seed_host_defaults`（shell `$CC -v | grep clang`）双端计算。**Bug fix**：`catalog_parse_mk` 新增 `+=`（append）支持——此前 `KEY += val` 被误解析为新 key `KEY +`。`driver_seed_obj_catalog.sh` 新增 `--cflags-export` 模式：输出 `CFLAGS` + `PIPELINE_GEN_CFLAGS`（与 Makefile export-try-heat-cflags 完全一致）。`build_tool.sh` 改为 shell catalog（0 make；`XLANG_CFLAGS_VIA_MAKE=1` 逃生口）。**Parity 验证**：shell==make OK。**双端 L2 绿**：mac + Ubuntu bootstrap-driver-seed OK + bootstrap-driver-bstrict OK。Tip **`2897fad7f`** |
| 2026-07-31 | **wave924** · 11.3.1 · shell-primary SEED_LINK_* export via catalog --link-export | **代码改动波** · **非**物理删 · **不升钉**。将 `LD_R_MULTIDEF_FLAGS` / `ASM_GLUE_DUP_LDFLAGS` / `SEED_LINK_PURE_OK` / `SEED_LINK_ENTRY` / `SEED_LINK_LD_TAIL` 从 Makefile inline `ifeq`（含 `else ifeq`）迁至 `mk/driver_seed_link_picks.mk`（G.7 单权威；转嵌套 `ifeq/else/endif` 供 catalog_parse_mk 解析）。`driver_seed_obj_catalog.sh` 新增 `--link-export phase1\|final` 模式：复用 `catalog_shell_parse_all`（mk 解析 + host defaults），解析 CC / LD / CFLAGS / TARGET（Makefile `?=` 同语义），输出 9 个 `SEED_LINK_*` 变量（与 Makefile export targets 完全一致）。产品路径调用点改为 shell catalog（0 make）：`bootstrap_driver_seed_link.sh` + `gen_g06_phase1_backend_stub.sh`（`XLANG_SEED_LINK_VIA_MAKE=1` 逃生口保留）。**Parity 验证**：shell==make OK（phase1 + final 9 keys 全一致）。**双端 L2 绿**：mac（Darwin/arm64）bootstrap-driver-seed OK + bootstrap-driver-bstrict OK；Ubuntu（Linux/x86_64）bootstrap-driver-seed OK + bootstrap-driver-bstrict OK。Makefile export-* targets 保留为 parity / escape 权威（**非**物理删）。Next: explicit auth → `phys-del-gate --delete-body` → 升钉盘。PLATFORM: SHARED. Tip **`e3635ea76`** |
| 2026-07-31 | **wave923** · 11.3.1 · 双端 L4 真冷全绿 + async rule parse-order fix | **代码改动波** · **非**物理删 · **不升钉**（待真删后升）。**Bug fix**：wave907 `$(ASYNC_THREE_SEED_OBJS)` multi-target FORCE rule 在 `include mk/driver_seed_r_lists.mk` **之前**（Makefile line 497 vs include line 547），make 解析时变量为空，rule 被静默丢弃，三个 async .o（`async_liveness` / `async_cps_codegen` / `async_asm_pool`）变 "Not a target"，真冷测（无旧 .o 缓存）报 "No rule to make target src/async/async_asm_pool.o"。**Fix**：把 rule block（17 行）移到 include 之后（line 533-547），与其他 7 个 multi-target FORCE rule 同列。**为什么之前没暴露**：日常 L2 有旧 .o 缓存，Windows gate 也有缓存，只有 L4 真冷全删 .o 才暴露。**双端 L4 真冷全绿** @ `81285129e`：mac（Darwin/arm64）bootstrap-driver-seed OK + g05 OK（xlang_asm 4186824B）+ run-all-bstrict OK（129 scripts, 699s）；Ubuntu（Linux/x86_64）bootstrap-driver-seed OK + g05 OK（xlang_asm 3981640B）+ run-all-bstrict OK（129 scripts, 477s）。**真删三条件**：✅ Windows 复证（wave922）· ✅ 双端 L4（wave923）· ⬜ explicit user auth。Next: explicit auth → `phys-del-gate --delete-body` → 升钉盘 `81285129e`。PLATFORM: SHARED |
| 2026-07-31 | **wave921** · 11.3.1 · B7B class-G filter family multi-target FORCE thin | 4 leaves: `$(FILTER_AGAINST_PARTIAL_OBJS)` = `build_asm/bootstrap_seed_backend_x86_64_enc_c_filtered.o` + `build_asm/bootstrap_seed_user_asm_seed_bridge_filtered.o` + `build_asm/bootstrap_seed_asm_backend_compat_stubs_filtered.o` (3 leaves; against_partial script) + `$(FILTER_PIPELINE_OBJS)` = `build_asm/bootstrap_seed_pipeline_filtered.o` (1 leaf; pipeline script) — 2 new lists in `mk/driver_seed_r_lists.mk` (G.7 single authority; export_lists never re-assigns); Makefile 4 per-leaf recipes → 2 multi-target FORCE (split by script: against_partial + pipeline); body = `@bash scripts/filter_bootstrap_seed_{against_partial,pipeline}_o.sh ensure $@` — script CATALOG owns OUT|SRC map; $@ passes leaf path directly; leaf_pattern_residual.sh: per-leaf count 4→0 + 2 multi-target checks (against_partial + pipeline) + logical expand +2 (against_partial hides 2 leaves) + dump WAVE wave835→wave835/921 + VIA mtime→mtime_multi_target. **class-G filter family closed** (4 leaves). macOS L2: 4 .o multi-target build OK (backend_x86_64_enc_c=31848B / user_asm_seed_bridge=8984B / asm_backend_compat_stubs=9968B / pipeline=1255632B) + bootstrap_driver_seed smoke OK (rv=42). Ubuntu L2: 4 .o multi-target build OK (backend_x86_64_enc_c=33408B / user_asm_seed_bridge=11536B / asm_backend_compat_stubs=11512B / pipeline=1330816B) + bootstrap_driver_seed OK. Dual-end L2 tip **`bac761c9a`**. **No pin bump**. Next: ~~net_merge~~ (wave897 done) / tip Windows reproof → wave922 → dual L4 → explicit auth ship delete. PLATFORM: SHARED |
| 2026-07-31 | **wave920** · 11.3.1 · B7B cp-alias family multi-target FORCE thin | 3 leaves: `$(CP_ALIAS_SHARED_OBJS)` = `ast_x.o` (1 leaf; SHARED G-02a C ABI alias) + `$(CP_ALIAS_LINUX_X86_64_OBJS)` = `crt0_user.o` + `freestanding_io.o` (2 leaves; Linux x86_64 freestanding wrappers) — 2 new lists in `mk/driver_seed_r_lists.mk` (G.7 single authority; export_lists never re-assigns); Makefile 3 per-leaf recipes → 2 multi-target FORCE (split by platform scope: SHARED + Linux x86_64 ifeq guard); body = `@bash scripts/ensure_cp_alias_o.sh ensure $@` — script CATALOG owns OUT|SRC map; $@ passes leaf name directly; leaf_pattern_residual.sh: per-leaf count 3→0 + 2 multi-target checks (SHARED + LINUX_X86_64) + logical expand +1 (LINUX_X86_64 hides 1 leaf) + dump WAVE wave836→wave836/920 + VIA mtime→mtime_multi_target. **cp-alias family closed** (3 leaves). macOS L2: ast_x.o multi-target build OK (11208B) + bootstrap_driver_seed smoke OK (rv=42); b19 FAIL pre-existing (stash-verified). Ubuntu L2: 3 .o built via multi-target (ast_x=14712B / crt0_user=776B / freestanding_io=1232B) + bootstrap_driver_seed OK. Dual-end L2 tip **`c0fd5b559`**. **No pin bump**. Next: ~~filter (4)~~ → wave921 / net_merge / tip Windows reproof → dual L4 → explicit auth ship delete. PLATFORM: SHARED + LINUX\|UBUNTU x86_64 (ifeq guard preserved) |
| 2026-07-31 | **wave919** · 11.3.1 · B7B migrate_x family multi-target FORCE thin | 3 leaves: `$(MIGRATE_X_OBJS)` = `parser_x.o` + `typeck_x.o` + `codegen_x.o` (new list in `mk/driver_seed_r_lists.mk`; G.7 single authority; export_lists never re-assigns); Makefile 3 per-leaf recipes → 1 multi-target FORCE; body = `@bash scripts/migrate_x_objs.sh $@` — script case statement already accepts `<name>_x.o` as MODE (no --auto seed-map needed, unlike cc_inc_tu wave917/918); leaf_pattern_residual.sh: per-leaf count 3→0 + multi-target check (MIGRATE_X_OBJS) + logical expand +2 + migrate/bootstrap recipes COUNT 8→6 + migrate_x_objs @bash COUNT 4→2 + dump validators. **migrate_x family closed** (3 leaves). macOS L2: 3 .o built via multi-target (parser_x=185984B / typeck_x=166496B / codegen_x=402320B) + product matrix green (rv=42 / opt=102 / si=0 / hello="Hello World\n"); Ubuntu L2: same multi-target build green (parser_x=286328B / typeck_x=192488B / codegen_x=414000B) + product matrix green. Dual-end L2 tip **`03a126f7e`**. **No pin bump**. Next: ~~cp-alias (3)~~ → wave920 / filter (4) / tip Windows reproof → dual L4 → explicit auth ship delete. PLATFORM: SHARED |
| 2026-07-31 | **wave918** · 11.3.1 · B7B cc_inc_tu Linux x86_64 guard multi-target FORCE thin --auto | 1 leaf: `$(CC_INC_TU_LINUX_X86_64_OBJS)` = `src/asm/bootstrap_nostdlib_stubs.o` (new list in `mk/driver_seed_r_lists.mk`; G.7 single authority; export_lists never re-assigns); Makefile per-leaf recipe → multi-target FORCE inside `ifeq (Linux,x86_64)` guard (crt0_user/freestanding_io cp-alias leaves untouched beside); body = `@bash scripts/cc_inc_tu.sh --auto $@`; cc_inc_tu.sh seed-map `cc_inc_tu_seed_for_out` extended with `bootstrap_nostdlib_stubs.o` → `seeds/bootstrap_nostdlib_stubs.from_x.c`; leaf_pattern_residual.sh: per-leaf count 1→0 + multi-target check (CC_INC_TU_LINUX_X86_64_OBJS) + dump update. **cc_inc_tu multi-target family closed** (SHARED 5 wave917 + Linux guard 1 wave918 = 6). macOS guard skipped (UNAME_S != Linux); Ubuntu L2 guard leaf build OK (53656 bytes) + product matrix green (rv=42 / opt=102 / si=0 / hello="Hello World\n"). Dual-end L2 tip **`67f44c7ef`**. **No pin bump**. Next: ~~net_merge~~ → wave919 migrate_x / tip Windows reproof → dual L4 → explicit auth ship delete. PLATFORM: LINUX\|UBUNTU guard (leaf) / SHARED (script+list) |
| 2026-07-31 | **wave917** · 11.3.1 · B7B cc_inc_tu SHARED family multi-target FORCE thin --auto | 5 leaves: `$(CC_INC_TU_OBJS)` = `src/asm/asm_experimental_symbol_bridge.o` + `src/lsp/lsp_diag_pipeline_sizes.o` (seed `_weak`) + `src/lexer/cfg_eval_bootstrap_stub.o` + `src/lsp/typeck_lsp_io_stub.o` + `src/build_tool_main.o` (list migrated to `mk/driver_seed_r_lists.mk`; G.7; export_lists never re-assigns); Makefile 5 per-leaf recipes → 1 multi-target FORCE; body = `@bash scripts/cc_inc_tu.sh --auto $@`; cc_inc_tu.sh `--auto` mode + `cc_inc_tu_seed_for_out` seed-map (G.7 single authority; 72+ explicit seed path call sites unchanged); leaf_pattern_residual.sh: per-leaf count 6→1 + multi-target check + thin-call logical expand +4 + dump `_MULTITARGET=5`. Not in list: `bootstrap_nostdlib_stubs.o` (ifeq Linux x86_64 guard; deferred → wave918). Dual-end L2 tip **`075b32d9a`**. **No pin bump**. Next: ~~bootstrap_nostdlib_stubs.o~~ wave918 / net_merge … · tip Windows reproof. PLATFORM: SHARED |
| 2026-07-31 | **wave916** · 11.3.1 · B7B B5 CFG_EVAL multi-target FORCE thin try-heat | 1 叶：`$(DRIVER_SEED_CFG_EVAL_OBJS)` = `src/lexer/cfg_eval.o`（列表迁 `mk/driver_seed_r_lists.mk` · G.7 有则补全;export_lists 从不复写);Makefile 单 multi-target FORCE+try-heat;body 仍 ensure try-heat → try-cfg-eval-ladder(wave783 multi-ladder:live -E-extern → pin gen → bootstrap stub rungs;LD/LD_RELFLAGS shell defaults via wave886)。ensure multi-target helper 列表扩 DRIVER_SEED_CFG_EVAL_OBJS;wave916 multi-target check(仿 wave915 PANIC,接受 multi-target 或 legacy single-leaf)。双端 L2 tip **`39b81c7d1`**。**不升钉**。下一:thin edges + mk lists hybrid(cc_inc_tu / net_merge…)· tip Windows 复证。PLATFORM: SHARED |
| 2026-07-31 | docs · **30 日二次归楼** | 主文档 §6 残留 **wave856–894**（+ 30 日 summary 行）整表并入 [自举进度-归档-2026-07-30.md](自举进度-归档-2026-07-30.md)（wave758–894 全日完整）；主文档 §6 仅留 **31 日** 流水。钉盘 **`53fd80927`** · tip L4 安全网 **`f8be401e9`** · residual tip **wave915** @ **`39b331968`**。 |
| 2026-07-31 | **wave915** · 11.3.1 · B7B R2 PANIC multi-target FORCE thin try-heat | 1 叶：`$(DRIVER_SEED_PANIC_OBJS)`（列表迁 `mk/driver_seed_r_lists.mk` · G.7 try-heat→try-r2-prefer/try-r2；export_lists 不再复写）；Makefile 单 multi-target FORCE+try-heat（收 `runtime_panic.o`）；body 仍 ensure try-heat→try-r2-prefer（wave760/776/796）。ensure multi-target helper 扩 DRIVER_SEED_PANIC_OBJS。双端 L2 tip **`39b331968`**。**不升钉**。下一：thin edges + mk lists hybrid（cfg_eval…）· tip Windows 复证。PLATFORM: SHARED |
| 2026-07-31 | **wave914** · 11.3.1 · B7B R2 TYPECK_F64 multi-target FORCE thin try-heat | 1 叶：`$(DRIVER_SEED_TYPECK_F64_OBJS)`（列表迁 `mk/driver_seed_r_lists.mk` · G.7 try-r2 host pick；export_lists 不再复写）；Makefile 单 multi-target FORCE+try-heat；UNAME ifeq hard-error 面收掉。双端 L2 tip **`3f041d897`**。**不升钉**。下一：~~R2 PANIC multi~~ wave915。PLATFORM: SHARED |
| 2026-07-31 | **wave913** · 11.3.1 · B7B R2 CRT0 multi-target FORCE thin try-heat | 6 叶：`$(DRIVER_SEED_CRT0_OBJS)`（列表迁 `mk/driver_seed_r_lists.mk` · G.7 try-r2；export_lists 不再复写）；Makefile 单 multi-target FORCE+try-heat（收 crt0_x86_64/arm64/darwin_x86_64/mingw + user/freestanding_io）；body 仍 ensure try-heat→try-r2（wave762/790/795）。cp-alias/nostdlib 仍 Linux ifeq。ensure multi-target helper 扩 DRIVER_SEED_CRT0_OBJS。双端 L2 tip **`33c94bdc2`**。**不升钉**。下一：thin edges + mk lists hybrid（cfg_eval/typeck_f64/panic…）· tip Windows 复证。PLATFORM: SHARED |
| 2026-07-31 | **wave912** · 11.3.1 · B7B FMT_CHECK multi-target FORCE thin try-heat | 2 叶：`$(FMT_CHECK_SEED_OBJS)`（`mk/driver_seed_r_lists.mk` 新列表 · G.7 try-other-l2-prefer map fmt_core/fmt；export-obj-catalog + catalog REQUIRED）；Makefile 单 multi-target FORCE+try-heat；body 仍 ensure try-heat→try-other-l2-prefer（wave771/775）。ensure --check multi-target helper 扩 FMT_CHECK。双端 L2 tip **`fc71dcc9c`**。**不升钉**。下一：~~R2 CRT0 multi~~ wave913。PLATFORM: SHARED |
| 2026-07-31 | **wave911** · 11.3.1 · B7B B3_LSP_SAT multi-target FORCE thin try-heat | 2 叶：`$(B3_LSP_SAT_SEED_OBJS)`（`mk/driver_seed_r_lists.mk` 新列表 · G.7 try-lsp-sat-prefer map sizes_nostub/stubs_no_c；export-obj-catalog + catalog REQUIRED）；Makefile 单 multi-target FORCE+try-heat；body 仍 ensure try-heat→try-lsp-sat-prefer（wave781）。ensure --check multi-target helper 扩 B3。双端 L2 tip **`33e029632`**。**不升钉**。下一：~~fmt multi~~ wave912。PLATFORM: SHARED |
| 2026-07-31 | **wave910** · 11.3.1 · B7B GEN_C_TO_O multi-target FORCE thin try-heat | 5 叶：`$(GEN_C_TO_O_SEED_OBJS)`（`mk/driver_seed_r_lists.mk` 新列表 · G.7 try-gen-c-to-o map lexer/ast_gen2/driver/preprocess/stubs；export-obj-catalog + catalog REQUIRED）；Makefile 单 multi-target FORCE+try-heat；body 仍 ensure try-heat→try-gen-c-to-o（wave782）。ensure --check multi-target helper 扩 GEN_C_TO_O。双端 L2 tip **`6091da5d1`**。**不升钉**。下一：~~B3 multi~~ wave911。PLATFORM: SHARED |
| 2026-07-31 | **wave909** · 11.3.1 · B7B GEN_X multi-target FORCE thin try-heat | 4 叶：`$(GEN_X_SEED_OBJS)`（`mk/driver_seed_r_lists.mk` 新列表 · G.7 try-gen-x map lsp_io/lsp/lsp_diag/pipeline；export-obj-catalog + catalog REQUIRED）；Makefile 单 multi-target FORCE+try-heat；body 仍 ensure try-heat→try-gen-x（wave761）。ensure --check multi-target helper 扩 GEN_X。双端 L2 tip **`db7a2bc65`**。**不升钉**。下一：~~B4 gen-c-to-o multi~~ wave910。PLATFORM: SHARED |
| 2026-07-31 | **wave908** · 11.3.1 · B7B B1_RUNTIME_OS multi-target FORCE thin try-heat | 23 叶：`$(B1_RUNTIME_OS_SEED_OBJS)`（`mk/driver_seed_r_lists.mk` 新列表 · G.7 单权威 try-runtime-os-prefer 表；export-obj-catalog + catalog REQUIRED）；Makefile 单 multi-target FORCE+try-heat（收 test_fn_invoke…process_os_glue）；body 仍 ensure try-heat→try-runtime-os-prefer（wave779）。ensure --check multi-target helper 扩 B1。双端 L2 tip **`962fd8c93`**。**不升钉**。下一：~~GEN_X multi~~ wave909。PLATFORM: SHARED |
| 2026-07-31 | **wave907** · 11.3.1 · B7B ASYNC_THREE multi-target FORCE thin try-heat | 3 叶：`$(ASYNC_THREE_SEED_OBJS)`（`mk/driver_seed_r_lists.mk` 新列表 · G.7 单权威 try-async-prefer 表；export-obj-catalog + catalog REQUIRED）；Makefile 单 multi-target FORCE+try-heat（收 async_liveness/cps/asm_pool）；body 仍 ensure try-heat→try-async-prefer（wave770）。ensure --check multi-target helper（R1/R3/ASYNC 家族）。双端 L2 tip **`5d5ac5fc3`**。**不升钉**。下一：thin edges + mk lists hybrid（B1/gen-x/…）· tip Windows 复证。PLATFORM: SHARED |
| 2026-07-31 | **wave906** · 11.3.1 · B7B R3_COLD multi-target FORCE thin try-heat | 9 叶：`$(R3_COLD_SEED_OBJS)`（`mk/driver_seed_r_lists.mk` 既有列表 · G.7 有则补全无第二 list）；Makefile 单 multi-target FORCE+try-heat（收 rio/rdabi/rdd/simd_enc/simd_loop/backend_*_dispatch 四）；body 仍 ensure try-heat→try-r3-prefer（wave757/763/764）。双端 L2 tip **`60de2d362`**。**不升钉**。下一：thin edges + mk lists hybrid（async/B1/…）· tip Windows 复证。PLATFORM: SHARED |
| 2026-07-31 | **wave905** · 11.3.1 · B7B R1_SEED_MAP multi-target FORCE thin try-heat | 5 叶：`$(R1_SEED_MAP_OBJS)`（`mk/driver_seed_r_lists.mk` 既有列表 · G.7 有则补全无第二 list）；Makefile 单 multi-target FORCE+try-heat（收 target_cpu/ast_seed/pipeline_bootstrap_orchestration/parser_asm_thin_glue/pipeline_glue_standalone）；body 仍 ensure try-heat（wave755/758/759/768/793/794/797；seed/flag map + target_cpu prefer 在 shell）。**R1 multi-target family closed**。双端 L2 tip **`a39f4ab69`**。**不升钉**。下一：~~R3_COLD multi~~ wave906。PLATFORM: SHARED |
| 2026-07-31 | **wave904** · 11.3.1 · B7B R1_MISC_BASENAME multi-target FORCE thin try-heat | 9 叶：`$(R1_MISC_BASENAME_OBJS)`（`mk/driver_seed_r_lists.mk` 既有列表 · G.7 有则补全无第二 list）；Makefile 单 multi-target FORCE+try-heat（收 runtime_link_abi_user_env/channel_glue/scheduler_glue/kv_mmap_glue/backend_x86_64_enc_c/backend_arm64_enc_c/lsp_diag_pipeline_ctx/pipeline_glue_strict_minimal/runtime_asm_build）；body 仍 ensure try-heat（wave754/790–794/767；ldpc/l2-asm prefer 在 shell ladder）。双端 L2 tip **`10d5d5116`**。**不升钉**。下一：~~SEED_MAP multi~~ wave905。PLATFORM: SHARED |
| 2026-07-31 | **wave903** · 11.3.1 · B7B R1_EXTRA_CFLAGS multi-target FORCE thin try-heat | 5 叶：`$(R1_EXTRA_CFLAGS_OBJS)`（`mk/driver_seed_r_lists.mk` 既有列表 · G.7 有则补全无第二 list）；Makefile 单 multi-target FORCE+try-heat（收 runtime_pipeline_abi/runtime_asm_io_stubs/runtime_sqlite_glue/runtime_sqlite_glue_stub/parser_asm_parse_expr_link）；body 仍 ensure try-heat（wave753/790/791/794/767；flag map 在 shell extras_for_extra_cflags）。双端 L2 tip **`b70148f7b`**。**不升钉**。下一：~~MISC multi~~ wave904。PLATFORM: SHARED |
| 2026-07-31 | **wave902** · 11.3.1 · B7B R1_ALIAS_STUBS multi-target FORCE thin try-heat | 8 叶：`$(R1_ALIAS_STUBS_OBJS)`（`mk/driver_seed_r_lists.mk` 既有列表 · G.7 有则补全无第二 list）；Makefile 单 multi-target FORCE+try-heat（收 x_frontend_link_alias/ast_asm_bare/backend_asm_bare/strict_fallback/typeck_c_module_stubs/user_asm_seed_bridge/asm_backend_compat_stubs/runtime_driver_strict_glue_stubs）；body 仍 ensure try-heat（wave752/790/792/769/771）。双端 L2 tip **`3f1511b53`**。**不升钉**。下一：~~EXTRA_CFLAGS multi~~ wave903。PLATFORM: SHARED |
| 2026-07-31 | **wave901** · 11.3.1 · B7B R1_MAIN_RUNTIME multi-target FORCE thin try-heat | 7 叶：`$(R1_MAIN_RUNTIME_OBJS)`（`mk/driver_seed_r_lists.mk` 既有列表 · G.7 有则补全无第二 list）；Makefile 单 multi-target FORCE+try-heat（收 main/main_x/main_driver/runtime/runtime_x/runtime_driver/runtime_driver_no_c）；body 仍 ensure try-heat（wave751/790/793/794；flag map + wave766 try-rt-prefer 在 shell）。双端 L2 tip **`d35aec524`**。**不升钉**。下一：~~ALIAS_STUBS multi~~ wave902。PLATFORM: SHARED |
| 2026-07-31 | **wave900** · 11.3.1 · B7B R1_FRONTEND_GLUE multi-target FORCE thin try-heat | 3 叶：`$(R1_FRONTEND_GLUE_OBJS)`（`mk/driver_seed_r_lists.mk` 既有列表 · G.7 有则补全无第二 list）；Makefile 单 multi-target FORCE+try-heat（收 lexer/ast/lsp_diag；含 LEGACY ifeq + default 路径双点归并）；body 仍 ensure try-heat（wave750/790/793）。双端 L2 tip **`5549ffb6f`**。**不升钉**。下一：~~MAIN_RUNTIME multi~~ wave901。PLATFORM: SHARED |
| 2026-07-31 | **wave899** · 11.3.1 · B7B R1_CORE_SEED multi-target FORCE thin try-heat | 5 叶：`$(R1_CORE_SEED_OBJS)`（`mk/driver_seed_r_lists.mk` 既有列表 · G.7 有则补全无第二 list）；Makefile 单 multi-target FORCE+try-heat（收 diag/runtime_link_abi/runtime_c_import/x_seed_bridge/seed_link_compat per-leaf）；body 仍 ensure try-heat（wave749/790/793）。双端 L2 tip **`7a215ce58`**。**不升钉**。下一：~~FRONTEND_GLUE multi~~ wave900。PLATFORM: SHARED |
| 2026-07-31 | **wave898** · 11.3.1 · B7B RT_SEED_SLICE multi-target FORCE thin try-heat | 5 叶：`$(RT_SEED_SLICE_OBJS)`（`mk/driver_seed_r_lists.mk` 既有列表 · G.7 有则补全无第二 list）；Makefile 单 multi-target FORCE+try-heat（收 rt_arena_buf/emit_state/preamble/stack/parse_diag per-leaf）；body 仍 ensure try-heat（wave748/790/792）。双端 L2 tip **`6dd9cf9de`**。**不升钉**。下一：~~R1_CORE multi~~ wave899。PLATFORM: SHARED |
| 2026-07-31 | **wave897** · 11.3.1 · B7B B2 std_core hybrid product edges list→mk multi-target thin | 5 叶：`STD_CORE_HYBRID_PRODUCT_OBJS` → `mk/std_core_hybrid_product_objs.mk`；Makefile 单 multi-target FORCE+try-heat（收 process/path/runtime/net/slice per-leaf）；body 仍 try-heat→try-std-core-prefer（wave780）。双端 L2 tip **`741888422`**。**不升钉**。下一：~~RT_SEED multi~~ wave898。PLATFORM: SHARED |
| 2026-07-31 | **wave896** · 11.3.1 · B7B driver_leaf product edges list→mk multi-target thin | 8 叶：`DRIVER_LEAF_PRODUCT_OBJS` → `mk/driver_leaf_product_objs.mk`；Makefile 单 multi-target FORCE+ensure（收 8 per-leaf）；shell catalog body 不变（wave814/828/860）。双端 L2 tip **`f39140ba7`**。**不升钉**。下一：~~B2 list→mk~~ wave897。PLATFORM: SHARED |
| 2026-07-31 | **wave895** · 11.3.1 · B7B std_x product edges list→mk multi-target thin | 22 叶：`STD_X_PRODUCT_OBJS` → `mk/std_x_product_objs.mk`；Makefile 单 multi-target FORCE+ensure（收 22 per-leaf）；shell catalog body 不变（wave825/827）。双端 L2 tip **`8e72a53a3`**。**不升钉**。下一：~~driver_leaf list→mk~~ wave896。PLATFORM: SHARED |

***
