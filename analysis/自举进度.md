# ShuX 自举进度

> **更新**：2026-07-21 · 20 号全日已归档；本日从 21 号起重新起表\
> **基线 SHA（产品 L4 放行钉盘）**：**`d4e2dca6`**（升自 `86281cc6`；双端 tip L4 真冷 + 产品轨 **129** 全绿 → 升钉；含 driver\_abi Cap residual pure wave14–33 收口）\
> **产品 bstrict 套件数**：**129**\
> **本波 tip**：**pipeline_abi Cap residual pure wave63** @ `cb229558`（日常 L2；钉盘仍 **`d4e2dca6`**）\
> **本波（21 日）状态**：wave63 typeck entry/sidecar pure orch + 双端 L2 hybrid 矩阵绿；钉盘未升。地图：[当前问题分析.md](当前问题分析.md)（非每微步必改）。

> **方法**：[自举方法.md](自举方法.md)（Cap/R/L/M；工程 ≠ 产品放行）\
> **Windows（删种子前）**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `shux-selfhost-product-gate`\
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md) · [19](自举进度-归档-2026-07-19.md) · [18](自举进度-归档-2026-07-18.md) · [17](自举进度-归档-2026-07-17.md) · [16](自举进度-归档-2026-07-16.md) · [14](自举进度-归档-2026-07-14.md) · [13](自举进度-归档-2026-07-13.md)

### 维护约定

1. 每完成一波 **必改两文**：本文（KPI/前排/§6）+ [`当前进度.md`](当前进度.md)。
2. **禁止**把 prove/Stage2/N++ 单独写成「可自举」。
3. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**；历史钉盘 125）。
4. 工程 KPI 写 **SHA + 日志路径**；过期整日流水移归档。
5. 大债地图默认写 [`当前问题分析.md`](当前问题分析.md)（承接原 `问题分析文档.md`）；非每微步。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 =** **`d4e2dca6`（双端 tip L4 真冷 + 129；wave14–33 收口）。**\
**本波 = pipeline_abi Cap residual pure wave63（`cb229558` · 日常 L2 · 钉盘未升）。**

| 轨                | 状态                                                                                           |
| ---------------- | -------------------------------------------------------------------------------------------- |
| 工程轨              | R2 + driver\_abi wave14–44 + pipeline\_abi hybrid wave45–**63** + nested comments |
| 产品轨 tip / 钉盘     | tip **`cb229558`**；钉盘 **`d4e2dca6`**（L4 未升）                                                                |
| mac 产品链 bstrict  | ✅ **OK (129)** tip L4 钉盘 · `/tmp/mac_true_bstrict_d4e2dca6.log`；本波 L2 hybrid+c-backend 矩阵绿（Darwin pure-asm CG002 tip 既有）                              |
| Ubuntu L4（金标）    | ✅ **OK (129)** tip L4 钉盘 · `/tmp/ubuntu_true_bstrict_d4e2dca6.log`；本波 L2 pure-asm 矩阵 + pure **W**                           |
| 前钉 L4            | ✅ 129 @ `d4e2dca6`（wave14–33 收口后兜底）                               |

***

## 1. 文档地图

| 文档                                             | 看什么                   |
| ---------------------------------------------- | --------------------- |
| [自举方法.md](自举方法.md)                             | Cap / R / L / M       |
| [自举步骤.md](自举步骤.md)                             | 可执行闸门                 |
| [自举验证.md](自举验证.md)                             | 怎么测                   |
| **本文**                                         | 长期 KPI + 前排（**每波必改**） |
| [当前进度.md](当前进度.md)                             | 当天快照（**每波必改**）        |
| **[当前问题分析.md](当前问题分析.md)**                     | tip 级产品债地图（21 日起权威）   |
| [自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md) | **20 号全日**（只读）        |
| skill `shux-selfhost-product-gate`             | L4 / 双端 / 假绿禁止        |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项                          | 结果                                                                                  | 日志 / 备注                                      |
| -------------------------- | ----------------------------------------------------------------------------------- | -------------------------------------------- |
| 产品 bstrict 套件数             | **129**                                                                             | 现行白名单                                        |
| 产品 L4 放行钉盘                 | **`d4e2dca6`**                                                                      | 21 日 tip L4 升钉（前钉 `86281cc6`）；双端 L4 129      |
| 本波 tip                     | pipeline\_abi residual pure wave63 @ `cb229558`                             | 钉盘仍 **`d4e2dca6`**（日常 L2）                     |
| mac 产品链 bstrict（钉盘 tip L4） | ✅ **OK (129)**                                                                      | L4 · `/tmp/mac_true_bstrict_d4e2dca6.log`    |
| Ubuntu L4（金标 · 钉盘 tip L4）  | ✅ **OK (129)**                                                                      | L4 · `/tmp/ubuntu_true_bstrict_d4e2dca6.log` |
| 本波 mac/Ubuntu L2           | ✅ Ubuntu pure-asm 矩阵 + pure **W**（entry/sidecar/for_ctx_impl）；mac hybrid+c-backend 矩阵 @ `cb229558`                                                                | `/tmp/mac_g05_wave63.log` · `/tmp/ubuntu_g05_wave63.log`                                      |
| 前钉 mac/Ubuntu L4           | ✅ 129 @ `d4e2dca6` · `86281cc6` · `cf07b0d9` 等                                                                | 见当日较早记录 / 20 号归档                             |

### 2.2 工程轨（继承）

| KPI           | 现                                        | 备注                   |
| ------------- | ---------------------------------------- | -------------------- |
| **T / EMPTY** | 18/18                                    | 18 号结案               |
| **N**         | 54/54 IDENTICAL                          | 回归基线                 |
| **R2 / 根修本波** | **pipeline_abi** Cap residual pure wave63（typeck entry/sidecar orch） | 双端 L2 hybrid；钉盘仍 `d4e2dca6` |

***

## 3. 当前前排（≤3 条）

1. **pipeline_abi Cap residual pure wave63（本波 · `cb229558`）**
   - ✅ `typeck_module_entry_only` / `with_sidecar` / `pipeline_typeck_module_for_ctx_impl` pure orch；Cap residual `typeck_module` + `typeck_dep_module_ptrs_base`；双端 L2；钉盘仍 **`d4e2dca6`**
2. rest R2：pipeline_abi 其它 Cap residual 叶 / 整波 tip L4（非每微步）
3. **可选** Windows hybrid；Darwin CG002 pure-asm（tip 既有）

***

## 4. 与 20 号归档的关系

- 20 号全日（R2 target\_cpu\_pure、C6 波 3、C7 125→129、钉盘 `deaf773b`、parser has\_body、shux-x 内存 B 等）已整表移入 [`自举进度-归档-2026-07-20.md`](自举进度-归档-2026-07-20.md)。
- **勿**用归档 tip 或旧钉 `deaf773b` / `cf07b0d9` / `86281cc6` / `bcd63b9c` 等冒充现行 tip L4 **`d4e2dca6`**。
- 21 日新工作只记本文 §0/§2/§3/§6。

***

## 5. 双端 / 平台

| 平台                     | 本波状态                                                              |
| ---------------------- | ----------------------------------------------------------------- |
| **macOS arm64**        | ✅ wave63 L2 hybrid+c-backend 矩阵 @ `cb229558` · `/tmp/mac_g05_wave63.log`；Darwin pure-asm CG002 tip 既有；钉盘 L4 129 继承 |
| **Ubuntu x86\_64（金标）** | ✅ wave63 L2 pure-asm 矩阵 + pure **W**（entry/sidecar/for_ctx_impl）@ `cb229558` · `/tmp/ubuntu_g05_wave63.log`；钉盘 L4 129 继承 |
| **Windows MSYS2**      | 继承 hybrid；本波未重跑（**不需要切换**）                                             |

***

## 6. 变更记录（倒序 · 21 日起；20 号见归档）

| 日期         | SHA                                                     | 摘要                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------- | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-21 | **`cb229558`** / docs（本波） | **R2 pipeline_abi Cap residual pure wave63**：`typeck_module_entry_only` + `typeck_module_with_sidecar` + `pipeline_typeck_module_for_ctx_impl` 真迁 pure orch（null 门 + `get_ndep` 分支；Cap residual `typeck_module` C 前端 + always-seed `typeck_dep_module_ptrs_base`；G.7 pure thin `pipeline_typeck_module_for_ctx`→`_impl`；seed 冷孪生 `#ifndef FROM_X`；FROM_X rest pure **extern**）。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave63.log`）。Ubuntu L2：hybrid pure **W**（entry/sidecar/for_ctx_impl）+ pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave63.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`5df38306`** / docs | **R2 pipeline_abi Cap residual pure wave62**：`shux_pipeline_one_ctx_for_dep_prerun_map_impl` 真迁 pure orch（tmp malloc arena/module + `parser_parse_into_init` + G.7 pure `driver_parse_into_buf_rc` 保留 ok 0\|-2；G.7 pure `shux_module_num_imports` / `import_path_cstr` / `find_loaded` / `pctx_update_dep_slots_no_reset`；硬失败回退全量 slots；seed 冷孪生 `#ifndef FROM_X`；FROM_X rest pure **extern**）。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave62.log`）。Ubuntu L2：hybrid pure **W**（map_impl）+ pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave62.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`3a7eaec9`** / docs | **R2 pipeline_abi Cap residual pure wave61**：`shux_preprocess_raw_to_malloc_impl` 真迁 pure orch（scratch + define table + `preprocess_x_buf` + owned dup；Cap residual preprocess_*；G.7 pure `pipeline_diag_preprocess_*` + `shux_ptr_slot_set` / `shux_size_slot_set`；oversized reportf cold-only / pure fail；seed 冷孪生 `#ifndef FROM_X`；FROM_X rest pure **extern**）。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave61.log`）。Ubuntu L2：hybrid pure **W** + map_impl **T** + pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave61.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`b4ced07d`** / docs | **R2 pipeline_abi Cap residual pure wave60**：`shux_pipeline_dep_prerun_typeck_only_impl` 真迁 pure orch（G.7 pure `pipeline_parse_set_main_from_buf_c` + `pipeline_load_and_sync_direct_import_deps_c` + `pipeline_typeck_dep_prerun_module_c`；parse fail → -2；INT32_MAX 闸；SHUX_DEBUG_PIPE notes cold-only；seed 冷孪生 `#ifndef FROM_X`；FROM_X rest pure **extern**）。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave60.log`）。Ubuntu L2：hybrid pure **W** + preprocess_impl **T** + pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave60.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`16763e93`** / docs | **R2 pipeline_abi Cap residual pure wave59**：`shux_pipeline_dep_prerun_parse_only_impl` 真迁 pure orch（G.7 pure `parser_parse_into_init` + `pipeline_parse_set_main_from_buf_c`；禁 bare `parser_parse_into`；INT32_MAX 闸；SHUX_ASM_DEBUG notes cold-only；seed 冷孪生 `#ifndef FROM_X`；FROM_X rest pure **extern**）。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave59.log`）。Ubuntu L2：hybrid pure **W** + typeck_only_impl **T** + pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave59.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`373c1d22`** / docs | **R2 pipeline_abi Cap residual pure wave58**：`shux_pipeline_dep_prerun_parse_skip_typeck_impl` 真迁 pure orch（G.7 `driver_check_only_get/set` + `driver_x_pipeline_skip_{typeck,codegen}_set` + `driver_pipeline_dep_ctx_{get,set}_asm_entry_module_only`；纯 large_stack wave56；禁 C 字段直写 pctx；seed 冷孪生 `#ifndef FROM_X`；FROM_X rest pure **extern**）。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave58.log`）。Ubuntu L2：hybrid pure **W** + pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave58.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`99830ff5`** / docs | **R2 pipeline_abi Cap residual pure wave57**：`shux_asm_codegen_elf_o_thread_fn_impl` + `shux_asm_codegen_elf_o_large_stack_impl` 真迁 pure orch（栈 AsmElfLargeArgs u8[48]；always-seed Cap-fn-ptr `shux_asm_codegen_elf_o_thread_fn_ptr` + Cap residual `shux_asm_codegen_elf_o_product_emit` 外部 reloc→bridge 强符号，禁同 TU weak stub -1；G.7 `driver_run_thread_on_large_stack`；sentinel -99 回退）。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave57.log`）。Ubuntu L2：hybrid pure **W** + product_emit/fn_ptr **T** + pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave57.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`9059161f`** / docs | **R2 pipeline_abi Cap residual pure wave56**：`pipeline_run_x_thread_fn_impl` + `shux_pipeline_run_x_pipeline_large_stack_impl` 真迁 pure orch（栈 PipelineRunSuArgs[56]；always-seed Cap-fn-ptr `pipeline_run_x_thread_fn_ptr`；G.7 `driver_run_thread_on_large_stack`；sentinel -99 回退；DEBUG_PIPE notes cold-only）；asm elf_o large-stack `_impl` 仍 seed（同 TU pure stub emit 禁 pure 调）。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave56.log`）。Ubuntu L2：hybrid pure **W** + Cap-fn-ptr **T** + pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave56.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`8a0be614`** / docs | **R2 pipeline_abi Cap residual pure wave55**：`shux_load_one_direct_resolve_read_preprocess` 真迁 pure orch（栈 resolved[4096] + FileView u8[32]；pure resolve multi；runtime_read_file_view；pure preprocess；release；open/preprocess diags）；seed 冷孪生 `#ifndef FROM_X`；FROM_X rest pure **extern**；G.7 load_one/paths_tmp 复用。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave55.log`）。Ubuntu L2：hybrid pure **W** + pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave55.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`910e0390`** / docs | **R2 pipeline_abi Cap residual pure wave54**：`shux_collect_strdup` 真迁 pure 薄壳（malloc + scan len + byte copy + trailing NUL；禁 libc strdup 短名）；seed 冷孪生 `#ifndef FROM_X`（仍 wrap strdup）；FROM_X rest pure **extern**。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave54b.log`）。Ubuntu L2：hybrid pure **W** + pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave54.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`6a127355`** / docs | **R2 pipeline_abi Cap residual pure wave53**：`shux_collect_paths_tmp_resolve_parse_enqueue` 真迁 pure orch（early ensure tmp + Cap residual `resolve_read_preprocess` + G.7 pure `tmp_parse_and_enqueue` + free prep）；seed 冷孪生 `#ifndef FROM_X`；FROM_X rest pure **extern**；paths_process_one 复用 pure paths_tmp。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave53.log`）。Ubuntu L2：hybrid pure **W** + pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave53b.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`f4b3b34e`** / docs | **R2 pipeline_abi Cap residual pure wave52**：`shux_collect_tmp_parse_and_enqueue` 真迁 pure orch（malloc/memset ensure + `pipeline_parse_into_bytes` + G.7 pure `enqueue_module_imports`）；seed 冷孪生 `#ifndef FROM_X`（保留 SHUX_DEBUG_PIPE note）；FROM_X rest pure **extern**；paths_tmp Cap residual 复用 pure tmp_parse。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave52.log`）。Ubuntu L2：hybrid pure **W** + pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave52.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`bdf150cf`** / docs | **R2 pipeline_abi Cap residual pure wave51**：`shux_load_one_direct_import_at` + `shux_load_direct_fail_cleanup` 真迁 pure orch；Cap residual `shux_load_one_direct_resolve_read_preprocess`（PATH_MAX+FILE view+preprocess→owned prep）；G.7 `paths_tmp` 复用同一 Cap residual（消双份 resolve/read/preprocess）；seed 冷孪生 `#ifndef FROM_X`；FROM_X rest pure **extern**。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave51.log`）。Ubuntu L2：hybrid pure **W** + pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave51.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`d2aeab88`** / docs | **R2 pipeline_abi Cap residual pure wave50**：`shux_collect_deps_transitive_impl` + `shux_collect_dep_paths_transitive_impl` 真迁 pure orch；栈 `to_load[32]`=256B + tmp void-star cells（G.7 同 check_one_file argv 模式）；G.7 复用 `seed_to_load` + pure process_one 排干；成功/失败 free 队列+tmp+partial deps；seed 冷孪生 `#ifndef FROM_X`；FROM_X rest pure **extern**。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave50.log`）。Ubuntu L2：hybrid pure **W** + pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave50.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`fe48289b`** / docs | **R2 pipeline_abi Cap residual pure wave49**：`shux_collect_paths_process_one` 真迁 pure orch；G.7 复用 `shux_collect_strdup` + `shux_collect_tmp_parse_and_enqueue`；Cap residual always-seed `shux_collect_paths_tmp_resolve_parse_enqueue`（ensure tmp + resolve/read/preprocess + parse+enqueue + free prep）；transitive_impl 仍 seed；FROM_X rest extern。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave49.log`）。Ubuntu L2：hybrid pure **W** + pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave49.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`fbc5b80f`** / docs | **R2 pipeline_abi Cap residual pure wave48**：`shux_collect_deps_process_one` 真迁 pure orch；G.7 复用 `shux_load_one_direct_import_at`（resolve/read/preprocess 写槽）；Cap residual always-seed `shux_collect_tmp_parse_and_enqueue`（tmp malloc+memset + parse_into_bytes + enqueue）；paths_process_one/transitive_impl 仍 seed；FROM_X rest extern。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave48.log`）。Ubuntu L2：hybrid pure **W** + pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave48.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`1dd91711`** / docs | **R2 pipeline_abi Cap residual pure wave47**：`shux_collect_seed_to_load` + `shux_collect_enqueue_module_imports` 真迁 pure；G.7 复用 wave46 import cstr / to_load_has / find_loaded / ptr slots；Cap residual always-seed `shux_collect_strdup`（禁 libc strdup 名冲突）；process_one/transitive_impl 仍 seed；FROM_X rest extern。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave47b.log`）。Ubuntu L2：hybrid pure **W** + pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave47.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`ad7359a6`** / fix **`77dcc558`** / docs | **R2 pipeline_abi Cap residual pure wave46**：ptr/size slots + i32_store + module_num_imports/import_path_cstr + collect_to_load_has + preprocess_directive_code 真迁 pure；seed cold twins；fix：FROM_X rest 补 pure 符号 extern（防 -Werror=implicit → hybrid fallback full seed）。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave46b.log`）。Ubuntu L2：hybrid pure **W** + pure-asm 矩阵 rv42（`/tmp/ubuntu_g05_wave46c.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED LP64 · **日常 L2 非 tip L4** |
| 2026-07-21 | **`f16da857`** / docs | **lexer 嵌套块注释**：`skip_whitespace_and_comments` 深度计数（`/*`+1 / `*/`−1 / depth=0 结束）；seed + empty\_surface 同 commit；探针 `tests/lexer/nested_block_comment.x` + `/**/`→99；docs 01/08。mac L2：g05 + c-backend 矩阵（`/tmp/mac_g05_nested_comment.log`；Darwin pure-asm CG002 tip 既有）。Ubuntu L2：pure-asm 矩阵 + nested rv42/99（`/tmp/ubuntu_g05_nested_comment.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`b023e122`** / docs | **R2 pipeline\_abi Cap residual pure wave45**：根因=`.x` docblock `char**/void*` 内嵌 `*/` 截断注释 → 后续 export function 静默未入 AST（`-E` 仅 extern）；根修 docblock（G.9）；产品 PREFER hybrid full `.x` pure + seed rest；always-seed BSS residual；去掉 debug export-extern 双权威。mac/Ubuntu L2。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2** |
| 2026-07-21 | **`cfcd00ce`** / docs | **R2 driver\_abi Cap residual pure wave44**：`driver_print_usage_write` pure color orch（NO\_COLOR nonnull / FORCE truthy / isatty）；always-seed residual `shux_driver_usage_write_stdout` 持巨型 plain/color lit + fwrite+fflush（.x 禁长 \\n 串，非单行绕过）；G.7 复用 env\_nonnull/flag\_truthy；cold twin + `#ifndef FROM_X` 无 pure-dup；**JSDoc**。mac L2：PREFER hybrid + g05 + help/color/c-backend（`/tmp/mac_g05_wave44d.log`；Darwin pure-asm CG002 tip 既有）。Ubuntu L2：矩阵 pure-asm rv42 + pure **W**（`/tmp/ubuntu_g05_wave44.log`）。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`77a23d78`** / parser **`c5994b0c`** / docs | **parser TOKEN\_SELF 绑定根修** + **R2 driver\_abi Cap residual pure wave43**：探针否定多行 export extern；真因=`self` 形参 → TOKEN\_SELF → param 只认 IDENT → 整函数静默丢 AST；根修 param/let/primary 接受 SELF（len=4）。wave43：`driver_dispatch_sibling_try_spawn` pure orch；G.7 `driver_argv0_basename_is`；path pure BSS 512；always-seed residual argv0\_get + access\_spawn；cold twin + `#ifndef FROM_X`；**print\_usage\_write 仍 always-seed**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 rv42（`/tmp/mac_g05_wave43f.log` · `/tmp/ubuntu_g05_wave43b.log`）；Ubuntu pure **W**。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`8e5c2286`** / docs | **R2 driver\_abi Cap residual pure wave42**：`driver_exec_compiled_body` 真迁 thin.x pure orch；G.7 复用 `driver_exec_path_is_non_exe`（rt\_run\_exec）；always-seed residual `shux_driver_exec_scan_out_path_opaque`（\*u8→char\*\* cast+scan）+ `shux_driver_exec_spawn_wait`（WIN spawn / POSIX fork+exec）；seed cold twin + `#ifndef FROM_X` 无 pure-dup；sibling/usage 仍 OS residual always-seed；**JSDoc**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + `shux run` rv42（`/tmp/mac_g05_wave42.log` · `/tmp/ubuntu_g05_wave42.log`）；Ubuntu pure 符号 **W**。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`87d03c82`** / docs | **R2 driver\_abi Cap residual pure wave41**：`driver_asm_mkstemp_fdopen` 真迁 thin.x pure orch；G.7 复用 wave27 `tmp_prefix`+cstr helpers；WINDOWS gate residual `shux_driver_asm_mkstemp_fdopen_enabled`；g05 `shux_driver_fdopen_wb_opaque`；fail 路径 close+unlink+clear path[0]；seed cold twin + `#ifndef FROM_X` 无 pure-dup；sibling/usage/exec 仍 OS residual always-seed；**JSDoc**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵（`/tmp/mac_g05_wave41.log` · `/tmp/ubuntu_g05_wave41.log`）；Ubuntu pure 符号 **W**。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`b403668d`** / docs | **R2 driver\_abi Cap residual pure wave40**：`driver_stdio_stderr` + `driver_asm_fflush_stdout` + `driver_asm_fopen_wb` + `driver_asm_write_metric_o` 真迁 thin.x；G.7 g05 `shux_driver_stderr_ptr` / `shux_driver_fflush_stdout` / `shux_driver_fopen_wb_opaque`（`"wb"` ≠ wave27 `"w"`）；write\_metric pure orch fopen\_wb+1×0 fwrite+fclose；seed cold twins + `#ifndef FROM_X` 无 pure-dup；mkstemp/sibling/usage/exec 仍 OS residual always-seed；**JSDoc**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵（`/tmp/mac_g05_wave40.log` · `/tmp/ubuntu_g05_wave40.log`）；Ubuntu pure 符号 **W**。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`7ab9a80b`** / docs `3b17b495` | **R2 driver\_abi Cap residual pure wave39**：`driver_stdio_stdout` + `driver_asm_fwrite` + `driver_x_emit_fwrite_stdout` 真迁 thin.x；G.7 g05 `shux_driver_stdout_ptr` / `shux_driver_fwrite_opaque`；Cap OS residual `shux_driver_fwrite_stdout_n`（fwrite+fflush 字节数 ABI）；seed cold twins + `#ifndef FROM_X` 无 pure-dup；fopen/mkstemp/fflush/write\_metric/sibling/usage/exec/stderr 仍 OS residual always-seed；**JSDoc**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵（`/tmp/mac_g05_wave39.log` · `/tmp/ubuntu_g05_wave39.log`）；Ubuntu pure 符号 **W**。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`6c98fa28`** / docs | **R2 driver\_abi Cap residual pure wave38**：`driver_asm_elf_ctx_free` + `driver_parse_into_buf_rc` 真迁 thin.x；free 配对 wave23 calloc；Cap-struct residual `shux_parser_parse_into_buf_rc` 解包 `parser_ParseIntoResult`（.x 不消费 C struct 返回）；seed cold twins + `#ifndef FROM_X` 无 pure-dup；sibling/fopen/usage/exec 仍 OS residual always-seed；**JSDoc**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵（`/tmp/mac_g05_wave38.log` · `/tmp/ubuntu_g05_wave38.log`）；Ubuntu pure 符号 **W**。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`395fac43`** / docs | **R2 driver\_abi Cap residual pure wave37**：`driver_arena/module_static_{slot,size}` + `driver_run_stack_esc_gate_on_large_stack` + `driver_parser_diag_fail_tok_kind` 真迁 thin.x；always-seed Cap-global base + Cap-fn-ptr `fn_ptr` residual；BSS pack `shux_slice_uint8_t` 16B LP64 + G.7 `parser_diag_fail_at_token_kind`；固定 128MiB/2MiB；seed cold twins + `#ifndef FROM_X` 无 pure-dup；sibling/fopen/usage 仍 OS residual always-seed；**JSDoc**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + Ubuntu pure-asm rv42（`/tmp/mac_g05_wave37.log` · `/tmp/ubuntu_g05_wave37.log`）；Ubuntu pure 符号 **W**。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED LP64 · **日常 L2 非 tip L4** |
| 2026-07-21 | **`4fa18ede`** / docs | **R2 driver\_abi Cap residual pure wave36**：`driver_dispatch_lib_roots_from_key` + `driver_dispatch_run_compiler_parsed` 真迁 thin.x；BSS 16×512 + 16×LP64 roots + G.7 `driver_lib_roots_from_key`；BSS pack `DriverCompileParsedAbi` 176 LP64（wave32 offsetof）+ `driver_run_compiler_parsed`；opt 共用 wave32 lit `"2"`；seed cold twins + `#ifndef FROM_X` 无 pure-dup；`sibling_try_spawn`/fopen 仍 OS residual always-seed；**JSDoc**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + Ubuntu pure-asm rv42（`/tmp/mac_g05_wave36b.log` · `/tmp/ubuntu_g05_wave36.log`）；Ubuntu pure 符号 **W**。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED LP64 · **日常 L2 非 tip L4** |
| 2026-07-21 | **`d213a091`** / docs | **R2 driver\_abi Cap residual pure wave35**：`driver_typeck_ndep_set`/`dep_ptrs_set` + `driver_diag_push_file`/`restore` + `driver_dispatch_lib_root_at`/`opt_default` 真迁 thin.x；G.7 `typeck_ndep_store` + dep_module/arena_set；diag_push_file/restore；G.7 `shux_ptr_slot_get`（max 16）；opt default 共用 wave32 BSS lit `"2"`；seed cold twins + `#ifndef FROM_X` 无 pure-dup；`lib_roots_from_key`/`run_compiler_parsed`/`sibling_try_spawn` 仍 always-seed；**JSDoc**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + Ubuntu pure-asm rv42（`/tmp/mac_g05_wave35.log` · `/tmp/ubuntu_g05_wave35.log`）；Ubuntu pure 符号 **W**。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED LP64 · **日常 L2 非 tip L4** |
| 2026-07-21 | **`440de2b0`** / docs `12913eef` | **R2 driver\_abi Cap residual pure wave34**：`driver_asm_try_c_frontend_early`/`try_c_typeck_precheck`/`use_compiler_impl_c` + `bind_lib_roots`/`argv0` + `collect_defines`/`defines_as_u8`/`ndefines_get` 真迁 thin.x；产品 NO_C 固定 -2/-1/0；one_root BSS + G.7；defines 64×LP64 BSS + pure `argv_collect_defines`；seed cold twins + `#ifndef FROM_X` 无 pure-dup；fopen/mkstemp/fwrite 永久 OS residual 仍 seed；**JSDoc**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + Ubuntu pure-asm rv42（`/tmp/mac_g05_wave34e.log` · `/tmp/ubuntu_g05_wave34.log`）；Ubuntu pure 符号 **W**。钉盘仍 **`d4e2dca6`**。PLATFORM: SHARED LP64 · **日常 L2 非 tip L4** |
| 2026-07-21 | **升钉 `d4e2dca6`** | **tip L4 收口**：前钉 `86281cc6`（15:04）后 ~3h / 41 commits（wave14–33 全 L2）→ 双端 L4 真冷全擦 + g05 + 矩阵 + Ubuntu pure-asm rv42 + bstrict **129** · `/tmp/mac_true_bstrict_d4e2dca6.log` · `/tmp/ubuntu_true_bstrict_d4e2dca6.log` → **钉盘 `86281cc6` → `d4e2dca6`**。注：mac bstrict 中并行 docs `7ec4edc2`（规划+parser_gen refresh）；产品钉盘诚实落在已验证的 residual pure tip。PLATFORM: SHARED · **构建级别 L4** |
| 2026-07-21 | **`ae08a970`** / docs `d4e2dca6`                          | **R2 driver\_abi Cap residual pure wave33**：`driver_x_emit` clear/bind/take/get/lib_root_at/effective_lib_roots + `try_extern_via_cparser` 真迁 thin.x；G.7 `shux_ptr_slot_*` on always-seed `c_path_cell`/`lib_roots_raw`/path+lib slots；Cap-global-bss 数据仍 `rt_emit_state`；stdout setvbuf/fwrite 永久 OS residual always-seed；seed cold twins + `#ifndef FROM_X` 无 pure-dup；**JSDoc**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42（`/tmp/mac_g05_wave33b.log` · `/tmp/ubuntu_g05_wave33d.log`）；Ubuntu pure 符号 **W**（G05\_X\_O\_WEAK OK）。钉盘仍 **`86281cc6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`cd367c6c`** / docs `a55ce483`                          | **R2 driver\_abi Cap residual pure wave32**：parsed field accessors（input/out/lib_roots/n_lib/want_asm/target/opt_level/use_lto）+ `try_c_after_pp` 真迁 thin.x；LP64 `DriverCompileParsedAbi` offsetof + LE i32 / G.7 `shux_ptr_slot_get`；lib_roots=`p+16` 嵌入数组基址；opt 默认 BSS lit `"2"`；try_c 产品 NO_C 固定 -2；seed cold twins + `#ifndef FROM_X` 无 pure-dup；**JSDoc**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42（`/tmp/mac_g05_wave32.log` · `/tmp/ubuntu_g05_wave32.log`）；Ubuntu pure 符号 **W**（G05\_X\_O\_WEAK OK）。钉盘仍 **`86281cc6`**。PLATFORM: SHARED LP64 · **日常 L2 非 tip L4** |
| 2026-07-21 | **`591644fe`** / docs `78437072`                          | **R2 driver\_abi Cap residual pure wave31**：`driver_parsed_deps_has_std_io_{core,driver}` + `apply_preamble_skip` + `maybe_dump_prep` 真迁 thin.x；G.7 `shux_ptr_slot_get` 扫 dep_paths + strcmp lit；mask 1\|4 / 8 对齐 codegen.h；dump=`shux_write_path_bytes`+`driver_diag_append_*`+`diag_report`（无 va\_list）；seed cold twins + `#ifndef FROM_X` 无 pure-dup；**JSDoc**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42（`/tmp/mac_g05_wave31.log` · `/tmp/ubuntu_g05_wave31.log`）；Ubuntu pure 符号 **W**（G05\_X\_O\_WEAK OK）。钉盘仍 **`86281cc6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`cae60d4c`** / docs `cedb7291`                        | **wave30 g05 sed 根修**：`mkstemp`/`rename` 补入 `codegen_is_libc_conflicting_extern_name`（`.x`+seed 同 commit）；曾仅 g05 sed 删行 = 双权威。g05 sed 注释标明仍防御 cold/old、opendir opaque、`shux_*` harness。mac L2：codegen\_x + g05 + C 矩阵 + `-E` thin 0 redecl（`/tmp/mac_g05_wave30b.log`）；Ubuntu L2：矩阵+pure-asm rv42（`/tmp/ubuntu_g05_wave30.log`）。钉盘仍 **`86281cc6`**。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                |
| 2026-07-21 | **`b9bac87b`** / docs `8d5b3ef7`                        | **R2 rt\_preamble Cap residual pure wave29**：根修 pure `io_net` N **219→224**（表实长）；**WEAK\_IO\_BATCH** skip 对齐 **178..181**（旧 cold `i==174` / 旧 .x `124..134` 双权威）；seed cold + `rt_preamble.x` + surface + thin pure N 同 commit；write 已 R2 pure；Cap 巨型字串表仍 always-seed；产品 permanent slice 仍 cold write（Darwin 不 cp hybrid）。mac L2：g05 + 产品 C 矩阵（`/tmp/mac_g05_wave29e.log`）；Ubuntu L2：矩阵+pure-asm rv42（`/tmp/ubuntu_g05_wave29.log`）。钉盘仍 **`86281cc6`**。PLATFORM: SHARED · **日常 L2 非 tip L4**                                             |
| 2026-07-21 | **`7e7589ee`** / docs `bf08c04a`                        | **R2 driver\_abi Cap residual pure wave28**：`driver_parsed_invoke_cc` 真迁 thin.x；std .o path pack + set/clear user .o + `shux_invoke_cc`；`c_paths[1]` via G.7 `shux_ptr_slot_set`；BLD001/KEEP\_C 用 `driver_diag_append_*` + `diag_report(_with_code)`（无 va\_list）；cold twin + `#ifndef FROM_X` 无 pure-dup；**JSDoc**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42（`/tmp/mac_g05_wave28b.log` · `/tmp/ubuntu_g05_wave28b.log`）；Ubuntu pure 符号 **W**（G05\_X\_O\_WEAK OK）。钉盘仍 **`86281cc6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`bf764a36`** / docs `4b5a9196`                        | **R2 driver\_abi Cap residual pure wave27**：`driver_parsed_open_out_file` 真迁 thin.x；stdout 门控 pure；模板=`shux_driver_tmp_prefix`+`shux_x.XXXXXX`；**close-before-rename（BLD001）**；g05 `fopen_write_opaque`；cold twin + `#ifndef FROM_X` 无 pure-dup；**JSDoc**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42（`/tmp/mac_g05_wave27.log` · `/tmp/ubuntu_g05_wave27.log`）。钉盘仍 **`86281cc6`**。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                     |
| 2026-07-21 | **`a451a6ed`** / docs `82cfc082`                        | **R2 driver\_abi Cap residual pure wave26**：`driver_parsed_fclose` / `fclose_rc` / `write_out` 真迁 thin.x；g05 `stdout_ptr`+`fclose_opaque`+`fwrite_opaque`（wave22 fputs 模式）；write\_out first\_line/need\_preamble pure + min preamble 短 lit fputs；`write_io_net`/`write_fs_path` 仍 seed 巨型表；cold twins + `#ifndef FROM_X` 无 pure-dup；**JSDoc**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42（`/tmp/mac_g05_wave26.log` · `/tmp/ubuntu_g05_wave26.log`）。钉盘仍 **`86281cc6`**。PLATFORM: SHARED · **日常 L2 非 tip L4**                 |
| 2026-07-21 | **`35c93231`** / docs `afd125b5`                        | **R2 driver\_abi Cap residual pure wave25**：tmp path BSS（`driver_asm_tmp_path_slot` 64B + `driver_parsed_tmp_c_{slot,buf}` 64/256B）真迁 thin.x；seed `driver_parsed_open_out_file` **只经 accessor 写 buf**（消 hybrid 双 BSS）；cold twins + `#ifndef FROM_X` 无 pure-dup；**JSDoc**。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42（`/tmp/mac_g05_wave25.log` · `/tmp/ubuntu_g05_wave25.log`）。钉盘仍 **`86281cc6`**。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                      |
| 2026-07-21 | **`cff39ad6`** / docs `a2635664`                        | **R2 driver\_abi Cap residual pure wave24**：outbuf `free`/`len`/`data` + ptr/size table `free`/`get`/`set` + `diag_snapshot_free` 真迁 thin.x（配对 wave23 calloc；G.7 `shux_ptr_slot_*` + LE usize\@i\*8；len=LE i32\@CAP）；seed cold twins + `#ifndef FROM_X` 无 pure-dup；**JSDoc** 块注释（`@param` type+role；`*/` 单独成行）。mac+Ubuntu L2：PREFER hybrid（g05\_try\_x\_to\_o 注入 fputs\_opaque）+ g05 + 矩阵 + pure-asm rv42。钉盘仍 **`86281cc6`**。PLATFORM: SHARED LP64 · **日常 L2 非 tip L4**                                                                 |
| 2026-07-21 | **`1c8ff602`** / docs `8a517649`                        | **R2 driver\_abi Cap residual pure wave23**：calloc 族（outbuf/dep\_ctx/ptr/size/diag/elf\_ctx）+ `driver_asm_pctx_apply_host_defaults` 真迁 thin.x（libc calloc/malloc/memset + 固定 LP64 size / `pipeline_sizeof_*`；host `#ifdef` → 永久 OS residual `shux_driver_host_*`；wave19 setter 写字段）；seed cold twins + `#ifndef FROM_X` 无 pure-dup；多行块注释 `*/` 单独成行。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42。钉盘仍 **`86281cc6`**。PLATFORM: SHARED LP64 · **日常 L2 非 tip L4**                                                                  |
| 2026-07-21 | **`a11f7134`** / docs `8bb27f39`                        | **R2 driver\_abi Cap residual pure wave22**：`driver_preamble_fputs` 真迁 thin.x（null 守卫 + g05 prologue `shux_driver_fputs_opaque` FILE\* cast；对齐 `shux_fmt_opendir` 模式；.x 无 FILE\*）；seed cold twin + `#ifndef FROM_X` 无 pure-dup。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42；mac 另验 `shux fmt`。钉盘仍 **`86281cc6`**。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                                        |
| 2026-07-21 | **`914a7658`** / docs `c658c5b6`                        | **R2 driver\_abi Cap residual pure wave21**：`driver_entry_fmt_argv_slot` 真迁 thin.x（绕 \*u8\[2] lit XT001：BSS 字节 lit `"shux"`/`"fmt"` + 2× LP64 ptr slots via G.7 `shux_ptr_slot_set`；返回表基址 ABI=`char**`）；seed cold twin + `#ifndef FROM_X` 无 pure-dup。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42；mac 另验 `shux fmt` 路径。钉盘仍 **`86281cc6`**。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                               |
| 2026-07-21 | **`abf7ea8e`** / docs `2a7b72ab`                        | **R2 driver\_abi Cap residual pure wave20**：`driver_preamble_{io_net,fs_path}_line_at/count` 真迁 thin.x（固定 N=219/21 + G.7 `shux_ptr_slot_get` on always-seed `*_lines_raw`；巨型字串表仍 `rt_preamble.from_x.c`）；seed cold twins + `#ifndef FROM_X` 无 pure-dup；顺带 hoist `pipeline_dep_ctx_heap_destroy` extern 供冷 seed host-cc。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42。钉盘仍 **`86281cc6`**。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                  |
| 2026-07-21 | **`176adc00`** / docs `e8df3d3f`                        | **R2 driver\_abi Cap residual pure wave19**：`driver_pipeline_dep_ctx_{set,get}_*`（use\_asm / target\_arch / cpu\_features / macho / coff / entry\_already / asm\_entry / skip\_codegen\_dep\_0）真迁 thin.x（LP64 固定 `offsetof` + wave14 LE load/store；无 C 结构体）；seed cold twins + `#ifndef FROM_X` 无 pure-dup；calloc / `driver_asm_pctx_apply_host_defaults` 仍 seed。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42。钉盘仍 **`86281cc6`**。PLATFORM: SHARED LP64 · **日常 L2 非 tip L4**                                                    |
| 2026-07-21 | **`184b09d5`** / docs `59000972`                        | **R2 driver\_abi Cap residual pure wave18**：`driver_parsed_work_{reset,p/i/z_*,cleanup}` 真迁 thin.x（p：G.7 `shux_ptr_slot_*` on LP64 raw `u8[192]` 24 槽；`i32[14]`/`usize[4]`；free + dep\_ctx destroy + `driver_parsed_fclose` + `unlink` tmp\_c；reset 清 seed tmp slots）；always-seed `driver_parsed_tmp_c_{slot,buf}`；seed cold twins + `#ifndef FROM_X` 无 pure-dup parsed work BSS。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42。钉盘仍 **`86281cc6`**。PLATFORM: SHARED · **日常 L2 非 tip L4**                                        |
| 2026-07-21 | **`005236bc`** / docs `1f1b8984`                        | **R2 driver\_abi Cap residual pure wave17**：`driver_asm_work_{reset,p/i/z_*,cleanup}` 真迁 thin.x（p：G.7 `shux_ptr_slot_*` on LP64 raw `u8[200]` 25 槽；`i32[16]`/`usize[4]`；free + `pipeline_dep_ctx_heap_destroy` + `driver_asm_fclose`；reset 清 seed `tmp_path[0]`）；seed cold twins + `#ifndef FROM_X` 无 pure-dup asm work BSS。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42。钉盘仍 **`86281cc6`**。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                             |
| 2026-07-21 | **`2ebf0e87`** / docs `83bdd33b`                        | **R2 driver\_abi Cap residual pure wave16**：`driver_x_emit_work_{reset,p/i/z_*,cleanup}` 真迁 thin.x（p：G.7 `shux_ptr_slot_*` on LP64 raw `u8[208]`；`i32[17]`/`usize[5]`；free + `pipeline_dep_ctx_heap_destroy`）；seed cold twins + `#ifndef FROM_X` 无 pure-dup work BSS。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42。钉盘仍 **`86281cc6`**。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                    |
| 2026-07-21 | **`aa8e35d7`** / docs `551bc40a`                        | **R2 driver\_abi Cap residual pure wave15**：`driver_entry_{ab,code,suggest,msg,tmp,tmp2}_slot` + `path_max`/`entry_dir` 真迁 thin.x（模块 BSS `u8[N]`）；`fmt_argv` 仍 seed（pointer-array lit XT001）；seed `#ifndef FROM_X` 无 pure-dup buffer slots。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42。钉盘仍 **`86281cc6`**。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                                              |
| 2026-07-21 | **`bf18c396`** / docs `24f46fc3`                        | **R2 driver\_abi Cap residual pure wave14**：`driver_asm_stub_gas_line_at/count` + `out_append_cstr` 真迁 thin.x（OutBuf `data[9MiB]`+LE `len`）；seed `#ifndef FROM_X` 无 pure-dup。mac+Ubuntu L2：PREFER hybrid + g05 + 矩阵 + pure-asm rv42。钉盘仍 **`86281cc6`**。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                                                                                                            |
| 2026-07-21 | **升钉** **`86281cc6`**                                   | Ubuntu L4：`run-all-bstrict OK (129)` · `/tmp/ubuntu_true_bstrict_86281cc6.log`；mac L4：同 · `/tmp/mac_true_bstrict_86281cc6.log` → **钉盘** **`cf07b0d9`** **→** **`86281cc6`**（产品 tip 含 R2 rio Cap residual pure + nostdlib lseek + 既有 bxec pure）                                                                                                                                                                                                                                                                                          |
| 2026-07-21 | **`e18e801d`** / fix **`72bb1963`** / docs `86281cc6` 前 | **R2 runtime\_io\_abi Cap residual pure**：4× `_impl` 真迁；seed FROM\_X H=0；`fs_posix_*` 短名；nostdlib `lseek`。mac+Ubuntu L2 → 后 tip L4 升钉。PLATFORM: SHARED                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-07-21 | **升钉** **`cf07b0d9`**                                   | Ubuntu L4：`run-all-bstrict OK (129)` · `/tmp/ubuntu_true_bstrict_cf07b0d9.log`；mac L4：同 · `/tmp/mac_true_bstrict_cf07b0d9.log` → **钉盘** **`bcd63b9c`** **→** **`cf07b0d9`**（产品 tip 含 R2 bxec Cap residual pure wave1+2 模块闭）                                                                                                                                                                                                                                                                                                             |
| 2026-07-21 | **`f21e530c`** / docs `cf07b0d9`                        | **R2 bxec pure wave2**：+38 `arch_x86_64_enc_*`（label/imm/jcc/jmp/call/rbp/arg/store）；模块 **92** pure 闭；seed 全 `#ifndef FROM_X`。mac+Ubuntu L2：g05 PREFER thin+rest + 矩阵 + pure-asm rv42。后 tip L4 升钉。PLATFORM: SHARED                                                                                                                                                                                                                                                                                                                      |
| 2026-07-21 | **`83253fc3`** / docs `fb475d2d`                        | **R2 bxec pure wave1**：`backend_x86_64_enc_c` `.x` 真迁 54× `arch_x86_64_enc_*`（prologue/epilogue + fixed + multi）；seed `#ifndef FROM_X`；G.7 产品 C ABI 权威=本 TU。mac+Ubuntu L2：g05 PREFER thin+rest + 矩阵 + pure-asm rv42。钉盘仍 **`bcd63b9c`**。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                                                                                                                            |
| 2026-07-21 | **升钉** **`bcd63b9c`**                                   | Ubuntu L4：`run-all-bstrict OK (129)` · `/tmp/ubuntu_true_bstrict_8232d918.log`；mac L4：同 · `/tmp/mac_true_bstrict_8232d918.log` → **钉盘** **`ff064051`** **→** **`bcd63b9c`**（验证 HEAD docs `8232d918`；产品 tip 含 nostdlib realloc）                                                                                                                                                                                                                                                                                                          |
| 2026-07-21 | **`bcd63b9c`**                                          | **nostdlib realloc 根修**：bump 堆块前 `BootstrapHeapHdr.user_size`；`realloc` 只拷 `min(old,new)`。修 Ubuntu 大 TU `-E` \~50% SIGSEGV（GrowVec 扩容读穿；ASLR 间歇；`setarch -R` 曾假绿）。Ubuntu L2 压力 segs=0 → 后 tip L4 升钉。PLATFORM: SHARED                                                                                                                                                                                                                                                                                                                    |
| 2026-07-21 | **升钉** **`ff064051`**                                   | Ubuntu L4：`run-all-bstrict OK (129)` · `/tmp/ubuntu_true_bstrict_da9cfcc2.log`；mac L4：同 · `/tmp/mac_true_bstrict_da9cfcc2.log` → **钉盘** **`46ba2297`** **→** **`ff064051`**（验证 HEAD docs `da9cfcc2`，产品 tip 同）                                                                                                                                                                                                                                                                                                                           |
| 2026-07-21 | **`ff064051`**                                          | **bare CLI path**：`main.x` entry 对 `.x`/含分隔符 argv\[1] 走 `main_cmd_run`（有 `-o` 仅编译）；pin `driver_gen.linux.x86_64.c` 同语义。**simd-s4**：非 x86 compile 用 `-backend c`。mac+Ubuntu L2 s4 OK + bare rv=42 → 后以 tip L4 升钉。PLATFORM: SHARED                                                                                                                                                                                                                                                                                                        |
| 2026-07-21 | **`961207bf`**                                          | **§3.6 asm CLI user** **`.o`**：`runtime_link_abi` 各 ld 分支 append `g_shux_user_extra_o_files`；`driver_run_asm_backend` set/clear（.x+seed+surface）。G.7 不另起第二表。Ubuntu pure-asm marker glue→42；mac L2 矩阵。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                                                                                                                                                              |
| 2026-07-21 | **升钉** **`46ba2297`**                                   | Ubuntu L4：`run-all-bstrict OK (129)` · `/tmp/ubuntu_true_bstrict_46ba2297.log`；mac L4：同 · `/tmp/mac_true_bstrict_46ba2297.log` → **钉盘** **`11a57351`** **→** **`46ba2297`**                                                                                                                                                                                                                                                                                                                                                             |
| 2026-07-21 | **`46ba2297`**                                          | **opaque DIR\***：去掉 `dirent.h`；**不** skip `opendir`/`closedir`（emit SHUX `*u8` extern）。根因=系统 `DIR*` 与 opaque 契约冲突。codegen + seed 同 commit。PLATFORM: SHARED · **tip L4**                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-21 | **`52dabaf6`**                                          | preamble 去掉 `fcntl.h`（`open` 签名冲突）；暂留 `dirent.h` 过渡。PLATFORM: SHARED                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-07-21 | **`25f043ee`**                                          | preamble 曾 include dirent/fcntl 以匹配 skip 表（后由 opaque 路径收口）。PLATFORM: SHARED                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-07-21 | **`769356dd`**                                          | codegen skip `memchr`/`strchr*` 族（`string.h` 后冲突）；seed 同 commit。PLATFORM: SHARED                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-07-21 | **`836d0d0d`**                                          | codegen skip `strtoul`/`strtol` 族（`stdlib.h` 后冲突）；seed 同 commit。PLATFORM: SHARED                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-07-21 | **`113fd367`**                                          | libc skip 与 stdlib/string preamble 成对；Darwin multi-file heap → `libtool -static`。PLATFORM: SHARED+MACOS                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-21 | **`faf5b269`**                                          | **fix**：PREFER/`g05_try_x_to_o` `-E` 最多 5 次重试（Ubuntu 偶发 SIGSEGV → 曾 silent cold fallback）。PLATFORM: SHARED harness                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-07-21 | **`05e2506c`**                                          | **产品 PREFER** **`async_liveness`** **+** **`async_cps_codegen`**：Makefile + g05\_ensure full.x + rest（FROM\_X slice\_marker）ld -r；TU 本已独立于 glue；-E prologue 对齐 g05\_try（stdlib/string）。mac+Ubuntu L2/L3 g05 + 矩阵 + prove 2× IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                                                                                                                             |
| 2026-07-21 | **`708d63e0`**                                          | **PREFER unbundle** **`async_asm_pool`**：`pipeline_glue` 不再 #include 冷 seed；独立 `src/async/async_asm_pool.o` 入全部 SUPPORT + g05\_relink\_env；PREFER full.x + rest marker；ensure 重建 stale pipeline\_x/standalone/Darwin filtered。mac+Ubuntu L2/L3 g05 + 矩阵 + prove IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                                                                                         |
| 2026-07-21 | **`e38f6624`**                                          | **G.7 fputs Cap residual 收敛**：删 module-local `async_liveness_fputs` / `async_cps_fputs`；`.x` emit pure 改调共享权威 `driver_preamble_fputs`（`runtime_driver_abi`）；保留 `*_fputs_i32_dec`；cold seed 仍原生 FILE\*；surface re-pin。mac+Ubuntu L2 prove 2× IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                                                                                                             |
| 2026-07-21 | **`0efbc570`**                                          | *R2 async\_liveness Cap residual pure FILE* emit\_\*\*：`.x` 真迁 `emit_frame_typedef` / `emit_frame_local` / `emit_codegen_comment` + `fputs_i32_dec`；opaque `async_liveness_fputs` always seed；G.7 type\_to\_c/lookup；长字面量拆片（-E 串 cap\~63）；宿主 layout LE；seed `#ifndef FROM_X`；surface re-pin。mac L2 + Ubuntu L3 后 prove IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                                  |
| 2026-07-21 | **`e364aaf2`**                                          | **R2 async\_cps\_codegen Cap residual pure wave5（begin/hoist）**：`.x` 真迁 `async_cps_codegen_begin` / `emit_param_statics` / `emit_hoisted_lets_impl` + `store_ptr` / `emit_static_decl` / run-seed take；G.7 `async_liveness_type_to_c_buf`；opaque `async_cps_fputs`；宿主 ASTFunc/ASTParam/ASTLet/num\_awaits LE；seed `#ifndef FROM_X`；surface re-pin。mac+Ubuntu L2 prove IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                   |
| 2026-07-21 | **`33505fbe`**                                          | \**R2 async\_cps\_codegen Cap residual pure wave4（FILE* emit）\*\*：`.x` 真迁 `async_cps_codegen_end` / `emit_phase_reset` / `after_await(_io)` / `after_await_impl` / `emit_sched_wrapper` + `store_i32` / `fputs_i32_dec`；opaque 桥 `async_cps_fputs` always seed；宿主 AsyncCpsCodegenCtx + AsyncFrameLayout LE；seed `#ifndef FROM_X`；begin/param\_statics/hoist\_impl 仍 seed。surface re-pin。mac+Ubuntu L2 prove IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                             |
| 2026-07-21 | **`33881f79`**                                          | **R2 async\_cps\_codegen Cap residual pure wave3（walk\_impl）**：`.x` 真迁 `expr_references_run_async_impl` / `block_has_run_async_ref_impl` + `ptr_at` / `is_binop` / `is_walk_unary`；宿主 AST LE（RUN/SPAWN target 匹配 + 递归 walk；CALL 只扫 args）；seed `#ifndef FROM_X`；FILE\* emit 仍 seed。surface re-pin。mac+Ubuntu L2 prove IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                                    |
| 2026-07-21 | **`b06cf472`**                                          | **R2 async\_liveness Cap residual pure（type/layout）**：`.x` 真迁 `lookup_var_type` / `type_to_c_buf` / `type_size` / `layout_func*` / `has_await` / `needs_cps_frame` / `analyze_func` / `module_struct_in_frame` + store/sort helpers；宿主 AST + AsyncFrameLayout LE；heap layout 4196；seed `#ifndef FROM_X`；FILE\* emit 仍 seed。surface re-pin。mac+Ubuntu L2 prove IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                           |
| 2026-07-21 | **`ec2336da`**                                          | **R2 async\_cps\_codegen Cap residual pure wave2**：`.x` 真迁 `func_uses_void_entry` / `module_references_run_async` / `resolve_sched_target` / `module_has_sched_extern` + cstr/module helpers；宿主 ASTFunc/ASTModule LE 偏移；seed `#ifndef FROM_X`；surface re-pin。Cap residual 余：FILE\* emit + walk `_impl`。mac+Ubuntu L2 prove IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                              |
| 2026-07-21 | **`8d1f2452`**                                          | **R2 async\_cps\_codegen Cap residual pure wave1**：`.x` 真迁 `expr_is_io_await` / `await_read` / `await_write` / `await_fd` / `future_wait` + LE load/name helpers；seed `#ifndef FROM_X`；surface re-pin。Cap residual 余：FILE\* emit + walk `_impl` + module/sched。mac+Ubuntu L2 prove IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                                                                      |
| 2026-07-21 | **`d194aff5`**                                          | **R2 async\_cps\_codegen pure surface**：`.x` 吃满 io/future\_wait/sched name gates + thin walk/hoist；修 `(e,target)` ABI；`#[no_mangle]` + 英文 G.9。FROM\_X 省略 pure C（`slice_marker`）；walk `_impl` + FILE\* emit Cap residual 仍 seed。surface + prove MODULES。mac+Ubuntu L2 prove IDENTICAL（Ubuntu L3 后 -E 重试）。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                                                           |
| 2026-07-21 | **`b12bf000`**                                          | **prove harness**：`-E` 最多 5 次重试（Ubuntu 主机偶发 SIGSEGV，含小模块）；IDENTICAL 仍硬。PLATFORM: SHARED harness                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-07-21 | **`f2ca6580`**                                          | **R2 async\_liveness pure surface**：`.x` 吃满 await walk/live/mangle/`frame_build_tag`；FROM\_X 省略 pure C（`slice_marker`）；Cap residual layout/type/emit FILE\* 仍 seed。`analyze_block_linear` `malloc(4096)` 非 `u8[4096]`。surface + prove MODULES。mac+Ubuntu L2 prove 5/5（`b12bf000` 重试后）。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                                                                             |
| 2026-07-21 | **`2a88053b`**                                          | **R2 async\_asm\_pool**：`build_layout` 真迁 .x；FROM\_X rest 仅 `slice_marker`（H=0）；cold seed 全 C 仍供 `pipeline_glue` #include。surface + prove MODULES。Ubuntu 初版 `u8[4096]` 栈 → -E SIGSEGV 假红，改 let-index 缓存后 5/5 IDENTICAL。mac+Ubuntu L2 prove。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                                                                                                                        |
| 2026-07-21 | **`f57e23df`**                                          | R2 async\_asm\_pool 初版（后由 `2a88053b` 栈修收口）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-21 | **`157f1dc1`**                                          | **P1 preprocess**：末行无 LF 时刷 `line_buf`（`preprocess_x`/`preprocess_x_buf` + seed）。历史症状 num\_funcs=0/XP003 被归为 parser EOF；真产生点=预处理丢最终 `}`。mac L2：no-NL/oneline/import 探针 `-o` rv=42 + 矩阵。PLATFORM: SHARED · **日常 L2 非 tip L4**                                                                                                                                                                                                                                                                                                            |
| 2026-07-21 | **`fda84704`**                                          | **P1 codegen**：`codegen_is_libc_conflicting_extern_name` 补全 getenv/malloc/…（跳过再声明，权威单一；seed 同 commit）。根因=SHUX 类型复述 libc 与系统头冲突，非 `*u8` 发不成指针。PLATFORM: SHARED                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-07-21 | **升钉** **`11a57351`**                                   | Ubuntu L4：`run-all-bstrict OK (129 scripts, compiler/shux is shux_asm)` · `/tmp/ubuntu_true_bstrict_11a57351.log`；mac 已 129 · `/tmp/mac_bstrict_d45ee9a4_p0.log` → **钉盘** **`deaf773b`** **→** **`11a57351`**                                                                                                                                                                                                                                                                                                                           |
| 2026-07-21 | **docs**                                                | 20 号全日归档 → `自举进度-归档-2026-07-20.md`；21 日起表；双端绿后写升钉                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-07-21 | **`11a57351`**                                          | **harness P0 + 地图**：run-json Darwin heavy 15s；lang-unsafe 用 `SHUX_BIN`；`当前问题分析.md`。mac 129 OK。PLATFORM: MACOS harness + SHARED                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-07-21 | **`d45ee9a4`**                                          | **产品根修**：net-context 弱符号 + std.context link\_only；user `.o` 注入；C-style cast。PLATFORM: SHARED                                                                                                                                                                                                                                                                                                                                                                                                                                            |

***

## 7. 禁止清单（本阶段）

- [ ] 未用 20 号归档 KPI 当 tip 仍绿
- [ ] 未把 best-effort / W3 fast 当产品放行
- [ ] 未把 mac 129 单独当双端升钉
- [ ] 未在 Ubuntu bstrict 未收口时宣称 tip L4 全绿
- [ ] 未在产品轨未绿时开 R2 庆祝
- [ ] 未 seed / `.x` 只改一边就关 P1

