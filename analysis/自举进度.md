# Xlang / X language 自举进度

> **更新**：2026-09-03 · tip **🔔 升钉 `e8176cbe5` → `b5be5ed97`（双端 L4 129）** · 日切：2 日 **52** 行已入 [归档](自举进度-归档-2026-09-02.md) · **金标 SSH＝`ubuntu-server`** · Ubuntu 验＝**git pull（禁 scp）** · Ubuntu L2 **钉盘节奏**\
> **产品 L4 放行钉盘**：**`b5be5ed97`**（2026-09-02 升钉 · 前 `e8176cbe5` 2026-08-23 · 再前 `f7424ae47` 2026-08-15）\
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅**（12 labi_*.x 切片 prefer；默认 FROM_X=1）· parser **7.2.2 关 pin ✅**（tip `-E` + assemble；产品默认 FROM_X=1）· codegen 7.4.2 wave323 ✅ · typeck 7.4.1 wave322 ✅ · runtime monofile wave321 ✅ · present **0** · 钉盘 **`b5be5ed97`** · Stage 8 tip **wave332 Track L 30/30 FULLY CLOSED**（**PRODUCT HALF=0** · **PRODUCT PINNED=0** · **PRODUCT RETIRED 23/23 = 100%**）· **7.4.3 PREFER_X_O 双端 8/8 wave338 ✅** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁**\
> **tip L4 安全网（历史）**：**`ffc0b646f`**（已由 tip L4 升钉吸收；前网 `4bacaf18c`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：双端 L4＠`b5be5ed97` **`product_l4_true_cold OK` 129**（Ubuntu 17m47s／Darwin JOBS=1 137m25s）。pin 蛋自本波 `xlang_asm` 刷新。Darwin L4 默认 JOBS=1＋GATE_CASE_JOBS=1（JOBS=8／4／2 本机 OOM）。
> **下波候选**：Windows hybrid last harvest unique **48**＠`7172e057a`（public preprocess）。Mac 已编 leftover unique 至 debug_trace／prepare_entry（WIN rest T=**946**）**未** Windows 收割（金标仍 Ubuntu）。本波 **P2 Darwin `-o` prerun skip throwaway import-map parse**：`parse_only` 不消费 ctx，跳过 `map_impl` tmp 全量解析。hello 指令 32.5e9→**21.8e9**／墙 **1.46s→0.96s**。`glue_asm_resolve` 仍闭。pabi leftover unique 编码已抽尽。P2 Darwin `-o` nm-per-probe 三刀＋sidecar 2-slot MRU＋binop peek 已闭。

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1；过程上 prefer+heat 一次／域，双端矩阵同波一次\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；**SHARED 双端 L2**；日常矩阵 **只** `./xbuild l2-matrix`（wave299：捕获 rv=42／opt=102 成功码，禁 set -e 截断）；谈自举／升钉才 L4 + 矩阵 + 全量 bstrict；bstrict **per-script timeout** Ubuntu 300s／Darwin L4 1200s + catalog cache；**墙钟**：`product_l4` 分步 wall + `run-all-bstrict`（`XLANG_BSTRICT_TIMING_FILE`）；并行 **`XLANG_BSTRICT_JOBS` 默认 1**（Ubuntu 金标 1；Darwin L4 默认 1／GATE_CASE_JOBS=1；JOBS=8／4／2 本机 OOM）

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[02](自举进度-归档-2026-09-02.md) · [01](自举进度-归档-2026-09-01.md) · [31](自举进度-归档-2026-08-31.md) · [30](自举进度-归档-2026-08-30.md) · [29](自举进度-归档-2026-08-29.md) · [28](自举进度-归档-2026-08-28.md) · [27](自举进度-归档-2026-08-27.md) · [26](自举进度-归档-2026-08-26.md) · [25](自举进度-归档-2026-08-25.md) · [24](自举进度-归档-2026-08-24.md) · [23](自举进度-归档-2026-08-23.md) · [22](自举进度-归档-2026-08-22.md) · [21](自举进度-归档-2026-08-21.md) · [20](自举进度-归档-2026-08-20.md) · [19](自举进度-归档-2026-08-19.md) · [18](自举进度-归档-2026-08-18.md) · [17](自举进度-归档-2026-08-17.md) · [16](自举进度-归档-2026-08-16.md) · [15](自举进度-归档-2026-08-15.md) · [14](自举进度-归档-2026-08-14.md) · [13](自举进度-归档-2026-08-13.md) · [12](自举进度-归档-2026-08-12.md) · [11](自举进度-归档-2026-08-11.md) · [09](自举进度-归档-2026-08-09.md) · [08](自举进度-归档-2026-08-08.md) · [07](自举进度-归档-2026-08-07.md) · [06](自举进度-归档-2026-08-06.md) · [05](自举进度-归档-2026-08-05.md) · [04](自举进度-归档-2026-08-04.md) · [03](自举进度-归档-2026-08-03.md) · [02](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · …\

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `b5be5ed97`**（2026-09-02 升钉 · 前 `e8176cbe5` 2026-08-23 · 再前 `f7424ae47` 2026-08-15）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**双端 Stage2 = ✅ SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709` · `xlang_asm_stage1`／`xlang_asm2` bit-identical）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（**变更记录只在本文 §6**）。\
**本波主刀 = P2 Darwin `-o` prerun skip throwaway import-map parse（`parse_only` 不消费 ctx 则跳过 `map_impl` tmp 全量解析；Windows leftover unique 已编待收割；钉盘仍 `b5be5ed97`）**。

| 指标 | 状态 |
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`b5be5ed97`**；residual tip present **0**；Stage 8 **30/30 FULLY CLOSED**；PRODUCT RETIRED 23/23 |
| Track MG | **11.3.1 MG ✅** |
| Track BC | **🟢 Stage8 30/30** · M4 **5/5** · present **0**；`pipeline_x` mega 仍 host-cc |
| Track L | **收 30/30 FULLY CLOSED** |
| mac / Ubuntu L4（钉盘） | ✅ 双端 L4 + bstrict **129** @ **`b5be5ed97`** |
| pin 蛋 | ✅ darwin.arm64 + linux.x86_64（本波 `xlang_asm`） |
| 本波构建级别 | 钉盘 **L4 已结＠b5be5ed97**；Ubuntu **129 OK** 17m47s；Darwin JOBS=1 **129 OK** 137m25s。check 闸门暂停 |

***

## 1. 文档地图

| 文档 | 看什么 |
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录**（**9 月 3 日**） |
| [自举进度-归档-2026-09-02.md](自举进度-归档-2026-09-02.md) | **9月2日** 升钉 `b5be5ed97`／Darwin L4 JOBS／Windows leftover-PE hybrid unique 113→50（**52** 行） |
| [自举进度-归档-2026-09-01.md](自举进度-归档-2026-09-01.md) | **9月1日** typeck pin 7.4.6–8／L4 多轮／Cap va／stage10（**131** 行） |
| [自举进度-归档-2026-08-31.md](自举进度-归档-2026-08-31.md) | **8月31日** stage10 开刀／seed dest-SLICE／leftover／假权威诚实等（**113** 行） |
| [自举进度-归档-2026-08-30.md](自举进度-归档-2026-08-30.md) | **8月30日** leftover XLANG fallthrough F-06／F-07 等（**111** 行） |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图（本波不改） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单；per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`b5be5ed97`** | 2026-09-02 升钉；双端 L4 真冷 + 129 |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自钉盘 L4 `xlang_asm` 刷新 |
| residual tip（BC） | present **0** | Stage 8 30/30 · M4 5/5 · prefer pure-asm 默认 · pipeline_abi mega **硬禁** |
| mac 钉盘 L4 | ✅ L4 + bstrict **129** · 137m25s JOBS=1 | `@b5be5ed97` `/tmp/xlang_l4_Darwin_b5be5ed97_jobs1.log` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 + bstrict **129** · 17m47s | `@b5be5ed97` `ubuntu-server` `/tmp/xlang_l4_Linux_b5be5ed97.log` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | rv42／opt102／hello0／si0／f32 |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ 双端 tip SHA256 STRICT match | 钉盘历史见归档 |
| **R2 / Cap** | 按需 | 明细见 29／31／1 日归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1** |
| `TREE_ARMED` / `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1** / **1** |
| post-delete residual | **✅** wave944–964 |

***

## 3. 当前前排（≤3 条）

1. **Windows hybrid 门** — 已切回 Ubuntu。last harvest＠`7172e057a` unique **48**；Mac 已编 `lsp_free`＋collect＋dep_prerun／run_x＋elf_o＋glue_type size／align＋rec＋append_reloc＋sret＋`pipeline_run_x_pipeline_impl`＋modlet 四 unique＋array_lit unique＋stack_off unique＋glue_func_return unique＋named_layout unique＋glue_call_return unique＋type_alias storage unique＋enum storage unique＋top_level_let storage unique＋struct_layout storage unique＋block_diverged leftover unique＋driver_emit leftover unique＋path_cap WINDOWS＋dir_cap WINDOWS＋typeck／codegen pin leftover-PE host-cc＋debug_trace／prepare_entry leftover unique（WIN rest T=869→881→892→896→900→905→906→912→913→920→922→923→924→925→926→928→930→932→934→936→942→**946**），闸门未收割。leftover PE `-E` 挂死已闭。
2. **邻域禁区** — **禁** mega prefer／默认 assemble parser.x；**禁** `rm` hybrid egg；**禁** nest>64；**禁**改 al06／print_any／shuffle／io-context／fs／channel／net-context 期望糊绿；pipeline_abi mega **硬禁**。
3. **下一刀** — Windows 收割已编 leftover unique（期望 unique **48→0** 若 leftover PE pin cc 过且 SAT link_abi 全 seed 过；pin cc 失败则 **48→7**／**8**）；金标现 Ubuntu。pabi leftover unique 编码已抽尽；`glue_asm_resolve` 仍闭（非 unique，seed stub）。P2 Darwin `-o` UNDEF＋defined T／t＋compress substring nm-per-probe 已闭（hello 17s→1.6s；`nm` 11→**0**）。sidecar 2-slot MRU 已闭。binop 1-token peek cache 已闭。prerun skip throwaway import-map parse 已闭（hello **0.96s**／指令 32.5e9→**21.8e9**）。

***

## 4. 与归档的关系

- **9月2日归档**：[自举进度-归档-2026-09-02.md](自举进度-归档-2026-09-02.md) — **52** 行 · 钉盘 **`b5be5ed97`**。
- **9月1日及前**：见各日归档链接（§1）。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `b5be5ed97`**。
- 日切后主文档只保留 KPI + 前排 + **9 月 3 日** §6；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
| **macOS arm64** | L4＠**`b5be5ed97` JOBS=1** **`product_l4_true_cold OK` 129** wall **137m25s**（bstrict 130m27s；hello／fs／channel／STD-092 **OK**）／矩阵 **5／5** · 钉盘 **`b5be5ed97`** |
| **Ubuntu x86_64（金标）** | L4＠**`b5be5ed97`** **`product_l4_true_cold OK` 129** wall **17m47s** · SSH＝**`ubuntu-server`** · 钉盘 **`b5be5ed97`** |
| **Windows MSYS2** | 已切回 Ubuntu · last harvest＠`7172e057a` unique **48** · Mac 已编 `lsp_free`＋collect＋dep_prerun／run_x＋elf_o＋glue_type size／align＋rec＋append_reloc＋sret＋impl＋modlet 四 unique＋array_lit unique＋stack_off unique＋glue_func_return unique＋named_layout unique＋glue_call_return unique＋type_alias storage unique＋enum storage unique＋top_level_let storage unique＋struct_layout storage unique＋block_diverged leftover unique＋driver_emit leftover unique＋path_cap WINDOWS＋dir_cap WINDOWS＋typeck／codegen pin leftover-PE host-cc＋debug_trace／prepare_entry leftover unique（WIN rest T=**946**）未收割 · 10.1.3 NT **暂缓** |

***

## 6. 变更记录（倒序 · **9 月 3 日**；2 日见 [归档](自举进度-归档-2026-09-02.md)）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-03 | **perf(selfhost)** **P2 Darwin `-o` prerun：parse_only 跳过 throwaway import-map 全量解析** | **代码＋文档** · **不升钉** · Darwin L2。五问：产生＝`xlang_pipeline_one_ctx_for_dep_prerun_map_impl` 为扫 import 把 dep 源全量 parse 进 tmp arena 随即释放；存储＝扔掉的 tmp 模块；消费＝仅 `parse_skip_typeck`／`for_asm_module_o` 的 ctx。hello `-o` 后续是 `parse_only`（不接 ctx）。同模式＝`rt_run_x_emit`／`rt_run_compiler_parsed`（本刀不搅，非产品 `-o` 热路径）。**修**：G.7 补全既有 `rt_ab_step_prerun`：仅当下一刀消费 ctx 才 fill＋map；seed／surface 同 commit。**证**：hello `-o` **0.96s** Hello World 0；指令 32.5e9→**21.8e9**；sample prerun 只剩 `parse_only`（`one_ctx` 消失）；`./xbuild l2-matrix` **5／5**。禁改测试期望。PLATFORM: SHARED。下一刀：Windows 收割已编 leftover unique（期望 48→0／7／8）。 |
| 2026-09-03 | **perf(selfhost)** **P2 Darwin `-o` binop 1-token peek cache：term…logor 十级** | **代码＋文档** · **不升钉** · Darwin L2。五问：产生＝`parser_asm_binop_single_tok_chain_c` 与 term／addsub／shift／rel／eq 每级对同一未消费 token `lexer_next_into`（hello `-o` sample 最热；sidecar 后仍 33.7e9 指令）；存储＝lexer 无 peek 槽（pos／line／col 无状态）；消费＝每个 expr 解析。同模式＝本 slice 六处 peek-and-restore。**修**：G.7 补全既有链：`__thread` 一槽按 source＋lex pos 缓存；命中运算符才 invalidate；不改 Pratt 语义；不 assemble parser.x。**证**：hello `-o` **1.46s** Hello World 0；指令 33.7e9→**32.5e9**；`./xbuild l2-matrix` **5／5**。禁改测试期望。PLATFORM: SHARED。下一刀：Windows 收割已编 leftover unique（期望 48→0／7／8）。 |
| 2026-09-03 | **perf(selfhost)** **P2 Darwin `-o` sidecar 2-slot MRU：onefunc／module／arena_sidecar_get** | **代码＋文档** · **不升钉** · Darwin L2。五问：产生＝`onefunc_sidecar_get` 每次字段访问线性扫 MAX=1024（hello `-o` sample 第二热，仅次 binop 链）；存储＝wave275 过程表；消费＝`parser_copy_onefunc_into`＋`pipeline_onefunc_*`（src／dst 对打）。同模式＝module／arena MAX=512。**修**：G.7 补全既有 getter：2 槽 MRU（一槽对 copy 对打 miss）；free 丢缓存槽。seed leftover-PE twin 同 commit。**证**：hello `-o` **1.53s** Hello World 0；指令 34.7e9→**33.7e9**；`./xbuild l2-matrix` **5／5**。剩热＝`parser_asm_binop_single_tok_chain_c`。禁改测试期望。PLATFORM: SHARED。下一刀：Windows 收割已编 leftover unique（期望 48→0／7／8）。 |
| 2026-09-03 | **perf(selfhost)** **P2 Darwin `-o` compress sibling：all-names＋UNDEF 子串与 UNDEF／T／t 同槽一次 mmap** | **代码＋文档** · **不升钉** · Darwin L2。五问：产生＝`link_abi_obj_exports_marker_impl`／`link_abi_obj_has_undef_sym_impl` 每个 compress marker／UNDEF 前缀 `popen("nm")` 子串（UNDEF＋T／t 缓存后 hello `-o` 仍 11 次全表 nm 打同一 user.o）；存储＝zlib／zstd／brotli on_demand 入链门＋TLS marker；消费＝每个产品 `-o`。同模式＝needs_undef／has_defined 已缓存（本刀补 substring，不开第二探针）。**修**：G.7 补全既有 `_impl`：同槽一次 Mach-O／ELF 扫描填 all-names（marker 是 d／s／b 不是 T／t）＋UNDEF 子串（Darwin 复原一个前导 `_` 以命中 `_ZSTD`／`_compress2`；Linux 不发明 `_`）。失败才一次 nm。**证**：hello `-o` **1.62s** Hello World 0；PATH nm wrap **11→0**；`./xbuild l2-matrix` **5／5**；`tests/run-compress.sh` OK。禁改测试期望。PLATFORM: SHARED residual／DARWIN Mach-O／LINUX ELF。下一刀：Windows 收割已编 leftover unique（期望 48→0／7／8）。 |
| 2026-09-03 | **perf(selfhost)** **P2 Darwin `-o` sibling：defined T／t 与 UNDEF 同槽一次 mmap** | **代码＋文档** · **不升钉** · Darwin L2。五问：产生＝`xlang_link_obj_has_defined_sym_impl` 每个 provides／ensure 符号 `popen("nm")` T／t（UNDEF 缓存后 hello `-o` 仍 11 次全表 nm 打同一 user.o）；存储＝skip hard-link mem.o／heap.o；消费＝每个产品 `-o`。同模式＝`has_undef_sym_impl`／`exports_marker_impl` 仍 substring nm（本刀不搅）。**修**：G.7 补全既有 `_impl`：与 UNDEF 同槽一次 Mach-O／ELF 扫描填 T／t（Darwin `__text`／指令节；ELF STT_FUNC 非 weak）；失败才一次 nm T／t。heap.o T／t 扫描＝nm **250／250**。不开第二探针。**证**：hello `-o` **1.72s** Hello World 0；`./xbuild l2-matrix` **5／5**；run-hello 2.13s。禁改测试期望。PLATFORM: SHARED residual／DARWIN Mach-O／LINUX ELF。下一刀：Windows 收割已编 leftover unique（期望 48→0／7／8）。 |
| 2026-09-03 | **perf(selfhost)** **P2 Darwin `-o`：UNDEF 探针一槽缓存＋Mach-O／ELF 扫描** | **代码＋文档** · **不升钉** · Darwin L2。五问：产生＝`xlang_link_obj_needs_undef_sym_impl` 每个 on_demand 符号 `popen("nm -u")`（hello `-o` sample 100% posix_spawn＋read）；存储＝on_demand std `.o` 入链门；消费＝每个产品 `-o`（assign-index-expr 45 案 ×～16s＝712s）。同模式＝`has_defined_sym_impl` 仍 nm（本刀不搅；下刀 sibling 已补）。**修**：G.7 补全既有 `_impl`：一槽 per-path UNDEF 名缓存；Darwin `nlist_64`／Linux ELF64 一次扫描，失败才一次 nm `-u`。不开第二探针。**证**：hello `-o` 17.08s→**1.90s**（page-reclaim 2.3M→38k）run Hello World 0；assign-index-expr **712s→18.8s** run=45；cfg-merge **658s→16.7s** run=41；`./xbuild l2-matrix` **5／5** 4.9s。禁改测试期望。PLATFORM: SHARED residual／DARWIN Mach-O／LINUX ELF。下一刀：Windows 收割已编 leftover unique（期望 48→0／7／8）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 debug_trace／prepare_entry leftover unique** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略独立 ifndef 里的 `pipeline_debug_trace_named_func_bodies`／`xlang_driver_asm_prepare_entry_elf_emit`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面。先前仍闭理由＝void* 定义 vs leftover rest struct* extern。**核**：struct* extern＠18600 在 FALSE `#ifndef FROM_X` wave144／145 nest（leftover rest 不解析）；leftover rest FROM_X 原型＠49851 是 void*（与 cold twin 同签）；leftover rest WAVE `parse_into_buf_impl_c` ALWAYS 调 wrapper＠49986。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 match＋impl＋wrapper（rest wrapper 调 rest impl）＋独立 prepare_entry（调 leftover rest T debug_trace）。邻 `pipeline_asm_debug_enabled`／mega_pre_reset／`product_emit`／`thread_fn_ptr` 不转（非 unique）。`glue_asm_resolve` 仍闭（非 unique，seed stub）。pabi leftover unique 编码已抽尽。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（wrapper POSIX **U**，`.x` thin 提供 T；prepare_entry ABSENT）；WIN rc=0 T=**946**／336328B（942→946＝+4 簇 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割全部已编 leftover unique（期望 48→0 若 pin cc＋SAT link_abi 过；pin cc 失败则 48→7／8）。 |
| 2026-09-03 | **fix(windows)** **leftover PE host-cc 当前 typeck／codegen pin（unique T）** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝leftover PE 不能 `-E` typeck.x／codegen.x，gitignored 7／31 gen 仍过旧子串 contract，`need_rebuild` 见旧 `.o` 更新而跳过 Track L；考古 pin HAS unique 体。存储＝`typeck_x.o`／`codegen_x.o`；消费＝phase1 unique harvest（6 typeck＋1 codegen）。同模式＝lexer 已走 `refresh_gen_from_seed_if_stale`；codegen `want_ensure_gen` 只在 gen==seed 才重入（7／31 漂移永不刷新）。**修**：G.7 补 `typeck_gen_contract_ok`／`codegen_gen_contract_ok` unique **defn**（`^int32_t name(`，拒 extern 假绿）；assemble fail 走既有 refresh；codegen want_ensure 加 seed-newer＋unique defn；leftover PE `.o` 缺 unique T 则强制 pin rebuild，MinGW cc 失败 **restore 旧 `.o`**（不挡 pabi harvest）。不开第二套 typeck 体。**证**：Mac `sh -n` 两脚本；pin unique defn 7／7；Darwin 产品 `.o` unique T；host-cc pin `/tmp/typeck_pin.o` rc=0／354960B 六面 T，`/tmp/codegen_pin.o` rc=0／478496B `codegen_builtin_type_name_into` T；本地 gen 仍过新 contract（POSIX 不 thrash）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE pin host-cc／POSIX `-E` assemble 不变。下一刀：Windows 收割 pabi leftover unique＋path_cap＋dir_cap＋typeck／codegen pin（期望 48→2 若 pin cc＋SAT link_abi 过；pin cc 失败则 48→9／10）。 |
| 2026-09-03 | **fix(windows)** **dir_cap WINDOWS 补 MinGW _findfirst／_findnext／_findclose** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝`xlang_dir_cap.h` 整文件 `#if !defined(_WIN32)`，SAT 全 seed 编 `fmt_check_cmd.from_x.c` 时 `walk_dir_collect_impl` 调未声明 `xlang_dir_open`；hybrid rest `xlang_fmt_closedir` 把 seed 第二套 void `closedir_win` 转 int32_t（MinGW 非法）。存储＝本头 inline；消费＝`xlang_fmt_{opendir,closedir,readdir_name}`＋cold `walk_dir_collect_impl`。同模式＝path_cap 已补；seed 私有 `_findfirst`＝双权威须收敛。**修**：G.7 补全既有 dir_cap 权威，MinGW `_findfirst`／`_findnext`／`_findclose`（close 返 0／-1）；fmt seed 删 opendir_win／readdir_win／void closedir_win，四面无条件走 `xlang_dir_*`。不开第三套；`std/fs/win32.x` FindFirstFileA 仍是产品用户 fs（不同层）；pabi sibling-dir 仍跳 Windows（该层产品语义，不顺手改）；runtime_dir_cap.from_x.c 仍 `!_WIN32`（posix.x 面）。pabi leftover unique 编码已抽尽（除两枚仍闭）。**证**：Mac smoke preprocessor depth 0；tiny POSIX TU rc=0；Darwin `-E` 走 libc `opendir`／`closedir`（Windows 分支未解析）；runtime_dir_cap T 四面；fmt FROM_X rest rc=0／9976B／`xlang_fmt_*` T；fmt 全 seed rc=0／43080B／`walk_dir_collect_impl` T。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE SAT／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc＋sret＋impl＋modlet＋array_lit＋stack_off＋glue_func_return＋named_layout＋glue_call_return＋type_alias＋enum＋tl＋sl＋block_diverged＋driver_emit＋path_cap＋dir_cap（期望 48→9，若 SAT link_abi 全 seed 过则 capture_opt_level 亦落）。 |
| 2026-09-03 | **fix(windows)** **path_cap WINDOWS 补 MinGW access／stat／fstat／realpath** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝`xlang_path_cap.h` 整文件 `#if !defined(_WIN32)`，SAT 全 seed 编 `runtime_link_abi.from_x.c` 时 `xlang_path_access`／`xlang_path_stat` 未声明（POSIX 由 Linux syscall／Darwin libc 提供）；leftover PE 不能编当前 link_abi，留下 7／31 `.o`，`xlang_link_capture_opt_level_from_argv` unique。存储＝本头 inline；消费＝`path_readable_impl`／`path_executable_impl`／`nonempty_regular_file_impl`（unconditional）＋phase1 `rt_run_asm_backend`。同模式＝`xlang_dir_cap.h` 仍 WINDOWS 侧（本刀不搅）。**修**：G.7 补全既有 path_cap 权威，MinGW `_access`／`stat`／`fstat`／`_fullpath`；不开 seed 第二套 path 探针；`link_abi_realpath_cap_impl` Windows 仍 NULL（该层产品语义，不顺手改）。pabi leftover unique 编码已抽尽（除两枚仍闭）；prepare_entry 仍闭（调 debug_trace）；`glue_asm_resolve` 仍闭（非 unique，seed stub）；debug_trace 仍闭（void* vs struct*）。**证**：Mac smoke POSIX full-seed link_abi rc=0／397280B／T=537／`xlang_link_capture_opt_level_from_argv` T；Darwin `-E` 走 libc `access`／`stat`（Windows 分支未解析）；preprocessor depth 0。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE SAT／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc＋sret＋impl＋modlet＋array_lit＋stack_off＋glue_func_return＋named_layout＋glue_call_return＋type_alias＋enum＋tl＋sl＋block_diverged＋driver_emit＋path_cap（期望 48→9，若 SAT link_abi 全 seed 过则 capture_opt_level 亦落）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 driver_emit leftover unique** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略嵌在 wave101 里的 wave104 `driver_emit_lib_root_release`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 已 T reset／append（unique 只列 release）。存储＝`src/runtime_pipeline_abi.o` BSS `wave104_emit_*`；消费＝phase1 57 objs（`rt_compile` 调 release）。只抽 release＋BSS 会把 sidecar 拆到两 TU（standalone append 写 standalone BSS，rest release 写 rest BSS）。**修**：先关 wave101（lsp_diag 后），再 G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 unique＋同 BSS 兄弟 reset／append／count／len／copy＋find（rest-first merge 赢 reset／append 重叠，leftover rest SET／GET／release 共 leftover rest BSS），OR 后 reopen wave101。邻 `asm_qual_sym_layer_*` 不转（非 unique，独立 BSS 族）。prepare_entry 仍闭（调 debug_trace）；`glue_asm_resolve` 仍闭（非 unique，seed stub）；debug_trace 仍闭（void* vs struct*）。pabi leftover unique 编码已抽尽（除两枚仍闭）。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（六面 ABSENT，`.x` thin 提供 T）；WIN rc=0 T=**942**／333608B（936→942＝+6 簇 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc＋sret＋impl＋modlet＋array_lit＋stack_off＋glue_func_return＋named_layout＋glue_call_return＋type_alias＋enum＋tl＋sl＋block_diverged＋driver_emit（期望 48→10）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 block_diverged leftover unique** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略 `glue_asm_block_diverged_set`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；seed wave213 簇从未有 slice9 格（无 seed twin）；leftover standalone 已 T getter（unique 只列 setter）。存储＝`src/runtime_pipeline_abi.o` BSS `g_wave213_block_diverged`；消费＝phase1 57 objs（`backend_call_dispatch` 调 setter）。**修**：独立 ifndef（lsp_free 后，不嵌 wave154／178）G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 setter＋getter＋BSS（rest-first merge 赢 getter 重叠，leftover rest SET／GET 共 leftover rest BSS）；体按 `.x`＠77841／＠77856。不把面加进 wave213 簇（cold full seed 编本 OR，同 TU 再定义会 dual-def）。邻 wave213 live-control 不转（非 unique，FALSE FROM_X nest）。prepare_entry 仍闭（调 debug_trace）；`driver_emit_lib_root_release` 仍闭（sidecar BSS）；`glue_asm_resolve` 仍闭（非 unique，seed stub）；debug_trace 仍闭（void* vs struct*）。pabi leftover unique 编码已抽尽（除三枚仍闭）。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（SET／GET ABSENT，`.x` thin 提供 T）；WIN rc=0 T=**936**／330936B（934→936＝+2 簇 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc＋sret＋impl＋modlet＋array_lit＋stack_off＋glue_func_return＋named_layout＋glue_call_return＋type_alias＋enum＋tl＋sl＋block_diverged（期望 48→11）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 struct_layout storage unique** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略嵌在 wave154／wave178 里的 wave266 `pipeline_module_struct_layout_storage_reset`／`storage_release`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面；seed 体是真逻辑（find_slot＋清 n／fn／tpn／free 三表＋header num_struct_layouts＠16），非 stub。存储＝`src/runtime_pipeline_abi.o`；消费＝leftover rest WAVE279 `ast_pool_module_reset`／`release`（ALWAYS 编，调这两枚）＋phase1 57 objs。**修**：先关 wave178＋wave154（tl 后重开，remaining wave265 后），再 G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 unique＋BSS＋find_slot＋header_n／set_header_n，OR 后 reopen 两层。邻 wave266 alloc／set／getters 不转（非 unique）；remaining wave265 alloc 不转（非 unique）。C 用直接 static BSS（leftover rest 无 `xlang_ptr_slot_*`）。prepare_entry 仍闭（调 debug_trace）；`driver_emit_lib_root_release` 仍闭（sidecar BSS）；`glue_asm_block_diverged_set` 仍闭（无 seed twin，与 getter 共 BSS）。pabi leftover unique 真 seed 体已抽尽。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（sl unique 非 T，WAVE279 leftover rest U，`.x` thin 提供 T）；WIN rc=0 T=**934**／330640B（932→934＝+2 簇 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc＋sret＋impl＋modlet＋array_lit＋stack_off＋glue_func_return＋named_layout＋glue_call_return＋type_alias＋enum＋tl＋sl（期望 48→12）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 top_level_let storage unique** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略嵌在 wave154／wave178 里的 wave265 `pipeline_module_top_level_let_storage_reset`／`storage_release`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面；seed 体是真逻辑（find_slot＋清 n／free 表＋header num_top_level_lets＠12），非 stub。存储＝`src/runtime_pipeline_abi.o`；消费＝leftover rest WAVE279 `ast_pool_module_reset`／`release`（ALWAYS 编，调这两枚）＋phase1 57 objs。**修**：先关 wave178＋wave154（enum 后重开，remaining wave264 后），再 G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 unique＋BSS＋find_slot＋header_n／set_header_n，OR 后 reopen 两层。邻 wave265 alloc／set／getters 不转（非 unique）；remaining wave264 alloc 不转（非 unique）；sl unique 仍闭（同 sidecar 下刀）。C 用直接 static BSS（leftover rest 无 `xlang_ptr_slot_*`）。prepare_entry 仍闭（调 debug_trace）；`driver_emit_lib_root_release` 仍闭（sidecar BSS）；`glue_asm_block_diverged_set` 仍闭（无 seed twin，与 getter 共 BSS）。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（tl unique 非 T，WAVE279 leftover rest U，`.x` thin 提供 T）；WIN rc=0 T=**932**／328904B（930→932＝+2 簇 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc＋sret＋impl＋modlet＋array_lit＋stack_off＋glue_func_return＋named_layout＋glue_call_return＋type_alias＋enum＋tl（期望 48→14）；金标 Ubuntu 时下一独立刀＝struct_layout storage unique。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 enum storage unique** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略嵌在 wave154／wave178 里的 wave264 `pipeline_module_enum_storage_reset`／`storage_release`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面；seed 体是真逻辑（find_slot＋清 n／free 表＋header num_module_enums＠64），非 stub。存储＝`src/runtime_pipeline_abi.o`；消费＝leftover rest WAVE279 `ast_pool_module_reset`／`release`（ALWAYS 编，调这两枚）＋phase1 57 objs。**修**：先关 wave178＋wave154（type_alias 后重开，wave263 后），再 G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 unique＋BSS＋find_slot＋header_n／set_header_n，OR 后 reopen 两层。邻 wave264 alloc／set／getters 不转（非 unique）；wave263 import 不转（非 unique）；tl／sl unique 仍闭（同 sidecar 下刀）。C 用直接 static BSS（leftover rest 无 `xlang_ptr_slot_*`）。prepare_entry 仍闭（调 debug_trace）；`driver_emit_lib_root_release` 仍闭（sidecar BSS）；`glue_asm_block_diverged_set` 仍闭（无 seed twin，与 getter 共 BSS）。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（enum unique ABSENT）；WIN rc=0 T=**930**／327800B（928→930＝+2 簇 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc＋sret＋impl＋modlet＋array_lit＋stack_off＋glue_func_return＋named_layout＋glue_call_return＋type_alias＋enum（期望 48→16）；金标 Ubuntu 时下一独立刀＝top_level_let storage unique。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 type_alias storage unique** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略嵌在 wave154／wave178 里的 wave262 `pipeline_module_type_alias_storage_reset`／`storage_release`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面；seed 体是真逻辑（find_slot＋清 n／free 表），非 stub。存储＝`src/runtime_pipeline_abi.o`；消费＝leftover rest WAVE279 `ast_pool_module_reset`／`release`（ALWAYS 编，调这两枚）＋phase1 57 objs。**修**：先关 wave178＋wave154（sret 后重开，wave261 后），再 G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 unique＋BSS＋find_slot，OR 后 reopen 两层。邻 wave262 alloc／set／getters 不转（非 unique）；wave224／wave261 不转（非 unique）；enum／tl／sl unique 仍闭（同 sidecar 下刀）。C 用直接 static BSS（leftover rest 无 `xlang_ptr_slot_*`）。prepare_entry 仍闭（调 debug_trace）；`driver_emit_lib_root_release` 仍闭（sidecar BSS）；`glue_asm_block_diverged_set` 仍闭（无 seed twin，与 getter 共 BSS）。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（type_alias unique ABSENT）；WIN rc=0 T=**928**／326704B（926→928＝+2 簇 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc＋sret＋impl＋modlet＋array_lit＋stack_off＋glue_func_return＋named_layout＋glue_call_return＋type_alias（期望 48→18）；金标 Ubuntu 时下一独立刀＝enum storage unique。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 glue_call_return unique** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略嵌在 wave154／wave178 里的 wave194 `glue_call_return_byte_size_c`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面；seed 体是 stub `return -1`，OR stub 会假绿 unique。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs（`backend_call_dispatch`）。**修**：先关 wave178＋wave154（glue_func_return 后重开，load_var 后），再 G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 unique，体按 `.x`＠72875 真逻辑（resolve callee → size return；void=0／TYPE_ARRAY=8／else size_simple；dep_ix≥0 走 typeck dep map），OR 后 reopen 两层。callee `size_simple` leftover rest 已 T；`func_return_type_at` leftover rest 已 T；`kind_ord`／`emit_dep_pipe`／`emit_module_ref` leftover rest U（SAT／leftover standalone 提供 T）；extract 带 `glue_asm_resolve`＋`get_dep_return_type` void* extern（leftover rest 无 twin）。邻 param_agg／load_var 不转（非 unique）；wave195–199 不转（非 unique）；`glue_asm_resolve` 仍闭（非 unique，seed stub）；prepare_entry 仍闭（调 debug_trace）；`driver_emit_lib_root_release` 仍闭（sidecar BSS）。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（glue_call_return unique ABSENT）；WIN rc=0 T=**926**／325848B（925→926＝+1 簇 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc＋sret＋impl＋modlet＋array_lit＋stack_off＋glue_func_return＋named_layout＋glue_call_return（期望 48→20）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 named_layout unique** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略嵌在 wave154／wave178 里的 wave191 `glue_type_named_layout_size_any_module_elf_c`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面；seed 体是 stub `return 0`，OR stub 会假绿 unique。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：先关 wave178＋wave154（glue_type 后重开），再 G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 unique，体按 `.x`＠72308 真逻辑（TYPE_NAMED=8／size_simple>8 信任／else 扫 dep_pipe layout），OR 后 reopen 两层。callee `size_simple` leftover rest 已 T；`typeck_x_type_size_from_layout_glue` leftover rest 已 T（WAVE285 always）；`kind_ord`／`named_name_into`／layout 名／dep_pipe／emit_module leftover rest U（SAT／leftover standalone 提供 T）；`ndep` 用＠2724 struct*，不重 extern void*。邻 pass_addr／dual_gp／param_home_width 不转（非 unique）；`glue_call_return` 仍闭（seed stub）；prepare_entry 仍闭（调 debug_trace）；`driver_emit_lib_root_release` 仍闭（sidecar BSS）。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（named_layout unique ABSENT）；WIN rc=0 T=**925**／325032B（924→925＝+1 簇 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc＋sret＋impl＋modlet＋array_lit＋stack_off＋glue_func_return＋named_layout（期望 48→21）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 glue_func_return unique** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略嵌在 wave154／wave178 里的 wave192 `glue_func_return_byte_size_c`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面；seed 体是 stub `return 0`，OR stub 会假绿 unique。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：先关 wave178＋wave154（glue_type 后重开），再 G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 unique，体按 `.x`＠72650 真逻辑（void=0／TYPE_ARRAY=8／else size_simple），OR 后 reopen 两层。callee `size_simple` leftover rest 已 T；`kind_ord` leftover rest U（SAT／leftover standalone 提供 T）；`num_funcs` leftover rest U（SAT／leftover standalone 提供 T）；`func_return_type_at` leftover rest 已 T。邻 dual_gp／param_home_width 不转（非 unique）；`glue_call_return` 仍闭（seed stub）；named_layout 仍闭（seed stub）；prepare_entry 仍闭（调 debug_trace）；`driver_emit_lib_root_release` 仍闭（sidecar BSS）。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（glue_func_return unique ABSENT）；WIN rc=0 T=**924**／323800B（923→924＝+1 簇 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc＋sret＋impl＋modlet＋array_lit＋stack_off＋glue_func_return（期望 48→22）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 stack_off unique** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略嵌在 wave136 余里的 wave148 `glue_asm_local_var_stack_off_scoped`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs（`backend_call_dispatch`）。**修**：先关 remaining wave136，再 G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 unique，OR 后 reopen remaining wave136。callee `var_name_len`／`into` leftover rest 已 T；`find_offset_scoped` leftover rest 已 T uint8_t*；`find_offset` leftover rest U（SAT／leftover standalone 提供 T）。邻 wave143 emit／empty／force_esz 不转（非 unique）；wave148 vector-lane emit 不转（非 unique）；named_layout 仍闭（seed stub）；`glue_func_return`／`glue_call_return` 仍闭（seed stub）；prepare_entry 仍闭（调 debug_trace）；`driver_emit_lib_root_release` 仍闭（sidecar BSS）。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（stack_off unique ABSENT）；WIN rc=0 T=**923**／323464B（922→923＝+1 簇 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc＋sret＋impl＋modlet＋array_lit＋stack_off（期望 48→23）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 array_lit unique** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略嵌在 wave136 余里的 wave143 `pipeline_asm_array_lit_elem_byte_sz_c`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面；unique 调 helper `pipeline_asm_array_lit_elem_type_ref`，须同刀转。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：先关 remaining wave136，再 G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 helper＋unique，OR 后 reopen remaining wave136。elem_type_ref 非 unique（SAT／leftover standalone 提供 T）。邻 wave140 index 不转（非 unique）；wave143 emit／empty／force_esz 不转（非 unique）；`glue_asm_local_var_stack_off_scoped` 仍闭（嵌 wave136 余）；named_layout 仍闭（seed stub）；prepare_entry 仍闭（调 debug_trace）；`driver_emit_lib_root_release` 仍闭（sidecar BSS）。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（array_lit unique ABSENT）；WIN rc=0 T=**922**／323008B（920→922＝+2 簇全 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc＋sret＋impl＋modlet＋array_lit（期望 48→24）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 modlet 簇** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略嵌在 wave136 里的 wave139 `pipeline_asm_modlet_prepare_and_emit_elf_c`／`seed_nonzero_inits`／`register_module_top_level_lets`／`emit_module_top_level_mutable_lit_inits`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面；四 unique 与 name_is_shared／load／store 共用 `g_pipeline_asm_modlet_cold` BSS，须同刀转。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：先关 wave136，再 G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 wave139 BSS＋helpers＋四 unique，OR 后 reopen wave136。name_is_shared／load／store 非 unique（SAT／leftover standalone 提供 T）。邻 wave140 index 不转（非 unique）；`pipeline_asm_array_lit_elem_byte_sz_c`／`glue_asm_local_var_stack_off_scoped` 仍闭（嵌 wave136 余）；named_layout 仍闭（seed stub）；prepare_entry 仍闭（调 debug_trace）；`driver_emit_lib_root_release` 仍闭（sidecar BSS）。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（modlet 簇 ABSENT）；WIN rc=0 T=**920**／321840B（913→920＝+7 簇全 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc＋sret＋impl＋modlet（期望 48→25）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 `pipeline_run_x_pipeline_impl`** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略嵌在 wave101 里的 `pipeline_run_x_pipeline_impl`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs（driver_emit／runtime_driver）。**修**：先关 wave101，再 G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 impl，OR 后 reopen wave101。callee parse_entry／load_deps／typeck／codegen wrappers 非 unique（WIN rest U，SAT／leftover standalone 提供 T）。邻 `driver_emit_lib_root_release` 不转（嵌 wave101 sidecar BSS）；named_layout 仍闭（seed stub）；prepare_entry 仍闭（调 debug_trace）。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（impl ABSENT）；WIN rc=0 T=**913**／308040B（912→913＝+1 簇 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc＋sret＋impl（期望 48→29）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 sret 簇** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略嵌在 wave154／wave178 里的 `pipeline_asm_emit_ctx_sret_*_set`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面；三 setter 与 getter 共用 BSS，须同刀转。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：先关 wave178＋wave154（glue_type 后重开），再 G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 wave223 BSS＋3 get＋3 set，OR 后 reopen 两层。getter 非 unique（SAT／leftover standalone 提供 T）。邻 wave222 module／dep_pipe 不转（非 unique）；named_layout 仍闭（seed stub）；storage 仍闭（嵌 wave178 sidecar）；prepare_entry 仍闭（调 debug_trace）。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（sret 簇 ABSENT，setter U）；WIN rc=0 T=**912**／306568B（906→912＝+6 簇全 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc＋sret（期望 48→30）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 append_reloc 簇** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略嵌在 wave154／wave178／wave273 重开里的 `pipeline_elf_ctx_append_reloc_absolute64`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs（`backend_call_dispatch`）。**修**：先关 wave273＋wave178＋wave154（F7 后重开），再 G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 absolute64，OR 后 reopen 三层。callee `append_reloc_typed` 非 unique（WIN rest U，SAT／leftover standalone 提供 T）。邻 `append_reloc`／typed／`reloc_sym_name_ptr` 不转（非 unique）；named_layout 仍闭（嵌 wave178）；prepare_entry 仍闭（调 debug_trace）。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（absolute64 ABSENT）；WIN rc=0 T=**906**／305824B（905→906＝+1 簇 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec＋append_reloc（期望 48→33）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 rec 簇** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略嵌在 wave149 里的 inner wave152 `pipeline_asm_emit_expr_elf_rec`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面；rec 调 fast，须同刀转 lit_i32／match_subject_field／elf_c wrapper／fast。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：先关 wave149，再 G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 wave152（不 reopen wave149；wave153 仍独立 ifndef）。fast Cap-fn-ptr 补 FROM_X rest 签名 extern（elem_ref／module_from_ctx／func_index／no_mangle／macho_underscore／lea_sym）。callee 非 unique（emit_binop／emit_call／module_from_ctx 等 WIN rest U，SAT／leftover standalone 提供 T）。邻 wave149 binop 不转；wave153 block_body 不转；`append_reloc_absolute64` 仍闭（嵌 wave273）；named_layout 仍闭（嵌 wave178）；prepare_entry 仍闭（调 debug_trace）。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变（rec 簇 ABSENT）；WIN rc=0 T=**905**／305648B（900→905＝+5 簇全 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type＋rec（期望 48→34）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 glue_type size／align 簇** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略 wave154 起点独立 ifndef 里的 `glue_type_size_simple`／`glue_type_align_simple`（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 切 wave154 起点（size＋align＋helpers empty_struct／layout_metrics／w154_layout_name_eq），OR 后 reopen `#ifndef FROM_X` 给 wave154 其余。callee 签名对齐 always-compiled FROM_X rest（`name_byte_at` uint8_t；ndep 用＠2724 struct*，不重 extern void*）。`glue_vector_type_lanes_esz_c` 不转（非 unique，WIN rest U）。邻 `typeck_typeck_struct_layout_metrics` 不转（非 unique）；`glue_type_named_layout_size_any_module_elf_c` 不转（嵌 wave178）。大块 rec／`append_reloc_absolute64` 仍闭。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变；WIN rc=0 T=**900**／292072B（896→900＝+4 簇全 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o＋glue_type（期望 48→35）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 elf_o 簇** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略独立 `xlang_asm_codegen_elf_o_thread_fn`／`large_stack` ifndef（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面；与 collect／dep_prerun 同：`_impl` 仍 ifndef，须连 wrapper 一起转。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 2 独立 ifndef（thread_fn_impl＋wrapper／large_stack_impl＋wrapper）。thread_fn_impl 调 always-extern `asm_asm_codegen_elf_o`；large_stack_impl 调 thread_fn＋`driver_run_thread_on_large_stack`。header 声明 large_stack＠297（非 dual-decl）；FROM_X 原型＋定义非 dual。邻 `product_emit`／`thread_fn_ptr` 不转（非 unique）；`xlang_driver_asm_prepare_entry_elf_emit` 不转（调仍闭 debug_trace）。大块 rec／glue_type_size／`append_reloc_absolute64` 仍闭。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变；WIN rc=0 T=**896**／285856B（892→896＝+4 簇全 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun＋elf_o（期望 48→37）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 dep_prerun／run_x 簇** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略独立 dep_prerun／`pipeline_run_x_thread_fn`／`large_stack` ifndef（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面；parse_skip_impl 调 large_stack，须同刀转。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 6 独立 ifndef（thread_fn_impl＋wrapper／large_stack_impl＋wrapper／parse_skip／typeck_only／parse_only／for_asm）。header 声明 wrappers＠242／246／250／254／257（非 dual-decl）；FROM_X 原型＋定义非 dual。邻 `pipeline_run_x_thread_fn_ptr` 不转（非 unique）；`xlang_asm_codegen_elf_o_*` 下簇；`pipeline_run_x_pipeline_impl` 仍闭（嵌 wave101）。大块 rec／glue_type_size／`append_reloc_absolute64` 仍闭。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变；WIN rc=0 T=**892**／284896B（881→892＝+11 簇全 T）。unique 仍 **48**（未 Windows 收割；金标仍 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect＋dep_prerun（期望 48→39）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 collect 簇** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略独立 collect ifndef（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 unique 面；与 load_direct 同：`_impl` 仍 ifndef，须连 wrapper 一起转。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 12 独立 ifndef（strdup／to_load_has／seed_to_load／enqueue／tmp_parse／deps_process_one／deps_transitive_impl／deps_transitive／paths_tmp／paths_process_one／dep_paths_impl／dep_paths）。header 声明 wrappers＠283／288（非 dual-decl）；FROM_X 原型＋定义非 dual。邻 `xlang_driver_asm_prepare_entry_elf_emit` 不转（调仍闭 debug_trace）。大块 rec／glue_type_size／`append_reloc_absolute64` 仍闭。**证**：Mac smoke POSIX rc=0 T=**739**／227776B 不变；WIN rc=0 T=**881**／279144B（869→881＝+12 簇全 T）。unique 仍 **48**（未 Windows 收割；金标已切回 Ubuntu）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：Windows 收割 `lsp_free`＋collect（期望 48→45）。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 `lsp_free`** | **代码** · **不升钉** · tip `41f78a13f`。独立 ifndef 转 OR `WIN_LEFTOVER_GROW_VEC`；`_impl` 始终编译（void**）；header＠304 原型＋定义非 dual。邻 `xlang_lsp_ptr_slot_clear` 不转（非 unique）。Mac smoke POSIX T=739；WIN T=**869**／272352B（`lsp_free` T＝+1）。Windows 闸门未收割（切回 Ubuntu）。PLATFORM: WINDOWS leftover-PE hybrid。 |
| 2026-09-03 | **docs(selfhost)** **日切：2 日归档／3 号主文档重写** | **文档** · **不升钉**。[自举进度-归档-2026-09-02.md](自举进度-归档-2026-09-02.md) 收 2 日 **52** 行（升钉 `b5be5ed97`／Darwin L4 JOBS／Windows leftover-PE hybrid unique 113→50）；主文档只留 KPI＋前排＋3 日 §6。PLATFORM: SHARED。 |
| 2026-09-03 | **fix(windows)** **pabi leftover-PE rest 编 public preprocess 簇** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略独立 `xlang_preprocess`／`with_path`／`quiet` ifndef（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无这三枚；header 声明 preprocess（char*）＠310、with_path／quiet＠93，seed 定义（非 dual-decl）；无 FROM_X 原型；wrapper 调已 T 的 `xlang_preprocess_raw_to_malloc_impl`；leftover rest 带 `XLANG_USE_X_PIPELINE`，LEGACY `preprocess_c_fallback` #else 不解析。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 独立 ifndef（与 load_direct 同模式）。大块 rec／glue_type_size／`append_reloc_absolute64` 仍闭；debug_trace 仍闭（void* vs struct*）；`lsp_free` 本波仍闭。**证**：tip `7172e057a` min-gate 52 leaf · SAT prefer hybrid **1249943B** · 三枚簇 **T** · phase1 unique **50→48**。path_cap link_abi cc 仍侧红。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：独立 `xlang_lsp_free_loaded_imports`（大块仍闭）。 |
