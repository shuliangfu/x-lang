# ShuX 自举进度

> **更新**：2026-07-22 · 21 号全日已归档；22 号起重新起表\
> **基线 SHA（产品 L4 放行钉盘）**：**`df1dde5b6`**（升自 `07684b813`；双端 tip L4 真冷 + 产品轨 **129** 全绿；含 wave102–**109** CG002/pure-asm 整波收口）\
> **产品 bstrict 套件数**：**129**\
> **本波 tip**：**tip L4 升钉** `df1dde5b6`（wave102–109 整波；产品含 `e1cff0c6` wave109）\
> **本波（22 日）状态**：wave110 ImportEntry pure 存储（结构债 pure 迁）；钉盘仍 `df1dde5b6`（日常 L2）。地图：[当前问题分析.md](当前问题分析.md)（非每微步必改）。

> **方法**：[自举方法.md](自举方法.md)（Cap/R/L/M；工程 ≠ 产品放行）\
> **Windows（删种子前）**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `shux-selfhost-product-gate`\
> **归档**：[自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md) · [19](自举进度-归档-2026-07-19.md) · [18](自举进度-归档-2026-07-18.md) · [17](自举进度-归档-2026-07-17.md) · [16](自举进度-归档-2026-07-16.md) · [14](自举进度-归档-2026-07-14.md) · [13](自举进度-归档-2026-07-13.md)

### 维护约定

1. 每完成一波 **必改两文**：本文（KPI/前排/§6）+ [`当前进度.md`](当前进度.md)。
2. **禁止**把 prove/Stage2/N++ 单独写成「可自举」。
3. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**；历史钉盘 125）。
4. 工程 KPI 写 **SHA + 日志路径**；过期整日流水移归档。
5. 大债地图默认写 [`当前问题分析.md`](当前问题分析.md)（承接原 `问题分析文档.md`）；非每微步。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 =** **`df1dde5b6`（双端 tip L4 真冷 + 129；wave102–109 CG002/pure-asm 整波收口）。**\
**产品代码 tip =** 钉盘 `df1dde5b6`（含 `e1cff0c6` wave109 arm64 spill/preserve mov）。

| 轨                | 状态                                                                                           |
| ---------------- | -------------------------------------------------------------------------------------------- |
| 工程轨              | R2 + **wave102–109** CG002/pure-asm expand 整波 tip L4（METHOD_CALL / deferred let / frame spill / arm64 string / spill mov） |
| 产品轨 tip / 钉盘     | tip = 钉盘 **`df1dde5b6`**（升自 `07684b813`）                                                                |
| mac 产品链 bstrict  | ✅ **OK (129)** tip L4 · `/tmp/mac_true_bstrict_df1dde5b6.log` · cold `/tmp/mac_true_cold_df1dde5b6.log` |
| Ubuntu L4（金标）    | ✅ **OK (129)** tip L4 · `/tmp/ubuntu_true_bstrict_df1dde5b6.log` · cold `/tmp/ubuntu_true_cold_df1dde5b6.log` |
| 前钉 L4            | ✅ 129 @ `df1dde5b6` · `07684b813` · `ed2dc1a1e` · `1f73ed275` 等                               |

***

## 1. 文档地图

| 文档                                             | 看什么                   |
| ---------------------------------------------- | --------------------- |
| [自举方法.md](自举方法.md)                             | Cap / R / L / M       |
| [自举步骤.md](自举步骤.md)                             | 可执行闸门                 |
| [自举验证.md](自举验证.md)                             | 怎么测                   |
| **本文**                                         | 长期 KPI + 前排（**每波必改**） |
| [当前进度.md](当前进度.md)                             | 当天快照（**每波必改**）        |
| **[当前问题分析.md](当前问题分析.md)**                     | tip 级产品债地图（21 日起权威）   |
| [自举进度-归档-2026-07-21.md](自举进度-归档-2026-07-21.md) | **21 号全日**（只读）        |
| skill `shux-selfhost-product-gate`             | L4 / 双端 / 假绿禁止        |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项                          | 结果                                                                                  | 日志 / 备注                                      |
| -------------------------- | ----------------------------------------------------------------------------------- | -------------------------------------------- |
| 产品 bstrict 套件数             | **129**                                                                             | 现行白名单                                        |
| 产品 L4 放行钉盘                 | **`df1dde5b6`**                                                                      | 22 日 tip L4 升钉（前钉 `07684b813`）；双端 L4 129；含 wave102–109      |
| 本波 tip                     | wave102–109 CG002/pure-asm 整波 tip L4 收口（钉盘 `df1dde5b6`）                             | **构建级别=L4**                     |
| mac 产品链 bstrict（tip L4） | ✅ **OK (129)**                                                                      | L4 · `/tmp/mac_true_bstrict_df1dde5b6.log` · cold `/tmp/mac_true_cold_df1dde5b6.log`    |
| Ubuntu L4（金标 · tip L4）  | ✅ **OK (129)**                                                                      | L4 · `/tmp/ubuntu_true_bstrict_df1dde5b6.log` · cold `/tmp/ubuntu_true_cold_df1dde5b6.log` |
| 产品矩阵（双端 tip L4）           | ✅ rv42 / option102 / si / hello + pure-asm option102/hello                                                                | mac + Ubuntu §4                                      |
| 前钉 mac/Ubuntu L4           | ✅ 129 @ `07684b813` · `ed2dc1a1e` · `1f73ed275` · `d4e2dca6` 等                                                                | 见 [21 号归档](自举进度-归档-2026-07-21.md) / 更早归档                             |

### 2.2 工程轨（继承）

| KPI           | 现                                        | 备注                   |
| ------------- | ---------------------------------------- | -------------------- |
| **T / EMPTY** | 18/18                                    | 18 号结案               |
| **N**         | 54/54 IDENTICAL                          | 回归基线                 |
| **R2 / 根修本波** | **wave110** ImportEntry pure 存储 | 日常 L2；钉盘仍 `df1dde5b6` |

***

## 3. 当前前排（≤3 条）

1. Ubuntu L2 验收 wave110 + 其它 rest R2
2. ImportEntry pure 整波 tip L4（双端）后升钉
3. **可选** Windows hybrid（**需要时再通知重启**）

***

## 4. 与 21 号归档的关系

- 21 号全日（async PREFER、bxec/rio、driver\_abi wave14–44、pipeline\_abi wave45–75、nested comments、钉盘升至 **`d4e2dca6`** 等）已整表移入 [`自举进度-归档-2026-07-21.md`](自举进度-归档-2026-07-21.md)。
- **勿**用归档 tip（如 `5421694c` wave75）或旧钉 `07684b813` / `ed2dc1a1e` / `1f73ed275` / `d4e2dca6` 等冒充现行 tip L4（产品 **`df1dde5b6`**）。
- 22 日新工作只记本文 §0/§2/§3/§6。

***

## 5. 双端 / 平台

| 平台                     | 本波状态                                                              |
| ---------------------- | ----------------------------------------------------------------- |
| **macOS arm64**        | ✅ tip L4 全擦 + 矩阵 + pure-asm option102/hello + **129** @ `df1dde5b6` |
| **Ubuntu x86\_64（金标）** | ✅ tip L4 全擦 + 矩阵 + pure-asm option102/hello + **129** @ `df1dde5b6` |
| **Windows MSYS2**      | 继承 hybrid；本波未重跑（**不需要切换**）                                             |

***

## 6. 变更记录（倒序 · 22 日起；21 号见归档）

| 日期         | SHA                                                     | 摘要                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------- | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-22 | **wave110** / docs | **R2 Cap residual pure wave110 ImportEntry 结构债**：pure 多模块 malloc 表（128 slot）+ 全套 `pipeline_module_import_*`（alloc/path/kind/binding/select）+ `pipeline_module_import_storage_release`；C `ImportEntry` 340B LE 布局；header `num_imports` off=8 soft-reset；`ast_pool` 同名 **SHUX_WEAK** 冷 twin + `ast_pool_module_release` 钩 pure free。闭合硬 Cap residual「ImportEntry 存储仍在 ast_pool GrowVec」。mac L2：hybrid pure **T** alloc/path_copy + 矩阵 rv42/option102/si/hello + pure-asm option102（`/tmp/mac_g05_wave110c.log`）。Ubuntu 待。钉盘仍 **`df1dde5b6`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`df1dde5b6` tip L4 升钉** | **真冷全测 L4**：wave102–109 CG002/pure-asm expand 整波收口（arm64 enc + macho_write + Darwin formal net.o / METHOD_CALL / deferred let / frame spill / arm64 string / GP spill-preserve mov）。ImportEntry 仍结构债暂缓。双端 `remaining_o=0` → bootstrap-driver-seed → g05 → §4 矩阵 + pure-asm option=102/hello → `SHUX_BSTRICT_SKIP_BUILD=1` **129** 全绿。mac `/tmp/mac_true_cold_df1dde5b6.log` · `/tmp/mac_true_bstrict_df1dde5b6.log`；Ubuntu 金标 `/tmp/ubuntu_true_cold_df1dde5b6.log` · `/tmp/ubuntu_true_bstrict_df1dde5b6.log`。钉盘 **`07684b813` → `df1dde5b6`**。PLATFORM: SHARED · **构建级别=L4** |
| 2026-07-22 | **`e1cff0c6`** / docs | **R2 CG002 wave109 arm64 GP spill/preserve mov**：根因 Darwin pure-asm option `let extra = m1+(m2 as i32)+…+(bp[0] as i32)` 左结合 ADD 后装 INDEX/AS 右操作数 → `glue_binop_preserve_rax_for_rbx_load` → `arch_arm64_enc_enc_mov_rax_to_x9` 仍为 seed_link_compat **weak stub return -1**（非 macho patch）。G.7：`backend_arm64_enc_c.from_x.c` 补全 ORR-as-MOV **x2/x9/x10–x15** 强符号（INDEX preserve + 7.3 spill）。mac L2：pure-asm **option -c/-o=102** · hello/rv pure-asm -c · c 矩阵。Ubuntu：x86 路径未改。钉盘仍 **`07684b813`**。PLATFORM: MACOS arm64 pure-asm · **日常 L2 非 tip L4** |
| 2026-07-22 | **`c79c01d32`** / docs | **R2 CG002 wave108 arm64 string-lit embed**：根因 `glue_asm_emit_jmp_skip_string_then_lea` / fmt string-lit import call 门控 `ta!=0` → Darwin pure-asm hello CG002。G.7：seed `_impl` + `.x` 补全 aarch64 **B + 4 对齐尾挂串 + ADR x0**；fmt 路径 len 用 `backend_enc_mov_imm32_to_rbx_arch`（w1）避免冲 x0。mac L2：g05 + 矩阵 rv42/option102/hello · pure-asm **hello -c** / rv42（option pure-asm 仍 CG002 residual）。Ubuntu：x86 路径未改，pull 后 g05 回归。钉盘仍 **`07684b813`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`0f2e810b1`** / docs | **R2 CG002 wave107 pure-asm compute_frame_size call-spill headroom**：根因 SysV `glue_sysv_spill_rax_rdx_to_frame_c` 每寄存器实参永久 `next_offset+=32`；`pipeline_asm_compute_frame_size_c` 仅历史 +512 → full option 帧 `0x360` ≪ max off `0xa80` → 栈写穿错返 -4。G.7：`glue_asm_sum_block_call_spill_bytes` AST 累加 CALL/METHOD_CALL spill；`scratch=max(512,call_spill)` 补全权威。Ubuntu L2：pure-asm **option=102** · frame `0x1280`≥max off `0x1058` · hello pure-asm · c rv42/option/si。mac L2：矩阵 + pure-asm option102。钉盘仍 **`07684b813`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`83c09e1bc`** / docs | **R2 CG002 wave106 pure-asm pass1 deferred let leaf**：根因两遍 emit pass0 把「纯」let 提前，但 `let extra = a + buf[0]` 等依赖 CALL 初值的 binop 也被 hoist → 入口读未初始化 `bp`/`a` → Ubuntu pure-asm option **SEGV**。G.7：`glue_block_compute_pass1_deferred_lets` seed INDEX/CALL/METHOD_CALL + **传递闭包**（init 用 deferred 槽则 defer）；pass0 skip deferred、pass1 原位 emit。Ubuntu L2：ord3=11 · noptr2=101 · pure-asm **hello** · c-backend option102；full option 仍 residual（frame `0x360` vs max off `0xa80` → 错返 -4）。mac L2：pure-asm ord3=11 · option run102 · 矩阵。钉盘仍 **`07684b813`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`4918983cf`** / docs | **R2 CG002 wave105 pure-asm hello METHOD_CALL leaf**：根因产品解析 `fmt.println("…")` 为 **METHOD_CALL (49)** 非 CALL+FIELD_ACCESS；METHOD_CALL import-binding 无 fmt 字符串 lit 特化 → 误 mangle `std_fmt_println_i32_reti32` → Ubuntu pure-asm BLD001 UNDEF。G.7：`glue_asm_try_emit_fmt_string_lit_import_call_elf_c` 接受 CALL/METHOD_CALL 实参表；method_call 路径先特化再 mangle；seed + `.x` 同 commit。Ubuntu L2：pure-asm **hello build/run** · U `std_fmt_println` · rv42/si · c-backend 矩阵（`/tmp/ubuntu_wave105_g05.log` · `/tmp/ubuntu_w105_hello.log`）；option pure-asm **SEGV residual**。mac L2：c-backend 矩阵 + pure-asm rv42；hello pure-asm **`.o` CG002**（arm64 ta≠0 string embed residual）。钉盘仍 **`07684b813`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`07684b813` tip L4 升钉** | **真冷全测 L4**：wave102–104 CG002 整波收口（arm64 enc + macho_write + Darwin formal net.o C path / `net_connect_many_c` no_mangle）。ImportEntry 仍结构债暂缓。双端 `remaining_o=0` → bootstrap-driver-seed → g05 → §4 矩阵 → `SHUX_BSTRICT_SKIP_BUILD=1` **129** 全绿。mac `/tmp/mac_true_cold_b5967f1b5.log` · `/tmp/mac_true_bstrict_07684b813.log`；Ubuntu 金标 `/tmp/ubuntu_true_cold_07684b81.log` · `/tmp/ubuntu_true_bstrict_07684b81.log`。钉盘 **`ed2dc1a1e` → `07684b813`**。PLATFORM: SHARED · **构建级别=L4** |
| 2026-07-22 | **`07684b813`** | **R2 CG002 wave104**：根因 tip L4 首跑 mac STD-092 net-context BLD001 — wave102 后 Darwin `-backend asm` 对 `std/net/{alpn,tcp,…}` 成功（pre-wave102 `code_len=0` 回退 C）；`ld -r` 合并 C mod.o 后 tls_stub/tcp_pool/io UNDEF 在产品 `-dead_strip` 下 live。G.7：Makefile Darwin net 子模块强制 `./shux-c`（Linux 仍 asm）；`net_connect_many_c` 双端 `#[no_mangle]` ≡ `net_accept_many_c` / mod.x extern。mac 重建 net.o + net/io-context 门禁绿 → 全量 bstrict 129。PLATFORM: MACOS formal net.o / SHARED no_mangle |
| 2026-07-22 | **wave103** / docs | **R2 CG002 Darwin pure-asm · macho_write 强符号叶**：根因 wave102 后 mega_body 绿仍 `macho_write=-1`（`asm_experimental_symbol_bridge` / full_link stubs **SHUX_WEAK** `platform_macho_write_macho_o_to_buf` return -1；真 `macho.x` 未入产品链）。G.7 在 `ast_pool.c` 新增 `pipeline_macho_write_o_to_buf_c`（端口 `macho.x::write_macho_o_to_buf`，与 `pipeline_elf_write_o_standard_to_buf_c` 同 `PipelineElfCtxAccess` + glue code_data 偏移）+ 强符号 `platform_macho_write_macho_o_to_buf`；`pipeline_glue.c` 前置声明。重建 `pipeline_glue_standalone.o` + g05。mac L2：pure-asm **macho_write=303** · Mach-O arm64 · **rv build/run=42**；§4 矩阵 rv42/option/si/hello（`/tmp/mac_wave103_g05.log` · `/tmp/mac_wave103_pureasm.log` · `/tmp/mac_wave103_matrix.log`）。钉盘仍 **`ed2dc1a1e`**。PLATFORM: MACOS MH_OBJECT + SHARED glue 面 · **日常 L2 非 tip L4** |
| 2026-07-22 | **wave102** / docs | **R2 CG002 Darwin pure-asm · arm64 enc 强符号叶**：根因产品 g05 链仅 `seed_link_compat` **SHUX_WEAK** `arch_arm64_enc_*`（体 `return -1`）→ `mega_body_c enc_label fail` / `code_len=0`；Ubuntu x86 有 `backend_x86_64_enc_c` 强符号故 pure-asm 绿。G.7 新增 `seeds/backend_arm64_enc_c.from_x.c`（端口 `arm64_enc.x` via `pipeline_elf_ctx_*`）+ Makefile/`g05_relink_env` 入链 `src/asm/backend_arm64_enc_c.o`（Darwin filtered + Linux USER_ASM；与 enc_dispatch 已有 call/sp 符号去重）。mac L2：g05 + §4 矩阵 rv42/option/si/hello；pure-asm 探针 **mega_body rc=0 · code_len=32 · num_labels=2**，仍 **CG002** 于 `macho_write=-1`（`asm_experimental_symbol_bridge` weak 桩；真 `macho.x` 未入产品链）。钉盘仍 **`ed2dc1a1e`**。PLATFORM: MACOS arm64 enc + SHARED 矩阵 · **日常 L2 非 tip L4** |
| 2026-07-22 | **`ed2dc1a1e` tip L4 升钉** | **真冷全测 L4**：wave82–101 Cap residual pure 整波收口（含 wave101 fn-as-value mangle soft）。决策：ImportEntry 为 ast_pool GrowVec **结构债**（非固定 BSS 微步 pure）→ 可迁 soft residual 已闭则整波主闸门 L4。双端 `remaining_o=0` → bootstrap-driver-seed → g05 → §4 矩阵（rv42 / option / si / hello）→ `SHUX_BSTRICT_SKIP_BUILD=1` **129** 全绿。mac `/tmp/mac_true_cold_ed2dc1a1e.log` · `/tmp/mac_true_bstrict_ed2dc1a1e.log`；Ubuntu 金标 `/tmp/ubuntu_true_cold_ed2dc1a1.log` · `/tmp/ubuntu_true_bstrict_ed2dc1a1.log`。钉盘 **`1f73ed275` → `ed2dc1a1e`**（产品 `fada2499` 含于钉盘）。PLATFORM: SHARED · **构建级别=L4** |
| 2026-07-22 | **`fada2499`** / docs | **R2 Cap residual pure wave101**：codegen soft residual — EXPR_VAR 同模块自由函数值（含 `(f as *u8)`）走 G.7 `codegen_try_emit_fn_as_value`（模块前缀 + `codegen_emit_func_link_name` ≡ def/call/extern）；`codegen_name_is_local_binding` 挡 param/let/const 误 mangle。闭合 soft residual「非 no_mangle fn-as-value 发裸源名」。Cap residual 仍 ImportEntry 存储（ast_pool）。seed pin + codegen.x 同 commit。mac L2：探针 build/run=0 + 矩阵 rv42/option/si/hello + force_c + shadow→9（`/tmp/mac_g05_wave101.log`）。Ubuntu L2：`-E` 同名 `fn_as_probe_helper_inner` + 矩阵 + force_c（`/tmp/ubuntu_g05_wave101b.log`；pure-asm 探针 CG002 tip 既有）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`035356e1`** / docs | **R2 pipeline_abi Cap residual pure wave100**：语言 residual — typeck 同模块 bare 函数名 → Cap-fn-ptr `*u8`（CALL 仍按名解析，不受影响）；pure `pipeline_run_x_thread_fn_ptr` / `shux_asm_codegen_elf_o_thread_fn_ptr` 体为 `(fn as *u8)`，无 g05 `shux_driver_*_thread_fn_ptr` U；seed typeck pin + pipeline cold twin 同 commit。闭合硬 Cap residual「g05 &fn cast 产品 pure 面」。soft：非 `#[no_mangle]` fn-as-value 仍发源名（codegen mangle residual）；ImportEntry 存储仍 Cap（ast_pool）。mac L2：pure fn_ptr adrp 体 + 矩阵 rv42/option/si/hello + nest→11 + force_c（`/tmp/mac_g05_wave100.log` · `/tmp/mac_g05_wave100_matrix.log`）。Ubuntu L2：pure **T** `return ((uint8_t *)(pipeline_run_x_thread_fn))` + 矩阵 + linux nest→11 + force_c（`/tmp/ubuntu_g05_wave100.log` · `/tmp/ubuntu_g05_wave100_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`bc3d2859`** / docs | **R2 pipeline_abi Cap residual pure wave99**：`parser_copy_module_import_path64` 真迁 pure thin（G.7 `pipeline_module_import_path_copy` + NUL 扫描 len ≡ 历史 parser_gen；parser_gen path64 demote **weak** cold twin；strict_glue 空 stub → 同语义 cold twin）。闭合硬 Cap residual「path64 under pure load_and_sync / load_import」叶。ImportEntry 存储仍 Cap residual（ast_pool）。Cap residual 仍 g05 &fn cast（语言）。mac L2：pure path64 bl path_copy + 矩阵 + nest→11 + force_c（`/tmp/mac_g05_wave99.log` · `/tmp/mac_g05_wave99_matrix.log`）。Ubuntu L2：pure path64 **W** call path_copy + 矩阵 rv42/option/si/hello + linux nest→11 + force_c（`/tmp/ubuntu_g05_wave99.log` · `/tmp/ubuntu_g05_wave99_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`ac475548`** / `40a908f5` / docs | **R2 pipeline_abi Cap residual pure wave98**：产品 complex #if 根修 — 闭 bootstrap stub 双权威。根因：`cfg_eval.x` 多 `#[cfg]` 同名 host lit 在 `-E-extern` 双重定义 → Makefile 静默 stub pin；pure 调 `cfg_eval_expr_c` 体是 C stub。根修：host lit → link_alias/host_lit C `#if`；eval 权威 cfg_eval.x；`PIPELINE_GEN_CFLAGS`+Clang `-Wno-parentheses-equality`；Ubuntu pin `seeds/cfg_eval_gen.linux.x86_64.c`（禁 plain `-E` 悬空 BSS SEGV）。闭合硬 Cap residual「产品 complex #if 体仍是 bootstrap stub」叶。Cap residual 仍 g05 &fn cast / `parser_copy_module_import_path64`。mac L2：`-E-extern+alias` + 矩阵 + complex macos→11（`/tmp/mac_g05_wave98.log`）。Ubuntu L2：linux pin gen + alias（`lexer_cfg_eval_expr_c` T）+ 矩阵 + complex linux→11（`/tmp/ubuntu_g05_wave98.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`626d430e`** / docs | **R2 pipeline_abi Cap residual pure wave97**：pure `pipeline_load_and_sync_direct_import_deps_c` step5 真迁 G.7 typeck.x 调用面（`typeck_merge_dep_struct_layouts_into_entry` / `typeck_wpo_unify_soa_layouts`；删 pure 上 `typeck_typeck_*` double-prefix hop；link-alias 仍服务其它 TU；cold twin 可保留 hop）。闭合硬 Cap residual「typeck merge+wpo 调用面 under pure load_and_sync」叶。Cap residual 仍 cfg_eval 复杂 / g05 &fn cast / `parser_copy_module_import_path64`。mac L2：hybrid pure merge U→typeck + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave97.log` · `/tmp/mac_g05_wave97_matrix.log`）。Ubuntu L2：hybrid pure **W**（load_and_sync size≡pure 0x734；U→typeck_merge/wpo）+ 矩阵 rv42/option/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave97.log` · `/tmp/ubuntu_g05_wave97_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`ff79c20a`** / docs | **R2 pipeline_abi Cap residual pure wave96**：`pipeline_parse_into_buf` 真迁 pure orch（G.7 pure `parser_parse_into_init` + `driver_parse_into_buf_rc` Cap-struct ok unpack + same-TU pure debug_trace/fixup on ok==0；`buf_len<=0` → -1；非 0 ok → -1；异于 wave64 `parse_into_bytes`）；glue 同名 **SHUX_WEAK** 冷 twin（改后须重建 `pipeline_x.o` + `pipeline_glue_standalone.o`）。闭合硬 Cap residual「parse under pure load_import」叶。Cap residual 仍 typeck merge 调用面 / cfg_eval 复杂 / g05 &fn cast。mac L2：hybrid pure parse_into_buf + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave96.log` · `/tmp/mac_g05_wave96_matrix.log`）。Ubuntu L2：hybrid pure **W**（parse_into_buf size≡pure 0xec）+ 矩阵 rv42/option/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave96.log` · `/tmp/ubuntu_g05_wave96_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`b498928e`** / docs | **R2 pipeline_abi Cap residual pure wave95**：`pipeline_resolve_path_x` / `pipeline_read_file_x` / `pipeline_preprocess_loaded_into_ctx` 真迁 pure orch（G.7 Cap residual try_one_lib_root / try_entry_dir / loop_should_continue；pure `shux_read_file_into_path` + set_loaded_len；pure `preprocess_x_buf` + preprocess_len LE store）；glue/ast_pool 同名 **SHUX_WEAK** 冷 twin（改后须重建 `pipeline_x.o` + `pipeline_glue_standalone.o`）。闭合硬 Cap residual「resolve/read/pp under pure load_import」叶。Cap residual 仍 parse_into_buf / typeck merge 调用面 / cfg_eval 复杂 / g05 &fn cast。mac L2：hybrid pure resolve/read/pp size≡pure + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave95.log` · `/tmp/mac_g05_wave95_matrix.log`）。Ubuntu L2：hybrid pure **W**（resolve 217 / read 191 / pp 222 size≡pure）+ 矩阵 rv42/option/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave95.log` · `/tmp/ubuntu_g05_wave95_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`30d64a37`** / docs | **R2 pipeline_abi Cap residual pure wave94**：`pipeline_load_import_from_disk_c` / `pipeline_sync_dep_slots_from_driver_c` 真迁 pure orch（G.7 same-TU pure `pipeline_bind_import_dep_buffers` + `pipeline_sync_one_dep_slot`；Cap residual `pipeline_resolve_path_x` / `pipeline_read_file_x` / `pipeline_preprocess_loaded_into_ctx` / `pipeline_parse_into_buf`）；glue/ast_pool 同名 **SHUX_WEAK** 冷 twin（改后须重建 `pipeline_x.o` + `pipeline_glue_standalone.o`）。闭合硬 Cap residual「disk load + dep-sync under pure load_and_sync」叶。Cap residual 仍 typeck merge+wpo / resolve-read-pp-parse 子叶 / cfg_eval 复杂 / g05 &fn cast。mac L2：hybrid pure load_import/sync/bind/sync_one size≡pure + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave94.log` · `/tmp/mac_g05_wave94_matrix.log`）。Ubuntu L2：hybrid pure **W**（load_import/sync/bind/sync_one size≡pure）+ 矩阵 rv42/option/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave94.log` · `/tmp/ubuntu_g05_wave94_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`d327590e`** / docs | **R2 pipeline_abi Cap residual pure wave93**：`pipeline_load_and_sync_direct_import_deps_c` 真迁 pure orch（G.7 same-TU pure `pipeline_try_bind_seeded_import` + `pipeline_dep_ctx_realign_ndep_for_entry_c`；Cap residual `load_import_from_disk_c` / `sync_dep_slots_from_driver_c` / typeck merge+wpo / `parser_copy_module_import_path64`）；`ast_pool` 同名 **SHUX_WEAK** 冷 twin（改后须重建 `pipeline_x.o` + `pipeline_glue_standalone.o`）。闭合硬 Cap residual「load_and_sync under pure dep-prerun typeck_only」叶。Cap residual 仍 disk load/sync/merge 子叶 / cfg_eval 复杂 / g05 &fn cast。mac L2：hybrid pure load_and_sync size≡pure + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave93b.log` · `/tmp/mac_g05_wave93_matrix.log`）。Ubuntu L2：hybrid pure **W**（load_and_sync/try_bind/realign size≡pure）+ 矩阵 rv42/option/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave93.log` · `/tmp/ubuntu_g05_wave93_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`f1e2be96`** / docs | **R2 pipeline_abi Cap residual pure wave92**：`pipeline_typeck_validate_struct_layouts_zero_padding_c` / `pipeline_typeck_patch_all_body_parent_links_c` 真迁 pure thin → G.7 typeck.x（`typeck_validate_struct_layouts_zero_padding` / `typeck_patch_all_body_parent_links`）；`pipeline_glue` 同名 **SHUX_WEAK** 冷 twin（glue metrics / inline parent walk）。闭合 Cap residual「layout validate+patch helpers under pure dep-prerun」叶。Cap residual 仍 load_and_sync / cfg_eval 复杂 / g05 &fn cast。mac L2：hybrid pure validate/patch + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave92.log` · `/tmp/mac_g05_wave92_matrix.log`）。Ubuntu L2：hybrid pure **W**（validate/patch_c U→typeck）+ 矩阵 rv42/option/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave92.log` · `/tmp/ubuntu_g05_wave92_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`455a9457`** / docs | **R2 pipeline_abi Cap residual pure wave91**：`pipeline_typeck_set_dep_ctx` / `get_dep_ctx` 真迁 pure LP64 BSS（G.7 `g_pipe_typeck_dep_ctx` u8[8] + `shux_ptr_slot_*`；删 ast_pool static `g_typeck_dep_ctx` 双权威；enum variant fallback 只读 get）；`pipeline_glue` 同名 **SHUX_WEAK** 冷 twin。闭合 Cap residual「set_dep_ctx」叶。Cap residual 仍 load_and_sync / cfg_eval 复杂 / g05 &fn cast / layout helpers。mac L2：hybrid pure set/get_dep_ctx + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave91.log` · `/tmp/mac_g05_wave91_matrix.log`）。Ubuntu L2：hybrid pure **W**（set/get_dep_ctx）+ 矩阵 rv42/option/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave91.log` · `/tmp/ubuntu_g05_wave91_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`ce96dc47`** / docs | **R2 pipeline_abi Cap residual pure wave90**：`pipeline_typeck_diag_soft_suppress_set` / `_get` 真迁 pure BSS（G.7 `g_pipe_typeck_diag_soft_suppress`；wave89 pure dep-prerun same-TU set；diagnostic get 共享 pure flag）；`pipeline_glue` 同名 **SHUX_WEAK** 冷 twin。闭合 Cap residual「soft-suppress」叶。Cap residual 仍 load_and_sync / cfg_eval 复杂 / g05 &fn cast / set_dep_ctx / layout helpers。mac L2：hybrid pure soft_suppress **T** + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave90.log` · `/tmp/mac_g05_wave90_matrix.log`）。Ubuntu L2：hybrid pure **W**（soft_suppress）+ 矩阵 rv42/option/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave90.log` · `/tmp/ubuntu_g05_wave90_matrix.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`293bc6f9a`** / docs | **R2 pipeline_abi Cap residual pure wave89**：`pipeline_typeck_dep_prerun_module_c` 真迁 pure orch（G.7 set_dep_ctx + soft_suppress + `typeck_x_ast_library` + Cap residual `validate_struct_layouts_zero_padding_c` / `patch_all_body_parent_links_c`；SHUX_DEBUG_PIPE 注 cold-only）；`pipeline_glue` 同名 **SHUX_WEAK** 冷 twin；strict_stubs weak -1 注释对齐。闭合 Cap residual「typeck dep-prerun」叶（wave60 typeck_only 调同 TU pure）。Cap residual 仍 load_and_sync / cfg_eval 复杂 / g05 &fn cast。mac L2：hybrid pure dep_prerun + 矩阵 + nest #if/-D + force_c（`/tmp/mac_g05_wave89.log` · `/tmp/mac_g05_wave89_matrix.log`）。Ubuntu L2：hybrid pure **W**（dep_prerun）+ 矩阵 rv42/option102/si/hello + nest + force_c（`/tmp/ubuntu_g05_wave89.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`10905a275`** / docs | **R2 pipeline_abi Cap residual pure wave88**：`preprocess_eval_condition_c` 真迁 pure orch（trim 首尾空白 + 简单标识 → 同 TU pure `preprocess_define_has`；含空格/`=`/`!`/`(`/`)` 复杂条件 → Cap residual `cfg_eval_expr_c` under unsafe）；glue `runtime_driver_strict_glue_stubs` 同名 **SHUX_WEAK** 冷 fallback。闭合 Cap residual「preprocess #if condition-eval」叶。Cap residual 仍 cfg_eval 复杂分支 / g05 &fn cast（`preprocess_x_buf` 已 pure preprocess.x cross-TU）。mac L2：hybrid pure eval + 矩阵 + nest #if/-D（`/tmp/mac_g05_wave88.log`）。Ubuntu L2：hybrid pure **W**（eval）+ 矩阵 rv42 + nest #if/-D（`/tmp/ubuntu_g05_wave88.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`a395783e`** / docs | **R2 pipeline_abi Cap residual pure wave87**：`pipeline_typeck_module_for_ctx` / `_impl` 真迁路由 → G.7 单权威 `typeck_x_ast` / `typeck_x_ast_library`（`pipeline_module_main_func_index` 选 entry/library ≡ `pipeline_impl_typecheck`）；`typeck_module_entry_only` / `with_sidecar` fail-closed（X 需 arena+ctx）。闭合 Cap residual「typeck_module C 前端」叶（已删 C 前端曾仅 weak -1）。Cap residual 仍 preprocess_x_buf（preprocess.x 引擎已 pure，pipeline orch export-extern）/ g05 &fn cast。mac L2：hybrid pure for_ctx + 矩阵 + force_c rv42（`/tmp/mac_g05_wave87.log`）。Ubuntu L2：hybrid pure **W**（for_ctx）+ 矩阵 rv42 + force_c（`/tmp/ubuntu_g05_wave87.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`128092729`** / docs | **R2 pipeline_abi Cap residual pure wave86**：`preprocess_if_stack_reset` / `len` / `push` / `pop` / `at` / `set_at` 真迁 pure 固定 `i32[32]` BSS（G.7 单权威 `g_pipe_pp_if_stack` + `g_pipe_pp_if_n`；`ast_pool` GrowVec 同名 **SHUX_WEAK** 冷 fallback）。闭合 Cap residual「preprocess #if stack」叶。Cap residual 仍 typeck_module C 前端 / preprocess_x_buf。mac L2：PREFER hybrid pure if_stack + 矩阵 + nest #if（`/tmp/mac_g05_wave86.log`）。Ubuntu L2：hybrid pure **W**（if_stack_*）+ 矩阵 rv42 + nest #if（`/tmp/ubuntu_g05_wave86.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`3b9f9f435`** / docs | **R2 pipeline_abi Cap residual pure wave85**：`preprocess_define_reset` / `preprocess_define_add` / `preprocess_define_has` 真迁 pure BSS（G.7 单权威 `g_pipe_pp_defines` 128×64 + `g_pipe_pp_ndefines`；glue `runtime_driver_strict_glue_stubs` 同名 **SHUX_WEAK** 冷 fallback）。闭合 Cap residual「preprocess -D define table」叶。Cap residual 仍 typeck_module C 前端 / preprocess_x_buf / if_stack。mac L2：PREFER hybrid pure **T** + 矩阵（`/tmp/mac_g05_wave85.log`）。Ubuntu L2：hybrid pure **W**（define_*）+ 矩阵 rv42（`/tmp/ubuntu_g05_wave85.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`458a1efa`** / **`e7d7c569`** / docs | **R2 pipeline_abi Cap residual pure wave84**：`pipeline_run_x_thread_fn_ptr` / `shux_asm_codegen_elf_o_thread_fn_ptr` 真迁 pure thin（G.7 g05 `shux_driver_pipeline_run_x_thread_fn_ptr` / `shux_driver_asm_elf_o_thread_fn_ptr` 持 &fn cast residual；`.x` 无法形成函数指针常量；seed 冷孪生 `#ifndef FROM_X`）。闭合 Cap residual「always-seed Cap-fn-ptr 产品面」叶。`e7d7c569`：g05 prologue 声明对齐 pure `*u8` 签名（Ubuntu gcc void\* 冲突 → hybrid 假回退）。mac L2：PREFER hybrid + 矩阵（`/tmp/mac_g05_wave84.log`）。Ubuntu L2：hybrid pure **W**（fn_ptr）+ 矩阵 rv42（`/tmp/ubuntu_g05_wave84.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`c4698724`** / docs | **R2 pipeline_abi Cap residual pure wave83**：`pipeline_sizeof_arena` / `pipeline_sizeof_module` 真迁 pure（固定 SHARED LP64 常量 **16 / 68** ≡ 双端 C `sizeof(struct ast_ASTArena/Module)`；glue `__attribute__((weak))` 冷 fallback）。闭合 Cap residual「pure export-extern glue sizeof」叶。Cap residual 仍 fn-ptr / typeck_module C 前端（+ preprocess 引擎）。mac L2：PREFER hybrid + 矩阵（`/tmp/mac_g05_wave83.log`）。Ubuntu L2：hybrid pure **W**（sizeof_arena/module）+ 矩阵 rv42（`/tmp/ubuntu_g05_wave83.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED LP64 · **日常 L2 非 tip L4** |
| 2026-07-22 | **`1e4fb85a5`** / docs | **R2 pipeline_abi Cap residual pure wave82**：`pipeline_debug_trace_named_func_bodies_impl` 真迁 pure orch（G.7 getenv + `pipeline_module_*` / `ast_ast_block_*` Cap residual + pure `pipeline_debug_body_func_match` + same-TU `pipe_diag_msg_append_*` + `diag_report`；无 va_list reportf；seed 冷孪生 `#ifndef FROM_X` 保留 reportf）。闭合 soft residual「pure 仍调 always-seed body-trace `_impl`」叶。Cap residual 仍 fn-ptr / typeck_module C 前端（+ sizeof / preprocess 引擎）。mac L2：PREFER hybrid + 矩阵（`/tmp/mac_g05_wave82.log`）。Ubuntu L2：hybrid pure **W**（body-trace + pipe_diag_msg_*）+ 矩阵 rv42（`/tmp/ubuntu_g05_wave82.log`）。钉盘仍 **`1f73ed275`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-22 | **`1f73ed275` tip L4 升钉** | **真冷全测 L4**：wave76–81 soft residual 整波收口。双端 `remaining_o=0` → bootstrap-driver-seed → g05 → §4 矩阵（rv42 / option / si / hello）→ `SHUX_BSTRICT_SKIP_BUILD=1` **129** 全绿。mac `/tmp/mac_true_cold_1f73ed275.log` · `/tmp/mac_true_bstrict_1f73ed275.log`；Ubuntu 金标 `/tmp/ubuntu_true_cold_1f73ed27.log` · `/tmp/ubuntu_true_bstrict_1f73ed27.log`。钉盘 **`d4e2dca6` → `1f73ed275`**（产品 `6e0796c6` 含于钉盘）。PLATFORM: SHARED · **构建级别=L4** |
| 2026-07-22 | **`6e0796c6`** / docs | **R2 pipeline_abi Cap residual pure wave81**：`shux_preprocess` / `shux_preprocess_quiet` / `shux_preprocess_with_path` 真迁 pure thin（G.7 pure `shux_preprocess_raw_to_malloc_impl`；产品始终 X-pipeline 路径；seed 冷孪生保留 LEGACY `preprocess_c_fallback` under `#ifndef FROM_X`）。闭合 soft residual「preprocess 公共面 always-seed」叶（引擎 `preprocess_*` 仍 Cap residual）。Cap residual 仍 fn-ptr / typeck_module C 前端（+ sizeof / preprocess 引擎）。mac/Ubuntu 日常 L2 后 **tip L4 升钉见上行**。PLATFORM: SHARED |
| 2026-07-22 | **`8b45a69a`** / docs | **R2 pipeline_abi Cap residual pure wave80**：`shux_asm_codegen_elf_o_product_emit` 真迁 pure thin（G.7 export-extern `asm_asm_codegen_elf_o` only — **删**同 TU weak `-1` stub，call 保持 external reloc → final strong `user_asm_seed_bridge`）；seed 冷孪生 `#ifndef FROM_X`；FROM_X rest pure **extern**。闭合硬 Cap residual「product_emit」叶。Cap residual 仍 fn-ptr / typeck_module C 前端（+ sizeof / preprocess）。mac L2：PREFER hybrid + c-backend 矩阵（`/tmp/mac_g05_wave80.log`）。Ubuntu L2：hybrid pure **W**（product_emit）+ **U** `asm_asm_codegen_elf_o` + pure-asm rv42/option/si（`/tmp/ubuntu_g05_wave80.log`）。PLATFORM: SHARED · **日常 L2 后并入 tip L4** |
| 2026-07-22 | **docs 归档**（本提交） | **文档**：21 号全日仪表盘 + §6 流水整表迁入 [`自举进度-归档-2026-07-21.md`](自举进度-归档-2026-07-21.md)；本文 22 日起重新起表（保留 wave76–80 当日流水）。当时钉盘 **`d4e2dca6`** · tip **`8b45a69a`**（已由 tip L4 升钉覆盖）。 |
| 2026-07-22 | **`ccc0f99e`** / docs | **R2 pipeline_abi Cap residual pure wave79**：`shux_path_try_realpath_inplace` 真迁 pure orch（G.7 g05 `shux_driver_realpath_opaque` char\* cast residual + 栈 `resolved[1024]` + `pipe_cstr_copy` ≡ seed snprintf；失败保留 path；非 POSIX harness null ≡ seed `#else` no-op）；seed 冷孪生 `#ifndef FROM_X`；FROM_X rest pure **extern**。闭合历史 Cap residual「OS realpath」叶。PLATFORM: SHARED · **日常 L2 后并入 tip L4** |
| 2026-07-22 | **`c6e36f0e`** / docs | **R2 pipeline_abi Cap residual pure wave78**：`shu_lsp_ptr_slot_clear` 真迁 pure thin（G.7 `shux_ptr_slot_set` null）+ `shux_fputs_stdout`（G.7 g05 `shux_driver_stdout_ptr` + `shux_driver_fputs_opaque`）+ `driver_asm_fp_is_stdout` / `driver_asm_fclose_file`（G.7 g05 stdout 身份比较 + `shux_driver_fclose_opaque`）；seed 冷孪生 `#ifndef FROM_X`；FROM_X rest pure **extern**。PLATFORM: SHARED · **日常 L2 后并入 tip L4** |
| 2026-07-22 | **`ba2c8362`** / docs | **R2 pipeline_abi Cap residual pure wave77**：`typeck_ndep` + `typeck_dep_{module,arena}_ptrs` 真迁 pure BSS（`g_pipe_typeck_*`）+ pure accessors；product seed cold writers→accessor。PLATFORM: SHARED LP64 · **日常 L2 后并入 tip L4** |
| 2026-07-22 | **`f0a89715`** / docs | **R2 pipeline_abi Cap residual pure wave76**：`shux_cstr_offset` 真迁 pure orch。PLATFORM: SHARED · **日常 L2 后并入 tip L4** |

***

## 7. 禁止清单（本阶段）
