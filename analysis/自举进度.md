# Xlang / X language 自举进度

> **更新**：2026-09-21 · **tip 本波** wave731：**FORCE leftover-first copy_large_struct_from_rax_ptr**。mega `export extern` `glue_copy_large_struct_from_rax_ptr_elf_c`（同 TU 5 caller 已 unsafe；thins assign_thin／assign_deref_* export-extern 无体仍 localize；FORCE mega T 先赢 leftover gcc W 0x482）。合链 leftover gcc W 0x482 唯一全局（endbr64 `sub $0x40`）。mega 发 U。min 探针未命中 copy_large（struct_i32／field 8B）。LIVE disasm leftover gcc。field_assign `-c` 绿 **run=9**。index_i32 `-c` 绿 **run=42**。index_assign `-c` 绿 **run=9**。**未** leftover-first rax_plus_rbx_scaled。**未** leftover-first local_slot rbx twin。**未** leftover-first wrapper `pipeline_asm_index_elem_byte_sz`（仍 mega smash T `sub $0x888`）。**未** leftover-first `glue_store_retval_pair_to_rbp_elf_c`（合链仍 mega smash T `sub $0x8e8`）。**未** leftover-first `glue_emit_vector_type_let_init_elf_c`（合链仍 mega smash T `sub $0xaa8`）。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`）。**未** leftover-first `pipeline_asm_emit_skip_heavy_or_thin_stub_elf_c`（合链仍 mega smash T `sub $0x948`）。return_if `-c` 绿 **run=7**。call_i32／struct_i32 `-c` 绿 **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5。钉盘 **`ecdb5cc1e`**（默认不升）
> **状态**：**尚未完全自举**。主刀 **M2**（FORCE 整合层）。
> **下波（w732）**：leftover-first 其余同 TU 砸体 callee（点名 leftover gcc W `glue_emit_vector_type_let_init_elf_c` 0x2c5；禁 leftover-first rax_plus_rbx_scaled／local_slot rbx twin／skip_heavy／wrapper `pipeline_asm_index_elem_byte_sz` 除非 WALL）。mega T ∩ leftover W 余 1970 名＝FORCE 默认帧，**禁止整表 localize**。8 EOF 仍 parse-drop。**禁止** `-E` 当修法；勿复用 w690_force；mega FORCE 仍非产品默认；勿整叶 elf_ctx_thin overlay；勿 leftover-first smash-only `modlet_prepare` 自身。勿 leftover-first `skip_heavy` 自身。HARD BAN 产品 PREFER `return_elf_impl_thin`。

闸门：双端 L2；暂停 `xlang check`；L4 仅整波／谈自举。

### 维护约定

1. 每波必改本文（KPI／前排≤3／§6 一行）。终局债改 [`C迁移追踪.md`](C迁移追踪.md)。
2. §6 倒序 · 一行一波。过期整日流水日切归档后主文档重写。

***

## 0. 一句话

**尚未完全自举** · 钉盘 **`ecdb5cc1e`** · **w731** leftover-first copy_large_struct_from_rax_ptr；FORCE field_assign run=9。LIVE leftover gcc W 0x482。

| 指标 | 状态 |
|------|------|
| 钉盘 L4 | ✅ 双端＠**`ecdb5cc1e`** |
| tip L4 | ✅ **双端全绿**＠`a54d1a99a`（连续十波）· 产品态健康 |
| 本波级别 | **w731＝FORCE leftover-first copy_large_struct_from_rax_ptr**（日常 L2，非 L4；field_assign run=9／index_i32 run=42，产品仍 w701 overlay） |
| 下一波 | **w732** leftover-first 其余同 TU 砸体 callee（点名 leftover gcc W `glue_emit_vector_type_let_init_elf_c` 0x2c5；禁整表 1970；禁 leftover-first rax_plus_rbx／local_slot rbx twin／skip_heavy 除非 WALL） |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举效率方法-M2主链.md](自举效率方法-M2主链.md) | M2 主链 |
| [C迁移追踪.md](C迁移追踪.md) | 终局债（短状态表） |
| **本文** | KPI · 前排 · §6（9 月 21 日） |
| [自举进度-归档-2026-09-20.md](自举进度-归档-2026-09-20.md) | **9 月 20 日**（w618–w691 FORCE 整合战役） |
| [自举进度-归档-2026-09-19.md](自举进度-归档-2026-09-19.md) | **9 月 19 日**（w597–w617） |

***

## 2. 仪表盘

| 项 | 结果 |
|----|------|
| 产品 bstrict | **131** 套（双端 tip 全绿＠`eab2c15b1`） |
| 钉盘 | **`ecdb5cc1e`** |
| MG／Stage8 | MG ✅ · Stage8 **30/30** |
| SSH | `ubuntu-server`／`windows-server` |

***

## 3. 当前前排（≤3）

1. **主迁刀（w732）** — FORCE leftover-weaken **asm_wpo_thin `-c` 绿** 81984B／74T。**field_assign `-c` 绿 run=9**。**index_i32 `-c` 绿 run=42**。**index_assign `-c` 绿 run=9**。**return_if `-c` 绿 run=7**。**call_i32／struct_i32 `-c` 绿 run=42**。合链 leftover gcc W `glue_copy_large_struct_from_rax_ptr_elf_c` 0x482 唯一全局（endbr64 `sub $0x40`）。mega 发 U。thins assign_thin／assign_deref_* 已 localize。LIVE disasm leftover gcc。min 探针未命中 copy_large（8B）。**未** leftover-first rax_plus_rbx_scaled。**未** leftover-first local_slot rbx twin。**未** leftover-first wrapper `pipeline_asm_index_elem_byte_sz`（仍 mega smash T `sub $0x888`）。**未** leftover-first `glue_store_retval_pair_to_rbp_elf_c`（仍 mega smash T `sub $0x8e8`）。**未** leftover-first `glue_emit_vector_type_let_init_elf_c`（仍 mega smash T `sub $0xaa8`）。**未** leftover-first `skip_heavy`（仍 mega smash T）。**未** leftover-first skip_heavy_or_thin_stub parent（仍 mega smash T `sub $0x948`）。下一刀＝leftover-first 其余同 TU 砸体 callee（点名 leftover gcc W `glue_emit_vector_type_let_init_elf_c` 0x2c5；禁 leftover-first rax_plus_rbx／local_slot rbx twin／skip_heavy／wrapper 除非 WALL）。禁整表 localize 1970 个 mega T ∩ leftover W。HARD BAN 仍跳：value_abi／wpo_dump／macho_write／arr_return／2 失败 thin。8 EOF 别名仍 parse-drop。产品 PREFER of assign_index／return_elf_impl_thin 仍 HARD BAN。
2. **次排** — elf_shndx sidecar 族原子；2 失败 thin（assign_rhsrax_arms P001／return_elf_impl T001）；malloc_trim weak。
3. **立卡不动** — arr_return 族（`x=call() then if` UND-drop＝真实发射能力缺口，M1 类）；MSVC va／WIN leftover；eq 深链；spawn 真体；nest 冻帽 64。

***

## 4. 本日要点（9 月 21 日）

| 波 | 结论 |
|----|------|
| w731 | **FORCE leftover-first copy_large_struct_from_rax_ptr**：mega `export extern` `glue_copy_large_struct_from_rax_ptr_elf_c`（同 TU 5 caller 已 unsafe；thins assign_thin／assign_deref_* export-extern 无体仍 localize；FORCE mega T 先赢 leftover gcc W 0x482）。合链 leftover gcc W 0x482 唯一全局（endbr64 `sub $0x40`）。mega 发 U。min 探针未命中 copy_large（struct_i32／field 8B）。LIVE disasm leftover gcc。field_assign `-c` 绿 813B **run=9**。index_i32 `-c` 绿 789B **run=42**。index_assign `-c` 绿 805B **run=9**。**未** leftover-first rax_plus_rbx_scaled。**未** leftover-first local_slot rbx twin `glue_enc_local_slot_ptr_or_addr_rbx_elf_c`。**未** leftover-first wrapper `pipeline_asm_index_elem_byte_sz`（仍 mega smash T `sub $0x888`）。**未** leftover-first `glue_store_retval_pair_to_rbp_elf_c`（合链仍 mega smash T `sub $0x8e8`）。**未** leftover-first `glue_emit_vector_type_let_init_elf_c`（合链仍 mega smash T `sub $0xaa8`）。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`）。**未** leftover-first `pipeline_asm_emit_skip_heavy_or_thin_stub_elf_c`（合链仍 mega smash T `sub $0x948`）。return_if `-c` 绿 757B **run=7**。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w730 | **FORCE leftover-first local_var_slot_needs_ptr_load**：mega `export extern` `glue_local_var_slot_needs_ptr_load_elf_c`（同 TU 4 caller 已 unsafe；thin param_ptr_slot_thin HAS body 须 localize；FORCE mega T 先赢 leftover gcc W 0x542）。合链 leftover gcc W 0x542 唯一全局（endbr64 `sub $0x150`）。mega 发 U。field_assign `-c` 绿 813B **run=9**（gdb leftover gcc `$rip=0x833dc3`＝W 0x833db1+0x12；FIN eax=0）。index_i32 `-c` 绿 789B **run=42**。index_assign `-c` 绿 805B **run=9**。LIVE disasm leftover gcc。**未** leftover-first rax_plus_rbx_scaled。**未** leftover-first local_slot rbx twin `glue_enc_local_slot_ptr_or_addr_rbx_elf_c`。**未** leftover-first wrapper `pipeline_asm_index_elem_byte_sz`（仍 mega smash T `sub $0x888`）。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`）。**未** leftover-first `pipeline_asm_emit_skip_heavy_or_thin_stub_elf_c`（合链仍 mega smash T `sub $0x948`）。return_if `-c` 绿 757B **run=7**。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w729 | **FORCE leftover-first var_or_field_base_to_rbx**：mega `export extern` `glue_try_index_var_or_field_base_to_rbx_elf_c`（同 TU 18 caller 已 unsafe；FORCE mega T 先赢 leftover gcc W 0x7dc）。合链 leftover gcc W 0x7dc 唯一全局（endbr64 `sub $0x60`）。mega 发 U。index_assign `-c` 绿 805B **run=9**（gdb leftover gcc `$rip=0x82cc36`＝W 0x82cc2a+0xc；FIN eax=0）。index_i32 `-c` 绿 789B **run=42**。field_assign `-c` 绿 813B **run=9**。LIVE disasm leftover gcc。**未** leftover-first rax_plus_rbx_scaled。**未** leftover-first local_slot rbx twin `glue_enc_local_slot_ptr_or_addr_rbx_elf_c`。**未** leftover-first wrapper `pipeline_asm_index_elem_byte_sz`（仍 mega smash T `sub $0x888`）。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`）。**未** leftover-first `pipeline_asm_emit_skip_heavy_or_thin_stub_elf_c`（合链仍 mega smash T `sub $0x948`）。return_if `-c` 绿 757B **run=7**。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w728 | **FORCE leftover-first index bounds_guard**：mega `export extern` `glue_emit_index_bounds_guard_elf_c`（scaled leftover-first 后 mega 无剩余 caller，未发 U；FORCE mega T 先赢 leftover gcc W 0x90f）。合链 leftover gcc W 0x90f 唯一全局（endbr64 `sub $0x270`）。index_i32 `-c` 绿 789B **run=42**（gdb leftover gcc `$rip=0x7f6f90`＝W 0x7f6f81+0xf；FIN eax=0）。index_assign `-c` 绿 805B **run=9**。field_assign `-c` 绿 813B **run=9**。LIVE disasm leftover gcc。**未** leftover-first rbx twin。**未** leftover-first wrapper `pipeline_asm_index_elem_byte_sz`（仍 mega smash T `sub $0x888`）。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`）。**未** leftover-first `pipeline_asm_emit_skip_heavy_or_thin_stub_elf_c`（合链仍 mega smash T `sub $0x948`）。**未** leftover-first rax_plus_rbx_scaled。return_if `-c` 绿 757B **run=7**。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w727 | **FORCE leftover-first rvalue_slice_once**：mega `export extern` `glue_try_index_rvalue_slice_once_elf_c`（scaled leftover-first 后 mega 无剩余 caller，未发 U；FORCE mega T 先赢 leftover gcc W 0xd2e）。合链 leftover gcc W 0xd2e 唯一全局（endbr64 `sub $0x290`）。index_i32 `-c` 绿 789B **run=42**（gdb leftover gcc `$rip=0x7f885f`＝W 0x7f8850+0xf；FIN eax=-2＝VAR array N/A）。index_assign `-c` 绿 805B **run=9**。field_assign `-c` 绿 813B **run=9**。LIVE disasm leftover gcc。**未** leftover-first rbx twin。**未** leftover-first wrapper `pipeline_asm_index_elem_byte_sz`（仍 mega smash T `sub $0x888`）。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`）。**未** leftover-first `pipeline_asm_emit_skip_heavy_or_thin_stub_elf_c`（合链仍 mega smash T `sub $0x948`）。**未** leftover-first `glue_emit_index_bounds_guard_elf_c`。return_if `-c` 绿 757B **run=7**。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w726 | **FORCE leftover-first var_or_field_base_to_rax**：mega `export extern` `glue_try_index_var_or_field_base_to_rax_elf_c`（同 TU localize 仍打 mega 本地 t；FORCE mega T 先赢 leftover gcc W 0xe0c）。合链 leftover gcc W 0xe0c 唯一全局（endbr64 `sub $0x190`）。index_i32 `-c` 绿 789B **run=42**（gdb leftover gcc `$rip=0x83022d`＝W 0x83021e+0xf；FIN eax=0）。index_assign `-c` 绿 805B **run=9**。field_assign `-c` 绿 813B **run=9**。LIVE disasm leftover gcc。**未** leftover-first rbx twin。**未** leftover-first wrapper `pipeline_asm_index_elem_byte_sz`（仍 mega smash T `sub $0x888`）。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`）。**未** leftover-first `pipeline_asm_emit_skip_heavy_or_thin_stub_elf_c`（合链仍 mega smash T `sub $0x948`）。return_if `-c` 绿 757B **run=7**。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w725 | **FORCE leftover-first index_eff_addr_scaled**：mega `export extern` `glue_emit_index_eff_addr_scaled_elf_c`（同 TU localize 仍打 mega 本地 t；FORCE mega T 先赢 leftover gcc W 0x73e）。合链 leftover gcc W 0x73e 唯一全局（endbr64 `sub $0x60`）。index_i32 `-c` 绿 789B **run=42**（gdb leftover gcc `$rip=0x7fcf6a`＝W 0x7fcf5e+0xc；FIN eax=0）。index_assign `-c` 绿 805B **run=9**。field_assign `-c` 绿 813B **run=9**。LIVE disasm leftover gcc。**未** leftover-first wrapper `pipeline_asm_index_elem_byte_sz`（仍 mega smash T `sub $0x888`）。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`）。**未** leftover-first `pipeline_asm_emit_skip_heavy_or_thin_stub_elf_c`（合链仍 mega smash T `sub $0x948`）。return_if `-c` 绿 757B **run=7**。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w724 | **FORCE leftover-first index_elem_byte_sz**：mega `export extern` `pipeline_asm_index_elem_byte_sz_c`（同 TU localize 仍打 mega 本地 t；FORCE mega T 先赢 leftover gcc W 0x1103）。合链 leftover gcc W 0x1103 唯一全局（endbr64 `sub $0x3d0`）。index_i32 `-c` 绿 789B **run=42**（gdb leftover gcc `$rip=0x7e2ee3`＝W 0x7e2ed4+0xf；FIN eax=4）。index_assign `-c` 绿 805B **run=9**。field_assign `-c` 绿 813B **run=9**。LIVE disasm leftover gcc。**未** leftover-first wrapper `pipeline_asm_index_elem_byte_sz`（仍 mega smash T `sub $0x888`）。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`）。**未** leftover-first `pipeline_asm_emit_skip_heavy_or_thin_stub_elf_c`（合链仍 mega smash T `sub $0x948`）。return_if `-c` 绿 757B **run=7**。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w723 | **FORCE leftover-first local_slot**：mega `export extern` `glue_enc_local_slot_ptr_or_addr_elf_c`（同 TU localize 仍打 mega 本地 t；FORCE mega T smash `sub $0x8b8` 先赢 leftover gcc W 0x82）。合链 leftover gcc W 0x82 唯一全局（endbr64 `sub $0x40`）。field_assign `-c` 绿 813B **run=9**（gdb leftover gcc `$rip=0x833e78`＝W 0x833e6c+0xc；eax=0）。LIVE disasm leftover gcc。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`）。**未** leftover-first `pipeline_asm_emit_skip_heavy_or_thin_stub_elf_c`（合链仍 mega smash T `sub $0x948`）。return_if `-c` 绿 757B **run=7**。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w722 | **FORCE leftover-first lvalue_eff_addr**：mega `export extern` `pipeline_asm_emit_lvalue_eff_addr_elf_c`（同 TU localize 仍打 mega 本地 t；FORCE mega T 先赢 leftover gcc W 0x890）。合链 leftover gcc W 0x890 唯一全局（endbr64 `sub $0x170`）。field_assign `-c` 绿 813B **run=9**（gdb leftover gcc emit_assign；未命中 lvalue_eff_addr，smash `glue_enc_local_slot_ptr_or_addr_elf_c` `sub $0x8b8` 仍绿）。LIVE disasm leftover gcc。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`）。**未** leftover-first `pipeline_asm_emit_skip_heavy_or_thin_stub_elf_c`（合链仍 mega smash T `sub $0x948`）。return_if `-c` 绿 757B **run=7**。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w721 | **FORCE leftover-first driver m8_tail**：mega `export extern` `asm_driver_m8_tail_thin_delegate_c_name`（同 TU localize 仍打 mega 本地 t；FORCE mega T 先赢 leftover gcc W 0x64）。合链 leftover gcc W 0x64 唯一全局（endbr64 `sub $0x20`）。min 探针未命中 driver m8_tail（parser EMIT_HEAVY 才走）；LIVE disasm leftover gcc。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`）。**未** leftover-first `pipeline_asm_emit_skip_heavy_or_thin_stub_elf_c`（合链仍 mega smash T `sub $0x948`）。return_if `-c` 绿 757B **run=7**。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w720 | **FORCE leftover-first typeck m8_tail**：mega `export extern` `asm_typeck_m8_tail_thin_delegate_c_name`（同 TU localize 仍打 mega 本地 t；FORCE mega T 先赢 leftover gcc W 0xed）。合链 leftover gcc W 0xed 唯一全局（endbr64 `sub $0x30`）。min 探针未命中 typeck m8_tail（parser EMIT_HEAVY 才走）；LIVE disasm leftover gcc。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`）。**未** leftover-first `pipeline_asm_emit_skip_heavy_or_thin_stub_elf_c`（合链仍 mega smash T `sub $0x948`）。return_if `-c` 绿 757B **run=7**。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w719 | **FORCE leftover-first pipeline m8_tail**：mega `export extern` `asm_pipeline_m8_tail_thin_delegate_c_name`（同 TU localize 仍打 mega 本地 t；FORCE mega T 先赢 leftover gcc W 0x130）。合链 leftover gcc W 0x130 唯一全局（endbr64 `sub $0x20`）。min 探针未命中 pipeline m8_tail（parser EMIT_HEAVY 才走）；LIVE disasm leftover gcc。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`）。**未** leftover-first `pipeline_asm_emit_skip_heavy_or_thin_stub_elf_c`（合链仍 mega smash T `sub $0x948`）。return_if `-c` 绿 757B **run=7**。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w718 | **FORCE leftover-first backend m8_tail**：mega `export extern` `asm_backend_m8_tail_thin_delegate_c_name`（同 TU localize 仍打 mega 本地 t；FORCE mega T 先赢 leftover gcc W 0xa62）。合链 leftover gcc W 0xa62 唯一全局（endbr64 `sub $0x20`）。min 探针未命中 backend m8_tail（parser EMIT_HEAVY 才走）；LIVE disasm leftover gcc。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`；gdb `$rip` smash skip_heavy）。**未** leftover-first `pipeline_asm_emit_skip_heavy_or_thin_stub_elf_c`（合链仍 mega smash T `sub $0x948`）。return_if `-c` 绿 757B **run=7**。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w717 | **FORCE leftover-first func_is_thin_delegate**：mega `export extern` `asm_parser_func_is_thin_delegate`（同 TU localize 仍打 mega 本地 t；FORCE mega T 先赢 leftover gcc W 0x12d3）。合链 leftover gcc W 0x12d3 唯一全局（endbr64 `sub $0x10`）。min 探针未命中 thin_delegate（parser EMIT_HEAVY 才走）；LIVE disasm leftover gcc。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`；gdb `$rip` smash skip_heavy）。return_if `-c` 绿 757B **run=7**。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w716 | **FORCE leftover-first return_elf_impl**：mega `export extern` `pipeline_asm_emit_return_elf_impl`（同 TU localize 仍打 mega 本地 t；FORCE mega T 先赢 leftover gcc W 0x1a7e；arr_return_thin／fixed_array_copy_thin 副本须 localize）。合链 leftover gcc W 0x1a7e 唯一全局（endbr64 `sub $0x1e8`）。gdb `$rip` leftover gcc（xlang 0x7fc163＝W+0x10）；return_if `-c` 绿 757B **run=7**。wrapper `pipeline_asm_emit_return_elf_c` 仍 mega smash T（`sub $0x8a8`）。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`）。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。HARD BAN 产品 PREFER `return_elf_impl_thin`。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w715 | **FORCE leftover-first collect_walk**：mega `export extern` `asm_wpo_collect_walk`（同 TU localize 仍打 mega 本地 t；FORCE mega T 先赢 leftover gcc W 0x1e56；thin 副本须 localize）。合链 leftover gcc W 0x1e56 唯一全局（endbr64 `sub $0x210`）。min 探针未命中 collect_walk；LIVE disasm leftover gcc。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`；gdb `$rip` smash skip_heavy eax=0）。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w714 | **FORCE leftover-first parser m8_tail**：mega `export extern` `asm_parser_m8_tail_thin_delegate_c_name`（同 TU localize 仍打 mega 本地 t；FORCE mega T 先赢 leftover gcc W 0x1f1a）。合链 leftover gcc W 0x1f1a 唯一全局（endbr64 `sub $0x20`）。min 探针未命中 m8_tail（parser_emit_heavy 才走）；LIVE disasm leftover gcc。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`；gdb `$rip` smash skip_heavy eax=0）。call_i32 `-c` 绿 809B **run=42**。struct_i32 `-c` 绿 797B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w713 | **FORCE leftover-first struct let-init 族**：mega `export extern` `glue_emit_struct_type_let_init_elf_c`／`pipeline_asm_emit_struct_let_init_elf_c`／`pipeline_asm_emit_struct_lit_fields_elf_c`／`pipeline_asm_emit_struct_lit_elf_c`（同 TU localize 仍打 mega 本地 t；leftover gcc 父面 CALL leftover gcc 子面）。合链 leftover gcc W 0x20c9／0xa0／0x169e／0x44 唯一全局（endbr64 `sub $0x3b8`／`$0x40`／`$0xb8`／`$0x20`）。gdb `$rip` leftover gcc（xlang struct let-init 0x7edfe7＝W+0x10）；struct_i32 `-c` 绿 797B **run=42**。**未** leftover-first `asm_skip_heavy_module_func_body`（合链仍 mega smash T `sub $0x1ff8`）。call_i32 `-c` 绿 809B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w712 | **FORCE leftover-first emit_heavy_safe_helper**：mega `export extern` `asm_parser_emit_heavy_safe_helper`（同 TU localize 仍打 mega 本地 t；FORCE mega smash `sub $0x87c8` 先赢 leftover gcc W 0x22d7）。合链 leftover gcc W 0x22d7 唯一全局（endbr64 `sub $0x10`）。**未** leftover-first `asm_skip_heavy_module_func_body`（wave650 `FORCE_FULL_BODIES` 逃生门；合链仍 mega smash T `sub $0x1ff8`；gdb `$rip` smash skip_heavy eax=0）。call_i32 `-c` 绿 809B **run=42**。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁 leftover-first `skip_heavy` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w711 | **FORCE leftover-first for_call_args**：mega `export extern` `pipeline_asm_emit_expr_elf_for_call_args`（同 TU localize 仍打 mega 本地 t；FORCE mega smash `sub $0x5c38`／cltq 先赢 leftover gcc W 0x2c4f）。合链 leftover gcc W 0x2c4f 唯一全局（endbr64 `sub $0x248`）。gdb `$rip` leftover gcc（xlang 0x87441f＝W+0x10 过 prologue）；call_i32 `-c` 绿 809B **run=42**；asm_wpo 命中 leftover gcc eax=0。**asm_wpo_thin `-c` 绿** 81984B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w710 | **FORCE leftover-first emit_assign 族**：mega `export extern` `pipeline_asm_emit_assign_elf_c`／`glue_emit_assign_rhs_to_rax_elf_c`／`glue_emit_assign_rhs_elf_c`（同 TU localize 仍打 mega 本地 t；leftover gcc VAR assign CALL leftover gcc rhs_to_rax W 0x59b；leftover gcc rhs_to_rax plain CALL leftover gcc rhs_elf_c W 0xd6）。合链 leftover gcc W emit_assign 0x4b79／rhs_to_rax 0x59b／rhs_elf_c 0xd6 唯一全局（endbr64 `sub $0x238`／`$0x60`／`$0x40`）。gdb `$rip` leftover gcc；assign_i32 `-c` 绿 741B **run=42**；vector let-init eax=-2（非向量，未 leftover-first）。**asm_wpo_thin `-c` 绿** 81192B／74T。**15 颗剩余卫星回归绿**。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁整叶 elf_ctx overlay。禁整表 localize ~1970 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w709 | **FORCE 扩剩余 assign_field／assign_index 卫星**：复用 leftover-weaken `/tmp/w708/force.o`（不重编 mega）。**14 颗剩余 assign_field `-c` 绿**（chain_walk／mag_fold／ptr_array／ptr_hit／ptr_hit_step／ptr_struct／var_array／var_root／var_root_finish／var_simd／var_stores／var_struct／var_struct_store／var_struct_pair）。**24 颗 assign_index 族 `-c` 绿**（setup／named／named_body／generic／try／try2／scaled／array／peel／walk／resolve／rbx／array_lit／mid／home／bulk／bulk_call／bulk_lval／simd／simd_body／struct_lit／arr／rbx／index；用户 `-c`，产品 PREFER 仍 HARD BAN）。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。mega T ∩ leftover W 余 1970＝FORCE 默认帧，禁整表 localize。下一刀点名 leftover gcc W `pipeline_asm_emit_assign_elf_c` 0x4b79。禁 leftover-first smash-only `modlet_prepare`。禁整叶 elf_ctx overlay。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w708 | **FORCE leftover-first add_label 族**：mega `export extern` `add_label`／`ensure_label`（同 TU localize 仍打 mega 本地 t；mega BSS `g_pipe_elf_label_shndx` ≠ leftover `g_pipeline_elf_label_shndx`）。leftover gcc add_label 用 leftover STATIC current_shndx／label_shndx_set。leftover gcc ensure_label CALL leftover gcc add_label，必须同族 leftover-first。合链 leftover gcc W add_label 0x1f0／ensure_label 0xd5 唯一全局。gdb `$rip` leftover gcc；add_label edx=21 eax=0；ensure_label 再 CALL leftover add_label eax=0。**asm_wpo_thin `-c` 绿** 81192B／74T。**15 颗剩余卫星回归绿**。**6 颗 assign_field 族 `-c` 绿**（assign_field／scalar／ptr／var_depth1／helpers／emit）。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁整叶 elf_ctx overlay。禁整表 localize ~2000 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w707 | **FORCE leftover-first add_common_sym 族**：mega `export extern` `add_sym`／`add_common_sym`（同 TU localize 仍打 mega 本地 t；mega BSS `g_pipe_elf_sym_is_common` ≠ leftover `g_pipeline_elf_sym_is_common`）。leftover gcc add_common_sym CALL leftover gcc add_sym，必须同族 leftover-first。合链 leftover gcc W add_sym 0x22d／add_common_sym 0x138 唯一全局。gdb `$rip` leftover gcc；edx=21；eax=0。**asm_wpo_thin `-c` 绿** 81192B／74T。**15 颗剩余卫星 `-c` 绿**（asm_locals／sidecar_pool／asm73_live_*／type_to_c_repr／block_tree／type_alias／parser_result／binop_var_slot_cache／asm_expr／mega_loop／emit_one／emit_frame／typeck_orch／typeck_check_expr_mega）。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare`。禁整叶 elf_ctx overlay。禁整表 localize ~2000 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w706 | **FORCE leftover-first F7 data family**：gdb 点名 `bake_scalar_imm_to_data` 返 −1（`g_aw_root_id=-1` wave344 .data）。同 TU localize mega T 仍把 prepare 的 CALL 绑到 mega 本地 t（`$rip`＝mega smash）。mega BSS `g_pipe_elf_data_len` ≠ leftover gcc `g_pipeline_elf_data_len`。G.7 mega 四面 `export extern`；合链 leftover gcc W emit 0x22／append 0xa2／poke 0x4c ＋ leftover smash bake T 唯一全局。**asm_wpo_thin `-c` 绿** 81192B／73T（prepare／bake／poke eax=0，mega_body eax=0）。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~2000 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w705 | **FORCE 扩中等／大卫星**：leftover-weaken `/tmp/w704/force.o`（非 w690）。**12 中等** `-c` 绿（type_pool／slot_bytes／mega_body／binop_block_peel／module_enum／module_import／w157_sum／top_level_let／module_func／bootstrap_glue／struct_layout／onefunc）。**8 大**绿（dep_ctx／fixed_array_copy／block_domain／expr_sidecar／ast_forwarders／elf_ctx **用户 `-c`**／modlet／assign）。墙＝`asm_wpo_thin` CG002 `code_len=0`：`count=73` 但 `at()`／`emit_one` 未进；gdb `modlet_prepare_and_emit#1 eax=-1`。该面 leftover T 亦砸（`sub $0x1f38`／cltq），**无 gcc W**。产品同文件 `-backend asm -c` 绿 81024B（`backend.x` 无 mega_body 闸）。FORCE L2 五件套绿（hello=0／option=102／si=0／f32=0／rv=42）。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁整叶 elf_ctx overlay（用户 `-c`≠overlay）。禁整表 localize ~2000 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w704 | **FORCE leftover-first emit_fast**：mega `export extern` `pipeline_asm_emit_expr_elf_fast`（砸体 `sub $0x2e98` 盖 leftover W 0x1200）。合链 leftover fast 活后 parse_orch 仍 CG002：emit_one#2 文件级 `const W323_ARENA_OFF_NUM_EXPRS` 走 leftover fast VAR，调 mega 砸体 helper。localize `glue_call_arg_resolve_var_stack_off_elf_c`（W 0x70b）／`glue_var_expr_stack_off_elf_c`（W 0x72）／`asm_module_top_level_const_lit_i32`（W 0x14c，`sub $0x8c8` 砸体）后 **parse_orch `-c` 绿**（26246B／59T）。FORCE L2 五件套绿（hello=0／option=102／si=0／f32=0）。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁整表 localize ~2000 个 `sub $0x888` mega T。禁 `-E`／禁 elf_ctx_thin overlay／禁 mega FORCE 产品默认。钉盘不升 |
| w703 | **FORCE 扩探针**：两函数／let／if／while／call-args／hello／option=102／si／f32 全绿；5 小卫星 thin `-c` 绿。中等 `parse_orch_thin`（产品 26358B／59T）CG002 `code_len=699`：emit_one#2 `w323_arena_num_exprs`（`return load_i32(a, CONST)`）在 `w499_mega_emit_body_sync` −1。根因＝mega 砸体先赢 leftover emit：`backend_emit_block_body_sync_elf` mega T `sub $0x8a8` 盖 leftover 84B gcc；内层 `pipeline_asm_emit_block_body_sync_elf` `sub $0x5228` 盖 0x22c6。G.7 mega 两面 `export extern`；合链 leftover W 唯一全局。下一内层 mega T `pipeline_asm_emit_expr_elf_fast`（`sub $0x2e98` 盖 leftover W 0x1200）。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5＠`509f4f6ef`。禁整表 localize ~2000 个 `sub $0x888` mega T（FORCE 默认帧）。禁 `-E`／禁 elf_ctx_thin overlay／禁 mega FORCE 产品默认。钉盘不升 |
| w702 | **mega 不得先赢 WPO emit-order**：FORCE mega/thin asm 把 `g_aw_pgo_emit_n` 比较编成立即数 0 → `at()` 恒 −1 → emit_one 空成功 → CG002 `code_len=0`。G.7 mega 三面 `export extern`；合链 localize 砸体 helper，leftover gcc W（prepare 0x270／count 0x2f／at 0x65）唯一全局。FORCE 两函数 `-o` **run=42**。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5＠`e6af7b3c2`。禁 `-E`／禁 elf_ctx_thin overlay／禁 mega FORCE 产品默认。钉盘不升 |
| w701 | **writers overlay reloc heap 单权威**：w697 keep-global 只露出 write_o，overlay 内 `t` helper 另有一份 heap；write_o 读空 heap → FORCE mega 后段 `call next_insn`（`block_alloc` return 12，产品同函数 return 1）→ parse P001。扩 keep-globals（append_reloc／offset_at／name_len／copy64／shndx_at／sidecar_reset）。mega `block_alloc` nrel 0→8，r_sym0 59→0。FORCE leftover-weaken 探针过 parse，新墙 CG002 `code_len=0`。Ubuntu 产品 overlay pabi 2410944／xlang 5539200。双端 L2 5/5＠`70312b027`。禁 `-E`／禁 elf_ctx_thin overlay。钉盘不升 |
| w700 | **leftover ELF UND 帽 256→2048**：活 writer 栈 `[256][128]` 静默丢 calloc/mmap → 1032 条 PLT32 r_sym=0（`pipe_gv_alloc_bytes` 调 0）。`.x` `pipe_elf_undef_cap` 已是 2048 但 leftover 强 rest 不用。补 BSS 2048 行＋跳过 rlen≤0（空名 UND 会让 ld 报 corrupt symtab）。FORCE mega UND 474／calloc·mmap＝U／r_sym=0 余 59；gdb：`calloc(1,0x21400)` 自 `pipe_gv_alloc_bytes` 进入 libc。新墙＝parse P001。产品保留 writer overlay（Ubuntu pabi 2376112／xlang 5517088）。双端 L2 5/5＠`ccf44fdad`。禁 `-E`／禁 elf_ctx_thin overlay。钉盘不升 |
| w699 | **FORCE mega `parser_*` 不得盖 parser_x**：mega 三面 `export extern`（parser_x 唯一 T）；库 TU 跳过 modlet runtime seed（wave344：无 main 不往第一导出面灌 COMMON 初值）。parser_x `parse_into_init` 补 `trait_reg_reset`。FORCE mega 2596074：三面 U、path64 T；合链 6368464／g05 8823120，链接 `parser_parse_into_init`＝parser_x T。探针过 parse_into_init，崩 `pipe_gv_alloc_bytes` 调 0。产品恢复 overlay＋modlet inject。双端 L2 5/5＠`8b3ae2899`。禁 `-E`。钉盘不升 |
| w698 | **leftover-weaken FORCE 全链（探针，产品恢复 overlay writer）**：264/266 thin（2 失败同 w627）＋w697 mega 2602022＋overlay pabi_weak＋filler 能链（T=2048 W=1424／6319424／g05 8778008）。COMMON 尺寸墙倒（合链 17408×2／86020×2；size1=2 非 297）。裸探针不再 sidecar SEGV，改崩 mega `parser_parse_into_init` T（0x0／1744B／`sub $0x888`／`lea 0x0(%rip)` 自写）盖 parser_x。localize 4 `parser_*` 后面改 `__stack_chk_fail`←`parser_onefunc_res_wire_dummy_head`；value_abi SEGV_ACCERR＠`0x790e72868000`。8 EOF 仍 leftover W。勿复用 w690_force。双端 L2 5/5＠`25e562753`。禁 `-E`。钉盘不升 |
| w697 | **leftover compact WAVE273 writer 入链（FORCE COMMON 尺寸墙倒）**：从 from_x WAVE273 leftover 布局抽 overlay TU（勿整叶 elf_ctx_thin；布局不匹配会卡死 FORCE）。localize-all 后只露出 3 writer＋`common_size_at`，writers-first `ld -r` 叠进 hybrid pabi。Ubuntu FORCE mega 2602022／297 COMMON hist 正确（8×138／256×33／512×28／17408／86020；size=1＝1）。Darwin 同配方 llvm-objcopy＋`ld -r`。双端 L2 5/5＠`e371c0495`。禁 `-E`／禁 elf_ctx_thin overlay。钉盘不升 |
| w696 | **FORCE COMMON size=1 定因＋writer 权威（源已修，产品 leftover writer 未入链）**：noFORCE mega 297 COMMON 尺寸正确（8/256/512…／align16）；FORCE 297×size=1。根因＝FORCE 砸 dual-copy sidecar，writer 信 sidecar=1 跳过 ctx offset。G.7 补 w672：ctx offset 先于 sidecar（`.x`＋thin＋from_x）。elf_ctx_thin 整叶／三面 overlay 与 leftover ctx 布局不匹配→FORCE 编译卡死（已恢复 hybrid pabi）。from_x 整叶 cc 类型冲突。双端 L2 5/5＠`9730d918d`。禁 `-E`。钉盘不升 |
| w695 | **FORCE 全链重建（探针波，产品恢复）**：mono 2614412／T=2048＋264 thin（2 失败同 w627）。裸合链不过（8 EOF 别名 parse-drop）。leftover-weaken 能链但 sidecar `pipe_load_i32_le`←`append_lib_root` 崩——根因＝mega 文件级 `u8[17408]` 全落 1 字节 Lxml COMMON（297×size=1；dep_ctx thin 同 let＝0x4400）。族原子藏 mega dep_ctx 97 面后 sidecar 过；get_copy 6 hit 全净（rdi=栈／rdx=1\|2，0×`0x1800000001`）；下一崩＝`pipe_elf_shndx_sidecar_reset` memset（同类）。勿复用 w690_force。双端 L2 5/5＠`a0b0bb721`。禁 `-E`。钉盘不升 |
| w694 | **value_abi 重编＋二次 get_copy 闭合**：w693 编译器编 thin，type_get dest＠0x450 ≠ local＠0x238（U）／0x20（D）。C sret 探针两次 get_copy 入口均为合法栈／arena 指针（非 `0x1800000001`），拷回 42/99，rc=0。旧 force 混链 sidecar 崩＝整合债（w695 全量重建）。产品未动。双端 L2 5/5＠`a0b0bb721`。禁 `-E`。钉盘不升 |
| w693 | **sret dest 与 532B 局部分离**：rest `mega_body_c` gcc overlay 入 hybrid pabi。fill_local 可能停在 let 起点 → home=`align8(next+ret_sz)`。探针 dest＠0x450、local＠0x238（U）／0x20（D）不重叠。双端 L2 5/5＋`&g=42`＠`1aad86a9e`。禁 `-E`。钉盘不升 |
| w692 | **sret 槽与 532B Type 局部重叠（根修波）**：C 调用面是标准 sret（`sizeof(ast_Type)=532`）；force `type_get` 把 hidden dest 存在 `-0x120`，532B `let t` 在 `-0x248` 盖住该指针。根因＝`next_offset+256` 空洞。修法＝`fill_local` 之后预约（emit_frame thin＋from_x 孪生）。双端 L2 5/5＋`&g=42`＠`df17ed485`。rest `mega_body_c` 待 Ubuntu 重编 from_x 入链。禁 `-E`。钉盘不升 |

***

## 5. 与归档

- **20 日**：[自举进度-归档-2026-09-20.md](自举进度-归档-2026-09-20.md)（w618–w691：PREFER 放开→mega FORCE 打通→72 桩／帽／strb／sret 尾转发）
- **19 日**：[自举进度-归档-2026-09-19.md](自举进度-归档-2026-09-19.md)（w597–w617：COMMON shndx、ARM64 栈参、wpo_dump／asm_locals PREFER；双端 L4 连续四波绿）
- **C 追踪流水账**：[archive/C迁移追踪-流水账归档-20260919.md](archive/C迁移追踪-流水账归档-20260919.md)
- **现行钉盘＝`ecdb5cc1e`**。

***

## 6. 变更记录（倒序 · **9 月 21 日**）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-21 | wave731 | FORCE leftover-first copy_large_struct_from_rax_ptr（export extern glue_copy_large_struct_from_rax_ptr_elf_c）。合链 leftover gcc W 0x482（endbr64 sub $0x40）。mega 发 U。thins assign_thin／assign_deref_* 已 localize。min 探针未命中 copy_large（8B）。LIVE disasm leftover gcc。field_assign run=9。index_i32 run=42。index_assign run=9。未 leftover-first rax_plus_rbx_scaled／local_slot rbx twin／wrapper／store_retval_pair／vector let-init／skip_heavy／skip_heavy_or_thin_stub parent。return_if run=7。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave730 | FORCE leftover-first local_var_slot_needs_ptr_load（export extern glue_local_var_slot_needs_ptr_load_elf_c）。合链 leftover gcc W 0x542（endbr64 sub $0x150）。mega 发 U。thin param_ptr_slot_thin 已 localize。field_assign run=9（gdb leftover gcc $rip=0x833dc3 eax=0）。index_i32 run=42。index_assign run=9。LIVE disasm leftover gcc。未 leftover-first rax_plus_rbx_scaled／local_slot rbx twin／wrapper／skip_heavy／skip_heavy_or_thin_stub parent。return_if run=7。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave729 | FORCE leftover-first var_or_field_base_to_rbx（export extern glue_try_index_var_or_field_base_to_rbx_elf_c）。合链 leftover gcc W 0x7dc（endbr64 sub $0x60）。mega 发 U。index_assign run=9（gdb leftover gcc $rip=0x82cc36 eax=0）。index_i32 run=42。field_assign run=9。LIVE disasm leftover gcc。未 leftover-first rax_plus_rbx_scaled／local_slot rbx twin／wrapper／skip_heavy／skip_heavy_or_thin_stub parent。return_if run=7。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave728 | FORCE leftover-first index bounds_guard（export extern glue_emit_index_bounds_guard_elf_c）。合链 leftover gcc W 0x90f（endbr64 sub $0x270）。index_i32 run=42（gdb leftover gcc $rip=0x7f6f90 eax=0）。index_assign run=9。field_assign run=9。LIVE disasm leftover gcc。未 leftover-first rbx twin／wrapper／skip_heavy／skip_heavy_or_thin_stub parent／rax_plus_rbx_scaled。return_if run=7。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave727 | FORCE leftover-first rvalue_slice_once（export extern glue_try_index_rvalue_slice_once_elf_c）。合链 leftover gcc W 0xd2e（endbr64 sub $0x290）。index_i32 run=42（gdb leftover gcc $rip=0x7f885f eax=-2 N/A）。index_assign run=9。field_assign run=9。LIVE disasm leftover gcc。未 leftover-first rbx twin／wrapper／skip_heavy／skip_heavy_or_thin_stub parent／bounds_guard。return_if run=7。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave726 | FORCE leftover-first var_or_field_base_to_rax（export extern glue_try_index_var_or_field_base_to_rax_elf_c）。合链 leftover gcc W 0xe0c（endbr64 sub $0x190）。index_i32 run=42（gdb leftover gcc $rip=0x83022d eax=0）。index_assign run=9。field_assign run=9。LIVE disasm leftover gcc。未 leftover-first rbx twin／wrapper／skip_heavy／skip_heavy_or_thin_stub parent。return_if run=7。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave725 | FORCE leftover-first index_eff_addr_scaled（export extern glue_emit_index_eff_addr_scaled_elf_c）。合链 leftover gcc W 0x73e（endbr64 sub $0x60）。index_i32 run=42（gdb leftover gcc $rip=0x7fcf6a eax=0）。index_assign run=9。field_assign run=9。LIVE disasm leftover gcc。未 leftover-first wrapper／skip_heavy／skip_heavy_or_thin_stub parent。return_if run=7。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave724 | FORCE leftover-first index_elem_byte_sz（export extern pipeline_asm_index_elem_byte_sz_c）。合链 leftover gcc W 0x1103（endbr64 sub $0x3d0）。index_i32 run=42（gdb leftover gcc $rip=0x7e2ee3 eax=4）。index_assign run=9。field_assign run=9。LIVE disasm leftover gcc。未 leftover-first wrapper／skip_heavy／skip_heavy_or_thin_stub parent。return_if run=7。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave723 | FORCE leftover-first local_slot（export extern glue_enc_local_slot_ptr_or_addr_elf_c）。合链 leftover gcc W 0x82（endbr64 sub $0x40）。field_assign run=9（gdb leftover gcc $rip=0x833e78 eax=0）。LIVE disasm leftover gcc。未 leftover-first skip_heavy／skip_heavy_or_thin_stub parent。return_if run=7。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave722 | FORCE leftover-first lvalue_eff_addr（export extern pipeline_asm_emit_lvalue_eff_addr_elf_c）。合链 leftover gcc W 0x890（endbr64 sub $0x170）。field_assign run=9。未命中 lvalue_eff_addr（smash local_slot 仍绿）。LIVE disasm leftover gcc。未 leftover-first skip_heavy／skip_heavy_or_thin_stub parent。return_if run=7。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave721 | FORCE leftover-first driver m8_tail（export extern asm_driver_m8_tail_thin_delegate_c_name）。合链 leftover gcc W 0x64（endbr64 sub $0x20）。min 探针未命中；LIVE disasm leftover gcc。未 leftover-first skip_heavy／skip_heavy_or_thin_stub parent。return_if run=7。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave720 | FORCE leftover-first typeck m8_tail（export extern asm_typeck_m8_tail_thin_delegate_c_name）。合链 leftover gcc W 0xed（endbr64 sub $0x30）。min 探针未命中；LIVE disasm leftover gcc。未 leftover-first skip_heavy／skip_heavy_or_thin_stub parent。return_if run=7。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave719 | FORCE leftover-first pipeline m8_tail（export extern asm_pipeline_m8_tail_thin_delegate_c_name）。合链 leftover gcc W 0x130（endbr64 sub $0x20）。min 探针未命中；LIVE disasm leftover gcc。未 leftover-first skip_heavy／skip_heavy_or_thin_stub parent。return_if run=7。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave718 | FORCE leftover-first backend m8_tail（export extern asm_backend_m8_tail_thin_delegate_c_name）。合链 leftover gcc W 0xa62（endbr64 sub $0x20）。min 探针未命中；LIVE disasm leftover gcc。未 leftover-first skip_heavy／skip_heavy_or_thin_stub parent。return_if run=7。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave717 | FORCE leftover-first func_is_thin_delegate（export extern asm_parser_func_is_thin_delegate）。合链 leftover gcc W 0x12d3（endbr64 sub $0x10）。min 探针未命中；LIVE disasm leftover gcc。未 leftover-first skip_heavy。return_if run=7。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave716 | FORCE leftover-first return_elf_impl（export extern pipeline_asm_emit_return_elf_impl；localize arr_return_thin／fixed_array_copy_thin 副本）。合链 leftover gcc W 0x1a7e（endbr64 sub $0x1e8）。gdb leftover gcc；return_if run=7。未 leftover-first skip_heavy。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave715 | FORCE leftover-first collect_walk（export extern asm_wpo_collect_walk；localize thin 副本）。合链 leftover gcc W 0x1e56（endbr64 sub $0x210）。未 leftover-first skip_heavy。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave714 | FORCE leftover-first parser m8_tail（export extern asm_parser_m8_tail_thin_delegate_c_name）。合链 leftover gcc W 0x1f1a（endbr64 sub $0x20）。未 leftover-first skip_heavy。call_i32／struct_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave713 | FORCE leftover-first struct let-init 族（export extern glue_emit_struct_type_let_init／struct_let_init／struct_lit_fields／emit_struct_lit）。合链 leftover gcc W 0x20c9／0xa0／0x169e／0x44。gdb leftover gcc；struct_i32 run=42。未 leftover-first skip_heavy。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave712 | FORCE leftover-first emit_heavy_safe_helper（export extern asm_parser_emit_heavy_safe_helper）。合链 leftover gcc W 0x22d7。未 leftover-first skip_heavy。call_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave711 | FORCE leftover-first for_call_args（export extern pipeline_asm_emit_expr_elf_for_call_args）。call_i32 run=42。asm_wpo_thin `-c` 绿 81984B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave710 | FORCE leftover-first emit_assign 族（export extern emit_assign／rhs_to_rax／rhs_elf_c）。assign_i32 run=42。asm_wpo_thin `-c` 绿 81192B／74T。15 颗回归绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave709 | FORCE 扩剩余 assign_field 14＋assign_index 24 颗 `-c` 绿（复用 w708 force.o，不重编 mega）。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave708 | FORCE leftover-first add_label 族（export extern add_label／ensure_label）。asm_wpo_thin `-c` 绿 81192B／74T。15 颗回归＋6 颗 assign_field `-c` 绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave707 | FORCE leftover-first add_common_sym 族（export extern add_sym／add_common_sym）。asm_wpo_thin `-c` 绿 81192B／74T。15 颗剩余卫星 `-c` 绿。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave706 | FORCE leftover-first F7 data family（export extern emit/append/poke/bake）。asm_wpo_thin `-c` 绿 81192B／73T。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave705 | FORCE 扩卫星：12 中等＋8 大 `-c` 绿。墙＝asm_wpo 砸体 `modlet_prepare`（无 gcc W）。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave704 | FORCE leftover-first emit_fast（export extern＋localize resolve／stack_off／top_level_const_lit）。parse_orch `-c` 绿（26246B／59T）。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave703 | FORCE 扩探针：hello／option／si／f32＋5 小卫星 thin 绿。parse_orch CG002＝mega 砸体先赢 leftover body_sync（两面 export extern）。下一内层 emit_fast。产品 overlay 未改。双端 L2 5/5＠`509f4f6ef` |
| 2026-09-21 | wave702 | mega 不得先赢 WPO emit-order（export extern＋localize 砸体）。FORCE 两函数 `-o` run=42。CG002 `code_len=0` 倒。产品 overlay 未改。双端 L2 5/5＠`e6af7b3c2` |
| 2026-09-21 | wave701 | writers overlay 与 append_reloc 共用 reloc heap（扩 keep-globals）。FORCE parse P001 倒。新墙 CG002 `code_len=0`。产品保留 overlay。双端 L2 5/5＠`70312b027` |
| 2026-09-21 | wave700 | leftover ELF UND 帽 256→2048＋跳过空名。calloc 调 0 倒。新墙 FORCE parse P001。产品保留 overlay。双端 L2 5/5＠`ccf44fdad` |
| 2026-09-21 | wave699 | FORCE mega `parser_*` 不得盖 parser_x（export extern＋库 TU 跳过 seed）。新墙 GrowVec／sidecar 调 0。产品恢复。双端 L2 5/5＠`8b3ae2899` |
| 2026-09-21 | wave698 | leftover-weaken FORCE 能链；COMMON 尺寸墙倒。新墙 mega `parser_parse_into_init` 盖 parser_x（localize 后 stack-chk）。产品恢复。双端 L2 5/5＠`25e562753` |
| 2026-09-21 | wave697 | leftover compact WAVE273 writer 入链（非 elf_ctx_thin）。FORCE mega 297 COMMON 尺寸正确（size=1 仅 1）。双端 L2 5/5＠`e371c0495` |
| 2026-09-21 | wave696 | FORCE COMMON size=1＝sidecar 砸尺寸；writer 改 ctx offset 权威。产品 leftover writer 未入链（thin overlay 卡死 FORCE）。双端 L2 5/5＠`9730d918d` |
| 2026-09-21 | wave695 | FORCE 全链重建：get_copy 6 hit 已净。墙＝mega `u8[N]` COMMON size=1（thin 0x4400）。族原子 dep_ctx 后下一崩 elf_shndx sidecar。双端 L2 5/5＠`a0b0bb721` |
| 2026-09-21 | wave694 | value_abi 用 w693 编译器重编；C sret 二次 get_copy 入口已净（非 0x1800000001）。旧 force 混链 sidecar 崩→w695 全量重建。双端 L2 5/5＠`a0b0bb721` |
| 2026-09-21 | wave693 | sret home 越过 ret_sz；rest mega_body overlay 入链。探针 dest＠0x450 ≠ local。双端 L2 5/5＠`1aad86a9e` |
| 2026-09-21 | wave692 | sret 槽改在 fill_local 后预约（去掉 +256 空洞）；Type 532B 局部不再盖 hidden dest。双端 L2 5/5＠`df17ed485`。FORCE 复测待 Ubuntu 重编 rest mega_body |
| 2026-09-21 | docs | 日切归档 20 号（w618–w691）；21 号主文档重写简写。钉盘仍 `ecdb5cc1e`。 |
