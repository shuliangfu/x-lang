# Xlang / X language 自举进度

> **更新**：2026-09-02 · tip **🔔 升钉 `e8176cbe5` → `b5be5ed97`（双端 L4 129）** · 日切：1 日 **131** 行已入 [归档](自举进度-归档-2026-09-01.md) · **金标 SSH＝`ubuntu-server`** · Ubuntu 验＝**git pull（禁 scp）** · Ubuntu L2 **钉盘节奏**\
> **产品 L4 放行钉盘**：**`b5be5ed97`**（2026-09-02 升钉 · 前 `e8176cbe5` 2026-08-23 · 再前 `f7424ae47` 2026-08-15）\
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅**（12 labi_*.x 切片 prefer；默认 FROM_X=1）· parser **7.2.2 关 pin ✅**（tip `-E` + assemble；产品默认 FROM_X=1）· codegen 7.4.2 wave323 ✅ · typeck 7.4.1 wave322 ✅ · runtime monofile wave321 ✅ · present **0** · 钉盘 **`b5be5ed97`** · Stage 8 tip **wave332 Track L 30/30 FULLY CLOSED**（**PRODUCT HALF=0** · **PRODUCT PINNED=0** · **PRODUCT RETIRED 23/23 = 100%**）· **7.4.3 PREFER_X_O 双端 8/8 wave338 ✅** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁**\
> **tip L4 安全网（历史）**：**`ffc0b646f`**（已由 tip L4 升钉吸收；前网 `4bacaf18c`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：双端 L4＠`b5be5ed97` **`product_l4_true_cold OK` 129**（Ubuntu 17m47s／Darwin JOBS=1 137m25s）。pin 蛋自本波 `xlang_asm` 刷新。Darwin L4 默认 JOBS=1＋GATE_CASE_JOBS=1（JOBS=8／4／2 本机 OOM）。
> **下波候选**：Windows hybrid＠`e5f3029ec` SAT leftover-PE rest **dep_prerun_entry_dir OK**（pabi 1242510B，`xlang_dep_prerun_entry_dir`／`_pick` **T**）后 phase1 **UNDEF unique 56**（57→56）。残 `.x` thin＝独立 `xlang_merge_deps_path_already_out`（需 `_scan`），以及仍闭的 `one_ctx`／`map_impl`（parser_parse_into）与大块 `pipeline_asm_emit_expr_elf_rec`／`glue_type_size_simple`／`append_reloc_absolute64`（helper／dual-decl）；`pipeline_debug_trace_named_func_bodies` 仍闭（void* vs struct* extern）。残：path_cap／dir_cap WINDOWS；P2 Darwin `-o`。

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1；过程上 prefer+heat 一次／域，双端矩阵同波一次\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；**SHARED 双端 L2**；日常矩阵 **只** `./xbuild l2-matrix`（wave299：捕获 rv=42／opt=102 成功码，禁 set -e 截断）；谈自举／升钉才 L4 + 矩阵 + 全量 bstrict；bstrict **per-script timeout** Ubuntu 300s／Darwin L4 1200s + catalog cache；**墙钟**：`product_l4` 分步 wall + `run-all-bstrict`（`XLANG_BSTRICT_TIMING_FILE`）；并行 **`XLANG_BSTRICT_JOBS` 默认 1**（Ubuntu 金标 1；Darwin L4 默认 1／GATE_CASE_JOBS=1；JOBS=8／4／2 本机 OOM）

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[01](自举进度-归档-2026-09-01.md) · [31](自举进度-归档-2026-08-31.md) · [30](自举进度-归档-2026-08-30.md) · [29](自举进度-归档-2026-08-29.md) · [28](自举进度-归档-2026-08-28.md) · [27](自举进度-归档-2026-08-27.md) · [26](自举进度-归档-2026-08-26.md) · [25](自举进度-归档-2026-08-25.md) · [24](自举进度-归档-2026-08-24.md) · [23](自举进度-归档-2026-08-23.md) · [22](自举进度-归档-2026-08-22.md) · [21](自举进度-归档-2026-08-21.md) · [20](自举进度-归档-2026-08-20.md) · [19](自举进度-归档-2026-08-19.md) · [18](自举进度-归档-2026-08-18.md) · [17](自举进度-归档-2026-08-17.md) · [16](自举进度-归档-2026-08-16.md) · [15](自举进度-归档-2026-08-15.md) · [14](自举进度-归档-2026-08-14.md) · [13](自举进度-归档-2026-08-13.md) · [12](自举进度-归档-2026-08-12.md) · [11](自举进度-归档-2026-08-11.md) · [09](自举进度-归档-2026-08-09.md) · [08](自举进度-归档-2026-08-08.md) · [07](自举进度-归档-2026-08-07.md) · [06](自举进度-归档-2026-08-06.md) · [05](自举进度-归档-2026-08-05.md) · [04](自举进度-归档-2026-08-04.md) · [03](自举进度-归档-2026-08-03.md) · [02](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · …\

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `b5be5ed97`**（2026-09-02 升钉 · 前 `e8176cbe5` 2026-08-23 · 再前 `f7424ae47` 2026-08-15）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**双端 Stage2 = ✅ SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709` · `xlang_asm_stage1`／`xlang_asm2` bit-identical）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（**变更记录只在本文 §6**）。\
**本波主刀 = Windows hybrid min-gate（phase1 leftover PE 不能 `-E` 的 `.x` thin：独立 `xlang_merge_deps_path_already_out`／`_scan`；`one_ctx`／`map_impl` 与大块 rec／glue_type_size／`append_reloc_absolute64` 仍闭；钉盘仍 `b5be5ed97`）**。

| 指标 | 状态 |
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`b5be5ed97`**；residual tip present **0**；Stage 8 **30/30 FULLY CLOSED**；PRODUCT RETIRED 23/23 |
| Track MG | **11.3.1 MG ✅** |
| Track BC | **🟢 Stage8 30/30** · M4 **5/5** · present **0**；`pipeline_x` mega 仍 host-cc |
| Track L | **收 30/30 FULLY CLOSED** |
| mac / Ubuntu L4（钉盘） | ✅ 双端 L4 + bstrict **129** @ **`b5be5ed97`** |
| pin 蛋 | ✅ darwin.arm64 + linux.x86_64（本波 `xlang_asm`） |
| 本波构建级别 | 钉盘 **L4 已结＠b5be5ed97**；Ubuntu **129 OK** 17m47s；Darwin JOBS=1 **129 OK** 137m25s。check 闸门暂停 |

***

## 1. 文档地图

| 文档 | 看什么 |
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录**（**仅 9 月 2 日**） |
| [自举进度-归档-2026-09-01.md](自举进度-归档-2026-09-01.md) | **9月1日** typeck pin 7.4.6–8／L4 多轮／Cap va／stage10（**131** 行） |
| [自举进度-归档-2026-08-31.md](自举进度-归档-2026-08-31.md) | **8月31日** stage10 开刀／seed dest-SLICE／leftover／假权威诚实等（**113** 行） |
| [自举进度-归档-2026-08-30.md](自举进度-归档-2026-08-30.md) | **8月30日** leftover XLANG fallthrough F-06／F-07 等（**111** 行） |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图（本波不改） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单；per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`b5be5ed97`** | 2026-09-02 升钉；双端 L4 真冷 + 129 |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自钉盘 L4 `xlang_asm` 刷新 |
| residual tip（BC） | present **0** | Stage 8 30/30 · M4 5/5 · prefer pure-asm 默认 · pipeline_abi mega **硬禁** |
| mac 钉盘 L4 | ✅ L4 + bstrict **129** · 137m25s JOBS=1 | `@b5be5ed97` `/tmp/xlang_l4_Darwin_b5be5ed97_jobs1.log` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 + bstrict **129** · 17m47s | `@b5be5ed97` `ubuntu-server` `/tmp/xlang_l4_Linux_b5be5ed97.log` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | rv42／opt102／hello0／si0／f32 |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ 双端 tip SHA256 STRICT match | 钉盘历史见归档 |
| **R2 / Cap** | 按需 | 明细见 29／31／1 日归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1** |
| `TREE_ARMED` / `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1** / **1** |
| post-delete residual | **✅** wave944–964 |

***

## 3. 当前前排（≤3 条）

1. **Windows hybrid 门** — 已切机。tip `e5f3029ec` seed **52 leaf**＋SAT merge **OK** pabi **1242510B**（`xlang_dep_prerun_entry_dir`／`_pick` **T**）；phase1 unique **56** `xlang_merge_deps_path_already_out`／`xlang_pipeline_one_ctx_for_dep_prerun`／`pipeline_asm_emit_expr_elf_rec`／`glue_type_size_simple`／`append_reloc_absolute64`。leftover PE `-E` 挂死已闭。
2. **邻域禁区** — **禁** mega prefer／默认 assemble parser.x；**禁** `rm` hybrid egg；**禁** nest>64；**禁**改 al06／print_any／shuffle／io-context／fs／channel／net-context 期望糊绿；pipeline_abi mega **硬禁**。
3. **下一刀** — leftover PE 不能 `-E` 的 `.x` thin（独立 `xlang_merge_deps_path_already_out`／`_scan`；`one_ctx`／`map_impl` 仍闭；大块 rec／glue_type_size／`append_reloc_absolute64` 仍闭）。残：path_cap／dir_cap WINDOWS；P2 Darwin `-o`。

***

## 4. 与归档的关系

- **9月1日归档**：[自举进度-归档-2026-09-01.md](自举进度-归档-2026-09-01.md) — **131** 行 · 钉盘仍 **`e8176cbe5`**。
- **8月31日及前**：见各日归档链接（§1）。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `b5be5ed97`**。
- 日切后主文档只保留 KPI + 前排 + **9 月 2 日** §6；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
| **macOS arm64** | L4＠**`b5be5ed97` JOBS=1** **`product_l4_true_cold OK` 129** wall **137m25s**（bstrict 130m27s；hello／fs／channel／STD-092 **OK**）／矩阵 **5／5** · 钉盘 **`b5be5ed97`** |
| **Ubuntu x86_64（金标）** | L4＠**`b5be5ed97`** **`product_l4_true_cold OK` 129** wall **17m47s** · SSH＝**`ubuntu-server`** · 钉盘 **`b5be5ed97`** |
| **Windows MSYS2** | 已切机 · min-gate＠`e5f3029ec` **52 leaf**＋SAT merge **OK** pabi 1242510B `xlang_dep_prerun_entry_dir`／`_pick` **T** · phase1 unique **56** `xlang_merge_deps_path_already_out`／`xlang_pipeline_one_ctx_for_dep_prerun`／`pipeline_asm_emit_expr_elf_rec`／`glue_type_size_simple`／`append_reloc_absolute64` · 10.1.3 NT **暂缓** |

***

## 6. 变更记录（倒序 · **9 月 2 日**；1 日见 [归档](自举进度-归档-2026-09-01.md)）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-02 | **fix(windows)** **pabi leftover-PE rest 编 dep_prerun_entry_dir／_pick** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略独立 `xlang_dep_prerun_entry_dir`／`_pick` ifndef（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 `entry_dir`；header 声明 `entry_dir`（const char*），seed 定义（非 dual-decl）；G-02f-51 始终编译 `_pick` 原型＋定义（非 dual）；`entry_dir` 调 `_pick`。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs（`rt_run_asm_backend`／`rt_run_x_emit`／`rt_run_compiler_parsed`）。**修**：G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 独立 ifndef（与 user_std_net 同模式）。大块 rec／glue_type_size／`append_reloc_absolute64` 仍闭。**证**：tip `e5f3029ec` min-gate 52 leaf · SAT prefer hybrid **1242510B** · 两枚簇 **T** · phase1 unique **57→56**。path_cap link_abi cc 仍侧红。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：独立 `xlang_merge_deps_path_already_out`／`_scan`（`one_ctx`／`map_impl` 仍闭；大块仍闭）。 |
| 2026-09-02 | **fix(windows)** **pabi leftover-PE rest 编 user_std_net／skip_typeck 簇** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略独立 `xlang_asm_user_std_net_dep_path`／`std_dep_skip_x_typeck`／`std_io_driver_dep_path`／`dep_parse_skip_typeck_path` ifndef（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无这四枚；header 声明四 wrapper（const char*），seed 定义（非 dual-decl）；wrapper 转调 always-extern pipeline.x 面；`dep_parse_skip_typeck_path` 调 net＋io_driver。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs（`rt_run_asm_backend`／`runtime_driver_no_c`）。**修**：G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 独立 ifndef（与 is_object／fclose 同模式）。大块 rec／glue_type_size／`append_reloc_absolute64` 仍闭。**证**：tip `11fec48a4` min-gate 52 leaf · SAT prefer hybrid **1242183B** · 四枚簇 **T** · phase1 unique **60→57**。path_cap link_abi cc 仍侧红。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：独立 `xlang_dep_prerun_entry_dir`／`_pick`（大块仍闭）。 |
| 2026-09-02 | **fix(windows)** **pabi leftover-PE rest 编 is_object／fclose 簇** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略独立 `xlang_asm_out_buf_is_object`／`_magic`／`driver_asm_fclose_asm_out`／`fp_is_stdout`／`fclose_file` ifndef（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无这五枚；header 声明 `fclose_asm_out`／`is_object`（FILE*／unsigned char*），seed 定义（非 dual-decl）；`is_object` 薄转调 `_magic`。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs（`rt_run_asm_backend`／`runtime_driver_abi`）。**修**：G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 独立 ifndef（与 wave67／path wrappers 同模式）。大块 rec／glue_type_size／`append_reloc_absolute64` 仍闭。**证**：tip `17b339fec` min-gate 52 leaf · SAT prefer hybrid **1241432B** · 五枚簇 **T** · phase1 unique **62→60**。path_cap link_abi cc 仍侧红。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：独立 `xlang_asm_user_std_net_dep_path`／`std_dep_skip_x_typeck`／`dep_parse_skip_typeck_path`（大块仍闭）。 |
| 2026-09-02 | **fix(windows)** **pabi leftover-PE rest 编 wave67／68／70 dep slots** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略独立 wave67／68／70 ifndef（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 `pipeline_set_dep_slots`／`set_entry_dir`／`get_dep_arena_slot`／`path_bufs_reset`／`copy_entry_dir`；BSS `entry_dir_buf`／`dep_*_slots` 与写者同 ifndef；header／seed 原型声明、seed 定义（非 dual-decl）；fill_ctx／pctx_seed wrapper 薄转调，`_impl` 已始终编译。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 独立 ifndef（与 wave123／path wrappers 同模式）。大块 rec／glue_type_size／`append_reloc_absolute64` 仍闭。**证**：tip `6a259f4ae` min-gate 52 leaf · SAT prefer hybrid **1240461B** · 十一枚 wave67／68／70 **T** · phase1 unique **72→62**。path_cap link_abi cc 仍侧红。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：独立 `xlang_asm_out_buf_is_object`／`_magic`／`driver_asm_fclose_asm_out`（大块仍闭）。 |
| 2026-09-02 | **fix(windows)** **pabi leftover-PE rest 编 path wrappers** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略独立 G-02f-229／231／232／63／wave79 path wrapper ifndef（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 `xlang_get_entry_dir`／`import_path_*`／`resolve_*`／`path_try_realpath_inplace`；`_impl` 已始终编译，wrapper 薄转调；header 声明五枚公开函数、seed 定义（非 dual-decl）；`cstr_ends_with_dot_x` 是 `import_path_is_file_path` 助手。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 独立 ifndef（与 wave123／wave73 同模式）。大块 rec／glue_type_size／`append_reloc_absolute64` 仍闭；`pipeline_set_dep_slots` 本波仍闭。**证**：tip `6b807cc35` min-gate 52 leaf · SAT prefer hybrid **1236988B** · 七枚 wrapper **T** · phase1 unique **78→72**。path_cap link_abi cc 仍侧红。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：独立 `pipeline_set_dep_slots`／`pipeline_set_entry_dir`（大块仍闭）。 |
| 2026-09-02 | **fix(windows)** **pabi leftover-PE rest 编 wave73 pipeline_diag** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略独立 wave73 `pipeline_diag_*` ifndef（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 `pipeline_diag_emitted_get`／`emitted_reset`／`import_open_fail_once`／`merge_dep_missing`；header 声明函数、seed 定义（非 dual-decl）。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 独立 ifndef（与 wave123／wave74 同模式）。大块 rec／glue_type_size／`append_reloc_absolute64` 仍闭；path wrappers／`pipeline_set_dep_slots` 本波仍闭。**证**：tip `45d1f5265` min-gate 52 leaf · SAT prefer hybrid **1235828B** · 十一枚 `pipeline_diag_*` **T** · phase1 unique **82→78**。path_cap link_abi cc 仍侧红。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：独立 path wrappers／`pipeline_set_dep_slots`（大块仍闭）。 |
| 2026-09-02 | **fix(windows)** **pabi leftover-PE rest 编 wave74 driver_dep／wave77 typeck_ndep** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略独立 wave74／wave77 ifndef（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无八枚 `driver_dep_*`／`typeck_ndep_store`／`typeck_dep_*_set`；header 声明函数、cold twin 数组在函数背后（非 dual-decl）；`clear_all` 写 typeck sidecar 数组。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：G.7 补 `WIN_LEFTOVER_GROW_VEC` OR 独立 ifndef（与 wave123／125 同模式）。大块 rec／glue_type_size／`append_reloc_absolute64` 仍闭。**证**：tip `452c8ea51` min-gate 52 leaf · SAT prefer hybrid **1232307B** · 八枚 `driver_dep_*`＋`typeck_ndep_store`／`typeck_dep_*_set` **T** · phase1 unique **93→82**。path_cap link_abi cc 仍侧红。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：独立 `pipeline_diag_*`／path wrappers（大块仍闭）。 |
| 2026-09-02 | **fix(windows)** **pabi leftover-PE rest 编 wave273 F7 data-section** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略 wave273 内 F7（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 `emit_data_len`／`append_data_u32_le`／`set_shndx_override`；整块打开 wave273 会 `append_bytes` dual-decl／BSS-after-use；F7 还被 wave154／wave178 外层 `#ifndef FROM_X` 包住。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs（`backend_call_dispatch`）。**修**：G.7 补 `WIN_LEFTOVER_GROW_VEC` close／reopen 外层＋wave273，BSS 提前，只编 F7 data-section。大块 rec／glue_type_size／`append_reloc_absolute64` 仍闭。**证**：tip `f5187998c` min-gate 52 leaf · SAT prefer hybrid **1226494B** · F7 三枚 **T** · phase1 unique **96→93**。path_cap link_abi cc 仍侧红。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：`pipeline_asm_emit_expr_elf_rec`／`glue_type_size_simple`（大块仍闭）。 |
| 2026-09-02 | **fix(windows)** **pabi leftover-PE rest 编 wave133 glue_enc sxt／zxt** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略 wave132 内 sxt／zxt（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 `glue_enc_sxt／zxt`；整块打开 wave132 会 `ty_ref` undeclared／`append_bytes` `uint8_t*` vs `void*` dual-decl。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：G.7 补 `WIN_LEFTOVER_GROW_VEC` close／reopen wave132，只编三枚 sxt／zxt（走 wave123 的 `uint8_t*` `append_bytes`）。大块 wave149／154／273／driver_dep 仍闭。**证**：tip `f0705ed51` min-gate 52 leaf · SAT prefer hybrid **1224817B** · `glue_enc_sxt_i32`／`zxt_u32`／`zxt_u8` **T** · phase1 unique **99→96**。path_cap link_abi cc 仍侧红。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：`pipeline_asm_emit_expr_elf_rec`／`glue_type_size_simple`（大块仍闭）。 |
| 2026-09-02 | **fix(windows)** **pabi leftover-PE rest 编 wave123／125 glue_arm64_mov／ctx_layout** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略 wave123／wave125（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 `glue_arm64_mov_*`／`pipeline_asm_ctx_layout`。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs。**修**：G.7 补 `WIN_LEFTOVER_GROW_VEC` 打开 wave123＋wave125 独立 ifndef。大块 wave149／154／273／driver_dep 打不开（undeclared helper／`void*` dual-decl）。**证**：tip `b5b5fb318` min-gate 52 leaf · SAT prefer hybrid **1223954B** · `glue_arm64_mov_x0_to_x8_elf_c`／`pipeline_asm_ctx_layout` **T** · phase1 unique **102→99**。path_cap link_abi cc 仍侧红。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：`pipeline_asm_emit_expr_elf_rec`／`glue_type_size_simple`（大块仍闭）。 |
| 2026-09-02 | **fix(windows)** **pabi leftover-PE rest 编 grow_vec／sidecar cold twin** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝FROM_X rest 省略 wave271／wave275（POSIX 由 `.x` thin `-E` 提供 T）；leftover PE 不能 `-E` mega；leftover standalone 无 `grow_vec`／sidecar；wave271 还被 wave154／wave178 外层 `#ifndef FROM_X` 包住。存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs（pabi 自己 U 411 refs）。**修**：G.7 补 `XLANG_RUNTIME_PIPELINE_ABI_WIN_LEFTOVER_GROW_VEC` 打开 wave271＋wave275；临时合上外层 FROM_X 再打开（wave272–274 仍冷路径）；`W277_NEED_GROWVEC` 防 FROM_X rest 重 typedef。**证**：tip `878b3fd10` min-gate 52 leaf · SAT log prefer hybrid **1221832B** · `grow_vec_at`／`arena_sidecar_get` **T** · pabi U grow_vec／sidecar **0** · phase1 unique **113→102**（leftover standalone **0／102** remaining）。path_cap link_abi cc 仍侧红。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：`glue_*`／`pipeline_asm_emit_expr_elf_rec` cold twin。 |
| 2026-09-02 | **fix(windows)** **pabi FROM_X rest＋leftover standalone merge** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝SAT FORCE keep 85KiB leftover pabi（0／49 twins）；cold seed 251 `void*`／`struct*`；leftover PE 不能 `-E` mega `.x`；存储＝`src/runtime_pipeline_abi.o`；消费＝phase1 57 objs（原 `parser_asm_thin_glue`／`asm_backend_partial`）。**修**：G.7 补 POSIX hybrid rest CC（`FROM_X`）＋`pure_ld_partial_merge` rest-first＋leftover `pipeline_glue_standalone.o`；MinGW `pure_ld_multidef_flags` `--allow-multiple-definition`。**证**：tip `f3cacbc51` min-gate seed **52／52** · SAT log prefer hybrid **1215720B** · `pipeline_type_elem_ref_at` **T** · phase1 **FAIL** unique **113**（pabi 自己 U `grow_vec_at`／sidecar 411 refs — FROM_X rest 省略 ifndef-FROM_X cold twin，POSIX 由 `.x` thin `-E` 提供）。禁改测试期望。PLATFORM: WINDOWS leftover-PE hybrid／POSIX `-E` 不变。下一刀：grow_vec／sidecar cold twin（不是 path_cap）。 |
| 2026-09-02 | **fix(windows)** **migrate-gen leftover PE tip `-E` 启动前 skip** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝`typeck_run_tip_e`／`codegen_run_tip_e` 对 leftover PE 无 timeout 循环 `-E` typeck.x／codegen.x；存储＝`xlang.exe`／`xlang_asm`／`xlang-c`；消费＝ensure_prereqs migrate-gen。同模式＝parser_run_tip_e（有 timeout）。**证**：tip `dcc6e9577` 杀 SAT 挂死后 leftover PE 空闲，prereqs 日志冻 807B（lexer 后 typeck tip `-E`），heartbeat 三二进制轮转。上次 PE 已挂所以 tip `-E` 秒失败走 pin。**修**：G.7 补 migrate-gen 谓词，三入口启动前 skip→archaeology pin。POSIX 金标仍 `-E`。**收割** tip `4b3153dfb` min-gate：typeck skip **OK** · pabi inject／SAT FORCE skip **OK** · seed **52／52** · SAT／lsp／bridge／user-asm **OK** · phase1 **CC residual 57 objs FAIL** UNDEF `pipeline_*`／`ast_pool_*`（leftover `pipeline_x.o` 对不上 tip glue）。禁改测试期望。PLATFORM: WINDOWS skip／POSIX 金标不变。下一刀：phase1 leftover `pipeline_x.o` UNDEF。 |
| 2026-09-02 | **fix(windows)** **SAT FORCE leftover PE mega `-E` 启动前 skip** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝SAT `-B` `XLANG_HOST_CC_SEED_FORCE=1` 跳过 inject-only keep，`ensure_pipeline_abi_prefer_one` 对 mega `runtime_pipeline_abi.x` 起 leftover PE `-E`（stderr 丢弃）；存储＝`xlang.exe` RSS ~3GB；消费＝min-gate seed 卡 SAT。同模式＝pabi thin inject／ptrslot／fnptr-arr-esz。**证**：tip `407040853` seed **52／52** 后 `pipeline-x` OK，SAT 编完 diagnostic 后日志冻 67180B（10:21–10:34+），heartbeat `proc=xlang`。**修**：G.7 谓词 `pipeline_abi_windows_leftover_pe_cannot_e`：FORCE hybrid／generic inject／ptrslot／fnptr-arr-esz **启动前** skip，保留既有 hybrid `.o`。POSIX 金标仍 `-E`。禁改测试期望。PLATFORM: WINDOWS skip／POSIX 金标不变。下一刀：path_cap／dir_cap WINDOWS。 |
| 2026-09-02 | **fix(windows)** **pipeline_abi leftover `-E` 保 hybrid `.o`** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝7/31 leftover PE 在所以「无 xlang」跳过不触发，但该 PE 不能 `-E` tip pipeline_abi thin；inject-only 路径 ptrslot `return 1`；存储＝`pipeline_abi_inject_thin_leaf`；消费＝52 叶 `src/runtime_pipeline_abi.o`。同模式＝全部 pabi thin inject。**证**：tip `7171aa43c` min-gate seed **51／1** 正确 abort（仅 pipeline_abi；`runtime_driver_no_c.o` hybrid slices **OK**）。**修**：G.7 补全 inject：MINGW／MSYS `-E` 失败保留既有 hybrid `.o`（与无二进制 skip 同语义）。POSIX 仍硬失败。禁改测试期望。PLATFORM: WINDOWS inject／POSIX 金标不变。下一刀：path_cap／dir_cap WINDOWS。 |
| 2026-09-02 | **fix(windows)** **sys/wait.h MinGW shim** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝MinGW 无 `<sys/wait.h>`，`rt_run_exec`／`rt_dispatch_thin` `#include` 后 rt-prefer 切片不全、拒 monofile；存储＝`include/sys/`（已有 `uio.h`）；消费＝`src/runtime_driver_no_c.o` 52 叶。同模式＝`sys/uio.h` `#include_next`。**证**：tip `fba49aba9` min-gate seed **50／2** 正确 abort（pipeline_abi leftover `-E` inject／driver_no_c `sys/wait.h`）。win32_compat `read`／`write`／`rmdir` 已过。**修**：G.7 补 `include/sys/wait.h`→`win32_compat` waitpid；补 `WNOHANG`。禁改测试期望。PLATFORM: WINDOWS。下一刀：pipeline_abi Windows `-E`／path_cap／dir_cap。 |
| 2026-09-02 | **fix(windows)** **win32_compat POSIX read／write／rmdir** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝MinGW CRT 只有 `_read`／`_write`／`_rmdir`，-E 预热发 POSIX `read()`；存储＝`win32_compat.h`（已有 `access`→`_access`）；消费＝rt-prefer `src/runtime_driver_no_c.o`。同模式＝三份 -E prologue。**证**：tip `2d5aa9894` min-gate seed **51／1** 正确 abort。**修**：G.7 补 alias＋prologue `#ifdef _WIN32` include。禁改测试期望。PLATFORM: WINDOWS。下一刀：path_cap／dir_cap WINDOWS。 |
| 2026-09-02 | **fix(windows)** **compiler-make 吞 rc＋hybrid dispatch** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝`xlang_compiler_make` `if ! dispatch; then rc=$?`（`$?` 恒 0）＋hub 无 `bootstrap-driver-hybrid` 臂；存储＝CLI 退出码／phony 表；消费＝Windows min-gate `_win_cm`。同模式＝一切 `bash compiler-make.sh <phony>`。**证**：tip `7ce9e67d0` seed **50／2**（pipeline_abi／runtime_driver_no_c）后仍进 hybrid，`try-heat bootstrap-driver-hybrid` 卡 catalog。**修**：G.7 先取 rc 再判断；补 hybrid／asm → `bootstrap_driver_hybrid.sh`；gate 不再管道函数、日志落到 TMPDIR。禁改测试期望。PLATFORM: SHARED hub／WINDOWS gate。下一刀：Windows Cap POSIX。 |
| 2026-09-02 | **fix(windows)** **min-gate 行缓冲日志 → C:/xlang_tmp/win_gate.log** | **代码** · **不升钉**。非 TTY SSH 下 `tee /tmp` 全缓冲，Windows 上看不到进度。G.7 补 gate：`stdbuf -oL` tee 到 **`C:/xlang_tmp/win_gate.log`**。看：`Get-Content C:\\xlang_tmp\\win_gate.log -Wait -Tail 40` 或 Notepad++ Monitoring。PLATFORM: WINDOWS。 |
| 2026-09-02 | **fix(windows)** **钉 TMPDIR=C:/xlang_tmp（min-gate 首跑红）** | **代码＋文档** · **不升钉** · Windows 探针。五问：产生＝`select_bootstrap_xlangc` can_run 硬编码 `/tmp`＋ensure `mktemp ${TMPDIR:-/tmp}`；存储＝TMPDIR／TEMP；消费＝MinGW `cc1.exe`（Win32，不懂 MSYS `/tmp`）。同模式＝`driver_leaf`／rt-prefer。**证**：tip `d752efd83` hybrid min-gate `cc1: /tmp/rtpref_x.* No such file`；`select_bootstrap_xlangc: no runnable seed`（7/31 PE 在，探针路径错）；`pipeline_x.o` 考古回退 `#include pipeline_glue.c`（已删）。**修**：G.7 补 `ci_windows_pin_tmpdir`；gate 先钉 `C:/xlang_tmp`；can_run 走 TMPDIR；uname `mingw*`→windows。禁改测试期望。PLATFORM: WINDOWS。下一刀：Windows pull 再跑 min-gate。 |
| 2026-09-02 | **docs** **Windows「明天」到期（不切机）** | **文档** · 昨晚承诺「明天再跑 Windows」＝**今天 9/2**；禁止再滚动写「明天」。现 SSH `ubuntu-server`＝Linux（uptime 5d）；`windows-server` 同 IP 也是 Linux，Windows **没在跑**。钉盘已升 **`b5be5ed97`**，前置齐。要验须本机重启进 Windows（GRUB）；切机前 Ubuntu 金标保持在线、本机继续 P2。 |
| 2026-09-02 | **docs+pin** **🔔 升钉 e8176cbe5 → b5be5ed97** | **钉盘** · 双端 L4 remaining_o=0 · 矩阵 **5／5** · bstrict **129**。Ubuntu **`product_l4_true_cold OK`** 17m47s `/tmp/xlang_l4_Linux_b5be5ed97.log`。Darwin JOBS=1 **OK** 137m25s（bstrict 130m27s；hello 21s／fs 94s／channel 18s／STD-092 26s；megas assign-index-expr **712s**／cfg-merge **658s**）`/tmp/xlang_l4_Darwin_b5be5ed97_jobs1.log`。pin 蛋自本波 `xlang_asm` 刷新。L4 Darwin 默认 **JOBS=1＋GATE_CASE_JOBS=1**（JOBS=8／4／2 本机 jetsam；auto-inner=2 会再 OOM）。禁改期望。PLATFORM: SHARED pin／MACOS L4 编排／LINUX 金标。当时写「Windows 明天」＝今天 9/2，已改到期。 |
| 2026-09-02 | **test(selfhost)** **Darwin L4＠b5be5ed97 JOBS=2 OOM（不升钉）** | **真冷** · 同 SHA Ubuntu **129** 17m47s。Darwin JOBS=2 矩阵 **5／5** 后 `run-hello.sh` 产品 `-o` **ec=137**×3；wall 17m51s `/tmp/xlang_l4_Darwin_b5be5ed97.log`。串行 leftover hello **OK** → jetsam（~2GB free），非产品红。回退 JOBS=1＋GATE_CASE_JOBS=1 同 SHA 再跑。**当时禁升钉**。 |
| 2026-09-02 | **perf(selfhost)** **Darwin L4 回退 JOBS=2（JOBS=4 仍 OOM）** | **代码＋文档** · **不升钉**。五问：产生＝L4 Darwin 默认 JOBS=4；存储＝`XLANG_BSTRICT_JOBS`／`gate_case_jobs` outer≤2 自动 inner=2（总并发 4）；消费＝Darwin L4＠`f63f3f959` void-main **Killed:9**／typeck／fmt-wrap **ec=137**（bstrict 10m26s）。同模式＝JOBS=8。**证**：同 SHA 串行 typeck 29／void-main 2／fmt-wrap 5／check **OK**；Ubuntu L4＠`f63f3f959` **129** 17m48s。**修**：G.7 补全 L4 Darwin 默认 JOBS=2（packing ~70–80 min）；`gate_case_jobs` 仅 outer=1 自动 inner=2（总并发 ≤2）。禁砍脚本／禁改期望。JOBS=1 可回退。钉盘仍 **`e8176cbe5`**。PLATFORM: MACOS L4 编排／LINUX 金标仍 JOBS=1。下一刀：双端 L4 同 SHA。 |
| 2026-09-02 | **test(selfhost)** **双端 L4＠f63f3f959（不升钉）** | **真冷** · remaining_o=0 · 双端 phase1／g05／矩阵 **5／5 OK**。Ubuntu **`product_l4_true_cold OK`** bstrict **129** wall **17m48s** `/tmp/xlang_l4_Linux_f63f3f959.log`。Darwin JOBS=4 **FAIL**：void-main **Killed:9**；typeck／fmt-wrap **ec=137**；wall **17m34s** `/tmp/xlang_l4_Darwin_f63f3f959.log`。串行同二进制四门 **OK** → 编排 OOM，非产品红。**禁升钉**。Windows 明天。下一刀：JOBS=2。 |
| 2026-09-02 | **perf(selfhost)** **Darwin L4 回退 JOBS=4＋CASE_TIMEOUT 180s** | **代码＋文档** · **不升钉**。五问：产生＝L4 Darwin 默认 JOBS=8；存储＝`XLANG_BSTRICT_JOBS`／`XLANG_CASE_TIMEOUT`／`gate_run_timeout` 把 SIGKILL 报成 124；消费＝Darwin L4＠`f6c511aab` while／typeck 「timeout」＋stdlib-import **Killed:9**（bstrict 10m33s）。同模式＝fmt-wrap。**证**：同 SHA 串行 while run=6／typeck 29／fmt-wrap 5／stdlib-import 1 **OK**；Ubuntu L4＠`f6c511aab` **129** 17m51s。**修**：G.7 补全 L4 Darwin 默认 JOBS=4（packing ~35–40 min）；Darwin L4 CASE_TIMEOUT 180s（typeck 默认 30s 并行会假红）；SIGKILL→137 不再冒充 timeout。禁砍脚本／禁改期望。JOBS=2／1 可回退。钉盘仍 **`e8176cbe5`**。PLATFORM: MACOS L4 编排／LINUX 金标仍 JOBS=1。下一刀：双端 L4 同 SHA。 |
| 2026-09-02 | **test(selfhost)** **双端 L4＠f6c511aab（不升钉）** | **真冷** · remaining_o=0 · 双端 phase1／g05／矩阵 **5／5 OK**。Ubuntu **`product_l4_true_cold OK`** bstrict **129**（http／fs／io-context／shuffle-select／fmt-std／channel／STD-092）wall **17m51s** `/tmp/xlang_l4_Linux_f6c511aab.log`。Darwin JOBS=8 **FAIL**：stdlib-import **Killed:9**；while／typeck／fmt-wrap 报 timeout（SIGKILL 映射）；wall **17m43s** `/tmp/xlang_l4_Darwin_f6c511aab.log`。串行同二进制四门 **OK** → 编排 OOM，非产品红。**禁升钉**。Windows 明天。下一刀：JOBS=4。 |
| 2026-09-02 | **perf(selfhost)** **Darwin L4 JOBS=8＋timeout 1200s** | **代码＋文档** · **不升钉**。五问：产生＝JOBS=4 packing 地板仍 ~35–40 min（sum/4）；最长 asm 门 840s 未到地板。存储＝`XLANG_BSTRICT_JOBS`（cap 8）／`XLANG_BSTRICT_SCRIPT_TIMEOUT`。消费＝Darwin L4 反馈环。**修**：G.7 补全 L4 Darwin 默认 JOBS=8（packing ~ max(840s, sum/8) ≈ 18–20 min bstrict）；Darwin L4 timeout 1200s（mega 门超产品默认 300s；本机无 timeout(1) 时本无影响，装了 coreutils 会假红）。Ubuntu 仍 JOBS=1／300s。禁砍脚本／禁改期望。JOBS=4／2／1 可回退。钉盘仍 **`e8176cbe5`**。PLATFORM: MACOS L4 编排／LINUX 金标仍 JOBS=1。下一刀：双端 L4 同 SHA。 |
| 2026-09-02 | **perf(selfhost)** **Darwin L4 JOBS=4＋asm case pool** | **代码＋文档** · **不升钉**。五问：产生＝129 门串行＋门内 45 次 `-o`（assign-index-expr ~840s）；存储＝`XLANG_BSTRICT_JOBS`／新 `gate_case_*`；消费＝Darwin L4 ~2.5h（Ubuntu 同套 17m）。同模式＝已有 bstrict 并行池。**修**：G.7 补全 L4 Darwin 默认 JOBS=4（18 核／64GB；packing ~35–40 min bstrict）；`gate-progress.sh` 补 intra-script case pool（JOBS≤2 时 Darwin 自动 2；JOBS≥4 自动 1，总并发 ≤4）；四条最慢 asm 门接入；Darwin heavy／types cooldown 默认 0。禁砍脚本／禁改期望。JOBS=1／2 可回退。钉盘仍 **`e8176cbe5`**。PLATFORM: MACOS L4 编排／LINUX 金标仍 JOBS=1。下一刀：Darwin JOBS=4 L4；Ubuntu 同 SHA 再 L4 才能升钉。 |
| 2026-09-02 | **test(selfhost)** **Ubuntu L4＠48979da1c 129 OK（不升钉）** | **真冷** · remaining_o=0 · 矩阵 **5／5** · bstrict **129**（http／fs／io-context／shuffle-select／fmt-std／channel／**STD-092 net-context 7s**）wall **17m47s** `/tmp/xlang_l4_Linux_48979da1c.log`。connect／accept clash 金标闭。**禁升钉**（Darwin 同 SHA 未绿）。Windows 明天。 |
| 2026-09-02 | **perf(selfhost)** **Darwin L4 bstrict 默认 JOBS=2** | **代码＋文档** · **不升钉**。五问：产生＝`product_l4_true_cold.sh` 不设 JOBS → `run-all-bstrict` 默认 1；存储＝env `XLANG_BSTRICT_JOBS`；消费＝Darwin 串行 asm 门 assign-index-expr ~840s／binop-cfg-merge ~760s → 全 L4 ~2.5h（Ubuntu 同套 17m）。同模式＝已有并行池（Ubuntu opt-in 2–4；Darwin 曾慎 OOM）。**修**：G.7 补全 L4 入口 Darwin 默认 JOBS=2（18 核／64GB；并行跳过 Darwin sleep）；Ubuntu 仍 1。禁砍脚本／禁改期望。JOBS=1 可回退。钉盘仍 **`e8176cbe5`**。PLATFORM: MACOS L4 编排／LINUX 金标仍 JOBS=1。下一刀：收割 Ubuntu L4＠`48979da1c`＋Darwin JOBS=2 L4。 |
| 2026-09-02 | **fix(formal_mod)** **Darwin net-context connect／accept libc-clash** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝Darwin wrapper 把 `std/net/mod.x` `export connect` 3 参／`accept` 2 参做成 `static connect`／`static accept`；存储＝`xlang_compile_std_module.sh` `_libc_clash`（无 connect／accept）；消费＝`cc -c` 撞 `sys/socket.h` POSIX `connect(2)` → 无 `net.o` → L4 STD-092 UNDEF `_std_net_*`。同模式＝listen／send。**修**：G.7 补表 connect／accept＋POSIX accept arity 3；dns_fast 补 `<fcntl.h>`（禁塞进 `xlang_net_cap.h`，open／variadic fcntl 会炸 KEEP_C）。禁改 context_connect 测试／禁 SHARED `#define`。证：Darwin `net.o` T `_std_net_connect`／t `_xlang_formal_bare_connect`／STD-092 **OK** run=1／矩阵 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: MACOS formal_mod＋POSIX dns_fast include。下一刀：Ubuntu L2 STD-092＋双端 L4。 |
| 2026-09-02 | **test(selfhost)** **双端 L4＠4cb6ef907（不升钉）** | **真冷** · remaining_o=0 · 双端 phase1／g05／矩阵 **5／5 OK**。Ubuntu **`product_l4_true_cold OK`** bstrict **129**（http／fs／io-context／shuffle-select／fmt-std／channel）wall 17m43s `/tmp/xlang_l4_Linux_4cb6ef907.log`。Darwin run-check／run-http／run-fs／run-channel **OK**（send／recv clash 冷链闭）后 **run-std-net-context-gate.sh FAIL**：`static connect` 撞 POSIX `connect` → UNDEF `_std_net_*` wall 154m41s `/tmp/xlang_l4_Darwin_4cb6ef907.log`。**禁升钉**。Windows 明天。下一刀：net connect／accept clash。 |
| 2026-09-02 | **fix(formal_mod)** **Darwin channel send／recv libc-clash** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝Darwin wrapper 把 `std/channel/mod.x` `export send` 2 参做成 `static send`；存储＝`xlang_compile_std_module.sh` `_libc_clash`（无 send／recv）；消费＝`cc -c` 撞 `sys/socket.h` POSIX `send(2)` → 无 `channel.o` → ld UNDEF `_std_channel_*`。同模式＝listen 混 arity（禁 SHARED `#define`）。**修**：G.7 补表 send／recv＋POSIX arity 4。禁改 channel 测试。证：Darwin `channel.o` T `_std_channel_send`／t `_xlang_formal_bare_send`／run-channel **OK** run=1／矩阵 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: MACOS formal_mod wrapper。下一刀：Ubuntu L2 channel＋双端 L4。 |
| 2026-09-02 | **test(selfhost)** **双端 L4＠b5d919060（不升钉）** | **真冷** · remaining_o=0 · 双端 phase1／g05／矩阵 **5／5 OK**。Ubuntu **`product_l4_true_cold OK`** bstrict **129**（http／fs／io-context／shuffle-select／fmt-std／channel）wall 17m42s `/tmp/xlang_l4_Linux_b5d919060.log`。Darwin run-check／run-http／run-fs **OK**（sendfile skip 冷链闭）后 **run-channel.sh FAIL**：`static send` 撞 POSIX `send` → UNDEF `_std_channel_*` wall 122m29s `/tmp/xlang_l4_Darwin_b5d919060.log`。**禁升钉**。Windows 明天。下一刀：channel send／recv clash。 |
| 2026-09-02 | **fix(codegen)** **skip sendfile libc-extern（Darwin fs.o）** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝KEEP_C 发 `extern sendfile` 6 参（i64*／u8*）撞 Darwin `<sys/socket.h>`（off_t*／sf_hdtr*）；存储＝`codegen_is_libc_conflicting_extern_name`（无 sendfile）＋pin 孪生；消费＝`xlang_compile_std_fs_formal.sh` cc → 无 `fs.o` → ld UNDEF `_std_fs_invalid`。同模式＝snprintf／read／write 冲突 extern。**修**：G.7 补谓词＋pin；Linux leftover 4 参由 fs_formal 加 `<sys/sendfile.h>`。禁改 fs 测试／禁 `_libc_clash` 定义改名。证：Darwin `fs.o` T `_std_fs_invalid`／run-fs **OK** run=4／矩阵 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED skip／LINUX fs_formal header／MACOS socket.h。下一刀：Ubuntu L2 fs＋双端 L4。 |
| 2026-09-02 | **test(selfhost)** **双端 L4＠a7330a236（不升钉）** | **真冷** · remaining_o=0 · 双端 phase1／g05／矩阵 **5／5 OK**。Ubuntu **`product_l4_true_cold OK`** bstrict **129**（http／io-context／shuffle-select／fmt-std／fs）wall 17m33s `/tmp/xlang_l4_Linux_a7330a236.log`。Darwin run-check **OK**／run-http **OK**（listen clash 冷链闭）后 **run-fs.sh FAIL**：`extern sendfile` 撞 socket.h → UNDEF `_std_fs_invalid` wall 111m50s `/tmp/xlang_l4_Darwin_a7330a236.log`。**禁升钉**。Windows 明天。下一刀：codegen skip sendfile。 |
| 2026-09-02 | **fix(formal_mod)** **Darwin http listen libc-clash** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝Darwin wrapper 把 `std/http/mod.x` `export listen` 3 参做成 `static listen`；存储＝`xlang_compile_std_module.sh` `_libc_clash`（无 listen）；消费＝`cc -c` 撞 `xlang_net_cap.h` `<sys/socket.h>` POSIX `listen(2)` → UNDEF `_std_http_get`。同模式＝mmap 族混 arity（禁 file-wide `#define`）。**修**：G.7 补表＋POSIX arity 2 时改写 X 3 参调用（`listen_on`→`xlang_formal_bare_listen`）。禁改 http 测试。证：Darwin `http.o`／`run-http.sh` **OK**／矩阵 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: MACOS formal_mod wrapper。下一刀：双端 L4。 |
| 2026-09-02 | **test(selfhost)** **双端 L4＠27f5682a3（不升钉）** | **真冷** · remaining_o=0 · 双端 phase1／g05／矩阵 **5／5 OK**。Ubuntu **`product_l4_true_cold OK`** bstrict **129**（io-context／shuffle-select／fmt-std／http）wall 17m43s `/tmp/xlang_l4_Linux_27f5682a3.log`。Darwin run-check **OK**（FMT001 冷链 102s）后 **run-http.sh FAIL**：`static listen` 跟 POSIX `listen` 冲突 → UNDEF `_std_http_get` wall 106m57s `/tmp/xlang_l4_Darwin_27f5682a3.log`。**禁升钉**。Windows 明天。下一刀：http `listen` mangle／std 发射。 |
| 2026-09-02 | **fix(runtime)** **Darwin ARM64 write syscall（FMT001）** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`xlang_io_write` Darwin aarch64 用 Linux ABI（x8＋`svc #0`）；存储＝`compiler/include/xlang_io_cap.h`（Cap 单权威）；消费＝`driver_fmt_one_file`→`xlang_write_path_bytes`（libc open 截断后 syscall write 失败，dest 0 字节）。同模式＝头内 Darwin aarch64 其它 Cap（read／writev 仍 ENOSYS，本债不搅）。**修**：G.7 补全既有函数：nr→**x16**、`svc #0x80`、CF→−errno。禁改 fmt-wrap／测试。证：反汇编 `ldr x16`／`svc #0x80`／`cset hs` · `fmt` wrap_cases **0** 写回 · Darwin fmt-wrap **5／5** · 矩阵 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: MACOS\|DARWIN aarch64。下一刀：双端 L4。 |
| 2026-09-02 | **test(selfhost)** **双端 L4＠6c0fdeebf（不升钉）** | **真冷** · remaining_o=0 · 双端 phase1／g05／矩阵 **5／5 OK**。Ubuntu **`product_l4_true_cold OK`** bstrict **129**（fmt-std／shuffle-select／**io-context OK**；CALL caller arena 金标生效）wall 17m29s `/tmp/xlang_l4_Linux_6c0fdeebf.log`。Darwin bstrict **run-check.sh FMT001 write failed**（wrap_cases dest 0 字节）wall 16m53s `/tmp/xlang_l4_Darwin_6c0fdeebf.log`。**禁升钉**。Windows 明天。下一刀：Darwin ARM64 syscall write。 |
| 2026-09-02 | **fix(asm)** **CALL resolve caller arena（STD-091 io_err_cancelled）** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`glue_asm_build_call_export_sym_c` 把 `typeck_resolve_for_emit(dep_mod, dep_arena, call_ref)` 喂 dep 池（CALL stamp 在 caller arena）；存储＝`.x`／seed `_impl`；消费＝产品 `-o` 裸 `io_err_cancelled()`。同模式＝dep 共发射 VAR CALL。**修**：G.7 补全既有权威：resolve 改 caller arena＋name／arity 闸（拒错 stamp 当 mid）。禁改 `context_read_write.x`。证：Darwin `-o` run=0 · nm `std_error_io_err_cancelled` · gate run=1。残：Ubuntu L2／L4；Darwin FMT001。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit。下一刀：Ubuntu io-context＋矩阵。 |
| 2026-09-02 | **test(selfhost)** **双端 L4＠7104b384b（不升钉）** | **真冷** · remaining_o=0 · 双端 phase1／g05／矩阵 **5／5 OK**。Ubuntu **fmt-std OK** · **shuffle-select OK**（i32 ymm VEX 生效）。Ubuntu bstrict **STD-091 io-context run=1**（asm 绑 `base_fs`／`base_async`；`-E` 正确）。Darwin bstrict **run-check.sh FMT001 write failed**。日志 `/tmp/xlang_l4_Linux_7104b384b.log`／`/tmp/xlang_l4_Darwin_7104b384b.log`（wall 17m42s／17m3s）。**禁升钉**。Windows 明天。下一刀：asm CALL resolve。 |
| 2026-09-02 | **fix(simd)** **AVX2 i32 select VEX（vpxor／vpand／vpandn／vpor）** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`simd_x86_vpxor_ymm3_ymm3` 写 `C5 F5 77 DB`（opcode 0x77＝EMMS #UD）；`vpand`／`vpandn`／`vpor` 抄 `C5 E5`（vvvv=ymm3）使 `a&mask` 变 `0&mask`。存储＝`simd_enc.x`／seed／surface。消费＝AVX2 Vec8i select。**修**：G.7 补全既有编码器 `C5 E5 EF DB`／`C5 FD DB C2`／`C5 ED DF D1`／`C5 FD EB C2`。禁改 roundtrip。残：f32 ymm select。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit／x86 AVX2。下一刀：Ubuntu L2 shuffle_select。 |
| 2026-09-02 | **fix(simd)** **vmovups ymm2 VEX.256.0F（select SIGILL）** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`vmovups_ymm2_from_rbp` 写 `C5 FE 10`（pp=F3＝vmovss，L=1 #UD）；ymm0／1 已是 `C4 E1 7C`。存储＝`simd_enc.x`／seed／surface。消费＝AVX2 select。**修**：G.7 孪生 `C4 E1 7C 10 95`；seed ymm0／1／store 同步。禁改测试。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit／x86 AVX。下一刀：Ubuntu shuffle 探针。 |
| 2026-09-02 | **fix(simd)** **vpshufd ymm VEX.pp F3→66** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`simd_x86_vpshufd_ymm0_imm8` 写 `C5 FE 70`（VEX.pp=F3＝`vpshufhw`）；存储＝`simd_enc.x`／`simd_enc.from_x.c`／surface；消费＝L4 金标 AVX2 Vec8i shuffle。**修**：G.7 补全既有编码器 `C5 FD 70 C0`（pp=66 `vpshufd`）。禁改 roundtrip 期望。残：Ubuntu L2 探针未跑。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit／x86 AVX2。下一刀：Ubuntu shuffle＋矩阵。 |
| 2026-09-02 | **docs(selfhost)** **日切：1 日归档／2 号主文档重写** | **文档** · **不升钉**。`自举进度-归档-2026-09-01.md` 收 1 日 **131** 行；主文档只留 KPI＋前排＋2 日 §6。 |
| 2026-09-02 | **test(selfhost)** **双端 L4＠1174c5bb3（不升钉）** | **真冷** · remaining_o=0 · 双端 phase1／g05／矩阵 **5／5 OK**。Ubuntu **fmt-std OK**（7.4.9 `println` 复合 any 生效）。Ubuntu bstrict **std-simd-shuffle-select exit=5**（`shuffle_select_roundtrip.x` Vec8i `r8[0]!=3`）。Darwin bstrict **run-check.sh FMT001 write failed**。日志 `/tmp/xlang_l4_Linux_1174c5bb3.log`／`/tmp/xlang_l4_Darwin_1174c5bb3.log`（wall 17m31s／17m11s）。**禁升钉**。Windows 明天。下一刀：Vec8i shuffle 冷链。 |
| 2026-09-02 | **fix(typeck)** **7.4.9 pin println 复合实参 JSON any** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`typeck.x` 已有 `typeck_call_is_fmt_debug_print_any`，pin 孪生无；存储＝`typeck_gen.linux.x86_64.c` `check_call_arg_types` score＜0 后；消费＝L4 g05 链上 typeck（L2 prefer `.x` 藏债）。**修**：G.7 补全 pin／empty_surface（std.fmt／std.debug 1 参 print／println 复合不 T001；无第三 scorer）。**证**：Darwin Track L `cc -c` **0**／354960B · pin 链 `print_any` JSON · fmt-std **6／6** · L2 **5／5**；Ubuntu L4＠**1174c5bb3** fmt-std **OK**。禁改 print_any 期望。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED pin C。 |
