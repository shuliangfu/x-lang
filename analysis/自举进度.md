# Shux 自举进度

> **更新**：2026-07-14 · Track L `labi_std_list` IDENTICAL；T **18/18**；N=16；diagnostic **CORE_OK**
> **金标准**：Ubuntu Linux x86_64（`ssh ubuntu-server`）  
> **方法**：[自举方法.md](../自举方法.md)（三轨 W / L / M）  
> **验收命令权威**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md)  
> **会话阻塞快照**：[当前进度.md](../当前进度.md)  
> **历史流水账**：[自举进度-归档-2026-07-13.md](自举进度-归档-2026-07-13.md)

### 维护约定（防再膨胀）

1. 每完成一波 **只改**：§2 KPI、§3 前排（≤3 条）、§8 一行变更。  
2. **禁止**在正文粘贴 >20 行迁移日志 → 写 git log 或归档。  
3. 改方法只动 `自举方法.md`，不在本文复述长篇方法论。

---

## 0. 一句话当前状态

**D+E-soft+F 已闭合 v1；主线是 E 的「三轨退役」：根因波次解锁 typeck → 叶子 nm 退役 seed → mega 去 pin。**  
冷启动 / G-05 / G-FFI-5 已绿；**typeck 地图 T=18/18**；主 KPI 转 N（nm 退役）与 mega 去 pin，**C 文本 diff=0 不是主 KPI**。

---

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](../自举方法.md) | 怎么做（W/L/M、门禁、禁止项） |
| **本文** | 做到哪了、下一步做什么 |
| [当前进度.md](../当前进度.md) | 当天工程阻塞与复现命令 |
| [SELFHOST.md](../compiler/docs/SELFHOST.md) | Stage2 / 金标准命令 |
| [自举进度-归档-2026-07-13.md](自举进度-归档-2026-07-13.md) | G-02f thin 切割、旧 checklist、性能细表 |
| [性能压榨.md](性能压榨.md) | perf 六层（**不阻塞**本周自举主线） |

---

## 2. 仪表盘

### 2.1 四 KPI（每周 / 每波次刷新，Ubuntu）

| KPI | 含义 | 当前（Ubuntu 2026-07-14） | 健康方向 |
|-----|------|------|----------|
| **T** | `diff_single_link` typeck OK / 18 | **Ubuntu 18/18**（`144506c0` + typeck_gen string_lit *u8 pin 后） | → 满 |
| **N** | `prove_module_selfhost` IDENTICAL 模块数 | **16**（7 driver 叶 + heap + token + sizes + path + gates + cc_list + ld_list + freestanding + **`labi_std_list`**）；diagnostic **CORE_OK**（不计 N） | ↑ |
| **S** | 构建链硬依赖的 pinned seed 面 | 🟢 **fmt…emit + lsp_io_std_heap 均 leaf 退役**；`sizes` / path / gates L9 / L5 / L6 / L7 freestanding / **L8 std_list** 产品已 PREFER_X_O（冷启动 seed）；`token` 仅 prove 门禁（产品仍 `token.h`）；mega 仍 pin | ↓ |
| **G** | 门禁：seed 冷启动 / G-FFI-5 | ✅ mac + Ubuntu | 全绿 |

刷新 T：

```bash
ssh ubuntu-server 'cd /home/shuliangfu/worker/shu/shux && timeout 180 bash tests/diff_single_link.sh 2>&1 | tail -40'
```

### 2.2 阶段总览（与 SELFHOST 对齐）

| 阶段 | 含义 | 状态 |
|------|------|------|
| D | 黄金自举 Stage2 | ✅ v1 |
| E | 编译器去 C（soft） | 🟡 默认 no_c；**pinned seed + patch 仍在** ← **本周主线** |
| F | std 无 C（F-ZC） | ✅ |
| G | 终局物理零 C / nostdlib 硬绿 | 🟡 非本周主线（先 E 三轨） |

### 2.3 拓扑提醒（修 typeck 时）

```
bootstrap-driver-seed → typeck_x.o (pinned gen + patch)
                      → pipeline_glue_strict_minimal.o  （≠ 整份 pipeline_glue.c）
```

热路径修复必须落在 **实际被链接** 的实现上（见方法文 §2.3）。

---

## 3. 当前前排（≤3 条）

1. **Track L 下一 pure leaf**：`labi_ondemand_list`（L8b）同构 if/else；或 **diagnostic hybrid→IDENTICAL**。  
2. **Track W 余债**：全局 `u8[N]`→`static uint8_t*`；AST 字符串池 >64；`labi_diag_pure` reportf/va_list。  
3. **纪律**：每波 **commit → push → Ubuntu prove/T → 再下一步**（codegen 改须 `codegen_gen`+relink；§8 东八区）。

**不在前排**：`labi_std_list` N=16（✅）；`labi_freestanding_list` N=15（✅）；`labi_invoke_ld_list` N=14（✅）；W-string-nul + `labi_invoke_cc_list` N=13（✅）；`labi_gates` N=12（✅）；path N=11（✅）；sizes N=10（✅）；token N=9（✅）；W-string-let（✅）；diagnostic CORE（✅）。

---

## 4. 三轨进度

### 4.1 Track W — 根因波次

| 波次 | 症状 | 状态 | 解锁 |
|------|------|------|------|
| W-string | `m=""` → `expected *u8, found ?` | ✅ `5d483df2` | diagnostic / diag 等 |
| W-keyword-param | `socket(..., type:)` 丢 extern | ✅ 同 commit | net tcp/udp/ipv6 |
| W-unsafe-expr | `if (unsafe { f() } != 0)` 坏 C | ✅ std 源改（let+stmt unsafe+if）；codegen 根修仍可选 | net/schema/socketio/security… |
| W-extern-unsafe | `extern call requires unsafe` | ✅ 接线 `-E` `allow_legacy`（`driver_run_x_emit_c` + typeck_gen patch） | compile/lsp/lsp_diag/typeck 等 -E 地图 |
| W-tail | implicit tail return | ✅ typeck：terminal final 优先，否则 peel unsafe region（`typeck.x` + `typeck_gen` patch + glue） | ast -E |
| W-isize | return isize vs i32；顺带 `heap.alloc` 误匹配 | ✅ `lsp_io`：`(-1) as isize` + `alloc_zero` | lsp_io -E |
| W-heap-overload | METHOD_CALL/跨模块 `alloc` 取首同名 | ✅ strict_minimal 按实参类型打分分派 + typeck.x by_name_overload | `heap.alloc(usize)` |
| W-escape | struct stack escape 误报 | ✅ glue 同帧 &local 放行 + emit 源改 | emit -E |
| W-lexer-timeout | lexer -E 超时刷屏 / Token layout 串池 | ✅ `field_base=-1` 哨兵 + `MODULE_ENUM_MAX_VARIANTS` 256；metrics `check_pad` 静默 | lexer typeck OK；Ubuntu `-E` 出 C |
| W-lsp-diag-expr | lsp_diag `check_block failed` @ hover/refs | ✅ 禁 `ast_arena_expr_get`/`type_get` 按值；`pipeline_expr_*`/`pipeline_type_*` 标量 + kind 序 | **T 18/18** |
| W-string-let | `let p: *u8 = "s"` → 整函数 parse skip → init_globals/`msg_cap` | ✅ body let 直建 `EXPR_STRING_LIT`；let 初值 expected=`ld_tr`；string 默认 `*u8` | diagnostic `-E` 可 cc；5 函数回位 |
| W-string-nul | `-E` 出 `(uint8_t[]){…}` **无尾 0**；slen 封顶 63 | ✅ `codegen.x`/`codegen_gen`：*u8 路径尾 `, 0`；slen≤64 | 短字面量 return/`let p` 可 C 互操作；解锁 list pure |

### 4.2 Track L — 叶子 nm 退役

| 模块 | typeck | nm prove | 构建去 seed |
|------|--------|----------|-------------|
| **fmt / check / test / build / run** | OK | ✅ IDENTICAL | ✅ **已退役**（`driver_leaf_x_to_o.sh`；冷启动 seeds/） |
| **lsp_io_std_heap** | OK | ✅ IDENTICAL | ✅ **已退役** leaf（g05 + Makefile；冷启动 seeds/） |
| **token** | OK | ✅ **IDENTICAL**（prove 门禁） | ⬜ 产品仍 `include/token.h`（非 leaf 链；冷启动 `seeds/token_gen.linux.x86_64.c`） |
| **lsp_diag_pipeline_sizes** | OK | ✅ **IDENTICAL**（prove 门禁） | ✅ 产品 `sizes_nostub` **PREFER_X_O**；冷启动 `seeds/lsp_diag_pipeline_sizes.from_x.c` |
| **labi_path_pure** | OK | ✅ **IDENTICAL**（prove 门禁） | ✅ 产品 L0 **PREFER_X_O**（g05 `g05_try_x_to_o`）；冷启动 `seeds/labi_path_pure.from_x.c` |
| **labi_gates** | OK | ✅ **IDENTICAL**（prove 门禁） | ✅ 产品 L9 **PREFER_X_O**；冷启动 `seeds/labi_gates.from_x.c` |
| **labi_invoke_cc_list** | OK | ✅ **IDENTICAL**（prove 门禁） | ✅ 产品 L5 **PREFER_X_O**；冷启动 `seeds/labi_invoke_cc_list.from_x.c` |
| **labi_invoke_ld_list** | OK | ✅ **IDENTICAL**（prove 门禁） | ✅ 产品 L6 **PREFER_X_O**；冷启动 `seeds/labi_invoke_ld_list.from_x.c` |
| **labi_freestanding_list** | OK | ✅ **IDENTICAL**（prove 门禁） | ✅ 产品 L7 **PREFER_X_O**；冷启动 `seeds/labi_freestanding_list.from_x.c` |
| **labi_std_list** | OK | ✅ **IDENTICAL**（prove 门禁） | ✅ 产品 L8 **PREFER_X_O**；冷启动 `seeds/labi_std_list.from_x.c` |
| diagnostic | OK（W-string-let） | 🟡 **CORE_OK**（`prove … diagnostic`）；非 IDENTICAL（hybrid thin/C-tail + x `append_*`） | ⬜ hybrid 收敛后可退役 |
| **compile** | OK | ✅ IDENTICAL | ✅ **已退役** leaf |
| **emit** | OK | ✅ IDENTICAL | ✅ **已退役** leaf（codegen 名字回退根修） |

主门禁：`tests/prove_module_selfhost.sh`（**不是** C 文本 diff=0）。

### 4.3 Track M — Mega / pin

| 模块 | 状态 | 说明 |
|------|------|------|
| typeck | 🟡 pinned + `patch_typeck_gen_lang007.py` | 语义在 typeck.x；链上 seed |
| codegen | 🟡 pinned | 手工同步 .x 改动 |
| parser | 🟡 pinned | 文本 diff 巨大，勿当主 KPI |
| 终局 | ⬜ | 替换 `*_x.o` smoke → Stage2 → 去 pin |

---

## 5. 模块地图摘要（18 模块）

工具：`tests/diff_single_link.sh`（typeck + C 行 diff 仅参考）。  
**Ubuntu / mac 复跑 2026-07-14**（W-lsp-diag-expr）：

| 状态 | 模块 | 备注 |
|------|------|------|
| typeck **OK**，diff 非空 | 全部 18 模块（mac+Ubuntu 2026-07-14） | T 满格 |
| 备注 | mac **lexer** 偶记 CODEGEN；Ubuntu lexer 出 C | 非 typeck 阻塞 |

**PASS_DIFF（C 文本空）= 0/18** — 不作为主 KPI。  
**每次 W 波次后重跑脚本，改本表与 KPI T。**

---

## 6. 下一步（本周队列）

| 序 | 动作 | 完成判据 |
|----|------|----------|
| 1 | ✅ … + compile leaf + **emit leaf** | N=7 mac |
| 2 | ✅ Ubuntu driver `-E` 段错误根修 | Ubuntu prove **7/7** |
| 3 | ✅ W-lexer-timeout + harness（NOSEED 仍跑 typeck；codegen fail→typeck OK） | T=17/18 |
| 4 | ✅ W-lsp-diag-expr（pipeline 标量读） | **T=18/18** mac+Ubuntu |
| 5 | ✅ W-string-let：diagnostic `-E` 可 cc | 见 §8 |
| 6 | ✅ diagnostic CORE prove + W-unsafe-expr std 源改 | 见 §8 |
| 7 | ✅ Track L 退役 `lsp_io_std_heap`（N=8） | 见 §8 |
| 8 | ✅ token 扩 prove → IDENTICAL（N=9） | 见 §8 |
| 9 | ✅ `lsp_diag_pipeline_sizes` 扩 prove → IDENTICAL（N=10） | 见 §8 |
| 10 | ✅ `labi_path_pure` typeck 根修（i32 长度）+ prove IDENTICAL（N=11） | 见 §8 |
| 11 | ✅ `labi_gates` 真迁 thin shell + PREFER_X_O L9 + prove IDENTICAL（N=12） | 见 §8 |
| 12 | ✅ W-string-nul + `labi_invoke_cc_list` PREFER_X_O L5 + prove IDENTICAL（N=13） | 见 §8 |
| 13 | ✅ `labi_invoke_ld_list` PREFER_X_O L6 + prove IDENTICAL（N=14） | 见 §8 |
| 14 | ✅ `labi_freestanding_list` PREFER_X_O L7 + prove IDENTICAL（N=15） | 见 §8 |
| 15 | ✅ `labi_std_list` PREFER_X_O L8 + prove IDENTICAL（N=16） | 见 §8 |
| 16 | 下一 labi list pure（ondemand L8b）/ diagnostic hybrid→IDENTICAL；W-global 数组 / 串池>64 | 下一波 |

详细步骤与纪律见 [自举方法.md](../自举方法.md) §9–§12。

---

## 7. 验收命令（日常）

```bash
# 冷启动
cd compiler && make bootstrap-driver-seed

# 地图
cd .. && timeout 180 bash tests/diff_single_link.sh

# 叶子 nm（15 IDENTICAL + diagnostic CORE）
bash tests/prove_module_selfhost.sh

# G-05 + FFI
cd .. && ./shux-build.sh build
SHUX=./compiler/shux bash tests/run-g-ffi-5-release-ci-gate.sh

# Stage2 / 金标准（Mega / 发布）— 见 SELFHOST.md
# make -C compiler bootstrap-verify-stage2-bstrict
```

更多命令：SELFHOST.md；性能：`性能压榨.md`（不阻塞本周）。

---

## 8. 变更记录（倒序，一行一条）

| 日期时间 | 摘要 |
|------|------|
| 2026-07-14 13:13 | **Track L labi_std_list**：`.x` if/else+let 真迁 58 步 plan；`plan_step_at` out 参 `*i32`/`*usize`/`*i32`；seed 无 static 表；g05 L8 PREFER_X_O；prove 扩门禁；mac **16 IDENTICAL + CORE_OK** |
| 2026-07-14 13:12 | **Ubuntu 金标 `de07b1f7`**：`labi_freestanding_list` **IDENTICAL**（产品 `shux`）；同批 fmt…run + path + gates + cc_list + ld_list **IDENTICAL** |
| 2026-07-14 13:11 | **Track L labi_freestanding_list**：`.x` if/else+let 真迁；catalog out 参 `*usize`；seed 无 static 表；g05 L7 PREFER_X_O；prove 扩门禁；mac **15 IDENTICAL + CORE_OK** |
| 2026-07-14 13:08 | **Ubuntu 金标 `cbc389f4`**：`labi_invoke_ld_list` **IDENTICAL**（产品 `shux`）；同批 fmt…run + path + gates + cc_list **IDENTICAL** |
| 2026-07-14 13:05 | **Track L labi_invoke_ld_list**：`.x` if/else+let 真迁；seed 无 static 表；g05 L6 PREFER_X_O；prove 扩门禁；mac **14 IDENTICAL + CORE_OK** |
| 2026-07-14 13:01 | **Ubuntu 金标 `a43addd6`**：`labi_invoke_cc_list` **IDENTICAL**（产品 `shux`）；同批 10 叶 IDENTICAL；`shux-fresh` 仅 shux-c 时 L5 需 string-let 产品链 |
| 2026-07-14 12:57 | **Track L + W-string-nul**：`codegen` STRING_LIT *u8 尾 NUL、slen≤64；`labi_invoke_cc_list.x` if/else+let 真迁；g05 L5 PREFER_X_O；prove IDENTICAL 改全局业务符号；mac **13 IDENTICAL + CORE_OK** |
| 2026-07-14 12:47 | **Ubuntu 金标 `cec072e6`**：`labi_gates` **IDENTICAL**（shux-c）；同批 10 叶 IDENTICAL（fmt…run+heap+token+sizes+path+gates）；compile/emit/diagnostic 仍需产品 `shux` |
| 2026-07-14 12:46 | **Track L labi_gates prove**：`labi_gates.x` 真迁 L9 thin null-check 转发（*u8 透传 `_impl`）；g05 L9 PREFER_X_O；`prove` 扩门禁；mac **12 IDENTICAL + CORE_OK**；`labi_diag_pure` 暂缓（string NUL / 全局数组 / va_list） |
| 2026-07-14 12:41 | **Ubuntu 金标 `28f8809a`**：`labi_path_pure` **IDENTICAL**（shux-c）；同批 9 叶 IDENTICAL（fmt…run+heap+token+sizes+labi）；compile/emit/diagnostic 需产品 `shux`（allow_legacy；本机 full relink 未齐） |
| 2026-07-14 12:38 | **Track L labi_path_pure prove**：`labi_path_pure.x` 长度改 i32（消 usize 字面量 typeck）；seed 对齐真 `-E`；`prove` 扩门禁；mac **11 IDENTICAL + CORE_OK**；产品本已 L0 PREFER_X_O |
| 2026-07-14 12:34 | **Ubuntu 金标 `f654d5c2`**：prove **10 IDENTICAL + CORE_OK**（含 `lsp_diag_pipeline_sizes`；diag seed+21） |
| 2026-07-14 12:33 | **Track L lsp_diag_pipeline_sizes prove**：`prove_module_selfhost` 扩三枚 sizeof 门闩；mac **10 IDENTICAL + CORE_OK**；产品本已 PREFER_X_O（`sizes_nostub`） |
| 2026-07-14 12:30 | **Ubuntu 金标 `79b2c3c3`**：prove **9 IDENTICAL + CORE_OK**（token 含；diag seed+21） |
| 2026-07-14 12:29 | **Track L token prove**：`prove_module_selfhost` 扩 `token`；seed 对齐真 `-E`（`seeds/token_gen.linux.x86_64.c`）；mac **9 IDENTICAL + CORE_OK**；产品链仍 `token.h`（不扩 S 退役） |
| 2026-07-14 12:26 | **Ubuntu 金标 `5bf1fa53`**：prove **8 IDENTICAL + CORE_OK**；T **18/18**；`lsp_io_std_heap` PREFER_X_O + relink |
| 2026-07-14 12:24 | **Track L lsp_io_std_heap**：`driver_leaf_x_to_o` + g05 leaf 表；N **8 IDENTICAL**；构建不读 workspace gen；冷启动 seeds/ |
| 2026-07-14 12:20 | **Ubuntu 金标**：prove **7 IDENTICAL + diagnostic CORE_OK**；T **18/18**；`patch_typeck_gen` 升 string_lit body（*u8）；`typeck_gen` 同步后 relink |
| 2026-07-14 12:15 | **diagnostic CORE + W-unsafe-expr**：`prove_module_selfhost` 扩 `diagnostic`（`core:` 模式，滤 `l_constinit`）；CORE_OK seed+23/x_allow+3；std `if (unsafe{…})` 批量改 let+stmt（schema/tls/socketio/security/regex/ws/win32）；N 仍 7 |
| 2026-07-14 12:08 | **W-string-let**：`parse_body_lets` TOKEN_STRING 勿重 parse_expr（token_start 在引号后）；直建 EXPR_STRING_LIT；typeck let 初值 expected=`ld_tr`；string 默认 `*u8`。diagnostic 五函数回位、`-E` cc 绿；nm 非 IDENTICAL（seed thin）；T 18/18 |
| 2026-07-14 10:59 | **W-lexer-timeout**：`field_base==0` 兼「未挂接」致 Token layout 串读 Lexer/LexerResult；`field_base=-1` 哨兵 + enum 变体上限 64→256；metrics size 路径不刷屏；地图 **T 17/18**；Ubuntu lexer `-E` 出 C；prove 7/7 |
| 2026-07-14 10:56 | **Ubuntu `-E` 根修** `bb4a6dc9`：pthin `ast_Func` 缺 `is_export`（0x80 vs get_copy 0x84）冲栈 `lex_out`；补字段后 Ubuntu+mac prove **7/7 IDENTICAL** |
| 2026-07-14 10:12 | **Track L emit**：codegen 类型已解析时禁用 `ctx`/`out` 名字 `->` 回退；emit leaf 退役；mac prove **7/7 IDENTICAL** |
| 2026-07-14 10:14 | **Track L compile** `522cdbaf`：prove 6/6 mac+Ubuntu；compile 退役；emit 曾 pin |
| 2026-07-14 10:08 | **W-heap-overload** `716706c5`：strict_minimal 实参打分 + typeck_x method_call fixup；mac/Ubuntu `heap.alloc(usize)`→*u8；G-FFI-5 绿 |
| 2026-07-14 09:50 | **W-tail** `ce7a4eeb`：terminal final 优先否则 peel unsafe region；mac 17/18；Ubuntu `ast -E` + G-FFI-5 绿 |
| 2026-07-14 09:43 | **W-escape** `0c4c1480`：`pipeline_glue` 同帧 &local 兄弟放行；`emit.x` 直调 extern pipeline + lib_root 用 *u8 key；-E typeck OK |
| 2026-07-14 09:38 | **W-isize** `32cee437`：`lsp_io` `return (-1) as isize` + `heap.alloc_zero`；-E typeck OK；根因备注 W-heap-overload |
| 2026-07-14 09:34 | **Track L** `deb26bdd`：fmt/check/test/build/run 构建链退役（`driver_leaf_x_to_o.sh` + Makefile/g05/asm）；prove 5/5；冷启动不读工作区 leaf gen |
| 2026-07-14 09:28 | **W-extern-unsafe** `e25ad004`：`-E` allow_legacy 接线；Ubuntu T **9→13/18**；bootstrap + G-FFI-5 双端绿 |
| 2026-07-14 09:15 | **重构** `f0886919`：进度文瘦身为仪表盘；旧文 → `自举进度-归档-2026-07-13.md` |
| 2026-07-14 09:08 | W-string + W-keyword-param `5d483df2` |
| 2026-07-13 及更早 | G-02f thin+rest → **见归档** |

---

## 9. 索引

- 方法：[自举方法.md](../自举方法.md)  
- 归档：[自举进度-归档-2026-07-13.md](自举进度-归档-2026-07-13.md)  
- 战略旧文：[自举分析.md](自举分析.md) · [NEXT.md](../NEXT.md)  
- 任务细表（终局）：[phase-g-full-selfhost-tasks-v1.md](phase-g-full-selfhost-tasks-v1.md)（考古；执行以前文三轨为准）  
- TU 合同：`analysis/tu_contracts/`（thin 时代产物，退役叶子时可参考）
