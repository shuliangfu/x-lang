# Xlang / X language 自举进度

> **更新**：2026-09-13 · tip **`751804fcb`**（日切归档；本刀 P19d 待 SHA-sync）· 前 **`b83035ba3`**／`76264e272` · **钉盘 `5cac88d00`** · **金标 SSH＝`ubuntu-server`** · Ubuntu 验＝**git pull（禁 scp；本波暂停）**\


> **产品 L4 放行钉盘**：**`5cac88d00`**（2026-09-10 晚二度升钉 · 双端 L4 真冷全绿＋bstrict 129/129）· 前 **`fa1061224`**／`e194cd294`／`d583f61f5`／`b5be5ed97`\
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅** · parser **7.2.2 关 pin ✅** · codegen 7.4.2 ✅ · typeck 7.4.1 ✅ · runtime monofile ✅ · present **0** · Stage 8 **30/30 FULLY CLOSED** · **7.4.3 PREFER_X_O 双端 8/8 ✅**\
> **终局债**：[C迁移追踪.md](C迁移追踪.md) · [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：主刀＝**7.2.1 P19d Route C**（`pthin_helpers.x` 有则补全 struct_field_name／ident_is_unsafe；hybrid 跳过 helpers.inc C 孪生；C trampoline 持 by-value lexer_result；IDENT 字段名走 P1b at_end copy；禁给 P19 加 P9a；禁把 ident_is_unsafe 并进 P4b；禁开新 P 道；align／parse_block_return_end_tail 仍 C）。leftover flatten **完**。验＝**仅 Darwin L2** 矩阵 5/5＠本波 `xlang_asm` 5487336 @08:51。Ubuntu 本波不验（主机重装）。**不升钉**。\
> **下波候选**：8.3.6 续缩仍 host-cc 的产品切片（glue_tail 包装／skip_tl skip_one_trait／parse_one_extern_and_add／primary parse 仍 C；P19 align 仍 C；try_skip_allow parse_into 仍 C；if_stmt parse／realign 仍 C）。lite／suite／lex_skip／helpers／primary ident／type_ref／simd／unary／binop／skip_if walks／skip_tl struct／enum／extern／impl header／generic_bound_scan／enum_register／parse_one_extern_skip／body_tl skip／glue skip_one_function_full／try_skip_allow padding／skip_imports／consume_path／try_skip／collect_imports／ctrl brace-skip／scan_sync／library_scan／diag_late after_structs／fail／body_tl cfg_skip／diag_first_ident／lex_skip count／ASI advance_past／peek-name／first_token／run_len extra／lex_at_token／rewind／struct_field_name／ident_is_unsafe／diag_skip_let_const_buf trampoline portable 冷回退仍留盘。pipeline_x mega 须点名。vx／summit HARD BAN。日常闸门见**提速纪律**。

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1\
> **eq 墙钟纪律（2026-09-11）**：`eq_mode`＝daily／close／full；软刀禁串行 OFF=128 全表\
> **提速纪律（2026-09-12 · 用户要求更高效率；同日改双端）**：\
> 1. **软刀闸门＝Darwin L2 矩阵＋FORCE eq smoke（`EQ_DEEP_MAX_FILE_OFF=0`／本域 `EQ_ONLY`）＋Ubuntu 同序 L2**（用户 2026-09-12「都要经过双端验证」；禁只 Darwin 结案）。\
> 2. **一波＝一条（或同链两条）ABI 根墙＋soft-FP 攀尽可解锁层**（允许多层 parse_into／exact），禁「一层 exact／一层 climb 一波」。\
> 3. **tip heat g05 立卡不挡**：矩阵／eq 用既有 `xlang_asm`；禁为软刀空等 heat 绿。\
> 4. **禁默认** drift-gate／compress／全表 close／全量 bstrict；close 仅 ABI 根大改或升钉；L4 仅整波收口／日终兜底／谈自举。\
> 5. **墙钟目标**：单刀 Darwin 验收 ≤**5 min**；单刀 Ubuntu 同序 ≤**15 min**。深层 combinator 实测可超：v5.69 3 层 JOBS=4 Darwin ~32 min（仍优于 3 波各 1 层＋3 次 Ubuntu pull）。\
> 6. **深链分批覆盖戏停（2026-09-12 用户再催）**：leftover flatten 完后，一层 25／3 层 75 的 eq 只是对已 T combinator 再跑 C↔.x，**不减 host-cc、不改 T**。实测 3 层 JOBS=4 Darwin ~32 min／Ubuntu ~41 min。**禁**再以改注释换波次再跑。下一刀＝8.3.6／7.2.1 已 T 冷孪生缩壳（真减 host-cc LOC）或 pipeline_x mega（须点名）。\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；leftover-PE 探针红不当双端 L2 产品红。

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景**：[自举时序.md](自举时序.md)\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[12](自举进度-归档-2026-09-12.md) · [11](自举进度-归档-2026-09-11.md) · [10](自举进度-归档-2026-09-10.md) · [09](自举进度-归档-2026-09-09.md) · [08](自举进度-归档-2026-09-08.md) · [07](自举进度-归档-2026-09-07.md) · [06](自举进度-归档-2026-09-06.md) · [05](自举进度-归档-2026-09-05.md) · [04](自举进度-归档-2026-09-04.md) · [03](自举进度-归档-2026-09-03.md) · [02](自举进度-归档-2026-09-02.md) · [01](自举进度-归档-2026-09-01.md) · [31](自举进度-归档-2026-08-31.md) · …

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波**）。**§6 表固定 3 列**；描述禁裸 ASCII `|`。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 过期整日流水 **日切归档**后主文档重新起表。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `5cac88d00`**（2026-09-10 晚二度升钉）。\
**MG 终局达成 = wave941/942** · **双端 Stage2 = ✅** · **选题权威 =** [C迁移追踪.md](C迁移追踪.md)。\
**本波主刀 = 7.2.1 P19d Route C**（`pthin_helpers.x` 有则补全 struct_field_name／ident_is_unsafe；hybrid 跳过 helpers.inc C 孪生；C trampoline 持 by-value lexer_result；IDENT 字段名走 P1b at_end copy；禁给 P19 加 P9a；禁把 ident_is_unsafe 并进 P4b；禁开新 P 道；align／parse_block_return_end_tail 仍 C）。leftover flatten **完**。立卡：MSVC va／WIN leftover／vx daily／summit HARD BAN／pipeline_x mega 须点名。leftover-to-audit unique **0**。leftover_helpers 余 **0**。残＝产品 `.inc` 切片仍 host-cc（含 glue_tail 包装、primary parse、P19 align、skip_tl skip_one_trait／parse_one_extern_and_add、try_skip_allow parse_into、if_stmt parse／realign）；lite／suite／lex_skip／helpers／primary ident／type_ref／simd／unary／binop／skip_if walks／skip_tl struct／enum／extern／impl header／generic_bound_scan／enum_register／parse_one_extern_skip／body_tl skip／glue skip_one_function_full／try_skip_allow padding／skip_imports／consume_path／try_skip／collect_imports／ctrl brace-skip／scan_sync／library_scan／diag_late after_structs／fail／body_tl cfg_skip／diag_first_ident／lex_skip count／ASI advance_past／peek-name／first_token／run_len extra／lex_at_token／rewind／struct_field_name／ident_is_unsafe portable 文件留作冷回退；`parser_asm_thin_c.from_x.c` 443KiB 仅冷 rest 回退（G-02f-330 全切片齐则 omit rest）。

| 指标 | 状态 |
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`5cac88d00`**；residual tip present **0**；Stage 8 **30/30**；PRODUCT RETIRED 23/23 |
| Track MG | **11.3.1 MG ✅** |
| Track BC | **🟢 Stage8 30/30** · M4 **5/5** · present **0** |
| Track L | **收 30/30 FULLY CLOSED** |
| mac / Ubuntu L4（钉盘） | ✅ 双端 L4 + bstrict **129/129** @ **`5cac88d00`** |
| pin 蛋 | ✅ darwin.arm64 + linux.x86_64 |
| 本波构建级别 | **L2（仅 Darwin）**：矩阵 5/5＠本波 `xlang_asm` 5487336 @08:51。Ubuntu 本波不验。**不升钉** |

***

## 1. 文档地图

| 文档 | 看什么 |
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | 先→后全景 · S0–S8 |
| [C迁移追踪.md](C迁移追踪.md) | 终局债 / BC·PC DAG |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild（MG ✅） |
| **本文** | KPI + 前排 ≤3 + **§6**（**9 月 13 日**起） |
| [自举进度-归档-2026-09-12.md](自举进度-归档-2026-09-12.md) | **9月12日** leftover flatten／eq＋深链分批覆盖戏停＋P9／P9b／P1b／P19b（**122** 行） |
| [自举进度-归档-2026-09-11.md](自举进度-归档-2026-09-11.md) | **9月11日** B-minus v5.13→v5.29＋eq≤11s＋Cap 4.2.8（**32** 行） |
| [自举进度-归档-2026-09-10.md](自举进度-归档-2026-09-10.md) | **9月10日** B-minus v1→v5.12＋两度升钉＋7.4.4／g05／8.3.6（**73** 行） |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图（本波不改） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`5cac88d00`** | 2026-09-10 晚二度升钉；双端 L4 + bstrict 129/129 |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自钉盘 L4 `xlang_asm` 刷新 |
| residual tip（BC） | present **0** | Stage 8 30/30 · prefer pure-asm 默认 |
| mac 钉盘 L4 | ✅ L4 + bstrict **129/129** | `@5cac88d00` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 + bstrict **129/129** | `@5cac88d00` `ubuntu-server` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | **本刀 Darwin 5/5**＠新链 `xlang_asm` 5487336 @08:51。Ubuntu 本波不验（主机重装） |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ 双端 tip SHA256 STRICT match | 钉盘历史见归档 |
| **R2 / Cap** | 按需 | 明细见归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1** |
| `TREE_ARMED` / `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1** / **1** |
| post-delete residual | **✅** wave944–964 |

***

## 3. 当前前排（≤3 条）

1. **下一刀（真减 host-cc）** — 本刀 P19d 已把 struct_field_name／ident_is_unsafe 收进 `pthin_helpers.x`（C trampoline 持 by-value lexer_result；IDENT 字段名走 P1b at_end copy；禁给 P19 加 P9a；禁把 ident_is_unsafe 并进 P4b；align／parse_block_return_end_tail 仍 C）。续＝8.3.6 仍 host-cc 的产品 `.inc`（glue_tail 包装／skip_tl skip_one_trait／parse_one_extern_and_add／primary parse 仍 C；P19 align 仍 C；try_skip_allow parse_into 仍 C；if_stmt parse／realign 仍 C）。lite／suite／lex_skip／helpers／primary ident／type_ref／simd／unary／binop／skip_if walks／skip_tl struct／enum／extern／impl header／generic_bound_scan／enum_register／parse_one_extern_skip／body_tl skip／glue skip_one_function_full／try_skip_allow padding／skip_imports／consume_path／try_skip／collect_imports／ctrl brace-skip／scan_sync／library_scan／diag_late after_structs／fail／body_tl cfg_skip／diag_first_ident／lex_skip count／ASI advance_past／peek-name／first_token／run_len extra／lex_at_token／rewind／struct_field_name／ident_is_unsafe portable 冷回退仍留盘。pipeline_x mega 须点名。summit／peak／zenith／versal／vx 仍 HARD BAN。Windows 卡离线不挑。Ubuntu 主机重装好后再补同序 L2。
2. **立卡（Windows）** — **10.7.1 MSVC va**＋ **seed 第二 WIN-leftover addr_of**（须切 Windows）。
3. **立卡不动** — eq 深链指数墙；spawn 真体仍 T；nest 冻帽 64。本刀 Darwin g05 ensure＋pure-ld 绿（heat 未挡）。钉盘 **`5cac88d00`**。

***

## 4. 与归档的关系

- **9月12日归档**：[自举进度-归档-2026-09-12.md](自举进度-归档-2026-09-12.md) — **122** 行 · leftover flatten／eq＋深链分批覆盖戏停＋P9／P9b／P1b／P19b。
- **9月11日归档**：[自举进度-归档-2026-09-11.md](自举进度-归档-2026-09-11.md) — **32** 行 · B-minus v5.13→v5.29＋eq 墙钟根治＋Cap 4.2.8。
- **9月10日归档**：[自举进度-归档-2026-09-10.md](自举进度-归档-2026-09-10.md) — **73** 行 · B-minus v1→v5.12＋两度升钉。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `5cac88d00`**。
- 日切后主文档只保留 KPI + 前排 + **9 月 13 日** §6。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
| **macOS arm64** | 钉盘 L4＠**`5cac88d00`** · 软刀 L2 验收端；本刀矩阵 5/5＠新链 `xlang_asm` 5487336 @08:51 |
| **Ubuntu x86_64（金标）** | 钉盘 L4＠**`5cac88d00`** · **本波不验**（主机重装）；前刀 L2 绿＠`b00c7d9db`；SSH＝**`ubuntu-server`** |
| **Windows MSYS2** | leftover unique **0** · min-gate 绿 · MSVC va／第二 leftover 离线立卡 |

***

## 6. 变更记录（倒序 · **9 月 13 日**；12 日见 [归档](自举进度-归档-2026-09-12.md)）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-13 | **feat(selfhost)** **7.2.1 P19d Route C：helpers.x 补 struct_field_name／ident_is_unsafe** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P19 `helpers.inc` 在 P19c 后仍 host-cc struct_field_name／ident_is_unsafe（IDENT 字段名＋keyword-as-ident 与 6 字节 `unsafe` 探针可 Route C 复用 P1b at_end copy＋P19b pos_before_run；align 仍需 skip_ws＋peek）；存储＝有则补全 `pthin_helpers.x`（禁给 P19 加 P9a）＋helpers.inc trampoline＋`#ifdef XLANG_PTHIN_HELPERS_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 kind＋buf；IDENT 字段名走 P1b at_end copy（禁再抄 copy_slice）；禁把 ident_is_unsafe 并进 P4b；禁开新 P 道。**g05 `-E`+cc**（pure-asm CG002，同 P19b／P19c）。实测 hybrid P19 C 6600 vs 冷 9904；glue.o 1977120；xlang 5487336；g05 ensure P19b／P19c／P19d＋P19 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5487336 @08:51。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **日切归档 12 号 122 行＋13 号主文档重写** | **文档** · **不升钉**。归档＝leftover flatten／eq＋深链分批覆盖戏停＋P9／P9b／P1b／P19b。主文档＝KPI／前排／§6 仅 13 日（P4b→P19c Route C 产品化链）。Ubuntu 主机重装期间 Darwin-only。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `b83035ba3`（7.2.1 P19c Route C helpers run_len extra／lex_at_token／rewind；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P19c Route C：helpers.x 补 run_len extra／lex_at_token／rewind** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P19 `helpers.inc` 在 P19b／P1e 后仍 host-cc run_len extra／lex_at_token／rewind（TOKEN→span 与 pos 还原／stmt-head 谓词可 Route C 复用 P9b stretch compact 表＋P19b pos_before_run；align 仍需 skip_ws＋peek）；存储＝有则补全 `pthin_helpers.x`（禁给 P19 加 P9a）＋helpers.inc trampoline＋`#ifdef XLANG_PTHIN_HELPERS_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 extra case／pos／kind；禁并进 stretch run_len（双编号）；禁把 ident_is_unsafe 并进 P4b；禁开新 P 道。**g05 `-E`+cc**（pure-asm CG002，同 P19b）。实测 glue.o 1976392；xlang 5470152；g05 ensure P19b／P19c＋P19 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5470152 @08:33。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `0895c0259`（7.2.1 P12f B-minus parse_one_extern_skip；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P12f B-minus：skip_tl.x 补 parse_one_extern_skip** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P12 `skip_tl.inc` 在 P12e 后仍 host-cc parse_one_extern_skip（`extern ["C"] function name(params): Ret;` token 走查可 B-minus 复用 P9a 桥＋P1b copy；type_ref／onefunc 仍 C helper）；存储＝有则补全 `pthin_skip_tl.x`＋C trampoline（语言无 local u8[N]／结构体按值）＋`#ifdef XLANG_PTHIN_SKIP_TL_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁包 skip_one_trait；禁抄 skip_one_extern（opaque paren skip 会丢形参／类型）；禁开新 P 道。**g05 `-E`+cc**（pure-asm CG002，同 P12c／P12d／P12e）。实测 glue.o 1975520；xlang 5469304；g05 ensure P12b／P12c／P12d／P12e／P12f＋P12 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5469304 @08:14。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `804e90e95`（7.2.1 P1e B-minus peek-name／first_token；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P1e B-minus：lex_skip.x 补 peek-name／first_token** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P19 `helpers.inc` 仍 host-cc `parse_peek_function_name`／`first_token_kind`（FUNCTION＋IDENT／SPAWN dest-buffer 与 init＋一步 kind 可 B-minus 复用 P9a 桥＋P1b copy）；存储＝有则补全 `pthin_lex_skip.x`（P1 已有 P9a，禁给 P19 加硬闸）＋helpers.inc trampoline＋`#ifdef XLANG_PTHIN_LEX_SKIP_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁包 leftover peek-name AUDIT；禁开新 P 道。**g05 `-E`+cc**（pure-asm CG002，同 P1b／P1c／P1d）。实测 hybrid P19 C 8304 vs 冷 9904；glue.o 1974912；xlang 5468664；g05 ensure P1b／P1c／P1d／P1e＋P19 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5468664 @07:51。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `e20d4a29f`（7.2.1 P1d B-minus ASI advance_past；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P1d B-minus：lex_skip.x 补 ASI advance_past** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P19 `helpers.inc` 仍 host-cc `advance_past_stmt_semicolon`／`advance_past_cond_rparen`（ASI 一／二 token 走查可 B-minus 复用 P9a 桥；`*r_out` 仍 C 回放 `lexer_next_into`）；存储＝有则补全 `pthin_lex_skip.x`（P1 已有 P9a，禁给 P19 加硬闸）＋helpers.inc trampoline＋`#ifdef XLANG_PTHIN_LEX_SKIP_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁把 ident_is_unsafe_stmt 并进 P4b buf；禁开新 P 道。**g05 `-E`+cc**（pure-asm CG002，同 P1b／P1c）。实测 hybrid P19 C 8696 vs 冷 9904；glue.o 1974424；xlang 5468200；g05 ensure P1b／P1c／P1d＋P19 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5468200 @07:26。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `c6bcc1db6`（7.2.1 P11d B-minus collect_imports dest-buffer；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P11d B-minus：imports.x 补 collect_imports dest-buffer** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P11 `imports.inc` 在 P11c 后仍 host-cc collect_imports（`const name = import("path");` token 走查可 B-minus 复用 P11c consume_path＋P18 cfg_skip＋P1b copy＋P9a 桥；module 写入仍 C helper）；存储＝有则补全 `pthin_imports.x`＋C trampoline（语言无 local u8[N]）＋`#ifdef XLANG_PTHIN_IMPORTS_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁抄 skip_imports（fail-leave 在 CONST vs 其后）；禁包 leftover collect walk AUDIT；禁开新 P 道。**g05 `-E`+cc**（pure-asm CG002，同 P11c）。实测 hybrid P11 C 3784 vs 冷 6104；glue.o 1972792；xlang 5467416；g05 ensure P11b／P11c／P11d＋P11 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5467416 @07:07。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `45c1c08d9`（7.2.1 P12e B-minus enum_register dest-buffer；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P12e B-minus：skip_tl.x 补 enum_register／append_variants** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P12 `skip_tl.inc` 在 P12d 后仍 host-cc skip_one_enum_register／append_variants（`enum Name { V, … }` token 走查可 B-minus 复用 P1b copy＋P9a 桥；module 写入仍 C helper）；存储＝有则补全 `pthin_skip_tl.x`＋C trampoline（语言无 local u8[N]）＋`#ifdef XLANG_PTHIN_SKIP_TL_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁包 skip_one_trait；禁抄 skip_one_enum（opaque brace skip 会丢变体）；禁开新 P 道。**g05 `-E`+cc**（pure-asm CG002，同 P12c／P12d）。实测 hybrid P12 C 93824 vs 冷 102056；glue.o 1972168；xlang 5467144；g05 ensure P12b／P12c／P12d／P12e＋P12 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5467144 @06:49。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `61ce5b0b5`（7.2.1 P12d B-minus generic_bound_scan dest-buffer；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P12d B-minus：skip_tl.x 补 generic_bound_scan dest-buffer** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P12 `skip_tl.inc` 在 P12c 后仍 host-cc generic_bound_scan（全文件 `<T: Trait>`／type-param／`callee<A,B>` token 走查可 B-minus 复用 P1b skip_generic_angle＋copy＋P9a 桥；file-local bound／call／gp 表仍 C）；存储＝有则补全 `pthin_skip_tl.x`＋C trampoline（语言无 file-local 静态表）＋`#ifdef XLANG_PTHIN_SKIP_TL_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁包 skip_one_trait；禁再抄 skip_generic_angle／copy_slice；禁开新 P 道。**g05 `-E`+cc**（pure-asm CG002，同 P12c）。实测 hybrid P12 C 94632 vs 冷 102056；glue.o 1970984；xlang 5450136；g05 ensure P12b／P12c／P12d＋P12 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5450136 @06:29。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `4a4c9ffd5`（7.2.1 P12c B-minus skip_one_impl dest-buffer；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P12c B-minus：skip_tl.x 补 skip_one_impl dest-buffer** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P12 `skip_tl.inc` 在 P12b 后仍 host-cc skip_one_impl（`impl [ <T> ] Type [ for Type ] {` 头走查可 B-minus 复用 P1b skip_generic_angle＋copy＋P9a 桥；file-local impl-seen 表仍 C）；存储＝有则补全 `pthin_skip_tl.x`＋C trampoline（语言无 file-local 静态表）＋`#ifdef XLANG_PTHIN_SKIP_TL_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁包 skip_one_trait；禁再抄 skip_generic_angle／copy_slice；禁开新 P 道；禁 skip_balanced_braces（wave390 留方法给外层 parse_into）。**g05 `-E`+cc**（pure-asm CG002，P12b 仅 struct／enum／extern 时曾过）。实测 hybrid P12 C 98808 vs 冷 102056；glue.o 1968424；xlang 5449704；g05 ensure P12b／P12c＋P12 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5449704 @06:07。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `a39d42e5b`（7.2.1 P17c G.7 diag_skip_let_const_buf trampoline over P18b into；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P17c G.7：diag_skip_let_const_buf trampoline over P18b into** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P17 `diag_late.inc` 在 P17b 后仍 host-cc buf C 走查孪生（相对 P18b into 缺 U8／U32／U64／USIZE 与 `[]` 后缀）；存储＝G.7 有则补全：buf 改 trampoline over P18b into_slice（同 body_skip_buf；禁新 .x 导出）；消费＝`parser_asm_thin_glue.o`。修＝杀掉双权威；禁再抄 into；禁开新 P 道。实测 hybrid P17 C 2568 vs 冷 5560；glue.o 1968128；xlang 5449336；g05 ensure P17b／P17c＋P17 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5449336 @05:49。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `d26bb4d4c`（7.2.1 P1c lex_skip.x 补 skip_generic_angle_list_count；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P1c B-minus：lex_skip.x 补 skip_generic_angle_list_count** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P1 `lex_skip.inc` 在 P1b 后仍 host-cc count＋pending 走查（`<T,U>` 计数＋声明位 IDENT 捕获可 B-minus 复用同文件 skip_generic_angle_into＋stmt-boundary＋copy＋P9a 桥；g_gp_pending_* 静态表仍 C）；存储＝有则补全 `pthin_lex_skip.x`＋C trampoline（语言无 global u8[8][64]）＋`#ifndef XLANG_PTHIN_LEX_SKIP_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁再抄 skip_generic_angle_list_into；禁开新 P 道。**g05 `-E`+cc**（pure-asm CG002，同 P1b）。实测 hybrid P1 C 6240 vs 冷 10208；glue.o 1969480；xlang 5449128；g05 ensure P1b／P1c＋P1 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5449128 @05:32。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `999b3d17d`（7.2.1 P18c body_tl.x 补 cfg_skip／diag_first_ident；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P18c B-minus：body_tl.x 补 cfg_skip／diag_first_ident** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P18 `body_tl.inc` 在 P18b 后仍 host-cc cfg_skip／diag_first_ident（`#[cfg]` 后顶层 CONST／LET／STRUCT／FUNCTION 顺序分发＋function 后首 IDENT ident_len 可 B-minus 复用同文件 skip_one_top_level＋P12b skip_one_struct＋P10b skip_one_function_full＋P9a 桥；P010–P014／onefunc_param_name_dup 仍 C）；存储＝有则补全 `pthin_body_tl.x`＋C trampoline（语言无 lexer_init）＋`#ifndef XLANG_PTHIN_BODY_TL_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁再抄 skip_one_struct／skip_one_function_full／skip_one_if；禁开新 P 道。**pure-asm 过**。实测 hybrid P18 C 6696 vs 冷 8688；glue.o 1968136；xlang 5448744；g05 ensure P18b／P18c＋P18 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5448744 @05:11。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `cba46b110`（7.2.1 P11c imports.x 补 consume_path／try_skip；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P11c B-minus：imports.x 补 consume_path／try_skip** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P11 `imports.inc` 在 P11b 后仍 host-cc consume_path／try_skip（`import("path");` 顺序走查＋IDENT＝import 失败还原可 B-minus 复用 P1b copy＋P9b validate／finalize＋P9a 桥；collect／copy_token_bytes 仍 C）；存储＝有则补全 `pthin_imports.x`＋C trampoline（语言无 local u8[128]）＋`#ifndef XLANG_PTHIN_IMPORTS_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁再抄 copy_slice／validate／finalize；禁开新 P 道；禁包 leftover collect walk AUDIT。**g05 `-E`+cc**（pure-asm CG002，同 P1b／P5b／P19b）。实测 hybrid P11 C 4808 vs 冷 6104；glue.o 1966880；xlang 5448264；g05 ensure P11b／P11c＋P11 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5448264 @04:47。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `f2c47b375`（7.2.1 P17b diag_late.x 跳过 after_structs／fail 走查；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P17b B-minus：编 diag_late.x 跳过 after_structs／fail 走查** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P17 `diag_late.inc` 仍 host-cc（after_structs STRUCT 循环＋fail_at_token_kind 顺序走查可 B-minus 复用 P12b skip_one_struct＋P18b scalar／body_skip＋P1b is_pointee＋P9a 桥；diag_skip_let_const_buf／body_skip_buf 仍 C）；存储＝P17b `g05_try_x_to_o`＋`XLANG_PTHIN_DIAG_LATE_BODIES_FROM_X`＋`#ifdef XLANG_PARSER_STRETCH_AUDIT`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；C 留 trampoline＋buf skip；禁再抄 skip_one_struct／body_skip／is_fn_sig／is_pointee；stash 仍 C。**pure-asm 过**（无 CG002）。实测 hybrid P17 C 3920 vs 冷 6856；glue.o 1965160；xlang 5447368；g05 ensure P17b＋P17 C（移到 P18b 后）＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5447368 @04:24。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `51ce1626d`（7.2.1 P15b library.x 跳过 library_scan 走查；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P15b B-minus：编 library.x 跳过 library_scan 走查** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P15 `library_wrap.inc` 仍 host-cc（library_scan ~190 行 token 走查＋into／buf ~1500 行已 T AUDIT 前缀含 HARD BAN 名可 B-minus／ifdef；into／buf parse／lex_from 仍 C）；存储＝P15b `g05_try_x_to_o`＋`XLANG_PTHIN_LIBRARY_BODIES_FROM_X`＋`#ifdef XLANG_PARSER_STRETCH_AUDIT`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 scan 走查；C 留 trampoline＋into／buf；禁再抄 P1b copy。**pure-asm 过**（无 CG002）。实测 hybrid P15 C 2592 vs 冷 4952；glue.o 1962272；xlang 5430136；g05 ensure P15b＋P15 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5430136 @04:02。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `f3ca4e1cb`（7.2.1 P5c ctrl.x 补 scan_sync pos；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P5c Route C：ctrl.x 补 scan_sync pos** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P5 `if_stmt.inc` 在 P5b 后仍 host-cc scan_sync（注释感知找 then `{`／else／else-if 循环；parse／realign 仍 C）；存储＝有则补全 `pthin_ctrl.x` pos 导出＋C by-value trampoline（语言无结构体按值）；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 scan_sync；禁再抄 P1b token skip_balanced／P9b skip_ws；禁包 leftover else-if AUDIT；禁迁 dead `sync_lex_after_if_cond_paren`。**g05 `-E`+cc**（pure-asm CG002，同 P5b／P1b／P19b）。实测 hybrid P5 C 19072 vs 冷 23184；glue.o 1958080；xlang 5429208；g05 ensure P5b／P5c＋P5 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5429208 @03:41。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `b3c02e46e`（7.2.1 P5b 编 ctrl.x 跳过注释感知花括号字节扫描／kw_at_pos；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P5b Route C：编 ctrl.x 跳过注释感知花括号字节扫描／kw_at_pos** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P5 `if_stmt.inc` 仍 host-cc（注释感知 `{...}` 字节扫描／kw_at_pos 可 Route C；parse／scan_sync／match／if_expr 仍 C；~770 行已 T AUDIT 前缀含 HARD BAN 名可 ifdef）；存储＝P5b `g05_try_x_to_o`＋`XLANG_PTHIN_CTRL_BODIES_FROM_X`＋`#ifdef XLANG_PARSER_STRETCH_AUDIT`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接字节扫描／kw_at_pos；C 留 slice trampoline＋parse／scan_sync；禁再抄 P1b token skip_balanced；skip_ws 仍 P9b；禁包 leftover else-if AUDIT。**g05 `-E`+cc**（pure-asm CG002，同 P1b／P19b）。实测 hybrid P5 C 21216 vs 冷 23184；glue.o 1957176；xlang 5428376；g05 ensure P5b＋P5 C＋P11b＋P13b＋P10b＋P9a＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5428376 @03:22。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `efd9c29b7`（7.2.1 P11b 编 imports.x 跳过 skip_imports 走查；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P11b B-minus：编 imports.x 跳过 skip_imports 走查** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P11 `imports.inc` 仍 host-cc（skip_imports 走查＋skip_imports_buf AUDIT＋collect_imports ~1500 行已 T AUDIT 前缀含 HARD BAN 名可 B-minus／ifdef；try_skip／consume_path／collect 走查仍 C）；存储＝P11b `g05_try_x_to_o`＋`XLANG_PTHIN_IMPORTS_BODIES_FROM_X`＋peek_int_val 桥补全；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 skip_imports 循环；C 留 trampoline＋try_skip／collect；禁再抄 consume_path。**pure-asm 过**（无 CG002）。实测 hybrid P11 C 6176B vs 冷 6104B；glue.o 1955352；xlang 5427720；g05 ensure P11b＋P11 C＋P13b＋P10b＋P9a＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5427720 @03:03。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `9b4d7db48`（7.2.1 P13b 编 try_skip_allow.x 跳过 padding 走查；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P13b B-minus：编 try_skip_allow.x 跳过 padding 走查** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P13 `try_skip_allow.inc` 仍 host-cc（padding_struct slice／buf ~1500 行已 T AUDIT 探针含 HARD BAN 名＋peek LPAREN／skip_balanced 走查可 B-minus 复用 P1b skip_balanced＋P9a 桥；write_result／parse_into 仍 C）；存储＝P13b `g05_try_x_to_o`＋`XLANG_PTHIN_TRY_SKIP_ALLOW_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；C 留 trampoline＋write_result／parse_into；禁再抄 skip_balanced。**pure-asm 过**（无 CG002）。实测 hybrid P13 C 1936B vs 冷 2248B；glue.o 1953472；xlang 5427080；g05 ensure P13b＋P13 C＋P10b＋P12b＋P18b＋P9a＋P1b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5427080 @02:39。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `26b1c5dab`（7.2.1 P10b 编 glue.x 跳过 skip_one_function_full 走查；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P10b B-minus：编 glue.x 跳过 skip_one_function_full 走查** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P10 `glue_tail.inc` 仍 host-cc（skip_one_function_full ~870 行已 T AUDIT 探针含 HARD BAN 名＋async／export／function／IDENT／parens／braces 走查可 B-minus 复用 P1b skip_balanced＋P12b skip_one_extern＋P9a 桥；glue 包装／parse glue 仍 C）；存储＝P10b `g05_try_x_to_o`＋`XLANG_PTHIN_GLUE_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；C 留 trampoline＋包装；禁再抄 skip_balanced／skip_one_extern。**pure-asm 过**（无 CG002）。实测 hybrid P10 C 35976B vs 冷 38464B；glue.o 1952712；xlang 5410280；g05 ensure P10b＋P10 C＋P12b＋P18b＋P9a＋P1b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5410280 @02:24。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `c77a76358`（7.2.1 P18b 编 body_tl.x 跳过 scalar 表＋let／const／if skip 走查；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P18b Route C＋B-minus：编 body_tl.x 跳过 scalar 表＋let／const／if skip 走查** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P18 `body_tl.inc` 仍 host-cc（is_fn_sig_scalar 可 Route C；diag_skip／body_skip ~760 行已 T AUDIT 探针含 HARD BAN 名＋skip_one_top_level let／const 可 B-minus 复用 P14b skip_one_if＋P9a 桥；P010–P014／diag_first_ident／cfg_skip 仍 C）；存储＝P18b `g05_try_x_to_o`＋`XLANG_PTHIN_BODY_TL_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接表＋走查；C 留 trampoline＋diag／cfg；禁再抄 skip_one_if。**pure-asm 过**（无 CG002）。实测 hybrid P18 C 7256B vs 冷 8688B；glue.o 1952936；xlang 5410024；g05 ensure P18b＋P18 C＋P14b＋P12b＋P9a＋P1b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5410024 @02:07。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `fc9458c6e`（7.2.1 P12b 编 skip_tl.x 跳过 struct／enum／extern 走查；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P12b B-minus：编 skip_tl.x 跳过 struct／enum／extern 走查** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P12 `skip_tl.inc` 仍 host-cc（skip_one_struct ~800 行已 T AUDIT 探针＋enum／extern 走查可 B-minus 复用 P1b skip_balanced／skip_generic_angle＋P9a 桥；trait-reg／enum register／parse_one_extern 仍 C）；存储＝P12b `g05_try_x_to_o`＋`XLANG_PTHIN_SKIP_TL_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；C 留 trampoline＋stash_source＋trait-reg；禁再抄 skip_balanced／skip_generic_angle。**pure-asm 过**（无 CG002）。实测 hybrid P12 C 100032B vs 冷 102056B；glue.o 1949616；xlang 5409288；g05 ensure P12b＋P12 C＋P14b＋P4bb＋P4ub＋P7b＋P3b＋P4b＋P9a＋P1b＋P19b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5409288 @01:50。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `82f592c45`（7.2.1 P14b 编 skip_if.x 跳过 trait／impl＋if 走查；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P14b B-minus：编 skip_if.x 跳过 trait／impl＋if 走查** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P14 `skip_if.inc` 仍 host-cc（trait／impl＋if-core／statement 可 B-minus 复用 P1b skip_balanced＋P9a 桥；enum register 仍 C；~780 行已 T AUDIT 探针产品侧是 nop）；存储＝P14b `g05_try_x_to_o`＋`XLANG_PTHIN_SKIP_IF_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；C 留 trampoline＋enum；禁再抄 skip_balanced。**pure-asm 过**（无 CG002）。实测 hybrid P14 C 2560B vs 冷 5232B；glue.o 1948088；xlang 5408776；g05 ensure P14b＋P14 C＋P4bb＋P4ub＋P7b＋P3b＋P4b＋P9a＋P1b＋P19b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5408776 @01:34。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `c505c6ac5`（7.2.1 P4bb 编 binop.x 跳过 TOKEN→ExprKind；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P4bb Route C：编 binop.x 跳过 TOKEN→ExprKind** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P4b `expr_binop.inc` 仍 host-cc（5 级 TOKEN if 可 Route C；parse／wrap／peek／single_tok_chain 仍 C；parse_logor ~700 行已 T AUDIT 探针产品侧是 nop）；存储＝P4bb `g05_try_x_to_o`＋`XLANG_PTHIN_EXPR_BINOP_BODIES_FROM_X`＋`#ifdef XLANG_PARSER_STRETCH_AUDIT`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 TOKEN→ExprKind；C 留 wrap＋parse；禁与 unary 表合并（STAR／AMP／MINUS 一元≠二元）。**pure-asm 过**（无 CG002）。实测 hybrid P4b C 9448B vs 冷 10024B；glue.o 1947112；xlang 5408232；g05 ensure P4bb＋P4b C＋P4ub＋P7b＋P3b＋P4b＋P9a＋P1b＋P19b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5408232 @01:16。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `8519db305`（7.2.1 P4ub 编 unary.x 跳过 TOKEN→ExprKind；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P4ub Route C：编 unary.x 跳过 TOKEN→ExprKind** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P4u `unary.inc` 907 行仍 host-cc（TOKEN→ExprKind 可 Route C；parse／wrap／arena 仍 C；~700 行已 T AUDIT 探针产品侧是 nop）；存储＝P4ub `g05_try_x_to_o`＋`XLANG_PTHIN_EXPR_UNARY_BODIES_FROM_X`＋`#ifdef XLANG_PARSER_STRETCH_AUDIT`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 TOKEN→ExprKind；C 留 wrap＋parse；8 路 if 收敛为一表。**pure-asm 过**（无 CG002）。实测 hybrid P4u C 2768B vs 冷 3064B；glue.o 1945600；xlang 5407928；g05 ensure P4ub＋P4u C＋P7b＋P3b＋P4b＋P9a＋P1b＋P19b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5407928 @01:00。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `32f8d0fca`（7.2.1 P7b 编 simd.x 跳过 shuffle／select ident／callee 名；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P7b Route C：编 simd.x 跳过 shuffle／select ident／callee 名** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P7 `simd_builtin.inc` 212 行仍 host-cc（shuffle／select ident 与 callee 名填充可 Route C；parse／arena 仍 C）；存储＝P7b `g05_try_x_to_o`＋`XLANG_PTHIN_SIMD_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 ident pack／callee 名；C 留 slice trampoline＋parse。**pure-asm 过**（无 CG002）。实测 hybrid P7 C 3024B vs 冷 3896B；glue.o 1945832；xlang 5407592；g05 ensure P7b＋P7 C＋P3b＋P4b＋P9a＋P1b＋P19b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5407592 @00:47。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `291506d74`（7.2.1 P3b 编 type_ref.x 跳过 kind／dyn／builtin TypeKind／vector IDENT；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P3b Route C：编 type_ref.x 跳过 kind／dyn／builtin TypeKind／vector IDENT** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P3 `type_ref.inc` 1036 行仍 host-cc（token_starts_type／ident_is_dyn／TOKEN→TypeKind／vector IDENT 可 Route C；parse／arena 仍 C；skip_tl 另有一份 TOKEN→TypeKind 静态表）；存储＝P3b `g05_try_x_to_o`＋`XLANG_PTHIN_TYPE_REF_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 portable 区；C 留 trampoline＋parse；skip_tl `xlang_trait_token_to_type_kind_c` 转调 `parser_asm_type_ref_builtin_kind_ord_c`（一表）。**pure-asm 过**（无 CG002）。实测 hybrid P3 C 16160B vs 冷 18376B；glue.o 1942912；xlang 5406920；g05 ensure P3b＋P3 C＋P4b＋P9a＋P9b＋P1b＋P19b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5406920 @00:34。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `03afbadc2`（7.2.1 P4b 编 primary.x 跳过 ident 拼写／asm-option-bit；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P4b Route C：编 primary.x 跳过 ident 拼写／asm-option-bit** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P4 `primary.inc` 2947 行仍 host-cc（8 个 IDENT 拼写／option-bit 可 Route C；`parse_primary_into` ~770 行已 T AUDIT 探针产品侧是 nop；parse／arena 仍 C）；存储＝P4b `g05_try_x_to_o`＋`XLANG_PTHIN_EXPR_PRIMARY_BODIES_FROM_X`＋`#ifdef XLANG_PARSER_STRETCH_AUDIT`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 ident 拼写／option-bit；C 留 slice trampoline＋parse；helpers `ident_is_unsafe_stmt` 仍 C（by-value ABI，本波不合并）。**pure-asm 过**（无 CG002）。实测 hybrid P4 C 40512B vs 冷 43096B；glue.o 1937840；xlang 5389512；g05 ensure P4b＋P4 C＋P9a＋P9b＋P1b＋P19b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5389512 @00:13。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
