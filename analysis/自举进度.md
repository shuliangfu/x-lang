# Xlang / X language 自举进度

> **更新**：2026-09-23 · **w851** 整文件删除 `seeds/cfg_eval_host_lit.from_x.c`。`cfg_host_os_lit` 与 `cfg_host_arch_lit` 的唯一函数体是 `src/lexer/cfg_eval_host_lit.x`。阶梯只做纯 asm，失败即停，没有 host-cc 回退，没有 gcc `-E`。当前编译器重汇编与链上对象逐字节相同（501）。没有重编 `cfg_eval.o`，没有重编 `runtime_driver_no_c.o`。Darwin 重链四个二进制 **3929656**（21:28），与重链前逐字节相同，L2 **5/5**。Ubuntu 既有 tip **12537856**（15:46）L2 **5/5**，不重链。Windows 既有 `xlang_asm.exe` **4385778**（15:27）L2 **5/5**，不重链。钉盘 **`ecdb5cc1e`**（不升）。
> **状态**：**尚未完全自举**。`cfg_eval` 的 link_alias、linux pin、bootstrap stub 仍 host-cc。marker 仍 host-cc。lexer 尾仍 host-cc。`rt_stack.x` 纯 asm CG002，本波不动。不重编 `runtime_driver_no_c.o`。pabi 活 rest 仍 host-cc。
> **下波**：再找一份业务已经在 `.x`、产品对象是独立 `.o`、且当前编译器重汇编的 `__text` 与链上对象一致的 from_x，再物理删除 C 体。对不上就不要重链。不要重开 `async_asm_pool`。lexer 尾还没有 `.x` 体。不要对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编。`rt_stack` 的 CG002 不要用 gcc `-E` 修。活 pabi rest 的 thin 面仍是 HARD BAN。禁 mega FORCE 当产品默认；禁 glue_tail；禁剥 thin_glue；禁升钉。Ubuntu 在 `g_pipeline_elf_data_*` 能链接、且不覆盖 15:46 slot_bytes tip 之前不重链。

闸门：**日常放行＝三端 L2 硬闸已启用**（见自举验证／测试环境）；暂停 `xlang check`；L4 仅整波／谈自举／经理另派。

### 维护约定

1. 每波必改本文（KPI／前排≤3／波次流水一行）。终局债改 [`C迁移追踪.md`](C迁移追踪.md)。
2. **禁止**新增／恢复「本日要点」章节；刀次只记波次流水（及既有其它节）。
3. 波次流水倒序 · 一行一波。过期整日流水日切归档后主文档重写。
4. **日切**：新的一天先归档前一天本文为 `自举进度-归档-YYYY-MM-DD.md`，再重写当天本文。

***



## Class CU · w851 · cfg_eval_host_lit 的 C 种子整文件删除

- **刀**：`seeds/cfg_eval_host_lit.from_x.c` 整文件删除。`cfg_host_os_lit` 与 `cfg_host_arch_lit` 的唯一函数体是 `src/lexer/cfg_eval_host_lit.x`（`#[cfg]` 字符串，`#[no_mangle]`）。`_cfg_eval_link_x_plus_host_lit` 只把该 `.x` 纯 asm 到临时对象，nm 看到两个符号后才换上；失败留下原对象并返回 1。没有 host-cc，没有 gcc `-E`。不重编 `src/lexer/cfg_eval.o`，不重编 `runtime_driver_no_c.o`。
- **证**：当前 tip 纯 asm 该 `.x` 与链上 `cfg_eval_host_lit.o` 逐字节相同（501，只有 `__text` 120）。`cfg_eval.o` 仍是 15000（09-22 08:42）。`runtime_driver_no_c.o` 仍是 125136（19:23）。Darwin 重链四个二进制都是 3929656（21:28），与重链前逐字节相同，L2 **5/5**（option=102）。不跑 mega `-E`。
- **残**：阶梯后面的 link_alias、linux pin、bootstrap stub 仍 host-cc。不是 host-cc=0。Ubuntu 既有 tip 12537856（15:46）L2 **5/5**。Windows 既有 `xlang_asm.exe` 4385778（15:27）L2 **5/5**。两端都不是本波重链。
- **禁**：把这份 C 种子或 host-cc 回退加回去；跑阶梯去重编 `cfg_eval.o`（`cfg_eval.x` 的当前汇编尚未和链上对象对过）；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；重链 `async_asm_pool`（当前汇编栈槽对不上）；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class CT · w849 · backend_arch_emit_dispatch 47 个分派壳的 C 体物理删除

- **刀**：47 个 `backend_arch_emit_*` ta 分派壳的 C 体从 `seeds/backend_arch_emit_dispatch.from_x.c` 删除。无人引用的 `seeds/backend_arch_emit_dispatch_thin.from_x.c` 整文件删除。唯一函数体是 `src/asm/backend_arch_emit_dispatch.x`。种子留下 `backend_arch_emit_dispatch_slice_marker`（返回 0）。`ensure_arch_emit_dispatch_pure` 只做 `pure_asm_x_to_o` 再 `cc` marker；失败即停。没有 gcc `-E`，没有冷种子。`ensure_one`、`try-r3-prefer`、`try-r3-cold` 和 `build_xlang_asm` 都走它。`XLANG_G05_PREFER_X_O` 被忽略。Windows 走同一条。不重编 `runtime_driver_no_c.o`。
- **证**：当前 tip 纯 asm 该 `.x` 后与 marker 合并。定义符号名与删前一致，未定义符号仍是 135 个。`__text` 与删前逐字节相同。marker 仍是 `mov w0, #0; ret`。Darwin 重链四个二进制都是 3929656（20:47），与 20:26 重链逐字节相同，L2 **5/5**（option=102）。`runtime_driver_no_c.o` 仍是 125136（19:23）。hello 打印 Hello World。不跑 mega `-E`。
- **残**：marker 仍 host-cc，`.c` 文件还在（701 字节）。不是 host-cc=0。Ubuntu 既有 tip 12537856（15:46）L2 **5/5**。Windows 既有 `xlang_asm.exe` 4385778（15:27）L2 **5/5**。两端都不是本波重链。
- **禁**：把这 47 个 C 体或 thin 种子加回去；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class CS · w848 · lsp_diag_pipeline_sizes 产品种子整文件删除

- **刀**：`seeds/lsp_diag_pipeline_sizes.from_x.c` 整文件删除。`lsp_diag_pipeline_sizeof_arena`、`lsp_diag_pipeline_sizeof_module`、`lsp_diag_pipeline_sizeof_dep_ctx` 的唯一函数体是 `src/lsp/lsp_diag_pipeline_sizes.x`（常量 16／40／1368）。产品对象 `src/lsp/lsp_diag_pipeline_sizes_nostub.o` 由 `try-lsp-sat-prefer` 只做 `pure_asm_x_to_o`；失败即停。没有 gcc `-E`，没有冷种子。Windows 走同一条。g05 热路径改走这个安装器。`XLANG_G05_PREFER_X_O` 被忽略。不重编 `runtime_driver_no_c.o`。
- **证**：删种子前，当前 tip 纯 asm 该 `.x` 与产品对象逐字节相同（580）。安装器重编后仍是 580，三个 T，无未定义符号。Darwin 重链四个二进制都是 3929656（20:26），与重链前逐字节相同，L2 **5/5**（option=102）。`runtime_driver_no_c.o` 仍是 125136（19:23）。hello 打印 Hello World。不跑 mega `-E`。
- **残**：非产品 `seeds/lsp_diag_pipeline_sizes_weak.from_x.c` 仍 host-cc（xlang-c 链的 `lsp_diag_pipeline_sizes.o`，不是这颗产品对象）。不是 host-cc=0。Ubuntu／Windows 产品二进制本波不重链。
- **禁**：把这颗 C 种子或 gcc `-E` 回退加回去；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class CR · w847 · x_frontend_link_alias 18 个别名的 C 体物理删除

- **刀**：18 个别名函数的 C 体，连同 `#if !defined(XLANG_XFLA_ASM)` 门，从 `seeds/x_frontend_link_alias.from_x.c` 删除。唯一函数体是 `x_frontend_link_alias.x`。种子留下 lexer 结构返回尾和带修饰 ABI 别名。`ensure_x_frontend_link_alias_prefer` 只做纯 asm，再 `cc` 种子，再 `pure_ld_partial_merge`；失败即停，不再整份 `cc` 回退。五个别名（`check_block_impl`、`check_expr_impl`、`find_or_alloc_ptr_type_ref`、`pipeline_typeck_set_active_ctx_c`、`pipeline_typeck_ptr_for_addr_of_operand_c`）仍经 `G05_X_O_WEAK_FUNCS` 保持 weak。Windows 走同一条。`try-heat`／`try-r1`、alias-stubs 家族和 g05 都走它。`-DXLANG_XFLA_ASM` 现在是空操作。不重编 `runtime_driver_no_c.o`。
- **证**：当前 tip 纯 asm 该 `.x` 后与 rest 合并。定义符号名 68 个，与删 C 体前一致；五个仍是 weak external。目标文件从 ar 9776 变成 Mach-O 7880。Darwin 重链四个二进制都是 3929656（20:10），与重链前逐字节相同，L2 **5/5**（option=102）。`runtime_driver_no_c.o` 仍是 125136（19:23）。Ubuntu 既有 `xlang`／`xlang_asm` 12537856（15:46）L2 **5/5**。Windows 既有 `xlang_asm.exe` 4385778（15:27）L2 **5/5**。两端都不是本波重链。不跑 mega `-E`。
- **残**：lexer 尾和带修饰别名仍 host-cc，`.c` 文件还在。不是 host-cc=0。`rt_stack.x` 纯 asm CG002。Ubuntu／Windows 产品二进制本波不重链。
- **禁**：把这 18 个 C 体或 `XLANG_XFLA_ASM` 门加回去；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class CQ · w846 · rt_parse_diag 精确诊断的 C 体物理删除

- **刀**：`runtime_report_precise_parse_failure_if_known` 的 C 体，连同 `PRECISE_BRIDGE` 包装，从 `seeds/rt_parse_diag.from_x.c` 删除。唯一函数体是 `src/runtime/rt_parse_diag.x`（`#[no_mangle]`）。种子留下 `runtime_report_parse_recovery_diagnostics` 和 `labi_rt_parse_diag_slice_marker`。`ensure_rt_parse_diag_prefer` 只做纯 asm 再 `cc` 种子；失败即停，不再整份 `cc` 回退。Windows 走同一条。g05 rt-slice、`build_xlang_asm`、strict glue、experimental bootstrap 都走它。不定义 `PRECISE_BRIDGE`。不重编 `runtime_driver_no_c.o`。
- **证**：当前 tip 纯 asm 该 `.x` 后与 rest `ld -r`，精确诊断、恢复诊断、marker 都是 T。目标文件 16792 字节，与删 C 体前的产品对象逐字节相同。Darwin 重链四个二进制都是 3929656（19:56），与重链前逐字节相同，L2 **5/5**（option=102）。Ubuntu 既有 `xlang`／`xlang_asm` 12537856（15:46）L2 **5/5**。Windows 既有 `xlang_asm.exe` 4385778（15:27）L2 **5/5**。两端都不是本波重链。不跑 mega `-E`。
- **残**：恢复诊断仍 host-cc，`.c` 文件还在。不是 host-cc=0。`rt_stack.x` 纯 asm CG002。Ubuntu／Windows 产品二进制本波不重链。
- **禁**：把精确诊断的 C 体或 `PRECISE_BRIDGE` 加回去；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class CP · w845 · rt_emit_state 五个 setter 的 C 体物理删除

- **刀**：`driver_run_x_emit_c_set_path`、`set_lib`、`set_n_lib_roots`、`set_emit_extern`、`driver_argv_parse_x_emit_c` 的 C 体从 `seeds/rt_emit_state.from_x.c` 删除。唯一函数体是 `src/runtime/rt_emit_state.x`（`#[no_mangle]`，经 driver_abi 槽写共享缓冲）。种子留下 BSS、`xlang_driver_x_emit_set_lib_name`、`xlang_driver_x_emit_lib_name_into`、`xlang_pipeline_pctx_set_entry_lib_prefix` 和 `labi_rt_emit_state_slice_marker`。`ensure_rt_emit_state_prefer` 只做纯 asm 再 `cc` 种子；失败即停，不再整份 `cc` 回退。Windows 走同一条。g05 rt-slice、`build_xlang_asm`、strict glue、experimental bootstrap 都走它。不重编 `runtime_driver_no_c.o`。
- **证**：当前 tip 纯 asm 该 `.x` 后与 rest `ld -r`，五个 setter、槽辅助、lib-name、入口前缀和 marker 都是 T，共享缓冲是 COMMON。目标文件 7712 字节，与删 C 体前的产品对象逐字节相同。Darwin 重链四个二进制都是 3929656（19:45），与重链前逐字节相同，L2 **5/5**（option=102）。Ubuntu 既有 `xlang`／`xlang_asm` 12537856（15:46）L2 **5/5**。Windows 既有 `xlang_asm.exe` 4385778（15:27）L2 **5/5**。两端都不是本波重链。不跑 mega `-E`。
- **残**：BSS、lib-name、入口前缀仍 host-cc，`.c` 文件还在。不是 host-cc=0。`rt_parse_diag` 的精确诊断函数仍有 C 孪生。`rt_stack.x` 纯 asm CG002。Ubuntu／Windows 产品二进制本波不重链。
- **禁**：把 C setter 加回去；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class CO · w844 · rt_arena_buf 两个函数的 C 体物理删除

- **刀**：`driver_arena_buf` 与 `driver_module_buf` 的 C 体从 `seeds/rt_arena_buf.from_x.c` 删除。唯一函数体是 `src/runtime/rt_arena_buf.x`（`#[no_mangle]`，经 driver_abi 槽访问 128MiB／2MiB 数组）。种子留下这两块 BSS 和 `labi_rt_arena_buf_slice_marker`。`ensure_rt_arena_buf_prefer` 只做纯 asm 再 `cc` 种子；失败即停，不再整份 `cc` 回退。Windows 走同一条。g05 rt-slice、`build_xlang_asm`、strict glue、experimental bootstrap 都走它。不重编 `runtime_driver_no_c.o`。
- **证**：当前 tip 纯 asm 该 `.x` 后与 BSS `ld -r`，两个函数和 marker 都是 T，两块数组是 S（地址差 128MiB），目标文件 1688 字节，与删 C 体前的产品对象逐字节相同。Darwin 重链四个二进制都是 3929656（19:34），L2 **5/5**（option=102）。Ubuntu 既有 `xlang`／`xlang_asm` 12537856（15:46）L2 **5/5**。Windows 既有 `xlang_asm.exe` 4385778（15:27）L2 **5/5**。两端都不是本波重链。不跑 mega `-E`。
- **残**：BSS 仍 host-cc，`.c` 文件还在。不是 host-cc=0。`rt_stack.x` 纯 asm CG002。Ubuntu／Windows 产品二进制本波不重链。
- **禁**：把 C 函数体加回去；对 `runtime_driver_no_c.o` 做优先 `.x` 整包重编；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class CN · w843 · rt_preamble 两个写入函数的 C 体物理删除

- **刀**：`write_io_net_abi_inline` 与 `write_fs_path_map_error_abi_inline` 的 C 循环从 `seeds/rt_preamble.from_x.c` 删除。唯一函数体是 `src/runtime/rt_preamble.x`（`#[no_mangle]`，经 `driver_preamble_fputs` 写 fd 句柄）。种子留下巨型字符串表、`labi_rt_preamble_slice_marker` 和两条不发符号的声明。`ensure_rt_preamble_prefer` 只做纯 asm 再 `cc` 种子；失败即停，不再整份 `cc` 回退。Windows 走同一条。g05 rt-slice、`build_xlang_asm`、strict glue、experimental bootstrap 都走它。
- **证**：当前 tip 纯 asm 该 `.x` rc=0。`ld -r` 后两个写入函数和 marker 都是 T，两张表是 S，目标文件 52408 字节。未定义符号是 `driver_preamble_fputs`、行访问和 `codegen_get_preamble_skip_mask`，产品里已有定义。Darwin 重链四个二进制都是 4007144（19:03），L2 **5/5**（option=102）。Windows 现有 `xlang_asm.exe` 把同一份函数体纯 asm 成两个 T（探针对象 1650 字节，不进产品）。不跑 mega `-E`。
- **残**：字符串表仍 host-cc，`.c` 文件还在。不是 host-cc=0。`rt_stack.x` 纯 asm CG002。Ubuntu／Windows 产品二进制本波不重链。
- **禁**：把 C 写入函数加回去；glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class CM · w842 · rt_parse_diag 切片退出整份 host-cc

- **刀**：POSIX 产品 `src/runtime/rt_parse_diag.o` 改为纯 asm `src/runtime/rt_parse_diag.x`，再 `cc` `seeds/rt_parse_diag.from_x.c` 并带上已有的 `-DXLANG_RT_PARSE_DIAG_FROM_X`。`runtime_report_precise_parse_failure_if_known` 不再进入这次 `cc`。`runtime_report_parse_recovery_diagnostics` 与 `labi_rt_parse_diag_slice_marker` 留在 rest。不传 `-DXLANG_RT_PARSE_DIAG_PRECISE_BRIDGE`：`.x` 是 `#[no_mangle]`，对象已导出短名；该桥只服务 try-rt-prefer 临时路径里 gcc `-E` 的带模块前缀符号。权威在 `ensure_rt_parse_diag_prefer`；g05、`build_xlang_asm`、strict glue 都走它。种子文件未改。
- **证**：无该宏时预处理是当前种子的 29625。加上宏 **29625→29041**。当前 tip 纯 asm 该 `.x` rc=0，唯一 T 是短名。`ld -r` 后精确诊断、恢复诊断、marker 都是 T，目标文件 16792 字节。未定义符号是 `parser_diag_fail_at_token_kind_buf` 与 `diag_report_with_code`，产品里已有定义。Darwin 重链 `xlang`／`xlang_asm`／`xlang-c`／`bootstrap_xlangc`（4007224，18:49），L2 **5/5**（option=102）。不跑 mega `-E`。
- **残**：rest 仍 host-cc。Windows 与 `XLANG_G05_PREFER_X_O=0` 仍整份 `cc`。不是 host-cc=0。`rt_preamble.x` 也能纯 asm，产品切片仍整份 `cc`（巨型字符串表留种子）。`rt_stack.x` 纯 asm CG002。Ubuntu／Windows 本波只待快进，不重链。
- **禁**：glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip；给这颗产品切片定义 `PRECISE_BRIDGE`。
- **钉盘**：ecdb5cc1e 不升。

## Class CL · w841 · rt_arena_buf 切片退出整份 host-cc

- **刀**：POSIX 产品 `src/runtime/rt_arena_buf.o` 改为纯 asm `src/runtime/rt_arena_buf.x`，再 `cc` `seeds/rt_arena_buf.from_x.c` 并带上已有的 `-DXLANG_RT_ARENA_BUF_FROM_X`。`driver_arena_buf`、`driver_module_buf` 不再进入这次 `cc`。128MiB `driver_arena_static`、2MiB `driver_module_static`、`labi_rt_arena_buf_slice_marker` 留在 rest。权威在 `ensure_rt_arena_buf_prefer`；g05、`build_xlang_asm`、strict glue 都走它。种子文件未改。
- **证**：无该宏时预处理是当前种子的 33303。加上宏 **33303→32850**。当前 tip 纯 asm 该 `.x` rc=0。`ld -r` 后两个函数是 T，两块 BSS 是 S（地址差 128MiB），目标文件 1688 字节。对象重定位是 `memset`。Darwin 重链 `xlang`／`xlang_asm`／`xlang-c`（4007224，18:39），L2 **5/5**（option=102）。不跑 mega `-E`。
- **残**：rest 仍 host-cc。Windows 与 `XLANG_G05_PREFER_X_O=0` 仍整份 `cc`。不是 host-cc=0。`rt_parse_diag` 的 `.x` 也能纯 asm，产品切片仍整份 `cc`。`rt_stack.x` 纯 asm CG002。Ubuntu／Windows 本波只待快进，不重链。
- **禁**：glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 的 CG002；把 HARD BAN 面送去 tip 纯 asm；g05 或裸重链 Ubuntu tip。
- **钉盘**：ecdb5cc1e 不升。

## Class CK · w840 · rt_emit_state 切片退出整份 host-cc

- **刀**：POSIX 产品 `src/runtime/rt_emit_state.o` 改为纯 asm `src/runtime/rt_emit_state.x`，再 `cc` `seeds/rt_emit_state.from_x.c` 并带上已有的 `-DXLANG_RT_EMIT_STATE_FROM_X`。`driver_run_x_emit_c_set_path`、`set_lib`、`set_n_lib_roots`、`set_emit_extern`、`driver_argv_parse_x_emit_c` 不再进入这次 `cc`。BSS、`xlang_driver_x_emit_set_lib_name`、`xlang_driver_x_emit_lib_name_into`、`xlang_pipeline_pctx_set_entry_lib_prefix`、`labi_rt_emit_state_slice_marker` 留在 rest。权威在 `ensure_rt_emit_state_prefer`；g05、`build_xlang_asm`、strict glue 都走它。
- **证**：无该宏时预处理与迁前逐字节相同（62257）。加上宏 **62257→60416**。当前 tip 纯 asm 该 `.x` rc=0。`ld -r` 后五个 setter 与 rest 的 T 都在，另有 `.x` 的槽辅助函数。Darwin 重链 `xlang`／`xlang_asm`／`xlang-c`（4007224，18:23），L2 **5/5**（option=102）。不跑 mega `-E`。
- **残**：rest 仍 host-cc。Windows 与 `XLANG_G05_PREFER_X_O=0` 仍整份 `cc`。不是 host-cc=0。同族 `rt_arena_buf`／`rt_parse_diag` 的 `.x` 也能纯 asm，产品切片仍整份 `cc`。`rt_stack.x` 纯 asm CG002。
- **禁**：glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；用 gcc `-E` 修 `rt_stack` 的 CG002；把 HARD BAN 面送去 tip 纯 asm。
- **钉盘**：ecdb5cc1e 不升。

## Class CJ · w839 · runtime_driver_diagnostic thin 退出整份 host-cc

- **刀**：`runtime_driver_diagnostic_thin.x` 不再放文件级 `let` 赋值。当前 tip 对 `let g: i32 = 0; g = k` 纯 asm 直接 CG002（读和取址可以）。asm BSS 八函数（`last_expr_kind_set`、`set_last_expr_kind`、`current_func_store`、`maybe_trace`、`set_current_func`、`print_current_func`、`var_not_found`、`fail_at`）连同它们的 static 缓冲留在 `seeds/runtime_driver_diagnostic.from_x.c`，FROM_X 也编译这块。其余 thin 由既有 `try-r3-prefer` 纯 asm。
- **证**：无该宏时预处理与迁前逐字节相同（126680）。加上宏后 rest **90641→94512**（这八个函数回到 rest）。thin 纯 asm 88 个 T，无 `xlang_panic_`。rest `cc -c` 成功，与 thin 符号不重叠。`ld -r` 后迁前 96 个全局 T 都在，另有 3 个 `*_impl`。`try-r3-prefer` 日志写明 thin+rest。Darwin 只重链 `xlang`（不跑 mega `-E`），L2 **5/5**（option=102）。
- **残**：这八个函数仍 host-cc。Windows 与冷种子仍可整份 `cc`。不是 host-cc=0。pabi 活 rest 的其余面仍 host-cc。`runtime_driver_abi_thin.x` 同样 CG002，`runtime_io_abi.x` 纯 asm 中止，本波不动。
- **禁**：glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；再搬已经 `#ifndef` 的冷孪生；把 HARD BAN 面再送去 tip 纯 asm；用 gcc `-E` 修文件级 `let` 赋值。
- **钉盘**：ecdb5cc1e 不升。

## Class CI · w838 · backend_enc_dispatch thin 退出整份 host-cc

- **刀**：`backend_enc_dispatch_thin.x` 里 4 处 extern 调用补上 `unsafe`（`glue_binop_var_slot_cache_invalidate_*` 与两处 AAPCS64 `arch_arm64_enc_enc_u32_le`）。同一处在 `backend_enc_dispatch.x` 对齐。种子在 `-DXLANG_L2_ENC_DISPATCH_THIN_FROM_X` 下补 `backend_enc_append_u32_le_c`／`backend_enc_append_u8_c` 的原型，并把 `backend_enc_store_arg_sp_offset_arch` 留在 rest（thin 没有这个分派）。既有 `try-r3-prefer` 因此能纯 asm thin，再 `cc` 种子尾。
- **证**：无该宏时预处理与迁前逐字节相同（109868）。加上宏后 **109868→72615**。thin 纯 asm 135 个 T，无 `xlang_panic_`。rest `cc -c` 成功，`backend_enc_addsd_rax_rbx_arch` 仍是 T。`ld -r` 后迁前 167 个全局符号都在，另有 6 个 `*_impl`（thin 的 forward 要它们）。Darwin 只重链 `xlang`（不跑 mega `-E`），L2 **5/5**（option=102）。
- **残**：f64／Cap 尾仍 host-cc。Windows 与冷种子仍可整份 `cc`。不是 host-cc=0。pabi 活 rest 的其余面仍 host-cc。
- **禁**：glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；再搬已经 `#ifndef` 的冷孪生；把 HARD BAN 面再送去 tip 纯 asm；用 gcc `-E` 修 const-lit。
- **钉盘**：ecdb5cc1e 不升。

## Class CH · w837 · x_frontend_link_alias 退出 POSIX 产品 host-cc

- **刀**：`x_frontend_link_alias.x` 的 18 个 `function` 在 `seeds/x_frontend_link_alias.from_x.c` 里包一层 `#if !defined(XLANG_XFLA_ASM)`。POSIX 产品 `g05_ensure` 用 `pure_asm_x_to_o` 编这份 `.x`，再 `cc` 种子尾（lexer 结构返回 + 带修饰 ABI 别名）。`check_block_impl`、`check_expr_impl`、`find_or_alloc_ptr_type_ref`、`pipeline_typeck_set_active_ctx_c`、`pipeline_typeck_ptr_for_addr_of_operand_c` 经 `G05_X_O_WEAK_FUNCS` 保持 weak。Windows 与纯 asm 失败时仍整份 `cc` 种子，不定义该宏。
- **证**：无该宏时预处理与迁前逐字节相同（13455）。加上宏后 **13455→9917**，这 18 个定义不再出现，lexer／mangled 尾还在。`cc -c` 尾成功。纯 asm 与尾 `libtool -static` 后定义符号 35 个，强弱与迁前对象一致。Darwin 只重链 `xlang`（不跑 mega `-E`），L2 **5/5**（option=102）。
- **残**：Windows 与冷种子仍 host-cc 这 18 个 C 体。种子尾仍 host-cc。不是 host-cc=0。pabi 活 rest 的其余面仍 host-cc。
- **禁**：glue_tail；剥 thin_glue；升钉；mega FORCE 当产品默认；再搬已经 `#ifndef` 的冷孪生；把 HARD BAN 面再送去 tip 纯 asm。
- **钉盘**：ecdb5cc1e 不升。

## Class CG · w836 · elf_codegen_forwarders 退出 POSIX 产品 host-cc

- **刀**：`runtime_pipeline_abi_elf_codegen_forwarders_thin.x` 的 19 个 `export function` 在 `runtime_pipeline_abi.from_x.c` 里包一层 `#if !defined(XLANG_PABI_ELF_CODEGEN_FORWARDERS_ASM)`。POSIX 产品 rest 的 `cc` 加上该宏。`pipeline_abi_inject_elf_codegen_forwarders_thin` 在哨兵 `pipeline_sizeof_elf_ctx` 还不是 strong text 时仍纯 asm 注入。`pipeline_sizeof_elf_ctx` 改为返回整数字面量 53477424（与种子宏相同）。原文件级 `const` 在本 TU 里会被 tip 的 `asm_module_top_level_const_lit_i32` 读坏指针，纯 asm 直接中止。
- **证**：无该宏时产品 FROM_X 预处理与 w835 基线逐字节相同（480542）。冷种子预处理与 HEAD 逐字节相同。加上宏后产品预处理 **480542→478004**，这 19 个定义不再出现。`cc -c` 该 rest 成功。与纯 asm thin `ld -r` 后 19 个符号都是 T。thin 里 `pipeline_sizeof_elf_ctx` 的返回立即数是 `0x3300030`（53477424）。Darwin 把 thin 盖上现有 `runtime_pipeline_abi.o` 后只重链 `xlang`（不跑 mega `-E`），L2 **5/5**（option=102）。Ubuntu tip 能纯 asm 出这 19 个 T；tip 与 pabi 未替换。
- **残**：Windows 与冷种子仍 host-cc 这些 C 体。活 rest 的其它面仍 host-cc。不是 host-cc=0。tip 对「文件级 const + unsafe 同 TU」的故障还在。
- **禁**：glue_tail；升钉；mega FORCE 当产品默认；再搬已经 `#ifndef` 的冷孪生；用 gcc `-E` 修 const-lit 故障；把 HARD BAN 面再送去 tip 纯 asm。
- **钉盘**：ecdb5cc1e 不升。

## Class CF · w835 · typeck_orch 退出 POSIX 产品 host-cc

- **刀**：`runtime_pipeline_abi_typeck_orch_thin.x` 的 7 个 `export function` 在 `runtime_pipeline_abi.from_x.c` 里包一层 `#if !defined(XLANG_PABI_TYPECK_ORCH_ASM)`。POSIX 产品 rest 的 `cc` 加上该宏。`pipeline_abi_inject_typeck_orch_thin` 在哨兵 `typeck_x_type_size_from_layout_glue` 还不是 strong text 时仍纯 asm 注入。
- **证**：无该宏时产品 FROM_X 预处理与 w834 基线逐字节相同（482077）。冷种子预处理与 HEAD 逐字节相同。加上宏后产品预处理 **482077→480542**，这 7 个定义不再出现。`cc -c` 该 rest 成功。与纯 asm thin `ld -r` 后 7 个符号都是 T。Darwin 把 thin 盖上现有 `runtime_pipeline_abi.o` 后只重链 `xlang`（不跑 mega `-E`），L2 **5/5**（option=102）。Ubuntu tip 能纯 asm 出这 7 个 T；tip 与 pabi 未替换。
- **残**：Windows 与冷种子仍 host-cc 这些 C 体。活 rest 的其它面仍 host-cc。不是 host-cc=0。`elf_codegen_forwarders` 更大，但当前 tip 纯 asm 该 thin 直接中止，没有改它。
- **禁**：glue_tail；升钉；mega FORCE 当产品默认；再搬已经 `#ifndef` 的冷孪生；用 gcc `-E` 修 elf forwarders 的中止。
- **钉盘**：ecdb5cc1e 不升。

## Class CE · w834 · ast_forwarders 退出 POSIX 产品 host-cc

- **刀**：`runtime_pipeline_abi_ast_forwarders_thin.x` 的 186 个 `export function` 在 `runtime_pipeline_abi.from_x.c` 里各包一层 `#if !defined(XLANG_PABI_AST_FORWARDERS_ASM)`。POSIX 产品 rest 的 `cc` 加上该宏。`pipeline_abi_inject_ast_forwarders_thin` 在哨兵 `pipeline_copy_lib_root_to_buf256` 还不是 strong text 时仍纯 asm 注入（stamp 不再把新鲜 hybrid 留成未定义）。
- **证**：无该宏时产品 FROM_X 与冷种子 `cc -E -P` 与迁前逐字节相同。加上宏后产品预处理 **513089→482077**，这 186 个定义不再出现。`cc -c` 该 rest 成功（263184→235520）。与纯 asm thin `ld -r` 后 186 个符号都是 T、没有 U。Darwin 把 thin 盖上现有 `runtime_pipeline_abi.o` 后只重链 `xlang`（不跑 mega `-E`），L2 **5/5**。
- **残**：Windows 与冷种子仍 host-cc 这些 C 体。活 rest 的其它面仍 host-cc。不是 host-cc=0。Ubuntu 上这份 thin 能纯 asm 出 186 个 T，但按现有 `.o` 重链 `xlang` 会缺 `g_pipeline_elf_data_*`／reloc sidecar（原 pabi 里也没有这些符号）。tip `xlang_asm` 与 pabi 已还原，没有换成这波对象。
- **禁**：glue_tail；升钉；mega FORCE 当产品默认；再搬已经 `#ifndef` 的冷孪生。
- **钉盘**：ecdb5cc1e 不升。

## Class CD · w833 · pipeline_abi 纯冷孪生出 seeds

- **刀**：`compiler/seeds/runtime_pipeline_abi.from_x.c` 里最外层、不带 `WIN_LEFTOVER`、且没有 `#else` 的 `#ifndef XLANG_RUNTIME_PIPELINE_ABI_FROM_X` 共 60 块，正文挪到 `analysis/archive/runtime_pipeline_abi/cold_L*.inc`，原处只留 `#include`。带引号的 `#include "runtime_pipeline_abi_asm_wpo.from_x.c"` 那一块留在 seeds（相对路径不能跟着搬）。小于 180 字节的空壳不搬。
- **证**：同一组 `-I` 下 `cc -E -P` 三份与迁前逐字节相同——产品 `-DXLANG_RUNTIME_PIPELINE_ABI_FROM_X`、冷种子（无该宏）、Win `-DFROM_X -DWIN_LEFTOVER_GROW_VEC`。产品 rest 不打开这些 inc。
- **残**：seeds 文件仍 **1512616**。活 rest 仍 host-cc。不是 host-cc=0。本波不重链 tip。
- **禁**：glue_tail；升钉；mega FORCE 当产品默认；把预处理相同说成已经停 cc。
- **钉盘**：ecdb5cc1e 不升。

## Class CC · w832 · Linux tip slot_bytes 源码固化

- **刀**：w831 的 ELF 补丁收进仓库。`compiler/scripts/overlay_tip_slot_bytes_gcc.sh` 用现存 `./xlang -E` 编 `runtime_pipeline_abi_slot_bytes_thin.x` 与 `runtime_pipeline_abi_fnptr_array_esz_thin.x`，`cc -c -fno-jump-tables`，再把 `pipe_local_slot_bytes_mod`／`glue_fixed_array_temp_bytes` 跳进 tip 注入页。g05 在 Linux 上对坏帧自动调用；好帧（endbr64）与已有跳板默认跳过。`--force` 只替换已有跳板，不改好帧。
- **证**：未打补丁 tip（`sub $0x1038`）经该路径后 Ubuntu L2 **5/5**（option=102）。Darwin／Win 脚本 no-op（这两端 tip 帧本就分开）。
- **残**：tip asm 仍会发出坏帧；Linux tip 这两枚仍 host-cc。不是 host-cc=0。
- **禁**：glue_tail；升钉；把 mega FORCE 当产品默认。
- **钉盘**：ecdb5cc1e 不升。

## Class CB-fix · w831 · Ubuntu tip cold slot_bytes（栈槽）· tip `ceaba871f`

- **刀**：禁 peel／债切。手术植入 warm leftover `pipe_local_slot_bytes_mod`＋`glue_fixed_array_temp_bytes` 入 Ubuntu tip 产品（既有 tip4of5＋array／return／as／lea 注入之上）。根因 tip peel 坏帧 → `u8[4]` 与 `Option_ptr_u8` 槽重叠（option 240）。
- **禁**：g05 整包重链 tip4of5；for_call_args PTR 改 9；warm／LEGACY 充数；升钉；可连砍。
- **Darwin tip**：cold L2 **5/5**。
- **Ubuntu tip cold**：注入产品 L2 **5/5**（option=102）；bak `/tmp/xlang_asm.cb_fix_slotbytes.bak`。
- **Win tip cold 新链**：`XLANG_LEGACY_C_FRONTEND=0` g05 ＋ overlay BSS → L2 **5/5**（exe 4385778；非 LEGACY 充数）。
- **钉盘**：ecdb5cc1e 不升。

## Class CB · w830 · type_ref tip 出链（P3 peel）· tip `e50c2004a`

- **刀**：tip thin rest 加 `XLANG_PTHIN_TYPE_REF_FROM_X`；出链 `parser_asm_type_ref_slice.inc`（~69KB）；g05 Class CB＝CA peel ＋ `pthin_type_ref.x`／seed（BODIES＋POSTFIX／PREFIX／FN／STAR／LINEAR／VEC／SCALAR／NAMED／GENERIC／IMPL／POINTEE 等门控）。
- **禁**：glue_tail；primary（级联未解，可下刀再试）；整吞 pabi；Cap／unwind 主刀。
- **Darwin tip**：CB peel cold 新链 L2 **5/5**；thin_glue ≈378312（CA≈374728；P3 .x 入链）。
- **Ubuntu**：P3b `pthin_type_ref.x` thin 失败 → **未走上 CB peel**（回落 thin seed）；终链 `host_is_*_impl` 缺；**warm 5/5**；**Win LEGACY 5/5**。
- **钉盘**：ecdb5cc1e 不升。

## Class CA · w829 · FN_BLOCK tip 出链（P6 peel）· tip `5d8d450a1`

- **刀**：tip thin rest 加 `XLANG_PTHIN_FN_BLOCK_FROM_X`；出链 `one_function_buf`（~74KB）＋`struct_layout`＋`library`＋`block_from_res`；g05 Class CA＝BX skip_tl peel ＋ `pthin_fn_block.x`／seed ＋ `pipeline_expr_set_binop_operands` stub。
- **禁**：glue_tail；primary（级联未解）；整吞 pabi；Cap／unwind 主刀。
- **Darwin tip**：CA peel cold 新链 L2 **5/5**；thin_glue ≈374728。
- **Ubuntu**：tip SEGV／**warm 5/5**；**Win LEGACY 5/5**。
- **钉盘**：ecdb5cc1e 不升。

## Class BZ · w828 · 死文件清仓（range_for＋skip_tl／seed_parse seeds 残留）· tip `79c124a37`

- **刀**：用户拍板删已迁残留。证明无引用后删：
  1) orphan `parser_asm_range_for_slice.inc` **12325**（无 #include）
  2) `parser_asm_skip_tl_slice.inc` **516194**：`pthin_skip_tl`／thin_c cold／g05 ntime → `analysis/archive/…`；seeds 侧删除
  3) BY seed_parse seeds wrapper **430**：引用直指 archive 后删 wrapper
- **保留**：`analysis/archive/parser_asm/**`；stretch `product_keep`＋`archive_parser_asm_emit_heavy_stretch_suite_slice.inc` 审计 wrapper
- **Darwin tip**：BX peel cold 新链 L2 **5/5**
- **Ubuntu**：tip SEGV／**warm 5/5**；**Win LEGACY 5/5**
- **钉盘**：ecdb5cc1e 不升

## Class BY · w827 · seed_parse tip 债归档（已空桩出链）· tip `71772596e`

- **刀**：`parser_asm_seed_parse_into_buf_slice.inc`（~217KB）迁出 `seeds/parser_asm/` → `analysis/archive/parser_asm/`；seeds 留 BW 同款 wrapper。tip G05 本已 `-DPARSER_ASM_THIN_GLUE_NO_SEED_PARSE`（不进产品 thin）。P8 smoke／experimental bootstrap 仍可经 wrapper 编 archive。
- **试刀回退**：primary_slice（~134KB）peel 需 ctrl.x／type_ref.x／set_unary 级联 → tip 终链 undef；已回退 BX peel。禁 glue_tail。
- **Darwin tip**：BX peel cold 新链 L2 **5/5**（product_l2_matrix）；thin_glue ≈372560（BY 仅归档，体积不变量级）。
- **Ubuntu**：tip SEGV／**warm 5/5**；**Win LEGACY 5/5**。
- **钉盘**：ecdb5cc1e 不升。

## Class BX · w826 · skip_tl tip 出链（P12 peel）· tip `08e3af401`

- **刀**：tip `parser_asm_thin_c` 不再 `#include parser_asm_skip_tl_slice.inc`（`XLANG_PTHIN_SKIP_TL_FROM_X`＋`LEX_SKIP_FROM_X`）；skip 走 `pthin_skip_tl.x`／seed＋lex_skip＋lex_step bridge（g05 Class BX peel）。BODIES cold twin 归档 `analysis/archive/parser_asm/parser_asm_skip_tl_bodies_cold.inc`；全量预 BX blob 亦归档。
- **禁**：glue_tail；未整吞 pabi；未 Cap／unwind 充主进度。全量 P1–P20 omit-rest hybrid tip SEGV → 默认关闭（`XLANG_G05_PTHIN_FULL=1` 才开）。
- **Darwin tip**：cold／BX peel 新链 L2 **5/5**；thin_glue ≈372560。
- **Ubuntu／Win**：见交刀短报。
- **钉盘**：ecdb5cc1e 不升。

## Class BW · w825 · host-cc=0 真迁（stretch suite .inc 出链＋call_dispatch from_x 债归档）· tip `71499488e`

- **刀**：战略改派后首刀真迁／删债（非 Cap／unwind 主进度）。
  1) `parser_asm_emit_heavy_stretch_suite_slice.inc`（1.70MB）迁出 `seeds/parser_asm/` → `analysis/archive/parser_asm/`；产品仍走 Class BA `product_keep`；AUDIT 经 321B wrapper。
  2) `seeds/backend_call_dispatch.from_x.c` tip 债 **299002→1129**（−297873）：cold twin 归档 `analysis/archive/backend_call_dispatch/backend_call_dispatch_cold_twin.inc`；产品 FROM_X 仅 slice_marker＋BM 表。
- **副作用／闸修复**：BU `__compact_unwind` 批剥损坏 LOH 对象（parser_x／typeck／codegen 等）→ tip SEGV；本刀 `bq_strip_compact_unwind_safe.sh` 暂 no-op；`parser_asm_thin_glue` 用 cold monothin（261848；prefer hybrid 与 pin parser 组合曾 SEGV）。
- **Darwin tip**：cold 新链 L2 **5/5**（product_l2_matrix）。
- **Ubuntu／Win**：见下（重编分 tip／warm／LEGACY）。
- **钉盘**：ecdb5cc1e 不升。
- **禁**：FORCE leftover／-E 当修／HARD BAN／glue_tail／CloudAgent／升钉。

## 0. 一句话

**尚未完全自举** · 钉盘 **`ecdb5cc1e`** · **三端 L2 硬闸已启用** · **Class CU** `cfg_host_os_lit`／`cfg_host_arch_lit` 只在 `.x`。`cfg_eval` 后段阶梯仍 host-cc。marker 仍 host-cc。lexer 尾仍 host-cc。pabi 活 rest 仍 host-cc。

| 指标 | 状态 |
|------|------|
| 钉盘 L4 | ✅ 双端＠**`ecdb5cc1e`**（不升） |
| tip L2 | ✅ Darwin 本波重链 **5/5**（3929656，21:28，与重链前逐字节相同）。Ubuntu 15:46 tip **5/5**、Win `xlang_asm.exe` 15:27 **5/5**，两端未重链 |
| BC inventory | present **0**（真债＝map 外 from_x／`.inc`／大户 leftover） |
| 本波级别 | **M2 Class CU** cfg_eval_host_lit C 种子整文件删除 |
| 下一波 | 再找一份业务已在 `.x`、产品对象是独立 `.o`、当前编译器 `__text` 与链上对象一致的 from_x 再删 C（对不上不重链；不重开 `async_asm_pool`）；不要优先 `.x` 整包重编 `runtime_driver_no_c.o`；`rt_stack` CG002 不拿 gcc `-E` 修；pabi 活 rest 的 thin 面仍是 HARD BAN；Ubuntu 在 BSS 符号能链接前不重链；L4／升钉仍经理另派 |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举效率方法-M2主链.md](自举效率方法-M2主链.md) | M2 主链 |
| [C迁移追踪.md](C迁移追踪.md) | 终局债（短状态表） |
| **本文** | KPI · 前排 · §6（**9 月 23 日**） |
| [自举进度-归档-2026-09-22.md](自举进度-归档-2026-09-22.md) | **9 月 22 日**全日弧（w749–w778／Class F–AC） |
| [自举进度-归档-2026-09-21.md](自举进度-归档-2026-09-21.md) | **9 月 21 日**（w692–w748） |

***

## 2. 仪表盘

| 项 | 结果 |
|----|------|
| 产品 bstrict | **131** 套（双端 tip 全绿＠`eab2c15b1`） |
| 钉盘 | **`ecdb5cc1e`**（不升） |
| MG／Stage8 | MG ✅ · Stage8 **30/30** |
| SSH | `ubuntu-server`／`windows-server` |
| tip | **三端 L2 硬闸已启用** · Darwin 本波重链 **5/5**（21:28，与重链前逐字节相同） · Ubuntu 15:46 tip **5/5**／Win `xlang_asm.exe` 15:27 **5/5**，两端未重链 |
| host-cc 真债 | BC present **0**；余＝map 外 seed／from_x／产品 `.inc`／大户 leftover 真减（AV seed Cap 已剥；Darwin tip 可 prefer labi） |

***

## 3. 当前前排（≤3）

1. **主迁刀** — **M2 大户真减 host-cc**：w851 删除 `cfg_eval_host_lit` 整份 C 种子。权威在 `.x`。`cfg_eval` 后段阶梯仍 host-cc。marker 仍 host-cc。lexer 尾仍 host-cc。不重编 `runtime_driver_no_c.o`。不重开 `async_asm_pool`。`rt_stack.x` CG002，不用 gcc `-E` 修。pabi 活 rest 仍 host-cc。Ubuntu／Windows 本波产品不重链。
2. **次排** — Ubuntu tip leftover；L6／L8 tip asm 码生成（指针下标）治本后可再 prefer。禁 mega FORCE。
3. **立卡不动** — arr_return Soft-Cap；MSVC va；eq 深链；spawn 真体；nest 64；L4／升钉仅经理另派。

***

## 4. 方法论要点

- 权威残量口径：BC inventory＝结构孤儿（present **0**）；真残＝map 外 seed／from_x／`.inc`／Cap always-mega／Darwin leftover 大户。
- 禁：CloudAgent／升钉／默 bstrict／glue_tail 主刀／mega FORCE／Soft-Cap tip `-E`／HARD BAN 分类主刀／FORCE 冲 Darwin leftover hybrid。
- Win：`XLANG_LEGACY_C_FRONTEND=1` 默认勿破。
- Ubuntu：tip 全量重链 SEGV 时用 **warm** 金标；硬闸仍启用。

***

## 5. 波次流水（倒序 · 9 月 23 日起）

| 日期 | 波 | 一行 |
|------|-----|------|
| 2026-09-23 | **wave851** | 整文件删除 `cfg_eval_host_lit` C 种子；`cfg_host_os_lit`／`cfg_host_arch_lit` 只在 `.x`；当前汇编与链上对象逐字节相同（501）；安装器只做纯 asm，无 `-E`、无 host-cc 回退；不重编 `cfg_eval.o`；Darwin 重链 L2 **5/5**（3929656，21:28，与重链前逐字节相同）；Ubuntu 15:46／Win 15:27 既有二进制 L2 **5/5**，不重链；钉盘不升 |
| 2026-09-23 | **wave849** | 删除 `backend_arch_emit_dispatch` 47 个分派壳的 C 体和 thin 种子；权威只在 `.x`；种子留 marker（返回 0，701）；`__text` 与删前逐字节相同；安装器只做纯 asm 加 marker，无 `-E`、无冷种子；Darwin 重链 L2 **5/5**（3929656，20:47，与 20:26 逐字节相同）；Ubuntu 15:46／Win 15:27 既有二进制 L2 **5/5**，不重链；钉盘不升 |
| 2026-09-23 | **wave848** | 整文件删除 `lsp_diag_pipeline_sizes` 产品种子；三枚 sizeof 只在 `.x`；产品对象 580（与删前逐字节相同，三个 T）；安装器只做纯 asm，无 `-E`、无冷种子；Darwin 重链 L2 **5/5**（3929656，20:26，与重链前逐字节相同）；Ubuntu 15:46／Win 15:27 既有二进制 L2 **5/5**，不重链；钉盘不升 |
| 2026-09-23 | **wave847** | 删除 `x_frontend_link_alias` 18 个别名的 C 体和 `XLANG_XFLA_ASM` 门；权威只在 `.x`；种子留 lexer 尾和带修饰别名（6137）；产品对象 Mach-O 7880（符号名 68 与删前一致，五个仍 weak）；Darwin 重链 L2 **5/5**（3929656，20:10，与重链前逐字节相同）；Ubuntu 15:46／Win 15:27 既有二进制 L2 **5/5**，不重链；钉盘不升 |
| 2026-09-23 | **wave846** | 删除 `rt_parse_diag` 精确诊断的 C 体和 `PRECISE_BRIDGE`；权威只在 `.x`；种子留恢复诊断和 marker（17226）；产品对象 16792（与删前逐字节相同）；Darwin 重链 L2 **5/5**（3929656，19:56）；Ubuntu 15:46／Win 15:27 既有二进制 L2 **5/5**，不重链；钉盘不升 |
| 2026-09-23 | **wave845** | 删除 `rt_emit_state` 五个 setter 的 C 体；权威只在 `.x`；种子留 BSS／lib-name／入口前缀和 marker（4277）；产品对象 7712（与删前逐字节相同）；Darwin 重链 L2 **5/5**（3929656，19:45）；Ubuntu 15:46／Win 15:27 既有二进制 L2 **5/5**，不重链；钉盘不升 |
| 2026-09-23 | **wave844** | 删除 `rt_arena_buf` 两个函数的 C 体；权威只在 `.x`；种子留 128MiB／2MiB BSS 和 marker（1591）；产品对象 1688（与删前逐字节相同）；Darwin 重链 L2 **5/5**（3929656，19:34）；Ubuntu 15:46／Win 15:27 既有二进制 L2 **5/5**，不重链；钉盘不升 |
| 2026-09-23 | **wave843** | 删除 `rt_preamble` 两个写入函数的 C 体；权威只在 `.x`；种子留字符串表和 marker；产品对象 52408；Darwin 重链 L2 **5/5**（4007144，19:03）；Ubuntu／Win 产品不重链；钉盘不升 |
| 2026-09-23 | **wave842** | POSIX 产品 `rt_parse_diag` 切片改走纯 asm（预处理 29625→29041）；精确诊断离开 `cc`；恢复诊断与 marker 留 rest；不定义 `PRECISE_BRIDGE`；种子未改；Darwin 重链 L2 **5/5**（4007224，18:49）；Ubuntu／Win 不重链；钉盘不升 |
| 2026-09-23 | **wave841** | POSIX 产品 `rt_arena_buf` 切片改走纯 asm（预处理 33303→32850）；两个函数离开 `cc`；128MiB／2MiB BSS 与 marker 留 rest；种子未改；Darwin 重链 L2 **5/5**（4007224，18:39）；Ubuntu／Win 不重链；钉盘不升 |
| 2026-09-23 | **wave840** | POSIX 产品 `rt_emit_state` 切片改走纯 asm（预处理 62257→60416）；五个 setter 离开 `cc`；BSS／lib-name／前缀留 rest；无宏预处理与迁前相同；Darwin 重链 L2 **5/5**；钉盘不升 |
| 2026-09-23 | **wave839** | POSIX 产品 `runtime_driver_diagnostic` 改走 thin 纯 asm；asm BSS 八函数留在 rest（宏下 90641→94512）；无宏预处理与迁前相同（126680）；Darwin 重链 L2 **5/5**；钉盘不升 |
| 2026-09-23 | **wave838** | POSIX 产品 `backend_enc_dispatch` 改走 thin 纯 asm（预处理 109868→72615）；f64 尾仍 host-cc；无宏预处理与迁前相同；Darwin 重链 L2 **5/5**；钉盘不升 |
| 2026-09-23 | **wave837** | POSIX 产品停 host-cc x_frontend_link_alias 18 个别名（预处理 13455→9917）；5 个保持 weak；lexer 尾仍 host-cc；无宏预处理与迁前相同；Darwin 重链 L2 **5/5**；钉盘不升 |
| 2026-09-23 | **wave836** | POSIX 产品 rest 停 host-cc elf_codegen_forwarders 19 面（预处理 480542→478004）；sizeof 返回字面量 53477424；无宏产品／冷种子预处理与迁前相同；Darwin 重链 L2 **5/5**；钉盘不升 |
| 2026-09-23 | **wave835** | POSIX 产品 rest 停 host-cc typeck_orch 7 面（预处理 482077→480542）；无宏产品／冷种子预处理与迁前相同；Darwin 重链 L2 **5/5**；钉盘不升 |
| 2026-09-23 | **wave834** | POSIX 产品 rest 停 host-cc ast_forwarders 186 面（预处理 513089→482077）；冷种子／无宏产品预处理不变；Darwin 重链 L2 **5/5**；钉盘不升 |
| 2026-09-23 | **wave833** | pipeline_abi 纯 `#ifndef FROM_X` 冷孪生 60 块出 seeds（2784374→1512616）；产品／冷种子／Win FROM_X 预处理与迁前相同；不重链；钉盘不升 |
| 2026-09-23 | **wave832** | Linux tip slot_bytes 源码固化：`.x` thin → host cc → tip ELF 跳板；未打补丁 tip 上 Ubuntu L2 **5/5**（option=102）；Darwin／Win 脚本 no-op；钉盘不升 |
| 2026-09-23 | **wave831** | CB-fix：Ubuntu tip cold **slot_bytes** leftover 植入；**三端 tip cold L2 5/5**（Darwin／Ubuntu option=102／Win 新链）；钉盘不升 |
| 2026-09-23 | **wave830** | M2 Class CB type_ref tip 出链（~69KB .inc）；Darwin tip **CB peel cold** L2 **5/5**；Ubuntu tip 链失败／**warm 5/5**；Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave829** | M2 Class CA FN_BLOCK tip 出链（one_function／layout／library／block_from_res）；Darwin tip **CA peel cold** L2 **5/5**；Ubuntu／Win 见短报；钉盘不升 |
| 2026-09-23 | **wave828** | M2 Class BZ 死文件清仓：range_for／skip_tl seeds／seed_parse wrapper 删；Darwin tip **BX peel cold** L2 **5/5**；Ubuntu／Win 见短报；钉盘不升 |
| 2026-09-23 | **wave827** | M2 Class BY seed_parse .inc 出链归档（216913→archive；seeds wrapper 430B）；Darwin tip **BX peel cold** L2 **5/5**；Ubuntu／Win 见短报；钉盘不升 |
| 2026-09-23 | **wave824** | M2 Class BV token／typekind 表化：pabi **−13288**；Darwin tip **cold** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave823** | M2 Class BU tip compact_unwind G05 全扫：合计 **−80520**；Darwin tip **cold** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave822** | M2 Class BT tip compact_unwind 扩剥：合计 **−25280**；Darwin tip **cold** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave821** | M2 Class BS unwind n_sect 修＋call／enc／diag 剥：合计 **−16440**；Darwin tip **cold** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave820** | M2 Class BR Cap-skip＋ASM_DEBUG Cap 剥：pabi **−7272**；call_dispatch **−6088**；Darwin tip **cold** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave819** | M2 Class BQ tip compact_unwind 安全剥：slc／diagnostic／try_inline **−20360**；Darwin tip **cold** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave818** | M2 Class BP leftover Cap-heavy skip／safe：pabi **1275064→1253384**；Darwin tip **cold** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave817** | M2 Class BO leftover Cap-heavy safe：pabi **1294664→1275064**；Darwin tip **cold** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave816** | M2 Class BN leftover Cap WPO reach／DCE：pabi **1310504→1294672**；Darwin tip **cold** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave815** | M2 Class BM call_dispatch heap redirect 表化：call_dispatch **140568→133624**；Darwin tip **cold** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave814** | M2 Class BL try_inline WPO_MONO Cap 剥：try_inline **79024→39512**；Darwin tip **prefer labi** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave813** | M2 Class BK call_dispatch Cap 剥：call_dispatch **167808→140568**；Darwin tip **prefer labi** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave812** | M2 Class BJ labi 26× od／fs needle：labi **406232→396392**；Darwin tip **prefer labi** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave811** | M2 Class BI leftover Cap PGO-Lite 余桩：pabi **1313088→1310504**；Darwin tip **prefer labi** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave810** | M2 Class BH leftover Cap PGO／WPO_MONO：pabi **1326360→1313088**；Darwin tip **prefer labi** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave809** | M2 Class BG labi icc needle：labi **410072→406232**；Darwin tip **prefer labi** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave808** | M2 Class BF labi 20× sym_at：labi **428888→410072**；Darwin tip **prefer labi** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave807** | M2 Class BE labi needle 表：labi **455872→428888**；Darwin tip **prefer labi** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave806** | M2 Class BD w502＋unwind：pabi **1338744→1325868**；labi **468712→455872**；driver_abi **85064→72704**；Darwin tip **prefer labi** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave805** | M2 Class BC leftover Cap 压桩：pabi **1361832→1338744**；Darwin tip **prefer labi** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave804** | M2 Class BB Darwin leftover compact_unwind 剥：pabi **1478272→1361832**；Darwin tip **prefer labi** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave803** | M2 Class BA stretch suite 产品 keep：thin_glue **792168→343168**；Darwin tip **prefer labi** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave802** | M2 Class AZ stretch suite 审计空桩：thin_glue **1837656→792168**；Darwin tip **prefer labi** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave801** | M2 Class AY Cap 剥 EMIT_TRACE／TRACE_EXPR_SET／LIST_FUNCS：seed ≈−2.1KB；Darwin tip **prefer labi** L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；钉盘不升 |
| 2026-09-23 | **wave800** | M2 Class AX ASM_BODY／FUNC_TRACE Cap 空化：seed＋mega＋thin ≈−5.2KB；Darwin tip **prefer labi** L2 **5/5**；钉盘不升 |
| 2026-09-23 | **wave799** | M2 Class AW pipeline_abi seed ASM_DEBUG Cap 大剥：≈−5.8KB；Darwin tip **prefer labi** L2 **5/5**；钉盘不升 |
| 2026-09-23 | **wave798** | M2 Class AV pipeline／rt_run Cap 剥：≈−5.2KB；Darwin tip **prefer labi** L2 **5/5**；钉盘不升 |
| 2026-09-23 | **wave797** | M2 Class AU labi prefer 根修：Darwin tip **prefer labi** L2 **5/5**；钉盘不升 |
| 2026-09-23 | **wave796** | M2 Class AT driver_abi Cap 剥：prefer **85712→85064**；Darwin tip 新链 L2 **5/5**（cold labi）；钉盘不升 |
| 2026-09-23 | **wave795** | M2 Class AS driver_abi Cap 剥：prefer **87128→85712**；Darwin tip 新链 L2 **5/5**（cold labi）；钉盘不升 |
| 2026-09-23 | **wave794** | M2 Class AR labi Cap push 剥：cold **421936→421408**；Darwin tip 新链 L2 **5/5**；钉盘不升 |
| 2026-09-23 | **wave793** | M2 Class AQ labi Cap 减量半刀：hello-stage1 Cap 空化；cold **425680→421936**；Darwin tip 新链 L2 **5/5**；钉盘不升 |
| 2026-09-23 | **wave792** | M2 Class AP Cap 减量半刀：diagnostic parse_commit gather 空化；prefer **48712→47848**；Darwin tip 新链 L2 **5/5**；钉盘不升 |
| 2026-09-23 | **wave791** | M2 Class AP 拼装半刀：Darwin tip 新链 L2 **5/5**；Ubuntu warm／Win LEGACY **5/5**；禁 FORCE hybrid；钉盘不升 |
| 2026-09-23 | **wave790** | M2 Class AO diagnostic ASM_FUNC_TRACE／Cap parse-step 剥；产品 `runtime_driver_diagnostic.o` **41328→39584**；三端 L2 **5/5**＠`7ad413d7e`（Ubuntu warm） |
| 2026-09-23 | **wave789** | M2 Class AN diagnostic TYPECK Cap 五联剥；产品 **45976→41328**；三端 L2 **5/5**＠`7eca664da`（Ubuntu warm） |
| 2026-09-23 | **wave788** | M2 Class AM diagnostic TYPECK_BINOP／PARSE_PARAM Cap 剥；产品 **48912→45976**；三端 L2 **5/5**＠`ec2de4c00`（Ubuntu warm） |
| 2026-09-23 | **wave787** | M2 Class AL diagnostic DEBUG Cap 剥；产品 **54592→48912**；三端 L2 **5/5**＠`e8f9fafe4`（Ubuntu warm） |
| 2026-09-23 | **wave786** | M2 Class AK labi DEBUG_LD 剥；产品 `runtime_link_abi.o` **532576→529720**；三端 L2 **5/5**＠`317dfa22c`（Ubuntu warm） |
| 2026-09-23 | **wave785** | M2 Class AJ lsp Cap DEBUG 剥；产品 `lsp_diag.o` **117784→116768**；三端 L2 **5/5**＠`3276a87f5`（Ubuntu warm） |
| 2026-09-23 | **wave784** | M2 Class AI driver_abi Cap／DEBUG 注记剥；产品 **105416→104960**；冷 twin **91704→90904**；三端 L2 **5/5**＠`ee521cb50`（Ubuntu warm） |
| 2026-09-23 | **wave783** | M2 Class AH Cap debug 剥（parser＋link_abi）；冷 primary／glue＋产品 link_abi 减量；三端 L2 **5/5**＠`7f5516eef`（Ubuntu warm） |
| 2026-09-23 | **wave782** | M2 Class AG parse_expr_link Cap debug 剥；产品 **10440→1008**；三端 L2 **5/5**＠`36bf1982b`（Ubuntu warm） |
| 2026-09-23 | **wave781** | M2 Class AF skip_tl Cap DBG 剥；seed **36600→36040**；三端 L2 **5/5**＠`852c83dfb`（Ubuntu warm） |
| 2026-09-23 | **wave780** | M2 Class AE expr zeros 孪生退役＋primary Cap TRACE；primary **25136→23744**；三端 L2 **5/5**＠`a23bc172b`（Ubuntu warm） |
| 2026-09-23 | **wave779** | M2 Class AD P20b foundation zeros 纯 asm＋剥 Cap watch；seed **1536→1088**；三端 L2 **5/5**＠`15f26dec6`（Ubuntu warm） |
| 2026-09-22 | wave778–w749 等 | 见 [自举进度-归档-2026-09-22.md](自举进度-归档-2026-09-22.md) |

### Class CB-fix · Ubuntu tip cold 栈槽（2026-09-23）· tip `ceaba871f`／矩阵 `d8e1b548e`
- 钉盘：`ecdb5cc1e` 不升 · **禁可连砍**（Win tip cold 新链未收口前）
- Darwin tip cold：`product_l2_matrix` **5/5**（rv42／opt102／si／hello／f32）
- Ubuntu tip cold：**5/5**（同矩阵；**option=102**）。产品＝tip4of5＋既有 leftover 注入＋**warm `pipe_local_slot_bytes_mod`／`glue_fixed_array_temp_bytes` 植入**（`/tmp/xlang_asm.cb_fix_slotbytes.bak`）。不得 warm 充数。
- Win tip cold 新链：L2 **5/5**（`5fadfad21` overlay BSS；exe≈4.39MB）。
- 根因（option 240＝−16）：
  1. tip peel `pipe_local_slot_bytes_mod`（跨度 ~0x213c／`sub $0x1038`）与 `glue_fixed_array_temp_bytes`（`sub $0x8a8`）坏帧 → `let buf: u8[4]` 槽未涨
  2. `Option_ptr_u8` 写回 `mov %rdx,-0xe0` 覆盖 `buf@-0xe0`（warm：`buf@-0x108`／sp 分离）
  3. 先链已绿：return-elf／array lit／as／lea_not_load；Cap ARRAY_LIT `0x2e→0x30`；禁改 for_call_args PTR=`9`
- 修复：注入 warm leftover 两符号后帧 `0x958→0x968`；sp 写 `-0xf0/-0xe8`，不再盖 buf；option=102
- 下一步：w832 已把植入收成 `overlay_tip_slot_bytes_gcc.sh`（权威 `.x`，host cc）。下刀转大户 from_x，不把 tip asm 坏帧当本波已治本。

