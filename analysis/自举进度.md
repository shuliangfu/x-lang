# Xlang / X language 自举进度

> **更新**：2026-08-20 · 钉盘仍 **`f7424ae47`**（**不升钉**）· Linux hosted 产品 `-o` ld-only **双端 L2 闭** · F7 仍绿 · 19 日已归档\
> **产品 L4 放行钉盘**：**`f7424ae47`**（2026-08-15 升钉 · 前 `e364f4a37` 2026-08-11 · 再前 `d79a368b2` 2026-08-10）\
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅**（12 labi_*.x 切片 prefer；默认 FROM_X=1）· parser **7.2.2 关 pin ✅**（tip `-E` + assemble；产品默认 FROM_X=1）· codegen 7.4.2 wave323 ✅ · typeck 7.4.1 wave322 ✅ · runtime monofile wave321 ✅ · present **0** · 钉盘 **`f7424ae47`** · Stage 8 tip **wave332 Track L 30/30 FULLY CLOSED**（**PRODUCT HALF=0** · **PRODUCT PINNED=0** · **PRODUCT RETIRED 23/23 = 100%**）· **7.4.3 PREFER_X_O 双端 8/8 wave338 ✅** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁**\
> **tip L4 安全网（历史）**：**`ffc0b646f`**（已由 tip L4 升钉吸收；前网 `4bacaf18c`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：钉盘仍 **`f7424ae47`**。**Linux hosted 产品 `-o` ld-only 双端 L2 闭**：产生＝`xlang_asm_invoke_ld_platform` Linux hosted 用 gcc 当链接器（crt／pie；emit 已是 asm；invoke_cc 已 ALLOW 闸；Darwin ld-only 已闭）。sit-red PATH 假 gcc → `FAKE_CC_SPAWN`；裸 `ld -e main` 历史 SIGSEGV。存储＝ld argv。消费＝产品 `-o`。G.7 补既有 ld（glibc Scrt1／crti／crtn＋PIE／`-z`；禁 gcc 回退／`-e main`）。wrapper rdi＝data **未改**。证：Ubuntu 假 gcc **clang_hits=0** ld_hits＝探针数 · argv[0]＝`ld` · rv／hello／opt／si／f32／add／sas／apa **42／0／102／0／0／7／7／6** · 矩阵 **5/5** · mac Darwin 假 clang **clang_hits=0** 矩阵 **5/5**。\
> **下波候选**：sat 盖 prefer／pipeline_abi mega 仍硬禁／ALLOW ensure 再瘦（须点名）。**nest 冻 64**。

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1；过程上 prefer+heat 一次／域，双端矩阵同波一次\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；**SHARED 双端 L2**；日常矩阵 **只** `./xbuild l2-matrix`（wave299：捕获 rv=42／opt=102 成功码，禁 set -e 截断）；谈自举／升钉才 L4 + 矩阵 + 全量 bstrict；bstrict **per-script timeout=300s** + catalog cache（防无界卡死）；**墙钟**：`product_l4` 分步 wall + `run-all-bstrict` 每脚本 wall／top-N（`XLANG_BSTRICT_TIMING_FILE`）；并行 **`XLANG_BSTRICT_JOBS` 默认 1**（Ubuntu 可 opt-in 2–4；Darwin 慎用 OOM）

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[19](自举进度-归档-2026-08-19.md) · [18](自举进度-归档-2026-08-18.md) · [17](自举进度-归档-2026-08-17.md) · [16](自举进度-归档-2026-08-16.md) · [15](自举进度-归档-2026-08-15.md) · [14](自举进度-归档-2026-08-14.md) · [13](自举进度-归档-2026-08-13.md) · [12](自举进度-归档-2026-08-12.md) · [11](自举进度-归档-2026-08-11.md) · [09](自举进度-归档-2026-08-09.md) · [08](自举进度-归档-2026-08-08.md) · [07](自举进度-归档-2026-08-07.md) · [06](自举进度-归档-2026-08-06.md) · [05](自举进度-归档-2026-08-05.md) · [04](自举进度-归档-2026-08-04.md) · [03](自举进度-归档-2026-08-03.md) · [02](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · …\

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `f7424ae47`**（2026-08-15 升钉 · 前 `e364f4a37` 2026-08-11 · 再前 `d79a368b2` 2026-08-10）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**双端 Stage2 = ✅ SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709` · `xlang_asm_stage1`／`xlang_asm2` bit-identical · 黄金自举金标准 · strict-link 根修后双端首过）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（Makefile 已删除；**变更记录只在本文 §6**）。

| 指标 | 状态 |
|------|------|
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`f7424ae47`**（L4 升钉冻结 · 双端 L4 真冷绿 · 含过夜 Darwin 刀：heap／queue／vec／stdtest companions · div0 panic · ARM64 add_imm SUB · extra-.o has_obj）；residual tip **wave332** present **0**；Stage 8 tip **wave332**（**30/30 FULLY CLOSED**；**PRODUCT RETIRED 23/23=100%**；PRODUCT HALF=0；PRODUCT PINNED=0） |
| Track MG | **11.3.1 MG ✅**（wave941 物理删 Makefile · wave942 catalog CFLAGS · 0 make in bootstrap · 产品入口 `./xbuild`） |
| Track BC | **开 🟢（30/30 FULLY CLOSED wave332）**（B′ 至 **wave326** link_abi **7.3.1 关 pin** ✅ · M4 **5/5 全 ✅**（runtime/typeck/codegen/parser/link_abi 五域冷链全闭）· present **0**；**BC Stage8 Batch3 收**＝**30/30 FULLY CLOSED**（PRODUCT 23 RETIRED 100% catalog+ladder；NON-PRODUCT 7 正确分类 never-product-chain = TEST×2 / STAGE×2 / EXTRACT_ONLY×1 / DELETED_ORPHAN×2；G.7 无 HALF 无 unresolved PINNED）|
| Track L (Stage 8) | **收（Batch 3 30/30 FULLY CLOSED wave332）**（批次 0 清理 ✅ · 批次 2 **wave1036–1041** 退役 ✅ · 批次 3 **wave327–332**（lsp_diag+lsp+pipeline+driver+preprocess+lexer+parser/typeck/codegen/ast_gen2 10 项 + audit 分类误判 3 项 RETIRED 修正 + ast_gen.c 孤儿物理删 + ast_gen2 cold-seed pin + 分母口径统一 is_product_denominator）✅ · **30/30 FULLY CLOSED**；PRODUCT RETIRED 23/23=100%；HALF=0；PINNED 产品=0 · 详见 [阶段8高效退役分析.md](阶段8高效退役分析.md)） |
| mac / Ubuntu L4（钉盘） | ✅ **双端 L4 真冷 remaining_o=0 + 矩阵 + bstrict 129** @ **`f7424ae47`**（mac wall=63m38s · Ubuntu wall=24m45s · G.8 SHARED 双端同钉闭环）|
| pin 蛋 | ✅ `seeds/bootstrap_xlangc.darwin.arm64` + `linux.x86_64` 自本波 L4 产品 `xlang_asm` 刷新 |
| 本波构建级别 | 钉盘 **L4 已结**；日常续 **L2**（check 闸门暂停） |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录**（**仅 8 月 20 日**） |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图（本波不改） |
| [自举进度-归档-2026-08-19.md](自举进度-归档-2026-08-19.md) | **8月19日** F7 dest leftover 收口（dest-SLICE／dest-ARRAY／INDEX／host-C ret）＋ `[2]*[2]i32` skip-trait T001 |
| [自举进度-归档-2026-08-18.md](自举进度-归档-2026-08-18.md) | **8月18日** TYPE_DYN F1–F6 + F7 asm 开刀 + hello `af 02 _main`（**10** 行） |
| [自举进度-归档-2026-08-17.md](自举进度-归档-2026-08-17.md) | **8月17日** nest 30–64／dest leftover／4.2.x pin-seed |
| [自举进度-归档-2026-08-16.md](自举进度-归档-2026-08-16.md) | **8月16日** dest-in-rbx 族／匿名 match／裸 enum／SIMD dest／nest 21–29（**124** 行） |
| [自举进度-归档-2026-08-15.md](自举进度-归档-2026-08-15.md) | **8月15日** 过夜 Darwin 刀 + 升钉 `f7424ae47` + dest／SIMD／METHOD／nest 至 DEREF dest（**66** 行） |
| [自举进度-归档-2026-08-14.md](自举进度-归档-2026-08-14.md) | **8月14日** dest-SLICE／import 模块 const／INDEX-on-METHOD／f32／Vec4f／宽 lit／STRUCT_LIT／http+queue companions（**79** 行） |
| [自举进度-归档-2026-08-13.md](自举进度-归档-2026-08-13.md) | **8月13日** METHOD(49) 物化／try_inline／>16B memcpy + SIMD leftover + soft residual 梯（**66** 行） |
| [自举进度-归档-2026-08-12.md](自举进度-归档-2026-08-12.md) | **8月12日** Stage12.0.5 prefer 默认 pure-asm · PC FORBID／ALLOW · mega ban／RCA · opaque WEAK · catalog cache · L4 墙钟 · **47** 行 |
| [自举进度-归档-2026-08-11.md](自举进度-归档-2026-08-11.md) | **8月11日** Stage12.0.5 pure-asm residual 闭口 + **升钉 `e364f4a37`**（pure_asm 13/13 · L2-asm／tcpu／ldpc · labi residual · full12 双端地图 · **41** 行 + 10 日余 **4**） |
| [自举进度-归档-2026-08-09.md](自举进度-归档-2026-08-09.md) | **8月9日** runtime thin dual leave → M4 五域关 pin → Stage 8 Batch 3 30/30 → PREFER_X_O 双端 8/8（**28** 行 · 钉盘 `36363b90f`） |
| [自举进度-归档-2026-08-08.md](自举进度-归档-2026-08-08.md) | **8月8日** B′ pipeline_x mega 首刀 bootstrap_glue→ast_pool 各域→elf/wpo→8.3 结构地板→gen dual leave→typeck.x 全量 `-E` T001（**wave262–314** · **56** 行） |
| [自举进度-归档-2026-08-07.md](自举进度-归档-2026-08-07.md) | **8月7日** B′ pure leave wave200–261 + 升钉 `36363b90f` + glue_statics（**64** 行） |
| [自举进度-归档-2026-08-06.md](自举进度-归档-2026-08-06.md) | **8月6日** B′ pure leave field_access→wave199 CALL-target resolve（**50** 行） |
| [自举进度-归档-2026-08-05.md](自举进度-归档-2026-08-05.md) | **8月5日** L4 冷恢复 + 8.3.3 R2 + pure-asm match + B′ leave 至 binop（**74** 行） |
| [自举进度-归档-2026-08-04.md](自举进度-归档-2026-08-04.md) | **8月4日 wave1241–1291**（BC ast_pool 16 波域分解 + glue 壳 thin + check 收口 + 升钉 `db809e00f`） |
| [自举进度-归档-2026-08-03.md](自举进度-归档-2026-08-03.md) | **8月3日 wave1227–1240**（BC glue 叶收口 + check 工具链启动 · 15 行） |
| [自举进度-归档-2026-08-02.md](自举进度-归档-2026-08-02.md) | **8月2日 wave1130–1220**（BC glue 续迁 + typeck 跨模块字段根修 · 43 行） |
| [自举进度-归档-2026-08-01.md](自举进度-归档-2026-08-01.md) | **8月1日 wave964–1111-1129**（BC 8.3.1 G.7 fold 50+ 叶 + Stage 8 退役 13/30） |
| [自举进度-归档-2026-07-31.md](自举进度-归档-2026-07-31.md) | **31 日全日 wave895–963**（MG 真删 + post-delete + 开 BC） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单；per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`f7424ae47`** | 2026-08-15 升钉；双端 L4 真冷 + 129（mac 63m38s · Ubuntu 24m45s）；含过夜 Darwin 刀（companions／div0／ARM64 SUB／extra-.o has_obj）；前 `e364f4a37` |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自本波 L4 `xlang_asm` 刷新（darwin 4.40MiB · linux 4.21MiB） |
| residual tip（BC） | wave339 | M4 **5/5** 全闭 · Stage 8 Batch 3 **30/30 FULLY CLOSED** · 7.4.3 PREFER_X_O **双端 8/8** · pure-ld 58/60 objs · **双端 L4** @ **`f7424ae47`** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁** · sat `try-r1` 已走 `ensure_labi_prefer_one`（产品仍 mega） |
| mac 钉盘 L4 | ✅ L4 + bstrict **129** · 63m38s | remaining_o=0 · `./xbuild l4` · `/tmp/xlang_l4_Darwin_f7424ae47.log` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 + bstrict **129** · 24m45s | `ubuntu-remote-server` · `/tmp/xlang_l4_Linux_f7424ae47.log` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | G.7 单权威 `product_l2_matrix.sh`（wave299）；rv42／opt102／hello0／si0／f32 一次跑全 |
| 前钉 | `e364f4a37` · `d79a368b2` · `36363b90f` · … | Stage12.0.5 residual → formal_mod → hang guards → … |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 18 号结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ **双端 SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709`） | `stage2-hash-gate OK (SHA256 match — 黄金自举金标准)` · `xlang_asm_stage1`／`xlang_asm2` 行为一致（42 + hello）· strict-link 根修后双端首过 · 日志 `/tmp/xlang_stage2_ubuntu_9e60e1290.log` + `/tmp/xlang_stage2_macos_fix2.log` |
| **R2 / Cap** | wave710 升钉后按需 | 明细见 29 归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1**（已执行） |
| `TREE_ARMED` | **1** |
| `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1**（wave941 物理删除） |
| `PHYS_DEL_WINDOWS_GATE_STATUS` | `reproven_green` @ `71dd60a12`（wave922 · wave940 一键冷启动） |
| preflight blockers | **全清零**（wave937–939） |
| 真删条件 | ✅ 已完成 @ wave941/942 · 钉盘 **`77b334842`**（后升至 **`db809e00f`**） |
| post-delete residual | **✅ 已收**（wave944–964） |

***

## 3. 当前前排（≤3 条）

1. **日常 L2** — 钉盘 **`f7424ae47` 已升且冻结**；F7 **双端已绿**；builtin coerce **已闭**；带参 dispatch **已闭**；host-C wrapper extras **已闭**；x86 SSE extras **已闭**；f32 FLOAT_LIT stamp **已闭**；host-C f32 调用点类型化 **已闭**；dyn stack extras **已闭**；host-C nparams>6 **已闭**；ARM64 wrapper x0 拷栈 **已闭**；skip-trait 前缀 `[N]T` extra **已闭**；host-C `[K][N]T` extra **已闭**；dyn `[]i32` ARRAY_LIT extra **已闭**；host-C ret `[2]i32`／`[]i32` **已闭**；host-C dyn ret NAMED／`*i32` **已闭**；dyn NAMED extra STRUCT_LIT **已闭**；dyn ret NAMED 叶子 `*T`／`[]T`／`[N]T` **已闭**；dyn `[]Pair` extra dest-stamp **已闭**；dyn ret `[K][N]T` ndims **已闭**；host-C `[K][N]T` ret emit **已闭**；asm dest `[K][N]T` 拷贝 **已闭**；dest-ARRAY `[2]Pair` extra dest-stamp **已闭**；UFCS extras dest-ARRAY／dest-SLICE STRUCT_LIT dest-stamp **已闭**；dest-SLICE extra `[][2]i32` dest-stamp **已闭**；dest-ARRAY extra `[2][]i32` dest-stamp **已闭**；dyn ret `*[2]i32` dest-stamp **已闭**；host-C `[]*i32` type_to_c_repr 标签清洗 **已闭**；dest-SLICE extra `[]*i32` dest-stamp **已闭**；asm binop `unsafe { e }` peel **已闭**；dest-ARRAY extra `[2][]Pair` dest-stamp **已闭**；dest-SLICE extra `[][2]Pair` dest-stamp＋布局 **已闭**；dest-SLICE extra `[]*Pair` dest-stamp **已闭**；dest-SLICE extra `[][]i32`／`[][]Pair` dest-stamp **已闭**；dest ret `*[2]Pair` dest-stamp **已闭**；dest-SLICE extra `[]*[N]T` dest-stamp＋emit **已闭**；dest-SLICE extra `[]*[]i32` dest-stamp **已闭**；Ubuntu asm INDEX of `[]*[]i32` DEREF(52) base **已闭**；dest-ARRAY extra `[2]*[2]i32` skip-trait T001 + dest-stamp **已闭**；类型位 trait 名免写 `dyn` **已闭**；类型位只留 trait 名一条路径（`dyn` 前缀 P013）**已闭**；dest-SLICE extra `[][][]T` dest-stamp **已闭**；dest-SLICE extra `[][][][]T` dest-stamp **已闭**；dest extras dest-SLICE of PTR `[]*[][]T` dest-stamp **已闭**；dest extras dest-ARRAY of PTR `[2]*[][]T` dest-stamp **已闭**；dest extras dest-ARRAY of SLICE `[2][][]T` dest-stamp **已闭**；dest extras dest-SLICE of ARRAY extra `[][2][]T` dest-stamp **已闭**；dest extras dest-SLICE of ARRAY extra `[][2]*T` dest-stamp **已闭**；dest extras dest-ARRAY of ARRAY extra `[2][2][]T` flatten **已闭**；dest extras dest-ARRAY of SLICE extra `[2][]*T` dest-stamp **已闭**；dest extras dest-SLICE of SLICE extra `[][]*T` dest-stamp **已闭**；dest extras dest-ARRAY of SLICE extra `[2][][2]T` dest-stamp **已闭**；dest extras dest-SLICE of SLICE extra `[][][2]T` dest-stamp＋布局 **已闭**；dest extras dest-SLICE of SLICE extra `[][][]*T` dest-stamp **已闭**；dest extras dest-SLICE of PTR extra `[]*[2][]T` dest-stamp **已闭**；dest extras dest-ARRAY of SLICE extra `[2][][]*T` dest-stamp **已闭**；dest extras dest-ARRAY of SLICE extra `[2][][2]*T` dest-stamp **已闭**；dest extras dest-SLICE of SLICE extra `[][][2]*T` dest-stamp **已闭**；dest extras dest-SLICE of ARRAY extra `[][2][]*T` dest-stamp **已闭**；**w189 *T param home／PTR-outer `*[N][]T` 已闭**；**host-C suffix NAMED／PTR／SLICE 已闭**；**host-C `[2]*[2]i32` INDEX 已闭**；**host-C `[][2][]T` 嵌套 GNU static 已闭**；**Darwin 产品 `-o` ld-only（禁 clang driver）已闭**；**Linux hosted 产品 `-o` ld-only（禁 gcc driver）已闭**。谈自举／SHARED 大面才再 L4。
2. **邻域** — F7 wrapper 形参已走 rdi＝data（勿回头改）。**禁**空 struct 糊绿／mega prefer／默认 assemble parser.x。dyn SSE extras **禁**放 ta==1。**禁**用 `mov_rax_to_rbx` 当 wrapper 暂存（兼写 x1）。
3. **PC 底盘** — prefer 族 pure-asm 默认 ✅ · Darwin 产品 `-o` ld-only ✅ · Linux hosted 产品 `-o` ld-only ✅ · pipeline_abi mega **仍硬禁**。nest 64 **冻帽**。

***

## 4. 与归档的关系

- **8月19日归档**：[自举进度-归档-2026-08-19.md](自举进度-归档-2026-08-19.md) — F7 dest leftover 收口＋`[2]*[2]i32` skip-trait T001 + dest-stamp · 钉盘仍 **`f7424ae47`**。
- **8月18日归档**：[自举进度-归档-2026-08-18.md](自举进度-归档-2026-08-18.md) — TYPE_DYN／vtable **F1–F6** + F7 asm 开刀 + hello `af 02 _main` store 拷贝 · **10** 行 · 钉盘仍 **`f7424ae47`**。
- **8月17日归档**：[自举进度-归档-2026-08-17.md](自举进度-归档-2026-08-17.md) — nest 30–64／dest leftover／4.2.x pin-seed · 钉盘仍 **`f7424ae47`**。
- **8月16日归档**：[自举进度-归档-2026-08-16.md](自举进度-归档-2026-08-16.md) — dest-in-rbx 族／匿名 match／裸 enum／SIMD dest／nest 21–29（scratch 384）· **124** 行 · 钉盘仍 **`f7424ae47`**。
- **8月15日归档**：[自举进度-归档-2026-08-15.md](自举进度-归档-2026-08-15.md) — 过夜 Darwin 刀 + **升钉 `f7424ae47`** + dest／SIMD／METHOD／nest 至 DEREF dest · **66** 行 · 钉盘 **`f7424ae47`**。
- **8月14日归档**：[自举进度-归档-2026-08-14.md](自举进度-归档-2026-08-14.md) — dest-SLICE 族／import 模块 const／INDEX-on-METHOD／f32 xmm／s0／Vec4f／LDRSW／i64 宽 lit／STRUCT_LIT／Ubuntu L4 @`7fcc8a02d` 非 tip／http heap／queue companions · **79** 行 · 钉盘仍 **`e364f4a37`**。
- **8月13日归档**：[自举进度-归档-2026-08-13.md](自举进度-归档-2026-08-13.md) — METHOD(49) 向量 let-init／field-access／try_inline 体／let-init 门／>16B memcpy · SIMD leftover · soft residual 梯 · **66** 行 · 钉盘仍 **`e364f4a37`**。
- **8月12日归档**：[自举进度-归档-2026-08-12.md](自举进度-归档-2026-08-12.md) — Stage12.0.5 prefer 默认 pure-asm · PC FORBID／ALLOW · mega ban／RCA／墙钟 · opaque WEAK · catalog cache · L4 墙钟画像 · **47** 行 · 钉盘仍 **`e364f4a37`**。
- **8月11日归档**：[自举进度-归档-2026-08-11.md](自举进度-归档-2026-08-11.md) — Stage12.0.5 pure-asm residual 闭口 + **升钉 `e364f4a37`**（pure_asm 13/13 · L2-asm hybrid 3/3 · tcpu／ldpc · labi residual RED×0 · full12 双端地图 · **41** 行）。
- **8月9日及前**：见各日归档链接（§1 文档地图）。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `f7424ae47`**（2026-08-15 升钉 · 前 `e364f4a37` · 再前 `d79a368b2`）；residual tip **wave339**（present **0** · 日常 L2 可续）。
- 日切后主文档只保留 KPI + 前排 + **8 月 20 日** §6（19 日及前见归档）；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
|------|----------|
| **macOS arm64** | **钉盘 L4+bstrict 129 ✅** @ **`f7424ae47`**（2026-08-15 升钉 · wall=63m38s · remaining_o=0 · 矩阵 5/5）· 双端 L4 真冷绿（G.8 SHARED 同钉闭环）· 产品只走 `./xbuild` · pin 蛋已刷 · check 闸门暂停 |
| **Ubuntu x86_64（金标）** | **钉盘 L4+bstrict 129 ✅** @ **`f7424ae47`**（2026-08-15 升钉 · wall=24m45s · remaining_o=0 · 矩阵 5/5）· 双端 L4 真冷绿 · pin 蛋已刷 · **SSH 权威 host＝`ubuntu-remote-server`** · **远端工作树＝/home/shuliangfu/worker/xlang/x-lang（branch=self-hosting）** · 同步流程：`git fetch origin` + `git merge --ff-only origin/self-hosting`（先 stash 远端 M/?? pin 文件避免冲突）|
| **Windows MSYS2** | wave940 一键冷启动绿 · STATUS=`reproven_green` · Makefile 已物理删除 · 本波未升 Windows pin（仓内无 windows pin 蛋） |

***

## 6. 变更记录（倒序 · **8 月 20 日**；19 日见 [归档](自举进度-归档-2026-08-19.md)）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-08-20 | **fix(link_abi)** **Linux hosted 产品 `-o` ld-only** · tip **`37f1b2894`** | **代码** · **双端 L2 闭**。五问：产生＝`xlang_asm_invoke_ld_platform` Linux hosted 用 gcc 当链接器（crt／pie；用户 emit 已是 asm；invoke_cc 已 ALLOW 闸；Darwin ld-only 已闭）。sit-red PATH 假 gcc → `FAKE_CC_SPAWN`；裸 `ld -e main` 缺 glibc `_start` 历史 SIGSEGV。存储＝ld argv。消费＝产品 `-o`。**修**：G.7 补既有 ld（`Scrt1`／`crti`／`crtn`／`-dynamic-linker`／`-pie`／`-z noexecstack,relro,now`／`--gc-sections`；禁 gcc 回退／第二套 linker／`-e main`）。wrapper rdi＝data **未改**。pin 未动。证：**Ubuntu 金标** @ **`37f1b2894`**：假 gcc **clang_hits=0** ld_hits＝8 · DEBUG argv[0]＝`ld` · rv／hello／opt／si／f32／add／sas／apa **42／0／102／0／0／7／7／6** · hello size **27488** · `-backend c` 无 ALLOW **host-cc-requires-allow** · 矩阵 **5/5**。**mac** 假 clang **clang_hits=0** · rv／hello／opt／si／f32／add／sas／apa **42／0／102／0／0／7／7／6** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: LINUX hosted ld-only · MACOS Darwin ld-only 无回归 · LINUX gold。 |
| 2026-08-20 | **fix(link_abi)** **Darwin 产品 `-o` ld-only** · tip **`b484adc93`** | **代码** · **双端 L2 闭**。五问：产生＝`xlang_asm_invoke_ld_platform` Darwin 先 spawn clang 当链接器（用户程序 emit 已是 asm；invoke_cc 已 ALLOW 闸）。sit-red PATH 假 clang → `FAKE_CC_SPAWN`／`ld failed`（无 syslibroot）。存储＝ld argv。消费＝产品 `-o`。**修**：G.7 补既有 Darwin ld 回退（`-syslibroot`／`-dynamic`／`-arch`／`-dead_strip`／`-e _main`／`-lSystem`；SDK＝SDKROOT 或 Xcode／CLT；禁 clang 回退／第二套 linker）。Linux hosted 仍 gcc-as-ld（crt／pie 另叶）。wrapper rdi＝data **未改**。pin 未动。证：**mac** 假 clang **clang_hits=0** · rv／hello／opt／si／f32／add／sas／apa **42／0／102／0／0／7／7／6** · rv size **16792** · `-backend c` 无 ALLOW **host-cc-requires-allow** · 矩阵 **5/5**。**Ubuntu 金标** @ **`b484adc93`**：rv／hello／add／sas **42／0／7／7** · strace 仍 `/usr/bin/gcc` 链 · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: MACOS ld-only · LINUX hosted gcc leftover · LINUX gold。 |
| 2026-08-20 | **fix(codegen)** **host-C `[][2][]T` 嵌套 GNU static** · tip **`47e13bff1`** | **代码** · **双端 L2 闭**。五问：产生＝dest-SLICE of ARRAY extra 把 `codegen_array_lit_tree_is_const`（叶子 LIT）当成 C 静态初值，`emit_braced` 把 ARRAY-of-SLICE 行打成 GNU stmt-expr，`static E al[][N]={{stmt-expr}}` BLD001。sit-red 真 host-C `dyn_add_slice_arr_slice` cc fail（asm 已 7；Darwin `--backend c -o` 假绿 asm 39KiB）。存储＝ARRAY_LIT dest-stamp。消费＝host cc 静态初值。**修**：G.7 补既有 wave341 memcpy（ARRAY elem＝TYPE_SLICE → `static E al[n][N]; memcpy`）。禁拓宽 `tree_is_const`（文件作用域 dest-SLICE 仍要叶子 LIT＝1）／第二套 ARRAY_LIT emit。`[][N]i32` 仍 `{{lits}}`。wrapper rdi＝data **未改**。pin 无 dest-SLICE-of-ARRAY `al[][N]` 路径（既有考古；产品 FROM_X）。证：**双端** `dyn_add_slice_arr_slice` 真 host-C／asm **7** · `-E` `static struct xlang_slice_int32_t __xlang_al[1][2]; memcpy((void*)(__xlang_al[0]), …)` · 无 `__xlang_al[][2]=` · slice_arr 仍 `{{2, 4}}` **7** · arr_slice／arr_slice_slice／arr_arr_slice／slice_slice／slice_arr_slice_ptr／arr_ptr_arr **7／6／8／7／7／6** · add／f32／f64／arr1／arr2／named／ptr／slice／slice_arr_ptr／ptr_arr_slice／implicit／P013／call_ok／ptr_for／multi／builtin／stack／stack8 **7／7／7／7／11／7／7／7／7／7／7／P013／7／7／9／7／7／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED host-C · LINUX gold。 |
| 2026-08-20 | **fix(codegen)** **host-C `[2]*[2]i32` INDEX named-array** · tip **`0737467dd`** | **代码** · **双端 L2 闭**。五问：产生＝`type_uses_named_array_decl` 只认 `*[N]T`／`[K][N]T`，`[2]*[2]i32` 走 emit_type 剥成 `int32_t **`；INDEX `(*((p)[0]))[0]` 语法本对，`*(p[0])` 是标量。sit-red 真 host-C `dyn_add_arr_ptr_arr` cc fail（asm 已 6；Darwin `--backend c -o` 假绿 asm 39KiB）。存储＝dest-stamp extra `type_ref`。消费＝emit_func／wrapper／suffix／ARRAY_LIT。**修**：G.7 补既有 named-array（ARRAY of PTR-to-ARRAY → `E (**name)[N]`）＋ARRAY_LIT `(E (*[])[N])`。`[K]*T` 仍 `E **`。禁第二套 INDEX emit／第二套 declarator。wrapper rdi＝data **未改**。pin seed 考古孪同 commit。证：**双端** `dyn_add_arr_ptr_arr` 真 host-C／asm **6** · 具名 **5** · `[2]*i32` extra 仍 `int32_t **` **7** · `-E` `int32_t (**p)[2]`／`(void*, int32_t (**)[2])`／`(int32_t (*[])[2]){&r0,&r1}` · add／f32／f64／arr1／arr2／named／ptr／slice／slice_arr／slice_arr_ptr／ptr_arr_slice／implicit／P013／call_ok／ptr_for／multi／builtin／stack／stack8／slice_arr_slice_ptr **7／7／7／7／11／7／7／7／7／7／7／7／P013／7／7／9／7／7／7／7** · 矩阵 **5/5**。GNU static `[][2][]T` 仍开。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED host-C · LINUX gold。 |
| 2026-08-20 | **fix(codegen)** **host-C suffix NAMED／PTR／SLICE extras** · tip **`f61c37aed`** | **代码** · **双端 L2 闭**。五问：产生＝`codegen_emit_dyn_host_c_fn_ptr_suffix` 只给标量 `param_kind` 打类型，NAMED／PTR／SLICE／ARRAY 仍 `, ...)`；Darwin AAPCS64 把 extra 放栈、wrapper 从 x1 读。sit-red 真 host-C named／ptr／slice **98／153／95**；i32 extra 已 7；Darwin `--backend c -o` 假绿 asm 39KiB。存储＝dest-stamp extra `type_ref`。消费＝wrapper 已 `emit_type`／named-array／SLICE `*`。**修**：G.7 补既有 suffix（`emit_type`＋抽象 `emit_c_ptr_to_fixed_array_decl`＋SLICE `*`；标量仍 registry）。LINEAR／VECTOR 仍 `…`。禁第二套 suffix。wrapper rdi＝data **未改**。pin seed 考古孪同 commit。证：**双端** `dyn_add_named`／`dyn_add_ptr`／`dyn_add_slice` 真 host-C／asm **7** · `-E` `(void*, struct Pair)`／`(void*, int32_t *)`／`(void*, struct xlang_slice_int32_t *)` · add／f32／f64／arr1／arr2／slice_arr／ptr_arr_slice／implicit／P013／call_ok／ptr_for／multi／builtin／stack／stack8／slice_arr_slice_ptr **7／7／7／7／11／7／7／7／P013／7／7／9／7／7／7／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED host-C · LINUX gold。 |
| 2026-08-20 | **fix(asm)** **w189 *T param home walk（PTR extra `self.v`／PTR-outer `*[N][]T`）** · tip **`3361758c4`**／inject **`e23915016`** | **代码** · **双端 L2 闭**。五问：产生＝`w189_stack_off_is_emit_param_ptr_slot` 用 `(off−8)/8` 把 by-value NAMED self 槽 16 错当成 param 1；extra 是 TYPE_PTR 时 `self.v` 把 v＝1 当指针再解 → 139。sit-red dyn extra `*i32`／`*[2][]i32`／具名 `(self: A, p: *i32)` **139**；无 `self.v` 已 7；dest-SLICE extra＋`self.v` 已 7（kind 11 不进 helper）。存储＝`fill_param_slots` param 0 从 16 起再 +8（或 +width；x86 宽槽高位）。消费＝`glue_enc_local_slot_ptr_or_addr` lea by-value NAMED。**修**：G.7 补既有 w189 walk（与 fill_param_slots 同槽）。thin inject `glue_local_var_slot_needs_ptr_load_elf_c`（禁 mega `-E`；Ubuntu blkpeel merge-fail 不得挡住 ptrslot）。禁 −3／第二套 mapper。wrapper rdi＝data **未改**。证：**双端** `dyn_add_ptr`／`dyn_add_ptr_arr_slice` asm／host-C **7** · 具名 **7** · `*[2]i32` **7** · add／slice／slice_arr_slice_ptr／implicit／P013／call_ok／ptr_for／multi／builtin **7／7／7／7／P013／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED freestanding · LINUX gold。 |
| 2026-08-20 | **fix(typeck／parse)** **dest extras dest-SLICE of ARRAY extra `[][2][]*T` dest-stamp** · tip **`cc8d448f8`** | **代码** · **双端 L2 闭**。五问：产生＝impl-match ARRAY leftover 先剥 extra PTR 再剥 extra SLICE → leftover SLICE-of-PTR of `[][2][]*T` vs PTR → T001。sit-red dyn extra／UFCS／lets **T001**；具名已 dest-stamp 7。ndims≥1 dims[0..ndims−1] 是 ARRAY dim（禁 −3）。存储＝elem＝ARRAY／eek＝leaf／ndims≥1／extra SLICE 在未用槽 dims[ndims]／extra PTR 在未用槽 dims[ndims+1]（两槽可同时置；1＝`[][2][]*T`；extra SLICE-only＝`[][2][]T`；extra PTR-only＝`[][2]*T`）。extra STAR／extra empty `[]` 已完整。dest extras dest-SLICE-of-ARRAY 已 wrap PTR of leaf then extra SLICE then ARRAY then outer SLICE。消费＝impl-match extra SLICE peels leftover SLICE then extra PTR peels leftover PTR。**修**：G.7 补既有 ARRAY leftover walk peel 顺序（先 extra SLICE 后 extra PTR）。禁 −3／第二套 dest-SLICE stamp。wrapper rdi＝data **未改**。`*[N][]T` PTR-outer 仍延期。证：**双端** `dyn_add_slice_arr_slice_ptr` asm／host-C **7** · extra wrap `[][2][][]*i32` **7** · lets／具名／UFCS **7** · `-E` `struct xlang_slice_xlang_arr2_xlang_slice_int32_t_p`＋inner `struct xlang_slice_int32_t_p (*data)[2]`＋`__xlang_al[1][2]`＋wrapper `void* data` · slice_arr_slice／slice_arr_ptr／slice_slice_arr_ptr／slice_arr／arr_slice_arr_ptr／arr_arr_slice **7／7／7／7／7／8** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck／parse · LINUX gold。 |
| 2026-08-20 | **fix(typeck／parse)** **dest extras dest-SLICE of SLICE extra `[][][2]*T` dest-stamp** · tip **`d1ca2294c`** | **代码** · **双端 L2 闭**。五问：产生＝dest extras dest-SLICE-of-SLICE wrap ARRAY of leaf then wrap SLICE twice → dest 是 `[][][2]i32` 不是 `[][][2]*i32`；impl-match leftover PTR vs eeek＝leaf after extra ARRAY peels → T001。sit-red dyn extra／lets **T001**；具名／UFCS 已 dest-stamp 6／7。ndims≥1 dims[0..ndims−1] 是 inner ARRAY（禁 −3／复用 dims[0]）。存储＝elem＝SLICE／eek＝leaf／ndims≥1／extra PTR wrap 在未用槽 dims[ndims]（1＝`[][][2]*T`；0＝无 extra PTR＝`[][][2]T`；与 dest extras dest-SLICE of ARRAY extra `[][2][]T` 及 dest extras dest-ARRAY of SLICE extra `[2][][2]*T` 同未用槽；判别是 elem_kind SLICE vs ARRAY 及 SLICE vs ARRAY 外层）。extra STAR 已完整（ARRAY-or-SLICE；先 commit pending dims）。消费＝dest extras wrap PTR of leaf extra times then wrap ARRAY inner-first then wrap SLICE twice；impl-match extra PTR peels leftover PTR after extra ARRAY peels。**修**：G.7 补既有 SLICE-of-SLICE walk extra PTR peels＋既有 dest extras dest-SLICE-of-SLICE extra PTR wraps＋dim accessor dim_ix＝ndims。禁 −3／第二套 dest-SLICE stamp。wrapper rdi＝data **未改**。`*[N][]T` PTR-outer 仍延期／`[][2][]*T` 仍 T001 延期。证：**双端** `dyn_add_slice_slice_arr_ptr` asm／host-C **7** · extra wrap `[][][2][2]*i32` **7** · lets **7** · `-E` `struct xlang_slice_xlang_slice_xlang_arr2_int32_t_p`＋inner `int32_t * (*data)[2]`＋`__xlang_al[1][2]`＋wrapper `void* data` · slice_slice_arr／arr_slice_arr_ptr／slice_slice_ptr／slice_arr_ptr／arr_slice_arr／arr_arr_slice **7／7／7／7／7／8** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck／parse · LINUX gold。 |
| 2026-08-20 | **fix(typeck／parse)** **dest extras dest-ARRAY of SLICE extra `[2][][2]*T` dest-stamp** · tip **`e41c12d85`** | **代码** · **双端 L2 闭**。五问：产生＝extra STAR after `[N][][M]`（ndims≥1、ARRAY 外层）置 elem_kind＝−1，dest extras dest-ARRAY-of-SLICE 不 extra-wrap PTR after extra ARRAY wraps → dest 是 `[2][][2]i32` 不是 `[2][][2]*i32`；嵌套 ARRAY_LIT **139**。sit-red dyn extra **139**（host-C **133**）；具名／UFCS／lets 已 dest-stamp 6／7／7。ndims≥1 dims[0..ndims−1] 是 inner ARRAY（禁 −3／复用 dims[0]）。存储＝elem＝SLICE／eek＝leaf／ndims≥1／extra PTR wrap 在未用槽 dims[ndims]（1＝`[2][][2]*T`；0＝无 extra PTR＝`[2][][2]T`；与 dest extras dest-SLICE of ARRAY extra `[][2][]T` 同未用槽；判别是 elem_kind SLICE vs ARRAY）。extra STAR 须先 commit pending LBRACKET dims，否则 ndims 仍 0 且 inner ARRAY 丢失。消费＝dest extras wrap PTR of leaf extra times then wrap ARRAY inner-first then wrap SLICE then ARRAY。ARRAY leftover SLICE vs eek＝SLICE 非 T001。**修**：G.7 补既有扫描器（ndims≥1 extra STAR keep SLICE；extra PTR 进 dims[ndims]；先 commit pending dims）＋既有 dest extras dest-ARRAY-of-SLICE extra PTR wraps＋dim accessor dim_ix＝ndims。禁 −3／第二套 dest-ARRAY stamp／impl-match extra PTR peels。wrapper rdi＝data **未改**。`[][][2]*T` 仍 T001 延期／`*[N][]T` PTR-outer 仍延期。证：**双端** `dyn_add_arr_slice_arr_ptr` asm／host-C **7** · extra wrap `[2][][2][2]*i32` **7** · `-E` `struct xlang_slice_xlang_arr2_int32_t_p`＋`int32_t * (*data)[2]`＋`__xlang_al[1][2]`＋wrapper `void* data` · arr_slice_arr／arr_slice_ptr／slice_arr_ptr／arr_slice_slice_ptr／slice_slice_arr／arr_arr_slice **7／7／7／7／7／8** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck／parse · LINUX gold。 |
| 2026-08-20 | **fix(typeck／parse)** **dest extras dest-ARRAY of SLICE extra `[2][][]*T` dest-stamp** · tip **`3cc3d9565`** | **代码** · **双端 L2 闭**。五问：产生＝extra STAR after `[N][][]`（ndims＝−2、ARRAY 外层）置 elem_kind＝−1，dest extras dest-ARRAY-of-SLICE 不 extra-wrap PTR after extra SLICE wraps → dest 是 `[2][][]i32` 不是 `[2][][]*i32`；嵌套 ARRAY_LIT **139**。sit-red dyn extra **139**（host-C **133**）；具名／UFCS／modfn／asg／lets 已 dest-stamp 7／7／6／7／7。ndims＝−2 dims[0] 是 extra SLICE（禁 −3／复用 dims[0]）。存储＝elem＝SLICE／eek＝leaf／ndims＝−2／extra PTR wrap 在未用槽 dims[1]（1＝`[2][][]*T`；0＝无 extra PTR＝`[2][][]T`；与 `[][][]*T` 同编码；判别是 ARRAY vs SLICE 外层）。消费＝dest extras wrap PTR of leaf extra times then wrap SLICE then extra SLICE wraps then ARRAY。ARRAY leftover SLICE vs eek＝SLICE 非 T001。**修**：G.7 补既有扫描器（ndims＝−2 extra STAR 认 ARRAY 或 SLICE 外层；keep SLICE；extra PTR 进 dims[1]）＋既有 dest extras dest-ARRAY-of-SLICE extra PTR wraps＋dim accessor ndims＝−2 dim_ix＝1。禁 −3／第二套 dest-ARRAY stamp／impl-match extra PTR peels。wrapper rdi＝data **未改**。证：**双端** `dyn_add_arr_slice_slice_ptr` asm／host-C **7** · extra wrap `[2][][][]*i32` **7** · `-E` `struct xlang_slice_xlang_slice_int32_t_p[]`＋inner `int32_t *`＋wrapper `void* data` · arr_slice_ptr／arr_slice_slice／slice_slice_slice_ptr／slice_slice_ptr／arr_slice_arr／slice_ptr_arr_slice／arr_arr_slice **7／6／7／7／7／7／8** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck／parse · LINUX gold。 |
| 2026-08-20 | **fix(typeck／parse)** **dest extras dest-SLICE of PTR extra `[]*[2][]T` dest-stamp** · tip **`594df0649`** | **代码** · **双端 L2 闭**。五问：产生＝extra empty `[]` after `[]*` then `[N]` 置 elem_kind＝−1，dest extras dest-SLICE-of-PTR 不 extra-wrap SLICE after ARRAY → ADDR_OF of typed `[2][]i32` **run＝1 panic**（host-C **133**）。sit-red dyn extra **1**；具名／UFCS／modfn／asg 已 dest-stamp 7／7／6／7。ndims＝−2 会冲掉 `[N]`（禁 −3／新字段）。存储＝elem＝PTR／eek＝leaf／ndims≥1／extra SLICE wrap 在未用槽 dims[ndims]（1＝`[]*[2][]T`；2＝`[]*[2][][]T`；0＝无 extra＝`[]*[2]i32`）。消费＝dest extras wrap SLICE of leaf extra times then wrap ARRAY then wrap PTR then outer SLICE。SLICE-outer PTR-elem impl-match 不 walk leftover SLICE（非 T001）。outer 须 SLICE 故 `*[N][]T` 仍延期。**修**：G.7 补既有扫描器（keep PTR；extra wrap 进 dims[ndims]；ARRAY 或 PTR elem；ndims＝−2 第一支不在 ndims＞0 时抢走 extra wrap）＋既有 dest extras dest-SLICE-of-PTR extra SLICE wraps＋dim accessor dim_ix＝ndims。禁 −3／第二套 dest-SLICE stamp／impl-match extra peels。wrapper rdi＝data **未改**。`[2][][]*T` 仍延期。证：**双端** `dyn_add_slice_ptr_arr_slice` asm／host-C **7** · extra wrap `[]*[2][][]i32` **7** · `-E` `struct xlang_slice_xlang_arr2_xlang_slice_int32_t_p`＋`struct xlang_slice_int32_t (*(*data))[2]`＋`__xlang_al[1])[2]`＋wrapper `void* data` · slice_ptr_arr／slice_arr_slice／slice_ptr_slice／slice_ptr_slice_slice／slice_slice_slice_ptr／slice_slice_arr／arr_arr_slice **7／7／7／7／7／7／8** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck／parse · LINUX gold。 |
| 2026-08-20 | **fix(typeck／parse)** **dest extras dest-SLICE of SLICE extra `[][][]*T` dest-stamp** · tip **`362bf6f58`** | **代码** · **双端 L2 闭**。五问：产生＝extra STAR after `[][][]`（ndims＝−2）置 elem_kind＝−1，dest extras dest-SLICE-of-SLICE 不入 → 嵌套 ARRAY_LIT **139**；只存 leaf 会 T001（leftover PTR vs eeek＝leaf）。sit-red dyn extra **139**；具名／UFCS／modfn／asg 已 dest-stamp 7。ndims＝−2 dims[0] 是 extra SLICE（禁 −3／复用 dims[0]）。存储＝elem＝SLICE／eek＝leaf／ndims＝−2／extra PTR wrap 在未用槽 dims[1]（1＝`[][][]*T`；0＝无 extra PTR＝`[][][]T`）。消费＝impl-match extra PTR peels leftover PTR after extra SLICE peels／dest extras wrap PTR of leaf extra times then wrap SLICE twice then extra SLICE wraps。outer 须 SLICE 故 `[2][][]*T` 仍延期。**修**：G.7 补既有扫描器（keep SLICE；extra PTR 进 dims[1]）＋既有 SLICE-of-SLICE walk extra PTR peels＋既有 dest extras dest-SLICE-of-SLICE extra PTR wraps＋dim accessor ndims＝−2 dim_ix＝1。禁 −3／第二套 dest-SLICE stamp。wrapper rdi＝data **未改**。`[]*[2][]T` 仍延期。证：**双端** `dyn_add_slice_slice_slice_ptr` asm／host-C **7** · extra wrap `[][][][]*i32` **7** · `-E` `struct xlang_slice_xlang_slice_xlang_slice_int32_t_p`＋inner `int32_t * *data`＋wrapper `void* data` · slice_slice_ptr／slice_slice_slice／slice_slice_slice_slice／slice_slice_arr／arr_slice_ptr／arr_arr_slice **7／7／7／7／7／8** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck／parse · LINUX gold。 |
| 2026-08-20 | **fix(typeck／codegen)** **dest extras dest-SLICE of SLICE extra `[][][2]T` dest-stamp＋布局** · tip **`987f198a3`** | **代码** · **双端 L2 闭**。五问：产生＝impl-match leftover ARRAY vs eeek＝leaf → T001；dest extras dest-SLICE-of-SLICE 只 wrap 两次 → dest 是 `[][]i32` 不是 `[][][2]i32`；host-C 缺 `xlang_slice_xlang_slice_xlang_arr2_int32_t`。sit-red dyn extra **T001**；具名／UFCS／modfn／asg 已 dest-stamp 7／7／6／6。存储＝elem＝SLICE／eek＝leaf／ndims≥1／dims[0]＝M（wave437 pending LBRACKET 已收集 inner ARRAY；extra PTR 仍 ndims＝0 dims[0]；extra SLICE 仍 ndims＝−2；禁 −3）。消费＝impl-match extra ARRAY peels leftover ARRAY／dest extras wrap ARRAY of leaf inner-first then wrap SLICE twice／layout walker 发 SLICE-of-(SLICE-of-ARRAY／PTR)。**修**：G.7 补既有 SLICE-of-SLICE walk extra ARRAY peels＋既有 dest extras dest-SLICE-of-SLICE extra ARRAY wraps＋既有 `codegen_emit_slice_of_fixed_array_layouts`（SLICE-of-SLICE 链触 ARRAY／PTR 才发外层 fat；标量 `[][]T` 仍 nest table）。禁 −3／第二套 dest-SLICE stamp／第二套 layout walker。wrapper rdi＝data **未改**。`[]*[2][]T`／`[][][]*T` 仍延期。证：**双端** `dyn_add_slice_slice_arr` asm／host-C **7** · extra wrap `[][][2][2]i32` **7** · `[][]*T` host-C 布局同 walker **7** · `-E` `struct xlang_slice_xlang_slice_xlang_arr2_int32_t`＋inner `int32_t (*data)[2]`＋`__xlang_al[][2]`＋wrapper `void* data` · slice_slice／arr_slice_arr／slice_arr／slice_slice_ptr／slice_slice_slice／arr_arr_slice／arr_slice_ptr **7／7／7／7／7／8／7** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck／codegen · LINUX gold。 |
| 2026-08-20 | **fix(typeck)** **dest extras dest-ARRAY of SLICE extra `[2][][2]T` dest-stamp** · tip **`dd5a7c875`** | **代码** · **双端 L2 闭**。五问：产生＝dest extras dest-ARRAY-of-SLICE 只 wrap 一次 → dest 是 `[2][]i32` 不是 `[2][][2]i32`。sit-red dyn extra 嵌套 lit **139**；具名／UFCS／asg／typed lets 已 dest-stamp 7；impl-match leftover SLICE vs eek＝SLICE 非 T001。存储＝elem＝SLICE／eek＝leaf／ndims≥1／dims[0]＝M（wave437 pending LBRACKET 已收集 inner ARRAY；extra PTR 仍 ndims＝0 dims[0]；extra SLICE 仍 ndims＝−2；禁 −3）。消费＝dest extras wrap ARRAY of leaf inner-first then wrap SLICE then ARRAY。**修**：G.7 补既有 dest extras dest-ARRAY-of-SLICE extra ARRAY wraps（typeck.x＋pin twin；inner-first）。禁 −3／第二套 dest-ARRAY stamp／impl-match extra ARRAY peels。wrapper rdi＝data **未改**。`[][][2]i32` 仍 T001 延期。证：**双端** `dyn_add_arr_slice_arr` asm／host-C **7** · extra wrap `[2][][2][2]i32` **7** · `-E` `struct xlang_slice_xlang_arr2_int32_t`＋`int32_t (*data)[2]`＋`__xlang_al[][2]`＋wrapper `void* data` · arr_slice／slice_arr／arr_slice_slice／arr_slice_ptr／slice_slice_ptr／arr_arr_slice／slice_arr_ptr **7／7／6／7／7／8／7** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck · LINUX gold。 |
| 2026-08-20 | **fix(typeck／parse)** **dest extras dest-SLICE of SLICE extra `[][]*T` dest-stamp** · tip **`0965b5b20`** | **代码** · **双端 L2 闭**。五问：产生＝dest extras dest-SLICE-of-SLICE 只 wrap 两次 → dest 是 `[][]i32` 不是 `[][]*i32`；extra STAR after `[][]` 原先只认 ARRAY 外层。sit-red dyn extra 嵌套 lit **139**；具名／UFCS／asg 已 dest-stamp 7；typed lets 曾 139。只存 leaf 会 T001（impl-match leftover PTR vs eeek＝leaf）。ndims＝−2 是 extra SLICE（禁 −3／新字段）。存储＝elem＝SLICE／eek＝leaf／ndims＝0／extra PTR wrap 在未用槽 dims[0]（1＝`[][]*T`；0＝无 extra PTR＝`[][]i32`）。消费＝impl-match extra PTR peels leftover PTR／dest extras wrap PTR of leaf then wrap SLICE twice。与 `[2][]*T` 同 dims[0] extra PTR 编码（判别是 SLICE vs ARRAY 外层）。`[][][]*T` 仍延期。**修**：G.7 补既有扫描器（keep SLICE；extra PTR 进 dims[0]；ARRAY 或 SLICE 外层）＋既有 SLICE-of-SLICE walk extra PTR peels＋既有 dest extras dest-SLICE-of-SLICE extra PTR wraps＋dim accessor ndims＝0 dim_ix＝0。禁 −3／第二套 dest-SLICE stamp。wrapper rdi＝data **未改**。证：**双端** `dyn_add_slice_slice_ptr` asm／host-C **7** · 具名／UFCS／lets **7** · `-E` `struct xlang_slice_xlang_slice_int32_t_p`＋`int32_t * *data`＋wrapper `void* data` · slice_slice／arr_slice_ptr／slice_arr_ptr／slice_ptr／slice_slice_slice／arr_slice_slice／arr_arr_slice **7／7／7／7／7／6／8** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck／parse · LINUX gold。 |
| 2026-08-20 | **fix(typeck／parse)** **dest extras dest-ARRAY of SLICE extra `[2][]*T` dest-stamp** · tip **`bffe2bb80`** | **代码** · **双端 L2 闭**。五问：产生＝dest extras dest-ARRAY-of-SLICE 只 wrap 一次 → dest 是 `[2][]i32` 不是 `[2][]*i32`；extra STAR after `[N][]` 走 token_to_type_kind＝−1（eek 未捕获）但 impl-match 已在 SLICE 层匹配（非 T001）。sit-red dyn extra 嵌套 lit **139**；具名／UFCS／typed lets 已 dest-stamp 6／7；邻域 `[2][]i32`／`[2][][]i32`／`[][2]*i32`／`[2]*i32` 已 7／6／7／7。ndims＝−2 是 extra SLICE（禁 −3／新字段）。存储＝elem＝SLICE／eek＝leaf／ndims＝0／extra PTR wrap 在未用槽 dims[0]（1＝`[2][]*T`；0＝无 extra PTR＝`[2][]i32`）。消费＝dest extras wrap PTR of leaf then wrap SLICE then ARRAY。**修**：G.7 补既有扫描器（keep SLICE；extra PTR 进 dims[0]；outer 须 ARRAY 故 `[][]*T` 仍延期）＋既有 dest extras dest-ARRAY-of-SLICE extra PTR wraps＋dim accessor ndims＝0 dim_ix＝0。禁 −3／第二套 dest-ARRAY stamp／impl-match extra PTR peels。wrapper rdi＝data **未改**。证：**双端** `dyn_add_arr_slice_ptr` asm／host-C **7** · 具名 **6** · UFCS **7** · `-E` `struct xlang_slice_int32_t_p`＋`int32_t * *data`＋`__xlang_al[2]`＋wrapper `void* data` · arr_slice／arr_slice_slice／slice_arr_ptr／arr1／arr_arr_slice **7／6／7／7／8** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck／parse · LINUX gold。 |
| 2026-08-20 | **fix(asm)** **dest extras dest-ARRAY of ARRAY extra `[2][2][]T` flatten** · tip **`cd505daf3`** | **代码** · **双端 L2 闭**。五问：产生＝asm flatten 把 dest-ARRAY of ARRAY of SLICE 写成 i32 叶子；dest extras dest-ARRAY wrap 已 dest-stamp（host-C 嵌套 lit 已 8）。sit-red dyn／具名／UFCS／模块函数 asm **139**；asg asm **1**。存储＝ARRAY ndims＝2 dims＝[2,2] elem＝SLICE eek＝leaf（已正确）。消费＝flatten 见 dest elem `[2][]i32`（ARRAY of SLICE）则逐格 `glue_emit_slice_from_array_let_init`。**修**：G.7 补既有 `pipeline_asm_emit_array_lit_flat_elf_c`（mega＋seed＋arrcopy thin 同符号）。禁 −3／第二套 flatten。wrapper rdi＝data **未改**。证：**双端** `dyn_add_arr_arr_slice` asm／host-C **8** · 具名／asg **8** · arr2／arr_slice／arr_slice_slice／slice_arr_ptr／arr1 **11／7／6／7** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED freestanding · LINUX gold。 |
| 2026-08-20 | **fix(typeck／parse)** **dest extras dest-SLICE of ARRAY extra `[][2]*T` dest-stamp** · tip **`88019b5b1`** | **代码** · **双端 L2 闭**。五问：产生＝extra STAR after ARRAY 走 token_to_type_kind＝−1 再 want_param_ty＝0，eek 未捕获；impl-match leftover PTR vs eeek＝−1 → T001。sit-red dyn／具名／UFCS **T001**；模块函数／assign 已 dest-stamp 6；邻域 `[][2]i32`／`[]*[2]i32`／`[2]*i32` 已 7。ndims＝−2 会冲掉 ARRAY dim（禁 −3／新字段）。存储＝elem＝ARRAY／eek＝leaf／ndims＝1／dims[0]＝2／extra PTR wrap 在未用槽 dims[ndims+1]（1＝`[][2]*T`；extra SLICE 仍 dims[ndims]）。消费＝impl-match ARRAY extra PTR peels／dest extras wrap PTR of leaf then ARRAY then outer SLICE。**修**：G.7 补既有扫描器（keep ARRAY；extra PTR 进 dims[ndims+1]；outer 须 SLICE）＋既有 ARRAY walk extra PTR peels＋既有 dest extras dest-SLICE-of-ARRAY extra PTR wraps＋dim accessor dim_ix＝ndims+1。禁 −3／第二套 dest-SLICE stamp。wrapper rdi＝data **未改**。证：**双端** `dyn_add_slice_arr_ptr` asm／host-C **7** · 具名／UFCS **7** · `-E` `struct xlang_slice_xlang_arr2_int32_t_p`＋`int32_t * (*data)[2]`＋`__xlang_al[1][2]`＋wrapper `void* data` · slice_arr／slice_ptr_arr／slice_arr_slice／slice_ptr／arr_slice_slice／arr1 **7／7／7／7／6／7** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck／parse · LINUX gold。 |
| 2026-08-20 | **fix(typeck／parse)** **dest extras dest-SLICE of ARRAY extra `[][2][]T` dest-stamp** · tip **`8222dcc9f`** | **代码** · **双端 L2 闭**。五问：产生＝dest extras dest-SLICE-of-ARRAY 只 wrap 一次（或 store 置 elem_kind＝−1 跳过 dest extras）→ dest 是 `[][2]i32` 不是 `[][2][]i32`；嵌套 ARRAY_LIT `[[[2], [4]]]` 停在未 stamp → 139。sit-red dyn extra 嵌套 lit **139**；具名／UFCS／模块函数已 dest-stamp 6／7；邻域 `[][2]i32` 已 7。ndims＝−2 会冲掉 ARRAY dim（禁 −3／新字段）。存储＝elem＝ARRAY／eek＝leaf／ndims＝1／dims[0]＝2／extra wrap 在未用槽 dims[ndims]（1＝`[][2][]T`）。消费＝impl-match ARRAY extra peels／dest extras extra wraps。**修**：G.7 补既有扫描器（keep ARRAY；extra wrap 进 dims[ndims]；outer 须 SLICE）＋既有 ARRAY walk extra peels＋既有 dest extras dest-SLICE-of-ARRAY extra wraps＋dim accessor dim_ix＝ndims。禁 −3／第二套 dest-SLICE stamp。wrapper rdi＝data **未改**。证：**双端** `dyn_add_slice_arr_slice` asm／host-C **7** · 具名 **6** · `-E` `struct xlang_slice_xlang_arr2_xlang_slice_int32_t`＋`__xlang_al[][2]`＋wrapper `void* data` · slice_arr／slice_slice／arr_slice_slice／slice_slice_slice／arr1 **7／7／6／7／7** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck／parse · LINUX gold。 |
| 2026-08-20 | **fix(typeck)** **dest extras dest-ARRAY of SLICE `[2][][]T` dest-stamp** · tip **`28f5d0ee0`** | **代码** · **双端 L2 闭**。五问：产生＝dest extras dest-ARRAY-of-SLICE 只 wrap 一次 → dest 是 `[2][]i32` 不是 `[2][][]i32`；嵌套 ARRAY_LIT `[[[2, 4]], [[3, 5]]]` 停在 `(int32_t[][1][2])` → 139。sit-red dyn extra 嵌套 lit **139**；具名／UFCS／模块函数 typed lets 已 dest-stamp 6；邻域 `[2][]i32` 已 7。impl-match 已在 SLICE 层匹配（非 T001）。存储＝elem＝SLICE／eek＝leaf／ndims＝−2／dims[0]＝0（0 表 1＝`[2][][]T`）。消费＝dest extras extra wraps。**修**：G.7 补既有 dest extras dest-ARRAY-of-SLICE extra wraps（dim accessor）。禁 −3／第二套 dest-ARRAY stamp／impl-match extra peels。wrapper rdi＝data **未改**。证：**双端** `dyn_add_arr_slice_slice` asm／host-C **6** · `-E` `struct xlang_slice_xlang_slice_int32_t[]`＋`__xlang_al`＋wrapper `void* data` · arr_slice／slice_slice／slice_slice_slice／slice_slice_slice_slice／arr_ptr_ss／arr_slice_named／arr1 **7／7／7／7／6／7／7** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck · LINUX gold。 |
| 2026-08-20 | **fix(typeck／parse)** **dest extras dest-ARRAY of PTR `[2]*[][]T` dest-stamp** · tip **`741af00ad`** | **代码** · **双端 L2 闭**。五问：产生＝ARRAY＋PTR impl-match 对 ndims＝−2 只 peel 一次（pelem 仍是 SLICE vs eek＝leaf）→ T001；dest extras dest-ARRAY-of-PTR 只 wrap 一次 → dest 会是 `[2]*[]i32` 不是 `[2]*[][]i32`。sit-red dyn extra T001；具名／UFCS／模块函数已 dest-stamp 7；邻域 `[2]*[]i32` 已 6。存储＝elem＝PTR／eek＝leaf／ndims＝−2／dims[0]＝2（0 表 1＝`[2]*[]T`）。消费＝impl-match extra peels／dest extras extra wraps。**修**：G.7 补既有 ARRAY＋PTR walk extra peels（孪 SLICE-of-SLICE）＋既有 dest extras dest-ARRAY-of-PTR extra wraps（dim accessor）。禁 −3／第二套 dest-ARRAY stamp。wrapper rdi＝data **未改**。证：**双端** `dyn_add_arr_ptr_slice_slice` asm／host-C **6** · `-E` `struct xlang_slice_xlang_slice_int32_t *[]`＋wrapper `void* data` · arr_ptr_arr／slice_ptr_slice／slice_ptr_slice_slice／slice_slice_slice／slice_slice_slice_slice／arr_slice／slice_ptr／arr1／slice_ptr_arr **6／7／7／7／7／7／7／7／7** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck／parse · LINUX gold。 |
| 2026-08-20 | **fix(typeck)** **dest extras dest-SLICE of PTR `[]*[][]T` dest-stamp** · tip **`17fb72ae4`** | **代码** · **双端 L2 闭**。五问：产生＝skip-trait `[]*` 后再空 `[]` 把 ndims＝−2 再写一遍（increment 只认 elem＝SLICE，extra wrap 仍 1）；dest extras dest-SLICE-of-PTR 只 wrap 一次 → dest 是 `[]*[]i32` 不是 `[]*[][]i32`。sit-red dyn extra compile＝0 run＝1 panic：0；具名／UFCS／模块函数已 7。存储＝elem＝PTR／eek＝leaf／ndims＝−2／dims[0]＝2（0 表 1＝`[]*[]T`）。消费＝dest extras extra wraps。**修**：G.7 补既有扫描器 extra wrap 计数（PTR 也 increment dims[0]）＋既有 dest extras dest-SLICE-of-PTR extra wraps（dim accessor）。禁 −3／第二套 dest-SLICE stamp。wrapper rdi＝data **未改**。证：**双端** `dyn_add_slice_ptr_slice_slice` asm／host-C **7** · 具名 **7** · `-E` `__xlang_sp`＋`struct xlang_slice_xlang_slice_int32_t *` · ptr_slice／slice_slice_slice／slice_slice_slice_slice／slice_slice／slice_ptr **7** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck／parse · LINUX gold。 |
| 2026-08-20 | **fix(typeck)** **dest-SLICE extra `[][][][]T` dest-stamp** · tip **`bbc93523a`** | **代码** · **双端 L2 闭**。五问：产生＝skip-trait `[][][]` 后再 `[` `]` 把 ndims＝−2 再写一遍（extra wrap 仍 1）；impl-match 只 peel 一次，pipeline pelem 仍是 SLICE vs eek＝leaf → T001（dyn／具名同红；UFCS／模块函数已 7／6）。存储＝elem＝SLICE／eek＝leaf／ndims＝−2／dims[0]＝2（0 表 1＝三层）。消费＝impl-match extra peels／dest extras extra wraps。**修**：G.7 补既有扫描器（extra wrap 计数进 dims[0]）＋SLICE-of-SLICE walk extra peels＋dest-SLICE-of-SLICE extra wraps＋ndims＝−2 时 dim accessor。禁 −3／第二套 dest-SLICE stamp。wrapper rdi＝data **未改**。证：**双端** `dyn_add_slice_slice_slice_slice` asm／host-C **7** · 具名 **7** · `-E` 四层 dest-SLICE wrap · slice_slice_slice／slice_slice／ptr_slice／slice_slice_named **7** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck／parse · LINUX gold。 |
| 2026-08-20 | **fix(typeck)** **dest-SLICE extra `[][][]T` dest-stamp** · tip **`72f00becb`** | **代码** · **双端 L2 闭**。五问：产生＝skip-trait `[][]` 后 `[` `]` 置 elem_kind＝−1（dest extras 不入）；只存 ndims＝−2 无 impl-match extra peel 会 T001。存储＝elem＝SLICE／eek＝leaf／ndims＝−2（与 `[]*[]T` 同哨兵）。消费＝dest extras 第三 wrap／impl-match extra peel。**修**：G.7 补既有扫描器（SLICE｜PTR＋ndims＝−2）＋SLICE-of-SLICE walk extra peel＋dest-SLICE-of-SLICE 第三 wrap。禁第二套 dest-SLICE stamp。wrapper rdi＝data **未改**。证：**双端** `dyn_add_slice_slice_slice` asm／host-C **7** · slice_slice／ptr_slice／slice_slice_named **7** · implicit／P013／add／call_ok／ptr_for／multi／builtin **7／P013／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED typeck／parse · LINUX gold。 |
| 2026-08-20 | **fix(parse)** **类型位去掉 `dyn` 前缀（只留 trait 名）** · tip **`ac78239f0`** | **代码** · **双端 L2 闭**。五问：产生＝type_ref `dyn` IDENT 前缀第二套 wrap TYPE_DYN；存储＝TYPE_DYN；消费＝F2／F3。用户要 AI 只写一条。**修**：删 peel wrap；`dyn`＋后续类型 **P013** sticky（`parser_report_dyn_prefix_p013_c`）；权威仍 `wrap_registered_trait_as_dyn`。禁第二套 dyn kind。wrapper rdi＝data **未改**。证：mac implicit asm／host-C **7** · rejected **P013** · type／let／ptr／add／call_ok／ptr_for／multi／builtin **7／7／7／7／7／7／9／7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED parse · LINUX gold。 |
| 2026-08-20 | **fix(parse)** **类型位 trait 名免写 `dyn`** · tip **`653a1cc33`** | **代码** · **双端 L2 闭**。五问：产生＝type_ref IDENT 把已注册 trait 名收成 TYPE_NAMED；存储＝type_ref kind／name；消费＝F2 coerce／F3 dispatch 要 TYPE_DYN。sit-red `let x: Clone = a` 赋值 mismatch（`dyn Clone` 已 7）。**修**：G.7 补既有 type_ref IDENT（`xlang_skip_trait_is_registered_c`＋单 TYPE_DYN allocator；当时 `dyn Trait` peel 仍合法）。禁第二套 dyn kind。wrapper rdi＝data **未改**。证：**双端** `dyn_type_implicit` asm／host-C **7** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED parse · LINUX gold。 |
