# Xlang / X language 自举进度

> **更新**：2026-08-28 · tip **`f054b276f`**（STD-048 queue-concurrent 残 fallthrough soft→硬绿双端） · 钉盘仍 **`e8176cbe5`**（**不升钉**）· 日切：27 日 **60** 行已入 [归档](自举进度-归档-2026-08-27.md) · **金标 SSH＝`ubuntu-server`** · Ubuntu L2 **5／5**\
> **产品 L4 放行钉盘**：**`e8176cbe5`**（2026-08-23 升钉 · 前 `f7424ae47` 2026-08-15 · 再前 `e364f4a37` 2026-08-11）\
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅**（12 labi_*.x 切片 prefer；默认 FROM_X=1）· parser **7.2.2 关 pin ✅**（tip `-E` + assemble；产品默认 FROM_X=1）· codegen 7.4.2 wave323 ✅ · typeck 7.4.1 wave322 ✅ · runtime monofile wave321 ✅ · present **0** · 钉盘 **`e8176cbe5`** · Stage 8 tip **wave332 Track L 30/30 FULLY CLOSED**（**PRODUCT HALF=0** · **PRODUCT PINNED=0** · **PRODUCT RETIRED 23/23 = 100%**）· **7.4.3 PREFER_X_O 双端 8/8 wave338 ✅** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁**\
> **tip L4 安全网（历史）**：**`ffc0b646f`**（已由 tip L4 升钉吸收；前网 `4bacaf18c`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：**STD-048 queue-concurrent 残 fallthrough soft→硬绿 ✅** · tip **`f054b276f`**（代码 **`57bf7ee45`**） · L2 **5／5** · 钉盘仍 **`e8176cbe5`**（**不升钉**）。
> **代码 tip**：`57bf7ee45` — Darwin queue-concurrent run=1 **obs=2**；Ubuntu queue-concurrent run=1 **obs=1** · tar-extended／atomic-ordering／tar-ustar／thread-pool／io-context／log-rotate-async／aes-gcm／compress／nfc／grapheme／http-h2／query-rows／next-row／col_text／col_blob／channel／ffi／blob-stream／socketio 积压仍绿。
> **下波候选**：软残续扫（非 ensure tinies／仍 soft SKIP→OK 或 prefer-c／auto-make／XLANG fallthrough 的 `std-*`：如 **sync-rwlock-condvar XLANG fallthrough** 等；**soft-ensure 族另案**；**unicode-normalization API 面缺失＝产品债**；**strview-zc4 绑 zc4／host-c 后置**）；**hash＊／http-context／reqresp／server-pool／unbounded／encoding-hex／uuid** 产品红跳过；unicode-nfc／grapheme／http-h2／query-rows／next-row／col_text／col_blob／channel／ffi／blob-stream／socketio／compress＊／aes-gcm nist2／sqlite＊ tip 产品 UNDEF／SEGV／exit≠0＝obs（产品另案）；**zc3／zc4／zc5 仍绑 host-c 后置**；禁 mega／升钉；仍跳 ensure_std／fmt-check／brotli／without-c。**nest 冻 64**。

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1；过程上 prefer+heat 一次／域，双端矩阵同波一次\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；**SHARED 双端 L2**；日常矩阵 **只** `./xbuild l2-matrix`（wave299：捕获 rv=42／opt=102 成功码，禁 set -e 截断）；谈自举／升钉才 L4 + 矩阵 + 全量 bstrict；bstrict **per-script timeout=300s** + catalog cache（防无界卡死）；**墙钟**：`product_l4` 分步 wall + `run-all-bstrict` 每脚本 wall／top-N（`XLANG_BSTRICT_TIMING_FILE`）；并行 **`XLANG_BSTRICT_JOBS` 默认 1**（Ubuntu 可 opt-in 2–4；Darwin 慎用 OOM）

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[27](自举进度-归档-2026-08-27.md) · [26](自举进度-归档-2026-08-26.md) · [25](自举进度-归档-2026-08-25.md) · [24](自举进度-归档-2026-08-24.md) · [23](自举进度-归档-2026-08-23.md) · [22](自举进度-归档-2026-08-22.md) · [21](自举进度-归档-2026-08-21.md) · [20](自举进度-归档-2026-08-20.md) · [19](自举进度-归档-2026-08-19.md) · [18](自举进度-归档-2026-08-18.md) · [17](自举进度-归档-2026-08-17.md) · [16](自举进度-归档-2026-08-16.md) · [15](自举进度-归档-2026-08-15.md) · [14](自举进度-归档-2026-08-14.md) · [13](自举进度-归档-2026-08-13.md) · [12](自举进度-归档-2026-08-12.md) · [11](自举进度-归档-2026-08-11.md) · [09](自举进度-归档-2026-08-09.md) · [08](自举进度-归档-2026-08-08.md) · [07](自举进度-归档-2026-08-07.md) · [06](自举进度-归档-2026-08-06.md) · [05](自举进度-归档-2026-08-05.md) · [04](自举进度-归档-2026-08-04.md) · [03](自举进度-归档-2026-08-03.md) · [02](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · …\

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `e8176cbe5`**（2026-08-23 升钉 · 前 `f7424ae47` 2026-08-15 · 再前 `e364f4a37` 2026-08-11）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**双端 Stage2 = ✅ SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709` · `xlang_asm_stage1`／`xlang_asm2` bit-identical · 黄金自举金标准 · strict-link 根修后双端首过）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（Makefile 已删除；**变更记录只在本文 §6**）。

| 指标 | 状态 |
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`e8176cbe5`**（L4 升钉冻结 · 双端 L4 真冷绿 · check-gate al06 入口 typeck）；residual tip **wave332** present **0**；Stage 8 tip **wave332**（**30/30 FULLY CLOSED**；**PRODUCT RETIRED 23/23=100%**；PRODUCT HALF=0；PRODUCT PINNED=0） |
| Track MG | **11.3.1 MG ✅**（wave941 物理删 Makefile · wave942 catalog CFLAGS · 0 make in bootstrap · 产品入口 `./xbuild`） |
| Track BC | **开 🟢（30/30 FULLY CLOSED wave332）**（B′ 至 **wave326** link_abi **7.3.1 关 pin** ✅ · M4 **5/5 全 ✅**（runtime/typeck/codegen/parser/link_abi 五域冷链全闭）· present **0**；**BC Stage8 Batch3 收**＝**30/30 FULLY CLOSED**（PRODUCT 23 RETIRED 100% catalog+ladder；NON-PRODUCT 7 正确分类 never-product-chain = TEST×2 / STAGE×2 / EXTRACT_ONLY×1 / DELETED_ORPHAN×2；G.7 无 HALF 无 unresolved PINNED）|
| Track L (Stage 8) | **收（Batch 3 30/30 FULLY CLOSED wave332）**（批次 0 清理 ✅ · 批次 2 **wave1036–1041** 退役 ✅ · 批次 3 **wave327–332**（lsp_diag+lsp+pipeline+driver+preprocess+lexer+parser/typeck/codegen/ast_gen2 10 项 + audit 分类误判 3 项 RETIRED 修正 + ast_gen.c 孤儿物理删 + ast_gen2 cold-seed pin + 分母口径统一 is_product_denominator）✅ · **30/30 FULLY CLOSED**；PRODUCT RETIRED 23/23=100%；HALF=0；PINNED 产品=0 · 详见 [阶段8高效退役分析.md](阶段8高效退役分析.md)） |
| mac / Ubuntu L4（钉盘） | ✅ **双端 L4 真冷 remaining_o=0 + 矩阵 + bstrict 129** @ **`e8176cbe5`**（mac wall=85m0s · Ubuntu rebuild 7m27s＋heat＋bstrict 4m10s · G.8 SHARED 双端同钉闭环）|
| pin 蛋 | ✅ `seeds/bootstrap_xlangc.darwin.arm64` + `linux.x86_64` 自本波 L4 产品 `xlang_asm` 刷新 |
| 本波构建级别 | 钉盘 **L4 已结**；日常续 **L2**（check 闸门暂停） |

***

## 1. 文档地图

| 文档 | 看什么 |
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录**（**仅 8 月 28 日**） |
| [自举进度-归档-2026-08-27.md](自举进度-归档-2026-08-27.md) | **8月27日** soft FAIL／prefer-c／SKIP→OK 多票硬绿（f-*／STD／B／sys／perf／lang／zc2／json＋log＋math 等）（**60** 行） |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图（本波不改） |
| [自举进度-归档-2026-08-26.md](自举进度-归档-2026-08-26.md) | **8月26日** STD soft→硬绿梯 ＋ f-phase／f04／f08／f-de-c 等 soft→硬绿（**82** 行） |
| [自举进度-归档-2026-08-25.md](自举进度-归档-2026-08-25.md) | **8月25日** soft 梯／EXC soft→硬绿／STD soft→硬绿多票（**65** 行） |
| [自举进度-归档-2026-08-24.md](自举进度-归档-2026-08-24.md) | **8月24日** 日切重写 · Stage2／WPO／零 make／假权威诚实 DOC 族 · b04／std-config c／NL-04／generic_layout／vscode 0.2／import async（**64** 行） |
| [自举进度-归档-2026-08-23.md](自举进度-归档-2026-08-23.md) | **8月23日** 升钉 `e8176cbe5` ＋ check-gate al06 ＋ labi／stubs unique UNDEF 族 ＋ Darwin CG002／process_merge／minos／g05 ＋ sat L4／fmt／modlet ＋ ALLOW ensure 空 ＋ g05 stubs scan ＋ Ubuntu tip L4（**44** 行） |
| [自举进度-归档-2026-08-22.md](自举进度-归档-2026-08-22.md) | **8月22日** typeck pin spelling_eq ＋ x86_64 `orq` ＋ http STRUCT_LIT ＋ P014 ＋ csv T001 ＋ AAPCS64 第 7 参 ＋ cli `-o` ＋ fmt 静默失败（**8** 行） |
| [自举进度-归档-2026-08-21.md](自举进度-归档-2026-08-21.md) | **8月21日** leftover extra SLICE peels RET twin ＋ dest-PARAM／RET extras ＋ link-abi wave956–958 ＋ 双端 L4（**13** 行） |
| [自举进度-归档-2026-08-20.md](自举进度-归档-2026-08-20.md) | **8月20日** dest extras dest-RET／PTR-outer ＋ ld-only／prefer hybrid／inject ＋ host-C／w189／dyn 前缀（**47** 行） |
| [自举进度-归档-2026-08-19.md](自举进度-归档-2026-08-19.md) | **8月19日** F7 dest leftover 收口（dest-SLICE／dest-ARRAY／INDEX／host-C ret）＋ `[2]*[2]i32` skip-trait T001 |
| [自举进度-归档-2026-08-18.md](自举进度-归档-2026-08-18.md) | **8月18日** TYPE_DYN F1–F6 + F7 asm 开刀 + hello `af 02 _main`（**10** 行） |
| [自举进度-归档-2026-08-17.md](自举进度-归档-2026-08-17.md) | **8月17日** nest 30–64／dest leftover／4.2.x pin-seed |
| [自举进度-归档-2026-08-16.md](自举进度-归档-2026-08-16.md) | **8月16日** dest-in-rbx 族／匿名 match／裸 enum／SIMD dest／nest 21–29（**124** 行） |
| [自举进度-归档-2026-08-15.md](自举进度-归档-2026-08-15.md) | **8月15日** 过夜 Darwin 刀 + 升钉 `f7424ae47` + dest／SIMD／METHOD／nest 至 DEREF dest（**66** 行） |
| [自举进度-归档-2026-08-14.md](自举进度-归档-2026-08-14.md) | **8月14日** dest-SLICE／import 模块 const／INDEX-on-METHOD／f32／Vec4f／宽 lit／STRUCT_LIT／http+queue companions（**79** 行） |
| [自举进度-归档-2026-08-13.md](自举进度-归档-2026-08-13.md) | **8月13日** METHOD(49) 物化／try_inline／>16B memcpy + SIMD leftover + soft residual 梯（**66** 行） |
| [自举进度-归档-2026-08-12.md](自举进度-归档-2026-08-12.md) | **8月12日** Stage12.0.5 prefer 默认 pure-asm · PC FORBID／ALLOW · mega ban／RCA · opaque WEAK · catalog cache · L4 墙钟 · **47** 行 |
| [自举进度-归档-2026-08-11.md](自举进度-归档-2026-08-11.md) | **8月11日** Stage12.0.5 pure-asm residual 闭口 + **升钉 `e364f4a37`**（pure_asm 13/13 · L2-asm／tcpu／ldpc · labi residual · full12 双端地图 · **41** 行 + 10 日余 **4**） |
| [自举进度-归档-2026-08-09.md](自举进度-归档-2026-08-09.md) | **8月9日** runtime thin dual leave → M4 五域关 pin → Stage 8 Batch 3 30/30 → PREFER_X_O 双端 8/8（**28** 行 · 钉盘 `36363b90f`） |
| [自举进度-归档-2026-08-08.md](自举进度-归档-2026-08-08.md) | **8月8日** B′ pipeline_x mega 首刀 bootstrap_glue→ast_pool 各域→elf/wpo→8.3 结构地板→gen dual leave→typeck.x 全量 `-E` T001（**wave262–314** · **56** 行） |
| [自举进度-归档-2026-08-07.md](自举进度-归档-2026-08-07.md) | **8月7日** B′ pure leave wave200–261 + 升钉 `36363b90f` + glue_statics（**64** 行） |
| [自举进度-归档-2026-08-06.md](自举进度-归档-2026-08-06.md) | **8月6日** B′ pure leave field_access→wave199 CALL-target resolve（**50** 行） |
| [自举进度-归档-2026-08-05.md](自举进度-归档-2026-08-05.md) | **8月5日** L4 冷恢复 + 8.3.3 R2 + pure-asm match + B′ leave 至 binop（**74** 行） |
| [自举进度-归档-2026-08-04.md](自举进度-归档-2026-08-04.md) | **8月4日 wave1241–1291**（BC ast_pool 16 波域分解 + glue 壳 thin + check 收口 + 升钉 `db809e00f`） |
| [自举进度-归档-2026-08-03.md](自举进度-归档-2026-08-03.md) | **8月3日 wave1227–1240**（BC glue 叶收口 + check 工具链启动 · 15 行） |
| [自举进度-归档-2026-08-02.md](自举进度-归档-2026-08-02.md) | **8月2日 wave1130–1220**（BC glue 续迁 + typeck 跨模块字段根修 · 43 行） |
| [自举进度-归档-2026-08-01.md](自举进度-归档-2026-08-01.md) | **8月1日 wave964–1111-1129**（BC 8.3.1 G.7 fold 50+ 叶 + Stage 8 退役 13/30） |
| [自举进度-归档-2026-07-31.md](自举进度-归档-2026-07-31.md) | **31 日全日 wave895–963**（MG 真删 + post-delete + 开 BC） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单；per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`e8176cbe5`** | 2026-08-23 升钉；双端 L4 真冷 + 129（mac 85m0s · Ubuntu rebuild 7m27s＋heat＋bstrict 4m10s）；含 check-gate al06 入口 typeck；前 `f7424ae47` |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自本波 L4 `xlang_asm` 刷新（darwin 4.7MiB · linux 4.6MiB） |
| residual tip（BC） | wave339 | M4 **5/5** 全闭 · Stage 8 Batch 3 **30/30 FULLY CLOSED** · 7.4.3 PREFER_X_O **双端 8/8** · pure-ld 58/60 objs · **双端 L4** @ **`e8176cbe5`** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁** · sat `try-r1` 已走 `ensure_labi_prefer_one`（产品仍 mega） |
| mac 钉盘 L4 | ✅ L4 + bstrict **129** · 85m0s | remaining_o=0 · `./xbuild l4` · `/tmp/xlang_l4_Darwin_e8176cbe5.log` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 remaining_o=0＋heat try_inline＋矩阵 5／5＋bstrict **129** · rebuild 7m27s＋bstrict 4m10s | `ubuntu-server` · `/tmp/xlang_l4_Linux_e8176cbe5.log` · `/tmp/xlang_bstrict_Linux_e8176cbe5.log` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | G.7 单权威 `product_l2_matrix.sh`（wave299）；rv42／opt102／hello0／si0／f32 一次跑全 |
| 前钉 | `f7424ae47` · `e364f4a37` · `d79a368b2` · … | 过夜 Darwin 刀 → Stage12.0.5 residual → formal_mod → … |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 18 号结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ **双端 tip SHA256 STRICT match**（Ubuntu `1ac895e51` · Darwin postlink＋HASH_STRICT 默认=1） | tip Ubuntu：同配方＋`atoi_stub_tu.c` · SHA256=`0ae06666…`／5760344 · `/tmp/stage2_atoi_stable_ubuntu.log` · tip Darwin：双遍 postlink OK＋STRICT SHA256=`42ffac6e…`／5094888 · `/tmp/stage2_darwin_postlink_hash.log` · 钉盘历史 `/tmp/xlang_stage2_ubuntu_9e60e1290.log` + `/tmp/xlang_stage2_macos_fix2.log` |
| **R2 / Cap** | wave710 升钉后按需 | 明细见 29 归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1**（已执行） |
| `TREE_ARMED` | **1** |
| `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1**（wave941 物理删除） |
| `PHYS_DEL_WINDOWS_GATE_STATUS` | `reproven_green` @ `71dd60a12`（wave922 · wave940 一键冷启动） |
| preflight blockers | **全清零**（wave937–939） |
| 真删条件 | ✅ 已完成 @ wave941/942 · 钉盘 **`77b334842`**（后升至 **`db809e00f`**） |
| post-delete residual | **✅ 已收**（wave944–964） |

***

## 3. 当前前排（≤3 条）

1. **日常 L2** — 钉盘 **`e8176cbe5` 已升且冻结**；谈自举／SHARED 大面才再 L4。23 日 unique UNDEF／labi／stubs／al06／CG002／process_merge **勿重开**。
2. **邻域禁区** — **禁**空 struct 糊绿／mega prefer／默认 assemble parser.x。dyn SSE extras **禁**放 ta==1。**禁**用 `mov_rax_to_rbx` 当 wrapper 暂存（兼写 x1）。**禁** `pipeline_module_num_funcs` 判空糊绿。**禁** shell `#define Result_*` 双权威。**禁**把 `run` 收成 IDENT 糊绿。**禁**改 enc_store sz>=16 全局顶 leftover fsz=8。**禁**伪造 mmap backend 1／2。**禁** `extern uint8_t *malloc`。**禁**改 al06 期望／禁把 check 整棵 std／core dep typeck。**禁** `-w` 糊 Darwin minos／**禁**把 `macho.x` minos 抬到 26.0。
3. **PC 底盘／下一刀** — prefer 族 pure-asm 默认 ✅ · … · **f04-net soft→硬绿 ✅** · **f04-crypto soft→硬绿 ✅** · **f05 soft→硬绿 ✅** · **f04-compress soft→硬绿 ✅** · **f02 soft→硬绿 ✅** · **f06 soft→硬绿 ✅** · **f07 soft→硬绿 ✅** · **f03-core soft→硬绿 ✅** · **f03-fs soft→硬绿 ✅** · **f03-heap-libc soft→硬绿 ✅** · **f03-heap-ops soft→硬绿 ✅** · **f03-io soft→硬绿 ✅** · **e06／c07／c08／b20／d01／a12 soft→硬绿 ✅** · **d02／d03／d04 soft→硬绿 ✅** \
 **DOD-CL-S1／S2 soft→硬绿 ✅** · **BOOT-010 force-stub soft→硬绿 ✅** · **BOOT-019 stage2-dogfood soft→硬绿 ✅** · **BOOT-015 semantic-smoke soft→硬绿 ✅** · **BOOT-014 std-link-contract soft→硬绿 ✅** · **C-07 frontend-parity soft auto-make →硬绿 ✅** · **io／net／queue soft auto-make →硬绿 ✅** · **STD-077／087／086／036／072 cli／cache／config／csv-row／bytes soft auto-make →硬绿 ✅** · **STD-080／081 option-result soft auto-make →硬绿 ✅** · **STD-155／074／076 bytes-arena／datetime／url soft auto-make →硬绿 ✅** · **STD-079／129／112 security／set-ops／heap-allocator soft auto-make →硬绿 ✅** · **STD-015／017／011 set-extend／heap-trace／error-unify soft auto-make →硬绿 ✅** · **STD-130／150／060 random-rng／sort-key／sort-stable soft auto-make →硬绿 ✅** · **STD-005／025／019 time／env-iter／fmt-multi soft auto-make →硬绿 ✅** · **STD-020／158／071 error-map／error-semantics／context soft auto-make →硬绿 ✅** · **STD-156／132／008 context-cookbook／env-platform／json soft auto-make →硬绿 ✅** · **STD-035／json-object-array／STD-053 json-serialize／log-multi soft auto-make →硬绿 ✅** · **STD-109／140／096／055 base64-stream／path-extreme／dynlib-last-error／ffi-cstring soft auto-make →硬绿 ✅** · **STD-119／135／098／050 config-yaml／datetime-timezone／channel-select／crypto-sha512-hmac soft fallthrough →硬绿 ✅** · **EXC-005 cli-lsp soft→硬绿 ✅** · **STBL-004 import-std-layout soft→硬绿 ✅** · **EXC-002 panic/abort soft→硬绿 ✅** · **EXC-006 error-recovery soft→硬绿 ✅** · **EXC-003 error-code-layer soft→硬绿 ✅** · **EXC-004 error-chain soft→硬绿 ✅** · **BOOT-029 std-sys soft→硬绿 ✅** · **STD-134 url-ipv6-host soft→硬绿 ✅** ·
 **STD-123 fs-dirmeta soft→硬绿 ✅** · **STD-049 aes-gcm soft→硬绿 ✅** · **STD-139 sqlite-stub soft→硬绿 ✅** · **STD-145 test-runner soft→硬绿 ✅** · **STD-111 sync-lock-diag soft→硬绿 ✅** · **STD-160 string-unicode soft→硬绿 ✅** · **STD-133 time-bench-timer soft→硬绿 ✅** · **STD-137 time-format-timezone soft→硬绿 ✅** · **STD-004 async-api soft→硬绿 ✅** · **db-kv-arrow soft→硬绿 ✅** · **STD-048 queue-concurrent soft→硬绿 ✅** · **STD-008 json soft→硬绿 ✅** · **STD-009 http soft→硬绿 ✅** · **STD-HTTP-H2 http-h2 soft→硬绿 ✅** · **STD-138 xplat-deep soft→硬绿 ✅** · **STD-012 examples soft→硬绿 ✅** · **STD-003 fs-crossplatform soft→硬绿 ✅** · **STD-152 tar-extended soft→硬绿 ✅** · **STD-021／022 path-fs-windows soft→硬绿 ✅** · **STD-061＋STD-153 simd-prod／autovec soft→硬绿 ✅** · **simd intrinsic＋STD-047 soft→硬绿 ✅** · **STD-027 dynlib-windows soft→硬绿 ✅** · **STD-142 process-xplat soft→硬绿 ✅** · **STD-092 net-context soft→硬绿 ✅** · **STD-106 log-rotate-async soft→硬绿 ✅** · **STD-136 datetime-iana soft→硬绿 ✅** · **STD-132 env-platform-encoding soft→硬绿 ✅** · **STD-002／001 net-api＋io-api soft→硬绿 ✅** · **STD-006 crypto soft→硬绿 ✅** · **STD-007 compress soft→硬绿 ✅** · **STD-026 io-fallback soft→硬绿 ✅** · **STD-016 strview-zc4 soft→硬绿 ✅** · **STD-028 runtime-panic-hook soft→硬绿 ✅** · **STD-005 std-time soft→硬绿 ✅** · **json-object-array soft→硬绿 ✅** · **STD-130 random-rng soft→硬绿 ✅** · **STD-046 atomic-ordering soft→硬绿 ✅** · **STD-052 backtrace-symbolicate soft→硬绿 ✅** · **STD-091 io-context soft→硬绿 ✅** · **STD-043 thread-pool soft→硬绿 ✅** · **STD-029 net-dns soft→硬绿 ✅** · **STD-045 sync-rwlock-condvar soft→硬绿 ✅** · **STD-038 tar-ustar soft→硬绿 ✅** · **STD-050 crypto sha512-hmac soft→硬绿 ✅** · **STD-034 http-https soft→硬绿 ✅** · **STD-033 http-chunked soft→硬绿 ✅** · **STD-032 http-methods soft→硬绿 ✅** · **STD-098 channel-select soft→硬绿 ✅** · **STD-135 datetime-timezone soft→硬绿 ✅** · **STD-036 csv-row soft→硬绿 ✅** · **STD-035 json-serialize soft→硬绿 ✅** · **STD-080/081 std.option／result soft→硬绿 ✅** · **STD-079 std.security soft→硬绿 ✅** · **STD-076 std.url soft→硬绿 ✅** · **STD-087 std.cache soft→硬绿 ✅** · **STD-119 config-yaml soft→硬绿 ✅** · **STD-086 std-config soft→硬绿 ✅** · **STD-053 log-multi-sink soft→硬绿 ✅** · **STD-055 ffi-cstring soft→硬绿 ✅** · **STD-140 path-extreme soft→硬绿 ✅** · **STD-096 dynlib-last-error soft→硬绿 ✅** · **STD-019 fmt-multi soft→硬绿 ✅** · **STD-109 base64-stream soft→硬绿 ✅** · **STD-011 error-unify soft→硬绿 ✅** · **STD-077 std-cli soft→硬绿 ✅** · **STD-074 std-datetime soft→硬绿 ✅** · **STD-072 std-bytes soft→硬绿 ✅** · **STD-073／110／139 codec＋stream＋buffer-reuse soft→硬绿 ✅** · **STD-144 mem-safe soft→硬绿 ✅** · **STD-060 sort-stable-cmp soft→硬绿 ✅** · **STD-150 sort-key-cmp soft→硬绿 ✅** · **STD-155 bytes-arena soft→硬绿 ✅** · **STD-071 context soft→硬绿 ✅** · **STD-156 context-cookbook soft→硬绿 ✅** · **STD-129 set-ops soft→硬绿 ✅** · **STD-112 heap-allocator soft→硬绿 ✅** · **STD-015 set-extend soft→硬绿 ✅** · **STD-158 error_semantics soft→硬绿 ✅** · **STD-020 error_map soft→硬绿 ✅** · **STD-017 heap_trace soft→硬绿 ✅** · **STD-025 env_iter soft→硬绿 ✅** · **ArrowColumnMem 32B pack／db_kv_arrow ✅** · **TYPE_SLICE i32[] schema A@ ✅** · **host-C STRUCT_LIT mono 标签统一 ✅** · **u8_slc fat-pointer ABI ✅** · **print_any JSON println ✅** · **view_lifecycle discard `_` ✅** · **zc_arena_concat T001 ✅** · **net_tcp_pool 真 create ✅** · **async_net_fs unique UNDEF ✅** · **WPO_DUMP_CALLGRAPH ✅** · **L6 unused-hint ✅** · **CORE／lang soft SKIP ✅** · **e-soft／c0x／f-phase soft→硬绿 ✅** · **f08／f10／f11／f12／inventory soft→硬绿 ✅** · **f-de-c 语法孤儿＋string／base64／random／time／process／encoding soft→硬绿 ✅** · **f-path／uuid／sort／math soft→硬绿 ✅** · **f-cli／env／url／hash soft→硬绿 ✅** · **f-log／queue／tar／sync soft→硬绿 ✅** · **f-thread／async／atomic／cache soft→硬绿 ✅** · **f-channel／config／context／csv soft→硬绿 ✅** · **f-datetime／dynlib／elf／ffi soft→硬绿 ✅** · **f-http／json／regex／runtime soft→硬绿 ✅** · **f-schema／security／simd／socketio soft→硬绿 ✅** · **f-task／test／trace／unicode／backtrace soft→硬绿 ✅** · **f-crypto v1 soft→硬绿 ✅** · **f-cache／queue／tar／config v2 soft→硬绿 ✅** · **f-context／datetime／url／dynlib v2 soft→硬绿 ✅** · **f-elf／hash／json／regex v2 soft→硬绿 ✅** · **f-schema／socketio／task／unicode v2 soft→硬绿 ✅** · **f-async-future／backtrace／test／trace v2 soft→硬绿 ✅** · **prefer-c／archaeology（zero-c-track＋closure-e-extern）soft→硬绿 ✅** · **archaeology e-extern family（c04／pipeline／lexer／parser／import）soft→硬绿 ✅** · **d02／d03／d04 soft→硬绿 ✅**。下一软刀＝soft SKIP 邻域续扫（若仍有 soft die→exit0；**d02／d03／d04／e06／c07／c08／b20／d01／a12／e-extern／c04／f／second-pass／B-01／B-02／B-03 soft FAIL 池空**；**zc 若仍绑 host-c 链则后置**；Stage2 SHA256 topology fork 16B 产品残；STD uuid／task／mem-safe／trace 等 UNDEF 跳；linux mmap smoke UNDEF 观测残；compress-stream／unified-stream 观测残；sqlite stub open／last_error 观测残；aes-gcm nist2 RUN≠0 后置；compress-brotli ld 红跳；test-executable／bench-fuzz 探针 ld UNDEF 跳；**encoding-extra＋sqlite-pool＋stmt-cache soft→硬绿**；**regex＋net-tls＋sqlite soft→硬绿**；跳 async-1m／language／future coop UNDEF）；仍跳过产品红／UNDEF（compress-stream／unified-stream ld／async-language／async-future／process-pipe XT001／mem-boundary UNDEF／encoding-hex-b64／xxhash／unicode-nfc／map-vec／channel-unbounded／http-reqresp／http-server-pool／http-h2／http-context／async-context／mac／mac512／codec／atomic-widen／csv-stream／metrics／math-special／elf-write／elf-parse／ffi-struct／json-typed-decode／Darwin json `needs_copy`／queue-sync／coop_pingpong／schema／socketio／chacha／ed25519 等）；asm ld `--export-dynamic`（backtrace 具名；非软）；by-value `Set_i32`+MEMORY（非软）；Darwin heat（禁 mega）。pipeline_abi mega **仍硬禁**。nest 64 **冻帽**。ALLOW ensure 再瘦 **已空**。

***

## 4. 与归档的关系

- **8月27日归档**：[自举进度-归档-2026-08-27.md](自举进度-归档-2026-08-27.md) — soft FAIL／prefer-c／SKIP→OK 多票硬绿（f-*／STD／B／sys／perf／lang／zc2／json＋log＋math 等）· **60** 行 · 钉盘仍 **`e8176cbe5`**。
- **8月26日归档**：[自举进度-归档-2026-08-26.md](自举进度-归档-2026-08-26.md) — STD soft→硬绿梯 ＋ f-phase／f04／f08／f-de-c 等 soft→硬绿 · **82** 行 · 钉盘仍 **`e8176cbe5`**。
- **8月25日归档**：[自举进度-归档-2026-08-25.md](自举进度-归档-2026-08-25.md) — soft 梯／EXC soft→硬绿／STD soft→硬绿多票 · **65** 行 · 钉盘仍 **`e8176cbe5`**。
- **8月24日归档**：[自举进度-归档-2026-08-24.md](自举进度-归档-2026-08-24.md) — 日切重写起点 · Stage2／WPO／零 make／假权威诚实 DOC 族 · b04／std-config c／NL-04／generic_layout／vscode 0.2／import async · **64** 行 · 钉盘仍 **`e8176cbe5`**。
- **8月23日归档**：[自举进度-归档-2026-08-23.md](自举进度-归档-2026-08-23.md) — **升钉 `e8176cbe5`** ＋ check-gate al06 ＋ labi／stubs／formal_mod unique UNDEF 族 ＋ Darwin CG002／process_merge／minos／g05 ＋ sat L4／fmt／modlet · **44** 行 · 钉盘 **`e8176cbe5`**。
- **8月22日及前**：见各日归档链接（§1 文档地图）。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `e8176cbe5`**（2026-08-23 升钉 · 前 `f7424ae47` · 再前 `e364f4a37`）；residual tip **wave339**（present **0** · 日常 L2 可续）。
- 日切后主文档只保留 KPI + 前排 + **8 月 28 日** §6（27 日及前见归档）；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
| **macOS arm64** | **钉盘 L4+bstrict 129 ✅** @ **`e8176cbe5`** · 本波 L2 **5／5** · link run=2 **obs=1**／cli run=2 **obs=1**／panic run=7 **obs=1** · mega **-E 仍硬禁** · 产品只走 `./xbuild` |
| **Ubuntu x86_64（金标）** | **回家局域网复验 tip soft→硬绿 ✅** @ **`e19c5c34a`** · L2 **5／5** · link run=2 **obs=1**／cli／panic obs=0＋boot-015／019 积压绿 · 钉盘仍 **`e8176cbe5`** · **SSH＝`ubuntu-server`**（外地曾用 `ubuntu-remote-server`／frp） |

| **Windows MSYS2** | wave940 一键冷启动绿 · STATUS=`reproven_green` · Makefile 已物理删除 · 本波未升 Windows pin（仓内无 windows pin 蛋） |

***


***

## 6. 变更记录（倒序 · **8 月 28 日**；27 日见 [归档](自举进度-归档-2026-08-27.md) · 26 日见 [归档](自举进度-归档-2026-08-26.md)）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-08-28 | **docs(selfhost)** **tip 对齐 STD-048 queue-concurrent 残 fallthrough 双端文档 SHA** · tip **`f054b276f`** | **文档** · **不升钉** · **日常 L2**。代码 **`57bf7ee45`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-048 queue-concurrent 残 fallthrough soft→硬绿** · tip **`57bf7ee45`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝闸 `for cand in XLANG asm …` 显式坏 XLANG 回落 asm＋`xlang_compiler_make -q` 或 make＋bootstrap-link＋`ensure_std_c_o`＋C contention auto-make＋报告 `check=`／`main=`／`sync=`／`c=`／`skip=`＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／soft auto-make／XLANG fallthrough／soft ensure 重建／extra CLI `.o`／C contention auto-make；显式坏 XLANG／缺 native 硬 die；check＋sync_queue_roundtrip（queue-sync UNDEF）＋host-C archaeology＝obs（现成 `.o` only）；`main.x` 产品 `-o` 硬绿；archive `## 4. Gate`；报告 `run=`／`obs=`／`skip=`。证：Darwin run=1 **obs=2**＋neg RC=1＋L2 **5／5**；Ubuntu run=1 **obs=1**＋neg RC=1＋L2 **5／5**。跳过 ensure 族／zc／brotli／hash＊／unicode-norm／sync-rwlock-condvar。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 STD-152 tar-extended 残 fallthrough 双端文档 SHA** · tip **`46fe69262`** | **文档** · **不升钉** · **日常 L2**。代码 **`22ad0199c`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-152 tar-extended 残 fallthrough soft→硬绿** · tip **`22ad0199c`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝闸 `for cand in XLANG asm …` 显式坏 XLANG 回落 asm＋`xlang_compiler_make -q` 或 make＋bootstrap-link＋`ensure_std_c_o`＋extra CLI `.o`＋报告 `check=`／`c=`／`x=`／`skip=`＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／soft auto-make／XLANG fallthrough／soft ensure 重建／extra CLI `.o`；显式坏 XLANG／缺 native 硬 die；check＋host-C archaeology＝obs（现成 `.o` only）；`long_path_dir.x` 产品 `-o` 硬绿；archive `## 4. Gate`；报告 `run=`／`obs=`／`skip=`。证：Darwin run=1 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu run=1 **obs=0**＋neg RC=1＋L2 **5／5**。跳过 ensure 族／zc／brotli／hash＊／unicode-norm／queue-concurrent。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 STD-046 atomic-ordering 残 fallthrough 双端文档 SHA** · tip **`330d040b8`** | **文档** · **不升钉** · **日常 L2**。代码 **`0a99e5a24`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-046 atomic-ordering 残 fallthrough soft→硬绿** · tip **`0a99e5a24`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝闸 `for cand in XLANG asm …` 显式坏 XLANG 回落 asm＋`xlang_compiler_make -q` 或 make＋bootstrap-link＋`ensure_std_c_o`＋报告 `check=`／`fence=`／`main=`＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／soft auto-make／XLANG fallthrough／soft ensure 重建；显式坏 XLANG／缺 native 硬 die；check＋host-C archaeology＝obs（现成 `.o` only）；`ordering_fence.x`＋`main.x` 产品 `-o` 硬绿；archive `## 5. Gate`；报告 `run=`／`obs=`／`skip=`。证：Darwin run=2 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu run=2 **obs=0**＋neg RC=1＋L2 **5／5**。跳过 ensure 族／zc／brotli／hash＊／unicode-norm／tar-extended。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 STD-038 tar-ustar 残 fallthrough 双端文档 SHA** · tip **`fd20f9fd0`** | **文档** · **不升钉** · **日常 L2**。代码 **`0135dd8f9`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-038 tar-ustar 残 fallthrough soft→硬绿** · tip **`0135dd8f9`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝闸 `for cand in XLANG asm …` 显式坏 XLANG 回落 asm＋`xlang_compiler_make -q` 或 make＋bootstrap-link＋`ensure_std_c_o`＋报告 `check=`／`rt=`／`main=`＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／soft auto-make／XLANG fallthrough／soft ensure 重建；显式坏 XLANG／缺 native 硬 die；check＋host-C archaeology＝obs（现成 `.o` only）；`ustar_roundtrip.x`＋`main.x` 产品 `-o` 硬绿；archive `## 5. Gate`；报告 `run=`／`obs=`／`skip=`。证：Darwin run=2 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu run=2 **obs=0**＋neg RC=1＋L2 **5／5**。跳过 ensure 族／zc／brotli／hash＊／unicode-norm／atomic-ordering。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 STD-043 thread-pool 残 fallthrough 双端文档 SHA** · tip **`c5f07e821`** | **文档** · **不升钉** · **日常 L2**。代码 **`707d7a357`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-043 thread-pool 残 fallthrough soft→硬绿** · tip **`707d7a357`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝闸 `for cand in XLANG asm …` 显式坏 XLANG 回落 asm＋`xlang_compiler_make -q` 或 make＋bootstrap-link＋`ensure_std_c_o`＋报告 `check=`／`pool=`／`name=`／`main=`＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／soft auto-make／XLANG fallthrough／soft ensure 重建；显式坏 XLANG／缺 native 硬 die；check＋host-C archaeology＝obs（现成 `.o` only）；`pool_roundtrip.x`＋`main.x` 产品 `-o` 硬绿；archive `## 5. Gate`；报告 `run=`／`obs=`／`skip=`。证：Darwin run=2 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu run=2 **obs=0**＋neg RC=1＋L2 **5／5**。跳过 ensure 族／zc／brotli／hash＊／unicode-norm／tar-ustar。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 STD-091 io-context 残 fallthrough 双端文档 SHA** · tip **`2d3cd9b1a`** | **文档** · **不升钉** · **日常 L2**。代码 **`b11c82fcf`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-091 io-context 残 fallthrough soft→硬绿** · tip **`b11c82fcf`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝闸 `for cand in XLANG asm …` 显式坏 XLANG 回落 asm＋`xlang_compiler_make -q` 或 make＋bootstrap-link＋`ensure_std_c_o`＋CLI extra `.o`＋报告 `check=`＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／soft auto-make／XLANG fallthrough／soft ensure 重建／extra CLI `.o`；显式坏 XLANG／缺 native 硬 die；check＋glue archaeology＝obs（现成 `.o` only）；`context_read_write.x` 产品 `-o` 硬绿；无 DOC／TSV（镜像 STD-092）；报告 `run=`／`obs=`／`skip=`。证：Darwin run=1 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu run=1 **obs=0**＋neg RC=1＋L2 **5／5**。跳过 ensure 族／zc／brotli／hash＊／unicode-norm／thread-pool／tar-ustar。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 STD-106 log-rotate-async 残 fallthrough 双端文档 SHA** · tip **`68fc3e856`** | **文档** · **不升钉** · **日常 L2**。代码 **`097ae3f40`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-106 log-rotate-async 残 fallthrough soft→硬绿** · tip **`097ae3f40`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝闸 `for cand in XLANG asm …` 显式坏 XLANG 回落 asm＋`xlang_compiler_make -q` 或 make＋bootstrap-link＋`ensure_std_c_o`＋报告 `check=`＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／soft auto-make／XLANG fallthrough／soft ensure 重建；显式坏 XLANG／缺 native 硬 die；check＋host-C＝obs（现成 `.o` only）；`rotate_async.x` 产品 `-o` 硬绿；archive `## 5. Gate`；报告 `run=`／`obs=`／`skip=`。证：Darwin run=1 **obs=2**＋neg RC=1＋L2 **5／5**；Ubuntu run=1 **obs=1**＋neg RC=1＋L2 **5／5**。跳过 ensure 族／zc／brotli／hash＊／unicode-norm／io-context。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 STD-049 aes-gcm 残 auto-make 双端文档 SHA** · tip **`06dd23ace`** | **文档** · **不升钉** · **日常 L2**。代码 **`3ad07fa1b`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-049 aes-gcm 残 auto-make soft→硬绿** · tip **`3ad07fa1b`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝闸仍调 `xlang_compiler_make -q` 或 make＋`std_crypto_resolve_shu` 显式坏 XLANG 回落 asm＋bootstrap-link＋报告 `check=`／`main=`／`nist2=`＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／soft auto-make／XLANG fallthrough；显式坏 XLANG／缺 native 硬 die；check＋nist2 RUN≠0＝obs；`main.x` 产品 `-o` 硬绿；archive `## 5. Gate`；报告 `run=`／`obs=`／`skip=`。证：Darwin run=1 **obs=2**＋neg RC=1＋L2 **5／5**；Ubuntu run=1 **obs=1**＋neg RC=1＋L2 **5／5**。跳过 ensure 族／zc／brotli／hash＊／unicode-norm／log-rotate-async。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 STD-007 compress 残双端文档 SHA** · tip **`935dea807`** | **文档** · **不升钉** · **日常 L2**。代码 **`9d920ec3b`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-007 compress 残 try_libs／auto-make soft→硬绿** · tip **`9d920ec3b`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝闸仍调 `std_compress_try_libs`（恒 return 0）＋soft `xlang_compiler_make -q` 或 make＋显式坏 XLANG 回落 asm＋报告 `check=`／`gzip=`／`zstd=`／`legacy=`＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／try_libs／soft auto-make／XLANG fallthrough；显式坏 XLANG／缺 native 硬 die；check＋tip 产品 UNDEF＝obs；archive `## Gate`；报告 `run=`／`obs=`／`skip=`。证：Darwin run=3 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu run=3 **obs=0**＋neg RC=1＋L2 **5／5**（闸 wall ~7s）。跳过 ensure 族／zc／brotli／hash＊／unicode-norm。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 STD-037／114 unicode-nfc＋grapheme-case 双端文档 SHA** · tip **`c62d49abb`** | **文档** · **不升钉** · **日常 L2**。代码 **`d5a2c1dbf`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-037／114 unicode-nfc＋grapheme-case soft→硬绿** · tip **`d5a2c1dbf`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝两闸 prefer-c（xlang-c 先、永不 asm）＋soft SKIP→OK（无 native／c-smoke 链失败仍 OK）＋soft `ensure_std_c_o`／soft auto-make＋硬 check＋`nfc=`／`main=`／`c=`／`x=`＝假权威；nfc 闸 `UNI_X` 双赋值盖掉 `mod.x`。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft ensure／soft auto-make；显式坏 XLANG／缺 native 硬 die；host-C 无 dedicated NFC harness／grapheme 仅预编 `unicode.o`＝obs；check＋tip product UNDEF／exit≠0＝obs；archive `## Gate`；报告 `run=`／`obs=`／`skip=`。拒开 STD-082（`nfd_buf` 等 API 面缺失）。证：Darwin nfc run=1 **obs=2**／grapheme run=0 **obs=3**＋neg RC=1＋L2 **5／5**；Ubuntu nfc run=1 **obs=1**／grapheme run=0 **obs=2**＋neg RC=1＋L2 **5／5**。跳过 ensure 族／zc／brotli／hash＊／compress 残／unicode-norm。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 STD-HTTP-H2 http-h2 双端文档 SHA** · tip **`aef9e149a`** | **文档** · **不升钉** · **日常 L2**。代码 **`6b133de79`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-HTTP-H2 http-h2 soft→硬绿** · tip **`6b133de79`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝闸 prefer-c／soft SKIP→OK（无 native／Docker 仍 OK）＋soft `ensure_std_c_o`／soft auto-make＋硬 check＋`wire=`／`client=`／`network=`／`flow=`＝假权威；TSV／DOC 化石 `hpack.inc.c`／`tests/http/wire.x`。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft ensure／soft auto-make；显式坏 XLANG／缺 native 硬 die；host-C 无 dedicated harness（拒 ensure）；check＋tip product UNDEF＝obs；TSV／DOC 锚对齐活 `http2_*.inc`／`http2_*.x`；archive `## Gate`；报告 `run=`／`obs=`／`skip=`；父 STD-009 仅文件存在。证：Darwin 闸 run=0 **obs=26**＋neg RC=1＋L2 **5／5**；Ubuntu 闸 run=0 **obs=25**＋neg RC=1＋L2 **5／5**。跳过 ensure 族／zc／brotli／hash＊／unicode-norm。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 STD-066 sqlite-query-rows 双端文档 SHA** · tip **`2a27a4468`** | **文档** · **不升钉** · **日常 L2**。代码 **`247b24b06`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-066 sqlite-query-rows soft→硬绿** · tip **`247b24b06`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝闸 prefer-c／soft SKIP→OK（无 native／typeck-fail／无 libsqlite3 仍 OK）＋soft `ensure_std_c_o`／soft `std_sqlite_build_o`／soft auto-make＋硬 check＋`rows_c=`／`rows_x=`＝假权威；TSV／DOC 化石导出名 `query_rows`。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft ensure／soft build_o；显式坏 XLANG／缺 native 硬 die；host-C 仅预编 `.o`＝obs；check＋tip product SEGV＝obs；API 锚对齐 `rows`；archive `## Gate`；报告 `run=`／`obs=`／`skip=`；父 STD-065 仅文件存在。证：Darwin 闸 run=0 **obs=3**＋neg RC=1＋L2 **5／5**；Ubuntu 闸 run=0 **obs=2**＋neg RC=1＋L2 **5／5**。跳过 ensure 族／zc／brotli／hash＊／http-h2／unicode-norm。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 STD-067／068／069 next-row＋col_text＋col_blob 双端文档 SHA** · tip **`fb6d5ff3e`** | **文档** · **不升钉** · **日常 L2**。代码 **`0ccda862a`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-067／068／069 next-row＋col_text＋col_blob soft→硬绿** · tip **`0ccda862a`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝三闸 prefer-c／soft SKIP→OK（无 native／typeck-fail／无 libsqlite3 仍 OK）＋soft `ensure_std_c_o`／soft `std_sqlite_build_o`／soft auto-make＋硬 check＋`cursor_c=`／`text_c=`／`blob_c=`＝假权威；TSV／DOC 化石 `query_begin`／`row_col_i32`／`query_end`／`row_col_text`／`row_col_blob`。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft ensure／soft build_o；显式坏 XLANG／缺 native 硬 die；host-C 仅预编 `.o`＝obs；check＋tip product SEGV＝obs；API 锚对齐 `begin`／`next_row`／`col`／`end`／`col_text`／`col_blob`；archive `## Gate`；报告 `run=`／`obs=`／`skip=`；父 STD-066 仅文件存在。证：Darwin 三闸均 run=0 **obs=3**＋neg RC=1＋L2 **5／5**；Ubuntu 三闸均 run=0 **obs=2**＋neg RC=1＋L2 **5／5**。跳过 ensure 族／zc／brotli／hash＊／http-h2／unicode-norm。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 channel-unbounded／ffi-struct-callback／sqlite-blob-stream 双端文档 SHA** · tip **`38b4cb8d9`** | **文档** · **不升钉** · **日常 L2**。代码 **`38b4cb8d9`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-044／151／137 channel-unbounded＋ffi-struct-callback＋sqlite-blob-stream soft→硬绿** · tip **`38b4cb8d9`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝三闸 prefer-c／soft SKIP→OK（无 native／typeck-fail／无 libsqlite3 仍 OK）＋soft `ensure_std_c_o`／soft `std_sqlite_build_o`／soft auto-make＋硬 check＋`unbounded=`／`c=`／`stream_c=`＝假权威；blob DOC 化石 `row_col_blob_len`。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft ensure／soft build_o；显式坏 XLANG／缺 native 硬 die；host-C 仅预编 `.o`＝obs；check＋tip product UNDEF／SEGV＝obs；blob API 锚对齐 `col_blob_len`／`col_blob_read`；archive `## Gate`；报告 `run=`／`obs=`／`skip=`；拒开 STD-069／unicode-normalization（API 面缺失）刀。证：Darwin channel run=1 **obs=2**／ffi run=1 **obs=2**／blob run=0 **obs=3**＋neg RC=1＋L2 **5／5**；Ubuntu channel run=1 **obs=1**／ffi run=1 **obs=1**／blob run=0 **obs=2**＋neg RC=1＋L2 **5／5**。跳过 ensure 族／zc／brotli／hash＊／http-h2／unicode-norm。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 compress-stream／unified／socketio 双端文档 SHA** · tip **`5e0344d4d`** | **文档** · **不升钉** · **日常 L2**。代码 **`540e9dd46`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-039／122／SOCKETIO compress-stream＋unified＋socketio soft→硬绿** · tip **`540e9dd46`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝stream／unified soft SKIP→OK（无 native 仍 OK）＋soft `std_compress_try_libs`／auto-make＋硬 check＋`stream=`；socketio prefer-c＋soft `ensure_std_c_o`＋soft SKIP→OK＋`smoke=`＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft ensure／soft auto-make；显式坏 XLANG／缺 native 硬 die；host-C 仅预编 `socketio.o`＝obs；check＋tip product UNDEF＝obs；archive `## Gate`；报告 `run=`／`obs=`／`skip=`。证：Darwin stream／unified run=0 **obs=2**／sio run=0 **obs=16**＋neg RC=1＋L2 **5／5**；Ubuntu stream／unified run=0 **obs=1**／sio run=0 **obs=16**＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_compress_{stream,unified}.log`／`/tmp/ubuntu_socketio.log`／`/tmp/ubuntu_l2_sio_stream.log`）。跳过 ensure 族／zc／brotli／hash＊。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 sqlite-pool／stmt-cache／encoding-extra 双端文档 SHA** · tip **`5b49e8a3f`** | **文档** · **不升钉** · **日常 L2**。代码 **`bb2138a00`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-084／070／127 sqlite-pool＋stmt-cache＋encoding-extra soft prefer-c →硬绿** · tip **`bb2138a00`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝三闸 prefer-c／soft SKIP→OK（无 native／无 libsqlite3 仍 OK）＋soft `std_sqlite_build_o\|\|true`／soft `ensure_std_c_o`／soft auto-make＋硬 check＋`pool_c=`／`bind_c=`／`c=`／`x=`＝假权威；TSV／DOC 化石 `pool_open`／`stmt_bind_i32`。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft ensure／soft build_o；显式坏 XLANG／缺 native 硬 die；host-C 仅预编 `.o`＝obs；check＋tip product SEGV／UNDEF＝obs；TSV／DOC 锚对齐产品 `open`／`acquire`／`bind`；archive `## Gate`；报告 `run=`／`obs=`／`skip=`。证：Darwin 三闸均 run=0 **obs=3**＋neg RC=1＋L2 **5／5**；Ubuntu pool／stmt／encoding-extra 均 run=0 **obs=2**＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_std{084_pool,070_stmt,127_enc}.log`／`/tmp/ubuntu_l2_std084.log`）。跳过 socketio／compress-stream／ensure／zc／brotli／hash＊。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 sqlite／regex／net-tls 双端文档 SHA** · tip **`7992c3e96`** | **文档** · **不升钉** · **日常 L2**。代码 **`9105a5489`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-28 | **test(selfhost)** **STD-057／051／030 sqlite＋regex＋net-tls soft prefer-c →硬绿** · tip **`9105a5489`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝三闸 prefer-c（xlang-c 优先）＋soft SKIP→OK（无 native／link debt 仍 gate OK）＋soft `ensure_std_c_o`／soft `net-o-*`／soft auto-make＋`c_smoke=`／`x=`／`stub=`／`typeck=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK／prefer-c／soft ensure；显式坏 XLANG／缺 native 硬 die；host-C 仅预编 `.o`＝obs；check＋tip product（sqlite SEGV／regex missing `_main`／tls typeck）＝obs；报告 `run=`／`obs=`／`skip=`。证：Darwin sqlite run=0 **obs=4**／regex run=0 **obs=29**／tls run=0 **obs=5**＋neg RC=1＋L2 **5／5**；Ubuntu sqlite **obs=3**／regex **obs=29**／tls **obs=4 skip=1**＋neg RC=1＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 elf 五闸双端文档 SHA** · tip **`c9128d088`** | **文档** · **不升钉** · **日常 L2**。代码 **`ad90b9b3b`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **test(selfhost)** **STD-113／126／031 chacha＋ed25519＋net-ws soft prefer-c →硬绿** · tip **`8effad203`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝三闸 prefer-c（xlang-c 优先）＋soft SKIP→OK（无 native 仍 gate OK）＋soft `ensure_std_c_o`／soft `ensure_runtime_*_glue_o`／soft auto-make＋check 硬绑＋报告 `c=`／`x=`／`accept=`／`frame=`／`typeck=`＝假权威；zstd 缺链 soft SKIP；TSV 化石 const／`chacha20_poly1305.x`。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft ensure／soft auto-make；显式坏 XLANG／缺 native 硬 die；host-C 仅预编 `.o`＝obs；check＋tip product UNDEF＝obs；TSV 锚对齐产品（`KEY_LEN`／`SEED_LEN`＝function；chacha `_c`→`chacha20_aead.x`）；archive `## Gate`；报告 `run=`／`obs=`／`skip=`。证：Darwin 三闸均 run=0 **obs=3**＋neg RC=1＋L2 **5／5**；Ubuntu 三闸均 run=0 **obs=2**＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_chacha.log`／`/tmp/ubuntu_ed25519.log`／`/tmp/ubuntu_net_ws.log`／`/tmp/ubuntu_l2_chacha_ed_ws.log`）。跳过 ensure 族／zc／brotli／hash＊／net-tls／regex。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **test(selfhost)** **STD-121／058／063／064／103 elf-write＋parse＋deep＋phdr＋sym-rela soft prefer-c →硬绿** · tip **`ad90b9b3b`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝五闸 soft prefer-c／soft SKIP→OK（无 xlang-c 仍 manifest OK）＋soft `ensure_std_c_o`／soft `xlang_compiler_make…\|\|true`＋check 硬绑＋报告 `c=`／`c_smoke=`／`x=`／`phdr_c=`／`deep_c=`＝假权威；deep／phdr soft SKIP .x 失败。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft ensure／soft auto-make；显式坏 XLANG／缺 native 硬 die；host-C 仅预编 `std/elf/elf.o`＝obs（缺 fs_* 依赖不 soft 补链）；check＋tip product `std_elf_*` UNDEF＝obs；archive `## Gate`；TSV script 锚全路径；报告 `run=`／`obs=`／`skip=`。证：Darwin 五闸均 run=0 **obs=3**＋neg RC=1＋L2 **5／5**；Ubuntu 五闸均 run=0 **obs=2**＋neg RC=1＋L2 **5／5**。跳过 ensure 族／zc／brotli／hash＊。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 math-fenv／capability／special 双端文档 SHA** · tip **`ffe65296f`** | **文档** · **不升钉** · **日常 L2**。代码 **`869eab8b4`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **test(selfhost)** **STD-059／149／115 math-fenv＋capability＋special soft prefer-c →硬绿** · tip **`869eab8b4`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝三闸 prefer-c（仅 xlang-c）＋soft SKIP→OK（无 native／「manifest OK」）＋soft `ensure_std_c_o`／soft `ensure_runtime_math_libm_o\|\|true`／soft `xlang_compiler_make…\|\|true`＋check 硬绑当唯一 `.x` 烟测＋报告 `c_smoke=`／`c=`／`x=`／`host=`＝假权威；capability section 硬扫化石顶层 DOC；lib 内 `set -e` 泄漏致 `return 1` 整闸死。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft ensure／soft auto-make；显式坏 XLANG／缺 native 硬 die；host-C 仅预编 `.o`＝obs（cap tip diag_reportf 静默仍 exit0＝绿）；check＋tip product `std_math_*` UNDEF＝obs；archive `## Gate`；TSV script 锚全路径；报告 `run=`／`obs=`／`skip=`。证：Darwin 三闸均 run=1 **obs=2**＋neg RC=1＋L2 **5／5**；Ubuntu 三闸均 run=1 **obs=1**＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_math_{fenv,fenv_cap,special}.log`／`/tmp/ubuntu_l2_math.log`）。跳过 ensure 族／zc／brotli／hash＊／elf-write。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **tip 对齐 task／trace／backtrace-xplat 双端文档 SHA** · tip **`0d21db18a`** | **文档** · **不升钉** · **日常 L2**。代码 **`f2a84f8e7`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **test(selfhost)** **STD-089／088／147 task＋trace＋backtrace-xplat soft prefer-c →硬绿** · tip **`f2a84f8e7`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝三闸 prefer-c（仅 xlang-c）＋soft SKIP→OK＋soft `ensure_std_c_o`／soft `xlang_compiler_make…\|\|true`＋check 硬绑＋报告 `c_smoke=`／`x=`／`quality=`＝假权威；backtrace section 硬扫化石顶层 DOC；TSV API 化石 `task_group_*`／`trace_new`／`span_*`。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft ensure／soft auto-make；显式坏 XLANG／缺 native 硬 die；host-C 仅预编 `.o`＝obs；check＋tip product／quality UNDEF＝obs；TSV／archive API 锚对齐产品短名；`## Gate`；报告 `run=`／`obs=`／`skip=`。证：Darwin task／trace run=0 **obs=3**／bt run=0 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu task run=0 **obs=3**／trace run=0 **obs=2**／bt run=0 **obs=1**＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_{task,trace,bt}.log`／`/tmp/ubuntu_l2_task_trace_bt.log`）。跳过 ensure 族／zc／brotli／hash＊。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **STD-128／116／146 csv-stream＋json-typed＋atomic-widen soft→硬绿双端** · tip **`3d39df1da`** | **文档** · **不升钉** · **日常 L2**。代码 **`5fccdd360`** 双端已绿：Darwin csv／json run=0 **obs=3**／widen run=0 **obs=2**；Ubuntu csv／json run=0 **obs=2**／widen run=0 **obs=1**＋neg RC=1＋L2 **5／5**。更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **test(selfhost)** **STD-128／116／146 csv-stream＋json-typed＋atomic-widen soft prefer-c →硬绿** · tip **`5fccdd360`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝三闸 prefer-c（仅 xlang-c）＋soft SKIP→OK＋soft `ensure_std_c_o …||true`／atomic soft glue ensure＋check 硬绑＋报告 `c=`／`x=`／`exec=`＝假权威；lib section 硬扫化石顶层 DOC。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft ensure；显式坏 XLANG／缺 native 硬 die；host-C 仅预编 `.o`＝obs；check＋tip product UNDEF＝obs；报告 `run=`／`obs=`／`skip=`；archive `## Gate` honesty；atomic section→TSV archive 路径。证：Darwin csv／json run=0 **obs=3**／widen run=0 **obs=2**＋neg RC=1＋L2 **5／5**；Ubuntu csv／json run=0 **obs=2**／widen run=0 **obs=1**＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_{csv_stream,json_typed,atomic_widen}.log`／`/tmp/ubuntu_l2_csv_json_widen.log`）。跳过 ensure 族／zc／brotli／hash＊。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs(selfhost)** **STD-041 async-future＋async-context soft→硬绿双端** · tip **`d541c801f`** | **文档** · **不升钉** · **日常 L2**。代码 **`bcb7f8b90`** 双端已绿：Darwin future run=0 **obs=6**／context run=0 **obs=5**；Ubuntu future run=0 **obs=5**／context run=0 **obs=3**＋neg RC=1＋L2 **5／5**。更新 KPI／§6／C迁移；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **test(selfhost)** **STD-041 async-future＋async-context soft prefer-c →硬绿** · tip **`bcb7f8b90`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝(1) `run-std-async-future` soft auto-make `future.o …\|\|true`＋报告 `c=`／`x=`／`emit=`；(2) `run-std-async-context` prefer-c（仅 xlang-c／xlang、无 asm）＋soft SKIP→OK（无 native 仍 OK）＋soft `ensure_std_c_o` 重建 F-07 已迁 scheduler／context／time／task＋check 硬绑＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft auto-make／soft ensure；显式坏 XLANG／缺 native 硬 die；host-C 仅预编 `.o`＝obs；check＋tip product `-o` `std_async_*` UNDEF＝obs；`-E` 工具失败硬 die／emit marker miss＝obs；报告 `run=`／`obs=`／`skip=`；archive Gate honesty。证：Darwin future run=0 **obs=6**／context run=0 **obs=5**＋neg RC=1＋L2 **5／5**；Ubuntu future run=0 **obs=5**／context run=0 **obs=3**＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_async_{future,context}.log`／`/tmp/ubuntu_l2_async_ctx.log`）。跳过 ensure 族／zc／brotli／hash＊。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **test(selfhost)** **STD-042 async-io-cps soft prefer-c →硬绿** · tip **`1411b086d`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-async-io-cps` prefer-c 优先＋soft auto-make scheduler.o／xlang-c＋soft SKIP→OK（无 native）＋check 硬绑＋报告 `align=`／`emit=`／`skip=`＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft auto-make；显式坏 XLANG／缺 native 硬 die；check＋tip product `-o` `std_async_*` UNDEF＋emit marker miss＝obs；`-E` 工具失败硬 die；archive DOC Gate honesty；报告 `run=`／`obs=`／`skip=`。证：Darwin run=0 **obs=5**＋neg RC=1＋L2 **5／5**；Ubuntu run=0 **obs=3**＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_async_io_cps.log`／`/tmp/ubuntu_l2_async_io_cps.log`）。跳过 ensure 族／zc／brotli／hash＊。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **test(selfhost)** **STD-078／117／090 metrics＋schema soft prefer-c →硬绿** · tip **`3ae28d864`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{metrics,metrics-obs,schema}` prefer-c 优先＋soft auto-make＋soft SKIP→OK（metrics／obs grep 假绿；schema 化石 `schema_new`／`get_i32` TSV＋soft ensure `.o`）＋报告 `x=`／`c_smoke=`＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft auto-make／grep-as-sole-green；显式坏 XLANG／缺 native 硬 die；check＋tip product `-o` `std_metrics_*`／`std_trace_*`／`std_schema_*` UNDEF＝obs；host-C 仅预编 `.o`（拒 soft ensure）；TSV 锚活面短名 `new`／`add_field`／`get`；archive DOC Gate honesty；报告 `run=`／`obs=`／`skip=`。证：Darwin metrics／obs run=0 **obs=2**／schema run=0 **obs=3**＋neg RC=1＋L2 **5／5**；Ubuntu metrics／obs run=0 **obs=1**／schema run=0 **obs=2**＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_metrics*.log`／`/tmp/ubuntu_schema.log`／`/tmp/ubuntu_l2_metrics_schema.log`）。跳过 async-io-cps／ensure 族／zc／brotli／hash＊。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-073／110／139 codec soft prefer-c →硬绿** · tip **`e316b2ef4`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-codec{,-stream,-buffer-reuse}` prefer-c 优先＋soft auto-make xlang-c＋soft SKIP→OK（无 native）＋`xlang check` 当唯一烟测绿＋报告 `x=`／`skip=`＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft auto-make；显式坏 XLANG／缺 native 硬 die；check＋tip product `-o` `std_codec_*` UNDEF＝obs（产品债另案）；archive DOC Gate honesty＋cross-link；报告 `run=`／`obs=`／`skip=`。证：Darwin 三闸均 run=0 **obs=2**＋neg RC=1＋L2 **5／5**；Ubuntu 三闸均 run=0 **obs=1**＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_codec*.log`／`/tmp/ubuntu_l2_codec.log`）。跳过 ensure 族／zc／brotli／hash＊。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-144 mem-safe soft fallthrough →硬绿** · tip **`84fa5e642`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-mem-safe` prefer-c 优先＋soft SKIP→OK（无 native）＋`xlang check` 当唯一烟测绿＋顶层 DOC／archive 双权威＋报告 `x=`／`skip=`＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft SKIP→OK／prefer-c／soft auto-make；显式坏 XLANG／缺 native 硬 die；check＋tip product `-o` `std_mem_*` UNDEF＝obs（同 STD-018）；删顶层双权威 DOC；README→archive；报告 `run=`／`obs=`／`skip=`。证：Darwin run=0 **obs=2**＋neg RC=1＋L2 **5／5**；Ubuntu run=0 **obs=1**＋neg RC=1＋L2 **5／5**。跳过 ensure 族／zc4。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-139／111／BOOT-029／145／133／137／134 soft fallthrough →硬绿** · tip **`2d66264fe`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{sqlite-stub,sync-lock-diag,sys,test-runner,time-bench-timer,time-format-timezone,url-ipv6-host}` 一过 prefer-asm 仍 soft XLANG fallthrough（显式坏仍回落 xlang-c）＋soft auto-make＋`check=`／`stub_c=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK／prefer-c／soft ensure／soft sqlite-o-stub 重建；显式坏 XLANG／缺 native 硬 die；产品 stub_behavior／lock_diag／write_stdout／runner_smoke／bench_timer／format_timezone／ipv6_host `-o` 硬绿；check／host-C／macos_thin／linux_nr＝obs（现成 `.o` only）；报告 `run=`／`obs=`／`skip=`。证：Darwin 七闸均 run=1 **obs=1～2**＋neg RC=1＋L2 **5／5**；Ubuntu 七闸 run=1 **obs=0～1**＋neg RC=1＋L2 **5／5**。无 soft-ensure 池本波清；跳过 zc4／ensure 族。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-027／021-022／142 dynlib／path-fs／process-xplat soft fallthrough →硬绿** · tip **`66ee21753`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{dynlib-windows,path-fs-windows,process-xplat}` 一过 prefer-asm 仍 soft XLANG fallthrough（显式坏仍回落 xlang-c）＋soft auto-make＋`check=`／`osc=`／`path=`／`xplat=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK／prefer-c／soft ensure 重建；显式坏 XLANG／缺 native 硬 die；产品 osc＋null＋win_path／path＋fs／xplat＋boundary `-o` 硬绿；check／host-C／xplat 委托／win-pipe＝obs；报告 `run=`／`obs=`／`skip=`。证：Darwin dynlib run=3 **obs=2**／pfw run=2 **obs=1**／proc run=2 **obs=3**＋neg RC=1＋L2 **5／5**；Ubuntu dynlib run=3 **obs=1**／pfw run=2 **obs=0**／proc run=2 **obs=2**＋neg RC=1＋L2 **5／5**。跳过 zc4／ensure 族。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-028／db-kv-arrow／STD-138 soft fallthrough →硬绿** · tip **`204c31c8c`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{runtime-panic-hook,db-kv-arrow,xplat-deep-boundary}` 一过 prefer-asm 仍 soft XLANG fallthrough（显式坏仍回落 xlang-c）＋soft auto-make／soft ensure＋`check=`／`hook=`／`kv=`／`x=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK／prefer-c／soft ensure 重建；显式坏 XLANG／缺 native 硬 die；产品 hook＋ready／kv＋arrow＋cookbook／must-policy xplat `-o` 硬绿；check／host-C／EXC／optional＝obs；报告 `run=`／`obs=`／`skip=`。证：Darwin panic run=2 **obs=1**／dbkv run=3 **obs=2**／xplat run=4 **obs=5**＋neg RC=1＋L2 **5／5**；Ubuntu panic run=2 **obs=1**／dbkv run=3 **obs=1**／xplat run=4 **obs=0**＋neg RC=1＋L2 **5／5**。跳过 zc4／ensure 族。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-012／001／026／160 examples／io-api／io-fallback／string-unicode soft fallthrough →硬绿** · tip **`a53b08183`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{examples,io-api,io-fallback,string-unicode}` 一过 prefer-asm 仍 soft XLANG fallthrough（显式坏仍回落其它二进制）＋soft auto-make＋`check=`／`x=`／`run=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK／prefer-c；显式坏 XLANG／缺 native 硬 die；产品 `hello`＋`io_batch`／`run-io`／`fallback_matrix`／`unicode_bridge` `-o` 硬绿；check＝obs；报告 `run=`／`obs=`／`skip=`。证：Darwin examples run=2 **obs=0**／io-api／fallback／unicode run=1 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu 四闸 **obs=0**＋neg RC=1＋L2 **5／5**。跳过 ensure 族闸（atomic／thread-pool／queue／log-rotate）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-061 simd-prod soft fallthrough →硬绿** · tip **`df77364c8`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-simd-prod` 一过 prefer-asm 仍 soft XLANG fallthrough（显式坏仍回落其它二进制）＋soft auto-make＋`check=`／`bench=` 报告＝假权威；顶层 DOC 与 archive 双权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`（拒 xlang-c Vec）；拒 soft auto-make／soft SKIP→OK；显式坏 XLANG／缺 native asm 硬 die；产品 `r04_simd_shuffle_select.x` `-o` 硬绿；check／perf ratio＝obs；删顶层双权威 DOC；报告 `run=`／`obs=`／`skip=`。证：Darwin run=1 **obs=1**（ratio soft）＋neg RC=1＋L2 **5／5**；Ubuntu run=1 **obs=0** ratio=2＋neg RC=1＋L2 **5／5**。跳过 hash＊（UNDEF／ensure）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **simd-intrinsic／STD-047 shuffle／STD-153 autovec soft fallthrough →硬绿** · tip **`6a10bf1c0`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-simd-{intrinsic,shuffle-select,autovec-strategy}` 早前 prefer-asm 仍 soft XLANG fallthrough（显式坏仍回落其它二进制）＋soft auto-make／soft ensure_std_c_o＋`check=`／`x=`／`shuffle=` 报告＝假权威；顶层 DOC 与 archive 双权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`（拒 xlang-c Vec）；拒 soft auto-make／soft SKIP→OK／soft ensure；显式坏 XLANG／缺 native asm 硬 die；产品 `intrinsic_binop_dot`／`shuffle_select_roundtrip`／`autovec_strategy` `-o` 硬绿；check／C／perf／非硬 s4＝obs；删顶层三份双权威 DOC；报告 `run=`／`obs=`／`skip=`。证：Darwin intrinsic run=1 **obs=1**／shuffle run=2 **obs=1**／autovec run=1 **obs=3**＋neg RC=1＋L2 **5／5**；Ubuntu intrinsic／shuffle **obs=0**／autovec **obs=2**＋neg RC=1＋L2 **5／5**。跳过 hash＊（UNDEF／ensure）／simd-prod（下刀）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-029／033／009 net-dns／http-chunked／http soft fallthrough →硬绿** · tip **`c1de093ff`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{net-dns,http-chunked,http}` 早前 prefer-asm 仍 soft XLANG fallthrough（显式坏仍回落其它二进制）＋soft ensure_std_c_o／soft auto-make＋`check=`／`resolve=`／`main=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK／soft ensure 重建；显式坏 XLANG／缺 native 硬 die；产品 `resolve_dns`＋`main`／`chunked_keepalive`／`http/main` `-o` 硬绿；check＝obs；报告 `run=`／`obs=`／`skip=`。证：Darwin net-dns run=2 **obs=1**／chunked／http run=1 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu 三闸 **obs=0**＋neg RC=1＋L2 **5／5**。跳过 hash-default／xxhash（产品 UNDEF／ensure）；跳过 http-context／reqresp／server-pool／unbounded。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-002／092／123／003 net-api／net-context／fs-dirmeta／fs-crossplatform soft fallthrough →硬绿** · tip **`4164236f7`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{net-api,net-context,fs-dirmeta,fs-crossplatform}` 早前 prefer-asm 仍 soft XLANG fallthrough（显式坏仍回落其它二进制）＋soft ensure_std_c_o／soft auto-make＋`check=`／`x=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK／soft ensure 重建；显式坏 XLANG／缺 native 硬 die；产品 `run-net`／`context_connect`／`dirmeta_roundtrip`／must-policy xplat `-o` 硬绿；check／C archaeology＝obs（现成 `.o` only）；报告 `run=`／`obs=`／`skip=`。证：Darwin net-api／net-ctx／fs-xplat run=1 **obs=1**／dirmeta run=1 **obs=2**＋neg RC=1＋L2 **5／5**；Ubuntu net-api／net-ctx／fs-xplat **obs=0**／dirmeta **obs=1**＋neg RC=1＋L2 **5／5**。跳过 http-context／reqresp／server-pool／unbounded（产品 UNDEF）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-136／032／034／004 datetime-iana／http-methods／http-https／async-api soft fallthrough →硬绿** · tip **`94dc5746f`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{datetime-iana,http-methods,http-https,async-api}` 早前 prefer-asm 仍 soft XLANG fallthrough（显式坏仍回落其它二进制）＋soft ensure_std_c_o／soft auto-make＋`check=`／`switch=`／`imp=`／`drain=`／`coop=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK／soft ensure 重建；显式坏 XLANG／缺 native 硬 die；产品 `iana_dst_smoke`／`methods_status`／`https_smoke`／`i06_async_switch`＋`async_mod_import`＋`async_drain_idle` `-o` 硬绿（async 三路并入 `run=`；coop＝obs）；check／host-C／OpenSSL archaeology＝obs（现成 `.o` only）；报告 `run=`／`obs=`／`skip=`。证：Darwin iana／methods／https run=1 **obs=1～3**／async run=3 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu iana **obs=1**／methods **obs=0**／https **obs=2**／async **obs=1**＋neg RC=1＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-119／135／098／050 config-yaml／datetime-timezone／channel-select／crypto-sha512-hmac soft fallthrough →硬绿** · tip **`5e424b90f`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{config-yaml,datetime-timezone,channel-select,crypto-sha512-hmac}` soft XLANG fallthrough（显式坏仍回落其它二进制）＋soft auto-make／soft ensure_std_c_o＋`check=`／`sha512=`／`hmac=`／`mac512=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK／soft ensure 重建；显式坏 XLANG／缺 native 硬 die；产品 `yaml_smoke`／`timezone`／六路 `select_*.x`／`sha512_abc`＋`hmac_sha512_rfc4231_tc1` `-o` 硬绿（sha512＋hmac 并入 `run=`；mac512＝obs）；check／host-C archaeology＝obs（现成 `.o` only）；报告 `run=`／`obs=`／`skip=`。证：Darwin yaml／tz run=1 **obs=1～2**／channel run=6 **obs=1**／sha512 run=2 **obs=2**＋neg RC=1＋L2 **5／5**；Ubuntu yaml／channel **obs=0**／tz **obs=1**／sha512 **obs=1**＋neg RC=1＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-109／140／096／055 base64-stream／path-extreme／dynlib-last-error／ffi-cstring soft auto-make →硬绿** · tip **`c94ec03d7`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{base64-stream,path-extreme,dynlib-last-error,ffi-cstring-lifecycle}` soft XLANG fallthrough（显式坏仍回落其它二进制）＋soft auto-make／soft ensure_std_c_o＋`check=`／`safe004=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK／soft ensure 重建；显式坏 XLANG／缺 native 硬 die；产品 `stream`／`extreme_clean`／`last_error`／`cstring_try_new` `-o` 硬绿；SAFE-004 并入 `run=`；check／host-C archaeology＝obs（现成 `.o` only）；报告 `run=`／`obs=`／`skip=`。证：Darwin b64／path／dynlib run=1 **obs=1～2**／ffi run=2 **obs=2**＋neg RC=1＋L2 **5／5**；Ubuntu b64／dynlib／ffi **obs=1**／path **obs=0**＋neg RC=1＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-035／json-object-array／STD-053 json-serialize／log-multi soft auto-make →硬绿** · tip **`46257f0b9`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{json-serialize,json-object-array,log-multi-sink}` soft XLANG fallthrough（显式坏仍回落其它二进制）＋soft auto-make／soft ensure_std_c_o＋`check=`／`oa=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK／soft ensure 重建；显式坏 XLANG／缺 native 硬 die；产品 `object_array_roundtrip`／`object_array_parse`／`level_filter` `-o` 硬绿；check／host-C archaeology＝obs；报告 `run=`／`obs=`／`skip=`（legacy `oa=` 并入 `run=`）。证：Darwin jsz／joa run=1 **obs=1**／logms run=1 **obs=2**＋neg RC=1＋L2 **5／5**；Ubuntu jsz／joa **obs=0**／logms **obs=1**＋neg RC=1＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-156／132／008 context-cookbook／env-platform／json soft auto-make →硬绿** · tip **`071264b0f`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{context-cookbook,env-platform-encoding,json}` soft XLANG fallthrough（显式坏仍回落其它二进制）＋soft auto-make／soft ensure_std_c_o＋`check=`／`main=`／`zc=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK／soft ensure 重建；显式坏 XLANG／缺 native 硬 die；产品 `context_cancel_deadline`／`platform_encoding`／`json/main` `-o` 硬绿；check／host-C／zc（Darwin needs_copy）＝obs；报告 `run=`／`obs=`／`skip=`。证：Darwin ctxcb run=1 **obs=0**／envpe run=1 **obs=2**／json run=1 **obs=2**＋neg RC=1＋L2 **5／5**；Ubuntu ctxcb／json **obs=0**／envpe **obs=1**＋neg RC=1＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-020／158／071 error-map／error-semantics／context soft auto-make →硬绿** · tip **`2653194b4`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{error-map,error-semantics,context}` soft XLANG fallthrough（显式坏仍回落其它二进制）＋soft auto-make／soft ensure_std_c_o＋`check=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK／soft ensure 重建；显式坏 XLANG／缺 native 硬 die；产品 `error_map_smoke`＋`error_module_base`／`error_semantics_smoke`＋`error_semantic_class`／`cancel_smoke` `-o` 硬绿；check／host-C archaeology＝obs；报告 `run=`／`obs=`／`skip=`。证：Darwin emap／esem run=2／ctx run=1 **obs=1／1／2**＋neg RC=1＋L2 **5／5**；Ubuntu **obs=0／0／1**＋neg RC=1＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-005／025／019 time／env-iter／fmt-multi soft auto-make →硬绿** · tip **`3a22f2041`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{time,env-iter,fmt-multi}` soft XLANG fallthrough（显式坏仍回落其它二进制）＋soft auto-make／soft ensure_std_c_o＋`check=`／`main=`／`precision=` 报告＝假权威（早前 prefer-asm 标绿未收 soft fallthrough）。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK／soft ensure 重建；显式坏 XLANG／缺 native 硬 die；产品 `main`＋`precision_smoke`／`env_iter`＋`env_args_iter`／`format_multi` `-o` 硬绿；check＝obs；报告 `run=`／`obs=`／`skip=`。证：Darwin time／env run=2／fmt run=1 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu 同形 **obs=0**＋neg RC=1＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-130／150／060 random-rng／sort-key／sort-stable soft auto-make →硬绿** · tip **`721435f80`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{random-rng,sort-key-cmp,sort-stable-cmp}` soft XLANG fallthrough（显式坏仍回落其它二进制）＋soft auto-make／soft ensure_std_c_o＋`check=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK／soft ensure 重建；显式坏 XLANG／缺 native 硬 die；产品 `rng_roundtrip`＋`main`／`key_stable`／`stable_i32`＋`cmp_desc` `-o` 硬绿；check／host-C archaeology＝obs（不碰 ensure_std 族）；报告 `run=`／`obs=`／`skip=`。证：Darwin rng run=2／key run=1／stable run=2 **obs=2**＋neg RC=1＋L2 **5／5**；Ubuntu 同形 **obs=1**＋neg RC=1＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-015／017／011 set-extend／heap-trace／error-unify soft auto-make →硬绿** · tip **`ba6ebcf0a`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{set-extend,heap-trace,error-unify}` soft XLANG fallthrough（显式坏仍回落其它二进制）＋soft auto-make xlang-c＋`check=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK；显式坏 XLANG／缺 native 硬 die；产品 `extend`／`trace_stats`（含 `XLANG_HEAP_TRACE=1`）／`error_unify_smoke` `-o` 硬绿；cookbook 邻域硬；check＝obs；报告 `run=`／`obs=`／`skip=`。证：Darwin set／heap／err run=1 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu 同形 **obs=0**＋neg RC=1＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-079／129／112 security／set-ops／heap-allocator soft auto-make →硬绿** · tip **`8451b1a07`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{security,set-ops,heap-allocator}` soft XLANG fallthrough（显式坏仍回落其它二进制）＋soft auto-make xlang-c＋`check=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK；显式坏 XLANG／缺 native 硬 die；产品 `roundtrip`／`ops`／`allocator_vec` `-o` 硬绿；cookbook 邻域硬；check／host-C archaeology＝obs；报告 `run=`／`obs=`／`skip=`。证：Darwin sec run=1 **obs=2**＋set／heap run=1 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu sec run=1 **obs=1**＋set／heap run=1 **obs=0**＋neg RC=1＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-155／074／076 bytes-arena／datetime／url soft auto-make →硬绿** · tip **`0a39b0549`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{bytes-arena,datetime,url}` soft auto-make xlang-c＋显式坏 XLANG 仍回落其它二进制＋`check=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`＋`dod_native_exe`；拒 soft auto-make／soft SKIP→OK；显式坏 XLANG／缺 native 硬 die；产品 `arena_external`／`roundtrip` `-o` 硬绿；check／host-C archaeology＝obs；报告 `run=`／`obs=`／`skip=`。证：Darwin ba run=1 **obs=1**＋dt／url run=1 **obs=2**＋neg RC=1＋L2 **5／5**；Ubuntu ba run=1 **obs=0**＋dt／url run=1 **obs=1**＋neg RC=1＋L2 **5／5**。探针 encoding-hex／uuid：产品 UNDEF（ensure／ondemand）→本波不收。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **STD-080／081 option-result soft auto-make →硬绿** · tip **`c805f5a0b`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-option-result` soft auto-make xlang-c＋显式坏 XLANG 仍回落其它二进制＋`check=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`；拒 soft auto-make／soft SKIP→OK；显式坏 XLANG／缺 native 硬 die；产品 `roundtrip.x` `-o` 硬绿；check＝obs；报告 `run=`／`obs=`／`skip=`。证：Darwin run=1 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu run=1 **obs=0**＋neg RC=1＋L2 **5／5**。探针 codec 族：旧闸已产品红（formal_mod／ondemand 无 codec 叶／UNDEF）→本波不收、不碰 ensure。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **cli／cache／config／csv-row／bytes soft auto-make →硬绿** · tip **`0d27572f8`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-std-{cli,cache,config,csv-row,bytes}` soft auto-make xlang-c＋显式坏 XLANG 仍回落其它二进制＋`check=` 报告＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`；拒 soft auto-make／soft SKIP→OK；显式坏 XLANG／缺 native 硬 die；产品 `-o` 硬绿；check／host-C smoke＝obs；报告 `run=`／`obs=`／`skip=`。证：Darwin cli／cache／config／bytes run=1＋csv-row run=2＋neg RC=1＋L2 **5／5**；Ubuntu 同形（obs 更少）＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **io／net／queue soft auto-make →硬绿** · tip **`93b7d04cd`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-{io,net,queue}` soft auto-make xlang-c／soft 默认 xlang／net soft gcc fallback＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`；拒 soft auto-make／soft SKIP→OK／prefer-c／soft gcc fallback；显式坏 XLANG 硬 die；io 9 案／queue 1／net 2 产品 `-o` 硬绿；net 保留 ensure_std 族；报告 `run=`／`obs=`／`skip=`。证：Darwin／Ubuntu io run=9＋queue run=1＋net run=2＋neg RC=1＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **c07 soft auto-make xlang-c →硬绿** · tip **`6b96a32e3`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-c07-frontend-parity` soft auto-make（`xlang_compiler_make…xlang-c…\|\|true`）＋`c07_native_xlang` 与 `dod_native_exe` 双权威＝假权威。**修**：拒 soft auto-make；REF 须现成 native xlang-c；CAND prefer asm；收敛 `dod_native_exe`；显式坏 C07_REF／C07_CAND 硬 die；REF typeck_ok／compile_fail 硬；CAND `-backend c` SEGV／diverge＝obs；报告 `run=`／`obs=`／`skip=`。证：Darwin／Ubuntu run=6 **obs=6**＋neg RC=1＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **boot-std-link／exc-cli／exc-panic soft auto-make →硬绿** · tip **`e19c5c34a`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-boot-std-link-contract`／`run-exc-cli-lsp-error`／`run-exc-panic-abort` soft auto-make（`xlang_compiler_make…\|\|true`）＋显式坏 XLANG 仍回落其它二进制＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`；拒 soft auto-make／soft SKIP→OK；显式坏 XLANG／缺 native 硬 die；check＝obs；json always＋2 golden＋7 case 产品硬绿；on_demand async＝obs；报告 `run=`／`obs=`／`skip=`。证：Darwin link run=2 **obs=1**＋cli run=2 **obs=1**＋panic run=7 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu link run=2 **obs=1**＋cli／panic obs=0＋neg RC=1＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **boot-015／019 soft auto-make →硬绿** · tip **`9e078c442`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-boot-015`／`run-boot-019` soft auto-make（`xlang_compiler_make…\|\|true`）＋019 soft bootstrap-link wrap＋subset link miss soft SKIP＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`；拒 soft auto-make／soft SKIP→OK；显式坏 XLANG／缺 native 硬 die；check＝obs；vec／map／heap＋6 smoke 产品 `-o` 硬绿；subset link miss 硬 die；报告 `run=`／`obs=`／`skip=`。证：Darwin boot015 run=3 **obs=3**＋boot019 run=6 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu 同形 obs=0＋neg RC=1＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **refresh-xlang-asm soft→硬绿** · tip **`fdf0f357d`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-refresh-xlang-asm-gate` local non-Linux soft SKIP→OK（无 `skip=`）＋soft auto-make＋硬绑 `./compiler/xlang`＋硬绑 `xlang check`＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`；拒 soft auto-make／soft SKIP→OK；local Darwin refresh-gate body＝`skip=1`（SHARED 烟测仍硬）；Linux／CI `xbuild refresh-gate` 硬；import 产品 `-o` exit0；const_hex `-E` MAGIC；check＝obs；region／linear 子闸硬；报告 `run=`／`obs=`／`skip=`。证：Darwin run=5 **obs=1 skip=1**＋neg RC=1＋L2 **5／5**；Ubuntu run=6 obs=0＋neg RC=1＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology／LINUX｜CI refresh gold。 |
| 2026-08-28 | **fix(selfhost)** **CORE-017／CORE-011 soft auto-make →硬绿** · tip **`9ac1b6780`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-core-mem-volatile-fence`／`run-core-fmt-f64-special` soft SKIP→OK（无 native 仍 gate OK）＋soft auto-make xlang-c＋bootstrap-link 裹 xlang-c＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`；拒 soft auto-make／soft SKIP→OK；check＝obs；volatile_fence／f64_special 产品 `-o` exit0 硬绿；报告 `run=`／`obs=`／`skip=`。证：Darwin mem run=1 **obs=1**＋fmt run=1 **obs=1**＋neg RC=1；Ubuntu 同形 obs=0＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **CORE-002/003／006／STD-131 soft auto-make →硬绿** · tip **`e5cd58dec`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-core-{option-result,iterator-protocol,str-find-split}` soft SKIP→OK（无 native 仍 gate OK）＋soft auto-make xlang-c＋bootstrap-link 裹 xlang-c＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`；拒 soft auto-make／soft SKIP→OK；check＝obs；option102／result173、iterator smoke＋cookbook10、find_split exit0 产品 `-o` 硬绿；报告 `run=`／`obs=`／`skip=`。证：Darwin or run=2 **obs=1**＋iter run=2 **obs=1**＋find run=1 **obs=1**＋neg RC=1；Ubuntu 同形 obs=0＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **CORE-012／007／004 soft auto-make →硬绿** · tip **`278673949`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-core-{debug-assert-extend,str-view,slice-api}` soft SKIP→OK（无 native 仍 gate OK）＋soft auto-make xlang-c＋bootstrap-link 裹 xlang-c＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`；拒 soft auto-make／soft SKIP→OK；check＝obs；assert_extend／bytes_view＋cookbook／subslice_split_chunks 产品 `-o` 硬绿；报告 `run=`／`obs=`／`skip=`。证：Darwin assert run=1 **obs=1**＋str-view run=2 **obs=1**＋slice run=1 **obs=1**＋neg RC=1；Ubuntu 同形 obs=0＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **CORE-001／CORE-016 soft prefer-c →硬绿** · tip **`4866e616c`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-core-types-generic-layout`／`run-core-option-result-unify` prefer-c（xlang-c 先于 asm）＋soft auto-make xlang-c＋无 native soft SKIP→OK＝假权威。**修**：prefer asm＋`XLANG_LINK_XLANG`；拒 soft auto-make／soft SKIP→OK；check＝obs；generic `-o` exit0／unify 双 smoke exit0 硬绿；报告 `run=`／`obs=`／`skip=`。证：Darwin gl run=1 **obs=1**＋unify run=2 **obs=1**；Ubuntu gl run=1＋unify run=2（obs=0）＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **migrate-x-gen soft→硬绿** · tip **`1591e7c3d`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-migrate-x-gen-gate` soft auto-make xlang-c＋mtime 陈旧时 soft wipe `*_gen.c` 再 rebuild（rebuild 失败可留空树＝假权威／毁产物）；存储＝parser／typeck／codegen gen 标记合同；消费＝refresh-asm／bstrict。**修**：默认 inspect-only（拒 soft wipe／soft auto-make）；FORCE 须现成 native xlang-c，经 `ensure_migrate_gen`＋`migrate_x_objs` 重建（不先 rm）；stale／缺 `.o` 无 FORCE＝obs；缺 gen／标记 miss＝硬 die；报告 `run=`／`obs=`／`skip=`。证：Darwin／Ubuntu migrate run=1 **obs=1**＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **x-pipeline／x-multi-file／lsp soft→硬绿** · tip **`1f417d7a9`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝三闸 soft auto-make bootstrap-pipeline／xlang-x＋soft SKIP→OK（XLANG 置位／缺 `-x -E`／timeout）／lsp 用 `--help` 缺 `--lsp` 触发 soft auto-make＝假权威；tip 产品已支持 `-x -E`；multi-file tip mangle `foo_bar`；lsp tip Darwin 空出／Ubuntu hits=0。**修**：prefer asm；拒 soft auto-make／soft SKIP→OK；multi 接受 `foo_bar`／`bar`；lsp tip residual＝obs（pipefail 零 label 不硬崩）。证：Darwin／Ubuntu x-pipeline run=1＋x-multi-file run=1＋lsp **obs=1**＋L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs／L2** **array→asm 积压＋近波 Ubuntu 复验** · tip **`c240aa616`** | **文档＋金标** · **不升钉** · **日常 L2**。`ssh ubuntu-server` @ tip：L2 **5／5**；typeck run=29；defer run=4；export run=2 **obs=10**；typeck-wpo run=2；wpo-dce run=5 **obs=3**；size run=5；wpo-self **obs=1**；lang-generic／pkgmgr／parser-x；hello run=1；sanitize run=2 **obs=2**；pipeline-wpo run=1 **obs=1**；array→asm／binary-expr／fmt-wrap／lexer／slice／struct 等积压门绿（`/tmp/honesty_ubuntu/*.log`）。补日切「双端文档待补」。注意：`build_xlang_asm` 实验链可留弱 `main_cmd_build=-1`，门后须 `./xbuild link-product-asm` 恢复。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **hello／sanitize／pipeline-wpo soft→硬绿** · tip **`c240aa616`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝hello 软默认 xlang＋非 x86 prefer-c＋RUN_ALL soft auto-make；sanitize prefer-c＋check 硬绑＋假硬红；pipeline-wpo soft SKIP→OK。**修**：prefer asm；hello Hello World exit0 硬；sanitize check／`-fsanitize -E` 无 bounds delta＝obs、OOB panic 硬；pipeline-wpo Darwin skip＝、Linux relink＋helpers 硬、return-value tip SIGSEGV＝obs。证：Darwin hello run=1＋sanitize run=2 **obs=2**＋pipeline **skip=1**；Ubuntu hello run=1＋sanitize run=2 **obs=2**＋pipeline run=1 **obs=1**＋L2 **5／5**（`/tmp/honesty_ubuntu/{hello,sanitize,pipeline_wpo}.log`）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology／LINUX pipeline-wpo。 |
| 2026-08-28 | **docs／L2** **回家 `ubuntu-server` 复验 tip soft→硬绿** · tip **`72e14c1a9`** | **文档＋金标** · **不升钉** · **日常 L2**。`ssh ubuntu-server` pull≥`72e14c1a9`：typeck run=29 · wpo-dce run=5 **obs=3** · size run=5 · wpo-compiler-self **obs=1** · lang-generic run=1 multi=1 · pkgmgr run=2 · parser-x run=2 · export run=2 **obs=10** · defer run=4 · typeck-wpo run=2 · L2 **5／5**（`/tmp/ubuntu_l2_home_72e14c1a9.log`）。同波修 parser-x `nm｜grep -q` pipefail 假阴＋typeck-wpo tip log 针。SSH 权威＝**`ubuntu-server`**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs** **金标 SSH 回家改回 `ubuntu-server`** · tip **`503043204`**（文档）／**`7fff3bb56`**（pkgmgr／parser-x） | **文档** · **不升钉** · **日常 L2**。昨夜断电后 Ubuntu 已开；外地 frp／`ubuntu-remote-server` 口径改回局域网 **`ssh ubuntu-server`**（`192.168.8.156`）。改：本文 §2.1／§5、README／README_zh、`L4测试性能分析`、`自举时序` 金标 SSH 行、skill 主机表。历史归档仍保留当时 `ubuntu-remote-server` 记录（不改写）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **docs** **日切归档 27 号 + 重写 28 号** · tip **`bc9aba2e9`**（代码）／文档双端 tip **`8a3c12fc9`** | **文档** · **不升钉** · **日常 L2**。§6 收 27 日 **60** 行入 [自举进度-归档-2026-08-27.md](自举进度-归档-2026-08-27.md)；主文档仅保留 28 日 KPI／前排／§6（既有 17 行 soft prefer-c／SKIP→OK 硬绿保留）。钉盘仍 **`e8176cbe5`**。**未**改 [自举时序.md](自举时序.md)／[当前问题分析.md](当前问题分析.md)。代码 tip `bc9aba2e9`（array／asm-8／asm-4／struct trio）Darwin 已绿、Ubuntu frp 断，**双端文档待补**。下一刀：Ubuntu 恢复后补文档；或 Darwin 续啃 assign-index-block／expr／binop-block／cfg-merge／`run-asm.sh` soft residual。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **debug／checklist-fast／rename-verify soft prefer-c →硬绿** · tip **`8a3c12fc9`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-{debug,checklist-fast,checklist-sequence,rename-verify}` 默认 xlang-c／seed＋soft auto-make＝prefer-c 假权威；rename 硬绑 `xlang check`＋化石 `compiler/Makefile` 锚＋旧路径正则误伤 `io_backend.xlang_*`／`runtime_net_workers.x`。存储＝四闸＋debug／rename fixtures；消费＝bstrict(debug)／checklist。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；debug 三案产品 `-o` 硬绿；checklist FAIL 节＝obs（ALLOW_WARN）；rename 产品树旧路径收紧＋锚 ensure／compile 脚本、check／harddeps＝obs；`tcp_pool.x` 注释英文化；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 asm-*／array soft auto-make／zc3–5 host-c／export-dynamic／Set MEMORY／升钉。证：Darwin debug run=3＋rename run=12 **obs=2**＋checklist-fast run=1（seq run=4 **obs=3**）＋neg RC=1＋L2 **5／5**；Ubuntu 同形＋L2 **5／5**（`/tmp/ubuntu_{debug,rename,checklist_fast}.log`／`/tmp/ubuntu_l2_debug_soft.log`）。残：soft SKIP 邻域续扫（**…／types＋b06＋if-expr＋ternary／debug＋checklist-fast＋rename-verify soft FAIL 池空**；余 soft：邻域再扫…／asm-* soft auto-make；array／atomic／bool／binary-expr soft auto-make；queue／sqlite TLS／debug diag＝obs；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **types／b06-ast-pool／if-expr／ternary soft prefer-c →硬绿** · tip **`bab6dd819`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-{types,b06-ast-pool,pool-limits,if-expr,ternary}` 默认 `./compiler/xlang-c`＋soft auto-make／soft SKIP→OK／types 硬绑 check＝prefer-c 假权威；pool-limits bootstrap-link 裹 xlang-c 藏 asm 大模块残。存储＝五闸＋types／if-expr／ternary／pool-limits fixtures；消费＝bstrict／bootstrap-bstrict-ci。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；types product `-o` overload exit0 硬／check＝obs；b06 活 ABI 锚＋pool-limits 十案硬绿；if-expr／ternary 产品 `-o` 硬绿；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 debug／checklist-fast／zc3–5 host-c／export-dynamic／Set MEMORY／升钉。证：Darwin types run=1＋if run=4＋ternary run=2＋b06 run=13＋neg RC=1＋L2 **5／5**；Ubuntu types run=1＋if run=4＋ternary run=2＋b06 run=13＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_{types,if_expr,ternary,b06}.log`／`/tmp/ubuntu_l2_types_soft.log`）。残：soft SKIP 邻域续扫（**…／bce＋owned＋sroa／types＋b06＋if-expr＋ternary soft FAIL 池空**；余 soft：邻域再扫…／debug prefer-c；checklist-fast／rename-verify prefer-c；queue／sqlite TLS／debug diag＝obs；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **bce／owned／scope-borrow／sroa／vec-push-arena soft prefer-c →硬绿** · tip **`72bffab90`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-{bce,owned-deinit,owned-return,scope-borrow,sroa,sroa-ctfe,vec-push-arena}-gate` 默认 `./compiler/xlang-c`＋soft auto-make＝prefer-c 假权威；scope-borrow 硬绑 `xlang check`＝暂停闸下 CHK002 假硬红；owned／SROA／MEM-C1 tip 残被硬红掩盖。存储＝七闸＋mem／typeck fixtures；消费＝bstrict／MEM 邻域。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；BCE emit＋exit36 硬绿；scope-borrow 产品 `-o` compile_fail（退役 check）；sroa／ctfe 产品 exit7 硬／SROA emit＝obs；owned tip parse／typeck＝obs；vec-push tip exit=5／缺 push_arena＝obs；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 types／b06-ast-pool／zc3–5 host-c／export-dynamic／Set MEMORY／升钉。证：Darwin bce run=2＋owned＊ run=0 **obs=1**＋scope run=2＋sroa run=2 **obs=3**＋ctfe run=2 **obs=2**＋vec run=1 **obs=2**＋neg RC=1＋L2 **5／5**；Ubuntu bce run=2＋owned＊ obs=1＋scope run=2＋sroa run=3 **obs=2**＋ctfe run=2 **obs=2**＋vec run=1 **obs=2**＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_{bce,owned-*,scope-borrow,sroa*,vec-push-arena}.log`／`/tmp/ubuntu_l2_mem_soft.log`）。残：soft SKIP 邻域续扫（**…／autovec／bce＋owned＋scope-borrow＋sroa＋vec-push-arena soft FAIL 池空**；余 soft：邻域再扫…／types／b06-ast-pool prefer-c；queue／sqlite TLS／debug diag＝obs；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **autovec loop／v2／v3／bce soft prefer-c →硬绿** · tip **`dc80a5c57`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`run-autovec-{loop,v2,v3,bce}-gate` 默认 `./compiler/xlang-c`＋soft auto-make＝prefer-c 假权威；tip 缺 `xlang_autovec_*`（`autovec.c` G-02a hard_retired）被硬红掩盖。存储＝四闸＋`tests/vec/autovec_*.x`；消费＝VEC／simd 邻域。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；`-E` emit 非空＋产品 `-o` exit0 硬绿；缺 helper／scalar 仍在＝obs；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 bce／owned／sroa prefer-c／zc3–5 host-c／export-dynamic／Set MEMORY／升钉。证：Darwin loop／v2／v3 run=2 **obs=2**＋bce run=1 **obs=1**＋neg RC=1＋L2 **5／5**；Ubuntu 同形＋L2 **5／5**（`/tmp/ubuntu_autovec_*.log`／`/tmp/ubuntu_l2_autovec.log`）。残：soft SKIP 邻域续扫（**…／MEM-alloc＋arena-ASP／autovec soft FAIL 池空**；余 soft：邻域再扫…／bce／owned／scope-borrow／sroa／vec-push-arena prefer-c；queue／sqlite TLS／debug diag＝obs；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **MEM allocator／arena-ASP soft prefer-c →硬绿** · tip **`aa0876a5b`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝allocator-escape／realloc／return／alloc-attr-inject／arena-stack-promote 默认 `./compiler/xlang-c`＋soft auto-make＝prefer-c 假权威；escape 把 UNDEF `std_heap_scope_alloc` 当 scope_alloc 诊断假绿；realloc tip 仍可编被硬红掩盖。存储＝五闸＋typeck／mem fixtures；消费＝bstrict／MEM 邻域。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；AL-04 return compile_fail 硬绿；promote＋alias 产品 `-o` exit7 硬绿；escape UNDEF／AL-05／#[alloc] typeck／ASP KEEP_C＋escape tip＝obs；拒路径 `tests/typeck` 假匹配；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 autovec prefer-c／zc3–5 host-c／export-dynamic／Set MEMORY／升钉。证：Darwin escape run=0 **obs=1**＋realloc run=0 **obs=1**＋return run=1＋attr run=0 **obs=1**＋arena run=2 **obs=3**＋neg RC=1＋L2 **5／5**；Ubuntu 同形＋L2 **5／5**（`/tmp/ubuntu_alloc_*.log`／`/tmp/ubuntu_arena_asp.log`／`/tmp/ubuntu_l2_alloc.log`）。残：soft SKIP 邻域续扫（**…／lexer-bounds＋link-hardening／MEM-alloc＋arena-ASP soft FAIL 池空**；余 soft：邻域再扫…／autovec prefer-c；queue／sqlite TLS／debug diag＝obs；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **lexer-bounds／link-hardening soft prefer-c →硬绿** · tip **`e0841fcce`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝(1) lexer-bounds 默认 `./compiler/xlang-c`＋soft auto-make＋硬绑 `xlang check`＝prefer-c 假权威／暂停闸下 CHK002 假硬红；(2) link-hardening 同形 prefer-c＋soft auto-make，Darwin／非 Linux soft SKIP→OK 无 `run=`／`skip=` 诚实报告。存储＝`run-lexer-bounds-gate`／`run-link-hardening-gate`＋manifest／lexer glue／`runtime_link_abi`；消费＝bstrict／a09／w3-p1。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；退役 check 绑闸；long-ident（60）／long-num（400-digit）产品 `-o` exit0 硬绿；Linux Type=DYN＋GNU_STACK NX＋exit42 硬绿／非 Linux `skip=1`；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 queue／sqlite TLS／zc3–5 host-c／export-dynamic／Set MEMORY／升钉。证：Darwin lexer run=3＋link run=1 **skip=1**＋neg RC=1＋L2 **5／5**；Ubuntu lexer run=3＋link run=2＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_lexer_bounds.log`／`/tmp/ubuntu_link_hardening.log`／`/tmp/ubuntu_l2_lexer_link.log`）。残：soft SKIP 邻域续扫（**…／signed＋layout-ovf／lexer-bounds＋link-hardening soft FAIL 池空**；余 soft：邻域再扫…／queue／sqlite TLS／debug diag＝obs；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology／LINUX harden smoke。 |
| 2026-08-28 | **fix(selfhost)** **signed／layout-overflow soft prefer-c →硬绿** · tip **`e221b2beb`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝(1) signed-overflow 默认 `./compiler/xlang-c`＋soft auto-make＋硬绑 `xlang check`＝prefer-c 假权威／暂停闸下 CHK002 假硬红；(2) layout-overflow 同形 prefer-c＋soft auto-make，掩盖已硬绿的 repr-c 子闸。存储＝`run-signed-overflow-gate`／`run-layout-overflow-gate`＋manifest／UB 策略 DOC；消费＝bstrict／a09／w3-p1。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；退役 check 绑闸；unsigned_wrap_ok 产品 `-o` exit42 硬绿；layout 委托 repr-c FAIL=1 硬绿；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 lexer-bounds／link-hardening／zc3–5 host-c／export-dynamic／Set MEMORY／升钉。证：Darwin signed／layout run=2＋neg RC=1＋L2 **5／5**；Ubuntu signed／layout run=2＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_signed_ovf.log`／`/tmp/ubuntu_layout_ovf.log`／`/tmp/ubuntu_l2_signed_layout.log`）。残：soft SKIP 邻域续扫（**…／ub＋struct／signed＋layout-ovf soft FAIL 池空**；余 soft：邻域再扫…／lexer-bounds prefer-c＋check；link-hardening prefer-c／Darwin SKIP；queue／sqlite TLS／debug diag＝obs；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **ub／struct-layout soft SKIP→OK →硬绿** · tip **`0da23a28f`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝(1) ub 默认 `./compiler/xlang`＋bootstrap-link prefer-c 遗产＋无界 Darwin hang（bounds_slice）＝portable 假绿／假挂；(2) struct-layout 硬绑 xlang-c `-E`＋`2>&1` 混 stderr 进 `.c`＝prefer-c 假权威／host-cc 假红。存储＝`run-ub`／`run-struct-layout-assert-gate`；消费＝bstrict／run-all／lang-unsafe hook。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；UB div_ok／unsigned_wrap／三 panic 产品 `-o` 硬绿（timeout hang＝obs）；struct simple／padding_allow＋asm `-E` Layout8 硬绿；host-cc static_assert＝obs；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 signed-overflow／zc3–5 host-c／export-dynamic／Set MEMORY／升钉。证：Darwin ub run=5＋struct run=4＋neg RC=1＋L2 **5／5**；Ubuntu ub run=5（panic=134）＋struct run=4＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_ub.log`／`/tmp/ubuntu_struct.log`／`/tmp/ubuntu_l2_ub_struct.log`）。残：soft SKIP 邻域续扫（**…／next-yellow／ub＋struct soft FAIL 池空**；余 soft：邻域再扫…／signed-overflow prefer-c；queue／sqlite TLS／debug diag＝obs；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **next-yellow-clear soft SKIP→OK →硬绿** · tip **`0e506b45f`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝(1) soft SKIP→OK（无 native 仍 gate OK）＋prefer-c（xlang-c 先于 asm）＋soft auto-make＝portable 假绿；(2) 硬绑 `xlang check`＝暂停闸下 CHK002 假硬红；(3) 顶层 DOC／`NEXT.md` 已归档／退役仍锚假红；(4) TSV 化石符号（`runtime_diag_enabled`／`vec_u16_push`／`thread_pool_stats`／`sqlite_is_available`）。存储＝NEXT-YELLOW 闸＋archive DOC＋TSV；消费＝portable／comprehensive-check。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；拒 `NEXT.md`／顶层 DOC 复活；活符号锚；product `-o` 8 硬绿；queue／sqlite TLS／debug diag／check＝obs；CORE-018 委托 bitops；DOC `## Gate`；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 queue pop／sqlite TLS 根修／zc3–5 host-c／export-dynamic／Set MEMORY／升钉。证：Darwin run=9 **obs=4**＋neg RC=1＋L2 **5／5**；Ubuntu run=10 **obs=3**＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_next_yellow.log`／`/tmp/ubuntu_l2_ny.log`）。残：soft SKIP 邻域续扫（**…／doc-*／next-yellow soft FAIL 池空**；余 soft：邻域再扫…／Darwin ub／struct hook＝obs；queue exit=5／sqlite TLS／debug diag＝obs；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **doc-* soft SKIP→OK →硬绿** · tip **`6b3ab0333`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝(1) phase2-close／memory-safety 顶层 DOC 假锚／化石 xref（lang-unsafe／code_layer／std-http-chunked）；(2) cookbook-expand／stdlib／doc-07 phase2／3 prefer-c＋硬绑 `xlang check`＝暂停闸下假硬红／soft SKIP 无 native→exit0；(3) runtime_panic_hook check parse 残被 typeck 硬红掩盖。存储＝DOC-001／004／006／008＋STD-141／171 闸＋archive DOC；消费＝portable／comprehensive-check。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；DOC→archive＋`## Gate`；product `-o` smoke 硬绿；check＝obs；phase3 tip residual＝obs；memory-safety hooks 存在硬／exec opt-in；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 zc3–5 host-c／export-dynamic／Set MEMORY／升钉／check 根修。证：Darwin mem run=1＋p2c run=1＋stdlib run=1＋expand run=3 **obs=1**＋07p2 run=4＋07p3 run=4 **obs=1**（sqlite SEGV）＋neg RC=1＋L2 **5／5**；Ubuntu mem／p2c／stdlib／expand run=3 **obs=1**＋07p2 run=4＋07p3 run=5＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_doc_*.log`／`/tmp/ubuntu_l2_doc.log`）。残：soft SKIP 邻域续扫（**…／typeck-linear＋tst-004／doc-* soft FAIL 池空**；余 soft：邻域再扫…；cookbook check parse＝obs；phase3 Darwin sqlite＝obs；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **typeck-linear／tst-004 soft SKIP→OK →硬绿** · tip **`847543219`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝(1) typeck-linear prefer-c＋soft auto-make＋硬绑 `xlang check`＝暂停闸下 CHK002 假硬红／portable 假绿；(2) tst-004 顶层 DOC 已归档仍锚假红＋soft SKIP 无 native→exit0＋prefer-c；(3) TSV 化石 `needs_o=heap.o／channel.o` 触发 F-07 `ensure_std_c_o` 禁令＝Ubuntu ASAN 假残。存储＝M-4 linear＋TST-004 闸＋archive DOC；消费＝portable／pre-push-p5。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；linear 产品 `-o` 四负一正硬绿；check＝obs；DOC→archive＋`## Gate`；tst-004 needs_o=-；Linux+ASAN 硬绿／Darwin skip；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 doc-*／zc3–5 host-c／export-dynamic／Set MEMORY／升钉。证：Darwin linear run=5 **obs=1**＋tst004 run=0 **skip=1**＋neg RC=1＋L2 **5／5**；Ubuntu linear run=5 obs=0＋tst004 run=2＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_typeck_linear.log`／`/tmp/ubuntu_tst004.log`／`/tmp/ubuntu_l2_linear_tst004.log`）。残：soft SKIP 邻域续扫（**…／core-builtin＋mem-intrinsic＋safe-ffi／typeck-linear＋tst-004 soft FAIL 池空**；余 soft：邻域再扫…／doc-*…；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。
| 2026-08-28 | **fix(selfhost)** **core-builtin／mem-intrinsic／safe-ffi soft SKIP→OK →硬绿** · tip **`f60a94e43`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝(1) soft SKIP→OK（无 native 仍 gate OK）＋prefer-c／soft auto-make＝portable 假绿；(2) safe-ffi 顶层 DOC＋archive 双权威漂移；(3) `__builtin_*` emit 假硬锚掩盖 pure .x。存储＝CORE-008／009／SAFE-004＋run-builtin＋archive DOC；消费＝bstrict／portable。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；DOC→archive/safe＋core `## Gate`；builtin／mem／ffi 产品 `-o` 硬绿；emit undercount＝obs；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 doc-*／zc3–5 host-c／export-dynamic／Set MEMORY／升钉。证：Darwin builtin run=2 **obs=1**＋mem run=1 **obs=1**＋ffi run=9＋neg RC=1＋L2 **5／5**；Ubuntu 同形＋L2 **5／5**（`/tmp/ubuntu_core_builtin.log`／`/tmp/ubuntu_core_mem.log`／`/tmp/ubuntu_safe_ffi.log`／`/tmp/ubuntu_l2_core_safe_ffi.log`）。残：soft SKIP 邻域续扫（**…／SAFE-005＋006＋007／core-builtin＋mem-intrinsic＋safe-ffi soft FAIL 池空**；余 soft：邻域再扫…／doc-*／run-tst-004／typeck-linear…；emit tip＝obs；crash UNDEF tip＝obs；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **SAFE-005／006／007 soft SKIP→OK →硬绿** · tip **`d7fcec4d4`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝(1) soft SKIP→OK（无 native 仍 exit0／gate OK）＋prefer-c＝portable 假绿；(2) 顶层 DOC 已归档仍锚假红（crash／race 缺文件硬红；leak 双权威）；(3) crash tip `std_backtrace_collect_crash_evidence` UNDEF 被 soft silence／DOC 假红掩盖。存储＝SAFE-005／006／007 闸＋archive DOC；消费＝bstrict／ci-nightly／a09。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；DOC→archive/safe＋`## Gate`；race mutex／atomic 产品 `-o` 硬绿；leak Ubuntu ASAN 硬绿、Darwin skip；crash UNDEF／panic 无 evidence＝obs；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 crash UNDEF 根修／zc3–5 host-c／export-dynamic／Set MEMORY／升钉。证：Darwin crash run=0 **obs=2**＋race run=2 **obs=1**＋leak run=0 **skip=1**＋neg RC=1＋L2 **5／5**；Ubuntu crash run=0 **obs=2**＋race run=2 **obs=1**＋leak run=1＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_safe_crash.log`／`/tmp/ubuntu_safe_race.log`／`/tmp/ubuntu_safe_leak.log`／`/tmp/ubuntu_l2_safe.log`）。残：soft SKIP 邻域续扫（**…／TST-001＋002＋mem-boundary＋tool-fmt／SAFE-005＋006＋007 soft FAIL 池空**；余 soft：邻域再扫…／core-builtin／core-mem-intrinsic／doc-*…；crash UNDEF tip＝obs；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **TST-001／002／mem-boundary／tool-fmt soft SKIP→OK →硬绿** · tip **`21f6a85e1`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝(1) soft SKIP→OK（无 native 仍 exit0）＋prefer-c／soft auto-make＝portable 假绿；(2) 硬绑 `xlang check`＝暂停闸下 CHK002 假硬红；(3) 顶层 TST DOC 已归档仍锚假红＋`codegen.c` 化石；(4) `// case N` 标记被冲仍以化石计数假红。存储＝TST-001／002＋STD-018＋TOOL-001 闸＋archive DOC；消费＝portable／bstrict。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；DOC→archive＋`## Gate`；TSV→archive／`codegen.x`；case 计数→live 非零 `return N`；fs／string／heap／vec／map／process／fmt hooks 硬绿；io tip UNDEF／net tip exit≠0／std_mem_* tip＝obs；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 ub／struct／zc3–5 host-c／export-dynamic／Set MEMORY／升钉。证：Darwin tst001 run=2 **obs=6**＋tst002 run=4 **obs=4**＋memb run=0 **obs=2**＋fmt run=1＋neg RC=1＋L2 **5／5**；Ubuntu tst001 run=2 **obs=2**＋tst002 run=4 obs=0＋memb run=0 **obs=1**＋fmt run=1＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_tst001.log`／`/tmp/ubuntu_tst002.log`／`/tmp/ubuntu_memb.log`／`/tmp/ubuntu_fmt.log`／`/tmp/ubuntu_l2_tst_memb_fmt.log`）。残：soft SKIP 邻域续扫（**…／process＋map-vec＋core／TST-001＋002＋mem-boundary＋tool-fmt soft FAIL 池空**；余 soft：邻域再扫…／io／net／std_mem_* tip＝obs；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **process／map-vec／core-cmp／fmt／i16 soft SKIP→OK →硬绿** · tip **`5b7492519`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝(1) soft SKIP→OK（无 native 仍 exit0）＋prefer-c／soft auto-make＝portable 假绿；(2) process／map-vec 硬绑 `xlang check`＝暂停闸下 CHK002 假硬红；(3) 化石 TSV `vec_u64_append_slice`／`vec_f64_from_slice`；(4) process tip `**u8` typeck 被 soft silence／硬红掩盖。存储＝STD-023／024／013／014＋CORE-005／010／013 闸＋archive DOC；消费＝portable／bstrict。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；map／vec／cmp／fmt／i16 产品 `-o` 硬绿（vec score=10）；process pipe tip＝obs、win 非 Windows＝skip；TSV→`extend(*Vec_u64)`／`from_slice(*f64)`；DOC `## Gate`；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 ub／struct／zc3–5 host-c／export-dynamic／Set MEMORY／升钉。证：Darwin cmp／fmt／i16 run=1 **obs=1**＋mve run=2 **obs=1**＋pps run=0 **obs=2** skip=1＋neg RC=1＋L2 **5／5**；Ubuntu cmp／fmt／i16 run=1 obs=0＋mve run=2 obs=0＋pps run=0 **obs=2** skip=1＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_core_*.log`／`/tmp/ubuntu_mve.log`／`/tmp/ubuntu_pps.log`／`/tmp/ubuntu_l2_core_std_soft.log`）。残：soft SKIP 邻域续扫（**…／type-borrow＋lint＋obs-phase＋zc-copy／process＋map-vec＋core-cmp＋fmt＋i16 soft FAIL 池空**；余 soft：邻域再扫…／Darwin ub／struct hook＝obs；process pipe tip `**u8`＝obs；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **type-borrow／lint／obs-phase／zc-copy soft SKIP→OK →硬绿** · tip **`615dac438`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝(1) soft SKIP→OK（无 native 仍 `OK`＋exit0）＋prefer-c／soft auto-make＝portable 假绿；(2) borrow／lint 硬绑 `xlang check`＝暂停闸下 CHK002 假硬红；(3) 顶层 borrow DOC 双权威＋zc 顶层 DOC/SEM 已归档仍锚假红；(4) obs-phase timing miss 仍 soft SKIP→OK。存储＝TYPE-003／TOOL-002／OBS-001／ZC-007 闸＋archive DOC；消费＝portable-suite。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；borrow pos／neg＋lint clean／error 产品 `-o` 硬绿；check 层 warn／info／phase-timing tip miss＝obs；DOC→archive＋`## Gate`；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 ub／struct／zc3–5 host-c／export-dynamic／Set MEMORY／升钉。证：Darwin borrow run=6 **obs=3**＋lint run=2 **obs=3**＋obs-phase run=0 **obs=1**＋zc run=1＋neg RC=1＋L2 **5／5**；Ubuntu borrow run=6 obs=0＋lint run=2 obs=0＋obs-phase run=0 **obs=1**＋zc run=1＋neg RC=1＋L2 **5／5**（`/tmp/ubuntu_borrow.log`／`/tmp/ubuntu_lint.log`／`/tmp/ubuntu_obs_phase.log`／`/tmp/ubuntu_zc_copy.log`／`/tmp/ubuntu_l2_borrow.log`）。残：soft SKIP 邻域续扫（**…／type-zero-cost＋type-ffi-bridge／type-borrow＋lint＋obs-phase＋zc-copy soft FAIL 池空**；余 soft：邻域再扫…／Darwin ub／struct hook＝obs；read_ptr region tip＝obs；zc2 mmap／view tip＝obs；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-28 | **fix(selfhost)** **type-zero-cost／type-ffi-bridge soft SKIP→OK →硬绿** · tip **`e737266fc`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝(1) soft SKIP→OK（无 native 仍 `OK`＋exit0）＋prefer-c／soft auto-make＝portable 假绿；(2) 顶层 DOC 已归档仍锚假权威／假红；(3) 化石 `bench/loop_i32.x`／`codegen.c`／`c_type_to_buf`；(4) typeck／region 硬绑 `xlang check`＝暂停闸下 CHK002。存储＝TYPE-005／004 闸＋runner＋TSV＋archive DOC；消费＝portable-suite。**修**：prefer asm＋`XLANG_LINK_XLANG`；显式坏 XLANG／缺 native 硬 die；DOC→archive/type＋`## Gate`；bench→`r01_`／`m03_`／`r10_`／`a01_*`；codegen.x `type_to_c_repr`；compile／putchar／cstr 产品 `-o` 硬绿；typeck／region check＝obs；bcmp＝skip；报告 `run=`／`obs=`／`skip=`；**未**开 mega／**未**啃 type-borrow／lint-check／zc3–5 host-c／export-dynamic／Set MEMORY／升钉。证：Darwin zc run=1 **obs=2** skip=4＋ffi run=2＋neg RC=1＋gate OK＋L2 **5／5**；Ubuntu zc run=1 obs=0 skip=4＋ffi run=2＋neg RC=1＋gate OK＋L2 **5／5**（`/tmp/ubuntu_type_*.log`／`/tmp/ubuntu_l2_type.log`）。残：soft SKIP 邻域续扫（**…／json＋log＋math／type-zero-cost＋type-ffi-bridge soft FAIL 池空**；余 soft：邻域再扫（type-borrow-conflict／lint-check／obs-phase／zc-copy-proof…）／Darwin ub／struct hook＝obs；read_ptr region tip＝obs；zc2 mmap／view tip＝obs；Darwin math check／type-zero typeck＝obs；…）。跳：brotli ld／zc3／zc4／zc5 host-c 后置。**非软**：`--export-dynamic`；`Set_i32`+MEMORY；Darwin heat（**禁 mega**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
