# Xlang / X language 自举进度

> **更新**：2026-09-13 · tip **`bf8c2728d`**（7.2.1 P11d B-minus：`collect_imports` dest-buffer；Ubuntu 本波不验／主机重装）· 前 **`45c1c08d9`**／`cabfc5ca5` · **钉盘 `5cac88d00`** · **金标 SSH＝`ubuntu-server`** · Ubuntu 验＝**git pull（禁 scp；本波暂停）**\


> **产品 L4 放行钉盘**：**`5cac88d00`**（2026-09-10 晚二度升钉 · 双端 L4 真冷全绿＋bstrict 129/129）· 前 **`fa1061224`**／`e194cd294`／`d583f61f5`／`b5be5ed97`\
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅** · parser **7.2.2 关 pin ✅** · codegen 7.4.2 ✅ · typeck 7.4.1 ✅ · runtime monofile ✅ · present **0** · Stage 8 **30/30 FULLY CLOSED** · **7.4.3 PREFER_X_O 双端 8/8 ✅**\
> **终局债**：[C迁移追踪.md](C迁移追踪.md) · [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：主刀＝**7.2.1 P11d B-minus**（`pthin_imports.x` 有则补全 `collect_imports`；hybrid 跳过 C 孪生；opaque module 写入仍走既有 `pipeline_module_import_*`；禁抄 skip_imports；禁包 leftover collect walk AUDIT；禁开新 P 道）。leftover flatten **完**。验＝**仅 Darwin L2** 矩阵 5/5＠本波 `xlang_asm` 5467416 @07:07。Ubuntu 本波不验（主机重装）。**不升钉**。\
> **下波候选**：8.3.6 续缩仍 host-cc 的产品切片（glue_tail 包装／skip_tl skip_one_trait／parse_one_extern／primary parse 仍 C；P19 rewind／run_len 仍 C；try_skip_allow parse_into 仍 C；if_stmt parse／realign 仍 C）。lite／suite／lex_skip／helpers／primary ident／type_ref／simd／unary／binop／skip_if walks／skip_tl struct／enum／extern／impl header／generic_bound_scan／enum_register／body_tl skip／glue skip_one_function_full／try_skip_allow padding／skip_imports／consume_path／try_skip／collect_imports／ctrl brace-skip／scan_sync／library_scan／diag_late after_structs／fail／body_tl cfg_skip／diag_first_ident／lex_skip count／diag_skip_let_const_buf trampoline portable 冷回退仍留盘。pipeline_x mega 须点名。vx／summit HARD BAN。日常闸门见**提速纪律**。

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1\
> **eq 墙钟纪律（2026-09-11）**：`eq_mode`＝daily／close／full；软刀禁串行 OFF=128 全表\
> **提速纪律（2026-09-12 · 用户要求更高效率；同日改双端）**：\
> 1. **软刀闸门＝Darwin L2 矩阵＋FORCE eq smoke（`EQ_DEEP_MAX_FILE_OFF=0`／本域 `EQ_ONLY`）＋Ubuntu 同序 L2**（用户 2026-09-12「都要经过双端验证」；禁只 Darwin 结案）。\
> 2. **一波＝一条（或同链两条）ABI 根墙＋soft-FP 攀尽可解锁层**（允许多层 parse_into／exact），禁「一层 exact／一层 climb 一波」。\
> 3. **tip heat g05 立卡不挡**：矩阵／eq 用既有 `xlang_asm`；禁为软刀空等 heat 绿。\
> 4. **禁默认** drift-gate／compress／全表 close／全量 bstrict；close 仅 ABI 根大改或升钉；L4 仅整波收口／日终兜底／谈自举。\
> 5. **墙钟目标**：单刀 Darwin 验收 ≤**5 min**；单刀 Ubuntu 同序 ≤**15 min**。深层 combinator 实测可超：v5.69 3 层 JOBS=4 Darwin ~32 min（仍优于 3 波各 1 层＋3 次 Ubuntu pull）。\
> 6. **深链分批覆盖戏停（2026-09-12 用户再催）**：leftover flatten 完后，一层 25／3 层 75 的 eq 只是对已 T combinator 再跑 C↔.x，**不减 host-cc、不改 T**。实测 3 层 JOBS=4 Darwin ~32 min／Ubuntu ~41 min。**禁**再以改注释换波次再跑。下一刀＝8.3.6／7.2.1 已 T 冷孪生缩壳（真减 host-cc LOC）或 pipeline_x mega（须点名）。\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；leftover-PE 探针红不当双端 L2 产品红。

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景**：[自举时序.md](自举时序.md)\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[11](自举进度-归档-2026-09-11.md) · [10](自举进度-归档-2026-09-10.md) · [09](自举进度-归档-2026-09-09.md) · [08](自举进度-归档-2026-09-08.md) · [07](自举进度-归档-2026-09-07.md) · [06](自举进度-归档-2026-09-06.md) · [05](自举进度-归档-2026-09-05.md) · [04](自举进度-归档-2026-09-04.md) · [03](自举进度-归档-2026-09-03.md) · [02](自举进度-归档-2026-09-02.md) · [01](自举进度-归档-2026-09-01.md) · [31](自举进度-归档-2026-08-31.md) · …

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波**）。**§6 表固定 3 列**；描述禁裸 ASCII `|`。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 过期整日流水 **日切归档**后主文档重新起表。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `5cac88d00`**（2026-09-10 晚二度升钉）。\
**MG 终局达成 = wave941/942** · **双端 Stage2 = ✅** · **选题权威 =** [C迁移追踪.md](C迁移追踪.md)。\
**本波主刀 = 7.2.1 P11d B-minus**（`pthin_imports.x` 有则补全 `collect_imports`；hybrid 跳过 C 孪生；语言无 local u8[N]，C trampoline 持 128 字节 path／bind dest buf；module 写入走既有 pipeline_module_import_*；禁抄 skip_imports；禁包 leftover collect walk AUDIT；禁开新 P 道）。leftover flatten **完**。立卡：MSVC va／WIN leftover／vx daily／summit HARD BAN／pipeline_x mega 须点名。leftover-to-audit unique **0**。leftover_helpers 余 **0**。残＝产品 `.inc` 切片仍 host-cc（含 glue_tail 包装、primary parse、P19 rewind／run_len、skip_tl skip_one_trait／parse_one_extern、try_skip_allow parse_into、if_stmt parse／realign）；lite／suite／lex_skip／helpers／primary ident／type_ref／simd／unary／binop／skip_if walks／skip_tl struct／enum／extern／impl header／generic_bound_scan／enum_register／body_tl skip／glue skip_one_function_full／try_skip_allow padding／skip_imports／consume_path／try_skip／collect_imports／ctrl brace-skip／scan_sync／library_scan／diag_late after_structs／fail／body_tl cfg_skip／diag_first_ident／lex_skip count portable 文件留作冷回退；`parser_asm_thin_c.from_x.c` 443KiB 仅冷 rest 回退（G-02f-330 全切片齐则 omit rest）。

| 指标 | 状态 |
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`5cac88d00`**；residual tip present **0**；Stage 8 **30/30**；PRODUCT RETIRED 23/23 |
| Track MG | **11.3.1 MG ✅** |
| Track BC | **🟢 Stage8 30/30** · M4 **5/5** · present **0** |
| Track L | **收 30/30 FULLY CLOSED** |
| mac / Ubuntu L4（钉盘） | ✅ 双端 L4 + bstrict **129/129** @ **`5cac88d00`** |
| pin 蛋 | ✅ darwin.arm64 + linux.x86_64 |
| 本波构建级别 | **L2（仅 Darwin）**：矩阵 5/5＠本波 `xlang_asm` 5467416 @07:07。Ubuntu 本波不验。**不升钉** |

***

## 1. 文档地图

| 文档 | 看什么 |
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | 先→后全景 · S0–S8 |
| [C迁移追踪.md](C迁移追踪.md) | 终局债 / BC·PC DAG |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild（MG ✅） |
| **本文** | KPI + 前排 ≤3 + **§6**（**9 月 13 日**起；12 日行仍在表内，待日切归档） |
| [自举进度-归档-2026-09-11.md](自举进度-归档-2026-09-11.md) | **9月11日** B-minus v5.13→v5.29＋eq≤11s＋Cap 4.2.8（**32** 行） |
| [自举进度-归档-2026-09-10.md](自举进度-归档-2026-09-10.md) | **9月10日** B-minus v1→v5.12＋两度升钉＋7.4.4／g05／8.3.6（**73** 行） |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图（本波不改） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`5cac88d00`** | 2026-09-10 晚二度升钉；双端 L4 + bstrict 129/129 |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自钉盘 L4 `xlang_asm` 刷新 |
| residual tip（BC） | present **0** | Stage 8 30/30 · prefer pure-asm 默认 |
| mac 钉盘 L4 | ✅ L4 + bstrict **129/129** | `@5cac88d00` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 + bstrict **129/129** | `@5cac88d00` `ubuntu-server` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | **本刀 Darwin 5/5**＠新链 `xlang_asm` 5467416 @07:07。Ubuntu 本波不验（主机重装） |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ 双端 tip SHA256 STRICT match | 钉盘历史见归档 |
| **R2 / Cap** | 按需 | 明细见归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1** |
| `TREE_ARMED` / `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1** / **1** |
| post-delete residual | **✅** wave944–964 |

***

## 3. 当前前排（≤3 条）

1. **下一刀（真减 host-cc）** — 本刀 P11d 已把 `collect_imports` 收进 `pthin_imports.x`（opaque module；path／bind dest-buffer）。续＝8.3.6 仍 host-cc 的产品 `.inc`（glue_tail 包装／skip_tl skip_one_trait／parse_one_extern／primary parse 仍 C；P19 rewind／run_len 仍 C；try_skip_allow parse_into 仍 C；if_stmt parse／realign 仍 C）。lite／suite／lex_skip／helpers／primary ident／type_ref／simd／unary／binop／skip_if walks／skip_tl struct／enum／extern／impl header／generic_bound_scan／enum_register／body_tl skip／glue skip_one_function_full／try_skip_allow padding／skip_imports／consume_path／try_skip／collect_imports／ctrl brace-skip／scan_sync／library_scan／diag_late after_structs／fail／body_tl cfg_skip／diag_first_ident／lex_skip count portable 冷回退仍留盘。pipeline_x mega 须点名。summit／peak／zenith／versal／vx 仍 HARD BAN。Windows 卡离线不挑。Ubuntu 主机重装好后再补同序 L2。
2. **立卡（Windows）** — **10.7.1 MSVC va**＋ **seed 第二 WIN-leftover addr_of**（须切 Windows）。
3. **立卡不动** — eq 深链指数墙；spawn 真体仍 T；nest 冻帽 64。本刀 Darwin g05 ensure＋pure-ld 绿（heat 未挡）。钉盘 **`5cac88d00`**。

***

## 4. 与归档的关系

- **9月11日归档**：[自举进度-归档-2026-09-11.md](自举进度-归档-2026-09-11.md) — **32** 行 · B-minus v5.13→v5.29＋eq 墙钟根治＋Cap 4.2.8。
- **9月10日归档**：[自举进度-归档-2026-09-10.md](自举进度-归档-2026-09-10.md) — **73** 行 · B-minus v1→v5.12＋两度升钉。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `5cac88d00`**。
- 日切后主文档只保留 KPI + 前排 + **9 月 12 日** §6。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
| **macOS arm64** | 钉盘 L4＠**`5cac88d00`** · 软刀 L2 验收端；本刀矩阵 5/5＠新链 `xlang_asm` 5467416 @07:07 |
| **Ubuntu x86_64（金标）** | 钉盘 L4＠**`5cac88d00`** · **本波不验**（主机重装）；前刀 L2 绿＠`b00c7d9db`；SSH＝**`ubuntu-server`** |
| **Windows MSYS2** | leftover unique **0** · min-gate 绿 · MSVC va／第二 leftover 离线立卡 |

***

## 6. 变更记录（倒序 · **9 月 13 日**；12 日行暂留；11 日见 [归档](自举进度-归档-2026-09-11.md)）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-13 | **feat(selfhost)** **7.2.1 P11d B-minus：imports.x 补 collect_imports dest-buffer** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P11 `imports.inc` 在 P11c 后仍 host-cc collect_imports（`const name = import("path");` token 走查可 B-minus 复用 P11c consume_path＋P18 cfg_skip＋P1b copy＋P9a 桥；module 写入仍 C helper）；存储＝有则补全 `pthin_imports.x`＋C trampoline（语言无 local u8[N]）＋`#ifdef XLANG_PTHIN_IMPORTS_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁抄 skip_imports（fail-leave 在 CONST vs 其后）；禁包 leftover collect walk AUDIT；禁开新 P 道。**g05 `-E`+cc**（pure-asm CG002，同 P11c）。实测 hybrid P11 C 3784 vs 冷 6104；glue.o 1972792；xlang 5467416；g05 ensure P11b／P11c／P11d＋P11 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5467416 @07:07。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `45c1c08d9`（7.2.1 P12e B-minus enum_register dest-buffer；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P12e B-minus：skip_tl.x 补 enum_register／append_variants** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P12 `skip_tl.inc` 在 P12d 后仍 host-cc skip_one_enum_register／append_variants（`enum Name { V, … }` token 走查可 B-minus 复用 P1b copy＋P9a 桥；module 写入仍 C helper）；存储＝有则补全 `pthin_skip_tl.x`＋C trampoline（语言无 local u8[N]）＋`#ifdef XLANG_PTHIN_SKIP_TL_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁包 skip_one_trait；禁抄 skip_one_enum（opaque brace skip 会丢变体）；禁开新 P 道。**g05 `-E`+cc**（pure-asm CG002，同 P12c／P12d）。实测 hybrid P12 C 93824 vs 冷 102056；glue.o 1972168；xlang 5467144；g05 ensure P12b／P12c／P12d／P12e＋P12 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5467144 @06:49。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `61ce5b0b5`（7.2.1 P12d B-minus generic_bound_scan dest-buffer；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P12d B-minus：skip_tl.x 补 generic_bound_scan dest-buffer** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P12 `skip_tl.inc` 在 P12c 后仍 host-cc generic_bound_scan（全文件 `<T: Trait>`／type-param／`callee<A,B>` token 走查可 B-minus 复用 P1b skip_generic_angle＋copy＋P9a 桥；file-local bound／call／gp 表仍 C）；存储＝有则补全 `pthin_skip_tl.x`＋C trampoline（语言无 file-local 静态表）＋`#ifdef XLANG_PTHIN_SKIP_TL_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁包 skip_one_trait；禁再抄 skip_generic_angle／copy_slice；禁开新 P 道。**g05 `-E`+cc**（pure-asm CG002，同 P12c）。实测 hybrid P12 C 94632 vs 冷 102056；glue.o 1970984；xlang 5450136；g05 ensure P12b／P12c／P12d＋P12 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5450136 @06:29。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `4a4c9ffd5`（7.2.1 P12c B-minus skip_one_impl dest-buffer；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P12c B-minus：skip_tl.x 补 skip_one_impl dest-buffer** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P12 `skip_tl.inc` 在 P12b 后仍 host-cc skip_one_impl（`impl [ <T> ] Type [ for Type ] {` 头走查可 B-minus 复用 P1b skip_generic_angle＋copy＋P9a 桥；file-local impl-seen 表仍 C）；存储＝有则补全 `pthin_skip_tl.x`＋C trampoline（语言无 file-local 静态表）＋`#ifdef XLANG_PTHIN_SKIP_TL_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁包 skip_one_trait；禁再抄 skip_generic_angle／copy_slice；禁开新 P 道；禁 skip_balanced_braces（wave390 留方法给外层 parse_into）。**g05 `-E`+cc**（pure-asm CG002，P12b 仅 struct／enum／extern 时曾过）。实测 hybrid P12 C 98808 vs 冷 102056；glue.o 1968424；xlang 5449704；g05 ensure P12b／P12c＋P12 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5449704 @06:07。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `a39d42e5b`（7.2.1 P17c G.7 diag_skip_let_const_buf trampoline over P18b into；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P17c G.7：diag_skip_let_const_buf trampoline over P18b into** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P17 `diag_late.inc` 在 P17b 后仍 host-cc buf C 走查孪生（相对 P18b into 缺 U8／U32／U64／USIZE 与 `[]` 后缀）；存储＝G.7 有则补全：buf 改 trampoline over P18b into_slice（同 body_skip_buf；禁新 .x 导出）；消费＝`parser_asm_thin_glue.o`。修＝杀掉双权威；禁再抄 into；禁开新 P 道。实测 hybrid P17 C 2568 vs 冷 5560；glue.o 1968128；xlang 5449336；g05 ensure P17b／P17c＋P17 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5449336 @05:49。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `d26bb4d4c`（7.2.1 P1c lex_skip.x 补 skip_generic_angle_list_count；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P1c B-minus：lex_skip.x 补 skip_generic_angle_list_count** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P1 `lex_skip.inc` 在 P1b 后仍 host-cc count＋pending 走查（`<T,U>` 计数＋声明位 IDENT 捕获可 B-minus 复用同文件 skip_generic_angle_into＋stmt-boundary＋copy＋P9a 桥；g_gp_pending_* 静态表仍 C）；存储＝有则补全 `pthin_lex_skip.x`＋C trampoline（语言无 global u8[8][64]）＋`#ifndef XLANG_PTHIN_LEX_SKIP_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁再抄 skip_generic_angle_list_into；禁开新 P 道。**g05 `-E`+cc**（pure-asm CG002，同 P1b）。实测 hybrid P1 C 6240 vs 冷 10208；glue.o 1969480；xlang 5449128；g05 ensure P1b／P1c＋P1 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5449128 @05:32。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `999b3d17d`（7.2.1 P18c body_tl.x 补 cfg_skip／diag_first_ident；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P18c B-minus：body_tl.x 补 cfg_skip／diag_first_ident** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P18 `body_tl.inc` 在 P18b 后仍 host-cc cfg_skip／diag_first_ident（`#[cfg]` 后顶层 CONST／LET／STRUCT／FUNCTION 顺序分发＋function 后首 IDENT ident_len 可 B-minus 复用同文件 skip_one_top_level＋P12b skip_one_struct＋P10b skip_one_function_full＋P9a 桥；P010–P014／onefunc_param_name_dup 仍 C）；存储＝有则补全 `pthin_body_tl.x`＋C trampoline（语言无 lexer_init）＋`#ifndef XLANG_PTHIN_BODY_TL_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁再抄 skip_one_struct／skip_one_function_full／skip_one_if；禁开新 P 道。**pure-asm 过**。实测 hybrid P18 C 6696 vs 冷 8688；glue.o 1968136；xlang 5448744；g05 ensure P18b／P18c＋P18 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5448744 @05:11。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `cba46b110`（7.2.1 P11c imports.x 补 consume_path／try_skip；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P11c B-minus：imports.x 补 consume_path／try_skip** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P11 `imports.inc` 在 P11b 后仍 host-cc consume_path／try_skip（`import("path");` 顺序走查＋IDENT＝import 失败还原可 B-minus 复用 P1b copy＋P9b validate／finalize＋P9a 桥；collect／copy_token_bytes 仍 C）；存储＝有则补全 `pthin_imports.x`＋C trampoline（语言无 local u8[128]）＋`#ifndef XLANG_PTHIN_IMPORTS_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；禁再抄 copy_slice／validate／finalize；禁开新 P 道；禁包 leftover collect walk AUDIT。**g05 `-E`+cc**（pure-asm CG002，同 P1b／P5b／P19b）。实测 hybrid P11 C 4808 vs 冷 6104；glue.o 1966880；xlang 5448264；g05 ensure P11b／P11c＋P11 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5448264 @04:47。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `f2c47b375`（7.2.1 P17b diag_late.x 跳过 after_structs／fail 走查；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P17b B-minus：编 diag_late.x 跳过 after_structs／fail 走查** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P17 `diag_late.inc` 仍 host-cc（after_structs STRUCT 循环＋fail_at_token_kind 顺序走查可 B-minus 复用 P12b skip_one_struct＋P18b scalar／body_skip＋P1b is_pointee＋P9a 桥；diag_skip_let_const_buf／body_skip_buf 仍 C）；存储＝P17b `g05_try_x_to_o`＋`XLANG_PTHIN_DIAG_LATE_BODIES_FROM_X`＋`#ifdef XLANG_PARSER_STRETCH_AUDIT`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；C 留 trampoline＋buf skip；禁再抄 skip_one_struct／body_skip／is_fn_sig／is_pointee；stash 仍 C。**pure-asm 过**（无 CG002）。实测 hybrid P17 C 3920 vs 冷 6856；glue.o 1965160；xlang 5447368；g05 ensure P17b＋P17 C（移到 P18b 后）＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5447368 @04:24。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `51ce1626d`（7.2.1 P15b library.x 跳过 library_scan 走查；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P15b B-minus：编 library.x 跳过 library_scan 走查** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P15 `library_wrap.inc` 仍 host-cc（library_scan ~190 行 token 走查＋into／buf ~1500 行已 T AUDIT 前缀含 HARD BAN 名可 B-minus／ifdef；into／buf parse／lex_from 仍 C）；存储＝P15b `g05_try_x_to_o`＋`XLANG_PTHIN_LIBRARY_BODIES_FROM_X`＋`#ifdef XLANG_PARSER_STRETCH_AUDIT`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 scan 走查；C 留 trampoline＋into／buf；禁再抄 P1b copy。**pure-asm 过**（无 CG002）。实测 hybrid P15 C 2592 vs 冷 4952；glue.o 1962272；xlang 5430136；g05 ensure P15b＋P15 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5430136 @04:02。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `f3ca4e1cb`（7.2.1 P5c ctrl.x 补 scan_sync pos；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P5c Route C：ctrl.x 补 scan_sync pos** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P5 `if_stmt.inc` 在 P5b 后仍 host-cc scan_sync（注释感知找 then `{`／else／else-if 循环；parse／realign 仍 C）；存储＝有则补全 `pthin_ctrl.x` pos 导出＋C by-value trampoline（语言无结构体按值）；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 scan_sync；禁再抄 P1b token skip_balanced／P9b skip_ws；禁包 leftover else-if AUDIT；禁迁 dead `sync_lex_after_if_cond_paren`。**g05 `-E`+cc**（pure-asm CG002，同 P5b／P1b／P19b）。实测 hybrid P5 C 19072 vs 冷 23184；glue.o 1958080；xlang 5429208；g05 ensure P5b／P5c＋P5 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5429208 @03:41。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `b3c02e46e`（7.2.1 P5b 编 ctrl.x 跳过注释感知花括号字节扫描／kw_at_pos；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P5b Route C：编 ctrl.x 跳过注释感知花括号字节扫描／kw_at_pos** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P5 `if_stmt.inc` 仍 host-cc（注释感知 `{...}` 字节扫描／kw_at_pos 可 Route C；parse／scan_sync／match／if_expr 仍 C；~770 行已 T AUDIT 前缀含 HARD BAN 名可 ifdef）；存储＝P5b `g05_try_x_to_o`＋`XLANG_PTHIN_CTRL_BODIES_FROM_X`＋`#ifdef XLANG_PARSER_STRETCH_AUDIT`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接字节扫描／kw_at_pos；C 留 slice trampoline＋parse／scan_sync；禁再抄 P1b token skip_balanced；skip_ws 仍 P9b；禁包 leftover else-if AUDIT。**g05 `-E`+cc**（pure-asm CG002，同 P1b／P19b）。实测 hybrid P5 C 21216 vs 冷 23184；glue.o 1957176；xlang 5428376；g05 ensure P5b＋P5 C＋P11b＋P13b＋P10b＋P9a＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5428376 @03:22。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `efd9c29b7`（7.2.1 P11b 编 imports.x 跳过 skip_imports 走查；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P11b B-minus：编 imports.x 跳过 skip_imports 走查** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P11 `imports.inc` 仍 host-cc（skip_imports 走查＋skip_imports_buf AUDIT＋collect_imports ~1500 行已 T AUDIT 前缀含 HARD BAN 名可 B-minus／ifdef；try_skip／consume_path／collect 走查仍 C）；存储＝P11b `g05_try_x_to_o`＋`XLANG_PTHIN_IMPORTS_BODIES_FROM_X`＋peek_int_val 桥补全；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 skip_imports 循环；C 留 trampoline＋try_skip／collect；禁再抄 consume_path。**pure-asm 过**（无 CG002）。实测 hybrid P11 C 6176B vs 冷 6104B；glue.o 1955352；xlang 5427720；g05 ensure P11b＋P11 C＋P13b＋P10b＋P9a＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5427720 @03:03。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `9b4d7db48`（7.2.1 P13b 编 try_skip_allow.x 跳过 padding 走查；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P13b B-minus：编 try_skip_allow.x 跳过 padding 走查** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P13 `try_skip_allow.inc` 仍 host-cc（padding_struct slice／buf ~1500 行已 T AUDIT 探针含 HARD BAN 名＋peek LPAREN／skip_balanced 走查可 B-minus 复用 P1b skip_balanced＋P9a 桥；write_result／parse_into 仍 C）；存储＝P13b `g05_try_x_to_o`＋`XLANG_PTHIN_TRY_SKIP_ALLOW_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；C 留 trampoline＋write_result／parse_into；禁再抄 skip_balanced。**pure-asm 过**（无 CG002）。实测 hybrid P13 C 1936B vs 冷 2248B；glue.o 1953472；xlang 5427080；g05 ensure P13b＋P13 C＋P10b＋P12b＋P18b＋P9a＋P1b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5427080 @02:39。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `26b1c5dab`（7.2.1 P10b 编 glue.x 跳过 skip_one_function_full 走查；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P10b B-minus：编 glue.x 跳过 skip_one_function_full 走查** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P10 `glue_tail.inc` 仍 host-cc（skip_one_function_full ~870 行已 T AUDIT 探针含 HARD BAN 名＋async／export／function／IDENT／parens／braces 走查可 B-minus 复用 P1b skip_balanced＋P12b skip_one_extern＋P9a 桥；glue 包装／parse glue 仍 C）；存储＝P10b `g05_try_x_to_o`＋`XLANG_PTHIN_GLUE_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；C 留 trampoline＋包装；禁再抄 skip_balanced／skip_one_extern。**pure-asm 过**（无 CG002）。实测 hybrid P10 C 35976B vs 冷 38464B；glue.o 1952712；xlang 5410280；g05 ensure P10b＋P10 C＋P12b＋P18b＋P9a＋P1b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5410280 @02:24。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `c77a76358`（7.2.1 P18b 编 body_tl.x 跳过 scalar 表＋let／const／if skip 走查；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P18b Route C＋B-minus：编 body_tl.x 跳过 scalar 表＋let／const／if skip 走查** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P18 `body_tl.inc` 仍 host-cc（is_fn_sig_scalar 可 Route C；diag_skip／body_skip ~760 行已 T AUDIT 探针含 HARD BAN 名＋skip_one_top_level let／const 可 B-minus 复用 P14b skip_one_if＋P9a 桥；P010–P014／diag_first_ident／cfg_skip 仍 C）；存储＝P18b `g05_try_x_to_o`＋`XLANG_PTHIN_BODY_TL_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接表＋走查；C 留 trampoline＋diag／cfg；禁再抄 skip_one_if。**pure-asm 过**（无 CG002）。实测 hybrid P18 C 7256B vs 冷 8688B；glue.o 1952936；xlang 5410024；g05 ensure P18b＋P18 C＋P14b＋P12b＋P9a＋P1b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5410024 @02:07。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `fc9458c6e`（7.2.1 P12b 编 skip_tl.x 跳过 struct／enum／extern 走查；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P12b B-minus：编 skip_tl.x 跳过 struct／enum／extern 走查** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P12 `skip_tl.inc` 仍 host-cc（skip_one_struct ~800 行已 T AUDIT 探针＋enum／extern 走查可 B-minus 复用 P1b skip_balanced／skip_generic_angle＋P9a 桥；trait-reg／enum register／parse_one_extern 仍 C）；存储＝P12b `g05_try_x_to_o`＋`XLANG_PTHIN_SKIP_TL_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；C 留 trampoline＋stash_source＋trait-reg；禁再抄 skip_balanced／skip_generic_angle。**pure-asm 过**（无 CG002）。实测 hybrid P12 C 100032B vs 冷 102056B；glue.o 1949616；xlang 5409288；g05 ensure P12b＋P12 C＋P14b＋P4bb＋P4ub＋P7b＋P3b＋P4b＋P9a＋P1b＋P19b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5409288 @01:50。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `82f592c45`（7.2.1 P14b 编 skip_if.x 跳过 trait／impl＋if 走查；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P14b B-minus：编 skip_if.x 跳过 trait／impl＋if 走查** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P14 `skip_if.inc` 仍 host-cc（trait／impl＋if-core／statement 可 B-minus 复用 P1b skip_balanced＋P9a 桥；enum register 仍 C；~780 行已 T AUDIT 探针产品侧是 nop）；存储＝P14b `g05_try_x_to_o`＋`XLANG_PTHIN_SKIP_IF_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接走查；C 留 trampoline＋enum；禁再抄 skip_balanced。**pure-asm 过**（无 CG002）。实测 hybrid P14 C 2560B vs 冷 5232B；glue.o 1948088；xlang 5408776；g05 ensure P14b＋P14 C＋P4bb＋P4ub＋P7b＋P3b＋P4b＋P9a＋P1b＋P19b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5408776 @01:34。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `c505c6ac5`（7.2.1 P4bb 编 binop.x 跳过 TOKEN→ExprKind；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P4bb Route C：编 binop.x 跳过 TOKEN→ExprKind** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P4b `expr_binop.inc` 仍 host-cc（5 级 TOKEN if 可 Route C；parse／wrap／peek／single_tok_chain 仍 C；parse_logor ~700 行已 T AUDIT 探针产品侧是 nop）；存储＝P4bb `g05_try_x_to_o`＋`XLANG_PTHIN_EXPR_BINOP_BODIES_FROM_X`＋`#ifdef XLANG_PARSER_STRETCH_AUDIT`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 TOKEN→ExprKind；C 留 wrap＋parse；禁与 unary 表合并（STAR／AMP／MINUS 一元≠二元）。**pure-asm 过**（无 CG002）。实测 hybrid P4b C 9448B vs 冷 10024B；glue.o 1947112；xlang 5408232；g05 ensure P4bb＋P4b C＋P4ub＋P7b＋P3b＋P4b＋P9a＋P1b＋P19b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5408232 @01:16。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `8519db305`（7.2.1 P4ub 编 unary.x 跳过 TOKEN→ExprKind；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P4ub Route C：编 unary.x 跳过 TOKEN→ExprKind** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P4u `unary.inc` 907 行仍 host-cc（TOKEN→ExprKind 可 Route C；parse／wrap／arena 仍 C；~700 行已 T AUDIT 探针产品侧是 nop）；存储＝P4ub `g05_try_x_to_o`＋`XLANG_PTHIN_EXPR_UNARY_BODIES_FROM_X`＋`#ifdef XLANG_PARSER_STRETCH_AUDIT`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 TOKEN→ExprKind；C 留 wrap＋parse；8 路 if 收敛为一表。**pure-asm 过**（无 CG002）。实测 hybrid P4u C 2768B vs 冷 3064B；glue.o 1945600；xlang 5407928；g05 ensure P4ub＋P4u C＋P7b＋P3b＋P4b＋P9a＋P1b＋P19b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5407928 @01:00。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `32f8d0fca`（7.2.1 P7b 编 simd.x 跳过 shuffle／select ident／callee 名；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P7b Route C：编 simd.x 跳过 shuffle／select ident／callee 名** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P7 `simd_builtin.inc` 212 行仍 host-cc（shuffle／select ident 与 callee 名填充可 Route C；parse／arena 仍 C）；存储＝P7b `g05_try_x_to_o`＋`XLANG_PTHIN_SIMD_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 ident pack／callee 名；C 留 slice trampoline＋parse。**pure-asm 过**（无 CG002）。实测 hybrid P7 C 3024B vs 冷 3896B；glue.o 1945832；xlang 5407592；g05 ensure P7b＋P7 C＋P3b＋P4b＋P9a＋P1b＋P19b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5407592 @00:47。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `291506d74`（7.2.1 P3b 编 type_ref.x 跳过 kind／dyn／builtin TypeKind／vector IDENT；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P3b Route C：编 type_ref.x 跳过 kind／dyn／builtin TypeKind／vector IDENT** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P3 `type_ref.inc` 1036 行仍 host-cc（token_starts_type／ident_is_dyn／TOKEN→TypeKind／vector IDENT 可 Route C；parse／arena 仍 C；skip_tl 另有一份 TOKEN→TypeKind 静态表）；存储＝P3b `g05_try_x_to_o`＋`XLANG_PTHIN_TYPE_REF_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 portable 区；C 留 trampoline＋parse；skip_tl `xlang_trait_token_to_type_kind_c` 转调 `parser_asm_type_ref_builtin_kind_ord_c`（一表）。**pure-asm 过**（无 CG002）。实测 hybrid P3 C 16160B vs 冷 18376B；glue.o 1942912；xlang 5406920；g05 ensure P3b＋P3 C＋P4b＋P9a＋P9b＋P1b＋P19b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5406920 @00:34。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `03afbadc2`（7.2.1 P4b 编 primary.x 跳过 ident 拼写／asm-option-bit；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-13 | **feat(selfhost)** **7.2.1 P4b Route C：编 primary.x 跳过 ident 拼写／asm-option-bit** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P4 `primary.inc` 2947 行仍 host-cc（8 个 IDENT 拼写／option-bit 可 Route C；`parse_primary_into` ~770 行已 T AUDIT 探针产品侧是 nop；parse／arena 仍 C）；存储＝P4b `g05_try_x_to_o`＋`XLANG_PTHIN_EXPR_PRIMARY_BODIES_FROM_X`＋`#ifdef XLANG_PARSER_STRETCH_AUDIT`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 ident 拼写／option-bit；C 留 slice trampoline＋parse；helpers `ident_is_unsafe_stmt` 仍 C（by-value ABI，本波不合并）。**pure-asm 过**（无 CG002）。实测 hybrid P4 C 40512B vs 冷 43096B；glue.o 1937840；xlang 5389512；g05 ensure P4b＋P4 C＋P9a＋P9b＋P1b＋P19b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5389512 @00:13。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `ab4945a55`（7.2.1 P19b 编 helpers.x 跳过 kind／copy／pos／match-kw；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 P19b Route C：编 helpers.x 跳过 kind／copy／pos／match-kw** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P19 `helpers.inc` 1363 行仍 host-cc（标量 kind／buf 拷／pos／match-kw 可 Route C；`first_token_kind_buf` ~880 行已 T AUDIT 探针产品侧是 nop；rewind／run_len 仍 C）；存储＝P19b `g05_try_x_to_o`＋`XLANG_PTHIN_HELPERS_BODIES_FROM_X`＋`#ifdef XLANG_PARSER_STRETCH_AUDIT`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 kind／copy／pos／match-kw；C 留 by-value trampoline＋rewind；TOKEN_* `_Static_assert` 钉 `token.h`；stretch 表仍 P9b。pure-asm CG002（label patch），g05 走 `-E`+cc。实测 hybrid P19 C 20 T／8.8KiB vs 冷 23 T／9.7KiB；glue.o 1565856；g05 ensure P19b＋P19 C＋P9a＋P9b＋P1b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5388216 @23:54。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `0f728a641`（7.2.1 P1b 编 lex_skip.x 跳过 kind／copy／skip_walk；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 P1b Route C＋B-minus：编 lex_skip.x 跳过 kind／copy／skip_walk** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P1 `lex_skip.inc` 555 行仍 host-cc（3 标量＋4 buf 拷＋skip_balanced／generic_into 可 B-minus；count＋pending 全局仍 C）；存储＝P1b `g05_try_x_to_o`＋`XLANG_PTHIN_LEX_SKIP_BODIES_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：`.x` 承接 kind／copy／inplace skip；C 留 by-value trampoline＋count；TOKEN_* `_Static_assert` 钉 `token.h`；P1b 依赖 P9a 桥（禁 UNDEF peek／step）。pure-asm CG002（label patch），g05 走 `-E`+cc。实测 hybrid P1 C 20 T／6.6KiB vs 冷 26 T／10KiB；glue.o 1566072；g05 ensure P1b＋P1 C＋P9a＋P9b＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5387400 @23:38。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 Route C 产品化：P9b 编 stretch.x 跳过 lite** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝lite 13 标量表已在 `pthin_stretch.x` 而 P9 仍 host-cc `.inc` 564 行；存储＝P9b `g05_try_x_to_o`＋`XLANG_PTHIN_STRETCH_LITE_FROM_X`；消费＝`parser_asm_thin_glue.o`。修＝G.7 有则补全：hybrid 跳过 lite include（同 P9a 跳 suite）；classify TOKEN_* `_Static_assert` 钉 `token.h`（禁静默双权威）；冷 lane 仍 include lite。pure-asm 对全局 `u8[]` 仍 CG002，g05 走 `-E`+cc。实测 hybrid P9 C 仅 2 T 标记／640B；glue.o 1565808；g05 ensure P9b＋P9a＋P9 C＋pure-ld。验＝Darwin 矩阵 5/5＠`xlang_asm` 5386488 @23:16。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 已 T 冷孪生缩壳：P9 跳过 suite 46k** | **代码＋文档** · **不升钉** · **L2（仅 Darwin）**。五问：产生＝P9 `pthin_stretch.from_x.c` 在 P9a 已提供 1978 T 后仍 `#include` suite_slice.inc（46k／2.6MiB，1978 体已 `#ifndef` 但仍预处理）；存储＝P9 hybrid 宏 `XLANG_PTHIN_STRETCH_AUDIT_FROM_X`；消费＝`parser_asm_thin_glue.o` host-cc。修＝G.7 有则补全：hybrid 跳过 suite include；classify／score 从 suite 迁 lite（token.h 权威，一处 C 定义）；冷 lane 仍 include suite。实测 hybrid P9 `-E` 2917 行／0.02s／9.2KiB vs 冷 32954 行／0.19s／843KiB。glue.o 仍 1.57MiB（P9a `.x`＋其它 P 切片未动）。拒编 stretch.x（TOKEN 钉拷风险）。拒深链 eq。验＝Darwin g05 ensure＋pure-ld＋矩阵 5/5＠`xlang_asm` 5369128 @23:02。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `37d383ac5`（停 深链分批覆盖戏；Darwin 矩阵 5/5；Ubuntu 本波不验）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **停 深链分批覆盖戏（v5.70 中止）** | **文档** · **不升钉** · **L2（仅 Darwin）**。用户否决「改注释再跑 30min eq」。五问：产生＝leftover flatten 完后 combinator 已是 T，eq 不迁码；存储＝`k_eq_nest_skip`／k_cases；消费＝7.2.1b 深链分批。根因＝覆盖戏违反效率纪律（须真减 host-cc LOC 或 present−1）。修＝杀 Darwin 3 层 eq；回滚 v5.70 注释；前排改 8.3.6／7.2.1 已 T 冷孪生缩壳（`parser_asm_thin_c.from_x.c` 仍 443KiB host-cc）。classify／score 留 stretch.x。pipeline_x mega 须点名。Darwin 矩阵 5/5＠既有 `xlang_asm` 5368904 @15:07。Ubuntu 本波不验（主机重装）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `c7cbda05a`（深链分批 3 层 75／150 表驱动双端绿）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **深链分批 3 层 75／150 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu `git pull --ff-only` `cc03334c8..b00c7d9db`。xlang／xlang_asm／xlang-c **5788944** @18:16（非旧 PP002 5755448）。矩阵 5/5 rv42／opt102／hello0／si0／f32。eq daily **225 OK**（3 层 exact 75／2031／JOBS=4／deep_off=0／EQ_SKIP_SYNTH=1／STRIDE=4）。墙钟 ~41 min 含 pull。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `c7cbda05a`（深链分批 3 层 75／150 表驱动；Darwin L2 绿）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.69：深链分批 smoke 3 层 75／150 表驱动精确过滤** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。用户要求更高效率；提速纪律第 2 条禁一层 exact 一波。五问：产生＝一层 25 条再双端＝吞吐过低；存储＝k_eq_nest_skip 一表（全链 hyper_mega→intergalactic_galactic）；消费＝7.2.1b leftover 后深链分批。修＝G.7 有则补全表驱动 `eq_tok_hits_name`（不再每波加 if）＋ daily 多 token 走 JOBS=4 shard。实测 6 层 JOBS=4＞15 min 被杀，本波收 versal 墙前前 3 层（ultimate_supreme／absolute_ultimate／transcendent_absolute；75 exact／2031）。HARD BAN 跳过 summit／peak／zenith／pinnacle_zenith／universal_*。拒抄 classify／score。验＝**双端 L2**：Darwin 矩阵 5/5＋eq **225 OK**（~32 min）；Ubuntu 矩阵 5/5＋eq **225 OK**（JOBS=4；exact 75／2031）。leftover_helpers 余 **0**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `cc03334c8`（深链分批 supreme_crown 25／270 精确过滤双端绿）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **深链分批 supreme_crown 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu `git pull --ff-only` `11dac830d..cc03334c8`。xlang／xlang_asm／xlang-c **5788944** @18:16（非旧 PP002 5755448）。矩阵 5/5 rv42／opt102／hello0／si0／f32。eq daily **75 OK**（supreme_crown exact 25／2031／deep_off=0／EQ_SKIP_SYNTH=1／STRIDE=4；禁吞 350 ultimate_supreme）。墙钟 ~211s。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.68：深链分批 smoke supreme_crown 25／270 精确过滤** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝`EQ_ONLY=supreme_crown` naive strstr 吞 ultimate_supreme＋（25 exact vs 375 hits／350 余）；存储＝k_cases 名＋is_deep_climb_name 已含 crown；消费＝7.2.1b leftover 后深链分批。HARD BAN 跳过 summit／peak／zenith／pinnacle_zenith。修＝G.7 有则补全 `eq_tok_hits_name`：hit 前为 `ultimate_` 则跳过；daily 逗号整词 `supreme_crown` 默认 SKIP_SYNTH=1＋STRIDE=4＋DEEP_MAX_FILE_OFF=0。拒抄 classify／score；拒 daily `*,*summit*`／`*,*peak*`／`*,*zenith*`／`*,*vx*`；拒把 crown／pinnacle 移出 is_deep_climb_name。验＝**双端 L2**：Darwin 矩阵 5/5＋eq **75 OK**（~180s）；Ubuntu 矩阵 5/5＋eq **75 OK**（exact 25／2031；deep_smoke 25＋null-guard；大文件 deep skip）。leftover_helpers 余 **0**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `11dac830d`（深链分批 crown_pinnacle 25／270 精确过滤双端绿）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **深链分批 crown_pinnacle 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu `git pull --ff-only` `013a1e4a2..11dac830d`。xlang／xlang_asm／xlang-c **5788944** @18:16（非旧 PP002 5755448）。矩阵 5/5 rv42／opt102／hello0／si0／f32。eq daily **75 OK**（crown_pinnacle exact 25／2031／deep_off=0／EQ_SKIP_SYNTH=1／STRIDE=4；禁吞 375 supreme_crown）。墙钟 ~90s。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.67：深链分批 smoke crown_pinnacle 25／270 精确过滤** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝`EQ_ONLY=crown_pinnacle` naive strstr 吞 supreme_crown＋（25 exact vs 400 hits／375 余）；存储＝k_cases 名＋is_deep_climb_name 已含 pinnacle／crown；消费＝7.2.1b leftover 后深链分批。HARD BAN 跳过 summit／peak／zenith／pinnacle_zenith。修＝G.7 有则补全 `eq_tok_hits_name`：hit 前为 `supreme_` 则跳过；daily 逗号整词 `crown_pinnacle` 默认 SKIP_SYNTH=1＋STRIDE=4＋DEEP_MAX_FILE_OFF=0。拒抄 classify／score；拒 daily `*,*summit*`／`*,*peak*`／`*,*zenith*`／`*,*vx*`；拒把 crown／pinnacle 移出 is_deep_climb_name。验＝**双端 L2**：Darwin 矩阵 5/5＋eq **75 OK**；Ubuntu 矩阵 5/5＋eq **75 OK**（exact 25／2031；deep_smoke 25＋null-guard；大文件 deep skip）。leftover_helpers 余 **0**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `013a1e4a2`（深链分批 apex_max 25／270 精确过滤双端绿）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **深链分批 apex_max 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu `git pull --ff-only` `00ea65249..013a1e4a2`。xlang／xlang_asm／xlang-c **5788944** @18:16（非旧 PP002 5755448）。矩阵 5/5 rv42／opt102／hello0／si0／f32。eq daily **75 OK**（apex_max exact 25／2031／deep_off=0／EQ_SKIP_SYNTH=1／STRIDE=4；禁吞 500 summit_apex）。墙钟 ~29s。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.66：深链分批 smoke apex_max 25／270 精确过滤** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝`EQ_ONLY=apex_max` naive strstr 吞 summit_apex＋（25 exact vs 525 hits／500 余）；存储＝k_cases 名＋is_deep_climb_name 已含 apex_max；消费＝7.2.1b leftover 后深链分批。修＝G.7 有则补全 `eq_tok_hits_name`：hit 前为 `summit_` 则跳过；daily 逗号整词 `apex_max` 默认 SKIP_SYNTH=1＋STRIDE=4＋DEEP_MAX_FILE_OFF=0。拒抄 classify／score；拒 daily `*,*summit*`／`*,*vx*`；拒把 apex_max 移出 is_deep_climb_name。验＝**双端 L2**：Darwin 矩阵 5/5＋eq **75 OK**；Ubuntu 矩阵 5/5＋eq **75 OK**（exact 25／2031；deep_smoke 25＋null-guard 50；大文件 deep skip）。leftover_helpers 余 **0**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `00ea65249`（深链分批 max_ultra 25／270 精确过滤双端绿）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **深链分批 max_ultra 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu `git pull --ff-only` `66b531ea5..00ea65249`。xlang／xlang_asm／xlang-c **5788944** @18:16（非旧 PP002 5755448）。矩阵 5/5 rv42／opt102／hello0／si0／f32。eq daily **75 OK**（max_ultra exact 25／2031／deep_off=0／EQ_SKIP_SYNTH=1／STRIDE=4；禁吞 525 apex_max）。墙钟 ~28s。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.65：深链分批 smoke max_ultra 25／270 精确过滤** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝`EQ_ONLY=max_ultra` naive strstr 吞 apex_max＋（25 exact vs 550 hits／525 余）；存储＝k_cases 名＋is_deep_climb_name 已含 max_ultra；消费＝7.2.1b leftover 后深链分批。修＝G.7 有则补全 `eq_tok_hits_name`：hit 前为 `apex_` 则跳过；daily 逗号整词 `max_ultra` 默认 SKIP_SYNTH=1＋STRIDE=4＋DEEP_MAX_FILE_OFF=0。拒抄 classify／score；拒 daily `*,*vx*`；拒把 max_ultra 移出 is_deep_climb_name。验＝**双端 L2**：Darwin 矩阵 5/5＋eq **75 OK**；Ubuntu 矩阵 5/5＋eq **75 OK**（exact 25／2031；deep_smoke 25＋null-guard 50；大文件 deep skip）。leftover_helpers 余 **0**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `66b531ea5`（深链分批 ultra_hyper 25／270 精确过滤双端绿）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **深链分批 ultra_hyper 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu `git pull --ff-only` `cca52c415..66b531ea5`。xlang／xlang_asm／xlang-c **5788944** @18:16（非旧 PP002 5755448）。矩阵 5/5 rv42／opt102／hello0／si0／f32。eq daily **75 OK**（ultra_hyper exact 25／2031／deep_off=0／EQ_SKIP_SYNTH=1／STRIDE=4；禁吞 550 max_ultra）。墙钟 ~26s。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.64：深链分批 smoke ultra_hyper 25／270 精确过滤** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝`EQ_ONLY=ultra_hyper` naive strstr 吞 max_ultra＋（25 exact vs 575 hits／550 余）；存储＝k_cases 名＋is_deep_climb_name 已含 ultra_hyper；消费＝7.2.1b leftover 后深链分批。修＝G.7 有则补全 `eq_tok_hits_name`：hit 前为 `max_` 则跳过；daily 逗号整词 `ultra_hyper` 默认 SKIP_SYNTH=1＋STRIDE=4＋DEEP_MAX_FILE_OFF=0。拒抄 classify／score；拒 daily `*,*vx*`；拒把 ultra_hyper 移出 is_deep_climb_name。验＝**双端 L2**：Darwin 矩阵 5/5＋eq **75 OK**；Ubuntu 矩阵 5/5＋eq **75 OK**（exact 25／2031；deep_smoke 25＋null-guard 50；大文件 deep skip）。leftover_helpers 余 **0**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `cca52c415`（深链分批 hyper_mega 25／270 精确过滤双端绿）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **深链分批 hyper_mega 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu `git pull --ff-only` `9dcba1106..cca52c415`。xlang／xlang_asm／xlang-c **5788944** @18:16（非旧 PP002 5755448）。矩阵 5/5 rv42／opt102／hello0／si0／f32。eq daily **75 OK**（hyper_mega exact 25／2031／deep_off=0／EQ_SKIP_SYNTH=1／STRIDE=4；禁吞 575 ultra_hyper）。墙钟 ~25s。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.63：深链分批 smoke hyper_mega 25／270 精确过滤** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝`EQ_ONLY=hyper_mega` naive strstr 吞 ultra_hyper＋（25 exact vs 600 hits／575 余）；存储＝k_cases 名＋is_deep_climb_name 已含 hyper_mega；消费＝7.2.1b leftover 后深链分批。修＝G.7 有则补全 `eq_tok_hits_name`：hit 前为 `ultra_` 则跳过；daily 逗号整词 `hyper_mega` 默认 SKIP_SYNTH=1＋STRIDE=4＋DEEP_MAX_FILE_OFF=0。拒抄 classify／score；拒 daily `*,*vx*`；拒把 hyper_mega 移出 is_deep_climb_name。验＝**双端 L2**：Darwin 矩阵 5/5＋eq **75 OK**；Ubuntu 矩阵 5/5＋eq **75 OK**（exact 25／2031；deep_smoke 25＋null-guard 50；大文件 deep skip）。leftover_helpers 余 **0**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `9dcba1106`（深链分批 super_mega 25／270 双端绿）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **深链分批 super_mega 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu `git pull --ff-only` `b57b6af30..9dcba1106`。xlang／xlang_asm／xlang-c **5788944** @18:16（非旧 PP002 5755448）。矩阵 5/5 rv42／opt102／hello0／si0／f32。eq daily **816 OK**（super_mega＋skip_allow／deep_off=0／EQ_SKIP_SYNTH=1／STRIDE=4／OFF=24；cases 34／2031）。墙钟 ~34s。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.62：深链分批 smoke super_mega 25／270** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝leftover flatten 改 helper ABI，深链组合器已是 audit.x T／lex-first k_cases，日常 daily 从未 smoke super_mega（HARD BAN 只挡 vx／summit／peak／zenith／versal；ultra_mega 已于 v5.61 收）；存储＝twins.h gated C＋k_cases；消费＝7.2.1b leftover 后回归。修＝G.7 有则补全 daily：exact super_mega 层（25 行，无 HARD BAN 子串，不在 is_deep_climb_name）默认 SKIP_SYNTH=1＋STRIDE=4。拒抄 classify／score；拒 daily `*,*vx*`；拒把 super_mega 加进 is_deep_climb_name。验＝**双端 L2**：Darwin 矩阵 5/5＋eq **816 OK**；Ubuntu 矩阵 5/5＋eq **816 OK**（super_mega＋skip_allow／deep_off=0／STRIDE=4）。leftover_helpers 余 **0**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `b57b6af30`（深链分批 ultra_mega 25／270 双端绿）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **深链分批 ultra_mega 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu `git pull --ff-only` `8bb2dd362..b57b6af30`。xlang／xlang_asm／xlang-c **5788944** @18:16（非旧 PP002 5755448）。矩阵 5/5 rv42／opt102／hello0／si0／f32。eq daily **816 OK**（ultra_mega＋skip_allow／deep_off=0／EQ_SKIP_SYNTH=1／STRIDE=4／OFF=24；cases 34／2031）。墙钟 ~32s。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.61：深链分批 smoke ultra_mega 25／270** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝leftover flatten 改 helper ABI，深链组合器已是 audit.x T／lex-first k_cases，日常 daily 从未 smoke（HARD BAN 只挡 vx／summit／peak／zenith／versal）；存储＝twins.h gated C＋k_cases；消费＝7.2.1b leftover 后回归。修＝G.7 有则补全 daily：exact ultra_mega 层（25 行，无 HARD BAN 子串）默认 SKIP_SYNTH=1＋STRIDE=4。拒抄 classify／score；拒 daily `*,*vx*`。验＝**双端 L2**：Darwin 矩阵 5/5＋eq **816 OK**；Ubuntu 矩阵 5/5＋eq **816 OK**（ultra_mega＋skip_allow／deep_off=0／STRIDE=4）。leftover_helpers 余 **0**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `8bb2dd362`（leftover_fromat eq 1/30 双端绿）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **leftover_fromat 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu `git pull --ff-only` `a0de7e403..8bb2dd362`。xlang／xlang_asm／xlang-c **5788944** @18:16（非旧 PP002 5755448）。矩阵 5/5 rv42／opt102／hello0／si0／f32。leftover eq daily **2028 OK**（simd_builtin_deep_from_at＋skip_allow／deep_off=0／EQ_SKIP_SYNTH=1／OFF=24）。墙钟 ~28s。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.60：leftover_fromat eq 1/30** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝simd_builtin_deep_from_at leftover_helpers 1 已是 audit.x T，无 k_cases 行（首参 at_kind: 非 lex:）；存储＝twins.h static C 同名阴影挡住 harness TU 调 .x；消费＝7.2.1b T 缺口 1977／2007。修＝G.7 有则补全独立 TU 链 audit_x.o，穷尽 TokenKind（null lex）＋AT+IDENT ident 角＋短 live-lex snippet 对 gated C twin（薄组合 simd_builtin／vector_type_ident／paren_expr_head／builtin_vec_token；不抄 classify／score 表）。诚实 flatten＝C 空 lex 跳 paren／.x paren 空 lex→0；任一子分>0→1。IDENT 样本 remaining≥ident_len（与 leftover_sourceoff 同）。验＝**双端 L2**：Darwin 矩阵 5/5＋eq **2028 OK**；Ubuntu 矩阵 5/5＋eq **2028 OK**（simd_builtin_deep_from_at＋skip_allow／deep_off=0）。leftover_helpers 余 **0**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `a0de7e403`（leftover_preamble eq 1/30 双端绿）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **leftover_preamble／pathpost 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu `git pull --ff-only` `745252f21..a0de7e403`。xlang／xlang_asm／xlang-c **5788944** @18:16（非旧 PP002 5755448）。矩阵 5/5 rv42／opt102／hello0／si0／f32。leftover eq daily **6652 OK**（import_path_post＋collect_imports_preamble＋skip_allow／deep_off=0／EQ_SKIP_SYNTH=1／OFF=24；顺带补 v5.58 pathpost）。墙钟 ~28s。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.59：leftover_preamble eq 1/30** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝collect_imports_preamble leftover_helpers 1 已是 audit.x T，无 k_cases 行（首参 kind: 非 lex:）；存储＝twins.h 无同名 static，但 first-kind 三 kind ABI 仍不能进 k_cases；消费＝7.2.1b T 缺口 1977／2007。修＝G.7 有则补全独立 TU 链 audit_x.o，穷尽 TokenKind（每槽一维，另两槽钉 CONST／IDENT／ASSIGN）＋classify-hit 小笛卡尔＋null／live／nodata／empty 对 gated C twin（薄包 classify_toplevel；不抄 classify 表）。诚实 flatten＝live source→1／null→0（classify 丢弃）。拒抄 classify／score 入 leftover-to-audit；拒混 from_at。验＝**双端 L2**：Darwin 矩阵 5/5＋eq **5046 OK**；Ubuntu 矩阵 5/5＋eq **6652 OK**（pathpost＋preamble＋skip_allow／deep_off=0）。leftover_helpers 余 **1**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `63c476502`（leftover_pathpost eq 1/30）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.58：leftover_pathpost eq 1/30** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝import_path_post leftover_helpers 1 已是 audit.x T，无 k_cases 行（首参 path_buf: 非 lex:）；存储＝twins.h 无同名 static，但 first-path_buf ABI 仍不能进 k_cases；消费＝7.2.1b T 缺口 1977／2007。修＝G.7 有则补全独立 TU 链 audit_x.o，ident 字节／点／非法字节／长度角含 path_len>63 cap 对 gated C twin（薄包 import_path_validate；不抄 ident_continue 表）。拒抄 classify／score 入 leftover-to-audit；拒混 from_at／preamble。验＝Darwin 矩阵 5/5；eq daily **2308 OK**（import_path_post＋skip_allow／deep_off=0）。leftover_helpers 余 **2**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `b73d50f14`（Ubuntu L2 波末绿）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **leftover flatten／eq 波末 Ubuntu L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu `git pull --ff-only` `80d43a666..745252f21`（44 commits）。g05 OK（xlang／xlang_asm／xlang-c 5788944 @18:16）；inject import-heap／read-file-x-view strong-over-weak；nm `pipeline_load_import_from_disk_c` T／`pipeline_read_file_x` T。矩阵 5/5 rv42／opt102／hello0／si0／f32。leftover eq daily **24508 OK**（v5.50–v5.57 全族＋skip_allow／deep_off=0／EQ_SKIP_SYNTH=1／OFF=24）。墙钟 ~1m46s。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `17b1c21f0`（leftover_validate eq 1/30）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.57：leftover_validate eq 1/30** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝validate_toplevel leftover_helpers 1 已是 audit.x T，无 k_cases 行（首参 kind: 非 lex:）；存储＝twins.h 无同名 static，但 first-kind span／bounds ABI 仍不能进 k_cases；消费＝7.2.1b T 缺口 1977／2007。修＝G.7 有则补全独立 TU 链 audit_x.o，穷尽 TokenKind（ident_len≤0／in-span／overflow）＋ident_len／token_start／slen 角含 unsigned-add wrap 对 gated C twin（EOF／ident_len≤0／span；不抄 token_run_len／verify_kw 表）。拒抄 classify／score 入 leftover-to-audit；拒混 from_at／path_post／preamble。验＝Darwin 矩阵 5/5；eq daily **8492 OK**（leftover_validate＋skip_allow／deep_off=0）。leftover_helpers 余 **3**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `fbad07ee1`（leftover_kindarr eq 2/30）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.56：leftover_kindarr eq 2/30** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝kinds-array leftover_helpers 2（peek_kind_chain／expr_binop_kinds_probe）已是 audit.x T，无 k_cases 行（首参 kinds: 非 lex:）；存储＝twins.h static C 同名阴影挡住 harness TU 调 .x；消费＝7.2.1b T 缺口 1977／2007。修＝G.7 有则补全独立 TU 链 audit_x.o，短 snippet＋guard／max_peek／num_kinds 角＋若干 start-pos 对 gated C twin（拷 *lex＋lexer_next_into；.x peek/step/restore）。拒抄 classify／score 入 leftover-to-audit；拒混 from_at／path_post／preamble／validate_toplevel。验＝Darwin 矩阵 5/5；eq daily **2592 OK**（leftover_kindarr＋skip_allow／deep_off=0）。leftover_helpers 余 **4**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `7358f26cd`（leftover_asbind eq 1/30）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.55：leftover_asbind eq 1/30** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝7-param as-bind leftover_helpers 1（import_as_bind）已是 audit.x T，无 k_cases 行（首参非 lex:）；存储＝twins.h 无同名 static，但 7 参 first-kind ABI 仍不能进 k_cases；消费＝7.2.1b T 缺口 1977／2007。修＝G.7 有则补全独立 TU 链 audit_x.o，穷尽 TokenKind（kind／next_kind 各一维）＋IDENT+"as" 拼写／长度／token_start 角＋next-ident 字节类／长度／next_start 角对 gated C twin（拼写检查＋bind_name_validate on data+next_start）。拒抄 classify／score 入 leftover-to-audit；拒混 kinds-array 活 lex。验＝Darwin 矩阵 5/5；eq daily **2431 OK**（leftover_asbind＋skip_allow／deep_off=0）。leftover_helpers 余 **6**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `40133dcfa`（leftover_twokind eq 1/30）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.54：leftover_twokind eq 1/30** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝two-kind+source+off leftover_helpers 1（simd_builtin）已是 audit.x T，无 k_cases 行（首参非 lex:）；存储＝twins.h static C 同名阴影挡住 harness TU 调 .x；消费＝7.2.1b T 缺口 1977／2007。修＝G.7 有则补全独立 TU 链 audit_x.o，穷尽 TokenKind（at／ident 各一维）＋AT+IDENT shuffle／select 拼写／长度／token_start 角对 gated C twin（拼写检查，非 bind_name_validate）。拒抄 classify／score 入 leftover-to-audit。验＝Darwin 矩阵 5/5；eq daily **2354 OK**（leftover_twokind＋skip_allow／deep_off=0）。leftover_helpers 余 **7**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `b6ee02f2d`（leftover_kindsrc eq 2/30）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.53：leftover_kindsrc eq 2/30** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝kind+source+off leftover_helpers 2 已是 audit.x T，无 k_cases 行（首参非 lex:）；存储＝twins.h static C 同名阴影挡住 harness TU 调 .x（match_subject_ident）；消费＝7.2.1b T 缺口 1977／2007。修＝G.7 有则补全独立 TU 链 audit_x.o，穷尽 TokenKind＋IDENT ident 字节类／长度／token_start 角对 gated C twin（import_dot_segment／match_subject_ident；薄包 bind_name_validate on data+off；import_dot_segment 加 I32／ASYNC 恒 1）。拒抄 classify／score 入 leftover-to-audit。验＝Darwin 矩阵 5/5；eq daily **2486 OK**（leftover_kindsrc＋skip_allow／deep_off=0）。leftover_helpers 余 **8**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `1e93fb79e`（leftover_sourceoff eq 6/30）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.52：leftover_sourceoff eq 6/30** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝source+off leftover_helpers 6 已是 audit.x T，无 k_cases 行（首参非 lex:）；存储＝twins.h static C 同名阴影挡住 harness TU 调 .x；消费＝7.2.1b T 缺口 1977／2007。修＝G.7 有则补全独立 TU 链 audit_x.o，ident 字节类＋长度＋token_start 角对 gated C twin（struct_field／enum_variant／field_access／import_select／import_select_item／vector_type_ident；薄包 bind_name_validate on data+off；vector 加 i3x*／Vec*）。拒抄 classify／score 入 leftover-to-audit。验＝Darwin 矩阵 5/5；eq daily **4343 OK**（leftover_sourceoff＋skip_allow／deep_off=0）。leftover_helpers 余 **10**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `bcee44862`（leftover_namelen eq 8/30）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.51：leftover_namelen eq 8/30** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝name/len leftover_helpers 8 已是 audit.x T，无 k_cases 行（首参非 lex:）；存储＝twins.h static C 同名阴影挡住 harness TU 调 .x；消费＝7.2.1b T 缺口 1977／2007。修＝G.7 有则补全独立 TU 链 audit_x.o，ident 字节类＋长度角对 gated C twin（function_name／struct_layout／block_bind／collect_imports_bind／library_field／library_param／onefunc_buf／extern_param；薄包 bind_name_validate）。拒抄 classify／score 入 leftover-to-audit。验＝Darwin 矩阵 5/5；eq daily **5140 OK**（leftover_namelen＋skip_allow／deep_off=0）。leftover_helpers 余 **16**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `c43596dfd`（leftover_kind eq 6/30）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.50：leftover_kind eq 6/30** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝leftover_helpers 30 已是 audit.x T，无 k_cases 行（首参非 lex:）；存储＝twins.h static C 同名阴影挡住 harness TU 调 .x；消费＝7.2.1b T 缺口 1977／2007。修＝G.7 有则补全独立 TU 链 audit_x.o，穷尽 TokenKind 对 gated C twin（is_type_start／enum_discriminant／const_import／builtin_vec／spawn_kw／brace_head）。拒抄 classify／score 入 leftover-to-audit。验＝Darwin 矩阵 5/5；eq daily **1584 OK**（leftover_kind＋skip_allow／deep_off=0）。leftover_helpers 余 **24**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `ef89fb16f`（resolve_read embed 诚实拒）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **resolve_read embed 诚实拒（pin-safe；view＋拒>4MiB）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝`pipeline_read_file_x` 走 `xlang_read_file_into_path` cap 4MiB 静默截断（n==cap 分不清 exact-fit／溢出）；存储＝ctx.loaded_buf PIN 4MiB；消费＝resolve_read。修＝G.7 有则补全 view 整文件＋拒 length>4MiB＋memcpy 入 embed；seed 同语义；C thin 强符号 overlay WEAK `pipeline_read_file_x`。拒抬 embed／拒新堆分配器。产品 import 仍 heap orch。验＝Darwin 矩阵 5/5；4.2MiB import `-o` run=42；小 import run=42；inject OK。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `32c53068f`（import ctx 4MiB heap orch）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **import ctx 4MiB heap orch（pin-safe；PP002 复用）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝`pipeline_load_import_from_disk_c` 走 `read_file_x`／`preprocess_loaded_into_ctx` 写 PipelineDepCtx embed 4MiB（PIN）静默截断；存储＝ctx.loaded_buf／preprocess_buf；消费＝产品 import parse。修＝G.7 有则补全 orch 改 `runtime_read_file_view`＋`xlang_preprocess_raw_to_malloc`（堆；递归安全；不改 pin）；seed `impl_c` 同语义；C thin 强符号 overlay WEAK `_c`。拒抬 embed／拒新堆分配器。验＝Darwin 矩阵 5/5；4.2MiB import `-o` run=42；小 import run=7；inject OK。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `03a1dbbc2`（v5.49 leftover flatten validate_toplevel）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.49：leftover helper Route C flatten validate_toplevel（＋1）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝validate leftover 的 lexer_result-by-val＋stretch 表（verify_kw 实际是 bounds；关键字 ident_len=0）；存储＝suite 冷孪生／eq leftover_helpers／P9a `.x`；消费＝parse_into_buf_loop（void 丢弃；生成器 v5.6 已 elide）。修＝G.7 压成 (kind,ident_len,token_start,source)＋桥 source_data／length；闸 C twin；不抄 token_run_len／verify_kw 表。classify／score 留 stretch.x。验＝Darwin 矩阵 5/5；`.x` -E 2007 funcs typeck OK；eq daily **9660 OK**（parse_into_buf／library_scan／match_subject／skip_allow／deep_off=0）。eq twins **1948**。leftover_helpers **30**。leftover-to-audit unique **0**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `3fdcd9391`（v5.48 leftover flatten import leftover 链）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.48：leftover helper Route C flatten import leftover 链（＋2）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝import_path_post leftover 的 still-C 包装只转 finalize（不在 eq TU；产品调用方已 finalize 且 AUDIT_CALL 丢弃）；collect leftover 的 3×lexer_result 只读 kind 并丢弃 classify／validate／skip_ws；存储＝suite 冷孪生／eq leftover_helpers／P9a `.x`；消费＝collect_imports 主循环／AUDIT_CALL post。修＝G.7 压成 validate／classify 权威（已是 audit.x extern）；闸 C twin；collect ABI 改 (kind,next,third,source) 且 void→i32。拒 validate_toplevel（verify_kw／token_run_len 表）。验＝Darwin 矩阵 5/5；`.x` -E 2006 funcs typeck OK；eq daily **8400 OK**（library_scan／match_subject／skip_allow／deep_off=0）。eq twins **1948**。leftover_helpers **29**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `f4ec4282a`（v5.47 leftover flatten mega_buf wrap_buf）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.47：leftover helper Route C flatten mega_buf wrap_buf（＋1）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝diag_fn_mega_full_deep_buf leftover 的 still-C 包装只 wrap_buf 再转已迁 slice restore-trio；存储＝suite 冷孪生／eq twins／P9a `.x`；消费＝diag_parse_ultra／diag_ultra2／AUDIT_CALL。修＝G.7 压成 wrap_buf＋slice 权威；闸 C twin；撤 HARNESS_MEGA_STILL_C。拒 validate／path_post／collect_imports。验＝Darwin 矩阵 5/5；`.x` -E 2004 funcs typeck OK；eq daily **5040 OK**（diag_fn_mega_full_deep_buf／diag_parse_ultra／diag_ultra2／skip_allow／deep_off=0）。eq twins **1948**。leftover_helpers **27**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `59f25ed4c`（v5.46 leftover flatten advance_to 族）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.46：leftover helper Route C flatten advance_to 族（＋7）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝advance_to leftover 的次游标 by-val＋out_body_lex 走只 peek kind 并 step；存储＝suite 冷孪生／eq twins／P9a `.x`；消费＝body_* C 孪生／eq daily。修＝G.7 压成 (lex_inout, source)；闸 C twin；7 处 C 调用改 ABI。拒 validate／path_post／collect_imports／mega_buf。验＝Darwin 矩阵 5/5；`.x` -E 2004 funcs typeck OK；eq daily **12600 OK**（advance_to／struct_fields_body／if_stmt_body／function_body／match_arms／skip_allow／deep_off=0）。eq twins **1947**。leftover_helpers **27**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `5dd26e2df`（v5.45 leftover flatten skip_* 链）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.45：leftover helper Route C flatten skip_balanced／skip_type_suffix／skip_one_param_type（＋3）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝skip_* leftover 的走只 peek kind 并推进不透明游标；存储＝suite 冷孪生／eq twins／P9a `.x`；消费＝桥 inplace／gated C 孪生／eq daily。修＝G.7 压成 (lex_inout, source)；闸 C twin；桥 inplace 薄包装；13 处 C 调用改 ABI。拒 validate／path_post／advance_to。验＝Darwin 矩阵 5/5；`.x` -E 1997 funcs typeck OK；eq daily **5040 OK**（skip_type_suffix／skip_one_param_type／skip_balanced_brackets／skip_allow／deep_off=0）。eq twins **1940**。leftover_helpers **27**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `b252e2edf`（v5.44 leftover flatten expr_binop_kinds_probe）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.44：leftover helper Route C flatten expr_binop_kinds_probe（＋1）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝expr_binop_kinds_probe leftover 的走只读每 token 的 kind 对 kinds[] 匹配，调用方 lex 净零；存储＝suite 冷孪生／eq leftover_helpers／P9a `.x`；消费＝8 个 binop C 孪生／eq daily。修＝G.7 压成 (kinds, num_kinds, lex, source)；闸 C twin；binop 调用改 ABI。拒 validate／path_post／advance_to。验＝Darwin 矩阵 5/5；`.x` -E 1994 funcs typeck OK；eq daily **11760 OK**（expr_shift／expr_rel／expr_eq／expr_bit／expr_log／library_scan／match_subject／skip_allow／deep_off=0）。eq twins **1937**。leftover_helpers **27**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `5f1e98ed6`（v5.43 leftover flatten peek_kind_chain）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.43：leftover helper Route C flatten peek_kind_chain（＋1）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝peek_kind_chain leftover 的走只读每 token 的 kind 写入 kinds[]，调用方 lex 净零；存储＝suite 冷孪生／eq leftover_helpers／P9a `.x`；消费＝hybrid thin_glue AUDIT_CALL／eq daily。修＝G.7 压成 (kinds, max_peek, lex, source)；闸 C twin；产品／suite 调用改 ABI。拒 validate／path_post／advance_to。验＝Darwin 矩阵 5/5；`.x` -E 1993 funcs typeck OK；eq daily **9240 OK**（peek_kind／toplevel_kind_peek／library_scan／match_subject／skip_allow／deep_off=0）。eq twins **1937**。leftover_helpers **26**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `6b2f5deea`（v5.42 leftover flatten from_at）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.42：leftover helper Route C flatten from_at（＋1）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝from_at leftover 的 next_lex 只抽 ident 的 kind／token_start／ident_len，paren 走已是迁走的 paren_expr_head；存储＝suite 冷孪生／eq leftover_helpers／P9a `.x`；消费＝hybrid thin_glue／eq daily／AUDIT_CALL。修＝G.7 压成 at／ident 标量＋lex_after_ident 薄组合 simd_builtin／vector_type_ident／paren_expr_head／builtin_vec_token；闸 C twin；产品／deep 调用改标量。拒 peek_kind／validate／path_post／advance_to。验＝Darwin 矩阵 5/5；`.x` -E 1992 funcs typeck OK；eq daily **9240 OK**（simd_builtin／library_scan／match_subject／skip_allow／deep_off=0）。eq twins **1937**。leftover_helpers **25**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `deac50180`（v5.41 leftover flatten lookahead）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.41：leftover helper Route C flatten lookahead（＋2）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝simd_builtin／import_as leftover 的 next_lex 只读下一 token 的 kind／token_start／ident_len；存储＝suite 冷孪生／eq leftover_helpers／P9a `.x`；消费＝hybrid thin_glue／eq daily／AUDIT_CALL。修＝G.7 压成 at／ident 标量＋`bind_name_validate` 薄包装；闸 C twin；from_at 调用改标量。拒 from_at paren 走／peek_kind／validate／path_post／advance_to。验＝Darwin 矩阵 5/5；`.x` -E 1991 funcs typeck OK；eq daily **9240 OK**（simd_builtin／library_scan／match_subject／skip_allow／deep_off=0）。eq twins **1937**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `646eb80fb`（v5.40 leftover flatten）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.40：leftover helper Route C flatten（＋4）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝lexer_result leftover 不全是次游标（spawn_kw 已是 kind；brace_head／import_dot／match_subject_ident 只读 kind／ident_len／token_start）；存储＝suite 冷孪生／eq leftover_helpers／P9a `.x`；消费＝hybrid thin_glue／eq daily。修＝G.7 压成 kind＋source＋off 标量＋`bind_name_validate` 薄包装；闸 C twin。拒 import_path_post（finalize 链不在 eq TU，禁 HELPERS 第二权威）。拒 advance_to／import_as·simd next_lex／peek_kind。验＝Darwin 矩阵 5/5；audit `.x` -E 1989 funcs typeck OK；eq daily **8400 OK**（library_scan／match_subject／skip_allow／deep_off=0）。eq twins **1937**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `f3d5b6e61`（v5.39 leftover helper）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.39：leftover helper Route C（＋19）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝53 leftover 非 byval helper 仍编进 hybrid P9（parse_suite leftover 已 0）；存储＝suite 冷孪生／eq twins／P9a `.x`；消费＝hybrid thin_glue／eq daily。修＝G.7 补全既有 `bind_name_validate` 薄包装＋kind 分类＋`skip_allow_modifiers` inout（lex-step 桥；C twin void*）。拒 advance_to 次游标／lexer_result by-val。验＝Darwin 矩阵 5/5；audit `.x` -E typeck OK；eq daily **3780 OK**（skip_allow／deep_off=0）。eq twins **1937**。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **PP002 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu：矩阵 5/5＋4.2MiB -E typeck OK＠`80d43a666`（inject fix `9c52a8097` 后 GNU ld -r）。与 Darwin 同形。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `79b57923f`（PP002 heap）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **PP002 heap preprocess（entry malloc_impl 按 raw_len）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝`xlang_preprocess_raw_to_malloc_impl` 拒 raw_len>4MiB＋`preprocess_x_buf` `pos>=4194304` 静默截断；存储＝heap scratch（非 PipelineDepCtx embed）；消费＝产品 `-E`。修＝G.7 补全引擎 *u8 尊 source_len／out_cap＋malloc_impl scratch=max(4MiB, raw_len) i32-fit；C thin 强符号 ld -r overlay WEAK mega（避 pabi 整颗 -E／tip heat）。ctx 4MiB pin 不动。顺手＝v5.37 top_level_let leftover 仍-byval 前向声明（P9 thin_glue）。验＝Darwin 矩阵 5/5；4.2MiB／8.1MiB -E typeck OK；stretch -E OK；eq daily **3780 OK**（top_level_let／deep_off=0）。下一刀＝53 helper。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **v5.38 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu：矩阵 5/5＋`.x` -E 1936 T typeck OK＠`3440d356c`；与 Darwin 同形。eq daily 3780 仅 Darwin（提速）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `5107c40e1`（v5.38）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.38：leftover ident compress＋parse_suite 攀尽（＋960 总 1936）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝920 leftover ident 256–656（versal-infix 塔；不是产品 Cap）；存储＝suite 冷孪生／eq twins；消费＝`.x` -E／eq daily。修＝G.7 只压 leftover C 名（最长优先全名替换；已迁 ≤255 英文名不动）＋`_vx_` 深链标记（eq skip／daily refuse）。soft-FP＝全部新≤255（640 gen＋320 deleg；parse_suite leftover 0）。根修＝harness `is_deep_climb_name` 纳 `_vx_`。拒产品 Cap 256→1024。`.x` 3.75／4.19 MiB（未 PP002）。验＝Darwin 矩阵 5/5；`.x` -E 1936 T typeck OK；eq daily **3780 OK**（top_level_let／9 cases／deep_off=0）。vx 单 case 深链仍立卡。下一刀＝heap preprocess 或 53 helper。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **v5.37 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu：矩阵 5/5＠`92241efa4`；与 Darwin 同形。eq daily 3780 仅 Darwin（提速）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `bc9e3b047`（v5.37）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.37：top_level_let extra-flag＋≤255 攀尽（＋31 总 976）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝parse_suite 不认 `(lex, source, is_const)`／`(lex, data, len, is_const)`（2/3 行）；存储＝suite 冷孪生／eq twins；消费＝`.x` -E／eq daily。修＝G.7 补全 flag_sigs＋emit `is_const: i32`＋C-twin／decl pointer ABI＋skip `(void)is_const`＋skip_one_param_type dest=`lex`（同 v5.35 skip_imports）＋slice score 第三参 ident。soft-FP＝deep_buf＋full/mega＋toplevel ultra→intergalactic（31）。根修＝harness buf4（lex,data,len,is_const）双极性。拒 L012 920 versal。验＝Darwin 矩阵 5/5；`.x` -E 976 T；eq daily **3780 OK**（9 cases／deep_off=0）。下一刀＝heap preprocess 或 L012。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **v5.36 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu：矩阵 5/5＠`97f8affa9`；与 Darwin 同形。eq daily 1680 仅 Darwin（提速）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `71b02f63a`（v5.36）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.36：leftover cluster＋≤255 攀尽（＋4 总 945）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝生成器缺 `r = diag_after_imports_then_structs`／`return from_at(r_at)`／`function_name_audit("main")` 三处理器；存储＝suite 冷孪生／eq twins；消费＝`.x` -E／eq daily。修＝G.7 补全（skip-struct 展开＋from_at 内联 shuffle／select／vec kinds＋name-lit 折 bind_name_validate）。soft-FP＝simd_deep buf 委托。根修＝harness 纳 still-C from_at／simd_audit／vec／diag_after_imports 叶＋mega_buf→c_ref。拒 L012 920／top_level_let_buf 第四参。验＝Darwin 矩阵 5/5；`.x` -E 945 T；eq daily **1680 OK**（4 cases／deep_off=0）。下一刀＝top_level_let_buf 或 heap preprocess。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **v5.35 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu：矩阵 5/5＠`438fbf808`；与 Darwin 同形。eq daily 2520 仅 Darwin（提速）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **docs(selfhost)** **tip SHA sync** | **文档** · **不升钉**。tip 对齐 `1d4be4f9f`（v5.35）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.35：skip_imports leftover＋≤255 攀尽（＋8 总 941）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。五问：产生＝生成器 skip_imports 只认 dest∈lexer_locals，noles 剥 `struct lexer lex` 后 dest `lex` 落 unhandled；存储＝suite 冷孪生／eq twins；消费＝`.x` -E／eq daily。修＝G.7 补全 skip_imports（dest `lex`＋`lexer_init_c()` reset+inplace）＋peek_kind `if>0 score++`＋init-probe 块＋noles `wrap_buf`。soft-FP＝diag_token／after_collect／pipeline_ok／collect／full_deep／first_token_kind／fn_multiversal。根修＝harness 退役 v5.32 still-C 五符号；collect_deep 冷孪生 `&lex`→`lex_init`。上帝视角：versal-infix 余＝L012 920，不是 4MiB。验＝Darwin 矩阵 5/5；`.x` -E 941 T；eq daily **2520 OK**（deep_off=0）。拒 simd／function_name 字面量／diag_fail lexer_result。下一刀＝heap preprocess 或 simd 字面量。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **v5.34 双端 L2 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu：矩阵 5/5＠`bc9f625aa`；与 Darwin 同形。eq 深链指数墙仍立卡，矩阵用既有 xlang_asm。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.34：noles ABI＋infinite→intergalactic 攀尽（＋252 总 933）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。生成器收 `(data,len)` noles（snap+reset-to-init+restore；call-site 字符串热路径）。soft-FP＝infinite／eternal／cosmic／universal／omnipotent／sovereign／imperial／divine／celestial／galactic／intergalactic（11 层）。根修 suite crown versal 自递归 first-callee；拒 ident>255（L012）。硬墙＝preprocess `u8[4194304]`（全量 4.6MiB PP002）／versal-infix。验＝Darwin 矩阵 5/5；`.x` -E 933 T（1.63MB）。eq 深链指数墙立卡（infinite combinator 单 case 分钟级）。下一刀＝heap preprocess cap＋versal-infix。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **v5.33 双端 L2／eq smoke 确认** | **文档** · **不升钉** · **L2（提速）**。Ubuntu：矩阵 5/5＋FORCE eq **23756 checks OK**＠`04a5347ae`；与 Darwin 同形（deep_off=0／~1.8min）。tip heat g05 仍立卡，矩阵／eq 用既有 xlang_asm。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.33：preamble／ultra ABI＋parse_into super→transcendent 攀尽（＋32 总 681）** | **代码＋测试＋文档** · **不升钉** · **L2（提速）**。同波＝diag_fn_mega_buf byval→pointer；手迁 diag_parse_ultra／ultra2／diag_super→transcendent（data,len→lex snap+reset-to-init+restore）；soft-FP preamble＋parse_into super→transcendent（14 层）。根修 suite summit→transcendent 自递归 first-callee；sync 正则须紧邻 FROM_X 门（防 ungated void* 吞 #endif）。验＝Darwin 矩阵 5/5＋FORCE eq **23756 OK**（deep_off=0／~2.8min）。拒 infinite+／versal。残＝tip heat g05。下一刀＝infinite／versal。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **日切归档 11 号 32 行＋12 号主文档重写（提速纪律）** | **文档** · **不升钉**。归档＝B-minus v5.13→v5.29＋eq≤11s＋Cap 4.2.8。主文档＝KPI／前排／§6 仅 12 日；新 **提速纪律**：Darwin 优先闸门／一波 ABI+攀尽／禁每刀双端／tip heat 不挡／单刀≤5min。下一刀＝v5.33 preamble／ultra ABI。 |
| 2026-09-12 | **docs(selfhost)** **v5.32 双端 L2／eq smoke 确认** | **文档** · **不升钉** · **L2**。Ubuntu：矩阵 5/5＋FORCE eq **1680 checks OK**＠`5b8b476de`；与 Darwin 同形（deep_off=0／parse_into mega 域）。tip heat g05（Darwin ld assert／Ubuntu transcendent UNDEF）仍立卡，矩阵／eq 用既有 xlang_asm。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.32：diag_parse_one_mega ABI widen（＋4 总 649）** | **代码＋测试＋文档** · **不升钉** · **L2**。手迁＝`diag_parse_one_mega_full_buf` `(data,len)`→`(lex,data,len)`（snap+reset-to-init+restore；仿 v5.10）。根修＝gen `join_logical` 不吞注释后 score+=。soft-FP＝parse_into_mega／ultra_mega／entry_mega。拒 super（卡 preamble_mega／diag_parse_ultra）。harness 纳 still-C full_deep 链 HELPERS。验＝Darwin：649 T＋矩阵 5/5＋FORCE eq **1680 OK**（deep_off=0）。残＝tip heat g05／parse_into super+。下一刀＝preamble_mega／diag_parse_ultra ABI。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **v5.31 双端 L2／eq smoke 确认** | **文档** · **不升钉** · **L2**。Ubuntu：矩阵 5/5＋eq 代表域 **27 checks OK**＠`4cd0de49e`；与 Darwin 同形（deep_off=0／9 non-buf）。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.31：exact-transcendent（＋21 总 645）** | **代码＋测试＋文档** · **不升钉** · **L2**。软刀＝exact transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega（expr／primary／import／match／struct／trait／fn／control_flow／body_skip／library／try_skip＋buf）。Dry-run 21 OK／20 拒（diag／toplevel 非 byval；parse_into mega→…卡 `diag_parse_one_mega` data,len-only／非 byval）。拒 infinite+／versal。eq＝代表域 FORCE daily smoke（9 non-buf／**27 checks**；`EQ_DEEP_MAX_FILE_OFF=0`；全表 close 小时级）。验＝Darwin：645 T＋矩阵 5/5＋smoke OK。残＝parse_into mega 根／infinite。下一刀＝diag_parse_one_mega ABI widen。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **v5.30 双端 L2／eq smoke 确认** | **文档** · **不升钉** · **L2**。Ubuntu：矩阵 5/5＋eq 代表域 **36 checks OK**＠`c236ca4bc`；与 Darwin 同形。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **feat(selfhost)** **7.2.1 B-minus v5.30：exact-absolute Cap-256 余量（＋20 总 624）** | **代码＋测试＋文档** · **不升钉** · **L2**。软刀＝Cap-256 解锁后 exact absolute（spans 128..139）＋prereq control_flow ultimate buf。Dry-run 20 OK／3 拒（diag／toplevel 非 byval；parse_into 卡 ultimate）。拒 transcendent+／versal。eq＝代表域 FORCE daily smoke（9 端口／36 checks；全表 close 小时级）。验＝Darwin：624 T＋矩阵 5/5＋smoke OK。残＝tip heat g05 ld assert／全表 close／parse_into。下一刀＝parse_into／transcendent。PLATFORM: SHARED。 |
| 2026-09-12 | **fix(selfhost)** **asm `-o` 长名 Cap-256（解 CG002）** | **代码＋测试＋文档** · **不升钉** · **L2**。五问：产生＝prefer `.x` `emit_call` `clen0>127` 硬拒；Darwin enc `reloc_name`／`mn` `[128]`／`i<127`；elf `add_sym`／`append_*` clamp 128；mega `export_sym[128]`。存储＝AST name[256]／ElfSym／reloc 行。消费＝asm `-o` call／def。修＝G.7 补全 call out_cap 256＋enc／elf／mega／heap 行宽→256。验＝Darwin call 64..255 `-o` run=42＋矩阵 5/5。下一刀＝absolute 续迁。PLATFORM: SHARED。 |
| 2026-09-12 | **fix(selfhost)** **func_name_byte_at Cap-256＋write_var tip-safe** | **代码＋测试＋文档** · **不升钉** · **L2**。五问：产生＝WAVE280 `byte_at` 仍 `i>=64`；seed `write_var` `>127→63`＋name_len＠160；`.x` 超长截 63。存储＝Func.name[256]／Expr name_len＠288。消费＝typeck／diag／hash／absolute `-E`。修＝G.7 补全 seed byte_at→256＋seed write_var／binop Cap-256 布局＋`.x` clamp→255。验＝双端 L2 矩阵 5/5＠`827f49e5e`＋长名 `-E` typeck OK（pin-PREGEN 全 hybrid tip SEGV→Darwin 热补／Ubuntu objcopy weaken+overlay）。残＝asm `-o` 名长≥128 CG002。下一刀＝absolute 余量。PLATFORM: SHARED。 |
| 2026-09-12 | **fix(selfhost)** **函数名发射 Cap-256（parser staging）** | **代码＋测试＋文档** · **不升钉** · **L2**。五问：产生＝parser dummy_name／onefunc_finish／copy_onefunc 仍 128；存储＝AST name[256]＋全 name_len；消费＝-E／call。修＝G.7 补全 Cap-256 staging（seed＋.x＋asm buf）。验＝Darwin deflen／call 127..255 绿＋矩阵 5/5＠`8a6c4e901`。下一刀＝byte_at／absolute 余量。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **absolute 软刀墙地图（codegen emit／byte_at）** | **文档** · **不升钉** · **L2**。五问：产生＝Cap-256 后 L012 已放行定义；存储＝AST name[256]；消费＝`-E`／typeck call-resolve。墙①codegen 发射函数名仍 128（span≥128 的 C 符号嵌 NUL）；墙②`func_name_byte_at` 仍 `i>=64`（seed 可修；pin-PREGEN mega hybrid 入 tip 本波试链 SEGV 已回滚）。Dry-run absolute 18 soft-OK。Darwin 矩阵 5/5＠`e7c2408ef`／docs tip `60b78b556`。下一刀＝codegen emit Cap-256。PLATFORM: SHARED。 |
| 2026-09-12 | **fix(selfhost)** **Cap 4.2.8 pabi／seed／codegen 余 `[128]` receivers** | **代码＋测试** · **不升钉** · **L2**。`import_path_copy64`／`*_name_into`／`*_copy64`／`codegen_dep_import_path_len_at`→`[128]` 砸栈面加宽→`[256]`（mega seed＋twins）。钉盘蛋 prefer 重注 pabi。PLATFORM: SHARED。 |
| 2026-09-12 | **docs(selfhost)** **Cap 4.2.8 邻域／pabi thin tip sync** | **文档** · **不升钉** · **L2**。双端矩阵 5/5＠code tip `3dc7c4c30`／docs tip `c15c749f5`。钉盘仍 `5cac88d00`。 |
| 2026-09-12 | **fix(selfhost)** **Cap 4.2.8 pabi thin inject name bufs** | **代码＋测试** · **不升钉** · **L2**。Ubuntu opt／si `__stack_chk_fail`：arrcopy／ptrslot／slotbytes／fnptr_as／field_load／unused_hints thin 仍 `[128]`←`*_name_into`（mega `.x` 已 256；first-wins thin 漂移）。强制 `rm` pabi＋prefer 重注。tip＝`3dc7c4c30`／`8448631b0`／`91e32457d`。PLATFORM: SHARED。 |
| 2026-09-12 | **fix(selfhost)** **Cap 4.2.8 邻域 leftover `[128]`→`[256]`** | **代码＋测试** · **不升钉** · **L2**。parser_asm `.inc`（type_alias／top_level_let／finish_struct_lit／one_function／seed_parse／param32 helpers）＋try_inline／call_dispatch／seed_link／mega_fallback＋`.x` 镜像。tip＝`b9b5325af`。PLATFORM: SHARED。 |
