# ShuX 自举进度

> **更新**：2026-07-21 · 20 号全日已归档；本日从 21 号起重新起表  
> **基线 SHA（产品 L4 放行钉盘）**：**`11a57351`**（升自 `deaf773b`；双端产品轨 **129** 全绿 → 升钉）  
> **产品 bstrict 套件数**：**129**  
> **本波 tip**：**`708d63e0`**（PREFER unbundle `async_asm_pool` from `pipeline_glue`；产品钉盘仍 **`11a57351`**）  
> **本波（21 日）状态**：**产品轨 P0 收口 · 双端 129 全绿 · tip 升钉**；P1 libc + preprocess EOF；**R2 async 族 + Cap residual pure + G.7 fputs**；**PREFER unbundle async_asm_pool**。mac `/tmp/mac_bstrict_d45ee9a4_p0.log` · Ubuntu L4 `/tmp/ubuntu_true_bstrict_11a57351.log`。地图：[当前问题分析.md](当前问题分析.md)。

> **方法**：[自举方法.md](自举方法.md)（Cap/R/L/M；工程 ≠ 产品放行）  
> **Windows（删种子前）**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)  
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `shux-selfhost-product-gate`  
> **归档**：[自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md) · [19](自举进度-归档-2026-07-19.md) · [18](自举进度-归档-2026-07-18.md) · [17](自举进度-归档-2026-07-17.md) · [16](自举进度-归档-2026-07-16.md) · [14](自举进度-归档-2026-07-14.md) · [13](自举进度-归档-2026-07-13.md)

### 维护约定

1. 每完成一波 **必改两文**：本文（KPI/前排/§6）+ [`当前进度.md`](当前进度.md)。  
2. **禁止**把 prove/Stage2/N++ 单独写成「可自举」。  
3. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**；历史钉盘 125）。  
4. 工程 KPI 写 **SHA + 日志路径**；过期整日流水移归档。  
5. 大债地图默认写 [`当前问题分析.md`](当前问题分析.md)（承接原 `问题分析文档.md`）；非每微步。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘升为 `11a57351`（升自 `deaf773b`；双端 129 全绿）。**  
**21 日主线 = 产品轨 P0 + tip 升钉 + P1 两债 + R2 async 族 + Cap residual pure + G.7 fputs + PREFER unbundle `async_asm_pool`（tip `708d63e0`）。** 下一前排 = 可选 tip L4 升钉 / 其它 R2 / Windows。

| 轨 | 状态 |
|----|------|
| 工程轨 | unbundle pool @ `708d63e0` 双端 prove IDENTICAL + 产品矩阵（+ G.7 fputs @ `e38f6624`；liveness/CPS Cap residual 见 §6） |
| 产品轨 tip / 钉盘 | 钉盘 **`11a57351`**；tip **`708d63e0`**（产品面 unbundle；未 tip L4 升钉） |
| mac 产品链 bstrict | ✅ **OK (129)** · `/tmp/mac_bstrict_d45ee9a4_p0.log` |
| Ubuntu L4（金标） | ✅ **OK (129)** · 全擦 + bootstrap + g05 + 矩阵 + bstrict · `/tmp/ubuntu_true_bstrict_11a57351.log` |
| 升钉 | ✅ **`deaf773b` → `11a57351`** |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](自举方法.md) | Cap / R / L / M |
| [自举步骤.md](自举步骤.md) | 可执行闸门 |
| [自举验证.md](自举验证.md) | 怎么测 |
| **本文** | 长期 KPI + 前排（**每波必改**） |
| [当前进度.md](当前进度.md) | 当天快照（**每波必改**） |
| **[当前问题分析.md](当前问题分析.md)** | tip 级产品债地图（21 日起权威） |
| [自举进度-归档-2026-07-20.md](自举进度-归档-2026-07-20.md) | **20 号全日**（只读） |
| skill `shux-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单 |
| 产品 L4 放行钉盘 | **`11a57351`** | 21 日升钉（前钉 `deaf773b` / 125）；双端 129 全绿 |
| 本波 tip | **`708d63e0`** | PREFER unbundle pool；钉盘仍 `11a57351` |
| mac 产品链 bstrict（21 日） | ✅ **OK (129)** | `/tmp/mac_bstrict_d45ee9a4_p0.log` |
| Ubuntu L4（金标 · 21 日） | ✅ **OK (129)** | 全擦 + bootstrap + g05（60 objs）+ 矩阵 + bstrict · `/tmp/ubuntu_true_bstrict_11a57351.log` |
| 前钉 mac/Ubuntu L4 | ✅ 125 @ `deaf773b` | 见 20 号归档 |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|-----|------|
| **T / EMPTY** | 18/18 | 18 号结案 |
| **N** | 54/54 IDENTICAL | 回归基线 |
| **R2 本波新增** | PREFER unbundle `async_asm_pool` + G.7 fputs + Cap residual pure 族 | unbundle @ `708d63e0`；fputs G.7 @ `e38f6624`；liveness/CPS 见 §6 |

***

## 3. 当前前排（≤3 条）

1. **R2 / 产品 unbundle**  
   - ✅ `async_asm_pool` full H=0 · ✅ **PREFER unbundle from `pipeline_glue`**（`708d63e0`）  
   - ✅ `async_liveness` / `async_cps` pure + Cap residual pure · ✅ G.7 fputs → `driver_preamble_fputs`  
   - 选题：可选 tip L4 升钉；liveness/cps 产品 PREFER 仿 pool unbundle

2. **P1 已收口** — 见 [当前问题分析.md](当前问题分析.md)  
   - ✅ libc 冲突跳过（`fda84704`）· preprocess 末行 flush（`157f1dc1`）  
   - 可选：asm user `.o` 对称；simd-s4 WARN；g05 sed 防御层收敛；Ubuntu `shux -E` 偶发 SIGSEGV 根因（prove 已 5 次重试）

3. **Windows hybrid 探针**（可选；双系统切换时再跑）

***

## 4. 与 20 号归档的关系

- 20 号全日（R2 target_cpu_pure、C6 波 3、C7 125→129、钉盘 `deaf773b`、parser has_body、shux-x 内存 B 等）已整表移入 [`自举进度-归档-2026-07-20.md`](自举进度-归档-2026-07-20.md)。  
- **勿**用归档 tip `e55adc65` 或钉盘 `deaf773b` 冒充 tip/`11a57351` 产品绿；**勿**把工程 tip `708d63e0` 写成 tip L4 已升钉。  
- 21 日新工作只记本文 §0/§2/§3/§6。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
|------|----------|
| **macOS arm64** | ✅ 产品链 129 @ `11a57351`；run-json heavy cooldown 15s |
| **Ubuntu x86_64（金标）** | ✅ L4 真冷 + 129 bstrict @ `11a57351` |
| **Windows MSYS2** | 继承 `deaf773b` hybrid；本波未重跑（可选补跑） |

***

## 6. 变更记录（倒序 · 21 日起；20 号见归档）

| 日期 | SHA | 摘要 |
|------|-----|------|
| 2026-07-21 | **`708d63e0`** | **PREFER unbundle `async_asm_pool`**：`pipeline_glue` 不再 #include 冷 seed；独立 `src/async/async_asm_pool.o` 入全部 SUPPORT + g05_relink_env；PREFER full.x + rest marker；ensure 重建 stale pipeline_x/standalone/Darwin filtered。mac+Ubuntu L2/L3 g05 + 矩阵 + prove IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`e38f6624`** | **G.7 fputs Cap residual 收敛**：删 module-local `async_liveness_fputs` / `async_cps_fputs`；`.x` emit pure 改调共享权威 `driver_preamble_fputs`（`runtime_driver_abi`）；保留 `*_fputs_i32_dec`；cold seed 仍原生 FILE*；surface re-pin。mac+Ubuntu L2 prove 2× IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`0efbc570`** | **R2 async_liveness Cap residual pure FILE* emit_***：`.x` 真迁 `emit_frame_typedef` / `emit_frame_local` / `emit_codegen_comment` + `fputs_i32_dec`；opaque `async_liveness_fputs` always seed；G.7 type_to_c/lookup；长字面量拆片（-E 串 cap~63）；宿主 layout LE；seed `#ifndef FROM_X`；surface re-pin。mac L2 + Ubuntu L3 后 prove IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`e364aaf2`** | **R2 async_cps_codegen Cap residual pure wave5（begin/hoist）**：`.x` 真迁 `async_cps_codegen_begin` / `emit_param_statics` / `emit_hoisted_lets_impl` + `store_ptr` / `emit_static_decl` / run-seed take；G.7 `async_liveness_type_to_c_buf`；opaque `async_cps_fputs`；宿主 ASTFunc/ASTParam/ASTLet/num_awaits LE；seed `#ifndef FROM_X`；surface re-pin。mac+Ubuntu L2 prove IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`33505fbe`** | **R2 async_cps_codegen Cap residual pure wave4（FILE* emit）**：`.x` 真迁 `async_cps_codegen_end` / `emit_phase_reset` / `after_await(_io)` / `after_await_impl` / `emit_sched_wrapper` + `store_i32` / `fputs_i32_dec`；opaque 桥 `async_cps_fputs` always seed；宿主 AsyncCpsCodegenCtx + AsyncFrameLayout LE；seed `#ifndef FROM_X`；begin/param_statics/hoist_impl 仍 seed。surface re-pin。mac+Ubuntu L2 prove IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`33881f79`** | **R2 async_cps_codegen Cap residual pure wave3（walk_impl）**：`.x` 真迁 `expr_references_run_async_impl` / `block_has_run_async_ref_impl` + `ptr_at` / `is_binop` / `is_walk_unary`；宿主 AST LE（RUN/SPAWN target 匹配 + 递归 walk；CALL 只扫 args）；seed `#ifndef FROM_X`；FILE* emit 仍 seed。surface re-pin。mac+Ubuntu L2 prove IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`b06cf472`** | **R2 async_liveness Cap residual pure（type/layout）**：`.x` 真迁 `lookup_var_type` / `type_to_c_buf` / `type_size` / `layout_func*` / `has_await` / `needs_cps_frame` / `analyze_func` / `module_struct_in_frame` + store/sort helpers；宿主 AST + AsyncFrameLayout LE；heap layout 4196；seed `#ifndef FROM_X`；FILE* emit 仍 seed。surface re-pin。mac+Ubuntu L2 prove IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`ec2336da`** | **R2 async_cps_codegen Cap residual pure wave2**：`.x` 真迁 `func_uses_void_entry` / `module_references_run_async` / `resolve_sched_target` / `module_has_sched_extern` + cstr/module helpers；宿主 ASTFunc/ASTModule LE 偏移；seed `#ifndef FROM_X`；surface re-pin。Cap residual 余：FILE* emit + walk `_impl`。mac+Ubuntu L2 prove IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`8d1f2452`** | **R2 async_cps_codegen Cap residual pure wave1**：`.x` 真迁 `expr_is_io_await` / `await_read` / `await_write` / `await_fd` / `future_wait` + LE load/name helpers；seed `#ifndef FROM_X`；surface re-pin。Cap residual 余：FILE* emit + walk `_impl` + module/sched。mac+Ubuntu L2 prove IDENTICAL。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`d194aff5`** | **R2 async_cps_codegen pure surface**：`.x` 吃满 io/future_wait/sched name gates + thin walk/hoist；修 `(e,target)` ABI；`#[no_mangle]` + 英文 G.9。FROM_X 省略 pure C（`slice_marker`）；walk `_impl` + FILE* emit Cap residual 仍 seed。surface + prove MODULES。mac+Ubuntu L2 prove IDENTICAL（Ubuntu L3 后 -E 重试）。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`b12bf000`** | **prove harness**：`-E` 最多 5 次重试（Ubuntu 主机偶发 SIGSEGV，含小模块）；IDENTICAL 仍硬。PLATFORM: SHARED harness |
| 2026-07-21 | **`f2ca6580`** | **R2 async_liveness pure surface**：`.x` 吃满 await walk/live/mangle/`frame_build_tag`；FROM_X 省略 pure C（`slice_marker`）；Cap residual layout/type/emit FILE* 仍 seed。`analyze_block_linear` `malloc(4096)` 非 `u8[4096]`。surface + prove MODULES。mac+Ubuntu L2 prove 5/5（`b12bf000` 重试后）。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`2a88053b`** | **R2 async_asm_pool**：`build_layout` 真迁 .x；FROM_X rest 仅 `slice_marker`（H=0）；cold seed 全 C 仍供 `pipeline_glue` #include。surface + prove MODULES。Ubuntu 初版 `u8[4096]` 栈 → -E SIGSEGV 假红，改 let-index 缓存后 5/5 IDENTICAL。mac+Ubuntu L2 prove。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`f57e23df`** | R2 async_asm_pool 初版（后由 `2a88053b` 栈修收口） |
| 2026-07-21 | **`157f1dc1`** | **P1 preprocess**：末行无 LF 时刷 `line_buf`（`preprocess_x`/`preprocess_x_buf` + seed）。历史症状 num_funcs=0/XP003 被归为 parser EOF；真产生点=预处理丢最终 `}`。mac L2：no-NL/oneline/import 探针 `-o` rv=42 + 矩阵。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-21 | **`fda84704`** | **P1 codegen**：`codegen_is_libc_conflicting_extern_name` 补全 getenv/malloc/…（跳过再声明，权威单一；seed 同 commit）。根因=SHUX 类型复述 libc 与系统头冲突，非 `*u8` 发不成指针。PLATFORM: SHARED |
| 2026-07-21 | **升钉 `11a57351`** | Ubuntu L4：`run-all-bstrict OK (129 scripts, compiler/shux is shux_asm)` · `/tmp/ubuntu_true_bstrict_11a57351.log`；mac 已 129 · `/tmp/mac_bstrict_d45ee9a4_p0.log` → **钉盘 `deaf773b` → `11a57351`** |
| 2026-07-21 | **docs** | 20 号全日归档 → `自举进度-归档-2026-07-20.md`；21 日起表；双端绿后写升钉 |
| 2026-07-21 | **`11a57351`** | **harness P0 + 地图**：run-json Darwin heavy 15s；lang-unsafe 用 `SHUX_BIN`；`当前问题分析.md`。mac 129 OK。PLATFORM: MACOS harness + SHARED |
| 2026-07-21 | **`d45ee9a4`** | **产品根修**：net-context 弱符号 + std.context link_only；user `.o` 注入；C-style cast。PLATFORM: SHARED |

***

## 7. 禁止清单（本阶段）

- [ ] 未用 20 号归档 KPI 当 tip 仍绿  
- [ ] 未把 best-effort / W3 fast 当产品放行  
- [ ] 未把 mac 129 单独当双端升钉  
- [ ] 未在 Ubuntu bstrict 未收口时宣称 tip L4 全绿  
- [ ] 未在产品轨未绿时开 R2 庆祝  
- [ ] 未 seed / `.x` 只改一边就关 P1  
