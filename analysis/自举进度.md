# Xlang / X language 自举进度

> **更新**：2026-09-20 · **tip `9e109cb1f`** wave630：**modlet 分裂归属图完整＋根修设计定案**——冷侧＝from_x rest 的 `pipeline_asm_modlet_prepare_and_emit_elf_c`（17532 行，写 `g_pipeline_asm_modlet_cold`）；热侧＝`-E` chunk 道弱成员（find/load/lea＋热表）。**top_level_let thin 只含 hoist（不含 modlet 族）**，现有 thin 无一承载全族 → **根修＝新建 `modlet_thin`（从 mega 抽取 prepare/find/load/lea/reset＋表，G.7 体＝mega leave）＋产品双端 PREFER（单成员单表）**。**双端 L4 连续十波全绿**（→`a54d1a99a`）。钉盘 **`ecdb5cc1e`**（默认不升）
> **状态**：**尚未完全自举**。主刀 **M2**（FORCE 整合层）。
> **更新**：2026-09-20 · **tip `9a68a58c2`** wave650：**FORCE 链破局——72 桩之谜＝Stage 12.0.5 自举硬门，逃生门后 mono 726KB 真码**——①勘误：w649 的「block_ptr 垃圾」是我 gdb 调用 32 位截断伪影（真指针 0x7fffcb964a00）；硬件观察点证明 block385 nso 全程未被清 → 门非败在 nso；②真门＝`asm_skip_heavy_module_func_body` 的 Stage 12.0.5 自举策略：**`nfuncs≥160 ∧ defined_ordinal≥72 → ret0-桩`＋`slots>48 → 桩`**（大自举模块尾段体故意由 C rest 提供——w648 的 72/2048 全解释）；③根修＝`XLANG_ASM_FORCE_FULL_BODIES=1` 逃生门（先只加 seed→无效：Linux 混成 mega 纯 asm 弱版赢；再上 `.x` 权威 `asm_env_force_full_bodies()`＋seed 冷孪生）；④**验证**：带 env 编 mono＝CG002 但 `code_len=726,320`（3.4× 真码）`num_labels=9269` **`num_patches=16384` 满**→下一帽＝elf ctx patch 表容量（w651：.x sidecar 偏移＋seed 结构体＋写者三方同步扩容）；产品 L2 5/5 两次（env 默认关零变化）**72 体截断定位到发射循环中途 AST 破坏（分析波）**——逐层排除：①parse 侧完整（`append_stmt_order` 全程 **40,142 次**）②codegen 入口时 AST 完好（gdb 调 C 访问器：fi455 body_ref=374 nso=7、fi456 body_ref=385 nso=23）③emit 循环全程跑完（post_fill ×2048）且门**零失败**＝纯跳过。**实锤＝循环进行到后期时 `pipeline_arena_block_ptr(ar, 385)` 返回 0x4a00 垃圾小整数**（非堆指针）→ 门读到 0 → 1976 函数静默跳体。首要嫌疑＝发射期 WA fixup／`rebuild_sparse_ifs`／fill 系中途改写/膨胀 arena sidecar 的 blocks grow_vec。**另帽（trace 实证）**：`start emit_n=2048 nf=2568`——`asm_wpo_max_funcs()=2048` 序容量截断 520 函数（FORCE 完整性也要修）。w650＝追 block_ptr 垃圾返回（sidecar blocks vec 中途失效/重分配）**FORCE 链阻断层重诊（分析波，产品未动）**——mono 重建（42s，2048T/29U）＋265/267 thins 弱编后逐层证伪：①非 WPO 剪枝（gdb：valid=0、root=-1、emit_n=2048 全进序）②非 parse 丢体（-E 全函数有体）③**真因＝parse 侧按池总量静默丢 stmt_order/体**：`backend_emit_block_body_sync_elf` 全程仅被调 **72 次**，源行 3476（`pipeline_read_file`）起 1976 函数全发空桩（prologue+mov eax,0）；把 read_file 体改成 `return 0;` 截断点不动（0x60dc 硬界）＝**容量/位置饱和而非构造中毒**。**w626 归层修正**：其「monofile 真被裁＝0」只在面层成立，体层 1976 面是空桩 → w627 force 合并（mono 强体 first-wins）＝**桩压过 thin 真体**→ flag_slot 返 NULL→`reset` 写 *0 → SEGV，w627 崩溃全链解释闭合**。w649＝找 parse 侧饱和池（onefunc→arena 提交路径的 blocks/stmt 池容量）**macOS modlet 表分裂双层根修完成，`&g` 双端 42**——①seed 层：MODLET_IN_REST 五公开面（name_is_shared／load／store／prepare／seed_nonzero）加 `XLANG_WEAK`，初始 thin+rest 混成变 weak-vs-weak first-wins（Darwin `cc -r` 弱+强=强赢 rest → 分裂；Linux `--allow-multiple-definition` first-wins 故只 mac 分裂）；②script 层：thin inject 的强 T 检测在 Darwin 用裸 `nm`（Mach-O 弱文本显示为 `T`，ELF 才显示 `W`）误判弱为强 → 触发破坏性 `--redefine-sym`（改名会连 base 自身调用点一起重定向到死副本：`addr_of` 调了 `_pabi_superseded` 的 lea → 冷表空 → `&g` CG002，lldb 反汇编 addr_of+360 实锤）→ 新 `pipeline_abi_strong_text_syms()`：Darwin 权威 `nm -m`（"weak external" vs "external"），ELF 保持裸 nm。重建后 **0 个 `_pabi_superseded`**。教训：**stamp 是产品门控不是缓存**（我全删 stamp 令 w519 等 HARD BAN 族重新落地 → pure-ld 缺 `glue_struct_lit_*` 未定义符号红；从 Ubuntu 恢复 stamp 全集后修复）。双端 &g=42＋L2 5/5＋零 L4——writer CG002 层已解（strong writer）；但 **macOS 表分裂的真正根因＝`G05_X_O_WEAK=1` 令 thin 全 weak，rest 的 STRONG 冷孪生 prepare 在 `cc -r` 合并中赢了 thin 版**（macOS `ld -r` 无 `--allow-multiple-definition`，weak+strong=strong 赢，与 Linux first-wins 不同）。**w647＝让 rest 的 modlet 冷孪生也 weak**（from_x.c 加 `XLANG_WEAK`），使 cc -r 双 weak first-wins thin 版。mac rv=42＋L2 5/5；Ubuntu &g=42＋L2 5/5；零 L4——`pabi_strong_writer.from_x.c` 编 STRONG `platform_macho_write` 后 `cc -r` 合入 pabi `.o`（macOS `ld -r` 正确解析 weak-vs-strong：strong 赢，无 duplicate 错）。mac rv=42＋L2 5/5；Ubuntu 回归 `&g`=42＋L2 5/5。**modlet macOS 注入仍有 w629 表分裂层**（prepare 写 cold 表、find 读热表；MACOS skip 维持）→w646 解。双端 L2；零 L4——①macho_write 函数里的 unbound `$stamp_prefer`（w634 错加到 macho_write 而非 modlet 函数内）②modlet inject 重加 MACOS skip（w642 weak-vs-weak 未解前 Darwin 禁注）。**根因链完整**：G05_X_O_WEAK=1（pabi thin 构建时全 weak）→ pabi `.o` 的 `platform_macho_write` weak → 与 bridge weak 桩竞态 → first-wins 桩赢 → writer -1 → CG002。**w645＝产生点修**（选择性 weak 或后处理去 weak）。mac rv=42＋L2 5/5；LINUX-only；零 L4——pabi `.o` 的 `platform_macho_write` 一直是 **weak**（`nm -m` 证 clean 态也 weak），bridge 桩也是 weak；两 weak 打架按链接序先来先得。**试移位修法反噬**（bridge `.o` 的其他 T 面（entry/main_*）在产品链被 pabi 版覆盖后回滚；clean 态也红）→ **已回滚恢复 rv=42**。真正的修法＝**让 pabi 侧该符号变 STRONG**（产生点＝G05_X_O_WEAK=1 令 thin 产物全 weak）。mac 绿态；LINUX-only；零 L4——step-over bridge 锁定 `bl +1508`→`seed_..._write_impl`→dispatch 槽指向 `platform_macho_write_macho_o_to_buf`；**合并态**该符号＝seed bridge 硬编码空壳（`sub/str/mov w0,#-1/ret`）；**clean 态**＝pabi `.x` wrapper（真体 `ldr/cbz→pipeline_macho_write_o_to_buf_c`）。合并 weaken 了 pabi 侧该符号→空壳 first-wins→返回 -1。**w643＝阻止空壳赢**。mac 已恢复 rv=42；LINUX-only；零 L4——bridge 全部 10 个 `__DATA` 槽的运行时目标经 lldb 读出并对照，**合并态全部指向正确函数**（同 clean 逻辑名）；w640 的「槽偏移→跳错拷贝」假说不成立。**-1 来自正确解析的间接调用内部的某处**。mac 已恢复 rv=42；LINUX-only；零 L4。**w642＝step-over 桥逐调用找返回 -1 的那条 bl**（绕开一切假说）——`asm_asm_codegen_elf_o`（bridge）通过读 `__DATA` 槽位（0x1003b8000/0x1003b9000 区）的函数指针间接调用（`adrp x16→ldr x16,[x16,#off]→br x16`），mac 合并后这些槽指向错拷贝或垃圾→被调函数返回 -1。这解释了 w638 调度槽发现（同机制但具体槽不同）与 w639 全部未命中（断点压在 stub 本身而不是槽内目标）。**w641＝逐 stub 读槽内真目标并归因**。mac 已恢复 rv=42；LINUX-only；零 L4——impl 16 个 -1 点与 outer→impl 的 `bl` 点**全部未命中**⇒实际执行路径根本不经过 w638 绘制的 seed 链（地址随每次重链漂移，早前断点命中的是别的拷贝）。**w640 刀口＝build 无关法**：断 CG002 诊断处，从 `rt_ab_step_finish` 帧读栈上 LR/返回地址直接拿真实 writer 调用点与被调地址，再对那个地址归因。mac 已恢复 rv=42；LINUX-only；零 L4——`platform_macho_write_macho_o_to_buf`（seed wrapper 0x10029bf5c）→ `seed_..._impl`（0x10029c690）→ 读全局槽 `[0x1003b98a0]` 非空即尾跳槽内指针（实测指向 wrapper 自身）→ impl 内 16 个 -1 装载点之一返回（首点＝槽 null 检查未命中）；**.x 权威 writer `pipeline_macho_write_o_to_buf_c` 从未被调**。w639＝对 16 个 -1 点逐一断点（或单步 impl）定位命中分支＋其读的状态量。mac 已恢复 rv=42；LINUX-only；零 L4——lldb 分层：`backend_asm_codegen_ast_to_elf` 返 0（emit 全绿）→ `platform_macho_write_macho_o_to_buf`（**seed_platform_macho_write…_impl**，w612 `-E` 赢者）返 **-1**＝写一个 2 标签 1 patch 的极小 `.o` 都失败。**单函数覆盖合并后 writer 内某分支读到错位状态**。w638＝writer 体内分段定位（resolve_patches/strtab/dysymtab 哪步 -1）。mac 已恢复 rv=42；LINUX-only；零 L4——三对照（A 无 let 无函数 OK／B 仅 let OK／C 仅函数无数据→CG002）＋函数从未被调＋合并无重复 T＋产物为正常 Mach-O ⇒ 单函数覆盖经 mac `cc -r` 合并即损坏产品 `.o` 布局（节/符号序/reloc 表位移），emit 启动读到错位状态。w637＝diff 合并体 vs 干净体结构。mac 已恢复 rv=42；LINUX-only 维持；零 L4——只注入这一个纯查询函数（无表访问、仅 extern 读 module lets）即令首函数 emit CG002；二分两轮方法论修正（①子集需补兄弟 extern 声明②坏二进制须先恢复+g05 重建健康基线再注入）。mac 已恢复绿（rv=42）。**w636 刀口＝lldb 注入态二进制断 `name_exists` 看调用方与返回路径**（疑 if 条件内裸 extern 调用在 mac 编译路径的行为差异）。**验证节奏＝双端 L2**。钉盘 **`ecdb5cc1e`**
> **状态**：**尚未完全自举**。主刀 **M2**。
> **下波（w632）**：**modlet 闭包裁剪＋复注**：把 thin 裁到「触表族＋无态 helper」；带态 helper 的调用点包 unsafe；独立绿→注入→`&g` 双形态/标量写/L2→双端 L4。**禁止**再用 `-E` 当修法。

闸门：双端 L2；暂停 `xlang check`；L4 仅整波／谈自举。

### 维护约定

1. 每波必改本文（KPI／前排≤3／§6 一行）。终局债改 [`C迁移追踪.md`](C迁移追踪.md)。
2. §6 倒序 · 一行一波。过期整日流水日切归档后主文档重写。

***

## 0. 一句话

**尚未完全自举** · 钉盘 **`ecdb5cc1e`** · tip **`c13a2fcf6`** · 19 日收于 **w617** asm_locals 双端 PREFER。

| 指标 | 状态 |
|------|------|
| 钉盘 L4 | ✅ 双端＠**`ecdb5cc1e`** |
| tip L4 | ✅ **双端全绿**＠`a54d1a99a`（连续十波）· 产品态健康 |
| 本波级别 | **w630＝设计波**（归属图＋根修设计定案） |
| 下一波 | **w633** thin 瘦身至种子＋表访问器→复注→L4 |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举效率方法-M2主链.md](自举效率方法-M2主链.md) | M2 主链 |
| [C迁移追踪.md](C迁移追踪.md) | 终局债（短状态表） |
| **本文** | KPI · 前排 · §6（9 月 20 日） |
| [自举进度-归档-2026-09-19.md](自举进度-归档-2026-09-19.md) | **9 月 19 日**（w597–w617 全记录） |

***

## 2. 仪表盘

| 项 | 结果 |
|----|------|
| 产品 bstrict | **131** 套（双端 tip 全绿＠`eab2c15b1`） |
| 钉盘 | **`ecdb5cc1e`** |
| MG／Stage8 | MG ✅ · Stage8 **30/30** |
| SSH | `ubuntu-server`／`windows-server` |

***

## 3. 当前前排（≤3）

1. **主迁刀（w631）** — **建 `runtime_pipeline_abi_modlet_thin.x`**：抽 mega 的 prepare_and_emit/find/load_to_rax/lea_rax_arch(+rbx)/reset/get_n/set_n/off_*/cell_* 全族（G.7 体＝mega leave；seed 孪生同 commit）→ 注入函数 PREFER 双端（w615 式 stamp 门控）→ 上述验证链→L4。
2. **次排** — FORCE 链族原子合并复验（diag 族应随 w631 复活）；9 面盲区卫星；2 失败 thin；malloc_trim weak。
3. **立卡不动** — arr_return 族（`x=call() then if` UND-drop＝真实发射能力缺口，M1 类）；MSVC va／WIN leftover；eq 深链；spawn 真体；nest 冻帽 64；dyn wrapper MEMORY。
3. **立卡不动** — P4b 侧支；MSVC va／WIN leftover；eq 深链；spawn 真体；nest 冻帽 64；dyn wrapper MEMORY。
3. **立卡不动** — P4b 侧支；MSVC va／WIN leftover；eq 深链；spawn 真体；nest 冻帽 64。
3. **立卡不动** — P4b 侧支；MSVC va／WIN leftover；eq 深链；spawn 真体；nest 冻帽 64。

***

## 4. 本日要点（9 月 20 日）

| 波 | 结论 |
|----|------|
| w650 | **FORCE 破局波**：①勘误 w649 垃圾指针＝gdb 32 位截断伪影（观察点证明 nso 全程 23 未清）②真门＝`asm_skip_heavy_module_func_body` Stage 12.0.5 自举硬门（`nfuncs≥160∧ord≥72→桩`、`slots>48→桩`）——自举架构故意让 C rest 供体③根修＝`XLANG_ASM_FORCE_FULL_BODIES` 逃生门：**先 seed-only 无效（混成 mega 纯版赢）→ 上 `.x` 权威 `asm_env_force_full_bodies`＋seed 冷孪生（G.7）**④mono 带门＝**726KB 真码**（3.4×），下一帽＝patches[16384] 满（w651 扩 ctx 容量）。L2 5/5×2（默认关）。commits `b11ccc0d2`/`9a68a58c2` |
| w649 | **72 体截断根因再下钻（分析波）**：排除 parse（40,142 appends 完整）与入口态（fi455/456 nso=7/23 完好）；post_fill×2048 全跑、门 0 失败＝纯跳过；**gdb 观察＝mega_body 循环中 `pipeline_arena_block_ptr(ar,385)` 返 0x4a00 垃圾**→后段函数门全假。嫌疑＝发射期 fill/wa_fixup/rebuild_sparse_ifs 破坏 arena sidecar blocks vec。副产物：trace 实证 `emit_n=2048 nf=2568`＝2048 序容量帽（520 函数整体丢失）。产品未动（w647 双端绿保持） |
| w648 | **FORCE 链阻断层重诊（分析波）**：重建 mono（Ubuntu 42s）＋thins 265/267；逐层证伪：WPO 剪枝（valid=0 排除）、parse 全丢体（-E 有体排除）、「if(call())守卫」构造中毒（改体实验排除）；**真因＝parse 侧池总量饱和静默丢体**（block body 仅 72 次调用；0x60dc 硬截断，源行 3476 起 1976 空桩）。修正 w626 归层（面层✓体层✗）；w627 SEGV 全链解释（mono 桩 first-wins 压 thin 真体→flag_slot NULL）。探针方法学沉淀：lldb/gdb 需 launch 后设断（macOS）；nm 弱文本显示 T 已 w647 修判。产品未动（w647 双端绿保持） |
| w647 | **macOS modlet 表分裂双层根修（收口）**：①`from_x.c` MODLET_IN_REST 五面加 `XLANG_WEAK`（宏块置于簇首；PE/MinGW 空宏不变）；②`ensure_host_cc_seed_o.sh` 新 `pipeline_abi_strong_text_syms()` 弱感知强 T 谓词（Darwin `nm -m`）替换 already_defined＋weaken 两个 verify 循环的裸 nm 检查——根因＝Mach-O 弱文本裸 nm 显示 T 误触发 `--redefine-sym` 改名（连 base 内部调用点一起改到死副本）。stamp 教训：全删 stamp 令 w519/w564 等 HARD BAN 族重新落地（pure-ld 未定义 `glue_struct_lit_*` 红）→ 从 Ubuntu tar 恢复 stamp 全集。验证：双端重建 0 superseded；macOS `&g` 编+链+跑=42（av_test/av2/av3 绿）；mac L2 5/5；Ubuntu `&g`=42＋L2 5/5（SHA `ed5f01ded`） |
| w646 | **MACOS unskip＋strong writer 接入＋表分裂根因终定**：去除 modlet inject MACOS skip；strong writer .o 接入 inject 成功路径。writer CG002 层已解。**表分裂根因终定＝macOS `cc -r` 无 `--allow-multiple-definition`，`G05_X_O_WEAK=1` 令 thin 全 weak，rest 的 STRONG 冷孪生 prepare 在合并中赢了 thin 版**（Linux 有 `--allow-multiple-definition` first-wins thin 版故工作）。修法＝rest 冷孪生加 `XLANG_WEAK` 使双 weak first-wins。mac rv=42＋L2 5/5；Ubuntu &g=42＋L2 5/5 |
| w645 | **strong writer seed 落地（探针波）**：`seeds/pabi_strong_writer.from_x.c` 编 STRONG `platform_macho_write`，`cc -r` 合入 pabi `.o` 后 macOS `ld -r` 正确解析（strong 赢无错；不同于最终 59-obj 链接的 strong+weak=duplicate）。mac rv=42＋L2 5/5。Ubuntu 回归 `&g`=42＋L2 5/5。**modlet macOS 注入仍有独立层＝w629 表分裂**（prepare 写 cold、find 读热；两个表都在最终二进制且都 b=local）→MACOS skip 维持→w646 解 |
| w644 | **双 bug 修＋根因链完整化**：①w634 的 MACOS skip 被错加到 macho_write_thin 函数内引用不存在的 `$stamp_prefer` → try-heat 崩（unbound variable）→已修；②modlet inject 重加 MACOS skip。**根因链完整**：`G05_X_O_WEAK=1`（pabi thin 构建全 weak）→ pabi `.o` 的 `platform_macho_write` weak → 与 bridge 桩 weak-vs-weak → first-wins 桩赢 → writer -1 → CG002。试修＝去 WEAK=1（ld 拒 13 重复）＋加 strong 到 seed_link_compat（strong+weak=Darwin 报 duplicate）→均回滚。mac 恢复 rv=42＋L2 5/5（正规 --rebuild-only） |
| w643 | **根因精修（探针波）**：`nm -m` 证 pabi `.o` 的 `platform_macho_write` **clean 态也是 weak**（非合并所致）；bridge 桩也是 weak→两 weak 竞态按链接序。试修＝移 bridge `.o` 到链尾→**反噬**（clean 态也红＝bridge 的 T 面（entry/main_*）入链序变先赢/后输改变了解析面）→git checkout 回滚恢复 rv=42。**真修法＝让 pabi 侧该符号 STRONG**：G05_X_O_WEAK=1 令 thin 产物全 weak（macho_write thin w612 注入时设了 WEAK=1） |
| w642 | **根因定案（探针波）**：step-over bridge 87 步后 x0 变 -1 ＝ `bl +1508`→`seed_..._write_impl`→dispatch 槽指向 `platform_macho_write_macho_o_to_buf`——**合并态该符号是 seed bridge 硬编码空壳**（体＝`sub/str/mov w0,#-1/ret`）；clean 态则＝pabi `.x` wrapper（真体调 `pipeline_macho_write_o_to_buf_c`）。mac `cc -r` 合并 weaken 了 pabi 侧该符号→空壳 first-wins→无条件返回 -1。**整条 w635–w642 追凶链关闭：modlet thin 本身没问题，是合并对不相关符号解析序的破坏**。mac 恢复 rv=42 |
| w641 | **GOT 槽假说证伪（探针波）**：lldb 运行时读 bridge 全部 10 个 `__DATA` 槽（chained fixup 由 loader 解析后才可读），合并态/clean 态均解析到正确逻辑函数（`_pipeline_asm_wpo_reach_clear` 等 10/10 SAME）。w640 假说不成立：**-1 不来自槽偏移而来自正确解析的间接调用内部**。mac 恢复 rv=42。w642＝step-over 桥逐调用直接定位（不再猜架构） |
| w640 | **-1 源头架构浮出（探针波）**：反汇编 `asm_asm_codegen_elf_o`（bridge）前半——所有跨模块调用经 **GOT 式 stub**（`adrp x16→ldr x16,[x16,#off]→br x16`，函数指针存于 `__DATA` 0x1003b8000/0x1003b9000 区）。mac 合并后某个/某些槽指向错拷贝→间接调用返回 -1。统一解释：w638 调度槽（同机制）＋w639 全未命中（断点压 stub 而非槽内目标）。mac 恢复 rv=42。w641＝逐 stub 读槽内真目标→clean 对照 |
| w639 | **侦察修正（探针波）**：impl 16 个 -1 点＋outer→impl `bl` 点全部断点未命中 ⇒ w638 绘制的 seed 链并非实际执行路径——地址每次重链漂移，早前「命中」的断点实为同名的其他拷贝（`platform_macho_write_macho_o_to_buf` 在二进制中至少两处 T 区）。**方法论教训＝漂移地址上的断点结论不可信**。mac 恢复 rv=42。w640＝栈上 LR 取证（build 无关） |
| w638 | **writer 调度链绘出（探针波）**：lldb＋反汇编——seed wrapper→impl→全局槽 `[0x1003b98a0]`（实测＝wrapper 自身）非空尾跳；impl 16 个 -1 点，首点（槽 null）未命中⇒-1 在更深处；**.x 权威 writer 全程未被调**＝合并态下调度即入 seed 链且在 impl 内挂。对照线索：clean 态同结构却能写成功（差异在 impl 深处的状态读取）。mac 恢复 rv=42 |
| w637 | **失败环节定案（探针波）**：lldb 分层断点——mega_body 返 0（emit 成功）→ `platform_macho_write_macho_o_to_buf`（**seed 孪生体**＝w612 `-E` 赢者）返 **-1**：写 main.x 的极小产物（code_len=40/2 标签/1 patch）都失败 ⇒ 破坏面收敛到 **writer 内部某分支读错位状态**（合并位移所致）。mac 恢复 rv=42。w638＝writer 分段定位 |
| w636 | **mac 破坏面定案（探针波）**：三对照隔离（A 头 OK／B 仅数据 OK／C 仅函数→CG002）＋lldb 证赢者拷贝从未被调＋弱化正常（`_pabi_superseded` 改名无重复 T）＋产物正常 Mach-O（非 libtool 回退）⇒ **单函数覆盖经 mac `cc -r` 合并即产生布局级损坏**（疑节/符号序/reloc 位移，emit 启动读错位状态）。mac 恢复 rv=42。w637＝结构 diff 定位位移数据→修 mac 合并路径 |
| w635 | **Darwin CG002 二分（探针波）**：修正版二分（每轮恢复 base＋g05 重建健康二进制再注入，避免坏二进制连坐）→ **收敛＝单函数 `asm_module_top_level_let_name_exists`**。该函数为纯查询（无表访问）：遍历 module 顶层 let 比名，全部调用走 extern；**if 条件内裸 extern 调用**（`if (pipeline_module_top_level_let_name_byte_at(...) != ...)`）是 wrapper 唯一未变换的形态（但 thin 独立编译绿）＝w636 首要疑点。二分方法论教训×2 已记录。mac 已恢复 rv=42 绿态；注入保持 LINUX-only |
| w634 | **w633 勘误＋LINUX-only**：①w633 双端 L4 因残留 w631 stamp **静默跳过 thin**（L4 绿≠修复入链）——清 stamp 重注后 Ubuntu `&g`＝42＋L2 5/5（真态）②**Darwin 注入即 CG002**（首函数 emit 失败 code_len=40、modlet_find 断点未命中＝Mach-O 侧遮蔽类）→ 注入 LINUX-only＋mac 重建回 L2 5/5。**教训＝stamp 也是旧态藏匿点**（与旧 `.o` 同类）；验证节奏按用户指令改为**双端 L2**，L4 随批次收口 |
| w633 | **modlet 极小核心入链＋`&模块标量 cell` 缺口关闭**：thin 只含 48 个 `pipe_modlet_*`/`pipeline_asm_modlet_*` 函数（**无人同名→first-wins 永不遮蔽**＝w631/632 两轮失败的根治设计）；表＋strpool 仅 2 common；43 extern 全 unsafe 包裹（declare-then-assign；多行语句合并包裹）。独立绿（T=48/UND=44）。Ubuntu 复注：**`&g` 双形态 build=0 run=0＋写经指针读回全局＝42（w628 记录的产品级缺口正式关闭）**＋标量写 5＋L2 5/5。**双端 L4 全绿**＠`c38babac2`（U 12m37s / D 21m33s）＝连续第十一波 |
| w632/632b | **裁剪版 thin：独立绿→复注仍崩→再 BAN**。无态闭包 159＋27 带态跨 TU（14 个原私有 helper 于 mega `#[no_mangle]` 导出；调用点 unsafe 包裹＝唯一改体分歧；scoped-let 作用域坑修 10＋12 处）。独立 `-c` 绿（rc 0）＋mega 自身绿。**产品复注仍启动崩**（新签名：`memcmp`←`driver_source_scan_top_level_import`＝文件读路径）——非传递态分析漏掉行为耦合 helper 或与其它族 thin 的 dup-T 赢面。`200dda474` 再 BAN；Ubuntu rebuild-only 恢复 L2 5/5。**w633＝thin 瘦身至 15 种子＋表访问器，其余全 extern** |
| w631/631b | **modlet thin：独立绿→产品断→硬 BAN→恢复**。生成器终修：①声明终止 `): ret;`（原 `);` 永不触发→extern 块空→prepare XT001）②207 个单行定义致解析失同步③docblock 文本误判定义。thin 独立 `-c` 绿（T=228/UND=28/40 common；`37e31176e`）＋注入接线（`a18e77480`）。**产品首注即断**（L2 0/5「cannot read file」＝启动级崩）——全闭包把**带态 helper**（arena sidecar/grow_vec 及其 `g_*`）一并拷贝，first-wins 后状态跨成员分裂＝自造双态（正是要修的类！）。`7865f519c` 硬 BAN（stamp-only）；Ubuntu rebuild-only 恢复 L2 5/5。**w632 刀口＝裁剪闭包至触表族＋无态 helper，带态载体调用点包 unsafe** |
| w631 | **modlet_thin 生成（WIP＠`021fb54cf`）**：完整生成器三迭代——①多行签名/注释剥离修复②全闭包拷贝策略（164 函数；避免 extern 化触发 T001）③伴生 let 收集＋去重（修 P012）。现 parse 绿；剩 prepare(#0) bare XT001（体逐字同 mega＝上下文性分歧；下一刀＝对半二分闭包或 diff 50 函数中间版的 extern 块——中间版曾编过 prepare）。**未注入产品**（WIP 明示） |
| w630 | **modlet 分裂归属图＋根修设计（设计波）**：冷侧＝from_x rest 的 prepare（17532，`reset_cold`＋`g_pipeline_asm_modlet_cold`）；热侧＝`-E` chunk 道弱成员（find/load/lea＋热表，产品 `.o` 里 find 为 **W 弱**、endbr64＝cc 产物）。**关键排除**：top_level_let thin 只含 hoist（1185 行无任何 modlet 函数）；264 个纯 asm thin 亦无一定义全族者 → 无现成 ONE-set 载体。**设计定案＝新建 modlet_thin**（抽 mega 全族约 10 函数，单成员单 Lxml 表，PREFER 双端）——w631 落地 |
| w629 | **&cell 缺口根因定案（诊断波）＝modlet 双表分裂**：lldb 全链：①prepare 入口 nlets=1、init_ref=2 均有效（排除早退/谓词）②prepare 返回后**热表 n=0 而 `_cold` 表 n=1**——活体 prepare（0xca2b4，`_cold` 家族成员）与活体 find（0x4ed38，另一成员）各写各读两份表（产品 `.o` 同时有 `b _g_pipeline_asm_modlet`＋`b _g_pipeline_asm_modlet_cold`＋局部 `t _…_reset_cold`）③故 addr_of→lea→find(-1)→CG002 -99；mono `mov $0` 同根。读写为何正常：读走常量折叠/其他 home，写走不含 find 的 store 路径。修法已明＝族单边化（ONE-set） |
| w628 | **首崩根因定案（诊断波）**：mono 的 `pipeline_diag_emitted_flag_slot` 体＝`mov $0x0,%eax` **无 LEA 无 reloc**（gdb：slot() 返 NULL）——不是合并问题，是 `&模块级标量 cell` 的发射缺口。用户级双形态复现（`let p:*i32=&g`／`slot(){return &g}`）均 **CG002 -99**（产品今天就红）；lldb 证 addr_of 权威被调、off<0 非本地分支进入 `pipe_modlet_lea_named_binding_addr_to_rax` 后失败（`modlet_find`/`lea_rax_arch` 待定位哪环）。reads 正常（t1 探针）＝仅 ADDR_OF 缺。产品态已恢复并双端验证绿 |
| w627 | **FORCE 首链实验（探针波）**：①263/265 thin 弱编（2 失败：assign_rhsrax_arms/return_elf_impl 记债）②mono 强体 first-wins＋thins 弱补充＝force.o（3429 T/197 UND）③缺口仅 9 真面＋malloc_trim（C 侧 weak、mono 强 UND→stub 满足）→＋备份补 9 面＝**3441 T 链接闭环、g05 绿**。运行态首崩（L2 0/5）＝`pipeline_diag_emitted_reset` SEGV（gdb 栈：rt_ab_step_read_pp→driver_run_asm_backend）＝**状态分裂类**（cell 初始化/使用分属两份）——链型证实可行，整合层＝族原子合并。**w625 盲区记录**：mono 自身 UND∩rest 提供的面当时被当自引用剔除（9 面）。产品已恢复（备份 .o 回位＋g05＋L2 5/5） |
| w626 | **FORCE 归层定案（分析波）**：264 面严格归层（初版正则误把 extern 声明当定义——修正为「定义带体」判据＋逐面直接 grep 复核）→ **①monofile 真被裁＝0**（初判 105 全是 extern 声明或 thin 已有体；`glue_arena_expr_at_ref`/`pipeline_module_func_ref_set` 也各有 thin 体）**②C-only＝0 ③264/264 卫星 thin 已有权威体**。结论：FORCE 无需任何新 `.x` 代码，落地＝编排（monofile＋卫星链替换 rest）。w627 首链实验的输入齐备 |
| w625 | **FORCE 卫星集清单（分析波）**：方法＝收集全部 59 链成员对 pabi 的 UND 引用（2887 条）→ 减 monofile T（2048）→ 减 libc/系统面 → 减独立 TU 全责面（backend_arch_emit／enc_c／ast_／lexer_／diag 等 705 条）→ 减产品 `.o` 也不提供的 → **264 个真卫星面**（`pipeline_*` 访问器／`pipeline_onefunc_*`／`typeck_*` 检查器／`glue_*` 填充器／parser_diag 等；全部现由 rest+overlay 提供）。另证：`XLANG_ASM_WPO_DCE=0` 不改发射面（2048 不变——emit 序的 face 门另有其因，归层①的证据）。DCE=0 全量 `.o` 与默认逐字节同（211466B） |
| w624 | **mega monofile 纯 asm 打通**：w623 的「codegen 起点 CG002」根因＝`pipe_modlet_max()=256`＜mega 自身 331 个可注册顶层 let（loud-fail 门正确触发；trace `mega_body_c modlet prepare fail` 直指）。根修 cap 256→512（.x 缓冲 86020＋seed 宏同步；表偏移均派生自 max 自洽）。**Ubuntu 全量纯 asm 编 mega 成功**：exit 0、211KB、**2048 T**（WPO emit_n=2048）＋**29 UND**（malloc/free/strchr＋driver/parser 外部面）——FORCE 阻断表头清除，M2 终局通道打开。**双端 L4 全绿**＠`a54d1a99a`（U 12m37s / D 21m30s）＝连续十波 |
| w623 | **mega FORCE 前置分类（分析波）**：`XLANG_PABI_ALLOW_PURE_ASM=1`＋25 分钟盒内 Ubuntu 单文件纯 asm 编 92k LOC mega——**parse/typeck 全过（旧 typeck 墙保持关闭），codegen 起点即 CG002**（code_len=0/num_labels=0：未发任何函数体，阻断早于首函数完成）。结论：FORCE 阻断表头＝codegen 极早期构造（模块前导级），下一步逐函数二分收清单；thins 全族独立 `-c` 绿（w615–w622 已证）→ 阻断大概率在跨函数/全局发射序，非单函数体 |
| w622 | **wpo_dump orch 产品 PREFER 双端**：w594「PRODUCT PREFER L2 SEGV 0/5」＝w613 COMMON 类（orch cell 钉只读→编排器自写崩）。独立 -c T=10/UND=21、6 cell 全 common；fresh rebuild-only 后单族探针 g05 链过＋dump JSON v2 233B＋L2 5/5。正式 stamp 门控 PREFER（main＋orch 全族零 host-cc）。**双端 L4 全绿**＠`e798ac3fe`（U 12m41s / D 21m22s）＝连续九波（D 侧一次并发双跑致 phase1 假红，幸存跑全绿）。peel／fixed_array_copy FULL 延后——raw 跨族注入破交叉引用（deref finish/scalar UND），须族函数翻转 |
| w621 | **x86 smash 余族八族批量 LINUX PREFER**：w597–w608 整类「smash leftover」墙确认全为旧组成时代产物（批量探针初尝遇 helper 外漏→改正式翻转＋L4 权威电池路线）。八注入函数 LINUX 分支 `PREFER_ASM=0→1`（MACOS 分支不动）。**双端 L4 全绿**＠`63afc6ea7`（U 12m52s / D 21m28s）＝连续八波；Ubuntu 终版五组原波复现电池全对（if-return 7／u8 idx 7／as-idx 3／call-arg 7／deref 42） |
| w620 | **assign_var 7 叶族产品 PREFER 双端放开**：w600「smash」（`sub $0x898` 无 endbr64、`di` 不写回、while 挂起）经 `inject-pabi-leaf` 7 叶探针证实为旧组成时代墙——当代链上 w600 复现＝3、嵌套 63、if-in-while 4、L2 5/5（MACOS 本就跑 PREFER overlay）。正式翻双端 PREFER（w620 prefer stamp；w473 heal stamps 维持）。**双端 L4 全绿**＠`9554b09a0`（U 12m35s / D 21m33s）＝连续七波。x86 smash 族首刀墙倒——余族按同模式批量重验 |
| w619 | **block_final_expr 源级 T001 修源＋产品 PREFER 双端**：thin 六处裸 extern 调用（final_expr ref／has_return／emit module+func_index／ctx layout／pipe_load）补 unsafe（thin＋mega 孪生同步；unsafe 在 `-E` C 输出为 no-op，seed 不动）。独立 `-c` 由红转绿（T=1/UND=14）。w601 双策略（LINUX -E／MACOS overlay）退役→双端 PREFER（w619 stamp）。Ubuntu：注入＋L2 5/5＋if-in-while 4＋嵌套 let 63 ✓。**双端 L4 全绿**＠`e9127a322`（U 12m44s / D 21m28s）＝连续六波。COMMON 清场累计 6 TU 零 host-cc |
| w618 | **emit_ctx 三件套产品 PREFER 双端放开**：w380（Darwin BRANCH26＋Ubuntu PREFER L2 SEGV）／w601（LINUX -E 双 BSS option SEGV）墙＝w612 typed-reloc＋w613 COMMON/双 sidecar 类。独立探针：bss T=15/UND=0（6 cell 全 common）、func_index T=2、module_dep T=4（2 cell）。**三件作为 ONE set 注入**（共用 w618 prefer stamp；部分替换正是 w601 双 BSS 诱因）。Ubuntu：干净注入＋g05＋**L2 5/5（opt=102＝w601 option getter 路径反绿）**＋if-in-while 探针 4 ✓。**双端 L4 全绿**＠`fbb246551`（U 13m27s / D 21m29s）＝连续五波。三 TU 双端零 host-cc；block_final_expr 自身 T001（fn#14 extern 未 unsafe）另记 w619 |

***

## 5. 与归档

- **19 日**：[自举进度-归档-2026-09-19.md](自举进度-归档-2026-09-19.md)（w597–w617：w613 COMMON shndx 权威根修、w613b 冷孪生体解锁冷链、w614/614b ARM64 自然栈参＋宽度感知存储、w615 wpo_dump 双端 PREFER、w616 vtable wrapper 自然转发、w617 asm_locals 双端 PREFER；双端 L4 连续四波绿）
- **C 追踪流水账**：[archive/C迁移追踪-流水账归档-20260919.md](archive/C迁移追踪-流水账归档-20260919.md)
- **现行钉盘＝`ecdb5cc1e`**。

***

## 6. 变更记录（倒序 · **9 月 20 日**）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-20 | wave650 | 72 桩＝Stage12.0.5 自举硬门；FORCE_FULL_BODIES 逃生门→mono 726KB 真码；下一帽 patches[16384]。`9a68a58c2` |
| 2026-09-20 | wave649 | 72 体截断＝发射循环中 AST 破坏（block_ptr 返垃圾）；2048 序容量另截 520 函数。分析波。`d3674cf3e` 后 |
| 2026-09-20 | wave648 | FORCE 链重诊：mono 72/2048 真体＝parse 侧池饱和静默丢体；w627 SEGV 闭环。分析波无产品改動。`00f6dee72` 后 |
| 2026-09-20 | wave647 | macOS modlet 表分裂双层根修：rest 五面 XLANG_WEAK＋强 T 检测弱感知（nm -m）；stamp=产品门控非缓存（w519 误落地教训）。双端 &g=42＋L2 5/5。`ed5f01ded` |
| 2026-09-20 | wave646 | MACOS unskip＋strong writer 接入＋表分裂根因终定（cc -r weak+strong=strong 赢）。`3f73a2633` |
| 2026-09-20 | wave645 | strong writer seed（cc -r weak-vs-strong 修）；modlet macOS 表分裂留 w646。`cf560117c` |
| 2026-09-20 | wave644 | macho_write unbound var 修＋modlet MACOS skip 重加＋根因链完整化。`7c9a244b1` |
| 2026-09-20 | wave643 | 根因精修＝weak-vs-weak 竞态；移位修法反噬已回滚。`b0ae0c911` 后 |
| 2026-09-20 | wave642 | 根因定案＝seed bridge 空壳 stub 赢了 .x writer（合并 weaken 所致）。`52156d61e` 后 |
| 2026-09-20 | wave641 | GOT 槽假说证伪（10/10 SAME）。`85c567087` 后 |
| 2026-09-20 | wave640 | -1 源头＝GOT 式 stub（__DATA 槽位函数指针）。`8a2a2ae4c` 后 |
| 2026-09-20 | wave639 | 侦察修正：16 点＋bl 点全未命中＝执行路径不在 w638 链上（地址漂移）。`d92d54217` 后 |
| 2026-09-20 | wave638 | writer 调度链绘出（槽→wrapper 自身；.x writer 未达）。`d40e821f2` 后 |
| 2026-09-20 | wave637 | 失败环节＝macho writer（seed 孪生）返 -1（emit 成功）。`3aa08f6ac` 后 |
| 2026-09-20 | wave636 | mac 破坏面定案＝ld -r 布局级损坏（三对照＋未被调＋无重复）。`22a587150` 后 |
| 2026-09-20 | wave635 | Darwin 二分收敛 name_exists（单函数即破）。mac 绿态恢复。`3295d1cc1` 后 |
| 2026-09-20 | wave634 | w633 勘误（stamp 藏 thin）＋Darwin CG002→LINUX-only。双端 L2 5/5。`29f3d8ef8` |
| 2026-09-20 | wave633 | modlet 极小核心（T=48）入链；&模块标量 cell 缺口关闭（写读回＝42）。双端 L4 全绿＠`c38babac2`＝连续十一波。`95bf0bb22`/`c38babac2` |
| 2026-09-20 | wave632/632b | 裁剪版独立绿→复注仍崩→再 BAN＋恢复。`bc2c9cbd8`/`200dda474` |
| 2026-09-20 | wave631/631b | modlet thin 独立绿→产品首注断（闭包带态双态）→硬 BAN＋恢复。`37e31176e`/`a18e77480`/`7865f519c` |
| 2026-09-20 | wave631wip | modlet_thin 生成器＋WIP 入库（parse 绿；prepare XT001 待二分）。`021fb54cf` |
| 2026-09-20 | wave630 | modlet 分裂归属图＋根修设计（新建 modlet_thin＋PREFER）。设计波。`9e109cb1f` 后 |
| 2026-09-20 | wave629 | &cell 缺口根因＝modlet 双表分裂（prepare 写 cold／find 读热）。诊断波。`ddd421e77` 后 |
| 2026-09-20 | wave628 | 首崩根因＝`&模块标量 cell` 发射缺口（产品级；mono 吞成 mov $0）。诊断波。`3553fb5ee` 后 |
| 2026-09-20 | wave627 | FORCE 首链：链接闭环（3441 T）；运行首崩＝diag 状态分裂。产品已恢复 L2 5/5。`87fd48582` |
| 2026-09-20 | wave626 | FORCE 归层定案（0/0/264；纯编排问题）。分析波无产品改動。`30a8f7866` 后 |
| 2026-09-20 | wave625 | FORCE 卫星集清单（264 面；分析波无产品改動）。`f00dae172` 后 |
| 2026-09-20 | wave624 | modlet cap 256→512；mega monofile 纯 asm 打通（2048 T/29 UND）。双端 L4 全绿＠`a54d1a99a`＝连续十波。`a54d1a99a` |
| 2026-09-20 | wave623 | mega FORCE 前置分类（分析波）：monofile 纯 asm parse+typeck 过、codegen 起点 CG002。无产品改動。`8fee08849` |
| 2026-09-20 | wave622 | wpo_dump orch PREFER 双端（w594 墙＝w613 类；wpo_dump 全族零 host-cc）。双端 L4 全绿＠`e798ac3fe`＝连续九波。`e798ac3fe` |
| 2026-09-20 | wave621 | x86 smash 余族八族 LINUX PREFER（w597–w608 整类墙倒）。双端 L4 全绿＠`63afc6ea7`＝连续八波。`63afc6ea7` |
| 2026-09-20 | wave620 | assign_var 7 叶族 PREFER 双端（w600 smash＝旧组成墙；族零 host-cc）。双端 L4 全绿＠`9554b09a0`＝连续七波。`9554b09a0` |
| 2026-09-20 | wave619 | block_final_expr T001 修源＋双端 PREFER（TU 零 host-cc；w601 双策略退役）。双端 L4 全绿＠`e9127a322`＝连续六波。`e9127a322` |
| 2026-09-20 | wave618 | emit_ctx 三件套产品 PREFER 双端（3 TU 零 host-cc；w601 option 路径反绿）。双端 L4 全绿＠`fbb246551`＝连续五波。`fbb246551` |
| 2026-09-20 | docs | 日切归档 19 号（w597–w617）；20 号主文档重写简写。钉盘仍 `ecdb5cc1e`。 |
