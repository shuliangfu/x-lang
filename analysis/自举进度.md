# Xlang / X language 自举进度

> **更新**：2026-07-30 · tip 快照 wave827 · 产品钉盘仍 **`53fd80927`**\
> **基线 SHA（产品 L4 放行钉盘）**：**`53fd80927`**（wave710 双端 L4 冷 + 129 全绿）\
> **产品 bstrict 套件数**：**129**\
> **产品代码 tip（钉盘）**：**wave710** · **`53fd80927`** · **文档 tip**：wave827（MG 不升钉）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态表 only）· [Makefile迁移表.md](Makefile迁移表.md)\
> **git 锚点（重命名前）**：tag **`anchor-pre-xlang-rename`** · **git 锚点（Cap 轨前）**：tag **`anchor-pre-wave576`** / **`anchor-pre-wave577`** · **git 锚点（wave710 L4 前）**：tag **`anchor-pre-wave710-l4`**\
> **现行工程**：品牌/CLI/env/ABI 已 **xlang**（主域 **`xlang.dev`** · 仓库 **`github.com/shuliangfu/x-lang`**）。\
> **本波状态**：wave827 **std_x FORCE dep-thin ✅**（22 · FORCE+ensure · **非**物理删）· wave826 **formal_mod FORCE dep-thin ✅**（38 · FORCE+ensure · **非**物理删）· wave825 **std_x shell-primary catalog ✅**（22 · ensure · **非**物理删）· wave824 **E_DIRS list→mk ✅**（dir-roots 26 · `mk/x_e_dirs.mk` · **非**物理删）· wave823 **SOURCE_DEPS list→mk ✅**（fixed 19 · `mk/x_source_deps.mk` · **非**物理删）· wave822 **RELINK_LEGACY list→mk ✅**（RELINK fixed 14 · `mk/driver_seed_composites.mk` · **非**物理删）· wave821 **ARCH_EXPERIMENT list→mk ✅**（EXPERIMENT 7 · `mk/archaeology_experiment_objs.mk` · **非**物理删）· wave820 **OBJS_CORE list→mk ✅**（product 16 · `mk/objs_core.mk` · **非**物理删 · Makefile 仍在）· wave819 SEED_LINK_PICKS list→mk ✅ · wave818 SEED_MODE list→mk ✅ · wave817 PIPELINE_X list→mk ✅ · wave816 DRIVER_SUBCMD list→mk ✅ · wave815 archaeology host-pick ✅ · wave814 driver_leaf catalog ✅ · wave813 STD_AND_PANIC list→mk ✅ · wave812 formal_mod ✅ · wave811 std_x thin ✅ · wave808 TREE_ARMED ✅ · … · **11.3.1 路径 🟡**（… · residual **shell-primary 残项 → tip Windows + 双端 L4 → 终局真删**）· **11.1.3/4 🟡** · **11.3 🟡** · **11.1.2 🟡** · **11.1.1 🟡** · **11.5.1–4 🟡** · **11.1.6 🟡** · **11.1.5 🟡** · **11.2.3 ✅** · **11.4.1/6 ✅** · **11.4.5 🟡** · **11.2.5/11.4.3 ✅** · **11.0.4 🟡**。\
> **方法**：[自举方法.md](自举方法.md)（Cap/R/L/M + MG）· [自举方法步骤与终局对齐分析.md](自举方法步骤与终局对齐分析.md)\
> **Windows（删种子前）**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[自举进度-归档-2026-07-29.md](自举进度-归档-2026-07-29.md) · [28](自举进度-归档-2026-07-28.md) · [27](自举进度-归档-2026-07-27.md) · …

### 维护约定

1. 每完成一波 **必改**本文（KPI/前排/**§6 变更记录**）；动终局债 / Makefile 叶迁移时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序表 · 一行一波）：**禁止**在 §0–§5 正文插 `### waveNNN` 长详表（一行摘要即可；细节可在 C迁移 / LEAF 文档）。
3. **禁止**把 prove/Stage2/N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**；历史钉盘 125）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水移归档。
6. 大债地图默认写 [`当前问题分析.md`](当前问题分析.md)（承接原 `问题分析文档.md`）；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 =** **`53fd80927`**（wave710；**双端 L4 真冷 + 129 全绿**；match 模式链闭环）。\
**MG 本波 =** **wave827**：11.3.1 — **std_x FORCE dep-thin**（22 叶 · Makefile FORCE+ensure · shell source mtime · **非**物理删）；**不升钉**。\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（删 Makefile 主闸门 + 零 host-cc 三义；**变更记录只在本文 §6**）。

| 指标 | 状态 |
|------|------|
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`53fd80927`**（129 ✅）；文档 tip wave827 |
| Track MG | **11.0.1–11.0.2 ✅** · **11.0.3 🟡** · **11.0.4 🟡** · **11.2.3 ✅** · **11.1.1 🟡** · **11.1.2 🟡** · **11.1.3/4 🟡**（cold+g05 pure-ld ✅ · drop silent CC fallback ✅）· **11.3 🟡** · **11.3.1 路径 🟡**（… · **std_x thin ✅ wave811** · **formal_mod catalog ✅ wave812** · **STD_AND_PANIC list→mk ✅ wave813** · **driver_leaf catalog ✅ wave814** · **archaeology host-pick ✅ wave815** · **DRIVER_SUBCMD list→mk ✅ wave816** · **PIPELINE_X list→mk ✅ wave817** · **SEED_MODE list→mk ✅ wave818** · **SEED_LINK_PICKS list→mk ✅ wave819** · **OBJS_CORE list→mk ✅ wave820** · **ARCH_EXPERIMENT list→mk ✅ wave821** · **RELINK_LEGACY list→mk ✅ wave822** · **SOURCE_DEPS list→mk ✅ wave823** · **E_DIRS list→mk ✅ wave824** · **std_x catalog ✅ wave825** · **formal_mod FORCE thin ✅ wave826** · **std_x FORCE thin ✅ wave827** · residual **shell-primary 残项 → tip Windows + 双端 L4 → 终局真删**）· **11.1.5/6 🟡** · **11.5.1–4 🟡** · **11.2.5 ✅** · **11.4.1/3/4/6 ✅** · **11.4.2/5 🟡** |
| mac / Ubuntu L4（钉盘） | ✅ 双端 L4 冷 OK (129) · `/tmp/*_53fd80927.log` |
| 本波构建级别 | **日常 L2 / shell+闸门**（residual `--check` 双端；无产品 codegen） |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / 删 Makefile DAG** |
| [Makefile迁移表.md](Makefile迁移表.md) | compiler/Makefile → xbuild 全量映射（11.0.1） |
| [自举步骤.md](自举步骤.md) | 可执行闸门 |
| [自举验证.md](自举验证.md) | 怎么测 |
| **本文** | 长期 KPI + 前排 + **§6 波次变更记录**（**每波必改**） |
| [C迁移追踪.md](C迁移追踪.md) | 终局债 / MG 阶段勾选（动债时改） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（动叶时改） |
| **[当前问题分析.md](当前问题分析.md)** | tip 级产品债地图 |
| [自举进度-归档-2026-07-29.md](自举进度-归档-2026-07-29.md) | **29 日全日 wave618–757**（只读；含 tip L4 升钉 `53fd80927` + Track MG 至 R3 cold-else） |
| [自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) | **28 日全日 wave534–617**（只读） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单 |
| 产品 L4 放行钉盘 | **`53fd80927`** | 29 日 wave710 升钉（前钉 `4fa4f07e7`）；双端 L4 真冷 + 129 |
| 本波 tip（29 日钉盘） | **wave710** tip L4 升钉（match 模式链整波收口） | tip **`53fd80927`** · 钉盘 **`53fd80927`**（钉盘 = tip） |
| mac 产品链 bstrict（钉盘） | ✅ **L4 冷 OK (129)** | `/tmp/mac_cold_53fd80927.log` · `/tmp/mac_bstrict_53fd80927.log` |
| Ubuntu L4（金标 · 钉盘） | ✅ **L4 冷 OK (129)** | `/tmp/ubuntu_cold_53fd80927.log` · `/tmp/ubuntu_bstrict_53fd80927.log` |
| 产品矩阵（钉盘 tip） | ✅ rv42 / option / si / hello | 双端 |
| 前钉 | `4fa4f07e7` · `01479a857` · `213bd306b` · `f1250f415` · … | 前钉日志 `/tmp/*_4fa4f07e7.log` |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 18 号结案 |
| **N** | 111/111 IDENTICAL | 28 日 wave539–575 扩至 111；本波不变 |
| **R2 / Cap 本波** | **wave710 tip L4 升钉**（match 模式链整波收口；详见 29 日归档） | tip **`53fd80927`** · 钉盘 **`53fd80927`** |

***

## 3. 当前前排（≤3 条）

1. **11.3.1 residual**：**先 shell-primary 残项**（lists / thin edges），**再** tip Windows 复证 + Mac/Ubuntu L4，**最后**终局真删（wave827 std_x FORCE thin ✅ · wave826 formal_mod FORCE thin ✅ · wave825 std_x catalog ✅ · wave824 E_DIRS list→mk ✅ · wave823 SOURCE_DEPS list→mk ✅ · wave822 RELINK_LEGACY list→mk ✅ · wave821 ARCH_EXPERIMENT list→mk ✅ · wave820 OBJS_CORE list→mk ✅ · … · wave811 std_x thin ✅ · TREE_ARMED ✅ · Makefile 仍在 · `--delete` 仍 never-rm；**禁** 未完成残项就 rm；**禁** mac-only 波绿）。  
2. **双端纪律**：SHARED 波 = mac + Ubuntu 同 SHA 绿（Ubuntu 金标）；禁 mac-only。  
3. **11.1.5** / Cap 硬叶 — 按挡产品红裁剪。

***

## 4. 与归档的关系

- **29 日归档**：[自举进度-归档-2026-07-29.md](自举进度-归档-2026-07-29.md) 包含 wave618–710 升钉 + wave714–757 Track MG（11.0.x … R3 cold-else）。
- **28 日归档**：[自举进度-归档-2026-07-28.md](自举进度-归档-2026-07-28.md) 包含 wave534–617。
- **30 日新内容（仅 §6 表）**：**wave758**–**wave827**（… → formal_mod FORCE → **std_x FORCE dep-thin**）；下波 **lists/thin 残项** 或 tip Windows 复证（仍 never-rm 直至终局真删波）。
- **勿**用归档 tip 或旧钉冒充现行钉盘。**现行产品钉盘 = `53fd80927`**（**129 双端 L4 真冷绿**）；MG 文档 tip 可不升钉。
- **文档约定（2026-07-30）**：`当前进度.md` 已删；进度只维护本文 + C迁移 + Makefile迁移表。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
|------|----------|
| **macOS arm64** | 钉盘 L4 ✅；MG tip 须 **本波 residual `--check`**（wave778 起强制双端） |
| **Ubuntu x86_64（金标）** | 钉盘 L4 ✅；**MG 波绿 = pull 后同 SHA residual/产品闸门绿**（禁 mac-only） |
| **Windows MSYS2** | **wave804**：MSYS2 B-hybrid 绿（proof tip `bb8f07263`，tip 会漂移须复证）· STATUS=`reproven_green` · TREE_ARMED ✅ · wave809–827 shell 残项 · Makefile 仍在 · 真删另终局波 |

***

## 6. 变更记录（倒序 · 30 日起）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-07-30 | **wave827 Track MG · 11.3.1 · std_x FORCE dep-thin** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — pure-.x std product **22** 叶 Makefile 目标 prereq 收为 `FORCE` + script（含 uuid 历史 order deps）；catalog source mtime 归 `xlang_compile_std_x` ensure（skip up-to-date）；LEAF `PHYS_DEL_STD_X_FORCE_THIN=1` · COUNT=22 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留（B2+thin+mk lists）。双端 L2 绿 tip `3f29cb132`（Mac + Ubuntu leaf + catalog + phys-del `--check`；std_x `--check` 22 FORCE；sample ensure skip）。**无产品 codegen** · **不升钉** `53fd80927`。下一：thin edges / B2 / driver_leaf FORCE 或 tip Windows 复证。PLATFORM: SHARED shell |
| 2026-07-30 | **wave826 Track MG · 11.3.1 · formal_mod FORCE dep-thin** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — formal_mod **38** 叶 Makefile 目标 prereq 收为 `FORCE` + script；catalog source mtime 归 `xlang_compile_std_module` ensure（skip up-to-date；`fs_formal` 同步）；LEAF `PHYS_DEL_FORMAL_MOD_FORCE_THIN=1` · COUNT=38 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留（B2+thin+mk lists）。双端 L2 绿 tip `3d7245ac1`（Mac + Ubuntu leaf + catalog + phys-del `--check`；formal_mod `--check` 38 FORCE；sample ensure skip）。**无产品 codegen** · **不升钉** `53fd80927`。下一：thin edges / B2 / std_x FORCE 或 tip Windows 复证。PLATFORM: SHARED shell |
| 2026-07-30 | **wave825 Track MG · 11.3.1 · std_x shell-primary catalog** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — pure-.x std product 22 叶 mode|x_path 表迁入 `xlang_compile_std_x.sh`；Makefile **ensure only**（禁 dual mode/path 再列）；`--check`/`list`/`ensure` shell-primary。LEAF `PHYS_DEL_STD_X_SHELL_PRIMARY=1` · COUNT=22 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留（formal_mod+B2+thin+mk lists）。双端 L2 绿 tip `13a6ac026`（Mac + Ubuntu leaf + catalog + phys-del `--check`；std_x `--check` 22）。**无产品 codegen** · **不升钉** `53fd80927`。下一：thin edges / std_core graph 残项 或 tip Windows 复证。PLATFORM: SHARED shell |
| 2026-07-30 | **wave824 Track MG · 11.3.1 · B7B E_DIRS list→mk** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — `MAIN_X_E_DIRS`（dir-roots 9）+ `LSP_X_E_DIRS`（8）+ `PIPELINE_X_E_DIRS`（9）迁入 `compiler/mk/x_e_dirs.mk`；Makefile **include only**（early；禁 dual inline 再列）；catalog shell parse mk；`ensure_driver_gen` / `ensure_lsp_pipeline_gen` / `ensure_archaeology_gen` 自 mk 加载 -L 根（禁双 bash 数组）；`driver_leaf` kind=lsp 自 mk 加载 LSP。LEAF `PHYS_DEL_B7B_E_DIRS_LIST=1` · COUNT=26 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留。双端 L2 绿 tip `341ac78d5`（Mac + Ubuntu leaf + catalog + phys-del `--check`；make words MAIN/LSP/PIPE 18/16/18；ensure MAIN load 18 tokens from mk）。**无产品 codegen** · **不升钉** `53fd80927`。下一：thin edges / std_core graph 残项 或 tip Windows 复证。PLATFORM: SHARED shell |
| 2026-07-30 | **wave823 Track MG · 11.3.1 · B7B SOURCE_DEPS list→mk** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — `SRCS`（4）+ `MAIN_X_DEPS`（4）+ `PREPROCESS_X_DEPS`（1）+ `PIPELINE_ASM_X_DEPS`/`PIPELINE_X_DEPS`（fixed 10 path tokens）迁入 `compiler/mk/x_source_deps.mk`；Makefile **include only**（early；禁 dual inline 再列）；catalog shell parse mk；`ensure_driver_gen.sh` 自 mk 加载 MAIN/PREPROCESS（禁双 bash 数组）。LEAF `PHYS_DEL_B7B_SOURCE_DEPS_LIST=1` · COUNT=19 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留。双端 L2 绿 tip `ff2ab3f61`（Mac + Ubuntu leaf + catalog + phys-del `--check`；SRCS/MAIN/PREP 4/4/1；PIPE expand 116 with wildcard；ensure MAIN load 4）。**无产品 codegen** · **不升钉** `53fd80927`。下一：E_DIRS / thin 残项 或 tip Windows 复证。PLATFORM: SHARED shell |
| 2026-07-30 | **wave822 Track MG · 11.3.1 · B7B RELINK_LEGACY list→mk** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — `RELINK_XLANG_PREREQS`（fixed multi-token **14**）+ `LEGACY_XLANG_C_LINK_BASE`/`USER_ASM_LINK`/`PREREQS` 迁入 `compiler/mk/driver_seed_composites.mk`；Makefile **include only**（禁 dual inline 再列）；catalog 已 parse composites（wave788）。LEAF `PHYS_DEL_B7B_RELINK_LEGACY_LIST=1` · COUNT=14 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留。双端 L2 绿 tip `7c522e9d6`（Mac + Ubuntu leaf + catalog + phys-del `--check`；RELINK expand Mac 63 / Ubuntu 52 host-filtered；LEGACY_PREREQ Mac 60 / Ubuntu 61）。**无产品 codegen** · **不升钉** `53fd80927`。下一：SRCS/X_DEPS lists 或 thin 残项 或 tip Windows 复证。PLATFORM: SHARED shell |
| 2026-07-30 | **wave821 Track MG · 11.3.1 · B7B ARCH_EXPERIMENT list→mk** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — `DRIVER_SEED_X_FRONTEND_EXPERIMENT_OBJS`（7）+ `DRIVER_NO_C_FRONTEND_OBJS` 迁入 `compiler/mk/archaeology_experiment_objs.mk`；Makefile **include only**（禁 dual inline 再列）；catalog parse mk after link_picks。LEAF `PHYS_DEL_B7B_ARCH_EXPERIMENT_LIST=1` · COUNT=7 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留。双端 L2 绿 tip `70ecb71f7`（Mac + Ubuntu leaf + catalog + phys-del `--check`；EXPERIMENT 7；NO_C MAIN_LINK Mac crt0_arm64 / Ubuntu crt0_x86_64）。**无产品 codegen** · **不升钉** `53fd80927`。下一：lists/thin 残项 或 tip Windows 复证。PLATFORM: SHARED shell |
| 2026-07-30 | **wave820 Track MG · 11.3.1 · B7B OBJS_CORE list→mk** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — archaeology `OBJS_CORE`（product incomplete **16** · LEGACY C-frontend layout）+ `OBJS` alias 迁入 `compiler/mk/objs_core.mk`；Makefile **include only**（禁 dual inline 再列）；catalog shell 解析 mk；产品 `make xlang` 仍 g05（wave786）；逃生 `XLANG_HOST_CC_OBJS_CORE=1` 仍链 `$(OBJS)`（expect UNDEF）。LEAF `PHYS_DEL_B7B_OBJS_CORE_LIST=1` · COUNT=16 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留。双端 L2 绿 tip `b3651b5aa`（Mac + Ubuntu leaf + catalog + phys-del `--check`；product OBJS_CORE 16 expand OK；LEGACY `main_driver` layout OK）。**无产品 codegen** · **不升钉** `53fd80927`。下一：lists/thin 残项 或 tip Windows 复证。PLATFORM: SHARED shell |
| 2026-07-30 | **wave819 Track MG · 11.3.1 · B7B SEED_LINK_PICKS list→mk** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — seed link picks `MAIN_LINK_O`/`REBUILD`/`FLAGS` · `LEXER_LINK_O`/`AST_LINK_O` · `LSP_DIAG_LINK_O` · `PREPROCESS_LINK_O` · `RELINK_XLANG_GLUE_SUFFIX`（product **2**）/`DRIVER_SEED_GLUE_SUFFIX` 迁入 `compiler/mk/driver_seed_link_picks.mk`；Makefile **include only**（禁 dual inline 再列）；catalog shell 解析 mk（去掉 MAIN_LINK/LEXER/LSP hardcode）；composites 仍 expand 入 `DRIVER_SEED_OBJS`。平台 crt0 嵌套 ifeq（catalog 可解析；禁 else-if/filter）。LEAF `PHYS_DEL_B7B_SEED_LINK_PICKS_LIST=1` · COUNT=2 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留。双端 L2 绿 tip `02766b0b9`（Mac + Ubuntu leaf + catalog + phys-del `--check`；MAIN_LINK Mac crt0_arm64 / Ubuntu crt0_x86_64 expand OK）。**无产品 codegen** · **不升钉** `53fd80927`。下一：lists/thin 残项 或 tip Windows 复证。PLATFORM: SHARED shell |
| 2026-07-30 | **wave818 Track MG · 11.3.1 · B7B SEED_MODE list→mk** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — seed mode picks `DRIVER_SEED_RUNTIME_O` / `FRONTEND_EXTRA` / `SUPPORT_EXTRA`（product **3**）/ `LINK_FLAGS` / `RUNTIME_REBUILD` + `C_FRONTEND_LEGACY` 迁入 `compiler/mk/driver_seed_mode_objs.mk`；Makefile **include only**（禁 dual inline 再列）；catalog shell 解析 mk（去掉 SUPPORT_EXTRA/RUNTIME_O hardcode）；composites 仍 expand 入 `DRIVER_SEED_OBJS`。LEGACY vs product no_c（`XLANG_NO_C_SEED_LINK` 与产品同 inventory）。LEAF `PHYS_DEL_B7B_SEED_MODE_LIST=1` · COUNT=3 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留。双端 L2 绿 tip `0e70c2a9d`（Mac + Ubuntu leaf + catalog + phys-del `--check`；LEGACY SUPPORT 5 叶 expand OK）。**无产品 codegen** · **不升钉** `53fd80927`。下一：lists/thin 残项 或 tip Windows 复证。PLATFORM: SHARED shell |
| 2026-07-30 | **wave817 Track MG · 11.3.1 · B7B PIPELINE_X list→mk** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — `PIPELINE_X_BASE/FRONTEND/SATELLITE`(9)/`LINK`/`SUPPORT` + product `PIPELINE_LIBS`（Linux `-lpthread`）迁入 `compiler/mk/pipeline_x_objs.mk`；Makefile **include only**（禁 dual inline 再列）；`driver_seed_obj_catalog.sh` shell 解析 mk（去掉 `PIPELINE_LIBS` hardcode）；`xlang-x-pipeline` / seed link / composites 仍 `$(PIPELINE_X_*)` / `$(PIPELINE_LIBS)`。LEAF `PHYS_DEL_B7B_PIPELINE_X_LIST=1` · COUNT=9 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留。双端 L2 绿 tip `1090cc667`（Mac + Ubuntu leaf + catalog + phys-del `--check`）。**无产品 codegen** · **不升钉** `53fd80927`。下一：lists/thin 残项 或 tip Windows 复证。PLATFORM: SHARED shell |
| 2026-07-30 | **wave816 Track MG · 11.3.1 · B7B DRIVER_SUBCMD list→mk** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — `DRIVER_SUBCMD_OBJS`（7）+ `DRIVER_LEAF_OBJS` / `DRIVER_SUBCMD_GEN` / `GEN_ALL` / `DIRS` 迁入 `compiler/mk/driver_subcmd_objs.mk`；Makefile **include only**（禁 dual inline 再列）；`driver_seed_obj_catalog.sh` shell 解析 mk（去掉 hardcode）；seed link / relink / composites 仍 `$(DRIVER_SUBCMD_OBJS)`。LEAF `PHYS_DEL_B7B_DRIVER_SUBCMD_LIST=1` · COUNT=7 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留。双端 L2 绿 tip `36b2db731`（Mac + Ubuntu leaf + catalog + phys-del `--check`）。**无产品 codegen** · **不升钉** `53fd80927`。下一：lists/thin 残项 或 tip Windows 复证。PLATFORM: SHARED shell |
| 2026-07-30 | **wave815 Track MG · 11.3.1 · archaeology host-pick phonies** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — `archaeology_host_pick_phony.sh` 表驱动 4 考古 phony（`net-o-stub` / `net-o-openssl` / `net-o-mbedtls` / `sqlite-o-stub`）；模式 `ensure|auto` · `list` · `--check`；Makefile thin-call `ensure $@` only；host 编译复用 `xlang_compile_std_x auto`（禁 dual if-ladder）。LEAF `PHYS_DEL_ARCH_HOST_PICK_PHONY=1` · COUNT=4 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留。双端 L2 绿 tip `3d7a76306`（Mac + Ubuntu leaf + phys-del `--check` + sample `net-o-stub`/`sqlite-o-stub` ensure）。**无产品 codegen** · **不升钉** `53fd80927`。下一：lists/thin 残项 或 tip Windows 复证。PLATFORM: SHARED shell |
| 2026-07-30 | **wave814 Track MG · 11.3.1 · driver_leaf shell-primary catalog** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — `driver_leaf_x_to_o.sh` 表驱动 8 叶（fmt/check/test/build/run/compile/emit + `lsp_io_std_heap`）；模式 `ensure|auto` · `list` · `--check`；Makefile thin-call `ensure $@` only；rename/-L/cold seed 进 catalog（禁 dual `DRIVER_*_RENAME`）。LEAF `PHYS_DEL_DRIVER_LEAF_SHELL_PRIMARY=1` · COUNT=8 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留。双端 L2 绿 tip `859a106e5`（Mac + Ubuntu leaf + phys-del `--check` + sample ensure）。**无产品 codegen** · **不升钉** `53fd80927`。下一：lists/thin 残项 或 tip Windows 复证。PLATFORM: SHARED shell |
| 2026-07-30 | **wave813 Track MG · 11.3.1 · B7B STD_AND_PANIC list→mk** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — `STD_AND_PANIC_O`（65 base + Linux x86_64 freestanding）迁入 `compiler/mk/std_and_panic_objs.mk`；Makefile **include only**（禁 dual inline 再列）；`std-objs` / test_c / bootstrap-* 仍 `$(STD_AND_PANIC_O)`。LEAF `PHYS_DEL_B7B_STD_AND_PANIC_LIST=1` · COUNT=65 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留（thin edges/B2/其它 mk lists）。双端 L2 绿 tip `8c28742dc`（Mac + Ubuntu leaf + phys-del `--check`；expand 65 base / Ubuntu 67 with freestanding）。**无产品 codegen** · **不升钉** `53fd80927`。下一：lists/thin 残项 或 tip Windows 复证。PLATFORM: SHARED shell |
| 2026-07-30 | **wave812 Track MG · 11.3.1 · formal_mod shell-primary catalog** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — `xlang_compile_std_module.sh` 表驱动 formal_mod（bare + sources + `fs_formal` dispatch）；模式 `ensure|auto` · `list` · `--check`；Makefile **38** 叶 thin-call `ensure $@` only（去掉源列表/string 宿主 if-ladder）；LEAF `PHYS_DEL_FORMAL_MOD_SHELL_PRIMARY=1` · COUNT=38 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留（thin edges/B2/lists）。双端 L2 绿 tip `009a33df4`（Mac + Ubuntu leaf + phys-del `--check` + map/string/types ensure make）。**无产品 codegen** · **不升钉** `53fd80927`。下一：lists/thin 残项 或 tip Windows 复证。PLATFORM: SHARED shell |
| 2026-07-30 | **wave811 Track MG · 11.3.1 · std_x product hybrid thin** | **11.3.1 路径 🟡**（**非**物理删 · Makefile 仍在）：G.7 有则补全 — `xlang_compile_std_x.sh` 增加 `auto-soft` / `auto-soft-merge`（host 选择 + soft-skip + socketio `ld -r`）；Makefile **22** 纯 `.x` 产品叶 thin-call only（去掉 if-ladder）；LEAF `PHYS_DEL_STD_X_HYBRID_THIN=1` · COUNT=22 · `PREFLIGHT_NEXT=continue_shell_primary_then_explicit_auth_ship_delete_body`；blocker 名保留（formal_mod/B2/lists）。双端 L2 绿 tip `a9fce252f`（Mac + Ubuntu leaf + phys-del `--check`）。**无产品 codegen** · **不升钉** `53fd80927`。下一：formal_mod shell-primary / lists。PLATFORM: SHARED shell |
| 2026-07-30 | **wave810 Track MG · 11.3.1 · delete-body commit honesty** | **11.3.1 路径 🟡**（**非**物理删 · **非** ship body · BODY_SHIPPED=0 · Makefile 仍在）：G.7 一层一波 — `phys_del_makefile_gate.sh`：`--delete-body-commit-honesty` pre_ship 列 co-change 面与 MUST_UPDATE/MUST_NOT（需 STATUS green + ENDGAME=1 + TREE_ARMED=1 + Makefile present）；post_ship（Makefile already absent）契约；ENDGAME/TREE_ARMED 不全 → exit 2；**不改 leaf · 不 rm**；`--delete` 仍 never-rm（即便 confirm）。LEAF `PHYS_DEL_DELETE_BODY_COMMIT_HONESTY=1` · `PREFLIGHT_NEXT=explicit_user_auth_then_ship_delete_body`。双端 L2：leaf + phys-del `--check`。**无产品 codegen** · **不升钉** `53fd80927`。双端 L2 绿 tip `1fbba5123`（Mac + Ubuntu leaf + phys-del `--check` + `--delete-body-commit-honesty` pre_ship READY=1）。下一：**explicit auth → ship `--delete` 体**。PLATFORM: SHARED shell |
| 2026-07-30 | **wave809 Track MG · 11.3.1 · delete-body prep/preview** | **11.3.1 路径 🟡**（**非**物理删 · **非** ship body · BODY_SHIPPED=0 · Makefile 仍在）：G.7 一层一波 — `phys_del_makefile_gate.sh`：`--delete-body-preview` 需 STATUS green + ENDGAME=1 + TREE_ARMED=1；READY=1 计划 only（TARGET=`rm_compiler_Makefile` · confirm env · co-change）；ENDGAME/TREE_ARMED 不全 → exit 2；**不改 leaf · 不 rm**；`--delete` 仍 never-rm（即便 confirm）。LEAF `PHYS_DEL_DELETE_BODY_PREP=1` · `PREFLIGHT_NEXT=delete_body_commit_honesty_then_confirm_rm_separate`。双端 L2：leaf + phys-del `--check`。**无产品 codegen** · **不升钉** `53fd80927`。双端 L2 绿 tip `54c3b2175`（Mac + Ubuntu leaf + phys-del `--check` + `--delete-body-preview` READY=1）。下一：**delete-body commit honesty** → 显式授权 ship `--delete` 体。PLATFORM: SHARED shell |
| 2026-07-30 | **wave808 Track MG · 11.3.1 · TREE_ARMED arm (ENDGAME=1)** | **11.3.1 路径 🟡**（**非**物理删 · **ENDGAME=1** · **TREE_ARMED=1** · Makefile 仍在）：G.7 一层一波 — 审阅树 arm 将 `ENDGAME_PHYSICAL_DELETE_MAKEFILE`→1 · `PHYS_DEL_ENDGAME_ARM_APPLY_TREE_ARMED`→1 · `PHYS_DEL_ENDGAME_TREE_ARMED=1`；honesty `--check` 改 expect ENDGAME=1/TREE_ARMED=1；`phys_del --check` 分支 TREE_ARMED 合同 + confirm 下 `--delete` 仍 never-rm；STATUS 保持 `reproven_green`。LEAF `PREFLIGHT_NEXT=confirm_delete_body_separate_wave_after_tree_arm`。双端 L2：leaf + phys-del `--check`。**无产品 codegen** · **不升钉** `53fd80927`。双端 L2 绿 tip `3f51971e5`（Mac + Ubuntu leaf + phys-del `--check`）。下一：**confirm 物理删体**（显式授权；仍 never-rm 直至 body 波）。PLATFORM: SHARED shell |
| 2026-07-30 | **wave807 Track MG · 11.3.1 · ENDGAME arm commit honesty** | **11.3.1 路径 🟡**（**非**树 arm ENDGAME=1 · **非**物理删 · **树 ENDGAME 仍 0** · TREE_ARMED=0）：G.7 **有则补全** — `phys_del_makefile_gate.sh`：`--endgame-arm-commit-honesty` pre_arm 列 co-change 面与 MUST_UPDATE/MUST_NOT；post_arm（temp leaf ENDGAME=1）要求 STATUS green + ENDGAME=1 + `--delete` 仍拒（never-rm）+ Makefile 仍在；`--check` 含 tree pre_arm + temp post_arm；**不改 leaf**。LEAF `PHYS_DEL_ENDGAME_ARM_COMMIT_HONESTY=1` · `DELETE_ALLOWED=0` · TARGET ENDGAME/TREE_ARMED=1；**保持** `ENDGAME=0` · `TREE_ARMED=0` · `STATUS=reproven_green`。双端 L2：leaf + phys-del `--check`。**无产品 codegen** · **不升钉** `53fd80927`。双端 L2 绿 tip `c07e2a7c3`（Mac + Ubuntu leaf + phys-del `--check`）。下一：**审阅树 arm（TREE_ARMED=1 + ENDGAME=1）** → confirm 物理删体。PLATFORM: SHARED shell |
| 2026-07-30 | **wave806 Track MG · 11.3.1 · ENDGAME arm apply harness** | **11.3.1 路径 🟡**（**非**树 arm ENDGAME=1 · **非**物理删 · **树 ENDGAME 仍 0** · TREE_ARMED=0）：G.7 **有则补全** — `phys_del_makefile_gate.sh`：`--endgame-arm-apply` 需 STATUS=`reproven_green` + `XLANG_PHYS_DEL_ENDGAME_ARM_APPLY=ARM_ENDGAME_I_UNDERSTAND`；无 confirm → exit 2；有 confirm 只改 `ENDGAME_PHYSICAL_DELETE_MAKEFILE`→1（STATUS 保持 green）；`--check` 仅 temp leaf 副本 apply；ENDGAME=1 后 `--delete` 仍拒（never-rm 体）。LEAF `PHYS_DEL_ENDGAME_ARM_APPLY_HARNESS=1` · `TREE_ARMED=0` · `DELETE_ALLOWED=0`；**保持** `ENDGAME=0` · `STATUS=reproven_green`。双端 L2：leaf + phys-del `--check`。**无产品 codegen** · **不升钉** `53fd80927`。双端 L2 绿 tip `8a8ea51da`（Mac + Ubuntu leaf + phys-del `--check`）。下一：**审阅树 arm（TREE_ARMED=1 + ENDGAME=1）** → confirm 物理删体。PLATFORM: SHARED shell |
| 2026-07-30 | **wave805 Track MG · 11.3.1 · ENDGAME arm prep/preview** | **11.3.1 路径 🟡**（**非** arm ENDGAME=1 · **非**物理删 · **ENDGAME 仍 0**）：G.7 **有则补全** — `phys_del_makefile_gate.sh`：`--endgame-preview` 在 STATUS=`reproven_green` 后打印 ENDGAME=1 计划（READY=1 · APPLIED=0 · TREE_ARMED=0 · DELETE_ALLOWED=0 · **不改 leaf**）；STATUS 非绿 → exit 2；`--check` 含 green READY + not_reproven 拒 + 树不变；`--delete` 仍拒。LEAF `PHYS_DEL_ENDGAME_PREP=1` · `TREE_ARMED=0` · `PREFLIGHT_NEXT=endgame_arm_after_preview_separate_wave`。**双端 L2 绿** tip `ba6d0e39d`（Mac + Ubuntu leaf + phys-del `--check`；双系统机 Windows→Ubuntu 重启后 pull）。**无产品 codegen** · **不升钉** `53fd80927`。下一：**审阅 ENDGAME arm（confirm）** → confirm 物理删体。PLATFORM: SHARED shell |
| 2026-07-30 | **wave804 Track MG · 11.3.1 · Windows min-gate proof + STATUS apply** | **11.3.1 路径 🟡**（**非**物理删 · **ENDGAME 仍 0**）：Windows MSYS2 正式 `./xbuild phys-del-gate --run-windows-gate` 绿（seed+hybrid + return-value 42 + win32 write/read；C-03 pipeline_gen=0）；proof stamp tip=`bb8f07263`；Mac `--verify-windows-proof` OK；confirm `--status-flip-apply` → `PHYS_DEL_WINDOWS_GATE_STATUS=reproven_green` · `TREE_APPLIED=1`；honesty `--check` 改 expect green；`--delete` 仍拒（ENDGAME≠1）。根修：write-smoke `Hello Shu!`→`Hello Xlang!` 对齐 gate EXPECTED。**无产品 codegen** · **不升钉** `53fd80927`。下一：**物理删 Makefile 另波**（ENDGAME=1 + confirm）。PLATFORM: WINDOWS min-gate + SHARED leaf/honesty |
| 2026-07-30 | **wave803 Track MG · 11.3.1 · STATUS flip commit honesty** | **11.3.1 路径 🟡**（**非**物理删 · **树 STATUS 仍 not_reproven** · **非** Windows 绿）：G.7 **有则补全** — `phys_del_makefile_gate.sh`：`--status-flip-commit-honesty` pre-flip 列 co-change 面与 MUST_UPDATE/MUST_NOT；post-flip（temp leaf）要求 STATUS=`reproven_green` + ENDGAME=0 + `--delete` 仍拒；`--delete` 认 `XLANG_PHYS_DEL_LEAF_FILE`。LEAF `PHYS_DEL_STATUS_FLIP_COMMIT_HONESTY=1` · `DELETE_ALLOWED=0` · `ENDGAME_REQUIRED=0`；**保持** `WINDOWS_GATE_STATUS=not_reproven_this_tip` · `ENDGAME=0`。双端 L2：leaf + phys-del `--check`。**无产品 codegen** · **不升钉** `53fd80927`。下一：**Windows hybrid min-gate 重跑**（切 Windows + proof + preview + confirm apply + honesty + commit）→ 物理删。PLATFORM: SHARED shell |
| 2026-07-30 | **wave802 Track MG · 11.3.1 · STATUS flip apply harness** | **11.3.1 路径 🟡**（**非**物理删 · **树 STATUS 仍 not_reproven** · **非** Windows 绿）：G.7 **有则补全** — `phys_del_makefile_gate.sh`：`--status-flip-apply` 需 verified proof + `XLANG_PHYS_DEL_STATUS_FLIP_APPLY=APPLY_STATUS_I_UNDERSTAND`；无 confirm → exit 2；有 confirm 只改 `PHYS_DEL_WINDOWS_GATE_STATUS`→`reproven_green`（ENDGAME 保持 0）；`--check` 仅 temp leaf 副本 apply。LEAF `PHYS_DEL_STATUS_FLIP_APPLY_HARNESS=1` · `TREE_APPLIED=0` · `DELETE_ALLOWED=0`；**保持** `WINDOWS_GATE_STATUS=not_reproven_this_tip` · `ENDGAME=0`。双端 L2：leaf + phys-del `--check`。**无产品 codegen** · **不升钉** `53fd80927`。下一：**Windows hybrid min-gate 重跑**（切 Windows + proof + preview + confirm apply + commit）→ 物理删。PLATFORM: SHARED shell |
| 2026-07-30 | **wave801 Track MG · 11.3.1 · STATUS flip prep / preview** | **11.3.1 路径 🟡**（**非**物理删 · **非** STATUS 翻转 · **非** Windows 绿）：G.7 **有则补全** — `phys_del_makefile_gate.sh`：`--status-flip-preview` 在 verified proof 后打印计划（TARGET=`reproven_green` · ENDGAME 保持 0 · APPLIED=0 · **不改 leaf**）；无/错 tip proof → exit 2。LEAF `PHYS_DEL_STATUS_FLIP_PREP=1` · `APPLIED=0` · `DELETE_ALLOWED=0`；**保持** `WINDOWS_GATE_STATUS=not_reproven_this_tip` · `ENDGAME=0`。双端 L2：leaf + phys-del `--check`（合成 proof 往返 + preview 拒改 leaf）。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~status-flip-apply harness~~（wave802）/ Windows min-gate 重跑。PLATFORM: SHARED shell |
| 2026-07-30 | **wave800b Track MG · leaf --check pipefail portability** | Ubuntu leaf `--check` 假红：`set -o pipefail` + `printf large | grep -q` → SIGPIPE(141) 即使 key 命中仍 FAIL。根修：here-string `grep … <<<"$_out"`（leaf + phys-del）。双端 L2 三连绿。**不升钉**。PLATFORM: SHARED shell |
| 2026-07-30 | **wave800 Track MG · 11.3.1 · Windows proof stamp harness** | **11.3.1 路径 🟡**（**非**物理删 · **非** Windows 绿 · **非** STATUS 翻转）：G.7 **有则补全** — `phys_del_makefile_gate.sh`：`--run-windows-gate` 绿后写 `/tmp/xlang_phys_del_windows_proof.txt`；`--verify-windows-proof` tip 对齐（scp Windows→mac）；LEAF `PHYS_DEL_WINDOWS_PROOF_HARNESS=1` · `STATUS_FLIP=0` · `DELETE_ALLOWED=0`；**保持** `WINDOWS_GATE_STATUS=not_reproven_this_tip` · `ENDGAME=0`。双端 L2：leaf + phys-del `--check`（含合成 stamp 往返）。**无产品 codegen** · **不升钉** `53fd80927`。下一：**Windows hybrid min-gate 重跑**（切 Windows + proof）→ 审阅翻转 → 物理删。PLATFORM: SHARED shell |
| 2026-07-30 | **wave799 Track MG · 11.3.1 · phys-del execute-gate** | **11.3.1 路径 🟡**（**非**物理删 · **非** Windows 绿）：G.7 **有则补全** — `phys_del_makefile_gate.sh` + `./xbuild phys-del-gate`：**硬拒** `rm Makefile`（STATUS≠green）；`--dry-run-delete` 列面；`--run-windows-gate` 仅 MSYS2；LEAF `PHYS_DEL_EXECUTE_GATE=1` · `REFUSES_DELETE=1` · `DELETE_ALLOWED=0`；**保持** `WINDOWS_GATE_STATUS=not_reproven_this_tip` · `ENDGAME=0`。双端 L2：leaf + phys-del `--check`。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~proof harness~~（wave800）/ Windows min-gate 重跑 → 物理删。PLATFORM: SHARED shell |
| 2026-07-30 | **wave798 Track MG · 11.3.1 · phys-del preflight** | **11.3.1 路径 🟡**（**非**物理删 · **非** Windows 绿）：G.7 **readiness inventory** — `PHYS_DEL_PREFLIGHT=1`；heat closed / B1–B6 / B7D / B7A cold / B7B catalog 就绪键；blockers=`windows_min_gate_not_reproven\|makefile_thin_call_edges\|b7b_lists_in_mk\|std_core_product_make_graph`；权威 cmd `tests/run-bootstrap-bstrict-windows-gate.sh`；**保持** `WINDOWS_GATE_STATUS=not_reproven_this_tip` · `ENDGAME_PHYSICAL_DELETE_MAKEFILE=0`。双端 L2：leaf `--check` · Windows gate 非 MSYS skip=0。**无产品 codegen** · **不升钉** `53fd80927`。下一：**Windows hybrid min-gate 重跑**（切 Windows）→ 物理删 Makefile。PLATFORM: SHARED shell/文档 |
| 2026-07-30 | **wave797 Track MG · 11.3.1 · B7A heat dep-edge thin orch** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — 最后 heat source-prereq 叶 `pipeline_bootstrap_orchestration` **+1 → 113 FORCE**；shell `ensure_one` 孪 `pipeline_gen.c` + `build_asm/pipeline_glue_types.inc`（+ seed/.x）。LEAF：`DEP_THIN_COUNT=113` · **`HEAT_RESIDUAL=0`**（source-prereq residual closed）。双端 L2：leaf/ensure `--check` · FORCE make orch + touch gen/types 重编 + skip。**无产品 codegen** · **不升钉** `53fd80927`。下一：**物理删 Makefile**（须 Windows hybrid min-gate 绿）。PLATFORM: SHARED shell |
| 2026-07-30 | **wave796 Track MG · 11.3.1 · B7A heat dep-edge thin net·panic·gen_x** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — net multi-merge mtime · panic UNAME stamp → FORCE · gen_x/B4（lsp trio/pipeline + lexer/ast_gen2/driver/preprocess/stubs）**+11 → 112 FORCE**；shell `net_merge` multi .x/seed mtime；panic host pick+stamp；try-heat→try-gen-x/c-to-o。排除 orch。LEAF：`DEP_THIN_COUNT=112` · `HEAT_RESIDUAL=1`。双端 L2：leaf/ensure `--check` · FORCE make 样本 + touch 重编。**无产品 codegen** · **不升钉** `53fd80927`。下一：物理删（Windows 绿后）/ orch residual。PLATFORM: SHARED shell |
| 2026-07-30 | **wave795 Track MG · 11.3.1 · B7A heat dep-edge thin cfg_eval·asm·std** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — cfg_eval multi · pure asm（crt0/freestanding/typeck_f64）· std path/runtime/process **+15 → 101 FORCE**；shell 既有 try-cfg-eval-ladder / try-r2 / std-core-prefer mtime + `force_thin` 含 crt0_mingw。排除 net multi-merge · panic stamp · gen_x · orch。LEAF：`DEP_THIN_COUNT=101` · `HEAT_RESIDUAL=1`。双端 L2：leaf/ensure `--check` · FORCE make 样本 + touch .s/seed 重编。**无产品 codegen** · **不升钉** `53fd80927`。下一：物理删（Windows 绿后）/ 再缩 net·stamp·gen_x。PLATFORM: SHARED shell |
| 2026-07-30 | **wave794 Track MG · 11.3.1 · B7A heat dep-edge thin twin·Makefile-flags·pure leftover** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — twin/Makefile-flags/pure leftover **+8 → 86 FORCE**（scheduler_glue·strict_glue_stubs·glue_standalone·slice·main_driver·runtime_driver{,_no_c}·pipeline_abi）；shell `seed_project_hdrs_newer`（twin #include）+ `force_thin_makefile_flags_newer`。排除 cfg_eval multi · asm/gen · stamp · std merge · gen_x。LEAF：`DEP_THIN_COUNT=86` · `HEAT_RESIDUAL=1`。双端 L2：leaf/ensure `--check` · FORCE make 样本 + touch twin/Makefile 重编。**无产品 codegen** · **不升钉** `53fd80927`。下一：物理删（Windows 绿后）/ 再缩 cfg_eval·asm·gen。PLATFORM: SHARED shell |
| 2026-07-30 | **wave793 Track MG · 11.3.1 · B7A heat dep-edge thin pure seed+.x+.h residual** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — pure seed+.x+.h residual **+19 → 78 FORCE**（diag/labi/io/async/target_cpu/simd/lexer/ast/lsp…）；prereq → `FORCE`+ensure；shell `seed_project_hdrs_newer`（#include BFS）+ seed/.x mtime。排除 twin · multi-seed · Makefile prereq · asm/gen · stamp。LEAF：`DEP_THIN_COUNT=78` · `HEAT_RESIDUAL=1`。双端 L2：leaf/ensure `--check` · FORCE make + touch hdr 重编。**无产品 codegen** · **不升钉** `53fd80927`。下一：物理删（Windows 绿后）/ 再缩 twin·c multi·asm·gen。PLATFORM: SHARED shell |
| 2026-07-30 | **wave792 Track MG · 11.3.1 · B7A heat dep-edge thin pure seed+.x residual** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — pure seed+.x residual **+31 → 59 FORCE**（R1 main/rt/alias/L2/async/lsp/`pipeline_glue_strict_minimal`/…）；prereq → `FORCE`+ensure；shell mtime。排除 hdr · twin（scheduler·strict_glue_stubs）· cfg_eval multi · asm/gen。LEAF：`DEP_THIN_COUNT=59` · `HEAT_RESIDUAL=1` · `PHYS_DEL_PREP_NEXT=…windows…`。双端 L2：leaf/ensure `--check` · FORCE make 样本 + touch seed 重编。**无产品 codegen** · **不升钉** `53fd80927`。下一：物理删（Windows 绿后）/ 再缩 hdr·c·asm·twin。PLATFORM: SHARED shell |
| 2026-07-30 | **wave791 Track MG · 11.3.1 · B7A heat dep-edge thin FORCE runtime_*** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — 28 个 pure `runtime_*` ensure 叶（seed+.x only；B1 prefer 表 + pure R1 channel/kv/sqlite/asm_io）Makefile **源 prereq 折叠**为 `FORCE` + `ensure_host_cc_seed_o.sh`；`try-heat` 管 seed/.x 新鲜度（FORCE 每建必调、壳内 cheap skip）。排除 scheduler（async_net_fs 孪）、hdr/c/asm/stamp 叶。LEAF：`SWALLOWED_B7A_HEAT_DEP_THIN=1` · `PHYS_DEL_BUCKET_B7A_HEAT_DEP_THIN=1` · `PHYS_DEL_BUCKET_B7A_HEAT_RESIDUAL=1` · `PHYS_DEL_PREP_NEXT=B7_physical_delete_makefile_after_windows_not_this_wave`。双端 L2：leaf/ensure `--check` · FORCE make env_os + touch .x 重编。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~pure seed+.x residual~~（wave792）/ 物理删（Windows 绿后）。PLATFORM: SHARED shell |
| 2026-07-30 | **wave790 Track MG · 11.3.1 · B7A heat thin-unify try-heat recipes** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — Makefile 全部 ensure **recipe**（115）统一 `try-heat $@`（mode 名留注释考古）；`try-heat` 梯次仍权威；dep 边 residual。LEAF：`SWALLOWED_B7A_HEAT_THIN_UNIFY=1` · `PHYS_DEL_BUCKET_B7A_HEAT_THIN_UNIFY=1` · `PHYS_DEL_BUCKET_B7A_HEAT_RESIDUAL=1`（dep edges）· `PHYS_DEL_BUCKET_B7A_BODY_SWALLOWED=0` · `PHYS_DEL_PREP_NEXT=B7_physical_delete_makefile_after_windows_not_this_wave`。双端 L2：leaf/ensure `--check` · make diag/rt_arena/labi/typeck_f64 · heat miss=3。**无产品 codegen** · **不升钉** `53fd80927`。下一：物理删（Windows 绿后）/ 再缩 dep 边。PLATFORM: SHARED shell |
| 2026-07-30 | **wave789 Track MG · 11.3.1 · B7A heat try-heat shell dispatch** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — `ensure_host_cc_seed_o.sh try-heat OUT` 梯次转调既有 membership 体（prefer/hybrid → pure R1 → R2 UNAME → gen-x）；`./xbuild heat-o OUT.o`；exit 3 = 非 ensure 叶。LEAF：`SWALLOWED_B7A_HEAT_SHELL_DISPATCH=1` · `PHYS_DEL_BUCKET_B7A_HEAT_SHELL_DISPATCH=1` · `PHYS_DEL_BUCKET_B7A_HEAT_RESIDUAL=1`（Makefile thin-call 边仍 residual）· `PHYS_DEL_BUCKET_B7A_BODY_SWALLOWED=0` · `PHYS_DEL_PREP_NEXT=B7_physical_delete_makefile_after_windows_not_this_wave`。双端 L2：leaf/ensure `--check` · heat diag/labi=0 · miss=3。**无产品 codegen** · **不升钉** `53fd80927`。下一：物理删（Windows 绿后）/ 缩 Makefile thin-call 边。PLATFORM: SHARED shell |
| 2026-07-30 | **wave788 Track MG · 11.3.1 · B7B shell-primary catalog** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — `driver_seed_obj_catalog.sh` 默认 **shell 解析** `compiler/mk/*.mk`（+ 产品默认 host picks）0-make 展开列表；`mk/driver_seed_r_lists.mk` 收 R1/R3/RT/ASM_GLUE；make export 仅 `XLANG_CATALOG_VIA_MAKE=1` / LEGACY 逃生；`--check` shell==make 对等。LEAF：`SWALLOWED_B7B_SHELL_CATALOG=1` · `PHYS_DEL_BUCKET_B7B_SHELL_CATALOG=1` · `PHYS_DEL_BUCKET_B7B_MAKE_EXPORT_ESCAPE=1` · `PHYS_DEL_BUCKET_B7B_LIST_STAYS_MK=1` · `PHYS_DEL_BUCKET_B7B_BODY_SWALLOWED=0` · `PHYS_DEL_PREP_NEXT=B7_physical_delete_makefile_after_windows_not_this_wave`。双端 L2：leaf/catalog `--check` · shell==make 29 keys。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~heat B7A 边~~（wave789）/ 物理删（Windows 绿后）。PLATFORM: SHARED shell |
| 2026-07-30 | **wave787 Track MG · 11.3.1 · B7A cold residual_make=0 + B7B list honesty** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **诚实命名** — 冷 `rebuild_leaves` 七模式（sat/lsp/bridge/panic/user-asm/glue/pipeline-x）**residual_make=0**（shell only）；heat = Makefile ensure thin-call edges；B7B 列表权威仍 mk+catalog（make export residual）。LEAF：`SWALLOWED_B7A_COLD_REBUILD_0MAKE=1` · `PHYS_DEL_BUCKET_B7A_COLD_0MAKE=1` · `PHYS_DEL_BUCKET_B7A_HEAT_RESIDUAL=1` · `PHYS_DEL_BUCKET_B7A_BODY_SWALLOWED=0` · `SWALLOWED_B7B_LIST_AUTHORITY_HONESTY=1` · `PHYS_DEL_BUCKET_B7B_LIST_STAYS_MK=1` · `PHYS_DEL_PREP_NEXT=B7_physical_delete_makefile_after_windows_not_this_wave`。双端 L2：leaf/dag `--check` · 七模式 residual_make=0。**无产品 codegen** · **不升钉** `53fd80927`。下一：物理删（Windows 绿后）/ heat B7A 边 / B7B make-export。PLATFORM: SHARED shell |
| 2026-07-30 | **wave786 Track MG · 11.3.1 · B7D host-cc product link → g05** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — 默认 `$(TARGET)` / `make xlang` → **`g05_prepare_and_relink.sh`**（禁不完整 `OBJS_CORE` UNDEF 链）；逃生 `XLANG_HOST_CC_OBJS_CORE=1` 考古 incomplete。LEAF：`SWALLOWED_B7D_HOST_CC_PRODUCT_LINK=1` · `PHYS_DEL_BUCKET_B7D_BODY_SWALLOWED=1` · `PHYS_DEL_BUCKET_B7_BODY_SWALLOWED=0` · `PHYS_DEL_PREP_NEXT=B7_physical_delete_makefile_after_windows_not_this_wave`。`compiler_all_ci` 注记 B7D。mac L2：leaf `--check` · `make xlang` pure-ld 60 objs · rv=42。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~B7A 冷 0-make~~（wave787）/ 物理删（Windows 绿后）。PLATFORM: SHARED shell |
| 2026-07-30 | **wave785 Track MG · 11.3.1 · B7 DAG inventory + archaeology CC thin** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — B7 子桶 **B7A** thin-call edges · **B7B** mk lists · **B7C** archaeology phony · **B7D** host-cc `xlang` link；**B7c thin**：`bootstrap-typeck/codegen` → `migrate_x_objs`；`bootstrap-self` lsp → 既有 thin leaves（ensure_gen / driver_leaf）；`bootstrap-x-compiler` typeck_x_x residual 诚实保留。LEAF：`SWALLOWED_B7_DAG_INVENTORY=1` · `PHYS_DEL_BUCKET_B7_INVENTORY=1` · `PHYS_DEL_BUCKET_B7_BODY_SWALLOWED=0` · `PHYS_DEL_BUCKET_B7C_ARCHAEOLOGY_CC_THINNED=1` · `PHYS_DEL_PREP_NEXT=B7_physical_delete_makefile_after_windows_not_this_wave`。双端 L2：leaf `--check`。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~可选 B7D~~（wave786）/ 物理删（Windows 绿后）。PLATFORM: SHARED shell |
| 2026-07-30 | **wave784 Track MG · 11.3.1 · B6 R5 compiler_all_ci** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — **`scripts/compiler_all_ci.sh`** 吞 R5 CI 编排体（`OPT` 默认 1 · `XLANG_RUN_ALL_BOOTSTRAP_XLANG=1`→bootstrap-driver-seed · else `make OPT=… xlang xlang-c`）；`./xbuild compiler-all` 直调 shell；Makefile `all` thin-call only。LEAF：`SWALLOWED_B6_R5_CI_COMPILER_ALL=1` · `PHYS_DEL_BUCKET_B6_BODY_SWALLOWED=1` · `PHYS_DEL_PREP_NEXT=B7_…`。**wave784b**：`xbuild` `exec bash`（Ubuntu dash 拒数组 bashism，否则进不了 body）。双端 L2：leaf/script `--check` 0 + 体入口 `OPT=1 xlang xlang-c` 命中；host-cc 全链 `xlang` 链失败为 **B7/树态 residual**（非本波引入；`xlang-c` 绿）。**无产品 codegen** · **不升钉** `53fd80927`。下一：B7 / 物理删（Windows 绿后）。PLATFORM: SHARED shell |
| 2026-07-30 | **wave783 Track MG · 11.3.1 · B5 try-cfg-eval-ladder** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — **`try-cfg-eval-ladder`** 单叶 `src/lexer/cfg_eval.o`（rungs：`-E-extern`±`-L` + `PIPELINE_GEN_CFLAGS` + link_alias `ld -r` → linux pin gen + alias → bootstrap stub）；Makefile thin-call only；删 multi-ladder 体。LEAF：`SWALLOWED_B5_CFG_EVAL_LADDER=1` · `PHYS_DEL_BUCKET_B5_BODY_SWALLOWED=1` · `PHYS_DEL_PREP_NEXT=B6_…`。residual：B6 R5 · 物理删（仍需 Windows 闸门）。ensure/leaf `--check` + L2 force rebuild。**无产品 codegen** · **不升钉** `53fd80927`。下一：B6 R5 吞体（双端 L2）。PLATFORM: SHARED shell |
| 2026-07-30 | **wave782 Track MG · 11.3.1 · B4 try-gen-c-to-o** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — **`try-gen-c-to-o`** 表驱动 **5** 叶（`lexer_x` · `ast_gen2` · `driver_x` · `preprocess_x` · `_x_stubs2`；body=`ensure_gen_x_o.sh` 扩展，**外** try-gen-x catalog）；Makefile thin-call only；删 inline `$(CC) -c` 体。LEAF：`SWALLOWED_B4_GEN_C_TO_O=1` · `PHYS_DEL_BUCKET_B4_BODY_SWALLOWED=1` · `PHYS_DEL_PREP_NEXT=B5_…`。residual：B5 · R5 · 物理删（仍需 Windows 闸门）。**同波**：删 `当前进度.md`（改约定：只维护 自举进度 + C迁移 + Makefile迁移表）。ensure/leaf `--check` + 抽样 L2 rebuild。**无产品 codegen** · **不升钉** `53fd80927`。下一：B5 吞体（双端 L2）。PLATFORM: SHARED shell |
| 2026-07-30 | **wave781 Track MG · 11.3.1 · B3 try-lsp-sat-prefer** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — **`try-lsp-sat-prefer`** 表驱动 **2** 叶（`lsp_diag_pipeline_sizes_nostub` · `lsp_diag_stubs_no_c`；leaf_kind direct_e / thin_rest_e；专用表，不并入 try-other-l2）；Makefile thin-call only；删 dual hybrid 体。LEAF：`SWALLOWED_B3_LSP_SAT_PREFER=1` · `PHYS_DEL_BUCKET_B3_BODY_SWALLOWED=1` · `PHYS_DEL_PREP_NEXT=B4_…`。residual：B4–B5 · R5 · 物理删（仍需 Windows 闸门）。ensure/leaf `--check` + 抽样 L2 rebuild。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~B4 吞体~~（wave782）。PLATFORM: SHARED shell |
| 2026-07-30 | **wave780 Track MG · 11.3.1 · B2 try-std-core-prefer** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — **`try-std-core-prefer`** 表驱动 **5** 叶（process/path/runtime/net + core/slice；leaf_kind direct/process_merge/net_merge）；Makefile thin-call only；删 dual hybrid 体。LEAF：`SWALLOWED_B2_STD_CORE_PREFER=1` · `PHYS_DEL_BUCKET_B2_BODY_SWALLOWED=1` · `PHYS_DEL_PREP_NEXT=B3_…`。residual：B3–B5 · R5 · 物理删（仍需 Windows 闸门）。ensure/leaf `--check` + 抽样 L2 rebuild。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~B3 吞体~~（wave781）。PLATFORM: SHARED shell |
| 2026-07-30 | **wave779 Track MG · 11.3.1 · B1 try-runtime-os-prefer** | **11.3.1 路径 🟡**（**非**物理删）：G.7 **有则补全** — **`try-runtime-os-prefer`** 表驱动 **23** 叶（`runtime_test_fn_invoke`…`process_os_glue`；复用 `rt_prefer_try_x_to_o`；leaf_kind std/http/ed25519/tls/net_udp）；Makefile thin-call only；删 dual hybrid 体。LEAF：`SWALLOWED_B1_RUNTIME_OS_PREFER=1` · `PHYS_DEL_BUCKET_B1_BODY_SWALLOWED=1` · `PHYS_DEL_PREP_NEXT=B2_…`。residual：B2–B5 · R5 · 物理删（仍需 Windows 闸门）。ensure/leaf `--check` + 抽样 L2 rebuild。**无产品 codegen** · **不升钉** `53fd80927`。下一：B2 吞体（双端 L2）。PLATFORM: SHARED shell |
| 2026-07-30 | **wave778 Track MG · 11.3.1 · Windows 闸门 + 双端验** | **11.3.1 路径 🟡**（**非**物理删 · **非**吞体）：G.7 **闸门 only** — `PHYS_DEL_WINDOWS_GATE=required_before_makefile_delete`（scope MSYS2 B-hybrid + PE pure-ld residual；doc `Windows兼容时序-删种子前后.md`；status not_reproven_this_tip）· `MG_VERIFY_DUAL_END=mac_plus_ubuntu_required` · `MG_VERIFY_GOLD=ubuntu` · `MG_VERIFY_FORBIDDEN=mac_only_claim_wave_green`。LEAF dump/check + 进度。允许 B1 吞体（非物理删）；**禁** Windows 未绿删 Makefile；**禁** mac 单端宣称波绿。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~B1 吞体~~（wave779）。PLATFORM: SHARED shell/文档/闸门 |
| 2026-07-30 | **wave777 Track MG · 11.3.1 · 物理删 prep 库存 B1–B7** | **11.3.1 路径 🟡**（**非**物理删 · **非**吞体）：G.7 **库存 only** — `PHYS_DEL_PREP_INVENTORY=1` + buckets **B1** runtime OS hybrid(23) · **B2** std/core product(5) · **B3** lsp satellite(2) · **B4** gen_c_to_o(5) · **B5** cfg_eval ladder(1) · **B6** R5 CI · **B7** Makefile DAG；heat：inline `$(CC) -c` 叶 36 / ensure thin ~79 / `$(CC) -c` 行 ~94；`ENDGAME_PHYSICAL_DELETE_MAKEFILE=0`。LEAF dump/check + 文档。residual：B1–B5 体 · R5 · 删 Makefile。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~Windows+双端闸门~~（wave778）· B1 吞体。PLATFORM: SHARED shell/文档/闸门 |
| 2026-07-30 | **wave776 Track MG · 11.3.1 · R2 panic PREFER try-r2-prefer** | **11.3.1 路径 🟡**（非物理删）：G.7 **有则补全** — **`try-r2-prefer`**（catalog `DRIVER_SEED_PANIC_OBJS`；PREFER=1 thin.x + seed rest FROM_X → `ld -r`，host pick 镜像 Makefile ifeq；失败/PREFER=0/pure-asm → cold `ensure_r2_panic_one`）；Makefile 全 UNAME 分支 thin-call；删 dual hybrid。LEAF：`SWALLOWED_R2_PANIC_PREFER=1` · `R2_PANIC_PREFER_SWALLOWED=1`。live：PREFER=0 cold 真编 · non-member exit 3。residual：~~prep~~（wave777）· R5 CI。ensure/leaf `--check`。**无产品 codegen** · **不升钉** `53fd80927`。下一：物理删 prep。PLATFORM: SHARED shell |
| 2026-07-30 | **wave775 Track MG · 11.3.1 · fmt_check_cmd.o dual → try-other-l2-prefer** | **11.3.1 路径 🟡**（非物理删）：G.7 **有则补全** — `src/driver/fmt_check_cmd.o` 并入 **`try-other-l2-prefer`**（`leaf_kind=fmt_core`：thin.x WEAK + rest FROM_X **无** `USE_X_PIPELINE`；cold ensure_one 同）；Makefile thin-call；删 dual hybrid 体。LEAF：`SWALLOWED_FMT_CHECK_CMD_O_DUAL=1`。live：PREFER=0 cold 真编 · non-member exit 3。residual：~~panic PREFER~~（wave776）· 物理删。ensure/leaf `--check`。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~panic PREFER~~（wave776）· 物理删。PLATFORM: SHARED shell |
| 2026-07-30 | **wave774 Track MG · 11.1.4 · drop silent CC residual fallback** | **11.1.4 🟡**（非终局；silent fallback 已吞）：G.7 **有则补全** — cold `run_pure_ld_required` / `run_cc_residual`；g05 `run_g05_pure_ld_required` / `run_g05_cc_residual`；**pure-ld eligible → hard fail on pure miss**（禁静默 `$CC -o`）；**FORCE_CC=1 / PURE_OK=0 / !freestanding → named CC residual only**。LEAF：`SWALLOWED_DROP_CC_FALLBACK=1` · `ENDGAME_DROP_CC_FALLBACK=1`。host/PLATFORM_LINKER 库存同步。residual：物理删 · fmt dual · Windows PE pure-ld · FORCE_CC 逃生口保留。self-test + host/leaf `--check`（禁 `falling back to CC`）。**无产品 codegen** · **不升钉** `53fd80927`。下一：物理删 · 可选 fmt dual。PLATFORM: SHARED shell |
| 2026-07-30 | **wave773 Track MG · 11.1.4 · g05 pure-ld prefer** | **11.1.4 🟡**（非终局）：G.7 **有则补全** — 抽 **`pure_ld_shared.sh`**（platform prefix / freestanding / multidef / try_link 单权威）；cold `bootstrap_driver_seed_link` 转调（禁第二 platform 表）；**`g05_relink_xlang` pure-ld**（freestanding；LINUX nostdlib → `-static --gc-sections` 无 `-lc`；else `-lSystem`/`-lc`）；当时失败/`FORCE_CC`/ineligible → `G05_CC` residual（~~silent after pure fail~~ wave774）。LEAF：`SWALLOWED_G05_PURE_LD=1` · `ENDGAME_G05_PURE_LD=1`。host/PLATFORM_LINKER 库存同步。residual：~~CC fallback~~（wave774）· 物理删 · fmt dual。self-test + host/leaf `--check`。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~去 CC fallback~~（wave774）· 物理删。PLATFORM: SHARED shell + MACOS ld sysroot + LINUX nostdlib |
| 2026-07-30 | **wave772 Track MG · 11.1.4 · pure-ld cold prefer** | **11.1.4 🟡**（非终局）：G.7 **有则补全** — cold phase1/final **pure-ld prefer**：Makefile export `SEED_LINK_LD`/`MULTIDEF`/`ENTRY`/`LD_TAIL`/`PURE_OK`（OBJS 仍 mk 单权威）；`bootstrap_driver_seed_link.sh` `try_pure_ld`（PLATFORM: MACOS syslibroot/arch/platform_version；LINUX `-lc`）；失败/`FORCE_CC`/`PURE_OK=0` → `SEED_LINK_CC` residual；`--self-test` 纯 ld 烟测。LEAF：`SWALLOWED_R6_PURE_LD=1` · `ENDGAME_COLD_PURE_LD=1`。host/PLATFORM_LINKER 库存同步。residual：CC fallback · ~~g05 `CC -o`~~（wave773）· 物理删 · fmt dual。export + self-test + host/leaf `--check`。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~g05 pure-ld~~（wave773）· 去 CC fallback · 物理删。PLATFORM: SHARED shell + MACOS ld sysroot |
| 2026-07-30 | **wave771 Track MG · 11.3.1 · g05 other L2 try-other-l2-prefer** | **11.3.1 路径 🟡**（非物理删）：G.7 **有则补全** — 表驱动单叶 **`try-other-l2-prefer`**（`seed_link_compat` / `strict_glue_stubs` / `fmt_check_cmd_driver` / `lsp_diag`：thin/full .x via `rt_prefer_try_x_to_o` + rest FROM_X → `$CC -r`；slc `G05_X_O_WEAK_FUNCS` 六桩；fmt cold `USE_X_PIPELINE`；cold ensure_one）；g05 + Makefile thin-call；删 g05 四叶 dual hybrid。LEAF：`SWALLOWED_G05_OTHER_L2_PREFER=1` · `G05_OTHER_L2_PREFER_SWALLOWED=1`（并补 wave770 async LEAF）。live：PREFER=0 cold · PREFER=1 hybrid / seed fallback · non-member exit 3。residual：~~pure-ld cold primary~~（wave772）· 物理删 · `fmt_check_cmd.o` Makefile dual。leaf/host-cc `--check`。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~pure-ld~~（wave772）· 物理删。PLATFORM: SHARED shell |
| 2026-07-30 | **wave770 Track MG · 11.3.1 · g05 async try-async-prefer** | **11.3.1 路径 🟡**（非物理删）：G.7 **有则补全** — 表驱动单叶 **`try-async-prefer`**（`async_liveness` / `async_cps_codegen` / `async_asm_pool`：full .x via `rt_prefer_try_x_to_o` + rest FROM_X → `$CC -r`；cold ensure_one）；g05 + Makefile thin-call；删 g05 三叶 dual hybrid。LEAF：`SWALLOWED_G05_ASYNC_PREFER=1` · `G05_ASYNC_PREFER_SWALLOWED=1`。live：PREFER=0 cold · PREFER=1 aliv/aap hybrid 绿 · cps hybrid 失败→seed fallback（与历史 g05 一致）· non-member exit 3。residual：~~other L2 four~~（wave771）· pure-ld · 物理删。leaf/host-cc `--check`。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~other L2~~（wave771）· pure-ld · 物理删。PLATFORM: SHARED shell |
| 2026-07-30 | **wave769 Track MG · 11.3.1 · g05 L2 asm try-l2-asm-prefer** | **11.3.1 路径 🟡**（非物理删）：G.7 **有则补全** — 表驱动单叶 **`try-l2-asm-prefer`**（`user_asm_seed_bridge` / `backend_x86_64_enc_c` / `asm_backend_compat_stubs`：thin .x via `rt_prefer_try_x_to_o` + rest FROM_X → `$CC -r`；cold ensure_one）；g05 + Makefile thin-call；删 g05 三叶 dual hybrid。LEAF：`SWALLOWED_G05_L2_ASM_PREFER=1` · `G05_L2_ASM_PREFER_SWALLOWED=1`。live：PREFER=0 cold · PREFER=1 bxec/abcs hybrid 绿 · uasb hybrid 失败→seed fallback（与历史 g05 一致）· non-member exit 3。residual：~~async three~~（wave770）· other L2 · pure-ld · 物理删。leaf/host-cc `--check`。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~async~~（wave770）· other L2 · pure-ld · 物理删。PLATFORM: SHARED shell |
| 2026-07-30 | **wave768 Track MG · 11.3.1 · g05 target_cpu try-target-cpu-prefer** | **11.3.1 路径 🟡**（非物理删）：G.7 **有则补全** — `try-target-cpu-prefer`（`target_cpu_flags.x` via `rt_prefer_try_x_to_o` + rest `XLANG_L2_TARGET_CPU_FLAGS_FROM_X` → `$CC -r`；cold pure seed）；g05 + Makefile thin-call；删 g05 dual hybrid。根修 seed 注释 `simd_*/` 提前结束块注释（FROM_X rest 历史假失败）。LEAF：`SWALLOWED_G05_TARGET_CPU_PREFER=1` · `G05_TARGET_CPU_PREFER_SWALLOWED=1`。residual：other L2 · pure-ld · 物理删。leaf/host-cc `--check` + live PREFER=0/1。**无产品 codegen** · **不升钉** `53fd80927`。下一：other L2 · pure-ld · 物理删。PLATFORM: SHARED shell |
| 2026-07-30 | **wave767 Track MG · 11.3.1 · g05 pipeline_abi/ldpc try-*-prefer** | **11.3.1 路径 🟡**（非物理删）：G.7 **有则补全** — `try-pipeline-abi-prefer`（full .x WEAK + rest `XLANG_RUNTIME_PIPELINE_ABI_FROM_X` → `$CC -r`；cold USE_X_PIPELINE）· `try-ldpc-prefer`（thin .x WEAK + rest `XLANG_L2_LSP_CTX_THIN_FROM_X` → `$CC -r`）；harness 复用 `rt_prefer_try_x_to_o`；g05 + Makefile thin-call；删 g05 dual hybrid。LEAF：`SWALLOWED_G05_PIPELINE_ABI_PREFER=1` · `SWALLOWED_G05_LDPC_PREFER=1`。residual：~~target_cpu~~（wave768）· pure-ld · 物理删。leaf/host-cc `--check` + live prefer。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~target_cpu~~（wave768）· pure-ld · 物理删。PLATFORM: SHARED shell |
| 2026-07-30 | **wave766 Track MG · 11.3.1 · g05 rt try-rt-prefer** | **11.3.1 路径 🟡**（非物理删）：G.7 **有则补全** — 单叶 **`try-rt-prefer`**（`runtime_driver_no_c` content..dispatch prefer .x/seed → rest FROM_X+NO_C → `$CC -r`；`RT_SEED_SLICE` 外链；失败 → ensure_one cold）；g05 + Makefile thin-call；删 g05 rt dual hybrid。LEAF：`SWALLOWED_G05_RT_PREFER=1` · `G05_RT_PREFER_SWALLOWED=1`。residual：~~pipeline_abi/ldpc~~（wave767）· ~~target_cpu~~（wave768）· pure-ld · 物理删。leaf/host-cc `--check` + live try-rt-prefer。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~pipeline_abi/ldpc/target_cpu~~ · pure-ld · 物理删。PLATFORM: SHARED shell |
| 2026-07-30 | **wave765 Track MG · 11.3.1 · g05 labi try-labi-prefer** | **11.3.1 路径 🟡**（非物理删）：G.7 **有则补全** — 单叶 **`try-labi-prefer`**（`runtime_link_abi` L0..L9+L8b+L8c prefer .x/seed → rest `XLANG_LABI_*_FROM_X` → `$CC -r -nostdlib`；失败 → ensure_one cold）；g05 + Makefile thin-call；删 g05 labi dual hybrid。LEAF：`SWALLOWED_G05_LABI_PREFER=1` · `G05_LABI_PREFER_SWALLOWED=1`。residual：g05 rt multi-slice · pipeline_abi · ldpc · pure-ld · 物理删。leaf/host-cc `--check` + live try-labi-prefer。**无产品 codegen** · **不升钉** `53fd80927`。下一：g05 rt multi-slice · pure-ld · 物理删。PLATFORM: SHARED shell |
| 2026-07-30 | **wave764 Track MG · 11.3.1 · g05 R3_COLD r3-prefer-family** | **11.3.1 路径 🟡**（非物理删）：G.7 **有则补全** — 同 catalog **`R3_COLD_SEED_OBJS`**；体 = **try-r3-prefer** 扩 **full→thin ladder**（simd/backend R2 full 先，失败 thin，再 cold）；g05 thin-call **`r3-prefer-family`**；删 g05 对九叶 dual hybrid。LEAF：`SWALLOWED_G05_R3_PREFER=1` · `G05_R3_PREFER_SWALLOWED=1`。residual：~~g05 labi~~（wave765）· rt multi-slice · pipeline_abi · ldpc · pure-ld · 物理删。leaf/host-cc `--check` + live try-r3-prefer。**无产品 codegen** · **不升钉** `53fd80927`。下一：~~labi~~（wave765）· rt multi-slice · pure-ld · 物理删。PLATFORM: SHARED shell + Darwin ld -r arch/multidef |
| 2026-07-30 | **wave763 Track MG · 11.3.1 · R3 PREFER thin try-r3-prefer** | **11.3.1 路径 🟡**（非物理删）：G.7 **有则补全** — catalog **`R3_COLD_SEED_OBJS`**（禁新列表）；体 = **`try-r3-prefer`**（PREFER=1 + xlang-c：thin.x/`-E` + rest `-D` + `ld -r`；失败/未开 → ensure_one cold；simd nm 闸门）；Makefile 九叶薄转调（禁内联 thin+rest）；PREFER 门控统一（Darwin 冷链）。LEAF：`SWALLOWED_R3_PREFER_THIN=1` · `R3_PREFER_THIN_SWALLOWED=1`。residual：~~g05 R3_COLD dual~~（wave764）· labi/rt · pure-ld · 物理删。leaf/host-cc `--check` + live try-r3-prefer。**无产品 codegen** · **不升钉** `53fd80927`。PLATFORM: SHARED shell + Darwin ld -r arch/multidef |
| 2026-07-30 | **wave762 Track MG · 11.3.1 · R2 typeck_f64 + crt0 try-r2** | **11.3.1 路径 🟡**（非物理删）：G.7 **有则补全** try-r2 — catalog **`DRIVER_SEED_TYPECK_F64_OBJS`** + **`DRIVER_SEED_CRT0_OBJS`**（lists=mk；禁双列表）；typeck_f64 host pick 平台 `.s`；crt0 固定 o→`.s` / mingw `cc_inc_tu`；Makefile UNAME 仅选 target、体薄转调 try-r2；g05_ensure + build_xlang_asm 收敛（禁双 body）；族模式 r2-typeck-f64 / r2-crt0（host gate 不装异平台 `.s`）。LEAF：`SWALLOWED_R2_TYPECK_F64=1` · `SWALLOWED_R2_CRT0=1`。residual：R3 PREFER thin · panic PREFER · pure-ld · 物理删 · bootstrap_nostdlib_stubs。leaf/host-cc `--check` + live try-r2。**无产品 codegen** · **不升钉** `53fd80927`。下一：PREFER thin · pure-ld · 物理删。PLATFORM: SHARED shell + per-host source pick |
| 2026-07-30 | **wave761 Track MG · 11.3.1 · R4 residual gen `*_x` + pipeline_x try-gen-x** | **11.3.1 路径 🟡**（非物理删）：G.7 **有则补全** — 新体 **`scripts/ensure_gen_x_o.sh`**（lsp_io_x / lsp_x / lsp_diag_x / pipeline_x；gen_driver STALE + PIPELINE_X_DEPS 自 mk）；`ensure try-gen-x` membership = catalog **DRIVER_SEED_LSP_X_OBJS** / **DRIVER_SEED_PIPELINE_X_OBJS**（禁双列表）；rebuild try-r1→try-r3-cold→try-r2→**try-gen-x**→residual make；Makefile 四叶薄转调；**lsp/pipeline-x shell-only**（gen_x=3/1 · residual_make=0）。LEAF：`SWALLOWED_R4_BODY_GEN_X=1`。residual：typeck_f64/crt0 · PREFER thin · pure-ld · 物理删 · sat 等 non-shell pattern。leaf/host-cc `--check` + lsp/pipeline 真跑。**无产品 codegen** · **不升钉** `53fd80927`。下一：R2 typeck_f64/crt0 · PREFER thin · pure-ld · 物理删。PLATFORM: SHARED shell/文档/闸门 |
| 2026-07-30 | docs | **日切归档**：29 号全日仪表盘 + §6 流水（wave618–757 · tip L4 升钉 `53fd80927` + Track MG 至 R3 cold-else）整表迁入 [`自举进度-归档-2026-07-29.md`](自举进度-归档-2026-07-29.md)；本文 30 日起重新起表（保留 30 日初 wave758–760）。钉盘仍 **`53fd80927`** · 文档 tip **wave760** · 下波 **wave761**。 |
| 2026-07-30 | **wave760 Track MG · 11.3.1 · R2 panic cold try-r2** | **11.3.1 路径 🟡**（非物理删）：G.7 **有则补全** — catalog **`DRIVER_SEED_PANIC_OBJS`**（禁新列表）；`ensure try-r2`：stamp `build_asm/runtime_panic.$(uname -s).$(uname -m).stamp` + 主机选源（LINUX\|x86_64 `.s` / arm64\|aarch64 `runtime_panic_arm64.from_x.c` / else `from_x`）；`rebuild_leaves` try-r1→try-r3-cold→**try-r2**→residual make；Makefile cold-else 薄转调 ensure；`build_xlang_asm` ensure_asm_link_objs 同调 try-r2（禁双 cold 体）；**panic shell-only**（r2=1 · residual_make=0）。LEAF：`SWALLOWED_R2_PANIC_COLD=1`。PREFER thin residual。residual：gen/pipeline-x · typeck_f64/crt0 · pure-ld · 物理删 · R3 PREFER thin。leaf/host-cc `--check` + panic force recompile。**无产品 codegen** · **不升钉** `53fd80927`。下一：R4 remaining · pure-ld · 物理删。PLATFORM: SHARED shell + per-host source pick |
| 2026-07-30 | **wave759 Track MG · 11.3.1 · R4 residual glue-standalone → seed-map** | **11.3.1 路径 🟡**（非物理删）：G.7 **有则补全** — `build_asm/pipeline_glue_standalone.o` 并入 **`R1_SEED_MAP_OBJS`**（禁新族/双列表）；seed map ← `seeds/pipeline_glue_standalone.from_x.c` + `-Wno-error=return-type -Ibuild_asm`；`ensure_one` 直 `cc -c`（seed 契约；替代 residual `cc_inc_tu`）并对 `pipeline_glue.c` / `ast_pool.c` / `pipeline_glue_types.inc` 刷新；Makefile 薄转调 ensure；**glue shell-only**（pure_r1=1 · residual_make=0）。LEAF：`SWALLOWED_R4_BODY_GLUE_STANDALONE=1`。residual：panic/gen/pipeline-x · pure-ld · 物理删 · R3 PREFER thin。leaf/host-cc `--check` + glue 真跑 force recompile。**无产品 codegen** · **不升钉** `53fd80927`。下一：R4 remaining · pure-ld · 物理删。PLATFORM: SHARED shell/文档/闸门 |
| 2026-07-30 | **wave758 Track MG · 11.3.1 · R4 residual thin_glue → seed-map** | **11.3.1 路径 🟡**（非物理删）：G.7 **有则补全** — `parser_asm_thin_glue.o` 并入 **`R1_SEED_MAP_OBJS`**（禁新族/双列表）；seed map ← `seeds/parser_asm_thin_c.from_x.c` + monothin `-DPARSER_ASM_THIN_GLUE_NO_SEED_PARSE -Isrc/lexer -Isrc/asm -Iseeds/parser_asm`；`ensure_one` 对 `seeds/parser_asm/*.inc` 刷新（Makefile prereq 孪）；Makefile 薄转调 ensure；**user-asm shell-only**（pure_r1 含 thin_glue · residual_make=0）。LEAF：`SWALLOWED_R4_BODY_THIN_GLUE=1`。residual：panic/gen/glue/pipeline-x · pure-ld · 物理删 · R3 PREFER thin。0-make + leaf/host-cc `--check` + user-asm 真跑。**无产品 codegen** · **不升钉** `53fd80927`。下一：R4 remaining · pure-ld · 物理删。PLATFORM: SHARED shell/文档/闸门 |
