# Xlang / X language 自举进度

> 更新：2026-09-25 · w967 Darwin 和 Windows 的 `(0 - 1) as u8` 也折成 255。钉盘 `ecdb5cc1e` 不升。没有跑 L4。
> 状态：尚未完全自举。option=102。三端 `let g: [2]u8 = [(0 - 1) as u8, 0]` 的 `.data` 是 `ff00`。`[1 as u8, 2]` 的 `.data` 仍是 `0102`。
> 下波：不跑 g05_prepare。不跑 L4，不升钉。Darwin 和 Windows 的 `[8 / 2, 1]` 仍是 CG002。

闸门：日常三端 L2。暂停 `xlang check`。L4 仅整波另派。

### 维护约定

1. 每波只改仪表盘、前排、流水一行。勾选改 [`C迁移追踪.md`](C迁移追踪.md)，一行一个 ✅。
2. 不写 Class 长文，不写「本日要点」。
3. 日切：新的一天先归档本文为 `自举进度-归档-YYYY-MM-DD.md`，再重写当天本文。

## 0. 一句话

尚未完全自举。钉盘 `ecdb5cc1e`。Ubuntu 产品是 `6c39101955bc6f30`。Darwin 产品是 `384fbdaeaca3647a`。Windows 产品是 `55714777cd7e86d0`。没有跑 L4。option=102。

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举效率方法-M2主链.md](自举效率方法-M2主链.md) | M2 主链 |
| [C迁移追踪.md](C迁移追踪.md) | 已完成勾选 |
| 本文 | 当天流水 |
| [自举进度-归档-2026-09-24.md](自举进度-归档-2026-09-24.md) | 9 月 24 日 |
| [自举进度-归档-2026-09-23.md](自举进度-归档-2026-09-23.md) | 9 月 23 日 |
| [自举进度-归档-2026-09-22.md](自举进度-归档-2026-09-22.md) | 9 月 22 日 |

## 2. 仪表盘

| 项 | 结果 |
|----|------|
| 钉盘 | `ecdb5cc1e`（不升） |
| Darwin | 3961528，`384fbdaeaca3647a`，L2 5/5。`xlang-c` 仍是 3945080 |
| Windows | 4601858，`55714777cd7e86d0`，L2 5/5。pabi 1660404。`xlang-c.exe` 仍是 5008116 |
| Ubuntu | 5033688，`6c39101955bc6f30`，L2 5/5。pabi 2220056。`xlang-c` 仍是 4675728 |
| 构建级别 | L2，不跑 `xlang check` |

## 3. 当前前排（≤3）

1. `[(1 as i32), 2]` 返回 42。`.data` 是 `01000000 02000000`。`[(1 as i64), 2]` 返回 42。`.data` 是 `0100000000000000 0200000000000000`。`[(1 as u32), 2]` 返回 42。`(0 - 1) as i64` 是 `ffffffffffffffff`。`(1 + 2) as i64` 是 `0300000000000000`。`S { v: 1 as i64 }` 返回 42。`((0 - 8) / 2) as i64` 是 `fcffffffffffffff`。`(2147483648.0 as i64) as i32` 是 `00000080`。`(1 as i32) as f32` 是 `0000803f`。不要跑 g05_prepare。
2. `[(1.0 as i64), 2]` 仍返回 42。`[(1.0 as i32), 2]` 仍返回 42。`[8 / 2, 1]` 仍返回 5。变量 `-8 / 2` 仍退出 252。`*p = 7` 仍返回 7。`i32[4]` 仍返回 100。
3. 三端 `[(0 - 1) as u8, 0]` 返回 255，`.data` 是 `ff00`。`[1 + 2, 4]` 返回 7。`(0 - 8) >> 1` 是 `fcffffff`。`[1 as u8, 2]` 仍返回 42。`[1 as bool, false]` 仍返回 42。Darwin 和 Windows 的 `[8 / 2, 1]` 仍是 CG002。`(0 - 8) / 2` 在 Ubuntu 返回 252，在 Darwin 和 Windows 是 CG002。L4 现在不能跑。

## 5. 波次流水（倒序 · 9 月 25 日）

| 波 | 一行 |
|----|------|
| w967 | `d1724cc3a` 让 Darwin 和 Windows 的 3 参数折叠收下整数加减乘和移位。`(0 - 1) as u8` 返回 255，`.data` 是 `ff00`。`[1 + 2, 4]` 返回 7。`6 * 7` 返回 42。`1 << 3` 返回 8。`(0 - 8) >> 1` 是 `fcffffff`。`[1 as u8, 2]` 仍返回 42。`[1 as bool, false]` 仍返回 42。`g[0] = 7` 仍返回 7。`[8 / 2, 1]` 仍是 CG002。`(0 - 8) / 2` 仍是 CG002，编译器没有被打崩。Darwin 两份 3961528（`384fbdaeaca3647a`）。侧路 L2 5/5 后装入。`xlang-c` 仍是 3945080。Windows 两份 4601858（`55714777cd7e86d0`）。`g = 7` 仍返回 7。`return g + 1` 仍返回 42。pabi 1660404。侧路 L2 5/5 后装入。`xlang-c.exe` 仍是 5008116。Ubuntu 没有重链，仍是 5033688（`6c39101955bc6f30`），`(0 - 1) as u8` 仍返回 255，`(0 - 8) / 2` 仍返回 252。没有跑 L4。 |
| w966 | `04895b8b3` 让 Darwin 和 Windows 的 3 参数数组折叠走同一份 `.x`。Darwin `[1 as u8, 2]` 返回 42，`.data` 是 `0102`。`[256 as u8, 1]` 是 `0001`。`[1 as bool, false]` 返回 42，`.data` 是 `0100`。`[true, false]` 返回 1 和 0。裸 `[2]u8` 返回 1。`g[0] = 7` 返回 7。本地 `b[2]` 返回 7。`(0 - 1) as u8` 仍是 CG002。两份 3961528（`885fa63079d115b7`）。侧路 L2 5/5 后装入。`xlang-c` 仍是 3945080。Windows 两份 4600322（`edff61a2d0a9ecf1`）。`1 as u8` 返回 42。`1 as bool` 返回 42。`256 as u8` 返回 0。`1 as i32` 返回 42。`g = 7` 仍返回 7。`(0 - 1) as u8` 仍是 CG002。pabi 1660404。侧路 L2 5/5 后装入。`xlang-c.exe` 仍是 5008116。Ubuntu 没有重链，仍是 5033688（`6c39101955bc6f30`），`1 as u8` 仍返回 42，`(0 - 1) as u8` 仍返回 255。没有跑 L4。 |
| w965 | `bb4e50577` 让数组元素折叠收下 `bool` 和 `u8`。Ubuntu `[1 as u8, 2]` 返回 42，`.data` 是 `0102`。`[256 as u8, 1]` 是 `0001`。`(0 - 1) as u8` 返回 255。`1.9 as u8` 返回 1。`[1 as bool, false]` 返回 42，`.data` 是 `0100`。`[true, false]` 返回 1 和 0。`[8 / 0, 1]` 仍是 CG002。两份 5033688（`6c39101955bc6f30`）。modlet.o 87406。侧路 L2 5/5 后装入。pabi 2220056。Windows 两份 4599316（`cd3dbfc5b835a020`）。`1 as u8` 返回 42。`1 as bool` 返回 42。`256 as u8` 返回 0。`1 as i32` 返回 42。`g = 7` 仍返回 7。`(0 - 1) as u8` 仍是 CG002。pabi 1661314。侧路 L2 5/5 后装入。`xlang-c.exe` 仍是 5008116。Darwin 没有重链，`1 as u8` 仍是 CG002。没有跑 L4。 |
| w964 | `df441484d` 让 Windows 的模块标量赋值走 egg 的 store。`g = 7` 返回 7。`g = g + 1` 返回 42。`return g + 1` 仍返回 42。`g[0] = 7` 仍返回 7。`return g[0]` 仍返回 1。本地数组仍返回 3。`let g: i32 = 0` 仍返回 1。本地 `x = 7` 仍返回 7。`i64` 的 `g = 7` 返回 7。`1 as u8` 仍是 CG002。`1 as bool` 仍是 CG002。两份 4599316（`6485e9d4f417441c`）。侧路 L2 5/5 后装入。pabi 1661102。Ubuntu 和 Darwin 没有重链。没有跑 L4。 |
| w963 | `51ec23953` 让 Windows 的模块读取和左值走 egg 那张表。`return g + 1` 返回 42，`.data` 是 `29000000`。`g[0] = 7` 返回 7。`return g[0]` 仍返回 1。本地数组仍返回 3。`let g: i32 = 0` 返回 1。`g = 7` 仍是 CG002。两份 4598804（`7342ac7a2ff64d4d`）。侧路 L2 5/5 后装入。pabi 1661102。Ubuntu 和 Darwin 没有重链。没有跑 L4。 |
| w962 | `3e443de46` 让 COFF 写出 `.data`。`87319227d` 只留 elf_ctx extra 的 shndx override。`return g[0]` 返回 1，`.data` 是 `01000000 02000000`。本地数组返回 3。标量和 `g[0]=7` 仍是 CG002。两份 4599388（`f0c2824b08e03ecd`）。侧路 L2 5/5 后装入。Ubuntu 和 Darwin 没有重链。没有跑 L4。 |
| w961 | `c08981b7a` 让 Windows FROM_X rest 编过。对象换成 1662100（备份 1668745）。重定位和 data_len 覆盖层链不上，因为那两个 BSS 现在是局部符号。侧链 4599671 放在 `C:/xlang_tmp/wave_archive/w961/`，没有装入。`return g[0]` coff_write=205，lea 指向 main 入口。本地数组返回 3。标量 CG002。产品 exe 仍是 4608166。没有跑 L4。 |
| w960 | Windows `return g[0]` 在 backend 入口失败，`code_len=12`，补丁 0。mega 调冷 prepare（`0x14004ea3e`，冷表），下标调活 load（`0x1400bfb10`，活表 `0x14089a680`）。`7ee18fefd` 在 Windows 不编冷 prepare。`XLANG_HOST_CC_SEED_FORCE=1` 重编 rest 失败：`pipeline_debug_trace_named_func_bodies_impl` 重定义，`pipe_release_tmp_arena_module` 未声明。对象仍是 1668745，与备份相同。没有重链，没有跑 L4。 |
| w959 | Darwin `g[0] = 7` 返回 7。`b[2] = 7` 返回 7。两份 3945064（`818a56003e3c48e1`）。Windows 两份 4608166（`43cec7aa414799f0`），L2 5/5，模块数组仍是 CG002。Ubuntu 5033688（`4d5e995dead6117e`）L2 5/5，没有重链。三端 L2 5/5。没有跑 L4。 |
| w958 | `[(1 as i32), 2]` 返回 42，`.data` 是 `01000000 02000000`。`[(1 as i64), 2]` 返回 42，`.data` 是 `0100000000000000 0200000000000000`。`[(1 as u32), 2]` 返回 42。`(0 - 1) as i64` 是 `ffffffffffffffff`。`S { v: 1 as i64 }` 返回 42。`(2147483648.0 as i64) as i32` 是 `00000080`。`1 as u8` 仍是 CG002。`[8 / 0, 1]` 仍是 CG002。Ubuntu 两份 5033688（`4d5e995dead6117e`）。侧路 L2 5/5 后装入。 |
| w957 | `[(1.0 as i64), 2]` 返回 42，`.data` 是 `0100000000000000 0200000000000000`。`(0.0 - 1.9) as i64` 是 `ffffffffffffffff`。`2147483648.0 as i64` 是 `0000008000000000`。字面量 `9223372036854775808.0 as i64` 是 `0000000000000080`。`1 as i64` 仍是 CG002。`(1.0 / 0.0) as i64` 仍是 CG002。Ubuntu 两份 5033688（`ac4fdf9f3c9688cf`）。侧路 L2 5/5 后装入。 |
| w956 | `[(1.0 as i32), 2]` 返回 42，`.data` 是 `01000000 02000000`。`1.9 as i32` 是 `01000000`。`(0.0 - 1.9) as i32` 是 `ffffffff`。`16777217.0 as i32` 是 `01000001`。`S { v: 1.0 as i32 }` 返回 42。`1.0 as i64` 仍是 CG002。`1 as i32` 仍是 CG002。`(1.0 / 0.0) as i32` 仍是 CG002。Ubuntu 两份 5033688（`20a79fc3b91ef95f`）。侧路 L2 5/5 后装入。 |
| w955 | `S { v: 1.0 }` 返回 42，`.data` 是 `0000803f`。`S { v: 1 as f32 }` 也是 `0000803f`。`(1 + 2) as f32` 字段是 `00004040`。f64 字段 `1.0` 是 `000000000000f03f`。`1.0 as i32` 仍是 CG002。`[8 / 0, 1]` 仍是 CG002。Ubuntu 两份 5029592（`c67c093ebbf16877`）。侧路 L2 5/5 后装入。 |
| w954 | `[(1 as f32), (2.0 as f32)]` 返回 42，`.data` 是 `0000803f 00000040`。`1 as f64` 的 `.data` 是 `000000000000f03f 0000000000000040`。`(1 + 2) as f32` 是 `00004040`。`16777217 as f32` 是 `0000804b`。`[8 / 0, 1]` 仍是 CG002。结构体数组的浮点字段仍是 CG002。Ubuntu 两份 5029592（`b591feec9f63f573`）。侧路 L2 5/5 后装入。 |
| w953 | `[8.0 / 2.0, 1.0]` 返回 42，`.data` 是 `0000000000001040 000000000000f03f`。f32 除法 `.data` 是 `00008040 0000803f`。`1.0 / 0.0` 是正无穷。`(1.0 as f32) / (3.0 as f32)` 是 `abaaaa3e`。`[8 / 2, 1]` 仍返回 5。`[8 / 0, 1]` 仍是 CG002。`1 as f32` 仍是 CG002。Ubuntu 两份 5029592（`cfdcd0fe5ca4eafb`）。侧路 L2 5/5 后装入。 |
| w952 | `[8 / 2, 1]` 返回 5，`.data` 是 `04000000 01000000`。`[-8 / 2, 1]` 退出码 253，`.data` 是 `fcffffff 01000000`。变量 `-8 / 2` 退出码 252。`[8 / 0, 1]` 仍是 CG002。`[(1.0 as f32) + (2.0 as f32), 4.0 as f32]` 仍返回 42。Ubuntu 两份 5029592（`0cbb6f6a7613b8ca`）。侧路 L2 5/5 后装入。 |
| w951 | `[(1.0 as f32) + (2.0 as f32), 4.0 as f32]` 返回 42，`.data` 是 `00004040 00008040`。f64 的 `(1.0 as f64) + (2.0 as f64)` 返回 42。`-(1.0 as f32)` 返回 42。`[1 + 2, 4]` 仍返回 7。`*p = 7` 仍返回 7。`[8 / 2, 1]` 仍是 CG002。Ubuntu 两份 4976872（`7b00002be103ba83`）。侧路 L2 5/5 后装入。 |
| w950 | `[-1.0, 2.0]` 返回 42，`.data` 是 `000080bf 00000040`。f64 的 `[1.0 + 2.0, 4.0]` 返回 42。`1.0 - 2.0` 返回 42。`2.0 * 3.0` 返回 42。`[1 + 2, 4]` 仍返回 7。`*p = 7` 仍返回 7。Ubuntu 两份 4976872（`725ed9b009f66100`）。侧路 L2 5/5 后装入。 |
| w949 | `[1 + 2, 4]` 返回 7。`[1.0, 2.0]` 返回 42，`.data` 是 `0000803f 00000040`。`(1 + 2) * 4` 返回 13。`[-1, 2]` 返回 1。整数结构体数组仍返回 3。`*p = 7` 仍返回 7。Ubuntu 两份 4976816（`4f2274790dc78f00`）。侧路 L2 5/5 后装入。 |
| w948 | `[2]S` 的函数指针字段返回 42。字符串字段返回 42。数组字段 `[3, 4]` 返回 7。整数结构体数组仍返回 3。`*p = 7` 仍返回 7。Ubuntu 两份 4972616（`7c78d2c447e6bc97`）。侧路 L2 5/5 后装入。 |
| w947 | `let f: *u8 = inc` 间接调用返回 42。`[inc, dec]` 返回 42。`["ab", "cd"]` 返回 42。`.rela.data` 是 `R_X86_64_64`。结构体数组仍返回 3。`*p = 7` 仍返回 7。Ubuntu 两份 4968520（`18c750da13b3a73d`）。侧路 L2 5/5 后装入。 |
| w946 | `let g: [2]S = [S { v: 1 }, S { v: 2 }]` 返回 3。两字段返回 5。`[2][2]S` 的 `g[1][0].v` 返回 3。`i32[4]` 返回 100。`*p = 7` 仍返回 7。Ubuntu 两份 4968520（`9fb881d20b05701c`）。侧路 L2 5/5 后装入。 |
| w945 | `*p = 7` 写回 7，接着 `g = g + 1` 返回 8。`add(20, 22)` 返回 42，结构体字段返回 20，`u8[4]` 返回 1。Ubuntu 两份 4915000（`11e0a5fcd1af1388`）。侧路 L2 5/5 后装入。 |
| w944 | 模块级 `g[0]` 和 `g[i]` 写回 7，本地数组返回 16。十六次调用的帧与上一份相同。九个探针与上一份相同。Ubuntu 两份 4861752（`5e16765e4657d1fc`）。侧路 L2 5/5 后装入。 |
| w943 | Linux 链接清单把自举体放在旧对象前面。`add(20, 22)` 返回 42，option 退出码 102，`u8[4]` 返回 1。大函数重新编过，对象 36095 字节。Ubuntu 两份 4837056（`f28123979a2f05d3`）。侧路 L2 5/5 后装入。 |
| w942 | 自举体链进正常代码段，注入页去掉。`add(20, 22)` 返回 42，`i32[4]` 返回 50，`u8[4]` 返回 1。大函数重新编过，对象 36095 字节。Ubuntu 两份 4816576（`68c6b8dbfb35b0e7`）。侧路 L2 5/5 后装入。 |
| w941 | 自举 `pipeline_asm_emit_expr_elf_for_call_args`。`add(20, 22)` 返回 42。`u8[4]` 返回 1，`i32[4]` 返回 7，标量仍是 42。Ubuntu 两份 4890048（`8b8545f091c96788`）。侧路 L2 5/5 后装入。 |
| w940 | 自举 `pipeline_asm_emit_array_lit_flat_elf_c` 和 `glue_struct_lit_store_fixed_array_field_elf_c`。`i32[4]` 返回 50，结构体字段返回 20，`[2][2]i32` 返回 5。Ubuntu 两份 4863466（`4f80cbe47f9ec1f2`）。侧路 L2 5/5 后装入。 |
| w939 | 自举 `pipeline_asm_emit_expr_elf_rec` 和 `w495_cell_i32`。`if` 返回 42，`0-3` 退出码 253，`u8[4]` 仍是 1。Ubuntu 两份 4844901（`bc5568672c88029c`）。侧路 L2 5/5 后装入。 |
| w938 | 自举 `glue_call_arg_var_use_lea_not_load_elf_c` 和 `call_arg_lea_query_store`。`u8[4]` 返回 1，`i32[4]` 返回 7，标量仍是 42。Ubuntu 两份 4839482（`1848329958ecf94d`）。侧路 L2 5/5 后装入。 |
| 更早 | 见 9 月 24 日归档。 |
