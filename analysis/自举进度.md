# Xlang / X language 自举进度

> **更新**：2026-08-14 · **4.2.17 `return S24[2]`** · 矩阵 **5/5** · 钉盘仍 **`e364f4a37`** · **不升钉**\
> **产品 L4 放行钉盘**：**`e364f4a37`**（2026-08-11 升钉 · 前 `d79a368b2` 2026-08-10 · 再前 `36363b90f` 2026-08-07）\
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅**（12 labi_*.x 切片 prefer；默认 FROM_X=1）· parser **7.2.2 关 pin ✅**（tip `-E` + assemble；产品默认 FROM_X=1）· codegen 7.4.2 wave323 ✅ · typeck 7.4.1 wave322 ✅ · runtime monofile wave321 ✅ · present **0** · 钉盘 **`e364f4a37`** · Stage 8 tip **wave332 Track L 30/30 FULLY CLOSED**（**PRODUCT HALF=0** · **PRODUCT PINNED=0** · **PRODUCT RETIRED 23/23 = 100%**）· **7.4.3 PREFER_X_O 双端 8/8 wave338 ✅** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁**\
> **tip L4 安全网（历史）**：**`ffc0b646f`**（已由 tip L4 升钉吸收；前网 `4bacaf18c`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：4.2.17 `return S24[2]` durable E* · 4.2.18 P011 核查闭 · 矩阵 **5/5** · s24 asm／c **110** · 钉盘仍 **`e364f4a37`** · **不升钉**\
> **下波候选**：①soft 129／升钉仍须明示；下一硬叶 `[lit] as T` parse_expr；`[][2]i32` INDEX 发射另层；TYPE_DYN／vtable 后期；**勿全量 assemble parser.x／pipeline_abi mega**；勿再动 score array→slice／coerce 递归／fat 布局／type_ref／impl／struct bound／PLUS／4.2.1／classifier／METHOD SIMD／const is_let 门／emit_index 9–16B deref／INDEX MEMORY consume／return／assign wrap／4.2.17 Path B0

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1；过程上 prefer+heat 一次／域，双端矩阵同波一次\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；**SHARED 双端 L2**；日常矩阵 **只** `./xbuild l2-matrix`（wave299：捕获 rv=42／opt=102 成功码，禁 set -e 截断）；谈自举／升钉才 L4 + 矩阵 + 全量 bstrict；bstrict **per-script timeout=300s** + catalog cache（防无界卡死）；**墙钟**：`product_l4` 分步 wall + `run-all-bstrict` 每脚本 wall／top-N（`XLANG_BSTRICT_TIMING_FILE`）；并行 **`XLANG_BSTRICT_JOBS` 默认 1**（Ubuntu 可 opt-in 2–4；Darwin 慎用 OOM）

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[13](自举进度-归档-2026-08-13.md) · [12](自举进度-归档-2026-08-12.md) · [11](自举进度-归档-2026-08-11.md) · [09](自举进度-归档-2026-08-09.md) · [08](自举进度-归档-2026-08-08.md) · [07](自举进度-归档-2026-08-07.md) · [06](自举进度-归档-2026-08-06.md) · [05](自举进度-归档-2026-08-05.md) · [04](自举进度-归档-2026-08-04.md) · [03](自举进度-归档-2026-08-03.md) · [02](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · …\

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `e364f4a37`**（2026-08-11 升钉 · 前 `d79a368b2` 2026-08-10 · 再前 `36363b90f` 2026-08-07）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**双端 Stage2 = ✅ SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709` · `xlang_asm_stage1`／`xlang_asm2` bit-identical · 黄金自举金标准 · strict-link 根修后双端首过）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（Makefile 已删除；**变更记录只在本文 §6**）。

| 指标 | 状态 |
|------|------|
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`e364f4a37`**（L4 升钉冻结 · 双端 L4 真冷绿 · 含 Stage12.0.5 pure-asm residual）；residual tip **wave332** present **0**；Stage 8 tip **wave332**（**30/30 FULLY CLOSED**；**PRODUCT RETIRED 23/23=100%**；PRODUCT HALF=0；PRODUCT PINNED=0） |
| Track MG | **11.3.1 MG ✅**（wave941 物理删 Makefile · wave942 catalog CFLAGS · 0 make in bootstrap · 产品入口 `./xbuild`） |
| Track BC | **开 🟢（30/30 FULLY CLOSED wave332）**（B′ 至 **wave326** link_abi **7.3.1 关 pin** ✅ · M4 **5/5 全 ✅**（runtime/typeck/codegen/parser/link_abi 五域冷链全闭）· present **0**；**BC Stage8 Batch3 收**＝**30/30 FULLY CLOSED**（PRODUCT 23 RETIRED 100% catalog+ladder；NON-PRODUCT 7 正确分类 never-product-chain = TEST×2 / STAGE×2 / EXTRACT_ONLY×1 / DELETED_ORPHAN×2；G.7 无 HALF 无 unresolved PINNED）|
| Track L (Stage 8) | **收（Batch 3 30/30 FULLY CLOSED wave332）**（批次 0 清理 ✅ · 批次 2 **wave1036–1041** 退役 ✅ · 批次 3 **wave327–332**（lsp_diag+lsp+pipeline+driver+preprocess+lexer+parser/typeck/codegen/ast_gen2 10 项 + audit 分类误判 3 项 RETIRED 修正 + ast_gen.c 孤儿物理删 + ast_gen2 cold-seed pin + 分母口径统一 is_product_denominator）✅ · **30/30 FULLY CLOSED**；PRODUCT RETIRED 23/23=100%；HALF=0；PINNED 产品=0 · 详见 [阶段8高效退役分析.md](阶段8高效退役分析.md)） |
| mac / Ubuntu L4（钉盘） | ✅ **双端 L4 真冷 remaining_o=0 + 矩阵 + bstrict 129** @ **`e364f4a37`**（mac wall=34m14s · Ubuntu wall=11m41s · G.8 SHARED 双端同钉闭环）|
| pin 蛋 | ✅ `seeds/bootstrap_xlangc.darwin.arm64` + `linux.x86_64` 自本波 L4 产品 `xlang_asm` 刷新 |
| 本波构建级别 | 钉盘 **L4 已结**；日常 BC 续 **L2**（check 闸门暂停） |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录** |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图 |
| [自举进度-归档-2026-08-13.md](自举进度-归档-2026-08-13.md) | **8月13日** METHOD(49) 物化／try_inline／>16B memcpy + SIMD leftover + soft residual 梯（**66** 行） |
| [自举进度-归档-2026-08-12.md](自举进度-归档-2026-08-12.md) | **8月12日** Stage12.0.5 prefer 默认 pure-asm · PC FORBID／ALLOW · mega ban／RCA · opaque WEAK · catalog cache · L4 墙钟 · **47** 行 |
| [自举进度-归档-2026-08-11.md](自举进度-归档-2026-08-11.md) | **8月11日** Stage12.0.5 pure-asm residual 闭口 + **升钉 `e364f4a37`**（pure_asm 13/13 · L2-asm／tcpu／ldpc · labi residual · full12 双端地图 · **41** 行 + 10 日余 **4**） |
| [自举进度-归档-2026-08-09.md](自举进度-归档-2026-08-09.md) | **8月9日** runtime thin dual leave → M4 五域关 pin → Stage 8 Batch 3 30/30 → PREFER_X_O 双端 8/8（**28** 行 · 钉盘 `36363b90f`） |
| [自举进度-归档-2026-08-08.md](自举进度-归档-2026-08-08.md) | **8月8日** B′ pipeline_x mega 首刀 bootstrap_glue→ast_pool 各域→elf/wpo→8.3 结构地板→gen dual leave→typeck.x 全量 `-E` T001（**wave262–314** · **56** 行） |
| [自举进度-归档-2026-08-07.md](自举进度-归档-2026-08-07.md) | **8月7日** B′ pure leave wave200–261 + 升钉 `36363b90f` + glue_statics（**64** 行） |
| [自举进度-归档-2026-08-06.md](自举进度-归档-2026-08-06.md) | **8月6日** B′ pure leave field_access→wave199 CALL-target resolve（**50** 行） |
| [自举进度-归档-2026-08-05.md](自举进度-归档-2026-08-05.md) | **8月5日** L4 冷恢复 + 8.3.3 R2 + pure-asm match + B′ leave 至 binop（**74** 行） |
| [自举进度-归档-2026-08-04.md](自举进度-归档-2026-08-04.md) | **8月4日 wave1241–1291**（BC ast_pool 16 波域分解 + glue 壳 thin + check 收口 + 升钉 `db809e00f`） |
| [自举进度-归档-2026-08-03.md](自举进度-归档-2026-08-03.md) | **8月3日 wave1227–1240**（BC glue 叶收口 + check 工具链启动 · 15 行） |
| [自举进度-归档-2026-08-02.md](自举进度-归档-2026-08-02.md) | **8月2日 wave1130–1220**（BC glue 续迁 + typeck 跨模块字段根修 · 43 行） |
| [自举进度-归档-2026-08-01.md](自举进度-归档-2026-08-01.md) | **8月1日 wave964–1111-1129**（BC 8.3.1 G.7 fold 50+ 叶 + Stage 8 退役 13/30） |
| [自举进度-归档-2026-07-31.md](自举进度-归档-2026-07-31.md) | **31 日全日 wave895–963**（MG 真删 + post-delete + 开 BC） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单；per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`e364f4a37`** | 2026-08-11 升钉；双端 L4 真冷 + 129（mac 34m14s · Ubuntu 11m41s）；含 Stage12.0.5 pure-asm residual 双端 L2 收口；前 `d79a368b2` |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自本波 L4 `xlang_asm` 刷新（darwin 4.45MB · linux 4.31MB） |
| residual tip（BC） | wave339 | M4 **5/5** 全闭 · Stage 8 Batch 3 **30/30 FULLY CLOSED** · 7.4.3 PREFER_X_O **双端 8/8** · pure-ld 58/60 objs · **双端 L4** @ **`e364f4a37`** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁** · **soft residual 类批 1+2+3** · PREFER mangle 双名根修（Ubuntu L4 重跑）|
| mac 钉盘 L4 | ✅ L4 + bstrict **129** · 34m14s | remaining_o=0 · `./xbuild l4` · `/tmp/xlang_l4_Darwin_e364f4a37.log` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 + bstrict **129** · 11m41s | `ubuntu-remote-server` · `/tmp/xlang_l4_Linux_e364f4a37.log` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | G.7 单权威 `product_l2_matrix.sh`（wave299）；rv42／opt102／hello0／si0／f32 一次跑全 |
| 前钉 | `d79a368b2` · `36363b90f` · `db809e00f` · … | formal_mod → hang guards → wave1277 → … |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 18 号结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ **双端 SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709`） | `stage2-hash-gate OK (SHA256 match — 黄金自举金标准)` · `xlang_asm_stage1`／`xlang_asm2` 行为一致（42 + hello）· strict-link 根修后双端首过 · 日志 `/tmp/xlang_stage2_ubuntu_9e60e1290.log` + `/tmp/xlang_stage2_macos_fix2.log` |
| **R2 / Cap** | wave710 升钉后按需 | 明细见 29 归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1**（已执行） |
| `TREE_ARMED` | **1** |
| `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1**（wave941 物理删除） |
| `PHYS_DEL_WINDOWS_GATE_STATUS` | `reproven_green` @ `71dd60a12`（wave922 · wave940 一键冷启动） |
| preflight blockers | **全清零**（wave937–939） |
| 真删条件 | ✅ 已完成 @ wave941/942 · 钉盘 **`77b334842`**（后升至 **`db809e00f`**） |
| post-delete residual | **✅ 已收**（wave944–964） |

***

## 3. 当前前排（≤3 条）

1. **钉盘 `e364f4a37` 已升且冻结** — 日常 **不升钉**；本波日常 **L2**（非 tip L4）。
2. **4.2.17／4.2.18 已收** — `return S24[2]` Path B0 dest TYPE_ARRAY durable E*；4.2.18 定义侧 P011。余：`[lit] as T` parse_expr。**勿全量 assemble parser.x／pipeline_abi mega**。勿再动 score array→slice／coerce 递归／fat 循环／type_ref／impl／struct scan／PLUS／4.2.1／classifier／METHOD SIMD／const is_let 门／emit_index 9–16B deref／INDEX MEMORY consume／return／assign wrap。
3. **PC／Stage12 底盘** — prefer 族 pure-asm 默认 ✅ · FORBID／ALLOW_HOST_CC ✅ · pipeline_abi mega **仍硬禁真开** · 树级 PREFER 硬 strip。

***

## 4. 与归档的关系

- **8月13日归档**：[自举进度-归档-2026-08-13.md](自举进度-归档-2026-08-13.md) — METHOD(49) 向量 let-init／field-access／try_inline 体／let-init 门／>16B memcpy · SIMD leftover · soft residual 梯 · **66** 行 · 钉盘仍 **`e364f4a37`**。
- **8月12日归档**：[自举进度-归档-2026-08-12.md](自举进度-归档-2026-08-12.md) — Stage12.0.5 prefer 默认 pure-asm · PC FORBID／ALLOW · mega ban／RCA／墙钟 · opaque WEAK · catalog cache · L4 墙钟画像 · **47** 行 · 钉盘仍 **`e364f4a37`**。
- **8月11日归档**：[自举进度-归档-2026-08-11.md](自举进度-归档-2026-08-11.md) — Stage12.0.5 pure-asm residual 闭口 + **升钉 `e364f4a37`**（pure_asm 13/13 · L2-asm hybrid 3/3 · tcpu／ldpc · labi residual RED×0 · full12 双端地图 · **41** 行）。
- **8月9日归档**：[自举进度-归档-2026-08-09.md](自举进度-归档-2026-08-09.md) — runtime thin dual leave → M4 五域关 pin → Stage 8 Batch 3 Track L **30/30 FULLY CLOSED** → 7.4.3 PREFER_X_O **双端 8/8** → wave339（**28** 行 · 钉盘仍 `36363b90f`）。
- **8月8日及前**：见各日归档链接（§1 文档地图）。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `e364f4a37`**（2026-08-11 升钉 · 前 `d79a368b2` · 再前 `36363b90f`）；residual tip **wave339**（present **0** · 日常 L2 可续）。
- 日切后主文档只保留 KPI + 前排 + **8 月 14 日** §6（13 日及前见归档）；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
|------|----------|
| **macOS arm64** | **钉盘 L4+bstrict 129 ✅** @ **`e364f4a37`**（2026-08-11 升钉 · wall=34m14s · remaining_o=0 · 矩阵 5/5）· 双端 L4 真冷绿（G.8 SHARED 同钉闭环）· 产品只走 `./xbuild` · pin 蛋已刷 · check 闸门暂停 |
| **Ubuntu x86_64（金标）** | **钉盘 L4+bstrict 129 ✅** @ **`e364f4a37`**（2026-08-11 升钉 · wall=11m41s · remaining_o=0 · 矩阵 5/5）· 双端 L4 真冷绿 · pin 蛋已刷 · **SSH 权威 host＝`ubuntu-remote-server`** · **远端工作树＝/home/shuliangfu/worker/xlang/x-lang（branch=self-hosting）** · 同步流程：`git fetch origin` + `git merge --ff-only origin/self-hosting`（先 stash 远端 M/?? pin 文件避免冲突）|
| **Windows MSYS2** | wave940 一键冷启动绿 · STATUS=`reproven_green` · Makefile 已物理删除 · 本波未升 Windows pin（仓内无 windows pin 蛋） |

***


## 6. 变更记录（倒序 · **8 月 14 日**；13 日见 [归档](自举进度-归档-2026-08-13.md)）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-08-14 | **fix(selfhost)** **4.2.17 `return S24[2]`** · 4.2.18 核查闭 · 矩阵 **5/5** | **代码+文档** · **不升钉**。C迁移 4.2.17：`return [S24…]`／`let s=mk()` 已绿；`return s`／`return w.xs` asm SEGV（lea 栈 E* 悬垂；host-C 110）。五问：①产生＝Path B0 只 dest SLICE；②存储＝TYPE_ARRAY 仍 E*；③消费＝caller INDEX。**根修（G.7 有则补全）**：Path B0 dest TYPE_ARRAY 同 COMMON 拷后 rax=E*；keep esz>8（wave632 NAMED；同层 `[]S24`）；dest SLICE 仍 pack length。**禁** stamp；**禁**全量 assemble pipeline_abi mega。权威 `runtime_pipeline_abi.x`。探针 `ret_fixed_s24`。4.2.18 核查闭：wave676 定义侧 P011（`untyped_formal`／`_noret`）；typeck `param_raw<=0` 仅 defensive。**同层未搅**：score／coerce／fat／bound／4.2.1／classifier／METHOD SIMD／parser assemble／INDEX MEMORY／return／assign wrap。证：Ubuntu L2 矩阵 **5/5** · s24 asm／c **110** · uf／noret **P011** · ret／asg **42** · retbad **T001** · tfs **42** · idx／wide **0** · nret／nasg **74／75** · const **70** · bound_method／impl_bound **7** · struct_bound **42** · plus **T001** · untyped free **P011** · n8／n16 **40／60** · hello0。**另层留下**：`[lit] as T` parse_expr；`[][2]i32` INDEX 发射。PLATFORM: SHARED emit · LINUX gold · tip **`51eee33cc`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **return／assign `[N]T→[]T`** · 矩阵 **5/5** | **代码+文档** · **不升钉**。C迁移 4.2.10 余叶：`return a`／`return w.xs`／`s = a` 仍 T001（let／call-arg 已绿）。五问：①产生＝return／assign 只认 type_refs_equal＋widen；②存储＝`resolved_type_ref` 必须仍 TYPE_ARRAY（stamp 后 emit 当已是 fat）；③消费＝host-C return／assign 与 asm Path B／VAR assign 无 wrap。**根修（G.7 有则补全）**：抽出 `typeck_array_to_slice_ok`（let coerce 仍 stamp；return／assign 只认不盖）；host-C return durable static memcpy＋fat；assign 同帧 `{.data,.length}`；asm return Path B0 COMMON＋bulk_mem＋dual-GP；assign 复用 `glue_emit_slice_from_array_let_init`。**禁** stamp SLICE；**禁**全量 assemble parser.x／pipeline_abi mega。权威 `typeck.x`＋`codegen.x`＋`runtime_pipeline_abi.x`。探针 `ret_array_as_slice`／`asg_array_as_slice`。**同层未搅**：score／coerce 递归／fat／bound／4.2.1／classifier／METHOD SIMD／parser assemble／INDEX MEMORY。证：Ubuntu L2 矩阵 **5/5** · ret／asg 三端 **42** · retbad **T001** · tfs／i64 **42** · nret／nasg **74／75** · const **70** · idx／wide **0** · bound_method／impl_bound **7** · struct_bound **42** · plus **T001** · untyped free **P011** · n8／n16 **40／60** · hello0。**另层留下**：`[lit] as T` parse_expr；`[][2]i32` INDEX 发射；4.2.17–18 核查。PLATFORM: SHARED typeck+emit · LINUX gold · tip **`c652eee5c`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **>16B INDEX MEMORY** · 矩阵 **5/5** | **代码+文档** · **不升钉**。C迁移 4.2.15 余叶：Wide `a[1].last` 本绿；`take(a[1])`／`let w=a[1]`／`return a[1]` 把留址当首 8B（take 返 0）。五问：①产生＝`emit_index` esz>16 留址（正确）；②存储＝rax＝元素地址；③消费＝METHOD UFCS push／f32-xmm CALL 栈／let memcpy／sret return 只认 48／49／44。**根修（G.7 有则补全）**：push／store INDEX 复用 FIELD `lvalue_eff_addr` 拷循环；store_retval memcpy 门 48\|\|49\|\|47；return Path A2 sret memcpy；默认 f32-xmm CALL 栈走同一 push（非 f32 路径先前已有）。**禁**扩冻结 classifier；**禁** emit_index 内 memcpy；**禁**全量 assemble pipeline_abi mega。权威 `runtime_pipeline_abi.x`＋`backend_call_dispatch.x`。探针 `index_method_wide`。**同层未搅**：classifier／score／coerce／fat／bound／4.2.1／METHOD SIMD／parser assemble／emit_index 9–16B。证：Ubuntu L2 矩阵 **5/5** · wide／idx／trip／quad／smf 三端 **0** · take_only **42** · const **70** · tfs／i64 **42** · bound_method／impl_bound **7** · struct_bound **42** · plus **T001** · untyped free **P011** · SIMD call／method **0** · n8／n16 **40／60** · hello0。**另层留下**：return／assign `[N]T→[]T`；`[lit] as T`；`[][2]i32` INDEX 发射；4.2.17–18 核查。PLATFORM: SHARED emit · LINUX gold · tip **`ec82327bb`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **4.2.15 INDEX method ≤16B** · 矩阵 **5/5** | **代码+文档** · **不升钉**。C迁移 4.2.15：`a[i].get()` 4B／8B／slice／trait／bound 三端本绿；12B Trip／16B Quad METHOD／let／take asm 垃圾或 0。五问：①产生＝`pipeline_asm_emit_index_elf_c` esz>8 只留址；②存储＝rax＝元素地址；③消费＝METHOD／let／call-arg 把指针当首 8B。**根修（G.7 有则补全）**：9–16B 复用 `pipeline_asm_deref_struct16_rax_ptr_elf_c`（*addr→rax+rdx）；**禁**扩冻结 classifier（ko≠48）；**禁**全量 assemble pipeline_abi mega（seed rest 活体）。权威 `.x`＋`seeds/runtime_pipeline_abi.from_x.c`。探针 `index_method*`。**同层未搅**：classifier／score／coerce／fat／bound／4.2.1／METHOD SIMD／parser assemble。证：Ubuntu L2 矩阵 **5/5** · idx／trip／quad／w713 三端 **0** · field **32** · const／1 **70／42** · tfs／i64 **42／42** · bound_method／impl_bound **7** · struct_bound **42** · plus **T001** · untyped free **P011** · SIMD call／method **0** · n8／n16 **40／60** · hello0。**本波顺带核实**：4.2.12 fs 双方法 **32**；4.2.13 dup first-wins **1／41**；4.2.14 `Box<T>.v` **42**／`.nope` **T001**。**另层留下**：>16B INDEX MEMORY（Wide.last CG002）；`[lit] as T`；return／assign `[N]T→[]T`；`[][2]i32` INDEX 发射。PLATFORM: SHARED emit · LINUX gold · tip **`8680599ba`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **4.2.3 体 const `[][]` parse** · 矩阵 **5/5** | **代码+文档** · **不升钉**。C迁移 4.2.3 余叶：体 `const x: [][]i32 = [[1,2]]` P001 no functions（1 层 const／顶层 const／let `[][]` 已绿）。五问：①产生＝`parse_body_lets_into` compound `[..]` 再解析门 `is_let != 0`；②存储＝OneFunc const sidecar；③消费＝onefunc_finish（函数静默丢）。**根修（G.7 有则补全）**：去掉 is_let 门，const 复用 `parse_body_let_bracket_compound_init_ref`。同层 `[][][]`／`[2][]` 一并开。权威 parser.x＋手术补 last-good `parser_gen.c`／seed（**禁** tip `-E` 全量 assemble：丢 `generic_bound_scan`→hello／option 红）。探针 `nested_slice_const*`。**同层未搅**：score／coerce／fat／bound／4.2.1／classifier／METHOD SIMD。证：Ubuntu L2 矩阵 **5/5** · const／const1／const3／arr 三端 **70／42／73／71** · let lit／3 **70／73** · tfs／i64 **42／42** · bound_method／impl_bound **7** · struct_bound **42** · plus **T001** · untyped free **P011** · default **7** · SIMD call／method **0** · n8／n16 **40／60** · hello0。**另层留下**：`[lit] as T` 仍 P001（parse_expr）；return／assign `[N]T→[]T`；`[][2]i32` INDEX 发射。PLATFORM: SHARED parse · LINUX gold · tip **`05069be34`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **4.2.10–11 核查闭** · 矩阵 **5/5** | **代码+文档** · **不升钉**。C迁移 4.2.10 现场：`take(W.xs)`／`take(W{}.xs)`／`take(mk().xs)`／`take(a)` 对 `[]f32`／`[]i32` 原 T001（非文档 CG002）。4.2.11 `take([10,32]): []i64` 三端已 42。五问：①产生＝`typeck_overload_arg_param_score` 无 ak=10／pk=11；②存储＝`resolved_type_ref` 仍 TYPE_ARRAY（emit 要看 ARRAY 才物化 fat）；③消费＝`emit_call_arg_slice_abi`。**根修（G.7 有则补全）**：score 等元 array→slice 返 100；**禁止 stamp SLICE**（stamp 后 host-C `&(w.xs)` BLD001、asm STRUCT_LIT.field SEGV）。权威 `typeck.x`（assemble→typeck_x.o）。探针 `take_field_slice*`／`i64_slice_lit_call`。**同层未搅**：coerce 递归／fat／bound／4.2.1／classifier／METHOD SIMD。证：Ubuntu L2 矩阵 **5/5** · tfs lit／call／var／arr／i32 三端 **42** · bad **T001** · i64／i32／u8／f64 lit **42** · n8／n16 **40／60** · named7／8／16 **45／55／65** · dyn／impl_bound／bound_method **7** · struct_bound **42** · plus **T001** · untyped free **P011** · default **7** · SIMD call／method **0** · hello0。**另层留下**：return／assign `[N]T→[]T` 仍 T001（emit wrap）；`const [][]` P001；nsi asm 偶发 134（4.2.7 last-wins；host-C 稳 32）。PLATFORM: SHARED typeck score · LINUX gold · tip **`eed0261a9`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **4.2.3 深 lit typeck** · 矩阵 **5/5** | **代码+文档** · **不升钉**。C迁移 4.2.3 现场：`let x: [][]i32 = [[1,2]]` T001 expected `[]i32` found `[2]i32`；`[2][]i32`／`[][][]i32`／return／assign／named 同洞。五问：①产生＝`typeck_coerce_array_lit_elem_types_to_decl` 递归只认 TYPE_ARRAY，内层 wave611 盖成 `[N]T`；②存储＝`resolved_type_ref`；③消费＝let／assign／return／call-arg。**根修（G.7 有则补全）**：递归 peel 加 TYPE_SLICE；else 复用 `typeck_coerce_init_slice_from_array`。权威 `typeck.x`（assemble→typeck_x.o）。探针 `nested_slice_lit*`。**同层未搅**：fat 循环／type_ref／bound／4.2.1／classifier／METHOD SIMD。证：Ubuntu L2 矩阵 **5/5** · lit／idx／n3／arr／ret／asg／named／var **70／32／73／71／74／75／76／78** · bad **T001**（expected i32 found bool）· take([[1,2]]) **81** · s1 **2** · n8／n9／n16 **40／50／60** · named7／8／16 **45／55／65** · dyn／impl_type／impl_bound／bound_method **7** · struct_bound **42** · plus **T001** · untyped free／inherent **P011** · default **7** · SIMD call／method **0** · hello0。**另层留下**：`const x: [][]i32` P001 no functions（parser）；`[][2]i32` INDEX 发射。PLATFORM: SHARED typeck · LINUX gold · tip **`89e611150`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **4.2.3 nested slice fat 1..16** · 矩阵 **5/5** | **代码+文档** · **不升钉**。C迁移 4.2.3 现场：wave698 写死 8 层 scalar／7 层 NAMED；9 层 `[]×9 i32`／8 层 `[]×8 Cell` host-cc incomplete（declared inside parameter list）。再 unroll 一层会撞 `u8[256]`（nest-9 uint32_t 行＞256）。**根修（G.7 有则补全）**：`codegen_emit_slice_fat_one` 按 nest 重复 `xlang_slice_` 分段发射；scalar 1..8 仍 `XLANG_SLICE_LAYOUTS`（rt_preamble N=224 不动）；9..16 新 guard `XLANG_SLICE_LAYOUTS_N16`（`-o` preamble 已定义第一 guard 仍能补深一层）。named companion 循环 1..16。硬顶 16。权威 `codegen.x`（assemble→codegen_x.o）。探针 `nested_slice8／9／16`＋`nested_named7／8／16`。**同层未搅**：深 lit typeck／type_ref／bound／4.2.1／classifier／METHOD SIMD。证：Ubuntu L2 矩阵 **5/5** · n8／n9／n16 **40／50／60** · named7／8／16 **45／55／65** · `-E` 有 take＋N16 · gcc 完整类型 · dyn／impl_type／impl_bound／bound_method **7** · struct_bound **42** · untyped free／inherent **P011** · default **7** · hello0。PLATFORM: SHARED host-C · LINUX gold · tip **`028959695`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **4.2.2 dyn／impl 类型位** · 矩阵 **5/5** | **代码+文档** · **不升钉**。C迁移 4.2.2 现场：`dyn` 非 KW，type_ref 把 IDENT `dyn` 收成 TYPE_NAMED，剩 `Clone` 致形参／返／let 解析失败——函数静默丢（`let x: dyn Clone` 丢 main→P001）；`impl Trait` 同洞。**根修（G.7 有则补全）**：`parser_asm_type_ref_slice.inc` 见 contextual `dyn`／TOKEN_IMPL 且下一 token 起类型则剥前缀再 parse；STAR `*dyn`／`*impl` 递归（alloc_pointee 只认 IDENT）。无 TOKEN_DYN（docs／01 未列 dyn）无 TYPE_DYN／vtable——消费既有 `x: Clone` TYPE_NAMED。权威 type_ref。探针 `dyn_type`／`_let`／`_ptr`／`_ret`／`impl_type`。**同层未搅**：bound scan／体方法／4.2.1／classifier／METHOD SIMD。证：Ubuntu L2 矩阵 **5/5** · dyn 形参／let／\*dyn／返／impl 形参 **7** · `-E` 有 take／mk · impl_bound **7** · viol／plus **T001** · struct_bound **42** · bound_method **7** · untyped free／inherent **P011** · default **7** · hello0。PLATFORM: SHARED parse · LINUX gold · tip **`7bfff4e6b`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **4.2.2 impl 级 bound** · 矩阵 **5/5** | **代码+文档** · **不升钉**。C迁移 4.2.2 现场：`impl<T: Clone> Foo<T>` 的 `Foo<B>`（B 无 Clone）假绿 run=7；function／struct 级 scan+check 已在，`<` 前无 IDENT。**根修（G.7 有则补全）**：`xlang_generic_bound_scan_c` 认 TOKEN_IMPL；`impl Type<T: Trait>` 走 last_ident_is_impl；`impl<T: Trait>` peek for-type（inherent／`Trait for Type`）后同 bound／g_fn_gp 表（名=for-type）；`skip_one_impl` 跳过 impl／Type／for-Type 尖括号（≡ struct）。权威 `parser_asm_skip_tl_slice.inc`。探针 `impl_bound.x`／`_viol`／`_plus`／`_plus_ok`／`_nobound`／`_type_viol`／`_for_viol`／`_sig`。**同层未搅**：dyn Trait／PLUS function scan／体方法／struct scan／4.2.1／classifier／METHOD SIMD。证：Ubuntu L2 矩阵 **5/5** · Foo`<A>` get **7** · Foo`<B>`／PLUS 缺 Default／type 级／FOR 级 **T001** · 无 bound **7** · 签名 Foo`<T>` **7** · struct_bound／bound_method／plus_bound **42／7／T001** · untyped free／inherent **P011** · default **7** · hello0。PLATFORM: SHARED parse · LINUX gold · tip **`7e2e9aac9`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **4.2.2 struct 级 bound** · 矩阵 **5/5** | **代码+文档** · **不升钉**。C迁移 4.2.2 现场：`struct Foo<T: Clone>` 的 `Foo<B>`（B 无 Clone）假绿 run=1；function 级 scan+check 已在。**根修（G.7 有则补全）**：`xlang_generic_bound_scan_c` 认 TOKEN_STRUCT 与 function 同走 bound／g_fn_gp 表；类型位 `>` 后 `{` `=` `;` `,` `)` 亦记实例；`check_type_args` 跳过 callee 自身类型参名（`Foo<T>` 签名不假红）。权威 `parser_asm_skip_tl_slice.inc`。探针 `struct_bound.x`／`_viol`／`_plus`／`_plus_ok`／`_nobound`／`_sig`。**同层未搅**：dyn Trait／impl`<T:>`／PLUS function scan／体方法／4.2.1／classifier／METHOD SIMD。证：Ubuntu L2 矩阵 **5/5** · Foo`<A>` **42** · Foo`<B>`／PLUS 缺 Default **T001** · 无 bound **1** · 签名 Foo`<T>` **7** · plus_ok／bound_method **42／7** · untyped free／inherent **P011** · default **7** · hello0。PLATFORM: SHARED parse · LINUX gold · tip **`377bd8d74`**／探针 **`500633057`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **4.2.2 泛型体方法** · 矩阵 **5/5** | **代码+文档** · **不升钉**。C迁移 4.2.2 下一硬叶：`function copy<T: Clone>(x: T){ return x.clone(); }` typeck LANG-004（codegen C6 单态化已在 wave445）。**根修（G.7 有则补全）**：skip_tl `xlang_generic_bound_method_on_param_c` 查 (fn, T-pos) bound＋trait 方法（PLUS 同 pos；arity=extras+self）；`typeck_method_call_resolve_generic_bound` 在 UFCS 后／LANG-004 前放行，Self／T 盖成 receiver，func_ix=-1 交给 C6。权威 `parser_asm_skip_tl_slice.inc`＋`typeck.x`。探针 `bound_method.x`／`_wrong`／`_nobound`／`_self`。**同层未搅**：dyn Trait／struct·impl bound／PLUS scan／4.2.1／classifier／METHOD SIMD。证：Ubuntu L2 矩阵 **5/5** · clone **7** · Self 返 **7** · PLUS+body **7** · 错方法／无 bound **T001** · `copy<B>` 仍 T001 · plus 缺 Default **T001** · plus_ok **42** · untyped free／inherent **P011** · default **7** · missing **T001** · hello0 · overload **0**。PLATFORM: SHARED typeck+parse · LINUX gold · tip **`10bfd56f4`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **4.2.2 PLUS multi-bound** · 矩阵 **5/5** | **代码+文档** · **不升钉**。C迁移 4.2.2 现场：`function foo<T: Trait>` 调用点已 T001；`T: Clone + Default` 的 PLUS 被 scan 丢掉 → A 只 impl Clone 仍过编。**根修（G.7 有则补全）**：`xlang_generic_bound_scan_c` `after_bound` 见 PLUS 再捕 IDENT，pos 不变。权威 `parser_asm_skip_tl_slice.inc`。探针 `plus_bound.x`／`plus_bound_ok.x`。**同层未搅**：泛型体 `x.clone()`／dyn Trait／struct·impl bound／4.2.1／classifier／METHOD SIMD。证：Ubuntu L2 矩阵 **5/5** · plus 缺 Default／缺 Clone／缺第三 Hash **T001** · 双 impl **42** · 单 bound ok／viol **42／T001** · trait／main **42** · default_minimal **42** · untyped free／inherent **P011** · default **7** · hello0 · overload **0** · missing method **T001**。PLATFORM: SHARED parse · LINUX gold · tip **`fe000ba3a`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **4.2.1 untyped self P011 + hoist** · 矩阵 **5/5** | **代码+文档** · **不升钉**。C迁移下一硬叶：无类型 `self` 被 skip。现场地图：live `parser_gen`／buf 对 free／inherent 已 P011；parser.x 无条件豁免未入链；trait 默认 `get(self){…}` hoist 走 buf 被同一 P011 假红。**根修（G.7 有则补全）**：`parser_allow_bare_self` 仅 hoist 开；buf／parser.x 同门；seed commit 补 wave477 `pty==0`→for-type（≡ skip_tl）。权威 `parser_asm_*_slice.inc`＋`parser.x`。探针 `untyped_self_{free,inherent,default}.x`。**同层未搅**：classifier／CALL-arg／METHOD SIMD／LANG-006。证：Ubuntu L2 矩阵 **5/5** · free／inherent **P011** · default **7** · `default_minimal`／`self: Self` **42** · trait／main **42** · mismatch **T001** · METHOD／import isolated **0** · hello0 · overload **0**。PLATFORM: SHARED parse · LINUX gold · tip **`008810f07`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **import METHOD ARRAY_LIT SIMD T001** · 矩阵 **5/5** | **代码+文档** · **不升钉**。本刀补全 import.binding extras 对 ARRAY_LIT→SIMD 的打分＋硬拒。五问：①产生＝`find_func_return_type_in_module_by_name_overload` 打分前不 coerce，wave611 盖成 `[N]T`，first_idx 仍绑同元数函数；`lanes_of_type` 不认 Vec4f／Vec8i；resolve 后直接成功；②存储＝`resolved_func_index`／`resolved_type_ref`；③消费＝`simd.hsum([1,2,3])` 假绿、`hsum([1,2,3,4])` run≠10。**根修（G.7 有则补全）**：`typeck_vector_lanes_of_type` 补 Vec4f=4／Vec8i=8（≡ elem_type_ref）；by_name／module overload 打分前 mapped coerce；`typeck_check_call_arg_types` 认 METHOD extras；import resolve 后走同一 T001 门。权威 `typeck.x`。探针 `tests/boundary/array_lit_vec4f_import.x`／`_badlen.x`。**同层未搅**：classifier／CALL-arg coerce／for_call_args SIMD arm／METHOD receiver emit／METHOD lane T001。证：Ubuntu L2 矩阵 **5/5** · hsum lit **0** · add let-lit **0** · badlen／5 元／add3 **T001** · METHOD／CALL isolated **0** · fold **0** · hello0 · overload **0** · peel **0** · default_alloc **0** · quad **0** · `id<T>` **0**。**另层留下**：import METHOD 标量 f32 走 GP 非 xmm（s2 `select_lane(0.0)` 软）；`add(…)[0]` INDEX-on-METHOD SEGV。PLATFORM: SHARED typeck · LINUX gold · tip **`ae9f83668`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **METHOD SIMD 元素数 T001** · 矩阵 **5/5** | **代码+文档** · **不升钉**。本刀补全 METHOD UFCS 对 ARRAY_LIT→SIMD 的硬拒。五问：①产生＝generic UFCS 把 NAMED `i32x4` 当自由 T，把 `[1,2,3]` 绑成 T 后直接成功，同层 coerce 拒收走不到；②存储＝`resolved_func_index`／`resolved_type_ref`；③消费＝`[1,2,3].take0()` 编译绿 run=1。**根修（G.7 有则补全）**：`typeck_type_is_free_type_param` 对 `typeck_vector_lanes_of_type>0` 返回 0；`typeck_method_call_generic_ufcs` 要求 `n_gp>0`；同层 UFCS 在 coerce 0＋ARRAY_LIT＋SIMD 时拒收候选并走 `driver_diagnostic_typeck_call_arg_type_mismatch`（≡ CALL）。权威 `typeck.x`。探针 `tests/boundary/array_lit_i32x4_method_badlen.x`。**同层未搅**：classifier／CALL-arg coerce／for_call_args SIMD arm／METHOD receiver emit。证：Ubuntu L2 矩阵 **5/5** · isolated **0** · NO_FOLD **0** · METHOD badlen／5 元／extra／badty **T001** · CALL badlen／badty 仍 T001 · fold **0** · hello0 · overload **0** · peel **0** · default_alloc **0** · quad **0** · `id<T>` generic UFCS **0**。PLATFORM: SHARED typeck · LINUX gold · tip **`78966e068`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **METHOD receiver ARRAY_LIT SIMD emit** · 矩阵 **5/5** | **代码+文档** · **不升钉**。本刀补全 METHOD UFCS 发射＋typeck receiver 盖印。五问：①产生＝METHOD 发射对 receiver 不设 pty 且只 mov rax→rdi，ARRAY_LIT rty 仍是 `[4]i32`；②存储＝形参槽 rdi+rsi；③消费＝`[x,8,9,10].take0()`。**根修（G.7 有则补全）**：typeck UFCS 在 type_refs_equal 前复用 `typeck_coerce_init_array_vector_lit_to_decl` 盖印 self／extra；emit UFCS leave 按 place［receiver, extra0..］classify＋`glue_emit_one_call_arg`(pty=formal0)＋dual-GP spill/load（≡ import METHOD／seed wave602）；seed receiver 三处改走 `glue_emit_one_call_arg`。权威 `backend_call_dispatch.x`＋seed `_impl`（Ubuntu L2 prefer thin+rest `_impl` 活）＋`typeck.x`。探针 `tests/boundary/array_lit_i32x4_method.x`。**同层未搅**：classifier／CALL-arg coerce／Quad dest／for_call_args SIMD arm。证：Ubuntu L2 矩阵 **5/5** · isolated **0** · NO_FOLD **0** · fold **0** · smf **0** · hello0 · overload **0** · peel **0** · default_alloc **0** · quad **0** · CALL badlen／badty 仍 T001。**另层留下**：METHOD `[1,2,3].take0()` 仍编译绿（UFCS 未走 CALL 同分硬拒）。PLATFORM: SHARED emit+typeck · LINUX gold · tip **`ef0fc2d7c`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **bare ARRAY_LIT SIMD CALL-arg emit** · 矩阵 **5/5** | **代码+文档** · **不升钉**。本刀只补全 `pipeline_asm_emit_expr_elf_for_call_args` 的 ARRAY_LIT 族。五问：①产生＝CALL 实参 rec 对 ARRAY_LIT 发 lea（rax=指针），callee `i32x4` 要 16B INTEGER dual-GP；②存储＝形参槽 rdi+rsi／rax+rdx；③消费＝`take_vec([7,8,9,10])` ／非 const `[x,8,9,10]`。**根修（G.7 有则补全）**：pty 或 resolved SIMD 盖印时复用 `glue_emit_vector_type_let_init` 占 ≤16B 槽再 load dual-GP（极性与 let-init 同）；>16B 仍 rec／MEMORY。权威 `runtime_pipeline_abi.x`（seed for_call_args 仍桩）。探针 `tests/boundary/array_lit_i32x4_call.x` 加 bare＋非 const。**同层未搅**：classifier／typeck coerce／Quad dest。证：Ubuntu L2 矩阵 **5/5** · isolated **0** · NO_FOLD **0** · fold **0** · smf **0** · hello0 · overload **0** · peel **0** · default_alloc **0** · quad **0** · badlen／badty 仍 T001。**另层留下**：METHOD `[lit].take0()` 编译绿 run≠7（receiver 未盖印且 METHOD 发射未设 pty）。PLATFORM: SHARED emit · LINUX gold · tip **`2dfd2ad2e`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **typeck ARRAY_LIT→i32x4 CALL T001** · 矩阵 **5/5** | **代码+文档** · **不升钉**。本刀只补全 `typeck_check_call_arg_types` 在打分前调用既有 `typeck_coerce_init_array_vector_lit_to_decl`。五问：①产生＝CALL 实参先按外层 ambient check_expr，wave611 盖成 `[N]i32`，resolve 后 score 见 `[4]i32` vs NAMED `i32x4` → T001；②存储＝`resolved_type_ref`；③消费＝`vec_add4([1,2,3,4],[10,20,30,40])` ／ `vec_const_spec_fold.x`。**根修（G.7 有则补全）**：coerce 提前到 score 之前（slice-region 原先已调、但在硬分之后到不了）；向量臂对已知元素类型不匹配拒绝盖印（`[true,…]` 仍 T001）。权威 `typeck.x`；empty_surface＋`typeck_gen` pin 同向。探针 `tests/boundary/array_lit_i32x4_call.x`＋既有 fold。**同层未搅**：classifier／Quad dest／try_inline。证：Ubuntu L2 矩阵 **5/5** · fold **0** · isolated **0** · smf **0**（不再打 T001）· hello0 · overload **0** · peel **0** · default_alloc **0** · badlen／badty 仍 T001。**另层留下**：bare `take_vec([lit])` emit。PLATFORM: SHARED typeck · LINUX gold · tip **`f9aaaacf4`**／探针 **`d31b61beb`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **9–16B Quad `.d` dest 占槽** · 矩阵 **5/5** | **代码+文档** · **不升钉**。本刀只补全 `pipeline_asm_emit_struct_lit_fields_elf_c` 隐式 dest。五问：①产生＝`return Quad{…}` 用 `next_offset` 当 home **不 bump**，16B 线性写盖住上一 8B 局部；②存储＝`.c` 写在 dest+8＝self；③消费＝`.d` 再读 self 变成 (self+2)+3。**根修（G.7 有则补全）**：`stack_slot_off<0` 且非 sret／DEST_IN_RBX 时按 layout 字节（否则字段跨度）占满槽，极性与 field-access 同（x86 high-end home=next+alloc）。权威只在 `.x`（seed 无活体）。探针 `tests/boundary/quad_field_d.x`。**同层未搅**：classifier／typeck T001。证：Ubuntu L2 矩阵 **5/5** · isolated **0**（dest `-0x28`／self `-0x10`）· smf **0** · hello0 · overload **0** · peel **0** · default_alloc **0**。PLATFORM: SHARED struct-lit dest · LINUX gold · tip **`8c9a29c0f`**／探针 **`93c2b1c98`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **METHOD(49) zero-arg default_alloc** · 矩阵 **5/5** | **代码+文档** · **不升钉**。本刀只补全 `glue_call_is_zero_arg_default_alloc`。五问：①产生＝helper 只认 CALL `kind!=48`／`nargs==0`／callee VAR 或 FIELD 名为 default_alloc；②存储＝const-struct-lit 字段 `init_ref`；③消费＝can_inline／fold／emit 只在 `ko==48` 时问 helper，METHOD 字段死。**根修（G.7 有则补全）**：helper 48\|\|49（METHOD extra==0、方法名 default_alloc；CALL 仍 nargs==0＋VAR／FIELD 名）；同层三门 can_inline／fold／emit 同扩 48\|\|49（否则 helper 对 METHOD 死）。`.x`＋seed `_impl` 同 commit。探针 `tests/boundary/default_alloc_method.x`。**同层未搅**：classifier／Quad／typeck T001。**L2 操作**：藏 full.x → prefer thin+rest `_impl` 活。证：Ubuntu L2 矩阵 **5/5** · isolated **0**（main 两次 `call std_heap_default_alloc`，无 `mk_*`；kind≠99）· smf **0** · hello0 · overload **0** · inner peel **0**。PLATFORM: SHARED try_inline · LINUX gold · tip **`914d20d8c`**／探针 **`9ec7eeefe`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **METHOD(49) inner factory peel** · 矩阵 **5/5** | **代码+文档** · **不升钉**。本刀只补全 `glue_inner_call_arg_for_field_access`。五问：①产生＝内层只认 CALL `kind!=48`／`call_arg_ref[pix]`，两 peel 点也只认 CALL；②存储＝`out_arg_ref` 为剥出的构造实参；③消费＝`take_a(recv.as_pair())`／`recv.as_pair().first()` 应发剥层实参而非调工厂。**根修（G.7 有则补全）**：helper 48\|\|49＋与 struct-lit 槽内联同一套 UFCS（nparams==nargs+1 → pix0=base）；single_field／field_sum peel 同扩 48\|\|49（否则 helper 对 METHOD 死）。`.x`＋seed `_impl` 同 commit。探针 `tests/boundary/inner_call_arg_method.x`＋smf 33–38。**同层未搅**：zero-arg-alloc／classifier。**L2 操作**：藏 full.x → prefer thin+rest `_impl` 活。证：Ubuntu L2 矩阵 **5/5** · isolated **0**（main 无 call，`mov $5`／`add`）· smf **0** · hello0 · overload **0**。PLATFORM: SHARED try_inline · LINUX gold · tip **`3a3c2a60d`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **METHOD(49) wpo_mono vector_lane try_inline** · 矩阵 **5/5** | **代码+文档** · **不升钉**。本刀只补全 `try_call_wpo_mono_vector_lane_of_binop_call_elf`。五问：①产生＝外层只认 CALL `callee_ref`／`nargs==1`／`call_arg_ref[0]`，内层只认 CALL `nargs==2`，METHOD 发射也不问；②存储＝两 array-lit 全 lane 打包后登记零实参 thunk 再 `call outer__wpo_l0..ln_r0..rn`；③消费＝`XLANG_WPO_MONO=1` 时 `recv.lane0()` ≡ `lane0_call(recv)`（recv 为 const vec_binop）。**根修（G.7 有则补全）**：外层 48\|\|49（METHOD extra==0、arg=base）；内层 48\|\|49（METHOD extra==1、arg0=base、arg1=extra[0]）；lookup 改 `glue_call_lookup`（METHOD 安全；CALL 仍优先 resolved_func_index）；thunk 基名改 looked-up outer func（copy64 cap）；METHOD 发射在 **import-binding 之后**、mono symbol 之后问同一函数（自门 `XLANG_WPO_MONO`，顺序与 CALL 同）。`.x`＋seed `_impl` 同 commit。探针 `tests/wpo/vec_const_spec_mono_method.x`（METHOD 内外层；不写 array-lit CALL，typeck T001 另层）。**同层未搅**：inner-field／zero-arg-alloc／classifier。**L2 操作**：藏 full.x → prefer thin+rest `_impl` 活。证：Ubuntu L2 矩阵 **5/5** · MONO METHOD **0**（`call lane0__wpo_1_2_3_4_10_20_30_40`，thunk `movabs $11`）· 默认 fold `movabs $11` · smf MONO／默认 **0**（含 `lane0__wpo_*`）· fold_add／scale MONO **0** · hello0 · overload **0**。PLATFORM: SHARED try_inline · LINUX gold · tip **`5fa5d5742`**／探针 **`c4bc69e9d`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **METHOD(49) wpo_mono symbol try_inline** · 矩阵 **5/5** | **代码+文档** · **不升钉**。本刀只补全 `try_call_wpo_mono_symbol_elf`。五问：①产生＝体只认 CALL `callee_ref`／`nargs==2`／`call_arg_ref[0,1]`，METHOD 发射也不问；②存储＝两 i32 常量 fold 后登记零实参 thunk 再 `call base__wpo_a0_a1`；③消费＝`XLANG_WPO_MONO=1` 时 `recv.fold_add(K)` ≡ `fold_add_call(recv, K)`。**根修（G.7 有则补全）**：体 48\|\|49（METHOD extra==1、arg0=base、arg1=extra[0]）；lookup 改 `glue_call_lookup`（METHOD 安全；CALL 仍优先 resolved_func_index）；thunk 基名改 looked-up func（copy64 cap）；METHOD 发射在 **import-binding 之后**、x+K 之后问同一函数（自门 `XLANG_WPO_MONO`，顺序与 CALL 同）。同波发现产品链只有 `codegen_wpo_mono_sym_format` WEAK 桩（恒 -1→CG002），在 try_inline `.x`＋seed **强定义**覆盖（`base__wpo_arg`／负 `_nABS`）。探针 `tests/wpo/const_spec_fold_method.x`。**同层未搅**：vector_lane mono／classifier。**L2 操作**：藏 full.x → prefer thin+rest `_impl` 活。证：Ubuntu L2 矩阵 **5/5** · MONO METHOD／CALL **0**（`call fold_add__wpo_10_3`／`fold_add_call__wpo_10_3`，thunk `movabs $13`）· scale MONO **0**（`scale__wpo_1024_64`）· 默认 fold `movabs $13` · hello0 · smf **0** · overload **0**。PLATFORM: SHARED try_inline · LINUX gold · tip **`7b65890ca`**／format **`edef38263`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **METHOD(49) wpo_const vector_lane try_inline** · 矩阵 **5/5** | **代码+文档** · **不升钉**。本刀只补全 `try_inline_wpo_const_vector_lane_of_binop_call_elf`。五问：①产生＝外层只认 CALL `callee_ref`／`nargs==1`／`call_arg_ref[0]`，内层只认 CALL `nargs==2`，METHOD 发射也不问；②存储＝两 array-lit 的 lane 常量 fold 后 `mov imm,rax`；③消费＝`recv.lane0()` ≡ `lane0(recv)`（recv 为 const vec_binop）。**根修（G.7 有则补全）**：外层 48\|\|49（METHOD extra==0、arg=base）；内层 48\|\|49（METHOD extra==1、arg0=base、arg1=extra[0]）；lookup 改 `glue_call_lookup`（METHOD 安全；CALL 仍优先 resolved_func_index）；METHOD 发射在 **import-binding 之后**、x+K 之后问同一 fold，并复用 CALL 的 `XLANG_WPO_MONO`／`XLANG_WPO_NO_FOLD` 门（先 vector_lane 再 scalar）。`.x`＋seed `_impl` 同 commit。探针 +`Lane0able.lane0`／`VecAdd4able.vec_add4`（Wide 前 25／26）。CALL `lane0(array-lit,array-lit)` 仍 T001（typeck 期望类型，另层；`vec_const_spec_fold.x` 旧红）。**同层未搅**：wpo_mono／classifier。**L2 操作**：藏 full.x → prefer thin+rest `_impl` 活。证：Ubuntu L2 矩阵 **5/5** · m25／m26／nb／cut26 **0** · method main 无 call（`movabs $11`）· hello0 · 全量 smf 含 Wide **0**（本二进制；非宣称 Wide 债已闭）。PLATFORM: SHARED try_inline · LINUX gold · tip **`7a2e8a5d9`**／探针 **`bdc07897b`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **METHOD(49) wpo_const 标量 try_inline** · 矩阵 **5/5** | **代码+文档** · **不升钉**。本刀只补全 `try_inline_wpo_const_scalar_binop_call_elf`。五问：①产生＝体只认 CALL `callee_ref`／`num_args==2`／`call_arg_ref[0,1]`，METHOD 发射也不问；②存储＝两 i32 常量 fold 后 `mov imm,rax`；③消费＝`recv.fold_add(K)` ≡ `fold_add_call(recv, K)`。**根修（G.7 有则补全）**：体 48\|\|49（METHOD extra==1、arg0=base、arg1=extra[0]）；lookup 改 `glue_call_lookup`（METHOD 安全；CALL 仍优先 resolved_func_index）；METHOD 发射在 **import-binding 之后**、x_plus_k 之后问同一 fold，并复用 CALL 的 `XLANG_WPO_MONO`／`XLANG_WPO_NO_FOLD` 门。`.x`＋seed `_impl` 同 commit。探针 +`FoldAddable.fold_add`／`fold_add_call`（Wide 前 23／24）。**同层未搅**：vector_lane／classifier。**L2 操作**：藏 full.x → prefer thin+rest `_impl` 活。证：Ubuntu L2 矩阵 **5/5** · fold_add／fold_add_call／plus_one／first／pair_sum／increment／cut24 **0** · methodonly main 无 call（`movabs $13`）· hello0 · 全量 smf 含 Wide **0**（本二进制；非宣称 Wide 债已闭）。PLATFORM: SHARED try_inline · LINUX gold · tip **`f2cf80da9`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **METHOD(49) x+K try_inline** · 矩阵 **5/5** | **代码+文档** · **不升钉**。本刀只补全 `try_inline_x_plus_k_call_elf`。五问：①产生＝体只认 CALL `callee_ref`／`num_args==1`／`call_arg_ref[0]`，METHOD 发射也不问；②存储＝发实参再 add K；③消费＝`recv.plus_one()` ≡ `add_one(recv)`。**根修（G.7 有则补全）**：体 48\|\|49（METHOD extra==0、arg=base）；lookup 改 `glue_call_lookup`（METHOD 安全；CALL 仍优先 resolved_func_index，overload pick 不漂）；METHOD 发射在 **import-binding 之后**、field_sum 之后问同一 fold。`.x`＋seed `_impl` 同 commit。探针 +`Addable.plus_one`／`add_one`（Wide 前 21／22）。**同层未搅**：wpo_const／classifier。**L2 操作**：藏 full.x → prefer thin+rest `_impl` 活。证：Ubuntu L2 矩阵 **5/5** · plus_one／add_one／first／pair_sum／increment／cut22 **0** · methodonly main 无 call（`mov $10; add $1; cmp $11`）· hello0 · 全量 smf 含 Wide **0**（本二进制；非宣称 Wide 债已闭）。PLATFORM: SHARED try_inline · LINUX gold · tip **`5d01edfda`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **METHOD(49) param0-field-sum try_inline** · 矩阵 **5/5** | **代码+文档** · **不升钉**。本刀只补全 `try_inline_param0_field_sum_call_elf`。五问：①产生＝体只认 CALL `num_args==1`／`call_arg_ref[0]`，METHOD 发射也不问；②存储＝recv 槽＋两字段 off＋add；③消费＝`recv.pair_sum()` ≡ `field_sum(recv)`。**根修（G.7 有则补全）**：体 48\|\|49（METHOD extra==0、arg=base）；METHOD 发射在 **import-binding 之后**、single_field 之后问同一 fold。`.x`＋seed `_impl` 同 commit。探针 +`Summable.pair_sum`／`field_sum`（Wide 前 19／20）。**同层未搅**：x_plus_k／classifier。**L2 操作**：pin-host prefer full.x 编整 TU 会毒邻居（first／increment／option 139）；藏 full.x → thin／cold-seed `_impl` 活。证：Ubuntu L2 矩阵 **5/5** · first／pair_sum／cut20 **0** · methodonly main 无 call（lea+load `.a`＋add 4＋load `.b`＋add，cmp 30）· hello0 · 全量 smf 含 Wide **0**（本二进制；非宣称 Wide 债已闭）。PLATFORM: SHARED try_inline · LINUX gold · tip **`8b09dab2c`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **fix(selfhost)** **METHOD(49) param0-single-field try_inline** · 矩阵 **5/5** | **代码+文档** · **不升钉**。其它 try_inline 族仍 CALL-only；本刀只补全 `try_inline_param0_single_field_call_elf`。五问：①产生＝体只认 CALL `num_args==1`／`call_arg_ref[0]`，METHOD 发射也不问；②存储＝recv 槽＋字段 off；③消费＝`recv.first()` ≡ `take_a(recv)`。**根修（G.7 有则补全）**：体 48\|\|49（METHOD extra==0、arg=base）；METHOD 发射在 **import-binding 之后** 问同一 fold（先问会让 option／si 编译 SEGV）。`.x`＋seed `_impl` 同 commit。探针 +`Firstable.first`／`take_a`（Wide 前 17／18）。**同层未搅**：field_sum／x_plus_k／classifier。证：Ubuntu L2 矩阵 **5/5** · first／cut18 **0** · main 无 call first／take_a（lea+load `.a`）· hello0 · 全量 smf Wide 钉盘冷编仍 139（先在债）。PLATFORM: SHARED try_inline · LINUX gold · tip **`80af0a4c7`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **revert** **struct16 deref METHOD** · 矩阵 **5/5** | **回滚** · **不升钉**。本刀开 `pipeline_asm_call_struct16_ret_needs_rax_deref_c` 48\|\|49 并让 slice／call-arg 问分类器。`7.as_wide().a` SEGV（`mk_wide` CALL 仍 0）。pin-host g05 仍红＝非狗食假因。分类器对 METHOD 跑 resolve／skip_heavy 不是 >16B sret 的安全 no-op。**已回滚** `.x`／seed @`ed6c425b5`。Quad 16B `.d` 是另层 home／rdx 布局（`.a` 绿、`.d` 错／组合 SEGV）。钉盘冷编矩阵 **5/5** hello0 mk.let=10；smf Wide 钉盘冷编仍红（原 tip 狗食 `4644688` 已覆盖）。PLATFORM: SHARED · LINUX gold · tip **`ed6c425b5`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-14 | **docs(日切)** **13 号归档 + 14 号主文档重写** | **文档** · **不升钉**。①`analysis/自举进度-归档-2026-08-13.md`：§6 收 13 日 **66** 行（METHOD(49) 物化／try_inline／>16B memcpy · SIMD leftover · soft residual 梯）。②`analysis/自举进度.md` 日切重写：KPI／前排保留 · §6 **仅 14 日**（classifier 回滚已记）。钉盘仍 **`e364f4a37`**。下一刀：其它 try_inline 族仍 CALL-only（勿再动 classifier）。PLATFORM: SHARED 文档 · **日常 L2 非 tip L4**。 |

