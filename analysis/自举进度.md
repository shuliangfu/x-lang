# Xlang / X language 自举进度

> **更新**：2026-09-14 · tip **`e1543ba69`**（P12g **收官＋真冷 L4 双端绿**：wave660 commutative-binop rbx-park 根修＋pthin_skip_tl 纯 asm **解禁**（默认面切纯 asm）＋wave661 PABI asg-thin weaken 清单补 `w157_walk_block_rec`（Darwin 冷链重复符号根修）；（g-3：skip_one_trait .x 权威上线＋23 差分；发射器六同族债全根修）· 前钉盘 `ecdb5cc1e`） · **金标 SSH＝`ubuntu-server-22`** · Ubuntu 验＝**git pull（禁 scp）**\


> **产品 L4 放行钉盘**：**`ecdb5cc1e`**（2026-09-13 晚 · 双端 **tip-egg** L4 真冷全绿：Ubuntu 10m23s＠md5 982a47b4＋Darwin 21m35s；bstrict 0 fail；4.2.8 七连根修＋egg 刷新阻塞根修后）· 前 **`5cac88d00`**／`fa1061224`／`e194cd294`／`d583f61f5`／`b5be5ed97`\
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅** · parser **7.2.2 关 pin ✅** · codegen 7.4.2 ✅ · typeck 7.4.1 ✅ · runtime monofile ✅ · present **0** · Stage 8 **30/30 FULLY CLOSED** · **7.4.3 PREFER_X_O 双端 8/8 ✅**\
> **终局债**：[C迁移追踪.md](C迁移追踪.md) · [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：主刀＝**P12g skip_one_trait 迁移收官战——收官＋真冷双端全绿**：wave660 rbx-park 根修＋解禁（默认面切纯 asm）后，**L4 真冷补测双端全绿**（Darwin 21m38s／Ubuntu 金标 9m53s，全擦 .o＋egg＋g05＋矩阵 5/5＋bstrict 0 fail）；首跑 Darwin 冷链暴露 **wave661**：PABI asg-thin 注入的 weaken 清单漏 `w157_walk_block_rec`（w157 波加在标记区内未同步清单→thin/base 双强定义；Darwin `-force_load` 才暴露、Ubuntu 归档选取静默择一＝平台互补）——补清单＋标记区全审计（余 static 或已列）＋维护纪律注释。冷产品 bound_method 0/7 复验。**不升钉**。\
> **下波候选**：①8.3.6 续缩仍 host-cc 产品切片（glue_tail 包装／parse_one_extern_and_add／primary parse／P19 align／if_stmt parse（回调 X mega））。②立案债：无 self trait 方法 rc=139 双端预存（另案）；ast_gen2 [64] 陈旧镜像；parser_asm_thin_c.from_x.c 443KiB 冷静回退。③pipeline_x mega（须点名）。vx／summit HARD BAN。日常闸门见**提速纪律**。

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1\
> **eq 墙钟纪律（2026-09-11）**：`eq_mode`＝daily／close／full；软刀禁串行 OFF=128 全表\
> **提速纪律（2026-09-12 · 用户要求更高效率；同日改双端）**：\
> 1. **软刀闸门＝Darwin L2 矩阵＋FORCE eq smoke（`EQ_DEEP_MAX_FILE_OFF=0`／本域 `EQ_ONLY`）＋Ubuntu 同序 L2**（用户 2026-09-12「都要经过双端验证」；禁只 Darwin 结案）。\
> 2. **一波＝一条（或同链两条）ABI 根墙＋soft-FP 攀尽可解锁层**（允许多层 parse_into／exact），禁「一层 exact／一层 climb 一波」。\
> 3. **tip heat g05 立卡不挡**：矩阵／eq 用既有 `xlang_asm`；禁为软刀空等 heat 绿。\
> 4. **禁默认** drift-gate／compress／全表 close／全量 bstrict；close 仅 ABI 根大改或升钉；L4 仅整波收口／日终兜底／谈自举。\
> 5. **墙钟目标**：单刀 Darwin 验收 ≤**5 min**；单刀 Ubuntu 同序 ≤**15 min**。深层 combinator 实测可超：v5.69 3 层 JOBS=4 Darwin ~32 min（仍优于 3 波各 1 层＋3 次 Ubuntu pull）。\
> 6. **深链分批覆盖戏停（2026-09-12 用户再催）**：leftover flatten 完后，一层 25／3 层 75 的 eq 只是对已 T combinator 再跑 C↔.x，**不减 host-cc、不改 T**。实测 3 层 JOBS=4 Darwin ~32 min／Ubuntu ~41 min。**禁**再以改注释换波次再跑。下一刀＝8.3.6／7.2.1 已 T 冷孪生缩壳（真减 host-cc LOC）或 pipeline_x mega（须点名）。\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；leftover-PE 探针红不当双端 L2 产品红。

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景**：[自举时序.md](自举时序.md)\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[13](自举进度-归档-2026-09-13.md) · [12](自举进度-归档-2026-09-12.md) · [11](自举进度-归档-2026-09-11.md) · [10](自举进度-归档-2026-09-10.md) · [09](自举进度-归档-2026-09-09.md) · [08](自举进度-归档-2026-09-08.md) · [07](自举进度-归档-2026-09-07.md) · [06](自举进度-归档-2026-09-06.md) · [05](自举进度-归档-2026-09-05.md) · [04](自举进度-归档-2026-09-04.md) · [03](自举进度-归档-2026-09-03.md) · [02](自举进度-归档-2026-09-02.md) · [01](自举进度-归档-2026-09-01.md) · [31](自举进度-归档-2026-08-31.md) · …

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波**）。**§6 表固定 3 列**；描述禁裸 ASCII `|`。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 过期整日流水 **日切归档**后主文档重新起表。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `5cac88d00`**（2026-09-10 晚二度升钉）。\
**MG 终局达成 = wave941/942** · **双端 Stage2 = ✅** · **选题权威 =** [C迁移追踪.md](C迁移追踪.md)。\
**本波主刀 = 7.2.1 P5d B-minus**（`pthin_ctrl.x` 有则补全 realign 六段走查；桥 peek 家族补 tok line/col/next_pos；P19 三标量补冷 C 孪生；g05 P5 lane 挪 P9a 后 gate p9a_ok；parse_if_stmt 仍 C）。leftover flatten **完**。立卡：MSVC va／WIN leftover／vx daily／summit HARD BAN／pipeline_x mega 须点名。leftover-to-audit unique **0**。leftover_helpers 余 **0**。残＝产品 `.inc` 切片仍 host-cc（含 glue_tail 包装、primary parse、P19 align、skip_tl skip_one_trait／parse_one_extern_and_add、if_stmt parse）；lite／suite／lex_skip／helpers／primary ident／type_ref／simd／unary／binop／skip_if walks／skip_tl struct／enum／extern／impl header／generic_bound_scan／enum_register／parse_one_extern_skip／body_tl skip／glue skip_one_function_full／try_skip_allow padding／write_result／parse_into／skip_imports／consume_path／try_skip／collect_imports／ctrl brace-skip／scan_sync／realign／library_scan／diag_late after_structs／fail／body_tl cfg_skip／diag_first_ident／lex_skip count／ASI advance_past／peek-name／first_token／run_len extra／lex_at_token／rewind／struct_field_name／ident_is_unsafe portable 文件留作冷回退；`parser_asm_thin_c.from_x.c` 443KiB 仅冷 rest 回退（G-02f-330 全切片齐则 omit rest）。

| 指标 | 状态 |
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`5cac88d00`**；residual tip present **0**；Stage 8 **30/30**；PRODUCT RETIRED 23/23 |
| Track MG | **11.3.1 MG ✅** |
| Track BC | **🟢 Stage8 30/30** · M4 **5/5** · present **0** |
| Track L | **收 30/30 FULLY CLOSED** |
| mac / Ubuntu L4（钉盘） | ✅ 双端 L4 + bstrict **129/129** @ **`5cac88d00`** |
| pin 蛋 | ✅ darwin.arm64 + linux.x86_64 |
| 本波构建级别 | **L2（仅 Darwin）**：矩阵 5/5＠本波 `xlang_asm` 5489640＋定向探针 p6 42 绿（A/B 旧二进制一致）。Ubuntu 本波不验。**不升钉** |

***

## 1. 文档地图

| 文档 | 看什么 |
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | 先→后全景 · S0–S8 |
| [C迁移追踪.md](C迁移追踪.md) | 终局债 / BC·PC DAG |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild（MG ✅） |
| **本文** | KPI + 前排 ≤3 + **§6**（**9 月 13 日**起） |
| [自举进度-归档-2026-09-12.md](自举进度-归档-2026-09-12.md) | **9月12日** leftover flatten／eq＋深链分批覆盖戏停＋P9／P9b／P1b／P19b（**122** 行） |
| [自举进度-归档-2026-09-11.md](自举进度-归档-2026-09-11.md) | **9月11日** B-minus v5.13→v5.29＋eq≤11s＋Cap 4.2.8（**32** 行） |
| [自举进度-归档-2026-09-10.md](自举进度-归档-2026-09-10.md) | **9月10日** B-minus v1→v5.12＋两度升钉＋7.4.4／g05／8.3.6（**73** 行） |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图（本波不改） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`5cac88d00`** | 2026-09-10 晚二度升钉；双端 L4 + bstrict 129/129 |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自钉盘 L4 `xlang_asm` 刷新 |
| residual tip（BC） | present **0** | Stage 8 30/30 · prefer pure-asm 默认 |
| mac 钉盘 L4 | ✅ L4 + bstrict **129/129** | `@5cac88d00` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 + bstrict **129/129** | `@5cac88d00` `ubuntu-server` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | **本刀 Darwin 5/5**＠新链 `xlang_asm` 5489640＋定向探针 p6（realign 后继）42 绿。Ubuntu 本波不验（主机重装） |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ 双端 tip SHA256 STRICT match | 钉盘历史见归档 |
| **R2 / Cap** | 按需 | 明细见归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1** |
| `TREE_ARMED` / `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1** / **1** |
| post-delete residual | **✅** wave944–964 |

***

## 3. 当前前排（≤3 条）

1. **下一刀（真减 host-cc）** — 本刀 P5d 已把 realign 六段走查收进 `pthin_ctrl.x`（全走 P9a 桥 peek 家族；桥补 tok line/col/next_pos；P19 三标量补冷 C 孪生；g05 P5 挪 P9a 后 gate p9a_ok；parse_if_stmt 仍 C——回调 X mega 勿 X emit）。续＝8.3.6 仍 host-cc 的产品 `.inc`（glue_tail 包装／skip_tl skip_one_trait／parse_one_extern_and_add／primary parse 仍 C；P19 align 仍 C）。lite／suite／lex_skip／helpers／primary ident／type_ref／simd／unary／binop／skip_if walks／skip_tl struct／enum／extern／impl header／generic_bound_scan／enum_register／parse_one_extern_skip／body_tl skip／glue skip_one_function_full／try_skip_allow padding／write_result／parse_into／skip_imports／consume_path／try_skip／collect_imports／ctrl brace-skip／scan_sync／realign／library_scan／diag_late after_structs／fail／body_tl cfg_skip／diag_first_ident／lex_skip count／ASI advance_past／peek-name／first_token／run_len extra／lex_at_token／rewind／struct_field_name／ident_is_unsafe portable 冷回退仍留盘。pipeline_x mega 须点名。summit／peak／zenith／versal／vx 仍 HARD BAN。Windows 卡离线不挑。Ubuntu 主机重装好后再补同序 L2。
2. **立卡（Windows）** — **10.7.1 MSVC va**＋ **seed 第二 WIN-leftover addr_of**（须切 Windows）。
3. **立卡不动** — eq 深链指数墙；spawn 真体仍 T；nest 冻帽 64。本刀 Darwin g05 ensure＋pure-ld 绿（heat 未挡）。钉盘 **`ecdb5cc1e`**。

***

## 4. 与归档的关系

- **9月12日归档**：[自举进度-归档-2026-09-12.md](自举进度-归档-2026-09-12.md) — **122** 行 · leftover flatten／eq＋深链分批覆盖戏停＋P9／P9b／P1b／P19b。
- **9月11日归档**：[自举进度-归档-2026-09-11.md](自举进度-归档-2026-09-11.md) — **32** 行 · B-minus v5.13→v5.29＋eq 墙钟根治＋Cap 4.2.8。
- **9月10日归档**：[自举进度-归档-2026-09-10.md](自举进度-归档-2026-09-10.md) — **73** 行 · B-minus v1→v5.12＋两度升钉。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `5cac88d00`**。
- 日切后主文档只保留 KPI + 前排 + **9 月 13 日** §6。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
| **macOS arm64** | 钉盘 L4＠**`5cac88d00`** · 软刀 L2 验收端；本刀矩阵 5/5＠新链 `xlang_asm` 5489640 |
| **Ubuntu x86_64（金标）** | 钉盘 L4＠**`5cac88d00`** · **本波不验**（主机重装）；前刀 L2 绿＠`b00c7d9db`；SSH＝**`ubuntu-server`** |
| **Windows MSYS2** | leftover unique **0** · min-gate 绿 · MSVC va／第二 leftover 离线立卡 |

***

## 6. 变更记录（倒序 · **9 月 14 日**；13 日见 [归档](自举进度-归档-2026-09-13.md)）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-14 | **fix(selfhost)** **P12g 真冷收官：wave661 PABI asg-thin weaken 清单补 `w157_walk_block_rec`——Darwin 冷链重复符号根修＋L4 双端真冷全绿（e1543ba69）** | **脚本（weaken 清单＋维护纪律注释）＋文档** · **不升钉** · **L4 双端真冷全绿**（Darwin 21m38s＠0dc341c26＋树内修复；Ubuntu 金标 9m53s＠e1543ba69；全擦 .o＋egg＋g05＋矩阵 5/5＋bstrict 0 fail）。机制：ASSIGN_THIN 标记区内每个非 static 定义会同时编入 thin 注入成员与整编 base 成员，合并靠显式 weaken 清单收敛 base 副本——w157 波把 `w157_walk_block_rec` 加进区内未同步清单→`.o` 双强定义→Darwin `-force_load` 冷链 `duplicate symbol`（首跑 L4 g05 步暴露；解禁波之前增量链不走注入故潜伏）；Ubuntu 归档按需选取静默择一＝平台互补实证。修：清单＋函数；标记区全审计（余者已列或 static 无冲突）；注释固化不变量（区内新增非 static 定义必须入清单，双端验证）。冷产品 bound_method 0/7 复验。 |
| 2026-09-14 | **fix+selfhost(selfhost)** **P12g 收官：wave660 commutative-binop rbx-park 根修＋pthin_skip_tl 纯 asm 解禁——第四分歧（T001 copy\<A\>）指令级闭环（a5113c657＋8f92a42c9）** | **代码（.x＋种子 .c 三孿生）＋脚本（禁令移除）＋文档** · **不升钉** · **双端默认面全绿**。指令级铁证（條件斷點釘停點＋watchpoint＋3000 步軌跡＋互動讀寄存器）：turbofish pl 真實調用返回 1（len("A")，正確），lens store 落點＝`g_call_typearg_lens+16`（lens[4]）——索引算出 4 而非 0；反匯＝`ldr x1(nargs); mov w1,#4; ldr x0(cn); mul; add x0,x0,x1`＝**mul 常量暫存 w1 覆寫 add 已裝載右操作數**。根因＝commutative 權威點「簡單邊先裝 rbx」分支後發射複雜對側（嵌套 binop 字面量經 mov_imm32_to_rbx 打同一 rbx）；wave338 只修 ADD 面裸 VAR 右操作數，CAST 包 VAR（`nargs as usize`）漏網——線性值跟蹤同族第六案。修：兩分支各加 `glue_expr_emit_may_clobber_rbx_elf_c(對側)==0` 前置門（先查後載），覆寫組合落既有左先尾部。禁令理由（rc=139 大函數實參編組）實為本 bug 幀污染表現，根修後解禁＝**默認面切純 asm**。驗：Darwin＋Ubuntu 金標（git pull）雙端無 env 默認面 bound_method **0/7**＋27/27 探針全同＋L2 矩陣 5/5。附工具翻案：「地址斷點不可靠」＝十六進制手算錯位；可靠鏈＝nm -p＋對齊 disassemble＋watchpoint＋$x30 條件斷點＋精確步數 batch 單步。 |
| 2026-09-14 | **fix(selfhost)** **P12g DIV：call 權威點 rax+rbx 快取雙失效（四孿生加固）＋turbofish lens 假設證偽（發射審計全綠）** | **代码（四孿生）＋文档** · **不升钉** · **產品轉譯軌恢復綠（trait=42/bm=7/矩陣 5/5）**。call 權威 `backend_enc_call_arch` 補 rax+rbx 快取失效（call 清空兩者；BM5 rbx 修復的配對完成）——語義中性加固。T001 lens=0 假設證偽：修復入鏈後仍 0；指令級審計 `pl=peek_ident_len` 賦值與 lens 存儲**發射均正確**——靶轉移到 (a) pass-2 運行時 peek 返回或 (b) 多 pass 重清零；純 asm 曾一次產出正確二進制＝通道相關分歧。lldb 地址斷點全程不可靠教訓入檔。 |
| 2026-09-14 | **fix(selfhost)** **P12g BM5 根修落地：rbx 變數槽快取在 mov-rax-to-rbx 權威點失效（四孿生）——bound_method SIGBUS 消除（511b51b9d）** | **代码（四孿生根修）＋文档** · **不升钉** · **產品轉譯軌恢復綠（trait=42/bm=7/矩陣 5/5）**。指令級實錘：連續同索引數組存儲的 store1 經 mov-rax-to-rbx 把結果停泊 x1/x19，binop 變數槽快取仍信「rbx 持 zi」→ store2 命中陳舊快取跳過 zi 重載→slot_off 加上 store1 的指針→兩指針相加 SIGBUS。修：權威點（125+ 位共用）統一失效，語義中性。兩次自舉後純 asm 軌 SIGBUS 消除；殘餘＝掃描存活後暴露的 .x/C 語義分歧（T001 copy<A> Clone 判定，下波對照修）。**同族三債閉環**（w157 撞號/staging 支配/rbx 快取——皆線性值跟蹤無失效）。 |
| 2026-09-14 | **docs(selfhost)** **P12g BM 波：bound_method 純 asm 預存債偵察（崩點定位到 generic_bound_scan 棧參溢出槽）＋產品恢復轉譯軌** | **文档** · **不升钉** · **產品雙驗綠（trait=42/bm=7/矩陣 5/5）**。stash 對照證實 bound_method rc=138 與 w157 根修無關（前後皆崩）＝純 asm 軌預存債。崩點：`.x` 26 參 `generic_bound_scan_into_c +4956` 寫垃圾地址（[x29,#0x38] 溢出槽＋索引）；scan 幀 6080 分塊正確、棧參基址（F+16）審計無誤——嫌疑收斂到晚期棧參 param-home 溢出槽未寫或 CALL packer 分類不一致。另錄 lane 緩存教訓（PABI .o 抖動時 pthin 純 asm .o 攜舊估算，測前必 touch 重發射）。解禁維持至該債關閉。 |
| 2026-09-14 | **fix(selfhost)** **P12g 發射器根修完成：EXPR_IF/EXPR_BLOCK 與 binop kind 撞號——else-if 鏈全臂從 spill 估算消失（0fdac31b8）** | **代码（雙孿生根修）＋文档** · **不升钉** · **walked 284/284 精確＋純 asm 軌崩潰消失**。終極根因：EXPR_IF(25)/EXPR_BLOCK(26) 共享 w157 binop 集合序數；parser 反-2^N 修復把深 else-if 鏈轉嵌套 EXPR_IF（臂＝EXPR_BLOCK 包裹、鏈尾＝final_expr 36284）；binop 分支無 else 子→首臂後全丟（284 計 17）→幀欠預留→spill 越幀（全部症狀閉環）。修：25/26 先試 if/block 訪問器（共享序數讀空落 binop）＋塊走查重構深度封頂遞歸 walker（兩孿生）。驗：走查 284/284；幀 0xe30→0xfff+；純 asm 軌（越禁測試）trait 探針全過無崩；bound_method SIGBUS(138)＝純 asm 預存債（歷史同崩，另案）故禁令維持。產品轉譯軌 23 差分 22/22＋矩陣 5/5。六波偵探鏈（幀審計→幾何→結構矩陣→停擺量化→ring 差集→445 解剖→撞號實錘）全錄 RFC。 |
| 2026-09-14 | **docs(selfhost)** **P12g 根修終極實錘：walk 停擺量化（29 塊/484 表達式 vs 期望 250/6000）——深 else 鏈 if 只在 stmt_order，數組視圖截斷** | **文档** · **不升钉** · **插桩已撤产品绿（trait=42）**。走查計數器實測 sig：block_visits=29/expr_visits=484/cspill=1984——else 脊柱 ~15 層後訪問器鏈斷流。對照探針矩陣再擴（16 臂鏈內嵌 while+region+調用精確 3072）全部排除淺層形狀。結論 v4：深 else 鏈超出深度後 if 僅存於 stmt_order（kind 5→if 池 idx，發射器同源可讀），w157 的 if_stmts 數組視圖截斷→深臂全漏。根修方案定案：w157 塊遍歷以 stmt_order 為權威（nso>0 只走 stmt_order 派發，防重複計數），驗證鏈＝cspill 跳升→幀自動增長→崩態探針→差分→雙端→解禁。 |
| 2026-09-14 | **fix(selfhost)** **P12g w157 加固（走查棧 256→8192 兩孿生）＋語句失蹤點破案：巨鏈臂體不在 if_then_body_ref** | **代码（加固）＋文档** · **不升钉** · **行為面＝純防禦性幀餘量**。結構探針矩陣（expr/let/while/region/else-if/嵌套/規模 N=512 精確）全綠——w157 覆蓋面與 caps 非截斷點；走查棧 256→8192 保留為加固（實測不改計數）。真實 sig 結構轉儲破案：top nso=63/while 體 nso=6 nif=3，**16 臂巨鏈首臂 then 塊讀出為空且無 else**——臂體在 AST 存於他處，w157 遞歸進空塊全丟（cspill 1984 vs 需求 ~36KB）。下波：轉儲 if#2/#3＋region 體定位真身存儲→擴遞歸→驗證鏈→解禁。插樁全撤、產品雙驗綠（trait=42＋矩陣 5/5＠`0e56cfd51`）；egg＋PABI .o 全量重建手冊入 RFC。 |
| 2026-09-14 | **docs(selfhost)** **P12g 發射器根因破案：w157 spill 估算不識 stmt_order——大函數幀溢出（機制 v3 數字齊備）** | **文档** · **不升钉** · **产品恢复绿（trait=42）**。幀審計插樁實測：sig frame=3616（含保底 scratch 2048）vs 反彙編實測 staging 槽 0xe78 越幀——w157 call-spill 估算對 284-call 的 sig 貢獻≈0（真實需求 ~28KB）。覆蓋面（CALL/METHOD/ASSIGN 兩版都有）非問題；**根因＝遍歷只走 per-kind 原始數組，stmt_order（nso>0）塊的語句真身全漏計**——小函數靠 2048 保底矇混、大函數 spill 峰值越幀寫入調用方幀（全部症狀閉環）。根修方案已錄 RFC：w157 在 nso>0 時改走 stmt_order kind/idx 派發（.x＋.c 兩孿生同步）；驗證鏈＝cspill 審計值→崩態探針→差分→雙端→解禁。插樁已撤、種子 .c 乾淨；PABI .o 為構建產物（含 env 門控審計，無害）。 |
| 2026-09-14 | **docs(selfhost)** **P12g 根修進展三：rec 賦值路徑 14 處未守衛槽解析鎖定（幀污染候選根因）** | **文档** · **不升钉** · **零代碼改動產品維持驗證態**。lvalue VAR 路徑排除（miss 響亮 -1）。靜態掃描鎖定 `pipeline_asm_emit_expr_elf_rec` 賦值路徑 **14 處** `glue_var_expr_stack_off_elf_c` 無 `<0` 守衛（行號已錄 RFC），每處緊跟手工 next_offset 操作；miss 時按 -1/垃圾 offset 幀寫——與 4 字節幀污染全部症狀相容。下波：單點加守衛重建崩態產品二分驗證→逐一補守衛＝根修→靜態全量審計→解禁。 |
| 2026-09-14 | **docs(selfhost)** **P12g 根修進展二：全零回退實證排除＋實參查找鏈全圖＋剩餘嫌疑鎖定** | **文档** · **不升钉** · **插桩已撤产品恢复（矩阵 5/5＠e8bf463b1）**。插桩實證：`pipeline_asm_local_offset_c` 全零名回退在崩態產品 trait 編譯全程**從不觸發**——該回退非機制。查找鏈全圖入檔（for_call_args→快徑 scoped 窄掃→miss 全表→lazy-append 重解；均無匿名回退）。剩餘頭號嫌疑：快徑 miss 落入通用 emit_expr 的 VAR 分支時對 off==-1 的處理——若靜默留空（rax 殘留）＝垃圾實參直達調用。崩態布局輪盤（同 .o 兩鏈一崩一綠）→ 根修驗證以 sig 反彙編 arg0 源槽全量靜態比對為準，不賭再現。 |
| 2026-09-14 | **docs(selfhost)** **P12g 發射器根修進展：兩條路徑排除＋staging 源頭鎖定（下波靶心明確）** | **文档** · **不升钉** · **产品维持转译轨（已验绿）**。排除：arm64 push 路徑（eval→直 mov 參寄存器，無 spill）與 EXPR_VAR 實參路徑（`for_call_args` 直取 home 槽）均乾淨。鎖定：**通用 call 路徑**（backend_call_dispatch.from_x.c:4505）每參求值後 `glue_sysv_spill_rax_rdx_to_frame_c` 溢出＋末尾統一重載，且溢出槽計數器**與局部槽分配器共享 ctx.next_offset**（註釋自認 +16 stride 同步）——與塊級 save/restore 的相互作用為頭號嫌疑。下波靶心與復現要領（崩態產品重建＋sig 重定位）已錄 RFC。本波純偵察零代碼改動，產品保持 cd4ab947a 驗證態。 |
| 2026-09-14 | **docs(selfhost)** **P12g 發射器機制釘死：call 實參 staging 槽線性作用域違規（根修線索齊備）** | **文档** · **不升钉** · **产品已恢复转译轨**。崩態產品上完成靜態審計：sig（52KB）284 個調用中 ~261 個 ent 讀寫調用的 arg0 取自 **~30 個共享 staging 槽**（0x280–0xc98），staging 寫入距取用數百條指令且**無支配保證**（實據：+3828 調用讀 +2160 才寫的槽 0x870）——分支臂跳過寫入時槽為垃圾 → 被調方持垃圾指針 4 字節部分寫污染調用方幀（銜接前波 out 槽實錘序列）。body→sig 編組乾淨；污染全在 sig 內部。根修方向錄 RFC：EXPR_VAR 實參直取 canonical home 槽（首選）或 staging 按 call site 唯一分配（備選）；驗證鐵律＝必須產品鏈接（隔離綠≠產品綠）。產品恢復（禁令＋touch 沖刷）：trait 探針綠＋矩陣 5/5＠`cd4ab947a`＋prologue 確認 clang 風格（轉譯軌）。 |
| 2026-09-14 | **docs(selfhost)** **P12g 發射器槽 bug 實錘：禁令解除→產品全崩→二度上架（產品恢復雙驗）** | **文档＋守衛** · **不升钉**。上午三形狀隔離重建均綠一度判「不可複現」；解除 pure-asm 禁令後**產品鏈接確定性全崩**（trait 探針全 rc=139）——bug 真實且**鏈接布局依賴**（隔離 8-.o 布局落良性垃圾、產品 68-.o 落惡性值）。lldb 逐步實錘污染序列：蹦床 `out` 槽經 **4 字節部分寫**逐步變 0xb6c4——`.x` 被調函數持編組錯的垃圾參數指針在調用方幀上亂寫（大函數 call-arg 臨時槽到達路徑未初始化）。二度上架禁令＋touch 強制重編後產品恢復（矩陣 5/5＠`51c4d5260`＋trait 探針綠）。根修約束錄 RFC：驗證必須走產品鏈接、優查 body→sig 編組槽初始化、lane .o 緩存慣性。 |
| 2026-09-14 | **feat(selfhost)** **7.2.1 P12g g-3 重放成功：skip_one_trait .x 權威上線（gate 開；轉譯軌）** | **代码＋文档** · **不升钉** · **行为面＝trait 走查改 .x**。四重放在 `c86a87926` 基線：unsafe 清掃（`iv` 提升×10＋bk6 包裹×8——`-c` 嚴格 typeck 過）；header 0/-1 區分返回碼；body fn_line/col 接 `peek_tok_line/col` 橋；params 尾 `else { saw_param_tok = 1; }`（裸 self 計參）。trampoline 落 `.inc`（registry 槽直寫＋成功才 bump＋scratch 兜底＋raw fallback 留 C）。**pure-asm 禁令**（發射器大函數調用參數槽 UB——上波定位；`XLANG_P12G_ALLOW_PURE_ASM=1` 可越）→ 本 .x 雙平台一律 `-E` 轉譯（隔離 harness 五形狀註冊表驗證全對）。**差分 23/23**：20 全同＋bound_method 三兄弟從編譯器崩潰（rc=138）修復為文檔期望（green run=7×2＋wrong 乾淨 compile=1）——.x 走查比 C 孿生更穩；無 self trait 方法運行期 rc=139 為雙端預存（stash 對照證實）另案。驗＝Darwin 差分＋矩陣 5/5＠`c49c9137d`。發射器槽 bug 後端線索（sidecar 全零名回退鏈）已錄 RFC。 |
