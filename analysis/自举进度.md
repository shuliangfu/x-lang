# Xlang / X language 自举进度

> **更新**：2026-08-13 · **METHOD/CALL FLOAT_LIT 按形参盖戳** + assemble simd 别名旧债 · 矩阵 **5/5** · 钉盘仍 **`e364f4a37`** · **不升钉**\
> **产品 L4 放行钉盘**：**`e364f4a37`**（2026-08-11 升钉 · 前 `d79a368b2` 2026-08-10 · 再前 `36363b90f` 2026-08-07）\
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅**（12 labi_*.x 切片 prefer；默认 FROM_X=1）· parser **7.2.2 关 pin ✅**（tip `-E` + assemble；产品默认 FROM_X=1）· codegen 7.4.2 wave323 ✅ · typeck 7.4.1 wave322 ✅ · runtime monofile wave321 ✅ · present **0** · 钉盘 **`e364f4a37`** · Stage 8 tip **wave332 Track L 30/30 FULLY CLOSED**（**PRODUCT HALF=0** · **PRODUCT PINNED=0** · **PRODUCT RETIRED 23/23 = 100%**）· **7.4.3 PREFER_X_O 双端 8/8 wave338 ✅** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁**\
> **tip L4 安全网（历史）**：**`ffc0b646f`**（已由 tip L4 升钉吸收；前网 `4bacaf18c`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：typeck resolve 后 FLOAT_LIT 按形参 f32/f64 盖戳 · assemble simd 别名补全 · s2／fill4 **0** · 矩阵 **5/5** @`960ad9d7a` · 钉盘 **`e364f4a37`** · **不升钉**\
> **下波候选**：①soft 129 扫（首红 check 族须明示）· ②`glue_vector_let_init_uses_direct_slot` 仍漏 METHOD(49)（栈预留，非本值债）· ③升钉仍须双端 L4+129

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1；过程上 prefer+heat 一次／域，双端矩阵同波一次\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；**SHARED 双端 L2**；日常矩阵 **只** `./xbuild l2-matrix`（wave299：捕获 rv=42／opt=102 成功码，禁 set -e 截断）；谈自举／升钉才 L4 + 矩阵 + 全量 bstrict；bstrict **per-script timeout=300s** + catalog cache（防无界卡死）；**墙钟**：`product_l4` 分步 wall + `run-all-bstrict` 每脚本 wall／top-N（`XLANG_BSTRICT_TIMING_FILE`）；并行 **`XLANG_BSTRICT_JOBS` 默认 1**（Ubuntu 可 opt-in 2–4；Darwin 慎用 OOM）

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[12](自举进度-归档-2026-08-12.md) · [11](自举进度-归档-2026-08-11.md) · [09](自举进度-归档-2026-08-09.md) · [08](自举进度-归档-2026-08-08.md) · [07](自举进度-归档-2026-08-07.md) · [06](自举进度-归档-2026-08-06.md) · [05](自举进度-归档-2026-08-05.md) · [04](自举进度-归档-2026-08-04.md) · [03](自举进度-归档-2026-08-03.md) · [02](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · …\

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `e364f4a37`**（2026-08-11 升钉 · 前 `d79a368b2` 2026-08-10 · 再前 `36363b90f` 2026-08-07）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**双端 Stage2 = ✅ SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709` · `xlang_asm_stage1`／`xlang_asm2` bit-identical · 黄金自举金标准 · strict-link 根修后双端首过）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（Makefile 已删除；**变更记录只在本文 §6**）。

| 指标 | 状态 |
|------|------|
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`e364f4a37`**（L4 升钉冻结 · 双端 L4 真冷绿 · 含 Stage12.0.5 pure-asm residual）；residual tip **wave332** present **0**；Stage 8 tip **wave332**（**30/30 FULLY CLOSED**；**PRODUCT RETIRED 23/23=100%**；PRODUCT HALF=0；PRODUCT PINNED=0） |
| Track MG | **11.3.1 MG ✅**（wave941 物理删 Makefile · wave942 catalog CFLAGS · 0 make in bootstrap · 产品入口 `./xbuild`） |
| Track BC | **开 🟢（30/30 FULLY CLOSED wave332）**（B′ 至 **wave326** link_abi **7.3.1 关 pin** ✅ · M4 **5/5 全 ✅**（runtime/typeck/codegen/parser/link_abi 五域冷链全闭）· present **0**；**BC Stage8 Batch3 收**＝**30/30 FULLY CLOSED**（PRODUCT 23 RETIRED 100% catalog+ladder；NON-PRODUCT 7 正确分类 never-product-chain = TEST×2 / STAGE×2 / EXTRACT_ONLY×1 / DELETED_ORPHAN×2；G.7 无 HALF 无 unresolved PINNED）|
| Track L (Stage 8) | **收（Batch 3 30/30 FULLY CLOSED wave332）**（批次 0 清理 ✅ · 批次 2 **wave1036–1041** 退役 ✅ · 批次 3 **wave327–332**（lsp_diag+lsp+pipeline+driver+preprocess+lexer+parser/typeck/codegen/ast_gen2 10 项 + audit 分类误判 3 项 RETIRED 修正 + ast_gen.c 孤儿物理删 + ast_gen2 cold-seed pin + 分母口径统一 is_product_denominator）✅ · **30/30 FULLY CLOSED**；PRODUCT RETIRED 23/23=100%；HALF=0；PINNED 产品=0 · 详见 [阶段8高效退役分析.md](阶段8高效退役分析.md)） |
| mac / Ubuntu L4（钉盘） | ✅ **双端 L4 真冷 remaining_o=0 + 矩阵 + bstrict 129** @ **`e364f4a37`**（mac wall=34m14s · Ubuntu wall=11m41s · G.8 SHARED 双端同钉闭环）|
| pin 蛋 | ✅ `seeds/bootstrap_xlangc.darwin.arm64` + `linux.x86_64` 自本波 L4 产品 `xlang_asm` 刷新 |
| 本波构建级别 | 钉盘 **L4 已结**；日常 BC 续 **L2**（check 闸门暂停） |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录** |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图 |
| [自举进度-归档-2026-08-12.md](自举进度-归档-2026-08-12.md) | **8月12日** Stage12.0.5 prefer 默认 pure-asm · PC FORBID／ALLOW · mega ban／RCA · opaque WEAK · catalog cache · L4 墙钟 · **47** 行 |
| [自举进度-归档-2026-08-11.md](自举进度-归档-2026-08-11.md) | **8月11日** Stage12.0.5 pure-asm residual 闭口 + **升钉 `e364f4a37`**（pure_asm 13/13 · L2-asm／tcpu／ldpc · labi residual · full12 双端地图 · **41** 行 + 10 日余 **4**） |
| [自举进度-归档-2026-08-09.md](自举进度-归档-2026-08-09.md) | **8月9日** runtime thin dual leave → M4 五域关 pin → Stage 8 Batch 3 30/30 → PREFER_X_O 双端 8/8（**28** 行 · 钉盘 `36363b90f`） |
| [自举进度-归档-2026-08-08.md](自举进度-归档-2026-08-08.md) | **8月8日** B′ pipeline_x mega 首刀 bootstrap_glue→ast_pool 各域→elf/wpo→8.3 结构地板→gen dual leave→typeck.x 全量 `-E` T001（**wave262–314** · **56** 行） |
| [自举进度-归档-2026-08-07.md](自举进度-归档-2026-08-07.md) | **8月7日** B′ pure leave wave200–261 + 升钉 `36363b90f` + glue_statics（**64** 行） |
| [自举进度-归档-2026-08-06.md](自举进度-归档-2026-08-06.md) | **8月6日** B′ pure leave field_access→wave199 CALL-target resolve（**50** 行） |
| [自举进度-归档-2026-08-05.md](自举进度-归档-2026-08-05.md) | **8月5日** L4 冷恢复 + 8.3.3 R2 + pure-asm match + B′ leave 至 binop（**74** 行） |
| [自举进度-归档-2026-08-04.md](自举进度-归档-2026-08-04.md) | **8月4日 wave1241–1291**（BC ast_pool 16 波域分解 + glue 壳 thin + check 收口 + 升钉 `db809e00f`） |
| [自举进度-归档-2026-08-03.md](自举进度-归档-2026-08-03.md) | **8月3日 wave1227–1240**（BC glue 叶收口 + check 工具链启动 · 15 行） |
| [自举进度-归档-2026-08-02.md](自举进度-归档-2026-08-02.md) | **8月2日 wave1130–1220**（BC glue 续迁 + typeck 跨模块字段根修 · 43 行） |
| [自举进度-归档-2026-08-01.md](自举进度-归档-2026-08-01.md) | **8月1日 wave964–1111-1129**（BC 8.3.1 G.7 fold 50+ 叶 + Stage 8 退役 13/30） |
| [自举进度-归档-2026-07-31.md](自举进度-归档-2026-07-31.md) | **31 日全日 wave895–963**（MG 真删 + post-delete + 开 BC） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单；per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`e364f4a37`** | 2026-08-11 升钉；双端 L4 真冷 + 129（mac 34m14s · Ubuntu 11m41s）；含 Stage12.0.5 pure-asm residual 双端 L2 收口；前 `d79a368b2` |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自本波 L4 `xlang_asm` 刷新（darwin 4.45MB · linux 4.31MB） |
| residual tip（BC） | wave339 | M4 **5/5** 全闭 · Stage 8 Batch 3 **30/30 FULLY CLOSED** · 7.4.3 PREFER_X_O **双端 8/8** · pure-ld 58/60 objs · **双端 L4** @ **`e364f4a37`** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁** · **soft residual 类批 1+2+3** · PREFER mangle 双名根修（Ubuntu L4 重跑）|
| mac 钉盘 L4 | ✅ L4 + bstrict **129** · 34m14s | remaining_o=0 · `./xbuild l4` · `/tmp/xlang_l4_Darwin_e364f4a37.log` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 + bstrict **129** · 11m41s | `ubuntu-remote-server` · `/tmp/xlang_l4_Linux_e364f4a37.log` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | G.7 单权威 `product_l2_matrix.sh`（wave299）；rv42／opt102／hello0／si0／f32 一次跑全 |
| 前钉 | `d79a368b2` · `36363b90f` · `db809e00f` · … | formal_mod → hang guards → wave1277 → … |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 18 号结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ **双端 SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709`） | `stage2-hash-gate OK (SHA256 match — 黄金自举金标准)` · `xlang_asm_stage1`／`xlang_asm2` 行为一致（42 + hello）· strict-link 根修后双端首过 · 日志 `/tmp/xlang_stage2_ubuntu_9e60e1290.log` + `/tmp/xlang_stage2_macos_fix2.log` |
| **R2 / Cap** | wave710 升钉后按需 | 明细见 29 归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1**（已执行） |
| `TREE_ARMED` | **1** |
| `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1**（wave941 物理删除） |
| `PHYS_DEL_WINDOWS_GATE_STATUS` | `reproven_green` @ `71dd60a12`（wave922 · wave940 一键冷启动） |
| preflight blockers | **全清零**（wave937–939） |
| 真删条件 | ✅ 已完成 @ wave941/942 · 钉盘 **`77b334842`**（后升至 **`db809e00f`**） |
| post-delete residual | **✅ 已收**（wave944–964） |

***

## 3. 当前前排（≤3 条）

1. **钉盘 `e364f4a37` 已升且冻结** — 日常 **不升钉**；tip L4 绿 @`3c75e615b`；本波日常 **L2**（非 tip L4）。
2. **Stage12 本波收 STD-061 peel 跨层** — `glue_peel_comptime_array_lit_mask_c` 沿 `parent_block_ref` 找 while 外层 comptime mask；热循环 shuffle 发 `pshufd`；**不**改 1.0× 门槛。
3. **PC／Stage12 底盘** — prefer 族 pure-asm 默认 ✅ · FORBID／ALLOW_HOST_CC ✅ · pipeline_abi mega **仍硬禁真开** · 树级 PREFER 硬 strip。

***

## 4. 与归档的关系

- **8月12日归档**：[自举进度-归档-2026-08-12.md](自举进度-归档-2026-08-12.md) — Stage12.0.5 prefer 默认 pure-asm · PC FORBID／ALLOW · mega ban／RCA／墙钟 · opaque WEAK · catalog cache · L4 墙钟画像 · **47** 行 · 钉盘仍 **`e364f4a37`**。
- **8月11日归档**：[自举进度-归档-2026-08-11.md](自举进度-归档-2026-08-11.md) — Stage12.0.5 pure-asm residual 闭口 + **升钉 `e364f4a37`**（pure_asm 13/13 · L2-asm hybrid 3/3 · tcpu／ldpc · labi residual RED×0 · full12 双端地图 · **41** 行）。
- **8月9日归档**：[自举进度-归档-2026-08-09.md](自举进度-归档-2026-08-09.md) — runtime thin dual leave → M4 五域关 pin → Stage 8 Batch 3 Track L **30/30 FULLY CLOSED** → 7.4.3 PREFER_X_O **双端 8/8** → wave339（**28** 行 · 钉盘仍 `36363b90f`）。
- **8月8日及前**：见各日归档链接（§1 文档地图）。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `e364f4a37`**（2026-08-11 升钉 · 前 `d79a368b2` · 再前 `36363b90f`）；residual tip **wave339**（present **0** · 日常 L2 可续）。
- 日切后主文档只保留 KPI + 前排 + **8 月 13 日** §6（12 日及前见归档）；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
|------|----------|
| **macOS arm64** | **钉盘 L4+bstrict 129 ✅** @ **`e364f4a37`**（2026-08-11 升钉 · wall=34m14s · remaining_o=0 · 矩阵 5/5）· 双端 L4 真冷绿（G.8 SHARED 同钉闭环）· 产品只走 `./xbuild` · pin 蛋已刷 · check 闸门暂停 |
| **Ubuntu x86_64（金标）** | **钉盘 L4+bstrict 129 ✅** @ **`e364f4a37`**（2026-08-11 升钉 · wall=11m41s · remaining_o=0 · 矩阵 5/5）· 双端 L4 真冷绿 · pin 蛋已刷 · **SSH 权威 host＝`ubuntu-remote-server`** · **远端工作树＝/home/shuliangfu/worker/xlang/x-lang（branch=self-hosting）** · 同步流程：`git fetch origin` + `git merge --ff-only origin/self-hosting`（先 stash 远端 M/?? pin 文件避免冲突）|
| **Windows MSYS2** | wave940 一键冷启动绿 · STATUS=`reproven_green` · Makefile 已物理删除 · 本波未升 Windows pin（仓内无 windows pin 蛋） |

***

## 6. 变更记录（倒序 · **8 月 13 日**；12 日见 [归档](自举进度-归档-2026-08-12.md)）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-08-13 | **fix(selfhost)** **METHOD/CALL FLOAT_LIT 按形参盖戳** · assemble simd 别名 · 矩阵 **5/5** | **代码+文档** · **不升钉**。splat emit pack 已绿；METHOD 实参仍按 call ambient（Vec4f）check，resolve 后未盖 f32。五问：①产生＝`typeck_check_expr_method_call`／CALL 实参 `check_expr(ambient)`；②存储＝FLOAT_LIT 默认 f64；③消费＝formal ABI xmm／emit `float_lit_to_rax`。CALL 已 stamp ARRAY_LIT。**根修（G.7 有则补全）**：`typeck_stamp_resolved_args_float_lit` 转调 `typeck_coerce_init_float_lit_to_decl`（dep kind → caller `ensure_by_kind_ord`；UFCS param_base=1）· typeck.x＋seed 同 commit · emit pack 仍 belt。s2 +`fill4(1.0)`。**同波旧债**：typeck.x `-E` UNDEF `is_simd_comptime_callee` mangled 面——companion `typeck_mangle_link_alias` 缺行（seed 已有）。有则补全 alias 表。证：Ubuntu L2 s2／splat／fill4 **0** UNDEF=NONE · ss／xffle／roundtrip **0** · 矩阵 **5/5**。PLATFORM: SHARED typeck · LINUX gold · tip **`960ad9d7a`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **splat(1.0)[0] f32 pack** · s2 Vec4f lane · 矩阵 **5/5** | **代码+文档** · **不升钉**。s2 曾暴露 `splat(1.0)[0]` diag rc=10。五问：①产生＝`try_inline_splat` FLOAT_LIT 走 `glue_emit_float_lit_to_rax(force=0)` 发 f64 位再 store-4；②存储＝未标 FLOAT_LIT 默认 f64（1.0＝`0x3FF00000_00000000` 低 32＝0.0f）；③消费＝INDEX `vf[0]`。ARRAY_LIT 已 stamp＋`force_esz==4`。**根修（G.7 有则补全）**：dest esz==4 复用 IEEE f64→f32 pack＋`imm_fill`（`.x`／seed 同 commit）· s2 回装 `vf[0]`／`vh[0]`。证：Ubuntu L2 splat-diag **0**（含 ident splat）· s2／ss／xffle／roundtrip **0** · 矩阵 **5/5**。PLATFORM: SHARED emit · LINUX gold · tip **`a54585045`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **g18 select_lane 针** · s2 断言 · 矩阵 **5/5** | **代码+文档** · **不升钉**。仅调 `simd.select_lane` UNDEF 两面；s2 用 `placeholder()` 当退出码假绿。五问：①产生＝产品 overload `select_lane` 已有 formal 面；②存储＝g18 针表 count 仍 21、无 mid；③消费＝ld ensure。**根修（G.7 有则补全）**：g18 +2 针 count **23**（`.x`／seed／surface 同 commit）· s2 断言 Vec8i splat/add + select_lane · **不**新开 formal。证：Ubuntu L2 仅调 select_lane **run=0 UNDEF=NONE** · s2／ss／xffle／roundtrip **0** · 矩阵 **5/5**。残：`splat(1.0)[0]` emit（diag rc=10，另刀）。PLATFORM: SHARED labi+smoke · LINUX gold · tip **`c53cb1c3b`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **129 xlangffle perf 薄包装** · dual smoke 收敛 · 矩阵 **5/5** | **代码+文档** · **不升钉**。gate 已薄 `exec`；129 `run-perf-simd-xlangffle-select` 仍抄一套 r04 双体。五问：①产生＝历史双关名复制 perf／smoke；②存储＝129 白名单必须留文件名；③消费＝bstrict／`bench.sh`。**根修（G.7 有则补全）**：perf 改薄 `exec` 权威 `run-perf-simd-shuffle-select.sh` · 删无引用 dual smoke／lib／tsv／r04 副本 · **不**改 129 名单 · **不**改 1.0 门槛。证：Ubuntu L2 xffle／ss gate OK shuffle=1 select=1 s4=1 · xffle perf 走权威 r04 ratio **2.3** · ss perf **2.08** · 矩阵 **5/5**。PLATFORM: SHARED 闸门 · LINUX gold · tip **`d734a6537`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **STD-061 splat+select compose** · ratio **2.42** · 矩阵 **5/5** | **代码+文档** · **不升钉**。peel 后热循环仍 U `splat_i32`×2＋`select_i32x8`（ratio 0.23）。五问：①产生＝`try_inline_select` 只认 IDENT、无 splat inline；②存储＝嵌套 CALL 无槽；③消费＝r04 `select(splat(1),r8,splat(0))`。**根修（G.7）**：权威 `pipeline_asm_simd_try_inline_select_call_elf_c` 补 fold `select(splat(k),a,b)`＋嵌套 splat 物化；**无才新增**同族 `try_inline_splat`（seed＋`.x` 同 commit）。**不**改 1.0 门槛。证：Ubuntu L2 splat_let **run=1／UNDEF=NONE** · compose **run=3** · mat **pcmpgtd×2／run=12** · r04 UNDEF **NONE** · pshufd×3 · ratio **2.42** 硬 OK · ss／xffle gate OK · 矩阵 **5/5**。PLATFORM: SHARED emit · LINUX gold · tip **`c990cbfba`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **STD-061 peel 跨 ancestor** · shuffle `pshufd` · 矩阵 **5/5** | **代码+文档** · **不升钉**。热循环 0.17×：同块 mask 能 `pshufd`，while 内 `simd.shuffle(v, m)` 仍 U `std_simd_shuffle_*`。五问：①产生＝`glue_peel_comptime_array_lit_mask_c` 只扫当前 while 体 lets；②存储＝typeck 已写 `parent_block_ref`；③消费＝shuffle inline／r04 热循环。**根修（G.7 有则补全）**：peel 沿已有 `pipeline_block_parent_block_ref_at` 上走（影子非 ARRAY_LIT 停）；seed＋`.x` 同 commit。**不**改 1.0 门槛。证：Ubuntu L2 while **pshufd／UNDEF=NONE／run=4** · bench shuffle UNDEF 清 · 余 U `splat`＋`select_i32x8` · ratio **0.23** 软 SKIP · roundtrip／s2 **0** · ss／xffle gate OK · 矩阵 **5/5**。PLATFORM: SHARED emit · LINUX gold · tip **`63bdffe3c`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **STD-047 shuffle/select 名权威** · xlangffle 薄包装 · 矩阵 **5/5** | **代码+文档** · **不升钉**。产品 export 已是 `shuffle`／`select`／`select_lane`；活 RFC 与 129 `xlangffle-select-gate` 仍写 `vec4f_shuffle` 并硬跑暂停的 `xlang check`。五问：①产生＝产品 overload `shuffle`／`select`／`select_lane`；②存储＝活 RFC＋xlangffle tsv／双体 gate；③消费＝129 白名单。**根修（G.7 有则补全）**：活文档对齐产品名 · 权威 gate 跳过 check · 129 脚本改薄 `exec` · **不**加 `vec4f_shuffle` 第二套。证：Ubuntu L2 **xffle／ss gate OK** shuffle=1 select=1 s4=1 · roundtrip／s2 **run=0** · 矩阵 **5/5**。PLATFORM: SHARED 文档／闸门 · LINUX gold · tip **`99b94ee26`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **STD-SIMD-INTRINSIC／STD-061 名权威** · 活文档 · 矩阵 **5/5** | **代码+文档** · **不升钉**。产品烟测已绿；闸门首红缺活 RFC，manifest 仍写 `vec4f_mul`／`bench/simd_shuffle_select.x`。五问：①产生＝产品 overload `mul`／`dot`／`fma` 与 `r04_` bench；②存储＝归档 RFC＋tsv＋README＋命名规范；③消费＝intrinsic／prod gate。**根修（G.7 有则补全）**：恢复活文档 · 消费端对齐产品名／r04 路径 · **不**加 `vec4f_mul` 第二套 · gate 跳过暂停的 `xlang check`。证：Ubuntu L2 **intrinsic gate OK** x=1 · **prod gate OK** benches=3（perf ratio 0.15 软 SKIP）· s2／roundtrip／autovec **run=0** · 矩阵 **5/5**。PLATFORM: SHARED 文档／闸门 · LINUX gold · tip **`2077ec459`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **STD-153 `recommend_path` 名权威** · 活文档 · 矩阵 **5/5** | **代码+文档** · **不升钉**。formal hw/path 已绿；文档／README／autovec manifest／gate 仍写 `recommend_simd_path`（活 RFC 已归档）。五问：①产生＝产品 export `recommend_path`；②存储＝gate 关键字＋manifest api 行＋归档文档；③消费＝STD-153 gate 首红缺文档／缺旧名。**根修（G.7 有则补全）**：恢复 `analysis/std-simd-autovec-strategy-v1.md` · 消费端对齐 `recommend_path` · **不**加第二套 export。证：Ubuntu L2 **symbols_miss=0** · s2／autovec／roundtrip／intrinsic **run=0** · default **11** · hw0=10 · 矩阵 **5/5**。PLATFORM: SHARED 文档／闸门 · LINUX gold · tip **`03ac69564`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **formal STD-153 hw/path** · default **11** · 矩阵 **5/5** | **代码+文档** · **不升钉**。s2 真链后 SIMD 烟测全绿；产品 `import std.simd` 的 `hw_available`／`recommend_path` 仍 formal 固定 0（与 `simd.x`／STD-153 漂）。五问：①产生＝c_face stub；②存储＝`std/simd/simd.o`；③消费＝autovec／策略查询。**根修（G.7 有则补全）**：同一 formal 面 hw=1 · recommend 认 `XLANG_SIMD_HW=0`／`AUTovec=scalar` 或 `0`。证：Ubuntu L2 **default=11** · hw0=10 · aut_scalar=10 · hw0+aut_hw=10 · autovec=0 · s2／s3 STRICT／s4 STRICT · 矩阵 **5/5**。PLATFORM: SHARED formal · LINUX gold · tip **`7702cd73a`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **s2 闸门真链** · exe＋run=0 · 矩阵 **5/5** | **代码+文档** · **不升钉**。上波封 UNDEF 后闸门仍 `-o .o` 不走 ld。soft 129 首红 `run-check.sh`／`fmt`（空换行 exit1＝暂停 check 族，非本刀）。五问：①产生＝闸门 compile-only；②存储＝`run-simd-s2-gate.sh`；③消费＝p5／预推。**根修（G.7 有则补全）**：权威闸门改 `-L . -o exe`＋run=0。证：Ubuntu L2 **s2 gate OK run=0** · s3 STRICT **全闸 OK** · s4 **4/4** · autovec／intrinsic **run=0** · 矩阵 **5/5**。PLATFORM: SHARED harness · LINUX gold · tip **`86ad6de1b`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **s2 真链 0** · formal placeholder／hw／path · g18 **16→21** · 矩阵 **5/5** | **代码+文档** · **不升钉**。扫红 `vec4f_vec8i` U `std_simd_placeholder`（s2 只编 `.o` 假绿）。五问：①产生＝import METHOD mid；②存储＝formal 缺标量面、g18 count 仍 16；③消费＝ld。sole `hw_available` 仍 U（针在表、count 不扫）。**根修（G.7 有则补全）**：formal 补 placeholder／hw_available／recommend_path／SIMD_PATH_* · g18 针＋count 21。证：Ubuntu L2 **vec4f_vec8i／autovec／intrinsic run=0** · s3 STRICT **全闸 OK** · s4 **4/4** · 矩阵 **5/5**。PLATFORM: SHARED formal+labi · LINUX gold · tip **`59ea41803`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **intrinsic_dot 0** · formal mul/dot/madd · FLOAT_LIT 打分 · 矩阵 **5/5** | **代码+文档** · **不升钉**。扫红 `intrinsic_binop_dot` U `std_simd_mul_*`／`dot`／`sub_i32x8_i32x8`／`madd`。五问：①产生＝import METHOD mid；②存储＝formal 仅 shuffle/select/splat；③消费＝ld。补 formal 后面 SEGV：`splat(0.0)` 两 overload 均 −1 → first_idx `splat_i32`＋rdi=0。**根修（G.7 有则补全）**：formal 补 add/sub/mul/hsum/dot/fma/madd · g18 针 6→16 · `typeck_overload_arg_param_score` FLOAT_LIT 弱配 f32/f64 · emit 重打分仅 sc≥11（≡seed）。证：Ubuntu L2 **run=0** · 调 `splat_f32`／`sub_f32x4` · s3 STRICT **全闸 OK** · s4 **4/4** · 矩阵 **5/5**。PLATFORM: SHARED formal+typeck · LINUX gold · tip **`872129cd5`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **i32 strip n=20** · xmm1 dest=ModRM · elem0=base · s3 **跑值绿** · 矩阵 **5/5** | **代码+文档** · **不升钉**。s3 对整数 peel／strip 只验 insn 不跑值。扫红 `vec8i_loop_strip_var_n` n=20 SIGILL。五问：①产生＝`simd_x86_movups_xmm1_from_rbx_rax4` 字节 `C5 F0 10 04`（dest 写进 VEX.vvvv，vmovups 是二元故非法）；②存储＝`.x`+seed `_impl`+surface；③消费＝strip at_idx lanes=4。同族 ymm at_idx 误用 0F38。修后仍 exit=0：at_idx `off-(n-1)*esz` 把基址当末元（peel 已认 off=elem0）。**根修（G.7）**：xmm1 `C5 F8 10 0C` · ymm map=0F · elem0=off。s3 补 peel／strip 跑值。证：Ubuntu L2 store **vmovups xmm1** · strip **6** · peel 99／11／12／3 · s3 STRICT **全闸 OK** · s4 **4/4** · 矩阵 **5/5**。残：`intrinsic_binop_dot` UNDEF `std_simd_*`。PLATFORM: LINUX+MACOS x86 AVX VEX · LINUX gold · tip **`f157cea4a`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **f32 SoA strip 10** · store_eax disp32 u32 · s3 **全闸绿** · 矩阵 **5/5** | **代码+文档** · **不升钉**。s3 全闸死 strip expect10 got8。五问：①产生＝thin `backend_enc_store_eax_to_rbp_arch` 对 `disp=-offset` 做有符号 `/256`（−224 向零截成高字节 0 → `+0x20`）；②存储＝产品 PREFER thin `.x`（seed C 已是 `uint32_t`）；③消费＝peel movss 写 `s`@−0xe0，epilogue assign／let 走 store_eax 写到 `rbp+0x20`，return 仍读 −0xe0。var_n=12 假绿因 12%4=0 不进尾。**根修（G.7 有则补全）**：thin `.x` 对齐 seed／monofile，`disp as u32` 拆 LE 字节。证：Ubuntu L2 store **−0xe0** · strip **10** · var_n **12** · peel **8** · s3 STRICT **全闸 OK** · s4 **4/4** · 矩阵 **5/5** `/tmp/ubu_s3_strip_b76e8317d.log`。PLATFORM: LINUX+MACOS x86_64 SysV · LINUX gold · tip **`b76e8317d`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **s3 fma packed** · vfmadd231ps B8/C2 · 矩阵 **5/5** | **代码+文档** · **不升钉**。上波误判「体内联成 vfmadd213ss」。五问：①产生＝`simd_x86_vfmadd231ps_xmm0_xmm1_xmm2` 字节 `C4 E2 71 A9 C1`（A9=213ss · C1=rm xmm1）；②存储＝`.x`+seed `_impl`+surface 三孪生；③消费＝try_inline_fma3 已命中 METHOD＋三路 movups，gate 认 `*ps`。**根修（G.7）**：权威改 `C4 E2 71 B8 C2`（VEX.128.66.0F38.W0 B8／r · rm=xmm2）。证：Ubuntu L2 **vfmadd231ps** · fma exe **0** · s3 STRICT **fma insn present**（全闸仍死 strip 8≠10）· s4 STRICT **4/4** · 矩阵 **5/5** `/tmp/ubu_s3_fma_packed_b10c9d586.log`。残：f32 SoA strip expect10 got8。PLATFORM: LINUX+MACOS x86 FMA3 · LINUX gold · tip **`b10c9d586`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **s3 var+var HW** · paddd／psubd／pmulld SSE2 双半 · 矩阵 **5/5** | **代码+文档** · **不升钉**。s3 非严格 WARN：Vec8i `a+b`／`a-b`／`a*b` 无 paddd／psubd／pmulld。五问：①产生＝`simd_enc_try_hw_vector_iadd_isub`／`imul` 仅 AVX2 ymm（lanes==8）；②存储＝pending 常 SSE2-only 跳过 detect_host（≡s4 pshufd）；③消费＝s3 objdump。**根修（G.7）**：权威补 SSE2 双半 128-bit paddd／psubd／pmulld（`.x`+seed `_impl`+surface 同 commit）。顺修 METHOD `simd.fma` 认 49＋bare `fma`（烟测仍走 std.simd 标量体 → `vfmadd213ss`，packed 未结）。证：Ubuntu L2 **add／sub／mul insn present** · s4 STRICT **4/4** · 矩阵 **5/5** `/tmp/ubu_s3_strict_883af6ac3.log`。残：fma packed · f32 SoA strip expect10 got8。PLATFORM: SHARED emit · LINUX gold · tip **`883af6ac3`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **s4 HW insn STRICT** · pshufd／pcmpgtd／cmpnleps · 矩阵 **5/5** | **代码+文档** · **不升钉**。s4 非严格 WARN：`simd.shuffle(v,m)` 发外部 CALL 无 pshufd。五问：①产生＝try_inline 只认 CALL(48)＋裸 `simd_shuffle`＋ARRAY_LIT mask；②存储＝METHOD 名 `shuffle`／mask 为同块 let；③消费＝s4 objdump。**根修（G.7）**：try_inline 认 METHOD(49)+bare shuffle/select＋peel 同块 ARRAY_LIT；hybrid 删 dual export extern（禁同参 mid `_reti32` U）；simd_enc Vec8i SSE2 双半 pshufd／select；cmpgtps 编码 `0F C2 D3 06`（旧 `0F 55` 是 ANDNPS）；gate 认 objdump `cmpnleps`。证：Ubuntu L2 **s4 STRICT 4/4** · 矩阵 **5/5** · soft **129 OK** `/tmp/ubu_s4_strict_144118847.log` `/tmp/ubu_soft_bstrict_144118847.log`。PLATFORM: SHARED emit · LINUX gold · tip **`144118847`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** **@shuffle／@select** body-let + typeck · **s4=1** · soft **129 OK** | **代码+文档** · **不升钉**。s4 residual：`at_builtin_smoke` P001 no functions。五问：①产生＝`parse_body_lets_into` 初值只路由 IDENT／SELF／LBRACKET／… **无 TOKEN_AT(129)** → init_handled=0 整函数软丢；②存储＝primary 已有 `@`→`parse_at_simd_builtin_into`→CALL `simd_shuffle`／`simd_select`；③消费＝typeck wave675 把 bare 名当 unresolved T001。**根修（G.7）**：body_lets 与 IDENT 同径路由 129 · `pipeline_typeck_is_simd_comptime_callee_c` fi-free + 返回类型取向量实参 · seed parser／typeck 孪生。证：Ubuntu **at_builtin.o** · s4 gate OK **s4=1** · 矩阵 **5/5** · soft **129 OK** `/tmp/ubu_soft_bstrict_68713f6a5.log`。残：s4 HW insn 非严格 WARN（无 pshufd 时仍 OK）· tip L4 未本波重跑。PLATFORM: SHARED · tip **`68713f6a5`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **Vec4f／TYPE_VECTOR ABI** · **roundtrip 0** · **soft 129 OK** | **代码+文档** · **不升钉**。soft 内 WARN：`shuffle_select_roundtrip` exit1。五问：①产生＝`glue_type_size_simple` 把 TYPE_VECTOR 当标量 **4**＋vector_let／flat FLOAT_LIT 走 f64 movabs 截低 32（0.0f）＋METHOD 回退只 store_rax；②存储＝formal `struct{float[4]}` 是 SSE xmm0+xmm1 与 pure dual-GP 冲突；③消费＝roundtrip／select_i32x8 SEGV。**根修（G.7）**：size=lanes×esz · array_lit peel VECTOR · typeck 向量 lane float coerce · force_esz scalar_elem · formal f32x4=uint64 lo/hi INTEGER dual-GP · SIMD let 复用 struct CALL sret／dual-GP · gate `select_lane`。证：Ubuntu L2 **idx／shuffle／select／roundtrip=0** · 矩阵 **5/5** · soft **129 OK** `/tmp/ubu_soft_bstrict_39aad13c1.log`（s4 at_builtin 仍 WARN residual）。残：@shuffle parse · tip L4 未本波重跑。PLATFORM: SHARED · tip **`39aad13c1`** · **日常 L2 非 tip L4** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** seed **simple-group g0..g19 ≡ pure** · **Ubuntu tip L4 129 OK** | **代码+文档** · **不升钉**。tip L4@`329e998e9` step8 首红 `run-compress`：U `std_compress_*`。五问：①产生＝产品 `labi_od_simple_group_count` seed **return 13**（`mov $0xd`；g12=simd）；②存储＝pure `.x` 已 **return 20**（g12=test…g15=compress…g18=simd g19=io）· prefer 超时回退冷 seed；③消费＝L4 擦 formal .o 后 g15 永不 ensure/push。**根修（G.7 冷孪生）**：`labi_ondemand_list.from_x.c` 表 ≡ pure · surface +g18/g19 · heavy_surface generic ensure-by-rel。Ubuntu L2：count **0x14** · wipe 后 **run-compress OK** · soft **129 OK** `/tmp/ubu_soft_bstrict_3c75e615b.log` · **tip L4 OK** wall=24m48s remaining_o=0 矩阵 5/5 bstrict 129 `/tmp/ubuntu_true_cold_l4_3c75e615b.log`（冷链 count 仍 **0x14**）。PLATFORM: SHARED · 构建级别 **L4** · tip **`3c75e615b`** · 钉盘仍 **`e364f4a37`**。 |
| 2026-08-13 | **fix(selfhost)** std.io **ctx-timeout formal** + **context companions** · soft 全绿 | **代码+文档** · **不升钉**。soft 首红 `run-std-io-context`：U `std_io_*_ctx`。**根修**：`std/io/formal_surface.c` c_face · g19 needles · heavy ensure + companions ≡ C need_context。soft@`329e998e9` **129 OK**（热残 .o 假绿；真冷见上条）。PLATFORM: SHARED · tip **`329e998e9`** · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **TYPE_VECTOR mid** + **std.simd formal** · perf-simd 链闭 | **代码+文档** · **不升钉**。soft @`8e88e90fb` 过 i64-ctfe 后首红 `run-perf-simd-xlangffle-select`：U `std_simd_shuffle_i32_a4` 等。五问：①产生＝import METHOD mid；②存储＝`glue_asm_type_ref_to_suffix` 缺 TYPE_VECTOR→跳过 Vec 参只剩 `_a4`；③消费＝无 formal simd.o。**根修（G.7 call_dispatch+formal）**：suffix `f32x4`／`i32x8`（≡codegen）· `std/simd/formal_surface.c` · simple-group／ensure by rel。Ubuntu：mangle 对齐 formal T · gate exit0（ratio soft）。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **i64 SUB** · **禁 cdqe 抹高位** · **run-i64-ctfe-gate 绿** | **代码+文档** · **不升钉**。soft @`a08cc452a` 过 lang-unsafe 后首红 `run-i64-ctfe-gate` exit1（expect42）。五问：①产生＝`0-INT64_MAX-1`；②存储＝pure `glue_binop_maybe_sxt` 在 INT/I32 侧后 **cdqe** 把 `0x8000…0001` 抹成 1、第二步 `subl` 32 位；③消费＝`zero==i64_min` 假真。**根修（G.7 pipeline_abi）**：`is_64bit` 认宽 lit＋嵌套 binop；64-bit 走 `subq`／禁 sxt；seed 冷孪同 commit。Ubuntu：main 无 cltq · **gate OK** · hello／div_ok。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **lang-unsafe gate** · **host-cc-requires-allow 闭** · **Ubuntu gate OK** | **代码+文档** · **不升钉**。soft @`9d08ed74d` 过 set／fmt 后首红 `run-lang-unsafe-gate`：4×`policy=run` 全 `host-cc-requires-allow`。五问：①产生＝`run_x_case` 硬编 `-backend c`；②存储＝PC `ALLOW_HOST_CC` 默认拒 spawn；③消费＝bstrict 产品 xlang 无 silent host-cc → BLD001。**根修（G.7 测试宿主 ≡ void-main／defer／safe-ffi）**：`run_x_case` 优先 pure-asm `-L . -o`，仅 `XLANG_ALLOW_HOST_CC` 走 `-backend c`。证：Ubuntu **lang-unsafe gate OK**（U1–U4 run + compile_fail + hooks + G-FFI-5）。日志 `/tmp/ubu_lang_unsafe_pure.log`。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** fmt formal **null `0x0`** · **run-fmt 绿** | **代码+文档** · **不升钉**。soft @`f795a29c7` 过 set／map／queue／vec 后首红 `run-fmt` exit20（`ptr_to_buf` null len≠3）。五问：①产生＝formal_surface `snprintf("%p",NULL)`；②存储＝glibc 写 **`(nil)` len5**（mac 常 `0x0` len3）；③消费＝tests/fmt 与 format(*u8,i32) 契约要 **`0x0` len3**。**根修（G.7 formal face）**：null 固定写 `0x0`，非空仍 `%p`。Ubuntu：**run-fmt OK** · soft 续扫。PLATFORM: SHARED formal · LINUX glibc · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure mid **同参 `_ret_<T>`** · **run-set 绿** | **代码+文档** · **不升钉**。soft @`d0b3d73b9` 过 tar／string／http 后首红 `run-set`：U `std_set_new_i32`（formal T `…_retSet_i32`）。五问：①产生＝pure `glue_asm_build_func_overload_mid_c` 只拼参后缀，**缺 seed 已有的同参签名 `_ret_<T>`**；②存储＝set.new(i32)→Set_i32／Set_u64 同参双 overload；③消费＝needs_std_set 只认 formal 长名 → 不 push 或链上仍 U。**根修（G.7 pure≡seed）**：补 `glue_asm_overload_param_sig_count_c` + mid 同参时 append `_ret_`。Ubuntu：seed host-cc call_dispatch（pure_asm 大叶超时回退）· g05 · user U=`std_set_new_i32_retSet_i32` · **run-set／map／queue／vec／string／import／result／mem／tar OK** · 矩阵 **5/5** · soft 续扫。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** fk0 冷孪生 **+tar／unicode／runtime** · **run-tar 绿** | **代码+文档** · **不升钉**。soft @`bbb6646d0` 过 ~76 脚本后首红 `run-tar`：UNDEF `std_tar_{read,write}_header`（`tar.o` 已有 T 面）。五问：①产生＝product `labi_fk0_rel_count` 仍 **return 16**（`mov $0x10` 冷 seed 形）；②存储＝ac064cc62／2d8b92871 只同步 pure heavy+surface，**list.from_x.c 冷孪生滞后**；L8b+L8c prefer 失败时 full L8b seed 盖住 heavy；③消费＝gate 永不扫 k16 → 不 push `std/tar/tar.o`。**根修（G.7 双权威）**：冷孪生 ≡ heavy return **19** + k16／17／18 全针。Ubuntu：force try-labi-prefer · product **0x13** · **run-tar／unicode／runtime／string／time／builtin／mem／http／channel OK** · soft 续扫。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** x86 pure-asm prologue **SysV 16B stack align** · **run-time 绿** | **代码** · **不升钉**。soft @`6ad7a0fec` 过 ~68 脚本后首红 `run-time`：`format_timezone` exit139 SEGV in glibc `mktime`→`tzset`→`sscanf`（`main`/`bench_timer` 绿）。五问：①产生＝`arch_x86_64_enc_enc_prologue` 恒 `push rbp; push rbx; sub frame`；②存储＝`compute_frame_size` 把 frame 凑成 **≡0 mod 16**；③消费＝entry RSP≡8 → 两 push 后 ≡8 → sub≡0 后 CALL 前仍 **≡8**（ABI 要 ≡0）→ SSE 路径 SEGV。**根修（G.7 enc_prologue）**：pad `sub imm` 至 **≡8 mod 16**（locals 仍 rbp 相对；epilogue lea 无关）；`.x`+seed+surface 同 commit @**`bd7876d10`**。Ubuntu：`sub $0x268` · tz_exit=0 · **run-time OK** · string/builtin/mem · 矩阵 **5/5** · soft 续扫。PLATFORM: SHARED x86_64 SysV · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** formal_mod **C23 unreachable** · **run-builtin 绿** | **代码** · **不升钉**。soft @`98e5b0779` 过 string 后首红 `run-builtin`：UNDEF 全族 `core_builtin_*`。五问：①产生＝formal ensure `core/builtin/mod.x`；②存储＝GCC15 C23 `stddef` `#define unreachable()`；③消费＝`void unreachable(void)` 被宏展开 → cc -c 失败 · .o 不落地 · g10 推空。**根修（G.7 formal_mod clash）**：pre-cc +`unreachable` rename；post-o `prod_pref` 映 `core_builtin_*`（禁硬编 std_）。Ubuntu：**run-builtin OK** · soft 续扫。PLATFORM: LINUX formal · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** x86 import METHOD **>16B SysV MEMORY stack** · **run-string 绿** | **代码** · **不升钉**。soft @`b71f99146` 过 ~61 脚本后首红 `run-string`：`string_empty` exit5（`length(s1)!=1`）。五问：①产生＝import METHOD 对 >16B 一律 lea→GP（mac arm64 残修，误标 SHARED）；②存储＝host-C `string.o` gcc `length(String)` 为 SysV MEMORY **栈**传（[rbp+0x10..]）；③消费＝pure lea→rdi，callee 读栈垃圾 → length=0（`string_len_ptr`=1 证 from_slice 写对）。**根修（G.7 call_dispatch）**：x86 `is_mem=1` + `push_sysv_memory` 多字；arm64 仍 `is_mem=2` lea；pure `.x`+seed 同 commit @**`98e5b0779`**。Ubuntu：len=1 · hello/slice/vector/ub/string/mem 邻域绿 · soft 全量续扫。**效率**（用户问）：禁止 `run-hello`→`run-slice` 串行；默认 ①L2 邻域 ②`run-all-bstrict SKIP_BUILD=1 JOBS=1` 批量 ③首红定点 ④类扫 ⑤JOBS 仅 opt-in ⑥L4 升钉。PLATFORM: LINUX\|x86 stack · MACOS\|ARM64 lea · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** idiv **slot-cache invalidate** · bounds_slice 真 slice · vector／ub pure-asm | **代码+文档** · **不升钉**。soft @`f7a65c8b3` 首红 `run-vector` silent `-backend c`；类扫白名单仅 vector+ub 仍强绑 host-cc → **`a014d2c21`** pure-asm 默认。续：ub `bounds_slice` 假测固定数组动态下标（Stage12.0.5 故意无 guard）→ 改真 `i32[]`；`div_ok` `a/b+a%b`=4 根因 idiv 后 slot-cache 仍认 rax=a → 右侧 `%` 不重载。**根修**：div／mod 后 invalidate rax+rbx（`.x`+seed 同 commit）@**`b71f99146`**。Ubuntu L2：div+mod=3／ub=0／vector=0／矩阵 **5/5** · soft 续扫。效率：类修 harness + soft 全量，禁逐脚本。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **import METHOD sret GP shift** · slice split_at SEGV | **代码+文档** · **不升钉**。Ubuntu soft 过 hello 后 `run-slice` `subslice_split_chunks` run=139。五问：①产生＝pure `pipeline_asm_emit_method_call_elf_c` import 路径 `gp_cur_m=0` 忽略 `call_sret_reg_shift`；②存储＝let `Split_i32` 已 lea sret→rdi + shift=1；③消费＝args 覆写 rdi，host `core_slice_split_at_i32(sret,slice*,at)` 把 at 当指针 SEGV。**根修（G.7 ≡ free CALL／seed）**：`gp_cur_m = sret_sh`（ta==0）；`run-slice` pure-asm 默认。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **run-hello** · 禁 silent `-backend c` · host-cc-requires-allow 闭 | **代码+文档** · **不升钉**。Ubuntu soft @ **`2c4bcfdcb`** 首红 `run-hello`：`hello compile failed (no executable)`。五问：①产生＝`run-hello` 在 `XLANG_LINK_XLANG` 下硬绑 `HELLO_BACKEND=-backend c`；②存储＝PC `ALLOW_HOST_CC` 默认拒 spawn；③消费＝bstrict 导出 LINK_XLANG 触发 silent host-cc → BLD001。**根修（G.7 测试宿主 ≡ void-main／option／defer）**：产品 xlang／xlang_asm 默认 pure-asm `-o`；仅 `ALLOW_HOST_CC=1`／`FORCE_LINK_BACKEND` 才 `-backend c`；`pipefail` + 唯一 `HELLO_OUT`。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **verify(selfhost)** enc_label Ubuntu L2 · overload=0／矩阵 **5/5** · soft bstrict 启 | **文档** · **不升钉**。tip **`2c4bcfdcb`** · host-cc enc 链已入 `xlang_asm` · `tests/types/overload` compile=0 run=0 · `./xbuild l2-matrix` **5/5** · soft 日志 `/tmp/ubu_soft_bstrict_2c4bcfdcb.log`（JOBS=2 假红后 JOBS=1 仍红 hello → 上波根修）。PLATFORM: LINUX gold · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** x86 **enc_label** pad／code_len pass0 hoist · multi-func SEGV | **代码+文档** · **不升钉**。L4 @ **`d0203b9dc`**：矩阵 **5/5**（jcc 真冷证）· step8 bstrict `types/overload` run=139。五问：①产生＝`arch_x86_64_enc_enc_label` 同块 `let code_len=emit_code_len()` 被 pure-asm pass0 提升到 `pad_code_to_4` **前**；②存储＝sym／label 指零 pad；③消费＝`pick_i64`／`main` 入口 `00 00 00` 再 `55…` → 假 prologue／坏 rbp SEGV。**根修（G.7 enc 权威 ≡ jcc）**：pad 与 emit_code_len **分两块**；`.x`+surface+rest seed 同 commit；Mach-O 名 cap 63→127 对齐 surface。PLATFORM: LINUX gold · SHARED enc · Ubuntu L2 已证。 |
| 2026-08-13 | **fix(selfhost)** x86 **jcc_rel32** pass0 hoist · Ubuntu option／si 绿 · L2 矩阵 **5/5** · **L4 矩阵 5/5** | **代码+文档** · **不升钉**。Ubuntu L4 矩阵 opt SIGILL(132)／si SEGV(139)：`main` 流在 `and $0xff`／setz 后被 patch 写穿。五问：①产生＝`x86_enc_jcc_rel32` 同块 `let rel32_at = emit_code_len()-4`（BINOP+嵌套 CALL）被 pass0 提升到 append **前**；②存储＝patch 写到 and／setz 中部；③消费＝jcc displ 仍 0 → 非法指令／空指。**根修（G.7 enc 权威）**：append 与 rel32_at **分两块**（同 jmp／call 形）；`.x`+surface seed 同 commit。证：host-cc seed · L2 **5/5** · **L4 真冷矩阵 5/5**（rv42／opt102／hello0／si0／f32；step8 另红 overload→enc_label）。PLATFORM: LINUX gold · SHARED enc。 |
| 2026-08-13 | **fix(selfhost)** Ubuntu L4 nostdlib **ftruncate／msync** · stubs 补 syscall 面 | **代码+文档** · **不升钉**。mangle 修后 g05 pure-ld 仍 U：PREFER `driver_x` 共编 `linux_mmap_rw`／`linux_msync_sync`（hosted），g05 nostdlib 去 `-lc`。**根修（G.7 stubs 权威）**：`bootstrap_nostdlib_stubs` +SYS_ftruncate=77／SYS_msync=26。PLATFORM: LINUX · **下波 Ubuntu L4 全绿**。 |
| 2026-08-13 | **fix(selfhost)** pure PREFER **mangle 双名** · `glue_asm_mangle_import_binding_call_sym` · **mac PREFER+L2 5/5** | **代码+文档** · **不升钉**。Ubuntu L4 phase1 pure-ld U `…_u8_ptr_…_reti32`：PREFER thin+rest 定义 `#[no_mangle]` 短名 T、export extern 调用发 mangle U。五问：①产生＝pure-asm 对 export extern 带类型后缀 mangle；②存储＝body `#[no_mangle]` 短面；③消费＝Ubuntu pure-ld 全符号。**根修**：去掉 body `#[no_mangle]`，定义与调用同 mangle 面；seed 仍 static 短名（seed-only 回退）。证：`XLANG_G05_PREFER_X_O=1` prefer T mangled、无 U · 矩阵 **5/5** · stdtest／time／option／string。PLATFORM: SHARED · **下波 Ubuntu L4**。 |
| 2026-08-13 | **fix(selfhost)** soft residual **类批 3** · **stdtest／time 绿** · **mac L2 5/5** | **代码+文档** · **不升钉**。①stdtest：formal test.o 已链，残 monofile C dual U `test_call_i32_void_c`／`env_getenv_c`／`time_now_monotonic_ns_c` → 薄 helper `labi_od_push_test_monofile_companions`（fn_invoke+env_os+time_os；need_test 与 g12 双挂，dedup 安全）。②time：ASLR SEGV 在 `append_on_demand` mega 帧 spill 把 `trel` 误读为 `0x100000000` → push_obj `ldrb` 崩；薄 helper `labi_od_push_time_formal_and_os` 小帧权威。seed heavy_surface 同 commit。证：**run-stdtest／run-time OK** · 邻域 debug／…／http／queue／string · 矩阵 **5/5**。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** soft residual **类批 2** · **debug／runtime／backtrace／fmt／compress／io-driver 绿** · **mac L2 5/5** | **代码+文档** · **不升钉**。方法续类批：①地图 RED＝assert／runtime／backtrace／fmt／compress／io-driver／stdtest。②**formal 车**：assert／backtrace 走 mod.x；runtime／fmt／compress／io.driver／debug 走 **c_face**（monofile 共编撞 zlib deflate／io 前缀漂移／panic U）。③**fk0 k18** runtime；**simple-group g13–g17** assert／fmt／compress／io.driver／debug + heavy ensure；seed surface 同 commit。证：**run-debug／runtime／backtrace／fmt／fmt-std／compress／io-driver OK** · 邻域 tar／unicode／channel／http · 矩阵 **5/5**。残：stdtest SEGV · time SEGV。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** soft residual **类批根修** · **tar／unicode／channel／http 绿** · **mac L2 5/5** | **代码+文档** · **不升钉**。用户改效率：禁止一脚本一波。方法＝①串行地图 28 脚本 → **13 RED／15 GREEN**；②按根因类归并。**类 A fk0 缺表**（flag_kind=0 有 plan 无 fk0）：+k16 tar（7 针）+k17 unicode（6 针）。**类 B formal 缺 catalog**：unicode／channel 入 formal_mod（41 叶）。**类 C companion**：http ≡ fs／net → error／context+atomic／time_os。**类 D fk7 针不全**：channel 扩至 10 exact。**类 E test 针**：std_test_* + monofile dual + g12 simple-group（stdtest 仍 SEGV／链，未结）。证：**run-tar／unicode／channel／http OK** · queue／process 邻域 · 矩阵 **5/5**。残：fmt／compress（无单 .o 车）· debug／assert · io-driver · runtime · backtrace · stdtest · time SEGV。日志 `/tmp/xlang_soft_map_seq/summary.txt` · `/tmp/mac_labi_final.log`。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **run-queue 绿** · monofile **sync／atomic／contention** companion · **mac L2 5/5** | **代码+文档** · **不升钉**。soft 过 process 后首红 `run-queue`：UNDEF `_std_sync_*`／`_std_atomic_store_i32_ptr_i32`／`_sync_queue_contention_smoke_c`。五问：①产生＝formal `queue.o` monofile 共编 SyncQueue 本地体（sync_new／try_pop／smoke）U 上述面；②存储＝ondemand 仅推 queue+heap+mem，contention 仅 `need_qc`（user 无 contention 针）且无 sync／atomic；user.o 扫不到 monofile 内 U；③消费＝整 .o 入链拉齐 monofile U（非 ar dead 成员）。**根修（G.7 ≡ net companion）**：①always ensure+push contention；②formal ensure+push sync.o／atomic.o；③SYNC_PAIR glue（lock_diag_tls+sync_os）+ flags[3]=have_sync；④atomic_glue；⑤rel helpers + seed surface 同 commit。证前须 force 重建 formal queue（export 滤 core_mem 为 local，消 mem.o multidef；旧 .o 为 08-12 未滤）。证：**run-queue OK** · process／env／path／set／sync／atomic／vec · 矩阵 **5/5**。日志 `/tmp/mac_run_queue_fix3.log` · `/tmp/mac_l2_matrix_queue.log`。下一残：fmt／unicode／compress。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **docs(日切)** **12 号归档 + 13 号主文档重写** | **文档** · **不升钉**。①`analysis/自举进度-归档-2026-08-12.md`：§6 收 12 日 **47** 行（prefer 默认 pure-asm · PC FORBID／ALLOW · mega ban／RCA · opaque WEAK · catalog cache · L4 墙钟）。②`analysis/自举进度.md` 日切重写：KPI／前排保留 · §6 **仅 13 日** soft bstrict pure-asm residual 梯（string→…→**run-process** · tip **`44d4d1655`**）。钉盘仍 **`e364f4a37`**。下一刀：soft 下一红／L4。PLATFORM: SHARED 文档 · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **run-process 绿** · process_merge **import_alias** + **fk1 28 精确针** · **mac L2 5/5** | **代码+文档** · **不升钉**。soft 过 run-env 后首红 `run-process`：UNDEF `_std_process_{arg,args_count,getenv,…}`。五问：①产生＝pure-asm import METHOD 发 `std_process_*`；②存储＝process_merge 仅 bare `process_*_c`（args_thin+argv+os_glue），C 轨靠 co-emit mod.x 补面；③消费＝fk1 仅 4 针且 `std_process_args` **非** exact match `args_count`（Darwin nm -u 无 type letter U → 无 substring 回退）→ 不推 process.o；process_argv 表误含 `std_process_*` 会开错 .o。**根修（G.7 process_merge + labi fk1）**：①`runtime_process_import_alias` 1:1 产品面（含 `_exit`）；②process_merge ld -r 并入 alias；③fk1→28 exact 针全表面；④process_argv 去掉 std_process_*（仅 argv glue + env args_iter）。证：**run-process OK (all)** · env／path · 矩阵 **5/5**。日志 `/tmp/mac_run_process_fix2.log` · `/tmp/mac_l2_matrix_process2.log`。下一残：fmt／queue／unicode。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **run-env 绿** · formal_mod **args_iter wrapper** + **process_xlang U** · **mac L2 5/5** | **代码+文档** · **不升钉**。soft 过 run-path 后首红 `run-env`：`env_iter` UNDEF `_std_env_args_iter{,_count,_next}`；修 wrapper 后链过但 exit1（a0==null）。五问：①产生＝Mac thin wrapper 误 blanket-skip `args_*`（仅应 skip co-emit `args_iter_*_c`）→ bare `args_iter` 无 `std_env_*`；②rt_preamble weak `process_xlang_*` 零桩被 Mac export 滤成本地 T，内部 args 链绑 return0；③ondemand complement 扫 U process_xlang 永不触发 → 不推 `runtime_process_argv.o`。**根修（G.7 formal_mod）**：①wrapper 只 skip `process_`（非 `args_`）；②弱零桩就地改 extern → .o 留 U；③env 面 underscore objcopy 补全。证：**run-env OK**（main／env_iter／platform_encoding）· path／hash · 矩阵 **5/5**。日志 `/tmp/mac_run_env_fix2.log` · `/tmp/mac_l2_matrix_env2.log`。下一残：process／fmt／queue／unicode。PLATFORM: MACOS formal + SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **run-path 绿** · fk0 **k==6 公共面补全** · **mac L2 5/5** | **代码+文档** · **不升钉**。soft 过 run-fs 后首红 `run-path`：`extension_stem_abs_clean` UNDEF `_std_path_{sep,is_absolute,extension,stem,clean}`（main／join 绿因 empty_len／join／basename 已在旧针）。五问：①产生＝fk0 k==6 仅 4 针 join／dirname／empty_len／basename；②存储＝exact match（rest==len），join 不盖 clean／sep；③消费＝sole-caller 用例永不 push `path.o`。**根修（G.7 labi fk0）**：k==6 扩至 12 精确针（+sep／is_absolute／is_sep／extension／stem／extension_and_stem／clean／resolve）。`.x`+list seed+heavy_surface 同 commit。证：**run-path OK** · fs／heap／io／string · 矩阵 **5/5**。日志 `/tmp/mac_run_path_fix.log` · `/tmp/mac_l2_matrix_path.log`。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **run-fs 绿** · fs companion **error/context** + **fk0 readv** + **formal export** · **mac L2 5/5** | **代码+文档** · **不升钉**。soft 过 run-heap 后首红 `run-fs`：UNDEF `_std_error_io_err_*`／`_std_context_*`（fs.o 传递）；再红 multidef core_result／main；再红 `std_fs_readv_buf` 无 gate。五问：①产生＝fs.o 共编 timeout/ctx IO；②存储＝fk0 推 fs 无 companion；formal vehicle KEEP_C 共编 core_result/io 为 global T；fk0 针仅 invalid/open/…/last_error；③消费＝user 只 U std_fs_*／readv_buf。**根修（G.7）**：①`labi_std_append_formal_ensure_for_rel` fs → ensure+push error/context + atomic/time_os（≡ net）；②`xlang_compile_std_fs_formal` 仅导出 `std_fs_*`（Mac export list／Linux localize）；③fk0 k==15 +2 针 readv_buf/writev_buf。`.x`+seed list/surface/heavy_surface 同 commit。证：**run-fs OK** · heap／net／io／set · 矩阵 **5/5**。下一残：path（fk0 针不全）。日志 `/tmp/mac_run_fs_fix3.log` · `/tmp/mac_l2_matrix_fs.log`。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **run-heap 绿** · formal **heap_trace_stats 别名** + **Mac ar export/flat** · **mac L2 5/5** | **代码+文档** · **不升钉**。soft 过 run-net 后首红 `run-heap`：UNDEF `_std_heap_libc_heap_trace_stats_c`（`heap.o` 内 mod U → bare T `heap_trace_stats_c` 无 namespaced 别名）。五问：①产生＝`xlang_compile_std_module` Mac thin alias 表 **case 空跳过**（误注「variadic」；实为固定 4×*u64）；②存储＝多文件 `ld -r` 失败→libtool ar，export 过滤整包 ar 失败 → co-emit `core_mem_*` 仍 global T；③消费＝product 推 heap+mem → multidef，且 `ld -r ar+alias` 会丢成员丢 `std_heap_*`。**根修（G.7 formal_mod 权威）**：①补 `heap_trace_stats_c` 4 指针 thin wrapper；②Mac ar **per-member** export 列表（Darwin 要求 list⊆本 .o）；③alias 后 ar→flat `ld -r` + 仅导出 `std_heap_*`（bare/local 化，消 `runtime_heap_user` multidef）。证：**run-heap OK** · set／map／io／net／string · 矩阵 **5/5**。下一残：fs／path（独立 UNDEF）。日志 `/tmp/mac_run_heap_fix5.log` · `/tmp/mac_l2_matrix_heap.log`。PLATFORM: MACOS formal + SHARED product · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **run-net 绿** · net companion **error/context + atomic/time_os** · **mac L2 5/5** | **代码+文档** · **不升钉**。soft 过 run-io 后首红 `run-net`：UNDEF `_std_error_{io,net}_err_*`／`_std_context_{deadline,is_cancelled,remaining}_ns`（thread_glue 已在）。五问：①产生＝`net.o` 共编 timeout/ctx IO 调 error/context；②存储＝ondemand `have_net` 只推 net+thread+udp/workers，**无** error/context；③消费＝fk0 k==8/9 只扫 **user.o**（main 仅 `net_tcp_listen_c`）→ 不 push；context 再 U `atomic_*_i32_c`／`time_now_monotonic_ns_c`。**根修（G.7 ≡ C need_context + encoding companion）**：`have_net` 后 formal ensure+push `error.o`／`context.o` + ensure+push `runtime_atomic_glue`／`runtime_time_os`；`.x` heavy + seed list + heavy_surface 同 commit。证：**run-net OK** · io／json／ffi／string · 矩阵 **5/5**。下一残：heap／fs／path（独立 UNDEF，非本波）。日志 `/tmp/mac_run_net_companion.log` · `/tmp/mac_l2_matrix_net_comp.log`。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **run-io 绿** · SLICE mid + println **len15** + io stubs · **mac L2 5/5** | **代码+文档** · **不升钉**。soft 过 safe-ffi 后首红 `run-io`：UNDEF `_std_fmt_println_i32_reti32` → 修 mid 后 `_std_fmt_println_i32` → print_str 截成 `_std_fmt_printl`。五问：①产生＝`glue_asm_type_ref_to_suffix` 无 TYPE_SLICE，u8[] 默认 i32 → 与 println(i32) 假碰撞挂 `_reti32`；②`pipeline_asm_redirect` 硬编 `std_fmt_println` **len=14**（真 strlen=**15**）→ redirect 截 `printl`；③标量 print/io.stdin 面仅 co-emit／formal 有，pure-asm 无 stub。**根修（G.7）**：call_dispatch seed+.x 补 SLICE=`_slc`／ARRAY=`_aN`、scalar kind 禁 silent default i32；pipeline_abi redirect／override／residual len 15／17；`runtime_asm_io_stubs` 补 println_i32／u32／i64… + stdin/stdout + write_u8_ptr_usize_u32 + stdin_slice。证：**run-io OK** · string／csv／base64／time／ffi／safe-ffi · 矩阵 **5/5**。日志 `/tmp/mac_run_io_final.log` · `/tmp/mac_l2_matrix_run_io.log`。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **safe-ffi-contract gate** · **host-cc-requires-allow 闭** · **mac L2 5/5** | **代码+文档** · **不升钉**。soft 过 ffi g11 后首红 `run-safe-ffi-contract-gate`：8 case 全 `host-cc-requires-allow`。五问：产生＝`safe_ffi_run_case` 硬编 `-backend c`；存储＝gate 无 pure-asm 优先；消费＝产品默认拒 silent host-cc（ALLOW 须 opt-in）；爆炸＝凡 source safe-ffi.sh 的 gate／F-ffi。**根修（G.7 测试宿主 ≡ defer-gate）**：`safe_ffi_run_case` 优先 pure-asm `-L . -o`，仅 `XLANG_ALLOW_HOST_CC` 走 `-backend c`；gate 默认 XLANG 优先 `xlang_asm`；去掉 C 轨 `ensure_std_c_o`／typeck-only 旁路（labi g11 ensure ffi.o）。证：**safe-ffi gate OK** · 8/8 case · run-ffi／encoding／builtin／string／csv · 矩阵 **5/5**。日志 `/tmp/mac_safe_ffi_gate_fix.log` · `/tmp/mac_l2_matrix_safe_ffi.log`。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **std.ffi simple-group g11** · **run-ffi 绿** · **mac L2 5/5** | **代码+文档** · **不升钉**。soft 过 encoding 后首红 `run-ffi`：UNDEF `_std_ffi_cstr_len`／`cstring_new`／`cstring_free`。五问：产生＝pure-asm import METHOD 发 `std_ffi_*`；存储＝simple-group 仅 0..10 无 ffi；消费＝ondemand 不 ensure/push `std/ffi/ffi.o`（formal 已 catalog 且 .o 有 T）。**根修（G.7 labi simple-group）**：g11 8 针（std_ffi_* + ffi_*_c）+ rel `std/ffi/ffi.o`；heavy sg==11 formal ensure；seed list／surface／heavy_surface 同 commit。证：**run-ffi OK** · encoding／builtin／string／base64／csv／mem／time · 矩阵 **5/5**。日志 `/tmp/mac_run_ffi_g11.log` · `/tmp/mac_l2_matrix_ffi_g11.log`。soft 旁路：`run-safe-ffi-contract-gate` 仍 `host-cc-requires-allow`（C 轨 ALLOW 债，非 g11）。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **encoding companion** + formal **leaf_*_c export** · **run-encoding 绿** · **mac L2 5/5** | **代码+文档** · **不升钉**。soft 过 builtin 后 `run-encoding`：user 仅 U `encoding_*_c` → 推 encoding.o 后仍 U `std_base64_*`／`std_string_string_{capacity,clear,data_ptr}`（encoding.o 传递依赖）。五问：产生＝simple-group g2 只扫 user.o；存储＝encoding formal 共编 base64/string 调用；消费＝无 companion 共推。补推 string 后 Darwin **32× core_mem multidef**（旧 encoding 把 core_mem 作 global T；export list 仅 `std_encoding_*` 时又把 `encoding_*_c` 本地化 → pure-asm UNDEF）。**根修（G.7）**：①ondemand g2 后 ensure+push string+base64（≡ g9 glue；pure heavy + seed list/surface）；②Mac formal `exported_symbols_list` 保留 `${leaf}_*_c` 且仍不导出 foreign core_*／std_*（mem 本地化，无 multidef）。证：**run-encoding OK** · builtin／base64／string／csv／mem · 矩阵 **5/5**。日志 `/tmp/mac_run_encoding_comp2.log` · `/tmp/mac_l2_matrix_enc_final.log`。PLATFORM: SHARED／MACOS formal export · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **core.builtin simple-group g10** + formal_mod · **run-builtin 绿** · **mac L2 5/5** | **代码+文档** · **不升钉**。soft 过 string 后首红 `run-builtin`：UNDEF 全族 `_core_builtin_*`。五问：产生＝pure-asm import 发外部 `core_builtin_*`（C 轨 G-01 仍 `__builtin_*`／`needs_core_builtin` stub0）；存储＝simple-group 仅 0..9 无 builtin；消费＝ondemand 不 ensure/push `core/builtin/builtin.o`；formal 目录缺 catalog。**根修（G.7 labi simple-group + formal_mod）**：g10 14 针 + rel `core/builtin/builtin.o`；heavy sg==10 formal ensure；catalog／mk 同 commit；seed list/surface/heavy_surface 对齐。证：**run-builtin OK** · string／mem／import／result／base64／csv／**time OK** · 矩阵 **5/5**。下一残：`run-encoding`（encoding.o 内 U base64／string_capacity 族——companion 债）。日志 `/tmp/mac_run_builtin_g10.log` · `/tmp/mac_l2_matrix_builtin_g10.log`。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **import METHOD overload mangle** + seed **>16B host-C lea** · **run-string 绿** · **mac L2 5/5** | **代码+文档** · **不升钉**。soft 过 run-mem 后首红 `run-string`：①UNDEF `_std_string_length`／`_is_empty`（缺 overload 后缀）；②mangle 后 arm64 SEGV／exit3。五问：产生＝pure METHOD bare `pre+name`（seed 已有 `glue_asm_mangle_import_binding_call_sym_c`）；host-C `std_string_length_String` 要 **x0=&String**（memmove 后 length_String），pure/seed 曾按 MEMORY 落栈不设 x0。**根修（G.7 call_dispatch）**：①pure 补全 mangle（type_ref_to_suffix NAMED／score／overload mid／dep path）METHOD+CALL；②seed import METHOD `sz>16` → is_mem=2 **lea→1 GP**（非 stack MEMORY；pure 同）。证：**run-string OK** · base64/csv **OK** · import／result／mem 邻域 · 矩阵 **5/5**。日志 `/tmp/mac_run_string_lea.log` · `/tmp/mac_l2_matrix_string_lea.log`。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **import METHOD call-ret harvest** · **run-mem 绿** · **mac L2 5/5** | **代码+文档** · **不升钉**。soft `run-mem` exit 8：`if (mem.mem_compare(x,z,2) >= 0)` 误真。五问：产生＝pure `pipeline_asm_emit_method_call_elf_c` import-binding 路径 call+cleanup 后**缺** `glue_asm_harvest_call_ret_to_gpr_c`（free CALL 已 harvest）；存储＝AAPCS64 `ldr w0` 把 i32 **-1** 零扩展为 `0xFFFFFFFF`；消费＝full-x0 有符号 `cmp`/`cset ge` 把「正大数」当 ≥0。对照：`let c = call(); if (c>=0)` 经 load_var **sxtw** 绿；`return call()` 得 255=-1；非 mem 逻辑。**根修（G.7 call_dispatch）**：import METHOD + UFCS leave 对齐 seed／free CALL，cleanup 后 harvest（i32 sxtw）。证：p1 inline **0** · **run-mem OK** · import／result 邻域绿 · 矩阵 **5/5**。日志 `/tmp/mac_run_mem.log` · `/tmp/mac_l2_matrix_method_harvest.log`。soft bstrict 后台 `/tmp/mac_soft_bstrict_method_harvest.log`。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** fk0 **json.o UNDEF gate complete** · **boundary 绿** · soft 停 **run-mem** · **mac L2 5/5** | **代码+文档** · **不升钉**。soft `run-boundary-encodings` 红：`json_invalid` 仅调 `parse_null` → UNDEF `_std_json_parse_null`（`run-json` 绿因 main 另有 `parse`）。五问：产生＝`labi_fk0_sym_*` k==4 仅 `std_json_parse`+死针 `stringify`；存储＝user.o UNDEF exact match（`rest==len`，`parse` 不盖 `parse_null`）；消费＝`labi_std_fk0_user_needs_rel` 不开 → 不 push `json.o`。**根修（G.7 ondemand heavy）**：k==4 补全 6 精确针；`.x`+list seed+heavy_surface 同 commit。证：sole parse_null run=1 · **json_invalid OK** · **boundary OK** · 邻域 import／result／defer／slice／json 绿 · 矩阵 **5/5**。**soft bstrict** @ **`431ab4354`**：boundary 22s 绿后停 **`run-mem`**（main exit **8**＝`mem_compare(x,z)` 符号错；脚本文案误写 align_of；独立 compare 探针可绿——疑 pure-asm 前序 mem_copy／move 后栈／索引）。日志 `/tmp/mac_soft_bstrict_431ab4354.log` · `/tmp/mac_l2_matrix_json_fk0.log`。下一波：`run-mem` mem_compare／INDEX 根修。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **import METHOD dual-GP** + **ARRAY E\* size** · **run-result／import 绿** · **mac L2 5/5** | **代码+文档** · **不升钉**。①soft `run-result` 红 173→253：import METHOD hard-code units=1 → Result 只 pack 低半。G.7 call_dispatch：size→units+gp_start；seed 去 arm64 `u=1`。②METHOD dual-GP 后 `run-import` 红：`fmt.print(u8[12],12)` 把 array size=12 当 dual-GP（len→x2）。G.7 `call_arg_value_byte_size`：TYPE_ARRAY／fixed／VAR decl **恒 8**（C decay，formal 为 *T 亦然）。证：or=42 · **run-result OK** · **run-import OK** · option OK · 矩阵 **5/5**。日志 `/tmp/mac_run_result_dualgp.log` · `/tmp/mac_run_import2.log` · `/tmp/mac_l2_matrix2.log`。**soft bstrict** @ **`064f15a80`**：import／result／defer／slice 全绿；停在 `run-boundary-encodings`（UNDEF `std_json_parse_null` + `library 'System' not found`——link/ensure 轨，非 dual-GP  packing）。下一波：boundary UNDEF 或先 L4 产品矩阵轨。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **MEM-B0 language defer** LIFO · **run-defer-gate 绿** · **mac L2 5/5** | **代码+文档** · **不升钉**。soft bstrict 在 `run-defer.sh` 红：gate 硬编 `build -backend c` → `host-cc-requires-allow`；pure-asm 此前不跑 `defer { }` 体（order_lifo=0／nested_if=10）。五问：产生＝`pipeline_asm_emit_block_body_sync_elf` 缺 language defer；存储＝Block.defer_base／`pipeline_block_defer_body_ref`；消费＝trailing `return` 为 final_expr（parser return_ends_block）须在 final 前 LIFO 跑 defer。**根修（G.7 pipeline_abi）**：`glue_emit_run_language_defers_elf`（≡ codegen `emit_run_defers`）· body_sync final 前调用；gate 改 pure-asm 优先（ALLOW 才 host-cc）。证：main42／lifo21／nested111／multi30 · gate OK · 矩阵 **5/5** · slice 邻域 OK。**soft bstrict** @ **`07df04655`**：defer 绿后 **`run-result` 红**（expect 173 got 253=-3）。地图：pure 本地 16B dual-GP（pick/or_wrap formals）pack x0:x1+x2:x3 正确；直调 host `core_result_or_i32` 仅 x0/x1 低半（units=1／size 当 8）。下一波：CALL-arg size／import formal dual-GP packing 根修。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |
| 2026-08-13 | **fix(selfhost)** pure-asm **Split nested field** + **INDEX bounds** + **Option_u8 load width** · **run-slice 绿** · **mac L2 5/5** | **代码+文档** · **不升钉**。五问：产生＝①FIELD 链 mid 把 TYPE_SLICE 当 *T peel ②INDEX bounds `slice_length` 对 FIELD fat 走 emit_expr+.data+8 ③`load_byte_sz` 全局按字段名扫 layout 误命中。**根修（G.7 pipeline_abi）**：`glue_field_chain_mid_auto_deref_ptr_rax`（*T only）· `glue_emit_slice_length_to_rbx` FIELD lvalue+length@8 · `load_byte_sz` typed-base first（删 bare name scan）。证：`subslice_split_chunks` pure-asm **0** · `length.x` **0** · `run-slice` OK · 矩阵 **5/5**。soft bstrict 本波后台。PLATFORM: SHARED · **日常 L2 非 tip L4**。 |

***
