# Xlang / X language 自举进度

> **更新**：2026-07-29 · **wave633 Cap pure-asm fixed TYPE_ARRAY let bulk esz>8**（S24[2] fx=110）· tip **`9a16fb325`** · 钉盘 **`01479a857`** · 下一 Cap residual / L4
> **基线 SHA（产品 L4 放行钉盘）**：**`01479a857`**（升自 `213bd306b`；双端 L4 129 全绿）\
> **产品 bstrict 套件数**：**129**\
> **产品代码 tip**：**wave633 Cap**（fixed S24[2] let from CALL/VAR bulk esz>8；**未** L4）\
> **git 锚点（重命名前）**：tag **`anchor-pre-xlang-rename`** · **git 锚点（Cap 轨前）**：tag **`anchor-pre-wave576`** / **`anchor-pre-wave577`**\
> **现行工程**：品牌/CLI/env/ABI 已 **xlang**（主域 **`xlang.dev`** · 仓库 **`github.com/shuliangfu/x-lang`**）。\
> **本波（29 日）状态**：**wave633 Cap L2**（Ubuntu pure-asm `let s: S24[2]=mk()` fx=110 · 矩阵绿）；钉盘仍 `01479a857`。\
> **方法**：[自举方法.md](自举方法.md)（Cap/R/L/M；工程 ≠ 产品放行）\
> **Windows（删种子前）**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) · [26](自举进度-归档-2026-07-26.md) · [25](自举进度-归档-2026-07-25.md) · [24](自举进度-归档-2026-07-24.md) · [23](自举进度-归档-2026-07-23.md) · [22](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md) · [19](自举进度-归档-2026-07-19.md) · [18](自举进度-归档-2026-07-18.md) · [17](自举进度-归档-2026-07-17.md) · [16](自举进度-归档-2026-07-16.md) · [14](自举进度-归档-2026-07-14.md) · [13](自举进度-归档-2026-07-13.md)

### 维护约定

1. 每完成一波 **必改两文**：本文（KPI/前排/§6）+ [`当前进度.md`](当前进度.md)。
2. **禁止**把 prove/Stage2/N++ 单独写成「可自举」。
3. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**；历史钉盘 125）。
4. 工程 KPI 写 **SHA + 日志路径**；过期整日流水移归档。
5. 大债地图默认写 [`当前问题分析.md`](当前问题分析.md)（承接原 `问题分析文档.md`）；非每微步。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 =** **`01479a857`**（升自 `213bd306b`；**双端 L4 129 全绿**）。\
**产品代码 tip =** **`9a16fb325`**（wave633 Cap pure-asm fixed TYPE_ARRAY let bulk esz>8；双端 L2 fx=110；**未** L4 / **未**升钉）。\
**git 锚点 =** tag **`anchor-pre-xlang-rename`** · **`anchor-pre-wave576`** · **`anchor-pre-wave577`**。\
| 2026-07-29 | **wave633** | **R2 Cap residual pure wave633**：**pure-asm fixed TYPE_ARRAY let from CALL/VAR bulk esz>8**。根因：wave632 已闭 `return [S24…]` durable E*；`let s: S24[2]=mk()` 走 `glue_struct_lit_store_fixed_array_field` CALL 路径，esz∉{1,4,8} 落 `load_i32` → 每元仅首 i32（Ubuntu fx=11=a0+a1；host-C memcpy 110；临时 INDEX 绿）。权威：G.7 扩 wave351/399 fixed-array let + wave630 bulk — esz>8 整块 `n*esz` `glue_emit_bulk_mem_copy_spills`（CALL/INDEX + VAR/FIELD 孪）；标量 1/4/8 仍 per-elem。探针：mac/Ubuntu fx/fields/varc/asg/direct/sl/i32/pt · host-C · 矩阵；Ubuntu disasm 6× qword bulk。soft：uninit `let s: S24[2]; s=mk()`；其它 Cap 叶；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave633.log` · `/tmp/ubuntu_g05_wave633.log`）。tip **`9a16fb325`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX gold · MACOS\|ARM64 co-path · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave632** | **R2 Cap residual pure wave632**：**pure-asm return large NAMED ARRAY_LIT durable + reent deep-copy**。根因三层：(1) wave631 durable 拒 force_esz>8 → `return [S24{…}]` 栈 dual-GP 悬垂（Ubuntu simple=193/deep=113≠110；host-C 110；mac 假绿）(2) 即便 COMMON 绿，`let s=mk()` reent deep-copy 把 esz=24 夹成 4 → 半字拷 (3) x86 high-end dest_off 过浅 + 大 nbytes 越 rbp → SIGSEGV 139。权威：G.7 扩 wave341/418/598/630 — ① durable force_esz>8 COMMON bulk（struct let-init + bulk_mem_copy）② deep-copy 保留 esz>8 + chunked bulk + frame pre-sum 64KiB ③ high-end byte0@dest_base+nbytes。探针：mac/Ubuntu simple/deep/dual/let/i32/pt · fs_abs · host-C · 矩阵；soft fixed `return S24[2]` fx=11。mac/Ubuntu L2。tip **`2a9d40d0c`**（`fde6a0366`+`ff3b021d6`+polarity）；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave631** | **R2 Cap residual pure wave631**：**pure-asm TYPE_SLICE ARRAY_LIT large NAMED esz（S24[]）**。根因：wave630 soft — `let s: S24[] = [S24{…},…]` force_esz=24 进 durable 后非 {1,2,4,8} 再 infer（`array_lit_elem_byte_sz` 默认 4 / `elem_type_ref` 仅 peel TYPE_ARRAY 对 SLICE stamp 返 0）→ COMMON 以 esz=4 存栈指针半字；INDEX 步长 24 → Ubuntu pure-asm RO=120·fields=200·sl bulk=66≠76（host-C 11/10/76；fixed S24[N] 绿）。权威：G.7 扩 wave625 durable + wave598/626 栈 — ① force_esz>0 非标量宽 → durable -1 ② `array_lit_elem_type_ref` peel TYPE_SLICE(11) ③ `pipeline_asm_emit_array_lit_force_esz_elf_c` 栈回落带 force_esz（let-init/call-arg/return）。探针：mac/Ubuntu s24_ro/fields/sl/z/fx/pt/asg/copy · fs · host-C · 矩阵；Ubuntu disasm 栈 in-place 无 Lxlang_al 4B pack。soft：其它 Cap 叶；return 大 NAMED durable 悬垂；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave631.log` · `/tmp/ubuntu_g05_wave631.log`）。tip **`8bbbf951b`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX gold · MACOS\|ARM64 co-path · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave630** | **R2 Cap residual pure wave630**：**pure-asm INDEX assign bulk esz>8 + try_* scale gate**。根因：wave629 soft — 非 STRUCT_LIT `a[i]=t`/`a[j]=a[i]`/`a[i]=mk()` esz∉{1,4,8}：① assign-side try_* → `rbx_plus_index_scratch_scaled` 仅×1/4/8（esz=12/24 落 ×8）② finish_store `store_rax_to_rbx_indirect` 宽≤8 且 rhs VAR 只 load 首 qword（Ubuntu s24_asg=14=1+10+3；s12=33；copy=184；call=139；host-C 66/120；arm64 常假绿）。权威：G.7 扩 wave628/629 INDEX — ① esz>8：src lvalue lea 或 CALL→`glue_emit_struct_type_let_init` temp，dest `glue_emit_index_eff_addr_scaled`（imul esz / slice .data），`glue_emit_bulk_mem_copy_spills` 8/4/1 chunk（禁 libc）② try_* 仅 esz∈{1,4,8} 否则 fallthrough imul。探针：mac/Ubuntu s24_asg/copy/s12/call/sl2(dual bulk)/lit 回归 · fs（`XLANG=./compiler/xlang_asm`）· host-C · 矩阵；Ubuntu disasm `imul $0x18` + 三 qword store。soft：非零 ARRAY_LIT（**wave631 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave630.log` · `/tmp/ubuntu_g05_wave630.log` · `/tmp/ubuntu_w630_probes2.log`）。tip **`66a0f3ffa`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave629** | **R2 Cap residual pure wave629**：**pure-asm TYPE_SLICE INDEX STRUCT_LIT assign in-place**。根因：wave628 仅 TYPE_ARRAY；`S24[] s[i]=S24{…}` 回落 generic → temp 指针 + finish_store ≤8（host-C 绿；fixed 绿）。扩 TYPE_SLICE→eff_addr(.data+imul esz)+DEST_IN_RBX 后 lit/one 绿，但 dual var-index Ubuntu panic:0 — DEST_IN_RBX 帧 `next_offset` spill 叠局部 `j`，二次 bounds 把指针当 index。权威：G.7 ① TYPE_SLICE 任意 index 走 scaled+DEST_IN_RBX（禁 fat home 当地址）② dest push CPU 栈 rehome（禁 next_offset 叠局部）。探针：mac/Ubuntu s24/s24one/s24lit/s24fx/s24take/pt/fx · fs/host-C · 矩阵。soft：assign-side non-STRUCT_LIT esz（**wave630 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave629c.log` · `/tmp/ubuntu_g05_wave629c.log` · `/tmp/ubuntu_w629_probes.log`）。tip **`2a0496153`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave628** | **R2 Cap residual pure wave628**：**pure-asm var-index STRUCT_LIT assign in-place**。根因：wave627 仅 INT_LIT；`a[i]=S24{…}` 回落 generic → rax=temp 指针 + finish_store 宽≤8 写指针进元素（Ubuntu s24_var=64 / s24_one=200；host-C 66·6；Pt 变下标 8B 常假绿）。assign-side `rbx_plus_index_scratch` 对 esz∉{1,4} 落 ×8 → a[j] esz=24 错位。权威：G.7 扩 wave627 — ① non-lit TYPE_ARRAY 基 `glue_emit_index_eff_addr_scaled`（通用 imul esz）→ rbx ② `GLUE_STRUCT_LIT_DEST_IN_RBX` 字段直写 ③ dest 指针 spill 帧槽 `rehome_off`（禁 sret_home 未激活重载）。探针：mac/Ubuntu fx_var/p0x/p1y/s24_var/s24_one/s24_lit/fx_lit/fx_sum/fld/sl · fs/host-C · 矩阵；Ubuntu disasm `imul $0x18` + [rbx+0/8/10]。soft：slice INDEX STRUCT_LIT（**wave629 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave628.log` · `/tmp/ubuntu_g05_wave628.log` · `/tmp/ubuntu_w628_probes.log`）。tip **`74c40b979`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave627** | **R2 Cap residual pure wave627**：**pure-asm INDEX STRUCT_LIT assign + multi-dim flat in-place**。根因：`a[i]=Pt{…}` 经 rvalue STRUCT_LIT(slot=-1) 物化到 `ly->next_offset`（fixed TYPE_ARRAY 后 ≡ a[0]）字段写覆盖 a[0]，再 finish_store bulk≤8 到 a[i] → Ubuntu pure-asm sum=32 / p0x=0（单 a[0]= 与 VAR/CALL rhs 绿；host-C 42）；`S24[i]=` store 宽≤8 → run=3；multi-dim `Pt[2][1]=[[…]]` flat 同 rvalue 类。权威：G.7 扩 wave626 — ① `emit_assign` INDEX 叶对 TYPE_ARRAY+INT_LIT 基址 `pipeline_asm_emit_struct_lit_fields` 于 arch-aware elem_home（禁 SLICE fat home）② `array_lit_flat` STRUCT_LIT/>8B 叶 `glue_emit_struct_type_let_init`。探针：mac/Ubuntu fx_asg/p0x/arr_of_arr/s24/fx_sum/fld/sl 回归 · fs/host-C · 矩阵；Ubuntu disasm a[1]@−0x18 直写。soft：var-index（**wave628 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave627b.log` · `/tmp/ubuntu_g05_wave627.log`）。tip **`ab3873604`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave626** | **R2 Cap residual pure wave626**：**pure-asm fixed `Pt[N]` STRUCT_LIT in-place（esz≤8）**。根因：rvalue STRUCT_LIT（slot_off=-1）物化到 `ly->next_offset` 且不抬升 high-end top；fixed TYPE_ARRAY 分配后 next_offset ≡ 数组 byte0 → 第二元字段写覆盖 a[0] 后再 bulk store +ai·esz（Ubuntu p0x=0/p0y=32/sum=32；host-C 42；arm64 low-end next 过分配假绿）。权威：G.7 扩 wave598 — `vector_let_init` + `emit_array_lit` 对 STRUCT_LIT(ko=45) **任意 esz** 走 `glue_emit_struct_type_let_init` 于 arch-aware elem_home（x86 base−ai·esz / arm64 base+ai·esz）。探针：mac/Ubuntu fixed p0x/p0y/p1x/p1y/sum=10/0/0/32/42 · fs/host-C · `[]Pt` take/sum · i32[2] · 矩阵；Ubuntu disasm elem0@−0x20 · elem1@−0x18。soft：其它 Cap 叶；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave626.log` · `/tmp/ubuntu_g05_wave626.log`）。tip **`becc92c72`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave625** | **R2 Cap residual pure wave625**：**pure-asm `[]Named` force_esz + FIELD dual-slot**。根因双层：(1) ARRAY_LIT→TYPE_SLICE force_esz 仅标量 TypeKind → durable default esz=4 只 str w0 of by-value STRUCT_LIT（y 丢）而 INDEX 用 glue_type_size_simple 步长 8；(2) `a[0].x+a[1].y` 的 clobber 分类器不 peel FIELD→INDEX → rhs@x1 被左 bounds/length 盖掉（sum=10+2=12；p1y 单读=32；let 拆分=42）。权威：G.7 ① `glue_array_lit_force_esz_from_elem_type_c`（scalar+TYPE_NAMED layout）接 let-init/call-arg/return 四口 ② `glue_binop_operand_index_addr_clobbers_rbx_elf_c` peel FIELD（复用既有 preserve）。探针：mac/Ubuntu pure+fs take/sum/let=42 · host-C · `[]i64`/dual lit binop · 矩阵；Ubuntu pure-asm fixed `Pt[2]` soft p0x=0。soft：其它 Cap 叶；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave625b.log` · `/tmp/ubuntu_g05_wave625.log`）。tip **`b278583c4`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave624** | **R2 Cap residual pure wave624**：**host-C `[]NamedStruct` incomplete type**。根因：TYPE_SLICE of user NAMED 发 `struct xlang_slice_<tag>` 无布局；empty prefix 强制 `ast_` 与 entry bare / 模块前缀 struct 双标签（formal `xlang_slice_mod_Pt` vs local `xlang_slice_ast_Pt`）。权威：G.7 ① struct 定义后 companion `xlang_slice_TAG { struct TAG *data; size_t length; }` ② `emit_type` TYPE_SLICE NAMED 用与 TYPE_NAMED 同前缀规则（entry bare）③ `type_to_c_repr` empty→bare ④ call-arg `emit_type(formal SLICE)`；codegen.x+seed+ast_pool 同 commit。探针：mac/Ubuntu host-C `[]Pt` take/local/bare-E+cc=42 · fixed/arr_take/`[]i64` 回归 · 矩阵；pure-asm `[]Pt` soft。soft：pure-asm/fs named INDEX esz；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave624.log` · `/tmp/ubuntu_g05_wave624.log`）。tip **`97b3e5d82`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED host-C named slice · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave623** | **R2 Cap residual pure wave623**：**host-C bare `-E` incomplete type**。根因：`codegen_x_ast_emit_header` 仅吐 stdint/stddef/sys/types，body 发 `struct xlang_slice_*` 无布局 → host-cc incomplete type（`-backend c -o` 经全量 rt_preamble 绿；wave619 已补全 preamble 标量集）。权威：G.7 扩 emit_header ≡ wave618–619 全套 fat layout + `XLANG_SLICE_LAYOUTS`；rt_preamble 同 guard 防 full `-o` 再吐 header 重定义；seed codegen_gen + codegen.x 同 commit。探针：mac/Ubuntu bare `-E`+host-cc i32/i64/u8/f32/f64/u32/isize/bool=42 · `-backend c -o`/pure-asm 回归 · 矩阵。soft：其它 Cap 叶；L4；Darwin `int64_t main` 硬要求 int。mac/Ubuntu L2（`/tmp/mac_g05_wave623.log` · `/tmp/ubuntu_g05_wave623.log`）。tip **`0081c8812`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED host-C minimal preamble · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave622** | **R2 Cap residual pure wave622**：**pure-asm slice lit call-arg 假绿**。根因：wave332 `pipeline_asm_emit_expr_elf_for_call_args` 对 ARRAY_LIT→TYPE_SLICE 仅用 formal esz 预留帧，载荷仍走 `emit_array_lit`→`array_lit_elem_byte_sz`（未 stamp INT_LIT→i32 esz=4），INDEX 按 formal 宽读 → `take([10,32])` 对 []i64/[]u8/[]f64 为 s[1]=0/sum=10（i32/f32 绿；let→take 经 force_esz durable 绿；host-C `-backend c -o` 绿）。权威：G.7 补全 call-arg ≡ let-init — formal force_esz + `glue_asm_emit_array_lit_durable_ptr_rax_elf_c` 优先，栈 fallback 保留。探针：mac/Ubuntu pure+fs i32/i64/u8/f32/f64 idx1=32·sum=42 · let 回归 · host-C · 矩阵。soft：host-C bare `-E`（**wave623 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave622.log` · `/tmp/ubuntu_g05_wave622.log`）。tip **`f917861d7`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding call-arg · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave621** | **R2 Cap residual pure wave621**：**pure-asm f32 ordered cmp 假绿**。根因：`glue_emit_cmp_finish_rbx_rax_elf_c` 仅门控 scalar f64→ucomisd/fcmp d；f32 落整数 cmp 64 位栈加载（高半垃圾→等值假红；负序 SF 错）。权威：G.7 扩 finish + `backend_enc_ucomiss_rbx_rax_arch`（x86 movd+ucomiss；arm64 fmov s+fcmp s）+ fp setcc；int-imm 捷径 skip f32。探针：mac/Ubuntu pure-asm/fs f32 ==/!=/<>/<=eq/>=eq/bool/eq_lit/neg_lt=42 · f64/wave620 回归 · 矩阵；Ubuntu disasm `ucomiss`；mac disasm `fcmp s`。soft：i64/u8/f32 slice lit call-arg（**wave622 已闭**）；host-C bare `-E` minimal preamble 缺 slice 布局；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave621.log` · `/tmp/ubuntu_g05_wave621.log`）。tip **`2d81d0dff`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding f32 cmp · LINUX x86 ucomiss · MACOS\|ARM64 fcmp s · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave620** | **R2 Cap residual pure wave620**：**pure-asm dual-slot float→int AS peel 假绿**。根因：`glue_try_binop_load_operand` 对 integer-target EXPR_AS peel（wave301 为 `u8[i] as i32`）；float-target 已 full emit_as，**float-source** `(f32 as i32)` 仍 peel → 整数加 IEEE 位（mac/Ubuntu pure-asm run=0，`$?` 8 位截断；host-C 绿；top-level `z as i32` 走 emit_as 绿）。权威：G.7 `glue_binop_as_needs_full_emit_elf_c` + full `pipeline_asm_emit_as_elf_impl`（cvttss2si/fcvtzs）；load-to-rbx full-AS 恒 clobber rax。探针：mac/Ubuntu host+pure f32_lit/idx/slice/asg/add=42 · u8 peel 回归=42 · 矩阵；Ubuntu disasm `cvttss2si`。soft：其它 Cap 叶；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave620.log` · `/tmp/ubuntu_g05_wave620b.log`）。tip **`6b7d3ba67`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding dual-slot · **日常 L2 非 tip L4** |
| 2026-07-29 | **wave619** | **R2 Cap residual pure wave619**：**host-C 全标量 TYPE_SLICE 标签补全**。根因：wave618 仅补 f32/f64；`bool[]`/`i64[]`/`u32[]`/`isize[]` 及 NAMED `i8`/`i16`/`u16`（旧 `xlang_slice_ast_*`）preamble 缺布局 → host-cc BLD001 incomplete type；call-arg 手写 map 与 local 漂移（缺 case→int32_t）。权威：G.7 ① type_to_c_repr NAMED i8/i16/u16→stdint ② preamble 全套 xlang_slice_* ③ call-arg `type_to_c_repr(formal SLICE)` 收敛（codegen.x+seed）。探针：mac/Ubuntu host-C 全标量 T[]+call-arg=42 · tag 对齐 · 矩阵；pure-asm f32 soft 0 · pure-asm i64[] call soft 10（local i64[] 绿）。soft：pure-asm f32；i64[] call-arg fs；L4。mac/Ubuntu L2。tip **`6c1262ee9`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED host-C type_to_c_repr+preamble · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave618** | **R2 Cap residual pure wave618**：**f32[]/f64[] host-C TYPE_SLICE C 名**。根因：`pipeline_codegen_type_kind_cstr` 仍用 LINEAR/VECTOR 前的 11/12/13 映射 float/double/void；TYPE_F32=14 落 default→int32_t → `type_to_c_repr` 发 `struct xlang_slice_int32_t` 而载荷 `static float[]`；mac host-C/clang -O2 UB（int 读 float 位再加）run 假 0；Ubuntu pure-asm 本绿。权威：G.7 单表修 ordinal 14/15/16 + preamble `xlang_slice_float`/`double` + call-arg map F32/F64。探针：mac/Ubuntu host-C+asm+fs f32[]/f64[]/f32[2]/i32[]=42 · `-E` `xlang_slice_float` · 矩阵。soft：其它 Cap 叶；L4。mac/Ubuntu L2。tip **`7a943902f`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED host-C type_to_c_repr · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave617** | **R2 Cap residual pure wave617**：**f32/f64 fixed ARRAY_LIT bare FLOAT_LIT stamp**。根因：`typeck_coerce_array_lit_elem_types_to_decl` 仅 `typeck_coerce_init_lit_to_decl`（整数 EXPR_LIT）；FLOAT_LIT 默认 f64。freestanding esz=4 store 写 f64 位低 32（10.0d 低半=0）→ Ubuntu host+fs `let a:f32[2]=[10,32]` sum cast run=0；`as f32` 与 host-C braces 绿；mac 常 CTFE 折叠。权威：G.7 复用 `typeck_coerce_init_float_lit_to_decl`（wave316）· typeck.x + seed 同 commit。探针：mac/Ubuntu host+fs f32_arr/idx/neg/cmp/add/f64/i32=42|32 · Ubuntu disasm `mov $0x41200000` · 矩阵。soft：f32[] slice lit；L4。mac/Ubuntu L2（`/tmp/mac_g05_w617.log` · `/tmp/ubuntu_g05_w617.log`）。tip **`1a11a6a72`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED typeck · LINUX pure-asm gold · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave616** | **R2 Cap residual pure wave616**：**arm64 freestanding float cast / ALU / f32 imm**。根因多层：(1) cast encoders `if(ta!=0)return -1` → CG002；(2) glue float binop 仅 `ta==0` → 整数加 IEEE 位；(3) `store_w0_to_rbp` STUR 负向 vs load 正向；(4) 产品路径 `xlang_arm64_mov_imm32_to_w0_c` MOVK hw=0 使 f32 lit 高半字错位。权威：G.7 补全 thin+seed float encoders（fmov/fcvtzs/scvtf/fadd…）+ glue `(ta==0||ta==1)` + store 正向 STR W + imm32 MOVK hw=1。探针：mac host+fs f32/f64 cast/add=42 · 矩阵；Ubuntu x86 既有绿回归。soft：f32 ARRAY_LIT（**wave617 已闭**）；f32 cmp 绿；L4。mac/Ubuntu L2。tip **`dbd0d2d16`**；钉盘仍 **`01479a857`**。PLATFORM: MACOS|ARM64 pure-asm · SHARED cast/arith 契约 · LINUX|x86 不变 · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave615** | **R2 Cap residual pure wave615**：**freestanding multi-dim INDEX 子行 → 定长 TYPE_ARRAY let/assign**（`let r: i32[2] = m[0]`）。根因：`glue_struct_lit_store_fixed_array_field` 仅 VAR/FIELD/CALL → INDEX `init_ko=47` 回 -2 → CG002（host-C `memcpy(r,(m)[0],sizeof)` 绿）。权威：G.7 补全同一函数 — INDEX 并入 CALL E* 路径（wave357 TYPE_ARRAY 留址 + esz 逐元 load/store）。探针：mac host+fs row_let/row1/row_field/row_asg/row_var/row_deep/arr_copy/asg_ord=42|3 · 矩阵；Ubuntu host+fs 同 · 矩阵。soft：fs f32/f64 emit；其它 Cap 叶；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave615.log` · `/tmp/ubuntu_g05_wave615.log` · `/tmp/ubuntu_w615_probes.log`）。tip **`aeac7201f`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding multi-dim · LINUX gold · MACOS\|ARM64 co-path · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave614** | **R2 Cap residual pure wave614**：**arm64 freestanding INDEX assign 写宽 + 变下标 EA 极性**。根因双层：(1) product seed `arch_arm64_enc_enc_store_rax_to_rbx_indirect` 忽略 dispatch 传入的 `elem_sz`，恒 `str x0,[x1]` → 8B 擦邻 i32（`m[0][1]=32; m[0][0]=10` fs=10；host-C 绿；wave391 已修 offset 孪）；(2) `enc_rbx_plus_x2_scale*` 为 `add x0,x1,x2,lsl #n`，assign finish_store pop rhs→x0 后 `str [x1]` 写 base（`a[i]=32` 实际写 `a[0]`；x86 `lea rbx` / riscv a1 已正确）。权威：G.7 补全 arm64 product seed + `arch/arm64_enc.x` 同 commit — store 1/4/8 宽表 + `add x1,x1,x2,lsl #n`。探针：mac host+fs asg_ord/1dv/var/vv/u8/n_dual=42|66 · 矩阵；Ubuntu host+fs 同（x86 本绿回归）· 矩阵。soft：其它 Cap 叶；L4；绝对 XLANG crt0；host-C multi-dim field call-arg。mac/Ubuntu L2（`/tmp/mac_g05_wave614b.log` · `/tmp/ubuntu_g05_wave614.log` · `/tmp/ubuntu_w614_probes.log`）。tip **`eb92a18b1`**；钉盘仍 **`01479a857`**。PLATFORM: MACOS\|ARM64 pure-asm · SHARED INDEX assign 契约 · LINUX\|x86 不变 · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave613** | **R2 Cap residual pure wave613**：**freestanding multi-dim nested ARRAY_LIT INDEX（`[[10,32],[1,2]][0][i]` / `[[[10,32]]][0][0][1]`）**。根因双层：(1) rvalue `pipeline_asm_emit_array_lit` 对嵌套 elem `emit_rec`→rax 指针再 store → 成指针数组，而 INDEX leave_addr+load 期望连续 T[N][M]（`let m:i32[2][2]=[[…]]` wave357 flat 已绿；host-C 绿）；(2) flat leaf_esz 只 peel 一层 → 三层 mid 行宽 8 当 leaf（`str x0` 步长 8 vs INDEX i32 步长 4 → fs `[0][0][1]=0`）。权威：G.7 ① rvalue 复用 `pipeline_asm_emit_array_lit_flat_elf_c`；② `pipeline_asm_array_lit_leaf_elem_byte_sz_c` 剥到标量（vector_let 同修）。探针：mac host+fs outer0/1/nested/row/deep/let2/var/t_mid1/lit/struct=10|32|42|11|42 · 矩阵；Ubuntu host+fs 同（相对 XLANG；绝对 path 既有 crt0 双前缀 soft）· disasm 连续 str w · 矩阵。soft：其它 Cap 叶；L4；绝对 XLANG freestanding crt0。mac/Ubuntu L2（`/tmp/mac_g05_wave613b.log` · `/tmp/ubuntu_g05_wave613.log` · `/tmp/ubuntu_w613_probes.log`）。tip **`82a1c1e1f`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding multi-dim · LINUX gold · MACOS\|ARM64 co-path · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave612** | **R2 Cap residual pure wave612**：**freestanding 同式双 ARRAY_LIT INDEX binop（`[10,32][0]+[10,32][1]`）**。根因：`glue_binop_operand_index_addr_clobbers_rbx_elf_c` 把 TYPE_ARRAY+lit 当「不碰 rbx」（仅 VAR/FIELD base+imm 真）；`pipeline_asm_emit_array_lit` 始终 `mov rax→rbx` 停 payload 基 → 右叶装 rbx 后左叶物化盖掉（mac fs=114 / Ubuntu fs=218；host-C 与 let-split 绿）。权威：G.7 补全同一分类器 — ARRAY_LIT/STRUCT_LIT/CALL/METHOD/嵌套 INDEX 及 FIELD-of-这些 恒 return 1；preserve/restore 路径已有。探针：mac host+fs lit_sum/mul/sub/let/idx/var/take=42|32 · 矩阵；Ubuntu host+fs 同（fs lit_sum was 218）· disasm push/pop rbx · 矩阵。soft：其它 Cap 叶；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave612.log` · `/tmp/ubuntu_g05_wave612.log` · `/tmp/ubuntu_w612_probes.log`）。tip **`05dd5df6f`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding dual-slot · LINUX gold · MACOS\|ARM64 co-path · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave611** | **R2 Cap residual pure wave611**：**ARRAY_LIT 基 INDEX（`[10,32][1]` / `[10,32][i]`）**。根因两层：(1) parser `parser_asm_primary_parse_array_lit` 成功后 early return，未进 `parser_asm_primary_ident_suffix_loop_c` → 尾 `[` 未消费 → 整函数 parse skip（XP003 out_len=0；host/fs 同；`let a=[…]; a[1]` 已绿）；(2) typeck `typeck_check_expr_array_lit` 故意不 stamp 字面量类型（仅 let/return coerce）→ INDEX 见 base_ty=0 → T001 subscript base。权威：G.7 ① primary 复用同一 suffix loop（≡ `(expr)` / 匿名 `{…}`）；② 无 resolved type 且元素类型齐次时 `find_or_alloc_array_type_ref` 推断 TYPE_ARRAY（coerce 仍可覆盖）；g05 mtime 监视 primary_slice.inc。探针：mac host+fs lit_idx/0/sum/dyn/paren/call_arg/let=32|10|42 · take_field/lit_arr 回归 · 矩阵；Ubuntu host+fs 同（单 INDEX）· 矩阵。soft：Ubuntu fs 同式双 ARRAY_LIT INDEX binop（**wave612 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave611*.log` · `/tmp/ubuntu_g05_wave611.log` · `/tmp/ubuntu_w611_probes.log`）。tip **`1c4a631e1`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED parse+typeck · LINUX gold · MACOS\|ARM64 co-path · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave610** | **R2 Cap residual pure wave610**：**freestanding FIELD fixed TYPE_ARRAY as T[N] call-arg（`take(Wrap{…}.xs)` / `take(w.xs)` / `take(mk().xs)`）**。根因：`pipeline_asm_emit_expr_elf_for_call_args` 仅 wave396 盖 FIELD→slice* fat；formal T[N]（E*，wave417）回落 emit_expr field rvalue leave_addr=0 → **load 首元**当指针 → callee INDEX SEGV（host-C 临时量 &field 绿；`take([10,32])` lea 已绿）。权威：G.7 补全同一 call-arg 入口 — formal TYPE_ARRAY + FIELD 定长数组 → 复用 `glue_try_index_var_or_field_base_to_rax`（wave609 INDEX leave_addr 权威），不新开第二套 FIELD 数组路径。探针：mac host+fs take_only/let/call/nest/lit=32 · lit_arr/let/field/method 回归 · 矩阵；Ubuntu host+fs 同 · 矩阵。soft：ARRAY_LIT 基 INDEX XP003；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave610.log` · `/tmp/ubuntu_g05_wave610.log` · `/tmp/ubuntu_w610_probes.log`）。tip **`d742a7ec7`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX gold · MACOS\|ARM64 co-path · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave609** | **R2 Cap residual pure wave609**：**freestanding INDEX on STRUCT_LIT/CALL array field（`Wrap{…}.xs[1]` / `mk().xs[i]`）**。根因：INDEX lit 路径 `glue_try_index_var_or_field_base_*` 仅 VAR / VAR-base FIELD → 非 VAR 回落 `emit_expr`；wave608 call_base 对 TYPE_ARRAY 字段 **load 首元**，INDEX 再当指针 add+load → freestanding SIGSEGV（host-C 临时量 `.` 绿）。权威：G.7 补全同一 call_base — `leave_addr=1` 物化后留字段槽地址；INDEX base helper 转调（rax/rbx 孪），不新开第二套 INDEX 物化。探针：mac host+fs lit_arr/0/call/nest/let=32\|10 · lit_field/method 回归 · 矩阵；Ubuntu host+pure+fs 同 · 矩阵。soft：`take(Wrap{}.xs)` ARRAY call-arg；ARRAY_LIT 基 INDEX；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave609.log` · `/tmp/ubuntu_g05_wave609.log` · `/tmp/ubuntu_w609_probes.log`）。tip **`e641caa4c`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX gold · MACOS\|ARM64 co-path · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave608** | **R2 Cap residual pure wave608**：**freestanding STRUCT_LIT.field rvalue（`Big{…}.a`）**。根因：`glue_field_access_call_base_rvalue_elf_c` 链根仅 CALL/METHOD（48/49）→ STRUCT_LIT（45）回落 `lvalue_eff_addr` UNHANDLED → CG002（host-C 临时量 `.` 绿；wave607 已能 parse postfix）。权威：G.7 补全同一 rvalue 物化路径 — 根 45 + `pipeline_asm_emit_struct_let_init_elf_c` 写 home（≡ wave605 MEMORY lit / let）+ offset load；不新开第二套 field 路径。探针：mac host+fs lit_field/paren/sum/b/nest/let=40|41 · mem_method_lit=42 · 矩阵；Ubuntu host+pure+fs 同 · 矩阵。soft：其它 Cap 叶；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave608.log` · `/tmp/ubuntu_g05_wave608b.log` · `/tmp/ubuntu_w608_probes.log`）。tip **`d6ac2e362`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX gold · MACOS\|ARM64 co-path · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave607** | **R2 Cap residual pure wave607**：**STRUCT_LIT 后缀 `.method()` / `.field`（裸 `Type{…}.sum()`）**。根因：`finish_struct_lit` / 限定 / 匿名 STRUCT_LIT 成功后 early return，未进 `parser_asm_primary_ident_suffix_loop_c` → `.sum()` 未消费 → return-stmt parse fail 静默丢 main（`num_funcs=1 main_idx=-1`）→ ld 无 `_main`；paren `(Type{…}).sum()` 本绿。权威：G.7 复用同一 suffix loop（≡ `(expr).method()`），命名/限定/匿名三处续链，不新开第二套 postfix。探针：mac host+fs mem_method_lit/dual_method_lit/let/paren=42 · wave606 回归 ufcs/field/call/arg/free=42 · 矩阵；Ubuntu host+pure+fs 同 · 矩阵。soft：fs `Big{…}.a` FIELD rvalue CG002（**wave608 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave607.log` · `/tmp/ubuntu_g05_wave607.log`）。tip **`cfa113ffc`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED parser_asm primary · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave606** | **R2 Cap residual pure wave606**：**arm64 freestanding UFCS method MEMORY by-value（self + args）**。根因：wave602 仅 `ta==0` 标 is_mem；arm64 把 MEMORY `u=0` 强制成 `u=1`（lea→x0）而 callee param_home 按栈字拷（wave599/603）→ mac fs `s.sum()`=234≠42（host-C 绿；free CALL arm64 wave603 已绿）。权威：G.7 补全 `emit_method_call_elf_c_impl` — MEMORY 双端 stack-only + arm64 reserve/`store_memory_to_sp`/cleanup ≡ free CALL wave603。探针：mac host+fs method_mem/field/call/arg_mem/dual/free=42（was 234/138/2）· Ubuntu host+pure+fs 同 · disasm sub sp+#20 · 3×str [sp] · 矩阵 rv/option/si/hello。soft：其它 Cap residual 叶；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave606.log` · `/tmp/ubuntu_g05_wave606.log`）。tip **`d4e63a37d`**；钉盘仍 **`01479a857`**。PLATFORM: MACOS\|ARM64 AAPCS64 · SHARED freestanding · LINUX\|x86 push 不变 · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave605** | **R2 Cap residual pure wave605**：**freestanding MEMORY call-arg from STRUCT_LIT/FIELD**。根因：wave601/603/604 物化源仅 VAR+CALL/METHOD → `sum(Big{…})`/`sum(o.t)` freestanding CG002（host-C 临时量 `.` 绿）。权威：G.7 补全 `push_sysv_memory`（x86 high-end）+ `store_memory_to_sp`（arm64 low-end）— STRUCT_LIT→`emit_struct_let_init` 帧 temp；FIELD→逐字 `lvalue_eff_addr`+`load_64_from_rax`（**禁** x86-only `load_qword_from_rbx`，`ta!=0` 恒 -1）。探针：mac host+fs mem_lit/field/call/var=42 · Ubuntu host+pure+fs 同 · 矩阵 rv/option/si/hello。soft：UFCS method MEMORY arm64（**wave606 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave605b.log` · `/tmp/ubuntu_g05_wave605.log`）。tip **`46aa389a1`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave604** | **R2 Cap residual pure wave604**：**arm64 freestanding MEMORY call-arg from CALL/METHOD**。根因：wave603 `store_memory_by_value_to_sp` 仅 VAR（`ko!=3` 早退）→ mac fs `sum(mk())` CG002；x86 `push_sysv_memory` 自 wave601 已 sret+push。权威：G.7 补全 CALL/METHOD 叶 — 帧 temp low-end + AAPCS64 sret x8（wave591，无 GP shift）+ multi-word store [sp+#off] ≡ x86 push 源分类。探针：mac host+fs mem_call/mem_call_arg/mem_dual/mem_var=42/21/42/42 · wave603 回归 var12/mem_param/call_dual · disasm lea→x8 bl mk · 3×str [sp] · 矩阵 rv/option/si/hello。soft：MEMORY STRUCT_LIT/FIELD（先 let）；UFCS method typeck；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave604.log` · `/tmp/ubuntu_g05_wave604.log`）。tip **`f1c9909aa`**；钉盘仍 **`01479a857`**。PLATFORM: MACOS\|ARM64 AAPCS64 · SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave603** | **R2 Cap residual pure wave603**：**arm64 freestanding dual-GP VAR call-arg + MEMORY by-value**。根因：(1) `glue_load_var_as_value_to_rax_rdx` 在 `ta!=0` 只 load 低半后早退 → spill 把脏 x1 当高半（mac fs var12=78/var16=70；call_dual via mk 绿）；(2) arm64 CALL 把 MEMORY `u=0` 强制成 `u=1` 并 lea 指针，而 param_home 按 by-value 从栈拷；(3) wave414 low-end 下 MEMORY `stack_pos=16` 与 home 重合 identity。权威：G.7 补全 dual load（low@off high@off+8→x0+x1）+ `store_memory_by_value_to_sp` + frame_size 写 stack_pos + arm64 reserve/cleanup 计 MEMORY 字数 ≡ x86 wave601。探针：mac host+fs var12/var16/call_dual/mem_param/var_take2/free_dual=42/42/42/42/41/42 · 矩阵；Ubuntu x86 pure/fs 本绿回归。soft：arm64 MEMORY from CALL/METHOD；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave603.log`）。tip **`00d2059d0`**；钉盘仍 **`01479a857`**。PLATFORM: MACOS\|ARM64 AAPCS64 + SHARED freestanding · LINUX\|x86 dual-GP 路径不变 · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave602** | **R2 Cap residual pure wave602**：**freestanding UFCS method self SysV dual-GP / MEMORY**。根因：同模块 `s.sum()` 路径 receiver 恒 1 GP（`mov rax→rdi`），9–16B 丢 rdx 高半、>16B lea 指针 vs callee MEMORY `[rbp+0x10..]` → Ubuntu pure method_impl=59 / method_mem=57（import METHOD 与 free CALL wave601 已绿；mac 常折叠/假绿）。权威：G.7 补全 `emit_method_call_elf_c_impl` UFCS 叶 places=[recv,args] ≡ import METHOD — dual-GP spill/load · MEMORY push · formal_ix=place · wave360 *T auto-ref 保留。探针：mac host+fs method_impl/mem/args/recv/dual2/free_dual/p24/var_dual=42/42/42/42/41/42/42/41 · 矩阵；Ubuntu pure+fs 同 · disasm dual rdi+rsi · MEMORY 4×push+add $0x20。soft：其它 Cap residual；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave602.log` · `/tmp/ubuntu_g05_wave602.log`）。tip **`ddb5f61ff`**；钉盘仍 **`01479a857`**。PLATFORM: LINUX+MACOS x86_64 SysV · arm64 dual-GP spill twin · SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave601** | **R2 Cap residual pure wave601**：**freestanding >16B MEMORY by-value CALL 实参**。根因：callee `param_home` 从 `[rbp+0x10..]` 拷 MEMORY，CALL 路径把 `sz>16` 当 1 GP（lea/指针→rdi）→ Ubuntu pure `sum(mk())` SEGV 139 / `sum(s)` 错值（mac arm64 假绿；host-C 绿）。权威：G.7 补全 CALL ≡ method import — `gp_units` MEMORY=0 + slot/n_stack 多字 + f32_xmm/legacy `push_sysv_memory` + cleanup 按字数；push 扩 CALL/METHOD sret 物化。探针：mac host+fs p16/p24/p24_var/mix/p32/var_dual/take2b=42/42/42/42/42/42/41 · 矩阵；Ubuntu pure+fs p16/p24/p24_var/mix/p32/var_dual/take2b=42（was 139）· 矩阵。soft：Ubuntu method/UFCS MEMORY|dual self（**wave602 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave601.log` · `/tmp/ubuntu_g05_wave601.log`）。tip **`58431a7fb`**；钉盘仍 **`01479a857`**。PLATFORM: LINUX+MACOS x86_64 SysV MEMORY · SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave600** | **R2 Cap residual pure wave600**：**arm64 multi dual-GP call-arg spill/load**。根因：AAPCS64 `emit_call_args` 硬编码 units=1 且只 spill x0；多 9–16B INTEGER 实参 `take2(mkA(),mkB())` / `f(n,s12,m)` 丢高半（mac fs take2=63≠41；wave599 单 dual-GP 形参已绿）。权威：G.7 `glue_sysv_spill/load` 开 ta==1 dual（x1@home+8 low-end）+ CALL 路径 gp_units from size + gp==0 high-first load（防 x0 盖 low）+ x86 补 mov rax→rdi。探针：mac fs take2b/onlyp/q/fields/mix/take3/ret_param/param_nest=41/30/11/43/42/42/41/43 · wave599 回归 · 矩阵；Ubuntu pure+fs take2b/mix/take3/param_sum/take_field=41/42/42/42/41 · 矩阵。soft：>16B MEMORY 形参；VAR dual-GP call-arg arm64；method_call dual；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave600.log` · `/tmp/ubuntu_g05_wave600b.log`）。tip **`b4a9519bd`**；钉盘仍 **`01479a857`**。PLATFORM: MACOS\|ARM64 AAPCS64 + SHARED freestanding · LINUX\|x86 load 补全 · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave599** | **R2 Cap residual pure wave599**：**arm64 freestanding 9–16B named dual-GP param home**。根因：(1) `emit_param_home` ta==1 每形参只 store 1 GP @16+i×8，丢 dual-GP 高半；(2) `fill_param` 多字槽恒 x86 high-end（home=off+w）而 arm64 局部/字段为 low-end（wave402）→ field 从空槽读 0（mac fs take_field=65≠41 · o.m.f=0；Ubuntu x86 dual-GP 已绿）。权威：G.7 fill_param+帧推进 host 极性（MACOS\|ARM64 low-end ≡ `asm_local_slot_reg_offset`；LINUX\|x86 不变）+ arm64 param_home dual-GP low@home high@home+8 + MEMORY 逐字 copy（≡ `store_retval_pair` half2）。探针：mac fs take_field/param_field/param_c/param_sum/take_only=41/20/3/42/41 · 回归 arr/nest/ptr · 矩阵；Ubuntu pure+fs take_field/param_sum=41/42 · 矩阵。soft：其它 Cap residual；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave599.log` · `/tmp/ubuntu_g05_wave599.log`）。tip **`bf8e22476`**；钉盘仍 **`01479a857`**。PLATFORM: MACOS\|ARM64 AAPCS64 + SHARED freestanding · LINUX\|x86 不变 · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave598** | **R2 Cap residual pure wave598**：**freestanding ARRAY_LIT of >8B named struct + INDEX NAMED stride**。根因：(1) `vector_let_init`/`emit_array_lit` 对 esz∉{1,2,4,8} clamp store_sz=4 且只 store rax → dual-GP/sret/STRUCT_LIT 载荷丢失（mac fs arr_sz12=20≠42；lit12=11≠32；host-C 假绿）；(2) INDEX 解析为 TYPE_NAMED 时 `glue_index_elem_byte_sz_from_type_ref` 默认 8 → xs[1].b 错位（写已对读仍错）。权威：G.7 per-elem `glue_emit_struct_type_let_init` arch-aware home（≡ nest_slot 极性）+ NAMED→`glue_type_size_simple`（≡ wave597 array_lit esz）。探针双端：arr_sz12/16/24/lit=42/42/42/32 · arr_simple/nest4 回归 · 矩阵。soft：mac fs take_field 12B 形参（tip 已 soft）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave598c.log` · `/tmp/ubuntu_g05_wave598.log`）。tip **`eabf6abc1`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave597** | **R2 Cap residual pure wave597**：**x86 sret 嵌套 STRUCT_LIT 帧极性 + ARRAY_LIT NAMED 元素宽**。根因：(1) sret nest temp `nest_off=next` + `load(nest+cop)` — x86 high-end lea(-nest)+foff 向 rbp 增长，Mid2@24 覆盖 sret_home@16，copy cop=8 读 [rbp-32] → Ubuntu nest4/ptr_nest_call SIGSEGV（arm64 low-end 已绿）；(2) `pipeline_asm_array_lit_elem_byte_sz` TYPE_NAMED 落默认 4 → `[mk(),mk()]` 只存 eax@stride4 → arr_simple=10≠22（mac 常折叠 22 假绿）。权威：G.7 nest 抬升 nest_alloc + load nest-cop；NAMED→`glue_type_size_simple`。探针双端：nest4/ptr_call/arr=41/41/22 · nest3/take/ptr_mid · 矩阵。soft：>8B CALL 数组元素 dual-GP/sret；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave597.log` · `/tmp/ubuntu_g05_wave597.log`）。tip **`ca0e97771`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave596** | **R2 Cap residual pure wave596**：**指针中间字段 FIELD_ACCESS + arm64 sret 序言冲 x0**。根因：(1) 链式 `w.p.f` lvalue 只 lea/add 偏移，从不 load *T 中间字段 → 外层 load 读指针低位（host-C 临时量掩盖）；INDEX 已有 `glue_index_deref_ptr_field_slot_rax`，FIELD 链缺；(2) CALL 链 `mk().m.p.f` 纯 offset 和同样跳过 *T；(3) wave591 arm64 sret `mov x8→x0` 销毁第一 GP 形参（AAPCS64 x8 独立、无 GP shift）。权威：G.7 lvalue ELF+text auto-deref；call_base 保留 chain_fa root→outer + 中间 *T load；param_home `store_x_reg_to_rbp(reg=8)`。探针：mac fs/host ptr_simple/field/mid/nest_call=42/42/45/41 · nest3/4 回归 · 矩阵；Ubuntu pure-asm ptr_simple/mid=42/45 · nest3=41 · nest4/ptr_call soft（**wave597 已闭**）。soft：L4。mac/Ubuntu L2（`/tmp/mac_g05_wave596b.log` · `/tmp/ubuntu_g05_wave596.log`）。tip **`5c982f705`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · MACOS\|ARM64 AAPCS64 sret · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave595** | **R2 Cap residual pure wave595**：**深嵌套 STRUCT_LIT（Outer+Mid+Inner）物化 + arm64 9–16B return**。根因：(1) 嵌套 lit 经 rec `slot=-1` 与父共享 `next_offset` → 覆盖父字段；9–16B dual store 恒写 16B 越界 → Ubuntu nest3 SIGSEGV；(2) arm64 dual-GP return 仅 `ta==0` → 返回栈指针 → mac 错值；(3) x86 high-end `nest_slot=base+foff` 把 Mid 放 Outer 下方。权威：G.7 `pipeline_asm_emit_struct_lit_fields_elf_c` — 嵌套就地（arm64 `base+foff` / x86 `base-foff`）；sret 走 temp+fsz copy；dual high 按 fsz 截断；arm64 frame dual-GP；返回前 re-home rbx。探针双端：nest3=41 · nest3_let=42 · n3f/g/a · n2f · nest2/sz12 回归 · 矩阵 rv/option/si/hello。soft：指针中间字段（**wave596 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave595b.log` · `/tmp/ubuntu_g05_wave595.log`）。tip **`9bedc1917`**（产品 `b7a768f4b`+`9bedc1917`）；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · LINUX\|x86 high-end · MACOS\|ARM64 low-end · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave594** | **R2 Cap residual pure wave594**：**pure-asm 9–16B dual-GP return-local 假 C pointer-in-rax**。根因：`pipeline_asm_call_struct16_ret_needs_rax_deref_c` 对一切 size>8 named 返回 1 → store_retval/CALL.field `mov rax,rbx; load *rbx` 把 dual-GP 低半（0x2a）当指针 → Ubuntu pure-asm SIGSEGV（mac freestanding CG002）。STRUCT_LIT return 仅因 try_inline 绿。权威：G.7 同一分类器 — pure-asm body → dual-GP by value（0）；仅 skip_heavy C 体仍 1；SLICE/ARRAY/ok_i32 早退不变。探针双端：sz12/sz16_local · retonly · use_sum freestanding+pure-asm=42 · inline/nest2 回归 · Ubuntu disasm 无假 deref · 矩阵 rv/option/si/hello。soft：深嵌套 STRUCT_LIT nest3（**wave595 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave594.log` · `/tmp/ubuntu_g05_wave594.log`）。tip **`13ced06b5`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding classifier · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave593** | **R2 Cap residual pure wave593**：**nested CALL.field 链 + 9–16B STRUCT_LIT CALL.field inline**。根因：(1) `glue_field_access_call_base_rvalue_elf_c` 仅 base=CALL/METHOD → `mk().i.f` base 为 FIELD 回落 lvalue → CG002；(2) ≤16 路径只 emit CALL+store_retval，未像 let_init 先 try_inline → 9–16B STRUCT_LIT `mk().a` CG002（let 内联绿）；(3) arm64 `store_retval_pair` `ta!=0` 早退只写 rax。权威：G.7 补全同一函数 — FIELD 链 walk+offset 累加；任意 size try_inline STRUCT_LIT 再 sret/dual-GP；arm64 dual-GP half@home+8。探针双端：nest2/sz4nest/sz8nest/sz12/sz16/sz24/callarg/true_sret freestanding+pure-asm=42 · nest2step 回归 · 矩阵 rv/option/si/hello。soft：return-local 中宽 dual-GP（**wave594 已闭**）；深嵌套大 STRUCT_LIT；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave593.log` · `/tmp/ubuntu_g05_wave593.log`）。tip **`789b485b0`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · MACOS\|ARM64 dual-GP · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave592** | **R2 Cap residual pure wave592**：**freestanding_io ensure when nostdlib face forces need_io**。根因：true-sret/large 路径 `call memcpy` → freestanding ld `need_nostdlib_face` → force `need_io`（stubs 要 `xlang_sys_mmap`）；prepare 仅 `xlang_freestanding_user_o_needs_io`（UNDEF `xlang_sys_*`）才 ensure → 冷树无 `compiler/freestanding_io.o` → BLD001（文案 `xlang_sys_write` 误导；STRUCT_LIT/short 无 memcpy 故绿）。权威：G.7 `labi_ensure_list` prepare 扩 `needs_io \|\| nostdlib_face`；`runtime_link_abi` freestanding ld 在 skip_missing 前 `xlang_ensure_freestanding_io_o`（≡ stubs ensure）。探针：**mac** true/let/32/two/lit/short def+fs=42 · **Ubuntu** 删 freestanding_io.o 后 `-freestanding` true-sret=42（ensure 自愈）· pure-asm 回归 · 矩阵 rv/option/si/hello。soft：其它侧门；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave592.log` · `/tmp/ubuntu_g05_wave592.log`）。tip **`2b2b2921b`**；钉盘仍 **`01479a857`**。PLATFORM: LINUX freestanding · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave591** | **R2 Cap residual pure wave591**：**arm64 freestanding >16B sret（AAPCS64 x8）**。根因：sret 仅 ta==0（x86 SysV rdi+GP shift）→ arm64 `return local` / `let s=mk()` / true-sret `mk().f` CG002；半开后 memcpy 复用 x86 序（mov arg0 no-op + pop 冲 dest）→ SIGSEGV。权威：G.7 glue 扩 sret 面 — 激活 sret_home · prologue 存 x8 · CALL/FIELD/let 装 x8（无 GP shift）· STRUCT_LIT sret_direct · memcpy dest@x0 src@x1 n@x2。探针：**mac** freestanding true/let/32/two/short/lit=42 · **Ubuntu** pure-asm 同形=42 · 矩阵 rv/option/si/hello。soft：Ubuntu `-freestanding` companion（**wave592 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave591*.log` · `/tmp/ubuntu_g05_wave591.log`）。tip **`4a1c8def5`**；钉盘仍 **`01479a857`**。PLATFORM: MACOS\|ARM64 AAPCS64 · SHARED freestanding face · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave590** | **R2 Cap residual pure wave590**：**freestanding FIELD_ACCESS on CALL return >16B（MEMORY/sret）**。根因：wave589 物化路径 `ret_sz>16` 早退 UNHANDLED → CG002（Ubuntu pure-asm `mk().f` 24B+ 暴露；host-C 临时量 `.` 掩盖）。权威：G.7 `glue_field_access_call_base_rvalue_elf_c` 对齐 `let s=mk()` / `glue_emit_struct_type_let_init` — STRUCT_LIT return inline → 帧 temp，否则 x86 SysV sret（hidden rdi=home + `call_sret_reg_shift`）+ layout field load。探针双端：Big24/32 STRUCT_LIT=42 · two-field=52 · true sret `return t; mk().f` Ubuntu pure-asm=42 · ≤16 回归 · 矩阵 rv/option/si/hello。soft：arm64 non-inline large（**wave591 已闭**）；`-freestanding` companion；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave590.log` · `/tmp/ubuntu_g05_wave590.log`）。tip **`73815a8d7`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · sret LINUX+MACOS x86_64 SysV · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave589** | **R2 Cap residual pure wave589**：**freestanding FIELD_ACCESS on CALL/METHOD return（`mk().x`）**。根因：field_access fast 回落 `lvalue_eff_addr`（仅 VAR/INDEX/DEREF）→ CALL 基 -1 → CG002（Ubuntu pure-asm 暴露；host-C 临时量 `.` 掩盖）。wave588 soft 误归「长字段」；短名 `mk().x` 同路径红。权威：G.7 `pipeline_glue.c` `glue_field_access_call_base_rvalue_elf_c` — ≤16B 物化帧 temp（`store_retval_pair` ≡ `let s=mk()`）+ layout offset load。探针双端：short/two-field/mk+let 1..127=42 · `-freestanding`75=42 · 矩阵 rv=42 · option · si · hello。soft：>16B sret（**wave590 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave589.log` · `/tmp/ubuntu_g05_wave589.log`）。tip **`f82b1390a`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave588** | **R2 Cap residual pure wave588**：**host-C STRUCT_LIT field designator 64→128（content ≤127）**。根因：sidecar/`StructLitFieldEntry.name[128]` 已存 ≤127，emit 仍 `flen>64` 截 64 → designator 与 layout 字段 decl 不一致（fldh 75/127 BLD001）；lookup flen_s/m/lk/flen_use 同截。权威：G.7 codegen.x + seed pin 同 commit。探针双端：fldh 4/63/64/75/127=42 · short lit · nested · 矩阵 rv=42 · option · si · hello。soft：fs CALL.field CG002（**wave589 已闭**）；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave588.log` · `/tmp/ubuntu_g05_wave588.log`）。tip **`2b18debe0`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED host-C · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave587** | **R2 Cap residual pure wave587**：**field typeck mnl/gnl/prebind 63→127**。根因：mono/ambient concrete 扫描仍 ≤63（buf 已 [128]）→ 长 concrete 字段类型被当 type-param 假 mono/ambient；`.x` prebind/base_nlen/lnl 相对 seed 漂移；seed reverse_infer fl / lval / rlen 落后 `.x`。权威：G.7 seed + strict_minimal.x + heavy field_access ambient。探针双端：ty/mono/envt 63..127=42 · host-C fld 4/63/64=42 · 矩阵 rv=42 · option · si · hello。soft：host-C designator~64（**wave588 已闭**）；fs 长字段 CG002；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave587.log` · `/tmp/ubuntu_g05_wave587.log`）。tip **`bbb7579f6`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave586** | **R2 Cap residual pure wave586**：**label/goto 名 32→128（content ≤127）**。根因：LabeledStmt/OneFuncLabeledEntry 仍 [32]+silent clamp>31；set_names/copy32/codegen scratch 同；前缀碰撞语义错；Ubuntu 未 pin `pipeline_gen.c` 时 sizeof[32] vs set_names[128] 错位 → ELF unresolved。权威：G.7 ast.x+ast_pool+stubs+codegen/glue 同向；*copy32 ABI 名保留 payload 128；**强制 pin seed→pipeline_gen.c**。探针双端：goto/ret 31..127=42 · coll=7 · -E 全名；矩阵 rv=42 · option=102 · si=0 · hello。soft：codegen/mnl residual；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave586.log` · `/tmp/ubuntu_g05_wave586.log`）。tip **`520593143`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave585** | **R2 Cap residual pure wave585**：**函数形参名 32→128（content ≤127）**。根因：FuncParamEntry/OneFunc param 仍 name[32]+`>31`；parser plen>31；parser_asm pname32[32] 在 *copy32 写 128 后栈砸（std.* import SIGABRT）。权威：G.7 ast_pool+parser+parser_asm+codegen 同向；*copy32 ABI 名保留 payload 128。探针双端：p31..127=42；矩阵 rv=42 · option=102 · si=0 · hello。soft：label/codegen residual；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave585*.log` · `/tmp/ubuntu_g05_wave585.log`）。tip **`ff25582ec`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
**现行主线 =** **wave630**：pure-asm INDEX assign bulk esz>8 闭；下一 Cap residual 叶或 L4；钉盘仍 `01479a857`。

| 轨                | 状态                                                                                           |
| ---------------- | -------------------------------------------------------------------------------------------- |
| 工程轨              | **wave576 已闭**（L4 真冷双端 129 全绿 · mac 10m17s / Ubuntu 3m22s · 升钉盘 `213bd306b` → `01479a857` · 0 模块 prove 文书新增 · Cap 轨前基线固化）；tip `01479a857`；钉盘 **`01479a857`**；下一 **wave577+（Cap 轨）** |
| 产品轨 tip / 钉盘     | tip = **`01479a857`**（工程轨 prove 111/111 IDENTICAL + L4 双端 129 全绿）· 钉盘 **`01479a857`**（升自 `213bd306b`） |
| mac L4 真冷        | ✅ **`01479a857`** remaining_o=0 · **OK (129)** · 10分17秒（wave576，L4 真冷双端升钉）· `/tmp/mac_true_bstrict_01479a857.log` |
| Ubuntu L4（金标）    | ✅ **`01479a857`** remaining_o=0 · **OK (129)** · 3分22秒（wave576，L4 真冷双端升钉）· `/tmp/ubuntu_true_bstrict_01479a857.log` |
| Ubuntu L2 真车（wave547） | ✅ **`86ef18adb`** · bstrict **OK (129)** · 2分38秒（crypto_inc_glue + sync_lock_diag_tls prove 文书） |
| 前钉 L4            | `213bd306b` · `f1250f415` · `d5de2f13b` · `2bc0885d3` · `d812aeb41` · `c3a81bfd6` · `70ca1089b` · … |

***

## 1. 文档地图

| 文档                                             | 看什么                   |
| ---------------------------------------------- | --------------------- |
| [自举方法.md](自举方法.md)                             | Cap / R / L / M       |
| [自举步骤.md](自举步骤.md)                             | 可执行闸门                 |
| [自举验证.md](自举验证.md)                             | 怎么测                   |
| **本文**                                         | 长期 KPI + 前排（**每波必改**） |
| [当前进度.md](当前进度.md)                             | 当天快照（**每波必改**）        |
| **[当前问题分析.md](当前问题分析.md)**                     | tip 级产品债地图（21 日起权威）   |
| [自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) | **27 日全日 wave491-533**（只读） |
| [自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md) | **26 日全日 wave446-491**（只读） |
| skill `xlang-selfhost-product-gate`             | L4 / 双端 / 假绿禁止        |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项                          | 结果                                                                                  | 日志 / 备注                                      |
| -------------------------- | ----------------------------------------------------------------------------------- | -------------------------------------------- |
| 产品 bstrict 套件数             | **129**                                                                             | 现行白名单                                        |
| 产品 L4 放行钉盘                 | **`01479a857`**                                                                      | 28 日 wave576 L4 升钉（前钉 `213bd306b`）；双端 L4 129；含 wave538-575 累计 38 波 L2 收口      |
| 本波 tip                     | **wave576**（L4 真冷双端 129 全绿 · 升钉盘 · Cap 轨前基线固化） | tip **`01479a857`**（L4 129 全绿）· 钉盘 **`01479a857`**（升自 `213bd306b`） |
| mac 产品链 bstrict（钉盘 L4） | ✅ **OK (129)**                                                                      | L4 · `/tmp/mac_true_bstrict_01479a857.log` · cold `/tmp/mac_true_cold_01479a857.log` · 10分17秒（wave576）    |
| Ubuntu L4（金标 · 钉盘）  | ✅ **OK (129)**                                                                      | L4 · `/tmp/ubuntu_true_bstrict_01479a857.log` · bstrict 3分22秒（wave576） |
| Ubuntu L2 真车（wave547） | ✅ **OK (129)**                                                                      | L2 · `/tmp/ubuntu_l2_bstrict_86ef18adb.log` · 2分38秒 |
| 产品矩阵（双端 tip L4）           | ✅ rv42 / option / hello / si + bstrict 129                                                                | mac + Ubuntu · `01479a857`（L4 钉盘）                                      |
| 前钉 mac/Ubuntu L4           | ✅ 129 @ `213bd306b` · `f1250f415` · `d5de2f13b` · `2bc0885d3` · `d812aeb41` · …                                                                | 前钉日志 `/tmp/*_213bd306b.log`                             |

### 2.2 工程轨（继承）

| KPI           | 现                                        | 备注                   |
| ------------- | ---------------------------------------- | -------------------- |
| **T / EMPTY** | 18/18                                    | 18 号结案               |
| **N**         | 111/111 IDENTICAL                          | 回归基线（wave539-575 累计 prove 白名单扩至 111：time_os + path_fast + dynlib_os + panic + panic_arm64 + process_argv + test_fn_invoke + net_workers + compress_zlib_glue + arrow_simd_glue + random_fill + tls_mbedtls_bio + ed25519_ref10_glue + net_udp_batch + net_sock_fast + net_dns_fast + net_ipv6_fast + net_addr_fast + slice_glue + crypto_inc_glue + sync_lock_diag_tls + asm_build + asm_io_stubs + string_fast + net_io_batch_fast + env_os + http_glue + queue_contention + backtrace_platform + sync_os + channel_glue + log_os + scheduler_glue + process_os_glue + asm_backend_compat_stubs + thread_glue + bootstrap_nostdlib_stubs + backend_seed_mega_fallback + parser_asm_parse_expr_link + lsp_diag_stubs_no_c + build_runtime + user_asm_seed_bridge + runtime_heap_user + target_cpu_flags + parser_asm_thin_c + runtime_driver_strict_glue_stubs + rt_dispatch_thin + lsp_diag_pipeline_ctx + x_seed_bridge + seed_link_compat + simd_loop_thin + runtime_math_libm + fmt_check_cmd + runtime_lsp_glue + backend_try_inline_dispatch_thin + backend_call_dispatch_thin + backend_arch_emit_dispatch_thin + runtime_driver_strict_glue_thin + simd_enc_thin + diag_thin + runtime_driver_abi + backend_x86_64_enc_c + diag + runtime + runtime_link_abi + backend_enc_dispatch_thin + runtime_pipeline_abi）|
| **R2 / 根修本波** | **wave576 已闭**（L4 真冷双端 129 全绿 · 升钉盘 `213bd306b` → `01479a857` · Cap 轨前基线固化 · 0 模块 prove 文书新增）；**wave575 已闭**（runtime_pipeline_abi full 270 nm T · 双端 prove nm IDENTICAL × 1 · prove 文书补全收口 · 工程轨不跑 bstrict）；**wave574 已闭**（backend_enc_dispatch_thin）；**wave573 已闭**（runtime_link_abi）；**wave572 已闭**（runtime）；**wave571 已闭**（diag.x 参数顺序 bug 根源修复 + diag）；**wave570 已闭**（backend_x86_64_enc_c）；**wave569 已闭**（diag_thin + runtime_driver_abi）；**wave568 已闭**（runtime_driver_strict_glue_thin + simd_enc_thin）；**wave567 已闭**（backend_call_dispatch_thin + backend_arch_emit_dispatch_thin）；**wave566 已闭**（runtime_lsp_glue + backend_try_inline_dispatch_thin）；**wave565 已闭**（fmt_check_cmd）；**wave564 已闭**（runtime_math_libm）；**wave563 已闭**（simd_loop_thin）；**wave562 已闭**（x_seed_bridge + seed_link_compat）；**wave561 已闭**（rt_dispatch_thin + lsp_diag_pipeline_ctx）；**wave560 已闭**（parser_asm_thin_c + runtime_driver_strict_glue_stubs）；**wave559 已闭**（runtime_heap_user + target_cpu_flags）；**wave558 已闭**（build_runtime + user_asm_seed_bridge）；**wave557 已闭**（parser_asm_parse_expr_link + lsp_diag_stubs_no_c）；**wave556 已闭**（bootstrap_nostdlib_stubs + backend_seed_mega_fallback）；**wave555 已闭**（asm_backend_compat_stubs + thread_glue）；**wave554 已闭**（scheduler_glue + process_os_glue）；**wave553-539 已闭**（累计 26 模块）；**wave538 已闭**（half-done 审计 + 5 模块 net/sync R2） | tip `01479a857`（工程轨 prove 111/111 IDENTICAL + L4 双端 129 全绿）· 钉盘 **`01479a857`**（升自 `213bd306b`） |

***

## 3. 当前前排（≤3 条）

1. **wave576 已闭**：L4 真冷双端 129 全绿（mac 10m17s / Ubuntu 3m22s · remaining_o=0 · 矩阵 rv=42/opt=0/si=0/hello=0 · bstrict OK 129）；升钉盘 `213bd306b` → `01479a857`（含 wave538-575 累计 38 波 L2 收口）；tip `01479a857`；**Cap 轨前基线固化**——为高风险编译器修改建立可秒级回退的 git 锚点（tag `anchor-pre-wave576`）
2. **Cap 轨候选**（wave577+ 起解锁路径）：pipeline_glue_strict_minimal.x（70 #[no_mangle] · L012 ident>63：含 75-char `pipeline_typeck_find_func_return_type_in_module_by_name_call_strict_minimal` 等 · AST name slots u8[64] 固定 · 扩容需改 429 检查点/55 文件 · **首选**）/ runtime_driver_diagnostic.x（67 · L011 string>limit：139-char 诊断串）/ codegen.x（typeck 推断缺口）/ typeck.x（L009）；main_c_entry.x wave559 已弃用（cc main 签名强制 char**）
3. 双端 L4 基线 **`01479a857`** 全绿（wave576）；钉盘 **`01479a857`**（双端验证通过）；wave538-575 累计 38 波 L2 收口已通过本波 L4 升钉固化

***

## 4. 与归档的关系

- **27 日归档**：[自举进度-归档-2026-07-27.md](自举进度-归档-2026-07-27.md) 包含 wave491-533（泛型 inherent impl 方法完整链路 + 8 个 R2 OS glue migration + 10 个 R2 migration + 6 个 half-done 模块 Makefile 补全 + 4 个 R2 DIRECT 模式迁移 + runtime_lsp_glue 迁移障碍记录）。
- **26 日归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md) 包含 wave446-491（泛型推断链 / trait 默认方法体 / struct mono 链 / multi-combo mangle 等系统性产品债解决）。
- **25 日归档**：[自举进度-归档-2026-07-25.md](自举进度-归档-2026-07-25.md) 包含 Cap residual wave396-445 + 26 日初 wave446。
- **28 日新内容**：本文记录 wave534-537（wave528-532 共 4 个 R2 migration 收口 L4 + runtime_lsp_glue R2 迁移守卫增量 + thin 源统一 + L4 真冷全测双端 129 全绿）+ wave538-575 累计 38 波 L2 工程轨 prove 文书补全 + wave576 L4 真冷双端 129 全绿升钉。
- **勿**用归档 tip 或旧钉（`213bd306b` / `d5de2f13b` / `2bc0885d3` 等）冒充现行 tip L4。**现行钉盘 `01479a857`**。

***

## 5. 双端 / 平台

| 平台                     | 本波状态                                                              |
| ---------------------- | ----------------------------------------------------------------- |
| **macOS arm64**        | ✅ tip L4 全擦 + 矩阵 + **129** @ **`01479a857`**（wave576） |
| **Ubuntu x86\_64（金标）** | ✅ tip L4 全擦 + 矩阵 + **129** @ **`01479a857`**（wave576） |
| **Windows MSYS2**      | 继承 hybrid；本波未重跑（**不需要切换**）；路径文档已 `…\xlang\x-lang`                                             |

***

## 6. 变更记录（倒序 · 28 日）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-07-28 | **wave592** | **R2 Cap residual pure wave592**：**freestanding_io ensure on nostdlib face**。根因：memcpy sret → force need_io 但 prepare 未 ensure。权威：G.7 prepare + ld ensure。探针双端 true-sret `-freestanding`=42 · 矩阵绿。soft：其它侧门；L4。tip **`2b2b2921b`**；钉盘仍 **`01479a857`**。PLATFORM: LINUX freestanding · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave591** | **R2 Cap residual pure wave591**：**arm64 AAPCS64 x8 sret >16B**。根因：sret 仅 ta==0；arm64 memcpy 序错。权威：G.7 glue 扩 x8 sret。探针双端 true/let/32=42 · 矩阵绿。soft：`-freestanding` companion（**wave592 已闭**）；L4。tip **`4a1c8def5`**；钉盘仍 **`01479a857`**。PLATFORM: MACOS\|ARM64 · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave590** | **R2 Cap residual pure wave590**：**freestanding CALL.field >16B sret/inline**。根因：wave589 `ret_sz>16` UNHANDLED → CG002。权威：G.7 glue 对齐 let（inline STRUCT_LIT ret / x86 SysV sret）。探针双端 Big/32/true-sret=42 · 矩阵绿。soft：arm64 non-inline（**wave591 已闭**）；`-freestanding` companion；L4。tip **`73815a8d7`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave589** | **R2 Cap residual pure wave589**：**freestanding FIELD_ACCESS on CALL return（`mk().x`）**。根因：lvalue_eff_addr 拒 CALL 基 → CG002；误诊「长字段」。权威：G.7 glue `glue_field_access_call_base_rvalue_elf_c`。探针双端 short/two/mk+let 1..127=42 · freestanding75 · 矩阵绿。soft：>16B sret（**wave590 已闭**）；L4。tip **`f82b1390a`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave588** | **R2 Cap residual pure wave588**：**host-C STRUCT_LIT field designator 64→128（content ≤127）**。根因：emit `flen>64` 截 64 而 storage 已 ≤127 → designator≠decl；lookup 同截。权威：G.7 codegen.x+seed pin。探针双端 fldh 4..127=42 · 矩阵绿。soft：fs CALL.field（**wave589 已闭**）；L4。tip **`2b18debe0`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED host-C · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave587** | **R2 Cap residual pure wave587**：**field typeck mnl/gnl/prebind 63→127**。根因：mono/ambient concrete 扫描仍 ≤63；`.x` prebind 漂移；seed fl/lval/rlen 落后。权威：G.7 seed+strict_minimal.x+heavy ambient。探针双端 ty/mono/envt 63..127=42 · 矩阵绿。soft：host-C designator~64（**wave588 已闭**）；L4。tip **`bbb7579f6`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave586** | **R2 Cap residual pure wave586**：**label/goto 名 32→128**。见 §0。tip **`520593143`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave584** | **R2 Cap residual pure wave584**：**library wrap / import binding 64→127**。根因：LibraryParseResult/ImportEntry 已 [128]，library_wrap name/param_type/field 仍 >63；imports bind_buf[64]+`<=63`；set_binding_name `len>64`；select 行宽 64 却 content 127（溢出）；import_path_copy64 只填 64/128；typeck binding hint `bl<=63`。权威：G.7 content 127；path 与 dep_path_rows 对齐 127；typeck.x+seed pin。探针双端：bind63..127 · libfn63..127=42；矩阵 rv=42 · option=102 · si=0 · hello+`0a`。soft：其它 Cap residual 侧门；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave584.log` · `/tmp/ubuntu_g05_wave584.log` · `/tmp/ubuntu_w584_probes.log`）。tip **`dbfa284da`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave583** | **R2 Cap residual pure wave583**：**struct 类型名 / 字段名 / type-param getter 64→127**。根因：storage 已 [128]，parser_asm struct_layout sname/fname/tp `[64]`+`>63`；finish_struct_lit tlen/flen；field_name_from_tok≤63；ast_pool field/type_param getter≤63；typeck merge/SoA/coerce≤63。权威：G.7 content 127；typeck.x+seed pin；SoA 128。探针双端：sn63..127 · fn63..127=42；矩阵。soft：library/import residual；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave583.log` · `/tmp/ubuntu_g05_wave583.log`）。tip **`9283a6cfb`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave582** | **R2 Cap residual pure wave582**：**type_alias / enum / match 名 64→127**。根因：storage 已 [128]，parser_asm type_alias/type_ref/enum 仍 >63；typeck alias_name_len≤63；Darwin 产品链 strict_minimal field prebind≤63 跳过长 enum base。权威：G.7 content 127；typeck.x+seed pin；strict_minimal 与 heavy 同门控；ast_pool set/byte_at 127。探针双端：ta63..127 · en63..127 · ev75 · em75=42；矩阵。soft：struct/library 等 residual；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave582*.log` · `/tmp/ubuntu_g05_wave582.log`）。tip **`c7dbb960d`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave581** | **R2 Cap residual pure wave581**：**let/const 局部名 64→128**。根因：parser 已接受 name_len≤127，但 `pipeline_onefunc_append_{let,const}` 仍 `>64` 拒收 → parse_body_lets false → 无 main（BLD001 `_main`）；host-C emit_nm `ci2<64` 却 `emit_nml=lname_len` 夹 NUL。权威：G.7 OneFunc 侧车行宽 128 + append/copy64 载荷 127；block/top-level append/set/hoist 127；parser_asm block_from_res/top_level_let；codegen.x+pin emit 拷贝环 128；asm_ctx_local_name_copy64。探针双端：let63/64/75/127=42 · host-C let75 · tl75 · fn75 回归；矩阵 rv/option/si/hello。soft：type_alias/enum/match 等 residual `>64`；L4。mac/Ubuntu L2（`/tmp/mac_g05_wave581*.log` · `/tmp/ubuntu_g05_wave581.log`）。tip **`dec1ef1fe`**；钉盘仍 **`01479a857`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-28 | **wave576** | **L4 真冷双端 129 全绿 · 升钉盘 `213bd306b` → `01479a857` · Cap 轨前基线固化**。范围：0 模块 prove 文书新增（无）；仅 L4 真冷全测双端验证 + 升钉盘。触发：wave575 prove 文书补全收口（常规候选池枯竭）+ Cap 轨前需 L4 固化基线（Cap 轨将修编译器 lexer.x 高风险）。**构建级别：L4 真冷**（全擦 compiler\|std\|core *.o + 删产品二进制 → pin seed → bootstrap → g05 → 矩阵 → bstrict 129）。**mac L4**：remaining_o=0 · 矩阵 rv=42/opt=0/si=0/hello=0 · bstrict OK (129) · 10分17秒 · `/tmp/mac_true_bstrict_01479a857.log`。**Ubuntu L4 金标**：remaining_o=0 · 矩阵 rv=42/opt=0/si=0/hello=0 · bstrict OK (129) · 3分22秒 · `/tmp/ubuntu_true_bstrict_01479a857.log`。**升钉盘**：`213bd306b` → `01479a857`（含 wave538-575 累计 38 波 L2 收口）。**Cap 轨前基线固化**：为高风险编译器修改建立可秒级回退的 git 锚点（tag `anchor-pre-wave576`）。prove 白名单 111/111 IDENTICAL 不变。下一波 wave577+ 进入 Cap 轨——首选扩 AST name slots 63→≥75 解锁 pipeline_glue_strict_minimal.x。PLATFORM: SHARED L4 |
| 2026-07-28 | **wave575** | **runtime_pipeline_abi full · 双端 prove nm IDENTICAL × 1 · 工程轨 · prove 文书补全收口**。范围：1 模块 prove 文书补全（1 full），扩 prove 白名单 +1。模块：runtime_pipeline_abi（270 #[no_mangle] · full · DIRECT compute: pipeline import/load/sync/parse/typeck orch + module import storage + debug_trace + diag_emitted + dep_seeded + ndep + cfg_eval complex ops · rest 含 333 extern bridges (parser_*/asm_*/pipeline_*/typeck_*/cfg_eval_*/driver_*/xlang_*) + 48 static BSS slots (g_import_open_*/g_pipe_*) + init_globals · 无 doc_anchor）。**Ubuntu 符号重命名修复**：Ubuntu xlang_asm -E 给非 #[no_mangle] 的 pipe_* export function 加 pipeline_ 前缀（macOS clang 不加）→ prove_module_selfhost.sh sym_rename 33 syms（pipeline_pipe_* → pipe_*）匹配 surface。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 1 · 不跑 bstrict 129）。mac prove：1/1 IDENTICAL。Ubuntu prove 金标：1/1 IDENTICAL。tip `df547ac04`；钉盘仍 `213bd306b`（wave538-575 L2 收口未升钉）。prove 白名单 110→111 IDENTICAL。**收口里程碑**：4 个超大模块（runtime 110 / runtime_link_abi 145 / backend_enc_dispatch_thin 133 / runtime_pipeline_abi 270）全部 prove IDENTICAL；常规候选池彻底枯竭，剩余未注册 .x 模块均为 Cap residual。PLATFORM: SHARED full |
| 2026-07-28 | **wave574** | **backend_enc_dispatch_thin thin full · 双端 prove nm IDENTICAL × 1 · 工程轨**。范围：1 模块 prove 文书补全（1 thin full），扩 prove 白名单 +1。模块：backend_enc_dispatch_thin（133 #[no_mangle] · thin full · arm64/riscv64/x86_64 enc dispatch shells + arch_*_enc_* forwards to _impl + DIRECT compute · arch_*_enc_* extern bridges + *_impl externs · 无 doc_anchor）。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 1 · 不跑 bstrict 129）。mac prove：1/1 IDENTICAL。Ubuntu prove 金标：1/1 IDENTICAL。tip `17e8d4b5e`；钉盘仍 `213bd306b`（wave538-574 L2 收口未升钉）。prove 白名单 109→110 IDENTICAL。PLATFORM: SHARED thin full |
| 2026-07-28 | **wave573** | **runtime_link_abi mixed · 双端 prove nm IDENTICAL × 1 · 工程轨**。范围：1 模块 prove 文书补全（1 mixed），扩 prove 白名单 +1。模块：runtime_link_abi（145 #[no_mangle] · mixed · 75 DIRECT compute (link_abi_user_o_needs_*/generated_c_needs_* + xlang_output_is_elf_o + xlang_asm_ld_lib_root_default + driver_copy_cstr_n + xlang_path_has_sep/last_sep + link_diag_code_for_kind + xlang_invoke_cc 等) + 70 thin+rest forwards to _impl (30 xlang_ensure_runtime_* + 12 link_diag_* + xlang_ensure_crt0/freestanding_io + 多个 link_abi_asm_ld_* + xlang_cc_compile_sync 等) · Cap residual: 91 _impl bridges + 7 helper externs · 无 doc_anchor）。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 1 · 不跑 bstrict 129）。mac prove：1/1 IDENTICAL。Ubuntu prove 金标：1/1 IDENTICAL。tip `fd3483759`；钉盘仍 `213bd306b`（wave538-573 L2 收口未升钉）。prove 白名单 108→109 IDENTICAL。PLATFORM: SHARED mixed |
| 2026-07-28 | **wave572** | **runtime mixed · 双端 prove nm IDENTICAL × 1 · 工程轨**。范围：1 模块 prove 文书补全（1 mixed），扩 prove 白名单 +1。模块：runtime（110 #[no_mangle] · mixed · 30 DIRECT compute + 80 thin+rest forwards to _impl · 95 extern bridges + 7 helper externs · 无 doc_anchor）。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 1 · 不跑 bstrict 129）。mac prove：1/1 IDENTICAL。Ubuntu prove 金标：1/1 IDENTICAL。tip `643162418`；钉盘仍 `213bd306b`（wave538-572 L2 收口未升钉）。prove 白名单 107→108 IDENTICAL。**里程碑**：常规候选池枯竭后转向超大模块 runtime.x（110）。L011/L012 Cap residual 调研：AST name slots 固定 u8[64]，扩容需改 429 检查点/55 文件。PLATFORM: SHARED mixed |
| 2026-07-28 | **wave571** | **diag.x 参数顺序 bug 根源修复 + diag mixed · L2 真测全绿 · 工程轨 prove**。范围：1 模块 prove 文书补全（1 mixed）+ diag.x 参数顺序 bug 根源修复，扩 prove 白名单 +1。**根源修复**：diag_io_fputc 17 处调用 (c,o) 与声明 (o: *u8, c: i32) 不一致；typeck 未报 i32 vs *u8 不匹配（Cap residual）→ 17 处 (c,o)→(o,c)。模块：diag（42 #[no_mangle] · mixed · 35 DIRECT compute + 7 thin+rest forwards to diag_ctx_*/diag_code_table_*/diag_entry_*/link_abi_getenv · 39 extern bridges · 无 doc_anchor）。构建级别：**L2 真测**（动产品面 · rm diag.o → g05 relink → rv=42/opt/si/hello 全绿 · 不跑 bstrict 129）。mac prove：1/1 IDENTICAL。Ubuntu prove 金标：1/1 IDENTICAL。mac L2 真测：rv=42/opt=0/si=0/hello=0 全绿。tip `63bafdec0`；钉盘仍 `213bd306b`（wave538-571 L2 收口未升钉）。prove 白名单 106→107 IDENTICAL。PLATFORM: SHARED mixed |
| 2026-07-28 | **wave570** | **backend_x86_64_enc_c mixed · 双端 prove nm IDENTICAL × 1 · 工程轨**。范围：1 模块 prove 文书补全（1 mixed），扩 prove 白名单 +1。模块：backend_x86_64_enc_c（104 #[no_mangle] · mixed · 104 DIRECT compute · 0 thin+rest · 9 extern bridges: pipeline_elf_ctx_* · has doc_anchor backend_x86_64_enc_c_x_doc_anchor）。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 1 · 不跑 bstrict 129）。mac prove：1/1 IDENTICAL。Ubuntu prove 金标：1/1 IDENTICAL。tip `8a9ed4726`；钉盘仍 `213bd306b`（wave538-570 L2 收口未升钉）。prove 白名单 105→106 IDENTICAL。**里程碑**：常规规模（≤110 #[no_mangle]）非 Cap residual .x 文件全部注册 prove，剩余仅超大模块 + Cap residual 模块。PLATFORM: SHARED mixed |
| 2026-07-28 | **wave569** | **diag_thin + runtime_driver_abi mixed · 双端 prove nm IDENTICAL × 2 · 工程轨**。范围：2 模块 prove 文书补全（2 mixed），扩 prove 白名单 +2。模块：diag_thin（78 #[no_mangle] · mixed · 19 DIRECT + 59 thin+rest forwards to _impl · 53 extern bridges · 无 doc_anchor · diag_thin.x ≠ diag.x）+ runtime_driver_abi（61 #[no_mangle] · mixed · 54 DIRECT + 7 thin+rest forwards to _impl · 39 extern bridges: driver_*_flag_slot + getenv + *_impl · 无 doc_anchor）。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 2 · 不跑 bstrict 129）。mac prove：2/2 IDENTICAL。Ubuntu prove 金标：2/2 IDENTICAL。tip `8b5bd45b0`；钉盘仍 `213bd306b`（wave538-569 L2 收口未升钉）。prove 白名单 103→105 IDENTICAL。PLATFORM: SHARED mixed |
| 2026-07-28 | **wave568** | **runtime_driver_strict_glue_thin + simd_enc_thin mixed · 双端 prove nm IDENTICAL × 2 · 工程轨**。范围：2 模块 prove 文书补全（2 mixed），扩 prove 白名单 +2。模块：runtime_driver_strict_glue_thin（11 #[no_mangle] · mixed · 8 DIRECT + 3 thin+rest forwards to driver_* · 9 extern bridges · 无 doc_anchor）+ simd_enc_thin（74 #[no_mangle] · mixed · 3 DIRECT + 71 thin+rest forwards to _impl · 71 extern bridges · 无 doc_anchor）。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 2 · 不跑 bstrict 129）。mac prove：2/2 IDENTICAL。Ubuntu prove 金标：2/2 IDENTICAL。tip `29cb0b4c8`；钉盘仍 `213bd306b`（wave538-568 L2 收口未升钉）。prove 白名单 101→103 IDENTICAL。PLATFORM: SHARED mixed |
| 2026-07-28 | **wave567** | **backend_call + backend_arch_emit mixed · 双端 prove nm IDENTICAL × 2 · 工程轨**。范围：2 模块 prove 文书补全（2 mixed），扩 prove 白名单 +2。模块：backend_call_dispatch_thin（44 #[no_mangle] · mixed · 6 DIRECT + 1 thin forward + 37 thin+rest forwards to _impl · 40 extern bridges · 无 doc_anchor）+ backend_arch_emit_dispatch_thin（47 #[no_mangle] · mixed · 43 pure dispatch by ta + 4 dispatch with DIRECT return 0 fallback · 135 extern bridges: 45 arch_arm64_* + 45 arch_riscv64_* + 45 arch_x86_64_* · 无 doc_anchor）。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 2 · 不跑 bstrict 129）。mac prove：2/2 IDENTICAL。Ubuntu prove 金标：2/2 IDENTICAL。tip `9b79f0997`；钉盘仍 `213bd306b`（wave538-567 L2 收口未升钉）。prove 白名单 99→101 IDENTICAL。PLATFORM: SHARED mixed |
| 2026-07-28 | **wave566** | **runtime_lsp_glue + backend_try_inline mixed · 双端 prove nm IDENTICAL × 2 · 工程轨**。范围：2 模块 prove 文书补全（2 mixed），扩 prove 白名单 +2。模块：runtime_lsp_glue（54 #[no_mangle] · mixed · 37 DIRECT + 17 thin+rest forwards to _impl · 23 extern bridges · has doc_anchor runtime_lsp_glue_x_doc_anchor）+ backend_try_inline_dispatch_thin（50 #[no_mangle] · mixed · 8 DIRECT + 42 thin+rest forwards to _impl · 42 extern bridges · 无 doc_anchor）。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 2 · 不跑 bstrict 129）。mac prove：2/2 IDENTICAL。Ubuntu prove 金标：2/2 IDENTICAL。tip `3d2bc4118`；钉盘仍 `213bd306b`（wave538-566 L2 收口未升钉）。prove 白名单 97→99 IDENTICAL。**里程碑**：进入边界大模块（44-54 nm T）。PLATFORM: SHARED mixed |
| 2026-07-28 | **wave565** | **fmt_check_cmd mixed · 双端 prove nm IDENTICAL × 1 · 工程轨**。范围：1 模块 prove 文书补全，扩 prove 白名单 +1。模块：fmt_check_cmd（25 #[no_mangle] · mixed · DIRECT compute: path_should_ignore + fmt_path_ends_with_dot_x + fmt_walk_skip_dot_name + check_one_need_fallback_diag + check_one_finalize_rc + check_lint_fail_on_warnings + xlang_path_is_absolute + parse_ignore_opt + closedir_win + thin+rest forwards to _impl · Cap residual: 30 extern bridges · 无 doc_anchor）。**弃用 diag.x**：diag.x -E 输出有 diag_io_fputc 参数类型转换 error（int → uint8_t*），属于 Cap residual（codegen 类型推断问题），非 prove 文书能解决。**surface 修复**：添加 #include <string.h> 解决 strstr 隐式声明。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 1 · 不跑 bstrict 129）。mac prove：1/1 IDENTICAL。Ubuntu prove 金标：1/1 IDENTICAL。tip `49ba2399e`；钉盘仍 `213bd306b`（wave538-565 L2 收口未升钉）。prove 白名单 96→97 IDENTICAL。PLATFORM: SHARED mixed |
| 2026-07-28 | **wave564** | **runtime_math_libm mixed · 双端 prove nm IDENTICAL × 1 · 工程轨**。范围：1 模块 prove 文书补全，扩 prove 白名单 +1。模块：runtime_math_libm（34 #[no_mangle] · mixed · 2 DIRECT compute: math_signum_c (sign function) + math_special_near (epsilon compare) + 32 thin+rest forwards to math_*_impl · Cap residual: 32 extern bridges: math_*_impl · 无 doc_anchor）。**里程碑**：进入中模块阶段（22+ #[no_mangle]），小模块（2-8）已于 wave563 耗尽。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 1 · 不跑 bstrict 129）。mac prove：1/1 IDENTICAL。Ubuntu prove 金标：1/1 IDENTICAL。tip `d59268331`；钉盘仍 `213bd306b`（wave538-564 L2 收口未升钉）。prove 白名单 95→96 IDENTICAL。PLATFORM: SHARED mixed |
| 2026-07-28 | **wave563** | **simd_loop_thin mixed · 双端 prove nm IDENTICAL × 1 · 工程轨**。范围：1 模块 prove 文书补全，扩 prove 白名单 +1。模块：simd_loop_thin（22 #[no_mangle] · mixed · 5 DIRECT compute: glue_f32_slot_rbp_disp32 + glue_soa_f32_col_rbp_disp32 + glue_simd_loop_pick_lanes_c + glue_var_array_i32_size_c + glue_var_is_array_i32_n_c + 17 thin+rest forwards to _impl · Cap residual: 22 extern bridges: 5 pipeline_* + 17 *_impl · 无 doc_anchor）。**里程碑**：小模块（2-8 #[no_mangle]）已耗尽，下一波起进入中模块（22+）。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 1 · 不跑 bstrict 129）。mac prove：1/1 IDENTICAL。Ubuntu prove 金标：1/1 IDENTICAL。tip `f8fdf74ed`；钉盘仍 `213bd306b`（wave538-563 L2 收口未升钉）。prove 白名单 94→95 IDENTICAL。PLATFORM: SHARED mixed |
| 2026-07-28 | **wave562** | **x_seed_bridge mixed + seed_link_compat mixed · 双端 prove nm IDENTICAL × 2 · 工程轨**。范围：2 模块 prove 文书补全，扩 prove 白名单 +2。模块：x_seed_bridge（14 #[no_mangle] · mixed · 5 thin+rest forwards: typeck_preprocess_x_buf + std_heap_alloc_zeroed/zero/free/alloc + 9 DIRECT: 8 io_* stubs (io_read_ptr/io_read_ptr_len/io_register_buffer/io_unregister_buffers/io_wait_readable/io_register_buffers_4/io_register_buffers_buf/io_register_buffers_buf_i32) + 1 forward chain xlang_io_register · Cap residual: 4 extern bridges: preprocess_x_buf + typeck_std_heap_alloc + calloc + free · 无 doc_anchor）+ seed_link_compat（19 #[no_mangle] · mixed · 11 thin+rest forwards: typeck_lsp_alloc/free/is_null/main_impl + typeck_std_heap_alloc/alloc_zeroed/free + std_sys_read_file_into + std_heap_free_u8_ptr + ast_pipeline_module_struct_layout_set_packed + backend_asm_ctx_slot_offset + 5 DIRECT stubs returning -1: lsp_diag_lsp_build_diagnostics/semantic_tokens + lsp_diag_hover/references/definition_at + 3 DIRECT compute: xlang_expr_is_func_param_at (buffer comparison) + xlang_expr_is_param0_field_access + xlang_module_func_index_by_name (func name lookup) · Cap residual: 20 extern bridges · 无 doc_anchor）。**sed 过滤修复**：x_seed_bridge -E preamble 含 io_* weak/static inline 定义与宏冲突 → prove_module_selfhost.sh sed 扩展过滤（extern .*io_read_ptr/io_register_buffer*/io_wait_readable/io_unregister_buffers + #define io_register_buffers_buf + std_io_driver/core variants + __attribute__((weak)) io_register_buffers_buf_c 单行 / io_register_buffers_4 / io_wait_readable / io_read_batch_buf / io_write_batch_buf 多行范围 + static inline io_register_buffers_buf_i32）。**回归验证**：5 个 io 相关模块（runtime_asm_io_stubs / runtime_net_io_batch_fast / runtime_driver_strict_glue_stubs / rt_dispatch_thin / lsp_diag_pipeline_ctx）全 IDENTICAL，sed 修改无副作用。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 2 · 不跑 bstrict 129）。mac prove：2/2 IDENTICAL。Ubuntu prove 金标：2/2 IDENTICAL。tip `60d5a7e44`；钉盘仍 `213bd306b`（wave538-562 L2 收口未升钉）。prove 白名单 92→94 IDENTICAL。PLATFORM: SHARED mixed + mixed |
| 2026-07-28 | **wave561** | **rt_dispatch_thin thin+rest + lsp_diag_pipeline_ctx thin+rest · 双端 prove nm IDENTICAL × 2 · 工程轨**。范围：2 模块 prove 文书补全，扩 prove 白名单 +2。模块：rt_dispatch_thin（4 #[no_mangle] · thin+rest · 4 thin forwards to _impl: driver_run_asm_backend_c + driver_run_emit_c_path_c + driver_run_compiler_full + driver_try_compile_via_shu_c_sibling · Cap residual: 4 _impl bridges · 无 doc_anchor）+ lsp_diag_pipeline_ctx（14 #[no_mangle] · thin+rest · 14 thin forwards to extern bridges: lsp_diag_x_alloc_dep_ctx_size + lsp_build_diagnostics_response + lsp_diag_hover_at + lsp_diag_references_at + lsp_hover_at + lsp_references_at + lsp_diag_definition_at + lsp_build_semantic_tokens_response + lsp_io_lsp_diag_invalidate_cache + lsp_diag_pipeline_ctx_fill_paths + typeck_lsp_main + lsp_write_all + lsp_debug_report_sqpoll_env + lsp_apply_default_io_policy · Cap residual: 12 extern bridges (typeck_lsp_diag_hover_at/typeck_lsp_diag_references_at shared by 2 wrappers each) · 无 doc_anchor）。**零修复**：surface 一次过。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 2 · 不跑 bstrict 129）。mac prove：2/2 IDENTICAL。Ubuntu prove 金标：2/2 IDENTICAL。tip `dc14dfaaf`；钉盘仍 `213bd306b`（wave538-561 L2 收口未升钉）。prove 白名单 90→92 IDENTICAL。PLATFORM: SHARED thin+rest + thin+rest |
| 2026-07-28 | **wave560** | **parser_asm_thin_c mixed + runtime_driver_strict_glue_stubs mixed · 双端 prove nm IDENTICAL × 2 · 工程轨**。范围：2 模块 prove 文书补全，扩 prove 白名单 +2。模块：parser_asm_thin_c（11 #[no_mangle] · mixed · 10 thin+rest forwards to _impl: copy_token_bytes_to_buf64 + extern_parse_set_fail_c + skip_trait_impl_block_raw_c + skip_one_top_level_let/const_into_slice_c + cfg_skip_pending_top_level_into_slice_c + try_skip_const_import_stmt + collect_imports_consume_path + write_try_skip_allow_result + lex_from_lr_next_c + 1 DIRECT pure compute: is_fn_sig_scalar_type_token_c · Cap residual: 10 _impl bridges · doc_anchor · seed 全守卫）+ runtime_driver_strict_glue_stubs（12 #[no_mangle] · mixed · 3 thin+rest forwards to driver_*: asm_driver_skip_codegen_dep_0_get + asm_driver_set_current_dep_path_for_codegen + typeck_driver_diagnostic_pipe_marker + 8 DIRECT via typeck_*_slot extern bridges: typeck_i32_ptr_store/read + typeck_layout_metrics_init_slot/init_depth/al_read_depth/sz_read_depth + typeck_call_resolve_dep_idx_peek + typeck_call_resolve_func_idx_peek + 1 thin+rest forward to append_text_to_codegen_buf_impl · Cap residual: 10 extern bridges · 无 doc_anchor · seed 全守卫）。**零修复**：surface 一次过。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 2 · 不跑 bstrict 129）。mac prove：2/2 IDENTICAL。Ubuntu prove 金标：2/2 IDENTICAL。tip `7d3c97f44`；钉盘仍 `213bd306b`（wave538-560 L2 收口未升钉）。prove 白名单 88→90 IDENTICAL。PLATFORM: SHARED mixed + mixed |
| 2026-07-28 | **wave559** | **runtime_heap_user DIRECT + target_cpu_flags DIRECT · 双端 prove nm IDENTICAL × 2 · 工程轨**。范围：2 模块 prove 文书补全，扩 prove 白名单 +2。模块：runtime_heap_user（7 #[no_mangle] · DIRECT · heap_alloc_c + heap_free_c + heap_realloc_c + heap_alloc_zeroed_c + heap_arena_init_c + heap_arena64_alloc_c + heap_arena64_deinit_c · DIRECT via malloc/free/realloc/calloc/heap_alloc_aligned_c extern bridges · struct runtime_heap_user_XlangHeapArena64 · 无 doc_anchor）+ target_cpu_flags（5 #[no_mangle] · DIRECT · driver_set/get_pending_target_cpu_features + tcp_tolower + tcp_eq5 + tcp_eq6 · pure compute + static BSS g_driver_pending_target_cpu_features · 无 doc_anchor · 无 Cap residual）。**零修复**：surface 一次过。**弃用 main_c_entry**：main 函数 argv 类型 uint8_t* 不符合 C 标准（cc 要求 char**），-E cc -c 失败 → 改选 runtime_heap_user。**prove 机制验证**：-E preamble 的 weak stub（io_/ctx_/process_）不影响 nm diff（prove 排除 W weak 符号）。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 2 · 不跑 bstrict 129）。mac prove：2/2 IDENTICAL。Ubuntu prove 金标：2/2 IDENTICAL。tip `4b4f44dc3`；钉盘仍 `213bd306b`（wave538-559 L2 收口未升钉）。prove 白名单 86→88 IDENTICAL。PLATFORM: SHARED DIRECT + DIRECT |
| 2026-07-28 | **wave558** | **build_runtime thin+rest + user_asm_seed_bridge mixed · 双端 prove nm IDENTICAL × 2 · 工程轨**。范围：2 模块 prove 文书补全，扩 prove 白名单 +2。模块：build_runtime（5 #[no_mangle] · thin+rest · 5 thin forwards to _impl: build_runtime_info/warn + build_patch_pipeline_gen_c/driver_gen_c + build_run_legacy_steps · Cap residual: 5 _impl bridges · seed 全守卫 #ifndef XLANG_BUILD_RUNTIME_FROM_X）+ user_asm_seed_bridge（7 #[no_mangle] · mixed · 4 thin+rest forwards to _impl: seed_elf_ctx_set_macho_leading_underscore + seed_asm_reject_empty_elf_text + seed_platform_macho_write_macho_o_to_buf + seed_platform_coff_write_coff_o_to_buf + 2 DIRECT env gates: seed_asm_debug_enabled + seed_asm_emit_trace_enabled via link_abi_getenv + 1 DIRECT pure compute: seed_elf_ctx_code_len · Cap residual: 4 _impl bridges + link_abi_getenv _impl · seed 全守卫 #ifndef XLANG_USER_ASM_SEED_BRIDGE_FROM_X）。**零修复**：surface 一次过。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 2 · 不跑 bstrict 129）。mac prove：2/2 IDENTICAL。Ubuntu prove 金标：2/2 IDENTICAL。tip `51c7ca0e3`；钉盘仍 `213bd306b`（wave538-558 L2 收口未升钉）。prove 白名单 84→86 IDENTICAL。PLATFORM: SHARED thin+rest + mixed |
| 2026-07-28 | **wave557** | **parser_asm_parse_expr_link thin+rest + lsp_diag_stubs_no_c DIRECT · 双端 prove nm IDENTICAL × 2 · 工程轨**。范围：2 模块 prove 文书补全，扩 prove 白名单 +2。模块：parser_asm_parse_expr_link（1 #[no_mangle] · thin+rest · parser_asm_parse_expr_debug_enabled thin forward to link_abi_getenv extern bridge · Cap residual: link_abi_getenv _impl host getenv · seed 全守卫 #ifndef XLANG_PARSER_ASM_PARSE_EXPR_LINK_FROM_X）+ lsp_diag_stubs_no_c（4 #[no_mangle] · DIRECT · lsp_diag_copy_text_impl + json_escape_str_impl pure compute + lsp_diag_copy_text + json_escape_str forward to _impl same module · 无 Cap residual · seed 全守卫 #ifndef XLANG_LSP_DIAG_STUBS_NO_C_FROM_X）。**零修复**：surface 一次过。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 2 · 不跑 bstrict 129）。mac prove：2/2 IDENTICAL。Ubuntu prove 金标：2/2 IDENTICAL。tip `35ebedea5`；钉盘仍 `213bd306b`（wave538-557 L2 收口未升钉）。prove 白名单 82→84 IDENTICAL。PLATFORM: SHARED thin+rest + DIRECT |
| 2026-07-28 | **wave556** | **bootstrap_nostdlib_stubs mixed + backend_seed_mega_fallback DIRECT · 双端 prove nm IDENTICAL × 2 · 工程轨**。范围：2 模块 prove 文书补全，扩 prove 白名单 +2。模块：bootstrap_nostdlib_stubs（6 #[no_mangle] · mixed · 5 thin+rest forwards to _impl: heap_grow/syscall3/syscall4/format_double/vfprintf_fd + 1 DIRECT pure compute align16 · rest 含 5 _impl bridges: bump heap mmap / Linux x86_64 syscall / vsnprintf+write）+ backend_seed_mega_fallback（2 #[no_mangle] · DIRECT · pipeline_seed_mega_ctx_reset + pipeline_dep_ctx_target_arch_local + 3 non-no_mangle helpers: mega_load_i32_le + mega_store_i32_le + mega_store_ptr_le · Cap residual: memset + pipeline_dep_ctx_target_arch extern bridges · seed 全守卫 #ifndef XLANG_BACKEND_SEED_MEGA_FALLBACK_FROM_X）。**surface 修复**：backend_seed_mega_fallback 初版 helper 函数未加模块前缀导致 nm 差异 8 行（mega_load_i32_le vs .x export 的 backend_seed_mega_fallback_mega_load_i32_le）→ 给 3 helper 加 backend_seed_mega_fallback_ 前缀 + 调用点同步。**前缀规律验证**：mega_/pipeline_ 前缀不触发 ast_（wave545+ 规律再确认）；non-#[no_mangle] helper 函数 .x 会加模块前缀，surface 需镜像。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 2 · 不跑 bstrict 129）。mac prove：2/2 IDENTICAL。Ubuntu prove 金标：2/2 IDENTICAL。tip `6fcc04602`；钉盘仍 `213bd306b`（wave538-556 L2 收口未升钉）。prove 白名单 80→82 IDENTICAL。PLATFORM: SHARED mixed + DIRECT |
| 2026-07-28 | **wave555** | **asm_backend_compat_stubs DIRECT + thread_glue thin+rest · 双端 prove nm IDENTICAL × 2 · 工程轨**。范围：2 模块 prove 文书补全，扩 prove 白名单 +2。模块：asm_backend_compat_stubs（3 #[no_mangle] · DIRECT · xlang_format_u32_to_buf pure compute + xlang_elf_ctx_append_u32_le + xlang_arm64_mov_imm32_to_w0_c via pipeline_elf_ctx_append_bytes extern bridge · seed 全守卫 #ifndef XLANG_ASM_BACKEND_COMPAT_STUBS_FROM_X）+ runtime_thread_glue（24 #[no_mangle] · thin+rest · 16 thin forwards to _impl: 2 xlang_cpu_ star + 14 thread_ star + 8 DIRECT std_thread_ star forwards to thread_ star _c · rest 含 16 _impl OS bridges pthread_ star / CreateThread / SetThreadAffinityMask / qos_class）。**零修复**：surface 一次过。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 2 · 不跑 bstrict 129）。mac prove：2/2 IDENTICAL。Ubuntu prove 金标：2/2 IDENTICAL。tip `63243653e`；钉盘仍 `213bd306b`（wave538-555 L2 收口未升钉）。prove 白名单 78→80 IDENTICAL。PLATFORM: SHARED DIRECT + thin+rest |
| 2026-07-28 | **wave554** | **scheduler_glue mixed + process_os_glue thin+rest · 双端 prove nm IDENTICAL × 2 · 工程轨**。范围：2 模块 prove 文书补全，扩 prove 白名单 +2。模块：runtime_scheduler_glue（17 #[no_mangle] · mixed · 10 thin+rest xlang_async_trace_now_us/bound_ctx_cancelled/coop_frame_step_jmp 等 + 7 DIRECT env gate xlang_async_q_occupancy/trace_topn/trace_sample_rate 等 · 10 _impl Cap residual + link_abi_getenv env bridge）+ runtime_process_os_glue（22 #[no_mangle] · thin+rest · process_getenv/setenv/unsetenv/getpid/getppid/getcwd/chdir/spawn/exec/waitpid 等 · 全 _impl_c 桥 OS syscall）。**surface 修复**：注释 `xlang_async_*/coop_frame_step_*` 的 `*/` 被 clang 当块注释结束 → 改 star 措辞（lessons learned 已记）；env_parse_u32_default: static helper → runtime_scheduler_glue_env_parse_u32_default 匹配 .x export 符号（no #[no_mangle] 仍加模块前缀）。构建级别：**工程轨 prove**（双端 prove nm IDENTICAL × 2 · 不跑 bstrict 129）。mac prove：2/2 IDENTICAL。Ubuntu prove 金标：2/2 IDENTICAL。tip `c1965127b`；钉盘仍 `213bd306b`（wave538-554 L2 收口未升钉）。prove 白名单 76→78 IDENTICAL。PLATFORM: SHARED mixed + thin+rest |
| 2026-07-28 | **wave547** | **crypto_inc_glue thin+rest + sync_lock_diag_tls mixed · L2 真车 129 全绿**。范围：2 模块 prove 文书补全，扩 prove 白名单 +2。模块：runtime_crypto_inc_glue（6 #[no_mangle] · thin+rest · 6 _impl 桥：xlang_sha256_block/rotr32/ch/maj + crypto_i32_sub/rotl32 · 纯 C 算术无 OS 调用 · xlang_/crypto_ 前缀不触发 ast_）+ runtime_sync_lock_diag_tls（5 #[no_mangle] · mixed · 2 thin+rest find_meta_idx/get_order _impl 桥 pthread_mutexattr protocol lookup + 3 DIRECT append_byte/lit/i32 纯计算 · sync_ 前缀不触发 ast_）。**ast_ 前缀规律验证**：xlang_/crypto_/sync_ 前缀的 #[no_mangle] 函数不触发 ast_ 前缀（wave545-546 规律再确认：只有 net_ 前缀触发）。构建级别：**日常 L2**（双端 prove nm IDENTICAL × 2 + bstrict 129）。mac 本地 prove：2/2 IDENTICAL。Ubuntu prove 金标：2/2 IDENTICAL。Ubuntu L2 真车：bstrict OK (129) exit 0 · 2m38s · 日志 `/tmp/ubuntu_l2_bstrict_86ef18adb.log`。tip `86ef18adb`；钉盘仍 `213bd306b`（wave538-547 L2 收口未升钉）。prove 白名单 63→65（待 N++ 扩 65→75）。PLATFORM: SHARED thin+rest + mixed |
| 2026-07-28 | **wave546** | **net_addr_fast thin+rest + slice_glue DIRECT · L2 真车 129 全绿**。范围：2 模块 prove 文书补全，扩 prove 白名单 +2。模块：runtime_net_addr_fast（1 #[no_mangle] · thin+rest · .x thin 手动字节解析无 libc · seed 4 rest getsockname/getpeername + htonl/htons asm codegen u16 store bug workaround）+ runtime_slice_glue（6 #[no_mangle] · DIRECT · 纯 .x 计算 · 3 slice struct + 3 from_ptr + 3 subslice · seed 全守卫 #ifndef XLANG_RUNTIME_SLICE_GLUE_FROM_X · 无 Cap residual）。**ast_ 前缀验证**：net_addr_fast net_ #[no_mangle] 触发 ast_ 前缀（wave545 规律确认）；slice_glue core_ #[no_mangle] 不触发 ast_ 前缀（新规律：只有 net_ 前缀触发）。构建级别：**日常 L2**（双端 prove nm IDENTICAL × 2 + bstrict 129）。mac 本地 prove：2/2 IDENTICAL。Ubuntu prove 金标：2/2 IDENTICAL。Ubuntu L2 真车：bstrict OK (129) exit 0 · 2m37s · 日志 `/tmp/ubuntu_l2_bstrict_386b555cb.log`。tip `386b555cb`；钉盘仍 `213bd306b`（wave538-546 L2 收口未升钉）。prove 白名单 61→63（待 N++ 扩 63→73）。PLATFORM: SHARED thin+rest + DIRECT |
| 2026-07-28 | **wave545** | **批量 4 模块 R2 prove 文书补全 + .x extern 根修 · L2 真车 129 全绿**。范围：4 net 模块 prove 文书补全，扩 prove 白名单 +4。模块：runtime_net_udp_batch（2 #[no_mangle] · thin+rest · sockaddr_in pack + poll/WSAPoll）+ runtime_net_sock_fast（2 #[no_mangle] · thin+rest · WSAStartup + constructor）+ runtime_net_dns_fast（3 #[no_mangle] · thin+rest · getaddrinfo hints + gai error map + WSA init）+ runtime_net_ipv6_fast（5 #[no_mangle] · thin+rest · IPv6 socket close/nonblock/poll/connect）。**.x extern 根修**：3 个 .x 文件（sock_fast/dns_fast/ipv6_fast）extern 声明用 _impl 后缀但 seed 定义用 _impl_c 后缀，根修 .x extern _impl→_impl_c 匹配 seed（避免未来 PREFER_X_O 启用时产品链接失败）。**ast_ 前缀适配**：3 模块 #[no_mangle] 函数名以 net_ 开头，触发 xlang 编译器给非 #[no_mangle] 函数加 ast_ 前缀（内部符号冲突避免），surface 文件 doc_anchor 用 ast_ 前缀匹配。构建级别：**日常 L2**（双端 prove nm IDENTICAL × 4 + bstrict 129）。mac 本地 prove：4/4 IDENTICAL。Ubuntu prove 金标：4/4 IDENTICAL。Ubuntu L2 真车：bstrict OK (129) exit 0 · 2m35s · 日志 `/tmp/ubuntu_l2_bstrict_5f98c7d36.log`。tip `5f98c7d36`；钉盘仍 `213bd306b`（wave538-545 L2 收口未升钉）。prove 白名单 57→61（待 N++ 扩 61→71）。PLATFORM: SHARED thin+rest |
| 2026-07-28 | **wave544** | **批量 3 模块 R2 prove 文书补全 · L2 真车 129 全绿**。范围：3 模块 prove 文书补全，扩 prove 白名单 +3。模块：runtime_random_fill（2 #[no_mangle] · thin+rest · BCryptGenRandom/getrandom/getentropy Cap residual）+ runtime_tls_mbedtls_bio（2 #[no_mangle] · thin+rest · mbedtls BIO 回调 Cap residual）+ runtime_ed25519_ref10_glue（3 #[no_mangle] · thin+rest · 8 .inc 文件经宏重命名发射 _impl_c · 纯 C 宏 .x 不可表达）。构建级别：**日常 L2**（双端 prove nm IDENTICAL × 3 + bstrict 129）。mac 本地 prove：3/3 IDENTICAL。Ubuntu prove 金标：3/3 IDENTICAL。Ubuntu L2 真车：bstrict OK (129) exit 0 · 2m36s · 日志 `/tmp/ubuntu_l2_bstrict_c3153be8d.log`。tip `c3153be8d`；钉盘仍 `213bd306b`（wave538-544 L2 收口未升钉）。prove 白名单 54→57（待 N++ 扩 57→67）。PLATFORM: SHARED thin+rest |
| 2026-07-28 | **wave537** | **runtime_lsp_glue 一波 R2 收口 L4 真冷全测 双端 129 全绿 · 钉盘升 `213bd306b`**。触发：wave535-536 完成 runtime_lsp_glue R2 迁移（守卫增量 + thin 源统一），按自举方法 §0.2「整波结束做一次扎实 L4」。范围：验证 wave535-536 全部改动：38 函数守卫（21 SKIP + 17 RENAME）+ imply 宏 + 14 dead wrapper 删除 + g05 thin 切换 + lsp_fmt_pure_thin.x 退役。构建级别：**L4 真冷全测**（全擦 .o + 全擦二进制 + 冷编译 bootstrap-driver-seed + g05 + 矩阵 + 全量 bstrict）。mac L4：remaining_o=0 ✅ · 二进制本波 mtime 00:37 ✅ · bstrict OK (129 scripts) exit 0 ✅ · 11m28s。Ubuntu L4：remaining_o=0 ✅ · 二进制本波 mtime 00:50 ✅ · bstrict OK (129 scripts) exit 0 ✅ · 3m25s。判据自查：remaining_o=0 ✓ · 二进制本波 mtime ✓ · bstrict exit 0 + 129 scripts ✓ · 双端结果一致 ✓ · 包含本 SHA `213bd306b` ✓。**钉盘升 `213bd306b`**（升自 `f1250f415`；wave535-536 runtime_lsp_glue R2 迁移）。PLATFORM: SHARED L4 双端 |
| 2026-07-28 | **wave535-536 累积** | **runtime_lsp_glue R2 迁移 · 已 L4 验证通过**。wave535：38 函数守卫（21 SKIP + 17 RENAME）+ 26 前向声明（守卫增量模式）。wave536：14 dead wrapper 删除 + imply 宏（`XLANG_L2_LSP_GLUE_FULL_FROM_X` implies `XLANG_L2_LSP_FMT_THIN_FROM_X`）+ g05 thin 切换（`lsp_fmt_pure_thin.x` 17 `#[no_mangle]` → `runtime_lsp_glue.x` 46 `#[no_mangle]`）+ `lsp_fmt_pure_thin.x` 退役（thin 统一模式）。L2 ✅ · L4 ✅。PLATFORM: SHARED lsp |
| 2026-07-28 | **wave534** | **wave528-532 共 4 个 R2 migration 收口 L4 真冷全测 双端 129 全绿 · 钉盘 `f1250f415`**。范围：wave528-532 共 4 个 R2 migration（http_glue thin+rest + slice_glue/path_fast/std_runtime_fast DIRECT）。mac L4：remaining_o=0 ✅ · bstrict OK (129) exit 0 ✅ · 9m47s。Ubuntu L4：remaining_o=0 ✅ · bstrict OK (129) exit 0 ✅ · 3m25s。钉盘：`f1250f415`（升自 `d5de2f13b`）。PLATFORM: SHARED L4 双端 |

***

## 7. 禁止清单（本阶段）

- [ ] 未用旧 stage1 / 未用归档 tip 冒充当前 tip L4
- [ ] 未把 prove / Stage2 / N++ 单独写成「可自举」
- [ ] 未 BEST_EFFORT / soft-skip 糊绿
- [ ] 未 seed/.x/glue 分叉合入
- [ ] 未把 mac `-dead_strip` 假绿当 Ubuntu 全链已过
- [ ] 日常 L2 未写成 tip L4；谈升钉须双端 L4 + 129
