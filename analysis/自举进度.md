# Xlang / X language 自举进度

> **更新**：2026-07-25 · **wave445 已闭**（generic 函数完整单态化 C4+C5+C6：identity mono body 硬编 `return param0` → 假绿（`foo<T: Clone>{ return x.clone(); }` run=42 但 clone() 未调）；根修 C4 body AST 遍历 + C5 类型替换 T→具体 + C6 方法调用 per-mono 重新解析（param-name fallback + local-let fallback + `codegen_mono_subst_type` name-match + receiver arg0 emit + module 前缀 emit）；PipelineDepCtx SHARED ABI 扩 mono 字段）· 日常 L2 · 钉盘仍 **`70ca1089b`**\
> **基线 SHA（产品 L4 放行钉盘）**：**`70ca1089b`**（升自 `605e7efa4`；含 wave421–431 Cap residual）\
> **产品 bstrict 套件数**：**129**\
> **产品代码 tip**：**wave445（待 commit）**（钉盘 **`70ca1089b`**；generic 函数完整单态化 C4+C5+C6）\
> **git 锚点（重命名前）**：tag **`anchor-pre-xlang-rename`**\
> **现行工程**：品牌/CLI/env/ABI 已 **xlang**（主域 **`xlang.dev`** · 仓库 **`github.com/shuliangfu/x-lang`**）。\
> **本波（25 日）状态**：wave445 日常 L2 已闭（钉盘仍 `70ca1089b`；SHARED ABI 改动 L4 待日终/Ubuntu 批跑）；下一波 **wave446**（待选：dyn Trait / generic 调用无 turbofish 类型推断 XP003 / R2 真迁 / `fn`+`-> Self`+无类型 `self` 语法 typeck gap）。\
> **方法**：[自举方法.md](自举方法.md)（Cap/R/L/M；工程 ≠ 产品放行）\
> **Windows（删种子前）**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[自举进度-归档-2026-07-24.md](自举进度-归档-2026-07-24.md) · [23](自举进度-归档-2026-07-23.md) · [22](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md) · [19](自举进度-归档-2026-07-19.md) · [18](自举进度-归档-2026-07-18.md) · [17](自举进度-归档-2026-07-17.md) · [16](自举进度-归档-2026-07-16.md) · [14](自举进度-归档-2026-07-14.md) · [13](自举进度-归档-2026-07-13.md)

### 维护约定

1. 每完成一波 **必改两文**：本文（KPI/前排/§6）+ [`当前进度.md`](当前进度.md)。
2. **禁止**把 prove/Stage2/N++ 单独写成「可自举」。
3. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**；历史钉盘 125）。
4. 工程 KPI 写 **SHA + 日志路径**；过期整日流水移归档。
5. 大债地图默认写 [`当前问题分析.md`](当前问题分析.md)（承接原 `问题分析文档.md`）；非每微步。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 =** **`70ca1089b`（双端 tip L4 真冷 + 129；wave421–431 收口升钉）。**\
**产品代码 tip =** **wave445（待 commit）**（钉盘 **`70ca1089b`**；日常 L2）。\
**git 锚点（考古）=** tag **`anchor-pre-xlang-rename`**。\
**现行主线 =** **续 Cap residual · wave445 已闭**（钉盘 `70ca1089b`；下一 wave446）。

| 轨                | 状态                                                                                           |
| ---------------- | -------------------------------------------------------------------------------------------- |
| 工程轨              | **wave445 闭**；tip L4 钉盘 **`70ca1089b`**；下一 **wave446** |
| 产品轨 tip / 钉盘     | tip = wave445 · 钉盘 **`70ca1089b`** |
| mac L4 真冷        | ✅ remaining_o=0 · **OK (129)** · `/tmp/mac_true_cold_70ca1089b.log` · `/tmp/mac_true_bstrict_70ca1089b.log` |
| Ubuntu L4（金标）    | ✅ remaining_o=0 · **OK (129)** · `/tmp/ubuntu_true_cold_70ca1089b.log` · `/tmp/ubuntu_true_bstrict_70ca1089b.log` |
| 前钉 L4            | `605e7efa4` · `ace59b6cd` · `75c638465` · … |

***

## 1. 文档地图

| 文档                                             | 看什么                   |
| ---------------------------------------------- | --------------------- |
| [自举方法.md](自举方法.md)                             | Cap / R / L / M       |
| [自举步骤.md](自举步骤.md)                             | 可执行闸门                 |
| [自举验证.md](自举验证.md)                             | 怎么测                   |
| **本文**                                         | 长期 KPI + 前排（**每波必改**） |
| [当前进度.md](当前进度.md)                             | 当天快照（**每波必改**）        |
| **[当前问题分析.md](当前问题分析.md)**                     | tip 级产品债地图（21 日起权威）   |
| [自举进度-归档-2026-07-24.md](自举进度-归档-2026-07-24.md) | **24 号全日**（只读）        |
| skill `xlang-selfhost-product-gate`             | L4 / 双端 / 假绿禁止        |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项                          | 结果                                                                                  | 日志 / 备注                                      |
| -------------------------- | ----------------------------------------------------------------------------------- | -------------------------------------------- |
| 产品 bstrict 套件数             | **129**                                                                             | 现行白名单                                        |
| 产品 L4 放行钉盘                 | **`70ca1089b`**                                                                      | 25 日 tip L4 升钉（前钉 `605e7efa4`）；双端 L4 129      |
| 本波 tip                     | **wave445（待 commit）**（钉盘 **`70ca1089b`**） | generic 函数完整单态化 C4+C5+C6 债（identity mono body 硬编 `return param0` → 假绿 `foo<T: Clone>{ return x.clone(); }` run=42 但 clone() 未调；根修 C4 `codegen_try_emit_generic_identity_mono` 复用 `emit_block`/`emit_expr` 遍历 body AST + C5 `emit_type` 类型替换 hook（T→具体，含 TYPE_NAMED 名称匹配 fallback）+ C6 `emit_expr` EXPR_METHOD_CALL mono 分支（param-name fallback + local-let fallback 推断 receiver 类型 + `codegen_mono_subst_type` name-match 替换 + `codegen_find_impl_method_for_type` 查 impl 方法 + receiver arg0 emit + module 前缀 emit）；PipelineDepCtx SHARED ABI 扩 `mono_active`/`mono_num_types`/`mono_generic_type_refs[8]`/`mono_concrete_type_refs[8]`）· **日常 L2** |
| mac 产品链 bstrict（钉盘 L4） | ✅ **OK (129)**                                                                      | L4 · `/tmp/mac_true_bstrict_70ca1089b.log` · cold `/tmp/mac_true_cold_70ca1089b.log`    |
| Ubuntu L4（金标 · 钉盘）  | ✅ **OK (129)**                                                                      | L4 · `/tmp/ubuntu_true_bstrict_70ca1089b.log` · cold `/tmp/ubuntu_true_cold_70ca1089b.log` |
| 产品矩阵（双端 tip L4）           | ✅ rv42 / option / hello（§4）+ bstrict 129                                                                | mac + Ubuntu · `70ca1089b`                                      |
| 前钉 mac/Ubuntu L4           | ✅ 129 @ `605e7efa4` · `ace59b6cd` · …                                                                | 前钉日志 `/tmp/*_605e7efa4.log`                             |

### 2.2 工程轨（继承）

| KPI           | 现                                        | 备注                   |
| ------------- | ---------------------------------------- | -------------------- |
| **T / EMPTY** | 18/18                                    | 18 号结案               |
| **N**         | 54/54 IDENTICAL                          | 回归基线                 |
| **R2 / 根修本波** | **wave445** generic 函数完整单态化 C4+C5+C6 债（identity mono body 硬编 `return param0` → 假绿 `foo<T: Clone>{ return x.clone(); }` run=42 但 clone() 未调；根修：C4 `codegen_try_emit_generic_identity_mono` 复用 `emit_block`/`emit_expr` 遍历 body AST（替代硬编 `return param0`）+ C5 `emit_type` 类型替换 hook（mono_active=1 时 T→具体，含 TYPE_NAMED 名称匹配 fallback）+ C6 `emit_expr` EXPR_METHOD_CALL mono 分支（param-name fallback + local-let fallback 推断 receiver + `codegen_mono_subst_type`（含 name-match fallback）+ `codegen_find_impl_method_for_type` + receiver arg0 emit + module 前缀 emit）+ PipelineDepCtx SHARED ABI 扩 mono 字段） | 日常 L2 · 钉盘仍 **`70ca1089b`** |

***

## 3. 当前前排（≤3 条）

1. **wave446 候选**（任选一）：① dyn Trait（vtable + fat pointer 设计，多波主特性）② generic 调用无 turbofish 类型推断（XP003，根源障碍 `ast_Func` 不存泛型名需 L4 ABI 扩展）③ 转 R2 真迁（产品矩阵已绿）④ `fn`+`-> Self`+无类型 `self` 语法 typeck gap（w445_clone_transform.x：typeck 未从 impl 上下文推断 self 类型 → `codegen_find_impl_method_for_type` param0 type_ref=0 → BLD001）· 日常 L2
2. leave-off soft：dyn Trait；generic 调用无 turbofish 类型推断（XP003）；`fn`/`-> Self`/无类型 `self` 语法 typeck gap；Ubuntu 失败路径次级 diag 139；**wave445 SHARED ABI 改动（PipelineDepCtx 扩 mono 字段）L4 待日终/Ubuntu 批跑**
3. **钉盘 `70ca1089b`**；改 seed typeck/codegen/parser_gen **必须**同 commit 同步链上 pin；日终若动产品面可考虑整体收口 L4

***

## 4. 与 24 号归档的关系

- 24 号全日（Cap residual pure **wave295–395**、slice/array/match/goto/mut/panic/trait/arm64 多叶、tip L4 升钉 **`605e7efa4`**）已整表移入 [`自举进度-归档-2026-07-24.md`](自举进度-归档-2026-07-24.md)。
- **勿**用归档 tip 或旧钉（`605e7efa4` / `ace59b6cd` / `75c638465` 等）冒充现行 tip L4（产品钉盘 **`70ca1089b`**；产品 tip wave431 **`15a09c5c5`**）。现行 wave437 为日常 L2（非 tip L4）。
- 25 日新工作只记本文 §0/§2/§3/§6。

***

## 5. 双端 / 平台

| 平台                     | 本波状态                                                              |
| ---------------------- | ----------------------------------------------------------------- |
| **macOS arm64**        | ✅ tip L4 全擦 + 矩阵 + **129** @ **`70ca1089b`**；wave445 日常 L2 探针（method_call/let_binding/no_method/clone_v2=42）+ 矩阵 + bstrict 129 OK（588s） |
| **Ubuntu x86\_64（金标）** | ✅ tip L4 全擦 + 矩阵 + **129** @ **`70ca1089b`**；wave445 待跑（日终批跑；SHARED ABI 改动 L4 待跑） |
| **Windows MSYS2**      | 继承 hybrid；本波未重跑（**不需要切换**）；路径文档已 `…\xlang\x-lang`                                             |

***

## 6. 变更记录（倒序 · 25 日起；24 号见归档）

| 日期         | SHA                                                     | 摘要                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------- | ------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-25 | **wave445（待 commit）** | **R2 Cap residual pure wave445**：**generic 函数完整单态化 C4+C5+C6 债**（identity mono body 硬编 `return param0` → 假绿：`foo<T: Clone>(x: T): T { return x.clone(); }` 编译通过 run=42 但 clone() 从未调用）。根因：wave444 的 `codegen_try_emit_generic_identity_mono`（C1+C2+C3）解决了符号冲突但 body 仍硬编 `return param0`（identity shape），非 identity body（含方法调用 `x.clone()`）的泛型函数体从未被遍历 emit → 方法调用被静默跳过 → run=42 假绿。权威：codegen.x（codegen 单源），同步 seed `codegen_gen.linux.x86_64.c` + runtime_pipeline_abi.h + ast.x + emit.x（PipelineDepCtx SHARED ABI 扩展）。根修（C4+C5+C6）：① **C4 body AST 遍历** — `codegen_try_emit_generic_identity_mono` 替换硬编 `return param0` 为 `emit_block`/`emit_expr` 遍历 body AST（设置 `ctx.mono_active=1` + `mono_generic_type_refs`/`mono_concrete_type_refs` 映射 T→具体类型）；② **C5 类型替换** — `emit_type` 添加类型替换 hook（`mono_active=1` 时，type_ref 匹配 `mono_generic_type_refs[i]` 则替换为 `mono_concrete_type_refs[i]`；TYPE_NAMED 走名称匹配 fallback——`pipeline_type_named_name_into` 比较类型名，解决 `let y: T` 的 T type_ref 与 param T type_ref 不同节点问题）；③ **C6 方法调用 per-mono 重新解析** — `emit_expr` EXPR_METHOD_CALL 分支添加 mono 分支，receiver 类型推断三段式：`pipeline_expr_resolved_type_ref` 返 0 时先按 `base_mono.var_name` 匹配 param name（`x.dup()`），再匹配 local let binding name（`y.dup()`，`pipeline_block_let_type_ref` 取 let 声明类型 T）→ `codegen_mono_subst_type`（含 name-match fallback，typeck 为 body T 分配新 TYPE_NAMED 节点致直接 type_ref 比较失败时按名称匹配）替换 T→具体 → `codegen_find_impl_method_for_type`（含 name-match fallback）在 `module->funcs` 查 impl 方法 → emit `<module 前缀><link_name>(<receiver arg0>, <explicit args>)`（receiver 作 self 首参 emit，module 前缀从 `ctx.current_codegen_prefix_mirror` 取防与 libc `dup(int)` 冲突）。**PipelineDepCtx SHARED ABI 扩展**（append-only，字段在结构体末尾）：`mono_active`/`mono_num_types`/`mono_generic_type_refs[8]`/`mono_concrete_type_refs[8]`（runtime_pipeline_abi.h 权威 + ast.x/emit.x 镜像）。**假绿已修复**：`foo<T: Clone>{ return x.clone(); }` 从 run=42（假绿）→ run=42（真正调用 `w445_method_call_dup(x)` 返 42，clone 执行）。**mac** w445_method_call（`x.dup()` param receiver run=42）· w445_let_binding（`let y: T; y.dup()` local-let receiver run=42；C5 类型替换 `let y: T`→`let y: A` 生效无 `ast_T`）· w445_no_method（identity mono `foo<T>{ return x }` run=42）· w445_clone_v2（`function`+显式 `self: A` 语法 `x.clone()` run=42）· w445_clone_transform（`fn`+`-> Self`+无类型 `self` 语法 typeck gap：typeck 未从 impl 上下文推断 self 类型 → param0 type_ref=0 → `codegen_find_impl_method_for_type` 不匹配 → BLD001，typeck 债非 codegen）· wave444 regression（multi_mono/three_mono/single/multi_arg=42）· wave443 regression（multi_arg/three_arg/single_ret/bound_ok/compare/no_bound=42）· wave439 bound regression（bound_violation 红 T001 / bound_ok/minimal/no_bound=42）· 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（588s）。soft：dyn Trait；generic 调用无 turbofish 类型推断（XP003）；`fn`/`-> Self`/无类型 `self` 语法 typeck gap；Ubuntu 失败路径次级 139。mac L2（SHARED ABI 改动 L4 待日终/Ubuntu 批跑）。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED codegen+ABI · **日常 L2 非 tip L4** |
| 2026-07-25 | **`cc721e73d`** | **R2 Cap residual pure wave444**：**generic mono mangled symbols + 多实例 emit 债**（`copy<A>(a)` + `copy<i32>(n)` 同泛型函数多类型调用 → 单实例化共享裸 link_name → C link 冲突/类型不匹配 BLD001）。根因：identity mono 早期实现仅生成首个 call site 的 mono 实例并用裸 link_name（`codegen_emit_func_link_name`），多 type-arg combo 无法共存 → wave443 多参数 emit 后多类型调用暴露符号冲突。权威：codegen.x（codegen 单源），同步 seed `codegen_gen.linux.x86_64.c`。根修（C1+C2+C3）：① C1 `codegen_emit_mono_mangled_name` 生成 `<link_name>__<suffix>` mangled 符号（双下划线分隔防与 overload `_` mangling 冲突，suffix 复用 `codegen_type_ref_to_suffix` 权威）；② C2 `codegen_collect_mono_combos_for_generic_func` 扫 arena 收集所有 (func, type-args) 组合并去重（≤16 combos × ≤8 params，匹配逻辑复用 `find_mono_type` 的 `call_resolved_func_index==fi` OR callee name==fn_local`）；③ C3 `codegen_emit_call_func_name` call site 按 type args 构造 mangled 符号（与 emit 侧 combo 对齐，type 提取失败 fallback 裸 link_name 保旧行为）；④ 重构 `codegen_try_emit_generic_identity_mono` 循环 per-combo emit mangled 实例（替代单实例查找）。另修：pn_len 重构回归（`pipeline_module_func_name_len_at` → `pipeline_module_func_param_name_len_at`；pn 持 param0 名非函数名）+ seed C 三新函数 null-check 括号不匹配（手写 nested-|| 翻译器括号风格难对齐，改 split-if 早返回语义等价）。body 仍硬编 `return param0;`（identity shape）；完整 body emit（C4+C5）+ trait 方法体（C6）为后续波。**mac** w444_multi_mono_ok（`copy<A>` + `copy<i32>` 双实例 run=42）· w444_three_mono_ok（3 combo run=42）· w444_single_regression（单参 identity=42）· w444_multi_arg_regression（多参 identity=42）· wave443 regression（multi_arg/three_arg/single_ret/bound_ok/compare/no_bound=42）· bound_violation/multi_bound_viol 红 T001 · 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（618s）。soft：dyn Trait；generic 调用无 turbofish 类型推断（XP003）；generic 函数完整单态化（body 仍硬编返回 param0，trait 方法体 `x.clone()` 不 emit → BLD001，C6 待调研）；Ubuntu 失败路径次级 139。mac L2（Ubuntu 待跑可日终批跑）。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED codegen · **日常 L2 非 tip L4** |
| 2026-07-25 | **`f0c7449e3`** | **R2 Cap residual pure wave443**：**多参数 identity mono 债**（`copy_both<T,U>` 因 `codegen_try_emit_generic_identity_mono` 门控 `num_params != 1` 返回 0 → 调用方 `continue` 跳过 → 永不 emit → 调用点 BLD001 undeclared）。根因：identity mono 早期实现仅支持单参 identity shape（`fn(x: T): T { return x; }`），多类型参数泛型函数（`copy_both<T,U>`）被硬门控跳过 → wave442 修复 multi-arg bound 后 BLD001 主因暴露。权威：codegen.x（codegen 单源），同步 seed `codegen_gen.linux.x86_64.c`。根修：① `codegen_try_emit_generic_identity_mono` 放宽门控至 N 参数（`num_params <= 0 || num_params > 8` 才返回 0，支持 2..8 个参数）；② `codegen_find_mono_type_for_generic_func` 补 `arg_idx` 参数按位置取 call site arg 类型 — `arg_idx == 0` 时保留 `e.resolved_type_ref` 快捷路径（identity 形状下 ret==param0）；`arg_idx > 0` 时**不**使用 resolved_type_ref 快捷路径（返回类型仅对 param0 有效），改走 `pipeline_expr_call_arg_ref(arena, ei, arg_idx)` + `pipeline_expr_resolved_type_ref(arena, a0)` 取对应位置 arg 类型；③ 循环 emit param 1..N-1 的 mono 类型与名称（param0 已由调用点 emit）。**mac** w443_multi_arg_ok（`copy_both<A,i32>` run=42）· w443_three_arg_ok（三参数泛型 run=42）· w443_bound_violation_regression（wave439 bound 检查不回归 红 T001）· w443_single_ret_regression（单参 identity 不回归=42）· w443_multi_bound_viol（wave442 multi-arg bound 不回归 红 T001）· w443_bound_ok/compare/no_bound（wave439/440 不回归=42）· 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（626s）。soft：dyn Trait；generic 调用无 turbofish 类型推断（XP003）；generic 函数完整单态化（body 仍硬编返回 param0，trait 方法体 `x.clone()` 不 emit）；Ubuntu 失败路径次级 139。mac L2（Ubuntu 待跑可日终批跑）。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED codegen · **日常 L2 非 tip L4** |
| 2026-07-25 | **`f4c4b7327`** | **R2 Cap residual pure wave442**：**multi-arg generic bound 假绿**（`copy_both<T:Clone,U:Default>` 调用 `copy_both<A,B>` 时 B 违反 `U:Default` 未报 T001 落 BLD001）。根因：wave439 实现的 `xlang_generic_bound_check_c`（skip_tl_slice.inc）scan 阶段只捕首个 type arg 入单槽 `g_call_typearg[ci]`，check 阶段对每条 bound 都用首 type arg 比对 → 多 type arg 泛型调用只校验 T0，T1+ 违反 bound 不报 → 假绿落 BLD001 undeclared（多类型参数泛型函数 codegen 限制不 emit 的次级症状掩盖了主因）。权威：G.7 单源 thin-glue TU。根修：① scan 阶段扩展捕全 type args — `g_call_typeargs[ci][pos][64]`（最多 4 args，宏 `XLANG_GENERIC_CALL_MAX_ARGS`）+ `g_call_typearg_lens[ci][pos]` + `g_call_nargs[ci]`（计数），angle list 内遇 `,` 切下一 pos 继续捕 IDENT；② scan 阶段同时记每条 bound 的位置 `g_fn_bound_pos[bi]`（bound 声明时第 N 个形参对应 type arg pos N）；③ check 阶段按 `pos` 取对应 type arg 比对 trait（`pos = g_fn_bound_pos[bi]`，越界 skip），而非全用首 arg。**mac** w441_multi_bound_viol（`copy_both<A,B>` B 未 impl Default）红 T001 :10:23 · w442_three_arg_viol（三 type param 第二个违反 bound）红 T001 · wave438-441 regression（单参泛型 bound_violation 红/bound_ok=42、compare `a<b` 不误捕=42、no_bound=42）无回归 · 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（600s）。soft：dyn Trait；generic 调用无 turbofish `<T>` 类型推断（XP003，`ast_Func` 不存泛型名需 L4 ABI 扩展）；generic 函数完整单态化（多类型参数 `foo<T,U>` 与 trait 方法体 `x.clone()` 不 emit → BLD001）；Ubuntu 失败路径次级 139。mac L2（Ubuntu 待跑可日终批跑）。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`fed3fc2b0`** | **R2 Cap residual pure wave441**：**两个 struct impl 同一 trait 假红**（method clone self type mismatch）。根因：`xlang_trait_check_impls_complete_c`（skip_tl_slice.inc）+ `parser_asm_seed_check_impl_missing_methods_c`（seed_parse_into_buf_slice.inc）方法查找 `found_fi` 只按方法名全 module 扫，找到第一个同名方法就 break，没有限定到当前 impl 块 → 两个 struct（A/B）impl 同一 trait（Clone）时，检查 B 的 impl 找到 A 的 clone 方法（self: A），wave428 self 检查 param0（A）≠ for（B）→ 假红 "self type mismatch"。权威：G.7 双轨单源。根修：① 提取 `xlang_{skip,seed}_impl_self_matches_for_c` helper（self 类型匹配 for 类型，复用 wave428 检查逻辑消除重复）— for_k<0 或 got0<0（untyped self）时返回 1（保旧行为）；PTR 分支 walk elem kind/name；NAMED 分支 memcmp name；② 查找循环优先选 self 类型匹配 for 的方法（两 impl 同 trait 时按 self 区分），无匹配时 fallback 到首个同名方法（静态方法 w/o self 的 param0≠for-type → helper 返 0 → fallback 保旧行为，不回归 signature-only 检查）；③ wave428 self 检查改调 helper 消除重复逻辑。**mac** two_impl（A/B impl Clone）run=42 · multi_impl_call（3 struct + 方法调用）run=60 · p2_bound_ok（copy<A>(a) A impls Clone）=42 · p3_bound_viol（copy<B>(b) B 未 impl Clone）红 T001 :9:23 · wave432-438 regression mm 红（parameter/return type mismatch）/ok=42 · r_ok2（两 struct self 方法调用）run=142 · 矩阵 rv=42/hello · bstrict 129 OK（673s）。soft：dyn Trait；generic 调用无 turbofish `<T>` 类型推断（XP003，根源障碍 `ast_Func` 不存泛型名需 L4 ABI 扩展）；Ubuntu 失败路径次级 139。mac L2（Ubuntu 待跑可日终批跑）。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`016d42ca1`** / docs | **R2 Cap residual pure wave440**：**generic bound diag 行号偏 -1**（wave439 引入的 soft 债）。根因：`xlang_generic_bound_scan_c` 用 `memset(&lex, 0, sizeof(lex))` 初始化 lexer 致 `lex.line=0`/`lex.col=0`；但 lexer 约定（`lexer_init` in lexer.x）为 `line=1`/`col=1` — `advance_one` 仅 newline 时 `line++` 且不 reset（line 累积），newline 时 `col=1` reset（col 仅 line 1 受影响）→ 所有报错行号差一（`copy<B>(b)` 在 line 23，报错指向 line 22 `b.w = 99;`）。权威：`lexer_init`（lexer.x:1392 `Lexer { pos: 0, line: 1, col: 1 }`）。根修：`memset` 后补 `lex.line = 1; lex.col = 1;`（与 `lexer_init` 一致）。**mac** bound_violation T001 现 `:23:23`（caret 指 `(` in `copy<B>(b)`；原 `:22:23` 指上一行）· bound_minimal/bound_ok=42 · no_bound/compare=42（无误捕）· wave438 regression ret_arr_elem_mm 红/cast_only 绿=42 · wave439 bound check 全探针过 · 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（614s）。soft：dyn Trait；generic 调用无 turbofish `<T>` 类型推断（XP003）；Ubuntu 失败路径次级 139。mac L2（Ubuntu 待跑可日终批跑）。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`d0a3830cc`** / docs | **R2 Cap residual pure wave439**：**generic trait bound 假绿**（`copy<B>(b)` 其中 B 未 impl bound trait 仍过编 run=42）。根因：编译器未验证泛型调用点 `foo<Type>(...)` 的类型实参是否实现了函数声明的边界 trait（`function foo<T: Trait>`）→ B 未 impl Clone 仍过编 run=42 假绿。权威：G.7 单源 thin-glue TU（skip_tl_slice.inc 内 scan+check，不双轨因 scan 在 thin glue TU 内、复用 trait check 调用点）。scan：`xlang_generic_bound_stash_source_c` 顶层 skip 函数（struct/enum/trait/impl）暂存全文件源码切片指针（trait check 仅收 *Module 无法 token 化源码）；`xlang_generic_bound_scan_c` token 流扫两类模式 — ① 边界声明 `function IDENT < IDENT : IDENT >`（存 fn_name→trait_name 对；`<` 后先 `lex=r.next_lex` 前进过 `<` 防 angle_depth 膨胀，内层循环 `IDENT : IDENT` 捕 bound trait 名）；② 调用点 `IDENT < IDENT > (`（存 callee→typearg+line+col；仅 `>` 后跟 `(` 才记，避免 `a < b` 比较误捕；嵌套泛型 `Vec<i32>`（angle_depth>1）soft-skip）；scan 在 `xlang_trait_check_impls_complete_c` 末尾调用保证产品路径必跑。check：`xlang_generic_bound_check_c` 对每个有 bound 的调用点查 impl registry `g_xlang_skip_impl_trait`/`g_xlang_skip_impl_for_name` 验证 `impl Trait for Type` 存在 → `generic bound not satisfied: %s does not impl %.*s`（T001）。**mac** bound_violation（B 未 impl Clone）红（T001 generic bound not satisfied）· bound_minimal/bound_ok（A impls Clone）=42（parse OK + run=42）· no_bound（非泛型 fn）=42 · compare `a < b` 不误捕（=42）· 嵌套泛型 soft-skip（=42）· wave438 regression ret_arr_elem_mm 红/cast_only 绿=42 · 矩阵 rv=42/option/hello(run=0, Hello World\n)/stdlib-import · bstrict 129 OK（609s）。soft：dyn Trait；bound diag 位置偏（报错 line/col 差一）；generic 调用无 turbofish `<T>` 类型推断（XP003）；Ubuntu 失败路径次级 139。mac L2（Ubuntu 待跑可日终批跑）。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`1807ddceb`** / docs | **R2 Cap residual pure wave438**：**trait 方法返回值 `*[]T`/`*[N]T`/`*[N][M]T` 类型不匹配假绿 + `as *[N]T` cast parse fail 假红**（参数侧 wave434-437 对称移植到返回值侧）。根因双轨：① 返回值侧状态机未扩展（wave434-437 仅扩展参数侧 `param_elem_*`，返回值侧 `ret_elem_*` 仍 soft-skip）→ trait ret `*[3]i32` vs impl `*[3]u8` 元素类型错/维度错仍过编 run=42；② `parser_asm_as_suffix_slice.inc` STAR 分支仅支持 `as *T`（简单指针，调 `parser_asm_alloc_pointee_type_ref_from_tok_c`），见 `as *[N]T` 的 `[` 返 0 → 整函数解析失败 → impl 方法被丢弃 → trait checker 报「missing method f」（对任意含 `as *[N]T` 转换的 impl 假红，含同形 c1）。权威：G.7 双轨单源。registry：skip_tl/seed 返回值侧对称扩展 `ret_elem_slice_need_rb`/`ret_elem_prefix_arr_need_rb`/`ret_elem_elem_pending`/`ret_elem_arr_need_rb`/`ret_elem_arr_need_size`/`ret_elem_suffix_pending`/`ret_elem_prefix_arr_more`（与参数侧 wave434-437 `param_elem_*` 同构）；`ret_elem_elem_pending` 见 `[` 转 `ret_elem_prefix_arr_more=1` 收集多维 dims；提交 dims 后 reset `ret_elem_dim_n=0` 防 postfix lift 误覆盖；check `gek==ek && eek==SLICE/ARRAY` 分支 walk pipeline PTR→elem(SLICE/ARRAY)→dims→leaf 递归比对 elem_elem（base T）→ `return type mismatch`。as_suffix：STAR 分支委托 `parser_asm_parse_type_ref_for_arena_into_slice_c`（复用 type_ref_slice.inc 完整类型解析，支持 `as *[N]T`/`as *[]T`/`as *[N][M]T`）；`pthin_expr_as_suffix.from_x.c` 补 extern 声明；移除手工 `tptr`/`elem_ref` 局部（已由委托覆盖）。**mac** ret_arr_elem_mm（`*[3]i32` vs `*[3]u8` elem 错）/ret_arr_sz_mm（`*[3]i32` vs `*[5]i32` size 错）红（T001 return type mismatch）· ret_slice_elem_mm（`*[]i32` vs `*[]u8` elem 错）红 · ret_arr2_elem_mm（`*[2][3]i32` vs `*[2][3]u8` elem 错）/ret_arr2_sz1_mm/ret_arr2_sz2_mm 红 · ret_arr_ok2/ret_slice_ok/ret_arr2_ok=42（parse OK + run=42）· cast_only（`as *[3]i32` 解析成功，方法不再 missing）绿 · simple `as *i32`/`*i32` ret 无回归 · wave437/436/435 regression mm 红/c1 绿=42 · 矩阵 rv=42/option/hello/import · bstrict 129 OK（601s）。soft：dyn Trait；generic bounds；Ubuntu 失败路径次级 139。mac L2（Ubuntu 待跑可日终批跑）。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`6e36d441f`** / docs | **R2 Cap residual pure wave437**：**trait 方法形参 `*[N][M]T` 多维 PTR-to-ARRAY 假绿**（wave436 soft-skip -1 → 真 check）。根因：wave436 为 `*[N][M]T`（PTR to multi-dim ARRAY）走 soft-skip — `param_elem_elem_pending` 见 `[` 即设 `elem_kinds=-1`（保 wave434 行为）→ check 跳过 → 同形 impl 假绿、元素类型错/任一维度错仍过编 run=42；wave436 已为 `*[N]T` 单维实现真 check（`param_elem_prefix_arr_need_rb` + `param_elem_elem_pending` 捕 base T），但多维仍 soft-skip。权威：G.7 双轨单源。registry：skip_tl/seed `param_elem_elem_pending` 见 `[` 不再 soft-skip -1，转 `param_elem_prefix_arr_more=1` + `param_elem_arr_need_size=1` 收集下一维 INT N，`param_elem_arr_need_rb` 见 `]` 后回 `param_elem_elem_pending` 捕 base T；base T 捕获时提交累积 dims 到 `method_param_elem_array_ndims/dims`；**关键 bugfix**：提交 dims 后必须 reset `param_elem_dim_n=0`，否则 wave434 `*T[N]` postfix lift 在 `,`/`)` 时因 `param_elem_dim_n>0` 误触发，把正确的 PTR-of-ARRAY 覆盖为 ARRAY-of-PTR（导致 wave436 c1 单维 `*[3]i32` 假红 — 这是 wave437 初版引入的回归，根因是 dims 提交点从 `param_elem_prefix_arr_need_rb` 延迟到 `param_elem_elem_pending` 但未同步搬移 reset）。check：复用 wave436 `gek==ek && eek==ARRAY` 分支 walk pipeline PTR→elem(ARRAY)→dims[N][M]→leaf 比对 elem_elem（base T）→ `parameter type mismatch`（多维 dims 循环 wave436 已支持，无需改动 check）。**mac** m1_ptr_arr2_elem_mm（`*[2][3]i32` vs `*[2][3]u8` elem 错）/m2_ptr_arr2_sz1_mm（`*[2][3]i32` vs `*[5][3]i32` 第一维 size 错）/m3_ptr_arr2_sz2_mm（`*[2][3]i32` vs `*[2][5]i32` 第二维 size 错）红（T001 parameter type mismatch）· c1_ptr_arr2_ok=42（parse OK + run=42）· wave436 regression mm 红/c1 绿=42 · wave435 regression mm 红/c1 绿=42/edge 绿 · wave434 regression mm 红/c1 绿=42 · wave433 regression mm 红/correct 绿=42 · 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（600s）。soft：dyn Trait；generic bounds；Ubuntu 失败路径次级 139。mac L2（Ubuntu 待跑可日终批跑）。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`5f3486baf`** / docs | **R2 Cap residual pure wave436**：**trait 方法形参 `*[N]T` PTR-to-ARRAY 假绿**（wave435 soft-skip -1 → 真 check）。根因：wave435 为 `*[N]T`（PTR to ARRAY）走 soft-skip — registry `param_elem_slice_need_rb` 见 INT N 即设 `elem_kinds=-1`（保 wave434 行为）→ check 跳过 → 同形 impl 假绿、元素类型错/数组维度错仍过编 run=42；wave435 已为 pipeline `*[` 递归解析铺路（`parser_asm_type_ref_slice.inc` STAR 后递归 `parse_type_ref_impl_c`），registry 侧却未跟上。权威：G.7 双轨单源。registry：skip_tl/seed `param_elem_slice_need_rb` 见 INT N 时改设 `elem_kinds=ARRAY`（不再 -1）+ 捕获 N 入 `param_elem_dims`；新增 `param_elem_prefix_arr_need_rb` 状态等 `]` 闭合 → 存 `method_param_elem_array_ndims/dims` → 转 `param_elem_elem_pending`（复用 wave435 slice 路径）捕获 base T（NAMED name / builtin）；`param_elem_elem_pending` 新增 `[` 分支软跳过多维 `*[N][M]T`。check：`gek==ek && eek==ARRAY` 分支 walk pipeline PTR→elem(ARRAY)→dims[N]→leaf 比对 elem_elem（base T）→ `parameter type mismatch`（与 wave433 ARRAY walk 对称，但作用于 elem 层）。**mac** m1_ptr_arr_elem_mm（`*[3]i32` vs `*[3]u8` elem 错）/m2_ptr_arr_sz_mm（`*[3]i32` vs `*[5]i32` size 错）/m3_ptr_arr_named_mm（`*[2]Point` vs `*[2]Other` named 错）红（T001 parameter type mismatch）· c1_ptr_arr_ok=42（parse OK + run=42）· wave435 regression mm 红/c1 绿=42 · wave434 regression mm 红/c1 绿=42 · wave433 regression mm 红/correct 绿=42 · wave435 edge `*[N]T` trait decl 绿（从 soft-skip 升为真 check 仍不假红）· 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（667s）。soft：dyn Trait；generic bounds；`*[N][M]T` 多维 PTR-to-ARRAY（registry soft-skip -1）；Ubuntu 失败路径次级 139。mac L2（Ubuntu 待跑可日终批跑）。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`de6dad1a5`** / docs | **R2 Cap residual pure wave435**：**trait 方法形参 `*[]T` pointer-to-slice 假绿 + pipeline `*[` parse fail 假红**。根因双轨：① trait registry（skip_tl/seed）wave434 对 `*[]T` soft-skip（elem=-1，check 跳过）→ 同形 impl 假绿、元素类型错仍过编；② pipeline parser（`parser_asm_type_ref_slice.inc` `parse_type_ref_impl_c`）STAR 分支只支持标量 pointee（`alloc_pointee_type_ref_from_tok_c`），见 `*[` 返 0 → 整函数解析失败 → impl 方法未 hoist → trait checker 报「missing method f」（对任意带 `*[]T` 形参的 impl 假红，含同形 c1）。权威：G.7 双轨单源。pipeline：STAR 后若下一 token 是 `[`，递归调 `parse_type_ref_impl_c` 解析内部 slice/array，再包 PTR；标量路径不变；C 风格后缀 `*T[N]`=ARRAY of PTR（`parse_postfix_array_type_ref_c`）保留。registry：新增 `param_elem_slice_need_rb` 状态区分 `*[]T`(SLICE, 捕获 base T)/`*[N]T`(INT N, soft-skip -1 保 wave434 行为)；新增 `param_elem_elem_pending` 状态捕获 slice base T（NAMED name / builtin）；check `gek==ek && eek==SLICE` 分支 walk pipeline SLICE leaf → elem → elem_elem（slice base T）比对 → `parameter type mismatch`。**副作用修复**：wave434 m3 探针（之前是 soft-skip 副作用「missing method f」）现正确报 T001 parameter type mismatch。**mac** m1_ptr_slice_elem_mm/m2_ptr_slice_named_mm/m3_ptr_slice_sz_mm 红（T001 parameter type mismatch）· c1_ptr_slice_ok=42（parse OK + run=42）· diag 红 · pipe_ok 绿（pipeline 支持 `*[]T`）· edge `*[N]T` trait decl 绿（soft-skip 不假红）· wave434 regression mm 红/correct 绿（m3 改善）· wave433 regression mm 红/correct 绿 · 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（586s）。soft：dyn Trait；generic bounds；`*[N]T` PTR-to-ARRAY（registry check 未实现，pipeline 已绿）；Ubuntu 失败路径次级 139。mac L2（`/tmp/mac_g05_wave435.log`；Ubuntu 待跑可日终批跑）。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`5f57abb98`** / docs | **R2 Cap residual pure wave434**：**trait 方法形参嵌套 `*T[N]` postfix wrap 假绿**。根因：wave433 仅捕获参数 ARRAY 后缀的顶层维度（`method_param_array_ndims`/`dims`），未处理指针元素为数组的情况 → `*T[N]` 在 wave433 解析为 PTR（见 `*` 即收 elem），但类型语法 `[]` 优先级高于 `*`，应解析为 ARRAY of PTR；impl `i32[3]`（ARRAY of PTR）vs trait `*i32[2]`（registry PTR）kind 不等 → 同形假红；impl scalar `*i32` 与 trait PTR 顶层等 → 假绿。权威：G.7 skip_tl/seed 双生 `method_param_elem_elem_kinds` + `method_param_elem_array_ndims` + `method_param_elem_array_dims`（指针元素为数组时的叶子类型与维度）；解析状态机扩 `param_elem_suffix_pending`（指针 elem 后保持扫描 `[`）+ `param_elem_arr_need_size`（见 `[` 等 INT/`]`）+ `param_elem_arr_need_rb`（见 `[N` 等 `]`）；finalize 将 `*T[N]` 提升为 ARRAY of PTR（kind=ARRAY, elem=PTR, elem_elem=T, dims 复用顶层字段）；check `gek==ek && ek==PTR` 分支 walk pipeline ARRAY leaf（PTR）→ elem → elem_elem（`*T` base）比对 → `parameter type mismatch`。**mac** m1_ptr_arr_sz_mm/m2_ptr_arr_scalar_mm 红（T001 parameter type mismatch）· m3_ptr_slice_elem_mm 红（副作用 missing method f，`*[]T` 仍 soft 非真比对）· c1_ptr_arr_ok=42 · wave433 regression mm 探针仍红/correct 仍绿 · 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（604s 附赠非 L2 必需）。soft：dyn Trait；generic bounds；`*[]T`（指针指向切片，elem 未捕获）；Ubuntu 失败路径次级 139。mac L2（`/tmp/mac_g05_wave434.log`；Ubuntu 待跑可日终批跑）。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`5fca9ad38`** / docs | **R2 Cap residual pure wave433**：**trait 方法形参后缀 `T[N]`/`T[N][M]` 假红/假绿**。根因：wave432 仅捕获 NAMED/PTR/SLICE 形参 elem kind，未处理 ARRAY 后缀 → trait `x: i32[2]` 注册为 TYPE_I32（解析器见 IDENT 即停）；impl `i32[2]`/`i32[3]` 在 pipeline 为 TYPE_ARRAY → 比对 kind 不等 → 同形假红；impl scalar `i32` 与 trait I32 顶层等 → 假绿。权威：G.7 skip_tl/seed 双生 `method_param_array_ndims` + `method_param_array_dims`（与 wave431 ret 对称）；解析状态机扩 `param_suffix_pending`（base IDENT/builtin 后保持扫描）+ `param_arr_need_size`（见 `[` 等 INT/`]`）+ `param_arr_need_rb`（见 `[N` 等 `]`）；`T[]` → TYPE_SLICE 旧路径保留；finalize 在 `,`/`)`/非 `[` token 时若 dims>0 则改 kind 为 ARRAY + 写 ndims/dims；check `got_pk==ek && ek==ARRAY` 分支递归 walk TYPE_ARRAY dims[0]=outer… + leaf kind/name → `parameter type mismatch`。**mac** m1_suffix_mm/m3_multidim_mm/m5_suffix_scalar_mm/m6_suffix_elem_mm 红（T001）· m2_suffix_ok/m4_multidim_ok=0 · p0_baseline/p1_scalar/p2_trait_only=0 · p3_impl_only 红（trait scalar vs impl ARRAY）· wave432 regression mm 探针仍红/correct 仍绿 · 矩阵 rv=42/option/hello/stdlib-import · bstrict 129 OK（附赠非 L2 必需）。soft：dyn Trait；generic bounds；nested `*[]T`/`*T[N]` postfix；Ubuntu 失败路径次级 139。mac L2（`/tmp/mac_g05_wave433.log`；Ubuntu 待跑可日终批跑）。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`67f89dd45`** / docs | **R2 Cap residual pure wave432**：**trait 方法形参非 builtin 类型假绿**。根因：wave426 参数检查仅比顶层 TypeKind ord → NAMED name（Point vs Other）/ `*T` elem（`*i32` vs `*u8`）/ `[]T` elem（`[]i32` vs `[]u8`）落 kind=-1 跳过 → impl 类型错仍过编 run=42。权威：G.7 skip_tl/seed 双生 `method_param_names` + `method_param_name_lens` + `method_param_elem_kinds`；解析状态机扩 `param_elem_pending`（见 STAR）+ `param_slice_need_rb`（见 LBRACKET）→ 收集 `*T` / `[]T` 元素类型；check 在顶层 kind 之外补 NAMED name memcmp + PTR/SLICE elem-kind + elem-name memcmp → `parameter type mismatch`。**mac** mm_named/mm_ptr/mm_slice 红（T001）· correct noparam/builtin/named/ptr/slice=42 · main=42 · run-trait · 矩阵 rv/option/hello · bstrict 129 OK。Ubuntu：hybrid pthin 自定义探针 segfault（ld -r 大 BSS 非逻辑 bug）；monothin 验证 mm 诊断正确；bstrict 无回归；PTR/SLICE elem kind 检测为 pre-existing 问题（`pipeline_module_func_param_type_ref_at` 对 builtin 返回 0，非本波引入）。soft：param suffix `T[N]` / multi-dim `T[N][M]`（与 wave430/431 ret 对称，延后）；dyn Trait；generic bounds；nested `*[]T`/`*T[N]` postfix；Ubuntu 失败路径次级 139。mac/Ubuntu L2（`/tmp/mac_g05_wave432.log` · `/tmp/ubuntu_g05_wave432.log`）。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`70ca1089b`** / docs | **tip L4 升钉**：双端真冷全擦 remaining_o=0 · 矩阵 rv42/option/hello · **bstrict OK (129)**。冷日志 `/tmp/{mac,ubuntu}_true_cold_70ca1089b.log` · bstrict `/tmp/{mac,ubuntu}_true_bstrict_70ca1089b.log`。前钉 `605e7efa4`。含 wave421–431 Cap residual（trait 签名硬失败簇）。soft 仍挂：dyn Trait；generic bounds；nested `*[]T`/`*T[N]`；Ubuntu 失败路径次级 139。**暂休** · 续 wave432 日常 L2。PLATFORM: SHARED · **tip L4 放行** |
| 2026-07-25 | **`15a09c5c5`** / docs | **R2 Cap residual pure wave431**：**trait multi-dim `T[N][M]` ret 假红/假绿**。根因：wave430 只存一维 → trait `: i32[2][3]` 成 ARRAY size=2 elem=i32；impl 同形 elem=ARRAY → ok 假红；impl `: i32[2]` 假绿。权威：G.7 skip_tl/seed `method_ret_array_ndims` + `dims[0]=outer…`；check 递归 TYPE_ARRAY size 再 leaf；诊断 `method %s return type mismatch`。**双端** md_ok=42 · md_mm/md_sz/md_inner_sz 红 · md1/arr/suffix_slice/named 绿 · self_mm 红 · main=42 · run-trait · 矩阵；Ubuntu mm 次级 139（wave425 起已有）。soft：dyn Trait；generic bounds；nested `*[]T` / `*T[N]` postfix。mac/Ubuntu L2（`/tmp/mac_g05_wave431.log` · `/tmp/ubuntu_g05_wave431.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`338688aae`** / docs | **R2 Cap residual pure wave430**：**trait 后缀 `T[N]`/`T[]` ret 假红/假绿**。根因：wave427 登记 ret 见 scalar/named base 即停 → `: i32[2]` 成 TYPE_I32；impl `i32[2]` 为 TYPE_ARRAY → ok 假红；impl 标量 `i32` 与错误 registry 假绿。权威：G.7 skip_tl/seed `ret_suffix_pending` + `method_ret_array_sizes`；check TYPE_ARRAY size（`pipeline_type_array_size_at`）+ elem；诊断 `method %s return type mismatch`。**双端** arr_ok/slice_ok/suffix_slice_ok=41 · named_arr_ok=42 · scalar/size/elem_mm 红 · self_mm 红 · main=42 · run-trait · 矩阵；Ubuntu mm 次级 139（wave425 起已有）。soft：dyn Trait；generic bounds；nested `*[]T` / multi-dim `T[N][M]`。mac/Ubuntu L2（`/tmp/mac_g05_wave430.log` · `/tmp/ubuntu_g05_wave430.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`6f696781c`** / docs | **R2 Cap residual pure wave429**：**trait typed self vs for-type 假绿**。根因：wave428 仅 ek0=-1 时比 for-type；trait `self: i32` + `impl for u64 { self: i32 }` param 循环对 trait 绿、忽略 for-type → 绑 i32 接收者 run=42。权威：G.7 skip_tl/seed 去掉 ek0&lt;0 门控；for-type 与 impl param0 均已知即硬比；诊断 `method %s self type mismatch`。**双端** typed_for_ok/untyped_ok/named_for_ok=42 · mm 红 · main=42 · run-trait · 矩阵；Ubuntu mm 次级 139（wave425 起已有）。soft：dyn Trait；generic bounds；nested `*[]T`/`T[N]` ret。mac/Ubuntu L2（`/tmp/mac_g05_wave429.log` · `/tmp/ubuntu_g05_wave429.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`512a9757c`** / docs | **R2 Cap residual pure wave428**：**trait untyped self vs for-type 假绿**。根因：param0 untyped 时 ek=-1 跳过 → `impl Double for i32 { double(self: u64) }` 过编 run≠42。权威：G.7 skip_tl/seed 登记 for-type（kind/named/`*T`）+ check param0 对齐；诊断 `method %s self type mismatch`。**双端** self_ok/named_ok/named_ret=42 · self_mm/named_mm 红 · main=42 · miss · run-trait · 矩阵；Ubuntu self_mm 次级 139（wave425 起已有）。soft：dyn Trait；generic bounds；nested `*[]T`/`T[N]`；typed self≠for-type（trait 已注解）。mac/Ubuntu L2（`/tmp/mac_g05_wave428b.log` · `/tmp/ubuntu_g05_wave428.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`23130f11a`** / docs | **R2 Cap residual pure wave427**：**trait named/ptr/slice ret 假绿**。根因：registry 仅 builtin ret → IDENT/`*T`/`[]T` kind=-1 跳过 → Point vs Other、[]i32 vs []u8 过编 run=42。权威：G.7 skip_tl/seed `method_ret_names` + `method_ret_elem_kinds`；登记 TYPE_NAMED/PTR/SLICE；check `pipeline_type_named_name_into`/`pipeline_type_elem_ref_at`。**双端** named/slice/ptr-ok=42 · mm 红 · main=42 · arity/miss/unk · fs named · run-trait · 矩阵；Ubuntu 失败路径次级 139（wave425 起已有）。soft：dyn Trait；generic bounds；untyped-self；nested `*[]T`/`T[N]`。mac/Ubuntu L2（`/tmp/mac_g05_wave427.log` · `/tmp/ubuntu_g05_wave427.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`b725de103`** / docs | **R2 Cap residual pure wave426**：**trait 方法形参 arity / typed param 假绿**。根因：registry 仅名+ret → impl 多/少参或 typed 形参 TypeKind 错仍过编（arity run=42）。权威：G.7 skip_tl/seed `method_param_counts` + `method_param_kinds`（builtin）+ check `pipeline_module_func_num_params_at`/`param_type_ref_at`；诊断 `parameter count/type mismatch`。**双端** ok=42 · arity/pty 诊断红 · unk=1 · run-trait · 矩阵；Ubuntu 失败路径次级 P001 后 139（wave425 起已有，非本波）。soft：dyn Trait；generic bounds；named ret；untyped-self。mac/Ubuntu L2（`/tmp/mac_g05_wave426.log` · `/tmp/ubuntu_g05_wave426.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`49aacb209`** / docs | **R2 Cap residual pure wave425**：**trait return_mismatch 假绿**。根因：registry 只存方法名 → impl `: bool` vs trait `: i32` 过编。权威：G.7 skip_tl/seed `method_ret_kinds`（builtin TypeKind）+ `reset(arena)` stash + check `pipeline_module_func_return_type_at`/`type_kind_ord`；诊断 `method %s return type mismatch`；pin parse_into/_buf reset。**双端** ok=42 · mm/i64/void 红 · missing/unknown · run-trait · 矩阵。soft：bounds/dyn；named ret。mac/Ubuntu L2（`/tmp/mac_g05_wave425.log` · `/tmp/ubuntu_g05_wave425.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`2fcbae844`** / docs | **R2 Cap residual pure wave424**：**unknown_trait 假绿**。根因：① skip_tl/seed `ti<0` soft-skip ② freestanding `-o` 只调 `parse_into_buf` 不经 with_init 后检。权威：G.7 `xlang_trait_check`/`seed_check` 硬报 `impl for unknown trait` + `parse_into_finish_ok` 挂 parse_into/_buf 成功口；pin parser_gen + parser.x + skip_tl/seed 同 commit。**mac** pure unknown fs+host 红 · main=42 · missing/no-impl · inherent=42 · 矩阵 · run-trait。soft：bounds/dyn；return_mismatch。mac L2（`/tmp/mac_g05_wave424*.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`0ff65027b`** / docs | **R2 Cap residual pure wave423**：**const 类型推断** `const name = init`。根因：① body/top 强制 `:`→P010 ② import 收集器吞 `const name =` 丢绑定 ③ typeck 不戳记 type_ref。权威：G.7 parse const+`=`（type_ref=0）+ `pipeline_block_set_const_type_ref` + `typeck_infer_top_level_const_types`；import 仅 `= import` 消费；pin parser_gen/typeck_gen + .x 同 commit；docs/06。**mac** body/top/typed/as/chain/fs=42 · let 仍 P010 · trait=42 · 矩阵。soft：trait bounds/dyn。mac L2（`/tmp/mac_g05_wave423.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED parse+typeck · **日常 L2 非 tip L4** |
| 2026-07-25 | **`a1b9ca3a2`** / docs | **R2 Cap residual pure wave422**：**untyped-let 软 P001 → 硬 P010**。根因：`parse_body_lets`/`top_level_let` 缺 `:` 仅 silent false → 整函数丢弃 → P001「no functions」/ BLD001。权威：G.7 `parser_report_untyped_binding_p010_c`（body_tl）+ body/top 调用；pin parser_gen + parser.x 同 commit；docs/06。**mac** body/top P010 · typed=42 · empty S=42 · trait=42 · 矩阵。soft：const 类型推断；trait bounds/dyn。mac L2（`/tmp/mac_g05_wave422.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-25 | **`98f9a49e5`** / docs | **R2 Cap residual pure wave421**：**trait 缺方法假绿 + no-impl 诊断**。根因：产品 skip_trait/impl 只 hoist free fn，无 trait 签名表 → `impl_missing_method` 过编；method_call 失败仅 XT001。权威：G.7 skip_tl 登记 trait 方法 + impl-seen + `xlang_trait_check_impls_complete_c`（`pipeline_parse_into_with_init_buf_impl_c` 后）；method_call（strict_minimal+weak）`no impl for type with method`；`method_no_impl` 改非空 struct。**mac** missing method triple · no-impl double · main=42 · run-lang-trait · 矩阵。soft：untyped-let；bounds/dyn；empty S{}。mac L2。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-25 | **`123555700`** / docs | **R2 Cap residual pure wave420**：**arm64 帧 [x29,#off] 字节 clamp 别名**。根因：LDR/STR 把 **字节** off 硬帽 4095 再 /8；dual-GP length@0x1000→0xff8 盖 data（escape/dual n≥1017 SIGSEGV）；add_imm 同 clamp → local n=2048 mid 错。权威：G.7 `backend_arm64_enc_c` scaled imm12=off/8（≤32760）+ multi-chunk ADD LEA/add_imm + 大 off x16 scratch；`arm64_enc.x` lea 孪。**mac** single/dual 1017..1024=42 · local 1024/2048=42 · disasm data@0xff8 length@0x1000 · 矩阵 rv/option/hello。soft：untyped-let；trait。mac L2（`/tmp/mac_g05_wave420.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: MACOS\|ARM64 · **日常 L2 非 tip L4** |
| 2026-07-25 | **`c8b15e988`** / docs | **R2 Cap residual pure wave419**：**host fixed→slice escape 硬帽 256→1024**。根因：`emit_return` 仅 `arr_sz<=256` 发 durable `__xlang_esc`；n>256 裸 `return s` 栈悬垂，双 call 靠 deep-copy 碰运气。权威：G.7 codegen.x+seed gate **1024**（≡ `GLUE_ARRAY_LIT_MAX_ELEMS`/max_n）。**mac** `-backend c` use/dual n=300..1024=42 · esc 发射 · 矩阵 rv/option/hello · fs dual≤800=42。soft：untyped-let；trait；**fs dual n=1024**。mac L2（`/tmp/mac_g05_wave419.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED host-C · **日常 L2 非 tip L4** |
| 2026-07-25 | **`27fff1d7b`** / docs | **R2 Cap residual pure wave418**：**let 帧 deep-copy 真递归 + max_n 1024 + arm64 fixed→slice escape**。根因：① COMMON 同 site 递归 last-wins（walk=8）② max_n=512 ③ arm64 escape `ta!=0`/`jle`。权威：G.7 use_frame let + frame_size 预留；host sd/ld[1024]+length=sn；escape jge+ADRP。**双端** walk=36 · lit=19 · dual≤600 · 矩阵。soft：untyped-let；trait；host dual n≥512。mac L2（`/tmp/mac_g05_wave418*.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED freestanding+host · MACOS\|ARM64 escape · **日常 L2 非 tip L4** |
| 2026-07-25 | **`53be4f60e`** / **`f8bdf67a5`** / docs | **R2 Cap residual pure wave417**：**fs T[N] 形参/返回 + arm64 动态 INDEX 步长**。根因：① `glue_func_param_agg_byte_size_c` 把 T[N] 当整 blob → fill_param high-end home vs arm64 8B GP（INDEX 基址 junk→SIGSEGV）；② arm64 scale4/8 imm6=6/7（×64/×128）非 2/3；③ return size>16 误 sret + 栈 ARRAY_LIT 悬垂；④ mid(a) dual-GP 误解 16B payload。权威：G.7 formal home=8 · use_lea/load · durable ARRAY_LIT→TYPE_ARRAY · return size=8 · seed+`arm64_enc.x` scale。**双端** pass n2..16 · fwd=100 · ret_only/ret_pass=36 · plit · localw · i64=100 · reent=15 · 矩阵 rv/option/hello。soft：untyped-let；trait；deep-copy>512。mac/Ubuntu L2（`/tmp/mac_g05_wave417*.log` · `/tmp/ubuntu_g05_wave417*.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED freestanding + MACOS\|ARM64 scale · **日常 L2 非 tip L4** |
| 2026-07-25 | **`4613c30c9`** / docs | **R2 Cap residual pure wave416**：**fs i64/u64 ARRAY_LIT esz=8**。根因：`pipeline_asm_array_lit_elem_byte_sz_c` 仅 F64→8；U64/I64/USIZE/ISIZE 落默认 4，INDEX 用 glue_index esz=8 → 写 4B 步读 8B（host-C 绿；sum=40≠100）。权威：G.7 ARRAY_LIT face 补 8B 整数/PTR（指针宽，非 pointee）。**双端** i64sum=100 · first+last=50 · u64=50 · i32=50 · slice=34 · reent=42 · 矩阵 rv/option/hello；disasm `str/mov` 8B 步。soft：untyped-let；trait；deep-copy max_n=512；ret `T[N]` by-value 大 N。mac/Ubuntu L2（`/tmp/mac_g05_wave416b.log` · `/tmp/ubuntu_g05_wave416.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-25 | **`2550b26e6`** / docs | **R2 Cap residual pure wave415**：**fs ARRAY_LIT nbytes>2048 / i32 n>512**。根因：durable `payload[2048]` + `nbytes≤2048` + `MAX_ELEMS=512` → n≥513 CG002（host-C 绿）。权威：G.7 `GLUE_ARRAY_LIT_MAX_ELEMS=1024` · `MAX_PAYLOAD=4096`（durable/escape/let 面）。**mac** asm n513/600/1024 last · u8_1024 · esc600 · eq600=42 · n1025 仍拒 · slice=34 · reent=42 · 矩阵 rv/option/hello。soft：untyped-let；trait；deep-copy max_n=512；fs i64 大数组；ret `T[N]` by-value 大 N。mac/Ubuntu L2（`/tmp/mac_g05_wave415.log` · `/tmp/ubuntu_g05_wave415.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-25 | **`bcee3af87`** / docs | **R2 Cap residual pure wave414**：**arm64 freestanding 大栈数组 SIGBUS**。根因：wave402 low-end `[x29,+off]` 本地 vs 旧 prologue `stp #-16!; sub sp,#frame` 帧在 x29 **下** → 本地写 caller/guard；i32[n] n≥118 / u8 n≥472。权威：G.7 `sub sp,#fs; stp x29,x30,[sp]; mov x29,sp`（整帧在 x29 底；fs>4095 分块 add/sub）；seed `backend_arm64_enc_c` + `arm64_enc.x` 孪。**mac** asm n70…512 last/eq · simple=49 · slice=34 · reent=42 · 矩阵 rv/option/hello · host512=42。Ubuntu x86 本叶无路径（大数组基线已绿）。soft：untyped-let；trait；fs nbytes>2048（wave415 闭）。mac L2（`/tmp/mac_g05_wave414.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: MACOS\|ARM64 · **日常 L2 非 tip L4** |
| 2026-07-25 | **`d41a7ce2e`** / docs | **R2 Cap residual pure wave413**：**freestanding ARRAY_LIT 元数硬帽 256→512**。根因：glue 多处 `n_arr>256` 早拒 → n≥257 `return […]` CG002；host/`__xlang_sdN[512]`/durable `payload[2048]` 已支持 512×i32。权威：G.7 `#define GLUE_ARRAY_LIT_MAX_ELEMS 512` 贯通 durable/let/return/flat/escape/call-arg。**双端** n257/300/512=42 · last512=11 · d300=12 · d3z=6 · pure0=60 · n70=70 · e8chk=42 · 矩阵 rv/hello · nm `Lxlang_al_*`。**诊断**：wave412 multi-INDEX「假红」= shell `$?` 8 位截断（273&0xFF=17）。soft：untyped-let；trait；fs nbytes>2048。mac/Ubuntu L2（`/tmp/mac_g05_wave413.log` · `/tmp/ubuntu_g05_wave413.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-25 | **`07a561ab4`** / docs | **R2 Cap residual pure wave412**：**freestanding TYPE_SLICE deep-copy max_n=64→512 runtime loop**。根因：wave411 展开 64 与 host `__xlang_sdN[512]` 分叉；展开 512 炸 text。权威：G.7 同 `glue_slice_let_reent_deep_copy_after_dual_gp_elf_c` — jge/jmp 循环 + 先 cap length + COMMON 零帧。**双端** d3/m28let/d64/d70h=6 · n70=203 · walk=55 · pure0=60 · 矩阵 rv/option/hello · nm `Lxlang_sd_*`。soft：untyped-let；trait；multi-INDEX（后证 `$?` 假红）。mac/Ubuntu L2（`/tmp/mac_g05_wave412.log` · `/tmp/ubuntu_g05_wave412.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-25 | **`7e3482b8e`** / docs | **R2 Cap residual pure wave411**：**freestanding TYPE_SLICE deep-copy 帧膨胀**。根因：wave409/410 帧内 payload bump `next_offset`，prologue `frame_size` 未计 → dual + 额外 local（`let z`）越 SP（m28let=138；N≥29 dual 红）。权威：G.7 孪 host `__xlang_sdN` — 唯一 `Lxlang_sd_<seq>` SHN_COMMON，**零帧增长**。**双端** m28let/dual3..64=6 · len=N · w410=96 · walk=55 · pure0=60 · 矩阵 rv/option/hello · nm `Lxlang_sd_*`。soft：untyped-let；trait；max_n=64。mac/Ubuntu L2（`/tmp/mac_g05_wave411.log` · `/tmp/ubuntu_g05_wave411.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-25 | **`c4cd674ca`** / docs | **R2 Cap residual pure wave410**：**freestanding 同 call 双 CALL 返回 TYPE_SLICE last-wins**。根因：call-arg 只存 dual-GP fat；callee `return […]` 函数 static/COMMON → `sum2(take(1),take(2))` fs 72≠69（host wave406 已深拷）。权威：G.7 复用 wave409 `glue_slice_let_reent_deep_copy_after_dual_gp` 于 `pipeline_asm_emit_expr_elf_for_call_args`。**双端** dual=69 · triple=108 · mid=69 · walk=69 · pure0=60 · 矩阵 rv/option/hello。soft：untyped-let；trait；deep-copy max_n=64。mac/Ubuntu L2（`/tmp/mac_g05_wave410.log` · `/tmp/ubuntu_g05_wave410.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-25 | **`de0af37b9`** / docs | **R2 Cap residual pure wave409**：**TYPE_SLICE let from CALL 真递归 reentrancy frame deep-copy**。根因：callee `return […]` 函数 static/COMMON `__xlang_al`；let 只 dual-GP 存 fat → walk 18≠36。权威：G.7 host `codegen_emit_slice_let_reent_finish`（栈 `__xlang_ldN[512]`）；fs `glue_slice_let_reent_deep_copy_after_dual_gp`（jge 非 jle·arm64）。**双端** escape=36 · pure0=60 · rec=12 · host dual=96 · fs dual=126 soft · 矩阵 rv/option/hello。soft：untyped-let；trait；fs call-arg dual last-wins。mac/Ubuntu L2（`/tmp/mac_g05_wave409.log` · `/tmp/ubuntu_g05_wave409.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED host-C+freestanding · **日常 L2 非 tip L4** |
| 2026-07-25 | **`c5501b9e3`** / docs | **R2 Cap residual pure wave408**：**mac arm64 freestanding TYPE_SLICE return→local dual-GP**。根因：① durable ARRAY_LIT 仅 ta==0 ② return ARRAY_LIT→slice 仅 ta==0 ③ `store_rdx_to_rbp` 仅 x86 ④ arm64 用 rbx(=x1) 停 data 与 length@x1 冲突 ⑤ let-init 只 store rax。权威：G.7 durable COMMON+ADRP(ta=1)；return push/pop + len_arg(1\|2)；store_rdx→str x1；`glue_store_retval_pair` TYPE_SLICE 双半。**mac** pure0=60 · rec=66 · ca=60 · idx=63 · dual=69 · 矩阵 rv/option/hello。soft：untyped-let；trait；reentrancy。mac L2（`/tmp/mac_g05_wave408c.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: MACOS\|ARM64 + SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-25 | **`096c47278`** / docs | **R2 Cap residual pure wave407**：**typeck ARRAY_LIT 递归检查元素**。根因：`check_expr` 对 kind=46 落空不递归 → 嵌套 INDEX 未跑 `typeck_check_expr_index` → `index_base_is_slice=0` → host 发 `(child)[i]`（by-value fat 非法 C / BLD001）；递归 `return [child[i]+1]` 同根。权威：G.7 `typeck_check_expr_array_lit` + mega kind=46；typeck.x+seed 同向。**双端** host idx_in_lit=63 · rec=66 · dual=69；Ubuntu **fs** 同绿；mac arm64 fs pure0/rec 仍 soft；矩阵 rv/option/hello。soft：untyped-let；trait；mac arm64 fs TYPE_SLICE return→local。mac/Ubuntu L2（`/tmp/mac_g05_wave407.log` · `/tmp/ubuntu_g05_wave407.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED typeck · **日常 L2 非 tip L4** |
| 2026-07-25 | **`e5b5fae51`** / **`45f288fc8`** / docs | **R2 Cap residual pure wave406**：**host-C 同 call 双 CALL 返回 TYPE_SLICE last-wins**。根因：wave345 `__xlang_sp` 仅拷 fat；callee `return […]` 用函数静态 `__xlang_al` → dual `sum2(take(1),take(2))` 双 data 指末次写（72≠69）。权威：G.7 `emit_call_arg_slice_abi` CALL/METHOD → unique `__xlang_spN` + deep-copy `__xlang_sdN[512]`（`codegen_next_host_call_array_tmp_id`）；ARRAY_LIT 仍 fat-only；codegen.x+seed 同向（`45f288fc8` 全量 resync seed 体）。**双端** dual/reent_mid=69 · nest=150 · ca=69 · 矩阵 rv/option/hello。soft：untyped-let；trait；真递归 reentrancy。mac/Ubuntu L2（`/tmp/mac_g05_wave406.log` · `/tmp/ubuntu_g05_wave406.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED host-C · **日常 L2 非 tip L4** |
| 2026-07-25 | **`f300deeb7`** / docs | **R2 Cap residual pure wave405**：**arm64 freestanding 跨函数模块 `let g`（set_g/get_g）**。根因：modlet prepare/load/store 仅 `ta==0`；arm64 各函数独立栈槽（exit 0；host-C/Ubuntu x86 COMMON 绿）。权威：G.7 prepare+load/store 扩 ta==1；ADRP x1+ADD PAGEOFF；Mach-O COMMON nlist `N_UNDF\|N_EXT` + typed reloc PAGE21/PAGEOFF12；inline `ldr x0,[x1]`。mac set_g/get_g/g_simple/nz=42 · nm `__DATA,__common` · 矩阵 rv/option/hello · ca=42。soft：untyped-let；trait；host static 其它 last-wins。mac L2（`/tmp/mac_g05_wave405.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: MACOS\|ARM64 + SHARED modlet · **日常 L2 非 tip L4** |
| 2026-07-25 | **`47e566b31`** / docs | **R2 Cap residual pure wave404**：**freestanding CALL 返回 `T[N]` 作 TYPE_SLICE 实参**。根因：wave345 一律 dual-GP `store_rdx`（arm64 ta≠0→CG002）；CALL resolved 常 PTR 未认 TYPE_ARRAY；size>8 误 struct16-deref；E* 指 callee 栈悬垂；x86 payload@home+8 盖 fat.data（sum=94）。权威：G.7 call-arg fat + 帧内 deep-copy（host `__xlang_caN` 孪）；ARRAY 不 struct16-deref；x86 `payload_off=home+N×esz`。双端 only=3 · ca=42 · sum=60 · dual=93 · mid=12 · 矩阵 rv/option/hello。soft：untyped-let；trait；static/COMMON 其它。mac/Ubuntu L2（`/tmp/mac_g05_wave404b.log` · `/tmp/ubuntu_w404_final.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-25 | **`0e40fb0d0`** / docs | **R2 Cap residual pure wave403**：**arm64 freestanding binop 左操作数跨 CALL 被冲**。根因：`preserve_rax` 用 volatile x9；右 CALL multi-INDEX 也用 x9 → `take(a)+take(a)`=9≠12；SP push 则 callee 形参 `[x29,#0x10]` 盖 parked left。权威：G.7 arm64 帧槽 `next_offset` 嵌套 spill；x86 仍 push/pop。探针 mid_twice/ab/lets=12 · mid_s0=2 · once=6 · idx_sum=60 · fsum2=12 · nest3=6 · seq=69 · 矩阵 rv/option/hello。soft：untyped-let；trait；static/COMMON；fs T[N] return call-arg CG002。mac L2（`/tmp/mac_g05_wave403b.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: MACOS\|ARM64 frame · LINUX\|x86 push · **日常 L2 非 tip L4** |
| 2026-07-25 | **`c909650b8`** / docs | **R2 Cap residual pure wave402**：**arm64 freestanding 多定长数组局部槽重叠**。根因：`asm_local_slot_reg_offset` 高端 `home=off+sz` 配正偏移 `[x29+off]` → ARRAY 自 home 向上盖下一 local（`b[2]`/`x` 重叠；seq 80/47）。权威：G.7 低端 `home=off`/`next=off+sz`（MACOS\|ARM64）；x86 仍高端。双端矩阵 rv/option/hello · mac seq/three/dual/mid_lets/local_slice 绿。soft：untyped-let；trait；static/COMMON；fs T[N] return call-arg CG002；binop x9 跨 call。mac L2（`/tmp/mac_g05_wave402b.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: MACOS\|ARM64 local slots · **日常 L2 非 tip L4** |
| 2026-07-25 | **`de9a5c6b8`** / docs | **R2 Cap residual pure wave401**：**freestanding 转发 TYPE_SLICE 形参作 call-arg**。根因：`emit_expr_elf_for_call_args` 对 TYPE_SLICE formal 一律 lea(home)；slice* 形参槽已是 fat* → 传 fat** → INDEX 读指针位（mid_twice=162 · reent_sp=239；host-C 绿）。权威：G.7 复用 `glue_enc_local_slot_ptr_or_addr_elf_c`（needs_ptr_load → load；本地 dual-GP 仍 lea；INDEX wave332e 同源）。双端 fs mid_once/direct=3 · mid_twice=6 · reent_*=33 · local_slice=60 · sum2=39 · host-C 同 · 矩阵 rv/option/hello。soft：untyped-let；trait；static/COMMON last-wins。mac/Ubuntu L2（`/tmp/mac_g05_wave401.log` · `/tmp/ubuntu_g05_wave401.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-25 | **`24ca1d0fd`** / docs | **R2 Cap residual pure wave400**：**host-C `ARRAY_LIT` → TYPE_SLICE 形参**。根因：wave345 仅 CALL/METHOD 走 `__xlang_sp`；ARRAY_LIT `emit_expr` 为 compound rvalue，裸 `&(lit)` → BLD001 cannot take address of rvalue。权威：G.7 `emit_call_arg_slice_abi` need_addr 扩 `EXPR_ARRAY_LIT=46`；seed codegen_gen + codegen.x 同 commit。双端 host single=42 · dual lit=39 · sp_dual=33 · dual CALL=39 · triple=45 · fs lit；矩阵 rv/option/hello。soft：untyped-let；trait；static reentrancy last-wins。mac/Ubuntu L2（`/tmp/mac_g05_wave400.log` · `/tmp/ubuntu_g05_wave400.log`）。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED host-C · **日常 L2 非 tip L4** |
| 2026-07-25 | **`abd606098`** / docs | **R2 Cap residual pure wave399**：**freestanding VAR/FIELD → `T[N]` 整数组拷贝（arm64）**。根因：wave334 `load_rbp_lane(src_off−i×esz)` 仅匹配 x86 `[rbp−off]`；arm64 槽 `x29+off` + 产品 64 位 scaled imm/8 → i32 步塌缩（0x20/0x18/0x18）· sum=12≠33。权威：G.7 `glue_struct_lit_store_fixed_array_field` lea(src)+i×esz + esz 宽 load（同 CALL E*）。双端 var_copy/assign/field=33 · var_var=66 · call=33 · 矩阵 rv/option/hello。soft：untyped-let；trait；`__xlang_ar` reentrancy。mac/Ubuntu L2。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED freestanding · MACOS\|ARM64 · **日常 L2 非 tip L4** |
| 2026-07-25 | **`94df961e3`** / docs | **R2 Cap residual pure wave398**：**freestanding `let t: T[N] = call()` / METHOD / STRUCT_LIT 字段**。根因：wave351 CALL 仅 `ta==0` → arm64 unhandled CG002；store_retval dual-GP 非 arm64；大 N 假 sret。权威：G.7 `glue_struct_lit_store_fixed_array_field` E* spill + esz 宽 ptr-copy。双端 asm sum/wave353/field=33 · host dual=69 · 矩阵 rv/option/hello。soft：untyped-let；trait；`__xlang_ar` reentrancy；fs VAR 整数组拷贝邻域。mac/Ubuntu L2。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED freestanding · **日常 L2 非 tip L4** |
| 2026-07-25 | **`784e592e8`** / docs | **R2 Cap residual pure wave397**：**CALL/METHOD 返回 T[N] 作 slice* 实参 · unique `__xlang_caN` 深拷**。根因：wave396 fat `.data=take3()` 直指 callee static `__xlang_ar` → 同 call 双实参 last-wins（host dual 66≠39）。权威：G.7 host `emit_call_arg_slice_abi` + `codegen_next_host_call_array_tmp_id`；VAR/FIELD 不变。双端 host dual=39 · seq=42 · var host+fs=42 · field host+fs=33 · 矩阵 rv/option/hello。soft：untyped-let；trait；fs TYPE_ARRAY return CG002；超 dual 的 `__xlang_ar` 重入。mac/Ubuntu L2。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED host-C · **日常 L2 非 tip L4** |
| 2026-07-25 | **`f68c9b4f2`** / docs | **R2 Cap residual pure wave396**：**TYPE_ARRAY CALL/FIELD → slice* 实参 fat**。根因：wave395 仅 VAR；`len_of(take3(1))` / `sum3(b.a)` 裸 `E*` 当 fat* → length 半读载荷（host 138）。权威：G.7 host `emit_call_arg_slice_abi` 扩 CALL/METHOD/FIELD（仍仅 formal TYPE_SLICE）；fs FIELD dual-GP fat+lea。双端 call=42 · field host+asm=33 · arr=4 · sequential=42 · 矩阵 rv/option/hello。soft：同 call 双实参 shared `__xlang_ar` last-wins；untyped-let；trait；fs TYPE_ARRAY return。mac/Ubuntu L2。钉盘仍 **`605e7efa4`**。PLATFORM: SHARED host-C+fs · **日常 L2 非 tip L4** |
| 2026-07-25 | docs | **日切归档**：24 号全日仪表盘 + §6 流水整表迁入 [`自举进度-归档-2026-07-24.md`](自举进度-归档-2026-07-24.md)；本文 25 日起重新起表。钉盘仍 **`605e7efa4`** · tip **wave395** @ **`e7cac169e`** · 下波 **wave396**。 |

***

## 7. 禁止清单（本阶段）

- [ ] 未用旧 stage1 / 未用 24 号归档 tip 冒充 25 日 tip L4
- [ ] 未把 prove / Stage2 / N++ 单独写成「可自举」
- [ ] 未 BEST_EFFORT / soft-skip 糊绿
- [ ] 未 seed/.x/glue 分叉合入
- [ ] 未把 mac `-dead_strip` 假绿当 Ubuntu 全链已过
- [ ] 日常 L2 未写成 tip L4；谈升钉须双端 L4 + 129
