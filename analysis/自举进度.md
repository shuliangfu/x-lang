# Xlang / X language 自举进度

> **更新**：2026-07-27 · **wave524 已闭**（runtime_arrow_simd_glue 根因修复 undeclared len bug + Makefile R2 thin+rest 补全 双端 L2）· tip **`656e04617`**（双端 L2，wave524；L4 待整波收口）· 钉盘 **`2bc0885d3`**（待 Ubuntu 钉盘升级）· 下一 **wave525**\
> **基线 SHA（产品 L4 放行钉盘）**：**`2bc0885d3`**（升自 `d812aeb41`；双端 L4 129 全绿）\
> **产品 bstrict 套件数**：**129**\
> **产品代码 tip**：**`2bc0885d3`**（wave501-512；L4 基线 `2bc0885d3`；钉盘 **`2bc0885d3`**）\
> **git 锚点（重命名前）**：tag **`anchor-pre-xlang-rename`**\
> **现行工程**：品牌/CLI/env/ABI 已 **xlang**（主域 **`xlang.dev`** · 仓库 **`github.com/shuliangfu/x-lang`**）。\
> **本波（27 日）状态**：**wave524 已闭**（runtime_arrow_simd_glue 根因修复 undeclared `len` bug + Makefile R2 thin+rest 补全 双端 L2 · nm Ubuntu 13 核心符号对齐 · 连续两波发现 pre-existing 编译错误 bug）；tip `656e04617`（双端 L2）；钉盘 `2bc0885d3`（待 Ubuntu 钉盘升级）；下一 **wave525**。\
> **方法**：[自举方法.md](自举方法.md)（Cap/R/L/M；工程 ≠ 产品放行）\
> **Windows（删种子前）**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md) · [25](自举进度-归档-2026-07-25.md) · [24](自举进度-归档-2026-07-24.md) · [23](自举进度-归档-2026-07-23.md) · [22](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md) · [19](自举进度-归档-2026-07-19.md) · [18](自举进度-归档-2026-07-18.md) · [17](自举进度-归档-2026-07-17.md) · [16](自举进度-归档-2026-07-16.md) · [14](自举进度-归档-2026-07-14.md) · [13](自举进度-归档-2026-07-13.md)

### 维护约定

1. 每完成一波 **必改两文**：本文（KPI/前排/§6）+ [`当前进度.md`](当前进度.md)。
2. **禁止**把 prove/Stage2/N++ 单独写成「可自举」。
3. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**；历史钉盘 125）。
4. 工程 KPI 写 **SHA + 日志路径**；过期整日流水移归档。
5. 大债地图默认写 [`当前问题分析.md`](当前问题分析.md)（承接原 `问题分析文档.md`）；非每微步。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 =** **`2bc0885d3`**（升自 `d812aeb41`；**双端 L4 全绿**）。\
**产品代码 tip =** **`2bc0885d3`**（wave501-510 双端验收 wave501-508 R2 OS glue migration + wave509 mac L4 + wave510 Ubuntu L4；L4 基线 `2bc0885d3`）。\
**git 锚点（考古）=** tag **`anchor-pre-xlang-rename`**。\
**现行主线 =** **Cap residual wave511**（wave510 已闭 · Ubuntu L4 金标验证双端钉盘升级）。

| 轨                | 状态                                                                                           |
| ---------------- | -------------------------------------------------------------------------------------------- |
| 工程轨              | **wave510 已闭**（Ubuntu L4 金标验证双端钉盘升级 `2bc0885d3` · bstrict 129 OK · 自 wave500 后累积 10 SHARED wave 双端通过）；tip L4 `2bc0885d3`（双端）；钉盘 **`2bc0885d3`**；下一 **wave511** |
| 产品轨 tip / 钉盘     | tip = **`2bc0885d3`**（双端 L4）· 双端 L4 基线 **`2bc0885d3`** · 钉盘 **`2bc0885d3`**（双端全绿） |
| mac L4 真冷        | ✅ **`2bc0885d3`** remaining_o=0 · **OK (129)** · 10分17秒（wave509，wave501-508 整体 R2 波） |
| Ubuntu L4（金标）    | ✅ **`2bc0885d3`** remaining_o=0 · **OK (129)** · 3分22秒（wave510，wave501-508 整体 R2 波） |
| 前钉 L4            | `d812aeb41` · `c3a81bfd6` · `70ca1089b` · … |

***

## 1. 文档地图

| 文档                                             | 看什么                   |
| ---------------------------------------------- | --------------------- |
| [自举方法.md](自举方法.md)                             | Cap / R / L / M       |
| [自举步骤.md](自举步骤.md)                             | 可执行闸门                 |
| [自举验证.md](自举验证.md)                             | 怎么测                   |
| **本文**                                         | 长期 KPI + 前排（**每波必改**） |
| [当前进度.md](当前进度.md)                             | 当天快照（**每波必改**）        |
| **[当前问题分析.md](当前问题分析.md)**                     | tip 级产品债地图（21 日起权威）   |
| [自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md) | **26 日全日 wave446-491**（只读） |
| [自举进度-归档-2026-07-25.md](自举进度-归档-2026-07-25.md) | **25 号全日 + 26 日初 wave446**（只读） |
| skill `xlang-selfhost-product-gate`             | L4 / 双端 / 假绿禁止        |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项                          | 结果                                                                                  | 日志 / 备注                                      |
| -------------------------- | ----------------------------------------------------------------------------------- | -------------------------------------------- |
| 产品 bstrict 套件数             | **129**                                                                             | 现行白名单                                        |
| 产品 L4 放行钉盘                 | **`d812aeb41`**                                                                      | 27 日 tip L4 升钉（前钉 `c3a81bfd6`）；双端 L4 129      |
| 本波 tip                     | **wave500**（Ubuntu L4 `d812aeb41` · wave491-498 整体功能波双端验收 · bstrict 129 OK） | Ubuntu L4 真冷 · 双端 tip **`d812aeb41`** · 钉盘 **`d812aeb41`**（双端全绿） |
| mac 产品链 bstrict（钉盘 L4） | ✅ **OK (129)**                                                                      | L4 · `/tmp/mac_true_bstrict_c3a81bfd6.log` · cold `/tmp/mac_true_cold_c3a81bfd6.log`    |
| Ubuntu L4（金标 · 钉盘）  | ✅ **OK (129)**                                                                      | L4 · cold `98f8dcdf3` · bstrict 3分26秒 |
| 产品矩阵（双端 tip L4）           | ✅ rv42 / option / hello / si + bstrict 129                                                                | mac + Ubuntu · `c3a81bfd6`                                      |
| 前钉 mac/Ubuntu L4           | ✅ 129 @ `605e7efa4` · `ace59b6cd` · …                                                                | 前钉日志 `/tmp/*_605e7efa4.log`                             |

### 2.2 工程轨（继承）

| KPI           | 现                                        | 备注                   |
| ------------- | ---------------------------------------- | -------------------- |
| **T / EMPTY** | 18/18                                    | 18 号结案               |
| **N**         | 54/54 IDENTICAL                          | 回归基线                 |
| **R2 / 根修本波** | **wave504 已闭**（runtime_sync_os R2 migration 双路验证：冷+R2 双路构建 OK · bstrict 129 OK；R2 full 模式 `.x` 16× public API + `.from_x.c` 16× OS bridge `_impl`（mutex/rwlock/condvar）+ thin+rest `ld -r`）；**wave505 候选** | 双端 L4 · tip `d812aeb41` · 钉盘 **`d812aeb41`**（双端全绿） |

***

## 3. 当前前排（≤3 条）

1. **wave504 已闭**：runtime_sync_os R2 migration 双路验证（bstrict 129 OK · 冷+R2 双路构建 OK · tip/钉 `d812aeb41`）
2. leave-off soft：dyn Trait；P010 untyped-let（语言契约非债）；**`fn`/`var`/untyped-`self` 非产品债**；Darwin residual
3. 双端 L4 基线 **`d812aeb41`** 全绿（wave499-500）；钉盘 **`d812aeb41`**（双端验证通过）

***

## 4. 与归档的关系

- **26 日归档**：[自举进度-归档-2026-07-26.md](自举进度-归档-2026-07-26.md) 包含 wave446-491（泛型推断链 / trait 默认方法体 / struct mono 链 / multi-combo mangle 等系统性产品债解决）。
- **25 日归档**：[自举进度-归档-2026-07-25.md](自举进度-归档-2026-07-25.md) 包含 Cap residual wave396-445 + 26 日初 wave446。
- **27 日新内容**：本文记录 wave492-505（泛型 inherent impl 方法完整链路 + 双端 L4 验收 + runtime_time_os + runtime_dynlib_os + runtime_env_os + runtime_sync_os + runtime_log_os R2 migration）。
- **勿**用归档 tip 或旧钉（`70ca1089b` / `c3a81bfd6` 等）冒充现行 tip L4。**现行钉盘 `d812aeb41`**。

***

## 5. 双端 / 平台

| 平台                     | 本波状态                                                              |
| ---------------------- | ----------------------------------------------------------------- |
| **macOS arm64**        | ✅ tip L4 全擦 + 矩阵 + **129** @ **`d812aeb41`**（wave499） |
| **Ubuntu x86\_64（金标）** | ✅ tip L4 全擦 + 矩阵 + **129** @ **`d812aeb41`**（wave500） |
| **Windows MSYS2**      | 继承 hybrid；本波未重跑（**不需要切换**）；路径文档已 `…\xlang\x-lang`                                             |

***

## 6. 变更记录（倒序 · 27 日）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-07-27 | **wave515** | **R2 Cap residual pure wave515**：**runtime_string_fast R2 migration 双路双端验证**。债：`runtime_string_fast` 产品 seed 仍为 C seed（8 纯计算函数：`ptr_at`/`memcmp`/`memcmp_at`/`copy`/`memchr`/`memrchr`/`portable_memmem`/`memmem`，共 653 行）；`.x` 仅 doc anchor；无 DIRECT 构建规则。根修：① 更新 `src/asm/runtime_string_fast.x`：8× `#[no_mangle]` 纯计算 thin wrappers + 完整英文 docblock + `runtime_string_fast_x_doc_anchor` ② 更新 `seeds/runtime_string_fast.from_x.c`：全部 8 函数 `#ifndef XLANG_RUNTIME_STRING_FAST_FROM_X` 守卫 ③ 更新 `xlang_compile_std_string_o.sh`：R2 DIRECT 模式（`xlang-c -lib-name "" -o` 直接编译 `.x`，无 OS 依赖无需 thin+rest `ld -r`）。冷路 nm 8 T 符号 ✓；R2 路 nm 8 T 符号 ✓（DIRECT 模式与冷路符号一致）。bstrict macOS 冷路 129 OK（11 分 47 秒）+ R2 路 129 OK（10 分 2 秒）✓；bstrict Ubuntu 冷路 129 OK + R2 路 129 OK ✓。关键发现：① 纯计算无 OS 依赖模块可使用 DIRECT 模式（`xlang-c -lib-name "" -o` 直接编译 `.x`），无需 thin+rest `ld -r` ② DIRECT 模式下 `.x` 与 C seed 产出符号完全一致（8 T 符号）③ `memrchr` 需手写反向扫描（xlang-c 无 `memrchr` builtin）④ `portable_memmem` 需处理零长度 needle 边界情况。PLATFORM: SHARED string · **双端验证通过（macOS+Ubuntu）** |
| 2026-07-27 | **wave514** | **R2 Cap residual pure wave514**：**runtime_random_fill R2 migration 双路验证**。债：`runtime_random_fill` 产品 seed 仍为混合模式（118 行，2 函数：`random_get_alg` Windows-only BCrypt 懒初始化 + `random_fill_bytes_c` 跨平台 CSPRNG）；`.x` 仅 1 个 wrapper；无 thin+rest 规则；`random_get_alg_impl` 为 Windows-only，macOS/Linux 下会因 ld -r 未定义引用而失败。根修：① `.x` 新增 `random_fill_bytes_impl` bridge 声明 + `random_fill_bytes_c` thin wrapper（含 null/len 前置校验）+ 规范化 docblock ② `.from_x.c` `random_fill_bytes_c` → `random_fill_bytes_impl` + `XLANG_RUNTIME_RANDOM_FILL_FROM_X` 守卫 + 非 Windows stub `random_get_alg_impl` 返回 NULL + forward declarations 补全 ③ `Makefile` thin+rest `ld -r`。冷路 nm 4 T 符号 ✓；R2 路 nm 31 T 符号 ✓。bstrict 冷路 129 OK（10 分 57 秒）+ R2 路 129 OK（10 分 52 秒）✓。关键发现：Windows-only 函数必须在非 Windows 提供 stub 返回 NULL；forward declaration 类型必须与定义一致（`void *` vs `uint8_t *`）。PLATFORM: SHARED random · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave513** | **R2 Cap residual pure wave513**：**runtime_thread_glue R2 migration 双路验证**。债：`runtime_thread_glue` 产品 seed 仍为混合模式（489 行，23 函数：thread_self/create/create_with_stack/join + affinity_self/affinity + qos_class_self + set_name_self + dummy_entry_ptr + 8 std_thread pipeline wrappers + 5 thread_pool + `xlang_cpu_zero/set`）；`.x` 仅 2 个 wrapper；无 thin+rest 规则。根修：① `.x` 全量扩展为 23 个 `#[no_mangle]` thin wrappers + 23 `extern "C"` `_impl` bridge 声明 ② `.from_x.c` 全部函数重命名为 `_impl` + `XLANG_RUNTIME_THREAD_GLUE_FROM_X` 守卫 + `thread_dummy_entry` 保留 real C 函数（地址被 `thread_dummy_entry_ptr_c` 取址）+ Windows `__stdcall` trampoline `thread_wrap` 保留 rest + forward declarations 补全 ③ `Makefile` thin+rest `ld -r`。冷路 nm 37 T 符号 ✓（macOS：Linux-only `xlang_cpu_zero/set` 不导出，行为正确）；R2 路 nm 70 T 符号（thin 21 + rest `_impl` + xlang-c weak 桩）✓。bstrict 冷路 129 OK（10 分 47 秒）+ R2 路 129 OK（10 分 43 秒）✓。关键发现：① `thread_dummy_entry` 必须是 real C 函数（取地址传 `thread_create`）② thread pool 全局状态（`pthread_mutex_t`/`pthread_cond_t`/`g_pool_q[]`）必须留在 rest ③ Windows `__stdcall` trampoline 必须留在 rest ④ xlang-c `-E` 生成大量 weak stdlib 桩（io/ctx/process/string/vec/args_iter 等），`ld -r --allow-multiple-definition` 安全合并。PLATFORM: SHARED thread · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave512** | **R2 Cap residual pure wave512**：**runtime_queue_contention R2 migration 双路验证**。债：`runtime_queue_contention` 产品 seed 仍为混合模式：`.x` 已有 smoke test 完整实现（209 行），但 OS 胶层（5× mutex + 1× spawn）仍为 C，无 thin+rest 构建规则。根修：① 更新 `src/asm/runtime_queue_contention.x`：5× `#[no_mangle]` OS thin wrappers（mutex create/destroy/lock/unlock + spawn）+ 5× `extern "C"` `_impl` bridge 声明 + docblock 规范化 ② 更新 `seeds/runtime_queue_contention.from_x.c`：5 OS 函数重命名为 `_impl` + `XLANG_RUNTIME_QUEUE_CONTENTION_FROM_X` 守卫 + POSIX trampoline cold 路径 `static` 化避免 ld -r 冲突 + Windows `__stdcall` trampoline 保留 rest ③ 更新 `Makefile`：thin+rest `ld -r`。冷路 nm 17 T 符号 ✓；R2 路 nm 5 `_impl`(rest) + 12 thin-provided 合并 ✓。bstrict 冷路 129 OK（9 分 33 秒）+ R2 路 129 OK（9 分 33 秒）✓。关键发现：`.x` 中 POSIX trampoline 符号与 rest 需避免重复（rest 用 `static` + 非导出 wrapper）；Windows `__stdcall` trampoline 必须留在 rest；xlang-c `-E` 生成 weak 符号（io/net/ctx 桩）属正常行为。PLATFORM: SHARED queue · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave511** | **R2 Cap residual pure wave511**：**runtime_math_libm R2 migration 双路验证**。债：`runtime_math_libm` 产品 seed 仍为纯 C（23× libm 函数 + 13× fenv 函数 = 36 个 public API；`.x` 仅有部分 fenv wrapper + `math_special_near`）。类型映射验证：xlang-c `-E` 探针确认 `f64` → `double` ✓，`i32` → `int32_t` ✓，零参数函数 `()` → `void` ✓。根修：① 更新 `src/asm/runtime_math_libm.x`：23× `#[no_mangle]` libm wrappers + 6× fenv wrappers + 3× fenv bridge wrappers + 2× 完整 `.x` 实现（signum/special_near）+ 32× `extern "C"` bridge 声明 ② 更新 `seeds/runtime_math_libm.from_x.c`：23 函数重命名为 `_impl` + 6 fenv `_impl_c` + `XLANG_RUNTIME_MATH_LIBM_FROM_X` 守卫 + forward declarations ③ 更新 `Makefile`：thin+rest `ld -r`。冷路 nm 68 T 符号 ✓；R2 路 nm 98 T 符号合并成功 ✓。bstrict 冷路 129 OK（10 分 43 秒）+ R2 路 129 OK（10 分 46 秒）✓。关键发现：`f64` → `double` 类型映射在 xlang-c `-E` 中工作正常；`void` 参数需使用 `()` 而非 `void` 声明；rest 测试函数需 `#ifdef XLANG_RUNTIME_MATH_LIBM_FROM_X` forward declarations。PLATFORM: SHARED math · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave510** | **Ubuntu L4 金标验证双端钉盘升级 `2bc0885d3`**。触发：wave501-509 共 9 个 SHARED wave（8 R2 OS glue migration + 1 mac L4 冷验证），按 AGENTS.md「SHARED 改动双端验证」原则，必须 Ubuntu 金标验证通过才能升级钉盘。流程：① `git push origin self-hosting`（`2bc0885d3`）→ ② SSH ubuntu-server `git pull` → ③ 全擦 `compiler\|std\|core` 下全部 `.o` + 产品二进制 → `remaining_o=0` ④ `make -C compiler build-tool` ⑤ `make -C compiler bootstrap-driver-seed` 冷启动 → ⑥ `FULL=0 scripts/g05_prepare_and_relink.sh` relink → ⑦ `XLANG_BSTRICT_SKIP_BUILD=1 run-all-bstrict.sh`。结果：`run-all-bstrict OK (129 scripts)` exit 0（耗时 3 分 22 秒）✓。**判据自查**：remaining_o=0 ✓ · 二进制本波 mtime ✓ · bstrict exit 0 + 129 scripts ✓ · 与 mac L4 结果一致 ✓ · 包含本 SHA `2bc0885d3` ✓。**tip L4 升 `2bc0885d3`（双端）**；钉盘升 **`2bc0885d3`**（升自 `d812aeb41`；自 wave500 后累积 10 SHARED wave 双端验证通过）。关键发现：Ubuntu 冷启动 8 个 R2 OS glue 模块（time/dynlib/env/sync/log/backtrace/process/atomic）无回归；Linux x86_64 上 `__atomic_*`/`pthread_*`/`dlopen`/`fork`/`backtrace` 桥接全部正常。PLATFORM: SHARED L4 双端 |
| 2026-07-27 | **wave509** | **tip L4 冷验证 wave501-508 整体 R2 波**。触发：连续 8 个 SHARED R2 OS glue migration wave 完成（time/dynlib/env/sync/log/backtrace/process/atomic），按「整波结束做一次扎实 L4」原则。流程：① 全擦 `compiler\|std\|core` 193 `.o` + 产品二进制 → remaining_o=0 ② `build-tool` 重编 ③ `bootstrap-driver-seed` 冷启动 ④ `g05_prepare_and_relink.sh` relink ⑤ `XLANG_BSTRICT_SKIP_BUILD=1 run-all-bstrict.sh`。结果：`run-all-bstrict OK (129 scripts)` exit 0（耗时 10 分 17 秒）✓。钉盘仍 `d812aeb41`（mac L4 真冷；Ubuntu 双端未验，不升级钉盘）。关键发现：8 个 R2 thin+rest 模式 OS glue 模块在冷路径和 R2 路径均通过，`__atomic_*`/`pthread_*`/`dlopen`/`fork`/`backtrace` 等跨平台桥接工作正常。PLATFORM: SHARED L4 mac · **mac L4 真冷（非双端）** |
| 2026-07-27 | **wave508** | **R2 Cap residual pure wave508**：**runtime_atomic_glue R2 migration 双路验证**。债：`runtime_atomic_glue` 产品 seed 仍为纯 C（33× public API：i16/u16/i32/u32/i64/u64 load/store/CAS/fetch_add/fetch_sub + seq_cst/acquire/release fence 全在 C，`.x` 仅有 doc anchor）。根因：G-02f-19 runtime OS glue 需 R2 full 模式。根修：① 更新 `src/asm/runtime_atomic_glue.x`：33× `#[no_mangle]` public API + 33× `extern "C"` bridge 声明 ② 更新 `seeds/runtime_atomic_glue.from_x.c`：33 函数重命名为 `_impl`（编译器内建 `__atomic_*` + C11 `<stdatomic.h>` + 内联 fallback）+ `#ifndef XLANG_RUNTIME_ATOMIC_GLUE_FROM_X` 守卫 + 前向声明补全 ③ 更新 `Makefile`：thin+rest `ld -r`。冷路 nm 60 T 符号（30× `_impl` + 30× `_c`）✓；R2 路 nm 91 T 符号合并成功 ✓。bstrict 冷路 129 OK（11 分 0 秒）+ R2 路 129 OK（10 分 54 秒）✓。PLATFORM: SHARED atomic · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave507** | **R2 Cap residual pure wave507**：**runtime_process_os_glue R2 migration 双路验证**。债：`runtime_process_os_glue` 产品 seed 仍为纯 C（22× public API：getenv/setenv/unsetenv/getpid/getppid/getcwd/chdir/self_exe_path/spawn/exec/waitpid/pipe 等全在 C，`.x` 仅有 doc anchor）。根因：G-02f-18 runtime OS glue 需 R2 full 模式。根修：① 更新 `src/asm/runtime_process_os_glue.x`：22× `#[no_mangle]` public API + 21× `extern "C"` bridge 声明 ② 更新 `seeds/runtime_process_os_glue.from_x.c`：22 函数重命名为 `_impl`（POSIX fork/execve/waitpid/pipe · Windows CreateProcess/GetExitCodeProcess/CreatePipe）+ `#ifndef XLANG_RUNTIME_PROCESS_OS_GLUE_FROM_X` 守卫 + Windows 桩补全 ③ 更新 `Makefile`：thin+rest `ld -r` ④ 类型适配：`int` → `int32_t`。冷路 nm 42 T 符号 ✓；R2 路 nm 73 T 符号合并成功 ✓。bstrict 冷路 129 OK（11 分 15 秒）+ R2 路 129 OK（9 分 42 秒）✓。PLATFORM: SHARED process · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave506** | **R2 Cap residual pure wave506**：**runtime_backtrace_platform R2 migration 双路验证**。债：`runtime_backtrace_platform` 产品 seed 仍为纯 C（14× public API：backtrace capture/symbolicate/crash evidence 全在 C，`.x` 仅有 doc anchor）。根因：G-02f-19 runtime OS glue 需 R2 full 模式。根修：① 更新 `src/asm/runtime_backtrace_platform.x`：14× `#[no_mangle]` public API + 13× `extern "C"` bridge 声明 ② 更新 `seeds/runtime_backtrace_platform.from_x.c`：13 函数重命名为 `_impl`（POSIX execinfo/dladdr · Windows CaptureStackBackTrace/DbgHelp）+ `#ifndef XLANG_RUNTIME_BACKTRACE_PLATFORM_FROM_X` 守卫 + 前向声明补全 ③ 更新 `Makefile`：thin+rest `ld -r` + 修复 mktemp TMPDIR 变量 ④ 类型适配：`*char` → `*u8`（xlang-c 类型映射）。冷路 nm 32 T 符号 ✓；R2 路 nm 61 T 符号合并成功 ✓。bstrict 129 OK（耗时 11 分 9 秒）✓。PLATFORM: SHARED backtrace · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave505** | **R2 Cap residual pure wave505**：**runtime_log_os R2 migration 双路验证**。债：`runtime_log_os` 产品 seed 仍为纯 C（17× public API：7× core + 10× `_c` convenience/state + 15× `_impl` 分散在 `.x`/C）。根因：G-02f-19 runtime OS glue 需 R2 full 模式。根修：① 更新 `src/asm/runtime_log_os.x`：17× `#[no_mangle]` public API + 15× `extern "C"` bridge 声明 ② 更新 `seeds/runtime_log_os.from_x.c`：8 个 `_c` 函数新增 `_impl` 实现 + `#ifndef XLANG_RUNTIME_LOG_OS_FROM_X` 守卫全部 `_c` 包装 + 3× convenience bridge + 前向声明补全（供 smoke test 引用 thin 符号） ③ 更新 `Makefile`：thin+rest `ld -r`。冷路 nm 36 T 符号 ✓；R2 路 nm 34 T 符号（17 thin + 17 rest）合并成功 ✓。bstrict 129 OK（耗时 9 分 53 秒）✓。PLATFORM: SHARED log · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave504** | **R2 Cap residual pure wave504**：**runtime_sync_os R2 migration 双路验证**。债：`runtime_sync_os` 产品 seed 仍为纯 C（16× public API：mutex 5 + rwlock 6 + condvar 5）。根因：G-02f-19 runtime OS glue 需 R2 full 模式。根修：① 更新 `src/asm/runtime_sync_os.x`：16× `#[no_mangle]` public API + 16× `extern "C"` bridge 声明 ② 更新 `seeds/runtime_sync_os.from_x.c`：16 函数重命名为 `_impl`（Windows CRITICAL_SECTION/SRWLOCK/CONDITION_VARIABLE · POSIX pthread）+ `#ifndef XLANG_RUNTIME_SYNC_OS_FROM_X` 守卫 + 2× smoke test 保留 C ③ 更新 `Makefile`：thin+rest `ld -r`。冷路 nm 34 T 符号 ✓；R2 路 nm 合并成功 ✓。bstrict 129 OK（耗时 9 分 50 秒）✓。PLATFORM: SHARED sync · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave503** | **R2 Cap residual pure wave503**：**runtime_env_os R2 migration 双路验证**。债：`runtime_env_os` 产品 seed 仍为混合模式（`env_build_key` 已迁 `.x`，其余 9 个 public API 全在 C）。根因：G-02f-19 runtime OS glue 需 R2 full 模式。根修：① 更新 `src/asm/runtime_env_os.x`：10× `#[no_mangle]` public API（`env_build_key` + 9× env API）+ 9× `extern "C"` bridge 声明 ② 更新 `seeds/runtime_env_os.from_x.c`：9 函数重命名为 `_impl` bridge + `#ifndef XLANG_RUNTIME_ENV_OS_FROM_X` 守卫 ③ 更新 `Makefile` + `build_xlang_asm.sh`：thin+rest `ld -r`。冷路 nm 19 T 符号 ✓；R2 路 nm 20 env 符号合并成功 ✓。bstrict 129 OK（耗时 10 分 54 秒）✓。PLATFORM: SHARED env · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave502** | **R2 Cap residual pure wave502**：**runtime_dynlib_os R2 migration 双路验证**。债：`runtime_dynlib_os` 产品 seed 仍为混合模式（Windows 函数 `dynlib_win_*` 部分 R2，主 API `dynlib_os_*_c` 全在 C）。根因：G-02f-20 dynlib OS glue 需 R2 full 模式。根修：① 更新 `src/asm/runtime_dynlib_os.x`：完整 `#[no_mangle]` public API（7 函数）+ `extern "C"` bridge 声明 ② 更新 `seeds/runtime_dynlib_os.from_x.c`：全部 public API 改为 `_impl` bridge + `XLANG_RUNTIME_DYNLIB_OS_FROM_X` 守卫 ③ 更新 `Makefile`：thin+rest `ld -r` 构建规则。冷路 nm 10 T 符号 ✓；R2 路 nm 合并成功 ✓。bstrict 129 OK（耗时 9 分 39 秒）✓。PLATFORM: SHARED dynlib · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave501** | **R2 Cap residual pure wave501**：**runtime_time_os R2 migration 双路验证**。债：`runtime_time_os` 产品 seed 仍为纯 C 实现。根因：G-02f-19 runtime OS glue 需 R2 full 模式。根修：① 新建 `src/asm/runtime_time_os.x`：5× `#[no_mangle]` public API + `extern "C"` bridge 声明 ② 更新 `seeds/runtime_time_os.from_x.c`：R2 full 模式守卫 ③ 更新 `Makefile` + `build_xlang_asm.sh`：thin+rest `ld -r`。冷路 nm 10 T 符号 ✓；R2 路 nm 合并成功 ✓。bstrict 129 OK（耗时 11 分 04 秒）✓。PLATFORM: SHARED time · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave500** | **R2 Cap residual wave500 Ubuntu L4 双端验收**：wave499 mac L4 已绿 `d812aeb41`，按 AGENTS.md「SHARED 改动双端验证」+ 钉盘升级条件触发 Ubuntu L4 验收。流程：① git push `self-hosting` → ② SSH ubuntu-server `git pull` → ③ 全擦 `compiler|std|core` 下全部 `.o` + 产品二进制 → `remaining_o=0` ④ `make bootstrap-driver-seed` 冷启动 → ⑤ `FULL=0 scripts/g05_prepare_and_relink.sh` relink → ⑥ 探针验证 → ⑦ 全量 `XLANG_BSTRICT_SKIP_BUILD=1 run-all-bstrict.sh`。探针：`/tmp/w498_multi_combo_probe.x` → exit 0 ✓（Ubuntu 冷编译验证 wave498 功能）。bstrict：`run-all-bstrict OK (129 scripts)` exit 0（耗时 3 分 24 秒）✓。**判据自查**：remaining_o=0 ✓ · 二进制本波 mtime ✓ · 探针全绿 ✓ · bstrict exit 0 + 129 scripts ✓ · 与 mac L4 结果一致 ✓。**tip L4 升 `d812aeb41`（双端）**；钉盘升 **`d812aeb41`**（升自 `c3a81bfd6`；双端验证通过）。PLATFORM: SHARED L4 双端 |
| 2026-07-27 | **wave499** | **R2 Cap residual wave499 mac L4 真冷验收**：wave491-498 构成「泛型 inherent impl 方法完整链路」整体功能波（Self 返回字面 + default 方法 → typeck 推断 → codegen unique combo → codegen multi-combo → 调用端 mangle），按自举方法.md §0.2「整波结束做一次扎实 L4」触发。流程：① 全擦 `compiler|std|core` 下 193 个 `.o` + 产品二进制 → `remaining_o=0` ② pin 产品 seed → ③ `make bootstrap-driver-seed` 冷启动 → ④ `FULL=0 scripts/g05_prepare_and_relink.sh` relink → ⑤ 探针验证 → ⑥ 全量 `XLANG_BSTRICT_SKIP_BUILD=1 run-all-bstrict.sh`。探针：`/tmp/w498_multi_combo_probe.x`（Box<i32>.get()=42 + Box<bool>.get()=true）→ exit 0 ✓ · `/tmp/w499_full_chain.x`（Wrap<i32>.get()=100 + Wrap<bool>.get()=false）→ exit 0 ✓。bstrict：`run-all-bstrict OK (129 scripts)` exit 0（耗时 11 分 33 秒）✓。**判据自查**：remaining_o=0 ✓ · 二进制本波 mtime ✓ · 探针全绿 ✓ · bstrict exit 0 + 129 scripts ✓ · 含本 SHA `d812aeb41` ✓。**tip L4 升 `d812aeb41`（mac）**；钉盘仍 **`c3a81bfd6`**（SHARED 改动需 Ubuntu 金标双端验证后可升钉）。PLATFORM: SHARED L4 mac · **mac L4 真冷（非双端）** |
| 2026-07-27 | **wave498** | **R2 Cap residual pure wave498**：**多 combo 泛型 inherent impl 方法 codegen 单态化**。债：`impl Box<T> { function get(self: Box<T>): T }` 同时实例化 `Box<i32>` + `Box<bool>` → 只发射一个泛型定义（ret=`struct T` incomplete BLD001）+ 符号冲突。根因：① hoisted impl methods `num_generic_params == 0`，bypass `codegen_try_emit_generic_identity_mono` ② wave495 仅处理 unique combo（nc==1），多 combo 仍发裸定义 ③ 调用端 mangle 只处理函数级泛型，不处理 generic inherent impl 方法。根修：① 新增 `codegen_try_emit_generic_impl_method_mono` 遍历参数收集所有 mono combo → 对每个 combo 激活 `mono_active` 上下文 → 发射 mangled 函数定义 ② 新增 `codegen_try_emit_generic_impl_method_extern_mono` extern 版本 ③ `emit_func` / `emit_func_extern_declaration` 调用 impl 级 mono ④ 新增 `codegen_try_emit_impl_method_mono_call_name` 调用端 mangle 辅助 ⑤ 修改所有调用端路径。codegen.x + seed 同 commit 同语义。探针：`Box<i32>.get()=42` + `Box<bool>.get()=true` → exit 0 ✓（双端 L2）。矩阵：bstrict 129 OK · 钉盘仍 **`c3a81bfd6`**。PLATFORM: SHARED codegen · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave497** | **R2 Cap residual wave497 Ubuntu L4 双端验收 wave491-495 整体功能波**：wave496 mac L4 已绿 `c3a81bfd6`，按 AGENTS.md「SHARED 改动双端验证」触发 Ubuntu L4。流程：① git push → SSH ubuntu-server `git pull` → ② 全擦 .o + 产品二进制 → remaining_o=0 → ③ `make -C compiler build-tool` → ④ `make -C compiler bootstrap-driver-seed` 冷启动 → ⑤ `./xlang-build.sh build` (g05 relink) → ⑥ 产品矩阵 → ⑦ 全量 bstrict。矩阵 rv42 / opt102 / si0 / hello0 全绿 ✓。bstrict 129 OK exit 0（耗时 3 分 26 秒）✓。**tip L4 升 `c3a81bfd6`（双端）**；钉盘升 **`c3a81bfd6`**（双端验证通过；前钉 `70ca1089b`）。PLATFORM: SHARED L4 双端 |
| 2026-07-27 | **wave496** | **R2 Cap residual wave496 mac L4 真冷验收 wave491-495 整体功能波**：wave491-495 构成「泛型 inherent impl 方法完整链路」（Self 返回字面 + default 方法 → 伪 gap 清理 → typeck 推断 → codegen 定义/extern 单态化），按「整波结束做一次扎实 L4」触发。流程：① 全擦 207 个 `.o` + 产品二进制 → `remaining_o=0` ② pin 产品 seed → ③ `make bootstrap-driver-seed` 冷启动 → `FULL=0 g05_prepare_and_relink.sh` relink → ④ 产品矩阵 → ⑤ 全量 bstrict。矩阵 rv42 / opt0 / si0 / hello0 全绿 ✓。bstrict 129 OK exit 0（耗时 11 分 11 秒）✓。**tip L4 升 `c3a81bfd6`（mac）**；钉盘仍 **`70ca1089b`**（Ubuntu 双端未验）。PLATFORM: SHARED L4 mac · **mac L4 真冷（非双端）** |
| 2026-07-27 | **wave495** | **R2 Cap residual pure wave495**：**泛型 inherent impl 方法定义/extern codegen 单态化**。债：wave494 typeck 已修 ret=i32，但 codegen 仍以原始 `T` 发射函数定义 + extern 前向声明 → `struct T` incomplete BLD001 + conflicting types。根因：① hoisted impl methods `num_generic_params == 0`，bypass `codegen_try_emit_generic_identity_mono` ② self 形参 `Wrap<T>` 被 wave489 unique-combo 救援，但自由返回类型 `T` 无救援机制 ③ `mono_active` 未设。根修：① 新增 `codegen_build_func_param_mono_map` 辅助（G.7 单一权威）② `emit_func` 在 ret 类型发射前设 `mono_active`，两条成功返回路径都 restore ③ `emit_func_extern_declaration` 同样设 `mono_active`。探针：`/tmp/w493_generic_explicit.x` 返回 42 ✓；`-E` 验证 extern + 定义均 `int32_t` ✓。mac bstrict 129 OK。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED codegen · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave494** | **R2 Cap residual pure wave494**：**泛型 inherent impl 方法 typeck 推断**。债：`w.get()` where w: Wrap<i32>，`impl Wrap<T> { function get(self: Wrap<T>): T }` → typeck 报 `expected i32, found T`。根因：① `w.get()` 解析为 EXPR_METHOD_CALL，走 `pipeline_typeck_check_expr_method_call_c` 非 EXPR_CALL ② 非泛型 UFCS 路径指针相等匹配 self 形参失败 ③ weak integer match 按 TYPE_NAMED kind 匹配成功，在 generic fixup 之前退出 ④ wave494 代码放在 EXPR_CALL 路径，EXPR_METHOD_CALL 永远不执行。根修：① 导出 3 个 pattern-unify 辅助 ② 新建 `pipeline_typeck_method_call_generic_ufcs_c` 共享函数 ③ **放在非泛型 UFCS 之前**调用 ④ weak + strong + .x 三处同 commit 同语义。探针：typeck 正确推断 ret=i32 ✓；codegen 仍以原始 T 发射（后续 wave）。mac bstrict 129 OK。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED typeck · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave493** | **R2 Cap residual pure wave493**：**inherent impl self 类型解析伪 gap 清理 + 文档审计**。债：`impl Point { function get_x(self): i32 {...} }` untyped self → `self.x` resolves to `?`。根因分析：① `parser_asm_seed_parse_into_buf_slice.inc` 被 `PARSER_ASM_THIN_GLUE_NO_SEED_PARSE` 宏排除 ② untyped `self`（无类型标注）**非标准 X 语法**。探针验证：`self: Point` / `self: Config` / `self: Wrap<T>` + i32 返回 **全绿**；唯一真实 gap：泛型 inherent impl 返回 `T`（非产品阻塞）。已回滚 seed 修改 + 删除 7 个伪探针。mac trait/std/generic 全绿 · bstrict 129 OK。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave492** | **R2 Cap residual pure wave492**：**多 empty impl Self + default 方法体验证**（探针波）。债：多 impl + default `Self` 返回 + struct-lit 验证。根修：**探针验证波（无根修）**：① 多 impl + default i32 返回 **已绿**（wave490/491 间接解决）② 多 impl + `Self` 返回 + struct-lit **已绿** ③ string 字面返回 typeck `expected string, found *u8` → **独立语言特性**（需语言级 `String` 类型）。探针：multi_empty_impl_self: trait 套件 OK。矩阵：rv42 / option / hello / stdlib-import。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-27 | **wave491 `85e74aac2`** | **R2 Cap residual pure wave491**：**`Self` 返回字面 + default 方法 method_call**。债：`c.increment().get()` → 假 segfault exit=11 实为正确结果；trait default `increment` 返回 `Self` + method_call 假红 missing method；struct 返回值嵌套 method_call 栈损坏。根因：wave490 multi-combo generic impl mangle 修复后结构类型匹配 + 方法名查找间接解决 `Self` 返回类型正确解析；parser_asm seed wave491 调试遗留 35 处 `fprintf(stderr, "DEBUG ...")` 污染 stdout 致 bstrict 假失败。根修：① 清理 parser_asm seed 全部 DEBUG fprintf（35 处）② 确认 `Self` 返回字面 / default 方法 method_call / struct 返回嵌套调用全绿。mac trait/std/generic 全绿 · bstrict 129 OK。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · codegen · **日常 L2 非 tip L4** |

***

## 7. 禁止清单（本阶段）

- [ ] 未用旧 stage1 / 未用归档 tip 冒充当前 tip L4
- [ ] 未把 prove / Stage2 / N++ 单独写成「可自举」
- [ ] 未 BEST_EFFORT / soft-skip 糊绿
- [ ] 未 seed/.x/glue 分叉合入
- [ ] 未把 mac `-dead_strip` 假绿当 Ubuntu 全链已过
- [ ] 日常 L2 未写成 tip L4；谈升钉须双端 L4 + 129
