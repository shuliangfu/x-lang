# Xlang / X language 自举进度

> **更新**：2026-09-05 · tip **🔔 升钉 `e8176cbe5` → `b5be5ed97`（双端 L4 129）** · 日切：3 日 **90** 行已入 [归档](自举进度-归档-2026-09-03.md) · **金标 SSH＝`ubuntu-server`** · Ubuntu 验＝**git pull（禁 scp）** · Ubuntu L2 **钉盘节奏**\
> **产品 L4 放行钉盘**：**`b5be5ed97`**（2026-09-02 升钉 · 前 `e8176cbe5` 2026-08-23 · 再前 `f7424ae47` 2026-08-15）\
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅**（12 labi_*.x 切片 prefer；默认 FROM_X=1）· parser **7.2.2 关 pin ✅**（tip `-E` + assemble；产品默认 FROM_X=1）· codegen 7.4.2 wave323 ✅ · typeck 7.4.1 wave322 ✅ · runtime monofile wave321 ✅ · present **0** · 钉盘 **`b5be5ed97`** · Stage 8 tip **wave332 Track L 30/30 FULLY CLOSED**（**PRODUCT HALF=0** · **PRODUCT PINNED=0** · **PRODUCT RETIRED 23/23 = 100%**）· **7.4.3 PREFER_X_O 双端 8/8 wave338 ✅** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁**\
> **tip L4 安全网（历史）**：**`ffc0b646f`**（已由 tip L4 升钉吸收；前网 `4bacaf18c`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：Windows 纯 asm：邻域 **6／6** ＋同文件 call／两 let／赋值／while／for／break／continue／`loop`／match／数组 INDEX／STRUCT_LIT＋FIELD／裸 STRUCT_LIT.field／CALL.field `mk().x`／nested `mk().p.f`／pointer intermediate／METHOD.field `P.mk().x`／`P.mk().get()`／`P.get(p)`／guarded match／match subject CALL／INDEX／FIELD／const-int guard `gfalse`／9–16B dual-GP CALL.field／sret ＞16B CALL.field／`take(p: *P) { p.x }`／`x as i32`／ADDR_OF／DEREF `unsafe { return *p }`／let-init DEREF／Cap-fn-ptr `foo as *u8`／TYPE_FN `function(): i32` 间接 call／参／字段／ADDR_OF INDEX／FIELD `&a[0]`／`&s.x`／TYPE_FN return `return foo`／TYPE_FN CALL of CALL `getf()()`／nested ADDR_OF `&s.a[0]`／`&a[0].x`／`&*p`／ADDR_OF CALL／STRUCT_LIT 基 `&mk().x`／`&(P { x: 3 }).x`／`&P.mk().x`／ARRAY_LIT TYPE_FN `[foo, bar]`／`fs[0]()`／STRUCT_LIT 字段 `H { fs: [foo] }`／`h.fs[0]()`／作参 `apply([foo])`／16B 作参 `apply([foo, bar])`／TYPE_SLICE 作参 `take([foo])`／VAR TYPE_SLICE packing lea fat\*／TYPE_SLICE TYPE_FN let-init COMMON BSS／INDEX TYPE_SLICE 形参 bounds／nso_at 非 RETURN expr-stmt／INDEX TYPE_SLICE 形参／局部 store／ADDR_OF INDEX TYPE_SLICE `&xs[0]`／ADDR_OF INDEX TYPE_SLICE 非字面量 `&xs[i]`／INDEX DEREF `*[]i32` `(*p)[1]`／TYPE_SLICE DEREF 作参 `take(*p)`／**CALL 返回 TYPE_SLICE 作参 `take(mk())`**（`slice_call_arg`／`slice_call_arg0`／`slice_call_len`／`slice_id_arg`／`fnslice_call` RUN=4／3／2／4／7）。leftover unique **0**。钉盘仍 `b5be5ed97`。
> **下波候选**：FIELD TYPE_SLICE 作参 `take(s.xs)`（`slice_field_arg` SEGV 139）。WAVE277 ALWAYS 已 T `rebuild_sparse_ifs`（**禁** leftover rest 第二份 dual-def）。钉盘不升。

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1；过程上 prefer+heat 一次／域，双端矩阵同波一次\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；**SHARED 双端 L2**；日常矩阵 **只** `./xbuild l2-matrix`（wave299：捕获 rv=42／opt=102 成功码，禁 set -e 截断）；谈自举／升钉才 L4 + 矩阵 + 全量 bstrict；bstrict **per-script timeout** Ubuntu 300s／Darwin L4 1200s + catalog cache；**墙钟**：`product_l4` 分步 wall + `run-all-bstrict`（`XLANG_BSTRICT_TIMING_FILE`）；并行 **`XLANG_BSTRICT_JOBS` 默认 1**（Ubuntu 金标 1；Darwin L4 默认 1／GATE_CASE_JOBS=1；JOBS=8／4／2 本机 OOM）

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[03](自举进度-归档-2026-09-03.md) · [02](自举进度-归档-2026-09-02.md) · [01](自举进度-归档-2026-09-01.md) · [31](自举进度-归档-2026-08-31.md) · [30](自举进度-归档-2026-08-30.md) · [29](自举进度-归档-2026-08-29.md) · [28](自举进度-归档-2026-08-28.md) · [27](自举进度-归档-2026-08-27.md) · [26](自举进度-归档-2026-08-26.md) · [25](自举进度-归档-2026-08-25.md) · [24](自举进度-归档-2026-08-24.md) · [23](自举进度-归档-2026-08-23.md) · [22](自举进度-归档-2026-08-22.md) · [21](自举进度-归档-2026-08-21.md) · [20](自举进度-归档-2026-08-20.md) · [19](自举进度-归档-2026-08-19.md) · [18](自举进度-归档-2026-08-18.md) · [17](自举进度-归档-2026-08-17.md) · [16](自举进度-归档-2026-08-16.md) · [15](自举进度-归档-2026-08-15.md) · [14](自举进度-归档-2026-08-14.md) · [13](自举进度-归档-2026-08-13.md) · [12](自举进度-归档-2026-08-12.md) · [11](自举进度-归档-2026-08-11.md) · [09](自举进度-归档-2026-08-09.md) · [08](自举进度-归档-2026-08-08.md) · [07](自举进度-归档-2026-08-07.md) · [06](自举进度-归档-2026-08-06.md) · [05](自举进度-归档-2026-08-05.md) · [04](自举进度-归档-2026-08-04.md) · [03](自举进度-归档-2026-08-03.md) · [02](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · …\

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `b5be5ed97`**（2026-09-02 升钉 · 前 `e8176cbe5` 2026-08-23 · 再前 `f7424ae47` 2026-08-15）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**双端 Stage2 = ✅ SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709` · `xlang_asm_stage1`／`xlang_asm2` bit-identical）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（**变更记录只在本文 §6**）。\
**本波主刀 = leftover rest CALL 返回 TYPE_SLICE wrap fat\*（`take(mk())`）＠`8b98c0572`（刀已闭）**。FIELD TYPE_SLICE 作参 `take(s.xs)` 另邻域。

| 指标 | 状态 |
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`b5be5ed97`**；residual tip present **0**；Stage 8 **30/30 FULLY CLOSED**；PRODUCT RETIRED 23/23 |
| Track MG | **11.3.1 MG ✅** |
| Track BC | **🟢 Stage8 30/30** · M4 **5/5** · present **0**；`pipeline_x` mega 仍 host-cc |
| Track L | **收 30/30 FULLY CLOSED** |
| mac / Ubuntu L4（钉盘） | ✅ 双端 L4 + bstrict **129** @ **`b5be5ed97`** |
| pin 蛋 | ✅ darwin.arm64 + linux.x86_64（本波 `xlang_asm`） |
| 本波构建级别 | 钉盘 **L4 已结＠b5be5ed97**；Ubuntu **129 OK** 17m47s；Darwin JOBS=1 **129 OK** 137m25s。check 闸门暂停 |

***

## 1. 文档地图

| 文档 | 看什么 |
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录**（**9 月 5 日**；4 日仍在表内） |
| [自举进度-归档-2026-09-03.md](自举进度-归档-2026-09-03.md) | **9月3日** leftover unique 113→0／P2 Darwin `-o`／windows-gate 全绿（**90** 行） |
| [自举进度-归档-2026-09-02.md](自举进度-归档-2026-09-02.md) | **9月2日** 升钉 `b5be5ed97`／Darwin L4 JOBS／Windows leftover-PE hybrid unique 113→50（**52** 行） |
| [自举进度-归档-2026-09-01.md](自举进度-归档-2026-09-01.md) | **9月1日** typeck pin 7.4.6–8／L4 多轮／Cap va／stage10（**131** 行） |
| [自举进度-归档-2026-08-31.md](自举进度-归档-2026-08-31.md) | **8月31日** stage10 开刀／seed dest-SLICE／leftover／假权威诚实等（**113** 行） |
| [自举进度-归档-2026-08-30.md](自举进度-归档-2026-08-30.md) | **8月30日** leftover XLANG fallthrough F-06／F-07 等（**111** 行） |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图（本波不改） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单；per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`b5be5ed97`** | 2026-09-02 升钉；双端 L4 真冷 + 129 |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自钉盘 L4 `xlang_asm` 刷新 |
| residual tip（BC） | present **0** | Stage 8 30/30 · M4 5/5 · prefer pure-asm 默认 · pipeline_abi mega **硬禁** |
| mac 钉盘 L4 | ✅ L4 + bstrict **129** · 137m25s JOBS=1 | `@b5be5ed97` `/tmp/xlang_l4_Darwin_b5be5ed97_jobs1.log` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 + bstrict **129** · 17m47s | `@b5be5ed97` `ubuntu-server` `/tmp/xlang_l4_Linux_b5be5ed97.log` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | rv42／opt102／hello0／si0／f32 |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ 双端 tip SHA256 STRICT match | 钉盘历史见归档 |
| **R2 / Cap** | 按需 | 明细见 29／31／1 日归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1** |
| `TREE_ARMED` / `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1** / **1** |
| post-delete residual | **✅** wave944–964 |

***

## 3. 当前前排（≤3 条）

1. **Windows 纯 asm 邻域** — `return`／`3+4`／嵌套／`ifelse`／`let+return`／`void` **6／6 RUN 绿**；同文件 call／两 let／赋值／while／for／break／continue／`loop`／match／数组 INDEX／STRUCT_LIT＋FIELD／裸 STRUCT_LIT.field／CALL.field `mk().x`／nested `mk().p.f`／pointer intermediate／METHOD.field `P.mk().x`／`P.mk().get()`／`P.get(p)`／guarded match／match subject CALL／INDEX／FIELD／const-int guard／9–16B dual-GP CALL.field／sret ＞16B CALL.field／`take(p: *P) { p.x }`／`x as i32`／`unsafe { return *p }`／`let y: i32 = *p`／Cap-fn-ptr `foo as *u8`／TYPE_FN `function(): i32` 间接 call／参／字段／ADDR_OF INDEX／FIELD `&a[0]`／`&s.x`／TYPE_FN return `return foo`／TYPE_FN CALL of CALL `getf()()`／nested ADDR_OF `&s.a[0]`／`&a[0].x`／`&*p`／ADDR_OF CALL／STRUCT_LIT 基 `&mk().x`／`&(P { x: 3 }).x`／`&P.mk().x`／ARRAY_LIT TYPE_FN `[foo, bar]`／`fs[0]()`／STRUCT_LIT 字段 `H { fs: [foo] }`／作参 `apply([foo])`／16B `apply([foo, bar])`／TYPE_SLICE `take([foo])`／VAR packing／TYPE_FN let-init COMMON BSS／INDEX TYPE_SLICE 形参 bounds／nso_at 非 RETURN expr-stmt／INDEX TYPE_SLICE 形参／局部 store／ADDR_OF INDEX TYPE_SLICE `&xs[0]`／ADDR_OF INDEX TYPE_SLICE 非字面量 `&xs[i]`／INDEX DEREF `*[]i32` `(*p)[1]`／TYPE_SLICE DEREF 作参 `take(*p)`／**CALL 返回 TYPE_SLICE 作参 `take(mk())`**。
2. **CALL 返回 TYPE_SLICE 作参 `take(mk())`** — leftover-PE get_return 剥 RETURN 成 ARRAY_LIT dest 指针；caller 16B dual-GP 当 2 GP。G.7 补全 mega_body durable COMMON＋rdx=n_arr，for_call_args CALL wrap lea fat*。`slice_call_arg`／`slice_call_arg0`／`slice_call_len` RUN=4／3／2。
3. **下一刀** — FIELD TYPE_SLICE 作参 `take(s.xs)`（`slice_field_arg` SEGV 139）。钉盘不升。

***

## 4. 与归档的关系

- **9月3日归档**：[自举进度-归档-2026-09-03.md](自举进度-归档-2026-09-03.md) — **90** 行 · leftover unique **0** · 钉盘 **`b5be5ed97`**。
- **9月2日及前**：见各日归档链接（§1）。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `b5be5ed97`**。
- 日切后主文档只保留 KPI + 前排 + **9 月 5 日** §6；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
| **macOS arm64** | L4＠**`b5be5ed97` JOBS=1** **`product_l4_true_cold OK` 129** wall **137m25s**（bstrict 130m27s；hello／fs／channel／STD-092 **OK**）／矩阵 **5／5** · 钉盘 **`b5be5ed97`** |
| **Ubuntu x86_64（金标）** | L4＠**`b5be5ed97`** **`product_l4_true_cold OK` 129** wall **17m47s** · SSH＝**`ubuntu-server`** · 钉盘 **`b5be5ed97`** |
| **Windows MSYS2** | leftover unique **0** · min-gate **全绿** · 纯 asm邻域 **6／6**＋call／两 let／赋值／while／for／break／continue／`loop`／match／数组 INDEX／STRUCT_LIT＋FIELD／裸 STRUCT_LIT.field／CALL.field `mk().x`／nested `mk().p.f`／pointer intermediate／METHOD.field `P.mk().x`／9–16B dual-GP CALL.field／sret ＞16B／`take(p: *P)`／`x as i32`／`unsafe { return *p }`／`let y: i32 = *p`／Cap-fn-ptr `foo as *u8`／TYPE_FN `function(): i32` 间接 call／参／字段／ADDR_OF INDEX／FIELD `&a[0]`／`&s.x`／TYPE_FN return `return foo`／TYPE_FN CALL of CALL `getf()()`／nested ADDR_OF `&s.a[0]`／`&a[0].x`／`&*p`／ADDR_OF CALL／STRUCT_LIT 基 `&mk().x`／`&(P { x: 3 }).x`／`&P.mk().x`／ARRAY_LIT TYPE_FN `[foo, bar]`／`fs[0]()`／STRUCT_LIT 字段 `H { fs: [foo] }`／作参 `apply([foo])`／16B `apply([foo, bar])`／TYPE_SLICE `take([foo])`／VAR packing／TYPE_FN let-init COMMON BSS／INDEX TYPE_SLICE 形参 bounds／nso_at 非 RETURN expr-stmt／INDEX TYPE_SLICE 形参／局部 store／ADDR_OF INDEX TYPE_SLICE `&xs[0]`／ADDR_OF INDEX TYPE_SLICE 非字面量 `&xs[i]`／INDEX DEREF `*[]i32` `(*p)[1]`／TYPE_SLICE DEREF 作参 `take(*p)`／**CALL 返回 TYPE_SLICE 作参 `take(mk())`**／guarded match／match subject CALL／INDEX／FIELD／const-int guard · 10.1.3 NT **暂缓** |

***

## 6. 变更记录（倒序 · **9 月 5 日**；4 日仍在表内；3 日见 [归档](自举进度-归档-2026-09-03.md)）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-05 | **fix(windows)** **CALL 返回 TYPE_SLICE wrap fat\*** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover-PE get_return 剥 RETURN 成 ARRAY_LIT，SAT emit_array_lit 留 dest 指针（remaining-wave emit_return dual-GP `#ifndef FROM_X` ABSENT）；caller rec 把 16B rax+rdx 当 2 GP，take 要 1 GP fat*。存储＝leftover rest mega_body get_return＋leftover rest for_call_args。消费＝want-exe `take(mk())`。修：G.7 补全 leftover rest mega_body ARRAY_LIT TYPE_SLICE return durable COMMON＋rdx=n_arr；for_call_args CALL／METHOD wrap store rax+rdx／lea fat*（POSIX ＠79744 同 wrap，不抄 reent deep-copy）。禁 leftover rest remaining-wave emit_return／221／SAT try_index。禁 dual-def WAVE277。证＠`8b98c0572`：leftover rest 430750B＋redirect patched **642**（for_call_args＠0x13c24 first-wins）＋SKIP_HOT（pabi 1411844；xlang_asm 4668833＠04:35）；`slice_call_arg`／`slice_call_arg0`／`slice_call_len`／`slice_id_arg`／`fnslice_call` RUN=4／3／2／4／7；slice_ptr_arg／slice_ptr／twoarg_deref／i32one_var／i32s1／fnslice_let／fnarr0／arr0／i32store／fnslice／rv42／callid／ifelse／nest_deref_arg 不回归。`take(s.xs)`／`let s=mk()` 仍 SEGV 139。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：FIELD TYPE_SLICE 作参 `take(s.xs)`。 |
| 2026-09-05 | **fix(windows)** **TYPE_SLICE DEREF 作参 wrap fat\*** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest for_call_args TYPE_SLICE packing 只认 VAR `enc_local`（local lea／formal load）；DEREF 走 rec 留 dual-GP（rax=data／rdx=length）当 1 GP fat*，callee extra-deref → SEGV 139。存储＝leftover rest WIN leftover for_call_args（`!FROM_X` 或 WIN；POSIX `.x` ＠79135）。消费＝want-exe `take(*p)`。修：G.7 补全 leftover rest for_call_args DEREF tk==11：rec 后 store rax+rdx 到 16B home／lea fat*（POSIX CALL TYPE_SLICE ＠79744 同 wrap）。kind 走 WAVE278 `resolved_type_ref`。禁 leftover rest T SAT try_index（arr0 intercept）。禁 remaining-wave 221／`!FROM_X` 或 WIN。禁抄 `.x` packer。禁 dual-def WAVE277。证＠`5322f3b2b`：leftover rest 429754B＋redirect patched **642**（for_call_args＠0x13c24 first-wins）＋SKIP_HOT（pabi 1410848；xlang_asm 4667809＠04:21）；`slice_ptr_arg`／`slice_ptr_arg0`／`slice_ptr_arg_len` RUN=4／3／2；`fnslice_ptr_arg`／`nest_deref_arg`／`twoarg_deref` RUN=7／4／4；slice_ptr／slice_ptr0／slice_ptr_var／slice_ptr_formal／addrof_sidx_var／addrof_sidx／i32store／i32one_var／i32s1／fnslice_let／fnarr0／arr0／arrstore／i32slen_var／fnslice／rv42／callid／ifelse／dbl_deref／ptr_store／ptr_add／slice_ptr_store／slice_ptr_len／addrof_slice_ptr／slice_ptr_let／slice_ptr_addasg／arr_ptr 不回归。`take(mk())` 仍 SEGV 139。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：CALL 返回 TYPE_SLICE 作参 `take(mk())`。 |
| 2026-09-05 | **fix(windows)** **INDEX DEREF `*[]i32` dual-GP fat** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest emit_deref TYPE_SLICE 留指针（ARRAY twin，`*i32` 刀）；SAT rvalue_slice_once wave773 把 DEREF of *[]T 当 dual-GP（rax=data／rdx=length），leave-ptr 把 fat home 当 data → `(*p)[1]` 读 *(fat+4)（`slice_ptr` RUN=246）。存储＝leftover rest WIN leftover emit_deref（rec 簇 `#ifdef FROM_X`；remaining-wave140 `#ifndef FROM_X` SAT T）。消费＝want-exe `(*p)[1]`。修：G.7 补全 leftover rest emit_deref TYPE_SLICE 走 SAT T `pipeline_asm_deref_struct16_rax_ptr_elf_c`（leftover rest remaining-wave stub `#ifndef FROM_X` ABSENT；同 leftover rest `glue_load_var_as_value_to_rax_rdx` U）。禁 leftover rest T SAT try_index（arr0 intercept）。禁 remaining-wave stub `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`3acdc367c`：leftover rest gcc＋redirect patched **642**（emit_deref＠0xfb26）＋SKIP_HOT（xlang_asm 5028033＠04:04）；`slice_ptr`／`slice_ptr0`／`slice_ptr_var`／`slice_ptr_formal` RUN=4／3／4／4；`(*p)[0]=9`／`(*p).length`／`&(*p)[1]`／`let s=*p`／`(*p)[0]+=6`／`(*a)[1]` TYPE_ARRAY 绿；addrof_sidx_var／addrof_sidx／i32store／i32one_var／i32s1／fnslice_let／fnarr0／arr0／arrstore／i32slen_var／fnslice／rv42／callid／ifelse／dbl_deref／ptr_store／ptr_add 不回归。`take(*p)` 仍 SEGV 139。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_SLICE DEREF 作参 `take(*p)`。 |
| 2026-09-05 | **fix(windows)** **ADDR_OF INDEX TYPE_SLICE 非字面量 scaled lea** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest lvalue INDEX 非字面量 `mul_imm_to_rbx`＋`backend_enc_add_rax_rbx_arch`（32 位 ADD EAX,EBX 零扩展 rax，丢掉 PE `0x14000xxxx` 高位 → SEGV 139；字面量 `&xs[1]` 走 REX.W `add_imm_to_rax` 仍绿）。存储＝leftover rest WIN leftover lvalue（rec 簇 `#ifdef FROM_X`；remaining-wave stub `#ifndef FROM_X` ABSENT）。消费＝want-exe `&xs[i]` 再 `unsafe { return *p }`。修：G.7 补全 leftover rest lvalue INDEX 非字面量：emit idx（i32 sxt）后 64-bit `lea [rax+rbx*esz]`。禁 leftover rest T SAT try_index（arr0 intercept）。禁 remaining-wave scaled `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`817b6447e`：leftover rest 429368B＋redirect patched **642**（lvalue＠0xef14）＋SKIP_HOT（pabi 1410462；xlang_asm 4667809＠03:47）；`addrof_sidx_var`／`addrof_sidx_var0`／`addrof_sidx_var_formal` RUN=4／3／4；`addrof_idx_var` RUN=4；objdump `lea (%rax,%rbx,4)`；addrof_sidx／addrof_sidx1／addrof_sidx_formal／i32store／i32store_local／i32one_var／i32s1／i32local／i32slen_var／fnslice／fnslice_let／fnslice_var／fnarr_arg／fnarr0／arr0／arrstore／rv42／callid／ifelse 不回归。`slice_ptr` `(*p)[1]` 仍 RUN=246。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：INDEX DEREF `*[]i32` `(*p)[1]`。 |
| 2026-09-05 | **fix(windows)** **ADDR_OF INDEX TYPE_SLICE fat.data** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest lvalue INDEX 对 TYPE_SLICE VAR 只 lea fat／load fat*，未剥 data（POSIX `.x` try_index 先 load fat.data；`&xs[0]` 把 fat 当地址 → RUN=127）。存储＝leftover rest WIN leftover lvalue（rec 簇 `#ifdef FROM_X`；remaining-wave stub `#ifndef FROM_X` ABSENT）。消费＝want-exe `&xs[0]` 再 `unsafe { return *p }`。修：G.7 补全 leftover rest lvalue INDEX：base rtk==11 则 load_64_from_rax。禁 leftover rest T SAT try_index（arr0 intercept）。禁 remaining-wave `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`80e3c6e04`：leftover rest 429226B＋redirect patched **642**（lvalue＠0xef14）＋SKIP_HOT（pabi 1410430；xlang_asm 4667809＠03:28）；`addrof_sidx`／`addrof_sidx1`／`addrof_sidx_formal` RUN=3／4／3；addrof_idx／i32store／i32store_local／i32one_var／i32s1／i32local／i32slen_var／fnslice／fnslice_let／fnslice_var／fnarr_arg／fnarr0／arr0／arrstore／rv42／callid／ifelse 不回归。`addrof_sidx_var` `&xs[i]` 仍 SEGV 139。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：ADDR_OF INDEX TYPE_SLICE 非字面量下标 `&xs[i]`。 |
| 2026-09-05 | **fix(windows)** **INDEX TYPE_SLICE store COMMON BSS** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT durable jmp-over 把 const i32 嵌进 RX `.text`（`let xs: []i32 = [3, 4]; xs[0] = 9` 写 RX → SEGV 139；`take([3, 4], 9)` 栈 dest 已绿）。存储＝SAT local t durable（leftover rest remaining-wave `#ifndef FROM_X` ABSENT）。消费＝want-exe INDEX TYPE_SLICE 形参／局部 store。修：G.7 leftover rest WIN leftover unique 跳过 jmp-over，走 SHN_COMMON BSS；redirect SAT slice_from_array intra SAT durable。禁 leftover rest T SAT emit_assign／try_index／global T。禁 remaining-wave `!FROM_X` 或 WIN。禁 leftover rest U SAT local t `seq_take`／`scalar_elem`（const-only＋本地 seq；非 const 退栈）。禁 dual-def WAVE277。证＠`f9f6f08ba`（unique＠`68d712661`）：leftover rest 429126B＋redirect patched **642**（durable＠0x14c89）＋SKIP_HOT（pabi 1410330；xlang_asm 4667809＠03:11）；`i32store`／`i32store_onearg`／`i32store_local`／`i32store1`／`i32asgslice` RUN=9；`nm` `B Lxlang_al_0`＠14000d1e0；i32one_var／i32s1／i32local／i32slen_var／fnslice／fnslice_let／fnslice_var／fnarr_arg／fnarr0／arr0／arrstore／arrstore_arg／param_asg／rv42／callid／ifelse 不回归。`addrof_sidx` `&xs[0]` 仍 127。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：ADDR_OF INDEX TYPE_SLICE `&xs[0]`。 |
| 2026-09-05 | **fix(windows)** **nso_at 非 RETURN expr-stmt** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest nso_at 只认 if／let／loop／for／region，无 let 的 ASSIGN／CALL mega 走 get_return 丢掉（`take(x){x=9; return x}` RUN=3）。存储＝W277_Block.num_expr_stmts＠0x48。消费＝mega nso＞0 → emit_block。修：G.7 补全 nso_at：非 RETURN expr-stmt 才抬 nso；纯 RETURN 仍 get_return（leftover-PE parser 不一定 fold trailing return；emit_block 与 final_expr 对 RETURN 互跳）。禁 leftover rest T SAT emit_assign／try_index。禁 dual-def WAVE277。禁 remaining-wave `!FROM_X` 或 WIN。证＠`824c7c23d`（盲 nexpr＠`5e5b82ac2` 曾 rv42＝1）：leftover rest 427620B＋redirect patched **633**＋SKIP_HOT（pabi 1408870；xlang_asm 4666215＠02:43）；`param_asg` RUN=9／`arrstore_arg` RUN=9；`i32store`／`i32store_local` 仍 SEGV 139；rv42／i32one_var／i32s1／i32local／i32slen_var／fnslice／fnslice_let／fnslice_var／fnarr_arg／fnarr0／arr0／arrstore／callid／ifelse 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：INDEX TYPE_SLICE 形参／局部 store。 |
| 2026-09-05 | **fix(windows)** **INDEX TYPE_SLICE 形参 bounds fat\*+8** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT emit_index intra SAT length_to_rbx intra SAT needs_ptr_load（local t 返 0）把 8B fat\* home 当 16B dual-GP，length 读 saved rbx（param_home+8）→ panic 127（`i32one_var`／`xs[1]`；`xs[0]` 可假绿）。存储＝redirect EMIT_SCAN。消费＝want-exe `take(xs){return xs[0]}`。修：G.7 补全表扫 SAT length → leftover rest WIN leftover needs_ptr_load tk==11。禁 leftover rest T SAT length／try_index／remaining-wave `!FROM_X` 或 WIN。禁 dual-def WAVE277。证＠`2fbf82dff`：redirect patched **7**（含 needs_ptr_load＠0x149ab）＋SKIP_HOT（xlang_asm 4666215＠02:20）；`i32one_var`／`i32s1`／`i32s1_var` RUN=3／4／4；objdump `load fat*; add 8; load length`；i32local／i32slen_var／fnslice／fnslice_let／fnarr_arg／fnarr16／fnarr0／arr0／rv42／callid／ifelse 不回归。禁改 mega／测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：INDEX TYPE_SLICE 形参 store。 |
| 2026-09-05 | **fix(windows)** **COFF COMMON BSS（TYPE_FN let-init）** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝COFF writer 把 SHN_COMMON 写成 SectionNumber=1 Type=FUNCTION，`Lxlang_al_*` 落 RX `.text`（store／call SEGV 139）；ELF／Mach-O 已进 BSS。存储＝`ElfSymEntry.sym_shndx=0xfff2`＋offset=size。消费＝want-exe `let fs: []function(): i32 = [foo]; fs[0]()`。修：G.7 补全 seed `seed_platform_coff_write_coff_o_to_buf`＋`coff.x` 孪生：COMMON → IMAGE_SYM_UNDEFINED＋Value=size＋EXTERNAL。禁 leftover rest remaining-wave `!FROM_X` 或 WIN。禁 leftover rest T SAT try_index／WAVE277。证＠`966d3c4c7`：seed_bridge 12082B＠01:59＋SKIP_HOT（xlang_asm 4666215＠02:03）；`nm` `B Lxlang_al_0`；`fnslice_let`／`fnslice_var` RUN=7；i32slen_var／fnslice／fnarr_arg／fnarr16／fnarr0／arr0／rv42／callid／ifelse 不回归。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／SHARED COFF cross-emit。下一刀：INDEX TYPE_SLICE 形参 bounds。 |
| 2026-09-05 | **fix(windows)** **leftover rest VAR TYPE_SLICE packing lea fat\*** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest for_call_args VAR 走 `load_var` dual-GP（data 当 1 GP）；形参要 fat\*。存储＝leftover rest WIN leftover for_call_args。消费＝want-exe `let fs; take(fs)`。修：G.7 补全 tk==11 走 leftover rest `glue_enc_local`（local lea／formal load）。kind 走 var_decl／WAVE278。禁 remaining-wave 221 pty。禁 leftover rest T SAT try_index。禁抄 `.x` TYPE_ARRAY-as-SLICE wrap。证＠`f736b04a4`：leftover rest 427508B＋redirect patched **626**＋SKIP_HOT（pabi 1408758；xlang_asm 4666548＠01:36）；`i32slen_var` RUN=2（fat\* 绿）；`fnslice_var`／`fnslice_let` 仍 SEGV 139（TYPE_FN let-init COMMON 落 `.text`）；`i32one_var` INDEX 127（INDEX 形参 bounds 读 saved rbx）。fnslice／fnarr_arg／fnarr16／fnarr0／arr0／rv42／callid／ifelse 不回归。刀未闭。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_SLICE TYPE_FN let-init COMMON。 |
| 2026-09-05 | **fix(windows)** **leftover rest TYPE_SLICE 作参 `take([foo])`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest for_call_args ARRAY_LIT tk≠10 仍 dest 指针；TYPE_SLICE 形参要 fat*（1 GP）；SAT dest 与 fat home 叠槽则 INDEX0 读 dest 指针位（127）。存储＝leftover rest WIN leftover for_call_args。消费＝want-exe `take([foo])`。修：G.7 补全 dest-SLICE stamp 后 wrap `{data,n_arr}`；先留 home＋16B 空隙再 rec。kind 走 WAVE278 `resolved_type_ref`。禁抄 `.x` durable／SIMD packer。禁 leftover rest T SAT try_index。禁 remaining-wave 221 `call_param_ty_get`。证＠`3b28e38ab`（wrap＠`e9590c023`／home＠`13361d13c`）：leftover rest 427444B＋redirect patched **626**＋SKIP_HOT（pabi 1408238→1408694；xlang_asm 4666548＠01:24）；`fnslice`／`fnslice2`／`i32slice` RUN=7；`i32one`／`i32s0`／`i32s1`／`i32slen` 绿；fnarr_arg／fnarr16／i32arg16／fnarr0／arr0／rv42／callid／ifelse 不回归。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：VAR TYPE_SLICE 作参。 |
| 2026-09-05 | **fix(windows)** **leftover rest TYPE_ARRAY 16B 作参 `apply([foo, bar])`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SysV ARRAY→8 E*，SAT try_index intra SAT enc_local lea 8B 槽当 payload（8B load_64 补偿）；16B dest 指针进 rdi → INDEX `call *dest` SEGV 139／`[4]i32` 垃圾和。VAR 16B dual-GP 同红。存储＝leftover rest WIN leftover needs_ptr_load／for_call_args。消费＝want-exe `apply([foo, bar])`。修：G.7 补全 T[N] 形参 nbytes＞8 load E*；8B 仍 payload lea；＞8B VAR lea dest；redirect SAT try_index → leftover rest enc_local。禁 leftover rest T SAT try_index（arr0 intercept）。TYPE_FN elem 宽 8 补 leftover rest `glue_fixed_array_total_bytes`。证＠`c04ad21c2`：leftover rest 426988B＋redirect patched **626**＋SKIP_HOT（pabi 1407792→1408238；xlang_asm 4666036＠00:52）；`fnarr16`／`fnarr16_let` RUN=7／`i32arg16` RUN=10；fnarr_arg／i32arg8／fnarr0／arr0／rv42／callid／ifelse 不回归。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：ARRAY_LIT TYPE_SLICE 作参。 |
| 2026-09-05 | **fix(windows)** **leftover rest ARRAY_LIT TYPE_FN 作参 `apply([foo])`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest rec ko==46 → SAT emit_array_lit dest 指针进 rdi，callee INDEX 对 E* 槽 lea → SEGV 139；VAR `apply(fs)` 已 load_var 8B。SAT `glue_try_index` 与局部 ARRAY 同面（禁拦截）。SAT remaining-wave 221 `call_param_ty_get` SAT standalone ABSENT（禁 leftover rest UNDEF／禁 remaining-wave 原件 WIN leftover 第二份）。存储＝leftover rest WIN leftover for_call_args。消费＝want-exe `apply([foo])`。修：G.7 补全 ARRAY_LIT TYPE_ARRAY ≤8B load_64 dest payload；kind 走 WAVE278 `resolved_type_ref`。证＠`eb50d1198`（load＠`5bfc3698d`）：leftover rest 426542B＋redirect patched **613**＋SKIP_HOT（pabi 1407548→1407792；xlang_asm 4665524＠00:25）；`fnarr_arg` RUN=7；fnarr_arg_let／fnarr0／fnarr_st／arr0／rv42／callid／ifelse 不回归。16B `apply([foo, bar])` 仍 dest 指针（另邻域）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：ARRAY_LIT TYPE_ARRAY 16B 作参。 |
| 2026-09-04 | **probe(windows)** **ARRAY_LIT TYPE_FN STRUCT_LIT 字段 `H { fs: [foo] }`／`h.fs[0]()`** | **探针** · **不升钉** · Windows leftover-PE L2＠`4d216d78c`（xlang_asm 4665524＠23:54）。五问：STRUCT_LIT 字段走 SAT emit_struct_lit＋INDEX 本槽（与 let 同，宽 8 已闭）。证：`fnarr_st`／`fnarr_st2_0` RUN=7／`fnarr_st2` RUN=11；fnarr0／fnarr1／fnfield／arr0／fnarr_arg_let 不回归。作参 `apply([foo])` caller 把 ARRAY_LIT dest 指针塞 rdi，callee INDEX 对槽 lea → SEGV 139（`let fs; apply(fs)` 已绿）。禁 SAT glue_try_index 拦截（与局部 ARRAY 同面）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。PLATFORM: WINDOWS leftover-PE。下一刀：ARRAY_LIT TYPE_FN 作参 `apply([foo])`。 |
| 2026-09-04 | **fix(typeck／windows)** **ARRAY_LIT TYPE_FN 元素 coerce `[foo, bar]`／`fs[0]()`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝`typeck_coerce_array_lit_elem_types_to_decl` 写死 `0 as *Module`（leftover-PE BSS 空 → XT001 expected function found *u8）；SAT emit_array_lit intra SAT `elem_byte_sz` 漏 TYPE_FN=18 → `mov %eax` 截断 PE 指针、INDEX 读 8 → SEGV 139。存储＝typeck.x ＋ pin ＋ leftover rest WIN leftover `array_lit_elem_byte_sz`（已认 kind 18／9 → 8）。消费＝want-exe `let fs: [2]function(): i32 = [foo, bar]; return fs[0]()`。修：G.7 补全 coerce 传 live module（wrapper／let／assign／return／call-arg；C 包装仍 0）＋redirect SAT emit_array_lit → leftover rest elem_byte_sz。证＠`cb3e910d6`（coerce＠`352c997d6`）：typeck_x 332784B＋redirect patched **9**（新面）＋SKIP_HOT（pabi 1407548；xlang_asm 4665524＠22:59）；`fnarr0`／`fnarr1n` RUN=7／`fnarr1` RUN=11／`fnarr_as`／`fnarr_asg` RUN=7；fnret／fnret_call／fntype／asfn／arr0／naddr_mkx／ptr7u／nestptr／asgimm／letret／rv42／callid 不回归。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: SHARED typeck／WINDOWS leftover-PE 探针。下一刀：ARRAY_LIT TYPE_FN 作 STRUCT_LIT 字段／作参。 |
| 2026-09-04 | **fix(windows)** **leftover rest ADDR_OF CALL／STRUCT_LIT 基 `&mk().x`／`&(P { x: 3 }).x`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest `lvalue_eff_addr` 只认 VAR／INDEX／FIELD／DEREF；`&mk().x`／`&(P { x: 3 }).x` recurse lvalue(CALL／STRUCT_LIT) → −1 → FAST_UNHANDLED。SAT `call_base_rvalue` 是 SAT local t（UNDEF 不解析）。存储＝leftover rest WIN leftover lvalue（rec 簇 `#ifdef FROM_X`；remaining-wave stub 仍 `#ifndef FROM_X`）。消费＝want-exe `&mk().x` 再 `unsafe { return *p }`。修：G.7 补全 leftover rest lvalue CALL／METHOD／STRUCT_LIT 物化（同 leftover rest field_access_fast；STRUCT_LIT SAT dest 与 let 槽重叠则 park＋qword copy，禁 enc_call memcpy）。证＠`aba7b5d20`（CALL 基＠`9472b6156`）：leftover rest 重编＋redirect patched **604**＋SKIP_HOT（pabi 1406022→1407548；xlang_asm 4663988→4665524）；`naddr_mkx`／`naddr_mky` RUN=3／4／`naddr_stx`／`naddr_sty` RUN=3／4／`naddr_mk`／`naddr_assocx` RUN=3；naddr_sa／naddr_ax／naddr_deref／mkx／stfield／arr0／ptr7u／fnret_call／asfn／nestptr／asgimm／letret／rv42／wh3／for3／ifelse／callid 不回归。ARRAY_LIT TYPE_FN 元素 coerce 仍 `0 as *Module`（另邻域）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：ARRAY_LIT TYPE_FN 元素 coerce。 |
| 2026-09-04 | **fix(windows)** **leftover rest nested ADDR_OF `&s.a[0]`／`&a[0].x`／`&*p`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest ADDR_OF INDEX／FIELD 只认 base_ko＝3；nested `&s.a[0]`／`&a[0].x` 与 ADDR_OF DEREF-operand `&*p` FAST_UNHANDLED → CG002。SAT `glue_emit_index_eff_addr` 是 SAT intra；leftover rest remaining-wave `lvalue_eff_addr` 是 `#ifndef FROM_X` stub −1（补 SAT stub 对 leftover-PE 无效）。存储＝leftover rest WIN leftover ADDR_OF／lvalue（rec 簇 `#ifdef FROM_X`）。消费＝want-exe nested ADDR_OF 再 `unsafe { return *p }`。修：G.7 补全 leftover rest WIN leftover `pipeline_asm_emit_lvalue_eff_addr_elf_c`（VAR／INDEX／FIELD／DEREF；*T 中段 load64；INDEX 对 FIELD 基 *T extra-load）再 ADDR_OF INDEX／FIELD／DEREF 转调。禁 UNDEF SAT index_eff_addr／`call_base_rvalue`。证＠`9faf656fc`：leftover rest 重编＋redirect patched **604**＋SKIP_HOT（pabi 1405594→1406022；xlang_asm 4663970→4663988）；`naddr_sa`／`naddr_sa1` RUN=7／8／`naddr_ax`／`naddr_ay`／`naddr_a1x` RUN=3／4／5／`naddr_deref` RUN=7／`naddr_idxfld` RUN=3；naddr_idx／naddr_fld／arr0／ptr7u／fnret_call／asfn／nestptr／mkx／asgimm／letret／rv42／wh3／for3／match2／ifelse／callid 不回归。ADDR_OF CALL／STRUCT_LIT 基仍 FAST_UNHANDLED（另邻域）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：leftover rest ADDR_OF CALL／STRUCT_LIT 基。 |
| 2026-09-04 | **fix(typeck)** **TYPE_FN CALL of CALL `getf()()`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝`typeck_check_expr_call_resolve` TYPE_FN／Cap 闸门只认 VAR／INDEX／FIELD；`getf()()` callee 是 EXPR_CALL＝48，外层 resolved_type 仍 `?` → XT001 expected i32 found ？。存储＝typeck.x ＋ pin `typeck_gen.linux.x86_64.c`。消费＝want-exe `return getf()()`。修：G.7 补全同一闸门接受 CALL／METHOD_CALL（check_expr 内层再 stamp TYPE_FN elem）。证＠`15b0fe3e7`：typeck_x 重编 332672B＋SKIP_HOT（xlang_asm 4663970；pabi 1405594 未动）；`fnret_call` RUN=7／`fnret_call_arg` RUN=11；fnret／fnret_arg／fntype／fnfield2／asfn／addrof_idx／addrof_fld／arr0／ptr7u／nestptr／sret24a／dual12c／mkx／asgimm／letret／rv42／wh3／for3／match2／ifelse／callid 不回归。nested ADDR_OF 仍 FAST_UNHANDLED（另邻域）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: SHARED typeck／WINDOWS leftover-PE 探针。下一刀：leftover rest nested ADDR_OF（base_ko ≠ 3）。 |
| 2026-09-04 | **fix(typeck)** **TYPE_FN return `return foo` 自 `function(): i32`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝`typeck_return_operand_matches` 把 `typeck_fnptr_surface_compat` 的 module 写死 0，再 fallback `pipeline_typeck_active_module_c`；leftover-PE BSS 空（WEAK／dual BSS）→ XT001 expected function found *u8。let／assign／字段已传 live module。存储＝typeck.x ＋ pin `typeck_gen.linux.x86_64.c` ＋ empty_surface 签名。消费＝want-exe `getf(): function(): i32 { return foo }`。修：G.7 补全 return 权威传 module（C 包装仍 0，保旧 ABI）。证＠`74b369e07`：typeck_x 重编 332608B＋SKIP_HOT（xlang_asm 4663458）；`fnret` RUN=7／`fnret_arg` RUN=11；fntype／fnfield2／asfn／addrof_idx／addrof_fld／arr0／ptr7u／nestptr／sret24a／dual12c／mkx／asgimm／letret／rv42／wh3／for3／match2／ifelse／callid 不回归。`getf()()` 仍 XT001 expected i32 found ？（另邻域）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: SHARED typeck／WINDOWS leftover-PE 探针。下一刀：TYPE_FN CALL of CALL `getf()()`。 |
| 2026-09-04 | **fix(windows)** **leftover rest ADDR_OF INDEX／FIELD `&a[0]`／`&s.x`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest WIN leftover `emit_addr_of` 只认 VAR／ARRAY_LIT；INDEX／FIELD 仍 FAST_UNHANDLED → CG002 labels=1。SAT `glue_emit_index_eff_addr` 是 SAT intra（UNDEF 不解析）；leftover rest `lvalue_eff_addr` 是 leftover rest stub −1＠31725。存储＝leftover rest ADDR_OF（rec 簇 `#ifdef FROM_X`；remaining-wave140 仍 `#ifndef FROM_X`）。消费＝want-exe `&a[0]`／`&s.x`。修：G.7 补全 leftover rest ADDR_OF VAR-base lea（leftover rest slots＋`glue_enc_local_slot` lea TYPE_ARRAY／load *T＋WAVE278 index／FIELD layout_offset＋enc add／mul）。禁 UNDEF SAT index_eff_addr／禁 dual-def leftover rest lvalue_eff_addr。证＠`e6b695272`：leftover rest 重编＋redirect patched **604**＋SKIP_HOT（pabi 1404386→1405594；xlang_asm 4662434→4663458）；`addrof_idx`／`addrof_idx1`／`addrof_idxd` RUN=7／8／7；`addrof_fld`／`addrof_fldy`／`addrof_fldd` RUN=3／4／3；addrof／arr0／stfield／ptr7u／fntype／fnfield2／asfn／sret24a／dual12c／mkx／nestptr／asgimm／letret／rv42／wh3／for3／match2／ifelse／callid 不回归。`return foo` 自 `function(): i32` 仍 XT001（typeck 另邻域）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_FN return `return foo`。 |
| 2026-09-04 | **fix(windows)** **leftover rest TYPE_FN 字段／间接 call** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT emit_struct_lit intra SAT `field_store_sz`（TYPE_FN kind miss → size 0 跳过写入；`*u8` 字段已绿）；VAR-base `h.f` SAT `load_byte_sz` 按字段名扫全 layout 把 `f` 当 i32（cltq＋call *trunc）。存储＝leftover rest WIN leftover `field_store_sz` first-wins（WAVE270 kind＋TYPE_FN=18→8）＋leftover rest emit_var_field_access／field_access_fast load 宽 8（WAVE278 resolved_type；SAT load_byte_sz 是 `#ifndef FROM_X`，UNDEF 不改 SAT standalone）。消费＝want-exe `H { f: foo }; h.f()`／`let g = h.f; g()`。修：G.7 补全 leftover rest field_store_sz＋redirect SAT fields_elf；FIELD load 先认 TYPE_FN／PTR。证＠`0e76284eb`（store＠`0b994fa20`）：leftover rest 重编＋redirect patched **604**＋SKIP_HOT（pabi 1403230→1404386）；`fnfield`／`fnfield2` RUN=7；`fntype`／`fntype_as`／`fntype_arg`／`fnparam`／`fnnest`／`capcall`／`capderef`／asfn／ptr7u／addrof／sret24a／dual12c／mkx／nestptr／arr0／asgimm／letret／rv42／wh3／for3／match2 不回归。`*fn(): i32` 是错语法；`return foo` 自 `function(): i32` 仍 XT001（typeck 另邻域）；`&a[0]`／`&s.x` 仍 CG002。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：ADDR_OF INDEX／FIELD。 |
| 2026-09-04 | **fix(windows)** **leftover rest Cap-fn-ptr `foo as *u8`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest emit_as_impl／emit_expr_fast VAR Cap 卡 `is_no_mangle`；leftover-PE 一律源名 `T foo`／`T id`（export／plain 与 `#[no_mangle]` 同）。存储＝leftover rest WIN leftover Cap 路径（`FROM_X && WIN`；POSIX `.x` 仍只认 no_mangle）。消费＝want-exe `foo as *u8`。修：G.7 补全名字命中即 lea（locals 仍优先）。证＠`1b29479b2`：leftover rest 重编＋redirect patched **565**＋SKIP_HOT（pabi 1403326→1403230）；`asfn`／`asfn_export`／`asfn_bare`／`asfn_noas` RUN=7；as32／ptr7u／addrof／takepu／takef／sret24a／dual12c／mkx／nestptr／arr0／asgimm／letret／rv42／wh3／for3／match2 不回归。`*fn(): i32` 仍 BLD001 无 WinMain（TYPE_FN 另邻域）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：TYPE_FN `*fn(): i32`／间接 call。 |
| 2026-09-04 | **fix(windows)** **leftover rest let-init DEREF／`unsafe { let y }`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝SAT emit_block `num_locals-nlets`；mega leftover rest fill 只填函数体 let，WAVE277 region let 无槽（`letinit_lit`／addrof CG002 labels=1；`return *p` 已绿因 region 无 let）。存储＝leftover rest fill_local_slots＋leftover rest WIN leftover emit_block_body（`FROM_X && WIN`；POSIX ABSENT）。消费＝want-exe `let y: i32 = *p`。修：G.7 补全 fill 走 WAVE277 region／if／while／for；emit_block_body 用 `block_slot_get`（≡ `.x` `slot_base_for`）＋SAT emit_block_inits／if／while／for 全局 T。禁 UNDEF SAT local t `glue_block_body_bind`／`set_scope_block`。证＠`4620ce855`：leftover rest 重编＋redirect patched **565**＋SKIP_HOT（pabi 1401718→1403326）；`letinit_lit`／`addrof`／`addrof_param` RUN=7；ptr7u／takepu／ifelse／wh3／for3／match2／sret24a／dual12c／mkx／nestptr／arr0／asgimm／letret／rv42 不回归。`asfn` CG002（Cap-fn-ptr 另邻域）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：Cap-fn-ptr `foo as *u8`。 |
| 2026-09-04 | **fix(windows)** **leftover rest ADDR_OF／DEREF `unsafe { return *p }`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest rec ko==51／52 调 SAT remaining-wave T，SAT intra SAT emit_expr／resolved_type；SAT emit_block intra SAT region_body 跳过 `unsafe { }`（ptr7u dump：lea `&x` 后直接 epilogue）；nso_at 在 nlet＝0 时返 0，无 let 的 `take` 走 get_return。存储＝leftover rest WIN leftover ADDR_OF／DEREF（rec 簇 `#ifdef FROM_X`，remaining-wave140 仍 `#ifndef FROM_X`）＋redirect WAVE277 region＋leftover rest nso_at `num_regions@0x30`。消费＝want-exe `unsafe { return *p }`。修：G.7 补全 leftover rest VAR ADDR_OF（slot＋lea）＋scalar DEREF（leftover rest emit_expr 同 .o＋load_i32）；redirect `region_body_ref`／`num_regions`；nso_at 计 region。证＠`f648c1bcb`：leftover rest 重编＋redirect patched **395**＋SKIP_HOT（pabi 1400734→1401718）；`ptr7u`／`takepu` RUN=7；takef／as32／sret24a／dual12c／mkx／mkassocx／nestptr／arr0／asgimm／letret／rv42 不回归。`let y = *p` 仍 CG002（let-init 另刀）；`asfn` CG002（Cap-fn-ptr 另邻域）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：let-init DEREF `let y: i32 = *p`。 |
| 2026-09-04 | **fix(windows)** **leftover rest `glue_emit_module_from_ctx` harvest stub** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest harvest unique stub `return NULL` first-wins；emit_as_impl fnptr／needs_ptr_load *T 形参／`glue_type_size_simple` NAMED 无 module。存储＝leftover rest WIN leftover T＠14642。消费＝want-exe `take(p: *P) { p.x }`。修：G.7 补全 leftover rest 体 ≡ `.x`（SAT `pipeline_asm_emit_module_ref_c` 全局 T，leftover rest remaining wave136 已闭，禁 leftover rest 第二份 T；fallback LP64 ctx.module_ref@16，leftover rest 无 `pipe_load_ptr_slot` T）。证＠`c889773f5`：leftover rest 重编＋redirect patched **390**＋SKIP_HOT（pabi 1400672→1400734）；`takef` RUN=7／`as32` RUN=7；sret24a／dual12c／mkx／mkassocx／nestptr／arr0／asgimm／letret／rv42 不回归。`ptr7u` `unsafe { *p }` RUN=40（ADDR_OF／DEREF 另刀）；`asfn` CG002（Cap-fn-ptr 另邻域）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：leftover rest ADDR_OF／DEREF `*p`。 |
| 2026-09-04 | **fix(windows)** **leftover rest callee sret ＞16B CALL.field** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest mega_body 已置 sret BSS 但不存 rdi；leftover rest param_home `np<=0` 早退；SAT emit_struct_lit intra SAT `sret_active` BSS=0 写本地 `rbp-0x18`。SAT `fields_elf` 是 SAT local t（UNDEF 不解析）；`enc_call memcpy` leftover-PE 打成递归 `call mk` RUN=127。存储＝leftover rest param_home／rec ko==45／mega_body sret_home。消费＝want-exe sret24a／sret24f。修：G.7 补全 leftover rest param_home 存 rdi＋GP 位移；rec SAT emit_struct_lit 全局 T 后 qword copy 到 dest；sret_home 停在 next+256（避开 SAT 本地 dest 盖 rdi）。证＠`d8318e94c`：leftover rest 重编＋redirect patched **390**＋SKIP_HOT（pabi 1399960→1400672）；`sret24a`／`sret24f` RUN=1／6；dual12c／dual16d／meth12c／nest12c／sret24let／mkx／st24f／mkassocx／nestptr／arr0／asgimm／letret／rv42 不回归。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：`glue_emit_module_from_ctx` stub。 |
| 2026-09-04 | **fix(windows)** **leftover rest field_access_fast 9–16B dual-GP CALL.field** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest WIN leftover `field_access_fast` CALL／METHOD 根只存 packed rax 8B；`mk().c`（12B offset 8）／`mk().d`（16B offset 12）RUN=0。SAT `glue_field_access_call_base_rvalue` 是 SAT local t（UNDEF 不解析）；`glue_store_retval` leftover rest FROM_X ABSENT（SAT remaining-wave 是 stub）。存储＝leftover rest WIN leftover field_access_fast。消费＝want-exe dual12c／dual16d。修：G.7 补全 CALL／METHOD 根 ret_sz（WAVE278 resolved_type＋leftover rest `glue_type_size_simple`／`glue_call_return_byte_size`）；≤8B 仍 rax@home；9–16B rax@home＋rdx@home-8（enc 全局 T）；＞16B 预布 SysV sret（lea dest／rdi／SAT `set_call_sret_reg_shift` 全局 T）。证＠`a63f9bb7f`：leftover rest 重编＋redirect patched **390**＋SKIP_HOT（pabi 1399342→1399960）；`dual12c`／`dual16d` RUN=3／4／`meth12c`／`nest12c` RUN=3；mkx／mk8y／st12c／st24f／dual12let／sret24let／mkassocx／nestptr／arr0／asgimm／letret／rv42 不回归。`sret24a`／`sret24f` 仍红（callee SAT emit_struct_lit 写 `rbp-0x18` 本地 dest、忽略 rdi；sret24let 走 try_inline 无 call）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：sret ＞16B CALL.field（callee 存 rdi＋memcpy dest）。 |
| 2026-09-04 | **fix(typeck)** **associated Type.method() `P.mk().x`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝`typeck_check_expr_method_call` UFCS 只认 nparams＝num_args＋1；`P.mk()` 基是类型名 VAR，check_expr 失败后 base_rc≠0 直接返 −1（未到 LANG-004）。存储＝METHOD_CALL resolve／resolved_type。消费＝want-exe `P.mk().x` XT001。修：G.7 补全关联路径（struct layout 名＋nparams＝num_args）＋ emit UFCS leave 在 nparams＝nargs 时 has_recv＝0。pin twin＋seed `backend_call_dispatch.from_x.c` 同 commit。证＠`20787a2b7`：typeck_x＋dispatch 重编＋SKIP_HOT（pabi 1399342 未动）；`mkassocx`／`mkassocy` RUN=3／4／`mkget`／`pget_ufcs` RUN=3／`mkid` RUN=7／`pmk_let` RUN=7；instget／mkx／nestptr／nestf／stfield／arr0／asgimm／letret／rv42／callid 不回归。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: SHARED typeck／emit／WINDOWS leftover-PE 探针。下一刀：＞8B dual-GP／sret。 |
| 2026-09-04 | **fix(windows)** **leftover rest field_access_fast pointer intermediate** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest WIN leftover `field_access_fast` walk FIELD 链到 CALL／STRUCT_LIT 根后只加偏移，中段 *T 不当 load；`(Outer { p: &i }).p.f` 编译 RUN=104。SAT `glue_index_deref_ptr_field_slot_rax` leftover rest FROM_X T 是空 stub（rest-first 会赢）；`glue_field_access_field_type_ref` leftover rest T 返 0；SAT `glue_field_chain_mid_auto_deref` 勿 UNDEF。存储＝leftover rest WIN leftover field_access_fast。消费＝want-exe nestptr。修：G.7 补全中段 hop（ci＞0）读 WAVE278 `resolved_type_ref`＋WAVE270 `type_kind_ord`，TYPE_PTR=9 则 `load_64_from_rax`（不剥 TYPE_SLICE=11）。证＠`91c055297`：leftover rest 重编＋redirect patched **390**＋SKIP_HOT（pabi 1399342）；`nestptr`／`mkptr` RUN=7／`nestptrg` RUN=5；nestf／nestst／nestlet／mkx／stfield／arr0／asgimm／letret／rv42／callid 不回归。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：METHOD.field `P.mk().x` XT001。 |
| 2026-09-04 | **fix(windows)** **leftover rest field_access_fast nested `mk().p.f`** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝leftover rest WIN leftover `field_access_fast` 只认直接 CALL／STRUCT_LIT 基；`mk().p.f` 外层基是 FIELD → lvalue_eff_addr → rec ko==44 仅 enum → CG002 labels=4／2 patches=0。SAT `glue_field_access_call_base_rvalue` 是 SAT local t（leftover rest UNDEF 不解析）。存储＝leftover rest WIN leftover field_access_fast。消费＝want-exe nestf。修：G.7 补全 walk FIELD 链（cap 16）到 CALL／METHOD／STRUCT_LIT 根；根物化一次（STRUCT_LIT：SAT emit_struct_lit dest 留 rbx；CALL：leftover rest rec＋≤8B packed rax layout 临时槽）再 inner→outer 加偏移、load 最外层。证＠`3faaa6ff3`：leftover rest 重编＋redirect patched **390**＋SKIP_HOT（pabi 1399178）；`nestf`／`nestf4`／`nestst` RUN=7／`nestoff` RUN=5／`nest3` RUN=6／`nestox` RUN=9／`nestlet` RUN=7；mkx／mky／mksum／stfield／stfieldsum／arr0／asgimm／callid／letret／rv42／methget 不回归。`nestptr` RUN=104（*T 中段另刀）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：pointer intermediate `nestptr`。 |
| 2026-09-04 | **fix(typeck)** **MATCH CTFE const-int guard** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝`typeck_fold_expr_ref_impl` MATCH CTFE 把第一个 `_` 当无 guard 终臂（注释写 guarded 不折、实现没读 `guard_ref`）；const subject `match 1 { _ if 0 => 1; _ => 7 }` 折成 `mov $1`。存储＝cap residual＋pin twin。消费＝want-exe `gfalse` RUN=1。VAR subject 已走 emit_match TEST／JZ RUN=7（guard_ref 有）。修：G.7 补全 first-match：const guard 0 跳过／非 0 取臂／非常量不折。证＠`8e3e65f74`：typeck_x 重编＋SKIP_HOT（pabi 1399206 未动）；`gfalse`／`gboolf` RUN=7／`gtrue`／`gboolt` RUN=1／`cmatch` RUN=20；gtn／match2／echo1／rv42／callid／mkx／arr0／asgimm 不回归。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: SHARED typeck CTFE／WINDOWS leftover-PE 探针。下一刀：nested `mk().p.f`。 |
| 2026-09-04 | **fix(parser)** **match subject IDENT 后缀 CALL／INDEX／FIELD** | **代码** · **不升钉** · Windows leftover-PE L2。五问：产生＝`parser_asm_parse_match_subject_into_c` IDENT 快路径一律 VAR，不看后续 `(`／`[`／`.` → `match echo1(1)` 把 `(1){…}` 当臂解析失败、`main` 不入模块（`-c` 仅 callee T）。存储＝parser_asm slice（pthin_ctrl／monothin 同 include；glue 薄转调）。消费＝want-exe 无 `_main`。修：G.7 有则补全 peek LPAREN／LBRACKET／DOT 走 `parse_expr_into`（原 lex）；裸 IDENT 仍 VAR（`{` 是臂、禁 STRUCT_LIT）。禁 assemble parser.x。证＠`f61fcffdc`：thin_glue 重编＋SKIP_HOT（pabi 1399206 未动）；`echo1` RUN=2／`echo0` RUN=7／`nestid` RUN=2／`addm` RUN=2／`wildcall` RUN=7／`idx0` RUN=1／`fldx` RUN=1；echo_let／match2／addsub／gtn／rv42／callid／mkx／arr0／asgimm 不回归。`gfalse` `_ if 0` 仍 RUN=1（另刀）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: SHARED parser_asm／WINDOWS leftover-PE 探针。下一刀：const-int guard `gfalse`。 |
| 2026-09-04 | **fix(windows)** **leftover rest emit_match guarded match** | **代码** · **不升钉** · Windows 探针。五问：产生＝SAT emit_match 旧两遍（wildcard 当终臂、不读 `guard_ref`）；leftover rest remaining wave134 sequential first-match 是 `#ifndef FROM_X` ABSENT。存储＝leftover rest WIN leftover emit_match＋`glue_enc_jz`（SAT jz 是 local t，UNDEF 不解析）。消费＝want-exe `_ if n > 0` RUN=0；`2 if v == 1` RUN=20。修：G.7 有则补全 leftover rest remaining emit_match／jz 为 WIN leftover first-wins；guard 走 leftover rest rec。证＠`ff10e2958`：redirect patched **390**；`gtn` RUN=1／`eqg` RUN=1／`litfalse` RUN=7／`litg` RUN=20／`match2` RUN=2；mkx／arr0／asgimm／callid／rv42 不回归。`match echo1(1)` 仍无 `main`（parser IDENT 快路径，另刀）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：parser match subject CALL。 |
| 2026-09-04 | **fix(windows)** **leftover rest field_access_fast CALL.field `mk().x`** | **代码** · **不升钉** · Windows 探针。五问：产生＝leftover rest WIN leftover `pipeline_asm_emit_field_access_elf_fast_c` 缺 CALL／METHOD 基物化；落 lvalue_eff_addr → rec ko==44 仅 enum → CG002 labels=4 patches=0。SAT `glue_field_access_call_base_rvalue` 是 SAT local t（leftover rest UNDEF 不解析；上刀 UNDEF 已证 final gcc 留 U）。存储＝leftover rest WIN leftover field_access_fast。消费＝want-exe `mk().x`。修：G.7 有则补全 leftover rest WIN leftover CALL／METHOD 基走 leftover rest rec（SAT emit_call 全局 T；SysV ≤8B packed rax）再 LP64 next_offset@4 临时槽 store／lea／load（compute_frame_size scratch ≥512）。证＠`8ab777335`：redirect patched **390**；`mkx` RUN=3／`mky` RUN=4／`mksum` RUN=7；stfield／st0／rv42／letret／add／nest／asgimm／wh3／for3／match2／arr0／callid／calllet 不回归。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：nested match／guarded match。 |
| 2026-09-04 | **fix(windows)** **leftover rest field_access_fast 裸 STRUCT_LIT.field** | **代码** · **不升钉** · Windows 探针。五问：产生＝leftover rest WIN leftover `pipeline_asm_emit_field_access_elf_fast_c` 缺 STRUCT_LIT 基物化；落 lvalue_eff_addr → rec ko==44 仅 enum → CG002 labels=2 patches=0。SAT `glue_field_access_call_base_rvalue`／`emit_struct_let_init` 是 SAT local t（leftover rest UNDEF 不解析）。存储＝leftover rest WIN leftover field_access_fast＋既有 redirect 表。消费＝want-exe `(P { x: 3, y: 4 }).x`。修：G.7 有则补全 leftover rest WIN leftover STRUCT_LIT 基走 SAT `emit_struct_lit` 全局 T（leftover rest rec ko==45 已用；dest 留 rbx）再字段偏移 load＋redirect WAVE278 FIELD_ACCESS 面。证＠`8dc497941`：redirect patched **390**；`stfield` RUN=3／`stfieldy` RUN=4／`stfieldsum` RUN=7；st0／stx／sty／stasg／rv42／letret／add／nest／asgimm／wh3／for3／match2／arr0／callid／calllet 不回归。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：nested match／guarded match。 |
| 2026-09-04 | **fix(windows)** **leftover-PE redirect STRUCT_LIT AST 面** | **代码** · **不升钉** · Windows 探针。五问：产生＝SAT emit_struct_lit intra SAT `pipeline_expr_struct_lit_num_fields`（SAT Expr 池 n_fields==0 → lea-empty）；leftover rest remaining wave154 `num_fields` 是 `#ifndef FROM_X` SAT 布局。存储＝WAVE278 sidecar `W278_Expr`＋既有 `win_pe_pabi_redirect` 表。消费＝want-exe `let p: P = P { x: 3, y: 4 }; return p.x+p.y` RUN=1。修：G.7 有则补全 WAVE278 WIN leftover FROM_X `num_fields`／`type_name`（读 W278_Expr，禁 leftover rest remaining SAT 布局 dual-def）＋redirect 加 STRUCT_LIT 面，scan SAT emit_struct_lit。证＠`b5579be4c`：redirect patched **290**；`st0`／`stx`／`sty`／`stanon`／`stasg` RUN 绿（`mov $3`＠+0／`mov $4`＠+4）；rv42／letret／add／nest／callid／calllet／asgimm／wh3／for3／match2／arr0 不回归。裸 `(P{…}).x` 仍 CG002（另邻域）。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：裸 STRUCT_LIT.field。 |
| 2026-09-04 | **fix(windows)** **leftover-PE redirect ARRAY_LIT／INDEX AST 面** | **代码** · **不升钉** · Windows 探针。五问：产生＝SAT emit_array_lit intra SAT `pipeline_expr_array_lit_*`（SAT Expr 池 n_arr==0 → lea-empty）；SAT emit_block_inits intra SAT `pipeline_block_let_type_ref`（SAT Block 池 → glue_block_let_is_fixed_array 假 → 把 ARRAY_LIT 指针写入 [N]T 槽，INDEX lea-slot 读指针）。存储＝既有 `win_pe_pabi_redirect` 表。消费＝want-exe `let a: i32[2] = [3, 4]; return a[0]+a[1]`。修：G.7 有则补全 redirect 加 WAVE278 array_lit／index 面＋WAVE277 let_type_ref，scan SAT emit_array_lit／emit_block_inits。证＠`dd701592e`：redirect 108＋31；`arr0`／`arridx0`／`arridx1`／`arrlit0`／`arrlit1`／`arrsum`／`arr1`／`arr3`／`arrasg` RUN 绿；rv42／letret／add／nest／ifelse／asgimm／wh3／for3／callid／calllet 不回归。禁 dual-def WAVE277／`glue_fixed_array`／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：STRUCT_LIT。 |
| 2026-09-04 | **fix(windows)** **leftover-PE redirect match AST 面** | **代码** · **不升钉** · Windows 探针。五问：产生＝SAT `pipeline_asm_emit_match_elf_c` intra SAT `pipeline_expr_match_*`（SAT Expr／arm 池空，WAVE278 sidecar 有）；for 邻域已由上波 redirect `ast_ast_block_for_*` 带绿（`for3`／`forsum`／`forlet`／`fornest`／break／continue／`loop` RUN 绿，无新债）。存储＝既有 `win_pe_pabi_redirect` 表。消费＝want-exe `match x { 1 => 2; … }` CG002 labels=5 patches=4。修：G.7 有则补全 redirect 加 match 面，scan SAT emit_match。证＠`99015b219`：redirect patched **64**；`match2`／`matchstmt` RUN=2；for／while／赋值／邻域 6／6 不回归。禁 dual-def WAVE277／278／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：数组／INDEX。 |
| 2026-09-04 | **fix(windows)** **leftover-PE redirect 赋值／while AST 面** | **代码** · **不升钉** · Windows 探针。五问：产生＝SAT emit_block intra SAT `num_expr_stmts`／`expr_stmt_ref`／`num_loops`／while cond／body（SAT Block 池空，WAVE277 sidecar 有）；leftover rest final_expr 把任意 stmt_order kind==2 当 return 跳过 → `x=4` 不发射、`return x` 也不 load（asgimm RUN=3）；while SAT cond_ref=0 → CG002（labels=2 patches=0）。存储＝既有 `win_pe_pabi_redirect` 表＋leftover rest final_expr。消费＝want-exe `let x=3; x=4; return x`／`while`。修：G.7 有则补全 redirect 加 expr_stmt／loop／while 面，scan SAT while；final_expr 只在 expr_stmt ko==41 时跳过。禁 dual-def WAVE277／禁改 mega。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：for／compound 邻域。 |
| 2026-09-04 | **fix(windows)** **leftover rest fill_local_slots 两 let 分槽** | **代码** · **不升钉** · Windows 探针。五问：产生＝mega_body `nso>0` 跳过 `fill_local_slots`，SAT emit_block intra-call SAT fill_tree／ensure（SAT 名／SAT locals），两 let 都 `mov %rax,0x0(%rbp)`；`add(x,y)` RUN=8。存储＝leftover rest WIN leftover OR fill_local_slots（WAVE277 `pipeline_block_let_name_*`）。消费＝want-exe `let x=3; let y=4; return add(x,y)`。修：G.7 有则补全 leftover rest fill＋mega 始终 fill；禁 dual-def WAVE277／禁改 mega。嵌套 `add(id(x),4)` 已由 leftover rest rec CALL 带绿（真双 `call` RUN=7）。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：赋值／while 邻域。 |
| 2026-09-04 | **fix(windows)** **leftover rest for_call_args local-let 实参** | **代码** · **不升钉** · Windows 探针。五问：产生＝SAT egg `glue_emit_one_call_arg` UNDEF `pipeline_asm_emit_expr_elf_for_call_args`；remaining wave216 stub `-1`（leftover rest FROM_X 省略）→ SAT standalone stub T 藏 `.x` 权威；INT_LIT 仍走 rec，VAR `add(x,4)` CG002。存储＝leftover rest WIN leftover OR for_call_args。消费＝want-exe 同文件 `let x=3; return add(x,4)`。修：G.7 有则补全 leftover rest VAR 走 leftover rest resolve＋load_var，else rec；禁 dual-def WAVE277／禁改 mega。证：`add(x,4)` RUN=7；`id(42)`／`add(3,4)`／邻域 6／6 不回归。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：同文件嵌套调用。 |
| 2026-09-04 | **fix(windows)** **leftover rest fill_param_slots／param_home 形参槽** | **代码** · **不升钉** · Windows 探针。五问：产生＝SAT egg `fill_param_slots` 相对调用 egg `num_params_at`（SAT Module 布局）；leftover rest WAVE280 sidecar `np=2`／名 `a` 但 nloc=0 → callee VAR CG002。存储＝leftover rest WIN leftover OR fill＋x86 GP param_home。消费＝want-exe 同文件 `id(x)`／`add(a,b)`。修：G.7 有则补全 leftover rest fill／param_home 走 WAVE280 访问器；禁 dual-def WAVE277／禁改 mega。证：`id(42)` 真 `call` RUN=42；`add(3,4)` 折叠 RUN=7；邻域 6／6 不回归。`add(x,4)` 仍 CG002（emit_call_args）。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：调用实参从 local let。 |
| 2026-09-04 | **fix(windows)** **leftover-PE redirect wrapper→rest mega_body void-main** | **代码** · **不升钉** · Windows 探针。五问：产生＝SAT wrapper 相对调用 thin mega_body（无 TYPE_VOID 门）；leftover rest WAVE290 ALWAYS 有 `rkind==16 && i==main_index` 则 `mov imm 0`。存储＝`win_pe_pabi_redirect` 既有 leftover-PE 消费路径。消费＝want-exe void main exit。修：G.7 消费 rest mega_body（与 get_return／nso 同级 redirect）；禁 dual-def／禁改 mega。证＠`6acc0f630`：void `mov $0,%eax` RUN=0；rv42／add／nest／ifelse／letret **不回归**（邻域 6／6）。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：同文件函数调用。 |
| 2026-09-04 | **fix(windows)** **leftover-PE stack_off 补全 VAR let 查找** | **代码** · **不升钉** · Windows 探针。五问：产生＝harvest unique `glue_var_expr_stack_off_elf_c` 恒 `-1`，rest-first 盖 SAT 真 t；leftover rest emit_expr_fast VAR／binop 找不到 `let x`。存储＝runtime_pipeline_abi.from_x.c WIN leftover rest。消费＝final_expr `return x`／`x+2`。修：G.7 有则补全，体与 `.x` 同（kind==VAR → `glue_asm_local_var_stack_off_scoped`）；named-let 复用 `glue_call_arg_resolve_anon_body_let_elf_c`。证＠`77781cef6`：letimm／letvar／letret **RUN 绿**；rv42／add／nest／ifelse **不回归**；void RUN=1。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：void exit=1。 |
| 2026-09-04 | **fix(windows)** **leftover-PE final_expr RETURN 走 return_impl jmp tail** | **代码** · **不升钉** · Windows 探针。五问：产生＝then-body nso=0，final_expr 把 EXPR_RETURN unwrap 成 emit_expr（`mov $2`）无 jmp tail；emit_if has_return 假 → `jmp done` rel32=0；外层 `return 9` 盖 eax。WAVE277 rebuild 已 T（nso=1 nif=1 kind=5），**禁** leftover rest 第二份 dual-def。存储＝leftover rest `glue_emit_block_final_expr_elf`。消费＝emit_block then-body。修：G.7 有则补全，ko==41 调 leftover rest `return_impl`。证＠`43daa2027`：ifelse `jmp 32` 跳过 `mov $9`；rv42／add／nest／ifelse **OK**；void RUN=1；letret CG002。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：letret CG002／void exit。 |
| 2026-09-04 | **docs(selfhost)** **日切：3 日归档／4 号主文档重写** | **文档** · **不升钉**。[自举进度-归档-2026-09-03.md](自举进度-归档-2026-09-03.md) 收 3 日 **90** 行（leftover unique 113→0／P2 Darwin `-o`／windows-gate 全绿）；主文档只留 KPI＋前排＋4 日 §6。活面＠`afdcdef52`：rv42／add／nest **OK**；ifelse RUN=9；void RUN=1；letret CG002。WAVE277 ALWAYS 已 T `rebuild_sparse_ifs`（禁 leftover rest 第二份 dual-def）。禁改测试期望。PLATFORM: SHARED 文档／WINDOWS leftover-PE 探针。下一刀：ifelse stmt_order 消费路径。 |
| 2026-09-04 | **fix(windows)** **leftover-PE rest proto `pipeline_block_stmt_order_kind`** | **代码** · **不升钉** · Windows 探针。五问：产生＝MinGW `glue_emit_block_final_expr_elf` 隐式 int vs 后声明 `uint8_t pipeline_block_stmt_order_kind`。存储＝runtime_pipeline_abi.from_x.c。消费＝emit_block final_expr。修：G.7 在用前补 `extern uint8_t`。证：hybrid 重编过＠`afdcdef52`。禁改测试期望。PLATFORM: WINDOWS leftover-PE。下一刀：ifelse stmt_order。 |
| 2026-09-04 | **docs(windows)** **纯 asm 邻域探针：算术绿／if·let·void 地图** | **探针＋文档** · **不升钉** · Windows。五问：产生＝thin mega `body_ref_at` 返 0 → 整段 emit_block 跳过；rest get_return（已 redirect）只扫 expr_stmts 末 return。存储＝hybrid pabi 双 T。消费＝want-exe。证：rv42／add=7／nest=14 **OK**；ifelse RUN=9；let／void CG002。试 redirect body_ref→rest：emit_block 入场但 `return imm` 退（nso=1 无 get_return）且 ifelse `jmp rel32=0`；已撤回 body_ref redirect。禁改测试期望。PLATFORM: WINDOWS leftover-PE。下一刀：body_ref_at＋stmt_order／jmp reloc。 |
| 2026-09-04 | **fix(windows)** **pabi merge 后 thin→rest redirect 闭合 return 42 空 .text** | **脚本＋代码** · **不升钉** · Windows 探针。五问：产生＝leftover rest＋`pipeline_glue_standalone` ld -r 双 T；thin mega 相对调用 thin `get_return`／nso（PE first-wins 不改 TU 内 twin）；thin 对 EXPR_RETURN 返 0 → 仅 prologue／epilogue。存储＝`src/runtime_pipeline_abi.o`。消费＝mega_body→emit_expr。修：WIN leftover 真体 get_return／nso（seed＋`.x`）；`ensure` merge 后 `win_pe_pabi_redirect_return_helpers` 把 thin 5 处 call 指 rest。证：redirect patched 5；`mov $0x2a`；WANT=0；**RUN=42**（无 exe 手补丁）。禁改测试期望。PLATFORM: WINDOWS leftover-PE／POSIX `-E` 不变。下一刀：min-gate／更多 pure-asm。 |
| 2026-09-04 | **fix(windows)** **COFF 去错下划线＋MinGW gcc 链 want-exe** | **代码** · **不升钉** · Windows 探针。五问：产生＝COFF writer 误加 Mach-O 前导 `_`→`_main` vs CRT `main`；lld-link 无 UCRT。存储＝user_asm_seed_bridge／runtime_link_abi。消费＝`-c` nm／want-exe。修：仅 Mach-O 加 `_`；COFF `/entry:main`；MinGW 用 `gcc` 作 link driver。证：nm `main`；want-exe WANT=0。PLATFORM: WINDOWS PE。下一刀：empty `.text`。 |
| 2026-09-04 | **fix(windows)** **PE 链接序 enc 先于 stubs 闭合 CG002 code_len=0** | **脚本** · **不升钉** · Windows 探针。五问：产生＝PE `XLANG_WEAK` 空＋`--allow-multiple-definition` FIRST-wins，`asm_full_link_stubs` 在 `backend_x86_64_enc_c` 前 → `arch_x86_64_enc_enc_label` 恒 `-1` → mega_body_c enc_label fail → CG002。存储＝g05_relink_env／build_xlang_asm companions。消费＝纯 asm `-c`／want-exe。修：G.7 WINDOWS 序：partial＋enc＋bridge＋dispatch 先，compat／full_link stubs 后（ELF／Darwin 仍 stubs-first＋weak）。证：dispatch 调真体（大栈帧）；`-c` EXIT=0 · 144B COFF；want-exe coff_write=144 后 lld-link `special section 0`。禁改测试期望。PLATFORM: WINDOWS PE／POSIX 弱符号不变。下一刀：want-exe ld。 |
| 2026-09-04 | **fix(windows)** **启 asm mkstemp_fdopen（MinGW want-exe 越过 BLD001）** | **代码** · **不升钉** · Windows 探针。五问：产生＝`xlang_driver_asm_mkstemp_fdopen_enabled` 永久返 0，cold twin WIN 空返回，want-exe 在 mkstemp 前 BLD001。存储＝runtime_driver_abi seed／thin／header。消费＝rt_run_asm_backend want_exe。修：G.7 enabled→1；cold twin 共用 MinGW mkstemp＋fdopen。证：Windows g05 重链后 enabled=`mov $1`、fdopen 调 `mkstemp`／`fdopen`；纯 asm 转 CG002 code_len=0；ALLOW_HOST_CC 回退仍绿。禁改测试期望。PLATFORM: SHARED／WINDOWS MinGW。下一刀：COFF emit code_len=0。 |
| 2026-09-04 | **fix(simd)** **AVX f32 select VEX 编码器补齐（vxorps／vcmpgtps／vandps／vandnps／vorps）** | **代码＋文档** · **不升钉**。五问：产生＝simd_enc.x／seed／surface 中 f32 ymm 5 处编码器保留了旧 xmm L=0、错 vvvv、错 opcode（vcmpgtps 曾误写为 4 字节 xorps）。存储＝simd_enc.x／simd_enc.from_x.c／simd_enc_surface.from_x.c。消费＝simd_enc_emit_f32_select_ymm_seq。修：G.7 补全 5 处标准 VEX.256 编码器（vxorps C5 E4 57 DB、vcmpgtps C5 EC C2 D3 0E 5 字节、vandps C5 FC 54 C2、vandnps C5 EC 55 D1、vorps C5 FC 56 C2）。证：L2 矩阵 5/5 OK；std-simd-shuffle-select gate OK；C迁移追踪 L4 select SIGILL 闭环。禁改 roundtrip。PLATFORM: SHARED emit／x86 AVX。下一刀：推进自举下一阶段。 |
