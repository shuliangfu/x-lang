# ShuX 自举进度

> **更新**：2026-07-19 · 18 号全日已归档（见 §1）；本日从 19 号起重新起表  
> **基线 SHA（产品 L4 放行钉盘）**：**`305cfbe1`**（双端真冷 L4+**125** @ 钉盘时；前钉 `4214eea4`；本波 ABI 边界 → 升钉）  
> **产品 bstrict 套件数**：**125**（`tests/run-all-bstrict.sh`；日志须见 `run-all-bstrict OK (125 scripts…)`）  
> **本波（19 日）状态**：**C6 X ABI P0b 波 2（ABI 一致性修复 3 处）· tip `305cfbe1`**（ABI 边界 → L4 双端真冷 + 升钉 `4214eea4` → `305cfbe1`；Windows 门禁待补跑）。改动 `compiler/src/runtime_io_abi.x` L164/180/196 共 3 处定义 `export function fs_posix_*_c` → `export extern "C" function fs_posix_*_c`（保留 `#[no_mangle]`），与 5 个声明点（main.x:30-31 / pipeline.x:81-82 / driver/emit.x:71-73）对齐；每处加 4 行英文 ABI 注释说明契约依据 + 委托 `std_fs_fs_*` 设计理由；函数体不变（底层 libc `close`/`read`/`write` 已在 `std_fs_fs_close` L99-105 / `std_fs_fs_read` L124-135 / `std_fs_fs_write` L145-156 内部用 `unsafe { }` 包裹）。修 ABI 一致性违反：声明 5 处全部 `extern "C"` ↔ 定义 3 处缺失 → P0a 语义降级下不断链，P1 激活 typeck 强制 `extern "C"` 调用约定后参数寄存器错位 / 运行期 UB。mac L4 log `/tmp/mac_true_bstrict_305cfbe1.log` · Ubuntu L4 log `/tmp/ubuntu_true_bstrict_305cfbe1.log` 各 `run-all-bstrict OK (125 scripts)` exit=0；Ubuntu link-hardening gate OK（Linux 专有）。**前波归档**：波 1 unsafe 包裹 @ `4214eea4`（main.x 6 + mod.x 3 unsafe，L4 双端 + 升钉 + Windows 门禁待补跑）· C6 P0b audit @ `7e507fb6`（docs-only 5466 extern 实测 + 9 处违规 + 3 处不一致 + 3 波拆分策略）· shux-x 5GB 方案 A @ `a14a1dd0`（per-ArenaSidecar init 4.3MB → 270KB 减 94%）· 日终 L4 兜底 @ `94a8e7d5`（双端真冷 + 125 bstrict 全绿；mac log `/tmp/mac_true_bstrict_94a8e7d5.log` / Ubuntu log `/tmp/ubuntu_true_bstrict_94a8e7d5.log`）· 5 波 C5 CTFE（`229708f1`/`a8a67708`/`d7a2388e`/`c174e70a`/`681b168f`）

> **方法**：[自举方法.md](自举方法.md)（Cap/R/L/M；工程 ≠ 产品放行）  
> **Windows（删种子前）**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md) — **删除种子 / G 清场前须跑通 Windows B-hybrid 最小门禁**（探针，≠ 125 金标；见本文 §5）  
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `shux-selfhost-product-gate`  
> **归档**：[自举进度-归档-2026-07-19.md](自举进度-归档-2026-07-19.md) · [自举进度-归档-2026-07-18.md](自举进度-归档-2026-07-18.md) · [自举进度-归档-2026-07-17.md](自举进度-归档-2026-07-17.md) · [自举进度-归档-2026-07-16.md](自举进度-归档-2026-07-16.md) · [自举进度-归档-2026-07-14.md](自举进度-归档-2026-07-14.md) · [自举进度-归档-2026-07-13.md](自举进度-归档-2026-07-13.md)

### 维护约定

1. 每完成一波 **必改两文**：
   - **本文**（长期仪表盘）：§2 KPI、§3 前排（≤3 条）、§6 一行；
   - **[`当前进度.md`](当前进度.md)**（当天快照）：本波 / SHA / 下一波前排。
2. **禁止**把 prove/Stage2/N++ 单独写成「可自举」。
3. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict` **125**（现行白名单；历史钉盘日志可能写 123）。
4. 工程 KPI 必须写 **SHA + 日志路径**；**过期整日流水移归档**（18 号全日已入归档），不在正文假装仍绿。
5. **`问题分析文档.md`：默认不改。** 仅新产品红 / 整波新债开场 / 根因换层（见 `自举步骤.md` §5.1）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 `305cfbe1`（升自 `4214eea4`；双端真冷 L4 + 125 bstrict 全绿；本波 ABI 边界 → 升钉）。**  
**本波（2026-07-19）= C6 X ABI P0b 波 2（ABI 一致性修复 3 处）@ tip `305cfbe1`**（ABI 边界 → L4 双端真冷 + 升钉 `4214eea4` → `305cfbe1`；Windows 门禁待补跑）：

- ① **C6 X ABI P0b 波 2 已完成**（[C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) §5.3 推荐执行方案）：修 ABI 一致性违反 3 处 — `compiler/src/runtime_io_abi.x` L164 (`fs_posix_close_c`) / L180 (`fs_posix_read_c`) / L196 (`fs_posix_write_c`) 共 3 处定义 `export function` → `export extern "C" function`（保留 `#[no_mangle]`），与 5 个声明点（main.x:30-31 / pipeline.x:81-82 / driver/emit.x:71-73）对齐；每处加 4 行英文 ABI 注释（契约依据 + 委托 `std_fs_fs_*` 设计理由）；**函数体不变**（调用 `std_fs_fs_close/read/write` 是 SHUX X-ABI 桥接，底层 libc `close/read/write` 已在 `std_fs_fs_close` L99-105 / `std_fs_fs_read` L124-135 / `std_fs_fs_write` L145-156 内部 `unsafe { }` 包裹）。**契约依据**：声明 5 处全部 `extern "C"` ↔ 定义 3 处缺失 → P0a 语义降级下不断链；P1 激活 `typeck.x:289 pipeline_typeck_check_extern_call_unsafe_boundary_c` 强制 `extern "C"` 调用约定后参数寄存器错位 / 运行期 UB。**双端 L4 真冷**：mac arm64 `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0（log `/tmp/mac_true_bstrict_305cfbe1.log`；全擦 .o + 删二进制 + bootstrap-driver-seed OK + g05_prepare_and_relink OK 58 objs Darwin/arm64）+ Ubuntu x86_64 `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0（log `/tmp/ubuntu_true_bstrict_305cfbe1.log`；git pull + 全擦 .o + 删二进制 + bootstrap-driver-seed OK + g05_prepare_and_relink OK 60 objs Linux/x86_64；**link-hardening gate OK（Linux 专有，macOS 跳过 non-Linux host）**）。**升钉决策**：ABI 一致性改动属 ABI 边界 → 升钉 `4214eea4` → `305cfbe1`。**Windows 门禁**：待补跑（ABI 边界 per §5.1 强制重跑 hybrid 最小门禁，与波 1 合并补跑；非阻塞升钉）。**波次**：波 2 of 3（audit `7e507fb6` → 波 1 `4214eea4` ✅ → **波 2 `305cfbe1` ✅** → 波 3 std/ ~557 远期顺延自举后 P1-P2）。**自举前 ABI 风险已解除**：波 1 + 波 2 完成后，P1 激活 typeck 强制 unsafe + X ABI 调用约定时不再有 bootstrap 陷阱。
- ② **本波额外工程债 · C7 plan 瘦身 PoC 完成**（工程债 · 非产品 · P3 · 路径 C 重构合并 · 2/8 完成 @ `e546b013` + `bedb40ca` · **非升钉**）：[C7-plan-audit.md](C7-plan-audit.md) audit 881 总脚本 vs 125 白名单 vs 23 W3 fast vs 752 非白名单（599 gate + 94 other + 24 perf + ...）；125 白名单内**无冗余**；23 W3 fast ⊆ 125（包含关系，非冗余）。8 个完全无引用 dead code 候选中 2/8 完成：① 合并 run-core-assert.sh → run-debug.sh（core.assert case，`bedb40ca`，L2 验证 run-debug.sh exit=0）② 删除 run-io-driver-process.sh（完全冗余：hello.x 被 run-hello.sh 覆盖 + spawn_wait.x 被 run-process.sh L84 覆盖，`e546b013`）。剩余 6 个阻塞：2 bug（std.debug.println 返回字节数 vs main.x 期望 a==0 + fmt 命令一元负号 return -i → return - i）+ 4 类型不匹配（STD-061 perf + STD-047/091/092 gate 不适合合并功能测试脚本）。**副产品发现 2 bug 转后续修复波次**。**升钉决策**：❌ 不升钉（测试脚本工程债，非 ABI/link 改动；钉盘保持 `305cfbe1`）。详见 §3 #4 + §6 `e546b013` 行
- ③ **前波归档**：波 1 unsafe 包裹 9 处 @ `4214eea4`（main.x L670-695 6 处 expression+statement form + asm_libroot/std/fs/mod.x L22-39 3 处 statement form，L4 双端 + 升钉 `5cc73d54` → `4214eea4` + Windows 门禁待补跑）· C6 P0b audit @ `7e507fb6`（docs-only 5466 extern 实测 + 9 处违规 + 3 处不一致 + 3 波拆分策略）· shux-x 5GB 内存根因方案 A @ `a14a1dd0`（perf(ast_pool) 承自 `94a8e7d5`；init cap 调优非 ABI / 非 link 改动 → 不升钉；mac+Ubuntu 双端 bstrict 125 OK exit=0 + lang-const 17/17 ✓）详见 §6 `a14a1dd0` 行 · 日终 L4 真冷全测双端全绿 @ `94a8e7d5`（mac arm64 + Ubuntu x86_64 各 `run-all-bstrict OK (125 scripts)` exit=0；mac L4 阻塞修复 = `src/asm/backend_enc_dispatch.o` stale `rm + make` 重建）详见 §6 L4 兜底行 · 5 波 C5 CTFE（`229708f1` EXPR_MATCH / `a8a67708` EXPR_STRUCT_LIT / `d7a2388e` EXPR_ENUM_VARIANT / `c174e70a` EXPR_TERNARY+IF+BLOCK 三连击 + `681b168f` EXPR_CLOSURE scoping docs）。

**已入钉盘（`305cfbe1`）**：C2 std.net PRIMARY UNDEF gate · C3=C4 parser bare-`{` struct-lit · backtrace L4 cold · C8 vec/set/map/queue duplicate · C6 P0b 波 1 unsafe 包裹 9 处 · **C6 P0b 波 2 ABI 一致性 3 处**（详见 §6 变更记录）。

**C1 复核结论**：T10 候选 C1（build error exit code 传播）在 `a74e25a1` 已修（`main_argv_has_o_flag` + `main_cmd_run` 显式 -o compile-only；build/run/bare 三路径 build error 都返回非 0），无需再做。

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](自举方法.md) | Cap / R / L / M |
| [自举步骤.md](自举步骤.md) | 可执行闸门 |
| **本文** | 长期 KPI + 前排（**每波必改**） |
| [当前进度.md](当前进度.md) | 当天快照（**每波必改**） |
| **[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)** | **删种子前先跑 Windows hybrid**；最小门禁 vs 125 金标；三阶段与 C6 触发（**决议**） |
| [自举进度-归档-2026-07-18.md](自举进度-归档-2026-07-18.md) | **18 号全日**（只读） |
| [自举进度-归档-2026-07-17.md](自举进度-归档-2026-07-17.md) | 17 号全日（只读） |
| [自举进度-归档-2026-07-16.md](自举进度-归档-2026-07-16.md) | 16 号全日（只读） |
| [自举进度-归档-2026-07-14.md](自举进度-归档-2026-07-14.md) | 14 号整表（只读） |
| [自举进度-归档-2026-07-13.md](自举进度-归档-2026-07-13.md) | G-02f thin 流水账（只读） |
| [问题分析文档.md](问题分析文档.md) | 产品/大债地图（**非每波**） |
| skill `shux-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **125** | `tests/run-all-bstrict.sh` |
| 产品 L4 放行钉盘 | **`305cfbe1`** | 19 日升钉（前钉 `4214eea4`）；双端 L4 真冷 + 125 全绿；本波 ABI 一致性 → 升钉 |
| 本波 tip | **`305cfbe1`**（fix(abi) 波 2 ABI 一致性 承自 `4214eea4`） | mac+Ubuntu 双端 L4 真冷全绿（ABI 边界 → 升钉 `4214eea4` → `305cfbe1`） |
| 本波 mac L4（波 2） | ✅ **`run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0** @ `305cfbe1` | 全擦 .o + 删二进制 + bootstrap-driver-seed OK (shux 3656096 bytes, seed smoke exit=42) + g05_prepare_and_relink OK (58 objs Darwin/arm64) + bstrict 125；log `/tmp/mac_true_bstrict_305cfbe1.log` |
| 本波 Ubuntu L4（金标 · 波 2） | ✅ **`run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0** @ `305cfbe1` | git pull + 全擦 .o + 删二进制 + bootstrap-driver-seed OK (shux 3490000 bytes) + g05_prepare_and_relink OK (60 objs Linux/x86_64) + bstrict 125；**link-hardening gate OK（Linux 专有）**；log `/tmp/ubuntu_true_bstrict_305cfbe1.log` |
| 日终 L4 mac arm64 | ✅ **`run-all-bstrict OK (125 scripts)` exit=0** @ `94a8e7d5` | 全擦 .o + 删二进制 + pin seeds + bootstrap + g05 + 矩阵 + bstrict 125；log `/tmp/mac_true_bstrict_94a8e7d5.log`；阻塞修复：`src/asm/backend_enc_dispatch.o` stale → rm + make 重建 |
| 日终 L4 Ubuntu x86_64（金标） | ✅ **`run-all-bstrict OK (125 scripts)` exit=0** @ `94a8e7d5` | 全擦 .o + 删二进制 + git pull + pin seeds + bootstrap + g05 + 矩阵 + bstrict 125；log `/tmp/ubuntu_true_bstrict_94a8e7d5.log` |
| 工程轨 KPI（T/N/R2/M） | 继承 18 号结案 | 本波未动工程轨 |

### 2.2 工程轨（继承 18 号结案）

| KPI | **现** | 备注 |
|-----|--------|------|
| **T** | **18/18** | ✅ 满 |
| **EMPTY** | **18/18** | ✅ 满 |
| **N** | **54/54 IDENTICAL** @ 18 号 tip | 回归基线 |
| **R2 / H** | Cap pure 收口；WPO ✅；typeck/codegen M ✅ | 18 号结案状态 |

***

## 3. 当前前排（≤4 条）

1. ✅ **shux-x 5GB 内存根因方案 A 已完成**（双端 L2 全绿 @ `a14a1dd0`）：拆分 `AST_POOL_INIT_CAP=256`（init 默认）与 `AST_POOL_GROW=4096`（grow step 保留），per-ArenaSidecar init 4.3MB → 270KB（减 94%）；50 deps init RSS 215MB → 14MB。mac+Ubuntu 双端 bstrict 125 OK exit=0 + lang-const 17/17 ✓。不升钉（init cap 调优非 ABI / 非 link 改动）。详见 [当前进度.md](当前进度.md) §本波摘要
2. ✅ **日终 L4 真冷全测（mac + Ubuntu 双端）已完成**（双端全绿 @ `94a8e7d5`）：本日 4 波 C5 fold + 1 docs 兜底。双端 L4 全过 `run-all-bstrict OK (125 scripts)` exit=0；不升钉（CTFE 扩展非 ABI / 非 link 改动）
3. **C6 X ABI P0b（波 2 ✅ → 推进 Windows 门禁补跑）**：[C6-P0b-scope-audit.md](C6-P0b-scope-audit.md) 实测 5466 extern 声明；核心 14 个 C ABI 调用全部已标注 ✅；3 波拆分中 **波 1 ✅ @ `4214eea4`** + **波 2 ✅ @ `305cfbe1`**（ABI 一致性 3 处 runtime_io_abi.x L164/180/196 定义改 `export extern "C" function`，ABI 边界，L4 双端 + 升钉 `4214eea4` → `305cfbe1` + Windows 门禁待补跑）→ **波 3** std/ ~557 标注（远期顺延自举后 P1-P2）。**自举前 ABI 风险已解除**：波 1 + 波 2 完成后，P1 激活 typeck 强制 unsafe + X ABI 调用约定时不再有 bootstrap 陷阱 → 仅剩 Windows 门禁补跑（波 1+波 2 合并）+ 波 3 远期
4. ✅ **C7 plan 瘦身 PoC 完成**（工程债 · 非产品 · P3 · 路径 C 重构合并 · 2/8 完成 @ `e546b013`）：[C7-plan-audit.md](C7-plan-audit.md) audit 881 总脚本 vs 125 白名单 vs 752 非白名单（599 gate + 94 other + 24 perf + ...）；125 白名单内**无冗余**；8 个完全无引用 dead code 候选中 2/8 完成：① 合并 run-core-assert.sh → run-debug.sh（core.assert case，`bedb40ca`，L2 验证 exit=0）② 删除 run-io-driver-process.sh（完全冗余：hello.x 被 run-hello.sh 覆盖 + spawn_wait.x 被 run-process.sh L84 覆盖，`e546b013`）；剩余 6 个阻塞（2 bug + 4 类型不匹配）转后续。**副产品发现 2 bug 转后续修复波次**：std.debug.println 返回字节数 vs main.x 期望 a==0 + fmt 命令一元负号 return -i → return - i。**非 ABI/link → 不升钉**（钉盘保持 `305cfbe1`）。详见 [当前进度.md](当前进度.md) §本波摘要

**已收口（19 日 tip）**：**C6 X ABI P0b 波 2 ABI 一致性 3 处（`305cfbe1` 升钉）** · C6 X ABI P0b 波 1 unsafe 包裹 9 处（`4214eea4` 升钉） · shux-x 5GB 方案 A（`a14a1dd0`）· C5 EXPR_MATCH CTFE（`229708f1`）· C5 EXPR_STRUCT_LIT 字段 CTFE 扩全（`a8a67708`）· C5 EXPR_ENUM_VARIANT CTFE（`d7a2388e`）· C5 EXPR_CLOSURE scoping（`681b168f` docs）· **C5 EXPR_TERNARY+IF+BLOCK CTFE 三连击（`c174e70a`）** —— 详细见 [当前进度.md](当前进度.md) §本波摘要；日终 L4 双端兜底已过

**不在前排**：
- **C5 EXPR_CLOSURE CTFE**（独立大工程 · 多波）：已 scoping（[C5-EXPR_CLOSURE-CTFE-scoping.md](C5-EXPR_CLOSURE-CTFE-scoping.md)）。SHUX 当前无 closure 支持任何层（无 parser 语法、无 AST 节点、无 TYPE_FN、无 typeck、无 codegen）。需 5 波独立工程：① fn 类型系统（1.5 波）→ ② EXPR_CLOSURE AST+parser（1.5 波）→ ③ typeck capture analysis（2 波）→ ④ codegen lowering（2 波）→ ⑤ CTFE fold（0.5 波，本 Cap）。总估时 ≈ 7.5 波。顺延自举后立项 `lang-closure-v1-rfc.md`，明确语法选择（Rust `|x|...` / Swift `{x in ...}` / Zig `fn(x) {...}`）+ capture 策略 + Linear/owned move 语义 + region 推断交互
- **Cap C9 match guard full-chain 支持**（独立大工程 · 多波）：经调查 guard 不在 cap-candidates C5 范围内。X pipeline `MatchArmEntry` 5 字段无 `guard_ref`；`pipeline_expr_append_match_arm` 签名无 guard 参数；`parser_parse_match_into_glue` extern 声明无定义；ELF emit / CTFE handler 均不处理 guard。要完整支持 guard 需要 6 个产生点改动（ast_pool 加字段 + append 加参数 + parser 实装 + typeck scope + emit 分支 + CTFE handler），**多波拆分**，先排期再动手
- tip L4 **`305cfbe1`**（= 钉盘；本波 `305cfbe1` 升钉——ABI 边界改动）；C2 std.net UNDEF gate + C3=C4 parser bare-`{` struct-lit + backtrace L4 cold + C8 vec/set/map/queue duplicate + C6 P0b 波 1 unsafe 包裹 9 处 + **C6 P0b 波 2 ABI 一致性 3 处** 均已入钉盘。

**不在前排**：18 号的 typeck CTFE / WPO / ASM PRIMARY / named_layout / slice / anon struct backfill / main_cmd_run -o 全部已入 18 号归档。

***

## 4. 与 18 号归档的关系

- 18 号全日自举仪表盘 + 变更流水已整表移入 [`自举进度-归档-2026-07-18.md`](自举进度-归档-2026-07-18.md)；**勿把归档内中间 tip / 单叶日志当 19 日现绿**。
- 产品 L4 放行 tip 仍以本文为准：18 日结案钉盘 `c51759eb` → 19 日凌晨 `a74e25a1`（同 18 号结案升钉序列的延续，归档快照截止 18 日 21:46 `b3540784` 钉盘 `c51759eb`；`a74e25a1` 是 19 日凌晨的升钉，本文承接）。
- 19 日新工作（C2 + 文档归档/起表）从本文起记录。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
|------|----------|
| **macOS arm64** | **L4 真冷**（波 2 @ `305cfbe1`）：全擦 .o（compiler/std/core/build_asm 共 0 remaining）+ 删二进制（shux/shux_asm/shux-c/bootstrap_shuxc）+ `bootstrap-driver-seed OK` (shux 3656096 bytes, seed smoke exit=42) + `g05_prepare_and_relink OK (shux_asm OK)` (58 objs Darwin/arm64) + `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0；log `/tmp/mac_true_bstrict_305cfbe1.log` |
| **Ubuntu x86_64（金标）** | **L4 真冷**（波 2 @ `305cfbe1`）：git pull `305cfbe1` Fast-forward + 全擦 .o + 删二进制 + `bootstrap-driver-seed OK` (shux 3490000 bytes) + `g05_prepare_and_relink OK (shux_asm OK)` (60 objs Linux/x86_64) + `run-all-bstrict OK (125 scripts, compiler/shux is shux_asm)` exit=0；**link-hardening gate OK（Linux 专有，Type=DYN stack=0x1）**；log `/tmp/ubuntu_true_bstrict_305cfbe1.log` |
| **Windows（探针 · 非自举金标）** | **本波 ABI 边界 → 必须跑** Windows hybrid 最小门禁（波 1+波 2 合并补跑，待执行）。**策略（强制）**见下 |

### 5.1 Windows 与删种子（策略锚点 · 非本波 KPI）

> **决议全文**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)

| 项 | 口径 |
|----|------|
| **删种子 / G 清场前** | **必须**先把 Windows **B-hybrid 最小门禁**跑通并保持可回归（阶段 1 DoD）；**禁止**「先彻底删种子，再第一次认真碰 Windows」 |
| **现在** | **可以**跑 hybrid 探针；不必等完全自举 / tip 升钉 |
| **最小门禁（权威）** | MSYS2 / `windows-server` 上：`./tests/run-bootstrap-bstrict-windows-gate.sh`（默认 hybrid） |
| **不是** | Windows 上 `./tests/run-all-bstrict.sh`（~125）当阶段 1 主验收；Windows L4 钉盘；宣称 Win 已零 C / 已自举 |
| **金标仍是** | Ubuntu（+ SHARED 时 mac）**125** `run-all-bstrict` + L4 |
| **强制重跑 Windows 门** | **C6 / ABI / link / `std.sys`** 大改前后；**宣称 G / 删种子**之前；阶段 2 合并点 |
| **进度话术** | Windows hybrid 绿 = **探针可用**；**≠** 产品 tip L4 / 可自举 |

***

## 6. 变更记录（倒序 · 20 日起；19 号见归档）

| 日期 | SHA | 摘要 |
|------|-----|------|
| 2026-07-20 | **`d25b82cc`**（Windows 修复，待 Windows 验证） | **Windows MSYS/MinGW shux-c exit=1 根因修复 · macOS + Ubuntu 双端 L4 真冷 + 125 bstrict 全绿 · Windows 待补跑**：根因 `compiler/Makefile` L791 `RUNTIME_DRIVER_CFLAGS`（LEGACY 模式 `runtime_driver.o` 编译参数）缺少 `-DSHUX_NO_C_FRONTEND`，而 `RUNTIME_DRIVER_NO_C_CFLAGS`（L792，no_c 默认模式）有该 define。导致 `seeds/runtime.from_x.c` L4320 `#if !defined(SHUX_NO_C_FRONTEND)` 守卫的 C typeck 预检块在 LEGACY 模式下被编译进来，调用 stub `parse()`（C 前端已物理删除，`runtime_driver_strict_glue_stubs.from_x.c` L71 SHUX_WEAK stub 返回 -1）→ `driver_c_typeck_entry_impl` L4690 走 `pr != 0` 分支 → return 1 → exit=1。macOS no_c 模式因 `SHUX_NO_C_FRONTEND` 已定义，L4320 块被剔除，X pipeline 自带 typeck → exit=0。**双平台 nm 反汇编验证**：macOS `nm shux` 无 `_driver_c_typeck_entry_large_stack` 符号（L4320 块被剔除）；Windows `nm shux.exe` 有 `driver_c_typeck_entry_large_stack` (T)（L4320 块被编译）。macOS `_parse` 反汇编 `mov w0, #-0x1; ret`（weak stub 不被调用）；Windows `parse` `mov $0xffffffff, %eax; ret`（stub 被 L4320 调用 → exit=1）。**前会话两个相关 commit**：`43c8a484`（Windows MSYS winpthreads `pthread_join` 清理崩溃 → 返回 1 绕过 pthread 路径）+ `447d0fa9`（Windows PE `--stack` 链接标志加 `-Wl,--stack,268435456` 256MB 栈 reserve）。**双端 L4 真冷全绿**：mac arm64 删 80 个 .o + 删二进制 + `bootstrap-driver-bstrict` 重建 58 objs + `run-all-bstrict OK (125 scripts)` exit=0；Ubuntu x86_64 git pull + `bootstrap-driver-seed` smoke exit=42 + `run-all-bstrict OK (125 scripts)` exit=0。**符号一致性验证**：`runtime_driver.o`（LEGACY）与 `runtime_driver_no_c.o`（no_c）`nm` diff 为空。**Windows 待补跑**：`shux-c.exe` 已构建但 Permission denied（exit=126），等用户进入 Windows 系统重试。权威 compiler/Makefile:791（RUNTIME_DRIVER_CFLAGS 加 -DSHUX_NO_C_FRONTEND）· seeds/runtime.from_x.c:4320-4347（C typeck 预检块 #if 守卫）· seeds/runtime_driver_strict_glue_stubs.from_x.c:71（parse SHUX_WEAK stub） |
| 2026-07-20 | **工程改进** | **`tests/run-all-bstrict.sh` 加 L4 真冷测一体化 opt-in 模式（SHUX_L4_COLD=1）**：原 L4 真冷测需手动执行三步（删 .o + 删二进制 + `make bootstrap-driver-bstrict` + `SHUX_BSTRICT_SKIP_BUILD=1 ./tests/run-all-bstrict.sh`），现合并为单命令 `SHUX_L4_COLD=1 ./tests/run-all-bstrict.sh`。脚本顶部新增 L4 purge 块：① `find compiler -name '*.o' -delete` + `find std -name '*.o' -delete` + `find core -name '*.o' -delete`（删全部 .o，禁止选择性删除 per AGENTS.md）② `rm -f compiler/shux compiler/shux-c compiler/shux_asm compiler/shux_asm2 compiler/bootstrap_shuxc` ③ `unset SHUX_BSTRICT_SKIP_BUILD` 强制走 `make -C compiler bootstrap-driver-bstrict` 重建分支 ④ shux_asm 存在检查在 L4 模式下跳过（避免 purge 后误报 exit 127）。**PLATFORM: SHARED** — macOS/Linux/Windows (MSYS) 都可用。**macOS 验证**：SHUX_L4_COLD=1 → purge 0 残留 .o → 重建 128+37 .o → bstrict 前 ~35 scripts 全绿（含 lang-unsafe S0_ub_bounds OK），run-ub.sh 因 macOS Darwin OOM Killed:9 失败 3 次重试均失败（19 号归档已知环境债：Darwin 连续 shux_asm -o 链接大量 .o 时内存峰值过高被杀；非回归；Ubuntu x86_64 是金标准不受影响）。**待补**：Ubuntu L4 冷测（SHUX_L4_COLD=1）+ Windows（进系统后）。权威 tests/run-all-bstrict.sh:13-36（L4 purge 块）+ :38-43（L4 模式跳过 shux_asm 检查） |
| 2026-07-20 | **docs（归档+起表）** | 19 号全日归档入 `自举进度-归档-2026-07-19.md`；本文从 20 日起重新起表 |

***

## 7. 禁止清单（本阶段）

- [ ] 未用 18 号归档 KPI 当「当前仍绿」而不查 tip SHA
- [ ] 未把 C5 EXPR_ENUM_VARIANT / EXPR_STRUCT_LIT / EXPR_MATCH 日常 L2 当 tip L4 放行（typeck CTFE 扩展非放行级；SHARED 须双端 L4 才升钉）
- [ ] 未用 N=54 全表 prove 单独宣称可自举（须 L4 + 双端 bstrict）
- [ ] 未把 Stage2 ✅ / R2 抽检当完全自举
- [ ] 未改 nm 期望糊过真 residual
- [ ] 未把 mac 全表预期当金标准（Ubuntu 记账为准）
- [ ] 产品 SHARED 仍须双端 bstrict（本波 harness-only 可记，动编译器须重开）
- [ ] **未把日常 L2 写成 tip L4 放行**
- [ ] **未在删种子 / G 宣称前跳过 Windows hybrid 最小门禁**（见 [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md) · 本文 §5.1）
- [ ] **未把 Windows hybrid 绿写成「已自举 / 已零 C」**；未在 Win 上用全量 125 `run-all-bstrict` 当阶段 1 放行
