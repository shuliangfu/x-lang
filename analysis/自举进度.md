# Shux 自举进度

> **更新**：2026-07-16 · 工程轨门禁修好后 **N re-baseline**  
> **基线 SHA**：`32bf8ad8`（prove 强符号 IDENTICAL；产品仍 `233e3b72` 双端 bstrict 123）  
> **方法**：[自举方法.md](../自举方法.md)（Cap/R/L/M；工程 ≠ 产品放行）  
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `shux-selfhost-product-gate`  
> **归档**：[自举进度-归档-2026-07-14.md](自举进度-归档-2026-07-14.md) · [自举进度-归档-2026-07-13.md](自举进度-归档-2026-07-13.md)

### 维护约定

1. 每完成一波 **只改**：§2 KPI、§3 前排（≤3 条）、§6 一行。  
2. **禁止**把 prove/Stage2/N++ 单独写成「可自举」。  
3. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict` 123。  
4. 工程 KPI 必须写 **SHA + 日志路径**；过期数字移归档。

---

## 0. 一句话当前状态

**产品轨已放行；工程门禁 N=30/54（全表未重跑）。**  
**Cap-T001 源 bare 五模块已清**；**Track L enc residual 已收**（surface 对齐 → `backend_enc_dispatch` IDENTICAL）。仍余 diagnostic / rt_fmt_one SYM。**禁**关 LANG-007；**未** L4 全量 bstrict（日常真测）。
---

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](../自举方法.md) | Cap / R / L / M |
| [自举步骤.md](../自举步骤.md) | 可执行闸门 |
| **本文** | KPI + 前排 |
| [自举进度-归档-2026-07-14.md](自举进度-归档-2026-07-14.md) | 14 号整表（只读） |
| [问题分析文档.md](../问题分析文档.md) | 产品债地图 |
| skill `shux-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

---

## 2. 仪表盘（@ `32bf8ad8`）

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|-------------|
| Ubuntu L4 真冷 + bstrict | ✅ **123/123** | `/tmp/ubuntu_true_bstrict_233e3b72.log` · 产品修复 SHA `233e3b72` |
| macOS bstrict | ✅ **123/123** | `/tmp/mac_true_bstrict_233e3b72.log` |
| 本波工程修 | prove/g05 prologue + 强符号 IDENTICAL | `1c73ce48` · `32bf8ad8`（**未**重跑 L4 bstrict；SHARED 仅 harness） |

### 2.2 工程轨

| KPI | 14 号（归档） | **re-baseline 初** `233e3b72` | **门禁修好后** `32bf8ad8` | 健康方向 |
|-----|---------------|-------------------------------|---------------------------|----------|
| **T** | 18/18 | 10/18 | **10/18**（未变） | → 满或写清 mega 不可满 |
| **N** | 54 | **0/54**（harness 假红） | **30/54 IDENTICAL** | ↑ 再吃 24 |
| **R2 / H** | R2≥49 | 冻结 | **仍冻结**（N 未满前不报 R2++） | N 稳后再抽检 |
| **S** | mega pin | pin | pin | ↓ 非本周先决 |

**日志**：

- prove：`/tmp/ubuntu_prove_32bf8ad8.log` · `PROVE N=30/54`  
- T：`/tmp/ubuntu_diff_t_32bf8ad8.log` · typeck 10/18  

**N=30 恢复路径（门禁，非重迁实现）**：

| # | 根因 | 修法 | commit |
|---|------|------|--------|
| A | prove/g05 prologue 缺 `sys/uio.h`/`poll.h`（`-E` 内联 readv/writev/poll） | 与 g05 同源补头 | `1c73ce48` |
| B | IDENTICAL 把 preamble **weak** 桩算进 x 面（Darwin plain nm 全是 T） | 只比强定义（ELF `TDBRSC`；Mach-O `nm -m` 非 weak external） | `32bf8ad8` |

**N 剩余 24 分类（`32bf8ad8`）**：

| 类 | 数 | 模块（摘） | 性质 |
|----|----|------------|------|
| **T001** extern 需 unsafe | **0**（源已清；全表 N 未 rebase） | 原 5 模块 commit1～3 已包 `unsafe` | Cap-T001 **源侧闭环**；非自动 N++ |
| **cc -c 非法 C** | 2 | `backend_try_inline_dispatch` · `backend_call_dispatch` | codegen 产物 |
| **seed cc 失败** | 1 | `lsp_io_std_heap` | seed 路径/源 |
| **符号前缀 / 面漂移** | 16 + residual | 原 16 + **diagnostic / rt_fmt_one**（**enc 已 IDENTICAL**） | Track L；**非**产品 bstrict 红 |

**T=10/18 失败**（`diff_single_link`）：  
`lexer` · `parser` · `typeck` · `codegen` · `lsp` · `lsp_diag` · `lsp_io` · `compile`  
（mega 地图；与产品 bstrict 用户路径分离，见方法 Track M）

### 2.3 阶段总览

| 阶段 | 含义 | 状态 |
|------|------|------|
| 产品冷链 / bstrict | 用户路径 | ✅ `233e3b72` 双端 123 |
| 工程 prove 门禁 | N 可测 | ✅ harness 真绿路径；**N=30** |
| D Stage2 | 黄金自举 | ⬜ 未在本 SHA 重跑 |
| E 真迁 / Cap | R2 + residual | 🟡 实现多在树；KPI 以 N/T 地图推进 |
| F/G | std 无 C / 终局 | ⬜ 非前排 |

---

## 3. 当前前排（≤3 条）

1. **Track L residual 续**：`diagnostic`（surface 多 `driver_env_flag_truthy` body）· `rt_fmt_one`（path helper 缺 `#[no_mangle]`）；地图 §0.26。  
2. **全表 prove re-baseline N**（可选）：诚实计入 compile / arch_emit / **enc** 等新 IDENTICAL；禁未全表写 N++。  
3. **T / Track M 地图**（说明向）：8 mega 红归层；**禁止**未 L4 + 未稳 N 谈 Stage2。

**不在前排**：整表重做 14 号 R2；关 LANG-007 / allow_legacy；改 nm 期望顶 residual；本微步不跑 L4 全量 bstrict。
---

## 4. 与 14 号实现的关系

| 问题 | 结论 |
|------|------|
| 实现还在吗？ | **大多在**；N=30 证明 surface 多数仍 IDENTICAL |
| 要不要整表重做？ | **不要**；吃剩余 24 + Cap |
| 14 号 N=54 能否当天追回？ | 未必然；先 T001 Cap + 前缀对齐，再评估 residual |

---

## 5. 验收命令

```bash
# 产品（新 SHA 动 SHARED 时 L4）
# skill §3 L4 + SHUX_BSTRICT_SKIP_BUILD=1 run-all-bstrict

# 工程
export SHUX=./compiler/shux_asm
bash tests/prove_module_selfhost.sh 2>&1 | tee /tmp/ubuntu_prove_$(git rev-parse --short HEAD).log
timeout 300 bash tests/diff_single_link.sh 2>&1 | tee /tmp/ubuntu_diff_t_$(git rev-parse --short HEAD).log
```

---

## 6. 变更记录（倒序）

| 日期时间 | 摘要 |
|------|------|
| 2026-07-16 | **Track L enc residual** `73bcff1e`：surface 对齐 riscv 短名（§0.26）；**mac+Ubuntu** prove `backend_enc_dispatch` **IDENTICAL**；日志 `/tmp/ubuntu_prove_track_l_enc_73bcff1e.log`；diagnostic/rt_fmt 仍 residual；**日常真测** |
| 2026-07-16 | **Cap-T001 commit3** `938fcb76`：两 backend dispatch bare 整层 `unsafe`；**mac+Ubuntu** arch_emit **IDENTICAL**、enc **E/cc OK + SYM residual**；U4 仍红；日志 `/tmp/ubuntu_prove_cap_t001_commit3_938fcb76.log`；**日常真测**（非 L4） |
| 2026-07-16 | **验证节奏** `f32a45b5`：日常真测 vs 真冷全测（整波主闸门 + 日终兜底）；`自举方法.md` §0.2 · skill §3.3 |
| 2026-07-16 | **Cap-T001 commit2** `a96bc382` `compile.x`：8 函数 FFI `unsafe`；**Ubuntu/mac prove compile IDENTICAL**；U4 仍红；日志 `/tmp/ubuntu_prove_cap_t001_commit2_compile_a96bc382.log`（全表 N 未重跑） |
| 2026-07-16 | **Cap-T001 地图+探针** `511302e9`：§0.25 源裸 extern；`diagnostic`/`rt_fmt_one` unsafe → Ubuntu `-E`/cc 绿、**SYM residual**（非 N++）；日志 `/tmp/ubuntu_prove_cap_t001_probe_511302e9.log` |
| 2026-07-16 | **工程门禁**：prologue uio/poll（`1c73ce48`）+ IDENTICAL 强符号（`32bf8ad8`）→ Ubuntu **N=30/54**；T 仍 10/18；日志 `/tmp/ubuntu_prove_32bf8ad8.log` |
| 2026-07-16 | 归档 14 号进度；产品双端 bstrict 123 @ `233e3b72`；初测 N=0 harness 假红 |
| 2026-07-14 及更早 | 见 [自举进度-归档-2026-07-14.md](自举进度-归档-2026-07-14.md) |

---

## 7. 禁止清单（本阶段）

- [ ] 未用 14 号 T/N/R2 当「当前仍绿」  
- [ ] 未在 N=30 时宣称 E 轨完成 / 可自举  
- [ ] 未把 Stage2 历史 ✅ 当本 SHA 已验  
- [ ] 未改 nm 期望糊过真 T001  
- [ ] 产品 SHARED 仍须双端 bstrict（本波 harness-only 可记，动编译器须重开）
