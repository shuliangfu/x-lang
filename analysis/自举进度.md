# Xlang / X language 自举进度

> **更新**：2026-09-19 · **tip `45f8c48ec`**（w613/613b/614/614b）wave614：Apple ARM64 自然栈参打包（模型单一权威＋宽度感知存储编码器）。**双端 L4 全绿**（Darwin 131/131 含 run-path · 22m3s；Ubuntu 131/131 · 12m46s）。钉盘 **`ecdb5cc1e`**（tip 已达升钉标准，默认不升）
> **状态**：**尚未完全自举**。主刀 **M2**。
> **下波（w615）**：**wpo_dump LINUX 产品 PREFER 重验**——w611 的「Ubuntu dump SEGV」根因即 w613 已修的 COMMON 误分类（只读 `__TEXT` 写崩），且 w613 修的 ELF writer 路径已随双端 L4 验证；LINUX 侧 stamp 门控放开→探针（dump JSON＋L2）→绿则该 TU 双端零 host-cc。次排：vtable wrapper 子字节 >8 参转发（需 per-param 类型管道）；模块级 let home 两笔（小已初始化 `n_sect=2` 无 `__DATA` 段；空 `[]` 数组 shndx 永不 65522）。**禁止**再用 `-E` 当修法。mega FORCE 禁。

闸门：双端 L2；暂停 `xlang check`；L4 仅整波／谈自举。

### 维护约定

1. 每波必改本文（KPI／前排≤3／§6 一行）。终局债改 [`C迁移追踪.md`](C迁移追踪.md)。
2. §6 倒序 · 一行一波。过期整日流水日切归档后主文档重写。

***

## 0. 一句话

**尚未完全自举** · 钉盘 **`ecdb5cc1e`** · tip **`792ea7f49`** · **w613/613b** COMMON shndx 权威＋wpo_dump MACOS PREFER＋冷孪生体 · **w614/614b** ARM64 自然栈参打包。

| 指标 | 状态 |
|------|------|
| 钉盘 L4 | ✅ 双端＠**`ecdb5cc1e`** |
| tip L4 | ✅ **双端全绿**＠`45f8c48ec`（Darwin 131/131 含 run-path · Ubuntu 131/131；矩阵齐） |
| 本波级别 | **L4 双端全绿**（w614/w614b）· tip 达升钉标准（默认不升） |
| 下一波 | **w615** wpo_dump LINUX 产品 PREFER 重验 |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举效率方法-M2主链.md](自举效率方法-M2主链.md) | M2 主链 |
| [C迁移追踪.md](C迁移追踪.md) | 终局债（短状态表） |
| **本文** | KPI · 前排 · **§6（9 月 19 日）** |
| [自举进度-归档-2026-09-18.md](自举进度-归档-2026-09-18.md) | **9 月 18 日** |

***

## 2. 仪表盘

| 项 | 结果 |
|----|------|
| 产品 bstrict | **129** 套 |
| 钉盘 | **`ecdb5cc1e`** |
| MG／Stage8 | MG ✅ · Stage8 **30/30** |
| SSH | `ubuntu-server`／`windows-server` |

***

## 3. 当前前排（≤3）

1. **主迁刀（w615）** — wpo_dump **LINUX 产品 PREFER 重验**：w611 Ubuntu dump SEGV 根因＝w613 已修的 COMMON 误分类；ELF writer 同修已随双端 L4 过。放 LINUX stamp 门控 → dump 探针＋L2 → 绿则 wpo_dump 双端产品 PREFER 齐。
2. **次排** — vtable wrapper（`pipeline_asm_emit_vtable_wrapper_def`）子字节 >8 参转发均匀槽（>8 子参 trait dyn 链本就错，需 per-param 类型管道）；模块级 let home 两笔（小已初始化 let `n_sect=2` 无 `__DATA` 段链接拒；空 `[]` 数组注册不走 65522 落 sect=1）。
3. **立卡不动** — P4b 侧支；MSVC va／WIN leftover；eq 深链；spawn 真体；nest 冻帽 64。

## 4. 本日要点（9 月 19 日）

| 波 | 结论 |
|----|------|
| w614/614b | **冷链 Darwin arm64 >8 参栈传参根修**：0867354e3 把 callee homing 改 Apple 自然对齐（修 clang→`into_c`）但 caller 四点位仍均匀 8 槽 → xlang↔xlang 9+ 参错读（run-path `bytes_eq` exit 2；L4 断链自 9/14 掩盖至 w613b 解锁）。clang 实证模型＝自然尺寸步长＋自然对齐（char +0/+1、char,int +0/+4、char,long +0/+8）。修：模型单一权威 `glue_arm64_stack_arg_align/advance_pos`（mega `#[no_mangle]` 导出＋pabi seed 常编冷孪生保 phase1）；caller 四点位（静态/动态/方法/UFCS）字节游标复用同两函数；w614b 补**宽度感知存储编码器**（strb/strh/str w 自然缩放——纯 8 字节 `str x0` 的 off/8 整除曾把 +1 落回槽 0）。非 apple 路径字节级不变（Ubuntu x86_64 回归绿）。**双端 L4 全绿**＠`45f8c48ec`（Darwin 131/131 含 run-path＝自 9/13 以来首次完整；22m3s。Ubuntu 12m46s）。11 参复现 42 ✓ |
| w613b | 冷孪生体：`parser_asm_struct_layout_slice.inc` 的 `pack_copy_fname`（P6e）无宏门直调 P19d `.x` 体 `from_kind_c` → 冷链（L4 phase1／擦后恢复）UNDEF 双端同符号（最后绿 L4 9/13＜P19d 9/14；旧 `.o` 藏）。补 `#ifndef XLANG_PTHIN_HELPERS_BODIES_FROM_X` 冷体（IDENT 拷贝＋soa/packed/type 拼写，同 `.x` 语义）。双端 phase1 过 |
| w613 | **根修**：COMMON 分类改读 **ctx 符号表 shndx==65522（单一权威）**，sidecar 只补 align／size（offset 字段兜底）。根因＝sidecar 是每编译副本全局态：seed rest 的 modlet 扫描 **TU 内直调**自己的 `add_common_sym`（superseded 改名拦不住 intra-TU 调用）写 from_x 静态，writer 查活体 overlay 副本 → 永远 miss → COMMON 落 `N_SECT/sect=1 __TEXT` 只读 → dump 写 Lxml 即崩（Darwin SIGBUS＠`w502_wpo_bfs+152` / Ubuntu SEGV 139 同根）。7 位点（thin＋mega macho/ELF/ELF-PGO＋from_x×3）。**wpo_dump 产品 PREFER MACOS 站住**（stamp `.pabi_w613`，单一体，LINUX 仍 `-E`）：thin 独立 `N_UNDF＋COMM_ALIGN＋value=尺寸`，ld -r 合并保 common（同其余 119 个），**dump 探针 JSON v2 233B 绿**（多函数文件 3 函数可达性正确）。Darwin L2 5/5。注意：重注入须干净 base（坏定义以 weak-defined 赢过新 tentative） |
| L4 | **Ubuntu 全绿**＠`792ea7f49`（12m45s：矩阵 5/5＋**131 bstrict**；日志 `/tmp/xlang_l4_Linux_792ea7f49.log`）。Darwin：冷链自 9/13 来首次过 phase1＋bootstrap＋g05＋矩阵；bstrict **130/131**（`run-path` 红＝w614 新债：冷链 arm64 >8 参栈传参错编；`/tmp/w613_l4_darwin2.log`）。Ubuntu 补探针：wpo thin 独立 `-c` Lxml 全 `SHN_COMMON COM`（ELF 侧修复生效） |
| w612 | **M2** Darwin COMMON lea PAGE21：leftover modlet lea 调 `append_reloc_typed_pabi_superseded`（gcc 静态 sidecar），活体 writer 读 elf_ctx PREFER `reloc_r_type_at` → ADRP 默认 BRANCH26。Darwin `-E` macho_write（走 accessor）＋`ld -r -alias` leftover→活体 typed。小文件 `let g: i32[4]` `-c` PAGE21／PAGOF12，链跑＝0。wpo_dump 独立 Lxml 0×BR26（仍禁产品 PREFER：Ubuntu dump SEGV）。L2 5/5＠20773672/6602528 |
| w611 | **M2** wpo_dump：独立 PREFER 双端 U-complete（Darwin T=24 UND=39／Ubuntu T=30 UND=39）。产品 PREFER 不绿：Darwin g05 `ARM64_RELOC_BRANCH26` 打在 COMMON Lxml `adrp`（权威 PAGE21＝`glue_asm_lea_rax_common_adrp_arm64`）；Ubuntu dump 路径 SEGV 139（smash leftover 仍能写 JSON v2）。stay `-E`／overlay。禁 PREFER wpo_dump helpers。L2 5/5＠20731640/6602528 |
| w610 | **M2** param_ptr_slot：独立 PREFER 双端 U-complete（T=3 UND=19）。w494 产品 PREFER opt=77＝当时编译器 smash；现 assign/deref 已 `-E`，产品 PREFER_ASM 换 gcc T。Ubuntu 活体无 endbr64 `sub $0x1168`；Darwin `subsp #0xfff+#0x171`。opt=102。L2 5/5＠20731640/6602528 |
| w609 | **M2** add_defs：独立 PREFER 已齐（`di++` 写回＋回边）；产品 leftover 政策仍 `-E`。双端 PREFER_ASM 换 gcc T。Ubuntu 活体 `sub $0x878` 无 endbr64；Darwin `sub #0x880`。`-D FOO=1` 编跑＝42。L2 5/5＠20731240/6594336 |
| w608 | M1 CALL-arg T[N]：leftover PREFER `glue_call_arg_var_use_lea_not_load_elf_c` 砸帧（`sub $0xac8`，无 endbr64）回 0，产品 `mov` 数组内容当指针（`0x4030201`）SEGV。LINUX `-E` 换 helpers（帧 `0x1b8`＋endbr64）。Ubuntu `get(u8[4])`／`get(i32[4])` 编跑＝3／7（`lea`）。禁 PREFER call_arg_lea。不 `-E` full thin。L2 5/5＠20731112/6594336 |
| w607 | M1 u8 `b[2]=7`：leftover PREFER `glue_emit_assign_index_*` 砸帧（gate `sub $0xb98`／try `sub $0x1098`，无 endbr64）多 pop 盖 dest。LINUX `-E` 换 assign_index 族（gate／try／generic 帧 `0x30`＋endbr64）。Ubuntu 编跑＝7（一次 `movb`）。禁 PREFER assign_index。不 Soft-Cap setup/walk/peel/resolve。L2 5/5＠20731112/6594248 |
| w606 | M1 `as` 下标：leftover PREFER `glue_emit_as_cast_orch_elf_c` 砸帧（`sub $0x898`，无 endbr64）掉 INDEX load。LINUX `-E` 换 fnptr_as 族（gate `0x28`／orch `0x40`／lea `0x148`＋endbr64）。Ubuntu `return b[2] as i32` 编跑＝3（`movzbl`）。`b[2]=7` 仍 3。禁 PREFER fnptr_as。L2 5/5＠20731112/6581096 |
| w605 | M1 u8 ARRAY_LIT：leftover PREFER `pipeline_asm_array_lit_elem_byte_sz_c` 砸帧（`sub $0xb38`，无 endbr64）回 4，flatten 发 `movl`。LINUX `-E` 换 fnptr_array_esz（帧 `0x50`＋endbr64）。Ubuntu `return b[2]` 编跑＝3。`return b[2] as i32` 仍 4＝smash `as_cast_orch` 掉 operand。禁 PREFER fnptr_arr_esz。L2 5/5＠20731112/6572088 |
| w604 | M1 嵌套 while＋内层 `let`：leftover `asm_ctx_block_slot_get` 与 `-E` `set` 双 BSS，get 恒 -1，`backend_block_slot_base_for` 退 `num_locals-nlet`。LINUX `-E` 整份 asm_locals（get+set 同 TU）。Ubuntu 编跑＝2。禁 PREFER get peer。L2 5/5＠20731160/6567952 |
| w603 | add_defs 独立 PREFER 已齐（early-return jmp 填、回边齐）。`sub $0x878`＝2048 scratch 设计值。产品仍 gcc `-E`。嵌套 while＋内层 `let`：Darwin＝2／Ubuntu let-init 偏一槽（find_offset `i=40` 却写 48）。禁 PREFER add_defs。L2 5/5＠20731160/6559072 |
| w602 | M1 return-in-if：leftover PREFER `pipeline_asm_emit_return_elf_impl` 砸帧（`sub $0xe98`，无 endbr64）发 operand 但不 `ENC_JMP` tail_join，if-done 落入 `return 1`。LINUX `-E` 换 operand+jmp exit（帧 `0x150`＋endbr64）。Ubuntu `if { return 7 }` 编跑＝7。add_defs 仍 `-E`。L2 5/5＠20731160/6559072。禁 PREFER return_impl；禁 un-BAN arr_return Soft-Cap |
| w601 | M1 if-in-while：while 体 final_expr 的 if 发完后 `glue_emit_block_final_expr_elf` 在 `func_index_get=-1` 时 fallback `jmp .Lf0_0`，回边成死码。LINUX `-E` emit_ctx BSS 双 BSS option SEGV → BAN。gate 未知身份不再跳 tail_join。Ubuntu `if { di++ }` 编跑＝3。L2 5/5＠20731160/6559024。禁 PREFER／`-E` emit_ctx BSS；禁 PREFER final_expr |
| w600 | M1 while hang：leftover PREFER `glue_emit_assign_var_elf_c` 族砸帧（`sub $0x898`，无 endbr64，r9 cltq）。`di=1` 不写回；`while(di<n){di++}` 超时 124。LINUX `-E` 换 7 叶（帧 `0x30`＋endbr64）。Ubuntu `di++`／`while_inc` 编跑＝1／3。add_defs 仍 `-E`。if-in-while `if{di++}` 仍跑＝1。L2 5/5＠20731160/6554888。禁 PREFER assign_var |
| w599 | M1 `*p=` CG002：leftover PREFER `rhs_to_rax` 巨帧砸 scalar（`sub $0x1158`，无 endbr64）。rhs_elf 自己回 0；pipe_load 读到 -1。LINUX `-E` 换 dispatcher（帧 `0x30`＋endbr64）。Ubuntu `*p=`／`*u8=`／param 编跑＝1。while／load 仍绿。L2 5/5＠20731160/6554584。禁 PREFER rhs_to_rax |
| w598 | M1 `*p=`：gdb 0x9＝peel/gate epilogue `pop %rbx`（rbp=1）。DEREF `resolved_type=0` 进 leftover peel；peel 已调 scalar，scalar leftover PREFER 砸 caller。gate 改走 scalar；LINUX `-E` 换 scalar。Ubuntu `*p=` 不再 SEGV，现 CG002（code_len=75）。L2 5/5＠20731160/6554544。禁 PREFER peel／scalar |
| w597 | M1 parse_skip：Ubuntu grow_vec 饿死 INIT_CAP=256（`pipeline_asm_local_offset_c` 后 set/get／200 lets 掉）。LINUX `-E` 换回 realloc/mmap；asm_locals T=36 含 set/get；wpo_dump 含 append_i32＋callgraph。`*p=` 仍 139。不 Soft-Cap `-E` 余叶。禁 PREFER grow_vec |
| w596 | M1：掉尾＝parse_skip 假绿（asm_locals set/get、wpo_dump 自 append_i32；去注释仍 skip）。`unsafe { *p=1 }` Darwin 绿／Ubuntu SEGV 139＠peel（0x9）；pipe-cell PREFER 与 LINUX `-E` 仍红 → BAN peel 再注入。不 Soft-Cap `-E` 余叶 |
| w595 | LINUX `-E` asm_locals get：Ubuntu 整份 thin 掉尾 `get`（独立/peer EXPORT_OK 1/5；Darwin 12 面齐）；LINUX PREFER get **L2 SEGV 0/5** → BAN 回 `-E` main。param_ptr_slot 19/19 stay `-E`（opt=77）。add_defs 2/2 stay `-E`（while hang）。不 Soft-Cap |
| w594 | LINUX `-E` wpo_dump：Ubuntu 整份 thin 从 `append_i32` 掉尾（Darwin export 齐）；orch 双端 EXPORT_OK 21/21；去 `*i32` 后 PREFER orch **L2 SEGV 0/5** → BAN 回 `-E` helpers。不 Soft-Cap。下一＝param_ptr_slot／asm_locals／add_defs |
| w593 | 余 PREFER 双端实探 U-complete **不 BAN**（copy／resolve_call／spill／call_method／fnptr_arr_esz／field_load／fnptr_as／type_to_c_repr／binop／assign overlay／preprocess／import／ast_fwd／block_tree／typeck_orch／elf_fwd 两端 UND 齐）。MACOS slot_bytes／fixed_array_copy full Darwin U-complete，Ubuntu rc=1＝LINUX 已 BAN。下一＝LINUX `-E` 产品路径（不 Soft-Cap）／mega FORCE 禁 |
| w592 | arr_struct_lit main tipU 14/14＋PRODUCT BAN（Ubuntu 原 UND=1＝只剩 zero_field＝if-before-call／中途赋值 src/iko/n_arr/esz／`rc=call` then `if`；helper＝`arr_struct_lit_store_encoders`）；first-loop 也 BAN；copy／resolve_call 不 BAN；禁 un-BAN arrlit＋main。下一＝余 PREFER inject |
| w591 | arr_struct_lit arrlit tipU 10/10＋PRODUCT BAN（Ubuntu 原 UND=1＝只剩 vector_let_init＝中途赋值 lit_n then `if`／if-before-return sret_direct／while／`rc=call` then `if`；helper＝`arr_struct_lit_arrlit_store_encoders`）；first-loop 也 BAN；禁 un-BAN arrlit＋main。下一＝arr_struct_lit main |
| w590 | slot_bytes helpers tipU 23/23＋LINUX PRODUCT BAN（Ubuntu 原 UND=1＝只剩 kind_ord＝if-before-call／中途赋值／嵌套 while／`u8[256]`／`*i32` metrics out；helper＝`slot_bytes_store_encoders`）；MACOS 仍 PREFER full thin。下一＝余 PREFER inject |
| w589 | reent_deep_copy tipU 36/36＋PRODUCT BAN（Ubuntu 原空 `.o`＝if-before-call／中途赋值／`rc=call` then `if`／嵌套 while COMMON／`u8` label；helper＝`reent_deep_copy_store_encoders`）；下一＝余 PREFER inject |
| w588 | for_call_args tipU 68/68＋PRODUCT BAN（Ubuntu 原空 `.o`＝if-before-call／中途赋值／`rc=call` then `if`／`u8[256]`／`*i32` out；helper＝`for_call_args_store_encoders`）；copy／resolve_call 实探 2/2 不 BAN。下一＝余 PREFER inject |
| w587 | arr_return main tipU 20/20＋PRODUCT BAN（Ubuntu 原 UND=1＝只剩 jmp＝中途赋值 ly/ret_op／if-before-call／`rc=path` then `if`／`rc=spills/cps` then `if`／while tj_lbl；helper＝`arr_return_store_encoders`）；arr_return 族 PREFER 清空。下一＝copy／resolve_call（U-complete 不 BAN） |
| w586 | arr_return d tipU 5/5＋PRODUCT BAN（Ubuntu 原 UND=0＝unused ko/sret sink then `if`／`rc=emit` then `if`／if-before-assign／中途赋值 rty/sty／`rc=promote` then `if`；helper＝`arr_return_d_store_encoders`）；下一＝arr_return main |
| w585 | arr_return c_fallback tipU 5/5＋PRODUCT BAN（Ubuntu 原 UND=0＝`rc=emit` then `if`／if-before-assign／中途赋值 rty/sty／`rc=promote` then `if`；helper＝`arr_return_c_fallback_store_encoders`）；下一＝arr_return d |
| w584 | arr_return c_slice tipU 14/14＋PRODUCT BAN（Ubuntu 原 UND=1＝if-before-call／中途赋值／嵌套 `tk=kind_ord` then `if`／`rc=durable` then `if`／`rc=force_esz` then `if`／`rc=push/mov/pop` then `if`，只剩 void bump；helper＝`arr_return_c_slice_store_encoders`）；下一＝arr_return c_fallback |
| w583 | arr_return c_dest_array tipU 9/9＋PRODUCT BAN（Ubuntu 原 UND=1＝if-before-call／中途赋值／`tk=kind_ord` then `if`／`rc=durable` then `if`／`rc=force_esz` then `if`，只剩 void bump；helper＝`arr_return_c_dest_array_store_encoders`）；下一＝arr_return c_slice |
| w582 | arr_return b tipU 6/6＋PRODUCT BAN（Ubuntu 原 UND=0＝if-before-call／`esc=call` then `if`／`rc=emit` then `if`／中途赋值／`rc=promote` then `if`；helper＝`arr_return_b_store_encoders`）；下一＝arr_return c_dest_array |
| w581 | arr_return b0_durable tipU 15/15＋PRODUCT BAN（Ubuntu 原 UND=2＝if-before-call／中途赋值／嵌套 while／`rc=call` then `if`；helper＝`arr_return_b0_durable_store_encoders`）；下一＝arr_return b |
| w580 | arr_return b0_prep tipU 14/14＋PRODUCT BAN（Ubuntu 原 SEGV 139＝`*i32` out 存／if-before-call／`rc=call` then `if`；helper＝`arr_return_b0_prep_store_encoders`）；下一＝arr_return b0_durable |
| w579 | arr_return a2 tipU 4/4＋PRODUCT BAN（Ubuntu 原 UND=0＝if-before-call／`rc=emit` then `if`／`rc=mov` then `if`／`rc=memcpy` then `if`；helper＝`arr_return_a2_store_encoders`）；下一＝arr_return b0_prep |
| w578 | arr_return a tipU 2/2＋PRODUCT BAN（Ubuntu 原 UND=0＝if-before-call／`rc=sret` then `if`；helper＝`arr_return_a_store_sret`）；下一＝arr_return a2 |
| w577 | main tipU 7/7＋PRODUCT BAN（Ubuntu 原 UND=2＝if-before-call／`ko=kind_ord` then `if`／dest_elem then dest_ek／inner then inner_k，只剩 rows＋scalar；helper＝`arr_lit_flat_store_encoders`）；arr_lit_flat 族 PREFER 清空。下一＝arr_return a |
| w576 | scalar tipU 3/3＋PRODUCT BAN（Ubuntu 原 UND=0＝`n_arr=num_elems` then `if`／`store_sz=leaf_esz` then `if`／`while rc=step` then `if`）；main 起仍 PREFER（库存 U2，先诊断） |
| w575 | step tipU 6/6＋PRODUCT BAN（Ubuntu 原 UND=2＝`elem_ref=call` then `if`／`ko=kind_ord` then `if`／`if(ko==46) flatten`／`tr=try_struct` then `if`；flatten＋one_scalar 仍发射）；scalar 起仍 PREFER |
| w574 | one_scalar tipU 7/7＋PRODUCT BAN（Ubuntu 原 UND=0＝`rc=call` then `if`／`may_clobber=call`／`fi=flat_i[0]`／`flat_i[0]=fi+1`；`flat_i[0]` 存未发射故无编译 SEGV）；step 起仍 PREFER |
| w573 | struct tipU 2/2＋PRODUCT BAN（Ubuntu 原 UND=0＝if-before-call／中途赋值／`st=call` then `if`；`flat_i[0]` 存未发射故无编译 SEGV）；one_scalar 起仍 PREFER |
| w572 | rows tipU 6/6＋PRODUCT BAN（Ubuntu 原 UND=0＝while／中途赋值／`st=call` then `if`）；struct 起仍 PREFER |
| w571 | one_row tipU 4/4＋PRODUCT BAN（Ubuntu 原 UND=1＝if-before-call／`ko=call` then `if`／`row_st=call` then `if`）；rows 起仍 PREFER |
| w570 | cells tipU 4/4＋PRODUCT BAN（Ubuntu 原 UND=0＝while＋`st=call` then `if`）；one_row 起仍 PREFER |
| w569 | one_cell tipU 2/2＋PRODUCT BAN（Ubuntu 原 UND=0＝if＋中途赋值）；cells 起仍 PREFER |
| w568 | repark tipU 5/5＋PRODUCT BAN（Ubuntu 原 UND=0＝`rc=call` then `if`）；one_cell 起仍 PREFER |
| w567 | resolve_vf tipU 8/8＋PRODUCT BAN（Ubuntu 原 SEGV 139＝`*i32` 存）；copy／call 调度叶不 BAN |
| w566 | call_bulk tipU 11/11＋PRODUCT BAN；call 调度叶不 BAN |
| docs | C迁移追踪简写；流水账进归档 |
| w565 | copy_bulk tipU 10/10＋PRODUCT BAN；copy 调度叶不 BAN |
| w564 | copy_elems tipU 12/12＋BAN |
| w530–w563 | Cap residual tipU＋BAN 连收（见 [18 日归档](自举进度-归档-2026-09-18.md)） |

stamp-only 禁 prefer。L2 5/5＠20731640／6602528。

***

## 5. 与归档

- **18 日**：[自举进度-归档-2026-09-18.md](自举进度-归档-2026-09-18.md)
- **C 追踪流水账**：[archive/C迁移追踪-流水账归档-20260919.md](archive/C迁移追踪-流水账归档-20260919.md)
- **现行钉盘＝`ecdb5cc1e`**。

***

## 6. 变更记录（倒序 · **9 月 19 日**）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-19 | wave614b | 宽度感知出参存储编码器 `backend_enc_arm64_store_arg_sp_offset_c`（四镜像）；caller 四点位传参宽；T001 unsafe 修正。`45f8c48ec` |
| 2026-09-19 | L4＠w614 | **双端 L4 全绿**（Darwin 131/131 含 run-path·22m3s；Ubuntu 131/131·12m46s）＠`45f8c48ec`。tip 达升钉标准（不升） |
| 2026-09-19 | wave614 | ARM64 自然栈参打包：模型单一权威（align/advance 导出＋seed 冷孪生）；caller 四点位字节游标。`6a232e6a0` |
| 2026-09-19 | wave613b | 冷孪生体 `pack_copy_fname`（struct_layout_slice.inc）补 `#ifndef` 冷体；双端 L4 phase1 UNDEF 解锁。`792ea7f49` |
| 2026-09-19 | L4＠w613 | **Ubuntu L4 全绿**（矩阵＋131 bstrict·12m45s）；Darwin 130/131（run-path＝冷链 >8 参错编→w614）。Ubuntu wpo thin Lxml `SHN_COMMON COM` ✓ |
| 2026-09-19 | wave613 | M2 根修：COMMON 分类＝ctx shndx 65522 单一权威（7 位点）；双 sidecar 实例＋intra-TU 调用根因闭环；wpo_dump 产品 PREFER **MACOS 站住**（dump JSON v2 绿·L2 5/5·LINUX 仍 `-E`）。`a716a5413` |
| 2026-09-19 | wave612 | M2 Darwin COMMON lea PAGE21。macho_write `-E` accessor＋`ld -r -alias` leftover typed。小文件链跑＝0。wpo_dump 独立 Lxml 0×BR26。L2 5/5＠20773672/6602528｜pabi＠18649488/3481176 |
| 2026-09-19 | wave611 | M2 wpo_dump 独立 PREFER 齐；产品 PREFER＝Darwin BRANCH26＋Ubuntu dump SEGV。stay `-E`。L2 5/5＠20731640/6602528｜pabi＠18620400/3481176 |
| 2026-09-19 | wave610 | M2 param_ptr_slot 产品 PREFER_ASM（本 TU 不再 host-cc）。opt=102。L2 5/5＠20731640/6602528｜pabi＠18620400/3481176 |
| 2026-09-19 | wave609 | M2 add_defs 产品 PREFER_ASM（本 TU 不再 host-cc）。`-D FOO=1`＝42。L2 5/5＠20731240/6594336｜pabi＠18615632/3474864 |
| 2026-09-19 | wave608 | M1 CALL-arg T[N]＝smash leftover `lea_not_load` 把数组内容当指针。LINUX `-E` 换 helpers。Ubuntu `get(u8[4])` 编跑＝3。禁 PREFER call_arg_lea。L2 5/5＠20731112/6594336｜pabi＠18615296/3474512 |
| 2026-09-19 | wave607 | M1 u8 `b[2]=7`＝smash leftover `assign_index` 多 pop 盖 dest。LINUX `-E` 换族。Ubuntu 编跑＝7。禁 PREFER assign_index。L2 5/5＠20731112/6594248｜pabi＠18615296/3469528 |
| 2026-09-19 | wave606 | M1 `as` 下标＝smash leftover `as_cast_orch`。LINUX `-E` 换 fnptr_as 族。Ubuntu `return b[2] as i32` 编跑＝3。`b[2]=7` 仍 3。禁 PREFER fnptr_as。L2 5/5＠20731112/6581096｜pabi＠18615296/3448376 |
| 2026-09-19 | wave605 | M1 u8 ARRAY_LIT store_sz＝smash leftover `elem_byte_sz`。LINUX `-E` 换。Ubuntu `return b[2]` 编跑＝3。禁 PREFER fnptr_arr_esz。L2 5/5＠20731112/6572088｜pabi＠18615296/3435840 |
| 2026-09-19 | wave604 | M1 嵌套 while＋内层 `let`＝asm_locals get/set 双 BSS。LINUX `-E` 整份 thin 换齐。Ubuntu 编跑＝2。禁 PREFER get peer。L2 5/5＠20731160/6567952｜pabi＠18615296/3431528 |
| 2026-09-19 | wave603 | add_defs 独立 PREFER 已齐仍 `-E`。下一＝嵌套 while＋内层 `let`（Ubuntu let-init 偏一槽）。禁 PREFER add_defs。L2 5/5＠20731160/6559072｜pabi＠18615296/3410600 |
| 2026-09-19 | wave602 | M1 return-in-if＝leftover PREFER `return_impl` 砸帧不发 tail_join jmp。LINUX `-E` 换 exit。Ubuntu `if { return 7 }` 编跑＝7。add_defs 仍 `-E`。禁 PREFER return_impl。L2 5/5＠20731160/6559072｜pabi＠18615296/3410600 |
| 2026-09-19 | wave601 | M1 if-in-while＝`final_expr` 未知身份 fallback 跳 `.Lf0_0`（smash `func_index_get=-1`）。LINUX `-E` emit_ctx BSS 双 BSS option SEGV → BAN。gate 未知身份＝0。Ubuntu `if { di++ }` 编跑＝3。禁 PREFER／`-E` emit_ctx BSS。L2 5/5＠20731160/6559024｜pabi＠18615296/3409352 |
| 2026-09-19 | wave600 | M1 while hang＝leftover PREFER `assign_var` 族砸帧（`sub $0x898`）。LINUX `-E` 换 7 叶。Ubuntu `while(di<n){di++}` 编跑＝3。add_defs 仍 `-E`。禁 PREFER assign_var。L2 5/5＠20731160/6554888｜pabi＠18615296/3407632 |
| 2026-09-19 | wave599 | M1 `*p=` CG002＝leftover PREFER `rhs_to_rax` 砸帧（`sub $0x1158`）。LINUX `-E` 换 dispatcher。Ubuntu `*p=` 编跑＝1。禁 PREFER rhs_to_rax。L2 5/5＠20731160/6554584｜pabi＠18615296/3403336 |
| 2026-09-19 | wave598 | M1 `*p=` SEGV＝leftover PREFER scalar 砸 caller rbp（0x9 unwind）。gate 跳过 peel（DEREF type=0）；LINUX `-E` 换 scalar。Ubuntu `*p=` 现 CG002 code_len=75。禁 PREFER peel／scalar。L2 5/5＠20731160/6554544｜pabi＠18615296/3401624 |
| 2026-09-19 | wave597 | M1 parse_skip＝grow_vec INIT_CAP=256（LINUX 无增长）。LINUX `-E` 换回 realloc/mmap；asm_locals set/get 齐；wpo_dump 尾齐。`*p=` 仍 139＠peel。禁 PREFER grow_vec。L2 5/5＠20731160/6550400｜pabi＠18615296/3397816 |
| 2026-09-19 | wave596 | M1 墙：Ubuntu 掉尾＝parse_skip 假绿；`unsafe { *p=1 }` Darwin 绿／Ubuntu SEGV 139＠`glue_emit_assign_deref_peel_elf_c`（0x9）。pipe-cell PREFER 与 LINUX `-E` 仍红 → BAN peel 再注入。不 Soft-Cap `-E` 余叶。L2 5/5＠20731160/6545976｜pabi＠18615296/3391224 |
| 2026-09-19 | wave595 | LINUX `-E` asm_locals get：Ubuntu 整份 thin 掉尾 `get`（独立/peer EXPORT_OK 1/5）；LINUX PREFER get **L2 SEGV 0/5** → BAN 回 `-E` main。param_ptr_slot 19/19 stay `-E`（opt=77）。add_defs 2/2 stay `-E`（while hang）。不 Soft-Cap。L2 5/5＠20731160/6545976｜pabi＠18615296/3391224 |
| 2026-09-19 | wave594 | LINUX `-E` wpo_dump：Ubuntu 整份 thin 从第 18 函数 `append_i32` 掉尾（Darwin export 齐）；orch 双端 EXPORT_OK 21/21；去 `*i32` 后 PREFER orch **L2 SEGV 0/5** → BAN 回 `-E` helpers。不 Soft-Cap。L2 5/5＠20731160/6545976｜pabi＠18615296/3391224 |
| 2026-09-19 | wave593 | 余 PREFER 双端实探 U-complete **不 BAN**（copy／resolve_call 2/2 · spill 2/2 · call_method 5/5 · fnptr_arr_esz 15/15 · field_load／fnptr_as／type_to_c_repr／binop／assign overlay／preprocess／import／ast_fwd 185/186／block_tree 23/23／typeck_orch 6/6／elf_fwd 16/19 两端 UND 齐；MACOS slot_bytes／fixed_array_copy full Darwin U-complete，Ubuntu rc=1＝LINUX 已 BAN）。无产品改。下一＝LINUX `-E` 产品路径（不 Soft-Cap）／mega FORCE 禁。L2 仍 5/5＠20731160/6545976｜pabi＠18615296/3391224 |
| 2026-09-19 | wave592 | arr_struct_lit main tipU＋PRODUCT BAN（14/14；Ubuntu 原 tip UND=1＝只剩 zero_field＝if-before-call／中途赋值 src/iko/n_arr/esz／`rc=call` then `if`；helper＝`arr_struct_lit_store_encoders`；stamp-only 禁 prefer first-loop；留 w440 overlay；copy／resolve_call 不 BAN；禁 un-BAN arrlit＋main；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave591 | arr_struct_lit arrlit tipU＋PRODUCT BAN（10/10；Ubuntu 原 tip UND=1＝只剩 vector_let_init＝中途赋值 lit_n then `if`／if-before-return sret_direct／while／`rc=call` then `if`；helper＝`arr_struct_lit_arrlit_store_encoders`；stamp-only 禁 prefer first-loop；留 w440 overlay；禁 un-BAN arrlit＋main；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave590 | slot_bytes helpers tipU＋LINUX PRODUCT BAN（23/23；Ubuntu 原 tip UND=1＝只剩 kind_ord＝if-before-call／中途赋值／嵌套 while／`u8[256]`／`*i32` metrics out；helper＝`slot_bytes_store_encoders`；LINUX stamp-only 禁 prefer；留 w415 overlay；MACOS 仍 PREFER full thin；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave589 | reent_deep_copy tipU＋PRODUCT BAN（36/36；Ubuntu 原 tip 空 `.o`＝if-before-call／中途赋值／`rc=call` then `if`／嵌套 while COMMON／`u8` label；helper＝`reent_deep_copy_store_encoders`；stamp-only 禁 prefer；留 w410 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave588 | for_call_args tipU＋PRODUCT BAN（68/68；Ubuntu 原 tip 空 `.o`＝if-before-call／中途赋值／`rc=call` then `if`／`u8[256]`／`*i32` out；helper＝`for_call_args_store_encoders`；stamp-only 禁 prefer；留 w375 overlay；copy／resolve_call 2/2 不 BAN；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave587 | arr_return main tipU＋PRODUCT BAN（20/20；Ubuntu 原 tip UND=1＝只剩 jmp＝中途赋值 ly/ret_op／if-before-call／`rc=path` then `if`／`rc=spills/cps` then `if`／while tj_lbl；helper＝`arr_return_store_encoders`；stamp-only 禁 prefer；留 w439 overlay；族 PREFER 清空；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave586 | arr_return d tipU＋PRODUCT BAN（5/5；Ubuntu 原 tip UND=0＝unused ko/sret sink then `if`／`rc=emit` then `if`／if-before-assign／中途赋值 rty/sty／`rc=promote` then `if`；helper＝`arr_return_d_store_encoders`；stamp-only 禁 prefer；留 w439 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave585 | arr_return c_fallback tipU＋PRODUCT BAN（5/5；Ubuntu 原 tip UND=0＝`rc=emit` then `if`／if-before-assign／中途赋值 rty/sty／`rc=promote` then `if`；helper＝`arr_return_c_fallback_store_encoders`；stamp-only 禁 prefer；留 w439 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave584 | arr_return c_slice tipU＋PRODUCT BAN（14/14；Ubuntu 原 tip UND=1＝if-before-call／中途赋值／嵌套 `tk=kind_ord` then `if`／`rc=durable` then `if`／`rc=force_esz` then `if`／`rc=push/mov/pop` then `if`，只剩 void bump；helper＝`arr_return_c_slice_store_encoders`；stamp-only 禁 prefer；留 w439 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave583 | arr_return c_dest_array tipU＋PRODUCT BAN（9/9；Ubuntu 原 tip UND=1＝if-before-call／中途赋值／`tk=kind_ord` then `if`／`rc=durable` then `if`／`rc=force_esz` then `if`，只剩 void bump；helper＝`arr_return_c_dest_array_store_encoders`；stamp-only 禁 prefer；留 w439 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave582 | arr_return b tipU＋PRODUCT BAN（6/6；Ubuntu 原 tip UND=0＝if-before-call／`esc=call` then `if`／`rc=emit` then `if`／中途赋值／`rc=promote` then `if`；helper＝`arr_return_b_store_encoders`；stamp-only 禁 prefer；留 w439 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave581 | arr_return b0_durable tipU＋PRODUCT BAN（15/15；Ubuntu 原 tip UND=2＝if-before-call／中途赋值／嵌套 while／`rc=call` then `if`；helper＝`arr_return_b0_durable_store_encoders`；stamp-only 禁 prefer；留 w439 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave580 | arr_return b0_prep tipU＋PRODUCT BAN（14/14；Ubuntu 原 tip SEGV 139＝`*i32` out 存／if-before-call／`rc=call` then `if`；helper＝`arr_return_b0_prep_store_encoders`；stamp-only 禁 prefer；留 w439 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave579 | arr_return a2 tipU＋PRODUCT BAN（4/4；Ubuntu 原 tip UND=0＝if-before-call／`rc=emit` then `if`／`rc=mov` then `if`／`rc=memcpy` then `if`；helper＝`arr_return_a2_store_encoders`；stamp-only 禁 prefer；留 w439 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave578 | arr_return a tipU＋PRODUCT BAN（2/2；Ubuntu 原 tip UND=0＝if-before-call／`rc=sret` then `if`；helper＝`arr_return_a_store_sret`；stamp-only 禁 prefer；留 w439 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave577 | arr_lit_flat main tipU＋PRODUCT BAN（7/7；Ubuntu 原 tip UND=2＝if-before-call／`ko=kind_ord` then `if`／dest_elem then dest_ek／inner then inner_k；helper＝`arr_lit_flat_store_encoders`；stamp-only 禁 prefer；留 w438 overlay；族内无剩余 PREFER；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave576 | arr_lit_flat_scalar tipU＋PRODUCT BAN（3/3；Ubuntu 原 tip UND=0＝`n_arr=num_elems` then `if`／`store_sz=leaf_esz` then `if`／`while rc=step` then `if`；stamp-only 禁 prefer；留 w438 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave575 | arr_lit_flat_step tipU＋PRODUCT BAN（6/6；Ubuntu 原 tip UND=2＝`elem_ref=call` then `if`／`ko=kind_ord` then `if`／`if(ko==46) flatten`／`tr=try_struct` then `if`；stamp-only 禁 prefer；留 w438 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave574 | arr_lit_flat_one_scalar tipU＋PRODUCT BAN（7/7；Ubuntu 原 tip UND=0＝`rc=call` then `if`／`may_clobber=call`／`fi=flat_i[0]`／`flat_i[0]=fi+1`；`flat_i[0]` 存未发射故无编译 SEGV；stamp-only 禁 prefer；留 w438 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave573 | arr_lit_flat_struct tipU＋PRODUCT BAN（2/2；Ubuntu 原 tip UND=0＝if-before-call／中途赋值／`st=call` then `if`；`flat_i[0]` 存未发射故无编译 SEGV；stamp-only 禁 prefer；留 w438 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave572 | arr_lit_flat_rows tipU＋PRODUCT BAN（6/6；Ubuntu 原 tip UND=0＝while／中途赋值／`st=call` then `if`；stamp-only 禁 prefer；留 w438 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave571 | arr_lit_flat_one_row tipU＋PRODUCT BAN（4/4；Ubuntu 原 tip UND=1＝if-before-call／`ko=call` then `if`／`row_st=call` then `if`；stamp-only 禁 prefer；留 w438 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave570 | arr_lit_flat_cells tipU＋PRODUCT BAN（4/4；Ubuntu 原 tip UND=0＝while＋`st=call` then `if`；stamp-only 禁 prefer；留 w438 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave569 | arr_lit_flat_one_cell tipU＋PRODUCT BAN（2/2；Ubuntu 原 tip UND=0＝if＋中途赋值；stamp-only 禁 prefer；留 w438 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave568 | arr_lit_flat_repark tipU＋PRODUCT BAN（5/5；Ubuntu 原 tip UND=0＝`rc=call` then `if`；stamp-only 禁 prefer；留 w438 overlay；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave567 | arr_struct_lit_resolve_vf tipU＋PRODUCT BAN（8/8；Ubuntu 原 tip SEGV 139＝`*i32` 间接存；if-after-assign 亦 UND-drop；copy／call 调度叶已齐不 BAN；stamp-only 禁 prefer；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave566 | arr_struct_lit_call_bulk tipU＋PRODUCT BAN（11/11；Ubuntu 原 tip 只剩 pipe_store；call 调度叶已齐不 BAN；stamp-only 禁 prefer；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | wave565 | arr_struct_lit_copy_bulk tipU＋PRODUCT BAN（10/10；Ubuntu 原 tip 只剩 pipe_store；copy 调度叶已齐不 BAN；stamp-only 禁 prefer；L2 5/5＠20731160/6545976｜pabi＠18615296/3391224） |
| 2026-09-19 | docs | C迁移追踪简写；流水账归档 20260919。钉盘仍 `ecdb5cc1e`。 |
| 2026-09-19 | wave564 | arr_struct_lit_copy_elems tipU＋PRODUCT BAN（12/12）。 |
| 2026-09-19 | docs | 日切归档 18 号＋19 号主文档重写简写。 |
| 2026-09-19 | w530–w563 | Cap residual tipU＋PRODUCT BAN 连收（详见 [18 日归档](自举进度-归档-2026-09-18.md)）。不升钉。 |
