# Xlang / X language 自举进度

> **更新**：2026-09-21 · **tip 本波** wave706：**FORCE leftover-first F7 data family**。mega `export extern` `pipeline_elf_ctx_emit_data_len`／`append_data_zeros`／`data_poke_u8`／`pipe_modlet_bake_scalar_imm_to_data`（同 TU `objcopy --localize` 仍把 mega_prepare 的 CALL 绑到 mega 本地 t；mega BSS `g_pipe_elf_data_len` ≠ leftover gcc `g_pipeline_elf_data_len`）。合链 leftover gcc W（emit 0x22／append 0xa2／poke 0x4c）＋ leftover smash bake T 唯一全局。**asm_wpo_thin `-c` 绿** 81192B／73T（gdb prepare／bake／poke eax=0）。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5。钉盘 **`ecdb5cc1e`**（默认不升）
> **状态**：**尚未完全自举**。主刀 **M2**（FORCE 整合层）。
> **下波（w707）**：FORCE 扩更大卫星／其余 mega 同 TU 砸体 callee（`add_common_sym` leftover gcc W 仍可 leftover-first）。8 EOF 仍 parse-drop。**禁止** `-E` 当修法；勿复用 w690_force；mega FORCE 仍非产品默认；勿整叶 elf_ctx_thin overlay；勿 leftover-first smash-only `modlet_prepare` 自身（与 mega helper 表布局不匹配）；勿把 ~2000 个 `sub $0x888` mega T 整表 localize。

闸门：双端 L2；暂停 `xlang check`；L4 仅整波／谈自举。

### 维护约定

1. 每波必改本文（KPI／前排≤3／§6 一行）。终局债改 [`C迁移追踪.md`](C迁移追踪.md)。
2. §6 倒序 · 一行一波。过期整日流水日切归档后主文档重写。

***

## 0. 一句话

**尚未完全自举** · 钉盘 **`ecdb5cc1e`** · **w706** FORCE leftover-first F7 data family；asm_wpo_thin `-c` 绿 81192B／73T。

| 指标 | 状态 |
|------|------|
| 钉盘 L4 | ✅ 双端＠**`ecdb5cc1e`** |
| tip L4 | ✅ **双端全绿**＠`a54d1a99a`（连续十波）· 产品态健康 |
| 本波级别 | **w706＝FORCE leftover-first F7 data family**（日常 L2，非 L4；asm_wpo `-c` 绿，产品仍 w701 overlay） |
| 下一波 | **w707** FORCE 扩更大卫星／其余同 TU 砸体 callee（`add_common_sym` leftover gcc W） |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举效率方法-M2主链.md](自举效率方法-M2主链.md) | M2 主链 |
| [C迁移追踪.md](C迁移追踪.md) | 终局债（短状态表） |
| **本文** | KPI · 前排 · §6（9 月 21 日） |
| [自举进度-归档-2026-09-20.md](自举进度-归档-2026-09-20.md) | **9 月 20 日**（w618–w691 FORCE 整合战役） |
| [自举进度-归档-2026-09-19.md](自举进度-归档-2026-09-19.md) | **9 月 19 日**（w597–w617） |

***

## 2. 仪表盘

| 项 | 结果 |
|----|------|
| 产品 bstrict | **131** 套（双端 tip 全绿＠`eab2c15b1`） |
| 钉盘 | **`ecdb5cc1e`** |
| MG／Stage8 | MG ✅ · Stage8 **30/30** |
| SSH | `ubuntu-server`／`windows-server` |

***

## 3. 当前前排（≤3）

1. **主迁刀（w707）** — FORCE leftover-weaken **asm_wpo_thin `-c` 绿** 81192B／73T（w706 F7 data family export-extern）。下一刀＝扩更大卫星／其余同 TU 砸体 callee（`add_common_sym` leftover gcc W 0x138）。HARD BAN 仍跳：value_abi／wpo_dump／macho_write／arr_return／2 失败 thin。8 EOF 别名仍 parse-drop。
2. **次排** — elf_shndx sidecar 族原子；2 失败 thin（assign_rhsrax_arms P001／return_elf_impl T001）；malloc_trim weak。
3. **立卡不动** — arr_return 族（`x=call() then if` UND-drop＝真实发射能力缺口，M1 类）；MSVC va／WIN leftover；eq 深链；spawn 真体；nest 冻帽 64。

***

## 4. 本日要点（9 月 21 日）

| 波 | 结论 |
|----|------|
| w706 | **FORCE leftover-first F7 data family**：gdb 点名 `bake_scalar_imm_to_data` 返 −1（`g_aw_root_id=-1` wave344 .data）。同 TU localize mega T 仍把 prepare 的 CALL 绑到 mega 本地 t（`$rip`＝mega smash）。mega BSS `g_pipe_elf_data_len` ≠ leftover gcc `g_pipeline_elf_data_len`。G.7 mega 四面 `export extern`；合链 leftover gcc W emit 0x22／append 0xa2／poke 0x4c ＋ leftover smash bake T 唯一全局。**asm_wpo_thin `-c` 绿** 81192B／73T（prepare／bake／poke eax=0，mega_body eax=0）。FORCE L2 五件套绿。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁 leftover-first smash-only `modlet_prepare` 自身。禁整叶 elf_ctx overlay。禁整表 localize ~2000 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w705 | **FORCE 扩中等／大卫星**：leftover-weaken `/tmp/w704/force.o`（非 w690）。**12 中等** `-c` 绿（type_pool／slot_bytes／mega_body／binop_block_peel／module_enum／module_import／w157_sum／top_level_let／module_func／bootstrap_glue／struct_layout／onefunc）。**8 大**绿（dep_ctx／fixed_array_copy／block_domain／expr_sidecar／ast_forwarders／elf_ctx **用户 `-c`**／modlet／assign）。墙＝`asm_wpo_thin` CG002 `code_len=0`：`count=73` 但 `at()`／`emit_one` 未进；gdb `modlet_prepare_and_emit#1 eax=-1`。该面 leftover T 亦砸（`sub $0x1f38`／cltq），**无 gcc W**。产品同文件 `-backend asm -c` 绿 81024B（`backend.x` 无 mega_body 闸）。FORCE L2 五件套绿（hello=0／option=102／si=0／f32=0／rv=42）。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁整叶 elf_ctx overlay（用户 `-c`≠overlay）。禁整表 localize ~2000 个 `sub $0x888` mega T。禁 `-E`／禁 mega FORCE 产品默认。钉盘不升 |
| w704 | **FORCE leftover-first emit_fast**：mega `export extern` `pipeline_asm_emit_expr_elf_fast`（砸体 `sub $0x2e98` 盖 leftover W 0x1200）。合链 leftover fast 活后 parse_orch 仍 CG002：emit_one#2 文件级 `const W323_ARENA_OFF_NUM_EXPRS` 走 leftover fast VAR，调 mega 砸体 helper。localize `glue_call_arg_resolve_var_stack_off_elf_c`（W 0x70b）／`glue_var_expr_stack_off_elf_c`（W 0x72）／`asm_module_top_level_const_lit_i32`（W 0x14c，`sub $0x8c8` 砸体）后 **parse_orch `-c` 绿**（26246B／59T）。FORCE L2 五件套绿（hello=0／option=102／si=0／f32=0）。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5。禁整表 localize ~2000 个 `sub $0x888` mega T。禁 `-E`／禁 elf_ctx_thin overlay／禁 mega FORCE 产品默认。钉盘不升 |
| w703 | **FORCE 扩探针**：两函数／let／if／while／call-args／hello／option=102／si／f32 全绿；5 小卫星 thin `-c` 绿。中等 `parse_orch_thin`（产品 26358B／59T）CG002 `code_len=699`：emit_one#2 `w323_arena_num_exprs`（`return load_i32(a, CONST)`）在 `w499_mega_emit_body_sync` −1。根因＝mega 砸体先赢 leftover emit：`backend_emit_block_body_sync_elf` mega T `sub $0x8a8` 盖 leftover 84B gcc；内层 `pipeline_asm_emit_block_body_sync_elf` `sub $0x5228` 盖 0x22c6。G.7 mega 两面 `export extern`；合链 leftover W 唯一全局。下一内层 mega T `pipeline_asm_emit_expr_elf_fast`（`sub $0x2e98` 盖 leftover W 0x1200）。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5＠`509f4f6ef`。禁整表 localize ~2000 个 `sub $0x888` mega T（FORCE 默认帧）。禁 `-E`／禁 elf_ctx_thin overlay／禁 mega FORCE 产品默认。钉盘不升 |
| w702 | **mega 不得先赢 WPO emit-order**：FORCE mega/thin asm 把 `g_aw_pgo_emit_n` 比较编成立即数 0 → `at()` 恒 −1 → emit_one 空成功 → CG002 `code_len=0`。G.7 mega 三面 `export extern`；合链 localize 砸体 helper，leftover gcc W（prepare 0x270／count 0x2f／at 0x65）唯一全局。FORCE 两函数 `-o` **run=42**。产品 overlay 未改（pabi 2410944／xlang 5539200）。双端 L2 5/5＠`e6af7b3c2`。禁 `-E`／禁 elf_ctx_thin overlay／禁 mega FORCE 产品默认。钉盘不升 |
| w701 | **writers overlay reloc heap 单权威**：w697 keep-global 只露出 write_o，overlay 内 `t` helper 另有一份 heap；write_o 读空 heap → FORCE mega 后段 `call next_insn`（`block_alloc` return 12，产品同函数 return 1）→ parse P001。扩 keep-globals（append_reloc／offset_at／name_len／copy64／shndx_at／sidecar_reset）。mega `block_alloc` nrel 0→8，r_sym0 59→0。FORCE leftover-weaken 探针过 parse，新墙 CG002 `code_len=0`。Ubuntu 产品 overlay pabi 2410944／xlang 5539200。双端 L2 5/5＠`70312b027`。禁 `-E`／禁 elf_ctx_thin overlay。钉盘不升 |
| w700 | **leftover ELF UND 帽 256→2048**：活 writer 栈 `[256][128]` 静默丢 calloc/mmap → 1032 条 PLT32 r_sym=0（`pipe_gv_alloc_bytes` 调 0）。`.x` `pipe_elf_undef_cap` 已是 2048 但 leftover 强 rest 不用。补 BSS 2048 行＋跳过 rlen≤0（空名 UND 会让 ld 报 corrupt symtab）。FORCE mega UND 474／calloc·mmap＝U／r_sym=0 余 59；gdb：`calloc(1,0x21400)` 自 `pipe_gv_alloc_bytes` 进入 libc。新墙＝parse P001。产品保留 writer overlay（Ubuntu pabi 2376112／xlang 5517088）。双端 L2 5/5＠`ccf44fdad`。禁 `-E`／禁 elf_ctx_thin overlay。钉盘不升 |
| w699 | **FORCE mega `parser_*` 不得盖 parser_x**：mega 三面 `export extern`（parser_x 唯一 T）；库 TU 跳过 modlet runtime seed（wave344：无 main 不往第一导出面灌 COMMON 初值）。parser_x `parse_into_init` 补 `trait_reg_reset`。FORCE mega 2596074：三面 U、path64 T；合链 6368464／g05 8823120，链接 `parser_parse_into_init`＝parser_x T。探针过 parse_into_init，崩 `pipe_gv_alloc_bytes` 调 0。产品恢复 overlay＋modlet inject。双端 L2 5/5＠`8b3ae2899`。禁 `-E`。钉盘不升 |
| w698 | **leftover-weaken FORCE 全链（探针，产品恢复 overlay writer）**：264/266 thin（2 失败同 w627）＋w697 mega 2602022＋overlay pabi_weak＋filler 能链（T=2048 W=1424／6319424／g05 8778008）。COMMON 尺寸墙倒（合链 17408×2／86020×2；size1=2 非 297）。裸探针不再 sidecar SEGV，改崩 mega `parser_parse_into_init` T（0x0／1744B／`sub $0x888`／`lea 0x0(%rip)` 自写）盖 parser_x。localize 4 `parser_*` 后面改 `__stack_chk_fail`←`parser_onefunc_res_wire_dummy_head`；value_abi SEGV_ACCERR＠`0x790e72868000`。8 EOF 仍 leftover W。勿复用 w690_force。双端 L2 5/5＠`25e562753`。禁 `-E`。钉盘不升 |
| w697 | **leftover compact WAVE273 writer 入链（FORCE COMMON 尺寸墙倒）**：从 from_x WAVE273 leftover 布局抽 overlay TU（勿整叶 elf_ctx_thin；布局不匹配会卡死 FORCE）。localize-all 后只露出 3 writer＋`common_size_at`，writers-first `ld -r` 叠进 hybrid pabi。Ubuntu FORCE mega 2602022／297 COMMON hist 正确（8×138／256×33／512×28／17408／86020；size=1＝1）。Darwin 同配方 llvm-objcopy＋`ld -r`。双端 L2 5/5＠`e371c0495`。禁 `-E`／禁 elf_ctx_thin overlay。钉盘不升 |
| w696 | **FORCE COMMON size=1 定因＋writer 权威（源已修，产品 leftover writer 未入链）**：noFORCE mega 297 COMMON 尺寸正确（8/256/512…／align16）；FORCE 297×size=1。根因＝FORCE 砸 dual-copy sidecar，writer 信 sidecar=1 跳过 ctx offset。G.7 补 w672：ctx offset 先于 sidecar（`.x`＋thin＋from_x）。elf_ctx_thin 整叶／三面 overlay 与 leftover ctx 布局不匹配→FORCE 编译卡死（已恢复 hybrid pabi）。from_x 整叶 cc 类型冲突。双端 L2 5/5＠`9730d918d`。禁 `-E`。钉盘不升 |
| w695 | **FORCE 全链重建（探针波，产品恢复）**：mono 2614412／T=2048＋264 thin（2 失败同 w627）。裸合链不过（8 EOF 别名 parse-drop）。leftover-weaken 能链但 sidecar `pipe_load_i32_le`←`append_lib_root` 崩——根因＝mega 文件级 `u8[17408]` 全落 1 字节 Lxml COMMON（297×size=1；dep_ctx thin 同 let＝0x4400）。族原子藏 mega dep_ctx 97 面后 sidecar 过；get_copy 6 hit 全净（rdi=栈／rdx=1\|2，0×`0x1800000001`）；下一崩＝`pipe_elf_shndx_sidecar_reset` memset（同类）。勿复用 w690_force。双端 L2 5/5＠`a0b0bb721`。禁 `-E`。钉盘不升 |
| w694 | **value_abi 重编＋二次 get_copy 闭合**：w693 编译器编 thin，type_get dest＠0x450 ≠ local＠0x238（U）／0x20（D）。C sret 探针两次 get_copy 入口均为合法栈／arena 指针（非 `0x1800000001`），拷回 42/99，rc=0。旧 force 混链 sidecar 崩＝整合债（w695 全量重建）。产品未动。双端 L2 5/5＠`a0b0bb721`。禁 `-E`。钉盘不升 |
| w693 | **sret dest 与 532B 局部分离**：rest `mega_body_c` gcc overlay 入 hybrid pabi。fill_local 可能停在 let 起点 → home=`align8(next+ret_sz)`。探针 dest＠0x450、local＠0x238（U）／0x20（D）不重叠。双端 L2 5/5＋`&g=42`＠`1aad86a9e`。禁 `-E`。钉盘不升 |
| w692 | **sret 槽与 532B Type 局部重叠（根修波）**：C 调用面是标准 sret（`sizeof(ast_Type)=532`）；force `type_get` 把 hidden dest 存在 `-0x120`，532B `let t` 在 `-0x248` 盖住该指针。根因＝`next_offset+256` 空洞。修法＝`fill_local` 之后预约（emit_frame thin＋from_x 孪生）。双端 L2 5/5＋`&g=42`＠`df17ed485`。rest `mega_body_c` 待 Ubuntu 重编 from_x 入链。禁 `-E`。钉盘不升 |

***

## 5. 与归档

- **20 日**：[自举进度-归档-2026-09-20.md](自举进度-归档-2026-09-20.md)（w618–w691：PREFER 放开→mega FORCE 打通→72 桩／帽／strb／sret 尾转发）
- **19 日**：[自举进度-归档-2026-09-19.md](自举进度-归档-2026-09-19.md)（w597–w617：COMMON shndx、ARM64 栈参、wpo_dump／asm_locals PREFER；双端 L4 连续四波绿）
- **C 追踪流水账**：[archive/C迁移追踪-流水账归档-20260919.md](archive/C迁移追踪-流水账归档-20260919.md)
- **现行钉盘＝`ecdb5cc1e`**。

***

## 6. 变更记录（倒序 · **9 月 21 日**）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-21 | wave706 | FORCE leftover-first F7 data family（export extern emit/append/poke/bake）。asm_wpo_thin `-c` 绿 81192B／73T。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave705 | FORCE 扩卫星：12 中等＋8 大 `-c` 绿。墙＝asm_wpo 砸体 `modlet_prepare`（无 gcc W）。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave704 | FORCE leftover-first emit_fast（export extern＋localize resolve／stack_off／top_level_const_lit）。parse_orch `-c` 绿（26246B／59T）。FORCE L2 五件套绿。产品 overlay 未改。双端 L2 5/5 |
| 2026-09-21 | wave703 | FORCE 扩探针：hello／option／si／f32＋5 小卫星 thin 绿。parse_orch CG002＝mega 砸体先赢 leftover body_sync（两面 export extern）。下一内层 emit_fast。产品 overlay 未改。双端 L2 5/5＠`509f4f6ef` |
| 2026-09-21 | wave702 | mega 不得先赢 WPO emit-order（export extern＋localize 砸体）。FORCE 两函数 `-o` run=42。CG002 `code_len=0` 倒。产品 overlay 未改。双端 L2 5/5＠`e6af7b3c2` |
| 2026-09-21 | wave701 | writers overlay 与 append_reloc 共用 reloc heap（扩 keep-globals）。FORCE parse P001 倒。新墙 CG002 `code_len=0`。产品保留 overlay。双端 L2 5/5＠`70312b027` |
| 2026-09-21 | wave700 | leftover ELF UND 帽 256→2048＋跳过空名。calloc 调 0 倒。新墙 FORCE parse P001。产品保留 overlay。双端 L2 5/5＠`ccf44fdad` |
| 2026-09-21 | wave699 | FORCE mega `parser_*` 不得盖 parser_x（export extern＋库 TU 跳过 seed）。新墙 GrowVec／sidecar 调 0。产品恢复。双端 L2 5/5＠`8b3ae2899` |
| 2026-09-21 | wave698 | leftover-weaken FORCE 能链；COMMON 尺寸墙倒。新墙 mega `parser_parse_into_init` 盖 parser_x（localize 后 stack-chk）。产品恢复。双端 L2 5/5＠`25e562753` |
| 2026-09-21 | wave697 | leftover compact WAVE273 writer 入链（非 elf_ctx_thin）。FORCE mega 297 COMMON 尺寸正确（size=1 仅 1）。双端 L2 5/5＠`e371c0495` |
| 2026-09-21 | wave696 | FORCE COMMON size=1＝sidecar 砸尺寸；writer 改 ctx offset 权威。产品 leftover writer 未入链（thin overlay 卡死 FORCE）。双端 L2 5/5＠`9730d918d` |
| 2026-09-21 | wave695 | FORCE 全链重建：get_copy 6 hit 已净。墙＝mega `u8[N]` COMMON size=1（thin 0x4400）。族原子 dep_ctx 后下一崩 elf_shndx sidecar。双端 L2 5/5＠`a0b0bb721` |
| 2026-09-21 | wave694 | value_abi 用 w693 编译器重编；C sret 二次 get_copy 入口已净（非 0x1800000001）。旧 force 混链 sidecar 崩→w695 全量重建。双端 L2 5/5＠`a0b0bb721` |
| 2026-09-21 | wave693 | sret home 越过 ret_sz；rest mega_body overlay 入链。探针 dest＠0x450 ≠ local。双端 L2 5/5＠`1aad86a9e` |
| 2026-09-21 | wave692 | sret 槽改在 fill_local 后预约（去掉 +256 空洞）；Type 532B 局部不再盖 hidden dest。双端 L2 5/5＠`df17ed485`。FORCE 复测待 Ubuntu 重编 rest mega_body |
| 2026-09-21 | docs | 日切归档 20 号（w618–w691）；21 号主文档重写简写。钉盘仍 `ecdb5cc1e`。 |
