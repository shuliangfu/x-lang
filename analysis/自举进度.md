# Xlang / X language 自举进度

> **更新**：2026-08-02 · tip **wave1213** BC 8.3.1 G.7 batch migrate 4 utility fns（glue_arena_expr_at_ref + glue_arena_expr_kind_at_ref + glue_var_expr_stack_off_elf_c + pipeline_sizeof_elf_ctx · glue 4337→4317 · L2 双端 · **不升钉**）\
> **产品 L4 放行钉盘**：**`77b334842`**（wave942 Makefile 物理删除 + 双端 L4 真冷）· 前钉盘 **`9bb7a757c`**（wave710）\
> **产品 residual tip**：**`91ffe7899`**（wave1213 代码）· Stage 8 tip **713277ef9**（wave1041）· 文档 tip **4bacaf18c** · 钉盘仍 **`77b334842`**\
> **tip L4 安全网**：**`4bacaf18c`**（wave1022 双端 L4 + 全量 bstrict 129 OK · **不升钉**；前网 `f80aa4593`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：**MG ✅** · **BC 开**（wave1210-1213 batch G.7 migrate 4 utility fns · glue 4337→4317）· **Stage 8 开**（批次 2 wave1036 lsp_io + wave1038–1040 build_* 三件套 + wave1041 cfg_eval 退役 · 13/30）· 钉盘 `77b334842`\
> **下波候选**：**BC 8.3.1** 继续 leaf 迁移（mega_body 主循环迁移 · 或 ast_pool_module_func 域扩补 · 或 assign/expr_stmt 残留簇）或 **8.3.3** soa·PC



> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[01](自举进度-归档-2026-08-01.md) · [31](自举进度-归档-2026-07-31.md) · [30](自举进度-归档-2026-07-30.md) · [29](自举进度-归档-2026-07-29.md) · …

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `77b334842`**（wave942 · Makefile 物理删除 + 双端 L4 真冷）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（Makefile 已删除；**变更记录只在本文 §6**）。

| 指标 | 状态 |
|------|------|
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`77b334842`**（wave942）；residual tip **45b0a6b3f**（wave1196 ast_ast_arena_init/alloc cluster + implicit_tail_expr_disallowed_by_glue）；Stage 8 tip **713277ef9**（wave1041）；文档 tip **4bacaf18c**；tip L4 安全网 **`4bacaf18c`**（**不升钉**） |
| Track MG | **11.3.1 MG ✅**（wave941 物理删 Makefile · wave942 catalog CFLAGS · 0 make in bootstrap） |
| Track BC | **开**（wave963 inventory · wave965–977 8.3.1 + wave978–994 8.3.2 + wave995–1021 … + **wave1022 slice reent** + **wave1023 var_decl+lazy_append** + **wave1024 slice dual-GP+slice_from_array** + **wave1025 sret return path** + **wave1026 field_access layout/offset** + **wave1027 lea/arm64 sret helpers** + **wave1028 async CPS emit domain** + **wave1029 x86 micro-encoders** + **wave1030 with_arena scope domain** + **wave1031 cmp cc helpers** + **wave1032 empty struct check** + **wave1033 TokenKind variant tag** + **wave1034 float bits lo/hi** + **wave1035 struct_lit field offset/type_ref** + **wave1037 16 个 try_index_var_* fwd 清理** + **wave1042 block_final_expr 同叶 fold** + **wave1043 array_let_empty_init 同叶 fold** + **wave1044 struct_layout_compute_field_offset 同叶 fold** + **wave1045 func_param_agg_byte_size 同叶 fold** + **wave1046 func_return_byte_size 同叶 fold** + **wave1047 func_param_home_width 同叶 fold** + **wave1048 call_return_byte_size 同叶 fold** + **wave1049 struct_layout_index_by_type_name 同叶 fold** + **wave1050 struct_layout_field_offset_by_name 同叶 fold** + **wave1051 struct_lit_value_bytes 同叶 fold** + **wave1052 sync_struct_layout_field_offsets 同叶 fold** + **wave1053 struct_layout_metrics + public wrapper 同叶 fold** + **wave1054 type_align_simple 同叶 fold** + **wave1055 array_temp_bytes 同叶 fold** + **wave1056 type_size_simple 同叶 fold（triad 收口）** + **wave1057 sysv_dual_gp_byte_size 同叶 fold** + **wave1058 store_retval_pair 同叶 fold** + **wave1059 func_param_is_f32/f64 同叶 fold + dead code delete** + **wave1060 deref_struct16_rax_ptr 同叶 fold** + **wave1061 call_struct16_ret_needs_rax_deref 同叶 fold** + **wave1062 call_return_type_kind_ord 同叶 fold** + **wave1063 call_param_type_ref_at 同叶 fold** + **wave1064 copy_large_struct_from_rax_ptr 同叶 fold** + **wave1065-1094 批量同叶 fold 30 叶**（assign-kind/import count/ctx reset/pair base/expr stmt/struct layout warn/block return scan/enum name match/call arg effective type/widen 四件套/type_refs_equal 域/alias resolver/let name match/import binding name 域/overload resolution 域）+ **wave1095-1099 generic call inference 域 5 叶同 fold** + **wave1100 modlet ELF emit 域 12 fns + typedef + macro + global 同 fold** + **wave1101-1110 6 域同 fold**（codegen outbuf／asm label format／wpo mono thunk／next-offset／fold primitives／count-up-while loop fold）+ **wave1111-1129 6 域 18 fns 同 fold**（dep return type mapping／struct layout name lookup／vector type-let／skipped-typeck backfill／struct same-shape／WPO-S3 stack-escape）+ **wave1130-1135 3 域 8 fns 同 fold**（float promote pair／linear name moved／lval param ptr field cluster）+ **wave1136-1137 stack-escape scan pair 同 fold**（scan_expr + scan_block → region_assign.c EOF）+ **wave1138-1140 spill byte cluster 同 fold**（3 fns + 3 macros → asm_emit_spill.c EOF）+ **wave1141-1145 fixed array helpers + call ptr compat 同 fold**（5 fns → vector_let.c EOF + method_call.c EOF）+ **wave1146-1149 TypeKind tag／debug reporter／bootstrap fixup／block locals tree 同 fold**（4 fns → cmp + method_call + block_inits EOF）+ **wave1150-1151 call-target resolver + f32 xmm param homing 同 fold**（2 fns → call_args.c EOF · GLUE_TYPECK_IMPORT_BINDING/SELECT enum 前移）+ **wave1152-1154 scope setter fold + dead code cleanup**（1 fn → block_body.c EOF · 2 dead fn 删除 · **glue.c 静态叶迁移基本收口**）+ **wave1155-1157 call return-type + diag fmt + linear clusters 同 fold**（12 fns → method_call + assign + check_block.c EOF · **extern 函数迁移启动**）+ **wave1158 type_refs_equal／bool／expr_type_ref extern wrappers 同 migrate**（9 fns → coerce_init.c EOF）+ **wave1159 call_resolve + method_call accessor extern wrappers 同 migrate**（13 fns → method_call.c EOF）+ **wave1160-1162 batch migrate expr accessor clusters**（33 fns + 12 ast_pipeline_* wrappers → expr_rec.c EOF · if/as/match/const_folded/enum_variant/block + index/field_access/line/col + lit/var/field_access/unary/binop/null · glue 10551→10251 · static fwd decl for glue_arena_expr_at_ref）+ **wave1163-1165 module_func/struct_lit/ret_coerce clusters 同 migrate**（12 fns → ast_pool_module_func + struct_lit + return.c EOF · glue 10251→10137）+ **wave1167-1170 region scope + dep return type + func resolution + overload wrapper clusters 同 migrate**（16 fns → region_assign.c + method_call.c EOF · glue 10137→9818 · region scope stack push/pop/len + scan_module_struct_stack_escape + dep return type mapping + func resolution + overload wrappers）+ **wave1175-1181 batch migrate field access + return + coerce + asm emit context + elf/codegen forwarder clusters 同 migrate**（32 fns → 6 domain files + 2 new files · pipeline_asm_emit_field_access +return + ast_pool_dep_ctx/module_func + coerce_init · **wave1180 新建 pipeline_asm_emit_context.c** 14 asm emit context setters/getters · **wave1181 新建 pipeline_elf_codegen_forwarders.c** 18 elf/codegen prefix forwarders · glue 9818→9014）+ **wave1182 migrate ast_pipeline forwarder cluster 同 migrate**（246 fns + 1 utility → pipeline_ast_forwarders.c new file · module_func/dep_ctx/ctx_lib_root/scratch_buf/codegen/elf_ctx/block/module/onefunc/arena forwarders · glue 9014→8410）+ **wave1183 migrate ast_ast_block + ast_pipeline_arena + ast_pipeline_expr forwarder clusters 同 migrate**（97 fns → 3 domain files EOF · ast_ast_block_*（31 fns：patch_block_parent_links + num_consts/lets/loops/for_loops/if_stmts/regions/expr_stmts/stmt_order + region_body_ref + stmt_order_kind/idx + const/let init/type_ref + expr_stmt/final_expr ref + while/for/if cond/body/init/step/then/else ref + resolve_var_to_type_ref + disallows_implicit_tail + apply_call_resolve）→ ast_pool_block.c EOF · ast_pipeline_arena_*（12 fns：type/expr/block/func get_copy/set_copy + expr_write_var/binop）+ ast_ast_arena_*_get/set + ast_arena_*_get/set（14 fns：block/func get/set + type/expr/block/func twins delegating to ast_ast_）→ ast_pool_arena.c EOF · ast_pipeline_expr_*（40 fns：call/method_call/match/struct_lit/array_lit/index/field_access/line/col/lit/var/unary/binop/null accessors + codegen/backend prefix forwarders）→ pipeline_asm_emit_expr_rec.c EOF · static fwd decls added for ast_ast_arena_type/expr_get/set in ast_pool_arena.c + 11 pipeline_expr_* append/prepare/on_created in expr_rec.c · glue 8410→8097 · -313 净）+ **wave1184 migrate pipeline_parser_library_init cluster 同 migrate**（8 fns → ast_pool_arena.c EOF · library_init_bool_type/named_type/var_expr/field_access_expr/enum_variant_expr/eq_expr/labeled_block + extern_init_arena_func · 11 fwd decls for pipeline_module_fill_u8_64 + ast_pipeline_block_append_labeled + pipeline_module_func_* · glue 8097→7924 · -173 净）+ **wave1185 migrate parser result copy/lex/slice helper cluster 同 migrate**（20 fns + 4 typedefs → pipeline_parser_result.c new file · parser_slice_from_buf/lexer_parser_slice_from_buf/pipeline_source_slice/parser_lexer_pos_before/parser_lex_from_*_into/pipeline_parser_extern_parse_set_fail_c/pipeline_parser_library_result_copy_into_c/pipeline_parser_try_skip_result_copy_into_c/pipeline_parser_set_onefunc_fail_c/pipeline_parser_onefunc_buf_into_set_success_c + 4 glue-local typedefs · fwd decl for pipeline_module_fill_u8_64_from_src_c · glue 7924→7718 · -206 净）+ **wave1186 migrate pipeline parse/load/typeck orchestration cluster 同 migrate**（24 fns → pipeline_parse_orch.c new file · pipeline_parse_into_buf_impl_c／_c + pipeline_load_import_from_disk_impl_c／_c + pipeline_sync_dep_slots_from_driver_impl_c／_c + pipeline_parse_into_with_init_buf_impl_c／_c + pipeline_parse_into_buf + pipeline_load_import_from_disk + pipeline_sync_dep_slots_from_driver + pipeline_resolve_path_x + pipeline_read_file_x + pipeline_parse_into_with_init_c + pipeline_lsp_diag_parse_typeck_buf_impl_c／_c + pipeline_lsp_diag_parse_entry_buf_impl_c + pipeline_typeck_after_parse_ok_impl_c／_c + pipeline_typeck_after_parse_ok_buf_impl_c + pipeline_typeck_x_stack_escape_gate_from_src_c + pipeline_lsp_diag_parse_typeck_buf + pipeline_typeck_after_parse_ok (XLANG_WEAK standalone) · XLANG_WEAK 定义保留允许产品纯代码强符号覆盖 · glue 7718→7300 · -418 净）） |
| Track L (Stage 8) | **开**（批次 0 清理 ✅ · 批次 2 **wave1036 lsp_io_gen + wave1038 build_gen + wave1039 build_runner_gen + wave1040 build_runtime_x_gen + wave1041 cfg_eval_gen 退役** ✅ · 13/30 retired · build_*_gen 三件套 + cfg_eval 全退役 · 详见 [阶段8高效退役分析.md](阶段8高效退役分析.md)） |
| mac / Ubuntu L4（钉盘） | ✅ Ubuntu L4 真冷（wipe + bootstrap + build + smoke）；✅ macOS build |
| mac / Ubuntu L4（tip 安全网） | ✅ `/tmp/mac_true_cold_4bacaf18c.log` · `/tmp/mac_true_bstrict_4bacaf18c.log` · `/tmp/ubuntu_true_cold_4bacaf18c.log` · `/tmp/ubuntu_true_bstrict_4bacaf18c.log`（**双端 129 OK**） |
| 本波构建级别 | tip 安全网 **L4** @ `4bacaf18c`（不升钉）；日常 BC 仍 L2；钉盘仍 `77b334842` |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录** |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图 |
| [自举进度-归档-2026-08-01.md](自举进度-归档-2026-08-01.md) | **8月1日 wave964–1111-1129**（BC 8.3.1 G.7 fold 50+ 叶 + Stage 8 退役 13/30） |
| [自举进度-归档-2026-07-31.md](自举进度-归档-2026-07-31.md) | **31 日全日 wave895–963**（MG 真删 + post-delete + 开 BC） |
| [自举进度-归档-2026-07-30.md](自举进度-归档-2026-07-30.md) | **30 日全日 wave758–894**（只读） |
| [自举进度-归档-2026-07-29.md](自举进度-归档-2026-07-29.md) | **29 日** wave618–757 + tip L4 升钉 `9bb7a757c` |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单 |
| 产品 L4 放行钉盘 | **`77b334842`** | 31 日 wave942 升钉；Makefile 物理删除 + 双端 L4 真冷 |
| tip L4 安全网 | **`4bacaf18c`** | 01 日 wave1022 后双端 L4 + bstrict **129** · **钉盘不升**（前 `f80aa4593`） |
| residual tip（BC） | **`91ffe7899`** | wave1213 代码（batch migrate 4 utility fns · glue_arena_expr_at_ref → as.c EOF ＋ glue_arena_expr_kind_at_ref → parse_orch.c EOF ＋ glue_var_expr_stack_off_elf_c → index_helpers.c EOF ＋ pipeline_sizeof_elf_ctx → elf_codegen_forwarders.c EOF · glue 4337→4317）；前 tip `e41bb1533`（wave1209）；钉盘仍 `77b334842` |
| mac 钉盘 L4 | ✅ build exit=0 | macOS Darwin/arm64 g05 relink OK |
| Ubuntu 钉盘 L4（金标） | ✅ L4 真冷 | Ubuntu Linux/x86_64 wipe + bootstrap + build + smoke 全绿 |
| 产品矩阵（tip L4） | ✅ rv42 / option / si / hello | 双端 L4 后矩阵全绿（Hello `0a`） |
| 前钉 | `77b334842` · `9bb7a757c` · `4fa4f07e7` · … | wave942 → wave710 → … |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 18 号结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **R2 / Cap** | wave710 升钉后按需 | 明细见 29 归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1**（已执行） |
| `TREE_ARMED` | **1** |
| `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1**（wave941 物理删除） |
| `PHYS_DEL_WINDOWS_GATE_STATUS` | `reproven_green` @ `71dd60a12`（wave922 · wave940 一键冷启动） |
| preflight blockers | **全清零**（wave937–939） |
| 真删条件 | ✅ 已完成 @ wave941/942 · 钉盘 **`77b334842`** |
| post-delete residual | **✅ 已收**（wave944–964） |

***

## 3. 当前前排（≤3 条）

1. **BC 轨（wave963 开 · wave965–1137）** — 8.3.1 typeck 六域 + asm emit **七十刀**（+…／array_lit durable／**slice reent deep-copy**／**var_decl+lazy_append 共享叶**／**slice dual-GP+slice_from_array 同叶**／**lea/arm64 sret helpers 同叶**／**async CPS emit domain 同叶**／**x86 micro-encoders 同叶**／**with_arena scope domain 同叶**／**cmp cc helpers 同叶**／**empty struct check 同叶**／**TokenKind variant tag 同叶**／**float bits lo/hi 同叶**／**struct_lit field offset/type_ref 同叶**／**block_final_expr 同叶（零自调用）**／**array_let_empty_init 同叶**／**struct_layout_compute_field_offset 同叶**／**func_param_agg_byte_size 同叶**／**func_return_byte_size 同叶**／**func_param_home_width 同叶**／**call_return_byte_size 同叶**／**struct_layout_index_by_type_name 同叶**／**struct_layout_field_offset_by_name 同叶**／**struct_lit_value_bytes 同叶**／**sync_struct_layout_field_offsets 同叶**／**struct_layout_metrics + public wrapper 同叶**／**type_align_simple 同叶**／**array_temp_bytes 同叶**／**type_size_simple 同叶（triad 收口）**／**sysv_dual_gp_byte_size 同叶（SysV ABI 分类域）**／**store_retval_pair 同叶（retval store 域）**／**func_param_is_f32/f64 同叶 + dead code delete（SysV param classification 域）**／**deref_struct16_rax_ptr 同叶（struct16 retval deref 域）**／**call_struct16_ret_needs_rax_deref 同叶（struct16 retval deref classifier 域）**／**call_return_type_kind_ord 同叶（call return TypeKind resolve 域）**／**call_param_type_ref_at 同叶（call param type_ref resolve 域）**／**copy_large_struct_from_rax_ptr 同叶（>16B struct retval memcpy 域）** 有则补全 + wave1065-1094 批量 30 叶 + wave1095-1099 generic call inference 域 5 叶 + **wave1100 modlet ELF emit 域 12 fns + typedef + macro + global** + wave1101-1110 6 域 + wave1111-1129 6 域 18 fns + **wave1130-1135 3 域 8 fns**（float promote pair／linear name moved／lval param ptr field cluster）+ **wave1136-1137 stack-escape scan pair**（scan_expr + scan_block → region_assign.c EOF）+ **wave1138-1140 spill byte cluster**（3 fns + 3 macros → asm_emit_spill.c EOF）+ **wave1141-1145 fixed array helpers + call ptr compat**（5 fns → vector_let.c EOF + method_call.c EOF）+ **wave1146-1149 TypeKind tag／debug reporter／bootstrap fixup／block locals tree**（4 fns → cmp + method_call + block_inits EOF）+ **wave1150-1151 call-target resolver + f32 xmm param homing**（2 fns → call_args.c EOF · GLUE_TYPECK_IMPORT_BINDING/SELECT enum 前移）+ **wave1152-1154 scope setter fold + dead code cleanup**（1 fn → block_body.c EOF · 2 dead fn 删除 · **glue.c 静态叶迁移基本收口** — 剩余 5 个 static 定义均为基础设施级（ctx_layout／arena accessor／var stack off／type kind 映射），跨 20+ 域文件调用，不适合单域迁移）+ **wave1155-1157 call return-type + diag fmt + linear clusters 同 fold**（12 fns → method_call + assign + check_block.c EOF · **extern 函数迁移启动**）+ **wave1158-1182 extern wrapper migration wave**（wave1158 type_refs_equal/bool/expr_type_ref 9 fns → coerce_init · wave1159 call_resolve + method_call accessor 13 fns → method_call · wave1160-1162 expr accessor 33 fns + 12 wrappers → expr_rec · wave1163-1165 module_func/struct_lit/ret_coerce 12 fns · wave1167-1170 region scope + dep return type + func resolution + overload 16 fns · wave1175-1181 field access + return + coerce + asm emit context + elf/codegen forwarder 32 fns + 2 new files · wave1182 ast_pipeline forwarder 246 fns + 1 utility → pipeline_ast_forwarders.c new file · glue 11126→8410）+ **wave1183 ast_ast_block + ast_pipeline_arena + ast_pipeline_expr forwarder clusters 同 migrate**（97 fns → ast_pool_block + ast_pool_arena + pipeline_asm_emit_expr_rec.c EOF · ast_ast_block_* getters + control flow + ast_pipeline_arena_* + ast_ast_arena_* + ast_arena_* twins + ast_pipeline_expr_* accessors + codegen/backend prefix · glue 8410→8097））+ 8.3.2 …+top_level residual 同 TU thin；host-cc 仍编 **pipeline_glue + ast_pool** 等大叶；下一刀 **继续 ast_pool.c 叶迁移** 或 **8.3.3 field_access／soa** 或 **ast_arena_* accessors**。
2. **post-delete residual**：**✅ 已收**（wave944–**964**）。
3. **PC 轨** — 用户默认仍可能 `labi_invoke_cc`；默认 asm backend 终局 ⬜。

***

## 4. 与归档的关系

- **8月1日归档**：[自举进度-归档-2026-08-01.md](自举进度-归档-2026-08-01.md) — wave964–1111-1129（BC 8.3.1 G.7 fold 50+ static 叶 → 域文件 EOF + Stage 8 build_*_gen + cfg_eval 退役 13/30 · 钉盘仍 `77b334842`）。
- **31 日归档**：[自举进度-归档-2026-07-31.md](自举进度-归档-2026-07-31.md) — wave895–963（B7B multi-target → 双端 L4 → 物理删 Makefile → post-delete residual → 开 BC inventory）。
- **30 日归档**：[自举进度-归档-2026-07-30.md](自举进度-归档-2026-07-30.md) — wave758–894。
- **29 日归档**：[自举进度-归档-2026-07-29.md](自举进度-归档-2026-07-29.md) — wave618–757 + 升钉 `9bb7a757c`。
- **勿**用归档 tip 或旧钉冒充现行钉盘。**现行产品钉盘 = `77b334842`**；tip L4 安全网 **`4bacaf18c`**（不升钉）。
- 归楼后主文档只保留 KPI + 前排 + **仅 8 月 2 日** §6；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
|------|----------|
| **macOS arm64** | 钉盘 build ✅ · tip L4+bstrict 129 ✅ @ `4bacaf18c` · Makefile 已物理删除 |
| **Ubuntu x86_64（金标）** | 钉盘 L4 真冷 ✅ @ `77b334842` · tip 安全网 L4+bstrict 129 ✅ @ `4bacaf18c` · Makefile 已物理删除 |
| **Windows MSYS2** | wave940 一键冷启动绿 · STATUS=`reproven_green` · Makefile 已物理删除 |

***

## 6. 变更记录（倒序 · 8 月 2 日；1 日见归档）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-08-02 | **wave1210-1213** · BC 8.3.1 · G.7 batch migrate 4 utility fns（→ as.c + parse_orch.c + index_helpers.c + elf_codegen_forwarders.c EOF） | **代码** · **不升钉** · **L2 双端**。批量迁移 4 个工具函数自 `pipeline_glue.c` 迁入 4 个域文件 EOF。**wave1210**：`glue_arena_expr_at_ref`（5 行，static）→ `pipeline_asm_emit_as.c` EOF — 读 arena expr 槽指针；无 static 依赖；5 个 consumer 文件 #include 均在 L1324 之后。**wave1211**：`glue_arena_expr_kind_at_ref`（7 行，static）→ `pipeline_parse_orch.c` EOF — 返回 expr kind enum；glue.c L443 保留 static fwd decl（覆盖 L1967 `pipeline_expr_kind_ord_at` 调用点）；parse_orch.c L897 已有 static fwd decl。**wave1212**：`glue_var_expr_stack_off_elf_c`（18 行，static）→ `pipeline_asm_emit_index_helpers.c` EOF — 解析 VAR 栈偏移（scoped table + name fallback）；glue.c L1109 保留 static fwd decl（覆盖 return.c L356 #include L1302 < L1530）；dep `glue_asm_local_var_stack_off_scoped`（vector_simd.c L93 #include L1513 < L1530 可见）；30+ callsites。**wave1213**：`pipeline_sizeof_elf_ctx`（6 行，#ifdef guarded）→ `pipeline_elf_codegen_forwarders.c` EOF — 返回 sizeof(ElfCodegenCtx)；`#ifdef XLANG_PARSER_EXE_PIPELINE_GLUE` TU 级守卫在任意位置有效；无 TU 内调用点（sole consumers = seeds rt_run_asm_backend.from_x.c via extern）。G.9 详细英文 docblock 全覆盖。glue 4337→4317（-20 净）；as.c 472→486（+14）；parse_orch.c 973→988（+15）；index_helpers.c 3136→3170（+34）；elf_codegen_forwarders.c 209→223（+14）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· seed smoke OK · examples/hello.x emit + run "Hello World" exit=0。PLATFORM: SHARED — 4 函数均平台无关工具。Tip **91ffe7899** |
| 2026-08-02 | **wave1205-1209** · BC 8.3.1 · G.7 batch migrate call-arg emit/size cluster（6 fns → call_args.c + block_if_stmt.c EOF） | **代码** · **不升钉** · **L2 双端**。批量迁移 6 个 call-arg 发射/查询函数簇自 `pipeline_glue.c` 迁入 `pipeline_asm_emit_call_args.c` EOF（5 fns）＋ `pipeline_asm_emit_block_if_stmt.c` EOF（1 fn），同域 fold 到 call-arg emit 域。**wave1205**：`pipeline_asm_emit_expr_call_c` + `pipeline_asm_emit_expr_method_call_c`（2 薄包装，各 8 行）— pool-copy ast_Expr 后委托 seed partial backend_emit_expr{,_method}_call；call_args.c L64-72 添加 extern fwd decls（原 glue.c L3208 在 #include L1660 之后）。**wave1206**：`pipeline_asm_emit_if_then_block_body_elf_c`（21 行）— 暂存/恢复 num_locals + next_offset，fill_local_slots + sync block body；block_if_stmt.c L25 添加 extern fwd decl for `pipeline_asm_fill_local_slots`（定义在 context.c L2564，在 #include L2421 之后）。**wave1207**：`pipeline_asm_push_sysv_memory_by_value_elf_c`（99 行，x86_64 SysV MEMORY push）— 从 4 种来源（VAR/CALL sret/STRUCT_LIT/FIELD_ACCESS）物化 >16B 聚合体到 high-end frame temp 后逆序压栈。**wave1208**：`pipeline_asm_store_memory_by_value_to_sp_elf_c`（102 行，arm64 AAPCS64 MEMORY store）— G.7 twin of push_sysv，物化到 low-end frame temp 后正序拷贝到 SP+sp_off。**wave1209**：`pipeline_asm_call_arg_value_byte_size_c`（98 行，call-arg value byte size query）— 解析 formal pty → expr resolved → VAR decl → FIELD_ACCESS field type 序列取 max；TYPE_SLICE/TYPE_ARRAY 返回 8（指针 decay）；call_args.c L79 添加 static fwd decl for `glue_field_access_layout_field_type_ref_by_name_c`（定义在 field_access.c L111，#include L1684 在 L1660 之后）。**Deps**：所有 static 依赖在 call_args.c #include point L1660 可见（struct_lit L1440 ／ vector_let L1455 ／ var_decl L1610 ／ index_helpers L1530 ／ struct_let L1520 均在 L1660 之前）；g_pipeline_asm_emit_module（glue.c L133 READ-ONLY）；backend_emit_expr{,_method}_call + backend_enc_*_arch + pipeline_asm_emit_set_call_sret_reg_shift_c + glue_arm64_mov_x0_to_x8_elf_c（extern）。**No TU-internal callsites** — sole callers are seeds (backend_call_dispatch.from_x.c L641/972/2540/2644/3783/4008/4123 + backend.x L1282/1610/1626) via extern。G.9 详细英文 docblock 全覆盖。glue 4669→4337（-332 净：迁移注释替换 6 函数+docblocks）；call_args.c 2962→3359（+397 含 G.9 docblocks + 迁移注释 + 3 fwd decls）；block_if_stmt.c 106→174（+68 含 extern fwd decl + G.9 docblock）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· seed smoke OK · examples/hello.x emit + run "Hello World" exit=0。PLATFORM: push_sysv=LINUX+MACOS x86_64 (ta==0) ／ store_sp=MACOS|ARM64 (ta==1) ／ 其余 SHARED。Tip **e41bb1533** |
| 2026-08-02 | **wave1204** · BC 8.3.1 · G.7 migrate pipeline_asm_let_init_stack_reserve_bytes（1 fn → pipeline_asm_emit_block_inits.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 1 个 let-init stack reserve 函数自 `pipeline_glue.c` L1494-1502 迁入 `pipeline_asm_emit_block_inits.c` EOF（与 wave1149 `glue_asm_init_expr_reserve_stack_bytes` 同文件 — 它是 sole non-zero fallback callee）。**根因修复**：原 glue.c L1494 定义在 vector_let.c #include (L2063) 之前，导致 `glue_fixed_array_let_init_uses_direct_slot` 无 fwd decl、依赖 C 隐式声明 — 迁移到 block_inits.c (#include at L2404，在所有依赖 #include 之后) 彻底解决。3 个 static callees 均在 block_inits.c 可见：`glue_vector_let_init_uses_direct_slot`（vector_simd.c L2218）＋`glue_fixed_array_let_init_uses_direct_slot`（vector_let.c L2063）＋`glue_asm_init_expr_reserve_stack_bytes`（block_inits.c EOF 同文件）。Callers：context.c L724/739 (#include L2564 after L2404 — 可见) ＋ ast_pool.c L8497/8511 (#include L3186 after L2404 — 可见) ＋ ast_pool_top_level.c L323 (extern decl L285 保留) ＋ modlet.c L530 (#include L1543 BEFORE L2404 — 需 extern fwd decl at modlet.c L336)。**额外清理**：移除 2 个 dead static fwd decl（`glue_asm_init_expr_reserve_stack_bytes` old L1081 ＋ `glue_vector_let_init_uses_direct_slot` old L1464 — 两者 sole caller 已迁走）。G.9 详细英文 docblock 全覆盖。glue 4670→4669（-1 净：18 行迁移注释替换 9 行函数+9 行 static fwd decls+注释）；block_inits.c 731→787（+56 含 G.9 docblock + 迁移注释）；modlet.c +6（extern fwd decl）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· seed smoke OK · examples/hello.x emit + run "Hello World" exit=0。PLATFORM: SHARED — pure stack byte arithmetic, no arch dependency。Tip **9cb5bb188** |
| 2026-08-02 | **wave1203** · BC 8.3.1 · G.7 migrate pipeline_module_fill_u8_64_from_src_c（1 fn → ast_pool_arena.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 1 个 64 字节批量填充工具函数自 `pipeline_glue.c` L1702-1716 迁入 `ast_pool_arena.c` EOF（与 wave1184 parser_library_init cluster 同文件 — 4 of 5 callsites at L541/560/584/676；第 5 个 caller 在 `pipeline_parser_result.c` L262，via extern fwd decl at L50）。Fwd decl at glue.c L228 同时移除（两个 consumer 文件各有自己的 fwd decl：ast_pool_arena.c L503 same-file ／ pipeline_parser_result.c L50 cross-file）。No glue.c callsites；no static deps — 纯字节拷贝工具（loop + bounds check）。G.9 详细英文 docblock 全覆盖。glue 4678→4670（-8 净：6 行迁移注释替换 15 行函数+1 行 fwd decl）；ast_pool_arena.c 855→898（+43 含 G.9 docblock）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· seed smoke OK · examples/hello.x emit + run "Hello World" exit=0。PLATFORM: SHARED — pure byte copy, no arch dependency。Tip **ac9afa766** |
| 2026-08-02 | **wave1202** · BC 8.3.1 · G.7 migrate pipeline_asm_index_elem_byte_sz public wrapper（1 fn → pipeline_asm_emit_index.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 1 个 public wrapper 自 `pipeline_glue.c` L2475-2478 迁入 `pipeline_asm_emit_index.c` EOF（与 static `pipeline_asm_index_elem_byte_sz_c` at L43 同文件 — wrapper 是该 static 实现的唯一 extern 入口）。Wrapper 纯委托：`pipeline_asm_index_elem_byte_sz(a, idx)` → `pipeline_asm_index_elem_byte_sz_c(a, idx)`，不重复 TYPE_PTR→8／pointee／ARRAY total_bytes／VECTOR lane 逻辑。No glue.c callsites（sole callers are seeds：`backend_try_inline_dispatch*.from_x.c` via extern decl）。No extern fwd decl needed in glue.c — definition visible via same-TU #include at L1556。G.9 详细英文 docblock 全覆盖。glue 4676→4678（+2 净：6 行迁移注释替换 4 行函数）；index.c 435→464（+29 含 G.9 docblock）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· seed smoke 42 OK · examples/hello.x emit + run "Hello World" exit=0。PLATFORM: SHARED。Tip **4ec424230** |
| 2026-08-02 | **wave1201** · BC 8.3.1 · G.7 migrate asm label format public wrappers（2 fns → pipeline_asm_label_format.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 2 个 asm label format public wrappers 自 `pipeline_glue.c` L2655-2702 迁入 `pipeline_asm_label_format.c` EOF（与 wave1102 integer format primitives 同文件，同 TU #include at glue.c L2653）：`pipeline_asm_emit_next_label_c`（emit ".Lf<scope>_<n>" into buf；advances AsmFuncCtx.label_counter per-function unique label；scope from pipeline_elf_label_mod_scope_active disambiguates cross-module collisions）+ `pipeline_asm_format_label_id_c`（format ".L_<id>" into buf；counterpart of backend.x format_label_id；does NOT advance label_counter）。Colocated with wave1102 integer format primitives — these 2 wrappers are the sole consumers of `glue_format_u32_to_buf`／`glue_format_i32_to_buf` (static above in same file since wave1102)，completes asm label format domain fold。所有 static deps 在 #include 点可见：pipeline_asm_ctx_layout static at glue.c L86 + pipeline_glue_AsmFuncCtxLayout struct decl at top + pipeline_elf_label_mod_scope_active extern L836。Extern fwd decl retained at glue.c L835（`pipeline_asm_emit_next_label_c` — called by glue.c L3173 mega_body，after this file's #include at L2653）。G.9 详细英文 docblock 全覆盖。glue 4713→4676（-37 净）；label_format.c 74→164（+90 含 G.9 docblock）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· seed smoke 42 OK · examples/hello.x emit + run "Hello World" exit=0。PLATFORM: SHARED。Tip **11a7ee20e** |
| 2026-08-02 | **wave1200** · BC 8.3.1 · G.7 migrate frame/param/local slots cluster（4 fns → pipeline_asm_emit_context.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 4 个 frame/param/local slots 函数自 `pipeline_glue.c` 迁入 `pipeline_asm_emit_context.c` EOF（与 asm emit context 域同文件，同 TU #include at glue.c L2570）：`pipeline_asm_compute_frame_size_c`（prologue 帧大小计算：params + top-level lets + block locals + array temp + wa temp + slice reent dc + call spill scratch min 512 + 64 pad；16 base 保留 [rbp-8] saved rbx）+ `pipeline_asm_fill_param_slots`（形参 home slot 填入 asm ctx sidecar；>16B MEMORY full-size home；arm64 low-end／x86 high-end）+ `pipeline_asm_emit_param_home_elf_c`（prologue param homing：reg→stack slot；SysV x86 f32 xmm／dual-GP／MEMORY paths；AAPCS64 arm64 low-end dual-GP／MEMORY paths；>16B sret save rdi/x8）+ `pipeline_asm_fill_local_slots`（block const/let slot 填入 asm ctx sidecar；+8 alignment + lit temp reserve）。Colocated with context setters — frame/param/local slots 是 context 的 prologue 消费面（set_module／set_arena／set_func_index）。所有 static deps 在 #include 点可见：g_pipeline_asm_emit_module／_arena／_func_sret_active／_sret_home_off at L132-188；pipeline_asm_ctx_layout static at L86；asm_ctx_local_reset／asm_ctx_fill_locals_block_tree fwd decls L962-963；pipeline_asm_abi_f32_xmm_enabled_c extern L890；pipeline_asm_hoist_target_func_index fwd decl L967；pipeline_asm_sum_module_top_level_lets_stack fwd decl L968；glue_func_param_home_width_c／glue_func_param_agg_byte_size_c／pipeline_asm_emit_param_home_elf_sysv_f32_xmm_c in call_args.c (#include L1656)。Fwd decls retained：pipeline_asm_compute_frame_size_c at L965 (harmless)。Sole callers all in glue.c after #include at L2570（L3145 emit_if_then／L3521 mega_body fill_param／L3558 mega_body frame_size／L3562 mega_body fill_local／L3576 mega_body param_home）。G.9 详细英文 docblock 全覆盖。glue 5136→4713（-423 净）；context.c 259→743（+484 含 G.9 docblock）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42／while sum_n(10)／for loop／add(3,4) OK。PLATFORM: SHARED。Tip **a67a86632** |
| 2026-08-02 | **wave1199** · BC 8.3.1 · G.7 migrate loop emit cluster（7 fns → pipeline_asm_emit_fold_count_up_while.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 7 个 loop emit 函数自 `pipeline_glue.c` 迁入 `pipeline_asm_emit_fold_count_up_while.c` EOF（与 count_up_while fold 域同文件，同 TU #include at glue.c L3395）：`backend_emit_loop_body_content_elf_sync`（loop body emit 编排：block stmts + nested loops + final expr）+ `backend_emit_while_loop_elf_sync`（while loop emit：fold hooks 优先 mem_copy／struct_pair_n2／u8_fill／u8_sum／lcg_xor／simd_peel／count_up；fold 失败回退 generic while）+ `backend_emit_for_loop_elf_sync`（for loop emit：desugar to while）+ `pipeline_asm_emit_while_loop_elf_c`／`pipeline_asm_emit_for_loop_elf_c`／`pipeline_asm_emit_loop_body_content_elf_c`（3 thin wrappers）+ `pipeline_asm_emit_skip_heavy_or_thin_stub_elf_c`（heavy/thin stub skip gate）。Colocated with count_up_while fold 域 — loop emit 是 fold 消费者，迁入同文件后 fold hooks + generic emit 同域管理。所有 static deps 在 #include 点可见：spill.c L1533（glue_loop_break_exit_push/pop + glue_asm_cache_invalidate_at_cfg_merge_selective + glue_asm_loop_phi_invalidate_carried_defs + glue_asm_loop_merge_live_union + glue_live_fwd_apply_expr_effect）；unary.c L1319（glue_enc_jz_after_bool_in_eax）；array_lit.c L1551 + expr_rec.c L1689（pipeline_asm_emit_expr_elf_rec static def）；glue_try_fold_*／backend_try_fold_count_up_while_elf 在 fold_count_up_while.c 本文件上方；pipeline_asm_ctx_layout static at glue.c L86。Fwd decls retained：backend_emit_* at L999-1006（before block_body.c #include at L2427）；pipeline_asm_emit_next_label_c extern at L835；backend_ensure_block_local_slots extern at L837。Sole caller of skip_heavy_or_thin_stub：mega_body at L3830（after this file's #include at L3395 — visible）。G.9 详细英文 docblock 全覆盖。glue 5415→5100（-315 净）；fold_count_up_while.c +370（含 G.9 docblock）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42／while sum_n(10)／for loop OK。PLATFORM: SHARED。Tip **9f5eb1829** |
| 2026-08-02 | **wave1198** · BC 8.3.1 · G.7 migrate typeck call-check cluster（2 fns → pipeline_typeck_check_expr.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 2 个 extern 函数自 `pipeline_glue.c` 迁入 `pipeline_typeck_check_expr.c` EOF（同 TU #include at L5444，after struct_lit.c L1438 + vector_simd.c L1509 + ast_pool.c L3985 + method_call.c L5307 — all same-TU static deps visible）：`pipeline_typeck_call_arg_repr_compatible_ok_c`（MOD-02: `*StructA` vs `*StructB` coercion under `#[repr(compatible)]` + same field shape；single authority for positive coerce）+ `pipeline_typeck_check_extern_call_unsafe_boundary_c`（LANG-007 S0: extern calls must be inside `unsafe { }`）。Fwd decl 管理：`call_arg_repr_compatible_ok_c` — extern fwd decl added in `method_call.c` L435（before L2734 callsite in `typeck_check_call_ptr_struct_compat_c` < check_expr.c #include at L5444）；`check_extern_call_unsafe_boundary_c` — fwd decl at glue.c L5021 retained（before L5043 callsite in `check_call_slice_region_c` which stays in glue.c — dual-authority seed file）；`glue_module_func_index_by_name_c` static fwd decl removed from glue.c（no remaining callers in glue.c body；static def in vector_simd.c L856 visible to check_expr.c via #include at L1509）。G.9 详细英文 docblock 全覆盖。glue 5496→5415（-81 净）；check_expr.c 1130→1275（+145 含 G.9 docblock）；method_call.c +6（extern fwd decl）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2。PLATFORM: SHARED。Tip **40e6fb17a** |
| 2026-08-02 | **wave1197** · BC 8.3.1 · G.7 migrate struct layout sizing cluster（3 fns → ast_pool_struct_layout.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 3 个函数自 `pipeline_glue.c` 迁入 `ast_pool_struct_layout.c` EOF（同 TU #include via ast_pool.c L1615，#include'd into glue.c at L3985 — after struct_lit.c L1438 where `glue_type_size_simple`／`glue_type_align_simple`／`glue_type_is_empty_struct_c` are defined as statics；definitions visible）：`pipeline_asm_type_ref_byte_size_c`（type_ref byte size；delegates to `glue_type_size_simple` with `g_pipeline_asm_emit_module`）+ `pipeline_struct_layout_next_field_offset_ex`（§11.1 aligned field offset；packed path + align(N) path）+ `pipeline_struct_layout_next_field_offset`（wrapper；align_req=0）。Colocated with `pipeline_module_struct_layout_*` accessors — completes struct layout sizing domain。Sole in-TU caller was `_offset→_ex`（internal，moved together）；all other callers are extern（typeck.x／parser.x／backend_call_dispatch seed）。G.9 详细英文 docblock 全覆盖。glue 5550→5496（-54 净）；ast_pool_struct_layout.c 385→500（+115 含 G.9 docblock）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2。PLATFORM: SHARED。Tip **e215c678c** |
| 2026-08-02 | **wave1196** · BC 8.3.1 · G.7 migrate ast_ast_arena_init/alloc cluster + implicit_tail_expr_disallowed_by_glue（7 fns → ast_pool_arena.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 7 个函数自 `pipeline_glue.c` 迁入 `ast_pool_arena.c` EOF（同 TU #include via ast_pool.c L886）：wave1196a `ast_expr_layout_prime_call_resolved`（1 fn；空 C 端 hook，供 ast.x 链接）+ `ast_ast_arena_init`（1 fn；zero arena counters；被 ast_pool.c L1278 strict parse 调用，在 ast_pool_arena.c L886 #include 之后定义可见）+ `ast_ast_arena_type/expr/block/func_alloc`（4 fns；delegates to pipeline_arena_*_alloc L66-99）。wave1196b `implicit_tail_expr_disallowed_by_glue`（1 fn；检查 expr kind 是否 RETURN/PANIC/BREAK/CONTINUE；原调用 `glue_arena_expr_kind_at_ref`（static glue.c L434）；迁移版内联相同逻辑 via `pipeline_arena_expr_ptr` L41）。Callers: `ast_pool_block.c` L1599（via `ast_ast_expr_disallows_implicit_tail` wrapper）+ `check_block.c` L677 + 跨 TU seeds。`glue_arena_expr_kind_at_ref`（static）保留 glue.c — 仍被 `pipeline_expr_kind_ord_at` L2335 调用。G.9 详细英文 docblock 全覆盖。glue 5574→5507（-67 净）；ast_pool_arena.c +142；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2。PLATFORM: SHARED。Tip **45b0a6b3f** |
| 2026-08-02 | **wave1195** · BC 8.3.1 · G.7 migrate ast_ref_is_null + call expected ret ty + array lit elem type ref（4 fns + 1 static var → ast_pool_arena.c + call_args.c + array_lit.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 4 个函数 + 1 个 static var 自 `pipeline_glue.c` 迁入 3 个域文件 EOF：wave1195a `ast_ref_is_null`（1 fn → `ast_pool_arena.c` EOF；trivial arena ref 验证器 `ref==0`；被 21 个文件调用）。wave1195b `pipeline_asm_set/call_expected_ret_ty_c`（2 fns + 1 static var `g_pipeline_asm_call_expected_ret_ty` → `pipeline_asm_emit_call_args.c` EOF；call expected return type 追踪，CALL emit 期设置，field_access/struct_let emit 读取；static var 改为 call_args.c file-scoped；glue.c L170 保留 extern fwd decl 供 struct_let.c L1539 前置调用）。wave1195c `pipeline_asm_array_lit_elem_type_ref`（1 fn → `pipeline_asm_emit_array_lit.c` EOF；array lit elem type_ref 解析 TYPE_ARRAY/TYPE_SLICE→elem_type_ref；glue.c L662 保留 extern fwd decl 供 as.c L1345/vector_let.c L1476 前置调用）。G.9 详细英文 docblock 全覆盖。glue 5591→5574（-17 净，实际 -55 行函数体替换为 17 行 extern 声明 + 迁移注释）；ast_pool_arena.c +18；call_args.c +45；array_lit.c +52。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2。PLATFORM: SHARED。Tip **aa3fd2315** |
| 2026-08-02 | **wave1194** · BC 8.3.1 · G.7 migrate ast_ast_arena type/expr get/set + parser helpers + std_io_driver stubs（10 fns → ast_pool_arena.c + pipeline_parse_orch.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 10 个函数自 `pipeline_glue.c` 迁入 2 个域文件 EOF（同 TU #include）：wave1194a `ast_ast_arena_type_get/set` + `ast_ast_arena_expr_get/set`（4 fns → `ast_pool_arena.c` EOF；colocated with wave1183 block/func get/set + ast_arena_* twin forwarders。前置声明替换为实际定义；ast_arena_* twins 现委托同文件定义。expr_set 保留 trace debug 逻辑（与 pipeline_arena_expr_set_copy 重复，留待后续 dedup 清理））。wave1194b `std_io_driver_submit_read/write_batch_buf`（2 fns → `pipeline_parse_orch.c` EOF；batch I/O 转发到 io.o）。wave1194c `pipeline_expr_ref_is_assign_lvalue`（1 fn → parse_orch.c EOF；parser lvalue 检查 VAR/INDEX/DEREF/FIELD_ACCESS）。wave1194d `compound_assign_token_to_expr_kind_from_glue`（1 fn → parse_orch.c EOF；TokenKind→ExprKind 复合赋值映射）。`glue_arena_expr_kind_at_ref`（static）保留 glue.c — 被 `implicit_tail_expr_disallowed_by_glue` + `pipeline_expr_kind_ord_at` 也调用；在 parse_orch.c #include 前定义故同 TU 可见。G.9 详细英文 docblock 全覆盖。glue 5679→5591（-88 净）；ast_pool_arena.c +111；parse_orch.c 873→973（+100）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2。PLATFORM: SHARED。Tip **e57dda82f** |
| 2026-08-02 | **wave1193** · BC 8.3.1 · G.7 migrate driver diagnostic + sizeof + check_only/skip cluster（24 fns → pipeline_parse_orch.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 24 个 extern 转发函数自 `pipeline_glue.c` L316-L544 迁入 `pipeline_parse_orch.c` EOF（与 wave1186 parse/load/typeck orchestration 域同文件）。4 个子簇：parser_diagnostic（4 fns：parser_parse_strict_enabled + parse_skip + parse_commit_fail + parse_func_generic）+ sizeof（5 fns：pipeline_sizeof_arena/module XLANG_WEAK + sizeof_dep_ctx + sizeof_onefunc_result + arena_offset_num_types）+ debug/diagnostic（4 fns：pipeline_debug_module_funcs + driver_get_module_num_funcs + driver_get_module_main_func_index + driver_diagnostic_entry_module）+ driver forwarding（11 fns：typeck_/pipeline_ driver_diagnostic_after_entry_parse + pipe_marker + pipeline_entry_source_len + check_only + shu_pipeline_check_only + typeck_skip_large_entry + asm_build_skip_typeck + x_pipeline_skip_typeck + driver_diagnostic_entry_block_after_parse）。排除 2 个有 #ifdef guard 的函数（pipeline_run_x_pipeline + pipeline_sizeof_elf_ctx）保留在 glue.c。7 个 driver_* extern 声明添加到 parse_orch.c L497-503。driver_asm_build_skip_typeck extern 声明保留在 glue.c（struct_lit.c L1027/L1071 在 parse_orch.c #include 前调用）。G.9 详细英文 docblock 全覆盖。glue 5838→5679（-159 净）；parse_orch.c 503→873（+370 含 docblock）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2。PLATFORM: SHARED。Tip **c1b21215b** |
| 2026-08-02 | **wave1191-1192** · BC 8.3.1 · G.7 migrate func_body + import resolution clusters（3 fns → check_block.c EOF + 3 fns → method_call.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 两个函数簇自 `pipeline_glue.c` 迁入已有域文件 EOF（同 TU #include）：wave1191 func_body implicit return tail cluster（3 fns → `pipeline_typeck_check_block.c` EOF）：`pipeline_typeck_func_body_tail_expr_ref_for_implicit_rule_c`（W-tail 分析：final RETURN/PANIC/BREAK/CONTINUE wins → peel trailing unsafe region → fall through to final expr/expr_stmt）+ `pipeline_typeck_func_body_has_implicit_return_tail_c`（判定块末是否存在不允许的隐式尾返回，递归检查 EXPR_BLOCK 内部 block）+ `pipeline_typeck_patch_all_body_parent_links_c` (XLANG_WEAK cold fallback for pure pipeline_abi)。Colocated with check_block walker 域 — func_body tail 分析是 block typeck 子域。wave1192 import resolution cluster（3 extern fns → `pipeline_typeck_method_call.c` EOF）：`pipeline_typeck_import_segment_at_c`（split import path into '.'-separated segments）+ `pipeline_typeck_resolve_dep_index_for_import_c`（map entry import slot → dep ctx slot by path）+ `pipeline_typeck_resolve_whole_import_call_ret_c`（resolve `platform.elf.fn(args)` qualified import callee return type via field_access chain walk + import path segment match）。Colocated with method_call 域 — qualified import call resolution 是 method-call target resolution 子域。添加 3 个 extern fwd decl 在 method_call.c L82-88 供 L178/2946/2981 callsites 在 EOF 定义前调用。XLANG_WEAK `patch_all_body_parent_links_c` 保留 weak 定义供产品纯代码强符号覆盖。G.9 详细英文 docblock 全覆盖。glue 6162→5838（-324 净）；check_block.c +177 行；method_call.c +287 行；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2。PLATFORM: SHARED。Tip **ec633e312** |
| 2026-08-02 | **wave1190** · BC 8.3.1 · G.7 migrate typeck check_expr try_propagate + call cluster（2 fns + 1 wrapper → pipeline_typeck_check_expr.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 2 个 check_expr 入口函数 + 1 个 wrapper 自 `pipeline_glue.c` 迁入 `pipeline_typeck_check_expr.c` EOF（与 wave1188 entry/dispatch 域 + wave1189 子类簇同文件）：`pipeline_typeck_check_expr_try_propagate_c`（ERR-01 Result `?` 传播）+ `typeck_check_expr_try_propagate` wrapper + `pipeline_typeck_check_expr_call_c`（EXPR_CALL typeck pipeline：unsafe boundary → arg → resolve → arity → arg_types → generic gate → slice region → return-type resolve + fixup）。try_propagate_c 原在 method_call.c #include 前（需 debug_try_propagate_report_glue_c 静态前向声明）；迁入 check_expr.c EOF 后在 method_call.c #include 之后，定义（wave1147 method_call.c EOF）直接可见，静态前向声明已删除。call_c 原在 method_call.c #include 后（依赖 method_call.c EOF 中的 glue_generic_call_fixup_resolved_type_c (wave1096) + pipeline_typeck_check_call_generic_type_args_c (wave1099) 静态函数）；迁入 check_expr.c EOF 后这两个静态函数仍可见（method_call.c #include 在 check_expr.c #include 之前）。添加 2 个 extern fwd decl（try_propagate_c／call_c）供 check_expr.c 内 impl_mega_c／check_expr_c 在定义前调用。typeck_check_expr_call wrapper 保留在 glue.c（通过 fwd decl 调用）。G.9 详细英文 docblock 全覆盖。glue 6298→6162（-136 净）；check_expr.c +230 行；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2。PLATFORM: SHARED。Tip **b6a9a7f47** |
| 2026-08-02 | **wave1189** · BC 8.3.1 · G.7 migrate typeck check_expr sub-class cluster（8 fns + 3 match helpers + 2 match statics → pipeline_typeck_check_expr.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 8 个 check_expr 子类函数 + 3 个 match 辅助函数 + 2 个 match 静态变量自 `pipeline_glue.c` 迁入 `pipeline_typeck_check_expr.c` EOF（同 TU #include，与 wave1188 entry/dispatch 域同文件）：check_expr sub-class cluster（`pipeline_typeck_check_expr_panic_c`／`_match_c`／`_return_c`／`_unary_c`／`_addr_of_c`／`_deref_c`／`_index_c`／`_var_c`）+ match subject helpers（`pipeline_typeck_match_set_subject_c`／`_clear_subject_c`／`_subject_field_type_c` + `g_typeck_match_subject_{ty,mod}` statics，sole users of those statics）。Colocated with wave1188 entry/dispatch 域 — 这 8 个子类函数被 `pipeline_typeck_check_expr_impl_mega_c`／`impl_c` 按 ExprKind 分派调用（panic/match/return/deref/var 直接调用，unary/addr_of/index 通过 typeck_check_expr_* extern wrapper 间接调用），迁入同文件后 impl_mega_c／impl_c 可见定义。`g_typeck_unsafe_depth` 静态变量保留在 `pipeline_glue.c`（与 `pipeline_typeck_check_block.c` 同 TU #include 共享）。添加 4 个 extern fwd decl（match_c／return_c／deref_c／var_c）在 #include 点前（impl_mega_c／impl_c 在定义前调用）。全 extern（非 static）：跨 TU 调用（typeck_x.o／typeck.o／seeds）。G.9 详细英文 docblock 全覆盖。glue 6834→6298（-536 净）；check_expr.c +1645 行（含 G.9 docblock）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2。PLATFORM: SHARED。Tip **45ab5667d** |
| 2026-08-02 | **wave1188** · BC 8.3.1 · G.7 migrate typeck check_expr entry/dispatch cluster（5 fns + 2 XLANG_WEAK → pipeline_typeck_check_expr.c new file） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 5 个 extern 函数 + 2 个 XLANG_WEAK 符号自 `pipeline_glue.c` L6768-6945 迁入新域文件 `pipeline_typeck_check_expr.c`（同 TU #include）：typeck check_expr entry/dispatch cluster（`pipeline_typeck_check_expr_impl_mega_c`（full ExprKind dispatch table）／`check_expr_impl_mega` (XLANG_WEAK glue-only fallback)／`pipeline_typeck_check_expr_impl_c`（simple kind C path + mega fallback）／`check_expr_impl` (XLANG_WEAK glue-only fallback)／`pipeline_typeck_check_expr_c`（boundary check → try_propagate / impl_c））。Colocated check_expr dispatch 入口 — 这 5 个函数构成 expr typeck 的分派面（check_expr → impl_c → impl_mega_c → sub-class helpers），XLANG_WEAK 定义保留供产品纯代码（typeck_x.o／typeck.o）强符号覆盖。子类 helper（panic/match/return/unary/addr_of/deref/index/var）仍在 glue.c（与 assign/soa/field_access 域切片交错）；call_c + try_propagate_c 仍在 glue.c（在 method_call.c #include 前依赖 debug_try_propagate_report_glue_c + glue_module_func_index_by_name_c）。全 extern（非 static）：跨 TU 调用（typeck_x.o／typeck.o／seeds）。G.9 详细英文 docblock 全覆盖。glue 6997→6834（-163 净）；新文件 265 行；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs · Ubuntu 需 `-fno-builtin-bcmp -fno-builtin-memcmp -fno-builtin-memset -U_FORTIFY_SOURCE` workaround）· smoke 42/3/2。PLATFORM: SHARED。Tip **c5fef1de6** |
| 2026-08-02 | **wave1187** · BC 8.3.1 · G.7 migrate typeck orchestration cluster（14 fns + 2 statics + 3 XLANG_WEAK → pipeline_typeck_orch.c new file） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 10 个 typeck 编排入口函数 + 2 个静态变量 + 3 个 XLANG_WEAK 符号自 `pipeline_glue.c` 迁入新域文件 `pipeline_typeck_orch.c`（同 TU #include）：typeck orchestration cluster（`pipeline_typeck_x_ast_check_one_func_c`／`pipeline_typeck_x_ast_impl_c`／`pipeline_typeck_x_ast_library_c`／`pipeline_typeck_x_ast_c` + `g_pipeline_typeck_diag_soft_suppress` static + `pipeline_typeck_diag_soft_suppress_set/get` (XLANG_WEAK) + `g_pipeline_typeck_dep_ctx_cold` static + `pipeline_typeck_set_dep_ctx/get_dep_ctx` (XLANG_WEAK) + `pipeline_typeck_dep_prerun_module_c` (XLANG_WEAK) + `typeck_validate_struct_layouts_zero_padding_glue` + `typeck_x_type_size_from_layout_glue`／`typeck_x_type_align_from_layout_glue`）。同时 4 个 codegen/backend struct_lit field offset/store_sz forwarder 迁入 `pipeline_asm_emit_expr_rec.c` EOF。XLANG_WEAK 定义保留供产品纯代码强符号覆盖。全 extern（非 static）：跨 TU 调用（driver.x／lsp.x／runtime_pipeline_abi.x／seeds）。glue 7300→6997（-303 净）；新文件 433 行；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2。PLATFORM: SHARED。Tip **a7f3dbd72** |
| 2026-08-02 | **wave1186** · BC 8.3.1 · G.7 migrate pipeline parse/load/typeck orchestration cluster（24 fns → pipeline_parse_orch.c new file） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 24 个函数自 `pipeline_glue.c` 迁入新域文件 `pipeline_parse_orch.c`（同 TU #include）：parse/load/typeck orchestration cluster（`pipeline_parse_into_buf_impl_c`／`pipeline_parse_into_buf_c`／`pipeline_load_import_from_disk_impl_c`／`pipeline_load_import_from_disk_c`／`pipeline_sync_dep_slots_from_driver_impl_c`／`pipeline_sync_dep_slots_from_driver_c`／`pipeline_parse_into_with_init_buf_impl_c`／`pipeline_parse_into_with_init_buf_c`／`pipeline_parse_into_buf`／`pipeline_load_import_from_disk`／`pipeline_sync_dep_slots_from_driver`／`pipeline_resolve_path_x`／`pipeline_read_file_x`／`pipeline_parse_into_with_init_c`／`pipeline_lsp_diag_parse_typeck_buf_impl_c`／`pipeline_lsp_diag_parse_typeck_buf_c`／`pipeline_lsp_diag_parse_entry_buf_impl_c`／`pipeline_typeck_after_parse_ok_impl_c`／`pipeline_typeck_after_parse_ok_c`／`pipeline_typeck_after_parse_ok_buf_impl_c`／`pipeline_typeck_x_stack_escape_gate_from_src_c`／`pipeline_lsp_diag_parse_typeck_buf` (XLANG_WEAK standalone)／`pipeline_typeck_after_parse_ok` (XLANG_WEAK standalone)／`pipeline_parse_into_with_init_buf` (XLANG_WEAK standalone)）。Colocated parse/load/typeck 编排链 — 这 24 个函数构成 pipeline 主编排路径（parse_into_buf → load_import → sync_dep → typeck_after_parse_ok），其中 `pipeline_parse_into_buf`／`pipeline_load_import_from_disk`／`pipeline_sync_dep_slots_from_driver`／`pipeline_typeck_after_parse_ok`／`pipeline_lsp_diag_parse_typeck_buf` 是 XLANG_WEAK standalone 入口，产品纯代码（`runtime_pipeline_abi.x`）可强符号覆盖；`_impl_c`／`_c` 后缀为 cold path 实现保留供 pipeline.x thin / non-PREFER 路径 weak dispatch。XLANG_WEAK 定义保留在 `pipeline_parse_orch.c`，迁移不改变 ABI 链。`pipeline_parse_orch.c` 加必要 extern fwd decls（`pipeline_sizeof_arena`／`pipeline_sizeof_module`／`pipeline_strict_parse_into_init`／`pipeline_debug_trace_named_func_bodies`／`pipeline_module_fixup_with_arena_stmt_orders`／`parser_parse_into_init`／`parser_parse_into_set_main_index`／`pipeline_typeck_scan_module_struct_stack_escape_c`／`driver_diagnostic_typeck_fail`／`xlang_trait_reg_reset_c`／`xlang_generic_bound_stash_source_buf_c`／`xlang_trait_check_impls_complete_c`）。全 extern（非 static）：跨 TU 调用（driver.x／lsp.x／runtime_pipeline_abi.x／seeds）。G.9 详细英文 docblock 全覆盖。glue 7718→7300（-418 净）；新文件 503 行；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs · Ubuntu 需 `-fno-builtin-bcmp -fno-builtin-memcmp` workaround 预存环境问题 · wave1185 同样存在）· smoke 42/3/2。PLATFORM: SHARED。Tip **a92c24ec9** |
| 2026-08-02 | **wave1185** · BC 8.3.1 · G.7 migrate parser result copy/lex/slice helper cluster（20 fns + 4 typedefs → pipeline_parser_result.c new file） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 20 个 extern 函数 + 4 个 glue-local typedefs 自 `pipeline_glue.c` 迁入新域文件 `pipeline_parser_result.c`（同 TU #include）：parser result 拷贝／lex 提取／slice 构造 helper cluster（`parser_slice_from_buf`／`lexer_parser_slice_from_buf`／`pipeline_source_slice`／`parser_lexer_pos_before`／`parser_lex_from_lexer_result_ptr_into`／`parser_lex_from_lexer_result_val_into`／`parser_lex_copy_from_collect_imports`／`parser_lex_from_onefunc_result_ptr_into`／`parser_lex_from_extern_parse_result_ptr_into`／`pipeline_parser_extern_parse_set_fail_c`／`pipeline_parser_library_result_copy_into_c`／`pipeline_parser_try_skip_result_copy_into_c`／`parser_lex_from_try_skip_result_val_into`／`parser_lex_from_library_result_val_into`／`pipeline_parser_set_onefunc_fail_c`／`pipeline_parser_onefunc_buf_into_set_success_c` + 4 typedefs `pipeline_glue_ExternParseResult`／`pipeline_glue_LibraryParseResult`／`pipeline_glue_TrySkipAllowResult`／`pipeline_glue_OneFuncResult`）。Colocated with parser result 消费域 — 这些 helper 被 `pipeline_glue.c` 内 parser library path（`parse_one_function_library`／`parse_one_extern_and_add_into`／`collect_imports`）调用，将 parser.x 产出的 result struct 拷贝／lex 提取／slice 构造为 C 可消费形式。X `out.name[ni]=` INDEX ASSIGN 会 asm fail 故 set_fail/set_success 用 C 实现。`pipeline_parser_result.c` L8 加 extern fwd decl for `pipeline_module_fill_u8_64_from_src_c`（定义在 `pipeline_glue.c` L229 < #include 故可见）。全 extern（非 static）：跨 TU 调用（parser.x／ast_pool.c／seeds）。G.9 详细英文 docblock 全覆盖。glue 7924→7718（-206 净）；新文件 269 行；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2。PLATFORM: SHARED。Tip **1e07d1852** |
| 2026-08-02 | **wave1184** · BC 8.3.1 · G.7 migrate pipeline_parser_library_init cluster（8 fns → ast_pool_arena.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 8 个 extern 函数自 `pipeline_glue.c` 迁入 `ast_pool_arena.c` EOF（同 TU #include）：`pipeline_parser_library_init_bool_type_c`／`_named_type_c`／`_var_expr_c`／`_field_access_expr_c`／`_enum_variant_expr_c`／`_eq_expr_c`／`_labeled_block_c` + `pipeline_parser_extern_init_arena_func_and_register_c`。Colocated with `pipeline_arena_{type,expr,block,func}_ptr` 域 wave987 — 这些函数初始化 arena type/expr/block/func 槽位供 parser library path（parse_one_function_library／parse_one_extern_and_add_into）使用，全部委托 `pipeline_arena_{type,expr,block,func}_ptr` 取槽位。`ast_pool_arena.c` 加 11 个 extern fwd decl for `pipeline_module_fill_u8_64_from_src_c`／`ast_pipeline_block_append_labeled`／`pipeline_module_func_*` 等（定义在 `ast_pool_block.c`／`ast_pool_module_func.c`／`pipeline_glue.c` 后续 #include）。全 extern（非 static）：跨 TU 调用。G.9 详细英文 docblock 全覆盖。glue 8097→7924（-173 净）；ast_pool_arena.c +224（含 G.9 docblock + fwd decls）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2。PLATFORM: SHARED。Tip **bf998319c** |
| 2026-08-02 | **wave1183** · BC 8.3.1 · G.7 migrate ast_ast_block + ast_pipeline_arena + ast_pipeline_expr forwarder clusters（97 fns → 3 domain files EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 97 个 extern 函数自 `pipeline_glue.c` 迁入 3 个已有域文件 EOF（同 TU #include）：wave1183a `ast_ast_block_*` cluster（31 fns ＝ `ast_ast_arena_patch_block_parent_links` + 20 个 getters（`num_consts/lets/loops/for_loops/if_stmts/regions/expr_stmts/stmt_order` + `region_body_ref` + `stmt_order_kind/idx` + `const/let init/type_ref` + `expr_stmt_ref` + `final_expr_ref`）+ 11 个 control flow 函数（`while/for/if cond/body/init/step/then/else ref` + `resolve_var_to_type_ref` + `disallows_implicit_tail` + `apply_call_resolve`）→ `ast_pool_block.c` EOF；colocated with `pipeline_block_*` append/region/defer/stmt_order 域 wave988-994）。wave1183b `ast_pipeline_arena_*` cluster（12 fns ＝ `ast_pipeline_arena_{type,expr,block,func}_{get,set}_copy` + `ast_pipeline_arena_expr_write_{var,binop}` → `ast_pool_arena.c` EOF；colocated with `pipeline_arena_*` 域 wave987）+ `ast_ast_arena_*_get/set` + `ast_arena_*_get/set` twin cluster（14 fns ＝ `ast_ast_arena_{block,func}_{get,set}` + `ast_arena_{type,expr,block,func}_{get,set}` twins delegating to `ast_ast_` → `ast_pool_arena.c` EOF；colocated with arena get/set 域）。wave1183c `ast_pipeline_expr_*` cluster（40 fns ＝ `call/method_call/match/struct_lit/array_lit/index/field_access/line/col/lit/var/unary/binop/null` accessors + `codegen/backend prefix` forwarders → `pipeline_asm_emit_expr_rec.c` EOF；colocated with `pipeline_asm_emit_expr_elf_*` recursion dispatcher 域 wave1020）。`ast_pool_arena.c` 加 4 extern fwd decl for `ast_ast_arena_type/expr_get/set`（定义在 `pipeline_glue.c` L5179-5224 含 trace debugging 逻辑，仅迁移声明不迁移实现）。`pipeline_asm_emit_expr_rec.c` 加 11 extern fwd decl for `pipeline_expr_append_call_arg/append_method_call_arg/append_match_arm/prepare_call_arg_slot/on_call_created` 等（定义在 `ast_pool_expr_sidecar.c` 后续 #include）。全 extern（非 static）：跨 TU 调用（typeck.x／ast_pool.c／seeds）。G.9 详细英文 docblock 全覆盖（用途／依赖／Contract／PLATFORM）。glue 8410→8097（-313 净）；3 域文件 +492（含 G.9 docblock + fwd decls）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2 · Ubuntu gate 全绿（while/typeck/option/import/compound-assign/multi-file/struct/return/return-expr/pool-limits/fmt/types/check）。PLATFORM: SHARED。Tip **46252f8d0** |
| 2026-08-02 | **wave1182** · BC 8.3.1 · G.7 migrate ast_pipeline forwarder cluster（246 fns → pipeline_ast_forwarders.c new file） | **代码** · **不升钉** · **L2 双端**。G.7 批量迁移 246 个 ast_pipeline_* forwarder 函数 + 1 个 utility（pipeline_copy_lib_root_to_buf256）+ 1 个 ast_ast_pool_onefunc_reset 自 `pipeline_glue.c` 迁入新域文件 `pipeline_ast_forwarders.c` EOF（同 TU #include）。Sub-clusters: module_func（11 fns）/ dep_ctx（26 fns）/ ctx_lib_root（4 fns）/ scratch_buf（7 fns: 64/96/128/256 + slots）/ codegen（20 fns: type_kind/vector/skip_emit/force_param/use_buf_wrapper/io_driver/lsp_io）/ elf_ctx（2 fns: append_patch/reloc）/ block（30 fns: append/fill/const/let/if/while/for/stmt_order/labeled/resolve_var）/ module（30 fns: import/enum/struct_layout/top_level_let）/ onefunc（20 fns: num_consts/lets/whiles/fors + append + sidecar）/ arena（8 fns: type/expr/block/func cap+alloc）。全 extern（非 static）纯转发：ast.x codegen 带 ast_ 前缀调用，ast_pool.c 实现无前缀 C 符号。Chinese section comments translated to English per G.9。glue 9014→8410（-604 净）；新文件 666 行；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2 · Ubuntu gate 全绿。PLATFORM: SHARED。Tip **b37e3c767** |
| 2026-08-02 | **wave1175-1181** · BC 8.3.1 · G.7 batch migrate field access + return + coerce + asm emit context + elf/codegen forwarder clusters（32 fns → 6 domain files + 2 new files） | **代码** · **不升钉** · **L2 双端**。G.7 批量同域 fold 32 个函数自 `pipeline_glue.c` 迁入 6 个域文件 EOF + 2 个新域文件（同 TU #include）：wave1175-1178 typeck field access + asm emit return + coerce init + ast_pool dep_ctx/module_func leaf clusters（14 fns → pipeline_asm_emit_field_access.c + pipeline_asm_emit_return.c + pipeline_typeck_coerce_init.c + ast_pool_dep_ctx.c + ast_pool_module_func.c EOF）；wave1179 pipeline_expr_enum_field_tag_via_module + pipeline_expr_enum_namespace_field_tag 迁入 pipeline_asm_emit_field_access.c EOF（2 fns · fwd decl for enum_field_tag_via_module）；wave1180 新建 `pipeline_asm_emit_context.c`（14 fns ＝ asm emit context setters/getters cluster：module/dep_pipe/arena/call_param_type_ref/call_arg_depth/func_index/elf_ctx · static globals 留 glue.c 因 pipeline_asm_emit_field_access.c 直接访问）；wave1181 新建 `pipeline_elf_codegen_forwarders.c`（18 fns ＝ elf/codegen prefix forwarder cluster：platform_elf_pipeline_elf_ctx_reloc_* 12 fns + codegen_codegen_out_buf_*/pipeline_codegen_out_buf_* 4 fns + codegen_pipeline_scratch_buf64* 2 fns · 纯转发无状态）。G.9 详细英文 docblock 全覆盖。glue 9818→9014（-804 净）；6 域文件 +1080 / 2 新文件 +468；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2 · Ubuntu gate 全绿（while/typeck/option/import/compound-assign/multi-file/struct/return-expr/pool-limits/fmt/types/check）。PLATFORM: SHARED。Tip **d1d20417b** |
| 2026-08-02 | **wave1167-1170** · BC 8.3.1 · G.7 batch migrate region scope + dep return type + func resolution + overload wrapper clusters（16 fns → region_assign.c + method_call.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 批量同域 fold 16 个 extern 函数自 `pipeline_glue.c` 迁入 2 个已有域文件 EOF（同 TU #include）：wave1167 region scope + scan_module + read_ptr + stamp_let cluster（7 extern fns ＝ `pipeline_dep_ctx_scope_region_push_c`／`_pop_c`／`_len_at`／`pipeline_typeck_scan_module_struct_stack_escape_c`／`pipeline_typeck_check_allocator_region_assign_c`／`pipeline_typeck_check_return_slice_region_in_scope_c`／`pipeline_typeck_stamp_let_region_label_c` + 3 statics ＝ `g_typeck_region_saved_len/saved_label/scope_n` + `TYPECK_REGION_SCOPE_MAX` macro → `pipeline_typeck_region_assign.c` EOF，#include @ L8373；colocated with `typeck_scan_block_stack_escape_c` wave1137 — region scope push/pop/len + module-level stack-escape scan + allocator region assign + return slice region + let stamp 全部属于 region-assign 域；glue.c L5610 加 fwd decl for scan_module 供 callsites L5645/5656/5703 在 #include 前；region_assign.c L327-329 加 3 fwd decls for push/pop/len 供 callsites L409/416/1112/1121/1128）；wave1168 dep return type + entry module cluster（3 extern fns ＝ `pipeline_typeck_set_entry_module_for_dep_map_c`／`pipeline_typeck_dep_return_type_to_caller_arena_c`／`pipeline_typeck_get_dep_return_type_in_caller_arena_c` + 1 static `g_typeck_entry_module_for_dep_map` → `pipeline_typeck_method_call.c` EOF；colocated with `resolve_call_callee_return_type_c` wave1155 — dep return type mapping twin of callee return type resolver；glue.c L770 加 fwd decl for get_dep_return_type）；wave1169 func resolution cluster（4 extern fns ＝ `pipeline_typeck_expr_var_name_equal_func_c`／`pipeline_typeck_find_func_return_type_in_module_by_name_c`／`pipeline_typeck_find_func_return_type_in_module_c`／`pipeline_typeck_expr_apply_call_resolve_c` → method_call.c EOF；colocated with overload resolution 域 wave1089-1094；method_call.c L66-72 加 fwd decl for `expr_apply_call_resolve_c` 供 callsites L200/218/307/2942/2988/3008/3033/3059 在 EOF 定义 L3508 前）；wave1170 overload wrapper cluster（2 extern fns ＝ `pipeline_typeck_resolve_call_func_index_for_emit_c`／`pipeline_typeck_pick_overload_func_index_for_call_c` → method_call.c EOF；colocated with overload scoring — emit-time pick wrappers delegate to static impls）。全 extern（非 static）：跨 TU 调用（typeck.x／ast_pool.c／seeds）。G.9 详细英文 docblock 全覆盖。glue 10137→9818（-319）；region_assign +221 / method_call +319（含 G.9 docblock + fwd decl）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED。Tip **c1d12dc44** |
| 2026-08-02 | **wave1163-1165** · BC 8.3.1 · G.7 migrate module_func/struct_lit/ret_coerce clusters（12 fns → 3 域文件 EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 12 个 extern 函数自 `pipeline_glue.c` 迁入 3 个已有域文件 EOF（同 TU #include）：wave1163 module_func name/body reader cluster（5 fns ＝ `pipeline_module_func_name_write`／`_name_copy64`／`_name_len_at`／`_is_extern_at`／`_body_ref_at` → `ast_pool_module_func.c` EOF；colocated with module_func accessor 域）；wave1164 struct_lit accessor cluster（4 fns ＝ `pipeline_expr_struct_lit_num_fields`／`_type_name_len`／`_type_name_into`／`_type_name_set` → `pipeline_asm_emit_struct_lit.c` EOF；colocated with STRUCT_LIT emit 域；`glue_arena_expr_at_ref` 经 static fwd decl in pipeline_asm_emit_as.c 可见）；wave1165 ret coerce cluster（3 fns ＝ `pipeline_typeck_return_operand_matches_c`／`pipeline_typeck_ret_coerce_integral_to_expect_i32_c`／`pipeline_typeck_ret_coerce_integral_widen_c` → `pipeline_asm_emit_return.c` EOF；colocated with return emit 域 wave1130-1131 float promote pair）。全 extern（非 static）。G.9 详细英文 docblock 全覆盖。glue 10251→10137（-114）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2。PLATFORM: SHARED。Tip **90ec6283b** |
| 2026-08-02 | **wave1160-1162** · BC 8.3.1 · G.7 batch migrate expr accessor clusters（33 fns + 12 wrappers → expr_rec.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 批量同域 fold 33 个 extern 函数 + 12 个 `ast_pipeline_*` wrappers（45 total）自 `pipeline_glue.c` 迁入 `pipeline_asm_emit_expr_rec.c` EOF（同 TU #include）：wave1160 if/as/match/const_folded/enum_variant/block accessor cluster（10 fns + 9 wrappers）；wave1161 index/field_access/line/col accessor cluster（10 fns + 3 wrappers）；wave1162 lit/var/field_access/unary/binop/null accessor cluster（13 fns）。Colocated with expr ELF recursion dispatcher 域。`glue_arena_expr_at_ref` 经 static fwd decl 可见。全 extern（非 static）。G.9 详细英文 docblock 全覆盖。glue 10551→10251（-300）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2。PLATFORM: SHARED。Tip **5f1fd93b1** |
| 2026-08-02 | **wave1159** · BC 8.3.1 · G.7 extern wrappers migrate call_resolve + method_call accessor cluster（13 fns → method_call.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 13 个 extern 函数自 `pipeline_glue.c` 迁入 `pipeline_typeck_method_call.c` EOF（同 TU #include @ L9703）：call_resolve cluster（8 fns ＝ `pipeline_expr_init_call_resolve_at_ref`／`pipeline_expr_apply_call_resolve`／`pipeline_expr_ptr_init_call_resolve`／`ast_pipeline_expr_ptr_init_call_resolve`／`ast_pipeline_expr_init_call_resolve_at_ref`／`ast_pipeline_expr_apply_call_resolve`／`pipeline_expr_call_resolved_dep_index_at`／`pipeline_expr_call_resolved_func_index_at` · L1018-1084）+ `pipeline_expr_call_callee_ref_at`（L3623）+ method_call accessor cluster（4 fns ＝ `pipeline_expr_method_call_base_ref_at`／`pipeline_expr_method_call_num_args_at`／`pipeline_expr_method_call_name_len`／`pipeline_expr_method_call_name_into` · L3647-3676）。Colocated with method_call typeck 域 — 所有 call-resolution write/read API + EXPR_METHOD_CALL field accessors 消费者均在 method_call.c。method_call.c 顶部加 9 个 extern fwd decl（L54-64）供文件内 callsites L79-2823 在 EOF 定义前可见；glue.c L1020 加 3 个 extern fwd decl 供 callsites L8635/9416/9417 在 #include L9703 前可见；`pipeline_expr_call_callee_ref_at` fwd decl 已存在 glue.c L1568。全 extern（非 static）：跨 TU 调用（typeck.x／ast_pool.c／seeds）。G.9 详细英文 docblock 全覆盖。glue 10643→10551（-92）；method_call 3052→3232（+180）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED。Tip **d79f08eb3** |
| 2026-08-02 | **wave1158** · BC 8.3.1 · G.7 extern wrappers migrate type_refs_equal／bool／expr_type_ref cluster（9 fns → coerce_init.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同域 fold 9 个 extern 函数自 `pipeline_glue.c` 迁入 `pipeline_typeck_coerce_init.c` EOF（同 TU #include）：`pipeline_typeck_type_refs_equal_named_c`／`pipeline_typeck_type_refs_equal_same_kind_c`／`pipeline_typeck_resolve_type_alias_ref_c`／`pipeline_typeck_type_refs_equal_c`／`pipeline_typeck_type_refs_equal_impl_c`／`pipeline_typeck_type_ref_is_bool_impl_c`／`pipeline_typeck_type_ref_is_bool_c`／`pipeline_typeck_expr_type_ref_impl_c`／`pipeline_typeck_expr_type_ref_c`（L7319-7423）。Colocated with wave1080-1083 static implementations — public wrappers delegate to static impls in same file。coerce_init.c 顶部加 2 extern fwd decl 供 callsites L277/816 在 EOF 定义前可见；glue.c L7356 加 9 extern fwd decl 供 callsites 在 #include 前可见。全 extern（非 static）。G.9 详细英文 docblock 全覆盖。不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· smoke 42/3/2。PLATFORM: SHARED。Tip **571fbc556** |
| 2026-08-02 | **wave1155-1157** · BC 8.3.1 · G.7 fold 3 clusters（call return-type + diag fmt + linear · 12 fns → 3 域文件 EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同叶 fold 12 个函数自 `pipeline_glue.c` 迁入 3 个已有域文件 EOF（同 TU #include）：wave1155 `pipeline_typeck_resolve_call_callee_return_type_c`（CALL/METHOD_CALL callee 返回类型 resolver · 4 路解析：whole-import call → import binding FIELD_ACCESS → local module find_func_return_type → dep scan · ~167 LOC → `pipeline_typeck_method_call.c` EOF，#include @ L10186；colocated with `resolve_call_func_index_c` wave1085-1094 — twin of call-target resolver pair（func_index + return_type）；sole glue.c callsite at L10353 在 #include 后故可见；deps 含 `GLUE_TYPECK_IMPORT_BINDING/SELECT` enum @ L2241 < #include）；wave1156 typeck diag fmt cluster（5 fns ＝ `diag_append_lit_c`／`diag_append_u32_dec_c`／`diag_fmt_type_at_c`／`diag_fmt_type_into_c`／`diag_fmt_type_or_question_c` · ~145 LOC → `pipeline_typeck_assign.c` EOF，#include @ L8265；colocated with assign mismatch diag 域 — 8 callsites in assign.c lines 265-336；sole glue.c callsite at L8142 在 #include 前故 glue.c L8013 加 extern fwd decl；assign.c 顶部加 2 extern fwd decl 供 callsites L265-336 在 EOF 定义前可见）；wave1157 linear type use-once move tracking cluster（6 fns ＝ `set_active_ctx_c`／`active_module_c`／`linear_reset_c`／`linear_use_var_c`／`linear_accepts_init_c`／`reject_addr_of_linear_c` · ~125 LOC → `pipeline_typeck_check_block.c` EOF，#include @ L10250；colocated with `typeck_linear_name_already_moved` wave1132 + check_block walker 域；wave1132 static fwd decl 删除（sole caller `linear_use_var_c` 现同文件 — direct call）；glue.c L6099 加 extern fwd decl 供 `set_active_ctx_c` callsite @ L6109 在 #include 前可见；`linear_reset_c` callsites @ L10267/10331/10398 在 #include 后故可见；globals `g_typeck_linear_moved_{n,names,lens}` + `g_typeck_active_ctx` 留 glue.c — 经 #include 可见）。**extern 函数迁移启动**：3 wave 中 4 个函数为 extern（非 static），跨 TU 调用（typeck.x／ast_pool.c／seeds）；通过 extern fwd decl 维持可见性。G.9 详细英文 docblock 全覆盖（用途／依赖／Caller／Invariant／PLATFORM）。COUNT 仍 62；glue 11126→10633（-493 净；含 docblock）；method_call +217 / assign +207 / check_block +183（含 G.9 docblock）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED。Tip **a4176904b** |
| 2026-08-02 | **wave1152-1154** · BC 8.3.1 · G.7 fold scope setter + dead code cleanup（1 fn → block_body.c EOF · 2 dead fn 删除） | **代码** · **不升钉** · **L2 双端**。G.7 同叶 fold + 死代码清理：wave1152 `glue_asm_ctx_set_scope_block`（3 LOC，block scope setter → `pipeline_asm_emit_block_body.c` EOF，#include @ L3623；colocated with block_body_sync 域 — 全部 13 callsites 在 block_body.c（3）／block_if_stmt.c（4，#include L3626 > 3623）／fold_count_up_while.c（5，#include ~5199 > 3623）／glue.c:5121（> 3623）；static fwd decl at block_body.c:53 供内部 callsites lines 295/700/865 在 EOF 定义前可见；deps `g_pipeline_asm_emit_scope_block`（L182）+ `asm_ctx_set_scope_block`（L191）均在 #include L3623 前声明故可见）；wave1153 删除 `parser_diagnostic_parse_commit_shape`（~21 LOC，static 壳函数委托 extern `driver_diagnostic_parse_commit_shape`；glue.c TU 内零调用 — 被 `runtime_driver_diagnostic.x` export 函数独立链接取代）；wave1154 `#if 0` 注释 `glue_emit_block_stmt_order_let_const_elf`（~134 LOC，glue.c TU 内零调用 — 被 `backend_emit_loop_body_content_elf_sync` + `block_body_sync_elf` 同路径取代）。**glue.c 静态叶迁移基本收口**：剩余 5 个 static 定义均为基础设施级（`pipeline_asm_ctx_layout` 59 callsites/20+ 域文件 · `glue_arena_expr_kind_at_ref` 3 callsites 全在 glue.c · `glue_var_expr_stack_off_elf_c` 100+ callsites/8 域文件 · `glue_arena_expr_at_ref` 83 callsites/5 域文件 · `glue_type_kind_from_ord` 4 callsites 全在 glue.c），跨多域调用不适合单域迁移。G.9 详细英文 docblock 全覆盖。COUNT 仍 62；glue 11137→11126（-11 净；含 #if 0 块保留）；block_body 1053→1089（+36）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED。Tip **1fb1a15b7** |
| 2026-08-02 | **wave1150-1151** · BC 8.3.1 · G.7 fold call-target resolver + f32 xmm param homing（2 fns → call_args.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同叶 fold 2 个静态函数自 `pipeline_glue.c` 迁入 `pipeline_asm_emit_call_args.c` EOF（同 TU #include @ L2251）：wave1150 `glue_asm_resolve_call_target_module_c`（CALL/METHOD_CALL 目标 module/func_index/dep_ix resolver · 5 路解析：pre-resolved func_index → typeck resolve_call_func_index → import binding METHOD_CALL（math.floor）→ import binding FIELD_ACCESS（CALL+FIELD_ACCESS 形）→ linear dep scan；是 call-arg packing 域的 dispatch twin — 所有 call-arg emitter 调此 resolver 取 mod/func_ix/dep_ix；`GLUE_TYPECK_IMPORT_BINDING/SELECT` enum 自 glue.c L8947 前移至 L2241（call_args.c #include L2251 前）使迁移函数可见；新增 extern fwd decl 7 个：`pipeline_typeck_find_func_return_type_in_module_c`（def glue.c L8926 > #include）／`_by_name_c`／`parser_get_module_num_imports`／`pipeline_expr_method_call_name_len/into`／`pipeline_module_import_kind_at/binding_name_len/binding_name_byte_at`；新增 static fwd decl `pipeline_typeck_resolve_call_func_index_c`（def method_call.c #include @ L10491 > call_args.c #include @ L2251）；struct_let.c:78 static fwd decl 注释更新指向新位置）；wave1151 `pipeline_asm_emit_param_home_elf_sysv_f32_xmm_c`（SysV x86 gp/xmm split-track param homing · XLANG_ABI_F32_XMM=1 · callee-side twin of call-arg packing · 直接消费 glue_func_param_agg_byte_size_c（wave1045）／glue_func_param_home_width_c（wave1047）／glue_func_param_is_f32_c／glue_func_param_is_f64_c（wave1059，全在 call_args.c 同叶）；sole caller `pipeline_asm_emit_param_home_elf_c` 在 glue.c L4742 > #include L2251 故无需 static fwd decl）。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl。G.9 详细英文 docblock 全覆盖（用途／依赖／Caller／Invariant／PLATFORM）。COUNT 仍 62；glue 11866→11137（-729 含 wave1146-1149）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED + LINUX+MACOS x86_64 SysV（f32 xmm split-track x86-only）。Tip **cda5f6429** |
| 2026-08-02 | **wave1146-1149** · BC 8.3.1 · G.7 fold 4 leaves（TypeKind tag／debug reporter／bootstrap fixup／block locals tree） | **代码** · **不升钉** · **L2 双端**。G.7 同叶 fold 4 个静态函数自 `pipeline_glue.c` 迁入 3 个已有域文件 EOF（同 TU #include）：wave1146 `pipeline_asm_typekind_variant_tag`（37 LOC，TypeKind name→tag 字节比较表 → `pipeline_asm_emit_cmp.c` EOF，#include @ L3547；glue.c L1958 保留 static fwd decl 供 field_access.c（#include L2281）+ cmp.c（#include L3547）callsites 在 #include 前可见；纯 memcmp buf scan 无 deps）；wave1147 `debug_try_propagate_report_glue_c`（37 LOC，debug-point D reporter → `pipeline_typeck_method_call.c` EOF，#include @ L10525；glue.c L10377 加 static fwd decl 供 sole caller `pipeline_typeck_check_expr_try_propagate_c` @ L10452 在 #include 前可见；deps: link_abi_getenv/link_abi_system extern + libc）；wave1148 `pipeline_typeck_bootstrap_expr_fixup_c`（37 LOC，METHOD_CALL i32.double fixup + generic CALL mono fallback → method_call.c EOF；colocated with glue_generic_call_fixup_resolved_type_c wave1096 同文件；glue.c L8131 保留 static fwd decl 供 sole caller `pipeline_typeck_check_expr_return_c` @ L8191 在 #include 前可见）；wave1149 `pipeline_asm_fill_block_locals_tree`（21 LOC）+ `glue_asm_init_expr_reserve_stack_bytes`（9 LOC）→ `pipeline_asm_emit_block_inits.c` EOF，#include @ L3617；`g_pipeline_asm_emit_module` 直接写替换为 `pipeline_asm_emit_set_module()` public setter（G.7 单权威）；block_inits.c 加 extern fwd decl for setter（def glue.c L4247 > #include）；glue.c L1672 加 static fwd decl for `glue_asm_init_expr_reserve_stack_bytes` 供 sole caller @ L2106 在 #include 前。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl。G.9 详细英文 docblock 全覆盖（用途／依赖／Caller／Invariant／PLATFORM）。COUNT 仍 62；不改语义；不升钉。mac L2：pipeline_x · g05 pure-ld（mac 60 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED。Tip **56447fd9f** |
| 2026-08-02 | **wave1141-1145** · BC 8.3.1 · G.7 fold fixed array helpers cluster + call ptr compat check（5 fns → vector_let.c EOF + method_call.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同叶 fold 5 个静态函数自 `pipeline_glue.c` 迁入 2 个已有域文件 EOF（同 TU #include）：wave1141-1144 fixed TYPE_ARRAY local let helpers cluster（4 fns ＝ `glue_type_is_fixed_array`／`glue_emit_fixed_array_type_let_init_elf_c`／`glue_block_let_is_fixed_array_type`／`glue_fixed_array_let_init_uses_direct_slot` → `pipeline_asm_emit_vector_let.c` EOF，#include @ L2063；colocated with `glue_struct_lit_store_fixed_array_field_elf_c`（vector_let.c L347，被 glue_emit_fixed_array_type_let_init_elf_c 调用）；`GLUE_TYPE_KIND_ARRAY` 宏替换为字面量 10 — 宏定义在 glue.c L2076 晚于 vector_let.c #include L2063 故不可见，宏保留在 glue.c 供 struct_let/index_helpers/spill/modlet/assign/array_lit/index/vector_simd/block_inits/field_access（均 #include 在 L2076 后）使用；与 vector_let.c 现有 L59/64/101/113/123/205/213/245/359 使用字面量 45/46 风格一致）；wave1145 `typeck_check_call_ptr_struct_compat_c`（MOD-02 *Struct 形参 vs 实参兼容性检查 → `pipeline_typeck_method_call.c` EOF，#include @ L10585；colocated with overload resolution 域 wave1089-1094 + import binding name 域 wave1085-1088；sole caller `pipeline_typeck_check_call_slice_region_c` 在 glue.c L10313 早于 #include 故 glue.c L10212 加 static fwd decl）。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl。G.9 详细英文 docblock 全覆盖（用途／依赖／Caller／Invariant／PLATFORM）。COUNT 仍 62；glue 11886→11866（-20）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED. Tip **d76801d5f** |
| 2026-08-02 | **wave1138-1140** · BC 8.3.1 · G.7 fold spill byte cluster（3 fns + 3 macros → asm_emit_spill.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同叶 fold 3 个静态函数 + 3 个宏自 `pipeline_glue.c` 迁入 `pipeline_asm_emit_spill.c` EOF（同 TU #include @ L2181）：wave1138 `glue_asm_sum_expr_call_spill_bytes`（递归 expr walker · CALL(48)／METHOD_CALL(49) 每 reg-class arg 32B 无回收 · binop/unary/index/field/if/array_lit/struct_lit 递归 · visit cap 65536）；wave1139 `glue_sum_block_slice_reent_dc_bytes_c`（迭代块遍历 · stack[256] · const/let/expr_stmt/final/while/for/if/region 子节点 · visit cap 65536）；wave1140 `glue_asm_sum_block_call_spill_bytes`（迭代块遍历 · stack[256] · const/let/expr_stmt/final/if/while/for 子节点 · 经 glue_asm_sum_expr_call_spill_bytes 汇总 expr spill）。3 宏：`GLUE_ASM_CALL_SPILL_SLOT_BYTES` (32)／`GLUE_ASM_FRAME_WALK_VISIT_MAX` (65536)／`GLUE_ASM_FRAME_WALK_DEPTH_MAX` (256)。Colocated with 7.3 Chaitin spill 域（pipeline_asm_emit_spill.c）— 所有 frame-size 估算逻辑单文件。Caller `pipeline_asm_compute_frame_size_c` 仍留 glue.c L4807。glue.c 原定义 + 宏删除（L4498-4805）+ 迁移标记 L4498。spill.c 加 extern fwd decl for 跨 TU 不可见符号：`ast_pipeline_block_if_cond_ref`（定义 ast_pool.c）／`pipeline_expr_method_call_*`（定义 glue.c 后部）／`pipeline_block_while/for/region_*`（定义 ast_pool.c，glue.c L4503-4510 亦有 fwd）。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl。G.9 详细英文 docblock 全覆盖（用途／依赖／Caller／PLATFORM）。COUNT 仍 62；glue 12044→11886（-158）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED. Tip **5ed985ed9** |
| 2026-08-02 | **wave1136-1137** · BC 8.3.1 · G.7 fold stack-escape scan pair（scan_expr + scan_block → region_assign.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同叶 fold 2 个静态函数自 `pipeline_glue.c` 迁入 `pipeline_typeck_region_assign.c` EOF（同 TU #include @ L9934）：wave1136 `typeck_scan_expr_stack_escape_c`（单 expr assign/call/return 栈逃逸扫描 · dispatches by expr kind → check_struct_stack_escape_assign／check_scope_borrow_assign／check_allocator_region_assign／check_scope_borrow_return／check_allocator_region_return／check_return_slice_region_in_scope／check_return_slice_region／check_call_struct_stack_escape — 全部定义在 region_assign.c 同域）；wave1137 `typeck_scan_block_stack_escape_c`（递归块扫描 · walk expr_stmts + final_expr + stmt_order: while/for/if-then-else/region · region 处理镜像 check_block_one_region: with_arena cap push/pop + labeled scope_region push/pop + unsafe_depth push/pop Cap-T001）。scan_block 递归 → static fwd decl at L933（定义 at L1038）。公共入口 `pipeline_typeck_scan_module_struct_stack_escape_c`（glue.c L10056）保留在 glue.c — 直接操作 globals `g_typeck_with_arena_scope_n`／`g_typeck_region_scope_n`；经 #include L9934 可见 scan_block。glue.c 原定义删除 + 迁移标记（L10010 wave1136 marker · L10051 wave1137 marker）。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl。G.9 详细英文 docblock 全覆盖（用途／契约／Invariant／PLATFORM）。COUNT 仍 62；glue 12044→11886（-158）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED. Tip **e2040c99a** |
| 2026-08-02 | **wave1130-1135** · BC 8.3.1 · 批量 G.7 fold 8 leaves（3 域：float promote pair／linear name moved／lval param ptr field cluster） | **代码** · **不升钉** · **L2 双端**。G.7 批量同叶 fold 8 个静态函数自 `pipeline_glue.c` 迁入 3 个已有域文件 EOF（同 TU #include）：wave1130-1131 float promote pair（2 fns ＝ `glue_maybe_promote_f32_to_f64_rax_elf_c`／`glue_float_promote_src_ty_ref_c` → `pipeline_asm_emit_return.c` EOF，#include @ L1913；colocated with wave314 return float-widen callsites L542/543/630/631/640/641；return.c L58-62 加 3 static fwd decl 含 `pipeline_typeck_float_widen_ok_c`（定义在 coerce_init.c #include @ L11099 晚于本叶 #include）；glue.c L1906-1909 + L1929-1932 冗余 fwd decl 删除（return.c 自带）；`GLUE_EXPR_KIND_VAR` 宏替换为字面量 3 — 宏定义在 glue.c L2085 晚于 return.c #include L1913，与现有 return.c L123/171 风格一致）；wave1132 linear name moved（1 fn ＝ `typeck_linear_name_already_moved` → `pipeline_typeck_check_block.c` EOF，#include @ L11606；colocated with check_block walker 域 — linear use-once move 跟踪由 `pipeline_typeck_linear_use_var_c` L9805 在 check_expr/check_block VAR 读取 TYPE_LINEAR 时查询；globals `g_typeck_linear_moved_{n,names,lens}` @ glue.c L9745-9747 早于 #include 故可见；glue.c L9790 加 1 static fwd decl 供 L9805 callsite 在 #include 前可见）；wave1133-1135 lval param ptr field cluster（5 fns ＝ `typeck_lval_is_param_ptr_field_c`／`typeck_block_expr_stmts_store_scan_c`／`typeck_block_final_expr_store_scan_c`／`typeck_block_stores_param_into_param_field_c`／`typeck_func_stores_param_into_param_field_c` → `pipeline_typeck_region_assign.c` EOF，#include @ L9934；colocated with WPO-S3 stack-escape helpers wave1125-1129（同 assign-site escape 分析路径）；region_assign.c L141 callsite 加 1 static fwd decl at file top L37；`typeck_block_stores_param_into_param_field_c` 递归加 1 static fwd decl at L707；`pipeline_typeck_ptr_for_addr_of_operand_c`（glue.c L9919-9931）保留在 glue.c（非 cluster 成员）。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl。G.9 详细英文 docblock 全覆盖（用途／契约／Invariant／PLATFORM）。COUNT 仍 62；glue 12235→12044（-191）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED. Tip **b96af367f** |

***
