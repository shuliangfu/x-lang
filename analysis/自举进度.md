# Xlang / X language 自举进度

> **更新**：2026-09-02 · tip **7.4.9 typeck pin println 复合实参**（SHA 见 §6）· 钉盘仍 **`e8176cbe5`**（**不升钉**；双端 L4 **未重跑**）· 日切：31 日 **113** 行已入 [归档](自举进度-归档-2026-08-31.md) · **金标 SSH＝`ubuntu-server`** · Ubuntu 验＝**git pull（禁 scp）** · Ubuntu L2 **钉盘节奏**\
> **产品 L4 放行钉盘**：**`e8176cbe5`**（2026-08-23 升钉 · 前 `f7424ae47` 2026-08-15 · 再前 `e364f4a37` 2026-08-11）\
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅**（12 labi_*.x 切片 prefer；默认 FROM_X=1）· parser **7.2.2 关 pin ✅**（tip `-E` + assemble；产品默认 FROM_X=1）· codegen 7.4.2 wave323 ✅ · typeck 7.4.1 wave322 ✅ · runtime monofile wave321 ✅ · present **0** · 钉盘 **`e8176cbe5`** · Stage 8 tip **wave332 Track L 30/30 FULLY CLOSED**（**PRODUCT HALF=0** · **PRODUCT PINNED=0** · **PRODUCT RETIRED 23/23 = 100%**）· **7.4.3 PREFER_X_O 双端 8/8 wave338 ✅** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁**\
> **tip L4 安全网（历史）**：**`ffc0b646f`**（已由 tip L4 升钉吸收；前网 `4bacaf18c`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：**7.4.9** pin／empty_surface 补 `typeck_call_is_fmt_debug_print_any`（Darwin pin 链 print_any／fmt-std **6／6**／L2 **5／5**）· 双端 L4 未重跑 · 钉盘仍 **`e8176cbe5`**。
> **下波候选**：双端 `./xbuild l4`；绿才升钉。Darwin FMT001／run-check 次刀。

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1；过程上 prefer+heat 一次／域，双端矩阵同波一次\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；**SHARED 双端 L2**；日常矩阵 **只** `./xbuild l2-matrix`（wave299：捕获 rv=42／opt=102 成功码，禁 set -e 截断）；谈自举／升钉才 L4 + 矩阵 + 全量 bstrict；bstrict **per-script timeout=300s** + catalog cache；**墙钟**：`product_l4` 分步 wall + `run-all-bstrict`（`XLANG_BSTRICT_TIMING_FILE`）；并行 **`XLANG_BSTRICT_JOBS` 默认 1**（Ubuntu 可 opt-in 2–4；Darwin 慎用 OOM）

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[31](自举进度-归档-2026-08-31.md) · [30](自举进度-归档-2026-08-30.md) · [29](自举进度-归档-2026-08-29.md) · [28](自举进度-归档-2026-08-28.md) · [27](自举进度-归档-2026-08-27.md) · [26](自举进度-归档-2026-08-26.md) · [25](自举进度-归档-2026-08-25.md) · [24](自举进度-归档-2026-08-24.md) · [23](自举进度-归档-2026-08-23.md) · [22](自举进度-归档-2026-08-22.md) · [21](自举进度-归档-2026-08-21.md) · [20](自举进度-归档-2026-08-20.md) · [19](自举进度-归档-2026-08-19.md) · [18](自举进度-归档-2026-08-18.md) · [17](自举进度-归档-2026-08-17.md) · [16](自举进度-归档-2026-08-16.md) · [15](自举进度-归档-2026-08-15.md) · [14](自举进度-归档-2026-08-14.md) · [13](自举进度-归档-2026-08-13.md) · [12](自举进度-归档-2026-08-12.md) · [11](自举进度-归档-2026-08-11.md) · [09](自举进度-归档-2026-08-09.md) · [08](自举进度-归档-2026-08-08.md) · [07](自举进度-归档-2026-08-07.md) · [06](自举进度-归档-2026-08-06.md) · [05](自举进度-归档-2026-08-05.md) · [04](自举进度-归档-2026-08-04.md) · [03](自举进度-归档-2026-08-03.md) · [02](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · …\

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `e8176cbe5`**（2026-08-23 升钉 · 前 `f7424ae47` 2026-08-15 · 再前 `e364f4a37` 2026-08-11）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**双端 Stage2 = ✅ SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709` · `xlang_asm_stage1`／`xlang_asm2` bit-identical）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（**变更记录只在本文 §6**）。\
**本波主刀 = 7.4.9 typeck pin println 复合实参（G.7 孪生 typeck.x）**。

| 指标 | 状态 |
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`e8176cbe5`**；residual tip present **0**；Stage 8 **30/30 FULLY CLOSED**；PRODUCT RETIRED 23/23 |
| Track MG | **11.3.1 MG ✅** |
| Track BC | **🟢 Stage8 30/30** · M4 **5/5** · present **0**；`pipeline_x` mega 仍 host-cc |
| Track L | **收 30/30 FULLY CLOSED** |
| mac / Ubuntu L4（钉盘） | ✅ 双端 L4 + bstrict **129** @ **`e8176cbe5`** |
| pin 蛋 | ✅ darwin.arm64 + linux.x86_64 |
| 本波构建级别 | 钉盘 **L4 已结＠e8176cbe5**；tip **7.4.9** Darwin L2 pin 链绿；双端 L4 未重跑；check 闸门暂停 |

***

## 1. 文档地图

| 文档 | 看什么 |
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录**（**9 月 1–2 日**） |
| [自举进度-归档-2026-08-31.md](自举进度-归档-2026-08-31.md) | **8月31日** stage10 开刀／seed dest-SLICE／leftover／假权威诚实等（**113** 行） |
| [自举进度-归档-2026-08-30.md](自举进度-归档-2026-08-30.md) | **8月30日** leftover XLANG fallthrough F-06／F-07 等（**111** 行） |
| [自举进度-归档-2026-08-29.md](自举进度-归档-2026-08-29.md) | **8月29日**（**139** 行） |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图（本波不改） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单；per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`e8176cbe5`** | 2026-08-23 升钉；双端 L4 真冷 + 129 |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自钉盘 L4 `xlang_asm` 刷新 |
| residual tip（BC） | present **0** | Stage 8 30/30 · M4 5/5 · prefer pure-asm 默认 · pipeline_abi mega **硬禁** |
| mac 钉盘 L4 | ✅ L4 + bstrict **129** · 85m0s | `@e8176cbe5` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 + bstrict **129** | `ubuntu-server` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | rv42／opt102／hello0／si0／f32 |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ 双端 tip SHA256 STRICT match | 钉盘历史见归档 |
| **R2 / Cap** | 按需 | 明细见 29／31 归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1** |
| `TREE_ARMED` / `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1** / **1** |
| post-delete residual | **✅** wave944–964 |

***

## 3. 当前前排（≤3 条）

1. **7.4.9 pin println 复合实参已落盘** — 权威 `typeck.x` `typeck_call_is_fmt_debug_print_any`；pin／empty_surface 孪生补全。Darwin Track L `cc` **0**／354960B · pin 链 print_any JSON · fmt-std **6／6** · L2 **5／5**。双端 L4 **未重跑**。钉盘仍 **`e8176cbe5`**。**禁升钉／禁切 Windows**。
2. **邻域禁区** — **禁** mega prefer／默认 assemble parser.x；**禁** `rm` hybrid egg；**禁** nest>64；**禁**改 al06 期望／整棵 check／改 print_any 期望糊绿；pipeline_abi mega **硬禁**。
3. **下一刀** — 双端 `./xbuild l4`（矩阵＋bstrict）。Darwin FMT001／run-check 次刀。

***

## 4. 与归档的关系

- **8月31日归档**：[自举进度-归档-2026-08-31.md](自举进度-归档-2026-08-31.md) — **113** 行 · 钉盘仍 **`e8176cbe5`**。
- **8月30日及前**：见各日归档链接（§1）。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `e8176cbe5`**。
- 日切后主文档只保留 KPI + 前排 + **9 月 1–2 日** §6；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
| **macOS arm64** | **7.4.9** Darwin L2 pin 链 print_any／矩阵 **5／5**；tip L4 未重跑（前红＠**`d9903f8e8`** FMT001）· 钉盘仍 **`e8176cbe5`** |
| **Ubuntu x86_64（金标）** | **7.4.9** 未金标重跑（前红＠**`d9903f8e8`** fmt-std T001）· SSH＝**`ubuntu-server`** · 钉盘仍 **`e8176cbe5`** |
| **Windows MSYS2** | wave940 一键冷启动绿 · 10.1.3 NT **暂缓**（无 Windows 金标宿主） |

***

## 6. 变更记录（倒序 · **9 月 1–2 日**；31 日见 [归档](自举进度-归档-2026-08-31.md)）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-02 | **fix(typeck)** **7.4.9 pin println 复合实参 JSON any** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`typeck.x` 已有 `typeck_call_is_fmt_debug_print_any`，pin 孪生无；存储＝`typeck_gen.linux.x86_64.c` `check_call_arg_types` score＜0 后；消费＝L4 g05 链上 typeck（L2 prefer `.x` 藏债）。**修**：G.7 补全 pin／empty_surface（std.fmt／std.debug 1 参 print／println 复合不 T001；无第三 scorer）。**证**：Darwin Track L `cc -c` **0**／354960B · T helper · pin 链 `print_any` compile／run JSON · fmt-std **6／6** · overload／print_scalar compile **0** · L2 矩阵 **5／5**。禁改 print_any 期望。残：双端 L4 未重跑。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED pin C。下一刀：双端 `./xbuild l4`。 |
| 2026-09-01 | **test(selfhost)** **双端 L4＠d9903f8e8（不升钉）** | **真冷** · remaining_o=0 · 双端 phase1／g05／矩阵 **5／5 OK**（pabi inject skip 生效；`let _` 不再 duplicate）。Ubuntu bstrict **fmt-std T001 argument type mismatch**（`fmt.println(Point／i32[3]／Option)`；pin typeck vs `.x`）。Darwin bstrict **run-check.sh FMT001 write failed**（fmt-wrap）。日志 `/tmp/xlang_l4_Linux_d9903f8e8.log`／`/tmp/xlang_l4_Darwin_d9903f8e8.log`。**禁升钉／禁 Windows**。下一刀：pin overload／any 对齐 typeck.x。 |
| 2026-09-01 | **fix(selfhost)** **Darwin pabi inject 幂等（g05 reent 双强符号）** | **代码＋文档** · **不升钉**。五问：产生＝g05 对已注入 MH_OBJECT 再 overlay reent-thin；Darwin `ld -r` 失败后 libtool 两成员均 T；`-force_load` 双拉。存储＝`ensure_host_cc_seed_o.sh` inject。消费＝g05 pure-ld xlang。**修**：G.7 补全既有 inject（thin 的 T 已在 base 则 skip）。PLATFORM: MACOS。下一刀：双端 L4。 |
| 2026-09-01 | **fix(typeck)** **7.4.8 pin `let _`／`const _` 不 T001** | **代码＋文档** · **不升钉**。五问：产生＝`typeck.x` 已豁免 discard `_`，pin 孪生无；存储＝`typeck_gen.linux.x86_64.c` `check_block_one_{let,const}`；消费＝L4 g05 链上 typeck（L2 prefer `.x` 藏债）。**修**：G.7 补全 pin（len==1 && byte 95 跳过 redecl）。证：Ubuntu＠**7f2754d80** fmt-std `print_any` T001。禁改测试期望。PLATFORM: SHARED pin C。下一刀：双端 L4。 |
| 2026-09-01 | **test(selfhost)** **双端 L4＠7f2754d80（不升钉）** | **真冷** · remaining_o=0 · 双端 phase1 **OK** · Ubuntu g05 **OK**／矩阵 **5／5**／bstrict **fmt-std T001**（17m）· Darwin g05 pabi duplicate `glue_slice_let_reent_deep_copy_after_dual_gp_elf_c`。日志 `/tmp/xlang_l4_Linux_7f2754d80.log`／`/tmp/xlang_l4_Darwin_7f2754d80.log`。**禁升钉／禁 Windows**。 |
| 2026-09-01 | **fix(typeck)** **7.4.7 archaeology patch 幂等（g05 重复定义）** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝7.4.5 pin 已有 `g_typeck_allow_legacy_extern_calls` 但无 `XLANG_ALLOW_LEGACY_EXTERN` 标记；`patch_typeck_gen_lang007.py` 只认该注释 → 再插一份；存储＝host-local `typeck_gen.c`；消费＝L4 g05 `cc typeck_gen.c`。**修**：G.7 补全既有 patcher（符号已在则 skip）＋pin 补标记。**证**：Darwin patch「already ok」· count=4 · `cc -c` **0**／353888B。残：双端 L4 未重跑。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED pin C。下一刀：双端 `./xbuild l4`。 |
| 2026-09-01 | **test(selfhost)** **双端 L4＠7dc2ef0ea phase1 OK／g05 红（不升钉）** | **真冷** · remaining_o=0 · phase1 pure-ld **OK**（UNDEF 消）· g05 typeck archaeology **redefinition**。日志 `/tmp/xlang_l4_Linux_7dc2ef0ea.log`／`/tmp/xlang_l4_Darwin_7dc2ef0ea.log`。**禁升钉／禁 Windows**。PLATFORM: SHARED 冷链。下一刀：7.4.7。 |
| 2026-09-01 | **fix(selfhost)** **L4 phase1 UNDEF：simd 孪生＋glue 始终导出＋Darwin realpath** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝① `simd_enc.x` 有 hsum／dot／fsub、cold seed 无（pin 蛋编不了 tip `.x`）② try_inline full.x 仅 `export extern "C"`、FROM_X rest H=0 跳过定义、`#define` 吃公共名 ③ Darwin POSIX `realpath` 无 `stdlib.h`；存储＝`simd_enc.from_x.c`／`backend_try_inline_dispatch.from_x.c`／`xlang_path_cap.h`；消费＝L4 phase1 pure-ld。**修**：G.7 补全既有 seed（SSE lanes==4 孪生＋thin forward）／FROM_X 尾始终 `_impl`＋非 THIN 公共包装／POSIX `#include <stdlib.h>`。**证**：Darwin `cc -c` simd **T** fsub／hsum／dot · try_inline cold／FROM_X **T** `glue_asm_ctx_module_ref_c` · THIN 仅 `_impl` · realpath 探针 **0**。残：双端 L4 未重跑。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED 冷链。下一刀：双端 `./xbuild l4`。 |
| 2026-09-01 | **test(selfhost)** **双端 L4 红＠93595ea81（不升钉）** | **真冷** · remaining_o=0 · 7.4.6 typeck Track L **OK** · bootstrap phase1 pure-ld **FAIL**。UNDEF：`glue_asm_ctx_module_ref_c`（try_inline `.x` extern；seed `#define` 把定义吃成 `_impl`）／`simd_enc_try_hw_vector_{hsum_f32x4,dot_f32x4,fsub}_rbp`（权威 `simd_enc.x` 有、cold seed `simd_enc.from_x.c` 无；pin 蛋编不了 tip `.x` 故走 seed）。Darwin 另：`xlang_path_cap.h` POSIX `realpath` 无声明。日志 `/tmp/xlang_l4_Linux_93595ea81.log`／`/tmp/xlang_l4_Darwin_93595ea81.log`。**禁升钉／禁 Windows**。PLATFORM: SHARED 冷链。下一刀：seed 孪生＋宏＋realpath。 |
| 2026-09-01 | **fix(typeck)** **7.4.6 pin extra-paren（L4 阻断）** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝typeck pin 孪生手写 `ko==18`／Cap va 名字链 if 多一个 `)`；存储＝`seeds/typeck_gen.linux.x86_64.c`；消费＝L4 Track L `cc` `typeck_x.o`（日常 L2 prefer `.x` 不编 pin）。**修**：去掉 9 处多余 `)`（G.7 补全既有 pin，无第三套 patch）。**证**：Darwin Track L 探针 `cc -c` **0**／353840B。残：双端 L4 尚未重跑。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED pin C。下一刀：双端 `./xbuild l4` → 升钉 → Windows。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice18 Cap va mixed overflow** · tip **d79894f86** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝slice17 GP-ov／FP-ov 各拷一份同栈 extras、两类游标独立走；存储＝header(24) GP／FP／共享 OV 游标＋OV[8] 只拷一次；消费＝asm `-o` mixed 探针 33／3.0／44／4.0。**修**：G.7 补全既有 intercept（class_end 后走共享 OV；jz 选槽）。**证**：Ubuntu＠**d79894f86** call_dispatch＋relink · LANG-VA-CAP-BUILTINS／ELLIPSIS／VA-CAP **run=1** · L2 **5／5**。残：MSVC（须切 Windows）· host-C 多份 `va_arg<i32>`。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · LINUX／x86_64 · aarch64 cross-emit。下一刀：MSVC／va_arg 单实例。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice17 Cap va stack extras** · tip **e8298b5b2** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝asm Cap 仅寄存器 spill、第 7 个 GP 读进 FP 槽；存储＝GP-ov／FP-ov 各 8 槽（va_start 从 [rbp+16]／[x29,#frame+16] 拷入）；消费＝`va_arg_i32` 第 7／8 个 trailing。**修**：G.7 补全既有 intercept（ov 接在 GP／FP save 后，-8 走下去；named 栈参 skip）。**证**：Ubuntu＠**e8298b5b2** call_dispatch＋relink · LANG-VA-CAP-BUILTINS／ELLIPSIS／VA-CAP **run=1** · L2 **5／5**。残：MSVC（须切 Windows）· mixed GP＋FP 同栈 · host-C 多份 `va_arg<i32>`。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · LINUX／x86_64 · aarch64 cross-emit。下一刀：MSVC／mixed／va_arg 单实例。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice16 f32／f64 va_arg Cap** · tip **5ace8c601** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝asm Cap 仅 GP 游标／`tk==14/15` fallthrough；存储＝VaList 头 GP＋FP 双游标＋xmm／v spill；消费＝`va_arg<f32/f64>` 产品 `-o`。**修**：G.7 补全既有 intercept（header[0] GP／header 指针-8 FP；8B 槽 **-8**；host-C f32→`va_arg(double)`）；rbp 负偏移 **+8→-8** 防 SIGSEGV。**证**：Ubuntu＠**5ace8c601** call_dispatch＋relink · LANG-VA-CAP-BUILTINS／ELLIPSIS／VA-CAP **run=1** · L2 **5／5**。残：MSVC（须切 Windows）· stack extras。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · LINUX／x86_64 · aarch64 cross-emit。下一刀：MSVC／stack extras。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice15 arm64 asm Cap va** · tip **f9fabc1cd** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝asm Cap va `ta!=0` 直接 fallthrough；存储＝同 try_emit_va_cap 拦截＋AAPCS x0..x7 spill；消费＝`-target aarch64-linux-gnu` `.o`。**修**：G.7 补全既有函数 ta==1（gp_n=8／64B；ldr／str x3／x1；游标 **-8**）；x86 6-GP 不变。**证**：Ubuntu＠**f9fabc1cd** call_dispatch＋relink · LANG-VA-CAP-BUILTINS／ELLIPSIS／VA-CAP **run=1**（含 a64 LE ldr／str／sub）· L2 **5／5**。残：MSVC（须切 Windows）· f32／f64。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · LINUX／aarch64 cross-emit。下一刀：MSVC／f32·f64。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice14 typed va_arg T turbofish** · tip **b6e791a29** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝仅 `va_arg_i32/i64/ptr` 具名助手；存储＝既有 turbofish type-arg sidecar＋Cap va intercept；消费＝`va_arg`＋T＋ap 产品 `-o`。**修**：typeck 豁免名／codegen 宏改写／asm 按 T 分类 i32／qword 加载（G.7 补全 size_of T＋Cap va，无 type-as-value 解析）。**证**：Ubuntu＠**b6e791a29** typeck＋codegen＋call_dispatch＋relink · LANG-VA-CAP-BUILTINS／ELLIPSIS／VA-CAP **run=1**（asm `-o` 42）· L2 **5／5**。残：arm64 asm Cap · MSVC · f32／f64。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · LINUX／x86_64。下一刀：arm64 asm Cap va · MSVC。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice13 asm Cap va_arg_i64/ptr** · tip **687fb1e56** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝asm 仅 Cap `va_arg_i32`／游标 `+8` 在 rbp spill 上读回 named GP；存储＝call_dispatch Cap qword＋游标 **-8**；消费＝默认 `-o` i32→i64→ptr。**修**：backend_call_dispatch.x／seed；smoke／gate。**证**：Ubuntu＠**687fb1e56** seed cc＋FORCE_CC · LANG-VA-CAP-BUILTINS／ELLIPSIS／VA-CAP **run=1**（asm `-o` 42）。残：typed `va_arg` · arm64 · MSVC。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · LINUX\|x86_64。下一刀：typed `va_arg` · arm64 · MSVC。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice12 默认 asm Cap va** · tip **1ef752e2e** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝asm `call va_*` UNDEF；存储＝call_dispatch Cap 拦截＋call-spill GP 存＋VaList 游标；消费＝产品默认 `-o`。**修**：backend_call_dispatch.x／seed。**证**：Ubuntu＠**1ef752e2e** prefer call_dispatch＋FORCE_CC · LANG-VA-CAP-BUILTINS／ELLIPSIS／VA-CAP **run=1**（asm `-o` 42）。残：typed `va_arg` · arm64 · MSVC。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · LINUX\|x86_64。下一刀：typed `va_arg` · arm64 · MSVC。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice11 产品 host-cc Cap `-I`** · tip **f7b6cc1ad** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝invoke_cc 仅 `-I` 仓根→`xlang_va_cap.h` 找不到；存储＝`g_labi_icc_cap_inc_buf`＋第二 `-I <repo>/compiler/include`；消费＝`ALLOW_HOST_CC`＋`-backend c -o`。**修**：labi_invoke_cc_list.x／cold／surface；gate 产品 `-o`＝42。**证**：Ubuntu＠**f7b6cc1ad** L5 prefer＋FORCE_CC · LANG-VA-CAP-BUILTINS／ELLIPSIS／VA-CAP **run=1**（product c `-o` 42）。残：默认 asm Cap va · typed `va_arg` · MSVC。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：asm Cap va · typed `va_arg` · MSVC。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice10 rt_preamble Cap＋va unsafe 豁免** · tip **f1e03a653** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`-o` prologue 无 Cap 头／`va_*` 被 LANG-007 当 FFI；存储＝stdint 槽折叠 include＋`typeck_is_cap_va_builtin_name`；消费＝product preamble／safe call。**修**：rt_preamble.from_x＋typeck.x／pin。**证**：Ubuntu＠**f1e03a653** rt-slice＋migrate typeck＋FORCE_CC · LANG-VA-CAP-BUILTINS／ELLIPSIS／VA-CAP **run=1**（preamble strings 含 Cap）。残：产品 `-o` asm 仍 UNDEF `va_start`。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：asm Cap va · typed `va_arg` · MSVC。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice9 Cap va host-cc/runtime** · tip **47ff2fe7b** | **探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝-E Cap 宏未证可链可跑；存储＝gate host-cc＋`-Iinclude`；消费＝exit 42。**修**：`run-lang-va-cap-builtins-gate` 升 host-cc/runtime。**证**：Ubuntu＠**47ff2fe7b** LANG-VA-CAP-BUILTINS／ELLIPSIS／VA-CAP **run=1**（exe **42**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：typed `va_arg(ap,T)` · MSVC · rt_preamble。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice8 变参 call arity** · tip **b381c73f5** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`num_args!=nparams` 硬拒 trailing；存储＝`is_variadic`＋`typeck_call_arity_compatible`；消费＝call／overload。**修**：typeck.x／pin；gate 要求 `probe(1,42)`。**证**：Ubuntu＠**b381c73f5** migrate typeck＋FORCE_CC · LANG-VA-CAP-BUILTINS／ELLIPSIS／VA-CAP **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：typed `va_arg(ap,T)` · host-cc／MSVC。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice7 语言 va Cap builtins** · tip **7ea872f86** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`.x` 无 Cap va_start／arg／end；存储＝`codegen_try_emit_va_cap_call`＋`VaList`→`xlang_va_list`＋emit_header include；消费＝host-C Cap 宏。**修**：codegen.x／pin 孪；gate `run-lang-va-cap-builtins-gate`。**证**：Ubuntu＠**7ea872f86** 重建 codegen_x＋FORCE_CC · LANG-VA-CAP-BUILTINS／ELLIPSIS／VA-CAP **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：typed `va_arg(ap,T)` · 变参 call arity · host-cc／MSVC。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice6 定义面 `, ...` emit** · tip **0f533bbfb** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝decl 有 `, ...` 而 def 无；存储＝`is_variadic`；消费＝host-C 定义。**修**：codegen.x／pin 定义收口前发 ellipsis。**证**：Ubuntu＠0f533bbfb LANG-VA-ELLIPSIS **run=1**（含 def）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：va_start／arg／end builtins · MSVC。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice5 语言 `...`→is_variadic** · tip **`c63ed795b`** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝parser 未认 `TOKEN_ELLIPSIS`／`is_variadic` 从未置 1；存储＝`OneFuncResult.is_variadic`＋`pipeline_module_func_set_is_variadic`；消费＝codegen `, ...`。**修**：parser.x＋产品 pin parser_gen＋abi mirror；gate `run-lang-va-ellipsis-gate`。**证**：Ubuntu＠**`c63ed795b`** 重建 parser_x＋relink · LANG-VA-ELLIPSIS **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：va_start／arg／end builtins · extern `...`。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice4 http_glue Cap va** · tip **`1734afa0d`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`xlang_http_fcntl_cap` 仍 raw `__builtin_va_*`；存储＝`xlang_va_*`（via fmt_cap）；消费＝http SETFL／fcntl。**修**：http_glue → Cap va。**证**：Darwin 编译绿 · Ubuntu＠**`1734afa0d`** 同证 · VA／FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：语言 .x `...`／va builtins · MSVC。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice3 driver_diagnostic／nostdlib Cap va** · tip **`e0ecfc6df`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝RDD report_x／nostdlib fmt 仍 `<stdarg.h>`；存储＝`xlang_va_*`；消费＝pipeline code reportf／freestanding vsnprintf。**修**：`runtime_driver_diagnostic`＋`bootstrap_nostdlib_stubs`。**证**：Ubuntu＠**`e0ecfc6df`** 两 seed 编译绿 · VA／FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：语言 .x va builtins · MSVC。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice2 Cap-fmt seeds → Cap va** · tip **`08a994c58`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝Cap fmt 叶仍 `<stdarg.h>`；存储＝`xlang_va_*`（via fmt_cap）；消费＝append_asmf／debugf／lsp reportf。**修**：seed_link／call_dispatch／try_inline／lsp_stubs／lsp_glue。**证**：Ubuntu＠**`08a994c58`** 五 seed 编译绿 · VA／FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：driver_diagnostic／nostdlib · 语言 .x va。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice1 diag reportf → Cap va** · tip **`c41e877aa`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝diag 仍 `<stdarg.h>`／`va_list`；存储＝`xlang_va_cap`；消费＝reportf／vreportf＋Cap fmt。**修**：`diag.h`＋`diag.from_x.c` Cap va。**证**：Ubuntu＠**`c41e877aa`** diag.o **无 stdarg** · VA／FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：其它 seed stdarg／语言 .x va。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice21 runtime cold run/exec／import Cap** · tip **`b5b7d323d`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝rt_run_exec／c_import／parsed／asm_backend cold `U snprintf`；存储＝Cap＋宏；消费＝test cmd／dep／tmp path。**修**：四 seed stdio 后 Cap。**证**：Ubuntu＠**`b5b7d323d`** 四 seed **无 U snprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：labi residual／纯 .x／语言 va。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice20 build_runtime → Cap snprintf** · tip **`f15275862`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝cold `build_runtime.from_x.c` `U snprintf`（16）；存储＝Cap＋宏；消费＝build step cmd。**修**：stdio 后 `#define snprintf`→Cap。**证**：Ubuntu＠**`f15275862`** seed .o **无 U snprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：rt_run_exec／c_import／语言 va。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice19 build_tool_libc_bridge → Cap snprintf** · tip **`8c8d1332f`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`build_tool`／`build_tool_libc_bridge.o` `U snprintf`；存储＝Cap＋宏；消费＝asm build cmd。**修**：stdio 后 `#define snprintf`→Cap。**证**：Ubuntu＠**`8c8d1332f`** bridge／build_tool **无 U snprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：build_runtime／rt_run_exec／语言 va。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice18 余 gen pin＋Track L -E Cap** · tip **`3a18e9475`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝余 cold seed／Track L -E crash-evidence `U snprintf`；存储＝Cap＋宏；消费＝crash path／leaf .o。**修**：pipeline／preprocess／lsp_io_std_heap／ast_gen2 gen pin＋driver_leaf／g05_try prologue Cap。**证**：Ubuntu＠**`3a18e9475`** 余 pin／lsp_io／lsp_diag **无 U snprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：纯 .x／语言 va。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice17 crash-evidence／*_gen → Cap snprintf** · tip **`cc3c6df17`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`*_x.o`／panic `U snprintf`（CRASH_EVIDENCE path）；存储＝Cap＋宏；消费＝crash bundle 路径。**修**：panic／backtrace seed＋driver／lexer／lsp*_gen。**证**：Ubuntu＠**`cc3c6df17`** driver_x／lsp_x／panic **无 U snprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：其余 gen pin／语言 va。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice16 pipeline_abi → Cap snprintf** · tip **`75bddfd2f`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`runtime_pipeline_abi.o` `U snprintf`（.x 0 处）；存储＝Cap＋宏重定向；消费＝import path／diag／float。**修**：首 stdio 后 `#define snprintf`→Cap。**证**：Ubuntu＠**`75bddfd2f`** pipeline.o **无 U snprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：*_x.o／语言 va。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice15 fmt_check_cmd → Cap snprintf** · tip **`80b335e5f`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`fmt_check_cmd_driver.o` `U snprintf`；存储＝Cap＋宏（含 Win32 opendir 前）；消费＝walk／lib／ignore path。**修**：stdio＋Cap 提前至 Win32 前。**证**：Ubuntu＠**`80b335e5f`** fmt_check.o **无 U snprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：pipeline_abi／*_x.o。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice14 async liveness／cps → Cap snprintf** · tip **`a89c3423b`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`async_liveness.o`／`async_cps_codegen.o` `U snprintf`；存储＝Cap＋宏重定向；消费＝frame／type／sched 名格式化。**修**：两 seed `#define snprintf`→Cap。**证**：Ubuntu＠**`a89c3423b`** 两 .o **无 U snprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：pipeline_abi／fmt_check。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice13 driver_abi → Cap snprintf** · tip **`6bb0a58c1`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`runtime_driver_abi.o` `U snprintf`；存储＝Cap＋宏重定向；消费＝uname／tmp path format。**修**：`#define snprintf`→Cap。**证**：Ubuntu＠**`6bb0a58c1`** driver_abi.o **无 U snprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：pipeline_abi／fmt_check／async。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice12 link_abi＋L8b ondemand → Cap snprintf** · tip **`9f74e4691`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝mega rest＋L8b 单独 seed `U snprintf`；存储＝Cap＋宏重定向；消费＝path／nm／ensure `../%s`。**修**：`runtime_link_abi`＋`labi_ondemand_list` 双 TU。**证**：Ubuntu＠**`9f74e4691`** prefer link_abi.o **无 U snprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：产品 *_x.o／语言 va。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice11 lsp_diag_stubs_no_c → Cap fmt** · tip **`0ce137a68`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝bootstrap NO_C `lsp_diag_stubs_no_c.o` `U snprintf／vsnprintf`；存储＝Cap＋宏重定向；消费＝JSON diag／typeck reportf。**修**：`#define snprintf／vsnprintf`→Cap。**证**：Ubuntu＠**`0ce137a68`** stubs.o **无 U snprintf／vsnprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：link_abi／产品 x.o。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice10 driver_diagnostic 冷 seed → Cap fmt** · tip **`360ba7cb3`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝冷 `runtime_driver_diagnostic.from_x.c` `U snprintf／vsnprintf`（prefer 产品 .o 已无）；存储＝Cap＋宏重定向；消费＝parse／typeck／warn／hint 消息。**修**：`#define snprintf／vsnprintf`→Cap。**证**：Ubuntu＠**`360ba7cb3`** 冷 seed **无 U snprintf／vsnprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：lsp_diag_stubs／link_abi。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice9 call_dispatch debugf → Cap vsnprintf** · tip **`cbe48a652`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`backend_call_dispatch.o` `U vsnprintf`；存储＝Cap fmt；消费＝ASM_DEBUG note。**修**：入链 `xlang_vsnprintf`（同 try_inline）。**证**：Ubuntu＠**`cbe48a652`** call_dispatch.o **无 U vsnprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：driver_diagnostic。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice8 http_glue → Cap snprintf** · tip **`f0b917422`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`runtime_http_glue.o` `U snprintf`（主 TU＋`.inc`）；存储＝Cap fmt＋`#define snprintf`；消费＝request／URL／chunked／h2。**修**：G.7 宏重定向全 TU。**证**：Ubuntu＠**`f0b917422`** http_glue.o **无 U snprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：call_dispatch／driver_diagnostic。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice7 log rotate → Cap snprintf** · tip **`d680c0347`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`runtime_log_os.o` `U snprintf`；存储＝rotate path format＋Cap；消费＝log 文件轮转。**修**：5 处 `xlang_snprintf`。**证**：Ubuntu＠**`d680c0347`** log_os.o **无 U snprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：call_dispatch／http_glue。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice6 time RFC3339 → Cap snprintf** · tip **`c71ce4b2b`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`runtime_time_os.o` `U snprintf`；存储＝`time_format_rfc3339_impl`＋Cap；消费＝RFC3339 格式化。**修**：`xlang_snprintf`。**证**：Ubuntu＠**`c71ce4b2b`** time_os.o **无 U snprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：call_dispatch／log_os。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice5 lsp snprintf → Cap** · tip **`1fd734bb6`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`lsp_diag.o` `U snprintf`；存储＝runtime_lsp_glue＋Cap；消费＝路径／JSON／type 名格式化。**修**：46 处 `snprintf`→`xlang_snprintf`。**证**：Ubuntu＠**`1fd734bb6`** lsp_diag.o **无 U snprintf／vsnprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：call_dispatch／stubs／driver_diagnostic。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice4 lsp typeck reportf → Cap vsnprintf** · tip **`26357d82a`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`lsp_diag.o` `U vsnprintf`；存储＝`lsp_diag_report_typeck*`＋Cap；消费＝typeck reportf／9.5.3。**修**：两处 reportf 入链。**证**：Ubuntu＠**`26357d82a`** lsp_diag.o **无 U vsnprintf**（snprintf 残）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：lsp snprintf／call_dispatch。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice3 try_inline debugf → Cap vsnprintf** · tip **`7d39448b5`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`backend_try_inline_debugf` `U vsnprintf`；存储＝Cap fmt；消费＝ASM_DEBUG note。**修**：入链 `xlang_vsnprintf`。**证**：Ubuntu＠**`7d39448b5`** try_inline.o **无 U vsnprintf** · FMT **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：driver_diagnostic／call_dispatch。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice2 seed_link_compat → Cap vsnprintf** · tip **`14f720431`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`xlang_append_asmf` `U vsnprintf`；存储＝Cap fmt；消费＝asm 行格式化。**修**：入链 `xlang_vsnprintf`。**证**：Ubuntu＠**`14f720431`** seed_link_compat.o **无 U vsnprintf** · FMT／VA **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：try_inline／driver_diagnostic。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice1 diag reportf → Cap vsnprintf** · tip **`51423a9af`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝diag `U vsnprintf`；存储＝`diag_vreportf_with_code`＋`xlang_fmt_cap`；消费＝reportf／9.5.3。**修**：产品 seed 入链 Cap。**证**：Ubuntu＠**`51423a9af`** diag.o **无 U vsnprintf** · FMT／VA **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：其它 seed／语言 va。 |
| 2026-09-01 | **feat(selfhost)** **10.7.2 slice0 Cap freestanding vsnprintf** · tip **`c3573ec41`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝nostdlib／libc 双套 fmt；存储＝`xlang_fmt_cap.h`＋stubs 薄包装；消费＝FMT-CAP／bootstrap nostdlib。**修**：G.7 抬升权威；`%.*s`／宽／浮点面保留。**证**：Darwin／Ubuntu＠**`c3573ec41`** FMT／VA-CAP **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：语言 va／产品 seed 入链。 |
| 2026-09-01 | **feat(selfhost)** **10.7.1 slice0 Cap va_list 无 stdarg.h** · tip **`61d670a19`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝Cap／reportf 仍 `<stdarg.h>`；存储＝`xlang_va_cap.h` builtins；消费＝VA-CAP／后续 vsnprintf。**修**：`xlang_va_list`＋start／arg／end／copy。**证**：Darwin／Ubuntu＠**`61d670a19`** VA-CAP **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED GCC／Clang。下一刀：10.7.2 vsnprintf。 |
| 2026-09-01 | **feat(selfhost)** **10.6.2 slice0 Windows Cap CreateThread spawn／join** · tip **`0d906f7e8`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝Win glue 旧 CreateThread 绕 Cap；存储＝`xlang_thread_cap.h` Win 面＋`runtime_thread_glue`；消费＝THREAD-CAP-WIN／产品 create／join。**修**：Cap spawn／join（CreateThread／WaitForSingleObject）；product glue 对齐 Linux join-handle ABI；gate skip／mingw obs。**证**：Ubuntu＠**`0d906f7e8`** THREAD-CAP-WIN **skip=1** · THREAD-CAP／STD-043／SYNC-CAP 回归绿。钉盘仍 **`e8176cbe5`**。PLATFORM: WINDOWS Cap · LINUX／Darwin 非金标。下一刀：10.7／MSYS run。 |
| 2026-09-01 | **feat(selfhost)** **10.6.1 slice2 thread_glue Linux Cap＋fk_gate** · tip **`e8777cb6b`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝产品 thread 仍 pthread；存储＝`runtime_thread_glue`＋Cap spawn／pool；消费＝STD-043。**修**：Linux Cap create／join／pool／affinity／name；fk_gate 补 pool UNDEF；glue 必推。**证**：Ubuntu＠**`e8777cb6b`** nm 无 pthread · THREAD-CAP **run=1** · STD-043 **run=2**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX Cap · Darwin pthread。下一刀：10.6.2／10.7。 |
| 2026-09-01 | **feat(selfhost)** **10.6.3 slice4 Cap rwlock＋sync_os 全 Cap** · tip **`7959148f7`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝Cap／sync_os 缺 rwlock＋cond smoke pthread；存储＝`xlang_cap_rwlock_*`＋sync_os `_impl`；消费＝SYNC-CAP／STD-045。**修**：futex rwlock；Linux rwlock／Cap spawn；nm 无 pthread。**证**：Ubuntu＠**`7959148f7`** Cap／thread **run=1** · STD-045 **run=2**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX Cap · Darwin pthread。下一刀：10.6.2／10.7。 |
| 2026-09-01 | **feat(selfhost)** **10.6.3 slice3 sync_os Linux mutex／cond → Cap** · tip **`72159127c`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝产品 sync_os 仍 pthread_mutex／cond；存储＝`runtime_sync_os.from_x.c` `_impl`＋`xlang_sync_cap.h`；消费＝STD-045／产品 sync。**修**：LINUX mutex／cond 入链 Cap；rwlock／cond smoke pthread 残。**证**：Ubuntu＠**`72159127c`** nm 无 pthread_mutex／cond · SYNC-CAP **run=1** · STD-045 **run=2**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX Cap · POSIX／Darwin pthread。下一刀：Cap rwlock／10.6.2。 |
| 2026-09-01 | **feat(selfhost)** **10.6.3 slice2 Cap futex semaphore** · tip **`58397fae1`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝Cap 缺 counting sem；存储＝`xlang_cap_sem_*` count＋futex；消费＝SYNC-CAP-RAW wait／post。**修**：wait／trywait／post。**证**：Ubuntu＠**`58397fae1`** sync／thread Cap **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：sync_os 入链。 |
| 2026-09-01 | **feat(selfhost)** **10.6.3 slice1 Cap futex condvar** · tip **`ed5bfbe1f`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝Cap 仅 mutex；存储＝`xlang_cap_cond_*` seq＋futex；消费＝SYNC-CAP-RAW wait／signal。**修**：wait 解锁睡／signal／broadcast。**证**：Ubuntu＠**`ed5bfbe1f`** sync／thread Cap **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：semaphore／sync_os。 |
| 2026-09-01 | **feat(selfhost)** **10.6.3 slice0 Cap futex mutex** · tip **`991b14ec0`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝sync 仍 pthread；存储＝`xlang_sync_cap.h`＋thread Cap futex；消费＝SYNC-CAP-RAW。**修**：CAS＋futex wait／wake mutex；并发 spawn 计数。**证**：Ubuntu＠**`991b14ec0`** sync／thread Cap **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：condvar／sync_os 入链。 |
| 2026-09-01 | **feat(selfhost)** **10.6.1 slice1 Linux clone trampoline spawn／join** · tip **`069b56009`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝raw clone C-continuation 子线程 SEGV；存储＝`xlang_clone_tramp` asm＋pack／futex join；消费＝THREAD-CAP-RAW。**修**：禁 C 续跑；x86_64／aarch64 trampoline→fn→exit。**证**：Ubuntu＠**`069b56009`** **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：pthread 入链／10.6.3。 |
| 2026-09-01 | **feat(selfhost)** **10.6.1 slice0 Linux futex／mmap-stack Cap** · tip **`5718bbbaa`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝线程仍依赖 libpthread／无 Cap；存储＝`xlang_thread_cap.h`＋syscall；消费＝host-cc smoke／gate。**修**：futex wait／wake＋MAP_STACK mmap／munmap。**证**：Ubuntu＠**`5718bbbaa`** THREAD-CAP-RAW **run=1** · Darwin **skip=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：clone／clone3。 |
| 2026-09-01 | **feat(selfhost)** **10.5.2 slice2 SIMD lang ARM SVE i32x8／f32x8** · tip **`2e03ae24c`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝SVE 仅 f32x4；存储＝`sve_ibinop`＋fbinop 双半 VL4＋feat 512；消费＝SVE x8 gate LE add／fadd。**修**：i32 add／mul／sub＋f32x8 dual。**证**：Ubuntu＠**`2e03ae24c`** SVE x8／f32x4／fma／NEON x8 **run=2**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX aarch64 cross-emit · NEON 默认不回退。下一刀：10.5 收口／MSYS。 |
| 2026-09-01 | **feat(selfhost)** **10.5.2 slice1 SIMD lang ARM SVE fma／hsum／dot** · tip **`4dc1b412d`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝SVE 仅 binop；存储＝`sve_fma／hsum／dot`＋feat 512 prefer；消费＝SVE fma-dot-hsum gate LE fmla／faddv。**修**：VL4＋fmla／faddv（llvm-mc 核）。**证**：Ubuntu＠**`4dc1b412d`** SVE fma／f32x4／NEON **run=2**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX aarch64 cross-emit · NEON 默认不回退。下一刀：更宽 VL／MSYS。 |
| 2026-09-01 | **feat(selfhost)** **10.5.2 slice0 SIMD lang ARM SVE f32x4** · tip **`779374da5`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝SVE 未接 lang；存储＝`simd_arm64_sve_fbinop_f32x4_rbp`＋feat 512 prefer；消费＝SVE gate `-target-cpu sve` LE ptrue／ld1w／fadd。**修**：VL4 限定 4 车道＋fadd／fmul／fsub。**证**：Ubuntu＠**`779374da5`** SVE／NEON／f32x4 **run=2**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX aarch64 cross-emit · NEON 默认不回退。下一刀：SVE fma／reduce · MSYS。 |
| 2026-09-01 | **feat(selfhost)** **10.5.1 slice10 SIMD lang host-C fallthrough** · tip **`0609c4a05`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝builtin panic 体在 xlang-c／缺 HW 时崩；存储＝`std/simd/builtin.x` 标量车道；消费＝hostc gate 经 xlang-c。**修**：add／mul／sub／fma／hsum／dot／i32x8／f32x8 标量体。**证**：Ubuntu＠**`0609c4a05`** hostc **run=1** · asm f32x4／fma／dot-hsum／arm64 **run=2** · intrinsic **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED host-C · asm intercept 不回退。下一刀：MSYS／SVE。 |
| 2026-09-01 | **feat(selfhost)** **10.5.1 slice9 SIMD lang aarch64 fma／hsum／dot** · tip **`6f004a125`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝slice7／8 仅 x86；存储＝`simd_arm64_fma／hsum／dot`＋try_emit ta==1；消费＝arm64 gate LE fmul／fadd／faddp。**修**：NEON fmul+fadd fma＋faddp reduce＋AAPCS reload／fmov。**证**：Ubuntu＠**`6f004a125`** arm64 fma-dot-hsum／f32x4 **run=2** · x86 fma／dot-hsum／f32x4 **run=2** · intrinsic **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED · LINUX cross-emit gold。下一刀：host-C／MSYS。 |
| 2026-09-01 | **feat(selfhost)** **10.5.1 slice8 SIMD lang hsum／dot f32x4** · tip **`a0ed9bee1`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝lang 缺标量 reduce；存储＝`hsum／dot_rbp`＋try_emit which 11／12（nlen 9／10）＋xmm0→eax；消费＝dot-hsum gate。**修**：panic 体＋SSE mulps／pshufd／addps。**证**：Ubuntu＠**`a0ed9bee1`** dot-hsum **run=2** · fma／f32x4／i32x8／f32x8 **run=2** · intrinsic **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX x86_64 SSE · SHARED surface。下一刀：aarch64 fma／reduce · host-C。 |
| 2026-09-01 | **feat(selfhost)** **10.5.1 slice7 SIMD lang fma_f32x4** · tip **`43356710b`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝lang 缺 3 参 fma；存储＝try_emit which=10＋既有 `fma_rbp`；消费＝fma gate objdump vfmadd／mulps。**修**：`fma_f32x4` panic 体＋3× spill／reload。**证**：Ubuntu＠**`43356710b`** fma **run=2** · f32x4／i32x8／f32x8 **run=2** · intrinsic **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX x86_64 SSE／FMA3 · SHARED surface。下一刀：dot／hsum／aarch64 fma。 |
| 2026-09-01 | **feat(selfhost)** **9.1.11 Win Cap backtrace／PE** · tip **`dcd335be4`** | **代码＋文档** · **不升钉** · **git-only Ubuntu+Darwin**。五问：产生＝Win 仍 DbgHelp／CaptureStackBackTrace；存储＝`xlang_backtrace_cap.h`＋platform seed；消费＝std.backtrace／invoke_cc。**修**：SHARED FP walk＋Win VirtualQuery+PE export Cap；drop `-ldbghelp`。**证**：Ubuntu＠**`dcd335be4`** capture／dladdr smoke **0** · xlang_asm **无 U backtrace/dladdr** · Darwin smoke **0**。残：MSYS 实机金标。钉盘仍 **`e8176cbe5`**。PLATFORM: WINDOWS Cap · LINUX｜DARWIN 不回退。下一刀：fma／MSYS。 |
| 2026-09-01 | **feat(selfhost)** **10.5.1 slice6 SIMD lang aarch64 f32x8/i32x8 NEON** · tip **`4c6cac242`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝32B VAR/sret 仍 x86-only；存储＝`simd_arm64_ibinop`＋fbinop dual-half＋try_emit ta==1；消费＝arm64 gates LE bytes。**修**：i32 add/mul/sub NEON＋开放 which 3/4/6/7/8/9。**证**：Ubuntu＠**`4c6cac242`** f32x8/i32x8/f32x4 arm64 **run=2** · x86 邻域绿 · intrinsic **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED · LINUX cross-emit gold。下一刀：9.1.11／fma。 |
| 2026-09-01 | **feat(selfhost)** **10.5.1 slice5 SIMD lang aarch64 f32x4 NEON** · tip **`257aa95da`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝cross-emit 仍 CALL panic；存储＝`simd_arm64_fbinop_128_rbp`＋try_emit ta==1；消费＝gate LE fadd/fmul/fsub bytes。**修**：NEON enc＋feat OR 256 cross-host＋AAPCS reload。**证**：Ubuntu＠**`257aa95da`** arm64/f32x4/f32x8/i32x8 **run=2** · intrinsic **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED · LINUX cross-emit gold。下一刀：aarch64 f32x8/i32x8。 |
| 2026-09-01 | **feat(selfhost)** **10.5.1 slice4 SIMD lang sub_f32x8** · tip **`27521f0a4`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝f32x8 缺 sub 内建；存储＝try_emit which=9＋`simd_enc` vsubps ymm/dual subps；消费＝gate objdump vsubps/subps。**修**：`sub_f32x8` panic 体＋探针 sub 步。**证**：Ubuntu＠**`27521f0a4`** f32x8/f32x4/i32x8 **run=2** · intrinsic **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX x86_64 AVX2 · SHARED surface。下一刀：aarch64 NEON／9.1.11。 |
| 2026-09-01 | **feat(selfhost)** **10.5.1 slice3 SIMD lang f32x8 AVX ymm** · tip **`b96e77dd6`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝lang 缺 f32x8 AVX 内建；存储＝fadd/fmul lanes=8 ymm＋f32x8 拼写注册；消费＝gate objdump vaddps/vmulps。**修**：`add_f32x8`／`mul_f32x8`＋`simd_enc` ymm enc＋`target_cpu`/`seed` f32x8 spelling。**证**：Ubuntu＠**`b96e77dd6`** f32x8/f32x4/i32x8 **run=2** · intrinsic **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX x86_64 AVX2 · SHARED surface。下一刀：sub_f32x8／aarch64。 |
| 2026-09-01 | **feat(selfhost)** **10.5.1 slice2 SIMD lang sub** · tip **`35fc832d8`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝lang 缺 sub 内建；存储＝try_emit 扩 which 5/6＋simd_enc fsub/subps；消费＝gate objdump subps/psubd。**修**：`sub_f32x4`／`sub_i32x8` panic 体＋探针 sub 步。**证**：Ubuntu＠**`35fc832d8`** i32x8/f32x4 **run=2** · intrinsic **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX x86_64 SSE/AVX2 · SHARED surface。下一刀：f32x8 ymm／aarch64。 |
| 2026-09-01 | **feat(selfhost)** **10.5.1 slice1 SIMD lang i32x8** · tip **`0a09b8056`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝Vec8i 32B sret let 无 lang 内建；存储＝try_emit VAR homes＋sret_home_off；消费＝gate objdump paddd/pmulld。**修**：`add_i32x8`／`mul_i32x8`＋pipeline let sret 发布 dest＋`simd_enc` ymm rbp VEX 修。**证**：Ubuntu＠**`0a09b8056`** i32x8 **run=2** · f32x4 **run=2** · intrinsic **run=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX x86_64 SSE2/AVX2 · SHARED surface。下一刀：slice2 ymm 扩 op／aarch64。 |
| 2026-09-01 | **feat(selfhost)** **10.5.1 slice0 SIMD lang f32x4** · tip **`e296cdc25`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝语言层无 SSE 内建仅 std.simd CALL；存储＝backend_call_dispatch CALL 路径；消费＝gate objdump addps/mulps。**修**：`std/simd/builtin.x` panic 体＋`try_emit_simd_lang_builtin_call_elf_c`（Vec4f spill＋simd_enc HW）。**证**：Ubuntu＠**`e296cdc25`** lang gate **run=2** · intrinsic **run=1** · L2 **5／5** · Darwin **skip=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX x86_64 SSE · SHARED surface。下一刀：i32x8／AVX2 ymm。 |
| 2026-09-01 | **fix(selfhost)** **STD-SIMD-INTRINSIC 硬绿** · tip **`0fc0ca343`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝co-emit std.simd mod.x 错 overload CALL；存储＝asm_codegen_elf_o dep skip；消费＝intrinsic_binop_dot gate。**修**：skip co-emit `std.simd`（misc＋link_only）＋call-export overload 评分；Cap print `target_cpu_features=`。**证**：Ubuntu＠**`0fc0ca343`** intrinsic **run=1** · s1 **run=2** · s2 **run=2** · prod **run=1 obs=1**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED SIMD formal。下一刀：10.5.1 AVX 内建。 |
| 2026-09-01 | **docs(selfhost)** **9.1.11 Linux+Darwin backtrace Cap** · tip **`3933726d0`** | **文档** · **不升钉**。C迁移追踪 9.1.11 标 Linux+Darwin ✅。下一刀：SIMD。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.11 s1 Darwin dladdr Cap** · tip **`e2bba0eed`** | **代码＋探针** · **不升钉** · **git-only 双端**。五问：产生＝libc `dladdr`；存储＝symbolicate_impl；消费＝std.backtrace／STD-052。**修**：G.7 Darwin `xlang_bt_dladdr`（proc_regionfilename＋mach_vm_region slide＋Mach-O LC_SYMTAB／nlist_64）＋Darwin weak symbolicate stub。证：macOS platform.o **无 U backtrace/dladdr** · capture／dladdr smoke **0** · Ubuntu Linux 面仍 **无 U backtrace/dladdr**。残：Win DbgHelp。钉盘仍 **`e8176cbe5`**。PLATFORM: DARWIN+LINUX SHARED backtrace。下一刀：SIMD。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.11 s0 Darwin backtrace Cap** · tip **`cd779f70b`** | **代码＋探针** · **不升钉** · **git-only 双端**。五问：产生＝libc `backtrace()`；存储＝capture_impl；消费＝crash evidence／Cap smokes。**修**：G.7 共享 `xlang_bt_backtrace`（Linux+Darwin）＋`HAVE_XLANG_BT_CAPTURE_CAP` on Apple＋Darwin weak capture stub。证：macOS capture smoke **0** · platform.o **无 U backtrace**。残：Darwin dladdr s1 · Win。钉盘仍 **`e8176cbe5`**。PLATFORM: DARWIN primary · LINUX gold 不回退。下一刀：dladdr s1。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.5.2 s1 Darwin print Cap** · tip **`390af683a`** | **代码** · **不升钉** · **git-only 双端**。五问：产生＝fprintf 三行；存储＝target_cpu_pure print；消费＝`--print-target-cpu`。**修**：G.7 `xlang_io_cap.h` Darwin syscall write；print Cap 扩至 macOS。证：macOS target_cpu.o **无 U fprintf** · `--print-target-cpu` **0** · Ubuntu Linux 面仍 **无 U fprintf/fopen**。钉盘仍 **`e8176cbe5`**。PLATFORM: DARWIN+LINUX SHARED print。下一刀：Darwin backtrace／SIMD。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.12 s1 Darwin detect Cap** · tip **`4dbe69e88`** | **代码** · **不升钉** · **git-only 双端**。五问：产生＝sysctlbyname；存储＝target_cpu_pure macOS detect；消费＝host feature 探测。**修**：arm64 去无效 FEAT_SVE sysctl；x86 macOS CPUID leaf1/7；fix print helper Linux gate。证：macOS target_cpu.o **无 U sysctl** · `--print-target-cpu` **0** · Ubuntu Linux 面仍 **无 U fprintf/fopen** · proc smoke **0**。残：Darwin print Cap · arm SVE caps。钉盘仍 **`e8176cbe5`**。PLATFORM: DARWIN primary · LINUX gold 不回退。下一刀：Darwin print／backtrace／SIMD。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.5.2 target_cpu_print Cap** · tip **`35c582cd9`** | **代码** · **不升钉** · **git-only Ubuntu**。五问：产生＝libc fprintf 三行；存储＝target_cpu_pure print；消费＝`--print-target-cpu`／rt_dispatch。**修**：G.7 Linux `xlang_io_write`＋手工 hex 格式化；Darwin 仍 fprintf fallback。证：Ubuntu＠**`35c582cd9`** target_cpu.o **无 U fprintf/fwrite** · `--print-target-cpu` 三行 **0**。残：Darwin print Cap · macOS sysctl。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：macOS sysctl／Darwin backtrace／SIMD。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.12 s0 proc Cap target_cpu** · tip **`3f7cc46c6`** | **代码＋探针＋link** · **不升钉** · **git-only Ubuntu**。五问：产生＝libc fopen／fgets 读 `/proc/cpuinfo`；存储＝target_cpu_pure detect；消费＝SIMD host feature 探测。**修**：G.7 `xlang_proc_cap.h`（open＋`xlang_io_read`＋行扫描）＋weak `xlang_target_cpu_detect_host` stub。证：Ubuntu＠**`3f7cc46c6`** target_cpu.o **无 U fopen／fgets** · `target_cpu_proc_raw_smoke` **0**。残：macOS sysctlbyname · 9.5.2 fprintf print · Darwin／Win。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：macOS sysctl／Darwin backtrace／SIMD。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.11 s1 dladdr Cap** · tip **`72f18126c`** | **代码＋探针＋link** · **不升钉** · **git-only Ubuntu**。五问：产生＝libc `dladdr`；存储＝symbolicate_impl；消费＝std.backtrace／STD-052。**修**：G.7 `xlang_bt_dladdr`（maps＋ELF sym）＋weak stub。证：Ubuntu＠**`72f18126c`** platform.o **无 U dladdr/backtrace** · dladdr smoke **0**。残：Darwin／Win DbgHelp。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：SIMD／Darwin。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.11 s0 backtrace Cap** · tip **`ced128036`** | **代码＋探针＋link** · **不升钉** · **git-only Ubuntu**。五问：产生＝libc `backtrace()`；存储＝`runtime_backtrace_platform` capture_impl；消费＝std.backtrace／crash evidence。**修**：G.7 `xlang_backtrace_cap.h` frame walk＋weak stub＋needs_io UNDEF。证：Ubuntu＠**`ced128036`** platform.o **无 U backtrace** · smoke **0**。残：**U dladdr** · Darwin／Win。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：dladdr s1／SIMD。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.8 cold io_abi read Cap** · tip **`c73745da4`** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝cold seed `read()`；存储＝read_fd／file_view_read_malloc／os_read_file_into；消费＝ast_pool／driver。**修**：G.7 cold→`xlang_io_read`。证：Ubuntu＠**`c73745da4`** io_abi **无 U read** · `io_read_raw_smoke` **0** · exe **无 U read**。残：Darwin／Win · 9.1.11。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：9.1.11／SIMD。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.8 cold io_abi read Cap** · tip **`a4240d086`** | **代码＋探针** · cold read 首 commit；探针后修 extern pipe＠**`c73745da4`**。 |
| 2026-09-01 | **docs(selfhost)** **stage9 9.1.10 dir Cap complete** · tip **`5935b6fbc`** | **文档** · **不升钉**。9.1.10 标 Ubuntu 全链收（含 pabi prologue）。下一刀：9.1.8 read 残／9.1.11。 |
| 2026-09-01 | **fix(selfhost)** **stage9 9.1.10 -E prologue dir Cap** · tip **`6ee38226e`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝`-E` harness 仍 libc `xlang_fmt_opendir`；存储＝ensure／g05 prologue；消费＝pabi prefer co-emit。**修**：G.7 prologue→`xlang_dir_cap.h`。证：Ubuntu＠**`6ee38226e`** pabi **无 U opendir** · fs／fmt 仍绿。残：Darwin／Win · 9.1.11。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：SIMD／9.1.11。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.10 Cap dir getdents64** · tip **`13246bba7`** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝libc opendir／readdir／closedir；存储＝posix／fmt／pabi cold；消费＝std.fs dir／fmt walk／import sibling。**修**：G.7 `xlang_dir_cap.h`＋dir_cap seed＋formal merge＋fmt／pabi Cap。证：Ubuntu＠**`13246bba7`** Cap smoke **0** · fs／fmt **无 U opendir**。残：pabi 旧叶 · Darwin／Win · 9.1.11。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：SIMD／9.1.11。 |
| 2026-09-01 | **fix(selfhost)** **stage9 9.1.7 preamble Cap poll** · tip **`3d10ea818`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝preamble libc `poll` co-emit；存储＝`rt_preamble` 内嵌串＋formal_mod；消费＝`io_libc_poll`→`net.o` U poll。**修**：G.7 preamble→`xlang_net_poll`；须 `rt_preamble.o`+g05 后 FORCE net／bytes／fs。证：Ubuntu＠**`3d10ea818`** `net.o`／bytes／fs **无 U poll** · reloc→`.text.xlang_net_poll`。残：WSAStartup／Darwin · 9.1.10。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：SIMD／9.1.10。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.9 统一 syscall Cap** · tip **`fecc624dd`** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝各 Cap 头重复 inline asm syscall；存储＝path／io／net／process／time／random；消费＝全部 Cap 面。**修**：G.7 `xlang_syscall_cap.h`（syscall0..6）＋六头 alias。证：Ubuntu＠**`fecc624dd`** Cap 头无重复 asm · `syscall_cap_raw_smoke`／io／dns **0**。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：SIMD／net U poll。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.7 slice3 http Cap socket 面** · tip **`86a06a6fd`** | **代码＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝http_glue／pool／h2 smoke libc socket／connect／fcntl；存储＝timeout_impl＋incs；消费＝http dial／serve。**修**：G.7 Cap fcntl＋TU 宏重定向 socket／connect／bind／listen／accept／poll／close／send／recv。证：Ubuntu＠**`86a06a6fd`** http_glue **net face clean** · dns／sock **0**。残：WSAStartup／Darwin · net.o U poll。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：SIMD／9.1.9。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.7 slice2 Cap DNS／getaddrinfo** · tip **`9c5bb4f00`** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝libc getaddrinfo；存储＝dns_fast／dns.x／http_glue／pool incs；消费＝std.net resolve／http dial。**修**：G.7 `xlang_dns_cap.h`（literal／hosts／UDP）＋net sendto／recvfrom＋dns／http Cap dial。证：Ubuntu＠**`9c5bb4f00`** dns_fast／http_glue **无 U getaddrinfo** · `net_dns_raw_smoke` **0**。残：http U socket／connect · WSAStartup／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：清 socket U／SIMD。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.8 write Cap** · tip **`7486a3f51`** | **代码＋探针＋文档** · **不升钉** · **git-only Ubuntu**。五问：产生＝libc write／preamble inline／io_abi；存储＝stubs／io_abi／preamble；消费＝std.io／std.fs／write_path。**修**：G.7 `xlang_io_cap.h`＋preamble Cap＋stubs weak `xlang_sys_*`＋io_abi 换 `xlang_sys_write`。证：Ubuntu＠**`7486a3f51`** stubs **无 U write** · `io_write_raw_smoke` **0** · exe **无 U write** · sock 邻域 **0**。残：cold io_abi U read · Darwin／Win。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：getaddrinfo／SIMD。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.7 slice1 recvmmsg／sendmmsg Cap** · tip **WIP** | **代码＋探针＋文档** · **不升钉**。五问：产生＝libc recvmmsg／sendmmsg；存储＝udp_batch／asm_io_stubs include；消费＝std.net udp_batch。**修**：G.7 补全 `xlang_net_cap.h` mmsg＋poll；seed 换 Cap；stubs 随 include 重建。证：Ubuntu udp_batch／stubs／exe **无 U recvmmsg／sendmmsg** · `net_mmsg_raw_smoke` **0** · L2 **5／5**。残：getaddrinfo／WSAStartup／Darwin／Win · net.o U poll（io）。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：getaddrinfo／9.1.8。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.7 slice0 socket／connect／poll Cap** · tip **WIP** | **代码＋探针＋文档** · **不升钉**。五问：产生＝libc socket／connect／poll；存储＝sock_fast／ipv6_fast／缺 `xlang_sys_*` 体；消费＝std.net listen／linux_syscall_*。**修**：G.7 `xlang_net_cap.h`＋补全 `xlang_sys_*`＋listen／udp_bind／ipv6。证：Ubuntu exe **无 U socket／connect／poll** · `net_sock_raw_smoke` **0** · L2 **5／5**。残：net.o U poll（`io_libc_poll`）· getaddrinfo／mmsg · Darwin／Win。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：mmsg／getaddrinfo／9.1.8。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.6 getrandom 离 libc** · tip **WIP** | **代码＋探针＋文档** · **不升钉**。五问：产生＝libc getrandom；存储＝`random_fill_bytes_impl`；消费＝std.random。**修**：G.7 `xlang_random_cap.h`（Linux syscall 318／278）。证：Ubuntu random_fill **无 U getrandom** · `random_raw_smoke`／rand_fill／main **0** · L2 **5／5**。残：Darwin getentropy · Win BCrypt。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：9.1.7／SIMD。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.5 clock／nanosleep／gmtime 离 libc** · tip **WIP** | **代码＋探针＋文档** · **不升钉**。五问：产生＝libc clock_gettime／nanosleep／gmtime_r；存储＝`time_*_impl`＋邻域 glue；消费＝std.time／trace／channel。**修**：G.7 `xlang_time_cap.h`（syscall＋civil gmtime）；time_os／scheduler／channel／sync／driver wall。证：Ubuntu time_os **无 U clock_gettime／nanosleep／gmtime_r** · `time_raw_smoke`／rfc3339／deep **0** · L2 **5／5**。残：localtime_r／mktime · mega 未注入 · Darwin／Win。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：9.1.6／SIMD。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.4 host system／spawn／waitpid 离 libc** · tip **WIP** | **代码＋文档** · **不升钉**。五问：产生＝mega libc fork／execvp／waitpid／system；存储＝`spawn_sync_impl`／`waitpid_retry_impl`／`system_impl`；消费＝invoke_cc／ld／ensure。**修**：G.7 `xlang_proc_execvp`＋三面＋cc／ld／curl 裸点。证：Ubuntu mega **无 U fork／exec＊／waitpid／system** · spawn_raw **0** · L2 **5／5**。**9.1.4 收**。残：signal／strerror · Darwin／Win。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：9.1.5／SIMD。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.4 spawn／exec／waitpid／pipe 离 libc** · tip **WIP** | **代码＋探针＋文档** · **不升钉**。五问：产生＝libc fork／execve／waitpid／pipe／_exit；存储＝process_os_glue＋import_alias；消费＝std_process_spawn／wait／pipe。**修**：G.7 `xlang_process_cap.h`（fork／execve／wait4／pipe／dup2／exit_group）；alias exit。证：Ubuntu process_merge · **无 U fork／execve／waitpid／pipe／_exit** · spawn／pipe／spawn_wait **0** · L2 **5／5**。残：U signal · host mega system／spawn · Darwin／Win。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：host system／SIMD。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.2 stat／access／realpath 离 libc** · tip **WIP** | **代码＋探针＋文档** · **不升钉**。五问：产生＝libc access／stat／fstat／realpath；存储＝`link_abi_path_*_impl`／`realpath_cap_impl`／`fs_libc_stat`；消费＝host ensure＋std.fs.stat。**修**：G.7 `xlang_path_cap.h`（faccessat／newfstatat／open+readlink）；posix raw_syscall；fk0＋`std_fs_stat`。证：Ubuntu mega **无 U access／stat／fstat／realpath** · fs.o **无 U stat** · `fs_stat_raw_smoke`／dirmeta **0** · L2 **5／5**。残：Darwin／Win · fmt_check.o stat。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：9.1.4／SIMD。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.1 getenv／setenv／unsetenv 离 libc** · tip **WIP** | **代码＋探针＋文档** · **不升钉**。五问：产生＝libc getenv／setenv／unsetenv；存储＝`link_abi_getenv_impl`＋process／env_os；消费＝std_process／std_env。**修**：G.7 `xlang_environ_cap.h` environ 走查／改写；twins 同语义。证：Ubuntu process_merge · **无 U getenv／setenv／unsetenv** · `process_env_raw_smoke`／`setenv_unsetenv` **0** · L2 **5／5**。残：Darwin／Win · 9.1.2。钉盘仍 **`e8176cbe5`**。PLATFORM: POSIX Cap residual · LINUX gold。下一刀：9.1.2／SIMD。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.3 getcwd／chdir 离 libc** · tip **WIP** | **代码＋探针＋文档** · **不升钉**。五问：产生＝libc getcwd／chdir；存储＝cwd 缓存＋process.o；消费＝std_process_getcwd／chdir。**修**：G.7 Linux getcwd=79／17、chdir=80／49 内联 syscall；缓存保留。证：Ubuntu process_merge · **无 U getcwd／chdir** · 探针／getcwd／chdir／pid **0** · L2 **5／5**。**9.1.3 四 API 收**。残：getenv／Darwin／Win。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary。下一刀：getenv 族。 |
| 2026-09-01 | **feat(selfhost)** **stage9 9.1.3 slice0 getpid／getppid 离 libc** · tip **WIP** | **代码＋探针＋文档** · **不升钉**。五问：产生＝`process_getpid_impl`→libc getpid；存储＝process.o merge；消费＝`std_process_getpid`。**修**：G.7 Linux x86／a64 内联 `syscall`／`svc`；`linux_getpid` API；mod.x linux cfg。证：Ubuntu process_merge · **无 U getpid** · objdump `syscall` · 探针／getpid／getppid／raw_syscall **0** · L2 **5／5**。残：getcwd／getenv／Darwin／Win。钉盘仍 **`e8176cbe5`**。PLATFORM: LINUX primary · SHARED face。下一刀：getcwd／getenv。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.2 slice2 arm64 x6／x7** · tip **WIP** | **代码＋探针＋文档** · **不升钉**。五问：产生＝名表仅 x0..x5＋x8；存储＝`mk<=6`；消费＝x6／x7 拒。**修**：G.7 开 mk 7／8＋`mk<=8`（.x＋seed）。证：Ubuntu g05 · x2／x6／x7／x1 encode OK · rdi／readonly **42** · L2 **5／5**。**AAPCS GP 面收**。残：qemu／10.2.3／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · aarch64 encode。下一刀：qemu／Win／NT。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.2 slice1 arm64 lateout GP** · tip **WIP** | **代码＋探针＋文档** · **不升钉**。五问：产生＝`mov_arg_reg_to_rax_arch` ta==1→−1；存储＝无 `enc_mov_arg_reg_to_rax`；消费＝lateout x1 失败。**修**：G.7 `arch_arm64_enc_enc_mov_arg_reg_to_rax`（mov_xn_xm 族）＋enc_dispatch／thin／surface／seed twin ta==1。证：Ubuntu g05 · in／lateout x1 encode **e1 03 00 aa／nop／e0 03 01 aa** · x8／rdi／readonly／L2 **5／5**。残：更多 GP／qemu／10.2.3／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · aarch64 encode。下一刀：多 GP／Win／NT。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.1 slice15–16 readonly／pure** · tip **WIP** | **代码＋探针＋文档** · **不升钉**。五问：产生＝readonly／pure 仅解析；存储＝`opt_bits` 8／16；消费＝lateout 仍 store；pure+noreturn 语义冲突。**修**：G.7 存拒门抬至 `opt_bits&28`；pure+noreturn→try_emit−1。证：Ubuntu g05 · readonly／pure **42** · lateout／pure+noreturn **build=1** · noreturn alone／邻域 **42** · L2 **5／5**。**options 族大体收**。残：10.2.3／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：Win asm／NT／Darwin／BC。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.1 slice14 nomem** · tip **WIP** | **代码＋探针＋文档** · **不升钉**。五问：产生＝nomem 仅解析；存储＝`opt_bits&4`；消费＝lateout 仍 store 栈。**修**：G.7 非 `_` out／lateout → try_emit−1；`_` 放行。证：Ubuntu g05 · nomem／clobber **42** · lateout 负例 **build=1** · 邻域 **42** · L2 **5／5**。残：readonly／pure／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：readonly／pure。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.1 slice13 nostack** · tip **WIP** | **代码＋探针＋文档** · **不升钉**。五问：产生＝nostack 仅解析；存储＝`opt_bits&1`；消费＝与 preserves_flags wrap 抢栈。**修**：G.7 nostack 跳过 pushfq／popfq（盖过 `&2`）；alone accept。证：Ubuntu g05 · `asm_nostack_smoke`／nostack+pf **42** 无 wrap · pf-only 仍 wrap · clobber **42** · L2 **5／5**。残：nomem／readonly／pure／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED · LINUX\|x86_64。下一刀：其余 options。 |
| 2026-09-01 | **docs(selfhost)** **日切归档 8/31（113 行）＋主文档 9/1 重写** · tip **WIP** | **文档** · **不升钉**。[自举进度-归档-2026-08-31.md](自举进度-归档-2026-08-31.md)；主文档仅当日 §6。PLATFORM: SHARED。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.1 slice12 preserves_flags** · tip **WIP**（基线 `0a2df1ee4`） | **代码＋探针＋文档** · **不升钉**。五问：产生＝options 位已存、除 noreturn 无 emit；存储＝`call_num_type_args`；消费＝无 flags 保存。**修**：G.7 同 `opt_bits` 读；`&2` → 模板前 `9c`／后 `9d`（x86）；aarch64 accept-only。证：Ubuntu seed host-cc＋g05 · `asm_preserves_flags_smoke` **42** · objdump `pushf;nop;popf` · 邻域 **42** · L2 **5／5**。残：nostack／nomem／readonly／pure／10.2.3／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED · LINUX\|x86_64 gold。下一刀：其余 options／NT。 |
| 2026-09-01 | **docs(selfhost)** **tip-align slice11 CFG-join noreturn** · tip **`0a2df1ee4`** | **文档** · **不升钉**。代码已合入 `409ff0988`＋探针／unsafe 臂修正；KPI／C迁移去掉「CFG 臂」残。PLATFORM: SHARED。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.1 slice10 lateout x8** · tip **`381431c00`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝slice7 拒 mk==102 out；存储＝同 regpack；消费＝缺 x8→x0。**修**：G.7 `enc_mov_x8_to_rax`（mov_xn_xm 族）＋out 放行 mk==102。证：Ubuntu＠**`381431c00`** `-target aarch64` LE **e8 03 00 aa / 1f 20 03 d5 / e0 03 08 aa** · 邻域 r10／noreturn **42** · L2 **5／5**。残：CFG 臂／其余 options／10.2.3／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · LINUX\|aarch64 encode。下一刀：options／NT。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.1 slice9 noreturn 块截断** · tip **`fdd4fbfcc`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝slice8 仍 emit fallthrough `return`；存储＝无 diverged；消费＝objdump `ud2` 后 `mov $1`。**修**：G.7 `glue_asm_block_diverged_*`＋pass1／final_expr 跳过；call_dispatch ud2 后 stamp。证：Ubuntu＠**`fdd4fbfcc`** **42** · `syscall; ud2`（无 `mov $0x1`）· 邻域 clobber／r10 **42** · L2 **5／5**。重建须 `XLANG_HOST_CC_SEED_FORCE=1 try-pipeline-abi-prefer`（禁 inject-only 漏 mega）。残：CFG 臂／其余 options／x8 out／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：x8 out／NT。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.1 slice8 options noreturn** · tip **`ea8dfd2d0`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝options 仅解析；存储＝无位；消费＝无 emit。**修**：G.7 `call_num_type_args` 位表＋noreturn→`ud2`；探针 rdi←42 先于 rax←60。证：Ubuntu＠**`ea8dfd2d0`** **42** · `mov $0x2a,%eax; mov %rax,%rdi; mov $0x3c; syscall; ud2` · 邻域 clobber／r10 **42** · L2 **5／5**。残：block 截断／其余 options／x8 out／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED · LINUX\|x86_64 noreturn. 下一刀：block 截断／NT。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.1 slice7 lateout r10** · tip **`e41625b57`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝slice6 拒 mk==101 out；存储＝同 regpack；消费＝缺 r10→rax。**修**：G.7 补 `enc_mov_r10_to_rax`（4C 89 D0）＋out 放行 mk==101。证：Ubuntu＠**`e41625b57`** **42** · `mov %rax,%r10; nop; mov %r10,%rax; store` · 邻域 clobber／lateout／rdi／syscall **42** · L2 **5／5**。**10.1.3 NT 暂缓**：`windows-server` 现指向 Linux，无 Windows 金标。残：options 语义／x8 out／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED · LINUX\|x86_64 r10。下一刀：options 语义／NT（须 Windows）。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.1 slice6 clobber `_`／options** · tip **`fc6445cc3`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝拒 `_`／options；存储＝无 discard／无 options 尾；消费＝store 误绑 `_`。**修**：G.7 parser `_`＋options 名表；emit 跳 store；typeck 跳 discard。证：Ubuntu＠**`fc6445cc3`** **42** · 单 store（无 rdi mov）· 邻域 lateout／rdi／syscall／nop **42** · L2 **5／5**。残：options 语义／r10 out／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：NT／options 语义。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.1 slice5 lateout SysV GP** · tip **`405e38c9b`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝slice4 仅 mk==0；存储＝同 int_val／regpack；消费＝store 前缺 GP→rax。**修**：G.7 复用 `mov_arg_reg_to_rax`／`mov_rbx_to_rax`。证：Ubuntu＠**`405e38c9b`** **42** · `mov %rax,%rdi; nop; mov %rdi,%rax; store` · 邻域 lateout／syscall／multi／nop **42** · L2 **5／5**。残：clobber／options／r10 out／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED · LINUX\|x86_64 gold out-GP。下一刀：clobber／NT。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.1 slice4 `lateout`／`out`** · tip **`bddb0113e`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝仅 in；存储＝`int_val=num_in`＋outs 在 `call_args[num_in..)`；消费＝store 回本地 VAR。**修**：G.7 parser out／lateout＋try_emit mk==0 store＋seed twin。证：Ubuntu＠**`bddb0113e`** **42** · `mov $7; nop; mov %rax,-0x18(%rbp)` · 邻域 syscall／multi／nop **42** · L2 **5／5**。残：非 rax out／clobber／options／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：非 rax out／clobber／NT。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.1 slice3 `asm!("syscall")`** · tip **`32f911a3b`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝仅 nop 模板；存储＝同 try_emit；消费＝0F05／svc。**修**：G.7 复用 `enc_syscall`／`enc_svc`＋r10／x8。证：Ubuntu＠**`32f911a3b`** **42** · `mov $0x27; 0f 05` · a64 LE svc · 邻域 multi／nop **42** · L2 **5／5**。残：out／clobber／options／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED · LINUX syscall ABI。下一刀：out／NT。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.1 slice2 多 `in`＋rax→GP** · tip **`b278f5c40`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝slice1 拒第 2 个 COMMA；存储＝call_arg＋逗号 reg pack；消费＝emit＋mov。**修**：G.7 抬至 6 `in`＋`mov_rax_to_arg_reg`／rbx。证：Ubuntu＠**`b278f5c40`** **42** · `mov $1; mov $2; mov %rax,%rdi; nop` · 邻域 in1／nop **42** · L2 **5／5**。残：out／clobber／syscall／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：syscall 模板／out／NT。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.1 slice1 `asm!(…, in("rax") expr)`** · tip **`76635cba3`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝slice0 拒 COMMA；存储＝call_arg＋method_call_name；消费＝emit_expr＋nop。**修**：G.7 parser 补 COMMA `in`＋typeck 操作数＋try_emit ctx＋thin。证：Ubuntu＠**`76635cba3`** **42** · `mov $0x7,%eax`＋`nop` · 邻域 nop **42** · L2 **5／5**。残：多操作数／out／clobber／options／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：多 `in`／`out`／NT。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.2 slice0 arm64 `asm!("nop")` 操作码** · tip **`146ad835a`** | **探针＋文档** · **不升钉** · **git-only**。五问：产生＝10.2.1 已 emit ta==1；存储＝同 try_emit；消费＝`-target aarch64` `.o`。**修**：G.7 **无新发射器**；探针 `asm_nop_arm64_smoke.x`。证：Ubuntu＠**`146ad835a`** LE **`1f 20 03 d5`** · x86 **42** · L2 **5／5**。残：操作数／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · aarch64 encode。下一刀：操作数／NT。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.2.1 slice0 `asm!("nop")`** · tip **`94c196e04`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝parser 不收 `asm!`（P001）；存储＝`ExprKind.EXPR_ASM=60`＋`var_name` 模板；消费＝`emit_expr_elf_rec`＋`try_emit`。**修**：G.7 primary_slice＋typeck unsafe void＋call_dispatch emit（into 非 VAR-only len）＋pabi asm-expr-thin。证：Ubuntu＠**`94c196e04`** **42** · objdump `nop` · L2 **5／5**。残：操作数／options／arm64 运行时／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · LINUX\|x86_64 gold。下一刀：操作数／10.2.2／NT。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.4.1 arm64 语言原子 i16（原子面收口）** · tip **`a2277e5e3`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝arm64 缺 i16；存储＝enc＋`try_emit` which 10–12；消费＝`-target aarch64` `.o`。**修**：G.7 同 intercept（`ldarh`／`stlrh`／`casalh`）。证：Ubuntu＠**`a2277e5e3`** i16/i32/i64+dmb 操作码 OK · x86 i16 **42** · L2 **5／5**。残：asm!／NT／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · aarch64 runtime。下一刀：10.2 asm!／NT。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.4.1 arm64 语言原子 i64** · tip **`374aaef65`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝arm64 仅 i32；存储＝enc＋`try_emit` which 4–6；消费＝`-target aarch64` `.o`。**修**：G.7 同 intercept（`ldar/stlr/casal` X 形）。证：Ubuntu＠**`374aaef65`** i32＋i64＋dmb 操作码 OK · x86 i64 **42** · L2 **5／5**。残：arm64 i16／asm!／NT。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · aarch64 runtime。下一刀：asm!／NT／arm64 i16。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.4.1/2 arm64 语言原子 i32＋dmb** · tip **`f2cc0d8d6`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝atomic intercept 仅 ta==0；存储＝arm64 enc＋`try_emit`；消费＝`-target aarch64` `.o`。**修**：G.7 同 intercept 开 ta==1（`ldar`／`stlr`／`casal`／`dmb`；i16/i64 仍 fallthrough）。证：Ubuntu＠**`f2cc0d8d6`** 六操作码 OK · x86 邻域 **42** · L2 **5／5**。残：arm64 宽整数／asm!／NT。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · aarch64 runtime。下一刀：asm!／NT／arm64 i64。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.4.1 slice3 语言原子 i16** · tip **`d3054d960`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝仅 i32/i64；存储＝`builtin`＋`try_emit`＋enc；消费＝`xlang_asm -o`。**修**：G.7 同 intercept 扩 which 10–12（`movzwl`／`xchg %dx`／`lock cmpxchg` 16-bit；CAS ptr→rbx）。证：Ubuntu＠**`d3054d960`** **42** · objdump · i32／i64／fence 邻域 · L2 **5／5**。残：arm64／asm!／NT。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · LINUX\|x86_64。下一刀：arm64／asm!／NT。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.4.2 语言原子 fence（mfence／lfence／sfence）** · tip **`d39f619ee`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝10.4.2 无语言屏障；存储＝`std.atomic.builtin`＋`try_emit_atomic`＋enc；消费＝`xlang_asm -o`。**修**：G.7 补全既有 intercept（which 7–9、0-arg）。证：Ubuntu＠**`d39f619ee`** **42** · objdump `sfence`/`mfence`/`lfence` · i32／i64 邻域 · L2 **5／5**。残：arm64／i16／asm!／NT。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · LINUX\|x86_64。下一刀：arm64／asm!／NT。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.4.1 slice2 语言原子 i64 load／store／cas** · tip **`c32b2695b`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝slice1 仅 i32；存储＝同 `try_emit_atomic`＋REX.W enc；消费＝`xlang_asm -o`。**修**：G.7 补全既有 intercept（which 4–6）；CAS ptr∈rbx、*expected→rax 最后。证：Ubuntu＠**`c32b2695b`** i32／i64 **42** · 无 `atomic_*_c` · L2 **5／5**。残：arm64／fence／i16／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · LINUX\|x86_64。下一刀：fence／asm!／NT。 |
| 2026-09-01 | **feat(selfhost)** **stage10 10.4.1 slice1 语言原子 load／store／cas asm intercept** · tip **`4cd022b44`** | **代码＋探针＋文档** · **不升钉** · **git-only**。五问：产生＝slice0 仍依赖 C11 glue；存储＝`std.atomic.builtin`＋`try_emit_atomic_builtin_call_elf_c`＋x86 enc；消费＝产品 `xlang_asm -o`。**修**：G.7 raw_syscall 同形 CALL／METHOD 拦截；CAS **ptr→rbx**、***expected→eax 在 cmpxchg 前最后装**（禁 rax 兼指针毁 eax）。证：Ubuntu prefer enc＋call_dispatch＋g05＠**`4cd022b44`** · 探针 **42** · 无 `atomic_*_c` · L2 **5／5**。残：arm64／fence／宽整数／Darwin／NT。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED emit · LINUX\|x86_64 runtime。下一刀：扩宽／asm!／NT。 |
