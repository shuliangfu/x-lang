# Xlang / X language 自举进度

> **更新**：2026-08-02 · tip **wave1155-1157** BC 8.3.1 G.7 fold 3 clusters（call return-type + diag fmt + linear · 12 fns → 3 域文件 EOF · L2 双端 · **不升钉**）\
> **产品 L4 放行钉盘**：**`77b334842`**（wave942 Makefile 物理删除 + 双端 L4 真冷）· 前钉盘 **`9bb7a757c`**（wave710）\
> **产品 residual tip**：**`a4176904b`**（wave1155-1157 代码）· Stage 8 tip **713277ef9**（wave1041）· 文档 tip **4bacaf18c** · 钉盘仍 **`77b334842`**\
> **tip L4 安全网**：**`4bacaf18c`**（wave1022 双端 L4 + 全量 bstrict 129 OK · **不升钉**；前网 `f80aa4593`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：**MG ✅** · **BC 开**（wave1155-1157 G.7 fold 3 clusters · 12 fns → method_call/assign/check_block.c EOF · **extern 函数迁移启动**）· **Stage 8 开**（批次 2 wave1036 lsp_io + wave1038–1040 build_* 三件套 + wave1041 cfg_eval 退役 · 13/30）· 钉盘 `77b334842`\
> **下波候选**：**BC 8.3.1** 继续 extern 函数迁移 或 **8.3.3** soa·PC 或 ast_pool.c 叶迁移



> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[31](自举进度-归档-2026-07-31.md) · [30](自举进度-归档-2026-07-30.md) · [29](自举进度-归档-2026-07-29.md) · …

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `77b334842`**（wave942 · Makefile 物理删除 + 双端 L4 真冷）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（Makefile 已删除；**变更记录只在本文 §6**）。

| 指标 | 状态 |
|------|------|
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`77b334842`**（wave942）；residual tip **a4176904b**（wave1155-1157 call return-type + diag fmt + linear clusters fold）；Stage 8 tip **713277ef9**（wave1041 cfg_eval 退役）；文档 tip **4bacaf18c**；tip L4 安全网 **`4bacaf18c`**（**不升钉**） |
| Track MG | **11.3.1 MG ✅**（wave941 物理删 Makefile · wave942 catalog CFLAGS · 0 make in bootstrap） |
| Track BC | **开**（wave963 inventory · wave965–977 8.3.1 + wave978–994 8.3.2 + wave995–1021 … + **wave1022 slice reent** + **wave1023 var_decl+lazy_append** + **wave1024 slice dual-GP+slice_from_array** + **wave1025 sret return path** + **wave1026 field_access layout/offset** + **wave1027 lea/arm64 sret helpers** + **wave1028 async CPS emit domain** + **wave1029 x86 micro-encoders** + **wave1030 with_arena scope domain** + **wave1031 cmp cc helpers** + **wave1032 empty struct check** + **wave1033 TokenKind variant tag** + **wave1034 float bits lo/hi** + **wave1035 struct_lit field offset/type_ref** + **wave1037 16 个 try_index_var_* fwd 清理** + **wave1042 block_final_expr 同叶 fold** + **wave1043 array_let_empty_init 同叶 fold** + **wave1044 struct_layout_compute_field_offset 同叶 fold** + **wave1045 func_param_agg_byte_size 同叶 fold** + **wave1046 func_return_byte_size 同叶 fold** + **wave1047 func_param_home_width 同叶 fold** + **wave1048 call_return_byte_size 同叶 fold** + **wave1049 struct_layout_index_by_type_name 同叶 fold** + **wave1050 struct_layout_field_offset_by_name 同叶 fold** + **wave1051 struct_lit_value_bytes 同叶 fold** + **wave1052 sync_struct_layout_field_offsets 同叶 fold** + **wave1053 struct_layout_metrics + public wrapper 同叶 fold** + **wave1054 type_align_simple 同叶 fold** + **wave1055 array_temp_bytes 同叶 fold** + **wave1056 type_size_simple 同叶 fold（triad 收口）** + **wave1057 sysv_dual_gp_byte_size 同叶 fold** + **wave1058 store_retval_pair 同叶 fold** + **wave1059 func_param_is_f32/f64 同叶 fold + dead code delete** + **wave1060 deref_struct16_rax_ptr 同叶 fold** + **wave1061 call_struct16_ret_needs_rax_deref 同叶 fold** + **wave1062 call_return_type_kind_ord 同叶 fold** + **wave1063 call_param_type_ref_at 同叶 fold** + **wave1064 copy_large_struct_from_rax_ptr 同叶 fold** + **wave1065-1094 批量同叶 fold 30 叶**（assign-kind/import count/ctx reset/pair base/expr stmt/struct layout warn/block return scan/enum name match/call arg effective type/widen 四件套/type_refs_equal 域/alias resolver/let name match/import binding name 域/overload resolution 域）+ **wave1095-1099 generic call inference 域 5 叶同 fold** + **wave1100 modlet ELF emit 域 12 fns + typedef + macro + global 同 fold** + **wave1101-1110 6 域同 fold**（codegen outbuf／asm label format／wpo mono thunk／next-offset／fold primitives／count-up-while loop fold）+ **wave1111-1129 6 域 18 fns 同 fold**（dep return type mapping／struct layout name lookup／vector type-let／skipped-typeck backfill／struct same-shape／WPO-S3 stack-escape）+ **wave1130-1135 3 域 8 fns 同 fold**（float promote pair／linear name moved／lval param ptr field cluster）+ **wave1136-1137 stack-escape scan pair 同 fold**（scan_expr + scan_block → region_assign.c EOF）+ **wave1138-1140 spill byte cluster 同 fold**（3 fns + 3 macros → asm_emit_spill.c EOF）+ **wave1141-1145 fixed array helpers + call ptr compat 同 fold**（5 fns → vector_let.c EOF + method_call.c EOF）+ **wave1146-1149 TypeKind tag／debug reporter／bootstrap fixup／block locals tree 同 fold**（4 fns → cmp + method_call + block_inits EOF）+ **wave1150-1151 call-target resolver + f32 xmm param homing 同 fold**（2 fns → call_args.c EOF · GLUE_TYPECK_IMPORT_BINDING/SELECT enum 前移）+ **wave1152-1154 scope setter fold + dead code cleanup**（1 fn → block_body.c EOF · 2 dead fn 删除 · **glue.c 静态叶迁移基本收口**）+ **wave1155-1157 call return-type + diag fmt + linear clusters 同 fold**（12 fns → method_call + assign + check_block.c EOF · **extern 函数迁移启动**）） |
| Track L (Stage 8) | **开**（批次 0 清理 ✅ · 批次 2 **wave1036 lsp_io_gen + wave1038 build_gen + wave1039 build_runner_gen + wave1040 build_runtime_x_gen + wave1041 cfg_eval_gen 退役** ✅ · 13/30 retired · build_*_gen 三件套 + cfg_eval 全退役 · 详见 [阶段8高效退役分析.md](阶段8高效退役分析.md)） |
| mac / Ubuntu L4（钉盘） | ✅ Ubuntu L4 真冷（wipe + bootstrap + build + smoke）；✅ macOS build |
| mac / Ubuntu L4（tip 安全网） | ✅ `/tmp/mac_true_cold_4bacaf18c.log` · `/tmp/mac_true_bstrict_4bacaf18c.log` · `/tmp/ubuntu_true_cold_4bacaf18c.log` · `/tmp/ubuntu_true_bstrict_4bacaf18c.log`（**双端 129 OK**） |
| 本波构建级别 | tip 安全网 **L4** @ `4bacaf18c`（不升钉）；日常 BC 仍 L2；钉盘仍 `77b334842` |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录** |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图 |
| [自举进度-归档-2026-07-31.md](自举进度-归档-2026-07-31.md) | **31 日全日 wave895–963**（MG 真删 + post-delete + 开 BC） |
| [自举进度-归档-2026-07-30.md](自举进度-归档-2026-07-30.md) | **30 日全日 wave758–894**（只读） |
| [自举进度-归档-2026-07-29.md](自举进度-归档-2026-07-29.md) | **29 日** wave618–757 + tip L4 升钉 `9bb7a757c` |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单 |
| 产品 L4 放行钉盘 | **`77b334842`** | 31 日 wave942 升钉；Makefile 物理删除 + 双端 L4 真冷 |
| tip L4 安全网 | **`4bacaf18c`** | 01 日 wave1022 后双端 L4 + bstrict **129** · **钉盘不升**（前 `f80aa4593`） |
| residual tip（BC） | **`a4176904b`** | wave1155-1157 代码（call return-type + diag fmt + linear clusters fold · 12 fns → method_call + assign + check_block.c EOF · extern 函数迁移启动）；钉盘仍 `77b334842` |
| mac 钉盘 L4 | ✅ build exit=0 | macOS Darwin/arm64 g05 relink OK |
| Ubuntu 钉盘 L4（金标） | ✅ L4 真冷 | Ubuntu Linux/x86_64 wipe + bootstrap + build + smoke 全绿 |
| 产品矩阵（tip L4） | ✅ rv42 / option / si / hello | 双端 L4 后矩阵全绿（Hello `0a`） |
| 前钉 | `77b334842` · `9bb7a757c` · `4fa4f07e7` · … | wave942 → wave710 → … |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 18 号结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **R2 / Cap** | wave710 升钉后按需 | 明细见 29 归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1**（已执行） |
| `TREE_ARMED` | **1** |
| `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1**（wave941 物理删除） |
| `PHYS_DEL_WINDOWS_GATE_STATUS` | `reproven_green` @ `71dd60a12`（wave922 · wave940 一键冷启动） |
| preflight blockers | **全清零**（wave937–939） |
| 真删条件 | ✅ 已完成 @ wave941/942 · 钉盘 **`77b334842`** |
| post-delete residual | **✅ 已收**（wave944–964） |

***

## 3. 当前前排（≤3 条）

1. **BC 轨（wave963 开 · wave965–1137）** — 8.3.1 typeck 六域 + asm emit **七十刀**（+…／array_lit durable／**slice reent deep-copy**／**var_decl+lazy_append 共享叶**／**slice dual-GP+slice_from_array 同叶**／**lea/arm64 sret helpers 同叶**／**async CPS emit domain 同叶**／**x86 micro-encoders 同叶**／**with_arena scope domain 同叶**／**cmp cc helpers 同叶**／**empty struct check 同叶**／**TokenKind variant tag 同叶**／**float bits lo/hi 同叶**／**struct_lit field offset/type_ref 同叶**／**block_final_expr 同叶（零自调用）**／**array_let_empty_init 同叶**／**struct_layout_compute_field_offset 同叶**／**func_param_agg_byte_size 同叶**／**func_return_byte_size 同叶**／**func_param_home_width 同叶**／**call_return_byte_size 同叶**／**struct_layout_index_by_type_name 同叶**／**struct_layout_field_offset_by_name 同叶**／**struct_lit_value_bytes 同叶**／**sync_struct_layout_field_offsets 同叶**／**struct_layout_metrics + public wrapper 同叶**／**type_align_simple 同叶**／**array_temp_bytes 同叶**／**type_size_simple 同叶（triad 收口）**／**sysv_dual_gp_byte_size 同叶（SysV ABI 分类域）**／**store_retval_pair 同叶（retval store 域）**／**func_param_is_f32/f64 同叶 + dead code delete（SysV param classification 域）**／**deref_struct16_rax_ptr 同叶（struct16 retval deref 域）**／**call_struct16_ret_needs_rax_deref 同叶（struct16 retval deref classifier 域）**／**call_return_type_kind_ord 同叶（call return TypeKind resolve 域）**／**call_param_type_ref_at 同叶（call param type_ref resolve 域）**／**copy_large_struct_from_rax_ptr 同叶（>16B struct retval memcpy 域）** 有则补全 + wave1065-1094 批量 30 叶 + wave1095-1099 generic call inference 域 5 叶 + **wave1100 modlet ELF emit 域 12 fns + typedef + macro + global** + wave1101-1110 6 域 + wave1111-1129 6 域 18 fns + **wave1130-1135 3 域 8 fns**（float promote pair／linear name moved／lval param ptr field cluster）+ **wave1136-1137 stack-escape scan pair**（scan_expr + scan_block → region_assign.c EOF）+ **wave1138-1140 spill byte cluster**（3 fns + 3 macros → asm_emit_spill.c EOF）+ **wave1141-1145 fixed array helpers + call ptr compat**（5 fns → vector_let.c EOF + method_call.c EOF）+ **wave1146-1149 TypeKind tag／debug reporter／bootstrap fixup／block locals tree**（4 fns → cmp + method_call + block_inits EOF）+ **wave1150-1151 call-target resolver + f32 xmm param homing**（2 fns → call_args.c EOF · GLUE_TYPECK_IMPORT_BINDING/SELECT enum 前移）+ **wave1152-1154 scope setter fold + dead code cleanup**（1 fn → block_body.c EOF · 2 dead fn 删除 · **glue.c 静态叶迁移基本收口** — 剩余 5 个 static 定义均为基础设施级（ctx_layout／arena accessor／var stack off／type kind 映射），跨 20+ 域文件调用，不适合单域迁移）+ **wave1155-1157 call return-type + diag fmt + linear clusters 同 fold**（12 fns → method_call + assign + check_block.c EOF · **extern 函数迁移启动**））+ 8.3.2 …+top_level residual 同 TU thin；host-cc 仍编 **pipeline_glue + ast_pool** 等大叶；下一刀 **继续 extern 函数迁移** 或 **8.3.3 field_access／soa** 或 **ast_pool.c 叶迁移**。
2. **post-delete residual**：**✅ 已收**（wave944–**964**）。
3. **PC 轨** — 用户默认仍可能 `labi_invoke_cc`；默认 asm backend 终局 ⬜。

***

## 4. 与归档的关系

- **31 日归档**：[自举进度-归档-2026-07-31.md](自举进度-归档-2026-07-31.md) — wave895–963（B7B multi-target → 双端 L4 → 物理删 Makefile → post-delete residual → 开 BC inventory）。
- **30 日归档**：[自举进度-归档-2026-07-30.md](自举进度-归档-2026-07-30.md) — wave758–894。
- **29 日归档**：[自举进度-归档-2026-07-29.md](自举进度-归档-2026-07-29.md) — wave618–757 + 升钉 `9bb7a757c`。
- **勿**用归档 tip 或旧钉冒充现行钉盘。**现行产品钉盘 = `77b334842`**；tip L4 安全网 **`4bacaf18c`**（不升钉）。
- 归楼后主文档只保留 KPI + 前排 + **仅 8 月 1 日** §6；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
|------|----------|
| **macOS arm64** | 钉盘 build ✅ · tip L4+bstrict 129 ✅ @ `4bacaf18c` · Makefile 已物理删除 |
| **Ubuntu x86_64（金标）** | 钉盘 L4 真冷 ✅ @ `77b334842` · tip 安全网 L4+bstrict 129 ✅ @ `4bacaf18c` · Makefile 已物理删除 |
| **Windows MSYS2** | wave940 一键冷启动绿 · STATUS=`reproven_green` · Makefile 已物理删除 |

***

## 6. 变更记录（倒序 · 8 月 1-2 日；31 日见归档）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-08-02 | **wave1155-1157** · BC 8.3.1 · G.7 fold 3 clusters（call return-type + diag fmt + linear · 12 fns → 3 域文件 EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同叶 fold 12 个函数自 `pipeline_glue.c` 迁入 3 个已有域文件 EOF（同 TU #include）：wave1155 `pipeline_typeck_resolve_call_callee_return_type_c`（CALL/METHOD_CALL callee 返回类型 resolver · 4 路解析：whole-import call → import binding FIELD_ACCESS → local module find_func_return_type → dep scan · ~167 LOC → `pipeline_typeck_method_call.c` EOF，#include @ L10186；colocated with `resolve_call_func_index_c` wave1085-1094 — twin of call-target resolver pair（func_index + return_type）；sole glue.c callsite at L10353 在 #include 后故可见；deps 含 `GLUE_TYPECK_IMPORT_BINDING/SELECT` enum @ L2241 < #include）；wave1156 typeck diag fmt cluster（5 fns ＝ `diag_append_lit_c`／`diag_append_u32_dec_c`／`diag_fmt_type_at_c`／`diag_fmt_type_into_c`／`diag_fmt_type_or_question_c` · ~145 LOC → `pipeline_typeck_assign.c` EOF，#include @ L8265；colocated with assign mismatch diag 域 — 8 callsites in assign.c lines 265-336；sole glue.c callsite at L8142 在 #include 前故 glue.c L8013 加 extern fwd decl；assign.c 顶部加 2 extern fwd decl 供 callsites L265-336 在 EOF 定义前可见）；wave1157 linear type use-once move tracking cluster（6 fns ＝ `set_active_ctx_c`／`active_module_c`／`linear_reset_c`／`linear_use_var_c`／`linear_accepts_init_c`／`reject_addr_of_linear_c` · ~125 LOC → `pipeline_typeck_check_block.c` EOF，#include @ L10250；colocated with `typeck_linear_name_already_moved` wave1132 + check_block walker 域；wave1132 static fwd decl 删除（sole caller `linear_use_var_c` 现同文件 — direct call）；glue.c L6099 加 extern fwd decl 供 `set_active_ctx_c` callsite @ L6109 在 #include 前可见；`linear_reset_c` callsites @ L10267/10331/10398 在 #include 后故可见；globals `g_typeck_linear_moved_{n,names,lens}` + `g_typeck_active_ctx` 留 glue.c — 经 #include 可见）。**extern 函数迁移启动**：3 wave 中 4 个函数为 extern（非 static），跨 TU 调用（typeck.x／ast_pool.c／seeds）；通过 extern fwd decl 维持可见性。G.9 详细英文 docblock 全覆盖（用途／依赖／Caller／Invariant／PLATFORM）。COUNT 仍 62；glue 11126→10633（-493 净；含 docblock）；method_call +217 / assign +207 / check_block +183（含 G.9 docblock）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED。Tip **a4176904b** |
| 2026-08-02 | **wave1152-1154** · BC 8.3.1 · G.7 fold scope setter + dead code cleanup（1 fn → block_body.c EOF · 2 dead fn 删除） | **代码** · **不升钉** · **L2 双端**。G.7 同叶 fold + 死代码清理：wave1152 `glue_asm_ctx_set_scope_block`（3 LOC，block scope setter → `pipeline_asm_emit_block_body.c` EOF，#include @ L3623；colocated with block_body_sync 域 — 全部 13 callsites 在 block_body.c（3）／block_if_stmt.c（4，#include L3626 > 3623）／fold_count_up_while.c（5，#include ~5199 > 3623）／glue.c:5121（> 3623）；static fwd decl at block_body.c:53 供内部 callsites lines 295/700/865 在 EOF 定义前可见；deps `g_pipeline_asm_emit_scope_block`（L182）+ `asm_ctx_set_scope_block`（L191）均在 #include L3623 前声明故可见）；wave1153 删除 `parser_diagnostic_parse_commit_shape`（~21 LOC，static 壳函数委托 extern `driver_diagnostic_parse_commit_shape`；glue.c TU 内零调用 — 被 `runtime_driver_diagnostic.x` export 函数独立链接取代）；wave1154 `#if 0` 注释 `glue_emit_block_stmt_order_let_const_elf`（~134 LOC，glue.c TU 内零调用 — 被 `backend_emit_loop_body_content_elf_sync` + `block_body_sync_elf` 同路径取代）。**glue.c 静态叶迁移基本收口**：剩余 5 个 static 定义均为基础设施级（`pipeline_asm_ctx_layout` 59 callsites/20+ 域文件 · `glue_arena_expr_kind_at_ref` 3 callsites 全在 glue.c · `glue_var_expr_stack_off_elf_c` 100+ callsites/8 域文件 · `glue_arena_expr_at_ref` 83 callsites/5 域文件 · `glue_type_kind_from_ord` 4 callsites 全在 glue.c），跨多域调用不适合单域迁移。G.9 详细英文 docblock 全覆盖。COUNT 仍 62；glue 11137→11126（-11 净；含 #if 0 块保留）；block_body 1053→1089（+36）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED。Tip **1fb1a15b7** |
| 2026-08-02 | **wave1150-1151** · BC 8.3.1 · G.7 fold call-target resolver + f32 xmm param homing（2 fns → call_args.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同叶 fold 2 个静态函数自 `pipeline_glue.c` 迁入 `pipeline_asm_emit_call_args.c` EOF（同 TU #include @ L2251）：wave1150 `glue_asm_resolve_call_target_module_c`（CALL/METHOD_CALL 目标 module/func_index/dep_ix resolver · 5 路解析：pre-resolved func_index → typeck resolve_call_func_index → import binding METHOD_CALL（math.floor）→ import binding FIELD_ACCESS（CALL+FIELD_ACCESS 形）→ linear dep scan；是 call-arg packing 域的 dispatch twin — 所有 call-arg emitter 调此 resolver 取 mod/func_ix/dep_ix；`GLUE_TYPECK_IMPORT_BINDING/SELECT` enum 自 glue.c L8947 前移至 L2241（call_args.c #include L2251 前）使迁移函数可见；新增 extern fwd decl 7 个：`pipeline_typeck_find_func_return_type_in_module_c`（def glue.c L8926 > #include）／`_by_name_c`／`parser_get_module_num_imports`／`pipeline_expr_method_call_name_len/into`／`pipeline_module_import_kind_at/binding_name_len/binding_name_byte_at`；新增 static fwd decl `pipeline_typeck_resolve_call_func_index_c`（def method_call.c #include @ L10491 > call_args.c #include @ L2251）；struct_let.c:78 static fwd decl 注释更新指向新位置）；wave1151 `pipeline_asm_emit_param_home_elf_sysv_f32_xmm_c`（SysV x86 gp/xmm split-track param homing · XLANG_ABI_F32_XMM=1 · callee-side twin of call-arg packing · 直接消费 glue_func_param_agg_byte_size_c（wave1045）／glue_func_param_home_width_c（wave1047）／glue_func_param_is_f32_c／glue_func_param_is_f64_c（wave1059，全在 call_args.c 同叶）；sole caller `pipeline_asm_emit_param_home_elf_c` 在 glue.c L4742 > #include L2251 故无需 static fwd decl）。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl。G.9 详细英文 docblock 全覆盖（用途／依赖／Caller／Invariant／PLATFORM）。COUNT 仍 62；glue 11866→11137（-729 含 wave1146-1149）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED + LINUX+MACOS x86_64 SysV（f32 xmm split-track x86-only）。Tip **cda5f6429** |
| 2026-08-02 | **wave1146-1149** · BC 8.3.1 · G.7 fold 4 leaves（TypeKind tag／debug reporter／bootstrap fixup／block locals tree） | **代码** · **不升钉** · **L2 双端**。G.7 同叶 fold 4 个静态函数自 `pipeline_glue.c` 迁入 3 个已有域文件 EOF（同 TU #include）：wave1146 `pipeline_asm_typekind_variant_tag`（37 LOC，TypeKind name→tag 字节比较表 → `pipeline_asm_emit_cmp.c` EOF，#include @ L3547；glue.c L1958 保留 static fwd decl 供 field_access.c（#include L2281）+ cmp.c（#include L3547）callsites 在 #include 前可见；纯 memcmp buf scan 无 deps）；wave1147 `debug_try_propagate_report_glue_c`（37 LOC，debug-point D reporter → `pipeline_typeck_method_call.c` EOF，#include @ L10525；glue.c L10377 加 static fwd decl 供 sole caller `pipeline_typeck_check_expr_try_propagate_c` @ L10452 在 #include 前可见；deps: link_abi_getenv/link_abi_system extern + libc）；wave1148 `pipeline_typeck_bootstrap_expr_fixup_c`（37 LOC，METHOD_CALL i32.double fixup + generic CALL mono fallback → method_call.c EOF；colocated with glue_generic_call_fixup_resolved_type_c wave1096 同文件；glue.c L8131 保留 static fwd decl 供 sole caller `pipeline_typeck_check_expr_return_c` @ L8191 在 #include 前可见）；wave1149 `pipeline_asm_fill_block_locals_tree`（21 LOC）+ `glue_asm_init_expr_reserve_stack_bytes`（9 LOC）→ `pipeline_asm_emit_block_inits.c` EOF，#include @ L3617；`g_pipeline_asm_emit_module` 直接写替换为 `pipeline_asm_emit_set_module()` public setter（G.7 单权威）；block_inits.c 加 extern fwd decl for setter（def glue.c L4247 > #include）；glue.c L1672 加 static fwd decl for `glue_asm_init_expr_reserve_stack_bytes` 供 sole caller @ L2106 在 #include 前。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl。G.9 详细英文 docblock 全覆盖（用途／依赖／Caller／Invariant／PLATFORM）。COUNT 仍 62；不改语义；不升钉。mac L2：pipeline_x · g05 pure-ld（mac 60 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED。Tip **56447fd9f** |
| 2026-08-02 | **wave1141-1145** · BC 8.3.1 · G.7 fold fixed array helpers cluster + call ptr compat check（5 fns → vector_let.c EOF + method_call.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同叶 fold 5 个静态函数自 `pipeline_glue.c` 迁入 2 个已有域文件 EOF（同 TU #include）：wave1141-1144 fixed TYPE_ARRAY local let helpers cluster（4 fns ＝ `glue_type_is_fixed_array`／`glue_emit_fixed_array_type_let_init_elf_c`／`glue_block_let_is_fixed_array_type`／`glue_fixed_array_let_init_uses_direct_slot` → `pipeline_asm_emit_vector_let.c` EOF，#include @ L2063；colocated with `glue_struct_lit_store_fixed_array_field_elf_c`（vector_let.c L347，被 glue_emit_fixed_array_type_let_init_elf_c 调用）；`GLUE_TYPE_KIND_ARRAY` 宏替换为字面量 10 — 宏定义在 glue.c L2076 晚于 vector_let.c #include L2063 故不可见，宏保留在 glue.c 供 struct_let/index_helpers/spill/modlet/assign/array_lit/index/vector_simd/block_inits/field_access（均 #include 在 L2076 后）使用；与 vector_let.c 现有 L59/64/101/113/123/205/213/245/359 使用字面量 45/46 风格一致）；wave1145 `typeck_check_call_ptr_struct_compat_c`（MOD-02 *Struct 形参 vs 实参兼容性检查 → `pipeline_typeck_method_call.c` EOF，#include @ L10585；colocated with overload resolution 域 wave1089-1094 + import binding name 域 wave1085-1088；sole caller `pipeline_typeck_check_call_slice_region_c` 在 glue.c L10313 早于 #include 故 glue.c L10212 加 static fwd decl）。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl。G.9 详细英文 docblock 全覆盖（用途／依赖／Caller／Invariant／PLATFORM）。COUNT 仍 62；glue 11886→11866（-20）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED. Tip **d76801d5f** |
| 2026-08-02 | **wave1138-1140** · BC 8.3.1 · G.7 fold spill byte cluster（3 fns + 3 macros → asm_emit_spill.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同叶 fold 3 个静态函数 + 3 个宏自 `pipeline_glue.c` 迁入 `pipeline_asm_emit_spill.c` EOF（同 TU #include @ L2181）：wave1138 `glue_asm_sum_expr_call_spill_bytes`（递归 expr walker · CALL(48)／METHOD_CALL(49) 每 reg-class arg 32B 无回收 · binop/unary/index/field/if/array_lit/struct_lit 递归 · visit cap 65536）；wave1139 `glue_sum_block_slice_reent_dc_bytes_c`（迭代块遍历 · stack[256] · const/let/expr_stmt/final/while/for/if/region 子节点 · visit cap 65536）；wave1140 `glue_asm_sum_block_call_spill_bytes`（迭代块遍历 · stack[256] · const/let/expr_stmt/final/if/while/for 子节点 · 经 glue_asm_sum_expr_call_spill_bytes 汇总 expr spill）。3 宏：`GLUE_ASM_CALL_SPILL_SLOT_BYTES` (32)／`GLUE_ASM_FRAME_WALK_VISIT_MAX` (65536)／`GLUE_ASM_FRAME_WALK_DEPTH_MAX` (256)。Colocated with 7.3 Chaitin spill 域（pipeline_asm_emit_spill.c）— 所有 frame-size 估算逻辑单文件。Caller `pipeline_asm_compute_frame_size_c` 仍留 glue.c L4807。glue.c 原定义 + 宏删除（L4498-4805）+ 迁移标记 L4498。spill.c 加 extern fwd decl for 跨 TU 不可见符号：`ast_pipeline_block_if_cond_ref`（定义 ast_pool.c）／`pipeline_expr_method_call_*`（定义 glue.c 后部）／`pipeline_block_while/for/region_*`（定义 ast_pool.c，glue.c L4503-4510 亦有 fwd）。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl。G.9 详细英文 docblock 全覆盖（用途／依赖／Caller／PLATFORM）。COUNT 仍 62；glue 12044→11886（-158）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED. Tip **5ed985ed9** |
| 2026-08-02 | **wave1136-1137** · BC 8.3.1 · G.7 fold stack-escape scan pair（scan_expr + scan_block → region_assign.c EOF） | **代码** · **不升钉** · **L2 双端**。G.7 同叶 fold 2 个静态函数自 `pipeline_glue.c` 迁入 `pipeline_typeck_region_assign.c` EOF（同 TU #include @ L9934）：wave1136 `typeck_scan_expr_stack_escape_c`（单 expr assign/call/return 栈逃逸扫描 · dispatches by expr kind → check_struct_stack_escape_assign／check_scope_borrow_assign／check_allocator_region_assign／check_scope_borrow_return／check_allocator_region_return／check_return_slice_region_in_scope／check_return_slice_region／check_call_struct_stack_escape — 全部定义在 region_assign.c 同域）；wave1137 `typeck_scan_block_stack_escape_c`（递归块扫描 · walk expr_stmts + final_expr + stmt_order: while/for/if-then-else/region · region 处理镜像 check_block_one_region: with_arena cap push/pop + labeled scope_region push/pop + unsafe_depth push/pop Cap-T001）。scan_block 递归 → static fwd decl at L933（定义 at L1038）。公共入口 `pipeline_typeck_scan_module_struct_stack_escape_c`（glue.c L10056）保留在 glue.c — 直接操作 globals `g_typeck_with_arena_scope_n`／`g_typeck_region_scope_n`；经 #include L9934 可见 scan_block。glue.c 原定义删除 + 迁移标记（L10010 wave1136 marker · L10051 wave1137 marker）。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl。G.9 详细英文 docblock 全覆盖（用途／契约／Invariant／PLATFORM）。COUNT 仍 62；glue 12044→11886（-158）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED. Tip **e2040c99a** |
| 2026-08-02 | **wave1130-1135** · BC 8.3.1 · 批量 G.7 fold 8 leaves（3 域：float promote pair／linear name moved／lval param ptr field cluster） | **代码** · **不升钉** · **L2 双端**。G.7 批量同叶 fold 8 个静态函数自 `pipeline_glue.c` 迁入 3 个已有域文件 EOF（同 TU #include）：wave1130-1131 float promote pair（2 fns ＝ `glue_maybe_promote_f32_to_f64_rax_elf_c`／`glue_float_promote_src_ty_ref_c` → `pipeline_asm_emit_return.c` EOF，#include @ L1913；colocated with wave314 return float-widen callsites L542/543/630/631/640/641；return.c L58-62 加 3 static fwd decl 含 `pipeline_typeck_float_widen_ok_c`（定义在 coerce_init.c #include @ L11099 晚于本叶 #include）；glue.c L1906-1909 + L1929-1932 冗余 fwd decl 删除（return.c 自带）；`GLUE_EXPR_KIND_VAR` 宏替换为字面量 3 — 宏定义在 glue.c L2085 晚于 return.c #include L1913，与现有 return.c L123/171 风格一致）；wave1132 linear name moved（1 fn ＝ `typeck_linear_name_already_moved` → `pipeline_typeck_check_block.c` EOF，#include @ L11606；colocated with check_block walker 域 — linear use-once move 跟踪由 `pipeline_typeck_linear_use_var_c` L9805 在 check_expr/check_block VAR 读取 TYPE_LINEAR 时查询；globals `g_typeck_linear_moved_{n,names,lens}` @ glue.c L9745-9747 早于 #include 故可见；glue.c L9790 加 1 static fwd decl 供 L9805 callsite 在 #include 前可见）；wave1133-1135 lval param ptr field cluster（5 fns ＝ `typeck_lval_is_param_ptr_field_c`／`typeck_block_expr_stmts_store_scan_c`／`typeck_block_final_expr_store_scan_c`／`typeck_block_stores_param_into_param_field_c`／`typeck_func_stores_param_into_param_field_c` → `pipeline_typeck_region_assign.c` EOF，#include @ L9934；colocated with WPO-S3 stack-escape helpers wave1125-1129（同 assign-site escape 分析路径）；region_assign.c L141 callsite 加 1 static fwd decl at file top L37；`typeck_block_stores_param_into_param_field_c` 递归加 1 static fwd decl at L707；`pipeline_typeck_ptr_for_addr_of_operand_c`（glue.c L9919-9931）保留在 glue.c（非 cluster 成员）。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl。G.9 详细英文 docblock 全覆盖（用途／契约／Invariant／PLATFORM）。COUNT 仍 62；glue 12235→12044（-191）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED. Tip **b96af367f** |
| 2026-08-01 | **wave1111-1129** · BC 8.3.1 · 批量 G.7 fold 18 leaves（6 域：dep return type mapping／struct layout name lookup／vector type-let helpers／skipped-typeck backfill cluster／struct layout same-shape／WPO-S3 stack-escape cluster） | **代码** · **不升钉** · **L2 双端**。G.7 批量同叶 fold 18 个静态函数 + 1 const 自 `pipeline_glue.c` 迁入 4 个已有域文件 EOF（同 TU #include）：wave1111-1112 dep return type mapping 域（2 fns ＝ `pipeline_typeck_map_import_binding_named_to_caller_c`／`pipeline_typeck_dep_return_type_to_caller_arena_impl` → `pipeline_typeck_method_call.c` EOF，#include @ L11693）；wave1113-1114 struct layout name lookup 域（2 fns ＝ `typeck_type_is_named_struct_c`／`typeck_layout_index_for_named_type_c` → `pipeline_asm_emit_struct_lit.c` EOF，#include @ L2051）；wave1115-1117 vector type/let helpers 域（3 fns ＝ `glue_vector_type_lanes_esz_c`／`glue_is_vector_lane_scalar_binop_ko`／`glue_vector_let_init_uses_direct_slot` → `pipeline_asm_emit_vector_simd.c` EOF，#include @ L2218；glue.c L2205 callsite 加 1 static fwd decl）；wave1118-1123 skipped-typeck array-lit/var type backfill cluster（5 fns ＝ `glue_fill_array_lit_from_decl`／`glue_expr_in_scope_block_c`／`glue_fill_var_types_from_params_for_func`／`glue_fill_var_types_from_lets_in_block`／`glue_fill_array_lit_types_in_block` → `pipeline_asm_emit_block_inits.c` EOF，#include @ L3689；零 fwd decl；`pipeline_expr_set_resolved_type_ref` 加 extern fwd decl at block_inits.c:43）；wave1124 struct layout same-shape comparator（1 fn ＝ `typeck_struct_layouts_same_shape_c` → `pipeline_asm_emit_struct_lit.c` EOF；`pipeline_typeck_type_refs_equal_c` 加 extern fwd decl at struct_lit.c:96）；wave1125-1129 WPO-S3 stack-escape analysis cluster（5 fns + 1 const ＝ `typeck_find_or_alloc_ptr_stack_local_c`／`typeck_ptr_has_stack_local_label_c`／`typeck_block_tree_has_var_c`／`typeck_var_is_block_local_c`／`typeck_expr_is_addr_of_block_local_c` + `TYPECK_STACK_LOCAL_PTR_LBL` → `pipeline_typeck_region_assign.c` EOF，#include @ L10231；glue.c L10223/10227 callsites 加 2 static fwd decl；region_assign.c L122/238/279 callsites 加 1 static fwd decl at file top L34）。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl。G.9 详细英文 docblock 全覆盖（用途／契约／Invariant／PLATFORM）。COUNT 仍 62；glue 12473→12235（-238）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED. Tip **b4290b006** |
| 2026-08-01 | **wave1101-1110** · BC 8.3.1 · 批量 G.7 fold 6 域（codegen outbuf／asm label format／wpo mono thunk／next-offset／fold primitives／count-up-while loop fold） | **代码** · **不升钉** · **L2 双端**。G.7 批量同叶 fold 6 个域自 `pipeline_glue.c` 全部迁入 6 个新域文件（同 TU #include；wave1103 stack escape analysis 域因与 `pipeline_typeck_region_assign.c` 循环依赖跳过留后续）：wave1101 codegen outbuf append 域（4 fns + macro ＝ `glue_codegen_out_append_bytes`／`_cstr`／`_int`／`_byte` → `pipeline_codegen_outbuf.c`，#include @ L855）；wave1102 asm label integer format 域（2 fns ＝ `glue_format_u32_to_buf`／`glue_format_i32_to_buf` → `pipeline_asm_label_format.c`，#include @ L5415）；wave1104 WPO-S2 monomorphization thunk 域（5 fns + 3 macros + 2 typedefs + 1 global ＝ `glue_wpo_mono_has_sym`／`_reset_pending`／`_register_thunk_n`／`_register_thunk`／`pipeline_asm_emit_wpo_mono_thunks_elf_c` → `pipeline_asm_emit_wpo_mono.c`，#include @ L6090）；wave1105 asm_func_ctx next_offset management 域（3 fns ＝ `glue_align_next_offset`／`pipeline_asm_bump_next_offset_for_array_lit`／`pipeline_asm_bump_next_offset_after_let_init` → `pipeline_asm_emit_next_offset.c`，#include @ L3714，AFTER array_lit.c L2260；BEFORE block_inits／block_body）；wave1106 fold pattern detection primitives 域（13 fns ＝ `glue_fold_expr_var_refs_same_c`／`glue_fold_parse_while_lt_i_n_c`／`glue_fold_block_let_init_lit_c`／`glue_parse_i_mul_add_lit_c`／`glue_is_assign_var_add_one_c`／`glue_expr_is_param0_field_access_c`／`glue_fold_func_returns_param0_field_sum_c`／`glue_is_field_assign_from_var_c`／`glue_is_field_assign_i_plus_one_c`／`glue_is_assign_s_plus_pair_field_sum_call_c`／`glue_is_assign_u8_index_store_cast_i_c`／`glue_is_assign_sum_plus_u8_index_cast_c`／`glue_expr_var_name_eq_let_idx_c` → `pipeline_asm_emit_fold_primitives.c`，#include @ L5680，AFTER vector_simd.c L2218 + as.c L1938；BEFORE x86_enc_helpers.c L6032）；wave1107-1110 count_up_while loop folding 域（17 fns ＝ `glue_match_struct_pair_n2_body_pattern_c`／`glue_try_fold_struct_pair_n2_while_elf_c`／`glue_match_u8_fill_body_pattern_c`／`glue_try_fold_u8_fill_index_while_elf_c`／`glue_match_u8_sum_body_pattern_c`／`glue_try_fold_u8_sum_while_elf_c`／`glue_match_mem_copy_outer_pattern_c`／`glue_try_fold_mem_copy_outer_while_elf_c`／`glue_match_lcg_xor_body_pattern_c`／`glue_try_fold_lcg_xor_while_elf_c`／`glue_fold_parse_affine_sum_body_c`／`glue_fold_affine_i_plus_k_expr_c`／`backend_try_fold_count_up_while_elf` + 4 helper → `pipeline_asm_emit_fold_count_up_while.c`，#include @ L5732，AFTER fold_primitives.c L5680）。全 static（非 extern）：same-TU 可见性 via #include 顺序。G.9 详细英文 docblock 全覆盖（用途／契约／Invariant／Asm-Perf／PLATFORM）。COUNT 仍 62；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED. Tip **669179a82** |
| 2026-08-01 | **wave1100** · BC 8.3.1 · G.7 fold modlet ELF emit 域（12 fns + typedef + macro + global） | **代码** · **不升钉** · **L2 双端**。G.7 同叶 fold 把 module-level mutable let ELF COMMON cell emit 整域自 `pipeline_glue.c` 迁入新域文件 `pipeline_asm_emit_modlet.c`（modlet 是一域：模块级 mutable let 跨函数共享存储 via SHN_COMMON／Mach-O N_UNDF linker BSS）：12 static 函数 ＋ `XLANG_ASM_MODLET_MAX` macro ＋ `pipeline_asm_modlet_table_t` typedef ＋ `g_pipeline_asm_modlet` global。Members: `pipeline_asm_modlet_reset`／`_name_is_shared`／`_find`／`_lea_rbx_rip_x86`（x86_64 RIP-relative lea 7B）／`_lea_rbx_adrp_arm64`（arm64 ADRP+ADD 8B）／`_lea_rbx_arch`（arch dispatch）／`_load_to_rax_elf_c`（lea + mov rax,[rbx]／ldr x0,[x1]）／`_store_from_rax_elf_c`（lea + mov [rbx],rax／str x0,[x1]）／`_prepare_and_emit_elf_c`（建表 + emit COMMON sym）／`_seed_nonzero_inits_elf_c`（emit 非零初值 store）／`_register_module_top_level_lets_c`（扫 module top-level let 入表）／`_emit_module_top_level_mutable_lit_inits_elf_c`（emit mutable lit 初始化）。新域文件 `#include` 自 `pipeline_glue.c` L2291（在 `pipeline_asm_emit_assign.c` 前：assign 域调用 `store_from_rax` + `modlet_name_is_shared`）。全 static（非 extern）：same-TU 可见性 via #include 顺序。G.9 详细英文 docblock 全覆盖（用途／契约／Invariant／Asm-Perf／PLATFORM）。修一处 extern 类型冲突：`pipeline_module_top_level_let_name_byte_at` 返回 `uint8_t`（match ast_pool.c:78 + ide/pipeline_glue_types.inc:92），原误写 `int32_t`。COUNT 仍 62；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED. Tip **5307b0460** |
| 2026-08-01 | **wave1095-1099** · BC 8.3.1 · 批量 G.7 fold 5 叶（generic call inference 域） | **代码** · **不升钉** · **L2 双端**。G.7 批量同叶 fold 5 个静态函数自 glue residual 全部迁入 `pipeline_typeck_method_call.c` EOF（method call + generic UFCS mono 域统一权威，generic call type-param 推导／trait bound 校验／type-arg count 验证／返回类型单态化 fixup 是 method call 子域）：wave1095 `pipeline_typeck_named_is_module_type_c`（40 LOC，TYPE_NAMED 是否模块定义的 struct／alias（具体类型）vs 自由类型参数 T 的分类器：扫 struct_layouts + type_aliases 做 glue_slice_equal_c 字节比较）→ method_call.c EOF + static fwd decl at method_call.c:348（callsites L476-L872 早于 #include L13803）；wave1096 `glue_generic_call_fixup_resolved_type_c`（319 LOC，泛型 CALL 返回类型单态化 fixup：签名返回为含自由类型参数的复合树（*T／[]T／T[N]／嵌套）或裸类型参数时，按 turbofish type-arg 或 value formal 推导并 stamp resolved_type_ref）→ method_call.c EOF（callsites glue.c:L14181 bootstrap_fixup + L14757 check_expr_call 均在 #include L13803 后）；wave1097 `pipeline_typeck_try_infer_generic_call_from_args_c`（130 LOC，无 turbofish 时从 value args 的 resolved_type_ref 反推泛型 callee 的 type-params：按形参位置匹配 arg 类型并写入 call type_args 缓存）→ method_call.c EOF（callsite 在同批迁移的 check_call_generic_type_args_c 内，同文件）；wave1098 `pipeline_typeck_check_inferred_generic_bounds_c`（124 LOC，校验推导出的 type-args 是否满足 callee 泛型参数的 trait bounds：逐 type-param 查 bound 列表并匹配）→ method_call.c EOF（callsite 在同批迁移的 check_call_generic_type_args_c 内，同文件）；wave1099 `pipeline_typeck_check_call_generic_type_args_c`（67 LOC，CALL type-arg count 验证总入口：count 不匹配报错，非泛型 callee 拒绝 type-args，bare call 触发 try_infer + check_bounds）→ method_call.c EOF（callsite glue.c:L14743 inside pipeline_typeck_check_expr_call_c 在 #include L13803 后）。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl（named_is_module_type_c 在 method_call.c:348 有 fwd decl 供早期 callsites）。COUNT 仍 62；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED. Tip **54609c727** |
| 2026-08-01 | **wave1085-1094** · BC 8.3.1 · 批量 G.7 fold 10 叶（import binding name 域 + overload resolution 域） | **代码** · **不升钉** · **L2 双端**。G.7 批量同叶 fold 10 个静态函数自 glue residual 全部迁入 `pipeline_typeck_method_call.c` EOF（method call 域统一权威，import binding 解析 + overload dispatch 是 method call 子域）：wave1085 `pipeline_typeck_import_path_segment_count_impl`（15 LOC，import 路径 `.` 分段计数）+ wave1086 `pipeline_typeck_import_path_slice_equal_impl`（14 LOC，import 路径 slice 字节比较）+ wave1087 `pipeline_typeck_import_binding_name_equal_impl`（16 LOC，import binding name 字节比较）+ wave1088 `pipeline_typeck_import_select_name_equal_impl`（16 LOC，import select name 字节比较）→ 4 件套组成 import binding/path resolution 域，callsites L11893-12106 全部早于 method_call.c #include L14053，故在 glue.c L11762 加 4 行 static fwd decl；wave1089 `pipeline_module_func_overload_count_c`（14 LOC，模块内同名非 extern 函数计数：overload gating）+ wave1090 `pipeline_typeck_call_arg_assignable_c`（65 LOC，overload arg→param assignability gate：精确匹配／integer widen refs／float widen／slice elem／array→ptr decay）+ wave1091 `pipeline_typeck_overload_match_score_c`（31 LOC，单候选 overload 得分：精确 +1000，可赋 +1，不匹配 -1）+ wave1092 `pipeline_typeck_overload_expect_match_c`（15 LOC，func return vs expected_ret_peek 二级匹配）+ wave1093 `pipeline_typeck_pick_overload_func_index_c`（48 LOC，按 arg score + expect_match tiebreak 选唯一 overload）+ wave1094 `pipeline_typeck_resolve_call_func_index_c`（35 LOC，CALL→func_ix 总入口：overload-aware + 缓存回落 + name-only scan）→ 6 件套组成 overload resolution 域，callsites L12695+ / L13233+ wrappers / L13360+ / L13743+ 全部早于 method_call.c #include L14053，故在 glue.c L13015 加 4 行 static fwd decl（resolve_call_func_index_c 已有 fwd decl at glue.c:12315）。wave1090 依赖 wave1079 `pipeline_typeck_integer_widen_ok_refs_c` + wave1076 `pipeline_typeck_float_widen_ok_c`（都在 coerce_init.c，已通过 glue.c:10354/10381 的 static fwd decl 可见）。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl。COUNT 仍 62；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main (exit 42) · arg1_i32 (exit 3) · match_min (exit 2)。PLATFORM: SHARED. Tip **3ba58c695** |
| 2026-08-01 | **wave1080-1084** · BC 8.3.1 · 批量 G.7 fold 5 叶（type_refs_equal 域 + alias resolver + let name match） | **代码** · **不升钉** · **L2 双端**。G.7 批量同叶 fold 5 个静态函数自 glue residual：wave1080 `typeck_named_unqual_offset_c`（7 LOC，NAMED type unqualified-name offset: find last '.'+1）→ coerce_init.c EOF + fwd decl；wave1081 `typeck_glue_type_refs_equal_named`（37 LOC，NAMED type_refs_equal: full-name then unqualified suffix match）→ coerce_init.c EOF + fwd decl；wave1082 `typeck_glue_type_refs_equal_impl`（12 LOC，type_refs_equal internal impl: read kind then delegate to same_kind）→ coerce_init.c EOF + fwd decl；wave1083 `pipeline_typeck_resolve_type_alias_ref_impl_c`（36 LOC，type alias chain resolver: NAMED→target ref, depth-limited recursion）→ coerce_init.c EOF（已有 fwd decl at glue.c:10141）；wave1084 `glue_let_name_matches_var`（19 LOC，block-internal let name vs EXPR_VAR match: length + byte compare）→ block_inits.c EOF。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl（coerce_init L14126 / block_inits L3830 均 < def EOF < callsites）。COUNT 仍 62；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld · noimport_main · arg1_i32 · match_min。PLATFORM: SHARED. Tip **6b092ec3a** |
| 2026-08-01 | **wave1075-1079** · BC 8.3.1 · 批量 G.7 fold 5 叶（call arg effective type + widen 系列四件套） | **代码** · **不升钉** · **L2 双端**。G.7 批量同叶 fold 5 个静态函数自 glue residual：wave1075 `pipeline_typeck_call_arg_effective_type_c`（31 LOC，call arg effective mono type for free-T generic unify: resolved_type_ref fallback to lit-kind defaults）→ method_call.c EOF（callsites L14810/14834/14860 全在 #include L14220 后）；wave1076 `pipeline_typeck_float_widen_ok_c`（10 LOC，f32→f64 IEEE float widen gate）→ coerce_init.c EOF + fwd decl；wave1077 `pipeline_typeck_integer_widen_ok_c`（36 LOC，first-class integer widen matrix）→ coerce_init.c EOF + fwd decl；wave1078 `pipeline_typeck_int_family_id_c`（19 LOC，family id for first-class ints + NAMED i8/i16/u16）→ coerce_init.c EOF + fwd decl；wave1079 `pipeline_typeck_integer_widen_ok_refs_c`（33 LOC，refs-based integer widen unifying first-class + NAMED）→ coerce_init.c EOF + fwd decl（calls int_family_id_c + integer_widen_ok_c defined above in same file）。全 static（非 extern）：same-TU 可见性 via #include 顺序 + fwd decl。COUNT 仍 62；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld · noimport_main · arg1_i32 · match_min。PLATFORM: SHARED. Tip **85083faa0** |
| 2026-08-01 | **wave1070-1074** · BC 8.3.1 · 批量 G.7 fold 5 叶（struct layout warn / block return scan / enum name match） | **代码** · **不升钉** · **L2 双端**。G.7 批量同叶 fold 5 个小静态函数自 glue residual：wave1070 `glue_pad_fields_warn_enabled`（3 LOC，XLANG_PAD_FIELDS env gate）→ struct_lit.c EOF；wave1071 `glue_field_type_atomic_sized`（6 LOC，4/8B atomic-sized field classifier）→ struct_lit.c EOF；wave1072 `glue_hot_reorder_warn_enabled`（3 LOC，XLANG_HOT_REORDER env gate）→ struct_lit.c EOF；wave1073 `glue_block_stmt_order_has_return`（25 LOC，block stmt_order EXPR_RETURN scanner）→ block_body.c EOF + fwd decl L48（callsites block_body.c:819/851 + block_if_stmt.c:82 via #include L3875）；wave1074 `glue_enum_field_name_equal`（8 LOC，enum variant name prefix match w/ lenient trailing-NUL）→ field_access.c EOF。全 static（非 extern）：same-TU 可见性 via #include 顺序（struct_lit L2095 / block_body L3872 / field_access L2419 均 < def EOF < callsites）。COUNT 仍 62；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main · arg1_i32 · match_min。PLATFORM: SHARED. Tip **9dd86c495** |
| 2026-08-01 | **wave1065-1069** · BC 8.3.1 · 批量 G.7 fold 5 叶（assign-kind / import count / ctx reset / pair base / expr stmt） | **代码** · **不升钉** · **L2 双端**。G.7 批量同叶 fold 5 个小静态函数自 glue residual：wave1065 `pipeline_typeck_expr_is_any_assign_kind_c`（6 LOC，assign-kind classifier）→ coerce_init.c EOF；wave1066 `pipeline_typeck_module_num_imports_c`（9 LOC，unified import count reader: sidecar-first then thin Module field）→ assign.c EOF；wave1067 `pipeline_asm_ctx_reset_for_func_c`（13 LOC，per-func AsmFuncCtx reset: frame/locals/break/continue/loop/tail_join zeroed）→ block_body.c EOF；wave1068 `glue_field_assign_pair_base_ref_c`（8 LOC，field-assign pair base VAR extractor for fold/affine pattern）→ asm_emit_assign.c EOF；wave1069 `glue_body_expr_stmt_at_c`（15 LOC，indexed expr stmt ref reader: stmt_order + pure expr_stmts normalize）→ asm_emit_assign.c EOF。全 static（非 extern）：same-TU 可见性 via #include 顺序。wave1068/1069 初迁 assign.c 因 callsite 在 #include 前不可见，改迁 asm_emit_assign.c（#include L2294 < callsites L6664+）。COUNT 仍 62；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main · arg1_i32 · match_min。PLATFORM: SHARED. Tip **d86a7a033** |
| 2026-08-01 | **wave1064** · BC 8.3.1 · glue_copy_large_struct_from_rax_ptr_elf_c G.7 同叶 fold（>16B struct retval memcpy 域） | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_call_args.c` 迁入 `glue_copy_large_struct_from_rax_ptr_elf_c`（~22 LOC body 自 return.c:751；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 call_args.c store_retval_pair 定义之后——>16B struct CALL retval 经 rax 指针 memcpy 到 let 槽，同 retval store 域）；被 wave1058 `glue_store_retval_pair_to_rbp_elf_c`（call_args.c:1876）唯一调用——同叶 colocated 证明；**static（非 extern）**：same-TU 可见性 via #include 顺序（return.c L1957 < call_args.c L2395）；新增 static fwd decl at call_args.c:367（store_retval_pair callsite L1876 之前；定义 L1969 之后）；return.c L23 文档注释更新引用新位置；依赖 `backend_enc_push_rax_arch`／`backend_enc_lea_rbp_to_rax_arch`／`backend_enc_mov_rax_to_arg_reg_arch`／`backend_enc_pop_rax_arch`／`backend_enc_mov_imm64_to_rax_arch`／`backend_enc_call_arch`（全 global extern）；COUNT 仍 62；glue 不变（16042）；return.c 772→754（-18）；call_args 2361→2431（+70）；不改语义；不升钉。mac L2：pipeline_x · g05 pure-ld（mac 60 objs）· noimport_main · arg1_i32 · arg1_nested · match_min。PLATFORM: LINUX+MACOS x86_64 SysV MEMORY class only — ta!=0 return -1（MACOS|ARM64 AAPCS64 x8 indirect result location 在 sret reg-shift path 处理）. Tip **b94d1bcb9** |
| 2026-08-01 | **wave1063** · BC 8.3.1 · pipeline_asm_call_param_type_ref_at_c G.7 同叶 fold（call param type_ref resolve 域） | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_call_args.c` 迁入 `pipeline_asm_call_param_type_ref_at_c`（~64 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 call_args.c 末尾，紧跟 wave1062 call_return_type_kind_ord 之后——CALL 形参 type_ref 解析，同 call/return ABI resolve 域）；被 `backend_call_dispatch.x:2248`（seed extern C link）+ `backend_call_dispatch_thin.x:117` 直接调用——同叶 colocated 证明；**extern（非 static）**：seed extern 原型 backend_call_dispatch.x:19 + backend_call_dispatch_thin.x:104；struct_let.c:98 注释引用（via #include L2269 < call_args.c L2395）；依赖 `glue_asm_resolve_call_target_module_c`（static fwd decl struct_let.c:78 via #include L2269 < 2395；def 仍在 glue.c:13555）+ `pipeline_module_func_param_type_ref_at`（extern L261 < 2395）+ `pipeline_typeck_get_dep_return_type_in_caller_arena_c`（extern L788 < 2395）+ `pipeline_dep_ctx_arena_at`（extern decl wave1062 已在 call_args.c）+ **4 个新增 extern fwd decl**：`pipeline_dep_ctx_ndep`／`pipeline_dep_ctx_module_at`／`pipeline_get_dep_arena_slot`／`pipeline_typeck_dep_return_type_to_caller_arena_c`（defs glue.c:11661/11663/11664/11846 > #include L2395）+ `g_pipeline_asm_emit_module`／`g_pipeline_asm_emit_dep_pipe`（globals）；COUNT 仍 62；glue 16093→16042（-51）；call_args 2241→2361（+120）；不改语义；不升钉。mac L2：pipeline_x · g05 pure-ld（mac 60 objs）· noimport_main · arg1_i32 · arg1_nested · match_min。PLATFORM: SHARED — LINUX+MACOS x86_64 SysV SSE/GP param classification + MACOS|ARM64 AAPCS64. Tip **75f39d198** |
| 2026-08-01 | **wave1062** · BC 8.3.1 · pipeline_asm_call_return_type_kind_ord_c G.7 同叶 fold（call return TypeKind resolve 域） | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_call_args.c` 迁入 `pipeline_asm_call_return_type_kind_ord_c`（~30 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 call_args.c 末尾，紧跟 wave1061 call_struct16_ret_needs_rax_deref 之后——CALL 返回 TypeKind 序数解析，同 call/return ABI resolve 域）；被 `backend_call_dispatch.x:3316`（glue_asm_harvest_sse_call_ret_to_gpr）+ `binop.c:869/914`（load_operand）+ `cmp.c:231/252`（cc select）+ `call_args.c:1352`（call-arg f32 fast-path gate）直接调用——同叶 colocated 证明；**extern（非 static）**：extern 原型 struct_let.c:99 via #include L2269 < call_args.c L2395 全 callsite 可见；依赖 `pipeline_expr_resolved_type_ref`（extern glue.c:1698 < 2395）+ `pipeline_type_kind_ord_at`（extern < 2395）+ `glue_asm_resolve_call_target_module_c`（static fwd decl struct_let.c:78 via #include L2269 < 2395；def 仍在 glue.c:13555）+ `pipeline_module_func_return_type_at`（extern glue.c:1954 < 2395）+ `pipeline_typeck_get_dep_return_type_in_caller_arena_c`（extern glue.c:788 < 2395）+ `pipeline_dep_ctx_arena_at`（extern glue.c:11662 > 2395——call_args.c 新增 extern fwd decl）+ `g_pipeline_asm_emit_dep_pipe`（global）；COUNT 仍 62；glue 16128→16093（-35）；call_args 2155→2241（+86）；不改语义；不升钉。mac L2：pipeline_x · g05 pure-ld（mac 60 objs）· noimport_main · arg1_i32 · arg1_nested · match_min。PLATFORM: SHARED — LINUX+MACOS x86_64 SysV SSE/GP harvest + MACOS|ARM64. Tip **9fe2349a1** |
| 2026-08-01 | **wave1061** · BC 8.3.1 · pipeline_asm_call_struct16_ret_needs_rax_deref_c G.7 同叶 fold（struct16 retval deref classifier 域） | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_call_args.c` 迁入 `pipeline_asm_call_struct16_ret_needs_rax_deref_c`（~60 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 call_args.c 末尾，紧跟 wave1060 deref_struct16_rax_ptr 之后——C/skip-heavy callee 16B struct 返回是否经 rax 指针（C sret）vs rax+rdx 按值（pure-asm dual-GP）的分类器，同 retval deref classifier 域）；被 wave1058 `glue_store_retval_pair_to_rbp_elf_c`（call_args.c:1878）+ `glue_load_var_as_value_to_rax_rdx_elf_c`（call_args.c）+ `pipeline_asm_emit_index_eff_addr.c:249/420` 直接调用——同叶 colocated 证明；**extern（非 static）**：extern 原型 glue.c:791 < #include L2395 可见；struct_let.c:77 `#define` alias 保留（glue_ 名 → pipeline_asm_，call_args.c / index_eff_addr.c callsites 用 glue_ 名经 #define 路由到本定义）；struct_let.c:75 dead static fwd decl 保留（无害，与 wave1060 dead fwd 同批清理留后续）；依赖 `glue_asm_resolve_call_target_module_c`（static fwd decl struct_let.c:78 included at L2269 < call_args.c L2395；def 仍在 glue.c:13555）+ `pipeline_module_func_return_type_at`／`pipeline_typeck_get_dep_return_type_in_caller_arena_c`／`pipeline_type_kind_ord_at`／`ast_TypeKind_TYPE_SLICE|TYPE_ARRAY`／`glue_type_is_fixed_array`／`pipeline_asm_module_func_name_len_at|copy64`／`asm_skip_heavy_module_func_body`／`glue_type_size_simple`（全 same-TU / extern 可见）+ `g_pipeline_asm_emit_module`／`g_pipeline_asm_emit_dep_pipe`（globals）；COUNT 仍 62；glue 16202→16128（-74）；call_args 2052→2155（+103）；不改语义；不升钉。mac L2：pipeline_x · g05 pure-ld（mac 60 objs）· noimport_main · arg1_i32 · arg1_nested · match_min。PLATFORM: SHARED classifier · LINUX gold + MACOS|ARM64 co-path. Tip **32f4a3a2a** |
| 2026-08-01 | **wave1060** · BC 8.3.1 · pipeline_asm_deref_struct16_rax_ptr_elf_c G.7 同叶 fold（struct16 retval deref 域） | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_call_args.c` 迁入 `pipeline_asm_deref_struct16_rax_ptr_elf_c`（7 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 call_args.c 末尾，紧跟 wave1059 is_f32/is_f64 之后——C/skip-heavy callee 16B struct 返回经 rax 指针 deref 载入 rax+rdx，同 retval deref 域）；被 wave1058 `glue_store_retval_pair_to_rbp_elf_c`（call_args.c）+ `glue_load_var_as_value_to_rax_rdx_elf_c`（call_args.c）直接调用——同叶 colocated 证明；**extern（非 static）**：extern 原型 glue.c:1493 < #include L2395 可见；struct_let.c:75 `#define` alias 保留（glue_ 名 → pipeline_asm_，call_args.c callsites 用 glue_ 名经 #define 路由到本定义）；struct_let.c:73 dead static fwd decl 保留（无害，清理留后续）；依赖 `backend_enc_mov_rax_to_rbx_arch`／`backend_enc_load_qword_from_rbx_to_rax_arch`／`backend_enc_load_qword_rbx8_to_rdx_arch`（全局 extern）；COUNT 仍 62；glue 16212→16202（-10 含 docblock 3）；call_args 2017→2052（+35 含 G.9 docblock）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main · arg1_i32 · arg1_nested · match_min。PLATFORM: SHARED. Tip **374ee6f30** |
| 2026-08-01 | **wave1059** · BC 8.3.1 · glue_func_param_is_f32/f64_c G.7 同叶 fold + dead code 删除（SysV param classification 域） | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_call_args.c` 迁入 `glue_func_param_is_f32_c`（~12 LOC）+ `glue_func_param_is_f64_c`（~10 LOC 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 call_args.c 末尾，紧跟 wave1058 store_retval_pair 之后——f32/f64 形参 SysV SSE class 分类，同 param classification 域）；调用者 `pipeline_asm_emit_param_home_elf_sysv_f32_xmm_c` 同循环内紧邻调用已迁移的 `glue_func_param_agg_byte_size_c`（wave1045）+ `glue_func_param_home_width_c`（wave1047）——同叶 colocated 证明；依赖 `GLUE_TYPE_KIND_F32_ORD/F64_ORD` 宏（glue.c:2185/2187 < #include L2395）+ `pipeline_module_func_param_type_ref_at`／`pipeline_type_kind_ord_at` extern；无 fwd decl 残留（调用点 5591/5592 全 > L2395）；**同波删除 dead code** `glue_sysv_x86_func_param_slot_c`（~60 LOC，全库零调用点，static same-TU only，被 `pipeline_asm_emit_param_home_elf_sysv_f32_xmm_c` 取代的旧 SysV slot 迭代设计残留）；COUNT 仍 62；glue 16284→16212（-92 含 dead code -65）；call_args 1950→2017（+67）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main · arg1_i32 · arg1_nested · match_min。PLATFORM: SHARED. Tip **788d89c71** |
| 2026-08-01 | **wave1058** · BC 8.3.1 · glue_store_retval_pair_to_rbp_elf_c G.7 同叶 fold（retval store 域） | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_call_args.c` 迁入 `glue_store_retval_pair_to_rbp_elf_c`（~67 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 call_args.c 末尾，紧跟 wave1057 sysv_dual_gp_byte_size_c 之后——CALL/expr 结果落 let 栈槽，同 retval store 域）；该函数是 SysV ABI retval store 权威——处理 9-16B 双 GP 存储、>16B 大 struct memcpy、TYPE_SLICE 双 half 存储+递归深拷贝；依赖 `glue_type_size_simple`（fwd decl L1887）、`glue_sysv_dual_gp_byte_size_c`（call_args.c EOF）、`glue_copy_large_struct_from_rax_ptr_elf_c`（return.c:751）、`glue_slice_dual_gp_length_off_c`（fwd decl L1899）、`glue_slice_let_reent_deep_copy_after_dual_gp_elf_c`（call_args.c:562）；`glue_deref_struct16_rax_ptr`／`glue_call_struct16_ret_needs_rax_deref` 经宏别名指向 pipeline_asm_* extern（struct_let.c:76-77）；glue.c:2076 static fwd decl 保留（struct_let.c:70 via #include at L2269 < call_args.c L2395）；struct_let.c:70 保留自身 fwd decl；COUNT 仍 62；glue 16351→16284（-67）；call_args 1811→1950（+139）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main · arg1_i32 · arg1_nested · match_min。PLATFORM: SHARED. Tip **9389919be** |
| 2026-08-01 | **wave1057** · BC 8.3.1 · glue_sysv_dual_gp_byte_size_c G.7 同叶 fold（SysV ABI type classification 域） | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_call_args.c` 迁入 `glue_sysv_dual_gp_byte_size_c`（~35 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 call_args.c 末尾，紧跟 `glue_call_return_byte_size_c` 之后——SysV INTEGER class 9–16B 双 GP 宽度分类，同 call/return ABI 域）；`glue_sysv_dual_gp_byte_size_c` 是 import POD 类型（Allocator/StrView/Result_i32）的 16B dual-GP 宽度恢复权威——当 `glue_type_size_simple` 未命中 dep layout 时，按名字后缀恢复 16B 宽度，使 call-arg packing／retval store／struct_lit field store 均发射正确的 rax+rdx／x0+x1 双 GP 序列；依赖 `glue_type_named_layout_size_any_module_elf_c`（已在 call_args.c:454）；call_args.c:358 fwd decl 保留（callsites L396/410/1571 在 EOF 定义前）；glue.c:2050 static fwd decl 保留（给 struct_lit.c:279 callsite via #include at L2095 < call_args.c L2395）；field_access.c:336 callsite via #include at L2419 > call_args.c L2395 可见；glue.c callsites（L3270/3398/3415/3421/3428）全 > L2395 可见；COUNT 仍 62；glue 16382→16351（-31）；call_args 1741→1811（+70）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main · arg1_i32 · arg1_nested · match_min。PLATFORM: SHARED. Tip **9a3e290db** |
| 2026-08-01 | **wave1056** · BC 8.3.1 · glue_type_size_simple G.7 同叶 fold（struct layout triad size 收口） | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_struct_lit.c` 迁入 `glue_type_size_simple`（~95 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 struct_lit.c 末尾，紧跟 wave1054 align_simple 之后——类型字节宽度计算，同 struct layout registry 域）；**struct layout triad 收口**：size（wave1056）+ align（wave1054）+ metrics（wave1053）三件套全部同域 colocated in struct_lit.c EOF；`glue_type_size_simple` 是 typeck.x `typeck_x_type_size` 的 C twin，是全 asm emit 域 type byte-width 计算的 G.7 权威（frame sizing / sret activation / call-arg packing / index stride / array_lit temp / struct_lit field store）；ARRAY/VECTOR 递归 + TYPE_NAMED 查 layout + dep module 跨模块 layout sizing（g_pipeline_asm_emit_dep_pipe + pipeline_dep_ctx_*）；新增 `typeck_soa_array_storage_size_glue` extern decl in struct_lit.c（定义在 pipeline_typeck_soa.c，原 extern at glue.c:2925 > #include L2095 不可见）；`typeck_x_type_size_from_layout_glue` extern at glue.c:233 < 2095 可见；glue.c:1887 static fwd decl 保留（给 return.c #include at L1957 < struct_lit.c L2095 + glue.c callsites before L2095）；struct_lit.c:81 fwd decl 保留（callsites L286/746/1019/1058 在 EOF 定义前）；array_lit.c/struct_let.c/index_helpers.c/index.c 冗余 fwd decl 无害（C 允许重复声明），清理留后续 wave；COUNT 仍 62；glue 16465→16382（-83）；struct_lit 1189→1360（+171）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main · arg1_i32 · arg1_nested · match_min。PLATFORM: SHARED. Tip **329cba059** |
| 2026-08-01 | **wave1055** · BC 8.3.1 · glue_fixed_array_temp_bytes + glue_array_temp_bytes_for_let_init G.7 同叶 fold | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_array_lit.c` 迁入 `glue_fixed_array_temp_bytes`（~35 LOC body）+ `glue_array_temp_bytes_for_let_init`（~35 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 array_lit.c 末尾——定长数组 T[N] 栈 temp 区字节宽度 + let init 推导，同 array temp sizing 域）；两者为纯叶簇——`fixed_array_temp_bytes` 仅被 `array_temp_bytes_for_let_init` 调用（同簇），`array_temp_bytes_for_let_init` 被 glue.c 4 处 callsites（bump_next_offset L3947/3971 + block stmt order L6331/6412）+ block_body.c 2 处（L473/606 let-init temp reservation）消费；array_lit.c #include at L2299 < 所有 callsites，定义经 same-TU 可见；新增 `ast_pipeline_expr_array_lit_num_elems_at` forward decl in array_lit.c（定义在 ast_pool.c glue.c:16314，原 fwd decl at glue.c:3902 > #include L2299 不可见）；其余依赖（`glue_type_size_simple` fwd decl L35 / `pipeline_arena_type_ptr` / `pipeline_type_kind_ord_at` / `pipeline_expr_resolved_type_ref` / `pipeline_expr_kind_ord_at` / `pipeline_asm_array_lit_elem_type_ref` @ L1278）均 < L2299 可见；glue.c 零 fwd decl 残留（callsites 全在 #include 后）；COUNT 仍 62；glue 16536→16465（-71）；array_lit 779→912（+133）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main · arg1_i32 · arg1_nested · match_min。PLATFORM: SHARED. Tip **4a214cad9** |
| 2026-08-01 | **wave1054** · BC 8.3.1 · glue_type_align_simple G.7 同叶 fold | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_struct_lit.c` 迁入 `glue_type_align_simple`（~50 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 struct_lit.c 末尾，紧跟 wave1053 metrics 之后——类型对齐计算，同 struct layout registry 域）；与 `glue_struct_layout_metrics_c`（wave1053 迁入）相互递归——align_simple 调 metrics 计算 TYPE_NAMED 对齐，metrics 调 align_simple 计算字段对齐；两者 fwd decl 在 struct_lit.c 顶部（L69/L72），定义同在 EOF（metrics 在前，align_simple 在后）；glue.c 保留 static fwd decl（L2787）——callsites 在 `pipeline_struct_layout_next_field_offset_ex` body + soa.c:94/140 via #include at L11697（fwd decl 在 #include 前可见）；struct_lit.c fwd decl 保留（callsites L726/L1040 在定义前）；COUNT 仍 62；glue 16582→16536（-46）；struct_lit 1092→1189（+97）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main · arg1_i32 · arg1_nested · match_min。PLATFORM: SHARED. Tip **d506651e7** |
| 2026-08-01 | **wave1053** · BC 8.3.1 · glue_struct_layout_metrics_c + typeck_typeck_struct_layout_metrics G.7 同叶 fold | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_struct_lit.c` 迁入 `glue_struct_layout_metrics_c`（~95 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 struct_lit.c 末尾，紧跟 wave1052 sync_field_offsets 之后——struct layout 总 size+align 计算，同 struct layout registry 域）+ public wrapper `typeck_typeck_struct_layout_metrics`（thin delegate，extern-called by ast_pool.c:8151，同 pipeline_x.o 符号无 link 变化）；metrics 与 `glue_type_align_simple`（glue.c:3006）相互递归——metrics 调 align 计算字段对齐，align 调 metrics 递归嵌套 TYPE_NAMED；两者 static fwd decl 在 struct_lit.c 顶部（L65/L72），定义跨文件但 same-TU 经 #include 可见；struct_lit.c 新增 `glue_type_size_simple` fwd decl + 3 个 `driver_diagnostic_typeck_struct_padding_before/trailing/field_bad_size` extern decl（glue.c:2781-2784 > #include at L2095 不可见）；`driver_asm_build_skip_typeck` extern 已在 glue.c:691 < 2095 可见；glue.c 保留 metrics static fwd decl（callsite at glue_type_align_simple L3050 递归）；public wrapper 从 glue.c 删除（3 处 typeck_validate_* callsites 经 same-TU 可见）；COUNT 仍 62；glue 16673→16582（-91）；struct_lit 925→1092（+167）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· noimport_main · arg1_i32 · arg1_nested · match_min。PLATFORM: SHARED. Tip **69ffd5bc8** |
| 2026-08-01 | **wave1052** · BC 8.3.1 · glue_sync_struct_layout_field_offsets_c G.7 同叶 fold | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_struct_lit.c` 迁入 `glue_sync_struct_layout_field_offsets_c`（~30 LOC body 自 glue residual；置于 struct_lit.c 末尾，紧跟 wave1051 `pipeline_expr_struct_lit_value_bytes` 之后——struct layout 字段偏移同步，同 struct layout registry 域）；glue.c 有 1 处自调用（L11850 fill_struct_layouts）——glue.c:3666 保留 forward decl（或通过 same-TU #include 可见）；依赖 `glue_struct_layout_compute_field_offset_c`（struct_lit.c wave1044 迁入）+ `link_abi_getenv` + `fprintf` + `memset`（struct_lit.c 已使用）；COUNT 仍 62；glue 16700→16673（-27）；struct_lit 853→925（+72）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· hello · array · slice · option · slice-field。PLATFORM: SHARED. Tip **3c64b01eb** |
| 2026-08-01 | **wave1051** · BC 8.3.1 · pipeline_expr_struct_lit_value_bytes G.7 同叶 fold | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_struct_lit.c` 迁入 `pipeline_expr_struct_lit_value_bytes`（~40 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 struct_lit.c 末尾——EXPR_STRUCT_LIT 按 layout 计算总字节大小，同 struct layout registry 域）；public 函数——glue.c:1693 forward decl 保留（public 符号声明）；跨 2 域消费（struct_lit.c:612 + field_access.c:330）——field_access.c #include at L2419 > struct_lit.c L2095，定义经 same-TU 可见；glue.c 零自调用——纯叶；依赖 `glue_struct_layout_metrics_c`（glue.c:2794 static，struct_lit.c:69 新增 forward decl——#include at L2095 < 2794 需 fwd）；COUNT 仍 62；glue 16737→16700（-37）；struct_lit 771→853（+82）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· hello · array · slice · option · slice-field。PLATFORM: SHARED. Tip **068a9e894** |
| 2026-08-01 | **wave1049** · BC 8.3.1 · glue_struct_layout_index_by_type_name_c G.7 同叶 fold | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_struct_lit.c` 迁入 `glue_struct_layout_index_by_type_name_c`（~22 LOC body 自 glue residual；置于 struct_lit.c 末尾，紧跟 wave1044 `glue_struct_layout_compute_field_offset_c` 之后——name→index 查表 + offset 计算同 struct layout registry 域）；跨 2 域消费（struct_lit.c:162 + field_access.c:830/882）——struct_lit.c:59 fwd decl 保留（callsite L162 在定义前）+ field_access.c:706 fwd decl 保留（field_access.c #include at L2419 > struct_lit.c L2095，定义经 same-TU 可见）；glue.c 零自调用——纯叶；COUNT 仍 62；glue 16779→16762（-17）；struct_lit 718→771（+53）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· hello · array · slice · option · slice-field。PLATFORM: SHARED. Tip **87f4774d8** |
| 2026-08-01 | **wave1048** · BC 8.3.1 · glue_call_return_byte_size_c G.7 同叶 fold | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_call_args.c` 迁入 `glue_call_return_byte_size_c`（~35 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 call_args.c 末尾——CALL 表达式返回值字节宽度，wave1046 `glue_func_return_byte_size_c`（func_index 变体）的 call-site 孪生）；跨 3 域消费（field_access.c:328 + struct_let.c:141 + glue.c:3269/3383）——call_args.c:356 新增 fwd decl（对 #include at L2392 之后所有 callsite 可见）+ glue.c:2072 fwd decl 删除 + struct_let.c:93 fwd decl 保留（struct_let.c #include at L2266 < call_args.c L2392）；依赖 `glue_asm_resolve_call_target_module_c`（fwd decl 来自 struct_let.c:78，在 call_args.c #include 前可见）+ `glue_type_size_simple`（fwd decl L1887 < include 2392）；COUNT 仍 62；glue 16809→16779（-30）；call_args 1662→1741（+79）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· hello · array · slice · option · slice-field。PLATFORM: SHARED. Tip **c27453e84** |
| 2026-08-01 | **wave1047** · BC 8.3.1 · glue_func_param_home_width_c G.7 同叶 fold | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_call_args.c` 迁入 `glue_func_param_home_width_c`（~5 LOC body 自 glue residual；置于 call_args.c 末尾，紧跟 wave1045/1046 的 param/return byte size 之后——home slot width 是 agg_byte_size 的直接消费者，三者同 SysV ABI func sizing 域）；glue.c:5570 forward decl 删除（定义经 same-TU `#include` at L2392 对所有 callsite 可见——L5922/6030/6186/6330/6410）；依赖 `glue_func_param_agg_byte_size_c`（call_args.c 末尾 wave1045 迁入）；COUNT 仍 62；glue 16810→16809（-1）；call_args 1621→1662（+41）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· hello · array · slice · option · slice-field。PLATFORM: SHARED. Tip **3195cbeec** |
| 2026-08-01 | **wave1046** · BC 8.3.1 · glue_func_return_byte_size_c G.7 同叶 fold | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_call_args.c` 迁入 `glue_func_return_byte_size_c`（~20 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 call_args.c 末尾，紧跟 wave1045 `glue_func_param_agg_byte_size_c` 之后——返回值与形参字节宽度双胞胎，同 SysV ABI func sizing 域）；glue.c:5585 forward decl 删除（定义经 same-TU `#include` at L2392 对所有 callsite 可见——frame-size reserve L5949 + sret activation L8968）；依赖 `glue_type_size_simple`（fwd decl L1887 < include 2392）；COUNT 仍 62；glue 16826→16810（-16）；call_args 1563→1621（+58）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· hello · array · slice · option · slice-field。PLATFORM: SHARED. Tip **35a3169e9** |
| 2026-08-01 | **wave1045** · BC 8.3.1 · glue_func_param_agg_byte_size_c G.7 同叶 fold | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_call_args.c` 迁入 `glue_func_param_agg_byte_size_c`（~53 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 call_args.c 末尾）；call_args.c 是 callee-side param sizing 的自然归属（与 caller-side call-arg packing 同域，共享 `glue_type_named_layout_size_any_module_elf_c` call_args.c:429）；glue.c:5588 forward decl 删除（定义经 same-TU `#include` at L2392 对所有 callsite 可见——`glue_func_param_home_width_c` L6048 + `fill_param_slots` / `emit_func_param_home` L6172/6249/6393/6473）；依赖 `glue_type_size_simple`（fwd decl L1887 < include 2392）+ `glue_sysv_dual_gp_byte_size_c`（fwd decl L2050 < include 2392）；COUNT 仍 62；glue 16874→16826（-48）；call_args 1469→1563（+94）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（mac 60 objs · Ubuntu 62 objs）· hello · array · slice · option · slice-field。PLATFORM: SHARED. Tip **035144c28** |
| 2026-08-01 | **wave1044** · BC 8.3.1 · glue_struct_layout_compute_field_offset_c G.7 同叶 fold | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_struct_lit.c` 迁入 `glue_struct_layout_compute_field_offset_c`（~37 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 struct_lit.c 末尾）；struct_lit.c:152/158 callsites + glue.c:3759/3772/3774/3804 internal callsites——保留 line 55 forward decl（callsites 在定义前）+ 新增 `glue_type_align_simple` forward decl（定义在 glue.c:3003 > include 2092）；依赖 `glue_type_size_simple`（fwd decl 1887 < include 2092）+ `glue_type_is_empty_struct_c`（struct_lit.c:78 wave1032 迁入）；COUNT 仍 62；glue 16911→16874（-37）；struct_lit 657→718（+61）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（60 objs）· hello · array · slice · option。PLATFORM: SHARED. Tip **7d7db592a** |
| 2026-08-01 | **wave1043** · BC 8.3.1 · glue_emit_array_let_empty_init G.7 同叶 fold | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_block_body.c` 迁入 `glue_emit_array_let_empty_init`（~17 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 block_body.c 末尾）；block_body.c:467/602 callsites + glue.c:6791/6872 internal callsites——加 static forward decl（line 42，block_body.c 顶部，`#include` 后对 glue.c 可见）；依赖 `backend_enc_lea_rbp_to_rax_arch` + `backend_enc_store_rax_to_rbp_arch` + `pipeline_asm_ctx_layout`（全 TU 共享可见）；COUNT 仍 62；glue 16927→16911（-16）；block_body 907→951（+44）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（60 objs）· hello · array · slice · option。PLATFORM: SHARED. Tip **440f654eb** |
| 2026-08-01 | **wave1042** · BC 8.3.1 · glue_emit_block_final_expr_elf G.7 同叶 fold | **代码** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_block_body.c` 迁入 `glue_emit_block_final_expr_elf`（~73 LOC body 自 glue residual；中文 docblock 按 G.9 改英文 Why/Invariant/Asm-Perf 三段；置于 block_body.c 末尾）；block_body.c:768 callsite 在定义前——加 static forward decl（line 39，`#define GLUE_BLOCK_LET_DEFER_MAX` 之后）；`glue_emit_block_final_expr_elf` 仅 block_body.c 内 1 callsite（`pipeline_asm_emit_block_body_sync_elf` 尾部 final_expr emit）+ glue.c 零自调用——完美 G.7 候选；依赖全可见：`glue_block_stmt_order_has_return`（glue.c:4399 < include 4453）+ `glue_index_scratch_spills_cleanup_all_elf_c`（fwd decl 1869）；COUNT 仍 62；glue 17001→16927（-74）；block_body 809→907（+98）；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（60 objs）· hello · array · slice · option。PLATFORM: SHARED. Tip **69d81370d** |
| 2026-08-01 | **wave1041** · Stage 8 批次 1 · cfg_eval_gen.c Track L 退役（bespoke ladder） | **代码+文档** · **不升钉** · **L2 双端**。经核实 `src/lexer/cfg_eval_gen.c` 是 gitignored 瞬态 `-E` 输出（非 tracked）；`ensure_cfg_eval_ladder_one` in `ensure_host_cc_seed_o.sh` 已将 `-E-extern -L ..` 设为 Rung 1（产品路径），pin seed 为 Rung 3（考古 fallback）——产品构建确认走 `-E-extern + alias` 路径（60 T 符号）；**G.7 单权威**：cfg_eval 有 `ld -r` alias merge 步骤（`cfg_eval_link_alias.from_x.c`），`driver_leaf_x_to_o.sh` catalog 不支持此模式，保留 bespoke ladder 为单权威，不重复实现；`audit_gen_retirement.sh` 添加 "Bespoke ladder-retired" 识别段（检测 `try-cfg-eval-ladder` + `Rung 1: live -E`）；`阶段8高效退役分析.md` 同步更新。mac+Ubuntu L2：pipeline_x · g05 pure-ld · hello · array · slice · option · slice-field · lsp。Stage 8 进度 12/30→**13/30**。PLATFORM: SHARED. Tip **713277ef9** |
| 2026-08-01 | **wave1040** · Stage 8 批次 2 · build_runtime_x_gen.c Track L 退役（build_*_gen 三件套全退役） | **代码** · **不升钉** · **L2 双端**。按 gen.c 文件大小升序选取下一候选（build_runtime_x_gen.c seed 15653B/245 行/6 函数）；**根源修复**：build_runtime_x.x 中 15 处 `build_exec_system(cmd)` extern 调用未包裹 `unsafe{}`——用表达式形式 `if (unsafe { build_exec_system(cmd) } != 0)` 避免重复 `let rc` 冲突（step_id==6 块内 9 处同作用域）；`copy_path` + `append_lit` 加 `#[no_mangle]` 防止 -E 加 `build_` 模块前缀（保持符号名与 seed gen.c 等价）；`build_tool.sh` 默认从 `../build_runtime_x.x` via `./xlang -x -E` 生成 build_runtime_x_gen.c，fallback seeds/build_runtime_x_gen.c（考古）；移除 `XLANG_BUILD_TOOL_REGEN` 条件分支（现已为默认产品路径）；-E 输出 6 目标符号等价（copy_path／append_lit／build_patch_after_step／build_patch_pipeline_gen_c／build_patch_driver_gen_c／build_run_step）；额外 28 个 import 闭包 weak 符号与 build_gen.o/build_runner.o 共存。mac+Ubuntu L2：build_tool 链接 OK · g05 pure-ld · hello · array · slice · option · slice-field · lsp。Stage 8 进度 11/30→**12/30**（build_*_gen 三件套全退役）。PLATFORM: SHARED. Tip **06980e20b** |
| 2026-08-01 | **wave1039** · Stage 8 批次 2 · build_runner_gen.c Track L 退役（最小文件优先策略） | **代码** · **不升钉** · **L2 双端**。按 gen.c 文件大小升序选取下一最小候选（build_runner_gen.c seed 2440B/4 函数）；**根源修复**：build_runner.x 中 9 处 extern 调用未包裹 `unsafe{}`（typeck 现要求）—— 补全 `build_run_legacy_steps` + `entry` 两函数内所有 extern 调用（build_get_step_count／build_get_step_at／build_run_step／driver_get_argv_i／build_run_asm_build／build_use_asm_only／build_copy_xlang_asm）包裹 `unsafe{}`；`entry` 加 `#[no_mangle] export` 防止 -E 加 `build_` 模块前缀（保持 `entry` 符号名与 build_tool_main.from_x.c 链接一致）；`build_tool.sh` 默认从 `../build_runner.x` via `./xlang -x -E` 生成 build_runner_gen.c，fallback seeds/build_runner_gen.c（考古）；-E 输出 4 目标符号等价（build_run_legacy_steps／build_argv2_is_asm／build_argv2_is_legacy／entry）；额外 import 闭包符号为 `__attribute__((weak))`——与 build_gen.o 共存无 duplicate symbol 错误；build_runtime_x_gen.c 保持 pinned（分离退役）；冷启动 fallback seed 保留。mac+Ubuntu L2：build_tool 链接 OK · g05 pure-ld · hello · array · slice · option · slice-field · lsp。Stage 8 进度 10/30→**11/30**。PLATFORM: SHARED. Tip **3aa578227** |
| 2026-08-01 | **wave1038** · Stage 8 批次 2 · build_gen.c Track L 退役（最小文件优先策略） | **代码** · **不升钉** · **L2 双端**。按 gen.c 文件大小升序选取最小候选（build_gen.c 658B/3 函数）；`build_tool.sh` 默认从 `../build.x` via `./xlang -x -E` 生成 build_gen.c，fallback seeds/build_gen.c（考古）；-E 输出符号等价（build_use_asm_only/build_get_step_at/build_get_step_count）；额外 import 闭包符号（args_iter_*/ctx_*/io_*）不冲突 build_tool 链接；build_runtime_x_gen.c / build_runner_gen.c 保持 pinned（分离退役）；冷启动 fallback seed 保留。mac+Ubuntu L2：build_tool 链接 OK · hello · array · slice · option · slice-field · lsp。Stage 8 进度 9/30→**10/30**。PLATFORM: SHARED. Tip **58f5fa348** |
| 2026-08-01 | **wave1037** · BC 8.3.1 · 16 个 glue_try_index_var_* 冗余 fwd 清理 | **代码** · **不升钉** · **L2 双端**。G.7 同叶 fold：删除 pipeline_glue.c 中 16 个 `glue_try_index_var_*_eff_addr_rax_elf_c` forward declarations（lines 1772-1851, ~76 LOC）；定义体已在 `pipeline_asm_emit_index_helpers.c` (line 2017+)，glue.c #include index_helpers.c 在 line ~2352，所有消费叶（assign/array_lit/index/index_eff_addr/call_args/field_access/block_inits）均在之后 #include；glue.c 本身零调用点——fwd 冗余。COUNT 仍 62；glue 17077→17001；不改语义；不升钉。mac+Ubuntu L2：pipeline_x · g05 pure-ld（60／62 objs）· hello · array · slice · option · slice-field · lsp。PLATFORM: SHARED. Tip **206ab0f44** |
| 2026-08-01 | **wave1036** · Stage 8 批次 2 · lsp_io_gen.c Track L 退役试点 | **代码+文档** · **不升钉** · **L2 双端**。`lsp_io_x.o` 从 `src/lsp/lsp_io.x` 直接 -E→cc 生成（via `driver_leaf_x_to_o.sh` catalog）；6 个级联符号重命名复刻 historic Makefile -D flags（std_io_read→io_read, std_heap_alloc_usize→typeck_std_heap_alloc→lsp_io_std_heap_std_heap_alloc 等）；`ensure_gen_x_o.sh:build_lsp_io_x()` 改为优先 Track L catalog、失败回退 gen.c 考古模式；修复 `lsp_io.x:132` 注释语法错误（导致 `lsp_io_read_message` 缺失于 -E 输出）；COUNT 仍 62；不改语义；不升钉。mac+Ubuntu L2：g05 pure-ld（60／62 objs）· hello · array · slice · option · slice-field · **lsp**。lsp_io_gen.c 保留为考古/验证 fallback。Stage 8 进度 8/30→**9/30**。PLATFORM: SHARED. Tip **6048f0b03** |
| 2026-08-01 | **wave1035** · BC 8.3.1 · struct_lit field offset/type_ref G.7 同叶 fold | **代码+文档** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_struct_lit.c` 迁入 `pipeline_expr_struct_lit_field_offset_at`（~24 LOC body）+ `pipeline_expr_struct_lit_field_type_ref_at`（~37 LOC body 自 glue residual；中文 docblock 按 G.9 改英文；置于 struct_lit.c 顶部 `glue_type_is_empty_struct_c` 之后）；COUNT 仍 62；glue 17138→17077；struct_lit 551→657；`field_offset_at` 仅 struct_lit.c 内 1 callsite（L276 field store offset）+ glue.c wrapper `codegen_/backend_` 2 处（#include 之后可见）+ seed `backend_try_inline_dispatch` extern 消费（符号仍在 pipeline_x.o）；`field_type_ref_at` 仅 struct_lit.c 内 2 callsite（L119 store_sz type 查询 + L278 field type_ref）；加 `glue_struct_layout_compute_field_offset_c` + `glue_struct_layout_index_by_type_name_c` 2 个 static forward decl（定义在 glue.c:3788/3892，struct_lit.c #include 在 :2172 早于定义）；旧函数体 glue.c:4025-4090 删除 + 旧 forward decl L1691-1694 删除。mac+Ubuntu L2：pipeline_x · g05 pure-ld（60／62 objs）· hello · array · slice · option · slice-field · struct。**不改语义** · 不升钉。下一：8.3.1 其它 leaf residual 或 8.3.3 soa · PC。PLATFORM: SHARED. Tip **b0b63b367** |
| 2026-08-01 | **wave1034** · BC 8.3.1 · float bits lo/hi G.7 同叶 fold | **代码+文档** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_as.c` 迁入 `pipeline_expr_float_bits_lo_at` + `pipeline_expr_float_bits_hi_at`（~18 LOC body 自 glue residual；中文 docblock 按 G.9 改英文；置于 as.c 顶部 STRING_LIT_ORD #define 之后）；COUNT 仍 62；glue 17150→17138；as.c 422→472；`pipeline_expr_float_bits_lo/hi_at` 仅 as.c 内 4 callsite 消费（array_lit scalar pack f32 + glue_emit_float_lit_to_rax）；ast_pool.c wrapper `ast_pipeline_expr_float_bits_lo/hi_at` 在 glue.c:16988（as.c #include 之后）——定义可见，无需 forward decl；加 `glue_arena_expr_at_ref` static forward decl（定义在 glue.c:2796，as.c #include 在 :2062 早于定义）；误改的 `GLUE_EXPR_STRING_LIT_ORD` 宏名恢复（被 glue.c:16516 + typeck_assign.c:230 + seed backend_call_dispatch 引用）；旧函数体 glue.c:4677-4694 删除 + 旧 forward decl L1669-1670 删除。mac+Ubuntu L2：pipeline_x · g05 pure-ld（60／62 objs）· hello · array · slice · option · slice-field。**不改语义** · 不升钉。下一：8.3.1 其它 leaf residual 或 8.3.3 soa · PC。PLATFORM: SHARED. Tip **8eee871d1** |
| 2026-08-01 | **wave1033** · BC 8.3.1 · TokenKind variant tag G.7 同叶 fold | **代码+文档** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_field_access.c` 迁入 `pipeline_token_kind_variant_tag`（~38 LOC body 自 glue residual；中文 docblock 按 G.9 改英文；置于 field_access.c 顶部 forward decls 之后）；COUNT 仍 62；glue ~17.1k→~17.1k；field_access ~1017→~1083；`pipeline_token_kind_variant_tag` 仅 field_access.c 内 2 callsite 消费（TokenKind fast-path enum resolution）；residual glue.c 调用者 `pipeline_expr_enum_namespace_field_tag` 在 #include 之后——无需 forward decl；旧 forward decl L2076 删除；`names[]` 表与 include/token.h TokenKind 枚举序一致。mac+Ubuntu L2：pipeline_x · g05 pure-ld（60／62 objs）· hello · array · slice · option · slice-field。**不改语义** · 不升钉。下一：8.3.1 其它 leaf residual 或 8.3.3 soa · PC。PLATFORM: SHARED. Tip **6a1ec497f** |
| 2026-08-01 | **wave1032** · BC 8.3.1 · empty struct check G.7 同叶 fold | **代码+文档** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_struct_lit.c` 迁入 `glue_type_is_empty_struct_c`（~48 LOC body 自 glue residual；中文 docblock 按 G.9 改英文；置于 struct_lit.c 顶部 forward decls 之后）；COUNT 仍 62；glue ~17.2k→~17.1k；struct_lit ~484→~551；`glue_type_is_empty_struct_c` 仅 struct_lit.c 内 2 callsite 消费（field store_sz ZST check）；residual glue.c 调用者（layout metrics / call return size）在 #include 之后——无需 forward decl；`GLUE_TYPE_NAMED` #define 上移到 struct_lit #include 之前；旧 forward decl L2126 删除。mac+Ubuntu L2：pipeline_x · g05 pure-ld（60／62 objs）· hello · array · slice · option · slice-field。**不改语义** · 不升钉。下一：8.3.1 其它 leaf residual 或 8.3.3 soa · PC。PLATFORM: SHARED. Tip **a6205740d** |
| 2026-08-01 | **wave1031** · BC 8.3.1 · cmp cc helpers G.7 同叶 fold | **代码+文档** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_cmp.c` 迁入 `pipeline_asm_cmp_cc_for_expr_kind_ord` + `pipeline_asm_arm64_cset_cond_enc_from_cc`（~46 LOC body 自 glue residual；中文 docblock 按 G.9 改英文；置于 cmp.c 顶部避免隐式声明）；COUNT 仍 62；glue ~17.3k→~17.2k；cmp.c ~316→~382；`cmp_cc_for_expr_kind_ord` 仅 cmp.c 内 6 callsite 消费；`arm64_cset_cond_enc_from_cc` 被 arm64_enc.x / backend_enc_dispatch.x seed extern——同 pipeline_x.o 符号，无链接变化。mac+Ubuntu L2：pipeline_x · g05 pure-ld（60／62 objs）· hello · array · slice · option · slice-field。**不改语义** · 不升钉。下一：8.3.1 其它 leaf residual 或 8.3.3 soa · PC。PLATFORM: SHARED. Tip **d3b3e5bda** |
| 2026-08-01 | **wave1030** · BC 8.3.1 · with_arena scope domain G.7 同叶 fold | **代码+文档** · **不升钉** · **L2 双端**。G.7 同叶补全：新建 `pipeline_asm_emit_with_arena.c`（133 LOC）迁入 `GLUE_WA_SCOPE_STACK_MAX` + `g_glue_wa_scope_off_stack` + `g_glue_wa_scope_n` + `g_glue_wa_temp_base` + `g_glue_wa_temp_next` + `g_glue_wa_func_body_ref` + `glue_with_arena_scope_active_c` + `glue_with_arena_scope_top_off_c` + `glue_wa_emit_begin_func_c` + `glue_wa_scope_alloc_off_c` + `glue_wa_scope_push_c` + `glue_wa_scope_pop_c` + `glue_emit_with_arena_init_elf` + `glue_emit_with_arena_deinit_elf`（8 函数 + 6 globals 自 glue residual；中文 docblock 按 G.9 改英文）；COUNT 仍 62；glue ~17.4k→~17.3k；被 block_body.c（default_alloc inline + with_arena init/deinit）消费——在 #include 之后无需前向声明。mac+Ubuntu L2：pipeline_x · g05 pure-ld（60／62 objs）· hello · array · slice · option · slice-field。**不改语义** · 不升钉。下一：8.3.1 其它 leaf residual 或 8.3.3 soa · PC。PLATFORM: SHARED. Tip **320f37b32** |
| 2026-08-01 | **wave1029** · BC 8.3.1 · x86 micro-encoders G.7 同叶 fold | **代码+文档** · **不升钉** · **L2 双端**。G.7 同叶补全：新建 `pipeline_asm_emit_x86_enc_helpers.c`（429 LOC）迁入 27 个 `glue_enc_x86_*` 原始字节编码器 + `glue_emit_lcg_xor_body_x86_c` 复合体（~377 LOC body 自 glue residual；中文 docblock 按 G.9 改英文）；COUNT 仍 62；glue ~17.7k→~17.3k；被 while-fold 优化路径（struct_pair_n2／u8_fill／lcg_xor 在 L8199+/L8324/L8413/L8976）消费——全部在 #include 之后无需前向声明。mac+Ubuntu L2：pipeline_x · g05 pure-ld（60／62 objs）· hello · array · slice · option · slice-field。**不改语义** · 不升钉。下一：8.3.1 其它 leaf residual 或 8.3.3 soa · PC。PLATFORM: LINUX+MACOS x86_64 SysV. Tip **831edbd87** |
| 2026-08-01 | **wave1028** · BC 8.3.1 · async CPS emit domain G.7 同叶 fold | **代码+文档** · **不升钉** · **L2 双端**。G.7 同叶补全：新建 `pipeline_asm_emit_async_cps.c`（240 LOC）迁入 `GlueAsyncCpsEmitState` + `g_glue_async_cps_emit` + `pipeline_module_func_is_async_at` extern + `glue_async_cps_mov_imm32_to_rax` + `glue_async_cps_emit_frame_phase_ptr` + `glue_async_cps_call_frame_memop` + `glue_async_cps_save_live` + `glue_async_cps_restore_live` + `glue_async_cps_emit_after_await` + `glue_async_cps_emit_phase_reset` + `pipeline_asm_emit_async_cps_entry_elf_c` + `pipeline_asm_emit_async_cps_end_func_elf_c`（~190 LOC body 自 glue residual；含 typedef + global + extern + 9 函数）；COUNT 仍 62；glue ~17.9k→~17.7k；被 as.c + return.c（phase_reset；L2066 fwd 保留）+ block_body.c（emit_after_await；在 async_cps 之后 #include 无需 fwd）共享。mac+Ubuntu L2：pipeline_x · g05 pure-ld（60／62 objs）· hello · array · slice · option · slice-field。**不改语义** · 不升钉。下一：8.3.1 其它 leaf residual 或 8.3.3 soa · PC。PLATFORM: SHARED. Tip **87f55346f** |
| 2026-08-01 | **wave1027** · BC 8.3.1 · lea/arm64 sret helpers G.7 同叶 fold | **代码+文档** · **不升钉** · **L2 双端**。G.7 同叶补全：新建 `pipeline_asm_emit_lea_common.c`（202 LOC）迁入 `glue_asm_lea_rax_common_rip_x86` + `glue_asm_lea_rbx_common_rip_x86` + `glue_arm64_mov_x0_to_x8_elf_c` + `glue_arm64_mov_x8_to_x0_elf_c` + `glue_asm_lea_rbx_common_adrp_arm64` + `glue_asm_lea_rax_common_adrp_arm64`（6 函数 body 自 glue residual；`pipeline_elf_ctx_append_reloc_typed` extern 同步迁入 leaf）；COUNT 仍 62；glue ~18.0k→~17.9k；被 array_lit + return + call_args + field_access + struct_let 共享（lea_common 在 glue L2037 #include 早于所有消费 leaf → 无需前向声明；array_lit 旧 fwd 多余但保留无害）。mac+Ubuntu L2：pipeline_x · g05 pure-ld（60／62 objs）· hello · array · slice · option · slice-field。**不改语义** · 不升钉。下一：8.3.1 其它 leaf residual 或 8.3.3 soa · PC。PLATFORM: SHARED. Tip **f48a68a60** |
| 2026-08-01 | **wave1026** · BC 8.3.1 · field_access layout/offset helpers G.7 同叶 fold | **代码+文档** · **不升钉** · **L2 双端**。G.7 同叶补全：`pipeline_asm_emit_field_access.c` 迁入 `glue_dep_layout_field_offset_by_name_c` + `glue_field_layout_offset_for_{var_base,base}_field` + `glue_field_access_effective_offset_c` + `pipeline_expr_field_access_layout_offset` + `glue_field_access_load_bytes_for_type_ref` + `pipeline_expr_field_access_load_byte_sz`（~395 LOC body 自 glue residual；file ~632→~1025）；COUNT 仍 62；glue ~18.4k→~18.0k；被 field_access + index_helpers／assign／index_eff_addr／vector_let 共享（glue 1726-1727 public fwd 保留；2500 static fwd 保留；新增 typeck_get_field_offset_from_layout_deps／pipeline_dep_ctx_ndep／pipeline_dep_ctx_module_at extern 供 field_access.c 早期调用）。mac+Ubuntu L2：pipeline_x · g05 pure-ld（60／62 objs）· hello · array · slice · option。**不改语义** · 不升钉。下一：8.3.1 其它 leaf residual 或 8.3.3 soa · PC。PLATFORM: SHARED. Tip **c8f7dd69e** |
| 2026-08-01 | **wave1025** · BC 8.3.1 · sret return path helpers G.7 同叶 fold | **代码+文档** · **不升钉** · **L2**。G.7 同叶补全：`pipeline_asm_emit_return.c` 迁入 `glue_emit_sret_memcpy_rbx_to_home_elf_c` + `glue_emit_sret_return_from_var_elf_c` + `glue_copy_large_struct_from_rax_ptr_elf_c`（~103 LOC body 自 glue residual；file ~643→~772）；COUNT 仍 62；glue ~18.5k→~18.4k；被 return + struct_lit（sret_memcpy）+ struct_let（copy_large_struct via store_retval_pair）共享（glue 1989-1993 fwd 保留；新增 glue_enc_local_slot_ptr_or_addr_rbx_elf_c fwd 供 return.c 早期调用——index_helpers 在 return 之后 #include）。mac L2：pipeline_x · g05 pure-ld · hello · array · slice · option。**不改语义** · 不升钉。下一：8.3.1 其它 leaf residual 或 8.3.3 field_access／soa · PC。PLATFORM: SHARED. Tip **1c350b137** |
| 2026-08-01 | **wave1024** · BC 8.3.1 · slice dual-GP helpers + slice_from_array G.7 同叶 fold | **代码+文档** · **不升钉** · **L2**。G.7 同叶补全：`pipeline_asm_emit_block_inits.c` 迁入 `glue_slice_dual_gp_length_off_c`（~5 LOC）+ `glue_slice_dual_gp_bump_past_home_c`（~14 LOC）+ `glue_emit_slice_from_array_let_init_elf_c`（~150 LOC 自 glue residual；file ~171→~402）；COUNT 仍 62；glue ~18.7k→~18.5k；被 block_inits + call_args（slice_let_reent_deep_copy）共享（length_off 早期 fwd 保留供 call_args）。mac L2：pipeline_x · g05 pure-ld · hello · array · slice · option。**不改语义** · 不升钉。下一：8.3.1 其它 leaf residual 或 8.3.3 field_access／soa · PC。PLATFORM: SHARED. Tip **f76af70d9** |
| 2026-08-01 | **wave1023** · BC 8.3.1 · var_decl + lazy_append G.7 共享叶 fold | **代码+文档** · **不升钉** · **L2**。G.7 共享基础设施 fold：新建 `pipeline_asm_emit_var_decl.c`（~125 LOC；`glue_var_decl_type_ref_elf_c` ~30 + `glue_lazy_append_block_let_local` ~25 自 glue residual）；glue 66 行定义体 → 9 行 #include；COUNT 仍 62（同 TU #include 不增 .o）；glue ~18.8k→~18.7k；被 7 切片共享（call_args/binop/assign/unary/expr_rec/block_inits/block_body）。mac L2：pipeline_x · g05 pure-ld · hello · rv42 · option · array／slice · slice-region。**不改语义** · 不升钉。下一：slice_from_array 或 8.3.3 field_access／soa · PC。PLATFORM: SHARED. Tip **83156b1d8** |
| 2026-08-01 | **docs** · tip 双端 L4 安全网（wave1022 后日终兜底） | **文档 only** · **不升钉** · **L4**。`remaining_o=0` + `./xbuild bootstrap-driver-seed` + g05 pure-ld + 矩阵 + `XLANG_BSTRICT_SKIP_BUILD=1` 全量 bstrict **129 OK** 双端。日志：`/tmp/mac_true_cold_4bacaf18c.log` · `/tmp/mac_true_bstrict_4bacaf18c.log` · `/tmp/ubuntu_true_cold_4bacaf18c.log` · `/tmp/ubuntu_true_bstrict_4bacaf18c.log` · 矩阵 `/tmp/*_l4_matrix_4bacaf18c.log`。simd-s4 `at_builtin_smoke` XP003 双端同现、gate 标 non-fatal（s4=0 仍 status=ok）。入口：`make bootstrap-driver-seed` 已废 → **`./xbuild bootstrap-driver-seed`**。钉盘仍 **`77b334842`**。下一：var_decl／slice_from_array 或 8.3.3 · PC。PLATFORM: SHARED. Tip **4bacaf18c** |
| 2026-08-01 | **wave1022** · BC 8.3.1 · slice reent deep-copy G.7 有则补全 | **代码+文档** · **不升钉** · **L2**。G.7 同叶补全：`pipeline_asm_emit_call_args.c` 迁入 `glue_slice_let_reent_deep_copy_after_dual_gp_elf_c`（~400 LOC 自 glue residual；file ~1052→~1458）；COUNT 仍 62；glue ~19.2k→~18.8k；inventory call_args min_loc 950→1300。store_retval 仍 glue 调用（use_frame=1）；g_pipeline_asm_al_nc_seq 仍 early glue。mac L2：pipeline_x · g05 pure-ld · hello · rv42 · option · array／slice · slice-field／call-arg 邻域。**不改语义** · 不升钉。下一：var_decl／slice_from_array 或 8.3.3 · PC。PLATFORM: SHARED. Tip **4681d9e42** |
| 2026-08-01 | **wave1021** · BC 8.3.1 · array_lit durable + force_esz G.7 有则补全 | **代码+文档** · **不升钉** · **L2**。G.7 同叶补全：`pipeline_asm_emit_array_lit.c` 迁入 `glue_asm_emit_array_lit_durable_ptr_rax_elf_c` + `glue_array_lit_force_esz_from_elem_type_c`（~390 LOC 自 glue residual；file ~336→~779）；COUNT 仍 62；glue ~19.6k→~19.2k；inventory array_lit min_loc 280→650。bump／slice_from_array 仍 glue。mac L2：pipeline_x · g05 pure-ld · hello · rv42 · option · array／slice · slice-field。**不改语义** · 不升钉。下一：slice reent／var_decl 或 8.3.3 · PC。PLATFORM: SHARED. Tip **309fecf84** |
| 2026-08-01 | **wave1020** · BC 8.3.1 · emit_expr_elf_fast + emit_expr_elf_c G.7 有则补全 | **代码+文档** · **不升钉** · **L2**。G.7 同叶补全：`pipeline_asm_emit_expr_rec.c` 迁入 `pipeline_asm_emit_expr_elf_c` + `pipeline_asm_emit_expr_elf_fast`（~212 LOC 自 glue residual；file ~179→~404）；`#include` 迁至 field_access 之后；COUNT 仍 62；glue ~19.8k→~19.6k；inventory expr_rec min_loc 100→300。mac L2：pipeline_x · g05 pure-ld · hello · rv42 · option · binary-expr · float · assign_index_struct_field · many_call_args。**不改语义** · 不升钉。下一：array_lit durable／slice residual 或 8.3.3 · PC。PLATFORM: SHARED. Tip **0ef74702f** |
| 2026-08-01 | **wave1019** · BC 8.3.1 · call_arg resolve + f32 VAR slot G.7 有则补全 | **代码+文档** · **不升钉** · **L2**。G.7 同叶补全：`pipeline_asm_emit_call_args.c` 迁入 `glue_call_arg_resolve_*` + `glue_load_f32_var_slot_to_{rax,rbx}` + unusable／append_at_offset／var_is_param（~160 LOC 自 glue residual；file ~876→~1052）；COUNT 仍 62；glue ~19.9k→~19.8k；var_decl_type_ref + lazy_append 仍 glue（多叶共享）；inventory call_args min_loc 750→950 · glue 20000→19000。mac L2：pipeline_x · g05 pure-ld · hello · rv42 · float add/mul/neg · Pair／field／many_call_args · binop／assign_index。**不改语义** · 不升钉。下一：其它 leaf residual 或 8.3.3 · PC。PLATFORM: SHARED. Tip **cc29ea345** |
| 2026-08-01 | **wave1018** · BC 8.3.1 · try_binop residual G.7 有则补全 | **代码+文档** · **不升钉** · **L2**。G.7 同叶补全：`pipeline_asm_emit_binop.c` 迁入 `glue_binop_as_needs_full_emit` + `try_binop_load_operand`／clobber／preserve-restore／commutative／left_rax／cmp （~740 LOC 自 glue residual；file ~1221→~1976）；COUNT 仍 62；glue ~20.7k→~19.9k；f32 VAR slot load 仍 glue（call_args 共用）；inventory binop min_loc 1000→1500。mac L2：pipeline_x · g05 pure-ld · hello · rv42 · binop／plus_eq／mul／cmp／div／shift／bitwise／logand · field／pair／field assign。**不改语义** · 不升钉。下一：leaf residual 或 8.3.3 · PC。PLATFORM: SHARED. Tip **fe2bb236c** |
| 2026-08-01 | **wave1017** · BC 8.3.1 · type_named_struct G.7 有则补全 | **代码+文档** · **不升钉** · **L2**。G.7 同叶补全：`pipeline_asm_emit_call_args.c` 迁入 `glue_type_ref_is_named_struct_layout_elf_c`（~30 LOC 自 glue residual；file ~831→~876）；COUNT 仍 62；glue ~20.7k→~20.7k；index_helpers 同 TU 前向；inventory call_args min_loc 700→750。mac L2：pipeline_x · g05 pure-ld · hello · rv42 · Pair call-arg／Point field／Big>16B／One dual／field assign／binop／plus_eq。**不改语义** · 不升钉。下一：try_binop residual／leaf 或 8.3.3 · PC。PLATFORM: SHARED. Tip **da358a342** |
| 2026-08-01 | **wave1016** · BC 8.3.1 · assign_rhs_to_rax G.7 有则补全 | **代码+文档** · **不升钉** · **L2**。G.7 同叶补全：`pipeline_asm_emit_assign.c` 迁入 `glue_emit_assign_rhs_to_rax_elf_c`（~89 LOC 自 glue residual；file ~558→~678）；COUNT 仍 62；glue ~20.8k→~20.7k；inventory assign min_loc 500→600 · glue 21000→20000。mac L2：pipeline_x · g05 pure-ld · hello · rv42 · assign plain/compound／SAR／mod／bitwise · binop_if_both_assign · for_plus_eq · assign_index 邻域。**不改语义** · 不升钉。下一：type_named_struct／try_binop residual 或 8.3.3 · PC。PLATFORM: SHARED. Tip **8a55ecc4a** |
| 2026-08-01 | **wave1015** · BC 8.3.1 · binop residual scalar／ptr／add G.7 有则补全 | **代码+文档** · **不升钉** · **L2**。G.7 同叶补全：`pipeline_asm_emit_binop.c` 迁入 scalar f32／f64 分类器 + ptr arith scale + `add_rax_rbx`（~306 LOC 自 glue residual；file ~904→~1221）；COUNT 仍 62；glue ~21.1k→~20.8k；assign_rhs 仍 glue 前向；inventory min_loc 700→1000。mac L2：pipeline_x · g05 pure-ld · hello · rv42 · unary_neg／fourteen_add／nested_mul／four_and／six_add／block_five。**不改语义** · 不升钉。下一：assign_rhs residual／type_named_struct 或 8.3.3 · PC。PLATFORM: SHARED. Tip **1119e9367** |
| 2026-08-01 | **wave1014** · BC 8.3.1 · thin ELF return／unary／as／break·continue faces G.7 有则补全 | **代码+文档** · **不升钉** · **L2**。G.7 同叶补全：public `pipeline_asm_emit_{return,neg,lognot,bitnot,as,break,continue}_elf_c` 自 glue residual 迁入 return／unary／as／spill 叶（~40 LOC；glue ~21.1k→~21.1k）；COUNT 仍 62；expr_rec 仍直呼 static impl。mac L2：pipeline_x · g05 pure-ld · rv42／hello · unary／return／break·continue／bitnot 邻域。**不改语义** · 不升钉。下一：binop residual／leaf 或 8.3.3 · PC。PLATFORM: SHARED. Tip **22f169e03** |
| 2026-08-01 | **wave1013** · BC 8.3.1 · lvalue_eff_addr_text G.7 有则补全 | **代码+文档** · **不升钉** · **L2**。G.7 同叶补全：`pipeline_asm_emit_index_helpers.c` 迁入 `pipeline_asm_emit_lvalue_eff_addr_text_c`（~77 LOC 自 glue；file ~3039→~3135）；与 ELF twin 同叶；local_slot_text 仍 index_eff_addr（同 TU static 前向声明）；COUNT 仍 62；glue ~21.2k→~21.1k。mac L2：pipeline_x · g05 pure-ld · hello · assign-index／index 邻域。**不改语义** · 不升钉。下一：thin ELF wrappers／leaf 或 8.3.3 · PC。PLATFORM: SHARED. Tip **84f51330b** |
| 2026-08-01 | **wave1012** · BC 8.3.1 · INDEX eff-addr base+public G.7 有则补全 | **代码+文档** · **不升钉** · **L2**。G.7 同叶补全：`pipeline_asm_emit_index_eff_addr.c` 迁入 local_slot_text + base_{elf,text} + public index_eff_addr_{elf,text}（~198 LOC；file ~697→~895）；COUNT 仍 62；lvalue_eff_addr_text 仍 glue residual；ELF lvalue 仍 index_helpers；inventory min_loc 700。mac L2：pipeline_x · g05 pure-ld · hello · asm index／assign-index 邻域。**不改语义** · 不升钉。下一：lvalue_text／leaf 或 8.3.3 · PC。PLATFORM: SHARED. Tip **`cd79949f8`** |
| 2026-08-01 | **wave1011** · BC 8.3.1 · asm ELF expr recursion dispatcher domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_expr_rec.c`（lit_i32 + emit_expr_elf_rec · ~139 LOC body／~179 file）自 `pipeline_glue.c` 抽出；face 仍各域叶；slow 仍 backend residual；`PIPELINE_X_DEPS` COUNT 61→62 + g05 unbundle STALE + `bc-inventory` + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv42／hello／binary-expr／array／asm index 邻域。**不改语义** · 不升钉。下一：lvalue_eff／index_eff_base 或 8.3.3 · PC。PLATFORM: SHARED. Tip **`c041f8139`** |
| 2026-08-01 | **wave1010** · BC 8.3.1 · asm ELF INDEX effective-address scaled domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_index_eff_addr.c`（rax_plus_rbx_scaled + bounds_guard + rvalue_slice_once + eff_addr_scaled · ~653 LOC body／~697 file）自 glue 抽出；try_index 仍 index_helpers；face 仍 index／assign；`PIPELINE_X_DEPS` COUNT 60→61 + g05 unbundle STALE + `bc-inventory` + leaf residual。mac L2：pipeline_x · g05 · rv42／option／hello · array／slice · asm index／assign-index／binop-index。**不改语义** · 不升钉。下一：expr_rec／leaf 或 8.3.3 · PC。PLATFORM: SHARED. Tip **`59d843307`** |
| 2026-08-01 | **wave1007** · BC 8.3.1 · asm ELF struct let-init domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_struct_let.c`（struct_let_init + type_let_init + sret · ~176 LOC body／~215 file）自 glue 抽出；`PIPELINE_X_DEPS` COUNT 57→58 + g05/inventory/leaf。mac L2 矩阵绿。**不改语义** · 不升钉。下一：spill／bulk_mem_copy／index residual 或 8.3.3 · PC。PLATFORM: SHARED. Tip **`8d9806d68`** |
| 2026-08-01 | **wave1008** · BC 8.3.1 · asm ELF INDEX residual helpers domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_index_helpers.c`（module_from_ctx + param/local slot + field_type_ref + fixed_array + esz_from_type_ref + try_index forest + soa_index_field_addr + lvalue_eff_addr · ~2988 LOC body／~3038 file）自 `pipeline_glue.c` 抽出；eff_addr_scaled／assign finish／bulk_mem_copy／Chaitin spill 仍 glue；`PIPELINE_X_DEPS` COUNT 58→59 + g05 unbundle STALE + `bc-inventory` + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv42／option／si／hello · array／struct／slice／generic／dual／slice-field。**不改语义** · 不升钉。下一：spill／bulk_mem_copy 或 8.3.3 · PC。PLATFORM: SHARED. Tip **`ff967e0b5`** |
| 2026-08-01 | **wave1009** · BC 8.3.1 · asm ELF 7.3 live／Chaitin spill／bulk_mem／index-assign residual thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_spill.c`（live_fwd + CFG phi + Chaitin K=6 color + bulk_mem_copy_spills + index_assign_finish_store + index scratch／binop_stack_spill method bodies · ~2487 LOC body／~2532 file）自 `pipeline_glue.c` 抽出；CAP statics 仍 index_helpers；eff_addr_scaled 仍 glue；`PIPELINE_X_DEPS` COUNT 59→60 + g05 unbundle STALE + `bc-inventory`（glue min_loc 24k→21k）+ leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv42／option／si／hello · array／slice／generic。**不改语义** · 不升钉。下一：eff_addr_scaled／8.3.3 · PC。PLATFORM: SHARED. Tip **`fd3c254d8`** |
| 2026-08-01 | **wave1006** · BC 8.3.1 · asm ELF SIMD vector lane／shuffle／select／fma domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_vector_simd.c`（~1436 LOC）自 glue 抽出；`PIPELINE_X_DEPS` COUNT 56→57。mac L2 矩阵绿。**不改语义** · 不升钉。Tip **`db8703563`** |
| 2026-08-01 | **wave1005** · BC 8.3.1 · asm ELF vector_let／fixed-array field store domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_vector_let.c`（leaf_elem_byte_sz + array_lit_flat + vector_let_init + field_frame_mag + store_fixed_array_field · ~645 LOC body／~686 file）自 `pipeline_glue.c` 抽出；glue_emit_fixed_array_type_let_init 仍 glue 薄包装；SIMD lane／shuffle／fma 仍 glue。`PIPELINE_X_DEPS` COUNT 55→56 + g05 unbundle STALE + `bc-inventory` + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv42／option／si／hello · array／literal／partial_init／slice 邻域。**不改语义** · 不升钉。下一：spill／SIMD vector lane 或 8.3.3 · PC。PLATFORM: SHARED. Tip **`feca3419e`** |
| 2026-08-01 | **wave1004** · BC 8.3.1 · asm ELF STRUCT_LIT emit domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_struct_lit.c`（field_store_sz + public wrapper + DEST_IN_RBX/rehome + fields + struct_lit_elf · ~444 LOC body／~484 file）自 glue 抽出；store_fixed／vector_let 本波后进 vector_let 叶。`PIPELINE_X_DEPS` COUNT 54→55。mac L2 矩阵绿。Tip **`045e35403`** |
| 2026-08-01 | **wave1003** · BC 8.3.1 · asm ELF CALL-arg emit domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_call_args.c`（lea_not_load + dual-GP load_var + named layout size + pass_addr + for_call_args · ~792 LOC body／~831 file）自 `pipeline_glue.c` 抽出；resolve_* 仍 glue（leaf VAR 共享）；CALL／METHOD_CALL 仍 seed `backend_call_dispatch`。`PIPELINE_X_DEPS` COUNT 53→54 + g05 unbundle STALE + `bc-inventory` + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv42／option／si／hello · generic／struct_get／mk_field／assign_index／many_call_args／array／slice／binop 邻域。**不改语义** · 不升钉。下一：struct_lit／spill／leaf 或 8.3.3 · PC。PLATFORM: SHARED. Tip **`c6fa3c221`** |
| 2026-08-01 | **wave1002** · BC 8.3.1 · asm ELF relational CMP emit domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_cmp.c`（emit_cmp_elf + enum RHS tag + 64bit/rex + f32/f64/int finish · ~278 LOC body／~315 file）自 `pipeline_glue.c` 抽出；typekind_variant_tag 仍 glue。`PIPELINE_X_DEPS` COUNT 52→53 + g05 unbundle STALE + `bc-inventory` + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv42／option／si／hello · cmp 邻域。**不改语义** · 不升钉。下一：call-arg 或 8.3.3 · PC。PLATFORM: SHARED. Tip **`e26842b17`** |
| 2026-08-01 | **wave1001** · BC 8.3.1 · asm ELF BINOP emit domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_binop.c`（add／sub／mul／div／mod／and／bitwise／shift + unsigned／64bit classifiers + nested rax／rbx helpers · ~857 LOC body／~904 file）自 `pipeline_glue.c` 抽出；panic include 前置同点；`PIPELINE_X_DEPS` COUNT 51→52 + g05 unbundle STALE + `bc-inventory` + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv42／option／si／hello · binop_return_fourteen_add／nested_mul／four_and／div／block_three／assign_index_binop／field_index suite。CALL／METHOD_CALL 仍 seed。**不改语义** · 不升钉。下一：cmp／leaf／call-arg 或 8.3.3 · PC。PLATFORM: SHARED. Tip **`9c7871b8a`** |
| 2026-08-01 | **wave1000** · BC 8.3.1 · asm ELF FIELD_ACCESS emit domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_field_access.c`（layout_by_name + call_arg struct／agg + call_base rvalue + var_field_access + field_access_elf_fast · ~583 LOC body／~632 file）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` COUNT 50→51 + g05 unbundle STALE + `bc-inventory` + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv42／option／si／hello · struct_get／mk_field_inline／assign_index_struct_field／slice data_field／struct simple。**不改语义** · 不升钉。下一：call／binop 或 8.3.3 · PC。PLATFORM: SHARED. Tip **`fbf3eba78`** |
| 2026-08-01 | **wave999** · BC 8.3.1 · asm ELF PANIC／div-zero face thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_panic.c`（xlang_panic_call + panic_elf + panic_int_div_zero + divisor_zero_check_rbx · ~121 LOC）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` COUNT 49→50 + g05 unbundle STALE + `bc-inventory` + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv42／hello／option／si · panic suite（exit1）· div=5／mod=1 · match／binop_div。**不改语义** · 不升钉。下一：field_access_emit 或 8.3.3 · PC。PLATFORM: SHARED. Tip **`24592db7e`** |
| 2026-08-01 | **wave998** · BC 8.3.1 · asm ELF MATCH／EXPR_IF emit domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_match.c`（match_elf + expr_if_elf · arm cmp+jeq／wildcard join／RETURN skip · cond jz then／else · ~179 LOC）自 `pipeline_glue.c` 抽出；CALL／METHOD_CALL 仍 seed `backend_call_dispatch`（glue 仅前向声明）。`PIPELINE_X_DEPS` COUNT 48→49 + g05 unbundle STALE + `bc-inventory` + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv42／hello · match suite · if_lt／binop_if · array。**不改语义** · 不升钉。下一：panic／field_access_emit 或 8.3.3 · PC。PLATFORM: SHARED. Tip **`a1bb8e748`** |
| 2026-08-01 | **wave997** · BC 8.3.1 · asm ELF INDEX／ADDR_OF／DEREF emit domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_index.c`（index_elem_byte_sz + emit_index + addr_of + deref · multi-dim／SLICE dual-GP／lea-slot／TYPE_ARRAY leave · ~434 LOC）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` COUNT 47→48 + g05 unbundle STALE + `bc-inventory` + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv42／option／si／hello · array／slice／pointer。**不改语义** · 不升钉。下一：match／call 或 8.3.3 · PC。PLATFORM: SHARED. Tip **`2ec7e5b2e`** |
| 2026-08-01 | **wave996** · BC 8.3.1 · asm ELF EXPR_ARRAY_LIT emit domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_array_lit.c`（elem_byte_sz + empty + emit／force_esz · nested flat／SLICE dual-GP／>8B STRUCT／may_clobber re-lea · ~335 LOC）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` COUNT 46→47 + g05 unbundle STALE + `bc-inventory` + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv42／option／si／hello · array／slice · asm assign-index 邻域。**不改语义** · 不升钉。下一：index／addr_of／match 或 8.3.3 · PC。PLATFORM: SHARED. Tip **`d12294fe9`** |
| 2026-08-01 | **wave995** · BC 8.3.1 · asm ELF EXPR_ASSIGN emit domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_assign.c`（lhs f32 + rhs + assign_elf · FIELD／INDEX／VAR／DEREF · slice dual-GP／fixed-array／STRUCT_LIT index／esz>8 bulk · ~556 LOC）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` COUNT 45→46 + g05 unbundle STALE + `bc-inventory` + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv42／option／hello · let-const／array／struct／slice · asm assign-index 邻域。**不改语义** · 不升钉。下一：array_lit／index／match 或 8.3.3 · PC。PLATFORM: SHARED. Tip **`3c05c56d9`** |
| 2026-08-01 | **wave994** · BC 8.3.2 · ast_pool top_level hoist_target／sum residual 有则补全 | **代码+文档** · **不升钉** · **L2**。G.7 有则补全 `ast_pool_top_level.c`：迁入 `pipeline_asm_hoist_target_func_index` + `pipeline_asm_sum_module_top_level_lets_stack`（~77 LOC；file ~326）；COUNT 仍 45；modlet／slot helpers 仍 glue extern。mac L2：g05 pure-ld OK · rv42／option／hello／let-const／toplevel_let／while／for／defer／goto 邻域。**不改语义** · 不升钉。下一：下一 asm／8.3.3 field_access · PC。PLATFORM: SHARED. Tip **`3eb037191`** |
| 2026-08-01 | **wave993** · BC 8.3.2 · ast_pool top_level name_is_const／hoist residual 有则补全 | **代码+文档** · **不升钉** · **L2**。G.7 有则补全 `ast_pool_top_level.c`：迁入 `pipeline_module_top_level_name_is_const` + `pipeline_module_hoist_top_level_lets_into_main`（~115 LOC；file ~249）；COUNT 仍 45；block 后 include 可见 static prepend_lets。mac L2：g05 pure-ld OK · rv42／option／hello／si／let-const／toplevel_let／while／for／defer／goto 邻域。**不改语义** · 不升钉。下一：下一 asm／8.3.3 field_access · PC。PLATFORM: SHARED. Tip **`702037f23`** |
| 2026-08-01 | **wave991** · BC 8.3.2 · ast_pool onefunc fill residual 有则补全 | **代码+文档** · **不升钉** · **L2**。G.7 有则补全 `ast_pool_onefunc.c`：迁入 defer/labeled/if/region/with_arena/unsafe/stmt_order/body_expr + fill_defers/labeled/regions/ifs/stmt_order/expr_stmts/whiles/fors（~438 LOC；file ~986）；COUNT 仍 45。core residual：module_top_level_name_is_const + stmt_order rebuild/fixup。mac L2：g05 pure-ld OK · rv42／option／hello／let-const／while／for／defer／goto 邻域。**不改语义** · 不升钉。下一：stmt_order rebuild residual／下一 asm · PC。PLATFORM: SHARED. Tip **`c27dcc832`** |
| 2026-08-01 | **wave990** · BC 8.3.2 · ast_pool block parent／resolve residual 有则补全 | **代码+文档** · **不升钉** · **L2**。G.7 有则补全 `ast_pool_block.c`：parent patch + resolve_var_type_ref／name_binding／local_redecl／find_var（~359 LOC；file ~1140）；COUNT 仍 45；`pipeline_arena_expr_ptr`。mac L2 邻域绿。**不改语义** · 不升钉。下一：fill residual。PLATFORM: SHARED. Tip **`253c8035c`** |
| 2026-08-01 | **wave989** · BC 8.3.2 · ast_pool block residual 有则补全 | **代码+文档** · **不升钉** · **L2**。G.7 有则补全 `ast_pool_block.c`：迁入 append expr_stmt/stmt_order/while/for/labeled + static block_*_at + while/for/labeled getters + const/let/if/expr/stmt_order getters（~438 LOC；file ~763 LOC）；COUNT 仍 45（无新叶）。core residual：onefunc defer/labeled + fill_* + parent patch + resolve。顺修 leaf residual COUNT 断言 44→45（wave988 漏）。mac L2：g05 pure-ld OK · rv42／option／hello／let-const／while／for／defer／goto 邻域。**不改语义** · 不升钉。下一：resolve residual／fill residual 或 下一 asm · PC。PLATFORM: SHARED. Tip **`e773f03bd`** |
| 2026-08-01 | **wave988** · BC 8.3.2 · ast_pool block append／region／defer domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`ast_pool_block.c`（block_pool_append_pos + append const/let/if/region/with_arena/unsafe/defer + region/defer accessors · ~297 LOC body）自 `ast_pool.c` 抽出；while/for/labeled/getters／resolve／onefunc defer residual 仍 core。`PIPELINE_X_DEPS` COUNT 44→45 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · return-value／option／hello／let-const／while／for／defer。**不改语义** · 不升钉。下一：block residual／resolve residual 或 下一 asm · PC。PLATFORM: SHARED. Tip **`ce6da9c29`** |
| 2026-08-01 | **wave987** · BC 8.3.2 · ast_pool ASTArena main-pool cold accessors domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`ast_pool_arena.c`（type/expr/block/func ptr/alloc/get/set + write_var/binop + num_types + caps · ~220 LOC body）自 `ast_pool.c` 抽出；block_at／sidecar helpers／block append／resolve 仍 core。`PIPELINE_X_DEPS` COUNT 43→44 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · return-value／option／hello／let-const／while／for。**不改语义** · 不升钉。下一：block／resolve residual 或 下一 asm · PC。PLATFORM: SHARED. Tip **`a0e768fc3`** |
| 2026-08-01 | **wave986** · BC 8.3.2 · ast_pool module Func cold accessors domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`ast_pool_module_func.c`（alloc/ref/flags/params/name_equal/byte/body_expr + arena_func param_write/copy_slot · ~409 LOC body）自 `ast_pool.c` 抽出；static helpers + visibility/L7 + glue name/body 仍 core。`PIPELINE_X_DEPS` COUNT 42→43 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · return-value／option／hello／let-const／while／for。**不改语义** · 不升钉。下一：arena／resolve residual 或 下一 asm · PC。PLATFORM: SHARED. Tip **`b4133d1bc`** |
| 2026-08-01 | **wave985** · BC 8.3.2 · ast_pool PipelineDepCtx cold accessors domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`ast_pool_dep_ctx.c`（reset／import_path／ndep／prefix／path_buf／entry_dir／source buf／mode flags／current_*／lib_root／empty_param · ~480 LOC body）自 `ast_pool.c` 抽出；path_append／resolve／load 仍 core。`PIPELINE_X_DEPS` COUNT 41→42 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · return-value／option／hello／let-const／while／for。**不改语义** · 不升钉。下一：module_func／arena／resolve residual 或 下一 asm · PC。PLATFORM: SHARED. Tip **`12e6c4e7a`** |
| 2026-08-01 | **wave984** · BC 8.3.2 · ast_pool OneFunc const/let/param/call/while/for domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`ast_pool_onefunc.c`（const/let/param/call/while/for + copy_sidecar · ~522 LOC body；`grow_vec_copy_append` 上提 core 供 onefunc+dep_ctx 共享）自 `ast_pool.c` 抽出；`PIPELINE_X_DEPS` COUNT 40→41 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv42／option／hello／let-const／while／for。**不改语义** · 不升钉。下一：dep_ctx／module_func／arena 或 下一 asm · PC。PLATFORM: SHARED. Tip **`cc9bd2de9`** |
| 2026-08-01 | **wave983** · BC 8.3.2 · ast_pool module ModuleEnumEntry + enum field-access mark domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`ast_pool_module_enum.c`（`pipeline_module_enum_*` + expr/codegen try_mark_enum_field_access · ~336 LOC body；domain-private `pipeline_enum_name_from_field_access_base`）自 `ast_pool.c` 抽出；`PIPELINE_X_DEPS` COUNT 39→40 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv42／option／hello · enum minimal/simple/match · c_enum_variant_const · match 邻域。**不改语义** · 不升钉。下一：arena_block／module_func 或 下一 asm · PC。PLATFORM: SHARED. Tip **`e73e71cd8`** |
| 2026-08-01 | **wave982** · BC 8.3.2 · ast_pool expr (+ type-pos) var-len sidecar domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`ast_pool_expr_sidecar.c`（call/method/match/struct_lit/array_lit + type_type_arg · ~620 LOC body；domain-private slot helpers）自 `ast_pool.c` 抽出；`PIPELINE_X_DEPS` COUNT 38→39 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv/option/si/hello · match/struct_mk/array_lit/many_call_args/generic/type_alias 邻域。**不改语义** · 不升钉。下一：arena_block 或 module_func/enum · PC。PLATFORM: SHARED. Tip **`0b0f08485`** |
| 2026-08-01 | **wave981** · BC 8.3.2 · ast_pool module TypeAliasEntry cold domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`ast_pool_type_alias.c`（`pipeline_module_type_alias_*` + `num_type_aliases_at` · ~80 LOC body）自 `ast_pool.c` 抽出；`PIPELINE_X_DEPS` COUNT 37→38 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · hello／type_alias／const_select_alias／return-value／let-const／struct_mk 邻域。**不改语义** · 不升钉。下一：arena_block 或 下一 asm · PC。PLATFORM: SHARED. Tip **`df576b287`** |
| 2026-08-01 | **wave980** · BC 8.3.2 · ast_pool module TopLevelLetEntry cold domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`ast_pool_top_level.c`（`pipeline_module_top_level_let_*` alloc/set/name/type/init/const/export · ~113 LOC）自 `ast_pool.c` 抽出；`name_is_const` 扫描消费方仍 core。`PIPELINE_X_DEPS` COUNT 36→37 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · hello／let-const／import const／run-toplevel-let／run-let-const 邻域。**不改语义** · 不升钉。下一：type_alias 或 下一 asm · PC。PLATFORM: SHARED. Tip **`144793cfb`** |
| 2026-08-01 | **wave979** · BC 8.3.2 · ast_pool module StructLayout cold domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`ast_pool_struct_layout.c`（`pipeline_module_struct_layout_*` / num / type_param meta · ~365 LOC + domain-private `layout_type_param_meta_at`）自 `ast_pool.c` 抽出；`PIPELINE_X_DEPS` COUNT 35→36 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · hello／return-value／struct_mk／struct_field／struct_pair／d2_struct_layout 邻域。**不改语义** · 不升钉。下一：top_level 或 下一 asm · PC。PLATFORM: SHARED. Tip **`1acb2be7c`** |
| 2026-08-01 | **wave978** · BC 8.3.2 · ast_pool module ImportEntry cold-twin domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`ast_pool_module_import.c`（XLANG_WEAK `pipeline_module_import_*` cold twins · wave110 Cap residual ~208 LOC）自 `ast_pool.c` 抽出；`PIPELINE_X_DEPS` COUNT 34→35 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · rv42／import-mod／nested-import／many_import_select／return-value／pure_lets／hello 邻域。**不改语义** · 不升钉。下一：8.3.2 下一域 或 下一 asm · PC。PLATFORM: SHARED. Tip **`928fa7501`** |
| 2026-08-01 | **wave977** · BC 8.3.1 · asm ELF block const/let init emit domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_block_inits.c`（const+let 循环 · VECTOR/SLICE/fixed-array/struct · empty dual-GP · f32→f64 ~171 LOC）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` COUNT 33→34 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · pure_lets／const_lets／let_if／if_else／nested／while_if／logand／ret_unary／multi_lets 邻域探针。**不改语义** · 不升钉。下一：8.3.2 ast_pool 或 下一 asm 域 · PC。PLATFORM: SHARED. Tip **`a7149738f`** |
| 2026-08-01 | **wave976** · BC 8.3.1 · asm ELF block-level if-stmt emit domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_block_if_stmt.c`（then-first + jz / else / done + live-end merge ~106 LOC）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` COUNT 32→33 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · let_if／if_else／nested／consec／while_if／pure_lets／logand／return／unary 邻域探针。**不改语义** · 不升钉。下一：block_inits 或 8.3.2 · PC。PLATFORM: SHARED. Tip **`9dd59b7bf`** |
| 2026-08-01 | **wave975** · BC 8.3.1 · asm ELF block body sync emit domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_block_body.c`（pass0／pass1 let defer + body_sync + backend_emit 入口 + Block accessors ~809 LOC）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` COUNT 31→32 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · let_if／nested／while／pure_lets／logand／return／unary 邻域探针。**不改语义** · 不升钉。下一：block_if_stmt／block_inits 或 8.3.2 · PC。PLATFORM: SHARED. Tip **`42dd6f2c7`** |
| 2026-08-01 | **wave974** · BC 8.3.1 · asm ELF LOGAND／LOGOR short-circuit emit domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_logand.c`（logand_impl + logor_impl ~102 LOC）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` COUNT 30→31 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 pure-ld OK · logand／logor／return／as／unary／binary-expr/logical 邻域探针。**不改语义** · 不升钉。下一：block body 或 8.3.2 · PC。PLATFORM: SHARED. Tip **`88016f213`** |
| 2026-08-01 | **wave973** · BC 8.3.1 · asm ELF return emit domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_return.c`（slice-escape match/finder + durable COMMON escape + return_impl ~633 LOC）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` COUNT 29→30 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 OK · return／as／unary／logand 邻域探针。**不改语义** · 不升钉。下一：logand·logor／block 或 8.3.2 · PC。PLATFORM: SHARED. Tip **`2bf69cd8a`** |
| 2026-08-01 | **wave972** · BC 8.3.1 · asm ELF as／await／try／float-lit emit domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_as.c`（ord-54 await／as 分类 + await_sync + try `?` + float_lit／array scalar + as_impl ~411 LOC）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` COUNT 28→29 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 OK · as／logand／unary／return 邻域探针。**不改语义** · 不升钉。下一：return／logand·logor／block 或 8.3.2 · PC。PLATFORM: SHARED. Tip **`65cdbf447`** |
| 2026-08-01 | **wave971** · BC 8.3.1 · asm ELF unary emit domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_asm_emit_unary.c`（NEG／LOGNOT／BITNOT + sxt／jz ~259 LOC）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` COUNT 27→28 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 OK · 邻域探针。**不改语义** · 不升钉。下一：~~as emit~~ wave972。PLATFORM: SHARED. Tip **`7bb2ef491`** |
| 2026-08-01 | **wave970** · BC 8.3.1 · typeck region-assign domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_typeck_region_assign.c`（M-3 slice region + return slice + WPO-S3 stack-escape assign + MEM-A3 scope-borrow + MEM-C1 with_arena／allocator region ~449 LOC）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` COUNT 26→27 + g05 unbundle STALE + inventory／leaf。mac L2：pipeline_x · g05 · 邻域绿。**不改语义** · 不升钉。下一：~~asm unary emit~~ wave971。PLATFORM: SHARED. Tip **`a275ef6aa`** |
| 2026-08-01 | **wave969** · BC 8.3.1 · typeck check_block domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_typeck_check_block.c`（block_impl bind／restore／touch + loop／unsafe depth + check_block_impl_c + weak check_block_impl + check_block_c + as_loop_body ~288 LOC）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` COUNT 25→26 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 OK · rv42／si／邻域。**不改语义** · 不升钉。下一：region-assign／asm emit 或 8.3.2 · PC。PLATFORM: SHARED. Tip **`ad38a9645`** |
| 2026-08-01 | **wave968** · BC 8.3.1 · typeck method_call domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_typeck_method_call.c`（weak method_call + pattern-unify／mono-map／subst + generic UFCS ~975 LOC）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` COUNT 24→25 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 OK · rv42=42 · si OK · method_i32=42。**不改语义** · 不升钉。下一：~~check_block~~ wave969。PLATFORM: SHARED. Tip **`25fca4a17`** |
| 2026-08-01 | docs · **31 日日切归档** | 主文档 §6 **wave895–963**（+ 30 日二次归楼 docs 行）整表迁入 [自举进度-归档-2026-07-31.md](自举进度-归档-2026-07-31.md)；本文 8 月 1 日起重新起表。钉盘 **`77b334842`** · tip L4 安全网 **`f80aa4593`** · residual tip **wave968** @ **`25fca4a17`**。 |
| 2026-08-01 | **wave967** · BC 8.3.1 · typeck coerce_init domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_typeck_coerce_init.c`（lit/float/enum/call/array｜vector/binop/struct/slice + `coerce_init_expr_to_decl_c` ~460 LOC）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` COUNT 23→24 + g05 unbundle STALE + `bc-inventory` 行 + leaf residual。mac L2：cc pipeline_x OK · g05 OK · 矩阵 rv42/opt/si/hello · coerce probe exit 44（300&0xff）。**不改语义** · 不升钉。下一：8.3.1 下一域 / 8.3.2 · PC。PLATFORM: SHARED. Tip **`4bd1e12bb`** |
| 2026-08-01 | **wave966** · BC 8.3.1 · typeck assign domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_typeck_assign.c` 自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` + g05 STALE + inventory/leaf 接线。mac L2：pipeline_x · g05 · 产品矩阵绿。**不改语义** · 不升钉。下一：~~coerce_init~~ wave967。PLATFORM: SHARED. Tip **`0995994bb`** |
| 2026-08-01 | **wave965** · BC 8.3.1 · typeck CTFE domain thin | **代码+文档** · **不升钉** · **L2**。G.7 同 TU `#include`：`pipeline_typeck_ctfe.c`（is_const + fold 生产者 ~1.2k）自 `pipeline_glue.c` 抽出；`PIPELINE_X_DEPS` + g05 unbundle STALE 收 field_access/soa/ctfe/ast_pool；`bc-inventory` 登记 8.3.1 ctfe 行。mac L2：cc pipeline_x OK · g05 OK · 矩阵 rv42/opt/si/hello · lang-const 核心 CTFE 绿（const_chain/match/if/ternary/struct）；3 soft（compare/logical T001 · array_len 序）非本波引入。**不改语义** · 不升钉。下一：8.3.1 下一域 / 8.3.2 · PC。PLATFORM: SHARED. |
| 2026-08-01 | **wave964** · post-delete · ensure_host_cc --check post_ship | **代码+文档** · **不升钉** · **L2**。`ensure_host_cc_seed_o.sh --check`：MF 缺席不再 `FAIL missing Makefile` → post_ship 验 catalog/mk R1–R3/R2 面 + seed map + try-heat/try-r1/try-r2/gen-x + CFLAGS catalog-load + wave950 ensure_xlang_c + B4/B5 表；MF thin residual N/A；warm `XLANG_CATALOG_CACHE_FILE` 防 pipe 子 shell 重解析。leaf honesty wave964 跑 `--check`。mac L2：ensure CHECK OK ~27s · leaf CHECK OK。**不改产品二进制** · 不升钉。下一：8.3.1/8.3.2 切片 · PC。PLATFORM: SHARED. |

***
