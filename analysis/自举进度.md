# Xlang / X language 自举进度

> **更新**：2026-08-07 · tip **wave248** typeck overload pick／resolve pure leave（method_call 子域 · **不升钉**）\
> **产品 L4 放行钉盘**：**`db809e00f`**（wave1277 升钉 · 前 `77b334842` wave942）· **4 日已合并到 dev(`02d080583`)/main(`a22474203`)**\
> **产品 residual tip**：8.3.1 B′ 域 pure leave wave248（overload pick／resolve → typeck.x）· inventory present **56** ROWS=119 · tip **`46f579912`**· 钉盘仍 **`db809e00f`** · L4 安全网仍 **`ffc0b646f`** · Stage 8 tip **713277ef9**\
> **tip L4 安全网**：**`ffc0b646f`**（冷+矩阵基线 `eaccdb1f5` · 修 harness 后双端 `SKIP_BUILD` 129 OK · **不升钉**；前网 `4bacaf18c`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：**MG ✅** · **BC 🟡**（wave248 overload pure leave **mac L2 绿** · Ubuntu 金标待补 · residual present **56** · method_call ~3209→2812 · **停 micro-cell**）· **L4 冷构建已恢复** · **Stage 8 开 13/30** · **钉盘 `db809e00f`** · **不升钉**\
> **下波候选（高效）**：**typeck method_call 残**（generic UFCS／fixup／method_call_c）；**elf_ctx／elf_write**；**ast_pool 叶**；pipeline_x mega 离 host-cc；升钉前 tip L4+bstrict 复证；Ubuntu 补 wave241–248 金标；tip 产品 `xlang` mega `-E` SEGV（prefer 用 `xlang-x`）\
> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1；过程上 prefer+heat 一次／域，双端矩阵同波一次\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；**SHARED 双端 L2**；谈自举／升钉才 L4 + 矩阵 + 全量 bstrict

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[06](自举进度-归档-2026-08-06.md) · [05](自举进度-归档-2026-08-05.md) · [04](自举进度-归档-2026-08-04.md) · [03](自举进度-归档-2026-08-03.md) · [02](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · …

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `db809e00f`**（wave1277 升钉 · 前 `77b334842` wave942）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（Makefile 已删除；**变更记录只在本文 §6**）。

| 指标 | 状态 |
|------|------|
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`db809e00f`**（wave1277）；residual tip wave248 overload pick／resolve pure leave（present **56** · 6 日见 [06 归档](自举进度-归档-2026-08-06.md)）；Stage 8 tip **713277ef9**（wave1041 · **13/30**）；tip L4 安全网 **`ffc0b646f`**（**不升钉**） |
| Track MG | **11.3.1 MG ✅**（wave941 物理删 Makefile · wave942 catalog CFLAGS · 0 make in bootstrap · 产品入口 `./xbuild`） |
| Track BC | **开 🟡**（8.3.1 B′ pure leave 推进至 wave248 overload pick／resolve；Cap residual present **56**；残 method_call ~2812／region_assign thin；下刀 method_call 残／elf／ast_pool／pipeline_x mega；**BC 终局＝pipeline_x 离 host-cc 仍 ⬜**） |
| Track L (Stage 8) | **开**（批次 0 清理 ✅ · 批次 2 **wave1036–1041** 退役 ✅ · **13/30** retired · 详见 [阶段8高效退役分析.md](阶段8高效退役分析.md)） |
| mac / Ubuntu L4（钉盘） | ✅ Ubuntu L4 真冷（wipe + bootstrap + build + smoke）；✅ macOS build |
| mac / Ubuntu L4（tip 安全网） | ✅ 冷+矩阵 `@eaccdb1f5`；✅ 修 r04 harness 后 bstrict `@ffc0b646f`（**双端 129 OK**） |
| 本波构建级别 | 日常 BC **L2**（SHARED 双端）；钉盘 **`db809e00f`**；tip L4 安全网历史 **`ffc0b646f`** |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录** |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图 |
| [自举进度-归档-2026-08-06.md](自举进度-归档-2026-08-06.md) | **8月6日** B′ pure leave field_access→wave199 CALL-target resolve（**50** 行） |
| [自举进度-归档-2026-08-05.md](自举进度-归档-2026-08-05.md) | **8月5日** L4 冷恢复 + 8.3.3 R2 + pure-asm match + B′ leave 至 binop（**74** 行） |
| [自举进度-归档-2026-08-04.md](自举进度-归档-2026-08-04.md) | **8月4日 wave1241–1291**（BC ast_pool 16 波域分解 + glue 壳 thin + check 收口 + 升钉 `db809e00f`） |
| [自举进度-归档-2026-08-03.md](自举进度-归档-2026-08-03.md) | **8月3日 wave1227–1240**（BC glue 叶收口 + check 工具链启动 · 15 行） |
| [自举进度-归档-2026-08-02.md](自举进度-归档-2026-08-02.md) | **8月2日 wave1130–1220**（BC glue 续迁 + typeck 跨模块字段根修 · 43 行） |
| [自举进度-归档-2026-08-01.md](自举进度-归档-2026-08-01.md) | **8月1日 wave964–1111-1129**（BC 8.3.1 G.7 fold 50+ 叶 + Stage 8 退役 13/30） |
| [自举进度-归档-2026-07-31.md](自举进度-归档-2026-07-31.md) | **31 日全日 wave895–963**（MG 真删 + post-delete + 开 BC） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单 |
| 产品 L4 放行钉盘 | **`db809e00f`** | wave1277 升钉；16 波 BC ast_pool 域分解 -84% + check 566/566 双端绿 + nostdlib Linux 修复；前 `77b334842`（wave942） |
| tip L4 安全网 | **`ffc0b646f`** | 冷+矩阵基线 `eaccdb1f5` + 修 harness 后双端 SKIP_BUILD bstrict **129** · **钉盘不升**（前 `4bacaf18c`） |
| residual tip（BC） | **`d6602da61`** | wave243 stamp_let＋read_ptr pure leave（mac L2）· inventory present **56** · 前 wave242 **1a696f3ac** · 6 日见 [06 归档](自举进度-归档-2026-08-06.md)；钉盘仍 `db809e00f` |
| mac 钉盘 L4 | ✅ build exit=0 | macOS Darwin/arm64 g05 relink OK |
| Ubuntu 钉盘 L4（金标） | ✅ L4 真冷 | Ubuntu Linux/x86_64 wipe + bootstrap + build + smoke 全绿 |
| 产品矩阵（tip L2） | ✅ rv42 / option / si / hello | wave191 双端 L2；assign lit=15 · continue=6 · field_index_fast=205 · soa_smoke=8 |
| 前钉 | `db809e00f` · `77b334842` · `9bb7a757c` · … | wave1277 → wave942 → … |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 18 号结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **R2 / Cap** | wave710 升钉后按需 | 明细见 29 归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1**（已执行） |
| `TREE_ARMED` | **1** |
| `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1**（wave941 物理删除） |
| `PHYS_DEL_WINDOWS_GATE_STATUS` | `reproven_green` @ `71dd60a12`（wave922 · wave940 一键冷启动） |
| preflight blockers | **全清零**（wave937–939） |
| 真删条件 | ✅ 已完成 @ wave941/942 · 钉盘 **`77b334842`**（后升至 **`db809e00f`**） |
| post-delete residual | **✅ 已收**（wave944–964） |

***

## 3. 当前前排（≤3 条）

1. **BC 轨 B′ 域 pure leave（7 日 · 高效转向）** — residual present **56**；wave224–248 已收 …／call-resolve／**overload pick／resolve（for_emit＋pick_for_call）**。**禁止** micro-cell 波。**下刀**：method_call 残（generic UFCS／fixup／method_call_c）／elf／ast_pool 整叶 pure leave 使 present−1；pipeline_x mega 离 host-cc；Ubuntu 补 wave241–248。**未完成**：产品链仍 `cc` 编 `pipeline_x`（BC 终局 ⬜）。
2. **L4 真冷构建已恢复**（5 日归档）— 4 层冷债已修；match field-bind／guard／field-lit 已闭。升钉前仍须 tip L4+全量 bstrict 复证（**不升钉**至复证绿）。
3. **PC 轨** — 用户默认仍可能 `labi_invoke_cc`；默认 asm backend 终局 ⬜。post-delete residual **✅ 已收**。check 闸门自举期 **暂停**。

***

## 4. 与归档的关系

- **8月6日归档**：[自举进度-归档-2026-08-06.md](自举进度-归档-2026-08-06.md) — B′ pure leave field_access／expr_rec／block_body／struct_lit → spill／7.3／CFG／index／call_args 至 wave199（**50** 行 · 钉盘仍 `db809e00f` · 日终 tip `b5d929d4e` · present **59**）。
- **8月5日归档**：[自举进度-归档-2026-08-05.md](自举进度-归档-2026-08-05.md) — L4 冷构建恢复 + 暂停 check + 8.3.3 R2 field/soa + pure-asm match + B′ pure leave 至 binop／vector_simd（**74** 行 · 钉盘仍 `db809e00f`）。
- **8月4日归档**：[自举进度-归档-2026-08-04.md](自举进度-归档-2026-08-04.md) — wave1241–1291（ast_pool 域分解 + glue 壳 thin + check 收口 + 升钉 `db809e00f` · 58 行）。
- **8月3日归档**：[自举进度-归档-2026-08-03.md](自举进度-归档-2026-08-03.md) — wave1227–1240。
- **8月2日归档**：[自举进度-归档-2026-08-02.md](自举进度-归档-2026-08-02.md) — wave1130–1220。
- **8月1日归档**：[自举进度-归档-2026-08-01.md](自举进度-归档-2026-08-01.md) — wave964–1129。
- **31 日归档**：[自举进度-归档-2026-07-31.md](自举进度-归档-2026-07-31.md) — wave895–963。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `db809e00f`**；tip L4 安全网 **`ffc0b646f`**（不升钉）；residual tip **wave248 overload pure leave**（日常 L2，非 tip L4）。
- 归楼后主文档只保留 KPI + 前排 + **8 月 7 日** §6（6 日见归档）；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
|------|----------|
| **macOS arm64** | 钉盘 build ✅ · tip L4+bstrict 129 ✅ @ `ffc0b646f` · tip **wave248** overload pure leave **mac pure-ld OK**（typeck_x hand-sync + heat pipeline_x）· 矩阵绿 · 产品只走 `./xbuild` · check 闸门暂停 · tip `xlang` mega `-E` 仍 SEGV（prefer 用 `xlang-x`） |
| **Ubuntu x86_64（金标）** | 钉盘 L4 真冷 ✅ @ `db809e00f` · tip 安全网 L4+bstrict 129 ✅ @ `ffc0b646f` · L4 冷构建已恢复 · tip **`00db73543`** `ubuntu-remote-server` L2 绿（prefer 用 `xlang-x` 恢复 pure hybrid · scp typeck_gen + typeck_x + heat pipeline_x + pure-ld · rv42／opt102／hello0／asg99／fif205／sub198／cont6／si／wa0／soa8 · allocator／region escape 拒 · nm T with_arena_scope_* in typeck_x · U from pipeline_x）· 升钉前 tip L4+bstrict 复证 |
| **Windows MSYS2** | wave940 一键冷启动绿 · STATUS=`reproven_green` · Makefile 已物理删除 |

***

## 6. 变更记录（倒序 · 8 月 7 日；6 日见 [归档](自举进度-归档-2026-08-06.md)）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-08-07 | **feat(8.3.1)** typeck overload pick／resolve pure leave (wave248) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu 金标待补）。B′ G.7：method_call 子域 overload — `pipeline_typeck_pick_overload_func_index_for_call_c`／`pipeline_typeck_resolve_call_func_index_for_emit_c` → typeck.x（#[no_mangle] thin→pure pick／resolve；score＝`typeck_overload_arg_param_score` via by_name_overload；typeck_gen hand-sync）；Cap residual 删第二 score 集群（count／assignable／match／expect／pick／resolve static）；method_call ~3209→2812；present 仍 **56**。mac：migrate typeck_x + heat pipeline_x + pure-ld；rv42／opt102／si0／hello0／asg99／sub99／import／overload；nm T for_emit／pick on typeck_x only · pipeline_x U pick。下刀：method_call 残（generic UFCS／fixup／method_call_c）；elf／ast_pool；pipeline_x mega；Ubuntu 补 wave241–248。PLATFORM: SHARED freestanding typeck CALL overload resolve. |
| 2026-08-07 | **feat(8.3.1)** typeck call-resolve pure leave (wave247) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu 金标待补）。B′ G.7：method_call 子域 call-resolve — `pipeline_typeck_resolve_call_callee_return_type_c` → typeck.x（#[no_mangle] thin→`resolve_call_callee_return_type`；check_expr_call 直调 pure；typeck_gen＋lang007 patch 同步）；import faces thin→typeck_*；删 4 static equal helpers；method_call ~3778→3209；present 仍 **56**。mac：migrate typeck_x + heat pipeline_x + pure-ld；rv42／opt102／si0／hello0／asg52／soa8／import／overload；nm T resolve_callee on typeck_x only · pipeline thin T import faces U pure。下刀：method_call 残（generic UFCS／overload／fixup）；elf／ast_pool；pipeline_x mega；Ubuntu 补 wave241–247。PLATFORM: SHARED freestanding typeck CALL target resolve. |
| 2026-08-07 | **feat(8.3.1)** typeck return_slice_in_scope pure leave (wave246) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu 金标待补）。B′ G.7：M-3 `pipeline_typeck_check_return_slice_region_in_scope_c` → typeck.x（#[no_mangle]；typeck_gen hand-sync；`typeck_expr_diag_line_col`＋built msg 无 printf）；Cap residual 删第二 body＋static expr_diag_line_col（dual-export ban）；region_assign ~286→240 thin-only；present 仍 **56**。mac：migrate typeck_x + heat pipeline_x + pure-ld；rv42／opt102／si0／hello0／asg52／lit99／soa8；region_return／assign／scope／allocator escape 拒；nm T return_slice_in_scope on typeck_x only · pipeline_x U。下刀：method_call；elf／ast_pool；pipeline_x mega；Ubuntu 补 wave241–246。PLATFORM: SHARED freestanding typeck region escape on return. |
| 2026-08-07 | **feat(8.3.1)** typeck ptr_for_addr＋stack_local pure leave (wave245) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu 金标待补）。B′ G.7：WPO-S3 `pipeline_typeck_ptr_for_addr_of_operand_c` → typeck.x（#[no_mangle]；typeck_gen hand-sync）；type-pool `pipeline_type_find_or_alloc_ptr`（ast_pool_type · 有则补全）＋`set_region_label_at` 扩 TYPE_PTR；Cap residual 删 stack_local helpers＋dead store-scan＋第二 body（dual-export ban）；region_assign ~740→286；present 仍 **56**。mac：migrate typeck_x + heat pipeline_x + pure-ld；rv42／opt102／si0／hello0／asg52／lit99／fif205／sub198／cont6／soa8；scope／allocator／region escape 拒；nm T ptr_for_addr on typeck_x only · T find_or_alloc_ptr on pipeline_x。下刀：method_call／return_slice_in_scope；elf／ast_pool；pipeline_x mega；Ubuntu 补 wave241–245。PLATFORM: SHARED freestanding typeck stack-local stamp. |
| 2026-08-07 | **feat(8.3.1)** typeck one_region＋call_struct pure leave (wave244) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu 金标待补）。B′ G.7：M-3 `pipeline_typeck_check_block_one_region_c`＋WPO-S3 `pipeline_typeck_check_call_struct_stack_escape_c` → typeck.x（#[no_mangle]；typeck_gen hand-sync）；pure `typeck_var_is_block_local` 补全 block-tree walk（residual fidelity）；Cap residual 删第二 body（dual-export ban）；region_assign ~901→740；present 仍 **56**。mac：migrate typeck_x + heat pipeline_x + pure-ld；rv42／opt102／si0／hello0／lit99／fif205／sub198／cont6／soa8；scope／allocator／region escape 拒；nm T one_region／call_struct on typeck_x only · pipeline_x U call_struct · T ptr_for_addr residual。下刀：stack_local／ptr_for_addr／method_call；elf／ast_pool；pipeline_x mega；Ubuntu 补 wave241–244。PLATFORM: SHARED freestanding typeck region／CALL escape. |
| 2026-08-07 | **feat(8.3.1)** typeck stamp_let＋read_ptr pure leave (wave243) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu 金标待补）。B′ G.7：M-5 `pipeline_typeck_is_read_ptr_slice_callee_c`／`pipeline_typeck_read_ptr_slice_return_ref_c`＋M-3 `pipeline_type_stamp_block_let_region_c` → typeck.x（#[no_mangle]；typeck_gen hand-sync；residual fidelity name_len 14／19／18／16）；Cap residual 删第二 body（dual-export ban）；region_assign ~977→901；present 仍 **56**。mac：migrate typeck_x + heat pipeline_x + pure-ld；rv42／opt102／si0／hello0／asg99；dual_escape／allocator assign＋return escape 拒；nm T stamp／read_ptr on typeck_x only。下刀：call_struct／stack_local／one_region／method_call；elf／ast_pool；pipeline_x mega；Ubuntu 补 wave241–243。PLATFORM: SHARED freestanding typeck stamp／read_ptr. |
| 2026-08-07 | **feat(8.3.1)** typeck post-scan tree pure leave (wave242) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu 金标待补）。B′ G.7：WPO-S3 `typeck_scan_expr_stack_escape_c`／`typeck_scan_block_stack_escape_c`／`pipeline_typeck_scan_module_struct_stack_escape_c` → typeck.x（#[no_mangle]；typeck_gen hand-sync）；Cap residual 删 static scan 体；`pipeline_typeck_check_call_struct_stack_escape_c` 升 public residual（pure scan＋check_expr）；region_assign ~1257→977；present 仍 **56**。产品 **`1a696f3ac`**。mac：migrate typeck_x + heat pipeline_x + pure-ld；rv42／opt102／si0／hello0／asg lit99／subadd3 99／cont6／soa8／wa0；dual_escape／allocator escape 拒。下刀：stamp_let／read_ptr／call_struct／method_call；elf／ast_pool；pipeline_x mega；Ubuntu 补 wave241–242。PLATFORM: SHARED freestanding typeck post-scan. |
| 2026-08-07 | **feat(8.3.1)** typeck region-label scope BSS pure leave (wave241) | **代码** · **不升钉** · **mac L2 绿**（Ubuntu SSH kex 暂断 · 金标待补）。B′ G.7：M-3 region-label nest BSS＋push／pop／len_at／reset → typeck.x（#[no_mangle]；typeck_gen hand-sync；flattened u8[1024]）；Cap residual region_assign 仅 extern 调 pure faces；scan tree／stamp_let／read_ptr 仍 residual；region_assign ~1324→1257；present 仍 **56**。产品 **`48517a824`**。mac：`xlang-x` prefer hybrid 恢复 `runtime_pipeline_abi.o` + typeck_x + heat pipeline_x + pure-ld；rv42／opt0／si0／hello0／asg99／fif205／sub198／cont6／soa8／wa0；region_block／return escape 拒 exit1。下刀：scan tree／method_call；elf／ast_pool；pipeline_x mega；Ubuntu 补 wave241。PLATFORM: SHARED freestanding typeck region cells. |
| 2026-08-07 | **feat(8.3.1)** typeck with_arena nest BSS pure leave (wave240) | **代码** · **不升钉** · **Ubuntu L2 绿**（mac pure-ld 当时受阻）。B′ G.7：MEM-C1 with_arena nest BSS＋push／pop／reset／n_at／current_body_ref → typeck.x；region_assign ~1347→1324；present **56**。产品 **`00db73543`**。下刀：region-label／scan tree／method_call。PLATFORM: SHARED freestanding typeck nest cells. |
| 2026-08-07 | **feat(8.3.1)** typeck call_slice region pure leave (wave239) | **代码** · **不升钉** · **Ubuntu L2 绿**（mac pure-ld 仍受阻）。B′ G.7：`typeck_check_call_slice_region` pure body（post-resolve slice region／array_lit stamp／MOD-02 *Struct compat／WPO-S3 &local+outer *Struct）；private `typeck_check_call_ptr_struct_compat`；Cap residual call_slice thin；retire method_call dual static；region_assign ~1470→1347；present 仍 **56**。产品 **`75bd48b38`**。Ubuntu `ubuntu-remote-server`：scp typeck_gen + cc typeck_x + g05 pure-ld；rv42／opt102／hello0／asg99／fif205／sub198／cont6／si／wa0／soa；region_call／assign escape 拒；nm T typeck_check_call_slice_region + thin T *_c + U pure。下刀：with_arena BSS／scan tree／method_call；elf／ast_pool；pipeline_x mega；mac pure.o。PLATFORM: SHARED freestanding typeck. |
| 2026-08-07 | **feat(8.3.1)** typeck CTFE fold pure leave (wave238) | **代码** · **不升钉** · **Ubuntu L2 绿**（mac pure-ld 仍受阻）。B′ G.7：`typeck_fold_expr`／`typeck_fold_block_const_init`／`typeck_fold_expr_in_block`／`typeck_block_const_init_is_const`／`typeck_const_init_not_constant`／`typeck_expr_is_c_static_const_init` pure body（lit／binop／unary／var／array／struct／AS／enum／CALL WPO／MATCH／ternary／if／block；P0-4 i32 fit）；Cap residual ctfe thin；`pipeline_expr_set_const_folded` pool face 补全；ctfe ~1177→51；present 仍 **56**。产品 **`c5fe7e46c`**。Ubuntu `ubuntu-remote-server`：scp typeck_gen + cc typeck_x + heat pipeline_x + pure-ld；rv42／opt102／hello0／asg99／fif205／soa8／sub198／cont6／si／wa0／ctfe5；const 非常量 init 拒。下刀：with_arena BSS／call_slice／method_call；elf／ast_pool；pipeline_x mega；mac pure.o。PLATFORM: SHARED freestanding typeck. |
| 2026-08-07 | **feat(8.3.1)** typeck allocator region assign＋return pure leave (wave237) | **代码** · **不升钉** · **Ubuntu L2 绿**（mac pure-ld 仍受阻）。B′ G.7：`typeck_check_allocator_region_assign`／`typeck_check_allocator_region_return` pure body（with_arena nest／lval outer／Allocator bare＋`heap.Allocator` 后缀）；export residual with_arena BSS faces `scope_n_at`／`current_body_ref`；Cap residual region_assign thin；KEEP with_arena push／pop／scan tree／call_slice residual；region_assign ~1535→1470；present 仍 **56**。产品 **`4042e87d8`**＋fix **`4d6eac944`**。Ubuntu `ubuntu-remote-server`：scp typeck_gen + migrate typeck_x + heat pipeline_x + pure-ld；rv42／opt102／hello0／asg99／lit99／fif205／soa8／sub198／cont6／si／wa0；allocator assign＋return 负例 product 拒。下刀：ctfe／with_arena BSS／call_slice／method_call；elf／ast_pool；pipeline_x mega；mac pure.o。PLATFORM: SHARED freestanding typeck. |
| 2026-08-07 | **feat(8.3.1)** typeck stack-escape＋scope-borrow pure leave (wave236) | **代码** · **不升钉** · **Ubuntu L2 绿**（mac pure-ld 仍受阻）。B′ G.7：`typeck_check_struct_stack_escape_assign`／`typeck_check_scope_borrow_assign`／`typeck_check_scope_borrow_return` pure body（stack_local／block-local／param-ptr-field／ancestor／lval-root）；`pipeline_block_parent_block_ref_at` block-pool face；Cap residual region_assign thin；KEEP with_arena／allocator／scan tree／call_slice residual；region_assign ~1595→1535；present 仍 **56**。产品 **`578978083`**。Ubuntu `ubuntu-remote-server`：scp typeck_gen + migrate typeck_x + heat pipeline_x + pure-ld；rv42／opt0／hello0／asg99／lit99／fif205／soa8／sub198／cont6／si；scope borrow 负例 product 拒；region escape 拒。下刀：ctfe／with_arena／allocator／call_slice／method_call；elf／ast_pool；pipeline_x mega；mac pure.o。PLATFORM: SHARED freestanding typeck. |
| 2026-08-07 | **feat(8.3.1)** typeck M-3 slice region assign＋return pure leave (wave235) | **代码** · **不升钉** · **Ubuntu L2 绿**（mac pure-ld 仍受阻）。B′ G.7：`typeck_check_slice_region_assign`／`typeck_check_return_slice_region` pure body（escape／conflict／diag line-col）；Cap residual region_assign thin；KEEP scope return／stack-escape／with_arena／call_slice mega residual；region_assign ~1700→1595；present 仍 **56**。产品 **`beaf19dda`**。Ubuntu `ubuntu-remote-server`：scp typeck_gen + migrate typeck_x + heat pipeline_x + pure-ld；rv42／opt102／hello0／asg99／lit99／soa8／si；region 负例 product 拒（escape／mismatch／return／call）；正例 link BLD 预存。下刀：ctfe／region 残／method_call；elf／ast_pool；pipeline_x mega；mac pure.o。PLATFORM: SHARED freestanding typeck. |
| 2026-08-07 | **feat(8.3.1)** typeck field_type＋call_arg_repr＋extern_unsafe pure leave (wave234) | **代码** · **不升钉** · **Ubuntu L2 绿**（mac pure-ld 本波受阻 · pure.o 恢复债）。B′ G.7：`typeck_match_subject_field_type`／`typeck_call_arg_repr_compatible_ok`／`typeck_check_extern_call_unsafe_boundary` pure body；Cap residual check_expr thin；match subject BSS 仍 residual（pure mega -E SEGV 延后）；LANG007 call patch 走 typeck S0；check_expr ~705→547；present 仍 **56**。产品 **`97341a69a`**。Ubuntu：scp typeck_gen + migrate typeck_x + heat pipeline_x + pure-ld；rv42／opt102／hello0／plus_lit99／struct_field99／sub198／cont6／soa8／fif205／si／match_destructure0（nm U typeck_* · thin T *_c）。下刀：ctfe／region／method_call；elf／ast_pool；pipeline_x mega；mac 恢复 pure.o。PLATFORM: SHARED freestanding typeck. |
| 2026-08-07 | **feat(8.3.1)** typeck int_binop＋resolve_alias＋int_lit pure leave (wave233) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：`typeck_coerce_init_int_binop_to_decl`／`typeck_resolve_type_alias_ref`／`typeck_check_expr_int_lit` 补 pure body；`type_refs_equal` peel 走 typeck alias；Cap residual int_binop／resolve_alias／int_lit thin→typeck；删 residual alias_impl；coerce ~578→428；present 仍 **56**。产品 **`c46dfddb1`**。mac：hand-sync typeck_gen + migrate typeck_x + heat pipeline_x + pure-ld；rv42／opt／hello0／asg99／lit99／fif205／soa8／sub198／cont6／si。Ubuntu `ubuntu-remote-server`：scp typeck_gen + 同路径同矩阵（nm U typeck_* · thin T *_c）。下刀：ctfe／region／method_call；elf／ast_pool；pipeline_x mega。PLATFORM: SHARED freestanding typeck. |
| 2026-08-07 | **feat(8.3.1)** typeck call generic fixup pure leave (wave232) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：`typeck_check_expr_call` 补 generic type-args gate＋ret resolve＋mono fixup（parity 旧 call_c）；导出 `glue_generic_call_fixup_resolved_type_c`／`pipeline_typeck_check_call_generic_type_args_c` 供 typeck_x 链；Cap residual call_c thin→typeck；dual-export ban；LANG-007 patch 装 pure-leave body（禁 call_c wrap 环）；check_expr ~792→705；present 仍 **56**。产品 **`a39ce4af5`**。mac：patch typeck_gen + migrate typeck_x + heat pipeline_x + pure-ld；rv42／opt／hello0／asg99／lit99／fif205／soa8／sub198／cont6／si。Ubuntu `ubuntu-remote-server`：同路径同矩阵。下刀：ctfe／region／method_call；coerce int_binop／resolve_alias；elf／ast_pool；pipeline_x mega。PLATFORM: SHARED freestanding typeck. |
| 2026-08-07 | **feat(8.3.1)** typeck match＋try_propagate＋struct_lit pure leave (wave231) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：`typeck_check_expr_match` 补 subject BSS set／restore＋迭代 arm；`typeck_check_expr_try_propagate` Result_? payload 权威；`typeck_coerce_init_struct_lit_to_decl` 匿名 lit 回填；Cap residual match／try／struct_lit thin→typeck；KEEP call_c generic fixup＋subject field_type BSS；check_expr ~902→792 · coerce ~612→578；present 仍 **56**。产品 **`54499d041`**。mac：typeck_gen hand-sync（-E SEGV）+ migrate typeck_x + heat pipeline_x + pure-ld；rv42／opt／hello0／asg99／lit99／fif205／soa8／sub99／cont6／si／msd0。Ubuntu：scp typeck_gen 对齐 + heat + pure-ld · 同矩阵。下刀：ctfe／region／method_call；call generic fixup；int_binop／resolve_alias；elf／ast_pool；pipeline_x mega。PLATFORM: SHARED freestanding typeck. |
| 2026-08-07 | **feat(8.3.1)** typeck type_refs＋integer_widen pure leave (wave230) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：typeck.x `type_refs_equal` 破 residual wrap 环（alias peel＋impl）；Cap residual `pipeline_typeck_type_refs_*`／`integer_widen`／`type_ref_is_bool`／`expr_type_ref` thin→typeck twins；KEEP resolve_alias／int_binop／struct_lit／int_lit／float_bits；coerce ~916→612；present 仍 **56**。产品 **`6e66ad3ea`**。mac：migrate typeck_x + heat pipeline_x + pure-ld；rv42／opt102／hello0／asg99／lit99／fif205／soa8／sub198／cont6／rsf0／si。Ubuntu：typeck_gen 对齐 cycle 修 + heat + pure-ld · 同矩阵（nm U typeck_type_refs_equal／typeck_integer_widen*）。下刀：ctfe／region／method_call；match／call／try；coerce int_binop／struct_lit；elf／ast_pool；pipeline_x mega。PLATFORM: SHARED freestanding typeck. |
| 2026-08-07 | **feat(8.3.1)** typeck return＋ret_coerce／float_widen pure leave (wave229) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：check_expr `return_c` 双权威退役→`typeck_check_expr_return` thin；coerce `float_widen_ok_c`／`return_operand_matches_c`／`ret_coerce_integral_*_c`→typeck twins；KEEP match／call／try＋int_binop／struct_lit／type_refs／integer_widen residual；check_expr ~1027→902 · coerce ~1019→916；present 仍 **56**。产品 **`9fa488518`**。mac：heat pipeline_x + pure-ld；rv42／opt102／hello0／asg99／lit99／fif205／soa8／sub198／cont6／rsf0。Ubuntu `ubuntu-remote-server`：heat + pure-ld · 同矩阵（nm U typeck_check_expr_return／typeck_float_widen_ok／typeck_ret_coerce* · thin T _c）。下刀：ctfe／region／method_call；match／call／try；coerce type_refs／int_binop；elf／ast_pool；pipeline_x mega。PLATFORM: SHARED freestanding typeck. |
| 2026-08-07 | **feat(8.3.1)** typeck orch＋check_expr domain pure leave (wave228) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：orch `pipeline_typeck_x_ast_*_c` 双权威退役→typeck_x_ast* thin；check_expr panic／unary／addr_of／index／deref／var thin→typeck twins；residual mega deref／var→typeck；删 dual-export typeck_check_expr_deref；KEEP match／return／call／try＋mega region；orch ~444→242 · check_expr ~1305→1027；present 仍 **56**。产品 **`69e737c60`**。mac：heat pipeline_x + pure-ld；rv42／opt102／hello0／asg99／lit99／fif205／soa8／sub198／cont6。Ubuntu `ubuntu-remote-server`：heat + pure-ld · 同矩阵（nm U typeck_x_ast*／typeck_check_expr_* · thin T _c）。下刀：ctfe／region／method_call；check_expr match／return／call／try；elf／ast_pool；pipeline_x mega。PLATFORM: SHARED freestanding typeck. |
| 2026-08-07 | **feat(8.3.1)** typeck_coerce_init domain pure leave (wave227) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：coerce-init 双权威 body 退役（lit／float／enum／named_call／array_vector／vector_binop／slice／expr_to_decl）；live＝typeck.x；residual 保留 int_binop_c（typeck thin-wrap 防环）＋struct_lit_c（无 twin）＋type_refs／widen／ret_coerce 集群；dual-export ban；inventory present 仍 **56**（min_loc 300→200 · ~1236→1019）。产品 **`b21779e69`**。mac：heat pipeline_x + pure-ld；rv42／opt102／hello0／asg99／lit99／fif205／soa8／sub198／cont6。Ubuntu `ubuntu-remote-server`：heat + pure-ld · 同矩阵（nm U typeck_coerce_init_* · thin T _c）。下刀：ctfe／region／method_call；coerce int_binop／struct_lit 残；elf／ast_pool；pipeline_x mega。PLATFORM: SHARED freestanding typeck. |
| 2026-08-07 | **feat(8.3.1)** typeck_check_block domain pure leave (wave226) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：check_block walker 双权威退役（impl／as_loop_body／tail）；live＝typeck.x（含 region stmt_order + parent_block_ref）；residual thin～484（ctx／depth sidecar + linear + has_implicit + thin faces）；dual-export ban；inventory present 仍 **56**（min_loc 250→180）。产品 **`f493f3434`**。mac：heat pipeline_x + pure-ld；rv42／opt102／hello0／asg99／lit99／fif205／soa8／sub198／cont6。Ubuntu `ubuntu-remote-server`：heat + pure-ld · 同矩阵（nm U typeck_check_block_impl · thin T impl_c）。下刀：coerce／ctfe／region／method_call；check_block linear／has_implicit 残；elf／ast_pool；pipeline_x mega。PLATFORM: SHARED freestanding typeck. |
| 2026-08-07 | **feat(8.3.1)** typeck_assign domain pure leave (wave225) | **代码** · **不升钉** · **双端 L2 绿**。B′ G.7：assign+diag-fmt Cap residual body 退役；live＝typeck.x（`typeck_check_expr_assign`／`typeck_diag_*`）；residual thin～108（C ABI face + active_module_set + module_num_imports）；dual-export ban；inventory present 仍 **56**（min_loc 250→40）。产品 **`3464ab3b4`**。mac：heat pipeline_x + pure-ld；rv42／opt102／hello0／asg99／lit99／fif205／soa8／sub198／cont6。Ubuntu `ubuntu-remote-server`：heat pipeline_x + pure-ld · 同矩阵（nm pipeline_x U typeck_* · thin T assign_c）。下刀：check_block／coerce／ctfe／region／method_call 域 pure leave；elf／ast_pool；pipeline_x mega。PLATFORM: SHARED freestanding typeck. |
| 2026-08-07 | **feat(8.3.1)** typeck_active module BSS pure leave (wave224) | **代码** · **不升钉** · **双端 L2 绿** · **residual BSS 时代收口**。B′ pure G.7：`pipeline_typeck_active_module_c`＋`pipeline_typeck_active_module_set_c`→`runtime_pipeline_abi` pure BSS；Cap residual check_block／assign 仅 pure setter；ctfe／coerce 仅 pure getter；删 glue_statics `g_typeck_active_module`（statics **0 residual BSS cell**）；`g_typeck_active_ctx` 仍 check_block-local；seed cold twin under `#ifndef FROM_X`；dual-export ban。inventory present 仍 **56**。产品 **`0e48f6630`**。mac：prefer + heat + pure-ld；rv42／opt102／hello0／asg99／fif205／soa8／sub198／cont6。Ubuntu `ubuntu-remote-server`：同矩阵（nm pure W typeck_active · pipeline_x U）。**效率转向**：停 micro-cell；下刀 typeck／elf／ast_pool **域 pure leave**。PLATFORM: SHARED freestanding typeck. |
| 2026-08-07 | **feat(8.3.1)** emit_ctx sret BSS pure leave (wave223) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`pipeline_asm_emit_ctx_sret_{active,home_off,ret_sz}_{get,set}`→`runtime_pipeline_abi` pure BSS（#[no_mangle]）；Cap residual mega_body 仅 pure setter 写；删 residual sret static／get；typeck_active 仍 residual（check_block／assign／ctfe／coerce 直写）；seed cold twins under `#ifndef FROM_X`；dual-export ban。inventory present 仍 **56**。产品 **`6dde4f611`**。mac：prefer T pipeline_abi + heat pipeline_x + pure-ld；rv42／opt102／hello0／asg99／fif205／soa8／sub198／cont6。Ubuntu `ubuntu-remote-server`：prefer + heat + pure-ld · 同矩阵（nm pure W sret faces · pipeline_x U setters）。下刀：typeck_active pure leave；typeck／elf／ast_pool residual；pipeline_x mega 离 host-cc。PLATFORM: SHARED freestanding sret cells. |
| 2026-08-07 | **feat(8.3.1)** emit_ctx module／dep_pipe BSS pure leave (wave222) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`pipeline_asm_emit_ctx_module_{get,set}`＋`dep_pipe_{get,set}`→`runtime_pipeline_abi` pure BSS（#[no_mangle]）；Cap residual `bind_module_dep_from_ctx`＋`ast_pool_struct_layout` type_ref_byte_size→pure faces；删 residual module／dep static；sret／typeck_active 仍 residual（mega／typeck 直写）；seed cold twins under `#ifndef FROM_X`；dual-export ban。inventory present 仍 **56**。产品 **`5119edba5`**。mac：prefer T pipeline_abi + heat pipeline_x + pure-ld；rv42／opt／si／hello0／lit99／asg99／fif205／soa8／mca64／subadd3=198／cont=6。Ubuntu `ubuntu-remote-server`：prefer + heat + pure-ld · 同矩阵。下刀：sret／typeck_active pure leave；typeck／elf／ast_pool residual；pipeline_x mega 离 host-cc。PLATFORM: SHARED freestanding emit module／dep cells. |
| 2026-08-07 | **docs** · 日切归档 8 月 6 日 | **文档 only** · **不升钉**。6 号全日 §6（B′ pure leave field_access→wave199 · **50** 行）整表迁入 [`自举进度-归档-2026-08-06.md`](自举进度-归档-2026-08-06.md)；本文从 7 日起重新起表。钉盘仍 **`db809e00f`** · tip residual **wave221** @ **`70536e195`** · present **56** · 下刀 residual module／dep／sret／typeck_active pure leave 或 typeck／elf／ast_pool residual 或 pipeline_x mega。 |
| 2026-08-07 | **feat(8.3.1)** emit_ctx accessor-only BSS pure leave (wave221) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`pipeline_asm_emit_ctx_{func_index,arena,call_param_ty,call_arg_depth,elf_ctx,scope_block}_{get,set}`＋`pipeline_asm_host_is_arm64_c`→`runtime_pipeline_abi` pure BSS（#[no_mangle]；cfg target_arch）；Cap residual statics 保留 module／dep_pipe／sret／typeck_active 直写；`glue_asm_ctx_set_scope_block`→pure scope set＋asm_ctx face；seed cold twins under `#ifndef FROM_X`；dual-export ban。inventory present 仍 **56**。产品 **`70536e195`**。mac：prefer T pipeline_abi + heat pipeline_x + pure-ld；rv42／opt102／hello0／lit99／asg99／fif205／soa8／mca64／subadd3=198／cont=6。Ubuntu `ubuntu-remote-server`：prefer + heat + pure-ld · 同矩阵。下刀：residual module／dep／sret／typeck_active pure leave；typeck／elf／ast_pool residual；pipeline_x mega 离 host-cc。PLATFORM: SHARED freestanding emit context. |
| 2026-08-07 | **feat(8.3.1)** if_expr_arm_emit_depth BSS pure leave (wave220) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`glue_if_expr_arm_emit_depth_get/set`＋`g_if_expr_arm_emit_depth` BSS→`runtime_pipeline_abi` pure（#[no_mangle]）；Cap residual emit_fwd body→extern；seed cold twin under `#ifndef FROM_X`；dual-export ban。inventory present 仍 **56**。产品 **`3eeb07bb0`**。mac：prefer T pipeline_abi + heat pipeline_x + pure-ld；rv42／opt102／hello0／lit99／asg99／fif205／soa8／mca64／subadd3=198／cont=6。Ubuntu `ubuntu-remote-server`：prefer + heat + pure-ld · 同矩阵。下刀：typeck／elf／ast_pool residual 或 pipeline_x mega 离 host-cc。PLATFORM: SHARED freestanding if/ternary arm nest depth. |
| 2026-08-07 | **feat(8.3.1)** al_nc_seq COMMON label seq pure leave (wave219) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`glue_pipeline_asm_al_nc_seq_take_c`＋`g_pipeline_asm_al_nc_seq` BSS→`runtime_pipeline_abi` pure（#[no_mangle]）；Cap residual glue body→extern；seed cold twin under `#ifndef FROM_X`；dual-export ban。inventory present 仍 **56**。产品 **`0da82a797`**。mac：prefer T pipeline_abi + heat pipeline_x + pure-ld；rv42／opt102／hello0／lit99／asg99／fif205／soa8／mca64／subadd3=198／cont=6。Ubuntu `ubuntu-remote-server`：prefer + heat + pure-ld · 同矩阵。下刀：typeck／elf／ast_pool residual 或 pipeline_x mega 离 host-cc。PLATFORM: SHARED freestanding emit label uniqueness. |
| 2026-08-07 | **feat(8.3.1)** Cap residual emit shells host-cc leave (wave218) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：删 prototype-only `pipeline_asm_emit_call_args.c`／`spill.c`／`index_helpers.c`（wave190–217 后 0 函数体）；early residual fwd 迁 `pipeline_glue_backend_fwd`；`PIPELINE_X_DEPS`／inventory／g05 STALE／leaf residual 同波 absent。inventory present **59→56**。产品 **`b0eb6991e`**。mac：heat pipeline_x + pure-ld；rv42／opt102／si／hello + lit99／asg99／fif205／soa8／mca64／subadd3=198／cont=6。Ubuntu `ubuntu-remote-server`：heat U + pure-ld · 同矩阵。下刀：typeck／elf／ast_pool residual 或 pipeline_x mega 离 host-cc。PLATFORM: SHARED freestanding Cap residual emit leave. |
| 2026-08-07 | **feat(8.3.1)** CALL／METHOD text thin wrappers pure leave (wave217) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`pipeline_asm_emit_expr_call_c`＋`pipeline_asm_emit_expr_method_call_c`（pool snapshot 712B Expr＋seed partial）；Cap residual call_args shell～623（prototype only）；seed cold twins −1 under `#ifndef FROM_X`；dual-export ban。产品 **`2218c25a7`**。mac：prefer T call／method + heat U + pure-ld；rv42／opt102／si／hello + lit99／asg99／fif205／soa8／mca64／subadd3=198／cont=6。Ubuntu `ubuntu-remote-server`：prefer W + heat U + pure-ld · 同矩阵。下刀：pipeline_x mega。PLATFORM: SHARED freestanding text M8-tail. |
| 2026-08-07 | **feat(8.3.1)** for_call_args mega pure leave (wave216) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`pipeline_asm_emit_expr_elf_for_call_args` 整 body→`runtime_pipeline_abi.x`（f32 lit／VAR array→slice fat／dual-GP／fixed array／STRUCT_LIT／INDEX／FIELD／rec）；Cap residual call_args shell～639（prototype＋CALL／METHOD thin wrappers）；seed cold twin −1 under `#ifndef FROM_X`；dual-export ban；T001 bare C extern→`unsafe` 使 mega typeck 绿。产品 **`8cac954d0`**。mac：prefer T for_call_args + heat U + pure-ld；rv42／opt102／si／hello + lit99／asg99／fif205／soa8／mca64／subadd3=198／cont=6。Ubuntu `ubuntu-remote-server`：prefer W + heat U + pure-ld · 同矩阵。下刀：CALL thin wrappers／pipeline_x mega。PLATFORM: SHARED freestanding CALL-arg packing · LINUX+MACOS SysV · MACOS\|ARM64. |
| 2026-08-07 | **feat(8.3.1)** live set arrays pure leave re-land (wave215) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7 重上 wave214（dual-GP packing 根修 `c5e0051fb` 后）：GlueBlockLiveFwd BSS（block_live_fwd／live_at_stmt[32]／snap_before_if／sub_exit／peak_live）＋loop break／continue stacks＋opaque u8 overlay thins＋fill_live_end／set_from_expr_uses／peak_clear。Cap residual spill→shell（cache_invalidate_at_cfg_merge only · ~434）。seed cold twins under `#ifndef FROM_X`；dual-export ban。产品 **`f908695e2`**。mac：prefer T live-set + heat U + pure-ld；rv42／opt102／hello + asg99／lit99／fif205／soa8／subadd3=198／cont=6。Ubuntu `ubuntu-remote-server`：同路径同矩阵 asg99。下刀：for_call_args mega／CALL thin wrappers／pipeline_x mega。PLATFORM: SHARED freestanding 7.3. |
| 2026-08-07 | **fix(8.3.1)** pure call_dispatch dual-GP spill-then-load (asg packing) | **代码** · **不升钉** · **双端 L2 绿**。wave214 live-set pure leave 后 Ubuntu `assign_index_struct_field` 134；revert 后 tip 仍红（核重建 asg4:134）。根因：pure `backend_call_dispatch.x` 用 **arg 下标当 GP 下标**，`set_in(WithArr,i,v)` 把 v 放 rdx 再 dual-load struct 冲掉 → i=15 bounds panic。G.7 有则补全 pure：gp_units／MEMORY words／spill rax+rdx→frame／load 到连续 GP；`arg_slot`+`n_stack` 双 GP；f32-xmm 同 spill-then-load。prefer 路径 thin→seed `_impl` 亦绿。产品 **`c5e0051fb`**。mac asg99／rv42／lit15；Ubuntu asg99／rv42／opt102／hello0／lit15／fif=205／soa=8／subadd3=198／cont=6。wave214 live-set 已 revert 不升钉。下刀：spill live set arrays re-land／for_call_args mega／pipeline_x mega。PLATFORM: SHARED · LINUX+MACOS x86_64 SysV. |
| 2026-08-07 | **feat(8.3.1)** live control＋interf BSS pure leave (wave213) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`glue_block_live_cfg_parent_*`／`active_*`／`emit_stmt_i_*`／`linear_max_live_n_*`／`pressure_live_thresh_get`／`interf_clear`／`n_get`／`off_at`／`has_edge`／`add_live_set_u8`／`add_live_at_stmt`／`push`／`pop_merge`／`cfg_interf_prepare`／`linear_ctx_bind`＋getters + pure BSS；Cap residual body→prototype；peak_clear residual thin；seed cold twins under `#ifndef FROM_X`；dual-export ban；`stack_spill_enabled` 读 pure parent＋max。产品只走 `./xbuild`。mac：prefer T interf／control + heat U + pure-ld；矩阵 rv42／opt102／hello + lit99／asg99 · fif=205 · soa=8 · mca=64 · f32soa=10 · subadd3=198 · f14=105 · cont=6。Ubuntu：prefer W + heat U + pure-ld · 同矩阵。spill ~1238。下刀：for_call_args mega／CALL thin wrappers／spill live set arrays／pipeline_x mega。PLATFORM: SHARED freestanding 7.3. tip product **2b749eb91**. |
| 2026-08-07 | **feat(8.3.1)** Chaitin color／pin BSS pure leave (wave212) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`glue_asm73_pin_spill_off_*`／`off_is_spill_pin`／`clear_spill_color_map`／`set_spill_color`／`off_spill_color_which`／`cfg_coloring_active` get\|set／`cfg_final_expr_use_n_set`／`stack_spill_enabled`／`var_prefers_stack_spill` + pure BSS pin[6]＋color map[16]＋cfg flags；Cap residual body→prototype；prepare 经 pure setter 清 final_expr；seed cold twins under `#ifndef FROM_X`；dual-export ban。产品只走 `./xbuild`。mac：prefer T color／pin + heat U + pure-ld；矩阵 rv42／opt102／hello + lit75／asg_lit99 · fif=205 · soa=8 · mca=64 · f32soa=10 · subadd3=198 · f14=105。Ubuntu：prefer W color／pin + heat U + pure-ld · 同矩阵。spill ~1525。下刀：for_call_args mega／CALL thin wrappers／spill live_fwd＋interf BSS／pipeline_x mega。PLATFORM: SHARED freestanding 7.3 ／ MACOS\|ARM64 AAPCS64. tip product **9843f1351**. |
| 2026-08-07 | **feat(8.3.1)** binop stack_spill try_reload enc pure leave (wave211) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`glue_binop_stack_spill_try_reload_elf_c`（find_depth wave208 + CAP depth_get wave207 + VAR stamp wave210 + `arch_arm64_enc_enc_ldr_sp_slot_to_xreg`）；Cap residual body→prototype；seed cold twin no-op under `#ifndef FROM_X`；dual-export ban。产品只走 `./xbuild`。mac：prefer T try_reload + heat U + pure-ld；矩阵 rv42／opt102／hello + lit75／asg_lit99 · fif=205 · soa=8 · mca=64 · f32soa=10 · subadd3=198。Ubuntu：prefer W try_reload + heat U + pure-ld · 同矩阵。spill ~1616。下刀：for_call_args mega／CALL thin wrappers／spill live_fwd＋color／interf BSS／pipeline_x mega。PLATFORM: SHARED freestanding 7.3 ／ MACOS\|ARM64 AAPCS64. tip product **5b779cbe7**. |
| 2026-08-07 | **feat(8.3.1)** binop VAR slot cache BSS pure leave (wave210) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`glue_binop_var_slot_cache_*`（clear／invalidate_rax\|rbx\|slot／kill_def／ctx_matches／hit_rax\|rbx／valid\|off getters／set_ctx\|rax\|rbx／set_valid\|off／set_spill_slot／x10–x15 valid\|off／set_valid_x*）+ pure flattened BSS；Cap residual 删 struct body→prototype；try_reload 改 pure setters；seed cold twins working BSS under `#ifndef FROM_X`；dual-export ban。stack_spill_drop_off pure wave208。产品只走 `./xbuild`。mac：prefer T VAR cache + heat U + cold enc pure-ld；矩阵 rv42／opt102／hello + lit=99 · fif=205 · soa=8 · mca=64 · math=0 · f32soa=10 · subadd3=198。Ubuntu：prefer W VAR cache + heat U + cold enc pure-ld · 同矩阵。spill ~1643；index_helpers ~659。下刀：for_call_args mega／CALL thin wrappers／spill live_fwd＋color／interf BSS／try_reload enc／pipeline_x mega。PLATFORM: SHARED freestanding 7.3 ／ MACOS\|ARM64 co-path. tip product **2491a79fb**. |
| 2026-08-07 | **feat(8.3.1)** CAP cache BSS pure leave (wave209) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：minus_pair／subadd3 CAP cache BSS + thin `clear`／`valid_get`／`slot_depth_get`／`ctx_matches`／`keys_eq`／`record` + `caches_hit_var`；Cap residual 删 static 表 body→prototype；seed cold twins working BSS under `#ifndef FROM_X`；dual-export ban。key hash pure wave177；CAP depth pure wave207。产品只走 `./xbuild`。mac：prefer T CAP cache + heat U + cold enc pure-ld；矩阵 rv42／opt102／hello + lit=99 · fif=205 · soa=8 · mca=64 · math=0 · f32soa=10 · subadd3=198。Ubuntu：prefer W CAP cache + heat U + cold enc pure-ld · 同矩阵。spill ~1786；index_helpers ~659。下刀：for_call_args mega／CALL thin wrappers／spill VAR＋color BSS／pipeline_x mega。PLATFORM: SHARED freestanding 7.3 ／ MACOS\|ARM64 co-path. tip product **18df7efd8**. |
| 2026-08-07 | **feat(8.3.1)** stack_spill table BSS pure leave (wave208) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`glue_binop_stack_spill_append_at_depth`／`cap_get`／`clear`／`drop_off`／`find_depth`／`n_get`／`off_at` + pure BSS `g_binop_stack_spill_off[12]`／`at_depth[12]`／`n`；Cap residual body→prototype；index_helpers 删 static 表；seed cold twins working BSS under `#ifndef FROM_X`；dual-export ban。try_reload enc 仍 residual（VAR cache＋arm64 ldr）。产品只走 `./xbuild`。mac：prefer T stack_spill + heat U + pure-ld；矩阵 rv42／opt102／hello + lit=99 · fif=205 · soa=8 · mca=64 · math=0 · f32soa=10 · subadd3=198。Ubuntu：prefer W stack_spill + heat U + pure-ld · 同矩阵。spill ~1897；index_helpers ~657。下刀：for_call_args mega／CALL thin wrappers／spill VAR＋color BSS／CAP cache structs／pipeline_x mega。PLATFORM: SHARED freestanding 7.3 ／ MACOS\|ARM64 co-path. tip product **2d9705840**. |
| 2026-08-07 | **feat(8.3.1)** CAP BSS thin accessors pure leave (wave207) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`glue_binop_rax_frame_spill_push/pop/depth/n_set` + `glue_index_scratch_stack_depth_get/set` + pure BSS `g_rax_frame_spill_off[16]`／`g_rax_frame_spill_n`／`g_index_scratch_stack_depth`；Cap residual body→prototype；spill record／try_reload 改走 getter；seed cold twins working BSS under `#ifndef FROM_X`；dual-export ban。产品只走 `./xbuild`。mac：prefer T CAP BSS + heat U + pure-ld；矩阵 rv42／opt102／hello + lit=99 · fif=205 · soa=8 · mca=64 · math=0 · f32soa=10 · subadd3=198。Ubuntu：prefer W CAP BSS + heat U + pure-ld · 同矩阵。index_helpers ~655；spill ~1969。下刀：for_call_args mega／CALL thin wrappers／spill VAR＋color BSS／CAP cache structs／pipeline_x mega。PLATFORM: SHARED freestanding 7.3 ／ MACOS\|ARM64 co-path. tip product **e839773a0**. |
| 2026-08-07 | **feat(8.3.1)** CALL expected_ret_ty BSS pure leave (wave206) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`pipeline_asm_set/call_expected_ret_ty_c` + pure BSS `g_call_expected_ret_ty`（clamp ≤0→0）；复用 wave132 sret BSS 同构；Cap residual body→prototype；seed cold twin working BSS under `#ifndef FROM_X`；dual-export ban（去 export extern）。产品只走 `./xbuild`。mac：prefer T expected_ret + heat U + pure-ld；矩阵 rv42／opt102／hello + lit=99 · fif=205 · soa=8 · mca=64 · math=0 · f32soa=10。Ubuntu：prefer W expected_ret + heat U + pure-ld · 同矩阵。call_args residual ~1176。下刀：for_call_args mega／CALL thin wrappers／spill BSS／CAP BSS／pipeline_x mega。PLATFORM: SHARED freestanding CALL expected-ret tracking. tip product **8a04644b7**. |
| 2026-08-07 | **feat(8.3.1)** TYPE_SLICE reent deep_copy pure leave (wave205) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`glue_slice_let_reent_deep_copy_after_dual_gp_elf_c`（use_frame=0 COMMON call-arg + use_frame=1 let frame；cap length + jge loop + bulk esz>8）；复用 pure index_elem／dual_gp_length／align_next／bulk_mem／lea_common + al_nc_seq_take + pipe next_offset@4；Cap residual body→prototype；seed cold twin −1；dual-export ban；store_retval same-module 直调 pure。产品只走 `./xbuild`。mac：prefer T deep_copy + heat U + pure-ld；矩阵 rv42／opt102／si／hello + lit=99 · fif=205 · soa=8 · mca=64 · math=0 · f32soa=10。Ubuntu：prefer W deep_copy + heat U + pure-ld · 同矩阵。call_args residual ~1198。下刀：for_call_args mega／expected_ret_ty BSS／CALL thin wrappers／spill BSS／CAP BSS／pipeline_x mega。PLATFORM: SHARED freestanding · LINUX x86 high-end · MACOS\|ARM64 low-end. tip product **8f6defef0**. |
| 2026-08-07 | **feat(8.3.1)** store_retval_pair + copy_large pure leave (wave204) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`glue_store_retval_pair_to_rbp_elf_c`（dual-GP + SLICE length + CALL/METHOD deep-copy use_frame=1 + >16B large）+ private `glue_copy_large_struct_from_rax_ptr_elf_c`（x86 memcpy path）；复用 pure type_size／dual_gp／struct16／slice_length；Cap residual bodies→prototype；deep_copy static→public（pure export extern）；seed cold twin −1；dual-export ban。产品只走 `./xbuild`。mac：prefer T store_retval + heat U + pure-ld；矩阵 rv42／opt102／si／hello + lit=99 · fif=205 · soa=8 · mca=64 · math=0 · f32soa=10。Ubuntu：prefer W store_retval + heat U + pure-ld · 同矩阵。call_args residual ~1596。下刀：for_call_args mega／deep_copy／spill BSS／CAP BSS／pipeline_x mega。PLATFORM: SHARED freestanding · SysV/AAPCS64. tip product **45ac55923**. |
| 2026-08-07 | **feat(8.3.1)** CALL-arg VAR stack_off resolve pure leave (wave203) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`glue_call_arg_resolve_var_stack_off_elf_c`（scoped 快路径 + body-let 懒登记／SKIP_TYPECK anon／zero-name）+ private `glue_block_let_name_is_unusable_c`／`glue_append_block_let_local_at_offset`／`glue_call_arg_resolve_anon_body_let_elf_c`；复用 pure stack_off／lazy_append／emit_module／func_index + pipe num_locals@8；Cap residual bodies→prototype；seed cold twin −1；dual-export ban。补齐 wave202 MEMORY pure 的 Cap residual 依赖。产品只走 `./xbuild`。mac：prefer T resolve + heat U + pure-ld；矩阵 rv42／opt102／si／hello + lit=99 · fif=205 · soa=8 · mca=64 · math=0 · f32soa=10。Ubuntu：prefer W resolve + heat U + pure-ld · 同矩阵。call_args residual ~1758。下刀：for_call_args mega／store_retval／deep_copy／spill BSS／CAP BSS／pipeline_x mega。PLATFORM: SHARED. tip product **c4a6ef8c7**. |
| 2026-08-07 | **feat(8.3.1)** MEMORY by-value CALL-arg pure leave (wave202) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`pipeline_asm_push_sysv_memory_by_value_elf_c`（x86 SysV 高位 push 逆序）+ `pipeline_asm_store_memory_by_value_to_sp_elf_c`（arm64 AAPCS64 SP 存储）；源 VAR／嵌套 CALL sret／STRUCT_LIT／FIELD_ACCESS；复用 pure return_size／struct_let／expr_rec／lvalue／sret + Cap residual var_stack_off + next_offset@4 pipe；Cap residual bodies→prototype；seed cold twins −1；dual-export ban。产品只走 `./xbuild`。mac：prefer T MEMORY + heat U + pure-ld；矩阵 rv42／opt／si／hello + lit=99 · fif=205 · soa=8 · mca=64 · math=0 · f32soa=10。Ubuntu：prefer W MEMORY + heat U + pure-ld · 同矩阵。call_args residual ~1859。下刀：for_call_args mega／spill BSS／CAP BSS／pipeline_x mega。PLATFORM: LINUX+MACOS x86_64 SysV + MACOS\|ARM64 AAPCS64. tip product **c7a5ca40d**. |
| 2026-08-07 | **feat(8.3.1)** SysV f32 xmm param home pure leave (wave201) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`pipeline_asm_emit_param_home_elf_sysv_f32_xmm_c`（gp／xmm 分轨 + dual-GP + MEMORY by-value copy）+ private `glue_func_param_is_f32_c`／`is_f64_c`；复用 pure agg／home_width；Cap residual bodies→prototype；seed cold twin −1；dual-export ban。产品只走 `./xbuild`。mac：prefer T param_home + heat U + pure-ld；矩阵 rv42／opt102／si／hello + lit=99 · fif=205 · soa=8 · mca=64 · math=0 · f32soa=10。Ubuntu：prefer W param_home + heat U + pure-ld · 同矩阵。call_args residual ~2070。下刀：for_call_args mega／MEMORY push／store／spill BSS／CAP BSS／pipeline_x mega。PLATFORM: LINUX+MACOS x86_64 SysV. tip product **0c4da0ad2**. |
| 2026-08-07 | **feat(8.3.1)** f32 VAR slot load pure leave (wave200) | **代码** · **不升钉** · **双端 L2 绿**。B′ pure G.7：`glue_load_f32_var_slot_to_rax_elf_c`／`_to_rbx_elf_c`（param：xmm 4B home 或 f64-widen→cvtsd2ss；local：4B lane）+ private `glue_var_is_current_func_param_c`（emit-module param scan）；Cap residual bodies→prototype；seed cold twins −1；dual-export ban。产品只走 `./xbuild`。mac：prefer T f32 load + heat U + pure-ld；矩阵 rv42／opt／si／hello + lit=99 · fif=205 · soa=8 · mca=64 · math=0 · f32soa=10。Ubuntu：prefer W f32 load + heat U + pure-ld · 同矩阵。call_args residual ~2242。下刀：for_call_args mega／MEMORY／param home／spill BSS／CAP BSS／pipeline_x mega。PLATFORM: SHARED. tip product **501476728**. |

***
