# Shux 自举进度

> **更新**：2026-07-16 · **产品轨双端 bstrict 绿后工程轨 re-baseline**；14 号整表归档  
> **基线 SHA**：`233e3b72`（`fix(link): ensure runtime_panic.o on C need_panic path`）  
> **方法**：[自举方法.md](../自举方法.md)（工程 Cap/R/L/M ≠ 产品放行）  
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `shux-selfhost-product-gate`  
> **归档**：[自举进度-归档-2026-07-14.md](自举进度-归档-2026-07-14.md) · [自举进度-归档-2026-07-13.md](自举进度-归档-2026-07-13.md)

### 维护约定

1. 每完成一波 **只改**：§2 KPI、§3 前排（≤3 条）、§8 一行。  
2. **禁止**把 prove/Stage2/N++ 单独写成「可自举」。  
3. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict` 123。  
4. 工程 KPI 必须写 **SHA + 日志路径**；过期数字移归档，不在正文假装仍绿。

---

## 0. 一句话当前状态

**产品轨已放行，工程轨须重开基线。**  
Ubuntu **L4 真冷** + **mac/Ubuntu 全量 bstrict 123** 在 **`233e3b72` 全绿**。  
14 号表上的 **T=18/18 · N=54 · R2≥49** 是 **未真冷产品验收** 下的历史记账；同 SHA 上 re-baseline 后 **不得沿用**。  
下一步自举主线 = **先修好工程门禁（prove / T 地图）并刷新 N/T**，再谈 R2/Cap 深迁。

---

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](../自举方法.md) | Cap / R / L / M 怎么做 |
| [自举步骤.md](../自举步骤.md) | 可执行步骤模板 |
| **本文** | 现在做到哪、KPI、前排 |
| [自举进度-归档-2026-07-14.md](自举进度-归档-2026-07-14.md) | 14 号完整仪表盘（只读） |
| [问题分析文档.md](../问题分析文档.md) | 产品债 / 根因地图 |
| skill `shux-selfhost-product-gate` | L4 / 双端 / 禁止假绿 |

---

## 2. 仪表盘（re-baseline @ `233e3b72`）

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|-------------|
| Ubuntu L4 真冷 | ✅ `remaining_o=0` → seed → g05 → 矩阵 | `/tmp/ubuntu_true_bstrict_233e3b72.log` |
| Ubuntu `run-all-bstrict` | ✅ **123/123** | 同上 · `L4 DONE bstrict_exit=0` |
| macOS `run-all-bstrict` | ✅ **123/123** | `/tmp/mac_true_bstrict_233e3b72.log` |
| 本波产品修复 | link ensure：random_fill / time_os / panic | `c0da0ce6` · `233e3b72` |

### 2.2 工程轨（14 号 KPI 对照）

| KPI | 14 号（归档） | **2026-07-16 re-baseline** | 健康方向 |
|-----|---------------|----------------------------|----------|
| **T** | 18/18 | **10/18** typeck OK · **8 FAIL** | → 满 |
| **N** | 54 IDENTICAL | **0/54**（全部 FAIL） | ↑ |
| **R2 / H** | R2≥49 · 各 rest T | **未重测**（prove 未过，数字冻结） | 先 N 再抽检 |
| **S** | mega pin | 仍 pin（未变） | ↓ 非本周先决 |

**T 失败模块（`tests/diff_single_link.sh`）**：  
`lexer` · `parser` · `typeck` · `codegen` · `lsp` · `lsp_diag` · `lsp_io` · `compile`  
（多为 T001 extern / XT001 等；日志 `/tmp/ubuntu_rebaseline_eng_233e3b72.log`）

**N 全红根因（不是 54 模块实现「一夜消失」）**：

| # | 原因 | 证据 |
|---|------|------|
| A | prove `gen_x_o` prologue 缺头：`-E` 现含 `readv`/`writev`/`poll`，cc 报 implicit declaration | 手工 `cc -c` 复现 |
| B | MODULES 里部分 seed 仍写 `driver_*_gen.c`，实际已迁 `seeds/*.linux.x86_64.c` | `driver_fmt_gen.c` MISS · `seeds/driver_fmt_gen.linux.x86_64.c` OK |
| C | prove 把 stderr 丢进 `/dev/null`，统一显示「shux -E 或 cc -c 失败」 | 脚本 `2>/dev/null` |

→ **14 号 N=54 在现行 harness 上无法复证**；修 A+B 后才能谈真实 N。

### 2.3 阶段总览

| 阶段 | 含义 | 状态（2026-07-16） |
|------|------|-------------------|
| 产品冷链 / bstrict | 用户路径可编译运行 | ✅ `233e3b72` 双端 123 |
| D Stage2 | 黄金自举 Stage2 | ⬜ **未在本 SHA 重跑**（不作已绿） |
| E 去 C（soft） | R2 / pin | 🟡 历史实现仍在树内；**KPI 失效待重测** |
| F std 无 C | F-ZC | 🟡 以产品 bstrict 为准，不单靠 14 号表 |
| G 终局零 C | nostdlib 等 | ⬜ 非当前前排 |

---

## 3. 当前前排（≤3 条）

1. **修工程门禁 harness**：`tests/prove_module_selfhost.sh` prologue 对齐 g05（补 `sys/uio.h` / `poll.h` 等）+ seed 路径改 `seeds/*`；stderr 落盘便于诊断。重跑 → **刷新 N**。  
2. **澄清 T=10/18**：区分「编译器模块 `-E` 地图假红」vs「产品 typeck 真回归」；修真红或修地图，目标 **T 回满或写清不可满原因**。  
3. **N/T 稳定后** 再恢复 R2/Cap residual 前排（fmt_check path BSS / diagnostic va_list / driver_abi …）；**禁止**在 N=0 时继续报 R2++。

**不在前排**：重做 14 号 50+ 模块 R2 深迁；改测试期望糊 N；未 L4 谈 Stage2 完成。

---

## 4. 与 14 号实现的关系

| 问题 | 结论 |
|------|------|
| 14 号实现还在吗？ | **大多在**（PREFER_X_O / surface.x / seeds 仍在树里） |
| 要不要整表重做？ | **不要**；要 **同 SHA 门禁 re-baseline** |
| 15–16 会否回退工程 KPI？ | **会表现为门禁红**（已见 T↓ N=0）；产品 bstrict 绿 **不能** 推出 N 仍 54 |
| 归档后怎么用旧表？ | 只读考古；红模块对照归档 §4.2 单点修 |

---

## 5. 验收命令（本波）

```bash
# —— 产品（Ubuntu 金标）——
# 见 skill L4；日志：/tmp/ubuntu_true_bstrict_<sha>.log

# —— 工程 re-baseline ——
ssh ubuntu-server 'cd /home/shuliangfu/worker/shu/shux && \
  export SHUX=./compiler/shux_asm && \
  bash tests/prove_module_selfhost.sh 2>&1 | tee /tmp/ubuntu_prove_<sha>.log'
ssh ubuntu-server 'cd /home/shuliangfu/worker/shu/shux && \
  timeout 300 bash tests/diff_single_link.sh 2>&1 | tee /tmp/ubuntu_diff_t_<sha>.log'
```

---

## 6. 变更记录（倒序，一行一条）

| 日期时间 | 摘要 |
|------|------|
| 2026-07-16 | **归档 14 号进度** → `自举进度-归档-2026-07-14.md`；**新开基线** @ `233e3b72`：产品双端 bstrict 123 ✅；工程 re-baseline **T=10/18 · N=0/54**（prove harness seed 路径 + prologue 漂移）；日志 `/tmp/ubuntu_rebaseline_eng_233e3b72.log` · `/tmp/ubuntu_true_bstrict_233e3b72.log` · `/tmp/mac_true_bstrict_233e3b72.log` |
| 2026-07-14 及更早 | 见 [自举进度-归档-2026-07-14.md](自举进度-归档-2026-07-14.md) §8 |

---

## 7. 禁止清单（本阶段）

- [ ] 未用 14 号 T/N/R2 数字当「当前仍绿」  
- [ ] 未在 prove 全红时宣称 E 轨完成  
- [ ] 未把 Stage2 历史 ✅ 当本 SHA 已验  
- [ ] 未跳过 harness 修复直接批量重迁 R2  
- [ ] 产品 SHARED 改动仍须双端 bstrict（已绿可记，新 SHA 重开）
