# Xlang / X language 自举进度

> **更新**：2026-08-31 · tip **`ef9f5c2da`**（**10.3.1 slice16** 不透明 Cap→TYPE_FN 硬拒／`as` 逃逸） · 钉盘仍 **`e8176cbe5`**（**不升钉**）· 日切：30 日 **111** 行已入 [归档](自举进度-归档-2026-08-30.md) · **金标 SSH＝`ubuntu-remote-server`**（回家 `ubuntu-server` 暂无路由） · Ubuntu L2 **钉盘节奏**\
> **产品 L4 放行钉盘**：**`e8176cbe5`**（2026-08-23 升钉 · 前 `f7424ae47` 2026-08-15 · 再前 `e364f4a37` 2026-08-11）\
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅**（12 labi_*.x 切片 prefer；默认 FROM_X=1）· parser **7.2.2 关 pin ✅**（tip `-E` + assemble；产品默认 FROM_X=1）· codegen 7.4.2 wave323 ✅ · typeck 7.4.1 wave322 ✅ · runtime monofile wave321 ✅ · present **0** · 钉盘 **`e8176cbe5`** · Stage 8 tip **wave332 Track L 30/30 FULLY CLOSED**（**PRODUCT HALF=0** · **PRODUCT PINNED=0** · **PRODUCT RETIRED 23/23 = 100%**）· **7.4.3 PREFER_X_O 双端 8/8 wave338 ✅** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁**\
> **tip L4 安全网（历史）**：**`ffc0b646f`**（已由 tip L4 升钉吸收；前网 `4bacaf18c`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：**10.3.1 slice16 不透明 Cap 硬门** · tip **`ef9f5c2da`** · 负例 **build=1** · `as` **42** · L2 **5／5**。
> **代码 tip**：**`bbdff5787`**／**`78d812acf`** — `allow_opaque`；coerce 硬拒／`as function` 放行。残：Darwin pin egg。
> **下波候选**：Darwin 仅当有 ≥1MiB MH_OBJECT · 或 stage10 下一债。**nest 冻 64**。
> **代码 tip（前）**：`e4283974b` Cap host-C cast · `98741dafd` 直 INDEX。

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1；过程上 prefer+heat 一次／域，双端矩阵同波一次\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；**SHARED 双端 L2**；日常矩阵 **只** `./xbuild l2-matrix`（wave299：捕获 rv=42／opt=102 成功码，禁 set -e 截断）；谈自举／升钉才 L4 + 矩阵 + 全量 bstrict；bstrict **per-script timeout=300s** + catalog cache（防无界卡死）；**墙钟**：`product_l4` 分步 wall + `run-all-bstrict` 每脚本 wall／top-N（`XLANG_BSTRICT_TIMING_FILE`）；并行 **`XLANG_BSTRICT_JOBS` 默认 1**（Ubuntu 可 opt-in 2–4；Darwin 慎用 OOM）

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[30](自举进度-归档-2026-08-30.md) · [29](自举进度-归档-2026-08-29.md) · [28](自举进度-归档-2026-08-28.md) · [27](自举进度-归档-2026-08-27.md) · [26](自举进度-归档-2026-08-26.md) · [25](自举进度-归档-2026-08-25.md) · [24](自举进度-归档-2026-08-24.md) · [23](自举进度-归档-2026-08-23.md) · [22](自举进度-归档-2026-08-22.md) · [21](自举进度-归档-2026-08-21.md) · [20](自举进度-归档-2026-08-20.md) · [19](自举进度-归档-2026-08-19.md) · [18](自举进度-归档-2026-08-18.md) · [17](自举进度-归档-2026-08-17.md) · [16](自举进度-归档-2026-08-16.md) · [15](自举进度-归档-2026-08-15.md) · [14](自举进度-归档-2026-08-14.md) · [13](自举进度-归档-2026-08-13.md) · [12](自举进度-归档-2026-08-12.md) · [11](自举进度-归档-2026-08-11.md) · [09](自举进度-归档-2026-08-09.md) · [08](自举进度-归档-2026-08-08.md) · [07](自举进度-归档-2026-08-07.md) · [06](自举进度-归档-2026-08-06.md) · [05](自举进度-归档-2026-08-05.md) · [04](自举进度-归档-2026-08-04.md) · [03](自举进度-归档-2026-08-03.md) · [02](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · …\

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `e8176cbe5`**（2026-08-23 升钉 · 前 `f7424ae47` 2026-08-15 · 再前 `e364f4a37` 2026-08-11）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**双端 Stage2 = ✅ SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709` · `xlang_asm_stage1`／`xlang_asm2` bit-identical · 黄金自举金标准 · strict-link 根修后双端首过）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（Makefile 已删除；**变更记录只在本文 §6**）。

| 指标 | 状态 |
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`e8176cbe5`**（L4 升钉冻结 · 双端 L4 真冷绿 · check-gate al06 入口 typeck）；residual tip **wave332** present **0**；Stage 8 tip **wave332**（**30/30 FULLY CLOSED**；**PRODUCT RETIRED 23/23=100%**；PRODUCT HALF=0；PRODUCT PINNED=0） |
| Track MG | **11.3.1 MG ✅**（wave941 物理删 Makefile · wave942 catalog CFLAGS · 0 make in bootstrap · 产品入口 `./xbuild`） |
| Track BC | **开 🟢（30/30 FULLY CLOSED wave332）**（B′ 至 **wave326** link_abi **7.3.1 关 pin** ✅ · M4 **5/5 全 ✅**（runtime/typeck/codegen/parser/link_abi 五域冷链全闭）· present **0**；**BC Stage8 Batch3 收**＝**30/30 FULLY CLOSED**（PRODUCT 23 RETIRED 100% catalog+ladder；NON-PRODUCT 7 正确分类 never-product-chain = TEST×2 / STAGE×2 / EXTRACT_ONLY×1 / DELETED_ORPHAN×2；G.7 无 HALF 无 unresolved PINNED）|
| Track L (Stage 8) | **收（Batch 3 30/30 FULLY CLOSED wave332）**（批次 0 清理 ✅ · 批次 2 **wave1036–1041** 退役 ✅ · 批次 3 **wave327–332**（lsp_diag+lsp+pipeline+driver+preprocess+lexer+parser/typeck/codegen/ast_gen2 10 项 + audit 分类误判 3 项 RETIRED 修正 + ast_gen.c 孤儿物理删 + ast_gen2 cold-seed pin + 分母口径统一 is_product_denominator）✅ · **30/30 FULLY CLOSED**；PRODUCT RETIRED 23/23=100%；HALF=0；PINNED 产品=0 · 详见 [阶段8高效退役分析.md](阶段8高效退役分析.md)） |
| mac / Ubuntu L4（钉盘） | ✅ **双端 L4 真冷 remaining_o=0 + 矩阵 + bstrict 129** @ **`e8176cbe5`**（mac wall=85m0s · Ubuntu rebuild 7m27s＋heat＋bstrict 4m10s · G.8 SHARED 双端同钉闭环）|
| pin 蛋 | ✅ `seeds/bootstrap_xlangc.darwin.arm64` + `linux.x86_64` 自本波 L4 产品 `xlang_asm` 刷新 |
| 本波构建级别 | 钉盘 **L4 已结**；日常续 **L2**（check 闸门暂停） |

***

## 1. 文档地图

| 文档 | 看什么 |
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录**（**仅 8 月 31 日**） |
| [自举进度-归档-2026-08-30.md](自举进度-归档-2026-08-30.md) | **8月30日** leftover XLANG fallthrough F-06／F-07 ＋ leftover unused compiler-make SOURCE／leftover nested f-* leftover auto-make leftover unused SOURCE（至 f-elf）（**111** 行） |
| [自举进度-归档-2026-08-29.md](自举进度-归档-2026-08-29.md) | **8月29日** leftover XLANG fallthrough F-05／F-04／F-03／F-02／win32／fossil DOC／catalog Honesty 多票 soft→硬绿（至 STD-120 db-compat）（**139** 行） |
| [自举进度-归档-2026-08-28.md](自举进度-归档-2026-08-28.md) | **8月28日** STD／CORE／boot／hello 等 soft prefer-c／SKIP→OK／fallthrough／auto-make →硬绿多票（至 STD-124 regex-atomic）（**113** 行） |
| [自举进度-归档-2026-08-27.md](自举进度-归档-2026-08-27.md) | **8月27日** soft FAIL／prefer-c／SKIP→OK 多票硬绿（f-*／STD／B／sys／perf／lang／zc2／json＋log＋math 等）（**60** 行） |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图（本波不改） |
| [自举进度-归档-2026-08-26.md](自举进度-归档-2026-08-26.md) | **8月26日** STD soft→硬绿梯 ＋ f-phase／f04／f08／f-de-c 等 soft→硬绿（**82** 行） |
| [自举进度-归档-2026-08-25.md](自举进度-归档-2026-08-25.md) | **8月25日** soft 梯／EXC soft→硬绿／STD soft→硬绿多票（**65** 行） |
| [自举进度-归档-2026-08-24.md](自举进度-归档-2026-08-24.md) | **8月24日** 日切重写 · Stage2／WPO／零 make／假权威诚实 DOC 族 · b04／std-config c／NL-04／generic_layout／vscode 0.2／import async（**64** 行） |
| [自举进度-归档-2026-08-23.md](自举进度-归档-2026-08-23.md) | **8月23日** 升钉 `e8176cbe5` ＋ check-gate al06 ＋ labi／stubs unique UNDEF 族 ＋ Darwin CG002／process_merge／minos／g05 ＋ sat L4／fmt／modlet ＋ ALLOW ensure 空 ＋ g05 stubs scan ＋ Ubuntu tip L4（**44** 行） |
| [自举进度-归档-2026-08-22.md](自举进度-归档-2026-08-22.md) | **8月22日** typeck pin spelling_eq ＋ x86_64 `orq` ＋ http STRUCT_LIT ＋ P014 ＋ csv T001 ＋ AAPCS64 第 7 参 ＋ cli `-o` ＋ fmt 静默失败（**8** 行） |
| [自举进度-归档-2026-08-21.md](自举进度-归档-2026-08-21.md) | **8月21日** leftover extra SLICE peels RET twin ＋ dest-PARAM／RET extras ＋ link-abi wave956–958 ＋ 双端 L4（**13** 行） |
| [自举进度-归档-2026-08-20.md](自举进度-归档-2026-08-20.md) | **8月20日** dest extras dest-RET／PTR-outer ＋ ld-only／prefer hybrid／inject ＋ host-C／w189／dyn 前缀（**47** 行） |
| [自举进度-归档-2026-08-19.md](自举进度-归档-2026-08-19.md) | **8月19日** F7 dest leftover 收口（dest-SLICE／dest-ARRAY／INDEX／host-C ret）＋ `[2]*[2]i32` skip-trait T001 |
| [自举进度-归档-2026-08-18.md](自举进度-归档-2026-08-18.md) | **8月18日** TYPE_DYN F1–F6 + F7 asm 开刀 + hello `af 02 _main`（**10** 行） |
| [自举进度-归档-2026-08-17.md](自举进度-归档-2026-08-17.md) | **8月17日** nest 30–64／dest leftover／4.2.x pin-seed |
| [自举进度-归档-2026-08-16.md](自举进度-归档-2026-08-16.md) | **8月16日** dest-in-rbx 族／匿名 match／裸 enum／SIMD dest／nest 21–29（**124** 行） |
| [自举进度-归档-2026-08-15.md](自举进度-归档-2026-08-15.md) | **8月15日** 过夜 Darwin 刀 + 升钉 `f7424ae47` + dest／SIMD／METHOD／nest 至 DEREF dest（**66** 行） |
| [自举进度-归档-2026-08-14.md](自举进度-归档-2026-08-14.md) | **8月14日** dest-SLICE／import 模块 const／INDEX-on-METHOD／f32／Vec4f／宽 lit／STRUCT_LIT／http+queue companions（**79** 行） |
| [自举进度-归档-2026-08-13.md](自举进度-归档-2026-08-13.md) | **8月13日** METHOD(49) 物化／try_inline／>16B memcpy + SIMD leftover + soft residual 梯（**66** 行） |
| [自举进度-归档-2026-08-12.md](自举进度-归档-2026-08-12.md) | **8月12日** Stage12.0.5 prefer 默认 pure-asm · PC FORBID／ALLOW · mega ban／RCA · opaque WEAK · catalog cache · L4 墙钟 · **47** 行 |
| [自举进度-归档-2026-08-11.md](自举进度-归档-2026-08-11.md) | **8月11日** Stage12.0.5 pure-asm residual 闭口 + **升钉 `e364f4a37`**（pure_asm 13/13 · L2-asm／tcpu／ldpc · labi residual · full12 双端地图 · **41** 行 + 10 日余 **4**） |
| [自举进度-归档-2026-08-09.md](自举进度-归档-2026-08-09.md) | **8月9日** runtime thin dual leave → M4 五域关 pin → Stage 8 Batch 3 30/30 → PREFER_X_O 双端 8/8（**28** 行 · 钉盘 `36363b90f`） |
| [自举进度-归档-2026-08-08.md](自举进度-归档-2026-08-08.md) | **8月8日** B′ pipeline_x mega 首刀 bootstrap_glue→ast_pool 各域→elf/wpo→8.3 结构地板→gen dual leave→typeck.x 全量 `-E` T001（**wave262–314** · **56** 行） |
| [自举进度-归档-2026-08-07.md](自举进度-归档-2026-08-07.md) | **8月7日** B′ pure leave wave200–261 + 升钉 `36363b90f` + glue_statics（**64** 行） |
| [自举进度-归档-2026-08-06.md](自举进度-归档-2026-08-06.md) | **8月6日** B′ pure leave field_access→wave199 CALL-target resolve（**50** 行） |
| [自举进度-归档-2026-08-05.md](自举进度-归档-2026-08-05.md) | **8月5日** L4 冷恢复 + 8.3.3 R2 + pure-asm match + B′ leave 至 binop（**74** 行） |
| [自举进度-归档-2026-08-04.md](自举进度-归档-2026-08-04.md) | **8月4日 wave1241–1291**（BC ast_pool 16 波域分解 + glue 壳 thin + check 收口 + 升钉 `db809e00f`） |
| [自举进度-归档-2026-08-03.md](自举进度-归档-2026-08-03.md) | **8月3日 wave1227–1240**（BC glue 叶收口 + check 工具链启动 · 15 行） |
| [自举进度-归档-2026-08-02.md](自举进度-归档-2026-08-02.md) | **8月2日 wave1130–1220**（BC glue 续迁 + typeck 跨模块字段根修 · 43 行） |
| [自举进度-归档-2026-08-01.md](自举进度-归档-2026-08-01.md) | **8月1日 wave964–1111-1129**（BC 8.3.1 G.7 fold 50+ 叶 + Stage 8 退役 13/30） |
| [自举进度-归档-2026-07-31.md](自举进度-归档-2026-07-31.md) | **31 日全日 wave895–963**（MG 真删 + post-delete + 开 BC） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单；per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`e8176cbe5`** | 2026-08-23 升钉；双端 L4 真冷 + 129（mac 85m0s · Ubuntu rebuild 7m27s＋heat＋bstrict 4m10s）；含 check-gate al06 入口 typeck；前 `f7424ae47` |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自本波 L4 `xlang_asm` 刷新（darwin 4.7MiB · linux 4.6MiB） |
| residual tip（BC） | wave339 | M4 **5/5** 全闭 · Stage 8 Batch 3 **30/30 FULLY CLOSED** · 7.4.3 PREFER_X_O **双端 8/8** · pure-ld 58/60 objs · **双端 L4** @ **`e8176cbe5`** · **prefer 族 pure-asm 产品默认 ✅** · **pipeline_abi pure-asm 硬禁** · sat `try-r1` 已走 `ensure_labi_prefer_one`（产品仍 mega） |
| mac 钉盘 L4 | ✅ L4 + bstrict **129** · 85m0s | remaining_o=0 · `./xbuild l4` · `/tmp/xlang_l4_Darwin_e8176cbe5.log` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 remaining_o=0＋heat try_inline＋矩阵 5／5＋bstrict **129** · rebuild 7m27s＋bstrict 4m10s | `ubuntu-server` · `/tmp/xlang_l4_Linux_e8176cbe5.log` · `/tmp/xlang_bstrict_Linux_e8176cbe5.log` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | G.7 单权威 `product_l2_matrix.sh`（wave299）；rv42／opt102／hello0／si0／f32 一次跑全 |
| 前钉 | `f7424ae47` · `e364f4a37` · `d79a368b2` · … | 过夜 Darwin 刀 → Stage12.0.5 residual → formal_mod → … |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 18 号结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ **双端 tip SHA256 STRICT match**（Ubuntu `1ac895e51` · Darwin postlink＋HASH_STRICT 默认=1） | tip Ubuntu：同配方＋`atoi_stub_tu.c` · SHA256=`0ae06666…`／5760344 · `/tmp/stage2_atoi_stable_ubuntu.log` · tip Darwin：双遍 postlink OK＋STRICT SHA256=`42ffac6e…`／5094888 · `/tmp/stage2_darwin_postlink_hash.log` · 钉盘历史 `/tmp/xlang_stage2_ubuntu_9e60e1290.log` + `/tmp/xlang_stage2_macos_fix2.log` |
| **R2 / Cap** | wave710 升钉后按需 | 明细见 29 归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1**（已执行） |
| `TREE_ARMED` | **1** |
| `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1**（wave941 物理删除） |
| `PHYS_DEL_WINDOWS_GATE_STATUS` | `reproven_green` @ `71dd60a12`（wave922 · wave940 一键冷启动） |
| preflight blockers | **全清零**（wave937–939） |
| 真删条件 | ✅ 已完成 @ wave941/942 · 钉盘 **`77b334842`**（后升至 **`db809e00f`**） |
| post-delete residual | **✅ 已收**（wave944–964） |

***

## 3. 当前前排（≤3 条）

1. **日常 L2** — 钉盘 **`e8176cbe5` 已升且冻结**；谈自举／SHARED 大面才再 L4。23 日 unique UNDEF／labi／stubs／al06／CG002／process_merge **勿重开**。
2. **邻域禁区** — **禁**空 struct 糊绿／mega prefer／默认 assemble parser.x。dyn SSE extras **禁**放 ta==1。**禁**用 `mov_rax_to_rbx` 当 wrapper 暂存（兼写 x1）。**禁** `pipeline_module_num_funcs` 判空糊绿。**禁** shell `#define Result_*` 双权威。**禁**把 `run` 收成 IDENT 糊绿。**禁**改 enc_store sz>=16 全局顶 leftover fsz=8。**禁**伪造 mmap backend 1／2。**禁** `extern uint8_t *malloc`。**禁**改 al06 期望／禁把 check 整棵 std／core dep typeck。**禁** `-w` 糊 Darwin minos／**禁**把 `macho.x` minos 抬到 26.0。
3. **PC 底盘／下一刀** — prefer 族 pure-asm 默认 ✅ · **stage10 10.3.2／10.3.1 slice0–16／10.3.3 slice0–3 Ubuntu ✅** · **下一刀＝Darwin pin egg（≥1MiB MH_OBJECT）／stage10 下一债**。pipeline_abi mega **仍硬禁**；**禁 `rm` hybrid egg**；nest 64 **冻帽**。

***

## 4. 与归档的关系

- **8月30日归档**：[自举进度-归档-2026-08-30.md](自举进度-归档-2026-08-30.md) — leftover XLANG fallthrough F-06／F-07 ＋ leftover unused compiler-make SOURCE／leftover nested f-* leftover auto-make leftover unused SOURCE（至 f-elf）· **111** 行 · 钉盘仍 **`e8176cbe5`**。
- **8月29日归档**：[自举进度-归档-2026-08-29.md](自举进度-归档-2026-08-29.md) — leftover XLANG fallthrough F-05 std-db／F-04 net／crypto／compress／F-03／F-02／win32／fossil DOC 及 catalog Honesty 多票 soft→硬绿（至 STD-120 db-compat）· **139** 行 · 钉盘仍 **`e8176cbe5`**。
- **8月28日归档**：[自举进度-归档-2026-08-28.md](自举进度-归档-2026-08-28.md) — STD／CORE／boot／hello 等 soft prefer-c／SKIP→OK／fallthrough／auto-make →硬绿多票（至 STD-124 regex-atomic）· **113** 行 · 钉盘仍 **`e8176cbe5`**。
- **8月27日归档**：[自举进度-归档-2026-08-27.md](自举进度-归档-2026-08-27.md) — soft FAIL／prefer-c／SKIP→OK 多票硬绿（f-*／STD／B／sys／perf／lang／zc2／json＋log＋math 等）· **60** 行 · 钉盘仍 **`e8176cbe5`**。
- **8月26日归档**：[自举进度-归档-2026-08-26.md](自举进度-归档-2026-08-26.md) — STD soft→硬绿梯 ＋ f-phase／f04／f08／f-de-c 等 soft→硬绿 · **82** 行 · 钉盘仍 **`e8176cbe5`**。
- **8月25日归档**：[自举进度-归档-2026-08-25.md](自举进度-归档-2026-08-25.md) — soft 梯／EXC soft→硬绿／STD soft→硬绿多票 · **65** 行 · 钉盘仍 **`e8176cbe5`**。
- **8月24日归档**：[自举进度-归档-2026-08-24.md](自举进度-归档-2026-08-24.md) — 日切重写起点 · Stage2／WPO／零 make／假权威诚实 DOC 族 · b04／std-config c／NL-04／generic_layout／vscode 0.2／import async · **64** 行 · 钉盘仍 **`e8176cbe5`**。
- **8月23日归档**：[自举进度-归档-2026-08-23.md](自举进度-归档-2026-08-23.md) — **升钉 `e8176cbe5`** ＋ check-gate al06 ＋ labi／stubs／formal_mod unique UNDEF 族 ＋ Darwin CG002／process_merge／minos／g05 ＋ sat L4／fmt／modlet · **44** 行 · 钉盘 **`e8176cbe5`**。
- **8月22日及前**：见各日归档链接（§1 文档地图）。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `e8176cbe5`**（2026-08-23 升钉 · 前 `f7424ae47` · 再前 `e364f4a37`）；residual tip **wave339**（present **0** · 日常 L2 可续）。
- 日切后主文档只保留 KPI + 前排 + **8 月 31 日** §6（30 日及前见归档）；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
| **macOS arm64** | **钉盘 L4+bstrict 129 ✅** @ **`e8176cbe5`** · 本波 L2 **5／5** · link run=2 **obs=1**／cli run=2 **obs=1**／panic run=7 **obs=1** · mega **-E 仍硬禁** · 产品只走 `./xbuild` |
| **Ubuntu x86_64（金标）** | **回家局域网复验 tip soft→硬绿 ✅** @ **`e19c5c34a`** · L2 **5／5** · link run=2 **obs=1**／cli／panic obs=0＋boot-015／019 积压绿 · 钉盘仍 **`e8176cbe5`** · **SSH＝`ubuntu-server`**（外地曾用 `ubuntu-remote-server`／frp） |

| **Windows MSYS2** | wave940 一键冷启动绿 · STATUS=`reproven_green` · Makefile 已物理删除 · 本波未升 Windows pin（仓内无 windows pin 蛋） |

***


***

## 6. 变更记录（倒序 · **8 月 31 日**；30 日见 [归档](自举进度-归档-2026-08-30.md) · 29 日见 [归档](自举进度-归档-2026-08-29.md)）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.1 slice16 不透明 Cap→TYPE_FN 硬拒** · tip **`ef9f5c2da`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝无溯源 Cap 赋 TYPE_FN 软放假绿；存储＝`typeck_fnptr_surface_compat`；消费＝let／assign／return／array／`as`。**修**：G.7 `allow_opaque`；coerce=0 硬拒；`as function`=1 逃逸（.x＋seed）。证：Ubuntu migrate typeck＋g05＠**`ef9f5c2da`** · 负例 **build=1** · `as`／prov／ffi／L2 **5／5**。残：Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：Darwin／stage10。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.1 slice15 真 Cap `*u8` host-C 赋／调强转** · tip **`e4283974b`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝TYPE_FN let＝Cap／`c(41)` 缺 `(Ret (*)(T))`→gcc incompatible／not a function；存储＝`emit_c_fnptr_decl`／emit_expr CALL；消费＝`-E`＋cc。**修**：G.7 赋包抽象 declarator＋`codegen_try_emit_cap_u8_call`（.x＋seed）。证：Ubuntu migrate codegen＋g05＠**`e4283974b`** · `-E` `(*f)=(...)c`／`((...)(c))(41)` · asm／host-cc **42** · 邻域／L2 **5／5**。残：不透明 Cap 软／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：Darwin／不透明硬门。 |
| 2026-08-31 | **fix(selfhost)** **stage10 10.3.1 slice14 直 INDEX Cap call `fs[0](x)`** · tip **`98741dafd`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝Cap CALL ret stamp 仅 VAR→`fs[0](41)` ret `?`；存储＝`typeck_check_expr_call_resolve`／arity；消费＝产品 CALL＋asm Cap blr。**修**：G.7 INDEX／FIELD 入 Cap stamp＋arity soft（.x＋seed）。证：Ubuntu migrate typeck＋g05＠**`98741dafd`** · direct／let／lit／Cap **42** · L2 **5／5**。残：真 FFI Cap／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：真 FFI Cap。 |
| 2026-08-31 | **fix(selfhost)** **stage10 10.3.1 slice13 TYPE_FN 数组 INDEX／Cap call** · tip **`bd5fe48cd`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝`elem_byte_sz`／force_esz 漏 TYPE_FN＝18→ARRAY_LIT `mov %eax` 截断；INDEX 8B 读错指针→SEGV；存储＝vector_let_init／force_esz／temp_bytes；消费＝`let g=fs[0]; g(41)`。**修**：G.7 TYPE_FN→8（.x＋seed＋thin weaken inject；禁 mega `-E`）。证：Ubuntu inject＋g05＠**`bd5fe48cd`** · movq＋stride8 · smoke **42** · lit／Cap／L2 **5／5**。残：`fs[0](…)` typeck · 真 FFI Cap／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：直 INDEX call／真 FFI Cap。 |
| 2026-08-31 | **fix(selfhost)** **stage10 10.3.1 slice12 ARRAY_LIT Cap→TYPE_FN coerce** · tip **`c2d94353f`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝array-lit elem 仅 `type_refs_equal`→裸名 Cap `*u8` vs TYPE_FN 假红；存储＝`typeck_coerce_array_lit_elem_types_to_decl`；消费＝`[helper,…]` let／struct 字段。**修**：G.7 复用 `typeck_fnptr_surface_compat`（.x＋seed）。证：Ubuntu migrate typeck＋g05＠**`c2d94353f`** · 正例 **42** · 负例 **build=1** · field／arr-decl／L2 **5／5**。残：TYPE_FN 数组 INDEX／call · 真 FFI Cap／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：INDEX／call。 |
| 2026-08-31 | **fix(selfhost)** **stage10 10.3.1 slice11 `[N]function` host-C `Ret (*name[N])(args)`** · tip **`393d254a8`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝ARRAY-of-TYPE_FN 走 peel+抽象 `Ret (*)(args)`＋`name[N]`→invalid C；存储＝`emit_c_fnptr_decl`／field／let；消费＝`-E`＋cc。**修**：G.7 `array_ty` dims 入 `(*name[N])`；field＋let 主／fallback（.x＋seed）。证：Ubuntu＠**`393d254a8`** · `-E` `int32_t (*fs[2])(int32_t)` · host-cc field／let **42** · field／c_repr／L2 **5／5**。残：`[helper,…]` lit Cap coerce／真 FFI Cap／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：真 FFI Cap／lit coerce。 |
| 2026-08-31 | **fix(selfhost)** **stage10 10.3.1 slice10 TYPE_FN 结构体字段 host-C declarator** · tip **`f826678b0`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝`codegen_emit_struct_field_decl_x` 抽象 `Ret (*)(args)`＋name→`int32_t (*)(int32_t) f` invalid C；存储＝field_decl／seed；消费＝`-E`＋cc／`fn_type_field_smoke`。**修**：G.7 TYPE_FN→`emit_c_fnptr_decl`（.x＋seed）。Darwin pin 再摸：仍无 ≥1MiB MH_OBJECT／禁 mega `-E`／禁 `rm` egg → **pivot**。证：Ubuntu migrate codegen＋g05＠**`f826678b0`** · `-E` `int32_t (*f)(int32_t)` · host-cc／asm **42** · sig／param／L2 **5／5**。残：`[N]function` 字段／真 FFI Cap／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：真 FFI Cap。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.1 slice9 Cap 溯源硬拒错 TYPE_FN** · tip **`2e1783648`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝`TYPE_FN←Cap` 在无裸名时软放→`let f: function(错)=c`／`bare as *u8` 假绿；存储＝`typeck_fnptr_surface_compat`；消费＝let／as／return。**修**：G.7 `typeck_recover_fn_fi_from_cap_expr` 剥 AS＋同块 Cap 局部初值；可恢复则 `matches_type_fn`；真不透明仍软。证：Ubuntu migrate typeck＋g05＠**`2e1783648`** · 正例 **42** · 两负例 **build=1** · param／sig／L2 **5／5**。残：真 FFI Cap／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：Darwin egg。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.1 slice8 TYPE_FN 形参／proto seed 全孪＋host-cc** · tip **`e9ccc020f`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝slice7 `.x` 形参已命名 declarator、cold seed 仍 `emit_type` 抽象＋二次 name→invalid C；存储＝seed emit_func／extern proto／fallback let；消费＝`-E`＋cc `apply(f,x)`。**修**：G.7 twin `emit_c_fnptr_decl` 形参／proto／fallback。证：Ubuntu＠**`e9ccc020f`** · `-E` `int32_t (*f)(int32_t)` · host-cc／asm **42** · 邻域／L2 **5／5**。残：Cap 软／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：Cap 软拒／Darwin egg。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.1 slice7 TYPE_FN C `Ret (*name)(args)`＋host-cc** · tip **`076c5f3e4`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝slice6 `uint8_t * f`→host-cc「not a function」；存储＝`emit_c_fnptr_decl`／seed；消费＝`-E`＋cc／`f(41)`。**修**：G.7 命名／抽象 declarator；let／emit_type／param（.x）；seed emit＋let。证：Ubuntu cold migrate codegen＋g05＠**`076c5f3e4`** · `-E` `int32_t (*f)(int32_t)` · host-cc／asm **42** · 邻域／L2 **5／5**。残：seed 形参全孪／Cap 软／Darwin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：形参 twin／Darwin egg。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.1 slice6 TYPE_FN host-C `uint8_t *`＋Darwin pin egg 地图** · tip **`e143a45ee`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝`emit_type_kind` 无 TYPE_FN→`-E` CG003；Darwin Cap＝旧 `xlang_asm`＠04:27＋pabi≈425KiB 残档／无 MH_OBJECT ≥1MiB pin／mega `-E` 硬禁（`739ccf1d3` 已拒 libtool 残档）。存储＝codegen／pabi ttc。消费＝`-E` TYPE_FN 局部。**修**：G.7 TYPE_FN→`uint8_t *`（Cap ABI）；**不** g05 Darwin。证：Ubuntu cold migrate codegen＠**`e143a45ee`** · `-E` `uint8_t * f = helper` 无 CG003 · asm sig／as **42** · L2 **5／5**。残：`Ret(*)(args)`／call-cast host-cc；Darwin pin。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED codegen · MACOS pin 阻。下一刀：declarator／Darwin egg。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.1 slice5 Cap/TYPE_FN 签名比对硬拒** · tip **`e8b1132c3`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝surface 软放 arity／ret／TYPE_FN↔TYPE_FN 假绿；`type_refs_equal_same_kind` TYPE_FN fallthrough true。存储＝typeck let／assign／as／return／equal。消费＝错签名赋值／`as`。**修**：G.7 `typeck_fn_type_sig_equal`＋`typeck_fnptr_surface_compat`（裸名恢复 module sig）；TYPE_FN 结构 equal。证：Ubuntu migrate＋g05＠**`e8b1132c3`** · sig **42** · 四负例 **build=1** · 邻域／L2 **5／5**。残：不透明 Cap 局部软。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：Darwin。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.1 slice4 `as function(...)` Cap/TYPE_FN** · tip **`84c393426`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝`as function` T001（class_ok 拒 TYPE_FN／无 surface↔surface）；emit 已有 LEA／透传。存储＝`typeck_as_cast_*`＋seed twin。消费＝Cap／裸名 `as function`／`as *u8`。**修**：G.7 surface 双端放行＋TYPE_FN 入 class；`int as function` 仍拒。证：Ubuntu migrate typeck＋g05＠**`84c393426`** · `fn_type_as_smoke` **42** · field／param／return／Cap／L2 **5／5**。残：签名比对。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：签名／Darwin。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.3 slice3 `h.f()` Cap/TYPE_FN 字段间接调** · tip **`75580cabf`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝`h.f(41)` 解析 METHOD_CALL→LANG-004；emit 无字段 Cap blr。存储＝typeck method_call／call_dispatch。消费＝产品 `h.f(args)`。**修**：G.7 `typeck_method_call_field_fnptr_ty`＋emit lvalue+offset+spill+blr。证：Ubuntu migrate typeck＋call_dispatch＋g05＠**`75580cabf`** · field／Cap **42** · 邻域／L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：签名／Darwin。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.3 slice2 TYPE_FN 字段 load＋间接调** · tip **`5995dd72e`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝field load `mov (%rax),%eax; cltq`（`glue_field_access_load_bytes` miss kind18→4）；rtpref mega 污染堵 g05。存储＝pabi load_bytes／backend load_byte_sz。消费＝`h.f`→local→`call *%rax`。**修**：G.7 TYPE_FN→8；去污 stubs／leaves；egg inject＋g05。证：Ubuntu field **42** · `movq` · 作参／返回／Cap／L2 **5／5**。残：`h.f()` 方法语法。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：字段。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.3 slice1 TYPE_FN 作返回＋size/align** · tip **`87c8037d8`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝`return helper` expected TYPE_FN found Cap *u8；字段 size＝0。存储＝`typeck_return_operand_matches`／`typeck_x_type_size|align`。消费＝return／layout。**修**：G.7 表面 match＋TYPE_FN＝8。证：Ubuntu＠**`62425fbfd`**＋探针 slim＠**`87c8037d8`** · return **42** · 作参／裸名／Cap／L2 **5／5**。字段 Cap init／`h.f` load SEGV 残。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：字段。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.3 slice0 TYPE_FN 作参（Cap↔TYPE_FN score）** · tip **`362eee083`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝`apply(helper,x)`／Cap→TYPE_FN formal T001（TYPE_FN→TYPE_FN 已绿）。存储＝`typeck_overload_arg_param_score`。消费＝call-arg／overload。同模式＝let／assign `typeck_is_fnptr_surface`。**修**：G.7 跨 kind 表面分 100（不抢 Cap↔Cap）。证：Ubuntu migrate typeck＋g05＠**`362eee083`** · `fn_type_param_smoke` **42** · 裸名／Cap／L2 **5／5**。pabi egg 未删。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：返回／字段。 |
| 2026-08-31 | **fix(selfhost)** **恢复 Ubuntu hybrid pabi egg＋裸名 Cap LEA 金标** · tip **`e99a8f74c`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝mega `wpo_dump_write` 缺 `xlang_driver_fwrite_*`／`fopen_write` extern → XT001 阻 `-E`；误 `rm` egg 后仅 235KiB rest。存储＝mega 声明／PREGEN／prefer。消费＝g05＋裸名探针。**修**：G.7 补全 thin 已有 extern；`XLANG_PREGEN_E_C`＋FORCE prefer→≈1.8MiB ELF；g05 sync；探针裸名。证：Ubuntu＠**`e99a8f74c`** 裸名 **42** · Cap42 · L2 **5／5**。`/tmp/runtime_pipeline_abi.hybrid.egg.bak`。**禁再删 egg**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：10.3.3／Darwin。 |
| 2026-08-31 | **feat(selfhost)** **裸名 Cap LEA 入树；pabi hybrid egg 误删待恢复** · tip **`0df731d7e`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝裸 `helper` 初值无栈槽 CG002（LEA 仅 EXPR_AS）。存储＝`emit_expr_elf_fast`／AS tgt TYPE_FN。消费＝`let f: function… = bare`。**修**：G.7 同 Cap AS 算法补 VAR off＜0 LEA。验：FORCE 重建时误 `rm` hybrid egg；mega `-E` XT001 `wpo_dump_write`；现用旧 `xlang_asm`＠20:29 Cap42＋`(as *u8)`42＋L2 **5／5**；探针暂回 AS。**禁再删 egg**。下一刀：恢复 ≥1MiB hybrid／裸名金标。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.1 slice2 TYPE_FN←Cap coerce＋间接调** · tip **`7da3889f8`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝TYPE_FN 与 Cap `*u8` 失配／CALL 不认 18。存储＝typeck let／assign／call；call_dispatch Cap 槽。消费＝`let f: function(...): T = (fn as *u8); f(x)`。同模式＝dyn_assign_ok／Cap `*u8` soft-skip。**修**：G.7 `typeck_is_fnptr_surface`；coerce＋ret stamp＋Cap emit `ko==18`；探针用 EXPR_AS LEA（裸名仍 CG002）。证：Ubuntu migrate typeck＋call_dispatch＋g05＠**`7da3889f8`** · `fn_type_parse_smoke` **run=42** · Cap42 · L2 **5／5**。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：裸名 LEA／10.3.3／Darwin。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.1 slice1 parser `function(...): T`→TYPE_FN** · tip **`4bb7e8b74`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝无类型位函数类型语法（C5 写 `fn`/`->` 与 X H02 冲突）。存储＝`parser_asm_type_ref_slice.inc` Type 池＋wave467 sidecar。消费＝let／param 类型注解。同模式＝`Linear(T)`＋`Name<T,U>` sidecar。**修**：G.7 补全既有 type_ref；`TOKEN_FUNCTION`+`(`…`)`+`:`+Ret → TYPE_FN＝18；elem＝ret／array_size＝n／append_type_arg；diag 格式 `function`；禁 Rust `fn`/`->`。证：Ubuntu rebuild `parser_asm_thin_glue.o`＋`typeck_x.o`＋g05 · 探针 `fn_type_parse_smoke.x` **`expected function, found i32`**（解析过）· Cap run=42 · L2 **5／5**。Darwin 不 g05。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：resolve／codegen。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.1 slice0 TYPE_FN＝18 地基** · tip **`64ca84e05`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝无 `TYPE_FN`／`fn(...) -> T`（Cap 暂借 `*u8`）。存储＝`ast.x` TypeKind／arena kind_ord。消费＝typeck／codegen／parser（后切片）。同模式＝TYPE_DYN＝17 append-only。**修**：G.7 末尾追加 TYPE_FN＝18；`type_kind_ordinal` hi→18；twin seeds／ast.h；**不**扩 primitive cache（FN 非 primitive）。证：Ubuntu＠**`64ca84e05`** Cap run=42 · L2 **5／5**。Darwin 不 g05 L2。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：parser。 |
| 2026-08-31 | **fix(selfhost)** **Darwin pabi 拒不完整 libtool archive（Cap 共验仍阻）** · tip **`739ccf1d3`** | **代码＋文档** · **不升钉** · **git-only**。五问：产生＝Darwin `ld -r`→`libtool -static` 把 `runtime_pipeline_abi.o` 收成残档（～260KiB）→ Cap LEA／g05 崩。存储＝ensure inject／prefer。消费＝Darwin 产品链。**修**：G.7 补 `pipeline_abi_o_is_libtool_archive`；inject 拒 out＜base；prefer 拒＜1MiB 残档。证：Darwin 旧 `xlang_asm` L2 **5／5**；Cap 探针仍需 hybrid pabi（mega `-E` 硬禁／无 pin egg）。Ubuntu＠**`739ccf1d3`** Cap run=42 · L2 **5／5**。续探：thin+seed-rest `cc -r`→MH_OBJECT 仍 g05 UNDEF hybrid。钉盘仍 **`e8176cbe5`**。PLATFORM: MACOS ensure · SHARED 门。下一刀：TYPE_FN／pin egg。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.2 slice4 Cap-fn-ptr 栈参（撤 reg_max 硬拒）** · tip **`410118683`** | **代码＋文档** · **不升钉** · **git-only 金标**。五问：产生＝Cap 拒 nargs＞reg_max；存储＝call_dispatch Cap 槽；消费＝`f7(…,21)` 第 7 参走栈。**修**：G.7 复用既有 `emit_call_args` 栈路径。证：Ubuntu `git reset --hard origin/self-hosting`＠**`410118683`** · rebuild call_dispatch＋g05 · build=0 **run=42** · L2 **5／5**（**禁 scp**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：TYPE_FN／Darwin。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.2 slice3 Cap-fn-ptr `(*f)()` DEREF peel** · tip **`d4ae8371c`** | **代码＋文档** · **不升钉** · **git-only 金标**。五问：产生＝`(*f)()` callee＝EXPR_DEREF(52)，typeck／asm 只 peel ADDR_OF(51) → ret `?`。存储＝call_resolve／call_arity／Cap emit。消费＝`(*f0)()`／`(*f1)(41)`。**修**：G.7 补全既有 peel（52｜51）；Cap ≡ `f()`（emit 指针值，非 u8 load）。证：Ubuntu `git reset --hard origin/self-hosting`＠**`d4ae8371c`** · migrate typeck＋call_dispatch＋g05 · build=0 **run=42** · L2 **5／5**（**禁 scp**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：TYPE_FN／栈参／Darwin。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.2 slice2 Cap-fn-ptr GP-arg 间接调（spill＋call_args＋blr）** · tip **`3db4cde35`** | **代码＋文档** · **不升钉** · **git-only 金标**。五问：产生＝slice1 拒 nargs≠0；存储＝call_dispatch Cap 槽；消费＝`f(41)`。**修**：G.7 复用 spill／emit_call_args／load_rbp／blr／reg_max。探针 `f0()`＋`f1(41)`。证：Ubuntu `git reset --hard origin/self-hosting`＠**`3db4cde35`** · rebuild call_dispatch＋g05 · build=0 **run=42** · `ff d0`×2 · L2 **5／5**（**禁 scp**）。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：`(*f)()`／TYPE_FN／Darwin。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.2 slice1 Cap-fn-ptr 0-arg 间接调（`f()`／`call *%rax`）** · tip **`e90e2a70d`** | **代码＋文档** · **不升钉** · **日常 L2（Ubuntu git pull）**。五问：产生＝`f()` typeck 当 unresolved CALL；asm 无 `*u8` 间接路径。存储＝`typeck.x`／`typeck_gen` Cap soft-skip＋stamp ret；`backend_call_dispatch.x`／seed emit_expr＋`blr`。消费＝产品 `xlang_asm -o`。同模式＝F7 dyn `backend_enc_blr_arch`。**修**：G.7 补全既有 CALL 拦截槽＋typeck Cap 门。证：push 后 Ubuntu `git reset --hard origin/self-hosting`＠**`e90e2a70d`** · migrate typeck＋call_dispatch＋pabi inject＋g05 · 探针 build=0 **run=42** · L2 **5／5**。Darwin **不** g05。跳过 mega／升钉／check／带参／TYPE_FN。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：带参／`(*f)()`／Darwin 恢复／10.3.1。 |
| 2026-08-31 | **feat(selfhost)** **stage10 10.3.2 slice0 Cap-fn-ptr 取址（LEA＋WPO is_used）** · tip **WIP** | **代码＋文档** · **不升钉** · **日常 L2（Ubuntu 金标）**。五问：产生＝`(fn as *u8)` typeck 已放行但 asm EXPR_AS fallthrough 当局部 → CG002；取址后 WPO 无 call 边裁掉体 → ld UNDEF。存储＝`runtime_pipeline_abi.x`／seed／`fnptr_as_thin.x`＋ensure inject；`typeck.x`／`typeck_gen` Cap 路径 `set_is_used`。消费＝产品 `xlang_asm -o`。同模式＝F7 vtable `set_is_used`＋`backend_enc_lea_sym_to_reg_arch`。**修**：G.7 补全 emit_as Cap-fn-ptr（#[no_mangle] LEA）＋typeck 保体；thin inject 避 mega `-E`。证：Ubuntu-remote inject pabi＋migrate typeck＋探针 `fnptr_addr_smoke.x` **T helper＋T main** build=0 run=0。Darwin **不** g05（本波 pabi archive 残；旧 `xlang_asm` 保留）。跳过 mega／升钉／check／间接调／TYPE_FN。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。下一刀：间接调／Darwin 恢复／10.3.1。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 stage10 S3.1 slice2 raw syscall0..6 asm-backend 金标文档 SHA** · tip **`71363283b`** | **文档** · **不升钉** · **日常 L2**。代码 **`274790051`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移（10.1.1 C＋asm ✅）；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **feat(selfhost)** **stage10 S3.1 slice2（10.1.1）raw_syscall0..6 asm-backend ELF intercept** · tip **`274790051`**（含 `ee45f4a55`） | **代码** · **不升钉** · **日常 L2**。五问：产生＝C 后端已拦截 raw_syscall0..6，asm 路径仍走 panic 体。存储＝`backend_call_dispatch.x`／seed from_x.c（`try_emit_raw_syscall_call_elf_c`）＋`backend_x86_64_enc_c.x`／seed（`enc_syscall` 0F 05＋`mov_rax_to_r10`）。消费＝产品 `xlang_asm -o`。同模式＝wave463 size_of CALL 站点。**修**：G.7 补全既有 CALL／METHOD_CALL 拦截槽＋spill 复用 glue_sysv_spill；根修 name_into 缓冲 16→128。证：Ubuntu g05＋smoke run＝`Hello Xlang!`＋objdump `syscall`×2＋option 102＋L2 **5／5**。Darwin 不 g05 L2 **5／5**。跳过 pipeline_abi mega／升钉／check／10.1.2 arm64。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED（emit）＋LINUX x86_64（runtime）。下一刀：10.1.2 arm64 svc／10.1.4 raw FFI／7.4.5 typeck pin。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 stage10 S3.1 slice1 raw syscall0..6 C-backend builtin 金标文档 SHA** · tip **`2671e5c0a`** | **文档** · **不升钉** · **日常 L2**。代码 **`5940e7fac`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移（10.1.1 🟡＋新增 7.4.5 typeck pin 缺体漂移）；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **feat(selfhost)** **stage10 S3.1 slice1（10.1.1）raw_syscall0..6 C-backend builtin twin codegen.x** · tip **`5940e7fac`**（含首刀 `a2b9a091e`） | **代码** · **不升钉** · **日常 L2** · **阶段 10 开刀（0→1 slice）**。五问：产生＝语言无 syscall 内建（asm! 全未实现，K1 仅 ast.h 枚举；syscall 全走 `extern xlang_sys_*` C 桥）→ 阶段 9 Cap residual 无法纯 .x 消灭。存储＝`std/sys/linux.x`（raw_syscall0..6 语言面，panic 诚实失败体）＋`codegen.x`（`codegen_try_emit_raw_syscall_call` CALL 站点拦截＋`codegen_emit_raw_syscall_helpers` emit_header helper）＋seed `codegen_gen.linux.x86_64.c` 孪生＋`tests/sys/raw_syscall_smoke.x`。消费＝`-E`＋host-cc 用户路径（产品 asm 路径下 callers 走 panic 诚实失败）。同模式＝wave463 size_of／align_of CALL 站点内建单权威。**修**：G.7 复用 wave463 形状；helper 走 `#if defined(__linux__) && defined(__x86_64__)`（host cc 预处理器＝平台真值；arm64 svc=10.1.2）；根修一＝首刀只挂 EXPR_CALL，点号调用解析为 METHOD_CALL（49）不匹配 → 补双形状＋emit_expr METHOD_CALL 分支 fmt_mc 块后挂点（fmt_lit 同形）。证：Ubuntu `-E` 展开精确 `((int64_t)(__xlang_raw_syscall0((long)(39))))`／raw_syscall3 四实参＋host-cc＋**run＝`Hello Xlang!` exit 0**（getpid 39／write 1 真实 raw syscall，无 libc 无 C 桥）＋L2 **5／5**。g05 重链 codegen_x.o（479440→488880 Track L cold seed 路）＋xlang_asm **16108864**。**波内发现既有债**：`./xbuild migrate` typeck cold-seed UNDEF（seed pin 缺 `typeck_get_allow_legacy_extern_calls` 体；装配有）→ pure-ld 失败；临时修＝typeck_x.o 由装配产物 host-cc；录 C迁移 **7.4.5**。Darwin 本波 **不** g05（冻结 `xlang_asm` L2 **5／5**；本地 tip 装配＋host-cc 0 错＋符号就位已验）。跳过 pipeline_abi mega／升钉／check。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED（emit）＋LINUX x86_64（runtime）。下一刀：10.1.1 asm 后端直出／10.1.2 arm64 svc／10.1.4 raw FFI／7.4.5 typeck pin 补体／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 seed dest-SLICE parent-block const scan 金标文档 SHA** · tip **`0380d58c0`** | **文档** · **不升钉** · **日常 L2**。代码 **`cb81ac7ed`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **seed dest-SLICE parent-block const scan twin codegen.x** · tip **`cb81ac7ed`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed try_emit VAR（历史只扫 prior lets 再 resolved TYPE_ARRAY；typeck stamp 把 `[a]`／`let s:[]T=a` 盖成 TYPE_SLICE 藏 N → wrap 丢）；codegen.x 已扫 same-block consts 再 parent lets／consts（32 hop）。存储＝seed `codegen_gen.linux.x86_64.c`。消费＝host-cc of `-E`／`tests/boundary/nested_slice_let_parent.x`／`nested_slice_const_var.x`／`nested_slice_parent_var.x`。同模式＝既有 dest-SLICE try_emit VAR／wrap_nc／wrap_br fat compound。**修**：G.7 补全既有 try_emit VAR 为 seed twin；不开第二 scan。证：Ubuntu `-E` nslp s0／s1 `.data=a,.length=2`＋s2 `.data=b`＋ss wrap_br＋无 bare `s0 = a`＋host-cc＋run=42／nslcvar／nslpvar／nslvar run=78／nslf run=42／asg／ras run=42／nslc run=43／nslcd／nslm run=43／nslac run=71／nslacd run=71／narrvar／narr／cms／las／cims host-cc＋run=42／L2 **5／5**。g05 重链 codegen_x.o；inject-only pabi 胀（16720592→16773944；**未** rm egg）。Darwin 本波 **不** g05（`xlang_asm` 仍 04:27 L2 **5／5**）。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／pipeline_abi mega。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 seed dest-SLICE non-VAR FIELD let-init 金标文档 SHA** · tip **`ac4b10dc9`** | **文档** · **不升钉** · **日常 L2**。代码 **`0987451e7`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **seed dest-SLICE non-VAR FIELD let-init twin codegen.x** · tip **`0987451e7`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed try_emit FIELD（历史 VAR-base-only `return 0` → `let s:[]T = W{}.xs`／`mk().xs`／`rows[i].xs` 落到 emit_expr，host-cc `s = ((W{…}).xs)` invalid initializer）；codegen.x 已 wrap：N 取自基 TYPE_NAMED 布局，STRUCT_LIT／INDEX 视图 `((base).field)`，CALL／METHOD memcpy `__xlang_fbN`（返回临时量死）。存储＝seed `codegen_gen.linux.x86_64.c`。消费＝host-cc of `-E`／`tests/boundary/nested_slice_let_field.x`。同模式＝既有 dest-SLICE try_emit FIELD／ASSIGN／RETURN fat compound。**修**：G.7 补全既有 try_emit FIELD 为 seed twin；不开第二 wrap。证：Ubuntu `-E` nslf s0 `((W{…}).xs)` 视图＋s1 `__xlang_fb` memcpy＋s2 `((rows)[1]).xs`＋s3 `w.xs`＋ss wrap_br＋无 bare `s0 = ((W{…}).xs)`＋host-cc＋run=42／asg run=42／ras run=42／nslc run=43／nslcd／nslm run=43／nslac run=71／nslacd run=71／narrvar／narr／cms／las／cims host-cc＋run=42／L2 **5／5**。g05 重链 codegen_x.o；inject-only pabi 胀（16667256→16720592；**未** rm egg）。Darwin 本波 **不** g05（`xlang_asm` 仍 04:27 L2 **5／5**）。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／pipeline_abi mega。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：leftover seed dest-SLICE parent-block const scan／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 seed dest-SLICE ASSIGN ARRAY wrap 金标文档 SHA** · tip **`8c2006b39`** | **文档** · **不升钉** · **日常 L2**。代码 **`ec7b3a793`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **seed dest-SLICE ASSIGN ARRAY wrap twin codegen.x** · tip **`ec7b3a793`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed EXPR_ASSIGN kind 28（历史 bare `s = a` 把 TYPE_ARRAY 的 E* 当 fat 赋给 dest-SLICE，host-cc BLD001）；codegen.x 已 wrap：LHS TYPE_SLICE + RHS TYPE_ARRAY，同帧 `(s = (T){.data=a,.length=N})`（dest 在本函数不悬空，异于 RETURN 的 `__xlang_rar` durable）。存储＝seed `codegen_gen.linux.x86_64.c`。消费＝host-cc of `-E`／`tests/boundary/asg_array_as_slice.x`。同模式＝既有 dest-SLICE RETURN／try_emit／wrap_br fat compound。**修**：G.7 补全既有 EXPR_ASSIGN dest-SLICE ASSIGN ARRAY wrap 为 seed twin；不开第二 wrap。证：Ubuntu `-E` asg `(s = (T){.data=a,.length=2})`＋无 `s = a;`＋host-cc＋run=42／from_var／from_field／from_lit 三端 wrap／ras `__xlang_rar` run=42／nslc run=43／nslcd／nslm run=43／nslac run=71／nslacd run=71／narrvar／narr／cms run=42／las host-cc＋run=42／L2 **5／5**。g05 重链 codegen_x.o；inject-only pabi 胀（16613912→16667256；**未** rm egg）。Darwin 本波 **不** g05（`xlang_asm` 仍 04:27 L2 **5／5**）。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／pipeline_abi mega。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：leftover seed dest-SLICE non-VAR FIELD let-init（`W{}.xs`／`mk().xs`／`rows[1].xs`）／parent-block const scan／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 seed dest-SLICE RETURN ARRAY wrap 金标文档 SHA** · tip **`a577bbfc1`** | **文档** · **不升钉** · **日常 L2**。代码 **`d7e37ffaa`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **seed dest-SLICE RETURN ARRAY wrap twin codegen.x** · tip **`d7e37ffaa`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed dest-SLICE return（历史 bare `return a` 把 TYPE_ARRAY 的 E* 当 fat 返回，host-cc BLD001）；codegen.x 已 wrap：operand 仍 TYPE_ARRAY（typeck 不 stamp），durable static memcpy 再 fat `__xlang_rar`。存储＝seed `codegen_gen.linux.x86_64.c`。消费＝host-cc of `-E`／`tests/boundary/ret_array_as_slice.x`。同模式＝既有 TYPE_ARRAY return durable 与 slice-escape `__xlang_esc`。**修**：G.7 补全既有 emit_return_stmt_with_context dest-SLICE RETURN ARRAY wrap 为 seed twin；不开第二 wrap。证：Ubuntu `-E` ras `__xlang_rar`＋无 `return a;`＋host-cc＋run=42／from_var／from_field／from_lit 三端 wrap／nslc run=43／nslcd／nslm run=43／nslac run=71／nslacd run=71／narrvar／narr／cms run=42／las host-cc＋reent＋run=42／L2 **5／5**。g05 重链 codegen_x.o；inject-only pabi 胀（16560576→16613912；**未** rm egg）。Darwin 本波 **不** g05（`xlang_asm` 仍 04:27 L2 **5／5**）。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／pipeline_abi mega。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：leftover seed dest-SLICE ASSIGN ARRAY（`s = a`）／non-VAR FIELD let-init（`W{}.xs`／`mk().xs`）／parent-block const scan／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 seed dest-SLICE let-init CALL reent gate 金标文档 SHA** · tip **`57bd2f723`** | **文档** · **不升钉** · **日常 L2**。代码 **`4cd078a7b`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **seed dest-SLICE let-init CALL reent gate twin codegen.x** · tip **`4cd078a7b`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed dest-SLICE let-init CALL／METHOD（历史无 `codegen_slice_let_call_returns_slice` 闸 → wave409 reent `__xlang_sp = mk()` 把 ARRAY 返回 E* 赋进 fat，host-cc BLD001）；codegen.x 已闸：callee TYPE_SLICE 才 reent，ARRAY 返回走 try_emit wrap。存储＝seed `codegen_gen.linux.x86_64.c`。消费＝host-cc of `-E`／`tests/boundary/nested_slice_let_call.x`。同模式＝既有 try_emit CALL／METHOD wrap。**修**：G.7 补全既有 let-init reent 闸为 seed twin（same-module＋dep-arena）；不开第二 wrap。证：Ubuntu `-E` nslc typed fat＋无 `__xlang_sp`＋host-cc＋run=43／nslcd `.data=nested_slice_mk_dep_x_mk()` host-cc＋run=43／nslm `.data=xs(w)` host-cc＋run=43／nslac run=71／nslitc run=78／nslitm run=78／nslacd run=71／nslf run=42／narrvar／narr／cms／nested／mut／cims／fn-slice run=42／las host-cc＋reent＋run=42／L2 **5／5**。g05 重链 codegen_x.o；inject-only pabi 胀（16507224→16560576；**未** rm egg）。Darwin 本波 **不** g05（`xlang_asm` 仍 04:27 L2 **5／5**）。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／pipeline_abi mega。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：leftover seed dest-SLICE RETURN ARRAY（`return a` E* vs fat）／parent-block const scan／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 seed dest-SLICE try_emit CALL／METHOD 金标文档 SHA** · tip **`f9fbab68c`** | **文档** · **不升钉** · **日常 L2**。代码 **`550851ecf`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **seed dest-SLICE try_emit CALL／METHOD twin codegen.x** · tip **`550851ecf`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed try_emit dest-SLICE CALL／METHOD 行（历史 kind 48／49 return 0 → wrap_br `{mk(), fat}` host-cc 把 fat 填进 `.length`）；codegen.x 已从 callee return TYPE_ARRAY 取 N 发 typed fat `(T){.data=mk(),.length=N}`。存储＝seed `codegen_gen.linux.x86_64.c`。消费＝host-cc of `-E`／`tests/boundary/nested_slice_lit_arr_call.x`。同模式＝既有 dest-SLICE wrap family（wrap_br VAR／INDEX／wrap_nc）。**修**：G.7 补全既有 try_emit 48／49（same-module＋dep-arena）；不开第二 wrap。证：Ubuntu `-E` nslac typed fat＋host-cc＋run=71／nslc run=78／nslm `.data=xs(w)` host-cc＋run=78／nslacd dep mk host-cc＋run=71／narrvar run=71／narr run=71／cms／nested／mut／cims／fn-slice run=42／L2 **5／5**。g05 重链 codegen_x.o；inject-only pabi 胀（16453888→16507224；**未** rm egg）。Darwin 本波 **不** g05（`xlang_asm` 仍 04:27 L2 **5／5**）。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／pipeline_abi mega。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：leftover seed dest-SLICE let-init CALL reent（`let s:[]T=mk()`）／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 seed dest-SLICE wrap_br 金标文档 SHA** · tip **`fcd4103b1`** | **文档** · **不升钉** · **日常 L2**。代码 **`767ce1477`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **seed dest-SLICE wrap_br twin codegen.x** · tip **`767ce1477`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed emit_braced dest-ARRAY `[N][]T` ARRAY_LIT 行（历史 recurse-braces／裸 emit_expr → `x[2] = {a,{3,4}}`／`{{1,2},{3,4}}` ints into fat）；codegen.x 已有 dest_elem_is_slice＋try_emit wrap_br。存储＝seed `codegen_gen.linux.x86_64.c`。消费＝host-cc of `-E`／`tests/boundary/nested_slice_lit_arr_var.x`。同模式＝既有 dest-SLICE wrap family（wrap_nc `[][]T=[a]`）。**修**：G.7 补全既有 emit_braced wrap_br（reuse try_emit＋module_array_var）；不开第二 wrap。证：Ubuntu `-E` var typed fat＋host-cc＋run=71／arr host-cc＋run=71／cms run=42／nested run=42／mut run=42／cims run=42／fn-slice run=42／L2 **5／5**。g05 重链 codegen_x.o；inject-only pabi 胀（16400552→16453888；**未** rm egg）。Darwin 本波 **不** g05（`xlang_asm` 仍 04:27 L2 **5／5**）。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／pipeline_abi mega。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：leftover seed dest-SLICE try_emit CALL／METHOD（`[N][]T=[mk()]`）／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 seed dest-SLICE file-scope wrap 金标文档 SHA** · tip **`e24639239`** | **文档** · **不升钉** · **日常 L2**。代码 **`dd5996114`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **seed dest-SLICE file-scope wrap twin codegen.x** · tip **`dd5996114`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed codegen_x_ast dest-SLICE 顶层 const／let（历史 emit_expr only → `static const T s = (A)[1]`／`= B`／GNU statement-expr ARRAY_LIT／init_globals `m = B` array decay）；codegen.x 已有 try_emit INDEX＋slice_tl／ig／dig＋emit_file_scope。存储＝seed `codegen_gen.linux.x86_64.c`。消费＝host-cc of `-E`／`tests/boundary/const_module_slice.x`。同模式＝既有 dest-SLICE wrap family。**修**：G.7 补全既有 try_emit INDEX（kind 47）＋emit_file_scope helper＋codegen_x_ast slice_tl／fa_slice_rows／slice_ig／slice_dig 调用点；不开第二 wrap。证：Ubuntu `-E` cms typed fat＋host-cc＋run=42／nested host-cc＋run=42／mut run=42／cims run=42／fn-slice run=42／n8 host-cc＋run=40／L2 **5／5**。g05 重链 codegen_x.o；inject-only pabi 胀（16347216→16400552；**未** rm egg）。Darwin 本波 **不** g05（`xlang_asm` 仍 04:27 L2 **5／5**）。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／pipeline_abi mega。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：leftover seed dest-SLICE wrap_br（`[N][]T=[A]`）／try_emit CALL／METHOD／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 seed dest-SLICE wrap import-module const FIELD 金标文档 SHA** · tip **`a885cfd26`** | **文档** · **不升钉** · **日常 L2**。代码 **`d073754db`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **seed dest-SLICE wrap import-module const FIELD twin codegen.x** · tip **`d073754db`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed dest-SLICE wrap of import-module const FIELD（`dep.A`／`[dep.A]`）；codegen.x 已有 `try_emit_dest_slice_from_import_const_field`；seed kind 44 return 0；try_emit 把 import FIELD 当结构体成员发 `sizeof(dep.A)` → host-cc undeclared `dep`。存储＝seed `codegen_gen.linux.x86_64.c`。消费＝host-cc of `-E`／`tests/boundary/const_import_module_slice.x`。同模式＝既有 `.x` sibling＋`emit_import_module_const_field` ARRAY_LIT durable `__xlang_icN`。**修**：G.7 补全既有 dest-SLICE wrap family 为 seed twin（sibling＋module_array_var dispatch＋emit_import dep-arena INT_LIT／ARRAY_LIT＋emit_block let／const fallback）；try_emit 加 ctx，import path 命中 return 0 让 sibling 接手；不开第二 wrap。证：Ubuntu `-E` cims typed fat＋`__xlang_icN`＋host-cc＋run=42／take([a]) host-cc＋run=7／take([A]) host-cc＋run=7／sa2 run=42／n8 host-cc＋run=40／L2 **5／5**。g05 重链 codegen_x.o；inject-only pabi 胀（16240528→16347216；**未** rm egg）。Darwin 本波 **不** g05（`xlang_asm` 仍 04:27 L2 **5／5**）。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／pipeline_abi mega。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 seed dest-SLICE ARRAY_LIT wrap `[][]T=[a]` 金标文档 SHA** · tip **`bf8f9d39a`** | **文档** · **不升钉** · **日常 L2**。代码 **`697270fa2`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **seed dest-SLICE ARRAY_LIT wrap `[][]T=[a]` twin codegen.x** · tip **`697270fa2`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed dest-SLICE ARRAY_LIT 非 const 赋值，缺 wrap_nc（codegen.x 已发 `{.data=a,.length=N}`）；`__xlang_al[i]=a` array decay → `E *` vs fat；seed helper 历史无类型 `{.data=a}` 赋值 host-cc 「expected expression before '{'」。存储＝seed `codegen_gen.linux.x86_64.c`。消费＝host-cc of `-E`／`take([a])`／`take([A])`。同模式＝既有 `.x` wrap_nc＋`try_emit_slice_init_from_array_var`／`try_emit_dest_slice_from_module_array_var`。**修**：G.7 补全既有 dest-SLICE ARRAY_LIT wrap_nc 为 seed twin；补全既有 helper 发 `(T){…}`；twin 模块顶层 `[A]`；不开第二 wrap。import-module const FIELD 仍 leftover。证：Ubuntu `-E` take([a]) typed fat＋host-cc＋run=6／take([A]) host-cc＋run=6／sa2 run=8／ssan run=9／n8 host-cc＋run=1／L2 **5／5**。g05 重链 codegen_x.o；inject-only pabi 胀（16133848→16240528；**未** rm egg）。Darwin 本波 **不** g05（`xlang_asm` 仍 04:27 L2 **5／5**）。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／pipeline_abi mega。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：leftover dest-SLICE ARRAY_LIT wrap import-module const FIELD／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 seed dest-SLICE ARRAY_LIT of ARRAY 金标文档 SHA** · tip **`0570b5ab9`** | **文档** · **不升钉** · **日常 L2**。代码 **`281f58bc8`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **seed dest-SLICE ARRAY_LIT of ARRAY twin codegen.x** · tip **`281f58bc8`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed `codegen_emit_expr` ARRAY_LIT is_slice，缺 dest-SLICE of ARRAY `E al[][N]`（codegen.x 已发）；`emit_type(ARRAY)` decay → `static int32_t * __xlang_al[]` vs layout `int32_t (*)[N]`。存储＝seed `codegen_gen.linux.x86_64.c`。消费＝host-cc of `-E`／`take([[3,4]])`。同模式＝既有 `.x` dest-SLICE ARRAY_LIT＋seed `emit_local_fixed_array_*`／`emit_braced_array_lit_init`。**修**：G.7 补全既有 dest-SLICE ARRAY_LIT 为 seed twin（含 `codegen_array_lit_tree_is_const`）；不开第二 ARRAY_LIT init。证：Ubuntu `-E` take([[3,4]]) `int32_t __xlang_al[][2]={{3,4}}`＋host-cc＋run=7＋memcpy `[[n,n+1]]` run=7＋sa2n／spn／ssan／n16 run=60＋n64 run=64＋n8 host-cc＋nn8 run=55＋SIMD＋L2 **5／5**。g05 重链 codegen_x.o；inject-only pabi 胀（16080496→16133848；**未** rm egg）。Darwin 本波 **不** g05（`xlang_asm` 仍 04:27 L2 **5／5**）。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／pipeline_abi mega。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 seed slice-of-ARRAY／PTR fat layouts 金标文档 SHA** · tip **`1cb9ab4ac`** | **文档** · **不升钉** · **日常 L2**。代码 **`558b74ad7`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **seed slice-of-ARRAY／PTR fat layouts twin codegen.x** · tip **`558b74ad7`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed `codegen_x_ast`，缺 `codegen_emit_slice_of_fixed_array_layouts`（codegen.x 已在 entry struct defs 后走 walker）；bare `-E` of `[][2]i32`／`[]*i32` incomplete `xlang_slice_xlang_arr2_int32_t`／`xlang_slice_int32_t_p`。存储＝seed `codegen_gen.linux.x86_64.c`。消费＝xlang-c／xlang_asm `-E` host-cc slice-of-ARRAY／PTR。同模式＝既有 `.x` walker＋seed `codegen_emit_c`／`codegen_type_is`／`emit_local_fixed_array_*`。**修**：G.7 补全既有 walker 为 seed twin；entry 调用点同 `.x`；不开第二 layout walker。证：Ubuntu `-E` sa2n／spn／ssan layout＋take＋host-cc＋run=7／11／13＋n16 run=60＋n64 run=64＋n8 host-cc＋nn8 run=55＋SIMD＋L2 **5／5**。g05 重链 codegen_x.o；inject-only pabi 胀（16012520→16080496；ptrslot 仍 1；**未** rm egg）。Darwin 本波 **不** g05（`xlang_asm` 仍 04:27 L2 **5／5**；旧二进制 `[][2]i32` 仍 incomplete）。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／pipeline_abi mega。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：leftover `[][2]i32` ARRAY_LIT init `int32_t *` vs `int32_t (*)[2]`／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover w110 field-type scratch 256→896 金标文档 SHA** · tip **`63a5ac35d`** | **文档** · **不升钉** · **日常 L2**。代码 **`eb8e463dd`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **leftover w110 field-type scratch 256→896 twin .x** · tip **`eb8e463dd`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover `pipeline_codegen_emit_struct_field_type_inner_w110` TYPE_SLICE 调 `type_to_c_repr` cap 256；活链 hybrid 是 WEAK `.x`（已 896）＋ codegen `emit_type` 896。存储＝`runtime_pipeline_abi.from_x.c` 冷孪生。消费＝cold-when-pure-FROM_X-object-not-linked；Ubuntu egg nm 仅 W。同模式＝既有 `.x` inner／ttc-thin 896。**修**：G.7 补全既有 w110 inner scratch／cap 256→896；vector／kind copy 仍 256；不开第二 field-type emitter；**不** g05／不叠 inject（egg leftover 非 strong）。证：Ubuntu `-E` n16／n64／n8 host-cc＋run=60／64／40＋nn8／16／64 Cell layouts 64＋take 8／16／64×tag＋host-cc＋run=55／65／74＋boxn64 field host-cc＋run=64＋SIMD＋L2 **5／5**。Darwin 本波 **不** g05（`xlang_asm` 仍 04:27 L2 **5／5**；旧二进制 named8 仍 7 层）。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／pipeline_abi mega。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 seed named companion 1..64 金标文档 SHA** · tip **`5eb0cd7fa`** | **文档** · **不升钉** · **日常 L2**。代码 **`aa7da4cfb`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **seed named companion 循环 1..64 twin codegen.x** · tip **`aa7da4cfb`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed `codegen_emit_companion_named_slice_layout`（wave698 展开 1..7 only），`codegen.x` 已循环 1..64 调 `codegen_emit_slice_fat_one`；bare `-E` of `[]×8 Cell` incomplete（take 标签在形参列表内声明）。存储＝seed `codegen_gen.linux.x86_64.c`。消费＝xlang-c／xlang_asm `-E` 与 host-cc named slice。同模式＝既有 fat-one helper。**修**：G.7 补全既有 companion（seed twin 循环 1..64）；不开第二 named-layout emitter。证：Ubuntu `-E` nn8／16／64 Cell layouts **64**＋take **8／16／64**×tag＋host-cc ELF＋`-o` run=55／65／74＋n16 run=60＋n64 run=64＋n8 host-cc＋SIMD＋L2 **5／5**。Darwin 本波 **不** g05（`xlang_asm` 仍 04:27 L2 **5／5**；旧二进制 named8 仍 7 层）。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／pipeline_abi mega。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：Cap residual／language L2 white／parser pin egg／w110 scratch 256。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 type_to_c_repr 19＋2×plen nest 64 金标文档 SHA** · tip **`d8a0bb677`** | **文档** · **不升钉** · **日常 L2**。代码 **`dd170d622`** Ubuntu 金标已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate；**未**改当前问题分析。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **type_to_c_repr SLICE cap 写循环非 19＋2×plen** · tip **`dd170d622`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 ttc-thin／`.x` TYPE_SLICE `19+2*plen` 预检；nest 64 i32 tag=782 进 896 但 2×plen nest 63≈1545 → XP003。存储＝`runtime_pipeline_abi.from_x.c` ttc-thin＋`runtime_pipeline_abi.x`。消费＝xlang-c／xlang_asm `-E` nested TYPE_SLICE 形参。同模式＝既有写循环 `w+2>=cap`。**修**：G.7 补全既有 cap（header 19；增长走写循环）；不开第三 type_to_c_repr；named companion stay leftover emit-path。证：Ubuntu `-E` n64 64×tag＋host-cc ELF＋`-o` run=64＋n16 run=60＋n9 run=50＋n8 host-cc＋SIMD＋L2 **5／5**。Darwin 本波 g05 未重链（inject 叠层后 hybrid egg 本地擦；`xlang_asm` 仍 04:27 L2 **5／5**；旧二进制 n64 仍 XP003）。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／pipeline_abi mega。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：Darwin pipeline_abi hybrid egg 恢复／named companion／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs** **日切归档 30 号 + 重写 31 号** · 代码 tip 仍 **`dc34e5539`** | **文档** · **不升钉** · **日常 L2**。§6 收 30 日 **111** 行入 [自举进度-归档-2026-08-30.md](自举进度-归档-2026-08-30.md)；主文档仅保留 31 日 KPI／前排／§6。钉盘仍 **`e8176cbe5`**。[自举时序.md](自举时序.md) 快照日 25→31。**未**改 [当前问题分析.md](当前问题分析.md)。下一刀：leftover type_to_c_repr 19＋2×plen nest 64／named companion／Cap residual／language L2 white／parser pin egg。PLATFORM: SHARED archaeology。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 type_to_c_repr scratch 256→896 twin codegen.x 双端文档 SHA** · tip **`a6369e835`** | **文档** · **不升钉** · **日常 L2**。代码 **`dc34e5539`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **type_to_c_repr scratch 256→896（twin codegen.x／pabi ttc-thin）** · tip **`dc34e5539`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed `codegen_emit_type` `slb`／`eb_bt` cap 256＋pabi ttc-thin `inner`／`eb` 256（first-wins over weak `.x` 896）；`19+2*plen` nest 16＝393 → nested_slice16 `-E` XP003。存储＝`codegen_gen.linux.x86_64.c`＋`runtime_pipeline_abi.from_x.c` ttc-thin。消费＝xlang-c／xlang_asm `-E` nested TYPE_SLICE 形参。同模式＝既有 codegen.x／runtime_pipeline_abi.x 896 族。**修**：G.7 补全既有 scratch（不开第三 type_to_c_repr）；不改 `19+2*plen` 门（n64 仍 XP003）。named companion stay leftover emit-path。证：Darwin `-E` n16 16×tag＋host-cc RC=0 Mach-O arm64／`-o` run=60／codegen_x.o 434464／n9 run=50／n8 host-cc RC=0／n64 XP003／L2 **5／5**；Ubuntu `-E` n16 16×tag＋host-cc RC=0 ELF x86-64／`-o` run=60／codegen_x.o 455048／n9 run=50／n8 host-cc RC=0／n64 XP003／L2 **5／5**。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／pipeline_abi mega。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：leftover type_to_c_repr 19＋2×plen nest 64／named companion／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 seed emit_header scalar slice nests 9..64 twin codegen.x 双端文档 SHA** · tip **`3b5d65e26`** | **文档** · **不升钉** · **日常 L2**。代码 **`74b32efad`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **seed emit_header scalar slice nests 9..64（twin codegen.x）** · tip **`74b32efad`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed `codegen_x_ast_emit_header`（wave698 内联 1..8 only），`codegen.x` 已调 `codegen_emit_scalar_slice_nests` 至 nest 64（N16／N17..N52／N64）；bare `-E` of `[]×9 i32` incomplete type。存储＝seed `codegen_gen.linux.x86_64.c`。消费＝xlang-c／xlang_asm `-E` 与 `-o` nested scalar slice。同模式＝既有 codegen.x helper。**修**：G.7 补全既有 `codegen_emit_xlang_slice_prefix_rep`／`codegen_emit_slice_fat_one`／`codegen_emit_scalar_slice_nests`（seed twin）并从 seed emit_header 调用；seed C 格式化两位数 N16..N64 卫（不开 36 份 u8[80] 表）；不开第三 layout emitter。named companion 与 seed `type_to_c_repr` scratch 256（codegen.x 896；n16／n64 XP003）stay leftover emit-path。证：Darwin `-E` n9 N16＋`xlang_slice×9_int32_t`／host-cc RC=0 Mach-O arm64／`-o` run=50／codegen_x.o 433976／n8 host-cc RC=0／L2 **5／5**；Ubuntu `-E` n9 N16＋tag／host-cc RC=0 ELF x86-64／`-o` run=50／codegen_x.o 455624／n8 host-cc RC=0／L2 **5／5**。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：leftover seed type_to_c_repr scratch 256／named companion／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 seed emit_header SIMD typedefs twin codegen.x 双端文档 SHA** · tip **`de93a59fa`** | **文档** · **不升钉** · **日常 L2**。代码 **`4a7a91bca`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **seed emit_header SIMD typedefs（twin codegen.x）** · tip **`4a7a91bca`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed `codegen_x_ast_emit_header`（include 后跳 TYPE_DYN），`codegen.x` 已调 `codegen_emit_vector_typedefs`（i32x4_t／u32x8_t／f32x4_t …）；bare `-E` unknown type i32x4_t。存储＝seed `codegen_gen.linux.x86_64.c`。消费＝xlang-c／xlang_asm `-E` 与 std_x host-cc SIMD TU。同模式＝既有 codegen.x helper。**修**：G.7 补全既有 `codegen_emit_vector_typedefs`（seed twin）并从 seed emit_header 调用；不开第三 typedef emitter。rt_preamble §10 仍是 `-o` twin（`XLANG_VECTOR_TYPES` 卫）。std_x inject 族仍 defense-in-depth。证：Darwin／Ubuntu `-E` SIMD typedefs 夹在六 include 与 TYPE_DYN 之间；Darwin host-cc SIMD `-E` RC=0 Mach-O arm64／codegen_x.o 462544／L2 **5／5**；Ubuntu host-cc SIMD `-E` RC=0 ELF x86-64／codegen_x.o 554472／L2 **5／5**。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 seed emit_header string／stdlib／unistd twin codegen.x 双端文档 SHA** · tip **`f4c1624a9`** | **文档** · **不升钉** · **日常 L2**。代码 **`db7fb8f80`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **seed emit_header string／stdlib／unistd（twin codegen.x）** · tip **`db7fb8f80`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝活链 `-E` 走 seed `codegen_x_ast_emit_header`（stdint／stddef／sys/types only），`codegen.x` 已发 string／stdlib／unistd；skip-decl 已丢 memcpy／getcwd 原型 → xlang-c `-E` undeclared memcpy／getcwd。存储＝seed `codegen_gen.linux.x86_64.c`。消费＝xlang-c／xlang_asm `-E` 与 std_x host-cc。同模式＝既有 codegen.x emit_header。**修**：G.7 补全既有 seed emit_header；另补 TU 内已有体的 forward decl（Darwin clang C99 implicit-decl）。std_x inject 族仍 defense-in-depth。不开第三 prologue。证：Darwin／Ubuntu `-E` 六 include；Darwin FORCE=1 USE_C=1 ensure socketio.o RC=0 Mach-O arm64 64576／`_main.o` absent／asm restore 101355／`--check` OK＋L2 **5／5**；Ubuntu FORCE=1 USE_C=1 ensure socketio.o RC=0 ELF x86-64 105912／`_main.o` absent／asm restore 112984／`--check` OK＋L2 **5／5**。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 std_x host-cc string.h inject socketio memcpy／memcmp 双端文档 SHA** · tip **`44eecf4a7`** | **文档** · **不升钉** · **日常 L2**。代码 **`42a028d4f`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **std_x host-cc `string.h` inject（socketio memcpy／memcmp）** · tip **`42a028d4f`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`xlang-c -E` 产品 prologue 只有 stdint／stddef／sys/types，codegen skip-decl 已丢掉 memcpy 原型；Darwin clang 「undeclared library function memcpy」（兼 memcmp／strlen／memset）→ `XLANG_COMPILE_STD_USE_C=1` 编不出 socketio.o。存储＝`std/socketio/socketio.o` gen C。消费＝std_x host-cc ensure。同模式＝既有 fcntl／stdlib inject。**修**：G.7 补全既有 inject 族；缺 `<string.h>` 且体用 memcpy／memcmp／memset／strlen 则 splice；不开第三 inject。leftover nested std-socketio stay observational。证：Darwin FORCE=1 USE_C=1 ensure socketio.o RC=0 Mach-O arm64 64576／`_main.o` absent／asm restore 101355／`--check` OK＋L2 **5／5**；Ubuntu FORCE=1 USE_C=1 ensure socketio.o RC=0 ELF x86-64 105912／`_main.o` absent／asm restore 112984／`--check` OK＋L2 **5／5**。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 std_x auto-soft-merge single-TU rename Darwin ld -r residual 双端文档 SHA** · tip **`a8e88135c`** | **文档** · **不升钉** · **日常 L2**。代码 **`c27d0ffa0`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **fix(selfhost)** **std_x auto-soft-merge single-TU rename（Darwin ld -r residual）** · tip **`c27d0ffa0`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝`xlang_compile_std_x.sh` auto-soft-merge 把单对象 `ld -r` 当改名（Darwin `ld -r` of xlang_asm Mach-O 两段 LC_SEGMENT_64 失败「more than one LC_SEGMENT found」）。存储＝`std/socketio/socketio.o`（catalog auto-soft-merge 唯叶）。消费＝SAT／产品 ensure。同模式＝两对象 `ld -r` 仍在 `archaeology_host_pick_phony.sh`（tls_mbedtls／sqlite）。**修**：G.7 补全既有 merge 体；单对象改名＝`mv` 非 `ld -r`；不开第三 merger。leftover nested std-socketio stay observational。leftover nested xlang-c host-cc `string.h` inject（memcpy／memcmp）stay 本波不搅。证：Darwin FORCE=1 ensure socketio.o RC=0 Mach-O arm64 101355／`_main.o` absent／`--check` OK＋L2 **5／5**；Ubuntu FORCE=1 ensure socketio.o RC=0 ELF x86-64 112984／`_main.o` absent／`--check` OK＋L2 **5／5**。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：leftover nested xlang-c host-cc `string.h` inject／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-uuid leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`726c0daf2`** | **文档** · **不升钉** · **日常 L2**。代码 **`61731b997`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-uuid leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`61731b997`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `uuid.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested std-uuid stay observational（product residual listed skip；parent smoke=0）；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-uuid-v1 static=1 ensure=1 manifest=1 smoke=0 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-uuid-v1 static=1 ensure=1 manifest=1 smoke=0 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：Honesty leftover nested f-* leftover auto-make leftover unused SOURCE **池空**；leave-class（strview-zc4／c05／wpo／catalog SOURCE／bootstrap-min／portable-suite）与 F-04 LIVE 不重写。产品轨：pipeline_x still host-cc mega／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-http leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`698a5d4d9`** | **文档** · **不升钉** · **日常 L2**。代码 **`c5acff901`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-http leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`c5acff901`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `runtime_http_glue.o`／`http.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested std-http／leftover nested std-http-chunked／leftover nested std-http-methods／leftover nested std-http-https stay HARD；leftover nested observational server-pool／reqresp／h2／context stay（product residual listed skip）；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-http-v1 static=1 ensure=1 glue=1 http=1 chunked=1 methods=1 https=1 pool=0 reqresp=0 h2=1 ctx=0 skip=0＋一闸 neg RC=1＋两闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-http-v1 static=1 ensure=1 glue=1 http=1 chunked=1 methods=1 https=1 pool=0 reqresp=0 h2=1 ctx=0 skip=0＋一闸 neg RC=1＋两闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested f-uuid leftover auto-make／leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：leftover nested f-uuid leftover auto-make leftover unused SOURCE。产品轨：pipeline_x still host-cc mega／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-hash leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`ba985b627`** | **文档** · **不升钉** · **日常 L2**。代码 **`4900b1efd`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-hash leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`4900b1efd`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `hash.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested std-hash-hasher-trait／leftover nested std-hash-xxhash／leftover nested std-hash-default-strategy stay observational；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-hash-v1 static=1 ensure=1 hasher=0 xxhash=0 default=0 skip=0／hash-v2 static=1 ensure=1 hasher=0 xxhash=0 default=0 skip=0＋两闸 neg RC=1＋两闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-hash-v1 static=1 ensure=1 hasher=0 xxhash=0 default=0 skip=0／hash-v2 static=1 ensure=1 hasher=0 xxhash=0 default=0 skip=0＋两闸 neg RC=1＋两闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested f-http leftover auto-make／leftover nested f-uuid leftover auto-make／leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：leftover nested f-http leftover auto-make leftover unused SOURCE。产品轨：pipeline_x still host-cc mega／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-time leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`0b7b76cb0`** | **文档** · **不升钉** · **日常 L2**。代码 **`e31e5c30e`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-time leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`e31e5c30e`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `time.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested std-time stay；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-time-v1 static=1 ensure=1 time=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-time-v1 static=1 ensure=1 time=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／hash＊／http／uuid／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。下一刀：Honesty leftover nested f-* leftover auto-make leftover unused SOURCE **池空**（product-red hash／http／uuid 跳过）。产品轨：pipeline_x still host-cc mega／Cap residual／language L2 white／parser pin egg。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-runtime leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`c26ca82b0`** | **文档** · **不升钉** · **日常 L2**。代码 **`1f4f2c16c`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-runtime leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`1f4f2c16c`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `runtime.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested std-runtime-panic-hook stay；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-runtime-v1 static=1 ensure=1 panic=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-runtime-v1 static=1 ensure=1 panic=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested f-time leftover auto-make／leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／hash＊／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-random leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`c2f588fc4`** | **文档** · **不升钉** · **日常 L2**。代码 **`fb9b72f55`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-random leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`fb9b72f55`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `random.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested std-random-rng stay；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-random-v1 static=1 ensure=1 rng=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-random-v1 static=1 ensure=1 rng=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested f-runtime leftover auto-make／leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／hash＊／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-process leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`fcd427741`** | **文档** · **不升钉** · **日常 L2**。代码 **`f212e8c92`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-process leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`f212e8c92`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `runtime_process_os_glue.o`／`runtime_process_argv.o`／`process.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested std-process-xplat stay；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-process-v1 static=1 ensure=1 xplat=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-process-v1 static=1 ensure=1 xplat=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested f-random leftover auto-make／leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／hash＊／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-math leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`a948ead90`** | **文档** · **不升钉** · **日常 L2**。代码 **`c5330122b`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-math leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`c5330122b`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `math.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested observational std-math-special／leftover nested observational std-math-fenv stay；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-math-v1 static=1 ensure=1 special=1 fenv=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-math-v1 static=1 ensure=1 special=1 fenv=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested f-process leftover auto-make／leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／hash＊／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-log leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`d4d888e26`** | **文档** · **不升钉** · **日常 L2**。代码 **`90e7f1c8d`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-log leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`90e7f1c8d`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `log.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested std-log-multi-sink／leftover nested std-log-rotate-async stay；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-log-v1 static=1 ensure=1 multi=1 rotate=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-log-v1 static=1 ensure=1 multi=1 rotate=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested f-math leftover auto-make／leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／hash＊／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-env leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`282d67162`** | **文档** · **不升钉** · **日常 L2**。代码 **`a6bd26dba`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-env leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`a6bd26dba`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `env.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested std-env-iter／leftover nested std-env-platform-encoding stay；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-env-v1 static=1 ensure=1 iter=1 plat=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-env-v1 static=1 ensure=1 iter=1 plat=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested f-log leftover auto-make／leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／hash＊／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-encoding leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`84daf0a6f`** | **文档** · **不升钉** · **日常 L2**。代码 **`f45dda16a`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-encoding leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`f45dda16a`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `encoding.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested encoding-hex-b64／leftover nested encoding-extra stay observational（产品红 listed skip，不 invoke）；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-encoding-v1 static=1 ensure=1 hex=0 extra=0 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-encoding-v1 static=1 ensure=1 hex=0 extra=0 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested f-env leftover auto-make／leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／hash＊／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-crypto leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`2f48b6a3f`** | **文档** · **不升钉** · **日常 L2**。代码 **`73dab0250`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-crypto leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`73dab0250`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `crypto.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested f04-std-crypto-closure／leftover nested std-crypto-aes-gcm／leftover nested std-crypto-sha512-hmac stay；leftover nested observational chacha／leftover nested observational ed25519 stay；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-crypto-v1 static=1 ensure=1 f04=1 aes=1 sha512=1 chacha=1 ed25519=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-crypto-v1 static=1 ensure=1 f04=1 aes=1 sha512=1 chacha=1 ed25519=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested f-encoding leftover auto-make／leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／hash＊／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-channel leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`1140770c0`** | **文档** · **不升钉** · **日常 L2**。代码 **`fb3cc7c14`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-channel leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`fb3cc7c14`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `runtime_channel_glue.o`／`channel.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested std-channel-select／leftover nested observational std-channel-unbounded stay；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-channel-v1 static=1 ensure=1 glue=1 select=1 unbounded=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-channel-v1 static=1 ensure=1 glue=1 select=1 unbounded=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested f-crypto leftover auto-make／leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／hash＊／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-base64 leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`ba10e3b2e`** | **文档** · **不升钉** · **日常 L2**。代码 **`3e91331e9`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-base64 leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`3e91331e9`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `base64.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested std-base64-stream stay；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-base64-v1 static=1 ensure=1 stream=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-base64-v1 static=1 ensure=1 stream=1 skip=0＋一闸 neg RC=1＋一闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested f-channel leftover auto-make／leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／hash＊／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-url leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`c544028d7`** | **文档** · **不升钉** · **日常 L2**。代码 **`68211c2e9`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-url leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`68211c2e9`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `url.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested std-url／std-url-ipv6-host stay；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-url-v1 static=1 ensure=1 url=1 ipv6=1 skip=0／url-v2 static=1 ensure=1 url=1 ipv6=1 skip=0＋两闸 neg RC=1＋两闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-url-v1 static=1 ensure=1 url=1 ipv6=1 skip=0／url-v2 static=1 ensure=1 url=1 ipv6=1 skip=0＋两闸 neg RC=1＋两闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested f-base64 leftover auto-make／leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／hash＊／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-trace leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`f4667abb0`** | **文档** · **不升钉** · **日常 L2**。代码 **`aa6c30ef3`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-trace leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`aa6c30ef3`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `trace.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested std-trace／std-trace-hooks stay；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-trace-v1 static=1 ensure=1 trace=1 hooks=1 skip=0／trace-v2 static=1 ensure=1 trace=1 hooks=1 skip=0＋两闸 neg RC=1＋两闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-trace-v1 static=1 ensure=1 trace=1 hooks=1 skip=0／trace-v2 static=1 ensure=1 trace=1 hooks=1 skip=0＋两闸 neg RC=1＋两闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested f-url leftover auto-make／leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／hash＊／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-test leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`5ba634107`** | **文档** · **不升钉** · **日常 L2**。代码 **`1c5a10cb5`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-test leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`1c5a10cb5`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `test.o`／`runtime_test_fn_invoke.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested std-test-runner stay；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-test-v1 static=1 ensure=1 runner=1 skip=0／test-v2 static=1 ensure=1 runner=1 skip=0＋两闸 neg RC=1＋两闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-test-v1 static=1 ensure=1 runner=1 skip=0／test-v2 static=1 ensure=1 runner=1 skip=0＋两闸 neg RC=1＋两闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested f-trace leftover auto-make／leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／hash＊／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
| 2026-08-31 | **docs(selfhost)** **tip 对齐 leftover nested f-task leftover auto-make leftover unused SOURCE 双端文档 SHA** · tip **`2d337261c`** | **文档** · **不升钉** · **日常 L2**。代码 **`f1119ce6e`** 双端已绿后文档 tip 对齐；更新 KPI／§6／C迁移；无 archive Gate（闸无独立 DOC／TSV）；**未**改自举时序。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED。 |
| 2026-08-31 | **test(selfhost)** **leftover nested f-task leftover auto-make leftover unused SOURCE soft→硬绿** · tip **`f1119ce6e`** | **代码** · **不升钉** · **日常 L2**。五问：产生＝leftover auto-make of `task.o`（`xlang_compiler_make` 即使 leaf 已在 — try-heat／g05 抢 L2）＋leftover unused compiler-make SOURCE after leftover auto-make retired。**修**：G.7 补全既有（不开第三 resolver）；缺 leaf 硬 die；leftover nested std-task stay；leftover nested std-strview-zc4（T001／zc4／host-c）／leftover nested c05 lsp auto-make／leftover nested wpo-s1／wpo-s3 check HARD 仍 leave-class；leftover nested F-04 net leftover closure LIVE／leftover nested F-04 net-slice v13b／v14 LIVE 不重写。证：Darwin leftover nested f-task-v1 static=1 ensure=1 task=1 skip=0／task-v2 static=1 ensure=1 task=1 skip=0＋两闸 neg RC=1＋两闸 missing-leaf RC=1＋L2 **5／5**；Ubuntu leftover nested f-task-v1 static=1 ensure=1 task=1 skip=0／task-v2 static=1 ensure=1 task=1 skip=0＋两闸 neg RC=1＋两闸 missing-leaf RC=1＋L2 **5／5**。跳过 leftover nested f-test leftover auto-make／leftover nested std-strview-zc4／leftover nested c05 lsp auto-make／leftover nested wpo-s1／leftover nested wpo-s3 leftover nested check HARD／leftover bootstrap-min／ensure 族本体／wrap 本体／zc3／zc4／zc5／brotli／hash＊／pipeline_abi。钉盘仍 **`e8176cbe5`**。PLATFORM: SHARED archaeology。 |
