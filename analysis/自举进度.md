# Xlang / X language 自举进度

> 更新：2026-09-27 · w1089 装入 Darwin。arm64 的 `runtime_compress_zlib_glue.o` 由共享薄层加 zlib 桥纯 asm 编出，再 ld -r。版本号是 `1.2.12`，`z_stream` 是 112 字节。编译器本体仍是 3972176，`02e6e1a61f9d8e49`。钉盘 `ecdb5cc1e` 不升。L2 5/5。没有跑 L4。
> 状态：尚未完全自举。option=102。
> 下波：不跑 g05_prepare。不跑 L4，不升钉。禁 bak 175735。不要用现行编译器重编 `backend_x86_64_enc_c.o`，也不要重编 `backend_enc_dispatch.o`（加宽后的帧会被盖掉）。不要把 w1058 归档里的坏 dispatch 或旧 labi 拷回来。不要把 w1064 的 `pabi_weak.prev.o` 拷回来。不要把 w1065 的 `pabi_weak.o.prev` 或 `xlang.prev` 拷回来。不要把 w1066 的 `pabi_weak.o.prev` 或 `xlang.prev` 拷回来。不要把 `pipeline_asm_host_is_arm64_c` 改回加载 BSS。不要把 STRUCT_LIT（45）从大于 16 字节的拷贝门拿掉，也不要把 ARRAY_LIT（46）加进去。不要用 gcc `-E` 编 emit_one。不要装入 w1055、w1056、w1057、w1058 侧路。机器码不是链上对象前缀的纯 asm，不要换进去。不要替换 `typeck_c_module_stubs.o`，也不要替换 `pipeline_bootstrap_orchestration.o`。不要为了凑旧对象去掉 x19 或 rbx 那 16 字节。不要再拿 clang 叶子或旧的 `0x858`／`0x860` 帧做前缀对照。不要重编 pabi 去换结构体字面量。不要把 `load_rbp_to_rdx` 在 ta 非 0 时改成返回 0。不要把 w1067 的 `pabi_weak.o.prev` 或 `xlang.prev` 拷回来。不要把大于 16 字节的变量改回只载入一个 qword。不要把 EXPR_VAR（3）从大于 16 字节的拷贝门拿掉。不要把 w1068 的 `pabi_weak.o.prev`、`field_cap_residual_load.o.prev` 或 `xlang.prev` 拷回来。不要把 FIELD（44）从大于 16 字节的拷贝门拿掉。不要把大于 16 字节的字段改回只载入一个 qword。不要把 w1069 的 `pabi_weak.o.prev` 或 `xlang.prev` 拷回来。不要让 9 到 16 字节的字段在没有调用时跳过 deref。不要把不超过 8 字节的非调用字段改成只载入一个 qword。不要把 w1070 的 `xlang.prev` 拷回来。不要清掉产品 TEXT 末尾的字段赋值洞，重链会丢掉它。不要把 9 到 16 字节的字段赋值改回只存一个 qword。不要把 w1071 的 `xlang.prev` 拷回来。不要把大于 16 字节的字段赋值改回只存源地址。不要把大于 16 字节的字段赋值送进 16 字节的成对存储。不要把 ARRAY_LIT（46）加进字段赋值的拷贝种类。不要把 w1072 的 `xlang.prev` 拷回来。不要把大于 16 字节的下标赋值改回只存源地址。不要把大于 16 字节的下标赋值送进间接的单 qword 存储。不要把 ARRAY_LIT（46）加进下标赋值的拷贝种类。不要把 w1073 的 `xlang.prev` 拷回来。不要把 9 到 16 字节的下标赋值改回只写低 8 字节。不要对步进为 12 的元素做 16 字节存储。不要把 w1074 的 `xlang.prev` 拷回来。不要把大于 16 字节的字面量成员改回只存源地址。不要把 9 到 16 字节的字面量成员改回只写低 8 字节。不要对 12 字节的字面量成员做 16 字节存储。不要把 w1075 的旧 `runtime_panic.o` 拷回来。Darwin arm64 的 panic 不要改回先走 host cc。不要把 w1076 的旧 `runtime_link_abi_user_env.o` 拷回来。Darwin arm64 的 user_env 不要改回先走 host cc。不要把这两个 getenv 符号改成强符号。不要把 Darwin arm64 的 random_fill 改回先走 host cc。不要把 Darwin arm64 的 time_os 改回先走 host cc。不要把 Darwin arm64 的 kv_mmap 改回先走 host cc。不要把 Darwin arm64 的 string_fast 改回先走 host cc。不要把 Darwin arm64 的 path_fast 改回先走 host cc。不要把 Darwin arm64 的 sync_os 改回先走 host cc。不要把 Darwin arm64 的 dynlib_os 改回先走 host cc。不要把 Darwin arm64 的 net_workers 改回先走 host cc。不要把 Darwin arm64 的 process_argv 改回先走 host cc。不要给这份对象补文件级存储或构造函数。不要把 Darwin arm64 的 queue_contention 改回先走 host cc。不要把烟测复制进 Darwin 文件。不要把 dlsym 改成函数名当指针。不要把 `q.data[i] = x` 写回去。不要把 Darwin arm64 的 env_os 改回先走 host cc。不要调用 libc getenv、setenv 或 unsetenv。不要直接给 env_os_owned 或临时目录缓存赋值。不要把文件级 let 的地址直接放进调用参数。不要用形参当下标去读环境项。不要用 `&ent[i]` 去取值的地址。不要把 Darwin arm64 的 log_os 改回先走 host cc。不要直接给日志状态槽赋值。不要把文件级 let 的地址直接放进调用参数。不要用形参当下标去读写异步槽。不要把 512 字节的栈数组写回去。不要把 Darwin arm64 的 compress_zlib 改回先走 host cc。不要把版本号 1.2.12 或 z_stream 的 112 字节改成别的平台的值。

闸门：日常三端 L2。暂停 `xlang check`。L4 仅整波另派。

### 维护约定

1. 每波只改仪表盘、前排、流水一行。勾选改 [`C迁移追踪.md`](C迁移追踪.md)，一行一个 ✅。
2. 不写 Class 长文，不写「本日要点」。
3. 日切：新的一天先归档本文为 `自举进度-归档-YYYY-MM-DD.md`，再重写当天本文。
4. 流水是第 4 节。表固定三列：`日期 | 波 | 一行`。描述里不用半角 `|`。

## 0. 一句话

尚未完全自举。钉盘 `ecdb5cc1e`。w1089：Darwin arm64 的 runtime_compress_zlib_glue.o 由共享薄层加 zlib 桥纯 asm 编出。8 字节能压缩再解回原文。编译器本体没有重链，仍是 3972176 `02e6e1a61f9d8e49`。L2 5/5。没有跑 L4。option=102。

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举效率方法-M2主链.md](自举效率方法-M2主链.md) | M2 主链 |
| [C迁移追踪.md](C迁移追踪.md) | 已完成勾选 |
| 本文 | 当天流水 |
| [自举进度-归档-2026-09-26.md](自举进度-归档-2026-09-26.md) | 9 月 26 日 |
| [自举进度-归档-2026-09-25.md](自举进度-归档-2026-09-25.md) | 9 月 25 日 |
| [自举进度-归档-2026-09-24.md](自举进度-归档-2026-09-24.md) | 9 月 24 日 |

## 2. 仪表盘

| 项 | 结果 |
|----|------|
| 钉盘 | `ecdb5cc1e`（不升） |
| Darwin | 3972176，`02e6e1a61f9d8e49`，L2 5/5。arm64 的 runtime_panic.o、runtime_link_abi_user_env.o、runtime_random_fill.o、runtime_time_os.o、runtime_kv_mmap_glue.o 由 .x 纯 asm 编出。string 的 runtime_string_fast 种子、std/path/path.o、runtime_sync_os.o、runtime_dynlib_os.o、runtime_net_workers.o、runtime_process_argv.o、runtime_queue_contention.o、runtime_env_os.o、runtime_log_os.o 和 runtime_compress_zlib_glue.o 也由 .x 纯 asm 编出。`xlang-c` 仍是 4091096 `181f5b58c5a40862` |
| Windows | 4739967，`1120a659cc81979e`，L2 5/5。尾跳 `e9`。pabi 1675145 |
| Ubuntu | 5594304，`1afb107d005e68da`，L2 5/5。`add` 为 `e9` jmp。pabi 2738144 |
| 构建级别 | L2，不跑 `xlang check` |

## 3. 当前前排（≤3）

1. 不跑 g05_prepare。不升钉。不跑 L4。
2. 禁 bak 175735。
3. Darwin 已装入 3972176 `02e6e1a61f9d8e49`。不要用现行编译器重编 dispatch。不要把 w1076 的旧 runtime_link_abi_user_env.o 拷回来。不要把 w1075 的旧 runtime_panic.o 拷回来。不要把 w1064、w1065、w1066、w1067、w1068、w1069、w1070、w1071、w1072、w1073 或 w1074 的旧对象拷回来。不要把 host_is_arm64 改回加载 BSS。不要把 EXPR_VAR（3）或 FIELD（44）从拷贝门拿掉。不要让 9 到 16 字节的字段跳过 deref。不要把 9 到 16 字节的字段赋值改回只存一个 qword。不要清掉 TEXT 末尾的字段赋值洞。不要把大于 16 字节的字段赋值改回只存源地址。不要把大于 16 字节的下标赋值改回只存源地址。不要把 9 到 16 字节的下标赋值改回只写低 8 字节。不要把大于 16 字节的字面量成员改回只存源地址。不要把 9 到 16 字节的字面量成员改回只写低 8 字节。不要把 Darwin arm64 的 random_fill 改回先走 host cc。不要把 Darwin arm64 的 time_os 改回先走 host cc。不要把 Darwin arm64 的 kv_mmap 改回先走 host cc。不要把 Darwin arm64 的 string_fast 改回先走 host cc。不要把 Darwin arm64 的 path_fast 改回先走 host cc。不要把 Darwin arm64 的 sync_os 改回先走 host cc。不要把 Darwin arm64 的 dynlib_os 改回先走 host cc。不要把 Darwin arm64 的 net_workers 改回先走 host cc。不要把 Darwin arm64 的 process_argv 改回先走 host cc。不要给这份对象补文件级存储或构造函数。不要把 Darwin arm64 的 queue_contention 改回先走 host cc。不要把烟测复制进 Darwin 文件。不要把 dlsym 改成函数名当指针。不要把 `q.data[i] = x` 写回去。不要把 Darwin arm64 的 env_os 改回先走 host cc。不要调用 libc getenv、setenv 或 unsetenv。不要直接给 env_os_owned 或临时目录缓存赋值。不要把文件级 let 的地址直接放进调用参数。不要用形参当下标去读环境项。不要用 `&ent[i]` 去取值的地址。不要把 Darwin arm64 的 log_os 改回先走 host cc。不要直接给日志状态槽赋值。不要把文件级 let 的地址直接放进调用参数。不要用形参当下标去读写异步槽。不要把 512 字节的栈数组写回去。不要把 Darwin arm64 的 compress_zlib 改回先走 host cc。不要把版本号 1.2.12 或 z_stream 的 112 字节改成别的平台的值。

## 4. 波次流水（倒序 · 9 月 27 日）

| 日期 | 波 | 一行 |
|------|----|------|
| 2026-09-27 | w1089 | Darwin arm64 的 runtime_compress_zlib_glue.o 由 runtime_compress_zlib_glue.x 与 runtime_compress_zlib_glue_darwin.x 纯 asm 编出，再 ld -r，冷路径不再先把这份种子交给 host cc。对象只链进用户程序，不在编译器本体里。公开包装仍在共享薄层。Darwin 文件只把 deflateInit2 和 inflateInit2 转到 deflateInit2_ 与 inflateInit2_，并带上本机 SDK libz 测得的版本号 1.2.12 和 z_stream 的 112 字节。8 字节原文压缩后能解回同样的 8 字节。Linux 和 Windows 仍编各自的种子，由那份种子的 zlib.h 提供版本号和结构大小。纯 asm 失败且对象缺失才退回 C 种子。编译器本体没有重链。L2 5/5。产品 3972176 02e6e1a61f9d8e49。没有跑 L4。 |
| 2026-09-27 | w1088 | Darwin arm64 的 runtime_log_os.o 由 runtime_log_os.x 与 runtime_log_os_darwin.x 纯 asm 编出，再 ld -r，冷路径不再先把这份种子交给 host cc。对象只链进用户程序，不在编译器本体里。文件级槽不能直接赋值，地址先放进局部变量再用 memcpy 写入。初次调用把文件描述符写成 -1、掩码写成标准错误。打开文件走 ___open，追加标志是 0x209，截断标志是 0x601，权限是 0644。st_size 在 stat 的偏移 96。轮转名字是路径加一个点和一位数字。异步队列是两块堆缓冲。形参当下标会多解引用，下标先抄进局部变量。log_emit_bytes_c 的 null 与 or 写法在现行编译器里不发射，改成等于 0 与或。写入 XLANG_LOG_MIN_LEVEL=1 后级别是 1，再改环境不会覆盖。级别 9 和 -1 被忽略。追加写入 hello 后再写 X，文件是 helloX。阈值 10、备份 2 时三次写入分别落在 .2、.1 和当前文件。备份 0 时截断后只留下新内容。异步启用后发出的字节要等 flush 才进文件。长度 513、空路径、备份 9、空指针分别返回 -1。掩码为 0 时不同步写入文件。标准错误写入返回 0。Linux 仍编 POSIX 种子，Windows 仍编 Win32 种子。纯 asm 失败且对象缺失才退回 C 种子。编译器本体没有重链。L2 5/5。产品 3972176 02e6e1a61f9d8e49。没有跑 L4。 |
| 2026-09-27 | w1087 | Darwin arm64 的 runtime_env_os.o 由 runtime_env_os.x 与 runtime_env_os_darwin.x 纯 asm 编出，再 ld -r，冷路径不再先把这份种子交给 host cc。对象只链进用户程序，不在编译器本体里。读取走 link_abi_getenv，不另写一套扫描，也不调用 libc getenv。setenv 和 unsetenv 改 _NSGetEnviron 指向的环境块，不调用 libc setenv 或 unsetenv。追加时新分配向量，只释放本翻译单元自己分配过的旧向量。文件级 owned 和临时目录缓存不能直接赋值，地址先放进局部变量再用 memcpy 写入。形参当下标去读环境项会把条目的第一个字当成指针，所以下标先抄进局部变量。取 &ent[i] 不是那一字节的地址，值按字节下标拷贝。空名字和带等号的名字 setenv 返回 -1。覆盖为 0 时保留旧值。空值能读回长度 0。删除后 exists 返回 0。临时目录两次长度相同且以斜杠开头。容量为 1 时返回 -1。Linux 仍编 POSIX 种子，Windows 仍编 Win32 种子。纯 asm 失败且对象缺失才退回 C 种子。编译器本体没有重链。L2 5/5。产品 3972176 02e6e1a61f9d8e49。没有跑 L4。 |
| 2026-09-27 | w1086 | Darwin arm64 的 runtime_queue_contention.o 由 runtime_queue_contention.x 与 runtime_queue_contention_darwin.x 纯 asm 编出，再 ld -r，冷路径不再先把这份种子交给 host cc。对象只链进用户程序，不在编译器本体里。烟测留在共享薄层，Darwin 文件只提供五个 _impl。互斥锁是 64 字节 pthread_mutex_t。两个工人用 pthread_create，入口用 dlsym 取 queue_os_worker_trampoline，因为函数名当指针时现行编译器退出 139。q.data 下标存储会多解引用一次，只当目的地的本地指针又写不进槽，所以元素写入改走 queue_smoke_store_i32。空互斥锁的锁、解锁和销毁是空操作。单线程压入 500 次后长度是 500。两个线程各压入 500 次后烟测返回 0。默认 pthread 栈，不用 C 种子里的 65536 属性。Linux 仍编 futex 种子，Windows 仍编 Win32 种子。纯 asm 失败且对象缺失才退回 C 种子。编译器本体没有重链。L2 5/5。产品 3972176 02e6e1a61f9d8e49。没有跑 L4。 |
| | 更早 | 见 [9 月 26 日归档](自举进度-归档-2026-09-26.md)。 |
