# Xlang / X language 自举进度

> **更新**：2026-07-24 · 23 号全日已归档；24 号起重新起表 · **Xlang 重命名 R0–R3 已闭** · **双端 tip L4 钉盘 `75c638465`** · **wave346 Cap residual pure 已闭**\
> **基线 SHA（产品 L4 放行钉盘）**：**`75c638465`**（升自 `3698e557f` / `a77fc5d90`；wave288 invoke_cc companion-push user_env；双端 tip L4 真冷 + 产品轨 **129** 全绿）\
> **产品 bstrict 套件数**：**129**\
> **产品代码 tip**：**wave346** @ **`fc81d397e`**（前 tip wave345=`199260fa0`；钉盘 **`75c638465`**）\
> **git 锚点（重命名前）**：tag **`anchor-pre-xlang-rename`**（考古；开工前 L4 父钉 = `a77fc5d90`）\
> **现行工程**：品牌/CLI/env/ABI 已 **xlang**（主域 **`xlang.dev`** · 仓库 **`github.com/shuliangfu/x-lang`**）；分析文 [xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)。自举已恢复；**wave346** 闭 fixed TYPE_ARRAY `.length`。\
> **本波（24 日）状态**：wave295–346 已闭；钉盘 **`75c638465`**；日常 L2（不升钉）。\
> **方法**：[自举方法.md](自举方法.md)（Cap/R/L/M；工程 ≠ 产品放行）\
> **Windows（删种子前）**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[自举进度-归档-2026-07-23.md](自举进度-归档-2026-07-23.md) · [22](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md) · [19](自举进度-归档-2026-07-19.md) · [18](自举进度-归档-2026-07-18.md) · [17](自举进度-归档-2026-07-17.md) · [16](自举进度-归档-2026-07-16.md) · [14](自举进度-归档-2026-07-14.md) · [13](自举进度-归档-2026-07-13.md)

### 维护约定

1. 每完成一波 **必改两文**：本文（KPI/前排/§6）+ [`当前进度.md`](当前进度.md)。
2. **禁止**把 prove/Stage2/N++ 单独写成「可自举」。
3. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**；历史钉盘 125）。
4. 工程 KPI 写 **SHA + 日志路径**；过期整日流水移归档。
5. 大债地图默认写 [`当前问题分析.md`](当前问题分析.md)（承接原 `问题分析文档.md`）；非每微步。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 =** **`75c638465`（双端 tip L4 真冷 + 129；wave288 invoke_cc user_env companion；升自 `3698e557f`）。**\
**产品代码 tip =** **`fc81d397e`**（wave346；钉盘仍 **`75c638465`**）。\
**git 锚点（考古）=** tag **`anchor-pre-xlang-rename`**。\
**现行主线 =** **Cap residual / wave347**（品牌已 xlang · CLI=`xlang_asm` · ABI=`xlang_*` · 主域 **`xlang.dev`** · 仓库 **`x-lang`**）。重命名分析：[xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md)。

| 轨                | 状态                                                                                           |
| ---------------- | -------------------------------------------------------------------------------------------- |
| 工程轨              | 重命名 R0–R3 已闭；**wave346** 已闭；下一波 **wave347** Cap residual |
| 产品轨 tip / 钉盘     | tip **`fc81d397e`** · 钉盘 **`75c638465`**（双端 tip L4 + 129） |
| mac L4 真冷        | ✅ remaining_o=0 · **OK (129)** · `/tmp/mac_true_cold_75c638465.log` · `/tmp/mac_true_bstrict_75c638465.log` |
| Ubuntu L4（金标）    | ✅ remaining_o=0 · **OK (129)** · `/tmp/ubuntu_true_cold_75c638465.log` · `/tmp/ubuntu_true_bstrict_75c638465.log` |
| 前钉 L4            | ✅ `3698e557f` · `a77fc5d90` · `d6648bee4` · `3c88f7e51` · `df1dde5b6` · `07684b813` 等 |
| 重命名前锚点           | tag **`anchor-pre-xlang-rename`** · 分析文 [xlang-项目重命名重构分析.md](xlang-项目重命名重构分析.md) |

***

## 1. 文档地图

| 文档                                             | 看什么                   |
| ---------------------------------------------- | --------------------- |
| [自举方法.md](自举方法.md)                             | Cap / R / L / M       |
| [自举步骤.md](自举步骤.md)                             | 可执行闸门                 |
| [自举验证.md](自举验证.md)                             | 怎么测                   |
| **本文**                                         | 长期 KPI + 前排（**每波必改**） |
| [当前进度.md](当前进度.md)                             | 当天快照（**每波必改**）        |
| **[当前问题分析.md](当前问题分析.md)**                     | tip 级产品债地图（21 日起权威）   |
| [自举进度-归档-2026-07-23.md](自举进度-归档-2026-07-23.md) | **23 号全日**（只读）        |
| skill `xlang-selfhost-product-gate`             | L4 / 双端 / 假绿禁止        |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项                          | 结果                                                                                  | 日志 / 备注                                      |
| -------------------------- | ----------------------------------------------------------------------------------- | -------------------------------------------- |
| 产品 bstrict 套件数             | **129**                                                                             | 现行白名单                                        |
| 产品 L4 放行钉盘                 | **`75c638465`**                                                                      | 23 日日终 L4 兜底升钉（前钉 `3698e557f`；wave288 user_env）；双端 L4 129      |
| 本波 tip                     | wave346：fixed TYPE_ARRAY `.length` compile-time N @ **`fc81d397e`** | **构建级别=L2**（钉盘仍 **`75c638465`**） |
| mac 产品链 bstrict（钉盘 L4） | ✅ **OK (129)**                                                                      | L4 · `/tmp/mac_true_bstrict_75c638465.log` · cold `/tmp/mac_true_cold_75c638465.log`    |
| Ubuntu L4（金标 · 钉盘）  | ✅ **OK (129)**                                                                      | L4 · `/tmp/ubuntu_true_bstrict_75c638465.log` · cold `/tmp/ubuntu_true_cold_75c638465.log` |
| 产品矩阵（双端 tip L4）           | ✅ rv42 / option / si / hello / process（bstrict）                                                                | mac + Ubuntu §4 · `75c638465`                                      |
| 前钉 mac/Ubuntu L4           | ✅ 129 @ `3698e557f` · `a77fc5d90` · `d6648bee4` · `3c88f7e51` · `df1dde5b6` · `07684b813` 等                                                                | 见 [23 号归档](自举进度-归档-2026-07-23.md) / 更早归档                             |

### 2.2 工程轨（继承）

| KPI           | 现                                        | 备注                   |
| ------------- | ---------------------------------------- | -------------------- |
| **T / EMPTY** | 18/18                                    | 18 号结案               |
| **N**         | 54/54 IDENTICAL                          | 回归基线                 |
| **R2 / 根修本波** | **wave346** fixed TYPE_ARRAY `.length`；下波 **wave347** | tip **`fc81d397e`** · 钉盘 **`75c638465`** |

***

## 3. 当前前排（≤3 条）

1. **wave347 Cap residual**（**wave346 闭**：fixed TYPE_ARRAY/VECTOR `.length` → compile-time N（typeck usize + host `((size_t)N)` + fs imm）。leave-off：AST 升布局；`*u8+*u8`；string/str；host residual；legacy if_then；riscv64；f64→f32 仍须 `as`；soft：untyped-let；reentrancy last-wins；`while`/for body-let XP003；usize→i32 let 须 `as`；struct 内 slice escape）
2. **leave-off** freestanding/host `_impl`（getenv/system/realpath_cap 体 / pool 结构）— 非本微步真迁目标
3. **日常 L2**；日终 / 整波再 tip L4（钉盘 **`75c638465`**）

***

## 4. 与 23 号归档的关系

- 23 号全日（Cap residual pure wave164–294、lexer L001–L012、float cast/arith 多叶、日终 tip L4 升钉 **`75c638465`**）已整表移入 [`自举进度-归档-2026-07-23.md`](自举进度-归档-2026-07-23.md)。
- **勿**用归档 tip（如 wave294 `ddd24de58`）或旧钉（`3698e557f` / `a77fc5d90` / `3c88f7e51` 等）冒充现行 tip L4（产品钉盘仍 **`75c638465`**；代码 tip **`3aeb3343d`**）。
- 24 日新工作只记本文 §0/§2/§3/§6。

***

## 5. 双端 / 平台

| 平台                     | 本波状态                                                              |
| ---------------------- | ----------------------------------------------------------------- |
| **macOS arm64**        | ✅ tip L4 全擦 + 矩阵 + **129** @ **`75c638465`** |
| **Ubuntu x86\_64（金标）** | ✅ tip L4 全擦 + 矩阵 + **129** @ **`75c638465`** |
| **Windows MSYS2**      | 继承 hybrid；本波未重跑（**不需要切换**）；路径文档已 `…\xlang\x-lang`                                             |

***

## 6. 变更记录（倒序 · 24 日起；23 号见归档）

| 日期         | SHA                                                     | 摘要                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------- | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-24 | **`fc81d397e`** / docs | **R2 Cap residual pure wave346**：**fixed TYPE_ARRAY `.length` compile-time N**。根因：typeck `pipeline_typeck_field_slice_c` 仅 TYPE_SLICE → fixed `a.length` 未 stamp；host 发 `a.length`（C 数组无 member）cc 红；fs offset0 load 首元（run=10≠3）。权威：G.7 补全 field_slice ARRAY/VECTOR `.length`→usize；host/seed `((size_t)N)`；fs `movabs $N`。探针 Ubuntu fs alen=3 · u64=4 · idx=23 · slen/re_len/mid 回归 · host-C=3 · disasm **movabs $0x3**；mac host 同。soft：untyped-let；reentrancy；while/for body-let XP003；usize→i32 let 须 `as`；struct escape。矩阵 rv/option/hello。mac/Ubuntu L2（`/tmp/ubuntu_g05_wave346.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck+emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`199260fa0`** / `4897aeb23` / `5cb2c9fd1` / docs | **R2 Cap residual pure wave345**：**TYPE_SLICE param return + call rvalue**。根因：① host `return s` 形参为 `slice*`、返回 by-value → host-cc 类型错 ② `pass(take())` 发 `&(take())`；栈 temp 在 gcc -O2 下死（Ubuntu SIGSEGV；-O0/mac 藏）③ fs CALL 只留 data@rax 当 fat*。权威：G.7 host `return *s`（复用 `field_access_base_is_pointer_param`）+ CALL/METHOD `({ static T __xlang_sp; …; &__xlang_sp; })`；fs dual-GP→fat home+lea（ARRAY_LIT call-arg 同源）。探针 Ubuntu fs/host param/rpe/esc/nested/mid=60 · re_len=240；mac 同；disasm **store rax+rdx + lea**。soft：untyped-let；reentrancy；fixed `.length`；while length；struct escape。矩阵 rv/option/hello。mac/Ubuntu L2（`/tmp/ubuntu_g05_wave345.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED host+fs · **日常 L2 非 tip L4** |
| 2026-07-24 | **`b9d3d2549`** / docs | **R2 Cap residual pure wave344**：**fixed→slice return escape runtime `s.length`**。根因：wave342/343 escape 用 compile-time N（host `memcpy(sizeof N)+length=N`；fs 自 fixed `a` 栈槽 copy）→ `s=[40,50]` 后仍 length=3（340）；fs reassign 内容仍可能读 a。权威：G.7 补全 escape emit — host `min(s.length,N)` memcpy + length=`__xlang_esc_n`；fs 自 **s dual-GP** data 早退 copy + length@rdx。探针 Ubuntu fs/host re_len=240 · view_re=240 · re2=150 · nested/flat=60 · same_fn=80（reentrancy leave-off）；mac 同。soft：untyped-let；reentrancy last-wins。矩阵 rv/option/hello。mac/Ubuntu L2（`/tmp/ubuntu_g05_wave344.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED host+fs · **日常 L2 非 tip L4** |
| 2026-07-24 | **`859a4922e`** / docs | **R2 Cap residual pure wave343**：**nested-block fixed→slice return durable escape**。根因：wave342 只扫函数体顶层 let → 嵌套 `{ let a:T[N]; let s:T[]=a; return s }` 漏 escape（host/fs run=1 vs 60）。权威：G.7 `pipeline_find_fixed_array_slice_escape` DFS（if/while/for/region + EXPR_BLOCK）+ init VAR resolved TYPE_ARRAY；host/fs 共用 finder；fs COMMON copy / host `__xlang_esc`+memcpy 不变。探针 Ubuntu nested/flat/if/nc=60 · view=149；mac 同；C 见 `__xlang_esc`。soft：untyped-let；reassign；reentrancy last-wins。矩阵 rv/option/hello。mac/Ubuntu L2（`/tmp/ubuntu_g05_wave343.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED host+fs · **日常 L2 非 tip L4** |
| 2026-07-24 | **`ca6bdb54d`** / `cdebd1129` / docs | **R2 Cap residual pure wave342**：**fixed TYPE_ARRAY→TYPE_SLICE return durable escape**。根因：`let s:T[]=a`（fixed）为栈 view；`return s` dual-GP 悬垂（host/fs run=1 vs 60）。邻域：fs `vector_let_init` 非 const 元冲 rbx → Ubuntu `let a:[3]i32=[n,n+10,n+20]` SIGSEGV（host 藏）。权威：G.7 ① host return `static E __xlang_esc[N]`+memcpy(s.data)（codegen.x+seed）② fs `glue_try_return_slice_escape_from_fixed_array` SHN_COMMON 自 fixed 槽 copy ③ `vector_let_init` 复用 wave340 may_clobber re-lea。探针 Ubuntu fs const/nc ret=60 · nc_local=60 · view=99 · ret_var_nc/lit=60；mac host 同；disasm **push+lea**。soft：untyped-let；nested-block escape。矩阵 rv/option/hello。mac/Ubuntu L2（`/tmp/ubuntu_g05_wave342b.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED host+fs emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`640491a6a`** / docs | **R2 Cap residual pure wave341**：**non-const ARRAY_LIT→TYPE_SLICE durable return**。根因：wave340 非 const 用 block compound `(E[]){n,…}` / fs 栈 payload → return 后悬垂（length OK；idx 垃圾；Ubuntu+host `return [n,n+10,n+20]` run=1）。权威：G.7 ① host `static E __xlang_al[N]` + runtime stores（codegen.x+seed）② fs `glue_asm_emit_array_lit_durable_ptr_rax` 非 const → SHN_COMMON BSS + emit stores（复用 modlet COMMON face；const 仍 text-embed）。探针 Ubuntu fs/host nc_ret=60 · ret_nc_local=60 · lit=60 · idx0=10 · idx_direct=60；mac 同。soft：untyped-let；non-ARRAY_LIT 拥有缓冲逃逸。矩阵 rv/option/hello。mac/Ubuntu L2（`/tmp/ubuntu_g05_wave341.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED host+fs emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`d2dba30d3`** / docs | **R2 Cap residual pure wave340**：**non-const ARRAY_LIT**。根因：① fs `pipeline_asm_emit_array_lit` payload base 仅 rbx；binop 元 `n+10` 写 ebx 冲 base → store 写垃圾（Ubuntu `let a=[n,n+10,n+20]` SIGSEGV；`[n,20,30]` 仍绿）② host wave335 一律 `static __xlang_al` → 非 const C 拒编。权威：G.7 ① may_clobber 后 push/re-lea temp_base→rbx/pop/store（dual-slot 对齐 wave338）② host 仅 LIT/BOOL→static，else `(slice){.data=(E[]){…},.length=N}`（codegen.x+seed 同 commit）。探针 Ubuntu nc_local=60 · idx=10 · mix/const/assign；host-C nc/const=60；disasm **push+lea restore**。soft：跨函数 return 非 const/栈 slice 寿命；untyped-let。矩阵 rv/option/hello。mac/Ubuntu L2（`/tmp/ubuntu_g05_wave340.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED freestanding+host emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`38cea6d0f`** / docs | **R2 Cap residual pure wave339**：**return-VAR TYPE_SLICE 栈寿命**（const ARRAY_LIT let-init durable）。根因：host `let a:i32[]=[const]` 已 `static __xlang_al`；fs 仅栈 emit_array_lit → `return a` dual-GP data 悬垂；Ubuntu trash clobber expect 60 got 84。权威：G.7 `glue_emit_slice_from_array_let_init` 优先 `glue_asm_emit_array_lit_durable_ptr_rax_elf_c`（wave335 return 同源）；非 const 栈 fallback；durable 跳过 next_offset bump。探针 Ubuntu clob=60 · ret_var idx=20 · lit/let/empty/u64/slice_let/take_once；disasm take **jmp-skip payload + lea [rip]**。矩阵 rv/option/hello。mac/Ubuntu L2（`/tmp/ubuntu_g05_wave339.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED freestanding emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`826b72c70`** / docs | **R2 Cap residual pure wave338**：**fs binop dual-slot `n*100+v` clobber**。根因：commutative add 先装 right VAR→rbx；嵌套 mul `mov $100,%ebx` 未 invalidate slot-cache → 仍假称 rbx=v → Ubuntu run=200（expect 120；arm64/host 藏）。权威：G.7 ① left-assoc add/mul/&/\|/^：right VAR + left may clobber rbx → 先 emit left 再装 right（跨 op，非仅同 op 链）；② 全 imm→rbx lit 路径 `invalidate_rbx`（mul/sub/and/bitwise/div/mod/shift）。探针 Ubuntu fs 120 · chain/addmul/sub/take_once/nested/or/muladd2；disasm imul 后 **reload** v。矩阵 rv/option/hello。mac/Ubuntu L2（`/tmp/ubuntu_g05_wave338.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED freestanding emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`15772d175`** / docs | **R2 Cap residual pure wave337**：**call-as-INDEX 单次 materialize**。根因：bounds（length）与 load（data）各 emit 一次 CALL → `take()[1]` 双求值。权威：G.7 `glue_try_index_rvalue_slice_once` CALL/METHOD TYPE_SLICE dual-GP/fat* → dual-GP temp 一次 materialize，bounds+addr 只 load。探针 Ubuntu fs `take()[1]`=20 · oob=134 · let=20 · take012=60 · side-effect n=1/v=20/if→120；disasm main **单次** `call take`+store rax/rdx；host once=120。soft：untyped-let / 非 durable return VAR / fs binop dual-slot `n*100+v` clobber。矩阵 rv/option/hello。mac/Ubuntu L2（`/tmp/ubuntu_g05_wave337.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED freestanding emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`fd6d5d84f`** / docs | **R2 Cap residual pure wave336**：**`take()[1]` call-as-INDEX** freestanding bounds. 根因：`glue_emit_slice_length_to_rbx` 非 VAR 把 CALL TYPE_SLICE 当 fat*（`rax+8`）且 clobber index@rax → length 垃圾 panic 134（host-C 已绿）。权威：G.7 CALL/METHOD_CALL + TYPE_SLICE → length@rdx（SysV dual-GP · wave333/335）；push/pop index；指针 fat* 仍 +8。探针 Ubuntu fs `[0/1/2]`=10/20/30 · oob=134 · let-bind/return-var lit=20；host=20；disasm `push;call;mov %rdx,%rax`. soft：双求值 call base / untyped-let。矩阵 rv/option/hello。mac/Ubuntu L2（`/tmp/ubuntu_g05_wave336.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED freestanding emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`7090e1abd`** / `b382d96c2` / `83e88d4a1` / docs | **R2 Cap residual pure wave335**：**return ARRAY_LIT→TYPE_SLICE durable** 杀 INDEX 悬垂。根因：① host `(E[]){…}` 自动存储 return 后悬空；② fs 栈 temp 同；③ `struct16_ret_needs_rax_deref` size>8 把 dual-GP 当指针 → length 垃圾 30/SIGSEGV。权威：G.7 host `({ static E __xlang_al[]=…; slice{.data=__xlang_al,.length=N} })`（codegen.x+seed）；fs jmp-skip text-embed（string lit 族）+ dual-GP；TYPE_SLICE needs_rax_deref=0；pack force_esz 自 slice elem（u64[]）。探针 Ubuntu fs lit idx=20 · len=3 · empty=0 · u64=3 · var-bind idx=20；host idx=20；fa/slice-let 回归。soft：`take()[1]` call-as-INDEX；return VAR 栈寿命；untyped-let；fs if。矩阵 rv/option/hello。mac/Ubuntu L2（`/tmp/ubuntu_g05_wave335c.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED host+fs emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`27dbb36e9`** / `6148dd577` / docs | **R2 Cap residual pure wave334**：**fixed TYPE_ARRAY whole-array assign** `a = [4,5,6]` / `a = b`。根因：host C `a = (T[]){…}` not assignable；fs emit_array_lit 后 store_rax 写穿数组首 8B。权威：G.7 host EXPR_ASSIGN→memcpy（codegen.x+seed）；fs ARRAY_LIT 复用 vector_let_init；VAR load `slot−i·esz` + store `+i·esz`（wave334b）。探针 Ubuntu fs lit/var=15 · elem/u64/slice 回归；host-C memcpy；disasm 无指针写槽。soft：return INDEX 悬垂 / untyped-let / fs if 真支跳过 return 表达式（host 绿）。矩阵 rv/option/hello。mac/Ubuntu L2（`/tmp/ubuntu_g05_wave334b.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED host+fs emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`3d7f5c3e7`** / docs | **R2 Cap residual pure wave333**：**return** `function f(): i32[] { return [1,2,3] }` ARRAY_LIT→TYPE_SLICE + freestanding dual-GP。根因：return 只 stamp TYPE_ARRAY/VECTOR → found `?`；fs 仅 data@rax。权威：G.7 `check_expr_return` 复用 `array_vector_lit`；return emit data@rax length@rdx；seed typeck 同 commit。探针 Ubuntu fs len=3 · empty=0 · var-ret len=3 · call-arg/let/assign 回归；host-C compound；INDEX 悬垂 soft（同 `return a`，SIGSEGV 139）。矩阵 rv/option/hello。soft：悬垂 index / untyped-let / fixed-array assign。mac/Ubuntu L2（`/tmp/ubuntu_g05_wave333.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck+emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`c1d2880ac`** / `02c38ffc4`… / docs | **R2 Cap residual pure wave332**：**call-arg** `sum([1,2,3])` ARRAY_LIT→TYPE_SLICE + freestanding slice*。根因：① resolve 前 check_expr 不 stamp → host `(uint8_t[])` vs `slice*`；② 缺 dual-GP temp；③ 形参当 dual-GP 值；④ length 经 rax 冲 index；⑤ INDEX 不 chase `.data`；⑥ payload 向高地址叠 fat（`home+8` 不够）。权威：G.7 `check_call_slice_region` 复用 `array_vector_lit`；call-arg dual-GP+lea；`needs_ptr_load` TYPE_SLICE；length push/pop；INDEX load fat.data；`next=home+payload_bytes`。探针 Ubuntu fs sum lit/var=6 · at2=3 · len=3 · empty=0 · only_empty=12；host `&((struct){…})`。矩阵 rv/option/hello。soft：untyped-let / return lit→slice / fixed-array assign。mac/Ubuntu L2（`/tmp/ubuntu_g05_wave332f.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck+emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`d2303fbe1`** / `09a1c0dc3` / docs | **R2 Cap residual pure wave331**：**slice assign** `a = []` / `a = [1,2]` + freestanding dual-GP。根因：① 产品 `pipeline_typeck_check_expr_assign_c` 无 ARRAY_LIT→TYPE_SLICE coerce（let-init wave328 已有）→ found `?`；② VAR assign 只 store_rax 丢 length@off-8（`a=b` 旧 length）；③ empty 后 next_offset 压 fat home → assign lit payload 写穿。权威：G.7 复用 `coerce_init_array_vector_lit` + typeck.x/seed TYPE_SLICE；emit dual-GP + temp past off+8 + let-init bump。探针 Ubuntu empty=0 · lit=6 · var=2 · only_empty=11 · u64/if/let 回归；host-C 同；disasm payload@-0x28。矩阵 rv/option/hello。soft：untyped-let。mac/Ubuntu L2（`/tmp/mac_g05_wave331c.log` · `/tmp/ubuntu_g05_wave331b.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck+emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`777a0002c`** / docs | **R2 Cap residual pure wave330**：freestanding **empty slice** `let a: i32[] = []`。根因：`glue_init_is_empty_array_lit` 在 block_inits / body_sync / stmt_order_let_const 整段跳过 store → TYPE_SLICE 只靠 prologue 清零（host-C 已写 `{.data=(T[]){},.length=0}`）。权威：G.7 empty TYPE_SLICE 路由既有 `glue_emit_slice_from_array_let_init_elf_c`（wave329 ARRAY_LIT n=0：lea temp + store data + mov length=0）。探针 Ubuntu fs empty=0 · take=0 · empty+lit=2 · lit/idx/var/u64/if；disasm `movabs $0`+store length；host-C 同。矩阵 rv/option/hello。soft：untyped-let；assign empty `a=[]` typeck `?`。mac/Ubuntu L2（`/tmp/mac_g05_wave330.log` · `/tmp/ubuntu_g05_wave330.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED freestanding emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`aa38bd724`** / docs | **R2 Cap residual pure wave329**：freestanding **slice array-lit** `let a: i32[] = [1,2,3]`。根因：① `glue_emit_slice_from_array_let_init_elf_c` 仅 VAR → ARRAY_LIT 只 store data 不写 dual-GP length（run=0 / index panic）；② `glue_binop_operand_index_addr_clobbers_rbx_elf_c` 把 lit 当下不 clobber，但 slice bounds 仍用 rbx → dual-slot sum 24。权威：G.7 扩 ARRAY_LIT 写 `{data,length}`+bump；slice base classifier 恒 true。探针 Ubuntu fs len=3 · idx=42 · fixed/var/var-idx · u64[]=4 · empty=0；host-C 同。矩阵 rv/option/hello。soft：untyped-let；empty 主动写 length=0。mac/Ubuntu L2（`/tmp/mac_g05_wave329b.log` · `/tmp/ubuntu_g05_wave329b.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED freestanding emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`725497942`** / docs | **R2 Cap residual pure wave328**：**slice array-lit host C** `let a: i32[] = [1,2,3]`。根因：typeck 仅 stamp ARRAY_LIT→TYPE_ARRAY → host emit `(uint8_t[]){…}`；slice compound 尾多 `)`（is_slice 原死路径）。权威：G.7 `typeck_coerce_array_lit_elem_types_to_decl`/`array_vector_lit` 接受 TYPE_SLICE + glue C 镜像 + codegen 去尾 `)`；seed 同 commit。探针 mac host len=3 · idx=42 · fixed/var-slice 回归 · u64[]=4；Ubuntu **`-backend c`** 同；Ubuntu 默认 asm freestanding array-lit→slice **soft residual run=0**。矩阵 rv/option/hello。soft：untyped-let；freestanding array-lit→slice。mac/Ubuntu L2（`/tmp/mac_g05_wave328b.log` · `/tmp/ubuntu_g05_wave328d.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck+host emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`8db782383`** / docs | **R2 Cap residual pure wave327**：**match multi-pattern `1 \| 2 =>`**。根因：`parser_asm_parse_match_into_c` 单 pattern 后只认 FATARROW，`TOKEN_PIPE` → 静默 XP003（docs/04 已写多模式）。权威：G.7 pattern 环收 PIPE 列表（cap 16）→ 共用 result 对各 lit/enum `append_match_arm`（复用 wave326 nested ternary / freestanding match）。探针双端 v=1/2→40 · v=3→42 · v=9→7 · `1\|2\|3=>99` · C `(v==1?(40):(v==2?(40):(v==3?(42):7)))` · baseline 分臂 · 矩阵 rv/option/hello。soft：untyped-let（docs 禁推断）；array `i32[]` host 类型可偏。mac/Ubuntu L2（`/tmp/mac_w327_build.log` · `/tmp/ubuntu_g05_wave327.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-24 | **`93f829b94`** / docs | **R2 Cap residual pure wave326**：**host-C EXPR_MATCH multi-arm nested ternary**。根因：host `emit_expr` EXPR_MATCH 仅 `pipeline_expr_match_arm_result_ref(...,0)` → `match v {1=>40;2=>42;_=>7}` 恒 40（mac host-gcc；Ubuntu freestanding `pipeline_asm_emit_match_elf_c` 已绿）。权威：G.7 `codegen_emit_match_from_arm` 嵌套三元（`matched==lit?(res):(rest)`；wildcard 收链；enum variant_index）+ seed `codegen_gen` 同 commit。探针双端 host/fs pick(1/2/3)=40/42/7 · C `(v==1?(40):(v==2?(42):7))` · store 回归 · 矩阵 rv/option/hello。soft：untyped-let（docs 禁推断）；`1\|2=>` 多模式臂；array `i32[]` host 类型可偏。mac/Ubuntu L2（`/tmp/mac_g05_wave326.log` · `/tmp/ubuntu_g05_wave326.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED host-C emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`9bfec4c05`** / docs | **R2 Cap residual pure wave325**：**body-let init unary `TOKEN_STAR`（`let v:T=*p`）**。根因：`parse_body_lets_into` unary 前缀仅 `-`/`!`/`(`/`~`，缺 STAR → bare `let v:i32=*p` init_ref=0 → body-let parse false → num_funcs=0 / XP003 out_len=0；`(*p)` / `(*p) as T` 经 LPAREN 可绿。权威：G.7 补全既有 unary 分支 + seed/parser_gen kind ord **98** 同 commit。探针 mac/Ubuntu let/u64/asg/ret；Ubuntu freestanding let=42；矩阵 rv/option/hello。soft residual：bare expr-stmt `*p=` 仍可掉 main（非本叶）。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave325.log` · `/tmp/ubuntu_g05_wave325.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-24 | **`6c8cc9516`** / docs | **R2 Cap residual pure wave324**：**freestanding EXPR_DEREF `*p=` store**。根因：pure-asm assign 仅 VAR/FIELD/INDEX → DEREF 左值 `return -1` → Ubuntu freestanding CG002（无 main）；mac host-gcc C `*(p)=` 藏。权威：G.7 `lvalue_eff_addr` ko=52（指针操作数、不 load）+ assign dual-slot `store_rax_to_rbx_indirect`；宽与 wave323 同源 type→byte_sz。探针 Ubuntu store/param/u64/`*p=*p+2`/load 回归；disasm `mov %eax,(%rbx)`。soft residual：`let v=*p` 仍可掉 main。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave324.log` · `/tmp/ubuntu_g05_wave324.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`9884888f4`** / docs | **R2 Cap residual pure wave323**：**freestanding EXPR_DEREF `*p` load**。根因：pure-asm rec 有 ADDR_OF(ko=51) 无 DEREF(ko=52) → `backend_emit_expr_elf_slow` 弱/空 → Ubuntu freestanding 用指针位当值（`*p+2` 垃圾 exit；param `return *p` 同）。权威：G.7 `pipeline_asm_emit_deref_elf_c`（emit 操作数 + `load_*_from_rax` 按结果宽；复用 INDEX 编码器）+ rec ko=52 + binop dual-slot。探针 Ubuntu local/param/asg/u64 + rem/if 回归；disasm `mov (%rax),%eax`。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave323.log` · `/tmp/ubuntu_g05_wave323.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`30c35eac7`** / docs | **R2 Cap residual pure wave322**：**freestanding unsigned rem**（u32/u64/usize `%`）。根因：产品 thin `backend_enc_rem_mod_unsigned_arch` 直发 `divl %ebx` 未 `xor edx` → Ubuntu freestanding **#DE / SIGFPE exit 136**（有符号 rem 有 cltd；unsigned div 经 `backend_enc_div_rbx_arch` 已 xor）。权威：G.7 补全 thin rem_mod_unsigned（xor+div+mov_edx_eax）+ seed thin/surface 同 commit；full.x 已转调 div_rbx_arch。探针双端 i32/u32/u64/usize rem=1 · remasg · remlit · u64div · wave321 iflet；Ubuntu disasm `xor %edx; div %ebx; mov %edx,%eax`。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave322.log` · `/tmp/ubuntu_g05_wave322.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED arith / LINUX+MACOS x86_64 emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`ef1066ce9`** / docs | **R2 Cap residual pure wave321**：**freestanding if/while/for 后 let 语句序**。根因：wave320 仅拦 kind=2；pass0 仍把 `let b=a` 提到 if/while 前 → Ubuntu `if(1){a=6;} let b=a` / while / for / chain / else **run=0**（mac host 可藏）。权威：G.7 扩 `glue_block_compute_pass1_deferred_lets`（读栈 init + 前序 kind 2–6 → pass1）+ 抽出 `glue_block_defer_lets_transitive` 并在屏障后再跑。探针双端 stmt2/if/while/for/chain/else=42；Ubuntu disasm `a=0; if a=6; b=a`。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave321.log` · `/tmp/ubuntu_g05_wave321.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`123752880`** / docs | **R2 Cap residual pure wave320**：**freestanding let-after-assign 语句序**。根因：`pipeline_asm_emit_block_body_sync_elf` pass0 提升纯 let 越过 kind=2 assign → `let a=0; a=6; let b=a` 发射 `b=a; a=6`（Ubuntu run=0；mac host 可藏）。权威：G.7 扩 `glue_block_compute_pass1_deferred_lets`（init 读栈槽 + 前序 expr_stmt → pass1 原位；仍允许跨 if/while 提升）。探针双端 p2/p3/p5/p6/p8/neg；Ubuntu disasm `a=0; mov $0x40c00000; b=a`。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave320.log` · `/tmp/ubuntu_g05_wave320.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`498644201`** / `7215cd6f5` / docs | **R2 Cap residual pure wave319**：**EXPR_NEG / int binop → f32/f64**。根因：int_binop 仅整数 kind → `let a:f32=-6` / `return -6` typeck 红；return 未复用 int_binop；产品 typeck 链 **weak** `pipeline_glue_strict_minimal`（filtered 本地化 glue）→ 只改 `pipeline_glue.c` 假绿；return/asg 未 CTFE fold 时 `mov $6;neg` 非 IEEE。权威：G.7 扩 `pipeline_typeck_coerce_init_int_binop_to_decl_c` 接受 F32/F64 + return 复用 + strict_minimal twin + f32/f64+NEG 时 stamp 裸 INT 操作数（emit IEEE+btc）。探针双端 let/ret/asg/binop；Ubuntu disasm `mov $0x40c00000; btc` / let cfold `$0xc0c00000`。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave319*.log` · `/tmp/ubuntu_g05_wave319*.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck+emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`fd69882f3`** / `4324d28d3` / `fb7721f62` / docs | **R2 Cap residual pure wave318**：**return/let bare int lit → f32/f64**。根因：return 仅 float_lit coerce → `function f(): f32 { return 6; }` expected f32 found i32；Ubuntu freestanding stamp 后仍 `mov $6`（denormal）→ run=0；CTFE fold 抢先于 EXPR_LIT 路径。权威：G.7 return 复用 `typeck_coerce_init_lit_to_decl`（typeck.x + seed + empty_surface + glue）+ ELF EXPR_LIT/cfold int→IEEE bits。探针双端 ret/let/asg f32/f64=6 · floatlit/binop/while 回归 · `return -6` 仍红。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave318*.log` · `/tmp/ubuntu_g05_wave318.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck+emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`50e1db760`** / `de7fc48cd` / docs | **R2 Cap residual pure wave317**：**f32 + bare float lit stays f32**（binop arith + cmp）。根因：FLOAT_LIT 默认 f64；wave296 usual-arith 把 `f32+1.0` / `a=a+1.0` 抬到 f64 → let/assign found f64；`a:f32 < 6.0` 未 coerce lit → Ubuntu while 不进环。权威：G.7 在 binop_arith **f64 widen 前** + binop_cmp 复用 `typeck_coerce_init_float_lit_to_decl`（wave316）；真 f32*f64 仍 widen；窄化 f64→f32 仍须 `as`。seed typeck_gen + empty_surface + ast_pool soft-infer twin 同向。探针双端 add/mul/sub/div/asg_binop/while_f32/cmp/lit+f32/neg_lit/mixed/wave316=42 · narrow 仍红。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave317.log` · `/tmp/ubuntu_g05_wave317*.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck · **日常 L2 非 tip L4** |
| 2026-07-24 | **`887bed776`** / docs | **R2 Cap residual pure wave316**：**f32 float-lit/NEG coerce**（let/assign/return/compound）。根因：`typeck_coerce_init_float_lit_to_decl` 仅 bare FLOAT_LIT 且 assign/return 热路径未调用 → `let a:f32=-6.0` / `a=6.0` / `a+=2.0` / `return -6.0` found f64（`ord_f64` 曾误写 14）。权威：G.7 扩 float_lit（FLOAT_LIT + EXPR_NEG of FLOAT_LIT；`ord_f64=15`）+ glue assign/return + seed/empty_surface 同 commit；窄化 f64→f32 仍须 `as`。探针双端 let_neg/asg/ca/ret=42 · f32→f64 回归 · narrow 仍红。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave316.log` · `/tmp/ubuntu_g05_wave316.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck · **日常 L2 非 tip L4** |
| 2026-07-24 | docs | **docs/02 类型表补全**：主表 + 宽度表正式 `i8`/`i16`/`u16`；§文本与字节（当前 `*u8`/`u8[]`/`String`/`StrView`）；§规划中的类型 **自举后**（`string`/`str`/插值/`char`/`never`/更多 SIMD…）；对照 + README + `基础类型缺口与自举时机分析.md`。**无产品代码** · 钉盘仍 **`75c638465`** |
| 2026-07-24 | **`3aeb3343d`** / docs | **R2 Cap residual pure wave315**：**freestanding let f32→f64 body_sync**。根因：nso>0 热路径 `pipeline_asm_emit_block_body_sync_elf` 存 f64 槽前无 `cvtss2sd`（wave314 只钩 block_inits nso==0 / stmt_order 旁路）→ Ubuntu `let b:f64=a` run=0（mac 常 const-fold 藏）。权威：G.7 热路径复用 `glue_maybe_promote_f32_to_f64_rax_elf_c` + 同块 VAR decl；FORCE `pipeline_x`+standalone。探针：Ubuntu let/asg/ret=6 · disasm `cvtss2sd` · i8=42；mac/Ubuntu L2 矩阵 rv/option/hello（`/tmp/mac_g05_wave315.log` · `/tmp/ubuntu_g05_wave315.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`15c815a2a`** / docs | **R2 Cap residual pure wave314**：**f32→f64 float widen**。根因：typeck exact float match → let/assign/return f32→f64 红。权威：G.7 `typeck_float_widen_ok`（F32=14→F64=15 only；窄化须 `as`）+ seed/empty_surface/glue；freestanding assign/return 经 `backend_enc_cvtss2sd_rax_from_f32_bits_arch`；let 不 stamp resolved。探针：typeck 双端绿；Ubuntu freestanding assign/return=6；**soft residual** freestanding let run=0（可用 `as f64`/assign）。mac L2 矩阵 rv/option/hello。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck + freestanding emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`efc6d5fbc`** / docs | **R2 Cap residual pure wave313**：**NAMED i8/i16/u16 integer widen**。根因：`typeck_integer_widen_ok` 仅 TypeKind ordinal；i8/i16/u16 为 TYPE_NAMED（无专用 kind）→ let/assign/return/call 拓宽红。权威：G.7 `typeck_int_family_id` + `typeck_integer_widen_ok_refs`（name tags 10/11/12）；调用点改 refs；seed typeck_gen + empty_surface + glue `pipeline_typeck_integer_widen_ok_refs_c` + strict_minimal score + ast_pool binop twin 同 commit。探针双端：i8/i16/u16→i32=42 · i8→i16 · assign/return/call · i32→i16 · wave312 回归。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave313b.log` · `/tmp/ubuntu_g05_wave313.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck · **日常 L2 非 tip L4** |
| 2026-07-24 | **`5ada506dd`** / docs | **R2 Cap residual pure wave312**：**first-class integer widen 补全**。根因：`typeck_integer_widen_ok` 在 wave311 后仍缺 **isize↔i64**、**usize↔u64**、**u32→i64/usize/isize**、**u8→i64/isize** → let/assign/return/call 变量拓宽红。权威：G.7 扩 `typeck_integer_widen_ok`（typeck.x + typeck_gen seed + empty_surface + glue `pipeline_typeck_integer_widen_ok_c`）+ strict_minimal overload score 全矩阵对齐。探针双端：isize↔i64 · usize↔u64 · u32→i64/usize/isize · u8→i64/isize · assign/return/call · wave311 回归。leave-off：NAMED i8/i16/u16。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave312.log` · `/tmp/ubuntu_g05_wave312.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck · **日常 L2 非 tip L4** |
| 2026-07-24 | **`294575bf9`** / docs | **R2 Cap residual pure wave311**：**i32 变量 → u64/u8 integer widen**。根因：`typeck_integer_widen_ok` 放行 i32→u32/usize/i64/isize 却漏 **i32→u64**（相对 LP64 usize 真拓宽洞）与 **i32→u8**（变量 let/assign/return residual；lit coerce 已绿）；单候选 call 不严检实参 → `takes_u64(x)` 藏 let 红。权威：G.7 扩 `typeck_integer_widen_ok`（typeck.x + typeck_gen seed + empty_surface + glue `pipeline_typeck_integer_widen_ok_c`）+ strict_minimal overload score 对齐（顺补 wave309 i32→isize 评分漂移）。探针双端：let/assign/return i32→u64/u8 · call · wave307–310 回归。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave311.log` · `/tmp/ubuntu_g05_wave311.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck · **日常 L2 非 tip L4** |
| 2026-07-24 | **`745cd6f24`** / docs | **R2 Cap residual pure wave310**：**u8/u16/u64 EXPR_NEG/binop assign coerce**。根因：`pipeline_typeck_coerce_init_int_binop_to_decl_c` 缺 TYPE_U8 + NAMED u16（旧 invariant 拒 unsigned narrow NEG）；assign 仅 wave308 lit coerce → `let a:u8=-1` / `u16` / `a:u64=-1` / `1-2` found i32（u32/usize 已绿；let u64=-1 已绿）。权威：G.7 扩 int_binop（U8/U32 + u16；strict_minimal 补 ISIZE 对齐 wave309）+ assign 复用 int_binop（typeck.x + typeck_gen seed + empty_surface）。探针双端：u8/u16 let/assign/binop · u64 assign · wave307–309 回归。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave310.log` · `/tmp/ubuntu_g05_wave310.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck · **日常 L2 非 tip L4** |
| 2026-07-24 | **`745cd6f24`** / docs | **R2 Cap residual pure wave310 收口**：**u8/u16/u64 EXPR_NEG/binop assign coerce** + freestanding 位宽。产品：`pipeline_typeck_coerce_init_int_binop_to_decl_c` 补 TYPE_U8/U32 + NAMED u16；mega assign 权威 `pipeline_typeck_check_expr_assign_c` 复用 int_binop（非 typeck.x 镜像）；freestanding cfold 按 u8/u16/u32 掩码 mov_imm32；NEG 后 and imm32；u8 禁用 83 e0 ff（imm8 符号扩展）。探针双端全绿。mac/Ubuntu L2（`/tmp/mac_g05_wave310*.log` · `/tmp/ubuntu_g05_wave310.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck+emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`4c78aa95d`** / docs | **R2 Cap residual pure wave309**：**isize EXPR_NEG/binop via i32→isize widen**。根因：`typeck_integer_widen_ok` 缺 TYPE_ISIZE 同种 + **i32→isize**（glue `pipeline_typeck_integer_widen_ok_c` 已有双权威漂移）；`pipeline_typeck_coerce_init_int_binop_to_decl_c` 放行 U64/USIZE 却漏 **ISIZE** → `let a:isize=-1` / `a=-1` / `1-2` found i32（i64/usize 同路径已绿）。权威：G.7 `typeck_integer_widen_ok`（typeck.x + typeck_gen seed + empty_surface）+ glue int_binop 加 TYPE_ISIZE 同 commit。探针双端：isize=-1→42 · assign · 1-2 · pos lit · i64/i32/i16/usize NEG · wave307 u64max>>60=15。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave309.log` · `/tmp/ubuntu_g05_wave309.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck · **日常 L2 非 tip L4** |
| 2026-07-24 | **`dc65d38cd`** / docs | **R2 Cap residual pure wave308**：**assign RHS full-i64 int-lit coerce**。根因：`typeck_check_expr_assign` 手写 lit 路径用 `pipeline_expr_int_val_at`（**i32 截断**）+ `int_val >= 0` 门控 u64/usize → `a = u64max` found i32、`a = i64max` found i64（wave307 仅修 let-init）。权威：G.7 复用 `typeck_coerce_init_lit_to_decl`（typeck.x + seed + empty_surface）+ glue assign 调 `pipeline_typeck_coerce_init_lit_to_decl_c`（named i16/u16 保留 lit_fits 回退）。探针双端：assign u64max>>60=15 · hex 同 · i64max→u64=7 · small=42 · usize max · u32max · let-init/wave306 回归。soft residual：`let a:isize=-1`（EXPR_NEG）。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave308b.log` · `/tmp/ubuntu_g05_wave308.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck · **日常 L2 非 tip L4** |
| 2026-07-24 | **`842d86716`** / docs | **R2 Cap residual pure wave307**：**u64/usize full-i64 int-lit coerce**。根因：`typeck_coerce_init_lit_to_decl` 用 `pipeline_expr_int_val_at`（**i32 截断**）+ `int_val >= 0` 门控 u64/usize → `let a:u64 = 18446744073709551615`（位型 -1）报 found i32；`i64max` 作 u64 时低 32 全 1→i32 -1 报 found i64。权威：G.7 `pipeline_expr_int64_val_at` + u64/usize 接受任意 bare EXPR_LIT 位型（对标 u32；unary `-N` 仍 EXPR_NEG）；`typeck.x` + `typeck_gen` seed + empty_surface + `pipeline_typeck_coerce_init_lit_to_decl_c` 同 commit。探针双端：u64max>>60=15 · hex 同 · i64max→u64>>60=7 · 2^63>>60=8 · usize max · small=42 · wave306 回归 -1>>32=255 · return -42=214。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave307.log` · `/tmp/ubuntu_g05_wave307.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED typeck · **日常 L2 非 tip L4** |
| 2026-07-24 | **`67f3c0a4c`** / docs | **R2 Cap residual pure wave306**：freestanding **i64 负 imm** + **arm64 全宽**。根因：① 负 `EXPR_LIT`/const_fold 走 `mov_imm32` → eax 写后 RAX 零扩展，`let a:i64=-1` 高 32=0；② `EXPR_NEG` 仅 `neg %eax`/`neg w0`，大值 `mov_imm64` 后再 32-bit neg → 1；③ arm64 `mov_imm64` 只发 3 halfword 丢 hi[31:16]；④ `backend_enc_shr_cl_rax` arm64 误调 32-bit `lsr w0`。权威：G.7 glue 负 lit/const_fold→`mov_imm64` + 64-bit 整数 NEG（`48 F7 D8` / `neg x0`）；arm64 seed 四 halfword + `enc_shl/shr/sar_cl_rax`；dispatch thin+seed 同 commit；seed_link_compat 弱桩。探针双端 freestanding：`-1>>32`=255 · `-i64max>>56`=128 · `i64max>>56`=127 · `u64 2^63>>60`=8 · `return -42`=214；Ubuntu disasm `neg %rax`+`shr %cl,%rax`。mac/Ubuntu L2 g05+矩阵（`/tmp/mac_g05_wave306.log` · `/tmp/ubuntu_g05_wave306.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED emit / MACOS\|ARM64 enc · **日常 L2 非 tip L4** |
| 2026-07-24 | **`7a208d664`** / docs | **R2 Cap residual pure wave305**：plain **full-i64 int lit**（let/return）。根因：OneFunc sidecar `let_init_val`/`return_val` 为 **i32**，`TOKEN_INT` 快路径截断 >2^31-1（`2147483648`→INT_MIN、`u64=2^63`→0；C 曾 `uint64_t a = 0`）。权威：G.7 新 `parser_alloc_int_lit`（`.x` + seed `parser_gen` 同 commit）+ plain let/return 走 `init_ref`/`return_expr_ref`（对标 float `parser_alloc_float_lit`）；freestanding `EXPR_LIT` 越 i32 用既有 `mov_imm64`（`pipeline_glue`）。探针：mac `u64 2^63>>60`=8 · hex=1 · i64max>>60=7 · ret `(2^31>>24)&255`=128 · 小 lit=42。Ubuntu L2 同。mac 矩阵 rv/option/si/hello。钉盘仍 **`75c638465`**。PLATFORM: SHARED parser / LINUX+MACOS x86_64 emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`b9cdef221`** / docs | **R2 Cap residual pure wave304**：freestanding **unsigned u64/u32→f32/f64**。根因：有符号 `cvtsi2s*` 把 u64`>2^63-1`、u32`>2^31-1` 当负整数 → freestanding **run=0**（Ubuntu `1<<63 as f64` / `u32=3e9 as f64` 复现；mac host 可藏）。权威：G.7 新 `backend_enc_cvtsi2sd_rax_from_u64_arch` / `cvtsi2ss_eax_from_u64_arch`（gcc/clang unsigned seq：test/jns/shr/or/cvt/add；seed + thin/full `.x` + surface）+ glue kinds 4/6 无符号、5/7 有符号、3=u32 zext+REX.W。探针：Ubuntu shift+cast=6 · u32=6 · small u64=6 · f32→u64 回归=6 · disasm `test %rax; jns; shr; addsd/addss`。mac L2 g05+矩阵（`/tmp/mac_g05_wave304.log`）。Ubuntu L2 同（`/tmp/ubuntu_g05_wave304.log` · `/tmp/ubuntu_w304_probes2.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED cast / LINUX+MACOS x86_64 emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`48f4c3cf1`** / docs | **R2 Cap residual pure wave303**：freestanding **f32/f64→u32/i64/u64/usize/isize**。根因：`pipeline_asm_emit_as_elf` 仅 f32/f64→i32（eax `cvtt*2si`）；宽目标再发射 IEEE 位型 → freestanding **run=0**（mac host 可藏）。权威：G.7 新 `backend_enc_cvttss2si_rax_from_f32_bits_arch` / `cvttsd2si_rax_from_f64_bits_arch`（seed + thin/full `.x` + surface；thin first4 u32 `739199219`/`739199218`=`F3/F2 48 0F 2C`）+ glue kinds 0/3 eax + 4–7 REX.W。探针：Ubuntu f32/f64→u32/i64/u64/usize/isize=6 · mul=42 · disasm `cvttss2si %xmm0,%rax` / `cvttsd2si %xmm0,%rax` / eax u32 · f32 mul / u8→f32 / i32→f64 回归。mac L2 g05+矩阵（`/tmp/mac_g05_wave303.log`）。Ubuntu L2 同（`/tmp/ubuntu_g05_wave303.log` · `/tmp/ubuntu_w303_probes.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED cast / LINUX+MACOS x86_64 emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`26c8538aa`** / docs | **R2 Cap residual pure wave302**：freestanding **i32-family int→f32/f64** kinds 补全。根因：`pipeline_asm_emit_as_elf` f32 路径 kinds `0/3/13`（13=VECTOR 非 NAMED；缺 u8=2；NAMED=8 从不命中）→ 再发射整数位型 + 外层 `as i32` 对 denormal `cvttss2si` → `(u8 as f32)`/`(i16 as f32)*f32` **run=0**（i32→f32 / u8→f64 绿）。权威：G.7 f32/f64 i32 族统一 kinds `0/2/3/8`；复用既有 `cvtsi2ss`/`cvtsi2sd` eax（无新 encoder）。探针：Ubuntu u8/i16/u16→f32=6 · i16/u16→f64=6 · u8×f32=42 · disasm `cvtsi2ss %eax,%xmm0` · i32/u64→f32 / pure f32 mul 回归；soft residual：`f32/f64→i64/u32/u64`（非本叶）。mac L2 g05+矩阵（`/tmp/mac_g05_wave302.log`）。Ubuntu L2 同（`/tmp/ubuntu_g05_wave302.log` · `/tmp/ubuntu_w302_probes.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED cast / LINUX+MACOS x86_64 emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`7356df615`** / docs | **R2 Cap residual pure wave301**：freestanding **binop dual-slot float AS** 经 `emit_as`。根因：`glue_try_binop_load_operand` 对 EXPR_AS 一律 peel 再 load 操作数槽；float 目标 AS 跳过 `pipeline_asm_emit_as_elf` 的 cvtsi2ss/cvtsi2sd/… → `(u64 as f32)*f32_var` **run=0**（bare cast 绿）。权威：G.7 float 目标 AS 在 dual-slot loader 复用 `pipeline_asm_emit_as_elf_impl`；整数 AS peel 不变。探针：Ubuntu fs soft=42 · bare=6 · lit mul=42 · f64 mul=42 · disasm `cvtsi2ss %rax,%xmm0`+`mulss`。mac L2 g05+矩阵（`/tmp/mac_g05_wave301.log`）。Ubuntu L2 同（`/tmp/ubuntu_g05_wave301.log` · `/tmp/ubuntu_w301_probes.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED cast / LINUX+MACOS x86_64 emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`ddce7353e`** / docs | **R2 Cap residual pure wave300**：freestanding **float-lit `as f32` force_ty**。根因：`pipeline_asm_emit_as_elf` f32 目标 + FLOAT_LIT 调 `glue_emit_float_lit` 时 `force_ty=0`，裸 `7.0` 仍 f64 `mov_imm64`；freestanding `mulss` 吃低 32 → `b * 7.0 as f32` **run=0**（`let c:f32=7.0` 绿）。权威：G.7 传 `force_ty_ref=tgt`（与 let/assign 同权威；无新 encoder）。探针：Ubuntu fs soft=42 · bare cast=7 · let=42 · disasm `mov $0x40e00000`+`mulss` · f64 lit mul 回归；soft residual：`(u64 as f32)*f32_var` 双槽可剥 AS（非本叶）。mac L2 g05+矩阵（`/tmp/mac_g05_wave300.log`）。Ubuntu L2 同（`/tmp/ubuntu_g05_wave300.log` · `/tmp/ubuntu_w300_probes.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED cast / LINUX+MACOS x86_64 emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`e791b7e20`** / docs | **R2 Cap residual pure wave299**：freestanding **u64/i64→f32** REX.W `cvtsi2ss`。根因：`pipeline_asm_emit_as_elf` TYPE_F32 仅 i32/u32 走 `cvtsi2ss`；64-bit kinds 再发射整数位型 → Ubuntu freestanding **run=0**（mac host-gcc 可藏；对标 wave295 u64→f64）。权威：G.7 新 `backend_enc_cvtsi2ss_eax_from_i64_arch`（seed + thin/full `.x` + surface 同 commit；thin first4 u32 `705644787`=`F3 48 0F 2A`）+ glue kinds 4–7。探针：Ubuntu fs `(u64 as f32) as i32`=6 · i64 同 · u64→f32×`let c:f32=7.0`=42 · disasm `cvtsi2ss %rax,%xmm0` · u64→f64/f32 mul 回归；soft residual：`b * 7.0 as f32` binop 内嵌 float-lit as（mulss 吃 f64 低 32；非本叶）。mac L2 g05+矩阵（`/tmp/mac_g05_wave299.log` · `/tmp/mac_w299_matrix.log`）。Ubuntu L2 同（`/tmp/ubuntu_g05_wave299.log` · `/tmp/ubuntu_w299_probes.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED cast / LINUX+MACOS x86_64 emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`a36dcb9eb`** / docs | **R2 Cap residual pure wave298**：freestanding **pure f32 SUB/DIV** `subss`/`divss`。根因：pure f32 `+`/`*` 已有 addss/mulss（wave294）；pure f32 `-`/`/` 仍走整数 sub/idiv IEEE 位型 → Ubuntu freestanding **run=0**（mac host-gcc 可藏）。权威：G.7 新 `backend_enc_subss_rbx_rax_arch` / `subss_rax_rbx_arch` / `divss_rax_rbx_arch`（seed + thin/full `.x` + surface 同 commit；thin u32 `3244036083`/`3244167155`）+ glue pure f32 SUB/DIV + assign `-=`/`/=` 同路径。探针：Ubuntu fs f32 sub/div/sub_rev=42 · add/mul · f64/mixed sub/div 回归 · disasm `subss`/`divss`+`cvttss2si`。mac L2 g05+矩阵（`/tmp/mac_g05_wave298.log` · `/tmp/mac_w298_matrix.log`）。Ubuntu L2 同（`/tmp/ubuntu_g05_wave298.log` · `/tmp/ubuntu_w298_probes.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED arith / LINUX+MACOS x86_64 emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`d15487255`** / docs | **R2 Cap residual pure wave297**：mixed f32±÷f64 提升 + f64 标量 op。根因：wave296 仅闭 mixed MUL；ADD/SUB/DIV 混合仍走整数 add/sub/idiv IEEE 位型 → Ubuntu freestanding **run=0**（mac host-gcc 可藏）。权威：G.7 补全 `glue_try_emit_mixed_f32_f64_arith_elf_c`（ADD/SUB/MUL/DIV 单一路径；固定放置 + wave293 `cvtss2sd` + 既有 addsd/subsd_rbx_rax/mulsd/divsd；无新 encoder）；typeck f64-before-f32 已在 wave296。探针：Ubuntu fs mixed `+`/`-`/`*`/`/` 双向=42 · disasm `cvtss2sd`+对应 sd op+`cvttsd2si` · pure f32/f64 add/mul · mul 回归。mac L2 g05+矩阵（`/tmp/mac_g05_wave297.log` · `/tmp/mac_w297_matrix.log`）。Ubuntu L2 同（`/tmp/ubuntu_g05_wave297.log` · `/tmp/ubuntu_w297_probes.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED type / LINUX+MACOS x86_64 emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`731b6c6e8`** / docs | **R2 Cap residual pure wave296**：f32*f64 mixed mul 提升 + mulsd。根因：typeck 混合先命中 f32 → 结果 f32；glue MUL 仅同标量类走 mulss/mulsd，混合落 `imul` → Ubuntu freestanding **run=0**（mac host-gcc 可藏）。权威：usual arithmetic conversion **f64 before f32**（`typeck.x` + `typeck_gen` + empty_surface + `ast_pool` 同 commit）+ glue `glue_try_emit_mixed_f32_f64_mul_elf_c` 固定放置复用 wave293 `cvtss2sd` 后 `mulsd`（G.7 无新 encoder）。探针：Ubuntu fs `f32*f64`=42 · `f64*f32`=42 · `let c:f64=a*b`=42 · disasm `cvtss2sd`+`mulsd`+`cvttsd2si` · pure f32/f64 mul · u64 as f64 回归。mac L2 g05+矩阵（`/tmp/mac_g05_wave296.log` · `/tmp/mac_w296_matrix.log`）。Ubuntu L2 同（`/tmp/ubuntu_g05_wave296.log` · `/tmp/ubuntu_w296_probes.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED type / LINUX+MACOS x86_64 emit · **日常 L2 非 tip L4** |
| 2026-07-24 | **`40f9b07b9`** / docs | **R2 Cap residual pure wave295**：freestanding **u64/i64→f64** REX.W `cvtsi2sd`。根因：`pipeline_asm_emit_as_elf` TYPE_F64 缺 **TYPE_U64 kind=4**；64-bit 源落 32-bit `cvtsi2sd xmm0,eax` 或再发射整数位型 → Ubuntu freestanding denormal `mulsd` **run=0**（mac host-gcc 可藏）。权威：新 `backend_enc_cvtsi2sd_rax_from_i64_arch`（G.7 邻接 i32 cvtsi2sd；seed + thin/full `.x` + surface 同 commit；thin first4 u32 `705644786`=`F2 48 0F 2A`）+ glue 接线 u64/i64/usize/isize。探针：Ubuntu fs `u64 as f64`=42 · `i64 as f64`=42 · disasm `cvtsi2sd %rax,%xmm0` · i32 cast/f32 mul/f64 mul 回归；leave-off：`f32*f64` mixed · u64 `>2^63-1` 无符号序列。mac L2 g05+矩阵（`/tmp/mac_g05_wave295.log` · `/tmp/mac_w295_matrix.log`）。Ubuntu L2 同（`/tmp/ubuntu_g05_wave295.log` · `/tmp/ubuntu_w295_probes.log`）。钉盘仍 **`75c638465`**。PLATFORM: SHARED cast / LINUX+MACOS x86_64 emit · **日常 L2 非 tip L4** |
| 2026-07-24 | docs | **日切归档**：23 号全日仪表盘 + §6 流水整表迁入 [`自举进度-归档-2026-07-23.md`](自举进度-归档-2026-07-23.md)；本文 24 日起重新起表（保留 wave295–304 当日流水）。钉盘仍 **`75c638465`** · tip **`b9cdef221`** wave304。 |

***

## 7. 禁止清单（本阶段）

- [ ] 未用旧 stage1 / 未用 23 号归档 tip 冒充 24 日 tip L4
- [ ] 未把 prove / Stage2 / N++ 单独写成「可自举」
- [ ] 未 BEST_EFFORT / soft-skip 糊绿
- [ ] 未 seed/.x/glue 分叉合入
- [ ] 未把 mac `-dead_strip` 假绿当 Ubuntu 全链已过
- [ ] 日常 L2 未写成 tip L4；谈升钉须双端 L4 + 129
