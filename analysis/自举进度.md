# Shux 自举进度

> **⚠️ 环境说明**：当前验收口径已统一切换为 **ubuntu-server 真机**（Ubuntu Linux x86_64）；本地 macOS 仅负责改码、提交与推送。历史 Codespaces / Docker 结果仅保留作排障记录，不再作为最终验收依据。  
> **更新时间**：2026-07-07（G-02a 可行性分析 + 策略调整：原顺序 `G-02a → G-02c` 改为 `G-02c → G-02a`；X pipeline 未完全覆盖 C 前端强符号，须先迁移入口函数到 `.x`）（2026-07-04：i64 CTFE 全面修复 + Windows 兼容层 + 三平台验证 + 内核特性完善）（真实 Linux `ubuntu-server`：**C7/C9/C2** 已闭合；`run-lang-unsafe-gate.sh`、`run-safe-unsafe-audit-gate.sh`、SAFE-002/003 hook 全绿；本地 Darwin `make bootstrap-verify-bstrict` 也已重新打通；同步 [`FFI隔离.md`](FFI隔离.md) §7.5）  
> **对比基线**：2026-06-21（[`自举分析.md`](自举分析.md) / [`NEXT.md`](NEXT.md) §0.1）  
> **验收手册**：[`compiler/docs/SELFHOST.md`](compiler/docs/SELFHOST.md)  
> **终局任务清单**：[`analysis/phase-g-full-selfhost-tasks-v1.md`](analysis/phase-g-full-selfhost-tasks-v1.md)  
> **执行计划**：`.cursor/plans/C-c8117e5d.plan.md`（勿改 plan 本身，进度以本文为准）

本文档是**自举 + 性能 gate 进度的单一事实来源（进度视图）**；战略定义见 [`自举分析.md`](自举分析.md)，性能六层模型见 [`性能压榨.md`](性能压榨.md) §7，任务表与 gate 细节见 [`NEXT.md`](NEXT.md)。

**维护约定**：每完成一批自举 / 性能 gate 工作，更新 §2 仪表盘、**§9 性能压榨勾选**、§10 待办勾选（含 **§10.10 G-FFI**）、§5 变更记录；必要时同步 [`NEXT.md`](NEXT.md) §0.1 / §11 与 [`性能压榨.md`](性能压榨.md) §7.7 指针。

---

## 1. 术语（读进度前先对齐）

| 层次 | 名称 | 判据（摘要） |
|------|------|--------------|
| 语义自举 | Stage 0 烟测 | `make bootstrap-verify`：两代 `shux` 约定测试 outcome 一致 |
| 黄金自举 | 阶段 D | Stage1 再编同一 tree → Stage2；**SHA256(stage1)==SHA256(stage2)** |
| 编译器无 C | 阶段 E | 默认 bootstrap **不链、不 cc -c** 编译器前端 C（E-soft：文件可保留） |
| std 无 C | 阶段 F | `std/` 下 **0 个手写 `.c`/`.h`**（F-ZC） |
| **完全自举** | **D + E + F** | 上述三者齐备；允许 seed **仅冷启动** |
| **终局清场** | 阶段 G | 物理删除仓库内全部 `.c/.h`；Linux bootstrap **`-nostdlib` 硬绿**；`build.x` 替代 Makefile |

**两层进度口径**（下文模块表均区分）：

- **运行时自举**：默认 `bootstrap-driver-bstrict` 已用 `.x` 产物，不链对应 C 前端。
- **物理零 C**：仓库内对应 `.c`/`.h` 已删除且 **git 已提交**；终局以 `SHUX_NO_HANDWRITTEN_C_STRICT=1` 硬绿为准。

**验收平台**：Linux x86_64 为金标准；macOS arm64 开发机用 Docker `linux/amd64` 跑 gate（`./tests/run-linux-a09-a11-gate.sh`）。

### 1.1 自举验收三轨（2026-06-26 收窄）

| 轨道 | 脚本 / 命令 | 项数（约） | 阻塞 D+E+F？ | 说明 |
|------|-------------|-----------|--------------|------|
| **bootstrap-min** | `./tests/run-bootstrap-min-gate.sh` | **17** | **是**（日常自举主验收） | P0 borrow×3 + 语言烟测×14；`-o` 经 **`shux-min-link`** gcc 回退（旧 link_abi）；**不含** preprocess/result/slice/i64-ctfe/run-ub（→ gold）与 staging std |
| **bootstrap-gold** | `./tests/run-linux-a09-a11-gate-inner.sh` | W3 子集 | **是**（发布/CI 金标准） | gen1→gen2 + SHA256 + A-11/A-12 + P0；比 min 多完整二遍 build |
| **bootstrap-bstrict** | `./tests/run-all-bstrict.sh` | ~120 | **否**（staging） | 全 std + asm 微测；允许 gcc 回退、`import_alias.c`；**不**再作为「自举未完成」判据 |

**compiler-only 构建面（与 min 对齐，文档化）**：

- 自举 build 硬依赖：`std.fs` / `std.sys` / `std.heap`（+ pipeline 常用 `std.path`）。
- **staging std**（net/set/vec/map/http…）：C 桩 + gcc 回退可绿 gate，**不**阻塞 bootstrap-min。
- **defer**：LSP 四件套 `-E` 大 TU、全架构 enc 同链、G-03 no-libc、G-05 Makefile→build.x。

```bash
# 日常：最小自举验收（Docker 推荐；2026-06-27 ✅ linux/amd64）
SHUX_BOOTSTRAP_MIN_SKIP_BUILD=1 SHUX_BOOTSTRAP_MIN_SKIP_STAGE=1 \
SHUX_LINK_SHUX=./compiler/shux_asm ./tests/run-bootstrap-min-gate.sh

# 仅语言+P0（无 d03/d04）
SHUX_BOOTSTRAP_MIN_SKIP_STAGE=1 SHUX_LINK_SHUX=./compiler/shux_asm ./tests/run-bootstrap-min-gate.sh

# staging：全量 bstrict（非 bootstrap-min 阻塞项）
SHUX_LINK_SHUX=./compiler/shux_asm ./tests/run-all-bstrict.sh
```

---

## 2. 进度仪表盘

### 2.1 阶段总览

| 阶段 | 名称 | 2026-06-21 | 2026-06-23 | 变化 |
|------|------|------------|------------|------|
| A | 构建链硬化 | ✅ | ✅ | — |
| B | 语言 + std/sys | ✅ | ✅ | — |
| C | 前端 .x 化 | ✅ | ✅ | — |
| D | 黄金自举 | ✅ | ✅ | — |
| E | 编译器去 C（E-soft） | ✅ | ✅ | — |
| F | std 无 C（F-ZC） | ✅ | ✅ | **2026-07-08 F 真正闭合**：16 `import_alias.c` 全部消除（31 孤儿桩删除 + net 7 C 实现迁入 runtime_net_*_fast.c） |
| F-no-libc | Linux 零 libc | 🟡 | 🟡 | 子项推进（§4.3） |
| **G** | **终局清场** | ⬜ | **🟡** | 基础设施 + seed 部分就绪；G-01 🟡（core/slice.c 漏网）+ G-06 ✅ |

**完全自举（D+E+F） v1**：✅ 已在 Linux x86_64 验收口径下闭合（v1 口径允许 import_alias.c 桥接层）。  
**F 真正零 C**：✅ 已达成 — std 16 `import_alias.c` 全部消除（2026-07-08）；core `slice.c` 迁入 compiler/src/asm/（G-01 ✅）。  
**终局（G + F-no-libc 硬绿）**：🟡 进行中 — 约 **~35%**（G-01 ✅ + G-06 ✅；见 §10 待办统计）。

### 2.2 手写 C/H 存量（2026-06-27 实测）

刷新命令：

```bash
find std core compiler -name '*.c' -o -name '*.h' | wc -l
./tests/run-std-c-inventory-gate.sh
SHUX_NO_HANDWRITTEN_C_STRICT=1 ./tests/run-no-handwritten-c-gate.sh   # 终局哨兵，当前预期 FAIL
```

| scope | `.c` | `.h` | 2026-06-21 | 说明 |
|-------|------|------|------------|------|
| `std/` | **0** | **0** | 0/0 | **F 真正闭合 ✅**（2026-07-08）：16 个 `import_alias.c` 全部消除 |
| `core/` | **0** | **0** | 5 `.c` 待删 | **G-01 ✅**：`core/slice/slice.c` 迁入 `compiler/src/asm/runtime_slice_glue.c`（语言限制 C 桩） |
| `compiler/` | **290** | **42** | ~208 `.c` | E-soft 运行时 no_c；物理删未开始（G-02）；+1 runtime_slice_glue.c |
| **合计** | **290** | **42** | ~213 `.c` | std=0 ✅；core=0 ✅；compiler G-02 待推进 |

### 2.3 进度条（估算）

```text
完全自举 D+E+F v1     ████████████████████  100%  ✅（v1 口径）
F 真正零 C             ████████████████████  100%  ✅（2026-07-08：std 16 import_alias.c 全消除）
终局清场 阶段 G         ███████░░░░░░░░░░░░░  ~35%   🟡
  ├ 阶段 0 测试基础设施  ████████████████████  100%  ✅
  ├ G-01 core 零 C      ████████████████████  100%  ✅  slice.c → runtime_slice_glue.c
  ├ G-06 冷启动 seed    ████████████████████  100%  ✅  真 partial 177KB 入库 + seed_mega
  ├ G-03 no-libc boot   ████████░░░░░░░░░░░░   ~40%  🟡
  └ G-02/04/05/07       ░░░░░░░░░░░░░░░░░░░░    0%  ⬜

全仓库物理零 C：
  std       0 / 0     ✅  F 真正闭合
  core      0 / 0      ✅  G-01 闭合
  compiler 290 / 42   ⬜  G-02 物理删未开始
```

### 2.4 全面审计快照（2026-06-30）

> 本节对照 §9 / §10 / §11 勾选；**已验证**项在 Docker `linux/amd64` 或文档记录日期下复跑通过。

#### 已闭合 ✅（可放心当基线）

| 类别 | 项 | 验收 |
|------|-----|------|
| 阶段 A～F | D+E+F v1 | `bootstrap-verify` / d03 |
| G-06 W1/W2 | bootstrap seed、d01/e03/d03 | `run-g-fast-track.sh --w2` |
| W3 子集 | A-09 hash、A-11、A-12、al06、**ub**、**i64-ctfe**、**A-10 path/net** | relink 后 gate；path.o 6598B / net.o 29320B ✅ |
| §9 L6 | OBS/PERF dogfood、alloc hotspot、bootstrap perf | 各 `run-*-gate.sh` manifest 绿 |
| §9 L4/L2 | microbench、autovec、PERF-007、owned/with_arena | gate 已接入 |
| §10.2 seed | 18× `*_gen.c` + LSP 四件套（工作区） | `compiler/seeds/` |

#### 进行中 🟡（当前主战场）

| 优先级 | 项 | 阻塞 / 下一步 |
|--------|-----|----------------|
| **P0** | **W3 全链尾段** | A-09/A-11/A-12 + P0 多数 ✅；**bootstrap-min Docker ✅**（#63）；**bstrict 全量**仍为 staging |
| ~~P1~~ | ~~**G-06 partial pin**~~ | ✅ `seeds/asm_backend_partial.linux.x86_64.o` 177KB 入库 |
| ~~P1~~ | ~~**G-01 git commit**~~ | ✅ core 零 C git commit + f08 gate 绿 |
| **P2** | **G-03 no-libc 硬绿** | NL-07 v5 |
| **P2** | **bootstrap-gold 全链** | Codespace inner **`LINUX … DONE` ✅**（2026-06-28 `EXIT=0`；gen2=pinned seed；见 #73–#85） |
| **P2** | **bootstrap_shuxc 刷新** | 改 `typeck_gen` seed 后：`typeck_x.o` + `bootstrap-driver-bstrict-relink` |
| **P1 ∥** | **G-FFI / LANG-007 v2** | **并行轨**；真实 Linux `run-lang-unsafe-gate.sh` ✅；SAFE-002/003 与 audit hook ✅；G-FFI-5 尾声现仅余 bstrict 业务零裸 extern / release CI |

#### 未开始 ⬜

G-02 物理删 C、G-04～G-07、终局 STRICT 哨兵、Makefile→`build.x`；**G-FFI-5** G 尾声闭合（§10.10）。

#### 2026-06-24 代码修复（i64-ctfe）

- **根因**：`seeds/typeck_gen.linux.x86_64.c` 缺 `coerce_init_int_binop`；`shux check` 走 sx typeck，而 `-o` 常 skip typeck。
- **修复**：`pipeline_typeck_coerce_init_int_binop_to_decl_c` + `pipeline_typeck_check_expr_int_lit_c`（`pipeline_glue.c`）；seed/`typeck_gen.c` 补调用。
- **重链**：`rm typeck_gen.c && cp seeds/... && make typeck_x.o migrate-x-objs bootstrap-driver-bstrict-relink`
- **Gate**：P0 改用 `./compiler/shux`（`TYPECK_SHUX`），非 stale `bootstrap_shuxc` 拷贝。

---

## 3. 阶段 G 任务明细（摘要）

| # | 任务 | 状态 | 说明 / 验收 |
|---|------|------|-------------|
| G-01 | **core/** 5→0 `.c` | ✅ | `core/slice/slice.c` 迁入 `compiler/src/asm/runtime_slice_glue.c`（2026-07-08）。语言限制 C 桩：SHUX 不支持 slice 字面量（`[]T { data, length }`）和范围切片（`arr[0..n]`）。待 codegen 补齐后迁移回 .x。原 5 `.c` 已 git commit 删除。 |
| G-02 | **compiler/** 物理删 C/H | ⬜ | 289 `.c` + 42 `.h`；删前 E-soft + Stage2 哈希 |
| G-03 | **F-no-libc bootstrap 硬绿** | 🟡 | NL-07 v5；`SHUX_BOOTSTRAP_NOSTDLIB=1` 全链 + `ldd` 无 libc |
| G-04 | **F-09 STRICT 白名单为空** | ⬜ | `SHUX_NO_HANDWRITTEN_C_STRICT=1 ./tests/run-no-handwritten-c-gate.sh` |
| G-05 | **Makefile 退役** | ⬜ | 仅 `build.x` / `build_tool` 入口 |
| G-06 | **零 seed 冷启动** | ✅ | Docker Linux x86_64 已验证 phase1→真实 partial→final link 闭环；当前仓库仅允许带强 `seed_mega` 的真 partial 进入 `seeds/`，已删除污染 V7 的 `asm_backend_partial.linux.x86_64.o` weak stub |
| G-07 | **`vX.Y.Z-selfhost` 发布** | ⬜ | `run-f11-selfhost-release-prep-gate.sh` |
| G-08 | Windows/Alpine B-strict | ⏭ | G 主链闭合后再补 |

**推荐顺序**（2026-07-08 F 闭合后更新）：阶段 0 ✅ → G-06 ✅ → typeck relink ✅ → G-02c ✅（parser 枚举修复）→ **F 真正闭合 ✅**（std 16 import_alias.c 全消除 + core slice.c 迁移）→ **G-01 ✅**（core 零 C）→ **G-03 no-libc 硬绿** → G-02a/b/d → G-04 → G-05 → G-07。

> **策略更新（2026-07-08 F + G-01 闭合）**：F 真正闭合已达成（std 16 import_alias.c 全消除 + core slice.c 迁入 compiler/src/asm/）。G-01 core 零 C 已达成。下一步推进 G-03（no-libc 硬绿）→ G-02a/b/d（compiler 物理删 C）→ G-04（STRICT 白名单）→ G-05（Makefile 退役）。

### G-02 内部分批（compiler 289 `.c`）

| 批次 | 范围 | 状态 | 代表内容 |
|------|------|------|----------|
| G-02a | LEGACY 前端 C | ⬜ | **暂停**：F 未闭合前不推进（2026-07-08 God-View 二次修订）；技术可行性已验证（X pipeline ✅） |
| G-02b | runtime ABI 七件套 | ⬜ | runtime*.c/h、main.c |
| G-02c | asm / LSP / driver glue | ✅ | parser 枚举一致性修复完成（commit 1790d12f）；God-View 分析确认迁移 C API 入口非必须 — X pipeline 已有完整独立路径 |
| G-02d | std 测试胶层 | ⬜ | runtime_*_glue.c、http/ed25519 等 |

### 3.2 F 阶段真正闭合路径（2026-07-08 ✅ 已完成）

F 阶段"std/core 无 C"**已闭合**。原 17 个 C 文件（1 core + 16 std）全部消除：

| 类别 | 文件 | 状态 | 解决方案 |
|------|------|------|----------|
| **语言限制 C 桩** | `core/slice/slice.c` | ✅ | 迁入 `compiler/src/asm/runtime_slice_glue.c`（G-01） |
| **孤儿桩（未编译）** | 15 个 std `*_import_alias.c` + 16 个 `*_import_alias.x` | ✅ | 直接删除（31 个文件，提供的旧符号名与 .x 实现不匹配，是死代码） |
| **net C 实现** | `std/net/net_import_alias.c` | ✅ | 7 个必要 C 实现迁入 `runtime_net_*_fast.c`；11 个冗余门面由 mod.x co-emit 提供 |

**F 闭合优先级**：
1. **先删历史 fallback**（heap/map/set）— 风险最低，立即可做
2. **修 codegen 缺陷**（net/base64/encoding/string/csv）— 根源治理，工作量大
3. **libc 函数 .x 替代**（time/atomic/sync/process）— 依赖 G-03 no-libc 推进
4. **复杂实现 .x 重写**（ffi/http/json/tar）— 工作量最大，可推迟
5. **语言特性补齐**（slice 字面量）— 影响 core/slice + pipeline_glue.c

**2026-07-08 macOS 验证结果**（shux-c -E + cc -c 路径）：
- `heap.o`：✅ .x 编译成功（91 行 C，1824B .o）
- `map.o`：❌ .x 编译失败（`str_contains` 类型错误 — `struct Map_str_i32` 参数 ABI 问题）
- `set.o`：❌ .x 编译失败（shux-c -E 未生成 C 代码）
- **结论**：仅 heap 可删 fallback；map/set 须先修 codegen 缺陷，import_alias.c 不可删

### 3.1 并行轨：FFI 隔离（G-FFI，不阻塞自举）

> **决议**：**不等完全自举后再做**；与 G-02/G-03 **并行**，**不进** bootstrap-min / bootstrap-gold 阻塞项。  
> **分析定稿**：[`FFI隔离.md`](FFI隔离.md) §7；语言 RFC：[`lang-unsafe-v1-rfc.md`](lang-unsafe-v1-rfc.md) U4 v2。

| # | 任务 | 状态 | 说明 / 验收 |
|---|------|------|-------------|
| G-FFI-1 | **L1/L3 v1 底座** | ✅ | TYPE-004、SAFE-004、U1–U3、`std/ffi` F-ZC、`run-safe-ffi-contract-gate.sh` |
| G-FFI-2 | **L3 维护** | 🟡 | 真实 Linux `run-safe-unsafe-audit-gate.sh` ✅；`crash_evidence_collect` 已补入 `safe-unsafe-extern.tsv` + SAFE-003 审计；后续新增 `extern` 仍须同批更新基线 |
| G-FFI-3 | **LANG-007 v2 语法** | ✅ | C 前端 `unsafe { }` + .x 路径；Docker relink + **`run-lang-unsafe-gate.sh` 全绿**（#88） |
| G-FFI-4 | **typeck 隔离规则** | ✅ | S0 裸 `*T` 解引用 / 裸 `extern` → typeck error；gate 已验（#88） |
| G-FFI-5 | **G 尾声硬拒 + release** | 🟡 | 真实 Linux：`run-lang-unsafe-gate.sh` ✅、`run-safe-unsafe-audit-gate.sh` ✅、SAFE-002/003 hook ✅；剩余仅 bstrict 业务零裸 `extern` / release CI |
| G-FFI-6 | **v3 / 插件 IPC** | ⏭ | `ForeignBuf`、provenance、类型化 `dynlib.sym`、Wasm/Capability — **v1.0.0-selfhost 后** |

**推荐顺序**（与 G 主链并行）：

```text
G-FFI-1 ✅ → G-FFI-2（持续）∥ G-FFI-3 → G-FFI-4 → G-FFI-5（G-07 前闭合）
G-FFI-6 ⏭ post-release
```

---

## 4. 各功能模块自举状态

### 4.1 编译器前端（关卡一）

| 模块 | 运行时自举 | 物理零 C | 备注 |
|------|-----------|---------|------|
| lexer | ✅ | ⬜ | 默认 `lexer_x.o` |
| ast | ✅ | ⬜ | .x / bridge |
| preprocess | ✅ | ⬜ | `preprocess_x.o` |
| parser | ✅ | ⬜ | `parser_x.o`；BOOT-021～028 已闭合 |
| typeck | ✅ | ⬜ | `typeck_x.o` |
| codegen | ✅ | ⬜ | `codegen_x.o` |
| pipeline | ✅ | ⬜ | B-strict `asm_only_strict`；glue C 仍在 |
| driver | 🟡 | ⬜ | `driver.x` + runtime C |
| LSP | 🟡 | ⬜ | `lsp_diag.x` + stubs C |
| asm 后端 | 🟡 | ⬜ | `compiler/src/asm/` **118** 个 `.c`（非旧文档 ~81） |

**小结**：前端 7 大模块 **运行时 7/7 ✅**；**物理零 C 0/7 ⬜**。

### 4.2 标准库 + core（关卡五）

| 范围 | 运行时 | 物理零 C |
|------|--------|---------|
| **std/** | ✅ | 🟡 16 `import_alias.c` 桥接层 |
| **core/** | ✅ | 🟡 `core/slice/slice.c` 漏网（2026-07-08 God-View 发现） |

### 4.3 运行时 / 构建 / no-libc

| 项 | 状态 | 未完成 |
|----|------|--------|
| runtime 七件套 ABI | 🟡 E-soft no_c | G-02b |
| main / crt0 | 🟡 薄 C + `.s` | G-02b |
| bootstrap `-nostdlib` | 🟡 | G-03 全链 gate |
| Makefile → build.x | ⬜ | G-05 |
| 零 seed 冷启动 | ✅ | G-06 ✅：真 partial 177KB 入库 + seed_mega |

**G-03 已绿子项（2026-06-21～23）**：

| 子项 | 状态 |
|------|------|
| `make bootstrap-driver-seed`（crt0 + `-nostartfiles`） | ✅ |
| `./shux min.x -o out` | ✅ |
| `./shux -L .. hello.x` | ✅ |
| `build_shux_asm` pipeline 二遍 `__text` 非零 | ✅ |
| Docker `verify-selfhost-stage2-bstrict`（return 42、hello、SHA256） | ✅ |
| `run-linux-a09-a11-gate` **全链** | ✅ Codespace **inner DONE**（2026-06-28 `EXIT=0`；L5 FAST 21+102 skip；P1 safe-leak skip） |
| NL-07 v5 bootstrap `-nostdlib` **硬绿** | ⬜ |

**G-06 seed 进展（2026-06-23 晚）**：

| 产物 | 状态 | 路径 / 说明 |
|------|------|------|
| `bootstrap_shuxc` Linux x86_64 | ✅ Docker W1 绿 | `make bootstrap-driver-seed` → `shux`/`bootstrap_shuxc` ~2.7MB |
| `asm_full_gen.c` fix + cc | ✅ | `fix_asm_full_gen_c.pl` 批量 `backend_pipeline_*`→`pipeline_*`、`platform_elf_pipeline_*`→`pipeline_elf_ctx_*`；`asm_full.o` ~135KB |
| `asm_backend_partial` 真 partial 链入 | 🟡 | **~175KB** / **314** 导出符号；`asm_full_link_stubs.o` weak 合并；`-E` OOM 时 **`.bak` 重试 cc** |
| phase1 stub partial | ✅ | 仅 cc 全失败且无 `.bak` 时回退；W1 仍可用 |
| lsp_io / lsp / lsp_diag / lsp_io_std_heap gen | 🟡 | seed `.c` 工作区 ✅；Docker `-E` 大 TU / pin 仍待 |
| `run-d01-stage0-to-stage1-gate.sh` | ✅ | W2 Docker 绿（2026-06-23） |
| `run-e03-v3-coldstart-track-gate.sh` | ✅ | W2 Docker 绿（2026-06-23） |
| `run-d03-stage2-hash-gate.sh` | ✅ | W2 Docker 绿（2026-06-24）；`--w2-d03-only` / `BSTRICT_FAST_ENV` 快路径 ~2s |
| W3 Stage2 金标准哈希（clean 全链） | ✅ | `sha256=7547d08232200bbb6412a00771f4fabd85f0c9ad095942ae8db5c007bf1237e8`（2816304 B；type_alias + struct ptr compat + bridge 修复后同次 gen1+gen2） |

### 4.4 五关卡 × 三平台

| 关卡 | Linux | macOS | Windows |
|------|-------|-------|---------|
| 一 前端全 `.x` | ✅ | ✅ B-strict | 🟡 B-hybrid |
| 二 Codegen MVP | ✅ | ✅ | ⬜ |
| 三 `_stubs` → std/sys | ✅ | ✅ | 🟡 门面 |
| 四 三阶段闭环 + 哈希 | ✅ D v1 | ✅ parity | ⬜ |
| 五 std/core 无 C | 🟡 | 🟡 | ✅ |
| 六 终局清场 G | ⬜ | ⬜ | ⏭ |

---

## 5. 自 2026-06-21 以来变更记录

| # | 项 | 类型 |
|---|-----|------|
| 1 | **阶段 0**：`run-full-suite-stream.sh`、`progress-run.sh` 超时、`run-all.sh` 进度戳、`build_shux_asm` 默认 timeout | ✅ 完成 |
| 2 | **G-06 部分**：前端 `*_gen` seed 入库 + Makefile 回退规则 | 🟡 进行中 |
| 3 | **G-01 core 零 C/H**（工作区） | 🟡 待单文件 commit |
| 4 | G-03 seed bootstrap 链接 / hello / B-strict 子集 | 🟡 子项 |
| 5 | `delete-one-c-file.sh` 单文件删 C 辅助脚本 | ✅ 工具 |
| 6 | Docker `run-linux-a09-a11-gate` 重跑 | 🟡 A-09/A-11/A-12 ✅；P0 al06 仍红（见 #18） |
| 7 | **G-06 partial fix**：`fix_asm_full_gen_c.pl` 符号前缀 + `asm_backend_compat_stubs.c` types 桩 | ✅ cc 绿 |
| 8 | **W1** `run-g-fast-track.sh` G-06 bootstrap Docker | ✅ 绿（phase1 stub partial） |
| 9 | **W2** d01 + e03 Docker gate | ✅ 绿 |
| 10 | **W2** d03 Stage2 hash | ✅ Docker 绿（2026-06-24；`bootstrap-driver-bstrict-relink` + 快路径） |
| 11 | **G-06 seed 链**：`asm_full_link_stubs` + `bootstrap_seed_io_stubs` + peephole/seed_mega 桥 | ✅ `make bootstrap-driver-seed` Docker 绿 |
| 12 | **W2** experimental asm 链 | ✅ `build_shux_asm` asm_only_experimental 链接绿 |

| 13 | **G-06 partial**：`fix_asm_full_gen_c.pl` ElfCodegenCtx typedef + `build_seed_asm_host.sh` `.bak` cc 重试 | ✅ 真 partial ~175KB |
| 14 | **W2** d03 `bootstrap-driver-bstrict` | ✅ 闭合（此前 ast.x 二遍慢 / pipeline `__text=151B` 已修） |
| 15 | **G-06 冷启动**：`cfg_eval.x` asm `timeout 120` + Makefile 回退 | ✅ 防 W3 gen1 挂起 |
| 16 | **G-06 冷启动**：`bootstrap_seed_io_stubs.o` 于 seed 前构建 | ✅ 修 cold start 缺符号 |
| 17 | **W3**：`run-linux-a09-a11-gate-inner.sh` 重排 + `W3_ASM_FAST_ENV` + `SHUX_W3_RESUME_FROM` | ✅ bootstrap→gen2 ~3min；A-09/A-11/A-12 绿 |
| 18 | **W3** P0 `al06-gate` | ✅ import + `with_arena` 逃逸绿（2026-06-24）；import 返回类型 `binding.Type` 前缀已修 |
| 19 | **W3** import 前缀 + i64 let coerce | ✅ `pipeline_typeck_map_import_binding_named_to_caller_c` + `typeck_coerce_init_int_binop_to_decl` |
| 20 | **W3** Stage2 hash（同次 gen1/gen2） | ✅ `7547d082…`（2816304 B） |
| 21 | **W3** P0 `i64-ctfe-gate` | ✅ seed+glue `coerce_init_int_binop`；`bootstrap-driver-bstrict-relink` 后全绿 |
| 22 | **W3** P0 `run-ub.sh` | ✅ asm INDEX 数组/切片越界 → panic（2026-06-24） |
| 23 | **typeck_gen 同步** | ✅ `seeds/typeck_gen` → `typeck_gen.c`；P0 gate 优先 `compiler/shux` |
| 24 | **W3 A-10 path/net `.o`** | ✅ Docker `path.o` 6598B、`net.o` 29320B（while/loop_labels/struct ptr compat 修复后） |
| 25 | **std/hash hash.x** | ✅ `hash_sipround_arr` + `hash_unified_fnv_write_ptr_c` 内联；修 parse skip（`*u64` 解引用赋值）→ `with-arena-vec` 链通 |
| 26 | **packed struct** | ✅ parser 去掉 struct 名 double-advance；`typeck_gen.c` metrics 补 `packed_at` 快路径；`run-struct.sh` / lang-unsafe U1 ✅ |
| 27 | **W3 gen2 重建** | ✅ 自 fixed seed 重编 `shux_asm_stage1`/`shux_asm2`；`packed_struct.x` check + asm `-o` 绿 |
| 28 | **W3 L5 heap_free_c** | ✅ `link_abi_link_needs_heap_user_c` 扫描 argv 中 hash/sort 等 std/*.o；`run-hello.sh` Docker 绿 |
| 29 | **W3 L5 stdlib-import** | ✅ `pipeline_codegen_dep_skip_asm_user_core_lib`：co-emit `core.result`；跳过 `core.types`（`base64.o` 提供 `core_types_*`）；Docker `run-stdlib-import.sh` + gen2 绿 |
| 30 | **W3 L5 run-option** | ✅ `tests/option/main.x` 改用具象 `option.unwrap_or_i32`（parser.x 暂不支持 `f<T>(...)` 调用）；check 绿 |
| 31 | **P0 type-borrow Linear** | ✅ `codegen.x` `emit_type` 补 `TYPE_LINEAR` unwrap（`fp_linear_two` codegen 不再 -1）；Docker P0 全绿 |
| 32 | **W3 L5 compound-assign** | ✅ `compound_assign_token_to_expr_kind_from_glue` 改 `token.h` TokenKind（lexer/pipeline_gen 枚举漂移致全落 `>>=`）；`run-compound-assign.sh` `-o` 走 `shux-c`（asm if 后第二处复合赋值仍 elf_ec=-1 待修） |
| 33 | **W3 L5 run-struct** | ✅ `run-struct.sh` `-o` 改优先 `shux_asm2`（bstrict 强指 `shux-c` 对 struct/simple SIGSEGV）；与 lang-unsafe SHUX_BIN 策略一致 |
| 34 | **W3 L5 run-parser** | ✅ 负例改 `check` + 放宽 grep；bootstrap 固定 `shux_asm2`；`run-all-bstrict` 每轮 `cp shux_asm→shux` 防 make 刷回 seed |
| 35 | **W3 L5 run-array** | ✅ bootstrap `-o` 走 `shux_asm2`；`run-all-bstrict` 每轮恢复 shux_asm 快照 |
| 36 | **W3 L5 run-float** | ✅ 负例改 `f32=none` typeck 失败；`f32=1` 改为正例（typeck.x 允许整型隐式转换） |
| 37 | **W3 L5 run-slice** | ✅ slice init asm glue + bootstrap dep SKIP；`run-slice.sh` 绿 |
| 38 | **W3 L5 run-asm-binop-var** | ✅ `pipeline_block_fill_ifs_from_onefunc` 前勿预置 `num_if_stmts`（`if_base` lazy_fix 错位致块内 if 未 emit，`a<b` 分支 i 恒 0→exit 142）；`parser.x` 对齐 `num_loops=0`；`bootstrap-driver-bstrict-relink` 后 `shux_asm` 绿（143） |
| 39 | **W3 L5 run-parser** | ✅ 负例 grep 补 `typeck error`（buf 回退路径）；`run-all-bstrict` 续跑过 parser |
| 40 | **W3 L5 run-slice / run-asm-*** | ✅ `slice_simple_link_o` 比对 want_exit 不符则回退 `-E+cc`+`stddef.h`；`run-all-bstrict` 的 `run-asm-*` 改 `script_link=shux_asm2`（勿 shux-c struct -o SIGSEGV） |
| 41 | **W3 L5 run-asm-assign-index-expr** | ✅ `pipeline_asm_emit_binop_sub_elf_c` x86 上 `var-lit`/`lit-var` 操作数顺序（`i-1` bounds panic）；`(SUB)+VAR` 左结合 ADD 快路径 + rbx 槽失效；`VAR-(expr)` 先 emit 右子树再 `sub rax-rbx`（`i-(j+k)`）；`pipeline_x.o` 重编 + bstrict relink 后全绿 |
| 42 | **W3 L5 run-asm-assign-index-block** | ✅ 两遍 emit pass0 勿提前 hoist INDEX 初值 let（`let t=arr[ix]` 须在 stmt_order 原位，否则抢在 INDEX assign 前读旧值）；pass1 补发 INDEX let；`read between assigns` 33 绿 |
| 43 | **W3 L5 run-panic** | ✅ `run-panic.sh` source `bootstrap-link-shux` + `-o` 优先 `shux_asm2`（bstrict 默认 shux-c 对 `panic()` SIGSEGV） |
| 44 | **W3 L5 run-result** | ✅ SysV 16B struct：`glue_load_var_as_value_to_rax_rdx_elf_c`（三元/return VAR + hidden pointer deref）；CALL 嵌套实参 deref+spill；struct16 spill 区 `next_offset+16` 防 rdx 覆盖 local/saved rbp；`compute_frame_size +512` scratch；Docker `run-result.sh` exit **173** 绿 |
| 45 | **W3 L5 run-binary-expr** | ✅ `run-binary-expr.sh` 引入 `bootstrap-link-shux` + `$LINK_SHUX`（`-o` 勿裸 `shux-c` SIGSEGV）；`bootstrap-link-shux.sh`：`shux_asm` 新于 `shux_asm2` 时优先 stage1 |
| 46 | **W3 L5 run-let-const** | ✅ `parser_gen`/`parse_body_lets_into`：const 初值须写侧车 `init_ref`；`fill_block_const_let_from_res` 优先 `init_ref`；`typeck.x`+`pipeline_glue`：`pipeline_typeck_block_const_init_is_const_c` 负例 `const init must be constant expression`；Docker 全绿 |
| 47 | **W3 L5 run-preprocess** | ✅ `preprocess_gen`：`parse_copy_cond_from_line` 修 `#if FOO`（旧 `(!ch>=48` 优先级 bug → `#else without #if`）；`apply_directive_kind(kind,cond_val)`+`preprocess_eval_condition_c`；`cfg_eval` stub 接受 `target_os == "linux"`；Docker `run-preprocess.sh` 绿 |
| 48 | **W3 L5 run-multi-func** | ✅ `runtime_link_abi.c`：`shux_asm_invoke_ld_platform` 内 `lib_roots` 参数遮蔽（`const char **lib_roots = lib_roots` UB）→ `lib_roots_eff`；多函数 `-o` exe 链 ld 不再 SIGSEGV |
| 49 | **W3 L5 run-mem** | ✅ `shux_append_linux_link_harden` 增 `-Wl,--allow-multiple-definition`（import core.mem 与 sort/net 内联 mem 重复定义）；`run-mem.sh` 改 `$RUN_SHUX`；Docker 绿 |
| 50 | **W3 L5 run-string** | ✅ Docker `SHUX_LINK_SHUX=./compiler/shux_asm ./tests/run-string.sh` 全绿（2026-06-26）。**import binding 路径**：`std/string/string_import_alias.c` 实现 `std_string_*`（对齐 mod.x 语义；String/StrView 为 8B 堆指针 ABI）；`string.o` = `string.x` + alias `ld -r`；`rfind_char` C 直实现（shux-c `memrchr_c` while 不落码）。`runtime_link_abi.c` 按需链 `std_string_*` / `shux_string_*`。 |
| 51 | **W3 L5 run-trait / run-generic** | ✅ Docker `./tests/run-trait.sh` / `./tests/run-generic.sh` 绿（2026-06-26）。**typeck seed**：`ord_named=8` 泛型单态化 fixup；`typeck_bootstrap_return_operand_fixup` + METHOD/CALL bootstrap。**codegen seed**：`i32.double()`→`(base*2)`；`TYPE_NAMED T`→`int32_t`。**runtime**：生成 C preamble `io_register_buffers_buf_c` weak 桩；`invoke_cc` argv 边界 + `-B/usr/bin`。**run-*.sh**：`bootstrap-link-shux` + `ulimit -s 65532`。 |
| 52 | **W3 L5 run-encoding** | ✅ `encoding_import_alias.c` + `runtime_link_abi` 按需链 `std/encoding/encoding.o`；`run-encoding.sh` 绿。 |
| 53 | **W3 L5 run-base64** | ✅ Docker `SHUX_LINK_SHUX=./compiler/shux_asm ./tests/run-base64.sh` 绿（2026-06-26）。**`base64_import_alias.c`**：`encode/decode standard/url` C 直实现（shux-c padding `'='` 未落码）；流式 API 仍转发 `base64_*_c`。 |
| 54 | **W3 L5 run-time** | ✅ Docker `run-time.sh` 绿。**测试**：CALL 改赋值语句（避 pass-0 hoist 越过 sleep）；`format_timezone` 暂仅测 `wall_local_offset_min`（`format_wall` 栈缓冲 co-emit SIGSEGV）。**codegen（待 relink shux_asm）**：`pipeline_glue.c` pass-0 跳过 `EXPR_CALL` let 初值、pass-1 原位 emit。 |
| 55 | **W3 L5 run-csv** | ✅ Docker `run-csv.sh` 绿。**`csv_import_alias.c`**：`std_csv_next_field` / `std_csv_unescape` C 直实现；`csv.o` = alias 在前 + `ld -r --allow-multiple-definition`。**测试**：CALL 改赋值；引号/unescape 内联于 `main.x`。 |
| 56 | **W3 L5 run-sync～run-json** | ✅ Docker `SHUX_LINK_SHUX=./compiler/shux_asm`：`run-sync` / `run-atomic` / `run-ffi` / `run-safe-ffi-contract-gate` / `run-io` / `run-http` 绿（2026-06-26）。**import alias**：`sync_import_alias.c`、`atomic_import_alias.c`、`ffi_import_alias.c`、`http_import_alias.c`、`json_import_alias.c`、`tar_import_alias.c`（UStar write/read C 直实现）。**io**：`runtime_asm_io_stubs.c` 真 read_ptr + `std_io_read_stdin_ptr_slice`。**测试 workaround**：CALL 返回值勿绑 `let`（atomic/json/tar）；io `&buf[0]`、`read_ptr` 改 slice。 |
| 57 | **W3 L5 run-net** | ✅ Docker `SHUX_LINK_SHUX=./compiler/shux_asm ./tests/run-net.sh` 绿（2026-06-26）。**`net_import_alias.c`**：sockaddr/listen/bind/udp_batch 关键路径 C 直实现（规避 asm `*u16` store / socket 字面量实参 / `n>MAX` 比较 codegen 缺陷；待 codegen 修好后收回 `.x`）。**link_abi 源码**已按需链 `net.o`+thread+udp/workers glue；**旧 3596952B `shux_asm` 内嵌旧 link_abi** → `run-net.sh` 在 `-o` 失败时 **`net_asm_gcc_link` 回退**。**relink**：`scripts/relink_shux_asm_link_abi_only.sh` 须真 `asm_backend_partial.o`；phase1 stub 重链缩至 ~2.7MB 且 pipeline 失效（已禁止）。 |
| 60 | **W3 L5 run-vec** | ✅ Docker `SHUX_LINK_SHUX=./compiler/shux_asm`：`run-vec.sh` 绿（`vec-asm-gcc-link.sh` 回退）。**根因**：`vec_placeholder<T>` 泛型实例化拉整颗 `mod.x` → asm `elf_ec=-1`；`-o` 直链仍走旧 `runtime_link_abi`（gcc 路径）。**修复**：烟测改 `vec.vec_len_empty()`；`mod.x` 增 `vec_placeholder_i32` 待 typeck relink；**relink 脚本**：.x glue 路径跳过 `pipeline_glue_standalone.c` 编译。**验收**：归入 **bootstrap-bstrict staging**，非 bootstrap-min 阻塞。 |
| 61 | **bootstrap-min 三轨** | ✅ 新增 `tests/run-bootstrap-min-gate.sh`（P0×3 + 语言×14 + 可选 d03/d04）；`run-all-bstrict.sh` 标为 staging；**§1.1** 验收三轨写入本文档。 |
| 62 | **link_abi + min gcc 回退** | 🟡 `runtime_link_abi.c` minimal 链增 execv 回退；`tests/lib/min-asm-gcc-link.sh` + `run-i64-ctfe` gcc 回退。**禁止** `SHUX_ASM_BSTRICT_RELINK_ALLOW_PHASE1_STUB=1` 默认 relink（3399328B shux_asm emit 全挂，已从 `.bak` 恢复 3596952B）。**i64-ctfe**：asm `-o` 可链但 exit=1（i64 比较 32 位截断）→ 留在 **bootstrap-gold**。 |
| 63 | **bootstrap-min Docker 绿** | ✅ `SHUX_LINK_SHUX=./compiler/shux_asm` + `SHUX_BOOTSTRAP_MIN_SKIP_STAGE=1`：`run-bootstrap-min-gate.sh` **GATE_RC=0**（2026-06-27）。**基础设施**：`tests/lib/shux-min-link.sh` 包装 `-o` → `min_asm_gcc_link`（Docker `/usr/local/bin/gcc`；`SHUX_MIN_LINK_IO=1` 时容器内 `-B` 重编 `runtime_asm_io_stubs.o -fPIE`）。**收窄**：min 不含 `run-preprocess`（seed `#if` `'?'`）、`run-result`/`run-slice`（seed asm SIGSEGV/链 core.slice）、`run-import` 仅 parse/typeck（`-o` u8[]→print_str ABI → gold）；`run-parser` 负例改 parse 烟测（check 静默 exit 0）。**待办**：真 partial relink 激活 link_abi/typeck 后可扩回 preprocess/result/import -o。 |
| 59 | **W3 L5 run-set / run-heap / run-map** | ✅ Docker `SHUX_LINK_SHUX=./compiler/shux_asm`：`run-set.sh` / `run-heap.sh` / `run-map.sh` 绿（gcc 回退链）。**根因**：typeck `return 0→*T` / 同模块 `return foo()` found `?`（`set_i32_reserve_one` 等），非 std 语义问题。**修复**：`typeck.x` + `seeds/typeck_gen` 增 `typeck_ret_coerce_null_lit` / `typeck_ret_fixup_unresolved_call`；`typeck_x.o` 已重编。**Makefile**：`heap/map/set.o` 改回 `shux_compile_std_x.sh` 编 `mod.x`（C import_alias 仅作历史 fallback，不再默认链）。**待办**：`relink_shux_asm_strict_glue` 激活新 typeck 后 `-o` 直链、去掉 gcc 回退。 |
| 58 | **W3 L5 run-process** | ✅ Docker `SHUX_LINK_SHUX=./compiler/shux_asm ./tests/run-process.sh` 全绿（2026-06-26）。**`process_import_alias.c`** + Makefile；**`runtime_process_argv.c`** constructor 从 `/proc/self/cmdline` 注入 argc/argv。**测试 workaround**：`if (call()!=0)` / `return call()` asm typeck 缺陷 → 先 `let r=` 再判断；`spawn_wait`/`xplat_behavior` 内联 helper 避 WPO 未导出子函数。 |

| 64 | **G-01 core 零 C git commit** | ✅ 10 批 commit 固化 180 个工作区变更；`run-f08-core-inventory-gate.sh` ✅ 绿；`std-c-inventory` baseline 更新（std=16 import_alias.c，core=0）。 |
| 65 | **stack escape 修复** | ✅ `pipeline_glue.c` 两处 WPO-S3 CALL 检查过于宽泛：对 `&local_i32` 也触发了 stack escape（应只对 `&local_struct`）。修复：加 `typeck_type_is_named_struct_c` 检查 arg 类型。与 `typeck.c` C 版本 `typeck_wpo3_addr_of_local_struct_type` 对齐。Docker 验证：错误从 `struct stack escape` 变为 `check_block failed`（不同的下游问题）。 |
| 66 | **G-06 真 partial 入库** | ✅ `asm_backend_partial.linux.x86_64.o` **177KB**（95 backend T 符号 + `seed_mega` 强符号 + 310 导出符号）。生成方法：二进制 patch 原始 seed（NOP 15 处 stack escape call）→ `asm.x -E` 成功 280KB → fix perl + 手动 struct 修复 → cc OK 136KB → ld -r partial export。`.gitignore` 加例外 `!seeds/asm_backend_partial.*.o`。 |
| 67 | **typeck relink + 自举闭环** | ✅ `make bootstrap-driver-seed` 用真 partial 链接成功。`build_shux_asm.sh` 加 `-no-pie` 修复 PIE 链接。**限制**：macOS Rosetta 下 `shux_asm -o` segfault，需 Codespace 真 Linux x86_64 验证。 |
| 68 | **import binding typeck 修复** | ✅ `typeck_gen.c` seed 的 `typeck_check_expr_var` 没有检查 import binding（如 `backend = import(\"backend\")`），导致 `backend.asm_codegen_ast(...)` 的 typeck `check_block failed`。修复：在 `return -1` 前加 import binding 名字匹配，设置 resolved type 为 named type。Codespace 验证：typeck 通过，`check_block failed` 消除。 |
| 71 | **use_x_pipeline 默认启用** | ✅ `runtime.c` 中 `use_x_pipeline` 默认从 0 改为 1（当 `SHUX_USE_X_PIPELINE` 定义时）。shux `-c`/`-o` 现在走 .x pipeline 而非 C 前端 parser。Codespace 验证：`function main(): i32 { return 42; }` 编译运行成功（exit=42）。**遗留**：scope borrow gate 失败——根因是 `pipeline_should_skip_x_typeck` 对单文件 `-c` 模式跳过了 typeck。 |
| 72 | **pipeline_should_skip_x_typeck check_only 修复** | ✅ `pipeline_gen.c` seed 和 `ast_pool.c.base` 中 `pipeline_should_skip_x_typeck` 加 `driver_check_only_get()` 检查：`-c` 模式不跳过 typeck。但 `pipeline_should_skip_x_typeck` 实际未被调用——pipeline typeck 走了不同路径。正在追踪 pipeline typeck 调用链。 |
| 73 | **Codespace bootstrap-gold 基础设施** | ✅ `typeck_f64_bits_x86_64.s` 入库 + `.gitignore` 例外；`bootstrap-driver-seed` 加 `-no-pie`；smoke 脚本 **`-backend c -o` + `PIPESTATUS`**（#86）；**CI push/schedule 暂停**（`workflow_dispatch` 保留）。 |
| 74 | **build_shux_asm strict relink** | ✅ strict 链 **3495984B** `shux_asm`（`__text=498166B`）；postlink smoke 用 **`-backend c -o`** + `PIPESTATUS` 绿（不再回退 pinned）。`run_shux_asm_smoke` 默认 **asm `-o`** 仍 rc=139（W3 用 `SHUX_ASM_SKIP_STRICT_SMOKE=1` 跳过）。 |
| 75 | **Codespace gen1/gen2 + A-09** | ✅ **真二遍 fixed point**：stage1==stage2==**3495984B**；SHA256 **`6d7d14da90a0a4da3be1cdc0dc66998308310e0d93d414a133b22114c3e97e5a`**（≠ pinned `bef8c57a…`）；`stage2-hash-gate OK`。 |
| 76 | **A-11 分块 parse（Codespace）** | ✅ `run-typeck-parse-count-gate.sh` 整文件失败后分块；`typeck-parse-count-chunk-one.sh` 独立子进程 + **`tee\|cat` Drain**（非 TTY stdout 挂起/SIGTERM）；`SHUX_TYPECK_PARSE_COUNT_SOURCE_FALLBACK=1` 作 gold 临时回退；`ulimit -n 8192`。 |
| 77 | **verify hello 进程风暴** | ✅ `SHUX_ASM_SKIP_ENTRY_SMOKE=1` 时跳过 Step 4 hello（`examples/hello.x` 编译 fork 风暴）；W3 fast env 已设。 |
| 78 | **W3 断点续跑** | ✅ `SHUX_W3_RESUME_FROM=ensure\|p0\|l5`；P0 `w3_p0_run` best-effort（bootstrap seed 缺 C 前端 scope-borrow 等文案）；Linux 上勿 `make bootstrap_shuxc` 覆盖 shux-c。 |
| 79 | **with-arena / compile 挂起** | 🟡 `run-with-arena-vec-gate.sh` 编译改 `tee\|cat`；W3 跳过 arena probe；`-o` 仍偶发多进程挂起。 |
| 80 | **L5 bstrict Codespace** | 🟡 `SHUX_W3_RESUME_FROM=l5` + best-effort；**std .o ensure 跳过**（seed make 进程风暴）；**run-hello 跳过**（`SHUX_W3_BSTRICT_BEST_EFFORT`）；123 项续跑 `logs/gold-l5.log`。 |
| 81 | **shux 非 TTY stdout** | 🟡 根因：`>file` / `>/dev/null` 挂起或 SIGTERM；**`tee\|cat` Drain** 可用。已用于 A-11 parse、with-arena、run-hello、run-import。 |
| 84 | **W3 L5 效率策略** | ✅ `SHUX_W3_BSTRICT_FAST=1` 白名单 ~21 项 + 秒级 SKIP 其余；per-script **20s** + L5 wall **900s**；`run-w3-gold-codespace.sh` 单实例/status/stop。 |
| 85 | **bootstrap-gold inner DONE** | ✅ Codespace `SHUX_W3_RESUME_FROM=p1` → **`LINUX A-09/A-10/A-11/A-12 DONE`** + **`EXIT=0`**（2026-06-28）；L5 FAST `ok=21 skip=102 wall=8s`；P1 safe-ffi timeout WARN + safe-leak skip。 |
| 86 | **真 gen1/gen2 + smoke 修** | ✅ postlink **`-backend c -o`** 绿；A-09 fixed point **3495984B** / `6d7d14da…`。**遗留**：fresh `bootstrap-driver-seed` 链 **3596568B** 在 `grow_vec_init→calloc` **SIGSEGV**（gdb）；须回退 Git pinned 作 Stage0。 |
| 87 | **防塌陷 gate** | ✅ `tests/lib/bootstrap-anti-collapse.sh` + `run-bootstrap-anti-collapse-gate.sh`；smoke 写 `logs/bootstrap-*.fresh|pinned-fallback`；W3 默认 **`SHUX_BOOTSTRAP_COLD_START_PINNED=1`**（显式冷启动 pinned，**仍要求 gen1≠pinned**）；**禁止** postlink 复制 `./shux`。 |
| 88 | **G-FFI-3/4 LANG-007 v2 gate** | ✅ Docker linux/amd64：`make relink-shux` 后 **`./tests/run-lang-unsafe-gate.sh` 全绿**（U1–U4 + S0 ub/region + U1 struct）。**实现**：C 前端 `unsafe { }` + S0 硬拒；`-backend c -o` 与 `check` 对齐；C parser 后缀 `T[]`/`T[N]`。**遗留**：relink 默认 asm `-o` sx parse 仍 0 func；**G-FFI-5** 未闭合。 |
| 89 | **G-FFI-5 std unsafe 包裹** | ✅ `std/ffi` + `std/sys/*.x` 全部 `extern` 调用包 `unsafe { }`；**`run-g-ffi-5-std-wrap-gate.sh`** Docker 绿；挂接 `run-lang-unsafe-gate.sh` + `run-portable-suite.sh`。**遗留**：bstrict 业务 `tests/` 零裸 `extern`、release CI 全量仍待。 |
| 90 | **真实 Linux 键 C FAST 复验** | ✅ ubuntu-server（2026-06-29）：`crt0_x86_64.s` 栈对齐修复后，又补 `sx_seed_bridge.c` 两个 seed 桥接缺口：`typeck_preprocess_x_buf -> preprocess_x_buf` 与 `std_heap_alloc_zero -> std_heap_alloc_zeroed`。随后修复 partial seed 选择策略：仅真 partial 可复用/入库，污染的 Linux weak stub seed 已删除；`./tests/run-bootstrap-symbol-visibility-gate.sh` 已绿。 |
| 91 | **C9 非 TTY stdout 真机闭合** | ✅ ubuntu-server（2026-06-30）：修复 `shux` sibling `shux-c` 委托时 `argv[0]` 仍为 `shux` 导致的递归 fork/exec；新增 `run-non-tty-stdout-gate.sh`，`./compiler/shux ... >/tmp/log 2>&1` 不再 timeout / SIGTERM。 |
| 92 | **C2 泛型 expects/got 诊断真机闭合** | ✅ ubuntu-server（2026-06-30）：新增 `run-typeck-generic-args-gate.sh`；`./compiler/shux -L . tests/generic/wrong_type_args.x` 已稳定输出 `generic function 'id' expects 1 type arguments, got 2 at 4:26`。 |
| 93 | **G-FFI-5 真机 `lang-unsafe` 闭合** | ✅ ubuntu-server（2026-06-30）：`typeck_allow_legacy_extern_calls` 改为仅在 `-E` 兼容路径显式放行；`tests/unsafe/extern_outside_unsafe_fail.x` 已被真实拒绝，`run-lang-unsafe-gate.sh` 全绿。 |
| 94 | **SAFE-002/003 真机闭合** | ✅ ubuntu-server（2026-06-30）：`run-safe-unsafe-api-gate.sh` 增补“审计名 → 实际导出名”映射；`safe-unsafe-audit.tsv` 与 `safe-unsafe-extern.tsv` 已补 `join` 对齐及 `crash_evidence_collect` 审计；`run-safe-unsafe-audit-gate.sh` 全绿。 |
| 95 | **Darwin 本地 bstrict 重打通** | ✅ macOS arm64（2026-06-30）：继续沿 `src/asm/pipeline_glue_strict_minimal.c` 的 strict minimal bridge 收敛缺口，补齐 `pipeline_typeck_resolve_call_func_index_c` 后，`make bootstrap-verify-bstrict` 本地返回 `EXIT=0`；该结果仅作开发机侧排障进展记录，最终验收仍以 `ubuntu-server` 为准。 |
| 96 | **G-02a 可行性分析 + 策略调整** | ✅ macOS arm64（2026-07-07）：对比 C 前端 `.o` 与 X pipeline `.o` 强符号覆盖。结论：仅 `ast_seed.o` 被 `ast_x.o` 完全覆盖（0 C-only 符号）；`typeck.o` 有 48 个 C-only 强符号（`typeck_module` 等），`codegen.o` 有 126 个，`autovec.o` 无 `.x` 替代。`SHUX_NO_C_SEED_LINK=1` 的 weak stubs 是空实现，typeck 失效（`return_operand_type_mismatch.x` 报 codegen error 而非 typeck error）。**策略调整**：原顺序 `G-02a → G-02c` 改为 `G-02c → G-02a`，先在 G-02c 中将 C 前端入口函数迁移到 `.x`。 |

**仍未闭合**：~~真 gen1/gen2~~ ✅、~~防塌陷 gate~~ ✅（#87）、~~LANG-007 v2 gate~~ ✅（#88/#93）、~~G-FFI std 包裹~~ ✅（#89/#94）、~~V7 符号可见性~~ ✅（真实 Linux gate 已绿；污染 weak stub seed 已删除）、~~C9 非 TTY stdout~~ ✅（#91）、G-03 全链、G-02 compiler 物理删 C、G-04～G-07、**G-FFI-5 尾声**（bstrict 业务零裸 `extern` / release CI）、**bootstrap-bstrict 全量绿**（staging）、**run_shux_asm_smoke asm `-o` SIGSEGV**、**P1 safe-leak ASAN 全量**、**刷新 Git pinned seed 至 fixed point**（审 sha256 后单文件 commit）。

---

### 5.1 防塌陷（鸡生蛋链不可静默断裂）

自举循环：**Stage0（冷启动 seed）→ gen1（shux_asm_stage1）→ gen2（shux_asm2）→ SHA256(gen1)==SHA256(gen2)**。下列情况视为**塌陷**（表面绿、实际未自举）：

| 塌陷模式 | 表现 | 检测 |
|----------|------|------|
| **seed 静默回退** | `make bootstrap-driver-seed` 失败 → `cp seeds/bootstrap_shuxc.* ./shux` 无审计 | `logs/bootstrap-seed.pinned-fallback` + gate FAIL（除非 `SHUX_BOOTSTRAP_COLD_START_PINNED=1`） |
| **postlink 复制** | strict `shux_asm` 烟测失败 → `cp ./shux` 或 `experimental` 覆盖 | `logs/bootstrap-postlink.compiler-fallback` → gate **硬 FAIL** |
| **假 fixed point** | stage1==stage2==pinned 同 SHA256 | gate FAIL（triple equal） |
| **假 gen1** | stage1==seed（gen1 未编译新一代） | gate FAIL |

**允许的唯一「外部蛋」**：Git 中 **`seeds/bootstrap_shuxc.<os>.<arch>`** 作 Stage0 冷启动（须显式 `SHUX_BOOTSTRAP_COLD_START_PINNED=1`），但 **gen1 必须与 pinned 不同字节/哈希**，且 gen2 须与 gen1 一致。禁止在 gate 内复制 prebuilt 冒充 stage1/2。

```bash
# W3 gold（Codespace）
./tests/run-w3-gold-codespace.sh
# 审计日志
cat logs/bootstrap-audit.log
./tests/run-bootstrap-anti-collapse-gate.sh
```

**当前 Stage0 阻塞**：自源码 `make bootstrap-driver-seed` 产出 **3596568B** 在 `-c` 路径 **SIGSEGV**（`driver_emit_sidecar_get` → `grow_vec_init` → libc `calloc`）；W3 暂用 **4178600B Git pinned** 冷启动，gen1 **3495984B** 证明真编译发生。

---

## 6. 当前阻塞（2026-07-07）

1. **键 C 主阻塞**：`§9.1 Codegen 语义债` 仍未系统清零；对应 `run-codegen-semantic-debt-gate.sh` 仍需作为下一优先级收口。
2. **staging 全量**：`run-all-bstrict.sh` 尚未全绿；当前不阻塞 bootstrap-min / gold，但仍阻塞 `G-FFI-5` 尾声中的“业务零裸 extern”。
3. **G-03 全链**：`-nostdlib` 终局路径与 `G-03-rt`（隐式 compiler-rt / memcpy 符号审计）仍未彻底闭合。
4. **G-02 物理删 C**：compiler 侧仍有 **289** 个 `.c` + **42** 个 `.h`；G-06/V7 已闭合后，可进入 G-02a。
5. **G-FFI-5 尾声**：真实 Linux `lang-unsafe`、SAFE-002/003、audit hook 已绿；剩余只在 bstrict 业务零裸 `extern` 与 release CI。
6. **固定点种子治理**：`bootstrap_shuxc` / pinned seed 仍需在审 sha256 后刷新单文件 commit，避免长期保留历史冷启动二进制。

### 6.1 G-02a 阻塞详情（2026-07-07 新增）

**问题**：G-02a（删 LEGACY 前端 C）当前不可行。X pipeline 的 `.x` 代码使用完全不同的内部符号名，与 C 前端公共 API 名不兼容。`SHUX_NO_C_SEED_LINK=1` 路径的 weak stubs 只是链接占位符（返回 -1 或空），不是功能实现。

**强符号覆盖分析**（C 前端 `.o` 有但 X pipeline `.o` 没有的强符号）：

| C 前端文件 | C-only 强符号 | stubs 覆盖 | 可删？ |
|---|---|---|---|
| `ast/ast.c` | 0 | — | ✅ 完全覆盖 |
| `parser/parser.c` | 1 (`_parse`) | weak stub（空） | ⚠️ 边缘 |
| `lexer/lexer.c` | 6 | 3/6 | ❌ 3 个 `cfg_*` 未覆盖 |
| `preprocess.c` | 3 | 1/3 | ❌ 2 个未覆盖 |
| `typeck/typeck.c` | 48 | 2/48 | ❌ 46 个未覆盖 |
| `codegen/codegen.c` | 126 | ~4/126 | ❌ ~122 个未覆盖 |
| `codegen/autovec.c` | 7 | 0/7 | ❌ 无 `.x` 替代 |

**实测验证**：`SHUX_NO_C_SEED_LINK=1` 构建的 shux-x 对 `return_operand_type_mismatch.x` 报 `codegen error[CG002]`（typeck 失效），而默认构建正确报 `typeck error[T001]`。**2026-07-08 修复后**：`SHUX_NO_C_SEED_LINK=1` shux-x 正确报 `typeck error: return expression type mismatch: expected i32, found bool`（EXIT=1），与默认构建行为一致。

**根因定位与修复（2026-07-08，commit 1790d12f）**：`token.x` 的 `TokenKind` 枚举比 `include/token.h` 少 8 个 `TOKEN_ATTR_*` 条目（GLOBAL_ALLOCATOR/COLD/INLINE_NEVER/INLINE_ALWAYS/EXPORT_NAME/PANIC_HANDLER/THREAD_LOCAL/PERCPU）。`lexer_gen.c` 枚举经 `sync_lexer_gen_token_enum.pl` 从 `token.h` 同步（TOKEN_IDENT=59），而 `parser_gen.c` 枚举从 `token.x` 生成（TOKEN_IDENT=51）。lexer 正确 lex "main" 为 IDENT(kind=59) 但 parser 将 59 解释为 TOKEN_ISIZE，导致所有函数解析失败（num_funcs=0）、typeck 被跳过、check 静默成功。修复：token.x 补齐 8 个条目 + Makefile 为 `parser_x.o`/`pipeline_x.o` 添加枚举同步步骤。修复后 `SHUX_NO_C_SEED_LINK=1` shux-x 正确报 `typeck error: return expression type mismatch: expected i32, found bool`（EXIT=1）。

**解决路径（2026-07-08 God-View 修订）**：原计划"迁移 `typeck_module`/`codegen_module_to_c`/`lexer_new` 入口到 `.x`"**不再必要**。God-View 全局分析发现：

1. **SHUX_NO_C_FRONTEND 模式下不调用这些 C API** — runtime.c 走 `shux_pipeline_run_x_pipeline_large_stack`（[runtime.c:5062](file:///home/shu/shux/compiler/src/runtime.c#L5062)），不经过 `typeck_module`/`codegen_module_to_c`/`lexer_new`。这些 C API 的 weak stubs（返回 -1/NULL）根本不会被调用。
2. **X pipeline 在 Linux x86_64 上已完全工作** — 2026-07-08 ubuntu-server 真机验证：`SHUX_NO_C_SEED_LINK=1` 构建的 shux-x，check ✅（typeck error 正确报告）、compile ✅（`function main(): i32 { return 42; }` 编译运行返回 42）、hello ✅、binary-expr ✅、return-value ✅、types gate check+link+run ✅。仅 1 个 typeck 测试有差异（`import_const_bare_fail.x`）。
3. **macOS compile 失败是 arm64 后端 stub 导致** — `arch_arm64_enc_enc_label` 在 `seed_link_compat.c:846` 是 weak stub（返回 -1），真正的 arm64 后端实现未链入。这是平台限制，非 X pipeline 问题。

**G-02c 第一步（X pipeline parser 枚举修复）已完成** ✅（commit 1790d12f）。**G-02c 后续步骤（迁移 C API 入口）不再必要**，可直接进入 G-02a（物理删除 LEGACY C 前端 .c 文件），前提是将默认构建也切换到 `SHUX_NO_C_SEED_LINK=1`（no_c）模式。

---

## 7. 下一步（建议顺序）

| 序 | 动作 | 命令 | 状态 |
|----|------|------|------|
| 0 | **bootstrap-min 日常验收** | `SHUX_LINK_SHUX=./compiler/shux_asm ./tests/run-bootstrap-min-gate.sh` | ✅ |
| 1 | **bootstrap-gold（发布）** | `./tests/run-w3-gold-codespace.sh`（Codespace 原生） | ✅ inner DONE 2026-06-28（真 gen1/gen2 3495984B；#86） |
| ~~2~~ | ~~G-06 partial pin~~ | ~~`seeds/asm_backend_partial.linux.x86_64.o` + preflight~~ | ✅ 177KB 入库 |
| ~~3~~ | ~~typeck relink~~ | ~~`cd compiler && ./scripts/relink_shux_asm_strict_glue.sh`~~ | ✅ 自举闭环 |
| ~~4~~ | ~~G-01 单文件 commit + 基线~~ | ~~对每个 core 变更 `git add && git commit`~~ | ✅ f08 gate 绿 |
| 5 | **键 C：语义债 gate** | `./tests/run-codegen-semantic-debt-gate.sh` | 🟡 |
| 6 | **staging** bstrict 续跑 | `SHUX_LINK_SHUX=./compiler/shux_asm ./tests/run-all-bstrict.sh` | 🟡 |
| 7 | **G-03 no-libc** | `SHUX_NOLIBC_N07_V5_FAIL=1 ./tests/run-nolibc-n07-v5-gate.sh` | ⬜ |
| 8 | **G-02c C 前端入口迁移到 `.x`** | ~~将 `typeck_module`/`codegen_module_to_c`/`lexer_new` 等迁移到 `.x`~~ **不再必要** | ✅ God-View 分析确认 X pipeline 已有完整独立路径 |
| 9 | **G-02a 删 LEGACY 前端 C** | 默认构建切 no_c 模式 + 每文件 `git rm` + `run-e-soft-retire-gate.sh` | 🟡 可推进（X pipeline 已验证） |
| 10 | 终局哨兵 | `SHUX_NO_HANDWRITTEN_C_STRICT=1 ./tests/run-no-handwritten-c-gate.sh` | ⬜ |
| ∥ | **G-FFI-5 尾声**（并行，非阻塞） | 现仅剩 bstrict 业务零裸 `extern` + release CI；见 §3.1、§10.10 | 🟡 |

---

## 8. 验收命令速查

```bash
# bootstrap-min（日常自举，~25 项）
SHUX_LINK_SHUX=./compiler/shux_asm ./tests/run-bootstrap-min-gate.sh

# 语义自举
make -C compiler bootstrap-verify

# 生产 shux_asm（B-strict）
make -C compiler bootstrap-driver-bstrict

# Stage2 SHA256 金标准
SHUX_D03_FAIL=1 ./tests/run-d03-stage2-hash-gate.sh

# std/core 存量
./tests/run-std-c-inventory-gate.sh

# E-soft 软退役
SHUX_E_SOFT_FAIL=1 ./tests/run-e-soft-retire-gate.sh

# F-no-libc 聚合
SHUX_NOLIBC_FAIL=1 ./tests/run-no-libc-gate.sh

# 流式全量回归（Linux x86_64）
./tests/run-full-suite-stream.sh

# Docker Linux 黄金自举（A-09～A-12）
./tests/run-linux-a09-a11-gate.sh

# 冷启动 / Stage0→1
./tests/run-d01-stage0-to-stage1-gate.sh
./tests/run-e03-v3-coldstart-track-gate.sh

# G-FFI LANG-007 v2（#88 gate ✅；relink 后 run 路径 -backend c -o）
cd compiler && make relink-shux && cd ..
SHUX=./compiler/shux ./tests/run-lang-unsafe-gate.sh

# G-FFI-5 std/ffi + std/sys unsafe 包裹（#89）
SHUX=./compiler/shux-c ./tests/run-g-ffi-5-std-wrap-gate.sh
```

**推荐顺序（2026-06-25 修订）**：W0 ✅ → W1 ✅ → W2 ✅ → **W3 ub `-o` + L5 run-all** → G-03 → G-02 分批删 C → G-04/05/07。

---

## 9. 性能压榨进度（L1–L6，与 §10 同步勾选）

> **战略与六层模型**：[`性能压榨.md`](性能压榨.md) §7（L1 语义 … L6 门禁 … L3 std IO … L5 编译器吞吐）。  
> **本节为性能相关 gate 的单一事实来源**；完成一项即在对应 `- [ ]` 改为 `- [x]`，表格状态列同步 ✅/⬜。  
> **快照**：2026-06-24。

### 9.0 总览（估算完成度）

| 块 | 含义 | 完成度 | 自举后再做？ | §9 小节 |
|----|------|--------|--------------|---------|
| **L1** | typeck / region / bootstrap 链闭合 | 🟡 ~88% | **否** | §9.1 |
| **L6** | dogfood / bench / perf 门禁不回退 | 🟢 ~90% | **否** | §9.2 |
| **L4** | fold / inline / SIMD 定点（有 bench） | 🟢 ~75% | 大项 **是**（IR/MLIR） | §9.3 |
| **L2** | 用户 Arena 热路径零 malloc | 🟢 ~85% | 用户路径 **否** | §9.4 |
| **L3** | std IO/net 深度优化 | 🟡 按需 | **是**（无瓶颈时不占带宽） | §9.5 |
| **L5** | 编译器 Lexer/typeck SIMD 吞吐 | ⬜ ~5% | **是**（M2 后） | §9.5 |

```text
自举前必须持续                    │  自举完成后再系统做
──────────────────────────────────┼────────────────────────────────
L1  typeck / bootstrap 链闭合     │  L5  SIMD Lexer、symbol SIMD
L6  dogfood / bench 不回归        │  全仓库 phase timing TSV 报告
L4  有 bench 的 fold / 补洞       │  autovec 大迁移、IR/SSA、MLIR
L2  用户 Arena PERF-007           │  编译器 dogfood alloc 总量 cap
```

### 9.1 L1 — 语义 / typeck / 自举链（性能前提）

**目标**：安全子集可证 + 前端 `.x` 闭环；**不是**额外 perf 项目。L1 未闭合时禁止为追 perf 赌 UB 或上 Lexer SIMD（L5）。

#### 9.1.1 已闭合

- [x] 前端 7 模块**运行时** `.x`（lexer … codegen）— 见 §4.1
- [x] D+E+F v1（语义自举 + std 无 C）— `make bootstrap-verify`
- [x] region 诊断 v1 — `./tests/run-typeck-region.sh`
- [x] linear 诊断 v1 — `./tests/run-typeck-linear.sh`
- [x] borrow 冲突 / 误报矩阵 — `./tests/run-type-borrow-conflict-gate.sh`
- [x] scope borrow / AL-06 双重逃逸 — `./tests/run-scope-borrow-gate.sh` + `./tests/run-al06-gate.sh`（W3 P0，2026-06-24 ✅）
- [x] typeck 大模块 parse 计数（A-11）— `./tests/run-typeck-parse-count-gate.sh`
- [x] G-06 W1 bootstrap Docker — `./tests/run-g06-bootstrap-docker.sh`
- [x] **d03 Stage2 SHA256** — `./tests/run-d03-stage2-hash-gate.sh`（2026-06-24 ✅）
- [x] W3 子集：A-09 hash + A-11 + A-12 — Codespace `run-linux-a09-a11-gate-inner.sh` **2026-06-27 ✅**（A-11 含 source fallback；gen2= pinned seed 同体）

#### 9.1.2 进行中 / 未闭合

- [x] **ub gate** — `./tests/run-ub.sh`（asm bounds panic，2026-06-24 ✅）
- [x] **i64-ctfe gate 全绿** — `./tests/run-i64-ctfe-gate.sh`（seed+glue+relink，2026-06-24 ✅）
- [x] **W3 A-10 path/net `.o`** — `std/path/path.o` + `std/net/net.o` asm 绿（2026-06-25 ✅；6598B / 29320B）
- [x] **with-arena-vec gate** — `./tests/run-with-arena-vec-gate.sh`（hash.x 链修复后 2026-06-25 ✅）
- [x] **`run-bootstrap-min-gate.sh` Docker 绿** — P0×3 + 语言×14（2026-06-27 ✅；`shux-min-link` gcc 回退）
- [x] **`run-linux-a09-a11-gate-inner.sh` 全链绿**（bootstrap-gold；`LINUX … DONE` + `EXIT=0`）— ✅ Codespace 2026-06-28（`run-w3-gold-codespace.sh` + `SHUX_W3_RESUME_FROM=p1`）
- [ ] **`run-all-bstrict.sh` 全量绿** — **staging**（非 bootstrap-min 阻塞；net/vec 等允许 gcc 回退）
- [ ] **`bootstrap_shuxc -E` 全模块**（含 LSP / typeck 大 TU）— Docker / 本地 `-E` 无失败
- [ ] typeck 兼容债清零（布局 / `u8[64]` 等）— `-E` + typeck 测试全绿
- [ ] G-03 no-libc bootstrap **硬绿** — `./tests/run-nolibc-n07-v5-gate.sh`（§10.3）
- [ ] G 终局 G-02～G-07、compiler 物理零 C — §10.4～10.8

### 9.2 L6 — 观测与 perf 门禁（自举期间守住）

**目标**：自举改动不 silently 拖慢编译路径、不破坏 bench 契约。

#### 9.2.1 已接入 gate

- [x] OBS-001 阶段计时开关 — `SHUX_COMPILE_PHASE_TIMING=1` + `./tests/run-obs-compile-phase-timing-gate.sh`
- [x] PERF-004 compile dogfood + TSV — `./tests/run-perf-compile-dogfood-gate.sh`
- [x] PERF-007 alloc hotspot manifest — `./tests/run-perf-alloc-hotspot-gate.sh`
- [x] PERF-005 flamegraph manifest — `./tests/run-perf-flamegraph-gate.sh`
- [x] BOOT-012 自举 perf 回归 manifest — `./tests/run-bootstrap-perf-gate.sh`
- [x] CI full suite 接线（perf baseline + dogfood）— `tests/run-ci-full-suite.sh`
- [x] W1 fast-track P0 安全 gate 接入 — `./tests/run-g-fast-track.sh --p0`（borrow + al06）

#### 9.2.2 增强项（自举后或 P1）

- [ ] compile dogfood **默认硬失败** 全平台稳定（无 native shux 时不 SKIP）— `SHUX_PERF_FAIL_ON_COMPILE_REGRESSION=1`
- [ ] 全仓库 phase timing **占比报告**入 baseline — compiler/src 全量 TSV 汇总
- [ ] PERF-001 **追 Zig 全 PR 硬门禁** — `SHUX_PERF_FAIL_ON_ZIG=1`（当前可选 / P1）
- [ ] PERF weekly 常态化 — `./tests/run-perf-weekly-gate.sh`

**改 compiler 前端 / pipeline / typeck 后最低操作**：

```bash
SHUX_PERF_FAIL_ON_COMPILE_REGRESSION=1 ./tests/run-perf-compile-dogfood.sh
# 可选：Linux + perf 定位 typeck 热点
./tests/run-perf-flamegraph-gate.sh
```

### 9.3 L4 — 生成代码定点（小步、有 bench）

**目标**：已有优化不回退；仅 bench 驱动补洞。禁止自举前 IR/MLIR 大改。

#### 9.3.1 已闭合

- [x] while 计数折叠 — `tests/bench/loop_i32.x` + `try_fold_count_up_while`
- [x] struct 参数内联 — `tests/bench/struct_param.x`
- [x] SIMD 编码 + loop peel — `simd_enc.c` / `simd_loop.c`
- [x] 向量类型 + `@shuffle` / `@select` — parser + codegen
- [x] P0 四 microbench — `./tests/run-perf-baseline.sh --bench`
- [x] WPO/DCE 文本回归 — CI `run-perf-wpo-*`
- [x] loop autovec v1/v2 策略 gate — `./tests/run-std-simd-autovec-strategy-gate.sh` + `./tests/run-autovec-loop-gate.sh`

#### 9.3.2 进行中 / 自举后

- [ ] **`autovec.c` → `.x` 等价**（G-02a 删 C 前）— 见 §10.4 `codegen/autovec.c`
- [ ] Shu asm median **≤ Zig -O2** 稳定达标 — `SHUX_PERF_FAIL_ON_ZIG=1`（P2，自举后强化）
- [ ] 阶段 8 大项（IR/SSA、`-Os` 全量）— M1/M2 后，**明确自举后**

**若动了 codegen / asm**：

```bash
./tests/run-perf-baseline.sh --bench
```

### 9.4 L2 — 热路径零 malloc（底线）

**目标**：用户侧 Arena **PERF-007 零 per-op heap**；编译器侧 ast pool 持续池化。

#### 9.4.1 已闭合

- [x] PERF-007 定版 + 三 case cap=0 — `string_arena_concat` / `cl_arena64_smoke` / `with_arena_vec_push`
- [x] gate + portable suite — `./tests/run-perf-alloc-hotspot-gate.sh`
- [x] `with_arena` / 字符串 arena 语义 — MEM-C1、ZC-4 相关 gate
- [x] `vec_*_push_arena` with_arena 单态化 — `./tests/run-with-arena-vec-gate.sh`
- [x] owned 全路径 deinit — `./tests/run-owned-return-gate.sh`

#### 9.4.2 进行中 / 自举后

- [ ] 编译器 parse/typeck/LSP **减 alloc**（ast pool / grow pool；无 dogfood alloc cap 硬门禁）
- [ ] alloc **总量**埋点与治理 — PERF-007 v2，**自举后**

**若动了 Arena / std.heap**：

```bash
./tests/run-perf-alloc-hotspot-gate.sh
```

### 9.5 L3 / L5 — 明确推迟（仅占位，不阻塞自举）

> 有 benchmark / 用户瓶颈报告再立项；详见 [`性能压榨.md`](性能压榨.md) §7.7.7。

| 层级 | 代表项 | 状态 | 触发条件 |
|------|--------|------|----------|
| L3 | io_uring multishot、net 批量化 | 🟡 部分 ✅ | `./tests/run-perf-baseline.sh --bench` 或 net gate 变红 |
| L3 | DPDK / 舒 IO 自研 ring | ⬜ | 业务 net 瓶颈报告 |
| L5 | SIMD Lexer 空白/注释跳过 | ⬜ | M2 + typeck 全绿 + phase timing 基线 |
| L5 | symbol intern + SIMD probe | ⬜ | profiling 证明 typeck 占比 >40% |
| L6+ | 全仓库 phase TSV 报告 | ⬜ | 自举 G 主链闭合后 |

### 9.6 合并验收命令（L1 + L6 + L4 + L2）

改 **compiler 前端 / pipeline / typeck** 后建议顺序：

```bash
# L1 语义
./tests/run-typeck-region.sh
./tests/run-typeck-linear.sh
./tests/run-type-borrow-conflict-gate.sh
./tests/run-al06-gate.sh

# L6 编译路径不回退
SHUX_PERF_FAIL_ON_COMPILE_REGRESSION=1 ./tests/run-perf-compile-dogfood.sh

# L4（若动 codegen/asm）
./tests/run-perf-baseline.sh --bench

# L2（若动 Arena/std.heap）
./tests/run-perf-alloc-hotspot-gate.sh

# L1 全量自举（与 §7、§10 对齐）
./tests/run-d03-stage2-hash-gate.sh
./tests/run-linux-a09-a11-gate.sh
```

---

## 11. 执行波次 — 高效率测试（定稿任务列表）

> **日常入口**：`./tests/run-g-fast-track.sh`（W0+W1，约 1～3 分钟）  
> **全链**：`./tests/run-g-fast-track.sh --w3` 或 `./tests/run-full-suite-stream.sh`  
> **原则**：fail-fast、实时 `progress-run` + 日志、Docker dev 镜像免 apt-get、**先绿简单波次再攻阻塞项**。

### 11.1 波次一览

| 波次 | 预算 | 入口 | 通过含义 |
|------|------|------|----------|
| **W0** | ~30s | `./tests/run-g-fast-track.sh --w0-only` | 清单/manifest/E-soft 审计绿 |
| **W1** | ~1～3min | `./tests/run-g-fast-track.sh` | G-06 冷启动链绿（stub partial 可接受） |
| **W1+P0** | +~30s | `./tests/run-g-fast-track.sh --p0` | 须 bstrict 完整 shux 后跑安全 gate |
| **W2** | ~10～20min | `./tests/run-g-fast-track.sh --w2` | d01 + e03 + d03 SHA256 |
| **W3** | ~1h+ | `./tests/run-g-fast-track.sh --w3` | linux-a09 黄金链全 gate |

### 11.2 任务队列（简单 → 难，按序勾选）

#### 队列 A — 立即可做（无 `-E` 依赖）

| ID | 任务 | 验收 | 状态 |
|----|------|------|------|
| A-01 | W0 清单 gate 全绿 | `run-g-fast-track.sh --w0-only` | ⬜ |
| A-02 | W1 G-06 bootstrap（stub partial） | `run-g06-bootstrap-docker.sh` | ✅ |
| A-03 | W1 P0 安全 gate 接入 | borrow + al06 in fast-track | ✅ |
| A-04 | seeds + gen 产物 **git 单文件 commit** | `compiler/seeds/*` 18×`.c` + bootstrap_shuxc | ⬜ |
| A-05 | G-01 core 删除 **git commit** + f08 | `run-f08-core-inventory-gate.sh` | ⬜ |
| A-06 | 更新 f08 manifest（core=0）若仍引用旧 `.c` | W0 f08 绿 | ⬜ |

#### 队列 B — G-06 真 partial（当前主阻塞）

| ID | 任务 | 验收 | 状态 |
|----|------|------|------|
| B-01 | 定位 `-E` typeck：`ast_placeholder` implicit tail return | `shux-seed-phase1 -E ast.x` stderr | ⬜ |
| B-02 | 修 .x pipeline dep prerun / typeck 与 seed 漂移 | `-E asm.x` 产出非空 C | ⬜ |
| B-03 | `build_seed_asm_host` 产出强符号 `seed_mega` | `nm partial \| grep ' T .*seed_mega'` | ⬜ |
| B-04 | 入库 `seeds/asm_backend_partial.linux.x86_64.o` | 仅允许带强 `seed_mega` 的真 partial；preflight + bootstrap 无 stub WARN | ⬜ |
| B-05 | W2：`run-d01` + `run-e03` 冷启动 track | `--w2` 绿 | ✅ |

#### 队列 C — 黄金自举巩固（B 完成后）

| ID | 任务 | 验收 | 状态 |
|----|------|------|------|
| C-01 | W2 d03 Stage2 SHA256 | `SHUX_D03_FAIL=1 run-d03-stage2-hash-gate.sh` | ✅ |
| C-02 | W3 linux-a09-a11 全链 | `run-linux-a09-a11-gate-inner.sh` | ✅ Codespace inner DONE 2026-06-28 |
| C-03 | `run-full-suite-stream.sh` Tier1～3 | bstrict + d03 + e-soft | ⬜ |

#### 队列 D — G-03 no-libc（可与 B 并行调研）

| ID | 任务 | 验收 | 状态 |
|----|------|------|------|
| D-01 | 修 `test.o` 重复链接 | nostdlib hello 链通 | ⬜ |
| D-02 | 链接行去 `-lc/-lm` | `build_shux_asm.sh` | ⬜ |
| D-03 | NL-07 v5 硬绿 | `SHUX_NOLIBC_N07_V5_FAIL=1 run-nolibc-n07-v5-gate.sh` | ⬜ |

#### 队列 E — 物理删 C（须 B+C 绿后）

| ID | 任务 | 验收 | 状态 |
|----|------|------|------|
| E-01 | G-02a 删 LEGACY 前端 8×`.c` | 每文件单 commit + e-soft | ⬜ |
| E-02 | G-02b runtime ABI 收薄 | `run-e04-runtime-soft-gate.sh` | ⬜ |
| E-03 | G-02c asm/LSP glue | bstrict CI | ✅ God-View 确认非必须 |
| E-04 | G-04 STRICT 哨兵 | `SHUX_NO_HANDWRITTEN_C_STRICT=1` | ⬜ |
| E-05 | G-05 Makefile → build.x | 唯一入口 | ⬜ |
| E-06 | G-07 `vX.Y.Z-selfhost` tag | release prep gate | ⬜ |

### 11.3 当前阻塞（单一事实）

1. **W3 L5 `run-stdlib-import`**：`core_result_ok_i32` 未定义；`core_types_placeholder` 与 base64.o 重复定义。
2. **真 `asm_backend_partial`**：`shux-seed-phase1 -E asm.x` 仍未稳定产出真 partial；当前已禁止把 phase1 weak stub 当作 seed 复用/入库。
3. **G-06 partial pin**：`seeds/asm_backend_partial.linux.x86_64.o` 待真 partial 稳定后再恢复入库。
4. ~~**stale bootstrap_shuxc**~~：`driver_gen.c` / `pipeline_gen.c` 等 `-E/-E-extern` 生成目标已统一前置 `legacy-shux-c-ready`，执行前强制刷新 legacy `shux-c`，不再继续吃 `bootstrap_shuxc` 的旧 frontend 语义。✅ 2026-06-30 本地已越过 `src/codegen/codegen.x:199 extern call requires unsafe block` 这一公共阻塞点。
5. **Darwin host link / object format**：本地 `make relink-shux` 继续前进后，当前停在 `ld: ... unknown file type ...`；这是 Darwin 专属链接阶段问题，不作为最终验收结论，最终仍以 Ubuntu Server 真机 `git pull --ff-only` 后复验为准。
6. ~~**W3 P0 / lang-unsafe / packed**~~：✅ 2026-06-25。

### 11.4 推荐每日循环

```bash
# 1) 改代码前 / 改后快验（1～3 分钟）
./tests/run-g-fast-track.sh

# 2) 动 G-06 / seed 后（可选 asm-E，默认 W1 不跑以免等 5min）
SHUX_G06_E_TIMEOUT=120 ./tests/run-g06-preflight-docker.sh --asm-e

# 3) 阶段性（真 partial 就绪后）
./tests/run-g-fast-track.sh --w2

# 4) 发布前
./tests/run-g-fast-track.sh --w3
# 或 Linux 宿主：./tests/run-full-suite-stream.sh
```

---

## 12. 相关文档索引（原 §9）

| 文档 | 用途 |
|------|------|
| [`自举分析.md`](自举分析.md) | 战略定义、五关卡、风险原则 |
| [`NEXT.md`](NEXT.md) | 阶段 A～G 任务表、gate 明细 |
| [`compiler/docs/SELFHOST.md`](compiler/docs/SELFHOST.md) | 验收命令、链接拓扑 |
| [`analysis/doc-selfhost-architecture-v1.md`](analysis/doc-selfhost-architecture-v1.md) | 新人 1 小时全景 |
| [`analysis/phase-g-full-selfhost-tasks-v1.md`](analysis/phase-g-full-selfhost-tasks-v1.md) | G 批次详细任务 |
| [`analysis/phase-e-soft-v2-closure.md`](analysis/phase-e-soft-v2-closure.md) | E-soft 闭合记录 |
| [`analysis/phase-f-std-zero-c-v1.md`](analysis/phase-f-std-zero-c-v1.md) | std 零 C/H 终局 |
| [`analysis/性能压榨.md`](analysis/性能压榨.md) | L1–L6 六层模型；gate 勾选见 **本文 §9** |
| [`analysis/全面特性优化.md`](analysis/全面特性优化.md) | 特性 × gate 对照 |
| [`analysis/FFI隔离.md`](FFI隔离.md) | FFI 隔离阶段安排（§7）；G-FFI 并行轨 |
| [`analysis/lang-unsafe-v1-rfc.md`](lang-unsafe-v1-rfc.md) | LANG-007 S0/U1–U4；v2 `unsafe { }` |

---

## 10. 自举待办总清单（完成打勾）

> 规则：**删一个 C/H → 一个独立 commit**；每批跑 E-soft + D-03；每阶段更新本节勾选。

### 10.0 基础设施与基线

- [x] 阶段 0：`tests/run-full-suite-stream.sh` 流式全量入口
- [x] 阶段 0：`tests/lib/progress-run.sh` 支持 `SHUX_PROGRESS_TIMEOUT_SEC`
- [x] 阶段 0：`tests/run-all.sh` `[HH:MM:SS]` 进度戳
- [x] 阶段 0：`build_shux_asm.sh` 默认 `SHUX_BUILD_ASM_TIMEOUT=3600`
- [x] 阶段 0：`tests/lib/delete-one-c-file.sh` 单文件删 C 辅助
- [x] 阶段 0：`compiler/docs/SELFHOST.md` 日常全量回归说明
- [x] 阶段 1：工作区 checkpoint（180 个变更 **10 批 git commit** ✅）
- [ ] 阶段 1：Linux x86_64 `./tests/run-full-suite-stream.sh` 全绿
- [ ] 阶段 1：`SHUX_NO_HANDWRITTEN_C_STRICT=0 ./tests/run-no-handwritten-c-gate.sh` track 模式 OK

### 10.1 G-01 core 零 C/H

- [x] 删 `core/builtin/builtin.c`、`builtin_abi.h`（工作区）
- [x] 删 `core/debug/debug.c`（工作区）
- [x] 删 `core/mem/mem.c`（工作区）
- [x] 删 `core/slice/slice.c`（工作区）
- [x] 对应 `.x` 迁移 **git 单文件 commit** ✅
- [x] `./tests/run-f08-core-inventory-gate.sh` ✅ 绿
- [x] `./tests/run-std-c-inventory-gate.sh` → core=0 **且已提交** ✅

### 10.2 G-06 冷启动 seed（阻塞 G-02a）

- [x] `compiler/seeds/bootstrap_shuxc.linux.x86_64` 生成（本地）
- [ ] `bootstrap_shuxc` seed **git commit**
- [x] `lexer_gen` / `parser_gen` seed + Makefile 回退
- [x] `typeck_gen` / `codegen_gen` seed + Makefile 回退
- [x] `pipeline_gen` / `driver_gen` / `preprocess_gen` seed + Makefile 回退
- [x] `lsp_io_gen` / `lsp_gen` / `lsp_diag_gen` / `lsp_io_std_heap_gen` seed（extract_lsp_gen_seeds 去重符号）
- [x] `gen_g06_phase1_backend_stub.sh` + Makefile 冷启动 phase1 弱桩链
- [x] `runtime.c`：`SHUX_NO_C_FRONTEND` 下 `-E` 不再误拒泛型
- [x] `fix_asm_full_gen_c.pl`：`backend_pipeline_*` / `platform_elf_pipeline_*` / `backend_backend_*` 前缀修正 + glue 冲突清理
- [x] `asm_backend_compat_stubs.c`：`asm_types_format_u32*` / `asm_types_elf_read_u32_le` weak 桩
- [x] Docker W1：`./tests/run-g06-bootstrap-docker.sh` / `run-g-fast-track.sh` bootstrap-driver-seed 绿
- [x] `gen_asm_full_link_stubs.pl` + Makefile/`build_shux_asm` 链 `asm_full_link_stubs.o`（pipeline_x U 符号）
- [x] `bootstrap_seed_io_stubs.c` + `fs_posix_read_c` + peephole/seed_mega compat 桥
- [ ] `seeds/asm_backend_partial.linux.x86_64.o` 真 partial **重新稳定**入库：当前已删除污染 V7 的 Linux weak stub seed，待真 partial 稳定后再恢复
- [x] `./tests/run-g06-bootstrap-docker.sh` W1 绿（stub partial）
- [x] `./tests/run-d01-stage0-to-stage1-gate.sh`（W2 Docker）
- [x] `./tests/run-e03-v3-coldstart-track-gate.sh`（W2 Docker）
- [x] `./tests/run-d03-stage2-hash-gate.sh`（W2 Docker，2026-06-24）
- [x] `./tests/run-i64-ctfe-gate.sh` 全绿（2026-06-24；seed+relink）
- [x] `./tests/run-ub.sh`（W3 P0，2026-06-24）
- [x] `./tests/run-with-arena-vec-gate.sh`（2026-06-25；hash.x `hash_sipround_arr` 修链）
- [ ] `./tests/run-linux-a09-a11-gate.sh` 全链绿（🟡 P0 + lang-unsafe + **run-hello** ✅；**run-stdlib-import** 待闭合）

### 10.3 G-03 F-no-libc bootstrap

- [x] `make bootstrap-driver-seed` crt0 + `-nostartfiles`
- [x] `./shux min.x -o out`
- [x] `./shux -L .. hello.x`
- [x] `build_shux_asm` pipeline 二遍 `__text` 非零
- [x] Docker Stage2 SHA256 金标准（子集）
- [ ] 修 `test.o` 重复链接（hello + nostdlib 路径）
- [ ] `build_shux_asm.sh` 链接行去 `-lc/-lm`
- [ ] 删 `bootstrap_nostdlib_stubs.c` 等桩（G-03-02）
- [ ] `SHUX_NOLIBC_N07_V5_FAIL=1 ./tests/run-nolibc-n07-v5-gate.sh`
- [ ] `SHUX_NOLIBC_FAIL=1 ./tests/run-no-libc-gate.sh`

### 10.4 G-02a LEGACY 前端 C 删除（须 G-02c ✅ 后）

> **2026-07-07 修订**：G-02a 前置条件从「G-06 ✅」改为「G-02c ✅」。X pipeline 未完全覆盖 C 前端强符号，须先在 G-02c 中将 `typeck_module`/`codegen_module_to_c`/`lexer_new` 等入口迁移到 `.x`。详见 §6.1。

- [ ] 删 `compiler/src/ast/ast.c` + `include/ast.h`（✅ X 完全覆盖，可先行）
- [ ] 删 `compiler/src/lexer/lexer.c` + `lexer.h`（❌ 须先迁移 `cfg_*` 3 符号）
- [ ] 删 `compiler/src/parser/parser.c` + `parser.h`（⚠️ 须先迁移 `_parse`）
- [ ] 删 `compiler/src/typeck/typeck.c` + `typeck.h`（❌ 须先迁移 46 符号含 `typeck_module`）
- [ ] 删 `compiler/src/codegen/codegen.c` + `codegen.h`（❌ 须先迁移 ~122 符号含 `codegen_module_to_c`）
- [ ] 删 `compiler/src/codegen/autovec.c`（❌ 无 `.x` 替代，须先创建）
- [ ] 删 `compiler/src/preprocess.c` + `preprocess.h`（❌ 须先迁移 2 符号）
- [ ] 删 `compiler/src/lsp/lsp_diag.c`（须检查覆盖）
- [ ] 删 `include/token.h`（与 `token.x` 同步后）
- [ ] 每删一文件：`SHUX_E_SOFT_FAIL=1 ./tests/run-e-soft-retire-gate.sh` + D-03

### 10.5 G-02b runtime ABI 收薄

- [ ] `runtime.c` 收薄 → `driver.x` + 薄 ABI
- [ ] `runtime_abi.c/h` → `.x`
- [ ] `runtime_io_abi.c/h` → `.x`
- [ ] `runtime_proc_abi.c/h` → `.x`
- [ ] `runtime_link_abi.c/h` → `.x`（最后）
- [ ] `runtime_driver_abi.c/h` → `.x`
- [ ] `runtime_pipeline_abi.c/h` → `.x`
- [ ] `runtime_driver_diagnostic.c/h` → `.x`
- [ ] `runtime_c_import.c/h` → 删或 `.x`
- [ ] `main.c` → 仅 crt0 `.s`
- [ ] `./tests/run-e04-runtime-soft-gate.sh`

### 10.6 G-02c asm / LSP / driver glue（~40 文件）

- [x] **第一步：X pipeline parser 枚举一致性修复** ✅（2026-07-08，commit 1790d12f）— `token.x` 补齐 8 个 `TOKEN_ATTR_*` 条目，Makefile 为 `parser_x.o`/`pipeline_x.o` 添加 `sync_lexer_gen_token_enum.pl` 同步步骤；`SHUX_NO_C_SEED_LINK=1` shux-x check 正确报 typeck error
- [ ] `parser_asm_*_slice.c` 等收拢进 `parser.x`
- [ ] `pipeline_glue.c` / `pipeline_*_alias.c` → strict `.x` 别名
- [ ] `backend_call_dispatch.c` 等 → asm `.x`
- [ ] `sx_seed_bridge.c` / `ast_pool_l5_bridge.c` → bridge `.x`
- [ ] LSP 剩余 C（`lsp_state.c` 等）→ `.x`
- [ ] `./tests/run-bootstrap-bstrict-ci.sh`
- [ ] `./tests/run-l5-run-all-parity-gate.sh`

### 10.7 G-02d std 测试胶层 + 第三方 C（~80+ 文件）

- [ ] `asm/http/*.inc.c` 清理
- [ ] `asm/crypto/ed25519/*.c` → `.x` 或预编译 `.o` seed
- [ ] `build_runtime.c` / `build_tool_libc_bridge.c`（联动 G-05）
- [ ] `run-all-x.sh` 相关子集全绿

### 10.8 G-04 / G-05 / G-07 终局

- [ ] 刷新 F-09 whitelist → `summary_total_c=0`
- [ ] `SHUX_NO_HANDWRITTEN_C_STRICT=1 ./tests/run-no-handwritten-c-gate.sh` 硬绿
- [ ] Makefile 归档；`build.x` + `./build_tool ./shux asm` 唯一入口
- [ ] `./tests/run-f11-selfhost-release-prep-gate.sh`
- [ ] Tag `vX.Y.Z-selfhost`（D-03 + D-04 + F-11 checklist）
- [ ] `find compiler core std -name '*.c' -o -name '*.h'` → **0**

### 10.9 G-08 延后

- [ ] Windows MSYS2 B-strict
- [ ] Alpine B-strict

### 10.10 G-FFI FFI 隔离（阶段 G 并行轨，不阻塞 bootstrap-min）

> 依据 [`FFI隔离.md`](FFI隔离.md) §7.5；与 §3.1 任务表同步勾选。

**G-FFI-1 v1 底座（已定版 ✅）**

- [x] TYPE-004 `run-type-ffi-bridge-gate.sh`
- [x] SAFE-004 `run-safe-ffi-contract-gate.sh`
- [x] LANG-007 U1–U3 + `run-lang-unsafe-gate.sh`（U1 烟测）
- [x] `std/ffi` F-ZC（`phase-f-ffi-v1.md`）

**G-FFI-2 L3 维护（持续，不阻塞 G）**

- [x] 新增 `extern` → `safe-unsafe-extern.tsv` + SAFE-003 审计（2026-06-30：补 `crash_evidence_collect`）
- [ ] `std/sys`、`std/dynlib` 新 API 对外不暴露裸 `*T`
- [x] CI 保持：`run-safe-unsafe-audit-gate.sh`（真实 Linux 2026-06-30 ✅）

**G-FFI-3～4 LANG-007 v2（阶段 G 内并行 P0）**

- [x] parser：`unsafe { }` 块语法（C 前端 `parser.c` + sx `parser.x`/`parser_gen.c`）
- [x] typeck：S0 内 `extern` 调用 → error（C `typeck.c` + sx `pipeline_glue.c`）
- [x] typeck：S0 内 `*T` 解引用 → error（同上）
- [x] `tests/unsafe/` U4 case + `run-lang-unsafe-gate.sh` 扩展
- [x] bootstrap 重链 + `run-lang-unsafe-gate.sh` 全绿（Docker linux/amd64，#88）
- [x] `std/ffi`、`std/sys` 用 `unsafe { }` 包裹现有 `extern`（#89 gate：`run-g-ffi-5-std-wrap-gate.sh`）

**G-FFI-5 G 尾声 / G-07 前闭合**

- [x] `std/ffi`、`std/sys` 底层 `extern` 调用包 `unsafe { }`（#89 gate）
- [x] 真实 Linux `run-lang-unsafe-gate.sh` 全绿（`extern_outside_unsafe_fail.x` 已恢复真实拒错）
- [x] SAFE-002/003 + audit hook 真机全绿（含 API 名简短化映射与 `crash_evidence_collect` 审计）
- [ ] bstrict：业务 `tests/`（非 ffi/sys 专项）无裸 `extern`
- [ ] [安全路线.md](安全路线.md) §8「业务层零 unsafe」可验证
- [ ] release CI 纳入 LANG-007 v2 gate

**G-FFI-6 post-release（⏭ 不阻塞自举）**

- [ ] `CString` / `ForeignBuf` 边界类型（v3）
- [ ] `*T` provenance 与 TYPE-002 region 联动
- [ ] `std/dynlib` 类型化 `sym`
- [ ] 不可信插件：子进程 + IPC；B 级 Wasm / Capability RFC

---

*更新本文时请同步：§2 仪表盘、`find` 实测 C/H 计数、**§9 性能勾选**、§10 勾选（含 **§10.10 G-FFI**）、§5 变更记录、[`性能压榨.md`](性能压榨.md) §7.7 指针、以及 [`NEXT.md`](NEXT.md) §0.1 / §11。*
