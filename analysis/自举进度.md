# Xlang / X language 自举进度

> **更新**：2026-08-11 · Stage 12.0.5 Darwin macho 前缀 ABI（`__error`→`___error`）+ tip Ubuntu L2 · 钉盘仍 `d79a368b2`\
> **产品 L4 放行钉盘**：**`d79a368b2`**（2026-08-10 升钉 · 前 `36363b90f` 2026-08-07 · 再前 `db809e00f` wave1277）\
> **产品 residual tip**：link_abi **7.3.1 关 pin ✅**（12 labi_*.x 切片 prefer；默认 FROM_X=1）· parser **7.2.2 关 pin ✅**（tip `-E` + assemble；产品默认 FROM_X=1）· codegen 7.4.2 wave323 ✅ · typeck 7.4.1 wave322 ✅ · runtime monofile wave321 ✅ · present **0** · 钉盘 **`d79a368b2`** · Stage 8 tip **wave332 Track L 30/30 FULLY CLOSED**（**PRODUCT HALF=0** · **PRODUCT PINNED=0** · **PRODUCT RETIRED 23/23 = 100%**）· **7.4.3 PREFER_X_O 双端 8/8 wave338 ✅**\
> **tip L4 安全网（历史）**：**`ffc0b646f`**（已由 tip L4 升钉吸收；前网 `4bacaf18c`）\
> **终局债**：[C迁移追踪.md](C迁移追踪.md)（状态 only · 无波次流水）· [Makefile迁移表.md](Makefile迁移表.md)\
> **本波状态**（一行）：**双端 L4 真冷绿** @ `d79a368b2`（formal_mod 收口）· **升钉 `36363b90f` → `d79a368b2`** · **bstrict 129** · **G.8 SHARED 双端同钉闭环** · MG ✅ · BC 🟢 FULLY CLOSED（wave332）· 7.4.3 PREFER_X_O **双端 8/8 真验证 ✅**（wave338 + 8/10 复验：formal_mod 修 ast_gen2/driver_x · 60s alarm 修 codegen_x）· **双端 Stage2 ✅ SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709` · strict-link 根修后双端首过）\
> **下波候选**：①Stage 12 零 cc——**12.2.5～12.2.7 ✅** + **12.0.5 helper ✅** + **Darwin `__error` mangling ✅**；**下一步**：①`xlang_panic_` g05 bag／bounds residual · ②限时修 `rt_run_x_emit`／`rt_run_asm_backend` XT001 · ③产品 hybrid pure-asm 再试装 · ④谈升钉才 L4 · **禁无界 mega 盲扫**


> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1；过程上 prefer+heat 一次／域，双端矩阵同波一次\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；**SHARED 双端 L2**；日常矩阵 **只** `./xbuild l2-matrix`（wave299：捕获 rv=42／opt=102 成功码，禁 set -e 截断）；谈自举／升钉才 L4 + 矩阵 + 全量 bstrict；bstrict **per-script timeout=300s** + catalog cache（防无界卡死）

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[09](自举进度-归档-2026-08-09.md) · [08](自举进度-归档-2026-08-08.md) · [07](自举进度-归档-2026-08-07.md) · [06](自举进度-归档-2026-08-06.md) · [05](自举进度-归档-2026-08-05.md) · [04](自举进度-归档-2026-08-04.md) · [03](自举进度-归档-2026-08-03.md) · [02](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · …

### 维护约定

1. 每完成一波 **必改**本文（KPI / 前排 ≤3 / **§6 一行**）；动终局债时同波改 [`C迁移追踪.md`](C迁移追踪.md) · [`Makefile迁移表.md`](Makefile迁移表.md)。
2. **波次流水只写 §6**（倒序 · **一行一波** · 描述宜短：做了什么 · tip · 不升钉 · 下一刀）。**禁止**在 §0–§5 堆 `waveNNN` 长清单；细节进 LEAF / C迁移 / 当日归档。
   - **§6 表固定 3 列**：`日期 | 波次 | 描述`。描述列**禁止**裸 ASCII `|`（会把行拆成 4+ 列）；并列名写 `／`、`或`、全角 `｜`，或改写避免。
3. **禁止**把 prove / Stage2 / N++ 单独写成「可自举」。
4. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**）。
5. 工程 KPI 写 **SHA + 日志路径**；过期整日流水 **日切归档**后主文档重新起表（见归档头）。
6. 大债地图写 [`当前问题分析.md`](当前问题分析.md)；非每微步。
7. **`当前进度.md` 已删除**（2026-07-30；与本文重复；勿再新建）。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 = `d79a368b2`**（2026-08-10 升钉 · 前 `36363b90f` 2026-08-07 · 再前 `db809e00f` wave1277）。\
**MG 终局达成 = wave941/942**（Makefile 物理删除 · 0 make in bootstrap · catalog 单权威 · 双端 L4 绿）\
**双端 Stage2 = ✅ SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709` · `xlang_asm_stage1`／`xlang_asm2` bit-identical · 黄金自举金标准 · strict-link 根修后双端首过）\
**选题权威 =** [C迁移追踪.md](C迁移追踪.md) DAG（Makefile 已删除；**变更记录只在本文 §6**）。

| 指标 | 状态 |
|------|------|
| 工程轨 | prove **111/111 IDENTICAL**；Cap residual 按需 L2 |
| 产品轨 tip / 钉盘 | 钉盘 **`d79a368b2`**（L4 升钉冻结 · 双端 L4 真冷绿）；residual tip **wave332** present **0**（product no_c 拒 monofile · formal_mod 收口升钉）；Stage 8 tip **wave332**（**30/30 FULLY CLOSED**；**PRODUCT RETIRED 23/23=100%**；PRODUCT HALF=0；PRODUCT PINNED=0） |
| Track MG | **11.3.1 MG ✅**（wave941 物理删 Makefile · wave942 catalog CFLAGS · 0 make in bootstrap · 产品入口 `./xbuild`） |
| Track BC | **开 🟢（30/30 FULLY CLOSED wave332）**（B′ 至 **wave326** link_abi **7.3.1 关 pin** ✅ · M4 **5/5 全 ✅**（runtime/typeck/codegen/parser/link_abi 五域冷链全闭）· present **0**；**BC Stage8 Batch3 收**＝**30/30 FULLY CLOSED**（PRODUCT 23 RETIRED 100% catalog+ladder；NON-PRODUCT 7 正确分类 never-product-chain = TEST×2 / STAGE×2 / EXTRACT_ONLY×1 / DELETED_ORPHAN×2；G.7 无 HALF 无 unresolved PINNED）|
| Track L (Stage 8) | **收（Batch 3 30/30 FULLY CLOSED wave332）**（批次 0 清理 ✅ · 批次 2 **wave1036–1041** 退役 ✅ · 批次 3 **wave327–332**（lsp_diag+lsp+pipeline+driver+preprocess+lexer+parser/typeck/codegen/ast_gen2 10 项 + audit 分类误判 3 项 RETIRED 修正 + ast_gen.c 孤儿物理删 + ast_gen2 cold-seed pin + 分母口径统一 is_product_denominator）✅ · **30/30 FULLY CLOSED**；PRODUCT RETIRED 23/23=100%；HALF=0；PINNED 产品=0 · 详见 [阶段8高效退役分析.md](阶段8高效退役分析.md)） |
| mac / Ubuntu L4（钉盘） | ✅ **双端 L4 真冷 remaining_o=0 + 矩阵 + bstrict 129** @ `d79a368b2`（mac wall=35m42s · Ubuntu wall=11m36s · G.8 SHARED 双端同钉闭环）|
| pin 蛋 | ✅ `seeds/bootstrap_xlangc.darwin.arm64` + `linux.x86_64` 自 L4 升钉波产品 `xlang_asm` 刷新 |
| 本波构建级别 | 钉盘 **L4 已结**；日常 BC 续 **L2**（check 闸门暂停） |

***

## 1. 文档地图

| 文档 | 看什么 |
|------|--------|
| [自举方法.md](自举方法.md) | Cap / R / L / M · 怎么干活 |
| [自举时序.md](自举时序.md) | **先→后全景** · 换 IDE 30 秒协议 · S0–S8 主航道 |
| [C迁移追踪.md](C迁移追踪.md) | **终局债 / BC·PC DAG**（状态 only · 无波次流水） |
| [Makefile迁移表.md](Makefile迁移表.md) | Makefile → xbuild 叶映射（MG 已 ✅） |
| [compiler/docs/LEAF_PATTERN_RESIDUAL.md](../compiler/docs/LEAF_PATTERN_RESIDUAL.md) | 叶 residual 人读地图 |
| **本文** | 长期 KPI + 前排 ≤3 + **§6 波次一行记录** |
| [当前问题分析.md](当前问题分析.md) | tip 级产品债地图 |
| [自举进度-归档-2026-08-08.md](自举进度-归档-2026-08-08.md) | **8月8日** B′ pipeline_x mega 首刀 bootstrap_glue→ast_pool 各域→elf/wpo→8.3 结构地板→gen dual leave→typeck.x 全量 `-E` T001（**wave262–314** · **56** 行） |
| [自举进度-归档-2026-08-07.md](自举进度-归档-2026-08-07.md) | **8月7日** B′ pure leave wave200–261 + 升钉 `36363b90f` + glue_statics（**64** 行） |
| [自举进度-归档-2026-08-06.md](自举进度-归档-2026-08-06.md) | **8月6日** B′ pure leave field_access→wave199 CALL-target resolve（**50** 行） |
| [自举进度-归档-2026-08-05.md](自举进度-归档-2026-08-05.md) | **8月5日** L4 冷恢复 + 8.3.3 R2 + pure-asm match + B′ leave 至 binop（**74** 行） |
| [自举进度-归档-2026-08-04.md](自举进度-归档-2026-08-04.md) | **8月4日 wave1241–1291**（BC ast_pool 16 波域分解 + glue 壳 thin + check 收口 + 升钉 `db809e00f`） |
| [自举进度-归档-2026-08-03.md](自举进度-归档-2026-08-03.md) | **8月3日 wave1227–1240**（BC glue 叶收口 + check 工具链启动 · 15 行） |
| [自举进度-归档-2026-08-02.md](自举进度-归档-2026-08-02.md) | **8月2日 wave1130–1220**（BC glue 续迁 + typeck 跨模块字段根修 · 43 行） |
| [自举进度-归档-2026-08-01.md](自举进度-归档-2026-08-01.md) | **8月1日 wave964–1111-1129**（BC 8.3.1 G.7 fold 50+ 叶 + Stage 8 退役 13/30） |
| [自举进度-归档-2026-07-31.md](自举进度-归档-2026-07-31.md) | **31 日全日 wave895–963**（MG 真删 + post-delete + 开 BC） |
| skill `xlang-selfhost-product-gate` | L4 / 双端 / 假绿禁止 |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项 | 结果 | 日志 / 备注 |
|----|------|------------|
| 产品 bstrict 套件数 | **129** | 现行白名单；per-script timeout=300s + catalog cache |
| 产品 L4 放行钉盘 | **`d79a368b2`** | 2026-08-10 升钉；双端 L4 真冷 + 129（mac 35m42s · Ubuntu 11m36s）；formal_mod 收口（per-symbol rename／`*_c` 不改名／foreign localize／`io_err_*` 面分离）；前 `36363b90f` |
| pin 蛋 | **darwin.arm64 + linux.x86_64** | 自本波 L4 `xlang_asm` 刷新（旧蛋 07-23 brand-clean） |
| residual tip（BC） | wave339 | M4 **5/5** 全闭 · Stage 8 Batch 3 **30/30 FULLY CLOSED**（wave332）· 7.4.3 PREFER_X_O **双端 8/8 wave338** · wave339 lsp.x orphan fix + codegen `extern int32_t main` fix · pure-ld 58 objs（mac）/ 60 objs（ubuntu）· **双端 L4 真冷绿** @ **`d79a368b2`** · 下刀：M 轨推进（PREFER_X_O 冷启动重建 binary 验证自举一致性） |
| mac 钉盘 L4 | ✅ L4 + bstrict **129** · 35m42s | remaining_o=0 · `./xbuild l4` · `/tmp/xlang_l4_Darwin_d79a368b2.log` |
| Ubuntu 钉盘 L4（金标） | ✅ L4 + bstrict **129** · 11m36s | `ubuntu-remote-server` · `/tmp/xlang_l4_Linux_d79a368b2.log` |
| 产品矩阵（日常 L2） | ✅ **`./xbuild l2-matrix`** | G.7 单权威 `product_l2_matrix.sh`（wave299）；rv42／opt102／hello0／si0／f32 一次跑全 |
| 前钉 | `db809e00f` · `77b334842` · `9bb7a757c` · … | wave1277 → wave942 → … |

### 2.2 工程轨（继承）

| KPI | 现 | 备注 |
|-----|----|----|
| **T / EMPTY** | 18/18 | 18 号结案 |
| **N** | 111/111 IDENTICAL | 本波不变 |
| **Stage2** | ✅ **双端 SHA256 match**（Ubuntu `9e60e1290` · macOS `343894709`） | `stage2-hash-gate OK (SHA256 match — 黄金自举金标准)` · `xlang_asm_stage1`／`xlang_asm2` 行为一致（42 + hello）· strict-link 根修后双端首过 · 日志 `/tmp/xlang_stage2_ubuntu_9e60e1290.log` + `/tmp/xlang_stage2_macos_fix2.log` |
| **R2 / Cap** | wave710 升钉后按需 | 明细见 29 归档 |

### 2.3 删 Makefile 闸门（诚实 · MG 已结）

| 键 | 值 |
|----|-----|
| `ENDGAME_PHYSICAL_DELETE_MAKEFILE` | **1**（已执行） |
| `TREE_ARMED` | **1** |
| `BODY_SHIPPED` / `DELETE_ALLOWED` | **1** / **1**（wave941 物理删除） |
| `PHYS_DEL_WINDOWS_GATE_STATUS` | `reproven_green` @ `71dd60a12`（wave922 · wave940 一键冷启动） |
| preflight blockers | **全清零**（wave937–939） |
| 真删条件 | ✅ 已完成 @ wave941/942 · 钉盘 **`77b334842`**（后升至 **`db809e00f`**） |
| post-delete residual | **✅ 已收**（wave944–964） |

***

## 3. 当前前排（≤3 条）

1. **BC 轨 mega gen pin／M4（高效 · 5/5 全 ✅）** — runtime monofile **wave321 ✅**；typeck **7.4.1 wave322 ✅**；codegen **7.4.2 关 pin wave323 ✅**；parser **7.2.2 关 pin wave325 ✅**（默认 FROM_X=1；pin 仅考古 egg）；**link_abi 7.3.1 关 pin wave326 ✅**（12 labi_*.x 默认 FROM_X=1）。M4 **5/5 全 ✅**（五域冷链全闭）。**残**：Stage 8 Track L 13／30（gen.c 退役）+ 前端 pin→Track L 对齐。**BC 终局仍 ⬜**。**下刀**：Stage 8 Batch 3 efficient retire（Track L 13→20 · 5 项批，独立低风险）。
2. **钉盘 `d79a368b2` 已升且冻结**（双端 L4 真冷 + 129 + pin 蛋 · G.8 SHARED 双端同钉闭环）— 日常 BC **不升钉**；谈自举／SHARED 大面才再 L4。
3. **PC 轨 + 工具债** — 用户默认仍可能 `labi_invoke_cc`；默认 asm backend 终局 ⬜。check 闸门自举期 **暂停**。

***

## 4. 与归档的关系

- **8月9日归档**：[自举进度-归档-2026-08-09.md](自举进度-归档-2026-08-09.md) — runtime thin dual leave → M4 五域关 pin（runtime/typeck/codegen/parser/link_abi）→ Stage 8 Batch 3 Track L **30/30 FULLY CLOSED**（wave327-332）→ 7.4.3 PREFER_X_O **双端 8/8**（wave335-338）→ wave339 lsp.x orphan fix + codegen `extern int32_t main` fix（**28** 行 · 钉盘仍 `36363b90f` · 日终双端 L2 5/5 绿 @ `97c7168e2`）。
- **8月8日归档**：[自举进度-归档-2026-08-08.md](自举进度-归档-2026-08-08.md) — B′ pipeline_x mega 首刀 bootstrap_glue→ast_pool 各域→elf/wpo→8.3 结构地板→gen dual leave→typeck.x 全量 `-E` T001（**wave262–314** · **56** 行 · 日终 present **0**）。
- **8月7日归档**：[自举进度-归档-2026-08-07.md](自举进度-归档-2026-08-07.md) — B′ pure leave call_args／spill／emit／typeck 域至 glue_statics **wave200–261** + **升钉 `36363b90f`**（**64** 行 · 日终 present **49**）。
- **8月6日归档**：[自举进度-归档-2026-08-06.md](自举进度-归档-2026-08-06.md) — B′ pure leave field_access／expr_rec／block_body／struct_lit → spill／7.3／CFG／index／call_args 至 wave199（**50** 行 · 钉盘仍 `db809e00f` · 日终 tip `b5d929d4e` · present **59**）。
- **8月5日归档**：[自举进度-归档-2026-08-05.md](自举进度-归档-2026-08-05.md) — L4 冷构建恢复 + 暂停 check + 8.3.3 R2 field/soa + pure-asm match + B′ pure leave 至 binop／vector_simd（**74** 行 · 钉盘仍 `db809e00f`）。
- **8月4日归档**：[自举进度-归档-2026-08-04.md](自举进度-归档-2026-08-04.md) — wave1241–1291（ast_pool 域分解 + glue 壳 thin + check 收口 + 升钉 `db809e00f` · 58 行）。
- **8月3日归档**：[自举进度-归档-2026-08-03.md](自举进度-归档-2026-08-03.md) — wave1227–1240。
- **8月2日归档**：[自举进度-归档-2026-08-02.md](自举进度-归档-2026-08-02.md) — wave1130–1220。
- **8月1日归档**：[自举进度-归档-2026-08-01.md](自举进度-归档-2026-08-01.md) — wave964–1129。
- **31 日归档**：[自举进度-归档-2026-07-31.md](自举进度-归档-2026-07-31.md) — wave895–963。
- **勿**用归档 tip 或旧钉冒充现行 tip L4。**现行产品钉盘 = `d79a368b2`**（2026-08-10 升钉 · 前 `36363b90f` · 再前 `db809e00f`）；residual tip **wave339**（M4 **5/5 全 ✅** · present **0** · 日常 L2 可续，**非** tip L4）。
- 归楼后主文档只保留 KPI + 前排 + **8 月 10 日** §6（9 日见归档）；再堆满则再日切。

***

## 5. 双端 / 平台

| 平台 | 本波状态 |
|------|----------|
| **macOS arm64** | **钉盘 L4+bstrict 129 ✅** @ `d79a368b2`（2026-08-10 升钉 · wall=35m42s · remaining_o=0 · 矩阵 5/5）· 双端 L4 真冷绿（G.8 SHARED 同钉闭环）· 产品只走 `./xbuild` · pin 蛋已刷 · check 闸门暂停 |
| **Ubuntu x86_64（金标）** | **钉盘 L4+bstrict 129 ✅** @ `d79a368b2`（2026-08-10 升钉 · wall=11m36s · remaining_o=0 · 矩阵 5/5）· 双端 L4 真冷绿 · pin 蛋已刷 · **SSH 权威 host＝`ubuntu-remote-server`** · **远端工作树＝/home/shuliangfu/worker/xlang/x-lang（branch=self-hosting）** · 同步流程：`git fetch origin` + `git merge --ff-only origin/self-hosting`（先 stash 远端 M/?? pin 文件避免冲突）|
| **Windows MSYS2** | wave940 一键冷启动绿 · STATUS=`reproven_green` · Makefile 已物理删除 · 本波未升 Windows pin（仓内无 windows pin 蛋） |

***

## 6. 变更记录（倒序 · 8 月 11 日；10 日见 [归档](自举进度-归档-2026-08-09.md)）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-08-11 | **fix(Stage12.0.5)** Darwin macho call 前缀：C 名 `__error`→`___error`（去 `name[0]!='_'` 误 skip） | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）。根因：arm64／x86 call reloc 在 `macho_leading_underscore` 时若 `name[0]=='_'` 跳过前缀 → pure-asm 产 U `__error`，host cc 产 `___error`（libSystem）。G.7 同波：`.x` + seed（`backend_enc_dispatch.from_x.c`／surface + `backend_x86_64_enc_c*`）一律 prepend。验收：`rt_diag_errno` pure-asm `U ___error` · pure_asm OK · `rt_*` pure_asm **20/23**（残：`xlang_panic_` 1 + XT001 2）· **mac L2 5/5**。补记：tip `bacd320be` 前 6 commit **此前未跑 Ubuntu L2** → 本会话 push 后 Ubuntu g05+L2 **5/5 @ bacd320be**。下一刀：g05 bag panic／XT001。PLATFORM: SHARED／MACOS mangling。 |
| 2026-08-11 | **feat(Stage12.0.5)** asm 模块覆盖清单 + G.7 `pure_asm_x_to_o`（`.o` 暂存 + 产品面护栏） | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）。①限时探针：`rt_*.x` **21/23** asm `-c` 绿（FAIL：`rt_run_x_emit` T001 unresolved · `rt_run_asm_backend` extern 需 unsafe）；`labi_*`／driver thin／diag／emit／lsp_diag 等绿。②G.7 权威 `pure_asm_x_to_o`（`pure_ld_shared.sh`）：`XLANG_PREFER_ASM_O=1` · 经 `*.o` 暂存（驱动仅 OUT 以 `.o` 结尾才产 reloc；ensure mktemp thin 无后缀会误走 final link）· 拒 U `xlang_panic`／裸 `__error`。③产品 hybrid 试装：pure-asm thin 入 `runtime_driver_no_c.o` → pure-ld 缺 panic 或 **L2 全 SIGSEGV** → **暂不** 自动挂 ensure／g05_try（仍 -E+$CC）；下一刀 ABI 对等。验收：helper 单测 + **L2 5/5**（默认路径）。PLATFORM: SHARED。 |
| 2026-08-11 | **fix(Stage12.2.7)** lsp_diag 尾部源损坏 + empty `[]` 字段 + emit 巨型 STRUCT_LIT CG002 | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）。三根因：①`lsp_diag.x` `lsp_build_semantic_tokens_response` 历史 stub（`return 0;` + `} */`）把真体甩成顶层垃圾→整模块 `copy_bytes` 假 fail；恢复完整 JSON 收尾。②`glue_struct_lit_store_fixed_array_field`：空 ARRAY_LIT `[]` 走 `vector_let_init` 拒 `n_arr==0`→CG002；空 `[]`≡零初始化（≤1024 逐元；>1024 接受不打爆破指令）；G.7 `.x`+seed 同语义。③`run_x_emit_x`：`PipelineDepCtx`／`CodegenOutBuf` 多 MiB + `data: []` 巨型 STRUCT_LIT 不可 asm；标量 STRUCT_LIT + `out` uninit/`length=`。验收：empty8／`lsp_diag.x`／`emit.x` asm `-c` rc=0（限时）；**L2 5/5**。PLATFORM: SHARED。 |
| 2026-08-11 | **fix(Stage12.2.6)** module const 嵌套 binop 左结合 load: 无栈槽 const→imm | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）。真根因：`glue_try_binop_load_operand_elf_c` 对 module `export const`（无 stack slot）返回 -2，左结合路径 `((A+B)+C)`／`W\|C\|T` 在 `load_operand!=0` 硬失败→CG002；首函数常整树 const-fold 掩盖，非首函数暴露（`rt_fs_open` 的 `driver_fs_open_write`）。G.7：`runtime_pipeline_abi.from_x.c` + `runtime_pipeline_abi.x` 对齐 emit_expr_fast：`asm_module_top_level_const_lit_i32`→`mov imm` 到 rax／rbx。验收：`rt_fs_open.x` rc=0；mini second_3sum／both_sum 绿；**L2 5/5**。残留限时 CG002：`driver/emit.x`（`run_x_emit_x`）／`lsp/lsp_diag.x`（`copy_bytes`）。PLATFORM: SHARED。 |
| 2026-08-11 | **fix(Stage12.2.5)** asm STRING_LIT 空串／长串 CG002 根修: allow `slen==0` + cap 126 | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）。真根因：`glue_asm_emit_*string*` 拒绝 `slen<=0` 与 seed 硬 cap `slen>63`，致 `return ""`／诊断长串（~82）向上冒泡成 CG002 `code_len=0`（表象 `diag_palette_kind_color` 等）——非 unsafe／if-return 缺口。G.7 同波：`.x` + `backend_call_dispatch.from_x.c` + surface 同语义（拒绝负长；上限 126；`string_lit_into` 清 128 字节）。验收：`diag.x`／`runtime_driver_diagnostic.x`／`runtime_driver_abi_thin.x` `-backend asm -c` rc=0（≤30s／文件）；**L2 5/5 绿** @ tip。探针纪律：禁无界扫 `runtime_pipeline_abi.x` 等 mega。下一步：按模块扩 asm 覆盖／超时列残留 CG002。PLATFORM: SHARED。 |
| 2026-08-11 | **feat(Stage12)** G.7 `forbid_host_cc` 覆盖 g05 系列 + 冷构建 LOG_ONLY 审计问题地图 | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）。①**G.7 单权威覆盖扩展**：`g05_ensure_relink_prereqs.sh`（L55 CC 解析后）+ `g05_relink_xlang.sh`（L37 CC 解析后）补 source `forbid_host_cc.sh`——此前仅 `build_xlang_asm.sh` + `ensure_host_cc_seed_o.sh` source · g05 系列 12 个 `$CC -c` 调用未受门禁覆盖 · 双重安装防护（L39-41 检测 `CC` 已为 wrapper 时 return 0）确保子脚本继承安全。②**冷构建 LOG_ONLY 审计**：删除全部 `.o`（114 个）强制 re-ensure · `XLANG_FORBID_HOST_CC=1 XLANG_FORBID_HOST_CC_LOG_ONLY=1 bash scripts/g05_prepare_and_relink.sh` 完整跑通（exit 0）· 违规日志 `/tmp/xlang_forbid_cc_g05_audit.log` 400 行 = **200 违规**。③**问题地图分类**：**LINK 36** = 全部 `$CC -r -nostdlib` partial-merge · `XLANG_ZERO_CC_LD=1`（`pure_ld_partial_merge`→`ld -r`）可全消灭 · **final link 0**（`g05_relink_xlang.sh` 已用 `pure_ld_try_link`）· **COMPILE 164** = **70 X-emit temp C**（`-x c -c` 编译 `xlang -E` 输出的 `/var/folders/` 临时 C）+ **88 seed `.from_x.c`**（直接 seed C 编译 · `rt_*` 25 + `labi_*` 10 + `runtime_*` 3 + 其他 50）+ **5 gen.c**（`parser_gen.c`/`typeck_gen.c`/`codegen_gen.c` 考古 seed）+ 1 其他。④**核心洞察**：全部 164 COMPILE 违规需 C 编译器 · PREFER_X_O 路径仍用 `$CC -c` 编译 X-emit C 输出 · **唯 asm backend `.x`→`.o` 直出**（`xlang -backend asm -o file.o module.x` · 无 C 中间）可消灭 COMPILE `$CC` · Stage 12 终局 = asm backend 模块覆盖全 + `XLANG_ZERO_CC_LD=1` + `XLANG_ZERO_CC_AS=1`。**验证**：**双端 L2 5/5 绿**（macOS arm64 · 冷构建后复验 · g05 产品链零回归）。PLATFORM: SHARED。 |
| 2026-08-11 | **fix(Stage12)** experimental 链 CG002 codegen 根源修复: `backend_arm64_enc_c.o` + `driver_x.o` + `runtime_process_argv.o` 补齐 | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）· commit `e5f3432b3`。`relink_xlang_asm_experimental_bootstrap.sh` 根源修复：前波 commit `4e20aed99` 补 8 对象后 experimental binary 仍有 CG002 `asm_codegen failed code_len=0`（rv/f32 失败 · 3/5 L2 绿）。本波通过 nm diff production vs experimental 定位 29 个 production-only 符号，识别 3 个关键缺失对象：①**`src/asm/backend_arm64_enc_c.o`**——arm64 指令编码器 · 提供 `arch_arm64_enc_enc_mov_imm32_to_w0`（`return N` → `mov w0, #N` 必需）· 缺失致 asm codegen 产零输出（code_len=0）· g05 `_USER_ASM_LINK` 同时含 x86_64 + arm64 编码器（跨目标支持）· experimental `$BSTRICT_DISPATCH` 仅 x86_64；②**`driver_x.o`**——main 入口 + 命令分发 · 提供 `_main_run_compiler_c_impl` 及 `_main_*` 辅助函数 · experimental 链有 `driver_fmt_x.o` 等子命令对象但缺 `driver_x.o` 本体 · 致 `xlang_asm build` 子命令 rc=255 静默失败；③**`runtime_process_argv.o`**——`process_xlang_argc_get`/`process_xlang_argv_get`/`xlang_process_argv_bind_from_crt` · g05 `_DRIVER_SEED_OBJS` 显含 · experimental 仅 `runtime_driver_asm_strict.o` 的 `process_args_count_c`。**LSP 对象不补**：experimental 链已通过 `$SEED_O/lsp_diag.o` + `$ST_LSP_X` 含 LSP 对象 · 再加 `lsp_x.o`/`lsp_diag_x.o`/`src/lsp/lsp_diag.o` 等致 145 duplicate symbols。**验证**：①macOS experimental `-backend asm` rv/f32 编译成功 · `build` 子命令 rc=0 产 binary · **experimental L2 5/5 绿**（rv42/opt102/hello0/si0/f32 · 从 3/5→5/5）；②**双端 L2 5/5 绿 @ `e5f3432b3`**（macOS arm64 + Ubuntu x86_64 · G.8 SHARED 双端闭环 · g05 产品链零回归）；③Ubuntu experimental binary segfault（rc=139 · Linux 专属 runtime 问题 · 非本波回归 · experimental 链此前在 Ubuntu 从未功能完整）。**Stage 12 experimental CG002 codegen 修复 ✅**（macOS）；剩余 $CC = COMPILE only；下一步 PREFER_X_O 或 L4 升钉。PLATFORM: SHARED。 |
| 2026-08-11 | **fix(Stage12)** experimental 链 533 缺失符号根源修复: `cfg_eval.o` 替 `cfg_eval_link_alias.o` + 8 对象补齐 | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）· commit `4e20aed99`。`relink_xlang_asm_experimental_bootstrap.sh` 根源修复：前波 commit `bb95ad56d` 补 4 rt_*.o + `runtime_pipeline_abi.o` 后，experimental 链仍有 533 个 g05 不缺的未定义符号。本波通过 diff g05 `G05_OBJS` vs `EXP_LINK_OBJS`（G.7 单权威 · `g05_relink_env.sh` 为生产真相）识别并补齐 8 个对象到 `EXP_LINK_OBJS`：①`src/seed_link_compat.o`（seed link 兼容 shim）②`build_asm/seed_host/asm_full_link_stubs.o`（asm full link stubs）③`src/runtime_driver_diagnostic.o`（driver 诊断符号）④`src/runtime_driver_abi.o`（driver ABI 符号）⑤`src/diag.o`（诊断符号）⑥`src/async/async_asm_pool.o`（async asm pool）⑦`src/lexer/cfg_eval.o`（lexer cfg eval · **关键修复**）⑧`src/typeck/typeck_f64_bits.o`（typeck f64 bits）。**关键根源修复**：前波用 `src/lexer/cfg_eval_link_alias.o`（仅 1400 字节 · `nm` 显示 `U _lexer_cfg_*`——CONSUMES 符号），应改为 `src/lexer/cfg_eval.o`（13912 字节 · `nm` 显示 `T _lexer_cfg_*`——PROVIDES 符号 · 是 `cfg_eval_x.o` + `cfg_eval_link_alias.o` 经 `ld -r` 合并的完整 partial merge · 与 g05 `_DRIVER_SEED_SUPPORT` no_c default 一致）。前波错误导致 link 失败 4 个 `lexer_cfg_*` undefined。**验证**：①experimental relink 成功（`OK copied to xlang_asm`）· binary runs（`--version`/parse/typeck OK）· undefined 符号从 533 降至 **101**（vs g05 产品 103 · 仅差 4 符号：`qsort` system + `_NSGetArgc`/`_NSGetArgv`/`_exit` 不同 runtime objects）；②**双端 L2 5/5 绿 @ `4e20aed99`**（macOS arm64 + Ubuntu x86_64 · G.8 SHARED 双端闭环 · g05 产品链零回归）；③experimental binary L2 3/5 绿（opt/si/hello）· 2/5 红（rv/f32 · CG002 `asm_codegen failed code_len=0` · cold-start 预存 codegen 缺口 · 非本波回归）。**Stage 12 experimental 链符号缺口关闭 ✅**；剩余 $CC = COMPILE only；下一步 CG002 codegen 修复。PLATFORM: SHARED。 |
| 2026-08-11 | **feat(Stage12)** 3 处遗漏 `.s` `pure_as_compile` 补全: typeck_f64_bits + runtime_panic | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）· commit `19455088d`。补全 commit `a6affeaf5`（9 `.s`→`as`）遗漏的 3 处 `.s` 文件 `$CC -c` → `pure_as_compile`：①`g05_ensure_relink_prereqs.sh:559` typeck_f64_bits `.s`（Linux/macOS 主路径 · `$_f64s` 变量）；②`build_xlang_asm.sh:4882` typeck_f64_bits_mingw `.s`（Windows/MinGW 路径）；③`relink_xlang_asm_strict_glue.sh:1902` runtime_panic `.s`（Linux x86_64 · `$_panic_src` 变量 · 需补 source `pure_ld_shared.sh`）。**验证**：**双端 L2 5/5 绿 @ `19455088d`**（`XLANG_ZERO_CC_AS=1` · macOS arm64 + Ubuntu x86_64 · G.8 SHARED 双端闭环 · flag 未设零回归 + flag 设零 cc `as` 路径均验）。**Stage 12 `.s` COMPILE 全零 cc ✅**（12 `.s` → `as` · 9 原 + 3 补全）；剩余 $CC = COMPILE only（`.c` seed 编译 · `ensure_host_cc_seed_o.sh` ×39 + `g05_ensure_relink_prereqs.sh` ×14）。PLATFORM: SHARED。 |
| 2026-08-11 | **feat(Stage12)** `emit_asm_text_stub_o` weak `.s` stub CC 消除: `pure_as_compile` 零 cc stub 生成 | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）· commit `95dc4db65`。`build_xlang_asm.sh` `emit_asm_text_stub_o` 重构：PRIMARY 路径从 `.c` stub（`$CC -c`）改为运行时生成平台特定 weak `.s` stub（Darwin `.weak_definition` + `_` 前缀 · Linux `.weak` 无前缀）经 `pure_as_compile` 编译——`XLANG_ZERO_CC_AS=1` 时 `as -o` 零 cc · flag 未设时 `$CC -c` 零回归。**符号等价验证**：`nm -m` 双端确认 `.s` stub 与 `.c` stub 均产 `weak external xlang_asm_ci_text_stub`（Darwin）／`W xlang_asm_ci_text_stub`（Linux ELF）——strict link multiple-definition 解析行为完全一致。Fallback 链：①运行时 weak `.s` stub（PRIMARY · 零 cc）→ ②`.c` stub via `$CC`（不支持平台 fallback · Windows/MSYS）→ ③静态 `.s` stub（last resort · 不同符号 `__xlang_asm_mod_stub`）。**验证**：**双端 L2 5/5 绿 @ `95dc4db65`**（`XLANG_ZERO_CC_AS=1` · macOS arm64 + Ubuntu x86_64 · G.8 SHARED 双端闭环）。**Stage 12 stub CC 消除 ✅**；剩余 $CC = COMPILE only（`ensure_host_cc_seed_o.sh` ×39 seed `.c` + `g05_ensure_relink_prereqs.sh` ×14 前置 `.c`）。PLATFORM: SHARED。 |
| 2026-08-11 | **fix(Stage12)** experimental bootstrap pure-ld 链 + rt_*.o／runtime_pipeline_abi.o 根源修复 | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）· commit `bb95ad56d`。`relink_xlang_asm_experimental_bootstrap.sh` 根源修复：①补 4 个 RT_SEED_SLICE `.o`（`rt_arena_buf`/`rt_emit_state`/`rt_preamble`/`rt_stack`）到 `EXP_LINK_OBJS` + ensure 步骤——`rt_emit_state.o` 提供 `driver_x_emit_c_path` 全局变量，缺失致 `dyld: symbol not found` 运行时崩溃（g05 产品链已有，experimental 链遗漏）；②补 `src/runtime_pipeline_abi.o`（G.7 pipeline 权威 · 2707 符号 · 提供 `xlang_ptr_slot_get` 等）；③修 Darwin `-Wl,-multiply_defined -Wl,suppress` 废弃 flag（新 macOS ld 报 `file cannot be open()ed, path=suppress`）——移除该 flag，G.7 单权威（`parser_x.o` 排除 `parser.o`）已处理重复符号；④新增 pure-ld 分支（`XLANG_ZERO_CC_LD=1` 时 `pure_ld_try_link` 替代 `$CC -o`）+ Darwin LC_MAIN auto-detection + `-undefined dynamic_lookup`；⑤`pure_ld_shared.sh` 新增 `pure_ld_hosted_crt1()` 工具函数。**发现**：experimental 链仍有 533 个 g05 不缺的未定义符号（缺 `seed_link_compat.o`/`diag.o`/`runtime_process_argv.o` 等）——属预存缺口，非零 CC 回归。**验证**：**双端 L2 5/5 绿 @ `bb95ad56d`**（macOS arm64 58 objs + Ubuntu x86_64 60 objs · g05 产品链 pure-ld · G.8 SHARED 双端闭环）。**g05 产品链（生产路径）LINK 全零 cc ✅**。PLATFORM: SHARED。 |
| 2026-08-11 | **feat(Stage12)** pure_as_compile zero-CC `.s` assembly: 9 `$CC -c` → `as` | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）· **Stage 12.2.3 COMPILE 首步 ✅**。commit `a6affeaf5`。`pure_ld_shared.sh` 新增 `pure_as_compile`（G.7 单权威 · `XLANG_ZERO_CC_AS=1` 时 `as -o OUT SRC.s` 零 cc · flag 未设时 `$CC -c` 零回归）；`ensure_host_cc_seed_o.sh` 3 处主路径（`ensure_r2_panic_one`/`ensure_r2_typeck_f64_one`/`ensure_r2_crt0_one` asm case）+ `build_xlang_asm.sh` 6 处 fallback（4 typeck_f64_bits + 1 runtime_panic + 1 crt0）`$CC -c -o` 全替换为 `pure_as_compile`；`build_xlang_asm.sh` 顶部补 source `pure_ld_shared.sh`（原仅 line 5009 中部 source）。**验证**：所有 `.s` 文件 0 cpp 指令（纯汇编）· macOS `as` 产出 `.o` nm 符号匹配（`_start`/`_main_entry`/`_exit`）· **双端 L2 5/5 绿 @ `a6affeaf5`**（macOS arm64 + Ubuntu x86_64 · G.8 SHARED 双端闭环 · flag 未设零回归 + flag 设零 cc `as` 路径均验）。**Stage 12 LINK 全零 cc + COMPILE 首步 9 `.s`→`as`**；剩余 $CC = COMPILE only（`.c` seed 编译 + stub 回退）。PLATFORM: SHARED。 |
| 2026-08-11 | **feat(Stage12)** pure_ld_partial_merge zero-CC partial-merge LINK 闭环 + LOG_ONLY 问题地图: 39 `$CC -r` → `ld -r` | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）· **Stage 12.2.3 partial-merge LINK 零 cc ✅**。3 commit：`228cb362c`（`forbid_host_cc.sh` LOG_ONLY 模式——log + exec real CC 不阻塞构建 · 收集 L4 重建 173 违规 = 154 COMPILE + 37 LINK + 12 OTHER · 双重安装守卫防 wrapper 递归）→ `1abc8db66`（12.2.2 文档精炼）→ `072ee3047`（`pure_ld_partial_merge` 落地）。**核心改动**：`pure_ld_shared.sh` 新增 `pure_ld_partial_merge`（G.7 单权威 · `XLANG_ZERO_CC_LD=1` 时 `ld -r $multidef -o OUT OBJS` 零 cc · flag 未设时 `$CC -r -nostdlib` 零回归）；`ensure_host_cc_seed_o.sh` 33 处 + `g05_ensure_relink_prereqs.sh` 6 处 `$CC -r -nostdlib -o` 全替换为 `pure_ld_partial_merge`。**验证**：①macOS L2 5/5 绿 flag 未设（零回归 SHA=228cb362c）；②macOS L2 5/5 绿 `XLANG_ZERO_CC_LD=1`（零 cc `ld -r`）；③macOS 删 g05 merged `.o`（`parser_asm_parse_expr_link.o`/`src/diag.o`/`src/x_seed_bridge.o`/`parser_asm_thin_glue.o`）+ `XLANG_ZERO_CC_LD=1` 重建成功（`ld -r` 正确重建 merged `.o`）；④**双端 L2 5/5 绿 @ `072ee3047`**（macOS arm64 + Ubuntu x86_64 · G.8 SHARED 双端闭环）。**Stage 12 LINK 路径全零 cc**（L4 3 链步 `pure_ld_try_link` + partial-merge `pure_ld_partial_merge`）；**剩余 $CC = COMPILE only**。PLATFORM: SHARED。 |
| 2026-08-11 | **feat(Stage12)** XLANG_ZERO_CC_LD crt0 pure-ld 落地 + L4 冷启动 LINK 全 pure-ld 闭环发现 | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）· **Stage 12.2.2 crt0 pure-ld + L4 LINK 闭环发现 ✅**。3 commit：`86dc3b686`（XLANG_ZERO_CC_LD opt-in ld 路径 + `bootstrap_link_tail_crt0_ld`）→ `b448864ac`（重构为 `pure_ld_try_link` G.7 单权威）→ `3b1ba4cf8`（文档）。**重大发现**：①**L4 冷启动 LINK 全 pure-ld 闭环 ✅**——L4 流（`product_l4_true_cold.sh`）3 个链步均走 `pure_ld_try_link`：`bootstrap_driver_seed_link.sh` phase1 + final（wave772）+ `g05_relink_xlang.sh`（wave773）；`build_xlang_asm.sh` **不在** L4 流中（是 `./xbuild build` 日常路径）；`pure_ld_shared.sh` self-test Ubuntu OK ✅；②**g05 双端 pure-ld OK ✅**（macOS 58 objs + Ubuntu 60 objs · `pure_ld_try_link` 含 multidef + platform prefix + entry）；③**crt0 冷启动路径 broken**——`build_xlang_asm.sh` crt0 链缺对象（`pipeline_module_func_param_type_ref_at_*` + `codegen_*` → undefined ref · $CC 与 ld 均失败 · multidef 已修但缺对象更深 · 非 L4 流）；④**剩余 $CC = 53 COMPILE**（L4 流：`ensure_host_cc_seed_o.sh` ×39 + `g05_ensure_relink_prereqs.sh` ×14 · 非 LINK · 需 PREFER_X_O 覆盖消灭）。验证：双端 L2 5/5 绿 @ `b448864ac`。**Stage 12 LINK 迁移完成；下一步 COMPILE 迁移（PREFER_X_O）**。PLATFORM: SHARED。 |
| 2026-08-11 | **feat(Stage12)** XLANG_FORBID_HOST_CC 门禁落地 + 零 cc 问题地图: 141 $CC 调用点全量识别 | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）· **Stage 12.2.1 门禁 + 问题地图 ✅**。新增 `compiler/scripts/forbid_host_cc.sh`（G.7 单权威 · wrapper 覆盖 $CC · COMPILE/LINK/OTHER 三类分类 · flag 未设=零影响 no-op）；`build_xlang_asm.sh:131` + `ensure_host_cc_seed_o.sh:321` source 注入。验证：①无回归——L2 5/5 绿 @ `33de4f840`（flag 未设=no-op）；②门禁功能——macOS `XLANG_FORBID_HOST_CC=1` 拦截 16 COMPILE 违规（4 模块 × 4 `emit_asm_text_stub_o` stub 回退）+ relink-only 拦截 `seeds/diag.from_x.c` COMPILE。**问题地图（源码分析 + 运行时验证）**：冷启动链共 **141 $CC 调用点** = **14 LINK**（`build_xlang_asm.sh` ×8 strict/crt0/experimental link paths · `g05_ensure_relink_prereqs.sh` ×5 · `g05_relink_xlang.sh` ×1）+ **127 COMPILE**（`build_xlang_asm.sh` ×74 含 `emit_asm_text_stub_o` stub 回退 · `ensure_host_cc_seed_o.sh` ×39 seed 编译 · `g05_ensure_relink_prereqs.sh` ×14）。**修复策略**：LINK 路径优先（8+5+1=14 处 `"$CC" $CFLAGS -o xlang_asm` → 直接 `ld` 调用，Linux crt0 路径最成熟）；COMPILE 路径随 PREFER_X_O seed 退役逐步消灭。PLATFORM: SHARED。 |
| 2026-08-10 | **verify(M-track)** PREFER_X_O 双端 8/8 真验证波: wave338 三缺口全修复确认 + M 轨 Stage 12 前置打通 | **文档 only** · **不升钉**（钉盘仍 `d79a368b2`）· **双端 PREFER_X_O 8/8 真验证 ✅**。wave338 记录 3 个 PREFER_X_O 缺口复验全通：①macOS `ast_gen2.o`（原 xlang-seed-phase1 codegen 双前缀 `ast_ast_arena_block_get`）→ formal_mod 波 `d79a368b2` 修 per-symbol bare rename → tip binary -E 输出 `ast_arena_block_get` ×13（0 双前缀）→ PREFER_X_O exit=0。②macOS `driver_x.o`（原 `main_run_compiler_c` 双定义）→ formal_mod 修 codegen 重复输出 → PREFER_X_O exit=0。③Ubuntu `codegen_x.o`（原 30s alarm `_e_rc=142` SIGALRM）→ wave338 per-leaf 60s override（`driver_leaf_x_to_o.sh:523`）→ Ubuntu 实测 37.3s < 60s → PREFER_X_O exit=0。macOS codegen_x.o 30.6s < 60s。双端 L2 5/5 绿 @ `d9e9ce5aa`（rv42／opt102／hello0／si0／f32）。**M 轨 Stage 12 前置打通**：PREFER_X_O 全通 → cold-seed egg 可降级为纯考古 fallback。PLATFORM: SHARED。 |
| 2026-08-10 | **fix(strict-link)** macOS Stage2 SHA256 金标波: Darwin strict-link duplicate symbol 根修 + macOS Stage2 黄金自举金标 ✅ | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）· **macOS Stage2 ✅ SHA256 match**。Darwin strict link 2 处 duplicate symbol 根修（commit `343894709`）：①`seed_link_compat.o` 同时在 `ASM_TRY_OBJS`（`NONEMPTY_ASM` filter 漏放）和 `ST_STRICT_COMPANIONS`（显式）→ 5 duplicate symbols。filter_experimental／filter_strict 排除列表补 `seed_link_compat.o`（同 `x_seed_bridge.o` 模式）。②`runtime_panic.o`（user-domain cold twin）定义 `link_abi_getenv`／`_impl`，与 `src/runtime_link_abi.o`（G.7 host 权威）dual-def → 2 duplicate symbols。seed 注释明言「NOT part of the g05 xlang_asm host link」；Darwin strict 路径跳过（host 编译器 `xlang_panic_`／`crash_evidence` 由 static locals 解决，验证自 experimental 引导路径从不链 `runtime_panic.o`）。验证：macOS `343894709` Stage2 `xlang_asm strict OK`（asm_only_strict，非 experimental fallback）· `stage2-hash-gate OK (SHA256 match — 黄金自举金标准)` · sha256=`93f4ca45...`（5,588,712 bytes）· `xlang_asm_stage1`／`xlang_asm2` 行为一致（42 + hello）。**双端 Stage2 闭环**（Ubuntu `9e60e1290` + macOS `343894709`）。日志：`/tmp/xlang_stage2_macos_fix2.log`。PLATFORM: SHARED（Darwin guards；Linux ld 容忍 duplicate 不变）。 |
| 2026-08-10 | **fix(strict-link)** Stage2 SHA256 金标波: strict-link undefined symbol 根修 + Ubuntu Stage2 黄金自举金标 ✅ | **代码+文档** · **不升钉**（钉盘仍 `d79a368b2`）· **Ubuntu Stage2 ✅ SHA256 match**。strict-link 链 6 commit 修根：①`b8329a057` Stage2 script MAKE 默认 `../xbuild`（Makefile 已删）· ②`b5ddb26a9` C迁移追踪状态对齐 + 自举进度重写· ③`0716fc305` drop retired pipeline_glue 引用· ④`560db1b66` guard retired seed in relink scripts· ⑤`107d09af2` check `runtime_pipeline_abi.o` as G.7 authority（非 stub `pipeline.o`）· ⑥`9e60e1290` **根修**：`ensure_typeck_x_no_layout_partial_obj` awk `/ T _?typeck_/` 只匹配 `typeck_*` 前缀，静默丢弃 133 符号（`pipeline_typeck_*`／`pipeline_expr_is_c_*`／`pipeline_dep_ctx_*`／`glue_typeck_*`）→ strict link undefined reference。改为 `/ T /`（全留 T），7 grep -v 继续排除 layout。验证：Ubuntu `9e60e1290` Stage2 `✓ B-strict Stage2 通过` · `stage2-hash-gate OK (SHA256 match — 黄金自举金标准)` · `xlang_asm_stage1`／`xlang_asm2` 行为一致（42 + hello）。WPO chain Linux OOM 非致命。日志：`/tmp/xlang_stage2_ubuntu_9e60e1290.log`。PLATFORM: SHARED。 |
| 2026-08-10 | **fix(formal_mod)** 升钉波: 双端 L4 真冷绿 @ `d79a368b2` + 升钉 `36363b90f` → `d79a368b2` | **代码+文档** · **升钉 ✅** · **双端 L4 绿（G.8 SHARED 双端同钉闭环）**。formal_mod 收口 4 commit 链（`5acd137bd` net type/export + multi-def · `71d9c714e` per-symbol bare rename + foreign co-emit localize · `775804765` formal_bare→product map + `*_c` 永不 rename · `d79a368b2` `io_err_*` 产品面 rename ≠ io driver）。根修 `xlang_compile_std_module.sh`（per-symbol bare→product rename · `*_c` C 实现面保持 bare · foreign `std_*`/`core_*` strong localize 消 multi-def · formal_bare 显式 map · `io_err_*` 与 io driver 面分离）。验证：①Ubuntu 金标 `./xbuild l4` exit 0 · `product_l4_true_cold OK SHA=d79a368b2 host=Linux-x86_64 wall=11m36s` · bstrict 129 · 矩阵 5/5；②macOS `./xbuild l4` exit 0 · `product_l4_true_cold OK SHA=d79a368b2 host=Darwin-arm64 wall=35m42s` · bstrict 129 · 矩阵 5/5。日志：`/tmp/xlang_l4_Linux_d79a368b2.log` + `/tmp/xlang_l4_Darwin_d79a368b2.log`。PLATFORM: SHARED（formal_mod post-`.o` 符号面处理跨平台；G.8 双端同钉闭环）。 |

***

***
