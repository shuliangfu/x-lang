# Xlang / X language 自举进度

> **更新**：2026-07-26 · **wave475 已闭**（default mid-body if/while/expr_stmt · L2）@ **`dc5c74615`** · tip L4 仍 **`b99e25de5`** · 钉盘仍 **`70ca1089b`** · 下一 **wave476**\
> **基线 SHA（产品 L4 放行钉盘）**：**`70ca1089b`**（升自 `605e7efa4`；tip L4 已绿可谈升钉）\
> **产品 bstrict 套件数**：**129**\
> **产品代码 tip**：**`dc5c74615`**（wave475；L4 基线 `b99e25de5` / 产品 `f99fafda8`；钉盘仍 **`70ca1089b`**）\
> **git 锚点（重命名前）**：tag **`anchor-pre-xlang-rename`**\
> **现行工程**：品牌/CLI/env/ABI 已 **xlang**（主域 **`xlang.dev`** · 仓库 **`github.com/shuliangfu/x-lang`**）。\
> **本波（26 日）状态**：**wave475 L2 双端绿**（default mid-body if/while/expr_stmt）；tip L4 @ `b99e25de5`；钉盘仍 `70ca1089b`；下一 **wave476**（for/match mid-body / multi mono / dyn soft）。\
> **方法**：[自举方法.md](自举方法.md)（Cap/R/L/M；工程 ≠ 产品放行）\
> **Windows（删种子前）**：[Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**：[自举进度-归档-2026-07-25.md](自举进度-归档-2026-07-25.md) · [24](自举进度-归档-2026-07-24.md) · [23](自举进度-归档-2026-07-23.md) · [22](自举进度-归档-2026-07-22.md) · [21](自举进度-归档-2026-07-21.md) · [20](自举进度-归档-2026-07-20.md) · [19](自举进度-归档-2026-07-19.md) · [18](自举进度-归档-2026-07-18.md) · [17](自举进度-归档-2026-07-17.md) · [16](自举进度-归档-2026-07-16.md) · [14](自举进度-归档-2026-07-14.md) · [13](自举进度-归档-2026-07-13.md)

### 维护约定

1. 每完成一波 **必改两文**：本文（KPI/前排/§6）+ [`当前进度.md`](当前进度.md)。
2. **禁止**把 prove/Stage2/N++ 单独写成「可自举」。
3. **产品放行** = 本 SHA **L4 真冷**（至少 Ubuntu）+ 双端 `run-all-bstrict`（现行 **129**；历史钉盘 125）。
4. 工程 KPI 写 **SHA + 日志路径**；过期整日流水移归档。
5. 大债地图默认写 [`当前问题分析.md`](当前问题分析.md)（承接原 `问题分析文档.md`）；非每微步。

***

## 0. 一句话当前状态

**产品 L4 放行钉盘 =** **`70ca1089b`**（历史钉；**tip L4 已绿**可谈升钉）。\
**产品代码 tip =** **`dc5c74615`**（wave475 mid-body if/while/expr_stmt；L4 基线 `b99e25de5`）。\
**git 锚点（考古）=** tag **`anchor-pre-xlang-rename`**。\
**现行主线 =** **Cap residual wave476**（wave475 已闭 · 日常 L2）。

| 轨                | 状态                                                                                           |
| ---------------- | -------------------------------------------------------------------------------------------- |
| 工程轨              | **wave475 L2 绿** @ **`dc5c74615`**；tip L4 **`b99e25de5`**；钉盘 **`70ca1089b`**；下一 **wave476** |
| 产品轨 tip / 钉盘     | tip = **`dc5c74615`** · L4 基线 **`b99e25de5`** · 钉盘 **`70ca1089b`** |
| mac L4 真冷        | ✅ remaining_o=0 · **OK (129)** · `/tmp/mac_true_cold_70ca1089b.log` · `/tmp/mac_true_bstrict_70ca1089b.log` |
| Ubuntu L4（金标）    | ✅ remaining_o=0 · **OK (129)** · `/tmp/ubuntu_true_cold_70ca1089b.log` · `/tmp/ubuntu_true_bstrict_70ca1089b.log` |
| 前钉 L4            | `605e7efa4` · `ace59b6cd` · `75c638465` · … |

***

## 1. 文档地图

| 文档                                             | 看什么                   |
| ---------------------------------------------- | --------------------- |
| [自举方法.md](自举方法.md)                             | Cap / R / L / M       |
| [自举步骤.md](自举步骤.md)                             | 可执行闸门                 |
| [自举验证.md](自举验证.md)                             | 怎么测                   |
| **本文**                                         | 长期 KPI + 前排（**每波必改**） |
| [当前进度.md](当前进度.md)                             | 当天快照（**每波必改**）        |
| **[当前问题分析.md](当前问题分析.md)**                     | tip 级产品债地图（21 日起权威）   |
| [自举进度-归档-2026-07-25.md](自举进度-归档-2026-07-25.md) | **25 号全日 + 26 日初 wave446**（只读） |
| skill `xlang-selfhost-product-gate`             | L4 / 双端 / 假绿禁止        |

***

## 2. 仪表盘

### 2.1 产品轨（放行面）

| 项                          | 结果                                                                                  | 日志 / 备注                                      |
| -------------------------- | ----------------------------------------------------------------------------------- | -------------------------------------------- |
| 产品 bstrict 套件数             | **129**                                                                             | 现行白名单                                        |
| 产品 L4 放行钉盘                 | **`70ca1089b`**                                                                      | 25 日 tip L4 升钉（前钉 `605e7efa4`）；双端 L4 129      |
| 本波 tip                     | **wave475**（钉盘 **`70ca1089b`**） | default mid-body if/while/expr_stmt · **日常 L2** · tip **`dc5c74615`** |
| mac 产品链 bstrict（钉盘 L4） | ✅ **OK (129)**                                                                      | L4 · `/tmp/mac_true_bstrict_70ca1089b.log` · cold `/tmp/mac_true_cold_70ca1089b.log`    |
| Ubuntu L4（金标 · 钉盘）  | ✅ **OK (129)**                                                                      | L4 · `/tmp/ubuntu_true_bstrict_70ca1089b.log` · cold `/tmp/ubuntu_true_cold_70ca1089b.log` |
| 产品矩阵（双端 tip L4）           | ✅ rv42 / option / hello（§4）+ bstrict 129                                                                | mac + Ubuntu · `70ca1089b`                                      |
| 前钉 mac/Ubuntu L4           | ✅ 129 @ `605e7efa4` · `ace59b6cd` · …                                                                | 前钉日志 `/tmp/*_605e7efa4.log`                             |

### 2.2 工程轨（继承）

| KPI           | 现                                        | 备注                   |
| ------------- | ---------------------------------------- | -------------------- |
| **T / EMPTY** | 18/18                                    | 18 号结案               |
| **N**         | 54/54 IDENTICAL                          | 回归基线                 |
| **R2 / 根修本波** | **wave475 已闭**（mid-body if/while/expr_stmt）；**wave476 待开** | 日常 L2 · 钉盘仍 **`70ca1089b`** |

***

## 3. 当前前排（≤3 条）

1. **wave476 候选**（任选一）：① default mid-body for/match ② multi mono 稀有形 ③ R2 真迁 ④ dyn Trait（仍 soft 大特性）· **勿** `fn`/`var` 伪探针
2. leave-off soft：dyn Trait；dual Method.POST；default for/match；**`fn`/`var` 非产品债**；Darwin residual
3. tip L4 基线 **`b99e25de5`** 已绿；钉盘仍 **`70ca1089b`**（升钉待你确认）；日终/整波可再 L4

***

## 4. 与 25 号归档的关系

- 25 号全日（Cap residual **wave396–445**、trait 签名/bound/mono 簇、tip L4 升钉 **`70ca1089b`**）+ **26 日初 wave446**（角列表根修 + Trae 文档审计）已整表移入 [`自举进度-归档-2026-07-25.md`](自举进度-归档-2026-07-25.md)。
- **勿**用归档 tip 或旧钉（`605e7efa4` / `ace59b6cd` / wave431 `15a09c5c5` 等）冒充现行 tip L4。**现行钉盘 `70ca1089b`**；**产品 tip wave446 `8a8aa1841`**（日常 L2，非 tip L4）。
- **Trae（25 日接续）**：产品代码 wave432–445 **真实有效**；文档侧误诊（lexer=`&&` typeck、整棵 trait 排除、tip 自相矛盾）已在 wave446 **校正**，**不是**「全程零问题」。
- 26 日后续工作记本文 §0/§2/§3/§6。

***

## 5. 双端 / 平台

| 平台                     | 本波状态                                                              |
| ---------------------- | ----------------------------------------------------------------- |
| **macOS arm64**        | ✅ tip L4 全擦 + 矩阵 + **129** @ **`70ca1089b`**；wave445/446 日常 L2 已过 |
| **Ubuntu x86\_64（金标）** | ✅ tip L4 全擦 + 矩阵 + **129** @ **`70ca1089b`**；wave445 L2 探针+bstrict 129；SHARED ABI tip L4 待收口 |
| **Windows MSYS2**      | 继承 hybrid；本波未重跑（**不需要切换**）；路径文档已 `…\xlang\x-lang`                                             |

***

## 6. 变更记录（倒序 · 26 日切后；25 号见归档）

| 日期         | SHA                                                     | 摘要                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------- | ------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-26 | **wave475** | **R2 Cap residual pure wave475**：**trait 默认 mid-body if/while/expr_stmt 假红 missing method**（free 同体绿）。根因：default hoist thin onefunc body_lets 后硬要求 `return`；commit 只 fill lets+final。权威：G.7 onefunc buf mid-body 循环 `parse_if_stmt_into` / cond+block+`append_while` / `parse_expr`+stmt_order；skip commit fill ifs/whiles/expr_stmts + src_stmt_order（seed_commit 双生）。**双端** if=7 · while=10 · assign=7 · letif=7 · combo=7 · ret7/self.v/let 回归 · run-trait · 矩阵。soft：dyn；for/match mid-body；dual POST。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** · tip **`dc5c74615`** · Ubuntu `/tmp/ubuntu_w475_probes.log` |
| 2026-07-26 | **wave474** | **R2 Cap residual pure wave474**：**`let x = self.v` / `return self.v` 默认体假红**（missing method；free 路径 return type `?`）。根因：① `parse_body_lets_into` init 仅 IDENT，TOKEN_SELF 未走 parse_expr；② onefunc return 仅 bare self\|ident / STRUCT_LIT，DOT 后缀失败。权威：G.7 body_lets IDENT\|SELF + onefunc compound return → `parse_expr_into`/`return_expr_ref`（seed+parser.x+onefunc 同 commit）。**双端** free/trait let self.v=7 · return self.v=7 · combo=7 · STRUCT/return self 回归 · run-trait · 矩阵。soft：mid-body if/loop；dyn；dual POST。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** · tip **`6a64de294`** |
| 2026-07-26 | **wave473** | **R2 Cap residual pure wave473**：**trait 默认 `let s: T = …; return s` 假红**（*cannot resolve return subexpression*；或污染 main body BLD001）。根因：① onefunc buf `body_skip_let_const` 丢 let 仍收 return ident；② commit 预置 `num_lets` 再 fill → 全局 lets 池 insert/shift。权威：G.7 `parser_parse_body_lets_into` + skip_tl fill 前 counts=0 + Self 改写 let 类型/STRUCT_LIT init。**双端** A/Self lit=7 · let Self/A/Self=A=7 · multi-let INT+STRUCT=7 · return self/multi=42 · run-trait · 矩阵。soft：`self.v` field init；mid-body if；dyn。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** · tip **`161632b90`** |
| 2026-07-26 | **wave472** | **R2 Cap residual pure wave472**：**trait 默认 `return Type {…}` / `return Self {…}` 空 impl 假红 missing method**。根因：onefunc return 仅 INT / bare self\|ident，`IDENT {` 解析失败 → hoist 不 inject。权威：G.7 onefunc → `finish_struct_lit_from_type_ident` → `return_expr_ref`；skip_tl commit STRUCT_LIT 名 Self→for + stamp resolved_type。**双端** A lit=7 · Self lit=7 · return self=42 · multi empty=42 · run-trait · 矩阵 rv/option/hello/si。soft：let+return 复杂体；dyn；dual method 直调。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** · tip **`c4c4ed1ed`** |
| 2026-07-26 | **tip L4** | **双端 tip L4 真冷 + 129 全绿 @ `b99e25de5`**（产品 `f99fafda8` + harness）。红点地图：① 陈旧 `xlang_asm2` 劫持 run-parser/panic/binary-expr/struct（July-14 wrong-binary）→ 产品路径默认 `xlang_asm`，`USE_ASM2` 才用 gen2；② **assign LHS** 误用函数 return ambient + wave465 field ambient → enum 字段 `out.method = m` 假红（Ubuntu mega 权威 = `pipeline_typeck_check_expr_assign_c`，仅改 typeck.x 不够）；③ http dual `method(Method)|method(u8)` 对 FIELD_ACCESS 误选 → execute 中间 `let mm: Method`。矩阵 rv42/option/hello；mac+Ubuntu **OK (129)**。日志：`/tmp/{mac,ubuntu}_true_cold_000d9a5ef.log` · `/tmp/{mac,ubuntu}_true_bstrict_b99e25de5.log`。钉盘仍 **`70ca1089b`**（未自动升钉）。soft：`method(Method.POST)` dual 直调；Self struct-lit default（**wave472 已收**）；dyn。PLATFORM: SHARED · **构建级别 L4** |
| 2026-07-26 | **wave471** | **R2 Cap residual pure wave471**：**① 多 empty-impl Self default 假红 parameter count mismatch** ② **\*Self self type mismatch** ③ **Self-ret default `return self` missing method**。根因：① buf onefunc 同栈槽复用 sidecar 键，`append_param` 累加（第 2 次 np=2）；parser_gen 有 `ast_pool_onefunc_reset`，buf 路径无。② rewrite/self_matches 仅 TYPE_NAMED Self，不接受 `*Self`/`*For` 接收者。③ onefunc 体只认 `return INT`。权威：G.7 `parse_one_function_buf_into` reset + skip_tl/seed twin rewrite/match + return SELF/IDENT。**双端** multi empty=42 · \*Self override=42 · \*Self empty default=42 · Self-ret id=42 · wrong \*B 仍红 · run-trait · 矩阵 rv/option/hello/si。soft：Self `{…}` struct-lit default；多 mono layout；dyn。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** · tip **`06fd3d453`** |
| 2026-07-26 | **wave470** | **R2 Cap residual pure wave470**：**trait 默认方法 `self: Self` 空 impl 假红**（wave468 hoist 保留字面 Self → method_call UFCS `type_refs_equal` 失败 *no impl*；wave469 仅修 completeness 名比对）。根因：default re-parse 不改写 Self；inject 全局一次、非 per-impl。权威：G.7 skip_tl + seed twin：`named_eq_self`/`self_matches_for` Self↔for 别名；per-impl hoist；commit 时 `pipeline_type_find_or_alloc_named` 把 param/ret Self→for。**mac** Self+default empty=42 · override=42 · concrete default=42 · explicit Self=42 · wrong B 仍红 · run-trait · 矩阵 rv/option/hello/si。soft：*Self；Self ret/struct-lit default body；多 empty impl Self。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** · tip **`4198ebbd4`** |
| 2026-07-26 | **wave469** | **R2 Cap residual pure wave469**：**trait 签名 TYPE_NAMED `Self` 假红**（`self: Self` / `: Self` vs impl `A` → parameter/return type mismatch）。根因：completeness 字面名比对 `"Self"`≠`A`。权威：G.7 `xlang_skip_trait_named_eq_self_c` + seed twin；全部 NAMED 名比对走 helper（Self→for 名）。**mac** Self full=42 · ret-only=42 · param-only=42 · wrong B 仍红 · explicit A=42 · default 回归=42 · Pair=42 · 矩阵 rv/option/hello/si。soft：Self+default hoist；*Self；多 mono layout；dyn。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** · tip **`b045c5175`** |
| 2026-07-26 | **wave468** | **R2 Cap residual pure wave468**：**trait 默认方法体**（`function m(...):T { body }` + 空/部分 `impl` 假红 missing method）。根因：skip_one_trait 只登记签名、丢弃 body；completeness 要求 impl free fn。权威：G.7 skip_tl/seed twin `method_has_default`+fn pos；check 前 onefunc re-parse + 最小 commit；override 同名已存在则跳过 inject；onefunc buf 接受 TOKEN_SELF。**mac** empty impl=42 · override=42 · partial=42 · sig-only 仍 T001 · run-trait OK · Pair 回归=42 · 矩阵 rv/option/hello/si。soft：dyn；Self 改写；无 impl 仅靠 UFCS。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** · tip **`7a89607fa`** |
| 2026-07-26 | **wave467** | **R2 Cap residual pure wave467**：**多参类型位 `Name<T,U>` + 泛型 struct 字段 mono**。根因：wave466 仅 `elem_type_ref=slot0`，mono 把所有类型参字段都换成首参 → `Pair<A,B>` host-C 两字段皆 A（BLD001）。权威：G.7 ast_pool type-arg/layout type-param sidecar + type_ref/struct_layout parse + codegen/field_access/strict 孪生。根修：全角列表入 sidecar；struct `<T,U>` 登记参名；字段类型名→槽→type_arg。**mac** Pair&lt;A,B&gt;=42 · Wrap=42 · trip=42 · 矩阵 rv/option/hello/si。soft：dyn；默认方法体；多 mono 实例共享单 layout。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED · **日常 L2 非 tip L4** · tip **`df917e968`** |
| 2026-07-26 | **wave466** | **R2 Cap residual pure wave466**：**类型位 `Name<T>` 解析 + 单参 generic struct mono**。根因：① type_ref IDENT 后不吃 `<…>` → `Wrap<i32>` XP003 ② struct 布局遇 TOKEN_LT 失败 → layout 假靠 STRUCT_LIT invent ③ 登记 `v:T` 后 host-C `struct ast_T` BLD001。权威：G.7 type_ref_slice + struct_layout/skip_tl + field_access heavy/strict 孪生 + codegen.x/seed。根修：parse type-pos angles（slot0→elem_type_ref）；skip struct 类型参角列表；field mono 先于 ambient；emit 前 mono 替换类型参字段。**mac+Ubuntu** type-pos=42 · bare=42 · take=42 · chain=7 · 矩阵 rv/option/hello · trait=42。soft：dyn；多参 Name&lt;T,U&gt; 字段映射。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED · **日常 L2 非 tip L4** · tip **`ad3fb69ca`** |
| 2026-07-26 | **wave465** | **R2 Cap residual pure wave465**：**泛型 struct 类型参字段 `return w.v` 假红 *found ?***（`struct Wrap<T>{v:T}`；`let x:i32=w.v` / `w.v+0` 绿）。根因：layout 把字段类型存为 TYPE_NAMED `T`（无 module concrete）；field_access 不把 ambient expected 灌字段结果（wave454 只禁灌 base）。权威：G.7 heavy `pipeline_typeck_field_access.c` + seed/`.x` strict_minimal 孪生。根修：字段结果为 null 或无 struct/enum 的 TYPE_NAMED 时 stamp ambient；base 仍不接 ambient。**链接**：OMIT mega 内 field_access 改 static，避免 Linux first-def 旧 `pipeline_x.o` 盖 standalone（`4ea8e16c1`）。**mac+Ubuntu** get_v=42 · chain=7 · 矩阵 rv/option/hello/si · trait main=42。soft：dyn；`Wrap<i32>` 类型位；默认方法体。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED field typeck · **日常 L2 非 tip L4** · tip **`4ea8e16c1`** |
| 2026-07-26 | **wave464** | **R2 Cap residual pure wave464**：**trait 方法签名缺 `;` 假红**（`function get(self): i32` 无分号 → 注册误吞 trait `}` → 整 trait 软跳 → impl 未 hoist → *no impl for type with method*）。根因：seed/skip_tl 方法扫在 depth==0 见 `}` 一律当「方法体结束」并 advance；签名无 `;` 时 trait 闭括号被吞。权威：G.7 seed `parser_asm_seed_register_trait_c` + skip_tl 孪生。根修：depth==0 的 `}` 留给外层；`function` 作 ASI 切下一方法；签名无 `;` 合法（与 free form 一致）。**mac** ASI no-semi get=42 · multi-method=42 · with-semi=42 · mono clone=42 · trait main=42 · missing method 仍红 · 矩阵 rv/option/hello/si。soft：dyn Trait。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave463** | **R2 Cap residual pure wave463**：**CORE-001 泛型 `size_of<T>`/`align_of<T>` host BLD001 + 桩体 0**。根因：① 零参仅 type_arg、ret 非 T → `cw_mono=0` 跳过 mono 名 → 裸 `core_types_size_of()`；② `core.types` 体 `return 0`。权威：G.7 `codegen_try_emit_size_align_of_call`（codegen.x + seed 同 commit）。根修：CALL 站 `((int32_t)(sizeof(T)))` / `_Alignof`；TYPE_ARRAY 用 wave357 `E[N]` 非 `E*`。**mac** free/import size_of=4 · generic_layout=0 · Pair/ptr/u8[4] · main/i16 core-types · A→B 回归 · 矩阵 rv42/option/hello/si。soft：dyn Trait。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED codegen · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave462** | **R2 Cap residual pure wave462**：**A→B / struct→scalar `as` host BLD001**（wave461 soft）。根因：identity 仅同型合法；异型 `struct B b=(a)` / `((i32)(a))` 非法 C。权威：G.7 codegen.x + seed 同 commit。根修：同型仍 identity；异型 struct op 发 GNU stmt-expr type-pun `({ OP_TY __xlang_as_o=(op); *(TGT*)(void*)&__xlang_as_o; })`；标量→struct compound 不变。**mac** A→B=42 · multi A→B=7 · struct→i32=42 · same=42 · scalar multi=7 · as_field=17 · as_t=7 · 矩阵 rv42/option/hello/si。soft：dyn Trait。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED codegen · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave461** | **R2 Cap residual pure wave461**：**struct 操作数 `as Struct` → compound first-field BLD001**（wave459 soft）。根因：wave459 目标为 user struct 时一律 `((TYPE){ (op) })`，`a as A`/`a as B` 把整 struct 当首字段。权威：G.7 codegen.x + seed 同 commit。根修：op 经 resolved+alias+mono 亦为 module user struct 时 **identity** emit `(op)`；compound 仅非 struct 操作数（标量→struct）。**mac** same-type as=42 · scalar multi=7 · as_field=17 · lit/var/as_t/mk 回归 · 矩阵 rv42/option/hello/si。soft：A→B / struct→scalar；dyn Trait（无 KW）。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED codegen · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave460** | **R2 Cap residual pure wave460**：**lit `7 as Struct` CTFE 整式折叠 → host C `struct T m = 7` BLD001**（wave459 soft）。根因：`glue_typeck_fold_expr_ref` EXPR_AS 在操作数可 fold 时一律 stamp `const_folded_valid`；emit 消费 fold 绕过 wave459 compound literal。权威：G.7 pipeline_glue.c CTFE producer（standalone 入链）。根修：目标 TYPE_NAMED/array/slice/vector/linear **不** stamp；操作数仍 fold；标量 AS 仍 stamp。**mac** lit multi/single=7 · var multi=7 · scalar=7 · as_t mono=7 · STRUCT_LIT=16 · lit call-arg=7 · 矩阵 rv/option/hello/si。soft：dyn Trait。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED CTFE · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave459** | **R2 Cap residual pure wave459**：**struct `as` 宿主 C 非法转型**（`x as A` / `as_t<A>(7)` → `((struct A)(x))` → BLD001）。根因：EXPR_AS 一律发 C cast；聚合类型非法。权威：G.7 codegen.x + seed 同 commit。根修：target 经 alias+mono 后若 `codegen_type_is_module_user_struct` → C99 compound literal `((TYPE){ (op) })`（首字段，其余零）；标量/指针仍 cast。**mac** simple as=42 · mono as_t multi=16 · STRUCT_LIT 回归=16 · scalar=42 · mk multi=14 · 矩阵 rv/option/hello/si。soft：dyn Trait；lit `7 as MultiField` 整式折叠边角。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED codegen · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave458** | **R2 Cap residual pure wave458**：**多不同 T 共享 mono 符号 + `T {…}` STRUCT_LIT 未 mono**（`mk<A>()+mk<B>()` / bare 同；`as_t<A>+as_t<B>` 值形参同 i32 → 单键）。根因：wave444 combo 仅值形参；零参 ret-T 裸 link 单实例；STRUCT_LIT 发源名 T；combo 去重用 type_ref 指针致同名 A 多份 redefinition。权威：G.7 codegen.x + seed 同 commit。根修：① `ret_extra` mono 键 ② 零参 ret-T multi-combo mangle ③ STRUCT_LIT 名 mono 替换 ④ TYPE_NAMED 名等价去重。**mac** mk T{}=7 · multi mk tf/bare=14 · as_t T{} multi=16 · id/mkd/unit/mk2u · 矩阵 rv/option/hello/si。soft：dyn Trait；struct cast `as`。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED codegen · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave457** | **R2 Cap residual pure wave457**：**bare multi ret-only 无 turbofish**（`mk2<T,U>():T` / `mk2u<T,U>():U` → requires_type_args）。根因：wave453 expected-return 推断硬门 `n_gp==1`；fixup 已能对 bare multi stamp expected、codegen mono 已 prefer resolved（wave455/456）。权威：G.7 单源 `pipeline_typeck_try_infer_generic_call_from_args_c`（pipeline_glue.c → standalone）。根修：expected-return 放宽 `n_gp≥1`（ret 类型参 + module TYPE_NAMED expected）；phantom type param 无值形参仍 unconstrained（与 bare `as2(x)` 同策略）。**mac** bare mk2=7 · bare mk2u=9 · as2/id2/mk_default · tf 回归 · bound ok=5/viol T001 · unit_t/prim expected 仍 requires_type_args · 矩阵 rv=42/option=102/hello/si。soft：dyn Trait；多 T 裸 link_name。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED glue typeck · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave456** | **R2 Cap residual pure wave456**：**零参 ret 非 slot0 mono**（`mk2u<T,U>():U` turbofish typeck 绿 → host C `ret A` body `B` BLD001）。根因：zero-param mono 扫 CALL 时 **name-only 幽灵**（`rfi=-1`、无 fixup）先于 typeck 真 site（`rfi==fi`、已 stamp U→B）；`concrete_at` 在 resolved=0 时用 type_arg[0]=A。权威：G.7 codegen mono（codegen.x + seed 同 commit）。根修：① 扫 CALL 两遍优先 `call_resolved_func_index==fi` ② multi type_arg 无 stamp 时 concrete_at 返 0（禁 slot0 臆造）。**mac** mk2u=9 · as2u=9 · mk2/as2=7 · mk_default/chain/id2/wrap · 矩阵 rv/option/hello/si。soft：bare multi。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED codegen · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave455** | **R2 Cap residual pure wave455**：**multi type_arg ret-only turbofish**（`as2<T,U>/mk2<T,U>():T` → *found T*）。根因：fixup 要求 gnames_n==n_gp 且无声明序参名；phantom U 使 gnames 短；产品 pure parse 不 stash 源。权威：G.7 skip_tl g_fn_gp_* + lex_skip pending + parser 登记 + glue fixup + codegen mono。根修：① count 捕 type-param 名 ② parse 后 register_pending ③ fixup 声明序 index→type_arg ④ mono prefer resolved_type_ref ⑤ reg_reset on trait_reg_reset。**mac** as2/mk2=7 · wrap_u bare+tf=42 · chain/as_t/id2 · 矩阵 rv/option/hello/si。soft：bare multi；mk2u ret-U mono。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse+typeck+codegen · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave454** | **R2 Cap residual pure wave454**：**链式 bare 泛型 `mk_default().v`**（field_access 把外层 i32 传给 CALL → wave453 误钉 T=i32；`return mk_default()` 假绿后 BLD001）。根因：① field_access ambient ≠ base 类型；② Darwin g05 live 路径是 **strict_minimal weak** field_access（filtered 把 heavy glue 降 local）。权威：G.7 heavy `pipeline_typeck_field_access.c` + product `pipeline_glue_strict_minimal` seed/.x 同语义；expected 门控仍在 pipeline_glue typeck。根修：① 禁止 field ambient→base；② CALL base 按字段名在 layouts 唯一反推 owner TYPE_NAMED；③ expected-ret/fixup 仅 module TYPE_NAMED。**mac** chain=7 · chain bound ok=5/viol T001 · bare mk=7 · as=42 · ret bare requires_type_args · ambig 红 · id/second/za · multi=42 · rv/option/hello/si。soft：dyn。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED field typeck · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave453** | **R2 Cap residual pure wave453**：**bare 零参/ret 仅类型参无 turbofish → requires_type_args 或 *found T***（`let a: A = mk_default()` / `as_t(42)`）。根因：wave448 只从值实参推断；fixup 要求 turbofish type_arg；bound 对 ret 仅类型参无 slot。权威：G.7 单源 pipeline_glue typeck。根修：① expected 保留到 generic 门控+fixup；② `n_gp==1` 且 ret 为类型参 + ambient expected 放行；③ fixup `n_ta==0` 戳 expected；④ bound 从 expected 填 ret 仅类型参 slot。**mac** bare mk=7 · bare as=42 · bound ok=5/viol **T001 红** · unit_t bare 仍 requires_type_args · wave452 回归 · multi-file-generic · rv42/hello。soft：链式 `.v`；dyn。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED glue typeck · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave452** | **R2 Cap residual pure wave452**：**ret 类型参不在值形参 → *found T* 假红**（`as_t<T>(i32):T` / `mk_default<T>():T`）。根因：parser 仅存 `call_num_type_args` 个数；fixup 只能靠形参名映射；codegen mono 表仅覆盖值形参。权威：G.7 单源 ast_pool 侧车 type_arg + primary 解析 + glue fixup + codegen mono。根修：① `pipeline_expr_append/call_type_arg_ref_at` 侧车；② primary 解析 turbofish type_ref；③ fixup 非 module 名 ret → type_arg；④ mono 补 ret T→concrete（含零参 bare）。**mac** as_t=42 · mk=7 · second/first_of/tf/id/getv/za · bound viol **T001 红** · multi-file-generic · rv42/hello/si。soft：bare 无 turbofish；dyn。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave451** | **R2 Cap residual pure wave451**：**泛型 CALL 返回位仅 identity → 非 param0 类型参数返回假红**（`second<T,U>(a,b):U` → *found U*；`first_of` 绿是 param0）。根因：`glue_generic_call_fixup_resolved_type_c` 只比 ret 与 **param0** 名，用 arg0 戳记；parser 无 type_arg type_ref。权威：G.7 单源 pipeline_glue fixup。根修：扫**全部**形参，ret TYPE_NAMED 名==formal[i] → 用 arg[i] resolved。**mac** second=42 · first_of=17 · tf second=33 · id/getv=42 · za_tf=14 · bound ok=42/viol T001 · multi-file-generic · 矩阵 rv/option/hello/si。soft：零参 ret T / ret T 不在形参。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED glue typeck · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave450** | **R2 Cap residual pure wave450**：**零参泛型 turbofish mono 不 emit → BLD001**（`unit_t<Marker>()` typeck 绿，调用点裸 `unit_t()`，定义侧跳过）。根因：`codegen_try_emit_generic_identity_mono` 硬门控 `num_params<=0`；parser 仅存 `call_num_type_args` 个数、无 type_arg type_ref；调用点 C3 仅 `np_mono>0` 时 mangle 故回落裸名。权威：codegen.x + seed 同 commit。根修：`num_params==0` 且存在匹配 CALL → 按 **裸 link_name** 单次 emit body（phantom T 共享一实例）。**mac** za_tf=7 · multi=22 · bare requires_type_args 红 · bound ok=5/viol T001 · id/getv=42 · 矩阵 rv/option/hello/si。soft：bare 零参返回位推断；ret `T` 需 type_arg type_ref；dyn。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED codegen · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave449** | **R2 Cap residual pure wave449**：**无 turbofish 泛型 bound 假绿**（wave448 后 `copy(b)` B 未 impl Clone 仍过编 run=99）。根因：bound 检查仅 parse-time 源扫 `IDENT < Type > (`；推断放行后 bare 调用无 type args 文本 → 永不入 g_call_*。权威：G.7 单源 `xlang_generic_bound_check_type_args_c`（skip_tl；turbofish 与 typeck 共用）+ typeck `pipeline_typeck_check_inferred_generic_bounds_c`（形参 TYPE_NAMED 首现序 → 实参具体类型名）。**mac** no-tf viol T001 红 · no-tf ok=42 · multi Default 违红 · multi ok=42 · turbofish viol 仍红 · id(a)=42 · 矩阵 rv/option/hello/si。soft：零参泛型；diag 行号可弱；dyn。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED parse+typeck · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave448** | **R2 Cap residual pure wave448**：**无 turbofish 泛型实参推断**（`id(a)` / `getv(a)` 等合法 X 调用 → typeck `requires_type_args` → XT001/XP003）。根因：`pipeline_typeck_check_call_generic_type_args_c` 在 `num_generic_params>0 && num_type_args==0` 硬拒；codegen mono 已从实参取类型（wave443–447）。权威：`pipeline_glue.c`（standalone 入链）。根修：`pipeline_typeck_try_infer_generic_call_from_args_c`（实参 resolved 齐 + 同名 TYPE_NAMED 形参统一）成功则放行，否则仍 requires_type_args。**mac** id/getv/absdiff/copy_c/copy_both/same-name unify/turbofish 回归=42 · mismatch 红 · 矩阵 rv/option/hello/si。soft：零参泛型；bound 无 turbofish 源扫描；dyn。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED glue typeck · **日常 L2 非 tip L4** |
| 2026-07-26 | **wave447** | **R2 Cap residual pure wave447**：**non-identity generic mono 债**（`getv<T>(x:T):i32{return x.v}` / `absdiff<T>(x,y):i32{if…}` call site 已 mangle 但 mono 不 emit → BLD001）。根因三层：① identity 门控要求 ret/p0 同名 TYPE_NAMED；② 签名返回类型硬用 param0 具体型；③ `codegen_call_mono_type_at` arg0 优先 call 返回类型（i32）非实参（A）；另放宽门控后 i32→i32 self-map 致 `emit_type` 无限递归 SEGV。权威：codegen.x + seed 同 commit。根修：放宽门控 + 签名 `emit_type(ret/param)` under mono_active + 实参优先 mono 类型 + `conc!=type_ref` 防自递归。**mac** field/if/clone/identity/multi/i32-param=42 · 矩阵 rv/option/hello/si。soft：XP003；dyn；`fn`/`Self`/`var` 非语言债。钉盘仍 **`70ca1089b`**。PLATFORM: SHARED codegen · **日常 L2 非 tip L4** |
| 2026-07-26 | docs | **日切归档**：25 号全日 + 26 日初 wave446 仪表盘与 §6 流水整表迁入 [`自举进度-归档-2026-07-25.md`](自举进度-归档-2026-07-25.md)；本文 26 日切后重新起表。钉盘仍 **`70ca1089b`** · tip **wave446** @ **`8a8aa1841`** · 下波 **wave447**。 |
| 2026-07-26 | **`8a8aa1841`** / **`54293a3aa`** / **`e1e49fe88`** | **R2 Cap residual pure wave446（日切前已闭，明细见 25 归档）**：**parser 角列表跨语句贪吞 → 假 *if condition must be bool***；根修 skip 语句边界 + lexer.x 结构体 lit `;`；Trae 文档审计。**日常 L2 非 tip L4** · 钉盘 **`70ca1089b`**。 |

***

## 7. 禁止清单（本阶段）

- [ ] 未用旧 stage1 / 未用 25 号归档 tip 冒充 26 日 tip L4
- [ ] 未把 prove / Stage2 / N++ 单独写成「可自举」
- [ ] 未 BEST_EFFORT / soft-skip 糊绿
- [ ] 未 seed/.x/glue 分叉合入
- [ ] 未把 mac `-dead_strip` 假绿当 Ubuntu 全链已过
- [ ] 日常 L2 未写成 tip L4；谈升钉须双端 L4 + 129
