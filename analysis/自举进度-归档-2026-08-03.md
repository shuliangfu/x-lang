# 自举进度归档 · 2026-08-03

> **归档说明（2026-08-04）**：本文档归档 **2026-08-03** 当日完成的全部自举波次与相关变更记录（**wave1227–1240** · §6 共 **15** 行）。\
> **前置归档**：[自举进度-归档-2026-08-02.md](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · [31](自举进度-归档-2026-07-31.md)\
> **后续进展**：见 [自举进度.md](自举进度.md)（3 号归档后 · 2026-08-04 起表）\
> **勿**把本归档 tip／单叶日志冒充现行钉盘或 tip L4 安全网。现行钉盘仍以主文档为准（**`77b334842`**）。\
> 请勿再向本归档追加「当前前排」。

***

## 归档背景

8 月 3 日为 **BC 8.3.1 glue 叶收口 + check 工具链启动** 全日（**产品钉盘仍 `77b334842`** · tip L4 安全网 **`ffc0b646f`** · **不升钉**）：

- **BC 8.3.1 G.7 migrate／fold／清理**：region／soa／codegen outbuf／loop labels／mega_body；物理删 `#if 0` 死块；dead fn + 冗余 fwd decl（wave1229–1240）
- **产品／工具债**：字符串字面量化（wave1232）；check multi-file spinner + summary（wave1227）+ DepCtx sidecar release（wave1228）+ freestanding `usleep`／spinner 闸（wave1231）
- **日切**：2 号全日 §6 迁入 [`自举进度-归档-2026-08-02.md`](自举进度-归档-2026-08-02.md)
- **日终 tip 锚点**：wave1240 **`b390cb660`**（dead fn 清理 · 双端 L2）
- **平台**：多数波双端 L2；wave1227–1229 初记 macOS L2（后补强制双端）
- **遗留（4 日起收）**：Ubuntu `xlang check` 路径／内存／spinner 工具债 → wave1241–1245 + braille spinner

***

## 变更记录（倒序 · 8 月 3 日）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-08-03 | **wave1240** · BC 8.3.1 · dead fn + 冗余 fwd decl 清理 | **代码** · **不升钉** · **L2 双端 ✅**。删 dead function pipeline_elf_ctx_diag_stderr（glue.c decl ＋ ast_pool.c:6606-6642 def · 零 caller 遍 .c/.x/.h · 被 pipeline_elf_log_unresolved_patch 取代）＋ 4 个冗余 fwd decl（pipeline_elf_ctx_reloc_offset_set／pipeline_macho_write_o_to_buf_c／pipeline_elf_ctx_total_code_len／pipeline_elf_ctx_diag_stderr · 均定义在 ast_pool.c #include L2283 之后、所有 mega-TU 调用之前 · decl 冗余）。保留：2 visibility decl（pipeline_visibility_mode／allow_func · API 文档）＋ WPO/PGO／asm_skip decl（mega_body L2281 调用在 ast_pool.c L2283 def 之前 · 需 fwd 可见）。2 files +5/−42（净 −37）。验证：rm pipeline_x.o → g05 pure-ld OK（mac 60 objs／Ubuntu 62 objs）· 双端 rv=42／hello／option=0／si=0。macOS check compiler：252 files, 0 errors, 31 warnings。PLATFORM: SHARED。下一：8.3.3 field_access／glue.c 更多 fwd decl 清理。Tip **b390cb660** |
| 2026-08-03 | **wave1239** · BC 8.3.1 · 物理删除 glue.c #if 0 死代码块 | **代码** · **不升钉** · **L2 双端 ✅**。删除 wave1154 隔离的 #if 0 死代码块（L2106-2246，~137 LOC），内含 static 函数 glue_emit_block_stmt_order_let_const_elf（零调用，已被 pipeline_asm_emit_block_body.c 的 backend_emit_block_body_sync_elf 取代，走相同 stmt_order let/const init 路径）。块已 #if 0 不参与编译，纯清理：glue.c 3550→3413，无功能变更。验证：rm pipeline_x.o → g05 pure-ld OK（mac 60 objs／Ubuntu 62 objs）· 双端 rv=42／hello／option=102／si。macOS check compiler：252 files, 0 errors, 31 warnings（unused fn）。**Ubuntu check 工具债**：`xlang check compiler` 报 "no .x files found" 且单文件 check 段错误（exit 139）— check 命令 Linux 路径扫描 bug，非 .x 语法错误（macOS 同源 check 0 errors + 编译器成功编所有 .x）。PLATFORM: SHARED。下一：glue.c 残留 fwd decl 清理／8.3.3 field_access。Tip **cfd66c422** |
| 2026-08-03 | **wave1238** · BC 8.3.1 · G.7 migrate mega_body（→ 新域 pipeline_asm_codegen_mega_body） | **代码** · **不升钉** · **L2 双端 ✅**。G.7 同域 fold：asm codegen 主循环 `pipeline_backend_asm_codegen_ast_to_elf_mega_body_c`（193行 · per-module WPO/PGO emit-order 函数迭代 ＋ prologue/param-home/block-body/epilogue ＋ sret/modlet lit/async cps）自 pipeline_glue.c L2411-2604 迁入新域文件 pipeline_asm_codegen_mega_body.c（single-purpose domain · #include 于原位）。同点机械平移：所有 deps 可见性不变（pipeline_glue_AsmFuncCtxLayout typedef L84 ＋ pipeline_asm_ctx_layout static L86 ＋ pipeline_asm_compute_frame_size_c L811 ＋ g_pipeline_asm_*sret globals ＋ GLUE_TYPE_KIND_F32/F64_ORD macros ＋ extern decls L2403-2409）。sole caller：ast_pool.c L1733。2 files +231/−194（glue 净 −186 · 新域 223 = 28 header ＋ 193 body ＋ blank）· glue.c 3736→3550。验证：rm pipeline_x.o → g05 pure-ld OK（mac 60 objs／Ubuntu 62 objs）· 双端 rv=42／hello `"Hello World\n"`／option=102／si。PLATFORM: SHARED。下一：glue.c #if 0 死代码清理（block_stmt_order_let_const ~134行）／残留 fwd decl 清理／8.3.3 field_access。Tip **2019768fa** |
| 2026-08-03 | **wave1237** · BC 8.3.1 · G.7 migrate backend_ctx_push/pop_loop_labels（→ fold_count_up_while） | **代码** · **不升钉** · **L2 双端 ✅**。G.7 同域 fold：AsmFuncCtx 循环 break/continue 标签栈 push/pop 自 pipeline_glue.c L963-1052 迁入 pipeline_asm_emit_fold_count_up_while.c EOF（紧接 sole caller backend_try_fold_count_up_while_elf L1413/1484 · loop folding domain）。fwd decl 保留在 glue.c L971-973（#include L2405 之前）供 callsite 可见（原 def 在 #include 前充当声明，迁移后须补 fwd）。deps 全经更早定义可见：pipeline_glue_AsmFuncCtxLayout typedef（L84）＋ pipeline_asm_ctx_layout static（L86）。无 #if guard（plain extern glue · .x seed 用未 mangle 名 ctx_push/pop_loop_labels · 非源码 dup）。2 files +112/−89（glue 删 89 块回填 14 marker/fwd · fold_count_up_while ＋98 block+header）。验证：rm pipeline_x.o → g05 pure-ld OK（mac 60 objs／Ubuntu 62 objs）· 双端 rv=42／hello `"Hello World\n"`／option=102／si。PLATFORM: SHARED。下一：glue.c leaf residual（block_stmt_order 131行／mega_body 192行）或 8.3.3 field_access。Tip **31edec0e8** |
| 2026-08-03 | **wave1236** · BC 8.3.1 · G.7 migrate codegen_try_emit_slice_init_from_array_var（→ outbuf） | **代码** · **不升钉** · **L2 双端 ✅**。G.7 同域 fold：slice init emit（`let s: T[] = arr` 写出 `{ .data, .length }`）自 pipeline_glue.c 迁入 pipeline_codegen_outbuf.c EOF（紧接 glue_codegen_out_append_* callees · codegen outbuf append domain）。#if guard 保留（extern fn dup of .x seed）。全 deps 经更早 fwd decl 可见（L213／396-406，全在 outbuf.c #include L445 之前）：pipeline_type_kind_ord_at／_array_size_at／pipeline_arena_expr_ptr／pipeline_block_let_*／pipeline_expr_var_name_*。sole extern caller：codegen_gen.c L13530 ＋ codegen.x seed。glue −119／outbuf ＋132，净 ＋13（迁移标记 ＋ docblock ＋ guard）。验证：rm pipeline_x.o → g05 pure-ld OK（mac 60 objs／Ubuntu 62 objs）· 双端 rv=42／hello `"Hello World\n"`／option=102／si。PLATFORM: SHARED。下一：glue.c leaf residual（loop_labels／block_stmt_order／mega_body）或 8.3.3 field_access。Tip **88e3f7feb** |
| 2026-08-03 | **wave1235** · BC 8.3.1 · G.7 migrate pipeline_codegen_emit_float_lit_c（→ outbuf） | **代码** · **不升钉** · **L2 双端 ✅**。G.7 同域 fold：float literal emit 自 pipeline_glue.c 迁入 pipeline_codegen_outbuf.c EOF（紧接 sole callee glue_codegen_out_append_cstr L76 static · codegen outbuf append domain）。deps：codegen_CodegenOutBuf（全局）＋ snprintf（libc 经 TU #include 链）。sole extern caller：codegen_gen.c L10593 ＋ codegen.x seed。glue −48／outbuf ＋56，净 ＋8（迁移标记 ＋ docblock）。验证：rm pipeline_x.o → g05 pure-ld OK（mac 60 objs／Ubuntu 62 objs）· 双端 rv=42／hello `"Hello World\n"`／option=102／si。PLATFORM: SHARED。下一：glue.c leaf residual（loop_labels／slice_init／block_stmt_order）或 8.3.3 field_access。Tip **1c64b25cf** |
| 2026-08-03 | **wave1234** · BC 8.3.1 · G.7 migrate pipeline_typeck_check_block_one_region_c（→ region_assign） | **代码** · **不升钉** · **L2 双端 ✅**。G.7 同域 fold：region/with_arena 单条 dispatch 自 pipeline_glue.c mega-TU 迁入 pipeline_typeck_region_assign.c EOF（紧接 with_arena scope + region scope push/pop · M-3／MEM-C1 region dispatch domain）。删冗余 unsafe_depth_push/pop fwd decl（原 L3750-3751 · L3238-3239 已覆盖全 callsite）。deps 全经更早 fwd decl 可见：pipeline_block_region_*（ast_pool.c #include L2839）＋ unsafe_depth_push/pop（L3238-3239）＋ with_arena／region scope（本文件）。glue −47／region_assign ＋59，净 ＋12（迁移标记 ＋ docblock）。验证：rm pipeline_x.o → g05 pure-ld OK（mac 60 objs／Ubuntu 62 objs）· 双端 rv=42／hello `"Hello World\n"`／option=102／si。PLATFORM: SHARED。下一：glue.c leaf residual（loop_labels／float_lit／slice_init）或 8.3.3 field_access。Tip **ea92708b4** |
| 2026-08-03 | **wave1233** · BC 8.3.1 · G.7 migrate pipeline_typeck_check_call_slice_region_c（→ region_assign） | **代码** · **不升钉** · **L2 双端 ✅**。G.7 同域 fold：CALL 实参 slice 域检查自 pipeline_glue.c mega-TU 迁入 pipeline_typeck_region_assign.c EOF（紧接 check_slice_region_assign_c · M-3 slice region domain）。2 个 fwd decl 跟随（typeck_check_call_ptr_struct_compat_c static ＋ check_extern_call_unsafe_boundary_c extern）。sole extern caller：typeck.x typeck_check_expr_call ＋ typeck_gen seed。glue −144／region_assign ＋139，净 −5（迁移标记）。验证：rm pipeline_x.o → g05 pure-ld OK（mac 60 objs／Ubuntu 62 objs）· 双端 rv=42／hello `"Hello World\n"`。PLATFORM: SHARED。下一：check_block_one_region。Tip **a5750236f** |
| 2026-08-03 | **wave1232** · 字符串字面量化重构 + *u8 typeck fix | **代码** · **不升钉** · **L2 双端 ✅**。Grok refactor：手写 ASCII 字节数组 `[102,117,...]` → 字符串字面量 `"function"`（59 files）；`match_keyword`／`match_keyword_buf` param `u8[]`→`*u8`；69 call sites 补 `"str" as *u8` 显式转换（X 字符串字面量不自动 coerce `*u8`）。修 Ubuntu `backend_enc_dispatch.o` 错误 -D 残留（rax 符号被 `#ifdef` 排除 → 重编 4 dispatch .o；g05_ensure 跳过列表不重建）。验证：双端 rv=42／hello `"Hello World\n"`／option／si。PLATFORM: SHARED。下一：check_call_slice_region。Tip **aae077672** |
| 2026-08-03 | **wave1231** · freestanding usleep + spinner nostdlib 闸 | **代码** · **不升钉** · **L2 双端 ✅**。根因：wave1227 spinner 调 `usleep`，Ubuntu g05 pure-ld nostdlib 无 `-lc` → UNDEF；且 nostdlib `pthread_create` 同步跑 start_routine，无限 spin 会死挂。修：`bootstrap_nostdlib_stubs` 补 `usleep`（SYS_nanosleep=35）；spinner 在 `bootstrap_nostdlib_pthread_is_stub` 时只写静态帧。验证：Ubuntu pure-ld 62 objs OK · mac pure-ld 60 objs OK · 双端 rv=42／hello=0／option=102／array=0／slice=20／slice-field OK。PLATFORM: LINUX freestanding + SHARED。Tip **98c0975e3** |
| 2026-08-03 | **wave1230** · BC 8.3.1 · G.7 migrate pipeline_fill_soa_field_access_for_asm_emit（→ typeck_soa） | **代码** · **不升钉** · **L2 双端 ✅**（Ubuntu 先被 wave1227 usleep 挡链，wave1231 修后同 tip 树绿）。G.7 同域 fold：public SoA／AoS FIELD_ACCESS 回填入口自 pipeline_glue.c 迁入 pipeline_typeck_soa.c（紧接 pipeline_typeck_field_soa_index_c）。deps 均在更早 same-TU #include（struct_lit／field_access／block_inits）可见。glue 4218→4141（-77 净）；soa 239→375（+136 含 G.9 英文 docblock）。No dual authority — seeds／ast_pool 仅 extern／weak。验证：pipeline_x · g05 pure-ld · nm 符号 T · 双端 rv／hello／option／array／slice／slice-field。PLATFORM: SHARED。下一：check_call_slice_region 或 host-cc 叶。Tip **a3e6ab014** |
| 2026-08-03 | **wave1229** · BC 8.3.1 · G.7 migrate pipeline_fill_array_lit_types_for_skipped_typeck（→ block_inits） | **代码** · **不升钉** · **L2 macOS**（后补：自本波起每步强制双端 L2）。G.7 同域 fold：public skipped-typeck ARRAY_LIT 回填入口自 pipeline_glue.c 迁入 pipeline_asm_emit_block_inits.c（紧接 static glue_fill_array_lit_types_in_block，wave1118–1123 同域）。sole callee 同文件；deps 在 glue #include 前已声明故同 TU 可见。glue 4235→4218（-17 净）；block_inits 787→846（+59 含 G.9 英文 docblock）。No dual authority — seeds／ast_pool 仅 extern／weak。验证：pipeline_x · g05 pure-ld · rv=42 · hello · option · array · slice · nm 符号 T。PLATFORM: SHARED。下一：fill_soa_field_access 或 check_call_slice_region。Tip **5145328af** |
| 2026-08-03 | **docs** · 日切归档 8 月 2 日 | **文档 only** · **不升钉**。2 号全日 §6（**wave1130–1220** · 43 行）整表迁入 [`自举进度-归档-2026-08-02.md`](自举进度-归档-2026-08-02.md)；本文从 3 日起重新起表（保留 wave1227／1228）。§2.1／§4／§5 tip L4 安全网对齐 **`ffc0b646f`**。钉盘仍 **`77b334842`**。下一：BC 续迁或 host-cc 叶。 |
| 2026-08-03 | **wave1228** · DepCtx／arena／module sidecar release-before-free | **代码** · **不升钉** · **L2 macOS**。根因：`g_xlang_depctx_sc`（MAX 64）进程级表在 `pipeline_dep_ctx_heap_destroy` 只 `free(ctx)` 后仍 `used`＋悬空 key → 批 check 表耗尽、后续文件 dep／parse 降级。**权威**：`depctx_sidecar_free` + `pipeline_dep_ctx_sidecar_release`（`ast_pool.c`）· `heap_destroy` 先 release 再 free（`ast_pool_dep_ctx.c`）· stack-escape gate 在 free 前 `ast_pool_arena_release`／`ast_pool_module_release`（`pipeline_parse_orch.c`）。G.7 单 teardown；PLATFORM: SHARED。验证：rv=42 · hello · option · `run-check` OK · 多文件 check summary 3／3。不升钉。Tip **5b1aa984d** |
| 2026-08-03 | **wave1227** · check multi-file spinner + summary | **代码** · **不升钉** · **L2 macOS**。`fmt_check_cmd_thin.x` + seed rest：N>1 旋转 spinner（`|/-\\`）+ 相对路径 + 结束 summary；N==1 成功全静默（deno/`run-check` 契约）；`spin_*` extern 包 `unsafe`；lib-root 亦匹配裸 `compiler` 路径。验证：`tests/run-check.sh` OK · 单文件 out_len=0 · 多文件 summary。不挡 BC。Tip **947a78ddf** |

***
