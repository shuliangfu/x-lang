# 自举前全面检查（阶段 9 启动前）

> 在开始用 .sx 重写编译器（自举）前，按本文逐项确认：无阻塞项、建议项已知晓。

---

## 一、自举前汇总表（analysis/自举前-目标与缺口分析.md）—— 全部 ✓

| 类别 | 项 | 状态 |
|------|----|------|
| C 互操作 | FFI、extern "C"、内存契约文档 | ✓ |
| 内存契约 | struct 默认对齐、Packed 显式、Slice (ptr,len) | ✓ |
| UB | 自举子集内 UB 清单与约定 | ✓ |
| ABI/布局 | 结构体/枚举/切片与 C 对应关系、layout_abi 测试 | ✓ |
| Result 寄存器化 | Result_i32 双槽布局、err 在第二槽 | ✓ |
| I/O 抽象 | Reader/Writer trait、ReadOnlySlice/WriteOnlySlice、超时预留 | ✓ |
| 零拷贝 Buffer | std.mem Buffer(ptr,len,handle) ABI 24 字节 | ✓ |
| std.io.driver 占位 | std/io/driver.sx、Buffer/IO_Result/Completion/submit 等 | ✓ |
| I/O 设计文档 | 高并发IO与多平台.md | ✓ |
| 异步状态机/方向 | 无分配状态机、显式环境、文档化 | ✓ |
| std.net 占位 | std/net/mod.sx、Ipv4Addr/TcpStream/connect/listen/accept | ✓ |
| 网络协议检查项 | 非阻塞、[]Buffer、超时 u32 ms（文档） | ✓ |
| 错误诊断 | 位置 + 期望/实际类型（见下） | ✓ |
| 子集稳定 | 自举期间少动语法/类型 | ✓ |
| 测试 | run-all.sh 与 Makefile test 全量回归 | ✓ |

**结论**：汇总表内「必须/建议」项在文档与实现上均已标为完成，无未完成项阻挡启动阶段 9。

---

## 二、错误诊断（面包屑）—— 已全部完成（改代码实现）

- **Parser**：`parse error at line:col: msg`（Token 带 line/col）。
- **Typeck**：类型不匹配时输出 `expected <类型>, found <类型>`，并带 **at line:col**（ASTExpr 已增加 line/col，parser 建 AST 时从 Token 写入；typeck 报错时调用 `typeck_err_loc(e)` 输出位置）。
- **结论**：位置（解析 + 类型检查）+ 期望/实际类型 已完整，错误诊断标为 ✓。

---

## 三、条件编译与多平台

- **分析文档要求**：标准库按 OS 选实现，需 `#if` 或等价机制，或**约定由构建系统 + 多文件**实现。
- **当前**：.sx 无 `#if`；README 与 std 约定为「内部按目标平台条件编译，用户只写一套 API」、多目标用 `-target`。
- **结论**：已采用「构建系统 + 多文件/多目标」约定，满足自举前要求；自举后若需 .sx 内条件编译再扩展。

---

## 四、子集稳定

- **含义**：自举过程中少动语法/类型，避免两代编译器行为不一致。当前仅为口头/隐含约定，未成文。

**若要标为「全部完成」，可二选一：**

| 做法 | 说明 |
|------|------|
| **写文档** | 写一份《自举语言子集》或在本文件/README 中增加一节，列出当前 shuc 支持的语言子集（关键字、类型、表达式、语句、泛型/trait/多文件等），并约定「自举期间仅小步扩展，新语法/新类型在自举验证通过后再加」。 |
| **不写文档** | 在 README 或本文中加一段话，约定「自举语言子集 = 当前已支持语法与类型，自举期间少动语法/类型」，即视为约定已达成，不单独成文。 |

当前为「✓ 约定」：若选「写文档」并落笔成文，可标为 ✓；若选「不写文档」并在一处明确写出上述约定，也可标为 ✓。

---

## 五、analysis/ 文档与 Git

- **约定**：`analysis/` 目录**故意不提交**到 Git（.gitignore 中含 `analysis/`）。该目录用于自举前分析与设计文档（如 自举前-目标与缺口分析.md、高并发IO与多平台.md、自举语言子集.md），**项目完成后将删除**，不作为仓库长期保留内容。

---

## 六、启动自举前最后确认清单

在真正开始写「用 .sx 写编译器前端」之前，建议做一次机械检查：

| # | 检查项 | 如何确认 |
|---|--------|----------|
| 1 | 全量测试通过 | `./tests/run-all.sh` 或 `make -C compiler test` 全部 OK |
| 2 | core/std 目录布局 | 各模块为「目录 + mod.sx」，import 解析先试 xxx.sx 再试 xxx/mod.sx |
| 3 | 宿主编译器可用 | `make -C compiler` 得到 `shuc`，能编译当前 examples 与 tests |
| 4 | 自举前必须项已落地 | 内存契约、Result 寄存器化、std.io.driver/std.net 占位、Reader/Writer、Buffer ABI、UB/ABI 文档或测试已存在且汇总表全 ✓ |
| 5 | 子集与约定 | 明确「自举子集 = 当前支持语法/类型」及「自举期间少动语法」 |

---

## 七、结论

- **无未完成项阻挡自举**：自举前目标与缺口分析汇总表中各项均已标为完成；错误诊断有「期望/实际类型」，缺的仅是 typeck 错误中的源码行号，属可选增强。
- **建议**：跑一遍 `./tests/run-all.sh`，确认通过后即可按 README 阶段 9 顺序启动（先 .sx 前端 + 宿主 C 后端，再中端/后端 .sx 化，最后自举验证）。

若你希望，我可以再根据当前仓库状态补一条「自举期间子集冻结」的说明到 README 或开发时序中。
