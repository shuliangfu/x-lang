# Xlang / X language 自举进度

> **本文 = 2026-09-07 日切归档**（21 行 §6）。现行 KPI／前排见 [`自举进度.md`](自举进度.md)。

> **更新**：2026-09-07 · **日切归档** · 钉盘 **`b5be5ed97`**（**不升钉**）· §6 收 7 日 **21** 行 · 9 月 8 日起主文档重写 · **金标 SSH＝`ubuntu-server`** · Ubuntu 验＝**git pull（禁 scp）**\
> **产品 L4 放行钉盘**：**`b5be5ed97`**（2026-09-02 升钉 · 前 `e8176cbe5` 2026-08-23 · 再前 `f7424ae47` 2026-08-15）\
> **日切时本波状态**（一行）：本日主刀＝**9.7.1 opaque FILE\* 脸整体换 fd 句柄面**（`75e074199`，9.5.3 slice3 残余统一主刀）＋ 9.5.3 slice2/3a/3b stdio 脱离 libc（`b0d1c8068`／`a5ba1d2f3`／`0a650a095`）＋ 9.7.2–9.7.7 脸/冷体收敛系列（GAS 行表／lib_roots 槽／driver_stdio fd 脸／usage_write 薄委托／stack_esc_gate 归一／large-stack `_impl` 中转坍缩）；同日 **9.4.2 `void*(*)(void*)` C ABI**（`fb4019d32`，fn-ptr 全局表／ADDR_OF 全局不 seed 邻域收口）＋ **9.4.3 `main char**` argv**（`7c70d81c3`，run 漏斗转发用户位置参）双端闭环；**9.6.0–9.6.3** 全局标量双家园／巨型字串表 NULL／`u8[N]` 丢零／static 重名覆盖四连根修；**9.5.4/9.5.5** write／read 路径 Cap 收口（`af4c479c1`／`47b3fd349`）；运维刀＝g05 Darwin/Windows relink `_xlang_sys_*`（`539c9360f`）＋ macos host-cc minos 11.0 对齐（`014c72fb2`）。leftover unique **0**（Windows 纯 asm 邻域终局）。钉盘仍 `b5be5ed97`。
> **下波候选**（日切时）：**9.2／9.3 域勘正立波**（9.2.1 mbedtls BIO Cap 化＋9.3 五条逐项勘正）——已于 9 月 8 日落地 `2a9f3dcae`＋`c5b2178a0`（9.3.3 裸十进制 `#if` 字面量根修＝9.3 域收官）。日常闸门＝**SHARED 双端 L2 产品矩阵**（mac＋Ubuntu 5/5）。

> **效率纪律（2026-08-07）**：禁止再开「一 BSS cell 一波」；一波至少一 **域文件** 真减 host-cc LOC 或 present−1；过程上 prefer+heat 一次／域，双端矩阵同波一次\
> **验证纪律**：**自举期暂停 check 闸门**（L2/L4 均不跑 `xlang check`，2026-08-05）；**日常闸门＝SHARED 双端 L2 产品矩阵**（mac + Ubuntu `./xbuild l2-matrix`；wave299：捕获 rv=42／opt=102 成功码，禁 set -e 截断）。谈自举／升钉才 L4 + 矩阵 + 全量 bstrict

> **方法**：[自举方法.md](自举方法.md) · [Windows兼容时序-删种子前后.md](Windows兼容时序-删种子前后.md)\
> **时序全景（换 IDE 续作）**：[自举时序.md](自举时序.md)（S0–S8 先→后 · 30 秒协议）\
> **验收**：[compiler/docs/SELFHOST.md](../compiler/docs/SELFHOST.md) · skill `xlang-selfhost-product-gate`\
> **归档**（本文＝7 日）：[06](自举进度-归档-2026-09-06.md) · [05](自举进度-归档-2026-09-05.md) · [04](自举进度-归档-2026-09-04.md) · [03](自举进度-归档-2026-09-03.md) · [02](自举进度-归档-2026-09-02.md) · [01](自举进度-归档-2026-09-01.md) · [31](自举进度-归档-2026-08-31.md) · [30](自举进度-归档-2026-08-30.md) · [29](自举进度-归档-2026-08-29.md) · [28](自举进度-归档-2026-08-28.md) · [27](自举进度-归档-2026-08-27.md) · [26](自举进度-归档-2026-08-26.md) · [25](自举进度-归档-2026-08-25.md) · [24](自举进度-归档-2026-08-24.md) · [23](自举进度-归档-2026-08-23.md) · [22](自举进度-归档-2026-08-22.md) · [21](自举进度-归档-2026-08-21.md) · [20](自举进度-归档-2026-08-20.md) · [19](自举进度-归档-2026-08-19.md) · [18](自举进度-归档-2026-08-18.md) · [17](自举进度-归档-2026-08-17.md) · [16](自举进度-归档-2026-08-16.md) · [15](自举进度-归档-2026-08-15.md) · [14](自举进度-归档-2026-08-14.md) · [13](自举进度-归档-2026-08-13.md) · [12](自举进度-归档-2026-08-12.md) · [11](自举进度-归档-2026-08-11.md) · [09](自举进度-归档-2026-08-09.md) · [08](自举进度-归档-2026-08-08.md) · [07](自举进度-归档-2026-08-07.md) · [06](自举进度-归档-2026-08-06.md) · [05](自举进度-归档-2026-08-05.md) · [04](自举进度-归档-2026-08-04.md) · [03](自举进度-归档-2026-08-03.md) · [02](自举进度-归档-2026-08-02.md) · [01](自举进度-归档-2026-08-01.md) · …

## §6 变更记录（2026-09-07 全 21 行 · 倒序）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-09-07 | **feat(selfhost)** **9.5.5 seed read 路径 — 有界读 Cap 闭环（`47b3fd349`）＝9.5.4 亲邻收口** | **代码＋测试** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。**建图**＝9.5.4 收掉 write 后，seed read 残两处：`runtime_process_argv` Linux /proc/self/cmdline（fopen/fread/realloc 堆增长手搓，1MiB 超界放弃）＋ `build_runtime` 两 patch 函数（fopen/fseek/ftell/rewind/fread 读＋"wb"/fwrite 写回，≥512K 高声拒）；9.1.12 权威 `xlang_proc_read_file` 无截断信号，直换＝静默截断债。**根修（G.7）**＝权威补全 `xlang_proc_read_file_bounded`：分块无 seek；填满 cap-1 后**探针再读 1 字节**，多则 -2（恰 cap-1 经 EOF 探针仍成功）；两 TU 三函数改 Cap 面（读＝bounded／写＝`xlang_io_open_write`＋`xlang_io_write`＋`xlang_proc_close_fd`），原契约逐条保留。**验收双端**：权威 smoke 5/5（正常/空/恰 cap-1/截断-2/缺文件-1）；Ubuntu 金标 /proc argc 绑定 probe——ctor 优先级 65535 启动全链真跑新读面，real=4/bound=4、argv 逐项对、rc=42；双端 TU 重编 rc=0（macOS proc_argv 零警告；build_runtime 唯一 remove_len 未用警告＝既有）＋nm **stdio U=0**（proc_argv 消 fopen/fread/fclose/realloc；build_runtime 消 fopen/fread/fwrite/fclose/fseek/ftell/rewind）＋g05 OK＋双端 L2 **5/5**＠`47b3fd349`＋**12 门全 ok**。**残**：async FILE\* emit 冷体（.x 脸已 Cap）；link_abi popen nm（9.2 域）；C 后端 cf_lib（PC 域）。**9.2 初勘**：zlib 已纯 `.x` 零 libc（条目待勘）；libm 32 桥＝fdlibm 级；ed25519＝.inc 宏域；sqlite/arrow 独立大域。下一刀：**9.2–9.3** 逐条建图立波。 |
| 2026-09-07 | **feat(selfhost)** **9.5.4 write 路径残 — fd 格式化写组合面 Cap 权威闭环（`af4c479c1`）** | **代码＋测试** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。**建图**＝「vsnprintf＋write」组合面 seed 层两处手写、无单一权威：`bootstrap_vfprintf_fd_impl`（stack 512＋malloc 堆增长＋vsnprintf＋write＋free，nostdlib TU）＋ `pabi_trace`（手写 512B 有界体，mega TU）；产品 `.o` stdio 裸脸已随 9.7.1 归零，本波收源级重复。**根修（G.7）**＝新权威 `include/xlang_fdprint_cap.h`：`xlang_vfdprintf`／`xlang_fdprintf`＝Cap fmt（`xlang_vsnprintf`）＋`xlang_io_write` 单次写，有界 512B 截断契约（pabi_trace 9.7.1 先例；独立新头而非扩 fmt_cap——io_cap 平台头会放大 fmt_cap ~20 消费面）；两落点薄转发（stubs impl 体＋pabi_trace 体，同 commit 同语义）。**验收双端**：Ubuntu 金标＝stubs TU 冷重编 rc=0（仅既有 -Wnonnull-compare 警告）＋nm 提供面 **15/15 T** 符号不缩（write/fwrite/fputs/fputc/puts/printf/fprintf/vfprintf/fflush/fileno/fread/fgets/fopen/fclose/fdopen）＋pipeline_abi FORCE 重建 rc=0＋g05 OK；macOS＝stub 编译 7 错 A/B 与原文件全同（LINUX-only TU 本不在 mac 编）＋头级行为 smoke 截断返回 511；双端 L2 **5/5**＠`af4c479c1`＋**12 Cap/simd 门全 status=ok**。**残（read 路径／其它域，非 write 面）**：`runtime_process_argv` /proc/self/cmdline 读、`build_runtime` pipeline_gen 读、async FILE\* emit 冷体（.x 脸已 Cap）；link_abi popen nm 扫描（9.2 域）；C 后端 cf_lib（PC 域）。下一刀：**9.2–9.3（P2 三方库／宏）**；备选 read 路径残。 |
| 2026-09-07 | **feat(selfhost)** **9.4.3 `main` 的 `char**` argv 双端闭环（`7c70d81c3`）— run 漏斗转发用户位置参** | **代码＋测试** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。**建图**＝`function main(argc: i32, argv: **u8): i32` 的 **build 产物路径双端本已通**：可执行产物无 crt/runtime 胶水，macOS LC_MAIN entryoff 即用户 `_main`（dyld 以寄存器 argc/argv/envp/apple 调入口＝约定凑巧对上）；Ubuntu 产物链 glibc（crt1 交付 (argc,argv,envp) 寄存器）。探针实证：`main(argc, argv)` typeck／索引／解引用全通，`./p.bin hi` 双端 42。**唯一缺口＝run/bare 漏斗**：`main_cmd_run`→编译→`driver_exec_compiled`→`driver_exec_compiled_body`（thin orch）→`xlang_driver_exec_spawn_wait(exe)`（always-seed）曾以 `av={path,NULL}` execv——用户 argc 恒 0（`xlang run p.x hi` rc=7 假象）。**根修（单权威，三面＋header 四触点同 commit）**＝`xlang_driver_exec_spawn_wait` 签名扩 `(exe, argc, argv_opaque)`：子 argv＝`[exe]`＋run_argv 中 **.x 源路径之后**的用户位置参（首个 `.x` 后缀项定位源；`-o` 对连同值跳过＝注入临时对在 argv 尾部、显式 `-o` 产物路径同属 driver 工件；其余 `-` 开头 flag 不转发）；`argv[0]=exe` 按 C 约定；argc∉[1,512] 或 argv 空回退 `[exe]`。调用面同步：thin .x `driver_exec_compiled_body` orch＋其冷 C twin（`#ifndef XLANG_L2_RDABI_THIN_FROM_X`）＋`runtime_driver_abi.h` 声明（**教训**：seed C 面改签名先 grep `runtime_driver_abi.h` 头声明，否则 cc conflicting types 首编即红＝好事，fail-fast）。static `cav[520]` 对齐同文件 `adj[512]` 惯例（单线程驱动进程一次 exec）。**验收双端**：`tests/probes/wave943`（m1 argc 计数 7/42；m2 argv 内容 `"hi"` 字节校验＋边界 7/42/43；m3 argv[0] 非空 42）——macOS：build 直跑＋`xlang run`＋bare 三路全绿＋flag 先于位置参（`run a2.x -O1 hi`=42）；Ubuntu：六探针全绿＋负路径 7/43；双端 `product_l2_matrix` **5/5**＠`7c70d81c3`（Ubuntu 侧 g05 默认 sync 双新二进制 23:09）。**残（v1 规则立卡）**：源路径后取独立非 `-` 值的 driver flag 会被当用户参转发（位置参规则；python 式尾随语义，后续需要再收敛 flag 值表）。下一刀：**9.5.4 write 路径残**；备选 9.2–9.3。 |
| 2026-09-07 | **feat(selfhost)** **9.4.2 `void*(*)(void*)` C ABI 双端闭环（`fb4019d32`）＋邻域收口：fn-ptr 全局表／ADDR_OF 全局不 seed** | **代码＋测试** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。载体 p2（xlang fn 地址 → `thread_create_c`(pthread_create) → pthread 回调 → join → 栈上副作用）双端绿；本波根修＝邻域既有卡（fn-ptr 全局表／ADDR_OF 全局 CG002）。**根修四件（.x 权威＋冷学生 seed twin 同 commit 同语义，G.7）**：①`pipe_modlet_lea_fn_sym_to_rax`（#[no_mangle] 权威 fn 符号 LEA spell：Mach-O `'_'` 前缀／ELF 裸名，`backend_enc_lea_sym_to_reg_arch`；as-impl／var-fast fnptr 块由内联 spell 改委托，G.7 单权威）；②`pipe_modlet_lea_named_binding_addr_to_rax`（`pipeline_asm_modlet_find`→COMMON cell LEA；否则同模 fn `glue_module_func_index_by_name_c`≥0→①；ADDR_OF 与 lvalue VAR scoped-miss 统一落此全局回退＝`&g`／非局部 VAR 读写在 rax/x0 双通，均非则高声 -1）；③prepare 门新谓词 `pipe_modlet_array_lit_has_ptr_addr_elem`（elem ty 9/18＋ek∈{3,51,54}，ek==46 嵌套行递归）接 use_data 门——含地址元素表留 COMMON（`.data` bake 无法表达重定位）；④entry seeder `pipe_modlet_seed_ptr_addr_elem_to_rbx`（flat 走查先试地址元素：ek54→as operand／ek51→unary operand is_addr_of／ek3→self；VAR 名→②或①；esz==8 高声校验；非地址元素仍走 fold）。**坑（根修迭代）**：首测门谓词传顶层 let 的**整数组 type_ref**（etk==10 分支只认 ARRAY_LIT 行）→ q2/q3 仍 bake→CG002；seeder 权威 caller 先 `et = pipeline_type_elem_ref_at(a, type_ref)` unwrap 后才传——门改传 unwrap 后 elem type（.x＋seed 镜像），q2/q3 即绿。**dev box 约束（立卡）**：fnptr-as thin 若改为引用新 helper，产品链接 UNDEF（mega `-E` macOS OOM ~22GB jetsam，纯 asm 又被禁）→ thin 保持 HEAD 历史内联 spell；macOS 产品面经 hybrid FORCE 重建后 `nm` 见 `.x` 权威符号（`pipe_modlet_lea_named_binding_addr_to_rax` 等 4 全 T、零 U）＝本波 `.x` 修复已入 macOS 产品 `.o`。**验收双端**：hybrid FORCE 重建＋g05 rc=0；`tests/probes/wave942`（本波入库）双端全绿——q2 裸 fn 全局表 `[inc,dec]`／q3 `as *u8` 表／q4 模块级 `&g`／q5 函数体 `&g` 翻绿（原 CG002）；q1 标量 fn 全局／q6 局部 fn 表间接调用／p2 C-ABI 线程环／base 回归保绿；Ubuntu `product_l2_matrix` 5/5＠`8dbf6b4b0`（**注意**：g05 `--no-sync` 后 `xlang_asm` 陈旧，矩阵默认取 `xlang_asm`——首跑 5/5 是旧二进制假绿；补默认 sync 后双新再验 5/5＋xlang_asm 探针抽检绿）；Ubuntu 侧 git pull 首跑 GnuTLS TLS handshake 瞬断，重试即过。**残（已立卡）**：`function(i32):i32` 全局赋值 typeck mismatch（p3 家族，前端卡）；seed 第二 WIN-leftover addr_of 变体无全局回退（Windows 高声拒）；字串 len>126 高声拒／RELA＋字串池＝真 `.data` 指针 init（既有）。**教训**：谓词/走查函数的 type 参语义必须对齐其镜像权威的 caller（unwrap 层级一致），否则门与 seed 判据分叉静默走旧路；dev box 上产品 `.o` 是否吃到 `.x` 修复要 `nm` 对符号而非只看构建 rc；多二进制产品（xlang／xlang_asm）跑矩阵前先核 mtime。下一刀：**9.4.3 `main` 的 `char**` argv**；备选 9.5.4 write 残／9.2–9.3。 |
| 2026-09-07 | **feat(selfhost)** **9.6.3 函数内 `static let`／`static const` ＋模块级重名静默覆盖根修（`afa7a7f7a`）＝9.6 子系列收官** | **代码＋测试** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。**语义**＝函数内 `static`（IDENT 非 keyword，token_start 6 字节 bounds 读）＋peek LET/CONST → 脱糖进模块级 let 表，唯一权威 `parse_one_top_level_let_into`（复用 9.6.0–9.6.2 modlet 机制；dup 守卫在其内）；init 时机＝程序启动、可见域扩全模块＝文档化 C 分歧；内层块遮蔽自然。**根修一（本波正主）**＝函数顶块由 `parse_one_function_impl` 自有语句循环解析（`parse_block_into_with_scratch` 只管嵌套块），旧 static 钩子只在后者＝顶块 `static` 落 XT001——**两条循环同挂钩**：parser.x＋pin `parser_gen.linux.x86_64.c`（语句循环＋buf 重试循环＋parse_into/parse_into_buf 模块指针窗口）＋C twin `parser_asm_one_function_buf_slice.inc`（buf 循环头，局部 extern＋bounds 字节读）＋`pthin_fn_block.from_x.c` 补 struct＋`parser_asm_seed_parse_into_buf_slice.inc`（主循环＋trait/impl 两点 set/clear 窗口），同 commit 同语义（G.7）。**根修二**＝模块级重名此前静默覆盖 → `parser_report_duplicate_name_p012` kind=2 高声拒（wave679 sticky 硬中止，rc=1）。**验收双端**：15 探针双端净（p1 persist b×3→1/2/3／p2 static_const／p3 shadow／p4–p6 dup 三连高声 rc=1／p7 nested／p8 ident-static 不触发／p9 模块回归＋v1–v6）；双端矩阵 5/5＠`1b996f3e4`；macOS 白名单 OK **129 scripts**（新旋钮 `XLANG_BSTRICT_SKIP_SUSPENDED_CHECK`＠`1b996f3e4` 跳 run-check/run-fmt-cmd＝check 闸门暂停＋fmt FMT001 既有债）；Ubuntu 全量逐脚本收集 **107/129 OK，红 22＝run-slice（既有卡）＋std/core 域 21（与立卡「std 红簇 21 脚本」数目吻合，名单首次枚举：boundary-encodings/string/builtin/http/json/net/process/set/queue/heap/fs/compress/debug/env/stdtest/channel/hash/math/unicode/runtime/std-net-context-gate）＝红集 ⊆ 立卡集无新红**。**std 簇取证增强（卡不动，根另刀）**：症状＝ld UNDEF（`std_io_backend_handle_from_fd`←`std_net_read_ctx`、`json_parse_null_c`←`std_json_json_libc_parse_null_c` 等）＋运行期错（string exit=3、builtin `core_builtin_clz_u32` 死循环 gdb SIGINT 栈实证）；**陈旧 `.o` 假设已排除**＝51 个 std/core `.o` 全旧于产品编译器，wipe 后 ensure 链重建仍 **22/22 红**。**教训**：fail-fast 白名单首红只证「到此为止绿」；全量红集须逐脚本收集（不重试、全跑完）再对照立卡域定罪；挂起型红（死循环）会卡收集循环，pkill 放行记 FAIL 即可；`.o` 全旧于编译器的树先做 wipe 对照再立「平台脸」结论。下一刀：**9.4.2 `void*(*)(void*)` C ABI**（邻域 fn-ptr 全局表／ADDR_OF 残卡）；备选 9.4.3 main argv／9.5.4 write 残。 |
| 2026-09-07 | **fix(selfhost)** **9.6.1 `u8[N]` 全局 BSS 正式收口＋数组字面量元素静默丢零根修（`c1c0171a1`）** | **代码** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。9.6.1 收口扫尾升级为根修波（扫尾建图暴露缺陷簇）：数组全局永不 hoist（Stage 12.0.5 先例），仅两路——prepare 非空 ARRAY_LIT（`ik2==46&&tk2==10&&ne2>0&&!has_string_elem`，`.data` 预算 `≤65536`）走 F7 bake 字节戳；否则 COMMON＋entry seed 走查。而 bake／seeder 只认 `ek==0` LIT，parser 复合重解析范形（`parse_body_let_bracket_compound_init_ref` 经 parse_expr）把 `-1` 落成 **EXPR_NEG(ek22)-over-LIT 而非负 LIT**——负元素**静默丢零**（s6 `[-1,2]`）；`ne>1024` 双处静默 `return 0`＝**整数组静默全零**（b_u8_1025 vs b_u8_1024 分界）；bake 有符号 `cur/256` peel 腐坏负数字节 1..3（s7 期候选根因）。根修三件套（.x `runtime_pipeline_abi.x`＋冷学生 `runtime_pipeline_abi.from_x.c` 同 commit 同语义，G.7 单权威）：① 新权威折算 `pipe_modlet_array_lit_elem_const_val`（ek0→int_val；ek22→取 unary operand 复验 ek0 后 `0-v`；其余＝非支持常量元素**高声拒** `return -1`→编译错 CG002，f1 `1+2` 复合元素证）；② seeder 走折算＋负数 `backend_enc_mov_imm64_to_rax_arch(ev, hi=-1)`（契约：hi/lo 为 imm64 无符号半，负数 hi 必须 -1，seed_link_compat:542 先例）＋`ne<=0` no-op／`ne>1024` 高声拒；③ bake 签名补 `span_bytes`（顶调传 csz2、递归传 row_sz＝容量守卫 `ne > span_bytes/esz` 高声拒）＋**u32 peel**（`ev as u32` 再除基，对齐 `pipe_store_i32_le` 5231 先例）＋ek59 防御高声拒。**不修 parser/typeck**＝折算放 modlet 边界避开 NEG 感知发射路径爆炸半径（Defect D 域另卡）。标量负 init 不受影响＝`init_kind∈{0,2}` 不含 NEG(22) 落 hoist 路本就正确（s8 探针假绿教训后 s8v2 相异码 rc=7 证真）。**验证双端**：hybrid FORCE 重建 prefer 行双端确认＋g05 rc=0；探针全绿双端一致——s6 负元素／s1v3 2000u8 bake／s9 COMMON 路负数（u8[60000]=[1] 填预算逼 i32[16000]=[-7,8,-9] 下 COMMON）／n1 嵌套负／c1 const 负／b_* 预算边界 u8 1024/1025/1200/1500/2000＋i32 2000／s7 低字节 168／s8v2 标量负 7／f1 高声 CG002；9.6.0/9.6.2 回归探针净（p3/p12/p13/p16/p1/p2/p8/p9/p10/p11=0、pa/pb/pc=2、pd=4、p14=6）；双端矩阵 5/5＠`c1c0171a1`；macOS 全量 bstrict 白名单 **127/128**（跳过暂停的 run-check.sh；唯一红 run-fmt-cmd＝**fmt FMT001 收集既有债**，`xlang fmt` 任意显式路径空收集，产品与本波前陈旧 xlang-c A/B 双红定罪非本波引入；探针侧 m1–m7/s1/t1 XT001 系 **Defect D** 既有卡非回归）。**立卡**：Defect D（typeck XT001 paren 复合左值 inside while 误报：`/`/`&`/`as`/`+` 全触发、bare `i==3` 绿、paren-RHS 绿、let 提升可绕 m8；同域 u8 vs i32 比较直拒 T001）＋fmt FMT001 收集债（源头 .x 逻辑，run-check/run-fmt-wrap/run-fmt-cmd 同域）。**教训**：静默 wrong-data 缺陷要靠**相异返回码**探针（s8 首版 `g&255` 对 0/-5 同码假绿）；预算分界探针成对放（1024/1025）；折算权威放消费边界而非产生点时必须配高声拒，否则新范形（NEG 之外的下一种）重演静默零。下一刀：9.6.3 static 共享状态（前置＝函数内 `static` 语法 XT001 语言债）；备选 9.4.2／9.4.3／9.5.4。 |
| 2026-09-07 | **fix(selfhost)** **9.6.2 巨型字串表／全局 `*u8[N]` lit init 静默 NULL 根修（`2bf4891aa`）** | **代码** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。真身＝modlet prepare 把非空 ARRAY_LIT（fits ≤65536）路由 F7 `.data` bake（pad→shndx_override(4)→add_label/add_sym→zeros→bake），而 bake（.x `pipe_modlet_bake_array_lit_elems_to_data`／冷学生 `_cold`）只认 `ek==0` LIT 元素——STRING_LIT（ek==59）元素**静默丢**＝全局指针表读 NULL（p3 rc=10；局部版 p10 绿＝帧发射走 `glue_asm_emit_string_lit_ptr_rax_elf_c` 正确路）。根修三件套（.x＋冷学生同 commit 同语义，G.7）：① 新权威递归谓词 `pipe_modlet_array_lit_has_string_elem`（STRING_LIT 即真；ek==46 递归＝`[K][N]*u8` 行覆盖）接 prepare use_data 门——含字串元素数组留 COMMON；② seeder `pipe_modlet_seed_array_lit_elems_to_rbx` 补 ek==59 分支＝string-lit lea `.text` 内联字节→`backend_enc_store_rax_to_rbx_offset_arch` 存 esz（`*T`/FN=8）；③ 冷学生 `pipeline_asm_modlet_seed_nonzero_inits_elf_c` 从标量 only 补全整段数组走查（emit ctx arena/module→`find_cold`→array&&非 data→`lea_rbx_arch_cold`→`seeder_cold`）＝闭合同步文档自认的 .x/C 分歧。全 LIT `.data` bake 不动（p2 绿）、空 `[]` COMMON 零（p9 绿）。**坑**：`glue_asm_emit_string_lit_ptr_rax_elf_c` extern 声明从 50377 挪文件头——C 翻译单元 decl-before-use，声明靠后时 hybrid 隐式声明失败**静默回退全量 seed**（构建 rc=0 但 .x 体全弱化，探针假象风险）。**验证双端**：hybrid FORCE 重建＋g05 rc=0 双端；macOS p3=0（原 10）＋15 探针回归净＋矩阵 5/5＠`2bf4891aa`＋全量 bstrict exit 0（仅暂停 check 闸门 fmt-wrap 既有红）；Ubuntu 探针 10/10（p3/p10/p2/p9/p1/p14＋9.6.0 四钉 p12/p13/pa/pd）＋矩阵 5/5。残卡：fn-ptr 全局表／ADDR_OF 元素不 seed（9.4.2 邻域）、字串 len>126 高声拒（既有）、RELA＋字串池＝真 `.data` 指针 init。**教训**：.x 新增跨文件 extern 调用，声明必须落文件头区块（与 558/806 enc extern 同区）；hybrid 回退是静默的，FORCE 重建后必须核对 `prefer thin+rest` 成功行而非只看 rc。下一刀：9.6.1 `u8[N]` BSS 扫尾。 |
| 2026-09-07 | **fix(selfhost)** **9.6.0 全局标量双家园误编译根修＋pass1-defer 非局部读次序（`423a342a9`）＝9.6 子系列开刀** | **代码** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。真身两面：**① 双家园**＝`pipeline_module_hoist_top_level_lets_into_main`（`runtime_pipeline_abi.x`）把模块级可变标量（LIT/BOOL init）追加进 main 块（帧槽＋局部表项），而 modlet prepare（wave139）已注册同名 `Lxml_*` SHN_COMMON 8B 家园＝一物二宅——slot-first 消费者（比较快路 31290／lit-left 孪生 31381／INDEX base／call args）读幻影槽（永 0）、RMW/store 走 modlet-first 读真家园（p12：`g=g+1`×2 后 `g==2` 比帧槽假）。根修 A＝Stage 12.0.5 数组先例扩标量：hoist 非 ARRAY 分支补跳 `init_kind∈{0,2}&&init_ref 有效&&非 const`（与 prepare 注册门逐条对齐防无家可归；`ik==0` 需复验 init_ref 因 LIT/无 init 二义；无 init let 两边同跳＝仍 hoist）；COMMON 成唯一家园，全消费者落通用 modlet-first VAR 发射（p16 证非 hoist 函数既有正确路径）。**② pass1-defer 次序（pd 探针证非 main 函数预存同病）**＝块发射两趟（pass0 let init／pass1 语句按 stmt_order），`glue_block_compute_pass1_deferred_lets` 的 use-collector 只见块局部名——`let c = 全局` init 读非局部名判零不 defer，pass0 抢跑读语句前旧值（非 main 函数一直坏；hoist 目标此前靠全局变块局部碰巧 defer；Fix A 使全局不再是局部＝main 也暴露）。根修 B＝新增权威 walker `glue_expr_tree_has_nonlocal_var_c`（ko==3 经 wave188 `glue_asm_local_var_stack_off_scoped<0` 判非局部；binop/unary/AS/field base/ARRAY_LIT/STRUCT_LIT 递归）接线 compute_pass1 首循环＋`use_defer==0` 内联支——读全局的 let 一律 defer 到 pass1 源序（过 defer 无害、欠 defer 即误编译）。冷学生 `runtime_pipeline_abi.from_x.c`（hoist 门＋walker＋接线）同 commit 同语义（G.7 禁双权威；hoist 在 prepare 填表前跑＝分类声明式不查表）。**行为探针双端全绿**：16 探针建图（p3 全局 `*u8[N]` lit init 静默 NULL＝9.6.2 正主卡）；修复五连 p12=0／p13=0／pa=2／pb=2／pd=4（修复前 1/1/0/0/2）＋回归 p14=6／p16=0／p4=2／pc=2／p11/p1/p2/p9/p10=0。**验证双端**：hybrid `runtime_pipeline_abi.o` FORCE 重建（try-pipeline-abi-prefer）；g05 rc=0；双端矩阵 5/5＠`423a342a9`；Ubuntu 探针 4/4＋邻域 gate 5/5；macOS 全量 bstrict exit 0（仅暂停 check 闸门既有红）；Ubuntu bstrict 两处红均 **A/B@55a961685 同红定罪既有债非本波**（⑥ experimental promote 链 `-E` 段错误＝wpo gate 自愈把 Ubuntu `-E` 既有坏的 experimental 体 cp 过产品 `xlang_asm` 再被 bstrict `cp`→`xlang`；run-slice length rc=1＝slice/Option 域平台差，macOS 同 commit 绿）——产品恢复后 Ubuntu 矩阵＋探针＋`-E` 全绿，slice 后余下脚本逐一取证见 C迁移追踪 9.6.0。**教训**：hoist 跳过会牵动 pass1-defer 的 use-collector 判据（只见局部名，全局必须同步）；探针连锁红先差分三路（直接读／let 拷读／非 main）再动刀；`ssh` 后台任务查 rc 必须落变量（`| tail` 吞 rc＝假绿）。下一刀：9.6.2 巨型字串表／全局指针数组 init（p3）。 |
| 2026-09-07 | **fix(selfhost)** **9.7.7 large-stack pthread 冷体 `_impl` 中转坍缩（`a7749a54d`）——9.7 子系列全闭环** | **代码** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。真身＝`seeds/runtime_driver_abi.from_x.c` 冷体残留两层 `_impl` 中转（`driver_run_thread_on_large_stack`→`_impl`→编排体；别名 `driver_run_on_large_stack_pthread`→`_impl`→`_impl`），thin .x 权威（`runtime_driver_abi_thin.x` wave12）公开函数直接持编排体＝冷公开面与 thin 形不同构＋多余两跳（G-02f-414「公开薄壳＋冷 `_impl`」时代形）。修复：编排体并回公开冷孪生（null 守卫→`is_large_stack_thread`→`call_fn_void_arg`／else `bump_stack_limit`→`nostdlib_pthread_is_stub`→`current_large_stack`／else `no_large_stack_env`→同／else `run_thread_on_large_stack_pthread`，与 thin wave12 同构）＋别名 null 守卫＋直呼＋删两个 `_impl`＋NL-07 注释转英文（G.9）。行为探针（single-TU real-body：真冷配 .o＋`nm U` auto-stub 家族减 libc/pthread 排除＋TU 内 T 符号不重定义）：四路早退＋真 256MiB pthread（证人 `pthread_get_stacksize_np`/`getattr_np` ≥200MiB）＋`mark(1)` 已大栈路径＋null 守卫＋别名——macOS PASS（96 桩）；Ubuntu 初红＝**探针毒桩假象非产品**：桩家族误含 7 个 `pthread_*` 桩（`pthread_create` 返 0 假成功**不建线程**→start routine 永不执行＝症状逐项吻合），删桩即 PASS（macOS 排除清单排了 `pthread_`、Ubuntu 漏）。**验证双端**：双配冷编译净；`runtime_driver_no_c.o` FORCE 重建；g05 rc=0；4 产品符号面齐（thin 两 T＋seed pthread 残差 T）、9.7.7 `_impl` 零残留；双端 L2 矩阵 5/5＠`a7749a54d`；11 Cap-raw＋fmt＋simd 13 gate 双端 OK（va-cap 既有债）。**教训**：auto-stub 家族自 `nm U` 生成时排除清单必须含 `pthread_`/`posix_memalign`（种子真用 pthread 的 TU），否则 `pthread_create` 假成功桩＝线程静默不跑、探针全链假死——探针红先查桩家族再疑产品。下一刀：阶段 9 P1 残余（9.6 建图／9.4.2–3／9.5.4 残）。 |
| 2026-09-07 | **fix(selfhost)** **9.7.6 stack_esc_gate 冷体归一：rt_stack 改经 wave37 fn-ptr 编排＋thread_fn null 守卫（`b2870a5e7`）** | **代码** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。真身两面（`seeds/rt_stack.from_x.c` 冷体落后 .x 权威 `runtime/rt_stack.x`）：**① 旁路 wave37 编排**＝冷体 `driver_stack_esc_gate_large_stack` 直接传 `thread_fn` 函数指针给 wave12 `driver_run_thread_on_large_stack`，绕过 thin wave37 `driver_run_stack_esc_gate_on_large_stack`（守卫→fn-ptr 残差→G.7 wave12）＝线程绑定权冷链两路（seed 头注释自认）；**② thread_fn 缺 null 守卫**＝.x/surface null arg 返 null，冷体裸解引用。修复：冷体 `large_stack` 改经编排（三方同构；wave12 是 driver_abi 同 TU 定义，链接依赖零新增）＋thread_fn 补守卫＋删死 extern。行为探针（真冷体 .o＋编排仿真 stub＋typeck stub）：编排收 `&args`＋线程内 typeck 一次返 7、null 守卫不触 typeck、-99（线程未跑）主线程回退返 7；nm 冷体 UNDEF 唯编排＋typeck＝直传 fn 消失。**验证双端**：双配冷编译净；`runtime_driver_no_c.o` FORCE 重建；g05 rc=0；4 产品 esc_gate 符号 4/4；双端 L2 矩阵 5/5＠`b2870a5e7`；11 Cap-raw＋fmt＋simd 双端 OK（va-cap 既有债）。**立卡**：退役 mega `src/runtime.x` stale esc_gate 委托块（extern `_impl` 全库无定义＋无构建引用＝retired-mega 卫生域）。下一刀：9.7.7 driver_run_thread_on_large_stack_pthread（9.7 收尾刀）。 |
| 2026-09-07 | **fix(selfhost)** **9.7.5 usage_write＋compiled_body：exec 冷体薄委托＋spawn 单权威＋9.7.1 遗留 doc 脸收敛（`d9ec90593`）** | **代码** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。真身两面：**① OS spawn 双权威**＝`seeds/rt_run_exec.from_x.c` 冷体 `driver_exec_compiled` 仍持 G-02f-299 时代内联 fork/execv/_spawnvp 单体，而 .x 权威早已拆薄（守卫→wave42 `driver_exec_compiled_body`：守卫＋G.7 `driver_exec_path_is_non_exe`＋残差）→ `xlang_driver_exec_spawn_wait` 唯一 OS 权威——fork 体冷链两份；**② doc 脸漂移**＝9.7.1（`75e074199`）只转主体未转注释，`xlang_driver_usage_write_stdout`（wave44 巨型 usage 表）与 `xlang_driver_fwrite_stdout_n`（wave39 计数写）均早已 `xlang_io_write(1,…)`，但 thin.x／driver_abi seed／.h 九处仍写 "fwrite+fflush"。修复：冷体改薄委托（与 .x 同构）＋删内联体＋死 `xlang_waitpid_retry` extern（`runtime_diag_errno_path` 留 test 路径；`driver_exec_scan_out_path`/`path_is_non_exe` 权威在本 TU 不动）；九处 doc 改真脸（符号名不动）。行为探针（真 `rt_run_exec.from_x.o`＋stub `_body`/usage/diag 家族）：委托透传 argc/argv、null/argc 守卫返 1 不触体、usage 委托；seed `fork()` 零出现＋.o 无 fork 族 UNDEF＝spawn 单权威。**验证双端**：双 seed 冷编译净；`runtime_driver_no_c.o` FORCE 重建；g05 rc=0；4 产品符号面齐；双端 L2 矩阵 5/5＠`d9ec90593`；11 Cap-raw＋fmt＋simd 双端 OK（va-cap 既有债）。**教训**：gate 脚本名手滑（`run-X-cap-gate.sh`≠实际 `run-X-cap-raw-gate.sh`）＝11 连红假红——全红先对名/对 cwd 再疑回归。下一刀：9.7.6 driver_run_stack_esc_gate（pthread）。 |
| 2026-09-07 | **feat(cap)** **9.7.4 driver_stdio_*＋driver_entry_*_slot：rt_entry 冷体 fd 句柄脸＋wave21 槽归一（`37f402db7`）** | **代码** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。真身＝`seeds/rt_entry.from_x.c` 冷体 **stale FILE* 脸**：`diag_print_code_table/explain/known_codes` extern 仍 `FILE*`（9.7.1 已换 `uint8_t*`，`include/diag.h` 为准）——LP64 指针兼容掩盖下**冷启动二进制把 libc `stdout`/`stderr` FILE* 值当 fd 句柄解码＝静默语义漂移**（产品 FROM_X 走 .x 权威无恙，冷路径已坏）；另 `driver_fmt_report_no_files` 本地 argv 数组旁路 wave21 槽。修复：掉 stdio.h；extern 换 `uint8_t*` 脸；explain list/known→`driver_stdio_stdout()`、unknown→`driver_stdio_stderr()`；`fputs("note: ",stderr)`→`xlang_io_write` raw fd 2（同 .x）；smoke `printf`×4→`xlang_snprintf`＋Cap fd-1 write-all；fmt_no_files→wave21 `driver_entry_fmt_argv_slot`（G.7 单权威）。行为探针（逐字 wave21/39/40 孪生＋stub diag）：smoke 字节全等、list/unknown 收到正确编码句柄（2/3）、fmt 收到槽 argv。**验证双端**：种子双配编译 OK＋nm stdio 脸=0；`runtime_driver_no_c.o` FORCE 重建；g05 rc=0；4 产品 stdio-face UNDEF=0；`--explain list/ZZZ`＋`--print-target-cpu` 双端功能正常；双端 L2 矩阵 5/5＠`37f402db7`；simd-s1＋11 Cap-raw＋fmt 13 gate 双端 OK（va-cap 既有债）。**教训**：`grep -c` 零匹配退出码 1 短路 `&&` 链＝下游步骤静默未跑＋rc=1 误报（Ubuntu 验证链踩中，g05 假失败），验证链 `grep -c` 后用 `;` 或落变量；macOS push 本波遇 LibreSSL SSL_ERROR_SYSCALL，重试无效即走 SSH-443 备用通道。下一刀：9.7.5 usage_write + compiled_body。 |
| 2026-09-07 | **feat(cap)** **9.7.3 GAS 行表＋OutBuf append 双权威收敛（`b38a3beac`）** | **代码** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。真身＝`seeds/rt_asm_stub.from_x.c` 冷体 `asm_codegen_ast` 自持**第三份 GAS 桩表副本＋内联 memcpy append**（旁路 wave14 访问器；另两份＝.x thin if-chain＋`runtime_driver_abi` 种子冷孪生）。修复＝冷体与 .x 权威（`rt_asm_stub.x`）／surface 三方同构：循环 `driver_asm_stub_gas_line_count()` 次 `gas_line_at(i)`＋`out_append_cstr(out, line)`；删本地 10 行静态表／内联 append／`struct codegen_CodegenOutBuf`／`X_CODEGEN_OUTBUF_CAP`（表与 append 唯一权威＝wave14 访问器，decl 在 `runtime_driver_abi.h`）；掉 string.h；`XLANG_WEAK` 保留。行为探针：冷体＋逐字拷贝访问器链接＝rc=0／110 字节与旧内联体逐字节一致。**验证双端**：种子冷/FROM_X 双配编译 OK；`runtime_driver_no_c.o` FORCE 重建（try-rt-prefer）；g05 rc=0；4 产品 stdio-face UNDEF=0；双端 L2 矩阵 5/5＠`b38a3beac`；simd-s1＋11 Cap-raw＋fmt 13 gate 双端 OK（va-cap 既有债＝10.7.1 域）。**立卡**：bridge 4 参 weak `asm_codegen_ast` 返回 -1 与真 3 参 stub 同名 weak、ELF/MachO 先到先得（同文件 typeck weak 桩已因同类先序删除；asm 版留作 asm-only 链 fallback，symbol-bridge 域另开刀）。**教训**：gate 全红别慌——先查 cwd（`tests/` 在仓根，从 `compiler/` 调用＝No such file 假红）；Ubuntu git GnuTLS 握手 flake 时 curl（OpenSSL）可通＝HTTP2 问题，`git -c http.version=HTTP/1.1 pull` 即绕过。下一刀：9.7.4 driver_stdio_* + driver_entry_*_slot。 |
| 2026-09-07 | **feat(cap)** **9.7.2 lib_roots 槽＋Parsed 填表：target_cpu_print 脸统一＋dispatch 种子冷体残面收口（`b93d1a9e4`）** | **代码** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。9.7.1 后上帝视角残余地图：`xlang_target_cpu_print` 脸分裂（头/种子冷体 `FILE*` vs .x 权威 `out: *u8`）· seed 死 `#else` fprintf×3 分支（`HAVE_XLANG_IO_PRINT_CAP=1` 无条件）· `rt_dispatch_impl`／`rt_run_exec` 种子冷体直呼 libc `stdout` · opt 默认字面 `"2"` 与 wave35 BSS lit 双权威。修复：`include/target_cpu.h` 脸→`uint8_t *`（stdio.h→stddef.h）；`target_cpu_pure.from_x.c` def 换脸＋删死分支＋掉 stdio.h；`rt_dispatch_impl.from_x.c` 冷体 `stdout`→`driver_stdio_stdout()`（9.7.1 fd 句柄）＋opt→`driver_dispatch_opt_default()`（G.7 单权威）＋掉 stdio.h；`rt_run_exec.from_x.c` 冷体同换＋掉 stdio.h（snprintf 已映射 `xlang_snprintf`）；`rt_dispatch_impl_surface`／`rt_run_exec_surface`（-E 同构）脸本已 `uint8_t*`，仅刷 `target_cpu_pure_surface` 注释。**验证双端**：3 种子冷编 OK＋nm stdio 裸脸=0；`target_cpu.o`（try-target-cpu-prefer）＋`runtime_driver_no_c.o`（try-rt-prefer）FORCE 重建；g05 rc=0；4 产品 stdio-face UNDEF=0；`--print-target-cpu` 输出不变（Darwin 0x100 neon／Ubuntu 0xef sse2…bmi2）；双端 L2 矩阵 5/5＠`b93d1a9e4`；simd-s1＋11 Cap-raw＋fmt gate 双端 13/13 OK（va-cap 双端红＝10.7.1 既有债，本波 diff 0 va 行）。**教训**：C 注释里写 `FILE*/stdout` 会因 `*/` 提前闭合块注释致编译错，须写 `FILE star / stdout`；Ubuntu 全量 107 .o 复扫现 7 残留（crt0/l5 partial、lsp gen、asm async seed）＝未圈域既有构建，mtime 早于本波 pull 非本波引入。下一刀：9.7.3 GAS 行表 + OutBuf append。 |
| 2026-09-07 | **feat(selfhost)** **9.7.1 opaque FILE\* 脸→fd 句柄面统一收口（`75e074199`）** | **代码** · **不升钉** · 新增权威 `include/xlang_driver_stream_cap.h`：handle＝`(u8*)(intptr_t)(fd+1)` 单射编码（NULL 非法、保指针同一性），`from_fd`／`to_fd`／`close`（fd≤2 不关保 std 流）。落点：diag 微 ABI（`diag_stderr`／`diag_io_*`，~60 print 点→`xlang_io_write`）· rt_preamble 2 个 inline 写入器 · driver_abi SIDECAR debug（fprintf→`xlang_snprintf`＋fd 2）· rt_run_compiler_parsed／rt_run_x_emit／rt_run_asm_backend（cf／asm_out／metric .o 全链 fopen/fdopen/fwrite/fclose→open_write＋handle＋`xlang_io_write`＋handle_close）· **runtime_pipeline_abi.from_x.c 首次收编（65790 行第四张种子图）**：fputs_stdout／fp_is_stdout／fclose_\* 转 fd 面，47 处 fprintf(stderr)→静态 `pabi_trace`（Cap vsnprintf 512B 栈缓冲＋单次 fd-2 写），fputc 循环→有界 `%.*s` · lsp_glue／strict_glue_stubs。头面同步 `runtime_pipeline_abi.h` `driver_asm_fclose_asm_out(FILE*)→(*u8)`（与 .x 权威一致）＋codegen.h／lsp_codegen_extern.h／diag.h；fmt 权威补 `%-`／`%s` 宽度／`%x` 零填充（宿主单测 ALL PASS）。**验证双端**：src/＋build_asm/ 全量 .o nm 扫描 stdio 裸脸=0（Ubuntu TOTAL=107）；4 产品 stdio-face UNDEF=0；L2 矩阵 5/5＠`75e074199`；Ubuntu 12 Cap＋fmt＋va-cap 13/13 OK，macOS 11/12 darwin gates。**立卡既有债**：`run-lang-va-cap-builtins-gate.sh` 双端红＝10.7.1 域（macOS clang 拒重声明内建；Ubuntu asm f32 槽 rc=4；父提交同红、本波 diff 0 va 行）。**教训**：g05 的 stale 循环跳过 `build_asm/*` 且查不到 seed 源（`src/runtime/rt_preamble.o` 源在 seeds/），rm 后须按 slice 菜谱手编再跑 g05；`ensure try-pipeline-abi-prefer` 的「inject-only thins」路径会保留 stale base（本波双端各踩一次），改 seed 后须 `XLANG_HOST_CC_SEED_FORCE=1`；git checkout 旧提交不会回滚 gitignored build 产物，父态 bisect 前先 nm 验产品含新符号（pabi_trace）判污染。下一刀：9.7.2 lib_roots 槽＋Parsed 填表。 |
| 2026-09-07 | **feat(cap)** **9.5.3 slice3b link_abi FILE/打印簇＋parse_expr_link 诊断脱离 libc stdio（`0a650a095`）** | **代码** · **不升钉** · **fmt 权威真缺口修复**：10.7.2 `xlang_fmt_cap.h` 不支持 `%z` 修饰符——落 default 输出字面 z 且**不弹变参**致后续参数错位；补 sizeflag 取 `size_t`（LP64 unix 与 Win x64/arm64 LLP64 全对），宿主单测验多参数。runtime_link_abi：.inc wrap 写手 fopen/fprintf/fclose → `xlang_io_open_write`＋3 段精确 `xlang_io_write`＋`xlang_proc_close_fd`（发射字节全等）；debug-server env 读手 fgets 循环 → `xlang_proc_read_file`＋`xlang_proc_next_line` 原地切行；两条 XLANG_DEBUG_PIPE trace → `xlang_snprintf`＋raw fd 2；stdio.h 仅保留为 popen/pclose/fgets nm 扫描器声明面（9.2 进程管道域另开刀）。parser_asm_parse_expr_link：12 处诊断（snippet 转义器＋enter/leave trace，fflush 删——raw fd 无缓冲）→ 本 TU `cap_err_puts/putc`＋`xlang_snprintf`；stdio.h 掉。两对象双端 nm 验 stdio 族=0；双端 L2 5/5。教训：`cc ... | head` 会 SIGPIPE 杀编译器致 .o 未更新的假象，编译验证勿截断输出。下一刀：slice3c driver_abi 跨 TU FILE\* 大簇（strict_glue_stubs／rt_preamble／runtime_driver_no_c 同流）。 |
| 2026-09-07 | **feat(cap)** **9.5.3 slice3a runtime_log_os smoke 读回脱离 libc FILE（`a5ba1d2f3`）** | **代码** · **不升钉** · 生产 log sink 本已 fd Cap（9.1.8）；残余 fopen/fread/fclose 在两个 C smoke helper（STD-053/STD-106 读回校验）。读回转 9.1.12 权威 `xlang_proc_read_file`（整文件读＋NUL＋close 一次调用），返回码与 strstr 金标不变；掉 `<stdio.h>`，`rename`（nostdlib stub 提供者，9.1.x 面）补 extern 声明。`runtime_log_os.o` nm 验 stdio=0（原 _fclose/_fopen）；std-log-rotate-async＋f-log-v1 门禁双端 OK；双端 L2 5/5。slice3 残余清单与跨 TU FILE\* 簇标注见 C迁移追踪 9.5.3。下一刀：slice3b parser_asm_parse_expr_link＋runtime_link_abi（自洽小 TU），再 driver_abi FILE 槽域大簇。 |
| 2026-09-07 | **fix(macos)** **host-cc 附属对象 minos 11.0 对齐 macho.x 链接目标（`014c72fb2`）** | **代码** · **不升钉** · 用户报 `./compiler/xlang examples/hello.x` 每次链接告警 `object file ... built for newer macOS (26.0) than being linked (11.0)`。五问：产生＝`runtime_panic.o`／`runtime_asm_io_stubs.o` 经 `build_xlang_asm.sh`（ensure_runtime_user_link_objs）与 g05 prereq 种子刷新规则用裸 host cc 编译，minos 被钉 host SDK 26.0；权威＝`macho.x` LC_BUILD_VERSION minos 11.0.0 是产品链接地板，`ensure_host_cc_seed_o.sh`／`cc_inc_tu.sh` 已有 guard（注释点名 stubs/panic companions），两条 `$CFLAGS`/`$BASE_CFLAGS` 源头漏掉；修复＝同款 Darwin guard 补进 `build_xlang_asm.sh` 129 行 CFLAGS 源头与 `g05_ensure_relink_prereqs.sh` 56 行 BASE_CFLAGS 源头（G.7 同一约定非新权威；Linux 不受影响）；双对象 otool 验 minos 11.0；hello.x 链接零告警；双端 L2 5/5。教训：本波手动 `cc` 重建对象绕过了带 guard 的标准路径，是告警引入点——重建对象须走标准 ensure 路径或带同款 flags。 |
| 2026-09-07 | **feat(cap)** **9.5.3 slice2 崩证／panic／JSON 调试打印脱离 libc stdio（`b0d1c8068`＋sync `20bad4a24`）** | **代码** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。五问：产生＝13 文件残余地图（崩溃证据 note/bundle、panic 消息、JSON debug 打印簇、parser_asm_thin 调试 trace）直呼 libc printf／fprintf／fputs／fputc／putchar／fopen／fclose／snprintf；存储＝新增 `xlang_io_open_write` 权威（`xlang_io_cap.h` 四平台段：LINUX raw open 0x241／DARWIN raw open 0x601／WINDOWS CRT `_open`／POSIX libc fallback；close 沿用 9.1.12 `xlang_proc_close_fd`）；`runtime_panic(.+_arm64).from_x.c`＋`runtime_backtrace_platform.from_x.c` 崩证落盘与 panic 消息全走 `xlang_io_open_write`＋`xlang_io_write`＋`xlang_snprintf`；`runtime_asm_io_stubs.from_x.c` 40 处调试打印转 `io_cap_println_*` helper 簇；`parser_asm_thin_c.from_x.c`＋5 个 .inc 6 处 fprintf(stderr) 转 `xlang_snprintf`＋`xlang_io_write(2)`；9 个 gen seed pin（driver／lexer／lsp／lsp_diag／lsp_io／lsp_io_std_heap／pipeline／preprocess／ast_gen2 `.linux.x86_64.c`）内嵌崩证/panic 模板原位转换（两种前缀变体逐字节保留）；3. 消费＝g05 链接面全部对象消费 Cap 面；4. 爆炸半径＝模板发射链认识：gen .o 把崩证模板烧进编译器二进制，旧 .o 会经 Track L `-E` 再发射旧模板，收敛次序必须是「叶 .o 先冷种子重建 → 再 relink 二进制」；Ubuntu `lsp_gen.c` 等工作副本由 `./xbuild build` 期间旧二进制 `-E` 再生（gitignored 工件）须 `cp -f seeds/*_gen.linux.x86_64.c` 刷新；**假绿教训**：`./xbuild build`+L2 全绿但 9 个 gen 叶 .o 仍是旧构建（mtime 早于 pull，up-to-date 跳过），nm 验 13 对象才算数——二进制链得过靠 nostdlib stdio stub 提供者兜底，绿≠残余清零；Ubuntu 强制冷种子 runge 法＝临时移走 `xlang`/`xlang-c`/`bootstrap_xlangc` 三二进制（同 Windows leftover-PE 合法路径）再 `FORCE=1 driver_leaf_x_to_o.sh ensure`，双端 13/13 对象 nm 验 stdio=0；5. 回归集＝双端 L2 矩阵 5/5（mac `/tmp/xlang_l2_Darwin_20bad4a24.log` · Ubuntu `/tmp/xlang_l2_Linux_20bad4a24.log`）；Ubuntu 12 Cap 门禁（io/fmt/dir/env/net/proc/process/sync/path/random/time/thread raw）全 OK；双端 ast_gen2 补 `link_abi_getenv` extern 声明修 3 错。PLATFORM: SHARED Cap · LINUX x86_64 · MACOS arm64 · WINDOWS CRT。下一刀：9.5.3 slice3 圈外 TU（§0 清单 12 TU）。 |
| 2026-09-07 | **fix(g05)** **Darwin/Windows 产品 relink `_xlang_sys_*` 提供者＋weak io 孪生（`539c9360f`）** | **代码** · **不升钉** · 根治**预存在**断链（git stash 后 clean tree＠`461834bd1` 复现 → 非 9.5.3 引入）：9.1.8 让 SHARED `rt_entry.x` 引用 `xlang_sys_write`，但提供者 `freestanding_io_x86_64.o` 仅 Linux 有 → Darwin/Windows g05 纯 ld UNDEF。三层修复：`g05_relink_env.sh` 三平台 `_MAIN_LINK_O` 槽加 `runtime_asm_io_stubs.o`（Darwin arm64/x86_64、Windows 待 MSYS2 门禁确认；PE 侧 XLANG_WEAK 展开空＋`--allow-multiple-definition` 先者胜）；`runtime_asm_io_stubs.from_x.c` 10 个 io 簇 def 标 `XLANG_WEAK`（强孪生 `x_seed_bridge.o` 在链时必胜，MachO nm -m 显 weak external）；`g05_ensure_relink_prereqs.sh` 顶部补种子新鲜度规则（seed 新于 `runtime_asm_io_stubs.o` 则重编——g05 路径不走 build_xlang_asm.sh 的 newer 规则，兼保 9.5.3 模板新鲜）。修复后 macOS `./xbuild build`＋L2 5/5。教训：此前「Darwin L2 绿」是旧二进制未重链的假绿（mtime 早于源 commit 掩盖断链）。下一刀：9.5.3 slice3。 |
| 2026-09-07 | **docs(selfhost)** **Cap 闭环批量收编 commit `461834bd1`＋双端 L2** | **文档** · **不升钉** · 日常闸门＝SHARED 双端 L2 产品矩阵。9/6 §6 所记 9.1.1–9.1.8／9.1.10／9.1.12／9.4.4–9.4.6／9.5.2／10.5.2／10.6.2-win 波（149 文件，同文件多层交叠不可分波）收编单 commit `461834bd1` 推送 origin；Ubuntu 金标 **git pull（禁 scp）**：旧工作树散件（typeck.x／typeck_gen／arm64_enc.x／xlang_asm 等 61 untracked）备份 `/tmp/xlang_ubuntu_scratch_bak_20260907` 后清场拉取，`bootstrap-driver-seed` 重建 2m10s，L2 矩阵 5/5 `/tmp/xlang_l2_Linux_461834bd1.log`；mac L2 5/5；io/net/time/proc/process-cap-raw 门禁 Ubuntu 新构建全 OK。钉盘仍 `b5be5ed97`。下一刀：9.5.3 reportf／va_list 残余（其它 reportf 叶）。 |
