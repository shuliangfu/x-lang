# 自举进度归档 · 2026-08-05

> **归档范围**：8 月 5 日全日 §6 波次流水（L4 冷构建恢复 + 8.3.3 R2 field/soa + pure-asm match + B′ pure-owned leave 至 binop/vector_simd）。
> **当日主线**：① 修 4 层 L4 冷债 → Ubuntu 真冷构建恢复；② **暂停 check 闸门**（自举期 L2/L4 均不跑 `xlang check`）；③ 8.3.3 R2 field_access/soa 主体迁 typeck.x；④ pure-asm match field-bind/guard；⑤ B′ pure leave 大批量（typeck_slots／scratch／loop_glue／import／diag／sidecar／… → as/cmp/match/unary/… → index/return/block_inits/vector_* → **binop**）；inventory present **~113→63**。
> **当日钉盘**：仍 **`db809e00f`**（**不升钉**）。tip L4 安全网仍 **`ffc0b646f`**。日终 residual tip 约 **binop `0dc2a35e1`**（vector_simd `cec278525`）。
> **合并**：self-hosting→dev(`02d080583`)→main(`a22474203`)。
> **回到主文档**：[自举进度.md](自举进度.md)（6 日起表）。

---

## 8 月 5 日 §6（倒序）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-08-05 | **feat(bc)** pure-owned leave binop | **代码** · **不升钉** · **双端 L2** @ tip **0dc2a35e1**。B′ pure-complete `pipeline_asm_emit_binop`（add／sub／mul／div／mod／and／bitwise／shift + try／load／placement + scalar f32／f64／unsigned／64bit + may_clobber + mixed／ptr scale）；Cap residual spill cache accessors + f32 VAR load + field_access_fast + rax_frame_spill；seed cold twins；delete host leaf；inventory present **64→63**。下刀：spill／index_helpers／call_args／field_access／expr_rec。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** vector_simd pure-owned leave | **代码** · **不升钉** · **双端 L2** @ `cec278525`。B′ pure-complete SIMD lane／shuffle／select／fma／binop2＋CTFE fold helpers＋local_var_stack_off_scoped＋module_func_index_by_name；Cap residual enc＋simd_enc＋pool＋backend_try_inline is_vector_lane_scalar_binop_ko；seed cold twins；delete `pipeline_asm_emit_vector_simd.c`；bc residual present **64**（ROWS 118；present 65→64）。矩阵 rv42／hello。下刀：binop／spill／index_helpers／重叶；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** index_eff_addr pure-owned leave | **代码** · **不升钉** · **双端 L2** @ `c557bc179`。B′ pure-complete scaled＋bounds＋slice_length＋rvalue_once＋base/text twins＋public elf/text faces；Cap residual enc scale＋try_index forest static→extern＋emit_expr＋local_slot＋field deref＋index flag getters；seed cold twins；delete `pipeline_asm_emit_index_eff_addr.c`；bc residual present **65**（ROWS 118；present 66→65）。矩阵 rv42／hello／array。下刀：vector_simd／重叶；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** vector_let pure-owned leave | **代码** · **不升钉** · **双端 L2** @ `339de9a0a`。B′ pure-complete leaf_elem＋flat＋vector_let_init＋field_frame_mag＋store_fixed_array_field＋type_is_fixed_array＋fixed_array let helpers；Cap residual expr_rec＋enc＋may_clobber＋var/field offset＋bulk_mem；seed cold twins；delete `pipeline_asm_emit_vector_let.c`；bc residual present **66**（ROWS 118；present 67→66）。矩阵 rv42／hello／array。下刀：index_eff_addr／vector_simd／重叶；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** block_inits pure-owned leave | **代码** · **不升钉** · **双端 L2** @ `8904742b0`。B′ pure-complete block const/let inits + dual-gp + slice_from_array + skip-typeck fill cluster（complete empty pure stub）+ fill_block_locals_tree + let_init_stack_reserve；Cap residual vector/struct/fixed/expr_rec/enc；seed cold twins；delete `pipeline_asm_emit_block_inits.c`；bc residual present **67**（ROWS 118；present 68→67）。矩阵 rv42／hello。下刀：vector_let／index_eff_addr／重叶；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** return pure-owned leave | **代码** · **不升钉** · **双端 L2** @ `1cdcd1a58`。B′ pure-complete EXPR_RETURN faces（slice escape / return_elf_impl/_c / sret / float promote / return_expr·lit helpers）；Cap residual expr_rec+float_widen+enc slot+dual-gp+type_size+spills；seed cold twin；delete `pipeline_asm_emit_return.c`；bc residual present **68**（ROWS 118 in-place absent；present 69→68）。矩阵 rv42／hello。下刀：block_inits／vector_let／重叶；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** index pure-owned leave | **代码** · **不升钉** · **双端 L2**。B′ pure-complete esz_c／esz／emit_index／addr_of／deref；Cap residual field_type_ref／fixed_array_total_bytes／esz_from_type_ref／var_fallback／assign_addr_cache／eff_addr_scaled／lvalue_eff_addr／enc load-lea；seed cold twins；delete `pipeline_asm_emit_index.c`；bc residual **72→71**。矩阵 rv42／hello + index／deref／addr_of 探针 42。下刀：context（wave141）／重叶／pipeline_x mega；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** context pure-owned leave | **代码** · **不升钉** · **双端 L2**。B′ pure-complete set/get context＋param-ptr＋compute_frame＋fill_param＋param_home＋fill_local；Cap residual glue_statics storage accessors＋host_is_arm64＋param width/agg/return＋frame walk＋enc/sret/PGO；seed cold twins；delete `pipeline_asm_emit_context.c`；bc residual present **71**（ROWS 117→118 absent+1）。矩阵 rv42／hello。下刀：重叶／pipeline_x mega；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** array_lit pure-owned leave | **代码** · **不升钉** · **双端 L2**。B′ pure-complete elem_byte_sz＋empty＋emit/force_esz＋force_esz_from_elem＋durable＋fixed/array_temp_bytes＋elem_type_ref；Cap residual leaf/flat vector_let static→extern＋al_nc_seq_take＋emit_expr；seed cold twins；delete `pipeline_asm_emit_array_lit.c`；bc residual present **69**（ROWS 118 in-place absent；present 70→69）。矩阵 rv42／hello + array 探针。下刀：return／block_inits／vector_let／重叶；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** assign pure-owned leave | **代码** · **不升钉** · **双端 L2**。B′ pure-complete lhs_f32＋rhs＋rhs_to_rax＋assign_elf＋field_assign_pair＋body_expr_stmt_at；Cap residual binop/try_index/spill/slice dual-gp/float promote/fixed_array/field offset/enc；seed cold twins；delete `pipeline_asm_emit_assign.c`；bc residual present **70**（ROWS 118 in-place absent；present 71→70）。矩阵 rv42／hello + assign 探针。下刀：重叶／pipeline_x mega；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** modlet pure-owned leave | **代码** · **不升钉** · **双端 L2**。B′ pure-complete name_is_shared／load／store／prepare／seed／register／lit_inits + table BSS 10500；Cap residual top_level／common_sym／enc load-store-mov／hoist／let_init_reserve／asm_ctx local；seed cold twins；delete `pipeline_asm_emit_modlet.c`；inventory ROWS **116→117** present 仍 **72**（新列 absent）。矩阵 rv42／opt／si／hello + modlet 探针。下刀：context／index／重叶／pipeline_x mega；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** as pure-owned leave | **代码** · **不升钉** · **双端 L2**。B′ pure-complete is_await／is_x_as／await_sync／try_propagate／float_lit／array_scalar／as_elf_impl／as_elf_c；Cap residual float_bits＋IEEE pack＋cast enc＋scratch cleanup＋kind 宏 residual shell；seed cold twins；delete `pipeline_asm_emit_as.c`；fold 去 Cap residual is_x_as（防 mangle 双面）。bc residual **73→72**。矩阵 rv42／opt／si／hello + as f64/f32 探针 42。下刀：modlet／重叶／pipeline_x mega；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** cmp pure-owned leave | **代码** · **不升钉** · **双端 L2**。B′ pure-complete CMP ELF + cc map + arm64 cset + typekind tag；Cap residual try_binop_cmp／stack_off／slot invalidate + enc ucomisd/ucomiss/setcc + CTFE folded；seed cold twins；delete `pipeline_asm_emit_cmp.c`；field_access 经 extern 调 pure typekind。bc residual **74→73**。矩阵 rv42／opt／si／hello + cmp 探针。下刀：as／modlet／重叶／pipeline_x mega；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** fold_primitives pure-owned leave | **代码** · **不升钉** · **双端 L2**。B′ pure-complete 13 fold detectors；Cap residual pool + `glue_expr_is_func_param_at_c`／`glue_fold_func_return_operand_ref_c`／`glue_expr_is_x_as_cast_at_c`（static→extern）；seed cold twins；delete `pipeline_asm_emit_fold_primitives.c`；fold_count 经 glue extern 调 pure。inventory ROWS **115→116** present 仍 **74**（新列 absent）。矩阵 rv42／opt／si／hello + while fold 探针。下刀：cmp／as／modlet／重叶／pipeline_x mega；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** x86_enc_helpers pure-owned leave | **代码** · **不升钉** · **双端 L2**。B′ pure-complete 27 `glue_enc_x86_*` + `glue_emit_lcg_xor_body_x86_c`；Cap residual `pipeline_elf_ctx_append_bytes`；seed cold twins；delete `pipeline_asm_emit_x86_enc_helpers.c`；fold_count_up_while 经 glue extern 调 pure。bc residual **75→74**。矩阵 rv42／opt／si／hello。下刀：fold_primitives／cmp／as／modlet／重叶／pipeline_x mega；升钉前 tip L4+bstrict。PLATFORM: LINUX+MACOS x86_64 SysV. |
| 2026-08-05 | **feat(8.3.1)** match pure-owned leave | **代码** · **不升钉** · **双端 L2**。B′ pure-complete MATCH／EXPR_IF + subject BSS + name_is_subject_field；Cap residual match／if arm faces + if_arm + enc + layout；seed cold twins；delete `pipeline_asm_emit_match.c`；expr_rec 走 pure faces。bc residual **76→75**。矩阵 rv42／opt／si／hello + match／if 探针。下刀：中小 emit／重叶／pipeline_x mega；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** unary pure-owned leave | **代码** · **不升钉** · **双端 L2**。B′ pure-complete NEG／LOGNOT／BITNOT + sxt + jz_after_bool；Cap residual f32／f64 classifiers static→extern + enc／int64；seed cold twins；delete `pipeline_asm_emit_unary.c`；expr_rec ko 22／23／24 走 public faces。bc residual **77→76**。矩阵 rv42／opt／si／hello + unary 探针。下刀：match／重叶／pipeline_x mega；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(bc)** pure-owned leave struct_let | **代码** · **不升钉** · **双端 L2** @ tip。B′ pure-complete `pipeline_asm_emit_struct_let_init_elf_c`／`glue_emit_struct_type_let_init_elf_c`／`set_call_sret_reg_shift`／`call_sret_reg_shift` + pure BSS sret flag；Cap residual struct_lit_fields + try_inline + type_size + store_retval + module_from_ctx（static→extern）；seed cold twins under #ifndef FROM_X；delete host-cc leaf；inventory present **78→77**。PLATFORM: SHARED. |
| 2026-08-05 | **feat(bc)** pure-owned leave async_cps | **代码** · **不升钉** · **双端 L2** @ tip。B′ pure-complete `glue_async_cps_emit_after_await`／`phase_reset`／`entry_elf_c`／`end_func_elf_c` + bag BSS 9124；Cap residual `async_asm_pool_build_layout` + enc + jz + next_label + local_find；seed cold twins under #ifndef FROM_X；delete host-cc leaf；inventory present **79→78**。PLATFORM: SHARED. |
| 2026-08-05 | **feat(bc)** pure-owned leave wpo_mono | **代码** · **不升钉** · **双端 L2** @ tip。B′ pure-complete `glue_wpo_mono_reset_pending`／`register_thunk_n`／`register_thunk`／`pipeline_asm_emit_wpo_mono_thunks_elf_c` + bag BSS 8708；Cap residual `codegen_wpo_mono_sym_format` + enc label/prologue/mov_imm64/epilogue + `pipeline_dep_ctx_target_arch`；seed cold twins under #ifndef FROM_X；delete host-cc leaf；inventory ROWS **114→115** present 仍 **79**（新列 absent leave）。PLATFORM: SHARED. |
| 2026-08-05 | **feat(bc)** pure-owned leave block_if_stmt | **代码** · **不升钉** · **双端 L2** @ tip。B′ pure-complete `block_if_stmt_elf` + `if_then_block_body_elf_c` in runtime_pipeline_abi.x；Cap residual scope/jz/body_sync/live-end merge/fill locals；static→public residual faces + snap copy helper；seed cold twins；delete host-cc leaf；inventory present **80→79**。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** panic pure-owned leave | **代码** · **不升钉** · **双端 L2**。**方法**：B′ pure-complete 4 面（`pipeline_asm_emit_xlang_panic_call_elf_c` + `pipeline_asm_emit_panic_elf_c` + `pipeline_asm_emit_panic_int_div_zero_elf_c` + `pipeline_asm_emit_divisor_zero_check_rbx_elf_c`）后 **delete-only** 整叶；Cap residual emit_expr_elf_c + enc mov/arg/call/test/jne/label + next_label + unary/kind/type；static→extern（backend_fwd／glue／mid_fwd）；seed cold twin under #ifndef FROM_X；name_len=14 保 byte-identical。删 `pipeline_asm_emit_panic.c`；live＝runtime_pipeline_abi pure。bc residual **82→81**。矩阵 rv42／opt／si／hello。下刀：中小 emit residual／pipeline_x mega；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** next_offset pure-owned leave | **代码** · **不升钉** · **双端 L2**。**方法**：B′ pure-complete 3 面（`glue_align_next_offset` + `pipeline_asm_bump_next_offset_for_array_lit` + `pipeline_asm_bump_next_offset_after_let_init`）后 **delete-only** same-TU 叶；Cap residual `glue_array_temp_bytes_for_let_init`（array_lit static→extern）+ expr/block faces；seed cold twin under #ifndef FROM_X；LP64 next_offset@4 via pipe_load/store。删 `pipeline_asm_emit_next_offset.c`；live＝runtime_pipeline_abi pure。inventory present 仍 **82**（同 TU 域未单列）。矩阵 rv42／opt／si／hello。下刀：panic／中小 emit／pipeline_x mega；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** ctx_layout pure-owned leave | **代码** · **不升钉** · **双端 L2**。**方法**：B′ pure-complete 恒等 cast `pipeline_asm_ctx_layout` 后 **delete-only** 整叶；**typedef** `pipeline_glue_AsmFuncCtxLayout` **留 glue 壳**（residual C 需 ly->field 语法，pure 无法托管 C typedef）；seed cold twin under #ifndef FROM_X（void* identity）。删 `pipeline_asm_ctx_layout.c`；live＝runtime_pipeline_abi pure。bc residual **83→82**。矩阵 rv42／opt／si／hello。下刀：中小 emit residual／pipeline_x mega；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** var_decl pure-owned leave | **代码** · **不升钉** · **双端 L2**。**方法**：B′ pure-complete 2 面（`glue_var_decl_type_ref_elf_c` + `glue_lazy_append_block_let_local`）后 **delete-only** 整叶；Cap residual expr/block/module type + emit module/func_index getters + asm_ctx local/slot + `asm_local_slot_reg_offset`；return/assign/lea_fwd static→extern；seed cold twin under #ifndef FROM_X。删 `pipeline_asm_emit_var_decl.c`；live＝runtime_pipeline_abi pure。bc residual **84→83**。矩阵 rv42／opt／si／hello。下刀：ctx_layout／中小 emit；升钉前 tip L4+bstrict。PLATFORM: SHARED. 同步刷新 `自举时序.md`（钉盘/S5/主刀）。 |
| 2026-08-05 | **feat(8.3.2)** lea_common pure-owned leave | **代码** · **不升钉** · **双端 L2**。**方法**：B′ pure-complete 6 面（lea rax/rbx rip_x86 + adrp_arm64 + arm64 mov x0↔x8）后 **delete-only** 整叶；Cap residual elf_ctx append/reloc；array_lit static→extern；seed cold twin under #ifndef FROM_X。删 `pipeline_asm_emit_lea_common.c`；live＝runtime_pipeline_abi pure。bc residual **85→84**。矩阵 rv42／opt／si／hello。下刀：var_decl／ctx_layout；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** with_arena pure-owned leave | **代码** · **不升钉** · **双端 L2**。**方法**：B′ pure-complete WA scope BSS + active/top_off + begin_func/alloc/push/pop + init/deinit 后 **delete-only** 整叶；Cap residual array_temp + emit_expr_elf_c + enc_*；**根修** arm64 init 参数序（先 cap→arg1 再 lea→arg0，防 x0 被 cap 冲掉）；deinit 符号长 21；seed cold twin under #ifndef FROM_X。删 `pipeline_asm_emit_with_arena.c`；live＝runtime_pipeline_abi pure。bc residual **86→85**。矩阵 rv42／opt／si／hello。下刀：lea_common／var_decl／ctx_layout；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** lint_meta pure-owned leave | **代码** · **不升钉** · **双端 L2**。删 `pipeline_lint_meta.c`；live＝`runtime_pipeline_abi` pure（visibility + L7 unused-private + module_num_funcs/main/set/reset/strict_parse + lint_set_source_buf）；seed cold twin under #ifndef FROM_X；bc residual **87→86**。pin 不动。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** emit_heavy_env pure-owned leave | **代码** · **不升钉** · **双端 L2**。**方法**：B′ pure-complete env gates／abort_lo_hi／path helpers／whitelist／orchestration_extern_only／name_has_prefix／top_level_const_lit 后 **delete-only** 整叶；Cap residual top_level_let／expr／driver_get；seed cold twin under #ifndef FROM_X；typedef `asm_boot_parse_sym_t` 迁 parser_emit_heavy。删 `pipeline_asm_emit_heavy_env.c`；live＝runtime_pipeline_abi pure。bc residual **89→88**。矩阵 rv42／opt／si／hello。下刀：parser_emit_heavy／lint_meta／中小 emit；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** skip_dispatch pure-owned leave | **代码** · **不升钉** · **双端 L2**。**方法**：B′ pure-complete `asm_empty_text_stub_label`／`asm_skip_heavy_module_func_body` + 根修 pure `set_pipeline_ctx` BSS（原空壳双权威）与 pure `typeck_mega_entry` 误 early `return 1`；Cap residual env／parser_heavy／block_tree 去 static；seed cold twin under #ifndef FROM_X。删 `pipeline_asm_skip_dispatch.c`；live＝runtime_pipeline_abi pure。bc residual **90→89**。双端 L2 矩阵 rv42／opt／si／hello（0a）。下刀：lint_meta／parser_emit_heavy／emit_heavy_env；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** safe_helper pure-owned leave | **代码** · **不升钉** · **双端 L2**。**方法**：B′ pure-complete 9× EMIT_HEAVY 第二遍 per-module 名分类器（typeck／pipeline／driver_compile_safe_helper + skip_heavy_backend_m8_helper／helper／m8_tail_thin_keep + typeck_helper_keep + backend／typeck_mega_entry；0 字段访问，全名匹配）in `runtime_pipeline_abi.x`（generator 展开 3× C 宏 → inline `equal_at`／`has_prefix_at` 链）后 **delete-only** 整叶；`pipeline_module_func_name_has_prefix_at` 去 static 供 pure extern；seed cold twin under #ifndef FROM_X（9 fn verbatim C + 宏保留）。删 `pipeline_asm_emit_heavy_safe_helper.c`；live＝runtime_pipeline_abi pure；skip_dispatch 经 ast_pool extern 重连。bc residual **91→90**。双端 L2 矩阵 rv42／opt／si／hello（0a）。下刀：parser_emit_heavy／emit_heavy_env／skip_dispatch；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** thin_delegate pure-owned leave | **代码** · **不升钉** · **双端 L2**。**方法**：B′ pure-complete 4× m8-tail thin delegate C-name 访问器（backend 38 行／pipeline 3 行 inline-literal unroll + driver/typeck 空表 + typeck func-name fallback）+ 私有 `asm_thin_delegate_emit` 拷贝 helper in `runtime_pipeline_abi.x` 后 **delete-only** 整叶；`k_asm_parser_thin_delegate` 表＋其 2 consumer 整体迁 `pipeline_asm_parser_emit_heavy.c`（同 TU 自含，byte-identical）；`asm_env_entry_emit_heavy` 去 static 供 pure extern；copy64 名对齐 pure 无 asm_ 的 `pipeline_module_func_name_copy64`；seed cold twin under #ifndef FROM_X。删 `pipeline_asm_thin_delegate.c`；live＝runtime_pipeline_abi pure。bc residual **92→91**。双端 L2 矩阵 rv42／opt／si／hello（0a）。下刀：lint_meta／skip_dispatch 等 B′ leave；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** selfhost pure-owned leave | **代码** · **不升钉** · **双端 L2**。**方法**：B′ pure-complete `asm_module_num_defined_funcs`／`defined_func_ordinal` + 9×`is_*_selfhost` 谓词后 **delete-only** 整叶；Cap residual num_funcs／name_equal_at／is_extern_at；seed cold twin under #ifndef FROM_X（访问器、无 incomplete Module 字段）；残差 TU 改 extern。删 `pipeline_asm_selfhost.c`；live＝runtime_pipeline_abi pure。bc residual **93→92**。矩阵 rv42／opt／si／hello。下刀：lint_delegate／lint_meta／skip_dispatch 等 B′ leave；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** asm_ctx_loop pure-owned leave | **代码** · **不升钉** · **双端 L2**。**方法**：B′ pure-complete 固定容量表（64 ctx 槽 × depth8×64B break/continue + be_cont 24×128B end）后 **delete-only** 整叶；seed cold twin under #ifndef FROM_X；无 GrowVec。删 `pipeline_asm_ctx_loop.c`；live＝runtime_pipeline_abi pure（asm_ctx_loop_*／asm_be_cont_*）。bc residual **94→93**。矩阵 rv42／opt／si／hello。下刀：其余 B′ leave／8.3.1 中等叶；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** backend_asm_wrapper pure-owned leave | **代码** · **不升钉** · **双端 L2**。**方法**：B′ pure-complete M8-tail `pipeline_backend_asm_codegen_ast_c`／`_to_elf_c` 编排（hoist + merge/wpo + emit_set + mega_body + WPO thunks）后 **delete-only** 整叶；Cap residual hoist/seed mega/emit_set/mega_body/wpo/typeck merge 仍 host-cc；省略 XLANG_ASM_DEBUG fprintf；seed cold twin under #ifndef FROM_X。删 `pipeline_backend_asm_wrapper.c`；live＝runtime_pipeline_abi pure。bc residual **95→94**。矩阵 rv42／opt／si／hello。下刀：其余 B′ leave／8.3.1 中等叶；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** parse_typeck_dispatch pure-owned leave | **代码** · **不升钉** · **双端 L2**。**方法**：B′ pure-complete parse scalars／typeck_parsed／entry／should_skip_c／load_import resolve+slot 后 **delete-only** 整叶；Cap residual impl_rc+result_c 留 parse_orch（struct return unpack）；seed cold twin under #ifndef FROM_X；复用 wave93 realign/load_and_sync pure。删 `pipeline_parse_typeck_dispatch.c`；live＝runtime_pipeline_abi pure。bc residual **96→95**。矩阵 rv42／opt／si／hello。下刀：其余 B′ leave／8.3.1 中等叶；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** type_to_c pure-owned leave | **代码** · **不升钉** · **双端 L2**。**方法**：B′ pure-complete type_kind_copy／append／vector_type_copy／type_to_c_repr 后 **delete-only** 整叶；seed cold twin under #ifndef FROM_X；struct_emit 改调 public type_to_c_repr（弃 same-TU static inner）；num_types bounds via pipeline_arena_num_types。删 `pipeline_codegen_type_to_c.c`；live＝runtime_pipeline_abi pure。bc residual **99→98**。矩阵 rv42／opt／si／hello。下刀：struct_emit 等 pure-complete leave；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** skip_force pure-owned leave | **代码** · **不升钉** · **双端 L2**。**方法**：B′ pure-complete call_num_args_override／path_is_std_io_*／dep_skip_*／should_skip_emit_*／entry_is_lsp_*／force_param_* 后 **delete-only** 整叶；seed cold twin under #ifndef FROM_X；复用 wave107 private prefix_eq；Cap residual alias path_is_std_io_driver_bytes_u8_ptr_reti32。删 `pipeline_codegen_skip_force.c`；live＝runtime_pipeline_abi pure。bc residual **100→99**。矩阵 rv42／opt／si／hello。下刀：struct_emit／type_to_c 等 pure-complete leave；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** codegen_residual pure-owned leave | **代码** · **不升钉** · **双端 L2** @ `104ff827f`。**方法**：B′ pure-complete use_buf_wrapper／skip_emit_extern_io_batch_buf／should_skip_emit_func_by_name／emit_seed_mega_enabled／is_submit_batch_buf_call／should_skip_emit_func_core_read_ptr／asm_io_core_extern_callee_sym／io_driver_buf_call_sym／std_io_fixed_fd_emit_impl 后 **delete-only** 整叶；seed cold twin under #ifndef FROM_X；private pure prefix_eq 替代 same-TU static codegen_name_prefix_eq 依赖；forwarders 补 pure extern。删 `pipeline_codegen_residual.c`；live＝runtime_pipeline_abi pure。bc residual **101→100**。矩阵 rv42／opt／si／hello。下刀：其余 pure-complete leave；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** run_x_pipeline pure-owned leave | **代码** · **不升钉** · **双端 L2** @ `500021217`。**方法**：B′ pure-complete last_rc／typeck_fail／null_return／typecheck_entry_c／load_deps_after_parse_c／typecheck_after_load_c／parse_entry_do_parse_c／typecheck_entry_emit_c／pipeline_run_x_pipeline 后 **delete-only** 整叶；seed cold twin under #ifndef FROM_X。删 `pipeline_run_x_pipeline.c`；live＝runtime_pipeline_abi pure。bc residual **102→101**。矩阵 rv42／opt／si／hello。同 commit 清工作区其它脏文件（parser／diagnostic／vscode／analysis）。下刀：codegen residual 等 pure-complete leave；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** resolve_path pure-owned leave | **代码** · **不升钉** · **双端 L2** @ `82c8c4377`。**方法**：B′ pure-complete path_append_*_c／probe／flat／off-sidecar／codegen_out_buf_*／resolve_path_x_impl_c|_c 后 **delete-only** 整叶；seed cold twin under #ifndef FROM_X。删 `pipeline_resolve_path.c`；live＝runtime_pipeline_abi pure。bc residual **103→102**。矩阵 rv42／opt／si／hello。下刀：其余 pure-complete leave；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** emit_sidecar pure-owned leave | **代码** · **不升钉** · **双端 L2** @ `02003272b`。**方法**：B′ pure-complete 固定容量表（driver_emit_lib_root_* 64 槽×32 roots×256B + asm_qual_sym_layer_* 32×64B；无 GrowVec）后 **delete-only** 整叶；seed cold twin under #ifndef FROM_X；摘 `g_driver_emit_sc`。删 `pipeline_emit_sidecar.c`；live＝runtime_pipeline_abi pure。bc residual **104→103**。矩阵 rv42／opt／si／hello。下刀：resolve_path 等 pure-complete leave；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** lsp_diag pure-owned leave | **代码** · **不升钉** · **双端 L2** @ `aaeb03103`。**方法**：B′ pure-complete 3 面（typeck_after_load／parse_entry／parse_typeck + Cap-fn-ptr large-stack thread_fn）后 **delete-only** 整叶；seed cold twin under #ifndef FROM_X。删 `pipeline_lsp_diag.c`；live＝runtime_pipeline_abi pure。bc residual **105→104**。矩阵 rv42／opt／si／hello。下刀：emit_sidecar／resolve_path 等 pure-complete leave；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** asm_diag pure-owned leave | **代码** · **不升钉** · **双端 L2** @ `015aa3c05`。**方法**：B′ pure-complete 6 面（start_func_skip + BODY/FUNC_TRACE）后 **delete-only** 整叶；seed cold twin under #ifndef FROM_X。删 `pipeline_asm_diag.c`；live＝runtime_pipeline_abi pure。bc residual **106→105**。矩阵 rv42／opt／si／hello。下刀：其余 pure-complete leave；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** import_bind pure-owned leave | **代码** · **不升钉** · **双端 L2** @ `58cde2ed5`。**方法**：pure 补全 remaining strong（preprocess_len_get／read_file_x_impl_c／_c／read_fd）后 **delete-only** 整叶；seed cold twin under #ifndef FROM_X。删 `pipeline_import_bind.c`；parse_typeck_dispatch 收 driver_dep extern。bc residual **107→106**。矩阵 rv42／opt／si／hello。下刀：其余 pure-complete leave；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** preprocess_if pure-owned WEAK leave | **代码** · **不升钉** · **双端 L2** @ `1fa1f8b6b`。**方法升级**：pure 已权威时 **delete-only** 删 host-cc WEAK cold（无 seed 迁）。删 `pipeline_preprocess_if.c`；live＝`runtime_pipeline_abi` 固定 i32[32]；strict `preprocess_if_stack_only.o` 改自 pure `.o` partial-export。bc residual **108→107**。矩阵＋preprocess #if 探针。下刀：其余 pure-owned WEAK 整叶；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** loop_glue 离 pipeline_x host-cc | **代码** · **不升钉** · **双端 L2** @ `347523170`。删 `pipeline_loop_glue.c`（#include／DEPS／g05 STALE／inventory absent）；live 有界循环谓词 + one_dep prepare 迁 codegen_gen seed→codegen_x.o；pipeline_x U-resolve。bc residual **109→108**。mac：rv42／opt／si／hello；soa=8／f32soa=10。下刀：8.3.1 其余 leaf／pipeline_x host-cc；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.2)** scratch_bufs 离 pipeline_x host-cc | **代码** · **不升钉** · **双端 L2** @ `afaa9765d`。删 `pipeline_scratch_bufs.c`（#include／DEPS／g05 STALE／inventory absent）；live BSS 访问器迁 codegen_gen seed→codegen_x.o；ast_／codegen_ 前缀 thin 面仍 pipeline_x forwarders。bc residual **110→109**。mac：rv42／opt／si／hello；soa=8／f32soa=10。下刀：8.3.1 其余 leaf／pipeline_x host-cc；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.1)** typeck_slots 离 pipeline_x host-cc | **代码** · **不升钉** · **双端 L2** @ `b7f93e785`。删 `pipeline_typeck_slots.c`（#include／DEPS／g05 STALE／inventory absent）；live BSS 访问器迁 typeck_gen seed→typeck_x.o；死代码 binop_arith_infer／try_packed 不迁（typeck.x 已有 widen／layout 权威）。bc residual **111→110**。mac+Ubuntu：rv42／opt／si／hello；soa=8／f32soa=10。下刀：8.3.1 其余 leaf／pipeline_x host-cc；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.3)** field_access／soa 离 pipeline_x host-cc | **代码** · **不升钉** · **双端 L2** @ `b49e666b9`。删 `pipeline_typeck_field_access.c`／`pipeline_typeck_soa.c`（#include／DEPS／g05 STALE／inventory absent）；产品调用直连 `typeck_*`／`typeck_soa_*`；strict_minimal 四 thin 面迁 typeck_x（seed 同步）；bc residual **113→111**。mac+Ubuntu：rv42／opt102／si／hello；soa=8／f32soa=10；match lit/guard/bind。下刀：8.3.1 leaf／pipeline_x 其余 host-cc；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **fix(asm)** pure-asm match arm guards | **代码** · **不升钉** · **双端 L2** @ `a2bb366a2`。根因：`pipeline_asm_emit_match_elf_c` 把所有 wildcard 当 default，未读 `match_arm_guard_ref`；struct field-lit 存为 wild+隐式 guard → pure 错臂。G.7：顺序 first-match 对齐 host-C wave708（pure wild 终臂／wild+guard jz／lit re-emit+jne+optional guard）；subject hop 保留。mac+Ubuntu freestanding：lit／lit_wild／destructure／min／match_guard=0；bind=30／classify=3；矩阵 rv42／opt／si／hello。下刀：host-cc 脱钩；升钉前 tip L4+bstrict。PLATFORM: SHARED. |
| 2026-08-05 | **fix(asm)** pure-asm match field-bind | **代码** · **不升钉** · **双端 L2** @ `9fcb4ec2f`。根因：`pipeline_asm_emit_match_elf_c` 未 set host-C subject；臂内 bare `x`/`y` 无栈槽 → Ubuntu pure-asm CG002。G.7：match emit 期间 `pipeline_codegen_match_set_subject_c`；VAR fast `glue_try_emit_match_subject_field_var_elf_c`（layout off + local slot load）。mac+Ubuntu：`match_struct_bind` pure=30／fs=30；classify=3；矩阵 rv42／opt／si／hello。**soft field-lit 多臂** 见上行已闭。PLATFORM: SHARED. |
| 2026-08-05 | **fix(typeck)** match_struct_destructure | **代码** · **不升钉** · **双端 L2**。根因：产品 `typeck_check_expr_var`（.x）缺 wave703 match subject field hop，而 match 走 C 设 `g_typeck_match_subject_*` → `Point { x, y } => x+y` XT001。G.7：`.x` VAR 调 `pipeline_typeck_match_subject_field_type_c`；seed 同步。mac 默认 host-C + Ubuntu `-backend c`：destructure/bind/classify 全绿；矩阵 rv42／opt／si／hello。**soft**：曾 pure-asm field-bind CG002（本表上行已闭）。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.3 R2)** bare-import-const | **代码** · **不升钉** · **双端 L2**。`typeck_reject_bare_import_const` C→typeck.x（G.7 与 `check_expr_var` 共用 find/hint/driver_diag）；C thin；field_access ~334→~218 全 thin；seed 同步。mac+Ubuntu：rv42=42／Point=7／rev.v=7／unknown T001／enum-no-variant／hello／alen=3／type_alias／shorthand／`import_const_bare_fail` T001 must be qualified。下刀：soa 宿主／host-cc 脱钩／match_struct。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.3 R2)** mono+concrete+hard_fail | **代码** · **不升钉** · **双端 L2**。`typeck_mono_field_type_from_base`／`typeck_named_is_module_concrete`／`typeck_field_unknown_hard_fail` C→typeck.x；C thin 三符号；field_access ~704→~334（仅 thin+bare-import-const）；seed 同步。mac+Ubuntu：rv42=42／Point=7／rev.v=7／mono=11／alias=10／unknown-field T001／enum-no-variant／hello／data_field。下刀：bare-import-const residual／soa 宿主／match_struct。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.3 R2)** field_access 主编排 | **代码** · **不升钉** · **双端 L2**。`typeck_check_expr_field_access` 编排体 C→typeck.x（+ reverse_infer／apply_mono／apply_ambient 同叶）；C thin `pipeline_typeck_check_expr_field_access_c`；residual mono_from_base／named_concrete／unknown_hard_fail 仍 C；seed 同步。mac：rv42=42／Point=7／rev.v=7／mono=11／alias=10／unknown-field 硬失败／data_field／struct shorthand。下刀：mono／concrete／hard_fail。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.3 R2)** import_binding | **代码** · **不升钉** · **双端 L2**。`typeck_field_import_binding` C→typeck.x（dep 函数／const／enum + const-import sugar；`typeck_dep_top_level_const_match` 共用）；C thin `pipeline_typeck_field_import_binding_resolve_c`；seed 同步。mac：rv42／opt=102／hello／import*／slice／f32soa=10／type_alias；bare-const 诊断绿。下刀：主编排。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.3 R2)** known_ptr | **代码** · **不升钉** · **双端 L2**。`typeck_field_known_ptr` C→typeck.x（`*ASTArena`／`*Module` 硬编码 SoA 字段+偏移／数组类型）；C thin `pipeline_typeck_field_known_ptr_types_c`；seed 同步。mac+Ubuntu：rv42／opt=102／hello／point=7／a.length=3／slice／f32soa=10／type_alias。下刀：import_binding／主编排。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.3 R2)** layout_named | **代码** · **不升钉** · **双端 L2**。`typeck_field_layout_named` C→typeck.x（alias peel／enum 返 2／TypeKind／mod.T 去前缀／layout deps／TokenKind.EOF）；C thin 转调；seed 同步。mac：rv42／opt=102／hello／type_alias／enum／a.length=3／slice／soa。下刀：known_ptr／import／主编排。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.3 R2)** field_prebind | **代码** · **不升钉** · **双端 L2**。`typeck_field_prebind` C→typeck.x（未绑 VAR base→TYPE_NAMED 同名；形参跳过）；C thin 转调；seed 同步。mac+Ubuntu：rv42／hello／opt=102／Point=7／a.length=3／slice data_field／soa=8／f32_soa_sum=10。下刀：layout_named／known_ptr。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.3 R2)** name+lexer fallback | **代码** · **不升钉** · **双端 L2**。`typeck_field_name_fallback` + `typeck_field_lexer_fallback` C→typeck.x（CodegenOutBuf.data／inline u8·i32 array／scalar；LexerResult wrapper + formal-type hop）；C thin 转调；seed 同步。mac+Ubuntu：rv42／hello／opt=102／slice field+length／SoA sum=10／Point.x+y=7／a.length=3。下刀：prebind／layout。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.3 R2)** field_slice | **代码** · **不升钉** · **双端 L2**。`typeck_field_slice` C→typeck.x（ARRAY/VECTOR `.length` + SLICE `.length`/`.data` fat@0/@8）；C thin `pipeline_typeck_field_slice_c`；seed 同步。mac 矩阵 rv42／opt／si／hello；SoA 8／8；slice field OK + length/data 绿。下刀：name_fallback／lexer_fallback。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.3 R2)** fill_soa | **代码** · **不升钉** · **双端 L2**。`typeck_soa_fill_field_access_for_asm_emit` C→typeck.x；C thin `pipeline_fill_soa_*`；4 glue static→link 面；seed 同步。mac 矩阵 rv42／opt／si／hello；SoA 8／8／10／10。下刀：field_access 主体。PLATFORM: SHARED. |
| 2026-08-05 | **feat(8.3.3 R2)** field_soa_index | **代码** · **不升钉** · **双端 L2**。`typeck_soa_field_soa_index` C→typeck.x（stride=`typeck_x_type_size`）；soa.c 仅 thin 转调 + fill 编排；seed 同步。mac 矩阵 rv42／opt／si／hello；SoA smoke 8／8／10／10。下刀：fill_soa 编排或 field_access。PLATFORM: SHARED. |
| 2026-08-05 | **fix(build)** seed 刷新 | **代码** · **不升钉**。`ensure_migrate_gen`／`migrate_x_objs`：tip seed 比本地 gitignored pin 新且内容不同时刷新（防 Ubuntu pure-ld UNDEF typeck_soa_*）。PLATFORM: SHARED. tip `fc5a25cf6` |
| 2026-08-05 | **feat(8.3.3 R2)** find_layout_module_and_idx | **代码** · **不升钉** · **双端 L2**。`typeck_soa_find_layout_module_and_idx` C→typeck.x（WPO dep 池 + `pipeline_asm_emit_dep_pipe_c`）；soa.c 仅 extern；seed 同步。mac+Ubuntu 矩阵 rv42／opt／si／hello；SoA smoke 4 绿。下刀：`field_soa_index`。PLATFORM: SHARED. tip `83a8e4334` |
| 2026-08-05 | **feat(8.3.3 R2)** col_base | **代码** · **不升钉** · **mac L2**（本波补 Ubuntu）。`typeck_soa_col_base_for_field` C→typeck.x（`typeck_x_type_align`／`size` 替 glue_type_*）；soa.c 仅 extern；seed 同步。矩阵 rv42／opt／si／hello；SoA smoke 4 绿。PLATFORM: SHARED. |
| 2026-08-05 | **合并** self-hosting→dev→main | dev `02d080583`（Merge self-hosting）· main `a22474203`（Merge dev）· 无冲突（分歧仅历史 merge commits）· 已 push。 |
| 2026-08-05 | **fix(parser-seed)** `d05350d22` · L4 冷债#1 parser seed/pin desync | **代码** · **不升钉**。sync `seeds/parser_gen.linux.x86_64.c` ← pin（seed 不编译 4 errors，pin 编译）。unblock L4 冷构建 bootstrap。L2 不受影响。PLATFORM: SHARED。 |
| 2026-08-05 | **fix(build)** `fdf96a5be`／`70e29e11a` · L4 冷债#2 sh→bash 可移植性 | **代码** · **不升钉**。4 个 bash-array gen 脚本（ensure_{migrate,driver,lsp_pipeline,archaeology}_gen）被 `sh` 调用，Ubuntu dash 拒绝数组。xlang-build + migrate_x_objs + ensure_gen_x_o + bootstrap_typeck_codegen 全改 `bash`。PLATFORM: SHARED。 |
| 2026-08-05 | **fix(build)** `ae7568270` · babash 笔误（自纠） | **代码** · **不升钉**。replace_all `sh scripts/X` 匹配到 `bash` 内的 `sh` → `babash`（migrate_x_objs L137）。仅冷 regen 触发。改回 `bash`。 |
| 2026-08-05 | **fix(build_tool)** `38eccee5b` · L4 冷债#3 build_runtime_x seed-pin | **代码** · **不升钉**。build_tool -E regen retired `build_runtime_x_gen.c` 丢 `build_run_step`（build_runtime.x 已 refactor 掉）→ build_tool link 失败。retired 文件 pin 到 seed（-E opt-in `XLANG_BUILD_RUNTIME_X_E=1`）。PLATFORM: SHARED。 |
| 2026-08-05 | **L4 真冷构建恢复**（Ubuntu 金标） | **验证** · **不升钉**。4 层冷债全修后：Ubuntu `clean→driver-gen→bootstrap 53/53→build OK→矩阵 rv=42／opt=102／hello／si→bare check exit=0` 全绿。L4 冷构建（升钉前提）恢复。 |
| 2026-08-05 | **~~待修~~ 已修** bstrict `match_struct_destructure` | 见上 **fix(typeck)** 行：G.7 补全 `.x` VAR subject hop；warm 亦红（非 seed 假冷）。 |

---

## 日终快照（归档时）

| 项 | 值 |
|----|-----|
| 产品钉盘 | `db809e00f`（不升） |
| tip L4 安全网 | `ffc0b646f`（不升） |
| residual tip（约） | binop pure leave `0dc2a35e1` |
| inventory present | **64→63**（binop leave） |
| check 闸门 | **暂停**（2026-08-05 用户修订） |
| L4 冷构建 | ✅ 已恢复（Ubuntu 金标） |
| 验证 | 日常 SHARED 双端 L2；升钉前 tip L4+bstrict |

> **勿**用本归档 tip 冒充 8 月 6 日后续现绿。
