# 自举进度归档 · 2026-08-21

> **归档日期**：2026-08-22（21 日条目从 [自举进度.md](自举进度.md) §6 迁出）
> **钉盘**：`f7424ae47`（不升钉）
> **条目范围**：leftover extra SLICE peels RET twin（`[]*[]T`／`[]*[][]T`）＋ PARAM eand＝−2／ndims≥1 ＋ dest-PARAM／dest-RET extras ndims＝0／−2 ＋ link-abi wave956–958（net／thread／map／heap／http／vec 探针表 ＋ `-lpthread`）＋ 双端 L4 验证（**13** 行）
> **当日 tip 跨度**：`8d4e74d69` → `096998149`

## §6 变更记录（21 日 · 倒序）

| 日期 | 波次 | 描述 |
|------|------|------|
| 2026-08-21 | **L4 真冷全测** **wave956–958 link-abi 双端 L4 验证** · tip **`096998149`** | **验证** · **双端 L4 真冷**。macOS arm64：remaining_o=0 ✅ · bootstrap-driver-seed ✅ · g05 relink ✅ · 矩阵 5/5（rv42／opt102／si0／hello0a）· bstrict 12 ok / 103 skip(W3 fast) / 7 fail（全 check/typeck gate 债务：run-scope-borrow-gate／run-al06-gate／run-type-borrow-conflict-gate／run-i64-ctfe-gate／run-safe-unsafe-audit-gate／run-signed-overflow-gate／run-lexer-bounds-gate；check 闸门暂停 2026-08-05）；mac wall=245s。Ubuntu x86_64（金标）：remaining_o=0 ✅ · bootstrap-driver-seed ✅ · g05 relink ✅ · bstrict 22 ok / 106 skip(W3) / **0 fail**；Ubuntu wall=502s。**结论**：wave956–958 link-abi 变更（vec/http/thread/map/heap/core_slice/unicode 探针表 + http glue companion + `-lpthread` fix）**无回归**；macOS 7 fail 全为 check gate 债务（macOS-specific typeck 行为差异），非 link-abi 引入；Ubuntu 0 fail。钉盘仍 **`f7424ae47`**（未升钉；macOS check gate 7 fail 待 check 闸门恢复后处理）。PLATFORM: SHARED link-abi · LINUX gold。 |
| 2026-08-21 | **fix(link-abi)** **create vec/http probe tables + expand string + http glue companion (wave958)** · tip **`d5d167803`** | **代码** · **双端 L2 闭**。五问：产生＝asm on-demand linker 缺 vec/http 探针表 + string 表缺 case_fold — ① 无 `labi_od_vec_sym_*` 表，Vec<u16/i32/u8> 用户程序永不触发 vec.o ensure；② 无 `labi_od_http_sym_*` 表，http 用户程序永不触发 http.o ensure；③ `labi_od_simple_group` g==0 string 表 9 项缺 `std_string_string_view_case_fold`；④ http.o 有 UNDEF `_http_*_c` 符号定义在 `compiler/runtime_http_glue.o`，需 companion push（同 thread_glue 模式）。存储＝`labi_ondemand_list.x`（.x）+ `labi_ondemand_list.from_x.c`（C seed）+ `labi_ondemand_heavy.x`（ensure block）。消费＝`xlang_asm_ld_append_on_demand_user_objs`。**修**：G.7 单权威 — ① create `labi_od_vec_sym_{count,at}`（12 项：push/get/length/deinit/capacity/clear/from_slice 跨 i32/u8/u16）+ `link_abi_user_o_needs_std_vec` + `labi_od_rel_vec` + standalone ensure block；② create `labi_od_http_sym_{count,at}`（5 项：parse_status_line/decode_chunked_body/has_chunked_encoding/has_keep_alive/headers_body_offset）+ `link_abi_user_o_needs_std_http` + `labi_od_rel_http` + standalone ensure block + `labi_od_rel_http_glue` + `xlang_ensure_runtime_http_glue_o`/`xlang_runtime_http_glue_o_path` companion push；③ expand string g==0 9→10（+`std_string_string_view_case_fold`）。证：**双端 L2** — 4/4 cookbook build=0（`vec_u16_push` run=0 Ubuntu／run=139 macOS runtime panic 非链接债；`http_parse_status` run=0；`http_chunked_decode` run=0；`string_case_fold` run=0）；wave956+957 回归 6/6 build=0；矩阵 rv42／hello0a；bstrict `run-net.sh` rc=0 双端。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED link-abi · LINUX gold。 |
| 2026-08-21 | **fix(link-abi)** **expand map/heap/core_slice/unicode on-demand probe tables (wave957)** · tip **`d75bf4090`** | **代码** · **双端 L2 闭**。五问：产生＝asm on-demand linker 的 UNDEF 探针表不完整或指向错误 .o — ① `labi_od_map_sym_*` 表只有 9 项（Map_i32_i32 + str），缺 Map<u64,i32> 的 6 个 mangled 变体；② `labi_od_heap_api_sym_*` 表 25 项缺 trace 符号（`std_heap_trace_on`/`reset`）；③ `labi_od_core_slice_sym_*` 表 6 项只有 C-bridge `_c` 后缀符号（在 `core/slice/slice.o` glue），缺 X-facing u64 符号（`chunks_len`/`split_at`/`subslice`，在 `core/slice/mod.o` API），且 ensure block 只推 glue 不推 mod.o；④ .x 源无 unicode simple_group（g0-g19），C seed `labi_fk0` k==17 有但 PREFER 路径用 .x → unicode 符号永不触发。存储＝`labi_ondemand_list.x`（.x）+ `labi_ondemand_list.from_x.c`（C seed）+ `labi_ondemand_heavy.x`（ensure block）。消费＝`xlang_asm_ld_append_on_demand_user_objs`。**修**：G.7 单权威 — ① expand map 9→15（+6 Map_u64_i32）；② expand heap_api 25→27（+2 trace）；③ expand core_slice 6→9（+3 X-facing u64）+ ensure block 加推 `core/slice/mod.o`（.x heavy + C seed 同 commit）；④ add g==20 unicode to .x `labi_od_simple_group_{count,sym_count,sym_at,rel}`（8 项）+ C seed sync + `labi_fk0_sym_at` k==17 加 2 新符号。证：**双端 L2** — 4/4 A1 cookbook build=0+run=0（`map_u64_get`／`heap_trace_reset`／`slice_u64_subslice`／`unicode_nfc_smoke`）；wave956 回归 4/4 build=0；矩阵 rv42；bstrict `run-net.sh` rc=0 双端。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED link-abi · LINUX gold。 |
| 2026-08-21 | **fix(link-abi)** **expand on-demand net/thread probe tables + standalone thread ensure (wave956)** · tip **`2575f14e2`** | **代码** · **双端 L2 闭**。五问：产生＝asm on-demand linker 的 UNDEF 探针表不完整 — ① `labi_od_net_sym_*` 表只有 17 项，缺 `std_net_resolve_ex`／`std_net_resolve_err_*`／`std_net_close_stream`／`std_net_connect_blocking`／`std_net_write_batch`／`std_net_tcp_pool_*`（10 个新符号）；② 无 `labi_od_thread_sym_*` 表，`thread.o` 只在 `need_net` 块内作为 net 的 companion 推入，只 import `std.thread` 的用户程序（`thread_pool_stats.x`）永远不触发 thread.o ensure → BLD001 UNDEF `std_thread_stats`。存储＝`labi_ondemand_list.x`（.x 源）+ `labi_ondemand_list.from_x.c`（C seed，`runtime_link_abi.from_x.c` `#include`）。消费＝`xlang_asm_ld_append_on_demand_user_objs`（`labi_ondemand_heavy.x` .x 版 + `labi_ondemand_list.from_x.c` C seed 版）。**修**：G.7 单权威 — ① expand `labi_od_net_sym_count` 17→27（+10 net 符号）；② add `labi_od_thread_sym_count/at`（4 项：create／join／start／stats）+ `link_abi_user_o_needs_std_thread` pure orch（twin of `needs_std_net`）；③ add standalone thread ensure block in `xlang_asm_ld_append_on_demand_user_objs`（.x `labi_ondemand_heavy.x` + C seed `labi_ondemand_list.from_x.c` 同 commit）：`need_thread && !have_thread` → push `thread.o` + `thread_glue.o`。证：**双端 L2** — 4/4 cookbook build=0（`net_resolve_invalid`／`net_stream_write`／`net_tcp_pool`／`thread_pool_stats`）；`thread_pool_stats` run=0（前 BLD001 UNDEF）；产品矩阵 rv42／opt0／si0／hello0a **4/4**；bstrict `run-net.sh` rc=0 双端。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED link-abi · LINUX gold。 |
| 2026-08-21 | **fix(link-abi)** **macOS mach tail libs `-pthread` → `-lpthread`** · tip **`f0adb5b3f`** | **代码** · **双端 L2 闭**。五问：产生＝`xlang_asm_ld_append_mach_tail_libs_impl` need_pt（thread/sync/channel）时无条件追加 `labi_ld_flag_pthread()`＝`-pthread`（driver flag）；存储＝macOS bare `/usr/bin/ld`（asm -o exe 路径）不认 `-pthread`（是 cc/clang driver 选项）→ `ld: unknown options: -pthread` → BLD001；消费＝任何 import std.net/thread/sync/channel 的用户程序（如 `examples/cookbook/net_listen_bind.x`）。判别 vs unix_gcc twin（`!have_io && need_pt` 分支）用 `labi_ld_flag_lpthread()`＝`-lpthread`。**修**：G.7 单权威（mach tail libs need_pt 分支）— `.x` L1225 + `from_x.c` L417 同 commit 改 `labi_ld_flag_pthread()` → `labi_ld_flag_lpthread()`；macOS ld 经 libSystem 解析（pthread 内置），Linux ld 经 libpthread。G.8 SHARED 双端。证：**双端 L2** `net_listen_bind.x` build0／run0（前 BLD001）· bstrict `run-net.sh` rc=0 · 矩阵 rv42／hello0a **4／4**。4 cookbook UNDEF（`std_net_resolve_err_host_not_found`／`std_thread_stats`）是 C2 第二阶段（函数 ensure/gate），非 `-pthread`，不溯。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED link-abi · LINUX gold。 |
| 2026-08-21 | **fix(parse／typeck)** **leftover extra SLICE peels leftover eek＝PTR eend＝−2 `[]*[]T`／`[]*[][]T` RET twin mismatch T001** · tip **`a56a93e4a`** | **代码** · **双端 L2 闭**。五问：产生＝PARAM `875d557e2` 昨日已活 eand＝−2 walk；**RET 孪漏 4 处** — ① line 3926 leftover PTR vs eek＝PTR 门控 `pgek==ARRAY||SLICE`（impl `[]*i32` PTR-pointee＝I32 不进门 → eend＝−2 extra SLICE 从未剥 → 直接 eeek＝I32 vs I32 假绿 compile＝0 run＝1 leftover skip）；② line 7024 builtin 捕获 base T 未开 `ret_elem_suffix_pending` 且 ret_elem_kinds＝PTR／SLICE 元未提交 PTR_TO_SLICE ndims＝−2；③ suffix RBRACKET（elem `[]` after base T）只扫 PTR 外层，SLICE／ARRAY 外层走 wave438 fallback elem_kind＝−1 eek＝−1 leftover skip；④ suffix TOKEN_STAR（extra `*` after `[]*[]`）同样只扫 PTR 外层未扫 SLICE／ARRAY outer STAR → eeek＝−1 跳过检查）。存储＝同 PARAM 权威形状（ndims＝−2／dims[0]＝N extra SLICE／dims[1]＝extra PTR）。判别 vs dest extras dest-RET PTR-outer STAR 分支（line 6835／6901／6953）是 ret_kinds＝SLICE／ARRAY vs ret_kinds＝PTR。消费＝leftover PTR vs eek＝PTR 非 T001 须先剥 leftover PTR 再 extra SLICE 再 extra PTR（RET 孪 line 3926 门控已去；eend＝−2 dims[0]／dims[1] peel 已活）。**修**：G.7 补 4 处 ②③④ 存储扫描仪 + ① 消费去门控（禁 −3／第二套 dest-PTR stamp／assemble parser.x／pipeline_abi mega）。wrapper rdi＝data **未改**。证：**双端 L2 6／6 T001 捕获** · core `[]*i32` vs `[]*[]i32` · ARRAY size `[]*[2]i32` vs `[]*[]i32` · SLICE count `[]*[][]i32` vs `[]*[]i32` · dual-slot STAR `[]*[]i32` vs `[]*[]*i32` · NAMED leaf `[]*A` vs `[]*[]A` · extra ARRAY dim `[]*i32` vs `[]*[2]i32`；**PARAM 正例 2／2 asm／host-C 7**（`dyn_add_slice_ptr_slice`／`dyn_add_slice_ptr_slice_slice`）；产品矩阵 rv42／opt102／si0／hello0a **4／4**；邻域 55／59 impl／trait／RET／SLICE_PTR boundary pass（4 arr 债旧存不溯）。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED parse／typeck · LINUX gold。 |
| 2026-08-21 | **fix(parse)** **leftover extra SLICE peels leftover eek＝PTR eand＝−2 `[]*[]T`／`[]*[][]T` leftover mismatch T001** · tip **`875d557e2`** | **代码** · **双端 L2 闭**。五问：产生＝leftover PTR vs eek＝PTR 匹配后 eand＝−2 不剥 extra SLICE（store-only leftover extras never compared＝leftover skip 假绿；impl `[]*i32` vs trait `[]*[]i32` compile＝0 run＝1 leftover skip）。存储已齐（elem＝PTR／eek＝leaf／ndims＝−2／dims[0]＝N extra SLICE／dims[1]＝extra PTR）。判别 vs leftover extra peels PARAM PTR-elem ndims≥1 `**[2][]T`／`**[2]*T`（已闭）是 eand＝−2 PTR-to-SLICE 哨兵 vs eand≥1。消费＝leftover PTR vs eek＝PTR 非 T001 须先剥 leftover PTR 再 extra SLICE（RET 孪 line 3681 已有 SLICE peels；extra PTR RET twin 无现存探针＝未来波）。**修**：G.7 补既有 leftover eek＝PTR eand＝−2 extra SLICE then extra PTR peels（PARAM 路径 line 3449；禁 −3／第二套 dest-PTR stamp／assemble parser.x／pipeline_abi mega）。leftover extra SLICE peels leftover eek＝PTR eand＝−2 RET twin（`[]*[]T` SLICE 外层 RET）无现存探针留未来波。wrapper rdi＝data **未改**。证：**双端** `dyn_add_slice_ptr_slice`／`dyn_add_slice_ptr_slice_slice` asm／host-C **7** · extra wrap COUNT＝2 `[]*[][]i32` **7** · leftover mismatch T001（impl `[]*i32` vs trait `[]*[]i32`）· leftover ARRAY size T001（impl `[]*[2]i32` vs trait `[]*[]i32`）· leftover SLICE count T001（impl `[]*[][]i32` vs trait `[]*[]i32`）· `-E` `struct xlang_slice_xlang_slice_int32_t_p * p`＋wrapper `void* data` · COUNT＝2 `-E` `struct xlang_slice_xlang_slice_xlang_slice_int32_t_p * p` · slice_ptr_slice／slice_ptr_slice_slice／ret_ptr_slice_ptr／ret_ptr_slice_slice_ptr／ret_ptr_slice_arr_slice／ret_ptr_slice_arr_ptr **7／7／7／7／7／7** · 假 clang／gcc **clang_hits=0** · hello size Ubuntu **27488** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED parse · LINUX gold。 |
| 2026-08-21 | **fix(parse)** **leftover extra peels PARAM PTR-elem ndims≥1 `**[2][]T`／`**[2]*T` leftover mismatch T001** · tip **`ef69c7533`** | **代码** · **双端 L2 闭**。五问：产生＝leftover PTR vs eek＝PTR 匹配后 eand≥1 不剥 extra ARRAY／SLICE／PTR（leftover extras never compared＝leftover skip 假绿；impl `**[2]i32` vs trait `**[2][]i32` compile＝0 run＝2 leftover skip；impl `**[2]i32` vs trait `**[2]*i32` compile＝0 run＝98 leftover skip）。存储已齐（elem＝PTR／eek＝leaf／ndims≥1／dims[0]＝N／extra SLICE 在 dims[ndims]／extra PTR 在 dims[ndims+1]）。判别 vs leftover extra peels PARAM PTR-elem ndims＝0 `**[]T`／`***T`（已闭）是 ndims≥1 vs ndims＝0。消费＝leftover PTR vs eek＝PTR 非 T001 须先剥 leftover PTR 再 ARRAY 再 extra SLICE 再 extra PTR（RET 孪已活）。**修**：G.7 补既有 leftover eek＝PTR eand≥1 extra ARRAY then extra SLICE then extra PTR peels。leftover extra SLICE peels leftover eek＝PTR eand＝−2（`[]*[]T`／`[]*[][]T`）仍延期。禁 −3／第二套 dest-PTR stamp／assemble parser.x／pipeline_abi mega。wrapper rdi＝data **未改**。证：**双端** `dyn_add_ptr_ptr_arr_slice`／`dyn_add_ptr_ptr_arr_ptr` extra ADDR_OF of typed dest／host-C **7** · extra wrap COUNT＝2 **7** · leftover mismatch T001 · leftover ARRAY size T001 · dual-slot leftover extra STAR mismatch T001 · `-E` `struct xlang_slice_int32_t * * p`＋wrapper `void* data` · add_ppas／ppap／ret_pps／add_pas／add_sps／add_spss／add_apss／add／f32／call_ok／implicit／named／slice／arr2／asas／sas／apas／ret_ptr／add_psap／add_psas／ret_ppp／ret_psap／ret_psas／ret_psp／ret_pssp／ret_pas／ret_ppas／ret_pap／ret_ppap／ret_pa／add_pps／spas／spap **7／7／7／7／7／7／6／7／7／7／7／7／7／11／7／7／6／7／7／7／7／7／7／7／7／7／7／7／7／7／7／7／7** · P013 · 假 clang／gcc **clang_hits=0** · hello size Ubuntu **27488** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED parse · LINUX gold。 |
| 2026-08-21 | **fix(parse)** **dest extras dest-PARAM extra STAR PTR-elem ndims＝0 `***T` dest-stamp** · tip **`611b13cf9`** | **代码** · **双端 L2 闭**。五问：产生＝extra STAR after `**`（elem＝PTR、PTR 外层、ndims＝0）走 `nd>0` 失败再 elem_kind＝−1 leftover 跳过 eek＝−1（extra ADDR_OF of typed dest dest-stamps via the formal＝leftover skip 假绿；leftover mismatch impl `**i32` vs trait `***i32` compile＝0 run＝7 leftover skip so leftover never compares）。存储＝elem＝PTR／eek＝leaf／ndims＝0／extra PTR wrap 在未用槽 dims[1]（1＝`***T`；2＝`****T`；0＝无 extra PTR＝`**T`／`**[]T`；extra SLICE 仍 dims[0]；两槽＝`**[]*T`）。判别 vs PARAM extra STAR PTR-elem ndims≥1 `**[2]*T`（extra PTR 在 dims[ndims+1]）是 ndims＝0 vs ndims≥1；vs PARAM extra empty `[]` PTR-elem ndims＝0 `**[]T`（extra SLICE 在 dims[0]；已闭）是 extra PTR vs extra SLICE。消费＝leftover PTR vs eek＝PTR 非 T001（walk 先剥 leftover PTR 再 extra SLICE 再 extra PTR when ndims＝0；leftover extra PTR peels 已活）；extra ADDR_OF of typed `**i32` dest-stamps via the formal（无 dest extras dest-PTR stamp）。**修**：G.7 补既有 PARAM extra STAR unused-slot 扫描器（PTR 元；PTR 外层；ndims＝0 extra PTR 进 dims[1]）＋leftover eand＝0 extra PTR peels（已活）＋PARAM dim accessor nd＝0 dim_ix＝1。禁 −3／第二套 dest-PTR stamp／assemble parser.x／pipeline_abi mega。wrapper rdi＝data **未改**。证：**双端** `dyn_add_ptr_ptr_ptr` extra ADDR_OF of typed dest／host-C **7** · extra wrap COUNT＝2 **7** · leftover mismatch T001 · `-E` `int32_t * * * p`＋wrapper `void* data` · add_ppas／ppap／ret_pps／add_pas／add_sps／add_spss／add_apss／add／f32／call_ok／implicit／named／slice／arr2／asas／sas／apas／ret_ptr／add_psap／add_psas／ret_ppp／ret_psap／ret_psas／ret_psp／ret_pssp／ret_pas／ret_ppas／ret_pap／ret_ppap／ret_pa／add_pps **7／7／7／7／7／7／6／7／7／7／7／7／7／11／7／7／6／7／7／7／7／7／7／7／7／7／7／7／7／7／7** · P013 · 假 clang／gcc **clang_hits=0** · hello size Ubuntu **27488** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED parse · LINUX gold。 |
| 2026-08-21 | **fix(parse)** **dest extras dest-PARAM extra empty `[]` PTR-elem ndims＝0 `**[]T` dest-stamp** · tip **`d4720c661`** | **代码** · **双端 L2 闭**。五问：产生＝extra empty `[]` after `**`（elem＝PTR、PTR 外层、ndims＝0）先被 ndims＝−2 PTR-to-SLICE 哨兵截走（0 表 1）再 leftover 跳过 eek＝−1（extra ADDR_OF of typed dest dest-stamps via the formal＝leftover skip 假绿；leftover mismatch impl `**i32` vs trait `**[]i32` compile＝0 run＝1）。存储＝elem＝PTR／eek＝leaf／ndims＝0／extra SLICE wrap 在未用槽 dims[0]（1＝`**[]T`；2＝`**[][]T`；0＝无 extra SLICE＝`**T`／`***T`；extra PTR 仍 dims[1]；两槽＝`**[]*T`）。判别 vs PARAM extra empty `[]` PTR-elem ndims≥1 `**[2][]T`（extra SLICE 在 dims[ndims]）是 ndims＝0 vs ndims≥1；vs PARAM extra STAR PTR-elem ndims＝0 `***T`（extra PTR 在 dims[1]；仍延期）是 extra SLICE vs extra PTR。消费＝leftover PTR vs eek＝PTR 非 T001（walk 先剥 leftover PTR 再 extra SLICE 再 extra PTR when ndims＝0）；extra ADDR_OF of typed `*[]i32` dest-stamps via the formal（无 dest extras dest-PTR stamp）。**修**：G.7 补既有 PARAM extra empty `[]` unused-slot 扫描器（PTR 元；PTR 外层；ndims＝0 extra SLICE 进 dims[0]）＋leftover eand＝0 extra SLICE peels＋ndims＝−2 哨兵排除 PTR-elem＋PTR 外层。禁 −3／第二套 dest-PTR stamp／assemble parser.x／pipeline_abi mega。wrapper rdi＝data **未改**。证：**双端** `dyn_add_ptr_ptr_slice` extra ADDR_OF of typed dest／host-C **7** · extra wrap COUNT＝2 **7** · leftover mismatch T001 · `-E` `struct xlang_slice_int32_t * * p`＋wrapper `void* data` · add_ppas／ppap／ret_pps／add_pas／add_sps／add_spss／add_apss／add／f32／call_ok／implicit／named／slice／arr2／asas／sas／apas／ret_ptr／add_psap／add_psas／ret_ppp／ret_psap／ret_psas／ret_psp／ret_pssp／ret_pas／ret_ppas／ret_pap／ret_ppap／ret_pa **7／7／7／7／7／7／6／7／7／7／7／7／7／11／7／7／6／7／7／7／7／7／7／7／7／7／7／7／7／7** · P013 · 假 clang／gcc **clang_hits=0** · hello size Ubuntu **27488** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED parse · LINUX gold。 |
| 2026-08-21 | **fix(parse／typeck)** **dest extras dest-RET extra empty `[]` PTR-elem ndims＝0 `**[]T` dest-stamp** · tip **`eb83f6579`** | **代码** · **双端 L2 闭**。五问：产生＝extra empty `[]` after `**`（elem＝PTR、PTR 外层、ndims＝0）走 `nd>0` 失败再 elem_kind＝−1 leftover 跳过 eek＝−1（dest extras dest-RET wrap rek3＝9 dest-stamps `**i32` 故 typed let `**[]i32` T001；dest-stamp via the local of typed dest 双端假绿；impl `**i32` vs trait `**[]i32` compile＝0）。存储＝elem＝PTR／eek＝leaf／ndims＝0／extra SLICE wrap 在未用槽 dims[0]（1＝`**[]T`；2＝`**[][]T`；0＝无 extra SLICE＝`**T`／`***T`；extra PTR 仍 dims[1]；两槽＝`**[]*T`）。判别 vs dest extras dest-RET extra empty `[]` PTR-elem ndims≥1 `**[2][]T`（extra SLICE 在 dims[ndims]）是 ndims＝0 vs ndims≥1；vs extra STAR PTR-elem ndims＝0 `***T`（extra PTR 在 dims[1]）是 extra SLICE vs extra PTR。消费＝leftover PTR vs eek＝PTR 非 T001（walk 先剥 leftover PTR 再 extra SLICE 再 extra PTR）；dest extras dest-RET wrap extra PTR of leaf extra times then extra SLICE then wrap PTR then wrap outer ptr（pin typeck 孪）。Assign-only：impl 返 `&self.p` of by-value self（dangling — 禁 INDEX；dest extras dest-RET PTR-to-ARRAY identity INDEX 是既有 emit leftover）。**修**：G.7 补既有 ret extra empty `[]` unused-slot 扫描器（PTR 元；PTR 外层；ndims＝0 extra SLICE 进 dims[0]）＋leftover eend＝0 extra SLICE peels＋dest extras dest-RET wrap rek3＝9 extra SLICE when ndims＝0。禁 −3／第二套 dest-PTR stamp／assemble parser.x／pipeline_abi mega。wrapper rdi＝data **未改**。证：**双端** `dyn_ret_ptr_ptr_slice` assign-only／host-C **7** · extra wrap COUNT＝2 assign-only **7** · leftover mismatch T001 · `-E` `struct xlang_slice_int32_t * * p` typed-cast＋wrapper `void* data` · ret_psap／psas／psp／pssp／pas／ppas／pap／ppap／pa／ppp／add_psap／add_psas／add／f32／call_ok／implicit／named／slice／arr2／add_ppas／asas／sas／apas／ret_ptr **7／7／7／7／7／7／7／7／7／7／7／7／7／7／7／7／7／7／11／7／7／7／6／7** · P013 · 假 clang／gcc **clang_hits=0** · hello size Ubuntu **27488** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED parse／typeck · LINUX gold。 |
| 2026-08-21 | **fix(parse／typeck)** **dest extras dest-RET extra STAR PTR-elem ndims＝0 `***T` dest-stamp** · tip **`6427e11dc`** | **代码** · **双端 L2 闭**。五问：产生＝extra STAR after `**`（elem＝PTR、PTR 外层、ndims＝0）走 `nd>0` 失败再 elem_kind＝−1 leftover 跳过 eek＝−1（dest extras dest-RET wrap rek3＝9 dest-stamps `**i32` 故 typed let `***i32` T001；dest-stamp via the local of typed dest 双端假绿；impl `**i32` vs trait `***i32` compile＝0）。存储＝elem＝PTR／eek＝leaf／ndims＝0／extra PTR wrap 在未用槽 dims[1]（1＝`***T`；2＝`****T`；0＝无 extra PTR＝`**T`；extra SLICE 仍 dims[0]＝`**[]T` 下叶）。判别 vs dest extras dest-RET extra STAR PTR-elem ndims≥1 `**[2]*T`（extra PTR 在 dims[ndims+1]）是 ndims＝0 vs ndims≥1；vs extra STAR SLICE-elem ndims＝0 `*[]*T`（extra PTR 在 dims[0]）是 PTR vs SLICE 元。消费＝leftover PTR vs eek＝PTR 非 T001（walk 先剥 leftover PTR 再 extra PTR）；dest extras dest-RET wrap extra PTR of leaf extra times then wrap PTR then wrap outer ptr（pin typeck 孪）。Assign-only：impl 返 `&self.p` of by-value self（dangling — 禁 INDEX；dest extras dest-RET PTR-to-ARRAY identity INDEX 是既有 emit leftover）。**修**：G.7 补既有 ret extra STAR unused-slot 扫描器（PTR 元；PTR 外层；ndims＝0 extra PTR 进 dims[1]）＋leftover eend＝0 extra PTR peels＋dest extras dest-RET wrap rek3＝9 extra PTR when ndims＝0。禁 −3／第二套 dest-PTR stamp／assemble parser.x／pipeline_abi mega。wrapper rdi＝data **未改**。证：**双端** `dyn_ret_ptr_ptr_ptr` assign-only／host-C **7** · extra wrap COUNT＝2 assign-only **7** · leftover mismatch T001 · `-E` `int32_t * * * p` typed-cast＋wrapper `void* data` · ret_psap／psas／psp／pssp／pas／ppas／pap／ppap／pa／add_psap／add_psas／add／f32／call_ok／implicit／named／slice／arr2／add_ppas／asas／sas／apas／ret_ptr **7／7／7／7／7／7／7／7／7／7／7／7／7／7／7／7／7／11／7／7／7／6／7** · P013 · 假 clang／gcc **clang_hits=0** · hello size Ubuntu **27488** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED parse／typeck · LINUX gold。 |
| 2026-08-21 | **fix(parse／typeck)** **dest extras dest-RET extra STAR SLICE-elem ndims＝−2 `*[][]*T` dest-stamp** · tip **`8d4e74d69`** | **代码** · **双端 L2 闭**。五问：产生＝extra empty `[]` after `*[]`（elem＝SLICE、PTR 外层、ndims＝0）走 `nd>0` 失败再 elem_kind＝−1 leftover 跳过 eek＝−1（dest extras dest-RET wrap rek3＝11 dest-stamps `*[]i32` 故 typed let `*[][]*i32` T001；dest-stamp via the local of typed dest 双端假绿；impl `*[][]i32` vs trait `*[][]*i32` compile＝0）。存储＝elem＝SLICE／eek＝leaf／ndims＝−2／extra SLICE wrap 在 dims[0]（0 表 1＝`*[][]T`；2＝`*[][][]T`）／extra PTR wrap 在未用槽 dims[1]（1＝`*[][]*T`；2＝`*[][]**T`；0＝无 extra PTR＝`*[][]T`）。判别 vs dest extras dest-RET extra STAR SLICE-elem ndims＝0 `*[]*T`（extra PTR 在 dims[0]）／ndims≥1 `*[][2]*T`（extra PTR 在 dims[ndims]）是 ndims＝−2 vs ndims＝0 vs ndims≥1。消费＝leftover PTR vs eek＝SLICE 非 T001（walk 先剥 leftover SLICE 再 extra SLICE 再 extra PTR）；dest extras dest-RET wrap extra PTR of leaf extra times then extra SLICE then wrap SLICE then wrap outer ptr（pin typeck 孪）。Assign-only：impl 返 `&self.p` of by-value self（dangling — 禁 INDEX；dest extras dest-RET PTR-to-ARRAY identity INDEX 是既有 emit leftover）。**修**：G.7 补既有 ret extra empty `[]` unused-slot 扫描器（SLICE 元；PTR 外层；ndims≤0 → ndims＝−2）＋ret extra STAR unused-slot 扫描器（ndims＝−2 extra PTR 进 dims[1]）＋leftover eend＝−2 extra SLICE then extra PTR peels＋dest extras dest-RET wrap rek3＝11 extra PTR／SLICE when ndims＝−2。禁 −3／第二套 dest-PTR stamp／assemble parser.x／pipeline_abi mega。wrapper rdi＝data **未改**。证：**双端** `dyn_ret_ptr_slice_slice_ptr` assign-only／host-C **7** · extra wrap COUNT＝2 assign-only **7** · leftover mismatch T001 · `-E` `struct xlang_slice_xlang_slice_int32_t_p * p` typed-cast＋wrapper `void* data` · ret_psap／psas／psp／pas／ppas／pap／ppap／pa／add_psap／add_psas／add／f32／call_ok／implicit／named／slice／arr2／add_ppas／asas／sas／apas／ret_ptr **7／7／7／7／7／7／7／7／7／7／7／7／7／7／7／11／7／7／7／6／7** · P013 · 假 clang／gcc **clang_hits=0** · hello size Ubuntu **27488** · 矩阵 **5/5**。钉盘仍 **`f7424ae47`**。PLATFORM: SHARED parse／typeck · LINUX gold。 |
