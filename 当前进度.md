# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **19** IDENTICAL（+ `labi_path_io`） |
| **diagnostic** | `-E`+cc ✅；**CORE_OK**（非 IDENTICAL / 非退役） |
| **W-string-nul** | ✅ STRING_LIT *u8 尾 `, 0`；slen≤64 |

## 本波：Track L `labi_path_io`（L3）✅

**事实**：path 探活 thin shell（null 门闩）；`stat` / `realpath` 主体在 mega rest `*_impl`（与 gates 同构）；seed 去掉 seed 内 OS body 以便 nm IDENTICAL。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/runtime/labi_path_io.x` | L3 thin 真迁 |
| `seeds/labi_path_io.from_x.c` | 冷启动同构 thin + U `_impl` |
| `seeds/runtime_link_abi.from_x.c` | `_impl` 常驻 rest；public under ifndef PATH_IO |
| `scripts/g05_ensure_relink_prereqs.sh` | L3 PREFER_X_O |
| `tests/prove_module_selfhost.sh` | 扩门禁 |

- 验收：`bash tests/prove_module_selfhost.sh` → **19 IDENTICAL** + diagnostic **CORE_OK**
- g05：`L3 path IO ← labi_path_io.x (Track L prefer .x)`

## Ubuntu 金标准（`5d1538e8`，2026-07-14 13:22 东八区，`date`）

| 项 | 结果 |
|----|------|
| `labi_path_io` | **IDENTICAL**（产品 `shux` @ worker tree / `self-hosting`） |
| 同批 8 叶 | path + gates + cc_list + ld_list + freestanding + std_list + ondemand + ensure **IDENTICAL** |

## 下一步

1. 下一 labi pure（`labi_host_lit` / `labi_diag_pure`）或 diagnostic hybrid→IDENTICAL  
2. W-global 数组；串池 >64；`labi_diag_pure` reportf  
3. 每波：**commit → push → Ubuntu prove → 再下一步**；§8 **东八区**（`date`，勿手填）
