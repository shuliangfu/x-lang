# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`a5e23ae7`**（NL-07 L8+：pure static si -o dual-depctx 关） |
| 产品 L4 放行 | 仍钉 **`5a636984`** 双端 L4+123（**未**升 tip 全擦 L4） |
| tip 双端 bstrict | ✅ **123/123 @ `36ac3d14`**（g05 + SKIP_BUILD；**非 L4**；本波未重跑全量） |
| **std.vec 产品 -o** | ✅ **双端** formal `vec.o` + on-demand · `with_arena_vec_push` |
| **NL-07 硬绿 L1–L6** | ✅ multi=0 · fflush · enc/emit · typeck/driver/lsp · parser/codegen · libc 面 · **crt0 link OK** · unique=0 |
| **NL-07 运行时 L7** | ✅ freestanding `%g`/`%e`；✅ crt0 真 `preprocess_x.o`；✅ crt0 `user_asm_seed_bridge`；✅ **postlink smoke 无 fallback 稳定绿**；✅ 纯 static f64 非 `g.0` |
| **NL-07 L7+ import typeck** | ✅ keep-global parser import APIs → pure static **XT001 关** |
| **NL-07 L8 hello -o** | ✅ pure static **hello -o 绿** run=0 · `Hello World` · C≈164k co-emit std.io；rv=42 · opt=102 |
| **NL-07 L8+ si -o** | ✅ pure static **si -o 绿** run=0 · depctx 单表 · types/result 前缀对齐 |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · NL-07 L8+ pure static si -o dual-depctx）

| 波 | 结果 |
|----|------|
| **入口** | L8 hello 已绿；pure static **si -o** host-cc 红（`core_types_*` 体调 `core_result_*` 隐式声明 / 前缀互换） |
| **根因地图** | ① `72110054` 将 `g_depctx_sc` 改为 weak `g_shux_depctx_sc`（消 per-TU static），仍 residual。② **真根**：`ld_partial_export` 的 Linux `objcopy --keep-global-symbols` / Darwin `-exported_symbols_list` 把 **未列入导出的** `g_shux_depctx_sc` **局部化** → crt0 L5 pipeline partial 内 static `depctx_sidecar_get` 绑私有 BSS；pipeline_x/glue 另一张表 → Cap `module_at` 与 path 分叉 → types/result 前缀互换。 |
| **证据** | 修前 `nm`：`b` + `V` 双 `g_shux_depctx_sc`；两 `depctx_sidecar_get` 分别 LEA 到 `0x91998e0` / `0x6cf4c0`。修后仅 **`V` 单表**；两 get 均 LEA `0x6cf4c0`。 |
| **根修** | **`72110054`** weak process-wide 表；**`a5e23ae7`** `ld_partial_export` 注入 keep `g_shux_depctx_sc`（`build_shux_asm.sh` + `relink_shux_asm_strict_glue.sh`） |
| **验收 Ubuntu pure static** | crt0 OK · smoke OK · **rv=42 · opt · hello · si -o/run 全绿** · 单 `V g_shux_depctx_sc` |
| **双端 g05** | mac 产品矩阵 rv/opt/hello/si **绿** |
| **日志** | Ubuntu rebuild `/tmp/ubuntu_si_depctx_rebuild_a5e23ae7.log` |

### 五问（NL-07 L8+ si -o）

| 问 | 答 |
|----|-----|
| 谁产生 | partial export 局部化 process-wide depctx 表 → dual embed 双 BSS |
| 谁存储 | `g_shux_depctx_sc`（须 GLOBAL weak 合并） |
| 谁消费 | static `depctx_sidecar_get` → `module_at` / path_copy → codegen 前缀 |
| 同模式 | 任何 `ld -r` + keep-global 抽 pipeline 且嵌 depctx 的 partial |
| 最小回归 | pure static rv/opt/hello/**si**；mac 产品矩阵 |

### residual 下一波

1. 用户明示时 tip **L4 真冷** + 双端 bstrict 123  
2. 默认 `SHUX_BOOTSTRAP_NOSTDLIB=1` / 去系统 pthread（策略文）  
3. 其它 pure static 边角（非 si 主矩阵）按需  

```text
构建级别：日常 L2/L3（NL-07 L8+ si -o；非 tip L4 全擦）
产品 tip L4 钉盘：5a636984
tip HEAD：a5e23ae7
NL-07 L1–L8 保持；L8+：pure static si -o 绿 · 产品矩阵四件套绿
双端产品矩阵：Ubuntu pure static + mac g05 rv=42 / opt / hello / si 绿
```
