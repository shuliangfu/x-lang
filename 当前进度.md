# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`4d1a3243`**（fs vec overload mid + nostdlib heap face） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm hello / hosted vec** | ✅ **Ubuntu L2** |
| **freestanding page_mmap** | ✅ **NL-03 gate OK** |
| **fs r42 / fs hello** | ✅ |
| **fs vec len_empty -o** | ✅ **Ubuntu L2**（bare mid + malloc face 关） |
| **fs vec push** | ❌ **SIGSEGV residual**（typeck XT001 on `new` / freestanding co-emit） |

## 本波摘要（2026-07-18 · fs vec mid + nostdlib face）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 freestanding std.vec / std.heap.libc |
| **地图** | fs co-emit：T `std_vec_reserve_Vec_*` vs U bare `std_vec_reserve`；user.o U malloc/free/…；`len_empty` 可链但 push 深债 |
| **根因** | ① call-export dep 路径只用 bare field mid（非 import-binding formal/score）；② freestanding ld 未链 mmap-backed malloc 面（stubs 已有权威） |
| **根修** | G.7 补全：`glue_asm_build_call_export_sym_c` formal/score mid；`link_abi_user_o_needs_freestanding_nostdlib_face` + ensure/push `bootstrap_nostdlib_stubs` |
| **验收** | Ubuntu L2：nolibc-heap · fs r42/hello · hosted rv/hello/vec · **fs len_empty -o 绿**；fs push SIGSEGV；**日常 L2 · 非 tip L4** |

### residual 下一波

1. freestanding **std.vec push 运行时**（typeck `new` / `heap_mem_set_c` XT001 仅 fs co-emit；勿用 len_empty 当 push 绿）  
2. （可选）f64 let-init binop residual  
3. （旁）typeck cfg-extern；ELF `.bss`；labi `need_vec` 补 `len_empty`  

```text
构建级别：L2（backend_call_dispatch + runtime_link_abi + g05；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：nolibc-heap / fs r42 / fs hello / hosted 矩阵 / fs len_empty 绿 · fs push residual
纪律：测试 hard timeout；本波未升 tip L4
feat SHA：4d1a3243
```
