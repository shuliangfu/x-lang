# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `964e8b51`（FAIL `lsp_io_std_heap` 收口） |
| 产品 | 双端 bstrict **123** @ `233e3b72`（L4 历史；本波工程未重跑 L4） |
| **T** | **10/18**（mega；未重跑） |
| **N** | **54/54 IDENTICAL**（Ubuntu 单叶累计记账；相对 `70666dcb` 全表 35 **+19**） |
| **R2 / H** | 仍冻结（N 已满；抽检/全表 re-baseline 另开） |
| **Track L SYM** | **0** |
| **FAIL residual** | **0** |

## 本波摘要

| 波 | SHA | 结果 |
|----|-----|------|
| FAIL try_inline | `277afa40` | hoist extern + g02f no_mangle |
| FAIL call_dispatch | `09e8ea4b` | hoist extern + LE no_mangle |
| **FAIL lsp_io_std_heap** | **`964e8b51`** | seed pin 路径 + 删 libc 冲突 extern；**mac+Ubuntu IDENTICAL** |

**产生点（lsp_io_std_heap）**：
1. prove 指向工作区 **gitignore** 的 `lsp_io_std_heap_gen.c`（干净 Ubuntu 树不存在 → seed cc FAIL）  
2. 入库 pin `seeds/lsp_io_std_heap_gen.linux.x86_64.c` 在 `#include <stdlib.h>` 后 **再声明** `uint8_t *malloc/free/calloc` → glibc `void*` 冲突类型  

**权威**：`compiler/seeds/lsp_io_std_heap_gen.linux.x86_64.c` + `tests/prove_module_selfhost.sh` seed 路径。  
**构建级别**：日常真测 L2/L3 单叶 prove（**非** L4）。

日志：mac `/tmp/mac_prove_lsp_io_std_heap.log` · Ubuntu `/tmp/ubuntu_prove_lsp_io_std_heap_964e8b51.log`

## 下一波前排

1. **N 满后**：可选全表 prove re-baseline 落盘（确认 54/54 非仅单叶累计）  
2. **T / Track M** 或 **R2 抽检**（N 已满可解冻节奏）  
3. 谈 Stage2 / 自举叙事前：**L4 真冷 + 双端 bstrict**（本波未做）  
