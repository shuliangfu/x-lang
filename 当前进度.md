# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` tip **`44e0703c`**（产品 L4 放行仍钉 **`96f1453c`**） |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `96f1453c`**（16 号 driver_abi Cap pure wave1–6 收口；详见归档） |
| **D Stage2** | ✅ Ubuntu freestanding D-03 @ `c3031883`（**≠** 产品链；勿把 `shux_asm2` 当产品） |
| **E R2** | ✅ diagnostic rest T **0**；driver_abi Cap rest `_impl` **6**（OS 真锁）；**fmt_check Cap residual pure** ALWAYS residual **9→8** @ `44e0703c`（lib path slots + try_append；日常 L2 双端） |
| **T / EMPTY / N** | **18/18** · **18/18** · **54/54** |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **fmt_check Cap residual pure** `44e0703c` | **lib path slots + full try_append 真迁 thin.x**（`g_fmt_check_lib_bufs` 8×512 + pure `fmt_check_lib_buf_at`/`store` + full `check_try_append_lib_root` via public `path_stat` + G.7 `shux_ptr_slot_set`）；FROM_X 无 pure-dup `try_append_impl`；Cap residual file_list ptrs / walk / store / clear / argv_append / one_file_body / run_fmt / run_check 仍 rest；**ALWAYS residual 9→8**；surface IDENTICAL；**.x 英文 G.9**；**日常真测 L2/L3**（**非 tip L4**） |

**构建级别（本波产品改动）**：**日常真测 L2/L3**（产品放行 tip 仍为 L4@`96f1453c`）。

| 端 | prove fmt_check | g05 hybrid | 矩阵 | 构建级别 | 日志 |
|----|-----------------|------------|------|----------|------|
| macOS | IDENTICAL | OK | rv=42 / opt=102 / hello=0 / check=0 / fmt=0 | L2/L3 | prove `/tmp/prove_fmt_try_append.log` · g05 `/tmp/g05_fmt_try_append.log` · matrix `/tmp/mac_matrix_fmt_try_append.log` |
| Ubuntu | IDENTICAL | OK | rv=42 / opt=102 / hello=0 / check=0 / fmt=0 | L2/L3 | prove `/tmp/ubuntu_prove_fmt_try_append.log` · g05 `/tmp/ubuntu_g05_fmt_try_append.log` · matrix `/tmp/ubuntu_matrix_fmt_try_append.log` |

## 产品放行 tip（沿用 · 勿重写为 17 日 L4）

| 端 | bstrict | 日志（@ `96f1453c`） |
|----|---------|----------------------|
| **Ubuntu** | **123/123** | cold `/tmp/ubuntu_true_cold_96f1453c.log` · bstrict `/tmp/ubuntu_true_bstrict_96f1453c.log` |
| **macOS** | **123/123** | cold `/tmp/mac_true_cold_96f1453c.log` · bstrict `/tmp/mac_true_bstrict_96f1453c.log` |

```text
构建级别：L4 真冷（双端；16 号结案）
SHA：96f1453c
SHUX：产品 g05 shux_asm（禁 stage1 / 禁 freestanding asm2）
```

## 下一波前排

1. **E 真迁续**：fmt_check Cap residual（walk opendir / path_stat / one_file_body / store+clear / argv_append / run_fmt / run_check · ALWAYS residual **8**）或 driver_abi OS 真锁（uname/setrlimit/pthread/path-read/gettimeofday/call_fn · 6）或 Stage2 WPO  
2. **禁止**把单叶 IDENTICAL / Stage2 / 抽检写成完全自举（产品放行 = tip 双端 L4@**`96f1453c`** 直至新 tip L4）  
3. 动 SHARED 产品面 → 新 SHA 再双端 L4（勿沿用旧 tip 日志）  
