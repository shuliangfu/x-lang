# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **（本波 commit 后）** link 补全 vec→heap（产品 L4 钉盘仍 **`abfcf576`**） |
| 产品 L4 放行 | ✅ **钉盘 `abfcf576`** 双端 L4 真冷 + bstrict 123（前钉 `723d5d97`） |
| tip 双端 bstrict | ✅ **123/123 @ `abfcf576`**（L4 全擦；本波 **非** tip L4 升钉） |
| **std.vec 产品 -o** | ✅ formal `vec.o` + **need_vec 同推 heap/mem**（补全 set/map 模式） |
| **std.math 产品 -o** | ✅ formal `math.o`（`shux_compile_std_module --bare-impl`）· `tests/math/main.x` run=0 |
| **NL-07 L1–L10 / v5** | ✅ 见上波；产品 g05 nostdlib |
| **freestanding execvp + %.\*s** | ✅ `2db64465` / `abfcf576` |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · pure static 边角 · vec→heap 补全）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」：L4 钉盘后前排 **pure static 边角** |
| **地图** | L4 后 `std/vec/vec.o` 缺失 → formal 编译后仍 UNDEF `std_heap_*`；根因 = **need_vec 只推 vec.o**，用户 C 无 `std_heap_*` use_line，**未像 set/map 同推 heap/mem**；`needs_std_heap_api` 探针缺 `default_alloc`/`kind_arena`/Allocator 族 |
| **根修** | G.7 权威 `runtime_link_abi.from_x.c`（+ `.x` 探针同向）：need_vec 成功推 vec 时推 heap+core_mem；补全 heap API 探针 |
| **验收** | **日常 L2/L3**（非 tip L4）：mac with_arena run=0 · rv=42 · hello；Ubuntu formal vec/heap/math + g05 后同验 |
| **日志** | mac `/tmp/mac_vec_heap_link_*.log` · `/tmp/mac_g05_vec_heap.log` · `/tmp/mac_wav.log` · Ubuntu `/tmp/ubuntu_formal_std_*.log` · edge `/tmp/ubuntu_pure_edge3_*.log` |

### residual 下一波

1. L4 真冷后 **formal std/*.o** 缺失/陈旧（需 compile 或 ensure；非仅 link 门）  
2. freestanding 数学桩 `erfc` 等无限递归 warning 按需（非门禁红）  
3. 工程轨 residual（非本波）  

```text
构建级别：L2/L3 日常（g05 + formal std 探针；非 tip L4 升钉）
产品 tip L4 钉盘：仍 abfcf576
本波：need_vec 补全 heap/mem + heap API 探针
mac：with_arena run=0 · /tmp/mac_wav.log
叙事：pure static 边角 — formal vec 产品链根修（link 层）
```
