# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`4b10fb1d`**（feat 链：`7c707ba7` labi formal + fs vehicle + env rename；L4 钉盘仍 **`f16f7d48`**） |
| 产品 L4 放行 | ✅ **钉盘 `f16f7d48`**（双端真冷 L4+123） |
| tip 双端 bstrict | ✅ **123/123 @ `f16f7d48`**（本波 **非** tip L4 升钉） |
| **asm env/fs BLD001** | ✅ **`4b10fb1d`**（env/fs asm run=0） |
| **asm sort(*u8) overload** | ✅ **`a2a09ea7`** |
| **asm sort link** | ✅ **`b96778ef`** |
| **asm set/map/queue** | ✅ **`28b934e9`** |
| **asm formal ensure** | ✅ **`ffd083cc`** |

## 本波摘要（2026-07-18 · asm fs/env BLD001 residual）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 asm fs/env BLD001（钉盘 `f16f7d48` 不动） |
| **地图** | C 路径 env/fs run=0（co-emit mod）；asm U `std_env_*` / `std_fs_invalid`。env.o 旧权威=env.x-only（无产品面）；fs 无 formal 且 asm 跳过 std.fs co-emit |
| **根因** | 同 sort：fk0 无探针 + formal 符号面错；fs 另：mod.x 作入口 host-cc 撞 libc；Ubuntu 裸 `getenv` 被 rename 成 `*_api` |
| **根修** | ① Makefile env.o=mod.x；fs formal vehicle KEEP_C（`shux_compile_std_fs_formal.sh`）；② labi fk0 `std_env_*`/`std_fs_*` + plan fs.o；③ compile 脚本产品名 `std_env_getenv` 非 `*_api`（**`7c707ba7`…`4b10fb1d`**） |
| **验收** | **日常 L2**：Ubuntu **env/fs asm run=0** · C env/fs · 矩阵 rv=42 opt/si/hello · sort/vec asm=0；mac formal+asm env/fs=0；**非 tip L4** |
| **钉盘** | 仍 **`f16f7d48`** |

### residual 下一波

1. （旁）slice asm SIGABRT · 常量折叠 IR/WPO  
2. （可选）tip L4 升钉含 **`4b10fb1d`** / **`a2a09ea7`** / **`b96778ef`**  
3. （旁）更重 fs 用例 asm（vehicle 已带 full co-emit；按需扩探针）  

```text
构建级别：L2（runtime_link_abi.o + g05 + formal env.o/fs.o；非 tip L4）
产品 tip L4 钉盘：f16f7d48（本波未升）
feat 链：7c707ba7 → 4db89d8e → 22966801 → 4b10fb1d
Ubuntu：env/fs asm run=0 · C 矩阵 rv=42 opt/si/hello · sort/vec=0
macOS：formal rebuild + env/fs asm=0
权威：labi_std_fk0_user_needs_rel · labi_std_list · shux_compile_std_fs_formal.sh
      · shux_compile_std_module.sh（env 产品名 rename）
纪律：禁止日常 L2 冒充 tip L4；一条债一层；fs vehicle 非第二 API 面
```

---

## 前波摘要（2026-07-18 · asm sort(*u8) overload residual）

| 波 | 结果 |
|----|------|
| **根修** | `backend_call_dispatch` INDEX/AS + re-score score≥11（**`a2a09ea7`**） |
| **验收** | full sort asm run=0 · i32+u8 双 call |

---
