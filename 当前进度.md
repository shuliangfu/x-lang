# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting`（本波 **codegen M1** 根修待合入 tip） |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **D Stage2** | ✅ Ubuntu freestanding D-03 @ `c3031883`（**≠** 产品链） |
| **E R2** | ✅ diagnostic / driver_abi / fmt_check Cap residual pure **收口** |
| **Stage2 WPO full-chain** | ✅ @ **`57665c75`** proxy **5.81%** |
| **M typeck** | ✅ **M1** + ✅ **M2 smoke** @ `256acd3b`（seed pin 未换） |
| **M codegen** | ✅ **Cap-T001** + ✅ **M1**（`-E` 全量绿）— residual 见下 |
| **T / EMPTY / N** | 记账仍 18/18 · 18/18 · 54/54 |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **codegen M1 根修（工程轨）** | 现象：Cap-T001 后 `collect parse dep=ast pr_ok=-2`，`parse_set_main` 仅 `num_funcs=5` → XT001 `#114`（`ast.ref_is_null` 未入 dep）。**五问**：产生=ast_pool OneFunc 侧车耗尽；存储=`g_onefunc_sc[MAX]`；消费=dep parse `onefunc_sidecar_get(create=1)` 失败→函数 skip。探针：codegen 路径 peak **onefunc≈385**，typeck 父仅 **≈206**（&lt;256）。**根修**：`MAX_ONEFUNC_SIDECARS` **256→1024**（`compiler/ast_pool.c`）。重建产品链：`pipeline_x.o` → `bootstrap_seed_pipeline_filtered.o` → g05。**M1 绿**：`codegen.x` `-E` out≈330KiB，`dep=ast` `pr_ok=0` `num_funcs=231`。mac 矩阵 rv/opt/hello/si 绿。**seed pin / M2 / tip L4 未动**。Follow-up：onefunc 泄漏降峰（impl_snap 未 release）。 |

### 已合入 / 本波

| SHA | 摘要 |
|-----|------|
| （本波） | **codegen M1**：`MAX_ONEFUNC_SIDECARS` 256→1024 |
| `106a6b73` | **codegen Cap-T001** whole-body unsafe ×64 |
| `256acd3b` | **typeck Cap-T001** whole-body unsafe（M1/M2 smoke） |

### mac 探针（本波）

| 项 | 状态 | 日志 / 备注 |
|----|------|-------------|
| codegen M1 `-E` | ✅ | out `/tmp/cg_m1.out` · pipe `/tmp/cg_m1.err` · `num_funcs=231` dep |
| 产品矩阵 | ✅ rv=42 · opt · hello(`0a`) · si | seed pin 未换 |
| 产品 tip L4 | 仍 **`5a636984`** | **非 tip L4** |

### 已知 residual

| 项 | 说明 |
|----|------|
| codegen M2 / seed re-pin | M1 已绿；M2 smoke / pin = 产品面 → 双端 L4 |
| onefunc 泄漏 | peak 385≫函数数；`parse_one_function_impl` 的 `impl_snap` 等未 `release`（容量已够，宜另波降峰） |
| parser M1 | XT001 `parser_rewind_lex_for_lparen_control_stmt` |
| 产品 tip L4 | 仍 **`5a636984`** |

## 下一波前排

1. **codegen M2 smoke**（本地 `codegen_x.o` 替换）/ seed re-pin（产品面 → 双端 L4）  
2. parser XT001 rewind / typeck seed re-pin  
3. **禁止**把 Cap-T001 / M1 / M2 smoke 写成 tip 产品 L4  

```text
构建级别：工程 L2（codegen M1 根修；非 tip L4）
产品 tip L4：5a636984
codegen M1：MAX_ONEFUNC 1024 · -E 绿 · dep=ast 231
产品矩阵：mac rv/opt/hello/si 绿（seed pin 未换）
```
