# Shux 当前进度（2026-07-12）

## 总体状态

| 指标 | 状态 |
|------|------|
| 基线 | G-02f-479（commit `50e704dc`）→ 自举修复链 `214b60ac` |
| PREFER_X_O | 50/50（44 rt + 6 frontend） |
| G05 objs | 51 |
| Ubuntu rv42 | 42 ✅ |
| P0 hang 修复 | ✅（commit `cf56e682`） |
| #[cfg] AND 修复 | ✅（commit `a1aecab9`） |
| -E 0-import 文件 | 18/18 通过 ✅ |
| §9.1 codegen semantic debt gate | 5/5 Ubuntu 全绿 ✅ |
| UB / i64-ctfe / lang-unsafe gate | 全绿 ✅ |
| F-no-libc（G-03） | ✅ |
| §0.1.1 步骤 3 cc -c 类型前缀 | ✅ 0 error（Ubuntu 验证通过） |
| §0.1.1 步骤 5 build_seed_asm_host | ✅ Ubuntu 复跑通过 |

---

## §0.1.1 前排 5 步 — 全部完成 ✅

| # | 步骤 | 状态 | 说明 |
|---|------|------|------|
| 1 | peephole.x `slice_eq` typeck 收口 | ✅ | commit `50e704dc` |
| 2 | asm_seed_full.x `-E` 复跑 | ✅ | Ubuntu `-E` rc=0 |
| 3 | `cc -c` 类型前缀 | ✅ | **0 error**（从 244 降到 0，见下） |
| 4 | `prove_x_o.sh` / `gen_tu_contract.sh` / `x_bootstrap_safe_lint.sh` | ✅ | 13 个 `*_thin.x` 试点闭合 |
| 5 | Ubuntu 复跑 `build_seed_asm_host.sh` | ✅ | `info: build_seed_asm_host: OK` |

---

## §0.1.1 第 3 步修复历程：244 → 0 error

从 `asm_seed_full.x -E + cc -c` 的 244 个 error 逐层修复到 0：

| 修复 | 消除 | 根因 |
|------|------|------|
| codegen seed 8 项同步 | 244→177 | emit_type 优先级反转、裸名提取、forward decl、dep_enum_prefix、u16/i16/i8、限定名兜底 |
| `#include <sys/types.h>` | 177→83 | ssize_t 未声明 |
| emit-ALL dep types（移除两阶段 phase） | 83→45 | dep 类型定义在使用之后 |
| `arch.arm64`→`arm64` + 缺失 import | 45→32 | binding 名错误 + pipeline 未加载 dep |
| 前缀冗余检查通用化 | 32→27 | `ast_` + `ast_arena_expr_get` 双前缀 |
| C 风格参数声明修正 | 27→27 | parser 无法解析 C 语法参数 |
| typeck dep module enum 搜索 | 27→21 | ExprKind 不在当前模块 enum 中 |
| codegen-time enum variant marking | 21→10 | typeck 未标记 → codegen 运行时标记 |
| `let` hoist from if-blocks | 10→9 | parser 不注册 if 内 let |
| `field_access_base_is_pointer_param` | 9→2 | dep module 指针字段 `.`→`->` |
| 同模块函数前向声明 | 2→1 | C 需要前向声明 |
| `o < ExprKind.X` 歧义 | 1→1 | `<` 被误解析为泛型 |
| `MODULE_ENUM_MAX_VARIANTS` 32→64 | 1→0 | ExprKind 57 变体超限 |

---

## 下一步

§0.1.1 全部完成。进入自举下一阶段：

1. **更新 `analysis/自举进度.md`** §0.1.1 全部 ✅
2. **扩大 `prove_x_o.sh` 试点范围**（从 13 个 thin.x 扩展到更多 TU）
3. **推进 Phase 1**：为已有 PREFER_X_O 路径补合同 + ABI snapshot
4. **考虑提交 `seeds/bootstrap_shuxc.linux.x86_64`** 更新（包含全部修复的新 seed）

---

## 环境信息

| 系统 | 连接方式 | 项目目录 |
|------|---------|---------|
| macOS | 本机 | /Users/shuliangfu/worker/shu/shux |
| Ubuntu | `ssh ubuntu-server` | /home/shuliangfu/worker/shu/shux |
| Windows | `ssh windows-server` | C:\Users\shuliangfu\worker\shu\shux |

## 关键约束

- macOS 开发 → git push → `ssh ubuntu-server` → Linux 真机验证
- Linux x86_64 为金标准
- `codegen_gen.linux.x86_64.c` 等 pinned seed 需手工同步 `.x` 改动
- `shux_compile_std_module.sh` 需 `-x -E` 支持；旧 seed 不支持 `-x`，需先从 `-E` 回退构建新 seed
- 修复必须遵循**根源治理**：Bug 回溯到 IR 发射器 / 前端 typeck / 内存分配器，禁止热路径打补丁
