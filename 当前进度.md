# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `213b6352`（M2 EMPTY **17/18**） |
| 产品 | 双端 bstrict **123** @ `233e3b72`（L4 历史；本波未重跑 L4） |
| **T** | **18/18** typeck |
| **EMPTY** | **17/18**（+ mega parser/typeck/codegen surface） |
| **N** | **54/54 IDENTICAL**（全表 @ `11f3ad55`） |
| **R2 / H** | 可解冻抽检 |
| **Track L SYM** | **0** |
| **FAIL residual** | **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **M2 EMPTY mega** | harness：`parser`/`typeck`/`codegen` ↔ `seeds/*_empty_surface.from_x.c`（plain `-E`）；产品 `*_gen.linux.x86_64.c` 不动 |
| **根因** | 产品 pin = 全量 single-file+deps 冷启动；plain `-E` 为 EMPTY 轨 surface（同 lsp*/diagnostic thin 双权威） |
| **残差 1** | **lexer CODEGEN**（entry emission XP003；无 `-E` 出 C） |

**构建级别**：日常真测 L2/L3（**非** L4）。  
日志：mac `/tmp/mac_diff_empty_mega.log` · Ubuntu `/tmp/ubuntu_diff_t_213b6352.log`

## 下一波前排

1. **EMPTY 最后 1**：lexer CODEGEN（根修 emit / 或产品 pin 策略）  
2. **L4 + 双端 bstrict**（谈 Stage2 前强制）  
3. **禁止**用 N/T/EMPTY 单独宣称「可自举」  
