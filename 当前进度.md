# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `g-02f-self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **7** driver 叶 IDENTICAL |
| **diagnostic** | `-E`+cc ✅；**CORE_OK**（非 IDENTICAL / 非退役） |
| **W-unsafe-expr（std 源）** | ✅ 批量 let+stmt 规避 |

## 本波：diagnostic CORE prove + W-unsafe-expr ✅

### Track L — diagnostic prove 策略

**事实**：seed 是 hybrid thin + C-tail 手抛光；`.x -E` 多 `driver_diag_append_*`，seed 多 `_impl`/scratch → 永远非全量 IDENTICAL，直到消双权威。

**落地**（`tests/prove_module_selfhost.sh`）：

| 模式 | 判据 | 计入 KPI N？ |
|------|------|-------------|
| （默认） | 已定义符号 **IDENTICAL** | ✅ |
| `core:allow…` | x 业务符号 ⊆ seed（滤 `l_*` 局部常量；allow 列 x 独有辅助） | ❌（中间门禁） |

- 模块：`diagnostic|…|core:driver_diag_append_cstr,driver_diag_append_i32,driver_diag_append_name`
- 验收：`bash tests/prove_module_selfhost.sh diagnostic` → **CORE_OK**（seed+23 x_allow+3）
- 七叶仍 IDENTICAL：`fmt check test build run compile emit`

### Track W — W-unsafe-expr（std 源改）

**根因备注**：codegen 把**表达式位置**的 `unsafe { call() }` 落成带 return 的 statement-expression → C 侧 void 与 int 比较。  
**本波策略**：与 `tcp.x` 一致 — `let rc; unsafe { rc = call(); } if (rc …)`。  
**覆盖**：`std/security`、`regex`、`schema`、`net/tls_*`、`ws_io`、`socketio/*`、`fs/win32` 等；`if (unsafe {` 在 std 仅余注释。

**未做（可选下一波）**：codegen 根修表达式 unsafe（一劳永逸）；diagnostic hybrid 消 C-tail → IDENTICAL。

## 下一步

1. diagnostic hybrid 收敛（thin/rest 单一权威）或接受 CORE 长期门禁  
2. 整理 multi-wave commit（parser/typeck pin + prove + W-unsafe std）  
3. Ubuntu 验证 pin + prove  
