# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **16** IDENTICAL（+ `labi_std_list`） |
| **diagnostic** | `-E`+cc ✅；**CORE_OK**（非 IDENTICAL / 非退役） |
| **W-string-nul** | ✅ STRING_LIT *u8 尾 `, 0`；slen≤64 |

## 本波：Track L `labi_std_list`（L8）✅

**事实**：L5–L7 同构可复用。58 步 `(op, rel, flag_kind)` 用 if/else+`let p: *u8`；`plan_step_at` 的 C `int*` / `const char**` 在 .x 用 `*i32` + `*usize`；seed 去掉 static 表以便全局符号 IDENTICAL。`default_std_rel_*` 为 OP_STD=1 子集 41 条。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/runtime/labi_std_list.x` | L8 真迁 |
| `seeds/labi_std_list.from_x.c` | 冷启动同构 if/else |
| `scripts/g05_ensure_relink_prereqs.sh` | L8 PREFER_X_O |
| `tests/prove_module_selfhost.sh` | 扩门禁 |

- 验收：`bash tests/prove_module_selfhost.sh` → **16 IDENTICAL** + diagnostic **CORE_OK**

## Ubuntu 金标准（`4e5a69e6`，2026-07-14 13:14 东八区）

| 项 | 结果 |
|----|------|
| `labi_std_list` | **IDENTICAL**（产品 `shux` @ worker tree / `self-hosting`） |
| 同批 10 叶 | fmt…run + path + gates + cc_list + ld_list + freestanding **IDENTICAL** |

## 下一步

1. 下一 labi list pure（`labi_ondemand_list` L8b）或 diagnostic hybrid→IDENTICAL  
2. W-global 数组；串池 >64；`labi_diag_pure` reportf  
3. 每波：**commit → push → Ubuntu prove → 再下一步**；§8 **东八区**
