# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `g-02f-self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **14** IDENTICAL（+ `labi_invoke_ld_list`） |
| **diagnostic** | `-E`+cc ✅；**CORE_OK**（非 IDENTICAL / 非退役） |
| **W-string-nul** | ✅ STRING_LIT *u8 尾 `, 0`；slen≤64 |

## 本波：Track L `labi_invoke_ld_list`（L6）✅

**事实**：L5 同构模式可复用。L6 brew/compress/tail 短字面量用 if/else+`let p: *u8`；seed 去掉 static 表以便全局符号 IDENTICAL。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/runtime/labi_invoke_ld_list.x` | L6 真迁 |
| `seeds/labi_invoke_ld_list.from_x.c` | 冷启动同构 if/else |
| `scripts/g05_ensure_relink_prereqs.sh` | L6 PREFER_X_O |
| `tests/prove_module_selfhost.sh` | 扩门禁 |

- 验收：`bash tests/prove_module_selfhost.sh` → **14 IDENTICAL** + diagnostic **CORE_OK**

## 下一步

1. 下一 labi list pure（std_list / freestanding / ondemand）或 diagnostic hybrid→IDENTICAL  
2. W-global 数组；串池 >64；`labi_diag_pure` reportf  
3. 每波：**commit → push → Ubuntu prove → 再下一步**；§8 **东八区**
