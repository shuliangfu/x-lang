# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`a2a09ea7`**（feat：asm import-binding 重载 re-score + INDEX/AS 实参类型；L4 钉盘仍 **`f16f7d48`**） |
| 产品 L4 放行 | ✅ **钉盘 `f16f7d48`**（双端真冷 L4+123；前钉 `5c4dea78`） |
| tip 双端 bstrict | ✅ **123/123 @ `f16f7d48`**（本波 **非** tip L4 升钉） |
| **asm sort(*u8) overload** | ✅ **`a2a09ea7`**（full sort asm run=0；i32+u8 双 call） |
| **asm sort link (fk0+formal)** | ✅ **`b96778ef`** |
| **asm set/map/queue on_demand** | ✅ **`28b934e9`** |
| **asm formal std ensure** | ✅ **`ffd083cc`** |

## 本波摘要（2026-07-18 · asm sort(*u8) overload residual）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 asm `sort.sort(*u8)` 错绑 i32（钉盘 `f16f7d48` 不动） |
| **地图** | C 全量 sort run=0（`std_sort_sort_u8_ptr_i32`）；asm 两次 call 皆 `*_i32_*` → run=134。探针：`let p:*u8` **asm 绿**；`&b[0]` / `(&b[0]) as *u8` **asm 红**。层=asm import-binding mangle，非 labi |
| **根因** | `glue_asm_mangle_import_binding_call_sym_c` 盲信 `call_resolved_func_index`（首同名 i32 residual）；C 路径 suffix 再评分；asm score 缺 INDEX/AS 恢复时 arg_ty=0 无法推翻 |
| **根修** | `backend_call_dispatch.from_x.c`：① `glue_asm_call_arg_type_ref_c` 补 INDEX 元素 / AS target / ADDR_OF(*T)；② overload 时 re-score，**仅 score≥11**（有类型证据）覆盖 r_func（**`a2a09ea7`**） |
| **验收** | **日常 L2**：Ubuntu full sort **asm run=0** · objdump i32+u8 双 call · idx/ptr/as 探针 · 矩阵 rv=42 opt/si/hello · set/map/queue/vec/i32 asm=0；mac g05+矩阵+sort C；**非 tip L4** |
| **钉盘** | 仍 **`f16f7d48`** |

### residual 下一波

1. （旁）asm **fs/env** BLD001（formal surface / co-emit 对齐 sort 模式）  
2. （旁）slice asm SIGABRT · 常量折叠 IR/WPO  
3. （可选）tip L4 升钉含 **`a2a09ea7`** / **`b96778ef`**  

```text
构建级别：L2（backend_call_dispatch.o + g05；非 tip L4）
产品 tip L4 钉盘：f16f7d48（本波未升）
feat：a2a09ea7
Ubuntu：full sort asm run=0 · i32+u8 call · 矩阵 rv=42 opt/si/hello · set/map/queue/vec=0
macOS：g05 + 矩阵四件套 + sort C=0
权威：glue_asm_mangle_import_binding_call_sym_c · glue_asm_call_arg_type_ref_c
      · glue_asm_score_import_binding_func_ix_ex_c（score≥11 才覆盖 r_func）
纪律：禁止日常 L2 冒充 tip L4；改 call_dispatch 须 .o + g05；一条债一层
```

---

## 前波摘要（2026-07-18 · asm sort link residual）

| 波 | 结果 |
|----|------|
| **根修** | Makefile `sort.o`=mod.x+sort.x；fk0 `std_sort_*`；g8 core.debug（**`b96778ef`**） |
| **验收** | link=0 · i32+assert asm=0；**full asm u8 residual** 本波已关 |

---
