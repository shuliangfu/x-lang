# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **D Stage2** | ✅ Ubuntu freestanding D-03 @ `c3031883`（**≠** 产品链） |
| **E R2** | ✅ diagnostic / driver_abi / fmt_check Cap residual pure **收口** |
| **Stage2 WPO full-chain** | ✅ @ **`57665c75`** proxy **5.81%** |
| **M typeck** | ✅ **M1** + ✅ **M2 smoke** @ `256acd3b`（seed pin 未换） |
| **M codegen** | ✅ Cap+M1；✅ **M2 smoke mac**（force-regen 链上矩阵绿；**seed pin 未换**） |
| **T / EMPTY / N** | 记账仍 18/18 · 18/18 · 54/54 |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **codegen M2 产品质量：let-hoist + Option/Result 权威** | force-regen 链上矩阵 **mac 全绿**（rv=42 · opt · si · hello `0a`）。根因三支：**(1)** pin X→C **let 上提**：`name_is_main` / `overload_count` 在 `copy_func_name64` 前求值 → 永不 `main`、永不 mangle extern；**(2)** struct_lit abi_dup **else 强制 `std_net_`** → `std_net_Option_i32` incomplete；**(3)** `.x` 缺 pin 已有的 **Result_i32/u8 skip** → redefinition。修法：零初值 + 语句赋值（hoist-safe）；Option_*→`core_option_` / Result→`core_result_` / 显式 Tcp*→`std_net_` / 其余 keep ctx；skip 补 Result。**g05 链 force-regen 矩阵绿后已 restore pin**；**seed pin / tip L4 未动**（对齐 typeck M2）。日志：`/tmp/codegen_m2_quality.c|.o` · `/tmp/m2_quality_g05.log` · `/tmp/m2q_{rv,opt,si,hello}.log`。 |

### 五问（本波）

| # | 答 |
|---|----|
| 产生 | pin 产品 codegen 对 `.x` 的 **let 上提**；struct_lit 前缀表 catch-all；Result skip 仅在 seed |
| 存储 | force-regen C 中 `name_is_main`/`overload_count` 错序；错误 C 标签 `std_net_Option_*` / 重复 `Result_*` |
| 消费 | M2 链上 `codegen_x.o` → 用户 `-o` 矩阵 |
| 同模式 | 任意「先 side-effect 填缓冲、后 `let x = f(缓冲)`」在 force-regen 下均危险 → 零初值 + 赋值 |
| 最小回归 | force-regen host-cc + 链上 rv/opt/si/hello；restore pin 后再绿 |

### 已合入 / 本波

| SHA | 摘要 |
|-----|------|
| （本波） | M2 产品质量：hoist-safe link_name/emit_func + Option/Result struct_lit + Result skip |
| `ffe2dbd1` | emit_expr parse-skip + float_lit glue |
| `5aa391f7` | M2 半收口：skip_struct 扁平 let + x_ast Cap 闭合 |
| `66c8496c` | codegen M1：`MAX_ONEFUNC` 1024 |
| `106a6b73` | codegen Cap-T001 |

### mac 探针（本波）

| 项 | 状态 | 日志 / 备注 |
|----|------|-------------|
| force-regen parse / static | **0** skip · static **15** | `/tmp/codegen_m2_quality.err` · num_funcs=230 |
| host-cc force-regen | ✅ | `/tmp/codegen_m2_quality.o` |
| M2 链上 g05 | ✅ | `/tmp/m2_quality_g05.log` |
| M2 链上矩阵 | ✅ rv=42 · opt · si · hello(`0a`) | `/tmp/m2q_*.log` |
| 产品矩阵（pin restore） | ✅ | seed pin 未换 |
| 产品 tip L4 | 仍 **`5a636984`** | **非 tip L4** |
| Ubuntu M2 smoke | ⬜ 未本波 | 金标 follow-up |

### 已知 residual

| 项 | 说明 |
|----|------|
| **seed pin 未换** | M2 smoke 绿 ≠ re-pin；与 typeck M2 同策略；re-pin / tip L4 另闸 |
| **Ubuntu M2 smoke** | 本波仅 mac；SHARED 应补 Ubuntu 链上矩阵 |
| lexer block-comment `"` | 产品 lexer 对 comment 内 `"` 敏感；应用层已避雷；**parser/lexer 根修 follow-up** |
| parser `{ let }` 匿名块 | 与 skip_struct 同源 — **开 parser 根修 follow-up** |
| let-hoist 类债 | force-regen 路径凡「依赖先写缓冲的 let 初值」仍可能炸 — 修时统一零初值+赋值 |

## 下一波前排

1. **Ubuntu codegen M2 smoke**（pull + force-regen `codegen_x.o` + g05 + 矩阵）→ 双端 smoke 后才谈 **re-pin**  
2. lexer：block comment 内不得开 string 扫描 + parser `{ let }`  
3. **禁止** Cap/M1/M2 冒充 tip 产品 L4  

```text
构建级别：工程 L2（codegen M2 smoke mac 矩阵绿；pin restore；非 tip L4）
产品 tip L4：5a636984
codegen M2：parse-skip 0；host-cc ✅；链上矩阵 mac ✅（seed pin 未换）
产品矩阵：mac rv/opt/hello/si 绿（pin + force-regen smoke 均绿）
```
