# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`48bca8c1`**（formal std ensure；产品 L4 钉盘仍 **`abfcf576`**） |
| 产品 L4 放行 | ✅ **钉盘 `abfcf576`** 双端 L4 真冷 + bstrict 123（前钉 `723d5d97`） |
| tip 双端 bstrict | ✅ **123/123 @ `abfcf576`**（L4 全擦；本波 **非** tip L4 升钉） |
| **std.vec 产品 -o** | ✅ formal `vec.o` + need_vec→heap/mem + **L4 wipe ensure** |
| **std.math 产品 -o** | ✅ formal `math.o` = **mod.x+math.x** `--bare-impl` · cold ensure 后 re-resolve push |
| **NL-07 L1–L10 / v5** | ✅ 见上波；产品 g05 nostdlib |
| **freestanding execvp + %.\*s** | ✅ `2db64465` / `abfcf576` |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · formal std ensure after L4 wipe）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」：L4 后 formal `std/*.o` 缺失 residual |
| **地图** | 产生=Makefile/`shux_compile_std_module`；存储=gitignored `.o`；消费=`push_existing` 静默 skip；爆炸=math/vec/set/map。半截 ensure→make→shux→need_* **fork bomb**；math 仅编 `math.x` 无 `std_math_*`；ensure 后仍 push 入口期空串 `math_o` |
| **根修** | G.7 `runtime_link_abi.from_x.c`：`shux_ensure_formal_std_make_o` + **`SHUX_FORMAL_STD_ENSURE` reentrancy**；need_math **re-resolve push**；Makefile `math.o` = mod.x+math.x；vec 规则保留 |
| **验收** | **日常 L2/L3**（非 tip L4）：**双端** wipe formal o 后 with_arena/math/hello **build=0 run=0** |
| **feat** | **`48bca8c1`** |
| **日志** | mac `/tmp/mac_formal_ensure_matrix5.log` · Ubuntu g05 `/tmp/ubuntu_formal_ensure_g05.log` · matrix `/tmp/ubuntu_formal_ensure_matrix_48bca8c1.log` |

### residual 下一波

1. freestanding 数学桩 `erfc` 等无限递归 warning 按需（非门禁红）  
2. 工程轨 residual（非本波）  
3. tip L4 升钉仅当双端真冷 + bstrict 123（**本波未做**）  

```text
构建级别：L2/L3 日常（g05 + formal ensure 冷探针；非 tip L4 升钉）
产品 tip L4 钉盘：仍 abfcf576
tip HEAD：48bca8c1
本波：formal std ensure reentrancy + math authority + re-resolve push
Ubuntu：/tmp/ubuntu_formal_ensure_g05.log · /tmp/ubuntu_formal_ensure_matrix_48bca8c1.log
mac：/tmp/mac_formal_ensure_matrix5.log
叙事：L4 residual — formal std/*.o ensure 根修（link 层）
```
