# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` tip **`256acd3b`**（typeck Cap-T001 M1/M2 smoke；前序 WPO **`57665c75`**；产品 L4 仍 **`5a636984`**） |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**；工程 M2 绿 ≠ tip L4） |
| **D Stage2** | ✅ Ubuntu freestanding D-03 @ `c3031883`（**≠** 产品链） |
| **E R2** | ✅ diagnostic / driver_abi / fmt_check Cap residual pure **收口** |
| **Stage2 WPO full-chain** | ✅ @ **`57665c75`** proxy **5.81%** |
| **M typeck** | ✅ **M1** typeck.x typeck OK + `-E`；✅ **M2 smoke** force-regen `typeck_x.o` + g05 + 矩阵（**产品 seed pin 未换**） |
| **T / EMPTY / N** | 记账仍 18/18 · 18/18 · 54/54；本波重扫 EMPTY 地图：typeck **OK**（非 EMPTY）；parser/codegen 仍 **FAIL**（既有，非本叶引入） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **typeck Cap-T001 → M1 + M2 smoke（工程轨）** | 根因：`typeck.x` 全量 `-E` 因 LANG-007 T001（bare export-extern）卡在 `#230 typeck_resolve_type_alias_ref_local`，挡 M2 force-regen。根修：130 个含 `pipeline_*`/glue 调用的函数 **whole-body `unsafe`**（注释/字符串/`\`` 感知括号匹配，避免注释内 `` `}` `` 截断函数体）。**M1**：typeck OK + `-E-extern`。**M2 smoke**：`shux_asm` regen → patch → `typeck_x.o` → g05 → 矩阵 rv=42 / option / hello(`0a`) / si **双端绿**。**产品** `seeds/typeck_gen.linux.x86_64.c` **未 re-pin**（M2 试替换 smoke，非 M4 去 pin）。 |

### 已合入

| SHA | 摘要 |
|-----|------|
| `256acd3b` | **typeck Cap-T001** whole-body unsafe（M1/M2 smoke） |
| `d3864c6c` / `b9ce9eb6` / `57665c75` | 前序 WPO proxy-off residual |

### Ubuntu 探针（@ `256acd3b`）

| 项 | 状态 | 日志 / 备注 |
|----|------|-------------|
| typeck `-E` M1 | ✅ typeck OK · 336122B | `/tmp/tck_e_final.err` |
| M2 regen `typeck_x.o` | ✅ cc_rc=0 · 153168B · `typeck_x_ast` T | 产品 `shux_asm` `-E-extern`（勿依赖冷 shux-c 无链） |
| g05 + 矩阵 | ✅ rv=42 · opt · hello · si | g05 `/tmp/ubuntu_m2_g05b.log` |
| mac 同路径 | ✅ 矩阵绿 | `/tmp/mac_m2_g05_ok.log` |
| 产品 tip L4 | 仍 **`5a636984`** | 本叶仅 typeck.x Cap-T001 |

### 已知 residual

| 项 | 说明 |
|----|------|
| 产品 typeck seed pin | 仍 `seeds/typeck_gen.linux.x86_64.c`；M2 smoke 用本地 regen，**未** M4 re-pin |
| EMPTY 地图 | typeck 现 typeck OK 但 diff≠EMPTY；parser/codegen 仍 FAIL（既有） |
| FORCE_REGEN 经 `shux-c` | 冷/半链 `shux-c` 可能空失败；M2 应用 **产品 `shux_asm`** 做 `-E-extern` |
| 产品 tip L4 | 仍 **`5a636984`** |

## 下一波前排

1. **M2 续**：typeck seed re-pin 评估 / codegen·parser Cap-T001 解锁 EMPTY 真重测；或用户指定 Cap  
2. **禁止**把 M2 smoke / WPO 工程绿写成 tip 产品 L4  
3. 动 SHARED 产品面（含 re-pin typeck seed）→ 新 SHA 双端 L4  

```text
构建级别：工程 L2/L3（typeck Cap-T001 M1/M2 smoke；非产品 tip L4）
产品 tip L4：5a636984
工程 tip：256acd3b
M1 typeck.x：Ubuntu+mac typeck OK + -E
M2 smoke：regen typeck_x.o + g05 + 矩阵双端绿（seed pin 未换）
```
