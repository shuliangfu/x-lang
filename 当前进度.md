# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` tip **`2c87981b`**（Stage2 WPO 工程修链；产品 L4 基线仍 **`5a636984`**） |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（wave13 debug_pipe Cap residual pure；**本波未动产品源**，勿把 Stage2 WPO 工程绿当 tip 产品放行） |
| **D Stage2** | ✅ Ubuntu freestanding D-03 @ `c3031883`（**≠** 产品链；勿把 `shux_asm2` 当产品） |
| **E R2** | ✅ diagnostic rest T **0**；driver_abi Cap residual pure **收口**；fmt_check Cap residual pure ALWAYS residual **0** |
| **Stage2 WPO 2c–2h** | 🟡 **已开前排**：G-02e 脚本漂移根修合入（`ba58349a`→`4c490da4`→`2c87981b`）；Ubuntu 仍红（artifact ensure / strict_glue UNDEF） |
| **T / EMPTY / N** | **18/18** · **18/18** · **54/54** |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **Stage2 WPO 开闸 + G-02e 漂移根修（工程轨）** | tip L4@`5a636984` 后全擦导致 WPO `.o` 缺失；ensure/strict_glue 仍 cc 已删 G-02e 源与错误 link 表 → 根修对齐 `build_shux_asm` 权威；**未**宣称 2c–2h 绿；**非 tip L4** |

### 已合入根修（工程脚本 · 一条债一 commit）

| SHA | 摘要 |
|-----|------|
| `ba58349a` | strict_glue/experimental 对齐 G-02e（runtime_link_abi / backend_call_dispatch）；禁止 WPO helpers 覆盖 full `pipeline.o`（T>80） |
| `4c490da4` | 去 `std_sys_shim.o`；ensure `lsp_diag_pipeline_sizes` weak seed |
| `2c87981b` | experimental `parser_parse_bootstrap` 补 `-Isrc/asm`（stretch audit header） |

### Ubuntu 探针（`2c87981b` 前 `4c490da4` 日志仍可参考）

| 项 | 状态 | 日志 |
|----|------|------|
| chain ensure | 🔴 缺 `driver_compile.o` / `typeck_wpo.o` / `backend_wpo.o`（product `typeck` 探针 `__text≈105821` ≫ max 2048；DCE 未压到 baseline） | `/tmp/ubuntu_stage2_wpo_chain_4c490da4.log` |
| strict link | 🔴 final link UNDEF（`pipeline_resolve_path_try_one_lib_root` / `pipeline_typeck_*_strict_minimal` / `lsp_write_all`…） | `/tmp/ubuntu_stage2_wpo_strict_4c490da4.log` |
| pipeline_wpo | ✅ product 可重建 `__text=7611` | 同上 |
| pipeline.o protect | ✅ full T=1330 后 residual 1321（不再 0-symbol） | 同上 |

## 下一波前排

1. **Stage2 WPO residual（工程）**：  
   - typeck/backend/driver_compile WPO 压缩（product 路径 DCE 无效 → 须 experimental 绿或抬/修 gate 权威）  
   - strict_glue WPO opt-in 链 UNDEF（strict_minimal / lsp / resolve_path 前缀）  
   - 日志续 `/tmp/ubuntu_stage2_wpo_*_2c87981b.log`  
2. **禁止**把 Stage2 WPO / 工程脚本修写成 tip 产品 L4 或完全自举（产品放行仍 **`5a636984`** 直至新 tip L4）  
3. 动 SHARED 产品面 → 新 SHA 再双端 L4  

```text
构建级别：工程 L2/L3（Stage2 WPO 脚本/链；非产品 tip L4）
产品 tip L4：5a636984（bstrict 123 双端已钉）
工程 tip：2c87981b
```
