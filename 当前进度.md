# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `277afa40`（FAIL `backend_try_inline_dispatch`） |
| 产品 | 双端 bstrict **123** @ `233e3b72`（L4 历史；本波工程未重跑 L4） |
| **T** | **10/18**（mega；未重跑） |
| **N** | **52/54 IDENTICAL**（Ubuntu 记账；相对 `70666dcb` 全表 35 单叶 **+17**）；mac 同叶绿 → 全表预期 **53/54**（mac 另见 `lsp_io_std_heap` IDENTICAL） |
| **R2 / H** | 仍冻结（N 未满；剩 FAIL 2） |
| **Track L SYM** | **0**（已清） |

## 本波摘要

| 波 | SHA | 结果 |
|----|-----|------|
| Track L SYM 0 | `c1735385` | `rt_run_compiler_parsed` 收口 |
| **FAIL try_inline** | **`277afa40`** | hoist 全部 `export extern "C"` 至首次使用前 + `g02f_*` no_mangle；**mac+Ubuntu IDENTICAL** |

**产生点（try_inline）**：
1. 中段 late-decl `export extern "C"` → `-E` 发 **类型 mangle 原型**，调用点仍用 **短名** → 未声明 + `cc -c` 失败  
2. `g02f_load/store_ptr_at` 模块前缀 mangle vs seed surface 短名  

**权威**：`compiler/src/asm/backend_try_inline_dispatch.x`（G.9 英文；PLATFORM SHARED）。  
**构建级别**：日常真测 L2/L3 单叶 prove（**非** L4）。

**剩余 2（Ubuntu）**：
- `backend_call_dispatch`：同型 late-decl / 短名调用漂移（`pipeline_module_import_path_byte_at` 等）
- `lsp_io_std_heap`：`.x` -E/cc OK；**seed** `lsp_io_std_heap_gen.c` 在 Ubuntu `cc -c` 失败（mac 全绿 IDENTICAL）

日志：mac `/tmp/mac_prove_backend_try_inline_dispatch.log` · Ubuntu `/tmp/ubuntu_prove_fail3_277afa40.log`

## 下一波前排

1. **FAIL `backend_call_dispatch`**：同 try_inline 模式 hoist externs（+ 必要 no_mangle）  
2. **`lsp_io_std_heap` seed 路径**（Ubuntu-only seed cc；与 mac 分列）  
3. 禁未稳 N 谈 Stage2；动 SHARED 才 L4  
