# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **D Stage2** | ✅ Ubuntu freestanding D-03 @ `c3031883`（**≠** 产品链） |
| **E R2** | ✅ diagnostic / driver_abi / fmt_check Cap residual pure **收口** |
| **Stage2 WPO full-chain** | ✅ @ **`57665c75`** proxy **5.81%** |
| **M typeck** | ✅ **M1** + ✅ **M2 smoke** @ `256acd3b`（seed pin 未换） |
| **M codegen** | ✅ Cap+M1；**M2 嵌套根因已修**；**链上 force-regen 矩阵仍 ❌**（forward-decl / mangle residual） |
| **T / EMPTY / N** | 记账仍 18/18 · 18/18 · 54/54 |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **pin if scan_sync 注释感知 brace** | 根因：`parser_asm_scan_sync_after_if_stmt_c` **按原始字节数 `{`/`}`**，**不跳过注释**。`codegen.x` `if (any_let…)` 内注释 `` `void {` 非法 C `` 的 lone `{` 使嵌套 if 的 scan_sync 多要一个 `}` → then 体吞掉 while 兄弟（per-func emit）→ Cap force-regen 只共 emit 首函数。 |
| **修法** | `parser_asm_if_stmt_slice.inc`：新增 `parser_asm_skip_balanced_braces_bytes_comment_aware_c`（//、block comment、`"..."`/`'...'` 内 brace 不计深度）；then/else brace 与 cond 括号扫描同源。**权威 = parser_asm thin glue**（pin 与产品 parser 共用）。 |
| **验证** | Cap force-regen C：`while` 内 `if(i==0)` **只含 prologue**；`skip_name`/`emit_func`/`i++` 在 while 层；`return 0` 在 while 后。option `-E` **全套** `core_option_*` 体 + `main`。host-cc Cap C 绿。**装链 force Cap 后矩阵仍红**：option 缺 **forward prototype**（`map_ptr_u8` 调用尚未定义的 `is_some_ptr_u8`；pin 在 body 前有 extern 墙）；hello `io.print` 未 mangle；si `unwrap_or` 无后缀。**pin restore 矩阵绿**（rv=42/opt/hello）。**seed pin / tip L4 未动**。 |

### 五问（本波）

| # | 答 |
|---|----|
| 产生 | `scan_sync_after_if` 原始 brace 计数把注释里的 `{` 当真实 open |
| 存储 | 嵌套 if 的 `lex_out` 过冲 → 外层 then/while 的 AST 兄弟语句并入 then 体 |
| 消费 | Cap `codegen_x_ast` 控制流错 → 用户 TU dep 体残缺 / UNDEF |
| 同模式 | 任意 if then 注释含未配对 `{`/`}`；else 臂同 helper |
| 最小回归 | Cap force-regen：`codegen_x_ast` 结构；option 全函数体；pin 矩阵；**勿** re-pin |

### 已合入 / 本波

| SHA | 摘要 |
|-----|------|
| （本波） | parser if scan_sync comment-aware braces + 进度 |
| `1442b3bd` | 前波：M2 dep 共 emit 根因 docs |
| `f6bfc43b` | M2 产品质量：let-hoist + Option/Result ABI |

### 探针（本波）

| 项 | 状态 | 日志 / 备注 |
|----|------|-------------|
| Cap force C 嵌套 | ✅ | `/tmp/codegen_m2_scanfix.c` · out≈436KiB · while/if 结构正确 |
| Cap host-cc | ✅ | `/tmp/codegen_m2_scanfix.o` |
| Cap force option `-E` 全函数体 | ✅ | `/tmp/opt_m2_force.c` 有完整 `core_option_*` 体 + main |
| Cap force 矩阵 | ❌ | option host-cc：forward-decl；hello `io.print`；si mangle |
| 产品 pin 矩阵 mac | ✅ | rv=42 / opt / hello · thin_glue 已 g05 |
| 产品 tip L4 | 仍 **`5a636984`** | **非 tip L4** |

### 已知 residual

| 项 | 说明 |
|----|------|
| **M2 链上矩阵红** | 嵌套已修；**Cap 共 emit 缺 body 前 extern 墙**（pin 有、Cap 无）→ 同模块前向调用 host-cc 红；hello/si mangle 另账 |
| **seed pin 未换** | pin 矩阵绿 ≠ re-pin |
| lexer block-comment `"` | follow-up |
| parser `{ let }` | follow-up |

## 下一波前排

1. **Cap force-regen 产品质量**：同模块 **forward prototype**（body 前 extern 墙与 pin 对齐）+ hello/si mangle → 双端 force-regen 矩阵绿 → 才谈 re-pin  
2. lexer block-comment 奇次 `"` + parser `{ let }`  
3. **禁止** Cap/M1/M2 冒充 tip L4；**禁止**装 `build_asm/*_x.o`；**禁止** 53KiB 残 Cap 装链  

```text
构建级别：工程 L2（parser thin glue；pin 矩阵绿；force Cap 嵌套修、矩阵未绿；非 tip L4）
产品 tip L4：5a636984
codegen M2：scan_sync 注释 brace 根修 ✅；链上矩阵 residual=forward-decl/mangle；seed pin 未换
产品矩阵：pin mac rv/opt/hello 绿
```
