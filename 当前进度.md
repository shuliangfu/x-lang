# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `922487e5`（T parser Cap-T001 → **T 18/18**） |
| 产品 | 双端 bstrict **123** @ `233e3b72`（L4 历史；本波未重跑 L4） |
| **T** | **18/18**（mac+Ubuntu @ `922487e5`） |
| **N** | **54/54 IDENTICAL**（全表 @ `11f3ad55`） |
| **R2 / H** | 可解冻抽检 |
| **Track L SYM** | **0** |
| **FAIL residual** | **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **T parser Cap-T001** | mega `parser.x`：整层 `unsafe`（~206 FFI 包装）+ `token.Token*` 限定 + 去 `std.heap`（`calloc`/`free`）→ typeck OK + `-E` 出 C；顺带 **typeck/codegen mega 亦 OK** → **T 18/18** |
| **g05 链接** | `runtime_process_argv.o` 入链；no_c 已有强符号时省略 RT seed slices（防 duplicate） |

**构建级别**：日常真测 L2/L3（**非** L4）。  
日志：mac `/tmp/mac_diff_t_final.log` · Ubuntu `/tmp/ubuntu_diff_t_922487e5.log`

## 下一波前排

1. **T 满后**：M2 候选（seed 退役 / diff EMPTY 逐叶）或 L4 真冷 + 双端 bstrict 收口  
2. 谈 Stage2 前：**L4 + 双端 bstrict**  
3. **禁止**用 N/T 单独宣称「可自举」  
