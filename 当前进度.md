# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` tip **codegen Cap-T001**（前序 typeck Cap **`256acd3b`** / WPO **`57665c75`**；产品 L4 仍 **`5a636984`**） |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **D Stage2** | ✅ Ubuntu freestanding D-03 @ `c3031883`（**≠** 产品链） |
| **E R2** | ✅ diagnostic / driver_abi / fmt_check Cap residual pure **收口** |
| **Stage2 WPO full-chain** | ✅ @ **`57665c75`** proxy **5.81%** |
| **M typeck** | ✅ **M1** + ✅ **M2 smoke** @ `256acd3b`（seed pin 未换） |
| **M codegen** | 🟡 **Cap-T001** 64 函数 whole-body unsafe（T001 清）；**M1 未绿** — residual 见下 |
| **T / EMPTY / N** | 记账仍 18/18 · 18/18 · 54/54；parser/codegen 仍 **FAIL**（codegen 现 T001 清但 XT001/dep 挡 M1） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **codegen Cap-T001（工程轨 · 未 M1）** | 根因：`codegen.x` 全量 `-E` 首红 **T001** @ `#107 codegen_dep_import_path_len_at`（bare export-extern `pipeline_*`）。根修：与 typeck 同构，**64** 个 FFI 调用函数 whole-body `unsafe`（注释/字符串/`\`` 感知括号）。**T001 已清**。**M1 未绿**：首红变为 **XT001** `#114 codegen_emit_async_run_seed_push_name`；`SHUX_DEBUG_PIPE` 见 **`collect parse dep=ast pr_ok=-2`**（typeck 父单元同路径 `pr_ok=0`）。根因归层：**parser/collect dep 在 mega codegen 入口解析后毒化**，非 Cap-T001 未包全。探针：`ast.ref_is_null` 在隔离 `.x` 绿、在 mega codegen 内 XT001。`SHUX_PARSE_STRICT` 可见 entry/dep 大量 `parse skip`。**产品 seed pin 未换**；mac 产品矩阵 rv/opt/hello/si 绿。**非 tip L4**。 |

### 已合入

| SHA | 摘要 |
|-----|------|
| （本波） | **codegen Cap-T001** whole-body unsafe ×64；M1 residual 地图 |
| `256acd3b` | **typeck Cap-T001** whole-body unsafe（M1/M2 smoke） |
| `d3864c6c` / `b9ce9eb6` / `57665c75` | 前序 WPO proxy-off residual |

### mac 探针（本波）

| 项 | 状态 | 日志 / 备注 |
|----|------|-------------|
| codegen Cap 后 `-E` | 🟡 T001 清 · XT001 residual | `/tmp/cg_final.err` · pipe `/tmp/cg_res.err` |
| ast dep collect | ❌ `pr_ok=-2` | 对比 typeck 父：`pr_ok=0` |
| 产品矩阵 | ✅ rv=42 · opt · hello(`0a`) · si | 本波不改 seed pin |
| parser `-E` | ❌ XT001 `#180 parser_rewind_lex_for_lparen_control_stmt` | Cap 已大体覆盖；非 T001 |
| 产品 tip L4 | 仍 **`5a636984`** | |

### 已知 residual

| 项 | 说明 |
|----|------|
| **codegen M1** | Cap-T001 后 XT001；**权威归层 = parser collect dep / 入口解析后状态**（`dep=ast pr_ok=-2`） |
| typeck seed pin | 仍 pin；M2 smoke 已证可替，**re-pin = 产品面 → 双端 L4** |
| parser M1 | XT001 `parser_rewind_lex_for_lparen_control_stmt`（非 T001） |
| 产品 tip L4 | 仍 **`5a636984`** |

## 下一波前排

1. **codegen M1 根修**：parser/collect 在 mega codegen 后 `dep=ast pr_ok=-2`（parse skip / 全局状态）；**禁止**用删 `ast.ref_is_null` 糊绿  
2. parser XT001 rewind / typeck seed re-pin（产品面 → 双端 L4）  
3. **禁止**把 Cap-T001 / M2 smoke 写成 tip 产品 L4  

```text
构建级别：工程 L2（codegen Cap-T001；非 M1、非 tip L4）
产品 tip L4：5a636984
codegen Cap-T001：64 函数 · T001 清 · M1 挡在 dep=ast pr_ok=-2
产品矩阵：mac rv/opt/hello/si 绿（seed pin 未换）
```
