# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `84056dbf`（feat Ubuntu 金标）/ docs 待推 |
| **T** | **18/18** typeck OK |
| **N** | **53** IDENTICAL（附注） |
| **R2** | **35** |
| **H** | 三十五切片 thin/public **H=0**；diagnostic/driver_abi 等 rest 仍持 Cap *_impl |

## 本波：Track R · `diagnostic` R2 thin full ✅

| 路径 | 变更 |
|------|------|
| `runtime_driver_diagnostic_thin.x` | 79 公共门闩（**Track L 已真迁**；本波 R2 标注） |
| `runtime_driver_diagnostic.from_x.c` | 新增 `runtime_driver_diagnostic_slice_marker`；R2 rest 注释 |
| `runtime_driver_diagnostic_thin_surface.from_x.c` | prove 锁 surface（同 thin.from_x.c） |
| `prove_module_selfhost.sh` / `g05` | R2 thin 标注 + surface 入口 |

- 验收：mac + Ubuntu `prove_module_selfhost diagnostic` → **IDENTICAL**
- KPI：**R2 34→35**；thin 公共面在 rest 下 **H=0**（无重复 thin 体）
- **Cap residual**：`*_impl` / `va_list` / `snprintf` 消息体仍在 full seed rest（未在本波迁移）

## 下一波前排

1. **R2** `driver_abi` L2 thin → R2 formalize（同 diagnostic 模式）  
2. **Track M** typeck M2 smoke — regen `typeck_x.o` 试替换链接  
3. 纪律：commit → push → Ubuntu → 更新进度文档；KPI **H↓ R2↑**