# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `88a34ad5`（feat）/ docs |
| **T** | **18/18** typeck OK |
| **N** | **53** IDENTICAL（附注） |
| **R2** | **27**（+`labi_path_pure`；Ubuntu 金标） |
| **H** | 上述二十七切片 **H=0**；其余 runtime hybrid 多仍 rest C（diagnostic thin… / labi L* list…） |

## 本波：Track R · `labi_path_pure` R2 full 真迁 ✅

| 路径 | 变更 |
|------|------|
| `src/runtime/labi_path_pure.x` | 吃满 7 公共门闩 + count |
| `seeds/labi_path_pure.from_x.c` | 业务符号 `#ifndef SHUX_LABI_PATH_PURE_FROM_X`；FROM_X 下 rest 仅前向 + marker → **业务 H=0** |
| Cap residual mega rest | Windows `#if` `'\\'` 分隔符（产品 PREFER 走 .x POSIX） |
| `seeds/labi_path_pure_surface.from_x.c` | full surface（prove 锁） |
| `tests/prove_module_selfhost.sh` | 标注 `labi_path_pure` R2 full → surface |

- 验收：mac + Ubuntu `prove_module_selfhost labi_path_pure` → **IDENTICAL**；FROM_X rest 仅 `labi_path_pure_slice_marker`
- KPI：**R2 26→27**；N **53**（附注）；该切片 H **1→0**
- 产品 hybrid：full .x + mega rest 无已迁 public 体（`#ifndef` 既有）

## 下一波前排

1. **R2** runtime 其余 hybrid rest（diagnostic thin rest… / labi L* list：ensure/invoke_cc/ld/freestanding/std/ondemand/gates…）· Cap-fn-ptr 批量  
2. **Track M** typeck M2 试替换 `typeck_x.o` smoke  
3. 纪律：commit → push → Ubuntu → docs；KPI **H↓ R2↑**
