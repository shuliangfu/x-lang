# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `9e0274ce`（feat R2 full simd_enc 真迁 + Ubuntu 金标） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **48** |
| **H** | **simd_enc rest 业务 H=0**（仅 slice_marker；FROM_X rest 业务 T 72→0） |

## 本波：Track R · `simd_enc` **R2 full 真迁** ✅

| 路径 | 变更 |
|------|------|
| `simd_enc.x` | 头注 R2 full；去死 `_impl` extern；75 `#[no_mangle]` 真体（74 公共 + anchor） |
| `simd_enc.from_x.c` | `#ifndef SHUX_SIMD_ENC_FROM_X` 冷全 C；`#else` 仅 `slice_marker` |
| `simd_enc_surface.from_x.c` | prove 锁 full surface（同构 full.x） |
| `g05_ensure_relink_prereqs.sh` | PREFER：**full.x** + rest FROM_X；失败再 L2 thin hybrid；再冷 seed |
| `prove_module_selfhost.sh` | full.x vs surface |

- 验收：mac/Ubuntu `prove_module_selfhost` → **simd_enc IDENTICAL**；全量 **54 IDENTICAL**
- KPI：**R2 47→48** · rest 业务 **T 72→0** · N 54→54
- **不是** thin 表面形式化：产品链 full.x 业务体，rest 无 `_impl`

## 下一波前排

1. **R2 真迁**（禁止只改注释/surface）：rest 仍有业务 C 且 .x 可承载的切片（如 Cap residual 深迁 / Cap-fn-ptr / 其它 rest H>0）  
2. **Track M** typeck M2 smoke  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档；报数写 **rest 业务 T 前后**
