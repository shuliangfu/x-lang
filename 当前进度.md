# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting`（本波 fix 待 commit） |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **D Stage2** | ✅ Ubuntu freestanding D-03 @ `c3031883`（**≠** 产品链） |
| **E R2** | ✅ diagnostic / driver_abi / fmt_check Cap residual pure **收口** |
| **Stage2 WPO full-chain** | ✅ @ **`57665c75`** proxy **5.81%** |
| **M typeck** | ✅ **M1** + ✅ **M2 smoke** @ `256acd3b`（seed pin 未换） |
| **M codegen** | ✅ Cap+M1；🟡 **M2 根修中**（2/3 parse-skip 已修；`emit_expr` 仍 skip → host-cc 仍红） |
| **T / EMPTY / N** | 记账仍 18/18 · 18/18 · 54/54 |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **codegen M2 根修（工程轨 · 半收口）** | **根因层=parser**（非 seed emit）：`SHUX_DEBUG_PARSE=1` 见 **parse skip** 后 residual 体被顶层 let 误收 → 文件级 `static` 重名 + 假 `init_globals`。**已修 2/3**：① `codegen_should_skip_emit_struct_layout_for_abi_dup` 匿名 `{ let… }` 块 → **扁平 let**（最小复现：`{ let y }` 整函数 skip）；② `codegen_x_ast` 补 **Cap whole-body unsafe 闭合 `}`**（否则 skip @ entry）。**仍红**：`emit_expr`（~2400 行 / EXPR_CALL 大支）仍 parse skip → static≈311 + 假 init_globals 残留。**对照**：typeck M2 绿；pin 矩阵绿。**seed pin / tip L4 未动**。日志：`/tmp/codegen_m2_final.err` · `/tmp/codegen_m2_final.c`。 |

### 五问（本波）

| # | 答 |
|---|----|
| 产生 | product parser `parse_one_function_*` 对特定体 **skip**（非 soft-skip codegen） |
| 存储 | skip 后 residual → `module.num_top_level_lets` / 非法 `codegen_gen.c` |
| 消费 | host `cc` / M2 `*_x.o` 替换 |
| 同模式 | 任意 mega/`{ let }` 匿名块；parser 根修 follow-up |
| 最小回归 | force-regen def 存在性 + mac 矩阵（pin） |

### 已合入 / 本波

| SHA | 摘要 |
|-----|------|
| （本波） | **M2 根修半收口**：skip_struct 扁平 let + x_ast Cap 闭合；emit_expr residual |
| `efe48e75` | docs：M2 smoke 红地图 |
| `66c8496c` | codegen M1：`MAX_ONEFUNC` 1024 |
| `106a6b73` | codegen Cap-T001 |

### mac 探针（本波）

| 项 | 状态 | 日志 / 备注 |
|----|------|-------------|
| force-regen parse skips | **3→1**（仅 `emit_expr`） | `/tmp/codegen_m2_final.err` |
| force-regen defs | skip_struct ✅ · x_ast ✅ · emit_expr ❌ | static 390→311 |
| host-cc force-regen | ❌ | emit_expr residual statics |
| 产品矩阵（pin） | ✅ rv=42 · opt · hello(`0a`) · si | seed pin 未换 |
| 产品 tip L4 | 仍 **`5a636984`** | **非 tip L4** |

### 已知 residual

| 项 | 说明 |
|----|------|
| **emit_expr parse skip** | ~2400 行；历史注释已示「超长 if -E-extern parse 失败」；方向：拆 EXPR_CALL 大支 / parser 根修 |
| seed re-pin | **blocked** on emit_expr + host-cc 绿 |
| parser `{ let }` 匿名块 | 语言块语法应支持；当前产品 parser skip — **开 parser 根修 follow-up** |
| onefunc 泄漏 / parser XT001 / tip L4 | 仍前序 residual |

## 下一波前排

1. **emit_expr parse skip 根修**（拆 mega 支 或 parser）→ force-regen host-cc → M2 smoke  
2. parser 匿名块 `{ let }` 根修（与 skip_struct 同源）  
3. **禁止** Cap/M1/M2 冒充 tip 产品 L4  

```text
构建级别：工程 L2（M2 根修半收口；非 tip L4）
产品 tip L4：5a636984
codegen M2：parse-skip 3→1（emit_expr）；host-cc 仍红
产品矩阵：mac rv/opt/hello/si 绿（seed pin 未换）
```
