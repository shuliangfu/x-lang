# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `88472b61`（feat driver_abi 数值 env pure 深迁 + Ubuntu 金标） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **49**（driver_abi 仍 R2 thin；本波 H↓） |
| **H** | **driver_abi rest T 176→173**（parse_u32 + stack_limit_want + phase_timing_enabled pure-dup 剔除）；fmt_check rest T=24；仍 Cap residual uname/setrlimit/pthread/path-read/BSS/format/debug_pipe |

## 本波：Track R · `driver_abi` **数值 env pure 深迁** ✅

| 路径 | 变更 |
|------|------|
| `runtime_driver_abi_thin.x` | pure 真体：`driver_env_nonnull` + `driver_parse_u32_cstr` + `driver_stack_limit_want_bytes` + `compile_phase_timing_enabled` / `driver_compile_phase_timing_enabled`；bump 调 pure want；timing begin/end/flush 调 pure enabled |
| `runtime_driver_abi.from_x.c` | FROM_X 下剔除 pure-dup want/timing `_impl`；去掉 `driver_compile_phase_timing_enabled`→`_impl` 宏映射 |
| `runtime_driver_abi_thin_surface.from_x.c` | prove 锁 thin surface；call-site `getenv("…")` |

- 验收：mac/Ubuntu `prove` → **driver_abi IDENTICAL**；rest T **173**；hybrid `shux check` OK；`-E` OK
- KPI：**H↓** rest 业务 T **176→173**
- **不是** thin 表面 KPI：数值 env 真体在 thin.x；Cap residual 仍 rest（uname/setrlimit/pthread/path-read/BSS/format/debug_pipe/gettimeofday）

## 下一波前排

1. **R2 真迁 / Cap residual 深迁**（禁止只改注释/surface）：`fmt_check` 余下 Cap residual（walk/stat/argv/BSS/one_file_body）/ Cap-fn-ptr / diagnostic debug residual / driver_abi uname·setrlimit·pthread  
2. **Track M** typeck M2 smoke  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档；报数写 **rest 业务 T 前后**
