# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b`（seed pin 未换） |
| **M codegen** | ✅ Cap+M1；**M2 嵌套+forward+CALL mangle 根修**；force 矩阵 residual：hello 共 emit 质量 + si link `core_types_placeholder` |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **Cap force CALL mangle（local-first + module ctx）** | 根因三层：① 共 emit 时 `current_codegen_module`/prefix_mirror 被 dep 走访污染；② 同模块裸名前缀误走脏 mirror；③ **dep 全表扫裸名**在本模块已有同名时仍抢先命中更早 dep（`unwrap_or`：option 先于 result → `core_option_unwrap_or`）。 |
| **修法（对齐 seed，补 `.x`）** | `codegen_x_ast`：prologue 后与 **每函数 emit_func 前** 恢复 module/arena/dep_index/**prefix_mirror**；同模块 CALL 前缀优先 `codegen_ctx_dep_path_for_current_codegen_module_into`；**local_has_name** 后才扫 `nd_call`。 |
| **验证** | Cap force：`fmt.fmt_*` → `core_fmt_*` ✅；`unwrap_or_gen` → `core_result_unwrap_or` ✅；si **host-cc** ✅；si **`-o`** ❌ UNDEF `_core_types_placeholder`；hello ❌（io 重定义 / undeclared / Error 类型等，非本波 mangle）。rv/opt Cap force ✅。**pin restore 矩阵绿**。**未 re-pin / 未 tip L4**。 |

### 五问

| # | 答 |
|---|----|
| 产生 | Cap 缺 seed 的 local-first + 每体前 module/prefix 恢复 |
| 存储 | `PipelineDepCtx.current_codegen_*` 与 dep 扫描顺序 |
| 消费 | 共 emit C 中 CALL 符号；host-cc / ld |
| 同模式 | 任意多 dep 同名裸调用（free / unwrap_or / …） |
| 最小回归 | Cap force hello `-E` mangle；si host-cc；pin 矩阵；勿 re-pin |

### 探针

| 项 | 状态 | 备注 |
|----|------|------|
| Cap force rv/opt | ✅ | |
| Cap force hello mangle `core_fmt_*` | ✅ | 前 `fmt.fmt_*` |
| Cap force si host-cc | ✅ | mangle 绿 |
| Cap force si `-o` | ❌ | UNDEF `core_types_placeholder` |
| Cap force hello `-o` | ❌ | io redef / undeclared / Error… |
| pin 矩阵 mac | ✅ | rv/opt/hello/si |

### residual 下一波

1. Cap force **si link** `core_types_placeholder` 入链/共 emit  
2. Cap force **hello** 共 emit 质量（io 重定义、`std_io_core_*` undeclared、Error/Result 类型）  
3. 全绿才 re-pin；**禁** tip L4  

```text
构建级别：工程 L2（codegen CALL mangle local-first；非 tip L4；seed pin 未换）
产品 tip L4：5a636984
codegen M2：嵌套✅ forward✅ CALL mangle✅；force 矩阵 residual=si link + hello co-emit
产品矩阵：pin mac 绿
```
