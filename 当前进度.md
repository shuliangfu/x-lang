# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`d1b9e984`**（core.slice formal + TYPE_SLICE call ABI；L4 钉盘仍 **`f16f7d48`**） |
| 产品 L4 放行 | ✅ **钉盘 `f16f7d48`**（双端真冷 L4+123） |
| tip 双端 bstrict | ✅ **123/123 @ `f16f7d48`**（本波 **非** tip L4 升钉） |
| **slice length BLD001** | ✅ **链闭**（`5748d795`…`d1b9e984`）；Ubuntu pure asm full 仍 **exit 4** residual（见下） |
| **slice data_field / region** | ✅ **`adbbb001`** |
| **slice main asm SIGABRT** | ✅ **`6fda8bd2`** |

## 本波摘要（2026-07-18 · slice length.x / core.slice formal BLD001）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 slice `length.x` / core.slice API BLD001（钉盘 `f16f7d48` 不动；**不**自动 tip L4） |
| **地图** | Ubuntu `-backend asm` length.x：**BLD001** U `core_slice_len_i32`…；C 后端=0；`run-slice` 默认 `-backend c` 假绿 |
| **根因链** | ① `slice.o` 仅 glue（from_ptr/subslice），无 mod.x API → 须 formal `core/slice/mod.o` + labi simple **g9**；② typeck `.data` 仅 TYPE_U8 写 `*elem` → i32[] `s.data[i]` T001 挡 formal；③ formal 裸名/ `std_slice_*` 错前缀 → 须 `core_slice_*`；④ asm 实参 dual-GP 与 C 形参 `struct *` 不匹配 → lea + **1 GP** 分类；⑤ mod.o 强定义 co-emit `core_option_*` 与 option.o 抢符号 |
| **根修** | G.7 多层单点：Makefile formal + ondemand g9 + ensure/glue 共推；`pipeline_typeck_field_slice_c` 全 T 的 `*elem`；`shux_compile_std_module` path-root 前缀 + localize co-emitted option；call-arg lea + agg/call_arg size=8 for TYPE_SLICE |
| **验收** | **日常 L2**：Ubuntu **length asm build=0** · length **c run=0** · main/region/data_field · rv/opt/hello/si · run-slice OK；mac **length asm run=0**；**非 tip L4** |
| **钉盘** | 仍 **`f16f7d48`** |
| **残差** | Ubuntu pure asm **full length.x run=4**（三枚 `Option_u8` 先赋值再检查；单 Option / 立刻检查则绿）— 疑 Option 多局部 dual-home 叠槽；**subslice_split_chunks** asm run=1 |

### residual 下一波

1. （旁）length.x Ubuntu multi-`Option_u8` deferred 使用 stack 叠槽（exit 4）  
2. （旁）subslice_split_chunks pure asm run residual  
3. （可选）tip L4 升钉含 slice 全波  

```text
构建级别：L2（pipeline_glue + pipeline_x + runtime_link_abi + g05 + formal mod.o；非 tip L4）
产品 tip L4 钉盘：f16f7d48（本波未升）
feat 链：5748d795 → c0188601 → cc0e9c30 → ee70ae44 → d2c44965 → d1b9e984
Ubuntu：length asm BLD001 关 · c=0 · full asm run=4 residual · run-slice OK · 矩阵绿
macOS：length asm/c=0 · 矩阵绿
权威：labi_od g9 · core/slice/mod.o · pipeline_typeck_field_slice_c · pipeline_asm_emit_expr_elf_for_call_args · glue_func_param_agg_byte_size_c · shux_compile_std_module core_ prefix
纪律：禁止日常 L2 冒充 tip L4；C 指针 ABI 与 fat 值 16B 勿混 dual-GP 打包
```

---

## 前波摘要（2026-07-18 · slice data_field / region）

| 波 | 结果 |
|----|------|
| **根修** | typeck field_access_offset + dual-GP length@slot_off-8（**`adbbb001`**） |
| **验收** | data_field asm=0；region=20 |
| **残差** | 本波已接 length BLD001 |
