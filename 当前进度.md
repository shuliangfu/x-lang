# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`580af4a1`**（freestanding mutable top-level lit lets） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **freestanding CG002** | ✅ **emit 关**（曾 elf_ec；现链 BLD001/mangle residual） |
| **prove residual** | ✅ **enc + rt_diag 双端 IDENTICAL** |

## 本波摘要（2026-07-18 · freestanding CG002 emit）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 freestanding CG002 elf_ec |
| **地图** | hosted asm vec **绿**（emit_n=1 formal）；freestanding co-emit **emit_n=89+** → 曾 CG002 |
| **根因** | freestanding 无 formal std `.o`，co-emit `std.heap.libc`；可变顶层 `let`（lit 初值，如 `shu_heap_trace_*`）在非 hoist 函数被当成 const **跳过栈槽登记** → `EXPR_ASSIGN` 无 off → emit fail → elf_ec=-1 |
| **根修** | G.7：仅 **true const** lit 走 imm 路径；**mutable** lit top-level 登记栈槽 + prologue 后 seed 初值；ELF `.bss` 真共享仍为 follow-on |
| **验收** | Ubuntu L2：CG002 **关**；fs hello/return42 绿；hosted vec/hello 绿；fs vec 现 **BLD001**（export 无类型后缀 vs call 有后缀 + nostdlib U malloc）= **另债**；**日常 L2 · 非 tip L4** |

### residual 下一波

1. （旁）freestanding co-emit **link mangle / nostdlib heap**（export bare vs call `_usize`；page_mmap / libc 面）  
2. （可选）f64 let-init binop（`let x = 0.0 - 1.5` → run=1 双端 residual）  
3. （旁）typeck residual：cfg export-extern skip 序毒化；ELF `.bss` 真共享 top-level  

```text
构建级别：L2（pipeline_glue；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：fs CG002 关 · hosted vec/hello 绿 · fs hello 绿
纪律：测试 hard timeout；本波未升 tip L4
feat SHA：580af4a1（debug 半程 556d4298）
```
