# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `6be38044`（feat R2 full backend_enc_dispatch 真迁 + Ubuntu 金标） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **46** |
| **H** | **enc_dispatch rest 业务 H=0**（仅 slice_marker；FROM_X rest T 业务 12→0） |

## 本波：Track R · `backend_enc_dispatch` **R2 full 真迁** ✅

| 路径 | 变更 |
|------|------|
| `backend_enc_dispatch.x` | typeck 修（禁 `let u32 = lit\|…`）；补 append_u8/cdqe/arch 别名；arm64 cmp_w0/cset 真体；头注 R2 full |
| `backend_enc_dispatch.from_x.c` | `#ifndef SHUX_BACKEND_ENC_DISPATCH_FROM_X` 冷全 C；`#else` 仅 `slice_marker` |
| `backend_enc_dispatch_surface.from_x.c` | prove 锁 full surface（同构 full.x） |
| `g05_ensure_relink_prereqs.sh` | PREFER：**full.x** + rest FROM_X；失败再 L2 thin hybrid；再冷 seed |
| `prove_module_selfhost.sh` | full.x vs surface |
| `x_frontend_link_alias.from_x.c` | try_inline/call_dispatch -E mangled→pipeline 弱别名（产品链） |
| `runtime_pipeline_abi.from_x.c` | `driver_asm_fflush_stdout` weak（Darwin ld 与 driver_abi 双 T） |

- 验收：mac/Ubuntu `prove_module_selfhost` → **backend_enc_dispatch IDENTICAL**；全量 **54 IDENTICAL**
- KPI：**R2 45→46** · rest 业务 **T 12→0** · N 54→54
- **不是** thin 表面形式化：产品链 full.x 业务体，rest 无 `_impl`

## 下一波前排

1. **R2 真迁**（禁止只改注释/surface）：rest 仍有业务 C 且 .x 可承载的切片（如 `simd_loop` / `backend_arch_emit_dispatch` / Cap-fn-ptr）  
2. **Track M** typeck M2 smoke  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档；报数写 **rest 业务 T 前后**
