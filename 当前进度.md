# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `39fab411`（M2 leaf token EMPTY） |
| 产品 | 双端 bstrict **123** @ `233e3b72`（L4 历史；本波未重跑 L4） |
| **T** | **18/18** typeck（mac+Ubuntu） |
| **EMPTY** | **1/18**（`token` 可退役 seed） |
| **N** | **54/54 IDENTICAL**（全表 @ `11f3ad55`） |
| **R2 / H** | 可解冻抽检 |
| **Track L SYM** | **0** |
| **FAIL residual** | **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **M2 leaf token EMPTY** | re-pin `seeds/token_gen.linux.x86_64.c` = 当前 `-E`（`shux_panic_` 声明；去 Auto 头）；harness 冷路径 alt `token_gen.c`→seed；**mac+Ubuntu EMPTY** |

**构建级别**：日常真测 L2/L3（**非** L4）。  
日志：mac `/tmp/mac_diff_t_token_empty.log` · Ubuntu `/tmp/ubuntu_diff_t_39fab411.log`

## 下一波前排

1. **EMPTY 扩叶**：driver 叶 seed 含 `driver_cmd_*` 冷启动 rename，**勿**盲 re-pin 成 bare `-E`；优先无 rename 的叶或 harness 归一  
2. **L4 + 双端 bstrict**（谈 Stage2 前强制）  
3. **禁止**用 N/T/EMPTY 单独宣称「可自举」  
