# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `268b9498`（feat+docs Ubuntu 金标） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **37** |
| **H** | 三十七切片 thin/public **H=0**；runtime_io_abi rest 仅 4 平台 Cap _impl |

## 本波：Track R · `runtime_io_abi` R2 full ✅（真迁收尾）

| 路径 | 变更 |
|------|------|
| `runtime_io_abi.x` | Track L 已真迁 19 公共 + 5 `_impl`（read/malloc/memcpy） |
| `runtime_io_abi.from_x.c` | `SHUX_RUNTIME_IO_ABI_FROM_X` 跳过 5 `_impl`；+ `runtime_io_abi_slice_marker` |
| `runtime_io_abi_surface.from_x.c` | prove 锁 full surface |
| `g05_ensure_relink_prereqs.sh` | rest 编译加 `-DSHUX_RUNTIME_IO_ABI_FROM_X`；strip `memcpy` extern |
| `prove_module_selfhost.sh` | +`runtime_io_abi` 入口；strip `memcpy` extern |

- 验收：mac `prove_module_selfhost runtime_io_abi` → **IDENTICAL**；全量 **54 IDENTICAL**
- g05：`runtime_io_abi.o ← .x + seed-rest` hybrid OK
- KPI：**R2 36→37** · **N 53→54**；rest 在 FROM_X 下公共+5 _impl **H=0**
- **Cap residual**：`runtime_read_file_view_impl` / `runtime_release_file_view_impl` / `shux_write_path_bytes_impl` / `shux_fs_open_write_flags_impl`（mmap/平台 #ifdef）

## 下一波前排

1. **R2** driver L2 thin（fmt_check / simd_* / backend_*）  
2. **Track M** typeck M2 smoke（Ubuntu 或带 C frontend 冷启动）  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档