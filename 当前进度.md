# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`fd46fa36`**（fs import-binding mid + heap redirect + ban heap_user） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm hello / hosted vec** | ✅ **Ubuntu L2** |
| **freestanding page_mmap** | ✅ **NL-03 gate OK** |
| **fs r42 / fs hello** | ✅ |
| **fs vec** | ❌ **BLD001 residual**（co-emit `std.heap.libc`→U malloc；vec bare overload mid） |

## 本波摘要（2026-07-18 · fs mangle / nostdlib heap）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 freestanding link mangle / nostdlib heap |
| **地图** | fs vec：call mid `*_u8_ptr` vs T bare；链 `runtime_heap_user`→U malloc；`heap_libc_free` 调 `heap_free_c`（redirect 误伤 libc free） |
| **根因** | ① import-binding formal miss → caller-suffix 发明 mid；② freestanding 仍 push heap_user；③ bare free/realloc 在 heap redirect 表（seed / .x / bootstrap 三处） |
| **根修** | G.7 补全：`glue_asm_mangle_import_binding_call_sym_c` res_mod recovery + unique bare；禁 fs heap_user；去 bare free/realloc redirect（三权威同 commit 对齐） |
| **验收** | Ubuntu L2：nolibc-heap OK · fs r42/hello · hosted rv/hello/vec 绿；fs vec 仍红（产品面 residual）；**日常 L2 · 非 tip L4** |

### residual 下一波

1. freestanding **std.vec / std.heap.libc**（零 libc = page_mmap 产品路径；勿再链 libc malloc）  
2. （可选）f64 let-init binop residual  
3. （旁）typeck cfg-extern；ELF `.bss`；labi `need_vec` 补 `len_empty`  

```text
构建级别：L2（backend_call_dispatch + runtime_link_abi + pipeline_x 含 bootstrap_glue；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：nolibc-heap / fs r42 / fs hello / hosted 矩阵绿 · fs vec residual
纪律：测试 hard timeout；本波未升 tip L4
feat SHA 链：58da9ad8 → 37a36f92 → 475faf63 → c98427ea → fd46fa36
```
