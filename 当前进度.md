# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`6fda8bd2`**（slice main asm SIGABRT；L4 钉盘仍 **`f16f7d48`**） |
| 产品 L4 放行 | ✅ **钉盘 `f16f7d48`**（双端真冷 L4+123） |
| tip 双端 bstrict | ✅ **123/123 @ `f16f7d48`**（本波 **非** tip L4 升钉） |
| **asm env/fs BLD001** | ✅ **`4b10fb1d`**（env/fs asm run=0） |
| **asm sort(*u8) overload** | ✅ **`a2a09ea7`** |
| **asm sort link** | ✅ **`b96778ef`** |
| **asm set/map/queue** | ✅ **`28b934e9`** |
| **asm formal ensure** | ✅ **`ffd083cc`** |

## 本波摘要（2026-07-18 · slice asm main SIGABRT）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 slice asm SIGABRT（钉盘 `f16f7d48` 不动） |
| **地图** | Ubuntu `-backend asm` `tests/slice/main.x` exit **134**（`shux_panic_` bounds）；C=20；mac asm 假绿 exit 20（栈垃圾过 bounds） |
| **根因** | `glue_block_let_is_simd_vector_type`：16B + VAR 初值启发式把 **TYPE_SLICE fat `{data,length}`** 当成 SIMD → 跳过 `glue_emit_slice_from_array_let_init`，按值 memcpy 数组字节；length=0 → bounds panic |
| **根修** | G.7 权威：① 启发式排除 TYPE_SLICE/TYPE_ARRAY；② INDEX base 对 TYPE_SLICE **load `.data`**（rax/rbx）（**`6fda8bd2`**） |
| **验收** | **日常 L2**：Ubuntu **slice main asm=20** · C opt/vec · sort asm=0；mac slice asm/c=20；**非 tip L4** |
| **残差** | `data_field` asm length 面、`region_array_smoke` slice 赋值拷贝（16B 连续假设 vs 槽布局）— 另波 |
| **钉盘** | 仍 **`f16f7d48`** |

### residual 下一波

1. （旁）slice `data_field` / `region` 赋值·字段 asm  
2. （可选）tip L4 升钉含 **`6fda8bd2`** / env-fs / sort  
3. （旁）IR/WPO 常量折叠（**不在 emit 扩**）  

```text
构建级别：L2（pipeline_glue_standalone.o + pipeline_x.o + g05；非 tip L4）
产品 tip L4 钉盘：f16f7d48（本波未升）
feat：6fda8bd2
Ubuntu：slice main asm=20 · 矩阵邻域
macOS：g05 + slice asm/c=20
权威：glue_block_let_is_simd_vector_type · glue_try_index_var_or_field_base_to_{rax,rbx}_elf_c
纪律：禁止日常 L2 冒充 tip L4；一条债一层；不在 emit 扩常量折叠
```

---

## 前波摘要（2026-07-18 · asm fs/env BLD001 residual）
## 前波摘要（2026-07-18 · asm sort(*u8) overload residual）

| 波 | 结果 |
|----|------|
| **根修** | `backend_call_dispatch` INDEX/AS + re-score score≥11（**`a2a09ea7`**） |
| **验收** | full sort asm run=0 · i32+u8 双 call |

---
