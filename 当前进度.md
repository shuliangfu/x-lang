# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin** · 🟢 slice 形参指针 ABI 半收口 |
| **parser residual** | ✅ body-lets · 🟢 hoist-safe param / block expr_stmt · 🟢 **force option host-cc=102** · 🟡 **M2 re-pin**（force **hello** 仍红：co-emit missing `core_fmt_fmt_*`） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排** | Cap force **option host-cc** 空参名 / `(void)()` / is_none=`return 0` |
| **叶 A 形参名** | pin X→C 把 `let param_idx = append_param(empty)` 提到 IDENT/copy 前 → 侧车空名 `int32_t )` |
| **根修 A** | `parse_one_function_impl` 形参循环 hoist-safe：顶 zero + 先 copy 再 append |
| **叶 B 块内 void** | `parse_block_into` while 顶 hoist `append_expr_stmt(0)`；return 后 pb_break 再一轮再 append → 2×`(void)()` |
| **根修 B** | 块循环顶 zero `stmt_start/expr_stmt_res/rpeek_fe/ex_pool_i`；append 仅在真 expr_stmt 成功路径 |
| **叶 C is_none** | library scan：`return` 后非 IDENT（`!`）误 ok + 伪造 field=`kind` → 抢占 impl → `return 0` |
| **根修 C** | `parser_asm_library_wrap_slice.inc`：return 后非 IDENT 直接 fail，交给 impl；bool return 改 `parse_cond_expr_into` |
| **M2 force 结果** | **rv=42** · **option=102** · **si=0** · **be 13/13 build** · let/mixed/const 绿；**hello host-cc 仍红** → **仍不 pin** |
| **产品** | **仍 seed `parser_x.o`**；合入 `parser.x` + library_wrap；mac seed **rv=42 / option=102** |
| **未做** | force hello 扩绿 → M2 re-pin；tip L4；Ubuntu SHARED |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | pin let-hoist：append_param / append_expr_stmt 空副作用；library scan 假阳性 |
| 谁存储 | OneFunc 侧车 param 名；Block expr_stmt；module func body（library） |
| 谁消费 | codegen 签名 / if 体 / is_none 体 |
| 同模式 | 其它 while 内 `let x = append_*` 副作用须继续审计 |
| 最小回归 | force：rv+option+be+let；seed：rv+option；**禁 pin** 至 hello 够绿 |

### residual 下一波

1. Cap force **hello**（`core_fmt_fmt_*` co-emit / host-cc）扩绿 → 再 M2 re-pin  
2. 审计其它 while 内 side-effect let hoist  
3. SHARED Ubuntu；升 tip 须明示  

```text
构建级别：日常 L2（parser.x + library_wrap；非 L4）
产品 tip L4：5a636984
parser M2：force option ✅；hello 仍红 → 未 re-pin
产品矩阵：mac seed rv/option ✅
```
