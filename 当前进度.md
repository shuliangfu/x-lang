# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **26** IDENTICAL（+ `simd_enc` thin） |
| **simd_enc** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 74） |
| **simd_loop** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 22） |
| **fmt_check** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 42） |
| **driver_abi** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 70） |
| **diagnostic** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 79） |
| **W-string-nul** | ✅ STRING_LIT *u8 尾 `, 0`；slen≤64 |

## 本波：Track L `simd_enc` hybrid→IDENTICAL（L2 thin）✅

**根因地图**：产品 hybrid 已走 **thin.x** + full seed rest（`-DSHUX_L2_SIMD_ENC_THIN_FROM_X`），但 prove 未锁 thin 公共面；与 simd_loop/fmt_check 同构缺口。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/asm/simd_enc_thin.x` | 权威 thin 公共面（产品本已用） |
| `seeds/simd_enc_thin.from_x.c` | 冷/prove 同构 thin（`-E` 抛光 + POSIX prologue） |
| `tests/prove_module_selfhost.sh` | 扩 `simd_enc` → thin.x + thin seed；空 mode → **IDENTICAL** |
| full seed rest | 不变（`_impl` / encode 仍 C 尾） |

- 验收：`bash tests/prove_module_selfhost.sh simd_enc` → **IDENTICAL**
- 全量：`bash tests/prove_module_selfhost.sh` → **26 IDENTICAL**
- 产品：g05 仍 `thin.x + seed-rest ld -r` → `simd_enc.o`（本波不改链接拓扑）

## Ubuntu 金标准（`4e36b04e`，2026-07-14 13:47 东八区，`date`）

| 项 | 结果 |
|----|------|
| `simd_enc` thin | **IDENTICAL**（产品 L2 thin @ `self-hosting`） |
| 同批 | simd_loop + fmt_check + diagnostic + driver_abi + labi 叶 + **全量 26 IDENTICAL** |

## 前排下一步

1. 下一可 IDENTICAL 叶（rt_dispatch / backend_*_dispatch thin）/ true-migrate rest
2. W-global 数组 / 串池 >64
3. 纪律：commit → push → Ubuntu → docs
