# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `f73d6c04`（feat Ubuntu 金标）/ docs 待推 |
| **T** | **18/18** typeck OK |
| **N** | **53** IDENTICAL（附注） |
| **R2** | **33** |
| **H** | 上述三十三切片 **H=0**；其余 runtime hybrid 多仍 rest C（diagnostic thin… / labi gates…） |

## 本波：Track R · `labi_ondemand_list` R2 full 真迁 ✅

| 路径 | 变更 |
|------|------|
| `src/runtime/labi_ondemand_list.x` | simple/kv/arrow/time/queue + rel_* 纯表（R2 标注） |
| `seeds/labi_ondemand_list.from_x.c` | 业务符号 `#ifndef SHUX_LABI_ONDEMAND_LIST_FROM_X`；FROM_X 下 rest 仅前向 + marker → **业务 H=0** |
| Cap residual | nm/push/ensure 仍在 mega `shux_asm_ld_append_on_demand_user_objs` |
| `seeds/labi_ondemand_list_surface.from_x.c` | full surface（prove 锁） |
| `tests/prove_module_selfhost.sh` | 标注 `labi_ondemand_list` R2 full → surface |

- 验收：mac + Ubuntu `prove_module_selfhost labi_ondemand_list` → **IDENTICAL**；FROM_X rest 仅 `labi_ondemand_list_slice_marker`
- KPI：**R2 32→33**；N **53**（附注）；该切片 H **1→0**
- 产品 hybrid：full .x + mega rest 无已迁 public 体（`#ifndef` 既有）

## 下一波前排

1. **R2** `labi_gates`（L9 — next logical labi target）  
2. **R2** diagnostic thin rest（harder；`_impl`/va_list 仍在 full seed）  
3. **Track M** typeck M2 smoke — try replacing `typeck_x.o`（parallel）  
4. 纪律：commit → push → Ubuntu → **更新 `analysis/自举进度.md` + 重写 `当前进度.md`**；KPI **H↓ R2↑**