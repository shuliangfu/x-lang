# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin** |
| **parser residual** | ✅ 匿名 `{ let }` · 🟢 **M1 -E** · 🟢 **类型序** · 🟢 **init_globals** · 🟢 **by-value return** · 🟢 **dual-extern** · 🟢 **array-init residual 关** |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排** | parser M1 host-cc **array-init residual 6** |
| **诊断** | STRUCT_LIT 对 `u8[N]` 字段发 `.name = z64` / `name64`；C 指定初始化器不能用数组/指针 RHS → pointer-to-integer on first elem（6× host-cc error） |
| **根修** | **codegen STRUCT_LIT 权威**（`codegen.x` + seed pin）：lookup 字段 TYPE_ARRAY；非 ARRAY_LIT 初值展开为 `.name = { src[0], …, src[N-1] }`（≤512）；`codegen_lookup_struct_field_type_ref` 扫 entry+deps layout |
| **验收** | parser.x `-E` 见 expand；host-cc **error=0**（array-init 关；dual-extern 仍关）；mac 矩阵 **rv=42 / opt / si / hello 绿** |
| **host-cc 现前排** | 🟢 array-init 关；`parse_into*` 仍 skip |
| **矩阵 mac L2** | **rv=42 / opt / si / hello ✅** |
| **未做** | tip L4；parse_into 真 parse；Ubuntu SHARED 复证 |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | STRUCT_LIT 字段 emit 对 TYPE_ARRAY 字段直接 `emit_expr`（VAR/param） |
| 谁存储 | 生成 C 指定初始化器非法 RHS |
| 谁消费 | host gcc |
| 同模式 | 任意 `Struct { arr: other_arr }` / 形参 `u8[N]` 写入数组字段 |
| 最小回归 | parser `-E` 无 `.name = z64`；host-cc error=0；矩阵绿 |

### residual 下一波

1. mega `parse_into*` 真 parse  
2. SHARED Ubuntu 复证；升 tip 须明示  

```text
构建级别：日常真测 L2（codegen seed pin array expand + g05；非 L4）
产品 tip L4：5a636984
parser M1：-E 绿；host-cc error=0（array-init 关）
产品矩阵：mac rv/opt/si/hello ✅
```
