# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `dea07cc1`（feat R2 full backend_arch_emit_dispatch 真迁 + Ubuntu 金标） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **49** |
| **H** | **arch_emit rest 业务 H=0**（仅 slice_marker；产品 PREFER full.x） |

## 本波：Track R · `backend_arch_emit_dispatch` **R2 full 真迁** ✅

| 路径 | 变更 |
|------|------|
| `backend_arch_emit_dispatch.x` | 头注 R2 full；47 ta 分派壳真体（arm64/riscv/x86） |
| `backend_arch_emit_dispatch.from_x.c` | `#ifndef SHUX_BACKEND_ARCH_EMIT_DISPATCH_FROM_X` 冷全 C；`#else` 仅 `slice_marker` |
| `backend_arch_emit_dispatch_surface.from_x.c` | prove 锁 full surface（同构 full.x + doc_anchor） |
| `g05_ensure_relink_prereqs.sh` | PREFER：**full.x** + rest FROM_X；失败再 L2 thin hybrid；再冷 seed |
| `prove_module_selfhost.sh` | full.x vs surface |

- 验收：mac/Ubuntu `prove_module_selfhost` → **arch_emit IDENTICAL**；全量 **54 IDENTICAL**
- KPI：**R2 48→49** · 产品 rest FROM_X **业务 T=0**（仅 marker）· N 54→54
- **不是** thin 表面：产品链 full.x 业务体；Cap residual `arch_*_emit_*` 在外 TU

## 下一波前排

1. **R2 真迁**（禁止只改注释/surface）：rest 仍有业务 C 的切片（`fmt_check`/`diagnostic`/`driver_abi` 的 Cap residual `_impl` 深迁，或其它 H>0）  
2. **Track M** typeck M2 smoke  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档；报数写 **rest 业务 T 前后**
