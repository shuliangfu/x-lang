# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`84f15d33`**（METHOD_CALL multi-arg stack · fs hello CG002） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm hello / hosted rv** | ✅ **Ubuntu L2** |
| **fs r42 / panic_div / hello** | ✅ **`run-freestanding-hello` OK** |
| **fs vec push / boundary / cookbook** | ✅ **Ubuntu L2**（MEMORY home 已关） |
| **nolibc-n06** | ✅ |

## 本波摘要（2026-07-18 · freestanding hello CG002 · METHOD_CALL multi-arg）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」→ multi-arg METHOD_CALL 修合入后 Ubuntu 验；用户「测试是不是卡了」 |
| **地图** | fs hello co-emit `submit_read_batch` final_expr：`ret_ko=METHOD_CALL` + 11 标量参；seed `pipeline_asm_emit_method_call_elf_c_impl` 硬限 `nargs>5` → -1 → CG002；修后曾 **hang**（脏 glue debug + 未干净重建 call_dispatch） |
| **根因** | G.7 权威半残：CALL 已有多参栈路径，METHOD_CALL import-binding 仍 `nargs>5` 失败 |
| **根修** | `backend_call_dispatch.from_x.c`：去掉 `nargs>5`；上限 `GLUE_ASM_MAX_CALL_ARGS`；>6 GP 走栈 + MEMORY pad；`.x` thin 同步门控（产品 thin→seed） |
| **卡死排查** | 旧日志停在 `mega_body emit_n=11` / `ctx_background_c` 后；**根因非死循环债**，是半成品对象。干净 `backend_call_dispatch.o` + standalone glue + g05 后 **&lt;12s 出二进制** |
| **验收** | Ubuntu L2：`run-freestanding-hello` **return42/panic_div/hello 全绿** · hosted rv=42 · hello · nolibc-n06；**日常 L2 · 非 tip L4** |
| **重建** | 改 call_dispatch：`src/asm/backend_call_dispatch.o` + g05；改 glue：`pipeline_x.o` + `pipeline_glue_standalone.o` 双重建 |

### residual 下一波

1. （可选）9–16B Allocator 双 GP home 全对齐 formal C  
2. 软 XT001 on freestanding co-emit `new` / `heap_mem_set_c`（非阻塞）  
3. （旁）f64 let-init；cfg-extern / `.bss` / labi `len_empty`；hosted `tests/vec/main.x` BLD001 若再挡产品面再开  

```text
构建级别：L2（backend_call_dispatch.o + g05；glue 已清 debug 串）
产品 tip L4 钉盘：5c8204ae
Ubuntu：fs hello 绿 · freestanding gate OK · hosted rv/hello · nolibc-n06
纪律：本波未升 tip L4；禁止 soft-skip 糊绿
feat SHA：84f15d33（前 MEMORY home 0338e3b6）
```
