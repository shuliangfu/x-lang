# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `7d910bef`（Track L `rt_pipeline_elf_diag`） |
| 产品 | 双端 bstrict **123** @ `233e3b72`（L4 历史；本波工程未重跑 L4） |
| **T** | **10/18**（mega；未重跑） |
| **N** | **45/54 IDENTICAL**（Ubuntu 记账；相对 `70666dcb` 全表 35 单叶 **+10**）；mac 同叶绿 → 全表预期 **46/54** |
| **R2 / H** | 仍冻结（N 未满） |

## 本波摘要

| 波 | SHA | 结果 |
|----|-----|------|
| Track L 同型 no_mangle 链 | …`5ed0434b` | …rt_content 双端 IDENTICAL |
| **Track L rt_pipeline_elf_diag** | **`7d910bef`** | 八 `rt_elf_*` helper `#[no_mangle] export`；**mac+Ubuntu IDENTICAL** |

**产生点**：非 export helper 模块前缀 mangle `pipeline_rt_elf_*` vs surface 短名。  
**权威**：`compiler/src/runtime/rt_pipeline_elf_diag.x`（surface 本就短名；G.9 英文）。  
**构建级别**：日常真测 L2/L3 单叶 prove（**非** L4）。

**剩余 9（Ubuntu）**：
- FAIL **3**：`backend_try_inline_dispatch` / `backend_call_dispatch` · `lsp_io_std_heap`
- SYM **6**：emit · rt_run_x_emit · rt_compile · rt_run_exec · rt_run_asm_backend · rt_run_compiler_parsed

日志：`/tmp/mac_prove_rt_pipeline_elf_diag.log` · Ubuntu `/tmp/ubuntu_prove_rt_pipeline_elf_diag_7d910bef.log`

## 下一波前排

1. **Track L 续吃 SYM 6**（优先同型 no_mangle 小模块）  
2. **T / Track M** 说明向  
3. 禁未稳 N 谈 Stage2；动 SHARED 才 L4  
