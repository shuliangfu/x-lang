# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`2d9da1ce`**（asm vec SysV sret + MEMORY by-value） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm vec 运行时 ABI** | ✅ **vec_i32 cookbook run=0**（sret + MEMORY 栈传） |
| **with_arena asm** | 🟡 run=5（len 过；元素 `v.ptr[i]` residual） |

## 本波摘要（2026-07-18 · asm freestanding vec 运行时 ABI）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 asm freestanding vec 运行时 ABI residual |
| **地图** | product C 绿；asm hosted `realloc invalid pointer` / SIGSEGV；根因非 mangle |
| **根因** | ① `vec.new()`=METHOD_CALL(49)，let sret 只认 CALL(48)→只 store rax；Vec=32B 半初始化；② `len(v)` >16B 按值误 lea→rdi，formal C 要栈上整结构；③ 对齐 pad 曾压在 [rsp+0] 偏移字段 |
| **根修** | G.7：`glue_emit_struct_type_let_init` sret 扩 48\|49 + let_ty size；`pipeline_asm_push_sysv_memory_by_value`；METHOD_CALL import 路径 sret_sh + MEMORY 栈传 + pad 在高地址 |
| **验收** | Ubuntu L2：`examples/cookbook/vec_i32_push.x` **-backend asm run=0**；rv=42；wa C=0；math_asm=0；with_arena asm run=5 residual；freestanding 仍 CG002 elf_ec；**日常 L2 · 非 tip L4** |

### residual 下一波

1. （旁）asm with_arena `v.ptr[i]` 元素 residual（len 已绿）  
2. （可选）METHOD_CALL 非 lit 实参 SSE 全覆盖  
3. （旁）prove residual：`backend_enc_dispatch` -E / `rt_diag_errno`；freestanding CG002 elf_ec  

```text
构建级别：L2（pipeline_x.o + backend_call_dispatch + g05；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：vec_i32 asm run=0；wa asm run=5；rv=42；math_asm=0
纪律：测试 hard timeout；本波未升 tip L4
feat SHA：ed8e5c6a · pad fix：2d9da1ce
日志：/tmp/ubuntu_g05_vec_abi2.log
```
