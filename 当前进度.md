# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `31ff798f`（feat diagnostic getenv pure 深迁 + Ubuntu 金标） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **49**（diagnostic 仍 R2 thin；本波 H↓） |
| **H** | **diagnostic rest T 41→39**（env_debug_pipe + parse_strict_enabled pure-dup 剔除）；fmt_check rest T=24；driver_abi rest T=173；仍 Cap residual walk/stat/argv/BSS/one_file_body / uname/setrlimit/pthread/path-read/snprintf/pipe_note |

## 本波：Track R · `diagnostic` **getenv pure 深迁** ✅

| 路径 | 变更 |
|------|------|
| `runtime_driver_diagnostic_thin.x` | pure 真体：`driver_env_flag_truthy` + `driver_diag_env_debug_pipe`（`SHUX_DEBUG_PIPE`）+ `driver_parse_strict_enabled`（`SHUX_PARSE_STRICT`）；pipe orch 调 pure public |
| `runtime_driver_diagnostic.from_x.c` | FROM_X 下剔除 pure-dup env_debug_pipe/parse_strict `_impl`；去掉 `driver_parse_strict_enabled`→`_impl` 宏映射 |
| `runtime_driver_diagnostic_thin_surface.from_x.c` | prove 锁 thin surface；call-site compound lit env 名 |

- 验收：mac/Ubuntu `prove` → **diagnostic IDENTICAL**；rest T **39**；hybrid `shux check` OK；`-E` OK
- KPI：**H↓** rest 业务 T **41→39**
- **不是** thin 表面 KPI：getenv 真体在 thin.x；Cap residual 仍 rest（pipe_note/report_prefixed/va_list/snprintf/debug_log/scratch）

## 下一波前排

1. **R2 真迁 / Cap residual 深迁**（禁止只改注释/surface）：`fmt_check` 余下 Cap residual（walk/stat/argv/BSS/one_file_body）/ Cap-fn-ptr / diagnostic pipe_note·report_prefixed residual / driver_abi uname·setrlimit·pthread  
2. **Track M** typeck M2 smoke  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档；报数写 **rest 业务 T 前后**
