# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **43** IDENTICAL（附注） |
| **R2** | **2**（`rt_util` + `rt_lib_root` full） |
| **H** | `rt_util`/`rt_lib_root` **H=0**；其余 runtime hybrid 多仍 rest C |
| Cap-empty-str | ✅ 已由 W-string 解锁 |

## 本波：Track R · `rt_lib_root` R2 真迁 ✅

| 路径 | 变更 |
|------|------|
| `src/runtime/rt_lib_root.x` | 吃满 `ptr_usable` + `default`（getenv+拷贝）+ `roots_from_key`（emit sidecar + 扁平行 512） |
| `seeds/rt_lib_root.from_x.c` | 三业务符号均 `#ifndef SHUX_RT_LIB_ROOT_FROM_X`；FROM_X 下 rest 仅前向 + marker |
| `seeds/rt_lib_root_surface.from_x.c` | full surface 3 符号（prove 锁） |
| `tests/prove_module_selfhost.sh` | 注释：R2 full |

- 验收：`bash tests/prove_module_selfhost.sh rt_lib_root` → **IDENTICAL**（3 符号）
- 全量：**43 IDENTICAL**
- 产品 hybrid：thin 3×T 业务 + rest 1×T marker → 业务 H=0
- 语义烟测：default env/empty/null、from_key n=0/multi/bad llen → **SMOKE_OK**

## Ubuntu 金标准（`64b37d13`，2026-07-14 14:54 CST）

| 项 | 结果 |
|----|------|
| `rt_lib_root` R2 full | **IDENTICAL**（3 符号；业务 H=0） |
| 全量 | **43 IDENTICAL** |

## 前排下一步

1. 下一批 R2：`rt_content` / `rt_emit_flags` / Cap-global-bss → `rt_arena_buf`
2. Track M：typeck M2 试替换
3. 纪律：commit → push → Ubuntu → 金标一行

## 方法备注

后 T 主线：**Cap → 批量 R2 → M 并行**；禁止为 N 再 surface 下一叶子。
