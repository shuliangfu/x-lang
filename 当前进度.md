# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `56e9ccb8`（feat driver_abi getenv pure 深迁 + call-site lit fix + Ubuntu 金标） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **49**（driver_abi 仍 R2 thin；本波 H↓） |
| **H** | **driver_abi rest T 183→176**（7 getenv 门闩 pure-dup 剔除）；fmt_check rest T=24；仍 Cap residual uname/setrlimit/pthread/path-read/BSS/format/数值 env |

## 本波：Track R · `driver_abi` **getenv pure 深迁** ✅

| 路径 | 变更 |
|------|------|
| `runtime_driver_abi_thin.x` | pure 真体：`driver_env_flag_truthy` + 7 公共门闩（force_c / asm_skip / emit_heavy / module_only / metric / no_large_stack / sanitize_address get） |
| `runtime_driver_abi.from_x.c` | FROM_X 下剔除 pure-dup getenv `_impl` |
| `runtime_driver_abi_thin_surface.from_x.c` | prove 锁 thin surface；**call-site** `getenv("NAME")`（勿全局 let u8[] → 悬空 init_globals） |

- 验收：mac/Ubuntu `prove` → **driver_abi IDENTICAL**；rest T **176**；hybrid `shux check` OK；`-E` OK
- KPI：**H↓** rest 业务 T **183→176**
- **不是** thin 表面 KPI：产品链 getenv 门闩在 thin.x；Cap residual 仍 rest（uname/pthread/数值 env/debug_pipe reportf）

## 下一波前排

1. **R2 真迁 / Cap residual 深迁**（禁止只改注释/surface）：`fmt_check` 余下 Cap residual（walk/stat/argv/BSS/one_file_body）/ Cap-fn-ptr / diagnostic debug residual / driver_abi 数值 env·uname  
2. **Track M** typeck M2 smoke  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档；报数写 **rest 业务 T 前后**
