# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin** · 🟢 **slice 形参指针 ABI 半收口**（本波） |
| **parser residual** | ✅ 匿名 `{ let }` · 🟢 **M1**…`parse_into*` 真 parse · 🟡 **M2 re-pin**（force 链上可编，parse 行为红） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排** | parser **M2 re-pin**（链上换 Cap force `parser_x.o`） |
| **诊断** | Cap force `-E` parser.x → host-cc/link 可过；链上 **SIGSEGV** → 后 **P001** 解析最小 `function main` 失败 |
| **根因（ABI）** | Cap 把 `u8[]` 形参发成 **by-value**，seed/glue 要 **`struct shux_slice_* *`**；by-value 进 glue 把 `"function"` 字节当指针（`0x6e6f6974636e7566`） |
| **根修（codegen）** | ① 形参 TYPE_SLICE 后补 ` *` ② `field_access_base_is_pointer_param` 含 SLICE ③ INDEX 对指针/slice 形参发 `->data` ④ `emit_call_arg_slice_abi`（`.x`；seed C 同步 ①–③） |
| **M2 force 结果** | ABI 对齐后 force **host-cc+g05 可链**；**parse 仍红**（`return-value` P001 `pr.ok=-1`）→ **不 pin force parser** |
| **产品** | **恢复 seed `parser_x.o`**；codegen slice ABI 入 seed pin；mac 矩阵 **rv=42 / opt=102 / si / hello 绿** |
| **未做** | force parse 行为根修后才 re-pin；tip L4；Ubuntu SHARED |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | codegen：TYPE_SLICE 形参 by-value + INDEX 硬编码 `.data` |
| 谁存储 | force `parser_gen` 与 glue 形参 ABI 分叉 |
| 谁消费 | g05 链上 `parser_x.o` / `lexer_next_into` / collect_imports_glue |
| 同模式 | 一切 `source: u8[]` 形参与 glue 边界 |
| 最小回归 | `/tmp/slice_field.x` → `source->length`；mac rv/opt/si/hello；**禁** force pin 至 parse 绿 |

### residual 下一波

1. Cap force `parse_into*` **行为**（非 ABI）根修 → 再 M2 re-pin  
2. 补全 seed 侧 `emit_call_arg_slice_abi`（本地 `slice` 传参 `&`）  
3. SHARED Ubuntu；升 tip 须明示  

```text
构建级别：日常 L2（codegen slice ABI seed pin + seed parser；非 L4）
产品 tip L4：5a636984
parser M2：force 可编可链，parse 行为仍红 → 未 re-pin
产品矩阵：mac rv/opt/si/hello ✅（seed parser）
```
