# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` tip **`96f1453c`** |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `96f1453c`**（Ubuntu 金标 + mac Darwin/arm64；真冷全擦 + 产品 `shux_asm`；driver_abi Cap residual pure wave1–6 整波收口） |
| **D Stage2** | ✅ Ubuntu freestanding D-03 @ `c3031883`（**≠** 产品链；勿把 `shux_asm2` 当产品） |
| **E R2** | ✅ 解冻抽检 13/13 @ `c3031883`；diagnostic Cap residual pure rest T **28→…→0**（wave6）；driver_abi Cap pure rest `_impl` **32→6**（wave1–6；剩 OS Cap 真锁） |
| **T / EMPTY / N** | **18/18** · **18/18** · **54/54** |
| **Track L SYM / FAIL** | **0** / **0** |

## 验证审计（用户追问：以前的步 Ubuntu 都真测了吗？）

**诚实结论：不是每一步都做了 Ubuntu 真测；更不是 tip 一直 L4。**

| 波段 | Ubuntu 证据 | 构建级别 | 备注 |
|------|-------------|----------|------|
| Cap-T001 / Track L / EMPTY 递进 | 多数有 `/tmp/ubuntu_*` 日志 + dual progress commit | **日常真测 L2/L3** | 双端 prove/diff 有落盘 |
| 全表 prove N=54 | `/tmp/ubuntu_prove_full_11f3ad55.log` | L2/L3 | 非 L4 |
| L4+bstrict 123 | `/tmp/ubuntu_true_cold_6da9f4cd.log` + `/tmp/ubuntu_true_bstrict_5a81af60.log` | **L4 真冷** | 产品放行面曾钉此 |
| Stage2 D-03 / R2 抽检 | `ubuntu_stage2_c3031883` / `ubuntu_r2_unfreeze_c3031883` | 专项 | Stage2 产物 **≠** 产品 g05 |
| **diagnostic Cap residual pure wave1–3** | **提交当时无 Ubuntu 日志** | 仅 mac L2/L3 | **缺口** |
| wave4 初版 | 曾只报 mac；补跑 L2 双端 | L2 hybrid | 树内仍有旧 `.o`，**不足以**当 tip 产品结案 |
| **tip 补救 L4（今日）** | remaining_o=0 → bootstrap+g05 → 矩阵 → bstrict | **L4** | 见下 |

### 14 号类假路径（今日 L4 实锤）

1. **旧产物**：L2 hybrid 带着 141 个旧 `.o` 矩阵绿 ≠ 全擦真冷。  
2. **错误二进制**：`run-all-bstrict` 在存在 `compiler/shux_asm2` 时 **优先 cp Stage2 freestanding（static ~5.3MB）→ shux**，盖掉产品 `shux_asm`（dyn ~3.3MB）→ ensure_std **SEGV**（`codegen_bytes=zu`）。  
3. **根修**：bstrict 默认只 `cp shux_asm`；`SHUX_BSTRICT_USE_ASM2=1` 才用 gen2。Stage2 后须隔离/删 `shux_asm2`/`stage1`/`experimental` 残留。  
4. **注释闸门**：wave2 英文 docblock `* without:` 触发 `scan_fmt_damage` star_decl → `4866f65d` 已改。

### tip 双端 L4 结果（`96f1453c` · driver_abi Cap residual pure wave1–6 收口）

| 端 | remaining_o | 矩阵 | prove 全表 | bstrict | 日志 |
|----|-------------|------|------------|---------|------|
| **Ubuntu** Linux/x86_64 | 0 起编 | rv=42 / opt=102 / hello=0 / si | 54 IDENTICAL | **123/123** | cold `/tmp/ubuntu_true_cold_96f1453c.log` · matrix `/tmp/ubuntu_l4_matrix_96f1453c.log` · bstrict `/tmp/ubuntu_true_bstrict_96f1453c.log` |
| **macOS** Darwin/arm64 | 0 起编 | rv=42 / opt=102 / hello=0 / si | 54 IDENTICAL | **123/123** | cold `/tmp/mac_true_cold_96f1453c.log` · matrix `/tmp/mac_l4_matrix_rerun_96f1453c.log` · prove `/tmp/mac_l4_prove_dabi_96f1453c.log` · bstrict `/tmp/mac_true_bstrict_96f1453c.log` |

```text
构建级别：L4 真冷（双端全擦 .o + 重链 shux/shux_asm/shux-c）
SHA：96f1453c（driver_abi Cap residual pure wave1–6 整波双端 L4 钉盘；含 bstrict 禁 Stage2 asm2）
SHUX：本波 g05 产品 shux_asm（非 stage1 / 非 freestanding asm2）
平台：macOS=✅ / Ubuntu=✅
防卡：矩阵单步 timeout 180s；Ubuntu bstrict wall 5400s + -k 30；短轮询 mtime/idle
```

历史 tip L4@`3d2f7d94`（diagnostic rest T=0）/ `02c5d234` 仍为前序放行面；**现放行 tip = `96f1453c`**。

## 本波摘要

| 波 | 结果 |
|----|------|
| **driver_abi Cap residual pure wave1–6 → tip 双端 L4** | pure 叶耗尽（flag-slot / path-len / format print / defines+os_lit / phase timing / flush）；Cap rest `_impl` **32→6**（剩 OS 真锁）；双端 L4 真冷 + 矩阵 + prove 54 IDENTICAL + **bstrict 123/123** @ `96f1453c` |
| 前序 diagnostic Cap residual pure wave6 → L4 | rest T=0 闭环 @ `3d2f7d94` |
| bstrict 假路径根修 | 默认禁止 Stage2 `shux_asm2` 冒充产品 shux |

**构建级别（本波结案）**：**L4 真冷全测**（双端）。  

| 端 | prove | Cap pure rest | g05 | 矩阵 | bstrict |
|----|-------|---------------|-----|------|---------|
| macOS | 54 IDENTICAL | pure 叶 0（剩 OS 6） | OK | rv=42 / opt=102 / hello / si | **123/123** |
| Ubuntu | 54 IDENTICAL | pure 叶 0（剩 OS 6） | OK | rv=42 / opt=102 / hello / si | **123/123** |

## 本波摘要（driver_abi Cap residual pure wave1）

| 波 | 结果 |
|----|------|
| **driver_abi Cap residual pure wave1** `88e7b540` | **9× flag-slot BSS 真迁 thin.x**；FROM_X rest 无 pure-dup flag_slot；**rest _impl 32→23**；surface IDENTICAL；**双端** g05 hybrid + 矩阵；**.x 英文 G.9**；日常 L2/L3 → **已并入 tip L4@`96f1453c`** |
| **driver_abi Cap residual pure wave2** | **path/len BSS 真迁 thin.x**；**rest _impl 24→18**；日常 L2/L3 → **已并入 tip L4@`96f1453c`** |
| **driver_abi Cap residual pure wave3** `fd1cacf2` | **format print 真迁 thin.x**；**rest _impl 18→12**；日常 L2/L3 → **已并入 tip L4@`96f1453c`** |
| **driver_abi Cap residual pure wave4** | **defines_set_at + os_define_lit 真迁 thin.x**；**rest _impl 12→9**；日常 L2/L3 → **已并入 tip L4@`96f1453c`** |
| **driver_abi Cap residual pure wave5** `d2ff2fcd` | **phase timing BSS + begin/end 真迁 thin.x**；**rest _impl 9→7**；日常 L2/L3 → **已并入 tip L4@`96f1453c`** |
| **driver_abi Cap residual pure wave6** `51dc7b8d` | **phase_timing_flush 真迁 thin.x**；**Cap residual rest _impl 7→6**；日常 L2/L3 → **tip 双端 L4 钉盘 @ `96f1453c`** |

## 下一波前排

1. **E 真迁续**：driver_abi Cap residual **OS 真锁**（uname / setrlimit / pthread / path-read IO / gettimeofday / call_fn · 剩余 Cap rest `_impl`=6）或 fmt_check Cap residual / Stage2 WPO  
2. **禁止**把单叶 IDENTICAL / Stage2 / 抽检写成完全自举（产品放行 = tip 双端 L4@**`96f1453c`** 直至新 tip L4）  
3. 动 SHARED 产品面 → 新 SHA 再双端 L4（勿沿用旧 tip 日志）  



