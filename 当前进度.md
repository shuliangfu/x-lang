# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin** · 🟢 slice 形参指针 ABI 半收口 |
| **parser residual** | ✅ body-lets · 🟢 hoist-safe while/for/expr_stmt/unsafe · 🟢 **parser M2 re-pin** · 🟢 **Ubuntu L2 矩阵** |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排** | parser M2 re-pin 后 SHARED Ubuntu L2 矩阵 |
| **叶 0 链** | 初 g05：`UNDEF parser_skip_one_top_level_let_into_glue` |
| **根因** | 陈旧 `parser_asm_thin_glue.o`（Makefile/g05 未把 monothin 全部 `.inc` 当依赖） |
| **修复** | 强制 `make parser_asm_thin_glue.o` + g05；补全 Makefile deps + g05 `-nt` 检 glue_tail/library_wrap 等 |
| **Ubuntu L2 矩阵** | **rv=42** · **option=102** · **si=0** · **hello ✅**（`Hello World` + `0a`）· be run=3 |
| **日志** | `/tmp/ubuntu_parser_m2_l2_matrix_5d088e92.log` |
| **未做** | tip L4；Ubuntu L4 真冷全量 bstrict；defer/region/with_arena hoist 根修 |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | monothin `#include glue_tail_slice.inc` 提供 `*_let_into_glue`；只改 `.inc` 不重建 thin.o |
| 谁存储 | `parser_asm_thin_glue.o` 入 g05 57 objs |
| 谁消费 | pin `parser_x.o` 包装 `parser_skip_one_top_level_let_into` → U 链 glue |
| 同模式 | 其它 monothin `.inc` 若未列 make/g05 依赖均会藏旧符号 |
| 最小回归 | Ubuntu L2：parser_x.o + thin_glue + g05 + rv/opt/si/hello；**禁 tip L4 叙事** |

### residual 下一波

1. 审计/根修 region · with_arena · defer 同款 mid-path `let append_*`（pin hoist 风险）  
2. 用户明示时再双端 **tip L4 + bstrict 123**  
3. 本叶 deps 修合入后 g05 应自动重建 thin（勿再手 rm 当结案）

```text
构建级别：日常 L2（Ubuntu parser pin + thin_glue 重建；非 L4）
产品 tip L4：5a636984
parser M2 re-pin：@ 5d088e92
Ubuntu 矩阵：rv=42 / opt=102 / si=0 / hello ✅
deps fix：Makefile + g05_ensure（本 commit）
```
