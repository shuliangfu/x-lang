# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `37a79c79`（feat diagnostic debug_log/parser_diag pure 深迁 + Ubuntu 金标） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **49**（diagnostic 仍 R2 thin；本波 H↓） |
| **H** | **diagnostic rest T 37→33**（debug_log + parser_diag_tok_kind/ident_len/scan_fail pure-dup 剔除）；fmt_check rest T=24；driver_abi rest T=173；仍 Cap residual walk/stat/argv/BSS/one_file_body / uname/setrlimit/pthread/path-read/snprintf/scratch/va_list |

## 本波：Track R · `diagnostic` **debug_log / parser_diag pure 深迁** ✅

| 路径 | 变更 |
|------|------|
| `runtime_driver_diagnostic_thin.x` | pure 真体：`driver_debug_log` + `parser_diag_tok_kind` / `ident_len` / `scan_fail`（`SHUX_DEBUG_PARSE` getenv 非空 **或** parse_strict + append_cstr/i32 + note，无 va_list `diag_reportf`） |
| `runtime_driver_diagnostic.from_x.c` | FROM_X 下剔除 pure-dup 四函数 `_impl`；冷启动保留 C 体 |
| `runtime_driver_diagnostic_thin_surface.from_x.c` | prove 锁 thin surface；-E 抛光 regen |

- 验收：mac/Ubuntu `prove` → **diagnostic IDENTICAL**；rest T **33**；hybrid OK；`-E` OK
- KPI：**H↓** rest 业务 T **37→33**
- **不是** thin 表面 KPI：pure 真体在 thin.x；Cap residual 仍 rest（va_list x_pipeline_code / scratch / snprintf residual）

## 下一波前排

1. **R2 真迁 / Cap residual 深迁**（禁止只改注释/surface）：`fmt_check` 余下 Cap residual（walk/stat/argv/BSS/one_file_body）/ Cap-fn-ptr / diagnostic scratch·va_list residual / driver_abi uname·setrlimit·pthread  
2. **Track M** typeck M2 smoke  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档；报数写 **rest 业务 T 前后**
