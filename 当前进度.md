# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`33a9c32d`**（fmt print overload mid → io stub） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm hello** | ✅ **Ubuntu L2 run=0**（`std_fmt_print_u8_ptr_i32`） |
| **freestanding CG002** | ✅ **emit 关**（`580af4a1`） |
| **fs vec** | ❌ **BLD001 residual**（co-emit libc/heap mangle + nostdlib U malloc） |

## 本波摘要（2026-07-18 · fmt print mid → stub）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步自举」；前排 freestanding link mangle / nostdlib heap |
| **地图** | 先复现：hosted hello **红** `U std_fmt_print_u8_ptr_i32`；hosted vec/boundary 绿；fs r42/hello 绿；fs vec BLD001（heap_libc mid + runtime_heap_user malloc） |
| **根因（hello）** | `std.fmt` 多 overload → import-binding mid=`print_u8_ptr_i32`；io stubs / redirect 仅 bare `std_fmt_print`（string-lit 路径）；G.7 权威半残 |
| **根修** | 补全 redirect mid→bare + stubs 导出 `std_fmt_print_u8_ptr_i32` / `println_…`（同 TU 转调 bare） |
| **验收** | Ubuntu L2：rv=42 · opt · si · **hello run=0** · vec boundary run=0 · math_asm run=0 · fs r42/hello 绿；**fs vec 仍 BLD001**；**日常 L2 · 非 tip L4** |

### residual 下一波

1. （旁）freestanding co-emit **link mangle / nostdlib heap**（export bare vs call `_usize`；禁 fs 链 `runtime_heap_user`/libc；page_mmap 面）  
2. （可选）f64 let-init binop residual  
3. （旁）typeck cfg-extern skip；ELF `.bss` 真共享 top-level；labi `need_vec` 补 `len_empty`  

```text
构建级别：L2（ast_pool_bootstrap_glue + runtime_asm_io_stubs；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：hello 绿 · fs vec BLD001 residual · 矩阵四件套+vec_b+math
纪律：测试 hard timeout；本波未升 tip L4
feat SHA：33a9c32d
```
