# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（**不**每波改；仅纪律/门禁变时改）

## 文档分工（已定）

| 文档 | 每波 |
|------|------|
| `analysis/自举进度.md` | **必更** KPI / 前排 / 变更一行 |
| `当前进度.md` | 当天阻塞 |
| `自举方法.md` | 仅「怎么做」变了才改 |

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `g-02f-self-hosting` |
| bootstrap / G-FFI-5 | ✅ mac（W-tail 后 relink） |
| **T** | mac **17/18**（+ast；仅 lexer TIMEOUT） |
| **N** | 5 driver 叶 |

## 本波：W-tail ✅

1. **症状**：`ast.x` #199 `ast_expr_disallows_implicit_tail` 等 sole `unsafe { return ...; }` 误报 implicit tail。  
2. **根因**：`func_body_tail_expr_ref_for_implicit_rule` 先信 `final_expr_ref`；sole unsafe body 外层 fin 可能是陈旧 **EXPR_LIT**，挡住 region peel。  
3. **次序（勿颠倒）**：  
   - final 已是 RETURN/PANIC/BREAK/CONTINUE → 用 final（`let; unsafe{x=f()}; return x`）  
   - 否则末条 unsafe region → peel  
   - 否则 final / expr_stmt  
4. **落点**：`typeck.x` + 热路径 `typeck_gen`（`patch_typeck_gen_lang007.py`）+ `pipeline_glue.c`（mega 收敛）。  
5. **mac 地图**：typeck **17/18**；`ast` OK。

## 下一步

1. Ubuntu 金标（relink typeck_x + map + G-FFI-5）  
2. W-heap-overload  
3. Track L prove `compile`/`emit`  
