# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting`（本波合入后 pin） |
| 产品 | 双端 bstrict **123** @ `233e3b72`（L4 历史；本波未重跑 L4） |
| **T** | **18/18** typeck |
| **EMPTY** | **18/18**（lexer CODEGEN 收口） |
| **N** | **54/54 IDENTICAL**（全表 @ `11f3ad55`） |
| **R2 / H** | 可解冻抽检 |
| **Track L SYM** | **0** |
| **FAIL residual** | **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **M2 EMPTY 18/18** | lexer plain `-E` 恢复；EMPTY surface + 产品 `lexer_gen.linux` 不动 |
| **根因 1** | `pipeline_codegen_try_mark_enum_field_access` 不认 `import.Enum.Variant` → 补 `pipeline_enum_name_from_field_access_base`；mark 写在 emit 同 arena |
| **根因 2 / 源** | `token.Token { kind: token.TokenKind.VARIANT }` 仍可 entry-emission 失败；lexer 改用 **TokenKind 序数**（与 seed `token_TokenKind_TOKEN_*` 同值） |
| **产品** | rv=42（`-backend c`）· hello 绿；产品 pin 未改 |

**构建级别**：日常真测 L2/L3（**非** L4）。  
日志：mac `/tmp/mac_diff_empty_18.log` · Ubuntu（push 后）

## 下一波前排

1. **L4 + 双端 bstrict**（谈 Stage2 前强制）  
2. **可选根修**：`Token { kind: Enum.Variant }` 仍红（probe `/tmp/lex_probe/mid.x`）  
3. **禁止**用 N/T/EMPTY 单独宣称「可自举」  
