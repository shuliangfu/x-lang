# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `g-02f-self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **8** IDENTICAL（7 driver 叶 + `lsp_io_std_heap`） |
| **diagnostic** | `-E`+cc ✅；**CORE_OK**（非 IDENTICAL / 非退役） |
| **W-unsafe-expr（std 源）** | ✅ 批量 let+stmt 规避 |

## 本波：Track L 退役 `lsp_io_std_heap` ✅

**事实**：prove 已 **IDENTICAL**，但构建仍 pin 工作区 `lsp_io_std_heap_gen.c`。

**落地**：

| 路径 | 变更 |
|------|------|
| `Makefile` | `lsp_io_std_heap_x.o` ← `driver_leaf_x_to_o.sh` + rename；冷启动 `seeds/lsp_io_std_heap_gen.linux.x86_64.c` |
| `g05_ensure_relink_prereqs.sh` | 并入 Track L leaf 表；去掉 workspace gen 回退 |
| `DRIVER_SEED_PREREQS` | 不再依赖 `lsp_io_std_heap_gen.c` |

- 验收：`bash tests/prove_module_selfhost.sh` → **8 IDENTICAL** + diagnostic **CORE_OK**
- 产品链：`make relink-shux` 不读 workspace gen

## Ubuntu 金标准（`5bf1fa53`，2026-07-14）

| 项 | 结果 |
|----|------|
| prove | **8 IDENTICAL** + diagnostic **CORE_OK** |
| T 地图 | **18/18** typeck OK |
| leaf 路径 | `driver_leaf_x_to_o: lsp_io_std_heap_x.o ← .x (PREFER_X_O)` + relink OK |

**注意**：`typeck_gen.c` 被 gitignore；Ubuntu 须跑 `patch_typeck_gen_lang007` 后 `make typeck_x.o` + `relink-shux`。

## 下一步

1. diagnostic hybrid 收敛（thin/rest 单一权威）或接受 CORE 长期门禁  
2. codegen 根修表达式 `unsafe`（可选；std 源侧已规避）  
3. 每波：**commit → push → Ubuntu prove/T → 再下一步** 
