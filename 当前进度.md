# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `e0c21f57`（M2 EMPTY **10/18**） |
| 产品 | 双端 bstrict **123** @ `233e3b72`（L4 历史；本波未重跑 L4） |
| **T** | **18/18** typeck（mac+Ubuntu） |
| **EMPTY** | **10/18**（token + preprocess + ast + 7 driver） |
| **N** | **54/54 IDENTICAL**（全表 @ `11f3ad55`） |
| **R2 / H** | 可解冻抽检 |
| **Track L SYM** | **0** |
| **FAIL residual** | **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **M2 EMPTY preprocess** | re-pin seed = `-E` + `shux_slice` 布局（cold `cc` 需要）；harness 对 `-E` 侧注入同定义行再 diff |
| **M2 EMPTY ast** | 新增 durable `seeds/ast_gen.linux.x86_64.c`（仅 EMPTY 地图；无 Makefile 产品 pin） |
| **产品探针** | g05 后 hello / option / rv(`-backend c`) 绿（Darwin asm CG002 已知） |

**构建级别**：日常真测 L2/L3（**非** L4）。  
日志：mac `/tmp/mac_diff_empty_pp_ast.log` · Ubuntu `/tmp/ubuntu_diff_t_e0c21f57.log`

## 下一波前排

1. **EMPTY 继续**：diagnostic / lsp* / mega residual（lexer CODEGEN）  
2. **L4 + 双端 bstrict**（谈 Stage2 前强制）  
3. **禁止**用 N/T/EMPTY 单独宣称「可自举」  
