# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `c1735385`（Track L `rt_run_compiler_parsed`；**SYM 0**） |
| 产品 | 双端 bstrict **123** @ `233e3b72`（L4 历史；本波工程未重跑 L4） |
| **T** | **10/18**（mega；未重跑） |
| **N** | **51/54 IDENTICAL**（Ubuntu 记账；相对 `70666dcb` 全表 35 单叶 **+16**）；mac 同叶绿 → 全表预期 **52/54** |
| **R2 / H** | 仍冻结（N 未满；剩 FAIL 3） |

## 本波摘要

| 波 | SHA | 结果 |
|----|-----|------|
| Track L 同型 no_mangle 链 | …`86820ebf` | …rt_run_asm_backend 双端 IDENTICAL |
| **Track L rt_run_compiler_parsed** | **`c1735385`** | 六十八 surface helper/step `#[no_mangle] export`；**mac+Ubuntu IDENTICAL**；**Track L SYM 清零** |

**产生点**：`src/runtime/rt_run_compiler_parsed.x` 模块前缀 mangle `rt_run_compiler_parsed_pp_*` / `pi_*` / `rt_cp_*` vs seed surface 短名。  
**权威**：`compiler/src/runtime/rt_run_compiler_parsed.x`（G.9 英文）；`driver_run_compiler_parsed` 入口不变。  
**构建级别**：日常真测 L2/L3 单叶 prove（**非** L4）。

**剩余 3（Ubuntu）—— 全为 FAIL（无 SYM）**：
- `backend_try_inline_dispatch` / `backend_call_dispatch`（cc -c 非法 C / `-E` FAIL）
- `lsp_io_std_heap`（seed cc 失败；mac 绿）

日志：`/tmp/mac_prove_rt_run_compiler_parsed.log` · Ubuntu `/tmp/ubuntu_prove_rt_run_compiler_parsed_c1735385.log`

## 下一波前排

1. **FAIL 3 单列**（优先归层：codegen 产物 vs seed 路径；**勿与 SYM 搅**）  
2. **T / Track M** 说明向  
3. 禁未稳 N 谈 Stage2；动 SHARED 才 L4；可选全表 prove 复核 N=51  
