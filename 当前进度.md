# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`34268d99`**（import METHOD_CALL SysV SSE；**无** `std_math_*` 名门控） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm with_arena / vec product** | ✅ **Ubuntu L2 build/run=0** |
| **asm string smoke** | ✅ **copy_proof run=0**（前波） |
| **asm math run** | ✅ **math_asm run=0 · 类型路径 · 无名门控** |

## 本波摘要（2026-07-18 · 收敛 std_math 名门控）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排收敛 `std_math_*` 名门控 |
| **根因** | 产品把 `math.floor(x)` 解析为 **METHOD_CALL**（非 CALL+FIELD_ACCESS）；import-binding method 路径只把实参放 **GP/rdi**，formal `std_math_*` 读 **xmm0**；名门控在 `enc_call_redirected` 用 `movq rax→xmm0` 糊绿 |
| **根修** | G.7：METHOD_CALL import 路径与 CALL 共用 SSE 分类（param / FLOAT_LIT / resolved）+ spill 后进 xmm0–7；CALL 后 type harvest；**删除** `std_math_*` 名门控 |
| **验收** | Ubuntu L2：math_asm **build=0 run=0**；产物 floor/ceil 见 `66 48 0f 6e`（movq→xmm0）；rv=42；wa=0；**日常 L2 · 非 tip L4** |
| **旁 residual** | abs/signum 实参为 binop 结果时仍可能走 rdi（xmm0 常因前序 subsd 残留而碰巧对）；METHOD_CALL 形参 map 可再补强 |

### residual 下一波

1. rest C leaf  
2. （旁）asm freestanding vec 运行时 ABI residual  
3. （可选）METHOD_CALL 非 lit 实参 resolved/param type → SSE 全覆盖（abs/signum 硬绿）

```text
构建级别：L2（backend_call_dispatch + g05；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：math_asm run=0（无名门控）；rv=42；wa=0
纪律：测试 hard timeout；本波未升 tip L4
feat SHA：34268d99
```
