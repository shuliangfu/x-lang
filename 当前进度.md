# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin** · 🟢 slice 形参指针 ABI 半收口 |
| **parser residual** | ✅ 匿名 `{ let }` · 🟢 **M1**…`parse_into*` · 🟢 **hoist-safe 源修** · 🟡 **M2 re-pin**（force **rv=42**；binop 仍红） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排** | Cap force `parse_into*` **let-hoist 行为**根修（非 ABI） |
| **诊断** | force emit 把 while 体全部 `let x = f()` 提到循环顶：`allow_legacy`/`type_ref`/`n_*` 用空 `res` 求值；`func_alloc_slot` 每轮抢槽 → P001 |
| **根修（parser.x）** | `parse_into` / `parse_into_buf` **hoist-safe**：先 zero-decl，控制流后赋值（与 `codegen_emit_func_link_name` 同纪律） |
| **M2 force 结果** | force host-cc+g05 可链；**return-value `rv=42`**；**binary-expr 仍 P001/XP003** → **仍不 pin force parser** |
| **产品** | **仍 seed `parser_x.o`**；仅合入 `parser.x` 源修；mac seed **rv=42 / binary-expr=3** |
| **未做** | force 扩绿（binop/body）后才 re-pin；tip L4；Ubuntu SHARED |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | product pin X→C：无 stmt_order 时 while 体 let-init 全提到块顶 |
| 谁存储 | force `parser_gen` 中 `parse_into_buf` while 的 decl 序 |
| 谁消费 | 每轮迭代：`allow_legacy`/`type_ref`/`n_*`/`alloc_slot` 与 lexer_next 错序 |
| 同模式 | 一切「while 内 late `let x = f(res)` / alloc」在 pin 路径上 |
| 最小回归 | force 链 return-value=42；seed 链 rv+binary-expr；**禁** force pin 至 binop 等绿 |

### residual 下一波

1. Cap force 扩绿：`return 1+2` / body let 等（仍 P001）→ 再 M2 re-pin  
2. 可选：codegen 保留 stmt_order / 副作用 let 不提前求值（产生点层）  
3. SHARED Ubuntu；升 tip 须明示  

```text
构建级别：日常 L2（seed parser + parser.x hoist-safe 源修；非 L4）
产品 tip L4：5a636984
parser M2：force rv=42；binop 仍红 → 未 re-pin
产品矩阵：mac seed rv/be ✅
```
