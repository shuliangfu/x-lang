# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin** · 🟢 slice 形参指针 ABI 半收口 |
| **parser residual** | ✅ body-lets · 🟢 hoist-safe param/block/while/for · 🟢 onefunc expr_stmt · 🟢 unsafe · 🟢 **parser M2 re-pin**（force **hello** ✅） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排** | Cap force **hello** host-cc：`core_fmt_fmt_*` / `io_libc_*` 缺失 |
| **叶 A while** | pin X→C 把 `let while_idx = append_while(...)` 提到 parse 前 → while 全红 |
| **根修 A** | block+onefunc while/for/loop hoist-safe：顶 zero + 成功后再 append |
| **叶 B assign-after-if** | onefunc 循环顶 `push_body_expr_stmt(0)` + 陈旧 `stmt_start` → if 后赋值失败 → 丢 `fmt_bool_to_buf` 等 |
| **根修 B** | onefunc 循环顶 zero `stmt_start/expr_stmt_res/ex_i`；parse 成功后 push；bare block 同模式 |
| **叶 C unsafe** | `let unsafe_idx = append_unsafe(0)` 先 hoist → 丢 `unsafe { return shux_sys_* }` → 无 `std_io_sync_io_libc_*` |
| **根修 C** | block+onefunc unsafe hoist-safe：先 parse_block 再 append |
| **M2 force 结果** | **rv=42** · **option=102** · **si=0** · **hello ✅**（`Hello World` + `0a`）· **be 13/13** · let-const 绿 |
| **产品** | **parser M2 re-pin** → `seeds/parser_gen.linux.x86_64.c`；mac pin 矩阵全绿 |
| **未做** | tip L4；Ubuntu SHARED 真冷 |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | pin X→C let-hoist：while/for append、onefunc push_body_expr_stmt、unsafe append 副作用提前 |
| 谁存储 | Block/OneFunc 侧车 while/expr_stmt/unsafe body_ref；模块函数表 |
| 谁消费 | typeck 可见函数集；codegen co-emit / mangle |
| 同模式 | region/with_arena/defer 等同款 side-effect let 可继续审计 |
| 最小回归 | force/pin：rv+option+si+hello+be+let-const；**禁 tip L4 叙事** |

### residual 下一波

1. SHARED Ubuntu L2/L4 矩阵（用户明示 tip 时再双端 L4+bstrict）  
2. 审计 region/with_arena/defer 等同款 hoist  
3. 升 tip 须明示  

```text
构建级别：日常 L2（parser.x + seed re-pin；非 L4）
产品 tip L4：5a636984
parser M2：force hello ✅ → re-pin ✅
产品矩阵：mac pin rv=42 / opt=102 / si / hello ✅ / be ✅
```
