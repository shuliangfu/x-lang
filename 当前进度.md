# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（**不**每波改；仅纪律/门禁变时改）

## 文档分工（已定）

| 文档 | 每波 |
|------|------|
| `analysis/自举进度.md` | **必更** KPI / 前排 / 变更一行 |
| `当前进度.md` | 当天阻塞 |
| `自举方法.md` | 仅「怎么做」变了才改 |

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `g-02f-self-hosting` |
| bootstrap / G-FFI-5 | ✅ mac（W-escape） |
| **T** | +lsp_io +**emit** |
| **N** | 5 driver 叶 |

## 本波：W-escape ✅

1. **根因（glue）**：`pipeline_typeck_check_call_struct_stack_escape_c` 把同帧两个 `&local_struct` 当成 outer；已放行「另一实参也是 block-local」。  
2. **生效面**：检查函数嵌在 **pipeline_x 静态副本** 中，改 `pipeline_glue.c`  alone 不立刻影响 -E。  
3. **emit 源改（地图立即绿）**：  
   - `emit_copy_lib_roots_to_ctx(*u8 key, *ctx)` 避免 param `*DriverXEmitState` + `&ctx`  
   - `run_x_emit_x` 对 `pipeline_run_x_pipeline_impl` **unsafe 直调**（同 main.x）

## 下一步

1. Ubuntu 验证本 commit  
2. W-tail（ast）  
3. Track L prove `compile`/`emit`  
