# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`6a945f5b`**（NL-07 L2 `fflush` nostdlib stubs） |
| 产品 L4 放行 | 仍钉 **`5a636984`** 双端 L4+123（**未**升 tip 全擦 L4） |
| tip 双端 bstrict | ✅ **123/123 @ `36ac3d14`**（g05 + SKIP_BUILD；**非 L4**；本波未重跑全量） |
| **std.vec 产品 -o** | ✅ **双端** formal `vec.o` + on-demand · `with_arena_vec_push` |
| **NL-07 硬绿 L1** | ✅ **multi-def=0** |
| **NL-07 硬绿 L2** | ✅ **`fflush` 有定义**（stubs；err 中 fflush 行=0） |
| **NL-07 crt0 全包** | 🔴 unique UNDEF≈**283**（head=`backend_enc_*`；**≠** 零 libc 硬绿） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · NL-07 L2 fflush）

| 波 | 结果 |
|----|------|
| **入口** | 前排硬绿分层第 2 层：stdio 桩 `fflush`（**非** tip L4） |
| **根因** | 2026-07-16 为防 g05 动态链盖掉 glibc `fflush`（-E 截 24KiB）在 stubs **故意不定义** `fflush`；crt0 `_start` hosted 枝 + `bootstrap_flush_stdio_and_exit` 仍 **引用** → nostdlib 静态链 UNDEF |
| **根修** | **`6a945f5b`**：在 `seeds/bootstrap_nostdlib_stubs.from_x.c` **补全** `fflush`（stub FILE* 无缓冲 write → no-op 成功）；**仅** nostdlib 入链，g05 不链 stubs → glibc 仍权威；G.7 **不**在 freestanding_io 开第二套 |
| **验收** | Ubuntu TRY_BUILD：`multi=0`；err **fflush 行=0**；unique UNDEF≈**283**（前排 `backend_enc_*` / typeck…）；`nm stubs.o` → `T fflush` |
| **门禁** | v4 gate **OK**（`bootstrap nostdlib final link OK` 匹配 loose；**仍无** `bootstrap nostdlib crt0 link OK` — crt0 bag 仍 UNDEF 红） |
| **产品** | Ubuntu `function main(): i32 { return 42; }` → **rv=42**；**禁止** 当 crt0 零 UNDEF 硬绿 / tip L4 升钉 |
| **日志** | `/tmp/ubuntu_n07_l2_6a945f5b.log` · err `compiler/build_asm/.bootstrap_nostdlib_link_err` |

### 五问（NL-07 L2 fflush）

| 问 | 答 |
|----|-----|
| 谁产生 | crt0 hosted 枝 `fflush@PLT` + stubs `bootstrap_flush_stdio_and_exit` 内 `extern fflush`；历史故意不定义 |
| 谁存储 | `seeds/bootstrap_nostdlib_stubs.from_x.c` → `src/asm/bootstrap_nostdlib_stubs.o` |
| 谁消费 | nostdlib `ld`（`SHUX_BOOTSTRAP_NOSTDLIB=1`）；g05 动态产品 **不** 链本 TU |
| 同模式 | 其它 stdio 已在 stubs；freestanding_io 只做 `shux_sys_*` — **禁止** 再写第二 `fflush` |
| 最小回归 | Ubuntu TRY_BUILD：err **无 fflush**；multi=0；仍允许 L3+ UNDEF 红 |

### residual 下一波

1. 用户明示时 tip **L4 真冷** + 双端 bstrict 123（升放行钉盘）  
2. NL-07 **硬绿 L3**：`backend_enc_*` / emit companion 入链或兼容桩表完整化（一条债一层）  
3. NL-07 L3+ 后：typeck / driver / lsp companion  
4. 其它产品面 / Cap  

```text
构建级别：日常 L2（NL-07 L2 fflush；非 tip L4 全擦）
产品 tip L4 钉盘：5a636984
tip HEAD：6a945f5b
NL-07 L1+L2：multi=0 · fflush 关 · crt0 仍红（UNDEF≈283 · head backend_enc）· gate final-link 绿
产品矩阵抽检：Ubuntu rv=42
```
