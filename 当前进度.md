# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **本波代码叶见 commit**（std.vec.o 产品入链）· 文档随波更新 |
| 产品 L4 放行 | 仍钉 **`5a636984`** 双端 L4+123（**未**升 tip 全擦 L4） |
| tip 双端 bstrict | ✅ **123/123 @ `36ac3d14`**（g05 + SKIP_BUILD；**非 L4**；本波未重跑全量） |
| **std.vec 产品 -o** | ✅ **formal `vec.o` + labi/C-path on-demand** · `with_arena_vec_push` mac 绿 |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · std.vec.o 入链）

| 波 | 结果 |
|----|------|
| **compile weak** | `shux_compile_std_module.sh`：preamble weak `std_vec_len_empty` / `std_vec_vec_len_empty` 与 `mod.x` 强定义冲突 → 有强定义时 strip weak（对齐 `args_iter_*` 模式）→ **formal `std/vec/vec.o` OK** |
| **labi plan** | plan **58→59**：追加 `std/vec/vec.o`（fk0）；`.x` + seed + surface 同 commit；`default_std_rel` **41→42** |
| **fk0 探针** | mega `labi_std_fk0_user_needs_rel`：`std_vec_new_retVec_*` / `push` / `len_Vec_*`（**不**单靠 weak `len_empty`） |
| **C 后端 invoke_cc** | `need_vec` + push `std/vec/vec.o`；`have_vec_body` **必须看函数体 `{`**，禁止用 `int32_t std_vec_*(` 误判 extern 为 co-emit（历史假绿根因） |
| **探针** | mac `tests/mem/with_arena_vec_push.x`：**-o 绿 · run rc=0**；rv=42 · hello · option 绿 |
| **残余** | Ubuntu 金标本波 g05 + with_arena 待 push 后复证；tip **L4 全擦** 未升；全量 bstrict 本波未重跑 |

### 五问（std.vec.o 入链）

| 问 | 答 |
|----|-----|
| 谁产生 | compile：preamble weak + mod 强定义同 TU；link：labi plan 缺 vec；C 路径 need 缺 / have_body 误杀 |
| 谁存储 | `std/vec/vec.o` · labi_std plan/fk0 · invoke_cc argv |
| 谁消费 | 用户 `-o` ld/cc（with_arena_vec UNDEF） |
| 同模式 | set/map/queue link_only；其它 preamble weak 与 std 强定义冲突 |
| 最小回归 | formal compile vec.o · with_arena_vec -o · rv/hello · 纯 return-value 不拖 vec |

### residual 下一波

1. **Ubuntu** pull 后 g05 + `with_arena_vec_push -o` + 产品矩阵（SHARED 金标）  
2. 用户明示时 tip **L4 真冷** + 双端 bstrict 123（升放行钉盘）  
3. 可选 **NL-07 v4 try**  

```text
构建级别：日常 L2/g05（非 tip L4 全擦）
产品 tip L4 钉盘：5a636984
with_arena_vec -o：mac OK run=0
矩阵：rv=42 / hello / option 绿
std.vec.o：formal compile OK + plan/fk0/need_vec 入链
```
