# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `dfa6ea6b`（feat fmt_check set_current_file/print/cwd_fallback pure 深迁 + Ubuntu 金标） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **49**（fmt_check 仍 R2 thin；本波 H↓） |
| **H** | **fmt_check rest T 24→21**（set_current_file + print_collected + cwd_fallback pure-dup 剔除）；diagnostic rest T=28；driver_abi rest T=173；仍 Cap residual walk/stat/argv/大 BSS/one_file_body / uname/setrlimit/pthread/path-read/snprintf/va_list |

## 本波：Track R · `fmt_check` **set_current_file / print / cwd_fallback pure 深迁** ✅

| 路径 | 变更 |
|------|------|
| `fmt_check_cmd_thin.x` | pure 真体：`driver_check_set_current_file`（512B BSS 字节拷贝）+ `driver_check_print_collected_diagnostics`（lsp human）+ `fmt_walk_cwd_fallback`（getcwd + walk public） |
| `fmt_check_cmd.from_x.c` | FROM_X 下剔除三函数 pure-dup `_impl`；冷启动保留 C 体；`check_one_file_body` 调 public set_current_file |
| `fmt_check_cmd_thin_surface.from_x.c` | prove 锁 thin surface；-E 抛光 regen；g05/prove 剥 getcwd redecl |

- 验收：mac/Ubuntu `prove` → **fmt_check IDENTICAL**；rest T **21**；hybrid OK；`-E` OK
- KPI：**H↓** rest 业务 T **24→21**
- **不是** thin 表面 KPI：pure 真体在 thin.x；Cap residual 仍 rest（walk opendir/stat/argv/大 BSS/missing-diag/one_file_body）

## 下一波前排

1. **R2 真迁 / Cap residual 深迁**（禁止只改注释/surface）：`fmt_check` 余下 Cap residual（walk/stat/argv/大 BSS/missing-diag/one_file_body）/ Cap-fn-ptr / diagnostic va_list·snprintf residual / driver_abi uname·setrlimit·pthread  
2. **Track M** typeck M2 smoke  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档；报数写 **rest 业务 T 前后**
