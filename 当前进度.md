# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`abfcf576`**（= **产品 L4 放行钉盘**） |
| 产品 L4 放行 | ✅ **钉盘 `abfcf576`** 双端 L4 真冷 + bstrict 123（前钉 `723d5d97`） |
| tip 双端 bstrict | ✅ **123/123 @ `abfcf576`**（L4 全擦；非仅 g05 SKIP_BUILD） |
| **NL-07 v5 / G-03 硬绿** | ✅ Ubuntu **crt0 + 产品 g05** 默认 nostdlib · `ldd` 无 `libc.so` · 矩阵四件套 |
| **std.vec 产品 -o** | ✅ **双端** formal `vec.o` + on-demand · `with_arena_vec_push` |
| **NL-07 硬绿 L1–L6** | ✅ multi=0 · fflush · enc/emit · typeck/driver/lsp · parser/codegen · libc 面 · **crt0 link OK** · unique=0 |
| **NL-07 运行时 L7** | ✅ freestanding `%g`/`%e`；✅ crt0 真 `preprocess_x.o`；✅ crt0 `user_asm_seed_bridge`；✅ **postlink smoke 无 fallback 稳定绿**；✅ 纯 static f64 非 `g.0` |
| **NL-07 L7+ import typeck** | ✅ keep-global parser import APIs → pure static **XT001 关** |
| **NL-07 L8 hello -o** | ✅ pure static **hello -o 绿** run=0 · `Hello World` |
| **NL-07 L8+ si -o** | ✅ pure static **si -o 绿** run=0 · depctx 单表 |
| **NL-07 L9 seed-support** | ✅ crt0 补 experimental 同源 companions · weak atoi · **v5 gate OK** |
| **NL-07 L10 g05/strict 链尾** | ✅ 产品 g05 **static** · shared nostdlib 权威 · 矩阵四件套绿 |
| **freestanding execvp + %.\*s** | ✅ `2db64465` PATH `execvp` · `abfcf576` vsnprintf `*`/`%.*s`（L4 真冷 residual 关） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · tip L4 升钉 @ `abfcf576`）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」：NL-07 L10 后 residual = **tip L4 升钉** |
| **地图** | L4 真冷 @ `3ce21080` 曝 **execvp=裸 execve**（`cc` ENOENT）→ 矩阵/bstrict 红；修后 bstrict 曝 **vsnprintf 无 `%.*s`**（diag 垃圾 `*s`） |
| **根修** | G.7 权威 `bootstrap_nostdlib_stubs.from_x.c`：`2db64465` 真 PATH `execvp` + environ `execve`；`abfcf576` vsnprintf 支持 `*` 宽/精度并尊重 `%s` prec |
| **Ubuntu L4** | remaining_o=0 · g05 · 矩阵 **rv=42 / opt / si / hello** · static · **bstrict 123 RC=0** |
| **macOS L4** | remaining_o=0 · g05 · 矩阵四件套绿 · **bstrict 123** |
| **feat** | `2db64465` + `abfcf576`（L4 验收钉 = tip） |
| **日志** | Ubuntu cold `/tmp/ubuntu_true_cold_abfcf576.log` · bstrict `/tmp/ubuntu_true_bstrict_abfcf576.log` · mac cold `/tmp/mac_true_cold_abfcf576.log` · bstrict `/tmp/mac_true_bstrict_abfcf576.log` |

### residual 下一波

1. pure static 边角（非矩阵四件套）按需  
2. 工程轨 residual（非本波）  
3. freestanding 数学桩 `erfc` 等无限递归 warning 按需（非门禁红）  

```text
构建级别：L4（双端真冷 + bstrict 123 升钉）
产品 tip L4 钉盘：abfcf576（前 723d5d97）
tip HEAD：abfcf576
L4 residual 关：execvp PATH + vsnprintf %.*s
Ubuntu：/tmp/ubuntu_true_cold_abfcf576.log · /tmp/ubuntu_true_bstrict_abfcf576.log
macOS：/tmp/mac_true_cold_abfcf576.log · /tmp/mac_true_bstrict_abfcf576.log
叙事：本波 = tip L4 放行升钉（双端真冷 bstrict 123 绿）
```
