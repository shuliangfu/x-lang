# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `g-02f-self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **13** IDENTICAL（+ `labi_invoke_cc_list`） |
| **diagnostic** | `-E`+cc ✅；**CORE_OK**（非 IDENTICAL / 非退役） |
| **W-string-nul** | ✅ STRING_LIT *u8 尾 `, 0`；slen≤64 |

## 本波：W-string-nul + Track L `labi_invoke_cc_list` ✅

**事实**：前排 list 表 / `labi_diag_pure` 撞 string lit **无 NUL**。根修 `codegen.x`/`codegen_gen` 后，短字面量 `let p: *u8 = "…"; return p` 可 C 互操作。L5 `labi_invoke_cc_list` 用 if/else（无全局表）真迁；prove IDENTICAL 改为只比 **全局业务符号**（忽略 l_constinit / l_.str / stack_chk）。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/codegen/codegen.x` + `codegen_gen.c` (+ linux seed) | STRING_LIT 尾 NUL；slen cap 64 |
| `src/runtime/labi_invoke_cc_list.x` | L5 真迁 |
| `seeds/labi_invoke_cc_list.from_x.c` | 冷启动同构 if/else |
| `scripts/g05_ensure_relink_prereqs.sh` | L5 PREFER_X_O |
| `tests/prove_module_selfhost.sh` | 扩门禁 + 全局符号 IDENTICAL |

- 验收：`bash tests/prove_module_selfhost.sh` → **13 IDENTICAL** + diagnostic **CORE_OK**
- 注意：禁止「函数体仅 `return "lit"`」（parse skip）；用 let+return

## 下一步

1. 下一 labi list pure（ld_list / std_list…）或 diagnostic hybrid→IDENTICAL  
2. W-global 数组；串池 >64；`labi_diag_pure` reportf  
3. 每波：**commit → push → Ubuntu prove → 再下一步**；§8 **东八区**
