# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin** · 🟢 slice 形参指针 ABI 半收口 |
| **parser residual** | ✅ 匿名 `{ let }` · 🟢 **M1**…`parse_into*` · 🟢 **hoist-safe parse_into*** · 🟢 **hoist-safe ret_type_ref**（force **binary-expr 全套绿**）· 🟡 **M2 re-pin**（body let 仍红） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排** | Cap force **binary-expr** 仍 P001：扩绿根修 |
| **诊断** | force `parse_one_function_impl` 把 `let ret_type_ref = parse_type_ref...` **hoist 到函数顶**，在读函数名之前就改 `lex` → 首 token 落在 `(`（FAIL kind=82）；impl 恒失败，仅 glue `buf_into` 兜底 `return N`，**binop/unary/paren/as 全挂** |
| **根修（parser.x）** | `ret_type_ref` / `lex_before_ret` / `type_ref_param` / `lex_before_type` **hoist-safe**：顶 zero-decl，`) :` / 形参 `:` 后再赋值 |
| **M2 force 结果** | 诚实 g05 链 force parser：**rv=42**；**binary-expr 全套 13/13 绿**（含 `1+2`/`-1`/`(1)`/`as`）；**body let 仍 P001** → **仍不 pin** |
| **产品** | **仍 seed `parser_x.o`**；仅合入 `parser.x`；mac seed **rv=42 / be=3** |
| **未做** | body let 扩绿 → M2 re-pin；tip L4；Ubuntu SHARED |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | product pin X→C：无 stmt_order 时函数级 `let x = f(lex)` 提到函数顶并副作用改 lex |
| 谁存储 | force `parser_gen` 中 `parse_one_function_impl` 顶的 `ret_type_ref = parse_type_ref...` |
| 谁消费 | 后续 `lexer_next` / 函数名 IDENT 与类型解析错序 |
| 同模式 | 一切「控制流后的副作用 let」在 pin 路径上（含 body let 下一波） |
| 最小回归 | force：rv=42 + binary-expr 全套；seed：rv+be；**禁 pin** 至 body 等够绿 |

### residual 下一波

1. Cap force **body let** 扩绿（仍 P001）→ 再 M2 re-pin  
2. 可选：codegen 保留 stmt_order / 副作用 let 不提前求值（产生点层）  
3. SHARED Ubuntu；升 tip 须明示  

```text
构建级别：日常 L2（seed parser + parser.x ret_type_ref hoist-safe；非 L4）
产品 tip L4：5a636984
parser M2：force rv+binary-expr ✅；body let 仍红 → 未 re-pin
产品矩阵：mac seed rv/be ✅
```
