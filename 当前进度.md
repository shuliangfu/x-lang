# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin**（mac+Ubuntu 矩阵全绿） |
| **parser residual** | ✅ 匿名 `{ let }` primary 根修（**mac+Ubuntu** @ `a2964988`） |
| **lexer soft residual** | ✅ block-comment 奇次 `"` **硬复现全绿**（本波收口；无代码改动） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **block-comment 奇次 `"` 硬复现** | 历史：`emit_expr` 内 `/* close " ) … } */` 曾 parse-skip（`ffe2dbd1` 应用层改措辞避雷）；`scan_sync` 已 comment-aware（`d23ce5ae`） |
| 硬探针矩阵 | 历史原句 / odd·even / mega~200 stmt / if 内 / doc / multi-fn / line-comment 对照 → **全部 `-o` run=42 + `-E` parse OK** |
| 树扫描 | `compiler/src` **0** 处奇次 `"` 块注释；`std/json` 等仍有奇次 `"` 文档块且可解析（旁证） |
| 产生点结论 | 产品 **lexer** `skip_whitespace_and_comments` 块注释不看 `"`；主路径无复现 bug → **无根修代码** |
| 矩阵 **mac** | **rv=42 / opt=0 / si=0 / hello=0** ✅（本波未改产品二进制逻辑） |
| **未做** | tip L4（须明示） |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | 历史：块注释内奇次 `"` + 注释内 `}` 曾触发 parse-skip / brace 过冲（应用层+scan_sync 已处理） |
| 谁存储 | 曾 onefunc skip → 残体顶层 let；今探针无 skip |
| 谁消费 | typeck/codegen 见完整函数 |
| 同模式 | 任意 `/* … " … */`；主路径 lexer 已忽略注释内引号 |
| 最小回归 | 硬探针矩阵 + 产品矩阵；**无代码改动则不必 Ubuntu 重建** |

### residual 下一波

1. 若升 tip：**双端 L4 真冷 + bstrict 123**（tip 仍 **`5a636984`**；须用户明示「升 tip / 真冷全测」）  
2. 前排软 residual（anon `{ let }` / odd-quote comment）已收口；下一微步待盘 Cap/M 外新债或 tip  
3. **禁** 把本叶 / M2 re-pin / 硬复现绿 写成 tip 产品 L4  

```text
构建级别：日常真测 L2 探针（硬复现；无 L4；无产品代码改动）
SHA：2a9737c0（基线）+ 本波 docs
产品 tip L4：5a636984
lexer residual：odd " in block-comment 硬复现全绿 ✅ 收口
产品矩阵：mac rv/opt/si/hello ✅
```
