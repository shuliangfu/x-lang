# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`a1f67402`**（asm math **link** gate；前 feat `67b61ea8` si） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`** 双端 L4 真冷 + bstrict 123（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`**（L4 全擦；本波日常 L2） |
| **asm hello `std_fmt_print`** | ✅ **`cb4605ee`** |
| **asm stdlib-import (si)** | ✅ **`67b61ea8`** Ubuntu `-backend asm` si run=0 |
| **asm math link (fk=9)** | ✅ **`a1f67402`** Ubuntu build=0；**run residual = f64 SysV codegen** |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · asm math link residual）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」+ **测试 hard timeout**；矩阵 si 已绿 → 扩邻域 |
| **探针** | Ubuntu hang-safe：`rv/opt/si/hello` asm 绿；`math_asm` 先 **U std_math_***；`wa_asm`/`string_asm` 另债（heap+process_argv / string 探针） |
| **根因（math link）** | fk==9 门控只认 `sin/cos` → `tests/math/main.x`（pi/e/floor…）永不推 `math.o` |
| **根修** | G.7 补全 fk==9 探针面 + push 前 formal ensure `math.o`；`have_math` 已拉 libm glue + process_argv |
| **验收** | **L2** + hard timeout；Ubuntu **math_asm build=0** · 矩阵 rv/si/hello/opt/binop 绿；**math_asm run=1**（codegen 另债） |
| **feat** | **`a1f67402`** |

### residual 下一波

1. **asm x86_64 f64 SysV**（call ret/arg/cmp 走 xmm0+comisd；现 GPR+整数 cmp → Ubuntu math_asm run=1）  
2. asm string/wa：fk0 string 探针不全 + heap 入链后 page_mmap/process_argv  
3. 工程轨 rest C 具体叶；**勿**本波 L2 写 tip L4  

```text
构建级别：L2（runtime_link_abi.o + g05；非 L4）
产品 tip L4 钉盘：5c8204ae（本波未升）
tip HEAD：a1f67402
本波：asm math **link** 绿（fk=9 全表面 + formal ensure）
Ubuntu：math_asm build=0 run=1（f64 codegen residual）· 矩阵四件套+binop timeout 绿
mac：math_asm build=0（arm64 路径；金标以 Ubuntu x86_64 为准）
纪律：测试必须 hard timeout，禁止无界挂死
```
