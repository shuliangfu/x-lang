# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin** |
| **parser residual** | ✅ 匿名 `{ let }` · 🟢 **M1 -E** · 🟢 **类型序** · 🟢 **init_globals** · 🟢 **by-value return 成 int 关** · 🟡 **dual-extern / incomplete ast_*** |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排** | parser M1 host-cc：by-value `return 0` 对 struct 非法 + dual-extern incomplete `ast_*` |
| **诊断** | (1) Cap-T001 体后 typeck 填充 `return 0` 被原样发成 C，对 `Lexer`/`OneFuncResult` 等 by-value 报 *returning 'int' from struct*；(2) `parser.x` 作入口时 **ast dep 完整 layout 未 co-emit**（typeck 仅 import ast 则绿；parser 有 lexer/token 污染前缀 `lexer_*` forward 与签名 `ast_*` 双 tag）；(3) path de-dupe 曾允许空槽抢先占路径 |
| **根修** | codegen **权威**（`.x` + seed 同 commit）：`emit_return_stmt_with_context` 对 TYPE_NAMED + lit0 → `return (struct Tag){0};`；`codegen_block_contains_return` + fallback 仅标量；path de-dupe **非空** first-wins；owner-forward 草稿在 `.x`（产品仍 pin seed） |
| **验收** | parser.x `-E` 见 65× compound zero；**by-value int residual 关**；host-cc **仍 dual-extern**（缺 `struct ast_*` 完整定义）；mac 矩阵 **rv=42 / opt/si/hello 绿** |
| **host-cc 现前排** | 🟡 **dual extern / incomplete ast_***（注入 typeck 的 ast 类型块后 dual-extern 消、仅剩 6 处 array init）；`parse_into*` 仍 skip |
| **矩阵 mac L2** | **rv=42 / opt=0 / si=0 / hello=0** ✅ |
| **未做** | tip L4；ast co-emit 根修进 seed 全绿 host-cc；parse_into 真 parse；Ubuntu |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | emit_return_stmt 原样 lit0；skipped_dep 未 emit ast 完整 layout |
| 谁存储 | 生成 C 双 tag / 非法 return 0 |
| 谁消费 | host gcc |
| 同模式 | 任意 Cap-T001 + by-value struct；任意 multi-dep 入口缺 owner layout |
| 最小回归 | compound zero 出现 + 无 *returning int from struct*；矩阵绿；下一叶 dual-extern |

### residual 下一波

1. **dual-extern / incomplete ast_***：根因 = parser 入口 co-emit 未产出 `struct ast_*` 完整体（typeck/ast 单入口可）；需权威保证 owner 模块 layout 必 emit  
2. mega `parse_into*` 真 parse  
3. SHARED Ubuntu；升 tip 须明示  

```text
构建级别：日常真测 L2（codegen_x.o from seed + g05；非 L4）
产品 tip L4：5a636984
parser M1：-E 绿；by-value return 关；dual-extern 仍红
产品矩阵：mac rv/opt/si/hello ✅
```
