# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `f1714116`（feat diagnostic 拼装 pure 深迁 + Ubuntu 金标） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **49**（diagnostic 仍 R2 thin；本波 H↓） |
| **H** | **diagnostic rest T 59→41**（剔除 18 pure-dup）；仍 Cap residual snprintf/va_list/debug |

## 本波：Track R · `diagnostic` **拼装 pure 深迁** ✅

| 路径 | 变更 |
|------|------|
| `runtime_driver_diagnostic_thin.x` | pure 真体：return/assign/call/struct/asm note 拼装 + fill/build/note（append_* 路径，无 snprintf） |
| `runtime_driver_diagnostic.from_x.c` | FROM_X 下剔除 18 pure-dup `_impl`（含 copy_bytes）；冷启动全 C 体仍在 |
| `runtime_driver_diagnostic_thin_surface.from_x.c` | prove 锁 thin surface（同构 thin.x） |
| `g05` / `prove` | 注释标明拼装 pure 深迁；产品仍 PREFER thin hybrid |

- 验收：mac/Ubuntu `prove` → **diagnostic IDENTICAL**；全量 **54 IDENTICAL**
- KPI：**H↓** rest 业务 T **59→41**（gone：return×3 + assign + call×3 + struct×3 + asm note×4 + fill/build/note + copy_bytes pure-dup）
- **不是** thin 表面 KPI：产品链拼装 pure 真体在 thin.x；Cap residual 仍 rest（snprintf/va_list/debug）

## 下一波前排

1. **R2 真迁 / Cap residual 深迁**（禁止只改注释/surface）：`driver_abi` pure-dup / `fmt_check` Cap residual（walk/stat/argv/BSS）/ Cap-fn-ptr / diagnostic 余下 debug residual  
2. **Track M** typeck M2 smoke  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档；报数写 **rest 业务 T 前后**
