# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `58ad4f3b`（feat Ubuntu 金标）/ docs 待推 |
| **T** | **18/18** typeck OK |
| **N** | **53** IDENTICAL（附注） |
| **R2** | **32**（+`labi_std_list`；Ubuntu 金标） |
| **H** | 上述三十二切片 **H=0**；其余 runtime hybrid 多仍 rest C（diagnostic thin… / labi ondemand/gates…） |

## 本波：Track R · `labi_std_list` R2 full 真迁 ✅

| 路径 | 变更 |
|------|------|
| `src/runtime/labi_std_list.x` | 吃满 58 步 plan + default_std_rel 纯表（R2 标注） |
| `seeds/labi_std_list.from_x.c` | 业务符号 `#ifndef SHUX_LABI_STD_LIST_FROM_X`；FROM_X 下 rest 仅前向 + marker → **业务 H=0** |
| Cap residual | IO/ensure/push 仍在 mega `shux_asm_ld_append_std_objs` |
| `seeds/labi_std_list_surface.from_x.c` | full surface（prove 锁） |
| `tests/prove_module_selfhost.sh` | 标注 `labi_std_list` R2 full → surface |

- 验收：mac + Ubuntu `prove_module_selfhost labi_std_list` → **IDENTICAL**；FROM_X rest 仅 `labi_std_list_slice_marker`
- KPI：**R2 31→32**；N **53**（附注）；该切片 H **1→0**
- 产品 hybrid：full .x + mega rest 无已迁 public 体（`#ifndef` 既有）

## 下一波前排

1. **R2** runtime 其余 hybrid rest（diagnostic thin rest… / labi L* list：ondemand/gates…）· Cap-fn-ptr 批量  
2. **Track M** typeck M2 试替换 `typeck_x.o` smoke  
3. 纪律：commit → push → Ubuntu → **更新 `analysis/自举进度.md` + 重写 `当前进度.md`**；KPI **H↓ R2↑**