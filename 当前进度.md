# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **20** IDENTICAL（+ `labi_host_lit`） |
| **diagnostic** | `-E`+cc ✅；**CORE_OK**（非 IDENTICAL / 非退役） |
| **W-string-nul** | ✅ STRING_LIT *u8 尾 `, 0`；slen≤64 |

## 本波：Track L `labi_host_lit`（L2）✅

**事实**：host 探测 thin 转发；C `#if` 主体在 mega rest `*_impl`（与 path_io/gates 同构）；`.x` 无法表达编译期 host 表。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/runtime/labi_host_lit.x` | L2 thin 真迁（→ `_impl`） |
| `seeds/labi_host_lit.from_x.c` | 冷启动同构 thin + U `_impl` |
| `seeds/runtime_link_abi.from_x.c` | `_impl` 常驻 rest（#if）；public under ifndef HOST_LIT |
| `scripts/g05_ensure_relink_prereqs.sh` | L2 PREFER_X_O |
| `tests/prove_module_selfhost.sh` | 扩门禁 |

- 验收：`bash tests/prove_module_selfhost.sh labi_host_lit` → **IDENTICAL**
- g05：`L2 host lit ← labi_host_lit.x (Track L prefer .x)`
- mac：产品 `src/runtime_link_abi.o` 含 `T host_is_*` + `T *_impl` + `T labi_host_lit_count`

## Ubuntu 金标准

> 推送 feat 后在 `ubuntu-server` 跑 prove；通过后补本节与 §8 Ubuntu 行（`date`）。

## 下一步

1. 下一 labi pure（`labi_diag_pure`）或 diagnostic hybrid→IDENTICAL  
2. W-global 数组；串池 >64；`labi_diag_pure` reportf  
3. 每波：**commit → push → Ubuntu prove → 再下一步**；§8 **东八区**（`date`，勿手填）
