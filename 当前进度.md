# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `bcb927c6`（feat fmt_check Cap residual pure 深迁 + Ubuntu 金标） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **49**（fmt_check 仍 R2 thin；本波 H↓） |
| **H** | **fmt_check rest T 35→29**（剔除 6 pure-dup `_impl`）；仍 Cap residual walk/stat/argv/BSS |

## 本波：Track R · `fmt_check` **Cap residual pure 深迁** ✅

| 路径 | 变更 |
|------|------|
| `fmt_check_cmd_thin.x` | pure 真体：`path_should_ignore` / `.x` 后缀 / lint env / `file_list_push` orch / `walk_dir_collect_process_child` / `check_one_finalize_rc` |
| `fmt_check_cmd.from_x.c` | Cap residual `walk_dir_collect_impl` 调 public process_child；FROM_X 下剔除 6 pure-dup `_impl` |
| `fmt_check_cmd_thin_surface.from_x.c` | prove 锁 thin surface（同构 thin.x） |
| `g05` / `prove` | 注释标明 pure 深迁；产品仍 PREFER thin hybrid |

- 验收：mac/Ubuntu `prove` → **fmt_check IDENTICAL**；全量 **54 IDENTICAL**
- KPI：**H↓** rest 业务 T **35→29**（gone：`path_should_ignore_impl` / `fmt_path_ends_with_dot_x_impl` / `check_lint_fail_on_warnings_impl` / `check_one_finalize_rc_lint_impl` / `file_list_push_impl` / `walk_dir_collect_process_child_impl`）
- **不是** thin 表面 KPI：产品链 pure 真体在 thin.x；Cap residual 仍 rest（opendir/stat/argv/BSS）

## 下一波前排

1. **R2 真迁 / Cap residual 深迁**（禁止只改注释/surface）：`fmt_check` 余下 Cap residual（walk/stat/argv/BSS getters）或 `diagnostic`/`driver_abi` pure-dup `_impl` 深迁  
2. **Track M** typeck M2 smoke  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档；报数写 **rest 业务 T 前后**
