# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin**（mac+Ubuntu 矩阵全绿） |
| **parser residual** | ✅ 匿名 `{ let }` · 🟢 **parser M1 -E 绿**（codegen owner-skip 根修） |
| **lexer soft residual** | ✅ block-comment 奇次 `"` 硬复现收口 |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排** | parser M1 codegen residual：`ec=-6` `out_len=34592` 断 `struct parser_Lexer { size_t pos;` |
| **诊断** | layout `Lexer` k=12：field0 `pos` ftr 好；**field1/2 `line`/`col` ftr=0**；dep lexer 侧 ftr 完整（tk=i32） |
| **根因地图** | entry 合入/struct-lit 残布局 + **codegen 仅 `cur_di>=0` 时做 owner skip** → entry 误 emit `parser_Lexer` → field_decl 遇 ftr=0 失败 |
| **根修** | `codegen_emit_module_struct_definitions` / forward_decl：**entry+dep 统一 owner skip**（`owner>=0 && owner!=cur_di`）；`codegen.x` + seed `codegen_gen.linux.x86_64.c` 同 commit |
| **M1 验收** | `shux_asm -E -L .. -L src/lexer -L src/ast -E-extern src/parser/parser.x` → **ec=0** · typeck OK · `codegen_bytes≈321346` · out≈344520 B |
| **矩阵 mac L2** | **rv=42 / opt=0 / si=0 / hello=0** ✅；WPO-S3 负例仍拒 ✅ |
| **host-cc residual** | `-E` 绿后 host gcc 仍红：`LexerResult` 先于 `lexer_Token` 完整定义（dep type 发射序）；**非本叶 blocker** |
| **未做** | tip L4；parser seed re-pin / M2；host-cc 全绿；Ubuntu 金标（SHARED seed 下一波须） |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | codegen 对 entry 不跳 dep-owned layout；entry 残 `Lexer` 字段 type_ref=0 |
| 谁存储 | module.struct_layouts（entry 合入/prime + dep 真源） |
| 谁消费 | `codegen_emit_struct_field_decl_x` → null type_ref 失败 |
| 同模式 | 任意 mega import 后 entry 残布局同名（Token/…） |
| 最小回归 | parser.x `-E` + 产品矩阵 + WPO 负例 |

### residual 下一波

1. **parser M1 host-cc residual**：skipped-dep struct **发射序**（`LexerResult` 引用尚未完整的 `lexer_Token`）  
2. parser M2 / seed re-pin（须 M1 host-cc 或明确范围）  
3. 若升 tip：**双端 L4 + bstrict**（仍 **`5a636984`**；须明示）  

```text
构建级别：日常真测 L2（codegen seed + codegen_x.o + g05；非 L4）
产品 tip L4：5a636984
parser M1：-E 绿（owner-skip）；host-cc 序 residual 地图已立
产品矩阵：mac rv/opt/si/hello ✅
```
