# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `g-02f-self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **10** IDENTICAL（7 driver 叶 + `lsp_io_std_heap` + `token` + `lsp_diag_pipeline_sizes`） |
| **diagnostic** | `-E`+cc ✅；**CORE_OK**（非 IDENTICAL / 非退役） |
| **W-unsafe-expr（std 源）** | ✅ 批量 let+stmt 规避 |

## 本波：Track L 扩 prove `lsp_diag_pipeline_sizes` ✅

**事实**：`lsp_diag_pipeline_sizes.x` 与 seed 仅三枚 `lsp_diag_pipeline_sizeof_*`，nm **IDENTICAL**。

**落地**：

| 路径 | 变更 |
|------|------|
| `tests/prove_module_selfhost.sh` | 增 `lsp_diag_pipeline_sizes\|…\|seeds/lsp_diag_pipeline_sizes.from_x.c` |
| 产品链 | **不变**：已 PREFER_X_O → `sizes_nostub.o`；冷启动 seed |

- 验收：`bash tests/prove_module_selfhost.sh` → **10 IDENTICAL** + diagnostic **CORE_OK**
- KPI **N↑**；**S** 产品侧本已 leaf 化（本波锁 prove 门禁）

## 下一步

1. diagnostic hybrid 收敛（thin/rest 单一权威）或接受 CORE 长期门禁  
2. codegen 根修表达式 `unsafe`（可选；std 源侧已规避）  
3. 每波：**commit → push → Ubuntu prove/T → 再下一步**；§8 时间用 **东八区墙钟**
