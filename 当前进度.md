# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin**（mac+Ubuntu 矩阵全绿） |
| **parser residual** | ✅ 匿名 `{ let }` · 🟢 **parser M1 -E 绿** · 🟢 **host-cc 类型序绿** |
| **lexer soft residual** | ✅ block-comment 奇次 `"` 硬复现收口 |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排** | parser M1 host-cc：`LexerResult` 内嵌 incomplete `lexer_Token`；双 tag `lexer_Token`/`token_Token` |
| **诊断** | (1) owner 对 non-export 同名 **cur 偏好** → lexer+token 双 emit；(2) dep 类型 **di 序** parent 先于 leaf；(3) STRUCT_LIT 用 entry 前缀 → `parser_Token`；(4) topo 去重若认「已 done 同 path」会让 di=2 假 lexer 抢先、di=0 真 lexer 被 skip |
| **根修** | codegen（`.x`+seed 同 commit）：owner 仅 export 时 cur 偏好、non-export 取 **later di**；**import-first** 类型发射；path 去重 **仅 lower di**；STRUCT_LIT 走 `owner_index` 前缀 |
| **类型序验收** | `token_Token` → `lexer_Lexer` → `lexer_LexerResult{ token_Token tok }`；**incomplete type = 0** |
| **host-cc residual** | 🟡 类型序已过；仍红 **init_globals** 提升函数参 `module`/`arena`（mega parser 体）— **下一叶** |
| **矩阵 mac L2** | **rv=42 / opt=0 / si=0 / hello=0** ✅ |
| **未做** | tip L4；parser seed re-pin / M2；host-cc 全绿；Ubuntu 金标（SHARED seed 须） |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | owner_index cur 偏好 + 扁平 di 类型序 + STRUCT_LIT 前缀 |
| 谁存储 | dep 池 path/module；struct_layouts 污染副本（Token is_export=0） |
| 谁消费 | host gcc 完整类型 / compound lit tag |
| 同模式 | 任意 leaf import 按值嵌套 parent 类型 |
| 最小回归 | parser.x `-E` + host-cc incomplete=0 + 产品矩阵 |

### residual 下一波

1. **parser M1 host-cc residual**：`init_globals` 误抬函数参 `module`/`arena`（类型序已关）  
2. parser M2 / seed re-pin（须 host-cc 全绿或明确范围）  
3. SHARED：Ubuntu 验本 seed；升 tip 须明示  

```text
构建级别：日常真测 L2（codegen seed + codegen_x.o + g05；非 L4）
产品 tip L4：5a636984
parser M1：-E 绿；host-cc 类型序绿；init_globals residual
产品矩阵：mac rv/opt/si/hello ✅
```
