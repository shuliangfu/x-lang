# Shux 当前进度（2026-07-04）

## 自举前必须清单状态

**✅ 87 项 / 🟡 9 项 / ⬜ 5 项**

### 三平台验证

| 平台 | shux-c 编译 | shux-c -E | QEMU 内核 gate | 自举清单 |
|------|------------|-----------|---------------|---------|
| macOS (arm64) | ✅ | ✅ | ✅ 30/30 | ✅ 7 节 0 FAIL |
| Ubuntu (x86_64) | ✅ | ✅ | ✅ 30/30 | ✅ 7 节 0 FAIL |
| Windows (x86_64) | ✅ MinGW gcc | ✅ | ✅ 8/8 | 5/7 节通过 |

### Windows 自举清单详情

| 节 | 结果 | 说明 |
|---|------|------|
| §3 语言与 ABI | ✅ PASS | L1-L9 全绿 |
| §4 标准库与 I/O | ✅ PASS | S7 全绿 |
| §5 编译器与工具链 | ❌ FAIL | C2 ✅ C9 ✅ §9.1 ✅，C5 需 shux_asm |
| §6 语义与性能 | ❌ FAIL | result/run-ub 需 shux -o + import |
| §7 自举验证 | ✅ PASS | V6 ✅ V7 ✅ |
| §8 阶段 G | ✅ PASS | G-03 ✅ |
| §9 dogfood | ✅ PASS | X2 ✅ |

Windows FAIL 原因：shux.exe 的 -o 内部编译链在 Windows 上找不到 runtime 辅助 .o 路径。shux.exe -E + 手动 gcc 编译运行 EXIT=42 ✅。

### ⬜ 未完成（5 项，全部需 Linux seed）

| ID | 项 | 原因 |
|----|-----|------|
| G-02a | 删 LEGACY 前端 C（289 .c） | 需 Linux bootstrap seed |
| G-02c | asm/LSP/driver glue C → .sx（118 .c） | 需 Linux |
| G-04 | 全仓库零 C/H | 依赖 G-02a/b/c/d |
| G-07 | 发布 tag | release prep gate 已通过，待打 tag |
| X7 | import 增量/模块缓存 | Defer 自举后 |

### 🟡 部分完成（9 项）

| ID | 项 | 状态 |
|----|-----|------|
| §9.1 | 语义债 | compound-assign OK ✅；i64 CTFE bug 已修；3 WARN 需 asm 后端 |
| C10 | LSP -E | Defer 不阻塞 |
| G-05 | Makefile 退役 | shux-build.sh 已创建 |
| G-FFI-5 | std unsafe + CI | 大部分 ✅，剩 bstrict + CI |
| X1 | mmap 读入 | C 侧已切 runtime_read_file_view，pipeline.sx 待改 |
| X2 | Arena 8B | typeck ✅，full bootstrap 待验 |
| X8 | FFI 深递归 | 1/4 PASS，3 个受 §9.1 阻塞 |
| G-03-rt | compiler-rt 审计 | macOS audit 5/5 ✅ |
| C6 | asm -o | exit=232，ast.sx struct 布局变更影响 |

## 本轮完成的工作

### 1. 内核级特性完善（10 项新特性）

- `asm goto!` 编译器实现（parser/codegen/ast）+ QEMU gate
- 位域 `bitfield :N` 编译器实现 + QEMU gate
- AtomicU32 高层原子类型 + intrinsic 函数 + QEMU gate
- Multiboot2 end tag + 4 字节对齐 gate
- x86_64 长模式 QEMU 启动 gate
- UEFI PE32+ 应用骨架 gate
- IST TSS64+IDTGate64 结构 gate
- 清 .bss + 全局栈 QEMU gate
- 30 个 QEMU 内核 gate 全绿

### 2. shux_asm_stage1 OOM 修复

- `std/fs/posix.sx` ptr[0].field 赋值在 if 后触发 .sx typeck 无限循环
- macOS gate 脚本全部加守卫跳过 shux_asm_stage1

### 3. i64 整数截断全面修复（10 处 bug）

| 文件 | 修复 |
|------|------|
| lexer.c | lex_number int→int64_t + lex_hex_number 不截断 |
| typeck.c | fold_expr int→int64_t（二元+一元 CTFE） |
| typeck.c | AST_EXPR_LIT i32 范围外自动推断 i64 |
| typeck.c | coerce_let_int_lit iv int→int64_t |
| typeck.c | eval_const_int + const_values + typeck_block int→int64_t |
| typeck.c | bce_bound_val int→int64_t |
| typeck.c | 参数推断 v int→int64_t |
| typeck.c | SHL/SHR i64 用 64 位移位 |
| codegen.c | const_folded_val + AST_EXPR_LIT %d→%lld |

验证：INT64_MAX/INT64_MIN/大整数乘法/64 位移位/30 QEMU gate 全通过

### 4. Windows 兼容层（~30 个 commit）

- `win32_compat.h` 统一 POSIX 兼容层（strndup/mmap/fork/dirent/getrlimit/setenv/realpath 等）
- `win32_pipeline_stubs.c` 30+ pipeline/AST/driver 桩函数
- `_WIN32` → `#if defined(_WIN32)||defined(_WIN64)`（MinGW-w64 64 位只定义 _WIN64）
- mmap stub 用 _read 填充文件内容
- runtime.c 硬编码 /tmp → Windows 用 TEMP 环境变量
- .gitattributes LF 行尾保护
- shux.exe + shux-c.exe 编译链接成功

### 5. gate 脚本修复

- BASH_SOURCE[0] 加 :-$0 回退（dash 兼容）
- C2 gate grep pattern 修复
- compound-assign + struct gate 加 shux-c -E + cc 回退
- V6 fresh seed gate Windows 跳过 docker
- smoke 脚本 cc→${CC:-cc}
- multiboot2 gate $((16#...)) → $((0x...)) POSIX 兼容

### 6. 文档更新

- docs/10-内核级特性.md + 三平台验证状态 + 新特性文档
- docs/01-关键字.md + unsafe/asm 关键字
- docs/02-类型定义.md + volatile/位域/repr(C)
- docs/README.md + 构建命令 + 内核工具链
- tests/kernel/README.md + 30 测试表
- 语法文档与实现对照 + i64 修复记录
- analysis/自举前必须清单.md v2.3 + Windows 兼容层

## 环境信息

### SSH 连接

| 系统 | 连接方式 | 项目目录 |
|------|---------|---------|
| macOS | 本机 | /Users/shuliangfu/worker/shu/shux |
| Ubuntu | `ssh ubuntu-server` | /home/shuliangfu/worker/shu/shux |
| Windows | `ssh windows-server` | C:\Users\shuliangfu\worker\shu\shux |

### 操作流程

1. macOS 改代码 → `git push`
2. SSH 到目标系统 → `git fetch origin && git rebase origin/dev`
3. 编译测试

### Windows 工具路径

- gcc: `C:\Users\shuliangfu\mingw64\bin\gcc.exe`
- zig: `C:\Users\shuliangfu\zig\zig.exe`
- QEMU: `C:\Users\shuliangfu\qemu\qemu-system-x86_64.exe`
- Git bash: `"C:\Program Files\Git\bin\bash.exe" -c "..."`
- SSH 免密码登录已配置

### Ubuntu 工具

- zig: `~/bin/zig`（需 `export PATH=$HOME/bin:$PATH`）
- QEMU: 已安装
- `export XDG_CACHE_HOME=/tmp/zigcache`

## 下一步

1. **Ubuntu**：拉取最新代码，重建 shux_asm，验证 i64 修复 + §9.1 语义债
2. **Windows**：修复 shux -o 内部编译链路径解析（runtime .o 路径）
3. **G-02a/c**：在 Linux 上开始 C→.sx 大规模迁移
4. **G-07**：打发布 tag
