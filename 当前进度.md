# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`de6846af`**（NL-07 L7+：crt0 keep-global parser import APIs） |
| 产品 L4 放行 | 仍钉 **`5a636984`** 双端 L4+123（**未**升 tip 全擦 L4） |
| tip 双端 bstrict | ✅ **123/123 @ `36ac3d14`**（g05 + SKIP_BUILD；**非 L4**；本波未重跑全量） |
| **std.vec 产品 -o** | ✅ **双端** formal `vec.o` + on-demand · `with_arena_vec_push` |
| **NL-07 硬绿 L1–L6** | ✅ multi=0 · fflush · enc/emit · typeck/driver/lsp · parser/codegen · libc 面 · **crt0 link OK** · unique=0 |
| **NL-07 运行时 L7** | ✅ freestanding `%g`/`%e`；✅ crt0 真 `preprocess_x.o`；✅ crt0 `user_asm_seed_bridge`；✅ **postlink smoke 无 fallback 稳定绿**；✅ 纯 static f64 非 `g.0` |
| **NL-07 L7+ import typeck** | ✅ keep-global `parser_get_module_num_imports`/`import_path` → 纯 static **XT001 关**；opt -o/run=102；hello/si **typeck OK**（-o 后层 residual） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · NL-07 L7+ pure static import typeck）

| 波 | 结果 |
|----|------|
| **入口** | L7 已关 smoke/f64/rv；前排 pure static hello/opt/si **XT001** check_block |
| **根因地图** | L5 `crt0_l5_parser_partial` keep-global **未列** `parser_get_module_num_imports` / `parser_get_module_import_path` → objcopy 降为 local；pipeline U-ref 绑到 `asm_experimental_symbol_bridge` **弱桩**（num=0 / 空 path）→ **从不 open dep** → typeck 在 main 上 XT001 |
| **证据** | 修前 strace 仅 `hello.x`；修后 open `std/fmt`/`std/io`/…；`nm` 出现 **T** `parser_get_module_num_imports`；`global_T_multi=0` |
| **根修** | **`de6846af`**：`build_shux_asm.sh` L5 parser cascade 补 keep-global 两项（权威仍 `parser_x.o`；G.7 禁第二套桩） |
| **验收 Ubuntu pure static** | crt0 link OK · smoke OK · rv=42 · **opt -o 绿 run=102** · hello **-E typeck OK**（55k C）· si typeck 过 · **global_T_multi=0** |
| **pure static residual（非 XT001）** | hello **-o** link：`std_io_print_u8_ptr_usize` UNDEF；si **-o** host cc：dep emit 顺序/`core_result_*` 隐式声明（**codegen/link 层**，≠ import 弱桩） |
| **双端 g05** | mac g05 rv/opt/hello/si **绿**；Ubuntu 本波 pure static 重建（g05 不链本改路径时不回归） |
| **日志** | build `/tmp/ubuntu_n07_l7p_de6846af.log` · mac `/tmp/mac_g05_n07_l7p.log` |

### 五问（NL-07 L7+ import typeck）

| 问 | 答 |
|----|-----|
| 谁产生 | L5 keep-global 表漏 import 面符号 → 真体变 local |
| 谁存储 | `crt0_l5_parser_partial.o` + experimental_symbol_bridge 弱桩 |
| 谁消费 | pipeline load_import → typeck check_block(main) |
| 同模式 | 与 L7 preprocess/asm bridge 弱桩同源：真物未 keep-global / 未入链 |
| 最小回归 | pure static opt -o；hello -E typeck；smoke；global_T_multi=0；mac g05 |

### residual 下一波

1. pure static **hello -o link**（`std_io_print_*` / std 入链门控）与 **si host-cc emit 序**  
2. 用户明示时 tip **L4 真冷** + 双端 bstrict 123  
3. 默认 `SHUX_BOOTSTRAP_NOSTDLIB=1` / 去系统 pthread（策略文）  

```text
构建级别：日常 L2/L3（NL-07 L7+ import typeck；非 tip L4 全擦）
产品 tip L4 钉盘：5a636984
tip HEAD：de6846af
NL-07 L1–L7 保持；L7+：keep-global parser import APIs · XT001 关 · pure static opt 绿
双端产品矩阵：mac g05 rv=42 / opt / hello / si 绿
```
