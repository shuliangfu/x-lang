# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `460dfc0c`（feat fmt_check one_file 门闩 pure 深迁 + Ubuntu 金标） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **49**（fmt_check 仍 R2 thin；本波 H↓） |
| **H** | **fmt_check rest T 27→26**（check_one_file pure 门闩；gone `check_one_file_impl`）；仍 Cap residual walk/stat/argv/BSS/missing-diag/cwd/one_file_body |

## 本波：Track R · `fmt_check` **one_file 门闩 pure 深迁** ✅

| 路径 | 变更 |
|------|------|
| `fmt_check_cmd_thin.x` | pure 真体：`check_one_file` 门闩 → `body_impl`；`check_try_append_lib_root` 早退；`parse_ignore_opt` `--ignore=` 前缀 |
| `fmt_check_cmd.from_x.c` | FROM_X 下剔除 pure-dup `check_one_file_impl`；Cap residual 改调 public（one_file / try_append / parse_ignore） |
| `fmt_check_cmd_thin_surface.from_x.c` | prove 锁 thin surface（同构 thin.x） |
| `run-fmt-check-cmd.sh` | 路径 regex `shu_`→`shux_`（rename 后漂移，根因修正） |

- 验收：mac/Ubuntu `prove` → **fmt_check IDENTICAL**；全量 **54 IDENTICAL**；`run-fmt-check-cmd` OK
- KPI：**H↓** rest 业务 T **27→26**
- **不是** thin 表面 KPI：产品链 pure 门闩/早退在 thin.x；Cap residual 仍 rest（walk opendir/stat/argv/BSS/missing-diag/cwd/one_file_body）

## 下一波前排

1. **R2 真迁 / Cap residual 深迁**（禁止只改注释/surface）：`fmt_check` 余下 Cap residual（walk/stat/argv/BSS/one_file_body）/ Cap-fn-ptr / diagnostic debug residual / driver_abi getenv 门闩  
2. **Track M** typeck M2 smoke  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档；报数写 **rest 业务 T 前后**
