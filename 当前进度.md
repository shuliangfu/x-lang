# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`f16f7d48`**（feat：顶层 `#undef` + mutable decl-init const 门；含 COMMON/labi/cfg 整波） |
| 产品 L4 放行 | ✅ **钉盘 `f16f7d48`**（双端真冷 L4+123；前钉 `5c4dea78`） |
| tip 双端 bstrict | ✅ **123/123 @ `f16f7d48`** |
| **asm hello / hosted** | ✅ **Ubuntu L4**（rv=42 · opt/si/hello） |
| **toplevel let two_lets** | ✅ **`5e5a0a5b`**（mutable decl-init 仅 C static-const；run=12） |
| **fs co-emit MAP_FAILED** | ✅ **`f16f7d48`**（`#undef` 后再 static const） |
| **asm 跨函数 mutable let** | ✅ **`8232ac62`**（COMMON BSS） |
| **asm vec len_empty labi** | ✅ **`c416015e`** |
| **heap_trace / lit let decl-init** | ✅ **`b2143e2a`** + **`5e5a0a5b`** 门控 |

## 本波摘要（2026-07-18 · tip L4 升钉 `5c4dea78` → `f16f7d48`）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 tip L4 升钉（钉盘仍 `5c4dea78`，tip 含 COMMON/labi/cfg/decl-init 整波） |
| **地图** | 真冷 bstrict 暴露两债：① `two_lets` `static b=(a+2)` 非法 C（`b2143e2a` 过宽 want_decl_init）；② `run-fs` preamble `#define MAP_FAILED` 与 co-emit `static const MAP_FAILED` 双权威 |
| **根修** | ① `pipeline_expr_is_c_static_const_init` 门控 mutable decl-init（`5e5a0a5b`）；② 顶层 let/const 发射前 `#undef` 名（`f16f7d48`） |
| **验收** | **双端 L4 真冷 + 123/123** @ `f16f7d48`；矩阵 rv=42 · opt/si/hello=0 · toplevel_let · fs OK |
| **钉盘** | **`5c4dea78` → `f16f7d48`** |

### residual 下一波

1. （旁）其它产品面 residual 挡闸再开  
2. 常量折叠权威放 IR/WPO（不在 emit 扩）  

```text
构建级别：L4 真冷（全擦 compiler+std+core .o + 重链 shux/shux_asm/shux-c）
产品 tip L4 钉盘：f16f7d48（前钉 5c4dea78）
Ubuntu：cold /tmp/ubuntu_true_cold_f16f7d48.log · bstrict /tmp/ubuntu_true_bstrict_f16f7d48.log · BSTRICT_RC=0 · 123
macOS：cold /tmp/mac_true_cold_f16f7d48.log · bstrict /tmp/mac_true_bstrict_f16f7d48.log · 123 OK
矩阵：rv_build=0 rv_run=42 · opt/si/hello_build=0 · hello_run=0 · toplevel_let OK · fs OK
权威：pipeline_expr_is_c_static_const_init · codegen #undef top-level · seed+codegen.x
纪律：双端真冷齐绿后升钉；禁止日常 L2 冒充 tip L4
```

---

## 前波摘要（2026-07-18 · asm 跨函数 mutable lit let / COMMON BSS）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 residual 真跨函数共享 let |
| **根修** | x86_64：`SHN_COMMON` + RIP lea；禁 RX `.text` |
| **验收** | Ubuntu L2 shared=42；feat **`8232ac62`** |
| **钉盘** | 当时仍 `5c4dea78`（本波已升） |

---
