# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`a0a0fc0e`**（NL-07 link-tail stdout 纯净） |
| 产品 L4 放行 | 仍钉 **`5a636984`** 双端 L4+123（**未**升 tip 全擦 L4） |
| tip 双端 bstrict | ✅ **123/123 @ `36ac3d14`**（g05 + SKIP_BUILD；**非 L4**；本波未重跑全量） |
| **std.vec 产品 -o** | ✅ **双端** formal `vec.o` + on-demand · `with_arena_vec_push` |
| **NL-07 v4 try** | 🟡 track：gate OK（fallback）；**nostdlib crt0 仍红**（真 UNDEF 地图已立） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · NL-07 v4 try）

| 波 | 结果 |
|----|------|
| **入口** | 前排 #2：Ubuntu `SHUX_NOLIBC_N07_V4_TRY_BUILD=1`（**非** tip L4） |
| **首红假因** | `bootstrap_link_tail_crt0/driver` 在 `$(...)` 内调 `ensure_*`，`echo " cc -c ..."` 污染 stdout → 链行被注入 `-c` → `cannot specify '-o' with '-c' with multiple files`（err 仅 2 行，掩盖真 UNDEF） |
| **根修** | **`a0a0fc0e`**：ensure 进度改 `1>&2`；权威 `build_shux_asm.sh`；PLATFORM LINUX/NL-07 |
| **修后真红** | nostdlib crt0 **ld 失败**：unique UNDEF ≈**291**；前缀聚合 backend≫typeck/driver/lsp + `fflush`；另 **multiple definition**（strict_minimal ∩ standalone / preprocess_if_stack） |
| **门禁** | v4 gate **OK**（`build_shux_asm` exit 0 + libc fallback）；**无** `bootstrap nostdlib crt0 link OK`；产物 `ldd` 仍 **libc.so.6** |
| **产品** | Ubuntu 试后 `shux_asm` rv=42 绿；**禁止** 当 nostdlib 硬绿 / tip L4 升钉 |
| **日志** | `/tmp/ubuntu_n07_v4_try_a0a0fc0e.log` · err `compiler/build_asm/.bootstrap_nostdlib_link_err` |

### 五问（NL-07 link-tail 污染）

| 问 | 答 |
|----|-----|
| 谁产生 | `bootstrap_link_tail_*` 内 `ensure_*` 的 stdout 进度行 |
| 谁存储 | `BOOT_CRT0_TAIL=$(…)` / `BOOT_DRIVER_TAIL=$(…)` |
| 谁消费 | `"$CC" … -o shux_asm … $BOOT_*_TAIL` |
| 同模式 | 任何 `$(fn)` 内 echo 进度到 stdout 的 ensure |
| 最小回归 | Ubuntu TRY_BUILD：err 不再是 `-c multi`；进入真实 ld UNDEF 列表 |

### residual 下一波

1. 用户明示时 tip **L4 真冷** + 双端 bstrict 123（升放行钉盘）  
2. NL-07 **硬绿前排**（分层，**勿一波搅多债**）：  
   - crt0 对象集 / 去 multiple-def（glue 拓扑）  
   - `fflush` 进 freestanding_io 或 stubs（与 crt0 对齐）  
   - backend_enc / typeck / driver 入链或 stub 表（按链路权威，禁旁路）  
3. 其它产品面 / Cap  

```text
构建级别：日常 L2（NL-07 try；非 tip L4 全擦）
产品 tip L4 钉盘：5a636984
tip HEAD：a0a0fc0e
NL-07 v4：try 完成 · nostdlib crt0 红（真 UNDEF）· gate fallback 绿
产品矩阵抽检：Ubuntu rv=42
```
