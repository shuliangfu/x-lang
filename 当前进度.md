# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ tip（M2 EMPTY **8/18**） |
| 产品 | 双端 bstrict **123** @ `233e3b72`（L4 历史；本波未重跑 L4） |
| **T** | **18/18** typeck（mac+Ubuntu） |
| **EMPTY** | **8/18**（token + 全 7 driver 叶） |
| **N** | **54/54 IDENTICAL**（全表 @ `11f3ad55`） |
| **R2 / H** | 可解冻抽检 |
| **Track L SYM** | **0** |
| **FAIL residual** | **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **M2 EMPTY compile/emit** | re-pin `driver_compile`/`driver_emit` seed = `-E` + 产品长 rename 表（与 Makefile `DRIVER_*_RENAME` / g05 一致） |
| **harness** | `diff_single_link` 比较前对 compile/emit 应用同 rename；cold seed `cc -c` 产品符号 OK |

**构建级别**：日常真测 L2/L3（**非** L4）。  
日志：mac `/tmp/mac_diff_empty_compile_emit.log` · Ubuntu（push 后补）

## 下一波前排

1. **EMPTY 继续**：preprocess / diagnostic / ast / lsp 等无 rename 叶，或 mega residual  
2. **L4 + 双端 bstrict**（谈 Stage2 前强制）  
3. **禁止**用 N/T/EMPTY 单独宣称「可自举」  
