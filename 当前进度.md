# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`57fa657d`**（NL-07 L3b crt0 seed `backend_emit_*` partial） |
| 产品 L4 放行 | 仍钉 **`5a636984`** 双端 L4+123（**未**升 tip 全擦 L4） |
| tip 双端 bstrict | ✅ **123/123 @ `36ac3d14`**（g05 + SKIP_BUILD；**非 L4**；本波未重跑全量） |
| **std.vec 产品 -o** | ✅ **双端** formal `vec.o` + on-demand · `with_arena_vec_push` |
| **NL-07 硬绿 L1** | ✅ **multi-def=0** |
| **NL-07 硬绿 L2** | ✅ **`fflush` 有定义** |
| **NL-07 硬绿 L3** | ✅ **`backend_enc_*` / arch_emit / try_inline / simd =0** |
| **NL-07 硬绿 L3b** | ✅ **`backend_emit_*=0`**（seed partial export） |
| **NL-07 crt0 全包** | 🔴 unique UNDEF≈**147**（head=`driver_*` / typeck / codegen；**≠** 零 libc 硬绿） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · NL-07 L3b seed backend_emit partial）

| 波 | 结果 |
|----|------|
| **入口** | 前排硬绿分层 L3b：crt0 入链 seed `backend_emit_*`（**非** tip L4） |
| **根因** | `asm_backend_compat_stubs` 转发 `backend_emit_*`；权威在 `seed_host/asm_backend_partial` 的 **weak** 桩；crt0 历史未链 seed；整颗 seed 会与 bag 内 `backend_asm_strict_fallback_alias` **multi-def** `backend_asm_codegen_ast{,_to_elf}` |
| **根修** | **`57fa657d`**：`ensure_crt0_backend_emit_seed_partial_obj` → `ld_partial_export` **仅** `backend_emit_*` T+W → `crt0_backend_emit_seed_partial.o`；`ensure_crt0_backend_companion_objs` 追加（G.7；**禁止** 第二套 emit 桩表） |
| **验收 Ubuntu** | TRY_BUILD：`multi=0`；`backend_emit=0`；`backend_enc=0`；`fflush=0`；unique UNDEF **156→147**；head=`driver_*`/typeck/codegen；**无** `crt0 link OK`；gate final-link 绿 |
| **双端产品矩阵** | mac g05 + rv/opt/hello/si **绿**；Ubuntu g05 + rv/opt/hello/si **绿**（`0a` 换行） |
| **门禁** | v4 gate **OK**（final-link loose）；**仍无** `bootstrap nostdlib crt0 link OK` |
| **日志** | Ubuntu N07 `/tmp/ubuntu_n07_l3b_57fa657d.log` · g05 `/tmp/ubuntu_g05_n07_l3b.log` · mac g05 `/tmp/mac_g05_n07_l3b.log` |

### 五问（NL-07 L3b backend_emit）

| 问 | 答 |
|----|-----|
| 谁产生 | compat_stubs / pipeline 引用 `backend_emit_*`；seed 有 W 桩（`backend_seed_mega_fallback`） |
| 谁存储 | `build_asm/seed_host/asm_backend_partial.o` → partial `crt0_backend_emit_seed_partial.o` |
| 谁消费 | Linux crt0 `ld` 行（`CRT0_BACKEND_COMPANIONS`） |
| 同模式 | experimental 链整颗 seed；crt0 **只** partial 导出 emit 簇（避 multi-def） |
| 最小回归 | err **无 backend_emit_**；multi=0；双端 g05 产品矩阵 |

### residual 下一波

1. 用户明示时 tip **L4 真冷** + 双端 bstrict 123（升放行钉盘）  
2. NL-07 **L4+**：typeck / driver / lsp companion 入 crt0（G.7 同源 experimental）  
3. 其它产品面 / Cap  

```text
构建级别：日常 L2/L3（NL-07 L3b；非 tip L4 全擦）
产品 tip L4 钉盘：5a636984
tip HEAD：57fa657d
NL-07 L1+L2+L3+L3b：multi=0 · fflush 关 · backend_enc=0 · backend_emit=0 · crt0 仍红（UNDEF≈147 · head driver/typeck）· gate final-link 绿
双端产品矩阵：mac+Ubuntu g05 rv=42 / opt / hello / si 绿
```
