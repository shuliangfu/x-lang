# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **17** IDENTICAL（+ `labi_ondemand_list`） |
| **diagnostic** | `-E`+cc ✅；**CORE_OK**（非 IDENTICAL / 非退役） |
| **W-string-nul** | ✅ STRING_LIT *u8 尾 `, 0`；slen≤64 |

## 本波：Track L `labi_ondemand_list`（L8b）✅

**事实**：L5–L8 同构可复用。simple groups（6）+ kv/arrow/time/queue 多符号组 + needs_* rel 常量；if/else + `let p: *u8`；seed 去掉 static 表以便全局符号 IDENTICAL。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/runtime/labi_ondemand_list.x` | L8b 真迁 |
| `seeds/labi_ondemand_list.from_x.c` | 冷启动同构 if/else |
| `scripts/g05_ensure_relink_prereqs.sh` | L8b PREFER_X_O |
| `tests/prove_module_selfhost.sh` | 扩门禁 |

- 验收：`bash tests/prove_module_selfhost.sh` → **17 IDENTICAL** + diagnostic **CORE_OK**

## Ubuntu 金标准（`a05f50e5`，2026-07-14 13:22 东八区）

| 项 | 结果 |
|----|------|
| `labi_ondemand_list` | **IDENTICAL**（产品 `shux` @ worker tree / `self-hosting`） |
| 同批 6 叶 | path + gates + cc_list + ld_list + freestanding + std_list **IDENTICAL** |

## 下一步

1. 下一 labi pure（`labi_ensure_list` / `host_lit` / `path_io` / `diag_pure`）或 diagnostic hybrid→IDENTICAL  
2. W-global 数组；串池 >64；`labi_diag_pure` reportf  
3. 每波：**commit → push → Ubuntu prove → 再下一步**；§8 **东八区**
