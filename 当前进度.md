# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin**（mac+Ubuntu 矩阵全绿） |
| **parser residual** | ✅ 匿名 `{ let }` · 🟢 **parser M1 半步**（Cap-T001 补全 + T001 unsafe 门控） |
| **lexer soft residual** | ✅ block-comment 奇次 `"` 硬复现收口 |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排新债** | soft residual 已空 → 开 **parser M2** 地图：产品仍 pin `parser_gen`；M1 = 当前 `shux_asm` 能 `-E` 消费 `parser.x` |
| **M1 首红** | `typeck error[T001]: struct stack escape` 于 `onefunc_res_wire_dummy_head`（`copy_into(res, &local)`）；**whole-body unsafe 不抑制 T001** |
| **根修 T001** | `pipeline_glue.c`：call/assign stack-escape + post-scan region **尊重 `typeck_unsafe_depth`**（Cap-T001 语义；**非**关 LANG-007） |
| **Cap-T001 补全** | `parser.x` 补 **58** 函数 whole-body `unsafe`（字符串/注释感知 brace 脚本；`parse_block_into` 体长未截断） |
| **M1 现状** | typeck 过 → **codegen 仍红**：entry emit `ec=-6` `out_len=34592`，partial C 断在 `struct parser_Lexer { size_t pos;`（prologue 类型发射中途） |
| **既有 parse-skip** | HEAD 即有：num_funcs≈471/515（`parse_into`/`parse_into_buf` 邻域）+ dep≈39；**非本叶引入** |
| **探针** | unsafe 允许 `&local`+outer ✅ run=7；无 unsafe 仍拒 ✅；WPO-S3 `escape_global` 仍拒 ✅ |
| **矩阵 mac L2** | **rv=42 / opt=0 / si=0 / hello=0** ✅ |
| **未做** | tip L4；parser M1 全绿 / seed re-pin；Ubuntu 金标（SHARED glue 下一波须） |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | WPO-S3 T001 在 call/assign/post-scan **不看** `unsafe_depth`；Cap-T001 仅盖 LANG-007 |
| 谁存储 | `g_typeck_unsafe_depth` 已在 check_block region 推入；post-scan 原先未推 |
| 谁消费 | mega `parser.x` / 用户 `unsafe { &local + *Struct }` |
| 同模式 | 任意 unsafe 内 stack-escape 模式；safe 路径不变 |
| 最小回归 | T001 探针矩阵 + WPO-S3 负例 + 产品矩阵；改 glue 须重建 `pipeline_glue_standalone.o` |

### residual 下一波

1. **parser M1 codegen residual**：entry prologue 断在 `parser_Lexer` 字段发射（`out_len=34592`）；先定产生点（struct field type emit / 错前缀 / parse-skip 残体）  
2. 若升 tip：**双端 L4 + bstrict**（仍 **`5a636984`**；须明示）  
3. **禁** 本叶写成 tip L4；**禁** 关 LANG-007 / `SHUX_SKIP_STACK_ESCAPE` 糊 M1  

```text
构建级别：日常真测 L2（pipeline_glue rebuild + g05；非 L4）
产品 tip L4：5a636984
parser M1：typeck 进 codegen；codegen entry residual 地图已立
产品矩阵：mac rv/opt/si/hello ✅
T001：unsafe 放行 / safe 拒 / WPO 负例 ✅
```
