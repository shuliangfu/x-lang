# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`9f73bb70`**（cfg-extern skip ABI；钉盘仍 `5c4dea78`） |
| 产品 L4 放行 | ✅ **钉盘 `5c4dea78`**（双端真冷 L4+123；**本波非 tip L4**） |
| tip 双端 bstrict | ✅ **123/123 @ `5c4dea78`**（钉盘；本波日常 L2） |
| **asm hello / hosted** | ✅ **Ubuntu L4**（rv=42 · opt/si/hello） |
| **f64 unary / cmp / mul / div** | ✅ **p5_ord / p6_mul / p7_div + METHOD_CALL ret cmp**（入链钉盘） |
| **math_asm import METHOD_CALL** | ✅ **`math.pi()/floor/…` run=0 · ucomisd**（`c28931a6` 入链） |
| **fs r42 / panic / hello** | ✅ **`run-freestanding-hello` OK**（入链） |
| **vec_u16 get** | ✅ **`f67e22a9`**（入链） |

## 本波摘要（2026-07-18 · cfg-extern skip ABI）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；钉盘 `5c4dea78` 已升；开旁 residual cfg-extern |
| **地图** | 非 typeck：`skip_one_extern` 缺 `extern "C"` ABI 串；与 `parse_one_extern` 分叉（G.7） |
| **根修** | `parser_asm_skip_one_extern_into_slice_c` 对齐 parse：可选 TOKEN_STRING 后接 function |
| **验收** | mac L2：keep+skip "C" + global assign + rtshape run=42；矩阵 rv/opt/si/hello；**日常 L2 非 tip L4** |
| **钉盘** | **仍 `5c4dea78`**（docs-only 不升；feat 待 Ubuntu 日常真测） |

### residual 下一波

1. （旁）ELF `.bss` / labi `len_empty`（挡产品面再开）  
2. 其它产品面 residual 挡闸再开  

```text
构建级别：L2（parser_asm_thin_glue.o + g05；非 tip L4）
产品 tip L4 钉盘：5c4dea78（不变）
权威：compiler/seeds/parser_asm/parser_asm_skip_tl_slice.inc → parser_asm_skip_one_extern_into_slice_c
探针：keep+skip extern "C" → main_idx 恢复 · run=42
纪律：parse/skip 同形；禁止只修 leaf 绕过
```

---

## 前波摘要（2026-07-18 · tip L4 升钉 `5c8204ae` → `5c4dea78`）


| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 tip L4 升钉（钉盘仍 `5c8204ae`，tip 含 f64/METHOD_CALL/fs dual-GP 等整波 L2 收口） |
| **地图** | 产品放行 = 全擦 `.o` + 重链 `shux`/`shux_asm`/`shux-c` + 矩阵 + 双端 `run-all-bstrict` 123；**非**日常 L2 冒充 |
| **动作** | Ubuntu：`git pull` → 全擦 → bootstrap-driver-seed + g05 → 矩阵 → bstrict；mac：同构 L4 |
| **验收** | **双端 L4 真冷 + 123/123** @ `5c4dea78`；Ubuntu rv=42 · opt/si/hello=0；mac 同矩阵 + 123 OK |
| **钉盘** | **`5c8204ae` → `5c4dea78`** |

### residual 下一波

1. （旁）cfg-extern / ELF `.bss` / labi `len_empty`（挡产品面再开）  
2. 其它产品面 residual 挡闸再开  

```text
构建级别：L4 真冷（全擦 compiler+std+core .o + 重链 shux/shux_asm/shux-c）
产品 tip L4 钉盘：5c4dea78（前钉 5c8204ae）
Ubuntu：cold /tmp/ubuntu_true_cold_5c4dea78.log · bstrict /tmp/ubuntu_true_bstrict_5c4dea78.log · BSTRICT_RC=0 · 123
macOS：cold /tmp/mac_true_cold_5c4dea78.log · bstrict /tmp/mac_true_bstrict_5c4dea78.log · BSTRICT_RC=0 · 123
矩阵：rv_build=0 rv_run=42 · opt/si/hello_build=0 · hello_run=0
纪律：双端真冷齐绿后升钉；禁止日常 L2 冒充 tip L4
```
