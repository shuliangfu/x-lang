# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`38e11e64`**（FLOAT let-init compound reparse） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm hello / hosted** | ✅ **Ubuntu L2**（opt/si/hello/math） |
| **f64 let-init binop** | ✅ **`0.0 - 1.5` / `1.0+2.5` asm+c run=0** |
| **fs r42 / panic / hello** | ✅ **`run-freestanding-hello` OK** |
| **vec_u16 get** | ✅ **`f67e22a9`**（本波未复链 std.vec ensure） |

## 本波摘要（2026-07-18 · f64 let-init FLOAT compound）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排旁侧 f64 let-init binop |
| **地图** | C `-E`：`let x: f64 = 0.0 - 1.5` → `double x = 0.0; (void)(-(1.5));`；asm 同：store 左字面量 + `neg %eax` |
| **根因** | `parse_body_lets` 的 **TOKEN_FLOAT** 只认首个浮点字面量；复合 `0.0 - 1.5` 余下被当成 expr_stmt（INT 路径已有 compound reparse） |
| **根修** | FLOAT 对齐 INT：peek 后非 plain 终止符则自 float 起点 `parse_expr_into`；`.x` + `parser_gen.linux.x86_64.c` 同 commit（G.7） |
| **验收** | Ubuntu L2：p2/p7/p9 **asm+c run=0**；plain lit/add-of-vars/math **0**；`run-freestanding-hello` OK；opt/si/hello/math 矩阵绿；**日常 L2 · 非 tip L4** |
| **重建** | `parser_x.o`（seed→parser_gen）+ `make shux_asm`（g05 relink） |

### residual 下一波

1. （旁）f64 **ordered cmp**（asm 对负 f64 仍 `cmpq` 位序，p5 `n<=-2.0`）；f64 **mul**（asm `imul` residual）；cfg-extern / `.bss` / labi `len_empty`  
2. tip L4 升钉仍须双端真冷+bstrict（钉盘仍 `5c8204ae`）  

```text
构建级别：L2（parser_x + g05/shux_asm；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：f64 let-init compound=0 · freestanding hello · math/opt/si/hello
纪律：本波未升 tip L4
feat SHA：38e11e64
```
