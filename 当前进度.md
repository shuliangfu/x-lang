# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **43** IDENTICAL（附注） |
| **R2** | **3**（`rt_util` + `rt_lib_root` + `rt_emit_flags` full） |
| **H** | 上述三切片 **H=0**；其余 runtime hybrid 多仍 rest C |
| Cap-empty-str | ✅ 已由 W-string 解锁 |

## 本波：Track R · `rt_emit_flags` R2 真迁 ✅

| 路径 | 变更 |
|------|------|
| `src/runtime/rt_emit_flags.x` | 吃满 `has_emit`（字节比 `-E`/`-E-extern` + 尾递归扫描）+ `set_use_lto` + `set_print_target_cpu`（同构 state 字段） |
| `seeds/rt_emit_flags.from_x.c` | 三业务符号均 `#ifndef SHUX_RT_EMIT_FLAGS_FROM_X`；FROM_X 下 rest 仅前向 → **业务 H=0** |
| `seeds/rt_emit_flags_surface.from_x.c` | full surface 3 公共 + helpers（prove 锁） |
| `tests/prove_module_selfhost.sh` | 注释：R2 full |

- 验收：`bash tests/prove_module_selfhost.sh rt_emit_flags` → **IDENTICAL**（3 公共符号）
- 全量：**43 IDENTICAL**
- 产品 hybrid：thin full .x + rest 空体 → 业务 H=0
- 语义烟测：argv 无/有 `-E`/`-E-extern`、lto/print_target_cpu 字段写入 → **SMOKE_OK**
- **根因坑**：`argv == 0 as **u8`** 会使 `-E` **整函数丢符号**；null 由调用约定保证

## Ubuntu 金标准（`21053467`，2026-07-14 15:01 CST）

| 项 | 结果 |
|----|------|
| `rt_emit_flags` R2 full | **IDENTICAL**（3 公共符号；业务 H=0） |
| 全量 | **43 IDENTICAL** |

## 前排下一步

1. 下一批 R2：`rt_content` path rest · 或 Cap-global-bss → `rt_arena_buf`
2. Track M：typeck M2 试替换
3. 纪律：commit → push → Ubuntu → 金标一行

## 方法备注

后 T 主线：**Cap → 批量 R2 → M 并行**；禁止为 N 再 surface 下一叶子。
