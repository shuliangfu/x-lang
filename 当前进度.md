# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`94f0ae11`**（NL-07 L4+ crt0 typeck/driver/lsp companions） |
| 产品 L4 放行 | 仍钉 **`5a636984`** 双端 L4+123（**未**升 tip 全擦 L4） |
| tip 双端 bstrict | ✅ **123/123 @ `36ac3d14`**（g05 + SKIP_BUILD；**非 L4**；本波未重跑全量） |
| **std.vec 产品 -o** | ✅ **双端** formal `vec.o` + on-demand · `with_arena_vec_push` |
| **NL-07 硬绿 L1** | ✅ **multi-def=0** |
| **NL-07 硬绿 L2** | ✅ **`fflush` 有定义** |
| **NL-07 硬绿 L3** | ✅ **`backend_enc_*` / arch_emit / try_inline / simd =0** |
| **NL-07 硬绿 L3b** | ✅ **`backend_emit_*=0`**（seed partial export） |
| **NL-07 硬绿 L4+** | ✅ **typeck_*=0**；driver 主簇关（残 `driver_run_compiler_check`/`driver_run_fmt`）；lsp 主簇大幅降 |
| **NL-07 crt0 全包** | 🔴 unique UNDEF≈**41**（head=`parser_*` / `codegen_*` / lsp sizes / nostdlib libc 面；**≠** 零 libc 硬绿） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · NL-07 L4+ typeck/driver/lsp companions）

| 波 | 结果 |
|----|------|
| **入口** | 前排硬绿分层 L4+：crt0 入链 typeck/driver/lsp companion（**非** tip L4 真冷） |
| **根因** | bridge/glue 引用 `driver_*` / `typeck_*` / `lsp_*`；experimental 已有同源对象，crt0 历史只链 bag+backend companions |
| **根修** | **`94f0ae11`**：`ensure_crt0_typeck_driver_lsp_companion_objs` → runtime_driver_{abi,diagnostic,asm_strict} + runtime_{pipeline,link,io}_abi + diag + target_cpu + rt_* slices + `driver_*_x.o` + `lsp_*_x.o` + lsp_diag seed；**typeck_x.o 仅当 bag typeck 未自举**（避 multi-def）；并入 `CRT0_BACKEND_COMPANIONS`（G.7；禁第三套桩表 / 禁整行 experimental） |
| **验收 Ubuntu** | TRY_BUILD：`multi=0`；`typeck=0`；`backend_emit/enc/fflush=0`；unique UNDEF **147→41**；head=`parser_*`/`codegen_*`；**无** `crt0 link OK`；gate final-link 绿 |
| **双端产品矩阵** | mac g05 + rv/opt/hello/si **绿**；Ubuntu g05 + rv/opt/hello/si **绿**（`0a` 换行） |
| **门禁** | v4 gate **OK**（final-link loose）；**仍无** `bootstrap nostdlib crt0 link OK` |
| **日志** | Ubuntu N07 `/tmp/ubuntu_n07_l4p_94f0ae11.log` · g05 `/tmp/ubuntu_g05_n07_l4p.log` · mac g05 `/tmp/mac_g05_n07_l4p.log` |

### 五问（NL-07 L4+ typeck/driver/lsp）

| 问 | 答 |
|----|-----|
| 谁产生 | bridge/glue 引用 driver_run_compiler_full / driver_cmd_* / typeck_* / lsp_io_* |
| 谁存储 | experimental 同源 `.o`（runtime_driver_* · driver_*_x · lsp_*_x · typeck_x 条件） |
| 谁消费 | Linux crt0 `ld` 行（`CRT0_BACKEND_COMPANIONS` 追加 TDL） |
| 同模式 | experimental 全行；crt0 **只** multi=0 安全子集（禁 parser_x/codegen_x/x_frontend_link_alias 双链） |
| 最小回归 | typeck residual=0；driver 主簇关；multi=0；双端 g05 产品矩阵 |

### residual 下一波

1. 用户明示时 tip **L4 真冷** + 双端 bstrict 123（升放行钉盘）  
2. NL-07 **L5**：codegen / parser companion（G.7；注意 multi-def）+ 残余 lsp sizes / pipeline_impl  
3. nostdlib libc 面（`fileno`/`isatty`/`puts`/`strerror`…）进 stubs  

```text
构建级别：日常 L2/L3（NL-07 L4+；非 tip L4 全擦）
产品 tip L4 钉盘：5a636984
tip HEAD：94f0ae11
NL-07 L1–L4+：multi=0 · fflush 关 · backend_enc=0 · backend_emit=0 · typeck=0 · driver 主簇关 · crt0 仍红（UNDEF≈41 · head parser/codegen）· gate final-link 绿
双端产品矩阵：mac+Ubuntu g05 rv=42 / opt / hello / si 绿
```
