# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `g-02f-self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **11** IDENTICAL（7 driver 叶 + `lsp_io_std_heap` + `token` + `lsp_diag_pipeline_sizes` + `labi_path_pure`） |
| **diagnostic** | `-E`+cc ✅；**CORE_OK**（非 IDENTICAL / 非退役） |
| **W-unsafe-expr（std 源）** | ✅ 批量 let+stmt 规避 |

## 本波：Track L 扩 prove `labi_path_pure` ✅

**事实**：`labi_path_pure.x` 原 `usize` 字面量/减法 typeck 阻 `-E`；改为 **i32 长度**（对齐 `rt_content.x`）后 `-E` 绿；seed 对齐真 `-E` 后 nm **IDENTICAL**。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/runtime/labi_path_pure.x` | 长度/索引 i32；保留 4 公共 API + `want_exe` + `labi_suffix_eq2/4` |
| `seeds/labi_path_pure.from_x.c` | 对齐真 `-E`（冷启动 / hybrid fallback） |
| `tests/prove_module_selfhost.sh` | 增 `labi_path_pure\|…\|seeds/labi_path_pure.from_x.c` |
| 产品链 | **不变**：g05 L0 已 PREFER_X_O → `g05_try_x_to_o` |

- 验收：`bash tests/prove_module_selfhost.sh` → **11 IDENTICAL** + diagnostic **CORE_OK**
- KPI **N↑**；**S** 产品侧本已 L0 prefer .x（本波锁 prove 门禁）

## Ubuntu 金标准（`28f8809a`，2026-07-14）

| 项 | 结果 |
|----|------|
| `labi_path_pure` | **IDENTICAL**（`shux-c`） |
| 同批 9 叶 | fmt/check/test/build/run + heap + token + sizes + labi **IDENTICAL** |
| compile / emit / diagnostic | 需产品 `shux`（`-E` allow_legacy）；本机 full relink 未齐，未计本波阻塞 |

## 下一步

1. diagnostic hybrid 收敛（thin/rest 单一权威）或下一 labi pure leaf  
2. Ubuntu 补齐产品 `shux` relink → 全量 prove 11+CORE（环境债）  
3. 每波：**commit → push → Ubuntu prove/T → 再下一步**；§8 时间用 **东八区墙钟**
