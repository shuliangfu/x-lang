# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **24** IDENTICAL（+ `fmt_check` thin） |
| **fmt_check** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 42） |
| **driver_abi** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 70） |
| **diagnostic** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 79） |
| **W-string-nul** | ✅ STRING_LIT *u8 尾 `, 0`；slen≤64 |

## 本波：Track L `fmt_check` hybrid→IDENTICAL（L2 thin）✅

**根因地图**：产品 hybrid 已走 **thin.x** + full seed rest（`-DSHUX_L2_FMT_CHECK_THIN_FROM_X`），但 prove 未锁 thin 公共面；与 diagnostic/driver_abi 同构缺口。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/driver/fmt_check_cmd_thin.x` | 权威 thin 公共面（产品本已用） |
| `seeds/fmt_check_cmd_thin.from_x.c` | 冷/prove 同构 thin（`-E` 抛光 + POSIX prologue） |
| `tests/prove_module_selfhost.sh` | 扩 `fmt_check` → thin.x + thin seed；空 mode → **IDENTICAL** |
| full seed rest | 不变（`_impl` / walk·argv 仍 C 尾） |

- 验收：`bash tests/prove_module_selfhost.sh fmt_check` → **IDENTICAL**
- 全量：`bash tests/prove_module_selfhost.sh` → **24 IDENTICAL**
- 产品：g05 仍 `thin.x + seed-rest ld -r` → `fmt_check_cmd_driver.o`（本波不改链接拓扑）

## Ubuntu 金标准（`025b3416`，2026-07-14 13:39 东八区，`date`）

| 项 | 结果 |
|----|------|
| `fmt_check` thin | **IDENTICAL**（产品 L2 thin @ `self-hosting`） |
| 同批 | diagnostic + driver_abi + labi 叶 + **全量 24 IDENTICAL** |

## 前排下一步

1. 下一可 IDENTICAL 叶（dispatch / simd thin / backend dispatch）/ true-migrate rest
2. W-global 数组 / 串池 >64
3. 纪律：commit → push → Ubuntu → docs
