# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `g-02f-self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **12** IDENTICAL（7 driver 叶 + heap + token + sizes + `labi_path_pure` + `labi_gates`） |
| **diagnostic** | `-E`+cc ✅；**CORE_OK**（非 IDENTICAL / 非退役） |
| **W-unsafe-expr（std 源）** | ✅ 批量 let+stmt 规避 |

## 本波：Track L 扩 prove `labi_gates` ✅

**事实**：前排 `labi_diag_pure` 受阻于 string lit 无 NUL / 全局数组 → `static uint8_t*` / va_list reportf（语言限制，非本波补丁）。改选 **L9 `labi_gates`**：thin null-check 转发、无字符串表，`-E` 后 nm 与 seed **IDENTICAL**。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/runtime/labi_gates.x` | 真迁 6 gate + `labi_gates_count`；`*u8` 不透明透传 `_impl` |
| `seeds/labi_gates.from_x.c` | 注释对齐（冷启动保留 C 精确签名） |
| `scripts/g05_ensure_relink_prereqs.sh` | L9 **PREFER_X_O** → `g05_try_x_to_o` |
| `tests/prove_module_selfhost.sh` | 增 `labi_gates\|…\|seeds/labi_gates.from_x.c` |

- 验收：`bash tests/prove_module_selfhost.sh` → **12 IDENTICAL** + diagnostic **CORE_OK**
- KPI **N↑**；产品 L9 prefer .x（失败回退 seed）

## 下一步

1. diagnostic hybrid 收敛 或 下一 labi pure leaf（`labi_diag_pure` 需 W-string/global 根修）  
2. Ubuntu 补齐产品 `shux` relink → 全量 prove 12+CORE（环境债）  
3. 每波：**commit → push → Ubuntu prove/T → 再下一步**；§8 时间用 **东八区墙钟**
