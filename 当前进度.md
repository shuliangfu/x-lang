# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **18** IDENTICAL（+ `labi_ensure_list`） |
| **diagnostic** | `-E`+cc ✅；**CORE_OK**（非 IDENTICAL / 非退役） |
| **W-string-nul** | ✅ STRING_LIT *u8 尾 `, 0`；slen≤64 |

## 本波：Track L `labi_ensure_list`（L4）✅

**事实**：ensure 目录 26 条目（stem / out.o / seed / flags / hint）；if/else + `let p: *u8`；`step_at` 经 accessor 聚合（避免巨型单函数 check_block 失败）；seed 去掉 static 表以便全局符号 IDENTICAL。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/runtime/labi_ensure_list.x` | L4 真迁 |
| `seeds/labi_ensure_list.from_x.c` | 冷启动同构 if/else + step_at accessor |
| `scripts/g05_ensure_relink_prereqs.sh` | L4 PREFER_X_O |
| `tests/prove_module_selfhost.sh` | 扩门禁 |

- 验收：`bash tests/prove_module_selfhost.sh` → **18 IDENTICAL** + diagnostic **CORE_OK**

## Ubuntu 金标准

（push 后在 `ubuntu-server` 复验；§8 时间以 `date` 为准）

## 下一步

1. 下一 labi pure（`labi_host_lit` / `path_io` / `diag_pure`）或 diagnostic hybrid→IDENTICAL  
2. W-global 数组；串池 >64；`labi_diag_pure` reportf  
3. 每波：**commit → push → Ubuntu prove → 再下一步**；§8 **东八区**（`date`，勿手填）
