# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **36** IDENTICAL（+ `rt_parse_diag` thin） |
| **rt_parse_diag** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 1） |
| **rt_run_x_emit** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 1） |
| **rt_pipeline_elf_diag** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 1） |
| **rt_fmt_one** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 1） |
| **rt_dispatch_impl** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 1） |
| **rt_dispatch** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 1） |
| **backend_call_dispatch** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 44） |
| **backend_try_inline_dispatch** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 50） |
| **backend_arch_emit_dispatch** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 47） |
| **backend_enc_dispatch** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 120） |
| **simd_enc** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 74） |
| **simd_loop** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 22） |
| **fmt_check** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 42） |
| **driver_abi** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 70） |
| **diagnostic** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 79） |
| **W-string-nul** | ✅ STRING_LIT *u8 尾 `, 0`；slen≤64 |

## 本波：Track L `rt_parse_diag` hybrid→IDENTICAL（L2 thin）✅

**根因地图**：产品 hybrid 已走 **rt_parse_diag.x** + hybrid rest seed（`-DSHUX_RT_PARSE_DIAG_FROM_X`）入 `runtime_driver_no_c`，但 prove 未锁 thin 公共面。产品 rest seed 含 precise parse body + marker，与 pure thin 同构面分离。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/runtime/rt_parse_diag.x` | 权威 thin 公共面（产品本已用；1 门闩 `runtime_report_precise_parse_failure_if_known`） |
| `seeds/rt_parse_diag_surface.from_x.c` | **新建** 冷/prove 同构 pure thin（`-E` 抛光 + POSIX prologue） |
| `seeds/rt_parse_diag.from_x.c` | **不变**（产品 hybrid rest：precise parse body/macro→_impl + marker） |
| `tests/prove_module_selfhost.sh` | 扩 `rt_parse_diag` → thin.x + surface seed；空 mode → **IDENTICAL** |

- 验收：`bash tests/prove_module_selfhost.sh rt_parse_diag` → **IDENTICAL**
- 全量：`bash tests/prove_module_selfhost.sh` → **36 IDENTICAL**
- 产品：g05 仍 `thin.x + hybrid rest ld -r` 入 `runtime_driver_no_c.o`（本波不改链接拓扑）

## Ubuntu 金标准（`d0f64299`，2026-07-14 14:15 CST，`date`）

| 项 | 结果 |
|----|------|
| `rt_parse_diag` thin | **IDENTICAL**（产品 L2 thin @ `self-hosting`） |
| 同批 | rt_run_x_emit + rt_pipeline_elf_diag + rt_fmt_one + rt_dispatch_impl + rt_dispatch + call_dispatch + try_inline + arch_emit + backend_enc + simd_* + fmt_check + diagnostic + driver_abi + labi 叶 + **全量 36 IDENTICAL** |

## 前排下一步

1. 下一可 IDENTICAL 叶（runtime 其它 hybrid thin）/ true-migrate rest
2. W-global 数组 / 串池 >64
3. 纪律：commit → push → Ubuntu → docs
