# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin** |
| **parser residual** | ✅ 匿名 `{ let }` · 🟢 **M1 -E** · 🟢 **类型序** · 🟢 **init_globals** · 🟢 **by-value return** · 🟢 **dual-extern** · 🟢 **array-init** · 🟢 **`parse_into*` 真 parse** |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排** | parser M1 **`parse_into*` 仍 skip** |
| **诊断** | 产品 parser 解析 mega `parse_into`/`parse_into_buf` 失败 → skip；二分定位到 `if (pending_cfg_skip)` 内 **裸 `{ ... }` 后跟语句** |
| **根因** | `parse_block_into` / onefunc 体循环把语句位 `TOKEN_LBRACE` 当 **block-expr** 并要求 `;`；裸 compound 后无分号 → 整函数 `ok=false` |
| **根修** | ① **parse_block_into + parse_one_function_impl**：语句位裸 `{` → 嵌套 `parse_block_into` + EXPR_BLOCK expr_stmt，**不要求 `;`**（`.x` + seed pin）② **slice 路径** `skip_one_top_level_let_into`（禁 `source as *u8` 非法 cast）+ glue_tail |
| **验收** | parser.x `-E`：`parser_parse_into`/`_buf` **真体**（~1.2k/1.1k 行）；host-cc **error=0**；export 函数 missing=0；mac 矩阵 **rv=42 / opt / si / hello 绿** |
| **矩阵 mac L2** | **rv=42 / opt / si / hello ✅** |
| **未做** | tip L4；Ubuntu SHARED 复证；parser M2 re-pin |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | `parse_block_into` 语句循环：`{` → parse_expr + require `;` |
| 谁存储 | OneFunc/Block AST 不完整 → parse_into skip |
| 谁消费 | M1 `-E` / host-cc（无真 parse_into 体） |
| 同模式 | 任意函数体/if 体内 `{ let … } next_stmt;` |
| 最小回归 | bare_assign 探针；parser.x `-E` 含 parse_into 体；host-cc 0；矩阵 |

### residual 下一波

1. SHARED Ubuntu 复证；升 tip 须明示  
2. parser M2 re-pin（链上换 `parser_x.o` 真物）  

```text
构建级别：日常真测 L2（parser seed pin bare-block + let_into glue + g05；非 L4）
产品 tip L4：5a636984
parser M1：-E 绿；parse_into* 真 parse；host-cc error=0
产品矩阵：mac rv/opt/si/hello ✅
```
