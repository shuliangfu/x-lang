# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **43** IDENTICAL（附注） |
| **R2** | **4**（`rt_util` + `rt_lib_root` + `rt_emit_flags` + `rt_content` full） |
| **H** | 上述四切片 **H=0**；其余 runtime hybrid 多仍 rest C |
| Cap-empty-str | ✅ 已由 W-string 解锁 |

## 本波：Track R · `rt_content` R2 path rest 真迁 ✅

| 路径 | 变更 |
|------|------|
| `src/runtime/rt_content.x` | 吃满 `content_has_*` + `driver_source_has_*`（`peek` extern + `u8[65536]` 栈 + `rt_path_copy_nul`） |
| `seeds/rt_content.from_x.c` | 四业务符号均 `#ifndef SHUX_RT_CONTENT_FROM_X`；FROM_X 下 rest 仅前向 + marker → **业务 H=0** |
| `seeds/rt_content_surface.from_x.c` | full surface（helpers + path wrappers；prove 锁） |
| `tests/prove_module_selfhost.sh` | 注释：R2 full |

- 验收：`bash tests/prove_module_selfhost.sh rt_content` → **IDENTICAL**
- 全量：**43 IDENTICAL**
- 产品 hybrid：thin full .x + rest 仅 marker → 业务 H=0
- 语义烟测：内存 generic/compound/trait/注释跳过；path 包装 + path_len 边界 → **SMOKE_OK**
- **能力点**：`u8[65536]` 局部数组 **已可 -E**；path rest 不再等 Cap-global-bss

## Ubuntu 金标准（`794f0b46`，2026-07-14 15:05 CST）

| 项 | 结果 |
|----|------|
| `rt_content` R2 full | **IDENTICAL**（content_has_* + path wrappers；业务 H=0） |
| 全量 | **43 IDENTICAL** |

## 前排下一步

1. 下一批 R2：runtime 其它 rest 业务切片 · 或 Cap-global-bss → `rt_arena_buf` 真迁 BSS
2. Track M：typeck M2 试替换
3. 纪律：commit → push → Ubuntu → 金标一行

## 方法备注

后 T 主线：**Cap → 批量 R2 → M 并行**；禁止为 N 再 surface 下一叶子。
