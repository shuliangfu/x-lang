# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`b4dbb111`**（asm import overload：let expected ret + ADDR_OF） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm vec 运行时 ABI** | ✅ **vec_i32 cookbook run=0**（sret + MEMORY 栈传） |
| **with_arena asm** | ✅ **run=0**（`v.ptr[i]` + push/new Vec_u8 mangle） |

## 本波摘要（2026-07-18 · asm with_arena `v.ptr[i]` residual）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 asm with_arena `v.ptr[i]` residual（run=5） |
| **地图** | C 绿；asm `v.ptr[0]` 过、`[1]` 失败；objdump 见 `std_vec_new_retVec_i32` / `push_Vec_i32` |
| **根因** | 用户 asm -o + import 跳过 entry .x typeck（C 预检另一 arena）→ `call_resolved` 空 → mangle 取首个 overload（Vec_i32）；`&v` ADDR_OF 无类型 → push 亦落 i32；i32 小端写 1 后 `ptr[1]` 读到 0 |
| **根修** | G.7：`pipeline_asm_call_expected_ret_ty` let 初值装期望返回；`glue_asm_call_arg_type_ref` ADDR_OF(VAR)→`*T`；import METHOD_CALL/CALL 评分用 expected ret |
| **验收** | Ubuntu L2：with_arena asm **run=0**；vptr_min **run=0**（符号 `new_retVec_u8` / `push_Vec_u8_ptr_u8`）；vec_i32 asm=0；wa C=0；rv=42；math_asm=0；**日常 L2 · 非 tip L4** |

### residual 下一波

1. （可选）METHOD_CALL 非 lit 实参 SSE 全覆盖  
2. （旁）prove residual：`backend_enc_dispatch` -E / `rt_diag_errno`  
3. （旁）freestanding CG002 elf_ec  

```text
构建级别：L2（pipeline_x.o + backend_call_dispatch + g05；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：wa asm run=0；vptr_min run=0；vec_i32 asm=0；rv=42；math_asm=0
纪律：测试 hard timeout；本波未升 tip L4
feat SHA：b4dbb111
日志：/tmp/ubuntu_g05_vec_u8_mangle.log
```
