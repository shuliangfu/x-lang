# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `g-02f-self-hosting` |
| `make bootstrap-driver-seed` | ✅ macOS（**无工作区 leaf gen** 亦绿） |
| G-FFI-5 | ✅ macOS |
| KPI **N** | **5**（fmt/check/test/build/run prove IDENTICAL + 构建退役） |
| `diff_single_link` T | **Ubuntu 13/18**（mac 14/18） |

## 本波次：Track L 叶子退役 ✅

**对象**：`fmt` / `check` / `test` / `build` / `run`

**做了什么**：

1. `tests/prove_module_selfhost.sh`：5/5 nm IDENTICAL（对照 `seeds/*_gen.linux.x86_64.c`）  
2. `scripts/driver_leaf_x_to_o.sh`：统一 `.x -E → rename → cc -c`；冷启动回退仅 `seeds/`  
3. Makefile：`driver_*_x.o` 不再依赖工作区 `driver_*_gen.c`；`DRIVER_SUBCMD_GEN` 仅剩 compile/emit  
4. g05 / `build_shux_asm`：叶子路径同上，**不读** pinned workspace gen  

**验收（mac）**：隐藏 5 个 `driver_*_gen.c` → `make bootstrap-driver-seed` OK → `./shux-build.sh build` + G-FFI-5 OK；`shux fmt`/`check` 可执行。

## 剩余 FAIL / 前排

| 模块 | 错误类 | 波次 |
|------|--------|------|
| `ast.x` | implicit tail return | W-tail |
| `lsp_io.x` | expected isize, found i32 | W-isize |
| `emit.x` | struct stack escape | W-escape |
| `lexer.x` | 30s TIMEOUT | 单独 profiling |

## 下一步

1. Ubuntu：pull 后 bootstrap + prove 5 叶 + G-FFI-5  
2. W-isize / W-escape / W-tail  
3. Track L 续：diagnostic / token / compile  
