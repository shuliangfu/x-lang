# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **43** IDENTICAL（附注） |
| **R2** | **5**（+`rt_fs_open`；前：util/lib_root/emit_flags/content） |
| **H** | 上述五切片 **H=0**；其余 runtime hybrid 多仍 rest C |
| Cap-empty-str | ✅ 已由 W-string 解锁 |

## 本波：Track R · `rt_fs_open` R2 full 真迁 ✅

| 路径 | 变更 |
|------|------|
| `src/runtime/rt_fs_open.x` | 吃满 `rt_fs_path_copy_nul` + `driver_fs_open_read_path` + `driver_fs_open_write`（`open` libc + `u8[512]` 栈；`O_CREAT`/`O_TRUNC` 按 `target_os` cfg） |
| `seeds/rt_fs_open.from_x.c` | 业务符号 `#ifndef SHUX_RT_FS_OPEN_FROM_X`；FROM_X 下 rest 仅前向 + marker → **业务 H=0** |
| `seeds/rt_fs_open_surface.from_x.c` | full surface（prove 锁；fcntl 宏可移植） |
| `tests/prove_module_selfhost.sh` | 注释：R2 full |

- 验收：`bash tests/prove_module_selfhost.sh rt_fs_open` → **IDENTICAL**
- 全量：**43 IDENTICAL**
- 产品 hybrid：thin full .x + rest 仅 marker → 业务 H=0
- 语义烟测：write→read 回环 + path_len 边界 + path_copy → **SMOKE_OK**

## Ubuntu 金标准（待本 commit push 后刷新）

| 项 | 结果 |
|----|------|
| `rt_fs_open` R2 full | 待 Ubuntu prove |
| 全量 | 待 43 IDENTICAL |

## 前排下一步

1. 下一批 R2：runtime 其它 rest（`rt_parse_diag` / `rt_stack` / …）· 或 Cap-global-bss → `rt_arena_buf`
2. Track M：typeck M2 试替换
3. 纪律：commit → push → Ubuntu → 金标一行

## 方法备注

后 T 主线：**Cap → 批量 R2 → M 并行**；禁止为 N 再 surface 下一叶子。
