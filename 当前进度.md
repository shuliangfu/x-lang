# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`bd0e8ed7`**（SysV 9–16B by-value + multi-arg spill） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm with_arena smoke** | ✅ **Ubuntu L2 build/run=0** |
| **asm string import mangle** | ✅ **len_StrView / bare get/subview** |
| **asm string smoke run** | ✅ **copy_proof run=0**（SysV 值路径关） |
| **asm math run (f64 SysV)** | ✅ Ubuntu math_asm run=0 |

## 本波摘要（2026-07-17 · asm string/struct SysV 值路径）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 asm string/struct SysV（run=1 residual） |
| **根因** | ① 9–16B POD 误 `lea` 传址，C 期望 `rdi+rsi` 按值；② unique import mid 被 caller fallback 再套 `_StrView`；③ multi-arg dual-load 用 `rdx` 盖掉已装 index |
| **已关** | `pipeline_glue` ≤16B by-value dual load；call_dispatch 双 GP 装配 + spill-then-load；unique mid 不 caller-suffix；Ubuntu **copy_proof run=0** |
| **邻域** | min string run=0；rv=42；wa=0；**日常 L2 · 非 tip L4** |
| **纪律** | hard timeout；**禁止** L2 半绿写 tip L4；改 glue 须重建 **pipeline_x.o**（产品链非 standalone） |

### residual 下一波

1. **vec.* METHOD_CALL mangle 探针**（共用权威应已覆盖；Ubuntu 验收）  
2. import 形参 type 映射 → 去掉 `std_math_*` 名门控  
3. rest C leaf  

```text
构建级别：L2（pipeline_x + call_dispatch + g05；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：string copy_proof build=0 run=0；min=0；rv=42；wa=0
纪律：测试 hard timeout；本波未升 tip L4
SHA 波：e8bf0757 → e40ce611 → 902b2985 → bd0e8ed7
```
