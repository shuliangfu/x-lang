# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **21** IDENTICAL（+ `labi_diag_pure`） |
| **diagnostic** | `-E`+cc ✅；**CORE_OK**（非 IDENTICAL / 非退役） |
| **W-string-nul** | ✅ STRING_LIT *u8 尾 `, 0`；slen≤64 |

## 本波：Track L `labi_diag_pure`（L1）✅

**事实**：`link_diag_code_for_kind` 真迁（strcmp + 短字面量 let p）；reportf/va_list/长文案 thin 转发，主体在 mega rest `*_impl`。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/runtime/labi_diag_pure.x` | L1 真迁 code_for_kind + 8 report thin |
| `seeds/labi_diag_pure.from_x.c` | 冷启动同构 thin + U `_impl` |
| `seeds/runtime_link_abi.from_x.c` | 8× `_impl` 常驻 rest；public under ifndef DIAG_PURE |
| `scripts/g05_ensure_relink_prereqs.sh` | L1 PREFER_X_O；滤 `-E` `strcmp` 与 string.h 冲突 |
| `tests/prove_module_selfhost.sh` | 扩门禁；同滤 strcmp |

- 验收：`bash tests/prove_module_selfhost.sh labi_diag_pure` → **IDENTICAL**
- g05：`L1 diag pure ← labi_diag_pure.x (Track L prefer .x)`
- mac：产品 `src/runtime_link_abi.o` 含 `T link_diag_*` + `T *_impl` + `T labi_diag_pure_count`；**21 IDENTICAL + CORE_OK**

## Ubuntu 金标准（`5db985d4`，2026-07-14 13:29 东八区，`date`）

| 项 | 结果 |
|----|------|
| `labi_diag_pure` | **IDENTICAL**（产品 L1 ← `labi_diag_pure.x` @ `self-hosting`） |
| 同批 | path + gates + ensure + path_io + host_lit **IDENTICAL** |

## 下一步

1. **diagnostic hybrid→IDENTICAL**（或下一可 nm 叶）  
2. W-global 数组；串池 >64；reportf/va_list 更深真迁  
3. 每波：**commit → push → Ubuntu prove → 再下一步**；§8 **东八区**（`date`，勿手填）
