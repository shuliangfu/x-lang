# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `60e46bb6`（Track L `rt_compile`） |
| 产品 | 双端 bstrict **123** @ `233e3b72`（L4 历史；本波工程未重跑 L4） |
| **T** | **10/18**（mega；未重跑） |
| **N** | **47/54 IDENTICAL**（Ubuntu 记账；相对 `70666dcb` 全表 35 单叶 **+12**）；mac 同叶绿 → 全表预期 **48/54** |
| **R2 / H** | 仍冻结（N 未满） |

## 本波摘要

| 波 | SHA | 结果 |
|----|-----|------|
| Track L 同型 no_mangle 链 | …`08671239` | …rt_run_exec 双端 IDENTICAL |
| **Track L rt_compile** | **`60e46bb6`** | 六 helper `#[no_mangle] export`（cmp/help/scan）；**mac+Ubuntu IDENTICAL** |

**产生点**：helper 模块前缀 mangle `rt_compile_rt_cmp_*` / `rt_help_token_is` 等 vs surface 短名。  
**权威**：`compiler/src/runtime/rt_compile.x`（surface 本就短名；G.9 英文）。  
**构建级别**：日常真测 L2/L3 单叶 prove（**非** L4）。

**剩余 7（Ubuntu）**：
- FAIL **3**：`backend_try_inline_dispatch` / `backend_call_dispatch` · `lsp_io_std_heap`
- SYM **4**：emit · rt_run_x_emit · rt_run_asm_backend · rt_run_compiler_parsed

日志：`/tmp/mac_prove_rt_compile.log` · Ubuntu `/tmp/ubuntu_prove_rt_compile_60e46bb6.log`

## 下一波前排

1. **Track L 续吃 SYM 4**（优先同型 no_mangle 小模块）  
2. **T / Track M** 说明向  
3. 禁未稳 N 谈 Stage2；动 SHARED 才 L4  
