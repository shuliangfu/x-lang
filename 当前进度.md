# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `f41ef8ff`（feat fmt_check collect orch pure 深迁 + Ubuntu 金标） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **49**（fmt_check 仍 R2 thin；本波 H↓） |
| **H** | **fmt_check rest T 29→27**（collect_paths + default_dirs pure orch）；仍 Cap residual walk/stat/argv/BSS/missing-diag/cwd |

## 本波：Track R · `fmt_check` **collect orch pure 深迁** ✅

| 路径 | 变更 |
|------|------|
| `fmt_check_cmd_thin.x` | pure 真体：`collect_paths_from_arg`（stat/walk/push 编排）+ `check_collect_default_product_dirs`（产品子目录 try_walk + cwd fallback 门闩） |
| `fmt_check_cmd.from_x.c` | FROM_X 下剔除 2 pure-dup `_impl`；entry Cap residual 改调 public；冷启动全 C 体仍在 |
| `fmt_check_cmd_thin_surface.from_x.c` | prove 锁 thin surface（同构 thin.x） |
| `g05` / `prove` | 注释标明 collect orch 深迁；产品仍 PREFER thin hybrid |

- 验收：mac/Ubuntu `prove` → **fmt_check IDENTICAL**；全量 **54 IDENTICAL**
- KPI：**H↓** rest 业务 T **29→27**（gone：`collect_paths_from_arg_impl` + `check_collect_default_product_dirs_impl`）
- **不是** thin 表面 KPI：产品链 pure 真体在 thin.x；Cap residual 仍 rest（walk opendir/stat/argv/BSS/missing-diag format/cwd getcwd）

## 下一波前排

1. **R2 真迁 / Cap residual 深迁**（禁止只改注释/surface）：`fmt_check` 余下 Cap residual（walk/stat/argv/BSS）/ Cap-fn-ptr / diagnostic debug residual / driver_abi getenv 门闩  
2. **Track M** typeck M2 smoke  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档；报数写 **rest 业务 T 前后**
