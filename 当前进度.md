# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`b96778ef`**（feat：asm sort formal+fk0 + core.debug on_demand；L4 钉盘仍 **`f16f7d48`**） |
| 产品 L4 放行 | ✅ **钉盘 `f16f7d48`**（双端真冷 L4+123；前钉 `5c4dea78`） |
| tip 双端 bstrict | ✅ **123/123 @ `f16f7d48`**（本波 **非** tip L4 升钉） |
| **asm sort link (fk0+formal)** | ✅ **`b96778ef`**（mod.x 面 + core.debug；i32+assert asm run=0） |
| **asm set/map/queue on_demand** | ✅ **`28b934e9`** |
| **asm formal std ensure** | ✅ **`ffd083cc`** |

## 本波摘要（2026-07-18 · asm sort link residual）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；产品 residual 再开（钉盘 `f16f7d48` 不动） |
| **地图** | Ubuntu `-backend asm` **sort/fs/env BLD001**；本波切 **sort**（一条债）：① `sort.o` 仅 `sort.x` → 裸 `sort_*` 无 `std_sort_sort_*`；② fk0 无 sort 探针 → 永不推入；③ `core.debug` 无 formal `.o` → `assert_eq_*` UNDEF |
| **根修** | Makefile `sort.o` = `mod.x`+`sort.x`（`compile_std_module` import 绑定）；`labi_std_fk0` 补 `std_sort_*`；simple-group g8 `core/debug/debug.o` + ensure；seed+`.x` 双权威（**`b96778ef`**） |
| **验收** | **日常 L2**：Ubuntu **sort asm build=0** · i32+assert **run=0** · C 全量 sort **run=0** · 矩阵 rv=42 opt/si/hello · set/map/queue/vec/math asm=0；mac g05+矩阵；**非 tip L4** |
| **半程 / 下一叶** | 全量 `tests/sort` asm **run=134**：asm 将 `sort.sort(*u8)` **错绑** `std_sort_sort_i32_ptr_i32`（C 正确绑 `*_u8_*`）→ typeck/codegen trust-resolve 债，**不**在本波 labi 层搅修 |
| **钉盘** | 仍 **`f16f7d48`** |

### residual 下一波

1. asm `sort.sort(*u8)` 重载错绑 i32（trust resolve / 实参 `*u8` 评分）  
2. （旁）asm **fs/env** BLD001（co-emit 权威；formal surface 同 sort 模式）  
3. （旁）slice asm SIGABRT（数组→切片槽）· 常量折叠 IR/WPO ·（可选）tip L4 升钉  

```text
构建级别：L2（runtime_link_abi.o + g05 + formal sort/debug.o；非 tip L4）
产品 tip L4 钉盘：f16f7d48（本波未升）
feat：b96778ef
Ubuntu：sort asm link=0 · i32-only run=0 · full sort asm run=134（u8 重载）· C full=0
       矩阵 rv=42 opt/si/hello · set/map/queue/vec/math_asm=0
macOS：g05 + 矩阵四件套 + sort C=0
权威：labi_std_fk0_user_needs_rel(sort) · labi_od_simple_group g8 core/debug
      Makefile sort.o=mod.x+sort.x · core/debug/debug.o
纪律：禁止日常 L2 冒充 tip L4；改 labi 须 runtime_link_abi.o + g05；一条债一层
```

---

## 前波摘要（2026-07-18 · asm set/map/queue on_demand residual）

| 波 | 结果 |
|----|------|
| **根修** | needs_std_set/map/queue + companions + hosted `--gc-sections`（**`28b934e9`**） |
| **验收** | **日常 L2**：asm set/map/queue run=0；钉盘仍 `f16f7d48` |

---
