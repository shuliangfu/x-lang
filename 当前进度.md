# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `5a81af60`（L4 真冷链 + 双端 bstrict **123**） |
| 产品 | **L4 真冷**（`6da9f4cd` 全擦）+ 双端 bstrict **123/123** @ `5a81af60` |
| **T** | **18/18** typeck |
| **EMPTY** | **18/18**（preprocess EMPTY 改 surface；产品 pin 恢复） |
| **N** | **54/54 IDENTICAL**（全表 @ `11f3ad55`） |
| **R2 / H** | 可解冻抽检 |
| **Track L SYM** | **0** |
| **FAIL residual** | **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **L4 真冷 + 双端 bstrict** | remaining_o=0 → bootstrap-driver-seed → g05 → 矩阵 → **123/123** |
| **根因 A（g05 UNDEF）** | `922487e5` 清空 `_RT_SEED_SLICE_OBJS`；no_c 对 arena/preamble/emit/parse 仍 U → **恢复 Makefile 同源 RT slices**（`6da9f4cd`） |
| **根因 B（Darwin g05）** | L4 后缺 `asm_experimental_symbol_bridge.o` → g05 ensure 从 seed 建（`c82ee9f7`） |
| **根因 C（preprocess PP002）** | `e0c21f57` 把产品 pin 换成破 `-E`（`kind` 在 parse 前）→ 恢复产品 pin；EMPTY 拆 `preprocess_empty_surface`（`5a81af60`） |
| **cfg_eval** | Darwin 冷 `-E` 非法 C 时回退 stub（不 hard-fail recipe） |

**构建级别**：**L4 真冷**（全擦 compiler+std+core `.o` + 重链 shux/shux_asm/shux-c）。  
日志：  
- mac cold `/tmp/mac_true_cold_6da9f4cd.log` · bstrict `/tmp/mac_true_bstrict_c82ee9f7.log`（123 OK；产品链含 pin 修复）  
- Ubuntu cold `/tmp/ubuntu_true_cold_6da9f4cd.log` · bstrict `/tmp/ubuntu_true_bstrict_5a81af60.log`（**123 OK**）

## 下一波前排

1. **Stage2**（黄金自举子集）—— 本 SHA 双端 L4+bstrict 已绿后方可谈  
2. **可选根修**：`Token { kind: Enum.Variant }` entry-emission；codegen `-E` 与 `.x` 主循环序（勿再拿破 `-E` 顶产品 pin）  
3. **禁止**用 N/T/EMPTY 单独宣称「可自举」（产品放行 = 本 SHA L4 + 双端 bstrict）  
