# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `g-02f-self-hosting` |
| `make bootstrap-driver-seed` | ✅ mac + Ubuntu（W-isize） |
| G-FFI-5 | ✅ mac + Ubuntu |
| KPI **N** | **5**（driver 叶） |
| KPI **T** | +**lsp_io** 双端 -E typeck OK |

## 本波次：W-isize ✅

**模块**：`compiler/src/lsp/lsp_io.x`

1. **`read_message` → isize**：整数字面量默认为 i32；错误路径改为 `return (-1) as isize`（与同文件 `write_fd` 一致）。  
2. **`lsp_alloc`**：`heap.alloc(size: usize)` 被跨模块 binding import 解析成首个 `alloc(i32): *u64` → `expected *u8, found *u64`。  
   - 源码改用 **`heap.alloc_zero(size)`**（唯一 `(usize)→*u8`，无歧义）。  
   - **typeck 根因**（下一波 **W-heap-overload**）：`find_func_return_type_in_module_by_name` 首同名；`find_func_return_type_in_module_overload` 在 `from_dep_index>=0` 时跳过实参类型匹配。

## 剩余 FAIL / 前排

| 模块 | 错误类 | 波次 |
|------|--------|------|
| `emit.x` | struct stack escape | W-escape |
| `ast.x` | implicit tail return | W-tail |
| `lexer.x` | 30s TIMEOUT | profiling |
| 多模块 `heap.alloc(usize)` | 跨模块重载 | W-heap-overload |

## 下一步

1. W-escape / W-tail  
2. W-heap-overload（typeck 跨模块重载）  

