# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `03c61322`（Track L `simd_loop`） |
| 产品 | 双端 bstrict **123** @ `233e3b72`（L4 历史；本波工程未重跑 L4） |
| **T** | **10/18**（mega；未重跑） |
| **N** | **39/54 IDENTICAL**（Ubuntu 记账；相对 `70666dcb` 全表 35 单叶 **+4**）；mac 同叶绿 → 全表预期 **40/54** |
| **R2 / H** | 仍冻结（N 未满） |

## 本波摘要

| 波 | SHA | 结果 |
|----|-----|------|
| 全表 prove re-baseline | `70666dcb` | Ubuntu **N=35/54**；mac 36/54 |
| Track L rt_fs_open | `ceb47079` | path helper `#[no_mangle]` 双端 IDENTICAL |
| Track L labi_diag_pure | `4647e849` | append helper `#[no_mangle]` 双端 IDENTICAL |
| Track L rt_diag_errno | `e8b6b4b2` | 三 helper `#[no_mangle]` 双端 IDENTICAL |
| **Track L simd_loop** | **`03c61322`** | `glue_simd_hw_env_disabled` `#[no_mangle]`；**mac+Ubuntu IDENTICAL** |

**产生点**：`.x` 模块前缀 mangle `simd_loop_glue_simd_hw_env_disabled` vs surface 短名。  
**权威**：`compiler/src/asm/simd_loop.x` + surface 注释同 commit（G.9 英文 docblock）。  
**构建级别**：日常真测 L2/L3 单叶 prove（**非** L4 / 全表 re-baseline）。

**剩余 15（Ubuntu）**：
- FAIL **3**：`backend_try_inline_dispatch` / `backend_call_dispatch` · `lsp_io_std_heap`
- SYM **12**：emit · fmt_check · rt_dispatch_impl · rt_pipeline_elf_diag · rt_run_x_emit · rt_emit_flags · rt_emit_state · rt_content · rt_compile · rt_run_exec · rt_run_asm_backend · rt_run_compiler_parsed

日志：`/tmp/mac_prove_simd_loop.log` · Ubuntu `/tmp/ubuntu_prove_simd_loop_03c61322.log`

## 下一波前排

1. **Track L 续吃 SYM 12**（优先 helper 缺 `#[no_mangle]` 的 rt_* / emit；一条债一 commit）  
2. **T / Track M** 说明向：8 mega 红归层  
3. 禁未稳 N 谈 Stage2；动 SHARED 才 L4  
