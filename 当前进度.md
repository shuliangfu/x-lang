# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **D Stage2** | ✅ Ubuntu freestanding D-03 @ `c3031883`（**≠** 产品链） |
| **E R2** | ✅ diagnostic / driver_abi / fmt_check Cap residual pure **收口** |
| **Stage2 WPO full-chain** | ✅ @ **`57665c75`** proxy **5.81%** |
| **M typeck** | ✅ **M1** + ✅ **M2 smoke** @ `256acd3b`（seed pin 未换） |
| **M codegen** | ✅ Cap+M1；🟡 **M2**：parse-skip **0** + host-cc ✅；**链上 smoke 矩阵仍红**（未 re-pin） |
| **T / EMPTY / N** | 记账仍 18/18 · 18/18 · 54/54 |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **codegen M2：emit_expr parse-skip 根修 + float_lit glue** | **parse-skip 3→0**。触发点=STRING_LIT 支内注释 `/* close " ) … } */`：**未配对 `"` 在 block comment 内**使产品 lexer/parser 整函数 skip（非 mega/EXPR_CALL 体量）。改英文注释后 `codegen_emit_expr` 完整进 force-regen；static 311→**15**；**host-cc ✅**。补齐缺失 `pipeline_codegen_emit_float_lit_c`（`pipeline_glue.c` + `pipeline_glue_strict_minimal.from_x.c` 同语义）。**M2 链上 smoke**：g05 链 force-regen `codegen_x.o` **链接绿**，但产品矩阵 **红**（incomplete `Option_i32`、hello overload 冲突、rv 缺 `main`）→ **已恢复 pin** `codegen_x.o`；矩阵 pin 再绿。**seed pin / tip L4 未动**。日志：`/tmp/codegen_m2_emitfix.c|.err` · `/tmp/m2_smoke_g05b.log`。 |

### 五问（本波）

| # | 答 |
|---|----|
| 产生 | 产品 lexer/parser：block comment 内未闭合 `"` → `emit_expr` **整函数 parse skip**；另 .x 声明但未实现的 `pipeline_codegen_emit_float_lit_c` |
| 存储 | skip residual → 非法 static / 假 init_globals；UNDEF float_lit 在链上 smoke |
| 消费 | force-regen host-cc / M2 `codegen_x.o` 替换 / 用户 `-o` |
| 同模式 | 任意 block comment 奇次 `"`；parser 匿名 `{ let }` 仍 follow-up |
| 最小回归 | force-regen def + host-cc + pin 矩阵；**勿**把链上 smoke 红当 tip 绿 |

### 已合入 / 本波

| SHA | 摘要 |
|-----|------|
| （本波） | emit_expr 注释去未配对 `"` + `pipeline_codegen_emit_float_lit_c` glue |
| `5aa391f7` | M2 半收口：skip_struct 扁平 let + x_ast Cap 闭合 |
| `efe48e75` | docs：M2 smoke 红地图 |
| `66c8496c` | codegen M1：`MAX_ONEFUNC` 1024 |
| `106a6b73` | codegen Cap-T001 |

### mac 探针（本波）

| 项 | 状态 | 日志 / 备注 |
|----|------|-------------|
| force-regen parse skips | **0** | `/tmp/codegen_m2_emitfix.err` · num_funcs=230 |
| force-regen defs | skip_struct ✅ · emit_expr ✅ · x_ast ✅ | static **15** |
| host-cc force-regen | ✅ | `/tmp/codegen_m2_emitfix.o` |
| M2 链上 g05 | ✅ link | force-regen `codegen_x.o` + strict_minimal float_lit |
| M2 链上矩阵 | ❌ | option/si incomplete type；hello overload；rv no main → **已 restore pin** |
| 产品矩阵（pin） | ✅ rv=42 · opt · hello(`0a`) · si | seed pin 未换 |
| 产品 tip L4 | 仍 **`5a636984`** | **非 tip L4** |

### 已知 residual

| 项 | 说明 |
|----|------|
| **M2 链上 codegen 质量** | force-regen 可解析/可 host-cc/可链，但 emit 仍产非法/不完整 C（Option 布局、print 重载声明、entry main）→ **blocked re-pin** |
| lexer block-comment `"` | 产品 lexer 对 comment 内 `"` 敏感；应用层已避雷；**parser/lexer 根修 follow-up** |
| parser `{ let }` 匿名块 | 与 skip_struct 同源 — **开 parser 根修 follow-up** |
| onefunc 泄漏 / parser XT001 / tip L4 | 仍前序 residual |

## 下一波前排

1. **M2 force-regen 产品质量**（Option layout / overload extern / main entry）→ 链上矩阵绿 → 才谈 re-pin  
2. lexer：block comment 内不得开 string 扫描（根修）+ parser `{ let }`  
3. **禁止** Cap/M1/M2 冒充 tip 产品 L4  

```text
构建级别：工程 L2（emit_expr parse-skip 清零 + host-cc 绿；M2 smoke 矩阵红已 restore pin；非 tip L4）
产品 tip L4：5a636984
codegen M2：parse-skip 0；host-cc ✅；链上矩阵 ❌（pin 矩阵 ✅）
产品矩阵：mac rv/opt/hello/si 绿（seed pin 未换）
```
