# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **32** IDENTICAL（+ `rt_dispatch_impl` thin） |
| **rt_dispatch_impl** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 1） |
| **rt_dispatch** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 1） |
| **backend_call_dispatch** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 44） |
| **backend_try_inline_dispatch** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 50） |
| **backend_arch_emit_dispatch** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 47） |
| **backend_enc_dispatch** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 120） |
| **simd_enc** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 74） |
| **simd_loop** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 22） |
| **fmt_check** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 42） |
| **driver_abi** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 70） |
| **diagnostic** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 79） |
| **W-string-nul** | ✅ STRING_LIT *u8 尾 `, 0`；slen≤64 |

## 本波：Track L `rt_dispatch_impl` hybrid→IDENTICAL（L2 thin）✅

**根因地图**：产品 hybrid 已走 **rt_dispatch_impl.x** + hybrid rest seed（`-DSHUX_RT_DISPATCH_IMPL_FROM_X`）入 `runtime_driver_no_c`，但 prove 未锁 thin 公共面。产品 rest seed 含 asm/emit/post_parse/full_x body/marker，与 pure thin 同构面分离。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/runtime/rt_dispatch_impl.x` | 权威 thin 公共面（产品本已用；1 门闩 `driver_run_compiler_full_x_impl_c`） |
| `seeds/rt_dispatch_impl_surface.from_x.c` | **新建** 冷/prove 同构 pure thin（`-E` 抛光 + POSIX prologue） |
| `seeds/rt_dispatch_impl.from_x.c` | **不变**（产品 hybrid rest：asm/emit/post_parse/full_x body/marker） |
| `tests/prove_module_selfhost.sh` | 扩 `rt_dispatch_impl` → thin.x + surface seed；空 mode → **IDENTICAL** |

- 验收：`bash tests/prove_module_selfhost.sh rt_dispatch_impl` → **IDENTICAL**
- 全量：`bash tests/prove_module_selfhost.sh` → **32 IDENTICAL**
- 产品：g05 仍 `thin.x + hybrid rest ld -r` 入 `runtime_driver_no_c.o`（本波不改链接拓扑）

## Ubuntu 金标准（`04497a36`，2026-07-14 14:05 东八区，`date`）

| 项 | 结果 |
|----|------|
| `rt_dispatch_impl` thin | **IDENTICAL**（产品 L2 thin @ `self-hosting`） |
| 同批 | rt_dispatch + call_dispatch + try_inline + arch_emit + backend_enc + simd_* + fmt_check + diagnostic + driver_abi + labi 叶 + **全量 32 IDENTICAL** |

## 前排下一步

1. 下一可 IDENTICAL 叶（runtime 其它 hybrid thin）/ true-migrate rest
2. W-global 数组 / 串池 >64
3. 纪律：commit → push → Ubuntu → docs
