# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`28b934e9`**（feat：asm set/map/queue on_demand + hosted gc；L4 钉盘仍 **`f16f7d48`**） |
| 产品 L4 放行 | ✅ **钉盘 `f16f7d48`**（双端真冷 L4+123；前钉 `5c4dea78`） |
| tip 双端 bstrict | ✅ **123/123 @ `f16f7d48`**（本波 **非** tip L4 升钉） |
| **asm set/map/queue on_demand** | ✅ **`28b934e9`**（探针 + companions + `--gc-sections`） |
| **asm formal std ensure** | ✅ **`ffd083cc`** |
| **toplevel let two_lets** | ✅ **`5e5a0a5b`** |
| **fs co-emit MAP_FAILED** | ✅ **`f16f7d48`** |
| **asm 跨函数 mutable let** | ✅ **`8232ac62`** |

## 本波摘要（2026-07-18 · asm set/map/queue on_demand residual）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 residual 再开（钉盘 `f16f7d48` 不动） |
| **地图** | Ubuntu `-backend asm` **set/map/queue BLD001**：① set 探针 `std_set_set_i32_*` 过时，真 U 为 `insert_Set_i32_ptr_i32` 等 → set.o 永不入链；② queue 仅 contention 表、无产品 `std_queue_*`；③ map 已入链但 hosted asm **无 `--gc-sections`**，未用体保留 heap U；map/set 缺 heap/hash 伴侣 |
| **根修** | G.7 补全 `needs_std_set`/`needs_std_map` + 新增 `needs_std_queue`；on_demand 伴侣 heap+mem(+hash for set)+formal ensure；hosted Linux asm `-Wl,--gc-sections` 对齐 C/freestanding（**`28b934e9`**） |
| **验收** | **日常 L2**：Ubuntu asm set/map/queue/vec/rv/hello/math **build=0 run=0/42**；C 矩阵 rv/opt/si/hello；mac g05+矩阵；**非 tip L4** |
| **钉盘** | 仍 **`f16f7d48`** |

### residual 下一波

1. （旁）其它产品面 residual 挡闸再开  
2. 常量折叠权威放 IR/WPO（不在 emit 扩）  
3. （可选）整波 tip L4 升钉含 `28b934e9` / `ffd083cc`  

```text
构建级别：L2（runtime_link_abi.o + g05；非 tip L4）
产品 tip L4 钉盘：f16f7d48（本波未升）
feat：28b934e9
Ubuntu：asm set/map/queue run=0 · 矩阵 rv=42 opt/si/hello · vec/math_asm
macOS：g05 + 矩阵四件套
权威：link_abi_user_o_needs_std_{set,map,queue} · shux_asm_invoke_ld_platform (--gc-sections)
纪律：禁止日常 L2 冒充 tip L4；改 labi 须 runtime_link_abi.o + g05
```

---

## 前波摘要（2026-07-18 · asm formal ensure after L4 wipe）

| 波 | 结果 |
|----|------|
| **根修** | gated std/core 推入前 `shux_ensure_formal_std_make_o`（**`ffd083cc`**） |
| **验收** | **日常 L2**：wipe vec → asm run=0；钉盘仍 `f16f7d48` |

---
