# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin**（force 矩阵全绿后 seed 换 Cap） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **Cap force hello 共 emit** | skip 表把 `shux_io_*` 误写成 `shu_io_*`（缺 x）；Cap 括号正确反而 0 命中 → redef weak；`submit_write` 无 weak 须 co-emit；`driver_read_ptr_gen` total_len=33 误早退；preamble 缺 provided_buffer #define |
| **修法** | `codegen.x` + seed：完整 `shux_io_*` 表；禁 skip `submit_write`；`full33_gen`；core_read_ptr 扩 backend/async；rt_preamble + runtime 补 `#define`/`extern` |
| **验证** | Cap force **rv/opt/si/hello `-o` ✅**（Hello World + `0a`）；**re-pin** seed=Cap force C；**pin 矩阵 mac 全绿**。**未 tip L4** |

### 五问

| # | 答 |
|---|----|
| 产生 | Cap `codegen_should_skip_emit_std_io_core_io_dup` / `core_read_ptr` 字节表 `shu_` 错拼 + 漏名；total_len=33 失配 `return 0` 短路 |
| 存储 | 共 emit C 双重定义 / 或 UNDEF submit_write；`std_io_core_shux_io_provided_buffer_ptr` 无宏 |
| 消费 | host-cc redef → 后 ld UNDEF → hello `-o` 红 |
| 同模式 | 历史「`shu_io_*` 表」与 call 侧 `shux_io_*_buf` 注释已纠；seed 曾靠 `\|\|` 优先级假 skip 糊绿 |
| 最小回归 | Cap/pin 矩阵 rv/opt/hello/si；禁 tip L4 |

### 探针

| 项 | 状态 | 备注 |
|----|------|------|
| Cap force rv/opt/si/hello | ✅ | hello 含 `0a` |
| re-pin seed | ✅ | `seeds/codegen_gen.linux.x86_64.c` ← Cap force |
| pin 矩阵 mac | ✅ | 与 Cap 同绿 |

### residual 下一波

1. Ubuntu 金标：pull + L2/L3 矩阵（SHARED re-pin）；谈 tip 前 **L4+bstrict**  
2. lexer block-comment / parser 匿名 `{ let }`  
3. **禁** 把本波 M2 re-pin 写成 tip 产品 L4（tip 仍 **`5a636984`**）  

```text
构建级别：工程 L2（codegen M2 hello skip + re-pin；非 tip L4）
产品 tip L4：5a636984
codegen M2：嵌套✅ forward✅ CALL mangle✅ placeholder✅ hello co-emit✅ re-pin✅
产品矩阵：pin mac 全绿（rv/opt/hello/si）
```
