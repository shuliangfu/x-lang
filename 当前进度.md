# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **D Stage2** | ✅ Ubuntu freestanding D-03 @ `c3031883`（**≠** 产品链） |
| **E R2** | ✅ diagnostic / driver_abi / fmt_check Cap residual pure **收口** |
| **Stage2 WPO full-chain** | ✅ @ **`57665c75`** proxy **5.81%** |
| **M typeck** | ✅ **M1** + ✅ **M2 smoke** @ `256acd3b`（seed pin 未换） |
| **M codegen** | ✅ Cap+M1；**M2 链上矩阵 ❌**（force-regen 共 emit dep 体残缺 → UNDEF；seed pin 绿） |
| **T / EMPTY / N** | 记账仍 18/18 · 18/18 · 54/54 |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **Ubuntu codegen M2 smoke（诚实复验）** | push `f6bfc43b` → Ubuntu pull。先遇 **旧 `pipeline_x.o`（MAX_ONEFUNC=256）**：Cap `codegen.x` 含 `emit_expr` 时 XT001 假栽在 `#114`；**重建 pipeline/ast_pool 1024** 后 `-E`/host-cc ✅（static=15 · out≈436KiB）。**链上 force-regen 矩阵**：rv=42 ✅ · **opt/si/hello ❌** `undefined reference`（`core_option_*` / `std_fmt_print_u8_ptr_i32`）。对照：seed pin 同树矩阵 **双端绿**。复验 mac：把 force-regen 装到 **`compiler/codegen_x.o`** 后矩阵同样 UNDEF——**前波 mac「M2 矩阵绿」为假绿**（曾 `cp` 到 `build_asm/codegen_x.o`，g05 链的是 `compiler/codegen_x.o` pin）。**pin 双端已 restore 绿**；**seed pin / tip L4 未动**。 |

### 五问（本波）

| # | 答 |
|---|----|
| 产生 | (1) Ubuntu 链上未重建 M1 `MAX_ONEFUNC` 后的 `pipeline_x.o`；(2) Cap force-regen 对 `core.option` 等 **dep 函数体共 emit 不全**（仅 extern / 部分体） |
| 存储 | 旧 `g_onefunc_sc[256]`；force-regen C 缺 `core_option_some_i32` 等定义 |
| 消费 | Ubuntu/mac 用户 `-o` 链接 UNDEF；seed pin 仍完整共 emit |
| 同模式 | g05 装错 `.o` 路径；SHARED 改 `ast_pool` 后 **必须** 重建嵌入它的 pipeline 产物 |
| 最小回归 | force-regen：装 **`compiler/codegen_x.o`** + g05 + rv/opt/si/hello；Ubuntu 须 pipeline 含 1024 |

### 已合入 / 本波

| SHA | 摘要 |
|-----|------|
| （本波 docs） | Ubuntu M2 诚实地图 + 假绿更正 |
| `f6bfc43b` | M2 产品质量：let-hoist + Option/Result ABI（前波） |
| `ffe2dbd1` | emit_expr parse-skip + float_lit glue |
| `5aa391f7` | M2 半收口：skip_struct + x_ast |
| `66c8496c` | M1：`MAX_ONEFUNC` 1024 |

### 探针（本波）

| 项 | 状态 | 日志 / 备注 |
|----|------|-------------|
| Ubuntu pull @ `f6bfc43b` | ✅ | git push/pull |
| Ubuntu 旧 pipeline → XT001 | 地图 | 未重建 MAX_ONEFUNC |
| Ubuntu rebuild pipeline 1024 | ✅ | `g_onefunc_sc` size `0xac000` |
| Ubuntu Cap typeck on chain | ✅ | `-E-extern` + slim + LANG007；`/tmp/ubuntu_typeck_m2_*` |
| Ubuntu force-regen `-E`/host-cc | ✅ | `/tmp/ubuntu_codegen_m2_quality.c|.o` |
| Ubuntu force-regen 矩阵 | ❌ opt/si/hello UNDEF | `/tmp/ubuntu_m2q_*.log` · rv=42 |
| mac force-regen 矩阵（真装路径） | ❌ 同 UNDEF | 更正前波假绿 |
| 产品 pin 矩阵双端 | ✅ | restore 后 rv/hello 绿 |
| 产品 tip L4 | 仍 **`5a636984`** | **非 tip L4** |

### 已知 residual

| 项 | 说明 |
|----|------|
| **M2 链上矩阵红（真）** | force-regen 共 emit **dep 函数体**残缺 → 仅 extern → 链接 UNDEF；**非** incomplete type（前波前缀修仍有效） |
| **seed pin 未换** | pin 矩阵绿 ≠ re-pin；re-pin / tip L4 另闸 |
| Ubuntu `pipeline_x.o` 与 `ast_pool` | 动 `MAX_ONEFUNC` / `ast_pool.c` 后须重建嵌入对象（本波已在 Ubuntu 重建；勿长期只改源不改 `.o`） |
| lexer block-comment `"` | 产品 lexer 对 comment 内 `"` 敏感；应用层已避雷 |
| parser `{ let }` 匿名块 | 与 skip_struct 同源 — follow-up |
| let-hoist 类债 | force-regen 路径「先写缓冲再 let 初值」仍危险 — 零初值+赋值 |

## 下一波前排

1. **codegen M2 真收口**：Cap force-regen 与 seed pin **对齐 dep 函数体共 emit**（option/fmt 等）→ 双端 force-regen 矩阵绿 → 才谈 re-pin  
2. lexer：block comment 内不得开 string 扫描 + parser `{ let }`  
3. **禁止** Cap/M1/M2 冒充 tip 产品 L4；**禁止**把 `build_asm/codegen_x.o` 当 g05 链上真物  

```text
构建级别：工程 L2（Ubuntu M2 诚实复验；force-regen 矩阵红；pin restore 绿；非 tip L4）
产品 tip L4：5a636984
codegen M2：-E/host-cc 双端 ✅（Ubuntu 须 pipeline 1024）；链上矩阵 force-regen ❌ UNDEF
产品矩阵：pin 双端 rv/hello 绿
```
