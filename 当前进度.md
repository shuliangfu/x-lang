# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`2742bafa`**（NL-07 L6 nostdlib libc face stubs） |
| 产品 L4 放行 | 仍钉 **`5a636984`** 双端 L4+123（**未**升 tip 全擦 L4） |
| tip 双端 bstrict | ✅ **123/123 @ `36ac3d14`**（g05 + SKIP_BUILD；**非 L4**；本波未重跑全量） |
| **std.vec 产品 -o** | ✅ **双端** formal `vec.o` + on-demand · `with_arena_vec_push` |
| **NL-07 硬绿 L1** | ✅ **multi-def=0** |
| **NL-07 硬绿 L2** | ✅ **`fflush` 有定义** |
| **NL-07 硬绿 L3** | ✅ **`backend_enc_*` / arch_emit / try_inline / simd =0** |
| **NL-07 硬绿 L3b** | ✅ **`backend_emit_*=0`**（seed partial export） |
| **NL-07 硬绿 L4+** | ✅ **typeck_*=0** · driver 主簇关 · lsp 主簇关 |
| **NL-07 硬绿 L5** | ✅ **parser_*/codegen_* 前排关**（residual-only partial；multi=0） |
| **NL-07 硬绿 L6** | ✅ **libc 面 9 符号在 stubs** · unique UNDEF=**0** · **`bootstrap nostdlib crt0 link OK`** |
| **NL-07 crt0 全包** | ✅ **链接硬绿**（err 空；`ldd` 静态无 libc）；postlink 烟测构建期偶发 WARN→experimental 回退，复测 smoke OK |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · NL-07 L6 nostdlib libc face）

| 波 | 结果 |
|----|------|
| **入口** | 前排硬绿分层 L6：L5 residual 仅 libc 面 9 符号 → stubs 补全（**非** tip L4 真冷） |
| **根因** | glue/seed 宿主头展开引用 `fileno`/`isatty`/`puts`/`strerror`/`fread`/`ferror`/`stdin`/`remove`/`__ctype_b_loc`；L2 已建 stubs stdio 权威，L5 后仅缺此面 |
| **根修** | **`2742bafa`**：`bootstrap_nostdlib_stubs.from_x.c` 补全 9 符号（+stdin；`remove`→unlink；glibc 布局 `__ctype_b_loc`）；G.7 仍本 TU only（**非** freestanding_io 第二套） |
| **验收 Ubuntu** | TRY_BUILD：`multi=0`；unique UNDEF **9→0**；**`bootstrap nostdlib crt0 link OK`** + final-link OK；gate **OK**（full-chain try） |
| **双端产品矩阵** | mac g05 + rv/opt/hello/si **绿**；Ubuntu g05 + rv/opt/hello/si **绿**（`0a` 换行） |
| **门禁** | v4 gate **OK**；**首次** `crt0 link OK`（链接层零 libc 硬绿） |
| **日志** | Ubuntu N07 `/tmp/ubuntu_n07_l6_2742bafa.log` · g05 `/tmp/ubuntu_g05_n07_l6.log` · mac g05 `/tmp/mac_g05_n07_l6.log` |

### 五问（NL-07 L6 libc face）

| 问 | 答 |
|----|-----|
| 谁产生 | 宿主头 / seed 编译物 U-ref 标准 libc 面（diag isatty/fileno；io fread/stdin；link remove/strerror；ctype macros → `__ctype_b_loc`） |
| 谁存储 | `bootstrap_nostdlib_stubs.from_x.c`（nostdlib-only 链；g05 不链 → glibc 保留） |
| 谁消费 | Linux crt0 `ld -nostdlib` 行（stubs.o + freestanding_io） |
| 同模式 | L2 `fflush`；禁止 freestanding_io 再抄一套 FILE* |
| 最小回归 | multi=0；unique=0；crt0 link OK；双端 g05 产品矩阵 |

### residual 下一波

1. 用户明示时 tip **L4 真冷** + 双端 bstrict 123（升放行钉盘）  
2. nostdlib **运行时质量**：构建期 postlink smoke 偶发 WARN；纯 static 编译器 hello 路径曾见 f64 发射 `g.0`（g05 绿；**≠** 链接 residual）  
3. 默认 `SHUX_BOOTSTRAP_NOSTDLIB=1` / 去系统 pthread 等（策略文）  

```text
构建级别：日常 L2/L3（NL-07 L6；非 tip L4 全擦）
产品 tip L4 钉盘：5a636984
tip HEAD：2742bafa
NL-07 L1–L6：multi=0 · fflush 关 · backend_enc=0 · backend_emit=0 · typeck=0 · driver/lsp 主簇关 · parser/codegen 前排关 · libc 面关 · crt0 link OK · unique UNDEF=0
双端产品矩阵：mac+Ubuntu g05 rv=42 / opt / hello / si 绿
```
