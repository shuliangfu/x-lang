# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`45670027`**（NL-07 L8：pure static hello -o co-emit） |
| 产品 L4 放行 | 仍钉 **`5a636984`** 双端 L4+123（**未**升 tip 全擦 L4） |
| tip 双端 bstrict | ✅ **123/123 @ `36ac3d14`**（g05 + SKIP_BUILD；**非 L4**；本波未重跑全量） |
| **std.vec 产品 -o** | ✅ **双端** formal `vec.o` + on-demand · `with_arena_vec_push` |
| **NL-07 硬绿 L1–L6** | ✅ multi=0 · fflush · enc/emit · typeck/driver/lsp · parser/codegen · libc 面 · **crt0 link OK** · unique=0 |
| **NL-07 运行时 L7** | ✅ freestanding `%g`/`%e`；✅ crt0 真 `preprocess_x.o`；✅ crt0 `user_asm_seed_bridge`；✅ **postlink smoke 无 fallback 稳定绿**；✅ 纯 static f64 非 `g.0` |
| **NL-07 L7+ import typeck** | ✅ keep-global parser import APIs → pure static **XT001 关** |
| **NL-07 L8 hello -o** | ✅ pure static **hello -o 绿** run=0 · `Hello World` · C≈164k co-emit std.io；rv=42 · opt=102 |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · NL-07 L8 pure static hello -o co-emit）

| 波 | 结果 |
|----|------|
| **入口** | L7+ 已关 XT001；前排 pure static **hello -o** `std_io_print_u8_ptr_usize` UNDEF |
| **根因地图** | ① `realign` 在 `ndep > entry imports` 时清零 ndep → load 只重绑 entry（std.fmt）丢 std.io；产品 mac 常因 set_ndep 布局漂移假绿。② keep 后 pure static `pctx_seed` 与 `module_at` 双权威布局漂移 → module=NULL 仍不 co-emit。 |
| **证据** | 修前 `-E` 55k 仅 fmt；修后 164k · `std_io` defs=99 · `dep emit j=1 path=std.io funcs=59` · host-cc 绿 · 输出 `48 65…0a` |
| **根修** | **`11cfeba0`** realign keep-closure；**`b90cc752`** re-seed pctx after pre-parse；**`45670027`** keep 分支从 driver_dep 槽 rebind module/path（本 TU `set_module`，G.7） |
| **验收 Ubuntu pure static** | crt0 OK · smoke OK · rv=42 · opt=102 · **hello -o 绿 run=0** · global_T_multi=0 |
| **pure static residual** | **si -o** host-cc emit 序 / `core_result_*`（codegen/link 层） |
| **双端 g05** | mac g05 rv/opt/hello/si **绿** |
| **日志** | Ubuntu `/tmp/ubuntu_n07_l8_45670027.log` · mac g05 本波 |

### 五问（NL-07 L8 hello -o）

| 问 | 答 |
|----|-----|
| 谁产生 | realign 清闭包 + pctx set_module 双权威漂移 → dep co-emit 无 std.io 体 |
| 谁存储 | `PipelineDepCtx.ndep` / module slots · `driver_dep_*` 发布槽 |
| 谁消费 | `codegen_one_dep_emit` → 生成 C → host-cc |
| 同模式 | 与 load_and_sync keep-closure / pctx_seed 布局 同源 |
| 最小回归 | pure static hello -o/run；rv；opt；mac g05 矩阵 |

### residual 下一波

1. pure static **si -o**（host-cc dep emit 序 / `core_result_*`）  
2. 用户明示时 tip **L4 真冷** + 双端 bstrict 123  
3. 默认 `SHUX_BOOTSTRAP_NOSTDLIB=1` / 去系统 pthread（策略文）  

```text
构建级别：日常 L2/L3（NL-07 L8 hello -o；非 tip L4 全擦）
产品 tip L4 钉盘：5a636984
tip HEAD：45670027
NL-07 L1–L7+ 保持；L8：pure static hello -o 绿 · rv/opt 绿
双端产品矩阵：mac g05 rv=42 / opt / hello / si 绿
```
