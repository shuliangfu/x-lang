# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`0338e3b6`**（SysV MEMORY by-value param home） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm hello / hosted vec** | ✅ **Ubuntu L2** |
| **fs r42** | ✅ |
| **fs vec len_empty -o** | ✅ |
| **fs vec push / boundary / cookbook** | ✅ **Ubuntu L2**（本波关 SIGSEGV） |
| **fs hello** | ❌ **CG002 residual**（`submit_read_batch` final_expr；非本债主面） |

## 本波摘要（2026-07-18 · fs vec push SysV MEMORY home）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 freestanding std.vec push SIGSEGV |
| **地图** | CALL 已对 >16B 按 SysV MEMORY 压栈；callee `is_indirect` 仍把 >8B 当 hidden pointer → `len(Vec)` 读 rdi 野指针；软 XT001 on `new`/`heap_mem_set_c` 非根因 |
| **根因** | G.7 权威半残：CALL 侧 MEMORY by-value 已修，callee param home / is_indirect 未对齐 formal C |
| **根修** | `pipeline_glue.c`：is_indirect 仅 9–16 保留 legacy pointer；>16 MEMORY 全宽 home（`off+width` 槽基，8B 保持历史偏移）；param_home 从 `[rbp+stack]` 拷入 |
| **验收** | Ubuntu L2：**fs push/boundary/cookbook=0** · hosted rv/hello/vec · fs r42 · len_empty · nolibc-n06；**fs hello CG002 residual**；**日常 L2 · 非 tip L4** |
| **重建** | 改 glue 须 `pipeline_x.o` + `pipeline_glue_standalone.o` 双重建（standalone 不覆盖旧 pipeline_x 时假绿） |

### residual 下一波

1. freestanding **hello CG002**（io `submit_read_batch` final_expr ko=41；旁）  
2. 软 XT001 on freestanding co-emit `new` / `heap_mem_set_c`（非阻塞 push）  
3. （可选）9–16B Allocator 双 GP home 全对齐 formal C；f64 let-init；cfg-extern / `.bss` / labi `len_empty`  

```text
构建级别：L2（pipeline_glue + pipeline_x.o + standalone + g05；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：fs vec push/boundary/cookbook 绿 · hosted 矩阵绿 · fs hello residual
纪律：改 glue 双重建 pipeline_x + standalone；本波未升 tip L4
feat SHA：0338e3b6（链 895a4c31 → f17e00b3 → 0338e3b6）
```
