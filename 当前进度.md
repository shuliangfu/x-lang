# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ tip（Stage2 **双端**；Ubuntu 金标 `0240c6b1`+；macOS Darwin 闭包本波） |
| 产品 | **L4 真冷** + 双端 bstrict **123/123** @ `5a81af60`；**Stage2 后恢复 g05 产品链**（勿把 freestanding 当产品） |
| **D Stage2** | ✅ Ubuntu @ `0240c6b1` gen1==gen2 SHA256；✅ **macOS** 行为 parity rv=42 + Step5 g05 恢复 |
| **T** | **18/18** typeck |
| **EMPTY** | **18/18**（preprocess EMPTY 改 surface；产品 pin 恢复） |
| **N** | **54/54 IDENTICAL**（全表 @ `11f3ad55`） |
| **R2 / H** | 可解冻抽检 |
| **Track L SYM** | **0** |
| **FAIL residual** | **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **Stage2（Ubuntu 金标）** | `bootstrap-verify-stage2-bstrict` **OK** @ `0240c6b1`；gen1/gen2 **rv=42**；struct_mk **exit 10**；**D-03** SHA256 **identical** `0b5b6a2d…4a0bf3`（freestanding **5324744** B） |
| **Stage2（macOS Darwin）** | 本波修 experimental 链闭包 + Darwin D-03 track-only；**行为 parity rv=42/42**；Step5 **g05 恢复产品**；日志 `/tmp/mac_stage2_*` |
| **根因（mac Step2）** | Darwin 为避多定义整颗丢掉 strict_glue_stubs + 空 glue → experimental UNDEF；改为 **filt stubs**（omit `asm_driver_*`）+ **minimal complement** + `runtime_process_argv`；strict 重链残差多定义 → **keep experimental**（Linux 仍 hard-fail strict） |
| **Stage2 WPO** | Step2 默认 `SKIP_WPO`；2c–2h Linux WPO **WARN**；Darwin WPO **N/A** |
| **残差（非阻塞）** | postlink smoke residual；Darwin D-03 hash 默认可不等（track-only；硬闸=行为 parity） |
| **L4 真冷 + 双端 bstrict** | remaining_o=0 → g05 → **123/123** @ `5a81af60`（产品放行面） |

**构建级别**：Stage2 Ubuntu = freestanding gen1/gen2 行为+hash；Stage2 mac = experimental gen2 + 行为 parity + g05 恢复；产品 = L4 + g05 dynamic。  
日志：  
- Stage2 Ubuntu `/tmp/ubuntu_stage2_0240c6b1_final.log`  
- Stage2 mac `/tmp/mac_stage2_3a561a35_full.log` · parity `/tmp/mac_stage2_*_parity.log`  
- L4 mac/Ubuntu 见上波  

## 下一波前排

1. **可选工程债**：Stage2 `SKIP_WPO_DOGFOOD` 默认覆盖 2c–2h；Darwin strict re-link residual（seed_link_compat / glue_support 去重）  
2. **可选根修**：`Token { kind: Enum.Variant }` entry-emission；codegen `-E` 主循环序  
3. **禁止**把 Stage2 ✅ 写成「完全自举 / 产品放行」——产品放行仍 = **L4 + 双端 bstrict**；完全自举另见 D+E+F  

