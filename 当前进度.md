# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `94adf3fa`（feat driver_abi pure 深迁 + Ubuntu 金标） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **49**（driver_abi 仍 R2 thin；本波 H↓） |
| **H** | **driver_abi rest T 196→183**（剔除 13 pure-dup）；仍 Cap residual getenv/uname/pthread/path-read/BSS |

## 本波：Track R · `driver_abi` **pure 深迁** ✅

| 路径 | 变更 |
|------|------|
| `runtime_driver_abi_thin.x` | pure 真体：scan/has import、argv collect、target OS kind、fail/smoke/peek 编排、entry_len_i32、large_stack 早退 orch |
| `runtime_driver_abi.from_x.c` | FROM_X 下剔除 13 pure-dup `_impl`；冷启动全 C 体仍在 |
| `runtime_driver_abi_thin_surface.from_x.c` | prove 锁 thin surface（同构 thin.x） |
| `g05` / `prove` | 注释标明 pure 深迁；产品仍 PREFER thin hybrid |

- 验收：mac/Ubuntu `prove` → **driver_abi IDENTICAL**；全量 **54 IDENTICAL**
- KPI：**H↓** rest 业务 T **196→183**（gone：scan/has×3 + argv_collect + target_os + fail + smoke + peek + entry_i32 + entry_len + large_stack×3）
- **不是** thin 表面 KPI：产品链 pure 真体在 thin.x；Cap residual 仍 rest（getenv/uname/setrlimit/pthread/path-read/BSS/format）
- 附注：Ubuntu `shux -o` 链偶发 BLD001（argv 脏）为预存；`-E` + `cc` 金标 **rv=42**

## 下一波前排

1. **R2 真迁 / Cap residual 深迁**（禁止只改注释/surface）：`fmt_check` Cap residual（walk/stat/argv/BSS）/ Cap-fn-ptr / diagnostic 余下 debug residual / driver_abi 余下 getenv 门闩  
2. **Track M** typeck M2 smoke  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档；报数写 **rest 业务 T 前后**
