# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **D Stage2** | ✅ Ubuntu freestanding D-03 @ `c3031883`（**≠** 产品链） |
| **E R2** | ✅ diagnostic / driver_abi / fmt_check Cap residual pure **收口** |
| **Stage2 WPO full-chain** | ✅ @ **`57665c75`** proxy **5.81%** |
| **M typeck** | ✅ **M1** + ✅ **M2 smoke** @ `256acd3b`（seed pin 未换） |
| **M codegen** | ✅ Cap+M1；**M2 嵌套 ✅**；**forward prototype 墙 ✅**；**链上 force 矩阵 residual = hello/si mangle** |
| **T / EMPTY / N** | 记账仍 18/18 · 18/18 · 54/54 |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **Cap same-module forward prototype 墙** | 根因：`codegen.x` 的 `codegen_x_ast` **缺** body 前同模块 `emit_func_extern_declaration` 墙；pin seed `codegen_gen.linux.x86_64.c` **已有**。同模块前向调用（`map_ptr_u8`→`is_some_ptr_u8`）host-cc 红。 |
| **修法** | 在 `codegen_x_ast` 的 `i==0` 段、entry enum/struct 之后、top-level let 之前，对 **非 extern** 函数循环 `emit_func_extern_declaration`（与 seed 对齐）。**权威 = codegen.x**；seed 已含，**未改 pin / 未 re-pin**。 |
| **验证** | Cap force-regen：option **`-o` 绿**（rv=42 绿）；option C：`extern …is_some_ptr_u8` **在** `map_ptr_u8` 体前。**hello/si 仍红**：hello 共 emit `std_fmt_*` 体写成 `fmt.fmt_i32(...)`（pin=`core_fmt_fmt_i32`）；si `core_result_*` 体误调 `core_option_unwrap_or`（pin=`core_result_unwrap_or`）。**pin restore 矩阵绿** rv/opt/hello/si。**tip L4 / seed pin 未动**。 |

### 五问（本波）

| # | 答 |
|---|----|
| 产生 | Cap `codegen_x_ast` 未在函数体前发射同模块 prototype（seed 有、.x 无） |
| 存储 | 单文件 C 共 emit 的 TU 顺序：体先于定义 → C99 隐式声明非法 |
| 消费 | host-cc option；装链 Cap 后产品矩阵 |
| 同模式 | 任意同模块后定义前调用（overload 后缀族 / map→is_some） |
| 最小回归 | Cap force option `-o`；pin 矩阵；**勿** re-pin / tip L4 |

### 已合入 / 本波

| SHA | 摘要 |
|-----|------|
| （本波） | codegen.x same-module forward prototype wall + 进度 |
| `d23ce5ae` | 前波：if scan_sync comment-aware braces |

### 探针（本波）

| 项 | 状态 | 日志 / 备注 |
|----|------|-------------|
| Cap force option `-o` | ✅ | `/tmp/opt_m2f.log` · forward 墙后绿 |
| Cap force rv=42 | ✅ | |
| Cap force hello | ❌ | `fmt.fmt_*` 未 mangle → `/tmp/hello_m2f.log` · `/tmp/hello_m2f_force.c` |
| Cap force si | ❌ | `core_option_unwrap_or` 无后缀 / 错模块 → `/tmp/si_m2f.log` |
| pin 矩阵 mac | ✅ | rv=42 / opt / hello / si · pin restore |
| 产品 tip L4 | 仍 **`5a636984`** | **非 tip L4** |

### 已知 residual

| 项 | 说明 |
|----|------|
| **hello Cap CALL mangle** | `const fmt = import("core.fmt")` → pin 发 `core_fmt_fmt_i32`；Cap 发 `fmt.fmt_i32`（field 形态）。权威应在 CALL + binding-import 路径。 |
| **si Cap callee 错前缀** | `core_result_unwrap_or_gen` 体 pin=`core_result_unwrap_or`；Cap=`core_option_unwrap_or`。 |
| **seed pin 未换** | forward 墙已对齐 .x↔seed；**未** re-pin Cap 入产品链 |
| lexer / `{ let }` | follow-up |

## 下一波前排

1. **Cap force CALL mangle**（binding import `fmt.fmt_*` + result→option 错调）→ Cap force hello/si 绿 → 双端 force 矩阵 → 才谈 re-pin  
2. lexer block-comment 奇次 `"` + parser `{ let }`  
3. **禁止** Cap/M1/M2 冒充 tip L4；**禁止**装 `build_asm/*_x.o`；**禁止** 53KiB 残 Cap 装链  

```text
构建级别：工程 L2（codegen.x forward 墙；Cap force option 绿；hello/si mangle residual；pin 矩阵绿；非 tip L4）
产品 tip L4：5a636984
codegen M2：嵌套 ✅ · forward prototype ✅ · force 矩阵 residual=mangle；seed pin 未换
产品矩阵：pin mac rv/opt/hello/si 绿
```
