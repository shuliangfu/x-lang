# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`5399acac`**（f64 ordered cmp ucomisd；前 `7ca398a1` neg+mulsd） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm hello / hosted** | ✅ **Ubuntu L2**（opt/si/hello/math） |
| **f64 unary / ordered cmp / mul** | ✅ **p5_ord/p_sign/p6_mul asm run=0** |
| **fs r42 / panic / hello** | ✅ **`run-freestanding-hello` OK** |
| **vec_u16 get** | ✅ **`f67e22a9`** |

## 本波摘要（2026-07-18 · f64 unary neg + ordered cmp + mulsd）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 f64 ordered cmp / mul |
| **地图** | ① `EXPR_NEG` 对 f64 发 `neg %eax` → 32 位运算清 RAX 高半 → `-2.0` 变 `0.0`；② 即使位正确，**有符号 `cmpq` 对负 IEEE 位序反转**（`-1.5_bits < -2.0_bits` 但 float `-1.5 > -2.0`）；③ f64 `*` 走 `imul` |
| **根修** | G.7 扩 asm 权威：`btc` 翻 IEEE 符号（f64/f32）；`ucomisd`+CF/ZF setcc；`mulsd` 对齐 addsd；finish 单 helper；须 **`pipeline_x.o` 重编**（glue 嵌 TU，standalone 非产品链） |
| **验收** | Ubuntu L2：p5_ord/p5_pos/p6/p_sign/p_le/i_mul **run=0**；rv=42 · opt/si/hello/math · freestanding hello；**日常 L2 · 非 tip L4** |
| **重建** | `backend_enc_dispatch.o` + **`pipeline_x.o`** + g05 `shux_asm` |

### residual 下一波

1. （旁）cfg-extern / ELF `.bss` / labi `len_empty`；f64 **div**（若 asm 仍 idiv）  
2. tip L4 升钉仍须双端真冷+bstrict（钉盘仍 `5c8204ae`）  

```text
构建级别：L2（pipeline_x + enc_dispatch + g05/shux_asm；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：f64 neg/cmp/mul=0 · freestanding hello · math/opt/si/hello
纪律：本波未升 tip L4
feat SHA：7ca398a1 + 5399acac
```
