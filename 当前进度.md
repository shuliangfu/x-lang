# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`c416015e`**（feat：labi `std_vec_len_empty` fk0 入链；钉盘仍 `5c4dea78`） |
| 产品 L4 放行 | ✅ **钉盘 `5c4dea78`**（双端真冷 L4+123；**本波非 tip L4**） |
| tip 双端 bstrict | ✅ **123/123 @ `5c4dea78`**（钉盘；本波日常 L2） |
| **asm hello / hosted** | ✅ **Ubuntu L4**（rv=42 · opt/si/hello） |
| **f64 unary / cmp / mul / div** | ✅ **p5_ord / p6_mul / p7_div + METHOD_CALL ret cmp**（入链钉盘） |
| **math_asm import METHOD_CALL** | ✅ **`math.pi()/floor/…` run=0 · ucomisd**（`c28931a6` 入链） |
| **fs r42 / panic / hello** | ✅ **`run-freestanding-hello` OK**（入链） |
| **vec_u16 get** | ✅ **`f67e22a9`**（入链） |
| **heap_trace / static let init** | ✅ **`b2143e2a`**（`SHUX_HEAP_TRACE=1` on=101；须 rebuild `heap.o`） |
| **asm vec len_empty labi** | ✅ **`c416015e`**（Ubuntu `-backend asm` `tests/vec` run=0） |

## 本波摘要（2026-07-18 · labi vec len_empty fk0）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；挡闸探针 labi `len_empty` / asm 跨函数 let |
| **地图** | Ubuntu 产品红：`-backend asm` `tests/vec/main.x` → `U std_vec_len_empty` 而 `labi_std_fk0_user_needs_rel` 未探针 → 不入 `vec.o` → BLD001；mac dead_strip/弱链掩盖 |
| **根修** | G.7 有则补全：`labi_std_fk0_user_needs_rel` 增 `std_vec_len_empty` / `std_vec_vec_len_empty`（`needs_undef` 仅认 U，C weak 不误拉） |
| **验收** | mac+**Ubuntu** L2：`le_asm` build/run=0 · rv=42 · opt/si/hello；须 `runtime_link_abi.o`+g05 + 有 `std/vec/vec.o` |
| **旁证** | 真跨函数共享 let 仍红（Ubuntu asm `set_g`/`get_g` 栈槽非 ELF BSS；本波不搅） |
| **钉盘** | **仍 `5c4dea78`**（日常 L2 非 tip L4） |

### residual 下一波

1. （旁）asm 真跨函数共享（per-fn stack ≠ ELF BSS；Ubuntu 单 TU `set_g`/`get_g` 仍 0）  
2. 其它产品面 residual 挡闸再开  

```text
构建级别：L2（runtime_link_abi.o + g05；非 tip L4）
产品 tip L4 钉盘：5c4dea78（不变）
权威：seeds/runtime_link_abi.from_x.c → labi_std_fk0_user_needs_rel
探针：Ubuntu -backend asm tests/vec/main.x → 0；nm exe T std_vec_len_empty
纪律：fk0 补全真实 export UNDEF；弱 preamble 不单独当入链依据
```

---

## 前波摘要（2026-07-18 · mutable let decl-init / BSS sentinel）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；旁 residual ELF `.bss` / labi `len_empty` 挡闸探针 |
| **地图** | 产品红：`std.heap` 预编 `.o` 无 main → `init_globals` 不跑；`shu_heap_trace_on = -1` 落 BSS=0 → 永不 `getenv` |
| **根修** | codegen `want_decl_init` 扩到非 const 非数组 let；seed+`.x` 对齐 |
| **验收** | mac+**Ubuntu** L2：`nm d` · `SHUX_HEAP_TRACE=1` exit **101** · 矩阵 |
| **钉盘** | **仍 `5c4dea78`** |

### residual 下一波（当时）

1. （旁）labi `len_empty` / 真 ELF 跨函数共享  

---

## 前前波摘要（2026-07-18 · cfg-extern skip ABI）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；钉盘 `5c4dea78` 已升；开旁 residual cfg-extern |
| **地图** | 非 typeck：`skip_one_extern` 缺 `extern "C"` ABI 串；与 `parse_one_extern` 分叉（G.7） |
| **根修** | `parser_asm_skip_one_extern_into_slice_c` 对齐 parse：可选 TOKEN_STRING 后接 function |
| **验收** | mac+**Ubuntu** L2：keep+skip "C" + assign run=42；矩阵 rv=42/opt/si/hello；**日常 L2 非 tip L4** |
| **钉盘** | **仍 `5c4dea78`** |

### residual 下一波（当时）

1. （旁）ELF `.bss` / labi `len_empty`（挡产品面再开）  

```text
构建级别：L2（parser_asm_thin_glue.o + g05；非 tip L4）
产品 tip L4 钉盘：5c4dea78（不变）
权威：parser_asm_skip_one_extern_into_slice_c
```

---

## 前前波摘要（2026-07-18 · tip L4 升钉 `5c8204ae` → `5c4dea78`）


| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 tip L4 升钉（钉盘仍 `5c8204ae`，tip 含 f64/METHOD_CALL/fs dual-GP 等整波 L2 收口） |
| **地图** | 产品放行 = 全擦 `.o` + 重链 `shux`/`shux_asm`/`shux-c` + 矩阵 + 双端 `run-all-bstrict` 123；**非**日常 L2 冒充 |
| **动作** | Ubuntu：`git pull` → 全擦 → bootstrap-driver-seed + g05 → 矩阵 → bstrict；mac：同构 L4 |
| **验收** | **双端 L4 真冷 + 123/123** @ `5c4dea78`；Ubuntu rv=42 · opt/si/hello=0；mac 同矩阵 + 123 OK |
| **钉盘** | **`5c8204ae` → `5c4dea78`** |

### residual 下一波

1. （旁）cfg-extern / ELF `.bss` / labi `len_empty`（挡产品面再开）  
2. 其它产品面 residual 挡闸再开  

```text
构建级别：L4 真冷（全擦 compiler+std+core .o + 重链 shux/shux_asm/shux-c）
产品 tip L4 钉盘：5c4dea78（前钉 5c8204ae）
Ubuntu：cold /tmp/ubuntu_true_cold_5c4dea78.log · bstrict /tmp/ubuntu_true_bstrict_5c4dea78.log · BSTRICT_RC=0 · 123
macOS：cold /tmp/mac_true_cold_5c4dea78.log · bstrict /tmp/mac_true_bstrict_5c4dea78.log · BSTRICT_RC=0 · 123
矩阵：rv_build=0 rv_run=42 · opt/si/hello_build=0 · hello_run=0
纪律：双端真冷齐绿后升钉；禁止日常 L2 冒充 tip L4
```
