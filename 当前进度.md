# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`eafe0aea`**（METHOD_CALL resolve → SSE param types） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm METHOD_CALL SSE** | ✅ **非 lit VAR / post-clobber abs/signum**（`movq xmm0` 非 `rdi`） |
| **with_arena / vec** | ✅ 前波 `b4dbb111` / `2d9da1ce` |

## 本波摘要（2026-07-18 · METHOD_CALL 非 lit SSE residual）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 METHOD_CALL 非 lit SSE（abs/signum 依赖 xmm0 残留） |
| **地图** | floor(lit) 有 `movq xmm0`；floor(var)/abs 后曾 `mov rdi`；abs(binop) run=0 因 subsd 留 xmm0；floor(var) run=3 |
| **根因** | 产品 `math.*` 为 EXPR_METHOD_CALL(49)；`glue_asm_resolve_call_target_module_c` 只处理 CALL+FIELD_ACCESS(44) → param_type_ref=0 → is_sse 仅 FLOAT_LIT 真 |
| **根修** | G.7：同权威 resolve 补 METHOD_CALL binding→dep func；param SSE + ret harvest 共用 |
| **验收** | Ubuntu L2：floor_var/abs_nonlit/clobber **run=0**；math_full **run=0**（含 VAR+clobber 断言）；产物见 `6e` before call + `7e` harvest；rv=42；wa asm=0；**日常 L2 · 非 tip L4** |

### residual 下一波

1. （旁）prove residual：`backend_enc_dispatch` -E / `rt_diag_errno`  
2. （旁）freestanding CG002 elf_ec  
3. （可选）f64 let-init binop（`let x = 0.0 - 1.5` 曾 integer neg residual，与本波 SSE 无关）  

```text
构建级别：L2（pipeline_x.o + g05；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：math_asm run=0；floor_var/clobber=0；rv=42；wa=0
纪律：测试 hard timeout；本波未升 tip L4
feat SHA：eafe0aea
日志：/tmp/ubuntu_g05_method_sse.log
```
