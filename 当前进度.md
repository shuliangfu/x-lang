# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`0d51a264`**（NL-07 L5 crt0 codegen/parser residual partials） |
| 产品 L4 放行 | 仍钉 **`5a636984`** 双端 L4+123（**未**升 tip 全擦 L4） |
| tip 双端 bstrict | ✅ **123/123 @ `36ac3d14`**（g05 + SKIP_BUILD；**非 L4**；本波未重跑全量） |
| **std.vec 产品 -o** | ✅ **双端** formal `vec.o` + on-demand · `with_arena_vec_push` |
| **NL-07 硬绿 L1** | ✅ **multi-def=0** |
| **NL-07 硬绿 L2** | ✅ **`fflush` 有定义** |
| **NL-07 硬绿 L3** | ✅ **`backend_enc_*` / arch_emit / try_inline / simd =0** |
| **NL-07 硬绿 L3b** | ✅ **`backend_emit_*=0`**（seed partial export） |
| **NL-07 硬绿 L4+** | ✅ **typeck_*=0** · driver 主簇关 · lsp 主簇关 |
| **NL-07 硬绿 L5** | ✅ **parser_*/codegen_* 前排关**（residual-only partial；multi=0） |
| **NL-07 crt0 全包** | 🔴 unique UNDEF=**9**（**仅** nostdlib libc 面：`fileno`/`isatty`/`puts`/`strerror`/`fread`/`ferror`/`stdin`/`remove`/`__ctype_b_loc`；**≠** 零 libc 硬绿） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · NL-07 L5 codegen/parser residual partials）

| 波 | 结果 |
|----|------|
| **入口** | 前排硬绿分层 L5：crt0 入链 codegen/parser residual（**非** tip L4 真冷） |
| **根因** | crt0 首遍 bag 为 OK-parser/codegen/pipeline-stub（size≈11）；glue_standalone U-ref 真机在 experimental/g05 同源对象；full `parser_x`/`codegen_x`/`strict_glue` 会 multi-def |
| **根修** | **`234affc4`…`0d51a264`**：`ensure_crt0_codegen_parser_companion_objs` — residual-only `ld_partial_export`（L3b 模式）：codegen_x / x_frontend / strict_glue / fmt_check / lsp_ctx / strict_minimal / parser_x cascade / thin_glue `parser_*_glue`；+ `lexer_x` / `cfg_eval` / lsp sizes nostub / `process_argv` / `parser_asm_parse_expr_link`；parser/pipeline partial **仅 bag 未自举** |
| **验收 Ubuntu** | TRY_BUILD：`multi=0`；L1–L4+ 保持；unique UNDEF **41→9**；head=libc 面；**无** `crt0 link OK`；gate final-link 绿 |
| **双端产品矩阵** | mac g05 + rv/opt/hello/si **绿**；Ubuntu g05 + rv/opt/hello/si **绿**（`0a` 换行） |
| **门禁** | v4 gate **OK**（final-link loose）；**仍无** `bootstrap nostdlib crt0 link OK` |
| **日志** | Ubuntu N07 `/tmp/ubuntu_n07_l5d_0d51a264.log` · g05 `/tmp/ubuntu_g05_n07_l5.log` · mac g05 `/tmp/mac_g05_n07_l5.log` |

### 五问（NL-07 L5 codegen/parser）

| 问 | 答 |
|----|-----|
| 谁产生 | glue_standalone / bridge U-ref `codegen_*` / `parser_*` / `cfg_*` / lsp sizes / `driver_run_{fmt,check}`；首遍 bag stub 无定义 |
| 谁存储 | experimental/g05 同源 `.o`；partial export 只留 residual T/W（G.7；禁整行 mega） |
| 谁消费 | Linux crt0 `ld` 行（`CRT0_BACKEND_COMPANIONS` 追加 L5） |
| 同模式 | L3b seed emit partial；full parser_x multi≈265 与 bag；thin_glue 全链 multi=4 → glue-only partial |
| 最小回归 | multi=0；parser/codegen residual=0；unique **9** libc 面；双端 g05 产品矩阵 |

### residual 下一波

1. 用户明示时 tip **L4 真冷** + 双端 bstrict 123（升放行钉盘）  
2. NL-07 **L6**：nostdlib libc 面（`fileno`/`isatty`/`puts`/`strerror`/`fread`/`ferror`/`stdin`/`remove`/`__ctype_b_loc`）进 stubs / freestanding  
3. 全链 `bootstrap nostdlib crt0 link OK`（L6 后）  

```text
构建级别：日常 L2/L3（NL-07 L5；非 tip L4 全擦）
产品 tip L4 钉盘：5a636984
tip HEAD：0d51a264
NL-07 L1–L5：multi=0 · fflush 关 · backend_enc=0 · backend_emit=0 · typeck=0 · driver/lsp 主簇关 · parser/codegen 前排关 · crt0 仍红（UNDEF=9 · 仅 libc 面）· gate final-link 绿
双端产品矩阵：mac+Ubuntu g05 rv=42 / opt / hello / si 绿
```
