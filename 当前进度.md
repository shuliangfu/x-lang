# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `ae3b412f`（feat diagnostic Cap residual pure 深迁 + Ubuntu 金标） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **49**（diagnostic 仍 R2 thin；本波 H↓） |
| **H** | **diagnostic rest T 73→59**（剔除 14 pure-dup）；仍 Cap residual snprintf/va_list/debug |

## 本波：Track R · `diagnostic` **Cap residual pure 深迁** ✅

| 路径 | 变更 |
|------|------|
| `runtime_driver_diagnostic_thin.x` | pure 真体：固定措辞 typeck 10 + pipe orch 4（before_codegen/source_len/after_entry_parse/pipe_marker） |
| `runtime_driver_diagnostic.from_x.c` | FROM_X 下剔除 14 pure-dup `_impl`；冷启动全 C 体仍在 |
| `runtime_driver_diagnostic_thin_surface.from_x.c` | prove 锁 thin surface（同构 thin.x） |
| `g05` / `prove` | 注释标明 pure 深迁；产品仍 PREFER thin hybrid |

- 验收：mac/Ubuntu `prove` → **diagnostic IDENTICAL**；全量 **54 IDENTICAL**
- KPI：**H↓** rest 业务 T **73→59**（gone：10 固定措辞 typeck + 4 pipe orch pure-dup `_impl`）
- **不是** thin 表面 KPI：产品链 pure 真体在 thin.x；Cap residual 仍 rest（snprintf/va_list/debug）

## 下一波前排

1. **R2 真迁 / Cap residual 深迁**（禁止只改注释/surface）：`diagnostic` 余下拼装 pure（return/assign/call/struct/asm note）或 `driver_abi` pure-dup / `fmt_check` Cap residual  
2. **Track M** typeck M2 smoke  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档；报数写 **rest 业务 T 前后**
