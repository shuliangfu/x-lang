# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `g-02f-self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **15** IDENTICAL（+ `labi_freestanding_list`） |
| **diagnostic** | `-E`+cc ✅；**CORE_OK**（非 IDENTICAL / 非退役） |
| **W-string-nul** | ✅ STRING_LIT *u8 尾 `, 0`；slen≤64 |

## 本波：Track L `labi_freestanding_list`（L7）✅

**事实**：L5/L6 同构模式可复用。env / io_sym / ensure 短字面量用 if/else+`let p: *u8`；`catalog_step_at` 的 C `const char**` 在 .x 用 `*usize` 写指针（同 ABI）；seed 去掉 static 表以便全局符号 IDENTICAL。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/runtime/labi_freestanding_list.x` | L7 真迁 |
| `seeds/labi_freestanding_list.from_x.c` | 冷启动同构 if/else |
| `scripts/g05_ensure_relink_prereqs.sh` | L7 PREFER_X_O |
| `tests/prove_module_selfhost.sh` | 扩门禁 |

- 验收：`bash tests/prove_module_selfhost.sh` → **15 IDENTICAL** + diagnostic **CORE_OK**

## Ubuntu 金标准

（推送后在 worker tree 验 `labi_freestanding_list` IDENTICAL；见 §8）

## 下一步

1. 下一 labi list pure（`labi_std_list` / ondemand）或 diagnostic hybrid→IDENTICAL  
2. W-global 数组；串池 >64；`labi_diag_pure` reportf  
3. 每波：**commit → push → Ubuntu prove → 再下一步**；§8 **东八区**
