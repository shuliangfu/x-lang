# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`d2ee663d`**（import 形参 map + FIELD_ACCESS resolve；math 名门控 residual） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm with_arena / vec product** | ✅ **Ubuntu L2 build/run=0**（mangle 符号全后缀） |
| **asm string smoke** | ✅ **copy_proof run=0** |
| **asm math run** | ✅ **math_asm run=0**（仍依赖 `std_math_*` 名门控 residual） |

## 本波摘要（2026-07-18 · vec mangle 验收 + import 形参半程）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 vec.* METHOD_CALL mangle |
| **vec mangle** | ✅ Ubuntu：`with_arena_vec_push` / `vec_i32_push` **build=0 run=0**；call site 为 `std_vec_new_retVec_*` / `push_Vec_*_ptr_*` / `len_Vec_*`（共用 import mangle 权威） |
| **import 形参** | 半程：`pipeline_asm_call_param_type_ref_at_c` 强制 dep→caller map；`resolve_call_target` 补 **FIELD_ACCESS** binding；harvest 走完整 `call_return_type_kind_ord`；**仍留** `std_math_*` pre/post 名门控（去门控后 math_asm 曾红） |
| **邻域** | rv=42；copy_proof=0；wa=0；vec=0；math_asm=0；**日常 L2 · 非 tip L4** |
| **纪律** | hard timeout；改 glue 重建 standalone/pipeline_x；改 call_dispatch + g05 |

### residual 下一波

1. **收敛 `std_math_*` 名门控**（类型权威必须在 product main emit 路径证明进 xmm；禁半绿去门控）  
2. rest C leaf  
3. （旁路）asm freestanding vec 运行时 ABI residual（`-backend asm` vec_i32 segfault；非 mangle）

```text
构建级别：L2（pipeline_glue_standalone + call_dispatch + g05；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：vec product run=0；math_asm run=0；rv=42；cp=0；wa=0
纪律：测试 hard timeout；本波未升 tip L4
SHA 波：3b9c9080 → 59b9fd38 → cd85ac92 → d2ee663d
```
