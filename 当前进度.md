# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`87b68e88`**（asm f64 SysV **math_asm run 绿**） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm hello / si / math-link** | ✅ `cb4605ee` / `67b61ea8` / `a1f67402` |
| **asm math run (f64 SysV)** | ✅ **Ubuntu math_asm run=0**（L2） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · asm f64 SysV 收口）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排钉 CALL 真实 emit 路径 |
| **根因** | ① 半程 harvest 写在 `with_cleanup`/import 分支，但 **mac/Ubuntu 工作区曾分叉** 致 Ubuntu 未吃到补丁；② 真权威出口是 **`glue_asm_enc_call_redirected`**；③ import 实参槽把 f64 误归 gp→`mov rdi`，callee 读 xmm0 |
| **修法（G.7 单权威）** | 在 `glue_asm_enc_call_redirected_impl` 对 **`std_math_*`**：CALL 前 `movq xmm0,rax`（arg），CALL 后 `movq rax,xmm0`（ret harvest）；f32 xmm 轨 `use_xmm` 改走 `glue_call_arg_is_sse_float_c` |
| **Ubuntu 产物** | `call std_math_floor` 前见 `66 48 0f 6e c0`；后见 `66 48 0f 7e c0`；**run=0** |
| **邻域** | L2：`rv=42` 绿；math_c 绿；**日常 L2 · 非 tip L4** |
| **纪律** | hard timeout；**禁止** 把 L2 绿写成 tip L4 |

### residual 下一波

1. asm string/wa heap 链  
2. import 形参 type 映射 → 去掉 `std_math_*` 名门控（收敛到类型权威）  
3. 工程轨 rest C 具体叶  

```text
构建级别：L2（call_dispatch + g05；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：math_asm build=0 run=0；rv=42；math_c=0
纪律：测试 hard timeout；本波未升 tip L4
```
