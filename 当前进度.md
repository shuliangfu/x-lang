# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **slice data_field asm**（feat 待合；L4 钉盘仍 **`f16f7d48`**） |
| 产品 L4 放行 | ✅ **钉盘 `f16f7d48`**（双端真冷 L4+123） |
| tip 双端 bstrict | ✅ **123/123 @ `f16f7d48`**（本波 **非** tip L4 升钉） |
| **slice main asm SIGABRT** | ✅ **`6fda8bd2`** |
| **slice data_field / region** | ✅ **本波**（data_field asm=0；region 随 dual-home 绿） |
| **asm env/fs BLD001** | ✅ **`4b10fb1d`** |

## 本波摘要（2026-07-18 · slice data_field / region 字段·赋值）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 slice `data_field` / `region`（钉盘 `f16f7d48` 不动） |
| **地图** | default asm：`data_field.x` exit **2**（`s.length != 4`）；C 后端=0；`region_array_smoke` 在 main 波后已 20/20 |
| **根因** | ① typeck `pipeline_typeck_field_slice_c` 只设 resolved type，**未写 `field_access_offset`** → asm lea 后 offset=0 读到 `.data`；② `glue_emit_slice_from_array_let_init` 把 length 存到 `slot_off+8`（越界），与 SysV dual-GP **high@slot_off-8** 约定相反（bounds 路径同号错误曾假绿） |
| **根修** | G.7：typeck 写 data@0 / length@8；layout_offset 内建 TYPE_SLICE；init/load length 用 **slot_off-8** |
| **验收** | **日常 L2**：mac data_field/main/region asm+c 绿 · rv=42 · opt build；Ubuntu 金标同矩阵（见 commit 后） |
| **钉盘** | 仍 **`f16f7d48`** |

### residual 下一波

1. （旁）slice `length.x` core API / BLD001 链（若仍红）  
2. （可选）tip L4 升钉含 slice 两波  
3. （旁）IR/WPO 常量折叠（**不在 emit 扩**）  

```text
构建级别：L2（pipeline_glue_standalone.o + g05；非 tip L4）
产品 tip L4 钉盘：f16f7d48（本波未升）
权威：pipeline_typeck_field_slice_c · glue_emit_slice_from_array_let_init_elf_c · glue_emit_slice_length_to_rbx_elf_c · pipeline_expr_field_access_layout_offset
纪律：禁止日常 L2 冒充 tip L4；一条债一层；dual-GP home 与 struct 16B 同约定
```

---

## 前波摘要（2026-07-18 · slice asm main SIGABRT）

| 波 | 结果 |
|----|------|
| **根修** | SIMD 启发式排除 TYPE_SLICE + INDEX load `.data`（**`6fda8bd2`**） |
| **验收** | Ubuntu/mac slice main asm=20 |
| **残差** | 本波已接 data_field/region |
