# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `fba6128b`（fmt_check path_resolve_abs pure + Cap path BSS slot + codegen.x 顶层 u8[N] 根修） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **49**（fmt_check 仍 R2 thin；本波 pure 体 + Cap residual 槽） |
| **H** | **fmt_check rest T 20**（resolve_abs_impl 剔除，+`fmt_check_path_bss_slot` Cap residual；净持平）；diagnostic rest T=28；driver_abi rest T=173；仍 Cap residual walk opendir/stat/argv/大 BSS/missing-diag/one_file_body / uname/setrlimit/pthread/path-read/snprintf/va_list |

## 本波：Track R · `fmt_check` **path_resolve_abs pure 深迁** + Cap path BSS ✅

| 路径 | 变更 |
|------|------|
| `fmt_check_cmd_thin.x` | pure 真体：`fmt_path_resolve_abs`（绝对字节拷 / 相对 getcwd+字节拼）；`set_current_file`/`print` 写读 Cap residual BSS slot |
| `fmt_check_cmd.from_x.c` | FROM_X 下剔除 `fmt_path_resolve_abs_impl`；新增 `fmt_check_path_bss_slot`（2×512 可写路径 BSS） |
| `codegen.x` | 根修：顶层 `u8[N]` 发 `static T name[N][= {…}]`，init_globals 跳过 TYPE_ARRAY（`codegen_gen.c` 待 force-regen 生效） |
| surface | prove 锁 thin surface；-E 抛光 regen |

- 验收：mac/Ubuntu `prove` → **fmt_check IDENTICAL**；`shux check` **rc=0**；rest T **20**；hybrid OK
- KPI：**H 净持平** rest 业务 T **20**（-resolve_abs_impl +path_bss_slot）；pure 真体在 thin.x
- **根因**：`-E` 顶层 `u8[N]` 退化为悬空指针 → check SIGSEGV；writable 缓冲暂走 Cap residual 槽，codegen_gen 再生后可收回槽

## 下一波前排

1. **R2 真迁 / Cap residual 深迁**（禁止只改注释/surface）：`fmt_check` 余下 Cap residual（walk/stat/argv/大 BSS/missing-diag/one_file_body；path BSS 槽待 codegen_gen 收回）/ Cap-fn-ptr / diagnostic va_list·snprintf residual / driver_abi uname·setrlimit·pthread  
2. **Track M** typeck M2 smoke；或 **codegen_gen force-regen** 使顶层 u8[N] 根修进产品 -E  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档；报数写 **rest 业务 T 前后**
