# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `cbc59f87`（feat Ubuntu 金标）/ docs 待推 |
| **T** | **18/18** typeck OK |
| **N** | **53** IDENTICAL（附注） |
| **R2** | **36** |
| **H** | 三十六切片 thin/public **H=0**；driver/diagnostic rest 仍持 Cap *_impl |

## 本波：Track R · `driver_abi` R2 thin full ✅

| 路径 | 变更 |
|------|------|
| `runtime_driver_abi_thin.x` | 61+ 公共门闩（**Track L 已真迁**；本波 R2 标注） |
| `runtime_driver_abi.from_x.c` | 新增 `runtime_driver_abi_slice_marker` |
| `runtime_driver_abi_thin_surface.from_x.c` | prove 锁 surface |
| `prove_module_selfhost.sh` / `g05` | R2 thin 标注 + surface 入口 |

- 验收：mac + Ubuntu `prove_module_selfhost driver_abi` → **IDENTICAL**
- KPI：**R2 35→36**；thin 公共面在 rest 下 **H=0**
- **Cap residual**：`*_impl` / 静态槽 / path-read / spawn 仍在 full seed rest

## 下一波前排

1. **R2** `fmt_check` L2 thin → R2（同 diagnostic/driver_abi 模式）  
2. **Track M** typeck M2 smoke  
3. 纪律：commit → push → Ubuntu → 更新进度文档