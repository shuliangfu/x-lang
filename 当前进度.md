# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `4b99f0e5`（fmt_check append_repo + missing_diag pure 深迁） |
| **T** | **18/18** typeck OK |
| **N** | **54** IDENTICAL（附注） |
| **R2** | **49**（fmt_check 仍 R2 thin；本波 pure 体剔除 2 Cap residual） |
| **H** | **fmt_check rest T 18**（20→18；-append_repo_impl -missing_diag_impl）；diagnostic rest T=28；driver_abi rest T=173；仍 Cap residual walk opendir/stat/argv/大 BSS/one_file_body / uname/setrlimit/pthread/path-read/snprintf/va_list |

## 本波：Track R · `fmt_check` **append_repo + missing_diag pure 深迁** ✅

| 路径 | 变更 |
|------|------|
| `fmt_check_cmd_thin.x` | pure 真体：`check_append_repo_lib_roots`（getcwd/字节拼/dirname 上溯 + try_append）；`collect_paths_missing_diag_pure`（字节拼 msg + `diag_report_with_code`） |
| `fmt_check_cmd.from_x.c` | FROM_X 下剔除 `check_append_repo_lib_roots_impl` / `collect_paths_missing_diag_impl` |
| surface | prove 锁 thin surface IDENTICAL；-E 抛光 regen |

- 验收：mac/Ubuntu `prove` → **fmt_check IDENTICAL**；`shux check` **rc=0**；rest T **18**；hybrid OK；missing path 烟测出 `cannot access path '…'`
- KPI：**H↓** rest 业务 T **20→18**；pure 真体在 thin.x
- Cap residual 余：path_bss_slot / walk opendir / path_stat / try_append body / init_user_lib_flags / argv_append / file_list store·clear / parse_ignore 体 / run_fmt / one_file_body / run_compiler_check / BSS getters / marker

## 下一波前排

1. **R2 真迁 / Cap residual 深迁**（禁止只改注释/surface）：`fmt_check` 余下 Cap residual（walk/stat/argv/大 BSS/one_file_body；path BSS 槽待 codegen_gen 收回）/ Cap-fn-ptr / diagnostic va_list·snprintf residual / driver_abi uname·setrlimit·pthread  
2. **Track M** typeck M2 smoke；或 **codegen_gen force-regen** 使顶层 u8[N] 根修进产品 -E  
3. 纪律：commit → push → Ubuntu 金标 → 更新进度文档；报数写 **rest 业务 T 前后**
