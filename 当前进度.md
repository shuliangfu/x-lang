# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`6ee437ac`**（slice residual：Option_u8 sret + TYPE_SLICE VAR/field；L4 钉盘仍 **`f16f7d48`**） |
| 产品 L4 放行 | ✅ **钉盘 `f16f7d48`**（双端真冷 L4+123） |
| tip 双端 bstrict | ✅ **123/123 @ `f16f7d48`**（本波 **非** tip L4 升钉） |
| **slice length BLD001** | ✅ **链闭**（`5748d795`…`d1b9e984`） |
| **length multi-Option_u8 residual** | ✅ **`81b0f333`**（false sret 关） |
| **subslice_split_chunks residual** | ✅ **`db295b27`+`6ee437ac`** |
| **slice data_field / region** | ✅ **`adbbb001`** |
| **slice main asm SIGABRT** | ✅ **`6fda8bd2`** |

## 本波摘要（2026-07-18 · Option_u8 false sret + TYPE_SLICE VAR/field）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 length multi-Option_u8 exit 4 + subslice asm residual（钉盘 `f16f7d48`；**不**自动 tip L4） |
| **地图·length** | Ubuntu pure asm full length.x **run=4**；三枚 `Option_u8` deferred；get_u8 反汇编 = **假 sret**（dest→rdi）而 formal 仍 rdi=slice、返回 rax |
| **根因·length** | `call_ret_sz=8` 正确；`named_layout` 把 Option_u8 抬到 **24**（跨 arena layout）；旧 inflate **总用 named 覆盖 ≤16** → 8→24 → TAKE_SRET → 槽未写 |
| **根修·length** | **`81b0f333`**：callee 已是寄存器类 **1..16** 时禁止 named 抬到 >16 假 sret；named 仅 dual-GP 内拓宽或弱 call_ret 升级真 MEMORY |
| **地图·subslice** | exit1：`sub.length` 无 +8；exit2：`sub[i]` lea fat 不 load `.data`；exit4：`sp.left.length` 嵌套 FIELD |
| **根因·subslice** | import asm **skip typeck** → VAR `resolved_type_ref=0`；let 注解 `i32[]` 未回落；嵌套 base 是 FIELD_ACCESS 非 VAR；INDEX field 仅对 `*T` deref |
| **根修·subslice** | **`db295b27`**：`glue_var_expr_type_ref_with_decl_fallback_c`（INDEX / field offset / elem size）；**`6ee437ac`**：FIELD_ACCESS base → field type 的 TYPE_SLICE +8；INDEX field load `.data` when TYPE_SLICE |
| **验收** | **日常 L2**：Ubuntu **length asm run=0** · **subslice asm run=0** · data_field · rv/opt/hello/si · slice main；**非 tip L4** |
| **钉盘** | 仍 **`f16f7d48`** |
| **残差** | 可选 tip L4 升钉；named_layout Option_u8=24 跨 arena 真因可另波收敛（本波 sret 门控已挡假绿） |

### residual 下一波

1. （可选）tip L4 升钉含 slice formal + residual 全波  
2. （旁）`glue_type_named_layout_size` Option_u8=24 跨 arena 根因（非阻塞）  
3. （旁）IR/WPO 折叠（禁 emit）  

```text
构建级别：L2（pipeline_x.o + pipeline_glue_standalone + g05；非 tip L4）
产品 tip L4 钉盘：f16f7d48（本波未升）
feat 链：81b0f333 → db295b27 → 6ee437ac
Ubuntu：length asm=0 · subslice asm=0 · data_field · 矩阵绿
权威：glue_emit_struct_type_let_init sret gate · glue_var_expr_type_ref_with_decl_fallback_c · field_access_layout_offset TYPE_SLICE · glue_index_deref_ptr_field_slot_*
纪律：禁止日常 L2 冒充 tip L4；call_ret 1..16 不得被 named 抬 sret
```

---

## 前波摘要（2026-07-18 · slice length.x / core.slice formal BLD001）

| 波 | 结果 |
|----|------|
| **根修** | formal mod.o + g9 + TYPE_SLICE 1GP call ABI + core_ prefix（**`d1b9e984`** 链） |
| **验收** | length asm BLD001 关；full run=4 residual（本波已接） |
| **残差** | 本波已闭 multi-Option + subslice |
