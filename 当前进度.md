# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `f3573739`（feat）/ docs 待合 |
| **T** | **18/18** typeck OK |
| **N** | **53** IDENTICAL（附注） |
| **R2** | **23**（+`rt_dispatch`；Ubuntu 金标） |
| **H** | 上述二十三切片 **H=0**；其余 runtime hybrid 多仍 rest C（labi / diagnostic thin…） |

## 本波：Track R · `rt_dispatch` R2 full 真迁 ✅

| 路径 | 变更 |
|------|------|
| `src/runtime/rt_dispatch_thin.x` | 吃满 asm/emit 薄门闩 + `run_compiler_full` + `try_compile_via_shu_c_sibling` |
| `seeds/rt_dispatch_thin.from_x.c` | 业务符号 `#ifndef SHUX_RT_DISPATCH_THIN_FROM_X`；FROM_X 下 rest 仅前向 + marker → **业务 H=0** |
| Cap residual `driver_abi` | `driver_dispatch_sibling_try_spawn`（path/access/fork/exec） |
| `seeds/rt_dispatch_thin_surface.from_x.c` | full surface（prove 锁） |
| `tests/prove_module_selfhost.sh` | 标注 `rt_dispatch` R2 full |

- 验收：mac + Ubuntu `prove_module_selfhost` → **53 IDENTICAL**（含 `rt_dispatch` R2）
- KPI：**R2 22→23**；N **53**（附注，此前 thin 已在 N）；该切片 H **1→0**
- 产品 hybrid：full .x + rest 仅 marker

## 下一波前排

1. **R2** runtime 其余 hybrid rest（labi / diagnostic thin rest…）· Cap-fn-ptr 批量  
2. **Track M** typeck M2 试替换 `typeck_x.o` smoke  
3. 纪律：commit → push → Ubuntu → docs；KPI **H↓ R2↑**
