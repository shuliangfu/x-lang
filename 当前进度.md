# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`fffc0d59`**（NL-07 L7 运行时：%g + preprocess_x + user_asm_seed_bridge） |
| 产品 L4 放行 | 仍钉 **`5a636984`** 双端 L4+123（**未**升 tip 全擦 L4） |
| tip 双端 bstrict | ✅ **123/123 @ `36ac3d14`**（g05 + SKIP_BUILD；**非 L4**；本波未重跑全量） |
| **std.vec 产品 -o** | ✅ **双端** formal `vec.o` + on-demand · `with_arena_vec_push` |
| **NL-07 硬绿 L1–L6** | ✅ multi=0 · fflush · enc/emit · typeck/driver/lsp · parser/codegen · libc 面 · **crt0 link OK** · unique=0 |
| **NL-07 运行时 L7** | ✅ freestanding `%g`/`%e`；✅ crt0 真 `preprocess_x.o`；✅ crt0 `user_asm_seed_bridge`；✅ **postlink smoke 无 fallback 稳定绿**；✅ 纯 static f64 非 `g.0` |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · NL-07 L7 nostdlib 运行时质量）

| 波 | 结果 |
|----|------|
| **入口** | L1–L6 链接硬绿已关；前排运行时：postlink smoke 偶发 WARN、纯 static f64 发 `g.0`（**非** tip L4） |
| **根因地图** | ① `pipeline_codegen_emit_float_lit_c` 用 `%.17g`，stubs `vsnprintf` 无 `%g` → default 吐字面 `g` + 补 `.0`；② crt0 缺 `preprocess_x.o` → experimental_symbol_bridge **弱桩** `preprocess_x_buf` 恒 -1 → PP002；③ crt0 缺 `user_asm_seed_bridge` → 弱桩 `asm_*_codegen_elf_o` 恒 -1 → CG002 / smoke WARN |
| **根修** | **`156d01f9`** stubs `%g/%e`→`bootstrap_format_double`；**`5adee364`** crt0 入链 `preprocess_x.o` + export `preprocess_eval_condition_c`；**`fffc0d59`** crt0 入链 `user_asm_seed_bridge.o`（G.7 与 experimental/strict 同源产品物，禁第二套桩） |
| **验收 Ubuntu pure static** | `SHUX_BOOTSTRAP_NOSTDLIB=1` + `NO_POSTLINK_FALLBACK=1`：crt0 link OK · multi=0 · **postlink smoke OK×3 无 fallback** · f64 `1.500000` · rv=42（c 与 default asm） |
| **纯 static residual** | hello / option / stdlib-import 仍 **XT001** check_block（import 型产品面；**≠** smoke/f64 已关项） |
| **双端 g05 产品矩阵** | mac + Ubuntu g05 rv/opt/hello/si **绿**（g05 不链 stubs；不回归） |
| **日志** | `/tmp/ubuntu_n07_l7c_fffc0d59.log` · smoke 复测同机 · mac g05 `/tmp/mac_g05_n07_l7b.log` |

### 五问（NL-07 L7 运行时）

| 问 | 答 |
|----|-----|
| 谁产生 | emit 用 `%.17g`；crt0 链缺 preprocess/asm 产品 companion → 弱桩覆盖 |
| 谁存储 | `bootstrap_nostdlib_stubs` vsnprintf；`ensure_crt0_codegen_parser_companion_objs` |
| 谁消费 | 纯 static `shux_asm` 的 -E 浮点字面量 / -c preprocess / -backend asm -o |
| 同模式 | L5 residual-only companions；禁在 freestanding_io / 热路径再开第二套 |
| 最小回归 | smoke 无 fallback；f64 非 g.0；rv=42；双端 g05 矩阵；multi=0 |

### residual 下一波

1. 纯 static **import 产品面** typeck（hello/option/si XT001）  
2. 用户明示时 tip **L4 真冷** + 双端 bstrict 123  
3. 默认 `SHUX_BOOTSTRAP_NOSTDLIB=1` / 去系统 pthread（策略文）  

```text
构建级别：日常 L2/L3（NL-07 L7 运行时；非 tip L4 全擦）
产品 tip L4 钉盘：5a636984
tip HEAD：fffc0d59
NL-07 L1–L6 链接硬绿保持；L7：%g + preprocess_x + user_asm_seed_bridge · smoke 稳定 · f64 OK · pure static hello 仍 XT001
双端产品矩阵：mac+Ubuntu g05 rv=42 / opt / hello / si 绿
```
