# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b`（seed pin 未换） |
| **M codegen** | ✅ Cap+M1；**M2 嵌套+forward+CALL mangle+placeholder emit**；force residual：**hello 共 emit**（io skip 表） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **Cap force si `core_types_placeholder`** | Cap `codegen_should_skip_emit_func_by_name` 仍 skip 裸名 `placeholder`；seed 已移除。共 emit `core.types` 时只出 size_of_* 体、丢 placeholder → si `-o` UNDEF。 |
| **修法（对齐 seed）** | 删除 by_name 对 placeholder / string_new / std_string_* 的 skip；仅保留 seed_mega 门控；prefix>0 时 legacy un-skip 扩为安全带（seed 亦有 core_mem 残）。 |
| **验证** | Cap force：**rv/opt/si `-o` ✅**（placeholder 体入 C）；**hello ❌**（`std_io_core_*` undeclared + `shux_io_*` / driver redef）。**pin 矩阵绿**。**未 re-pin / 未 tip L4**。 |

### 五问

| # | 答 |
|---|----|
| 产生 | Cap by_name 落后 seed，仍 skip 真实 export `placeholder` |
| 存储 | 共 emit C 中缺 `core_types_placeholder` 强定义；preamble 仅 extern |
| 消费 | si main → ld UNDEF |
| 同模式 | core_mem 已有 un-skip 历史；fmt 等亦 export placeholder |
| 最小回归 | Cap force si `-o`；pin 矩阵；勿 re-pin |

### 探针

| 项 | 状态 | 备注 |
|----|------|------|
| Cap force rv/opt | ✅ | |
| Cap force si `-o` | ✅ | 本波关闭 UNDEF |
| Cap force hello `-o` | ❌ | io co-emit skip residual |
| pin 矩阵 mac | ✅ | rv/opt/hello/si |

### residual 下一波

1. Cap force **hello** 共 emit：`codegen_should_skip_emit_std_io_core_io_dup` / 相关 skip 表与 seed 对齐（Cap 多 emit `shux_io_read_fixed` 等与 weak preamble 重定义；`std_io_core_shux_io_provided_buffer_ptr` 无 #define）  
2. 全绿才 re-pin；**禁** tip L4  

```text
构建级别：工程 L2（codegen by_name placeholder seed-align；非 tip L4；seed pin 未换）
产品 tip L4：5a636984
codegen M2：嵌套✅ forward✅ CALL mangle✅ placeholder✅；force residual=hello co-emit
产品矩阵：pin mac 绿；Cap force rv/opt/si 绿、hello 红
```
