# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`39913152`**（freestanding soft libm） |
| 产品 L4 放行 | ✅ **钉盘 `364cf3e7`** 双端 L4 真冷 + bstrict 123（前钉 `abfcf576`） |
| tip 双端 bstrict | ✅ **123/123 @ `364cf3e7`**（L4 全擦；本波 **非** tip L4 升钉） |
| **std.vec 产品 -o** | ✅ formal `vec.o` + need_vec→heap/mem + L4 wipe ensure |
| **std.math 产品 -o** | ✅ formal `math.o` + hosted `-lm`；freestanding soft libm smoke 绿 |
| **NL-07 L1–L10 / v5** | ✅ 见上波；产品 g05 nostdlib |
| **freestanding execvp + %.\*s** | ✅ `2db64465` / `abfcf576` |
| **freestanding soft libm** | ✅ **`39913152`** 关 erfc/`__builtin_*` 自递归 |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · freestanding soft libm）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」：前排 freestanding 数学桩无限递归 warning |
| **地图** | `bootstrap_nostdlib_stubs` 中 `double erfc(x){return __builtin_erfc(x);}` 等同名 `__builtin_*`：非硬件 intrinsic 时降为 call 自身 → Ubuntu `-Winfinite-recursion` + 调用即挂；hosted std.math 仍走 `runtime_math_libm`+`-lm`（双权威边界清晰） |
| **根修** | G.7 权威 = 本 TU soft helpers（`shux_soft_*`）+ public 仅转发；禁 `__builtin_*` 同名自递归；floor/ceil/sqrt/fabs + 超越函数 + A&S erf/erfc |
| **feat** | **`39913152`** |
| **验收** | **构建级别=L3**（日常；非 tip L4）：Ubuntu 全量 stubs `infinite recursion=0` · soft smoke OK · g05 + 矩阵 rv=42/opt=102/si/hello + `tests/math` -o run=0 · `run-math.sh` OK；mac 矩阵四件套 + `run-math.sh` OK |
| **日志** | Ubuntu stubs `/tmp/ubuntu_soft_libm_cc.log` · g05 `/tmp/ubuntu_g05_soft_math_39913152.log` · math `/tmp/ubuntu_run_math_39913152.log` · mac math `/tmp/mac_run_math_39913152.log` |

### residual 下一波

1. 工程轨 residual（非本波）  
2. 谈 tip L4 升钉时再双端 L4+bstrict（含 soft libm 入链）  

```text
构建级别：L3（日常真测；产品 L4 钉盘仍 364cf3e7）
产品 tip L4 钉盘：364cf3e7
tip HEAD：39913152
本波：freestanding soft libm 关 __builtin 自递归
Ubuntu：soft smoke OK · g05 L3 · 矩阵四件套 · math -o
mac：矩阵四件套 · run-math OK
叙事：前排 erfc 无限递归 residual 关（非 tip L4 升钉）
```
