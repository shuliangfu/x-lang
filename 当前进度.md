# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin**（mac+Ubuntu 矩阵全绿） |
| **parser residual** | ✅ 匿名 `{ let }` primary 根修（**mac+Ubuntu** 矩阵绿 @ `a2964988`） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **parser 匿名 `{ let }` 根修** | primary `TOKEN_LBRACE` 消歧：stmt-like → `EXPR_BLOCK`；struct field 形仍走匿名 struct lit |
| 探针 | `{ let y; return y }` → **42**；mid-block assign=12；if 内嵌=3；原红 parse-skip 清零 |
| 矩阵 **mac** | **rv=42 / opt=102 / si=0 / hello** ✅ |
| 矩阵 **Ubuntu L2** | pull `a2964988` → glue+g05 → **rv=42 / opt=102 / si=0 / hello / anon42=42** ✅ |
| **未做** | tip L4；block-comment 奇次 `"` 硬复现（简单探针已绿） |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | `parser_asm_primary_slice.inc` 对裸 `{` 一律 `anonymous_struct_lit` |
| 谁存储 | 失败 → onefunc skip → residual 顶层 let |
| 谁消费 | typeck/codegen 见残体；整函数 parse-skip |
| 同模式 | 任意函数体内匿名块含 let/return/if |
| 最小回归 | anon `{ let }` 探针 + rv/opt/si/hello + struct lit 形（IDENT:`/`/`}`） |

### residual 下一波

1. lexer block-comment 奇次 `"`：简单探针已绿；需硬复现再动（emit_expr 应用层已避雷；scan_sync 已 comment-aware）  
2. 若升 tip：**双端 L4 真冷 + bstrict 123**（tip 仍 **`5a636984`**）  
3. **禁** 把本叶 / M2 re-pin 写成 tip 产品 L4  

```text
构建级别：双端 L2（parser_asm_thin_glue + g05 + 矩阵；非 tip L4）
SHA：a2964988
产品 tip L4：5a636984
parser：anon { let } → EXPR_BLOCK ✅
产品矩阵：mac ✅ + Ubuntu L2 ✅（rv/opt/si/hello + anon42）
```
