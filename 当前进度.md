# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`c28931a6`**（METHOD_CALL f64 ret → ucomisd；前 `0d6cbc03` divsd） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm hello / hosted** | ✅ **Ubuntu L2**（opt/si/hello/math_asm） |
| **f64 unary / cmp / mul / div** | ✅ **p5_ord / p6_mul / p7_div + METHOD_CALL ret cmp** |
| **math_asm import METHOD_CALL** | ✅ **`math.pi()/floor/…` run=0 · ucomisd** |
| **fs r42 / panic / hello** | ✅ **`run-freestanding-hello` OK** |
| **vec_u16 get** | ✅ **`f67e22a9`** |

## 本波摘要（2026-07-18 · METHOD_CALL f64 ret for ucomisd）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步自举」；扫产品面发现 **`-backend asm` math run=1**（默认 `-o` 绿） |
| **地图** | `math.pi() <= 3.0` 为 **METHOD_CALL(49)**；`glue_binop_operand_is_scalar_f64` 仅对 CALL(48) 再查 resolved，METHOD_CALL 常无 resolved → cmp finish 走 **32-bit cmpl** 低半 IEEE（`setle`），非 ucomisd；objdump 曾见 `cmp %eax,%ebx` |
| **根修** | G.7 补全权威：CALL\|METHOD_CALL 回落 `pipeline_asm_call_return_type_kind_ord_c`（已解析 kind 49）；f32 对称；**须 `pipeline_glue_standalone.o` + `pipeline_x.o` + g05** |
| **验收** | Ubuntu L2：**math_asm run=0**；objdump pi 比较 = `ucomisd`+`setbe`/`setae`；rv=42 · opt/si/hello · freestanding hello；**日常 L2 · 非 tip L4** |
| **重建** | `pipeline_glue_standalone.o` + **`pipeline_x.o` FORCE** + g05 `shux_asm` |

### residual 下一波

1. （旁）cfg-extern / ELF `.bss` / labi `len_empty`（挡产品面再开）  
2. tip L4 升钉仍须双端真冷+bstrict（钉盘仍 `5c8204ae`）  

```text
构建级别：L2（standalone glue + pipeline_x FORCE + g05/shux_asm；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：math_asm=0 · ucomisd · freestanding · rv/opt/si/hello
纪律：本波未升 tip L4
feat SHA：c28931a6
```
