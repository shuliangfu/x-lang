# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `41667e76`（feat Ubuntu 金标）/ docs 待推 |
| **T** | **18/18** typeck OK |
| **N** | **53** IDENTICAL（附注） |
| **R2** | **34** |
| **H** | 上述三十四切片 **H=0**；labi L0–L9 全 R2；余 diagnostic thin rest 等仍 rest C |

## 本波：Track R · `labi_gates` R2 full ✅

| 路径 | 变更 |
|------|------|
| `src/runtime/labi_gates.x` | 6 thin gates + count（**Track L 已真迁**；本波仅 R2 标注） |
| `seeds/labi_gates.from_x.c` | `#ifndef SHUX_LABI_GATES_FROM_X`；FROM_X 下仅前向 + `labi_gates_slice_marker` |
| `seeds/labi_gates_surface.from_x.c` | full surface（prove 锁） |
| mega `runtime_link_abi.from_x.c` | 既有 `#ifndef SHUX_LABI_GATES_FROM_X`（产品 rest 无 gate 体） |

- 验收：mac + Ubuntu `prove_module_selfhost labi_gates` → **IDENTICAL**；FROM_X seed 仅 `labi_gates_slice_marker`
- KPI：**R2 33→34**；N **53**（附注）；该切片 H **1→0**
- **labi L0–L9 十册 hybrid 全部 R2 full H=0**

## 下一波前排

1. **R2** diagnostic thin rest（`_impl`/va_list 仍在 full seed）  
2. **Track M** typeck M2 smoke — try replacing `typeck_x.o`  
3. 纪律：commit → push → Ubuntu → 更新进度文档；KPI **H↓ R2↑**