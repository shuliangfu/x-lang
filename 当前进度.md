# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin** · 🟢 **slice 形参 + call-site ABI 收口** |
| **parser residual** | ✅ body-lets · 🟢 hoist-safe while/for/expr_stmt/unsafe · 🟢 defer/region/with_arena · 🟢 parser M2 re-pin |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排** | force re-pin 阻塞：codegen 链上 slice call-site 裸 `slice` vs 形参 `struct shux_slice_* *` |
| **根因** | `codegen.x` 已有 `emit_call_arg_slice_abi`；**产品 seed** 仍走 `emit_expr` → force C host-cc 类型错 |
| **修复** | seed `codegen_gen.linux.x86_64.c` 补全权威 `codegen_emit_call_arg_slice_abi` + 12 处 call-arg 接入；与 `.x` 同语义（本地 `&(slice)` / 形参透传） |
| **mac L2** | g05 + 矩阵 **rv=42** · **opt=102** · **si=0** · **hello ✅**（`0a`）· force `-E` parser **host-cc error=0** · `amp_slice=79` |
| **未做** | tip L4；force 全量 re-pin 入产品 seed（仍 `main_idx`/矩阵链上验收）；Ubuntu L2 本叶（SHARED 待金标） |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | seed codegen call-arg 发射缺 slice ABI |
| 谁存储 | `seeds/codegen_gen.linux.x86_64.c` → `codegen_x.o` |
| 谁消费 | force `-E` parser / 任何 u8[] 形参 call-site host-cc |
| 同模式 | 形参指针已 @ `928eceed`；本叶补 call-site；`.x` 早已有权威 |
| 最小回归 | g05 + rv/opt/si/hello；force parser host-cc 无 slice 错；**禁 tip L4 / 禁 raw force pin** |

### residual 下一波

1. 用户明示时再双端 **tip L4 + bstrict 123**  
2. force 全量 re-pin 产品 parser：strip `main` + 链上矩阵（slice host-cc **已不阻塞**）  
3. region 正例 `-o` / typeck escape 诊断邻域  

```text
构建级别：日常 L2（codegen seed call-site ABI + g05；非 L4）
产品 tip L4：5a636984
mac 矩阵：rv=42 / opt=102 / si=0 / hello ✅
force parser host-cc：slice residual 关
```
