# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`0b279dbc`**（asm string/wa 半程） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm with_arena smoke** | ✅ **Ubuntu L2 build/run=0**（heap_user ensure） |
| **asm string/vec overload mangle** | 🔴 residual（仍 U bare `std_string_len` / `std_vec_*`） |
| **asm math run (f64 SysV)** | ✅ Ubuntu math_asm run=0 |

## 本波摘要（2026-07-17 · asm string/wa 半程）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 string·wa heap 链 |
| **已关（link）** | process_argv 扫 argv；heap→page_mmap 扫 argv；heap_user 直接编 seed（禁空 wrap 944B）；**with_arena smoke 绿** |
| **未关（codegen mangle）** | import binding `string.len` / `vec.new` 仍发裸 `std_string_len` / `std_vec_new`；formal 为 `len_String` / `new_retVec_u8`。已扩 type_ref 后缀 + scope 解析，Ubuntu 仍 bare → **类型源未进 emit 路径** 或需更深 typeck 信任 resolved |
| **邻域** | L2：rv=42；math_asm run=0；**日常 L2 · 非 tip L4** |
| **纪律** | hard timeout；**禁止** L2 半绿写 tip L4 |

### residual 下一波

1. **asm import overload mangle**（string.len / vec.new·push·len）— 钉 typeck resolved / scope 类型进 CALL 符号权威  
2. import 形参 type 映射 → 去掉 `std_math_*` 名门控  
3. 工程轨 rest C 具体叶  

```text
构建级别：L2（call_dispatch + link_abi + g05；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：wa_smoke build=0 run=0；string_asm residual bare len；wav residual bare vec；rv=42；math_asm=0
纪律：测试 hard timeout；本波未升 tip L4
```
