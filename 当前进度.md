# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `g-02f-self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **9** IDENTICAL（7 driver 叶 + `lsp_io_std_heap` + `token`） |
| **diagnostic** | `-E`+cc ✅；**CORE_OK**（非 IDENTICAL / 非退役） |
| **W-unsafe-expr（std 源）** | ✅ 批量 let+stmt 规避 |

## 本波：Track L 扩 prove `token` ✅

**事实**：`token.x` 与 seed 已定义符号仅 `token_is_eof`，nm **IDENTICAL**。

**落地**：

| 路径 | 变更 |
|------|------|
| `tests/prove_module_selfhost.sh` | 增 `token\|src/lexer/token.x\|token_gen.c` |
| `seeds/token_gen.linux.x86_64.c` | 真 `-E` 对齐（非 stub `return 0`） |
| 产品链 | **不变**：仍用 `include/token.h`（C 头）；不增 `token_x.o` leaf |

- 验收：`bash tests/prove_module_selfhost.sh` → **9 IDENTICAL** + diagnostic **CORE_OK**
- KPI **N↑**；**S** 不因本波收缩（token 本就不在构建 pin 面）

## Ubuntu 金标准（待本 commit 后验证）

| 项 | 期望 |
|----|------|
| prove | **9 IDENTICAL** + diagnostic **CORE_OK** |
| T 地图 | **18/18** typeck OK |

**注意**：`typeck_gen.c` 被 gitignore；Ubuntu 须跑 `patch_typeck_gen_lang007` 后 `make typeck_x.o` + `relink-shux`（本波未触 typeck）。

## 下一步

1. diagnostic hybrid 收敛（thin/rest 单一权威）或接受 CORE 长期门禁  
2. codegen 根修表达式 `unsafe`（可选；std 源侧已规避）  
3. 每波：**commit → push → Ubuntu prove/T → 再下一步**；§8 时间用 **东八区墙钟**
