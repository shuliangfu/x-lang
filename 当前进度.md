# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `0a7a0707`（feat）/ docs 待合 |
| **T** | **18/18** typeck OK |
| **N** | **53** IDENTICAL（附注） |
| **R2** | **25**（+`labi_path_io`；Ubuntu 金标） |
| **H** | 上述二十五切片 **H=0**；其余 runtime hybrid 多仍 rest C（labi_host_lit / diagnostic thin…） |

## 本波：Track R · `labi_path_io` R2 full 真迁 ✅

| 路径 | 变更 |
|------|------|
| `src/runtime/labi_path_io.x` | 吃满 3 公共门闩 + count（null 门闩 + skip_missing；转发 Cap `_impl`） |
| `seeds/labi_path_io.from_x.c` | 业务符号 `#ifndef SHUX_LABI_PATH_IO_FROM_X`；FROM_X 下 rest 仅前向 + marker → **业务 H=0** |
| Cap residual mega rest | `shux_path_is_nonempty_regular_file_impl`（stat）+ `shux_runtime_o_realpath_if_exists_impl`（realpath） |
| `seeds/labi_path_io_surface.from_x.c` | full surface（prove 锁） |
| `tests/prove_module_selfhost.sh` | 标注 `labi_path_io` R2 full → surface |

- 验收：mac + Ubuntu `prove_module_selfhost labi_path_io` → **IDENTICAL**；FROM_X rest 仅 `labi_path_io_slice_marker`
- KPI：**R2 24→25**；N **53**（附注，此前已在 N）；该切片 H **1→0**
- 产品 hybrid：full .x + mega rest 无已迁 public thin（`#ifndef` 既有）

## 下一波前排

1. **R2** runtime 其余 hybrid rest（labi_host_lit / diagnostic thin rest…）· Cap-fn-ptr 批量  
2. **Track M** typeck M2 试替换 `typeck_x.o` smoke  
3. 纪律：commit → push → Ubuntu → docs；KPI **H↓ R2↑**
