# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`f3b7fe97`**（METHOD_CALL import mangle） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm with_arena smoke** | ✅ **Ubuntu L2 build/run=0** |
| **asm string import mangle** | ✅ **link 符号 `len_StrView` / `is_empty_StrView`**（原 bare residual 关） |
| **asm string smoke run** | 🔴 residual run=1（符号已解析；值/ABI 另债） |
| **asm math run (f64 SysV)** | ✅ Ubuntu math_asm run=0 |

## 本波摘要（2026-07-17 · METHOD_CALL import mangle）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 asm import overload mangle |
| **根因** | `string.len(v)` 为 **EXPR_METHOD_CALL (ko=49)**，非 CALL+FIELD_ACCESS；此前 mangle 只修 CALL 路径 → 永不执行 |
| **已关** | G.7 权威 `glue_asm_mangle_import_binding_call_sym_c` 共用 CALL + METHOD_CALL；Ubuntu L2：U bare → **`std_string_len_StrView` / `is_empty_StrView`**；`-o` 全链 build=0 |
| **未关** | string smoke **run=1**（C 同源 run=0）→ struct 返回/传参 ABI 另债；vec.* 未本波探针 |
| **邻域** | rv=42；wa_smoke run=0；**日常 L2 · 非 tip L4** |
| **纪律** | hard timeout；**禁止** L2 半绿写 tip L4 |

### residual 下一波

1. **asm string/struct SysV 值路径**（view/len 链接已绿、run=1）  
2. **vec.* METHOD_CALL mangle 探针**（共用权威应已覆盖；需 Ubuntu 验收）  
3. import 形参 type 映射 → 去掉 `std_math_*` 名门控  

```text
构建级别：L2（call_dispatch + g05；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：string_asm nm=len_StrView build=0 run=1；rv=42；wa=0
纪律：测试 hard timeout；本波未升 tip L4
```
