# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 + **expected-return overload**（本叶） |
| **M codegen** | ✅ Cap+M1+**M2 re-pin** · 🟢 slice 形参 + call-site ABI |
| **parser residual** | ✅ body-lets · hoist · **force 全量 re-pin** |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排** | 修 `with_arena_vec` typeck：`Vec_u8` vs `vec.Vec_i32`（发现即修，非「本波回归才修」） |
| **根因** | ① 零参 `vec.new()` 为 **METHOD_CALL**；overload 只按 arity 取首个 `Vec_i32`；② LANG007 re-dispatch 清掉 expected 后再 pick；③ `i32→usize` 实参弱分缺失导致 `heap.alloc(al, cap)` 淘汰 2 参 `*u8` |
| **权威** | `typeck_overload_expected_ret_slot`（ast_pool）· pick 加 return 分 · `pipeline_typeck_check_expr_call_c` / method_call 安装 expected · LANG007 method re-dispatch 同装 · strict_minimal 整数拓宽 + last-segment |
| **验收** | `shux_asm -E tests/mem/with_arena_vec_push.x` → **typeck OK** · mangle `std_vec_new_retVec_u8`；矩阵 rv/opt/si/hello/atomic 绿 |
| **残余** | 产品 `-o` 链 **std.vec.o** 仍缺（link_only 预编轨；模块内 `new` mangle `std_vec_new` 未带 ret 后缀 → 编 vec.o 红）— **typeck 叶已关，link/codegen 模块 .o 另债** |
| **Ubuntu L2** | g05 + **typeck OK** `std_vec_new_retVec_u8` · 矩阵 rv/opt/si/hello |

### 五问

| 问 | 答 |
|----|-----|
| 谁产生 | typeck overload pick（零参/return 上下文 + re-dispatch） |
| 谁存储 | `g_typeck_overload_expected_ret` · resolved_func_index |
| 谁消费 | let/assign 类型比对 · codegen mangle |
| 同模式 | heap.alloc 多返回；零参 `new` 族；LANG007 re-dispatch |
| 最小回归 | with_arena_vec `-E` typeck · rv/opt/si/hello · atomic |

### residual 下一波

1. **std.vec.o** 产品 link_only：模块内 overload mangle / 编 `std/vec/vec.o`  
2. 用户明示时双端 tip L4 + bstrict  
3. region 正例 `-o` 其它 escape 邻域  

```text
构建级别：日常 L2（typeck_x + pipeline filtered + strict_minimal + g05；非 L4）
产品 tip L4：5a636984
with_arena_vec typeck：OK（std_vec_new_retVec_u8）
with_arena_vec -o：UNDEF std.vec symbols（另债）
矩阵：rv=42 / opt / si / hello / atomic 绿
```
