# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` tip **`23447401`**（产品 L4 放行仍钉 **`96f1453c`**） |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `96f1453c`**（16 号 driver_abi Cap pure wave1–6 收口；详见归档） |
| **D Stage2** | ✅ Ubuntu freestanding D-03 @ `c3031883`（**≠** 产品链；勿把 `shux_asm2` 当产品） |
| **E R2** | ✅ diagnostic rest T **0**；driver_abi Cap **OS rest `_impl` 0**（wave12 pthread @ `23447401` 收口 OS pure 叶）；fmt_check Cap residual pure 收口 ALWAYS residual **0** @ `4a5d7626` |
| **T / EMPTY / N** | **18/18** · **18/18** · **54/54** |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **driver_abi Cap residual pure wave12** `23447401` | **pthread pure orch**：public → 永久 OS 面 `shux_driver_run_thread_on_large_stack_pthread`（无 pthread_attr/posix_memalign 布局）；FROM_X 无 pure-dup `driver_run_thread_on_large_stack_pthread_impl`；**OS rest `_impl` 1→0**（OS Cap residual pure **收口**）；surface IDENTICAL；**.x 英文 G.9**；**日常真测 L2/L3**（**非 tip L4**） |

**构建级别（本波产品改动）**：**日常真测 L2/L3**（产品放行 tip 仍为 L4@`96f1453c`）。

| 端 | prove driver_abi | g05 hybrid | 矩阵 | 构建级别 | 日志 |
|----|------------------|------------|------|----------|------|
| macOS | IDENTICAL | hybrid OK · nm 无 `pthread_*_impl` · 有 `shux_driver_run_thread_on_large_stack_pthread` · OS residual 0 | rv=42 / opt=0 / hello=0 | L2/L3 | prove `/tmp/prove_dabi_wave12.log` · g05 `/tmp/g05_dabi_wave12.log` · matrix `/tmp/mac_matrix_dabi_wave12_summary.log` |
| Ubuntu | IDENTICAL | hybrid OK · nm 无 pure-dup pthread · 有 pthread surface · OS residual 0 | rv=42 / opt=0 / hello=0 | L2/L3 | prove `/tmp/ubuntu_prove_dabi_wave12.log` · g05 `/tmp/ubuntu_g05_dabi_wave12.log` · matrix `/tmp/ubuntu_matrix_dabi_w12.log` |

## 产品放行 tip（沿用 · 勿重写为 17 日 L4）

| 端 | bstrict | 日志（@ `96f1453c`） |
|----|---------|----------------------|
| **Ubuntu** | **123/123** | cold `/tmp/ubuntu_true_cold_96f1453c.log` · bstrict `/tmp/ubuntu_true_bstrict_96f1453c.log` |
| **macOS** | **123/123** | cold `/tmp/mac_true_cold_96f1453c.log` · bstrict `/tmp/mac_true_bstrict_96f1453c.log` |

```text
构建级别：L4 真冷（双端；16 号结案）
SHA：96f1453c
SHUX：产品 g05 shux_asm（禁 stage1 / 禁 freestanding asm2）
```

## 下一波前排

1. **E 真迁续**：Stage2 WPO · 或 debug_pipe Cap residual · 或 driver_abi OS pure 整波 tip 升 L4（wave7–12 OS rest `_impl` 6→0 已 pure）  
2. **禁止**把单叶 IDENTICAL / Stage2 / 抽检写成完全自举（产品放行 = tip 双端 L4@**`96f1453c`** 直至新 tip L4）  
3. 动 SHARED 产品面 → 新 SHA 再双端 L4（勿沿用旧 tip 日志）  
