# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `215c1d36`（feat）/ docs 待合 |
| **T** | **18/18** typeck OK |
| **N** | **53** IDENTICAL（附注） |
| **R2** | **22**（+`rt_asm_stub`；Ubuntu 金标） |
| **H** | 上述二十二切片 **H=0**；其余 runtime hybrid 多仍 rest C（`rt_dispatch` thin rest 等） |

## 本波：Track R · `rt_asm_stub` R2 full 真迁 ✅

| 路径 | 变更 |
|------|------|
| `src/runtime/rt_asm_stub.x` | 吃满 `driver_asm_output_want_exe` + `asm_codegen_ast`（GAS main→42 循环） |
| `seeds/rt_asm_stub.from_x.c` | 业务符号 `#ifndef SHUX_RT_ASM_STUB_FROM_X`；FROM_X 下 rest 仅前向 + marker → **业务 H=0** |
| Cap residual `driver_abi` | GAS 行表 `line_at/count` + `out_append_cstr`（9MiB OutBuf 布局） |
| `seeds/rt_asm_stub_surface.from_x.c` | full surface（prove 锁） |
| `tests/prove_module_selfhost.sh` | 扩 `rt_asm_stub` R2 full |

- 验收：mac + Ubuntu `prove_module_selfhost` → **53 IDENTICAL**（含 `rt_asm_stub`）
- KPI：**R2 21→22**；N **52→53**（附注）；该切片 H **1→0**
- 产品 hybrid：full .x + rest 仅 marker

## 下一波前排

1. **R2** `rt_dispatch` thin rest（emit/full/sibling）· Cap-fn-ptr 批量  
2. **Track M** typeck M2 试替换 `typeck_x.o` smoke  
3. 纪律：commit → push → Ubuntu → docs；KPI **H↓ R2↑**
