# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin** · 🟢 slice 形参指针 ABI 半收口 |
| **parser residual** | ✅ 匿名 `{ let }` · 🟢 **M1…parse_into*** · 🟢 hoist-safe parse_into* / ret_type_ref · 🟢 **body let 成功路径 + name hoist-safe**（force **let_only/mixed/return-var/binary-expr 绿**）· 🟡 **M2 re-pin**（force **option host-cc 仍红**） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排** | Cap force **body let / return-var** 仍红：扩绿根修 |
| **诊断 1** | while 体 `name_len/name_start/zi` 被 hoist 到 `lexer_next` 前 → LET 的 ident_len=0 / zi=0 清名 |
| **诊断 2** | `;` 后 `let lex_after_semi = lex_at_token_from_result(after_semi)` 被 hoist 到 next 前 |
| **诊断 3（主杀）** | `parse_body_lets_into` while `break` 后落到 `return false`（原标 unreachable 的 typeck 占位）→ **body lets 解析成功仍返回 false**；`buf_into` glue 能救 `return N` 救不了 `return x` |
| **根修（parser.x）** | ① name_len/name_start/zi/name_row hoist-safe；② 分号后 inline `lex_at_token_from_result`；③ **成功写 lex_out + return true 放进 unsafe while 后**，`return false` 仅 unsafe 外 typeck |
| **M2 force 结果** | 诚实 g05：**rv=42**；**binary-expr 13/13**；**let_only=42 / mixed / const_expr / return-var=40**；**option -E OK 但 host-cc BLD001**（空参名/`(void)()`）→ **仍不 pin** |
| **产品** | **仍 seed `parser_x.o`**；仅合入 `parser.x`；mac seed **rv=42 / be=3 / let_only=42 / option 绿** |
| **未做** | force option/形参名残红 → M2 re-pin；tip L4；Ubuntu SHARED |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | pin X→C：while 后 `return false` 可达；副作用 let hoist |
| 谁存储 | force `parse_body_lets_into` 控制流 / 绑定名捕获 |
| 谁消费 | `parse_one_function_impl` → 失败则 `buf_into` glue 兜底 |
| 同模式 | 其它 unsafe 块 while+break 后若仍有 `return false` 占位须审计 |
| 最小回归 | force：rv+be 13 + let_only/mixed；seed：rv/be/let/option；**禁 pin** 至 option 等够绿 |

### residual 下一波

1. Cap force **option host-cc**（空形参名 / 坏 `(void)()`）扩绿 → 再 M2 re-pin  
2. 审计同类 unsafe `return false` 占位  
3. SHARED Ubuntu；升 tip 须明示  

```text
构建级别：日常 L2（seed parser + parser.x body-lets success-path；非 L4）
产品 tip L4：5a636984
parser M2：force body let + return-var + be ✅；option host-cc 仍红 → 未 re-pin
产品矩阵：mac seed rv/be/let/option ✅
```
