# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `70666dcb`（全表 N re-baseline） |
| 产品 | 双端 bstrict **123** @ `233e3b72`（L4 历史；本波工程未重跑 L4） |
| **T** | **10/18**（mega；未重跑） |
| **N** | **35/54 IDENTICAL**（Ubuntu 金标准 @ `70666dcb`）；mac **36/54**（仅 `lsp_io_std_heap` 金标 FAIL） |
| **R2 / H** | 仍冻结（N 未满） |

## 本波摘要

| 波 | SHA | 结果 |
|----|-----|------|
| Cap-T001 + Track L residual | `511302e9`…`9200c379` | 源 bare 清 + enc/diagnostic/rt_fmt SYM 收 |
| **全表 prove re-baseline** | `70666dcb` | Ubuntu **N=35/54**（+5）；mac 36/54 |

**+5 相对 `32bf8ad8` N=30**：`compile` · `backend_arch_emit_dispatch` · `backend_enc_dispatch` · `diagnostic` · `rt_fmt_one`

**剩余 19（Ubuntu）**：
- FAIL **3**：`backend_try_inline_dispatch` / `backend_call_dispatch`（`-E` 非法 C）· `lsp_io_std_heap`（seed cc）
- SYM **16**：主要为模块前缀 mangle vs surface 短名（Track L 同型；如 `rt_fs_open` path helper）

日志：`/tmp/ubuntu_prove_70666dcb.log` · `/tmp/mac_prove_70666dcb.log`

## 下一波前排

1. **Track L 续吃 SYM 16**（优先 path/helper 缺 `#[no_mangle]` 同型；一条债一 commit）  
2. **T / Track M** 说明向：8 mega 红归层  
3. 禁未稳 N 谈 Stage2；动 SHARED 才 L4  
