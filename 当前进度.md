# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin** |
| **parser residual** | ✅ 匿名 `{ let }` · 🟢 **M1 -E** · 🟢 **类型序** · 🟢 **init_globals** · 🟢 **by-value return 成 int 关** · 🟢 **dual-extern / incomplete ast_* 关** |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排** | parser M1 host-cc **dual-extern / incomplete `ast_*`** |
| **诊断** | dep 池 di=2 装的是 **ast 完整 layout**，但 `path=lexer`（与 di=0 真 lexer 同 path）；path de-dupe first-non-empty 压制 di=2 → 无 `struct ast_*` 完整体；签名用 `ast_*`、forward 用 `lexer_*` → dual incomplete / conflicting types |
| **根修** | **load/sync 权威**（`pipeline_glue.c` + `ast_pool.c`，产品 `pipeline_x.o` 含 glue）：`load_import_from_disk` **在 parse 前 pin path**；取消 `bound_any` 早退漏 load/漏 path；每 import 从 entry 重钉 path + 未 seed 则 disk load；`sync` 仍对齐 seed 槽 |
| **验收** | parser.x `-E` 见 **`struct ast_Module`/`Expr`/`Type`/… 完整体**；host-cc **dual-extern 消**（可见 warning=0）；剩 **6× array-init**（`OneFuncResult.name = z64` 等）；mac 矩阵 **rv=42 / opt / si / hello 绿** |
| **host-cc 现前排** | 🟡 **array-init residual**（6）；`parse_into*` 仍 skip |
| **矩阵 mac L2** | **rv=42 / opt / si / hello ✅** |
| **未做** | tip L4；array-init 6 处；parse_into 真 parse；Ubuntu SHARED 复证 |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | load 未写 dep path；`bound_any` 早退可跳过 path 钉死 |
| 谁存储 | dep 槽 path 与 module 内容错位（ast 内容 + lexer path） |
| 谁消费 | codegen path de-dupe + 前缀 / host gcc |
| 同模式 | 任意 multi-import 入口 path 未钉死 |
| 最小回归 | `-E` 见 `struct ast_* {`；无 dual incomplete；矩阵绿 |

### residual 下一波

1. **array-init residual（6）**：`.name = z64` 对 `uint8_t[64]` 字段非法  
2. mega `parse_into*` 真 parse  
3. SHARED Ubuntu；升 tip 须明示  

```text
构建级别：日常真测 L2（pipeline_x 含 glue path 钉死 + g05；非 L4）
产品 tip L4：5a636984
parser M1：-E 绿；dual-extern 关；array-init residual 6
产品矩阵：mac rv/opt/si/hello ✅
```
