# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`a3b3b803`**（asm f64 SysV 半程；前 `a1f67402` math-link） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm hello / si / math-link** | ✅ `cb4605ee` / `67b61ea8` / `a1f67402` |
| **asm math run (f64 SysV)** | 🔴 **Ubuntu math_asm run=4**（ceil residual；半程见下） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · asm f64 SysV 半程）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排 ① f64 SysV（math_asm run residual） |
| **已合入** | G.7 扩 f32 xmm 轨：f64 movq 编解码、addsd/subsd、cmpq（RHS FLOAT_LIT）、dep kind、std_math_* harvest 尝试 |
| **Ubuntu 产物证据** | main 内 **`subsd` + `cmpq`** 已出现；`rv=42` / `si` / `hello` 绿（L2 hang-safe） |
| **仍红** | math_asm **run=4**（ceil）：CALL 后 **xmm0→rax harvest 未进 main**（`call std_math_*; push %rax`）；floor/ceil 实参仍 **`mov %rax,%rdi`**（应得 xmm0） |
| **地图** | harvest 代码在 `emit_call_elf_c_impl` / `with_cleanup` 已链入 shux_asm，但用户 main 的 pi/e/floor 路径未见后随 `66 48 0f 7e c0`；需继续追 **哪条 CALL 发射路径** 绕过 with_cleanup |
| **纪律** | hard timeout；**日常 L2 · 非 tip L4** |

### residual 下一波

1. **钉死 math.pi CALL 的真实 emit 路径** → 权威点补 xmm0 harvest + f64 实参进 xmm（禁第二套）  
2. asm string/wa heap 链  
3. 工程轨 rest C 具体叶  

```text
构建级别：L2（call_dispatch + pipeline_x + g05；非 L4）
产品 tip L4 钉盘：5c8204ae
tip HEAD：a3b3b803（f64 半程）
Ubuntu：math_asm build=0 run=4；rv/si/hello 绿；math_c run=0
纪律：测试 hard timeout
```
