# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `0240c6b1`（Stage2 Ubuntu 金标绿；L4 双端 bstrict 基线 `5a81af60`） |
| 产品 | **L4 真冷** + 双端 bstrict **123/123** @ `5a81af60`；**Stage2 后恢复 g05 产品链**（勿把 freestanding 当产品） |
| **D Stage2** | ✅ Ubuntu @ `0240c6b1`：gen1==gen2 SHA256 + rv=42 + struct_mk |
| **T** | **18/18** typeck |
| **EMPTY** | **18/18**（preprocess EMPTY 改 surface；产品 pin 恢复） |
| **N** | **54/54 IDENTICAL**（全表 @ `11f3ad55`） |
| **R2 / H** | 可解冻抽检 |
| **Track L SYM** | **0** |
| **FAIL residual** | **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **Stage2（Ubuntu 金标）** | `bootstrap-verify-stage2-bstrict` **OK** @ `0240c6b1`；gen1/gen2 **rv=42**；struct_mk **exit 10**；**D-03** SHA256 **identical** `0b5b6a2d…4a0bf3`（freestanding **5324744** B） |
| **根因（gen2 SIGSEGV）** | freestanding 路径 `fflush` 撞 skip stub `FILE*` → `b5470cde` helper + skip stub；`0240c6b1` **weak** `bootstrap_flush_stdio_and_exit`（hosted g05 走 libc flush，不破坏产品） |
| **Stage2 WPO** | Step2 默认 `SKIP_WPO`（parity 与 WPO dogfood 正交）；2c–2h Linux WPO **WARN 续跑**（非 hard-fail） |
| **残差（非阻塞）** | postlink `-c` smoke SIGSEGV（warn）；WPO strict link 0 symbols after subtract |
| **L4 真冷 + 双端 bstrict** | remaining_o=0 → g05 → **123/123** @ `5a81af60`（本 SHA 前波已绿，仍是产品放行面） |

**构建级别**：Stage2 = freestanding gen1/gen2 行为+hash 闸；产品 = L4 + g05 dynamic（Stage2 脚本结束恢复 seed+parser_x / g05，**禁止**留 freestanding 当产品）。  
日志：  
- Stage2 Ubuntu `/tmp/ubuntu_stage2_0240c6b1_final.log`（`bootstrap-verify-stage2-bstrict OK`）  
- L4 mac cold `/tmp/mac_true_cold_6da9f4cd.log` · bstrict `/tmp/mac_true_bstrict_c82ee9f7.log`  
- L4 Ubuntu cold `/tmp/ubuntu_true_cold_6da9f4cd.log` · bstrict `/tmp/ubuntu_true_bstrict_5a81af60.log`

## 下一波前排

1. **可选工程债**：Stage2 脚本把 `SKIP_WPO_DOGFOOD` 默认真正覆盖 2c–2h（缩短金标墙钟）；postlink smoke residual  
2. **可选根修**：`Token { kind: Enum.Variant }` entry-emission；codegen `-E` 与 `.x` 主循环序  
3. **禁止**把 Stage2 ✅ 写成「完全自举 / 产品放行」——产品放行仍 = 本 SHA **L4 + 双端 bstrict**；完全自举另见 D+E+F  
