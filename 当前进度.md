# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `0e4c8949`（M2 EMPTY **14/18**） |
| 产品 | 双端 bstrict **123** @ `233e3b72`（L4 历史；本波未重跑 L4） |
| **T** | **18/18** typeck |
| **EMPTY** | **14/18**（+ lsp* plain -E surface） |
| **N** | **54/54 IDENTICAL**（全表 @ `11f3ad55`） |
| **R2 / H** | 可解冻抽检 |
| **Track L SYM** | **0** |
| **FAIL residual** | **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **M2 EMPTY lsp\*** | harness 对齐：`lsp{,_diag,_io}.x` ↔ `seeds/*_empty_surface.from_x.c`（= Ubuntu 金标 plain `-E`）；产品 `seeds/lsp*_gen.linux.x86_64.c` 不动 |
| **根因** | 产品 seed = rename + 全类型 / C-04 标记；plain `-E` 永不对齐产品 pin（同 diagnostic thin 双权威） |
| **平台** | Ubuntu pin 含 `page_mmap` freestanding；harness 双端 strip 该 cfg 依赖行 → 双端 EMPTY |

**构建级别**：日常真测 L2/L3（**非** L4）。  
日志：mac `/tmp/mac_diff_empty_lsp_surface3.log` · Ubuntu `/tmp/ubuntu_diff_t_0e4c8949.log`

## 下一波前排

1. **EMPTY 继续**：lexer CODEGEN / mega residual（parser / typeck / codegen）  
2. **L4 + 双端 bstrict**（谈 Stage2 前强制）  
3. **禁止**用 N/T/EMPTY 单独宣称「可自举」  
