# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **22** IDENTICAL（+ `diagnostic` thin） |
| **diagnostic** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 79） |
| **W-string-nul** | ✅ STRING_LIT *u8 尾 `, 0`；slen≤64 |

## 本波：Track L `diagnostic` hybrid→IDENTICAL（L2 thin）✅

**根因地图**：旧 prove 比 **full** `runtime_driver_diagnostic.x` vs **full** seed → CORE（seed+16 `_impl`/C 尾；x+3 `append_*`）。产品 hybrid 实际走 **thin.x** + full seed rest（`-DSHUX_L2_RDD_THIN_FROM_X`），prove 面与产品面不一致。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/runtime_driver_diagnostic_thin.x` | 权威 thin 公共面（产品本已用） |
| `seeds/runtime_driver_diagnostic_thin.from_x.c` | 冷/prove 同构 thin（`-E` 抛光 + POSIX prologue） |
| `tests/prove_module_selfhost.sh` | diagnostic → thin.x + thin seed；空 mode → **IDENTICAL** |
| full seed rest | 不变（va_list / snprintf / `_impl` 仍 C 尾） |

- 验收：`bash tests/prove_module_selfhost.sh diagnostic` → **IDENTICAL**
- 全量：`bash tests/prove_module_selfhost.sh` → **22 IDENTICAL**
- 产品：g05 仍 `thin.x + seed-rest ld -r`（本波不改链接拓扑）

## Ubuntu 金标准

（本波 mac 绿后 commit → push → `ssh ubuntu-server` prove diagnostic；金标行见 `analysis/自举进度.md` §8）

## 前排下一步

1. 下一可 IDENTICAL 叶 / diagnostic 消息体 true-migrate（从 rest 推进）
2. W-global 数组 / 串池 >64
3. 纪律：commit → push → Ubuntu → docs
