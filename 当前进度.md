# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`b613f58e`**（NL-07 L3 crt0 backend enc companions） |
| 产品 L4 放行 | 仍钉 **`5a636984`** 双端 L4+123（**未**升 tip 全擦 L4） |
| tip 双端 bstrict | ✅ **123/123 @ `36ac3d14`**（g05 + SKIP_BUILD；**非 L4**；本波未重跑全量） |
| **std.vec 产品 -o** | ✅ **双端** formal `vec.o` + on-demand · `with_arena_vec_push` |
| **NL-07 硬绿 L1** | ✅ **multi-def=0** |
| **NL-07 硬绿 L2** | ✅ **`fflush` 有定义** |
| **NL-07 硬绿 L3** | ✅ **`backend_enc_*` / arch_emit / try_inline / simd =0** |
| **NL-07 crt0 全包** | 🔴 unique UNDEF≈**156**（head=`backend_emit_*` → typeck/driver；**≠** 零 libc 硬绿） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · NL-07 L3 backend enc companions）

| 波 | 结果 |
|----|------|
| **入口** | 前排硬绿分层第 3 层：crt0 入链 backend enc/dispatch companion（**非** tip L4） |
| **根因** | pipeline/backend `build_asm/*.o` **引用** `backend_enc_*_arch` / `backend_arch_emit_*` / try_inline / simd；产品 experimental/strict 已用 `BSTRICT_DISPATCH_OBJS`，**crt0 历史只链 NONEMPTY_ASM bag + freestanding/stubs** → dispatch 在 `src/asm/` 从未入 crt0 链 |
| **根修** | **`b613f58e`**：`ensure_crt0_backend_companion_objs` 复用 **`ensure_bstrict_seed_support_objs` + `BSTRICT_DISPATCH_OBJS` + simd_***（G.7 单权威；**禁止** 第二套 enc 桩表）；Linux crt0 链行追加 companions |
| **验收** | Ubuntu TRY_BUILD：`multi=0`；`backend_enc_*=0`；`backend_arch_*=0`；`simd=0`；unique UNDEF **283→156**；residual head=`backend_emit_*`(10) → typeck/driver |
| **门禁** | v4 gate **OK**（final-link loose）；**仍无** `bootstrap nostdlib crt0 link OK`（crt0 bag 仍 UNDEF 红） |
| **产品** | Ubuntu `function main(): i32 { return 42; }` → **rv=42**；**禁止** 当 crt0 零 UNDEF / tip L4 升钉 |
| **日志** | `/tmp/ubuntu_n07_l3_b613f58e.log` · err `compiler/build_asm/.bootstrap_nostdlib_link_err` |

### 五问（NL-07 L3 backend enc）

| 问 | 答 |
|----|-----|
| 谁产生 | backend/pipeline 对象引用 `backend_enc_*`；实现已在 `backend_enc_dispatch.o` 等 |
| 谁存储 | `src/asm/backend_enc_dispatch.o` 等（`ensure_bstrict_seed_support_objs`） |
| 谁消费 | Linux crt0 `ld` 行（nostdlib 与 libc fallback 同） |
| 同模式 | experimental/strict 已链同一 `BSTRICT_DISPATCH_OBJS` — **禁止** 另开 enc stub TU |
| 最小回归 | err **无 backend_enc_**；multi=0；unique↓；仍允许 L3+ `backend_emit`/typeck 红 |

### residual 下一波

1. 用户明示时 tip **L4 真冷** + 双端 bstrict 123（升放行钉盘）  
2. NL-07 **L3b**：`backend_emit_*`（seed heavy partial 入链；**非** 新 weak 桩表）  
3. NL-07 L4+：typeck / driver / lsp companion  
4. 其它产品面 / Cap  

```text
构建级别：日常 L2（NL-07 L3 backend companions；非 tip L4 全擦）
产品 tip L4 钉盘：5a636984
tip HEAD：b613f58e
NL-07 L1+L2+L3：multi=0 · fflush 关 · backend_enc=0 · crt0 仍红（UNDEF≈156 · head backend_emit）· gate final-link 绿
产品矩阵抽检：Ubuntu rv=42
```
