# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` tip **`3d2f7d94`** |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `3d2f7d94`**（Ubuntu 金标 + mac Darwin/arm64；真冷全擦 + 产品 `shux_asm`；含 wave6 rest T=0） |
| **D Stage2** | ✅ Ubuntu freestanding D-03 @ `c3031883`（**≠** 产品链；勿把 `shux_asm2` 当产品） |
| **E R2** | ✅ 解冻抽检 13/13 @ `c3031883`；diagnostic Cap residual pure rest T **28→…→0**（wave6） |
| **T / EMPTY / N** | **18/18** · **18/18** · **54/54** |
| **Track L SYM / FAIL** | **0** / **0** |

## 验证审计（用户追问：以前的步 Ubuntu 都真测了吗？）

**诚实结论：不是每一步都做了 Ubuntu 真测；更不是 tip 一直 L4。**

| 波段 | Ubuntu 证据 | 构建级别 | 备注 |
|------|-------------|----------|------|
| Cap-T001 / Track L / EMPTY 递进 | 多数有 `/tmp/ubuntu_*` 日志 + dual progress commit | **日常真测 L2/L3** | 双端 prove/diff 有落盘 |
| 全表 prove N=54 | `/tmp/ubuntu_prove_full_11f3ad55.log` | L2/L3 | 非 L4 |
| L4+bstrict 123 | `/tmp/ubuntu_true_cold_6da9f4cd.log` + `/tmp/ubuntu_true_bstrict_5a81af60.log` | **L4 真冷** | 产品放行面曾钉此 |
| Stage2 D-03 / R2 抽检 | `ubuntu_stage2_c3031883` / `ubuntu_r2_unfreeze_c3031883` | 专项 | Stage2 产物 **≠** 产品 g05 |
| **diagnostic Cap residual pure wave1–3** | **提交当时无 Ubuntu 日志** | 仅 mac L2/L3 | **缺口** |
| wave4 初版 | 曾只报 mac；补跑 L2 双端 | L2 hybrid | 树内仍有旧 `.o`，**不足以**当 tip 产品结案 |
| **tip 补救 L4（今日）** | remaining_o=0 → bootstrap+g05 → 矩阵 → bstrict | **L4** | 见下 |

### 14 号类假路径（今日 L4 实锤）

1. **旧产物**：L2 hybrid 带着 141 个旧 `.o` 矩阵绿 ≠ 全擦真冷。  
2. **错误二进制**：`run-all-bstrict` 在存在 `compiler/shux_asm2` 时 **优先 cp Stage2 freestanding（static ~5.3MB）→ shux**，盖掉产品 `shux_asm`（dyn ~3.3MB）→ ensure_std **SEGV**（`codegen_bytes=zu`）。  
3. **根修**：bstrict 默认只 `cp shux_asm`；`SHUX_BSTRICT_USE_ASM2=1` 才用 gen2。Stage2 后须隔离/删 `shux_asm2`/`stage1`/`experimental` 残留。  
4. **注释闸门**：wave2 英文 docblock `* without:` 触发 `scan_fmt_damage` star_decl → `4866f65d` 已改。

### tip 双端 L4 结果（`3d2f7d94` · wave6 residual 收口）

| 端 | remaining_o | 矩阵 | prove diagnostic | bstrict | 日志 |
|----|-------------|------|------------------|---------|------|
| **Ubuntu** Linux/x86_64 | 0 起编 | rv=42 / opt / hello / si | IDENTICAL | **123/123** | cold `/tmp/ubuntu_true_cold_3d2f7d94.log` · bstrict `/tmp/ubuntu_true_bstrict_3d2f7d94.log` · prove `/tmp/ubuntu_l4_prove_diag_3d2f7d94.log` |
| **macOS** Darwin/arm64 | 0 起编 | rv=42 / opt / hello / si | IDENTICAL | **123/123** | cold `/tmp/mac_true_cold_3d2f7d94.log` · bstrict `/tmp/mac_true_bstrict_3d2f7d94.log` · prove `/tmp/mac_l4_prove_diag_3d2f7d94.log` · restT `/tmp/mac_l4_rdd_rest_T_3d2f7d94.txt` |

```text
构建级别：L4 真冷（双端全擦 .o + 重链 shux/shux_asm/shux-c）
SHA：3d2f7d94（wave6 rest T=0 后整波双端 L4 钉盘；含 bstrict 禁 Stage2 asm2）
SHUX：本波 g05 产品 shux_asm（非 stage1 / 非 freestanding asm2）
平台：macOS=✅ / Ubuntu=✅
```

历史 tip L4@`02c5d234` 仍有效为前序放行面；**现放行 tip = `3d2f7d94`**。

## 本波摘要

| 波 | 结果 |
|----|------|
| **wave6 residual → tip 双端 L4** | rest T=0 闭环：双端 L4 真冷 + 矩阵 + prove diagnostic IDENTICAL + **bstrict 123/123** @ `3d2f7d94` |
| **diagnostic Cap residual pure wave6** | **rest T 4→0**：G.7 `lsp_diag_get_enabled`→`runtime_lsp_glue`/`stubs`；FROM_X 剔死 va_list report_x；thin pure `slice_marker`；**.x 英文 G.9** |
| diagnostic Cap residual pure wave1–5 | rest T 28→4；历史 L4@`02c5d234` |
| bstrict 假路径根修 | 默认禁止 Stage2 `shux_asm2` 冒充产品 shux |

**构建级别（本波结案）**：**L4 真冷全测**（双端）。  

| 端 | prove | rest T | g05 | 矩阵 | bstrict |
|----|-------|--------|-----|------|---------|
| macOS | IDENTICAL | **0** | OK | rv=42 / opt / hello / si | **123/123** |
| Ubuntu | IDENTICAL | **0** | OK | rv=42 / opt / hello / si | **123/123** |

## 本波摘要（driver_abi Cap residual pure wave1）

| 波 | 结果 |
|----|------|
| **driver_abi Cap residual pure wave1** `88e7b540` | **9× flag-slot BSS 真迁 thin.x**；FROM_X rest 无 pure-dup flag_slot；**rest _impl 32→23**；surface IDENTICAL；**双端** g05 hybrid + 矩阵 rv=42/opt=102/hello/si；**.x 英文 G.9**；**日常真测 L2/L3** mac prove `/tmp/prove_dabi_wave2.log` · Ubuntu `/tmp/ubuntu_prove_dabi_wave2.log`（非 tip L4；产品放行仍 `3d2f7d94`） |

**构建级别（本波）**：**日常真测 L2/L3**（mac；Ubuntu 同波补测）。产品 tip L4 放行面仍为 **`3d2f7d94`**，直至本波收口后再升 tip L4。


## 本波摘要（driver_abi Cap residual pure wave2）

| 波 | 结果 |
|----|------|
| **driver_abi Cap residual pure wave2** | **path/len BSS 真迁 thin.x**（dep path *u8 · entry_source_len i64 · path_last_preprocess_len i64 + store/load pure）；FROM_X rest 无 pure-dup path/len store/load _impl；path_read 经 store 写 len；**rest _impl 24→18**（nm -g hybrid）；surface IDENTICAL；**双端** g05 hybrid + 矩阵 rv=42/opt=102/hello/si；**.x 英文 G.9**；**日常真测 L2/L3** mac prove `/tmp/prove_dabi_wave2.log` · Ubuntu `/tmp/ubuntu_prove_dabi_wave2.log`（非 tip L4；产品放行仍 `3d2f7d94`） |

**构建级别（本波）**：**日常真测 L2/L3**。产品 tip L4 放行面仍为 **`3d2f7d94`**。

## 本波摘要（driver_abi Cap residual pure wave3）

| 波 | 结果 |
|----|------|
| **driver_abi Cap residual pure wave3** `fd1cacf2` | **format print 真迁 thin.x**（check_ok / fail XP003·XP004 / smoke parse·typeck：`driver_diag_append_*` + `diag_report*`，无 va_list reportf）；FROM_X rest 无 pure-dup print/fail format `_impl`；**rest _impl 18→12**；surface IDENTICAL；**双端** g05 hybrid + 矩阵 rv=42/opt=102/hello/si；**.x 英文 G.9**；**日常真测 L2/L3** mac prove `/tmp/prove_dabi_wave3.log` · g05 `/tmp/g05_dabi_wave3.log` · Ubuntu prove `/tmp/ubuntu_prove_dabi_wave3.log` · g05 `/tmp/ubuntu_g05_dabi_wave3.log`（非 tip L4；产品放行仍 `3d2f7d94`） |

**构建级别（本波）**：**日常真测 L2/L3**。产品 tip L4 放行面仍为 **`3d2f7d94`**。

## 本波摘要（driver_abi Cap residual pure wave4）

| 波 | 结果 |
|----|------|
| **driver_abi Cap residual pure wave4** | **defines_set_at + os_define_lit 真迁 thin.x**（G.7 `shux_ptr_slot_set` 写 defines 槽；OS_* 字面量表）；FROM_X rest 无 pure-dup set_at/os_lit `_impl`；**rest _impl 12→9**（nm -g hybrid rest T `*_impl`）；surface IDENTICAL；**双端** g05 hybrid + 矩阵 rv=42/opt=102/hello/si；**.x 英文 G.9**；**日常真测 L2/L3** mac prove `/tmp/prove_dabi_wave4.log` · g05 `/tmp/g05_dabi_wave4.log` · Ubuntu prove `/tmp/ubuntu_prove_dabi_wave4.log` · g05 `/tmp/ubuntu_g05_dabi_wave4.log`（非 tip L4；产品放行仍 `3d2f7d94`） |

**构建级别（本波）**：**日常真测 L2/L3**。产品 tip L4 放行面仍为 **`3d2f7d94`**。

## 本波摘要（driver_abi Cap residual pure wave5）

| 波 | 结果 |
|----|------|
| **driver_abi Cap residual pure wave5** `d2ff2fcd` | **phase timing BSS + begin/end 真迁 thin.x**（acc_ms/start_sec f64[3] + active i32[3]；`acc_ms_get`/`timing_clear` 供 flush Cap）；FROM_X rest 无 pure-dup begin/end `_impl`；**rest _impl 9→7**（Ubuntu hybrid nm；mac 含 `asm_use_compiler_impl_c` 计 8）；flush `diag_reportf` floats 仍 Cap；`-E` 避坑：`1000.0`→0.0 用 `(1000 as f64)`；surface IDENTICAL；**双端** g05 hybrid + 矩阵 rv=42/opt=102/hello/si + phase_timing 烟测；**.x 英文 G.9**；**日常真测 L2/L3** mac prove `/tmp/prove_dabi_wave5.log` · g05 `/tmp/g05_dabi_wave5.log` · Ubuntu prove `/tmp/ubuntu_prove_dabi_wave5.log` · g05 `/tmp/ubuntu_g05_dabi_wave5.log`（非 tip L4；产品放行仍 `3d2f7d94`） |

**构建级别（本波）**：**日常真测 L2/L3**。产品 tip L4 放行面仍为 **`3d2f7d94`**。

## 本波摘要（driver_abi Cap residual pure wave6）

| 波 | 结果 |
|----|------|
| **driver_abi Cap residual pure wave6** `51dc7b8d` | **phase_timing_flush 真迁 thin.x**（whole-ms i32 via `driver_diag_append_*` + `diag_report`；无 va_list reportf floats）；FROM_X rest 无 pure-dup flush `_impl`；**Cap residual rest _impl 7→6**（nm -g hybrid；另 `asm_use_compiler_impl_c` 计总 7）；冷 twin 同整型 ms 格式；surface IDENTICAL；**双端** g05 hybrid + 矩阵 rv=42/opt=102/hello/si + phase_timing 烟测 `parse_ms=0 typeck_ms=…`；**.x 英文 G.9**；**日常真测 L2/L3** mac prove `/tmp/prove_dabi_wave6.log` · g05 `/tmp/g05_dabi_wave6.log` · Ubuntu prove `/tmp/ubuntu_prove_dabi_wave6.log` · g05 `/tmp/ubuntu_g05_dabi_wave6.log`（非 tip L4；产品放行仍 `3d2f7d94`） |

**构建级别（本波）**：**日常真测 L2/L3**。产品 tip L4 放行面仍为 **`3d2f7d94`**。

## 下一波前排

1. **E 真迁续**：driver_abi Cap residual 真锁（uname / setrlimit / pthread / path-read IO / gettimeofday · 剩余 Cap rest `_impl`=6）或 fmt_check Cap residual / Stage2 WPO  
2. **禁止**把单叶 IDENTICAL / Stage2 / 抽检写成完全自举（产品放行 = tip 双端 L4@**`3d2f7d94`** 直至新 tip L4）  
3. 动 SHARED 产品面 → 新 SHA 再双端 L4（勿沿用旧 tip 日志）  



