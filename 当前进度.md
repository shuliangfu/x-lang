# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **43** IDENTICAL（附注） |
| **R2** | **1**（`rt_util` full：unlink + argv0_basename） |
| **H** | `rt_util` **H=0**；其余 runtime hybrid 多仍 rest C |
| **rt_util** | 产品 **R2 PREFER_X_O**：full .x；FROM_X rest 仅前向声明 |
| Cap-empty-str | ✅ 已由 W-string 解锁（本波无需再修编译器） |

## 本波：Track R · `rt_util` R2 真迁 ✅

**判定**：旧「不支持 `""`」为过时注释；`let empty: *u8 = ""` 与纯索引实现均可。选 **纯索引**（与 `runtime.x` 同源）避免 libc/空串依赖。

| 路径 | 变更 |
|------|------|
| `src/runtime/rt_util.x` | 吃满 `driver_unlink_failed_output` + `driver_argv0_basename_is` |
| `seeds/rt_util.from_x.c` | 两符号均 `#ifndef SHUX_RT_UTIL_FROM_X`；FROM_X 下 rest **T=0** |
| `seeds/rt_util_surface.from_x.c` | full surface 2 符号（prove 锁） |
| `tests/prove_module_selfhost.sh` | 注释：R2 full |

- 验收：`bash tests/prove_module_selfhost.sh rt_util` → **IDENTICAL**（2 符号）
- 全量：**43 IDENTICAL**
- 产品 hybrid：thin.o 2×T + rest.o 0×T → merged 2×T 全来自 .x
- 语义烟测：path/bare/mismatch/null/win-slash → **SMOKE_OK**

## Ubuntu 金标准（`672c931a`，2026-07-14 14:50 CST）

| 项 | 结果 |
|----|------|
| `rt_util` R2 full | **IDENTICAL**（2 符号；H=0） |
| 全量 | **43 IDENTICAL** |

## 前排下一步

1. 下一批 R2：`rt_lib_root` rest / content rest / 或 Cap-global-bss → `rt_arena_buf`
2. Track M：typeck M2 试替换
3. 纪律：commit → push → Ubuntu → 金标一行

## 方法备注

后 T 主线：**Cap → 批量 R2 → M 并行**；禁止为 N 再 surface 下一叶子。
