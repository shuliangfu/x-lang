# Shux 当前进度（2026-07-18）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **17 号全日归档**：[analysis/自举进度-归档-2026-07-17.md](analysis/自举进度-归档-2026-07-17.md) · [16 号](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`ea8dcc4a`**（dep prerun soft XT001 suppress） |
| 产品 L4 放行 | ✅ **钉盘 `5c8204ae`**（本波 **未**升 tip L4） |
| tip 双端 bstrict | ✅ **123/123 @ `5c8204ae`** |
| **asm hello / hosted rv** | ✅ **Ubuntu L2** |
| **fs r42 / panic_div / hello** | ✅ **`run-freestanding-hello` OK** |
| **fs cookbook vec_i32_push** | ✅ **run=0 · 无 XT001** |
| **nolibc-n06** | ✅ |

## 本波摘要（2026-07-18 · freestanding soft XT001 · dep prerun）

| 波 | 结果 |
|----|------|
| **入口** | 用户「继续下一步」；前排软 XT001 on freestanding co-emit `new` / `heap_mem_set_c` |
| **地图** | dep prerun 全量 library typeck：`one_ctx` 常 `ndep=1` 且 path 槽 `num_funcs=0` → `heap.default_alloc` / `mem.mem_set` METHOD_CALL 失败 → soft XT001；随后 **light fallback** 仍绿出二进制 |
| **根因** | 探测性全量 typeck 失败后的 soft 诊断噪声（非 entry 硬红）；ctx 空槽是 prerun 序/闭包填槽限制 |
| **根修** | `pipeline_typeck_dep_prerun_module_c` 期间 soft-suppress XT001；`driver_diagnostic_typeck_func_fail` 门控；附 METHOD_CALL 空槽跨 dep 回退（`5ae92b80`）+ STRUCT_LIT field expected=0（`85c6cf67`） |
| **验收** | Ubuntu L2：fs cookbook **无 XT001 · run=0** · freestanding gate OK · hosted rv/hello/cookbook · **日常 L2 · 非 tip L4** |
| **重建** | glue：`pipeline_x.o` + `pipeline_glue_standalone.o`；diagnostic.o + g05 |

### residual 下一波

1. （可选）9–16B Allocator 双 GP home 全对齐 formal C  
2. （旁）hosted/fs `vec_u16` BLD001 mangle；f64 let-init；cfg-extern / `.bss` / labi `len_empty`  

```text
构建级别：L2（pipeline_x + standalone + diagnostic + g05；非 L4）
产品 tip L4 钉盘：5c8204ae
Ubuntu：fs cookbook 无 XT001 · freestanding gate · hosted 矩阵
纪律：本波未升 tip L4；抑制仅限 dep prerun 探测窗
feat SHA：ea8dcc4a（链 85c6cf67 → 5ae92b80 → ea8dcc4a）
```
