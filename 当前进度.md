# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（Cap / R / L / M）  
> **步骤模板**：[自举步骤.md](自举步骤.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `9fd324d0`（feat）/ docs 待合 |
| **T** | **18/18** typeck OK |
| **N** | **53** IDENTICAL（附注） |
| **R2** | **24**（+`labi_diag_pure`；Ubuntu 金标） |
| **H** | 上述二十四切片 **H=0**；其余 runtime hybrid 多仍 rest C（labi_path_io / diagnostic thin…） |

## 本波：Track R · `labi_diag_pure` R2 full 真迁 ✅

| 路径 | 变更 |
|------|------|
| `src/runtime/labi_diag_pure.x` | 吃满 `code_for_kind` + 7 report 消息体（栈拼装 + `diag_report_with_code`） |
| `seeds/labi_diag_pure.from_x.c` | 业务符号 `#ifndef SHUX_LABI_DIAG_PURE_FROM_X`；FROM_X 下 rest 仅前向 + marker → **业务 H=0** |
| Cap residual mega rest | `link_diag_ld_debug_argv_impl`（char** 遍历） |
| `seeds/labi_diag_pure_surface.from_x.c` | full surface（prove 锁） |
| `seeds/runtime_link_abi.from_x.c` | hybrid FROM_X 下丢弃已迁 `_impl`（argv_impl 常驻） |
| `tests/prove_module_selfhost.sh` | 标注 `labi_diag_pure` R2 full → surface |

- 验收：mac + Ubuntu `prove_module_selfhost labi_diag_pure` → **IDENTICAL**；g05 L1 prefer .x + hybrid link_abi.o
- KPI：**R2 23→24**；N **53**（附注，此前已在 N）；该切片 H **1→0**
- 产品 hybrid：full .x + mega rest 无已迁 report `_impl`

## 下一波前排

1. **R2** runtime 其余 hybrid rest（labi_path_io / labi_host_lit / diagnostic thin rest…）· Cap-fn-ptr 批量  
2. **Track M** typeck M2 试替换 `typeck_x.o` smoke  
3. 纪律：commit → push → Ubuntu → docs；KPI **H↓ R2↑**
