# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| **T** | **18/18** typeck OK |
| **N** | **33** IDENTICAL（+ `rt_fmt_one` thin） |
| **rt_fmt_one** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 1） |
| **rt_dispatch_impl** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 1） |
| **rt_dispatch** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 1） |
| **backend_call_dispatch** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 44） |
| **backend_try_inline_dispatch** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 50） |
| **backend_arch_emit_dispatch** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 47） |
| **backend_enc_dispatch** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 120） |
| **simd_enc** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 74） |
| **simd_loop** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 22） |
| **fmt_check** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 42） |
| **driver_abi** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 70） |
| **diagnostic** | 产品 L2 thin **PREFER_X_O**；prove **IDENTICAL**（公共面 79） |
| **W-string-nul** | ✅ STRING_LIT *u8 尾 `, 0`；slen≤64 |

## 本波：Track L `rt_fmt_one` hybrid→IDENTICAL（L2 thin）✅

**根因地图**：产品 hybrid 已走 **rt_fmt_one.x** + hybrid rest seed（`-DSHUX_RT_FMT_ONE_FROM_X`）入 `runtime_driver_no_c`，但 prove 未锁 thin 公共面。产品 rest seed 含 read/format/write body，与 pure thin 同构面分离。

**落地**：

| 路径 | 变更 |
|------|------|
| `src/runtime/rt_fmt_one.x` | 权威 thin 公共面（产品本已用；1 门闩 `driver_fmt_one_file`） |
| `seeds/rt_fmt_one_surface.from_x.c` | **新建** 冷/prove 同构 pure thin（`-E` 抛光 + POSIX prologue） |
| `seeds/rt_fmt_one.from_x.c` | **不变**（产品 hybrid rest：read/format/write body/marker） |
| `tests/prove_module_selfhost.sh` | 扩 `rt_fmt_one` → thin.x + surface seed；空 mode → **IDENTICAL** |

- 验收：`bash tests/prove_module_selfhost.sh rt_fmt_one` → **IDENTICAL**
- 全量：`bash tests/prove_module_selfhost.sh` → **33 IDENTICAL**
- 产品：g05 仍 `thin.x + hybrid rest ld -r` 入 `runtime_driver_no_c.o`（本波不改链接拓扑）

## Ubuntu 金标准（待 push 后补 hash，`date` 东八区）

| 项 | 结果 |
|----|------|
| `rt_fmt_one` thin | **IDENTICAL**（产品 L2 thin @ `self-hosting`；mac 已绿） |
| 同批 | rt_dispatch_impl + rt_dispatch + call_dispatch + try_inline + arch_emit + backend_enc + simd_* + fmt_check + diagnostic + driver_abi + labi 叶 + **全量 33 IDENTICAL** |

## 前排下一步

1. 下一可 IDENTICAL 叶（runtime 其它 hybrid thin：`rt_pipeline_elf_diag` / `rt_run_x_emit` / `rt_parse_diag`…）/ true-migrate rest
2. W-global 数组 / 串池 >64
3. 纪律：commit → push → Ubuntu → docs
