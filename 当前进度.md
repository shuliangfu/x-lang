# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`d6ff0371`**（docs）· 产品代码叶 **`a9cd4201`** · Darwin al06 **`36ac3d14`** |
| 产品 L4 放行 | 仍钉 **`5a636984`** 双端 L4+123（**未**升 tip 全擦 L4） |
| tip 双端 bstrict | ✅ **123/123 @ `36ac3d14`**（g05 + `SHUX_BSTRICT_SKIP_BUILD=1`，**非 L4 全擦**） |
| **M typeck** | ✅ + 同模块 zero-arg expected-return（`a9cd4201`） |
| **M codegen** | ✅ + 信任 typeck resolve 做 overload 链名（`a9cd4201`） |
| **parser residual** | ✅ body-lets · hoist · force 全量 re-pin |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 晚 · tip 续）

| 波 | 结果 |
|----|------|
| **双端 bstrict** | tip **`36ac3d14`**：mac `/tmp/mac_bstrict_36ac3d14.log`（**BSTRICT_RC:0**）· Ubuntu `/tmp/ubuntu_bstrict_36ac3d14.log`（**OK 123 scripts + al06**；日志末 `BSTRICT_RC:127` 为 waiter 假码，以 OK 行为准） |
| **Darwin al06 根修** | `36ac3d14`：去掉 strict_minimal weak 空桩 `pipeline_block_set_let_type_ref`（恒 -1）；filtered pipeline 私有化真实现后 stamp 失败 → dual_escape 误报 `found ?`；现 **slice region escape** 绿 |
| **无 libc 分析** | `analysis/无libc基础与-libc开关分析.md`：L1 用户 freestanding 有骨架；L2 产品/bootstrap 仍 `-lc`；**不在自举后再切**；短期待强化 `-freestanding`，慎加默认 `--libc` |
| **std.vec 根修** | `a9cd4201`：① glue `pick_overload` +expected-return 5000；② codegen 不再丢弃 overload 的 typeck resolve → 体内 `std_vec_new_retVec_u8()` 与定义对齐；`std/vec/mod.x` **typeck OK** |
| **矩阵** | rv=42 · hello 绿（本波 g05 后） |
| **残余** | **std.vec.o 产品入链**：compile 脚本 weak 重定义；labi **未** on-demand 链 `std/vec/vec.o` → `with_arena_vec -o` 仍 UNDEF |

### 五问（std.vec / dual_escape）

| 问 | 答 |
|----|-----|
| 谁产生 | typeck 同模块 pick_overload（缺 expected-return）；codegen emit_call 拒绝 resolve；Darwin set_let weak 空桩 |
| 谁存储 | `g_typeck_overload_expected_ret` · `call_resolved_func_index` · region label / let.type_ref |
| 谁消费 | let 类型比对 · C 符号名 · al06-gate 文案 · ld |
| 同模式 | 任意零参 overload（`new` 族）；其它 weak 空桩 vs filtered 导出 |
| 最小回归 | `std/vec/mod.x` typeck · with_arena_vec `-E` · rv/hello · al06 dual_escape |

### residual 下一波

1. **std.vec.o**：`shux_compile_std_module` 去 weak 冲突 + **产品 link on-demand** 入链 → 关 `with_arena_vec -o`  
2. 用户明示时 tip **L4 真冷** + 双端 bstrict 123（升放行钉盘）  
3. 可选 **NL-07 v4 try**（Ubuntu nostdlib bootstrap track）  

```text
构建级别：日常 L2/g05（非 tip L4 全擦）
产品 tip L4 钉盘：5a636984
tip HEAD：d6ff0371
tip bstrict@36ac3d14：双端 123 + al06 ✅
with_arena_vec typeck：OK（std_vec_new_retVec_u8）
with_arena_vec -o：UNDEF std.vec（入链另债）
矩阵：rv=42 / hello 绿
```
