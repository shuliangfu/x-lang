# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting`（tip **`66c8496c`** + 本波 docs） |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **D Stage2** | ✅ Ubuntu freestanding D-03 @ `c3031883`（**≠** 产品链） |
| **E R2** | ✅ diagnostic / driver_abi / fmt_check Cap residual pure **收口** |
| **Stage2 WPO full-chain** | ✅ @ **`57665c75`** proxy **5.81%** |
| **M typeck** | ✅ **M1** + ✅ **M2 smoke** @ `256acd3b`（seed pin 未换） |
| **M codegen** | ✅ **Cap-T001** + ✅ **M1**；❌ **M2 smoke 红**（host-cc 非法 C）— 地图见下 |
| **T / EMPTY / N** | 记账仍 18/18 · 18/18 · 54/54 |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **codegen M2 smoke（工程轨 · 红）** | 目标：`shux_asm` force-regen `codegen.x` → `codegen_gen.c` → `codegen_x.o` + g05 + 矩阵（**不改 seed pin**）。**M1 仅证 `-E`/typeck OK，未证 host `cc`**。本波：`-E-extern` rc=0、typeck OK、out≈330KiB，但 **host `cc` 失败**。**现象**：文件级 `static` ≈390（`op`×21 / `comma`×11 重名）；假 `static void init_globals(void)` 体≈229 行（形如 `emit_expr` 直线化体，引用 `arena`/`expr_ref`）；**缺 4 函数体**：`emit_expr`、`codegen_x_ast`、`codegen_should_skip_emit_struct_layout_for_abi_dup`、`codegen_arena_for_module`（源仍在；pin 有完整 `SHUX_LIB_WEAK`）。**五问**：产生=产品 seed codegen 发射 Cap 后 mega `codegen.x`；存储=非法 `codegen_gen.c`；消费=host `cc` / M2 替换。**对照**：typeck M2 force-regen host-cc 绿；codegen **pin** 绿。**恢复**：`codegen_gen.c`/`codegen_x.o` **回 pin**；g05 + mac 矩阵 **rv=42 / opt / hello(`0a`) / si** 绿。**seed pin / tip L4 未动**。日志：坏 C `/tmp/codegen_m2_gen.c` · pipe `/tmp/codegen_m2_e.err` · 矩阵本波 restore 后绿。 |

### 已合入 / 本波

| SHA | 摘要 |
|-----|------|
| `66c8496c` | **codegen M1**：`MAX_ONEFUNC_SIDECARS` 256→1024 |
| （本波 docs） | **codegen M2 smoke 红**：地图 + residual；非 tip L4 |
| `106a6b73` | **codegen Cap-T001** whole-body unsafe ×64 |
| `256acd3b` | **typeck Cap-T001** whole-body unsafe（M1/M2 smoke） |

### mac 探针（本波）

| 项 | 状态 | 日志 / 备注 |
|----|------|-------------|
| codegen M2 force-regen | ❌ host-cc | 坏 C `/tmp/codegen_m2_gen.c` · 缺 4 函数 + static 重名 |
| pin 恢复 + g05 | ✅ | `/tmp/mac_m2_restore_g05.log` |
| 产品矩阵 | ✅ rv=42 · opt · hello(`0a`) · si | seed pin 未换 |
| 产品 tip L4 | 仍 **`5a636984`** | **非 tip L4** |

### 已知 residual

| 项 | 说明 |
|----|------|
| **codegen M2** | force-regen 非法 C；根修方向：seed 对 mega/`emit_expr`（~97KB 体）发射，或 Cap 对 mega 改细粒度 unsafe（勿 soft-skip） |
| seed re-pin | **blocked on M2 绿**；pin = 产品面 → 双端 L4 |
| onefunc 泄漏 | peak 385≫函数数；`impl_snap` 等未 release（容量已够） |
| parser M1 | XT001 `parser_rewind_lex_for_lparen_control_stmt` |
| 产品 tip L4 | 仍 **`5a636984`** |

## 下一波前排

1. **codegen M2 根修**（非法 C / 缺 `emit_expr`·`codegen_x_ast`）→ 再 M2 smoke / seed re-pin  
2. parser XT001 rewind / typeck seed re-pin  
3. **禁止**把 Cap-T001 / M1 / M2 smoke 写成 tip 产品 L4  

```text
构建级别：工程 L2（codegen M2 smoke 红 + pin 恢复；非 tip L4）
产品 tip L4：5a636984
codegen M1：MAX_ONEFUNC 1024 · -E 绿
codegen M2：force-regen host-cc 红 · pin 恢复 · 矩阵绿
产品矩阵：mac rv/opt/hello/si 绿（seed pin 未换）
```
