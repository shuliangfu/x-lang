# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| tip HEAD | **`e3d63a68`**（NL-07 L1 crt0 glue 单一权威） |
| 产品 L4 放行 | 仍钉 **`5a636984`** 双端 L4+123（**未**升 tip 全擦 L4） |
| tip 双端 bstrict | ✅ **123/123 @ `36ac3d14`**（g05 + SKIP_BUILD；**非 L4**；本波未重跑全量） |
| **std.vec 产品 -o** | ✅ **双端** formal `vec.o` + on-demand · `with_arena_vec_push` |
| **NL-07 硬绿 L1** | ✅ **multi-def=0**（crt0 bag 去 strict_minimal∩standalone）；nostdlib crt0 **仍红**（UNDEF） |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要（2026-07-17 · NL-07 L1 拓扑 / multi-def）

| 波 | 结果 |
|----|------|
| **入口** | 前排 #2 硬绿分层第 1 层：crt0 对象拓扑（**非** tip L4） |
| **根因** | crt0 整包 `NONEMPTY_ASM` 同时链 `pipeline_glue_standalone` + `pipeline_glue_strict_minimal` + `preprocess_if_stack_only` → 9 个 multiple definition；strict/experimental 早已单一 glue 权威，crt0 未对齐 |
| **根修** | **`e3d63a68`**：`filter_crt0_asm_objs` — standalone 在时 drop strict_minimal + preprocess_if_stack_only；权威 `build_shux_asm.sh`；PLATFORM LINUX/NL-07 L1 |
| **验收** | Ubuntu TRY_BUILD：`multi=0`（was 9）；log 见 `crt0 bag: glue authority=pipeline_glue_standalone`；unique UNDEF ≈**284**（前排仍 `fflush` / backend_enc / typeck…） |
| **门禁** | v4 gate **OK**（fallback）；**无** `bootstrap nostdlib crt0 link OK`；`ldd` 仍 **libc.so.6** |
| **产品** | Ubuntu `function main(): i32 { return 42; }` → **rv=42**；**禁止** 当 nostdlib 硬绿 / tip L4 升钉 |
| **日志** | `/tmp/ubuntu_n07_l1_e3d63a68.log` · err `compiler/build_asm/.bootstrap_nostdlib_link_err` |

### 五问（NL-07 L1 multi-def）

| 问 | 答 |
|----|-----|
| 谁产生 | crt0 整包同时入链 subset glue（strict_minimal / preprocess_if_stack_only）与 full standalone |
| 谁存储 | `build_asm/pipeline_glue_*.o` · `preprocess_if_stack_only.o` |
| 谁消费 | crt0 链 `CRT0_ASM` → `ld` |
| 同模式 | strict/experimental 已 filter 后单 `ST_GLUE_OBJ`；crt0 是漏网 |
| 最小回归 | Ubuntu TRY_BUILD：err **multi=0**；仍允许 UNDEF 红（下层） |

### residual 下一波

1. 用户明示时 tip **L4 真冷** + 双端 bstrict 123（升放行钉盘）  
2. NL-07 **硬绿 L2**：`fflush` 进 freestanding_io 或 nostdlib stubs（G.7 有则补全）  
3. NL-07 L3+：backend_enc / typeck / driver companion 入链（一条债一层）  
4. 其它产品面 / Cap  

```text
构建级别：日常 L2（NL-07 L1；非 tip L4 全擦）
产品 tip L4 钉盘：5a636984
tip HEAD：e3d63a68
NL-07 L1：multi-def 关 · nostdlib crt0 仍红（UNDEF≈284）· gate fallback 绿
产品矩阵抽检：Ubuntu rv=42
```
