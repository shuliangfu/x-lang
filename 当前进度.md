# Shux 当前进度（2026-07-16）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` @ `d2d0ac54`（T lexer M1） |
| 产品 | 双端 bstrict **123** @ `233e3b72`（L4 历史；本波工程未重跑 L4） |
| **T** | **12/18**（mac+Ubuntu @ `d2d0ac54`；lexer typeck OK / CODEGEN residual） |
| **N** | **54/54 IDENTICAL**（全表 mac+Ubuntu @ `11f3ad55`） |
| **R2 / H** | 可解冻抽检（N 全表已满） |
| **Track L SYM** | **0** |
| **FAIL residual** | **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **T / Track M1 · lexer** | Cap-T001 unsafe 包 bare extern；`token.Token`/`token.TokenKind` 限定；`lexer_next_punct_into` 拆标点链 → **typeck OK**（双端）；codegen residual 另账 |

**构建级别**：日常真测 L2/L3（**非** L4；产品仍 pin lexer seed）。  
**SHUX**：本机/Ubuntu 既有 `compiler/shux_asm`。

日志：
- mac `/tmp/mac_diff_t_lexer_fix.log` · T **12/18**
- Ubuntu `/tmp/ubuntu_diff_t_d2d0ac54.log` · T **12/18**

## 下一波前排

1. **T 剩余 6**：`parser` · `typeck` · `codegen` · `lsp` · `lsp_diag` · `lsp_io`（优先最小 T001 / 同模式）  
2. 谈 Stage2 前：**L4 真冷 + 双端 bstrict**  
3. **禁止**用 N=54 / T=12 单独宣称「可自举」  
