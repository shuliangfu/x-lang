# Shux 当前进度（2026-07-14）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)  
> **方法**：[自举方法.md](自举方法.md)（**不**每波改；仅纪律/门禁变时改）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `g-02f-self-hosting` |
| **N** | **6**（+compile IDENTICAL） |
| **emit** | typeck OK；PREFER_X_O **cc 失败**（局部 `PipelineDepCtx` 字段赋值被 -E 编成 `ctx->`） |

## 本波：Track L compile ✅

1. prove：`compile` **IDENTICAL**（rename 映射 short→`driver_*`）。  
2. 退役：`driver_compile_x.o` ← `driver_leaf_x_to_o.sh` + seeds 冷启动。  
3. emit：尝试 `pipeline_dep_ctx_fill_for_emit(*ctx)`；`run_x_emit_x` 内其它 `ctx.field =` 仍被 -E 误编 → 仍 pin seed。

## 下一步

1. emit：根治 -E 对**局部大 struct** 字段赋值（`.` vs `->`）或继续源侧绕开  
2. lexer TIMEOUT / NOSEED  
3. W-unsafe-expr  
