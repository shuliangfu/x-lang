# Shux 当前进度（2026-07-17）

> **长期仪表盘**：[analysis/自举进度.md](analysis/自举进度.md)（KPI 真值）  
> **方法**：[自举方法.md](自举方法.md) · **步骤**：[自举步骤.md](自举步骤.md) §5.1  
> **纪律**：每波更新 **本文 + 自举进度**；`问题分析文档.md` 非每波  
> **铁律锚点**：`AGENTS.md`（根源/G.7/G.8/G.9）· skill `shux-selfhost-product-gate` · 双轨假绿杜绝（§0.A）  
> **16 号全日归档**：[analysis/自举进度-归档-2026-07-16.md](analysis/自举进度-归档-2026-07-16.md)（含当日 `当前进度` 附录）

## 总体状态

| 指标 | 状态 |
|------|------|
| 分支 | `self-hosting` |
| 产品 L4 放行 | **双端 tip L4+bstrict 123/123 @ `5a636984`**（**本波未升 tip L4**） |
| **M typeck** | ✅ M1+M2 @ `256acd3b` |
| **M codegen** | ✅ Cap+M1+**M2 re-pin**（mac+Ubuntu 矩阵全绿） |
| **parser residual** | ✅ 匿名 `{ let }` · 🟢 **parser M1 -E 绿** · 🟢 **类型序绿** · 🟢 **init_globals residual 关** |
| **lexer soft residual** | ✅ block-comment 奇次 `"` 硬复现收口 |
| **Track L SYM / FAIL** | **0** / **0** |

## 本波摘要

| 波 | 结果 |
|----|------|
| **前排** | parser M1 host-cc residual：`init_globals` 内 `module`/`arena` undeclared |
| **诊断** | (1) 宿主 product parser **parse-skip** mega `parse_into` / `parse_into_buf`（onefunc 体过大失败）；(2) `skip_one_function_full` 锚点常落在 **已越过 `function` 的 IDENT 名**（pos 在 name 前空格），首 token ≠ FUNCTION → **早退不跳体**；(3) 顶层循环把残体 `let` 当 top_level_lets → static + init_globals 引用形参 |
| **根修** | `parser_asm_glue_tail_slice.inc` **skip 权威**：接受 `export` / `function` / 已过 keyword 的 **IDENT 名** 三种锚点，再 skip 签名+balanced braces（G.7 扩旧，无第二套） |
| **验收** | force-regen **无** `init_globals`；**0** residual static；`undeclared module/arena = 0`；codegen_bytes≈304k |
| **host-cc residual 现前排** | 🟡 init_globals 已关；仍红 **dual extern conflicting** + **ast_* incomplete** + **by-value return 成 int**；且 `parse_into`/`parse_into_buf` 仍 skip（体未 emit）— **下一叶** |
| **矩阵 mac L2** | **rv=42 / opt=0 / si=0 / hello=0** ✅ |
| **未做** | tip L4；parser seed re-pin / M2；host-cc 全绿；Ubuntu（SHARED skip 须） |

### 五问（本叶）

| 问 | 答 |
|----|-----|
| 谁产生 | skip_one_function_full 对 post-`function` 锚点早退 |
| 谁存储 | module.num_top_level_lets 被残体 let 污染 |
| 谁消费 | codegen init_globals / host gcc |
| 同模式 | 任意 mega 函数 parse 失败 + skip 锚点错位 |
| 最小回归 | parser.x `-E` 无 init_globals + 无 undeclared module/arena + 产品矩阵 |

### residual 下一波

1. **parser M1 host-cc residual**：dual extern / incomplete ast_* / by-value return；或让 mega `parse_into*` 真 parse 成功  
2. parser M2 / seed re-pin（须 host-cc 更绿或明确范围）  
3. SHARED：Ubuntu 验本 skip；升 tip 须明示  

```text
构建级别：日常真测 L2（parser_asm_thin_glue.o + g05；非 L4）
产品 tip L4：5a636984
parser M1：-E 绿；类型序绿；init_globals residual 关；host-cc 仍有后续 residual
产品矩阵：mac rv/opt/si/hello ✅
```
