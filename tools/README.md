# tools/ — 工具（可选）

**配套工具**：格式化、测试运行器、包管理等，非编译器本体。

- **用途**：提升开发体验（如 `shuxfmt` 格式化 .sx、`shuxtest` 跑测试）；按需实现，不强制。
- **内容**：各工具单独子目录或单入口；可由 .sx 编写，用 shux 编译。
- **阶段**：M3 及以后按需添加；初期可空目录保留占位。

详见 `analysis/README.md` 中「开发体验」与里程碑。
