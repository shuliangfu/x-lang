/**
 * Cookbook DB-01：sqlite_is_available 运行时探测（STD-167）。
 */
const sqlite = import("std.db.sqlite");

function main(): i32 {
  let av: i32 = sqlite.sqlite_is_available();
  if (av != 0 && av != 1) { return 1; }
  return 0;
}
