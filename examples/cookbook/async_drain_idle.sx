/**
 * Cookbook ASYNC-02：协作调度器 drain 空转（链入 scheduler.o）。
 */
const async_mod = import("std.async");

extern function shux_async_run_drain_until_idle(): i32;

function main(): i32 {
  let _: i32 = async_mod.placeholder();
  let __: i32 = async_mod.shux_async_run_drain_until_idle();
  return 0;
}
