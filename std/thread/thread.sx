// std/thread/thread.sx — F-thread v1 模块锚点（pthread 封装在 thread_glue.c）
//
// 【文件职责】
// 提供 F-thread v1 可编译 .sx 单元；经 ld -r 与 thread_glue.c 合并为 thread.o。
// 对外 API 在 mod.sx。

/** F-thread v1 版本标记；供聚合 gate 校验 thread.sx 已参与 ld -r 合并。 */
function thread_f_thread_v1_marker_c(): i32 {
  return 1;
}
