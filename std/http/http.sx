// std/http/http.sx — F-http v1 模块锚点（HTTP/1.x/H2 在 http_glue.c + *.inc.c）
//
// 【文件职责】
// 提供 F-http v1 可编译 .sx 单元；经 ld -r 与 http_glue.c 合并为 http.o。
// 对外 API 在 mod.sx。

/** F-http v1 版本标记；供聚合 gate 校验 http.sx 已参与 ld -r 合并。 */
function http_f_http_v1_marker_c(): i32 {
  return 1;
}
