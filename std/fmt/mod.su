// std.fmt — 格式化与标准输出（对标 Zig std.fmt、Rust std::fmt）
//
// 【文件职责】
// 重导出 core.fmt；提供 print/println；format_2/format_3 通过函数重载按实参类型分派。
const fmt = import("core.fmt");
const io = import("std.io");

/** Tier-S 烟测钩子。 */
function placeholder(): i32 { return 0; }

/** 单占位模板：pat 中首个 brace-pair 替换为 i32 十进制（STD-166）。 */
function format_template_1_i32(buf: *u8, cap: i32, pat: *u8, pat_len: i32, val: i32): i32 {
  let i: i32 = 0;
  let o: i32 = 0;
  let replaced: i32 = 0;
  while (i < pat_len) {
    if (replaced == 0 && i + 1 < pat_len && pat[i] == 123 && pat[i + 1] == 125) {
      let n: i32 = fmt.fmt_i32_to_buf(&buf[o], cap - o, val);
      if (n < 0) { return -1; }
      o = o + n;
      i = i + 2;
      replaced = 1;
    } else {
      if (o >= cap) { return -1; }
      buf[o] = pat[i];
      o = o + 1;
      i = i + 1;
    }
  }
  return o;
}

function print(ptr: *u8, len: i32): i32 { return io.print_str(ptr, len as usize); }
function println(ptr: *u8, len: i32): i32 {
  let r: i32 = io.print_str(ptr, len as usize);
  let nl: [1]u8 = [10];
  let ign: i32 = io.print_str(&nl[0], 1 as usize);
  return r;
}

/** format_2 整数 overload 共用体：按形参类型调用 fmt_scalar_to_buf。 */
function format_2(buf: *u8, cap: i32, a: i32, b: i32): i32 {
  let n1: i32 = fmt.fmt_scalar_to_buf(buf, cap, a);
  if (n1 < 0) { return -1; }
  let n2: i32 = fmt.fmt_scalar_to_buf(&buf[n1], cap - n1, b);
  if (n2 < 0) { return -1; }
  return n1 + n2;
}
function format_2(buf: *u8, cap: i32, a: i32, b: u32): i32 {
  let n1: i32 = fmt.fmt_scalar_to_buf(buf, cap, a);
  if (n1 < 0) { return -1; }
  let n2: i32 = fmt.fmt_scalar_to_buf(&buf[n1], cap - n1, b);
  if (n2 < 0) { return -1; }
  return n1 + n2;
}
function format_2(buf: *u8, cap: i32, a: u32, b: i32): i32 {
  let n1: i32 = fmt.fmt_scalar_to_buf(buf, cap, a);
  if (n1 < 0) { return -1; }
  let n2: i32 = fmt.fmt_scalar_to_buf(&buf[n1], cap - n1, b);
  if (n2 < 0) { return -1; }
  return n1 + n2;
}
function format_2(buf: *u8, cap: i32, a: u32, b: u32): i32 {
  let n1: i32 = fmt.fmt_scalar_to_buf(buf, cap, a);
  if (n1 < 0) { return -1; }
  let n2: i32 = fmt.fmt_scalar_to_buf(&buf[n1], cap - n1, b);
  if (n2 < 0) { return -1; }
  return n1 + n2;
}
function format_2(buf: *u8, cap: i32, a: i64, b: i32): i32 {
  let n1: i32 = fmt.fmt_scalar_to_buf(buf, cap, a);
  if (n1 < 0) { return -1; }
  let n2: i32 = fmt.fmt_scalar_to_buf(&buf[n1], cap - n1, b);
  if (n2 < 0) { return -1; }
  return n1 + n2;
}
function format_2(buf: *u8, cap: i32, a: i32, b: i64): i32 {
  let n1: i32 = fmt.fmt_scalar_to_buf(buf, cap, a);
  if (n1 < 0) { return -1; }
  let n2: i32 = fmt.fmt_scalar_to_buf(&buf[n1], cap - n1, b);
  if (n2 < 0) { return -1; }
  return n1 + n2;
}
function format_2(buf: *u8, cap: i32, a: i64, b: i64): i32 {
  let n1: i32 = fmt.fmt_scalar_to_buf(buf, cap, a);
  if (n1 < 0) { return -1; }
  let n2: i32 = fmt.fmt_scalar_to_buf(&buf[n1], cap - n1, b);
  if (n2 < 0) { return -1; }
  return n1 + n2;
}
function format_2(buf: *u8, cap: i32, a: u64, b: u64): i32 {
  let n1: i32 = fmt.fmt_scalar_to_buf(buf, cap, a);
  if (n1 < 0) { return -1; }
  let n2: i32 = fmt.fmt_scalar_to_buf(&buf[n1], cap - n1, b);
  if (n2 < 0) { return -1; }
  return n1 + n2;
}
function format_2(buf: *u8, cap: i32, a: usize, b: usize): i32 {
  let n1: i32 = fmt.fmt_scalar_to_buf(buf, cap, a);
  if (n1 < 0) { return -1; }
  let n2: i32 = fmt.fmt_scalar_to_buf(&buf[n1], cap - n1, b);
  if (n2 < 0) { return -1; }
  return n1 + n2;
}
function format_2(buf: *u8, cap: i32, a: isize, b: i32): i32 {
  let n1: i32 = fmt.fmt_scalar_to_buf(buf, cap, a);
  if (n1 < 0) { return -1; }
  let n2: i32 = fmt.fmt_scalar_to_buf(&buf[n1], cap - n1, b);
  if (n2 < 0) { return -1; }
  return n1 + n2;
}
function format_2(buf: *u8, cap: i32, a: i32, b: usize): i32 {
  let n1: i32 = fmt.fmt_scalar_to_buf(buf, cap, a);
  if (n1 < 0) { return -1; }
  let n2: i32 = fmt.fmt_scalar_to_buf(&buf[n1], cap - n1, b);
  if (n2 < 0) { return -1; }
  return n1 + n2;
}
function format_2(buf: *u8, cap: i32, p: *u8, v: i32): i32 {
  let n1: i32 = fmt.fmt_ptr_to_buf(buf, cap, p);
  if (n1 < 0) { return -1; }
  let n2: i32 = fmt.fmt_i32_to_buf(&buf[n1], cap - n1, v);
  if (n2 < 0) { return -1; }
  return n1 + n2;
}
function format_2(buf: *u8, cap: i32, a: bool, b: bool): i32 {
  let n1: i32 = fmt.fmt_bool_to_buf(buf, cap, a);
  if (n1 < 0) { return -1; }
  let n2: i32 = fmt.fmt_bool_to_buf(&buf[n1], cap - n1, b);
  if (n2 < 0) { return -1; }
  return n1 + n2;
}
function format_2(buf: *u8, cap: i32, x: f64, v: i32): i32 {
  let n1: i32 = fmt.fmt_f64_to_buf(buf, cap, x);
  if (n1 < 0) { return -1; }
  let n2: i32 = fmt.fmt_i32_to_buf(&buf[n1], cap - n1, v);
  if (n2 < 0) { return -1; }
  return n1 + n2;
}

function format_3(buf: *u8, cap: i32, a: i32, b: i32, c: i32): i32 {
  let n1: i32 = fmt.fmt_scalar_to_buf(buf, cap, a);
  if (n1 < 0) { return -1; }
  let n2: i32 = fmt.fmt_scalar_to_buf(&buf[n1], cap - n1, b);
  if (n2 < 0) { return -1; }
  let n3: i32 = fmt.fmt_scalar_to_buf(&buf[n1 + n2], cap - n1 - n2, c);
  if (n3 < 0) { return -1; }
  return n1 + n2 + n3;
}
function format_3(buf: *u8, cap: i32, a: i32, b: u32, c: usize): i32 {
  let n1: i32 = fmt.fmt_scalar_to_buf(buf, cap, a);
  if (n1 < 0) { return -1; }
  let n2: i32 = fmt.fmt_scalar_to_buf(&buf[n1], cap - n1, b);
  if (n2 < 0) { return -1; }
  let n3: i32 = fmt.fmt_scalar_to_buf(&buf[n1 + n2], cap - n1 - n2, c);
  if (n3 < 0) { return -1; }
  return n1 + n2 + n3;
}

/** 重导出 core.fmt 指针格式化。 */
function fmt_ptr_to_buf(buf: *u8, cap: i32, p: *u8): i32 {
  return fmt.fmt_ptr_to_buf(buf, cap, p);
}
