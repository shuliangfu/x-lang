// std/async/scheduler.sx — F-async v1 调度锚点（协作帧/MPSC/CPS 在 scheduler_glue.c）
//
// 【文件职责】
// 提供 F-async v1 可编译 .sx 单元；经 ld -r 与 scheduler_glue.c 合并为 scheduler.o。
// 对外 API 在 mod.sx。

/** F-async v1 调度版本标记；供聚合 gate 校验 scheduler.sx 已参与 ld -r 合并。 */
function scheduler_f_async_scheduler_v1_marker_c(): i32 {
  return 1;
}
