# std.db — DEPRECATED

> **请改用 `import("std.sqlite")`**。本目录为 STD-120 迁移兼容层，API 全部转发至 `std/sqlite/`。

- 文档：`analysis/std-db-compat-v1.md`
- 门禁：`./tests/run-std-db-compat-gate.sh`
- 烟测：`tests/std-db/compat_smoke.su`
