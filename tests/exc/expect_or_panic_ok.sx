// tests/exc/expect_or_panic_ok.sx — EXC-002：expect_i32_or_panic 仅用于「必成功」路径
const result = import("core.result");

function main(): i32 {
  let r: Result_i32 = result.ok_i32(42);
  let v: i32 = result.expect_i32_or_panic(r);
  if (v != 42) { return 1; }
  return 0;
}
