const bad = nope("x")

/** Internal function `broken`.
 * Implements `broken`.
 * @return i32
 */
function broken(): i32

/** Internal function `ok_after`.
 * Implements `ok_after`.
 * @return i32
 */
function ok_after(): i32 {
  return 1;
}
