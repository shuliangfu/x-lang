// run_async_type_err.sx — run v3：实参类型与形参不一致应被拒绝
async function id_u32(x: u32): i32 {
  let v: u32 = await x;
  return v as i32;
}

function main(): i32 {
  let bad: i32 = 1;
  return run id_u32(bad);
}
