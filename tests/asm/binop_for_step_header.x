// main: see function docblock below.
/** Internal function `main`.
 * Program/test entry point.
 * @return i32
 */
function main(): i32 {
  let a: i32 = 0;
  let b: i32 = 2;
  for ( ; a < 1 ; a += 1) {
    /* See implementation. */
  }
  return a + b;
}
