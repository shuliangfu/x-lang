// 7.3：线性块四槽同活（|live|>2 触发清 cache）；return a+b+c+d → 10（block-var）。
function main(): i32 {
  let a: i32 = 1;
  let b: i32 = 2;
  let c: i32 = 3;
  let d: i32 = 4;
  let t: i32 = a + b;
  let u: i32 = t + c;
  return u + d;
}
