// 7.3：cmp 左 INDEX 已在 rbx、右 binop slow emit 时须 x2 暂存 rbx，勿覆盖左操作数（run-asm-cmp-index-binop.sh）。
function main(): i32 {
  let arr: [3]i32 = [5, 10, 15];
  if (arr[1] < arr[0] + arr[1]) {
    return 1;
  }
  return 0;
}
