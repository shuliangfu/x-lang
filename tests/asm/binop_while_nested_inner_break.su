// 7.3：内层 while break 仅退出内层；外层继续 a+=；return a+b → 4（cfg-merge）。
function main(): i32 {
  let a: i32 = 0;
  let b: i32 = 2;
  let c: i32 = a + b;
  while (a < 2) {
    while (a < 1) {
      a += 1;
      break;
    }
    a += 1;
  }
  return a + b;
}
