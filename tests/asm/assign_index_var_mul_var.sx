// 7.3：arr[i*j]=value 双 VAR MUL 下标 assign scratch（run-asm-assign-index-expr.sh）。
function main(): i32 {
  let arr: [4]i32 = [10, 20, 30, 40];
  let i: i32 = 2;
  let j: i32 = 1;
  arr[i * j] = 99;
  return arr[2];
}
