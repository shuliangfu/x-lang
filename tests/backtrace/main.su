// tests/backtrace/main.su — std.backtrace 回归：capture 至少返回 1 帧（main）。
// 不支持的平台 capture 返回 0，测试仍通过（跳过）。

const backtrace = import("std.backtrace");

function main(): i32 {
  let buf: [64]u8 = [];
  let n: i32 = backtrace.capture(&buf[0], 8);
  if (n <= 0) {
    return 0;
  }
  return 0;
}
