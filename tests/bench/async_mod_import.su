// async_mod_import.su — import std.async；runtime 按 nm 未定义符号按需链 scheduler.o
import std.async;

/** 烟测：coop_pingpong_jmp 经 mod.su 转发至 scheduler.c。 */
function main(): i32 {
  let total: i64 = coop_pingpong_jmp(1000);
  if (total != 2000) {
    return 1;
  }
  return 0;
}
