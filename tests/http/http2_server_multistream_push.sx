// STD-HTTP-H2-v19：h2c/h2 server 多 stream + push 组合 serve 烟测
const http = import("std.http");

function main(): i32 {
  if (http.http2_server_multistream_push_smoke() != 0) { return 1; }
  if (http.http2_server_multistream_push_is_available() == false) { return 2; }
  return 0;
}
