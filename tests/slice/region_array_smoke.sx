/**
 * ZC-3 运行时 smoke：region 块内 [N]T → []T 零拷贝视图 + 同域 slice 赋值。
 * arr[1]=20 → t[1] → exit 20。
 */
function main(): i32 {
  region ra {
    let arr: [4]i32 = [10, 20, 30, 40];
    let s: []i32 = arr;
    let t: []i32 = s;
    return t[1];
  }
  return 0;
}
