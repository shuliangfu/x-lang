// 测试绑定 import：函数经模块前缀调用（避免与本地同名符号冲突时用不同绑定名）。
const io = import("std.io");

function main(): i32 {
  let msg: [12]u8 = [72, 101, 108, 108, 111, 32, 87, 111, 114, 108, 100, 10];
  let n: i32 = io.print_str(&msg[0], 12);
  return if (n >= 0) { 0 } else { 1 };
}
