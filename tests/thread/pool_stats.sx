// STD-165：thread_pool_stats 烟测（未启动池时返回 -1）
const thread = import("std.thread");

function main(): i32 {
  let st: ThreadPoolStats = ThreadPoolStats { pending: 0, in_flight: 0, workers: 0 };
  if (thread.thread_pool_stats(&st) != -1) { return 1; }
  return 0;
}
