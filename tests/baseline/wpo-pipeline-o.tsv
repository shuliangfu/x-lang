# build_asm/pipeline_wpo.o WPO dogfood: runtime_pipeline_abi.x orch root (G.7).
# wave335+: pipeline.x is pure-extern (0 bodies) → empty emit; live = runtime_pipeline_abi.x
#   (pipeline_run_x_pipeline_impl + Cap faces). Tip abi-scale __text ~800KiB.
# Historical ≤12288B / dce_off 11588B was pre-leave pipeline.x-body era — do not
# restore those as hard FAIL (use soft size like main; STRICT_SIZE=1 escape).
# min_save soft: abi mega is not compressible to old proxy; symbol+reach stay hard.
pipeline_wpo_max_text_bytes	2097152
pipeline_wpo_min_save_bytes	0
pipeline_dce_off_text	1700000
