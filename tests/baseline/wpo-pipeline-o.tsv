# build_asm/pipeline_wpo.o WPO dogfood: runtime_pipeline_abi.x orch root (G.7).
# wave335+: pipeline.x is pure-extern (0 bodies) → empty emit; live = runtime_pipeline_abi.x
#   (pipeline_run_x_pipeline_impl + Cap faces).
# 2026-08-24 true compress: fix WPO emit_order OOB (n past ASM_WPO_MAX_FUNCS=1024)
#   that re-emitted ~971× T _strchr → tip Darwin __text ~37KiB (was ~814KiB fake).
# Soft max leaves headroom for EMIT_HEAVY growth; STRICT_SIZE=1 hard-fails over max.
# Historical ≤12288B was pre-leave pipeline.x-body era.
pipeline_wpo_max_text_bytes	98304
pipeline_wpo_min_save_bytes	0
pipeline_dce_off_text	40000
