// boundary/json_invalid.su — 非法 JSON 须解析失败（退出码 1）
import std.json;

function main(): i32 {
  let consumed: i32 = 0;
  let bad: [4]u8 = [123, 125, 125, 0];
  if (parse_null(&bad[0], 3, &consumed) != 0) {
    return 0;
  }
  return 1;
}
