// dead_fn.sx — WPO-S1 烟测：dead_helper 不可达，used_helper 从 main 可达
/** 不可达死函数：wpo_dce 应标记为 dead。 */
function dead_helper(): i32 {
  return 42;
}

/** 从 main 可达的辅助函数。 */
function used_helper(): i32 {
  return 1;
}

function main(): i32 {
  return used_helper();
}
