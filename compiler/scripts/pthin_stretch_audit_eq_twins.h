/* AUTO-GENERATED by sync_stretch_audit_harness.py — do not edit.
 * Reference twins for every migrated audit; refreshed each wave. */

/* ── helper authority copies (verbatim from their slices; refresh together) ── */
extern void parser_asm_lex_from_result_val_into(struct parser_asm_lexer *out, struct parser_asm_lexer_result r);
/* v5.36: skip stubs live after suite helpers; diag_after_imports calls these. */
struct parser_asm_lexer parser_asm_skip_one_struct_slice_c(struct parser_asm_lexer lex,
                                                           struct parser_asm_slice_u8 *source);
struct parser_asm_lexer parser_asm_skip_imports_slice_c(struct parser_asm_lexer lex,
                                                        struct parser_asm_slice_u8 *source);
void parser_asm_lex_from_result_val_into(struct parser_asm_lexer *out, struct parser_asm_lexer_result r) {
  if (!out)
    return;
  out->pos = r.next_lex.pos;
  out->line = r.next_lex.line;
  out->col = r.next_lex.col;
}
int32_t parser_asm_stretch_is_type_start_kind_c(int32_t kind) {
  return kind == (int32_t)TOKEN_I32 || kind == (int32_t)TOKEN_I64 || kind == (int32_t)TOKEN_BOOL
      || kind == (int32_t)TOKEN_U8 || kind == (int32_t)TOKEN_U32 || kind == (int32_t)TOKEN_U64
      || kind == (int32_t)TOKEN_USIZE || kind == (int32_t)TOKEN_VOID || kind == (int32_t)TOKEN_IDENT;
}

static int32_t parser_asm_stretch_ident_byte_ok_c(uint8_t c, int32_t is_first) {
  int alpha = (c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z') || c == '_';
  if (is_first != 0)
    return alpha ? 1 : 0;
  return alpha || (c >= '0' && c <= '9') ? 1 : 0;
}
int32_t parser_asm_stretch_bind_name_validate_c(const uint8_t *name, int32_t len) {
  int32_t i;
  if (!name || len <= 0 || len > 63)
    return 0;
  if (parser_asm_stretch_ident_byte_ok_c(name[0], 1) == 0)
    return 0;
  for (i = 1; i < len; i++) {
    if (parser_asm_stretch_ident_byte_ok_c(name[i], 0) == 0)
      return 0;
  }
  return 1;
}
int32_t parser_asm_is_compound_assign_token_c(int32_t kind) {
  return kind == (int32_t)TOKEN_PLUS_EQ || kind == (int32_t)TOKEN_MINUS_EQ || kind == (int32_t)TOKEN_STAR_EQ
      || kind == (int32_t)TOKEN_SLASH_EQ || kind == (int32_t)TOKEN_PERCENT_EQ || kind == (int32_t)TOKEN_AMP_EQ
      || kind == (int32_t)TOKEN_PIPE_EQ || kind == (int32_t)TOKEN_CARET_EQ || kind == (int32_t)TOKEN_LSHIFT_EQ
      || kind == (int32_t)TOKEN_RSHIFT_EQ;
}
/* Top-level coarse classification codes (suite enum; needed by classify helper). */
enum {
  STRETCH_TOP_UNKNOWN = 0,
  STRETCH_TOP_IMPORT = 1,
  STRETCH_TOP_CONST_BIND = 2,
  STRETCH_TOP_FUNCTION = 3,
  STRETCH_TOP_STRUCT = 4,
  STRETCH_TOP_ENUM = 5,
  STRETCH_TOP_EXTERN = 6,
  STRETCH_TOP_LET = 7,
  STRETCH_TOP_TRAIT = 8,
  STRETCH_TOP_IMPL = 9
};
/* Stretch token aliases used by field-kind classifiers (≡ heavy_stretch_slice). */
enum {
  STRETCH_TOKEN_IDENT = 1,
  STRETCH_TOKEN_ALIGN = 33
};
/* v4.8: kind classifiers + thin bind audits (single authority = bind_name_validate). */
int32_t parser_asm_stretch_struct_field_name_kind_c(int32_t kind) {
  if (kind == STRETCH_TOKEN_IDENT)
    return 1;
  if (kind == 17) /* TOKEN_PACKED legacy */
    return 1;
  if (kind == 18) /* TOKEN_SOA legacy */
    return 1;
  if (kind == (int32_t)TOKEN_TYPE)
    return 1;
  if (kind == (int32_t)TOKEN_PACKED)
    return 1;
  if (kind == (int32_t)TOKEN_SOA)
    return 1;
  if (kind == STRETCH_TOKEN_ALIGN)
    return 1;
  return 0;
}
int32_t parser_asm_stretch_struct_field_continues_kind_c(int32_t kind) {
  return parser_asm_stretch_struct_field_name_kind_c(kind) != 0 || kind == STRETCH_TOKEN_ALIGN;
}
int32_t parser_asm_stretch_struct_field_bind_audit_c(struct parser_asm_slice_u8 *source, size_t token_start,
                                                     int32_t name_len) {
  if (!source || name_len <= 0)
    return 0;
  return parser_asm_stretch_bind_name_validate_c(source->data + token_start, name_len);
}
int32_t parser_asm_stretch_enum_variant_bind_audit_c(struct parser_asm_slice_u8 *source, size_t token_start,
                                                     int32_t name_len) {
  if (!source || name_len <= 0)
    return 0;
  return parser_asm_stretch_bind_name_validate_c(source->data + token_start, name_len);
}
int32_t parser_asm_stretch_enum_discriminant_kind_audit_c(int32_t kind) {
  return kind == (int32_t)TOKEN_I32 || kind == (int32_t)TOKEN_I64 || kind == (int32_t)TOKEN_INT ? 1 : 0;
}
/* v4.9: function_name_audit is a thin wrap of bind_name_validate (G.7). */
int32_t parser_asm_stretch_function_name_audit_c(const uint8_t *name, int32_t name_len) {
  return parser_asm_stretch_bind_name_validate_c(name, name_len);
}
/* v5.7: struct_layout_name_audit is the same thin wrap (G.7). */
int32_t parser_asm_stretch_struct_layout_name_audit_c(const uint8_t *name, int32_t name_len) {
  return parser_asm_stretch_bind_name_validate_c(name, name_len);
}
/* v5.8: loop_stmt_body is a real flag3 .x port; c_ref twin comes from the
 * gated suite body via sync (no harness stub — stub returned 0 and would
 * diverge on score+= callers unlocked this wave). */
/* v5.9: import_select_list is a real flag3 inout .x port; c_ref twin comes
 * from the gated suite body via sync. item_bind stays suite-local thin wrap
 * of bind_name_validate (G.7). path_validate copy mirrors pthin_stretch.x
 * (product authority) so audit_x.o UNDEF resolves in this TU. */
/* v5.10: diag_lex_after_imports(+buf) are real .x ports (ABI-widened no-lex
 * roots); c_ref twins come from the gated suite bodies via sync. Fresh
 * lexer_init copy matches foundation authority (pos=0,line=1,col=1) so the
 * diag_lex c_ref twin can call it without pulling pthin_foundation.o. */
struct parser_asm_lexer parser_asm_lexer_init_c(void) {
  struct parser_asm_lexer lex;
  lex.pos = 0;
  lex.line = 1;
  lex.col = 1;
  return lex;
}
/* v5.32: diag_parse_one_mega(+full_deep chain) are real .x ports (ABI-widened
 * no-lex roots). Still-C (data,len) leaves they call are provided after c_ref
 * fwds as HARNESS_MEGA_STILL_C (product suite remains authority). */
int32_t parser_asm_stretch_import_select_item_bind_audit_c(struct parser_asm_slice_u8 *source, size_t token_start,
                                                           int32_t name_len) {
  if (!source || name_len <= 0)
    return 0;
  return parser_asm_stretch_bind_name_validate_c(source->data + token_start, name_len);
}
int32_t parser_asm_stretch_import_path_validate_c(const uint8_t *path, int32_t path_len) {
  int32_t i;
  if (!path || path_len <= 0)
    return 0;
  if (path_len > 63)
    path_len = 63;
  for (i = 0; i < path_len; i++) {
    uint8_t c = path[i];
    if (c == 0)
      break;
    if (c == (uint8_t)'.')
      continue;
    if (parser_asm_stretch_ident_byte_ok_c(c, 0) == 0)
      return 0;
  }
  return 1;
}

/* Forward decls so twins / advance_to helpers may call each other. */
static int32_t c_ref_fn_param_list_audit(void *lex_inout, void *source);
static int32_t c_ref_skip_return_type_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_sig_audit(void *lex_inout, void *source);
static int32_t c_ref_import_select_list_audit(void *lex_inout, void *source, int32_t max_names);
static int32_t c_ref_struct_fields_probe(void *lex_inout, void *source, int32_t *out_field_count);
static int32_t c_ref_enum_variants_probe(void *lex_inout, void *source, int32_t *out_variant_count);
static int32_t c_ref_trait_methods_probe(void *lex_inout, void *source, int32_t *out_method_count);
static int32_t c_ref_impl_items_probe(void *lex_inout, void *source, int32_t *out_item_count);
static int32_t c_ref_extern_fn_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_modifiers_audit(void *lex_inout, void *source);
static int32_t c_ref_async_fn_prefix_audit(void *lex_inout, void *source);
static int32_t c_ref_match_arms_probe(void *lex_inout, void *source, int32_t *out_arm_count);
static int32_t c_ref_block_stmt_kind_probe(void *lex_inout, void *source, int32_t *out_stmt_score);
static int32_t c_ref_if_header_audit(void *lex_inout, void *source);
static int32_t c_ref_loop_header_audit(void *lex_inout, void *source, int32_t expect_while);
static int32_t c_ref_let_const_decl_audit(void *lex_inout, void *source);
static int32_t c_ref_import_stmt_audit(void *lex_inout, void *source);
static int32_t c_ref_enum_header_audit(void *lex_inout, void *source);
static int32_t c_ref_extern_param_count_audit(void *lex_inout, void *source, int32_t *out_param_count);
static int32_t c_ref_struct_header_audit(void *lex_inout, void *source);
static int32_t c_ref_trait_header_audit(void *lex_inout, void *source);
static int32_t c_ref_impl_header_audit(void *lex_inout, void *source);
static int32_t c_ref_function_header_audit(void *lex_inout, void *source);
static int32_t c_ref_match_kw_audit(void *lex_inout, void *source);
static int32_t c_ref_else_if_chain_audit(void *lex_inout, void *source);
static int32_t c_ref_type_ref_peek_audit(void *lex_inout, void *source);
static int32_t c_ref_toplevel_kind_peek_audit(void *lex_inout, void *source);
static int32_t c_ref_return_stmt_audit(void *lex_inout, void *source);
static int32_t c_ref_break_continue_audit(void *lex_inout, void *source, int32_t want_break);
static int32_t c_ref_else_stmt_audit(void *lex_inout, void *source);
static int32_t c_ref_cond_int_as_audit(void *lex_inout, void *source);
static int32_t c_ref_let_in_block_audit(void *lex_inout, void *source);
static int32_t c_ref_label_stmt_audit(void *lex_inout, void *source);
static int32_t c_ref_assign_stmt_audit(void *lex_inout, void *source);
static int32_t c_ref_library_fn_shape_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_head_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_mul_binop_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_addsub_binop_audit(void *lex_inout, void *source);
static int32_t c_ref_panic_kw_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_shift_binop_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_rel_binop_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_eq_binop_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_bitand_binop_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_bitxor_binop_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_bitor_binop_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_logand_binop_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_logor_binop_audit(void *lex_inout, void *source);
static int32_t c_ref_as_suffix_chain_audit(void *lex_inout, void *source);
static int32_t c_ref_unary_prefix_audit(void *lex_inout, void *source);
static int32_t c_ref_if_expr_branch_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_lit_fields_probe(void *lex_inout, void *source, int32_t *out_field_count);
static int32_t c_ref_array_lit_head_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_suffix_chain_probe(void *lex_inout, void *source);
static int32_t c_ref_ternary_op_audit(void *lex_inout, void *source);
static int32_t c_ref_assign_op_audit(void *lex_inout, void *source);
static int32_t c_ref_if_stmt_branch_audit(void *lex_inout, void *source);
static int32_t c_ref_type_ref_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_call_args_shape_probe(void *lex_inout, void *source, int32_t *out_arg_count);
static int32_t c_ref_diag_fn_header_audit(void *lex_inout, void *source);
static int32_t c_ref_paren_expr_head_audit(void *lex_inout, void *source);
static int32_t c_ref_diag_toplevel_after_imports_audit(void *lex_inout, void *source);
static int32_t c_ref_diag_fn_param_sig_audit(void *lex_inout, void *source);
static int32_t c_ref_allow_kw_paren_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_allow_paren_audit(void *lex_inout, void *source);
static int32_t c_ref_linear_type_audit(void *lex_inout, void *source);
static int32_t c_ref_array_type_bracket_audit(void *lex_inout, void *source);
static int32_t c_ref_slice_type_bracket_audit(void *lex_inout, void *source);
static int32_t c_ref_diag_after_collect_preamble_audit(void *lex_inout, void *source);
static int32_t c_ref_balanced_parens_depth_probe(void *lex_inout, void *source);
static int32_t c_ref_diag_fn_return_type_audit(void *lex_inout, void *source);
static int32_t c_ref_library_return_type_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_align_paren_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_let_const_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_method_return_audit(void *lex_inout, void *source);
static int32_t c_ref_impl_type_for_trait_audit(void *lex_inout, void *source);
static int32_t c_ref_extern_return_type_audit(void *lex_inout, void *source);
static int32_t c_ref_diag_skip_let_const_type_audit(void *lex_inout, void *source);
static int32_t c_ref_diag_skip_let_const_type_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_impl_fn_return_audit(void *lex_inout, void *source);
static int32_t c_ref_trait_method_paren_audit(void *lex_inout, void *source);
static int32_t c_ref_balanced_braces_depth_probe(void *lex_inout, void *source);
static int32_t c_ref_balanced_braces_depth_probe_buf(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_skip_one_trait_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_skip_one_impl_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_one_extern_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_balanced_parens_depth_probe_buf(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_skip_one_struct_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_skip_one_enum_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_collect_imports_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_skip_one_if_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_skip_one_extern_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_allow_paren_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_allow_kw_paren_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_lex_after_imports_audit(void *lex_inout, void *source);
static int32_t c_ref_diag_lex_after_imports_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_skip_one_enum_register_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_skip_imports_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_enum_variants_body_audit(void *lex_inout, void *source);
static int32_t c_ref_enum_variants_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_peek_function_name_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_let_const_type_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_fields_body_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_fields_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_enum_variants_body_from_header_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_after_imports_then_structs_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_buf_preamble_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_methods_body_audit(void *lex_inout, void *source);
static int32_t c_ref_trait_methods_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_impl_items_body_audit(void *lex_inout, void *source);
static int32_t c_ref_impl_items_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_record_layout_body_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_record_layout_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_fail_at_token_kind_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_skip_one_function_full_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_if_stmt_body_audit(void *lex_inout, void *source);
static int32_t c_ref_if_stmt_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_function_body_block_stmt_audit(void *lex_inout, void *source);
static int32_t c_ref_function_body_block_stmt_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_fn_shape_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_extern_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_arms_body_audit(void *lex_inout, void *source);
static int32_t c_ref_match_arms_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_let_const_then_if_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_scan_audit(void *lex_inout, void *source);
static int32_t c_ref_collect_imports_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_token_after_collect_imports_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_one_function_buf_deep_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_return_type_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_scan_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_skip_one_if_else_chain_audit(void *lex_inout, void *source);
static int32_t c_ref_skip_one_if_else_chain_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_let_const_then_if_audit(void *lex_inout, void *source);
static int32_t c_ref_type_ref_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_skip_one_extern_body_audit(void *lex_inout, void *source);
static int32_t c_ref_skip_one_extern_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_buf_loop_toplevel_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_async_fn_prefix_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_cond_int_as_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_head_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_lit_fields_body_audit(void *lex_inout, void *source);
static int32_t c_ref_peek_kind_chain_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_parse_one_after_collect_imports_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_first_token_kind_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_one_function_ok_for_pipeline_audit(void *lex_inout, void *source);
static int32_t c_ref_parse_one_function_ok_for_pipeline_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_cond_expr_audit(void *lex_inout, void *source);
static int32_t c_ref_parse_cond_expr_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_let_bracket_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_loop_stmt_body_audit(void *lex_inout, void *source, int32_t expect_while);
static int32_t c_ref_parse_into_buf_preamble_peek_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_select_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_expr_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_balanced_brackets_depth_probe(void *lex_inout, void *source);
static int32_t c_ref_balanced_brackets_depth_probe_buf(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_expr_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_subject_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_subject_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_onefunc_buf_deep_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_expr_prefix_chain_audit(void *lex_inout, void *source);
static int32_t c_ref_parse_expr_prefix_chain_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_if_body_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_if_body_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_one_function_ok_pipeline_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_type_ref_bracket_composite_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_skip_one_if_core_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_skip_one_if_core_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_skip_one_function_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_parse_one_collect_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_function_branch_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_ternary_assign_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_ternary_assign_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_binop_lower_chain_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_binop_lower_chain_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_impl_header_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_enum_body_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_loop_stmt_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_binop_upper_chain_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_binop_upper_chain_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_binop_mid_chain_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_binop_mid_chain_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_if_expr_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_skip_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_extern_skip_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_skip_let_const_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_diag_skip_let_const_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_impl_body_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_preamble_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_block_stmt_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_block_stmt_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_binop_full_chain_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_binop_full_chain_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_if_stmt_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_allow_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_allow_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_after_imports_structs_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_loop_iter_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_scan_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_scan_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_onefunc_slice_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_call_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_call_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_enum_body_deep_slice_audit(void *lex_inout, void *source);
static int32_t c_ref_collect_imports_post_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_fn_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_diag_fn_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_impl_type_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_trait_impl_type_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_layout_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_layout_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_skip_imports_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_stmt_full_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_stmt_full_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_block_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_block_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_parse_one_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_let_const_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_let_const_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_preamble_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_if_stmt_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_if_expr_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_cond_expr_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_parse_cond_expr_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_simd_builtin_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_simd_builtin_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_lit_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_lit_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_extern_skip_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_enum_skip_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_onefunc_buf_full_deep_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_scan_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_loop_stmt_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_collect_imports_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_type_ref_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_block_stmt_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_ternary_assign_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_ternary_assign_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_expr_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_expr_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_impl_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_skip_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_stmt_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_stmt_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_entry_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_subject_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_subject_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_async_fn_sig_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_async_fn_sig_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_cast_unary_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_cast_unary_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_return_stmt_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_return_stmt_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_skip_let_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_diag_skip_let_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_onefunc_slice_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_onefunc_slice_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_loop_entry_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_balanced_delim_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_balanced_delim_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_skip_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_if_else_chain_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_if_else_chain_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_sig_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_sig_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_arms_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_arms_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_path_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_path_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_struct_layout_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_parse_struct_layout_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_parse_one_mega_full_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_binop_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_binop_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_fn_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_fn_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_allow_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_allow_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_extern_skip_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_impl_skip_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_enum_skip_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_collect_imports_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_collect_imports_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_if_control_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_if_control_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_type_ref_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_type_ref_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_onefunc_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_onefunc_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_skip_one_function_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_fn_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_block_stmt_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_block_stmt_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_skip_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_skip_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_skip_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_path_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_path_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_allow_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_allow_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_struct_layout_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_parse_struct_layout_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_balanced_delim_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_balanced_delim_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_async_fn_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_async_fn_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_parse_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_loop_control_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_stmt_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_stmt_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_skip_imports_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_skip_imports_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_control_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_control_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_ternary_assign_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_ternary_assign_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_expr_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_expr_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_stmt_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_stmt_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_entry_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_subject_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_subject_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_cast_unary_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_cast_unary_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_stmt_control_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_stmt_control_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_skip_let_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_diag_skip_let_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_onefunc_slice_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_onefunc_slice_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_if_else_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_if_else_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_sig_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_sig_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_arms_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_arms_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_impl_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_preamble_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_ultra_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_ultra_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_ultra_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_ultra_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_ultra_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_ultra_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_ultra_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_ultra2_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_ultra_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_ultra_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_ultra_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_super_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_super_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_super_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_max_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_super_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_super_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_super_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_super_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_super_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_super_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_super_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_control_flow_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_diag_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_expr_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_expr_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_primary_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_primary_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_import_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_import_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_parse_into_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_match_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_match_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_struct_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_struct_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_trait_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_fn_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_control_flow_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_diag_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_body_skip_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_body_skip_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_library_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_library_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_try_skip_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);
static int32_t c_ref_try_skip_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len);
static int32_t c_ref_fn_multiversal_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source);

static int32_t parser_asm_stretch_expr_binop_kinds_probe_c(struct parser_asm_lexer lex,
                                                             struct parser_asm_slice_u8 *source,
                                                             const int32_t *kinds, int32_t num_kinds) {
  struct parser_asm_lexer_result r;
  int32_t n;
  int32_t i;
  int32_t hit;
  if (!source || !kinds || num_kinds <= 0)
    return 0;
  n = 0;
  lexer_next_into(&r, lex, source);
  for (;;) {
    hit = 0;
    for (i = 0; i < num_kinds; i++) {
      if (r.tok.kind == kinds[i]) {
        hit = 1;
        break;
      }
    }
    if (!hit)
      return n;
    n++;
    if (n > 32)
      return n;
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, source);
  }
}

void parser_asm_skip_balanced_parens_into_slice_c(struct parser_asm_lexer *out, struct parser_asm_lexer lex,
                                                  struct parser_asm_slice_u8 *source) {
  int32_t depth = 1;
  struct parser_asm_lexer_result r;
  if (!out || !source)
    return;
  (void)0;
  (void)0;
  (void)0;
  while (depth > 0) {
    lexer_next_into(&r, lex, source);
    if (r.tok.kind == (int32_t)TOKEN_LPAREN)
      depth++;
    else if (r.tok.kind == (int32_t)TOKEN_RPAREN) {
      depth--;
      if (depth == 0) {
        out->pos = r.next_lex.pos;
        out->line = r.next_lex.line;
        out->col = r.next_lex.col;
        return;
      }
    }
    if (r.tok.kind == (int32_t)TOKEN_EOF) {
      out->pos = lex.pos;
      out->line = lex.line;
      out->col = lex.col;
      return;
    }
    parser_asm_lex_from_result_val_into(&lex, r);
  }
  out->pos = lex.pos;
  out->line = lex.line;
  out->col = lex.col;
}

void parser_asm_skip_balanced_braces_into_slice_c(struct parser_asm_lexer *out, struct parser_asm_lexer lex,
                                                  struct parser_asm_slice_u8 *source) {
  int32_t depth = 1;
  struct parser_asm_lexer_result r;
  if (!out || !source)
    return;
  (void)0;
  (void)0;
  (void)0;
  while (depth > 0) {
    lexer_next_into(&r, lex, source);
    if (r.tok.kind == (int32_t)TOKEN_LBRACE)
      depth++;
    else if (r.tok.kind == (int32_t)TOKEN_RBRACE) {
      depth--;
      if (depth == 0) {
        out->pos = r.next_lex.pos;
        out->line = r.next_lex.line;
        out->col = r.next_lex.col;
        return;
      }
    }
    if (r.tok.kind == (int32_t)TOKEN_EOF) {
      out->pos = lex.pos;
      out->line = lex.line;
      out->col = lex.col;
      return;
    }
    parser_asm_lex_from_result_val_into(&lex, r);
  }
  out->pos = lex.pos;
  out->line = lex.line;
  out->col = lex.col;
}

void parser_asm_stretch_skip_balanced_brackets_into_c(struct parser_asm_lexer *out, struct parser_asm_lexer lex,
                                                             struct parser_asm_slice_u8 *source) {
  int32_t depth;
  struct parser_asm_lexer_result r;
  if (!out || !source)
    return;
  depth = 1;
  while (depth > 0) {
    lexer_next_into(&r, lex, source);
    if (r.tok.kind == (int32_t)TOKEN_LBRACKET)
      depth++;
    else if (r.tok.kind == (int32_t)TOKEN_RBRACKET) {
      depth--;
      if (depth == 0) {
        out->pos = r.next_lex.pos;
        out->line = r.next_lex.line;
        out->col = r.next_lex.col;
        return;
      }
    }
    if (r.tok.kind == (int32_t)TOKEN_EOF) {
      out->pos = lex.pos;
      out->line = lex.line;
      out->col = lex.col;
      return;
    }
    parser_asm_lex_from_result_val_into(&lex, r);
  }
  out->pos = lex.pos;
  out->line = lex.line;
  out->col = lex.col;
}

struct parser_asm_lexer parser_asm_stretch_skip_type_suffix_c(struct parser_asm_lexer lex,
                                                                     struct parser_asm_slice_u8 *source) {
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer after;
  int32_t guard;
  guard = 0;
  for (;;) {
    if (guard++ > 64)
      break;
    lexer_next_into(&r, lex, source);
    if (r.tok.kind == (int32_t)TOKEN_STAR) {
      lex = r.next_lex;
      continue;
    }
    if (r.tok.kind == (int32_t)TOKEN_LBRACKET) {
      parser_asm_stretch_skip_balanced_brackets_into_c(&after, r.next_lex, source);
      lex = after;
      continue;
    }
    break;
  }
  return lex;
}

struct parser_asm_lexer parser_asm_stretch_skip_one_param_type_c(struct parser_asm_lexer lex,
                                                                        struct parser_asm_slice_u8 *source) {
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, source);
  if (r.tok.kind == (int32_t)TOKEN_STAR) {
    lex = parser_asm_stretch_skip_type_suffix_c(r.next_lex, source);
    return lex;
  }
  if (!parser_asm_stretch_is_type_start_kind_c(r.tok.kind))
    return lex;
  lex = parser_asm_stretch_skip_type_suffix_c(r.next_lex, source);
  return lex;
}

int32_t parser_asm_stretch_classify_toplevel_c(int32_t kind, int32_t next_kind, int32_t third_kind) {
  if (kind == (int32_t)TOKEN_IMPORT)
    return STRETCH_TOP_IMPORT;
  if (kind == (int32_t)TOKEN_FUNCTION)
    return STRETCH_TOP_FUNCTION;
  if (kind == (int32_t)TOKEN_STRUCT)
    return STRETCH_TOP_STRUCT;
  if (kind == (int32_t)TOKEN_ENUM)
    return STRETCH_TOP_ENUM;
  if (kind == (int32_t)TOKEN_EXTERN)
    return STRETCH_TOP_EXTERN;
  if (kind == (int32_t)TOKEN_LET)
    return STRETCH_TOP_LET;
  if (kind == (int32_t)TOKEN_TRAIT)
    return STRETCH_TOP_TRAIT;
  if (kind == (int32_t)TOKEN_IMPL)
    return STRETCH_TOP_IMPL;
  if (kind == (int32_t)TOKEN_CONST && next_kind == (int32_t)TOKEN_IDENT && third_kind == (int32_t)TOKEN_ASSIGN)
    return STRETCH_TOP_CONST_BIND;
  return STRETCH_TOP_UNKNOWN;
}

int32_t parser_asm_stretch_peek_kind_chain_c(struct parser_asm_lexer lex, struct parser_asm_slice_u8 *source,
                                             int32_t *kinds, int32_t max_peek) {
  struct parser_asm_lexer_result r;
  int32_t n;
  int32_t i;
  if (!source || !kinds || max_peek <= 0)
    return 0;
  n = 0;
  lexer_next_into(&r, lex, source);
  kinds[n++] = r.tok.kind;
  for (i = 1; i < max_peek; i++) {
    if (r.tok.kind == (int32_t)TOKEN_EOF)
      break;
    lexer_next_into(&r, r.next_lex, source);
    kinds[n++] = r.tok.kind;
  }
  return n;
}

static int32_t parser_asm_stretch_struct_advance_to_body_lex_c(struct parser_asm_lexer lex,
                                                               struct parser_asm_slice_u8 *source,
                                                               struct parser_asm_lexer *out_body_lex) {
  struct parser_asm_lexer_result r;

  if (!source || !out_body_lex)
    return 0;
  (void)c_ref_struct_modifiers_audit(&lex, source);
  lexer_next_into(&r, lex, source);
  if (r.tok.kind != (int32_t)TOKEN_STRUCT)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, source);
  if (r.tok.kind != (int32_t)TOKEN_LBRACE)
    return 0;
  *out_body_lex = r.next_lex;
  return 1;
}

static int32_t parser_asm_stretch_enum_advance_to_body_lex_c(struct parser_asm_lexer lex,
                                                             struct parser_asm_slice_u8 *source,
                                                             struct parser_asm_lexer *out_body_lex) {
  struct parser_asm_lexer_result r;

  if (!source || !out_body_lex)
    return 0;
  (void)c_ref_enum_header_audit(&lex, source);
  lexer_next_into(&r, lex, source);
  if (r.tok.kind != (int32_t)TOKEN_ENUM)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, source);
  if (r.tok.kind != (int32_t)TOKEN_LBRACE)
    return 0;
  *out_body_lex = r.next_lex;
  return 1;
}

static int32_t parser_asm_stretch_trait_advance_to_body_lex_c(struct parser_asm_lexer lex,
                                                              struct parser_asm_slice_u8 *source,
                                                              struct parser_asm_lexer *out_body_lex) {
  struct parser_asm_lexer_result r;

  if (!source || !out_body_lex)
    return 0;
  (void)c_ref_trait_header_audit(&lex, source);
  lexer_next_into(&r, lex, source);
  if (r.tok.kind != (int32_t)TOKEN_TRAIT)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, source);
  if (r.tok.kind != (int32_t)TOKEN_LBRACE)
    return 0;
  *out_body_lex = r.next_lex;
  return 1;
}

static int32_t parser_asm_stretch_match_advance_to_arms_lex_c(struct parser_asm_lexer lex,
                                                               struct parser_asm_slice_u8 *source,
                                                               struct parser_asm_lexer *out_arms_lex) {
  struct parser_asm_lexer_result r;
  int32_t guard;

  if (!source || !out_arms_lex)
    return 0;
  (void)c_ref_match_kw_audit(&lex, source);
  lexer_next_into(&r, lex, source);
  if (r.tok.kind != (int32_t)TOKEN_MATCH)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  guard = 0;
  while (guard++ < 64) {
    lexer_next_into(&r, lex, source);
    if (r.tok.kind == (int32_t)TOKEN_LBRACE) {
      *out_arms_lex = r.next_lex;
      return 1;
    }
    if (r.tok.kind == (int32_t)TOKEN_EOF)
      return 0;
    parser_asm_lex_from_result_val_into(&lex, r);
  }
  return 0;
}

static int32_t parser_asm_stretch_impl_advance_to_body_lex_c(struct parser_asm_lexer lex,
                                                             struct parser_asm_slice_u8 *source,
                                                             struct parser_asm_lexer *out_body_lex) {
  struct parser_asm_lexer_result r;

  if (!source || !out_body_lex)
    return 0;
  (void)c_ref_impl_header_audit(&lex, source);
  lexer_next_into(&r, lex, source);
  if (r.tok.kind != (int32_t)TOKEN_IMPL)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, source);
  if (r.tok.kind == (int32_t)TOKEN_FOR) {
    (void)c_ref_impl_type_for_trait_audit(&lex, source);
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, source);
    if (r.tok.kind != (int32_t)TOKEN_IDENT)
      return 0;
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, source);
  }
  if (r.tok.kind != (int32_t)TOKEN_LBRACE)
    return 0;
  *out_body_lex = r.next_lex;
  return 1;
}

static int32_t parser_asm_stretch_function_advance_to_body_lex_c(struct parser_asm_lexer lex,
                                                                 struct parser_asm_slice_u8 *source,
                                                                 struct parser_asm_lexer *out_body_lex) {
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex_cur;

  if (!source || !out_body_lex)
    return 0;
  (void)c_ref_async_fn_prefix_audit(&lex, source);
  (void)c_ref_function_header_audit(&lex, source);
  if (!c_ref_fn_sig_audit(&lex, source))
    return 0;
  lexer_next_into(&r, lex, source);
  if (r.tok.kind != (int32_t)TOKEN_FUNCTION)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, source);
  if (r.tok.kind != (int32_t)TOKEN_LPAREN)
    return 0;
  lex_cur = lex;
  parser_asm_skip_balanced_parens_into_slice_c(&lex_cur, r.next_lex, source);
  lexer_next_into(&r, lex_cur, source);
  if (r.tok.kind != (int32_t)TOKEN_COLON)
    return 0;
  parser_asm_lex_from_result_val_into(&lex_cur, r);
  {
    struct parser_asm_lexer ret_audit = lex_cur;
    (void)c_ref_skip_return_type_audit(&ret_audit, source);
  }
  lexer_next_into(&r, lex_cur, source);
  while (r.tok.kind != (int32_t)TOKEN_LBRACE && r.tok.kind != (int32_t)TOKEN_EOF) {
    parser_asm_lex_from_result_val_into(&lex_cur, r);
    lexer_next_into(&r, lex_cur, source);
  }
  if (r.tok.kind != (int32_t)TOKEN_LBRACE)
    return 0;
  *out_body_lex = r.next_lex;
  return 1;
}

static int32_t parser_asm_stretch_if_advance_to_body_lex_c(struct parser_asm_lexer lex,
                                                           struct parser_asm_slice_u8 *source,
                                                           struct parser_asm_lexer *out_body_lex) {
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex_cur;

  if (!source || !out_body_lex)
    return 0;
  (void)c_ref_if_header_audit(&lex, source);
  lexer_next_into(&r, lex, source);
  if (r.tok.kind != (int32_t)TOKEN_IF)
    return 0;
  lexer_next_into(&r, r.next_lex, source);
  if (r.tok.kind != (int32_t)TOKEN_LPAREN)
    return 0;
  parser_asm_skip_balanced_parens_into_slice_c(&lex_cur, r.next_lex, source);
  *out_body_lex = lex_cur;
  return 1;
}

int32_t parser_asm_stretch_spawn_kw_audit_c(int32_t after_function_kind) {
  return after_function_kind == (int32_t)TOKEN_SPAWN ? 1 : 0;
}

int32_t parser_asm_stretch_match_subject_ident_audit_c(struct parser_asm_lexer_result r,
                                                       struct parser_asm_slice_u8 *source) {
  if (!source || r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0)
    return 0;
  return parser_asm_stretch_bind_name_validate_c(source->data + r.token_start, r.tok.ident_len);
}

int32_t parser_asm_stretch_simd_builtin_audit_c(struct parser_asm_lexer_result r_at,
                                                struct parser_asm_slice_u8 *source) {
  struct parser_asm_lexer_result r;
  int32_t nlen;
  size_t start;
  if (!source)
    return 0;
  if (r_at.tok.kind != (int32_t)TOKEN_AT)
    return 0;
  lexer_next_into(&r, r_at.next_lex, source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT)
    return 0;
  nlen = r.tok.ident_len;
  start = r.token_start;
  if (nlen == 7 && start + 6 < source->length && source->data[start] == 115 && source->data[start + 1] == 104
      && source->data[start + 2] == 117 && source->data[start + 3] == 102 && source->data[start + 4] == 102
      && source->data[start + 5] == 108 && source->data[start + 6] == 101)
    return 1;
  if (nlen == 6 && start + 5 < source->length && source->data[start] == 115 && source->data[start + 1] == 101
      && source->data[start + 2] == 108 && source->data[start + 3] == 101 && source->data[start + 4] == 99
      && source->data[start + 5] == 116)
    return 1;
  return 0;
}

int32_t parser_asm_stretch_vector_type_ident_audit_c(struct parser_asm_slice_u8 *source, size_t token_start,
                                                     int32_t nlen) {
  uint8_t name_buf[64];
  int32_t i;
  if (!source || nlen <= 0 || nlen > 63)
    return 0;
  for (i = 0; i < nlen && token_start + (size_t)i < source->length; i++)
    name_buf[i] = source->data[token_start + (size_t)i];
  name_buf[i < 63 ? i : 63] = 0;
  if (nlen == 5 && name_buf[0] == 105 && name_buf[1] == 51 && name_buf[2] == 120)
    return 1;
  if (nlen >= 5 && name_buf[0] == 86 && name_buf[1] == 101 && name_buf[2] == 99)
    return 1;
  return parser_asm_stretch_bind_name_validate_c(name_buf, nlen);
}

int32_t parser_asm_stretch_builtin_vec_token_audit_c(int32_t kind) {
  return kind == (int32_t)TOKEN_I32X4 || kind == (int32_t)TOKEN_I32X8 || kind == (int32_t)TOKEN_I32X16
         || kind == (int32_t)TOKEN_U32X4 || kind == (int32_t)TOKEN_U32X8 || kind == (int32_t)TOKEN_U32X16
         || kind == (int32_t)TOKEN_F32X4 ? 1 : 0;
}

int32_t parser_asm_stretch_simd_builtin_deep_from_at_audit_c(struct parser_asm_lexer_result r_at,
                                                             struct parser_asm_slice_u8 *source) {
  struct parser_asm_lexer_result r;
  int32_t score;

  if (!source || r_at.tok.kind != (int32_t)TOKEN_AT)
    return 0;
  score = parser_asm_stretch_simd_builtin_audit_c(r_at, source);
  lexer_next_into(&r, r_at.next_lex, source);
  if (r.tok.kind == (int32_t)TOKEN_IDENT)
    score += parser_asm_stretch_vector_type_ident_audit_c(source, r.token_start, r.tok.ident_len);
  score += c_ref_paren_expr_head_audit(&r.next_lex, source);
  score += parser_asm_stretch_builtin_vec_token_audit_c((int32_t)r.tok.kind);
  return score > 0 ? 1 : 0;
}

struct parser_asm_lexer_result parser_asm_diag_after_imports_then_structs_slice_c(
    struct parser_asm_lexer lex, struct parser_asm_slice_u8 *source) {
  struct parser_asm_lexer_result r;
  if (!source) {
    memset(&r, 0, sizeof(r));
    r.next_lex = lex;
    r.tok.kind = (int32_t)TOKEN_EOF;
    return r;
  }
  lexer_next_into(&r, lex, source);
  while (r.tok.kind == (int32_t)TOKEN_STRUCT) {
    (void)0;
    (void)0;
    lex = parser_asm_skip_one_struct_slice_c(lex, source);
    lexer_next_into(&r, lex, source);
  }
  (void)0;
  return r;
}

/* v5.6 harness stub — G.7 product authority remains suite skip_one_struct_slice. */
struct parser_asm_lexer parser_asm_skip_one_struct_slice_c(struct parser_asm_lexer lex,
                                                           struct parser_asm_slice_u8 *source) {
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer after;
  int32_t guard;
  if (!source)
    return lex;
  guard = 0;
  for (;;) {
    if (guard++ > 256)
      return lex;
    lexer_next_into(&r, lex, source);
    if (r.tok.kind == (int32_t)TOKEN_LBRACE) {
      parser_asm_skip_balanced_braces_into_slice_c(&after, r.next_lex, source);
      return after;
    }
    if (r.tok.kind == (int32_t)TOKEN_EOF)
      return lex;
    lex = r.next_lex;
  }
}

/* v5.6 harness stub — G.7 product authority remains suite skip_imports_slice. */
struct parser_asm_lexer parser_asm_skip_imports_slice_c(struct parser_asm_lexer lex,
                                                        struct parser_asm_slice_u8 *source) {
  struct parser_asm_lexer_result r;
  int32_t guard;
  if (!source)
    return lex;
  guard = 0;
  for (;;) {
    if (guard++ > 64)
      return lex;
    lexer_next_into(&r, lex, source);
    if (r.tok.kind != (int32_t)TOKEN_CONST)
      return lex;
    /* Consume a coarse "const … ;" span (eq corpus import shapes). */
    lex = r.next_lex;
    {
      int32_t g2 = 0;
      for (;;) {
        if (g2++ > 128)
          return lex;
        lexer_next_into(&r, lex, source);
        if (r.tok.kind == (int32_t)TOKEN_SEMICOLON) {
          lex = r.next_lex;
          break;
        }
        if (r.tok.kind == (int32_t)TOKEN_EOF)
          return lex;
        lex = r.next_lex;
      }
    }
  }
}

/* v5.36: mega_buf still-C wrapper → c_ref slice (pointer ABI). */
int32_t parser_asm_stretch_diag_fn_mega_full_deep_buf_audit_c(void *lex_inout, uint8_t *data, int32_t len) {
  struct parser_asm_lexer lex;
  struct parser_asm_slice_u8 sl;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_diag_fn_mega_full_deep_audit(&lex, &sl);
}
/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_param_list_audit_c. */
static int32_t c_ref_fn_param_list_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex;
  int32_t nparams;
  int32_t guard;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  nparams = 0;
  guard = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_RPAREN) {
    *(struct parser_asm_lexer *)lex_inout = r.next_lex;
    return 1;
  }
  for (;;) {
    if (guard++ > 128)
      return 0;
    if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0 || r.tok.ident_len > 63)
      return 0;
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind != (int32_t)TOKEN_COLON)
      return 0;
    lex = parser_asm_stretch_skip_one_param_type_c(r.next_lex, source);
    nparams++;
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind == (int32_t)TOKEN_RPAREN) {
      *(struct parser_asm_lexer *)lex_inout = r.next_lex;
      (void)nparams;
      return 1;
    }
    if (r.tok.kind != (int32_t)TOKEN_COMMA)
      return 0;
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  }

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_return_type_audit_c. */
static int32_t c_ref_skip_return_type_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex;
  int32_t guard;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  guard = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  while (r.tok.kind != (int32_t)TOKEN_LBRACE && r.tok.kind != (int32_t)TOKEN_EOF) {
    if (guard++ > 96)
      return 0;
    if (r.tok.kind == (int32_t)TOKEN_STAR) {
      lex = r.next_lex;
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      continue;
    }
    if (parser_asm_stretch_is_type_start_kind_c(r.tok.kind)) {
      lex = parser_asm_stretch_skip_type_suffix_c(r.next_lex, source);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      continue;
    }
    if (r.tok.kind == (int32_t)TOKEN_LBRACKET) {
      struct parser_asm_lexer after;
      parser_asm_stretch_skip_balanced_brackets_into_c(&after, r.next_lex, source);
      lex = after;
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      continue;
    }
    return 0;
  }
  if (r.tok.kind != (int32_t)TOKEN_LBRACE)
    return 0;
  *(struct parser_asm_lexer *)lex_inout = lex;
  return 1;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_sig_audit_c. */
static int32_t c_ref_fn_sig_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex;
  struct parser_asm_lexer lex_cur;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_FUNCTION)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_LPAREN)
    return 0;
  lex_cur = r.next_lex;
  if (!c_ref_fn_param_list_audit(&lex_cur, source))
    return 0;
  lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_COLON)
    return 0;
  lex_cur = r.next_lex;
  return c_ref_skip_return_type_audit(&lex_cur, source);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_select_list_audit_c. */
static int32_t c_ref_import_select_list_audit(void *lex_inout, void *source, int32_t max_names) {

  struct parser_asm_lexer *inout_lex;
  struct parser_asm_lexer_result r;
  int32_t count;
  int32_t guard;
  if (!lex_inout || !source || max_names <= 0)
    return 0;
  inout_lex = (struct parser_asm_lexer *)lex_inout;
  count = 0;
  guard = 0;
  lexer_next_into(&r, *inout_lex, (struct parser_asm_slice_u8 *)source);
  while (r.tok.kind == (int32_t)TOKEN_IDENT) {
    if (guard++ > max_names)
      return 0;
    if (r.tok.ident_len <= 0 || r.tok.ident_len > 63)
      return 0;
    (void)parser_asm_stretch_import_select_item_bind_audit_c((struct parser_asm_slice_u8 *)source, r.token_start,
                                                             r.tok.ident_len);
    if (parser_asm_stretch_import_path_validate_c((const uint8_t *)r.tok.ident, r.tok.ident_len) == 0) {
      uint8_t tmp[64];
      int32_t bi;
      for (bi = 0; bi < r.tok.ident_len && bi < 63; bi++) {
        if (((struct parser_asm_slice_u8 *)source)->data
            && r.token_start + (size_t)bi < ((struct parser_asm_slice_u8 *)source)->length)
          tmp[bi] = ((struct parser_asm_slice_u8 *)source)->data[r.token_start + (size_t)bi];
      }
      tmp[bi < 63 ? bi : 63] = 0;
      if (parser_asm_stretch_import_path_validate_c(tmp, r.tok.ident_len) == 0)
        return 0;
    }
    count++;
    parser_asm_lex_from_result_val_into(inout_lex, r);
    lexer_next_into(&r, *inout_lex, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind == (int32_t)TOKEN_COMMA) {
      parser_asm_lex_from_result_val_into(inout_lex, r);
      lexer_next_into(&r, *inout_lex, (struct parser_asm_slice_u8 *)source);
      continue;
    }
    if (r.tok.kind == (int32_t)TOKEN_RBRACE)
      break;
    return 0;
  }
  if (r.tok.kind != (int32_t)TOKEN_RBRACE)
    return 0;
  parser_asm_lex_from_result_val_into(inout_lex, r);
  return count > 0 ? count : 1;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_fields_probe_c. */
static int32_t c_ref_struct_fields_probe(void *lex_inout, void *source, int32_t *out_field_count) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t nf;
  int32_t guard;
  nf = 0;
  guard = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  while (r.tok.kind != (int32_t)TOKEN_RBRACE && r.tok.kind != (int32_t)TOKEN_EOF) {
    if (guard++ > 256)
      return 0;
    if (r.tok.kind == (int32_t)TOKEN_ALIGN) {
      (void)c_ref_struct_align_paren_audit(&lex, source);
      parser_asm_lex_from_result_val_into(&lex, r);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      if (r.tok.kind != (int32_t)TOKEN_LPAREN)
        return 0;
      parser_asm_skip_balanced_parens_into_slice_c(&lex, r.next_lex, source);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      if (!parser_asm_stretch_struct_field_continues_kind_c(r.tok.kind))
        return 0;
      continue;
    }
    if (!parser_asm_stretch_struct_field_name_kind_c(r.tok.kind))
      return 0;
    (void)parser_asm_stretch_struct_field_bind_audit_c(source, r.token_start, r.tok.ident_len);
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind != (int32_t)TOKEN_COLON)
      return 0;
    lex = parser_asm_stretch_skip_one_param_type_c(r.next_lex, source);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind != (int32_t)TOKEN_SEMICOLON && r.tok.kind != (int32_t)TOKEN_COMMA
        && r.tok.kind != (int32_t)TOKEN_RBRACE)
      return 0;
    nf++;
    if (r.tok.kind == (int32_t)TOKEN_RBRACE)
      break;
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  }
  if (out_field_count)
    *out_field_count = nf;
  return r.tok.kind == (int32_t)TOKEN_RBRACE ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_enum_variants_probe_c. */
static int32_t c_ref_enum_variants_probe(void *lex_inout, void *source, int32_t *out_variant_count) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t nv;
  int32_t guard;
  nv = 0;
  guard = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  while (r.tok.kind != (int32_t)TOKEN_RBRACE && r.tok.kind != (int32_t)TOKEN_EOF) {
    if (guard++ > 512)
      return 0;
    if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0)
      return 0;
    (void)parser_asm_stretch_enum_variant_bind_audit_c(source, r.token_start, r.tok.ident_len);
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind == (int32_t)TOKEN_ASSIGN) {
      parser_asm_lex_from_result_val_into(&lex, r);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      (void)parser_asm_stretch_enum_discriminant_kind_audit_c(r.tok.kind);
      if (r.tok.kind != (int32_t)TOKEN_I32 && r.tok.kind != (int32_t)TOKEN_I64)
        return 0;
      parser_asm_lex_from_result_val_into(&lex, r);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    }
    if (r.tok.kind == (int32_t)TOKEN_COMMA) {
      nv++;
      parser_asm_lex_from_result_val_into(&lex, r);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      continue;
    }
    if (r.tok.kind == (int32_t)TOKEN_RBRACE) {
      nv++;
      break;
    }
    return 0;
  }
  if (out_variant_count)
    *out_variant_count = nv;
  return 1;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_methods_probe_c. */
static int32_t c_ref_trait_methods_probe(void *lex_inout, void *source, int32_t *out_method_count) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t nm;
  int32_t guard;
  nm = 0;
  guard = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  while (r.tok.kind != (int32_t)TOKEN_RBRACE && r.tok.kind != (int32_t)TOKEN_EOF) {
    if (guard++ > 128)
      return 0;
    if (r.tok.kind != (int32_t)TOKEN_FUNCTION) {
      parser_asm_lex_from_result_val_into(&lex, r);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      continue;
    }
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind != (int32_t)TOKEN_IDENT)
      return 0;
    (void)parser_asm_stretch_function_name_audit_c(((struct parser_asm_slice_u8 *)source)->data + r.token_start, r.tok.ident_len);
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind != (int32_t)TOKEN_LPAREN)
      return 0;
    (void)c_ref_trait_method_paren_audit(&r.next_lex, source);
    parser_asm_skip_balanced_parens_into_slice_c(&lex, r.next_lex, source);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind != (int32_t)TOKEN_COLON)
      return 0;
    (void)c_ref_trait_method_return_audit(&lex, source);
    lex = r.next_lex;
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    while (r.tok.kind != (int32_t)TOKEN_SEMICOLON && r.tok.kind != (int32_t)TOKEN_EOF) {
      if (parser_asm_stretch_is_type_start_kind_c(r.tok.kind)) {
        lex = parser_asm_stretch_skip_type_suffix_c(r.next_lex, source);
        lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
        continue;
      }
      if (r.tok.kind == (int32_t)TOKEN_STAR) {
        lex = r.next_lex;
        lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
        continue;
      }
      return 0;
    }
    if (r.tok.kind != (int32_t)TOKEN_SEMICOLON)
      return 0;
    nm++;
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  }
  if (out_method_count)
    *out_method_count = nm;
  return r.tok.kind == (int32_t)TOKEN_RBRACE ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_impl_items_probe_c. */
static int32_t c_ref_impl_items_probe(void *lex_inout, void *source, int32_t *out_item_count) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t ni;
  int32_t guard;
  ni = 0;
  guard = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  while (r.tok.kind != (int32_t)TOKEN_RBRACE && r.tok.kind != (int32_t)TOKEN_EOF) {
    if (guard++ > 256)
      return 0;
    if (r.tok.kind == (int32_t)TOKEN_FUNCTION) {
      struct parser_asm_lexer fn_lex = lex;
      struct parser_asm_lexer after;
      if (!c_ref_fn_sig_audit(&fn_lex, source))
        return 0;
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      if (r.tok.kind != (int32_t)TOKEN_FUNCTION)
        return 0;
      parser_asm_lex_from_result_val_into(&lex, r);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      if (r.tok.kind != (int32_t)TOKEN_IDENT)
        return 0;
      parser_asm_lex_from_result_val_into(&lex, r);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      if (r.tok.kind != (int32_t)TOKEN_LPAREN)
        return 0;
      parser_asm_skip_balanced_parens_into_slice_c(&lex, r.next_lex, source);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      if (r.tok.kind != (int32_t)TOKEN_COLON)
        return 0;
      (void)c_ref_impl_fn_return_audit(&lex, source);
      parser_asm_lex_from_result_val_into(&lex, r);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      while (r.tok.kind != (int32_t)TOKEN_LBRACE && r.tok.kind != (int32_t)TOKEN_EOF) {
        parser_asm_lex_from_result_val_into(&lex, r);
        lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      }
      if (r.tok.kind != (int32_t)TOKEN_LBRACE)
        return 0;
      parser_asm_skip_balanced_braces_into_slice_c(&after, r.next_lex, source);
      lex = after;
      ni++;
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      continue;
    }
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  }
  if (out_item_count)
    *out_item_count = ni;
  return 1;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_extern_fn_audit_c. */
static int32_t c_ref_extern_fn_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex_cur;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_EXTERN)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_FUNCTION)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_LPAREN)
    return 0;
  lex_cur = r.next_lex;
  if (!c_ref_fn_param_list_audit(&lex_cur, source))
    return 0;
  lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_COLON)
    return 0;
  lex_cur = r.next_lex;
  lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
  while (r.tok.kind != (int32_t)TOKEN_SEMICOLON && r.tok.kind != (int32_t)TOKEN_EOF) {
    if (r.tok.kind == (int32_t)TOKEN_STAR) {
      lex_cur = r.next_lex;
      lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
      continue;
    }
    if (parser_asm_stretch_is_type_start_kind_c(r.tok.kind)) {
      lex_cur = parser_asm_stretch_skip_type_suffix_c(r.next_lex, source);
      lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
      continue;
    }
    return 0;
  }
  return r.tok.kind == (int32_t)TOKEN_SEMICOLON ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_modifiers_audit_c. */
static int32_t c_ref_struct_modifiers_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer cur;
  int32_t score;
  int32_t guard;
  score = 0;
  guard = 0;
  cur = lex;
  for (;;) {
    if (guard++ > 12)
      break;
    lexer_next_into(&r, cur, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind == (int32_t)TOKEN_PACKED || r.tok.kind == (int32_t)TOKEN_SOA || r.tok.kind == (int32_t)TOKEN_ALIGN) {
      score += 8;
      cur = r.next_lex;
      continue;
    }
    if (r.tok.kind == (int32_t)TOKEN_IDENT && r.tok.ident_len == 5 && ((struct parser_asm_slice_u8 *)source)->data && r.token_start + 4 < ((struct parser_asm_slice_u8 *)source)->length
        && ((struct parser_asm_slice_u8 *)source)->data[r.token_start] == (uint8_t)'a' && ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 1] == (uint8_t)'l'
        && ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 2] == (uint8_t)'l' && ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 3] == (uint8_t)'o'
        && ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 4] == (uint8_t)'w') {
      score += 4;
      cur = r.next_lex;
      lexer_next_into(&r, cur, (struct parser_asm_slice_u8 *)source);
      if (r.tok.kind == (int32_t)TOKEN_LPAREN) {
        parser_asm_skip_balanced_parens_into_slice_c(&cur, r.next_lex, source);
        score += 4;
      }
      continue;
    }
    break;
  }
  return score;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_async_fn_prefix_audit_c. */
static int32_t c_ref_async_fn_prefix_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer_result r2;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_ASYNC)
    return 0;
  lexer_next_into(&r2, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r2.tok.kind == (int32_t)TOKEN_FUNCTION ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_arms_probe_c. */
static int32_t c_ref_match_arms_probe(void *lex_inout, void *source, int32_t *out_arm_count) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t arms;
  int32_t guard;
  arms = 0;
  guard = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  while (r.tok.kind != (int32_t)TOKEN_RBRACE && r.tok.kind != (int32_t)TOKEN_EOF) {
    if (guard++ > 256)
      return 0;
    if (r.tok.kind == (int32_t)TOKEN_ARROW) {
      arms++;
      parser_asm_lex_from_result_val_into(&lex, r);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      if (r.tok.kind == (int32_t)TOKEN_COMMA) {
        parser_asm_lex_from_result_val_into(&lex, r);
        lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      }
      continue;
    }
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  }
  if (out_arm_count)
    *out_arm_count = arms;
  return r.tok.kind == (int32_t)TOKEN_RBRACE ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_block_stmt_kind_probe_c. */
static int32_t c_ref_block_stmt_kind_probe(void *lex_inout, void *source, int32_t *out_stmt_score) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t score;
  int32_t depth;
  int32_t guard;
  score = 0;
  depth = 1;
  guard = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  while (depth > 0 && r.tok.kind != (int32_t)TOKEN_EOF) {
    if (guard++ > 4096)
      return 0;
    if (r.tok.kind == (int32_t)TOKEN_LBRACE)
      depth++;
    else if (r.tok.kind == (int32_t)TOKEN_RBRACE) {
      depth--;
      if (depth == 0)
        break;
    } else if (r.tok.kind == (int32_t)TOKEN_LET || r.tok.kind == (int32_t)TOKEN_CONST) {
      score += 2;
      (void)c_ref_let_in_block_audit(&lex, source);
    } else if (r.tok.kind == (int32_t)TOKEN_IDENT) {
      struct parser_asm_lexer_result rnx;
      lexer_next_into(&rnx, r.next_lex, (struct parser_asm_slice_u8 *)source);
      if (rnx.tok.kind == (int32_t)TOKEN_COLON) {
        score += 2;
        (void)c_ref_label_stmt_audit(&lex, source);
      } else if (rnx.tok.kind == (int32_t)TOKEN_ASSIGN) {
        score += 1;
        (void)c_ref_assign_stmt_audit(&lex, source);
      }
    } else if (r.tok.kind == (int32_t)TOKEN_RETURN) {
      score += 3;
      (void)c_ref_return_stmt_audit(&lex, source);
    }     else if (r.tok.kind == (int32_t)TOKEN_BREAK) {
      score += 1;
      (void)c_ref_break_continue_audit(&lex, source, 1);
    } else if (r.tok.kind == (int32_t)TOKEN_CONTINUE) {
      score += 1;
      (void)c_ref_break_continue_audit(&lex, source, 0);
    } else if (r.tok.kind == (int32_t)TOKEN_MATCH) {
      score += 3;
      (void)c_ref_match_kw_audit(&lex, source);
    } else if (r.tok.kind == (int32_t)TOKEN_IF) {
      score += 2;
      (void)c_ref_if_header_audit(&lex, source);
    } else if (r.tok.kind == (int32_t)TOKEN_WHILE) {
      score += 2;
      (void)c_ref_loop_stmt_body_audit(&lex, source, 1);
      (void)c_ref_loop_header_audit(&lex, source, 1);
    } else if (r.tok.kind == (int32_t)TOKEN_FOR) {
      score += 2;
      (void)c_ref_loop_stmt_body_audit(&lex, source, 0);
      (void)c_ref_loop_header_audit(&lex, source, 0);
    } else if (r.tok.kind == (int32_t)TOKEN_PANIC) {
      score += 2;
      (void)c_ref_panic_kw_audit(&lex, source);
    }
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  }
  if (out_stmt_score)
    *out_stmt_score = score;
  return depth == 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_header_audit_c. */
static int32_t c_ref_if_header_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  struct parser_asm_lexer_result r;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IF)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind == (int32_t)TOKEN_LPAREN ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_loop_header_audit_c. */
static int32_t c_ref_loop_header_audit(void *lex_inout, void *source, int32_t expect_while) {

  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t want;
  want = expect_while != 0 ? (int32_t)TOKEN_WHILE : (int32_t)TOKEN_FOR;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != want)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind == (int32_t)TOKEN_LPAREN ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_let_const_decl_audit_c. */
static int32_t c_ref_let_const_decl_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_LET && r.tok.kind != (int32_t)TOKEN_CONST)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_COLON)
    return 0;
  return 1;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_stmt_audit_c. */
static int32_t c_ref_import_stmt_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t guard;
  guard = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IMPORT)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  for (;;) {
    if (guard++ > 64)
      return 0;
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind == (int32_t)TOKEN_SEMICOLON)
      return 1;
    if (r.tok.kind == (int32_t)TOKEN_EOF)
      return 0;
    if (r.tok.kind == (int32_t)TOKEN_IDENT && r.tok.ident_len == 2 && ((struct parser_asm_slice_u8 *)source)->data && r.token_start + 1 < ((struct parser_asm_slice_u8 *)source)->length
        && ((struct parser_asm_slice_u8 *)source)->data[r.token_start] == (uint8_t)'a' && ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 1] == (uint8_t)'s')
      continue;
    parser_asm_lex_from_result_val_into(&lex, r);
  }

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_enum_header_audit_c. */
static int32_t c_ref_enum_header_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_ENUM)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind == (int32_t)TOKEN_LBRACE ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_extern_param_count_audit_c. */
static int32_t c_ref_extern_param_count_audit(void *lex_inout, void *source, int32_t *out_param_count) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex_cur;
  int32_t nparams;
  int32_t guard;
  nparams = 0;
  guard = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_EXTERN)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_FUNCTION)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0)
    return 0;
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_LPAREN)
    return 0;
  lex_cur = r.next_lex;
  lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_RPAREN) {
    if (out_param_count)
      *out_param_count = 0;
    return 1;
  }
  for (;;) {
    if (guard++ > 128)
      return 0;
    if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0 || r.tok.ident_len > 63)
      return 0;
    parser_asm_lex_from_result_val_into(&lex_cur, r);
    lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind != (int32_t)TOKEN_COLON)
      return 0;
    lex_cur = parser_asm_stretch_skip_one_param_type_c(r.next_lex, source);
    nparams++;
    lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind == (int32_t)TOKEN_RPAREN) {
      if (out_param_count)
        *out_param_count = nparams;
      return 1;
    }
    if (r.tok.kind != (int32_t)TOKEN_COMMA)
      return 0;
    parser_asm_lex_from_result_val_into(&lex_cur, r);
    lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
  }

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_header_audit_c. */
static int32_t c_ref_struct_header_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_STRUCT)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind == (int32_t)TOKEN_LBRACE ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_header_audit_c. */
static int32_t c_ref_trait_header_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_TRAIT)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind == (int32_t)TOKEN_LBRACE ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_impl_header_audit_c. */
static int32_t c_ref_impl_header_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex_cur;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IMPL)
    return 0;
  parser_asm_lex_from_result_val_into(&lex_cur, r);
  lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0)
    return 0;
  parser_asm_lex_from_result_val_into(&lex_cur, r);
  lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_FOR) {
    parser_asm_lex_from_result_val_into(&lex_cur, r);
    lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0)
      return 0;
    parser_asm_lex_from_result_val_into(&lex_cur, r);
    lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
  }
  return r.tok.kind == (int32_t)TOKEN_LBRACE ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_function_header_audit_c. */
static int32_t c_ref_function_header_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex;
  struct parser_asm_lexer lex_cur;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  lex_cur = lex;
  lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_ASYNC) {
    parser_asm_lex_from_result_val_into(&lex_cur, r);
    lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
  }
  if (r.tok.kind != (int32_t)TOKEN_FUNCTION)
    return 0;
  parser_asm_lex_from_result_val_into(&lex_cur, r);
  lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind == (int32_t)TOKEN_LPAREN ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_kw_audit_c. */
static int32_t c_ref_match_kw_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex;
  struct parser_asm_lexer lex_cur;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t guard;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_MATCH)
    return 0;
  lex_cur = r.next_lex;
  guard = 0;
  for (;;) {
    if (guard++ > 512)
      return 0;
    lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind == (int32_t)TOKEN_LBRACE)
      return 1;
    if (r.tok.kind == (int32_t)TOKEN_EOF || r.tok.kind == (int32_t)TOKEN_SEMICOLON)
      return 0;
    lex_cur = r.next_lex;
  }

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_else_if_chain_audit_c. */
static int32_t c_ref_else_if_chain_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_ELSE)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IF)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind == (int32_t)TOKEN_LPAREN ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_type_ref_peek_audit_c. */
static int32_t c_ref_type_ref_peek_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer after;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_STAR)
    return 1;
  if (!parser_asm_stretch_is_type_start_kind_c(r.tok.kind))
    return 0;
  after = parser_asm_stretch_skip_type_suffix_c(r.next_lex, source);
  return after.pos != lex.pos ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_toplevel_kind_peek_audit_c. */
static int32_t c_ref_toplevel_kind_peek_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t kinds[4];
  int32_t n;
  int32_t score;
  n = parser_asm_stretch_peek_kind_chain_c(lex, source, kinds, 4);
  if (n <= 0)
    return 0;
  score = kinds[0];
  if (n >= 2)
    score += parser_asm_stretch_classify_toplevel_c(kinds[0], kinds[1], n >= 3 ? kinds[2] : (int32_t)TOKEN_EOF);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_return_stmt_audit_c. */
static int32_t c_ref_return_stmt_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex;
  struct parser_asm_lexer lex_cur;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t guard;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_RETURN)
    return 0;
  lex_cur = r.next_lex;
  guard = 0;
  for (;;) {
    if (guard++ > 256)
      return 0;
    lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind == (int32_t)TOKEN_SEMICOLON)
      return 1;
    if (r.tok.kind == (int32_t)TOKEN_EOF)
      return 0;
    lex_cur = r.next_lex;
  }

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_break_continue_audit_c. */
static int32_t c_ref_break_continue_audit(void *lex_inout, void *source, int32_t want_break) {

  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t want;
  want = want_break != 0 ? (int32_t)TOKEN_BREAK : (int32_t)TOKEN_CONTINUE;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != want)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind == (int32_t)TOKEN_SEMICOLON ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_else_stmt_audit_c. */
static int32_t c_ref_else_stmt_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_ELSE)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_IF || r.tok.kind == (int32_t)TOKEN_LBRACE)
    return 1;
  return 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_cond_int_as_audit_c. */
static int32_t c_ref_cond_int_as_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer_result r2;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_INT && r.tok.kind != (int32_t)TOKEN_I32)
    return 0;
  lexer_next_into(&r2, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r2.tok.kind == (int32_t)TOKEN_AS ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_let_in_block_audit_c. */
static int32_t c_ref_let_in_block_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  return c_ref_let_const_decl_audit(&lex, source);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_label_stmt_audit_c. */
static int32_t c_ref_label_stmt_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind == (int32_t)TOKEN_COLON ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_assign_stmt_audit_c. */
static int32_t c_ref_assign_stmt_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind == (int32_t)TOKEN_ASSIGN ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_fn_shape_audit_c. */
static int32_t c_ref_library_fn_shape_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex_cur;
  if (c_ref_function_header_audit(&lex, source) == 0)
    return 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_FUNCTION)
    return 0;
  parser_asm_lex_from_result_val_into(&lex_cur, r);
  lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_SPAWN) {
    parser_asm_lex_from_result_val_into(&lex_cur, r);
    lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
  }
  if (r.tok.kind != (int32_t)TOKEN_IDENT)
    return 0;
  parser_asm_lex_from_result_val_into(&lex_cur, r);
  lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_LPAREN)
    return 0;
  lex_cur = r.next_lex;
  return c_ref_fn_param_list_audit(&lex_cur, source);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_head_audit_c. */
static int32_t c_ref_primary_head_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t score;
  score = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  switch (r.tok.kind) {
  case (int32_t)TOKEN_INT:
  case (int32_t)TOKEN_FLOAT:
    score += 1;
    break;
  case (int32_t)TOKEN_TRUE:
  case (int32_t)TOKEN_FALSE:
    score += 1;
    break;
  case (int32_t)TOKEN_IDENT:
    (void)parser_asm_stretch_bind_name_validate_c(((struct parser_asm_slice_u8 *)source)->data + r.token_start, r.tok.ident_len);
    score += 2;
    break;
  case (int32_t)TOKEN_LPAREN:
  case (int32_t)TOKEN_LBRACKET:
    score += 1;
    break;
  case (int32_t)TOKEN_IF:
    (void)c_ref_if_header_audit(&lex, source);
    score += 3;
    break;
  case (int32_t)TOKEN_MATCH:
    (void)c_ref_match_kw_audit(&lex, source);
    score += 3;
    break;
  case (int32_t)TOKEN_PANIC:
    score += 2;
    break;
  case (int32_t)TOKEN_AT:
    score += 2;
    break;
  case (int32_t)TOKEN_BREAK:
    (void)c_ref_break_continue_audit(&lex, source, 1);
    score += 1;
    break;
  case (int32_t)TOKEN_CONTINUE:
    (void)c_ref_break_continue_audit(&lex, source, 0);
    score += 1;
    break;
  default:
    break;
  }
  return score;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_mul_binop_audit_c. */
static int32_t c_ref_expr_mul_binop_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t n;
  n = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  while (r.tok.kind == (int32_t)TOKEN_STAR || r.tok.kind == (int32_t)TOKEN_SLASH
         || r.tok.kind == (int32_t)TOKEN_PERCENT) {
    n++;
    if (n > 32)
      return n;
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  }
  return n;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_addsub_binop_audit_c. */
static int32_t c_ref_expr_addsub_binop_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t n;
  n = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  while (r.tok.kind == (int32_t)TOKEN_PLUS || r.tok.kind == (int32_t)TOKEN_MINUS) {
    n++;
    if (n > 32)
      return n;
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  }
  return n;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_panic_kw_audit_c. */
static int32_t c_ref_panic_kw_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_PANIC)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind == (int32_t)TOKEN_LPAREN || r.tok.kind == (int32_t)TOKEN_SEMICOLON ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_shift_binop_audit_c. */
static int32_t c_ref_expr_shift_binop_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  static const int32_t kinds[2] = {(int32_t)TOKEN_LSHIFT, (int32_t)TOKEN_RSHIFT};
  return parser_asm_stretch_expr_binop_kinds_probe_c(lex, source, kinds, 2);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_rel_binop_audit_c. */
static int32_t c_ref_expr_rel_binop_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  static const int32_t kinds[4] = {(int32_t)TOKEN_LT, (int32_t)TOKEN_LE, (int32_t)TOKEN_GT, (int32_t)TOKEN_GE};
  return parser_asm_stretch_expr_binop_kinds_probe_c(lex, source, kinds, 4);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_eq_binop_audit_c. */
static int32_t c_ref_expr_eq_binop_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  static const int32_t kinds[2] = {(int32_t)TOKEN_EQ, (int32_t)TOKEN_NE};
  return parser_asm_stretch_expr_binop_kinds_probe_c(lex, source, kinds, 2);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_bitand_binop_audit_c. */
static int32_t c_ref_expr_bitand_binop_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  static const int32_t kinds[1] = {(int32_t)TOKEN_AMP};
  return parser_asm_stretch_expr_binop_kinds_probe_c(lex, source, kinds, 1);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_bitxor_binop_audit_c. */
static int32_t c_ref_expr_bitxor_binop_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  static const int32_t kinds[1] = {(int32_t)TOKEN_CARET};
  return parser_asm_stretch_expr_binop_kinds_probe_c(lex, source, kinds, 1);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_bitor_binop_audit_c. */
static int32_t c_ref_expr_bitor_binop_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  static const int32_t kinds[1] = {(int32_t)TOKEN_PIPE};
  return parser_asm_stretch_expr_binop_kinds_probe_c(lex, source, kinds, 1);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_logand_binop_audit_c. */
static int32_t c_ref_expr_logand_binop_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  static const int32_t kinds[1] = {(int32_t)TOKEN_AMPAMP};
  return parser_asm_stretch_expr_binop_kinds_probe_c(lex, source, kinds, 1);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_logor_binop_audit_c. */
static int32_t c_ref_expr_logor_binop_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  static const int32_t kinds[1] = {(int32_t)TOKEN_PIPEPIPE};
  return parser_asm_stretch_expr_binop_kinds_probe_c(lex, source, kinds, 1);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_as_suffix_chain_audit_c. */
static int32_t c_ref_as_suffix_chain_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t n;
  int32_t guard;
  n = 0;
  guard = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  while (r.tok.kind == (int32_t)TOKEN_AS) {
    n++;
    if (n > 16 || guard++ > 32)
      return n;
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind == (int32_t)TOKEN_STAR) {
      parser_asm_lex_from_result_val_into(&lex, r);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    }
    if (!parser_asm_stretch_is_type_start_kind_c(r.tok.kind))
      return n;
    lex = parser_asm_stretch_skip_type_suffix_c(r.next_lex, source);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  }
  return n;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_unary_prefix_audit_c. */
static int32_t c_ref_unary_prefix_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer_result r2;
  int32_t depth;
  depth = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  while (r.tok.kind == (int32_t)TOKEN_AWAIT || r.tok.kind == (int32_t)TOKEN_RUN || r.tok.kind == (int32_t)TOKEN_SPAWN
         || r.tok.kind == (int32_t)TOKEN_MINUS || r.tok.kind == (int32_t)TOKEN_BANG) {
    depth++;
    if (depth > 16)
      return depth;
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  }
  if (r.tok.kind == (int32_t)TOKEN_AMP) {
    lexer_next_into(&r2, r.next_lex, (struct parser_asm_slice_u8 *)source);
    if (r2.tok.kind != (int32_t)TOKEN_AMP)
      depth++;
  }
  return depth;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_expr_branch_audit_c. */
static int32_t c_ref_if_expr_branch_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  return c_ref_if_header_audit(&lex_at_if, source);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_lit_fields_probe_c. */
static int32_t c_ref_struct_lit_fields_probe(void *lex_inout, void *source, int32_t *out_field_count) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t nf;
  int32_t guard;
  int32_t depth;
  nf = 0;
  guard = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  while (r.tok.kind != (int32_t)TOKEN_RBRACE && r.tok.kind != (int32_t)TOKEN_EOF) {
    if (guard++ > 64)
      return 0;
    if (r.tok.kind != (int32_t)TOKEN_IDENT)
      return 0;
    (void)parser_asm_stretch_bind_name_validate_c(((struct parser_asm_slice_u8 *)source)->data + r.token_start, r.tok.ident_len);
    lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
    if (r.tok.kind != (int32_t)TOKEN_COLON)
      return 0;
    nf++;
    lex = r.next_lex;
    depth = 0;
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    while (r.tok.kind != (int32_t)TOKEN_EOF) {
      if (guard++ > 512)
        return 0;
      if (depth == 0 && (r.tok.kind == (int32_t)TOKEN_COMMA || r.tok.kind == (int32_t)TOKEN_RBRACE))
        break;
      if (r.tok.kind == (int32_t)TOKEN_LPAREN || r.tok.kind == (int32_t)TOKEN_LBRACE)
        depth++;
      else if (r.tok.kind == (int32_t)TOKEN_RPAREN || r.tok.kind == (int32_t)TOKEN_RBRACE)
        depth--;
      parser_asm_lex_from_result_val_into(&lex, r);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    }
    if (r.tok.kind == (int32_t)TOKEN_COMMA) {
      lex = r.next_lex;
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      continue;
    }
    break;
  }
  if (out_field_count)
    *out_field_count = nf;
  return r.tok.kind == (int32_t)TOKEN_RBRACE ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_array_lit_head_audit_c. */
static int32_t c_ref_array_lit_head_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_LBRACKET)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind == (int32_t)TOKEN_RBRACKET || r.tok.kind == (int32_t)TOKEN_INT
         || r.tok.kind == (int32_t)TOKEN_IDENT || r.tok.kind == (int32_t)TOKEN_TRUE
         || r.tok.kind == (int32_t)TOKEN_FALSE ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_suffix_chain_probe_c. */
static int32_t c_ref_primary_suffix_chain_probe(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t score;
  int32_t guard;
  score = 0;
  guard = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  while (guard++ < 64) {
    if (r.tok.kind == (int32_t)TOKEN_DOT) {
      score += 2;
      lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
      if (r.tok.kind != (int32_t)TOKEN_IDENT)
        break;
      (void)parser_asm_stretch_bind_name_validate_c(((struct parser_asm_slice_u8 *)source)->data + r.token_start, r.tok.ident_len);
      parser_asm_lex_from_result_val_into(&lex, r);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    } else if (r.tok.kind == (int32_t)TOKEN_LBRACKET) {
      score += 2;
      parser_asm_stretch_skip_balanced_brackets_into_c(&lex, r.next_lex, source);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    } else if (r.tok.kind == (int32_t)TOKEN_LPAREN) {
      score += 3;
      parser_asm_skip_balanced_parens_into_slice_c(&lex, r.next_lex, source);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
    } else {
      break;
    }
  }
  return score;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_ternary_op_audit_c. */
static int32_t c_ref_ternary_op_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind == (int32_t)TOKEN_QUESTION ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_assign_op_audit_c. */
static int32_t c_ref_assign_op_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_ASSIGN)
    return 1;
  return parser_asm_is_compound_assign_token_c(r.tok.kind) ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_stmt_branch_audit_c. */
static int32_t c_ref_if_stmt_branch_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  return c_ref_if_header_audit(&lex_at_if, source);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_type_ref_deep_audit_c. */
static int32_t c_ref_type_ref_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer after;
  int32_t score;
  score = c_ref_type_ref_peek_audit(&lex, source);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_STAR)
    return score + 1;
  if (r.tok.kind == (int32_t)TOKEN_LBRACKET) {
    parser_asm_stretch_skip_balanced_brackets_into_c(&after, r.next_lex, source);
    return score + 2;
  }
  if (parser_asm_stretch_is_type_start_kind_c(r.tok.kind)) {
    after = parser_asm_stretch_skip_type_suffix_c(r.next_lex, source);
    if (after.pos != r.next_lex.pos)
      score += 2;
  }
  return score;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_call_args_shape_probe_c. */
static int32_t c_ref_call_args_shape_probe(void *lex_inout, void *source, int32_t *out_arg_count) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t nargs;
  int32_t depth;
  int32_t guard;
  nargs = 0;
  depth = 1;
  guard = 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  while (r.tok.kind != (int32_t)TOKEN_EOF && depth > 0) {
    if (guard++ > 128)
      return 0;
    if (depth == 1 && r.tok.kind == (int32_t)TOKEN_RPAREN)
      break;
    if (depth == 1 && r.tok.kind != (int32_t)TOKEN_COMMA && nargs == 0)
      nargs = 1;
    if (depth == 1 && r.tok.kind == (int32_t)TOKEN_COMMA) {
      nargs++;
      parser_asm_lex_from_result_val_into(&lex, r);
      lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
      continue;
    }
    if (r.tok.kind == (int32_t)TOKEN_LPAREN || r.tok.kind == (int32_t)TOKEN_LBRACE
        || r.tok.kind == (int32_t)TOKEN_LBRACKET)
      depth++;
    else if (r.tok.kind == (int32_t)TOKEN_RPAREN || r.tok.kind == (int32_t)TOKEN_RBRACE)
      depth--;
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  }
  if (out_arg_count)
    *out_arg_count = nargs;
  return depth == 1 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_fn_header_audit_c. */
static int32_t c_ref_diag_fn_header_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t score;
  score = c_ref_function_header_audit(&lex, source);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_FUNCTION)
    return score;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_IDENT && r.tok.ident_len > 0)
    (void)parser_asm_stretch_bind_name_validate_c(((struct parser_asm_slice_u8 *)source)->data + r.token_start, r.tok.ident_len);
  return score + 1;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_paren_expr_head_audit_c. */
static int32_t c_ref_paren_expr_head_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_LPAREN)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind != (int32_t)TOKEN_RPAREN ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_toplevel_after_imports_audit_c. */
static int32_t c_ref_diag_toplevel_after_imports_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  return c_ref_toplevel_kind_peek_audit(&lex, source);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_fn_param_sig_audit_c. */
static int32_t c_ref_diag_fn_param_sig_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer param_lex;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_LPAREN)
    return 0;
  param_lex = r.next_lex;
  return c_ref_fn_param_list_audit(&param_lex, source);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_allow_kw_paren_audit_c. */
static int32_t c_ref_allow_kw_paren_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer_result r2;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len != 5)
    return 0;
  if (!((struct parser_asm_slice_u8 *)source)->data
      || r.token_start + 4 >= ((struct parser_asm_slice_u8 *)source)->length)
    return 0;
  if (((struct parser_asm_slice_u8 *)source)->data[r.token_start] != (uint8_t)'a'
      || ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 1] != (uint8_t)'l'
      || ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 2] != (uint8_t)'l'
      || ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 3] != (uint8_t)'o'
      || ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 4] != (uint8_t)'w')
    return 0;
  lexer_next_into(&r2, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r2.tok.kind == (int32_t)TOKEN_LPAREN ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_allow_paren_audit_c. */
static int32_t c_ref_try_skip_allow_paren_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  return c_ref_paren_expr_head_audit(&lex, source);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_linear_type_audit_c. */
static int32_t c_ref_linear_type_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer_result r2;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len != 6)
    return 0;
  if (!((struct parser_asm_slice_u8 *)source)->data || r.token_start + 5 >= ((struct parser_asm_slice_u8 *)source)->length)
    return 0;
  if (((struct parser_asm_slice_u8 *)source)->data[r.token_start] != (uint8_t)'L' || ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 1] != (uint8_t)'i'
      || ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 2] != (uint8_t)'n' || ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 3] != (uint8_t)'e'
      || ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 4] != (uint8_t)'a' || ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 5] != (uint8_t)'r')
    return 0;
  lexer_next_into(&r2, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r2.tok.kind == (int32_t)TOKEN_LPAREN ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_array_type_bracket_audit_c. */
static int32_t c_ref_array_type_bracket_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_LBRACKET)
    return 0;
  (void)c_ref_balanced_brackets_depth_probe(&r.next_lex, source);
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind == (int32_t)TOKEN_INT ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_slice_type_bracket_audit_c. */
static int32_t c_ref_slice_type_bracket_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_LBRACKET)
    return 0;
  (void)c_ref_balanced_brackets_depth_probe(&r.next_lex, source);
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind == (int32_t)TOKEN_RBRACKET ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_after_collect_preamble_audit_c. */
static int32_t c_ref_diag_after_collect_preamble_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t kinds[4];
  int32_t score;
  score = c_ref_diag_toplevel_after_imports_audit(&lex, source);
  if (parser_asm_stretch_peek_kind_chain_c(lex, source, kinds, 4) > 0)
    score += kinds[0];
  return score;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_balanced_parens_depth_probe_c. */
static int32_t c_ref_balanced_parens_depth_probe(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind != (int32_t)TOKEN_RPAREN ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_fn_return_type_audit_c. */
static int32_t c_ref_diag_fn_return_type_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_COLON)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_STAR)
    return 1;
  if (r.tok.kind == (int32_t)TOKEN_LBRACKET)
    return 1;
  return parser_asm_stretch_is_type_start_kind_c(r.tok.kind) ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_return_type_audit_c. */
static int32_t c_ref_library_return_type_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_COLON)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_BOOL)
    return 1;
  if (r.tok.kind == (int32_t)TOKEN_IDENT && r.tok.ident_len == 4 && ((struct parser_asm_slice_u8 *)source)->data && r.token_start + 3 < ((struct parser_asm_slice_u8 *)source)->length
      && ((struct parser_asm_slice_u8 *)source)->data[r.token_start] == (uint8_t)'b' && ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 1] == (uint8_t)'o'
      && ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 2] == (uint8_t)'o' && ((struct parser_asm_slice_u8 *)source)->data[r.token_start + 3] == (uint8_t)'l')
    return 1;
  return parser_asm_stretch_is_type_start_kind_c(r.tok.kind) ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_align_paren_audit_c. */
static int32_t c_ref_struct_align_paren_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer_result r2;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_ALIGN)
    return 0;
  lexer_next_into(&r2, r.next_lex, (struct parser_asm_slice_u8 *)source);
  return r2.tok.kind == (int32_t)TOKEN_LPAREN ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_let_const_buf_audit_c. */
static int32_t c_ref_body_skip_let_const_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_let_const_decl_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_method_return_audit_c. */
static int32_t c_ref_trait_method_return_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  return c_ref_diag_fn_return_type_audit(&lex, source);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_impl_type_for_trait_audit_c. */
static int32_t c_ref_impl_type_for_trait_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer_result r2;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_FOR)
    return 0;
  lexer_next_into(&r2, r.next_lex, (struct parser_asm_slice_u8 *)source);
  if (r2.tok.kind != (int32_t)TOKEN_IDENT || r2.tok.ident_len <= 0)
    return 0;
  return parser_asm_stretch_bind_name_validate_c(((struct parser_asm_slice_u8 *)source)->data + r2.token_start, r2.tok.ident_len);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_extern_return_type_audit_c. */
static int32_t c_ref_extern_return_type_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  return c_ref_diag_fn_return_type_audit(&lex, source);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_skip_let_const_type_audit_c. */
static int32_t c_ref_diag_skip_let_const_type_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_COLON)
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_STAR)
    return 1;
  return parser_asm_stretch_is_type_start_kind_c(r.tok.kind) ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_skip_let_const_type_buf_audit_c. */
static int32_t c_ref_diag_skip_let_const_type_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_diag_skip_let_const_type_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_impl_fn_return_audit_c. */
static int32_t c_ref_impl_fn_return_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  return c_ref_diag_fn_return_type_audit(&lex, source);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_method_paren_audit_c. */
static int32_t c_ref_trait_method_paren_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  return c_ref_paren_expr_head_audit(&lex, source);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_balanced_braces_depth_probe_c. */
static int32_t c_ref_balanced_braces_depth_probe(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind != (int32_t)TOKEN_RBRACE ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_balanced_braces_depth_probe_buf_c. */
static int32_t c_ref_balanced_braces_depth_probe_buf(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_balanced_braces_depth_probe(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_one_trait_buf_audit_c. */
static int32_t c_ref_skip_one_trait_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_trait_header_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_one_impl_buf_audit_c. */
static int32_t c_ref_skip_one_impl_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_impl_header_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_one_extern_buf_audit_c. */
static int32_t c_ref_parse_one_extern_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_extern_fn_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_balanced_parens_depth_probe_buf_c. */
static int32_t c_ref_balanced_parens_depth_probe_buf(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_balanced_parens_depth_probe(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_one_struct_buf_audit_c. */
static int32_t c_ref_skip_one_struct_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_struct_modifiers_audit(&lex, &sl);
  score += c_ref_struct_header_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_one_enum_buf_audit_c. */
static int32_t c_ref_skip_one_enum_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_enum_header_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_collect_imports_buf_audit_c. */
static int32_t c_ref_collect_imports_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_toplevel_kind_peek_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_one_if_buf_audit_c. */
static int32_t c_ref_skip_one_if_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_if_header_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_one_extern_buf_audit_c. */
static int32_t c_ref_skip_one_extern_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_extern_fn_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_allow_paren_buf_audit_c. */
static int32_t c_ref_try_skip_allow_paren_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_allow_paren_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_allow_kw_paren_buf_audit_c. */
static int32_t c_ref_allow_kw_paren_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_slice_u8 sl;
  if (!lex_inout || !data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_allow_kw_paren_audit(lex_inout, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_lex_after_imports_audit_c. */
static int32_t c_ref_diag_lex_after_imports_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  int32_t score;
  (void)lex_inout;
  if (!source)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_import_stmt_audit(&lex, source);
  score += c_ref_toplevel_kind_peek_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_lex_after_imports_buf_audit_c. */
static int32_t c_ref_diag_lex_after_imports_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_slice_u8 sl;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_diag_lex_after_imports_audit(lex_inout, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_one_enum_register_buf_audit_c. */
static int32_t c_ref_skip_one_enum_register_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  return c_ref_skip_one_enum_buf_audit(&lex, data, len);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_imports_buf_audit_c. */
static int32_t c_ref_skip_imports_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_stmt_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_enum_variants_body_audit_c. */
static int32_t c_ref_enum_variants_body_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  return c_ref_enum_variants_probe(&lex, source, 0);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_enum_variants_body_buf_audit_c. */
static int32_t c_ref_enum_variants_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_enum_variants_body_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_peek_function_name_buf_audit_c. */
static int32_t c_ref_parse_peek_function_name_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_function_header_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_let_const_type_audit_c. */
static int32_t c_ref_body_skip_let_const_type_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  return c_ref_diag_skip_let_const_type_audit(&lex, source);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_fields_body_audit_c. */
static int32_t c_ref_struct_fields_body_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer body_lex;

  if (!parser_asm_stretch_struct_advance_to_body_lex_c(lex, source, &body_lex))
    return 0;
  return c_ref_struct_fields_probe(&body_lex, source, 0);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_fields_body_buf_audit_c. */
static int32_t c_ref_struct_fields_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_fields_body_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_enum_variants_body_from_header_buf_audit_c. */
static int32_t c_ref_enum_variants_body_from_header_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  struct parser_asm_lexer body_lex;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  if (!parser_asm_stretch_enum_advance_to_body_lex_c(lex, &sl, &body_lex))
    return 0;
  return c_ref_enum_variants_probe(&body_lex, &sl, 0);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_after_imports_then_structs_buf_audit_c. */
static int32_t c_ref_diag_after_imports_then_structs_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  struct parser_asm_lexer_result r;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = 0;
  lexer_next_into(&r, lex, &sl);
  while (r.tok.kind == (int32_t)TOKEN_STRUCT) {
    score += c_ref_struct_header_audit(&lex, &sl);
    score += c_ref_struct_fields_body_audit(&lex, &sl);
    lex = parser_asm_skip_one_struct_slice_c(lex, &sl);
    lexer_next_into(&r, lex, &sl);
  }
  score += c_ref_diag_toplevel_after_imports_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_buf_preamble_audit_c. */
static int32_t c_ref_parse_into_buf_preamble_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_diag_after_collect_preamble_audit(&lex, &sl);
  score += c_ref_diag_after_imports_then_structs_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_methods_body_audit_c. */
static int32_t c_ref_trait_methods_body_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer body_lex;

  if (!parser_asm_stretch_trait_advance_to_body_lex_c(lex, source, &body_lex))
    return 0;
  return c_ref_trait_methods_probe(&body_lex, source, 0);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_methods_body_buf_audit_c. */
static int32_t c_ref_trait_methods_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_trait_methods_body_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_impl_items_body_audit_c. */
static int32_t c_ref_impl_items_body_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer body_lex;

  if (!parser_asm_stretch_impl_advance_to_body_lex_c(lex, source, &body_lex))
    return 0;
  return c_ref_impl_items_probe(&body_lex, source, 0);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_impl_items_body_buf_audit_c. */
static int32_t c_ref_impl_items_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_impl_items_body_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_record_layout_body_audit_c. */
static int32_t c_ref_struct_record_layout_body_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;

  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_IDENT || r.tok.ident_len <= 0)
    return 0;
  (void)parser_asm_stretch_struct_layout_name_audit_c(((struct parser_asm_slice_u8 *)source)->data + r.token_start, r.tok.ident_len);
  parser_asm_lex_from_result_val_into(&lex, r);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_SOA) {
    parser_asm_lex_from_result_val_into(&lex, r);
    lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  }
  if (r.tok.kind != (int32_t)TOKEN_LBRACE)
    return 0;
  return c_ref_struct_fields_probe(&r.next_lex, source, 0);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_record_layout_body_buf_audit_c. */
static int32_t c_ref_struct_record_layout_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_record_layout_body_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_fail_at_token_kind_buf_audit_c. */
static int32_t c_ref_diag_fail_at_token_kind_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  struct parser_asm_slice_u8 sl;
  struct parser_asm_lexer lex;
  struct parser_asm_lexer_result r;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  /* v5.10: diag_lex root widened to (lex, source); init before call. */
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_lex_after_imports_audit(&lex, &sl);
  score += c_ref_skip_imports_buf_audit(&lex, data, len);
  lex = parser_asm_skip_imports_slice_c(lex, &sl);
  score += c_ref_diag_after_imports_then_structs_buf_audit(&lex, data, len);
  r = parser_asm_diag_after_imports_then_structs_slice_c(lex, &sl);
  if (r.tok.kind == (int32_t)TOKEN_FUNCTION) {
    parser_asm_lex_from_result_val_into(&lex, r);
    score += c_ref_diag_fn_header_audit(&lex, &sl);
    score += c_ref_diag_fn_param_sig_audit(&lex, &sl);
  }
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_one_function_full_buf_audit_c. */
static int32_t c_ref_skip_one_function_full_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_function_header_audit(&lex, &sl);
  score += c_ref_async_fn_prefix_audit(&lex, &sl);
  score += c_ref_fn_sig_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_stmt_body_audit_c. */
static int32_t c_ref_if_stmt_body_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer body_lex;
  struct parser_asm_lexer_result r;

  if (!parser_asm_stretch_if_advance_to_body_lex_c(lex, source, &body_lex))
    return 0;
  (void)c_ref_cond_int_as_audit(&body_lex, source);
  lexer_next_into(&r, body_lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_LBRACE)
    return c_ref_block_stmt_kind_probe(&r.next_lex, source, 0);
  return c_ref_if_stmt_branch_audit(&lex, source);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_stmt_body_buf_audit_c. */
static int32_t c_ref_if_stmt_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_if_stmt_body_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_function_body_block_stmt_audit_c. */
static int32_t c_ref_function_body_block_stmt_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer body_lex;

  if (!parser_asm_stretch_function_advance_to_body_lex_c(lex, source, &body_lex))
    return 0;
  return c_ref_block_stmt_kind_probe(&body_lex, source, 0);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_function_body_block_stmt_buf_audit_c. */
static int32_t c_ref_function_body_block_stmt_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_function_body_block_stmt_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_fn_shape_buf_audit_c. */
static int32_t c_ref_library_fn_shape_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_library_fn_shape_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_extern_body_buf_audit_c. */
static int32_t c_ref_extern_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;
  int32_t param_count;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_extern_fn_audit(&lex, &sl);
  score += c_ref_extern_param_count_audit(&lex, &sl, &param_count);
  score += c_ref_extern_return_type_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_arms_body_audit_c. */
static int32_t c_ref_match_arms_body_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer arms_lex;

  if (!parser_asm_stretch_match_advance_to_arms_lex_c(lex, source, &arms_lex))
    return 0;
  return c_ref_match_arms_probe(&arms_lex, source, 0);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_arms_body_buf_audit_c. */
static int32_t c_ref_match_arms_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_arms_body_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_let_const_then_if_buf_audit_c. */
static int32_t c_ref_body_skip_let_const_then_if_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_let_const_buf_audit(&lex, data, len);
  score += c_ref_if_stmt_body_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_scan_audit_c. */
static int32_t c_ref_library_scan_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_fn_shape_audit(&lex, source);
  score += c_ref_library_return_type_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_collect_imports_deep_buf_audit_c. */
static int32_t c_ref_collect_imports_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  struct parser_asm_lexer after_imports;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_collect_imports_buf_audit(&lex, data, len);
  score += c_ref_skip_imports_buf_audit(&lex, data, len);
  score += c_ref_import_stmt_audit(&lex, &sl);
  after_imports = parser_asm_skip_imports_slice_c(lex, &sl);
  score += c_ref_diag_after_collect_preamble_audit(&after_imports, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_token_after_collect_imports_buf_audit_c. */
static int32_t c_ref_diag_token_after_collect_imports_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  struct parser_asm_slice_u8 sl;
  struct parser_asm_lexer lex;
  int32_t peek_kinds[6];
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  lex = parser_asm_lexer_init_c();
  score = c_ref_collect_imports_deep_buf_audit(&lex, data, len);
  lex = parser_asm_skip_imports_slice_c(parser_asm_lexer_init_c(), &sl);
  if (parser_asm_stretch_peek_kind_chain_c(lex, &sl, peek_kinds, 6) > 0)
    score++;
  score += c_ref_diag_toplevel_after_imports_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_one_function_buf_deep_audit_c. */
static int32_t c_ref_parse_one_function_buf_deep_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_function_header_audit(&lex, &sl);
  score += c_ref_async_fn_prefix_audit(&lex, &sl);
  score += c_ref_fn_sig_audit(&lex, &sl);
  score += c_ref_parse_peek_function_name_buf_audit(&lex, data, len);
  score += c_ref_function_body_block_stmt_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_return_type_buf_audit_c. */
static int32_t c_ref_library_return_type_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_library_return_type_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_scan_buf_audit_c. */
static int32_t c_ref_library_scan_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_fn_shape_buf_audit(&lex, data, len);
  score += c_ref_library_return_type_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_one_if_else_chain_audit_c. */
static int32_t c_ref_skip_one_if_else_chain_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_else_if_chain_audit(&lex, source);
  score += c_ref_else_stmt_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_one_if_else_chain_buf_audit_c. */
static int32_t c_ref_skip_one_if_else_chain_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_skip_one_if_else_chain_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_let_const_then_if_audit_c. */
static int32_t c_ref_body_skip_let_const_then_if_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_let_const_decl_audit(&lex, source);
  score += c_ref_body_skip_let_const_type_audit(&lex, source);
  score += c_ref_if_stmt_body_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_type_ref_deep_buf_audit_c. */
static int32_t c_ref_type_ref_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_type_ref_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_one_extern_body_audit_c. */
static int32_t c_ref_skip_one_extern_body_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;
  int32_t param_count;

  score = c_ref_extern_fn_audit(&lex, source);
  score += c_ref_extern_param_count_audit(&lex, source, &param_count);
  score += c_ref_extern_return_type_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_one_extern_body_buf_audit_c. */
static int32_t c_ref_skip_one_extern_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_skip_one_extern_body_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_buf_loop_toplevel_buf_audit_c. */
static int32_t c_ref_parse_into_buf_loop_toplevel_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  struct parser_asm_lexer_result r;
  int32_t kinds[4];
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_toplevel_kind_peek_audit(&lex, &sl);
  lexer_next_into(&r, lex, &sl);
  /* elide void validate_toplevel (v5.6; matches .x) */
  if (parser_asm_stretch_peek_kind_chain_c(lex, &sl, kinds, 4) >= 2)
    score += parser_asm_stretch_classify_toplevel_c(kinds[0], kinds[1], kinds[2]);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_async_fn_prefix_buf_audit_c. */
static int32_t c_ref_async_fn_prefix_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_async_fn_prefix_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_cond_int_as_buf_audit_c. */
static int32_t c_ref_cond_int_as_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_cond_int_as_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_head_buf_audit_c. */
static int32_t c_ref_primary_head_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_head_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_lit_fields_body_audit_c. */
static int32_t c_ref_struct_lit_fields_body_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  return c_ref_struct_lit_fields_probe(&lex, source, 0);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_peek_kind_chain_buf_audit_c. */
static int32_t c_ref_peek_kind_chain_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t kinds[6];

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return parser_asm_stretch_peek_kind_chain_c(lex, &sl, kinds, 6) > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_parse_one_after_collect_imports_buf_audit_c. */
static int32_t c_ref_diag_parse_one_after_collect_imports_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  struct parser_asm_slice_u8 sl;
  struct parser_asm_lexer lex;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  lex = parser_asm_skip_imports_slice_c(parser_asm_lexer_init_c(), &sl);
  {
    /* B-minus pointer ABI: temp so &lex_init is a stable address (v5.6). */
    struct parser_asm_lexer lex_init = parser_asm_lexer_init_c();
    score = c_ref_collect_imports_deep_buf_audit(&lex_init, data, len);
  }
  score += c_ref_diag_after_collect_preamble_audit(&lex, &sl);
  score += c_ref_function_header_audit(&lex, &sl);
  score += c_ref_fn_sig_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_first_token_kind_buf_audit_c. */
static int32_t c_ref_first_token_kind_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t kinds[4];

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return parser_asm_stretch_peek_kind_chain_c(parser_asm_lexer_init_c(), &sl, kinds, 4) > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_one_function_ok_for_pipeline_audit_c. */
static int32_t c_ref_parse_one_function_ok_for_pipeline_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_diag_toplevel_after_imports_audit(&lex, source);
  score += c_ref_function_header_audit(&lex, source);
  score += c_ref_fn_sig_audit(&lex, source);
  score += c_ref_function_body_block_stmt_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_one_function_ok_for_pipeline_buf_audit_c. */
static int32_t c_ref_parse_one_function_ok_for_pipeline_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  struct parser_asm_slice_u8 sl;
  struct parser_asm_lexer lex;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  lex = parser_asm_skip_imports_slice_c(parser_asm_lexer_init_c(), &sl);
  return c_ref_parse_one_function_ok_for_pipeline_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_cond_expr_audit_c. */
static int32_t c_ref_parse_cond_expr_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_cond_int_as_audit(&lex, source);
  score += c_ref_paren_expr_head_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_cond_expr_buf_audit_c. */
static int32_t c_ref_parse_cond_expr_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_parse_cond_expr_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_let_bracket_deep_audit_c. */
static int32_t c_ref_body_let_bracket_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_array_lit_head_audit(&lex, source);
  score += c_ref_let_in_block_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_loop_stmt_body_audit_c. */
static int32_t c_ref_loop_stmt_body_audit(void *lex_inout, void *source, int32_t expect_while) {

  struct parser_asm_lexer lex;
  struct parser_asm_lexer body_lex;
  struct parser_asm_lexer_result r;
  struct parser_asm_lexer lex_cur;

  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  if (!c_ref_loop_header_audit(&lex, source, expect_while))
    return 0;
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (expect_while != 0 ? (int32_t)TOKEN_WHILE : (int32_t)TOKEN_FOR))
    return 0;
  lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind != (int32_t)TOKEN_LPAREN)
    return 0;
  (void)c_ref_cond_int_as_audit(&r.next_lex, source);
  parser_asm_skip_balanced_parens_into_slice_c(&lex_cur, r.next_lex, (struct parser_asm_slice_u8 *)source);
  lexer_next_into(&r, lex_cur, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_LBRACE)
    return c_ref_block_stmt_kind_probe(&r.next_lex, source, 0);
  body_lex = lex_cur;
  return c_ref_assign_stmt_audit(&body_lex, source);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_buf_preamble_peek_buf_audit_c. */
static int32_t c_ref_parse_into_buf_preamble_peek_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_parse_into_buf_preamble_audit(&lex, data, len);
  score += c_ref_peek_kind_chain_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_select_deep_buf_audit_c. */
static int32_t c_ref_import_select_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  struct parser_asm_lexer sel_lex;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_import_stmt_audit(&lex, &sl);
  sel_lex = lex;
  score += c_ref_import_select_list_audit(&sel_lex, &sl, 32);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_expr_deep_buf_audit_c. */
static int32_t c_ref_primary_expr_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_primary_head_buf_audit(&lex, data, len);
  score += c_ref_unary_prefix_audit(&lex, &sl);
  score += c_ref_primary_suffix_chain_probe(&lex, &sl);
  score += c_ref_as_suffix_chain_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_balanced_brackets_depth_probe_c. */
static int32_t c_ref_balanced_brackets_depth_probe(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;

  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  return r.tok.kind != (int32_t)TOKEN_RBRACKET ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_balanced_brackets_depth_probe_buf_c. */
static int32_t c_ref_balanced_brackets_depth_probe_buf(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;

  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_balanced_brackets_depth_probe(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_expr_deep_audit_c. */
static int32_t c_ref_primary_expr_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_head_audit(&lex, source);
  score += c_ref_unary_prefix_audit(&lex, source);
  score += c_ref_primary_suffix_chain_probe(&lex, source);
  score += c_ref_as_suffix_chain_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_subject_deep_audit_c. */
static int32_t c_ref_match_subject_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t score;

  score = c_ref_match_kw_audit(&lex, source);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_MATCH)
    lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  score += parser_asm_stretch_match_subject_ident_audit_c(r, source);
  score += c_ref_match_arms_body_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_subject_deep_buf_audit_c. */
static int32_t c_ref_match_subject_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_subject_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_onefunc_buf_deep_audit_c. */
static int32_t c_ref_onefunc_buf_deep_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_fn_sig_audit(&lex, &sl);
  score += c_ref_function_body_block_stmt_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_expr_prefix_chain_audit_c. */
static int32_t c_ref_parse_expr_prefix_chain_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_unary_prefix_audit(&lex, source);
  score += c_ref_primary_head_audit(&lex, source);
  score += c_ref_as_suffix_chain_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_expr_prefix_chain_buf_audit_c. */
static int32_t c_ref_parse_expr_prefix_chain_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_parse_expr_prefix_chain_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_body_deep_audit_c. */
static int32_t c_ref_if_body_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_if_stmt_body_audit(&lex, source);
  score += c_ref_else_if_chain_audit(&lex, source);
  score += c_ref_skip_one_if_else_chain_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_body_deep_buf_audit_c. */
static int32_t c_ref_if_body_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_if_body_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_one_function_ok_pipeline_deep_buf_audit_c. */
static int32_t c_ref_parse_one_function_ok_pipeline_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  struct parser_asm_slice_u8 sl;
  struct parser_asm_lexer lex;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  lex = parser_asm_skip_imports_slice_c(parser_asm_lexer_init_c(), &sl);
  score = c_ref_parse_one_function_ok_for_pipeline_buf_audit(&lex, data, len);
  score += c_ref_function_body_block_stmt_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_type_ref_bracket_composite_buf_audit_c. */
static int32_t c_ref_type_ref_bracket_composite_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_array_type_bracket_audit(&lex, &sl);
  score += c_ref_slice_type_bracket_audit(&lex, &sl);
  score += c_ref_type_ref_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_one_if_core_deep_audit_c. */
static int32_t c_ref_skip_one_if_core_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t score;

  score = c_ref_if_header_audit(&lex, source);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_IF)
    lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_LPAREN) {
    score += c_ref_cond_int_as_audit(&r.next_lex, source);
    score += c_ref_balanced_parens_depth_probe(&r.next_lex, source);
  }
  score += c_ref_if_stmt_body_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_one_if_core_deep_buf_audit_c. */
static int32_t c_ref_skip_one_if_core_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_skip_one_if_core_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_one_function_full_deep_buf_audit_c. */
static int32_t c_ref_skip_one_function_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_skip_one_function_full_buf_audit(&lex, data, len);
  score += c_ref_function_body_block_stmt_buf_audit(&lex, data, len);
  score += c_ref_match_arms_body_buf_audit(&lex, data, len);
  score += c_ref_match_subject_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_parse_one_collect_deep_buf_audit_c. */
static int32_t c_ref_diag_parse_one_collect_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  /* v5.35: noles historical (data,len) had no lexer local; callsite widen
   * must not invent `&lex`. Fresh init ≡ C local lexer_init. */
  struct parser_asm_lexer lex_init = parser_asm_lexer_init_c();
  int32_t score;

  score = c_ref_diag_parse_one_after_collect_imports_buf_audit(&lex_init, data, len);
  score += c_ref_parse_one_function_ok_pipeline_deep_buf_audit(&lex_init, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_function_branch_deep_buf_audit_c. */
static int32_t c_ref_parse_into_function_branch_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_async_fn_prefix_buf_audit(&lex, data, len);
  score += c_ref_function_header_audit(&lex, &sl);
  score += c_ref_fn_sig_audit(&lex, &sl);
  score += c_ref_function_body_block_stmt_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_ternary_assign_deep_audit_c. */
static int32_t c_ref_ternary_assign_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_ternary_op_audit(&lex, source);
  score += c_ref_assign_op_audit(&lex, source);
  score += c_ref_parse_expr_prefix_chain_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_ternary_assign_deep_buf_audit_c. */
static int32_t c_ref_ternary_assign_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_ternary_assign_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_binop_lower_chain_audit_c. */
static int32_t c_ref_expr_binop_lower_chain_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_mul_binop_audit(&lex, source);
  score += c_ref_expr_addsub_binop_audit(&lex, source);
  score += c_ref_expr_shift_binop_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_binop_lower_chain_buf_audit_c. */
static int32_t c_ref_expr_binop_lower_chain_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_binop_lower_chain_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_impl_header_deep_buf_audit_c. */
static int32_t c_ref_trait_impl_header_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_trait_header_audit(&lex, &sl);
  score += c_ref_impl_header_audit(&lex, &sl);
  score += c_ref_skip_one_trait_buf_audit(&lex, data, len);
  score += c_ref_skip_one_impl_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_enum_body_deep_buf_audit_c. */
static int32_t c_ref_enum_body_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_enum_header_audit(&lex, &sl);
  score += c_ref_enum_variants_probe(&lex, &sl, 0);
  score += c_ref_skip_one_enum_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_loop_stmt_body_buf_audit_c. */
static int32_t c_ref_loop_stmt_body_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_loop_stmt_body_audit(&lex, &sl, 1);
  score += c_ref_loop_stmt_body_audit(&lex, &sl, 0);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_binop_upper_chain_audit_c. */
static int32_t c_ref_expr_binop_upper_chain_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_rel_binop_audit(&lex, source);
  score += c_ref_expr_eq_binop_audit(&lex, source);
  score += c_ref_expr_bitand_binop_audit(&lex, source);
  score += c_ref_expr_bitxor_binop_audit(&lex, source);
  score += c_ref_expr_bitor_binop_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_binop_upper_chain_buf_audit_c. */
static int32_t c_ref_expr_binop_upper_chain_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_binop_upper_chain_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_binop_mid_chain_audit_c. */
static int32_t c_ref_expr_binop_mid_chain_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_logand_binop_audit(&lex, source);
  score += c_ref_expr_logor_binop_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_binop_mid_chain_buf_audit_c. */
static int32_t c_ref_expr_binop_mid_chain_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_binop_mid_chain_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_expr_deep_audit_c. */
static int32_t c_ref_if_expr_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t score;

  score = c_ref_if_expr_branch_audit(&lex_at_if, source);
  score += c_ref_else_if_chain_audit(&lex_at_if, source);
  score += c_ref_if_header_audit(&lex_at_if, source);
  lexer_next_into(&r, lex_at_if, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_IF)
    lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_LPAREN)
    score += c_ref_cond_int_as_audit(&r.next_lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_skip_deep_buf_audit_c. */
static int32_t c_ref_struct_skip_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_skip_one_struct_buf_audit(&lex, data, len);
  score += c_ref_struct_fields_body_buf_audit(&lex, data, len);
  score += c_ref_struct_record_layout_body_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_extern_skip_deep_buf_audit_c. */
static int32_t c_ref_extern_skip_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_skip_one_extern_buf_audit(&lex, data, len);
  score += c_ref_extern_body_buf_audit(&lex, data, len);
  score += c_ref_skip_one_extern_body_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_skip_let_const_deep_audit_c. */
static int32_t c_ref_diag_skip_let_const_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_diag_skip_let_const_type_audit(&lex, source);
  score += c_ref_body_let_bracket_deep_audit(&lex, source);
  score += c_ref_let_in_block_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_skip_let_const_deep_buf_audit_c. */
static int32_t c_ref_diag_skip_let_const_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_diag_skip_let_const_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_impl_body_deep_buf_audit_c. */
static int32_t c_ref_trait_impl_body_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_trait_methods_body_buf_audit(&lex, data, len);
  score += c_ref_impl_items_body_buf_audit(&lex, data, len);
  score += c_ref_trait_impl_header_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_preamble_deep_buf_audit_c. */
static int32_t c_ref_parse_into_preamble_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_collect_imports_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_into_buf_preamble_peek_buf_audit(&lex, data, len);
  score += c_ref_parse_into_buf_loop_toplevel_buf_audit(&lex, data, len);
  /* v5.10: diag_lex buf widened to (lex, data, len). */
  score += c_ref_diag_lex_after_imports_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_block_stmt_deep_audit_c. */
static int32_t c_ref_block_stmt_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_block_stmt_kind_probe(&lex, source, 0);
  score += c_ref_loop_stmt_body_audit(&lex, source, 1);
  score += c_ref_loop_stmt_body_audit(&lex, source, 0);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_block_stmt_deep_buf_audit_c. */
static int32_t c_ref_block_stmt_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_block_stmt_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_binop_full_chain_audit_c. */
static int32_t c_ref_expr_binop_full_chain_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_binop_lower_chain_audit(&lex, source);
  score += c_ref_expr_binop_upper_chain_audit(&lex, source);
  score += c_ref_expr_binop_mid_chain_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_binop_full_chain_buf_audit_c. */
static int32_t c_ref_expr_binop_full_chain_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_binop_full_chain_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_stmt_deep_audit_c. */
static int32_t c_ref_if_stmt_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t score;

  score = c_ref_if_stmt_branch_audit(&lex_at_if, source);
  score += c_ref_if_body_deep_audit(&lex_at_if, source);
  score += c_ref_if_header_audit(&lex_at_if, source);
  lexer_next_into(&r, lex_at_if, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_IF)
    lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_LPAREN)
    score += c_ref_cond_int_as_audit(&r.next_lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_allow_deep_audit_c. */
static int32_t c_ref_try_skip_allow_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_allow_paren_audit(&lex, source);
  score += c_ref_balanced_parens_depth_probe(&lex, source);
  score += c_ref_balanced_braces_depth_probe(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_allow_deep_buf_audit_c. */
static int32_t c_ref_try_skip_allow_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_allow_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_after_imports_structs_deep_buf_audit_c. */
static int32_t c_ref_diag_after_imports_structs_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_diag_after_imports_then_structs_buf_audit(&lex, data, len);
  score += c_ref_struct_skip_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_loop_iter_deep_buf_audit_c. */
static int32_t c_ref_parse_into_loop_iter_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_parse_into_buf_loop_toplevel_buf_audit(&lex, data, len);
  score += c_ref_parse_into_function_branch_deep_buf_audit(&lex, data, len);
  score += c_ref_try_skip_allow_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_scan_deep_audit_c. */
static int32_t c_ref_library_scan_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t score;

  score = c_ref_library_scan_audit(&lex, source);
  score += c_ref_library_fn_shape_audit(&lex, source);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_FUNCTION)
    lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  score += parser_asm_stretch_spawn_kw_audit_c((int32_t)r.tok.kind);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_scan_deep_buf_audit_c. */
static int32_t c_ref_library_scan_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_library_scan_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_onefunc_slice_deep_audit_c. */
static int32_t c_ref_onefunc_slice_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_function_header_audit(&lex, source);
  score += c_ref_async_fn_prefix_audit(&lex, source);
  score += c_ref_fn_sig_audit(&lex, source);
  score += c_ref_function_body_block_stmt_audit(&lex, source);
  score += c_ref_block_stmt_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_call_deep_audit_c. */
static int32_t c_ref_primary_call_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_expr_deep_audit(&lex, source);
  score += c_ref_call_args_shape_probe(&lex, source, 0);
  score += c_ref_paren_expr_head_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_call_deep_buf_audit_c. */
static int32_t c_ref_primary_call_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_call_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_enum_body_deep_slice_audit_c. */
static int32_t c_ref_enum_body_deep_slice_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_enum_header_audit(&lex, source);
  score += c_ref_enum_variants_body_audit(&lex, source);
  score += c_ref_enum_variants_probe(&lex, source, 0);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_collect_imports_post_deep_buf_audit_c. */
static int32_t c_ref_collect_imports_post_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_collect_imports_buf_audit(&lex, data, len);
  score += c_ref_collect_imports_deep_buf_audit(&lex, data, len);
  score += c_ref_import_select_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_after_collect_preamble_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_fn_deep_audit_c. */
static int32_t c_ref_diag_fn_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_diag_fn_header_audit(&lex, source);
  score += c_ref_diag_fn_param_sig_audit(&lex, source);
  score += c_ref_diag_fn_return_type_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_fn_deep_buf_audit_c. */
static int32_t c_ref_diag_fn_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_diag_fn_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_impl_type_deep_audit_c. */
static int32_t c_ref_trait_impl_type_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_impl_type_for_trait_audit(&lex, source);
  score += c_ref_trait_method_return_audit(&lex, source);
  score += c_ref_trait_method_paren_audit(&lex, source);
  score += c_ref_impl_fn_return_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_impl_type_deep_buf_audit_c. */
static int32_t c_ref_trait_impl_type_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_trait_impl_type_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_layout_deep_audit_c. */
static int32_t c_ref_struct_layout_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_modifiers_audit(&lex, source);
  score += c_ref_struct_fields_body_audit(&lex, source);
  score += c_ref_struct_record_layout_body_audit(&lex, source);
  score += c_ref_struct_header_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_layout_deep_buf_audit_c. */
static int32_t c_ref_struct_layout_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_layout_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_imports_deep_buf_audit_c. */
static int32_t c_ref_skip_imports_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_skip_imports_buf_audit(&lex, data, len);
  score += c_ref_collect_imports_post_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_stmt_full_audit_c. */
static int32_t c_ref_expr_stmt_full_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_binop_full_chain_audit(&lex, source);
  score += c_ref_ternary_assign_deep_audit(&lex, source);
  score += c_ref_parse_expr_prefix_chain_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_stmt_full_buf_audit_c. */
static int32_t c_ref_expr_stmt_full_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_stmt_full_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_block_deep_audit_c. */
static int32_t c_ref_match_block_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_subject_deep_audit(&lex, source);
  score += c_ref_match_arms_body_audit(&lex, source);
  score += c_ref_match_kw_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_block_deep_buf_audit_c. */
static int32_t c_ref_match_block_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_block_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_parse_one_full_deep_buf_audit_c. */
static int32_t c_ref_diag_parse_one_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  struct parser_asm_slice_u8 sl;
  struct parser_asm_lexer lex;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  lex = parser_asm_skip_imports_slice_c(parser_asm_lexer_init_c(), &sl);
  score = c_ref_diag_parse_one_collect_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_one_function_buf_deep_audit(&lex, data, len);
  score += c_ref_parse_one_function_ok_pipeline_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_let_const_deep_audit_c. */
static int32_t c_ref_body_skip_let_const_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_let_const_then_if_audit(&lex, source);
  score += c_ref_diag_skip_let_const_deep_audit(&lex, source);
  score += c_ref_if_stmt_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_let_const_deep_buf_audit_c. */
static int32_t c_ref_body_skip_let_const_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_let_const_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_preamble_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_preamble_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_parse_into_preamble_deep_buf_audit(&lex, data, len);
  score += c_ref_collect_imports_post_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_into_loop_iter_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_stmt_deep_buf_audit_c. */
static int32_t c_ref_if_stmt_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_if_stmt_deep_audit(&lex_at_if, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_expr_deep_buf_audit_c. */
static int32_t c_ref_if_expr_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_if_expr_deep_audit(&lex_at_if, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_cond_expr_deep_audit_c. */
static int32_t c_ref_parse_cond_expr_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_parse_cond_expr_audit(&lex, source);
  score += c_ref_parse_expr_prefix_chain_audit(&lex, source);
  score += c_ref_expr_binop_lower_chain_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_cond_expr_deep_buf_audit_c. */
static int32_t c_ref_parse_cond_expr_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_parse_cond_expr_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_simd_builtin_deep_audit_c. */
static int32_t c_ref_simd_builtin_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r_at;

  lexer_next_into(&r_at, lex, (struct parser_asm_slice_u8 *)source);
  return parser_asm_stretch_simd_builtin_deep_from_at_audit_c(r_at, source);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_simd_builtin_deep_buf_audit_c. */
static int32_t c_ref_simd_builtin_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_simd_builtin_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_lit_deep_audit_c. */
static int32_t c_ref_struct_lit_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_lit_fields_body_audit(&lex, source);
  score += c_ref_struct_lit_fields_probe(&lex, source, 0);
  score += c_ref_array_lit_head_audit(&lex, source);
  score += c_ref_primary_head_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_lit_deep_buf_audit_c. */
static int32_t c_ref_struct_lit_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_lit_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_extern_skip_full_deep_buf_audit_c. */
static int32_t c_ref_extern_skip_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t param_count;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_extern_skip_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_one_extern_buf_audit(&lex, data, len);
  score += c_ref_extern_param_count_audit(&lex, &sl, &param_count);
  score += c_ref_extern_return_type_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_enum_skip_full_deep_buf_audit_c. */
static int32_t c_ref_enum_skip_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_skip_one_enum_buf_audit(&lex, data, len);
  score += c_ref_enum_body_deep_buf_audit(&lex, data, len);
  score += c_ref_enum_body_deep_slice_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_onefunc_buf_full_deep_audit_c. */
static int32_t c_ref_onefunc_buf_full_deep_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_onefunc_buf_deep_audit(&lex, data, len);
  score += c_ref_diag_fn_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_scan_full_deep_buf_audit_c. */
static int32_t c_ref_library_scan_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_scan_deep_buf_audit(&lex, data, len);
  score += c_ref_library_return_type_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_loop_stmt_full_deep_buf_audit_c. */
static int32_t c_ref_loop_stmt_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_loop_stmt_body_buf_audit(&lex, data, len);
  score += c_ref_block_stmt_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_cond_expr_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_collect_imports_full_deep_buf_audit_c. */
static int32_t c_ref_collect_imports_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_collect_imports_buf_audit(&lex, data, len);
  score += c_ref_import_select_deep_buf_audit(&lex, data, len);
  score += c_ref_skip_imports_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_type_ref_full_deep_buf_audit_c. */
static int32_t c_ref_type_ref_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_type_ref_deep_buf_audit(&lex, data, len);
  score += c_ref_type_ref_bracket_composite_buf_audit(&lex, data, len);
  score += c_ref_linear_type_audit(&lex, &sl);
  score += c_ref_array_type_bracket_audit(&lex, &sl);
  score += c_ref_slice_type_bracket_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_block_stmt_full_deep_buf_audit_c. */
static int32_t c_ref_block_stmt_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_block_stmt_deep_buf_audit(&lex, data, len);
  score += c_ref_if_stmt_deep_buf_audit(&lex, data, len);
  score += c_ref_loop_stmt_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_ternary_assign_full_deep_audit_c. */
static int32_t c_ref_ternary_assign_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_ternary_assign_deep_audit(&lex, source);
  score += c_ref_expr_stmt_full_audit(&lex, source);
  score += c_ref_expr_binop_full_chain_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_ternary_assign_full_deep_buf_audit_c. */
static int32_t c_ref_ternary_assign_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_ternary_assign_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_expr_full_deep_audit_c. */
static int32_t c_ref_primary_expr_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_expr_deep_audit(&lex, source);
  score += c_ref_primary_call_deep_audit(&lex, source);
  score += c_ref_balanced_brackets_depth_probe(&lex, source);
  score += c_ref_parse_expr_prefix_chain_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_expr_full_deep_buf_audit_c. */
static int32_t c_ref_primary_expr_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_expr_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_impl_full_deep_buf_audit_c. */
static int32_t c_ref_trait_impl_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_trait_impl_header_deep_buf_audit(&lex, data, len);
  score += c_ref_trait_impl_body_deep_buf_audit(&lex, data, len);
  score += c_ref_trait_impl_type_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_skip_full_deep_buf_audit_c. */
static int32_t c_ref_struct_skip_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_skip_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_layout_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_fields_body_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_stmt_full_deep_audit_c. */
static int32_t c_ref_import_stmt_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer sel_lex;
  int32_t peek_kinds[6];
  int32_t score;

  sel_lex = lex;
  score = c_ref_import_stmt_audit(&lex, source);
  score += c_ref_import_select_list_audit(&sel_lex, source, 32);
  score += parser_asm_stretch_peek_kind_chain_c(lex, source, peek_kinds, 6);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_stmt_full_deep_buf_audit_c. */
static int32_t c_ref_import_stmt_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_stmt_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_entry_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_entry_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_parse_into_preamble_full_deep_buf_audit(&lex, data, len);
  score += c_ref_collect_imports_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_after_imports_structs_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_subject_full_deep_audit_c. */
static int32_t c_ref_match_subject_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_subject_deep_audit(&lex, source);
  score += c_ref_match_block_deep_audit(&lex, source);
  score += c_ref_match_arms_probe(&lex, source, 0);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_subject_full_deep_buf_audit_c. */
static int32_t c_ref_match_subject_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_subject_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_async_fn_sig_full_deep_audit_c. */
static int32_t c_ref_async_fn_sig_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_async_fn_prefix_audit(&lex, source);
  score += c_ref_function_header_audit(&lex, source);
  score += c_ref_fn_sig_audit(&lex, source);
  score += c_ref_diag_fn_param_sig_audit(&lex, source);
  score += c_ref_diag_fn_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_async_fn_sig_full_deep_buf_audit_c. */
static int32_t c_ref_async_fn_sig_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_async_fn_prefix_buf_audit(&lex, data, len);
  score += c_ref_async_fn_sig_full_deep_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_cast_unary_full_deep_audit_c. */
static int32_t c_ref_cast_unary_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_unary_prefix_audit(&lex, source);
  score += c_ref_as_suffix_chain_audit(&lex, source);
  score += c_ref_primary_head_audit(&lex, source);
  score += c_ref_type_ref_peek_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_cast_unary_full_deep_buf_audit_c. */
static int32_t c_ref_cast_unary_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_cast_unary_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_return_stmt_full_deep_audit_c. */
static int32_t c_ref_return_stmt_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_return_stmt_audit(&lex, source);
  score += c_ref_parse_cond_expr_deep_audit(&lex, source);
  score += c_ref_break_continue_audit(&lex, source, 0);
  score += c_ref_label_stmt_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_return_stmt_full_deep_buf_audit_c. */
static int32_t c_ref_return_stmt_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_return_stmt_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_skip_let_full_deep_audit_c. */
static int32_t c_ref_diag_skip_let_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_diag_skip_let_const_deep_audit(&lex, source);
  score += c_ref_body_let_bracket_deep_audit(&lex, source);
  score += c_ref_type_ref_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_skip_let_full_deep_buf_audit_c. */
static int32_t c_ref_diag_skip_let_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_diag_skip_let_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_onefunc_slice_full_deep_audit_c. */
static int32_t c_ref_onefunc_slice_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_onefunc_slice_deep_audit(&lex, source);
  score += c_ref_expr_stmt_full_audit(&lex, source);
  score += c_ref_match_block_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_onefunc_slice_full_deep_buf_audit_c. */
static int32_t c_ref_onefunc_slice_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_onefunc_slice_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_loop_entry_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_loop_entry_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_parse_into_loop_iter_deep_buf_audit(&lex, data, len);
  score += c_ref_try_skip_allow_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_into_function_branch_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_into_buf_loop_toplevel_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_balanced_delim_full_deep_audit_c. */
static int32_t c_ref_balanced_delim_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_balanced_parens_depth_probe(&lex, source);
  score += c_ref_balanced_braces_depth_probe(&lex, source);
  score += c_ref_balanced_brackets_depth_probe(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_balanced_delim_full_deep_buf_audit_c. */
static int32_t c_ref_balanced_delim_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_balanced_delim_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_skip_full_deep_buf_audit_c. */
static int32_t c_ref_trait_skip_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_skip_one_trait_buf_audit(&lex, data, len);
  score += c_ref_trait_impl_full_deep_buf_audit(&lex, data, len);
  score += c_ref_trait_methods_body_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_else_chain_full_deep_audit_c. */
static int32_t c_ref_if_else_chain_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_if_stmt_deep_audit(&lex_at_if, source);
  score += c_ref_else_if_chain_audit(&lex_at_if, source);
  score += c_ref_skip_one_if_else_chain_audit(&lex_at_if, source);
  score += c_ref_if_body_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_else_chain_full_deep_buf_audit_c. */
static int32_t c_ref_if_else_chain_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_if_else_chain_full_deep_audit(&lex_at_if, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_sig_full_deep_audit_c. */
static int32_t c_ref_fn_sig_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_sig_audit(&lex, source);
  score += c_ref_diag_fn_param_sig_audit(&lex, source);
  score += c_ref_diag_fn_return_type_audit(&lex, source);
  score += c_ref_function_header_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_sig_full_deep_buf_audit_c. */
static int32_t c_ref_fn_sig_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_sig_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_arms_full_deep_audit_c. */
static int32_t c_ref_match_arms_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_arms_body_audit(&lex, source);
  score += c_ref_match_arms_probe(&lex, source, 0);
  score += c_ref_match_kw_audit(&lex, source);
  score += c_ref_match_block_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_arms_full_deep_buf_audit_c. */
static int32_t c_ref_match_arms_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_arms_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_path_full_deep_audit_c. */
static int32_t c_ref_import_path_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t score;

  score = c_ref_import_stmt_full_deep_audit(&lex, source);
  score += c_ref_toplevel_kind_peek_audit(&lex, source);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  /* v5.16: import_dot_segment(IMPORT) always returns 0 — elide (eq-honest;
   * matches .x port). Keep the peek so r is still consumed. */
  (void)r;
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_path_full_deep_buf_audit_c. */
static int32_t c_ref_import_path_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_path_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_struct_layout_full_deep_audit_c. */
static int32_t c_ref_parse_struct_layout_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t field_count;
  int32_t score;

  field_count = 0;
  score = c_ref_struct_layout_deep_audit(&lex, source);
  score += c_ref_struct_modifiers_audit(&lex, source);
  score += c_ref_struct_fields_probe(&lex, source, &field_count);
  score += c_ref_struct_header_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_struct_layout_full_deep_buf_audit_c. */
static int32_t c_ref_parse_struct_layout_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_parse_struct_layout_full_deep_audit(&lex, &sl);
  score += c_ref_struct_record_layout_body_buf_audit(&lex, data, len);
  score += c_ref_struct_layout_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_parse_one_mega_full_buf_audit_c. */
static int32_t c_ref_diag_parse_one_mega_full_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  int32_t score;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_parse_one_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_after_imports_structs_deep_buf_audit(&lex, data, len);
  score += c_ref_collect_imports_post_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_binop_mega_full_deep_audit_c. */
static int32_t c_ref_expr_binop_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_binop_full_chain_audit(&lex, source);
  score += c_ref_primary_expr_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_full_deep_audit(&lex, source);
  score += c_ref_ternary_assign_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_binop_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_binop_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_binop_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_fn_full_deep_audit_c. */
static int32_t c_ref_library_fn_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t score;

  score = c_ref_library_scan_deep_audit(&lex, source);
  score += c_ref_library_fn_shape_audit(&lex, source);
  score += c_ref_panic_kw_audit(&lex, source);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  if (r.tok.kind == (int32_t)TOKEN_FUNCTION)
    lexer_next_into(&r, r.next_lex, (struct parser_asm_slice_u8 *)source);
  score += parser_asm_stretch_spawn_kw_audit_c((int32_t)r.tok.kind);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_fn_full_deep_buf_audit_c. */
static int32_t c_ref_library_fn_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_library_scan_full_deep_buf_audit(&lex, data, len);
  score += c_ref_library_fn_full_deep_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_parse_into_entry_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_into_loop_entry_full_deep_buf_audit(&lex, data, len);
  /* v5.32: diag_parse_one mega widened to (lex, data, len). */
  score += c_ref_diag_parse_one_mega_full_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_allow_full_deep_audit_c. */
static int32_t c_ref_try_skip_allow_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_lexer_result r;
  int32_t score;

  score = c_ref_try_skip_allow_deep_audit(&lex, source);
  score += c_ref_balanced_delim_full_deep_audit(&lex, source);
  lexer_next_into(&r, lex, (struct parser_asm_slice_u8 *)source);
  /* v5.16: allow_kw_paren pointer ABI; lex still at the peeked token. */
  (void)r;
  score += c_ref_allow_kw_paren_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_allow_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_allow_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_allow_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_extern_skip_mega_full_deep_buf_audit_c. */
static int32_t c_ref_extern_skip_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_extern_skip_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_one_extern_buf_audit(&lex, data, len);
  score += c_ref_extern_fn_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_let_const_deep_audit(&lex, source);
  score += c_ref_body_skip_let_const_then_if_audit(&lex, source);
  score += c_ref_diag_skip_let_full_deep_audit(&lex, source);
  score += c_ref_block_stmt_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_impl_skip_mega_full_deep_buf_audit_c. */
static int32_t c_ref_impl_skip_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_trait_impl_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_items_body_buf_audit(&lex, data, len);
  score += c_ref_impl_items_probe(&lex, &sl, 0);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_enum_skip_mega_full_deep_buf_audit_c. */
static int32_t c_ref_enum_skip_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_enum_skip_full_deep_buf_audit(&lex, data, len);
  score += c_ref_enum_header_audit(&lex, &sl);
  score += c_ref_enum_variants_body_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_collect_imports_mega_full_deep_audit_c. */
static int32_t c_ref_collect_imports_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t peek_kinds[6];
  int32_t score;

  score = c_ref_import_stmt_full_deep_audit(&lex, source);
  score += c_ref_diag_after_collect_preamble_audit(&lex, source);
  score += c_ref_diag_toplevel_after_imports_audit(&lex, source);
  score += parser_asm_stretch_peek_kind_chain_c(lex, source, peek_kinds, 6);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_collect_imports_mega_full_deep_buf_audit_c. */
static int32_t c_ref_collect_imports_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_collect_imports_full_deep_buf_audit(&lex, data, len);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_control_mega_full_deep_audit_c. */
static int32_t c_ref_if_control_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_if_else_chain_full_deep_audit(&lex_at_if, source);
  score += c_ref_if_expr_deep_audit(&lex_at_if, source);
  score += c_ref_skip_one_if_core_deep_audit(&lex_at_if, source);
  score += c_ref_if_stmt_body_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_control_mega_full_deep_buf_audit_c. */
static int32_t c_ref_if_control_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_if_control_mega_full_deep_audit(&lex_at_if, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_type_ref_mega_full_deep_audit_c. */
static int32_t c_ref_type_ref_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_type_ref_deep_audit(&lex, source);
  score += c_ref_type_ref_peek_audit(&lex, source);
  score += c_ref_struct_align_paren_audit(&lex, source);
  score += c_ref_linear_type_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_type_ref_mega_full_deep_buf_audit_c. */
static int32_t c_ref_type_ref_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_type_ref_full_deep_buf_audit(&lex, data, len);
  score += c_ref_type_ref_mega_full_deep_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_mega_full_deep_audit_c. */
static int32_t c_ref_primary_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_expr_full_deep_audit(&lex, source);
  score += c_ref_struct_lit_deep_audit(&lex, source);
  score += c_ref_array_lit_head_audit(&lex, source);
  score += c_ref_primary_suffix_chain_probe(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_onefunc_mega_full_deep_audit_c. */
static int32_t c_ref_onefunc_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_onefunc_slice_full_deep_audit(&lex, source);
  score += c_ref_fn_sig_full_deep_audit(&lex, source);
  score += c_ref_async_fn_sig_full_deep_audit(&lex, source);
  score += c_ref_body_skip_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_onefunc_mega_full_deep_buf_audit_c. */
static int32_t c_ref_onefunc_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_onefunc_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_ultra_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_parse_into_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_peek_kind_chain_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_mega_full_deep_audit_c. */
static int32_t c_ref_match_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_subject_full_deep_audit(&lex, source);
  score += c_ref_match_arms_full_deep_audit(&lex, source);
  score += c_ref_match_kw_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_one_function_mega_full_deep_buf_audit_c. */
static int32_t c_ref_skip_one_function_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_skip_one_function_full_deep_buf_audit(&lex, data, len);
  score += c_ref_block_stmt_full_deep_buf_audit(&lex, data, len);
  score += c_ref_onefunc_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_fn_mega_full_deep_audit_c. */
static int32_t c_ref_diag_fn_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_diag_fn_deep_audit(&lex, source);
  score += c_ref_fn_sig_full_deep_audit(&lex, source);
  score += c_ref_diag_after_collect_preamble_audit(&lex, source);
  score += parser_asm_stretch_function_name_audit_c((const uint8_t *)"main", 4);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_block_stmt_mega_full_deep_audit_c. */
static int32_t c_ref_block_stmt_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_block_stmt_deep_audit(&lex, source);
  score += c_ref_block_stmt_kind_probe(&lex, source, 0);
  score += c_ref_let_in_block_audit(&lex, source);
  score += c_ref_assign_stmt_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_block_stmt_mega_full_deep_buf_audit_c. */
static int32_t c_ref_block_stmt_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_block_stmt_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_skip_mega_full_deep_audit_c. */
static int32_t c_ref_struct_skip_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_layout_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_full_deep_audit(&lex, source);
  score += c_ref_struct_header_audit(&lex, source);
  score += c_ref_struct_fields_probe(&lex, source, 0);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_skip_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_skip_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_struct_skip_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_struct_layout_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_fields_body_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_skip_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_skip_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_skip_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_path_mega_full_deep_audit_c. */
static int32_t c_ref_import_path_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_path_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_toplevel_kind_peek_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_path_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_path_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_path_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_allow_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_allow_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_allow_full_deep_audit(&lex, source);
  score += c_ref_struct_skip_mega_full_deep_audit(&lex, source);
  score += c_ref_balanced_delim_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_allow_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_allow_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_allow_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_mega_full_deep_audit_c. */
static int32_t c_ref_library_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_fn_full_deep_audit(&lex, source);
  score += c_ref_library_fn_shape_audit(&lex, source);
  score += c_ref_library_scan_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_library_scan_full_deep_buf_audit(&lex, data, len);
  score += c_ref_library_mega_full_deep_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_struct_layout_mega_full_deep_audit_c. */
static int32_t c_ref_parse_struct_layout_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_parse_struct_layout_full_deep_audit(&lex, source);
  score += c_ref_struct_modifiers_audit(&lex, source);
  score += c_ref_struct_fields_probe(&lex, source, 0);
  score += c_ref_struct_layout_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_struct_layout_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_struct_layout_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_parse_struct_layout_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_balanced_delim_mega_full_deep_audit_c. */
static int32_t c_ref_balanced_delim_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_balanced_delim_full_deep_audit(&lex, source);
  score += c_ref_function_body_block_stmt_audit(&lex, source);
  score += c_ref_balanced_braces_depth_probe(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_balanced_delim_mega_full_deep_buf_audit_c. */
static int32_t c_ref_balanced_delim_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_balanced_delim_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_async_fn_mega_full_deep_audit_c. */
static int32_t c_ref_async_fn_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_async_fn_sig_full_deep_audit(&lex, source);
  score += c_ref_fn_sig_full_deep_audit(&lex, source);
  score += c_ref_async_fn_prefix_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_async_fn_mega_full_deep_buf_audit_c. */
static int32_t c_ref_async_fn_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_async_fn_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_parse_ultra_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_parse_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  int32_t score;
  struct parser_asm_lexer lex;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  /* v5.32: diag_parse_one mega widened to (lex, data, len). */
  score = c_ref_diag_parse_one_mega_full_buf_audit(&lex, data, len);
  /* v5.33: diag_fn_mega_buf widened to pointer ABI. */
  score += parser_asm_stretch_diag_fn_mega_full_deep_buf_audit_c(&lex, data, len);
  score += c_ref_import_path_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_loop_control_mega_full_deep_buf_audit_c. */
static int32_t c_ref_loop_control_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_loop_stmt_full_deep_buf_audit(&lex, data, len);
  score += c_ref_block_stmt_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_return_stmt_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_stmt_mega_full_deep_audit_c. */
static int32_t c_ref_expr_stmt_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_return_stmt_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_full_deep_audit(&lex, source);
  score += c_ref_ternary_assign_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_stmt_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_stmt_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_stmt_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_imports_mega_full_deep_audit_c. */
static int32_t c_ref_skip_imports_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_path_mega_full_deep_audit(&lex, source);
  score += c_ref_import_stmt_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_skip_imports_mega_full_deep_buf_audit_c. */
static int32_t c_ref_skip_imports_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_skip_imports_deep_buf_audit(&lex, data, len);
  score += c_ref_import_path_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_control_mega_full_deep_audit_c. */
static int32_t c_ref_fn_control_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_async_fn_mega_full_deep_audit(&lex, source);
  score += c_ref_onefunc_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_stmt_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_control_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_control_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_control_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_ternary_assign_mega_full_deep_audit_c. */
static int32_t c_ref_ternary_assign_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_ternary_assign_full_deep_audit(&lex, source);
  score += c_ref_expr_stmt_mega_full_deep_audit(&lex, source);
  score += c_ref_assign_op_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_ternary_assign_mega_full_deep_buf_audit_c. */
static int32_t c_ref_ternary_assign_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_ternary_assign_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_expr_mega_full_deep_audit_c. */
static int32_t c_ref_primary_expr_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_expr_full_deep_audit(&lex, source);
  score += c_ref_primary_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_suffix_chain_probe(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_expr_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_expr_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_expr_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_stmt_mega_full_deep_audit_c. */
static int32_t c_ref_import_stmt_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_stmt_full_deep_audit(&lex, source);
  score += c_ref_import_path_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_stmt_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_stmt_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_stmt_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_entry_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_entry_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_entry_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_into_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_into_ultra_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_subject_mega_full_deep_audit_c. */
static int32_t c_ref_match_subject_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_subject_full_deep_audit(&lex, source);
  score += c_ref_match_mega_full_deep_audit(&lex, source);
  score += c_ref_match_arms_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_subject_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_subject_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_subject_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_cast_unary_mega_full_deep_audit_c. */
static int32_t c_ref_cast_unary_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_cast_unary_full_deep_audit(&lex, source);
  score += c_ref_expr_stmt_mega_full_deep_audit(&lex, source);
  score += c_ref_unary_prefix_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_cast_unary_mega_full_deep_buf_audit_c. */
static int32_t c_ref_cast_unary_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_cast_unary_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_stmt_control_mega_full_deep_audit_c. */
static int32_t c_ref_stmt_control_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_return_stmt_full_deep_audit(&lex, source);
  score += c_ref_break_continue_audit(&lex, source, 1);
  score += c_ref_break_continue_audit(&lex, source, 0);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_stmt_control_mega_full_deep_buf_audit_c. */
static int32_t c_ref_stmt_control_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_return_stmt_full_deep_buf_audit(&lex, data, len);
  score += c_ref_loop_control_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_stmt_control_mega_full_deep_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_skip_let_mega_full_deep_audit_c. */
static int32_t c_ref_diag_skip_let_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_diag_skip_let_full_deep_audit(&lex, source);
  score += c_ref_body_skip_mega_full_deep_audit(&lex, source);
  score += c_ref_type_ref_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_skip_let_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_skip_let_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_diag_skip_let_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_onefunc_slice_mega_full_deep_audit_c. */
static int32_t c_ref_onefunc_slice_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_onefunc_slice_full_deep_audit(&lex, source);
  score += c_ref_onefunc_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_sig_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_onefunc_slice_mega_full_deep_buf_audit_c. */
static int32_t c_ref_onefunc_slice_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_onefunc_slice_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_else_mega_full_deep_audit_c. */
static int32_t c_ref_if_else_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_if_else_chain_full_deep_audit(&lex_at_if, source);
  score += c_ref_if_control_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_block_stmt_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_if_else_mega_full_deep_buf_audit_c. */
static int32_t c_ref_if_else_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_if_else_mega_full_deep_audit(&lex_at_if, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_sig_mega_full_deep_audit_c. */
static int32_t c_ref_fn_sig_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_sig_full_deep_audit(&lex, source);
  score += c_ref_async_fn_sig_full_deep_audit(&lex, source);
  score += c_ref_fn_control_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_sig_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_sig_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_sig_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_arms_mega_full_deep_audit_c. */
static int32_t c_ref_match_arms_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_arms_full_deep_audit(&lex, source);
  score += c_ref_match_mega_full_deep_audit(&lex, source);
  score += c_ref_block_stmt_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_arms_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_arms_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_arms_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_impl_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_impl_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_impl_full_deep_buf_audit(&lex, data, len);
  score += c_ref_trait_skip_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_preamble_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_preamble_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_preamble_full_deep_buf_audit(&lex, data, len);
  score += c_ref_collect_imports_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_parse_ultra_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_ultra_mega_full_deep_audit_c. */
static int32_t c_ref_expr_ultra_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_binop_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_stmt_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  score += c_ref_ternary_assign_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_ultra_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_ultra_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_ultra_mega_full_deep_audit_c. */
static int32_t c_ref_primary_ultra_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_expr_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_binop_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_ultra_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_ultra_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_ultra_mega_full_deep_audit_c. */
static int32_t c_ref_import_ultra_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_stmt_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_ultra_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_ultra_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_super_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_into_entry_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_into_ultra_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_ultra_mega_full_deep_audit_c. */
static int32_t c_ref_match_ultra_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_subject_mega_full_deep_audit(&lex, source);
  score += c_ref_match_arms_mega_full_deep_audit(&lex, source);
  score += c_ref_match_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_ultra_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_ultra_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_ultra_mega_full_deep_audit_c. */
static int32_t c_ref_struct_ultra_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_skip_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_layout_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_ultra_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_ultra_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_ultra_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_impl_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_trait_skip_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_enum_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_ultra_mega_full_deep_audit_c. */
static int32_t c_ref_fn_ultra_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_sig_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_control_mega_full_deep_audit(&lex, source);
  score += c_ref_onefunc_slice_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_ultra_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_ultra_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_ultra_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_ultra_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_if_else_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_stmt_control_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_ultra_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_if_else_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_stmt_control_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_loop_control_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_control_flow_ultra_mega_full_deep_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_ultra2_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_ultra2_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  int32_t score;
  struct parser_asm_lexer lex;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_parse_ultra_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_skip_let_mega_full_deep_buf_audit(&lex, data, len);
  score += parser_asm_stretch_diag_fn_mega_full_deep_buf_audit_c(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_ultra_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_ultra_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_mega_full_deep_audit(&lex, source);
  score += c_ref_diag_skip_let_mega_full_deep_audit(&lex, source);
  score += c_ref_block_stmt_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_ultra_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_ultra_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_ultra_mega_full_deep_audit_c. */
static int32_t c_ref_library_ultra_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_sig_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_ultra_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  score = c_ref_library_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_onefunc_buf_full_deep_audit(&lex, data, len);
  score += c_ref_library_ultra_mega_full_deep_audit(&lex, &sl);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_ultra_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_ultra_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_allow_mega_full_deep_audit(&lex, source);
  score += c_ref_balanced_delim_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_skip_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_ultra_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_ultra_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_ultra_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_super_mega_full_deep_audit_c. */
static int32_t c_ref_expr_super_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_ultra_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_ultra_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_ultra_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_super_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_super_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_super_mega_full_deep_audit_c. */
static int32_t c_ref_primary_super_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_ultra_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_ultra_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_super_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_super_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_super_mega_full_deep_audit_c. */
static int32_t c_ref_import_super_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_ultra_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_super_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_super_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_max_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_max_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_super_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_ultra_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_ultra2_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_super_mega_full_deep_audit_c. */
static int32_t c_ref_match_super_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_ultra_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_ultra_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_ultra_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_super_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_super_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_super_mega_full_deep_audit_c. */
static int32_t c_ref_struct_super_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_ultra_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_ultra_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_super_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_super_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_super_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_ultra_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_ultra_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_super_mega_full_deep_audit_c. */
static int32_t c_ref_fn_super_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_ultra_mega_full_deep_audit(&lex, source);
  score += c_ref_library_ultra_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_ultra_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_super_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_super_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_super_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_super_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_ultra_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_ultra_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_ultra_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_super_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_ultra_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_ultra_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_ultra_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_super_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  int32_t score;
  struct parser_asm_lexer lex;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_ultra2_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_super_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_super_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_super_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_ultra_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_ultra_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_ultra_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_super_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_super_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_super_mega_full_deep_audit_c. */
static int32_t c_ref_library_super_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_ultra_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_ultra_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_super_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_ultra_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_ultra_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_super_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_super_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_ultra_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_ultra_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_ultra_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_super_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_super_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_super_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_super_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_super_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_super_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_super_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_super_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_super_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_max_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_super_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_super_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_super_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_super_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_super_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_super_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_super_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_super_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_super_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_super_mega_full_deep_audit(&lex, source);
  score += c_ref_library_super_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_super_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_super_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_super_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_super_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_super_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_super_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_super_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  int32_t score;
  struct parser_asm_lexer lex;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_super_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_super_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_super_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_super_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_super_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_super_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_super_mega_full_deep_audit(&lex, source);
  score += c_ref_import_super_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_super_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_super_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_super_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_super_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_super_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  int32_t score;
  struct parser_asm_lexer lex;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  int32_t score;
  struct parser_asm_lexer lex;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  int32_t score;
  struct parser_asm_lexer lex;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  int32_t score;
  struct parser_asm_lexer lex;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  int32_t score;
  struct parser_asm_lexer lex;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  int32_t score;
  struct parser_asm_lexer lex;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  int32_t score;
  struct parser_asm_lexer lex;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  int32_t score;
  struct parser_asm_lexer lex;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  int32_t score;
  struct parser_asm_lexer lex;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  int32_t score;
  struct parser_asm_lexer lex;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  int32_t score;
  struct parser_asm_lexer lex;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  int32_t score;
  struct parser_asm_lexer lex;
  (void)lex_inout;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  int32_t score;
  struct parser_asm_lexer lex;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  int32_t score;
  struct parser_asm_lexer lex;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  int32_t score;
  struct parser_asm_lexer lex;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  int32_t score;
  struct parser_asm_lexer lex;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  int32_t score;
  struct parser_asm_lexer lex;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  int32_t score;
  struct parser_asm_lexer lex;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  int32_t score;
  struct parser_asm_lexer lex;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  int32_t score;
  struct parser_asm_lexer lex;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  int32_t score;
  struct parser_asm_lexer lex;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_control_flow_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_control_flow_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_body_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_match_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  int32_t score;
  struct parser_asm_lexer lex;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_expr_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_expr_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_primary_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_expr_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_expr_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_expr_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_primary_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_primary_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_cast_unary_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_primary_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_primary_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_primary_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_import_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_import_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_collect_imports_mega_full_deep_audit(&lex, source);
  score += c_ref_skip_imports_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_import_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_import_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_import_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_parse_into_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_parse_into_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_parse_into_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_diag_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_match_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_match_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_control_flow_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_match_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_match_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_match_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_struct_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_struct_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_try_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_parse_struct_layout_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_struct_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_struct_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_struct_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_trait_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_trait_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_trait_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_struct_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_impl_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_fn_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_fn_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_control_flow_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_control_flow_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex_at_if;
  if (!lex_inout || !source)
    return 0;
  lex_at_if = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_control_flow_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_body_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  score += c_ref_match_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex_at_if, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_diag_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_diag_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  (void)lex_inout;
  int32_t score;
  struct parser_asm_lexer lex;

  if (!data || len <= 0)
    return 0;
  lex = parser_asm_lexer_init_c();
  score = c_ref_diag_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_import_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_parse_preamble_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_body_skip_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_body_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_expr_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_body_skip_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_body_skip_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_body_skip_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_library_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_library_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_fn_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_import_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_library_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_library_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  if (!data || len <= 0)
    return 0;
  score = c_ref_library_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_fn_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(&lex, data, len);
  score += c_ref_extern_skip_mega_full_deep_buf_audit(&lex, data, len);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_try_skip_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_try_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_struct_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_try_skip_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit_c. */
static int32_t c_ref_try_skip_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_buf_audit(void *lex_inout, uint8_t *data, int32_t len) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !data || len <= 0)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  struct parser_asm_slice_u8 sl;

  if (!data || len <= 0)
    return 0;
  sl.data = data;
  sl.length = (size_t)len;
  return c_ref_try_skip_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, &sl);

}

/* Reference twin — verbatim copy of the gated C authority for parser_asm_stretch_fn_multiversal_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit_c. */
static int32_t c_ref_fn_multiversal_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(void *lex_inout, void *source) {

  struct parser_asm_lexer lex;
  if (!lex_inout || !source)
    return 0;
  lex = *(struct parser_asm_lexer *)lex_inout;
  int32_t score;

  score = c_ref_fn_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_library_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  score += c_ref_body_skip_intergalactic_galactic_celestial_divine_imperial_sovereign_omnipotent_universal_cosmic_eternal_infinite_transcendent_absolute_ultimate_supreme_crown_pinnacle_zenith_peak_summit_apex_max_ultra_hyper_mega_full_deep_audit(&lex, source);
  return score > 0 ? 1 : 0;

}
