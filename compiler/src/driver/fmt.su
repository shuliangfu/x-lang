// fmt.su — shu fmt 子命令（尚未实现）
// 符号名 driver_cmd_fmt：源码标识符 cmd_fmt + 模块前缀 driver_（与 main.su extern 一致）

import ast;

/** 未实现：返回 1，shell 可见非零退出码。 */
function cmd_fmt(argc: i32, argv: *u8): i32 {
  let _a: i32 = argc;
  let _v: *u8 = argv;
  return 1;
}
