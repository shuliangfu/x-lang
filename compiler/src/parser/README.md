# parser/ — 语法分析

将 **Token 流** 转为 **AST（抽象语法树）**。

- **输入**：lexer 产出的 Token 流。
- **输出**：AST（见 `ast/` 目录定义）。
- **职责**：递归下降或等价方式，按 .sx 语法规则建树；错误可报「期望 xxx」「未闭合括号」等。

流水线位置：lexer 之后，产出交给 ast/ 与 typeck/。
