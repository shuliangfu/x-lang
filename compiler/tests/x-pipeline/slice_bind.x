// Minimal: assign array to i32[] slice.

/**
 * Bind fixed array a to slice s; return 0 if bind succeeds.
 */
function main(): i32 {
  let a: i32[4] = [10, 20, 30, 40];
  let s: i32[] = a;
  return 0;
}
