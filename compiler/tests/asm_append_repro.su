// asm_append_repro.su — 最小复现：跨模块调用 append_asm_line 的 return 类型解析
import asm.types;
import codegen;

function f(out: *CodegenOutBuf): i32 {
  let nl: [1]u8 = [10];
  return append_asm_line(out, &nl[0], 1);
}
