const csv = import("std.csv");

function main(): i32 {
  let line: [8]u8 = [97, 98, 0, 0, 0, 0, 0, 0];
  let buf: [64]u8 = [];
  let n: i32 = csv.escape(&line[0], 2, &buf[0], 64);
  if (n != 4) {
    return 100 + n;
  }
  return buf[1];
}
