---
description: 修改代码前的上帝视角分析规则——防止修好这里破坏那里，必须从根源系统性修复。永恒生效。
alwaysApply: true
---

# 修改前全面分析规则（永恒记忆）

## 核心原则：修改前必须全面分析，避免修好这里破坏那里

### 一、修改前的上帝视角分析要求

**每次准备修改代码之前，必须先做到：**

1. **完整理解整个项目架构**：
   - 双编译器架构（xlang vs xlang-c）的职责边界与交互
   - 种子文件体系（bootstrap_xlangc、asm_backend_partial.o）的作用与 Darwin 限制
   - 符号链入机制（runtime_link_abi.c on-demand 检测 + 无条件链入逻辑）
   - codegen_dep_module_skip_emit 的跳过模块列表及对应预编 .o

2. **分析所有受影响的路径**：
   - 本次修改影响的直接文件 + import 依赖传递链路
   - 符号命名一致性（core_types_*、std_ 前缀规则、weak/strong）
   - .x → .o → C 代码生成的完整链路

3. **建立全局问题清单**：
   - 当前所有已知问题的状态与因果关系
   - 修复优先级与暂缓项

### 二、修改时的防破坏策略（禁止逐点修 bug，必须从根源系统性修复）

1. **符号命名一致性检查**：任何函数导出名/符号前缀修改后，检查所有引用处
2. **needs_* 体系同步检查**：新增/修改模块时，确保检测与链接逻辑同步更新
3. **全量回归**：修改后必须运行 CI=1 ./tests/run-all.sh，关注失败数趋势

### 三、已知全局约束（不可违反）

1. Darwin 上 asm_backend_partial.o 仅有 14 个 weak 符号 → -backend asm 产出 code_len=0
2. xlang-c 不支持 -backend asm（架构设计约束）
3. core.types 必须 skip emit → 否则重复强符号
4. std .o 构建后端选择是核心链路
5. 轻量级约束：不引入巨型依赖，按需链接

### 四、操作规范

1. 修改前 → 写分析文档，列出所有影响点
2. 修改时 → 按逻辑波次提交（相关文件可同 commit；不再强制一文件一 commit）
3. 修改后 → 全量回归测试，确认失败数未增加
