# Shux 自举 / bstrict 问题分析文档（2026-07-15 续）

> **分支**：`self-hosting`  
> **金标准**：Ubuntu x86_64  
> **最新 HEAD**：见 `git log -1`

## 已绿

| 项 | 状态 |
|----|------|
| 产品矩阵 return-value / option / stdlib-import / hello | **绿**（rv=42, opt=102, si=0, hello=0） |
| process.getcwd 用户 -o | **绿** |
| `make bootstrap-driver-bstrict` | **绿** |
| process.o 重建 | **绿**（args thin seed + OS glue，不走 -E） |
| string.o 重建 | **绿**（`-o` 完整 C + Arena64 对齐） |

## 本轮根因与修复

### C（原 getcwd 符号冲突）→ 扩大为 std 库模块构建链

1. **entry `-x -E` 截断**（4KiB 对齐、半个函数体）  
   - 现象：process/string/time 等库模块 `-E` 输出不完整。  
   - 对策：`shux_compile_std_module.sh` **优先 `-o` + 直接 .o**；失败再 KEEP_C / -E。  
   - process 特例：`runtime_process_args_thin.from_x.c` + glue（不依赖 -E）。

2. **libc 裸名 / Arena64 双标签**  
   - Arena64：`#define heap_libc_Arena64 std_heap_libc_LibcArena64`。  
   - process 不再进 -E，避免 getcwd 与 unistd 冲突。

3. **脚本改动触发全量 std 重建**  
   - Makefile：compile 脚本改为 **order-only** 依赖。

4. **heap 别名**  
   - 始终补 `std_heap_libc_heap_alloc_c` → `heap_alloc_c`（http.o 需要）。

## 当前状态（2026-07-15 夜 · bstrict 推进）

**Ubuntu 金标 SHA**：见 `git log -1`（约 `5cdddcf6`）

| 门闩 | 状态 |
|------|------|
| 产品矩阵 | 绿 |
| `run-typeck.sh` | **绿** |
| `tests/types/overload.x` | **绿**（call_resolve + x+k inline 用 resolved index） |
| `run-all-bstrict` | **红**：`run-parser.sh` multi-error recovery（`check` 无诊断） |

### 本波已修

1. typeck lit_lhs / isize / const·let 上下文 / type alias active module + coerce  
2. call_resolve 接 glue overload picker  
3. asm `try_inline_x_plus_k` 优先 `call_resolved_func_index`（勿按名 first-match）  
4. run-typeck breadcrumb：勿用假绿 `check` import 路径  

### 当前阻塞：parser multi-error + `check` 诊断

```
$SHUX check tests/parser/multi_error_recovery.x
# 期望：expected ';' after let + expected '(' after 'if' + aborting due to
# 实际：空输出 exit 0，或仅 CHK001 / parse produced no functions
```

根因在 **check 子命令 / parser 同步恢复诊断路径**，不是 typeck 负例。  
须从 `driver_cmd_check` + parser recovery 发射点修，禁止 BEST_EFFORT 糊绿。

### 纪律

全量 `run-all-bstrict` 绿之前 **禁止下一步自举**。


## 尚未根治（记录）

| 项 | 说明 |
|----|------|
| `-x -E` 库模块截断 | 仍复现；产品 std 构建已绕过，须从 CodegenOutBuf/emit 环修 |
| encoding/http 等完整冷重建 | 依赖 dep struct 完整 emit（StackStr/HeapTraceStats）；现用既有 .o + order-only 避免误重建 |
| typeck u64→usize | **当前门闩** |

## 纪律

全量 `run-all-bstrict` 绿之前 **禁止进入下一步自举**。
