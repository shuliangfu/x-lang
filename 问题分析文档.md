# Shux 自举 / bstrict 问题分析文档（2026-07-15 续）

> **分支**：`self-hosting`  
> **金标准**：Ubuntu x86_64  
> **最新 HEAD**：见 `git log -1`

## 已绿

| 项 | 状态 |
|----|------|
| 产品矩阵 return-value / option / stdlib-import / hello | **绿**（rv=42, opt=102, si=0, hello=0） |
| process.getcwd 用户 -o | **绿** |
| `make bootstrap-driver-bstrict` | **绿** |
| process.o 重建 | **绿**（args thin seed + OS glue，不走 -E） |
| string.o 重建 | **绿**（`-o` 完整 C + Arena64 对齐） |

## 本轮根因与修复

### C（原 getcwd 符号冲突）→ 扩大为 std 库模块构建链

1. **entry `-x -E` 截断**（4KiB 对齐、半个函数体）  
   - 现象：process/string/time 等库模块 `-E` 输出不完整。  
   - 对策：`shux_compile_std_module.sh` **优先 `-o` + 直接 .o**；失败再 KEEP_C / -E。  
   - process 特例：`runtime_process_args_thin.from_x.c` + glue（不依赖 -E）。

2. **libc 裸名 / Arena64 双标签**  
   - Arena64：`#define heap_libc_Arena64 std_heap_libc_LibcArena64`。  
   - process 不再进 -E，避免 getcwd 与 unistd 冲突。

3. **脚本改动触发全量 std 重建**  
   - Makefile：compile 脚本改为 **order-only** 依赖。

4. **heap 别名**  
   - 始终补 `std_heap_libc_heap_alloc_c` → `heap_alloc_c`（http.o 需要）。

## 当前状态（2026-07-15 续 · typeck 波）

**SHA**：`c6c76038` 一带（见 `git log -1`）

| 门闩 | 状态 |
|------|------|
| 产品矩阵 rv/option/si/hello | 绿 |
| `run-typeck.sh` 全量 | **绿**（含 lit_lhs / type_alias / const isize） |
| `run-all-bstrict` | **红**：`tests/types/overload.x` run exit=2 |

### 本波已修（根源）

1. **lit_lhs 假绿**：`typeck_check_expr_binop_arith` 字面量在左取非 lit 侧类型（对齐 typeck.x）
2. **isize/u16 lit**：`typeck_coerce_init_lit_to_decl` 补 isize 与 i16/u16 named
3. **const/let 上下文**：`one_const`/`one_let` 用声明类型作 check 上下文 + coerce
4. **type alias 假红**：`pipeline_typeck_parsed_module_c` 未 `set_active_ctx` → alias 无法展开（ast_pool.c）
5. **type alias coerce**：`typeck_coerce_init_resolved_alias_to_decl` 回绑
6. run-typeck 面包屑：`check` 对 import 假绿 → 改用 compile 路径；诊断接受 `result.Result_i32`

### 当前阻塞：overload 分派

```
tests/types/overload.x
pick(20 as i64) 应走 pick_i64 → 22，实际 inlined 成 pick_i32 体（+1→21）→ exit 2
```

根因：seed `typeck_gen` **缺** typeck.x 的 W-heap-overload（`typeck_overload_arg_param_score` /
`find_func_return_type_in_module_overload`），同名首匹配导致 `resolved_func_index` 错。

**修法**：将 typeck.x 重载分派迁入 seed，并在 call 路径写 `call_resolved_func_index`（勿 BEST_EFFORT）。

### 纪律

全量 `run-all-bstrict` 绿之前 **禁止进入下一步自举**。


## 尚未根治（记录）

| 项 | 说明 |
|----|------|
| `-x -E` 库模块截断 | 仍复现；产品 std 构建已绕过，须从 CodegenOutBuf/emit 环修 |
| encoding/http 等完整冷重建 | 依赖 dep struct 完整 emit（StackStr/HeapTraceStats）；现用既有 .o + order-only 避免误重建 |
| typeck u64→usize | **当前门闩** |

## 纪律

全量 `run-all-bstrict` 绿之前 **禁止进入下一步自举**。
