# Shux 自举 / bstrict 问题分析文档（2026-07-15 续 VI）

> **分支**：`self-hosting`  
> **金标准**：Ubuntu x86_64  
> **纪律**：全量 `run-all-bstrict` 绿之前 **禁止进入下一步自举**

---

## 0. 本轮结论（2026-07-15 晚）

### 已根治并本地验证（mac L2，`-backend c` / g05）

| 项 | 状态 | 根因与修法 |
|----|------|-----------|
| `core_mem` duplicate | **绿** | co-emit 已提供 `core_mem_*` 时不链 `mem.o` |
| `run-asm-binop-var` CG002 | **绿** | bstrict 误用旧 `shux_asm.experimental` → 优先本波 `shux_asm` |
| struct 数组 lit | **绿** | `.arr = {e0,e1}` 非 `(T[]){…}` |
| string / heap / http | **绿** | 勿无条件硬链 `http.o`；co-emit 与 `.o` 冲突时 `.o` 权威 |
| base64 / json | **绿** | `pipeline_codegen_std_dep_link_only`：用户 import 跳过 std co-emit，链预编 `.o` |
| csv | **绿** | 去掉 no_mangle 裸符号；`mod.x` 自洽 next_field/unescape；库 `.o` 无 U |
| init_globals 写 heap_trace | **绿** | link_only dep 不写用户 TU 的 init_globals |

**产品矩阵（mac，本波 shux）**：rv=42 / hello OK / mem OK / string OK / base64 OK / json OK / csv OK

### 权威原则（本轮固化）

1. **有预编 `std/*.o` 的模块**：用户程序 **只链 `.o`，不 co-emit 函数体**（`pipeline_codegen_std_dep_link_only`）。
2. **库模块自编 `.o`**：入口 pin 同包时 **仍 emit**（否则 csv.x 体丢失）。
3. **invoke_cc**：`http`/`json`/`csv`/`net`/… **按生成 C 真实引用**按需链入；禁止无条件硬链。
4. **hello ≠ net.o**：未引用的 std 不得入链（产品 C 后端已验）。

### 0.1 未用 std 不链接 — 全面检查（2026-07-16）

#### 产品轨 C 后端（Darwin 默认 `-backend c`）— **已按需**

| 机制 | 位置 | 作用 |
|------|------|------|
| `use_line` 扫描 | `link_abi_generated_c_contains_substr_use_line` | 忽略 `extern`/`#define`/`struct`/`typedef`/placeholder，只认真实调用行 |
| 模块 need_* | `shux_invoke_cc_impl` ~3855+ | 各 std 仅 `need_X` 时 `push_existing` |
| net 针脚 | `net_api[]` 具体 API | **禁止**裸 `std_net_`（preamble 假阳性） |
| context | 仅 entry API | 不把 co-emit 体内的 `is_cancelled` 当 need |
| dead_strip / gc | Darwin `-Wl,-dead_strip`；Linux `--gc-sections` | 未引用 co-emit 体不拖依赖 |
| heap | 产品路径 `heap_o=NULL` + on_demand | 非恒链 |
| `ensure_std_net_o_auto_tls` | 仅 `SHUX_NET_TLS` 非空才 work | 默认不碰 net |

**实测（本波 `compiler/shux_asm`，mac）**：

| 程序 | 体积 | `std_net*` / heavy std 符号 | 生成 C 含 `std_net_` |
|------|------|------------------------------|---------------------|
| return-value | ~16K | 0 / 0 | — |
| hello | ~33K | 0 / 0 | **0** |
| mem | ~16K | 0 / 0 | — |
| net test | ~34K | **有** | 有 |

hello `main` 仅 `std_fmt_print_u8_ptr_i32`；print 体 co-emit 在用户 C，**不链 `net.o`/`json.o`/`http.o`/`context.o`**。

#### ASM 后端 residual（Linux 金标 / `-backend asm`）

| 路径 | 行为 |
|------|------|
| `shux_asm_ld_append_std_objs` | **仍 bulk-push** `labi_std_plan`（~58 步：string/http/json/csv/thread/… 只要 `.o` 存在就入 argv） |
| `shux_asm_ld_append_on_demand_user_objs` | net/set/map/heap 等 **按 U 符号**按需 |
| **net.o** | **不在 bulk plan**，只走 on_demand（`link_abi_user_o_needs_std_net`） |

→ ASM 上 hello **也不会链 net.o**，但仍可能 bulk 链入已存在的 http/json/csv 等（与「未用勿链」原则不符）。  
根治方向：bulk 仅留 io_stubs/panic 等最小 runtime；其余一律 on_demand（须扩 `labi_od_simple_group` / needs 表，再跑 Ubuntu asm 矩阵）。**未在本轮改**，避免 bstrict 半绿。

#### 仍注意

| 项 | 说明 |
|----|------|
| Ubuntu 全量 bstrict | 待 push + L2/L3 金标 |
| `run-all-bstrict` 冷构建 | 默认 bootstrap 会盖 L2；验产品用 `SHUX_BSTRICT_SKIP_BUILD=1` |
| Darwin 默认 `-o` | 走 C backend；asm 微测以 Linux x86_64 为准 |
| hello 生成 C 仍大 | preamble/co-emit 大量 `extern std_io_*`（~598）；**链接**已按需，体积/扫描成本另题 |
| ASM bulk std | 见上 residual |

### 0.2 run-set 红 → 绿（2026-07-16）

| 现象 | 根因 | 修法 |
|------|------|------|
| `set.o` OK，用户 `tests/set/main.x` cc 4 errors | `std.set` **未** `link_only`；`-o` co-emit 时同名重载 `slot/find/insert` 把 **Set_u64 体错绑到 Set_i32 符号** | `pipeline_codegen_std_dep_link_only` 加 `std.set`/`std.map`（**权威在 `runtime_link_abi.from_x.c`**，与 ast_pool 同形） |
| 链上缺 set.o | invoke_cc 无 need_set | `use_line("std_set_")` → push `std/set/set.o` + heap + core.mem + hash |
| typeck `heap_mem_set_c` soft | `ops.x` 传 `i32` 给 `mem_set(..., usize)` | `n as usize` |

**验证**：`bash tests/run-set.sh` → `set test OK`；hello 仍无 net/set 符号。

**注意**：`link_only` 有两份列表（ast_pool + runtime_link_abi）；产品 Darwin 胜出的是 **link_abi** 那份，改必须双改。

### 0.3 map.o 假绿 / redefinition（2026-07-16）

| 现象 | 根因 | 修法 |
|------|------|------|
| `map.o` redefinition `std_map_Map_i32_i32` | **preamble 全量定义** + map 入口 emit 完整体 = 双权威 | `rt_preamble` / `runtime.from_x` 改为 **仅 `struct std_map_Map_i32_i32;`** |
| 曾出现 3.5K 假 map.o（无 `std_map_*`，仅 io/ctx 桩） | `shux_compile_std_module` 优先 `-o` 成功即采用；坏 C 仍可能产出可链 .o | 修 preamble 后 `-o` 正常；**验收须 `nm \| grep std_map_empty`** |

**验证**：`make ../std/map/map.o` 后 `nm` 含 `std_map_*`；`tests/map/main.x -o` run=0。

### 0.4 run-queue（2026-07-16）

| 现象 | 根因 | 修法 |
|------|------|------|
| `Queue_u8` 调 `at_Queue_i32` | 与 set 同：co-emit 重载错绑 | `link_only` + `need_queue` 链 `queue.o` |
| 旧 `queue.o` 仅 marker | Makefile 只编 `queue.x` 非 `mod.x` | Makefile → `mod.x` + `queue.x` via `shux_compile_std_module` |

**验证**：`bash tests/run-queue.sh` → `queue test OK`。

### 0.5 run-vec（2026-07-16）

| 现象 | 根因 | 修法 |
|------|------|------|
| `no member named 'al'` | preamble 旧 3 字段 `Vec_i32` vs .x 4 字段（含 `al`） | preamble 仅 forward Vec；Allocator/Arena64 先完整定义 |
| `heap.Arena64` 非法 C 名 | mangle 后缀保留 `.` | `codegen_type_ref_to_suffix`：`'.'→'_'` |
| Allocator redefinition | preamble 与 heap layout 双发 | `abi_dup` skip `Allocator`/`Arena64` |
| `std.vec` link_only | 与 set/queue 同策略 | 已加入；烟测 `len_empty` 由 weak + 用户调用满足 |

**验证**：`run-vec` / `run-set` / `run-queue` / hello 绿。

### 0.6 run-heap（2026-07-16）

| 现象 | 根因 | 修法 |
|------|------|------|
| `Undefined _std_heap_alloc_size_zero` | heap 已 `link_only`；on-demand 只 nm 已入链 **.o**，C 后端源码直链时用户 .c 尚非 .o | invoke_cc：对生成 C **use_line 扫 `std_heap_*`** 亦触发 push heap.o |

**验证**：`run-heap` / vec / set / hello 绿。

### 0.7 run-fs（2026-07-16 已修）

| 现象 | 根因 | 修法 |
|------|------|------|
| `conflicting types for read/write/unlink` | posix bare libc extern vs unistd | `shux_sys_*` + 去 bare extern |
| typeck soft → 体未 emit | io isize/i32、stack escape | stubs/sync `as isize`；posix 内联 fill |
| `redefinition std_io_sync_Iovec` | preamble 完整 + io.sync co-emit | `abi_dup` skip `Iovec` |
| incomplete `struct std_fs_posix_Iovec` | **typedef 不建 tag**；codegen 发 `(struct std_fs_posix_Iovec){...}` | preamble `#define std_fs_posix_Iovec std_io_sync_Iovec`（FsIovecBuf 同） |
| incomplete `readv_writev_buf_FsIovecBuf` | STRUCT_LIT 用 entry 前缀 | compound lit 改写：`FsIovecBuf`→`std_fs_`，`Iovec`→`std_io_sync_` |

**验证**：`bash tests/run-fs.sh` → `fs test OK`。

### 0.8 run-path（2026-07-16 已修）

| 现象 | 根因 | 修法 |
|------|------|------|
| `duplicate symbol _std_path_clean`（path.o × user.o） | co-emit `std.path` 体 + needs 链 `path.o`（runtime_path_fast）双权威 | `link_only` 加 `std.path`（link_abi + ast_pool） |

**验证**：`bash tests/run-path.sh` → `path test OK`。

### 0.9 小脚本续跑（2026-07-16 现场）

| 脚本 | 状态 | 根因 / 修法 |
|------|------|-------------|
| path | OK | link_only `std.path` |
| map | OK | 烟测 gcc 补 `core/mem/mem.o` |
| error | OK | link_only `std.error` + need_error 扫 `std_error_` |
| compress | OK | 去掉错误 link_only；局部 `let s:*T` 字段 `->`；Z_OK 双定义；size_of 字面量 |
| thread | OK | `thread.o` 由 marker 改为 `mod.x` 编译 |
| **fmt** | **红** | overload 调用点一律绑 `to_buf_*_i32`（bool/u64/f64 全错） |

**Darwin 注意**：g05 relink 后须 `codesign -s - -f compiler/shux_asm`，否则 SIGKILL（Code Signature Invalid）。

**当前小脚本进度**：path/map/error/compress/thread/fmt **OK**；i64-ctfe/pool-limits **OK**；env/vector/slice **OK**（2026-07-16 续）。

### 0.10 run-env / run-vector / run-slice（2026-07-16 续）

| 脚本 | 根因 | 修法 |
|------|------|------|
| **env** | preamble weak `process_args_count_c` 恒 `return 0` 盖过 runtime_process_argv；Darwin 先赢 weak + dead_strip 掉 getter | 桩转发 `process_shux_argc_get` / `process_shux_argv_get` |
| **vector** | `need_string` 只认 `std_string_`，`vec_add_verify` 直接 `shux_string_memcmp_c` 不链 string.o | use_line 亦匹配 `shux_string_` |
| **slice** | ① Option 被标成 `core_slice_Option_*` incomplete；② 仅 preamble 有 Option_i32 布局；③ co-emit `s[i]` 无 `.data`；④ `unsafe{return}`/`return 0` 毒非 int 返回 | Option_*→`core_option_`；preamble 补 u8/u64/ptr_u8；slice 用 `.data`；subslice 改 let+return |

**验证**：`run-env` / `run-vector` / `run-slice` / rv=42 / hello OK（mac L2，`SHUX=./compiler/shux_asm`）。

### 0.11 run-backtrace（2026-07-16 续）

| 现象 | 根因 | 修法 |
|------|------|------|
| `Undefined _process_shux_argc_get/argv_get` | §0.10 env 修：preamble weak `process_args_*` 转发 `process_shux_*_get`；`need_backtrace` 在 Darwin 加 `-Wl,-export_dynamic` **保活** weak 桩，但未链 `runtime_process_argv.o`（hello 靠 dead_strip 丢桩故绿） | `runtime_link_abi`：生成 C 含 `process_shux_*_get` → `need_process_argv_glue`；**仅当未推 process.o** 时链 `runtime_process_argv.o`（process.o 已含 glue，禁双链） |

**验证**：`run-backtrace` / env / process / channel / rv=42 / hello OK；晚段 12 项回归绿。

### 0.12 run-stdlib-import / run-option（2026-07-16 续）

| 现象 | 根因 | 修法 |
|------|------|------|
| `redefinition of 'core_option_Option_u8/u64/ptr_u8'` | §0.10 slice 修在 preamble 补了完整 Option_u8/u64/ptr_u8；`abi_dup` 只 skip `Option_i32` → co-emit core.option 再 emit 同名完整 struct | `codegen_should_skip_emit_struct_layout_for_abi_dup`：凡裸名前缀 `Option_` 一律 skip（preamble 权威）；同步 `codegen.x` + `codegen_gen.c` + seed |

**验证**：`run-stdlib-import` / `run-option`（真 `-o`，非 check-only 假绿）/ slice / backtrace / env / rv=42 / hello OK。

### 0.13 假绿清扫 + defer 真绿（2026-07-16）

| 假绿 / 红 | 根因 | 修法 |
|-----------|------|------|
| `run-defer` 报 OK 但产品 `-o` 失败 | ① gate 编译失败仍 `exit 0`（`SHUX_DEFER_GATE_FAIL` 默认 0）；② **parser**：`defer {` 后未 `lex = r.next_lex`，`parse_block_into` 要的是 **lex_after_lbrace**（while/for 已正确，defer 漏了）→ P001 / XP003 | `parser.x` + `parser_gen.c` + seed 两处补 `lex = r.next_lex`；`run-defer-gate.sh` **默认硬失败**、产品 `SHUX`、四用例真 `-o`+exit |
| `run-option` check-only fallback | 编译失败但 check 过则 `exit 0` | 删除 fallback；产品 `$SHUX -backend c -o` 硬过 |
| `run-repr-c-layout` 软退 | `FAIL` 默认 0 | 默认 `FAIL=1`；无 SHUX 硬失败 |
| `type-borrow-conflict` 无 shux 仍 OK | SKIP 后 `exit 0` | 无 native shux 一律 `exit 1` |
| Darwin g05 链失败假绿风险 | `PREFER_X_O=1` 时空 `_RT_SEED_SLICE_OBJS`，no_c 对 `driver_x_emit_*`/`write_io_net_*` 仍为 **U** | `g05_relink_env.sh` **始终**链入 RT slice `.o` |

**验证（mac L2，`SHUX=./compiler/shux_asm`）**：

- defer 四用例 exit 42/21/111/30 全过
- option `-o` run exit 102；stdlib-import / hello / rv=42
- `SHUX_BSTRICT_SKIP_BUILD=1 bash tests/run-all-bstrict.sh` → **`run-all-bstrict OK (123 scripts)`** EXIT=0
- 日志中 **无** `defer-gate FAIL` / `check-only fallback`

**纪律**：全量 bstrict 未绿前 **禁止自举**；Ubuntu 金标待 push 后 L2/L3。仓库内大量 `run-f-*-gate` 等 **不在** bstrict 清单的软 `FAIL:-0` 未本轮全改；进 bstrict 树的软绿已清。

### 0.14 Ubuntu crypto.o 冷编假死（2026-07-16）

| 现象 | 根因 | 修法 |
|------|------|------|
| `shux_compile_std_module --bare-impl` 编 `std/crypto/*.x` 时 `mod_1.c:298 end of input` | ① 无 `-lib-name` 时脚本**一律**拒绝 asm 已产出的裸符号 `.o`，退 `-x -E`；② `-E` preamble 在 **20480** 字节处截断（无函数体） | **直接用已裸符号的 asm `.o`**（impl）；`mod.x` 若缺 `std_<mod>_` 前缀则 **objcopy 重命名**；需剥前缀时优先 `-backend c` + `SHUX_KEEP_C`；拒绝无 `}` 的截断 `-E` |
| `undefined reference to sha512` | 陈旧 `runtime_crypto_inc_glue.o`（仍 U `sha512`）未随 seed 重建 | 冷编重建 glue；现应对 `ed25519_ref10_sha512` |
| 链接缺 `std_crypto_mem_eq` | mod.x 裸 `mem_eq` 未加 `std_crypto_` | 上表 objcopy 前缀 |

**验证（Ubuntu L2 冷 `shux_asm`，非 pin）**：`make`/`shux_compile_std_module` → `crypto.o` 含 `std_crypto_mem_eq` + `crypto_mem_eq_c`；`shux_asm -backend c -o` crypto/rv/option/hello 绿；`run-crypto.sh` OK。

**仍待**：`-E` 20KiB 截断根因（codegen/out_buf 写路径）；`bootstrap-driver-bstrict` experimental 链缺 `rt_parse_diag` 时须用 `SHUX_BSTRICT_SKIP_BUILD=1` + 已有冷 `shux_asm`。

### 0.15 Ubuntu bstrict 宿主绑 pin 假红（2026-07-16）

| 假红点 | 根因 | 修法 |
|--------|------|------|
| `run-typeck` 空 got | bstrict 见脚本含 `-o` 就改 `SHUX=shux-c`（pin） | `run-typeck/check/lexer` 保留产品 `shux` |
| `run-check` fmt CHK001 | `SHUX_RUN_ALL_BOOTSTRAP_SHUX` 强绑 pin shux-c；fmt-wrap 又用 `RUN_SHUX` wrap | 取消强绑；fmt/check 用真实二进制 |
| types gate link 失败 | `SHUX_LINK_SHUX=shux-c` + `FORCE_BACKEND=c` → wrap pin | types gate `-o` 优先产品 `SHUX -backend c` |
| slice 假路径 | bstrict 清空 backend 后拼截断 `-E` | 保留 `-backend c`，删 splice 假绿 |
| `run-vector` 静默红 | bstrict x86_64 强绑 pin `shux-c` | 产品 `shux_asm -backend c`；失败打印 build 日志 |

**bstrict14 进度（Ubuntu 冷，`SHUX_BSTRICT_SKIP_BUILD=1`）**：过 vector + asm 微测段；全量 123 仍跑中。

### 0.16 base64.o 缺 `base64_stream_enc_init_c`（run-core-types 红，2026-07-16）

| 现象 | 表层 | 修法（产品面，已做） |
|------|------|----------------------|
| `implicit declaration of 'base64_stream_enc_init_c'` | 生成 C 无定义、调用裸名 | `base64.x` enc/dec_init：**unsafe 后一次 `let s = state as *T`**（`db5d80b8`） |

**验证**：Ubuntu `base64.o` 含 `base64_stream_enc_init_c`/`dec_init_c`；`run-core-types` OK。  
**bstrict24** 止于本条；**bstrict25**（同 SHA）续跑中。

> ⚠️ **禁止**据此批量改 std 里所有 `s = p as *T`：见 §0.17 爆炸半径。std 一次绑定是**合法等价写法**，不是 typeck 根治。

---

### 0.17 上帝视角问题地图：unsafe 后赋值 cast（2026-07-16）

#### 0.17.1 完整数据流（谁产生 / 谁存 / 谁消费）

```
std/*.x 源
  → parser（函数体 / unsafe region / let / assign）
  → typeck check_block（stmt_order：let → region(unsafe push/pop) → expr_stmt assign）
       ├ pipeline_typeck_check_block_one_region_c  push/pop g_typeck_unsafe_depth
       ├ typeck_check_expr_assign → check 左 VAR / 右 EXPR_AS
       └ 失败时：函数返回类型塌成 `?` 或体被静默丢弃
  → codegen emit_func
       ├ 坏函数：体不发射；部分 let 出现在 top-level init_globals（假绿）
       └ 调用点找不到同 TU 符号 → 裸名 → cc implicit declaration
  → shux_compile_std_module（-backend c + KEEP_C + cc -c + ld -r）
  → std/base64/base64.o（缺符号）→ run-core-types / 用户 -o 链入
```

**权威落点**（双权威禁令）：

| 层 | 权威文件 | 改动须同 commit 验收 |
|----|----------|----------------------|
| typeck 逻辑 | `typeck.x` + `seeds/typeck_gen.linux.x86_64.c` | Ubuntu 重建 `typeck_x.o` + g05 |
| unsafe depth / region | `pipeline_glue.c`（`pipeline_typeck_check_block_one_region_c` / push/pop） | 动 glue → Ubuntu 重建 `pipeline_x.o` |
| codegen 静默丢体 | `codegen.x` + `seeds/codegen_gen.linux.x86_64.c` | 与 typeck 同波矩阵 |
| std 写法 | `std/**/*.x` | 仅等价重写，不顶 typeck 债 |

#### 0.17.2 最小复现矩阵（产品 `shux_asm -check`，Ubuntu 已测）

| ID | 模式 | 结果 |
|----|------|------|
| gA | `let s=0` → `unsafe{memset}` → **`s = p as *T`（块外）** | **红**（main 见返回 `?`） |
| gB | 同上但 **assign 写在 unsafe 内** | 绿 |
| gC | `unsafe { s = calloc(...) as *T }` | 绿 |
| gD | `unsafe{memset}` → **`let s = p as *T`（一次绑定）** | 绿 |
| gE | gA 但 `*u8` | **红**（非仅 struct） |
| gF | gD + 字段写 `s.magic=` | 绿 |
| gG | 无 unsafe，仅 `s = p as *T` | 绿 |
| gH | **空** `unsafe{}` 后 `s = p as *T` | **红**（与 memset 无关） |
| gMulti | 坏函数 + 好函数；main **不调用**坏函数 | **假绿**：typeck OK，坏函数体消失，`s` 进 `init_globals` |

**精确触发条件**（当前证据）：

> 函数体内 **任意** `unsafe { ... }` region **关闭之后**，再对已绑定局部做 **`s = <expr> as *T` 赋值** → typeck 失败 / 体静默丢失。  
> **同一赋值写在 unsafe 内** 或 **改用 `let s = ... as *T` 新绑定** → 正常。

#### 0.17.2b 解析层铁证（比 typeck 更底层，2026-07-16 补）

| 源形态 | `parse OK num_funcs` | 生成 C |
|--------|----------------------|--------|
| 无 unsafe：`let s=0; s=p as *T; return 0` + 另一函数 | **2** | 两函数体完整 |
| 空 `unsafe{}` 夹在 let 与 assign 之间 + 另一函数 | **1**（坏函数**未入 AST**） | 仅好函数；`s` 成 **top-level** `init_globals` |
| `unsafe{memset}` 后 assign + 另一函数 | **2** | 坏函数体仍丢；`s` 仍可能进 init_globals；typeck 可假绿 |

结论（更新产生点排序）：

1. **parser（第一嫌疑）**：`unsafe { }` region 关闭后，函数体后续 stmt 解析/挂接失败 → 函数未登记或体残缺，局部 let 被抬成 module top-level。  
2. **typeck（第二）**：对残缺/半挂接 AST 报 `?` 或整模块假绿（无调用点时）。  
3. **codegen（第三）**：忠实发射残缺 AST（init_globals + 缺符号），**不是**首恶。

根修必须先查 **parser 对 function-body 内 unsafe region 的 stmt_order / 括号恢复**，再修 typeck 假绿；禁止只在 std 或 codegen 打补丁。

#### 0.17.3 耦合面与爆炸半径（禁止乱修）

| 错误修法 | 为何禁止 | 会破坏什么 |
|----------|----------|------------|
| 批量改 std 49+ 处 null→assign | 多数 **IN_UNSAFE 已绿**；与根因无关 | 噪声 diff、回归面巨大、掩盖 typeck 债 |
| 只改 base64 当结案 | 根因仍在 typeck；dns/task/kv… 同模式可再爆 | 下一 std 模块冷编再红 |
| typeck 对 assign 失败 soft-skip | 扩大假绿 | 更残缺的 `.o` 进链 |
| 只改 seed 不改 typeck.x（或反之） | 双权威漂移 | mac/Ubuntu / 自举后再崩 |
| 动 glue 不重建 `pipeline_x.o` | 旧 depth push/pop | 假绿或假红 |
| 关 LANG-007 unsafe 检查 | 安全边界回退 | deref/extern 无防护 |

**std 扫描（参考，非修改清单）**：

- null-init 后再 assign-cast：约 68 处  
- 其中 assign 在 unsafe **内**：约 25（当前矩阵 **绿**）  
- assign 在 unsafe **外/后**：约 49（**候选**触发；须按函数是否先出现 unsafe region 再筛）

#### 0.17.4 根治顺序（上帝视角，禁止跳步）

1. **闸门**：`run-all-bstrict` 全绿前禁止自举；产品轨 `SHUX=./compiler/shux_asm`。  
2. **探针固化**（先红后绿）：把 gA/gB/gD/gH/gMulti 收成 `tests/` 或 probes，**禁止**只靠 base64 当回归。  
3. **定位产生点**（尚未合入代码，按层）：  
   - **parser 优先**：function body 内 `unsafe` region 后的 stmt 是否仍挂在同一 body；空 region 是否吞掉整个 `export function`（gEmptyU num_funcs=1）；局部 let 为何进 `module.top_level_lets`。  
   - **typeck 次之**：残缺 AST 上 assign 是否把返回类型塌成 `?`；无调用点时为何仍 typeck OK（假绿）。  
   - **codegen 再次**：仅作忠实发射；init_globals 有 top-level let 时说明 AST 已脏。  
4. **单点根修**：先 parser（`parser.x` + `seeds/parser_gen.linux.x86_64.c` 同 commit），再视情况 typeck；**禁止**先改 codegen/std 顶锅。  
5. **验收矩阵（最小，防破坏别处）**：  
   - gA–gH 全绿；gEmptyU/gMulti **num_funcs 正确且体完整**（禁止假绿）；  
   - 既有 unsafe 内赋值（gB/gC）回归仍绿；  
   - rv=42 / option / hello / string / base64 / core-types；  
   - Ubuntu 全量 bstrict；  
   - **不**顺手改 link_abi bulk/on_demand（另图 §0.1）。  
6. **std**：仅对已证红的模块做一次绑定（base64 已做）；parser 根修后 gA 须不改 std 即绿，再考虑风格统一。

#### 0.17.5 与其它进行中债的边界（勿搅在一波）

| 债 | 层 | 与本条关系 |
|----|-----|------------|
| `-E` 20KiB 截断 | codegen/out_buf | **独立**；std 模块已走 `-o`+KEEP_C，勿用 -E 验库 |
| ASM bulk 硬链 std.o | link_abi | **独立**；未用勿链；本条不改 |
| pin 假绿 | bstrict 宿主 | 已收；验产品勿绑 shux-c |
| link_only init_globals heap_trace | codegen | 已修；勿与本条「坏函数 let 升顶」混淆 |

#### 0.17.6 当前状态

| 项 | 状态 |
|----|------|
| base64 产品绕过（一次绑定） | **已合入** `db5d80b8`，core-types 绿 |
| parser/typeck 根修（unsafe 后体） | **未做**（待 §0.17.4） |
| 假绿 gMulti | **未做**硬失败 |
| bstrict25 | 止于 **run-atomic**（见 §0.18）；此前 ~71 绿 |

---

### 0.18 atomic：overload 把 `*i64`/`*u32` 误派到 `*_i32`（bstrict25，2026-07-16）

#### 现象

```
std_atomic_store_i32_ptr_i32(&(y), 1000);   // y:i64 → 期望 store_i64
// expected int32_t * but argument is int64_t *
```

`load(&y)` 正确 → `load_i64_ptr`；`store`/`fetch_add` 的 **第二实参为整型字面量** 时误派 i32。

#### 数据流

```
tests/atomic/main.x
  atomic.store(&y, 1000)   // method call, binding import std.atomic
  → typeck_check_expr_method_call
  → pipeline_typeck_find_func_return_type_in_module_by_name_call_strict_minimal
  → pipeline_typeck_overload_arg_score_strict_minimal（权威分）
  → resolved_func_index → codegen mangle std_atomic_store_* 
```

#### 根因（评分，非测试写错）

| 候选 | arg0 `*i64` | arg1 lit `1000`(默认 i32) | 总分（修前） |
|------|-------------|---------------------------|--------------|
| `store(*i64,i64)` | 精确 1000 | lit→i64 **−1** → 整候选淘汰 | — |
| `store(*i32,i32)` | 若 PTR 弱匹配… 或回退 **首同名 i32** | 精确 1000 | 误胜 |

修前 glue 已对 `*T` **elem 不一致给 −1**（sort 债），但 lit 无法匹配 i64/u32 → **全灭回退首同名 i32**。

#### 修法（单点权威）

| 文件 | 改动 |
|------|------|
| `seeds/pipeline_glue_strict_minimal.from_x.c` | `EXPR_LIT` 对整数 TypeKind（I32/U8/U32/U64/I64/USIZE/ISIZE）弱分 **100** |
| `typeck.x` 的 `typeck_overload_arg_param_score` | 同 lit 策略 + **PTR elem 硬匹配**（与 glue 对齐，消双权威） |

**禁止**：改 `tests/atomic` 去掉 i64；禁止只改 mangle 字符串。

#### 爆炸半径与回归

| 必测 | 期望 |
|------|------|
| `tests/types/overload.x` | `pick(10)`→i32，`pick(20 as i64)`→i64 仍绿 |
| `tests/atomic` | store/fetch_add 对 i32/u32/i64 生成正确 mangle |
| sort / fmt print 重载 | 不回归（PTR elem 规则保持硬匹配） |
| Ubuntu 重建 | `pipeline_glue_strict_minimal.o` + g05 relink `shux_asm`（typeck.x 变则 typeck_x.o） |

### 验证

```bash
# Darwin：g05 须链上 RT slice（见 g05_relink_env）
FULL=0 bash compiler/scripts/g05_prepare_and_relink.sh
codesign -s - -f compiler/shux_asm   # Darwin
export SHUX=./compiler/shux_asm
$SHUX -backend c -o /tmp/rv tests/return-value/main.x && /tmp/rv   # 42
$SHUX -backend c -o /tmp/d tests/defer/main.x && /tmp/d            # 42
bash tests/run-defer.sh && bash tests/run-option.sh
SHUX_BSTRICT_SKIP_BUILD=1 bash tests/run-all-bstrict.sh
```

---

## 1. 核心阻塞 A：`core_mem_placeholder`（已修）

### 1.1 现象

```
Undefined symbols: _core_mem_placeholder
ld: symbol(s) not found
```

`tests/mem/main.x` 经 `import("core.mem")` 调用 `mem.placeholder()` 等；链接期望 `core_mem_*`。

### 1.2 错误修复路径（已证伪）

| 尝试 | 为何无效 |
|------|----------|
| 仅在 `rt_run_compiler_parsed.from_x.c` 设 `current_codegen_prefix` | 产品 `-x -E` **不走** `driver_run_compiler_parsed`，走 `driver_run_x_emit_c` |
| 假定 `driver_c_frontend_smoke` 做 codegen | `SHUX_NO_C_FRONTEND` 下 smoke 被 `#if` 裁掉；且 smoke 本身不做 codegen |
| 入口 inherit `current_codegen_prefix` | pipeline **先** codegen 各 dep，会把 `current_*` 写成最后 dep 前缀，入口被污染 |

### 1.3 真正数据流（权威）

```
main.x (-x -E) 
  → driver_run_x_emit_c          # seeds/runtime.from_x.c
  → pctx_e 钉 entry 前缀
  → shux_pipeline_run_x_pipeline
  → codegen_x_ast(dep_i)         # 各 dep 写 current_codegen_prefix = dep 前缀
  → codegen_x_ast(entry, -1)     # 必须读 entry_module_import_path_*，不能读 current_*
  → emit_func / 同模块 CALL
```

库模块 `.o`：`shux_compile_std_module.sh` 优先 `-o`，失败回退 `-x -E`；符号面须与 import 调用端 `core_mem_*` 一致。

用户程序（`tests/mem/main.x`）：**co-emit** 会把 `core.mem` 函数体写进用户 C；若 dep 侧 skip 掉 `placeholder`，用户 `.o` 缺该符号且 on-demand 因其余 `core_mem_*` 已定义而不链 `mem.o` → 仅 `placeholder` undefined。

### 1.4 根因列表（已修）

1. **`-x -E` 未设入口前缀**  
   修：`driver_run_x_emit_c` 与 `driver_run_compiler_parsed` 均写：
   - `current_codegen_prefix_mirror/len`
   - **`entry_module_import_path_mirror/len`**（钉死，dep 不可覆写）

2. **`codegen_x_ast(entry)` 清空/误用前缀**  
   修：`dep_index < 0` 时用 `entry_module_import_path_*` 填 `prefix_buf`。

3. **同模块 CALL 只认 dep 池路径**  
   修：`codegen_emit_prefix_len_from_ctx` + 同模块 CALL 路径统一走该函数；入口优先 entry pin。

4. **`placeholder` 全局 skip**  
   修：`prefix` 以 `core_mem_` 开头时 **不** skip 裸名 `placeholder` / `string_new`（仍 skip `core_types_` / `std_string_` 的弱桩场景）。

5. **局部类型 `struct String` 无前缀**  
   修：user struct 的 `emit_type` 入口也走 `codegen_emit_prefix_len_from_ctx`（入口读 pin）。

### 1.5 修改文件

| 文件 | 作用 |
|------|------|
| `compiler/seeds/runtime.from_x.c` | `driver_run_x_emit_c` + `driver_run_compiler_parsed` 钉入口前缀 |
| `compiler/seeds/rt_run_compiler_parsed.from_x.c` | 与 mega 对称（FROM_X 切片时） |
| `compiler/src/codegen/codegen.x` | entry pin、prefix_from_ctx、同模块 CALL、placeholder skip、user struct 前缀 |
| `compiler/codegen_gen.c` | 本地 pin 与 `.x` 对齐 |
| `compiler/seeds/codegen_gen.linux.x86_64.c` | Ubuntu seed 对齐 |

### 1.6 验证命令

```bash
# 1) 前缀 -E
cd compiler
./shux -x -E -L .. ../core/mem/mod.x 2>/dev/null | grep -E 'core_mem_(placeholder|mem_copy|mem_set)'

# 2) 建 mem.o
rm -f ../core/mem/mem.o
bash scripts/shux_compile_std_module.sh ../core/mem/mem.o ../core/mem/mod.x
nm ../core/mem/mem.o | grep core_mem_placeholder

# 3) mem 测试
cd ..
bash tests/run-mem.sh

# 4) 产品矩阵（本机 arm64 默认 asm 可能 CG002，用 -backend c）
./compiler/shux -backend c -o /tmp/rv tests/return-value/main.x && /tmp/rv   # 42
./compiler/shux -backend c -o /tmp/opt tests/option/main.x && /tmp/opt       # 102
./compiler/shux -backend c -o /tmp/si tests/stdlib-import/main.x && /tmp/si  # 0
```

---

## 2. 阻塞 B：`std/string/string.o` 重定义（未修）

### 现象

```
error: redefinition of 'std_string_String'
```

同一生成 C 中完整定义出现两次（约 L202 单行体 + L946 多行体）。`pipeline_codegen_struct_tag_try_claim` 未挡住第二条路径。

### 排查方向

1. `codegen_emit_skipped_dep_type_definitions` 与 entry `codegen_emit_module_struct_definitions` 是否对同一 mangled tag 都 emit 完整体。  
2. claim 键是否 prefix 不一致（空 vs `std_string_`）。  
3. 修点：单一权威路径 emit 完整 struct；第二次只允许 forward 或 skip。

### 验证

```bash
cd compiler
bash scripts/shux_compile_std_module.sh ../std/string/string.o ../std/string/mod.x
# 期望：OK，nm 有 std_string_* 符号
```

---

## 3. 阻塞 C：hello `std_io_print_u8`（未修）

### 现象

```
error: call to undeclared function 'std_io_print_u8'
```

定义端为重载 mangle：`std_io_print_u8_ptr_usize`；`std.fmt` 内 `io.print(ptr, len)` 生成未完整 mangle 的 `std_io_print_u8`。

### 数据流

```
std.fmt: io.print(ptr, len as usize)
  → import field + CALL
  → 应解析到 std.io 的 print(*u8, usize)
  → 定义: std_io_print_u8_ptr_usize
  → 实际调用: std_io_print_u8   # 缺 overload 后缀
```

### 验证

```bash
./compiler/shux -backend c -E -L . examples/hello.x 2>/dev/null \
  | grep -E 'std_io_print_u8'
# 期望调用与定义同为 std_io_print_u8_ptr_usize
bash tests/run-hello.sh
```

---

## 4. 阻塞 D：默认 asm `-o` / 冷 bstrict 构建

| 项 | 说明 |
|----|------|
| 默认 `want_asm_backend=1` | 无 `-backend c` 时 arm64 上易 CG002 empty text |
| `run-return-value.sh` 等 | 可能依赖 shux-c 或二次 `cc` 源；勿假绿 |
| `run-all-bstrict` 入口 | 默认 `make bootstrap-driver-bstrict`；改 `codegen.x` 后会 regen `driver_gen` 失败 |
| 缓解 | `touch driver_gen.c pipeline_gen.c ...`；`SHUX_BSTRICT_SKIP_BUILD=1` 用已链好的 `shux`/`shux_asm` |

### 全量冷验证（目标，当前未过）

```bash
# mac 提交后
git push origin self-hosting

# Ubuntu 金标准
ssh ubuntu-server
cd /home/shuliangfu/worker/shu/shux
git pull --ff-only origin self-hosting
cd compiler
cp -f seeds/codegen_gen.linux.x86_64.c codegen_gen.c
# 必要时 pin：touch driver_gen.c pipeline_gen.c ...
rm -f codegen_x.o src/runtime_driver_no_c.o
make codegen_x.o
make bootstrap-driver-seed   # L3
export SHUX="$(pwd)/shux"
cd ..
SHUX_COMPILE_STD_USE_C=1 bash tests/run-all-bstrict.sh 2>&1 | tee /tmp/bstrict_ubuntu.log
```

---

## 5. 建议修复顺序（P0 → 自举）

| 序 | 任务 | 完成标准 |
|----|------|----------|
| **P0a** | string struct 双 emit / claim | `string.o` 冷重建成功 |
| **P0b** | import field CALL overload mangle | `run-hello.sh` 绿 |
| **P0c** | ensure 其余 `std/*.o` 冷重建 | bstrict ensure 段不红 |
| **P1** | `SHUX_BSTRICT_SKIP_BUILD=0` 全量 `run-all-bstrict` | exit 0，无 BEST_EFFORT |
| **P2** | Ubuntu L3 冷启动 + bstrict | 金标准绿 |
| **其后** | 才允许讨论下一步自举 | — |

---

## 6. 纪律与同步

- 全量 bstrict 未绿 → **禁止**下一步自举  
- git commit **仅 macOS**；Ubuntu 只 `git pull` 验证  
- 改 `codegen.x` 必须 seed `codegen_gen.linux.x86_64.c` 同向  
- 禁止双权威：入口前缀只认 `entry_module_import_path_*` + `shux_entry_lib_name_from_path`  
- 禁止 BEST_EFFORT / SKIP_BUILD 当放行  
- 汇报须带：**构建级别**（L2/L3）、**SHUX 路径**、**矩阵结果**

---

## 7. 关键文件索引

| 文件 | 角色 |
|------|------|
| `compiler/seeds/runtime.from_x.c` | `driver_run_x_emit_c` / `driver_run_compiler_parsed` |
| `compiler/src/codegen/codegen.x` | 前缀、emit_func、同模块 CALL、struct |
| `compiler/seeds/codegen_gen.linux.x86_64.c` | Ubuntu codegen seed |
| `compiler/seeds/runtime_pipeline_abi.from_x.c` | `shux_entry_lib_name_from_path`（core/ + std/） |
| `compiler/scripts/shux_compile_std_module.sh` | std/core → .o |
| `compiler/src/runtime_link_abi.x` | on-demand `core_mem_*` 检测 |
| `tests/run-mem.sh` / `tests/run-all-bstrict.sh` | 门禁 |

---

*文档更新：2026-07-15 续 IV — core_mem 根因闭合；string 重定义 / hello overload / 全量 bstrict 仍为闸门。*
