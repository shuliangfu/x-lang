# 语法文档与实现对照

本文档对照 `docs/` 下语言参考与编译器实现（ast.h、token.h、parser、preprocess），标出已写全、遗漏与建议补充项。

## 一、总体结论

| 维度 | 状态 | 说明 |
|------|------|------|
| 关键字 | 写全 | 已含 packed、extern、向量 i32x16/u32x16 |
| 类型与复合类型 | 写全 | 02 缺 **packed 结构体** 说明 |
| 控制流 | 写全 | if/else、while/loop/for、match、defer、label/goto、return/panic 均有 |
| 表达式与运算符 | 写全 | 04 与 ast 一致；三元、赋值、调用、下标、字段、结构体/数组字面量均有 |
| 函数与模块 | 基本写全 | 05 缺 **extern 函数声明**（FFI） |
| 变量与常量 | 写全 | let/const、块与顺序、最终表达式 |
| 注释与分号 | 写全 | 三种注释、语句与分号规则已补 |
| 预处理 | **已单独成文** | [09-条件编译.md](./09-条件编译.md)：`#if` + `#[cfg(...)]`、选用原则、标准库模式 |

---

## 二、分项对照

### 2.1 关键字（01-关键字.md）

| 实现有（token.h） | 文档是否写出 |
|------------------|-------------|
| function, let, const, if, else, while, loop, for, break, continue, return, panic, defer, match, struct, enum, goto, trait, impl, self, _, import | ✅ 均已列出 |
| **packed** | ❌ 未写（用于无填充结构体） |
| **extern** | ❌ 未写（FFI 声明 C 函数） |
| 类型名 i32x4/i32x8/i32x16、u32x4/u32x8/u32x16 | ✅ 01、02 均已写全 |

### 2.2 类型定义（02-类型定义.md）

| 实现有（ast.h） | 文档是否写出 |
|----------------|-------------|
| 内建类型、指针 *T、数组 T[N]、切片 T[]、结构体、枚举、trait/impl | ✅ 均有 |
| **packed 结构体**（无填充布局） | ❌ 未写 |

### 2.3 控制流（03-控制流.md）

| 实现有 | 文档是否写出 |
|--------|-------------|
| if/else 表达式、while/loop/for、break/continue、match、defer、label/goto、return/panic | ✅ 均有 |

### 2.4 表达式与运算符（04-表达式与运算符.md）

| 实现有（ASTExprKind） | 文档是否写出 |
|----------------------|-------------|
| 字面量、二元/一元、三元、赋值、括号、调用、方法调用、字段访问、下标、结构体/数组字面量 | ✅ 均有 |
| 优先级表 | ✅ 已写 |

### 2.5 函数与模块（05-函数与模块.md）

| 实现有 | 文档是否写出 |
|--------|-------------|
| 自定义函数、main、泛型函数、import、编译与链接 | ✅ 均有 |
| **extern 函数**（无体、链接 C 符号） | ❌ 未写 |

### 2.6 预处理与条件编译（09-条件编译.md）

| 实现有 | 文档是否写出 |
|--------|-------------|
| `#if` / `#elseif` / `#else` / `#endif`（含 `target_os` 表达式） | ✅ 01 + 09 |
| `#[cfg(...)]` 顶层与函数内剪枝 | ✅ 09 |
| `-target` / `-D` / `SHUX_OS_*` 符号 | ✅ 09 + analysis/条件编译.md |
| `#if` vs `#[cfg]` 选用原则 | ✅ 09 §一 |

---

## 三、建议补充（已落实）

1. **01-关键字.md**：已在「声明与定义」中增加 `packed`、`extern`；类型名已补全 i32x16、u32x16。
2. **02-类型定义.md**：已在结构体小节增加 packed 结构体（无填充、对齐 1）说明。
3. **05-函数与模块.md**：已增加「extern 函数声明（FFI）」小节。
4. **预处理**：已在 01 末尾增加「预处理（条件编译）」小节；**09-条件编译.md** 统一 `#if` 与 `#[cfg]`；README 索引已含「预处理 / 条件编译」。

---

## 四、文档索引建议

- 若在 01 中增加「预处理」小节，则 README 索引 01 的概要可写为：「保留关键字、类型名、注释、语句与分号、预处理」。
- 若新建 08-预处理与条件编译.md，则 README 表格增加一行指向 08。
- **已落实**：独立文档 [09-条件编译.md](./09-条件编译.md)，README 已索引。

以上对照基于当前 ast.h / token.h / preprocess.c 与 docs/ 内容；实现或设计变更时请同步更新本文档与对应篇目。
