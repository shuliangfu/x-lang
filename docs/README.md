# Shu 语言文档

本目录为 Shu（.sx）语言的简明参考文档，按类型分篇，便于查阅。

## 文档索引

| 文档 | 内容概要 |
|------|----------|
| [01-关键字](./01-关键字.md) | 保留关键字、类型名、注释、预处理 |
| [02-类型定义](./02-类型定义.md) | 内建类型、结构体、枚举、trait、指针、数组、切片 |
| [03-控制流](./03-控制流.md) | if/else、while、loop、for、break、continue、match、defer、goto、return、panic |
| [04-表达式与运算符](./04-表达式与运算符.md) | 字面量、二元/一元运算、三元、赋值、优先级 |
| [05-函数与模块](./05-函数与模块.md) | 自定义函数定义、参数、返回类型、import、main、泛型 |
| [06-变量与常量](./06-变量与常量.md) | let、const 声明与作用域 |
| [07-内置与标准库](./07-内置与标准库.md) | core.* / std.* 模块、内建占位与常用 API |
| [08-语法规范](./08-语法规范.md) | 缩进、分号、块与 `}`、命名、空格等书写规范 |
| [09-条件编译](./09-条件编译.md) | `#if` 与 `#[cfg(...)]` 选用、`target_os` / `-target`、标准库模式 |
| [10-内核级特性](./10-内核级特性.md) | 内联汇编 `asm!`/`asm goto!`、volatile 指针、位域、内核属性、构建链、QEMU 测试套件（30 项） |

## 约定

- **文件后缀**：`.sx`
- **入口**：含 `main` 的模块为可执行程序；无 `main` 的模块可作为库被 `import`
- **风格**：表达式为主；语句后加分号（如 `const`/`let`/`return`/`goto` 等）

## 内核开发工具链

```sh
make kernel              # 跑全部 30 个 QEMU 内核测试
make kernel-build SX=tests/kernel/shuxos.sx  # 构建单个内核 ELF
make kernel-longmode    # x86_64 长模式 QEMU 启动
make kernel-ist          # IST 结构验证
```

依赖：`zig cc`（交叉编译）、`qemu-system-x86_64`（QEMU 测试）。详见 [10-内核级特性](./10-内核级特性.md)。

## 文档维护

- 本目录为**语言规范/参考**，与编译器实现（含阶段性子集）同步更新。
- 注释已与当前词法实现一致（三种：`//`、`/* */`、`#`）；若扩展注释形式或预处理规则，请同步修改 [01-关键字.md](./01-关键字.md) 与 lexer。
- 若某语法或标准库在实现上尚为占位或未实现，文档中会以「占位」「以 typeck 为准」等表述说明；大版本或阶段变更时建议做一次全面核对。
- 实现与文档的逐项对照见 [语法文档与实现对照.md](./语法文档与实现对照.md)，便于查漏与维护。
